defmodule <%= web_module %>.Auth.SudoHTML do
  @moduledoc """
  HTML templates for sudo/re-authentication mode.

  Single-purpose page: password field + confirm button, centered in a
  narrow column matching the login form width. Follows GitHub's sudo
  pattern per D-20 to D-23.

  Generated by `mix sigra.install`. Customize freely -- this module
  is owned by your application.
  """

  use <%= web_module %>, :html
  import <%= web_module %>.SigraAuthComponents

  def new(assigns) do
    ~H"""
    <.sigra_auth_page>
      <div class="sigra-auth-flow sigra-auth-stack sigra-auth-stack--6">
      <.header>
        Re-enter your password
        <:subtitle>For your security, please re-enter your password to continue.</:subtitle>
      </.header>

      <.form :let={f} for={@form} action={~p"/users/sudo"} method="post" class="sigra-auth-stack sigra-auth-stack--4">
        <input type="hidden" name="sudo[return_to]" value={@return_to} />
        <.input
          field={f[:password]}
          type="password"
          label="Password"
          autocomplete="current-password"
          autofocus
          required
        />
        <.sigra_auth_button class="sigra-auth-action sigra-auth-action--primary sigra-auth-action--block">Confirm password</.sigra_auth_button>
      </.form>

      <p class="sigra-auth-copy sigra-auth-copy--center">
        <.link navigate={@return_to || ~p"/"}>
          Go back
        </.link>
      </p>
      </div>
    </.sigra_auth_page>
    """
  end
end
