defmodule ExampleWeb.AdminUserShowLiveTest do
  use ExampleWeb.ConnCase, async: false

  @moduledoc """
  Wave 0 contract coverage for the Phase 28 admin user detail surface.
  """

  describe "Phase 28 admin user show contracts" do
    @tag :skip
    test "detail sections render in the locked Identity Status Sessions Security Identities Organizations Recent Audit Danger Zone order", _ctx do
      assert true
    end

    @tag :skip
    test "Revoke session and revoke all sessions keep the current scope and target identity visible in confirmations", _ctx do
      assert true
    end

    @tag :skip
    test "platform admins can pivot from a global user detail page into an organization-scoped view", _ctx do
      assert true
    end
  end
end
