defmodule ExampleWeb.AdminUserIndexLiveTest do
  use ExampleWeb.ConnCase, async: false

  @moduledoc """
  Wave 0 contract coverage for the Phase 28 admin user index surface.
  """

  describe "Phase 28 admin user index contracts" do
    @tag :skip
    test "search and pagination preserve URL state when operators open a user and return", _ctx do
      assert true
    end

    @tag :skip
    test "desktop rows and mobile cards share the same user query contract", _ctx do
      assert true
    end

    @tag :skip
    test "Open user remains the primary row action on the list surface", _ctx do
      assert true
    end
  end
end
