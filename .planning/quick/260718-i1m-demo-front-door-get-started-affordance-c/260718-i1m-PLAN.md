---
phase: quick-260718-i1m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/assets/js/admin_hooks.js
  - test/example/priv/static/assets/js/app.js
  - test/example/priv/static/assets/css/app.css
  - test/example/lib/example_web/controllers/page_html/home.html.heex
  - test/example/lib/example_web/controllers/page_controller.ex
  - test/example/lib/example_web/live/demo/credentials_live.ex
  - test/example/lib/example/demo/personas.ex
  - test/example/lib/example_web/controllers/session_controller.ex
  - test/example/lib/example_web/controllers/session_html.ex
  - test/example/lib/example_web/router.ex
  - test/example/lib/example_web/components/layouts.ex
  - test/example/lib/example_web/user_auth.ex
  - test/example/priv/playwright/tests/demo-showcase.spec.ts
  - test/example/test/example_web/controllers/page_controller_test.exs
  - test/example/test/example/demo/personas_test.exs
  - test/example/test/example_web/controllers/session_controller_test.exs
  - test/example/test/example_web/live/app_live_test.exs
  - .planning/seeds/SEED-008-generated-host-first-run-admin-bootstrap.md
autonomous: true
requirements: [FRONTDOOR-01, FRONTDOOR-02, FRONTDOOR-03, FRONTDOOR-04]

must_haves:
  truths:
    - "Clicking a route-shaped code chip (/users/log_in, /admin, /admin/organizations/acme-corp) never copies to clipboard; only credential codes (persona email/password) marked vt-code--copy remain click-to-copy"
    - "The home page shows a curated get-started persona picker (admin, morgan, alice, pat, dave) directly after the hero, each with a working 'Sign in as {persona}' button targeting the real /users/log_in?demo={key} route"
    - "Visiting /users/log_in?demo={key} prefills the email field with that persona's real email; the login form is never auto-submitted and no password ever appears in a URL"
    - "In a dev_routes build, a password-hint/Fill affordance and a dev-only GET /demo/use/:persona fast-switch route exist; under mix test (dev_routes=false) both are absent — the route 404s and the hint markup does not render"
    - "The logged-in 'Demo personas' switch affordance is a separate component from Sigra's impersonation_banner — distinct CSS namespace (vt-* not sg-impersonation), distinct copy (never the word 'impersonate'), and also compiled out under dev_routes=false"
    - "SEED-008 exists at .planning/seeds/ documenting the generated-host first-admin-bootstrap gap for a future milestone"
  artifacts:
    - path: "test/example/lib/example_web/controllers/page_html/home.html.heex"
      provides: "Re-sequenced IA (orient -> get-started picker -> showcase -> reference) with copy-scope-fixed code chips"
      contains: "id=\"get-started\""
    - path: "test/example/lib/example_web/controllers/session_controller.ex"
      provides: "?demo= email prefill + dev-gated password hint assign + demo_switch/2 action"
      contains: "demo_persona_lookup"
    - path: ".planning/seeds/SEED-008-generated-host-first-run-admin-bootstrap.md"
      provides: "Captured installer-DX gap for a future milestone"
      contains: "sigra_admin_policy"
  key_links:
    - from: "home.html.heex persona-card 'Sign in as {persona}' button"
      to: "session_controller.ex new/2 params[\"demo\"]"
      via: "/users/log_in?demo={key} query param (never credentials)"
      pattern: "demo=#{c.local}"
    - from: "layouts.ex demo_persona_switch link"
      to: "router.ex GET /demo/use/:persona -> SessionController.demo_switch/2 -> UserAuth.log_out_user/2 :to redirect"
      via: "dev_routes-gated route"
      pattern: "demo_switch"
    - from: "admin_hooks.js installCopyDelegate + label selectors"
      to: "app.css .vt-code--copy opt-in class (mirrored in priv/static/assets/js/app.js)"
      via: "shared opt-in marker convention"
      pattern: "vt-code--copy"
---

<objective>
Fix the demo's copy-scope bug (routes are wrongly click-to-copy; only credentials should be) and give the running Sigra demo (test/example, "Tasklane") an obvious front door: a curated get-started persona picker directly after the hero, a real (never bypassed, never auto-submitted) prefilled login, and a fast dev-only way to switch personas while logged in — all routed through Sigra's real auth, dev-gated so nothing leaks to a non-dev build.

Full approved design: `/Users/jon/.claude/plans/whats-our-roadmap-gsd-indexed-narwhal.md` (authoritative — this plan implements it precisely).

Purpose: an evaluator lands on `/`, immediately sees "get started" with 5 differentiator personas (including a rough-edge, Dave/locked), clicks "Sign in as X", arrives at the real `/users/log_in` with email prefilled, and can hit Sigra's real MFA/passkey/lockout paths — without ever bypassing auth or leaking demo credentials into a URL.
Output: copy-scope fix (JS delegate + CSS, source + mirrored built bundle), re-sequenced home page with persona picker, prefilled real login + dev-only fast-switch, and a planted SEED-008 for the follow-on installer-DX gap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<discovery>
## VERIFIED FACTS (grep-confirmed before planning; do not re-derive)

**Build-free CSS, build-mirrored JS.** `test/example/priv/static/assets/css/app.css` is the
ONLY app.css in the repo — hand-maintained, no source/build split, edit it directly (no mirror
needed). JS is DIFFERENT: `test/example/assets/js/admin_hooks.js` is the source; the SAME code
is duplicated (not compiled) into the served bundle `test/example/priv/static/assets/js/app.js`
at the identical relative offsets (`installCopyDelegate` at admin_hooks.js:495 / app.js:8273).
Every JS edit in this plan MUST be applied to BOTH files identically or the served demo lags
silently (see memory `reference_example_js_bundle_drift`).

**dev_routes gating precedent.** `Application.compile_env(:example, :dev_routes)` is set `true`
only in `config/dev.exs:84`; absent (nil/falsy) in `config/test.exs`/`config/prod.exs`/
`config/runtime.exs`. The ONLY existing use is a module-level `if Application.compile_env(...)
do ... end` wrapping route definitions in `router.ex:182-206` (wraps `/dev/mailbox`,
`/demo/credentials`, `/admin/_design`). `mix test` sets `MIX_ENV=test` itself, so any code path
gated this way is provably absent under plain `mix test` — `credentials_live_test.exs` already
proves this pattern: `GET /demo/credentials` returns 404 under `mix test`. Reuse this EXACT
proof pattern for the new dev-only surfaces in Task 3 instead of grepping a prod build.

**Persona keys = email local-parts.** `Example.Demo.Personas.all/0` (in
`test/example/lib/example/demo/personas.ex`) returns 10 personas; the "key" referenced
throughout this plan (`?demo={key}`, `/demo/use/:persona`) is the email local-part string:
`admin`, `alice`, `bob`, `carol`, `dave`, `frank`, `morgan`, `pat`, `grace`, `zoe`. (The
orchestrator's illustrative `?demo=org_admin` example was generic — there is no `org_admin`
persona; `morgan` is the org-admin persona's local-part. Use local-parts throughout.)

**`.input` field id convention (core_components.ex:189-197).** `<.input field={f[:password]}
.../>` on a form built via `Phoenix.Component.to_form(%{"email"=>...}, as: "user")` renders
`id="user_password"` / `name="user[password]"` regardless of whether "password" is a key in the
seed map (id derives from form id + field name only). Verified directly from
`core_components.ex` — the SAME `@form` already renders both email and password fields in
`session_html.ex`, so `document.getElementById("user_password")` is the correct, already-proven
JS target for the Fill-password button.

**`log_out_user/1` already redirects (`user_auth.ex:152-164`).** It ends the conn with
`redirect(to: ~p"/")` — you CANNOT chain another `redirect` after calling it (already sent).
Task 3 extends it to `log_out_user/2` with a `:to` option (default unchanged, `~p"/"`) rather
than duplicating its session-clearing logic in the controller.

**`.vt-link` exists** (app.css:397) — the correct existing class for the converted route
`<a>` tags (NOT `.vt-btn`, which is heavier/button-styled and used for real CTAs).

**Persona-picker layout: reuse `.vt-persona-list` > `.vt-persona-row` as-is — zero new CSS
needed for the picker.** `.vt-persona-list` (app.css:603-611) is a single-column stacked grid of
`.vt-persona-row` articles; each row is internally 2-column at >=680px (app.css:1041-1051):
left = name + feature text, right = `.vt-identity-grid` (email/password). This is ALREADY the
"curated card" pattern the design calls for — just curate to 5 personas and add a "Sign in as
{name}" button inside the LEFT column (under the feature `<p>`), which needs no grid-span CSS.

**`.vt-card-grid` assumes exactly 2 children at desktop** (`grid-template-columns: minmax(0,
0.9fr) minmax(0, 1.1fr)` at `min-width: 860px`, app.css:1023-1026). The current "Start with
these accounts" + "Seeded evidence" panels are TWO siblings inside ONE `<section
class="vt-card-grid">` (home.html.heex:246-318). Since the persona panel is moving away from the
seed-evidence panel, do NOT keep either alone inside `.vt-card-grid` (a lone child leaves an
empty second grid column). Wrap each panel directly as its own single-panel section instead
(reuse `.vt-panel` directly, no grid wrapper).

**No existing test pins the copy that changes in this plan** ("Sign in to Tasklane", "Start
with these accounts" — verified via grep, zero hits outside the one line being changed). The
ONLY Playwright assertion that WILL break is `demo-showcase.spec.ts:850-857`, which asserts
`getComputedStyle(...).cursor === "copy"` on the first `code.vt-code` inside
`[data-testid="home-featured-personas"]` — after Task 1's CSS change only `.vt-code--copy`
carries `cursor: copy`. Fix the selector there (Task 1); keep `data-testid="home-featured-personas"`
on the picker's card-grid container in Task 2 so lines 826/827/850 in that spec still resolve
once the orchestrator runs live Playwright post-merge.

**Executor cannot reach the live demo.** `mix compile` / `mix test` (against the isolated
worktree's own test Postgres, per project convention) are the available automated checks. Live
Playwright against the booted demo (127.0.0.1:51803) is run by the ORCHESTRATOR after
worktree merge-back — do not attempt to boot the demo or run Playwright in this plan.
</discovery>

# Files this plan reads/edits — read each once, in full, before editing
@test/example/assets/js/admin_hooks.js
@test/example/priv/static/assets/js/app.js
@test/example/priv/static/assets/css/app.css
@test/example/lib/example_web/controllers/page_html/home.html.heex
@test/example/lib/example_web/controllers/page_controller.ex
@test/example/lib/example_web/live/demo/credentials_live.ex
@test/example/lib/example/demo/personas.ex
@test/example/lib/example_web/controllers/session_controller.ex
@test/example/lib/example_web/controllers/session_html.ex
@test/example/lib/example_web/router.ex
@test/example/lib/example_web/components/layouts.ex
@test/example/lib/example_web/user_auth.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy-scope decouple — vt-code becomes pure formatting, opt-in vt-code--copy carries the click-to-copy behavior</name>
  <files>
    test/example/assets/js/admin_hooks.js, test/example/priv/static/assets/js/app.js,
    test/example/priv/static/assets/css/app.css,
    test/example/lib/example_web/controllers/page_html/home.html.heex,
    test/example/lib/example_web/live/demo/credentials_live.ex,
    test/example/priv/playwright/tests/demo-showcase.spec.ts
  </files>
  <action>
CSS (app.css:315-337): remove `cursor: copy;` from the base `.vt-code` rule (it becomes pure
inline-code formatting — no other property changes). Add a new opt-in rule `.vt-code--copy {
cursor: copy; }`. Rename the existing `.vt-code:hover` rule to `.vt-code--copy:hover` (hover
affordance should only appear on the actually-copyable variant — a non-interactive code chip
should not visually invite a click).

JS delegate (admin_hooks.js:502 AND the identical line in app.js:8280): change
`target.closest(".sg-admin-shell code.sg-code, code.vt-code")` to
`target.closest(".sg-admin-shell code.sg-code, code.vt-code--copy")`. Admin `sg-code` behavior is
unchanged — only the demo half of the selector gains the `--copy` suffix.

JS label hint (admin_hooks.js:526 AND app.js:8299, inside the `querySelectorAll` call): same
substitution, `code.vt-code` -> `code.vt-code--copy`. Apply BOTH JS edits identically to
`admin_hooks.js` (source) and `priv/static/assets/js/app.js` (served bundle) — this app is
build-free, so the served file only updates if you edit it directly too.

Apply the `vt-code--copy` marker ONLY to credential values (persona email/password), and
convert route-shaped codes to real `<a class="vt-link">` links, in the STABLE regions below
(do NOT touch the "Start with these accounts" persona-list block at
home.html.heex:246-274/256-273 — Task 2 rebuilds that section from scratch and will apply the
marker there):
  - home.html.heex operator panel (`aside.vt-panel--operator`, currently lines 82-104): the
    `/users/log_in` code (line 86) -> `<a href="/users/log_in" class="vt-link">/users/log_in</a>`;
    `admin@demo.tasklane.test` (line 94) -> add the `vt-code--copy` marker (class becomes
    `"vt-code vt-code--copy"`); `/admin` (line 95) -> `<a href="/admin"
    class="vt-link">/admin</a>`; `morgan@demo.tasklane.test` (line 100) -> add the marker;
    `/admin/organizations/acme-corp` (line 101) -> `<a href="/admin/organizations/acme-corp"
    class="vt-link">/admin/organizations/acme-corp</a>`.
  - home.html.heex seed-evidence "Org admin path" bullet (currently line 314, inside the
    `vt-seed-list` at the bottom): `morgan@demo.tasklane.test` -> add the `vt-code--copy` marker;
    `/admin/organizations/acme-corp` -> the same `<a class="vt-link">` conversion.
  - Leave home.html.heex lines 23/25 (cohort domain / local-origin header strip) and line 229
    (email preview `email_from_address`) UNCHANGED — they are non-credential, non-route
    identifiers; dropping `cursor: copy` from the base `.vt-code` rule already makes them
    non-copyable with zero markup edit.
  - credentials_live.ex: the email code (line 82) and password code (line 83) inside the
    persona table each get the `vt-code--copy` marker added (class becomes
    `"vt-code vt-code--copy"`). Leave line 39 (cohort domain) unchanged, same reasoning as above.

Fix the one Playwright assertion this behavior change breaks: in `demo-showcase.spec.ts` around
line 850, the selector `'[data-testid="home-featured-personas"] code.vt-code'` (and the
`cursor` -> `"copy"` expectation at line 855-857) must target
`'[data-testid="home-featured-personas"] code.vt-code--copy'` instead — only the marked
credential chip carries `cursor: copy` now. This is a source-only static edit (no live
Playwright run available to the executor); it prepares the spec for the orchestrator's
post-merge live verification. Also update the stale inline comment at admin_hooks.js:11
referencing `code.vt-code` to say `code.vt-code--copy`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra; CSS=test/example/priv/static/assets/css/app.css; JS1=test/example/assets/js/admin_hooks.js; JS2=test/example/priv/static/assets/js/app.js; HOME=test/example/lib/example_web/controllers/page_html/home.html.heex; CRED=test/example/lib/example_web/live/demo/credentials_live.ex; sed -n '/^\.vt-code {/,/^}/p' "$CSS" | grep -q 'cursor: copy' && { echo FAIL-cursor-still-in-base; exit 1; }; grep -q '\.vt-code--copy' "$CSS" || { echo FAIL-no-copy-class; exit 1; }; for f in "$JS1" "$JS2"; do grep -n 'target.closest' "$f" | grep -q 'vt-code--copy' || { echo "FAIL-delegate $f"; exit 1; }; grep -n 'querySelectorAll' "$f" -A2 | grep -q 'vt-code--copy' || { echo "FAIL-label $f"; exit 1; }; done; for pat in '<code class="vt-code">/users/log_in</code>' '<code class="vt-code">/admin</code>' '<code class="vt-code">/admin/organizations/acme-corp</code>'; do grep -qF "$pat" "$HOME" && { echo "FAIL-stale-code-route: $pat"; exit 1; }; done; [ "$(grep -c 'vt-code--copy' "$HOME")" -ge 3 ] || { echo FAIL-home-markers; exit 1; }; for pat in '<code class="vt-code">{c.email}</code>' '<code class="vt-code">{c.password}</code>'; do grep -qF "$pat" "$CRED" && { echo "FAIL-stale-cred-code: $pat"; exit 1; }; done; [ "$(grep -c 'vt-code--copy' "$CRED")" -ge 2 ] || { echo FAIL-cred-markers; exit 1; }; cd test/example && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Base `.vt-code` rule carries no `cursor: copy` (pure formatting); `.vt-code--copy` is the sole opt-in copy affordance and both `admin_hooks.js` and the served `app.js` require it in the delegate + label selectors identically; the operator-panel and seed-evidence route codes are real `<a class="vt-link">` links; their adjacent credential codes carry `vt-code--copy`; `credentials_live.ex` email/password codes carry `vt-code--copy`; `demo-showcase.spec.ts`'s cursor assertion targets `code.vt-code--copy`; `mix compile --warnings-as-errors` is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Front-door IA re-sequence — get-started persona picker (D-guardrails: real login only, no auto-submit, key not credentials in URL)</name>
  <files>
    test/example/lib/example_web/controllers/page_html/home.html.heex,
    test/example/lib/example_web/controllers/page_controller.ex,
    test/example/lib/example/demo/personas.ex,
    test/example/test/example_web/controllers/page_controller_test.exs,
    test/example/test/example/demo/personas_test.exs
  </files>
  <action>
personas.ex: add a new public function `featured_keys/0` (with `@spec featured_keys() ::
[String.t()]` and a doc comment) returning the curated differentiator set as local-part
strings: `admin` (Platform Admin, TOTP MFA), `morgan` (Org Admin, non-platform), `alice`
(standard confirmed happy-path), `pat` (passkey-only), `dave` (locked/rough-edge) — in that
order. This becomes the single source of truth for BOTH the home-page picker (this task) and
the Task 3 "Demo personas" switch bar — do not hardcode the list twice.

page_controller.ex: change the `featured_credentials` filter from the current hardcoded
`&(&1.local in ~w(admin alice morgan pat))` to `&(&1.local in Personas.featured_keys())` — now
5 personas instead of 4. Leave the rest of `home/2` (assigns, `local_origin/1`) unchanged.

home.html.heex — re-sequence per the approved design (orient -> get-started -> showcase ->
reference):
  1. Hero (`vt-hero`, currently lines 29-105): simplify the CTA row (`vt-action-row`,
     currently 4 near-equal buttons). For the anonymous case, change the primary button's
     `href` from `/users/log_in` to `#get-started` (an in-page anchor to the new picker
     section — this does NOT bypass login, it scrolls an evaluator to the persona picker so
     picking a persona stays the fast path instead of skipping it) and its label to something
     like "Get started — pick a persona". Leave the logged-in primary (`href={~p"/app"}`,
     "Open your dashboard") and the conditional "Log out" link unchanged. De-emphasize "Open
     Sigra Admin" and "Inspect mailbox" from button-styled CTAs (`vt-btn--ghost` /
     `vt-btn--secondary`) to lighter `vt-link` inline links — they remain functional
     secondary utility links, just visually subordinate to the one primary CTA. Keep their
     exact `href`s (`/admin`, `/dev/mailbox`) and text unchanged (an existing Playwright
     assertion checks `getByRole("link", { name: "Open Sigra Admin" })` -> `href="/admin"` —
     preserve both the accessible name and href exactly).
  2. NEW get-started section, placed immediately after the hero's closing `</section>` and
     BEFORE the current `vt-brand-lab` section: `<section id="get-started" aria-label="Get
     started — choose an evaluator persona">` wrapping a `.vt-panel` with a kicker ("Get
     started") + title ("Choose an evaluator persona") + the existing "View all personas" link
     (update its label to "View all 10 personas ->", href unchanged: `/demo/credentials`).
     Inside, reuse the CURRENT `vt-persona-list` / `vt-persona-row` markup pattern
     (home.html.heex:256-273 today) verbatim EXCEPT: (a) iterate `@featured_credentials`
     (now 5 personas via `Personas.featured_keys()`), (b) keep `data-testid=
     "home-featured-personas"` on the `vt-persona-list` container (an existing Playwright spec
     depends on this exact testid), (c) add `vt-code--copy` to BOTH the email and password
     `<code class="vt-code">` chips in each row (following Task 1's convention — this section
     is being freshly authored here, not migrated, so apply the marker directly), (d) add a
     "Sign in as {persona}" primary button inside the row's LEFT column (under the existing
     `<p class="vt-copy">{c.feature}</p>`, no grid-span CSS needed) with `href={~p"/users/log_in?
     #{%{demo: c.local}}"}` (verified-routes dynamic-query pattern, same convention already
     used in `session_controller.ex:60`) and class `vt-btn vt-btn--primary`. The persona `key`
     in the URL is the local-part string (`c.local`) — NEVER the email or password.
  3. Move the existing `vt-brand-lab` section (currently lines 107-244) to directly AFTER
     the new get-started section — unchanged internally, just repositioned (showcase, after
     get-started).
  4. The "Seeded evidence" panel (currently the second child of the shared `vt-card-grid` at
     lines 276-317) becomes its own standalone section at the bottom (reference), wrapped
     directly in `<section class="vt-panel" aria-label="Seeded evidence">` (NOT
     `vt-card-grid` — that class assumes exactly 2 grid children at desktop and would leave an
     empty column with only one panel; see discovery note). Its internal markup (metric-grid,
     panel-header, seed-list) is unchanged.
  5. Delete the now-empty shared `<section class="vt-card-grid">` wrapper that used to hold
     both panels together.

Update `page_controller_test.exs`'s single big `GET /` test: add assertions that `dave@
demo.tasklane.test` and `demo=dave` both appear, that `id="get-started"` appears, and that
`Sign in as` appears (the new button copy). Update `personas_test.exs`: add a new test under
a `"featured_keys/0"` describe block asserting `Personas.featured_keys() ==
~w(admin morgan alice pat dave)` and that every key is a member of `@expected_handles`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors && mix test test/example_web/controllers/page_controller_test.exs test/example/demo/personas_test.exs</automated>
  </verify>
  <done>`Personas.featured_keys/0` returns the 5 curated local-parts and is the single source consumed by `page_controller.ex`; home.html.heex reads orient (hero, one clear primary CTA anchoring to `#get-started`) -> get-started (5-persona picker, each with a working "Sign in as {persona}" button targeting `/users/log_in?demo={key}`) -> showcase (`vt-brand-lab`, moved down) -> reference (standalone Seeded-evidence panel); `data-testid="home-featured-personas"` preserved; `mix test` green on the two updated test files.</done>
</task>

<task type="auto">
  <name>Task 3: Prefilled real login + dev-only fast switch (D-guardrails: never auto-submit, dev_routes-gated hint/switch, distinct from impersonation)</name>
  <files>
    test/example/lib/example_web/controllers/session_controller.ex,
    test/example/lib/example_web/controllers/session_html.ex,
    test/example/lib/example_web/router.ex,
    test/example/lib/example_web/components/layouts.ex,
    test/example/lib/example_web/user_auth.ex,
    test/example/assets/js/admin_hooks.js,
    test/example/priv/static/assets/js/app.js,
    test/example/priv/static/assets/css/app.css,
    test/example/test/example_web/controllers/session_controller_test.exs,
    test/example/test/example_web/live/app_live_test.exs
  </files>
  <action>
session_controller.ex `new/2`: change the param from `_params` to `params`. Add two private
helpers. `demo_persona_lookup/1` — ALWAYS active (not dev-gated: it only prefills a non-secret
email, and the homepage picker buttons that drive it exist in every build): given
`params["demo"]`, find the persona in `Example.Demo.Personas.all()` whose email
`String.starts_with?(email, key <> "@")`; return the persona map or nil (nil for missing/blank
key or no match — never raise on garbage input). Use its `.email` (if found) as the prefill,
falling through to the existing flash-based prefill and then `""`, in that priority order (keep
the existing flash-email-on-error behavior intact). Pass a NEW `demo_persona_hint` assign to
`render/3`, computed by a SECOND helper, `demo_persona_hint/1`, which is dev_routes-gated:
wrap its definition in a module-level `if Application.compile_env(:example, :dev_routes) do
... else ... end` (mirroring the existing gating idiom at `router.ex:182`) — the `if` branch
clause returns the persona map unchanged (or nil), the `else` branch clause ALWAYS returns nil
regardless of input. This ensures the persona's PASSWORD only ever reaches the render assigns
in a dev_routes build.

session_html.ex `:new` template: immediately after the closing `</.form>` of the primary login
form (currently line 91) and before the passkey `<%= if @passkey_primary_enabled do %>` block,
add a block gated `:if={@demo_persona_hint}`: a "Disposable demo account — never use in
production" kicker (matches the existing `/demo/credentials` disclaimer tone), a line showing
`@demo_persona_hint.email` in a `vt-code vt-code--copy` chip, and a `type="button"` (NOT
`type="submit"`) button labeled "Fill password" carrying two data attributes: a boolean marker
`data-demo-fill-password` and the value-carrying `data-demo-password={@demo_persona_hint.password}`.
This is a GESTURE-fired affordance only — it must never fill the field on page load, only on
click. Give the wrapper `data-testid="demo-login-hint"`.

admin_hooks.js (source) AND priv/static/assets/js/app.js (mirror, identical edit): add a new
function `installDemoPasswordFill()` next to `installCopyDelegate()`, following the SAME
delegated-click pattern (no per-page mount — this file boots globally on every page including
the plain-controller login page). On click, `target.closest("[data-demo-fill-password]")`; if
found, read `data-demo-password` off it, set `document.getElementById("user_password").value`
to that value, and dispatch a bubbling `input` event on the input (so any listeners observe the
change) — do NOT run this on `DOMContentLoaded` or any load event, ONLY inside the click
handler. Call `installDemoPasswordFill();` at the same top-level boot spot as the existing
`installCopyDelegate();` call (admin_hooks.js:1146 and the equivalent line in app.js).

router.ex: inside the EXISTING `if Application.compile_env(:example, :dev_routes) do` block
(lines 182-206), inside the existing `scope "/demo", ExampleWeb do ... end` sub-scope (right
after `live "/credentials", Demo.CredentialsLive`), add `get "/use/:persona", SessionController,
:demo_switch`. This route (and therefore `SessionController.demo_switch/2`, which is otherwise
unreachable) only exists in a dev_routes build — no additional gating needed inside the
controller action itself (matches the existing `/demo/credentials` precedent, where only the
ROUTE is gated).

user_auth.ex: extend `log_out_user/1` to `log_out_user(conn, opts \\ [])`, reading `to =
Keyword.get(opts, :to, ~p"/")` and using `to` in place of the hardcoded `~p"/"` in the final
`redirect(to: ...)` call. Every other line of the function body is unchanged. This is
backward-compatible — the two existing zero-arg call sites (`session_controller.ex`'s
`delete/2` and `user_auth_test.exs`) keep working identically.

session_controller.ex: add `demo_switch/2`: `def demo_switch(conn, %{"persona" => persona}),
do: UserAuth.log_out_user(conn, to: ~p"/users/log_in?#{%{demo: persona}}")`. This clears the
current session (via the now-parameterized `log_out_user/2`) and redirects straight into a
prefilled real login for the new persona — never bypasses auth, never both logs-out-and-in in
one hop.

layouts.ex: add a NEW function component `demo_persona_switch/1` (attr `current_scope`) —
completely separate from `admin_shell.ex`'s `impersonation_banner/1` (different module,
different CSS namespace: `vt-demo-switch`, never `sg-impersonation*`). Body: a slim bar with a
"DEMO" badge (reuse the existing `vt-status-pill` class, same pattern as
`credentials_live.ex`'s "DEV ONLY" badge — no new CSS needed for the badge itself), a short
label ("Demo personas — switch account:"), and one link per `Personas.featured_keys()` entry
(reuse the display_name from `Personas.all()`), each `href={~p"/demo/use/#{key}"}` styled
`vt-btn vt-btn--ghost`. Give the wrapper `data-testid="demo-persona-switch"`. Wire it into
`app/1`, placed right after the existing `<.impersonation_banner .../>` call and before
`<main>`: `<.demo_persona_switch :if={Application.compile_env(:example, :dev_routes) &&
@current_scope} current_scope={@current_scope} />`. PITFALL: call
`Application.compile_env(:example, :dev_routes)` directly (a compile-time macro call) inside
the `:if` — do NOT write `@dev_routes` expecting it to read a module attribute; inside a `~H`
template `@name` reads `assigns.name`, not a module `@` attribute, and `app/1`'s `assigns` has
no `:dev_routes` key, so that would silently and permanently disable the feature.

app.css: add two small new rules near the other `vt-*` component blocks (e.g. beside
`.vt-panel--operator`) — `.vt-demo-hint` (bordered block under the login form: reuse
`--vt-radius`, `--sg-space-3`/`--sg-space-4` padding, a soft background via `color-mix(in
oklab, var(--vt-color-accent-soft) ...)` — the SAME accent family `.vt-code` already uses, NOT
any red/danger tone) and `.vt-demo-switch` + `.vt-demo-switch__label` (a slim flex row, small
gap, positioned as a bar below the app header). Do not reference or reuse any
`sg-impersonation*` selector.

Add tests to `session_controller_test.exs`: (1) `GET /users/log_in?demo=admin` prefills the
email input with `admin@demo.tasklane.test` (works in any build — assert this WITHOUT any
dev_routes assumption); (2) `GET /users/log_in?demo=not-a-real-persona` does not crash and does
not prefill (falls through to `""`); (3) under `mix test` (dev_routes=false),
`GET /users/log_in?demo=admin` does NOT render `"Disposable demo account"` /
`data-demo-fill-password` (proves the hint is compiled out, mirroring
`credentials_live_test.exs`'s established 404-proof pattern); (4) `GET /demo/use/admin`
returns 404 under `mix test` (identical pattern to the existing `/demo/credentials` 404 test).
Add one assertion to `app_live_test.exs`'s existing authenticated test: `refute html =~
"demo-persona-switch"` (proves the switch bar is compiled out under `mix test` too).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors && mix test test/example_web/controllers/session_controller_test.exs test/example_web/user_auth_test.exs test/example_web/live/app_live_test.exs test/example_web/live/demo/credentials_live_test.exs</automated>
  </verify>
  <done>`?demo={key}` prefills the real login's email field in ANY build; the password-hint/Fill affordance, `/demo/use/:persona`, and the `demo_persona_switch` bar all exist in a dev_routes build and are all PROVABLY absent under `mix test` (404 for the route, `refute` for the two markup blocks — same proof pattern as the existing `/demo/credentials` gate); the Fill button only acts on a real click (delegated handler, no load-time fill); `log_out_user/2` is backward compatible (both existing zero-arg call sites still pass); the switch bar is a separate component/CSS-namespace from `impersonation_banner`, never says "impersonate".</done>
</task>

<task type="auto">
  <name>Task 4: Plant SEED-008 — generated-host first-run admin-bootstrap gap</name>
  <files>.planning/seeds/SEED-008-generated-host-first-run-admin-bootstrap.md</files>
  <action>
Create `.planning/seeds/SEED-008-generated-host-first-run-admin-bootstrap.md` following the
existing frontmatter convention (see `.planning/seeds/SEED-007-secret-key-base-compile-env-
decoupling.md` for the exact schema: `id`, `status`, `planted`, `planted_during`,
`trigger_when`, `scope`). Use `status: open` (this is a freshly captured gap, not a
deliberately-deferred one), `planted: 2026-07-18`, `planted_during: "quick task 260718-i1m —
demo front-door get-started affordance"`, `scope: Medium`.

Body content — capture EXACTLY the substance from the approved design's "GSD seed to plant"
section (do not paraphrase away specifics):
  - Problem: after `mix sigra.install`, `/admin` is routable but the generated
    `sigra_admin_policy.ex` is a stub returning `false`
    (`priv/templates/sigra.install/admin/policy.ex:12-26`, verified present); there is no
    generated seed, mix task, or doc to (a) create the first user and (b) mark them platform
    admin. The installer's final printed guidance (`lib/sigra/install/features/admin.ex:65-86`)
    stops at "define platform-admin access explicitly" and never closes the loop. Sigra
    deliberately will not infer admin from signup order/email domain. A fresh adopter cannot
    reach their own `/admin` without reverse-engineering. The installer also generates no
    landing/getting-started surface (the rich Tasklane landing built in this quick task is
    demo-only, not generated into host apps).
  - Idea: a safe generated first-run affordance — e.g. an interactive `mix sigra.gen.admin`
    (or a documented `mix run` snippet / generated seed stub) to bootstrap the first operator,
    plus post-install "create your first admin" guidance that actually closes the loop;
    optionally a minimal generated getting-started section.
  - Guardrails (from research): must NEVER generate demo credentials or an auth-bypass into a
    host app (CWE-798/489); first-admin bootstrap must be explicit and safe; any convenience
    must be compile-gated.
  - Trigger: an adopter-onboarding / installer-DX milestone, or an adopter reporting "how do I
    get into /admin after install." Ties to the project North Star's "clear integration path"
    and "great DX on the happy path AND the rough edges."
  - Key files (all verified present at plan time): `lib/mix/tasks/sigra.install.ex`,
    `lib/sigra/install/features/admin.ex`, `lib/sigra/install/features/core.ex` (post-install
    text), `priv/templates/sigra.install/admin/policy.ex`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra; F=.planning/seeds/SEED-008-generated-host-first-run-admin-bootstrap.md; [ -f "$F" ] || { echo FAIL-missing; exit 1; }; for pat in 'sigra_admin_policy' 'CWE-798' 'sigra.gen.admin' 'lib/mix/tasks/sigra.install.ex' 'priv/templates/sigra.install/admin/policy.ex'; do grep -qF "$pat" "$F" || { echo "FAIL-missing-content: $pat"; exit 1; }; done; echo OK</automated>
  </verify>
  <done>SEED-008 exists with the full problem/idea/guardrails/trigger/key-files captured, matching the approved design's seed content, ready to be picked up in a future installer-DX milestone.</done>
</task>

</tasks>

<verification>
Cross-task checks the executor should run once, after all four tasks are committed (no live server needed):

1. Build-free mirror parity: `diff <(sed -n '/installCopyDelegate/,/^  }/p' test/example/assets/js/admin_hooks.js | head -40) <(sed -n '/installCopyDelegate/,/^  }/p' test/example/priv/static/assets/js/app.js | head -40)` should show only the expected structural offset, not a missing `vt-code--copy` edit in one file and not the other — spot-check both files contain `installDemoPasswordFill` too.
2. `cd test/example && mix compile --warnings-as-errors` clean from a fresh `mix clean` (catches any stray reference across the whole plan, not just per-task).
3. `cd test/example && mix test` — full suite green (or only the pre-existing known failures documented in project memory `reference_v139_known_pretest_failures` / `reference_preexisting_mix_test_failures` — no NEW failures attributable to this plan).
4. `git diff --name-only` matches the `files_modified` list in this plan's frontmatter — no unintended files touched.
5. Grep-confirm no password ever appears as a URL/query-string literal anywhere in the diff (`git diff | grep -E 'password=.+@'` should be empty) — the only place a password appears in new markup is the dev-gated `vt-code--copy` chip inside `data-testid="demo-login-hint"`, never in an `href`.

Explicitly OUT of scope for the executor (performed by the ORCHESTRATOR after worktree
merge-back, against the live demo on 127.0.0.1:51803 per `scripts/uat/up.sh --reset`):
copy-click-to-clipboard-and-toast behavior, the persona picker's real click-through into a
prefilled login, the Fill-password button's real DOM effect, the `/demo/use/:persona` real
logout-then-redirect round trip, and any Playwright `.spec.ts` run.
</verification>

<success_criteria>
- Base `.vt-code` is pure formatting (no `cursor: copy`); only `.vt-code--copy` triggers the
  click-to-copy delegate, identically in `admin_hooks.js` and the served `app.js`.
- Every route-shaped code chip touched by this plan (`/users/log_in`, `/admin`,
  `/admin/organizations/acme-corp`) is a real `<a class="vt-link">`, not a copy-styled code.
- Home page order is orient (hero, one clear primary CTA) -> get-started (5-persona picker with
  working "Sign in as {persona}" buttons) -> showcase (`vt-brand-lab`) -> reference (seeded
  evidence).
- `/users/log_in?demo={key}` prefills the real email field in any build; the login form is
  never auto-submitted; no password ever appears in a URL.
- The password-hint/Fill affordance, `/demo/use/:persona`, and the "Demo personas" switch bar
  all exist under `dev_routes=true` and are all provably absent under `mix test`.
- The switch bar is lexically and structurally distinct from Sigra's impersonation banner.
- SEED-008 is planted with the full problem/idea/guardrails/trigger/key-files.
- `mix compile --warnings-as-errors` and the targeted `mix test` runs listed in each task are
  green; no unintended files touched.
</success_criteria>

<output>
Create `.planning/quick/260718-i1m-demo-front-door-get-started-affordance-c/260718-i1m-SUMMARY.md` when done. In the SUMMARY, explicitly flag for the orchestrator: (1) which live-Playwright checks remain to run post-merge (copy/toast behavior, picker click-through, Fill button, fast-switch round trip), (2) confirm the build-free JS mirror (admin_hooks.js <-> app.js) stayed in sync across all edits, and (3) confirm no password ever landed in a URL/query string anywhere in the diff.
</output>
