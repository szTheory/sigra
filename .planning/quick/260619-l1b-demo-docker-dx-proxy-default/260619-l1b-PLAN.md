---
phase: quick-260619-l1b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/uat/up.sh
  - scripts/uat/docker-compose.yml
  - scripts/uat/docker-compose.watch.yml
  - scripts/uat/down.sh
  - guides/recipes/local-development.md
  - guides/introduction/demo-showcase.md
  - scripts/uat/RUNBOOK.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [DEMO-DX-L1B]
must_haves:
  truths:
    - "scripts/uat/up.sh with no flags boots the Dockerized demo behind shared Traefik with live reload"
    - "The printed URL is only shown as live after a readiness probe (HTTP 200) succeeds; otherwise labeled STARTING"
    - "Editing a test/example admin template hot-reloads in the browser without an image rebuild"
    - "--dev runs the host-run path and actually starts + health-gates Phoenix"
    - "scripts/uat/down.sh tears down containers AND kills any host-run Phoenix PID"
  artifacts:
    - path: "scripts/uat/up.sh"
      provides: "Default-proxy flip, new flags, readiness helpers, grouped routes, claim-based alias, auto-open"
    - path: "scripts/uat/docker-compose.yml"
      provides: "web service healthcheck enabling up -d --wait"
    - path: "scripts/uat/docker-compose.watch.yml"
      provides: "Bind-mount override for live reload in proxy mode"
    - path: "scripts/uat/down.sh"
      provides: "Host-run PID cleanup"
  key_links:
    - from: "scripts/uat/up.sh"
      to: "scripts/uat/docker-compose.watch.yml"
      via: "WATCH_FILE var passed as -f to compose up/build in proxy mode"
      pattern: "docker-compose.watch.yml"
    - from: "scripts/uat/up.sh"
      to: "scripts/uat/docker-compose.yml"
      via: "up -d --wait blocks on web healthcheck"
      pattern: "--wait"
---

<objective>
Make `scripts/uat/up.sh` (no flags) a single hands-off command that boots the Dockerized demo behind the shared Traefik proxy WITH live reload, health-gates the URL, auto-opens `/demo/credentials`, and prints grouped routes (auth / admin / ops). Keep host-run as an explicit `--dev` opt-in but fix it to actually start + health-gate Phoenix. Relax the `sigra.localhost` alias to claim-based so feature branches still get the clean URL. Update docs to match.

Purpose: The current default mode prints a live-looking URL but never starts Phoenix (dead URL), and the proxy path that does work is hidden behind `--proxy` and prints its URL before boot completes.
Output: Updated scripts (up.sh, down.sh, compose) + new docker-compose.watch.yml + reader-empathetic docs.

Source of truth: `/Users/jon/.claude/plans/does-this-not-have-serialized-karp.md`. Convert it faithfully; do NOT re-architect the confirmed decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/uat/up.sh
@scripts/uat/docker-compose.yml
@scripts/uat/down.sh
@scripts/uat/lib/naming.sh

# Routes verified against test/example/lib/example_web/router.ex:
#   /admin /admin/users /admin/audit /admin/auth-branding /admin/users/:id/audit
#   /admin/_design (DesignGalleryLive) /admin/organizations/:org
#   /dev/mailbox (Swoosh) /demo/credentials
# Dockerfile.example already has inotify + cached deps/build layers (reference only — do NOT rebuild it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip up.sh default to proxy+watch, add flags, health-gate helpers, grouped routes, claim-based alias, auto-open</name>
  <files>scripts/uat/up.sh, scripts/uat/docker-compose.yml, scripts/uat/docker-compose.watch.yml</files>
  <action>
**A. Default-mode flip + flags (arg loop + flag-defaults + mode-selection case).**
Today the mode-selection case (`up.sh` ~lines 538-544) resolves `SIGRA_UAT_PROXY_MODE` to `none` when no flag is given. Flip it so the no-flag default resolves to `shared`. Introduce a `--dev`/`--host` flag that selects the host-run path (sets the old `none` behavior), and keep `--proxy` as an explicit alias of the new default. Keep `--private-traefik` unchanged and the mutual-exclusion guard. Add new flag default vars near the existing block (`RESET_DB`, `SEED_DEMO`, etc.): `ENABLE_DEV_HOST=0`, `SIGRA_UAT_OPEN="${SIGRA_UAT_OPEN:-1}"`, `ENABLE_ATTACH=0`, `ENABLE_WATCH=1`. Wire these flags into the `while [[ $# -gt 0 ]]` arg loop: `--dev`/`--host` → `ENABLE_DEV_HOST=1`; `--no-open` → `SIGRA_UAT_OPEN=0`; `--attach`/`--iex` → `ENABLE_ATTACH=1`; `--no-watch` → `ENABLE_WATCH=0`. Update the usage header comment block (lines 12-20) and the `--help` sed range so all new flags are documented. The mode-selection becomes: `--private-traefik` → `private-traefik`; else `--dev`/`--host` → `none`; else (default, or explicit `--proxy`) → `shared`.

**B. New helpers** (place near the other helpers; match the existing `cyan/green/yellow/red` style — directive prose only, no inline code blocks):
  - `wait_for_http <url> [timeout]`: loop `curl -fsS --max-time 2 "<url>" >/dev/null 2>&1` until success or timeout (default ~60s), sleeping 1s between tries. On success set global `SIGRA_UAT_READY=1` and green-log; on timeout set `SIGRA_UAT_READY=0` and yellow-warn (do NOT exit — print_status will show STARTING). Always probe the RAW `127.0.0.1:<published-port>` URL (`SIGRA_UAT_RAW_URL`), which resolves in every browser and bypasses `.localhost` DNS.
  - `start_host_server`: `cd "${EXAMPLE_DIR}"`, launch `mix phx.server` in the background with the same `PG*`/`PORT`/`SIGRA_EXAMPLE_*` env the existing `SIGRA_UAT_SERVER_COMMAND` already sets, redirect stdout+stderr to `tmp/uat-phoenix.log`, write `$!` to `tmp/uat-phoenix.pid`.
  - `ensure_port_free`: if the chosen host-run port (`SIGRA_EXAMPLE_PORT`) is now in use, re-run `find_free_port` and reassign `SIGRA_EXAMPLE_PORT` + `SIGRA_UAT_RAW_URL` + `SIGRA_EXAMPLE_URL` (cheap TOCTOU guard; only meaningful in `none` mode).
  - `maybe_open_browser`: if `SIGRA_UAT_OPEN=1` and `SIGRA_UAT_READY=1`, open `${SIGRA_UAT_DEMO_URL}` with `open` (macOS) or `xdg-open` (Linux), tolerant of absence (`|| true`).

**C. shared branch (`setup_docker_example`).** Define `WATCH_FILE="${REPO_ROOT}/scripts/uat/docker-compose.watch.yml"` near the compose-file vars. When `ENABLE_WATCH=1` (default), append `-f "${WATCH_FILE}"` to BOTH the `build web` and the `up` compose invocations inside `setup_docker_example` (and the initial `stop web`). Switch the final `up -d web` to `up -d --wait web` so it blocks on the new healthcheck. After it returns and `SIGRA_UAT_WEB_HOST_PORT`/`SIGRA_UAT_RAW_URL` are discovered, call `wait_for_http "${SIGRA_UAT_RAW_URL}"`.

**D. host-run branch (`--dev`, mode `none`).** After `setup_host_example`: if `ENABLE_ATTACH=1`, `exec` the foreground `iex -S mix phx.server` command (the existing `SIGRA_UAT_SERVER_COMMAND` form) so it is terminal-bound; else `ensure_port_free` → `start_host_server` → `wait_for_http "${SIGRA_UAT_RAW_URL}"`.

**E. print_status.** (a) Branch the `PRIMARY URL` line on `SIGRA_UAT_READY`: when `0`, append `  STARTING — not yet responding (see tmp/uat-phoenix.log)` so it never lies. (b) Add an ADMIN route group printed from `${base}` (verified routes): `/admin`, `/admin/users`, `/admin/audit`, `/admin/auth-branding`, `/admin/_design`, `/admin/organizations/acme-corp` — include a one-line note "sign in as admin@demo.vaultr.test". Keep `/dev/mailbox` + `/demo/credentials` under OPS. (c) Reword the shared-mode server line from a "run this" command into "Logs: docker compose ... logs -f web"; for host-run mode reword to "Logs: tail -f tmp/uat-phoenix.log  ·  Attach IEx: <existing iex command>" since the server is already running.

**F. Persist + auto-open.** Add `SIGRA_UAT_READY`, host-run PID path (`tmp/uat-phoenix.pid`) and log path (`tmp/uat-phoenix.log`) into `write_state_file` so `--status` and `down.sh` can read them. After the final `print_status` call at end of script, call `maybe_open_browser`.

**G. Claim-based alias relax.** In the `shared)` case (`up.sh` ~line 581), change the gate `if is_default_branch && ! alias_claimed_by_other` to drop the `is_default_branch` requirement → `if ! alias_claimed_by_other` (keep the conflict check). Update the adjacent comment + the `is_default_branch`-gated `yellow` note so the message no longer implies default-branch-only. Net effect: the first stack to claim `sigra.localhost` on ANY branch gets it; others fall back to their unique per-checkout host.

**H. compose healthcheck (docker-compose.yml).** Add a `healthcheck` to the `web` service hitting the public no-auth dev route, e.g. test `wget -qO- http://127.0.0.1:4000/demo/credentials || exit 1` (the slim image ships wget; if not, use the curl form already proven elsewhere) with `interval: 3s`, `timeout: 3s`, `retries: 20`, `start_period: 30s`. This enables `up -d --wait`.

**I. New docker-compose.watch.yml (override, applied by default in proxy mode).** Create the file with a `web` service override that:
  - bind-mounts the repo root: `../..:/app:cached` (image `WORKDIR` is `/app/test/example`).
  - declares named volumes that SHADOW compiled artifacts so host source never clobbers in-image NIFs/builds, at BOTH levels: `_build` and `deps` under `/app` AND under `/app/test/example` (4 volume mounts + the named-volume declarations).
  - COMPILE-ENV INVARIANT (call out inline as a comment): do NOT set/override `SIGRA_EXAMPLE_BIND`, `PORT`, `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, `PGDATABASE`, or `SIGRA_EXAMPLE_URL` here — the container's runtime values must stay equal to the values baked at compile time in Dockerfile.example, or `validate_compile_env` aborts boot on the in-container recompile. The override is bind-mount + volume-shadowing ONLY.
  - reference the `proxy` external network the same way the base file does if the override needs the network key; otherwise rely on compose merge.
  </action>
  <verify>
    <automated>bash -n scripts/uat/up.sh && bash -n scripts/uat/down.sh</automated>
    Additional checks (run all):
    - `docker compose -f scripts/uat/docker-compose.yml -f scripts/uat/docker-compose.watch.yml --profile proxy config -q` (merge + override validates)
    - `grep -q 'docker-compose.watch.yml' scripts/uat/up.sh`
    - `grep -q -- '--wait' scripts/uat/up.sh`
    - `grep -Eq 'wait_for_http|start_host_server|ensure_port_free|maybe_open_browser' scripts/uat/up.sh` (all four helpers present — confirm each individually)
    - `grep -Eq 'SIGRA_UAT_OPEN|--no-open|--dev|--attach|--no-watch' scripts/uat/up.sh`
    - `grep -q '/admin/auth-branding' scripts/uat/up.sh && grep -q '/admin/_design' scripts/uat/up.sh && grep -q 'acme-corp' scripts/uat/up.sh`
    - `grep -q 'healthcheck' scripts/uat/docker-compose.yml` (under the web service)
    - confirm the watch override does NOT contain `SIGRA_EXAMPLE_BIND`, `PGHOST`, or `PORT:` keys: `! grep -Eq 'SIGRA_EXAMPLE_BIND|PGHOST|^\s*PORT:' scripts/uat/docker-compose.watch.yml`
    - confirm the alias gate dropped is_default_branch: `! grep -q 'is_default_branch && ! alias_claimed_by_other' scripts/uat/up.sh`
  </verify>
  <done>
    No-flag `up.sh` resolves to `shared` mode with watch override; `--dev` selects host-run and starts+gates Phoenix; readiness gate sets SIGRA_UAT_READY and print_status never prints a live URL while STARTING; ADMIN route group prints; alias is claim-based; compose files validate; watch override preserves compile-env. All grep/syntax/config checks pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: down.sh host-run PID cleanup</name>
  <files>scripts/uat/down.sh</files>
  <action>
After the existing `docker compose ... down` invocations (both `--purge` and default branches), add tolerant host-run cleanup: read `tmp/uat-phoenix.pid` if present, `kill` the PID when still alive (`kill -0` check), then `rm -f tmp/uat-phoenix.pid tmp/uat-phoenix.log`. Wrap each step with `|| true` since proxy-default runs may never create these files. Use the same `${REPO_ROOT}`-anchored paths up.sh writes. Keep the `--purge` STATE_FILE removal behavior intact.
  </action>
  <verify>
    <automated>bash -n scripts/uat/down.sh</automated>
    - `grep -q 'uat-phoenix.pid' scripts/uat/down.sh`
    - `grep -q 'uat-phoenix.log' scripts/uat/down.sh`
  </verify>
  <done>
    `down.sh` stops containers AND kills any recorded host-run Phoenix PID, removing the pid/log files, tolerant of their absence. Syntax check passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reader-empathetic docs update</name>
  <files>guides/recipes/local-development.md, guides/introduction/demo-showcase.md, scripts/uat/RUNBOOK.md, README.md, CLAUDE.md</files>
  <action>
**guides/recipes/local-development.md** — rewrite the "Running the demo app" section to the new reality: default = one command → builds + boots behind Traefik → waits until live → auto-opens `/demo/credentials` → prints grouped routes. Document the flags: `--dev` (fast host-run live reload), `--attach` (foreground IEx), `--no-watch` (disable bind-mount reload in proxy mode), `--no-open`. Add a short "Running several Sigra-family libs at once" subsection (shared proxy, per-checkout `*.localhost` hosts, claim-based `sigra.localhost`). Add a troubleshooting line pointing at `tmp/uat-phoenix.log` and noting macOS bind-mount reload lag → use `--dev`. Keep/refresh the existing TL;DR gameplan box at the top.

**guides/introduction/demo-showcase.md** — update the evaluator command/blurb to "one command, opens itself"; keep persona/screenshot content unchanged.

**scripts/uat/RUNBOOK.md** — update the one-liner blurb + the flag table to the new default and new flags.

**README.md** — adjust the quick-start demo pointer to the new single-command default (no second terminal needed).

**CLAUDE.md** — adjust the "Local development prerequisites" demo one-liner to the new default (no second terminal needed). Make a minimal, surgical edit; do not disturb unrelated sections.
  </action>
  <verify>
    <automated>grep -Eq -- '--dev|--no-open|--no-watch' guides/recipes/local-development.md</automated>
    - `grep -Eq 'sigra.localhost|one command' guides/recipes/local-development.md`
    - `grep -q 'tmp/uat-phoenix.log' guides/recipes/local-development.md`
    - confirm RUNBOOK + demo-showcase mention the new default: `grep -Eq 'up.sh' scripts/uat/RUNBOOK.md`
  </verify>
  <done>
    Docs describe the new one-command default, the new flags, multi-lib coexistence, and host-run troubleshooting. README + CLAUDE.md demo one-liners updated. Grep checks pass.
  </done>
</task>

</tasks>

<verification>
Runnable (CI-safe) checks:
- `bash -n scripts/uat/up.sh && bash -n scripts/uat/down.sh` (no syntax errors)
- `docker compose -f scripts/uat/docker-compose.yml -f scripts/uat/docker-compose.watch.yml --profile proxy config -q` (override merges cleanly)
- All grep assertions in the task `<verify>` blocks pass (flags, helpers, routes, healthcheck, compile-env invariant, alias gate).
- `mix test` still green (no test references the scripts; also confirm nothing in `scripts/ci` depends on the old default `none` mode: `grep -rn "up.sh" scripts/ci || true`).

Manual end-to-end checklist (mirrors the plan Verification section — run by maintainer, Docker required, cannot run in CI):
1. `scripts/uat/down.sh && scripts/uat/up.sh` from current branch → image build, `--wait` blocks on health, `wait_for_http` passes, browser auto-opens `/demo/credentials` at a `*.localhost` (ideally `sigra.localhost`) URL that loads immediately.
2. Log in as `admin@demo.vaultr.test`; click each printed ADMIN route → all 200.
3. Edit a `test/example` admin template → hot reload in browser without rebuild; edit `lib/` sigra source → reload too.
4. `scripts/uat/up.sh --dev` → host-run auto-starts, gated, prints live `127.0.0.1:<port>`, loads; `--attach` drops into IEx.
5. Second checkout/branch `up.sh` → distinct `*.localhost`, no collision; `sigra.localhost` held by exactly one.
6. `scripts/uat/down.sh` → containers down AND host-run PID killed; `tmp/uat-phoenix.{pid,log}` cleaned.
7. Re-run `up.sh` after CSS-only change → no dep re-download/recompile (cached), fast.
</verification>

<success_criteria>
- No-flag `scripts/uat/up.sh` boots Dockerized demo behind shared Traefik with live reload, health-gates, auto-opens `/demo/credentials`, prints grouped auth/admin/ops routes.
- URL never shown as live until readiness probe passes (STARTING label otherwise).
- `--dev`/`--attach`/`--no-watch`/`--no-open` flags work; host-run path actually starts + health-gates Phoenix.
- `sigra.localhost` is claim-based (works on feature branches).
- Watch override preserves the compile-env invariant.
- `down.sh` cleans up host-run PID/log.
- Docs updated to the new default. All CI-safe checks pass.
</success_criteria>

<output>
Create `.planning/quick/260619-l1b-demo-docker-dx-proxy-default/260619-l1b-SUMMARY.md` when done.
</output>
