---
quick_id: 260718-sar
title: "Demo-chrome coherence + admin users reorder (3 parts)"
status: complete
completed: 2026-07-18
commits:
  - ad177a3e  # Part A — demo-switch bar above app header
  - f1a562d5  # Part B — dev-gated demo Fill-password band on sudo page
  - 73128103  # Part C — admin users: User health above Find users
---

# Quick 260718-sar Summary

Three atomic, code-only commits landing sequentially on `main` (base `4b408962`).

## Part A — demo-switch bar above the app header (`ad177a3e`)
- `test/example/lib/example_web/components/layouts.ex` (`app/1`): moved
  `<.demo_persona_switch>` to be the FIRST element, above `<header class="vt-app-header">`.
  `<.impersonation_banner>` stays below the header.
- `test/example/priv/static/assets/css/app.css`: corrected the stale `.vt-demo-switch`
  position comment ("below the app header" → "above the app header"). No rule change.

## Part B — dev-gated demo Fill-password band on the sudo page (`f1a562d5`)
- `sudo_controller.ex`: added dev-gated `demo_persona_for/1` (mirrors
  `session_controller.ex`'s `compile_env(:example, :dev_routes)` gate). Passes
  `demo_persona:` on BOTH render paths (`new/2` and the `create/2` failure re-render).
  Also added the missing `form:` to the failure re-render — fixes a latent `@form`
  KeyError crash on wrong-password submit (Rule 1 bug, surfaced by the plan).
- `sudo_html.ex`: added a standalone `vt-demo-switch--login` band as the first element
  (sibling above `<section data-testid="sudo">`), with `data-testid="demo-sudo-hint"`.
  No `<select>`; reuses existing CSS.
- `admin_hooks.js` + `priv/static/assets/js/app.js` (both): broadened
  `installDemoPasswordFill()` selector to also match `input[name="sudo[password]"]`.
- Added `sudo_controller_test.exs`: under `mix test` (`dev_routes=false`), `GET /users/sudo`
  refutes `"Disposable demo account"`, `data-demo-fill-password`, and
  `data-testid="demo-sudo-hint"` (dev-gated → compiled out).

## Part C — admin users: User health above Find users (`73128103`)
- `lib/sigra/admin/live/users_index_live.ex` (`render/1`): moved the three
  `<% total_users/locked_users/deletion_scheduled_users %>` assigns TOGETHER WITH the
  User health `<section>` above the Find users `<section>`. Assigns remain immediately
  above their usage. New order: page header → scope ribbon → User health → Find users →
  results. Spacing preserved by the parent `sg-stack--6` symmetric gap — no CSS touched.
- `test/example/lib/example_web/live/admin/design_gallery_live.ex`: reordered the twin
  `board-cfg-users-list` board to match.

## Verification
- `cd test/example && mix compile --warnings-as-errors` — clean (lib + example).
- `git diff --stat 4b408962 HEAD` — 9 files: `lib/sigra/admin/live/users_index_live.ex`
  plus `test/example/**` only. Scope guard confirmed NO `priv/templates/`, NO
  `test/fixtures/`, NO `*.png` changes.
- Tests (DB up via `tmp/db.env`, `localhost:5432`): `session_controller_test.exs`,
  `sudo_controller_test.exs`, `admin_user_filters_live_test.exs` with `--include example_app`
  → **14 tests, 0 failures**.
- Pre-existing `/dev/mailbox` route warning surfaces under the dev-routes-gated `mix test`
  build in `settings_live.ex` (untouched) — unrelated to this task.

## CI follow-through (deferred, not done here)
Part C shifts two visual-baseline sets that MUST be regenerated by the CI-native recapture
gate (darwin pixel drift makes local recapture invalid — per plan, no `*.png` touched):
- `global-user-index-*` (admin-checkpoints lane)
- `board-cfg-users-list-*` (admin-design lane)

## Self-Check: PASSED
- Commits present: `ad177a3e`, `f1a562d5`, `73128103` (all in `git log`).
- Changed files exist and compile clean; targeted test suite green.
