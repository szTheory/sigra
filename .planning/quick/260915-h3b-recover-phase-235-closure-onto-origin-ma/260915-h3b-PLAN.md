---
quick_id: 260915-h3b
phase: quick-260915-h3b
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [RECOVER-235-CLOSURE]
files_modified:
  - scripts/ci/verify-fast-01-source-complete-attestation-offline.sh
  - test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs
  - test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs
  - .planning/MILESTONE-ARC.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
  - .planning/seeds/SEED-005-ci-cd-pipeline-performance-audit.md
  - .planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-16-EXECUTION-DIAGNOSTICS.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-16-SUMMARY.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-17-SUMMARY.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-18-SUMMARY.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-19-PLAN.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-19-SUMMARY.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT-TRUSTED-ROOT.jsonl
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.attestation.jsonl
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-REVIEW.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-SECURITY.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-UAT.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-UI-REVIEW.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-VALIDATION.md
  - .planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md

estimate:
  tokens: 35000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Branch recover/phase-235-closure carries exactly one content commit on top of origin/main (fb11c8d3)."
    - "That commit's tree differs from origin/main in exactly the 28 authorized paths and in no others."
    - "The three branding todos added by #238 still exist at HEAD — nothing on the branch is a delete."
    - "Every one of the 28 paths at HEAD is byte-identical to its blob at 6496cada."
    - "235-VERIFICATION.md at HEAD reports status: passed."
    - "The local .tool-versions modification and the untracked .gsd/ directory were not swept into the commit."
  artifacts:
    - ".planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md at HEAD"
    - ".planning/phases/235-terminal-ratification-measured-not-read/235-19-PLAN.md at HEAD"
    - "scripts/ci/verify-fast-01-source-complete-attestation-offline.sh at HEAD"
  key_links:
    - "git diff --name-status origin/main HEAD produces zero D entries (the #238 todos survive)."
    - "The 19 non-planning branding/chimeway/template paths that also differ between origin/main and 6496cada are NOT touched — taking them would revert #236/#237/#238."
---

<objective>
Recover Phase 235's stranded closure artifacts from commit `6496cada` (on branch
`ci/phase-235-16-source-complete`) onto a clean squash-ready branch forked from
`origin/main`, leaving behind the 469 later "235.1" commits.

Purpose: Phase 235's passing VERIFICATION, its 235-16..19 summaries, the 235-19 plan,
and the UAT/SECURITY/UI-REVIEW/REVIEW/COVERAGE/VALIDATION artifacts are the only
record that the phase closed. They are stranded on a branch that will never merge.
Output: exactly ONE content commit on `recover/phase-235-closure` containing 28 paths.

This is a git content-recovery operation. It is NOT a code change. Write no new code,
fix no tests, resolve no lint findings in the recovered files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

Repo facts verified at planning time with live git (authoritative — spot-check, do not re-derive):
- `origin/main` = `fb11c8d3` ("fix(branding): add dark_logo_url ... (#238)")
- Recovery source commit = `6496cada` (tip of `ci/phase-235-16-source-complete`)
- merge-base(origin/main, 6496cada) = `c6580d79` (#223)
- Working branch `recover/phase-235-closure` ALREADY EXISTS, is already checked out,
  and was forked from `origin/main` with `--no-track`. Do not create another branch.
- Local `main` is `f9e036d2` and is STALE (19 ahead / 5 behind origin/main). IGNORE it.
  Never check it out, never merge into it, never fast-forward it.
- `git diff --name-status origin/main 6496cada` yields 50 paths: the 28 authorized
  paths (A or M), the 3 branding todos as `D`, and 19 further non-planning source
  paths (branding/chimeway/install-template/golden-fixture) as M or D.

**CRITICAL correction to any earlier framing:** it is NOT true that only 3 non-planning
files differ. 19 additional non-planning paths differ because `origin/main` gained
`#236`/`#237`/`#238` (chimeway + branding work) which `6496cada` predates. Taking
`6496cada`'s version of those would REVERT shipped work. They are hard-excluded.
Consequence: a blanket `git checkout 6496cada -- .` or `git add -A` is a REGRESSION,
not a shortcut. Only the explicit 28-path list may be checked out.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Preflight — assert the recovery preconditions match the authorized scope</name>
  <files>(read-only; no files modified)</files>
  <precondition>Current branch is `recover/phase-235-closure`, its HEAD equals `origin/main` (`fb11c8d3`), and the git index has nothing staged.</precondition>
  <action>
Write the 28 authorized paths to a temp manifest outside the repo so the working tree
stays clean: `TAKE=$(mktemp -d)/take-paths.txt`, populated via a quoted heredoc with
exactly the paths listed in this plan's `files_modified` frontmatter, one per line.

Then assert, halting with a clear report on ANY mismatch:
1. `git rev-parse --abbrev-ref HEAD` == `recover/phase-235-closure`.
2. `git rev-parse HEAD` == `git rev-parse origin/main` == `fb11c8d35611bb5707ca7dc675d5eeeda7e0e78a`.
3. `git diff --cached --name-only` is empty (nothing pre-staged).
4. `git cat-file -e 6496cada^{commit}` succeeds.
5. Every path in the manifest exists at `6496cada` (`git rev-parse -q --verify 6496cada:<path>` for each).
6. The manifest has exactly 28 lines and no duplicates.
7. `git diff --name-status origin/main 6496cada` marks each of the 3 branding todos
   (`.planning/todos/pending/2026-09-15-asymmetric-dark-token-fallback.md`,
   `...-branding-profile-has-no-dark-logo-url.md`,
   `...-demo-brand-presets-split-only-for-logo.md`) as `D`, and none of those 3
   appears in the manifest.

MUTABLE-SCOPE AUTHORITY: the 28-path manifest is authorized as observed at planning
time. If any assertion above fails, STOP, make no commit, and report the exact
divergence. Do not improvise a substitute path list, do not add paths you think are
missing, and do not drop paths that look stale.
  </action>
  <verify>
    <automated>All 7 assertions above print PASS and the manifest file contains 28 lines.</automated>
  </verify>
  <done>Preconditions proven; the 28-path manifest exists at a temp location outside the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Check out the 28 authorized paths from 6496cada and make ONE commit</name>
  <files>the 28 paths in the manifest from Task 1</files>
  <action>
Run `git checkout 6496cada -- $(cat "$TAKE" | tr '\n' ' ')` — or safer, feed the
manifest via `xargs`: `xargs -a "$TAKE" -d '\n' git checkout 6496cada --`. This both
updates the working tree and stages exactly those paths.

FORBIDDEN, no exceptions: `git add -A`, `git add .`, `git add -u`, `git commit -a`,
`git checkout 6496cada -- .`, `git checkout 6496cada` (branch switch), `git merge`,
`git rebase`, `git cherry-pick`, `git reset --hard`, `git stash`, any operation on
local `main`, and any `git push` / `gh pr create`. Do not push. Do not open a PR —
the operator decides that afterward.

Before committing, assert `git diff --cached --name-only | sort` equals
`sort "$TAKE"` exactly. If the staged set is larger or smaller, STOP and report;
in particular `.tool-versions` and anything under `.gsd/` must NOT be staged.

Then make exactly one commit (no `-a`):

  git commit -m "$(cat <<'EOF'
  docs(235): recover phase 235 closure artifacts onto current main

  Recovers the Phase 235 closure record stranded on ci/phase-235-16-source-complete
  (6496cada) without the 469 later 235.1 commits on that branch.

  Takes 25 planning artifacts (235-16..19 summaries, 235-19-PLAN, the passing
  235-VERIFICATION, plus UAT/SECURITY/UI-REVIEW/REVIEW/COVERAGE/VALIDATION and the
  FAST-01 remeasurement evidence) and the 3 superseding non-planning files (the
  offline attestation verifier and the two FAST-01 contract tests, which supersede
  the earlier versions squashed in via #232).

  Deliberately excludes the branding/chimeway source changes that #236/#237/#238
  landed after 6496cada, and preserves the three 2026-09-15 branding todos.

  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  EOF
  )"

Leave the heredoc body unindented when actually running it.
  </action>
  <verify>
    <automated>`git rev-list --count origin/main..HEAD` prints `1`, and `git diff --cached --name-only` is empty after the commit.</automated>
  </verify>
  <done>Exactly one commit sits on top of origin/main on `recover/phase-235-closure`, containing only the 28 authorized paths.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the recovery — run the six acceptance gates and capture output</name>
  <files>(read-only; no files modified)</files>
  <action>
Run all six gates and capture the literal output for the summary. Report a gate as
FAIL rather than working around it; do not amend the commit to "fix" a gate without
reporting what was wrong first.

Gate 1 — exact path set:
  `git diff --name-only origin/main HEAD | sort` equals `sort "$TAKE"` (28 paths,
  nothing else). Explicitly confirm none of the 3 branding todos and no
  branding/chimeway source path appears.

Gate 2 — no deletions:
  `git diff --name-status origin/main HEAD | awk '$1 ~ /^D/'` produces no output.

Gate 3 — byte-identical blobs:
  For every path in the manifest, `git rev-parse HEAD:<path>` equals
  `git rev-parse 6496cada:<path>`. Report a count of 28/28 matches.

Gate 4 — verification status:
  Read `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md`
  at HEAD and quote its frontmatter `status:` and `score:` lines verbatim in the
  summary. Expected: `status: passed`, `score: 11/11 must-haves verified`.

Gate 5 — static syntax checks on the 3 recovered non-planning files:
  `mix format --check-formatted test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs`
  and `bash -n scripts/ci/verify-fast-01-source-complete-attestation-offline.sh`.

Gate 6 — working-tree noise excluded:
  `git status --porcelain` still shows ` M .tool-versions` and `?? .gsd/` as
  uncommitted/untracked.

DO NOT run `mix test`, `mix ci`, `mix credo`, or the recovered contract tests. Per
CLAUDE.md those tests assert against CI-produced evidence and require a live Postgres
(`scripts/db/up.sh`); a red local run here is EXPECTED and is NOT a signal. Gate 5's
static checks are the only Elixir-side verification in scope.
  </action>
  <verify>
    <automated>All six gates report PASS, with gate 3 showing 28/28 blob matches and gate 2 showing zero D entries.</automated>
  </verify>
  <done>Six-gate output captured in the summary, including the quoted VERIFICATION status line. Branch left committed, unpushed, with no PR opened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stranded branch → origin/main lineage | Content from a commit that predates #236/#237/#238 crosses into a branch based on main |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h3b-01 | Tampering | branding/chimeway source files | high | mitigate | Hard path allowlist (28 paths) + Gate 1 exact-set equality; blanket `checkout -- .` explicitly forbidden |
| T-h3b-02 | Denial of Service | the 3 branding todos from #238 | medium | mitigate | Gate 2 asserts zero `D` entries in the branch diff |
| T-h3b-03 | Tampering | local env noise (`.tool-versions`, `.gsd/`) | low | mitigate | Staged-set equality check before commit + Gate 6 after |
| T-h3b-04 | Repudiation | recovered artifact fidelity | medium | mitigate | Gate 3 blob-hash equality against `6496cada` for all 28 paths |
| T-h3b-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
Gates 1-6 in Task 3 are the acceptance gates. No package-manager or test-suite
execution is in scope.
</verification>

<success_criteria>
- One commit on `recover/phase-235-closure` above `origin/main`.
- Diff vs `origin/main` = exactly the 28 authorized paths, zero deletions.
- All 28 blobs byte-identical to `6496cada`.
- 235-VERIFICATION.md at HEAD reads `status: passed`.
- Nothing pushed, no PR opened, local `main` untouched.
</success_criteria>

<output>
Create `.planning/quick/260915-h3b-recover-phase-235-closure-onto-origin-ma/260915-h3b-SUMMARY.md`
when done, including the literal gate output and the quoted VERIFICATION status line.
</output>
