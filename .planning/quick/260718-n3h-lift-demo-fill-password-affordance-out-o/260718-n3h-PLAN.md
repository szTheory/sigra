---
phase: 260718-n3h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/controllers/session_html.ex
  - test/example/priv/static/assets/css/app.css
autonomous: true
requirements: [DEMO-DX]
must_haves:
  truths:
    - "On a dev build, /users/log_in?demo=<persona> renders a full-width demo band at the TOP of the page, visually separate from the login card."
    - "The login card (.vt-auth__panel) no longer contains any demo block wedged between the primary and passkey forms."
    - "A bare /users/log_in (no ?demo=) renders no band and a pristine login card."
    - "Fill password and email-chip copy keep working with zero JS changes (delegated global listeners)."
    - "Under mix test (dev_routes=false) the existing refute test still passes UNCHANGED (dev gate + tokens intact)."
  artifacts:
    - test/example/lib/example_web/controllers/session_html.ex
    - test/example/priv/static/assets/css/app.css
  key_links:
    - "Preserved DOM tokens: data-testid=\"demo-login-hint\", data-demo-fill-password, data-demo-password, code.vt-code.vt-code--copy, copy \"Disposable demo account — never use in production\"."
    - ":if={@demo_persona_hint} dev-gate preserved on the band."
---

<objective>
Lift the dev-only demo "Fill password" affordance out of the login card and render it as a distinct full-width `.vt-demo-switch` band above the centered auth section — mirroring the authenticated app layout's top demo band. This is a pure demo-chrome relocation in the example app.

Purpose: The demo helper currently sits wedged inside `.vt-auth__panel` between the primary login form and the passkey form, making it "look jammed onto the actual login form." Moving it into the same top band used post-login gives one consistent "this is a demo harness" mental model and lets the login card read as the real Sigra sign-in experience.

Output: Relocated demo band in `session_html.ex` (reusing `.vt-demo-switch`) and CSS cleanup in the build-free example `app.css` (dead `.vt-demo-hint` rules removed, optional minimal `--login` modifier).

Scope guardrails (DO NOT exceed): EXAMPLE-ONLY, dev-gated. No installer template change, no golden fixture, no byte-sync. No JS change (`admin_hooks.js` / `app.js` untouched — delegated global listeners). No test change (existing refute test stays green unchanged). Do NOT restructure `.vt-auth` centering.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@test/example/lib/example_web/controllers/session_html.ex
@test/example/lib/example_web/components/layouts.ex
@test/example/priv/static/assets/css/app.css
@test/example/test/example_web/controllers/session_controller_test.exs

Interface facts for the executor (already verified during planning):
- `layouts.ex:138` is the authed-layout precedent: `<div class="vt-demo-switch" data-testid="demo-persona-switch">` renders as a full-width top band, a sibling above page content. The login band mirrors this structure (top-level, full-bleed).
- The example root layout drops template content straight into `<body>`, so a top-level `<div>` emitted before `<section class="vt-auth …">` sits full-bleed above the centered auth section.
- Base `.vt-demo-switch` (app.css:207) already provides `display:flex; flex-wrap; align-items:center; gap; padding; bottom hairline; accent-soft tint` — fits the pill+label+chip+button row as-is.
- `.vt-status-pill` and `.vt-demo-switch__label` classes already exist in app.css.
- Current demo block lives at session_html.ex:93-106 inside `.vt-auth__panel`.
- Dead `.vt-demo-hint` rules to remove are at app.css:1359-1376 (the block comment, `.vt-demo-hint`, `.vt-demo-hint .vt-kicker`, `.vt-demo-hint .vt-copy`).
- The refute test at session_controller_test.exs:113-124 asserts (under dev_routes=false) that "Disposable demo account", "data-demo-fill-password", and `data-testid="demo-login-hint"` do NOT render — these tokens compile out via the `:if` gate, so preserving them verbatim keeps the test green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate demo block into a full-width top band in session_html.ex</name>
  <files>test/example/lib/example_web/controllers/session_html.ex</files>
  <action>
Remove the existing demo block (currently the `<div :if={@demo_persona_hint} class="vt-demo-hint" data-testid="demo-login-hint">…</div>` sitting inside `.vt-auth__panel` between the primary login `.form` and the `passkey_primary_enabled` form).

Emit the relocated band as the FIRST element returned by `new/1`, as a SIBLING ABOVE `<section class="vt-auth vt-auth--login" …>` (top-level in the `~H` template, before the `<section>`). This mirrors the authed layout's band-above-content structure at layouts.ex:138.

The band MUST keep the `:if={@demo_persona_hint}` dev-gate and use class `vt-demo-switch vt-demo-switch--login`. Preserve these tokens VERBATIM so the refute test (session_controller_test.exs:113-124) and the delegated global JS both keep working with zero changes: `data-testid="demo-login-hint"`, `data-demo-fill-password`, `data-demo-password`, the `code` element carrying classes `vt-code vt-code--copy` for the email chip, and the exact copy string "Disposable demo account — never use in production".

Band contents in order: a `span.vt-status-pill` reading `DEMO`; a `span.vt-demo-switch__label` with the "Disposable demo account — never use in production" copy; a `code.vt-code.vt-code--copy` rendering `{@demo_persona_hint.email}`; and a `button` (`type="button"`, class `vt-btn vt-btn--ghost`, attributes `data-demo-fill-password` and `data-demo-password={@demo_persona_hint.password}`) reading `Fill password`.

Do NOT change `admin_hooks.js`, `app.js`, or any test file. Do NOT alter the `.vt-auth` section, its panel, or any auth form.
  </action>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors 2>&1 | tail -5 && ! grep -q "vt-demo-hint" lib/example_web/controllers/session_html.ex && grep -q 'vt-demo-switch--login' lib/example_web/controllers/session_html.ex && grep -q 'data-testid="demo-login-hint"' lib/example_web/controllers/session_html.ex</automated>
  </verify>
  <done>Compile is clean under --warnings-as-errors; `vt-demo-hint` no longer appears in session_html.ex; the `vt-demo-switch--login` band with `data-testid="demo-login-hint"`, `data-demo-fill-password`, `data-demo-password`, the `vt-code vt-code--copy` email chip, and the "Disposable demo account — never use in production" copy is emitted as a sibling above `<section class="vt-auth …">`, gated by `:if={@demo_persona_hint}`.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead .vt-demo-hint CSS and add minimal --login modifier</name>
  <files>test/example/priv/static/assets/css/app.css</files>
  <action>
Delete the now-dead `.vt-demo-hint` rules at app.css:1359-1376: the block comment ("Dev-only \"Fill password\" hint block …"), the `.vt-demo-hint` rule, `.vt-demo-hint .vt-kicker`, and `.vt-demo-hint .vt-copy`. The `.vt-demo-hint` class is no longer emitted anywhere after Task 1.

Reuse the existing base `.vt-demo-switch` rule (app.css:207) as-is. Add a small `.vt-demo-switch--login` modifier ONLY for login-context layout — prefer `justify-content: center` so the band content aligns under the centered login card. Keep it minimal (a single small rule); do not duplicate base declarations. Place the modifier adjacent to the base `.vt-demo-switch` rules (near app.css:207-222) for locality.

Do NOT restructure `.vt-auth` centering (app.css around 1151) — a short top band above the section is acceptable; a dev-only minor scroll is fine.
  </action>
  <verify>
    <automated>cd test/example && test "$(grep -c 'vt-demo-hint' priv/static/assets/css/app.css)" = "0" && grep -q '.vt-demo-switch--login' priv/static/assets/css/app.css</automated>
  </verify>
  <done>`grep -c "vt-demo-hint" test/example/priv/static/assets/css/app.css` returns 0 (dead rules and their comment removed); a minimal `.vt-demo-switch--login` modifier exists; the base `.vt-demo-switch` rule and `.vt-auth` centering are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change relocates existing dev-gated demo markup within the example app and edits example-only CSS. No new trust boundary is crossed; no auth logic, tokens, or server behavior changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260718-n3h-01 | Information Disclosure | Demo password/email band leaking into a production build | low | mitigate | The band retains the existing `:if={@demo_persona_hint}` dev-gate (dev_routes compile_env); the refute test at session_controller_test.exs:113-124 proves the demo tokens compile out under dev_routes=false. No change to the gate. |
</threat_model>

<verification>
Executor-run (fast, no browser):
- `cd test/example && mix compile --warnings-as-errors` is clean.
- `cd test/example && mix test test/example_web/controllers/session_controller_test.exs` is green — the refute test passes UNCHANGED, proving the dev gate holds and the preserved tokens are intact.
- `git diff --name-only` shows ONLY `test/example/lib/example_web/controllers/session_html.ex` and `test/example/priv/static/assets/css/app.css` changed — no diff in `admin_hooks.js`, `app.js`, or any test file.
- `grep -c "vt-demo-hint" test/example/priv/static/assets/css/app.css` returns 0; `vt-demo-hint` no longer appears in `session_html.ex`.

Orchestrator-run live verification (AFTER execution — NOT required of the executor; dev_routes is false under `mix test`, so the band only renders in a dev build via `scripts/uat/up.sh`):
1. From the home front door, click a persona (e.g. Alice) → lands on `/users/log_in?demo=alice`. A full-width demo band renders at the TOP of the page (DEMO pill + "Disposable demo account — never use in production" + copyable `alice@demo.tasklane.test` chip + Fill password), visually distinct from the login card.
2. The login card below is clean — no demo block wedged between the login and passkey forms.
3. Fill password still populates the password field (delegated JS unchanged); clicking the email chip still copies (toast "Copied").
4. A bare `/users/log_in` (no `?demo=`) shows no band and the pristine card.
</verification>

<success_criteria>
- Demo helper renders as a full-width `.vt-demo-switch vt-demo-switch--login` band above the auth section on dev builds with `@demo_persona_hint` present; login card is clean.
- `mix compile --warnings-as-errors` clean; `session_controller_test.exs` green unchanged.
- Only the two target files changed; JS and tests untouched.
- Dead `.vt-demo-hint` CSS removed (grep count 0); `.vt-auth` centering and base `.vt-demo-switch` rule unchanged.
</success_criteria>

<output>
Create `.planning/quick/260718-n3h-lift-demo-fill-password-affordance-out-o/260718-n3h-SUMMARY.md` when done.
</output>
