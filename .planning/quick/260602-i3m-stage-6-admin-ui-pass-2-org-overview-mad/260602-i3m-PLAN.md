---
phase: quick-260602-i3m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/organizations/detail.ex
  - test/sigra/admin/organizations_detail_test.exs
  - lib/sigra/admin/live/organization_live.ex
  - test/example/lib/example/demo/personas.ex
  - test/example/lib/example/demo/seeds.ex
  - test/example/test/example/demo/personas_test.exs
  - test/example/lib/example_web/live/demo/credentials_live.ex
  - test/example/priv/static/assets/css/app.css
autonomous: true
requirements: [ADMIN-UI-PASS2-STAGE6]

must_haves:
  truths:
    - "A new org-scoped data-layer module returns the member roster (user + role + confirmed/locked status) for the admin scope's organization and EXCLUDES other orgs' members (fails closed)."
    - "The same module returns only PENDING invitations (accepted_at IS NULL AND revoked_at IS NULL) for the org and flags expired ones (expires_at < now)."
    - "The module resolves host schemas by namespace inference (optional_schema) and returns [] gracefully when membership/invitation schema is absent."
    - "Library unit tests for roster + pending invitations pass under root `mix test`."
    - "The org overview (organization_live.ex) renders a Members roster section and a Pending invitations section, each with a teaching empty state."
    - "The pinned substring 'Work inside this organization scope' and the existing jobs-first structure are preserved."
    - "A Morgan org-admin persona is seeded as an :admin member of Acme Corp and can log in (non-platform-admin)."
    - "At least one persona (admin) has 2-3 seeded UserSession rows with distinct IPs and staggered last_active_at so the detail Sessions table renders populated."
    - "Seeds remain idempotent and deterministic; the existing personas and the golden-path CI fixture segregation are unbroken."
    - "demo-showcase Playwright subset checks still pass (Morgan is additive, not in DEMO_LOCALS)."
    - "`mix compile --warnings-as-errors` is clean on root and example."
  artifacts:
    - path: "lib/sigra/admin/organizations/detail.ex"
      provides: "member_roster/2 + pending_invitations/2, org-scoped, fails closed, schema-inferred"
      contains: "def member_roster"
    - path: "test/sigra/admin/organizations_detail_test.exs"
      provides: "Library unit tests mirroring users_query_test.exs (PostgresRepo)"
      contains: "use Sigra.Test.PostgresCase"
    - path: "lib/sigra/admin/live/organization_live.ex"
      provides: "Members roster + pending invitations rendering"
      contains: "Members"
    - path: "test/example/lib/example/demo/personas.ex"
      provides: "Morgan org-admin persona (7th)"
      contains: "morgan@demo.sigra.dev"
    - path: "test/example/lib/example/demo/seeds.ex"
      provides: "Morgan Acme admin membership + multi-session seed"
      contains: "morgan@demo.sigra.dev"
  key_links:
    - from: "lib/sigra/admin/live/organization_live.ex"
      to: "Sigra.Admin.Organizations.Detail"
      via: "mount loads member_roster/2 + pending_invitations/2"
      pattern: "Organizations.Detail"
    - from: "lib/sigra/admin/organizations/detail.ex"
      to: "Authorizer.authorize_organization!"
      via: "org-scope enforcement before query"
      pattern: "authorize_organization!"
    - from: "test/example/lib/example/demo/seeds.ex"
      to: "Example.Accounts.OrganizationMembership"
      via: "Morgan :admin membership of Acme"
      pattern: "morgan"
---

<objective>
Make the admin org overview a REAL screen. Add a library org-scoped data-layer module
(member roster + pending invitations), render both sections in `organization_live.ex`,
seed a Morgan org-admin persona + multiple active sessions, and add library unit tests.

Purpose: Stage 6 of the approved Admin-UI Pass 2 plan — the least-built path (Morgan's
org-admin journey). After this stage, the org overview shows who is in the org and what
invitations are outstanding, and the demo DB has a real org-admin login plus populated
session evidence.

Output:
- NEW `lib/sigra/admin/organizations/detail.ex` (roster + pending invitations)
- NEW `test/sigra/admin/organizations_detail_test.exs` (library unit tests)
- Extended `lib/sigra/admin/live/organization_live.ex` (Members + Pending invitations)
- Morgan persona + multi-session enrichment in example seeds (+ updated persona ExUnit + cosmetic docs)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contracts extracted from the codebase. Executor: use these directly, no exploration. -->

GROUND TRUTH — schema resolution seam (mirror exactly from Users.Query.helpers/1 and
Users.Detail.helpers/1, lib/sigra/admin/users/{query,detail}.ex):

  accounts_module(config):
    - config.accounts_module (atom) || config.accounts (atom)
    - || parent-namespace-of(config.user_schema): Module.split |> Enum.drop(-1) |> Module.safe_concat (rescue ArgumentError -> nil)
  optional_schema(nil, _name) -> nil
  optional_schema(module, name):
    schema = Module.concat(module, name)
    if Code.ensure_loaded?(schema), do: schema, else: nil
  membership_schema = config[:membership_schema] || optional_schema(accounts_module, :OrganizationMembership)
  organization_schema = config[:organization_schema] || optional_schema(accounts_module, :Organization)
  invitation_schema  = optional_schema(accounts_module, :OrganizationInvitation)   # NEW resolution
  config.repo is the Repo. config.user_schema is the user schema.

Sigra.Admin.Scope (lib/sigra/admin/scope.ex):
  %Scope{mode: :global | :organization, organization_id, organization_slug, organization, ...}
  Scope.global?/1, Scope.organization?/1

Sigra.Admin.Authorizer (lib/sigra/admin/authorizer.ex):
  authorize_organization!(%Scope{}, org_id_or_map) :: :ok  (raises UnauthorizedError otherwise)
  — org-scope MUST call this with admin_scope.organization_id before querying.

Example schemas (test/example/lib/example/accounts/):
  OrganizationMembership: field :role, Ecto.Enum [:owner,:admin,:member];
    belongs_to :organization; belongs_to :user.
  OrganizationInvitation: :email, :role (Ecto.Enum [:owner,:admin,:member]),
    :hashed_token, :accepted_at, :revoked_at, :expires_at;
    belongs_to :organization, :invited_by, :accepted_by, :revoked_by.
    PENDING = accepted_at IS NULL AND revoked_at IS NULL. EXPIRED = expires_at < now.
  User: :id, :email, :display_name, :confirmed_at, :locked_at, :deleted_at.
  UserSession (table user_sessions): :hashed_token (binary, REQUIRED), :type (default "standard"),
    :ip, :user_agent, :geo_city, :geo_country_code, :last_active_at (:utc_datetime_usec),
    :active_organization_id, belongs_to :user. timestamps updated_at:false (inserted_at only).
    Session store (Sigra.SessionStores.Ecto.list_by_user) orders by inserted_at DESC.

organization_live.ex current shape (Stage 2 reshaped):
  mount/3 reads socket.assigns.admin_scope, builds config via runtime_config!(),
  assigns :sigra_config, :summary_counts (Query.summary_counts/2), :organization_name, :page_title.
  render/1: header (h1 @organization_name + pinned copy "Work inside this organization scope...")
    -> sg-grid--2 task_card x2 (Support members / Investigate org events)
    -> "Scoped attention" sg-card -> sg-posture-strip (risk pill + metric_link strip + capability line).
  PRESERVE all of the above. ADD roster + pending-invitations sections AFTER the posture content.

Library test harness (test/sigra/admin/users_query_test.exs is THE pattern):
  use Sigra.Test.PostgresCase, async: false ; @repo Sigra.Test.PostgresRepo.
  setup_all: Sigra.Test.PostgresCase.checkout_repo!(fn repo -> CREATE TABLE IF NOT EXISTS ... end).
  Define test-local Ecto schemas (admin_query_* style table names) + struct! |> @repo.insert!.
  Build %Scope{} structs inline (mode: :organization | :global). config is a plain map.

CONTRACT WATCH (these WILL break if Morgan is added naively — handle in same task):
  test/example/test/example/demo/personas_test.exs asserts:
    - length(Personas.all()) == 6
    - @expected_handles ~w(admin alice bob carol dave frank)  (sorted handle equality)
  test/example/lib/example_web/live/demo/credentials_live.ex moduledoc says "all six demo persona".
  test/example/lib/example/demo/personas.ex moduledoc says "six demo personas".
  demo-showcase.spec.ts uses DEMO_LOCALS=['admin','alice','bob','carol','dave','frank'] +
    DEMO_EMAILS subset toBeVisible() — Morgan additive is SAFE for Playwright (no count assert).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Org-scoped data-layer module (roster + pending invitations)</name>
  <files>lib/sigra/admin/organizations/detail.ex</files>
  <action>
Create `Sigra.Admin.Organizations.Detail` as a clean mirror of `Sigra.Admin.Users.Detail`
style (moduledoc, @spec on public fns, private `helpers/1` + `accounts_module/1` +
`optional_schema/2` adapted from the Users modules — do NOT modify Users.Query/Detail).

Public API (org-scoped only — NO global-scope behavior needed; these surfaces are
inherently per-org):

  @spec member_roster(map(), Scope.t()) :: [map()]
  def member_roster(config, %Scope{} = admin_scope)
    - Resolve membership_schema + user_schema via helpers/1.
    - If membership_schema is nil -> return [] (legacy installs; fail soft, do not raise).
    - Authorize: Authorizer.authorize_organization!(admin_scope, admin_scope.organization_id).
    - Query memberships WHERE organization_id == admin_scope.organization_id, join the user,
      select a row map:
        %{user: user_struct, role: membership.role,
          confirmed?: not is_nil(user.confirmed_at),
          locked?: not is_nil(user.locked_at),
          display_name: user.display_name || user.email}
      (Select the full user struct so the LiveView can read email/display_name — mirror how
       Users.Query selects `user: user`.) Do NOT over-build: a role+status roster is the goal;
       only add a cheap mfa left-join if it mirrors Query.maybe_join_mfa_summary trivially —
       otherwise skip it.
    - Order deterministically: owners -> admins -> members, then display_name/email asc.
      Roles are Ecto.Enum stored as text; build a role-rank ordering (e.g. an Ecto
      `fragment` CASE on the role text, or fetch+sort in Elixir if cleaner) ranking
      owner=0, admin=1, member=2, then by downcased coalesce(display_name, email).

  @spec pending_invitations(map(), Scope.t()) :: [map()]
  def pending_invitations(config, %Scope{} = admin_scope)
    - Resolve invitation_schema = optional_schema(accounts_module(config), :OrganizationInvitation).
    - If nil -> return [] (legacy installs).
    - Authorize: Authorizer.authorize_organization!(admin_scope, admin_scope.organization_id).
    - now = DateTime.utc_now(). Query invitations WHERE organization_id == org_id
      AND is_nil(accepted_at) AND is_nil(revoked_at). Order by expires_at asc, then email asc.
    - Shape rows AFTER loading (compute expired? in Elixir against `now` to avoid DB-time skew):
        %{email: i.email, role: i.role, expires_at: i.expires_at,
          expired?: not is_nil(i.expires_at) and DateTime.compare(i.expires_at, now) == :lt}

FAIL CLOSED: every query MUST filter by admin_scope.organization_id and call
authorize_organization! first; never return rows from another org.

Keep `import Ecto.Query`; alias Sigra.Admin.{Authorizer, Scope}. Only copy the helper
privates you actually use so `mix compile --warnings-as-errors` stays clean (no unused fns).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>Module compiles warnings-clean; exports member_roster/2 and pending_invitations/2; both authorize the org and filter by organization_id; both return [] when their schema is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Library unit tests for the org data-layer module</name>
  <files>test/sigra/admin/organizations_detail_test.exs</files>
  <behavior>
    - member_roster returns the scoped org's members with their roles and EXCLUDES another org's members (fails closed).
    - member_roster carries confirmed?/locked? flags derived from the user row.
    - member_roster orders owners -> admins -> members.
    - pending_invitations returns ONLY pending rows (excludes accepted_at-set and revoked_at-set rows).
    - pending_invitations flags expired? true when expires_at < now and false when in the future.
    - Empty results: member_roster and pending_invitations return [] for an org with no members/invites.
    - Graceful absence: a config whose accounts module does not namespace an invitation schema -> pending_invitations returns [] (no raise).
  </behavior>
  <action>
Mirror `test/sigra/admin/users_query_test.exs` exactly for harness shape:
  use Sigra.Test.PostgresCase, async: false ; @repo Sigra.Test.PostgresRepo.
  setup_all uses Sigra.Test.PostgresCase.checkout_repo!/1 to CREATE TABLE IF NOT EXISTS for
  test-local tables (e.g. admin_org_detail_users, admin_org_detail_organizations,
  admin_org_detail_memberships, admin_org_detail_invitations). Define matching test-local
  Ecto schemas inside the test module (binary_id PKs; role as :string on membership/invitation;
  user with email/confirmed_at/locked_at/display_name; invitation with
  email/role/accepted_at/revoked_at/expires_at/organization_id).

  Schema-resolution mechanism: to exercise the production optional_schema invitation path,
  define a test `defmodule Accounts` (mirror users_query_test) and namespace the invitation
  schema under it so Module.concat(Accounts, :OrganizationInvitation) resolves; set
  config.accounts_module to that test Accounts module. Pass membership_schema explicitly via
  config (matching how Task 1 resolves it). Keep the SAME resolution contract the module uses.
  For the graceful-absence test, use a config whose accounts_module has NO OrganizationInvitation
  child and assert pending_invitations returns [].

  setup inserts: org1 + org2; 3 users with mixed confirmed/locked; memberships in org1
  covering at least one owner, one admin, one member, and a member in org2; invitations for
  org1 — one pending+future, one pending+expired, one accepted (accepted_at set), one revoked
  (revoked_at set); plus a pending invite for org2. Build org1_scope and org2_scope
  (%Scope{mode: :organization, organization_id: orgN.id, ..., platform_admin?: false,
  admin_org_ids: [orgN.id]}).

  Assertions implement the behavior list. Fail-closed: assert org1_scope roster contains ONLY
  org1 member user_ids (never org2's) and org1 pending_invitations contains ONLY org1's pending
  invites (never org2's, never the accepted/revoked ones). For expiry, use a clearly-past
  ~U[2020-01-01 00:00:00Z] expires_at (asserts expired? == true) and a clearly-future
  ~U[2099-01-01 00:00:00Z] (asserts expired? == false) so the test is stable against wall-clock
  (the module calls DateTime.utc_now() internally). Use deterministic ~U[...] timestamps in all
  fixtures (no DateTime.utc_now() in setup).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres PGPASSWORD=postgres mix test test/sigra/admin/organizations_detail_test.exs 2>&1 | tail -20</automated>
  </verify>
  <done>New test file green under root mix test; covers roster scope-isolation (fail closed), status flags, role ordering, pending-only invitations, expired flagging, empty results, and graceful schema absence.</done>
</task>

<task type="auto">
  <name>Task 3: Render Members roster + Pending invitations in the org overview</name>
  <files>lib/sigra/admin/live/organization_live.ex</files>
  <action>
Extend `Sigra.Admin.Live.OrganizationLive`:

  alias Sigra.Admin.Organizations.Detail (alongside existing aliases).

  In mount/3, after the existing assigns, load and assign:
    :members (Detail.member_roster(config, admin_scope))
    :pending_invitations (Detail.pending_invitations(config, admin_scope))
  (Stage 2 mount has no handle_params; loading in mount is correct here.)

  In render/1, ADD two new sections AFTER the existing sg-posture-strip section, INSIDE the
  outer `<section class="sg-stack sg-stack--6">`. PRESERVE the header pinned copy
  "Work inside this organization scope...", the two task_cards, the "Scoped attention" card,
  and the posture strip exactly as they are.

  Members section (sg-card sg-stack):
    - h2 "Members" with the existing `sg-section-heading` class.
    - If @members == [] -> teaching empty state: <p class="sg-section-copy">No members yet —
      invite teammates to populate this organization.</p> (or equivalent next-action copy).
    - Else render an sg-list (reuse sg-list / sg-list-row already used in the "Scoped attention"
      card) — one row per member showing display_name/email, a role pill, and confirmed/locked
      status pills. Role pill = <span class="sg-status-pill" data-tone={...}>Owner|Admin|Member.
      Tone mapping reuses EXISTING tones: owner/admin -> "info" (brand-ish), member -> "ok"/neutral;
      the Stage-0 status-redundancy glyph is keyed off data-tone via ::before, so set data-tone
      correctly and add no per-pill glyph markup. Status pills: confirmed -> data-tone "ok"
      "Confirmed", unconfirmed -> data-tone "warn" "Unconfirmed"; locked -> data-tone "risk" "Locked".

  Pending invitations section (sg-card sg-stack):
    - h2 "Pending invitations" (sg-section-heading).
    - If @pending_invitations == [] -> teaching empty state:
      <p class="sg-section-copy">No pending invitations.</p>
    - Else an sg-list, one row per invite: invited email, a role pill (same mapping), and expiry —
      "Expires <formatted date>" normally, OR a data-tone="risk" pill reading "Expired" when
      row.expired? is true. Format expires_at with a small private helper
      (Calendar.strftime(dt, "%Y-%m-%d"), guarded for nil).

  Add private function-component(s)/helpers as needed (mirror metric_link/task_card style with
  attr declarations). Keep grammar consistent with the other admin screens (noun headings).
  No JS. No new !important. Prefer reusing sg-list / sg-list-row / sg-status-pill /
  sg-meta-label / sg-meta-value; only add CSS in Task 4 if a primitive is genuinely missing.

  Keep `mix compile --warnings-as-errors` clean. PRESERVE pinned text this view owns
  ("Organization overview", @organization_name, the users/audit hrefs,
  "Work inside this organization scope").
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -3 && cd /Users/jon/projects/sigra/test/example && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres PGPASSWORD=postgres mix test test/example_web/admin_shell_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>Org overview compiles warnings-clean; renders Members + Pending invitations with teaching empty states; admin_shell_test.exs still green; "Work inside this organization scope" preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Seed enrichment — Morgan org-admin persona + multi-session + contract updates</name>
  <files>test/example/lib/example/demo/personas.ex, test/example/lib/example/demo/seeds.ex, test/example/test/example/demo/personas_test.exs, test/example/lib/example_web/live/demo/credentials_live.ex, test/example/priv/static/assets/css/app.css</files>
  <action>
1) Morgan persona (personas.ex):
   - Add a 7th persona map to `Personas.all()`:
       email: "morgan@demo.sigra.dev", display_name: "Morgan (org admin)",
       password: a policy-passing public demo credential (12+ chars, mixed case, digit, symbol —
         e.g. "MorganDemo1!OrgAdmin"), confirmed: true, totp: false, passkey: false,
         locked: false, scheduled_deletion: false, identity_github: false,
         org_owner: nil, org_member: nil.
   - Extend the persona schema with a NEW optional key `org_admin:` (org handle atom or nil) and
     set Morgan's to `:acme`. Add `org_admin: nil` to ALL existing 6 personas so the map shape
     stays uniform (and document the new key in the moduledoc's persona-map key list).
   - Add a feature_map/0 entry "morgan" => "Org admin — Acme Corp admin, non-platform, org-scoped console".
   - Update moduledoc "six demo personas" -> "seven demo personas".

2) Seeds (seeds.ex):
   - seed_memberships/3: after the existing Acme block, seed Morgan's Acme membership at :admin.
     Read morgan = users["morgan@demo.sigra.dev"] and call
     upsert_membership(morgan.id, acme.id, :admin). (Use the org_admin persona key as the source
     of truth if you wire a generic loop, but a direct line mirroring the existing admin/alice/
     carol lines is acceptable and clearest.) Keep idempotent (upsert_membership already uses
     on_conflict: :nothing, conflict_target [:user_id, :organization_id]).
   - NEW seed_sessions/1 step (call it from run/0 after seed_passkey or seed_memberships):
     seed 3 Example.Accounts.UserSession rows for the admin persona with DISTINCT ips and
     staggered last_active_at, idempotently. Each row needs a REQUIRED hashed_token (binary) —
     derive a deterministic per-session token, e.g.
       :crypto.hash(:sha256, "sigra-demo-admin-session-" <> n)
     so re-runs are stable. Fields: user_id, hashed_token, type "standard", ip (e.g.
     "203.0.113.10" / "198.51.100.22" / "192.0.2.44"), user_agent (a couple distinct UAs),
     last_active_at (fixed ~U[...] staggered, e.g. anchored off @seed_reference_ts minus 0/1/3 days),
     inserted_at (fixed ~U[...] matching/near last_active_at). Idempotency: check-then-insert per
     hashed_token (Repo.get_by(UserSession, hashed_token: ht)) OR
     Repo.insert(..., on_conflict: :nothing, conflict_target: [:hashed_token]) — confirm a unique
     index exists on hashed_token before relying on conflict_target; if unsure use check-then-insert.
     Note: UserSession timestamps use updated_at:false — only set inserted_at, never updated_at.
     alias Example.Accounts.UserSession at the top with the other aliases.
   - Update seeds.ex moduledoc enumerations that say "six ... personas" to "seven ...".
   - (OPTIONAL, include ONLY if clean and idempotent) a fresh unconfirmed-not-locked persona or an
     identity-unlink / mfa-disable audit row. If it risks bloating or the demo-showcase subset,
     DEFER and note it in the SUMMARY. Do NOT add it if it complicates the count-threshold audit
     guard — the audit guard compares against length(@audit_actions)+length(@persona_audit_events);
     if you add persona audit rows you MUST keep that guard arithmetic correct.

3) Contract updates (REQUIRED — these break otherwise):
   - test/example/test/example/demo/personas_test.exs:
     change `assert length(personas) == 6` -> `== 7`; add "morgan" to
     `@expected_handles ~w(admin alice bob carol dave frank morgan)`.
     (The display_name/password/domain/policy loop tests already iterate all/0 — Morgan's
     policy-passing password and @demo.sigra.dev email satisfy them; verify no new failure.)
   - credentials_live.ex moduledoc: "all six demo persona" -> "all seven demo persona".

4) CSS (app.css): ONLY if Task 3 needed a missing primitive. Additive sg-* tokens, no new
   !important, mobile-first. If Task 3 reused existing primitives, leave app.css unchanged.

Reseed must work end-to-end. Keep everything deterministic (fixed ~U[...], no DateTime.utc_now()
in seed data) and idempotent (a second run/0 produces identical counts).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres PGPASSWORD=postgres mix test test/example/demo/personas_test.exs 2>&1 | tail -12 && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres PGPASSWORD=postgres PGDATABASE=example_dev mix ecto.reset 2>&1 | tail -8</automated>
  </verify>
  <done>Morgan persona present (7 total) as Acme :admin; admin has 2-3 seeded sessions with distinct IPs; personas_test.exs updated to 7 and green; reseed (ecto.reset) succeeds and is idempotent; moduledocs say "seven"; warnings-as-errors clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org-admin LiveView -> org data layer | An org-admin (Morgan) must see ONLY their org's members/invitations; cross-org leakage is the core risk. |
| seeds -> demo DB | Seeds must never touch the CI golden-path fixture DB (domain segregation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-i3m-01 | Information Disclosure | Organizations.Detail.member_roster/2 + pending_invitations/2 | mitigate | Call Authorizer.authorize_organization!(scope, scope.organization_id) before querying AND filter every query by organization_id; Task 2 asserts org2 rows are excluded from org1 scope (fail closed). |
| T-i3m-02 | Elevation of Privilege | Morgan persona authz | accept | Morgan is seeded as a real :admin membership of Acme; org-admin access for an :admin/:owner is existing authorizer behavior — no new authz code, no platform-admin grant. |
| T-i3m-03 | Tampering | Example seeds vs CI fixture DB | mitigate | All personas use @demo.sigra.dev; seeds.exs carries the MIX_ENV==:test raise-guard; seed steps stay idempotent + deterministic. |
| T-i3m-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this stage; N/A. |
</threat_model>

<verification>
Local Postgres on 5432 (postgres/postgres). Gates the orchestrator will run after merge:
1. Root library tests: `mix test test/sigra/admin/organizations_detail_test.exs` green; existing
   library admin tests still green.
2. Example admin contracts: `cd test/example && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres
   PGPASSWORD=postgres mix test test/example_web/live/admin_*_live_test.exs
   test/example_web/admin_shell_test.exs` green.
3. Persona catalog: `cd test/example && ... mix test test/example/demo/personas_test.exs` green (7 personas).
4. Reseed: `PGDATABASE=example_dev mix ecto.reset` succeeds; run twice for idempotency.
5. `mix compile --warnings-as-errors` clean (root + example).
6. demo-showcase Playwright remains green (Morgan additive; baselines shift -> Stage 8 refresh).
</verification>

<success_criteria>
- New `Sigra.Admin.Organizations.Detail` returns org-scoped roster + pending invitations, fails
  closed, schema-inferred, [] when schema absent.
- Library unit tests green under root `mix test`.
- Org overview renders Members roster + Pending invitations with teaching empty states; pinned
  "Work inside this organization scope" preserved; jobs-first structure intact.
- Morgan org-admin persona seeded as Acme :admin (7 personas total); admin has 2-3 sessions with
  distinct IPs + staggered last_active_at.
- personas_test.exs updated (6 -> 7, morgan handle) and green; moduledocs say "seven".
- All existing gates green; `mix compile --warnings-as-errors` clean; demo-showcase subset unbroken.
</success_criteria>

<flags>
- Library-owned changes (organizations/detail.ex + organization_live.ex) require a server RESTART
  to view in the running demo (path-dep is not hot-reloaded). app.css (if touched) is static.
- demo-showcase + admin-checkpoint baselines WILL shift from the new org-overview sections + the
  7th persona; baseline refresh is deferred to Stage 8 (do not regenerate snapshots here).
- GROUND-TRUTH CORRECTION (baked into Task 4): the orchestrator's note "No ExUnit asserts an exact
  persona count" is INCORRECT — test/example/test/example/demo/personas_test.exs DOES assert
  `length == 6` and pins handles. Task 4 updates it to 7 + adds the morgan handle in the same change.
- Any OPTIONAL seed extras (fresh-unconfirmed persona / extra audit rows) that risk bloat or the
  demo-showcase subset are to be DEFERRED and noted in the SUMMARY, per the scope's optional clause.
</flags>

<output>
Create `.planning/quick/260602-i3m-stage-6-admin-ui-pass-2-org-overview-mad/260602-i3m-SUMMARY.md` when done
</output>
