---
phase: 260718-oa1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/controllers/page_html/home.html.heex
  - test/example/lib/example_web/controllers/page_controller.ex
  - test/example/test/example_web/controllers/page_controller_test.exs
  - test/example/priv/static/assets/css/app.css
  - test/example/lib/example_web/live/demo/credentials_live.ex
  - test/example/test/example_web/live/demo/credentials_live_test.exs
  - test/example/lib/example_web/controllers/session_controller.ex
  - test/example/lib/example_web/controllers/session_html.ex
  - test/example/assets/js/admin_hooks.js
  - test/example/priv/static/assets/js/app.js
autonomous: true
requirements: [DEMO-FRONTDOOR]
must_haves:
  truths:
    - "Home shows two operator pickers + a 'View all 10 personas →' escape hatch; the 'Choose an evaluator persona' card is gone."
    - "/demo/credentials shows one 'Sign in as X' button per persona row (no raw email/password columns)."
    - "/users/log_in in a dev build renders a low-noise 'Switch persona…' dropdown; choosing a persona navigates to /users/log_in?demo=<key>."
    - "Every path routes through the REAL prefilled login (?demo=<key>) — no bypass/auto-submit."
  artifacts:
    - test/example/lib/example_web/controllers/page_html/home.html.heex
    - test/example/lib/example_web/live/demo/credentials_live.ex
    - test/example/lib/example_web/controllers/session_html.ex
    - test/example/assets/js/admin_hooks.js
    - test/example/priv/static/assets/js/app.js
  key_links:
    - "session_html demo band gate flips from :if={@demo_persona_hint} to :if={@demo_personas != []} while credential+Fill parts stay conditional on @demo_persona_hint (keeps non-dev refutes green)."
    - "installDemoPersonaSwitch() present + registered in BOTH admin_hooks.js and served app.js (hand-maintained mirror)."
    - "All changes example-only — no priv/templates/, no test/fixtures/."
---

<objective>
Consolidate the demo front-door across three surfaces so the fast path is dead simple and the full roster is one low-friction click away. The design is ALREADY APPROVED — this plan formalizes it verbatim; do NOT redesign or add scope.

- Home = two operator pickers + a "View all personas" escape hatch; delete the evaluator-persona card.
- /demo/credentials = one "Sign in as X" button per persona (credentials filled at login, not listed here).
- /users/log_in = a dev-only, low-noise persona-switcher dropdown.

Everything routes through the REAL prefilled login (`/users/log_in?demo=<key>`), never a bypass/auto-submit — auth is the product.

Purpose: reduce demo-front-door noise per Jon's direction; continue the demo-DX slice (260718 front-door family).
Output: ~10 example-only files edited across 3 committed parts (home, credentials, login-switcher). No installer template / golden fixture touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Approved source-of-truth plan (formalized here — reproduce faithfully):
@$HOME/.claude/plans/whats-our-roadmap-gsd-indexed-narwhal.md

# Files edited by this plan (read before editing):
@test/example/lib/example_web/controllers/page_html/home.html.heex
@test/example/lib/example_web/controllers/page_controller.ex
@test/example/test/example_web/controllers/page_controller_test.exs
@test/example/lib/example_web/live/demo/credentials_live.ex
@test/example/lib/example_web/controllers/session_controller.ex
@test/example/lib/example_web/controllers/session_html.ex
@test/example/assets/js/admin_hooks.js
@test/example/priv/static/assets/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1 (COMMIT: Part A — Home): delete evaluator card, move "View all" into operator aside</name>
  <files>test/example/lib/example_web/controllers/page_html/home.html.heex, test/example/lib/example_web/controllers/page_controller.ex, test/example/test/example_web/controllers/page_controller_test.exs, test/example/priv/static/assets/css/app.css</files>
  <action>
In home.html.heex: DELETE the entire `<section id="get-started">…</section>` (the `.vt-panel` with the "Choose an evaluator persona" heading and the `@featured_credentials` `:for` block — roughly lines 104-139). MOVE the `id="get-started"` attribute onto the operator `<aside>` (the aside that already carries `data-testid="home-shared-login-copy"`, roughly line 81) so the hero CTA anchor `href="#get-started"` ("Get started — pick a persona") keeps pointing at the picker and the home test's `id="get-started"` assertion survives. ADD a "View all N personas →" ghost button INSIDE the aside, positioned after the context `<p>` and before `<ul class="vt-operator-list">`: an `<a href="/demo/credentials" class="vt-btn vt-btn--ghost vt-btn--block">View all 10 personas →</a>` (use the same persona count the roster actually renders — confirm against `Example.Demo.Personas.all()`; adjust the "10" if the real count differs).

In page_controller.ex: REMOVE the `featured_credentials` computation and its assign (and the now-unused `feature_map` lookup only if it is used nowhere else). KEEP all other assigns (`demo_domain`, `local_origin`, branding, etc.).

In page_controller_test.exs: update the `GET /` test to the new IA (legitimate — content is removed by design, not weakened coverage). DROP assertions for deleted content: `data-testid="home-featured-personas"`, `admin@demo.tasklane.test`, `pat@demo.tasklane.test`, `dave@demo.tasklane.test`, `demo=dave`, and the `morgan@…` EMAIL assert (Morgan survives only as `demo=morgan` via the aside, not as an email). KEEP `id="get-started"` (now on the aside), `/admin/organizations/acme-corp` (aside hint), and `Sign in as` (pickers). ADD `demo=admin` and `href="/demo/credentials"` (and/or "View all"). For any other assertion referencing removed markup (e.g. a `>10<` count) make the test match rendered reality — run it and fix drift rather than guessing.

In app.css: REMOVE the `#get-started { margin-top: var(--sg-space-5) }` rule (now moot — it would wrongly apply to the aside once the id moves). Leave the persona-list / `.vt-persona-row` CSS in place (harmless, may be reused).

Confirm no `priv/templates/` or `test/fixtures/` file is touched. Commit this part on its own: home consolidation.
  </action>
  <verify>
    <automated>cd test/example && mix test test/example_web/controllers/page_controller_test.exs --include example_app 2>&1 | tail -20</automated>
  </verify>
  <done>page_controller_test GET / passes against the new IA; the evaluator-persona `<section id="get-started">` is gone; the operator aside carries `id="get-started"` and a "View all … personas →" ghost button; `featured_credentials` assign removed; dead `#get-started` CSS rule removed.</done>
</task>

<task type="auto">
  <name>Task 2 (COMMIT: Part B — /demo/credentials): credentials table → "Sign in as X" button per persona</name>
  <files>test/example/lib/example_web/live/demo/credentials_live.ex, test/example/test/example_web/live/demo/credentials_live_test.exs</files>
  <action>
In credentials_live.ex `<table class="vt-table">`: replace the **Email** and **Password** columns/cells with a single **action** column holding a primary button per row: `<a href={~p"/users/log_in?#{%{demo: c.local}}"} class="vt-btn vt-btn--primary">Sign in as {c.display_name}</a>`. New column layout: `Persona | Auth feature demonstrated | (Sign in)`. KEEP the per-row testid `data-testid={"demo-persona-row-#{c.local}"}`, the `data-testid="demo-credentials-table"`, the `data-testid="demo-dev-only-badge"`, and the header `.vt-panel` copy (roughly lines 47-66) that contains `admin@…` / `morgan@…` / `/admin/organizations/acme-corp` (the credentials_live test asserts these — do NOT remove them). REWORD the footer note (roughly lines 90-92) away from "Passwords are public-by-design demo credentials" toward: the demo password is filled for you at the login screen — and KEEP the "Never use in production." phrase.

In credentials_live_test.exs: no existing assertion breaks (there are no per-row email/password asserts). ADD a button assertion, e.g. `assert html =~ "demo=admin"` and/or `assert html =~ "Sign in as"`.

Commit this part on its own: credentials buttons.
  </action>
  <verify>
    <automated>cd test/example && mix test test/example_web/live/demo/credentials_live_test.exs --include example_app 2>&1 | tail -20</automated>
  </verify>
  <done>Each persona row renders a "Sign in as X" primary button linking to `/users/log_in?demo=<key>`; email/password columns removed; header `.vt-panel` copy + DEV-ONLY badge + per-row testids intact; footer reworded; a new button assertion is green.</done>
</task>

<task type="auto">
  <name>Task 3 (COMMIT: Part C — /users/log_in): dev-only low-noise persona-switcher dropdown</name>
  <files>test/example/lib/example_web/controllers/session_controller.ex, test/example/lib/example_web/controllers/session_html.ex, test/example/assets/js/admin_hooks.js, test/example/priv/static/assets/js/app.js, test/example/priv/static/assets/css/app.css</files>
  <action>
In session_controller.ex: add a dev-gated options helper mirroring the existing `demo_persona_hint/1` gate — wrap in `if Application.compile_env(:example, :dev_routes) do … else defp demo_persona_options, do: [] end`. In the dev branch, `defp demo_persona_options` maps `Example.Demo.Personas.all()` into `%{key: p.email |> String.split("@") |> hd(), display_name: p.display_name}`. Pass `demo_personas: demo_persona_options()` into the `render(conn, :new, …)` call alongside the existing assigns. In the test / non-dev build this returns `[]` so nothing renders and the existing session_controller_test refutes stay green. (See the approved plan file for the exact helper body.)

In session_html.ex: change the demo band gate from `:if={@demo_persona_hint}` to `:if={@demo_personas != []}` so the band renders in ANY dev build (including a bare `/users/log_in`). Make the credential + Fill-password portion conditional on `@demo_persona_hint`:
- keep `class="vt-demo-switch vt-demo-switch--login" data-testid="demo-login-hint"` on the outer `<div>`;
- the "Disposable demo account — never use in production" label, the `<code>` email node, and the `data-demo-fill-password` `<button>` each carry `:if={@demo_persona_hint}`;
- add a fallback `:if={is_nil(@demo_persona_hint)}` "Demo personas — never use in production" label;
- add a `<select class="vt-demo-switch__select" data-demo-persona-switch aria-label="Switch demo persona">` with a disabled placeholder option ("Switch persona…", selected when no hint) and `<option :for={p <- @demo_personas} value={p.key} selected={@demo_persona_hint && String.starts_with?(@demo_persona_hint.email, p.key <> "@")}>{p.display_name}</option>`.
CRITICAL: preserve the verbatim tokens the non-dev test refutes — `data-testid="demo-login-hint"`, `data-demo-fill-password`, and the literal "Disposable demo account" — all safely inside the dev-gated band so `session_controller_test` refutes stay green. Reproduce the HEEx block from the approved plan file exactly.

In admin_hooks.js AND priv/static/assets/js/app.js (edit BOTH — the served app.js is a hand-maintained mirror): add an idempotent delegated `installDemoPersonaSwitch()` that guards on `window.__sigraDemoPersonaSwitchInstalled`, then adds a delegated `change` listener that finds `event.target.closest("select[data-demo-persona-switch]")` and, if the select has a value, does `window.location.assign("/users/log_in?demo=" + encodeURIComponent(sel.value))`. Register `installDemoPersonaSwitch();` in the startup block of BOTH files, beside the existing `installDemoPasswordFill();`. Copy the function body verbatim from the approved plan file.

In app.css: add a minimal, theme-token-based `.vt-demo-switch__select` rule (subtle border/line, muted bg, `min-height: var(--sg-control-md)` or similar, small font) so the `<select>` sits tidily in the band. Keep it low-noise.

Commit this part on its own: login persona switcher.
  </action>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/example_web/controllers/session_controller_test.exs --include example_app 2>&1 | tail -20</automated>
  </verify>
  <done>`mix compile --warnings-as-errors` is clean; session_controller_test (incl. non-dev refutes for `data-testid="demo-login-hint"` / `data-demo-fill-password` / "Disposable demo account") stays green; `installDemoPersonaSwitch` exists and is registered in both admin_hooks.js and served app.js; `.vt-demo-switch__select` CSS added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → demo login | Persona-switch `<select>` value is placed into a `/users/log_in?demo=<key>` URL via `window.location.assign` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-oa1-01 | Elevation | dev-only demo persona band/switcher | low | mitigate | Entire band + switcher + options helper are gated behind `Application.compile_env(:example, :dev_routes)`; `demo_persona_options/0` returns `[]` in non-dev builds so nothing renders. Non-dev refutes asserted in session_controller_test. |
| T-oa1-02 | Tampering | `installDemoPersonaSwitch` `window.location.assign` | low | accept | Value is `encodeURIComponent`-escaped and used only as a `demo` query key routed through the REAL login (no bypass/auto-submit); example-only demo surface, no production exposure. |
| T-oa1-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan — pure example-only edits (HEEx/Elixir/JS/CSS). No dependency surface changed. |
</threat_model>

<verification>
End-to-end (after all three parts, run from repo root):

1. `cd test/example && mix compile --warnings-as-errors` — clean.
2. `cd test/example && mix test test/example_web/controllers/page_controller_test.exs test/example_web/live/demo/credentials_live_test.exs test/example_web/controllers/session_controller_test.exs --include example_app` — all green (page test updated to new IA; credentials + session tests green; session refutes still hold because the dev-gated band/switcher do not render under `mix test`).
3. `git diff --stat` touches ONLY the listed example files — no `priv/templates/`, no `test/fixtures/`.
4. `grep -rl installDemoPasswordFill priv/templates/` returns nothing (demo JS confirmed absent from installer templates — verified at planning time).

Live browser verification (home main-2 layout, credentials buttons, login switcher navigation + pre-selection) is performed by the ORCHESTRATOR post-execution against the running demo — the executor does NOT drive a browser.
</verification>

<success_criteria>
- Home: evaluator-persona card gone; operator aside carries `id="get-started"` + "View all … personas →" ghost button above the two pickers; page still flows hero → brand-lab → seeded-evidence with no gap artifact.
- /demo/credentials: one "Sign in as X" button per row (→ `/users/log_in?demo=<key>`), no email/password columns; header + DEV-ONLY badge intact.
- /users/log_in (bare, dev build): low-noise "Switch persona…" dropdown renders in the demo band; selecting a persona navigates to `/users/log_in?demo=<key>` with the current persona pre-selected; no auto-submit.
- Three atomic commits (home, credentials, login-switcher); all changes example-only.
- `mix compile --warnings-as-errors` clean; the three targeted test files green; `git diff --stat` shows no installer-template / golden-fixture drift.
</success_criteria>

<output>
Create `.planning/quick/260718-oa1-consolidate-demo-front-door-main-2-home-/260718-oa1-SUMMARY.md` when done.
</output>
