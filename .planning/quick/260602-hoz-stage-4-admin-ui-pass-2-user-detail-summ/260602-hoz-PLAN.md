---
phase: quick-260602-hoz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/live/user_show_live.ex
  - test/example/priv/static/assets/css/app.css
autonomous: true
requirements: [STAGE-4-SUMMARY-FIRST]
must_haves:
  truths:
    - "The top 'Identity & Status' card shows a scannable facts strip with MFA state, passkey count, active-session count, and last activity"
    - "When the account has a headline risk (locked > unconfirmed > no-MFA), a single concise tone callout line appears in the summary pointing at the relevant control below"
    - "A healthy account shows no risk callout (or an optional calm 'All clear')"
    - "The strict section-order test (Identity & Status < Sessions < Security < Identities < Organizations < Recent Audit < Danger Zone) still passes"
    - "The Sessions table shows a relative-recency cue (e.g. 'x ago') alongside the absolute timestamp"
  artifacts:
    - path: "lib/sigra/admin/live/user_show_live.ex"
      provides: "Enriched top-card security summary + risk callout + session recency cue"
      contains: "sg-summary-facts"
    - path: "test/example/priv/static/assets/css/app.css"
      provides: "Additive .sg-summary-facts strip styling (token-driven, BEM)"
      contains: ".sg-summary-facts"
  key_links:
    - from: "lib/sigra/admin/live/user_show_live.ex top card"
      to: "@detail.security / @detail.sessions"
      via: "in-render computation (no new queries)"
      pattern: "last_activity|passkey_count|length\\(@detail.sessions\\)"
---

<objective>
Stage 4 of the approved Admin-UI Pass 2 plan: make the user-detail screen "summary-first" for Sam (support). Enrich the FIRST `<section class="sg-card">` (the "Identity &amp; Status" card) so it reads as a security state summary — not just identity + pills — by adding a compact, scannable facts strip (MFA · passkeys · active-session count · last activity) plus a single foregrounded risk callout that names the highest-priority issue and points at the control below. Add a light relative-recency cue to the Sessions table. No data-layer, no JS, no test changes.

Purpose: Sam should see the account's security posture at a glance and know the likely fix without scrolling through the deep panels.

Output: Enriched `user_show_live.ex` top card + minimal additive `.sg-summary-facts` CSS. All existing pinned strings and the strict section-order contract remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# The screen being edited (library-owned LiveView — read fully before editing):
@lib/sigra/admin/live/user_show_live.ex

# The STRICT section-order + pinned-string contract (read fully; do NOT edit this file):
@test/example/test/example_web/live/admin_user_show_live_test.exs

# sg-* layer (add minimal additive CSS only; reuse primitives first):
@test/example/priv/static/assets/css/app.css

<ground_truth>
RESTART REQUIRED: `lib/sigra/admin/**` is a path dep, NOT hot-reloaded — a running server must be restarted to view changes. `app.css` is static and reloads immediately. The orchestrator handles server restart; do not block on it.

`@detail` (from `Detail.load!`) ALREADY PROVIDES everything needed — NO new queries:
- `@detail.user` → `id`, `email`, `inserted_at`
- `@detail.display_name`
- `@detail.identity` → `confirmed?`, `locked?`, `deleted?`, `inserted_at`
- `@detail.sessions` → list of `%{type, ip, hashed_token, last_active_at}`
- `@detail.security` → `mfa_status`, `passkeys`, `passkey_count`
- `@detail.scope_label`, `@detail.danger_zone`, `@detail.organizations`, `@detail.recent_audit`, `@detail.identities*`

Computed facts (all in-render, no queries):
- MFA state: reuse existing `mfa_value(@detail.security.mfa_status)` → "Enabled" / "Not configured"
- Passkeys: `@detail.security.passkey_count` (may be nil → treat as 0)
- Active-session count: `length(@detail.sessions)`
- Last activity: max of `session.last_active_at` over `@detail.sessions` → "Not available" when none or all nil

Existing reusable helpers in this module (DO NOT duplicate):
`mfa_value/1`, `mfa_enabled?/1`, `activity_value/1`, `pluralize/2`, `status_pills/1`, `security_pill/1`.
</ground_truth>

<reserved_needle_rule>
HARD CONSTRAINT. The section-order test maps the FIRST-occurrence byte offset of each EXACT string and asserts strictly increasing order:
`Identity &amp; Status` < `Sessions` < `Security` < `Identities` < `Organizations` < `Recent Audit` < `Danger Zone`.

Therefore the NEW summary content added to the TOP card MUST NOT contain ANY of these six capitalized needle substrings:
`Sessions`  `Security`  `Identities`  `Organizations`  `Recent Audit`  `Danger Zone`
(Introducing e.g. a "Security summary" heading or an "Active Sessions" label with capital S in the top card would move that needle's first occurrence above its real section and BREAK the order test.)

SAFE labels to use in the facts strip / callout (avoid the six words entirely):
- session count → label it `Active` (capital-S "Sessions" is the needle; "Active" is safe). Lowercase "active sessions" in prose is technically safe but PREFER `Active` to be maximally robust.
- recency → label it `Last seen`
- MFA → `MFA` (safe — not a needle)
- passkeys → `Passkeys` (SAFE — "Passkeys" is NOT one of the six needles; "Identities" is. The current top card already emits "Passkeys" via `security_pill/1` and the test passes — confirmed.)

The `Identity &amp; Status` kicker MUST remain (it is the first needle / anchor). Keep all six real section headings EXACTLY as they are.
</reserved_needle_rule>

<pinned_strings>
DO NOT change or break (asserted by the test file): "Revoke session", "Revoke all sessions", "Start impersonation", the confirm-dialog copy ("Revoke this session for … in …?…" / "Revoke every active session…"), "Open organization-scoped view for {org}", "View full audit", "End impersonation before starting another session.", "Support actions affect {label}", "No active sessions.", Presenter labels ("Session Create", "Session Revoke_all", "Impersonation started", "Impersonation" badge, `sg-status-pill`), and the `<th>` cells "Type"/"IP address"/"Last activity"/"Action". `Start impersonation` must stay AFTER `Danger Zone`. Keep `phx-click` handlers and the confirm `<dialog>` working. Do NOT touch Danger Zone structure/strings (it stays its own `sg-danger-panel` with flat `sg-btn--danger`). No modal motion (Stage 7).
</pinned_strings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security-summary facts strip + risk callout to the top card</name>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <action>
Enrich the FIRST `<section class="sg-card">` (the "Identity &amp; Status" card, render lines ~97–111). Keep everything currently there: the `sg-page-kicker` "Identity &amp; Status", the h1 (`@detail.display_name || @detail.user.email`), the email line, the `sg-code` id, and the existing `status_pills/1` cluster. ADD, below the existing `sg-cluster--between` identity/pills block, two new pieces inside the same card:

1. A scannable facts strip. Render a `<dl class="sg-summary-facts">` (the small additive class from Task 3) containing four term/value pairs using the `sg-kv__term` / `sg-kv__value` primitive classes (reuse — do not invent value styles). The four facts, in order, with RESERVED-NEEDLE-SAFE labels:
   - term `MFA` → value `mfa_value(@detail.security.mfa_status)` ("Enabled" / "Not configured")
   - term `Passkeys` → value the passkey count (use `passkey_count(@detail.security)` — see helper below — rendered as a plain integer; add class `sg-summary-facts__num` for tabular-nums)
   - term `Active` → value `length(@detail.sessions)` (plain integer, `sg-summary-facts__num`)
   - term `Last seen` → value `last_activity(@detail.sessions)` (see helper below)
   Do NOT use the words `Sessions`, `Security`, `Identities`, `Organizations`, `Recent Audit`, or `Danger Zone` anywhere in this block. Use the labels above verbatim.

2. A single foregrounded risk callout line, rendered ONLY when there is a headline issue. Use a `sg-list-row` with `data-tone` (reuse the tone-row primitive — left tone bar + tint + the automatic `::before` glyph via the pill is not needed here; the list-row tone bar is the cue). Pick the SINGLE highest-priority issue via a new helper `summary_alert/1` returning `{tone, message}` or `nil`:
   - locked? → `{"risk", "Locked — revoke active logins and unlock below."}`
   - else unconfirmed (not `confirmed?`) → `{"warn", "Email unconfirmed — the user cannot complete sign-in."}`
   - else MFA not enabled → `{"warn", "No MFA configured — recommend enabling a second factor."}`
   - else → `nil` (healthy: render NO callout; an optional calm `{"ok", "All clear."}` is acceptable but NOT required — default to nil for restraint)
   Render with `:if={summary_alert(@detail)}` guarding the row; bind the result and emit `<p class="sg-text-sm">{message}</p>` inside `<div :if={...} class="sg-list-row" data-tone={tone}>`. Keep it ONE line — no wall. IMPORTANT: the callout copy MUST avoid the six reserved needles — the messages above are pre-vetted (note "active logins" not "active Sessions"; "second factor" not "Security").

Add these private helpers near the existing `activity_value/1` / `mfa_value/1` group:
  - `passkey_count/1`: `defp passkey_count(%{passkey_count: n}) when is_integer(n), do: n` ; `defp passkey_count(_), do: 0`
  - `last_activity/1`: takes the sessions list; collect non-nil `last_active_at` `%DateTime{}` values, return `activity_value(Enum.max(list, DateTime))` reusing the existing `activity_value/1` formatter, or `activity_value(nil)` ("Not available") when empty. Use `Enum.filter` + pattern match on `%DateTime{}`; guard the empty case so `Enum.max` is never called on `[]`.
  - `summary_alert/1`: as specified above; reuse `mfa_enabled?/1` for the MFA branch and `@detail.identity` for locked?/confirmed?.

Compute `summary_alert/1` once if convenient (assign or inline `:let`-free binding via a `<% alert = summary_alert(@detail) %>` is NOT allowed in HEEX; instead call the helper in the `:if` AND inside, OR wrap the row in a `:if={summary_alert(@detail)}` and re-call for tone/message — acceptable since it is pure and cheap). Prefer the simplest correct HEEX: guard with `:if={summary_alert(@detail)}`, then inside reference `elem(summary_alert(@detail), 0)` and `elem(summary_alert(@detail), 1)`, OR add a tiny render helper component. Keep it readable.

Do NOT add fenced code in markup; this is HEEX (`~H`). No JS. No new queries. Remove nothing that the test pins.
  </action>
  <verify>
    <automated>cd test/example && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres PGPASSWORD=postgres mix test test/example_web/live/admin_user_show_live_test.exs</automated>
  </verify>
  <done>Top card renders the four facts (MFA, Passkeys count, Active count, Last seen) and a single tone callout when locked/unconfirmed/no-MFA (none when healthy). Section-order test green; all pinned strings intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add a light relative-recency cue to the Sessions table</name>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <action>
In the Sessions table "Last activity" cell (render line ~144, currently `<td class="sg-muted">{activity_value(session.last_active_at)}</td>`), keep the absolute timestamp EXACTLY (`activity_value/1` output is pinned-adjacent; do not remove it) and append a subtle relative cue beside it. Render the cell as a small stack: the existing absolute value on top (keep `tabular-nums` alignment by adding class `sg-summary-facts__num` to a wrapping span, or `sg-tabular` if that primitive exists — verify in app.css; if neither, leave as-is) and below it a muted relative string `<span class="sg-muted sg-text-xs">{relative_activity(session.last_active_at)}</span>` rendered only when a timestamp exists.

Add a private helper `relative_activity/1`:
  - `defp relative_activity(%DateTime{} = at)`: compute `DateTime.diff(DateTime.utc_now(), at, :second)` and return a coarse human string: `< 60` → "just now"; `< 3600` → "Nm ago"; `< 86_400` → "Nh ago"; else → "Nd ago". Integer math only; no new deps.
  - `defp relative_activity(_), do: nil` (cell shows only the absolute "Not available").

Do NOT change the heading "Sessions", the `<th>` cells, the "Revoke session" / "Revoke all sessions" buttons, or the empty-state "No active sessions." string. Keep it minimal — one extra muted line. This adds no reserved needles (the relative strings contain none of the six words).
  </action>
  <verify>
    <automated>cd test/example && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres PGPASSWORD=postgres mix test test/example_web/live/admin_user_show_live_test.exs</automated>
  </verify>
  <done>Each session row shows the absolute "Last activity" timestamp plus a muted relative "x ago" cue; all Sessions-section pinned strings unchanged; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal additive .sg-summary-facts CSS</name>
  <files>test/example/priv/static/assets/css/app.css</files>
  <action>
Add a small, token-driven, BEM, mobile-first block inside the existing `@layer sg-components { … }` (place it near the `.sg-kv` / `.sg-metric` definitions, ~line 1120). No new `!important`. Every value references an existing token.

Define:
  - `.sg-summary-facts` — a horizontal, wrapping facts strip: `display: flex; flex-wrap: wrap; gap: var(--sg-space-5);` with `margin-top: var(--sg-space-3);` so it sits below the identity block. Each child pair is a `<div>` (the `<dl>`'s direct children) — set `.sg-summary-facts > div { display: flex; flex-direction: column; gap: var(--sg-space-1); }` so term sits above value. (The `sg-kv__term` / `sg-kv__value` classes already style the text; this block only owns LAYOUT.)
  - `.sg-summary-facts__num` — `font-variant-numeric: tabular-nums;` (counts align). Nothing else.

Mobile-first: the flex-wrap already collapses gracefully on narrow widths; no media query needed. Do NOT restyle `sg-kv__term`/`sg-kv__value`. Confirm the block compiles (static CSS — no build step; the file is served directly).
  </action>
  <verify>
    <automated>grep -n "sg-summary-facts" test/example/priv/static/assets/css/app.css</automated>
  </verify>
  <done>`.sg-summary-facts` and `.sg-summary-facts__num` exist in `@layer sg-components`, token-driven, no new `!important`, no changes to existing primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → rendered HTML | All values (`@detail.*`) are already trusted, scope-loaded data; HEEX auto-escapes interpolations. No new untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hoz-01 | Information disclosure | top-card summary facts | accept | Facts (MFA state, passkey count, session count, last activity) are already rendered elsewhere on this same scope-authorized page; no new data exposure. |
| T-hoz-02 | Tampering | additive CSS only | accept | No package installs; no JS; CSS is static and token-driven. No supply-chain surface. |
</threat_model>

<verification>
1. Section-order + pinned-string contract (PRIMARY GATE):
   `cd test/example && PGHOST=127.0.0.1 PGPORT=5432 PGUSER=postgres PGPASSWORD=postgres mix test test/example_web/live/admin_user_show_live_test.exs` → all green, especially the order test.

2. Reserved-needle proof — confirm the NEW top-card summary block contains NONE of the six needles. Inspect the added facts-strip + callout markup (render lines between the identity `sg-cluster` and the closing `</section>` of the first card) and verify it uses `Active` / `Last seen` / `MFA` / `Passkeys` only:
   `grep -nE "Sessions|Security|Identities|Organizations|Recent Audit|Danger Zone" lib/sigra/admin/live/user_show_live.ex` → the only matches must be the SIX real section headings (and pinned strings like "Revoke all sessions" / "No active sessions." / "active session" copy that already existed) — NO new occurrence inside the top `Identity &amp; Status` card. Manually confirm no new needle was introduced above its real section.

3. Compile clean: `mix compile --warnings-as-errors` (from repo root) → no warnings; no unused private fns (every new helper — `passkey_count/1`, `last_activity/1`, `summary_alert/1`, `relative_activity/1` — is referenced in render).

4. CSS present: `grep -n "sg-summary-facts" test/example/priv/static/assets/css/app.css`.
</verification>

<success_criteria>
- Top "Identity &amp; Status" card shows a facts strip: MFA state + passkey count + active-login count + last-activity, with tabular-nums counts.
- A single foregrounded risk callout (locked > unconfirmed > no-MFA) appears when applicable; none when healthy.
- Sessions table shows a muted relative "x ago" cue beside each absolute timestamp; all Sessions pinned strings unchanged.
- Danger Zone untouched (separate `sg-danger-panel`, flat danger buttons, "Start impersonation" still after "Danger Zone").
- `admin_user_show_live_test.exs` green (section-order especially); reserved needles absent from the summary block; all pinned strings intact.
- `mix compile --warnings-as-errors` clean; no JS, no new queries; only additive token-driven BEM CSS.
- FLAG for orchestrator: library-owned LiveView → restart server to view; screenshot baselines are refreshed in Stage 8 (not this stage).
</success_criteria>

<output>
Create `.planning/quick/260602-hoz-stage-4-admin-ui-pass-2-user-detail-summ/260602-hoz-SUMMARY.md` when done.
</output>
