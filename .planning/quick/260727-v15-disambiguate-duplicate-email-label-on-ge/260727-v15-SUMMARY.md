---
quick_id: 260727-v15
title: "Disambiguate the duplicate \"Email\" label on the generated login page"
status: complete
completed: 2026-07-27
branch: worktree-v1.46-login-email-label
commit: a31e60c0
files_modified:
  - priv/templates/sigra.install/core/login_html.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/controllers/session_html.ex
metrics:
  tasks: 2
  files: 2
  lines_changed: 2
---

# Disambiguate the duplicate login-page email label — Summary

One-liner: the generated login page's top-level magic-link email field now reads
`Email for sign-in link`, so expanding the "Other ways to sign in" disclosure no longer
renders two identically-labeled email inputs; the install-golden fixture was mirrored and
proven byte-identical to generator output.

## What changed

Exactly one line in each of two files — the `label=` attribute value only.

**`priv/templates/sigra.install/core/login_html.ex` line 85** — the `f[:email]` input of the
`id="magic_link_form"` form opening at line 83 inside the `<%% else %>` (non-passkey-primary)
branch. `label={dgettext("sigra", "Email")}` → `label={dgettext("sigra", "Email for sign-in link")}`.
`field`, `type`, `autocomplete="username"`, and `required` are untouched. The replacement msgid
already existed at line 68, so no new gettext msgid was introduced and no catalog regeneration
was needed.

**`test/fixtures/install_golden/tree/.../controllers/session_html.ex` line 85** — the identical
anchor, hand-mirrored. `rebless_golden --check` then proved the hand edit byte-matches what a
full rebless produces, so no full rebless was required and there is zero unrelated fixture churn.

Post-state of both files is identical and matches the plan's expected counts:

| Line | Label | Form |
|------|-------|------|
| 44 | `Email` | passkey form (`autocomplete="username webauthn"`) — untouched |
| 68 | `Email for sign-in link` | passkey-primary branch magic-link — pre-existing |
| 85 | `Email for sign-in link` | **changed** — non-passkey-primary magic-link |
| 111 | `Email` | `password_form/1` (`#login_form`) — untouched by design |

## Verification — commands actually run and their real output

All commands run from the worktree root with the ephemeral test Postgres loaded
(`scripts/db/up.sh` → `127.0.0.1:54204`, then `set -a; . tmp/db.env; set +a`).

**Preflight**

- `mix archive | grep phx_new` → `* phx_new-1.8.8` (matches the CI pin).
- `scripts/db/up.sh` → Postgres ready on `127.0.0.1:54204`, env written to `tmp/db.env`.
- `mix deps.get` — required because this was a fresh worktree with no `deps/`. `mix.lock` and
  `mix.exs` remained unmodified (`git status --short mix.lock mix.exs` printed nothing), so no
  dependency surface changed. No new package was added; this restored the already-locked set.

**Gate: label counts (both files)**

```
template bare Email:      2
template sign-in link:    2
fixture bare Email:       2
fixture sign-in link:     2
```

Exactly the `2`/`2` the plan required in each file (was `3`/`1`).

**Gate 1 — `mix compile --warnings-as-errors`** → `compile EXIT=0`, clean.

**Gate 2 — `MIX_ENV=test mix sigra.fixture.rebless_golden --check`** → `EXIT=0`, final line:

```
==> sigra.fixture.rebless_golden: scaffolding fresh tmp app via InstallFixture
OK: fixture is up-to-date (check mode).
```

This is the end-to-end template → generator → fixture agreement proof and the CI hard gate
(`.github/workflows/ci.yml:301`). No full rebless was needed, so the "STOP if rebless touches
other files" branch never triggered.

**Gate 3 — ExUnit**

```
mix test test/sigra/install/golden_diff_test.exs \
         test/sigra/install/idempotency_test.exs \
         test/sigra/install/auth_ui_contract_test.exs
```

→ `EXIT=0`, `10 tests, 0 failures` (68.8s). Incidental log noise only: Tzdata auto-updated its
release 2025a → 2026c on first boot (a dep-local data refresh, no repo files touched).

**Gate 4 — Playwright selector safety (static)**

`grep -rn "getByLabel(['\"]Email" test/example/priv/playwright/tests/ | wc -l` → `4`, unchanged:

- `admin-generated.spec.ts:71` — the only login-page hit; line 70 is
  `const passwordForm = page.locator("#login_form")` and line 71 is
  `passwordForm.getByLabel("Email", { exact: true })`. `#login_form` **is** `password_form/1`,
  whose label is unchanged, so this still resolves unambiguously.
- `organizations.spec.ts:463`, `:464`, `ga-uat-shift-left.spec.ts:146` — invitation-accept page
  in the example app, unrelated to the login page.

Zero spec edits. No server boot / live Playwright run was needed: gate 4 plus the `#login_form`
scoping covers selector safety and gate 2 covers generated output.

**Gate 5 — `git diff --stat`** → exactly two files, one line each:

```
 priv/templates/sigra.install/core/login_html.ex                       | 2 +-
 .../tree/lib/..._web/controllers/session_html.ex                      | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```

No other fixture churn, no `*.png`, no planning/doc files in the commit. Post-commit deletion
check (`git diff --diff-filter=D --name-only HEAD~1 HEAD`) returned empty.

## Commit

`a31e60c0` — `fix(quick-260727-v15): disambiguate duplicate Email label on generated login page`
(code + fixture only; one atomic commit, as the plan required).

## Must-haves

| Truth | Status |
|-------|--------|
| On the generated login page with the disclosure expanded, the two visible email fields carry distinct labels | Met — line 85 now `Email for sign-in link`, line 111 stays `Email` |
| The committed install-golden fixture still byte-matches what the generator emits | Met — `rebless_golden --check` exit 0 |

## Deviations from Plan

None. The plan's verified facts held exactly: line 85 (not 86) was the target in both files,
the replacement string already existed at line 68, no ExUnit test or Playwright selector needed
changing, and no gettext regeneration was required.

The only unplanned command was `mix deps.get`, required to make a fresh worktree compilable.
It changed no tracked files and is not a deviation to the change itself.

## Known Stubs

None.

## Out-of-scope observations (recorded, not acted on)

Carried forward verbatim from the plan; both were left untouched per the scope boundary:

1. **The passkey-primary branch has the same duplicate class.** When `@passkey_primary_enabled`
   is true, the top passkey form's field (line 44, `autocomplete="username webauthn"`) and
   `password_form/1`'s field (line 111) both render the bare `Email` label once the disclosure is
   expanded. Same defect shape, different branch.
2. **The example demo twin has its own duplicate.**
   `test/example/lib/example_web/controllers/session_html.ex` uses plain `label="Email"` at lines
   78 and 148 with no gettext. Structurally different `vt-*`-branded composition; not the
   generated-host surface PROOF-03 captures. `installer_drift_test.exs` does not pair these
   labels.

Also explicitly untouched: the disclosure focus ring, button variants / `--primary` / `--secondary`
classes, `sigra_auth.css`, all other auth copy, and every admin surface.

## Threat model outcome

| Threat ID | Disposition | Outcome |
|-----------|-------------|---------|
| T-v15-01 fixture drift | mitigate | `rebless_golden --check` exit 0; `git diff --stat` bounded to 2 files |
| T-v15-02 Playwright selector ambiguity | mitigate | login-page selector still `#login_form`-scoped with `exact: true`; call-site count unchanged at 4 |
| T-v15-03 package installs | accept | No package added; `mix.lock`/`mix.exs` byte-unchanged |

## Self-Check: PASSED

- `priv/templates/sigra.install/core/login_html.ex` — FOUND, line 85 carries the new label.
- `test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/controllers/session_html.ex`
  — FOUND, line 85 carries the new label.
- Commit `a31e60c0` — FOUND in `git log`, 2 files changed, 2 insertions, 2 deletions.
