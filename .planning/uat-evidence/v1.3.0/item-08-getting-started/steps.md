# SEED-8 — Getting started guide (machine closure)

## CI job

- **getting_started_uat_contract** → `scripts/ci/getting-started-contract.sh`
  — internal `.md`/`.html` links resolve; required command strings
  (`mix phx.server`, `mix sigra.install`, `mix ecto.migrate`) appear in
  `guides/introduction/getting-started.md`.

## Local reproduction

```bash
bash scripts/ci/getting-started-contract.sh
```
