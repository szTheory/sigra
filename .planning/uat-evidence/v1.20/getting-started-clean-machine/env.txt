# GAUAT-08 environment capture
# Status: pending human witness run
# Release-candidate SHA: f67b9fd
#
# Run these on the fresh temporary Phoenix host and paste the exact output below:
#
# $ date -u +"%Y-%m-%dT%H:%M:%SZ"
# $ uname -a
# $ sw_vers 2>/dev/null || lsb_release -a 2>/dev/null || cat /etc/os-release 2>/dev/null
# $ elixir --version
# $ erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().' -noshell
# $ mix phx.new --version
# $ psql --version
#
# Optional if relevant during the run:
# $ node --version
# $ npm --version
