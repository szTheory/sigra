# GAUAT-08 environment capture
# Status: pending human witness run
# Release-candidate SHA: 367a164
#
# Record exact values from the fresh host:
# uname -a
# elixir --version
# erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().' -noshell
# mix phx.new --version
# psql --version
