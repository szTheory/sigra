---
phase: 35
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - test/example/priv/playwright/package.json
  - test/example/priv/playwright/package-lock.json
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
  - test/example/priv/playwright/tests/admin-generated.spec.ts
  - test/example/priv/playwright/playwright.config.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "@axe-core/playwright runs on each of the five checkpoint pages in admin-checkpoints.spec.ts (per project)."
    - "Committed snapshot baselines exist for five checkpoints × three projects (15 paths) for the example admin-checkpoints lane."
  artifacts:
    - path: "test/example/priv/playwright/package.json"
      provides: "axe-core devDependency"
nyquist_compliant: true
---

<objective>
Implement **ROADMAP SC3**: add **axe-core** violations checks and Playwright **`toHaveScreenshot`** baselines for the five curated admin checkpoint views across **chromium**, **mobile**, and **dark** projects; extend generated-host spec where it captures the same logical checkpoints.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation |
|----|--------|---------|----------|------------|
| T-35-03 | Tampering | Snapshots hide malicious UI | low | Pair with axe + existing functional asserts |
</threat_model>

<context>
@.planning/phases/35-shift-left-verification-automation/35-CONTEXT.md
@test/example/priv/playwright/tests/admin-checkpoints.spec.ts
@test/example/priv/playwright/playwright.config.ts
@test/example/priv/playwright/helpers/adminArtifacts.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add @axe-core/playwright dependency</name>
  <files>test/example/priv/playwright/package.json</files>
  <read_first>
    - test/example/priv/playwright/package.json
    - test/example/priv/playwright/package-lock.json
  </read_first>
  <action>
    1. Add devDependency `"@axe-core/playwright": "^4.10.0"` (or latest 4.x compatible with `@playwright/test@^1.48.0`).
    2. Run `npm install` in `test/example/priv/playwright` and commit updated `package-lock.json`.
  </action>
  <acceptance_criteria>
    - `grep -q '@axe-core/playwright' test/example/priv/playwright/package.json` exits 0.
    - `grep -q '@axe-core/playwright' test/example/priv/playwright/package-lock.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright && npm ci</automated>
  </verify>
  <done>axe dependency installed reproducibly.</done>
</task>

<task type="execute">
  <name>Task 2: axe scan helper + calls after each checkpoint capture</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
  </read_first>
  <action>
    1. Import `AxeBuilder` from `@axe-core/playwright`.
    2. Add `async function assertNoAxeViolations(page: Page, label: string)` that runs `await new AxeBuilder({ page }).analyze()` and `expect(violations, label).toEqual([])` (or `toHaveLength(0)` with JSON.stringify capped at 2000 chars on failure).
    3. After each `captureAndVerify` call for the five checkpoint names (`shell-global-desktop`, user detail, org-scoped shell, impersonation banner journey, audit explorer — use **exact** `name` strings already passed to `captureAdminCheckpoint` in the spec), invoke `assertNoAxeViolations`.
  </action>
  <acceptance_criteria>
    - `grep -q "AxeBuilder" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` exits 0.
    - `grep -q "assertNoAxeViolations" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-chromium --list</automated>
  </verify>
  <done>axe runs on chromium project list parse.</done>
</task>

<task type="execute">
  <name>Task 3: toHaveScreenshot baselines (15) + config</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/playwright.config.ts
  </read_first>
  <action>
    1. Under `test/example/priv/playwright/tests/`, add directory `admin-checkpoints.spec.ts-snapshots/` (Playwright default) **or** set `snapshotPathTemplate: '{testDir}/snapshots/{testFilePath}-snapshots/{arg}{ext}'` in config—pick one and document in spec comment.
    2. After each `captureAndVerify`, add `await expect(page).toHaveScreenshot({ fullPage: true, maxDiffPixels: 200 })` with a **stable** snapshot name including checkpoint slug and **not** including timestamps (use `testInfo.project.name` in snapshot name via `expect(page).toHaveScreenshot({ name: ... })` API).
    3. Generate baselines locally once with `npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-chromium --project=admin-checkpoints-mobile --project=admin-checkpoints-dark --update-snapshots`; commit **15** PNG files.
    4. Mirror the same pattern for `admin-generated.spec.ts` for any checkpoint captures that correspond to the five reviewer pages **if** those code paths exist; if generated-host only has a subset, document in plan SUMMARY and still meet **15** total by counting only example lane **or** add generated snapshots until total PNG snapshot count is **15** (align with ROADMAP SC6).
  </action>
  <acceptance_criteria>
    - `find test/example/priv/playwright/tests -name '*.png' 2>/dev/null | wc -l` prints **≥ 15** after implementation (adjust find path to actual snapshot dir used).
    - `grep -q "toHaveScreenshot" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-chromium</automated>
  </verify>
  <done>SC3 visual + a11y baselines committed.</done>
</task>

</tasks>

<verification>
- `cd test/example/priv/playwright && npm ci && npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-chromium --project=admin-checkpoints-mobile --project=admin-checkpoints-dark` exits 0
</verification>

<success_criteria>
- SC3: axe violations gate + 15 snapshot files committed and CI-parseable.
</success_criteria>
