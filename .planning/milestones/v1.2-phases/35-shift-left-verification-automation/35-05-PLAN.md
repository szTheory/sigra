---
phase: 35
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - scripts/ci/installer-milestone-audit.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "New bash script encodes INT-01..INT-03 greps from v1.2-MILESTONE-AUDIT.md."
    - "CI job runs only on paths priv/templates/sigra.install/** or lib/sigra/install/**."
  artifacts:
    - path: "scripts/ci/installer-milestone-audit.sh"
      provides: "Installer-scoped integration pre-merge audit"
nyquist_compliant: true
---

<objective>
Implement **ROADMAP SC5**: installer-scoped CI that fails on **CRITICAL** integration blockers for generated templates (INT-01 router mounts, INT-02 impersonation controller template presence, INT-03 audit export wired in `Features.Admin.files/1`).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation |
|----|--------|---------|----------|------------|
| T-35-05 | Repudiation | Audit thinks it ran but paths wrong | medium | `paths` filter + explicit `echo` steps in script |
</threat_model>

<context>
@.planning/milestones/v1.2-MILESTONE-AUDIT.md
@priv/templates/sigra.install/admin/router_injection.ex
@lib/sigra/install/features/admin.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: installer-milestone-audit.sh</name>
  <files>scripts/ci/installer-milestone-audit.sh</files>
  <read_first>
    - .planning/milestones/v1.2-MILESTONE-AUDIT.md (integration INT-01..INT-03)
    - priv/templates/sigra.install/admin/router_injection.ex
    - lib/sigra/install/features/admin.ex
  </read_first>
  <action>
    1. Create `scripts/ci/installer-milestone-audit.sh` with `set -euo pipefail`.
    2. **INT-01:** `grep -q "UsersIndexLive" priv/templates/sigra.install/admin/router_injection.ex` AND `grep -q "UserShowLive" priv/templates/sigra.install/admin/router_injection.ex` — on failure print “INT-01: missing Live mount in router_injection.ex”.
    3. **INT-02:** `test -f priv/templates/sigra.install/admin/impersonation_controller.ex` AND `grep -q "defmodule" priv/templates/sigra.install/admin/impersonation_controller.ex`.
    4. **INT-03:** `grep -q "impersonation_controller.ex" lib/sigra/install/features/admin.ex` AND `grep -q "audit_export_controller.ex" lib/sigra/install/features/admin.ex` (both literal substrings inside `def files` region—use `awk`/`sed` range if needed to avoid matching comments incorrectly; minimum bar: file contains both path strings).
    5. Print `OK: installer-milestone-audit` on success.
  </action>
  <acceptance_criteria>
    - `grep -q "UsersIndexLive" scripts/ci/installer-milestone-audit.sh` exits 0.
    - `grep -q "audit_export_controller" scripts/ci/installer-milestone-audit.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/installer-milestone-audit.sh && bash scripts/ci/installer-milestone-audit.sh</automated>
  </verify>
  <done>Script green on current tree.</done>
</task>

<task type="execute">
  <name>Task 2: paths-filtered CI job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
  </read_first>
  <action>
    1. Add job `installer_milestone_audit` with:
       - `on:` reuse workflow triggers
       - `paths: ['priv/templates/sigra.install/**', 'lib/sigra/install/**']` under `pull_request` and `push` using workflow `paths` filter OR job-level `paths` if supported—if GitHub syntax requires top-level, use `paths-ignore` inverse pattern documented in YAML comment.
    2. Run `bash scripts/ci/installer-milestone-audit.sh` only in that job.
  </action>
  <acceptance_criteria>
    - `grep -q "installer-milestone-audit" .github/workflows/ci.yml` exits 0.
    - `grep -q "priv/templates/sigra.install" .github/workflows/ci.yml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>ruby -ryaml -e "YAML.load_file('.github/workflows/ci.yml')" 2>/dev/null || python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <done>SC5 CI job present with path filter.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/installer-milestone-audit.sh` exits 0
</verification>

<success_criteria>
- SC5 satisfied.
</success_criteria>
