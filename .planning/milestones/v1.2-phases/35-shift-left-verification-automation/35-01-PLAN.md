---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/templates/generator_emission_audit_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ExUnit test scans priv/templates/sigra.install for <%= web_module %>.X references."
    - "Every reference is attributed to the correct Sigra.Install.Features.* module and covered by that feature's files/1 for a canonical binding."
  artifacts:
    - path: "test/sigra/templates/generator_emission_audit_test.exs"
      provides: "Generator emission audit — INT-01/02/03 class prevention"
nyquist_compliant: true
---

<objective>
Implement **ROADMAP SC1**: `generator_emission_audit_test.exs` proves every `<%= web_module %>.…` reference in installer templates is backed by an emitted file on the owning feature’s `files/1` list.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-35-FN | Tampering | Test weakened to always pass | medium | Concrete grep-able `@moduledoc` + negative case fixture or self-test |
| T-35-01 | Info disclosure | N/A — test only | low | — |
</threat_model>

<context>
@.planning/phases/35-shift-left-verification-automation/35-CONTEXT.md
@.planning/phases/35-shift-left-verification-automation/35-RESEARCH.md
@lib/sigra/install/features/admin.ex
@lib/sigra/install/features/core.ex
@lib/sigra/install/features/organizations.ex
@lib/sigra/install/features/passkeys.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add generator_emission_audit_test.exs</name>
  <files>test/sigra/templates/generator_emission_audit_test.exs</files>
  <read_first>
    - test/sigra/templates/installer_drift_test.exs (repo root anchoring via Path.expand)
    - test/sigra/install/features/admin_test.exs (canonical binding shape if present)
    - lib/sigra/install/features/admin.ex (def files/1 return shape)
    - priv/templates/sigra.install/admin/router_injection.ex (sample web_module references)
  </read_first>
  <action>
    1. Create `test/sigra/templates/generator_emission_audit_test.exs` with `use ExUnit.Case, async: true`.
    2. Define `@repo_root Path.expand("../../..", __DIR__)` and a private function `canonical_binding/0` returning the minimum keyword list required so `Sigra.Install.Features.Admin.files/1`, `Core.files/1`, `Organizations.files/1`, and `Passkeys.files/1` all run without raising (mirror values from existing install feature tests: `web_module: "ExampleWeb"`, `otp_app: :example`, `context_module: "Example.Accounts"`, `context_alias: "Accounts"`, `schema_alias: "User"`, `opts: [live: true, api: false, jwt: false]` — adjust keys to match real callbacks).
    3. Map template **directory prefix** after `priv/templates/sigra.install/` to feature module: `admin` → `Sigra.Install.Features.Admin`, `core` → `Sigra.Install.Features.Core`, `organizations` → `Sigra.Install.Features.Organizations`, `passkeys` → `Sigra.Install.Features.Passkeys`.
    4. For each `*.ex` file under `priv/templates/sigra.install/` (recursively), read UTF-8 text and collect all regex matches of the EEx pattern: `<%= web_module %>.` followed by a dotted module tail (regex in Elixir source, not HTML-escaped).
    5. For each match, derive the **expected emitted path fragment** the feature should include: convert dot-separated suffix after `web_module` to a file path convention already used in `files/1` (e.g. `Admin.ImpersonationController` → `"admin/impersonation_controller.ex"` in template list). Document the mapping rules in `@moduledoc`.
    6. Assert the derived path string appears in **at least one** tuple’s template path from `feature_module.files(canonical_binding())` for the mapped feature.
    7. If a reference cannot be mapped deterministically, `flunk` with the file path and match—do not silently skip.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/templates/generator_emission_audit_test.exs` exits 0.
    - `grep -q "Sigra.Install.Features.Admin" test/sigra/templates/generator_emission_audit_test.exs` exits 0.
    - `grep -q "web_module" test/sigra/templates/generator_emission_audit_test.exs` exits 0.
    - `grep -q "files(" test/sigra/templates/generator_emission_audit_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/templates/generator_emission_audit_test.exs</automated>
  </verify>
  <done>SC1 emission audit green locally.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/templates/generator_emission_audit_test.exs` exits 0
</verification>

<success_criteria>
- SC1 satisfied: new test file exists and passes against current `priv/templates/sigra.install/**`.
</success_criteria>
