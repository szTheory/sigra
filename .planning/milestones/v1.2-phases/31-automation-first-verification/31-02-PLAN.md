---
phase: 31-automation-first-verification
plan: 2
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - test/example/priv/playwright/tests/admin-user-operations.spec.ts
  - test/example/priv/playwright/tests/impersonation.spec.ts
  - test/example/priv/playwright/tests/admin-audit.spec.ts
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
autonomous: true
requirements:
  - VFY-01
  - VFY-02
  - VFY-04
must_haves:
  truths:
    - "The example app proves the canonical admin browser journeys without spreading correctness coverage across a wide browser matrix."
    - "Curated desktop, mobile, and dark-mode checkpoints capture the specific admin pages reviewers need in order to judge scope chrome, dense layout, impersonation state, and audit usability."
    - "Browser artifacts on green runs are reviewable because screenshots are intentional and attached to the Playwright HTML report."
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-user-operations.spec.ts"
      provides: "Canonical user search/detail/session-revoke and org-pivot browser journey"
    - path: "test/example/priv/playwright/tests/impersonation.spec.ts"
      provides: "Canonical stale-sudo, fresh-sudo, banner-persistence, and stop/restore browser journey"
    - path: "test/example/priv/playwright/tests/admin-audit.spec.ts"
      provides: "Canonical audit filtering, impersonation visibility, and CSV export browser journey"
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts"
      provides: "Curated screenshot-only checkpoint coverage for desktop, mobile, and dark admin review pages"
  key_links:
    - from: "test/example/priv/playwright/tests/admin-user-operations.spec.ts"
      to: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts"
      via: "shared admin pages that separate workflow truth from review checkpoints"
      pattern: "admin/users|organization-scoped|checkpoint"
    - from: "test/example/priv/playwright/tests/impersonation.spec.ts"
      to: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts"
      via: "active impersonation banner captured on a non-admin or org page"
      pattern: "impersonation|banner|stop"
    - from: "test/example/priv/playwright/tests/admin-audit.spec.ts"
      to: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts"
      via: "audit explorer filter/export usability and screenshot capture"
      pattern: "audit|export|screenshot"
nyquist_compliant: true
---

<objective>
Finish the example-app browser contract for Phase 31: keep one canonical operator-journey suite for admin behavior truth, then add targeted checkpoint specs for mobile and dark-mode review artifacts.

Purpose: Per D-01, D-02, D-04, D-19 through D-21, and D-26 through D-30, the example app is the rich deterministic browser test bed, and it must carry the admin milestone's canonical UX proof without becoming a negative-case matrix.
Output: Updated example-app Playwright specs for the Phase 31 journey contract plus a dedicated checkpoint spec with curated desktop/mobile/dark screenshots.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-automation-first-verification/31-CONTEXT.md
@.planning/phases/31-automation-first-verification/31-RESEARCH.md
@.planning/phases/31-automation-first-verification/31-PATTERNS.md
@.planning/phases/31-automation-first-verification/31-01-PLAN.md
@/Users/jon/projects/sigra/test/example/priv/playwright/tests/admin-user-operations.spec.ts
@/Users/jon/projects/sigra/test/example/priv/playwright/tests/impersonation.spec.ts
@/Users/jon/projects/sigra/test/example/priv/playwright/tests/admin-audit.spec.ts

<interfaces>
From `test/example/priv/playwright/tests/admin-user-operations.spec.ts`:

```ts
async function waitForLiveViewReady(page: Page)
async function expectScopeChrome(page: Page, scopeLabel: string)
```

From `test/example/priv/playwright/tests/admin-audit.spec.ts`:

```ts
const download = page.waitForEvent('download');
await page.getByRole('link', { name: 'Export CSV' }).click();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the Phase 31 example-app browser contract and checkpoint inventory in specs</name>
  <files>test/example/priv/playwright/tests/admin-user-operations.spec.ts, test/example/priv/playwright/tests/impersonation.spec.ts, test/example/priv/playwright/tests/admin-audit.spec.ts, test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <behavior>
    - Test 1: per D-04, the user-operations journey covers search/filter, open user detail, revoke session, and scope persistence, plus the explicit global-to-organization pivot.
    - Test 2: per D-04, the impersonation journey covers stale sudo redirect, fresh sudo success, persistent banner on a non-admin or org-scoped page, stop, and return to admin context.
    - Test 3: per D-04, the audit journey covers filter alignment, visible impersonation semantics, and CSV export from global and scoped paths.
    - Test 4: per D-27 through D-29, the checkpoint spec captures the required pages in `chromium`, `mobile`, and `dark-chromium` with explicit screenshot names instead of whole-run raw output retention.
  </behavior>
  <action>Add or tighten the example-app Playwright specs so they describe the exact Phase 31 browser contract before implementation changes. Update the three existing admin specs to line up with D-04's canonical journeys, and add `admin-checkpoints.spec.ts` for the five required reviewer pages from D-28. Keep the assertions behavior-first and reviewer-artifact-aware; do not move denied impersonation, export-scope, malformed-param, or blocked-mutation matrices into browser coverage because D-06 keeps those outside Playwright.</action>
  <verify>
    <automated>bash -lc 'cd test/example && MIX_ENV=dev PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix ecto.create >/dev/null && mix ecto.migrate >/dev/null && PHX_SERVER=true mix phx.server >/tmp/sigra-phase31-example.log 2>&1 & SERVER_PID=$!; trap "kill $SERVER_PID" EXIT; for i in $(seq 1 30); do curl -sf http://localhost:4000/ >/dev/null && break; sleep 1; done; cd priv/playwright && CI=true SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-user-operations.spec.ts tests/impersonation.spec.ts tests/admin-audit.spec.ts tests/admin-checkpoints.spec.ts --project=chromium --project=mobile --project=dark-chromium'</automated>
  </verify>
  <done>The repo contains focused browser specs that fully describe the example-app admin journeys and the curated checkpoint inventory before the Playwright contract is turned green.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the example-app admin journeys and checkpoints green without widening the browser boundary</name>
  <files>test/example/priv/playwright/tests/admin-user-operations.spec.ts, test/example/priv/playwright/tests/impersonation.spec.ts, test/example/priv/playwright/tests/admin-audit.spec.ts, test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <action>Implement the browser coverage promised in Task 1 per D-01, D-02, D-04, D-19 through D-21, and D-26 through D-30. Keep the main behavioral assertions in the existing example-app journey specs, use the harness partition from Plan 31-01 so only the checkpoint spec expands across `mobile` and `dark-chromium`, and attach explicit screenshots for the required review pages rather than retaining broad raw output. Preserve the current narrow Playwright style: real routes, a few high-value assertions per step, and download content checks where browser semantics matter. Do not recreate ExUnit's negative-case matrices here.</action>
  <verify>
    <automated>bash -lc 'cd test/example && MIX_ENV=dev PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix ecto.create >/dev/null && mix ecto.migrate >/dev/null && PHX_SERVER=true mix phx.server >/tmp/sigra-phase31-example.log 2>&1 & SERVER_PID=$!; trap "kill $SERVER_PID" EXIT; for i in $(seq 1 30); do curl -sf http://localhost:4000/ >/dev/null && break; sleep 1; done; cd priv/playwright && CI=true SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-user-operations.spec.ts tests/impersonation.spec.ts tests/admin-audit.spec.ts tests/admin-checkpoints.spec.ts --project=chromium --project=mobile --project=dark-chromium'</automated>
  </verify>
  <done>The example app now carries the canonical admin browser proof for Phase 31, and the checkpoint spec produces reviewer-usable desktop/mobile/dark artifacts without expanding the whole suite into a broad matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example-app browser journeys -> admin workflow truth | The browser suite must prove only the canonical operator flows where real browser semantics matter |
| checkpoint spec -> review artifact bundle | The screenshots become reviewer-facing evidence and therefore must stay explicit, scoped, and reproducible |
| LiveView/browser timing -> suite reliability | Longpoll and warm-route timing can mask true regressions if the suite grows into a broad matrix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | T | example-app Playwright specs | mitigate | Per D-01, D-02, and D-04, keep only the canonical admin journeys in browser coverage and leave negative-case matrices in ExUnit. |
| T-31-05 | D | checkpoint suite runtime | mitigate | Per D-26 through D-29, restrict viewport/theme multiplication to the checkpoint spec so LiveView timing stays manageable and meaningful. |
| T-31-06 | I | passing-run screenshots | mitigate | Per D-20 and D-25, attach only named curated screenshots for the required pages, making green artifacts reviewable without uploading indiscriminate raw outputs. |
</threat_model>

<verification>
Boot the example app in dev mode, then run the focused admin journey specs plus the checkpoint spec across the partitioned projects. The plan is complete only when the browser suite proves the canonical journeys and emits the intended checkpoint artifacts without broadening the browser boundary.
</verification>

<success_criteria>
- The example app proves the full D-04 admin journey contract in Playwright.
- Mobile and dark-mode checkpoints exist as a compact dedicated layer instead of a full browser matrix.
- Green browser runs can be reviewed asynchronously through attached screenshots and the HTML report.
</success_criteria>

<output>
After completion, create `.planning/phases/31-automation-first-verification/31-automation-first-verification-02-SUMMARY.md`
</output>
