---
phase: 31-automation-first-verification
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/admin/authorizer_test.exs
  - test/sigra/admin/audit/query_test.exs
  - test/sigra/plug/forbid_during_impersonation_test.exs
  - test/example/test/example_web/controllers/impersonation_controller_test.exs
  - test/example/test/example_web/controllers/admin/audit_export_controller_test.exs
  - scripts/ci/http-smoke.sh
  - scripts/ci/admin-acceptance-smoke.sh
autonomous: true
requirements:
  - VFY-01
  - VFY-03
must_haves:
  truths:
    - "Authorization, impersonation, and export semantics remain proven through direct-path ExUnit coverage outside the browser happy path."
    - "Thin process-external smoke proves example-host boot, generated-host install/boot, session continuity, and a few admin-critical success and denial responses without becoming a second full functional suite."
    - "Generated-host parity is explicit because a freshly generated app is installed, seeded, booted, and exercised deterministically."
  artifacts:
    - path: "test/sigra/admin/authorizer_test.exs"
      provides: "Library-owned admin scope and denial coverage that remains outside Playwright"
    - path: "test/sigra/admin/audit/query_test.exs"
      provides: "Library-owned negative-case coverage for export and audit filter normalization"
    - path: "test/example/test/example_web/controllers/impersonation_controller_test.exs"
      provides: "Example-host direct-path coverage for sudo redirects, start/stop restore semantics, and denial outcomes"
    - path: "test/example/test/example_web/controllers/admin/audit_export_controller_test.exs"
      provides: "Example-host direct-path coverage for scoped export responses and CSV contract behavior"
    - path: "scripts/ci/http-smoke.sh"
      provides: "Thin example-host runtime smoke for boot and admin-critical routes"
    - path: "scripts/ci/admin-acceptance-smoke.sh"
      provides: "Generated-host install, seed, boot, and narrow admin runtime smoke"
  key_links:
    - from: "test/sigra/admin/authorizer_test.exs"
      to: "test/example/test/example_web/controllers/impersonation_controller_test.exs"
      via: "library policy truth stays below example-host integration tests"
      pattern: "authorize|impersonation|scope"
    - from: "test/example/test/example_web/controllers/admin/audit_export_controller_test.exs"
      to: "scripts/ci/http-smoke.sh"
      via: "direct-path export and route semantics remain aligned with thin real-HTTP smoke"
      pattern: "export|csv|http"
    - from: "scripts/ci/admin-acceptance-smoke.sh"
      to: "test/example/priv/playwright/tests/admin-generated.spec.ts"
      via: "generated-host boot harness executes the shallow parity suite against a fresh install"
      pattern: "admin-generated|seed|boot"
nyquist_compliant: true
---

<objective>
Close the direct-path side of Phase 31 by extending ExUnit coverage for policy/runtime truth and by tightening the thin process-external smoke that proves wiring and generated-host parity.

Purpose: Per D-07 through D-18, correctness for authorization, scope, impersonation, and export rules stays owned by library and example ExUnit, while shell smoke proves only the boot/runtime seams ExUnit cannot see.
Output: Expanded direct-path tests plus narrow example-host and generated-host runtime smokes for admin-critical routes.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-automation-first-verification/31-CONTEXT.md
@.planning/phases/31-automation-first-verification/31-RESEARCH.md
@.planning/phases/31-automation-first-verification/31-PATTERNS.md
@.planning/phases/30-audit-exploration-and-export/30-VERIFICATION.md
@/Users/jon/projects/sigra/test/sigra/admin/authorizer_test.exs
@/Users/jon/projects/sigra/test/sigra/admin/audit/query_test.exs
@/Users/jon/projects/sigra/test/sigra/plug/forbid_during_impersonation_test.exs
@/Users/jon/projects/sigra/test/example/test/example_web/controllers/impersonation_controller_test.exs
@/Users/jon/projects/sigra/test/example/test/example_web/controllers/admin/audit_export_controller_test.exs
@/Users/jon/projects/sigra/scripts/ci/http-smoke.sh
@/Users/jon/projects/sigra/scripts/ci/admin-acceptance-smoke.sh

<interfaces>
From `scripts/ci/http-smoke.sh`:

```bash
HOST="${HOST:-http://localhost:4000}"
ROUTES=( "/" "/users/register" "/users/log_in" "/users/sudo" "/dev/mailbox" "/users/sessions" )
```

From `scripts/ci/admin-acceptance-smoke.sh`:

```bash
TEST_TARGET="all"
PLAYWRIGHT_SPEC="tests/admin-generated.spec.ts"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct-path tests for policy, impersonation, and export regressions that must stay outside Playwright</name>
  <files>test/sigra/admin/authorizer_test.exs, test/sigra/admin/audit/query_test.exs, test/sigra/plug/forbid_during_impersonation_test.exs, test/example/test/example_web/controllers/impersonation_controller_test.exs, test/example/test/example_web/controllers/admin/audit_export_controller_test.exs</files>
  <behavior>
    - Test 1: per D-07, D-12, and D-13, admin authorizer and export query coverage includes denied and out-of-scope cases rather than only happy paths.
    - Test 2: per D-07 and D-15, the example-host controller coverage keeps stale-sudo redirect, fresh-sudo success, stop/restore, and scoped denial semantics outside the browser suite.
    - Test 3: per D-12 and D-15, export controller tests keep asserting HTTP status, redirect, CSV schema/filtering, and scope denial semantics rather than presentation details.
    - Test 4: per D-14 through D-15, impersonation-blocked mutation and authorization rules remain library-owned or example-host direct-path tests instead of moving into Playwright.
  </behavior>
  <action>Extend the existing library and example ExUnit files so Phase 31 keeps its negative-case truth outside the browser, explicitly implementing D-07 through D-15 and D-18. Add or tighten denial coverage in `authorizer_test.exs`, export/query normalization coverage in `query_test.exs`, and impersonation-blocked request coverage in `forbid_during_impersonation_test.exs`. In the example app, extend controller coverage for sudo redirect, start/stop restore, scoped denial, CSV scope behavior, and any Phase 30 parity gaps called out in `30-VERIFICATION.md`. Do not add UI-copy-only assertions or move these rules into Playwright.</action>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/admin/authorizer_test.exs test/sigra/admin/audit/query_test.exs test/sigra/plug/forbid_during_impersonation_test.exs --max-failures 1 && cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/controllers/impersonation_controller_test.exs test/example_web/controllers/admin/audit_export_controller_test.exs --max-failures 1'</automated>
  </verify>
  <done>The repo has direct-path tests that prove the admin milestone's authorization, impersonation, and export rules outside the browser happy path, including the critical denied and out-of-scope cases.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten thin example-host and generated-host runtime smoke without turning bash into a second functional suite</name>
  <files>scripts/ci/http-smoke.sh, scripts/ci/admin-acceptance-smoke.sh</files>
  <action>Update the process-external smoke harnesses per D-08 through D-18. Extend `http-smoke.sh` only far enough to prove example-host boot, route reachability, cookie/session continuity, and a few admin-critical success and denial responses. Extend `admin-acceptance-smoke.sh` so generated-host verification explicitly covers fresh install, compile, migrate, deterministic admin seeding, boot, and a narrow admin route/acceptance pass, including the Phase 30 generated-app parity gap called out in `30-VERIFICATION.md`. Keep assertions on statuses, redirects, route availability, and CSV/runtime seams; do not attempt detailed LiveView interaction or a broad authorization matrix in shell scripts.</action>
  <verify>
    <automated>bash -lc 'cd test/example && MIX_ENV=dev PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix ecto.create >/dev/null && mix ecto.migrate >/dev/null && PHX_SERVER=true mix phx.server >/tmp/sigra-phase31-http-smoke.log 2>&1 & SERVER_PID=$!; trap "kill $SERVER_PID" EXIT; for i in $(seq 1 30); do curl -sf http://localhost:4000/ >/dev/null && break; sleep 1; done; cd ../.. && HOST=http://localhost:4000 scripts/ci/http-smoke.sh && GITHUB_WORKSPACE=$(pwd) scripts/ci/admin-acceptance-smoke.sh --test all'</automated>
  </verify>
  <done>The example-host and generated-host smokes prove runtime wiring and parity through real HTTP and fresh installs, while staying intentionally narrow and complementary to ExUnit rather than duplicating it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library/example direct-path tests -> security-sensitive admin rules | Authorization, scope, impersonation, and export behavior must fail closed outside browser coverage |
| running Phoenix app -> shell smoke scripts | Real HTTP smoke proves only wiring/runtime seams and must not become a second functional runner |
| generated-host fresh install -> admin parity checks | Template/runtime parity must be proven on a newly generated host instead of inferred from example-app behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | E | direct-path admin policy coverage | mitigate | Per D-07, D-12, D-13, and D-14, keep denied/out-of-scope/blocked cases in library and example ExUnit so the security boundary is proven outside browser success flows. |
| T-31-08 | D | shell smoke scripts | mitigate | Per D-08, D-09, and D-18, constrain smoke scripts to boot, route, cookie, and minimal admin-critical status checks so they remain fast and trustworthy. |
| T-31-09 | T | generated-host parity | mitigate | Per D-10, D-11, and D-17, always scaffold, seed, and boot a fresh generated host before running narrow admin parity checks. |
</threat_model>

<verification>
Run the focused library and example direct-path suites first, then boot the example app for HTTP smoke and execute the generated-host acceptance smoke. The plan is complete only when direct-path truth and thin runtime parity both pass without broadening bash coverage into a second functional suite.
</verification>

<success_criteria>
- VFY-03 is satisfied through explicit direct-path and process-external coverage, not inferred from browser success.
- Generated-host parity is exercised on a fresh install.
- Shell smoke stays thin and policy-focused.
</success_criteria>

<output>
After completion, create `.planning/phases/31-automation-first-verification/31-automation-first-verification-03-SUMMARY.md`
</output>
