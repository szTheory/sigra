---
phase: 31-automation-first-verification
plan: 4
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-02
  - 31-03
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - VFY-01
  - VFY-02
  - VFY-03
  - VFY-04
must_haves:
  truths:
    - "CI runs admin browser truth, admin checkpoints, direct-path/runtime smoke, and generated-host parity as separate verification seams rather than one monolithic job."
    - "Dedicated admin verification jobs publish reviewer-usable artifacts on green runs and richer diagnostics on failures, with short-lived retention tuned to branch type."
    - "Mobile and dark-mode admin regressions are visible in CI artifacts without widening artifact uploads to unrelated jobs."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Dedicated admin verification jobs, scoped artifact uploads, and retention policy for example and generated-host admin review bundles"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "test/example/priv/playwright/playwright.config.ts"
      via: "CI job commands target the partitioned behavior and checkpoint projects"
      pattern: "playwright test|dark-chromium|admin-checkpoints"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/http-smoke.sh"
      via: "direct-path/runtime seam remains a dedicated job instead of being folded into browser jobs"
      pattern: "http-smoke|admin-acceptance-smoke"
    - from: ".github/workflows/ci.yml"
      to: "test/example/priv/playwright/playwright-report/"
      via: "scoped artifact publication for passing review bundles and failing diagnostics"
      pattern: "upload-artifact|playwright-report|test-results"
nyquist_compliant: true
---

<objective>
Wire the Phase 31 verification architecture into CI so each seam runs independently and publishes the artifact bundle reviewers need.

Purpose: Per D-19 through D-25 and D-26 through D-30, Phase 31 is not finished when tests merely pass locally; the CI workflow must preserve the admin milestone's automation-first review model with inspectable artifacts on green and richer diagnostics on failure.
Output: Updated CI jobs for example-admin browser truth, checkpoint capture, direct-path/runtime smoke, generated-host parity, and scoped artifact publication.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-automation-first-verification/31-CONTEXT.md
@.planning/phases/31-automation-first-verification/31-RESEARCH.md
@.planning/phases/31-automation-first-verification/31-PATTERNS.md
@.planning/phases/31-automation-first-verification/31-01-PLAN.md
@.planning/phases/31-automation-first-verification/31-02-PLAN.md
@.planning/phases/31-automation-first-verification/31-03-PLAN.md
@/Users/jon/projects/sigra/.github/workflows/ci.yml

<interfaces>
From `.github/workflows/ci.yml`:

```yaml
example_http_smoke:
example_playwright_smoke:
generated_admin_playwright_smoke:
```

From `actions/upload-artifact@v4` usage already in `ci.yml`:

```yaml
with:
  name: playwright-report
  path: test/example/priv/playwright/playwright-report/
  retention-days: 7
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split admin verification jobs by seam and publish green review bundles for the example app</name>
  <files>.github/workflows/ci.yml</files>
  <action>Update the example-app CI jobs per D-19 through D-25 and D-26 through D-30 so admin browser truth, admin checkpoints, and direct-path/runtime smoke remain separate seams. Add or retarget dedicated admin Playwright commands to run the canonical behavior specs and the checkpoint spec against the partitioned projects from Plan 31-01. Publish the Playwright HTML report plus curated checkpoint screenshots on every admin verification run, and upload `test-results/` only on failure so traces, failure screenshots, and any retained `video: 'retain-on-failure'` outputs stay scoped to diagnostics. Keep this policy confined to the dedicated admin jobs per D-22 instead of widening unrelated Playwright or smoke jobs.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const text = fs.readFileSync('.github/workflows/ci.yml', 'utf8');
const exampleJob = text.match(/example_playwright_smoke:[\s\S]*?(?=^\S|\Z)/m)?.[0];
if (!exampleJob) throw new Error('missing example_playwright_smoke job block');
const required = [
  'admin-user-operations.spec.ts',
  'impersonation.spec.ts',
  'admin-audit.spec.ts',
  'admin-checkpoints.spec.ts',
  'dark-chromium',
  'artifacts/admin-checkpoints/',
  'admin-example-report',
  'playwright-report/',
  'test-results/',
];
for (const needle of required) {
  if (!exampleJob.includes(needle)) {
    throw new Error(`missing example job text: ${needle}`);
  }
}
const artifactNames = [...exampleJob.matchAll(/name:\s*([A-Za-z0-9._-]+)/g)].map((m) => m[1]);
if (new Set(artifactNames).size !== artifactNames.length) {
  throw new Error('duplicate artifact names in example_playwright_smoke job');
}
if (!text.includes('example_http_smoke:')) {
  throw new Error('missing example_http_smoke job');
}
console.log('workflow example-admin structure ok');
NODE</automated>
  </verify>
  <done>The example-app CI section now runs the admin behavior, checkpoint, and runtime seams separately and always publishes a reviewer-usable green bundle without uploading the full diagnostic bundle on success.</done>
</task>

<task type="auto">
  <name>Task 2: Publish generated-host admin artifacts with scoped retention and no monolithic verification job</name>
  <files>.github/workflows/ci.yml</files>
  <action>Finish the CI wiring by updating the generated-host admin job and retention behavior per D-10, D-11, D-17, and D-19 through D-25. Keep generated-host verification explicit and narrow, preserve separate job boundaries instead of creating one monolithic verification job, and apply short-lived retention that is 7 days for push/PR review bundles and 14 days for main or release-oriented runs. Use stable artifact names for example-admin review bundles, generated-admin review bundles, and failure diagnostics so reviewers and maintainers can inspect mobile and dark-mode evidence without guesswork. Wire failure-bundle uploads so retained video from the selective `retain-on-failure` policy is actually published alongside traces and screenshots when those jobs fail.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const text = fs.readFileSync('.github/workflows/ci.yml', 'utf8');
const generatedJob = text.match(/generated_admin_playwright_smoke:[\s\S]*?(?=^\S|\Z)/m)?.[0];
if (!generatedJob) throw new Error('missing generated_admin_playwright_smoke job block');
for (const needle of ['admin-acceptance-smoke.sh', '--test all', 'playwright-report/', 'test-results/']) {
  if (!generatedJob.includes(needle)) {
    throw new Error(`missing generated job text: ${needle}`);
  }
}
const artifactNames = [...generatedJob.matchAll(/name:\s*([A-Za-z0-9._-]+)/g)].map((m) => m[1]);
if (new Set(artifactNames).size !== artifactNames.length) {
  throw new Error('duplicate artifact names in generated_admin_playwright_smoke job');
}
if (!text.match(/retention-days:\s*7/) || !text.match(/retention-days:\s*14/)) {
  throw new Error('missing branch-aware 7/14 retention rules');
}
console.log('workflow generated-admin structure ok');
NODE</automated>
  </verify>
  <done>The workflow publishes scoped example-admin and generated-admin artifacts with predictable names and branch-aware retention, and the admin milestone's verification seams remain decomposed rather than collapsed into one job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job graph -> verification evidence | Job decomposition controls whether failures isolate cleanly and whether reviewers can inspect the right artifact bundle |
| artifact upload policy -> repository-wide CI cost/noise | Admin review artifacts must stay scoped to the dedicated admin jobs rather than silently broadening repository-wide uploads |
| event type -> artifact retention | Retention must be short-lived and branch-aware so review bundles stay available long enough without turning into long-term storage clutter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-10 | D | CI job decomposition | mitigate | Per D-22 and the repo's existing pattern, keep direct-path smoke, example Playwright, and generated-host parity as separate jobs so failures isolate by seam. |
| T-31-11 | I | artifact publication policy | mitigate | Per D-19 through D-25, upload HTML report plus curated screenshots on success, and upload `test-results/` only on failure for dedicated admin jobs. |
| T-31-12 | D | retention configuration | mitigate | Per D-23, use short-lived retention with 7-day PR/push bundles and 14-day main/release bundles so artifacts remain inspectable without lingering indefinitely. |
</threat_model>

<verification>
Inspect the workflow for dedicated admin job commands, artifact names, and retention values, then confirm the CI graph still follows the repo's seam-based decomposition rather than a single monolithic verification job.
</verification>

<success_criteria>
- CI exposes distinct admin verification seams for browser truth, checkpoints, direct-path smoke, and generated-host parity.
- Green admin runs always publish reviewer-usable artifacts.
- Failure diagnostics remain richer but scoped.
</success_criteria>

<output>
After completion, create `.planning/phases/31-automation-first-verification/31-automation-first-verification-04-SUMMARY.md`
</output>
