---
phase: 28-user-operations-surface
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - lib/sigra/admin/users/hooks.ex
  - lib/sigra/admin/users/default_hooks.ex
  - test/example/lib/example/accounts/user.ex
  - test/example/priv/repo/migrations/20260416193000_add_display_name_to_users.exs
  - test/example/lib/example/sigra_admin_users.ex
  - test/example/lib/example/accounts.ex
  - test/sigra/admin/users_query_test.exs
  - test/sigra/admin/users_actions_test.exs
  - test/example/test/example_web/live/admin_user_index_live_test.exs
  - test/example/test/example_web/live/admin_user_filters_live_test.exs
  - test/example/test/example_web/live/admin_user_show_live_test.exs
  - test/example/priv/playwright/tests/admin-user-operations.spec.ts
autonomous: true
requirements:
  - USER-01
  - USER-02
must_haves:
  truths:
    - "Admins can be found by a real display name in the shipped example app, so operator-facing identification and name search are provable instead of hypothetical."
    - "List and detail surfaces can safely render host-provided display data and narrow admin-user extensions without custom forks or hidden glue code."
  artifacts:
    - path: "lib/sigra/admin/users/hooks.ex"
      provides: "Explicit host hook behaviour and runtime resolver for display-name/search/detail extensions"
      exports: ["behaviour_info", "display_name_field", "display_name", "extra_search_fields", "extra_list_badges", "extra_list_columns", "extra_detail_sections", "copy_overrides", "resolve"]
    - path: "lib/sigra/admin/users/default_hooks.ex"
      provides: "Concrete no-op default implementation for the Phase 28 hook contract"
      exports: ["display_name_field", "display_name", "extra_search_fields", "extra_list_badges", "extra_list_columns", "extra_detail_sections", "copy_overrides"]
    - path: "test/example/lib/example/sigra_admin_users.ex"
      provides: "Example host implementation of the Phase 28 admin user hooks"
    - path: "test/example/priv/repo/migrations/20260416193000_add_display_name_to_users.exs"
      provides: "Concrete example-app display_name search source"
  key_links:
    - from: "test/example/lib/example/sigra_admin_users.ex"
      to: "test/example/lib/example/accounts/user.ex"
      via: "display_name_field/0 and display_name/1"
      pattern: "display_name"
    - from: "mix.exs"
      to: "lib/sigra/admin/users/query.ex"
      via: "Flop and Flop.Phoenix dependencies"
      pattern: "flop"
---

<objective>
Lock the Phase 28 contracts before feature work starts: add the pagination/filtering dependency, define the narrow host hook seam required by D-31, give the example app a concrete display-name field for D-16 name search, and create the Wave 0 validation files called out in 28-VALIDATION.md before feature implementation begins.

Purpose: downstream list and detail plans should build against real interfaces, real Wave 0 contract files, and an explicit example-app data seam, not inferred assumptions about the user schema.
Output: dependency wiring, admin-user hook modules, example display-name support, and Wave 0 validation scaffolding.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-user-operations-surface/28-CONTEXT.md
@.planning/phases/28-user-operations-surface/28-RESEARCH.md
@.planning/phases/28-user-operations-surface/28-PATTERNS.md
@.planning/phases/28-user-operations-surface/28-UI-SPEC.md
@.planning/phases/27-admin-access-foundation/27-admin-access-foundation-02-SUMMARY.md
@.planning/phases/27-admin-access-foundation/27-admin-access-foundation-03-SUMMARY.md

<interfaces>
From [lib/sigra/admin/scope.ex](/Users/jon/projects/sigra/lib/sigra/admin/scope.ex):
```elixir
@type t :: %Sigra.Admin.Scope{
  mode: :global | :organization,
  scope: term(),
  organization: map() | nil,
  organization_id: term() | nil,
  organization_slug: String.t() | nil,
  platform_admin?: boolean(),
  admin_org_ids: [term()]
}
```

From [lib/sigra/admin/authorizer.ex](/Users/jon/projects/sigra/lib/sigra/admin/authorizer.ex):
```elixir
authorize_global!(admin_scope)
authorize_organization!(admin_scope, organization_or_id)
scope_query(queryable, admin_scope)
```

From [test/example/lib/example/accounts/user.ex](/Users/jon/projects/sigra/test/example/lib/example/accounts/user.ex):
```elixir
schema "users" do
  field :email, :string
  field :confirmed_at, :utc_datetime
  field :locked_at, :utc_datetime
  field :deleted_at, :utc_datetime
  field :must_change_password, :boolean, default: false
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 28 dependency and host hook contract</name>
  <read_first>
    mix.exs
    lib/sigra/admin/authorizer.ex
    test/example/AGENTS.md
    .planning/phases/28-user-operations-surface/28-CONTEXT.md
    .planning/phases/28-user-operations-surface/28-RESEARCH.md
    .planning/phases/28-user-operations-surface/28-PATTERNS.md
  </read_first>
  <files>mix.exs, mix.lock, lib/sigra/admin/users/hooks.ex, lib/sigra/admin/users/default_hooks.ex</files>
  <action>Update `mix.exs` to add `{:flop, "~> 0.26.3"}` and `{:flop_phoenix, "~> 0.26.0"}` in the main dependency list next to the Phoenix/Ecto web runtime dependencies, then refresh `mix.lock`. Create `lib/sigra/admin/users/hooks.ex` as the explicit Phase 28 host seam per D-31/D-32. That file must define only the behaviour callbacks and the runtime resolver; do not place the concrete default implementation in the same file because `test/example/AGENTS.md` forbids multiple modules in one file. The behaviour must expose these exact callbacks: `display_name_field/0 :: atom() | nil`, `display_name/1 :: String.t() | nil`, `extra_search_fields/0 :: [atom()]`, `extra_list_badges/1 :: [map()]`, `extra_list_columns/0 :: [map()]`, `extra_detail_sections/1 :: [map()]`, and `copy_overrides/0 :: map()`. These callbacks are the full D-31 extension set for extra search fields, extra list badges/columns, extra detail sections, and copy overrides; do not reduce them to only display-name support. In the same file, add an explicit runtime resolver function `resolve/1` that takes the Sigra config struct/module access path used by admin runtime code, checks whether the configured accounts module exports `admin_user_hooks/0`, and returns that module when present; otherwise it must return `Sigra.Admin.Users.DefaultHooks`. Create `lib/sigra/admin/users/default_hooks.ex` as the no-op implementation module for that behaviour. Keep the contract narrow and data-oriented; do not introduce macros, template copying, or a resource DSL. The default implementation must return `nil`, `nil`, `[]`, `[]`, `[]`, `[]`, and `%{}` respectively so later plans can always call the hooks safely.</action>
  <acceptance_criteria>
    - `mix.exs` contains the exact strings `{:flop, "~> 0.26.3"}` and `{:flop_phoenix, "~> 0.26.0"}`.
    - `lib/sigra/admin/users/hooks.ex` defines `@callback display_name_field/0`, `@callback display_name/1`, `@callback extra_search_fields/0`, `@callback extra_list_badges/1`, `@callback extra_list_columns/0`, `@callback extra_detail_sections/1`, and `@callback copy_overrides/0`.
    - `lib/sigra/admin/users/hooks.ex` exports `resolve/1`.
    - `lib/sigra/admin/users/hooks.ex` contains a concrete lookup path that calls `admin_user_hooks/0` on the configured accounts module when available and falls back to `Sigra.Admin.Users.DefaultHooks` otherwise.
    - `lib/sigra/admin/users/default_hooks.ex` defines the no-op implementation and returns `nil`, `nil`, `[]`, `[]`, `[]`, `[]`, and `%{}` for those callbacks.
  </acceptance_criteria>
  <verify>
    <automated>mix deps.get &amp;&amp; mix compile</automated>
  </verify>
  <done>The repo compiles with Flop installed and later plans have a concrete host hook contract instead of an implied seam.</done>
</task>

<task type="auto">
  <name>Task 2: Give the example app a real display-name implementation for D-16</name>
  <read_first>
    test/example/lib/example/accounts/user.ex
    test/example/lib/example/accounts.ex
    test/example/AGENTS.md
    test/example/priv/repo/migrations/20260410125242_create_sigra_auth_tables.exs
    .planning/phases/28-user-operations-surface/28-CONTEXT.md
    .planning/phases/28-user-operations-surface/28-UI-SPEC.md
  </read_first>
  <files>test/example/lib/example/accounts/user.ex, test/example/priv/repo/migrations/20260416193000_add_display_name_to_users.exs, test/example/lib/example/sigra_admin_users.ex, test/example/lib/example/accounts.ex</files>
  <action>From `test/example`, generate the migration with `mix ecto.gen.migration add_display_name_to_users`, then populate the generated `*_add_display_name_to_users.exs` file so it adds nullable `:display_name` to `users` and creates an index on `display_name`. Update `Example.Accounts.User` to declare `field :display_name, :string` and cast it in `registration_changeset/3` so fixtures and tests can set names directly. Create `Example.SigraAdminUsers` as the example hook module that implements `Sigra.Admin.Users.Hooks`; it must return `:display_name` from `display_name_field/0`, prefer `user.display_name` in `display_name/1`, return `[]` from `extra_search_fields/0`, `extra_list_badges/1`, `extra_list_columns/0`, and `extra_detail_sections/1`, and return `%{}` from `copy_overrides/0`. Update `Example.Accounts` to expose an `admin_user_hooks/0` helper returning `Example.SigraAdminUsers` so the `Sigra.Admin.Users.Hooks.resolve/1` path added in Task 1 has one concrete host source of truth to call.</action>
  <acceptance_criteria>
    - `test/example/lib/example/accounts/user.ex` contains `field :display_name, :string`.
    - `test/example/lib/example/accounts/user.ex` casts `:display_name` in `registration_changeset/3`.
    - The generated migration matching `test/example/priv/repo/migrations/*_add_display_name_to_users.exs` adds `:display_name` and an index on `display_name`.
    - `test/example/lib/example/sigra_admin_users.ex` contains `def display_name_field, do: :display_name`.
    - `test/example/lib/example/accounts.ex` contains `def admin_user_hooks, do: Example.SigraAdminUsers`.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example &amp;&amp; mix ecto.migrate &amp;&amp; mix compile</automated>
  </verify>
  <done>The shipped example app has a real name source and a concrete host hook implementation, so Phase 28 can satisfy name search without assuming every host schema looks identical.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Wave 0 contract files before feature implementation</name>
  <read_first>
    .planning/phases/28-user-operations-surface/28-VALIDATION.md
    .planning/phases/28-user-operations-surface/28-RESEARCH.md
    test/example/test/example_web/live/organization_members_live_test.exs
    test/example/priv/playwright/tests/organizations.spec.ts
  </read_first>
  <files>test/sigra/admin/users_query_test.exs, test/sigra/admin/users_actions_test.exs, test/example/test/example_web/live/admin_user_index_live_test.exs, test/example/test/example_web/live/admin_user_filters_live_test.exs, test/example/test/example_web/live/admin_user_show_live_test.exs, test/example/priv/playwright/tests/admin-user-operations.spec.ts</files>
  <action>Create the six Wave 0 contract files listed in `28-VALIDATION.md` before Plans 28-02 through 28-04 implement feature behavior. These files may start as red or pending contracts, but they must exist with named scenarios that lock the Phase 28 surface area: query/search/filter contracts in `test/sigra/admin/users_query_test.exs`, revoke and audit expectations in `test/sigra/admin/users_actions_test.exs`, list and filter UX contracts in the two admin index/filter LiveView tests, detail and org-pivot contracts in `admin_user_show_live_test.exs`, and the end-to-end browser journey in `test/example/priv/playwright/tests/admin-user-operations.spec.ts`. The files must encode the exact Phase 28 requirements and locked decisions even if the implementation is not green yet.</action>
  <acceptance_criteria>
    - All six files listed in this task exist on disk before execution moves to Plan 28-02.
    - `test/sigra/admin/users_query_test.exs` contains the strings `confirmed`, `mfa`, `passkeys`, `locked`, `deleted`, `provider`, `registered_from`, and `registered_to`.
    - `test/sigra/admin/users_actions_test.exs` contains the strings `revoke_session`, `revoke_all_sessions`, and `audit`.
    - `test/example/priv/playwright/tests/admin-user-operations.spec.ts` contains the strings `Open user`, `Revoke session`, and `@smoke`.
  </acceptance_criteria>
  <verify>
    <automated>test -f test/sigra/admin/users_query_test.exs &amp;&amp; test -f test/sigra/admin/users_actions_test.exs &amp;&amp; test -f test/example/test/example_web/live/admin_user_index_live_test.exs &amp;&amp; test -f test/example/test/example_web/live/admin_user_filters_live_test.exs &amp;&amp; test -f test/example/test/example_web/live/admin_user_show_live_test.exs &amp;&amp; test -f test/example/priv/playwright/tests/admin-user-operations.spec.ts</automated>
  </verify>
  <done>Wave 0 validation assets exist before feature implementation, so later plans can turn contracts green instead of inventing them late.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host hook module -> library runtime | Host-owned display/copy extensions cross into library-owned admin runtime |
| test contracts -> later implementation | Later plans must satisfy exact filters/actions instead of weakening scope or action semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `lib/sigra/admin/users/hooks.ex` | mitigate | Keep the hook API read-only and data-returning; no callback may bypass `Sigra.Admin.Authorizer` or mutate queries directly. |
| T-28-02 | Spoofing | `test/example/lib/example/sigra_admin_users.ex` | mitigate | Use an explicit `display_name` field in the example app instead of deriving a fake name from ambient admin context. |
| T-28-03 | Repudiation | Wave-0 test files | mitigate | Encode the exact revoke, scope, and section-order contracts in tests before implementation so later plans cannot silently reduce the source requirements. |
</threat_model>

<verification>
Run `mix compile`, then `cd test/example && mix ecto.migrate && mix compile`. This plan ends at a compiling contract/setup baseline; focused feature tests start in Plan 28-02.
</verification>

<success_criteria>
- Flop and Flop.Phoenix are installed and compiling.
- The admin-user host seam exists as an explicit behaviour plus a default implementation.
- The example app has a concrete `display_name` source and exported hook module.
- The repo is ready for the list/detail plans to add focused verification without first inventing host seams or example data.
</success_criteria>

<output>
After completion, create `.planning/phases/28-user-operations-surface/28-user-operations-surface-01-SUMMARY.md`
</output>
