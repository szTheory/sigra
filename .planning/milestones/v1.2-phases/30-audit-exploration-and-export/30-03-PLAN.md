---
phase: 30-audit-exploration-and-export
plan: 3
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
files_modified:
  - lib/sigra/admin/live/audit_user_live.ex
  - lib/sigra/admin/users/detail.ex
  - lib/sigra/admin/live/user_show_live.ex
  - test/example/lib/example_web/router.ex
  - priv/templates/sigra.install/admin/router_injection.ex
  - test/example/test/example_web/live/admin_audit_user_live_test.exs
  - test/example/test/example_web/live/admin_user_show_live_test.exs
autonomous: true
requirements:
  - AUD-02
  - AUD-03
must_haves:
  truths:
    - "Admins can investigate one user's full audit history from global and organization-scoped user routes using the same normalized filters as the main explorer."
    - "The Phase 28 recent-audit preview now reflects the same per-user subject semantics as the full user explorer, so preview rows do not disappear just because they use `effective_user_id` instead of `target_id`."
    - "Per-user audit routes preserve prior list/detail context and remain structurally scoped under the same global or organization boundary as the rest of the user detail surface."
  artifacts:
    - path: "lib/sigra/admin/live/audit_user_live.ex"
      provides: "Per-user audit explorer LiveView for global and organization user contexts"
    - path: "lib/sigra/admin/users/detail.ex"
      provides: "Phase 28 recent-audit preview aligned to the new per-user subject query semantics"
    - path: "lib/sigra/admin/live/user_show_live.ex"
      provides: "Full-audit entry point from the existing user detail page"
    - path: "test/example/lib/example_web/router.ex"
      provides: "Per-user audit explorer routes under both global and organization user paths"
  key_links:
    - from: "lib/sigra/admin/live/audit_user_live.ex"
      to: "lib/sigra/admin/audit/explorer.ex"
      via: "per-user explorer delegates to the same normalized explorer service"
      pattern: "Sigra\\.Admin\\.Audit\\.Explorer"
    - from: "lib/sigra/admin/users/detail.ex"
      to: "lib/sigra/admin/audit/query.ex"
      via: "recent-audit preview uses the same subject-user query semantics as the full explorer"
      pattern: "subject|effective_user_id|target_id"
    - from: "lib/sigra/admin/live/user_show_live.ex"
      to: "test/example/lib/example_web/router.ex"
      via: "detail page links into scoped per-user audit routes with return context"
      pattern: "/audit|return_to"
nyquist_compliant: true
---

<objective>
Extend the new explorer model into the per-user investigation path, then align the existing Phase 28 recent-audit preview to the same subject-user semantics so one-user history stays consistent everywhere.

Purpose: The user prompt explicitly requires per-user explorer support and Phase 28 preview alignment, and research finding 2 shows the current preview misses important rows when it filters only `target_id`.
Output: Per-user audit explorer LiveView, routed entry points from user detail, aligned recent-audit preview logic, and focused example tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-audit-exploration-and-export/30-RESEARCH.md
@.planning/phases/30-audit-exploration-and-export/30-PATTERNS.md
@.planning/phases/30-audit-exploration-and-export/30-VALIDATION.md
@.planning/phases/28-user-operations-surface/28-CONTEXT.md
@.planning/phases/29-secure-impersonation/29-CONTEXT.md
@.planning/phases/30-audit-exploration-and-export/30-01-PLAN.md
@.planning/phases/30-audit-exploration-and-export/30-02-PLAN.md
@/Users/jon/projects/sigra/lib/sigra/admin/live/user_show_live.ex
@/Users/jon/projects/sigra/lib/sigra/admin/users/detail.ex
@/Users/jon/projects/sigra/test/example/lib/example_web/router.ex

<interfaces>
From `lib/sigra/admin/live/user_show_live.ex`:

```elixir
def handle_params(%{"id" => user_id} = params, _uri, socket)
def render(assigns)
```

From `lib/sigra/admin/users/detail.ex`:

```elixir
load!(config, admin_scope, user_id)
recent_audit_preview(config, admin_scope, user_id)
```

From `lib/sigra/admin/audit/explorer.ex`:

```elixir
list(config, admin_scope, params_or_filters)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add example tests for per-user audit routes and preview alignment</name>
  <files>test/example/test/example_web/live/admin_audit_user_live_test.exs, test/example/test/example_web/live/admin_user_show_live_test.exs</files>
  <behavior>
    - Test 1: per AUD-02, `/admin/users/:id/audit` and `/admin/organizations/:org/users/:id/audit` load a scoped per-user explorer and preserve filter context in the URL.
    - Test 1a: the org-scoped per-user route intentionally includes that user's global support rows in addition to org-local rows, matching the resolved `organization_scope: {:including_global, org_id}` behavior from research.
    - Test 2: per research finding 2, the Phase 28 recent-audit preview and the full per-user explorer both include rows that match through `effective_user_id`, not just `target_id`.
    - Test 3: the user detail page exposes an intentional "View full audit" path that preserves `return_to` or equivalent local admin context rather than dropping operators onto an unscoped page.
  </behavior>
  <action>Add focused example tests before implementation. Create `admin_audit_user_live_test.exs` for the per-user global and organization routes, including at least one row that would be absent under the old `target_id`-only preview semantics and one org-scoped case that intentionally keeps the same user's global support activity visible via `organization_scope: {:including_global, org_id}`. Extend `admin_user_show_live_test.exs` to require a visible full-audit entry point from the existing Recent Audit section and to assert that the recent preview stays aligned with the new subject-user explorer behavior. Keep export coverage out of this plan; it lands after the full per-user surface exists.</action>
  <verify>
    <automated>cd test/example && mix test test/example_web/live/admin_audit_user_live_test.exs test/example_web/live/admin_user_show_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Focused example tests describe the per-user audit route contract, the full-audit entry point from user detail, and the preview/explorer alignment before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement per-user audit explorer routes and align the Phase 28 preview to the shared subject-user query contract</name>
  <files>lib/sigra/admin/live/audit_user_live.ex, lib/sigra/admin/users/detail.ex, lib/sigra/admin/live/user_show_live.ex, test/example/lib/example_web/router.ex, priv/templates/sigra.install/admin/router_injection.ex</files>
  <action>Implement the per-user audit slice using the shared Phase 30 explorer runtime rather than a second query path. Create `Sigra.Admin.Live.AuditUserLive` for `/admin/users/:id/audit` and `/admin/organizations/:org/users/:id/audit`, keeping the current scope structural boundary intact and preserving local return context back to the user detail page. For the org-scoped per-user route, intentionally preserve the Phase 28 continuity behavior by querying with `organization_scope: {:including_global, org_id}` so the operator sees org-local plus globally-originated support rows for that same user; keep org-wide explorer pages on `{:only, org_id}` from Plan 30-02. Update `Sigra.Admin.Users.Detail.recent_audit_preview/3` so it uses the same subject-user query semantics from Plan 30-01 instead of filtering only `target_id`, which is the gap identified in research finding 2. Update `Sigra.Admin.Live.UserShowLive` to expose a visible full-audit entry point from the Recent Audit section, and wire the same routes into both the example router and generated admin router injection. Reuse the shared normalized filter contract and presenter helpers from the earlier plans; do not fork a "preview-only" or "user-detail-only" audit model.</action>
  <verify>
    <automated>cd test/example && mix test test/example_web/live/admin_audit_user_live_test.exs test/example_web/live/admin_user_show_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Admins can move from a user detail page into a full scoped audit explorer for that user, and the Recent Audit preview stays consistent with the full explorer instead of dropping `effective_user_id` rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user detail route params -> per-user audit explorer | Untrusted user ids must stay within the resolved admin scope and not pivot to other users or organizations |
| user detail preview -> full audit explorer | Preview and full explorer must share one evidence model so operators are not misled by mismatched history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | E | per-user audit routes | mitigate | Mount global and organization per-user explorer routes under the existing scope split and load the user through the same scope-safe admin path. |
| T-30-08 | I | recent-audit preview alignment | mitigate | Reuse the subject-user query semantics from Plan 30-01 so preview and explorer show the same underlying history dimensions. |
| T-30-09 | R | full-audit entry from user detail | mitigate | Preserve explicit scoped routes and `return_to` context so the investigation trail remains understandable and reproducible. |
</threat_model>

<verification>
Run the focused example suites for per-user audit routes and user detail. Confirm that per-user explorer routes load, preview rows stay aligned, and the user detail page links into the full scoped audit surface.
</verification>

<success_criteria>
- Per-user audit exploration works in both global and organization routes.
- The old `target_id`-only preview gap is closed through the shared subject-user query semantics.
- User detail remains the intentional bridge into full audit investigation instead of a dead-end preview.
</success_criteria>

<output>
After completion, create `.planning/phases/30-audit-exploration-and-export/30-audit-exploration-and-export-03-SUMMARY.md`
</output>
