---
phase: 30-audit-exploration-and-export
plan: 4
type: execute
wave: 4
depends_on:
  - 30-02
  - 30-03
files_modified:
  - lib/sigra/admin/audit/csv_export.ex
  - lib/sigra/admin/audit/export.ex
  - test/example/lib/example_web/controllers/admin/audit_export_controller.ex
  - test/example/lib/example_web/router.ex
  - priv/templates/sigra.install/admin/router_injection.ex
  - priv/templates/sigra.install/admin/audit_export_controller.ex
  - test/example/test/example_web/controllers/admin/audit_export_controller_test.exs
  - test/example/priv/playwright/tests/admin-audit.spec.ts
autonomous: true
requirements:
  - AUD-04
  - AUD-02
  - AUD-03
must_haves:
  truths:
    - "Admins can download the currently filtered audit slice as CSV from global, organization, and per-user explorer flows without the export changing filter semantics."
    - "CSV export stays scope-respecting and uses a stable column schema that preserves canonical actor, effective user, organization, action, outcome, and impersonation state."
    - "Explorer and export verification cover both direct-path controller behavior and browser-visible operator flows."
  artifacts:
    - path: "lib/sigra/admin/audit/export.ex"
      provides: "Shared export orchestration over the normalized audit filter contract"
    - path: "lib/sigra/admin/audit/csv_export.ex"
      provides: "Stable CSV header/row mapping for audit evidence"
    - path: "test/example/lib/example_web/controllers/admin/audit_export_controller.ex"
      provides: "Thin example controller seam for CSV download"
    - path: "priv/templates/sigra.install/admin/audit_export_controller.ex"
      provides: "Generated controller parity for audit export"
    - path: "test/example/priv/playwright/tests/admin-audit.spec.ts"
      provides: "Browser verification for filter-driven explorer and export flows"
  key_links:
    - from: "test/example/lib/example_web/controllers/admin/audit_export_controller.ex"
      to: "lib/sigra/admin/audit/export.ex"
      via: "thin controller delegates normalized params into shared export service"
      pattern: "Sigra\\.Admin\\.Audit\\.Export"
    - from: "lib/sigra/admin/audit/export.ex"
      to: "lib/sigra/admin/audit/query_params.ex"
      via: "export reuses the exact normalized filter contract from the explorer UI"
      pattern: "Sigra\\.Admin\\.Audit\\.QueryParams"
    - from: "test/example/lib/example_web/router.ex"
      to: "priv/templates/sigra.install/admin/router_injection.ex"
      via: "generated/example parity for export endpoints across global and org/user scopes"
      pattern: "export\\.csv"
    - from: "test/example/priv/playwright/tests/admin-audit.spec.ts"
      to: "test/example/test/example_web/controllers/admin/audit_export_controller_test.exs"
      via: "browser flow and direct-path controller coverage prove the same export semantics"
      pattern: "audit export|text/csv|download"
nyquist_compliant: true
---

<objective>
Finish Phase 30 by adding stable CSV export over the exact same normalized filter contract as the explorer UI, then verify the full investigation flow through focused controller and browser coverage.

Purpose: AUD-04 is only satisfied if export is scope-safe, filter-identical to the UI, and proven outside the happy-path browser render alone.
Output: Shared export service and CSV mapper, generated/example controller and route wiring, direct-path export tests, and Playwright coverage for audit investigation/export flows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-audit-exploration-and-export/30-RESEARCH.md
@.planning/phases/30-audit-exploration-and-export/30-PATTERNS.md
@.planning/phases/30-audit-exploration-and-export/30-VALIDATION.md
@.planning/phases/30-audit-exploration-and-export/30-01-PLAN.md
@.planning/phases/30-audit-exploration-and-export/30-02-PLAN.md
@.planning/phases/30-audit-exploration-and-export/30-03-PLAN.md
@/Users/jon/projects/sigra/test/example/lib/example_web/controllers/admin/impersonation_controller.ex
@/Users/jon/projects/sigra/test/example/lib/example_web/router.ex
@/Users/jon/projects/sigra/priv/templates/sigra.install/admin/router_injection.ex
@/Users/jon/projects/sigra/test/example/priv/playwright/tests/impersonation.spec.ts

<interfaces>
From `test/example/lib/example_web/controllers/admin/impersonation_controller.ex`:

```elixir
def create(conn, params)
def delete(conn, params)
```

From `lib/sigra/admin/audit/explorer.ex`:

```elixir
list(config, admin_scope, params_or_filters)
```

From `lib/sigra/admin/audit/query_params.ex`:

```elixir
normalize(params, admin_scope)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add controller and browser tests for scope-safe CSV export over the shared filter contract</name>
  <files>test/example/test/example_web/controllers/admin/audit_export_controller_test.exs, test/example/priv/playwright/tests/admin-audit.spec.ts</files>
  <behavior>
    - Test 1: per AUD-04, the export controller returns `text/csv` for the currently filtered slice and keeps the same scope restrictions as the matching explorer route.
    - Test 2: per research finding 5, the export endpoint accepts the exact same normalized filter params as the explorer UI rather than a second export-only contract.
    - Test 2a: the CSV header order is fixed and excludes raw `metadata`, relying instead on canonical and derived columns for impersonation visibility.
    - Test 2b: cells that begin with dangerous spreadsheet prefixes (`=`, `+`, `-`, `@`, tab, carriage return) are escaped or emitted through a library-backed mitigation so exported evidence cannot trigger formula execution when opened in spreadsheet software.
    - Test 3: browser coverage proves an operator can filter a global or organization audit explorer, trigger CSV export, and keep visible impersonation distinctions in the on-page rows.
  </behavior>
  <action>Before implementation, add a focused example controller test file for the new export endpoint that asserts content type, deterministic header order, scoped row selection, filter parity with the explorer contract, the explicit absence of raw `metadata` columns from the v1 CSV schema, and CSV formula-injection mitigation for dangerous cell prefixes. In the same task, add `test/example/priv/playwright/tests/admin-audit.spec.ts` covering one global and one organization or per-user audit investigation flow with filter changes, visible impersonation labeling, and CSV download trigger/assertions. Keep the browser flow narrow and representative; the heavy correctness burden stays in the controller and service tests.</action>
  <verify>
    <automated>cd test/example && mix test test/example_web/controllers/admin/audit_export_controller_test.exs --max-failures 1 && pnpm exec playwright test priv/playwright/tests/admin-audit.spec.ts --project=chromium</automated>
  </verify>
  <done>The repo has focused direct-path and browser tests that describe scope-safe CSV export and filter parity before the export service is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared CSV export, generated/example controller wiring, and final audit-flow verification</name>
  <files>lib/sigra/admin/audit/csv_export.ex, lib/sigra/admin/audit/export.ex, test/example/lib/example_web/controllers/admin/audit_export_controller.ex, test/example/lib/example_web/router.ex, priv/templates/sigra.install/admin/router_injection.ex, priv/templates/sigra.install/admin/audit_export_controller.ex</files>
  <action>Implement Phase 30's export path as a thin controller seam over the exact same normalized filter contract used by the explorer UI. Create `Sigra.Admin.Audit.Export` to reuse `Sigra.Admin.Audit.QueryParams`, the shared explorer/query runtime, and stable ordering for export rows. Create `Sigra.Admin.Audit.CSVExport` with a fixed v1 column order that includes canonical ids, human-readable labels, action, outcome, timestamps, and derived impersonation state, explicitly excludes raw `metadata`, and mitigates spreadsheet-formula injection for dangerous cell prefixes. Use either a vetted library-backed mitigation such as NimbleCSV's documented escaping support or an explicit dangerous-prefix escaping pass inside `CSVExport`; whichever path lands, pin it in `admin_audit_export_controller_test.exs`. Add thin audit export controllers in both the example app and generated templates, and wire export endpoints for the global, organization, and per-user routes into both `test/example/lib/example_web/router.ex` and `priv/templates/sigra.install/admin/router_injection.ex`. The export endpoints must accept the same query-string filters as the UI and must preserve Phase 27/28 scope guarantees. Finish by turning the new controller and Playwright specs green; do not invent an async export job, background queue, or alternate export filter format in this phase.</action>
  <verify>
    <automated>cd test/example && mix test test/example_web/controllers/admin/audit_export_controller_test.exs --max-failures 1 && pnpm exec playwright test priv/playwright/tests/admin-audit.spec.ts --project=chromium</automated>
  </verify>
  <done>Admins can export the currently filtered audit slice as stable CSV across the supported scopes, and both direct-path and browser verification prove the export matches the explorer semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explorer query string -> export controller | Evidence download must not accept a looser filter contract than the UI |
| export service -> CSV output | Stable evidence files must preserve canonical actor/effective-user fields and avoid scope bleed |
| browser download flow -> generated/example routes | The visible export action must stay aligned with the mounted scoped routes in both example and generated apps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | T | export controller params | mitigate | Reuse `Sigra.Admin.Audit.QueryParams` unchanged so export cannot interpret a broader or different filter set than the explorer UI. |
| T-30-11 | I | CSV output | mitigate | Emit a fixed canonical column order that includes actor/effective-user/organization and impersonation state, and assert it in controller tests. |
| T-30-13 | T | spreadsheet consumers of CSV evidence | mitigate | Escape dangerous spreadsheet prefixes in `Sigra.Admin.Audit.CSVExport` or use a vetted CSV library mitigation, and assert the behavior in controller tests. |
| T-30-12 | E | scoped export routes | mitigate | Mount global, organization, and per-user export endpoints under the same existing admin scope boundaries as the explorer routes. |
</threat_model>

<verification>
Run the focused export controller suite, then the Playwright audit spec. The phase is complete only when both direct-path and browser checks prove the same scoped filter-to-export behavior.
</verification>

<success_criteria>
- CSV export uses the exact same normalized filter contract as the explorer UI.
- Export routes stay scope-safe across global, organization, and per-user views.
- Phase 30 ends with both ExUnit and browser verification for audit investigation and export flows.
</success_criteria>

<output>
After completion, create `.planning/phases/30-audit-exploration-and-export/30-audit-exploration-and-export-04-SUMMARY.md`
</output>
