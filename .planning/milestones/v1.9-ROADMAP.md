# Milestone v1.9: Audit atomicity (bounded SEED-002)

**Status:** SHIPPED 2026-04-23  
**Phases:** 66–67  
**Total plans (on-disk):** 3 — **`066-01`**, **`066-02`**, **`067-01`** under **`.planning/phases/066-*`** / **`067-*`**.

## Overview

Continue **SEED-002** in a **bounded** batch: move a **C-1** deferral slice from hybrid **`log_safe/3`** post-commit audit to **`Ecto.Multi`** + **`Sigra.Audit.log_multi_safe/3`** (or documented substitute) with merge-gated audit-aware tests, then align **Phase 9** executive summary and **C-1** matrix with post-batch truth without unnecessary **`09-VERIFICATION.md`** churn.

## Phases (archived table)

| Phase | Name | Goal | Requirements | Success criteria |
|-------|------|------|--------------|------------------|
| **66** | **SEED-002 bounded batch** | Convert the next **C-1** deferral slice to **`Ecto.Multi`** + **`log_multi_safe`** (or documented substitute) with audit-aware tests. | AUD-09 | 1) Inventory row(s) for this batch identified and linked in phase planning. 2) Production path(s) co-fate audit with business writes per **T1** semantics in **`docs/audit-semantics.md`**. 3) New or extended tests assert audit row(s) on success and absence/rollback on intentional failure. 4) CI green (`library_tests` + any scoped contracts touched). |
| **67** | **C-1 planning closure** | Align Phase **9** executive summary + **C-1** matrix with post-batch truth. | AUD-10 | 1) **`09-03-SUMMARY.md`** lists phase **66** batch with mechanism pointer. 2) **`09-VERIFICATION.md`** updated for any row whose verdict/tier/mechanism changed; otherwise explicit rationale for no edit. 3) Cross-links to inventories remain valid. |

**Coverage:** 2 requirements → 2 phases. Phase numbering continues from **v1.8** (last phase **65**).

### Success criteria (milestone)

1. At least one **AUD-04** / **C-1** batch from Phase **9** deferrals ships with **T1**-style atomic audit + regression tests (**AUD-09**).
2. **`09-03-SUMMARY.md`** honestly narrates the batch; **D-06**-class attestation when **`09-VERIFICATION.md`** needs no edit (**AUD-10**).

---

## Milestone summary

**Key decisions**

- **Bounded SEED-002:** Ship one production batch + planning closure per milestone, not a whole-library conversion.

**Issues resolved**

- **`confirm_enrollment/5`** **AUD-04-020..021** paths co-fated with audit via **`Multi`** + **`mfa_audit_atomicity_test.exs`** coverage.
- **Executive summary** and **C-1** trace aligned after phase **66** without spurious verification file edits.

**Technical debt / follow-ups**

- **`gsd-sdk query milestone.complete`** still fails (`version required for phases archive`); manual archival (v1.3–**v1.9** pattern).
- **SEED-002** remainder — further **`log_safe/3` → `Ecto.Multi`** batches remain backlog-triggered (see **`.planning/ROADMAP.md`** Backlog).

---

_For current roadmap, see [.planning/ROADMAP.md](../ROADMAP.md)._
