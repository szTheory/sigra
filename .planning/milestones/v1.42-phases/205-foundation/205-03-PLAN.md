---
phase: 205-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 205-02
files_modified:
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - test/example/priv/playwright/tests/admin-design.spec.ts
autonomous: true
requirements:
  - INSTR-02

must_haves:
  truths:
    - "design_gallery_live.ex contains 4 board-cfg-* composites: board-cfg-overview, board-cfg-users-list, board-cfg-user-detail, board-cfg-audit (D-08)"
    - "Each composite uses only sg-* primitives + Sigra.Admin.Components with static literal assigns — no DB, no Query, no scope/session/Repo coupling (D-09)"
    - "Each composite carries an in-LiveView comment pointing to its source archetype + real LiveView file (D-09)"
    - "admin-design.spec.ts has a CONFIG_BOARDS array with the 4 composite ids; composites are folded into the element-scoped screenshot loop and the responsive/overflow loop (D-10)"
    - "Snapshot test passes: board-cfg-overview, board-cfg-users-list, board-cfg-user-detail, board-cfg-audit snapshots are clean across chromium/mobile/dark (D-11)"
    - "Impersonation-banner and board-notice canaries remain byte-stable; both snapshot allowlists empty at phase close (D-11)"
    - "Lightweight per-composite structural assertion exists in admin-design.spec.ts (D-09)"
    - "maxDiffPixels for CONFIG_BOARDS boards is above MG-board values (larger surfaces, D-10)"
  artifacts:
    - "board-cfg-overview composite in design_gallery_live.ex"
    - "board-cfg-users-list composite in design_gallery_live.ex"
    - "board-cfg-user-detail composite in design_gallery_live.ex"
    - "board-cfg-audit composite in design_gallery_live.ex"
    - "CONFIG_BOARDS array in admin-design.spec.ts"
  key_links:
    - "board-cfg-overview -> index_live.ex Overview archetype composition (admin-design-contract.md Overview section)"
    - "board-cfg-users-list -> users_index_live.ex List archetype composition (admin-design-contract.md List section)"
    - "board-cfg-user-detail -> user_show_live.ex Detail archetype composition (admin-design-contract.md Detail section)"
    - "board-cfg-audit -> audit_index_live.ex Audit archetype composition (admin-design-contract.md)"
    - "CONFIG_BOARDS in admin-design.spec.ts -> element-scoped capture loop (inherits waitForLiveViewReady + Space Grotesk font gate)"
---

<objective>
Author the 4 `board-cfg-*` real-page composite boards in `design_gallery_live.ex` and register them in `admin-design.spec.ts` via a new `CONFIG_BOARDS` array. Composites use the same static-literal-assign pattern as MG-1…MG-11, assembled from `sg-*` primitives + `Sigra.Admin.Components`, mirroring each of the 4 admin page archetypes in their loaded (populated) state. Run Playwright to capture initial baselines; verify canaries byte-stable; confirm both allowlists empty at close.

Purpose: Isolated MG boards show individual group health; composites catch composition-level bugs the isolated boards cannot — spacing rhythm, card-in-card, header→notice→grid flow, cross-MG visual conflict. The `board-cfg-*` boards are the evidence substrate for Phase 208 L2 elevation work.

Output: Modified `design_gallery_live.ex` (4 new board sections at end of file before closing tag); modified `admin-design.spec.ts` (CONFIG_BOARDS array + folded into screenshot/responsive/structural assertion loops).

Note: This plan depends on Plan 02 (FIXT-01) because zoe and ghost-org change the `/admin/users` row count (46 total, 2 pages). Snapshot captures must happen AFTER the seeds are committed, so the board-cfg-users-list composite (which uses static literal assigns, NOT live data) is not affected — but the admin-checkpoints baselines for real pages may drift. The composites themselves use static assigns and are unaffected by seed count; their captures are purely gallery-driven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-foundation/205-CONTEXT.md
@test/example/lib/example_web/live/admin/design_gallery_live.ex
@test/example/priv/playwright/tests/admin-design.spec.ts
@guides/reference/admin-design-contract.md
@lib/sigra/admin/components.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 board-cfg-* composites in design_gallery_live.ex (D-08, D-09)</name>
  <files>test/example/lib/example_web/live/admin/design_gallery_live.ex</files>
  <read_first>
    - test/example/lib/example_web/live/admin/design_gallery_live.ex — full file; read the pattern for board-mg-1 through board-mg-3 to understand the static-assign composite structure; note the section headings (Components, Groups), the sg-card wrapper pattern, in-board comment style
    - guides/reference/admin-design-contract.md — Overview Archetype section (~lines 172-209), List Archetype section (~lines 211-283), Detail Archetype section (~lines 284-320) — these are the composition blueprints
    - lib/sigra/admin/components.ex — scan for available function components: notice/1, task_card/1, summary_chip/1, stat/1, applied_chip/1, scope_ribbon/1, page_back/1, empty_state/1, audit_table_row/1, skeleton/1 — note the slot structures (especially notice's inner-block slots)
    - lib/sigra/admin/live/index_live.ex — skim header + notice section + stat-strip pattern for Overview archetype reference
    - lib/sigra/admin/live/users_index_live.ex — skim header + scope_ribbon + filter-panel + metric-strip + table pattern for List archetype reference
    - lib/sigra/admin/live/user_show_live.ex — skim header + scope_ribbon + page_back + identity section + detail panels for Detail archetype reference
    - lib/sigra/admin/live/audit_index_live.ex — skim header + filter-panel + results table for Audit archetype reference
  </read_first>
  <action>
    In `design_gallery_live.ex`, add a new top-level `<section class="sg-stack sg-stack--4">` titled "Page Composites" (with `<h2 class="sg-section-heading">Page Composites</h2>`) immediately before the closing `</section>` of the outermost `sg-stack sg-stack--6` wrapper. Inside this section, add the 4 boards:

    BOARD: board-cfg-overview
    - id: "board-cfg-overview"
    - Outer: `<section id="board-cfg-overview" class="sg-stack sg-stack--4">` (no sg-card wrapper — Overview is a full-page archetype using direct section children)
    - Source archetype comment: "board-cfg-overview — Overview archetype; see index_live.ex + admin-design-contract.md Overview Archetype"
    - Content mirrors the Overview archetype: sg-page-header (kicker + h1 + copy), one .notice tone={:risk} (populated alarm with inline text e.g. "2 accounts locked — Review accounts"), then a sg-grid sg-grid--3 with 3 .task_card elements (e.g. "Manage users", "Review audit trail", "Manage organizations"). Use all static literal string assigns. NO `sg-metric-grid` strip (Overview archetype has task cards, not the stats-strip — that is the List archetype's demoted metric section).

    BOARD: board-cfg-users-list
    - id: "board-cfg-users-list"
    - Outer: `<section id="board-cfg-users-list" class="sg-stack sg-stack--4">`
    - Source archetype comment: "board-cfg-users-list — List archetype; see users_index_live.ex + admin-design-contract.md List Archetype"
    - Content mirrors the List archetype: sg-page-header (kicker "User operations" + h1 "Users"), scope_ribbon (label "All users"), then a sg-stack sg-stack--4 section with h2 "Find users" + a static filter-panel (sg-filter-panel sg-stack with a static input + a static applied_chip "Status: Locked" + a "Clear all" link), then a sg-stack sg-stack--3 section h2 "User health" with a sg-metric-grid with 3 .summary_chip elements (Total users neutral, Locked risk, Deletion scheduled warn). Use static literal assigns. No live data.

    BOARD: board-cfg-user-detail
    - id: "board-cfg-user-detail"
    - Outer: `<section id="board-cfg-user-detail" class="sg-stack sg-stack--4">`
    - Source archetype comment: "board-cfg-user-detail — Detail archetype; see user_show_live.ex + admin-design-contract.md Detail Archetype"
    - Content mirrors the Detail archetype: sg-page-header (kicker + h1 with an email address literal), scope_ribbon, then a sg-card sg-stack sg-stack--3 section with identity summary (display name, email code.sg-code, status pills), then a sg-card sg-stack sg-stack--3 section titled "Sessions" with one row showing a session item, then a .empty_state (title="No MFA credentials") representing the zero-state panel — demonstrating both populated and empty sections in one composite. Use static literal assigns.

    BOARD: board-cfg-audit
    - id: "board-cfg-audit"
    - Outer: `<section id="board-cfg-audit" class="sg-stack sg-stack--4">`
    - Source archetype comment: "board-cfg-audit — Audit archetype; see audit_index_live.ex + admin-design-contract.md"
    - Content mirrors the Audit/List archetype applied to audit: sg-page-header (kicker "Audit" + h1 "Audit events"), then a sg-stack sg-stack--4 section for the filter panel (static date inputs + action filter + a static applied_chip + "Clear all"), then a table (class="sg-table") with thead (Event, Actor, Target, Occurred at) and 2 static .audit_table_row calls. Use static literal assigns.

    Constraints:
    - All assigns are static string/atom literals — no assigns coming from socket/DB/Repo
    - No `import Ecto.Query` or any DB module — the existing gallery module has none and must stay clean
    - sg-* CSS classes must match the existing gallery patterns exactly
    - Each board must have a data-sg attribute or comment pointing to source archetype + real LiveView filename
    - No card-in-card nesting (sg-card inside sg-card) — use section/div/article for inner grouping
  </action>
  <verify>
    <automated>
      grep -c "board-cfg-overview\|board-cfg-users-list\|board-cfg-user-detail\|board-cfg-audit" test/example/lib/example_web/live/admin/design_gallery_live.ex
      grep -c "Page Composites" test/example/lib/example_web/live/admin/design_gallery_live.ex
      cd test/example && mix compile --warnings-as-errors 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    All 4 board-cfg-* ids appear in `design_gallery_live.ex`; "Page Composites" section heading is present; `mix compile --warnings-as-errors` passes in test/example/; no Ecto.Query or DB module is imported in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register CONFIG_BOARDS in admin-design.spec.ts, add structural assertion, capture baselines (D-10, D-11)</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts — full file; focus on: COMPONENT_BOARDS and GROUP_BOARDS arrays (lines 91-109), the `for (const boardId of [...COMPONENT_BOARDS, ...GROUP_BOARDS])` screenshot loop (line 248), the responsive/overflow loop (line 260-277), the GROUP_STATE_MARKERS record (lines 111-123), and the assertBoardScreenshot helper (lines 77-87) with its maxDiffPixels values
    - test/example/lib/example_web/live/admin/design_gallery_live.ex — after Task 1; confirm the 4 board-cfg-* ids and their internal structure for the structural assertion selector
  </read_first>
  <action>
    Edit `admin-design.spec.ts`:

    1. Add `CONFIG_BOARDS` array after GROUP_BOARDS (after line ~109, before GROUP_STATE_MARKERS):
       `const CONFIG_BOARDS = ['board-cfg-overview', 'board-cfg-users-list', 'board-cfg-user-detail', 'board-cfg-audit'] as const;`

    2. In the `assertBoardScreenshot` function (lines ~77-87), add a condition for CONFIG_BOARDS with HIGHER maxDiffPixels/maxDiffPixelRatio than MG-board values. The current MG-board values are:
       - maxDiffPixels: `ci ? 200_000 : dark ? 75_000 : mobile ? 45_000 : 30_000`
       - maxDiffPixelRatio: `ci ? 0.22 : dark ? 0.1 : mobile ? 0.08 : 0.06`
       For CONFIG_BOARDS (larger surfaces), use:
       - maxDiffPixels: `ci ? 300_000 : dark ? 120_000 : mobile ? 80_000 : 50_000`
       - maxDiffPixelRatio: `ci ? 0.30 : dark ? 0.15 : mobile ? 0.12 : 0.09`
       Detect CONFIG_BOARDS membership by checking if boardId starts with "board-cfg-": `const isCfgBoard = boardId.startsWith('board-cfg-');`
       Apply the higher budget when isCfgBoard is true.

    3. In the main screenshot loop (line ~248), spread CONFIG_BOARDS in:
       `for (const boardId of [...COMPONENT_BOARDS, ...GROUP_BOARDS, ...CONFIG_BOARDS])`

    4. In the responsive/overflow loop (line ~260-277), spread CONFIG_BOARDS similarly:
       `for (const boardId of [...COMPONENT_BOARDS, ...GROUP_BOARDS, ...CONFIG_BOARDS])`

    5. Add a structural assertion test for CONFIG_BOARDS (mirroring the "component and group boards do not overflow" pattern but for cfg boards). Add a new test in the 'Design gallery board snapshots' describe block:
       ```
       test('config boards expose expected archetype sections', async ({ page }) => {
         for (const boardId of CONFIG_BOARDS) {
           const board = page.locator(`#${boardId}`);
           await expect(board).toBeVisible();
           // Each composite must have an h1 or h2 (archetype header)
           const headings = board.locator('h1, h2');
           await expect(headings.first()).toBeVisible();
         }
       });
       ```

    6. The test registration count check (`expect(COMPONENT_BOARDS).toHaveLength(13)`) should remain unchanged — it checks COMPONENT_BOARDS only.

    After code changes, run the Playwright design gallery in capture mode to create the initial baselines for the 4 new board-cfg-* boards:
    - Command: `cd test/example && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test --config priv/playwright/playwright.config.ts priv/playwright/tests/admin-design.spec.ts --project=admin-design-chromium --update-snapshots 2>&1 | tail -20`
    - Repeat for `admin-design-mobile` and `admin-design-dark` projects
    - Only run --update-snapshots for tests matching the board-cfg-* ids (or run full gallery update-snapshots if the server is available)

    CANARY STABILITY (D-11): After capture, verify:
    - `board-notice.png` (impersonation-banner canary) is byte-stable: `git diff --name-only test/example/priv/playwright/snapshots/ | grep -v board-cfg | grep board` should return only board-cfg-* files
    - Both allowlists (snapshot-allowlist-checkpoints.txt, snapshot-allowlist-design.txt) remain empty

    If Playwright server is not running (no local :4011 server), document the captures as "pending capture on next `scripts/uat/up.sh` run" and note the CONFIG_BOARDS registration is complete; the snapshots will be auto-created on first run (treat as net-new captures, not recaptures, per D-11).
  </action>
  <verify>
    <automated>
      grep -c "CONFIG_BOARDS" test/example/priv/playwright/tests/admin-design.spec.ts
      grep -c "board-cfg-overview" test/example/priv/playwright/tests/admin-design.spec.ts
      grep -c "isCfgBoard\|board-cfg" test/example/priv/playwright/tests/admin-design.spec.ts
    </automated>
  </verify>
  <done>
    `CONFIG_BOARDS` array with 4 board ids is defined in the spec; the ids are spread into the screenshot loop and responsive loop; the structural assertion test exists; `isCfgBoard` logic applies higher maxDiffPixels budget; snapshot files for the 4 boards exist (or are documented as pending first-run capture if server unavailable). Canaries byte-stable; allowlists empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design_gallery_live.ex → browser/Playwright | Static-assign gallery — no DB, no user data; trust boundary is the admin auth gate (platform-admin+ required to access /admin/_design) |
| admin-design.spec.ts → Playwright runner | Playwright captures element-scoped PNGs; no external network calls in snapshot comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-08 | Information Disclosure | design_gallery_live.ex — static literal assigns | low | accept | Gallery is dev-only (compile_env(:example, :dev_routes) gated); static assigns contain no real user data |
| T-205-09 | Denial of Service | Snapshot baseline drift from board-cfg-* additions | low | mitigate | D-11: net-new slugs are capture-only; canaries byte-stable; allowlists empty at close; isCfgBoard budget is wider to absorb rendering variance |
| T-205-10 | Tampering | card-in-card nesting in composites | low | mitigate | D-09 constraint: no sg-card inside sg-card; enforced by authoring discipline and existing no-card-in-card test that covers GROUP_BOARDS (composites follow same pattern) |
</threat_model>

<verification>
1. `grep -c 'board-cfg-overview\|board-cfg-users-list\|board-cfg-user-detail\|board-cfg-audit' test/example/lib/example_web/live/admin/design_gallery_live.ex` returns 4
2. `grep -c 'CONFIG_BOARDS' test/example/priv/playwright/tests/admin-design.spec.ts` ≥ 3 (definition + 2 loop spreads)
3. `cd test/example && mix compile --warnings-as-errors` exits 0
4. `grep -c 'isCfgBoard\|board-cfg' test/example/priv/playwright/tests/admin-design.spec.ts` ≥ 1 (higher budget logic present)
5. `grep -c 'Ecto\|Repo\|Query' test/example/lib/example_web/live/admin/design_gallery_live.ex` returns same count as before this plan (no new DB imports)
6. D-11 canary check: `git diff --name-only test/example/priv/playwright/ | grep 'board-notice\.png'` returns empty (impersonation-banner canary untouched); both allowlist files are empty or unchanged
</verification>

<success_criteria>
- 4 board-cfg-* composite boards authored in design_gallery_live.ex with static assigns, in-LiveView source-archetype comments, and correct sg-* class patterns
- CONFIG_BOARDS array registered in admin-design.spec.ts; integrated into screenshot + responsive loops; structural assertion test exists
- Higher maxDiffPixels budget applied to board-cfg-* boards (isCfgBoard detection)
- mix compile --warnings-as-errors passes in test/example/
- Snapshot baselines captured (or documented as pending first-run capture per D-11 net-new semantics)
- board-notice canary byte-stable; both allowlists empty
</success_criteria>

<output>
Create `.planning/phases/205-foundation/205-03-SUMMARY.md` when done
</output>
