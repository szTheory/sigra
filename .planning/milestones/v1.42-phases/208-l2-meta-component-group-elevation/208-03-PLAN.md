---
phase: "208"
plan: "03"
type: execute
wave: 3
depends_on:
  - "208-01"
  - "208-02"
files_modified:
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02

must_haves:
  truths:
    - "D-09: Exactly the 11 mg-* L2 rows in guides/reference/admin-quality-ledger.md (~:74-84) are flipped from 1 to bare 2: mg-1-metric-summary-strip, mg-2-filter-panel-applied-chips, mg-3-task-card-grid, mg-4-alarm-notice-band, mg-5-user-results-pagination, mg-6-audit-feed-pagination, mg-7-organization-member-roster, mg-8-pending-invitations, mg-9-identity-header-summary-facts, mg-10-detail-facts-membership-panels, mg-11-destructive-action-confirmation. No other rows are modified"
    - "D-09: Every flipped tier value is a bare integer 2 in column 4 — NEVER 2* or any decorated value (a decorated integer breaks the awk -F'|' /^[012]$/ monotonic parse and silently drops the row from the guard)"
    - "D-09: Each mg-* row's Evidence column carries the rich semicolon-delimited Tier-2 shape mirroring the freshly-flipped L1 rows (~:61-73): board-mg-N axe+screenshot ×3 projects (admin-design.spec.ts assertBoardScreenshot, GROUP_BOARDS loop); no-card-in-card (.sg-card .sg-card check ~:349-361); intra-group single-tier sg-stack rhythm; right-component-for-job (exact-count assertion); zero/loading/error state evidence with explicit N/A notes per D-08; content-equivalence for mg-5/mg-6 ONLY (N/A elsewhere per D-07); the board-cfg-* composite snapshot-clean ×3 projects citation per D-05; the MG-7/MG-8 isolated-board-only ruling per D-06"
    - "GROUP-02: The mg-* evidence cites the board-cfg-* composite proof (board-cfg-overview / board-cfg-users-list / board-cfg-user-detail / board-cfg-audit snapshot-clean ×3 projects from Plan 02) for the groups composited there, and records MG-7/MG-8 as isolated-board-only (no board-cfg-org, D-06)"
    - "After the flip, scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 and every L0/L1/L2 cell reads bare 2 (the entire building-block stack is Tier-2 — the L0 token cell + 13 L1 cells + 11 L2 cells)"
  artifacts:
    - guides/reference/admin-quality-ledger.md
  prohibitions:
    - statement: "D-09: The tier value is bare 2 — never 2* or any decorated integer; a decorator makes the row invisible to the awk -F'|' monotonic parse"
      status: enforced
      verification: "grep -E '^\\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$' returns empty output"
    - statement: "D-06: The mg-7 and mg-8 evidence strings contain the explicit isolated-board-only ruling so the absence of a board-cfg-org composite reads as intentional, not a coverage hole"
      status: enforced
      verification: "grep -E '^\\| mg-7-' guides/reference/admin-quality-ledger.md cites isolated-board-only / no board-cfg-org; grep -E '^\\| mg-8-' likewise"
    - statement: "D-08: The mg-3 evidence records the deliberate state-N/A note pattern (mg-3-zero-note / mg-3-loading-note); mg-9 and mg-11 evidence records real zero/loading/error states — no fabricated states, no mislabeling a real state as a note"
      status: enforced
      verification: "grep -E '^\\| mg-3-' guides/reference/admin-quality-ledger.md mentions the state-N/A note (zero-note/loading-note); grep -E '^\\| mg-9-' and '^\\| mg-11-' cite real zero/loading/error states"
    - statement: "D-01: No page-composition / page-pass work is recorded — only the 11 L2 group rows are flipped; no L3 page row is altered (those are Phase 209)"
      status: enforced
      verification: "git diff guides/reference/admin-quality-ledger.md shows only the 11 mg-* L2 rows changed; the L3 page rows (index-live, organization-live, users-index-live, user-show-live, user-sessions, audit-index-live, audit-user-live, branding-live) are byte-unchanged"
  key_links:
    - "The monotonic guard reads column 4 with awk -F'|' and /^[012]$/ — bare 2 only; a decorated value loses regression protection"
    - "Plan 01's SUMMARY per-group audit findings (single-tier rhythm, no card-in-card, right-component counts, true state-marker set, mg-5/mg-6 content-equivalence, mg-7/mg-8 isolated-only ruling) become the 11 mg-* evidence strings"
    - "Plan 02's 12 committed board-cfg-* baselines are the criterion-2 proof each composited mg-* evidence string cites (board-cfg-* snapshot-clean ×3 projects)"
---

<objective>
Flip exactly the 11 mg-* L2 rows in guides/reference/admin-quality-ledger.md (~:74-84) from
tier 1 to bare 2 — each with a rich semicolon-delimited evidence string mirroring the
freshly-flipped L1 rows (~:61-73). Cite, per group: the board-mg-N axe+screenshot ×3-project
gate, the no-card-in-card check, intra-group single-tier rhythm, right-component-for-job, the
true zero/loading/error state evidence (with explicit N/A notes for mg-3 per D-08),
content-equivalence for mg-5/mg-6 only (D-07), the board-cfg-* composite snapshot-clean proof
(D-05), and the MG-7/MG-8 isolated-board-only ruling (D-06). Verify the monotonic guard exits 0
vs origin/main and the entire L0/L1/L2 column now reads bare 2.

Purpose: This is the ledger-flip step (criterion 4) — the final assertion locking Tier-2 status
for all 11 L2 meta-component groups under the monotonic guard's forward-only protection. Every
evidence string cites the automated gates (admin-design.spec.ts axe+screenshot harness, the
catalog-state + card-in-card + content-equivalence tests) and the documented audit/baseline
proofs from Plans 01–02. With this flip, every L0/L1/L2 building block is Tier-2 — the
precondition for the Phase 209 page judgment pass.

Output: guides/reference/admin-quality-ledger.md with the 11 mg-* rows at bare tier 2; full
L0/L1/L2 column at Tier-2; monotonic guard green vs origin/main.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/208-l2-meta-component-group-elevation/208-CONTEXT.md
@.planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md
@.planning/phases/208-l2-meta-component-group-elevation/208-02-SUMMARY.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-04-PLAN.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
@scripts/ci/quality-ledger-monotonic.sh
</context>

## Artifacts this phase produces

- 11 flipped rows in `guides/reference/admin-quality-ledger.md` (mg-1…mg-11) — each bare tier `2` with a rich semicolon-delimited evidence string. This plan EDITS existing ledger rows; it creates no net-new symbol or file.

<tasks>

<task type="auto">
  <name>Task 1: Flip the 11 mg-* L2 ledger rows to bare tier 2 with rich evidence</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — read the full Quality Ledger; the 11 target rows are the mg-* L2 rows at ~:74-84 (currently tier 1 with sparse link-style evidence). The Tier-2 exemplar evidence shape = the freshly-flipped L1 rows at ~:61-73 (stat, stat_link, task_card, … audit_row). Also read the "Asserting Tier 2" / parsing-rules section explaining the bare-2 requirement and forbidden decorators.
  - .planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md "Per-Group Audit" — per-group findings: single-tier rhythm, no card-in-card (and the mg-3/mg-5 <section> + mg-6 sg-card-with-inner-results notes), right-component exact counts, the TRUE state-marker set per group (mg-3 = N/A notes; mg-9/mg-11 = real states), content-equivalence mg-5/mg-6 only, the MG-7/MG-8 isolated-board-only ruling, and the "CSS edited: yes/no" verdict
  - .planning/phases/208-l2-meta-component-group-elevation/208-02-SUMMARY.md — confirmation that the 12 board-cfg-* baselines are captured CI-native and snapshot-clean ×3 projects; the cfg-board mapping (board-cfg-overview / board-cfg-users-list / board-cfg-user-detail / board-cfg-audit) for the per-group cfg citation
  - test/example/priv/playwright/tests/admin-design.spec.ts — GROUP_BOARDS (~:104), CONFIG_BOARDS (~:118), GROUP_STATE_MARKERS (~:120-131), card-in-card check (~:349), MG-5/MG-6 equivalence (~:162-207, test ~:364-383), catalog-state + exact-count test (~:318-362) — for accurate citation anchors in the evidence strings
  - guides/reference/admin-fractal-scorecard.md "L2 Meta-Component Group Add-ons" (~:61-79) — the proxy vocabulary the evidence strings use
  </read_first>

  <action>
  Use Edit (scoped replacement) to update each of the 11 mg-* rows — one Edit call per row, using the row's existing sparse cell text as the unique old_string. Never Write the full ledger file.

  Each new row format: | {name} | L2 | 2 | {evidence-string} |
  The tier column MUST be the bare integer 2 — no asterisks, no decorators. The awk -F'|' parser reads column 4 and requires /^[012]$/ after trimming whitespace.

  L2 evidence template (semicolon-delimited; use N/A for inapplicable proxies), mirroring the L1 Wave rows:
  "admin-design.spec.ts assertBoardScreenshot board-mg-N — 3 projects (admin-design-chromium / -mobile / -dark) × toHaveScreenshot + assertNoAxeViolations (wcag2a/2aa/21a/21aa/22aa) — 0 violations; no card-in-card: .sg-card .sg-card check (admin-design.spec.ts ~:349-361) — 0 nested, no data-sg-card-nesting-audit-only escape hatch; intra-group rhythm: single-tier sg-stack--4; right-component: {exact-count assertion}; zero/loading/error: {state evidence per D-08}; content-equivalence: {mg-5/mg-6 only, else N/A per D-07}; real-page composite: {board-cfg-* snapshot-clean ×3 projects per D-05, or isolated-board-only per D-06}; byte-coherent reuse: {note}; overlay-axe: N/A; APG: N/A"

  Per-row specifics (cite the exact board-mg-N id and the audit findings from Plan 01's SUMMARY):

  - mg-1-metric-summary-strip: right-component: 7 .sg-metric (admin-design.spec.ts ~:331); states: mg-1-populated/zero/loading/error all rendered; content-equivalence: N/A; real-page composite: board-cfg-overview snapshot-clean ×3.
  - mg-2-filter-panel-applied-chips: right-component: 6 .sg-applied-chip (~:332); states: full set; byte-coherent reuse: applied-chips reused on board-cfg-users-list; content-equivalence: N/A; real-page composite: board-cfg-users-list snapshot-clean ×3.
  - mg-3-task-card-grid: right-component: 2 article.sg-card, board-mg-3 is a <section> (asserts not.toHaveClass(sg-card) ~:334); states: mg-3-zero-note / mg-3-loading-note — DELIBERATE state-N/A notes per D-08 (not fabricated states) + real mg-3-error; content-equivalence: N/A; real-page composite: N/A (task-card grid not on a cfg composite) or cite the applicable cfg board if Plan 01 found one.
  - mg-4-alarm-notice-band: right-component: 3 .sg-notice (~:335) incl notice tone={:risk} error; states: full set incl real alarm zero-state; content-equivalence: N/A; real-page composite: board-cfg-overview snapshot-clean ×3.
  - mg-5-user-results-pagination: right-component: results table + mobile cards (mg-5-desktop-results/mg-5-mobile-results ~:336-337); states: full set; content-equivalence: assertUserResultEquivalence green (~:364-371) — table+mobile-card swap proven (D-07); real-page composite: board-cfg-users-list snapshot-clean ×3.
  - mg-6-audit-feed-pagination: right-component: 3 article.sg-list-row (~:340), mg-6-desktop-results/mg-6-mobile-results; states: full set; content-equivalence: assertAuditResultEquivalence green (~:373-377) + Previous/Next/Export CSV controls (~:378-383) — D-07; byte-coherent reuse: audit feed reused on board-cfg-audit; real-page composite: board-cfg-audit snapshot-clean ×3.
  - mg-7-organization-member-roster: right-component: 3 .sg-list-row (~:341), zero uses <.empty_state>; states: mg-7-populated/zero/loading/error; content-equivalence: N/A; real-page composite: ISOLATED-BOARD-ONLY — there is no board-cfg-org composite and per D-06 (user-ratified) one is intentionally NOT authored; isolated board-mg-7 coverage is sufficient.
  - mg-8-pending-invitations: right-component: 3 .sg-list-row (~:342), zero uses <.empty_state>; states: mg-8-populated/zero/loading/error; content-equivalence: N/A; real-page composite: ISOLATED-BOARD-ONLY — no board-cfg-org composite, intentionally not authored per D-06; isolated board-mg-8 coverage is sufficient.
  - mg-9-identity-header-summary-facts: right-component: 1 .sg-summary-facts (~:343); states: REAL mg-9-zero / mg-9-loading / mg-9-error (not notes — per D-08); content-equivalence: N/A; real-page composite: board-cfg-user-detail snapshot-clean ×3.
  - mg-10-detail-facts-membership-panels: right-component: 2 .sg-detail-grid (~:344); states: full set; content-equivalence: N/A; real-page composite: board-cfg-user-detail snapshot-clean ×3.
  - mg-11-destructive-action-confirmation: right-component: 2 .sg-confirm-overlay .sg-confirm-dialog (~:345); states: REAL mg-11-zero / mg-11-loading / mg-11-error (not notes — per D-08); byte-coherent reuse: confirmation reused on board-cfg-user-detail; content-equivalence: N/A; real-page composite: board-cfg-user-detail snapshot-clean ×3.

  Pull the exact counts, state-marker shapes, and the "CSS edited: yes/no" context from the Plan 01/02 SUMMARYs. Preserve table column spacing. Do NOT decorate the tier value. Do NOT place any literal a conformance guard negative-greps for inside an evidence string.
  </action>

  <verify>
    <automated>bash -c 'grep -E "^\| mg-[0-9]+-" guides/reference/admin-quality-ledger.md | awk -F"|" "{t=\$4; gsub(/^ +| +\$/,\"\",t); print t}" | grep -xc 2'</automated>
  </verify>

  <acceptance_criteria>
  - All 11 mg-* rows show bare 2 in column 4: the verify command returns 11
  - No decorated tier values anywhere in the ledger: grep -E '^\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$' returns empty output
  - Each of the 11 mg-* rows cites the axe/screenshot harness: grep -E '^\| mg-[0-9]+-' guides/reference/admin-quality-ledger.md | grep -c 'assertBoardScreenshot' returns 11
  - mg-7 and mg-8 evidence record the isolated-board-only ruling: grep -E '^\| mg-(7|8)-' guides/reference/admin-quality-ledger.md | grep -ci 'isolated-board-only\|no board-cfg-org' returns 2 (D-06)
  - mg-3 evidence cites the deliberate state-N/A note: grep -E '^\| mg-3-' guides/reference/admin-quality-ledger.md | grep -c 'note' returns >= 1 (D-08); mg-5 and mg-6 evidence cite content-equivalence: grep -E '^\| mg-(5|6)-' guides/reference/admin-quality-ledger.md | grep -c 'content-equivalence' returns 2 with non-N/A values (D-07)
  - Non-target rows untouched: the L0 token-layer cell + 13 L1 cells stay 2; the L3 page rows + L4 flow rows are byte-unchanged (only the 11 mg-* L2 rows changed in git diff)
  </acceptance_criteria>

  <done>All 11 mg-* L2 rows show bare 2 with rich evidence (right-component counts, true state markers, content-equivalence mg-5/mg-6, cfg-composite citation, mg-7/mg-8 isolated-only ruling); no decorators; non-target rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run monotonic guard to confirm no regressions and full L0/L1/L2 Tier-2</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — re-read after edits to confirm all 11 mg-* rows are bare 2 with rich evidence and no row was accidentally decorated before running the guard
  - scripts/ci/quality-ledger-monotonic.sh — confirm --base origin/main usage and that it parses column 4 with awk -F'|' and /^[012]$/; the PASS line reports a cell count
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-04-SUMMARY.md — the prior cell count baseline so this plan's count (now including 11 mg-* at 2) is >= it
  </read_first>

  <action>
  Run the monotonic guard against origin/main to confirm:
  1. No tier values decreased vs main (the 11 mg-* rows moved 1 → 2, an allowed increase).
  2. The entire L0/L1/L2 column now reads bare 2 — the L0 token-layer cell, all 13 L1 component cells, and all 11 L2 group cells.

  Command: bash scripts/ci/quality-ledger-monotonic.sh --base origin/main
  Expected: "quality-ledger-monotonic: PASS (N cells checked vs origin/main)".

  If the guard exits non-zero or the cell count is lower than expected, diagnose: a decorated tier value (2*, 2 ✓) makes a row invisible to the awk parse — find and replace with bare 2; or a row was accidentally decreased. Fix and re-run.

  Also run the guard self-test to confirm the guard itself is still green:
  bash scripts/ci/quality-ledger-monotonic.test.sh

  Confirm the full L0/L1/L2 Tier-2 claim: grep the ledger for the L0 + 13 L1 + 11 L2 rows and assert every tier cell is 2.
  </action>

  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && bash scripts/ci/quality-ledger-monotonic.test.sh</automated>
  </verify>

  <acceptance_criteria>
  - bash scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 and emits a PASS line
  - bash scripts/ci/quality-ledger-monotonic.test.sh exits 0 (guard self-test still green)
  - Every L0 + L1 + L2 row reads bare 2: grep -E '^\| [a-z].* \| L[012] \|' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -vxc 2 returns 0 (no L0/L1/L2 row at a tier other than 2)
  - git diff --stat shows only guides/reference/admin-quality-ledger.md modified in this plan
  </acceptance_criteria>

  <done>Monotonic guard exits 0 vs origin/main; the L0 cell + 13 L1 cells + 11 L2 cells all read bare 2; guard self-test green; no other files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger edit → awk parse | A malformed tier column (e.g. "2*") silently removes the row from monotonic-guard regression protection |

This phase edits only a docs ledger (and, in sibling plans, example-only gallery markup, sg-*
CSS, and Playwright baselines). There is NO auth / session / token / PII surface in scope and NO
new attack surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208-06 | Tampering | Ledger tier column format | high | mitigate | Acceptance criteria verify no decorated values exist (grep -vE '^[012]$' returns empty) and the guard cell count is >= the 207 baseline; any invisible row is caught |
| T-208-07 | Repudiation | Evidence string accuracy | low | accept | Evidence strings are human-curated, bounded by Plan 01 audit findings + Plan 02 baseline proofs; the cited gates are independently runnable |
</threat_model>

<verification>
After both tasks complete:
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main   (exits 0)
  bash scripts/ci/quality-ledger-monotonic.test.sh                 (exits 0)
  grep -E '^\| mg-[0-9]+- ' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -xc 2   (returns 11)
  grep -E '^\| mg-(7|8)-' guides/reference/admin-quality-ledger.md | grep -ci 'isolated-board-only\|no board-cfg-org'   (returns 2)
</verification>

<success_criteria>
- 11 mg-* L2 rows at bare tier 2 with rich evidence (right-component counts, true state markers, content-equivalence mg-5/mg-6, cfg-composite citation, mg-7/mg-8 isolated-only ruling)
- No decorated tier values anywhere in the ledger
- Monotonic guard exits 0 vs origin/main; cell count >= the 207 baseline
- Entire L0/L1/L2 column at Tier-2 (L0 cell + 13 L1 cells + 11 L2 cells all == 2)
- Guard self-test green
</success_criteria>

<output>
Create .planning/phases/208-l2-meta-component-group-elevation/208-03-SUMMARY.md when done
</output>
