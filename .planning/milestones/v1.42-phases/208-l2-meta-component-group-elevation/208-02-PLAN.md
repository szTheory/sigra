---
phase: "208"
plan: "02"
type: execute
wave: 2
depends_on:
  - "208-01"
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-dark.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-dark.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-dark.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-dark.png
autonomous: true
requirements:
  - GROUP-02

must_haves:
  truths:
    - "D-05: The 4 board-cfg-* composites (board-cfg-overview, board-cfg-users-list, board-cfg-user-detail, board-cfg-audit) have their 12 PNG baselines (4 boards × admin-design-chromium / -mobile / -dark) captured CI-NATIVE via the admin_design_recapture ubuntu job (ci.yml ~:1386) — NEVER on darwin (darwin captures are reverted at 43f5a3e4). At close, 12 net-new board-cfg-* PNGs exist under test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ and the hard-gating design_gallery PR job (ci.yml ~:1044) passes snapshot-clean ×3 projects for the cfg boards"
    - "GROUP-02: With the 12 cfg baselines committed, the admin-design suite's MG-5/MG-6 desktop↔mobile content-equivalence assertions (admin-design.spec.ts ~:364-383) and the config-board archetype-section test (~:308-316) run green ×3 projects"
    - "D-05: This plan folds the pending todo 2026-06-28-phase205-debt-ci-native-board-baselines.md item #1 (missing board-cfg-* baselines); the todo is moved to resolved at phase close"
    - "D-10: IF Plan 01 landed a sg-* CSS edit affecting board-mg-* pixels, the genuinely-affected board-mg-* baselines are recaptured in the SAME CI-native run (admin_design_recapture re-runs all boards with --update-snapshots ~:1499-1514); IF Plan 01 landed NO edit, the 33 existing board-mg-* PNGs stay byte-stable and only the 12 net-new cfg PNGs (+ any deliberate board-notice canary re-birth the job already performs) are added"
    - "D-10: The impersonation-banner and board-notice canaries stay byte-stable/untouched (board-notice is re-established as 'added' by the job's existing OQ1 deliberate re-baseline step, never allowlisted); both allowlists (snapshot-allowlist, snapshot-allowlist-design) contain zero non-comment/non-blank lines at phase close"
  artifacts:
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-chromium.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-mobile.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-dark.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-chromium.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-mobile.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-dark.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-chromium.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-mobile.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-dark.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-chromium.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-mobile.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-dark.png
  prohibitions:
    - statement: "D-05: PNG baselines MUST be captured CI-native via the admin_design_recapture ubuntu job, NEVER on darwin — darwin captures produce OS-font-metric drift and are reverted (proven at 43f5a3e4). Do not run --update-snapshots on a darwin/macOS dev machine and commit the result"
      status: enforced
      verification: "The 12 cfg PNGs land on the ci/recapture-admin-design-<run_id> branch the admin_design_recapture job opens (ci.yml ~:1384-1385), reviewed and merged via PR; the SUMMARY records the CI run id / PR, not a local darwin capture"
    - statement: "D-10: Both allowlists (snapshot-allowlist, snapshot-allowlist-design) are empty (zero non-comment/non-blank lines) at phase close; no slug is committed into either allowlist"
      status: enforced
      verification: "grep -vcE '^#|^$' test/example/priv/playwright/snapshot-allowlist returns 0 AND grep -vcE '^#|^$' test/example/priv/playwright/snapshot-allowlist-design returns 0"
    - statement: "D-10: The impersonation-banner canary (checkpoint lane) and board-notice canary (design lane) stay byte-stable — board-notice is re-born as 'added' by the job's OQ1 step, never modified-in-place and never allowlisted"
      status: enforced
      verification: "snapshot-canary-guard output in the admin_design_recapture run shows board-notice as 'added' (legitimate birth path) and impersonation-banner unchanged; no canary 'modify' is reported"
    - statement: "D-06: No board-cfg-org composite is captured — CONFIG_BOARDS lists exactly 4 cfg ids; only 12 cfg PNGs (4×3) are added"
      status: enforced
      verification: "ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | grep -c 'board-cfg-org' returns 0; the count of net-new board-cfg-* PNGs is exactly 12"
  key_links:
    - "Plan 01's 'CSS edited: yes/no' verdict + board-mg-* slug list determines whether the CI-native run also produces board-mg-* PNG deltas (edit) or leaves the 33 mg baselines byte-stable (no edit)"
    - "The 12 committed cfg baselines are the load-bearing criterion-2 proof that Plan 03's mg-* ledger evidence cites (board-cfg-* snapshot-clean ×3 projects)"
    - "The admin_design_recapture job is non-PR-gated (if: github.event_name != 'pull_request') — it runs on push to a branch and opens a PR; the SUMMARY records the run id and the review PR per OQ2"
---

<objective>
Capture the 12 net-new board-cfg-* PNG baselines (board-cfg-overview, board-cfg-users-list,
board-cfg-user-detail, board-cfg-audit × admin-design-chromium / -mobile / -dark) CI-NATIVE via
the admin_design_recapture ubuntu job — folding the Phase-205 baseline-debt todo. This is
criterion-2's load-bearing proof: with the cfg baselines committed, the hard-gating
design_gallery PR job passes snapshot-clean ×3 projects for every group's real-page composite,
and the MG-5/MG-6 content-equivalence assertions run green. Keep both canaries byte-stable and
both allowlists empty.

Purpose: There are 0 committed board-cfg-* baselines today (vs 33 board-mg-*). Without them the
design_gallery PR job fails on missing snapshots, so criterion 2 ("every group's board-cfg-*
composite passes snapshot-clean ×3 projects") cannot be proven. Capture MUST be CI-native
(ubuntu) — darwin captures carry OS-font-metric drift and are reverted (43f5a3e4). The
admin_design_recapture job runs --update-snapshots over all boards, which creates the 12 cfg
PNGs as net-new "added" files (and, if Plan 01 edited sg-* CSS, recaptures the affected mg PNGs
in the same pass).

Output: 12 net-new board-cfg-* PNGs committed (via the job's PR branch + review per OQ2); both
canaries byte-stable; both allowlists empty; the Phase-205 baseline-debt todo resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/208-l2-meta-component-group-elevation/208-CONTEXT.md
@.planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md
@.planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md
@.github/workflows/ci.yml
@scripts/ci/snapshot-recapture-gate.sh
@scripts/ci/snapshot-canary-guard.sh
@test/example/priv/playwright/playwright.config.ts
@test/example/priv/playwright/tests/admin-design.spec.ts
</context>

## Artifacts this phase produces

- 12 NET-NEW PNG baseline files (the only net-new artifacts of this phase):
  - `board-cfg-overview-admin-design-{chromium,mobile,dark}.png`
  - `board-cfg-users-list-admin-design-{chromium,mobile,dark}.png`
  - `board-cfg-user-detail-admin-design-{chromium,mobile,dark}.png`
  - `board-cfg-audit-admin-design-{chromium,mobile,dark}.png`
  all under `test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/`
- (conditional) recaptured `board-mg-*` PNGs — ONLY if Plan 01 landed a sg-* CSS edit (expected: none → 33 mg baselines unchanged)
- the Phase-205 baseline-debt todo moved to `.planning/todos/resolved/`

<tasks>

<task type="auto">
  <name>Task 1: Trigger CI-native cfg baseline capture via admin_design_recapture and land the 12 cfg PNGs</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-dark.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-dark.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-dark.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-dark.png</files>

  <read_first>
  - .planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md — the "CSS edited: yes/no" verdict + (if yes) the affected board-mg-* slug list; this determines whether the CI-native run also produces mg PNG deltas
  - .github/workflows/ci.yml — the admin_design_recapture job (~:1386-1530): non-PR-gated (if: github.event_name != 'pull_request' ~:1389), boots example on ubuntu with postgres service + seeds, runs `npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --update-snapshots` (~:1499-1514) over ALL boards (COMPONENT + GROUP + CONFIG), the OQ1 board-notice canary re-birth step (~:1486-1498), and the snapshot-canary-guard + commit-to-PR-branch step (~:1515+, OQ1/OQ2)
  - .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md — the folded debt: item #1 = missing board-cfg-* baselines (this plan's scope); items #2 (local main behind origin/main) and #3 (~12 Phase-205 behavioral failures) handled as needed during execution
  - test/example/priv/playwright/tests/admin-design.spec.ts — CONFIG_BOARDS (~:118, exactly 4 cfg ids), the board screenshot loop that includes CONFIG_BOARDS (~:257), assertBoardScreenshot, the config-board archetype-section test (~:308-316), MG-5/MG-6 equivalence test (~:364-383)
  - scripts/ci/snapshot-canary-guard.sh — the canaries: board-notice (design lane) re-born as 'added', impersonation-banner (checkpoint lane) byte-stable
  - test/example/priv/playwright/snapshot-allowlist and snapshot-allowlist-design — confirm both are empty (zero non-comment/non-blank lines); never commit a slug into either
  </read_first>

  <action>
  The board-cfg-* baselines MUST be captured CI-native (D-05) — NEVER recaptured/committed from a
  darwin dev machine (reverted at 43f5a3e4). The admin_design_recapture ubuntu job already runs
  `--update-snapshots` over ALL admin-design boards, so its run produces the 12 board-cfg-* PNGs
  as net-new "added" files (the cfg boards have 0 prior baselines).

  1. Ensure local main is reconciled with origin/main first (folded debt item #2): `git fetch origin main` and rebase/merge as needed so the recapture compares against current ubuntu baselines. Record the reconcile in the SUMMARY.

  2. Push the working branch (carrying Plan 01's changes, if any) so the admin_design_recapture job runs (it is gated `if: github.event_name != 'pull_request'`, so it runs on a non-PR push event). The job:
     - boots the example on ubuntu with the postgres service + demo seeds,
     - runs the 3 admin-design projects with `--update-snapshots` over all boards,
     - re-establishes board-notice as 'added' (OQ1), gates via snapshot-canary-guard, and commits the recaptured PNGs to a `ci/recapture-admin-design-<run_id>` branch, opening a PR for review (OQ2).

  3. From the recapture PR, confirm the net-new files are EXACTLY the 12 board-cfg-* PNGs (4 boards × 3 projects) — plus the deliberately re-born board-notice canary (added, not modified) — plus any board-mg-* deltas ONLY if Plan 01's verdict was "CSS edited: yes" for those slugs. If Plan 01 made no CSS edit, the 33 board-mg-* PNGs MUST be unchanged; restore any incidentally-changed mg PNG that was not an intended slug.

  4. Merge the 12 cfg PNGs (and any intended mg deltas) into the phase working branch via the review PR. Do NOT commit any slug into either allowlist.

  5. After the baselines land, confirm the design_gallery suite passes snapshot-clean for the cfg boards and that the MG-5/MG-6 content-equivalence + config-board archetype-section tests are green (these run as part of the admin-design suite the design_gallery PR job ~:1044 gates).

  6. Move .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md to .planning/todos/resolved/ (item #1 closed by this capture); note in the SUMMARY whether any item-#3 behavioral failure proved non-208 infra and was re-deferred to Phase 211.
  </action>

  <verify>
    <automated>bash -c 'cd /Users/jon/projects/sigra; n=$(ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | grep -E "^board-cfg-(overview|users-list|user-detail|audit)-admin-design-(chromium|mobile|dark)\.png$" | wc -l | tr -d " "); org=$(ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | grep -c "board-cfg-org"); [ "$n" = "12" ] && [ "$org" = "0" ] && echo "12 cfg PNGs present, no cfg-org"'</automated>
  </verify>

  <acceptance_criteria>
  - Exactly 12 board-cfg-* PNGs exist under the snapshot dir (4 boards × 3 projects): the verify command prints "12 cfg PNGs present, no cfg-org"
  - No board-cfg-org PNG exists: ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | grep -c 'board-cfg-org' returns 0 (D-06)
  - The 12 cfg PNGs were captured CI-native via admin_design_recapture (ubuntu) — the SUMMARY records the CI run id / review PR, NOT a darwin capture (D-05)
  - IF Plan 01 made no CSS edit: the 33 board-mg-* PNGs are byte-stable (git shows no board-mg-* PNG modified); IF Plan 01 edited CSS: only the declared board-mg-* slugs changed (D-10)
  - The Phase-205 baseline-debt todo is moved to .planning/todos/resolved/
  </acceptance_criteria>

  <done>12 board-cfg-* PNGs captured CI-native and committed; no board-cfg-org; mg baselines byte-stable (or only declared slugs changed); Phase-205 baseline-debt todo resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify canary byte-stability, empty allowlists, and monotonic guard green</name>
  <files>test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/snapshot-allowlist-design</files>

  <read_first>
  - scripts/ci/snapshot-canary-guard.sh — confirm board-notice (design lane, re-born 'added') and impersonation-banner (checkpoint lane, unchanged) are the canaries; a canary 'modify' aborts the gate
  - test/example/priv/playwright/snapshot-allowlist and snapshot-allowlist-design — both must have zero non-comment/non-blank lines at phase close
  - scripts/ci/quality-ledger-monotonic.sh — confirms no tier regression; Plan 03 has not flipped rows yet so all 11 mg-* rows are still 1 → the guard exits 0
  - CLAUDE.md "admin-design baselines are CI-native" + the Docker test-PG prereqs if a local compare-mode confirmation is run (compare-mode only — never --update-snapshots on darwin)
  </read_first>

  <action>
  Confirm snapshot discipline (D-10) after the cfg baselines land:

  1. board-notice canary appears as 'added' (legitimate re-birth via the job's OQ1 step), NOT modified-in-place; impersonation-banner canary is byte-unchanged. Confirm from the recapture PR's snapshot-canary-guard output.

  2. Both allowlists (snapshot-allowlist, snapshot-allowlist-design) have zero non-comment/non-blank lines — no cfg or mg slug was committed into either.

  3. Run scripts/ci/quality-ledger-monotonic.sh --base origin/main → exits 0 (mg-* rows still at 1; Plan 03 flips them, so no regression here).

  4. (Optional local confirmation, compare-mode ONLY — never --update-snapshots on darwin) If desired, boot the test Postgres + example app per CLAUDE.md and run the admin-design suite in compare-mode to confirm the new cfg baselines are self-consistent. Skip if the CI recapture PR already proves it green; CI is the source of truth for these baselines.
  </action>

  <verify>
    <automated>bash -c 'set -e; cd /Users/jon/projects/sigra; bash scripts/ci/quality-ledger-monotonic.sh --base origin/main; al=$(grep -vcE "^#|^$" test/example/priv/playwright/snapshot-allowlist); ald=$(grep -vcE "^#|^$" test/example/priv/playwright/snapshot-allowlist-design); [ "$al" = "0" ] && [ "$ald" = "0" ] && echo "allowlists empty + monotonic green"'</automated>
  </verify>

  <acceptance_criteria>
  - test/example/priv/playwright/snapshot-allowlist has zero non-comment/non-blank lines: grep -vcE '^#|^$' returns 0
  - test/example/priv/playwright/snapshot-allowlist-design has zero non-comment/non-blank lines: grep -vcE '^#|^$' returns 0
  - board-notice canary is 'added' (not modified); impersonation-banner canary byte-stable (from the recapture PR's canary-guard output)
  - bash scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 (no tier regression; mg-* rows still 1)
  </acceptance_criteria>

  <done>Both canaries byte-stable (board-notice re-born 'added', impersonation-banner unchanged); both allowlists empty; monotonic guard green vs origin/main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright recapture → PNG commit | Recaptured/net-new PNGs become the new ground truth; wrong snapshots (e.g. darwin-rendered) become a false baseline — mitigated by CI-native-only capture |
| Canary PNGs → snapshot-canary-guard | A board-notice 'modify' or an impersonation-banner change would silently disarm the drift tripwire |

This phase adds only example-only Playwright PNG baselines for a grading surface. There is NO
auth / session / token / PII surface in scope and NO new attack surface is introduced; the only
host-shipped change in the milestone is presentational sg-* CSS (Plan 01).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208-03 | Tampering | Darwin-rendered baseline | high | mitigate | Capture is CI-native via admin_design_recapture only; darwin captures are reverted (43f5a3e4); SUMMARY records the CI run id, not a local capture (D-05) |
| T-208-04 | Tampering | Canary byte-stability | high | mitigate | board-notice + impersonation-banner checked by snapshot-canary-guard; board-notice re-born as 'added' (OQ1), never allowlisted; any canary modify aborts the gate (D-10) |
| T-208-05 | Tampering | Recapture scope creep onto mg baselines | medium | mitigate | If Plan 01 made no CSS edit, the 33 mg PNGs must be unchanged; incidental deltas restored; only the 12 cfg PNGs (+ any declared mg slugs) land; allowlists cleared (D-10) |
</threat_model>

<verification>
After both tasks complete:
  ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | grep -Ec '^board-cfg-(overview|users-list|user-detail|audit)-admin-design-(chromium|mobile|dark)\.png$'   (returns 12)
  ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | grep -c 'board-cfg-org'   (returns 0)
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main   (exits 0)
  grep -vcE '^#|^$' test/example/priv/playwright/snapshot-allowlist          (returns 0)
  grep -vcE '^#|^$' test/example/priv/playwright/snapshot-allowlist-design   (returns 0)
</verification>

<success_criteria>
- 12 board-cfg-* PNGs captured CI-native via admin_design_recapture (ubuntu); SUMMARY records the run id / review PR
- No board-cfg-org PNG (D-06); mg baselines byte-stable unless Plan 01 declared a slug edit
- Both canaries (board-notice 'added', impersonation-banner unchanged) byte-stable; both allowlists empty
- Monotonic guard exits 0 vs origin/main (mg-* rows still 1; Plan 03 flips them)
- Phase-205 baseline-debt todo resolved
</success_criteria>

<output>
Create .planning/phases/208-l2-meta-component-group-elevation/208-02-SUMMARY.md when done
</output>
