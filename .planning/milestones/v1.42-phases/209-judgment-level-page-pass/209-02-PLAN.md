---
phase: 209-judgment-level-page-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/uat-evidence/v1.42-persona-jtbd/index-live.md
  - .planning/uat-evidence/v1.42-persona-jtbd/organization-live.md
  - .planning/uat-evidence/v1.42-persona-jtbd/users-index-live.md
  - .planning/uat-evidence/v1.42-persona-jtbd/user-show-live.md
  - .planning/uat-evidence/v1.42-persona-jtbd/user-sessions.md
  - .planning/uat-evidence/v1.42-persona-jtbd/audit-index-live.md
  - .planning/uat-evidence/v1.42-persona-jtbd/audit-user-live.md
  - .planning/uat-evidence/v1.42-persona-jtbd/branding-live.md
  - .planning/v1.42-PERSONA-JTBD-PANEL.md
  - scripts/ci/panel-schema-check.sh
autonomous: true
requirements: [PAGE-01]
must_haves:
  truths:
    - "One schema-valid scored review doc exists per surface for all 8 admin pages, each with 3-persona verdicts + a per-surface disposition"
    - "Every (lens x question) cell in every doc holds a DOM-anchored finding OR the literal NONE — searched for token (forced-finding floor)"
    - "A roll-up index links all 8 docs with a raw->resolved disposition mapping defined"
    - "No table in any doc places a bare 0/1/2 in the 4th pipe column (monotonic-guard false-match prohibition)"
  artifacts:
    - ".planning/uat-evidence/v1.42-persona-jtbd/<surface>.md x 8"
    - ".planning/v1.42-PERSONA-JTBD-PANEL.md"
    - "scripts/ci/panel-schema-check.sh (a small validator used by verify)"
  key_links:
    - "each doc frontmatter surface == ledger row key exactly (index-live, organization-live, users-index-live, user-show-live, user-sessions, audit-index-live, audit-user-live, branding-live)"
    - "roll-up disposition column holds clean/actionable/blocked — never 0/1/2"
---

<objective>
Instantiate the adversarial persona/JTBD rubric (`guides/reference/admin-persona-jtbd-rubric.md`) FRESH against current source for all 8 admin pages (D-01), producing one schema-valid scored review doc per surface plus the roll-up index (D-02). This is the judgment that produces the `actionable` verdict set the Wave-2 remediation plans resolve.

Purpose: Satisfy PAGE-01 / SC-1. The panel must capture the PRE-FIX live DOM as adversarial evidence — authored BEFORE remediation so the docs record the real defect state (RESEARCH Sequencing). `.planning/v1.42-IA-DIAGNOSTIC.md` is advisory input ONLY, not a pre-scored panel to transcribe (D-01).
Output: 8 per-surface docs + 1 roll-up + a tiny schema-check helper. No source code is edited in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-judgment-level-page-pass/209-CONTEXT.md
@.planning/phases/209-judgment-level-page-pass/209-RESEARCH.md
@.planning/phases/209-judgment-level-page-pass/209-UI-SPEC.md
@guides/reference/admin-persona-jtbd-rubric.md
@.planning/v1.42-IA-DIAGNOSTIC.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-design-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the panel schema-check helper + author the 4 list/overview surface docs</name>
  <files>scripts/ci/panel-schema-check.sh, .planning/uat-evidence/v1.42-persona-jtbd/index-live.md, .planning/uat-evidence/v1.42-persona-jtbd/organization-live.md, .planning/uat-evidence/v1.42-persona-jtbd/users-index-live.md, .planning/uat-evidence/v1.42-persona-jtbd/audit-index-live.md</files>
  <read_first>
    - `guides/reference/admin-persona-jtbd-rubric.md` — the 3 lenses (platform-admin `/admin`; support-investigator `/admin/users/:id`; org-admin `/admin/organizations/:slug`), the 3 verdict questions (earning-its-place / ia-muddy / redundant-coherent-surprising), the forced-finding floor rule, the exact YAML frontmatter schema (all 9 `verdicts` keys + `findings[]`), the markdown body structure (one `## <Lens>` section, 3 `### <question>` subsections each), the surface-disposition rollup. Follow it verbatim.
    - RESEARCH Verified Anchor Table (26 anchors) + NEW-1 `organization_live.ex:69` bare "All clear"; NEW-2 `scope_copy/1` divergent per page.
    - The live LiveViews for THIS task: `lib/sigra/admin/live/index_live.ex`, `organization_live.ex`, `users_index_live.ex`, `audit_index_live.ex` — read them to anchor findings against current DOM.
    - RESEARCH "Terminology reconciliation": the rubric's mechanical `kill->blocked` is the RAW disposition; the roll-up reflects the RESOLVED state.
    - `guides/reference/admin-quality-ledger.md:85-92` — ledger row keys used as surface filenames.
  </read_first>
  <action>
    Write `scripts/ci/panel-schema-check.sh` — a small bash+python validator that, given a surface doc path, asserts: frontmatter parses; `surface` equals the filename stem; `rubric_version` is the string 1.0; `disposition` is one of clean/actionable/blocked; `verdicts` has exactly the 3 lens keys each with the 3 question keys; `findings` is a list (each item having element/lens/question/refutation/disposition_action when non-empty); and the markdown body contains all 3 lens headings and, per lens, the 3 question subheadings. It must also fail if any table row in the doc has a bare 0/1/2 as its 4th pipe-delimited field (the monotonic-guard false-match rule). Exit non-zero with a clear message on any violation.

    Define the raw->resolved disposition mapping and record it in EACH doc body under a "Disposition mapping" note: the rubric raw rollup is kill->blocked / tighten(no kill)->actionable / all keep->clean; the RESOLVED per-surface disposition used in SC-1 is: any non-`keep` verdict that CAN be remediated in-place this phase -> `actionable` (it gets a Wave-2 diff or documented waiver); all-`keep` -> `clean`; `blocked` reserved ONLY for a verdict genuinely un-fixable this phase (external dep) — none expected here.

    Author 4 docs at `.planning/uat-evidence/v1.42-persona-jtbd/{index-live,organization-live,users-index-live,audit-index-live}.md`, each with full frontmatter (surface = exact ledger key, ledger_cell same, rubric_version 1.0, disposition, all 9 verdicts keys, findings[]) and a markdown body of one `## <Lens>` section per lens (rubric order), each with 3 `### <question>` subsections, each ending in a cited DOM-anchored finding (file:line) OR the literal `NONE — searched for: <what>`. Every (lens x question) cell must be present — partial compliance invalidates the review. Expected floor findings (verify against live source, don't blindly transcribe): index-live bare "All clear" (:52) + total-users dup; organization-live bare `<p>` empty-states (:95/:117) + members dead-end (:96) + bare "All clear" (:69 NEW-1); users-index-live chips-inside-form is CANONICAL (keep) + phx-click disclosure acceptable (keep); audit-index-live scope_ribbon BELOW header (:56) outlier + chips OUTSIDE form + "Effective user" present.

    Never place a bare 0/1/2 in the 4th pipe column of any table in these docs (D-02); use keep/tighten/kill/clean/actionable/blocked/lens-names.
  </action>
  <verify>
    <automated>for f in index-live organization-live users-index-live audit-index-live; do bash scripts/ci/panel-schema-check.sh ".planning/uat-evidence/v1.42-persona-jtbd/$f.md"; done && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main</automated>
  </verify>
  <acceptance_criteria>
    - `panel-schema-check.sh` exists and passes for all 4 docs (schema-valid, all cells present, no column-4 integer).
    - `quality-ledger-monotonic.sh --base origin/main` exits 0 (proves no doc table false-matches the guard).
    - Each doc's `surface` equals its filename stem and matches a ledger row key.
  </acceptance_criteria>
  <done>The schema-check helper exists and the 4 list/overview surface docs are schema-valid, forced-finding-floor-complete, and column-4-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 4 leaf/detail surface docs (user-show, user-sessions, audit-user, branding)</name>
  <files>.planning/uat-evidence/v1.42-persona-jtbd/user-show-live.md, .planning/uat-evidence/v1.42-persona-jtbd/user-sessions.md, .planning/uat-evidence/v1.42-persona-jtbd/audit-user-live.md, .planning/uat-evidence/v1.42-persona-jtbd/branding-live.md</files>
  <read_first>
    - `guides/reference/admin-persona-jtbd-rubric.md` — same schema/floor rules as Task 1.
    - The live LiveViews for THIS task: `lib/sigra/admin/live/user_show_live.ex`, `user_sessions_live.ex`, `audit_user_live.ex`, `branding_live.ex` — read them to anchor findings.
    - RESEARCH Verified Anchor Table rows 8-14, 21-24, 26 + OQ-1 (org invite dead-end -> likely waiver; but that's organization-live, covered in Task 1) + OQ-2 (branding has NO `scope_copy/1`; the fix adds a context-appropriate helper, NEW-2).
    - RESEARCH row 23: `audit_user_live.ex` "Effective user" ABSENT is DEFENSIBLE -> waiver (D-05/D-07), score as a documented intentional asymmetry, not a `kill`.
    - `guides/reference/admin-quality-ledger.md:85-92` — the row key `user-sessions` (not `user-sessions-live`) and `branding-live` (no checkpoint slug).
  </read_first>
  <action>
    Author 4 docs at `.planning/uat-evidence/v1.42-persona-jtbd/{user-show-live,user-sessions,audit-user-live,branding-live}.md`, same frontmatter + body structure and forced-finding floor as Task 1, with the same "Disposition mapping" note. Expected floor findings (verify against live source): user-show-live — sessions-count duplicated header dl (:57) + Sessions-panel sub-heading (:84); "Manage sessions" easily-missed secondary button (:86); 4 divergently-worded empty_state copies (:115/:146/:182/:202); terse kicker "User" (:47). user-sessions — `<h1>Sessions` heading divergence from sibling entity-name H1 (:108); revoke copy "They can sign in again" at :205/:209 (security-remediation-posture concern — score it, but note copy/IA ONLY, NO tier ratchet per D-08). audit-user-live — chips OUTSIDE form (:163-172); "Effective user" ABSENT scored as a documented defensible asymmetry -> waiver-track, NOT a kill. branding-live — scope_ribbon hardcoded literal (:106) vs the computed `scope_copy/1` pattern; NEW-2: branding has no `scope_copy/1` helper so the fix must add a context-appropriate one.

    Use `user-sessions` (exact ledger key) as the filename/surface for the sessions doc, NOT `user-sessions-live`. Never place a bare 0/1/2 in the 4th pipe column of any table (D-02).
  </action>
  <verify>
    <automated>for f in user-show-live user-sessions audit-user-live branding-live; do bash scripts/ci/panel-schema-check.sh ".planning/uat-evidence/v1.42-persona-jtbd/$f.md"; done && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main</automated>
  </verify>
  <acceptance_criteria>
    - All 4 leaf docs pass `panel-schema-check.sh` (schema-valid, cells complete, no column-4 integer).
    - The sessions doc filename/surface is `user-sessions` (matches the ledger key exactly).
    - The audit-user "Effective user" absence is scored as a documented defensible asymmetry (waiver-track), not a kill.
    - `quality-ledger-monotonic.sh --base origin/main` exits 0.
  </acceptance_criteria>
  <done>All 8 per-surface panel docs now exist and are schema-valid, floor-complete, and column-4-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Author the roll-up index linking all 8 docs</name>
  <files>.planning/v1.42-PERSONA-JTBD-PANEL.md</files>
  <read_first>
    - `guides/reference/admin-persona-jtbd-rubric.md` — the roll-up index format: `| surface | disposition | kill-count | tighten-count | links |`; the `disposition` column holds clean/actionable/blocked, NEVER 0/1/2.
    - The 8 docs authored in Tasks 1-2 (read their frontmatter dispositions + finding counts).
    - RESEARCH Pitfall 2 — design the roll-up table so the 4th pipe column (tighten-count) is never a bare integer: express counts non-numerically (e.g. `x2`, `2 tighten`, `none`) OR arrange columns so column 4 is not a bare `^[012]$`. Verify with the monotonic guard.
  </read_first>
  <action>
    Write `.planning/v1.42-PERSONA-JTBD-PANEL.md`: a header describing the panel + the raw->resolved disposition mapping rule (once, authoritatively), then the summary table `| surface | disposition | kill-count | tighten-count | links |` with one row per surface, each linking its per-surface doc. The disposition column MUST be clean/actionable/blocked (never 0/1/2). Express kill-count / tighten-count so no cell that lands in the table's 4th pipe-delimited field is a bare 0/1/2 (e.g. write `2 tighten` / `x2` / `none`, or add a leading blank column so the guard's column-4 read never sees a bare integer). Cross-reference from the roll-up back to the ledger and to this phase's remediation plans.
  </action>
  <verify>
    <automated>test -f .planning/v1.42-PERSONA-JTBD-PANEL.md && grep -Eq '^\| *surface *\|' .planning/v1.42-PERSONA-JTBD-PANEL.md && ! grep -Eq '^\| [a-z][^|]*\|[^|]*\|[^|]*\| *[012] *\|' .planning/v1.42-PERSONA-JTBD-PANEL.md && for s in index-live organization-live users-index-live user-show-live user-sessions audit-index-live audit-user-live branding-live; do grep -q "$s" .planning/v1.42-PERSONA-JTBD-PANEL.md || { echo "missing $s"; exit 1; }; done && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main</automated>
  </verify>
  <acceptance_criteria>
    - Roll-up exists with the `| surface | disposition | kill-count | tighten-count | links |` table linking all 8 docs.
    - No table row places a bare 0/1/2 in its 4th pipe column.
    - `quality-ledger-monotonic.sh --base origin/main` exits 0 with the roll-up present.
    - The raw->resolved disposition mapping rule is stated once, authoritatively, in the roll-up.
  </acceptance_criteria>
  <done>SC-1 is satisfied: 8 schema-valid per-surface docs + a roll-up index linking all 8, with the disposition semantics defined and no monotonic-guard false-match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs -> monotonic guard parser | Markdown tables in panel docs are parsed by `awk -F'|'`; a bare 0/1/2 in the 4th column would inject a phantom ledger row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-02-01 | Tampering | panel/roll-up markdown tables vs `quality-ledger-monotonic.sh` | low | mitigate | Column-4 integer prohibition (D-02) enforced by `panel-schema-check.sh` + a per-task run of `quality-ledger-monotonic.sh --base origin/main` proving no false-match. |
| T-209-02-02 | Information Disclosure | admin-page DOM anchors in planning docs | low | accept | Docs cite file:line + copy strings only; no secrets, tokens, or PII. Planning artifacts already contain equivalent source references. |
| T-209-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — pure markdown authoring + a bash/python validator using the stdlib. |
</threat_model>

<verification>
- `panel-schema-check.sh` passes for all 8 docs (schema-valid, forced-finding floor complete, column-4 clean).
- `quality-ledger-monotonic.sh --base origin/main` exits 0 with all docs + roll-up present.
- All 8 `surface` frontmatter values equal their ledger row keys exactly.
- Manual (per 209-VALIDATION Manual-Only): adversarial verdict quality — every cell is a DOM-anchored finding or the NONE token; DOM anchors verified against live source.
</verification>

<success_criteria>
- SC-1: 8 committed scored review docs (3-persona verdicts + per-surface disposition) + a roll-up linking all 8.
- Forced-finding floor holds (no empty cells); column-4 integer prohibition holds; disposition mapping defined.
</success_criteria>

<output>
Create `.planning/phases/209-judgment-level-page-pass/209-02-SUMMARY.md` when done.
</output>
