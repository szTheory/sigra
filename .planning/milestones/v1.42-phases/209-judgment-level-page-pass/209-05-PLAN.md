---
phase: 209-judgment-level-page-pass
plan: 05
type: execute
wave: 2
depends_on: [209-02]
files_modified:
  - lib/sigra/admin/live/audit_index_live.ex
  - lib/sigra/admin/live/audit_user_live.ex
  - lib/sigra/admin/live/users_index_live.ex
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements: [PAGE-02]
must_haves:
  truths:
    - "audit_index_live renders scope_ribbon ABOVE the header, matching sibling pages (the lone outlier is fixed)"
    - "The applied-chips form position asymmetry across audit pages is resolved to one canonical position OR documented as intentional per the design contract"
    - "users_index_live chips-inside-form and phx-click disclosure are confirmed unchanged (canonical / acceptable divergence — no regression)"
    - "The quality ledger evidence strings for the touched pages are refreshed; no tier decreases"
  artifacts:
    - "lib/sigra/admin/live/audit_index_live.ex (remediated)"
    - "lib/sigra/admin/live/audit_user_live.ex (remediated or waiver-confirmed)"
    - "guides/reference/admin-quality-ledger.md (evidence strings refreshed; tiers unchanged)"
  key_links:
    - "audit_index scope_ribbon moves above <header> (currently below at :56)"
    - "ledger monotonic guard stays green after evidence-string edits"
---

<objective>
Resolve the cross-page composition `actionable` verdicts from the Wave-1 panel: move `audit_index_live`'s scope_ribbon from BELOW the header (:56, the lone sibling outlier) to ABOVE; resolve the applied-chips form-position asymmetry across the audit pages to one canonical position OR confirm it as the intentional Audit-Explorer archetype pattern with a documented rationale; and confirm the `users_index_live` chips-inside-form + `phx-click` disclosure are canonical/acceptable (no regression — keep, not fix). Refresh the ledger evidence strings for the touched pages without decreasing any tier.

Purpose: PAGE-02 / SC-2 (cross-page "same job -> same component" coherence) + SC-3 (monotonic guard green). In-place library-owned single-source edits. Per D-06/D-08.
Output: audit_index (+ audit_user if the chips asymmetry is aligned rather than waived) remediated; users_index confirmed unchanged; ledger evidence refreshed. Touches slugs `audit-explorer` (+ `user-audit` if audit_user DOM changes); `global-user-index` only if users_index changes (expected: no change).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-judgment-level-page-pass/209-CONTEXT.md
@.planning/phases/209-judgment-level-page-pass/209-RESEARCH.md
@.planning/phases/209-judgment-level-page-pass/209-UI-SPEC.md
@.planning/uat-evidence/v1.42-persona-jtbd/audit-index-live.md
@.planning/uat-evidence/v1.42-persona-jtbd/audit-user-live.md
@.planning/uat-evidence/v1.42-persona-jtbd/users-index-live.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-glossary.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: audit_index_live — move scope_ribbon above the header + resolve the chips-position asymmetry</name>
  <files>lib/sigra/admin/live/audit_index_live.ex, lib/sigra/admin/live/audit_user_live.ex, .planning/uat-evidence/v1.42-persona-jtbd/audit-index-live.md, .planning/uat-evidence/v1.42-persona-jtbd/audit-user-live.md</files>
  <read_first>
    - `.planning/uat-evidence/v1.42-persona-jtbd/audit-index-live.md` + `.../audit-user-live.md` — authored panel verdicts (authoritative worklist).
    - `lib/sigra/admin/live/audit_index_live.ex:55-56` — `</header>` at :55, `<.scope_ribbon>` at :56 (BELOW header — the lone outlier; siblings render it ABOVE); `:82` `<details>` (D-05 already-resolved, DO NOT touch); `:91` "Effective user" filter; `:140,142-149` chips OUTSIDE the form.
    - `lib/sigra/admin/live/audit_user_live.ex:105` `<details>` (D-05, DO NOT touch); `:161,163-172` chips OUTSIDE form; "Effective user" ABSENT (RESEARCH row 23 — DEFENSIBLE waiver, subject-scoped page).
    - A sibling page rendering scope_ribbon ABOVE its `<header>` (e.g. user_show_live / users_index_live) to match the exact placement/structure.
    - UI-SPEC Interaction Contract "Scope ribbon position — Above the page `<header>` on every list/leaf page; NOT below (fixing audit_index_live.ex)"; and the applied-chips rows: audit post-form position may be the Audit-Explorer archetype's DEFINED pattern (contract D-03) -> verify intentional per the contract OR align to one canonical position with a documented rationale.
    - `guides/reference/admin-design-contract.md` — the List vs Audit-Explorer archetype chip-position definitions.
  </read_first>
  <action>
    In `lib/sigra/admin/live/audit_index_live.ex`: move the `<.scope_ribbon>` (:56) to render ABOVE the page `<header>`, matching the sibling pattern exactly (the UI-SPEC names this the fix). Do NOT touch the `<details>` disclosure (:82) or the "Effective user" filter (:91).

    Resolve the applied-chips form-position asymmetry per the panel + the design contract: determine whether the audit pages' post-form chip position is the DEFINED Audit-Explorer archetype pattern (contract D-03). If it IS the defined pattern, record a documented rationale (`Waiver: post-form chip position is the Audit-Explorer archetype per admin-design-contract.md D-03`) in the audit panel docs and leave the chips as-is (no DOM change to chips). If the panel judges it a genuine incoherence, align both audit pages to one canonical position with a documented rationale. Prefer the documented-intentional resolution unless the contract says otherwise (avoids needless baseline churn per D-05/Pitfall 5). If audit_user chips are NOT changed, that file's only touch is the panel-doc waiver note (no DOM edit).

    Keep copy glossary-clean. Append resolution notes (diff ref or Waiver) to the audit-index and audit-user panel docs for each actionable verdict, and record the audit_user "Effective user" absence as the documented defensible waiver (D-05/D-07).
  </action>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && ! grep -nA1 '</header>' lib/sigra/admin/live/audit_index_live.ex | grep -q 'scope_ribbon'</automated>
  </verify>
  <acceptance_criteria>
    - `audit_index_live` renders scope_ribbon ABOVE `<header>` (no longer immediately after `</header>`).
    - The chips-position asymmetry is resolved: either a documented intentional-archetype waiver (no chip DOM change) OR both audit pages aligned to one canonical position with rationale.
    - `<details>` disclosures and "Effective user" filter untouched (D-05); audit_user "Effective user" absence recorded as a defensible waiver.
    - `glossary_test.exs` passes; monotonic guard exits 0; panel docs carry resolution notes.
  </acceptance_criteria>
  <done>Audit Explorer's scope_ribbon matches sibling placement and the chips asymmetry is coherently resolved (aligned or documented); verdicts resolved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: users_index_live no-regression confirmation + ledger evidence refresh</name>
  <files>lib/sigra/admin/live/users_index_live.ex, guides/reference/admin-quality-ledger.md, .planning/uat-evidence/v1.42-persona-jtbd/users-index-live.md</files>
  <read_first>
    - `.planning/uat-evidence/v1.42-persona-jtbd/users-index-live.md` — the panel verdicts (expected: chips-inside-form CANONICAL / phx-click disclosure acceptable -> keep, no fix).
    - `lib/sigra/admin/live/users_index_live.ex:90,107-114,176` — `<form>` open/close bracketing the chips INSIDE (canonical List Archetype per UI-SPEC/contract D-01); `:121-128` `phx-click` `toggle_filters` disclosure (D-05 acceptable divergence); `:188` "Total users" — **Plan 03 is the SOLE OWNER of the Total-users dedup** (it drops/reframes the Overview side, index_live:88). This plan does a **confirmation-only, no-edit** check that `users_index_live.ex:188` remains the retained owner (assert the `label="Total users"` is still present; make NO mutation to it). Do NOT touch the Total-users metric here under any circumstances — the dedup decision is single-owned by Plan 03 to avoid a double-remove/double-keep race (both plans run in Wave 2 in parallel).
    - `guides/reference/admin-quality-ledger.md:85-92` — the L3 page rows + their evidence strings; the monotonic-guard parse reads the tier column (must not decrease).
    - RESEARCH Pitfall 2 + the monotonic-guard awk: leading `|` means the tier is the 4th pipe field; when editing evidence strings, keep each row's tier digit exactly where it is and do NOT introduce a stray bare 0/1/2 elsewhere in the evidence prose that could false-match.
  </read_first>
  <action>
    Confirm `lib/sigra/admin/live/users_index_live.ex` needs NO remediation for chips-inside-form (canonical) and the `phx-click` disclosure (acceptable divergence) — record these as `keep`/waiver in the users-index panel doc; do NOT edit the file for these. **Total-users dedup — confirmation-only, NO edit:** Plan 03 single-owns the dedup (it drops the Overview side, index_live:88). Here, ONLY assert that `users_index_live.ex:188` `label="Total users"` remains present as the retained metric owner — make NO mutation to it (no removal, no reframing). Do NOT edit the Total-users metric on this side; the two plans must not both mutate the metric (they run in parallel in Wave 2), so this side is a read-only confirmation.

    Refresh `guides/reference/admin-quality-ledger.md` evidence strings for the pages touched this phase (index-live, organization-live, users-index-live, user-show-live, user-sessions, audit-index-live, audit-user-live, branding-live) so the evidence reflects the remediations — WITHOUT changing any tier digit (7 pages stay Tier-2; user-sessions stays Tier-1 per D-08). Do not introduce a bare 0/1/2 into evidence prose in a position the guard's `awk -F'|'` reads as column 4.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && bash scripts/ci/quality-ledger-monotonic.test.sh && grep -q 'label="Total users"' lib/sigra/admin/live/users_index_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - users_index chips-inside-form + phx-click disclosure confirmed unchanged (keep/waiver in the panel doc); no needless DOM churn.
    - `users_index_live.ex:188` `label="Total users"` remains present (confirmation-only); this plan makes NO mutation to the Total-users metric (Plan 03 single-owns the dedup by dropping the Overview side).
    - Ledger evidence strings refreshed for the 8 touched pages with NO tier digit changed (7 Tier-2, user-sessions Tier-1).
    - `quality-ledger-monotonic.sh --base origin/main` exits 0 AND its self-test passes.
  </acceptance_criteria>
  <done>users_index is confirmed regression-free, the retained Total-users owner (`:188`) is asserted present with no mutation on this side (Plan 03 single-owns the dedup), and the ledger evidence reflects the remediations without any tier regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning ledger -> monotonic guard | Evidence-string edits to the ledger are parsed by the monotonic guard; a stray column-4 integer or a lowered tier would corrupt the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-05-01 | Tampering | ledger evidence-string edits | low | mitigate | Edits touch evidence prose only; tier digits stay put; `quality-ledger-monotonic.sh --base origin/main` + its self-test run per commit prove no tier decreased and no phantom column-4 row was introduced. |
| T-209-05-02 | Elevation of Privilege | audit page scope_ribbon / chips reposition | low | accept | Repositioning is presentational (component placement); it does not alter the GET-form filter contract, auth-scoping, or the subject-scoping of the per-user audit page. |
| T-209-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — Elixir/HEEx + markdown edits only. |
</threat_model>

<verification>
- `glossary_test.exs` green.
- `quality-ledger-monotonic.sh --base origin/main` exits 0 AND `quality-ledger-monotonic.test.sh` self-test passes.
- Grep gate confirms scope_ribbon no longer sits immediately after `</header>` in audit_index_live.
- Panel docs carry resolution notes / waivers for every cross-page verdict; audit_user "Effective user" waiver recorded.
</verification>

<success_criteria>
- Cross-page composition `actionable` verdicts resolved (aligned or documented); ledger evidence refreshed with zero tier regression.
</success_criteria>

<output>
Create `.planning/phases/209-judgment-level-page-pass/209-05-SUMMARY.md` when done.
</output>
