---
phase: 209-judgment-level-page-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [PAGE-02]
must_haves:
  truths:
    - "A CI-native ubuntu job recaptures the admin-checkpoint PNG baselines (never darwin)"
    - "The recapture job commits regenerated baselines to a reviewable branch + PR, not main"
    - "The impersonation-banner canary is re-established as 'added' (delete-before-recapture), never a forbidden 'modify'"
  artifacts:
    - ".github/workflows/ci.yml (new admin_checkpoint_recapture job)"
  key_links:
    - "admin_checkpoint_recapture → snapshot-canary-guard.sh (--canary impersonation-banner, --allowlist snapshot-allowlist)"
    - "recapture job → admin-checkpoints.spec.ts (3 checkpoint projects)"
---

<objective>
Add the missing CI-native ubuntu recapture job for the admin **checkpoint** lane (OQ-3, HIGH-priority enabler). The design lane already has `admin_design_recapture`; the checkpoint analog does not exist, and D-09 forbids darwin recapture (baselines are platform-pinned — no `{platform}` token in `pathTemplate`). Without this job the phase stalls at SC-4 (baseline recapture).

Purpose: Provide the ubuntu-native mechanism that Wave 3 (Plan 06) uses to recapture the checkpoint slugs touched by the Wave 2 remediations, and to re-establish the `impersonation-banner` canary (D-10 part 2). Per D-09.
Output: A new `admin_checkpoint_recapture` job in `.github/workflows/ci.yml`, mirroring `admin_design_recapture` but scoped to the checkpoint spec/projects/dir/canary/allowlist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-judgment-level-page-pass/209-RESEARCH.md
@.planning/phases/209-judgment-level-page-pass/209-CONTEXT.md

# The proven pattern to mirror (design lane) + the guard it calls:
@.github/workflows/ci.yml
@scripts/ci/snapshot-canary-guard.sh
@test/example/priv/playwright/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin_checkpoint_recapture job mirroring the design-lane recapture pattern</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` — the existing `admin_design_recapture` job (job key `admin_design_recapture`, ~line 1386 to ~line 1600): its boot prelude (checkout/setup-beam/setup-node SHA-pinned actions, postgres service, MIX_ENV dev, deps cache, ecto.create/migrate, seeds, playwright npm ci + browser install, background phx.server + readiness/warmup loop), its canary delete-before-recapture step, its `--update-snapshots` recapture step, its snapshot-canary-guard.sh gate + `ci/recapture-admin-design-<run_id>` branch commit + `gh pr create` step, and its `if: github.event_name != 'pull_request'` trigger + `permissions: {contents: write, pull-requests: write}`.
    - `test/example/priv/playwright/playwright.config.ts:114-145` — the 3 checkpoint project names: `admin-checkpoints-chromium`, `admin-checkpoints-mobile`, `admin-checkpoints-dark`.
    - `scripts/ci/snapshot-canary-guard.sh` — the `slug_of` suffix-strip (handles `-admin-checkpoints-{chromium,mobile,dark}.png`), the `--canary` / `--allowlist` flags, and the canary rule (`added` OK / `modified` FAIL).
    - `test/example/priv/playwright/snapshot-allowlist` — the checkpoint allowlist file path (canary explicitly banned from it).
  </read_first>
  <action>
    Clone the `admin_design_recapture` job in `.github/workflows/ci.yml` under a new job key `admin_checkpoint_recapture` with name "Recapture admin-checkpoint baselines (in-CI)". Reuse the boot prelude byte-for-byte (same SHA-pinned action refs, postgres service, MIX_ENV dev, deps cache key/paths, ecto.create/migrate, seeds, playwright npm ci + browser install, background phx.server + wait/warmup loop) — the checkpoint lane needs the same running example app. Keep the SAME trigger `if: github.event_name != 'pull_request'`, the SAME `needs: release_ref_guard`, and the SAME `permissions: {contents: write, pull-requests: write}`. Do NOT add it to `ci-gate.needs` (it is an out-of-band recapture job, matching the design lane).

    Change ONLY the checkpoint-specific parameters relative to the design job:
    - Snapshot dir: `test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots` (call it SNAP_DIR).
    - Canary re-establish step: delete `impersonation-banner-admin-checkpoints-*.png` (not `board-notice-...`) before `--update-snapshots`, so the canary recaptures as `added` (the guard's legitimate birth path) not `modified`. Echo the removed files. This is the D-10 part-2 mechanism — a deliberate one-time re-baseline preserving the shipped WCAG contrast fix, NOT allowlisting the canary.
    - Recapture step: `npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-chromium --project=admin-checkpoints-mobile --project=admin-checkpoints-dark --update-snapshots`.
    - Guard step: `slug_of` strip pattern `-admin-checkpoints-(chromium|mobile|dark)\.png` in the changed-slug sed; call `bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist test/example/priv/playwright/snapshot-allowlist --canary impersonation-banner "${allow_args[@]}"`; exclude only `impersonation-banner` from `--allow` args (it recaptures as `added`).
    - Commit/PR step: branch `ci/recapture-admin-checkpoints-${{ github.run_id }}`, commit message `ci(209): recapture admin-checkpoint baselines in ubuntu CI [skip ci]`, `gh pr create --base main` with a body explaining: automated checkpoint recapture; impersonation-banner canary re-established as 'added' (D-10 part 2, WCAG contrast-fix rationale); merge after visual review.

    Do NOT copy the design job's OQ3 "cross-lane compare" tail step (it is design-lane specific). Do NOT introduce a `workflow_dispatch` input; the `github.event_name != 'pull_request'` trigger (push-to-main + nightly cron + manual dispatch) matches the design lane and is sufficient. Keep the `[skip ci]` marker in the commit so the recapture push does not re-trigger this job.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['admin_checkpoint_recapture']; assert j['if'].strip()==\"github.event_name != 'pull_request'\", j['if']; assert 'contents: write' in yaml.safe_dump(j['permissions']); assert 'admin-checkpoints.spec.ts' in yaml.safe_dump(j); print('admin_checkpoint_recapture job valid')"</automated>
  </verify>
  <acceptance_criteria>
    - Job `admin_checkpoint_recapture` exists in ci.yml, YAML parses, trigger is `github.event_name != 'pull_request'`, has `contents: write` + `pull-requests: write` permissions.
    - The recapture step targets `tests/admin-checkpoints.spec.ts` across all 3 checkpoint projects with `--update-snapshots`.
    - The canary re-establish step deletes `impersonation-banner-admin-checkpoints-*.png` before recapture.
    - The guard call uses `--canary impersonation-banner --allowlist test/example/priv/playwright/snapshot-allowlist`.
    - The commit lands on `ci/recapture-admin-checkpoints-<run_id>` with `[skip ci]` and opens a PR (not a push to main).
    - Job is NOT added to `ci-gate.needs`.
  </acceptance_criteria>
  <done>A CI-native ubuntu checkpoint recapture job exists, mirrors the proven design-lane pattern, re-establishes the canary as 'added', and produces a reviewable PR — the D-09 recapture mechanism Wave 3 depends on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo (write) | The recapture job holds `contents: write` + opens a PR; it must not push to main directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-01-01 | Elevation of Privilege | `admin_checkpoint_recapture` write perms | low | mitigate | Job commits to a `ci/recapture-admin-checkpoints-<run_id>` branch + `gh pr create` only — never pushes to `main`; PR review under branch protection gates the merge (mirrors the audited design-lane pattern). `[skip ci]` prevents recursive triggering. |
| T-209-01-02 | Tampering | Canary re-baseline via delete-before-recapture | low | accept | The delete targets ONLY `impersonation-banner-admin-checkpoints-*.png`; the guard still enforces that every other changed slug is explicitly allowlisted. Re-establishing the canary as `added` is the documented D-10 resolution, not a guard bypass. Rationale is recorded in the PR body + phase artifacts. |
| T-209-01-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs — the job reuses the existing pinned `npm ci` + `npx playwright install` from the design-lane prelude; no new deps introduced. |
</threat_model>

<verification>
- YAML parses and the new job validates (Task 1 automated check).
- The job is a structural clone of the audited `admin_design_recapture` pattern with only checkpoint-specific parameters changed; no new attack surface beyond the already-accepted design-lane job.
</verification>

<success_criteria>
- `admin_checkpoint_recapture` present, valid, checkpoint-scoped, canary-safe, PR-based, out-of-band (not required).
</success_criteria>

<output>
Create `.planning/phases/209-judgment-level-page-pass/209-01-SUMMARY.md` when done.
</output>
