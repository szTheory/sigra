---
phase: "207"
plan: "03"
type: execute
wave: 2
depends_on:
  - "207-01"
  - "207-02"
files_modified:
  - guides/reference/admin-token-reference.md
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-empty-state-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-empty-state-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-empty-state-admin-design-dark.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-page-back-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-page-back-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-page-back-admin-design-dark.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-scope-ribbon-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-scope-ribbon-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-scope-ribbon-admin-design-dark.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-field-help-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-field-help-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-field-help-admin-design-dark.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-skeleton-admin-design-chromium.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-skeleton-admin-design-mobile.png
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-skeleton-admin-design-dark.png
autonomous: true
requirements:
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "D-09: IF Plan 02 landed a CSS edit, the genuinely-affected board-* PNG baselines are recaptured via scripts/ci/snapshot-recapture-gate.sh with the hyphenated slugs (board-empty-state / board-page-back / board-scope-ribbon / board-field-help / board-skeleton); IF Plan 02 landed NO edit, recapture is a no-op and the admin-design suite runs in compare-mode to confirm zero drift"
    - "D-09: The impersonation-banner and board-notice canaries stay byte-stable and untouched; both allowlists (snapshot-allowlist, snapshot-allowlist-design) contain zero non-comment/non-blank lines at phase close"
    - "COMP-03: admin-token-reference.md is refreshed to cite the conformance evidence — it references scripts/ci/admin-token-completeness.sh (the D-06 guard) and scripts/ci/admin-css-conformance.sh (hex/[+px]); the stale '96/96' notion is corrected to the verified 100/100 (or cites the self-enforcing guard with no number)"
    - "D-09: scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 after this plan (no tier regressions; Plan 04 has not flipped rows yet so all 6 target rows are still 1 — guard exits 0)"
    - "D-07a: IF Plan 01 took the D-07a fallback (no px CHECK 3), admin-token-reference.md records the reviewed raw-px set as a documented manual proxy; IF Plan 01 added CHECK 3, the doc cites the automated px guard instead"
  artifacts:
    - guides/reference/admin-token-reference.md
  key_links:
    - "The recaptured (or compare-verified) board baselines are the byte-stable ground truth the snapshot-canary-guard enforces forward"
    - "The refreshed admin-token-reference.md citation of admin-token-completeness.sh is the COMP-03 criterion-2 proof that Plan 04's token-layer ledger evidence cites"
    - "Plan 02's SUMMARY 'CSS edited: yes/no' verdict + slug list determines whether this plan recaptures PNGs or runs compare-mode only"
---

<objective>
Run the v1.41 snapshot-recapture gate for any board-* baselines affected by a CSS edit in
Plan 02 (expected: none → compare-mode no-op), confirm the impersonation-banner and
board-notice canaries stay byte-stable with both allowlists empty, and refresh
admin-token-reference.md to cite the new COMP-03 conformance evidence (admin-token-completeness.sh
+ admin-css-conformance.sh; correct the stale '96/96' to the verified 100/100; record the
D-07/D-07a px outcome).

Purpose: Snapshot discipline (D-09) — any CSS edit touching pixels forces baseline recapture
under allowlist-then-clear discipline; if no edit landed, compare-mode proves zero drift. The
doc refresh satisfies COMP-03 criterion 2 ("admin-token-reference.md refreshed to cite the
conformance evidence") and makes the token-completeness claim self-enforcing.

Output: refreshed admin-token-reference.md; recaptured-or-compare-verified board baselines;
both canaries byte-stable; both allowlists empty; monotonic guard green vs origin/main.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-CONTEXT.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-01-SUMMARY.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-SUMMARY.md
@scripts/ci/snapshot-recapture-gate.sh
@scripts/ci/snapshot-canary-guard.sh
@guides/reference/admin-token-reference.md
@test/example/priv/playwright/playwright.config.ts
@test/example/priv/playwright/tests/admin-design.spec.ts
</context>

## Artifacts this phase produces

- a refreshed conformance-evidence section in `guides/reference/admin-token-reference.md` citing `admin-token-completeness.sh` + `admin-css-conformance.sh`, the verified 100/100 token count, and the D-07/D-07a px outcome
- (conditional) recaptured `board-*` PNG baselines for the 5 component boards — ONLY if Plan 02 landed a CSS edit (expected: none → no PNG changes)

<tasks>

<task type="auto">
  <name>Task 1: Refresh admin-token-reference.md to cite conformance evidence (COMP-03 / D-07a)</name>
  <files>guides/reference/admin-token-reference.md</files>

  <read_first>
  - guides/reference/admin-token-reference.md — read the intro (~:1-9): line 3 describes "every --sg-* custom property in the :root layer" (no literal "96"); identify where to add a short "Conformance" note (a new subsection near the top or bottom)
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-01-SUMMARY.md — read the "D-07 decision" section: whether PATH A (px CHECK 3 added to admin-css-conformance.sh) or PATH B (D-07a manual-review fallback) was taken; cite the correct proof shape
  - scripts/ci/admin-token-completeness.sh — confirm the exact guard name + PASS line wording (100 tokens) to cite verbatim
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-RESEARCH.md "D-06" section — the verified 100/100 count (correct the stale "96/96" paraphrase)
  </read_first>

  <action>
  Add (or refresh) a short "Token Conformance" note in admin-token-reference.md that cites the automated evidence backing COMP-03. The note must:

  1. State that the :root --sg-* token set is verified complete against this document by scripts/ci/admin-token-completeness.sh (the D-06 guard) — it fails CI if any :root token is undocumented or any documented token is missing from :root. Cite the verified current count: 100 unique --sg-* tokens (correct any stale "96/96" notion — research verified 100/100). Prefer citing the self-enforcing guard so the number cannot rot; if a number is stated, state 100.

  2. State that raw-hex conformance (no hex outside :root) is guarded by scripts/ci/admin-css-conformance.sh (CHECK 2).

  3. Record the raw-px outcome per Plan 01's decision:
     - IF PATH A (CHECK 3 added): state that raw-px in token-eligible property contexts is guarded by admin-css-conformance.sh CHECK 3.
     - IF PATH B (D-07a fallback): record the reviewed raw-px set as a documented manual proxy (mirroring the existing target-size manual proxy) — list the legitimate idiom categories (media-query breakpoints, 1px hairline borders, box-shadow geometry, micro transform nudges, border-radius: 999px pill, a11y clip) and state that none bypass the --sg-space-*/--sg-text-* token scales; note that admin-token-completeness.sh is the load-bearing automated COMP-03 proof.

  Use Edit (scoped) — do NOT Write the whole file. Make a minimal, additive edit; do not reorder or reformat existing token tables. Do not introduce any literal that a conformance guard negative-greps for as a head comment.
  </action>

  <verify>
    <automated>bash -c 'grep -q "admin-token-completeness" guides/reference/admin-token-reference.md && grep -q "admin-css-conformance" guides/reference/admin-token-reference.md && echo "evidence cited"'</automated>
  </verify>

  <acceptance_criteria>
  - grep -c 'admin-token-completeness' guides/reference/admin-token-reference.md returns >= 1 (D-06 guard cited)
  - grep -c 'admin-css-conformance' guides/reference/admin-token-reference.md returns >= 1 (hex/[+px] guard cited)
  - grep -c '96/96' guides/reference/admin-token-reference.md returns 0 (no stale count introduced; if a number is present it is 100)
  - The doc records the D-07/D-07a px outcome consistent with Plan 01's SUMMARY decision
  - bash scripts/ci/admin-token-completeness.sh exits 0 after the doc edit (the doc's backtick token set still matches :root exactly — the edit must not add/remove backtick --sg-* tokens that break the 100/100 match)
  </acceptance_criteria>

  <done>admin-token-reference.md cites admin-token-completeness.sh + admin-css-conformance.sh, states the verified 100 count (no stale 96/96), records the px outcome, and the completeness guard still exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Recapture affected board baselines (or compare-mode no-op) + canary discipline</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-empty-state-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-empty-state-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-empty-state-admin-design-dark.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-page-back-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-page-back-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-page-back-admin-design-dark.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-scope-ribbon-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-scope-ribbon-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-scope-ribbon-admin-design-dark.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-field-help-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-field-help-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-field-help-admin-design-dark.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-skeleton-admin-design-chromium.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-skeleton-admin-design-mobile.png, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-skeleton-admin-design-dark.png</files>

  <read_first>
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-SUMMARY.md — the "CSS edited: yes/no" verdict + (if yes) the exact affected board-slug list; this gates recapture vs compare-mode
  - scripts/ci/snapshot-recapture-gate.sh — usage: pass intended hyphenated slug(s) as positional args; it boots/expects the example at SIGRA_EXAMPLE_URL, runs the 3 admin-design projects, the canary guard, and component goldens; requires every declared slug to actually change (--require-all semantics)
  - scripts/ci/snapshot-canary-guard.sh — confirm the canaries: board-notice (design lane) and impersonation-banner (checkpoint lane) must stay byte-stable
  - test/example/priv/playwright/snapshot-allowlist-design and test/example/priv/playwright/snapshot-allowlist — confirm both are currently empty (zero non-comment/non-blank lines); intended slugs are declared during the recapture run only, never committed to the allowlist
  - test/example/priv/playwright/playwright.config.ts — the 3 project names: admin-design-chromium, admin-design-mobile, admin-design-dark; base URL
  - CLAUDE.md "Local development prerequisites" — boot the test Postgres (scripts/db/up.sh) + example app; PORT/PG compile-time caveats; disposable-PG fallback if local Postgres is saturated
  - .planning/phases/206-l1-component-elevation-wave-a/206-03-PLAN.md Task 2 — the proven recapture/compare-mode/canary sequence to mirror
  </read_first>

  <action>
  BRANCH on Plan 02's verdict:

  IF "CSS edited: no" (expected): NO recapture. Boot the test Postgres + example app (per CLAUDE.md), then run the admin-design suite in COMPARE-MODE (no --update-snapshots) across all 3 projects to confirm zero snapshot drift and axe-clean:
    cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark
  Must exit 0. Note in SUMMARY: "recapture no-op — zero CSS edit; compare-mode confirms zero drift + axe-clean". No PNG files change.

  IF "CSS edited: yes — <slugs>": recapture ONLY the genuinely-affected hyphenated slugs (subset of: board-empty-state, board-page-back, board-scope-ribbon, board-field-help, board-skeleton):
    1. Boot Postgres + example app.
    2. For each affected slug, run: npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --update-snapshots=all --grep "board-<slug>" (use the board id grep form that matches the spec's test titles; mirror 206-03).
    3. Run scripts/ci/snapshot-recapture-gate.sh <slug...> with the affected hyphenated slugs — it runs compare-mode, the canary guard (board-notice + impersonation-banner byte-stable), and component goldens; --require-all ensures every declared slug actually changed.
    4. Restore any incidentally-changed PNG that was NOT an intended slug. Do not commit any slug into the allowlist files; clear before close.

  ALWAYS (both branches):
    - Confirm board-notice and impersonation-banner canaries are byte-stable.
    - Confirm both allowlists have zero non-comment/non-blank lines.
    - Run scripts/ci/quality-ledger-monotonic.sh --base origin/main → exits 0 (rows still at 1; Plan 04 flips them).
    - Run mix test test/sigra/admin/components_test.exs → exits 0.
  </action>

  <verify>
    <automated>bash -c 'set -e; bash scripts/ci/quality-ledger-monotonic.sh --base origin/main; al=$(grep -vE "^#|^$" test/example/priv/playwright/snapshot-allowlist-design | wc -l | tr -d " "); ald=$(grep -vE "^#|^$" test/example/priv/playwright/snapshot-allowlist | wc -l | tr -d " "); [ "$al" = "0" ] && [ "$ald" = "0" ] && echo "allowlists empty + monotonic green"'</automated>
  </verify>

  <acceptance_criteria>
  - test/example/priv/playwright/snapshot-allowlist-design has zero non-comment/non-blank lines at phase close
  - test/example/priv/playwright/snapshot-allowlist has zero non-comment/non-blank lines at phase close
  - board-notice canary and impersonation-banner canary are byte-stable (no modify/delete in the canary-guard output, or git status shows their PNGs unchanged)
  - IF no CSS edit: admin-design suite compare-mode across all 3 projects exits 0 and no board-*.png changed (git status --porcelain on the snapshots dir is empty)
  - IF CSS edit: scripts/ci/snapshot-recapture-gate.sh <affected-slugs> exits 0 and only the declared slugs changed
  - bash scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0
  - mix test test/sigra/admin/components_test.exs exits 0
  </acceptance_criteria>

  <done>Affected board baselines recaptured (or zero-drift confirmed via compare-mode); both canaries byte-stable; both allowlists empty; monotonic guard green vs origin/main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright recapture → PNG commit | Recaptured PNGs replace prior baselines; wrong snapshots become the new ground truth |
| Doc edit → completeness guard | Adding/removing a backtick --sg-* token in the doc can break the 100/100 match |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-06 | Tampering | Canary byte-stability | high | mitigate | board-notice + impersonation-banner checked by snapshot-canary-guard; any modification aborts the gate (D-09) |
| T-207-07 | Tampering | Recapture scope creep | medium | mitigate | Only declared slugs recaptured; --require-all ensures each declared slug actually changed; incidental PNGs restored; allowlists cleared before close |
| T-207-08 | Repudiation | Doc edit breaks token match | medium | mitigate | admin-token-completeness.sh re-run after the doc edit must exit 0 — gates the doc change on the 100/100 match |
</threat_model>

<verification>
After both tasks complete:
  grep -c 'admin-token-completeness' guides/reference/admin-token-reference.md   (returns >= 1)
  bash scripts/ci/admin-token-completeness.sh                                    (exits 0)
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main                 (exits 0)
  mix test test/sigra/admin/components_test.exs                                  (exits 0)
  grep -vE '^#|^$' test/example/priv/playwright/snapshot-allowlist-design | wc -l   (returns 0)
  grep -vE '^#|^$' test/example/priv/playwright/snapshot-allowlist | wc -l          (returns 0)
</verification>

<success_criteria>
- admin-token-reference.md refreshed to cite the conformance evidence (admin-token-completeness.sh + admin-css-conformance.sh), verified 100 count, px outcome recorded
- Affected board PNGs recaptured or zero-drift confirmed via compare-mode (expected: no-op)
- Both canaries (board-notice, impersonation-banner) byte-stable
- Both allowlists empty at phase close
- quality-ledger-monotonic.sh --base origin/main exits 0; component golden suite exits 0
</success_criteria>

<output>
Create .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-03-SUMMARY.md when done
</output>
