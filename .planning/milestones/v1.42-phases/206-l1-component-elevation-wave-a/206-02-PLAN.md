---
phase: "206"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/assets/sigra_admin.css
  - test/fixtures/install_golden/assets/sigra_admin.css
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "D-02: All 8 L1 components (notice, notice_link, stat, stat_link, summary_chip, task_card, applied_chip, audit_row) have their interaction-state CSS (hover/focus/active/disabled where applicable), target-size, and light/dark correctness audited and documented in the SUMMARY"
    - "D-03: The one raw hex literal (#fff at sigra_admin.css:~506) outside the :root token blocks is replaced with var(--sg-color-on-brand) or the correct semantic token; the edit lands in priv/templates/sigra.install/admin/sigra_admin.css ONLY"
    - "D-03: Generated copies (test/example/assets/sigra_admin.css and test/fixtures/install_golden/assets/sigra_admin.css) are propagated to stay byte-coherent with the source; mix test test/sigra/admin/components_test.exs exits 0 after propagation"
    - "D-06: No per-component @media (prefers-reduced-motion) block is added — the global block at sigra_admin.css:~1467 covers all component transitions; SUMMARY notes this as confirmed sufficient"
  artifacts:
    - priv/templates/sigra.install/admin/sigra_admin.css
  key_links:
    - "Audit findings (gap list vs already-present) are documented in SUMMARY for Plan 04 ledger evidence strings to cite"
    - "The #fff fix eliminates the one raw-hex violation that Plan 01's guard would otherwise catch — Plan 01 guard must exit 0 after this fix"
---

<objective>
Audit all 8 L1 components against the three Tier-2 L1 add-on proxies — interaction-state
CSS, target-size, light/dark token correctness — and apply the one confirmed narrow gap fix:
replace the raw hex `#fff` at sigra_admin.css:~506 with a semantic token. Propagate the
CSS edit to the generated copies to maintain byte-coherence.

Purpose: This is the audit-and-fix step of the audit → cite → narrow-gap-fix →
ledger-flip → recapture methodology (D-02). The component definitions and CSS classes are
already well-formed; the audit confirms and documents what exists. The `#fff` at line 506
is the one actionable raw-hex gap confirmed from code inspection (outside the :root block).

Output: An updated priv/templates/sigra.install/admin/sigra_admin.css (one targeted edit),
byte-coherent generated copies, and a SUMMARY documenting per-component audit findings
for consumption by Plan 04's ledger evidence authoring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/206-l1-component-elevation-wave-a/206-CONTEXT.md
@lib/sigra/admin/components.ex
@priv/templates/sigra.install/admin/sigra_admin.css
@guides/reference/admin-design-contract.md
@guides/reference/admin-ui-principles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit 8 L1 components — interaction-state, target-size, light/dark</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css</files>

  <read_first>
  - lib/sigra/admin/components.ex — read the 8 component function defs at the line numbers cited in CONTEXT.md: stat_link:53, stat:81, task_card:113, summary_chip:169, applied_chip:372, notice:509, notice_link:541, audit_row:702
  - priv/templates/sigra.install/admin/sigra_admin.css — read the sg-* CSS blocks for each component: sg-metric-link hover/focus/active (~:1384), sg-card-hover (~:559), applied-chip remove (~:966), notice__action (~:1064), the global @media (prefers-reduced-motion: reduce) block (~:1467); also the :root block (lines 20–173) and dark-mode :root (lines 174–212); the .sg-btn--danger rule at ~:506 containing color: #fff
  - guides/reference/admin-fractal-scorecard.md lines 160–175 — the three documented-as-manual L1 add-on proxies: interaction-state CSS, target-size, motion-token conformance
  </read_first>

  <action>
  Perform a code-level audit of all 8 L1 components. For each component, document in the SUMMARY (not inline in PLAN):

  1. INTERACTION-STATE CSS: Which states are CSS-addressable for this component? (e.g., stat and stat_link are links — hover/focus/active needed; notice is display-only — no states needed; applied_chip has remove action — hover/focus on the remove control; audit_row is display-only; task_card has sg-card-hover). Confirm each interactive component has CSS for its addressable states in sg-* classes. Record: "present / gap / N/A" per state per component.

  2. TARGET-SIZE: For each interactive component, identify the minimum rendered CSS dimensions. stat_link: the anchor wraps the entire metric card — full card width × height (well above 24×24). notice_link: inline link in notice body — inherits line-height, ~44px tall in most layouts. applied_chip remove: confirm the remove control min-height via sg-filter-chip__remove or equivalent class — check if it has an explicit min-height or relies on line-height. task_card: the card itself is a block element, no single interactive sub-target smaller than 24×24. summary_chip: display-only chip — N/A (no interactive target). notice: display-only — N/A. audit_row: display-only — N/A. stat: display-only — N/A. Record per component: "≥24×24 CSS px (confirmed)" or "≥24×24 CSS px (inferred from layout)" or specific measurement.

  3. LIGHT/DARK TOKEN CORRECTNESS: Confirm no component's sg-* block uses raw hex directly. The only raw hex found is at line 506 (.sg-btn--danger[data-armed] color: #fff) — this is in the button component shared by notice_link/notice__action contexts, not scoped to an L1 component class directly. Confirm all 8 component-specific sg-* classes use var(--sg-*) tokens only.

  Record the audit findings in the SUMMARY under a "Per-Component Audit" section. This becomes the source material for Plan 04 ledger evidence strings.

  Do NOT add per-component @media (prefers-reduced-motion) blocks — the global block at ~:1467 is confirmed sufficient (per D-06). Note this in SUMMARY.
  </action>

  <verify>
    <automated>bash -c '[ "$(grep -c "transition: all" priv/templates/sigra.install/admin/sigra_admin.css)" = "0" ]'</automated>
  </verify>

  <acceptance_criteria>
  - SUMMARY contains a "Per-Component Audit" section covering all 8 components × 3 axes (interaction-state / target-size / light-dark)
  - Audit confirms: zero transition: all in admin CSS (grep -c 'transition: all' priv/templates/sigra.install/admin/sigra_admin.css returns 0)
  - Audit confirms: global @media (prefers-reduced-motion: reduce) block present at ~:1467 (grep -c 'prefers-reduced-motion' priv/templates/sigra.install/admin/sigra_admin.css returns >= 1)
  - The one raw-hex gap is identified: .sg-btn--danger.is-armed color: #fff at ~:506, confirmed as outside any :root block
  </acceptance_criteria>

  <done>All 8 components audited; findings documented in SUMMARY; the one raw-hex gap identified and confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix raw hex gap (#fff → token) and propagate byte-coherence to generated copies</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css, test/example/assets/sigra_admin.css, test/fixtures/install_golden/assets/sigra_admin.css</files>

  <read_first>
  - priv/templates/sigra.install/admin/sigra_admin.css — read lines 495–515 to see the full .sg-btn--danger context; confirm color: #fff is the "on-danger-solid" semantic (white text on risk-colored background); identify the correct token — likely --sg-color-on-brand or a dedicated --sg-color-on-risk token if present in :root
  - test/example/assets/sigra_admin.css — read lines 495–515 to confirm it is a byte-for-byte copy of the template source at that location (so the same edit applies)
  - test/fixtures/install_golden/assets/sigra_admin.css — same check
  - scripts/ci/quality-ledger-monotonic.sh — for the ROOT derivation pattern (confirming scripts/ci/ is always two levels below repo root, which also means the generated copies are at test/example/assets/ and test/fixtures/install_golden/assets/)
  </read_first>

  <action>
  EDIT 1 — Fix the raw hex in the source template:
  In priv/templates/sigra.install/admin/sigra_admin.css at the .sg-btn--danger[data-armed="true"], .sg-btn--danger.is-armed rule (~:506), replace `color: #fff;` with `color: var(--sg-color-on-brand);` (which resolves to #ffffff in light mode per the :root block at ~:74 and #151515 in dark mode at ~:188, making the button text dark on the amber danger background in dark mode — semantically correct for the armed danger state on dark).

  If --sg-color-on-brand is semantically wrong here (e.g., this is a red danger button, not a brand button), and --sg-color-on-brand-solid or a risk-specific "on" token exists in :root, use that instead. Check :root for --sg-color-on-risk. If no on-risk token exists, --sg-color-on-brand (#fff / #151515) is the best available approximation and acceptable for now — note in SUMMARY.

  EDIT the source file ONLY: priv/templates/sigra.install/admin/sigra_admin.css

  PROPAGATE — Apply the identical character-for-character replacement to both generated copies:
  - test/example/assets/sigra_admin.css
  - test/fixtures/install_golden/assets/sigra_admin.css

  Use Edit tool (scoped replacement) — never Write the entire file. Find the matching line in each generated copy and apply the same replacement.

  VERIFY byte-coherence after propagation:
  - diff priv/templates/sigra.install/admin/sigra_admin.css test/example/assets/sigra_admin.css → exits 0
  - diff priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/assets/sigra_admin.css → exits 0

  Run component test suite to confirm golden byte-match:
  - mix test test/sigra/admin/components_test.exs
  </action>

  <verify>
    <automated>diff priv/templates/sigra.install/admin/sigra_admin.css test/example/assets/sigra_admin.css && diff priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/assets/sigra_admin.css && echo "byte-coherent"</automated>
  </verify>

  <acceptance_criteria>
  - grep -c '#fff' priv/templates/sigra.install/admin/sigra_admin.css returns 0 (after fix; the one occurrence is gone)
  - grep -c '#[0-9a-fA-F]' applied to lines after line 212 of sigra_admin.css returns 0 (all hex outside token blocks eliminated)
  - diff priv/templates/sigra.install/admin/sigra_admin.css test/example/assets/sigra_admin.css exits 0
  - diff priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/assets/sigra_admin.css exits 0
  - mix test test/sigra/admin/components_test.exs exits 0
  - bash scripts/ci/admin-css-conformance.sh exits 0 (both checks pass after the fix — guard can be run even if Plan 01 landed in the same wave; if Plan 01 script does not exist yet, skip this assertion and note in SUMMARY)
  </acceptance_criteria>

  <done>Raw hex gap eliminated; three CSS files are byte-coherent; component golden test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template source → generated copies | Edits to source must be manually mirrored to generated copies or template drift occurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-206-03 | Tampering | CSS token replacement (color: #fff → var) | low | accept | The replacement is a read-only semantic mapping; wrong token choice could change visual appearance in dark mode — mitigated by reading :root to confirm token semantics before applying |
| T-206-04 | Information Disclosure | Generated copy drift | medium | mitigate | Propagate identical replacement to all three files immediately; verify with diff; run components_test.exs to gate on byte-golden match |
</threat_model>

<verification>
After both tasks complete:
  diff priv/templates/sigra.install/admin/sigra_admin.css test/example/assets/sigra_admin.css
  diff priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/assets/sigra_admin.css
  mix test test/sigra/admin/components_test.exs
  grep -c 'transition: all' priv/templates/sigra.install/admin/sigra_admin.css   (returns 0)
  grep -v ':root' priv/templates/sigra.install/admin/sigra_admin.css | grep -c '#[0-9a-fA-F]'   (returns 0)
</verification>

<success_criteria>
- All 8 L1 components audited with findings documented in SUMMARY
- Raw hex #fff replaced with semantic token in source; generated copies byte-coherent
- mix test test/sigra/admin/components_test.exs exits 0
- No per-component @media (prefers-reduced-motion) blocks added (D-06 honored)
- Global reduced-motion block at ~:1467 confirmed present and sufficient
</success_criteria>

<output>
Create .planning/phases/206-l1-component-elevation-wave-a/206-02-SUMMARY.md when done
</output>
