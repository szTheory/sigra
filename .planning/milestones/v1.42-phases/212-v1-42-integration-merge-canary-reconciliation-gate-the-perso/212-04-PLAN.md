---
phase: 212-v1-42-integration-merge-canary-reconciliation-gate-the-perso
plan: 04
type: execute
wave: 3
depends_on:
  - 212-01
  - 212-02
  - 212-03
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - test/example/priv/playwright/snapshot-allowlist
  - test/example/priv/playwright/snapshot-allowlist-design
  - .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md
autonomous: false
requirements:
  - SC-4
  - GATE-01
  - FLOW-01
  - GATE-02
user_setup: []

must_haves:
  truths:
    - "PR #63's ship/v1.42-ci-gate-remediation branch is force-updated to local main's tip so PR #63 carries the FULL v1.42 backlog (388 commits incl. Phases 209/210/211), base still origin/main (D-12) — ship ⊊ local main, so the un-updated PR #63 would NOT deliver the gate-closing work"
    - "The prerequisite recapture PR (Plan 01/D-13) is merged to origin/main FIRST, and BOTH allowlists carry the legit drift slugs in the PR #63 diff (D-14) — so both snapshot lanes read green against origin/main before merge"
    - "PR #63 is taken out of DRAFT and all required lanes go green (fast_checks both snapshot lanes + admin_behavior 3 flow specs + generated_admin_playwright_smoke RUNS/PASSES + ci-gate), then merged to origin/main"
    - "v1.42 → shipped is NOT re-flipped (already committed locally at 4a5dd5f7 / Phase 211-05, rides in the backlog); at merge time origin/main reflects shipped only-as-of-the-merge (atomic = honest), and STATE is reconciled (D-16)"
    - "POST-MERGE ONLY: BOTH allowlists reset to empty (D-03/D-14) and the stale phase-205 baseline-debt todo is deleted (D-11)"
  artifacts:
    - "ship/v1.42-ci-gate-remediation force-updated to local main's tip (388 commits, base origin/main)"
    - "origin/main containing the merged full v1.42 backlog (PR #63 merged) with the already-committed shipped flag landing atomically"
    - ".planning/STATE.md reconciled to reflect merged/shipped v1.42 (post-merge)"
    - "test/example/priv/playwright/snapshot-allowlist + snapshot-allowlist-design reset to comment-only steady state (post-merge, D-03/D-14)"
  key_links:
    - "ship force-update + recapture-PR-merge (prerequisites) ↔ PR #63's two snapshot lanes reading green vs origin/main — the merge is blocked until both land"
    - "gate greenness (Plans 01/02/03) ↔ PR #63 fast_checks + ci-gate on GitHub — merge is blocked until all required lanes are green"
    - "merge-landed ↔ STATE reconcile + both allowlist resets — strict ordering: NOTHING resets before merge (D-03/D-14); the shipped flag is NOT re-flipped, it rides in the backlog (D-16)"
---

<objective>
Close SC-4 (merge + honest status) under STRICT, CORRECTED ORDERING. The topology audit
established the branch chain is LINEAR: origin/main (old canary bytes 9743e2d4, lacks the
WCAG fix) → +314 commits → ship/v1.42-ci-gate-remediation (PR #63 head, WCAG bytes) → +74
commits → local main (same WCAG bytes). So ship ⊊ local main: PR #63 as-it-stands stops at
Phase 208.1 and does NOT contain the Phase 209/210/211 work that actually closes the gates
(the admin_checkpoint_recapture job 272e187c, the canary re-designation rationale 3d129bb2,
the FLOW-01 ledger flips 4fc936f8, the board-mg-* design recapture f5833b0b, and the
already-committed premature v1.42→shipped flip 4a5dd5f7). Merging PR #63 as-it-stood would
NOT deliver the gates — so the merge vehicle must first grow to the full backlog.

Corrected ordering (D-12/D-13/D-14/D-16 — supersedes D-10's stale "push local main" wording):
  1. FORCE-UPDATE ship/v1.42-ci-gate-remediation → local main's tip so PR #63 carries the
     FULL v1.42 backlog (388 commits), base still origin/main (D-12). This grows PR #63's
     scope 314 → 388; it stays the single reviewed merge vehicle.
  2. LAND the prerequisite recapture PR to origin/main (Plan 01 / D-13) so origin/main carries
     the WCAG-fixed canary bytes and PR #63's canary lane reads byte-green.
  3. Confirm BOTH allowlists (checkpoint + design) carry the legit drift slugs in the PR #63
     diff (Plan 01 / D-02/D-14) — this is already in the diff via Plan 01; verify it here.
  4. Take PR #63 out of DRAFT.
  5. Drive fast_checks (BOTH snapshot lanes) + admin_behavior (3 flow specs) +
     generated_admin_playwright_smoke (RUNS/PASSES) + ci-gate green on GitHub.
  6. Merge PR #63 to origin/main.
  7. POST-MERGE ONLY: reset BOTH allowlists to empty (D-03/D-14); delete the stale phase-205
     board-baseline todo (D-11); reconcile STATE (D-16); optionally backfill a Phase 208
     VERIFICATION.md traceability pointer (D-11).

Honest status (D-16): the v1.42→shipped flag is ALREADY committed locally at 4a5dd5f7 and
rides in the backlog. Do NOT re-flip it. Because the full backlog merges atomically via PR
#63, the flag lands EXACTLY when the merge lands — so origin/main reflects "shipped" only
as-of-the-merge, which is honest. Verify this at merge time and reconcile STATE accordingly.

Purpose: turn "every phase verified vs HEAD" into "the milestone is genuinely integrated,
gated, merged, and honestly flagged" — via the full-backlog merge vehicle, not the stale PR.
Output: force-updated ship branch + merged origin/main + reconciled STATE + reset allowlists.

NOTE (autonomous: false): the ship force-update (force-push to a remote PR branch), the
recapture-PR review/merge (Plan 01's checkpoint, a prerequisite here), un-drafting PR #63,
watching CI go green, and the merge are inherently human/GitHub-side. The executor performs
the git/gh operations it can and PAUSES at the blocking human checkpoints below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-CONTEXT.md
@.planning/v1.42-MILESTONE-AUDIT.md
</context>

<artifacts_this_phase_produces>
This plan creates/modifies:
- `ship/v1.42-ci-gate-remediation` (remote PR branch) — FORCE-UPDATED to local main's tip
  (388 commits, base origin/main) so PR #63 carries the full v1.42 backlog (D-12).
- `origin/main` — receives the merged PR #63 (the full v1.42 ADMIN-DS-ELEVATION + CI-gate
  remediation backlog, incl. the already-committed shipped flag at 4a5dd5f7 landing
  atomically with the merge).
- `.planning/STATE.md` — milestone/status/position reconciled to merged+shipped v1.42
  (post-merge), stale "PR #63 (draft) open" / "N commits ahead unpushed" prose removed.
- `test/example/priv/playwright/snapshot-allowlist` + `snapshot-allowlist-design` — BOTH
  reset to comment-only steady state (the slug lines from Plan 01 removed) AFTER merge
  (D-03/D-14). Intentionally NOT part of the merged PR diff (the merged diff must contain
  the slugs so the drift was reviewable).
- `.planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md` — DELETED
  (its board-cfg PNGs now exist; 84 board files present) (D-11).
- (optional) a Phase 208 VERIFICATION.md traceability pointer noting Phase 208's work is
  covered by 208.1 + 210-02 (pointer, not re-verification) (D-11).

This plan does NOT re-flip the ROADMAP v1.42 shipped flag (already committed at 4a5dd5f7,
rides in the backlog — D-16); it only verifies the flag lands atomically and reconciles STATE.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm gates green locally + force-update ship → local main's tip (D-12)</name>
  <read_first>
    - .planning/phases/212-.../212-01-SUMMARY.md, 212-02-SUMMARY.md, 212-03-SUMMARY.md (confirm all three gate plans completed and their local verifications passed; 212-01 must confirm the recapture PR merged to origin/main)
    - .planning/phases/212-.../212-CONTEXT.md <planning_corrections> (D-12 topology: ship ⊊ local main; ship stops at 208.1, local main is +74 commits including the gate-closing work 209/210/211)
    - .planning/v1.42-MILESTONE-AUDIT.md §81/§158 (the premature-shipped-flag finding this plan must not repeat)
  </read_first>
  <files>(git operations + verification — no tracked file edits in this task)</files>
  <action>
    First re-confirm the three gates locally, in order:
      - `git fetch origin main` then `bash scripts/ci/snapshot-canary-guard.sh --base origin/main`
        exits 0 (checkpoint lane, GATE-01) AND the design-lane invocation
        (SNAP_DIR=admin-design + --allowlist snapshot-allowlist-design + --canary board-notice)
        exits 0 (D-04). Both depend on Plan 01's recapture PR having merged to origin/main.
      - `grep` confirms the 3 admin-flow specs are in ci.yml's admin_behavior step (FLOW-01)
        and the generated_admin_playwright_smoke `if:` is branch-scoped to
        ship/v1.42-ci-gate-remediation (GATE-02).
    Then FORCE-UPDATE the PR #63 branch to local main's tip (D-12). PR #63 as-it-stands (ship
    at Phase 208.1) does NOT contain 209/210/211, so it would not deliver the gates. Fast-forward
    is impossible (ship ⊊ main but the PR branch ref must point at main's tip), so this is a
    deliberate force-update of the ship branch to main's tip, base still origin/main:
      - Verify the topology first: `git merge-base --is-ancestor ship/v1.42-ci-gate-remediation main`
        must succeed (ship is an ancestor of local main — the linear-chain premise). If it does
        NOT (histories diverged), STOP and surface it; do not force-push over unexpected history.
      - Then update the PR branch: `git push origin main:ship/v1.42-ci-gate-remediation --force-with-lease`
        (push local main's tip onto the remote ship branch). Use --force-with-lease, not a bare
        --force, so a concurrent update on the remote ship branch aborts the push rather than
        clobbering it.
    Do NOT push local main directly to origin/main (rejected in D-13: it would void PR #63 and
    bypass review of the 74 commits). This task does NOT un-draft PR #63 and does NOT flip any
    status flag.
  </action>
  <verify>
    <automated>git fetch origin main -q; bash scripts/ci/snapshot-canary-guard.sh --base origin/main; echo "checkpoint-exit=$?"; SNAP_DIR="test/example/priv/playwright/tests/admin-design.spec.ts-snapshots" bash scripts/ci/snapshot-canary-guard.sh --base origin/main --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice; echo "design-exit=$?"; grep -q 'tests/admin-flow-platform-admin.spec.ts' .github/workflows/ci.yml && grep -q "ship/v1.42-ci-gate-remediation" .github/workflows/ci.yml && git merge-base --is-ancestor ship/v1.42-ci-gate-remediation main && echo "topology-ok"</automated>
  </verify>
  <acceptance_criteria>
    - BOTH snapshot lanes exit 0 vs origin/main (checkpoint + design).
    - The 3 flow specs and the branch-scoped smoke `if:` are present in ci.yml.
    - ship/v1.42-ci-gate-remediation is an ancestor of local main (linear-chain premise holds).
    - The remote ship branch is force-updated (with --force-with-lease) to local main's tip, or the divergence/rejection is surfaced, not clobbered.
  </acceptance_criteria>
  <done>Gates confirmed green locally (both lanes); PR #63's branch now carries the full 388-commit v1.42 backlog; ready to un-draft.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Un-draft PR #63 and drive all required lanes green on GitHub (D-12 ordering steps 3-5)</name>
  <action>Confirm the prerequisite recapture PR (Plan 01/D-13) has merged to origin/main and PR #63 now shows the full 388-commit backlog with both allowlists' slugs in its diff. Take PR #63 out of draft (`gh pr ready 63` or GitHub UI) and observe the required lanes until all are green, then PAUSE for human confirmation of live GitHub state. Do NOT merge in this step.</action>
  <what-built>
    The three gates are wired and green locally (both snapshot lanes), and PR #63's branch is
    force-updated to local main's tip (388 commits). The prerequisite recapture PR (Plan 01)
    must already be merged to origin/main. PR #63 now needs to be taken out of DRAFT and driven
    green on GitHub — steps that require live GitHub state confirmation.
  </what-built>
  <how-to-verify>
    1. Confirm the recapture PR (Plan 01 / D-13) is MERGED to origin/main (origin/main carries
       the WCAG-fixed impersonation-banner mobile bytes). If not, STOP — the canary lane on
       PR #63 will be red until it merges.
    2. Confirm PR #63 now shows the full backlog (388 commits) and its diff contains BOTH
       allowlist edits (snapshot-allowlist: 4-5 checkpoint slugs; snapshot-allowlist-design:
       15 board slugs).
    3. Take PR #63 out of draft (`gh pr ready 63`, or the human does it in the GitHub UI).
       Confirm PR #63 shows "Ready for review", not "Draft".
    4. Watch the required lanes on PR #63:
       - `fast_checks` — BOTH the "Snapshot drift guard (canary allowlist)" step AND the
         "Snapshot drift guard — design lane" step are green (GATE-01 / D-04).
       - `example_playwright_smoke` — the admin_behavior step ran the 3 admin-flow specs green
         (FLOW-01).
       - `Generated admin Playwright smoke` — shows as RUN (not skipping) and PASSED via
         admin-acceptance-smoke.sh --test all (GATE-02).
       - `ci-gate` — green.
       Use `gh pr checks 63` / `gh run list` to observe. If the accepted-known NoopTest shard-2
       log-capture flake flares (Phase 211 D-05), re-run that job alone — not a gate failure.
    5. Confirm `golden_diff_test.exs` (installer↔example byte-parity) is green — do NOT
       regenerate the golden fixture to absorb phx_new drift (SEED-004 pins 1.8.7).
    6. Confirm every required release lane is green and the PR is mergeable.
  </how-to-verify>
  <resume-signal>Type "gates green on PR #63" (with a link/run-id) once the recapture PR is merged, PR #63 carries the full backlog + both allowlist edits, all required lanes are green, and the PR is out of draft and mergeable — or describe which lane is red.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Merge PR #63 to origin/main (D-12 ordering step 6 — point of no return)</name>
  <action>Merge PR #63 into origin/main (`gh pr merge 63 --merge` per repo policy, or GitHub UI), then PAUSE for human confirmation that the merge landed and that origin/main now reflects shipped only-as-of-the-merge. Do NOT touch STATE/allowlists in this step.</action>
  <what-built>
    All required lanes are green and PR #63 (full 388-commit backlog) is mergeable. The merge
    itself is the point-of-no-return integration step and requires human confirmation before
    the STATE reconcile and allowlist resets.
  </what-built>
  <how-to-verify>
    1. Merge PR #63 into origin/main (`gh pr merge 63 --merge` per repo merge policy, or the
       human merges in the UI). Confirm PR #63 shows "Merged".
    2. Confirm origin/main now contains the full v1.42 backlog + the three gate fixes:
       `git fetch origin main` then `git log origin/main` shows the merge and the 209/210/211
       commits (272e187c / 3d129bb2 / 4fc936f8 / f5833b0b) and the shipped-flag commit 4a5dd5f7.
    3. HONEST-STATUS CHECK (D-16): confirm origin/main reflects "v1.42 shipped" ONLY now — i.e.
       the shipped flag (4a5dd5f7) arrived atomically WITH this merge, not before. Before the
       merge, origin/main must NOT have carried the shipped flag. This is the D-10/D-16 guard
       against the premature-flag mistake.
    4. Do NOT proceed to the STATE reconcile or allowlist resets until the merge is confirmed
       landed.
  </how-to-verify>
  <resume-signal>Type "PR #63 merged" once the merge has landed on origin/main and origin/main reflects shipped only-as-of-the-merge, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconcile STATE to merged/shipped v1.42 (D-16 — POST-MERGE ONLY; do NOT re-flip the shipped flag)</name>
  <read_first>
    - .planning/STATE.md frontmatter (milestone: v1.42, status, current_phase, progress) and Current Position / Next prose
    - .planning/ROADMAP.md v1.42 milestone status line (already flipped to shipped at 4a5dd5f7 / Phase 211-05 — DO NOT re-flip; the merge landed it atomically)
    - .planning/v1.42-MILESTONE-AUDIT.md "Recommended Closure" (the honest-completion definition)
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    ONLY after the merge checkpoint confirmed PR #63 merged and origin/main reflects shipped:
    - Do NOT re-flip ROADMAP v1.42 → shipped: it was already committed at 4a5dd5f7 (Phase
      211-05) and rode into origin/main atomically with the merge (D-16). Verify it reads
      shipped on the merged origin/main and leave it. If ROADMAP's Phase 212 plan checklist
      entries are not yet checked off by the backlog, check them off now with a SCOPED edit
      (touch only the Phase 212 block — do NOT rewrite the whole ROADMAP, preserve every other
      phase entry).
    - STATE.md: reconcile frontmatter (status → the post-ship state, current_phase → 212,
      last_activity_desc → "v1.42 merged + shipped (PR #63)") and the Current Position / Next
      prose, removing the stale "PR #63 (draft) open" / "N commits ahead unpushed" lines now
      that both are resolved. Use scoped edits.
    Do NOT reconcile anything if the merge was not confirmed in the prior checkpoint.
  </action>
  <verify>
    <automated>grep -qi 'ship' .planning/ROADMAP.md && grep -q '212' .planning/STATE.md && python3 -c "import yaml; f=open('.planning/STATE.md').read(); yaml.safe_load(f.split('---')[1])"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP v1.42 reads shipped (verified, NOT re-flipped) and Phase 212 plans are checked off.
    - STATE.md frontmatter parses and reflects merged/shipped v1.42; stale draft/unpushed lines removed.
    - No unrelated ROADMAP phase entries were altered.
  </acceptance_criteria>
  <done>STATE honestly reflects a merged, shipped v1.42; the shipped flag was NOT re-flipped (it landed atomically with the merge, D-16).</done>
</task>

<task type="auto">
  <name>Task 3: Post-merge bookkeeping sweeps — reset BOTH allowlists (D-03/D-14) + delete stale todo (D-11)</name>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist (holds the 4-5 checkpoint slug lines added by Plan 01; header: "Reset to empty once the re-recording PR merges")
    - test/example/priv/playwright/snapshot-allowlist-design (holds the 15 design slug lines added by Plan 01; same reset discipline)
    - .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md (the stale todo; its board-cfg PNGs now exist — 84 board files present, D-11)
    - .planning/phases/208-.../ (check whether a 208-VERIFICATION.md pointer is worth backfilling — optional, per D-11)
  </read_first>
  <files>test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/snapshot-allowlist-design, .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md</files>
  <action>
    POST-MERGE steady-state discipline (only after Task 2 confirmed the merged status):
    - Reset test/example/priv/playwright/snapshot-allowlist back to comment-only (remove the
      4-5 checkpoint slug lines added in Plan 01), restoring the empty steady state so the next
      PR's real drift on those slugs can't pass silently (D-03). Keep the comment header verbatim.
    - Reset test/example/priv/playwright/snapshot-allowlist-design back to comment-only (remove
      the 15 board slug lines added in Plan 01) (D-14/D-03). Keep the comment header verbatim.
      Note: BOTH resets are follow-up changes on origin/main AFTER the merge — intentionally NOT
      part of the merged PR diff (the merged diff must contain the slugs so the drift was reviewable).
    - Delete .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md
      (git rm) — its board-cfg PNGs now exist (84 board files present), so the debt is resolved (D-11).
    - Optionally backfill a short Phase 208 VERIFICATION.md traceability pointer stating its work
      is covered by 208.1 + 210-02 (a pointer, not re-verification) — skip if it adds churn without value.
  </action>
  <verify>
    <automated>test $(grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -c .) -eq 0 && test $(grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -c .) -eq 0 && test ! -f .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md</automated>
  </verify>
  <acceptance_criteria>
    - BOTH allowlists are back to comment-only (0 slug lines each) — steady state restored.
    - The stale phase-205 baseline-debt todo no longer exists in pending/.
    - Both header comments preserved.
  </acceptance_criteria>
  <done>Steady-state discipline restored on both allowlists and stale bookkeeping swept; v1.42 is honestly closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main → remote ship branch (force-update) | Publishing the full backlog onto PR #63's branch; a bare force-push could clobber a concurrent remote update — --force-with-lease guards this. |
| PR #63 → origin/main (merge) | The integration point; merging with a skipped/false-green gate would ship unproven parity, or merging the STALE PR #63 would ship without the gate-closing 209/210/211 work. |
| planning bookkeeping → honest status | The shipped flag must land atomically with the merge, never before (the audit's premature-flag finding). |

## STRIDE Threat Register

Integration/merge + bookkeeping; no runtime attack surface. Threats are process/integrity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-09 | Repudiation (dishonest status) | ROADMAP shipped flag | high | mitigate | D-16: the shipped flag is NOT re-flipped — it rides in the backlog (4a5dd5f7) and lands atomically with the merge. The merge-confirmation checkpoint (Task before Task 2) verifies origin/main reflects shipped only-as-of-the-merge before STATE is reconciled. |
| T-212-10 | Spoofing (false-green merge) | ci-gate skipped-need | high | mitigate | GATE-02 (Plan 03) makes the runtime smoke RUN on PR #63 so ci-gate reads a real result; the human-verify checkpoint confirms all required lanes (both snapshot lanes incl.) green before merge. |
| T-212-11 | Spoofing (stale merge vehicle) | PR #63 branch scope | high | mitigate | D-12: ship is force-updated to local main's tip so PR #63 carries the full 388-commit backlog; Task 1 verifies ship is an ancestor of main before force-pushing, and the un-draft checkpoint verifies PR #63 shows the full backlog + both allowlist edits. Merging the stale 314-commit PR is prevented. |
| T-212-12 | Tampering (history clobber) | remote ship force-update | high | mitigate | Task 1 uses --force-with-lease (not a bare --force) and pre-verifies ancestry; a concurrent remote update aborts the push rather than clobbering it. Local main is never force-pushed to origin/main (D-13 rejected push-first). |
| T-212-13 | Tampering (silent future drift) | both snapshot allowlists | medium | mitigate | D-03/D-14 post-merge reset to empty on BOTH lanes restores the tripwires so a later PR's real drift on those slugs fails the guards. |
| T-212-SC | Tampering | supply-chain / installs | low | accept | This phase adds no npm/pip/cargo installs and no new runtime deps; the generated-host smoke uses the already-pinned phx_new 1.8.7 (SEED-004). No new package legitimacy surface. |
</threat_model>

<verification>
- BOTH snapshot lanes exit 0 vs origin/main before merge (checkpoint + design).
- ship/v1.42-ci-gate-remediation force-updated to local main's tip (388 commits); ancestry verified pre-push.
- The prerequisite recapture PR (Plan 01/D-13) merged to origin/main before PR #63 is driven green.
- PR #63 out of draft; all required lanes (incl. RUN generated-host smoke + both snapshot lanes) green; ci-gate green.
- PR #63 merged to origin/main; the shipped flag (4a5dd5f7) landed atomically — NOT re-flipped (D-16).
- Post-merge: BOTH allowlists reset to empty; stale phase-205 todo deleted.
</verification>

<success_criteria>
SC-4 satisfied: ship force-updated to the full backlog, the recapture PR landed on origin/main,
PR #63 driven green (both snapshot lanes + flows + runtime smoke) and merged to origin/main, the
shipped flag landing atomically (not re-flipped, D-16), STATE reconciled, and BOTH allowlists
reset. Honest status — no premature shipped flag, no stale merge vehicle.
</success_criteria>

<output>
Create `.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-04-SUMMARY.md` when done
</output>
