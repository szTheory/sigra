---
phase: 210-remaining-cell-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - PAGE-03
  - FLOW-01

must_haves:
  truths:
    - "SC-1 (PAGE-03): The user-sessions L3 row (~:89) reads bare 2 and its Evidence column adds the three documented-as-manual reviewed clauses (motion-tokens / density-rhythm / target-size) plus content-equivalence: N/A, mirroring the sibling user-show-live row (~:88); the existing user-sessions checkpoint-slug + admin-modal-interaction.spec.ts (axe-while-open + 7 APG confirm-dialog gates) + glossary-clean citations are preserved"
    - "SC-2+SC-3 (FLOW-01): The 3 flow-* L4 rows (~:93-95) read bare 2, each citing the Phase-209 persona roll-up .planning/v1.42-PERSONA-JTBD-PANEL.md plus the per-surface docs under .planning/uat-evidence/v1.42-persona-jtbd/ carrying that lens's verdicts; the existing admin-flow-*.spec.ts citations (happy/main-error/boundary, scope/return-context, keyboard, reduced-motion, theme-persistence) are preserved"
    - "D-06: Every flipped tier value is a bare integer 2 in column 4 — never 2* or any decorated value; a decorated integer breaks the awk -F'|' /^[012]$/ monotonic parse and silently drops the row from the guard"
    - "SC-4 (D-07): After the flip, scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 (all moves are 1→2 increases) and scripts/ci/quality-ledger-monotonic.test.sh stays green"
  artifacts:
    - guides/reference/admin-quality-ledger.md
  key_links:
    - "The monotonic guard reads column 4 with awk -F'|' and /^[012]$/ — bare 2 only; a decorated value loses forward-only regression protection"
    - "The user-show-live L3 row (~:88) is the copy-template for user-sessions (both own the confirm dialog); the three documented-as-manual reviewed clauses + content-equivalence: N/A mirror it exactly"
    - "The Persona-JTBD rubric binds each lens 1:1 to a flow cell via the roll-up (admin-persona-jtbd-rubric.md Lens Definitions ~:48-52); the flow-* Evidence resolves the Phase-209 persona-review-doc citation to the roll-up + the per-surface docs for that lens's entry-point surfaces"
  prohibitions:
    - statement: "D-02: No LiveView/source edits — no lib/sigra/admin/live/*.ex, no test/example/ copies, no Playwright baselines, no golden fixtures. This plan edits guides/reference/admin-quality-ledger.md ONLY"
      status: enforced
      verification: "git diff --stat shows only guides/reference/admin-quality-ledger.md modified"
    - statement: "D-04: No net-new per-flow persona doc is authored, and no unratified 'edge'-path assertion is added to the flow specs. 'edge' in ROADMAP SC-2 prose is NOT a scorecard L4 proxy (L4 add-ons = happy + main-error + boundary per admin-fractal-scorecard.md ~:108-110)"
      status: enforced
      verification: "No new files under .planning/uat-evidence/; no flow spec .ts file is modified (git diff --stat is ledger-only)"
    - statement: "D-03: user-sessions content-equivalence is genuinely N/A (scope-safe control table, not a desktop-table↔mobile-card pattern) — do NOT invent an assertUserResultEquivalence-style assertion for it"
      status: enforced
      verification: "grep '^| user-sessions ' cites 'content-equivalence: N/A' and does not cite assertUserResultEquivalence"
    - statement: "D-06: Column-4 tier value is bare 2 — never decorated"
      status: enforced
      verification: "grep -E '^\\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$' returns empty output"
    - statement: "D-08: No baseline recapture, no allowlist changes, no canary changes — no LiveView DOM changed, so all checkpoint/board-* PNGs stay byte-stable and both allowlists stay empty"
      status: enforced
      verification: "git diff --stat shows no .png files and no snapshot-allowlist* files modified"
---

<objective>
Flip 4 ledger cells in guides/reference/admin-quality-ledger.md to bare tier 2: the
user-sessions L3 page row (PAGE-03) and the 3 persona-flow L4 rows flow-platform-admin,
flow-support-investigator, flow-org-admin (FLOW-01). This is pure evidence/ledger authoring —
every Tier-2 automated proxy for these cells is already wired and green; this plan authors
the Evidence-column citations and flips the column-4 integers.

Purpose: Complete the L3 page pass (user-sessions is the last L3 row at tier 1) and the L4
flow pass (the 3 flow-* rows are the last L4 rows at tier 1). After this plan plus the sibling
GROUP-02 plan, every ledger cell reads bare 2 — the terminal all-Tier-2 state Phase 211
ratifies.

Output: guides/reference/admin-quality-ledger.md with user-sessions + 3 flow-* rows at bare
tier 2, each with an accurate Evidence string; monotonic guard green vs origin/main; guard
self-test green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/210-remaining-cell-elevation/210-CONTEXT.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
@guides/reference/admin-persona-jtbd-rubric.md
@.planning/v1.42-PERSONA-JTBD-PANEL.md
@scripts/ci/quality-ledger-monotonic.sh
</context>

## Artifacts this phase produces

No new symbols or files — this phase edits existing rows in guides/reference/admin-quality-ledger.md only. This plan flips 4 existing ledger rows (user-sessions + 3 flow-*) from tier 1 to bare tier 2 and expands their Evidence columns; it creates no net-new symbol, file, or persona doc.

<tasks>

<task type="auto">
  <name>Task 1: Flip the user-sessions L3 row to bare tier 2 mirroring the user-show-live sibling template (PAGE-03)</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — read the full Quality Ledger. The target is the single user-sessions L3 row (~:89, currently tier 1). The copy-template is the sibling user-show-live L3 row (~:88, already tier 2 — both own the confirm dialog). Also read the "Asserting Tier 2" / Parsing Rules section (~:14-54) explaining the bare-2 requirement and forbidden decorators.
  - guides/reference/admin-fractal-scorecard.md "Tier-2 Award-grade Add-on" (~:132-176) — the Documented-as-manual proxy vocabulary: motion-token conformance (no `transition: all`, references `--sg-motion-*` / `--sg-ease` tokens), density/whitespace rhythm (consistent `sg-stack--N` tier), target-size minimum (≥24×24 CSS px, dense-control suppression precedent).
  - .planning/uat-evidence/v1.42-persona-jtbd/user-sessions.md — records the two now-resolved tighten findings (H1 entity-name, revoke copy); read-only confirmation the surface is settled, no source edit needed.
  </read_first>

  <action>
  Use Edit (scoped replacement) on the user-sessions row only, matching its existing full cell text as the unique old_string. Never Write the full ledger file.

  Flip the tier column from 1 to the bare integer 2 (no asterisks, no decorators — the awk -F'|' parser requires /^[012]$/ after trimming).

  PRESERVE the existing citations verbatim: the admin-checkpoints user-sessions checkpoint slug (3 projects × toHaveScreenshot + axe), the axe-while-open + 7 APG focus-trap/restore gates (confirm dialog ownership) admin-modal-interaction.spec.ts citation, and glossary-clean: glossary_test.exs.

  EXPAND the Evidence column to append, mirroring the user-show-live sibling row (~:88), the three documented-as-manual "reviewed — …" clauses plus content-equivalence:
  - motion-tokens: reviewed — no `transition: all`; component transitions reference `--sg-motion-*` / `--sg-ease` tokens
  - density/rhythm: reviewed — `sg-stack--6` outer, `sg-stack--3` card inner (consistent with the sibling user-show-live cadence)
  - target-size: reviewed — all interactive targets ≥24×24 CSS px (documented-as-manual)
  - content-equivalence: N/A — the sessions table is a scope-safe control table, not a desktop-table↔mobile-card equivalence pattern (per D-03; do NOT cite assertUserResultEquivalence)

  Do NOT decorate the tier value. Do NOT edit any other row. Preserve table column spacing.
  </action>

  <verify>
    <automated>bash -c 'grep -E "^\| user-sessions " guides/reference/admin-quality-ledger.md | awk -F"|" "{t=\$4; gsub(/^ +| +\$/,\"\",t); print t}" | grep -xc 2'</automated>
  </verify>

  <acceptance_criteria>
  - The user-sessions row shows bare 2 in column 4: `grep -E '^\| user-sessions ' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}'` returns exactly `2`
  - The row cites all three documented-as-manual clauses: `grep -E '^\| user-sessions ' guides/reference/admin-quality-ledger.md | grep -c 'motion-tokens'` returns 1, `grep -c 'density/rhythm'` returns 1, `grep -c 'target-size'` returns 1
  - The row cites content-equivalence: N/A and does NOT cite assertUserResultEquivalence: `grep -E '^\| user-sessions ' guides/reference/admin-quality-ledger.md | grep -c 'content-equivalence: N/A'` returns 1; `grep -c 'assertUserResultEquivalence'` returns 0 (D-03)
  - The pre-existing citations are preserved: `grep -E '^\| user-sessions ' guides/reference/admin-quality-ledger.md | grep -c 'admin-modal-interaction.spec.ts'` returns >= 1 and `grep -c 'glossary_test.exs'` returns 1
  - No decorated tier values anywhere: `grep -E '^\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$'` returns empty output (D-06)
  - Only the user-sessions row changed in this task's diff (the 3 flow-* rows are still at 1 until Task 2)
  </acceptance_criteria>

  <done>The user-sessions L3 row reads bare 2 with the three documented-as-manual reviewed clauses (motion-tokens / density-rhythm / target-size) + content-equivalence: N/A appended, mirroring user-show-live; existing checkpoint + admin-modal-interaction + glossary-clean citations preserved; no decorators.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the 3 flow-* L4 rows to bare tier 2 citing the Phase-209 persona roll-up + per-surface docs (FLOW-01)</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — the 3 target rows are flow-platform-admin (~:93), flow-support-investigator (~:94), flow-org-admin (~:95), currently tier 1 with a single admin-flow-*.spec.ts citation. Also read the "Persona-JTBD Rubric (Cross-Reference)" section (~:97-102) noting the L4 flow cells are the ledger binding points for the rubric.
  - guides/reference/admin-persona-jtbd-rubric.md "Lens Definitions" (~:48-52) — the 1:1 lens↔flow-cell binding by entry point + intent: Platform admin (/admin triage) → flow-platform-admin; Support investigator (/admin/users/:id investigate) → flow-support-investigator; Org admin (/admin/organizations/:slug bound) → flow-org-admin.
  - .planning/v1.42-PERSONA-JTBD-PANEL.md — the roll-up each flow cell cites (D-04); the Panel Summary table (~:28-38) maps the 8 per-surface docs; all 8 are `actionable` (no kill/blocked).
  - guides/reference/admin-fractal-scorecard.md "L4 Flow Add-ons" (~:103-121) — the L4 add-on proxy vocabulary: Persona JTBD happy + main-error + boundary; scope/return-context preserved; full keyboard operability; calm reduced-motion; Light/Dark/System persists; deterministic fixture. NOTE: "edge" is NOT an L4 proxy (D-04) — do NOT introduce it.
  </read_first>

  <action>
  Use Edit (scoped replacement) on each of the 3 flow-* rows — one Edit call per row, using the row's existing cell text as the unique old_string. Never Write the full ledger file.

  For each row, flip the tier column from 1 to the bare integer 2 (no decorators; awk requires /^[012]$/).

  PRESERVE the existing admin-flow-*.spec.ts citation verbatim (it already asserts the full FLOW-01 checklist: happy/main-error/boundary paths, scope/return-context continuity, full keyboard operability, reduced-motion, theme persistence + reload).

  APPEND to each row's Evidence a "persona review doc (Phase 209)" citation resolving to the roll-up plus the per-surface docs for that lens's entry-point surfaces (per the Lens Definitions binding):
  - flow-platform-admin (triage, entry /admin): cite .planning/v1.42-PERSONA-JTBD-PANEL.md + the per-surface docs the triage lens scans — .planning/uat-evidence/v1.42-persona-jtbd/index-live.md, users-index-live.md, organization-live.md
  - flow-support-investigator (investigate, entry /admin/users/:id): cite the roll-up + .planning/uat-evidence/v1.42-persona-jtbd/user-show-live.md, user-sessions.md, audit-user-live.md, audit-index-live.md
  - flow-org-admin (bound, entry /admin/organizations/:slug): cite the roll-up + .planning/uat-evidence/v1.42-persona-jtbd/organization-live.md

  Each flow-cell citation MUST include the roll-up path v1.42-PERSONA-JTBD-PANEL.md. Note in each that the panel disposition is `actionable` — all findings resolved in-place / waived in Phase 209, no kill/blocked. Do NOT author any net-new per-flow persona doc. Do NOT add an "edge"-path assertion (D-04). Do NOT decorate the tier value.
  </action>

  <verify>
    <automated>bash -c 'grep -E "^\| flow-(platform-admin|support-investigator|org-admin) " guides/reference/admin-quality-ledger.md | awk -F"|" "{t=\$4; gsub(/^ +| +\$/,\"\",t); print t}" | grep -xc 2'</automated>
  </verify>

  <acceptance_criteria>
  - All 3 flow-* rows show bare 2 in column 4: the verify command returns 3
  - Each flow-* row cites the persona roll-up path: `grep -E '^\| flow-(platform-admin|support-investigator|org-admin) ' guides/reference/admin-quality-ledger.md | grep -c 'v1.42-PERSONA-JTBD-PANEL.md'` returns 3 (D-04)
  - Each flow-* row preserves its admin-flow spec citation: `grep -E '^\| flow-(platform-admin|support-investigator|org-admin) ' guides/reference/admin-quality-ledger.md | grep -c 'admin-flow-'` returns 3
  - No net-new "edge"-path assertion introduced: `grep -E '^\| flow-' guides/reference/admin-quality-ledger.md | grep -ci 'edge-path\|edge path assertion'` returns 0 (D-04)
  - No new persona doc authored: no new files under .planning/uat-evidence/ (git status shows no additions there)
  - No decorated tier values anywhere: `grep -E '^\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$'` returns empty output (D-06)
  </acceptance_criteria>

  <done>All 3 flow-* L4 rows read bare 2, each citing v1.42-PERSONA-JTBD-PANEL.md + the relevant per-surface docs and preserving its admin-flow-*.spec.ts citation; no net-new per-flow doc; no "edge" assertion; no decorators.</done>
</task>

<task type="auto">
  <name>Task 3: Run the monotonic guard + self-test to confirm forward-only, no regressions (D-07)</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — re-read after edits to confirm the 4 flipped rows (user-sessions + 3 flow-*) are bare 2 with expanded evidence and no row was accidentally decorated before running the guard.
  - scripts/ci/quality-ledger-monotonic.sh — confirm --base origin/main usage and that it parses column 4 with awk -F'|' and /^[012]$/; the PASS line reports a cell count.
  </read_first>

  <action>
  Run the monotonic guard against origin/main to confirm no tier decreased vs main (the 4 rows moved 1 → 2, allowed increases):
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main
  Expected: a PASS line, exit 0.

  Run the guard self-test to confirm the guard itself is still green:
  bash scripts/ci/quality-ledger-monotonic.test.sh
  Expected: exit 0.

  If the guard exits non-zero or the cell count is lower than expected, diagnose: a decorated tier value (2*, 2 ✓) makes a row invisible to the awk parse — find and replace with bare 2; or a row was accidentally decreased. Fix and re-run.

  Confirm no other file changed: git diff --stat should show only guides/reference/admin-quality-ledger.md.
  </action>

  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && bash scripts/ci/quality-ledger-monotonic.test.sh</automated>
  </verify>

  <acceptance_criteria>
  - `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 and emits a PASS line (D-07)
  - `bash scripts/ci/quality-ledger-monotonic.test.sh` exits 0 (guard self-test still green) (D-07)
  - `git diff --stat` shows only guides/reference/admin-quality-ledger.md modified — no source, no PNG, no allowlist, no canary (D-02, D-08)
  </acceptance_criteria>

  <done>Monotonic guard exits 0 vs origin/main; guard self-test green; only the ledger file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger edit → awk parse | A malformed tier column (e.g. "2*") silently removes the row from monotonic-guard forward-only regression protection |

No security-relevant surface: documentation-only change to guides/reference/admin-quality-ledger.md. No code, no data flow, no auth/authz/input-handling change, no trust-boundary change. This plan changes ZERO executable code (D-02).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-210-01 | Tampering | Ledger tier column format | low | mitigate | Acceptance criteria + Task 3 verify that no decorated tier value exists (grep -vE '^[012]$' returns empty) and the monotonic guard exits 0; any invisible row is caught before merge |
| T-210-02 | Repudiation | Evidence string accuracy | low | accept | Evidence strings are human-curated, bounded by the user-show-live sibling template + the Phase-209 persona roll-up; the cited gates (admin-modal-interaction.spec.ts, admin-flow-*.spec.ts, glossary_test.exs) are independently runnable |
</threat_model>

<verification>
After all three tasks complete:
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main   (exits 0)
  bash scripts/ci/quality-ledger-monotonic.test.sh                 (exits 0)
  grep -E '^\| user-sessions ' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}'   (returns 2)
  grep -E '^\| flow-(platform-admin|support-investigator|org-admin) ' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -xc 2   (returns 3)
  grep -E '^\| flow-' guides/reference/admin-quality-ledger.md | grep -c 'v1.42-PERSONA-JTBD-PANEL.md'   (returns 3)
  git diff --stat   (only guides/reference/admin-quality-ledger.md)
</verification>

<success_criteria>
- user-sessions L3 row at bare tier 2 with the three documented-as-manual reviewed clauses + content-equivalence: N/A (PAGE-03)
- 3 flow-* L4 rows at bare tier 2, each citing v1.42-PERSONA-JTBD-PANEL.md + relevant per-surface docs, preserving admin-flow-*.spec.ts citations (FLOW-01)
- No decorated tier values anywhere in the ledger (D-06)
- No net-new per-flow persona doc; no "edge" assertion (D-04); no source/baseline/allowlist/canary changes (D-02, D-08)
- Monotonic guard exits 0 vs origin/main; guard self-test green (D-07)
</success_criteria>

<output>
Create .planning/phases/210-remaining-cell-elevation/210-01-SUMMARY.md when done
</output>
