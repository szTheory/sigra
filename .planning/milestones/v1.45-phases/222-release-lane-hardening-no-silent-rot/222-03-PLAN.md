---
phase: 222-release-lane-hardening-no-silent-rot
plan: 03
type: execute
wave: 3
depends_on: [222-02]
files_modified:
  - MAINTAINING.md
  - test/sigra/planning/phase_222_release_lane_hardening_test.exs
autonomous: false
requirements: [HARD-02]
must_haves:
  truths:
    - "the release-please publish path is proven green via hex-publish.yml dry_run=true against tag v1.3.0 (compile + test + package + provenance) with no Hex write"
    - "an operator can find, in MAINTAINING.md, the recovery/manual-dispatch runbook: the hex-publish.yml inputs (tag + release_version + dry_run), when to use it vs release-please auto-publish, how to read a gate-ci-green timeout, and how to red-probe the new loud signal"
    - "the runbook cross-references docs/release-runbook-v1-0.md (the canonical runbook) rather than duplicating the release matrix"
  artifacts:
    - MAINTAINING.md
    - test/sigra/planning/phase_222_release_lane_hardening_test.exs
  key_links:
    - "the runbook documents the notify tracking Issue (label release-lane-rot) produced by Plan 02"
    - "the runbook's dry-run command targets hex-publish.yml with tag=v1.3.0 release_version=1.3.0 dry_run=true"
    - "the runbook cross-refs docs/release-runbook-v1-0.md and follows the D-14 forced-failure-probe precedent"
user_setup:
  - service: github-actions
    why: "The hex-publish.yml dry_run=true proof is an operator-dispatched workflow run (requires authenticated gh with workflow_dispatch); Hex writes stay operator-gated per 221 D-16"
---

<objective>
Close HARD-02's readiness evidence and recovery documentation (D-06.1 + D-08). Prove the release-please
publish path is green without cutting a throwaway version (Jon declined the live v1.3.1 bump, D-06) by
running `hex-publish.yml` with `dry_run=true` against the already-shipped tag `v1.3.0` — a full
compile + Postgres-backed `mix test` + `mix hex.build --unpack` inspection + `mix hex.publish --dry-run`
with every real-Hex-write step short-circuited (guards verified in 222-RESEARCH.md Finding 3). Then
document the operator recovery/diagnosis runbook in MAINTAINING.md.

Per 222-RESEARCH.md Finding 5: insert the runbook as a new subsection immediately after the "Recovery /
one-off publish:" line (MAINTAINING.md:276), in the copy-paste `gh workflow run` style of the
"Forced-failure probe runbook (D-14)" precedent (:178-198). Cross-reference the canonical
`docs/release-runbook-v1-0.md` (:262) — do NOT create a new top-level doc and do NOT duplicate the
release matrix (the file's anti-duplication guidance + D-08).

Purpose: prove auto-publish readiness without a churned release, and give the operator a documented
path to publish or diagnose a stranded release.
Output: a captured dry-run readiness-evidence run link; a MAINTAINING.md recovery runbook subsection;
structural coverage of the runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/222-release-lane-hardening-no-silent-rot/222-CONTEXT.md
@.planning/phases/222-release-lane-hardening-no-silent-rot/222-RESEARCH.md
@.planning/phases/222-release-lane-hardening-no-silent-rot/222-VALIDATION.md
</context>

<artifacts_produced>
NEW symbols this plan introduces (exclude from drift/dead-symbol checks):
- MAINTAINING.md subsection heading `### Release-lane rot signals & recovery (HARD-01/HARD-02)`
- new phase_222 test assertions covering the runbook (the test file itself is created in Plan 02)
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Prove the publish path green via hex-publish.yml dry_run=true against v1.3.0</name>
  <files>none (operator-dispatched CI run; Hex-write-side, not a repo edit)</files>
  <action>
    Operator-gated readiness proof (D-06.1, 221 D-16) — inherently CI/Hex-side, not a repo edit. The agent
    may dispatch (with authenticated `gh`) or hand the exact command to the operator:
    `gh workflow run "Hex publish (manual recovery)" -f tag=v1.3.0 -f release_version=1.3.0 -f dry_run=true`,
    then observe the run via `gh run watch`/`gh run view` and confirm it is GREEN with the real `Publish to
    Hex` + post-publish steps SKIPPED (no Hex write). Capture the run URL as HARD-02 readiness evidence in
    222-03-SUMMARY.md. Idempotency-safe and fully re-runnable.
  </action>
  <what-built>
    The release-please publish path is exercised end-to-end without a Hex write. `hex-publish.yml`
    (inputs `tag`, `release_version`, `dry_run`) runs the full compile + Postgres-backed `mix test` +
    `mix hex.build --unpack` package inspection + `mix hex.publish --dry-run`; the idempotency check, the
    real publish step, and all post-publish verify/evidence steps are guarded off when `dry_run=true`
    (222-RESEARCH.md Finding 3). This is HARD-02's "prove the publish path is green" readiness evidence
    (D-06.1) and respects operator-gated Hex writes (221 D-16). The proof is idempotency-safe and fully
    re-runnable — the same artifact serves as the repeatable proof the Plan-03 runbook documents.
  </what-built>
  <how-to-verify>
    1. From an authenticated `gh` session on the repo, dispatch the dry-run against the shipped tag:
       `gh workflow run "Hex publish (manual recovery)" -f tag=v1.3.0 -f release_version=1.3.0 -f dry_run=true`
    2. Watch the run: `gh run list --workflow hex-publish.yml --limit 1` then `gh run watch <id>` (or open the Actions URL).
    3. Confirm the run is GREEN and that: compile (warnings-as-errors) passed, `mix test` passed, the
       package inspection passed, and the `Dry run Hex publish` step ran while the `Publish to Hex` step and
       the post-publish `Verify version on Hex.pm` steps were SKIPPED (no real Hex write).
    4. Capture the run URL — it is recorded in 222-03-SUMMARY.md as the HARD-02 readiness evidence.
  </how-to-verify>
  <verify>
    <automated>gh run list --workflow hex-publish.yml --limit 1 --json conclusion,databaseId,url</automated>
  </verify>
  <resume-signal>Type "approved" with the dry-run run URL, or describe the failure to triage.</resume-signal>
  <done>The hex-publish.yml dry_run=true run against v1.3.0 concluded green with no Hex write; the run URL is captured as HARD-02 readiness evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add the MAINTAINING.md recovery/manual-dispatch runbook subsection + phase_222 structural assertions</name>
  <files>MAINTAINING.md, test/sigra/planning/phase_222_release_lane_hardening_test.exs</files>
  <read_first>
    - MAINTAINING.md (the file being modified — "Release automation (default)" :266; "Recovery / one-off publish:" line :276; canonical runbook pointer docs/release-runbook-v1-0.md :262; the "Forced-failure probe runbook (D-14)" precedent :178-198 this subsection mirrors)
    - .github/workflows/hex-publish.yml (:11-24 — the `tag`/`release_version`/`dry_run` input descriptions the runbook documents verbatim)
    - test/sigra/planning/phase_222_release_lane_hardening_test.exs (the file being extended — created in Plan 02; mirror its read!/=~ style)
    - 222-RESEARCH.md Finding 5 (the exact insertion point + the four things the runbook must document)
  </read_first>
  <action>
    In `MAINTAINING.md`, add a new `###` subsection titled `### Release-lane rot signals & recovery
    (HARD-01/HARD-02)` immediately after the "Recovery / one-off publish:" line (:276), in the copy-paste
    `gh workflow run` style of the D-14 forced-failure-probe runbook (:178-198). Document, concisely and
    without duplicating the canonical matrix: (1) the `hex-publish.yml` manual dispatch — the verbatim
    `gh workflow run "Hex publish (manual recovery)" -f tag=<tag> -f release_version=<version> -f
    dry_run=true|false` command with the `tag` + `release_version` + `dry_run` inputs, and when to use it
    vs release-please auto-publish; (2) how to read a `gate-ci-green` timeout (the ~30-min stall =
    release-please waiting for a green ci-gate on the release SHA) and where the new tracking Issue (label
    `release-lane-rot`, opened by Plan 02's notify jobs) surfaces the failure; (3) how to red-probe the new
    loud signal, mirroring the D-14 probe pattern (force a failure on main / release-please and confirm the
    Issue opens); (4) a cross-reference to `docs/release-runbook-v1-0.md` (:262) as the canonical runbook —
    do NOT duplicate the release gate matrix, and do NOT create a new top-level doc (D-08 + the file's
    anti-duplication guidance). Then extend `phase_222_release_lane_hardening_test.exs` (mirror phase_147's
    read!/=~ style) asserting MAINTAINING.md contains: the new subsection heading, the `hex-publish.yml`
    dispatch inputs (`tag`, `release_version`, `dry_run`), the `release-lane-rot` signal reference, and the
    `docs/release-runbook-v1-0.md` cross-reference.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_222_release_lane_hardening_test.exs && grep -q 'docs/release-runbook-v1-0.md' MAINTAINING.md && grep -q 'dry_run' MAINTAINING.md</automated>
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md has the new subsection with the hex-publish.yml dispatch command/inputs, the timeout-reading + tracking-Issue guidance, the red-probe steps, and the canonical cross-reference — no matrix duplication, no new top-level doc.
    - phase_222 structural test passes, including the new runbook assertions.
  </acceptance_criteria>
  <done>The operator has a documented, tested path to publish or diagnose a stranded release, cross-linked to the canonical runbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator dispatch -> hex-publish.yml (HEX_API_KEY) | The dry-run proof runs in a job that has access to the Hex write key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-222-03-01 | Tampering | accidental real Hex publish during the "proof" | medium | mitigate | The dispatch sets `dry_run=true`; hex-publish.yml guards the real `Publish to Hex` step and all post-publish steps on `dry_run != true` (verified 222-RESEARCH.md Finding 3), and the idempotency check would skip a re-publish of the already-shipped v1.3.0 anyway |
| T-222-03-02 | Information Disclosure | HEX_API_KEY exposure in the dry-run run | low | mitigate | `dry_run=true` never invokes `mix hex.publish --yes`; the key is only referenced by guarded steps; GitHub masks the secret in logs. No new secret handling is added |
| T-222-03-SC | Tampering | package installs | low | accept | No new package installs authored in this plan — the dispatched workflow uses the already-shipped hex-publish.yml; this plan edits MAINTAINING.md + an ExUnit test only |
</threat_model>

<verification>
- The operator dry-run run link (captured in the checkpoint) is the HARD-02 readiness evidence: publish path green, no Hex write.
- `mix test test/sigra/planning/phase_222_release_lane_hardening_test.exs` proves the runbook subsection + inputs + cross-reference exist.
- The wiring trace (release_created -> gate-ci-green -> publish-hex, confirmed in 222-RESEARCH.md Finding 4) + the now-green ci-gate on main + the already-shipped v1.3.0 stand as the remaining readiness evidence (D-06.2). Per the operator-truth D-05 (v1.2.0/v1.3.0 shipped via the manual hex-publish.yml path, so release-please auto-publish is unproven end-to-end and proven to stall silently), this evidence — not a churned throwaway release — is what makes the OR-branch honest; recorded in the SUMMARY.
</verification>

<success_criteria>
Maps to ROADMAP SC2 (HARD-02 auto-publish proven-or-fails-loudly — the dry-run proves the publish path is
green; Plan 02's notify makes the next real release fail loudly if blocked) and SC3 (the recovery /
manual-dispatch runbook is documented so an operator can publish or diagnose a stranded release).
</success_criteria>

<output>
Create `.planning/phases/222-release-lane-hardening-no-silent-rot/222-03-SUMMARY.md` when done
</output>
