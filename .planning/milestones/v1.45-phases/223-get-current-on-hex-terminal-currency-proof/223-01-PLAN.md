---
phase: 223-get-current-on-hex-terminal-currency-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md
  - .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md
  - .planning/todos/done/2026-07-03-hex-retire-stray-1-20-0.md
autonomous: false
requirements: [PROOF-01]
must_haves:
  truths:
    - "Hex latest_stable_version drops to 1.3.0 and stray 1.20.0 appears in retirements, verified via live curl to https://hex.pm/api/packages/sigra"
  artifacts:
    - "223-01-SUMMARY.md recording verbatim pre-retire and post-retire curl|jq output plus the reversible --unretire fallback"
    - ".planning/todos/done/2026-07-03-hex-retire-stray-1-20-0.md (moved from pending/ only after the retire is verified)"
  key_links:
    - "operator dashboard-minted Hex write key -> mix hex.retire sigra 1.20.0 -> hex.pm registry latest_stable_version = 1.3.0"
---

<objective>
Un-defer and execute the retire of the stray Hex `1.20.0` (D-01), the HARD
prerequisite (D-02) for both PUB-05 (adopter resolution) and PROOF-01 (trust
bundle). Live Hex today reports `latest_stable_version: 1.20.0` with empty
`retirements: {}`, so `{:sigra, "~> 1.0"}` resolves to the stray. Both remaining
requirements are literally unsatisfiable until this lands.

This plan is `autonomous: false`: the retire itself requires an operator to mint
a hex.pm dashboard API write key and run an interactive Hex write command (Hex 2.5
has no CLI key-gen and its device-flow token is not authorized to retire). The
agent's role is the pre-state snapshot, guiding/verifying the operator step, and
the post-state verification.

Purpose: Make the real GA (`1.3.0`) the resolved `latest_stable_version` so
adopter resolution and the trust bundle can be captured truthfully.
Output: Verified currency on Hex + 223-01-SUMMARY.md evidence + folded todo moved
to done/.

## Artifacts this phase produces (context)
- `223-PROOF.md` — release-currency trust bundle (produced in 223-03).
- `223-VERIFICATION.md` — standard verifier report (produced downstream by
  `/gsd-verify-phase`, not by this plan).
- Throwaway scratch resolution project (produced in 223-02; disposable, under
  `tmp/`, not committed).
- This plan produces: verified Hex currency + `223-01-SUMMARY.md` + the folded
  retire todo moved `pending/ -> done/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-CONTEXT.md
@.planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-retire live-Hex snapshot</name>
  <files>.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-CONTEXT.md (D-02 live re-query mandate; D-16 lineage from 221)
    - .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md (runbook + verify command at :53)
  </read_first>
  <action>
    Re-query live Hex immediately before the operator step (per D-02 / 221 D-16 —
    registry state drifts) and record the pre-state as evidence. Run
    `curl -s https://hex.pm/api/packages/sigra | jq '.latest_stable_version, .latest_version, .retirements'`
    and record the exact command and its verbatim output into a new
    223-01-SUMMARY.md under a "Pre-retire snapshot" heading. Confirm the pre-state
    shows `latest_stable_version` = `1.20.0` and empty `retirements` (the condition
    that makes the retire necessary). If Hex already shows `1.3.0` with `1.20.0`
    retired (state may have changed), record that and note the operator retire step
    is a no-op verification rather than an action — do not fabricate a pre-state.
  </action>
  <verify>
    <automated>test -f .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md &amp;&amp; grep -q 'latest_stable_version' .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    223-01-SUMMARY.md exists and contains the verbatim `curl … | jq` command and
    its observed output under a "Pre-retire snapshot" heading, reflecting the true
    current registry state.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator retires stray 1.20.0 via dashboard write key</name>
  <action>
    BLOCKING operator checkpoint (autonomous: false) — the retire cannot be
    automated (interactive Hex write-auth). The agent pauses here; the operator
    mints a hex.pm dashboard API write key and runs `mix hex.retire` per the
    steps below, then signals resume. See <how-to-verify> for exact commands.
  </action>
  <what-built>
    The agent has recorded the pre-retire live-Hex state. The retire itself cannot
    be automated: it needs interactive Hex write-auth. Hex 2.5 dropped CLI key-gen,
    and its device-flow token is NOT authorized to retire (returns "key not
    authorized for this action"). The remaining path (untried at 221 close, expected
    to work) is a dashboard-minted API write key.
  </what-built>
  <how-to-verify>
    Operator steps (from the folded runbook, D-01):
    1. Mint an API **write** key on the web dashboard: https://hex.pm/dashboard/keys
       (grant API / write permission). Hex 2.5 has no CLI key-gen.
    2. Run the retire with that key via env override (exact message + reason per D-01):
       `HEX_API_KEY=<key> mix hex.retire sigra 1.20.0 invalid --message "Published in error during dev cycle; not a real release — use 1.3.0+"`
    3. Confirm the command reports success (no "key not authorized" error).
    Reversible fallback if ever needed (D-03): `mix hex.retire sigra 1.20.0 --unretire`.
    Do NOT paste the key into any committed file, log, or SUMMARY — env var only.
  </how-to-verify>
  <resume-signal>Type "retired" once `mix hex.retire` reported success, or describe the error (e.g. "key not authorized") so the agent can advise.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify post-retire currency and fold the todo</name>
  <files>.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md, .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md, .planning/todos/done/2026-07-03-hex-retire-stray-1-20-0.md</files>
  <read_first>
    - .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md (Done-when at :58-62)
    - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md (pre-state from Task 1)
  </read_first>
  <action>
    Re-query live Hex after the operator retire and assert the currency flip. Run
    `curl -s https://hex.pm/api/packages/sigra | jq '.latest_stable_version, .retirements'`
    and record the exact command + verbatim output into 223-01-SUMMARY.md under a
    "Post-retire verification" heading. Assert BOTH conditions hold:
    `latest_stable_version` == `1.3.0` AND the `retirements` object contains a
    `1.20.0` key (reason `invalid`). Also record the reversible fallback command
    (`mix hex.retire sigra 1.20.0 --unretire`, D-03) in the SUMMARY. Only after both
    conditions verify, move the folded retire todo from
    `.planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md` to
    `.planning/todos/done/2026-07-03-hex-retire-stray-1-20-0.md` (git mv). If the
    assertion fails (e.g. operator hit "key not authorized"), STOP — do not move the
    todo, do not mark the plan complete; record the failure and the blocker in the
    SUMMARY so downstream plans (which depend on this) do not run against a stale
    registry.
  </action>
  <verify>
    <automated>curl -s https://hex.pm/api/packages/sigra | jq -e '.latest_stable_version == "1.3.0" and (.retirements | has("1.20.0"))'</automated>
  </verify>
  <acceptance_criteria>
    Live Hex reports `latest_stable_version` = `1.3.0` and `1.20.0` present in
    `retirements`; 223-01-SUMMARY.md records the verbatim post-retire output and the
    `--unretire` fallback; the retire todo is moved to `.planning/todos/done/`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator laptop -> hex.pm API | A Hex API **write** key crosses here to perform the retire. |

## STRIDE Threat Register

This is a non-code, release-ops phase — it changes no source and adds no runtime
attack surface. The only new-in-phase secret is the operator's Hex write key.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-223-01 | Information Disclosure | Hex API write key handling | medium | mitigate | Key passed only as an `HEX_API_KEY=` env var for a single command; never written to any committed file, log, or SUMMARY; task actions explicitly prohibit pasting it. |
| T-223-02 | Tampering | hex.pm registry state | low | accept | Retire is reversible via `mix hex.retire sigra 1.20.0 --unretire` (D-03); post-retire state is verified against the live API before any downstream plan proceeds. |
| T-223-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; no legitimacy audit required. |
</threat_model>

<verification>
- Pre-retire snapshot recorded verbatim in 223-01-SUMMARY.md.
- Post-retire live-Hex query proves `latest_stable_version` = `1.3.0` and `1.20.0`
  in `retirements`.
- Folded retire todo moved `pending/ -> done/` only after verification.
- Reversible `--unretire` fallback documented (D-03).
</verification>

<success_criteria>
Hex reports `1.3.0` (not `1.20.0`) as `latest_stable_version`, `1.20.0` is retired,
and the evidence + reversible fallback are recorded — unblocking 223-02 (PUB-05)
and 223-03 (PROOF-01).
</success_criteria>

<output>
Create `.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md` when done.
</output>
