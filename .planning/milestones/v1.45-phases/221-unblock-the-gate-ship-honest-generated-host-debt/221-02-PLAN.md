---
phase: 221-unblock-the-gate-ship-honest-generated-host-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/uat/up.sh
  - test/fixtures/css/orphan_after_terminated_decl.css
  - scripts/ci/app-css-corruption-check.test.sh
  - scripts/ci/app-css-corruption-check.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [SHIP-02, SHIP-03]
must_haves:
  truths:
    - "scripts/uat/up.sh --help prints the --print-env usage line with headroom, un-clipped (SHIP-02b / D-08)"
    - "scripts/ci/app-css-corruption-check.sh exits 1 on an orphan bare value placed immediately after a ;-terminated :root declaration, and exits 0 on a clean file (SHIP-03 / D-09)"
    - "A committed regression fixture + bash driver prove the guard's new behavior and are wired into CI (D-10)"
  artifacts:
    - "test/fixtures/css/orphan_after_terminated_decl.css (net-new regression fixture)"
    - "scripts/ci/app-css-corruption-check.test.sh (net-new bash driver)"
    - "scripts/ci/app-css-corruption-check.sh (last_was_prop reset fix)"
    - "scripts/uat/up.sh (widened --help window)"
    - ".github/workflows/ci.yml (self-test step wiring the new driver)"
  key_links:
    - "app-css-corruption-check.test.sh → app-css-corruption-check.sh exit-code contract (1 on corrupt, 0 on clean)"
    - "ci.yml self-test step (alongside existing *.test.sh at ~139-160) → the new driver"
---

<objective>
Two independent, no-golden-impact hardening fixes to CI/operator tooling: widen the `up.sh --help` window so the `--print-env` usage line is printed with headroom (SHIP-02b), and close the `app-css-corruption-check.sh` false-negative where an orphan bare value after a `;`-terminated declaration reads as a legitimate multi-line continuation (SHIP-03) — proven by a net-new committed regression fixture + bash driver wired into CI (RED then GREEN).

Purpose: The corruption guard is a merge-blocking gate; a false-negative lets app.css corruption re-enter silently. The `up.sh --help` line is a DX nit. Neither touches the install golden tree.
Output: Widened `up.sh` help window; net-new CSS fixture + bash driver; `last_was_prop` reset in the guard; CI self-test wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-CONTEXT.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-RESEARCH.md

@scripts/ci/app-css-corruption-check.sh
@scripts/uat/up.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: SHIP-02b — widen the up.sh --help / --print-env window</name>
  <files>scripts/uat/up.sh</files>
  <read_first>
    - scripts/uat/up.sh:25 (the `--print-env` usage line — last line of the comment block; line 26 is blank, line 27 begins `set -euo pipefail`) and :745 (`sed -n '2,25p'`)
    - scripts/db/up.sh:135 (sibling window `2,30p | sed 's/^# \{0,1\}//'` — do NOT copy the `2,30p` bound: up.sh's comment block ends at line 25, so 2,30p would leak `set -euo pipefail`/code into --help; Research Pitfall 3)
  </read_first>
  <action>
    In `scripts/uat/up.sh` at line 745, change the `--help` printer from `sed -n '2,25p'` to `sed -n '2,26p'` (adds one line of headroom while staying strictly inside the comment block that ends at line 25). Do NOT widen to `2,30p` — that leaks executable lines into --help (Pitfall 3). This is framed as hardening: the `--print-env` line is currently printed with zero headroom, not truncated — so the acceptance is "prints --print-env", not "un-truncate".
  </action>
  <verify>
    <automated>bash scripts/uat/up.sh --help | grep -q -- '--print-env'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/uat/up.sh --help | grep -q -- '--print-env'` exits 0
    - `bash scripts/uat/up.sh --help` output does NOT contain `set -euo pipefail` or any code line (window stays inside the comment block)
  </acceptance_criteria>
  <done>up.sh --help prints the full --print-env usage line with headroom and leaks no code.</done>
</task>

<task type="auto">
  <name>Task 2: SHIP-03 (RED) — commit regression fixture + bash driver, wire into CI</name>
  <files>test/fixtures/css/orphan_after_terminated_decl.css, scripts/ci/app-css-corruption-check.test.sh, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-RESEARCH.md (§ "Regression fixture + driver (SHIP-03 / D-10)")
    - .github/workflows/ci.yml:139-160 (existing `*.test.sh` self-test steps: `settled-findings-lint.test.sh`, `stale-render-guard.test.sh`, and the existing `app-css-corruption-check.sh` step) — mirror this wiring
    - scripts/ci/settled-findings-lint.test.sh (bash driver precedent for structure/exit conventions)
  </read_first>
  <action>
    Create `test/fixtures/css/orphan_after_terminated_decl.css`: a minimal `:root { ... }` block containing a valid `;`-terminated custom-property declaration immediately FOLLOWED by an orphan bare value line (a value fragment with no `property:` prefix, e.g. a bare `0 0 0 1px rgba(0,0,0,0.2);`) — the exact corruption class the guard must catch. Create `scripts/ci/app-css-corruption-check.test.sh`: a bash driver (mirroring the repo's `*.test.sh` self-tests) that (a) runs `app-css-corruption-check.sh` against the corrupt fixture and asserts a non-zero exit, and (b) runs it against a clean CSS file (or a clean inline temp fixture) and asserts exit 0; print OK/FAIL and exit non-zero on any mismatch. Make the driver executable. Wire a new CI step "app.css corruption guard self-test" into `.github/workflows/ci.yml` immediately after the existing "Check app.css for orphaned corruption" step, calling `bash scripts/ci/app-css-corruption-check.test.sh`. NOTE: this task is RED — the driver's "exit 1 on corrupt" assertion will FAIL until Task 3 lands the guard fix; do not proceed to commit the wave until Task 3 makes it GREEN.
  </action>
  <verify>
    <automated>test -f test/fixtures/css/orphan_after_terminated_decl.css && test -x scripts/ci/app-css-corruption-check.test.sh && grep -q 'app-css-corruption-check.test.sh' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/css/orphan_after_terminated_decl.css` exists and contains an orphan bare value on the line after a `;`-terminated `:root` declaration
    - `scripts/ci/app-css-corruption-check.test.sh` exists, is executable, asserts non-zero exit on the corrupt fixture and exit 0 on a clean input
    - `.github/workflows/ci.yml` invokes `bash scripts/ci/app-css-corruption-check.test.sh` as a self-test step next to the existing corruption-check step (~line 155)
    - RED confirmation: `bash scripts/ci/app-css-corruption-check.sh test/fixtures/css/orphan_after_terminated_decl.css` currently exits 0 (the false-negative — proves the driver is exercising the real gap before Task 3)
  </acceptance_criteria>
  <done>Fixture + driver committed and CI-wired; the driver fails against the unpatched guard (RED baseline established).</done>
</task>

<task type="auto">
  <name>Task 3: SHIP-03 (GREEN) — reset last_was_prop on single-line declarations</name>
  <files>scripts/ci/app-css-corruption-check.sh</files>
  <read_first>
    - scripts/ci/app-css-corruption-check.sh:117-140 (the two `property:` opener branches that unconditionally set `last_was_prop=1; next`, and the continuation branch)
    - .planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-RESEARCH.md (§ "Pattern: awk state-machine reset (SHIP-03 / D-09)")
  </read_first>
  <action>
    In the awk state machine, patch BOTH opener branches — the `--` custom-property branch (line ~118) and the `word:` standard-declaration branch (line ~124). For each, before `next`, set `last_was_prop = 0` when the trimmed line already ends in `;` (a complete single-line declaration — NOT an opener), and set `last_was_prop = 1` only when it is a genuine multi-line opener (no trailing `;`). This closes the false-negative where an orphan bare value after a `;`-terminated declaration was absorbed by the `if (last_was_prop)` continuation branch. Do not alter the continuation branch or the final bare-value flagging logic. Do not reconstruct any `--sg-*` tokens (Anti-Pattern: they belong in sigra_admin.css).
  </action>
  <verify>
    <automated>bash scripts/ci/app-css-corruption-check.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/app-css-corruption-check.sh test/fixtures/css/orphan_after_terminated_decl.css` now exits non-zero (guard catches the mid-block orphan)
    - `bash scripts/ci/app-css-corruption-check.sh` (default target: test/example app.css) still exits 0 — no false positive on the real, clean app.css
    - `bash scripts/ci/app-css-corruption-check.test.sh` exits 0 (driver GREEN)
  </acceptance_criteria>
  <done>Guard resets last_was_prop on complete single-line decls; catches the orphan-after-; corruption; the clean app.css still passes; the self-test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR diff → merge-blocking CI guard | app.css corruption must be caught before it enters the shipped/example CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-221-03 | Tampering | `app-css-corruption-check.sh` false-negative | medium | mitigate | Reset `last_was_prop` on single-line decls (D-09) + committed regression fixture/driver wired into CI (D-10) so the gap cannot silently reopen |
| T-221-04 | Information Disclosure | `up.sh --help` window leaking code lines | low | accept | Bounded to `2,26p` (stays inside the comment block); verified `--help` prints no `set -euo pipefail`/code — Pitfall 3 |

Package legitimacy: N/A — no package installs in this plan.
</threat_model>

<verification>
- `bash scripts/ci/app-css-corruption-check.test.sh` exits 0.
- `bash scripts/ci/app-css-corruption-check.sh` (default) exits 0; against the corrupt fixture exits non-zero.
- `bash scripts/uat/up.sh --help | grep -q -- '--print-env'` exits 0 with no code leak.
</verification>

<success_criteria>
The corruption guard catches the orphan-after-`;` class (proven by a committed regression case wired into CI), the clean app.css still passes, and `up.sh --help` prints the full `--print-env` line.
</success_criteria>

<output>
Create `.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-02-SUMMARY.md` when done.
</output>
