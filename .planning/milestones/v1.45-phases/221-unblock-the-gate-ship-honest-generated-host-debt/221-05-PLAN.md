---
phase: 221-unblock-the-gate-ship-honest-generated-host-debt
plan: 05
type: execute
wave: 4
depends_on: ["221-01", "221-02", "221-03", "221-04"]
files_modified: []
autonomous: false
requirements: [PUB-01, PUB-04]
user_setup:
  - service: hex.pm
    why: "Retiring 1.20.0 needs an operator-minted Hex WRITE key (interactive password); the CI HEX_API_KEY is read-only (D-16)"
    env_vars:
      - name: HEX write key (local, operator-minted)
        source: "mix hex.user key generate --key-name sigra-retire --permission api (prompts for hex.pm password)"
    dashboard_config:
      - task: "Retire the stray 1.20.0 with reason 'invalid'"
        location: "mix hex.retire sigra 1.20.0 invalid --message ..."
must_haves:
  truths:
    - "The stray Hex 1.20.0 is retired so latest_stable_version resolves to the real GA 1.3.0 (PUB-04 / D-14 step 4)"
    - "On push-to-main with the merged PR (SHIP edits + pin), the Upgrade smoke job compiles clean under --warnings-as-errors and concludes success, and ci-gate prints 'passed: all required release lanes succeeded' (PUB-01)"
  artifacts:
    - "Hex registry: sigra 1.20.0 retired (external state); latest_stable_version == 1.3.0"
    - "A green upgrade_smoke + ci-gate run on main (CI/registry-side proof)"
  key_links:
    - "mix hex.retire → latest_stable_version + ~> 1.0 adopter resolution (does NOT change the smoke sort — the pin does; Pitfall 1)"
    - "merged PR (pin from Plan 03 + SHIP edits) → push-to-main → upgrade_smoke → ci-gate"
---

<objective>
Close the phase: retire the stray Hex `1.20.0` for adopter honesty (PUB-04), then observe the terminal PUB-01 proof — `upgrade_smoke` → success and `ci-gate` green on a push-to-`main` run of the merged PR (SHIP edits from Plans 01/02 + the smoke pin from Plan 03).

Purpose: The retire fixes `latest_stable_version`/`~> 1.0` resolution but does NOT change the smoke's `sort -V` (Pitfall 1) — the pin (Plan 03) is what greens the smoke; the retire is orthogonal adopter honesty. The gate-green is inherently CI/registry-side: `upgrade_smoke` is `skipped` on PRs and only runs on push-to-`main` — it is NOT provable locally or from a PR (D-16, RESEARCH § Environment Availability).
Output: 1.20.0 retired; a green `upgrade_smoke` + `ci-gate` on `main`.

NOTE on scope fence: this plan observes gate-green; it does NOT build the durable retired-filter (Phase 222 / HARD-01) nor the full currency proof bundle / adopter `~> 1.0` resolution proof (Phase 223 / PUB-05, PROOF-01). Those remain out of scope (D-15).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-CONTEXT.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-RESEARCH.md

@scripts/ci/upgrade-smoke.sh
@.planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: PUB-04 — retire the stray 1.20.0 (operator write-auth)</name>
  <files>none (external Hex registry state)</files>
  <action>
    Operator-gated (D-16) — needs an operator-minted Hex WRITE key; the CI HEX_API_KEY is read-only. Agent preflight: re-query live Hex `curl -s https://hex.pm/api/packages/sigra | jq '.latest_stable_version, (.retirements // {})'` and confirm 1.2.0 + 1.3.0 are published (Plan 04 done) and 1.20.0 is still un-retired (`latest_stable_version` currently 1.20.0). Per Pitfall 1, retire does NOT drop 1.20.0 from the smoke's `mix hex.info` sort — this step is adopter-honesty only (the pin greens the smoke). Operator mints a write key `mix hex.user key generate --key-name sigra-retire --permission api` (prompts for hex.pm password), then runs `mix hex.retire sigra 1.20.0 invalid --message "Published in error during dev cycle; not a real release — use 1.3.0+"` (reversible via `--unretire`). Agent verifies via the Hex API.
  </action>
  <what-built>
    Agent verifies the operator's retire of the stray 1.20.0 via the live Hex API (latest_stable_version flips to 1.3.0).
  </what-built>
  <how-to-verify>
    1. Agent: confirm 1.2.0 & 1.3.0 present, 1.20.0 un-retired.
    2. Operator: mint write key + `mix hex.retire sigra 1.20.0 invalid --message "..."` (commands in <action>).
    3. Agent: Hex API `latest_stable_version` == 1.3.0 and retirements includes 1.20.0.
  </how-to-verify>
  <verify>
    <automated>test "$(curl -s https://hex.pm/api/packages/sigra | jq -r '.latest_stable_version')" = "1.3.0"</automated>
  </verify>
  <resume-signal>Type "1.20.0 retired" once latest_stable_version == 1.3.0, or describe the failure.</resume-signal>
  <done>Hex latest_stable_version resolves to 1.3.0; 1.20.0 is retired (reason: invalid).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PUB-01 — observe upgrade_smoke → success + ci-gate green on main</name>
  <files>none (CI/registry-side observation)</files>
  <action>
    Terminal proof, inherently CI/registry-side (D-16) — NOT provable locally or from a PR (`upgrade_smoke` is skipped on PRs, runs only on push-to-`main`). All repo edits are ready to merge: SHIP-01/02a template + re-blessed golden (Plan 01), SHIP-02b/03 tooling + guard + fixture/driver (Plan 02), and the SIGRA_UPGRADE_SMOKE_START_VERSION=1.3.0 pin (Plan 03); v1.2.0 + v1.3.0 are published (Plan 04). Agent may run an optional local PREVIEW (reproduce `upgrade-smoke.sh:44-53` — the raw list still sorts to 1.20.0, but the pinned override target 1.3.0 is now published so the override validates) and document it as a preview, not proof. Operator merges/pushes the phase PR to `main`. Agent then uses `gh run watch`/`gh run view` on the push run to confirm the `Upgrade smoke (published source series -> local candidate)` job concluded `success` (log shows start version 1.3.0, not 1.20.0) and the `ci-gate` job concluded success printing `ci-gate passed: all required release lanes succeeded`.
  </action>
  <what-built>
    Agent observes the terminal PUB-01 proof on a push-to-main run: upgrade_smoke → success (floor = 1.3.0) and ci-gate green.
  </what-built>
  <how-to-verify>
    1. Agent optional local preview (not proof).
    2. Operator: merge/push the phase PR to `main`.
    3. Agent: `gh run view` — upgrade_smoke = success (start version 1.3.0); ci-gate = success.
  </how-to-verify>
  <verify>
    <automated>gh run list --branch main --workflow ci.yml --limit 1 --json conclusion,databaseId</automated>
  </verify>
  <resume-signal>Type "gate green" once upgrade_smoke → success and ci-gate is green on main, or describe the failing job/log.</resume-signal>
  <done>On the push-to-main run, upgrade_smoke concluded success (floor resolved to 1.3.0) and ci-gate is green — PUB-01 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Hex registry (retire write) | Retiring mutates public registry metadata; needs an operator write key, not the read-only CI key |
| adopter `mix deps.update` → Hex resolution | Adopters must not silently resolve the stray 1.20.0 as a real release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-221-08 | Spoofing / Tampering | adopter `~> 1.0` resolving the stray `1.20.0` as GA | high | mitigate | `mix hex.retire sigra 1.20.0 invalid` → `latest_stable_version` = 1.3.0 (supply-chain honesty, ASVS V14 / D-14 step 4) |
| T-221-09 | Repudiation | claiming gate-green without CI evidence | medium | mitigate | Terminal proof is an actual push-to-`main` run (`gh run view`); explicitly NOT claimed local/PR (D-16) |

Package legitimacy: N/A — no package installs.
</threat_model>

<verification>
- Live Hex API: `latest_stable_version` == `1.3.0`; 1.20.0 in retirements.
- `gh run view` on the push-to-`main` run: `upgrade_smoke` = success (start version 1.3.0), `ci-gate` = success.
- Gate-green is documented as CI/registry-side (not a local/PR claim).
</verification>

<success_criteria>
The stray 1.20.0 is retired (latest_stable_version = 1.3.0) and, on push-to-`main`, `upgrade_smoke` concludes success with `ci-gate` green — PUB-01 resolved.
</success_criteria>

<output>
Create `.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-05-SUMMARY.md` when done.
</output>
