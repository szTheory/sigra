---
phase: 203-consistency-propagation
plan: 05
type: execute
wave: 2
depends_on: ["203-01", "203-02", "203-03"]
files_modified:
  - guides/reference/admin-quality-ledger.md
  - .planning/todos/pending/2026-06-17-page04-branding-explicit-scoring.md
autonomous: true
requirements: [PROP-01]
must_haves:
  truths:
    - "index-live, organization-live, and branding-live ledger cells are ratcheted column-4 from bare 1 to bare 2 (D-08)"
    - "Each ratcheted cell's Evidence cites ONLY honestly-applicable Tier-2 proxies; inapplicable proxies are cited explicitly as N/A (D-08)"
    - "The Overviews cite content-equivalence + overlay-axe + 7-APG as N/A (no results table, no modal); branding cites overlay-axe + 7 APG as EARNED by the Plan 03 D-06 test, content-equivalence N/A (D-08)"
    - "The monotonic guard passes forward-only against origin/main with the three cells now at 2 (D-08)"
    - "The PAGE-04 branding-scoring todo is resolved by the branding-live ratchet + expanded evidence, not a new ledger row (D-09)"
    - "global-overview and org-overview baselines recapture idempotently via snapshot-recapture-gate.sh; canaries byte-stable; both allowlists empty at end (D-10)"
    - "MG-3/MG-7/MG-8 markup is verified unchanged and NOT recaptured (Pitfall 5)"
  artifacts:
    - guides/reference/admin-quality-ledger.md
  key_links:
    - "branding-live evidence cites the Plan 03 admin-modal-interaction #restore-defaults-overlay case (hard prereq, Pitfall 6)"
    - "ledger column-4 stays bare single [012] integer so quality-ledger-monotonic.sh awk -F'|' positional parse counts the cell (Pitfall 1)"
  prohibitions:
    - "Do NOT decorate the tier cell (no 'Tier 2', no bold, no parens) — bare 2 only, or the monotonic guard silently drops the cell (Pitfall 1)"
    - "Do NOT fabricate an inapplicable Tier-2 proxy (no content-equivalence/overlay-axe claim on an Overview that owns neither a table nor a modal) — false claims fail Phase 204 (Pitfall 2)"
    - "Do NOT write the branding-live overlay-axe/APG evidence unless Plan 03's D-06 test is green (Pitfall 6)"
    - "Do NOT bake MG-3/MG-7/MG-8 baselines — their markup is unchanged by the Overview pill edits (Pitfall 5)"
    - "Do NOT leave either snapshot allowlist non-empty at end-of-phase — Phase 204 owns the terminal reset (Pitfall 7)"
    - "Do NOT add a new ledger row for PAGE-04 — its premise is stale; resolve by expanding the existing branding-live row (D-09)"
---

<objective>
Ratchet the three target ledger cells from bare `1` to bare `2` (D-08), expand each Evidence column with only honestly-applicable Tier-2 proxies (N/A where true), fold the PAGE-04 branding-scoring todo (D-09), and prove the `global-overview` + `org-overview` recapture is idempotent with both allowlists left empty (D-10). This is the consolidation wave — it depends on the Overview pill edits (Plan 01), the branding promotion (Plan 02), and the green D-06 branding modal test (Plan 03, the hard prerequisite for the branding overlay-axe/APG claim).

Purpose: Record the elevated Tier-2 bar on the three propagated surfaces honestly and forward-only, and close the deferred PAGE-04 todo.
Output: Three ratcheted ledger cells (bare 2 + honest proxy evidence); a resolved PAGE-04 todo; a proven idempotent recapture of the two Overview slugs; both allowlists empty; canaries byte-stable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-consistency-propagation/203-CONTEXT.md
@.planning/phases/203-consistency-propagation/203-RESEARCH.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ratchet the three cells 1->2 with honest Tier-2 evidence (D-08) + fold PAGE-04 (D-09)</name>
  <files>guides/reference/admin-quality-ledger.md, .planning/todos/pending/2026-06-17-page04-branding-explicit-scoring.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (:85 index-live, :86 organization-live, :92 branding-live — all bare `1`; :87 users-index-live — the bare `2` Tier-2 evidence TEMPLATE incl. exactly how N/A proxies are cited; :14-27 positional parse rules)
    - guides/reference/admin-fractal-scorecard.md (:135-167 — Tier-2 Add-on proxy applicability: overlay-axe + 7 APG = modal-only; content-equivalence = results-table-only; glossary-clean = always; motion/density/target-size = documented-as-manual)
    - .planning/phases/203-consistency-propagation/203-RESEARCH.md (Pattern 1 bare-integer ratchet; Pattern 2 honest N/A citations with the exact verified applicability; Pitfall 1, 2, 6)
    - .planning/todos/pending/2026-06-17-page04-branding-explicit-scoring.md (resolves_phase: 203 — its "no separate L3 row exists" premise is stale; the row exists at :92)
    - test/example/priv/playwright/tests/admin-modal-interaction.spec.ts (CONFIRM the Plan 03 branding #restore-defaults-overlay case exists and is green BEFORE writing the branding overlay-axe/APG evidence — Pitfall 6 hard prereq)
  </read_first>
  <action>
    Flip column 4 from bare `1` to bare `2` for exactly three rows: index-live (:85), organization-live (:86), branding-live (:92). Keep the value a BARE single `2` — no `Tier 2`, no bold, no parens, no decorators — so quality-ledger-monotonic.sh's awk -F'|' positional ^[012]$ parse still counts the cell (Pitfall 1). Expand each row's Evidence column to mirror the users-index-live template (:87), citing ONLY honestly-applicable proxies:
    - index-live + organization-live (Overviews): content-equivalence N/A (no results table — the org roster is an sg-list, not a desktop-table<->mobile-card pattern); overlay-axe N/A (owns no modal dialog); 7-APG gates N/A (no overlay). Applicable: glossary-clean (glossary_test.exs scopes both pages), motion-tokens (reviewed — no `transition: all`), density/rhythm (reviewed — sg-stack--N tiers), target-size (reviewed — documented-as-manual). Also note the recaptured global-overview/org-overview checkpoint screenshots+axe.
    - branding-live: overlay-axe + 7 APG focus-trap/restore gates EARNED by the Plan 03 admin-modal-interaction #restore-defaults-overlay case (cite it — verify it is green first, Pitfall 6); content-equivalence N/A (no results table); plus glossary-clean, motion-tokens, density/rhythm, target-size (documented-as-manual). Do NOT fabricate content-equivalence on branding or overlay-axe on the Overviews (Pitfall 2 — false claims fail Phase 204).
    Then fold PAGE-04: resolve `.planning/todos/pending/2026-06-17-page04-branding-explicit-scoring.md` (move to `.planning/todos/resolved/` and/or mark resolved per the repo's todo-resolution convention), recording that it is satisfied by the branding-live cell ratchet + expanded evidence (D-09) — NOT by adding a new ledger row (the row already exists at :92; its "no separate L3 row exists" premise is stale).
  </action>
  <acceptance_criteria>
    - `git show origin/main:guides/reference/admin-quality-ledger.md | grep -E '^\| (index-live|organization-live|branding-live) '` shows bare `1`; the working copy shows bare `2` for all three (forward ratchet)
    - The three column-4 values are bare single `2` (no decorators) — verify each row's 4th pipe-field trimmed == "2"
    - The branding-live Evidence cites the admin-modal-interaction #restore-defaults-overlay case; the Overviews cite overlay-axe/APG/content-equivalence as N/A
    - `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 (PASS; the three cells counted and forward-only)
    - The PAGE-04 todo is resolved (no longer in .planning/todos/pending/) with a note pointing to the branding-live ratchet; NO new ledger row was added
  </acceptance_criteria>
  <verify>
    <automated>git fetch origin main >/dev/null 2>&1; bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && grep -E '^\| (index-live|organization-live|branding-live) ' guides/reference/admin-quality-ledger.md</automated>
  </verify>
  <done>The three cells are bare 2 with honest proxy evidence (Overviews cite N/A for table/modal proxies; branding cites the earned D-06 overlay-axe/APG); the monotonic guard passes forward-only; PAGE-04 is folded into the branding-live row without a new row.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent recapture of global-overview + org-overview; verify MG unchanged; allowlists empty (D-10)</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts (baselines), scripts/ci/snapshot-recapture-gate.sh (invoked, not edited)</files>
  <read_first>
    - scripts/ci/snapshot-recapture-gate.sh (per-lane slug routing; RECAPTURE_DRYRUN=1 seam) and scripts/ci/snapshot-canary-guard.sh (:104 — forbids modify/delete of an established canary)
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts (:193 global-overview, :204 org-overview slugs)
    - test/example/lib/example_web/live/admin/design_gallery_live.ex (:556 MG-3 task-card grid, :934 MG-7 member roster, :964 MG-8 pending invitations — verify markup unchanged; MG-7 carries only a role pill, does NOT mirror the org-roster status pills, Pitfall 5)
    - .planning/phases/203-consistency-propagation/203-RESEARCH.md (Pitfall 5 verify-then-skip MG; Pitfall 7 allowlists empty + canaries byte-stable; Code Examples — RECAPTURE_DRYRUN dry-run + md5 parity)
  </read_first>
  <action>
    The Overview pill edits (Plan 01) change the rendered global-overview and org-overview surfaces, so their checkpoint baselines need recapture. FIRST prove idempotency/routing without baking: run `RECAPTURE_DRYRUN=1 bash scripts/ci/snapshot-recapture-gate.sh global-overview org-overview` and confirm both slugs route to the checkpoint lane. THEN, against a booted example (SIGRA_EXAMPLE_URL=http://localhost:4011), recapture ONLY the two Overview checkpoint slugs through snapshot-recapture-gate.sh (NOT the canary guard directly); prove a zero-drift second run before treating the new PNGs as final (bake only after a clean idempotent run). VERIFY-THEN-SKIP the gallery: confirm MG-3 (:556), MG-7 (:934), MG-8 (:964) markup is unchanged by the Overview pill edits (MG-7 carries only a role pill and does NOT mirror the org-roster status pills — Pitfall 5) and DO NOT bake any MG baseline; if a recapture diff unexpectedly shows an MG PNG change, STOP and investigate a shared-class side effect before baking. Keep the board-notice and impersonation-banner canaries byte-stable. End-of-phase: BOTH snapshot allowlists empty (Phase 204 owns the terminal reset — Pitfall 7). This task is recapture + drift hygiene; the full Playwright capture lane runs out-of-band by design.
  </action>
  <acceptance_criteria>
    - `RECAPTURE_DRYRUN=1 bash scripts/ci/snapshot-recapture-gate.sh global-overview org-overview` routes both slugs to the checkpoint lane (dry-run prints CK_ALLOW including both)
    - After recapture, a second capture run shows zero drift on global-overview/org-overview (idempotency proven before baking)
    - `git diff` shows NO change to MG-3/MG-7/MG-8 gallery baselines (markup verified unchanged)
    - `git diff` shows NO change to the board-notice or impersonation-banner canary PNGs (byte-stable)
    - Both snapshot allowlists are empty at end-of-phase (no allowlist entry committed)
  </acceptance_criteria>
  <verify>
    <automated>RECAPTURE_DRYRUN=1 bash scripts/ci/snapshot-recapture-gate.sh global-overview org-overview && bash -n scripts/ci/snapshot-recapture-gate.sh</automated>
  </verify>
  <done>global-overview + org-overview recapture idempotently (zero-drift second run before baking); MG-3/MG-7/MG-8 baselines and the two canaries are byte-stable; both allowlists are empty at phase close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger doc -> monotonic guard | Column-4 positional parse; a decorated cell silently escapes forward-only protection |
| recapture flow -> committed baselines | Allowlist/canary hygiene governs which baselines change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-05-R | Repudiation (false Tier-2 claim) | admin-quality-ledger.md branding-live evidence | high | mitigate | branding overlay-axe/APG cited ONLY after the Plan 03 D-06 test is green (Pitfall 6); Overviews cite table/modal proxies as N/A (no fabrication, Pitfall 2) |
| T-203-05-T | Tampering (silent tier downgrade) | ledger column-4 | medium | mitigate | Bare `2` keeps the cell inside the monotonic guard's ^[012]$ parse (Pitfall 1); guard run forward-only against origin/main |
| T-203-05-canary | Tampering | board-notice / impersonation-banner canaries | medium | mitigate | snapshot-canary-guard forbids modify/delete of an established canary; recapture routed through snapshot-recapture-gate.sh; allowlists left empty (Pitfall 7) |
</threat_model>

<verification>
- `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 (three cells at bare 2, forward-only)
- The three column-4 values are bare single `2`
- branding-live Evidence cites the D-06 #restore-defaults-overlay case; Overviews cite N/A proxies honestly
- PAGE-04 todo resolved without a new ledger row
- `RECAPTURE_DRYRUN=1 ...snapshot-recapture-gate.sh global-overview org-overview` routes both to the checkpoint lane
- MG-3/MG-7/MG-8 and the two canaries are byte-stable; both allowlists empty
</verification>

<success_criteria>
index-live, organization-live, and branding-live are at bare Tier 2 with honest proxy evidence; the monotonic guard passes forward-only; PAGE-04 is folded into the branding-live row (no new row); global-overview + org-overview recapture idempotently with MG baselines and canaries byte-stable and both allowlists empty for Phase 204's terminal reset.
</success_criteria>

<artifacts_this_phase_produces>
- RATCHETED ledger cells (bare `1` -> bare `2`) with expanded honest Tier-2 evidence: `index-live` (:85), `organization-live` (:86), `branding-live` (:92) in `guides/reference/admin-quality-ledger.md`.
- RESOLVED todo: `2026-06-17-page04-branding-explicit-scoring.md` folded into the branding-live row (no new ledger row).
- RECAPTURED baselines: `global-overview` + `org-overview` checkpoint PNGs (idempotent; allowlists left empty). NO new MG baselines, NO canary changes.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/203-consistency-propagation/203-05-SUMMARY.md` when done
</output>
