---
phase: 203-consistency-propagation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/components.ex
  - lib/sigra/admin/live/branding_live.ex
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
autonomous: true
requirements: [PROP-01]
must_haves:
  truths:
    - "branding_live.ex's color_field / preview_pair / detail_input are no longer private to the LiveView — they live in Sigra.Admin.Components and the LiveView calls them as <.name ...> (D-05)"
    - "The branding workbench obeys UI-principle :29 (reusable markup routes through components.ex) (D-05)"
    - "The promoted components use attr/slot signatures (not bare assigns passthrough) and contain no raw/1 (T-203-02)"
    - "The branding workbench renders the same markup before and after the promotion (pure refactor)"
    - "If any genuinely new sg-* class is introduced, it is byte-identical across all three sigra_admin.css copies (D-12)"
  artifacts:
    - lib/sigra/admin/components.ex
    - lib/sigra/admin/live/branding_live.ex
  key_links:
    - "branding_live.ex call-sites now reference <.color_field>/<.preview_pair>/<.detail_input> resolved from Sigra.Admin.Components"
    - "promoted components keep existing sg-branding-* / sg-tabs classes (D-05) — golden-diff CSS lockstep only if a genuinely new class is required (D-12)"
  prohibitions:
    - "Do NOT invent net-new public components beyond what the D-05 routing requires (no dedicated branding-preview route, no speculative wrappers)"
    - "Do NOT modify the #restore-defaults-overlay ConfirmDialog markup — its contract is consumed by the D-06 test in Plan 03 and must stay byte-stable"
    - "Do NOT introduce raw/1 in the promoted components (mirror 202 T-202-02)"
    - "Do NOT touch only one CSS copy — any new sg-* class MUST hit all three byte-identically or golden-diff fails (D-12)"
---

<objective>
Promote branding's three private preview components — `color_field` (`branding_live.ex:547`), `preview_pair` (`:579`), `detail_input` (`:493`) — into `Sigra.Admin.Components`, delete the privates, and update the call-sites to `<.color_field>` / `<.preview_pair>` / `<.detail_input>`. This makes the workbench obey UI-principle `:29` (reusable markup routes through `components.ex`) — same-job -> same-component (D-05). This is the 200/202 shared-component-promotion pattern (Pattern 3).

Purpose: The branding workbench is the heaviest lift of the phase — bespoke composition with private preview components. Routing them through the public component surface is the prerequisite for the D-07 Workbench archetype doc (Plan 04) and the honest branding Tier-2 claim (Plan 05).
Output: Three promoted public components in `components.ex`; `branding_live.ex` privates deleted and call-sites rewired; CSS triple-copy lockstep ONLY if a genuinely new class is required (expected: none — reuse `sg-branding-*`/`sg-tabs`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-consistency-propagation/203-CONTEXT.md
@.planning/phases/203-consistency-propagation/203-UI-SPEC.md
@lib/sigra/admin/components.ex
@lib/sigra/admin/live/branding_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote color_field / preview_pair / detail_input to components.ex (D-05)</name>
  <files>lib/sigra/admin/components.ex, lib/sigra/admin/live/branding_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/branding_live.ex (detail_input at :493, color_field at :547, preview_pair at :579 — read the full body of each private component AND every call-site that renders them)
    - lib/sigra/admin/components.ex (:110 summary_chip, :166 task_card, :462 notice, :506 scope_ribbon — the attr/slot signature convention to mirror; this is the promotion destination)
    - .planning/phases/203-consistency-propagation/203-UI-SPEC.md (Component Inventory — Promotion rules D-05: attr/slot pattern, existing sg-branding-*/sg-tabs classes, no raw/1)
    - .planning/phases/203-consistency-propagation/203-RESEARCH.md (Pattern 3 — 202 deleted privates audit_tone/multi_page?/format_timestamp and moved them to components.ex; mirror that exactly)
  </read_first>
  <action>
    Move detail_input/1 (:493), color_field/1 (:547), and preview_pair/1 (:579) out of branding_live.ex and into Sigra.Admin.Components as PUBLIC function components. For each: convert the private defp name to def name with explicit attr/slot declarations matching the existing components.ex convention (mirror summary_chip's attr style — do NOT leave a bare assigns passthrough; declare each assign the component reads as an attr with type and required-or-default). Preserve the rendered markup verbatim — keep every existing sg-branding-* / sg-tabs class byte-for-byte (do NOT rename or add classes; D-05 keeps the existing classes). Ensure NO raw/1 appears in any promoted component (T-203-02; mirror 202 T-202-02). After moving, DELETE the three defp definitions from branding_live.ex, and update each call-site to the dot-component form so they resolve from Sigra.Admin.Components (verify the existing import/alias in branding_live.ex — it already renders other components.ex components, so the same import covers these). Do NOT modify the #restore-defaults-overlay ConfirmDialog block (it is consumed byte-stable by Plan 03's D-06 test).
  </action>
  <acceptance_criteria>
    - `grep -c 'defp detail_input\|defp color_field\|defp preview_pair' lib/sigra/admin/live/branding_live.ex` returns 0 (privates deleted)
    - `grep -c 'def detail_input\|def color_field\|def preview_pair' lib/sigra/admin/components.ex` returns 3 (promoted as public)
    - The promoted components contain no `raw(` call (verify the new region in components.ex)
    - `mix compile --warnings-as-errors` exits 0 (call-sites resolve; no undefined-component or unused-attr warnings)
    - The `#restore-defaults-overlay` block in branding_live.ex is byte-unchanged
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && echo "privates_remaining=$(grep -c 'defp detail_input\|defp color_field\|defp preview_pair' lib/sigra/admin/live/branding_live.ex)" && echo "public_added=$(grep -c 'def detail_input\|def color_field\|def preview_pair' lib/sigra/admin/components.ex)"</automated>
  </verify>
  <done>The three preview components are public in components.ex with attr/slot signatures and no raw/1; the branding_live.ex privates are deleted; call-sites resolve; compile is clean; the ConfirmDialog overlay block is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: CSS triple-copy lockstep gate (D-12) — only if a new class was introduced</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</files>
  <read_first>
    - priv/templates/sigra.install/admin/sigra_admin.css (installer source of truth)
    - test/example/priv/static/assets/sigra_admin.css (example mirror)
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css (golden fixture mirror)
    - .planning/phases/203-consistency-propagation/203-RESEARCH.md (Pitfall 3 — 184->185 regression class; md5 currently 9b281962ee8fe33254829c877af00382; requires phx_new 1.8.7 for golden-diff)
    - CLAUDE.md (note: install golden tests require phx_new 1.8.7 — `mix archive.install --force hex phx_new 1.8.7`)
  </read_first>
  <action>
    Determine whether Task 1 introduced any genuinely new sg-* class. D-05 explicitly keeps the existing sg-branding-* / sg-tabs classes, so the EXPECTED outcome is ZERO new CSS (the promotion is a markup move, not a new affordance). First, verify no new class is needed: if the three sigra_admin.css copies still share their single md5, this task is a no-op gate (the lockstep was not tripped) — record that in the SUMMARY and run golden-diff to confirm green. ONLY if the promotion genuinely required a new sg-* class (e.g. a new preview-rail affordance not already in the system): write that class byte-identically into ALL THREE copies (installer template, example mirror, golden fixture mirror) so they share one md5, then run the install golden-diff test (requires phx_new 1.8.7 installed locally). Never touch one copy alone.
  </action>
  <acceptance_criteria>
    - The three `sigra_admin.css` copies share exactly one md5 (whether unchanged at `9b281962...` or a new shared hash if a class was genuinely added)
    - `mix test test/sigra/install/golden_diff_test.exs` passes (requires phx_new 1.8.7) — generated host receives a styled, byte-parity admin CSS
  </acceptance_criteria>
  <verify>
    <automated>md5 -q priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css | sort -u | wc -l && mix test test/sigra/install/golden_diff_test.exs</automated>
  </verify>
  <done>The three CSS copies share one md5 and golden-diff is green — either the promotion needed zero new CSS (expected) or any new class was written byte-identically across all three copies (D-12 lockstep honored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operator -> branding LiveView | Admin-gated config surface (SigraAdminPolicy, unchanged) |
| installer template -> generated host CSS | Admin CSS ships installer-template -> generated host; byte-parity-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-02 | Tampering (stored XSS) | Promoted color_field/preview_pair/detail_input | medium | mitigate | HEEx auto-escaping; no `raw/1` in any promoted component (mirror 202 T-202-02 "no raw/1 in shared row"); branding form values already validated via Profile.new/1 (RESEARCH Security Domain V5) |
| T-203-02-CSS | Tampering | Three sigra_admin.css copies | high | mitigate | Any new sg-* class written byte-identically to all three copies; golden-diff gate (184->185 regression class) blocks divergence — generated hosts never get an unstyled page |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0
- `grep -c 'defp detail_input\|defp color_field\|defp preview_pair' lib/sigra/admin/live/branding_live.ex` == 0
- `grep -c 'def detail_input\|def color_field\|def preview_pair' lib/sigra/admin/components.ex` == 3
- The three sigra_admin.css copies share one md5
- `mix test test/sigra/install/golden_diff_test.exs` passes (phx_new 1.8.7)
</verification>

<success_criteria>
The three branding preview components are public in Sigra.Admin.Components with attr/slot signatures and no raw/1; branding_live.ex no longer owns them privately; the workbench renders the same markup; the CSS triple-copy lockstep is honored (zero new CSS expected; any new class is byte-identical across all three copies and golden-diff stays green).
</success_criteria>

<artifacts_this_phase_produces>
- PROMOTED public components in `Sigra.Admin.Components`: `color_field/1`, `preview_pair/1`, `detail_input/1` (moved from branding_live.ex privates; attr/slot signatures; reuse existing `sg-branding-*`/`sg-tabs` classes; no `raw/1`).
- New `sg-*` class(es): NONE expected (D-05 reuses existing classes). If any is genuinely required, it is written byte-identically across all three `sigra_admin.css` copies (D-12).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/203-consistency-propagation/203-02-SUMMARY.md` when done
</output>
