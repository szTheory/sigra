---
phase: 203-consistency-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/live/organization_live.ex
  - lib/sigra/admin/live/index_live.ex
autonomous: true
requirements: [PROP-01]
must_haves:
  truths:
    - "The org roster on /admin/organizations/:slug no longer renders an always-on green Confirmed pill (D-02)"
    - "The org roster still renders decision-bearing pills: role, Locked, Deletion scheduled, Unconfirmed (D-02)"
    - "The global overview's Authentication coverage summary_chip is demoted/removed per the 201 precedent (D-03)"
    - "The same status signal renders identically across the org overview and the Users Index (same-job -> same-component)"
    - "Both Overviews compile with --warnings-as-errors and stay glossary-clean"
  artifacts:
    - lib/sigra/admin/live/organization_live.ex
    - lib/sigra/admin/live/index_live.ex
  key_links:
    - "organization_live.ex:105 Confirmed/ok pill removal mirrors users_index_live.ex status_pills/1 reduction"
    - "index_live.ex:113-122 coverage chip demotion mirrors users_index_live 201 D-03 KPI demotion"
  prohibitions:
    - "Do NOT restructure the Overview page composition (D-01 — light component-level pass, NOT recomposition)"
    - "Do NOT introduce any new sg-* CSS class (pill reduction reuses existing sg-status-pill / summary_chip classes — zero new CSS expected; D-12)"
    - "Do NOT promote user_status_cluster/user_name_stack to components.ex — the org roster uses a lighter sg-list-row/sg-cluster pill set, not the Users Index desktop-table+mobile-card DOM, so promotion is NOT DRY-justified (D-04, RESEARCH A3)"
    - "Do NOT recapture any baseline in this plan — recapture is owned by Plan 05 (D-10)"
---

<objective>
Align the two Overview surfaces to the reduced-pill vocabulary Phase 201 established, so the same status signal renders identically across the org overview, the global overview, and the Users Index (same-job -> same-component). This is a component-level alignment pass, NOT a recomposition (D-01).

Two concrete edits:
- D-02: drop the org roster's always-on green `Confirmed`/`ok` pill (`organization_live.ex:105`).
- D-03: demote/remove the global overview's "Authentication coverage" MFA/passkey `summary_chip` (`index_live.ex:113-122`) per the 201 KPI-demotion precedent.

Purpose: Make "same job -> same component" literally true across admin status signals before the ledger ratchet (Plan 05) claims Tier-2 consistency.
Output: Edited `organization_live.ex` and `index_live.ex`; zero new CSS; glossary-clean; clean compile.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-consistency-propagation/203-CONTEXT.md
@.planning/phases/203-consistency-propagation/203-UI-SPEC.md
@guides/reference/admin-ui-principles.md
</context>

<tasks>

<!-- planner-discipline-allow: data-tone="ok">Confirmed -->
<task type="auto">
  <name>Task 1: Drop the org roster always-on Confirmed pill (D-02)</name>
  <files>lib/sigra/admin/live/organization_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/organization_live.ex (roster pills at :100-107 — the sg-list-row / sg-cluster member loop)
    - lib/sigra/admin/live/users_index_live.ex (:393-405 status_pills/1 — the 201 reduced-pill source-of-truth: drops Confirmed because "absence of Unconfirmed implies confirmed — green pills are noise")
    - .planning/phases/203-consistency-propagation/203-UI-SPEC.md (Interaction Contracts -> Status Pills — reduced vocabulary D-02)
  </read_first>
  <action>
    In the member roster loop in organization_live.ex (the `sg-cluster` div inside `sg-list-row`, around :100-107), DELETE exactly the line that renders the always-on green Confirmed pill:
    `<span :if={member.confirmed?} class="sg-status-pill" data-tone="ok">Confirmed</span>` (verified at :105).
    KEEP all four remaining decision-bearing pills unchanged: the role pill (`data-tone={role_tone(member.role)}` / `role_label(member.role)`), `Locked` (`data-tone="risk"`), `Deletion scheduled` (`data-tone="warn"`), and `Unconfirmed` (`:if={not member.confirmed?}` `data-tone="warn"`). This mirrors users_index_live.ex status_pills/1 per D-02: only decision-bearing signals are surfaced; the absence of Unconfirmed already implies confirmed. Do NOT add or rename any sg-* class — this is a pure markup deletion that reuses existing classes (zero new CSS, D-12). Do NOT promote to a shared component — the org roster's lighter sg-list-row pill set does not share the Users Index desktop-table+mobile-card DOM (D-04, RESEARCH A3).
  </action>
  <acceptance_criteria>
    - `grep -c 'data-tone="ok">Confirmed' lib/sigra/admin/live/organization_live.ex` returns 0 (the always-on Confirmed pill is gone)
    - `grep -c 'Unconfirmed' lib/sigra/admin/live/organization_live.ex` returns >= 1 (decision-bearing Unconfirmed pill retained)
    - `grep -c 'Deletion scheduled' lib/sigra/admin/live/organization_live.ex` returns >= 1 (retained)
    - `grep -c 'data-tone="risk">Locked' lib/sigra/admin/live/organization_live.ex` returns >= 1 (retained)
    - No new `sg-` class introduced (the three CSS copies are untouched in this plan)
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && grep -c 'data-tone="ok">Confirmed' lib/sigra/admin/live/organization_live.ex</automated>
  </verify>
  <done>The org roster renders role/Locked/Deletion scheduled/Unconfirmed only; the always-on green Confirmed pill is removed; compile is clean; no new CSS class introduced.</done>
</task>

<!-- planner-discipline-allow: overview-metric-auth-coverage -->
<task type="auto">
  <name>Task 2: Demote the global Authentication coverage chip (D-03)</name>
  <files>lib/sigra/admin/live/index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/index_live.ex (:108-127 — the `overview-metric-auth-coverage` summary_chip: MFA% value + passkey% subvalue + "Coverage uses total users as the denominator." help)
    - lib/sigra/admin/live/users_index_live.ex (status_pills/1 region — the Users Index dropped the coverage KPI entirely as non-decision-bearing, 201 D-03)
    - .planning/phases/203-consistency-propagation/203-UI-SPEC.md (Color -> Phase 203 pill-reduction rule D-03; Open Questions 2 in RESEARCH.md recommends removal to match the Users Index)
  </read_first>
  <action>
    Re-evaluate the global overview's `summary_chip` with `id="overview-metric-auth-coverage"` (`index_live.ex:113-122`: `label="Authentication coverage"`, MFA% value, passkey% subvalue) against the 201 decision to demote non-decision-bearing coverage KPIs (201 D-03). The Users Index removed this coverage KPI entirely. To make the same status signal render identically across surfaces (UI-SPEC Color D-03; RESEARCH Open Question 2 recommendation), REMOVE the `overview-metric-auth-coverage` `<.summary_chip ... />` block from the metrics `<dl>` so `--sg-color-brand-soft` / coverage-KPI noise is dropped on the global overview to match the Users Index. Leave every other `summary_chip` in the `<dl>` (total users, active-this-week, etc.) unchanged. Do NOT restructure the surrounding `<section>`/`<dl>` composition (D-01). If removing leaves an unused helper (e.g. `mfa_users`/`passkey_users` assigns or `percent_of/2` now-unreferenced), only remove a binding if `mix compile --warnings-as-errors` flags it as unused — do not speculatively delete shared helpers used elsewhere. No new sg-* class is introduced (this is a deletion).
  </action>
  <acceptance_criteria>
    - `grep -c 'overview-metric-auth-coverage' lib/sigra/admin/live/index_live.ex` returns 0 (coverage chip removed)
    - `grep -c 'Authentication coverage' lib/sigra/admin/live/index_live.ex` returns 0
    - `mix compile --warnings-as-errors` exits 0 (no unused-binding warnings introduced)
    - No new `sg-` CSS class introduced; the three CSS copies are untouched in this plan
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && grep -c 'overview-metric-auth-coverage' lib/sigra/admin/live/index_live.ex</automated>
  </verify>
  <done>The global overview no longer renders the Authentication coverage chip; the metrics dl is otherwise unchanged; compile is clean with no unused-binding warnings; no new CSS.</done>
</task>

<task type="auto">
  <name>Task 3: Prove both Overviews stay glossary-clean and CSS untouched (D-11/D-12)</name>
  <files>lib/sigra/admin/live/organization_live.ex, lib/sigra/admin/live/index_live.ex</files>
  <read_first>
    - test/sigra/admin/glossary_test.exs (:21-31 @in_scope_files — index_live.ex and organization_live.ex are both scoped)
    - .planning/phases/203-consistency-propagation/203-RESEARCH.md (Pitfall 3 — pill-vocabulary reduction needs ZERO new CSS; A1)
  </read_first>
  <action>
    Confirm the two pill/chip edits introduced NO microcopy synonym drift and NO new sg-* class. Run the glossary drift guard (it scopes both index_live.ex and organization_live.ex per glossary_test.exs:22-23). Confirm the three CSS copies remain byte-identical (no class was added by D-02/D-03; RESEARCH A1/Pitfall 3 — pill reduction reuses existing classes). This task writes no source; it is the in-plan gate that proves the D-12 lockstep was NOT tripped by the Overview pass.
  </action>
  <acceptance_criteria>
    - `mix test test/sigra/admin/glossary_test.exs` passes (no banned synonyms in the two edited files)
    - The three `sigra_admin.css` copies still share one md5 (unchanged; currently `9b281962ee8fe33254829c877af00382`)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && md5 -q priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css | sort -u | wc -l</automated>
  </verify>
  <done>Glossary guard green for both Overviews; the three CSS copies still share a single md5 (the Overview reduction introduced zero new CSS, D-12 untripped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operator -> admin LiveView | Admin-gated read/config surface; already governed by SigraAdminPolicy/platform_admin? (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-01-I | Information disclosure | Org roster / global overview pills | low | accept | Removing pills only reduces surfaced data; no new data exposed. Pills already render server-side from already-authorized assigns. |
| T-203-01-T | Tampering (stored XSS) | Edited HEEx markup | low | mitigate | HEEx auto-escaping preserved; no `raw/1` introduced; edits are pure pill/chip deletions reusing existing escaped bindings. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0
- `grep -c 'data-tone="ok">Confirmed' lib/sigra/admin/live/organization_live.ex` == 0
- `grep -c 'overview-metric-auth-coverage' lib/sigra/admin/live/index_live.ex` == 0
- `mix test test/sigra/admin/glossary_test.exs` passes
- The three sigra_admin.css copies share one md5 (no new CSS class)
</verification>

<success_criteria>
The org roster drops the always-on Confirmed pill; the global overview drops the Authentication coverage chip; both edits reuse existing classes (zero new CSS); both Overviews compile clean and stay glossary-clean; the same status signal now renders identically across the org overview, global overview, and Users Index.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces NO net-new public symbols, components, classes, or docs blocks. It is a pure reductive markup pass:
- REMOVED: org roster `Confirmed`/`ok` always-on pill (`organization_live.ex:105`)
- REMOVED: `overview-metric-auth-coverage` summary_chip (`index_live.ex:113-122`)
No new `sg-*` class, no new component, no new archetype.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/203-consistency-propagation/203-01-SUMMARY.md` when done
</output>
