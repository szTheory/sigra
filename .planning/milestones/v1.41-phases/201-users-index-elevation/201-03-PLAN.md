---
phase: 201
plan: 03
type: execute
wave: 2
depends_on:
  - 201-01
  - 201-02
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - guides/reference/admin-quality-ledger.md
  - guides/reference/admin-design-contract.md
autonomous: true
requirements:
  - INDEX-03
  - INDEX-04
user_setup: []

must_haves:
  truths:
    - "A Playwright test actually SUBMITS the filter form (types a query into the search input and clicks Search / submits) and asserts the filtered result — guarding the D-01/D-02 reflow, which the existing equivalence spec (navigates via pre-built query strings) would NOT catch (D-02, INDEX-03)."
    - "The td:nth-child(3) (Organizations) and td:nth-child(4) (Activity) positional selectors in assertUserResultEquivalence (admin-design.spec.ts:158-159) still target the correct columns after Plan 01's recompose, because the frozen column order was preserved (D-06)."
    - "The users-index-live ledger cell (admin-quality-ledger.md:87) is ratcheted from column-4 integer 1 to a bare single 2 (no decorators) with expanded Evidence citing the applicable Tier-2 proxies; overlay-axe + APG-gate proxies are cited as N/A (no modal), not fabricated (D-09, INDEX-04)."
    - "The stale List Archetype block in admin-design-contract.md:211-243 is rewritten to document the elevated composition (search-first, demoted slim metric strip, contiguous applied chips, <.user_row_fields> DRY note) (D-12)."
  artifacts:
    - "form-submit Playwright test in admin-design.spec.ts (D-02)"
    - "ratcheted users-index-live ledger cell (Tier 2, bare integer) with Tier-2 proxy evidence"
    - "rewritten List Archetype block in admin-design-contract.md"
  key_links:
    - "The new form-submit test drives [data-testid=admin-users-desktop-results] / admin-users-mobile-results after a real form submission (not ?q= navigation)."
    - "Ledger column-4 stays a single [012] integer so the monotonic guard's positional awk -F'|' parse keeps working (D-09)."
    - "The ledger Evidence column mirrors the user-show-live Tier-2 cell (:88) format but cites only applicable proxies and marks overlay-axe/APG N/A."
  prohibitions:
    - statement: "Do NOT change the td:nth-child(3)/(4) selectors (admin-design.spec.ts:158-159) UNLESS the column order changed in Plan 01 — it must not have (D-06 frozen). Only update them if a column was reordered/inserted/removed; otherwise confirm they still target Organizations/Activity and leave them."
      status: resolved
      verification: "Confirm Plan 01 preserved the five-column order; `grep -F 'td:nth-child(3) span' test/example/priv/playwright/tests/admin-design.spec.ts` still reads Organizations and `td:nth-child(4) span` still reads Activity; if Plan 01 moved a column, the selectors are updated to match in this same plan."
    - statement: "Ledger column-4 MUST remain a bare single integer [012] with NO decorators (no markup, no parenthetical) — the monotonic guard's positional `awk -F'|'` parse depends on it (D-09)."
      status: resolved
      verification: "`scripts/ci/quality-ledger-monotonic.sh` (run vs origin/main) exits 0; the users-index-live row column-4 field is exactly `2`."
---

<objective>
Land the test + documentation guards for the elevated Users Index (depends on Plans 01 + 02): add a Playwright test that actually SUBMITS the filter form to guard the D-01/D-02 reflow (the current equivalence spec navigates via pre-built query strings and would miss a broken GET form), confirm the frozen `td:nth-child(3)/(4)` equivalence selectors still target the right columns (D-06), ratchet the `users-index-live` ledger cell to a bare Tier `2` with applicable Tier-2 proxy evidence (D-09), and rewrite the stale List Archetype block in the design contract (D-12).

Purpose: INDEX-03 needs the GET-form contract proven by a real submission, not just pre-built URLs; INDEX-04 needs the ledger ratcheted with proxy evidence and the design contract updated to document the elevated composition.

Output: a new form-submit Playwright test, confirmed/updated equivalence selectors, the ratcheted+expanded ledger cell, and the rewritten List Archetype block — all green, ledger monotonic guard passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-users-index-elevation/201-CONTEXT.md
@.planning/phases/201-users-index-elevation/201-PATTERNS.md
@.planning/phases/201-users-index-elevation/201-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- New Playwright test (e.g. `test('filter form submits via real GET submission', ...)`) in admin-design.spec.ts that types a query into the search input and submits the form, then asserts the filtered result in the desktop + mobile results containers (D-02 guard).
- Ratcheted `users-index-live` ledger cell: column-4 `1` → bare `2`; Evidence expanded to cite content-equivalence (MG-5/6 + live assertUserResultEquivalence), glossary-clean (glossary_test.exs:24), no-`transition: all` (CSS grep), density rhythm (`sg-stack--6` outer / `--4`/`--3` inner), target-size ≥24px; overlay-axe + 7-APG-gate proxies marked N/A (no modal).
- Rewritten List Archetype block (admin-design-contract.md:211-243) matching the UI-SPEC Page Composition Contract.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add a real form-submit Playwright test and confirm the frozen equivalence selectors</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts (assertUserResultEquivalence `:151-164`; td:nth-child(3) Organizations `:158`, td:nth-child(4) Activity `:159`; the users-results testids admin-users-desktop-results / admin-users-mobile-results `:347-351`; firstTexts / expectTokensInBothContainers helpers; existing waitForLiveViewReady usage)
    - lib/sigra/admin/live/users_index_live.ex (the recomposed filter panel from Plan 01: <form method="get">, the search input name/placeholder, the Search submit button copy, the data-testid result containers)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Interaction Contract Filter Form D-02 :288-301; Copywriting Contract search input placeholder "Email, user id, or name" :245, Search submit "Search" :246; Desktop Table Column Order FROZEN :310-320)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (D-02 add a form-submit test `:250-253`; the assertUserResultEquivalence excerpt `:234-245`)
  </read_first>
  <action>
    Add a new Playwright test to admin-design.spec.ts that proves the GET-form submission contract end-to-end (D-02): navigate to the Users index WITHOUT a pre-built query string, locate the search input (by its label/placeholder from the UI-SPEC Copywriting Contract — placeholder "Email, user id, or name"), TYPE a query that matches a known seeded user, SUBMIT the form (click the "Search" submit button or press Enter in the input), wait for LiveView/navigation ready, and assert the filtered result appears in `[data-testid="admin-users-desktop-results"]` AND `[data-testid="admin-users-mobile-results"]`. The distinction from the existing test is that it MUST exercise a real form submission, not `page.goto('?q=...')` — so a filter input accidentally placed outside `<form>` (the D-01 reflow risk) fails this test. Reuse the existing `waitForLiveViewReady` / `firstTexts` / container-locator helpers; keep the test glossary-clean and deterministic (target a stable seeded persona, not a random row).

    Confirm the frozen column order (D-06): verify that `assertUserResultEquivalence`'s `td:nth-child(3) span` (`:158`) still reads Organizations and `td:nth-child(4) span` (`:159`) still reads Activity after Plan 01's recompose. Because Plan 01 preserved the five-column order, NO selector change should be needed — leave `:158-159` as-is. ONLY if Plan 01 moved a column (it must not have), update those selectors in this same change to match the new positions.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && grep -cF 'td:nth-child(3) span' priv/playwright/tests/admin-design.spec.ts && grep -cF 'td:nth-child(4) span' priv/playwright/tests/admin-design.spec.ts && grep -c 'admin-users-desktop-results' priv/playwright/tests/admin-design.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - A new test exists that submits the filter form via a real submission (types into the search input + clicks Search / presses Enter), NOT via a `?q=` pre-built URL — `grep -c 'goto' priv/playwright/tests/admin-design.spec.ts` for the new test shows no `?q=` query-string navigation in that block, and the new test references the search input + submit.
    - `grep -cF 'td:nth-child(3) span' test/example/priv/playwright/tests/admin-design.spec.ts` returns >=1 and `grep -cF 'td:nth-child(4) span' test/example/priv/playwright/tests/admin-design.spec.ts` returns >=1 (frozen positional selectors intact, still reading Organizations / Activity).
    - The new test asserts the filtered result appears in both `admin-users-desktop-results` and `admin-users-mobile-results`.
  </acceptance_criteria>
  <done>A real form-submit Playwright test guards the GET-form reflow, and the frozen td:nth-child(3)/(4) equivalence selectors are confirmed (or updated in lockstep) to read Organizations / Activity.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet the users-index-live ledger cell to bare Tier 2 with applicable proxy evidence</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (the users-index-live row `:87` — column-4 currently `1`; the user-show-live Tier-2 exemplar row `:88` for the Evidence format to mirror; parse rules `:14-27`; idempotency proof method `:115-118`)
    - guides/reference/admin-fractal-scorecard.md (Tier-2 Add-on proxy definitions `~:123-167` — which proxies are applicable vs modal-only)
    - test/sigra/admin/glossary_test.exs (`:24` — confirms glossary-clean proxy already scopes users_index_live)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (D-09 ratchet detail `:281-297`; bare-integer rule `:358-362`)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Quality-Ledger Ratchet Contract :388-416 — the proxy table incl. N/A rows)
  </read_first>
  <action>
    Edit the `users-index-live` row (`admin-quality-ledger.md:87`): flip column-4 from `1` to a bare single `2` — exactly the character `2`, no decorators, no parenthetical, no markup (the monotonic guard's positional `awk -F'|'` parse depends on an undecorated `[012]` integer, D-09). Expand the Evidence column (column-5) mirroring the user-show-live cell's format (`:88`) but citing ONLY the applicable Tier-2 proxies per the UI-SPEC proxy table: content-equivalence (MG-5/6 + the live assertUserResultEquivalence in admin-design.spec.ts, including the new form-submit test), glossary-clean (glossary_test.exs:24 scopes users_index_live), motion-tokens / no-`transition: all` (CSS grep), density rhythm (`sg-stack--6` outer / `sg-stack--4`/`--3` inner sections), and target-size ≥24px (documented-as-manual). Explicitly mark the overlay-axe + 7-APG focus-trap/restore gates as N/A — the Users Index owns no modal dialog (unlike Detail `:88` / Sessions) — do NOT fabricate a modal-interaction reference. Keep the row's pipe structure and column count identical to the surrounding rows.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The `users-index-live` row column-4 field is exactly `2` (bare integer, no decorators) — confirmed by the awk parse: `awk -F'|' '/users-index-live/ {gsub(/ /,"",$4); print $4}' guides/reference/admin-quality-ledger.md` prints `2`.
    - The Evidence column cites content-equivalence, glossary-clean, motion-tokens, density, and target-size proxies, and explicitly marks overlay-axe / APG gates as N/A (the literal `N/A` appears for the modal proxies in that row).
    - `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 (monotonic forward-only ratchet, no regression).
  </acceptance_criteria>
  <done>The users-index-live ledger cell is ratcheted to a bare Tier 2 with applicable proxy evidence and N/A-marked modal proxies, and the monotonic guard passes vs origin/main.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the stale List Archetype block in the design contract</name>
  <files>guides/reference/admin-design-contract.md</files>
  <read_first>
    - guides/reference/admin-design-contract.md (stale List Archetype block `:211-243` — shows markup the code does not render: a sg-page-copy line + a sg-metric-grid inside sg-page-header, metric strip first, applied-chip row as detached post-form sibling; the host-seam blind-spot note `:294`; the Detail Archetype block `:251-296` rewritten by Phase 200 as the format precedent)
    - lib/sigra/admin/live/users_index_live.ex (the recomposed structure from Plan 01 — header emits only kicker + title `:78-81`; metric strip is a separate <section> now demoted below the filter panel)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Page Composition Contract :151-208 — the target elevated structure to document)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (D-12 rewrite detail `:301-311`)
  </read_first>
  <action>
    Rewrite the List Archetype block (`admin-design-contract.md:211-243`) to document the elevated composition from the UI-SPEC Page Composition Contract (`201-UI-SPEC.md:151-208`): search-first ordering (Find users filter panel as the dominant first affordance below the page header), the demoted slim metric strip (User health, below the panel, Total + Locked + Deletion-scheduled chips), the applied-chip row contiguous with the filter panel (not a detached post-form sibling), the reduced decision-bearing pill vocabulary, and the `<.user_row_fields>`-style DRY note (desktop `<td>` + mobile `sg-kv` share one set of field components, frozen five-column order, extra_badges/extra_columns host seam preserved in both layouts). Remove the stale claims: the `sg-page-copy` line and `sg-metric-grid` shown INSIDE `sg-page-header` (the live header emits only kicker + title), the metric-strip-first ordering, and the detached applied-chip sibling. Match the structural style/format of Phase 200's rewritten Detail Archetype block (`:251-296`). Keep the rewrite glossary-clean and use the canonical term "Users".
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c 'user_row_fields' guides/reference/admin-design-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - The rewritten List Archetype block documents search-first ordering, the demoted slim metric strip, the contiguous applied-chip row, and the `<.user_row_fields>` DRY note — `grep -c 'user_row_fields' guides/reference/admin-design-contract.md` returns >=1 and the block no longer shows the metric strip rendering before the filter panel.
    - The stale `sg-page-copy` line inside `sg-page-header` is removed from the List Archetype block (verify by reading `:211-243` region — no `sg-page-copy`/`sg-metric-grid` claimed inside `sg-page-header`).
    - The block uses the canonical term "Users" and stays glossary-clean.
  </acceptance_criteria>
  <done>The List Archetype block documents the elevated search-first composition with the DRY row-fields note; the stale metric-strip-first / detached-chip / sg-page-copy claims are removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → GET filter form submission | the new Playwright test crosses this boundary by typing + submitting; it is the contract guard for D-02 |
| CI monotonic guard → ledger column-4 parse | the ratcheted ledger cell is parsed positionally by `awk -F'|'`; a decorated integer would break the forward-only guard |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-201-03-01 | Tampering | broken GET-form submission undetected (D-02) | high | mitigate | The new form-submit test types + submits the form (not `?q=` navigation), so a filter input placed outside `<form>` during the Plan 01 reflow fails CI. This is the only guard that catches a silently-broken form. |
| T-201-03-02 | Tampering | positional equivalence selectors misaligned (D-06) | high | mitigate | Confirm `td:nth-child(3)/(4)` still target Organizations/Activity post-recompose; update them in the same change if (and only if) a column moved. Prevents the gate silently reading the wrong cells. |
| T-201-03-03 | Repudiation | ledger ratchet integrity (D-09) | medium | mitigate | Column-4 stays a bare `[012]` integer so the monotonic guard's positional parse keeps working; the guard run vs origin/main proves forward-only (no silent down-grade or parse break). |
| T-201-03-04 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — N/A; no new dependencies added. |
</threat_model>

<verification>
- A new Playwright test submits the filter form via a real submission (typed query + submit), asserting the filtered result in both desktop + mobile result containers (D-02).
- `td:nth-child(3)/(4)` equivalence selectors confirmed (or updated in lockstep) to read Organizations / Activity (D-06).
- The users-index-live ledger column-4 is exactly `2` (bare integer); Evidence cites applicable Tier-2 proxies and marks overlay-axe/APG N/A; `quality-ledger-monotonic.sh --base origin/main` exits 0 (D-09).
- The List Archetype block is rewritten to the elevated composition; stale `sg-page-copy`/metric-strip-first/detached-chip claims removed (D-12).
</verification>

<success_criteria>
- The GET-form contract is proven by a real submission test, and the frozen equivalence selectors are confirmed intact (INDEX-03).
- The ledger cell is ratcheted to a bare Tier 2 with applicable proxy evidence and the monotonic guard passes; the design contract documents the elevated composition (INDEX-04).
</success_criteria>

<output>
Create `.planning/phases/201-users-index-elevation/201-03-SUMMARY.md` when done.
</output>