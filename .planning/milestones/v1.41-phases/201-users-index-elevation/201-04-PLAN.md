---
phase: 201
plan: 04
type: execute
wave: 3
depends_on:
  - 201-01
  - 201-02
  - 201-03
files_modified:
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
  - test/example/priv/playwright/tests/admin-design.spec.ts
autonomous: true
requirements:
  - INDEX-03
  - INDEX-04
user_setup: []

must_haves:
  truths:
    - "Honest pagination is PROVEN at list-scale: with the Phase-199 cohort seeded (36 loadtest-* + 9 personas = 45 users → 2 pages at page_size 25), the global-user-index checkpoint renders the pagination <nav> (multi-page) rather than the single-page all_results_label (D-08, INDEX-03)."
    - "Only the affected non-canary slugs are recaptured through snapshot-recapture-gate.sh: global-user-index (3 PNGs: -chromium/-dark/-mobile) plus mg-1/mg-2/mg-5/mg-6 design-gallery boards iff their markup changed (D-10, INDEX-04)."
    - "The relevant canaries stay byte-stable (impersonation-banner for checkpoints, board-notice for design); zero-drift idempotency is proven before baking new baselines; allowlists are empty at end-of-phase (D-10)."
  artifacts:
    - "recaptured global-user-index baseline PNGs (3 projects) reflecting the elevated, list-scale composition"
    - "recaptured mg-1/mg-2/mg-5/mg-6 board baselines (only if those boards' markup changed)"
  key_links:
    - "Capture MUST run against a dev DB with seeds run (MIX_ENV=dev-only; hard-blocked in test) so the list-scale cohort renders the pagination <nav> — otherwise the overflow elevation is captured under-populated and unproven (D-08)."
    - "Recapture routes through snapshot-recapture-gate.sh (changed slugs only), NOT the canary guard; the impersonation-banner / board-notice canaries must NOT change."
  prohibitions:
    - statement: "Do NOT touch the canary slugs (impersonation-banner checkpoints, board-notice design) and do NOT leave any recapture allowlist non-empty at end-of-phase — Phase 204 owns terminal allowlist reset; 201 must stay green on its own (D-10)."
      status: resolved
      verification: "`git diff --name-only` shows NO change to the impersonation-banner or board-notice canary baseline PNGs; the recapture-gate allowlist files are empty at end-of-phase; `snapshot-canary-guard.sh` passes."
    - statement: "Pagination evidence MUST be captured at list-scale (multi-page), not on an under-populated single-page list — the capture harness must boot a seeded dev DB (D-08)."
      status: resolved
      verification: "The recaptured global-user-index baseline shows the pagination <nav> (multi-page state); the capture was run against a dev DB with the Phase-199 seed cohort (45 users → 2 pages)."
---

<objective>
Prove the honest-pagination elevation at list-scale and bake the affected baselines (depends on Plans 01-03): boot the capture harness against a seeded dev DB so the Phase-199 cohort (45 users → 2 pages at page_size 25) renders the pagination `<nav>` (D-08), then recapture ONLY the affected non-canary slugs (`global-user-index` + any changed `mg-*` boards) through the recapture gate with zero-drift idempotency proven and canaries byte-stable (D-10).

Purpose: INDEX-03's honest-pagination clause is already implemented but must be PROVEN at list-scale (not on an under-populated single-page list); INDEX-04 requires the page proven award-grade across the full matrix with baselines recaptured cleanly through the gate and allowlists left empty.

Output: recaptured `global-user-index` baselines (3 projects) showing the elevated list-scale composition with pagination, recaptured `mg-*` boards iff their markup changed, canaries intact, zero-drift idempotency proven, allowlists empty.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-users-index-elevation/201-CONTEXT.md
@.planning/phases/201-users-index-elevation/201-PATTERNS.md
@.planning/phases/201-users-index-elevation/201-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- Recaptured `global-user-index` baseline PNGs (`-chromium`, `-dark`, `-mobile`) reflecting the elevated, list-scale composition (pagination nav visible).
- Recaptured `mg-1` (metric strip), `mg-2` (filter/applied-chip), `mg-5` (Users results + pagination), `mg-6` (audit feed) board baselines — ONLY if the live markup those boards mirror changed in Plans 01-03.
- A proven zero-drift idempotency result (a second capture run produces no diff) before baselines are baked.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Sync the mg-1/mg-2/mg-5/mg-6 design-gallery boards to the elevated live markup (only if changed)</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - test/example/lib/example_web/live/admin/design_gallery_live.ex (board-mg-1 metric strip `:455-488`; board-mg-2 filter/applied-chip `:489-...`; board-mg-5 Users results + pagination `:611-733`, mg-5-desktop-results `:616` / mg-5-mobile-results `:665`; board-mg-6 audit feed `:734-...`; board-notice canary)
    - lib/sigra/admin/live/users_index_live.ex (the recomposed live markup from Plan 01 — the boards mirror the live pill set / column structure / applied-chip placement / metric strip)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (design gallery boards conditional recapture `:315-323`; board-notice canary must stay byte-stable)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Snapshot Recapture Scope :405-416)
  </read_first>
  <action>
    Determine whether the live markup each board mirrors changed in Plans 01-03 (reduced pill set, demoted/slim metric strip, contiguous applied-chip placement, DRY column structure). For each board whose mirrored markup CHANGED, update the board's static markup in design_gallery_live.ex to match the elevated live composition so the gallery stays an honest evidence surface (mg-1 metric strip → 3-chip slim set; mg-2 filter board → contiguous applied chips; mg-5 Users results → reduced pills + DRY column structure + pagination; mg-6 audit feed → only if its shared markup moved). If a board's mirrored markup did NOT change, leave that board untouched. Do NOT touch the `board-notice` canary. This task only edits the gallery boards' markup; the actual baseline recapture happens in Task 2. Keep board copy glossary-clean.

    Note: this file (admin-design.spec.ts) is also touched by Plan 03 (the form-submit test); this plan is Wave 3 and runs after Plan 03, so coordinate by appending/adjusting board-recapture-related assertions without reverting Plan 03's form-submit test. (The gallery boards themselves live in design_gallery_live.ex; admin-design.spec.ts drives them — read both before editing.)
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && grep -c 'board-notice' priv/playwright/tests/admin-design.spec.ts && grep -c 'mg-5' lib/example_web/live/admin/design_gallery_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - Any board whose live-mirrored markup changed in Plans 01-03 is updated to match (mg-1 shows 3 slim chips if the metric strip changed; mg-5 shows the reduced pill set + DRY column structure if the table changed); boards whose markup is unchanged are left untouched.
    - The `board-notice` canary markup is unchanged (`git diff` shows no change to the board-notice board).
    - `cd test/example && mix compile --warnings-as-errors` succeeds (design_gallery_live.ex compiles).
  </acceptance_criteria>
  <done>The mg-* gallery boards mirror the elevated live composition (only where markup changed); the board-notice canary is untouched; the gallery compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Recapture global-user-index (+ changed mg-* boards) at list-scale against a seeded dev DB; prove zero-drift; leave allowlists empty</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts (global-user-index block `:210-216`: goto `/admin/users?q=...`, captureAndVerify + assertCheckpointScreenshot 'global-user-index' `:215-216`; impersonation-banner canary)
    - scripts/ci/snapshot-recapture-gate.sh (slug-arg routing for changed-slug recapture) + scripts/ci/snapshot-canary-guard.sh (impersonation-banner / board-notice canaries)
    - test/example/lib/example/demo/seeds.ex (`:11-13`, `:43-47`, `:96-119` — the Phase-199 list-scale cohort: 36 loadtest-* users + admin persona events → 45 users)
    - guides/reference/admin-quality-ledger.md (`:115-118` — Phase-192/199 zero-drift idempotency proof method)
    - CLAUDE.md (Admin baseline auto-gate + Admin checkpoint Playwright recapture notes — boot dev server on alt PORT, pre-compile to avoid code-reload crash, seeds are MIX_ENV=dev-only)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (Snapshot recapture routing `:364-369`; checkpoint recapture detail `:257-277`)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Snapshot Recapture Scope :405-416 — blast radius, canaries, seeded dev DB requirement)
  </read_first>
  <action>
    Recapture the affected non-canary baselines through the recapture gate, against a SEEDED dev DB so the list-scale pagination renders (D-08). Steps:

    (1) Boot the capture harness against a dev DB with the Phase-199 seed cohort run (`MIX_ENV=dev` seeds; seeds are hard-blocked in test, so this MUST be a dev DB) — follow the CLAUDE.md "Admin checkpoint Playwright" recapture playbook (boot dev server on an alt PORT to avoid the Rulestead Docker 4000 collision, pre-compile to avoid the code-reload crash). Confirm the index shows 45 users → 2 pages at page_size 25 (the pagination `<nav>` renders, not the single-page `all_results_label`).

    (2) Recapture ONLY the affected non-canary slugs through `snapshot-recapture-gate.sh`: `global-user-index` (3 PNGs: `-chromium`, `-dark`, `-mobile`) and any `mg-1`/`mg-2`/`mg-5`/`mg-6` boards whose markup changed in Task 1. Do NOT route through the canary guard. Do NOT recapture the `impersonation-banner` (checkpoints) or `board-notice` (design) canaries.

    (3) Prove zero-drift idempotency (Phase-192/199 method, ledger `:115-118`): run the capture a second time and confirm it produces no diff against the just-baked baselines before considering them final.

    (4) Confirm the canaries stayed byte-stable (`snapshot-canary-guard.sh` passes; the impersonation-banner / board-notice baseline PNGs are unchanged in `git diff`) and leave both recapture-gate allowlists EMPTY at end-of-phase (Phase 204 owns terminal reset; 201 stays green on its own).

    If the global-user-index checkpoint navigation uses a `?q=` filter that would collapse the list to a single user (hiding pagination), adjust the checkpoint to land on the unfiltered (or broadly-filtered) list page so the multi-page `<nav>` is the captured state — the point is to PROVE list-scale overflow.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/snapshot-canary-guard.sh 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The recaptured `global-user-index` baselines (`-chromium`, `-dark`, `-mobile`) show the elevated list-scale composition with the pagination `<nav>` visible (multi-page state, not the single-page label).
    - Recapture ran through `snapshot-recapture-gate.sh` for `global-user-index` (+ any changed `mg-*` boards) only; `git diff --name-only` shows NO change to the `impersonation-banner` or `board-notice` canary baseline PNGs.
    - A second capture run produces zero drift against the baked baselines (idempotency proven).
    - `bash scripts/ci/snapshot-canary-guard.sh` passes; both recapture-gate allowlist files are EMPTY at end-of-phase.
  </acceptance_criteria>
  <done>global-user-index (+ changed mg-* boards) are recaptured at list-scale against a seeded dev DB showing pagination; canaries stay byte-stable; zero-drift idempotency is proven; allowlists are left empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture harness → baked baseline PNGs | recaptured baselines become the merge-blocking visual contract; capturing under-populated or canary-touching state corrupts the contract |
| seed cohort (dev DB) → captured list-scale state | the list-scale pagination evidence depends on a correctly seeded dev DB (MIX_ENV=dev-only) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-201-04-01 | Tampering | under-populated capture hides pagination (D-08) | high | mitigate | Capture MUST run against a seeded dev DB (45 users → 2 pages); the acceptance criteria require the baked baseline to show the pagination `<nav>` (multi-page), so an under-populated single-page capture fails the gate. |
| T-201-04-02 | Tampering | canary drift / non-empty allowlist (D-10) | high | mitigate | Recapture routes ONLY through snapshot-recapture-gate.sh for changed slugs; snapshot-canary-guard.sh proves impersonation-banner / board-notice stayed byte-stable; allowlists left empty so the phase stays green independently. |
| T-201-04-03 | Repudiation | non-idempotent baselines | medium | mitigate | Zero-drift idempotency is proven (Phase-192/199 method) before baking — a second run produces no diff, so the baseline is stable and reproducible. |
| T-201-04-04 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — N/A; no new dependencies added. |
</threat_model>

<verification>
- The recaptured global-user-index baselines show list-scale pagination (`<nav>`) in all three projects (chromium/dark/mobile) (D-08).
- Recapture routed through snapshot-recapture-gate.sh for global-user-index (+ changed mg-* boards) only; canaries (impersonation-banner / board-notice) byte-stable (D-10).
- Zero-drift idempotency proven before baking; recapture-gate allowlists empty at end-of-phase.
- `snapshot-canary-guard.sh` passes.
</verification>

<success_criteria>
- Honest pagination is proven award-grade at list-scale (45 users → 2 pages) in the recaptured baselines (INDEX-03).
- Only the affected non-canary slugs are recaptured cleanly through the gate, canaries intact, idempotency proven, allowlists empty (INDEX-04).
</success_criteria>

<output>
Create `.planning/phases/201-users-index-elevation/201-04-SUMMARY.md` when done.
</output>
