---
phase: 201
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/live/users_index_live.ex
  - lib/sigra/admin/users/query.ex
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
autonomous: true
requirements:
  - INDEX-01
  - INDEX-02
  - INDEX-03
user_setup: []

must_haves:
  truths:
    - "All filter controls (search row, quick-filter checkboxes, advanced disclosure, and the applied-filter-chip row) appear inside or visually contiguous with one sg-filter-panel; the applied-chip block is no longer a detached sibling after </form> (D-01, INDEX-01)."
    - "The User health metric strip renders BELOW the Find users filter panel in DOM + visual order (search is the dominant first affordance), is slimmed to Total + Locked + Deletion-scheduled chips, and is not deleted or hidden behind <details> (D-03, INDEX-02)."
    - "Per-row status pills are reduced to Unconfirmed / Locked / Deletion scheduled plus a surfaced No MFA (warn tone) when unsecured; the always-on Confirmed pill is dropped and the four-way security pill is collapsed; fully-secured rows show zero security pills (D-04, INDEX-02)."
    - "Desktop <td> cells and mobile sg-kv share one set of field-emitting helpers/components so the per-row presentation is authored once (DRY); desktop table semantics and the sg-kv mobile pattern are both preserved (D-05, INDEX-03)."
    - "sg-chevron is resolved across all three byte-identical CSS copies — either styled byte-identically in all three or removed from the LiveView markup (D-11)."
  artifacts:
    - "lib/sigra/admin/live/users_index_live.ex (recomposed: search-first, demoted slim metric strip, reduced pills, DRY shared row fields)"
    - "shared per-row field component(s) (e.g. <.user_row_fields> or field-slice components) feeding both desktop <td> and mobile sg-kv"
  key_links:
    - "applied_chips/1, any_filter_active?/1, remove_chip_path/4 reused unchanged for the relocated applied-chip row (data already present)."
    - "extra_badges / extra_columns from decorate_rows/2 render in BOTH the desktop status/activity <td> cells and the mobile cluster/dl via the shared component (D-07 seam preserved on the markup side)."
    - "summary_stats/3 stays intact; empty_summary_stats/0 + summary_count/2 stay shape-consistent so a dropped chip key cannot silently mask a wiring bug (D-03)."
  prohibitions:
    - statement: "PRESERVE the GET-form submission contract (D-02): keep `<form method=\"get\" action={index_path(...)}>` (:156) and quick-filter chips as form checkboxes (name={@key}, value \"true\"); do NOT convert quick-filter chips to phx-click. Only toggle_filters stays a LiveView event. Every named filter input must stay INSIDE the <form>; relocated applied chips must be navigation-only <a> tags with NO named inputs. If any input lands outside the form, search silently stops submitting."
      status: verified-in-plan-03
      verification: "`grep -F 'method=\"get\"' lib/sigra/admin/live/users_index_live.ex` returns >=1; `grep -c 'phx-click=\"toggle_filters\"' lib/sigra/admin/live/users_index_live.ex` returns >=1; the quick-filter checkboxes still carry `name=` and no quick-filter chip uses phx-click; Plan 03 adds a Playwright test that TYPES a query and SUBMITS the form (not pre-built query strings) and asserts the filtered result."
    - statement: "FROZEN desktop column order User / Status / Organizations / Activity / Action (headers :253-257) MUST be preserved exactly five <td> cells in that order (D-06), because assertUserResultEquivalence reads td:nth-child(3) (Organizations) and td:nth-child(4) (Activity) positionally. If any column is reordered/inserted/removed, the admin-design.spec.ts:158-159 selectors MUST be updated in the same change."
      status: verified-in-plan-03
      verification: "`grep -c '<th' lib/sigra/admin/live/users_index_live.ex` for the desktop thead yields 5 columns in order User/Status/Organizations/Activity/Action; the desktop <tr> emits exactly 5 <td>; Plan 03 confirms td:nth-child(3)/(4) selectors still target Organizations/Activity."
    - statement: "FROZEN host-seam contract (D-07): extra_badges and extra_columns MUST survive in BOTH the desktop and mobile copies of the DRY refactor. badge_text/1 and column_text/2 helpers stay part of the contract. Do not drop either seam from either layout."
      status: verified-in-plan-02
      verification: "`grep -c 'extra_badges' lib/sigra/admin/live/users_index_live.ex` shows the badge seam rendered (desktop status <td> + mobile cluster); `grep -c 'extra_columns' lib/sigra/admin/live/users_index_live.ex` shows the column seam rendered (desktop activity <td> + mobile dl); Plan 02 makes the example hook emit non-empty values so the live equivalence spec exercises both seams."
---

<objective>
Recompose `lib/sigra/admin/live/users_index_live.ex` in place into an award-grade, search-first operator list: consolidate the detached applied-filter-chip block into the filter panel (D-01), demote + slim the User health metric strip below the search panel (D-03), reduce per-row status pills to decision-bearing signals (D-04), DRY the duplicated desktop-table and mobile-card per-row presentation behind a shared component while preserving the frozen column order (D-05, D-06), keep the GET-form submission contract intact (D-02), and resolve the unstyled `sg-chevron` across the three CSS copies (D-11).

Purpose: INDEX-01 wants one coherent filter panel; INDEX-02 wants search as the dominant first affordance with reduced pill noise; INDEX-03 wants a DRY, content-equivalent desktop/mobile presentation. This is the heart of the phase — all data helpers already exist; this is a markup/composition recompose plus a `status_pills/1` body trim.

Output: a recomposed `users_index_live.ex` that compiles clean, reuses existing `sg-*` primitives and presentation helpers, keeps the GET form + frozen column order + host seam intact, and a resolved `sg-chevron` (styled in all three CSS copies or dropped) with the three copies still byte-identical.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-users-index-elevation/201-CONTEXT.md
@.planning/phases/201-users-index-elevation/201-PATTERNS.md
@.planning/phases/201-users-index-elevation/201-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan introduces / changes the following symbols (Plans 02-04 depend on them):
- New shared per-row component(s): a `<.user_row_fields row={row} />`-style function component (name + arg shape at Claude's discretion per UI-SPEC :143-145), feeding BOTH the desktop `<td>` cells and the mobile `sg-kv`/card. Preferred shape per PATTERNS.md (A): small field-slice components so the desktop `<td>` boundaries (which td:nth-child selectors depend on) stay authored in the table while the inner content is shared.
- Reduced `status_pills/1` body: returns Unconfirmed / No MFA (warn) / Locked / Deletion scheduled only — Confirmed dropped, security pill collapsed.
- Recomposed render/1: section order is page-header → scope_ribbon → Find users filter panel (with contiguous applied-chip row) → demoted slim User health strip → desktop table → mobile cards → empty state → honest pagination.
- `sg-chevron` resolution: either new byte-identical CSS rules in all three `sigra_admin.css` copies OR removal of the `<span class="sg-chevron">` usage at :185 (Claude's discretion per D-11). No OTHER new `sg-*` class is expected — reuse existing primitives.
- No new data logic in `query.ex` — it is keep-in-sync-only (touch only if a dropped chip key requires `empty_summary_stats/0` parity; prefer keeping all keys).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reduce status_pills/1 to decision-bearing signals</name>
  <files>lib/sigra/admin/live/users_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/users_index_live.ex (status_pills/1 `:415-430` — current body returns [confirmation, security] then appends Locked/Deletion; the always-Confirmed branch `:417`; the 4-way security cond `:419-425`; maybe_append/3 `:432-433`)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Reduced Pill Vocabulary table `:214-229`; No MFA uses data-tone="warn" not risk `:114`)
    - lib/sigra/admin/users/query.ex (needs_review filter `:353-357` — depends on the Locked signal; do NOT drop Locked)
  </read_first>
  <action>
    Rewrite the `status_pills/1` body (`:415-430`) to return only decision-bearing pills, reusing the existing `maybe_append/3` (`:432-433`, which takes a truthy/nil first arg). Drop the always-present `{"Confirmed", "ok"}` branch (`:417`). Collapse the four-way security `cond` (`:419-425`, MFA+passkeys / MFA / Passkeys / No MFA) to surface only `{"No MFA", "warn"}` when the row is unsecured (no MFA and no passkeys) and NOTHING when secured — fully-secured rows show zero security pills (UI-SPEC `:229`). Use `warn` tone for No MFA, NOT `risk` (UI-SPEC `:114`). Keep `Unconfirmed` (warn, on `is_nil(row.user.confirmed_at)`), `Locked` (risk, on `row.user.locked_at`), and `Deletion scheduled` (warn, on `row.user.deleted_at`). Do NOT drop `Locked` — it is the at-a-glance signal for the `needs_review` filter (query.ex `:353-357`). Introduce a small private predicate (e.g. `no_security?/1`) for the unsecured check that mirrors how the current security `cond` reads MFA/passkey state. Keep all copy glossary-clean.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"Confirmed"' lib/sigra/admin/live/users_index_live.ex` returns 0 (the always-on Confirmed pill is gone from the index).
    - `grep -F '"No MFA"' lib/sigra/admin/live/users_index_live.ex` returns >=1 and the No MFA pill carries the `warn` tone (not `risk`).
    - `grep -F '"Locked"' lib/sigra/admin/live/users_index_live.ex` and `grep -F '"Deletion scheduled"' lib/sigra/admin/live/users_index_live.ex` and `grep -F '"Unconfirmed"' lib/sigra/admin/live/users_index_live.ex` each return >=1.
    - `grep -F '"MFA + passkeys"' lib/sigra/admin/live/users_index_live.ex` returns 0 (the 4-way security pill is collapsed).
    - `mix compile --warnings-as-errors` succeeds with no warnings.
  </acceptance_criteria>
  <done>status_pills/1 returns only Unconfirmed / No MFA (warn) / Locked / Deletion scheduled; Confirmed is dropped, the security pill is collapsed, secured rows show no security pill, and the module compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: DRY the per-row presentation behind a shared component (frozen column order + host seam)</name>
  <files>lib/sigra/admin/live/users_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/users_index_live.ex (desktop <thead> `:253-257`; desktop <tr> `:261-295` with status pills `:271-275`, extra_badges desktop `:274`, org summary `:277-282`, activity/extra_columns desktop `:283-289`; mobile <article> `:305-345` with status pills `:313-317`, extra_badges mobile `:316`, org `:319-326`, activity/extra_columns mobile `:335-337`; field helpers primary_name/1 `:641-643`, activity_label/1 `:645-650`, registered_label/1 `:652-654`, badge_text/1 `:656-658`, column_text/2 `:660-667`, pluralize/2 `:669-670`)
    - lib/sigra/admin/live/audit_index_live.ex (the in-repo DRY precedent: desktop <table> hand-authored <td> + <.audit_row :for=...> mobile `:148-203`)
    - lib/sigra/admin/components.ex (audit_row/1 attr block + <article> body `:685-714` — the shape to mirror for the shared component)
    - lib/sigra/admin/users/query.ex (decorate_rows/2 host-seam reads `:534-549` — extra_badges/extra_columns keys that MUST survive both layouts, D-07)
    - test/example/priv/playwright/tests/admin-design.spec.ts (assertUserResultEquivalence `:151-164`; positional td:nth-child(3) Organizations `:158`, td:nth-child(4) Activity `:159` — D-06)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (D-05 mechanism (A) field-slice components `:43-91`; D-06 frozen column order `:93-101`; D-07 host-seam preservation `:337-342`)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Page Composition Contract :177-201; Component Inventory new shared component note :143-145; Desktop Table Column Order FROZEN :310-320; Host-Seam Preservation :322-324)
  </read_first>
  <action>
    Introduce a shared per-row field component (preferred per PATTERNS.md (A): small private field-slice function components in this module — one for the name/email/id stack, one for the status-pills + extra_badges cluster, one for the org summary, one for the activity/registered + extra_columns group — OR a single `<.user_row_fields layout={...}>`; name + arg shape at Claude's discretion). Call these shared components from BOTH the desktop `<td>` cells and the mobile `<dl class="sg-kv">`/card so each logical field group is authored ONCE. Keep the field-emitting helpers (`primary_name/1`, `activity_label/1`, `registered_label/1`, `badge_text/1`, `column_text/2`, `pluralize/2`) unchanged and call them from inside the shared component.

    Preserve the desktop table semantics: keep exactly FIVE `<td>` cells per `<tr>` in the FROZEN order User / Status / Organizations / Activity / Action (matching the `<thead>` at `:253-257`) — do NOT reorder, insert, or remove a column (D-06). The desktop `<td>` boundaries stay authored in the table (the `td:nth-child(3)`/`(4)` equivalence selectors depend on Organizations being the 3rd cell and Activity the 4th); only the inner content is shared. Preserve the mobile `sg-kv` `<dl>` pattern.

    Preserve the host seam in BOTH layouts (D-07): `extra_badges` must render in the Status cell/cluster (desktop `:274`, mobile `:316`) and `extra_columns` must render in the Activity cell/dl (desktop `:287`, mobile `:335-337`) — both seams in both the desktop and mobile copies. Do not CSS-collapse the two layouts into one markup block (that would abandon the table semantics and the sort headers). This mirrors the audit feed's separate-DOM + shared-token precedent, extended to feed both layouts.

    Make no behavior change to `query.ex`. Keep all copy glossary-clean.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The desktop `<thead>` renders the five columns in order User, Status, Organizations, Activity, Action (verify by inspecting the thead block; `grep -F '>User<' ... >Status< >Organizations< >Activity< >Action<` all present and in that source order) and the desktop `<tr>` emits exactly five `<td>` cells.
    - `grep -c 'extra_badges' lib/sigra/admin/live/users_index_live.ex` shows the badge seam rendered in both desktop and mobile contexts (>=2 render sites), and `grep -c 'extra_columns' lib/sigra/admin/live/users_index_live.ex` shows the column seam rendered in both (>=2 render sites).
    - The shared row-field component is defined once and invoked from both the desktop `<td>` region and the mobile card region (the duplicated name/email/id and pills blocks at `:262-268`/`:306-310` and `:269-276`/`:312-317` are no longer hand-duplicated).
    - `mix compile --warnings-as-errors` succeeds with no warnings.
  </acceptance_criteria>
  <done>Per-row presentation is authored once behind a shared component feeding both desktop <td> and mobile sg-kv; the frozen five-column order is preserved; both host seams render in both layouts; the module compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate filter panel (relocate applied chips, demote slim metric strip) + resolve sg-chevron; preserve GET form</name>
  <files>lib/sigra/admin/live/users_index_live.ex, priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</files>
  <read_first>
    - lib/sigra/admin/live/users_index_live.ex (metric strip <section aria-labelledby="users-health-heading"> `:91-149` rendering first; summary_chip count bindings `:85-90`; the six summary_chip calls Total `:94-100`, Confirmed `:101-109`, MFA `:110-118`, Passkeys `:119-127`, Locked `:128-137`, Deletion `:138-147`; filter form <form method="get"> `:156`, search row `:157-171`, quick chips `:173-175`, more-filters disclosure `:177-229`, sg-chevron span `:185`; detached applied-chip block after </form> `:236-243`; applied_chips/1 `:531-550`, any_filter_active?/1 `:524-528`, remove_chip_path/4 `:566-574`; toggle_filters `:66-68`; index_path `:606-622`; empty_summary_stats/0 `:465-478`; summary_count/2 `:483-484`; @quick_filter_keys/@more_filter_keys `:14-15`)
    - lib/sigra/admin/components.ex (applied_chip/1 `:369`; quick_filter checkbox shape)
    - priv/templates/sigra.install/admin/sigra_admin.css (existing sg-filter-panel, sg-search-row, sg-cluster, sg-metric-grid primitives; sg-chevron currently 0 rules)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (D-01 consolidate `:140-150`; D-03 demote+slim `:125-138`; CSS triple-copy + sg-chevron decision `:210-227`; GET-form contract `:351-356`)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Page Composition Contract :151-208; Interaction Contract Filter Form D-02 :288-301; Applied-Chip Panel Placement :303-308; metric strip slim to Total+Locked+Deletion :128-176)
  </read_first>
  <action>
    Three coordinated edits to `users_index_live.ex`, all preserving the GET-form contract (D-02):

    (1) Consolidate applied chips (D-01): move the detached applied-chip block (the `<div :if={any_filter_active?(@current_params)}>` sibling after `</form>` at `:236-243`) up to sit directly below the search row (`:157-171`), inside or visually contiguous with the `sg-filter-panel`. Reuse `applied_chips/1`, `any_filter_active?/1`, `remove_chip_path/4`, and `<.applied_chip>` unchanged — the data already exists, this is relocation only. CRITICAL (D-02): the chips must remain navigation-only `<a>` tags (no named inputs); if they move INSIDE `<form>`, ensure no chip introduces a named input that would alter GET submission. Keep `<form method="get" action={index_path(@admin_scope)}>` (`:156`) and the checkbox-based `quick_filter/1` exactly; only `toggle_filters` (`:66-68`) stays a LiveView event. Keep the "Clear all" link.

    (2) Demote + slim the metric strip (D-03): move the `<section aria-labelledby="users-health-heading">` (`:91-149`) BELOW the Find users filter `<section>` (`:151-234`) in DOM + visual order so search renders first. Keep it visible — no `<details>`, no delete. Cut the six `<.summary_chip>` calls to THREE: Total (`:94-100`), Locked (`:128-137`), Deletion scheduled (`:138-147`); drop Confirmed (`:101-109`), MFA (`:110-118`), Passkeys (`:119-127`) and remove their now-unused `<% ... %>` count bindings (`:85-90`). Do NOT touch `Query.summary_stats/3` (it computes all six cheaply). Keep `empty_summary_stats/0` (`:465-478`) and `summary_count/2` (`:483-484`) shape intact so a dropped key cannot silently mask a wiring bug (if a dropped chip key would otherwise leave an unreferenced key, leave the key in the stats map — prefer keeping all keys per PATTERNS.md).

    (3) Resolve sg-chevron (D-11): the `<span class="sg-chevron">▾</span>` at `:185` has ZERO CSS rules in all three copies. Either (a) drop the span from the markup, or (b) add byte-identical CSS rules for `sg-chevron` to ALL THREE copies (priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css). If adding rules, write them byte-identically (same bytes, same position) so the golden-diff md5 parity holds; use only `--sg-*` tokens and never `transition: all`. Prefer (a) drop if the chevron carries no decoration value — fewest moving parts. Introduce NO other new `sg-*` class (reuse existing primitives for the consolidated panel).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -3 && echo "---css-parity---" && md5 -q priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The applied-chip row no longer renders as a detached sibling after `</form>`; it renders inside or directly contiguous with the `sg-filter-panel` below the search row (verify by source position: the `applied_chips`/`<.applied_chip>` render site precedes `</form>` or sits immediately adjacent to the panel, not after the metric strip).
    - `grep -F 'method="get"' lib/sigra/admin/live/users_index_live.ex` returns >=1 and `grep -c 'phx-click="toggle_filters"' lib/sigra/admin/live/users_index_live.ex` returns >=1; no quick-filter chip uses `phx-click` (quick-filter checkboxes still carry `name=`).
    - The User health `<section aria-labelledby="users-health-heading">` renders AFTER the Find users filter `<section>` in source order (search-first); it is not wrapped in `<details>` and is not deleted.
    - Exactly three `<.summary_chip` calls remain in the metric strip and they are Total + Locked + Deletion scheduled; `grep -c '<.summary_chip' lib/sigra/admin/live/users_index_live.ex` returns 3.
    - `sg-chevron` is resolved: EITHER `grep -c 'sg-chevron' lib/sigra/admin/live/users_index_live.ex` returns 0 (dropped) OR `grep -c 'sg-chevron' <each of the 3 css copies>` returns the same non-zero count in all three.
    - The three CSS copies remain byte-identical: `md5 -q <3 copies> | sort -u | wc -l` returns 1.
    - `mix compile --warnings-as-errors` succeeds with no warnings.
  </acceptance_criteria>
  <done>The applied-chip row is consolidated into the filter panel, the slim 3-chip metric strip is demoted below search, the GET form contract is intact, sg-chevron is resolved, the three CSS copies stay byte-identical, and the module compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → GET filter form / URL query params | filter + pagination state crosses from the client as URL/query params (untrusted) and drives the list query |
| host app → extra_list_badges / extra_list_columns seam | generated-host-provided badge/column data renders into the operator-facing list markup |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-201-01 | Tampering | filter GET-form submission contract (D-02) | high | mitigate | Keep `<form method="get" action={index_path(...)}>` and quick-filter checkboxes; relocate applied chips as navigation-only `<a>` with NO named inputs; only `toggle_filters` stays a LiveView event. A misplaced input outside the form silently breaks search submission. Plan 03 adds a Playwright test that TYPES + SUBMITS the form (the current equivalence spec navigates via pre-built query strings and would miss a broken form). |
| T-201-02 | Tampering/Repudiation | frozen desktop column order (D-06) | high | mitigate | Preserve exactly five `<td>` in User/Status/Organizations/Activity/Action order so the positional `td:nth-child(3)/(4)` equivalence selectors keep reading the correct cells. Any column move requires updating admin-design.spec.ts:158-159 in the same change (tracked in Plan 03). |
| T-201-03 | Tampering | host-seam contract extra_badges/extra_columns (D-07) | high | mitigate | Both seams must survive in BOTH desktop and mobile copies of the DRY refactor. The example returns `[]` so CI alone won't catch a one-sided drop — Plan 02 makes the example emit non-empty values so the live equivalence spec exercises the seam. |
| T-201-04 | Information disclosure | reduced status pills (D-04) | low | accept | Pills are derived from already-loaded row state (confirmed_at/locked_at/deleted_at/MFA/passkey); reducing which pills render exposes no new data — it only changes which already-authorized signals are surfaced. No new data path. |
| T-201-05 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — N/A; no new dependencies added. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (library).
- `grep -c '"Confirmed"'` → 0; `"No MFA"` present with warn tone; Locked/Unconfirmed/Deletion scheduled present.
- Desktop thead = 5 columns (User/Status/Organizations/Activity/Action); desktop `<tr>` = 5 `<td>`.
- extra_badges + extra_columns render in both desktop and mobile.
- GET form (`method="get"`) intact; quick-filter chips are checkboxes (no phx-click); only `toggle_filters` is a LiveView event.
- Metric strip renders AFTER the filter section; exactly 3 summary_chip calls (Total/Locked/Deletion).
- sg-chevron resolved (dropped or styled in all three CSS copies); the three `sigra_admin.css` copies remain byte-identical (md5 parity = 1).
</verification>

<success_criteria>
- Filter controls (search + quick toggles + advanced + applied chips) read as one coherent panel; applied chips no longer detached after `</form>` (INDEX-01).
- Metric strip is demoted below search and slimmed to 3 decision-bearing chips; per-row pills reduced to decision-bearing signals (INDEX-02).
- Desktop `<td>` and mobile `sg-kv` share one set of field components (DRY) with frozen column order + host seam preserved (INDEX-03).
- GET-form contract, frozen column order, and host seam all intact; CSS triple-copy parity holds; module compiles clean.
</success_criteria>

<output>
Create `.planning/phases/201-users-index-elevation/201-01-SUMMARY.md` when done.
</output>