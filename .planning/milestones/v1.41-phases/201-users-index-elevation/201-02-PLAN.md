---
phase: 201
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example/sigra_admin_users.ex
autonomous: true
requirements:
  - INDEX-03
user_setup: []

must_haves:
  truths:
    - "The example's extra_list_badges/1 and extra_list_columns/0 host-seam hooks each emit one non-empty value (a binary badge and a label/value column map) instead of [], so the live assertUserResultEquivalence spec exercises the extra_badges and extra_columns seam in BOTH desktop and mobile (D-07, INDEX-03)."
    - "The emitted badge/column copy is glossary-clean (it renders into users_index_live markup, which glossary_test.exs:24 scopes)."
  artifacts:
    - "test/example/lib/example/sigra_admin_users.ex (extra_list_badges/extra_list_columns return non-empty)"
  key_links:
    - "badge_text/1 (users_index_live.ex:656-658) accepts a binary; column_text/2 (:660-667, dual map-shape read) accepts a %{label:, value:} map — the emitted shapes must match one of these."
    - "After this change the equivalence spec sees the badge in the desktop status <td> (:274) + mobile cluster (:316) and the column in the desktop activity <td> (:287) + mobile dl (:335-337) — proving Plan 01's DRY refactor kept both seams in both layouts."
  prohibitions:
    - statement: "Do not change the host-seam callback CONTRACT (arity / return type) — extra_list_badges/1 still returns a list of badge values and extra_list_columns/0 still returns a list of column maps. Only the example's RETURN VALUE changes from [] to one non-empty element (D-07)."
      status: resolved
      verification: "`grep -c 'def extra_list_badges' test/example/lib/example/sigra_admin_users.ex` returns 1 and `grep -c 'def extra_list_columns' test/example/lib/example/sigra_admin_users.ex` returns 1; both bodies return a non-empty list; `cd test/example && mix compile --warnings-as-errors` succeeds."
---

<objective>
Close the host-seam blind spot (D-07): make the example app's `extra_list_badges/1` and `extra_list_columns/0` hooks emit one non-empty value each so the live `assertUserResultEquivalence` Playwright spec actually exercises the `extra_badges` / `extra_columns` seam in both the desktop table and the mobile card stack.

Purpose: the example currently returns `[]` for both list seams, so CI cannot catch a one-sided drop of the seam during Plan 01's DRY refactor — the exact blind-spot class as Phase 200's `extra_detail_sections`. Emitting non-empty values gives the equivalence spec real coverage of the frozen generated-host contract (INDEX-03).

Output: an updated `test/example/lib/example/sigra_admin_users.ex` that emits one non-empty badge + one non-empty column, glossary-clean, with the host-seam callback contract unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-users-index-elevation/201-CONTEXT.md
@.planning/phases/201-users-index-elevation/201-PATTERNS.md
@.planning/phases/201-users-index-elevation/201-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan changes return values only (no new symbols):
- `extra_list_badges/1` returns one non-empty binary badge value (e.g. `["Example badge"]`) instead of `[]`.
- `extra_list_columns/0` returns one non-empty column map (e.g. `[%{label: "Region", value: "us-east"}]`) instead of `[]`.
- This mirrors Phase 200's identical move for `extra_detail_sections` seam coverage.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Emit one non-empty extra_list_badges + extra_list_columns from the example hook</name>
  <files>test/example/lib/example/sigra_admin_users.ex</files>
  <read_first>
    - test/example/lib/example/sigra_admin_users.ex (extra_list_badges/1 `:20` returns []; extra_list_columns/0 `:23` returns [])
    - lib/sigra/admin/live/users_index_live.ex (badge_text/1 `:656-658` accepts a binary; column_text/2 `:660-667` dual map-shape read accepts %{label:, value:})
    - lib/sigra/admin/users/query.ex (decorate_rows/2 `:534-549` — where the hooks are read into extra_badges/extra_columns row keys)
    - .planning/phases/201-users-index-elevation/201-PATTERNS.md (example host-seam blind-spot close `:179-206`, including the exact suggested return shapes)
    - .planning/phases/201-users-index-elevation/201-UI-SPEC.md (Host-Seam Preservation :322-324; Generated-Host Contract Notes #2 :437)
  </read_first>
  <action>
    Change `extra_list_badges/1` (`:20`) to return a one-element list with a non-empty binary badge value (e.g. `["Example badge"]`) — `badge_text/1` (`users_index_live.ex:657`) accepts a binary directly. Change `extra_list_columns/0` (`:23`) to return a one-element list with a non-empty column map matching `column_text/2`'s map shape (e.g. `[%{label: "Region", value: "us-east"}]`). Keep the `@impl true` annotations and the callback arities unchanged. Keep the chosen copy glossary-clean — the badge/column text renders into `users_index_live` markup, which `glossary_test.exs:24` scopes (avoid synonyms of canonical admin terms; a neutral demo label like "Region"/"us-east" and "Example badge" is safe). Do not change any other hook in the file (`extra_search_fields` etc. stay as-is).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def extra_list_badges(_user), do: []' test/example/lib/example/sigra_admin_users.ex` returns 0 (the empty default is gone) and the new body returns a non-empty list (a binary element).
    - `grep -F 'def extra_list_columns, do: []' test/example/lib/example/sigra_admin_users.ex` returns 0 and the new body returns a non-empty list containing a `%{label:` ... `value:` ...`}` map.
    - `grep -c '@impl true' test/example/lib/example/sigra_admin_users.ex` is unchanged from before the edit (callback contract intact).
    - `cd test/example && mix compile --warnings-as-errors` succeeds with no warnings.
  </acceptance_criteria>
  <done>The example list seams emit one non-empty badge + column, glossary-clean, with the callback contract unchanged — giving the live equivalence spec real seam coverage in both layouts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app extra_list_badges/extra_list_columns → admin list markup | generated-host-provided badge/column data renders into the operator-facing Users list (this plan exercises that seam from the example host) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-201-02-01 | Tampering | host-seam one-sided drop (D-07) | high | mitigate | The example emitting non-empty badge + column is the lever that makes the live `assertUserResultEquivalence` spec catch a desktop-or-mobile-only drop of the seam in Plan 01's DRY refactor; without it the `[]` default hides the regression (same blind spot as Phase 200's extra_detail_sections). |
| T-201-02-02 | Information disclosure | XSS via host-provided badge/column text | low | accept | Badge/column values render through HEEx (auto-escaped) via badge_text/1 + column_text/2; this is a test-fixture string under our control, not user input. No new escaping surface. |
| T-201-02-03 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — N/A; no new dependencies added. |
</threat_model>

<verification>
- `cd test/example && mix compile --warnings-as-errors` clean.
- `extra_list_badges/1` and `extra_list_columns/0` each return a non-empty list (binary badge + label/value column map); the `[]` defaults are gone.
- `@impl true` count unchanged (callback contract intact).
- Emitted copy is glossary-clean (covered by glossary_test.exs:24 via the rendered markup).
</verification>

<success_criteria>
- The example list host seams emit non-empty values so the live equivalence spec exercises the extra_badges/extra_columns seam in both desktop and mobile (INDEX-03 / D-07).
- The host-seam callback contract (arity / return type) is unchanged.
</success_criteria>

<output>
Create `.planning/phases/201-users-index-elevation/201-02-SUMMARY.md` when done.
</output>
