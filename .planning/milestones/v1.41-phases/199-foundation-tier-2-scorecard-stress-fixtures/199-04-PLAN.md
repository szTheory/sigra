---
phase: 199-foundation-tier-2-scorecard-stress-fixtures
plan: 04
type: execute
wave: 2
depends_on: ["199-03"]
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots
  - test/example/priv/playwright/snapshot-allowlist-design
  - test/example/priv/playwright/snapshot-allowlist
  - .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md
autonomous: false
user_setup: []
requirements: [FIXT-01]
must_haves:
  truths:
    - "The MG-5/MG-6 desktop↔mobile content-equivalence test is un-skipped and passes against the ≥25-event seeded data, proving pagination renders on the first-listed user's audit feed (FIXT-01, D-13)"
    - "Only the baseline PNGs that ACTUALLY move from the seed change are recaptured — blast radius is determined empirically, not pre-emptively (Finding 2/3, D-12, D-14)"
    - "Both snapshot canaries (impersonation-banner for the checkpoint lane, board-notice for the design lane) stay byte-stable; recapture uses the correct per-lane allowlist (D-14)"
    - "Both allowlists are reset to empty (comments only) at end of phase — Phase 199 ends green on its own, not deferred to Phase 204 (D-15)"
    - "The tracked admin-design-mg5-6-content-equivalence-data-dependent todo is resolved/moved (closes via FIXT-01)"
  artifacts:
    - "Un-skipped content-equivalence test in admin-design.spec.ts"
    - "Recaptured non-canary baseline PNGs for slugs that empirically moved"
    - "Both allowlist files back to empty steady-state"
  key_links:
    - "snapshot-recapture-gate.sh auto-routes each intended slug to its owning lane and applies the correct canary/allowlist — use it (with RECAPTURE_DRYRUN=1 first), not hand-called canary-guard, to avoid cross-lane mixups (Pitfall 4)"
    - "The content-equivalence test navigates to the first-listed (/admin/users newest = inserted_at DESC) user; plan 03 seeded the bulk cohort BEFORE personas so admin stays first-listed with ≥25 events"
---

<objective>
Prove FIXT-01 behaviorally: un-skip the MG-5/MG-6 content-equivalence test so the ≥25-event seed data makes pagination render, then recapture only the baselines that actually moved (empirically), keep both canaries byte-stable, reset both allowlists to empty, and close the tracked todo. This is the phase's green-on-its-own gate (D-15).

Purpose: The seed data (plan 03) is only validated by un-skipping the test that consumes it. The recapture must be surgical — research shows most surfaces are insulated (checkpoint lane uses its own per-run users; gallery boards are static-assign).
Output: An un-skipped, passing content-equivalence test; minimal recaptured PNGs; empty allowlists; resolved todo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-CONTEXT.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-RESEARCH.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-03-SUMMARY.md

# Files / procedure (read in full before acting):
@test/example/priv/playwright/tests/admin-design.spec.ts
@scripts/ci/snapshot-recapture-gate.sh
@.planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md
# RESEARCH "Snapshot / Recapture Operator Procedure" is the authoritative runbook for tasks 2-3.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-skip the MG-5/MG-6 content-equivalence test and make it land on the ≥25-event user (FIXT-01, D-13)</name>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts lines 325-391 — the test currently calls `test.skip(...)` at line 328 with a message about no seeded user reaching the ≥25-event threshold; lines 369-391 navigate to the FIRST-listed user's detail → "View full audit" and assert Previous/Next/Export pagination affordances.
    - 199-RESEARCH.md Finding 1 + Open Question 1 — the test reaches the first-listed user via `/admin/users` (unscoped → newest inserted_at wins). Plan 03 seeded the bulk cohort BEFORE personas so a persona (admin) is newest; CONFIRM empirically which user the test lands on. If it does NOT land on a ≥25-event user, the in-scope fix is to add an explicit scope to the test navigation (e.g. `/admin/users?q=admin@demo.tasklane.test`) so it deterministically reaches the admin persona.
    - CLAUDE.md "Local development prerequisites" + MEMORY "Example Playwright boot" — boot the example with seeds on an alternate PORT (4011; SIGRA_EXAMPLE_URL=http://localhost:4011); use scripts/db/up.sh + disposable-PG fallback when local Postgres is saturated.
  </read_first>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <action>
    Remove the `test.skip(...)` call at line 328 so the content-equivalence test runs. Boot the example with demo seeds (the ≥25-event admin + bulk cohort from plan 03) and run the design lane to observe which user the first-listed navigation reaches. If it lands on the admin persona with ≥25 events and the Previous/Next/Export assertions pass, leave the navigation as-is. If it lands on a different user lacking pagination, add a deterministic scope to the test's `/admin/users` navigation (line ~369) so it opens the admin persona explicitly (mirror the existing `?q=` scoping pattern used elsewhere in the suite), keeping the assertion semantics unchanged. Do not weaken any assertion. The test must be GREEN against seeded data after un-skip.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && ! grep -q "test.skip('data-dependent pagination" test/example/priv/playwright/tests/admin-design.spec.ts && grep -q "content-equivalent" test/example/priv/playwright/tests/admin-design.spec.ts && echo UNSKIPPED_OK</automated>
  </verify>
  <acceptance_criteria>
    - The `test.skip('data-dependent pagination ...')` line is removed; the content-equivalence test body runs.
    - With the example booted on seeded data, the design lane content-equivalence test passes (desktop↔mobile equivalence + Previous/Next/Export pagination affordances present on the first-listed user's audit feed).
    - If a navigation scope was added, it deterministically targets the admin persona and no assertion was weakened.
  </acceptance_criteria>
  <done>The MG-5/MG-6 content-equivalence test is un-skipped and passes against the ≥25-event seeded fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Empirically determine blast radius and recapture only the moved non-canary slugs through the gate (D-12, D-14, Finding 2/3)</name>
  <read_first>
    - 199-RESEARCH.md "Snapshot / Recapture Operator Procedure" (the authoritative runbook): lane map (checkpoint lane canary impersonation-banner / allowlist snapshot-allowlist; design lane canary board-notice / allowlist snapshot-allowlist-design); step (1) determine blast radius by running both lanes in compare mode; step (2) recapture only moved non-canary slugs via `snapshot-recapture-gate.sh <slug>...` (dry-run routing first with RECAPTURE_DRYRUN=1); the dedicated in-CI admin_design_recapture job is NOT the right tool here.
    - scripts/ci/snapshot-recapture-gate.sh in full — it auto-routes each slug to its owning lane by globbing the snapshot dir, runs compare-mode lanes, then the per-lane canary guard with --require-all.
    - 199-RESEARCH.md Finding 2 — expect the checkpoint lane (own per-run @example.test users, scoped queries) and the static-assign gallery boards to NOT move; the genuine mover is the content-equivalence live-navigation surface. Confirm empirically; do not pre-emptively recapture all four surfaces.
  </read_first>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts-snapshots</files>
  <action>
    Boot the example with seeds (PORT 4011). Run both Playwright lanes in compare mode to enumerate which baseline PNGs actually drift from the seed change (the recapture gate's step (a)/(a2) do exactly this; or run the lanes directly in compare mode and inspect failures). Record the exact set of moved slugs. For ONLY those moved non-canary slugs: run `RECAPTURE_DRYRUN=1 bash scripts/ci/snapshot-recapture-gate.sh <slug>...` to confirm per-lane routing, then `npx playwright test --update-snapshots` scoped to the intended slugs, restore any unintended PNGs, and run `bash scripts/ci/snapshot-recapture-gate.sh <slug>...` to prove all-green per lane. NEVER list `impersonation-banner` or `board-notice` in any allowlist — if a seed change would move a canary, STOP and investigate (red flag, not an allowlist entry). If empirically NO baseline moves (plausible per Finding 2 if the content-equivalence test asserts structure, not pixels), record that and skip recapture entirely — do not force-recapture unchanged PNGs. Commit only the genuinely moved PNGs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && RECAPTURE_DRYRUN=1 bash scripts/ci/snapshot-recapture-gate.sh board-notice 2>&1 | grep -q "DESIGN_ALLOW" && git diff --name-only -- 'test/example/priv/playwright/**/*impersonation-banner*' 'test/example/priv/playwright/**/*board-notice*' | wc -l | grep -qx 0 && echo CANARY_STABLE</automated>
  </verify>
  <acceptance_criteria>
    - The set of moved baseline PNGs was determined empirically (compare-mode run), and only those non-canary slugs were recaptured via the gate (or none, if nothing moved).
    - `bash scripts/ci/snapshot-recapture-gate.sh <moved-slug>...` exits 0 (all-green per lane) for any recaptured slugs.
    - Neither canary PNG changed: `git diff` shows zero changes to any `impersonation-banner` or `board-notice` baseline.
    - No PNG was force-recaptured that did not genuinely move (the commit's snapshot diff matches the empirically-determined moved set exactly).
  </acceptance_criteria>
  <done>Only the empirically-moved non-canary baselines are recaptured and pass the per-lane gate; both canaries are byte-stable.</done>
</task>

<task type="auto">
  <name>Task 3: Reset both allowlists to empty and resolve the tracked todo (D-15)</name>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist and snapshot-allowlist-design — confirm their current steady-state (research notes both are currently empty/comments-only); after recapture they must return to that empty steady state.
    - .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md — the todo FIXT-01 closes (resolves_phase: 199).
    - 199-CONTEXT.md D-15 (allowlists empty at end-of-phase, mirroring Phase 192) and the Folded Todos note (resolves_phase: 199).
  </read_first>
  <files>test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/snapshot-allowlist-design, .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md</files>
  <action>
    After Task 2's recapture is proven green, ensure BOTH allowlist files contain only comments (no active slug entries) — reset them to the empty steady state mirroring Phase 192. (The recapture gate uses allowlists transiently during recapture; the committed end-state is empty.) Then resolve the tracked todo: move `.planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md` to `.planning/todos/resolved/` (create the dir if needed) and annotate it as closed by FIXT-01/Phase 199 (the un-skipped content-equivalence test now passes on the ≥25-event fixture). Do NOT defer allowlist cleanup to Phase 204 — Phase 199 ends green on its own.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && ! grep -vE '^\s*#|^\s*$' test/example/priv/playwright/snapshot-allowlist && ! grep -vE '^\s*#|^\s*$' test/example/priv/playwright/snapshot-allowlist-design && test ! -f .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md && echo ALLOWLISTS_EMPTY_TODO_RESOLVED</automated>
  </verify>
  <acceptance_criteria>
    - Both `snapshot-allowlist` and `snapshot-allowlist-design` contain only comment/blank lines (no active slug entries) — the grep for non-comment, non-blank lines returns nothing.
    - The todo file is no longer in `.planning/todos/pending/` (moved to resolved with a closed-by-FIXT-01/Phase-199 annotation).
    - `bash scripts/ci/snapshot-canary-guard.sh --base origin/main` (checkpoint lane) and the design-lane equivalent both pass with empty allowlists.
  </acceptance_criteria>
  <done>Both allowlists are empty steady-state, the canary guards pass, and the tracked content-equivalence todo is resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — recapture scope, canary stability, empty allowlists, green design lane</name>
  <action>Human confirms the recapture scope and steady-state per the steps below; no further automated work in this task.</action>
  <what-built>Un-skipped MG-5/MG-6 content-equivalence test passing on the ≥25-event seed fixture; empirically-scoped baseline recapture with both canaries byte-stable; both allowlists reset to empty; tracked todo resolved.</what-built>
  <how-to-verify>
    1. Confirm the recapture diff: `git diff --stat -- test/example/priv/playwright/**/*.png` shows ONLY the empirically-moved slugs (or no PNGs if nothing moved) — no canary PNG (`impersonation-banner`, `board-notice`) appears.
    2. Confirm both allowlists are empty: open `test/example/priv/playwright/snapshot-allowlist` and `snapshot-allowlist-design` — only comments.
    3. Confirm the content-equivalence test is un-skipped and the design lane is green (CI design lane, or local `SIGRA_EXAMPLE_URL=http://localhost:4011` run on seeded data).
    4. Confirm the todo moved to `.planning/todos/resolved/`.
    Expected: a minimal, justified recapture; stable canaries; empty allowlists; green design lane; resolved todo.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. an unexpected canary move or an over-broad recapture to investigate).</resume-signal>
</task>

</tasks>

<verification>
- Content-equivalence test un-skipped and passing on seeded data (FIXT-01, D-13).
- `bash scripts/ci/snapshot-recapture-gate.sh <moved-slug>...` all-green for any recaptured slugs; zero canary PNG changes (D-14).
- Both allowlists contain only comments; canary guards pass vs origin/main (D-15).
- Tracked todo moved to resolved (FIXT-01 closes it).
- Human checkpoint approves the recapture scope and steady-state.
</verification>

<success_criteria>
The ≥25-event fixture is behaviorally proven via the un-skipped content-equivalence test; recapture is surgical and empirically justified; both canaries are byte-stable; both allowlists are empty so the phase is green on its own (D-15); the tracked todo is resolved. ROADMAP success criterion 3 (pagination renders, todo closed) is fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-04-SUMMARY.md` when done.
</output>
