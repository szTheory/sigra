---
phase: 199-foundation-tier-2-scorecard-stress-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-fractal-scorecard.md
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements: [LEDGER-01]
must_haves:
  truths:
    - "The fractal scorecard defines concrete, named Tier-2 proxies (motion-token / no transition:all, overlay-open axe-clean, desktop↔mobile content-equivalence, focus-trap/restore APG, glossary-clean microcopy, density/whitespace rhythm, target-size minimum), each mapped to either an existing automated gate or marked documented-as-manual"
    - "A maintainer can read the ledger and know exactly how to assert a cell reached Tier 2: flip column-4 to `2` and cite the proving spec/test per applicable proxy in the Evidence column"
    - "The ledger's terminal-ratification prose no longer says 'Tier 2 is NOT declared here' — it is reconciled to describe the new objective-proxy Tier-2 contract"
  artifacts:
    - "guides/reference/admin-fractal-scorecard.md — new 'Tier-2 Award-grade Add-on' block mirroring the per-level add-on structure"
    - "guides/reference/admin-quality-ledger.md — Tier-2 assertion convention documented + reconciled terminal-ratification prose"
  key_links:
    - "Ledger column-4 stays a bare `[012]` integer — the Tier-2 proxy block adds NO decorators/columns so the monotonic guard's `awk -F'|'` positional parse keeps working"
    - "Each proxy cross-references a concrete spec/test path (admin-modal-interaction.spec.ts, admin-design.spec.ts, glossary_test.exs) so Tier-2 evidence is verifiable, not narrative"
---

<objective>
Encode the Tier-2 "Award-grade" proxies as documentation so Tier 2 becomes objectively *earnable*, and reconcile the ledger prose that currently says Tier 2 is never declared. This is the LEDGER-01 docs deliverable — the measuring instrument's rubric half. It does NOT ratchet any cell to Tier 2 (that is Phases 200-204).

Purpose: Without named, gate-mapped proxies, "Tier 2" is a subjective verdict and cannot be guarded. This plan turns it into a checklist of concrete evidence.
Output: A new Tier-2 add-on block in the fractal scorecard (D-01) and a documented Tier-2 assertion convention + reconciled prose in the ledger (D-03, D-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-CONTEXT.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-RESEARCH.md

# The two docs being edited (read in full before editing):
@guides/reference/admin-fractal-scorecard.md
@guides/reference/admin-quality-ledger.md
# Governance docs the proxies cross-reference:
@guides/reference/admin-design-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Tier-2 Award-grade Add-on block to the fractal scorecard (D-01, D-02)</name>
  <read_first>
    - guides/reference/admin-fractal-scorecard.md lines 42-122 — the existing "Per-Level Add-ons" structure (L1/L2/L3/L4) is the EXACT template to mirror: a heading followed by pass-criteria bullets that cross-reference concrete specs.
    - 199-CONTEXT.md D-01 and D-02 — the seven named proxies and their gate-mapping (which are automated vs documented-as-manual).
  </read_first>
  <files>guides/reference/admin-fractal-scorecard.md</files>
  <action>
    Append a new section titled "Tier-2 Award-grade Add-on" after the L4 Flow Add-ons block (after line 121, before the closing cross-reference line at 125), mirroring the per-level add-on bullet structure already in the file. The block states that a surface earns Tier 2 only when, in addition to its Tier-1 ratification, every APPLICABLE proxy below passes. Enumerate the seven proxies as pass-criteria bullets, each annotated with its mapping per D-02:
      - "Overlay-open axe-clean" → automated: the admin-modal-interaction.spec.ts "axe-while-open" check.
      - "Focus-trap and focus-restore (APG)" → automated: the existing "7 APG gates" in admin-modal-interaction.spec.ts.
      - "Desktop↔mobile content-equivalence" → automated: admin-design.spec.ts MG-5/MG-6 plus the un-skipped content-equivalence test (delivered by FIXT-01 in plan 04).
      - "Glossary-clean microcopy" → automated: glossary_test.exs.
      - "Motion-token conformance / no transition:all" → documented-as-manual (no current automated gate); reviewer greps the surface for the literal all-properties transition shorthand and confirms motion uses --sg-duration/--sg-ease tokens.
      - "Density / whitespace rhythm" → documented-as-manual; reviewer confirms consistent sg-stack tier usage per the L3 page-vertical-rhythm add-on.
      - "Target-size minimum" → documented-as-manual; reviewer confirms interactive targets meet the WCAG 2.2 target-size floor (note the established admin-dense-control suppression precedent).
    Cross-reference admin-design-contract.md and admin-quality-ledger.md from the block. Use prose/bullets only — do NOT introduce a machine-parseable proxy file or per-proxy table columns (D-01 explicitly forbids this).
  </action>
  <verify>
    <automated>grep -c "Tier-2 Award-grade Add-on" guides/reference/admin-fractal-scorecard.md | grep -qx 1 && grep -q "axe-while-open" guides/reference/admin-fractal-scorecard.md && grep -q "documented-as-manual" guides/reference/admin-fractal-scorecard.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The string "Tier-2 Award-grade Add-on" appears exactly once as a section heading after the L4 add-ons.
    - All seven proxies are present as bullets; each is labeled either with its automated gate (spec/test filename) or "documented-as-manual".
    - The four automated proxies name their specs: admin-modal-interaction.spec.ts (axe + APG), admin-design.spec.ts (content-equivalence), glossary_test.exs (microcopy).
    - No new markdown table column and no new proxy file were created (the change is additive prose/bullets in this one file).
  </acceptance_criteria>
  <done>The fractal scorecard contains a Tier-2 add-on block naming all seven proxies with correct gate mappings, mirroring the existing add-on structure.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Tier-2 ledger assertion convention and reconcile terminal-ratification prose (D-03, D-06)</name>
  <read_first>
    - guides/reference/admin-quality-ledger.md lines 14-31 — the Parsing Rules section (column-4 is a single `[012]` integer; the guard's awk parse). DO NOT change the table column shape.
    - guides/reference/admin-quality-ledger.md lines 72-89 — the "Terminal Ratification — Phase 192" prose, specifically lines 81-84 ("Tier 2 is NOT declared here ... earned-separately path"). This prose now contradicts the Tier-2 contract and must be reconciled (D-06).
    - 199-CONTEXT.md D-03 (how a maintainer asserts Tier 2) and D-06 (prose reconciliation requirement).
  </read_first>
  <files>guides/reference/admin-quality-ledger.md</files>
  <!-- planner-discipline-allow: Tier 2 is NOT declared here -->
  <action>
    Two edits in this file, both prose-only — the ledger TABLE rows and column-4 integers stay byte-identical (this phase ratchets nothing):
    1. Under the Parsing Rules / Tier Vocabulary area (or a new short "Asserting Tier 2" subsection placed before the Quality Ledger table), document the D-03 convention: a maintainer asserts a cell reached Tier 2 by (a) changing that row's Tier column to the integer `2` (still a bare `[012]` value, no decorators — preserving the awk parse) AND (b) expanding that row's Evidence column to cite the specific spec/test proving each APPLICABLE Tier-2 proxy (cross-reference the scorecard's Tier-2 Award-grade Add-on block). State explicitly that decorators/footnotes in column-4 are forbidden because they break the monotonic guard parse.
    2. Reconcile the Phase 192 terminal-ratification prose (~lines 81-84): replace the "Tier 2 is NOT declared here / subjective / earned-separately" language so it instead says Tier 2 is now objectively earnable via the scorecard's proxy block and protected forward-only by the same monotonic guard (which already treats `2` as a higher integer). Keep the historical fact that Phase 192 locked all cells at Tier 1 as the floor; only remove the now-false claim that Tier 2 can never be declared. Do not delete the proof-method paragraph.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-ledger-monotonic.sh --base HEAD && grep -qi "assert" guides/reference/admin-quality-ledger.md && ! grep -q "Tier 2 is NOT declared here" guides/reference/admin-quality-ledger.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The monotonic guard `quality-ledger-monotonic.sh --base HEAD` exits 0 and reports the same cell count as before (proves the table/column-4 shape was not broken).
    - The literal phrase "Tier 2 is NOT declared here" no longer appears in the file.
    - The file documents the D-03 assertion convention (flip column-4 to `2` + cite proxy evidence) and explicitly warns that column-4 decorators break the guard.
    - No ledger table row changed its Tier integer (git diff shows only prose additions/edits, no `| ... | 2 |` cell flips).
  </acceptance_criteria>
  <done>The ledger documents how to assert Tier 2 objectively, the stale "Tier 2 not declared" prose is reconciled, and the monotonic guard still parses every cell.</done>
</task>

</tasks>

<verification>
- `grep -c "Tier-2 Award-grade Add-on" guides/reference/admin-fractal-scorecard.md` == 1.
- `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 with the full cell count (parse intact, LEDGER-01 parse-smoke from VALIDATION.md).
- `! grep -q "Tier 2 is NOT declared here" guides/reference/admin-quality-ledger.md`.
- No Tier integer in any ledger table row changed vs HEAD (this is an instrument-definition phase, not a ratchet phase).
</verification>

<success_criteria>
The scorecard names all seven Tier-2 proxies with correct automated/manual mapping; the ledger documents the objective Tier-2 assertion convention and is internally consistent (no contradictory "Tier 2 not declared" prose); the monotonic guard parse is provably unbroken. ROADMAP success criterion 1 (objective Tier-2 proxies defined) is satisfied at the docs level.
</success_criteria>

<output>
Create `.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-01-SUMMARY.md` when done.
</output>
