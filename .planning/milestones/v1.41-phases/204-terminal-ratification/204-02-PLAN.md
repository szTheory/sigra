---
phase: 204-terminal-ratification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/planning/phase_192_known_failure_contract_test.exs
  - test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs
  - guides/introduction/demo-showcase.md
  - doc/llms.txt
  - llms.txt
autonomous: true
requirements: [RATIFY-01]
must_haves:
  truths:
    - "Sigra.Planning.Phase192KnownFailureContractTest no longer asserts a now-absent admin-design.spec.ts MG-5/6 test.skip( quarantine marker (deleted or relaxed per D-08)."
    - "Sigra.Planning.Phase148EvaluatorFunnelAndFirstRunDxTest passes against current docs with Vaultr→Tasklane drift resolved (D-08)."
    - "Companion docs (demo-showcase.md, llms.txt) referencing the demo app reflect the Tasklane rename, not Vaultr (D-08)."
    - "mix test for both planning contract tests exits 0."
  artifacts:
    - test/sigra/planning/phase_192_known_failure_contract_test.exs
    - test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs
    - guides/introduction/demo-showcase.md
  key_links:
    - "The phase148 test reads demo-showcase.md, README.md, mix.exs, and doc/llms.txt; its assertions must match the actual committed doc strings after the Vaultr→Tasklane fix."
  prohibitions:
    - "MUST NOT touch the 3 Sigra.UpgradeIntegrationTest env-DB failures (upgrade_test.exs:212 seed_users!/2) — accepted known failures per D-09, explicitly out of scope."
    - "MUST NOT change the seeded demo email domain or persona logic — this plan reconciles DOC/TEST drift to the already-renamed Tasklane app, not the reverse."
---

<objective>
Make `mix test` clean for the terminal gate by fixing the two stale planning contract tests
(D-08) and reconciling the companion doc drift from the demo's Vaultr→Tasklane rename. The 3
`Sigra.UpgradeIntegrationTest` env-DB failures are accepted (D-09) and explicitly NOT touched.

Purpose: A terminal ratification gate cannot run against a `mix test` that fails on stale
self-healing contracts. Phase192's known-failure contract asserts a quarantine marker that no
longer exists; phase148 asserts doc strings that drifted when the demo app was renamed.

Output: Both contract tests green; demo-showcase.md + llms.txt copy reflect Tasklane.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204-terminal-ratification/204-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete/relax the stale Phase192 known-failure contract test (D-08)</name>
  <files>test/sigra/planning/phase_192_known_failure_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_192_known_failure_contract_test.exs (asserts admin-design.spec.ts MG-5/6 still carries a `test.skip(` quarantine marker — the quarantined test is gone)
    - test/example/priv/playwright/tests/admin-design.spec.ts (confirm the MG-5/6 content-equivalence test no longer has a test.skip( marker; the companion todo is already in .planning/todos/resolved/)
  </read_first>
  <action>
    Confirm via grep that `admin-design.spec.ts` no longer contains a `test.skip(` marker on the
    "MG-5 and MG-6 desktop and mobile representations are content-equivalent" test (the quarantine
    was already lifted; the companion todo
    `2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md` is already in
    `.planning/todos/resolved/`). Since the test's only remaining assertion (192-KF-03) now
    asserts a marker that should NOT exist, DELETE the entire
    `test/sigra/planning/phase_192_known_failure_contract_test.exs` file — all three original
    known failures are resolved and the self-healing contract has no live assertion left to make.

    If grep shows the marker DOES still exist (unexpected), do not delete: instead relax 192-KF-03
    to assert the marker is ABSENT (the resolved state) and update the @moduledoc to record the
    resolution date. Prefer deletion when the marker is gone.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c 'test.skip(' test/example/priv/playwright/tests/admin-design.spec.ts; test ! -f test/sigra/planning/phase_192_known_failure_contract_test.exs && echo DELETED || (source tmp/db.env 2>/dev/null; MIX_ENV=test mix test test/sigra/planning/phase_192_known_failure_contract_test.exs)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test.skip(' test/example/priv/playwright/tests/admin-design.spec.ts` confirms the MG-5/6 quarantine marker is gone (the grep on the MG-5/6 region returns no match).
    - The phase192 contract test file is deleted (preferred) OR its 192-KF-03 assertion is inverted to assert absence and passes.
    - `MIX_ENV=test mix test test/sigra/planning/` does not fail on a phase192 marker assertion.
  </acceptance_criteria>
  <done>The stale Phase192 known-failure contract no longer blocks `mix test`; resolved-todo state is consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Vaultr→Tasklane doc drift + update Phase148 contract test (D-08)</name>
  <files>guides/introduction/demo-showcase.md, doc/llms.txt, llms.txt, test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs (asserts `Vaultr Example App` in llms.txt, `admin@demo.vaultr.test` … persona emails, and a `Vaultr is the runnable local companion …` string from demo-showcase.md)
    - guides/introduction/demo-showcase.md (title + body still say "Vaultr"; persona emails still `@demo.vaultr.test`)
    - doc/llms.txt and llms.txt (the `- [Demo Showcase — Vaultr Example App](demo-showcase.md)` line)
    - test/example/lib/example/demo/personas.ex and seeds.ex (CANONICAL truth: the live seeded demo uses `@demo.tasklane.test` emails — docs/tests must match the app)
  </read_first>
  <action>
    The demo app was renamed Vaultr→Tasklane and the live seed emails are `@demo.tasklane.test`,
    but `demo-showcase.md` and the llms indexes still say "Vaultr" / `@demo.vaultr.test`. Reconcile
    DOCS to the already-renamed app, then update the phase148 test assertions to match.

    Doc edits (source-of-truth fix): in `guides/introduction/demo-showcase.md` replace the demo
    app name `Vaultr` → `Tasklane` in the title and prose, and the persona email domain
    `@demo.vaultr.test` → `@demo.tasklane.test` for every seeded persona row (admin/alice/bob/
    carol/dave/frank/morgan/pat). In `doc/llms.txt` and `llms.txt` replace
    `Demo Showcase — Vaultr Example App` → `Demo Showcase — Tasklane Example App`. Verify the new
    email domain matches `test/example/lib/example/demo/personas.ex` exactly before writing.

    Test edits: in `phase_148_evaluator_funnel_and_first_run_dx_test.exs` update the expected
    strings to the Tasklane forms — the llms `Vaultr Example App` → `Tasklane Example App`, the
    persona email list `@demo.vaultr.test` → `@demo.tasklane.test`, and the
    `Vaultr is the runnable local companion …` assertion → the Tasklane form (match the exact new
    demo-showcase.md wording). Keep all other assertions (lanes, troubleshooting, runnable
    command, screenshot proof) intact — those are not part of the rename.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -ci 'vaultr' guides/introduction/demo-showcase.md doc/llms.txt llms.txt test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs; source tmp/db.env 2>/dev/null; MIX_ENV=test mix test test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'vaultr'` on demo-showcase.md, doc/llms.txt, llms.txt, and the phase148 test all report 0 (no Vaultr remnants in these files).
    - The persona email domain written to demo-showcase.md matches `personas.ex` exactly (`@demo.tasklane.test`).
    - `MIX_ENV=test mix test test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs` exits 0.
  </acceptance_criteria>
  <done>Phase148 contract passes against Tasklane-aligned docs; no Vaultr drift remains in the companion copy.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/sigra/planning/` passes (no phase192 marker failure, phase148 green).
- `grep -rci 'vaultr' guides/introduction/demo-showcase.md doc/llms.txt llms.txt` is all 0.
- The 3 `Sigra.UpgradeIntegrationTest` failures are untouched (D-09 accepted env-DB failures).
</verification>

<success_criteria>
The two stale planning contract tests no longer fail `mix test`, and the demo doc copy is
Tasklane-consistent — clearing the path for the terminal `mix test` gate.
</success_criteria>

<output>
Create `.planning/phases/204-terminal-ratification/204-02-SUMMARY.md` when done.
</output>
