---
phase: 204-terminal-ratification
plan: 05
type: execute
wave: 4
depends_on: [204-01, 204-02, 204-03, 204-04]
files_modified:
  - .planning/milestones/v1.41-MILESTONE-AUDIT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RATIFY-01, RATIFY-02]
must_haves:
  truths:
    - ".planning/milestones/v1.41-MILESTONE-AUDIT.md exists, same artifact shape as v1.40-MILESTONE-AUDIT.md, with explicit adversarial checks against the four RATIFY-02 criteria (D-10)."
    - "Tier-2 lock VERIFIED (not ratcheted): the ~7-8 ledger cells already at Tier 2 are forward-only protected by quality-ledger-monotonic.sh --base origin/main exiting 0; no new Tier-2 cell introduced (D-11)."
    - "Phase 201 is ticked [ ]→[x] in ROADMAP.md (D-13)."
    - "RATIFY-01 and RATIFY-02 are flipped to satisfied in REQUIREMENTS.md (lines ~47-48 and the coverage table at ~87-88) (D-13)."
  artifacts:
    - .planning/milestones/v1.41-MILESTONE-AUDIT.md
  key_links:
    - "The audit's evidence rolls up the prior plans: empty allowlists + recapture gate green (plan 03), generated-host parity (plan 04), clean mix test (plans 01/02), monotonic guard vs origin/main."
  prohibitions:
    - "MUST NOT introduce any NEW Tier-2 ratchet — 204 verifies the lock, it does not flip new cells (D-11)."
    - "MUST NOT mark RATIFY-01/RATIFY-02 satisfied unless the upstream evidence (plans 01-04 gates green) actually holds; the audit must be honest, not rubber-stamp."
    - "MUST NOT count the 3 accepted Sigra.UpgradeIntegrationTest env-DB failures as a milestone blocker (D-09)."
---

<objective>
The genuinely terminal plan: run RATIFY-02's adversarial milestone review via the
`gsd-audit-milestone` skill (D-10), VERIFY the Tier-2 cell lock without ratcheting (D-11), and
do the milestone-close housekeeping (D-13). This runs AFTER all evidence exists (plans 01-04).

Purpose: RATIFY-02 demands a final adversarial pass confirming no usability-for-aesthetics
regression, no generated-host-contract friction, no broken dark/mobile/keyboard/reduced-motion
paths, and no screenshot-only quality — plus locked Tier-2 cells. D-13 closes the milestone's
loose ticks so the roadmap/requirements reflect proven reality.

Output: v1.41-MILESTONE-AUDIT.md; Phase 201 ticked; RATIFY-01/02 satisfied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/204-terminal-ratification/204-CONTEXT.md
@.planning/milestones/v1.40-MILESTONE-AUDIT.md
@guides/reference/admin-quality-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the Tier-2 cell lock (no ratchet) (D-11)</name>
  <files></files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (the ~7-8 cells already at Tier 2: index-live, organization-live, users-index-live, user-show-live, audit-index-live, audit-user-live, branding-live — Phase 192's terminal block frames the closing phase as locking, not ratcheting)
    - scripts/ci/quality-ledger-monotonic.sh (--base origin/main forward-only guard)
  </read_first>
  <action>
    VERIFY the lock — do NOT flip any new cell. Run
    `scripts/ci/quality-ledger-monotonic.sh --base origin/main` and confirm exit 0 (no Tier-2 cell
    decreased vs origin/main; the ratchet is intact and forward-only). Enumerate the current Tier-2
    cells from the ledger and confirm the count matches the phases-199-203 set (~7-8 cells); confirm
    no cell was added to Tier 2 in this phase's diff vs origin/main. If the `audit-user-live` cell's
    pagination citation was corrected to point at the new per-user test (plan 01), that is a
    citation-accuracy edit, NOT a tier change — confirm the tier digit is unchanged.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main; echo "monotonic_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0.
    - The set of Tier-2 cells equals the phases-199-203 set; no cell's tier digit increased to 2 in this phase vs origin/main.
    - Any ledger edit in this phase is citation-accuracy only (no tier digit change).
  </acceptance_criteria>
  <done>The Tier-2 ratchet is verified locked and forward-only; no new ratchet introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial milestone review via gsd-audit-milestone (RATIFY-02 / D-10)</name>
  <files>.planning/milestones/v1.41-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.40-MILESTONE-AUDIT.md (the artifact shape/precedent: Scores, Requirements Coverage cross-reference, Phase Verification Roll-Up, Cross-Phase Integration, Nyquist Compliance, Tech Debt, Verdict)
    - .planning/ROADMAP.md (v1.41 milestone goal + phases 199-204)
    - .planning/REQUIREMENTS.md (RATIFY-01/02 and the full v1.41 requirement set + traceability table)
    - .planning/phases/204-terminal-ratification/204-01-SUMMARY.md, 204-02-SUMMARY.md, 204-03-SUMMARY.md, 204-04-SUMMARY.md (the in-phase evidence to roll up)
  </read_first>
  <action>
    Invoke the `gsd-audit-milestone` skill to conduct RATIFY-02's adversarial milestone review for
    v1.41 ADMIN-UX-ELEVATION, producing `.planning/milestones/v1.41-MILESTONE-AUDIT.md` with the
    same artifact shape as `v1.40-MILESTONE-AUDIT.md`. The audit MUST include explicit adversarial
    checks against the four RATIFY-02 criteria, each with cited evidence:
      (a) no usability-for-aesthetics regression — operator JTBD improved, not just prettier
          (cite the elevation phases' VERIFICATION.md and the per-page JTBD restructuring);
      (b) no generated-host-contract friction — host extension seams intact, contract doc current
          (cite plan 04 install-golden byte-diff + admin-acceptance-smoke parity);
      (c) no broken dark/mobile/keyboard/reduced-motion paths — cite the 6-project compare-mode
          zero-drift + axe-clean overlays-open + APG focus-trap gates;
      (d) no screenshot-only quality — interactions work, not just static captures (cite the
          modal-interaction APG gates + the per-user pagination/disclosure ExUnit tests from plan 01).
    Roll up the in-phase evidence: empty allowlists + recapture gate green (plan 03), generated-host
    parity (plan 04), clean `mix test` with the 3 accepted UpgradeIntegrationTest env-DB failures
    explicitly excluded (D-09), and the monotonic guard green vs origin/main. State the verdict
    honestly (pass / tech_debt) per the gsd-audit-milestone rubric.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && test -f .planning/milestones/v1.41-MILESTONE-AUDIT.md && grep -ciE 'usability|generated-host|dark|mobile|keyboard|reduced-motion|screenshot|tier' .planning/milestones/v1.41-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.41-MILESTONE-AUDIT.md` exists.
    - It contains explicit adversarial coverage of all four RATIFY-02 criteria (usability, generated-host friction, dark/mobile/keyboard/reduced-motion, screenshot-only) with cited evidence.
    - It rolls up the in-phase gate evidence (recapture gate, parity, monotonic guard) and a stated verdict, matching the v1.40 artifact shape.
  </acceptance_criteria>
  <done>The adversarial milestone review is recorded; RATIFY-02's four criteria are checked with evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Milestone-close housekeeping — tick Phase 201 + flip RATIFY-01/02 (D-13)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 201 entry at line ~26 still `[ ]` though 201-VERIFICATION.md status passed)
    - .planning/REQUIREMENTS.md (RATIFY-01 line ~47, RATIFY-02 line ~48 both `[ ]`; traceability table lines ~87-88 mark both Pending)
    - .planning/phases/201-*/201-VERIFICATION.md (confirm status passed before ticking)
  </read_first>
  <action>
    Using scoped `Edit` only (never whole-file Write on ROADMAP.md / REQUIREMENTS.md):
    1. Confirm 201-VERIFICATION.md status is passed, then tick the Phase 201 roadmap entry
       `- [ ] **Phase 201: Users Index Elevation**` → `- [x] …` in ROADMAP.md (D-13).
    2. In REQUIREMENTS.md flip `- [ ] **RATIFY-01**:` → `- [x] **RATIFY-01**:` and
       `- [ ] **RATIFY-02**:` → `- [x] **RATIFY-02**:` (lines ~47-48), and update the traceability
       table rows (lines ~87-88) `| RATIFY-01 | Phase 204 | Pending |` → `| … | Complete |` and the
       RATIFY-02 row likewise — only because plans 01-04 + Tasks 1-2 of this plan proved them.
    Do not edit any other roadmap/requirements entries.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -n 'Phase 201:' .planning/ROADMAP.md | head -1; grep -nE 'RATIFY-0[12]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase 201 roadmap entry shows `- [x]` (ticked).
    - RATIFY-01 and RATIFY-02 checkbox lines both show `- [x]` in REQUIREMENTS.md.
    - The traceability table rows for RATIFY-01 and RATIFY-02 read `Complete`, not `Pending`.
    - No other roadmap/requirements lines changed (`git diff` scoped to those entries).
  </acceptance_criteria>
  <done>Milestone-close housekeeping done: Phase 201 ticked, RATIFY-01/02 satisfied — roadmap/requirements reflect proven reality.</done>
</task>

</tasks>

<verification>
- `scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 (Tier-2 lock verified, no new ratchet).
- `.planning/milestones/v1.41-MILESTONE-AUDIT.md` exists with the four RATIFY-02 adversarial checks + verdict.
- Phase 201 is `[x]` in ROADMAP.md; RATIFY-01/RATIFY-02 are `[x]` + `Complete` in REQUIREMENTS.md.
</verification>

<success_criteria>
The milestone closes honestly: an adversarial review records no usability/contract/accessibility
regression, the Tier-2 cells are verified locked (no new ratchet), and the roadmap/requirements
ticks reflect the proven evidence chain from plans 01-04.
</success_criteria>

<output>
Create `.planning/phases/204-terminal-ratification/204-05-SUMMARY.md` when done.
</output>
