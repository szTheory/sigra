---
phase: 204-terminal-ratification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/test/example_web/live/admin_audit_user_live_test.exs
autonomous: true
requirements: [RATIFY-02]
must_haves:
  truths:
    - "A per-user audit pagination boundary test exists in admin_audit_user_live_test.exs: ≥26 events renders the pagination nav with user-scoped hrefs; ≤25 events renders no nav (WR-02 closed per D-06)."
    - "A structural test in admin_audit_user_live_test.exs asserts the desktop Event-cell codes (event id + action code) live inside a default-collapsed <details> whose <summary> is the visible affordance (WR-01 closed per D-07)."
    - "mix test for admin_audit_user_live_test.exs passes."
  artifacts:
    - test/example/test/example_web/live/admin_audit_user_live_test.exs
  key_links:
    - "The per-user pagination test threads user_id + return_to through the page hrefs (Sigra.Admin.Live.AuditUserLive page_path/4), matching the per-user cursor href contract."
    - "The Event-codes disclosure test targets the <.audit_table_row> output (lib/sigra/admin/components.ex) rendered by the per-user desktop results table."
  prohibitions:
    - "MUST NOT change product behavior of the per-user audit LiveView — both tests lock existing intent only (no edits to lib/sigra/admin/live/audit_user_live.ex or lib/sigra/admin/components.ex)."
    - "MUST NOT introduce non-determinism: event counts and ordering must be explicit (insert exactly N events; do not rely on seed data)."
---

<objective>
Fold the two milestone test-hardening items into ratification: add a deterministic per-user
audit pagination boundary test (WR-02 / D-06) and a structural Event-codes disclosure
assertion (WR-01 / D-07) to the example host's per-user audit LiveView test. Both lock
existing intent so the `audit-user-live` Tier-2 ledger citation becomes honest.

Purpose: The `audit-user-live` ledger cell currently cites the *index* test for its
pagination evidence; D-06 makes the per-user claim honest by giving it its own boundary
test. D-07 locks the ratified Phase 202 "inline code disclosure" archetype for the desktop
Event cell so a future regression that un-collapses or removes the disclosure fails CI.

Output: Two new tests in `admin_audit_user_live_test.exs`, both passing, no product change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204-terminal-ratification/204-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic per-user audit pagination boundary test (WR-02 / D-06)</name>
  <files>test/example/test/example_web/live/admin_audit_user_live_test.exs</files>
  <read_first>
    - test/example/test/example_web/live/admin_audit_user_live_test.exs (the file being modified — reuse its `insert_audit_event/1` helper and `platform_admin_fixture/0`)
    - lib/sigra/admin/live/audit_user_live.ex (page_path/4 at lines 351-357, audit_pagination_nav usage at lines 216-220; the per-user cursor href threads user_id + return_to)
    - lib/sigra/admin/components.ex (audit_pagination_nav/1 at line ~796 — nav is absent entirely when results fit one page; present when previous_page or next_page is non-nil)
  </read_first>
  <action>
    Add a `describe "204 per-user audit pagination boundary (WR-02 / D-06)"` block with two
    tests against the existing global per-user audit route `/admin/users/:id/audit`. Reuse the
    file's `insert_audit_event/1` helper and `platform_admin_fixture/0`.

    Test A (nav PRESENT): insert exactly 26 audit events tied to one subject (effective_user_id
    = subject.id), GET the per-user audit route with an explicit small `page_size` so 26 events
    span >1 page (e.g. page_size=25), and assert the pagination nav is present with a Next link
    whose href is user-scoped — it MUST contain `/admin/users/#{subject.id}/audit`, MUST carry a
    `cursor=` param, and MUST preserve `return_to` when a return_to is supplied in the request.
    Assert the href threads `user_id` (the subject id appears in the path) so the page_path/4
    contract is exercised, not page_path/3.

    Test B (nav ABSENT): insert exactly 25 events for a fresh subject, GET the route with
    page_size=25 (single page), and assert NO pagination nav renders — refute the Next/Previous
    affordance markup (assert the nav element / its `cursor=` next href is absent). Keep ordering
    and counts explicit; do not depend on seed data.

    Use distinct subject emails per test for isolation. Do not edit any LiveView or component
    source — assertion-only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && source tmp/db.env 2>/dev/null; MIX_ENV=test mix test test/example/test/example_web/live/admin_audit_user_live_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/example/test/example_web/live/admin_audit_user_live_test.exs` exits 0.
    - The new describe block contains a 26-event test asserting nav present with an href containing both `/admin/users/` + the subject id and `cursor=`, and preserving `return_to`.
    - The new describe block contains a 25-event test asserting the pagination nav is absent.
    - File `git diff` touches ONLY admin_audit_user_live_test.exs (no LiveView/component source change).
  </acceptance_criteria>
  <done>Per-user pagination boundary is deterministically proven at the 25/26 threshold with user-scoped hrefs; WR-02 closed.</done>
</task>

<task type="auto">
  <name>Task 2: Structural Event-codes disclosure lock (WR-01 / D-07)</name>
  <files>test/example/test/example_web/live/admin_audit_user_live_test.exs</files>
  <read_first>
    - lib/sigra/admin/components.ex (audit_table_row/1 at lines 755-791 — the desktop Event cell wraps `{@row.id}` + `{@row.action}` in `<details><summary class="sg-text-sm sg-muted">Event codes</summary>…</details>`)
    - test/example/test/example_web/live/admin_audit_user_live_test.exs (desktop results table id `admin-audit-user-desktop-results`, data-testid present)
  </read_first>
  <action>
    Add a test (in the same 204 describe block or a sibling `describe "204 Event-codes disclosure
    archetype (WR-01 / D-07)"`) that GETs the per-user audit route for a subject with at least one
    audit event, then asserts the desktop Event cell renders the event id and action code inside a
    default-collapsed disclosure: assert the response HTML contains a `<details>` whose `<summary>`
    is the visible affordance carrying the disclosure label (the existing summary text — locate it
    from the component, do not hardcode a guess), and that the `<details>` is NOT force-opened
    (no `open` attribute on that details element). Assert the event id (`row.id`) and action code
    (`row.action`) markup appears inside the disclosure region (i.e. after the `<summary>` close),
    using the rendered `sg-code` code elements as anchors.

    Keep this a string/structure assertion on the rendered HTML — no Playwright, no behavior
    change. Reuse the file's helpers. If matching the disclosure region precisely needs a small
    private helper to slice the HTML around the `<summary>`, add it to the test module.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && source tmp/db.env 2>/dev/null; MIX_ENV=test mix test test/example/test/example_web/live/admin_audit_user_live_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/example/test/example_web/live/admin_audit_user_live_test.exs` exits 0.
    - The new test asserts a `<details>` with a visible `<summary>` affordance and that the details element has no `open` attribute (default-collapsed).
    - The new test asserts the event id and action code render inside the disclosure region (after the summary).
    - `git diff` touches ONLY admin_audit_user_live_test.exs.
  </acceptance_criteria>
  <done>The desktop Event-cell inline-code disclosure archetype is structurally locked; WR-01 closed.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/example/test/example_web/live/admin_audit_user_live_test.exs` exits 0 with the two new tests present and green.
- `git diff --name-only` for this plan lists exactly one file: the audit-user live test.
</verification>

<success_criteria>
WR-01 and WR-02 are closed by deterministic, behavior-neutral tests in the example host's
per-user audit LiveView test, making the `audit-user-live` Tier-2 ledger citation honest.
</success_criteria>

<output>
Create `.planning/phases/204-terminal-ratification/204-01-SUMMARY.md` when done.
</output>
