---
phase: 204-terminal-ratification
plan: 04
type: execute
wave: 3
depends_on: [204-03]
files_modified: []
autonomous: true
requirements: [RATIFY-01]
user_setup:
  - service: phx_new-archive
    why: "Install-golden byte-diff must run against the CI-pinned generator (SEED-004)"
    dashboard_config:
      - task: "Ensure phx_new 1.8.7 is installed locally — a newer archive injects a root_tag_attribute config block producing a spurious byte-diff. Do NOT regenerate the fixture to fix it."
        location: "mix archive.install --force hex phx_new 1.8.7"
must_haves:
  truths:
    - "The install-golden byte-diff suite is green against the phx_new 1.8.7 pin (D-12 / SEED-004) — generated host matches the committed fixture byte-for-byte."
    - "scripts/ci/admin-acceptance-smoke.sh renders the elevated, styled admin UI on a freshly generated phx_new 1.8.7 host (runtime generated-host parity proven, D-12)."
    - "The parity lane runs green via RUN_PARITY=1 on the recapture gate."
  artifacts: []
  key_links:
    - "Generated-host parity connects the example proof to a freshly generated phx_new 1.8.7 host via the install-golden fixture + admin-acceptance-smoke.sh; the styled admin it renders must reflect the post-pill-fix elevated bar from plan 03."
  prohibitions:
    - "MUST NOT regenerate the install-golden fixture to silence a 1.8.8 root_tag_attribute byte-diff — install phx_new 1.8.7 instead (SEED-004 / CLAUDE.md)."
    - "MUST NOT modify installer templates or fixtures as a 'fix' for environmental archive drift."
---

<objective>
Prove generated-host parity (D-12) two ways: install-golden byte-diff green against the
phx_new 1.8.7 pin, AND `admin-acceptance-smoke.sh` rendering the elevated, styled admin UI on a
freshly generated host (the parity lane, `RUN_PARITY=1`). This confirms the milestone's
elevated admin bar survives the lib→generator boundary onto a real new Phoenix app.

Purpose: RATIFY-01 requires generated-host parity as a first-class proof — the elevated admin is
library-owned but rendered through generated host scaffolding; this plan proves a fresh host
boots and renders the post-pill-fix styled admin, not just the example app.

Output: Byte-diff green + acceptance-smoke green; parity lane green via the recapture gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204-terminal-ratification/204-CONTEXT.md
@scripts/ci/admin-acceptance-smoke.sh
@scripts/ci/snapshot-recapture-gate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install-golden byte-diff green against phx_new 1.8.7 (D-12 / SEED-004)</name>
  <files></files>
  <read_first>
    - CLAUDE.md (phx_new 1.8.7 pin: a newer archive injects a root_tag_attribute config block → spurious byte-diff; install 1.8.7, do NOT regenerate the fixture — SEED-004)
    - test/sigra/install/golden_diff_test.exs (the install golden byte-diff suite)
    - reference: installer template drift (priv/templates/sigra.install/ vs hand-maintained test/example)
  </read_first>
  <action>
    Confirm the local generator archive is phx_new 1.8.7 (the CI pin). If a newer archive is
    installed, run `mix archive.install --force hex phx_new 1.8.7` before running the suite — a
    1.8.8 archive emits a `config :phoenix_live_view, root_tag_attribute: …` block that produces a
    spurious byte-diff against the committed fixture (SEED-004). Do NOT regenerate the golden
    fixture to absorb that drift.

    Run the install golden byte-diff suite (`test/sigra/install/golden_diff_test.exs` and the
    sibling `mix sigra.install` fixture tests) and confirm green. If it fails, first verify the
    archive version is exactly 1.8.7 (the most common cause of a spurious diff) before treating any
    diff as a real regression.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix archive | grep -i phx_new; source tmp/db.env 2>/dev/null; MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix archive | grep phx_new` reports `phx_new-1.8.7` (the CI pin).
    - `MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs` exits 0 (byte-diff green).
    - No installer template or golden fixture was modified to absorb archive drift (`git status` clean for priv/templates and the fixture).
  </acceptance_criteria>
  <done>Install-golden byte-diff is green against the pinned generator; the lib→generator boundary is byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: admin-acceptance-smoke renders the elevated styled admin on a fresh host (D-12)</name>
  <files></files>
  <read_first>
    - scripts/ci/admin-acceptance-smoke.sh (scaffolds phx.new + sigra.install + boot + Playwright; PORT=4017, --test chrome; the runtime generated-host parity proof — DIST-06 / RUN_PARITY)
    - scripts/ci/snapshot-recapture-gate.sh (step d runs the parity lane when RUN_PARITY=1)
    - reference: generated-host acceptance smoke
  </read_first>
  <action>
    Run the generated-host runtime parity proof. Preferred: drive it through the recapture gate's
    parity lane so the proof is integrated with the same all-green approval mechanism — boot the
    example app (alt PORT, SIGRA_EXAMPLE_URL set) and run
    `RUN_PARITY=1 scripts/ci/snapshot-recapture-gate.sh user-audit audit-explorer` (the same
    intended slugs from plan 03), which runs `admin-acceptance-smoke.sh` as step (d). Alternatively
    run `scripts/ci/admin-acceptance-smoke.sh` directly. Confirm it scaffolds a fresh phx_new 1.8.7
    host, installs sigra, boots, and the Playwright assertions render the elevated, styled admin UI
    (the post-pill-fix bar from plan 03) — i.e. the admin surface is styled (sg-* design system
    applied), not unstyled. Treat a non-zero exit as a real parity failure.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/admin-acceptance-smoke.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/admin-acceptance-smoke.sh` exits 0 (fresh host scaffolds, installs, boots, renders styled admin).
    - When driven via the gate, `RUN_PARITY=1 scripts/ci/snapshot-recapture-gate.sh user-audit audit-explorer` reaches and passes step (d) (parity lane) and prints the all-green PASS line.
    - The rendered admin reflects the elevated bar (styled sg-* admin, not unstyled) on the generated host.
  </acceptance_criteria>
  <done>Generated-host runtime parity is proven: a freshly generated host renders the elevated styled admin UI.</done>
</task>

</tasks>

<verification>
- `mix test test/sigra/install/golden_diff_test.exs` exits 0 against phx_new 1.8.7.
- `scripts/ci/admin-acceptance-smoke.sh` (or `RUN_PARITY=1` via the recapture gate) exits 0 and renders the styled elevated admin on a fresh host.
- No installer template / golden fixture edits were made to mask archive drift.
</verification>

<success_criteria>
Generated-host parity is proven both byte-wise (install-golden vs phx_new 1.8.7) and at runtime
(admin-acceptance-smoke renders the elevated styled admin on a fresh host) — closing the
RATIFY-01 generated-host-parity obligation.
</success_criteria>

<output>
Create `.planning/phases/204-terminal-ratification/204-04-SUMMARY.md` when done.
</output>