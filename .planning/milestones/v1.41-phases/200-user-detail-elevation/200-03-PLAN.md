---
phase: 200-user-detail-elevation
plan: 03
type: execute
wave: 3
depends_on:
  - 200-01
  - 200-02
files_modified:
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
  - guides/reference/admin-quality-ledger.md
  - guides/reference/admin-design-contract.md
autonomous: true
requirements:
  - DETAIL-03
  - DETAIL-04
user_setup: []

must_haves:
  truths:
    - "A new `user-sessions` Playwright checkpoint captures the dedicated sessions page (Revoke all sessions visible) and the `user-detail` checkpoint no longer asserts a Revoke session button on the detail page."
    - "The `user-show-live` ledger cell is ratcheted to a bare `2` with expanded Tier-2 proxy evidence, and a new clean `user-sessions` ledger cell is added."
    - "The admin design contract Detail Archetype block documents the new JTBD-first composition and explicitly notes the preserved extra_detail_sections/1 seam."
    - "Affected snapshot slugs (user-detail, user-sessions) are recaptured through the recapture gate; the impersonation-banner and board-notice canaries stay byte-stable; allowlists end empty."
  artifacts:
    - "user-sessions checkpoint slug + baseline PNGs"
    - "user-sessions L3 ledger cell; ratcheted user-show-live cell (Tier 2)"
  key_links:
    - "user-sessions checkpoint navigates to /admin/users/:id/sessions (route from Plan 01)."
    - "user-detail checkpoint assertion update reflects the removed revoke flow (Plan 02)."
    - "Ledger column-4 stays a single [012] integer so the monotonic guard awk parse keeps working (D-09)."
---

<objective>
Land the verification + evidence for the elevated User Detail and new Sessions surface (depends on Plans 01 and 02): add a `user-sessions` Playwright checkpoint and fix the `user-detail` checkpoint for the moved revoke flow, recapture only the affected slugs through the recapture gate (D-10), ratchet the `user-show-live` ledger cell to bare Tier `2` with proxy evidence plus add a clean `user-sessions` cell (D-09), and update the admin design contract Detail Archetype block to document the new composition and the preserved host seam (D-08).

Purpose: DETAIL-04 requires the page proven award-grade across the matrix with the ledger cell ratcheted; DETAIL-03's APG dialog evidence is anchored on the new surface; success-criterion-2 requires the design contract updated.

Output: updated Playwright checkpoints + recaptured baselines, the ratcheted/extended quality ledger, and the updated design contract — all green, canaries intact, allowlists empty.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-user-detail-elevation/200-CONTEXT.md
@.planning/phases/200-user-detail-elevation/200-PATTERNS.md
@.planning/phases/200-user-detail-elevation/200-UI-SPEC.md
@.planning/phases/200-user-detail-elevation/200-01-SUMMARY.md
@.planning/phases/200-user-detail-elevation/200-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- New Playwright checkpoint slug: `user-sessions` (+ its baseline PNGs across the 3 admin checkpoint projects).
- New L3 quality-ledger cell: `user-sessions`.
- Ratcheted ledger cell: `user-show-live` Tier 1 → bare `2` with expanded Tier-2 proxy evidence.
- Updated `admin-design-contract.md` Detail Archetype composition block.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add user-sessions checkpoint and fix the user-detail checkpoint for the moved revoke flow</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts (the `openUserDetail` helper `:74-81`, the user-detail checkpoint block `:218-231`, the impersonation-banner canary `:268`)
    - .planning/phases/200-user-detail-elevation/200-PATTERNS.md (checkpoint clone guidance `:219-234`)
    - .planning/phases/200-user-detail-elevation/200-UI-SPEC.md (Snapshot Recapture Scope `:343-350`, State Matrix `:310-323`)
    - .planning/phases/200-user-detail-elevation/200-02-SUMMARY.md (what the recomposed detail page now shows)
  </read_first>
  <action>
    In test/example/priv/playwright/tests/admin-checkpoints.spec.ts: update the `user-detail` checkpoint block (`:218-231`) so it no longer asserts a `Revoke session` button on the detail page — that control moved to UserSessionsLive (Plan 02 removed it). Keep the `Global user operations` and `Start impersonation` visibility assertions, and add an assertion that the new `Manage sessions` link is visible on the detail page. Then add a NEW checkpoint named `user-sessions` immediately after: navigate to `/admin/users/${userId}/sessions` (add a small helper mirroring `openUserDetail` or navigate directly using the same target user the existing block resolves), `await waitForLiveViewReady(page)`, assert the `Revoke all sessions` button is visible, then `captureAndVerify(page, testInfo, 'user-sessions')` and `assertCheckpointScreenshot(page, testInfo, 'user-sessions')` mirroring the existing two-call capture+assert structure.

    Do NOT touch the `impersonation-banner` canary block (`:268`) — it must stay byte-stable (D-10). Do not rename or alter the modal-interaction selectors elsewhere.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c "user-sessions" test/example/priv/playwright/tests/admin-checkpoints.spec.ts && grep -c "Revoke session" test/example/priv/playwright/tests/admin-checkpoints.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'user-sessions'" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` returns at least 2 (captureAndVerify + assertCheckpointScreenshot).
    - `grep -F "Manage sessions" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` returns at least 1 (detail page now asserts the link-out).
    - The user-detail block no longer asserts `getByRole('button', { name: 'Revoke session' })` — `grep -F "name: 'Revoke session'" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` returns 0.
    - `grep -c "impersonation-banner" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` is unchanged from baseline (canary block intact).
  </acceptance_criteria>
  <done>The checkpoints spec captures the new user-sessions page (Revoke all visible) and the user-detail checkpoint reflects the moved revoke flow + the new Manage sessions link-out, with the canary untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Recapture the user-detail and user-sessions snapshot slugs through the recapture gate</name>
  <files>test/example/priv/playwright/tests/__screenshots__ (recaptured user-detail + user-sessions baselines)</files>
  <read_first>
    - scripts/ci/snapshot-recapture-gate.sh (slug-arg routing; RECAPTURE_DRYRUN seam)
    - scripts/ci/snapshot-canary-guard.sh (CANARY=impersonation-banner / design --canary board-notice)
    - .planning/phases/200-user-detail-elevation/200-CONTEXT.md (D-10 recapture routing + canary/allowlist rules)
    - CLAUDE.md (Example Playwright boot: PORT/PG gotcha; SIGRA_EXAMPLE_URL=http://localhost:4011 for local runs)
  </read_first>
  <action>
    Recapture ONLY the affected checkpoint slugs through `scripts/ci/snapshot-recapture-gate.sh user-detail user-sessions` (D-10) — NOT the canary guard. Boot the example demo server on the alternate port per CLAUDE.md (default 4000 collides with Rulestead Docker; use 4011 with `SIGRA_EXAMPLE_URL=http://localhost:4011`), ensure the demo DB is seeded with the admin persona the checkpoints target, and pre-compile to avoid live-reload crash. The recapture gate routes each positional slug to the lane(s) whose snapshot dir contains it (checkpoint lane here) and runs the canary/drift guard for that lane.

    The `impersonation-banner` checkpoint canary and `board-notice` design canary MUST stay byte-stable — confirm they are NOT in the recaptured set. Do NOT recapture mg-9/mg-10/mg-11 design boards: this phase did not change the gallery board markup (Plan 02 introduced no new `sg-summary-facts` arrangement; gallery is owned by Phase 203). Leave both allowlists empty at end-of-phase (Phase 204 owns the terminal reset, but Phase 200 stays green on its own).

    If a full live recapture cannot run in this environment (no booted demo / saturated local Postgres), run `RECAPTURE_DRYRUN=1 scripts/ci/snapshot-recapture-gate.sh user-detail user-sessions` to prove the routing (both slugs route to the checkpoint lane, canary untouched) and record in the SUMMARY that the live PNG recapture is deferred to CI per the zero-human-UAT model.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && RECAPTURE_DRYRUN=1 bash scripts/ci/snapshot-recapture-gate.sh user-detail user-sessions 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run routing output shows `user-detail` and `user-sessions` routed to the checkpoint lane and does NOT route either canary slug.
    - `git status --porcelain test/example/priv/playwright` shows NO modification to any `impersonation-banner*` or `board-notice*` baseline (canaries byte-stable).
    - No new entries remain in the recapture allowlists at end of plan (allowlists empty).
  </acceptance_criteria>
  <done>The user-detail and user-sessions baselines are recaptured (or routing-proven with live recapture deferred to CI), canaries byte-stable, allowlists empty.</done>
</task>

<task type="auto">
  <name>Task 3: Ratchet the user-show-live ledger cell to Tier 2, add the user-sessions cell, and update the design contract</name>
  <files>guides/reference/admin-quality-ledger.md, guides/reference/admin-design-contract.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (Asserting Tier 2 convention `:32-54`, the `user-show-live` cell `:88`, the L3 row format `:85-91`)
    - guides/reference/admin-design-contract.md (Detail Archetype block `:251-286`)
    - guides/reference/admin-fractal-scorecard.md (Tier-2 proxy definitions `:123-167`)
    - .planning/phases/200-user-detail-elevation/200-UI-SPEC.md (Quality-Ledger Ratchet Contract `:327-350`, Tier-2 Proxy Evidence table `:333-341`)
    - .planning/phases/200-user-detail-elevation/200-CONTEXT.md (D-08 contract update, D-09 ledger ratchet)
  </read_first>
  <action>
    In guides/reference/admin-quality-ledger.md: ratchet the `user-show-live` row (`:88`) column-4 Tier value from `1` to a BARE `2` — single `[012]` integer, NO decorators/footnotes/asterisks (D-09; the monotonic guard's positional `awk -F'|'` parse depends on column-4 being a single integer). Expand its Evidence column to cite each applicable Tier-2 proxy per the UI-SPEC Proxy Evidence table: axe-while-open + 7 APG gates (admin-modal-interaction.spec.ts), desktop↔mobile content-equivalence (admin-design.spec.ts MG-5/6), glossary-clean (glossary_test.exs), plus the documented-as-manual proxies (no `transition: all`, `sg-stack--N` density rhythm, target-size ≥24px). Then ADD a new L3 ledger cell `user-sessions` authored clean, citing its new `user-sessions` checkpoint slug and — since it now owns the confirm dialog — the admin-modal-interaction.spec.ts APG/axe-while-open evidence. Whether `user-sessions` lands Tier 1 or Tier 2 is a judgment call (author it clean; default Tier 1 acceptable, must not regress).

    In guides/reference/admin-design-contract.md: replace the Detail Archetype "Current component composition" diagram (`:255-279`) with the JTBD-first composition from the UI-SPEC Page Composition Contract (identity bar → Sessions preview/link-out → Security+Identities grid → Organizations preview → Recent audit → host extra-sections → Danger Zone; confirm dialog now on UserSessionsLive). Add an explicit Note that the `extra_detail_sections/1` host seam is preserved and rendered after lib sections / before Danger Zone (D-08, success-criterion-2).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && awk -F'|' '/user-show-live/{gsub(/ /,"",$4); print "col4="$4}' guides/reference/admin-quality-ledger.md</automated>
  </verify>
  <acceptance_criteria>
    - The `user-show-live` ledger row column-4 is exactly `2` (bare integer) — `awk -F'|' '/user-show-live/{gsub(/ /,"",$4); print $4}' guides/reference/admin-quality-ledger.md` prints `2`.
    - A `user-sessions` row exists in the ledger — `grep -c '| user-sessions ' guides/reference/admin-quality-ledger.md` returns 1.
    - The monotonic quality guard (`scripts/ci/quality-ledger-monotonic.sh`, vs origin/main) exits 0 (no Tier decrease; `user-show-live` increased 1→2).
    - `grep -F 'extra_detail_sections' guides/reference/admin-design-contract.md` returns at least 1 (host seam documented in the Detail Archetype block).
    - The Detail Archetype block no longer shows the old `[Sessions card]` full-table + inline `[confirm dialog]` composition (replaced by previews + link-out; confirm noted as on UserSessionsLive).
  </acceptance_criteria>
  <done>The user-show-live cell is Tier 2 with proxy evidence, a clean user-sessions cell exists, the monotonic guard is green, and the design contract documents the new composition + preserved seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI monotonic guard → quality ledger | the guard parses ledger column-4 positionally; a malformed cell could false-pass |
| recapture gate → snapshot baselines | recapturing the wrong slug could mask a real visual regression (canary) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-09 | Tampering | quality-ledger column-4 parse | high | mitigate | Ratchet to a BARE `2` only (no decorators) so the `awk -F'|'` parse stays valid (D-09); verified by the monotonic guard exiting 0 and the awk col4 assertion. |
| T-200-10 | Spoofing/regression-masking | snapshot recapture | high | mitigate | Recapture ONLY user-detail + user-sessions through the recapture gate (not the canary guard); assert impersonation-banner + board-notice canaries are byte-stable; allowlists empty (D-10). |
| T-200-11 | Repudiation | DETAIL-03 APG evidence | low | accept | The APG dialog evidence is anchored by the existing admin-modal-interaction.spec.ts (unchanged selectors from Plan 01); this plan only cites it in the ledger. |
| T-200-12 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — N/A; no new dependencies. |
</threat_model>

<verification>
- `user-sessions` checkpoint added; `user-detail` checkpoint updated (no Revoke session assertion; Manage sessions asserted); impersonation-banner canary block unchanged.
- Recapture gate routes user-detail + user-sessions to the checkpoint lane; canaries byte-stable; allowlists empty.
- `user-show-live` ledger column-4 is bare `2`; `user-sessions` cell present; monotonic guard exits 0 vs origin/main.
- Design contract Detail Archetype block documents the JTBD-first composition + preserved extra_detail_sections/1 seam.
</verification>

<success_criteria>
- DETAIL-03: APG destructive-confirm evidence anchored on the new sessions checkpoint + modal-interaction spec.
- DETAIL-04: page proven across the matrix via recaptured baselines; `user-show-live` ratcheted to Tier 2 with proxy evidence; monotonic guard green.
- Design contract updated (D-08); canaries intact; allowlists empty.
</success_criteria>

<output>
Create `.planning/phases/200-user-detail-elevation/200-03-SUMMARY.md` when done.
</output>
