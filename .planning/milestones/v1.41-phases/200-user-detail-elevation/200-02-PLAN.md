---
phase: 200-user-detail-elevation
plan: 02
type: execute
wave: 2
depends_on:
  - 200-01
files_modified:
  - lib/sigra/admin/live/user_show_live.ex
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
user_setup: []

must_haves:
  truths:
    - "The identity header is one calm bar: primary identity (name/email/id) + exactly one priority alert (summary_alert/1, locked>unconfirmed>no-MFA) + a compact key-metrics strip — no stacked 4-fact <dl> sitting under a separate pills cluster + alert."
    - "Sessions is a bounded display-only preview (max 3 rows, no revoke buttons) with a 'Manage sessions' link to /admin/users/:id/sessions; the session revoke handlers and confirm overlay no longer exist on this page."
    - "Organizations is a bounded preview (max 3) with a 'View all organizations' link; Recent audit keeps its existing 'View full audit' link-out unchanged."
    - "The extra_detail_sections/1 host seam renders unchanged (dual atom/string :title/:body reads) after lib-owned sections and before Danger Zone."
  artifacts:
    - "lib/sigra/admin/live/user_show_live.ex (recomposed JTBD-first)"
  key_links:
    - "summary_alert/1, status_pills/1, mfa_value/1, last_activity/1, passkey_count/1 reused unchanged in the identity bar."
    - "The 'Manage sessions' link targets the route created in Plan 01 (/admin/users/:id/sessions)."
    - "extra_detail_sections render block at the same position relative to Danger Zone (generated-host contract D-07)."
---

<objective>
Recompose `lib/sigra/admin/live/user_show_live.ex` in place into a calm, JTBD-first User Detail page: a single calm identity bar (DETAIL-01 / D-01), Sessions and Organizations reduced to bounded previews with link-outs (DETAIL-02 / D-04, D-05), the session revoke flow and confirm overlay removed (they now live on `UserSessionsLive` from Plan 01), and the `extra_detail_sections/1` host seam preserved exactly (D-07).

Purpose: deliver "orient in under two seconds" — primary identity + exactly one priority alert + a thin metrics strip, with unbounded sub-lists as link-outs rather than inline stacks.

Output: a restructured `user_show_live.ex` that compiles clean, reuses existing `sg-*` primitives and presentation helpers, and depends on the Sessions route + page shipped in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-user-detail-elevation/200-CONTEXT.md
@.planning/phases/200-user-detail-elevation/200-PATTERNS.md
@.planning/phases/200-user-detail-elevation/200-UI-SPEC.md
@.planning/phases/200-user-detail-elevation/200-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces no new public symbols. It REMOVES from `user_show_live.ex` (they moved to UserSessionsLive in Plan 01): the `open_revoke_session` / `open_revoke_all_sessions` / `confirm_action` event handlers, the `confirm_action` assign, the confirm overlay markup, `revoke_session_copy/1`, `revoke_all_sessions_copy/1`, and any now-unused session helpers. It introduces NO new `sg-*` CSS class (reuse-only). It adds a private `_path` helper for the Sessions link-out (`/admin/users/:id/sessions`, scope-aware) and a bounded-preview cap.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Recompose the identity header into one calm identity bar (DETAIL-01)</name>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex (current header `:102-139`, helpers `status_pills/1` `:435-444`, `mfa_value/1` `:463-469`, `last_activity/1` `:483-493`, `passkey_count/1` `:478-479`, `summary_alert/1` `:497-513`)
    - .planning/phases/200-user-detail-elevation/200-UI-SPEC.md (Page Composition Contract identity bar `:146-152`, Typography `:68-79`, Component Inventory `:120-135`, Copywriting Contract `:206-247`)
  </read_first>
  <action>
    Recompose the `sg-page-header` block (currently `:102-139`: a cluster of `status_pills` + a separate 4-fact `<dl class="sg-summary-facts">` + a `<.notice>`) into a single calm identity bar per the UI-SPEC composition. Primary identity: kicker `User`, h1 `@detail.display_name || @detail.user.email` (do NOT force nowrap — long names must wrap, State Matrix), secondary muted email + `sg-code` UUID. Then exactly one priority alert via the existing `summary_alert/1` (already single-priority locked>unconfirmed>no-MFA — plug in unchanged). Then a compact key-metrics strip reduced to the 3-4 most scan-worthy facts (e.g. active sessions count, MFA via `mfa_value/1`, last seen via `last_activity/1`) using the existing `sg-summary-facts` / `sg-kv__term` / `sg-kv__value` pattern (keep `sg-summary-facts__num` on numeric values for tabular alignment). Keep the compact status-pill cluster via `status_pills/1`.

    Reuse the existing helpers unchanged (D-01) — do NOT add a new function component unless it is genuinely reused elsewhere on this page (it is not). Introduce NO new `sg-*` CSS class; reuse `sg-page-header`, `sg-summary-facts`, `sg-status-pill`, `sg-cluster`, `sg-stack--N`. This keeps the three-copy CSS parity untouched and avoids the D-02 mg-9/admin-design.spec.ts:304 cascade (no new `.sg-summary-facts` arrangement requiring a board+assertion update). Use only `--sg-*` tokens; no inline hex, no `transition: all`. The page kicker copy is `User` per the Copywriting Contract.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sg-summary-facts' lib/sigra/admin/live/user_show_live.ex` shows the metrics strip still present (>=1) but there is exactly one `<dl class="sg-summary-facts">` block (single identity-bar metrics strip, not a second stacked one).
    - `grep -F 'summary_alert(@detail)' lib/sigra/admin/live/user_show_live.ex` returns at least 1 (single priority alert reused).
    - `grep -F 'sg-page-kicker">User' lib/sigra/admin/live/user_show_live.ex` returns 1 (kicker copy per contract).
    - `grep -c 'transition: all' lib/sigra/admin/live/user_show_live.ex` returns 0.
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>The identity header renders one calm bar: identity + one priority alert + a compact metrics strip, reusing existing helpers and primitives with no new CSS class.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Sessions and Organizations to bounded previews + link-outs; remove the session revoke flow (DETAIL-02)</name>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex (Sessions card `:141-194`, revoke handlers `:42-94`, Organizations `:228-253`, Recent audit `:255-273`, confirm overlay `:315-333`, `pivot_path/4` `:379-387`, `full_audit_path/3` `:394-401`, `reload_detail/2` `:338-341`)
    - .planning/phases/200-user-detail-elevation/200-CONTEXT.md (D-04 sessions link-out, D-05 orgs preview + audit preserved)
    - .planning/phases/200-user-detail-elevation/200-UI-SPEC.md (Sessions preview `:154-158`, Organizations preview `:164-167`, Recent audit `:169-172`, State Matrix `:310-323`, Copywriting Contract `:206-247`)
  </read_first>
  <action>
    Replace the full Sessions card (`:141-194`) with a bounded display-only preview (D-04): an `sg-card sg-stack sg-stack--3` with heading `Sessions` + a count, a preview of at most 3 sessions rendered display-only (Type / IP / Last activity — NO per-row revoke button, NO revoke-all button), an `<.empty_state title="No active sessions">` (body: `This user has no active sessions in the current scope.`) when there are none, and a `Manage sessions` link (native `<a>`, `sg-btn sg-btn--secondary sg-btn--sm`) to the new sessions route. Add a scope-aware private `sessions_path/3` helper (clone the `full_audit_path/3` shape `:394-401`): global `/admin/users/#{user_id}/sessions`, org `/admin/organizations/#{slug}/users/#{user_id}/sessions`, threaded through `with_return_to/2`. The "Manage sessions" CTA must meet the ≥44px/≥24px target-size guidance (it already does via `sg-btn`).

    REMOVE from this file (they live on UserSessionsLive now, per D-04): the `open_revoke_session`, `open_revoke_all_sessions`, and `confirm_action` event handlers (`:42-94`), the `:confirm_action` assigns in `mount/3` and `handle_params/3`, the entire confirm overlay markup (`:315-333`), and the now-unused helpers `revoke_session_copy/1` / `revoke_all_sessions_copy/1` / `reload_detail/2` (and `cancel_confirm` if no longer referenced). Also remove the "Revoke all sessions" danger button currently in the Danger Zone (`:295-302`) — session revocation is fully deferred to UserSessionsLive (Claude's-discretion default per D-04 / CONTEXT "defer entirely"). Keep the Danger Zone's impersonation start form and account-state copy.

    Convert Organizations (`:228-253`) to a bounded preview (D-05): cap to at most 3 org rows (org name + role), keep the existing per-org `pivot_path/4` link where shown, and add a `View all organizations` link when there are more than 3. Empty state title `No organizations`, body `This user has not joined any organizations.` per the Copywriting Contract. Leave Recent audit (`:255-273`) and its `full_audit_path/3` `View full audit` link-out unchanged (D-05).

    Use only existing `sg-*` primitives and `--sg-*` tokens; introduce NO new CSS class; no `transition: all`. All copy verbatim from the Copywriting Contract (glossary-clean).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'open_revoke_session\|open_revoke_all_sessions\|user-session-confirm-overlay\|sg-confirm-dialog' lib/sigra/admin/live/user_show_live.ex` returns 0 (session revoke flow + confirm overlay fully removed from this file).
    - `grep -F 'Manage sessions' lib/sigra/admin/live/user_show_live.ex` returns 1 (sessions link-out present).
    - `grep -F '/sessions' lib/sigra/admin/live/user_show_live.ex` shows the sessions_path helper targets the Plan 01 route.
    - `grep -F 'View all organizations' lib/sigra/admin/live/user_show_live.ex` returns 1 and `grep -F 'View full audit' lib/sigra/admin/live/user_show_live.ex` returns 1 (org preview link + preserved audit link-out).
    - `mix compile --warnings-as-errors` clean (no unused-function warnings — confirms the removed helpers are fully gone).
  </acceptance_criteria>
  <done>Sessions and Organizations are bounded previews with link-outs, the destructive session flow and confirm overlay are gone from User Detail, Recent audit link-out is preserved, and the file compiles warning-free.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the host extra-section seam and verify JTBD composition order (DETAIL-02 / D-07)</name>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex (extra-section render `:310-313`, Danger Zone `:275-308`)
    - lib/sigra/admin/users/hooks.ex (the `@callback extra_detail_sections/1` contract `:24`)
    - lib/sigra/admin/users/default_hooks.ex (the `[]` default `:24`)
    - lib/sigra/admin/users/detail.ex (the `extra_detail_sections` data path `:35`)
    - .planning/phases/200-user-detail-elevation/200-UI-SPEC.md (Page Composition Contract full target order `:142-181`, host-seam note `:174-175`)
  </read_first>
  <action>
    Confirm and preserve the `extra_detail_sections/1` host seam EXACTLY (D-07, generated-host contract — the example's `[]` no-op will NOT catch a regression). Keep the render block that iterates `@detail.extra_detail_sections` into distinct `sg-card` sections with the dual atom/string reads (`Map.get(section, :title) || Map.get(section, "title")` and the `:body`/`"body"` pair), positioned AFTER all lib-owned sections (Sessions preview, Security+Identities grid, Organizations preview, Recent audit) and BEFORE the Danger Zone — matching the UI-SPEC target order. Do NOT change the callback signature on `hooks.ex`, the default in `default_hooks.ex`, or the `detail.ex` data path.

    After the Task 1/2 restructure, order the rendered sections to the JTBD-first composition from the UI-SPEC: [1] identity bar, [2] Sessions preview, [3] Security+Identities grid, [4] Organizations preview, [5] Recent audit, [6] host extra-sections, [7] Danger Zone. Outer rhythm stays `sg-stack sg-stack--6`; each card stays `sg-card sg-stack sg-stack--3`. Native `<details>`/`hidden` only if any section is collapsed — NO JS-driven disclosure (D-03). Do not introduce new CSS.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/sigra/admin/glossary_test.exs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'Map.get(section, :title) || Map.get(section, "title")' lib/sigra/admin/live/user_show_live.ex` returns 1 (dual-key seam read preserved).
    - `grep -F 'extra_detail_sections' lib/sigra/admin/live/user_show_live.ex` returns at least 1 and the render iterates `@detail.extra_detail_sections`.
    - `git diff --quiet lib/sigra/admin/users/hooks.ex lib/sigra/admin/users/default_hooks.ex lib/sigra/admin/users/detail.ex` — these three host-seam files are UNCHANGED (exit 0).
    - The extra-section render block appears before the Danger Zone block in source order (manual confirm via reading; verify the `sg-danger-panel` block follows the `extra_detail_sections` loop).
    - `mix compile --warnings-as-errors` clean and `mix test test/sigra/admin/glossary_test.exs` passes.
  </acceptance_criteria>
  <done>The host extra-section seam is byte-preserved and rendered after lib sections / before Danger Zone, the JTBD section order matches the UI-SPEC, and the glossary guard still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app → extra_detail_sections/1 callback | generated host injects detail sections via a read-only data callback |
| operator browser → User Detail page | admin operator views a per-user detail page keyed by an `:id` path param |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-05 | Tampering/contract | extra_detail_sections/1 host seam | high | mitigate | Preserve callback signature, data path, and dual atom/string :title/:body render position exactly (D-07). Verified by `git diff --quiet` on hooks.ex/default_hooks.ex/detail.ex + grep on the render read — the example no-op cannot catch a break, so the contract files are guarded directly. |
| T-200-06 | Information disclosure | identity bar / previews | low | accept | The page renders only data already loaded scope-safely by `Detail.load!/3` (unchanged); bounding previews to 3 rows reduces, never widens, exposure. |
| T-200-07 | Elevation | removed session-revoke controls | low | accept | Destructive controls are removed from this page (deferred to the scope-gated UserSessionsLive in Plan 01); this plan only shrinks the destructive surface on User Detail. |
| T-200-08 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — N/A; no new dependencies. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (no unused-function warnings — proves the removed revoke helpers are fully gone).
- `mix test test/sigra/admin/glossary_test.exs` — 0 failures (copy stays glossary-clean).
- `git diff --quiet lib/sigra/admin/users/hooks.ex lib/sigra/admin/users/default_hooks.ex lib/sigra/admin/users/detail.ex` — host-seam contract files unchanged.
- No new `sg-*` CSS class introduced (CSS three-copy parity untouched).
- No `transition: all` and no JS-driven disclosure introduced.
</verification>

<success_criteria>
- Identity header is one calm bar (identity + one priority alert + compact metrics) — DETAIL-01.
- Sessions + Organizations are bounded previews with link-outs; session revoke flow + confirm overlay removed; Recent audit link-out preserved; host seam intact — DETAIL-02.
- File compiles warning-free; host-seam contract files untouched; glossary clean.
</success_criteria>

<output>
Create `.planning/phases/200-user-detail-elevation/200-02-SUMMARY.md` when done.
</output>
