---
phase: 238-generated-auth-runtime-proof
plan: "09"
type: execute
wave: 8
depends_on:
  - 238-08
files_modified:
  - test/example/priv/playwright/tests/generated-auth.spec.ts
  - test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
  - test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
  - .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "AUTH-01 / D-01 / D-02 / D-05: after registration, both generated-auth browser proofs reach an unauthenticated state through the rendered generated sessions control and its server-side revocation path; browser cookie or storage mutation does not substitute for that transition."
    - "AUTH-02 / D-03: the focused and complete proofs still begin Google OAuth at the generated /auth/google route from a visibly logged-out browser and reach the loopback signed-state/PKCE collision outcome."
    - "AUTH-03 / D-04 / D-06: the complete journey retains scoped Axe/DOM assertions, role/label locators, observable LiveView readiness, serial execution, zero retries, and no elapsed-time synchronization."
    - "A fast source contract inspects both complete generated-auth spec files and fails on any browser cookie/storage mutation used to manufacture auth state, rather than checking only a helper with one expected name."
    - "The replacement runtime receipt identifies a successful workflow and direct generated-auth job whose head SHA exactly equals the final browser/source-contract correction commit, while preserving every earlier attempt including run 31287052180."
  artifacts:
    - path: test/example/priv/playwright/tests/generated-auth.spec.ts
      provides: "Complete email/OAuth journey with a rendered post-registration session revocation transition"
      contains: "Log out this device"
    - path: test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
      provides: "Focused OAuth collision proof with the same rendered post-registration transition"
      contains: "/users/sessions"
    - path: test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
      provides: "Whole-file guard for both generated-auth specs against cookie/storage auth-state shortcuts"
    - path: .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
      provides: "Fresh exact-correction-SHA generated-host receipt with retained lineage"
      contains: "previous_attempts"
  key_links:
    - from: test/example/priv/playwright/tests/generated-auth.spec.ts
      to: priv/templates/sigra.install/core/session_live.ex
      via: "role-addressable Log out this device control revokes the authenticated generated session"
      pattern: "Log out this device"
    - from: test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
      to: priv/templates/sigra.install/core/session_live.ex
      via: "the focused probe traverses the same generated sessions route before starting OAuth"
      pattern: "users/sessions"
    - from: test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
      to: test/example/priv/playwright/tests/generated-auth.spec.ts
      via: "the contract scans the complete journey and focused probe, independent of helper naming"
      pattern: "@journey"
    - from: .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
      to: .github/workflows/generated-auth-runtime-proof.yml
      via: "receipt workflow/run/job metadata matches one fresh successful workflow_dispatch on the immutable correction SHA"
      pattern: "head_sha"
---

<objective>
Close Phase 238's sole verifier blocker by replacing both post-registration auth-state shortcuts with rendered generated session revocation, locking that invariant across both browser specs, and ratifying the correction with fresh exact-SHA CI evidence.

Purpose: The current runtime suite is green but contradicts Plan 02's explicit browser-visible journey truth because both specs mutate the browser cookie jar after registration. The proof must demonstrate the generated transition it claims.
Output: Two corrected generated-auth specs, a non-vacuous whole-file source guard, and a replacement exact-SHA generated-host receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md
@.planning/phases/238-generated-auth-runtime-proof/238-VERIFICATION.md
@.planning/phases/238-generated-auth-runtime-proof/238-02-PLAN.md
@.planning/phases/238-generated-auth-runtime-proof/238-08-SUMMARY.md
@.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
@test/example/priv/playwright/tests/generated-auth.spec.ts
@test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
@test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
@priv/templates/sigra.install/core/session_live.ex
@scripts/ci/generated-auth-runtime-proof.sh
@.github/workflows/generated-auth-runtime-proof.yml
</context>

<gap_closure>
## Sole verification gap closed by this plan

| Failed observable truth | Root cause | Exact closure proof |
|-------------------------|------------|---------------------|
| All interactions use generated routes/forms and the browser session; direct account/context calls and cookie clearing do not substitute for the rendered journey. | The complete journey's post-registration helper and the focused OAuth probe's logout helper directly erase browser cookies before navigating to login. The existing source contract checks only one specifically named helper in one file, so both shortcuts escaped the gate. | Traverse `/users/sessions`, wait for connected LiveView readiness, activate the role-visible `Log out this device` control with its confirmation, prove login plus protected-route denial in both specs, add a whole-file two-spec source guard, and run both allowlisted specs in fresh exact-SHA CI. |
</gap_closure>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace both post-registration shortcuts with rendered generated session revocation</name>
  <files>test/example/priv/playwright/tests/generated-auth.spec.ts, test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts, test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs</files>
  <read_first>
    - `238-VERIFICATION.md` gap entry and disconfirmation pass for the exact failed Plan 02 truth and the weakness in the existing helper-name-specific source assertion.
    - `238-02-PLAN.md` must-have truths and success criteria for D-01, D-02, D-05, rendered transitions, browser-session continuity, and deterministic synchronization.
    - `generated-auth.spec.ts` for `clearBrowserSession`, the proven `logOut` helper, `waitForLiveViewReady`, `assertAuthState`, and the Plan 08 independent BrowserContext reset-session proof that must remain intact.
    - `generated-auth-oauth-probe.spec.ts` for its post-registration helper and focused generated `/auth/google` state/PKCE/collision path.
    - `priv/templates/sigra.install/core/session_live.ex` for the `revoke_current` event, `Log out this device` accessible name, confirmation hook, and login redirect.
    - `phase_238_generated_auth_runtime_proof_test.exs` for the current narrow logout regex and existing no-delay, role/label, LiveView-readiness, accessibility, and two-spec partition contracts.
  </read_first>
  <behavior>
    - Test 1: after generated registration signs the browser in, each spec visits `/users/sessions`, observes a connected LiveView, accepts the rendered confirmation, activates `Log out this device`, and reaches the role-visible `Sign in` state at `/users/log_in`.
    - Test 2: requesting `/users/settings` after that transition returns to `/users/log_in` with the `Sign in` heading, proving server-side session invalidation rather than crediting a public redirect alone.
    - Test 3: the complete journey continues from the logged-out state through confirmation, password, magic-link, reset, independent-session revocation, Google collision, and D-04/D-06 auth-state assertions; the focused probe continues through signed state, S256 PKCE, and collision.
    - Test 4: the Phase 238 source contract reads both browser specs in full and rejects cookie/storage mutation primitives regardless of helper name, while retaining its existing checks against browser timing delays.
  </behavior>
  <action>
    Write the source-contract regression first so it fails against both current specs. Replace the helper-name-specific logout assertion with a whole-file predicate over both `journey` and `oauth_probe` that rejects Playwright cookie-jar or storage-state mutation primitives anywhere in either file. Keep the independent BrowserContext and extra-page setup used by Plan 08 to prove cross-session reset invalidation; that is a real isolated browser-session scenario, not an auth-state shortcut. Retain the existing fixed-delay and JavaScript-timer guards.

    In `generated-auth.spec.ts`, remove `clearBrowserSession` and reuse the already-proven rendered `logOut(page)` path immediately after registration. The helper must navigate to `/users/sessions`, wait for `[data-phx-session].phx-connected`, locate the button named `Log out this device`, accept its rendered confirmation dialog, activate the control, assert `/users/log_in` plus the `Sign in` heading, then request `/users/settings` and assert the same denial state. Preserve the serial single-test ordering, mailbox link selection, scoped Axe/DOM checks, reset races, independent existing-session proof, and OAuth observation exactly.

    Give the focused OAuth probe the same browser-visible session-revocation behavior after registration before it begins at `/auth/google`. Keep the helper local unless an already-existing shared utility provides this exact generated-host contract; do not introduce a generic auth abstraction or modify production templates for a two-call-site proof correction. Use page navigation, LiveView readiness, role locators, dialog handling, URL checks, and visible headings only. Do not use application context functions, request clients, browser cookie/storage mutation, callback shortcuts, database/token reads, or elapsed-time waits to manufacture the transition. Preserve the loopback-only provider, signed state, S256 PKCE, and collision assertions per D-03.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs &amp;&amp; bash -n scripts/ci/generated-auth-runtime-proof.sh &amp;&amp; cd test/example/priv/playwright &amp;&amp; npx playwright test tests/generated-auth.spec.ts tests/generated-auth-oauth-probe.spec.ts --project=generated-auth --retries=0 --list &amp;&amp; git diff --check</automated>
  </verify>
  <done>Both generated-auth specs leave the post-registration session only through the rendered generated sessions UI, prove the formerly authenticated session is denied, retain their complete email/OAuth/accessibility behavior, and are guarded against cookie/storage auth-state shortcuts across each full file.</done>
</task>

<task type="auto">
  <name>Task 2: Replace runtime evidence on the exact browser-correction SHA</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`; the executor can push the scoped Phase 238 evidence ref and dispatch `.github/workflows/generated-auth-runtime-proof.yml`; if REST core remaining is 250 or fewer, or GitHub returns HTTP 403/429, execution halts until the reported reset/retry time without further polling.</precondition>
  <files>.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json</files>
  <read_first>
    - `AGENTS.md` for one watcher per run, 60-second watch intervals, one post-run structured summary, failed-log-only-on-failure, one transient retry, and hard rate-limit stops.
    - `238-EVIDENCE.json` for the complete `previous_attempts` lineage and current successful run `31287052180`; retain every entry when superseding the current top-level receipt.
    - `.github/workflows/generated-auth-runtime-proof.yml` for the evidence-ref guard, stable workflow identity, direct job ID/name, PostgreSQL/Chromium provisioning, and retained `--all` command.
    - `scripts/ci/generated-auth-runtime-proof.sh` for the two-spec allowlist, fresh canonical generated host, one-worker/zero-retry project, local OIDC double, and diagnostics.
    - The committed Task 1 diff plus `git status --short`; freeze only the three authorized test-file corrections without staging, hiding, or altering unrelated work.
  </read_first>
  <action>
    Rerun Task 1's full gate and commit the two browser specs plus Phase 238 source contract before editing evidence. Record that immutable 40-character correction SHA and confirm its authorized diff is limited to the three Task 1 files. Push it to the scoped Phase 238 evidence ref; do not include the independently modified verification report or any unrelated work.

    Immediately before the long wait, query `gh api rate_limit` once and record `resources.core.remaining` and `reset`; make no further CI polling request at 250 or fewer. Dispatch `Generated auth runtime proof` on the exact pushed ref with `evidence_ref` equal to that ref. Correlate exactly one new `workflow_dispatch` run by workflow identity, ref, dispatch time, and head SHA equal to the correction SHA; ambiguity blocks. Use exactly one watcher for that run: `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`. After completion, fetch one structured run/job summary and fetch failed logs only when the conclusion is failure. Diagnose and repair a deterministic failure with a new correction SHA and a new run. Retry a transient infrastructure failure at most once on the same SHA, never with overlapping watchers. A failed, skipped, cancelled, neutral, timed-out, wrong-SHA, or missing direct job cannot become evidence.

    Update the top-level receipt only when both the workflow and stable-ID `generated_auth_runtime_proof` job named `Generated auth runtime proof` conclude success. Preserve all existing `previous_attempts` entries and append run `31287052180`, SHA `e06d68a0f46263d37afa2d201c59ec460c8a60e4`, job `93177695136`, success conclusion, and a reason that it was superseded by the verifier-required browser-transition correction. Record the new exact head SHA, typed workflow/run/job metadata, harness command, and rate-limit preflight. Store metadata only: never logs, secrets, environment values, mailbox links, OAuth codes, browser cookies, raw tokens, or hashes.

    Commit the receipt and executor-owned `238-09-SUMMARY.md` separately after the tested correction SHA. Prove every committed path after `head_sha` is limited to those two Phase 238 evidence/tracking artifacts. Do not modify `238-VERIFICATION.md`, `238-REVIEW.md`, `STATE.md`, `ROADMAP.md`, or `REQUIREMENTS.md`; their owning workflows may consume the new receipt afterward.
  </action>
  <verify>
    <automated>evidence_path='.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json' &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs &amp;&amp; bash -n scripts/ci/generated-auth-runtime-proof.sh &amp;&amp; jq -e '.schema_version == 1 and .status == "passed" and (.head_sha | test("^[0-9a-f]{40}$")) and .workflow.name == "Generated auth runtime proof" and .workflow.path == ".github/workflows/generated-auth-runtime-proof.yml" and .run.event == "workflow_dispatch" and .run.conclusion == "success" and .job.id == "generated_auth_runtime_proof" and .job.name == "Generated auth runtime proof" and .job.conclusion == "success" and .harness_command == "GITHUB_WORKSPACE=\"$PWD\" scripts/ci/generated-auth-runtime-proof.sh --all" and any(.previous_attempts[]; .run_id == 31287052180 and .conclusion == "success" and .head_sha == "e06d68a0f46263d37afa2d201c59ec460c8a60e4" and .job_id == 93177695136)' "$evidence_path" &amp;&amp; tested_sha="$(jq -r '.head_sha' "$evidence_path")" &amp;&amp; git cat-file -e "${tested_sha}^{commit}" &amp;&amp; git merge-base --is-ancestor "$tested_sha" HEAD &amp;&amp; test -z "$(git diff --name-only "${tested_sha}..HEAD" -- | awk '$0 == ".planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json" { next } $0 == ".planning/phases/238-generated-auth-runtime-proof/238-09-SUMMARY.md" { next } { print }')" &amp;&amp; gh run view "$(jq -r '.run.id' "$evidence_path")" --repo szTheory/sigra --json headSha,conclusion,jobs,url | jq -e --arg sha "$tested_sha" --argjson job_id "$(jq -r '.job.database_id' "$evidence_path")" '.headSha == $sha and .conclusion == "success" and any(.jobs[]; .databaseId == $job_id and .name == "Generated auth runtime proof" and .conclusion == "success")'</automated>
  </verify>
  <done>The two corrected generated-auth specs pass together in a fresh canonical generated host on the exact immutable correction SHA, and the metadata-only receipt preserves all prior attempts while identifying the successful direct job for that SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered sessions UI → generated revoke event | The browser supplies an event token, while authenticated socket scope and generated auth code must own revocation and redirect behavior. |
| browser session → OAuth start route | The provider collision proof must begin from a genuinely logged-out generated browser session, not client-manufactured auth state. |
| source contract → two Playwright specs | A narrow helper-name matcher can miss equivalent forbidden shortcuts elsewhere in either proof file. |
| GitHub workflow metadata → evidence receipt | Remote success must belong to the exact final correction SHA and stable direct generated-auth job. |

## STRIDE Threat Register — OWASP ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-38 | Spoofing | post-registration logout state | high | mitigate | Traverse the rendered current-session control, accept its confirmation, and require protected-route denial plus the role-visible login state in both specs. |
| T-238-39 | Tampering | browser proof implementation | high | mitigate | Scan both complete spec sources for cookie/storage auth-state mutation independent of helper naming, while retaining the runtime path assertions. |
| T-238-40 | Elevation of Privilege | OAuth collision precondition | high | mitigate | Start generated OAuth only after server-side current-session revocation and verify the local signed-state/PKCE collision outcome. |
| T-238-41 | Repudiation | exact-SHA runtime receipt | high | mitigate | Freeze and push the three-file correction before dispatch; correlate workflow/ref/event/SHA/stable job and restrict post-SHA commits to receipt and Plan 09 summary. |
| T-238-42 | Information Disclosure | CI diagnostics and receipt | medium | mitigate | Keep the receipt metadata-only and exclude credentials, cookies, codes, mailbox links, tokens, hashes, environment values, and logs. |
| T-238-43 | Denial of Service | GitHub API/watch lifecycle | low | accept | Use one rate-limit preflight, one watcher per run at 60-second intervals, one structured summary, one transient retry maximum, and hard-stop on low budget or HTTP 403/429. |
| T-238-SC | Tampering | package supply chain | low | accept | No package, lockfile, action, or workflow change is planned; reuse the repository-locked Playwright/Axe stack and existing dispatch workflow. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Deterministic browser/accessibility proof for generated B2C email and Google journeys without credentials | 238-09 | COVERED | Both corrected specs rerun together on the exact correction SHA in a fresh generated host. |
| REQ | AUTH-01 | Complete generated-host email journey including rendered logout | 238-09 | COVERED | Post-registration session exit now uses the generated sessions UI and proves protected-route denial. |
| REQ | AUTH-02 | Credential-free Google start/callback/collision | 238-09 | COVERED | Both OAuth paths begin after rendered revocation and rerun against the unchanged local double. |
| REQ | AUTH-03 | Per-state Axe, label/control, and duplicate-ID checks | 238-09 | COVERED | Existing D-04/D-06 assertions remain mandatory in the complete exact-SHA run. |
| RESEARCH | — | Fresh generated host rather than example/source-only proof | 238-09 | COVERED | Task 2 dispatches the retained canonical generated-host workflow. |
| RESEARCH | — | Real generated browser transitions rather than direct application calls | 238-09 | COVERED | Task 1 uses `/users/sessions`, LiveView readiness, the role-visible revoke control, and denial assertions. |
| RESEARCH | — | Observable readiness, serial Playwright, zero retries, no sleeps | 238-09 | COVERED | Task 1 retains the source locks and Task 2 reruns the dedicated project. |
| RESEARCH | — | Host-local credential-free Google/OIDC double | 238-09 | COVERED | No provider change; the full exact-SHA job reruns the locked double. |
| CONTEXT | D-01 | Fresh canonical generated B2C host | 238-09 | COVERED | The exact-SHA dispatch provisions a new canonical host. |
| CONTEXT | D-02 | One serial browser-visible complete email journey | 238-09 | COVERED | The verifier-identified shortcut is replaced by a rendered session transition. |
| CONTEXT | D-03 | Credential-free local Google double and collision | 238-09 | COVERED | Existing costly boundary is preserved and rerun from a visibly logged-out browser. |
| CONTEXT | D-04 | Scoped Axe/DOM, role/label selectors, readiness, no sleeps | 238-09 | COVERED | Task 1 retains and source-locks these constraints; Task 2 executes them. |
| CONTEXT | D-05 | Complete AUTH-01 journey rather than login-only coverage | 238-09 | COVERED | The same complete journey remains intact after the transition correction. |
| CONTEXT | D-06 | Auth-state Axe coverage folded into AUTH-03 | 238-09 | COVERED | Existing state checks remain required by source and runtime proof. |
| VERIFICATION | GAP-01 | No cookie clearing substitutes for rendered generated auth transitions | 238-09 | COVERED | Both call sites are replaced, both full files are guarded, and both specs run on the receipt SHA. |

Excluded without gap: CONTEXT deferred passkey-primary identifier work, CSS/admin/CI-gate/release/Crosswake scope, and RESEARCH alternatives involving real providers, example-only proof, or new dependencies remain outside Phase 238.

## Artifacts this plan produces

- `test/example/priv/playwright/tests/generated-auth.spec.ts` — complete journey with rendered post-registration session revocation.
- `test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts` — focused OAuth probe with the same visible revocation boundary.
- `test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs` — whole-file two-spec guard against cookie/storage auth-state shortcuts.
- `.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json` — fresh exact-correction-SHA receipt retaining all historical attempts.

<verification>
1. The focused Phase 238 source test inspects both browser specs and fails on cookie/storage auth-state mutation or timing delays regardless of helper naming.
2. Playwright discovery finds exactly the complete journey and focused OAuth probe in the isolated generated-auth project with retries 0.
3. One fresh workflow-dispatch run on the immutable correction SHA provisions the canonical host and executes both specs successfully with one worker, no timing sleeps, and the local OIDC double.
4. The replacement receipt passes local schema, immutable-SHA/diff allowlist, retained-lineage, and one live structured GitHub predicate.
</verification>

<success_criteria>
- The sole 238-VERIFICATION.md gap is closed in both named artifacts through rendered generated session revocation and protected-route denial.
- The source contract cannot be bypassed by renaming the helper or moving a cookie/storage shortcut elsewhere in either generated-auth spec.
- AUTH-01, AUTH-02, AUTH-03, and D-01 through D-06 remain green together in one fresh exact-correction-SHA direct job.
- No direct application-context call, cookie/storage auth-state mutation, elapsed-time wait, callback shortcut, source-only claim, wrong-SHA run, or non-success outcome substitutes for the browser proof.
- The evidence receipt preserves complete attempt lineage and contains no secret or bearer material.
</success_criteria>

<output>
Create `.planning/phases/238-generated-auth-runtime-proof/238-09-SUMMARY.md` when done.
</output>
