---
phase: 238-generated-auth-runtime-proof
plan: 05
type: execute
wave: 4
depends_on:
  - 238-04
files_modified:
  - .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "A green `generated_auth_runtime_proof` CI job is correlated to the exact implementation commit; a skipped, cancelled, stale, wrong-workflow, or wrong-SHA run cannot close the phase."
    - "Machine-readable evidence records commit SHA, workflow/run/job identifiers, URLs, event/ref, timestamps, and success conclusions without credentials or token-bearing logs."
    - "Missing or red exact-commit evidence remains blocked with durable diagnostics; human UAT, local fixture checks, or a different commit never substitute."
  artifacts:
    - path: .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
      provides: "Exact-commit generated-auth CI evidence"
      contains: "generated_auth_runtime_proof"
  key_links:
    - from: .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
      to: .github/workflows/ci.yml
      via: "job databaseId/name and workflow run identify the dedicated generated-auth lane"
      pattern: "head_sha"
---

<objective>
Close AUTH-01 through AUTH-03 with durable exact-commit CI evidence for the generated-host runtime proof, never with a manual or wrong-SHA substitute.

Purpose: The fresh-host PostgreSQL/browser boundary is the authoritative phase proof and must remain independently auditable after execution.
Output: Validated machine-readable CI evidence tied to the implementation commit.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md
@.planning/phases/238-generated-auth-runtime-proof/238-VALIDATION.md
@.planning/phases/238-generated-auth-runtime-proof/238-04-SUMMARY.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture and validate exact-commit generated-auth CI evidence</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`, Plan 04's implementation commit exists, and the dedicated `generated_auth_runtime_proof` job is present in `.github/workflows/ci.yml`.</precondition>
  <files>.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json</files>
  <read_first>
    - `AGENTS.md` for GitHub rate-limit, single-watcher, structured-summary, retry, and durable blocker rules.
    - `.github/workflows/ci.yml` for workflow inputs, release-ref guard, job ID, and display name.
    - `.planning/phases/238-generated-auth-runtime-proof/238-04-SUMMARY.md` for the exact implementation commit and local validation results.
    - `scripts/ci/correlate-terminal-ratification-dispatch.sh` for the repository's before/after dispatch correlation concepts; do not reuse terminal-ratification-specific IDs.
  </read_first>
  <action>
    Record the clean implementation `git rev-parse HEAD` before creating evidence. Check `gh api rate_limit` once; if REST core remaining is 250 or less, wait until its reported reset without further polling. Dispatch the existing `CI` workflow on a scoped evidence branch/ref with `recapture_branch` set to that same ref so `release_ref_guard` accepts the manual evidence run, or use an already-triggered pull-request run whose `headSha` exactly matches the implementation SHA. Correlate exactly one new run by workflow identity, event, ref, creation time, and head SHA; reject multiple ambiguous matches.

    Use exactly one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`. After completion, fetch the structured run summary once and the job list once. Fetch failed logs only if the conclusion is failure; retry one transient infrastructure failure once, but diagnose deterministic failures and do not waive them. Require workflow conclusion `success` and dedicated job conclusion `success`; reject skipped/cancelled/neutral outcomes.

    Write `238-EVIDENCE.json` with schema/version, `head_sha`, workflow database/name/path, run ID/URL/event/ref/created/updated/conclusion, generated-auth job ID/name/URL/conclusion/timestamps, and the exact local harness command represented by the lane. Exclude raw logs, environment values, credentials, email links, and tokens. Validate JSON types, exact SHA equality, expected job ID/name, success conclusions, and URL presence before committing the evidence artifact. If authorization, rate limit, dispatch, correlation, or the job remains blocked, stop with durable diagnostics and leave AUTH-01 through AUTH-03 open.
  </action>
  <verify>
    <automated>jq -e --arg sha "$(jq -r '.head_sha' .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json)" '.schema_version == 1 and .head_sha == $sha and (.head_sha | test("^[0-9a-f]{40}$")) and .workflow.conclusion == "success" and .job.id == "generated_auth_runtime_proof" and .job.conclusion == "success" and (.run.url | startswith("https://github.com/szTheory/sigra/actions/runs/"))' .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json &amp;&amp; gh run view "$(jq -r '.run.id' .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json)" --repo szTheory/sigra --json headSha,conclusion,jobs | jq -e --arg sha "$(jq -r '.head_sha' .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json)" '.headSha == $sha and .conclusion == "success" and any(.jobs[]; .name == "Generated auth runtime proof" and .conclusion == "success")'</automated>
  </verify>
  <done>The committed JSON identifies one green generated-auth job on the exact implementation SHA, and any unavailable/red/ambiguous evidence is reported as blocked rather than passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → local evidence artifact | Remote run metadata must be correlated to the intended immutable commit and job. |
| evidence reader → phase completion claim | Machine evidence must distinguish success from skipped/cancelled/ambiguous outcomes. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-17 | Spoofing | workflow run correlation | high | mitigate | Match workflow identity, exact head SHA, ref/event/time window, unique run, and dedicated job name/ID. |
| T-238-18 | Repudiation | completion evidence | high | mitigate | Persist typed run/job metadata and independently re-query the recorded run in the automated verification. |
| T-238-19 | Information Disclosure | evidence JSON | medium | mitigate | Store metadata only; exclude logs, env, credentials, mailbox URLs, OAuth codes, and tokens. |
| T-238-20 | Denial of Service | API polling/rate limits | low | accept | One rate-limit check, one 60-second watcher, one structured summary, and hard stop on 403/429 follow repository policy. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Deterministic browser and accessibility proof for generated B2C email and Google journeys without credentials | 238-01..05 | COVERED | Fresh-host tracer, full suite, CI lane, exact evidence. |
| REQ | AUTH-01 | Registration, confirmation, password sign-in/logout, magic-link, password reset | 238-02, 04, 05 | COVERED | One rendered serial journey, isolated CI, exact SHA evidence. |
| REQ | AUTH-02 | Google start/callback and collision through deterministic double | 238-01, 03, 04, 05 | COVERED | Wave 0 Assent seam then same full suite and CI proof. |
| REQ | AUTH-03 | Axe, label/control, and duplicate IDs on every rendered auth state | 238-03, 04, 05 | COVERED | Explicit named-state helper, CI execution, exact SHA evidence. |
| RESEARCH | — | Wave 0 Assent 0.3.1 local `base_url` proof | 238-01 | COVERED | First executable tracer. |
| RESEARCH | — | Phase 237 canonical fresh-host lifecycle | 238-01, 02, 04 | COVERED | One retained harness copies exact order and runs in CI. |
| RESEARCH | — | Observable mailbox polling and generated route extractors | 238-02 | COVERED | `expect.poll`, purpose-specific newest-message selection. |
| RESEARCH | — | Dedicated Playwright project and CI artifact collection | 238-04 | COVERED | Exclusive project plus PostgreSQL/Chromium job. |
| RESEARCH | — | Missing local prerequisites require exact-commit CI | 238-05 | COVERED | Machine evidence blocks on unavailable/wrong/red run. |
| CONTEXT | D-01 | Same fresh canonical B2C lifecycle as Phase 237 | 238-01, 02, 04 | COVERED | Harness preserves exact scaffold/install/OAuth order. |
| CONTEXT | D-02 | One serial rendered email journey with dev mailbox | 238-02 | COVERED | All named email flows in one test, no direct calls. |
| CONTEXT | D-03 | Local Google double plus password-email collision, costly seam | 238-01, 03 | COVERED | Focused proof and full-suite collision; reversibility recorded. |
| CONTEXT | D-04 | Per-state scoped Axe/labels/IDs and no sleeps | 238-03, 04 | COVERED | Explicit helper calls and source locks. |
| CONTEXT | D-05 | Fold login-only gap into complete AUTH-01 journey | 238-02 | COVERED | Complete email journey replaces narrow login proof. |
| CONTEXT | D-06 | Fold missing auth Axe coverage into AUTH-03 | 238-03 | COVERED | State-scoped Axe plus DOM invariants. |

<verification>
1. Run `bash -n scripts/ci/generated-auth-runtime-proof.sh` and the Phase 238 source-contract ExUnit test.
2. Run `GITHUB_WORKSPACE="$PWD" scripts/ci/generated-auth-runtime-proof.sh --all` where PostgreSQL/Chromium exist; both specs must pass with no sleeps, credentials, or external provider traffic.
3. Validate the dedicated Playwright project is isolated and retries remain zero.
4. Obtain one green `generated_auth_runtime_proof` job on the exact implementation SHA under the AGENTS.md one-watcher/rate-limit protocol.
5. Validate `238-EVIDENCE.json` locally and against `gh run view`; do not close any requirement on wrong-SHA, skipped, cancelled, red, ambiguous, or unavailable evidence.
</verification>

<success_criteria>
- AUTH-01, AUTH-02, and AUTH-03 have deterministic runtime proof on the exact implementation commit.
- D-01 through D-06 are implemented without scope reduction.
- No real credentials/network, passkeys, admin/org behavior, Crosswake, physical-device proof, sleep synchronization, or human-UAT substitution appears.
- All high-severity threat mitigations pass; missing evidence blocks completion.
</success_criteria>

<output>
Create `.planning/phases/238-generated-auth-runtime-proof/238-05-SUMMARY.md` when done.
</output>
