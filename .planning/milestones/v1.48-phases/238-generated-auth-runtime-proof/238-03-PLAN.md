---
phase: 238-generated-auth-runtime-proof
plan: 03
type: execute
wave: 2
depends_on:
  - 238-01
  - 238-02
files_modified:
  - test/example/priv/playwright/tests/generated-auth.spec.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "AUTH-02 / D-03: the complete generated-auth suite begins Google at the generated start route and proves local callback plus existing-password-email collision without credentials or network."
    - "AUTH-03 / D-04 / D-06: every materially rendered B2C auth state reached by the serial journey passes state-scoped Axe, stable label/control, and duplicate-ID assertions."
    - "Accessibility checks inspect only `main.sigra-auth` after observable readiness and identify the state name in failures."
  artifacts:
    - path: test/example/priv/playwright/tests/generated-auth.spec.ts
      provides: "Complete email plus Google generated-host journey with per-state accessibility gates"
      contains: "assertAuthState"
  key_links:
    - from: test/example/priv/playwright/tests/generated-auth.spec.ts
      to: test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
      via: "reuses the already-proven start/callback/collision sequence in the complete suite"
      pattern: "/auth/google"
    - from: test/example/priv/playwright/tests/generated-auth.spec.ts
      to: main.sigra-auth
      via: "Axe plus DOM invariants run after each named material render"
      pattern: "main.sigra-auth"
---

<objective>
Complete AUTH-02 and AUTH-03 by folding the proven local Google collision into the serial generated-auth suite and making every reached auth render carry deterministic accessibility evidence per D-03, D-04, and D-06.

Purpose: A green journey must prove both authentication semantics and that no intermediate generated auth state becomes unusable or structurally ambiguous.
Output: One complete generated-host browser spec with named per-state security and accessibility assertions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md
@.planning/phases/238-generated-auth-runtime-proof/238-RESEARCH.md
@.planning/phases/238-generated-auth-runtime-proof/238-PATTERNS.md
@.planning/phases/238-generated-auth-runtime-proof/238-01-SUMMARY.md
@.planning/phases/238-generated-auth-runtime-proof/238-02-SUMMARY.md
@test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fold the generated Google collision into the complete serial suite</name>
  <reversibility rating="costly">Per D-03, this keeps the generated callback and provider-double contracts coupled as deliberate end-to-end evidence.</reversibility>
  <files>test/example/priv/playwright/tests/generated-auth.spec.ts</files>
  <read_first>
    - `test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts` for the passing Wave 0 state/PKCE/HS256-ID-token collision path.
    - `test/example/priv/playwright/tests/generated-auth.spec.ts` for the account and browser session produced by Plan 02.
    - `priv/templates/sigra.gen.oauth/oauth_controller.ex` for the generated collision flash and login redirect.
    - `test/sigra/oauth/callback_test.exs` for the identity non-linking semantic.
  </read_first>
  <behavior>
    - Test 1: after the email journey has established a password account with the double email, `/auth/google` traverses the local double and returns through the generated callback.
    - Test 2: the collision ends at generated login with `An account with this email exists` messaging and does not authenticate or silently link the identity.
    - Test 3: the complete spec never navigates directly to callback and accepts only the loopback discovery, authorization, and token endpoints; it requires no nonce, userinfo, or JWKS path.
  </behavior>
  <action>
    Align the serial journey's unique password-account email with the deterministic Wave 0 provider profile, or configure the harness's ephemeral double email from an explicit safe test environment value. After the final email-flow logout, execute the same passing generated start/redirect/callback sequence from the focused probe. Assert the login route, visible collision flash, logged-out state, and lack of a silent linked/authenticated outcome. Keep the focused Wave 0 spec as the narrow Assent contract and the complete spec as AUTH-02 acceptance; both must use the real generated routes and the same harness-owned double.
  </action>
  <verify>
    <automated>GITHUB_WORKSPACE="$PWD" scripts/ci/generated-auth-runtime-proof.sh --spec generated-auth</automated>
  </verify>
  <done>The full generated-auth suite proves Google start, callback, and existing-email collision after the email journey, while the focused probe continues to isolate Assent seam regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate every material auth render with Axe and DOM invariants</name>
  <files>test/example/priv/playwright/tests/generated-auth.spec.ts</files>
  <read_first>
    - `test/example/priv/playwright/tests/admin-generated.spec.ts` for the existing `main.sigra-auth` Axe include and four WCAG tags.
    - `test/example/priv/playwright/tests/generated-auth.spec.ts` after Task 1 for the definitive material-state sequence.
    - `.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md` D-04 and D-06 for the exact no-sleep, role/label, state-scoped contract.
  </read_first>
  <behavior>
    - Test 1: each named material state has exactly one visible `main.sigra-auth` root before analysis.
    - Test 2: scoped Axe with `wcag2a`, `wcag2aa`, `wcag21a`, and `wcag21aa` reports zero violations for every named state.
    - Test 3: every in-scope `label[for]` has a non-empty target ID resolving to exactly one form control inside the same auth root, and every interactive form control has a stable accessible label relationship.
    - Test 4: all non-empty IDs inside the auth root are unique; failure output names the rendered state and offending labels/controls/IDs.
  </behavior>
  <action>
    Add one `assertAuthState(page, stateName)` helper. Require the scoped root, run `AxeBuilder.include('main.sigra-auth')` with the established four tags, evaluate label/control relationships inside that root, and collect duplicate non-empty IDs with deterministic sorted diagnostics. Check controls by semantic form-control types and accept native `label[for]`, wrapping labels, or explicit ARIA naming only when they yield a real accessible relationship; do not weaken the rule to element presence.

    Invoke the helper immediately after observable stability for every material auth render actually reached: registration, login collapsed state, login password disclosure, confirmation-related auth state if it renders before redirect, logged-out login, magic-link request and sent/verification auth states, reset request/sent/token form/completion auth states, and Google collision login. If a transition immediately redirects to a non-auth page, assert the redirect instead of fabricating an auth scan. Keep all state calls explicit so adding or removing a journey state produces a reviewable diff. Do not use screenshots or human review as AUTH-03 evidence.
  </action>
  <verify>
    <automated>GITHUB_WORKSPACE="$PWD" scripts/ci/generated-auth-runtime-proof.sh --spec generated-auth</automated>
  </verify>
  <done>Every material generated B2C auth state reached by AUTH-01 and AUTH-02 has passing scoped Axe, stable label/control, and duplicate-ID proof with state-specific diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated OAuth callback → account routing | Provider claims collide with an existing password identity and must not elevate silently. |
| rendered auth DOM → assistive/browser interaction | Duplicate IDs or broken naming relationships can route input or announcements to the wrong control. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-09 | Elevation of Privilege | OAuth collision branch | high | mitigate | Assert logged-out collision UI after the real callback and no silent identity/session outcome. |
| T-238-10 | Tampering | callback state path | high | mitigate | Reuse the Wave 0 generated-start path and reject direct callback entry or non-loopback provider endpoints. |
| T-238-11 | Denial of Service | generated auth accessibility | medium | mitigate | Run Axe and explicit label/control/ID predicates on every reached material auth state. |
| T-238-12 | Repudiation | state coverage | medium | mitigate | Require explicit state names in every helper call and include them in deterministic failure diagnostics. |
</threat_model>

<verification>
Run the complete fresh-host target and the focused OAuth probe. Both must pass with zero retries; searching the new spec and mailbox fixture must find no explicit timeout-wait calls.
</verification>

<success_criteria>
- AUTH-02 is proven in the same fresh-host suite as AUTH-01 without credentials or network.
- AUTH-03 covers each reached material state with Axe, labels/controls, and duplicate IDs.
- D-03, D-04, and D-06 remain executable and deterministic.
</success_criteria>

<output>
Create `.planning/phases/238-generated-auth-runtime-proof/238-03-SUMMARY.md` when done.
</output>
