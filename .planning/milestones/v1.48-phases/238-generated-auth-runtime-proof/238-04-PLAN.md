---
phase: 238-generated-auth-runtime-proof
plan: 04
type: execute
wave: 3
depends_on:
  - 238-03
files_modified:
  - test/example/priv/playwright/playwright.config.ts
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "The generated-auth specs run only in a dedicated desktop Chromium project with workers 1, retries 0, failure artifacts, and the generated-host base URL."
    - "A PostgreSQL-backed CI job runs the full Phase 237 fresh-host lifecycle plus both generated-auth specs with no provider/email secrets or external network."
    - "Fast source contracts fail if project isolation, no-sleep mailbox behavior, the locked local-base_url/dummy-credentials/HS256/code_verifier contract, full journey states, accessibility gates, or CI execution is removed."
  artifacts:
    - path: test/example/priv/playwright/playwright.config.ts
      provides: "Dedicated generated-auth Playwright partition"
      contains: "generated-auth"
    - path: .github/workflows/ci.yml
      provides: "PostgreSQL/Chromium generated-auth runtime proof job and diagnostics"
      contains: "generated_auth_runtime_proof"
    - path: test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
      provides: "Fast source-lock contract for the complete automated evidence chain"
      contains: "Generated Auth Runtime Proof"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/ci/generated-auth-runtime-proof.sh
      via: "dedicated job executes the full fresh-host command"
      pattern: "generated-auth-runtime-proof.sh"
    - from: test/example/priv/playwright/playwright.config.ts
      to: test/example/priv/playwright/tests/generated-auth.spec.ts
      via: "exclusive testMatch and general-project testIgnore partition"
      pattern: "GENERATED_AUTH"
---

<objective>
Turn the passing generated-host suite into an isolated, source-locked CI lane that can produce exact-commit AUTH-01 through AUTH-03 evidence without manual UAT.

Purpose: Local prerequisites are intentionally absent on some workstations; completion therefore requires deterministic PostgreSQL/Chromium CI execution rather than a fixture-only claim.
Output: Playwright partition, CI job/artifacts, and fast contract tests protecting the lane.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md
@.planning/phases/238-generated-auth-runtime-proof/238-RESEARCH.md
@.planning/phases/238-generated-auth-runtime-proof/238-PATTERNS.md
@.planning/phases/238-generated-auth-runtime-proof/238-03-SUMMARY.md
@.github/ci-skip-manifest.tsv
@scripts/ci/prohibitions/_lib.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate and source-lock the generated-auth Playwright project</name>
  <files>test/example/priv/playwright/playwright.config.ts, test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs</files>
  <read_first>
    - `test/example/priv/playwright/playwright.config.ts` for matcher constants, general-project exclusions, `admin-generated` partition, serial defaults, and retained-video convention.
    - `test/sigra/planning/phase_230_ci_timeouts_test.exs` and `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` for planning/CI source-contract style.
    - `scripts/ci/generated-auth-runtime-proof.sh`, `test/example/priv/playwright/fixtures/mailbox.ts`, and both generated-auth specs for the predicates the contract must pin.
  </read_first>
  <behavior>
    - Test 1: a `generated-auth` project exclusively matches both generated-auth specs and uses Desktop Chrome with retained failure video.
    - Test 2: general chromium/mobile and unrelated dedicated projects cannot accidentally run generated-auth specs against `test/example`.
    - Test 3: the source contract detects removal of the canonical flags, local `base_url`, dummy credentials, HS256 ID token, `code_verifier: true`, discovery/authorization/token-only route set, generated start route, no-sleep polling, all AUTH-01 flow markers, scoped Axe, label/control checks, duplicate-ID checks, and zero-retry project policy.
  </behavior>
  <action>
    Add a matcher covering `generated-auth.spec.ts` and `generated-auth-oauth-probe.spec.ts`, exclude it from the existing generic chromium/mobile projects, and add one dedicated `generated-auth` Desktop Chrome project using the repository's retained-failure video convention. Preserve global serial execution, workers 1, retries 0, base URL injection, and existing project behavior.

    Create a focused ExUnit source-contract test that reads the committed harness, mailbox fixture, specs, config, and workflow. Assert stable semantic sentinels for D-01 through D-06 and AUTH-01 through AUTH-03, including absence of fixed-delay synchronization in the mailbox/generated-auth region. Use structural/fixed predicates that fail on missing files; do not claim browser runtime behavior from this fast test.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs &amp;&amp; cd test/example/priv/playwright &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>The generated-auth specs have one isolated project and a fast contract that detects weakening of the fresh-host, OAuth, mailbox, state, and accessibility evidence surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Add the PostgreSQL-backed generated-auth CI lane and artifacts</name>
  <files>.github/workflows/ci.yml, test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` jobs `passkeys_opt_out_smoke` and `generated_admin_playwright_smoke` for PostgreSQL, pinned toolchain, npm/browser installation, timeouts, and artifact retention.
    - `AGENTS.md` for one-watcher, rate-limit, exact-commit, retry-once, and durable-diagnostics rules.
    - `.github/ci-skip-manifest.tsv`, `scripts/ci/honest-skip-verdict.sh`, and `scripts/ci/prohibitions/_lib.mjs` before choosing job gating; avoid silently adding a skipped-as-passed lane to `ci-gate`.
    - `test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs` after Task 1 for the workflow contract extension.
  </read_first>
  <action>
    Add a dedicated `generated_auth_runtime_proof` job that runs on every CI event rather than carrying a PR skip predicate. Depend on `release_ref_guard`, use PostgreSQL 15, the repository's strict Beam toolchain, pinned `phx_new 1.8.8`, Node/npm cache, `npm ci`, Chromium installation, and dummy-only environment. Run `GITHUB_WORKSPACE="$PWD" scripts/ci/generated-auth-runtime-proof.sh --all`; do not pass Google credentials or enable network-backed providers. Give the job a bounded timeout sized for fresh scaffold/deps/assets/migration/browser work.

    Always upload the Playwright HTML report plus focused test-results/server diagnostics with repository-consistent 14-day main and 7-day non-main retention; missing diagnostics must warn rather than hide the primary failure. Keep this job a direct workflow signal and do not add it to the legacy skipped-is-pass `ci-gate` without updating every honest-skip/terminal-ratification contract. Extend the Phase 238 source test to pin the job ID/name, unconditional event posture, PostgreSQL/browser prerequisites, full harness command, and artifact paths.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs &amp;&amp; ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml", aliases: true)' &amp;&amp; bash -n scripts/ci/generated-auth-runtime-proof.sh</automated>
  </verify>
  <done>CI has a non-vacuous PostgreSQL/Chromium job that runs the complete generated B2C auth proof and retains deterministic diagnostics without credentials, network, sleeps, or human verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository config → Playwright selection | Incorrect match/ignore rules can run the suite against the wrong host and produce false confidence. |
| GitHub Actions → disposable generated host | CI provisions databases, dependencies, browser, and local services and must not expose secrets or externalize provider traffic. |
| CI conclusion → requirement claim | A skipped, wrong-SHA, or fixture-only result must not be accepted as runtime evidence. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-13 | Spoofing | Playwright project/host selection | high | mitigate | Exclusive matcher plus general-project ignores and source tests prevent running generated-auth specs against the maintained example host. |
| T-238-14 | Information Disclosure | CI env/artifacts | high | mitigate | Pass no provider/mail credentials, use loopback dummy configuration, and retain diagnostics without emitting secrets/tokens. |
| T-238-15 | Repudiation | CI runtime evidence | medium | mitigate | Dedicated non-PR-skipped job plus Plan 05 SHA/run correlation establishes what commit actually passed. |
| T-238-16 | Denial of Service | fresh-host CI lifecycle | medium | mitigate | Bounded job/server/test timeouts, one browser, workers 1, retries 0, traps, and failure artifacts. |
</threat_model>

<verification>
Run the Phase 238 contract test, TypeScript check, YAML parse, and full harness where prerequisites exist. Runtime completion is not credited until Plan 05 records a green exact-commit CI job.
</verification>

<success_criteria>
- The complete suite is isolated from example/admin projects.
- CI executes the real fresh-host proof on PostgreSQL/Chromium for every relevant commit.
- Fast tests prevent vacuous or weakened lane configuration.
</success_criteria>

<output>
Create `.planning/phases/238-generated-auth-runtime-proof/238-04-SUMMARY.md` when done.
</output>
