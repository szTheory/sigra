# Phase 240.1 Context — Canonical B2C OAuth Recipe Handoff

## Locked Decisions

- Keep `cloak_ecto` host-owned; `mix sigra.gen.oauth` must not mutate an adopter's dependency manifest.
- Make the public recipe explicitly add `{:cloak_ecto, "~> 1.3"}` and run `mix deps.get` before Google OAuth generation.
- Prove the documented ordering with a deterministic source contract and retain the existing fresh-host fixture as behavioral coverage.

## Scope Fence

- Do not change OAuth generator ownership, provider behavior, generated templates, or runtime configuration.
- Do not claim real-provider, mail, or device proof from repository CI.
