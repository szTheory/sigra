---
phase: 240.2-close-gap-ops-01-add-controller-mode-generated-host-compile
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/passkeys-opt-out-smoke.sh
  - test/sigra/install/generated_rate_limit_contract_test.exs
  - test/sigra/install/generator_passkeys_opt_out_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "OPS-01 / D-01: a fresh controller-mode B2C host reaches dependency refresh, warning-free compile, assets, migration, boot, and bounded readiness through the canonical smoke lifecycle."
    - "D-02: the exact controller invocation `--no-admin --no-organizations --no-passkeys --no-live` is distinct from and does not replace the exact canonical LiveView B2C invocation."
    - "D-03: both B2C legs use the existing `run_leg(flags, label)` lifecycle; no duplicate or source-only proof substitutes for the real fresh-host smoke."
    - "D-04: `scripts/ci/generated-auth-runtime-proof.sh` remains LiveView-only and retains its OAuth/limiter/browser proof ownership."
    - "D-05: generated-host proof unsets inherited Google credentials, retains only the disposable Cloak fixture, and makes no provider, mail, device, or staging claim."
    - "Readiness remains bounded and deterministic, with no fixed sleeps or human UAT."
  artifacts:
    - path: "scripts/ci/passkeys-opt-out-smoke.sh"
      provides: "Distinct controller-mode generated-host leg sharing the established fresh-host lifecycle"
      contains: "run_leg \"--no-admin --no-organizations --no-passkeys --no-live\" \"sigra_b2c_controller\""
    - path: "test/sigra/install/generated_rate_limit_contract_test.exs"
      provides: "Exact dual-lane topology, lifecycle ordering, and runtime ownership contract"
      contains: "generated-host compile lanes cover both LiveView and controller router output"
    - path: "test/sigra/install/generator_passkeys_opt_out_test.exs"
      provides: "Canonical LiveView profile preservation and distinct controller invocation guard"
      contains: "fresh-host smoke invokes the exact canonical B2C Alpha profile"
  key_links:
    - from: "scripts/ci/passkeys-opt-out-smoke.sh"
      to: "run_leg(flags, label)"
      via: "the exact `sigra_b2c_controller` invocation"
      pattern: "run_leg.*--no-live.*sigra_b2c_controller"
    - from: "run_leg(flags, label)"
      to: "fresh generated Phoenix host"
      via: "post-install deps.get, warnings-as-errors compile, assets, Ecto migration, phx.server, and bounded curl readiness"
      pattern: "mix deps.get[\\s\\S]*mix compile --warnings-as-errors[\\s\\S]*mix assets.deploy[\\s\\S]*mix ecto.migrate[\\s\\S]*curl --fail"
    - from: "test/sigra/install/generated_rate_limit_contract_test.exs"
      to: "scripts/ci/passkeys-opt-out-smoke.sh"
      via: "exact source markers for both B2C lane invocations and generic lifecycle ordering"
      pattern: "sigra_b2c_(alpha|controller)"
---

<objective>
Close the single broken OPS-01 integration by proving controller-mode generated authentication output in a real fresh Phoenix host while preserving every existing LiveView and no-secrets boundary (D-01 through D-05).

Purpose: Controller-only router, limiter, template, supervision, or dependency drift must fail the same canonical generated-host smoke that already protects the LiveView B2C profile.
Output: One distinct controller `run_leg` invocation plus focused ExUnit contracts that lock exact dual-lane topology, lifecycle ordering, and credential-free proof ownership.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.48-MILESTONE-AUDIT.md
@.planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-CONTEXT.md
@.planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-RESEARCH.md
@.planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-PATTERNS.md
@.planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-VALIDATION.md

<interfaces>
From scripts/ci/passkeys-opt-out-smoke.sh:
- `run_leg(flags, label)` creates an isolated Phoenix host and owns install, post-install dependency refresh, assertions, compile, assets, migration, boot, and bounded curl readiness.
- `cleanup_leg_dir(app_dir)` deletes only explicitly allowlisted leg directories and refuses all other paths.
- The `label == sigra_b2c_alpha` branch exclusively owns Google OAuth generation and the bounded generated limiter probe.

From test/sigra/install/generated_rate_limit_contract_test.exs:
- `assert_contains!(source, marker, context)` is the established exact-marker source-contract helper.
- The lifecycle ordering contract locates installer, dependency refresh, limiter probe, and warning-free compile markers by byte offset.
</interfaces>
</context>

## Requirement traceability and visible spec-less fallback skip

Phase 240.2 has no mapped `phase_req_ids`; therefore `requirements: []` is intentional and no REQUIREMENTS.md identifier is invented. The spec-less fallback is visibly skipped for this inserted gap phase. Goal-backward must-haves derive from the Phase 240.2 goal, OPS-01 in the milestone audit, RESEARCH.md, and locked decisions D-01 through D-05. OPS-02 is verification-only boundary preservation, not added phase scope.

## Artifacts this phase produces

- `scripts/ci/passkeys-opt-out-smoke.sh`: a new exact assertion-bearing invocation, `run_leg "--no-admin --no-organizations --no-passkeys --no-live" "sigra_b2c_controller"`, plus the corresponding guarded cleanup allowlist entry.
- `test/sigra/install/generated_rate_limit_contract_test.exs`: exact source assertions for both the existing `sigra_b2c_alpha` LiveView lane and the new `sigra_b2c_controller` lane.
- `test/sigra/install/generator_passkeys_opt_out_test.exs`: an explicit assertion that the controller invocation is separate while the existing canonical LiveView positive and controller-on-alpha negative guards remain.
- No new Elixir modules, public functions, packages, workflows, browser specifications, database schemas, migrations, or external-service interfaces are created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace exact controller B2C generation through the real fresh-host compile and boot lifecycle</name>
  <reversibility rating="reversible">The new internal smoke invocation and focused contract assertions can be removed locally without migrating data or breaking a published interface.</reversibility>
  <read_first>
    - .planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-CONTEXT.md
    - .planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-RESEARCH.md
    - .planning/v1.48-MILESTONE-AUDIT.md
    - scripts/ci/passkeys-opt-out-smoke.sh
    - scripts/ci/generated-auth-runtime-proof.sh
    - test/sigra/install/generated_rate_limit_contract_test.exs
    - test/sigra/install/generator_passkeys_opt_out_test.exs
    - lib/mix/tasks/sigra.install.ex
    - lib/sigra/install/features/core.ex
    - test/sigra/install/features/core_test.exs
  </read_first>
  <files>scripts/ci/passkeys-opt-out-smoke.sh, test/sigra/install/generated_rate_limit_contract_test.exs</files>
  <behavior>
    - Red: the focused contract fails until the smoke contains the exact canonical LiveView invocation and the exact distinct controller invocation.
    - Green: `sigra_b2c_controller` is cleanup-allowlisted and calls `run_leg` with exactly `--no-admin --no-organizations --no-passkeys --no-live`.
    - End to end: the controller host receives the shared post-install dependency refresh, warnings-as-errors compile, asset build, Ecto migration, server boot, and bounded curl readiness check.
    - Boundary: only `sigra_b2c_alpha` enters the existing Google OAuth and limiter-probe branch; the controller leg does not acquire browser/provider behavior.
  </behavior>
  <action>Per D-01 and D-02, first strengthen the compile-lanes test in `Sigra.Install.GeneratedRateLimitContractTest` so it requires the exact existing `run_leg "--no-admin --no-organizations --no-passkeys" "sigra_b2c_alpha"` marker and exact new `run_leg "--no-admin --no-organizations --no-passkeys --no-live" "sigra_b2c_controller"` marker, then run that focused test and record the expected red result before implementation. Per D-03, add `sigra_b2c_controller` to `cleanup_leg_dir/1`'s explicit `${TMP_ROOT}` allowlist and append the exact controller invocation after the unchanged canonical LiveView invocation; do not create a second lifecycle function or script. Preserve the generic `run_leg(flags, label)` ordering: install, post-install `MIX_ENV=dev mix deps.get`, `MIX_ENV=dev mix compile --warnings-as-errors`, assets deployment, Ecto database creation/migration, ephemeral-port server boot, and the existing bounded curl retry. Per D-04, leave the `label == sigra_b2c_alpha` OAuth/limiter-probe branch and `scripts/ci/generated-auth-runtime-proof.sh` behavior unchanged; do not add a controller-specific limiter request probe because it is not effectively free through the label-specific LiveView/OAuth branch. Per D-05, preserve the global Google credential unsets and existing disposable `CLOAK_KEY` fixture exactly in ownership and meaning. Do not add packages, workflows, sleeps, browser tests, provider calls, mail/device evidence, or broader CI topology changes.</action>
  <acceptance_criteria>
    - `generated_rate_limit_contract_test.exs` asserts both exact `run_leg` invocation strings and still asserts the runtime harness contains no controller-mode installer flag.
    - `cleanup_leg_dir` explicitly permits `${TMP_ROOT}/sigra_b2c_controller` while retaining its refusal branch for every unexpected directory.
    - The existing canonical `sigra_b2c_alpha` invocation is byte-for-byte unchanged and remains before the distinct controller invocation.
    - The controller invocation reaches the existing shared lifecycle; there is only one `run_leg()` definition and no copied compile/boot lifecycle.
    - The `sigra_b2c_alpha` conditional remains label-specific, so Google OAuth generation and `install_generated_rate_limit_probe` do not run for `sigra_b2c_controller`.
    - The real smoke exits successfully after the controller host compiles with warnings as errors, builds assets, migrates, boots, and answers the bounded root readiness request.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/install/generated_rate_limit_contract_test.exs &amp;&amp; bash -n scripts/ci/passkeys-opt-out-smoke.sh &amp;&amp; GITHUB_WORKSPACE="$(pwd)" scripts/ci/passkeys-opt-out-smoke.sh</automated>
  </verify>
  <done>OPS-01's broken controller-limiter-to-fresh-host connection is executable end to end, while the canonical LiveView B2C lane and its OAuth/limiter behavior remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ratchet canonical lane separation and no-secrets invariants</name>
  <reversibility rating="reversible">These repository-local regression assertions can be adjusted without changing generated output or public contracts.</reversibility>
  <read_first>
    - .planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-CONTEXT.md
    - .planning/v1.48-MILESTONE-AUDIT.md
    - scripts/ci/passkeys-opt-out-smoke.sh
    - scripts/ci/generated-auth-runtime-proof.sh
    - test/sigra/install/generated_rate_limit_contract_test.exs
    - test/sigra/install/generator_passkeys_opt_out_test.exs
    - test/sigra/planning/phase_240_no_secrets_ci_test.exs
  </read_first>
  <files>test/sigra/install/generator_passkeys_opt_out_test.exs</files>
  <behavior>
    - The canonical profile test requires the unchanged LiveView invocation and the distinct controller invocation.
    - The canonical label remains forbidden from receiving the controller-mode flag.
    - Existing lifecycle ordering, no-sleep, explicit Google credential unset, disposable Cloak fixture, and truthful local-proof contracts stay green.
  </behavior>
  <action>Expand `fresh-host smoke invokes the exact canonical B2C Alpha profile` in `Sigra.Install.GeneratorPasskeysOptOutTest` with a positive assertion for `run_leg "--no-admin --no-organizations --no-passkeys --no-live" "sigra_b2c_controller"`, alongside the existing exact positive LiveView assertion and the existing negative assertion that prevents controller flags on `sigra_b2c_alpha` (D-02). Do not relax the source markers to substring-only flag checks. Then run the focused installer contracts together with `Sigra.Planning.Phase240NoSecretsCiTest` to prove D-03 ordering, D-04 lane ownership, and D-05 credential/fixture boundaries remain intact. Make no changes to the no-secrets test, runtime harness, workflow topology, or generated feature implementation unless a deterministic failure demonstrates that this scoped change violated an existing locked invariant; if so, diagnose and repair only the controller-leg integration.</action>
  <acceptance_criteria>
    - `generator_passkeys_opt_out_test.exs` contains positive exact assertions for both named B2C invocations and retains the exact negative guard against applying controller mode to `sigra_b2c_alpha`.
    - The generated-rate-limit ordering contract still proves post-install dependency refresh precedes both the limiter probe and warning-free compile.
    - `phase_240_no_secrets_ci_test.exs` passes without modification and continues to prove inherited Google credentials are unset before `run_leg()`, the Cloak value is a disposable fixture, and no secret injection or external-success claim exists.
    - Shell syntax passes and focused ExUnit contracts pass without retries, sleeps, or human verification.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/install/generated_rate_limit_contract_test.exs test/sigra/install/generator_passkeys_opt_out_test.exs test/sigra/planning/phase_240_no_secrets_ci_test.exs &amp;&amp; bash -n scripts/ci/passkeys-opt-out-smoke.sh</automated>
  </verify>
  <done>Both lane invocations are exact and independently guarded; shared lifecycle ordering, LiveView runtime ownership, and no-secrets boundaries remain deterministic and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library installer → generated Phoenix host | Generated controller routes, limiter plugs, templates, application children, and dependencies cross into host-owned code that must compile together. |
| Developer/CI environment → disposable harness | Inherited environment variables could leak real provider credentials into a proof intended to be credential-free. |
| Harness process → local generated server | Boot/readiness evidence must be bounded and observable rather than timing-dependent or indefinitely blocked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-240.2-01 | Tampering | Controller-only generated router/limiter output | high | mitigate | Exact `sigra_b2c_controller` topology contracts plus the real shared fresh-host warnings-as-errors compile, migration, boot, and curl proof prevent LiveView-green results from masking controller rot. |
| T-240.2-02 | Information Disclosure | `scripts/ci/passkeys-opt-out-smoke.sh` environment boundary | high | mitigate | Preserve `unset GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET`, the explicitly disposable `CLOAK_KEY`, and the Phase 240 no-secrets source contract; add no provider calls or secret-bearing configuration. |
| T-240.2-03 | Denial of Service | Generated-host readiness loop | medium | mitigate | Reuse the existing bounded `curl --retry 30 --retry-connrefused --retry-delay 0` readiness check, server cleanup trap, and per-leg ephemeral port; introduce no fixed sleep or unbounded poll. |
| T-240.2-04 | Repudiation | OPS-01 closure evidence | medium | mitigate | Require both focused ExUnit contracts and successful execution of the canonical fresh-host smoke; source presence alone cannot close the audit gap. |
| T-240.2-05 | Elevation of Privilege | Proof ownership boundary | low | accept | The controller leg compiles existing generated authentication code but adds no new authorization semantics or external authority; existing generator feature tests remain the behavior owner. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Close the urgent generated controller limiter → fresh controller host compile-proof gap | 240.2-01 | COVERED | Tracer runs the full real host lifecycle. |
| REQ | — | No phase requirement IDs are mapped | 240.2-01 | COVERED | Visible spec-less fallback skip; OPS-01 audit traceability is preserved without inventing an ID. |
| RESEARCH | — | Reuse `run_leg(flags, label)` with `sigra_b2c_controller` and guarded cleanup | 240.2-01 | COVERED | Task 1 implements exact recommended topology. |
| RESEARCH | — | Preserve dependency refresh, compile, assets, migration, boot, and bounded readiness | 240.2-01 | COVERED | Task 1 verifies the real smoke. |
| RESEARCH | — | Strengthen exact dual-lane contracts and no-secrets/order guards | 240.2-01 | COVERED | Tasks 1-2 cover focused contracts. |
| CONTEXT | D-01 | Close only OPS-01's broken controller compile-proof integration | 240.2-01 | COVERED | No adjacent operations or CI changes planned. |
| CONTEXT | D-02 | Distinct exact controller lane; canonical LiveView lane unchanged | 240.2-01 | COVERED | Both tasks assert exact invocations. |
| CONTEXT | D-03 | Full shared fresh-host lifecycle, not source-only proof | 240.2-01 | COVERED | Tracer executes the smoke. |
| CONTEXT | D-04 | Browser/provider runtime remains in the Phase 238 lane | 240.2-01 | COVERED | Runtime script is read/verified but not modified. |
| CONTEXT | D-05 | Credential-free execution and disposable Cloak fixture | 240.2-01 | COVERED | Existing no-secrets contract is a blocking automated gate. |

Deferred Nyquist refreshes, real-provider/mail/device/adopter evidence, all reviewed TODOs, and a controller-specific limiter request probe are excluded exactly as directed by CONTEXT.md and research.

<verification>
1. Focused contracts: `mix test test/sigra/install/generated_rate_limit_contract_test.exs test/sigra/install/generator_passkeys_opt_out_test.exs test/sigra/planning/phase_240_no_secrets_ci_test.exs`.
2. Shell parse: `bash -n scripts/ci/passkeys-opt-out-smoke.sh`.
3. Required behavioral proof: `GITHUB_WORKSPACE="$(pwd)" scripts/ci/passkeys-opt-out-smoke.sh` completes all legs, including `sigra_b2c_controller`.
4. Audit closure input is machine-verifiable: both exact invocations are source-locked and the real controller host reaches compile/boot readiness; milestone re-audit remains the orchestrator's next workflow, not phase implementation scope.
</verification>

<success_criteria>
- The exact controller-mode B2C leg completes the established disposable fresh-host lifecycle, including post-install dependency refresh, warning-free compile, assets, migration, boot, and bounded readiness.
- The exact canonical LiveView B2C leg remains unchanged and retains exclusive OAuth/limiter-probe behavior.
- Focused source contracts require both exact invocations, preserve lifecycle ordering, and keep the independent runtime harness LiveView-only.
- The no-secrets contract proves inherited Google credentials remain unset and the Cloak value remains a disposable local fixture.
- No new package, workflow, browser suite, provider integration, schema, sleep, human UAT, or external-success claim is introduced.
</success_criteria>

<output>
Create `.planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-01-SUMMARY.md` when done.
</output>
