---
phase: 240-alpha-operations-rehearsal
plan: 04
type: execute
wave: 2
depends_on:
  - 240-05
  - 240-01
files_modified:
  - scripts/ci/passkeys-opt-out-smoke.sh
  - scripts/ci/generated-auth-runtime-proof.sh
  - .github/workflows/ci.yml
  - .github/workflows/generated-auth-runtime-proof.yml
  - test/sigra/planning/phase_240_no_secrets_ci_test.exs
autonomous: true
requirements:
  - OPS-02
must_haves:
  truths:
    - "D-10: Fresh canonical B2C generation and rendered loopback-OIDC runtime remain separate, independently runnable credential-free proofs."
    - "D-10/D-11: Inherited GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET are unset; no live Google, mail, deploy, or GitHub-secret credential is injected by either workflow or script."
    - "D-11: Fixed CLOAK_KEY and loopback OIDC client values are named disposable fixtures, and CI claims only generator shape, local state/PKCE/callback, and rendered B2C behavior."
    - "D-03/D-11: Real Google Console/tenant, mail provider, DNS/TLS, reverse proxy, and physical-device evidence remain explicitly outside the CI pass surface."
    - "FLAGGED ASSUMPTION — OPS-02 unclassified: absent a more specific edge classification, source contracts fail closed on lane merging, secret references, inherited Google variables, fixture mislabeling, and provider/deployment/device success language."
  artifacts:
    - path: test/sigra/planning/phase_240_no_secrets_ci_test.exs
      provides: Positive lane topology and negative credential/claim contracts.
    - path: scripts/ci/passkeys-opt-out-smoke.sh
      provides: Independent fresh-generator proof with disposable fixtures.
    - path: scripts/ci/generated-auth-runtime-proof.sh
      provides: Independent rendered-host proof using only a loopback OIDC double.
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/ci/passkeys-opt-out-smoke.sh
      via: Dedicated fresh canonical generator invocation.
    - from: .github/workflows/generated-auth-runtime-proof.yml
      to: scripts/ci/generated-auth-runtime-proof.sh
      via: Separate rendered-runtime job with no live provider credentials.
    - from: generated-auth-runtime-proof.sh
      to: loopback OIDC double
      via: Fixed disposable localhost client values after unsetting inherited Google variables.
---

<objective>
Seal the no-secrets evidence topology per D-10 and D-11: two independent credential-free proofs, explicit fixture provenance, inherited-Google unsetting, and negative contracts preventing repository CI from claiming host staging success.

Purpose: Make OPS-02 fail closed if workflow drift introduces secrets, merges distinct proofs, or overstates local evidence.
Output: Focused CI/source contracts and minimally corrected lane annotations or environment boundaries.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/240-alpha-operations-rehearsal/240-CONTEXT.md
@.planning/phases/240-alpha-operations-rehearsal/240-RESEARCH.md
@.planning/phases/240-alpha-operations-rehearsal/COVERAGE.md
@.planning/phases/240-alpha-operations-rehearsal/240-05-SUMMARY.md
@.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md
@test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
@scripts/ci/passkeys-opt-out-smoke.sh
@scripts/ci/generated-auth-runtime-proof.sh
@.github/workflows/ci.yml
@.github/workflows/generated-auth-runtime-proof.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce independent credential-free lanes and truthful claims</name>
  <reversibility rating="costly">The workflow/job topology is repository evidence consumed by protected CI and milestone ratification; changing it later requires coordinated contract and branch-policy review.</reversibility>
  <files>scripts/ci/passkeys-opt-out-smoke.sh, scripts/ci/generated-auth-runtime-proof.sh, .github/workflows/ci.yml, .github/workflows/generated-auth-runtime-proof.yml, test/sigra/planning/phase_240_no_secrets_ci_test.exs</files>
  <read_first>Read D-03, D-07, D-10, and D-11; the Wave 0 no-secrets contract; existing Phase 238 no-secret and no-sleep assertions; both scripts' environment/setup/logging code; both workflows' `env`, step, and job topology; and `COVERAGE.md`'s exact local-only no-external-API declaration.</read_first>
  <behavior>
    - Test 1: The fresh-generator command and rendered-runtime command are distinct invocations/jobs and neither substitutes for the other.
    - Test 2: Both workflow/script surfaces reject `${{ secrets.* }}` and named Google, transactional-mail, deployment, or GitHub credential injection; the runtime script contains `unset GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET` before boot/proof.
    - Test 3: Fixed CLOAK_KEY and OIDC client values are adjacent to explicit `disposable fixture` wording and are used only with disposable/loopback hosts.
    - Test 4: Allowed claims are limited to generator shape/compile/boot, local OIDC state/PKCE/callback, and rendered B2C behavior; prohibited real Google, mail provider, DNS/TLS, reverse proxy, and physical-device success claims fail the test.
    - Test 5: Scripts and browser sources contain no fixed sleeps, rate-window waits, token/mail/provider payload logging, cookie/storage mutation, or retry-based waiver.
    - Test 6: `COVERAGE.md` contains exactly the local-only declaration that Phase 240 adds no external API client; it contains no detector-backed status claim and the test verifies the declaration from repository source.
  </behavior>
  <action>Run the Wave 0 `Sigra.Planning.Phase240NoSecretsCiTest` first and preserve its RED evidence. Parse each workflow's relevant job/step region and each script's environment/proof region so negative checks are scoped and cannot be defeated by unrelated repository prose. Assert the two D-10 lanes remain independently invoked and that the generated-auth runtime lane stays outside the legacy skip-tolerant aggregate unless an explicit evidence-contract change is made. Require `unset GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET` before the loopback runtime boot. Reject any workflow reference to GitHub secrets and any Google, mailer, deploy, or provider credential injection. Require the fixed CLOAK_KEY in the generator/runtime harness and the fixed local OIDC client secret to be labeled `disposable fixture`, never deployment credentials, per D-11. Keep the Wave 0 exact source assertion for `COVERAGE.md`: it must declare only that Phase 240 adds no external API client and must not claim a detector ran or passed. Add or tighten comments/environment setup in the two scripts and workflows only where the failing tests identify ambiguity; do not add credentials, external calls, a new combined harness, elapsed-window waits, retry waivers, or provider-success assertions. Preserve Playwright's role selectors, stable hooks, LiveView readiness, and zero retries/sleeps.</action>
  <verify>
    <automated>mix test test/sigra/planning/phase_240_no_secrets_ci_test.exs test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs &amp;&amp; bash -n scripts/ci/passkeys-opt-out-smoke.sh scripts/ci/generated-auth-runtime-proof.sh &amp;&amp; test "$(sed -n '3p' .planning/phases/240-alpha-operations-rehearsal/COVERAGE.md)" = "No external API integration: this phase configures generated-host rate limiting and verifies local evidence without adding or expanding an external API client."</automated>
  </verify>
  <acceptance_criteria>D-10/D-11 are enforced by deterministic positive and negative contracts: two separate no-secret proofs remain runnable, inherited Google credentials are unset, disposable fixtures are unambiguous, and no repository surface claims real provider/mail/deployment/proxy/device success.</acceptance_criteria>
  <done>The no-secrets and Phase 238 regression contracts pass, both harnesses parse, and the focused repository-source assertion proves the phase's exact non-integration declaration.</done>
</task>

</tasks>

## Artifacts this phase produces

- `phase_240_no_secrets_ci_test.exs` — fail-closed CI credential and claim contract.
- Explicit disposable-fixture annotations and inherited-Google boundary in existing scripts.
- Detector-backed non-integration declaration in `COVERAGE.md` (planning-time artifact, verified here).

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Provider-neutral, no-secrets launch-readiness gate | Wave 0 Plan 05 plus implementation Plans 01-04 collectively cover executable contracts, generated enforcement, operator rehearsal, and truthful CI evidence. |
| REQ | OPS-01 | Plans 01-03 cover generated limiter ownership plus origin/session/Google/Cloak/rate-limit/mail rehearsal. |
| REQ | OPS-02 | Plans 03-04 cover separate no-secret lanes and required-but-unpassable Google/mail/iPhone host gates. |
| RESEARCH | Explicit generated Hammer dependency/module/child/config ownership | Plan 01 tracer. |
| RESEARCH | Separate route-IP and LiveView/context mail-request enforcement | Plan 02. |
| RESEARCH | Three evidence tiers, literal host tuple, truthful Doctor scope, redacted receipt | Plan 03. |
| RESEARCH | Independent fresh generator and loopback-OIDC runtime lanes with negative credential assertions | Plan 04. |
| RESEARCH | Four named Nyquist contract artifacts before implementation | Wave 0 Plan 05. |
| RESEARCH | Package legitimacy audit (`hammer` approved through HexDocs/Hex registry; no npm/pip/cargo install gate) | Plan 01 uses the audited `~> 7.4` dependency. |
| CONTEXT | D-01, D-02, D-03 | Plan 03 task action and must-haves. |
| CONTEXT | D-04, D-05, D-06, D-07 | Plan 03 task action and must-haves. |
| CONTEXT | D-08, D-09 | Plans 01-02 task actions and must-haves. |
| CONTEXT | D-10, D-11 | Plan 04 task action and must-haves. |
| CONTEXT | Deferred hosted control plane, provider CI, native authority, general UI, organizations/passkeys/MFA/billing | Excluded from every plan. |
| SPEC probe | OPS-01 boundary and precision unresolved | Explicit FLAGGED ASSUMPTION truths in Plans 01-03 with executable boundary/rounding tests. |
| SPEC probe | OPS-02 unclassified unresolved | Explicit FLAGGED ASSUMPTION truths in Plans 03-04 with fail-closed claim/credential contracts. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub workflow environment -> disposable generated host | CI variables must never import adopter/provider/deployment secrets. |
| loopback OIDC double -> rendered generated host | Local fixed values prove protocol mechanics only, not provider acceptance. |
| CI result -> release/readiness claim | A green lane must not imply host-owned staging gates passed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240-13 | Information Disclosure | workflow/script environment and logs | high | mitigate | Per D-07/D-10, reject secret injection, unset inherited Google values, and prohibit token/mail/provider payload logging. |
| T-240-14 | Spoofing | loopback OIDC evidence | high | mitigate | Per D-11, label fixed values disposable and constrain claims to local state/PKCE/callback mechanics. |
| T-240-15 | Repudiation | merged/skipped CI proof | high | mitigate | Per D-10, retain two independent invocations and keep runtime proof outside the skip-tolerant aggregate. |
| T-240-16 | Tampering | fixture credentials reused as deploy values | medium | mitigate | Source contracts require disposable wording and loopback/disposable scope adjacent to literals. |
</threat_model>

<verification>
Phase 240 and Phase 238 source contracts pass, both scripts parse, the exact `COVERAGE.md` declaration is proven by an executable source check, and no secret-bearing or provider-success evidence is introduced.
</verification>

<success_criteria>
OPS-02 is mechanically protected: repository CI proves only its credential-free canonical generator and loopback runtime contracts, while real staging evidence remains a required but unpassable host gate.
</success_criteria>

<output>
Create `.planning/phases/240-alpha-operations-rehearsal/240-04-SUMMARY.md` when done.
</output>
