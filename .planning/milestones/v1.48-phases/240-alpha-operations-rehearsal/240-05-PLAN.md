---
phase: 240-alpha-operations-rehearsal
plan: 05
type: execute
wave: 0
depends_on: []
files_modified:
  - test/sigra/install/generated_rate_limit_contract_test.exs
  - test/sigra/install/generated_rate_limit_context_test.exs
  - test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs
  - test/sigra/planning/phase_240_no_secrets_ci_test.exs
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "D-08/D-09: Before generated-host implementation starts, executable RED contracts define concrete Hammer ownership, the selected route/context flow map, bounded exhaustion, independent keys, generic outcomes, and Retry-After precision without rate-window waits."
    - "D-01 through D-07: Before recipe edits start, an executable RED contract defines the three evidence tiers, literal host tuple, Doctor boundary, required staging gates, and redacted receipt rules."
    - "D-10/D-11: Before CI edits start, an executable RED contract defines separate credential-free lanes, inherited-Google unsetting, disposable fixtures, truthful claims, and the exact local-only COVERAGE.md declaration."
    - "All four Wave 0 files are syntactically valid ExUnit modules with behavior assertions, not empty files or skipped placeholders."
  artifacts:
    - path: test/sigra/install/generated_rate_limit_contract_test.exs
      provides: RED generated ownership, threshold, Retry-After, idempotency, and fresh-host tracer contract.
    - path: test/sigra/install/generated_rate_limit_context_test.exs
      provides: RED route/context flow-map, independent-key, and non-enumerating outcome contract.
    - path: test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs
      provides: RED recipe tier, host tuple, staging-only, redaction, and Doctor claim contract.
    - path: test/sigra/planning/phase_240_no_secrets_ci_test.exs
      provides: RED separate-lane, disposable-fixture, inherited-environment, no-secret, claim, and coverage-declaration contract.
  key_links:
    - from: test/sigra/install/generated_rate_limit_contract_test.exs
      to: Plan 240-01
      via: Plan 01 turns the generated ownership and one protected POST tracer from RED to green.
    - from: test/sigra/install/generated_rate_limit_context_test.exs
      to: Plan 240-02
      via: Plan 02 turns the complete selected route/context map and independent-key assertions green.
    - from: test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs
      to: Plan 240-03
      via: Plan 03 turns the operator evidence and claim-boundary contract green.
    - from: test/sigra/planning/phase_240_no_secrets_ci_test.exs
      to: Plan 240-04
      via: Plan 04 turns the separate-lane, no-secret, and COVERAGE.md declaration contract green.
---

<objective>
Create every Nyquist Wave 0 contract artifact required by `240-VALIDATION.md` before implementation begins, preserving the RED half of D-01 through D-11 and OPS-01/OPS-02.

Purpose: Give each implementation plan a concrete executable contract instead of asking it to invent its tests while changing production behavior.
Output: Four formatted, syntactically valid ExUnit contract modules whose focused assertions initially expose the missing Phase 240 behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/240-alpha-operations-rehearsal/240-CONTEXT.md
@.planning/phases/240-alpha-operations-rehearsal/240-RESEARCH.md
@.planning/phases/240-alpha-operations-rehearsal/240-VALIDATION.md
@.planning/phases/240-alpha-operations-rehearsal/COVERAGE.md
@test/sigra/plug/rate_limit_test.exs
@test/sigra/install/golden_diff_test.exs
@test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs

<interfaces>
Wave 0 owns test definitions only. Plans 240-01 through 240-04 own the production, generated-host, documentation, script, and workflow changes that make these contracts pass. Each downstream plan must retain its focused test command as its automated verification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define generated-host limiter ownership and flow-map contracts</name>
  <files>test/sigra/install/generated_rate_limit_contract_test.exs, test/sigra/install/generated_rate_limit_context_test.exs</files>
  <read_first>Read D-08/D-09, the resolved route/context and deterministic-probe decisions in `240-RESEARCH.md`, Plans 240-01/240-02, `Sigra.Plug.RateLimit`'s existing test API, installer render helpers, and golden/idempotency helpers.</read_first>
  <behavior>
    - Test 1: Rendered canonical B2C output must own Hammer dependency/module/child/config and an explicit route limiter rather than the Noop fallback per D-08.
    - Test 2: The selected controller-IP and magic-link/reset context map has stable independent prefixes/keys; exhausting one key does not consume another.
    - Test 3: A low injected test limit proves N-1/N/N+1 behavior, generic 429 plus positive ceiling-rounded Retry-After where the route owns the response, and generic non-enumerating context outcomes per D-09.
    - Test 4: The bounded generated-host probe contract contains no elapsed-window wait and the installer ownership chain is idempotent.
  </behavior>
  <action>Create both ExUnit modules in the RED state. Reuse existing installer render and Mox/plug test helpers rather than adding production helpers. Assert identifiers, generated paths, option names, route/context ownership, independent key material, threshold boundaries, integer Retry-After rounding, generic outward outcomes, idempotent markers, and the fresh-host probe seam with precise failures. The assertions may fail because Plans 01-02 have not implemented the behavior yet; the files themselves must parse, format, and contain active tests with no skip tags, empty bodies, sleeps, or time-window polling.</action>
  <verify>
    <automated>mix format --check-formatted test/sigra/install/generated_rate_limit_contract_test.exs test/sigra/install/generated_rate_limit_context_test.exs &amp;&amp; elixir -e 'Enum.each(System.argv(), fn path -&gt; Code.string_to_quoted!(File.read!(path), file: path) end)' -- test/sigra/install/generated_rate_limit_contract_test.exs test/sigra/install/generated_rate_limit_context_test.exs</automated>
  </verify>
  <acceptance_criteria>Both D-08/D-09 test artifacts exist as active formatted ExUnit contracts and give Plans 01-02 deterministic RED targets for ownership, mapping, exhaustion, independence, generic outcomes, precision, and no-wait proof.</acceptance_criteria>
  <done>The two generated-host contract files parse and format cleanly and contain the complete behavior map required by the first two validation rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define operator evidence and no-secrets CI contracts</name>
  <files>test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs, test/sigra/planning/phase_240_no_secrets_ci_test.exs</files>
  <read_first>Read D-01 through D-07 and D-10/D-11; OPS-01/OPS-02; Plans 240-03/240-04; the Phase 238 planning-test helpers; both CI workflows/scripts; and the exact single declaration in `COVERAGE.md`.</read_first>
  <behavior>
    - Test 1: The recipe contract requires exactly three tiers, every row's owner/result/must-not-claim fields, one literal origin/proxy/session tuple, separate wiring/delivery gates, truthful Doctor scope, and redacted receipts per D-01 through D-07.
    - Test 2: Real Google, controlled confirmation/reset/magic-link delivery, and physical-iPhone HTTPS return remain mandatory host gates with no repository-pass marker per D-02/D-03.
    - Test 3: The two CI lanes remain independent; inherited Google variables are unset; live Google, mail, deployment, and GitHub-secret injection or claim inflation is rejected; fixed Cloak/OIDC values require disposable-fixture provenance per D-10/D-11.
    - Test 4: `COVERAGE.md` contains the exact local-only no-external-API declaration and no detector-backed status claim.
  </behavior>
  <action>Create both async ExUnit source-contract modules in the RED state using scoped region parsing patterned after the Phase 238 test. Give the recipe assertions structured heading/row parsing and explicit sensitive-field rejection. Give the CI assertions distinct workflow/script regions, ordering proof for inherited-Google unsetting, disposable-fixture adjacency, no-secret/no-provider-success rules, and an exact assertion for `COVERAGE.md`'s declaration. The assertions may fail until Plans 03-04 run; keep active tests, actionable failures, and no skipped or vacuous assertions.</action>
  <verify>
    <automated>mix format --check-formatted test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs test/sigra/planning/phase_240_no_secrets_ci_test.exs &amp;&amp; elixir -e 'Enum.each(System.argv(), fn path -&gt; Code.string_to_quoted!(File.read!(path), file: path) end)' -- test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs test/sigra/planning/phase_240_no_secrets_ci_test.exs</automated>
  </verify>
  <acceptance_criteria>Both documentation/CI artifacts exist as active formatted contracts and give Plans 03-04 deterministic RED targets for D-01 through D-07, D-10/D-11, OPS-01/OPS-02, redaction, truthful claims, and the coverage declaration.</acceptance_criteria>
  <done>The two planning contract files parse and format cleanly and cover every operator-evidence, no-secret, and local-only coverage behavior named by validation.</done>
</task>

</tasks>

## Artifacts this phase produces

- Four Wave 0 ExUnit contract modules named exactly as required by `240-VALIDATION.md`.
- A deterministic RED-to-green dependency seam for each implementation plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| contract test -> generated host implementation | Tests must fail closed on absent limiter ownership without embedding secrets or environment-specific state. |
| repository evidence -> host launch claim | Source contracts must keep CI-local proof distinct from provider/deployment/device evidence. |
| workflow/script source -> test diagnostics | Assertions may inspect names and structure but must never echo credential values or payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240-W0-01 | Tampering | inactive or vacuous Wave 0 tests | high | mitigate | Require active ExUnit cases with precise positive/negative assertions, valid syntax, and no skip tags or empty bodies. |
| T-240-W0-02 | Information Disclosure | CI/receipt contract diagnostics | high | mitigate | Assert only structural markers and prohibited field names; never load or print live environment values, tokens, mail bodies, or provider payloads. |
| T-240-W0-03 | Spoofing | local proof presented as staging proof | high | mitigate | Encode D-03/D-11 negative claim boundaries before workflow and recipe edits begin. |
| T-240-W0-04 | Denial of Service | limiter contract timing | medium | mitigate | Use injected low bounds and synchronous request counts; prohibit waits across limiter windows. |
</threat_model>

<verification>
All four exact paths exist, are formatted and syntactically valid, and contain active behavior contracts that downstream Plans 01-04 run to green.
</verification>

<success_criteria>
`240-VALIDATION.md` has an executable Wave 0 producer for every previously missing test artifact, and every implementation plan explicitly depends on it.
</success_criteria>

<output>
Create `.planning/phases/240-alpha-operations-rehearsal/240-05-SUMMARY.md` when done.
</output>
