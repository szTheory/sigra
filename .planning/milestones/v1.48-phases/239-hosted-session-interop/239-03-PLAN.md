---
phase: 239-hosted-session-interop
plan: 03
type: execute
wave: 3
depends_on: [239-02]
files_modified:
  - test/example/lib/example/accounts/crosswake_session_adapter.ex
  - test/example/test/example/accounts/crosswake_session_adapter_test.exs
autonomous: true
requirements: [XW-02]
must_haves:
  truths:
    - "Per D-03/D-04/D-06, missing, deleted/revoked, non-active, idle-expired, and absolute-expired current rows deny from fresh storage state."
    - "Storage/time denial occurs before the Crosswake evaluator is invoked, so an old serialized active lane cannot revive authority."
  assumptions:
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the deterministic currentness/expiry matrix is the explicit verification surface."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT represent a personal account by manufacturing organization authority or accepting a blank organization value."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-01; no fallback descriptor is invented."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT carry a raw session credential, stored token digest, provider payload, or OAuth credential across the companion boundary."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-02; no fallback descriptor is invented."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or hosted-return evidence select a session, replace fresh host resolution, or independently admit a route."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-05; no fallback descriptor is invented."
  artifacts:
    - path: test/example/lib/example/accounts/crosswake_session_adapter.ex
      provides: "fresh currentness and expiry denial before evaluation"
    - path: test/example/test/example/accounts/crosswake_session_adapter_test.exs
      provides: "real-row replay/revocation/expiry matrix"
  key_links:
    - from: test/example/lib/example/accounts/crosswake_session_adapter.ex
      to: test/example/lib/example/accounts.ex
      via: "fresh lookup on every projection and replay"
      pattern: "get_user_and_session_by_token"
---

<objective>
Expand the tracer with deterministic storage-currentness and expiry denial.

Purpose: Prove that a previously valid Crosswake fact set cannot outlive the canonical SIGRA session row or its configured time limits.
Output: Fail-closed host checks and a real Ecto denial matrix.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-RESEARCH.md
@.planning/phases/239-hosted-session-interop/239-02-SUMMARY.md
@lib/sigra/plug/fetch_session.ex
@test/example/test/example/accounts/session_invalidation_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deny missing, revoked, and inactive server state before evaluation</name>
  <files>test/example/lib/example/accounts/crosswake_session_adapter.ex, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>
    - `test/example/lib/example/accounts/crosswake_session_adapter.ex` and its tracer tests from Plan 02.
    - `test/example/test/example/accounts/session_invalidation_test.exs:19-79` for real row deletion/revocation behavior.
    - `lib/sigra/plug/fetch_session.ex:113-162` for missing/invalid fail-closed precedent.
  </read_first>
  <behavior>
    - Test 1: unknown/malformed cookie, deleted session row, missing owning user, and revoked row state return the same safe denial class.
    - Test 2: an otherwise valid serialized lane from before deletion cannot be used to invoke the evaluator after the row disappears.
    - Test 3: evaluator injection/Mox records zero invocations for every host-binding failure.
  </behavior>
  <action>Add tests before code, using real Ecto rows and an injected evaluator seam. Make every public projection/replay operation repeat canonical session/user lookup. Normalize malformed, absent, deleted/revoked, missing-user, and non-active outcomes to a safe host denial before building a lane or calling Crosswake. Keep diagnostics categorical and free of raw tokens, digests, IDs, or supplied values. Do not accept a cached lane/current scope as fallback authority.</action>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_session_adapter_test.exs --only crosswake_currentness</automated>
  </verify>
  <acceptance_criteria>Every missing/revoked/non-active case denies with evaluator invocation count zero, and a cached pre-deletion lane cannot affect the result.</acceptance_criteria>
  <done>Canonical storage absence/currentness dominates any previously projected Crosswake facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deny idle and absolute expiry with an injected server clock</name>
  <files>test/example/lib/example/accounts/crosswake_session_adapter.ex, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>
    - `lib/sigra/plug/fetch_session.ex:132-162` for standard, remember-me, and pending timeout calculations.
    - `test/sigra/plug/fetch_session_test.exs` for fixed-record expiry assertions and eager cleanup expectations.
    - `test/example/lib/example/accounts/crosswake_session_adapter.ex` after Task 1.
  </read_first>
  <behavior>
    - Test 1: standard session at the idle boundary and beyond denies; one microsecond inside remains valid.
    - Test 2: standard session at the absolute boundary and beyond denies; one microsecond inside remains valid.
    - Test 3: expired rows deny before evaluator invocation, using fixed UTC values with no sleeping.
  </behavior>
  <action>Centralize current-row validity in the adapter using an injected `as_of` and the example host's configured session limits, matching Sigra's strict-less-than boundary semantics. Return denial before contract/evaluator work for idle or absolute expiry. Keep Crosswake lane expiry fields derived from the same server calculation so the host and evaluator see consistent facts. Exercise exact boundary values deterministically; do not use wall-clock waits.</action>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_session_adapter_test.exs --only crosswake_expiry</automated>
  </verify>
  <acceptance_criteria>Exact idle/absolute boundaries fail closed, inside-boundary controls pass, evaluator non-invocation is asserted for expirations, and the test contains no sleep.</acceptance_criteria>
  <done>Replay cannot outlive current SIGRA idle or absolute validity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached continuation → current database | Previously valid facts may be replayed after server revocation. |
| server clock/config → authority facts | Time calculations decide whether authority still exists. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-07 | Elevation of Privilege | revoked/deleted replay | high | mitigate | Fresh database/user resolution and zero evaluator calls on absence/currentness failure. |
| T-239-08 | Tampering | serialized lane reuse | high | mitigate | Never accept cached facts as a substitute; reconstruct only after fresh validation. |
| T-239-09 | Elevation of Privilege | idle/absolute expiry | high | mitigate | Match strict host timeout semantics with injected UTC time and exact boundary tests. |
| T-239-10 | Information Disclosure | denial diagnostics | medium | mitigate | Emit stable categorical codes without echoing credentials, digests, IDs, or callback values. |
</threat_model>

<verification>
Run both focused tags and then the complete adapter file; all commands use fixed time and real Ecto rows.
</verification>

<success_criteria>
- Revocation/deletion and missing user/session are immediately authoritative.
- Idle/absolute expiry semantics match the host.
- No invalid host state reaches the pure evaluator.
</success_criteria>

## Artifacts this phase produces

- Currentness/revocation denial branch in the host adapter.
- Exact deterministic expiry boundary proof.

<output>
Create `.planning/phases/239-hosted-session-interop/239-03-SUMMARY.md` when done.
</output>

