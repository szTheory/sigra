# Phase 240.3: Wire Hosted Crosswake Runtime Flow - Discussion Log (Assumptions Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-08-11
**Phase:** 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
**Mode:** assumptions with advisor research
**Areas analyzed:** runtime host boundary, continuation authority, response semantics, deterministic closure evidence, adopter DX/JTBD

## Assumptions Presented

### Runtime Host Boundary

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| Close the gap with a narrow authenticated runtime consumer in `test/example`, not a Sigra-core dependency. | Confident | `.planning/v1.48-MILESTONE-AUDIT.md`; `.planning/phases/239-hosted-session-interop/239-PATTERNS.md`; `test/example/mix.exs` |

### Request, Continuation, and Authority Source

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| Take the raw token only from the host Plug session and retain departure-time expected binding in host-controlled continuation state. | Confident | `test/example/lib/example_web/user_auth.ex`; `test/example/lib/example/accounts/crosswake_session_adapter.ex`; Phase 239 Plans 04/05 |

### Runtime Response Semantics

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| Use a fixed host-owned route and controller response boundary; return data remains evidence only. | Likely | Existing adapter API/tests; milestone audit missing request-to-response handoff |

### Deterministic Closure Evidence

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| Add request-level router/session proof while retaining the adapter matrix. | Likely | `ConnCase`; Phase 239 proof runner; repository-wide absence of runtime call sites |

## Advisor Research Applied

Three focused advisor passes examined Phoenix/Plug/LiveView architecture, continuation and return security, and runtime proof plus adopter DX. They compared example-only controllers, Plugs, LiveView, generated integration, Sigra-core integration, multiple continuation state models, route/destination ownership, and ConnCase/browser/fresh-host/network evidence levels. They also applied relevant repository prompt research and primary-source Phoenix, Plug, OAuth security, and Playwright guidance.

Key refinements beyond the initial assumptions:

- Selected a database-backed, expiring, atomically consumed continuation record instead of leaving continuation storage to implementation discretion.
- Selected an authenticated `POST` start and an explicitly classified browser return rather than a generic route guard.
- Added one focused Playwright happy-path journey on top of adapter, Ecto, and `ConnCase` layers.
- Explicitly deferred generator support until the example/reference contract proves stable.
- Added consumer-focused recovery copy, redacted observability, domain language, and truthful CI claim boundaries.

## Corrections Made

No user corrections. The user requested deeper research across every decision area and then accepted the resulting cohesive recommendation set in full.

## Methodology

- **Decisive Defaulting:** Chose one coherent controller/context/continuation architecture rather than preserving an option matrix.
- **Escalation Threshold:** Preserved locked security, public-contract, generated-host, and proof boundaries; no unresolved choice remained above the threshold.
- **Prompt and Prior-Art Weighting:** Applied Sigra's hybrid host/library philosophy, Phoenix context/controller conventions, successful auth-library lessons, and OAuth transaction-security guidance.
- **User Experience Bias:** Kept successful infrastructure invisible, recovery generic and actionable, and implementation vocabulary out of end-user copy.
- **Truthful Evidence:** Required layered deterministic proof without claiming external services, providers, deployment, or devices.
