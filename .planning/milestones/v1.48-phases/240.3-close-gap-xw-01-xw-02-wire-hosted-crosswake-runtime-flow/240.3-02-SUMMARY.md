---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "02"
subsystem: hosted-crosswake-continuation
tags: [crosswake, ecto, postgres, replay-protection, pkce, security]
requires:
  - phase: 240.3-01
    provides: "Host-owned continuation tracer and authenticated runtime entry points"
provides:
  - "Atomic digest-addressed continuation claims with strict expiry and one-time consumption"
  - "Digest-only persistence and stable redacted terminal outcomes"
  - "PostgreSQL race, replay, expiry, cleanup, and authority-isolation matrix"
affects: [240.3-03, 240.3-05]
tech-stack:
  added: []
  patterns: ["Conditional UPDATE ... RETURNING-style Ecto claim", "Bounded terminal-row cleanup through an oldest-first ID subquery", "Programmatic trusted Ecto changesets"]
key-files:
  created:
    - test/example/test/example/accounts/crosswake_continuations_test.exs
  modified:
    - test/example/lib/example/accounts/crosswake_continuation.ex
    - test/example/lib/example/accounts/crosswake_continuations.ex
    - test/example/priv/repo/migrations/20260811170000_create_crosswake_continuations.exs
decisions:
  - "The conditional claim query selects and returns the claimed row directly, eliminating a post-claim lookup path."
  - "Continuation timestamps use microsecond precision so strict before/equality expiry behavior remains exact."
metrics:
  duration: 22min
  completed: 2026-08-11
status: complete
---

# Phase 240.3 Plan 02: Harden Hosted Crosswake Continuation Summary

**The example host now persists only digest-addressed, one-time Crosswake correlation records and proves replay/race resistance against real PostgreSQL rows.**

## Accomplishments

- Replaced the continuation issue changeset's request-style casting with a narrow programmatic allowlist, digest validation, and required opaque server fields.
- Claimed records with a single digest/unused/strict-expiry update that returns the winning row, then retained consumption through every allowed or denied terminal result.
- Added redacted stable outcome classes and bounded, oldest-first deletion of terminal or expired rows.
- Added deterministic real-row coverage for digest-only storage, mismatched state/PKCE, exact expiry, sequential replay, synchronized racing claims, binding isolation, and 500-row cleanup limits.

## Verification

- `set -a; source tmp/db.env; set +a; cd test/example && MIX_ENV=test mix ecto.migrate --quiet && mix test test/example/accounts/crosswake_continuations_test.exs` — passed (6 tests).
- `mix precommit` — blocked only by the pre-existing `/dev/mailbox` verified-route warning in `settings_live.ex:133`; no continuation test or compilation error was reported before the repository-wide warnings-as-errors gate stopped.

## Task Commits

1. **Task 1 RED: add continuation persistence matrix** — `b25c84db` (test)
2. **Task 1 GREEN: harden Crosswake continuation claims** — `80d6e4b9` (feat)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Correctness] Preserve microsecond-precise continuation boundaries.**
- **Found during:** Task 1 GREEN verification.
- **Issue:** The tracer migration and schema used second-precision UTC fields, which rejects deterministic microsecond timestamps and cannot prove exact expiry boundaries.
- **Fix:** Changed continuation lifecycle fields to `:utc_datetime_usec` in the schema and its unshipped migration.
- **Files modified:** `test/example/lib/example/accounts/crosswake_continuation.ex`, `test/example/priv/repo/migrations/20260811170000_create_crosswake_continuations.exs`.
- **Verification:** The exact-boundary focused PostgreSQL matrix passes.
- **Commit:** `80d6e4b9`.

**Total deviations:** 1 auto-fixed (1 Rule 1).
**Impact:** The stored continuation now preserves the exact strict-expiry contract required by D-12.

## Deferred Issues

- Repository-wide `mix precommit` remains blocked by the unrelated verified-route warning documented in `deferred-items.md`.

## Known Stubs

None.

## Threat Flags

None. The task only narrows the existing PostgreSQL continuation trust boundary and adds its proof matrix.

## Self-Check: PASSED

- Confirmed all three continuation artifacts exist.
- Confirmed task commits `b25c84db` and `80d6e4b9` exist in Git history.
- Confirmed the explicit migration plus focused real-PostgreSQL matrix passes.
