---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "10"
type: execute
wave: 10
depends_on: ["240.3-09"]
files_modified:
  - scripts/ci/hosted-session-interop-proof.sh
  - test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs
  - .planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json
autonomous: true
gap_closure: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "The exact-SHA proof runs the corrected router and Chromium flow plus real/bad/clean prohibition enforcement before writing evidence, so URL confidentiality and fail-first disposition are receipt-gating behavior."
    - "The regenerated receipt records all three prohibitions as resolved/test with their deterministic descriptors and red/green evidence, while retaining the two genuinely unresolved spec-less edge assumptions."
    - "The evidence remains bound to one clean committed SHA and continues to claim only the in-process example-host runtime with no external API, provider, deployment, generated-host, or device scope."
  artifacts:
    - path: "scripts/ci/hosted-session-interop-proof.sh"
      provides: "Ordered prohibition-enforcement command before same-SHA receipt sealing"
    - path: "test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs"
      provides: "Receipt schema/order/status/descriptor and callback-secret source contract"
    - path: ".planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json"
      provides: "Exact-SHA receipt with resolved test-tier prohibitions and preserved unresolved assumptions"
      contains: "sigra.phase240_3.hosted-crosswake-runtime-evidence.v1"
  key_links:
    - from: "scripts/ci/hosted-session-interop-proof.sh"
      to: "scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs"
      via: "bounded real/bad/clean command runs before source contract and receipt writer"
      pattern: "p14-crosswake-authority-secrets"
    - from: "scripts/ci/hosted-session-interop-proof.sh"
      to: ".planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json"
      via: "same clean TESTED_SIGRA_SHA recheck followed by receipt-last write"
      pattern: "assert_same_clean_worktree_sha"
    - from: "test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs"
      to: ".planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json"
      via: "machine assertions distinguish resolved test prohibitions from unresolved edge assumptions"
      pattern: "prohibitions"
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-01 unclassified; the receipt preserves this owner-owned assumption after mechanical prohibition closure."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-02 unclassified; deterministic denial evidence does not fabricate its classification."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: resolved
      verification: test
      statement: "MUST NOT manufacture organization authority for a personal SIGRA session."
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json
      check_clean_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json
      reason: "Exact-SHA proof runs the shipped guard and its content-caused fail-first controls."
    - requirement_id: XW-01
      category: secret-boundary
      status: resolved
      verification: test
      statement: "MUST NOT expose the PKCE verifier in the callback URL, browser history, logs, or observable request metadata."
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json
      check_clean_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json
      reason: "Exact-SHA router/browser proof plus the fail-first guard enforce the corrected secret boundary."
    - requirement_id: XW-02
      category: authority-smuggling
      status: resolved
      verification: test
      statement: "MUST NOT let callback data select or reconstruct the session, subject, policy route, grant decision, or navigation destination."
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json
      check_clean_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json
      reason: "Exact-SHA controller denial proof plus the fail-first guard enforce fixed authority/navigation."
---

<objective>
Seal the verifier-confidential runtime and three deterministic prohibition dispositions into the existing exact-SHA receipt-last proof.

Purpose: Make Plans 08–09 durable evidence rather than an unsealed local correction, without converting the separate edge assumptions into invented approvals.
Output: Updated ordered runner/contract and one regenerated machine-readable Phase 240.3 receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-VERIFICATION.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-REVIEW.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-07-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-08-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-09-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/COVERAGE.md

<interfaces>
- Preserve the receipt schema identifier and immutable Phase 239 release coordinates; add one ordered local command and replace the old flagged prohibition collection with resolved test-tier `prohibitions` entries carrying descriptors and enforcement outcomes.
- Preserve `unresolved_assumptions` with XW-01/XW-02 exactly; they are not prohibition statuses and do not become resolved here.
- The full runner remains `scripts/ci/hosted-session-interop-proof.sh`; `--browser-only` remains diagnostic and never writes evidence.
- Only the exact receipt path may differ during the proof; Task 2 must run after Task 1 and Plans 08–09 are committed.
</interfaces>
</context>

## Artifacts this phase produces

- No new file paths or public symbols.
- `scripts/ci/hosted-session-interop-proof.sh` gains one bounded real/bad/clean prohibition command and the corresponding receipt row/status payload.
- `test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` gains ordered-command and resolved-prohibition assertions.
- `240.3-HOSTED-RUNTIME-EVIDENCE.json` is regenerated last at the committed Task 1 SHA.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make verifier confidentiality and prohibition enforcement receipt-gating</name>
  <reversibility rating="reversible">The runner, internal receipt payload, and source contract can be changed together and regenerated without data migration or public API breakage.</reversibility>
  <files>scripts/ci/hosted-session-interop-proof.sh, test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs</files>
  <read_first>
    - Read both target files in full at the committed Plan 09 state, plus `scripts/ci/lib/exact-sha-worktree.sh` for the immutable pre/post guard contract.
    - Read the committed Plan 08 controller/router/browser artifacts and Plan 09 guard/fixtures so source assertions name real symbols and test names.
    - Read the current evidence JSON and preserve its schema ID, release coordinates, command/result shape, capability boundary, API detector, and two unresolved assumptions.
    - Read `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/COVERAGE.md`; it remains the only API coverage declaration.
  </read_first>
  <behavior>
    - The runner's new bounded prohibition command passes the guard normally, requires the known-bad invocation to fail, and requires the known-clean invocation to pass; any inverted/vacuous outcome prevents receipt writing.
    - The prohibition command runs after router/browser behavior and before the phase source contract, same-SHA recheck, and write_evidence.
    - The source contract requires seven ordered passed command rows, absence of pkce_verifier from the callback URL contract, three resolved/test prohibition entries with complete descriptor/evidence keys, and exactly two unresolved edge assumptions.
    - The evidence writer cannot emit `flagged_unverified_prohibitions`; it emits the three categories under `prohibitions` with status resolved, verification test, check descriptors, and repository/fail-first/clean-control passed outcomes.
  </behavior>
  <action>Start RED by strengthening the phase source contract around the committed Plan 08 callback/session markers, the Plan 09 guard/fixture paths, seven-command order, and the new receipt disposition shape. In the full runner, add one `run_bounded` shell invocation that executes the real node guard, explicitly inverts the known-bad fixture command, and executes the known-clean control; keep the command free of secret values and use only committed paths. Place it after the browser command and before the phase source contract so behavior and enforcement both gate the same-SHA receipt. Update `write_evidence` to append that ordered local command/test category; remove the pass-shaped flagged-unverified prohibition field and emit exactly three resolved/test items with categories, statements, flat check descriptors, and explicit `repository_passed`, `fail_first_passed`, and `clean_control_passed` evidence booleans. Preserve both unresolved assumptions unchanged and do not add an owner-approved field. Decision trace: preserve host-only ownership (D-01), thin controller orchestration (D-02), authenticated start/browser return pipelines (D-03), fresh departure binding (D-04), digest-only persistence and secret confinement (D-05), atomic claim/evaluator ordering (D-06), evidence-only callback input (D-07), fixed route/destination (D-08), safe 303 completion (D-09), generic recovery (D-10), redacted observability (D-11), adapter/continuation matrices (D-12), real-router hostile matrix (D-13), deterministic Chromium proof (D-14), exact-SHA receipt-last evidence (D-15), copyable truthful recipe (D-16), and deferred public/generator scope (D-17). Keep historical Phase 239 evidence, API detector false, redaction allowlist, and all six prior commands. Do not address terminal outcome-write or ignored-runtime-input warnings in this task: the gap-closure contract classifies them as advisory and they are not required to transport or prove the callback verifier safely.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The source contract fails before the runner/writer changes and passes only when the real/bad/clean command is ordered before same-SHA recheck and receipt writing.
    - Runner retains strict mode, bounded commands, all prior schema/adapter/continuation/controller/browser/contract gates, and `--browser-only`; it writes no evidence if the bad fixture unexpectedly passes or clean/real execution fails.
    - Writer emits seven ordered zero-status command rows, three resolved/test prohibition entries with complete descriptors and three passed enforcement booleans, and exactly two unresolved assumptions.
    - `flagged_unverified_prohibitions` is absent from the writer payload; no field claims owner approval.
    - Existing receipt, Phase 239 artifacts, COVERAGE.md, runtime code, schemas, dependencies, Sigra core, and generated templates are unchanged by Task 1.
  </acceptance_criteria>
  <done>The committed runner and source contract require the corrected browser behavior plus non-vacuous prohibition enforcement before a receipt can be sealed.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the exact-SHA receipt with honest prohibition dispositions</name>
  <precondition>Plans 08–09 and Task 1 are committed; `tmp/db.env` exists; and no tracked, staged, unstaged, or non-ignored untracked input differs except the exact Phase 240.3 evidence path allowed by the existing guard.</precondition>
  <reversibility rating="reversible">The receipt is generated evidence and can be regenerated from the same committed proof inputs.</reversibility>
  <files>.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json</files>
  <read_first>
    - Read the committed Task 1 runner/contract and the current receipt before invoking the proof.
    - Read `scripts/ci/lib/exact-sha-worktree.sh` and run read-only `git status --porcelain=v1 --untracked-files=all` plus `git rev-parse HEAD`; stop with paths if the existing precondition is not met.
    - Read Plan 09's three prohibition descriptors and preserve them byte-for-byte in the evidence payload.
  </read_first>
  <action>Run the full `scripts/ci/hosted-session-interop-proof.sh` exactly once from repository root after the precondition passes. Do not delete the prior receipt: it is the sole allowed difference and the writer replaces it only after migrations, immutable-release validation, adapter/continuation/controller/browser, prohibition real/bad/clean, phase source-contract, and same-SHA/worktree gates all pass. On any failure, preserve user-owned state and stop with durable diagnostics; do not rerun, edit a pass-shaped receipt manually, relax a guard, or change the evidence exclusion. After success, run only read-only validation: receipt SHA equals the pre-run committed HEAD; seven commands are ordered/passed; the callback/browser command and prohibition command are present; immutable release coordinates and `api_detector.detected == false` remain; unresolved assumptions are exactly XW-01/XW-02; prohibitions are exactly the three expected categories with resolved/test/descriptors and three true enforcement outcomes; the old flagged collection and secret-shaped fields are absent.</action>
  <verify>
    <automated>scripts/ci/hosted-session-interop-proof.sh</automated>
  </verify>
  <acceptance_criteria>
    - One full bounded run exits zero and writes the receipt only after the same clean SHA is revalidated.
    - Receipt SHA equals the committed Task 1 HEAD and all seven ordered local commands have numeric exit status 0 and outcome passed.
    - `unresolved_assumptions` retains exactly XW-01/XW-02; `prohibitions` contains exactly authority-integrity, secret-boundary, and authority-smuggling, each resolved/test with complete descriptors and true repository/fail-first/clean-control outcomes.
    - The receipt contains no `flagged_unverified_prohibitions`, owner-approval claim, verifier/cookie/session/binding/token/credential value, external API claim, or expanded provider/deployment/device scope.
    - The evidence JSON is the only task-created post-proof worktree difference; unrelated user state is preserved if the precondition blocks execution.
  </acceptance_criteria>
  <done>The receipt is sealed last at the exact committed SHA and truthfully records both deterministic prohibition closure and the still-unresolved owner-owned edge assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime/browser/guard commands -> runner outcomes | Every behavior and fail-first control must genuinely execute before evidence. |
| committed worktree -> receipt writer | The SHA and all exercised inputs must remain unchanged through sealing. |
| prohibition evidence -> receipt consumer | Resolved test evidence must not be confused with owner approval or unresolved edge classification. |
| receipt -> milestone decision | Capability and secret boundaries must remain narrow and machine-readable. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-10-01 | Repudiation | pass-shaped prohibition status | high | mitigate | Ordered real/bad/clean command plus descriptor/evidence contract blocks narrative-only resolution. |
| T-240.3-10-02 | Tampering | tested SHA/worktree | high | mitigate | Existing full-tree pre/post guard and captured SHA remain immediately around all seven commands and writer. |
| T-240.3-10-03 | Information Disclosure | runner logs/receipt | high | mitigate | No dynamic secrets enter guard inputs or payload; existing field allowlist and callback negative tests remain receipt-gating. |
| T-240.3-10-04 | Spoofing | owner approval | high | mitigate | Receipt distinguishes resolved mechanical prohibitions from unresolved assumptions and contains no approval assertion. |
| T-240.3-10-05 | Elevation of Privilege | omitted router/authority suite | high | mitigate | All adapter, continuation, controller, browser, and authority-smuggling guard commands remain ordered prerequisites. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe deterministic example-host request/session, one-time continuation, evaluator, and response without scope widening | 08–10 | COVERED | URL secret transport fixed, guarded, and receipt-sealed. |
| REQ | XW-01 | Personal projection without invented org or credential/token exposure | 08–10 | COVERED | Encrypted session transport plus authority/secret guards and receipt. |
| REQ | XW-02 | Missing/stale/switched/replayed state fails closed; return data alone grants nothing | 08–10 | COVERED | URL input narrowed and fixed-authority guard remains full-proof gating. |
| RESEARCH | — | PKCE validation, redaction, deterministic router/browser proof | 08–10 | COVERED | Corrects the insecure query transport while retaining digest validation. |
| CONTEXT | D-01–D-17 | All locked implementation/proof/scope decisions | 08–10 | COVERED | Directly implements D-05/D-11; regression tests and exact-SHA proof preserve the remainder. |
| VERIFICATION | CR-01 | Raw verifier in callback URL/history/metadata | 08, 10 | COVERED | Production transport correction plus router/browser/exact-SHA evidence. |
| VERIFICATION | Prohibition dispositions | Three flags require owner resolution or deterministic enforcement | 09, 10 | COVERED | Mechanical items become resolved/test with fail-first evidence; edge assumptions remain unresolved. |
| REVIEW | WR-01 | Ignored terminal outcome-write failure | — | EXCLUDED | Advisory and not tightly coupled to callback secret transport or prohibition enforcement. |
| REVIEW | WR-02 | Ignored runtime inputs in exact-SHA guard | — | EXCLUDED | Advisory and not tightly coupled; existing tracked/non-ignored guard remains unchanged. |
| CONTEXT | Deferred ideas | Generator/public abstraction/network/provider/device work | — | EXCLUDED | Explicitly deferred and absent. |

API coverage detector: existing `COVERAGE.md` remains valid and is not replaced by a fabricated surface; the phase still uses only the existing in-process package. Assumption-delta detector: `detected: true` pluralization signals (`session`, `cookie`, `callback`) were recorded in Plan 08 and produce no new selectable/configurable decision. Schema-push detector: no supported JS/TS ORM pattern is present; Ecto changes are outside that detector and no Ecto schema changes are planned.

<verification>
Commit Task 1, assert the exact-SHA precondition, then run the full proof once. Validate the receipt structurally and semantically without rerunning the writer. Every high-severity threat blocks completion; neither source assertions alone nor owner-approval language can substitute for the real/bad/clean and browser runs.
</verification>

<success_criteria>
- Full proof at one immutable clean SHA includes the corrected callback/browser path and fail-first prohibition enforcement.
- Receipt distinguishes three resolved mechanical prohibitions from two unresolved owner-owned edge assumptions.
- XW-01/XW-02 close without core/generated/API/schema/provider/deployment/device scope expansion.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-10-SUMMARY.md` when done.
</output>
