---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
verified: 2026-08-11T21:48:25Z
status: passed
score: 15/15 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 12/15
  gaps_closed:
    - "The raw PKCE verifier is no longer present in the GET /crosswake/return URL contract."
    - "The three mechanical XW-01/XW-02 prohibitions now have resolved test-tier real/bad/clean enforcement."
  gaps_remaining: []
  regressions: []
---

# Phase 240.3: Hosted Crosswake Runtime Flow Verification Report

**Phase Goal:** Make the already-proven personal-session Crosswake adapter reachable through a deterministic example-host request/session, one-time continuation, evaluator, and safe response flow without widening Sigra core or generated-host scope.
**Verified:** 2026-08-11T21:48:25Z
**Status:** passed
**Re-verification:** Yes — after gap-closure Plans 08–10

## Goal Achievement

The previous callback-secret and prohibition-disposition blockers are closed by inspected, wired code and deterministic evidence. The committed exact-SHA receipt was written after a single clean-database bounded run that includes the real Chromium cookie-jar command; the source-contract test confirms that this receipt gates on the ordered browser, prohibition, and same-SHA checks.

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | An authenticated example-host request reaches the existing adapter through a database continuation and fixed `/app` destination. | ✓ VERIFIED | `CrosswakeController.start/2` calls `issue/2`; return calls `complete/4`; the focused controller suite passed 8/8 and asserts both 303s, adapter evaluation, personal `org_id: nil`, and `/app`. |
| 2 | Departure and return use the Plug session plus a fresh SIGRA backend session, not callback authority. | ✓ VERIFIED | The controller takes `:user_token` from session on both legs; `issue/3` calls `expected_binding/2`; `complete/5` evaluates with the stored binding and raw session token. Focused controller denial cases passed. |
| 3 | Continuation persistence is digest-only, one-time, strictly expires, and preserves independent departure bindings. | ✓ VERIFIED | Schema stores SHA-256 correlation fields; `claim/2` requires unused and `expires_at > as_of`; five focused continuation tests passed, including mismatch, exact-expiry, race/replay, and concurrent-departure cases. |
| 4 | Callback input cannot select authority, route, grant, or navigation destination. | ✓ VERIFIED | `@allowed_return_keys` is exactly `continuation,state`; `scalar_return_input/1` rejects extra/scalar-invalid input; context creates fixed route and `/app`. Controller smuggling test and real prohibition guard pass. |
| 5 | The raw PKCE verifier is confined to continuation-bound encrypted HttpOnly session transport, never the callback URL. | ✓ VERIFIED | `start/2` redirects with only `continuation` and `state`; verifier is session-stored with exact expiry and removed before completion. Endpoint config has signing/encryption, HttpOnly, SameSite=Lax, and Secure-by-default policy. Real/bad/clean secret-boundary guard passed. |
| 6 | Missing, revoked, expired, switched, malformed, and replayed returns deny before evaluator use. | ✓ VERIFIED | `take_transport/3` consumes/prunes before completion; controller suite passed its 8 cases, including currentness/switch/replay/input matrices and evaluator non-invocation assertions. |
| 7 | Success uses fixed safe 303 navigation; denials use generic recovery and redacted telemetry. | ✓ VERIFIED | `allow/1` redirects only to `CrosswakeContinuations.destination/0`; `return/2` adds `Referrer-Policy: no-referrer`; telemetry metadata is fixed to outcome/reason/correlation nil. |
| 8 | Chromium proves the real cookie-jar callback and no-referrer final navigation without exposing verifier or correlation data. | ✓ VERIFIED | Exact-SHA receipt for `e3e8544104…` records `scripts/ci/hosted-session-interop-proof.sh --browser-only` passed after the controller suite and before real/bad/clean enforcement; the only later commit changes the receipt itself. |
| 9 | Three mechanical XW prohibitions have non-vacuous deterministic enforcement; unresolved edge assumptions are not portrayed as approvals. | ✓ VERIFIED | Current `node --test` repository guard passed; known-bad fixture failed all three assertions; known-clean fixture passed. Receipt source contract passed 8/8 and the JSON records three `resolved/test` results plus two separate unresolved assumptions. |
| 10 | The correction remains example-host composition and does not widen Sigra core, generators, persistence schema beyond its host table, or external API scope. | ✓ VERIFIED | Runtime implementation remains under `test/example`; source contract passed 8/8 and explicitly rejects root dependency/public generator/network additions. |

**Score:** 15/15 truths verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `test/example/lib/example/accounts/crosswake_continuation.ex` | Digest-only host continuation schema | ✓ VERIFIED | Substantive Ecto schema/changesets; only digest correlation and server-owned fields are persisted. |
| `test/example/lib/example/accounts/crosswake_continuations.ex` | Issue, atomic claim, completion, cleanup, adapter seam | ✓ VERIFIED | `issue/3`, `complete/5`, and conditional `Repo.update_all` are substantive and consumed by controller/tests. `cleanup_expired/1` is test-only (not production-scheduled), an advisory wiring note. |
| `test/example/lib/example_web/controllers/crosswake_controller.ex` | Thin safe start/return orchestration | ✓ VERIFIED | Router reaches both actions; controller delegates application work and owns only transient session transport. |
| `test/example/lib/example_web/endpoint.ex` plus config | Secure encrypted cookie policy | ✓ VERIFIED | `Plug.Session` is signed/encrypted, HttpOnly, SameSite=Lax; shared config true and `test.exs` narrowly overrides false. |
| Controller/continuation suites | Router, session, claim, and denial matrix | ✓ VERIFIED | Controller suite passed 8/8; five named continuation security tests passed. |
| Browser spec/config/runner | Real serial cookie-jar confidentiality proof | ✓ VERIFIED | Exact-SHA receipt records the browser-only command as passed from a clean reset `example_test` database; source contract asserts its ordered receipt gate. |
| Prohibition guard/fixtures/source contract | Real/bad/clean deterministic enforcement and receipt gates | ✓ VERIFIED | Three Node tests pass on repository and clean fixture; bad fixture fails as required; source contract passed 8/8. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Router | Controller | authenticated POST `/crosswake/start`, browser GET `/crosswake/return` | ✓ WIRED | Concrete Phoenix routes reference `CrosswakeController`. |
| Controller | Continuations context | `CrosswakeContinuations.issue` / `complete` | ✓ WIRED | Direct calls on request path; controller suite exercised them. |
| Continuations context | Session adapter | `expected_binding` / `evaluate_return` | ✓ WIRED | Calls occur at issuance/evaluation with stored departure binding. |
| Controller | Encrypted Plug session | `crosswake_pkce` continuation-bound entries | ✓ WIRED | Start stores handle/verifier/expiry; return takes and deletes before completion. |
| Browser runner | Playwright proof | bounded `--browser-only` invocation | ✓ WIRED | Runner starts the test-only loopback host and runs the dedicated one-worker, zero-retry project. |
| Receipt runner | prohibition guard and receipt | real/bad/clean before same-SHA recheck and writer | ✓ WIRED | `hosted-session-interop-proof.sh` runs the three-command guard before source contract/recheck/write. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Controller/context | raw session token and continuation | Plug session → fresh adapter lookup → Ecto continuation → server-built envelope | Yes | ✓ FLOWING |
| Controller transport | verifier keyed by continuation | `issue/3` random verifier and persisted exact expiry → encrypted session entry → private `complete/5` input | Yes | ✓ FLOWING |
| Context | claimed continuation | conditional `Repo.update_all` over `auth.crosswake_continuations` | Yes | ✓ FLOWING |
| Browser proof | callback and `/app` document requests | real browser navigation requests | Yes — receipt records the bounded Chromium command passed at tested SHA `e3e8544104…` | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Three prohibition boundaries and fail-first controls | `node --test …p14… && ! bad-fixture && clean-fixture` | repository 3/3 pass; bad fixture 3/3 fail; clean 3/3 pass | ✓ PASS |
| Source/receipt/scope contract | `MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` | 8 tests, 0 failures | ✓ PASS |
| Core continuation behavior | five named continuation tests | 5 tests, 0 failures | ✓ PASS |
| Router/session/evaluator behavior | `MIX_ENV=test PORT=4002 mix test …crosswake_controller_test.exs --include example_app` | 8 tests, 0 failures | ✓ PASS |
| Full continuation suite | `cd test/example && mix test test/example/accounts/crosswake_continuations_test.exs` | Exact-SHA receipt records pass after the scoped `example_test` DB reset | ✓ PASS |
| Chromium browser behavior | `scripts/ci/hosted-session-interop-proof.sh --browser-only` | Exact-SHA receipt records exit 0 / `passed` after controller suite and before prohibition/source-contract gates | ✓ PASS |

The earlier verifier-run cleanup aggregate failure came from residual data in its already-populated configured test database. It is not a source failure: the later exact-SHA full proof reset the scoped `example_test` database and records the complete continuation suite as passed. The receipt is valid for the runtime source SHA because `git diff e3e8544104…..7ae42c18` contains only the receipt path.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| XW-01 | 01–10 | A backend-validated personal session projects without organization invention or credential/token exposure. | ✓ SATISFIED | Fresh personal binding, `org_id: nil`, verifier-free callback, encrypted host-session handoff, and passing authority/secret guard. |
| XW-02 | 01–10 | Missing, expired, revoked, or account-switched state fails closed; return data alone never grants access. | ✓ SATISFIED | Atomic claim, strict expiry, stored binding, fresh lookup, input filtering, smuggling denials, and passing controller/guard tests. |

All ten plan frontmatters declare both XW-01 and XW-02. `REQUIREMENTS.md` maps those IDs to Phase 239 rather than this inserted gap-closure phase; no additional phase-240.3 requirement is listed, so there are no orphaned requirement IDs.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `test/example/lib/example/accounts/crosswake_continuations.ex` | 62 | `cleanup_expired/1` has no production caller | ℹ️ Info | Bounded cleanup is an explicit testable maintenance operation, not part of the request/return authority path. |
| `scripts/ci/hosted-session-interop-proof.sh` | 70 | `mktemp` | ℹ️ Info | Normal temporary redacted server-log handling, not a stub. |

No `TBD`, `FIXME`, `XXX`, placeholder, empty handler, or hardcoded user-visible empty-data implementation was found in phase runtime artifacts.

### Gaps Summary

No gaps remain. The raw callback verifier and unresolved mechanical prohibition dispositions are closed, and the exact-SHA receipt supplies deterministic current browser evidence without requiring repeat service startup.

---

_Verified: 2026-08-11T21:48:25Z_
_Verifier: the agent (gsd-verifier)_
