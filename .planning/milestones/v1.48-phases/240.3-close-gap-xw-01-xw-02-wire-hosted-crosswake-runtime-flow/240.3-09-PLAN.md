---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "09"
type: execute
wave: 9
depends_on: ["240.3-08"]
files_modified:
  - scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
  - test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json
  - test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json
  - test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs
autonomous: true
gap_closure: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "The three XW-01/XW-02 must-NOT statements have one deterministic node:test enforcement surface that reads the shipped host artifacts, fails non-vacuously on a known violation, and passes on both a known-clean control and the real repository."
    - "The verifier can locate and machine-prove the guard through complete flat check descriptors; no owner approval or manual judgment is fabricated."
    - "The two spec-less edge assumptions remain explicitly unresolved because mechanical prohibition evidence does not decide their separate owner-owned classification."
  artifacts:
    - path: "scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs"
      provides: "Non-vacuous mechanical guard for authority-integrity, secret-boundary, and authority-smuggling"
      contains: "GSD_PROHIB_SUBJECT"
    - path: "test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json"
      provides: "Known violating subject that proves the guard goes red"
    - path: "test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json"
      provides: "Known clean control proving content-caused red/green behavior"
    - path: "test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs"
      provides: "Phase source contract pinning the guard, fixtures, and descriptor paths"
  key_links:
    - from: "scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs"
      to: "test/example host Crosswake runtime artifacts"
      via: "normal execution derives non-vacuous facts from endpoint, controller, adapter, router tests, and browser proof"
      pattern: "readRepoFile"
    - from: "must_haves.prohibitions"
      to: "scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs"
      via: "node-test descriptor with violation and clean fixtures"
      pattern: "check_target"
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-01 unclassified; deterministic must-NOT enforcement does not supply the missing owner classification."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-02 unclassified; its assumption remains visible even though the fail-closed prohibition is mechanically guarded."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: resolved
      verification: test
      statement: "MUST NOT manufacture organization authority for a personal SIGRA session."
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json
      check_clean_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json
      reason: "Mechanically enforced by non-vacuous adapter personal-org projection and real-router evaluator assertions."
    - requirement_id: XW-01
      category: secret-boundary
      status: resolved
      verification: test
      statement: "MUST NOT expose the PKCE verifier in the callback URL, browser history, logs, or observable request metadata."
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json
      check_clean_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json
      reason: "Mechanically enforced by the encrypted HttpOnly session contract plus dynamic router/browser negative assertions."
    - requirement_id: XW-02
      category: authority-smuggling
      status: resolved
      verification: test
      statement: "MUST NOT let callback data select or reconstruct the session, subject, policy route, grant decision, or navigation destination."
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json
      check_clean_fixture: test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json
      reason: "Mechanically enforced by exact callback-key extraction and fixed route/destination/evaluator test markers."
---

<objective>
Convert the three mechanically checkable Crosswake prohibitions from honest unresolved flags into fail-first-proven test-tier enforcement.

Purpose: Close the verification disposition gap with deterministic evidence where the repository can prove it, while keeping genuinely owner-owned edge assumptions unresolved.
Output: One node:test prohibition guard, known-bad/known-clean subjects, complete verifier descriptors, and a phase contract that prevents evidence wiring drift.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-VERIFICATION.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-REVIEW.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-08-SUMMARY.md
@/Users/jon/.codex/gsd-core/references/prohibition-probe.md

<interfaces>
- `check prohibition-enforcement` accepts `check_kind: node-test`, injects `GSD_PROHIB_SUBJECT`, requires a non-vacuous red on `check_violation_fixture`, and optionally requires green on `check_clean_fixture`.
- Normal guard execution reads real repository artifacts; injected execution reads the fixture JSON fact shape. The same three named assertions run in both modes.
- All three prohibition items deliberately share one guard and fixture pair because the protected authority/secret boundary is one coupled host composition; each item retains its own statement/category/status.
</interfaces>
</context>

## Artifacts this phase produces

- `scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs` — three named node:test assertions and the normal-repository/injected-fixture subject adapter.
- `test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json` — known-bad fact subject that violates all three assertions.
- `test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json` — known-clean control satisfying all three assertions.
- No new production symbols or product/API/schema surface.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove all three prohibitions red-first and green on shipped artifacts</name>
  <reversibility rating="reversible">The guard and fixtures are internal verification artifacts and can be replaced without changing runtime behavior or persisted data.</reversibility>
  <files>scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs, test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json, test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json, test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs</files>
  <read_first>
    - Read `scripts/ci/prohibitions/_lib.mjs` and two existing `p*.test.mjs` guards with violation and clean fixtures for subject injection, comment stripping, non-vacuity, and node:test naming conventions.
    - Read the four Plan 08 target artifacts at their committed state plus `test/example/lib/example/accounts/crosswake_session_adapter.ex` and its personal-account test assertions.
    - Read the full Phase 240.3 source contract before adding guard/fixture/descriptors markers; preserve its exact-SHA, no-external-API, host-only, and historical-receipt assertions.
    - Read `/Users/jon/.codex/gsd-core/bin/lib/prohibition-enforcement.cjs` node-test fail-first/clean-control execution path only as needed to match the request and TAP requirements.
  </read_first>
  <behavior>
    - The injected bad JSON makes three distinctly named node tests fail through assertions rather than load/parse crashes.
    - The injected clean JSON makes the identical three tests pass, proving the red result is caused by violating content.
    - Normal execution derives facts from shipped source/test artifacts, asserts a realistic marker floor for every file, and passes only when personal org remains nil, verifier transport stays encrypted/HttpOnly and URL-free, and callback input cannot choose authority/navigation.
    - The phase contract names the guard/fixtures and all five flat descriptor scalars, preventing a green runtime suite with missing prohibition enforcement wiring.
  </behavior>
  <action>Create a single node:test guard using `node:assert/strict` and the repository's `scripts/ci/prohibitions/_lib.mjs` helpers. When `GSD_PROHIB_SUBJECT` is set, parse the injected JSON into an explicit boolean/string-count fact object and fail loudly on missing keys; otherwise read the committed endpoint, controller, adapter, controller test, and browser spec, strip comments where appropriate, and derive the same fact object. Add non-vacuity floors that require the exact endpoint session block, controller start/return functions, personal `org_id: nil` projection/evaluator assertion, exact callback-key assertion, fixed route/destination assertion, dynamic verifier negative assertions, and HttpOnly/encryption checks to be found before any prohibition can pass. Define three distinctly named tests matching authority-integrity, secret-boundary, and authority-smuggling. The bad fixture must parse successfully and violate all three; the clean fixture must parse successfully and satisfy all three. Do not make the fixture path itself trigger failure, and do not let comments satisfy source assertions. Extend `Phase2403HostedCrosswakeRuntimeTest` with exact path/descriptor and runner-marker expectations only; Plan 10 owns runner/receipt mutation. Preserve the two unresolved edge assumptions and do not label them resolved. Verify the real guard, explicit bad red, explicit clean green, and three `check prohibition-enforcement` invocations built from the frontmatter descriptors; every result must report green, located, failFirst, passed, and unflagged.</action>
  <verify>
    <automated>bash -lc 'node --test scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs &amp;&amp; ! GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json node --test scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs &amp;&amp; GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json node --test scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs &amp;&amp; for category in authority-integrity secret-boundary authority-smuggling; do request=$(jq -nc --arg category "$category" '\''{prohibition:{statement:$category,status:"resolved",verification:"test"},check:{kind:"node-test",target:"scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs",violationFixture:"test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json",cleanFixture:"test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json"},mode:"autonomous"}'\''); node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check prohibition-enforcement --json "$request" | jq -e '\''(.status == "green") and (.flagged == false) and (.located == true) and (.evidence[0].failFirst == true) and (.evidence[0].passed == true)'\''; done'</automated>
  </verify>
  <acceptance_criteria>
    - Normal guard execution reports exactly three passing named tests against the real repository and fails loudly if any source extraction is empty or below its marker floor.
    - The bad fixture produces at least three assertion failures with no parse/load crash; the clean control produces three passes.
    - For each of the three frontmatter prohibitions, `gsd-tools check prohibition-enforcement` returns `status: green`, `flagged: false`, `located: true`, `failFirst: true`, and `passed: true` using only the authored descriptor.
    - The phase source contract pins guard, violation fixture, clean fixture, node-test kind, and all three prohibition categories without changing the current receipt before Plan 10.
    - No production, schema, dependency, API, core, generator, browser configuration, or documentation scope is added.
  </acceptance_criteria>
  <done>All three XW must-NOT statements have one non-vacuous, content-caused, fail-first-proven mechanical enforcement surface; the separate XW edge assumptions remain unresolved and visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shipped runtime/tests -> guard facts | Source extraction can silently become vacuous or accept comments instead of behavior. |
| violation/clean fixtures -> fail-first producer | Fixtures must prove content-caused red/green rather than parse failure or environment presence. |
| plan descriptor -> verifier | Missing or partial flat scalars would make enforcement unlocatable and must fail closed. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-09-01 | Spoofing | pass-shaped but vacuous guard | high | mitigate | Exact file/marker floors, comment stripping, and named assertions block empty extraction. |
| T-240.3-09-02 | Tampering | fail-first fixture | high | mitigate | Valid known-bad JSON triggers assertion reds; known-clean control proves causation. |
| T-240.3-09-03 | Repudiation | prohibition disposition | high | mitigate | Complete flat descriptors and producer verdicts replace narrative claims with machine evidence. |
| T-240.3-09-04 | Information Disclosure | test output | high | mitigate | Guard asserts structure/markers only and never prints dynamic verifier, cookie, session, or binding values. |
| T-240.3-09-05 | Elevation of Privilege | authority guard drift | high | mitigate | One coupled guard checks personal nil-org, fixed authority/navigation, and secret transport together. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Deterministic safe hosted runtime | 09 | COVERED | Makes the three must-NOT boundaries machine-enforceable. |
| REQ | XW-01 | Personal projection and secret confidentiality | 09 | COVERED | Authority-integrity and secret-boundary checks are test-tier. |
| REQ | XW-02 | Callback evidence cannot grant/select authority | 09 | COVERED | Authority-smuggling check is test-tier. |
| RESEARCH | — | Redaction, fixed route, personal org, fail-closed evidence | 09 | COVERED | Real source/test markers plus fail-first fixtures. |
| CONTEXT | D-01–D-17 | Locked boundaries | 09 | COVERED | Enforcement observes the implemented decisions without widening them. |
| CONTEXT | Deferred ideas | Generator/public abstraction/network/provider/device work | — | EXCLUDED | Explicitly deferred. |

API coverage detector: existing `COVERAGE.md` remains unchanged and no API surface is created. Assumption-delta detector: prior `detected: true` pluralization signals remain recorded in Plan 08; this verification-only plan introduces no additional architectural assumption. Schema-push detector: no qualifying ORM pattern or schema file is touched.

<verification>
Run the node guard against the shipped repository, known-bad subject, and known-clean control, then run the phase source contract and all three producer checks. A direct `node --test` pass without machine-proven fail-first is insufficient. Every high-severity threat blocks completion.
</verification>

<success_criteria>
- Three prohibition assertions are non-vacuous on real artifacts and content-sensitive across bad/clean fixtures.
- Verifier descriptors are complete and independently produce green, unflagged enforcement verdicts.
- XW-01/XW-02 edge assumptions remain visibly unresolved rather than being conflated with prohibition enforcement.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-09-SUMMARY.md` when done.
</output>
