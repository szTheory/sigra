---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "01"
subsystem: hosted crosswake runtime
tags: [phoenix, ecto, crosswake, continuation, session-security]
requires:
  - phase: 239-hosted-session-interop
    provides: CrosswakeSessionAdapter fresh-resolution and evaluator boundary
provides:
  - Digest-only, one-time host continuation persistence for the example app
  - Authenticated start and browser-only return routes around the released adapter
  - A real-router same-session Crosswake runtime tracer
affects: [240.3-02, 240.3-03, XW-01, XW-02]
tech-stack:
  added: []
  patterns: [host-owned continuation context, atomic Ecto claim, fixed-route callback orchestration]
key-files:
  created:
    - test/example/priv/repo/migrations/20260811170000_create_crosswake_continuations.exs
    - test/example/lib/example/accounts/crosswake_continuation.ex
    - test/example/lib/example/accounts/crosswake_continuations.ex
    - test/example/lib/example_web/controllers/crosswake_controller.ex
  modified:
    - test/example/config/config.exs
    - test/example/lib/example_web/router.ex
    - test/example/test/example_web/controllers/crosswake_controller_test.exs
key-decisions:
  - "Persist only SHA-256 correlation digests and opaque adapter binding values; never retain a session credential or raw callback value."
  - "Keep the fixed Crosswake route and /app destination inside the host context, not callback input."
  - "Use a 303 start/return flow with the existing CrosswakeSessionAdapter as the sole authority and evaluator seam."
patterns-established:
  - "Continuation claim: conditional update by digest, unused status, and expiry before any callback correlation or evaluator call."
  - "Callback input: accept exactly scalar continuation, state, and PKCE verifier values; build the released AuthReturn envelope server-side."
metrics:
  duration: 18m
  completed_date: 2026-08-11
  tasks_completed: 1
  files_changed: 7
status: complete
---

# Phase 240.3 Plan 01: Hosted Crosswake Runtime Flow Summary

One real authenticated example-host request now travels through a digest-only continuation, the existing Crosswake adapter, and a safe `303 /app` return.

## Tasks Completed

1. **Prove one authenticated start/return path end to end**
   - Added the RED real-router tracer, then implemented the migration, continuation schema/context, controller, routes, TTL, and Phoenix log filtering.
   - The start route is authenticated and CSRF-protected; the browser-only return route atomically claims the continuation, validates state/PKCE digests, and supplies only a server-built released envelope to the existing adapter.

## Verification

- `set -a; source tmp/db.env; set +a; cd test/example && MIX_ENV=test mix ecto.create --quiet && MIX_ENV=test mix ecto.migrate --quiet && mix test test/example_web/controllers/crosswake_controller_test.exs --only crosswake_runtime_tracer` — passed.
- `cd test/example && mix test test/example_web/controllers/crosswake_controller_test.exs --include example_app` — passed (2 tests).
- `cd test/example && mix format --check-formatted ...` for all task files — passed.
- `mix precommit` was attempted and correctly exposed an unrelated pre-existing verified-route warning for `/dev/mailbox`, which the project's warnings-as-errors policy turns into a failure. The task's focused checks remain green.

## TDD Gate Compliance

- RED: `a39ad0d5` — authenticated start returned 404 before the routes/runtime consumer existed.
- GREEN: `5961cc1f` — real router tracer passes with a same-session `303` flow and one personal (`org_id: nil`) evaluator projection.

## Deviations from Plan

### Auto-fixed Issues

1. **[Rule 1 - Bug] Avoided assuming `Repo.update_all/3` returns records**
   - **Found during:** Task 1 GREEN verification.
   - **Issue:** PostgreSQL's update result did not provide the claimed continuation row in this runtime.
   - **Fix:** Keep the atomic conditional update, then fetch the now-claimed row by its unique digest for evidence construction.
   - **Files modified:** `test/example/lib/example/accounts/crosswake_continuations.ex`
   - **Commit:** `5961cc1f`

2. **[Rule 1 - Bug] Passed a server-built map to the adapter evidence validator**
   - **Found during:** Task 1 GREEN verification.
   - **Issue:** The released adapter revalidates map/keyword inputs and cannot enumerate an `AuthReturn.Envelope` struct directly.
   - **Fix:** Validate the assembled envelope once, then pass `Map.from_struct/1` to the unchanged adapter.
   - **Files modified:** `test/example/lib/example/accounts/crosswake_continuations.ex`
   - **Commit:** `5961cc1f`

## Known Stubs

None.

## Self-Check: PASSED

- All seven task files exist at their planned paths.
- Both task commits exist: `a39ad0d5`, `5961cc1f`.
