---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "08"
subsystem: hosted-crosswake-runtime-security
tags: [crosswake, pkce, phoenix, session, playwright]
requires:
  - phase: 240.3-07
    provides: bounded hosted runtime proof runner
provides:
  - PKCE verifier transport confined to encrypted HttpOnly host session state
  - Secure-by-default host cookie policy with a test-only loopback exception
affects: [XW-01, XW-02]
tech-stack:
  added: []
  patterns: [continuation-bound-session-transport, exact-expiry, consume-before-complete]
key-files:
  created: []
  modified:
    - test/example/lib/example_web/controllers/crosswake_controller.ex
    - test/example/lib/example/accounts/crosswake_continuations.ex
    - test/example/lib/example_web/endpoint.ex
    - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
key-decisions:
  - Store the PKCE verifier only in a continuation-bound encrypted host session entry; callback URLs carry continuation and state only.
  - Secure cookies default to true outside MIX_ENV=test; the deterministic loopback browser harness is the sole Secure=false exception.
metrics:
  duration: 18m
  completed_date: 2026-08-11
  tasks_completed: 2
  files_changed: 8
status: complete
---

# Phase 240.3 Plan 08: Confidential Crosswake Verifier Transport Summary

**The hosted Crosswake return now carries only opaque continuation and state URL parameters; the PKCE verifier is encrypted, HttpOnly session transport consumed before completion.**

## Accomplishments

- Configured the example host session with signing, encryption, HttpOnly, SameSite=Lax, and a compile-time Secure default; `test.exs` is the only false override.
- Returned the persisted continuation expiry from issuance and stored handle-bound verifier entries in the host session, pruning and removing entries before completion.
- Updated router and Chromium proofs to assert the two-key callback and observed cookie policy without exposing cookie or verifier values.
- Ran the browser server under `MIX_ENV=test` on its selected loopback port with the narrowly gated fixture seed path.

## Verification

- `cd test/example && MIX_ENV=test mix test test/example/accounts/crosswake_continuations_test.exs test/example_web/controllers/crosswake_controller_test.exs --include example_app` — passed: 14 tests, 0 failures.
- `scripts/ci/hosted-session-interop-proof.sh --browser-only` — passed: Chromium observed the test-only cookie policy and completed the real hosted callback into `/app`.
- `git diff --check` — passed before task completion.

## Task Commits

1. Task 1 RED — `1e35fa0f` test contract for cookie policy.
2. Task 1 GREEN — `20d40eff` secure encrypted host session and test harness policy.
3. Task 2 RED — `6926e1c1` two-key private verifier transport contract.
4. Task 2 GREEN — `15979904` continuation-bound one-time session verifier transport.
5. Rule 1 fixes — `c6712eb9`, `f49ebfc0` loopback port and rendered-destination readiness corrections.

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 3 - Blocking issue] Enabled the bounded test browser fixture seed path.
- **Found during:** Task 1 browser proof.
- **Issue:** `seeds.exs` correctly rejects ordinary `MIX_ENV=test` execution, preventing the explicitly required test-harness seed step.
- **Fix:** Allow only `SIGRA_BROWSER_PROOF=true` in the bounded proof runner; all ordinary test seeding remains denied.
- **Files modified:** `test/example/priv/repo/seeds.exs`, `scripts/ci/hosted-session-interop-proof.sh`.

2. [Rule 1 - Bug] Honored the selected loopback port in test configuration and used DOM readiness for the non-LiveView destination.
- **Found during:** Task 1 browser proof.
- **Fix:** Test endpoint now consumes its scoped `PORT`; browser proof waits for the stable rendered application hook rather than a LiveView socket absent from this destination.
- **Files modified:** `test/example/config/test.exs`, `test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts`.

## Known Stubs

None.

## Self-Check: PASSED

- Confirmed all modified host artifacts exist and commits `1e35fa0f`, `20d40eff`, `6926e1c1`, `15979904`, `c6712eb9`, and `f49ebfc0` resolve in Git history.
