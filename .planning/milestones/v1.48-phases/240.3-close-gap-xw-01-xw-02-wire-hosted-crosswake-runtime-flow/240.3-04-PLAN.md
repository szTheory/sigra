---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "04"
type: execute
wave: 4
depends_on: ["240.3-03"]
files_modified:
  - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
  - test/example/priv/playwright/playwright.config.ts
  - scripts/ci/hosted-session-interop-proof.sh
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-14, a real Chromium cookie jar authenticates, starts the host continuation, follows the local return, and lands at the fixed safe destination."
    - "Per D-09/D-11/D-14, the final browser URL and DOM contain no raw handle, evidence value, binding, token, or database identifier."
    - "The browser proof is serial, retry-free, readiness-driven, credential-free, and makes no network Crosswake/provider/device claim."
  artifacts:
    - path: "test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts"
      provides: "Focused real-cookie-jar hosted runtime journey"
    - path: "test/example/priv/playwright/playwright.config.ts"
      provides: "Dedicated crosswake-hosted-runtime Chromium partition"
      contains: "crosswake-hosted-runtime"
    - path: "scripts/ci/hosted-session-interop-proof.sh"
      provides: "Bounded --browser-only server/migration/readiness/test mode"
  key_links:
    - from: "test/example/priv/playwright/playwright.config.ts"
      to: "test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts"
      via: "exclusive testMatch for crosswake-hosted-runtime project"
      pattern: "crosswake-hosted-runtime"
    - from: "scripts/ci/hosted-session-interop-proof.sh"
      to: "test/example/priv/playwright/playwright.config.ts"
      via: "SIGRA_EXAMPLE_URL and dedicated project invocation after dev migration/readiness"
      pattern: "--project=crosswake-hosted-runtime"
---

<objective>
Prove the hosted runtime through a real browser cookie jar and clean final navigation.

Purpose: Add the one browser-level signal D-14 requires while leaving hostile security breadth in deterministic ConnCase tests.
Output: Focused Playwright spec/project and a bounded browser-only proof mode that boots the existing example host.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@test/example/AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-CONTEXT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-RESEARCH.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-PATTERNS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-03-SUMMARY.md

<interfaces>
- Browser start route: authenticated CSRF-protected `POST /crosswake/start`; return redirects through `/crosswake/return?continuation=...&amp;state=...&amp;pkce_verifier=...`; final success URL is `/app`.
- Existing deterministic dev persona: `alice@demo.tasklane.test` / `AliceDemoPass1!`; authenticate through rendered `/users/log_in` controls.
- Existing config invariants: `fullyParallel: false`, `workers: 1`, `retries: 0`, and `SIGRA_EXAMPLE_URL` base URL.
- Proof runner browser mode must migrate the dev schema, start `PHX_SERVER=true MIX_ENV=dev` on a validated local free port, wait by bounded readiness request, run one project, and always terminate the server.
</interfaces>
</context>

## Artifacts this phase produces

- `test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts` — one cookie-jar departure/return/final-redaction journey.
- `test/example/priv/playwright/playwright.config.ts` — `crosswake-hosted-runtime` project and exclusive matcher.
- `scripts/ci/hosted-session-interop-proof.sh` — bounded `--browser-only` boot/readiness/browser mode, later composed into the receipt path by Plan 05.
- Final recipe, contract, API declaration, and exact-SHA receipt artifacts are completed in Plan 05.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive the real hosted return with a deterministic browser</name>
  <files>test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts, test/example/priv/playwright/playwright.config.ts, scripts/ci/hosted-session-interop-proof.sh</files>
  <read_first>
    - All three target paths; confirm the new spec is absent, read the full config, and read the current proof runner before editing.
    - `test/example/priv/playwright/tests/golden-path.spec.ts` for LiveView readiness and cookie-jar navigation conventions.
    - `test/example/priv/playwright/tests/demo-showcase.spec.ts` login helper around `alice@demo.tasklane.test`, translating interactions to role/label selectors where available.
    - `scripts/ci/generated-auth-runtime-proof.sh` function `boot_and_run_spec` for dev migration, `PHX_SERVER=true`, readiness, server log, and cleanup precedent.
    - `test/example/config/dev.exs` and `runtime.exs` for `PORT`/server configuration.
    - `test/example/lib/example_web/controllers/crosswake_controller.ex` and its complete test to consume the real route/response contract rather than adding browser-only behavior.
  </read_first>
  <behavior>
    - The browser signs in through labeled email/password inputs and a named Log in button, preserving a real host cookie jar.
    - The start POST obtains/uses the rendered CSRF token, receives 303 to the local GET carrying generated state and PKCE verifier correlation, and follows the actual POST -> GET -> 303 chain without direct context calls or cookie injection.
    - The final location is exactly the authenticated `/app` destination and a stable accessible app landmark/heading is visible.
    - Captured intermediate handle/evidence sentinels are absent from the final URL and page content; token/binding/database identifier vocabulary is also absent.
    - The dedicated project executes exactly this spec with one worker and zero retries; no timeout sleeps or external network service is used.
  </behavior>
  <action>Create exactly one test named `hosted Crosswake local return preserves the real cookie jar and clears correlation material`. Log in as the seeded Alice persona using `getByLabel`/`getByRole` scoped to the rendered password form. From that authenticated document, create and submit a transient ordinary POST form to `/crosswake/start` containing the rendered meta CSRF token; this is a navigation through the endpoint, not a direct context call or cookie injection. Register `page.waitForRequest` for pathname `/crosswake/return` before submission, require its method GET, and parse its URL only in memory to capture the exact `continuation`, `state`, and `pkce_verifier` query values generated by the server. Let Chromium's cookie jar follow the POST 303 to that GET and the GET 303 to `/app`; do not issue or reconstruct the return request in test code. Assert the final URL pathname is exactly `/app`, a stable accessible app landmark/heading is visible, and handle/state/verifier plus `return_ref`, authority, route, destination, binding, token, and database-identifier sentinels are absent from final URL and DOM. Never print, attach, screenshot, or trace the intermediate URL/values per D-09/D-11/D-14. Do not add UI, browser-side session mutation, direct Accounts calls, sleeps, retries, provider doubles, or negative-security duplication from Plan 03. Add a dedicated `crosswake-hosted-runtime` Chromium project with exclusive `testMatch`, retaining global serial/one-worker/zero-retry settings. Extend the existing proof runner with a `--browser-only` mode modeled on the generated-auth runner: source `tmp/db.env`, explicitly apply `MIX_ENV=dev mix ecto.migrate --quiet`, choose/validate a local port, start the example host with `PHX_SERVER=true MIX_ENV=dev`, wait through bounded curl readiness rather than sleeps, invoke the repository-managed Playwright CLI with `SIGRA_EXAMPLE_URL` and the dedicated project, capture redacted diagnostics on failure, and terminate the server via trap. Preserve the runner's default Phase 239 path until Plan 05 composes the new full receipt flow.</action>
  <acceptance_criteria>
    - The spec contains one test and uses role/label selectors for rendered login/final-state interactions, a stable readiness signal, and no fixed delays or retry overrides.
    - The named test uses a real authenticated cookie jar and observes the actual POST 303 -> local GET with state/PKCE -> 303 route chain; no test-only controller endpoint, direct context call, reconstructed return request, or manual cookie insertion exists.
    - The final URL is `/app`; dynamic handle/evidence values and sensitive vocabulary are absent from URL and DOM.
    - `crosswake-hosted-runtime` matches only the new spec and inherits `workers: 1`, `fullyParallel: false`, and `retries: 0`.
    - `--browser-only` applies the dev migration before server boot and exits nonzero on readiness, browser, or cleanup failure without external network calls.
  </acceptance_criteria>
  <verify>
    <automated>scripts/ci/hosted-session-interop-proof.sh --browser-only</automated>
  </verify>
  <done>A deterministic real-browser session completes the in-process hosted flow and proves the final navigation/DOM disclosure boundary without widening the phase into provider or device E2E.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| rendered login/start -> browser cookie jar | Browser owns the real authenticated host session and CSRF token. |
| browser -> return route | Intermediate handle/evidence URL is untrusted and transient. |
| return route -> final DOM/location | Sensitive transient data must be eliminated by fixed 303 navigation. |
| browser runner -> evidence seal | Browser success becomes one ordered input to Plan 05 receipt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-01 | Elevation of Privilege | replay/race | high | mitigate | Browser covers one winner; Plan 02 owns concurrent/replay proof and Plan 05 requires both commands. |
| T-240.3-02 | Spoofing | browser account/session | high | mitigate | Rendered login creates a real cookie; Plan 03 remains the switch matrix. Browser never injects a cookie. |
| T-240.3-03 | Tampering | route/destination | high | mitigate | Browser follows server response chain and asserts exact `/app`; Plan 03 proves hostile inputs cannot influence it. |
| T-240.3-04 | Information Disclosure | intermediate URL/final URL/DOM/artifacts | high | mitigate | Dynamic sentinel assertions, no value logging/attachments, clean final route and DOM. Blocks on any observed value. |
| T-240.3-05 | Repudiation | false exact-SHA browser claim | high | mitigate | Dedicated no-retry project is invoked by the bounded runner and later recorded only after its zero exit status. |

## OWASP ASVS 4.0.3 Level 1 Mapping

| ASVS family | Plan control | Executable acceptance / evidence |
|---|---|---|
| V2 Authentication | Rendered password login establishes the real browser session; no cookie injection is permitted. | Named Playwright test uses labeled controls and must land authenticated at `/app`, blocking T-240.3-02. |
| V3 Session Management | One cookie jar follows the authenticated POST, local return GET, and final redirect. | Observed POST 303 -> GET -> 303 chain and exact final pathname provide browser evidence for T-240.3-01/T-240.3-02. |
| V4 Access Control | Browser cannot choose evaluator route or destination. | Exact `/app` assertion complements Plan 03 hostile-route tests and blocks T-240.3-03. |
| V5 Validation | Browser transports only the server-generated state/PKCE values and does not reconstruct the return request. | Waited return request must be GET with both values; dedicated project fails on protocol drift. |
| V6 Cryptography | Opaque correlation values remain transient and absent from final browser artifacts. | Dynamic handle/state/verifier negative URL/DOM assertions and no trace/screenshot rule block T-240.3-04. |
</threat_model>

<verification>
Use the committed browser-only runner so dev migration, server readiness, exact Playwright project, and cleanup are one deterministic command. No manual UAT or already-running local server is accepted.
</verification>

<success_criteria>
- A real cookie-jar journey closes the final response/navigation segment of the audit flow.
- Final browser state is free of transient handle/evidence/authority material.
- The evidence truthfully remains an in-process reference-host proof only.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-04-SUMMARY.md` when done.
</output>
