---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "03"
subsystem: api
tags: [phoenix, conncase, crosswake, session-security, telemetry]
requires:
  - phase: 240.3-02
    provides: Database-backed one-time Crosswake continuation claims and canonical return evidence validation.
provides:
  - Router-dispatched Crosswake return denial matrix for session changes, replay, and input smuggling.
  - Fixed generic recovery redirects and redacted continuation telemetry.
affects: [crosswake-runtime-proof, hosted-session-interop]
tech-stack:
  added: []
  patterns: [Thin controller response mapping, telemetry metadata allowlist, real-router ConnCase security matrix]
key-files:
  created: []
  modified:
    - test/example/lib/example_web/controllers/crosswake_controller.ex
    - test/example/test/example_web/controllers/crosswake_controller_test.exs
key-decisions:
  - "Only session-unavailable returns redirect to sign-in; every other terminal return restarts at the fixed local root."
  - "Continuation telemetry emits only correlation_ref, outcome, and reason metadata."
patterns-established:
  - "Crosswake return tests preserve the browser session from POST start through GET return and assert evaluator non-invocation for denials."
requirements-completed: [XW-01, XW-02]
coverage:
  - id: D1
    description: "Real Phoenix routes allow only a same-session Crosswake return and fail closed for state, session, replay, and smuggling cases."
    requirement: XW-02
    verification:
      - kind: integration
        ref: "test/example/test/example_web/controllers/crosswake_controller_test.exs"
        status: pass
    human_judgment: false
  - id: D2
    description: "Controller redirects and continuation telemetry retain the fixed disclosure boundary."
    requirement: XW-01
    verification:
      - kind: integration
        ref: "MIX_ENV=test mix test --include example_app test/example_web/controllers/crosswake_controller_test.exs"
        status: pass
    human_judgment: false
duration: 14min
completed: 2026-08-11
status: complete
---

# Phase 240.3 Plan 03: Hosted Crosswake Router Security Matrix Summary

**Real Phoenix Crosswake returns now allow only the departure session, route every terminal failure through generic recovery, and emit metadata-only telemetry.**

## Performance

- **Duration:** 14min
- **Completed:** 2026-08-11T17:06:19Z
- **Tasks:** 1/1
- **Files modified:** 2

## Accomplishments

- Added real-router ConnCase proof for missing, deleted, revoked, idle/absolute-expired, account-switched, replacement-session, replay, and malformed-return cases.
- Kept state/PKCE and authority/route/destination smuggling outside the evaluator boundary.
- Mapped unavailable sessions to sign-in guidance, all other failures to restart guidance, and constrained telemetry to an explicit metadata allowlist.

## Task Commits

1. **Task 1: Prove every hostile return fails closed through the router** - `92c399d9` (test), `0cf0e0ce` (feat)

## Files Created/Modified

- `test/example/lib/example_web/controllers/crosswake_controller.ex` - Fixed response mapping plus redacted continuation telemetry.
- `test/example/test/example_web/controllers/crosswake_controller_test.exs` - Router-dispatched hostile-return and disclosure matrix.

## Decisions Made

- Session absence or invalidation is the sole sign-in-recovery class; correlation, replay, binding, and smuggling failures use the generic restart recovery.
- Telemetry exposes only stable outcome/reason classes and a nil-safe correlation reference, never request data or session material.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Verification] Included the app-tagged test suite explicitly.**
- **Found during:** Task 1
- **Issue:** The plan command omits `--include example_app`, so Mix excluded all seven scoped ConnCase tests.
- **Fix:** Ran the equivalent migrated controller command with `--include example_app`.
- **Verification:** 7 tests passed.

---

**Total deviations:** 1 auto-fixed (Rule 3)
**Impact on plan:** Verification exercised the intended real-router suite; no product scope changed.

## Issues Encountered

- `MIX_ENV=test mix precommit` remains blocked by the pre-existing missing `/dev/mailbox` verified-route warning in `ExampleWeb.SettingsLive`; it is unrelated to these controller files. The scoped test suite passed with the same warning.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The router security matrix is ready for downstream browser, proof-runner, and receipt plans.
- The existing `/dev/mailbox` route warning should be repaired separately before a warnings-as-errors full precommit can pass.

## Self-Check: PASSED

- Confirmed both modified runtime artifacts exist.
- Confirmed task commits `92c399d9` and `0cf0e0ce` exist in git history.
