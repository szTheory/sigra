---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "06"
subsystem: hosted-crosswake-runtime-security
tags: [crosswake, phoenix, referrer-policy, playwright, security]
requires:
  - phase: 240.3-05
    provides: "Host-owned controller return flow and bounded browser proof runner"
provides:
  - "Callback-wide no-referrer response boundary for every Crosswake return redirect"
  - "Real-router and Chromium proof that the next /app document request has no Referer"
affects: [XW-01, XW-02, hosted-session-interop]
tech-stack:
  added: []
  patterns:
    - "Set response-level navigation privacy policy at the query-bearing controller boundary"
    - "Observe the actual next document request instead of inferring safety from the final URL"
key-files:
  created: []
  modified:
    - test/example/lib/example_web/controllers/crosswake_controller.ex
    - test/example/test/example_web/controllers/crosswake_controller_test.exs
    - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
key-decisions:
  - "Apply Referrer-Policy: no-referrer at the beginning of return/2 so validation failures and evaluator denials inherit the same boundary."
  - "Capture the real /app document navigation in Chromium and assert the Referer header is absent."
requirements-completed: [XW-01, XW-02]
coverage:
  - id: D1
    description: "All Crosswake callback redirects expose no-referrer while retaining their fixed destinations and generic recovery."
    requirement: XW-01
    verification:
      - kind: integration
        ref: "test/example/test/example_web/controllers/crosswake_controller_test.exs"
        status: pass
    human_judgment: false
  - id: D2
    description: "Chromium follows the real callback 303 and sends no Referer on the subsequent /app document request."
    requirement: XW-01
    verification:
      - kind: automated_ui
        ref: "scripts/ci/hosted-session-interop-proof.sh --browser-only"
        status: pass
    human_judgment: false
metrics:
  duration: 1m
  completed_date: 2026-08-11
  tasks_completed: 1
  files_changed: 3
status: complete
---

# Phase 240.3 Plan 06: Callback Referer Boundary Summary

**The query-bearing Crosswake callback now applies `Referrer-Policy: no-referrer` before every allow or recovery redirect, with real Chromium proof that `/app` receives no Referer.**

## Accomplishments

- Applied the response policy at the start of `CrosswakeController.return/2`, preserving the locked GET/303 flow and all existing evaluator and recovery behavior.
- Extended real-router assertions so successful, generic-denial, and sign-in-recovery callback responses all require exactly `referrer-policy: no-referrer`.
- Extended the serial cookie-jar Playwright journey to await the real `/app` document request and assert its `referer` header is absent.

## Verification

- `set -a; source tmp/db.env; set +a; cd test/example && mix test test/example_web/controllers/crosswake_controller_test.exs --include example_app` — passed: 7 tests, 0 failures.
- `scripts/ci/hosted-session-interop-proof.sh --browser-only` — passed: one serial, zero-retry Chromium test; the real `/app` document request carried no `referer` header.
- `cd test/example && mix precommit` — blocked by the pre-existing warnings-as-errors `/dev/mailbox` verified-route warning in `ExampleWeb.SettingsLive`; recorded in `deferred-items.md` because this plan does not touch that LiveView.

## Task Commits

1. **Task 1 RED: Add callback referrer boundary contract** — `9625dc4a` (test)
2. **Task 1 GREEN: Suppress Crosswake callback referrers** — `24bd1d65` (feat)

## Decisions Made

- Place the policy in `return/2`, before scalar input validation, rather than in `start/2` or individual allow/deny branches.
- Treat the browser request header as the security truth; a clean final URL and DOM alone are insufficient.

## Deviations from Plan

None - plan executed exactly as written.

## Deferred Issues

- The unrelated `ExampleWeb.SettingsLive` `/dev/mailbox` verified-route warning prevents the repository-wide example precommit alias from compiling with warnings-as-errors. The focused router and browser verification passed.

## Known Stubs

None.

## Self-Check: PASSED

- Confirmed all three modified host-only artifacts exist.
- Confirmed commits `9625dc4a` and `24bd1d65` exist in Git history.
