---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/config/config.exs
  - test/example/priv/repo/migrations/20260811170000_create_crosswake_continuations.exs
  - test/example/lib/example/accounts/crosswake_continuation.ex
  - test/example/lib/example/accounts/crosswake_continuations.ex
  - test/example/lib/example_web/controllers/crosswake_controller.ex
  - test/example/lib/example_web/router.ex
  - test/example/test/example_web/controllers/crosswake_controller_test.exs
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-01/D-02, one authenticated example-host request reaches the already-finished CrosswakeSessionAdapter through a database-backed continuation and returns to a fixed local destination."
    - "Per D-03/D-04/D-06, departure and return use the real Plug session and freshly resolved SIGRA backend session rather than cached scope or callback authority."
    - "Per D-05/D-07/D-08/D-09, the continuation stores only a digest and opaque expected binding, callback input cannot choose authority or navigation, and success ends with a 303 to /app."
  artifacts:
    - path: "test/example/lib/example/accounts/crosswake_continuation.ex"
      provides: "Host-owned digest-only continuation schema"
      contains: "defmodule Example.Accounts.CrosswakeContinuation"
    - path: "test/example/lib/example/accounts/crosswake_continuations.ex"
      provides: "Issue and complete operations around the existing adapter"
      exports: ["issue/3", "complete/5"]
    - path: "test/example/config/config.exs"
      provides: "Host-owned five-minute continuation TTL policy"
      contains: "crosswake_continuation_ttl_seconds"
    - path: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      provides: "Thin HTTP start and return orchestration"
      exports: ["start/2", "return/2"]
    - path: "test/example/test/example_web/controllers/crosswake_controller_test.exs"
      provides: "Real-router happy-path tracer"
      contains: "crosswake_runtime_tracer"
  key_links:
    - from: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      to: "test/example/lib/example/accounts/crosswake_continuations.ex"
      via: "start/2 and return/2 delegate application operations"
      pattern: "CrosswakeContinuations\.(issue|complete)"
    - from: "test/example/lib/example/accounts/crosswake_continuations.ex"
      to: "test/example/lib/example/accounts/crosswake_session_adapter.ex"
      via: "departure expected_binding/2 and return evaluate_return/5"
      pattern: "CrosswakeSessionAdapter\.(expected_binding|evaluate_return)"
    - from: "test/example/lib/example_web/router.ex"
      to: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      via: "authenticated POST start and browser-only GET return routes"
      pattern: "crosswake/(start|return)"
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The descriptor-less edge probe could not classify XW-01; this plan assumes the explicit adapter-to-runtime reachability and disclosure matrix is the required backstop."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The descriptor-less edge probe could not classify XW-02; this plan assumes replay, changed-session, and evidence-only denial tests are the required backstop."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT manufacture organization authority for a personal SIGRA session."
    - requirement_id: XW-01
      category: secret-boundary
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT turn the continuation, projection, or evidence receipt into a portable credential or identifier disclosure channel."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT let callback data select or reconstruct the session, subject, policy route, grant decision, or navigation destination."
---

<objective>
Wire one production-quality hosted Crosswake happy path through the example host without re-implementing the Phase 239 adapter.

Purpose: Close the architectural dead end first: prove `request/session -> continuation -> CrosswakeSessionAdapter -> evaluator -> 303 response` through the real router, Plug session, Ecto repository, and released in-process dependency per D-01 through D-09.
Output: Host-only continuation persistence, thin controller/routes, and a tagged ConnCase tracer for XW-01/XW-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@test/example/AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.48-MILESTONE-AUDIT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-CONTEXT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-RESEARCH.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-PATTERNS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-VALIDATION.md
@.planning/phases/239-hosted-session-interop/239-02-SUMMARY.md
@.planning/phases/239-hosted-session-interop/239-04-SUMMARY.md
@.planning/phases/239-hosted-session-interop/239-05-SUMMARY.md

<interfaces>
Existing authority seam — consume directly, do not copy:
- `Example.Accounts.CrosswakeSessionAdapter.expected_binding(raw_token, as_of) -> {:ok, %ExpectedBinding{session_ref, subject_ref, session_version}} | {:error, :session_unavailable}`
- `Example.Accounts.CrosswakeSessionAdapter.evaluate_return(raw_token, as_of, route_entry, expected_binding, return_input) -> {:allow, result} | {:deny, %{reason: reason}}`
- `ExampleWeb.ConnCaseHelpers.log_in_user(conn, user, opts \\ [])` writes the canonical `:user_token` Plug-session key.

New internal contracts fixed by this plan:
- `Example.Accounts.CrosswakeContinuations.issue(raw_token, as_of, opts \\ [])`
- `Example.Accounts.CrosswakeContinuations.complete(handle, raw_token, return_input, as_of, opts \\ [])`
- `issue/3` returns transient `%{handle, state, pkce_verifier}` values only to the controller; persistence retains the SHA-256 handle/state digests and S256 PKCE challenge digest plus a server-owned `return_ref`.
- The GET controller accepts exactly scalar `%{"continuation" => handle, "state" => state, "pkce_verifier" => verifier}`. It passes handle separately to `complete/5`; the context never passes the query map directly to Crosswake and constructs the released `AuthReturn` input only after digest validation.
- `ExampleWeb.CrosswakeController.start/2` and `return/2`
- `POST /crosswake/start` through `[:browser, :require_authenticated]`
- `GET /crosswake/return` through `[:browser]`, with exact filtered query keys `continuation`, `state`, and `pkce_verifier`
</interfaces>
</context>

## Artifacts this phase produces

- `test/example/priv/repo/migrations/20260811170000_create_crosswake_continuations.exs` — `Example.Repo.Migrations.CreateCrosswakeContinuations`, the host-only `auth.crosswake_continuations` table and claim indexes.
- `test/example/lib/example/accounts/crosswake_continuation.ex` — `Example.Accounts.CrosswakeContinuation`, `issue_changeset/1`, and `outcome_changeset/3`.
- `test/example/lib/example/accounts/crosswake_continuations.ex` — `Example.Accounts.CrosswakeContinuations`, `issue/3`, `complete/5`, `cleanup_expired/1`, and internal digest/claim/route/evidence helpers.
- `test/example/lib/example_web/controllers/crosswake_controller.ex` — `ExampleWeb.CrosswakeController.start/2` and `return/2`.
- `test/example/test/example/accounts/crosswake_continuations_test.exs` — digest, expiry, atomicity, concurrency, and sensitive-field test matrix.
- `test/example/test/example_web/controllers/crosswake_controller_test.exs` — tagged runtime tracer plus real-router denial/smuggling/redaction matrix.
- `test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts` — one deterministic cookie-jar journey.
- `test/example/priv/playwright/playwright.config.ts` — `crosswake-hosted-runtime` serial Chromium project.
- `test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` — phase runtime/recipe/proof source contract.
- `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/COVERAGE.md` — explicit no-external-API declaration.
- `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json` — receipt-last exact-SHA in-process proof.
- Updated `scripts/ci/hosted-session-interop-proof.sh` and `guides/recipes/b2c-alpha.md` — bounded proof and copyable host contract.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one authenticated start/return path end to end</name>
  <reversibility rating="costly">Changing the host-only persisted shape later requires a follow-on example migration, but it does not alter Sigra core, generated templates, or a published external contract.</reversibility>
  <files>test/example/config/config.exs, test/example/priv/repo/migrations/20260811170000_create_crosswake_continuations.exs, test/example/lib/example/accounts/crosswake_continuation.ex, test/example/lib/example/accounts/crosswake_continuations.ex, test/example/lib/example_web/controllers/crosswake_controller.ex, test/example/lib/example_web/router.ex, test/example/test/example_web/controllers/crosswake_controller_test.exs</files>
  <read_first>
    - Every target path above; confirm each new path is absent before creation and read `router.ex` completely before scoped editing.
    - `test/example/AGENTS.md` for migration generation, no nested modules, Ecto field ownership, and deterministic test rules.
    - `test/example/lib/example/accounts/crosswake_session_adapter.ex` for the only allowed expected-binding/evaluator seam.
    - `test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/auth_return.ex` and the adapter tests' `valid_return_envelope/0` for the exact released envelope/evidence keys, enums, forbidden fields, and validation boundary.
    - `test/example/lib/example/accounts/organization_auth_policy.ex` and `test/example/priv/repo/migrations/20260526043000_create_organization_auth_policies.exs` for auth-prefix schema conventions.
    - `test/example/lib/example_web/controllers/enterprise_sso_controller.ex` for thin start/return mapping and fixed recovery redirects.
    - `test/example/test/example_web/controllers/enterprise_sso_controller_test.exs`, `test/example/test/support/conn_case.ex`, and `test/example/test/support/conn_case_helpers.ex` for real-router/session proof.
    - `.planning/phases/239-hosted-session-interop/239-INTEROP-EVIDENCE.json` to preserve the already-completed adapter claims without presenting them as phase output.
  </read_first>
  <behavior>
    - RED: an authenticated real-router POST start followed by its returned GET handle path must fail before the routes/runtime consumer exist.
    - GREEN: departure calls `expected_binding/2`; generates independent 256-bit handle, state, and PKCE-verifier values; inserts only the SHA-256 handle/state digests and S256 PKCE challenge digest plus a server-owned opaque `return_ref`, opaque binding/version, issue/expiry timestamps, and fixed `crosswake-hosted-account` / `crosswake-hosted-return` route metadata; and never stores the raw transient values or session token.
    - GREEN: POST start returns 303 to `GET /crosswake/return?continuation=...&amp;state=...&amp;pkce_verifier=...`; the real Plug cookie jar carries the same host session, while all query values are filtered from logs and remain correlation evidence only.
    - GREEN: the first same-session GET atomically claims the row; constant-time validates the state and PKCE-verifier digests; server-assembles the exact released OAuth `AuthReturn` map; calls `evaluate_return/5`; and returns `303 See Other` to `/app`.
    - GREEN: the tagged test observes one evaluator invocation and `org_id: nil`, and the final location contains no handle or evidence value.
  </behavior>
  <action>Start with named test `crosswake_runtime_tracer: local AuthReturn state and PKCE correlation permits one same-session return`, then implement the complete thin slice. Per D-01 and D-17, keep every runtime artifact under `test/example`; do not touch root dependencies, `lib/sigra`, installer output, or generated templates. Run `mix ecto.gen.migration create_crosswake_continuations` as required by the example-host instructions, retain the generated convention, and normalize the generated filename to the exact planned path. Create the separate `CrosswakeContinuation` schema—never nest it in the context—with binary UUID, `auth` prefix, handle/state digests, S256 PKCE challenge digest, server-owned opaque `return_ref`, opaque refs, exact version, immutable route and return-route IDs, issue/expiry/consumption timestamps, and stable redacted outcome/correlation fields; omit raw transient values, session/user foreign keys, arbitrary URLs, provider data, and authority payloads per D-04/D-05. Set `config :example, :crosswake_continuation_ttl_seconds, 300`; `issue/3` reads it by default while fixed-clock tests may inject TTL. Generate independent 32-byte handle, state, and PKCE verifier values; SHA-256 digest handle/state and derive/store the verifier's S256 challenge before insert. Fix the internal evaluator route to `%RouteEntry{id: "crosswake-hosted-account", path: "/app", runtime: :phoenix, auth_min_level: :password, requires_recent_auth: 600, auth_posture: :strict_recent}`, return route ID to `"crosswake-hosted-return"`, and destination to `/app`.

Make POST start extract canonical `:user_token`, call `issue/3`, and respond 303 to `/crosswake/return` carrying only scalar `continuation`, `state`, and `pkce_verifier` query values. Extend Phoenix parameter filtering with those exact names while preserving all existing secret filters, and add a log-capture assertion so D-11 forbids raw correlation values in structured/request logs. Make GET return reject missing/duplicate/list/malformed correlation, pass the continuation handle separately to `complete/5`, atomically claim by handle digest before constant-time state/PKCE comparison, and map mismatch to `oauth_state_or_pkce_failure` with zero evaluator calls. After correlation passes, server-assemble—never merge request params—the exact `AuthReturn.new_envelope/1` input: `typ: "sigra.auth_return"`, stored `return_ref`, `version: "1"`, `issuer: "example-local-reference"`, `audience: "example"`, `kind: :oauth`, stored route IDs, `transport: :http_callback`, stored issue/expiry timestamps, `replay_posture: :server_record_required`, `link_verification: :not_applicable`, `validation_posture: %{"state" =&gt; "matched", "pkce" =&gt; "verified"}`, and evidence `%{provider_kind: :local_reference, state: :matched, pkce: :verified, redirect: :verified, replay: :not_seen}`. Pass only that server-built map, the stored expected binding, fixed `%RouteEntry{}`, fresh raw cookie, and test evaluator option to unchanged `evaluate_return/5`. On allow, return 303 to `/app`; generic recovery stays server-owned per D-02/D-06 through D-10. Add POST start behind `[:browser, :require_authenticated]` and GET return behind `[:browser]` without the authentication redirect per D-03. Do not add a LiveView flow, evaluator Plug, network Crosswake call, success interstitial, or new branded/admin surface.</action>
  <acceptance_criteria>
    - The migration creates `auth.crosswake_continuations` with unique handle digest lookup, state/PKCE digest correlation, server-owned return reference, and no foreign key, URL, raw-token, raw-handle/state/verifier, provider, or evidence-body columns.
    - `CrosswakeContinuations.issue/3` and `complete/5` are the only application operations called by the controller; the controller contains no binding derivation, evaluator construction, or callback-selected route/destination logic.
    - The start route is a CSRF-protected authenticated POST; the return route uses the browser pipeline without `:require_authenticated`.
    - Phoenix parameter filtering covers `continuation`, `state`, and `pkce_verifier`; captured request/telemetry logs contain none of their dynamic values.
    - The named tagged ConnCase test dispatches POST start then local GET return with the generated correlation values, asserts the exact server-built released `AuthReturn.Envelope`, observes exactly one evaluator call with personal `org_id: nil`, receives both 303 responses, and resolves only to `/app`.
    - Root `mix.exs`, `lib/sigra`, `priv/templates`, and installer-generated artifacts remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>set -a; source tmp/db.env; set +a; cd test/example &amp;&amp; MIX_ENV=test mix ecto.create --quiet &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet &amp;&amp; mix test test/example_web/controllers/crosswake_controller_test.exs --only crosswake_runtime_tracer</automated>
  </verify>
  <done>A real authenticated host request can start and complete one same-session Crosswake continuation through the existing adapter and safely land on `/app`; this is the first runtime caller and does not claim the Phase 239 adapter as new work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser -> Phoenix routes | Untrusted session state, handle, and return parameters enter the host. |
| Phoenix context -> PostgreSQL | One-time continuation authority is persisted and atomically claimed. |
| host authority -> Crosswake evaluator | Only freshly validated fact projection may cross into the existing in-process evaluator. |
| proof runner -> evidence receipt | Repository behavior is converted into an exact-SHA claim in Plan 05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-01 | Elevation of Privilege | continuation claim | high | mitigate | D-04/D-06: unique digest plus one conditional unconsumed/unexpired update; Plan 02 proves race and replay behavior. Blocks completion if absent. |
| T-240.3-02 | Spoofing | departure/return session | high | mitigate | D-04/D-07: persist departure binding and pass it to the adapter's fresh return-time lookup; Plan 03 proves account and same-account session switches deny. Blocks completion if absent. |
| T-240.3-03 | Tampering | return parameters and navigation | high | mitigate | D-07/D-08: fixed record-owned route and `/app` destination; public AuthReturn validation and hostile ConnCase matrix reject smuggled authority. Blocks completion if absent. |
| T-240.3-04 | Information Disclosure | storage, response, telemetry, evidence | high | mitigate | D-05/D-09/D-11: digest-only row, generic redirect/flash, redacted metadata, and negative storage/DOM/receipt assertions. Blocks completion if any sensitive value is observed. |
| T-240.3-05 | Repudiation | exact-SHA proof | high | mitigate | D-15: Plan 05 commits guards before running the bounded suite and emits the receipt last with the tested HEAD SHA and ordered zero-status commands. Blocks completion on mismatch. |

## OWASP ASVS 4.0.3 Level 1 Mapping

| ASVS family | Plan control | Executable acceptance / evidence |
|---|---|---|
| V2 Authentication | The authenticated POST reads the canonical host session and the return freshly re-resolves it. | Tracer acceptance requires real `log_in_user/3`, both real routes, one evaluator call, and `org_id: nil`; focused ConnCase command blocks T-240.3-02. |
| V3 Session Management | Departure binding, exact version, five-minute expiry, and one-time digest claim bind the return to the originating session. | Named tracer asserts POST/GET cookie continuity and 303; Plans 02-03 add replay/switch denials for T-240.3-01/T-240.3-02. |
| V4 Access Control | Route entry and `/app` destination are server constants; evaluator runs only after host gates. | Captured route must be `crosswake-hosted-account` and final location `/app`; hostile route/destination coverage in Plan 03 blocks T-240.3-03. |
| V5 Validation | GET accepts scalar state/PKCE only and constructs the released AuthReturn allowlist without merging request keys. | Named local AuthReturn tracer asserts exact envelope; Plan 03 named smuggling tests and evaluator non-invocation block T-240.3-03. |
| V6 Cryptography | Independent 256-bit random values are SHA-256 digested; correlation comparisons are constant-time. | Migration/schema acceptance forbids raw values; Plan 02 persisted-row and mismatch tests block T-240.3-01/T-240.3-04. |
</threat_model>

<verification>
Apply the example-host test schema explicitly, run the tagged tracer through the endpoint, and confirm no root/public/generated file changed. The normal `mix test` alias also migrates, but the plan command names `mix ecto.migrate` so the schema dependency is visible and ordered.
</verification>

<success_criteria>
- XW-01 is runtime-reachable through a backend-validated personal session and retains `org_id: nil` without secret disclosure.
- XW-02 has one real continuation binding and single-use claim on the happy path; expansion plans close all hostile cases.
- The already-completed adapter remains the sole authority projection/evaluator boundary.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-01-SUMMARY.md` when done.
</output>
