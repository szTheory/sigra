---
phase: 236-closeout-evidence-reconciliation
plan: 05
type: execute
wave: 5
depends_on: [236-04]
files_modified:
  - scripts/planning/phase-236-audit-snapshot.exs
  - scripts/planning/phase-236-audit-snapshot-test.exs
  - .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json
  - .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json
  - .planning/v1.47-v1.47-MILESTONE-AUDIT.md
  - test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "D-05: `$gsd-audit-milestone v1.47` runs through installed GSD skill/agent orchestration after the successful validation replay and produces the sole fresh terminal report."
    - "The audit is evaluated against a committed, complete snapshot of its actual repository inputs and resolved milestone/Nyquist orchestration state, not a hand-curated subset."
    - "The fresh report closes the phase only when it shows 24/24 requirements, 6/6 phases, 8/8 integrations, 7/7 flows, all six Nyquist-compliant phases, and empty requirement/integration/flow blocker arrays."
    - "D-06 debt remains separately reported and does not substitute for or conceal a blocker array."
    - "The repository proves the frozen inputs, stable audit boundary, and output contract; it does not claim that a snapshot cryptographically authenticates the LLM/skill invocation."
  artifacts:
    - path: "scripts/planning/phase-236-audit-snapshot.exs"
      provides: "Deterministic input discovery and pre/post snapshot checker; never invokes the audit workflow"
    - path: ".planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json"
      provides: "Committed pre-audit file hashes and resolved orchestration inputs"
    - path: ".planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json"
      provides: "Post-audit input-stability comparison and exact report digest/classification"
    - path: ".planning/v1.47-v1.47-MILESTONE-AUDIT.md"
      provides: "Fresh canonical milestone audit output"
    - path: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      provides: "Adversarial source-to-report and bounded-provenance contract"
  key_links:
    - from: ".planning/ROADMAP.md, .planning/REQUIREMENTS.md, Phase 230-235 evidence"
      to: "236-AUDIT-INPUT-SNAPSHOT.json"
      via: "complete path-sorted SHA-256 manifest"
    - from: "resolved init/phases/find-phase/hook/checker state"
      to: "236-AUDIT-INPUT-SNAPSHOT.json"
      via: "normalized output of the same GSD queries audit-milestone consumes"
    - from: "$gsd-audit-milestone v1.47"
      to: ".planning/v1.47-v1.47-MILESTONE-AUDIT.md"
      via: "installed GSD skill/agent orchestration, not an arbitrary command"
    - from: "236-AUDIT-INPUT-SNAPSHOT.json"
      to: "236-AUDIT-OUTPUT-SNAPSHOT.json"
      via: "pre/post equality for all inputs with the audit report as the sole declared output"
---

<objective>
Freeze the complete resolved v1.47 audit input set, invoke the supported milestone-audit skill, and validate the fresh report against those deterministic sources.

Purpose: Close D-05 without a fake CLI or unverifiable provenance receipt, while making the audit's inputs, output, and claim boundary reviewable and fail-closed.
Output: A tested snapshot utility, committed pre-audit snapshot, fresh canonical audit, post-audit comparison, and source-bound terminal contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/config.json
@.planning/v1.47-v1.47-MILESTONE-AUDIT.md
@.planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md
@.planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md
@.planning/phases/236-closeout-evidence-reconciliation/236-04-SUMMARY.md
@test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
@/Users/jon/.codex/gsd-core/workflows/audit-milestone.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Discover and commit the complete canonical audit input snapshot</name>
  <files>scripts/planning/phase-236-audit-snapshot.exs, scripts/planning/phase-236-audit-snapshot-test.exs, .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json, test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</files>
  <read_first>
    AGENTS.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/config.json,
    .planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs,
    /Users/jon/.codex/gsd-core/workflows/audit-milestone.md
  </read_first>
  <behavior>
    - Test 1: snapshot discovery includes AGENTS, PROJECT, STATE, ROADMAP, REQUIREMENTS, config, the installed audit workflow, every Phase 230-235 VERIFICATION and VALIDATION, and every scoped SUMMARY the workflow uses for exports/requirements/routes.
    - Test 2: normalized resolved inputs include `init.milestone-op`, `phases.list`, every Phase 230-235 `find-phase`, `loop render-hooks verify:post --raw`, `agent-skills gsd-integration-checker`, and `resolve-model gsd-integration-checker --raw`, including present/absent markers and exit status.
    - Test 3: a missing, extra, duplicated, or reordered phase/file; changed ROADMAP membership; changed requirement row; changed VALIDATION/VERIFICATION/SUMMARY; changed config or hook state; or failed resolver command changes the manifest digest and fails comparison.
    - Test 4: the utility only snapshots and compares inputs; it has no mode or code path that invokes validate-phase, audit-milestone, a Codex session, or a GSD skill.
  </behavior>
  <action>
    Create `scripts/planning/phase-236-audit-snapshot.exs` as a deterministic snapshot/compare utility, not an audit wrapper. It must derive milestone membership from the current ROADMAP plus normalized `phases.list`/`find-phase` results and then inventory every actual file class read by `/Users/jon/.codex/gsd-core/workflows/audit-milestone.md`: `AGENTS.md`, `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/config.json`, the installed audit workflow, every Phase 230-235 `*-VERIFICATION.md` and `*-VALIDATION.md`, and all Phase 230-235 SUMMARY frontmatter/files used for exports, requirement-completion, routes, and the three-source cross-check. Include the successful Phase 236 validation replay baseline/summary as the ordering precondition, but do not treat Phase 236 as a v1.47 audited member unless the resolved workflow scope does.

    Capture the workflow's non-file inputs by calling the query interface only for read-only resolution: `init.milestone-op`, `phases.list`, `find-phase` for every resolved member, `loop render-hooks verify:post --raw`, `agent-skills gsd-integration-checker`, and `resolve-model gsd-integration-checker --raw`. Canonically normalize JSON where applicable; record command identity, exit status, stdout digest, resolved value, and present/absent state. Include `.planning/config.json` and any additional config path explicitly reported as loaded by `init.milestone-op`; if a reported config input is already dirty, record its exact staged and unstaged patch digests and require them to stay unchanged rather than absorbing later edits. Never print secrets or environment values.

    Write path-sorted file entries with blob SHA-256, normalized resolver entries, aggregate manifest SHA-256, starting commit, and explicit claim limits to `236-AUDIT-INPUT-SNAPSHOT.json`. Add a hermetic fixture test covering discovery completeness, ordering, mutation rejection, resolver failure, dirty-input stability, and the absence of any audit-dispatch code path. Extend the Phase 236 ExUnit contract to independently assert the production snapshot's required classes and resolved membership. Run both tests and create a dedicated commit containing only the utility, tests, and input snapshot before invoking the audit skill. Preserve unrelated working-tree changes exactly.
  </action>
  <verify>
    <automated>elixir scripts/planning/phase-236-audit-snapshot-test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs --only audit_input_snapshot &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The committed snapshot is complete according to the installed workflow and resolved v1.47 membership; it includes ROADMAP, REQUIREMENTS, all relevant validation/verification/summary artifacts, configuration, and Nyquist/checker orchestration state; every omitted or mutated class fails deterministically; the utility cannot dispatch an audit.
  </acceptance_criteria>
  <done>The audit's real trust boundary is frozen end-to-end before the canonical skill runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invoke the canonical milestone audit and bind its output to the frozen sources</name>
  <precondition>The committed `236-AUDIT-INPUT-SNAPSHOT.json` matches the current repository and resolved orchestration state, and Plan 04 reports all three replayed validation skills compliant.</precondition>
  <files>.planning/v1.47-v1.47-MILESTONE-AUDIT.md, .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json, test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</files>
  <read_first>
    /Users/jon/.codex/gsd-core/workflows/audit-milestone.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/v1.47-v1.47-MILESTONE-AUDIT.md,
    .planning/phases/230-tier-1-critical-path-reclamation/230-VERIFICATION.md,
    .planning/phases/231-gate-honesty-nightly-revival/231-VERIFICATION.md,
    .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VERIFICATION.md,
    .planning/phases/233-library-suite-economics/233-VERIFICATION.md,
    .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md,
    .planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
  </read_first>
  <behavior>
    - Test 1: immediately before audit execution, every file/resolver/config/hook entry matches the committed input snapshot; immediately afterward, the same inputs still match and only the declared audit output has changed.
    - Test 2: the fresh report and output snapshot independently agree on 24/24 requirements, 6/6 phases, 8/8 integrations, 7/7 flows, compliant phases [230,231,232,233,234,235], no partial/not-validated/missing phases, overall compliant, and exact empty requirement/integration/flow arrays.
    - Test 3: stale output digest, altered score, moved Nyquist phase, inserted blocker with green totals, missing three-source requirement evidence, or D-06 debt moved into a blocker array fails with a field-specific error.
    - Test 4: output metadata states that the live SUMMARY records `$gsd-audit-milestone v1.47` orchestration, while repository snapshots prove source/output consistency and do not cryptographically authenticate the invoking LLM.
  </behavior>
  <action>
    First run the snapshot utility in compare mode and stop if any committed input, resolved membership, config source, validate-phase hook state, integration-checker skill/model resolution, or dirty-input patch differs. Then use the installed GSD skill/agent orchestration in this live runtime to invoke `$gsd-audit-milestone v1.47`. This is an explicit supported workflow action: do not run an invented `gsd-tools audit-milestone` command, do not wrap the workflow in a shell script, and do not treat a PATH-resolved executable, loader hash, nonce, or transcript as proof that the skill ran. Let the skill and its integration-checker agent produce `.planning/v1.47-v1.47-MILESTONE-AUDIT.md`; do not hand-edit its scores, arrays, classifications, or prose.

    Immediately after the skill returns, regenerate the complete input manifest and require equality with the committed pre-audit snapshot. The only permitted changed audited artifact is `.planning/v1.47-v1.47-MILESTONE-AUDIT.md`. Write `236-AUDIT-OUTPUT-SNAPSHOT.json` with the unchanged pre/post manifest digest, audit SHA-256, parsed exact terminal fields, execution timestamp, and the bounded claim: the live execution SUMMARY records the skill invocation, while the repository artifacts deterministically bind sources to output but cannot cryptographically establish LLM identity.

    Extend the Phase 236 contract to independently parse the ROADMAP/REQUIREMENTS traceability, all relevant VERIFICATION/SUMMARY sources, all six VALIDATION classifications, the two snapshots, and the fresh report. Require 24/24 requirements, 6/6 phases, 8/8 integration checks, 7/7 flows, exact Nyquist sets, and exact empty blocker arrays. Retain D-06 items only in the report's debt classification. Add adverse in-memory mutations for every behavior above. If the skill returns gaps, or any source changes during execution, preserve the exact generated report and diagnostics, write a noncompliant output snapshot, and stop without closeout or manual relabeling. Do not query new CI/runtime evidence.
  </action>
  <verify>
    <automated>elixir scripts/planning/phase-236-audit-snapshot.exs compare .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; jq -e '.result == "compliant" and .scores == {"requirements":"24/24","phases":"6/6","integration":"8/8","flows":"7/7"} and .nyquist == {"compliant_phases":[230,231,232,233,234,235],"partial_phases":[],"not_validated_phases":[],"missing_phases":[],"overall":"compliant"} and .gaps == {"requirements":[],"integration":[],"flows":[]}' .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The live runtime invokes the installed milestone-audit skill after a matching committed snapshot; all actual inputs remain stable through execution; the fresh report and independent contract agree on every terminal score, Nyquist set, and blocker array; any mismatch remains an exact durable blocker; no provenance claim exceeds the observable evidence.
  </acceptance_criteria>
  <done>D-05 is closed by a supported canonical audit over frozen complete inputs, and D-06 remains correctly separated from acceptance blockers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository/config/resolver state → audit input snapshot | Every input class the installed workflow consumes must be inventoried without omission or secret leakage. |
| Frozen snapshot → installed audit skill | The canonical audit must run only after exact input equality and successful validation replay. |
| Audit report → terminal closeout claim | Scores, Nyquist classes, and all blocker arrays must agree with independent source parsing. |
| Execution record → provenance claim | SUMMARY can record the live action; durable artifacts must not overstate cryptographic proof of LLM identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-236-18 | Tampering | Audit input selection | high | mitigate | Derive membership from ROADMAP plus GSD resolution; inventory all workflow-read source classes and reject omission/reordering/mutation. |
| T-236-19 | Tampering | Config or Nyquist/checker state | high | mitigate | Snapshot loaded config paths, dirty patch digests, hooks, phase resolution, checker skill, and model resolution before/after. |
| T-236-20 | Repudiation | Audit invocation | high | mitigate | Use explicit installed skill orchestration and record it in SUMMARY; bound repository claims to stable inputs/output and disclose the identity-proof limitation. |
| T-236-21 | Elevation of privilege | Hand-edited green terminal report | high | mitigate | Independent source-to-report contract asserts exact scores, all Nyquist sets, and empty blocker arrays; adverse green-total/hidden-gap cases must fail. |
| T-236-22 | Information disclosure | Resolver/config capture | medium | mitigate | Capture only paths, normalized non-secret outputs, presence, exit status, and digests; never serialize environment values or credentials. |
| T-236-23 | Denial of service | Audit or integration-checker failure | medium | mitigate | Preserve the canonical report/diagnostics and stop; no duplicate CI watcher, external evidence recapture, or automatic waiver. |
</threat_model>

<verification>
Run the hermetic snapshot suite and focused input tag before audit invocation. After the installed skill returns, compare the full source/resolution snapshot, run the complete Phase 236 contract, assert the exact output JSON, and check formatting/diff hygiene. No human UAT or fabricated Phase 236 requirement probe applies.
</verification>

<success_criteria>
- A committed pre-audit snapshot includes every actual repository file class and resolved orchestration/config/Nyquist input consumed by the installed workflow.
- `$gsd-audit-milestone v1.47` executes through the live installed skill/agent orchestration, not a fabricated CLI or wrapper.
- Pre/post audit inputs are identical; the audit report is the sole declared changed output.
- Report, snapshots, and independent source parsing agree on 24/24, 6/6, 8/8, 7/7, all six compliant phases, and three empty blocker arrays.
- D-06 debt remains separate unless the canonical report makes it a blocker.
- Any noncompliant output or source drift remains durable and stops closeout.
- The plan states exactly what current artifacts prove and does not claim retroactive or cryptographic LLM provenance.
- No product, CI topology, required-check, retained runtime-evidence, or operational-debt implementation changes.
</success_criteria>

## Artifacts this phase produces

- `scripts/planning/phase-236-audit-snapshot.exs`
- `scripts/planning/phase-236-audit-snapshot-test.exs`
- `.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json`
- `.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json`
- Fresh `.planning/v1.47-v1.47-MILESTONE-AUDIT.md` emitted by the canonical audit skill
- Source-to-report and bounded-provenance assertions in `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs`

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reconcile metadata, canonical validation, and fresh audit without product/CI/runtime-evidence changes | 236-01..05 | COVERED | Plans 04-05 replace the rejected fake-provenance design with honest forward replay and source-bound audit. |
| REQ | — | Phase 236 has no requirement IDs or probe predicate | — | COVERED | `requirements: []` is intentional; no ID is fabricated. |
| RESEARCH | — | Minimal truthful SUMMARY/traceability reconciliation | 236-01 | COVERED | Already implemented and verified. |
| RESEARCH | — | Established validation workflow and canonical lifecycle artifacts | 236-04 | COVERED | Phase 230 is pre-existing canonical; 231/232/234 receive fresh supported skill transitions. |
| RESEARCH | — | Fresh milestone audit from deterministic sources | 236-05 | COVERED | Complete resolved input snapshot precedes the supported audit skill. |
| CONTEXT | D-01 | Exact four SUMMARY declarations | 236-01 | COVERED | Existing contract remains intact. |
| CONTEXT | D-02 | Exact eight traceability rows under three-source rule | 236-01, 236-05 | COVERED | Existing reconciliation plus fresh source-to-report check. |
| CONTEXT | D-03 | Canonical validation for 230/231/232/234 only on success | 236-04 | COVERED | 230 verified pre-existing; exact baselines and fresh skill transitions for 231/232/234. |
| CONTEXT | D-04 | Preserve compliant phases and retained evidence | 236-04 | COVERED | Existing audit history and protected bytes remain; no 230 replay. |
| CONTEXT | D-05 | Fresh canonical audit is sole honest verdict | 236-05 | COVERED | Installed skill action plus complete pre/post source/output contract. |
| CONTEXT | D-06 | Operational debt remains separate unless auditor elevates it | 236-05 | COVERED | Debt and blocker arrays are tested independently. |

Deferred ideas, CI topology, product behavior, required-check policy, runtime-evidence recapture, and named debt implementations remain excluded by the locked phase boundary and are not planning gaps.

<output>
Create `.planning/phases/236-closeout-evidence-reconciliation/236-05-SUMMARY.md` when done.
</output>
