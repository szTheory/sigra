---
phase: 236-closeout-evidence-reconciliation
plan: 08
type: execute
wave: 8
depends_on: [236-07]
files_modified:
  - test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "D-05: the committed Plan 06 and Plan 07 execution ranges are collected as the deduplicated union of every commit's changed paths, so a forbidden path changed and restored before a valid endpoint is still rejected."
    - "Every Git command used to enumerate a range or a commit's paths must succeed; an invalid boundary or any per-commit collection failure aborts the scope fence instead of yielding a partial path set."
    - "The existing five-path `@scope_allowlist` remains byte-for-byte unchanged and is the single authority applied to both committed execution ranges and the tracked/untracked worktree collectors."
    - "A deterministic temporary-repository regression changes a non-allowlisted path, restores it before the endpoint, and proves that the scope fence rejects the history even though an endpoint-tree diff would omit that path."
    - "D-01, D-02, D-03, D-04, and D-06 remain verified and unopened; no product, CI topology, required-check, retained runtime-evidence, audit, validation, verification, or snapshot artifact changes."
  artifacts:
    - path: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      provides: "Fail-closed per-commit range collector, immutable Plan 07 endpoint, and adversarial restored-path regression"
  key_links:
    - from: "git rev-list from..to"
      to: "git diff-tree --no-commit-id --name-only -r for each returned commit"
      via: "required-success enumeration followed by sorted deduplicated union"
      pattern: "rev-list|diff-tree"
    - from: "Plan 06 and Plan 07 committed execution ranges"
      to: "@scope_allowlist"
      via: "changed_paths_between!/2 returns every per-commit path before the identical allowlist rejects non-members"
      pattern: "changed_paths_between!|scope_allowlist"
    - from: "tracked_paths!/0 and untracked_paths!/0"
      to: "@scope_allowlist"
      via: "the same unchanged module attribute remains the worktree authority"
      pattern: "tracked_paths!|untracked_paths!"
---

<objective>
Close the sole remaining Phase 236 verification gap by making committed-range scope collection fail closed over every commit, including paths restored before the range endpoint.

Purpose: Make the D-05 scope-fence evidence match its claim that every non-allowlisted path changed during the audited Plan 06/07 executions is rejected, without reopening any passed reconciliation, validation, provenance, or audit truth.
Output: One hardened planning contract with a real changed-then-restored Git regression and deterministic local verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md
@.planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md
@.planning/phases/236-closeout-evidence-reconciliation/236-07-PLAN.md
@.planning/phases/236-closeout-evidence-reconciliation/236-07-SUMMARY.md
@test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject restored forbidden paths across committed execution ranges</name>
  <reversibility rating="reversible">The change is confined to one local deterministic contract and can be reverted without altering evidence, product behavior, or a published interface.</reversibility>
  <files>test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</files>
  <read_first>
    AGENTS.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-07-PLAN.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-07-SUMMARY.md,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
  </read_first>
  <behavior>
    - Test 1: a temporary Git repository has a baseline, an intermediate commit that changes both an allowed path and a forbidden path, and a final commit that restores only the forbidden path; endpoint diffing reports only the allowed path, while `changed_paths_between!/3` reports the sorted union of both paths.
    - Test 2: applying the scope validator to that per-commit union raises a deterministic scope violation naming the forbidden path.
    - Test 3: an invalid range boundary fails during `rev-list`, and an injected failure while collecting any enumerated commit fails the whole helper rather than returning paths collected so far.
    - Test 4: the real Plan 06 range from `4aaa3a739b3d53b86de41136857ece240c0807a5` to `b6c67ed987fae364bb8e41dab21a8942bbc952a1` still yields exactly the four recorded execution paths under per-commit collection.
    - Test 5: the real Plan 07 range ends at immutable completion commit `287065751e2ed44d39d112801a06503de740e45d`, yields only existing `@scope_allowlist` members, and is unaffected by later verification or planning bookkeeping commits.
    - Test 6: committed-range, tracked staged/unstaged, and untracked non-ignored collectors all pass their results through the same unchanged `@scope_allowlist`; any extra path fails.
  </behavior>
  <action>
    Start RED by adding a `:scope_fence` regression that creates a uniquely named repository under the system temporary directory, initializes it with local deterministic author configuration, and commits three states: a baseline, an intermediate allowed-plus-forbidden mutation, and an endpoint that restores the forbidden file while retaining an allowed change. Register cleanup only for that exact temporary directory. First demonstrate that the endpoint-tree command omits the restored forbidden path, then require the production range helper and scope validator to surface and reject it. Keep the fixture local: no branch rewrite, current-repository commit, network call, CI dispatch, sleep, or human UAT.

    Replace `changed_paths_between!/2` endpoint-tree collection with a wrapper over a repository-aware `changed_paths_between!/3`. Enumerate commits with `git rev-list from..to`; for each returned SHA, collect paths with `git diff-tree --no-commit-id --name-only -r`; flatten, deduplicate, and sort only after every command succeeds. Route both enumeration and every per-commit collection through a helper that checks the `System.cmd/3` exit status and raises a contextual `ArgumentError` on nonzero status. Do not rescue, default to an empty list, discard stderr, or return the paths from successful earlier commits after a later failure. Add deterministic adverse coverage for a bad revision and for a simulated failure after at least one commit has been collected.

    Extract or reuse one scope-validation helper that reports rejected paths. Leave the five strings and ordering in `@scope_allowlist` byte-for-byte unchanged. Pass that same module attribute to the real Plan 06 committed range, real Plan 07 committed range, `tracked_paths!/0`, and `untracked_paths!/0`; do not introduce a second production allowlist, range-specific exception, prefix rule, or new bookkeeping path. Add `@plan_07_completion_sha` with the full SHA `287065751e2ed44d39d112801a06503de740e45d` and bound the historical Plan 07 range to `@plan_07_baseline_sha..@plan_07_completion_sha` so later verification/plan records are not misclassified as Plan 07 execution. Preserve the exact existing Plan 06 boundaries and execution-path expectation.

    Run the new `:scope_fence` test first, then the complete Phase 236 contract and formatter/diff gates. Do not modify `scripts/planning/phase-236-audit-snapshot.exs`, either committed snapshot JSON, the milestone audit, any Phase 230-235 artifact, `236-VERIFICATION.md`, product code, `.github/workflows/**`, repository configuration, requirement metadata, or retained evidence. This task closes only the unresolved D-05 scope-fence verification evidence described in `236-VERIFICATION.md`: endpoint-only collection cannot prove rejection of a transient forbidden mutation.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs --only scope_fence &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; mix format --check-formatted test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The adversarial changed-then-restored history is rejected by the same helper shape used for the real committed ranges; the real Plan 06 and fixed Plan 07 ranges pass; invalid-boundary and mid-collection failures abort without partial results; `@scope_allowlist` is unchanged and remains the sole authority for both committed and worktree paths; the complete Phase 236 contract, formatter check, and diff check pass locally with no protected artifact change.
  </acceptance_criteria>
  <done>D-05's remaining scope-fence claim is fail-closed for transient committed mutations, and all previously passed Phase 236 truths remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git history → committed-range scope verdict | Endpoint trees are insufficient; every commit and every path collection must be treated as untrusted until enumerated successfully. |
| Collected paths → shared allowlist | A path set can authorize completion only after comparison with the one unchanged Phase 236 allowlist. |
| Temporary adversarial fixture → developer repository | The regression may mutate only its unique temporary repository and must clean only that exact directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-236-32 | Tampering | `changed_paths_between!/2` | critical | mitigate | Enumerate `from..to`, collect every commit with `diff-tree`, deduplicate only after complete success, and prove a restored forbidden path is rejected. |
| T-236-33 | Repudiation | Git collection failures | high | mitigate | Check every command status, preserve contextual diagnostics, and test both invalid-boundary and mid-collection failures. |
| T-236-34 | Elevation of privilege | Scope allowlist drift | high | mitigate | Keep `@scope_allowlist` byte-for-byte unchanged and pass it to all committed/tracked/untracked scope validations. |
| T-236-35 | Tampering | Historical Plan 07 range | medium | mitigate | Pin the already-committed Plan 07 completion SHA so later bookkeeping cannot expand or alter that execution range. |
| T-236-SC | Tampering | Package installation surface | high | accept | No package installation occurs; the plan uses repository Git and existing Elixir/ExUnit tooling only. |
</threat_model>

<verification>
Run the adversarial `:scope_fence` tag, the complete Phase 236 ExUnit contract, exact check-only formatting for the owned test, and `git diff --check`. Verification is deterministic and local: no live CI dispatch, GitHub query, human UAT, new requirement probe, audit regeneration, or retained-evidence refresh is permitted.
</verification>

<success_criteria>
- `changed_paths_between!/2` returns a sorted, deduplicated union of paths from every commit in `from..to`, not the endpoint-tree difference.
- Any `rev-list` or per-commit `diff-tree` failure aborts the collector with diagnostics; no incomplete collection is accepted.
- A real temporary history proves that a forbidden changed-then-restored path is visible to the collector and rejected by the scope fence.
- The exact existing `@scope_allowlist` remains the single authority for both fixed committed execution ranges and current tracked/untracked collectors.
- Plan 06 and Plan 07 historical ranges remain exact and green, with Plan 07 bounded at its existing completion commit.
- No product, CI topology, required-check, requirement, audit, validation, verification, snapshot, configuration, or retained runtime-evidence artifact changes.
</success_criteria>

## Artifacts this phase produces

- Updated `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs` with fail-closed per-commit range collection and the restored-path adversarial regression.
- `.planning/phases/236-closeout-evidence-reconciliation/236-08-SUMMARY.md` recording the local deterministic verification and the unchanged scope boundary.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reconcile closeout evidence and retain an honest audit without product, CI-topology, or runtime-evidence changes | 236-01..08 | COVERED | Plan 08 closes only the last fail-closed scope-proof defect. |
| REQ | — | Phase 236 has no requirement IDs or probe predicates | 236-08 | COVERED | `requirements: []` is intentional; no identifier or predicate is fabricated. |
| RESEARCH | — | Provenance preservation and deterministic fail-closed contracts | 236-08 | COVERED | Per-commit Git collection and a hermetic adversarial repository close the observed verifier gap. |
| CONTEXT | D-01 | Exact four SUMMARY declarations | 236-01, 236-06..08 | COVERED | Passed ownership assertions are retained; historical declarations are not edited. |
| CONTEXT | D-02 | Exact eight traceability rows | 236-01, 236-06..08 | COVERED | Passed 24-row and three-source assertions remain unchanged. |
| CONTEXT | D-03 | Canonical validation lifecycle | 236-04, 236-06..08 | COVERED | Passed replay assertions remain unchanged. |
| CONTEXT | D-04 | Preserve retained evidence byte-for-byte | 236-01..08 | COVERED | Plan 08 modifies only the contract test and forbids protected-artifact changes. |
| CONTEXT | D-05 | Fresh audit is the sole honest verdict, including fail-closed scope evidence | 236-05..08 | COVERED | Plan 08 explicitly repairs the sole remaining verification finding without regenerating the audit. |
| CONTEXT | D-06 | Operational debt remains separate | 236-01..08 | COVERED | No debt item or adjacent CI work is implemented. |

Deferred product/CI/debt work, live CI dispatch, audit regeneration, and retained-evidence rewrites remain excluded and are not planning gaps.

<output>
Create `.planning/phases/236-closeout-evidence-reconciliation/236-08-SUMMARY.md` when done. The SUMMARY must report the per-commit collector, the changed-then-restored regression, explicit Git-failure coverage, fixed Plan 06/07 boundaries, the unchanged allowlist, and the exact local verification commands.
</output>
