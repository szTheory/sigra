---
status: complete
phase: 231-gate-honesty-nightly-revival
source: [231-VERIFICATION.md, 231-EVIDENCE.md]
started: 2026-07-30T12:30:00Z
updated: 2026-07-31T14:53:40Z
---

## Current Test

[testing complete]

## Tests

### 1. First Post-Merge Scheduled Nightly
expected: The first `schedule`-triggered `ci.yml` run after PR #125 merges uses the merge SHA and concludes success, with every job successful or legitimately skipped.
result: pass
evidence: GitHub Actions run `30607570671` (`event: schedule`, `headSha: 4bba9c71ae95a51bc2c3586010518a1c3439ab5f`, overall `success`) completed with 25 successful jobs and only the correctly gated `Notify on red ci-gate (release-lane-rot)` job skipped.

### 2. GitHub Pages Post-Merge Disposition
expected: The Pages source either self-heals to `gh-pages` or remains on `main`/`/` with the existing diagnosed, owned D-18 defect continuing as the explicit fallback disposition.
result: pass
evidence: Pages publisher run `30613728531` succeeded on the merge SHA, including the seed, publish, and REST steps; its log recorded a `403` when updating the Pages source. The live API still returned `source: {branch: main, path: /}` and `status: errored`. The fallback remains filed at `.planning/todos/pending/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md` with repo admin `szTheory` as owner.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]

## Human Verification Record

# Phase 231: Gate Honesty + Nightly Revival — UAT / Human Verification Record

**Status:** Complete. The owner accepted scheduled run `30607570671` as the
GATE-01 receipt on 2026-07-31. The run is a literal `schedule` event on PR
#125's merge SHA and concluded `success`.

**Authority:** `231-VERIFICATION.md` is the deciding document for every claim
below. This UAT restates its findings for the human closing the loop
post-merge; it does not re-derive anything the verifier did not already
independently confirm.

---

## 1. GATE-01 closure receipt

**Literal text (`.planning/REQUIREMENTS.md`):** "The nightly scheduled run is
green, or every remaining red lane is a filed, diagnosed defect with an
owner."

The required run now exists: GitHub Actions run `30607570671`, created
2026-07-31T05:43:10Z, is a literal `schedule` event at merge SHA
`4bba9c71ae95a51bc2c3586010518a1c3439ab5f`. It completed successfully at
2026-07-31T06:11:26Z. All 25 executing jobs concluded `success`; the sole
`skipped` job was the correctly event-gated red-notification job, because
`ci-gate` was green. The two previously-known nightly reds both executed and
passed: `Generated admin Playwright smoke` and `Admin eval render + probe`.

## 2. Every previously-known nightly red is independently confirmed fixed

Per `231-VERIFICATION.md`, both defects that made the pre-fix nightly
(`30425416933`, 23/25 green — the only two reds being these) fail are fixed
and proven on live runs, independent of the nightly slot itself:

- **D-17 — Pages-publisher seeding.** `playwright-github-pages.yml`'s publish
  job now runs an unconditional `Run demo seeds` step before booting the app
  it tests (copied verbatim from `ci.yml`'s unguarded seeds block). Proven on
  dispatched run `30529885885`, job `90829454715` (`conclusion: success`):
  all three checkpoint projects (`admin-checkpoints-chromium`, `-mobile`,
  `-dark`) passed the pagination assertion that failed on the pre-fix
  scheduled run `30432494488` (`admin-checkpoints.spec.ts:230`,
  `getByRole('link', { name: 'Next page' })`). The one observed flake was an
  unrelated, already-filed first-load `waitForLiveViewReady` timing class
  (`.planning/todos/resolved/2026-07-04-admin-eval-first-nav-flake.md`),
  which passed cleanly on Playwright's own retry. Guarded by a new fail-first
  prohibition test, `p15` (`scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs`,
  4/4 passing, proven red pre-fix / green post-fix with two negative
  controls).
- **D-19 — schedule-lane leniency removed.** `ci-observe.yml`'s Verdict step
  lost its `RUN_EVENT == schedule` early-exit / warn-instead-of-fail branch
  entirely (deleted at the corrected span, not just the conditional — no
  stale prose survives describing removed code, apart from the one comment
  fixed in this close-out pass, see below). The step's sole terminal branch
  is now an unconditional `exit 1` on a failing verdict, applying uniformly
  to every trigger. Guarded by a new fail-first prohibition test, `p16`
  (`scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs`, 4/4
  passing, proven red pre-deletion / green post-deletion with a negative
  control that reintroduces the branch).

## 3. Post-merge verification result

PR #125 merged to `main` at 2026-07-30T14:36:14Z as
`4bba9c71ae95a51bc2c3586010518a1c3439ab5f`. The following read-only checks
captured the first subsequent scheduled nightly:

```bash
gh run list --repo szTheory/sigra --workflow ci.yml --event schedule \
  --json databaseId,createdAt,conclusion,headSha --limit 5

gh run view 30607570671 --repo szTheory/sigra --json event,headSha,conclusion,jobs \
  --jq '.jobs[] | {name, conclusion, startedAt, completedAt}'
```

**Disposition:** GATE-01 closes on the green-run branch of its pre-written
decision procedure. There were no red lanes to file or diagnose.

## 4. Standing post-merge backstops (same structurally-unobservable-pre-merge class)

These items do not block GATE-01's now-green scheduled run:

- **D-18 — GitHub Pages source builds `main` root, not `gh-pages`.** The
  Pages publisher's two self-heal steps (`Publish to gh-pages branch`,
  `Point GitHub Pages at gh-pages (REST API)`) are both hard-gated on
  `github.ref == 'refs/heads/main'`. `workflow_dispatch` always resolves the
  workflow definition from the dispatched ref, so no single pre-merge
  dispatch can exercise both the fix (which requires dispatching from the
  phase branch) and the `ref == main` gate (which requires dispatching from
  `main`, which would run `main`'s pre-fix copy). Confirmed unchanged via
  `gh api repos/szTheory/sigra/pages` immediately after dispatch run
  `30529885885` (`source: {"branch": "main", "path": "/"}`). Filed at
  `.planning/todos/pending/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md`
  with both candidate root causes (insufficient `GITHUB_TOKEN` scope vs. a
  Settings-level block) left open rather than guessed at. Post-merge
  publisher run `30613728531` executed both steps successfully on the merge
  SHA, but the ensure script logged a `403` while attempting the Pages-source
  update. The live API still returned `source: {"branch":"main","path":"/"}`
  and `status: errored`. The existing defect therefore remains the explicit
  fallback disposition, owned by repo admin `szTheory`; no duplicate issue is
  needed and Phase 231 does not expand into repo-admin configuration.
- **DX-05's `gate-ci-green` ceiling half.** The `timeout-minutes: 75` /
  `wait-for-ci-gate.sh --max-attempts 120` fix (`release-please.yml`) is only
  exercised on a real `release_created == 'true'` release — it cannot be
  forced on a non-release PR. Its hermetic self-tests (11/11 passing) prove
  the polling logic in isolation; the ceiling itself is only provable
  end-to-end on the next real release.

## 5. The change most deserving human review before merge: the floor-rebase mechanism

`scripts/ci/quality-findings-monotonic.sh`'s floor-rebase declaration
mechanism (231-05, commits `8c49722b` (mechanism) / `af1b192c` (declaration
data), declaration schema at `guides/reference/floor-rebase-declarations.json`)
was authorized as an out-of-fence deviation during 231-05: the old
merge-blocking floor (33,642 open findings) had been measured on renders
that aborted early and so undercounted; the honest current value is 38,016.
The alternatives were a knowingly-false ledger or a permanently-red merge
gate — neither acceptable.

**The verifier independently exercised this mechanism against the real
pre/post-rebase git boundary** (not just read the code) and found it
genuinely bounded:

- Comparing at the actual pre-rebase→post-rebase boundary
  (`--base be970b50^`), the declaration validates and authorizes exactly the
  claimed 114 cells — matching both `prior_totals` (33,642, confirmed against
  `be970b50^`'s ledger) and `new_totals` (38,016, confirmed against the
  current ledger) exactly.
- Fail-closed by default: absent a declaration, behavior is today's exact
  strict behavior.
- Cannot be reused for a second, different increase: negative controls
  (Tests E, F, G, H) independently re-run and confirmed — absent declaration
  still fails (E), a mismatched declaration is rejected (F), drift beyond the
  declared floor still fails (H), and a base that already contains the
  rebase (`--base be970b50`) trivially passes with the declaration correctly
  reported as non-matching / inert, not reused.
- Both `prior_totals` and `new_totals` are verified against actual ledger
  content before authorizing anything — a declaration cannot launder an
  unrelated increase.

**Conclusion: reviewed and sound.** It is noted here — not as a defect, but
because it changes how this repo re-baselines a quality floor going forward,
and that class of change warrants an owner's eye before merge, independent
of whether the mechanism itself is correct.

## 6. Five deferred defects (filed, none silently absorbed into "complete")

| Defect | Filed at |
|---|---|
| D-25 — `example_unit_smoke` required but absent from `ci-gate.needs` | `.planning/todos/pending/2026-07-29-example-unit-smoke-required-but-absent-from-ci-gate-needs.md` |
| `Upgrade smoke`'s pre-existing `<.button type>` debt | `.planning/todos/pending/2026-07-10-upgrade-smoke-button-type-hex-publish.md` |
| Recapture-job transient hex.pm mirror flake (re-diagnosed by 231-11 away from 231-06's misleading Postgres framing — the FATAL line is harmless service-container health-check noise, not a real failure cause) | `.planning/todos/pending/2026-07-30-recapture-job-transient-hexpm-mirror-failure.md` |
| Audit-presets Actor-filter race (`admin-generated.spec.ts` ~454-458) | `.planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md` |
| `sigra_auth.css` mirror drift (example ~412 lines vs. library ~1134 lines) | `.planning/todos/pending/2026-07-28-w2-example-sigra-auth-css-stale-no-parity-gate.md` |

All five are filed and discoverable; none is silently absorbed into a
"complete" claim anywhere in this phase's artifacts.

---

*Written: 2026-07-30; completed with the first post-merge scheduled-nightly
receipt on 2026-07-31.*
