---
phase: 235-terminal-ratification-measured-not-read
plan: 13
type: execute
wave: 13
depends_on: [235-12]
files_modified:
  - scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.attestation.jsonl
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT-TRUSTED-ROOT.jsonl
  - test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "FAST-01 closes only from one newly attested, protected-main, post-remediation population containing at least ten unique terminal pull_request runs and no old receipt ID."
    - "The authoritative calculation retains all conclusions, queue-inclusive wall time, canonical {wall_seconds, run_id} ordering, floor(n/2), and strict p50 < 720; equality is a miss."
    - "The immutable old receipt remains n=13/p50=724/miss; a new miss appends evidence and leaves the phase gap open rather than replacing/cherry-picking either population."
    - "On a pass, FAST-01 alone becomes Complete with exact cutoff, endpoint, n, p50, producer run, and receipt; on a miss, it stays Gaps Found with the same exact fields recorded."
    - "GATE-05 remains independently Complete: its requirement line, protected run 30782184713, 93-row ownership proof, files, offline verifier, and contract do not regress."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: p50 equal to 720 remains miss; cutoff/endpoint bounds admit only rows within the protected interval."
    - requirement_id: FAST-01
      category: empty
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: absent subject or fewer than ten rows fails the precondition and cannot reach reconciliation."
    - requirement_id: FAST-01
      category: ordering
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: equal durations are canonical by ascending run_id before floor(n/2)."
    - requirement_id: GATE-05
      category: unclassified
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: independent terminal offline verification plus exact requirement/protected-file non-regression is required in both verdict branches."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT dispatch before readiness, rerun for a favorable p50, omit non-success conclusions, alter the threshold/cutoff, or overwrite either prior receipt."
      reason: "Descriptor-less flagged-unverified spec-less fallback; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT couple performance reconciliation to any downgrade, replacement, or reopening of the protected ownership proof."
      reason: "Descriptor-less flagged-unverified spec-less fallback; existing proof remains independently authoritative."
  artifacts:
    - path: scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh
      provides: "network-denied provenance and adverse-case verification for the new exact subject"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.json
      provides: "new independent protected population and strict verdict"
    - path: test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs
      provides: "independent recomputation and FAST/GATE status reconciliation contract"
  key_links:
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json
      to: .github/workflows/fast-01-gap-closure-evidence.yml
      via: "readiness authorizes only one dispatch when count>=10; its endpoint/rows never become subject authority"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.attestation.jsonl
      to: scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh
      via: "repository/signer/main-ref/subject digest verification under network denial"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.json
      to: .planning/REQUIREMENTS.md
      via: "offline-proven independent recomputation drives FAST-01 only"
---

<objective>
Capture and reconcile the one qualifying independent population, preserving an honest miss if the strict target is still not met.

Purpose: automation, readiness, and sample availability are prerequisites—not proof; only protected run data with valid offline provenance can close FAST-01.
Output: new receipt/bundle/root/verifier namespace and evidence-driven FAST-01/residual reconciliation with GATE-05 non-regression.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-12-SUMMARY.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json
@.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json
@.github/workflows/fast-01-gap-closure-evidence.yml
@scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh
@scripts/ci/verify-terminal-ratification-attestation-offline.sh
</context>

<flagged_planner_assumptions>
- Every supplied edge row is explicitly resolved by the precondition, population validator, comparator, canonical ordering, or GATE-05 non-regression check.
- Both bespoke transparency prohibitions remain descriptor-less flagged-unverified per the spec-less fallback; provenance/population checks are task acceptance criteria, not fabricated prohibition descriptors.
- Existing `235-COVERAGE.md` owns the external-call matrix. No new package, schema, product API, or UI surface exists.
</flagged_planner_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one ready population through protected dispatch and offline verification</name>
  <precondition>The exact endpoint-free readiness command has just produced `sigra.fast-01-gap-closure-readiness/v1`, remediation cutoff unchanged, at least ten unique terminal PR runs, status `ready`, and null statistics/verdict; `gh auth status` succeeds; one `gh api rate_limit` read reports core remaining above 250; protected origin/main contains exact Plan 12 workflow/collector blobs; and no live watcher exists for this workflow run.</precondition>
  <reversibility rating="costly">The attestation is append-only external evidence; local copies are replaceable only by another independently signed subject, which this plan forbids for result preference.</reversibility>
  <files>scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.json, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.attestation.jsonl, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT-TRUSTED-ROOT.jsonl, test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs</files>
  <read_first>
    - `AGENTS.md` and `235-COVERAGE.md` — exact authorized external operations, watcher/rate/retry rules, and hard stops.
    - Plan 12 workflow/collector/readiness/summary — producer blobs, schema, cutoff, and current readiness.
    - `scripts/ci/correlate-terminal-ratification-dispatch.sh` — bounded pre/post workflow-run set-difference selector.
    - `scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh` — closest repository/signer/ref/network-denial verifier pattern.
    - All old FAST receipt/bundle/root files — immutable adverse overlap/digest controls.
    - Existing GATE-05 receipt/bundle/root and verifier — non-regression authority, not input to FAST calculation.
  </read_first>
  <behavior>
    - "The local readiness endpoint/rows cannot be copied into the protected subject; the input-free workflow independently fixes its endpoint."
    - "Exactly one new workflow_dispatch run matches workflow ID, main ref, protected-main SHA, and not-before boundary; zero/multiple/stale/newest-only selection fails."
    - "Receipt, bundle, root, repository, signer workflow, source ref, subject digest, cutoff, and complete population verify with network denied; each adverse alteration fails."
    - "The population has n>=10, unique IDs, no overlap with old IDs, complete pages, all terminal conclusions, valid chronology, canonical order, and strict pass/miss."
  </behavior>
  <action>
    Assert every precondition read-only. If Plan 12 blobs are not on protected main, create/reuse one scoped evidence PR for only its producer/contract/coverage files, push without force, enable squash auto-merge with `--match-head-commit`, and watch exactly one selected PR run at 60-second intervals; never approve, bypass, waive, or dispatch from a branch. After merge, prove exact origin/main blob equality.

    Refresh readiness once using the exact endpoint-free command. If count falls below ten or any rate/auth predicate fails, retain exact diagnostics and stop without dispatch. Otherwise resolve numeric workflow ID and main SHA, take bounded pre-dispatch projection, record not-before UTC, dispatch exactly once, take bounded post-projection, and require exactly one new matching run through the production set-difference selector. Persist ID/URL, then use one mandated watcher. Fetch one structured summary after completion and failed logs once only on failure; retry one non-rate-limit transient operation once and hard-stop on 403/429/core<=250. Never rerun because the p50 is undesirable.

    Download the exact new subject, bundle, and contemporaneous trusted root into the new paths. Create the offline verifier from the proven pattern with fresh temp HOME, credentials/proxies unset, required Darwin sandbox or proven Linux network namespace, exact repository/signer/main-ref/digest, and independent adverse cases for altered receipt, bundle, root, signer, ref, cutoff, and old-ID injection. Validate source pages, bounds, identities, all conclusions, n, ordering, and strict verdict only after provenance passes. Never commit secrets, request headers, cookies, raw logs, or authenticated state.
  </action>
  <verify>
    <automated>bash scripts/ci/capture-fast-01-gap-closure.test.sh &amp;&amp; bash scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh &amp;&amp; jq -e '.schema_version == "sigra.fast-01-gap-closure-remeasurement/v1" and .authority == "protected_main_attestation" and .eligible_pr_run_count &gt;= 10 and .status == "measured" and (.verdict == "pass" or .verdict == "miss") and .statistics.mode == "wall" and .statistics.ordering == "{wall_seconds, run_id}"' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.json &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Readiness is refreshed and ready before exactly one protected-main dispatch; local endpoint/rows never become authority.
    - One exact run is selected by identity set difference and observed under every AGENTS.md bound.
    - Receipt/bundle/root verify offline, and every adverse provenance/population case fails under the same network denial.
    - The new population is n>=10, strictly post-remediation, disjoint from old rows, complete, canonical, and measured once.
    - Old FAST and GATE-05 protected artifacts remain byte-identical.
  </acceptance_criteria>
  <done>One and only one new independent protected population has a reproducible strict verdict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile FAST-01 from the protected verdict and prove GATE-05 unchanged</name>
  <precondition>The new offline verifier exits 0 and independent recomputation finds at least ten unique post-remediation terminal PR rows with no old-ID overlap.</precondition>
  <reversibility rating="reversible">Requirement and residual prose can be reverted; the protected old/new receipts remain the immutable sources for repeating reconciliation.</reversibility>
  <files>test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs, .planning/REQUIREMENTS.md, .planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md</files>
  <read_first>
    - All three files in `<files>` — current FAST/GATE status, historical residual, and Task 1 validators.
    - New receipt/bundle/root/verifier — sole new verdict authority.
    - Old FAST receipt and terminal ledger — immutable historical misses to preserve.
    - Existing GATE-05 protected files/verifier and Phase 235 terminal contract — exact independent non-regression requirements.
    - `235-VERIFICATION.md` — strict remaining gap language.
  </read_first>
  <behavior>
    - "Independent recomputation from attested rows is required; stored statistics/verdict alone never drives status."
    - "Pass branch: n>=10 and recomputed p50<720 changes FAST-01 checkbox/traceability to Complete and closes the residual while preserving 772 and 724 history."
    - "Miss branch: p50>=720 leaves FAST-01 unchecked/Gaps Found, appends exact new window/provenance/result, and returns a durable unresolved gap without another dispatch."
    - "GATE-05 line/status, protected pointers/files, 93-row count, and verifier result remain unchanged in both branches."
    - "Invalid provenance, nine/duplicate/overlapping/filtered rows, moved cutoff/endpoint, altered comparator, pass-shaped miss, or any GATE-05 change fails."
  </behavior>
  <action>
    Start RED with in-memory mutations for receipt-only trust, invalid provenance metadata, nine rows, duplicate/old IDs, omitted failure/cancelled rows, pre-cutoff/post-endpoint chronology, equal-duration ordering, 719/720/721 medians, altered threshold/comparator, FAST status contradiction, and every GATE-05 status/provenance/ownership change. Invoke the offline verifier before parsing, recompute exact rows/statistics, and compare canonical receipt fields.

    Branch only on recomputed strict result. On pass, change only FAST-01's checkbox and traceability row to Complete with exact remediation cutoff, protected endpoint, n, p50, producer run ID, and new receipt path; append a dated residual closure that retains both earlier misses. On miss, leave FAST-01 unchecked/Gaps Found, append the same exact fields as a third honest result, and return unresolved gap status without a rerun, new cutoff, threshold change, or preferred-window selection.

    In both branches leave GATE-05 and every other requirement unchanged; run its offline verifier and 93-row terminal contract. Do not modify old receipts, terminal ledger, ci.yml, contributor docs, SEED-005, MILESTONE-ARC, product/runtime tests, UI, schema, packages, retries, or timeouts.
  </action>
  <verify>
    <automated>bash scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - The contract independently derives n/p50 and rejects every provenance, population, comparator, status, overlap, and GATE-05 mutation.
    - A protected p50 below 720 completes FAST-01 with exact evidence and closes the residual without erasing earlier misses.
    - A protected p50 at/above 720 retains FAST-01 as Gaps Found and appends the exact miss without another dispatch.
    - GATE-05 remains checked/Complete; its old protected files, verifier, 93 rows, and traceability are unchanged.
    - No unrelated requirement or excluded file changes.
  </acceptance_criteria>
  <done>FAST-01 states exactly what the new independent protected population proves, and the phase closes only on a strict measured pass.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh` — new network-denied verifier; no CLI flags beyond its fixed repository-local paths.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.json` — new `sigra.fast-01-gap-closure-remeasurement/v1` fields `authority`, `repository`, `workflow`, `event`, `cutoff`, `window`, `runs`, `eligible_pr_run_count`, `statistics`, `verdict`, `status`.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.attestation.jsonl` — new exact-subject provenance bundle.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT-TRUSTED-ROOT.jsonl` — new retained official trust material.
- `test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs` — expanded protected-provenance, independent-population, recomputation, and requirement reconciliation helpers.
- `.planning/REQUIREMENTS.md` — FAST-01-only pass/miss reconciliation; no new field/symbol.
- `.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md` — exact new pass closure or miss addendum; prior evidence retained.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data | 13 | COVERED | Only protected independent data drives final status. |
| REQ | FAST-01 | n>=10, p50 strictly below 720 | 13 | COVERED | Independent recomputation; pass/miss branches honest. |
| REQ | GATE-05 | Single protected ownership proof remains complete | 13 | COVERED | Existing verifier/contract/status/files unchanged. |
| RESEARCH | — | Exact commands/run IDs, all conclusions, canonical median | 13 | COVERED | New protected receipt plus independent calculation. |
| CONTEXT | D-01–D-03 | Immutable cutoff/method/honest result | 13 | COVERED | No reroll or preferred window. |
| CONTEXT | D-04–D-06 | Ownership/contributor truth | 13 | COVERED | No topology/doc changes; exact non-regression. |
| CONTEXT | D-07 | Residual reflects measured truth | 13 | COVERED | Close on pass, append on miss. |
| CONTEXT | D-08 | No unrelated work | 13 | COVERED | Exact excluded scope in Task 2. |
| VERIFICATION | FAST-01 gap | New protected independent n>=10 p50<720 population | 13 | COVERED | This plan is the sole closure authority. |

No source item is missing. If the external population is unavailable, Task 1 halts on its deterministic precondition. If the protected verdict misses, the phase remains gaps_found; automation existence never counts as completion.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Readiness → dispatch | Stale or undersized local state must not authorize a protected result. |
| GitHub workflow → local evidence | Exact run identity and subject provenance must survive external retrieval. |
| New receipt → requirements | Only independent recomputation may change FAST status; GATE status is isolated. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-13-01 | Spoofing | dispatch/run identity | high | mitigate | Workflow ID/event/ref/SHA/not-before set difference; persisted ID; zero/multiple rejection; one watcher. |
| T-235-13-02 | Tampering | attestation subject | high | mitigate | Network-denied repository/signer/ref/digest/root verification and adverse receipt/bundle/root/signer/ref tests. |
| T-235-13-03 | Tampering | population/verdict | high | mitigate | Complete pages, cutoff/endpoint, old-ID disjointness, all conclusions, canonical recomputation, n>=10, strict comparator mutations. |
| T-235-13-04 | Repudiation | requirement status | high | mitigate | Contract binds exact protected evidence to checkbox/traceability/residual in both branches. |
| T-235-13-05 | Tampering | GATE-05 proof | high | mitigate | Existing offline verifier, 93-row contract, byte/prose non-regression, excluded edits. |
| T-235-13-06 | Denial of service | external operations | medium | mitigate | One preflight, one 60-second watcher, bounded calls, one summary/failure-log read, one transient retry, hard stop on rate limit. |
| T-235-13-07 | Information disclosure | retained evidence | low | accept | Public metadata/crypto only; credentials, headers, cookies, proxies, and raw logs excluded. |
</threat_model>

<verification>
Verify new provenance offline, recompute the complete population, run old/new FAST and terminal contracts, verify GATE-05 offline, then run all planning tests. Confirm exact pass-only closure or honest miss retention.
</verification>

<success_criteria>
- One protected independent n>=10 population exists and verifies offline.
- FAST-01 closes only for p50 strictly below 720; otherwise the durable gap remains.
- Prior 772/724 misses remain immutable historical evidence.
- GATE-05 remains independently Complete and unchanged.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-13-SUMMARY.md` when done.
</output>
