---
phase: 235-terminal-ratification-measured-not-read
plan: 10
type: execute
wave: 10
depends_on: [235-09]
files_modified:
  - scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.attestation.jsonl
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-TRUSTED-ROOT.jsonl
  - test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "A protected-main remeasurement is dispatched only after the fixed window has at least ten terminal PR runs; its exact run is correlated before one 60-second watcher starts."
    - "The retained receipt, attestation bundle, and trusted root verify offline against repository szTheory/sigra, signer fast-01-remeasurement-evidence.yml, source ref refs/heads/main, protected workflow SHA, and exact subject digest."
    - "D-01/D-02/D-03: the attested population is complete, independently recomputed with queue-inclusive all-conclusion wall semantics, and FAST-01 closes only when observed_p50_seconds is strictly below 720."
    - "A measured p50 at or above 720 remains a miss, updates the owned residual with the new exact receipt, and stops without moving the cutoff, redispatching for a preferred result, or changing the threshold."
    - "GATE-05 remains checked and Complete from the existing terminal ownership proof; none of its artifacts, 93 rows, or provenance are rewritten."
    - "D-08: the plan performs one measurement and status reconciliation only; binding-pole remediation remains separate."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: 719 passes, 720 and 721 miss."
    - requirement_id: FAST-01
      category: empty
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: the plan precondition requires at least ten terminal rows; missing or partial evidence blocks."
    - requirement_id: FAST-01
      category: ordering
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: equal durations use run ID as the stable secondary key before floor(n/2)."
    - requirement_id: GATE-05
      category: unclassified
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: existing offline ownership verification is the non-regression oracle and remains independent of FAST-01 status."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT represent FAST-01 as achieved from fewer than ten eligible post-cutoff PR runs, a p50 at or above 720 seconds, filtered conclusions, or substituted timing semantics."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT regress or reopen GATE-05 while reconciling the separate performance requirement."
      reason: "GATE-05 has an independently verified protected artifact and remains satisfied."
  artifacts:
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json
      provides: "protected independent population, exact run identities, statistics, and strict FAST-01 verdict"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.attestation.jsonl
      provides: "GitHub/Sigstore provenance bundle for the exact remeasurement subject"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-TRUSTED-ROOT.jsonl
      provides: "retained contemporaneous root material for deterministic offline replay"
    - path: scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh
      provides: "network-denied positive and adversarial verification for the fresh receipt"
  key_links:
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json
      to: .github/workflows/fast-01-remeasurement-evidence.yml
      via: "the exact local `--readiness OUTPUT` invocation self-derives current UTC and may authorize dispatch only at n≥10/status ready; the protected workflow independently derives the sole verdict endpoint"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.attestation.jsonl
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json
      via: "offline repository/signer/main-ref/subject verification inside a network-denied process"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json
      to: .planning/REQUIREMENTS.md
      via: "strict protected verdict: only pass checks FAST-01; miss preserves Gaps Found and updates the residual"
    - from: scripts/ci/verify-terminal-ratification-attestation-offline.sh
      to: .planning/REQUIREMENTS.md
      via: "existing GATE-05 proof must pass before and after the FAST-only status edit"
---

<objective>
Capture exactly one qualifying independent PR population through protected main, verify it offline, and reconcile FAST-01 to the measured result.

Purpose: close the sole verification gap only from an attested n≥10 population with p50 strictly below 720, or retain the gap with equally durable evidence when it misses.
Output: protected fresh receipt/bundle/root, offline verifier, focused adversarial contract, and evidence-driven FAST-01 status/residual.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-08-SUMMARY.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-09-SUMMARY.md
@.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md

<interfaces>
Readiness gate: run exactly `bash scripts/ci/capture-fast-01-remeasurement.sh --readiness .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json` immediately before dispatch. The command accepts no endpoint/cutoff override and must produce the exact cutoff SHA/time, `authority=readiness_only`, `endpoint_source=collector_current_utc`, `eligible_pr_run_count >= 10`, `status=ready`, and null statistics/verdict. It authorizes dispatch only; its bytes and endpoint are forbidden as verdict inputs.
Verdict authority: `.github/workflows/fast-01-remeasurement-evidence.yml` independently generates its UTC start endpoint after dispatch and attests the resulting protected subject. That protected endpoint/population is the sole source of measured pass/miss status.
Dispatch: `gh workflow run fast-01-remeasurement-evidence.yml --repo szTheory/sigra --ref main`.
Single watcher: `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`.
Fresh offline policy: repository `szTheory/sigra`, signer `github.com/szTheory/sigra/.github/workflows/fast-01-remeasurement-evidence.yml`, source ref `refs/heads/main`, subject `235-FAST-01-REMEASUREMENT.json`.
Status invariant: FAST-01 text and threshold never change; GATE-05 remains checked/Complete and its protected terminal files remain byte-identical.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one qualifying protected dispatch through offline subject verification</name>
  <precondition>The exact command `bash scripts/ci/capture-fast-01-remeasurement.sh --readiness .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json` has just produced the fixed cutoff, `authority=readiness_only`, `endpoint_source=collector_current_utc`, at least ten terminal PR runs, `status=ready`, and null statistics/verdict; `gh auth status` succeeds; one `gh api rate_limit` read reports `.resources.core.remaining > 250`; and protected origin/main contains the exact Plan 09 workflow/collector blobs. If any predicate fails, retain/update the readiness artifact with exact diagnostics and halt without publishing, dispatching, or editing requirement status.</precondition>
  <reversibility rating="costly">The external attestation is append-only evidence; the local copies can be replaced only by another independently signed fixed-cutoff capture, never by editing their bytes.</reversibility>
  <files>scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.attestation.jsonl, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-TRUSTED-ROOT.jsonl</files>
  <read_first>
    - `AGENTS.md` — exact one-watcher, 60-second interval, rate-limit, summary/log, retry, and hard-stop constraints.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-09-SUMMARY.md` — workflow/collector blob IDs and latest readiness state produced by the dependency plan.
    - `.github/workflows/fast-01-remeasurement-evidence.yml` and `scripts/ci/capture-fast-01-remeasurement.sh` — exact protected producer and subject schema.
    - `scripts/ci/correlate-terminal-ratification-dispatch.sh` — bounded pre/post set-difference selector precedent; reuse or extend its generic identity checks rather than selecting newest-by-time.
    - `scripts/ci/verify-terminal-ratification-attestation-offline.sh` — network-denied positive/adversarial verification precedent.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md` — complete external call inventory and blocking outcomes.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` and its three protected evidence files — historical miss and GATE-05 proof to preserve, not inputs to the fresh calculation.
  </read_first>
  <action>
    Assert the precondition with read-only checks. If Plan 09 files are not yet on protected main, create or reuse one scoped evidence branch containing only the Plan 09 producer/contract/coverage files, push without force, open a scoped PR, enable squash auto-merge with `--match-head-commit`, identify its exact PR run by head SHA/event, preflight the REST budget, and use one repository-mandated 60-second watcher. Never approve, use admin bypass, waive a check, or dispatch from the branch. After merge, fetch origin/main and prove exact workflow/collector blob equality to the Plan 09 summary before continuing.

    Refresh readiness once with exactly `bash scripts/ci/capture-fast-01-remeasurement.sh --readiness .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json`; pass no endpoint/cutoff argument or environment value. Validate `authority=readiness_only`, `endpoint_source=collector_current_utc`, null statistics/verdict, the fixed lower cutoff, and status/count congruence. The focused contract must reject a third positional argument, `--endpoint`, endpoint-like environment variables, a preseeded favorable endpoint, locally emitted measured/pass/miss status, and any attempt to copy the readiness endpoint or rows into the protected subject. If fewer than ten rows remain, atomically retain `insufficient_population` and stop. A ready artifact authorizes only the dispatch decision and is never the verdict window.

    Otherwise resolve the numeric workflow ID and protected-main SHA; take one bounded pre-dispatch projection for that workflow/event/branch/SHA, record the UTC not-before boundary, dispatch exactly once, take one bounded post-dispatch projection, and require exactly one new matching run through the production set-difference selector. The input-free protected workflow must independently derive its UTC endpoint after it starts; do not pass, read, or reuse the local readiness endpoint. Persist the selected ID/URL before starting exactly one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`. After completion fetch one structured summary; fetch failed logs once only on failure. Retry one non-rate-limit transient operation once; on 403/429 or core remaining at or below 250, stop until the reported reset/retry time. Do not dispatch additional PR CI or rerun a successfully attested measurement because its p50 is undesirable.

    Download the exact receipt artifact as `235-FAST-01-REMEASUREMENT.json`, its bundle as `235-FAST-01-REMEASUREMENT.attestation.jsonl`, and contemporaneous official trusted material as `235-FAST-01-REMEASUREMENT-TRUSTED-ROOT.jsonl`, using the already-proven bounded trusted-root recovery only if the default official retrieval path is unavailable. Create `verify-fast-01-remeasurement-attestation-offline.sh` from the existing verifier pattern: fresh temporary directory/HOME, credentials and proxy variables unset, Darwin sandbox or proven Linux network namespace required, exact repository/signer/main-ref policy, and independent failure after altering receipt, bundle, root, signer, and source ref. Validate complete pages, bounds, identities, chronology, all conclusions, n≥10, stable median, and strict verdict only after offline provenance passes. Never commit tokens, headers, cookies, raw logs, or authenticated state.
  </action>
  <verify>
    <automated>bash scripts/ci/capture-fast-01-remeasurement.test.sh &amp;&amp; jq -e '.authority == "readiness_only" and .endpoint_source == "collector_current_utc" and .eligible_pr_run_count &gt;= 10 and .status == "ready" and .statistics == null and .verdict == null' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json &amp;&amp; bash scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh &amp;&amp; jq -e '.eligible_pr_run_count &gt;= 10 and .status == "measured" and (.verdict.status == "pass" or .verdict.status == "miss") and .verdict.threshold_seconds == 720 and .verdict.comparator == "lt"' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json</automated>
  </verify>
  <acceptance_criteria>
    - The exact endpoint-free local readiness command self-derives current UTC, has at least ten terminal PR runs/status ready before any dispatch, and stays non-authoritative with null statistics/verdict; endpoint override/favorable-window attempts fail deterministically.
    - The input-free protected workflow independently derives the endpoint for the sole attested measured subject; no readiness endpoint, rows, or status can substitute for it.
    - One exact protected-main run is selected by bounded pre/post identity difference, persisted, watched once at 60 seconds, and summarized once.
    - Receipt, bundle, and root verify offline for the exact repository/signer/main-ref/subject; all five adversarial alterations fail under the same network denial.
    - The receipt contains a complete exact population and a strict pass or miss computed only from attested run data.
    - The old terminal/GATE-05 evidence files and requirement status remain unchanged in this task.
  </acceptance_criteria>
  <done>One independent n≥10 fixed-window result has protected provenance and a reproducible strict verdict; no alternate window or rerun can replace it for convenience.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile FAST-01 and retain GATE-05 from the protected verdict</name>
  <precondition>The fresh offline verifier exits 0 and the decoded receipt has at least ten unique terminal PR runs after the fixed cutoff with a strict pass or miss verdict.</precondition>
  <reversibility rating="reversible">Requirement and residual text can be reverted; the protected receipt remains the immutable source for repeating the reconciliation.</reversibility>
  <files>test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs, .planning/REQUIREMENTS.md, .planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md</files>
  <read_first>
    - The three files in `<files>` — current FAST-01/GATE-05 representations, old residual, and Plan 09 contract.
    - The protected receipt/bundle/root from Task 1 — sole fresh-verdict authority.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — exact remaining truth and no-regression ruling.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — historical 19/772 miss that remains historical and is not overwritten.
    - `scripts/ci/verify-terminal-ratification-attestation-offline.sh` — independent GATE-05 acceptance command that must remain green.
  </read_first>
  <behavior>
    - "Offline provenance is required before the fresh receipt can influence requirement text."
    - "A pass requires n≥10 and observed_p50_seconds < 720; 720 or greater leaves FAST-01 unchecked and Gaps Found."
    - "On pass, FAST-01 becomes checked/Complete with exact cutoff, endpoint, n, p50, protected run ID, and receipt path; the old residual becomes closed with both historical and fresh results preserved."
    - "On miss, FAST-01 remains unchecked/Gaps Found and the residual appends the fresh exact window/result; the task exits with the gap still open rather than selecting another window."
    - "GATE-05 line, traceability status, protected receipt pointers, and 93-row contract remain byte-for-byte or semantically unchanged and verified."
    - "Receipt/digest coherence without valid attestation, fewer than ten rows, filtered conclusions, altered threshold, and pass-shaped status on a miss all fail."
  </behavior>
  <action>
    Start RED with in-memory mutations for invalid/missing attestation metadata, nine rows, duplicate IDs, excluded failure/cancelled rows, 719/720/721 medians, moved cutoff, altered endpoint, changed threshold/comparator, pass status on a miss, FAST-01 text/status contradiction, and any GATE-05 status/provenance/ownership change. Then make the focused contract invoke the offline verifier before decoding the fresh receipt and recompute exact count/statistics independently from the attested rows using D-01/D-02 semantics.

    Branch status edits only on the protected computed verdict per D-03/D-07. If it passes, change only FAST-01's checkbox and traceability row to Complete, recording exact cutoff, endpoint, n, p50, protected workflow run ID, and receipt path; close the owned residual with a dated addendum that preserves the historical 19/772 miss and the new independent proof. If it misses, leave FAST-01 unchecked/Gaps Found, append the exact fresh window/result/provenance to the residual, and stop with a durable gap result; do not dispatch again, change the threshold, or select a different cutoff. In both branches leave GATE-05 checked/Complete and all other requirements unchanged.

    Per D-08, do not alter the historical terminal ledger, ownership artifacts, ci.yml, contributor docs, SEED-005, MILESTONE-ARC, runtime tests, UI, snapshots, schema, packages, timeouts, retries, or binding-pole code. Run the existing GATE-05 offline verifier and full planning contracts before accepting either branch.
  </action>
  <verify>
    <automated>bash scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - The contract independently derives the protected n/p50 and rejects every provenance, population, comparator, status, and GATE-05 mutation named in `<behavior>`.
    - If p50 is below 720, FAST-01 alone becomes checked/Complete with exact protected evidence and the residual is closed without erasing the 772-second history.
    - If p50 is 720 or greater, FAST-01 remains unchecked/Gaps Found, the residual gains the exact fresh miss, and execution returns a durable unresolved-gap result without another dispatch.
    - GATE-05 remains checked/Complete and its existing offline verifier plus 93-row contract pass.
    - All unrelated requirement rows and excluded files remain unchanged.
  </acceptance_criteria>
  <done>FAST-01 status states exactly what the single protected independent population proves, while GATE-05 and the historical terminal evidence remain intact.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/ci/verify-fast-01-remeasurement-attestation-offline.sh` — new network-denied verifier for the fresh repository/signer/main-ref/subject policy.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json` — new protected fixed-window population and strict verdict.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.attestation.jsonl` — new attestation bundle for the exact receipt.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-TRUSTED-ROOT.jsonl` — new retained trusted material for offline replay.
- `test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs` — expanded protected-verdict and requirement-status contract; no public runtime API.
- `.planning/REQUIREMENTS.md` — FAST-01-only status reconciliation; GATE-05 remains Complete.
- `.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md` — closes on a protected pass or appends the exact protected miss; historical evidence is preserved.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data | 235-10 | COVERED | One protected qualifying window drives the final FAST status. |
| REQ | FAST-01 | Strict under-12-minute p50 over at least ten post-change PR runs | 235-10 | COVERED | n≥10 precondition, complete attested rows, strict comparator, evidence-driven status. |
| REQ | GATE-05 | Existing before/after ownership proof remains satisfied | 235-10 | COVERED | Existing verifier/contract run before and after; no ownership artifact edits. |
| RESEARCH | — | Exact commands/IDs, wall mode, all conclusions, binding evidence | 235-10 | COVERED | Protected receipt plus independent recomputation; binding optimization remains excluded. |
| RESEARCH | — | One watcher, 60-second interval, rate preflight, hard stop | 235-10 | COVERED | Task 1 carries exact external-operation contract. |
| CONTEXT | D-01 | Fixed cutoff and qualifying population | 235-09..10 | COVERED | Readiness is refreshed; under-ten blocks. |
| CONTEXT | D-02 | Queue-inclusive all-conclusion stable p50 | 235-10 | COVERED | Attested rows are independently recomputed. |
| CONTEXT | D-03 | Exact command/run IDs and honest miss | 235-10 | COVERED | Both pass and miss branches are explicit; no reroll. |
| CONTEXT | D-04–D-06 | Ownership and contributor truth remain valid | 235-10 | COVERED | No re-audit/edit; existing contracts verify non-regression. |
| CONTEXT | D-07 | Evidence-backed closeout/residual | 235-10 | COVERED | Residual closes only on pass and is extended on miss. |
| CONTEXT | D-08 | No pending todos or unrelated remediation | 235-10 | COVERED | Exact excluded-file/action list in Task 2. |
| VERIFICATION | FAST-01 gap | Future independently measured qualifying population | 235-10 | COVERED | Protected n≥10 receipt is the sole status authority. |

No source item is missing. Deferred ideas and unrelated Phase 230–234/product/release/UI work remain excluded. No package install, schema push, new product API, test deletion, timeout/retry change, or performance remediation is planned. The existing external-evidence coverage artifact from Plan 09 covers the GitHub calls; no new API matrix is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Readiness artifact → protected dispatch | Stale or undersized local state must not authorize an external measurement. |
| GitHub dispatch/run → retained receipt | The selected producer run must be unique and tied to protected main. |
| Attestation bundle/root → local receipt | External provenance must authenticate the exact bytes before parsing. |
| Fresh verdict → requirements/residual | Status edits must reflect strict measured truth and preserve completed GATE-05. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-10-01 | Spoofing | dispatch run identity | high | mitigate | Exact workflow ID/event/main SHA/not-before pre/post set difference, persisted ID, one watcher, and zero/multiple/stale rejection. |
| T-235-10-02 | Tampering | fresh receipt/provenance | high | mitigate | Network-denied offline verification of repository, signer, ref, subject digest, bundle, and trusted root plus five adverse cases. |
| T-235-10-03 | Tampering | p50 verdict | high | mitigate | Independent complete-population recomputation, n≥10, all conclusions, stable order, and strict comparator mutation tests. |
| T-235-10-04 | Repudiation | requirement status | high | mitigate | Contract binds exact protected pass/miss to FAST checkbox/traceability/residual and rejects contradictions. |
| T-235-10-05 | Tampering | GATE-05 proof | high | mitigate | Existing offline verifier and 93-row contract must pass; protected ownership files and status are excluded from edits. |
| T-235-10-06 | Information disclosure | external evidence | high | mitigate | Retain public metadata and cryptographic material only; exclude tokens, headers, cookies, raw logs, and authenticated state. |
| T-235-10-07 | Denial of service | CI/API operations | medium | mitigate | One rate preflight, bounded discovery, one 60-second watcher, one summary/failure-log read, one transient retry, and hard stop on 403/429/core≤250. |

ASVS L1 requires every critical/high threat above to be mitigated; the automated commands exercise each mitigation. No application auth, session, database, schema, UI, package, or product API boundary changes.
</threat_model>

<verification>
Verify the fresh receipt offline, validate the complete population and strict verdict, run the fresh and terminal Phase 235 contracts, run the existing GATE-05 offline verifier, then run the full planning suite. Confirm FAST-01 alone follows the protected verdict and GATE-05 remains Complete.
</verification>

<success_criteria>
- One independently attested n≥10 population is captured from the fixed post-topology cutoff with no favorable reroll.
- FAST-01 closes only for observed p50 below 720; otherwise the gap and residual remain explicit.
- GATE-05 remains independently satisfied and unchanged.
- All external operations and evidence paths obey AGENTS.md and every high threat is machine-mitigated.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-10-SUMMARY.md` when done.
</output>
