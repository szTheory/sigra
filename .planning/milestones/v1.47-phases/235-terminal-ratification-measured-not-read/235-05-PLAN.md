---
phase: 235-terminal-ratification-measured-not-read
plan: 05
type: execute
wave: 5
depends_on: [235-04]
files_modified:
  - .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "D-01/D-02/D-03: the immutable capture endpoint and exact canonical gh run-list receipt independently bind every retained run identity, URL, event, timestamp, conclusion, and head SHA used by the honest 19-run, 772-second FAST-01 miss."
    - "D-03: each binding-pole diagnosis is derived from a digest-checked canonical `gh run view RUN_ID --repo szTheory/sigra --json databaseId,event,url,jobs` receipt for a retained pull_request run; identity and raw job timestamps come from those exact bytes, while the mandated metrics instrument calculates the matching job duration during deterministic offline replay."
    - "D-04/D-05: every ownership family/event row equals its declared direct owner, aggregate, receiver, and receipt contract, and every live after-owner/aggregate resolves to a parsed ci.yml job ID or a declared non-job seam as appropriate."
    - "The three ci_gate_aggregate rows name the live ci-gate workflow job consistently; wrong-but-plausible owner, aggregate, receiver, and receipt substitutions fail closed."
    - "D-06/D-07: terminal closeout validation composes the supplied contributor topology content instead of ignoring it, while the measured FAST-01 residual and other closeout records remain unchanged."
    - "D-08: this closure hardens existing evidence only; it does not recapture runs, alter CI topology, fabricate a passing target, or fold unrelated pending work into Phase 235."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: When two things are exactly equal or just touch, do they merge, collide, or separate?"
    - requirement_id: FAST-01
      category: empty
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: What is the result for empty, single-element, or null input?"
    - requirement_id: FAST-01
      category: ordering
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: When elements compare equal, is output order specified and stable?"
    - requirement_id: GATE-05
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT represent FAST-01 as achieved from fewer than ten eligible post-cutoff PR runs, a p50 at or above 720 seconds, filtered conclusions, or substituted timing semantics."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present coverage as complete while omitting an affected spec or suite, naming only a terminal aggregate, or lacking a receiver and execution receipt for moved work."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
  artifacts:
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      provides: "capture-pinned raw run population, byte-pinned per-run identity-plus-raw-jobs receipts with metrics outputs, corrected ownership rows, and declared ownership semantics"
    - path: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      provides: "adversarial source-receipt, binding-pole, ownership-semantics, and closeout-composition contract"
  key_links:
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      to: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      via: "pinned capture instant + canonical source receipt bytes + expected SHA-256 -> retained measurement identities -> recomputed honest verdict"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      to: scripts/ci/ci-run-metrics.sh
      via: "byte-pinned `gh run view` identity-plus-raw-jobs JSON -> offline `gh` replay of that exact `.jobs` array -> mandated name/conclusion/duration JSON -> binding-pole assertion"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      to: .github/workflows/ci.yml
      via: "declared family/event ownership semantics checked against parsed live job IDs, aggregate needs, and retained evidence receipts"
    - from: CONTRIBUTING.md
      to: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      via: "the supplied contributor content is composed into closeout validation and contradiction mutations"
---

<objective>
Close the remaining Phase 235 evidence-boundary and ownership-semantics gaps without changing the measured result.

Purpose: FAST-01 and GATE-05 must resist coherent replacement of the source population, fabricated binding-pole jobs, and plausible-but-false ownership destinations per D-01 through D-08.
Output: source-bound raw run and jobs receipts, exact ownership semantics with corrected `ci-gate` identifiers, and composed closeout validation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-REVIEW.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-04-SUMMARY.md
@.github/workflows/ci.yml
@test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
@.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
</context>

<flagged_planner_assumptions>
- **FAST-01 / adjacency — FLAGGED UNRESOLVED:** When two things are exactly equal or just touch, do they merge, collide, or separate? Existing D-02 logic remains in force, but this spec-less fallback row is not marked resolved.
- **FAST-01 / empty — FLAGGED UNRESOLVED:** What is the result for empty, single-element, or null input? Existing minimum-population validation remains in force, but this spec-less fallback row is not marked resolved.
- **FAST-01 / ordering — FLAGGED UNRESOLVED:** When elements compare equal, is output order specified and stable? Existing sorted floor(n/2) behavior remains in force, but this spec-less fallback row is not marked resolved.
- **GATE-05 / unclassified — FLAGGED UNRESOLVED:** unclassified — review manually. The exact ownership mapping and mutations provide a review surface without inventing a classification.
- Assumption-delta detector: `detected:false`; no additional delta is introduced.
</flagged_planner_assumptions>

## Artifacts this phase produces

- New paths: none. This gap closure extends the existing ledger and focused contract only.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — adds canonical raw run-list receipt bytes; for each binding-pole run, adds exact `gh run view ... --json databaseId,event,url,jobs` stdout plus its command and SHA-256 and the existing metrics command's literal output; adds an explicit ownership-semantics declaration; corrects the three `ci_gate_aggregate` destinations to `ci-gate`.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — adds private helpers equivalent to `validate_source_population!/1`, `validate_binding_pole_receipts!/1`, `expected_ownership_semantics!/0`, `workflow_job_ids!/1`, and `validate_ownership_semantics!/2`, plus the mutation cases below. No public runtime symbol is introduced.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the canonical GitHub run-list receipt into the immutable FAST-01 population</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` and `235-REVIEW.md` — authoritative capture-endpoint, population-digest, retained-identity, and coherent-forgery gaps.
    - Both files listed in `<files>` — current capture endpoint, unused `population_sha256`, 19/2/2 retained measurements, and `validate_capture!/1` / `validate_captured_ledger!/1` behavior.
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — D-01/D-02 source command fields, bounded wall-mode semantics, and canonical JSON conventions.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-01, D-02, D-03, D-07, and D-08.
  </read_first>
  <behavior>
    - "The capture instant is exactly 2026-08-02T18:07:04Z and parses as one UTC ISO-8601 instant; the immutable cutoff remains exactly 2026-08-01T02:06:30Z."
    - "A canonical raw receipt retains the source-command result used at capture, and its literal bytes hash to the pinned population SHA-256."
    - "Every retained measurement run has a positive integer ID and exactly matches one canonical source row on ID, URL, event, created/updated timestamps, conclusion, and head SHA."
    - "Every source row admitted to the bounded terminal population appears exactly once in the corresponding event measurement, with no nil, string, duplicate, invented, pre-cutoff, or post-endpoint identity."
    - "Changing the endpoint plus all mirrored endpoint fields, coherently replacing all retained rows/statistics/output receipts, or substituting a different self-consistent source receipt fails against pinned constants."
  </behavior>
  <action>
    Start RED with separate mutations for the capture instant, source command, population digest, raw source receipt, nil ID, string ID, invented positive ID, URL/event/timestamp/conclusion/head-SHA mismatch, and a coherent replacement that updates endpoint, measurements, statistics, canonical metric receipts, and hashes together. Demonstrate that the current self-consistent ledger boundary admits the coherent replacement before hardening it.

    Per D-01/D-02/D-03, use the existing `gh` evidence-capture tooling once to retrieve the already-recorded run identities without dispatching, watching, or selecting a new window. Re-fetch the retained IDs (or run the recorded source command and deterministically select only rows bounded by the pinned cutoff and capture endpoint), commit the literal canonical bounded source receipt, and replace `population_sha256` only if needed so it digests those exact receipt bytes. Pin `@capture_instant` and the resulting `@population_sha256` in the focused contract, require the capture timestamp to parse as the same UTC instant, and verify the literal raw receipt bytes with SHA-256. Decode the receipt as a list of source rows, normalize only the documented GitHub field-name casing into the ledger's snake-case names, and require unique positive integer `databaseId` values plus well-formed canonical run URLs. A current GitHub lookup authenticates the historical IDs; it must not admit a run created or updated outside the original endpoint.

    Implement exact set-and-field reconciliation between that canonical receipt and all retained `measurements.{pull_request,push,schedule}.runs` inside the immutable cutoff/capture bounds. Preserve all conclusions and require exact equality for ID, URL, event, timestamps, conclusion, and head SHA. Keep the 19-run/772-second strict miss and D-07 residual untouched. Per D-08, no new capture, CI edit, performance remediation, or unrelated record update belongs in this task.
  </action>
  <acceptance_criteria>
    - The unchanged capture endpoint and raw receipt validate against pinned constants and the literal receipt digest.
    - The retained populations equal the canonical bounded source population by identity and all provenance fields, not merely by self-supplied `run_ids`.
    - Endpoint, digest, source-receipt, nil/string/invented-ID, field-mismatch, and coherent-forgery mutations each fail with deterministic diagnostics.
    - The honest FAST-01 result remains 19 eligible pull-request runs with p50 772 seconds and status `miss`.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh</automated>
  </verify>
  <done>The immutable bounded FAST-01 population is cryptographically and semantically bound to the canonical raw GitHub capture receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind each miss diagnosis to a retained PR run and canonical jobs receipt</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - Both files listed in `<files>` — current `receipts.binding_pole`, ownership receipt digests, and permissive miss branch in `validate_verdict!/1`.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` and `235-REVIEW.md` — canonical per-run jobs receipt and binding-pole field requirements.
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — the mandated `--jobs RUN_ID --format json` duration calculation and its exact stripped output shape: a list of job name, conclusion, and `duration_seconds` values, with no run ID, URL, event, or job timestamps.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-03 honest binding-pole diagnosis and D-07 residual linkage.
  </read_first>
  <behavior>
    - "Each binding-pole run ID is a positive integer present in the retained pull_request population, and its URL/wall duration equal that retained run."
    - "Each source receipt records the exact command `gh run view RUN_ID --repo szTheory/sigra --json databaseId,event,url,jobs`, literal stdout bytes including their trailing newline, and a lowercase 64-hex SHA-256 of those bytes; the decoded value is one object with top-level `databaseId`, `event`, `url`, and `jobs` fields."
    - "The decoded source receipt's positive integer `databaseId`, canonical run `url`, and `pull_request` event equal the selected retained run; every raw job used for the diagnosis supplies exact `name`, `conclusion`, `startedAt`, and `completedAt` values from the same bound object."
    - "median_neighbor identifies the source-bound retained PR run at the committed sorted floor(n/2) wall duration; maximum_duration identifies the source-bound retained PR maximum."
    - "The named binding-pole job exists exactly once in the bound raw jobs array; deterministic offline replay of that exact `.jobs` array through `scripts/ci/ci-run-metrics.sh --jobs RUN_ID --format json` produces the committed stripped metrics output and the same name, conclusion, and script-defined `max(completedAt - startedAt, 0)` duration declared as evidence."
    - "Unknown IDs, wrong URLs/events/selections/job names/conclusions/timestamps/durations, malformed hashes, altered source or metrics bytes, and invented plausible jobs all fail."
  </behavior>
  <action>
    Start RED with independent mutations for an unknown positive run ID, non-PR run ID, mismatched run URL, wrong event, swapped selection, malformed source digest, altered source bytes, altered job `startedAt`/`completedAt`, malformed metrics digest, altered metrics bytes, wrong-but-plausible job name, wrong conclusion, and off-by-one duration. Confirm the current permissive receipt check does not bind these fields.

    Per D-03, acquire each already-selected run exactly once with `gh run view 30723615281 --repo szTheory/sigra --json databaseId,event,url,jobs` and `gh run view 30723593560 --repo szTheory/sigra --json databaseId,event,url,jobs`. Do not add `--jq` or pipe the commands through `jq`: retain each command's exact stdout bytes, including its trailing newline, as the canonical source receipt. In each existing binding-pole entry add `source_receipt` with exactly the fields `command`, `output`, and `sha256`; `output` must decode to exactly one object with top-level `databaseId`, `event`, `url`, and `jobs`, and `sha256` must digest those literal bytes. Replace the entry's loose metrics command/hash fields with `metrics_receipt`, also exactly `command`, `output`, and `sha256`: preserve `bash scripts/ci/ci-run-metrics.sh --jobs RUN_ID --format json` as `command`, retain its literal stdout as `output`, and digest those literal output bytes as `sha256`. The metrics output is intentionally only the script's list of `{name, conclusion, duration_seconds}` values; never use it as the source of run identity, URL, event, or job timestamps.

    Verify each canonical source digest before decoding, then derive run ID, URL, event, raw job name, conclusion, `startedAt`, and `completedAt` only from those bound bytes. Reconcile identity against the source-bound PR population from Task 1. Compute each retained run's wall duration from its captured run timestamps, implement `median_neighbor` and `maximum_duration` over that source-bound population deterministically, and require the selected IDs to remain 30723615281 and 30723593560 respectively. Match the binding-pole job by exact name and require valid UTC job timestamps with duration `max(completedAt - startedAt, 0)`.

    Prove that the mandated instrument calculated that duration from the bound receipt by making the ExUnit contract create a temporary deterministic `gh` executable whose sole accepted invocation is the metrics script's `gh run view RUN_ID --repo szTheory/sigra --json jobs --jq .jobs` call and whose response is the canonical source receipt's exact decoded `.jobs` array. Run `bash scripts/ci/ci-run-metrics.sh --jobs RUN_ID --repo szTheory/sigra --format json` with that temporary executable first on `PATH`, assert exit 0, and require stdout byte equality with `metrics_receipt.output`; then digest and decode that output and reconcile the unique binding-pole job's name, conclusion, and duration. This offline replay uses no network, sleep, watcher, or mutable GitHub state and makes the script's stripped output a duration-calculation receipt without attributing identity fields to it.

    Require D-07's residual to keep citing the same validated run IDs and commands after the new receipt validation succeeds. Preserve the miss exactly; binding-pole integrity is evidence hardening, never authority to replace 772 seconds or claim FAST-01 passed.
  </action>
  <acceptance_criteria>
    - Both existing binding-pole declarations validate from digest-checked canonical identity-plus-raw-jobs source bytes tied to retained PR runs, and each exact source command is recorded.
    - Selection, run identity, URL, event, wall duration, job name, conclusion, timestamps, and duration are derived from bound sources and exact; the metrics script's byte-equal offline replay proves the duration calculation without treating its stripped JSON as identity evidence.
    - Every mutation listed in `<behavior>` is rejected independently while the unchanged selected runs and newly committed receipt set pass without network access.
    - The residual continues to cite the validated run IDs and commands, and no measured value changes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh</automated>
  </verify>
  <done>The FAST-01 miss diagnosis is reproducible offline from byte-pinned per-run identity-plus-raw-jobs evidence passed through the mandated metrics instrument and cannot be satisfied by a fabricated or identity-detached receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce exact ownership semantics and compose contributor closeout validation</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - Both files listed in `<files>` — all 93 ownership rows, three inaccurate `ci_gate_aggregate` rows, `validate_rows!/1`, `required_non_playwright_families/0`, and `_contributing` in `validate_closeout_records!/5`.
    - `.github/workflows/ci.yml` — complete parsed job-ID universe, direct executors, terminal aggregates, `needs` relations, and live `ci-gate` identifier.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — D-05 source of Playwright family ownership.
    - Phase 230–234 evidence receipts referenced by `ownership.rows[*].receipt` — declared execution evidence that each mapping must resolve to.
    - `CONTRIBUTING.md` — D-06 content passed into both topology and closeout validation.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` and `235-REVIEW.md` — ownership and closeout-composition mutation requirements.
  </read_first>
  <behavior>
    - "Every declared after direct owner and terminal aggregate that represents a workflow job occurs in the parsed ci.yml job-ID set; `ci_gate_aggregate` uses ci-gate for direct owner, seam, aggregate ID, and receiver."
    - "Each family/event row exactly equals one declared semantic mapping for direct owner, invocation/seam, aggregate, state, receiver, phase, and receipt; Playwright mappings continue to derive from the pinned Phase 234 inventory."
    - "Each receipt key resolves to the existing ledger receipt registry and its phase/event semantics agree with the row; aggregate relationships agree with parsed workflow `needs` where applicable."
    - "Wrong-but-plausible owner, aggregate, receiver, phase, and receipt substitutions fail even when every value is nonempty and names another live job or receipt."
    - "`validate_closeout_records!/5` consumes the supplied contributor content through the existing contributor-topology validator; nil and contradicted supplied content fail the composed closeout."
  </behavior>
  <action>
    Start RED by mutating representative rows to another live-but-wrong direct owner, aggregate, receiver, phase, and receipt, plus `nil` and contradictory contributor content supplied to closeout. Confirm that nonempty-string validation and the ignored contributor argument admit these cases.

    Correct all three `ci_gate_aggregate` rows from the nonexistent underscore spelling to the live `ci-gate` job ID consistently across after direct owner, seam, terminal aggregate ID, and receiver. Preserve historical before-state labels only where they are explicitly historical and are not asserted as live job IDs.

    Per D-04/D-05, add one explicit family/event ownership-semantics declaration to the ledger or focused contract and validate every row field against it. Derive Playwright current owners from the pinned Phase 234 inventory, parse the full live workflow job-ID set, and validate direct owner, aggregate, receiver, `needs`, event applicability, and receipt-registry linkage with declared exceptions only for evidence labels that are not workflow jobs. Compare complete semantic maps, not nonempty tokens, and cover plausible substitutions among real jobs/receipts.

    Per D-06/D-07, replace the ignored closeout contributor argument with a composed call to the existing contributor-topology contract using the live workflow, Mix, Playwright config, and package inputs already loaded by the test. Add nil and contradiction mutations at the closeout boundary. Do not rewrite CONTRIBUTING when its supplied current content already passes; the gap is composition. Per D-08, do not alter workflow behavior or expand the ownership universe.
  </action>
  <acceptance_criteria>
    - All 93 ownership rows equal the declared family/event semantic map and resolve to live jobs/seams plus existing evidence receipts.
    - The `ci_gate_aggregate` PR, push, and schedule rows use `ci-gate` consistently for every live destination field.
    - Live-but-wrong owner/aggregate/receiver, wrong phase/receipt, and aggregate-needs mutations fail deterministically.
    - Closeout fails for nil or contradicted supplied contributor content and passes only when contributor topology validation also passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs test/sigra/planning/phase_234_playwright_inventory_contract_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/</automated>
  </verify>
  <done>GATE-05 names the real executable topology with receipt-backed exact semantics, and terminal closeout cannot bypass contributor-content validation.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data; maintainers can see what moved and where it landed | 235-05 | COVERED | Tasks 1–3 bind source population, job diagnosis, ownership, and closeout composition. |
| REQ | FAST-01 | Strict under-12-minute p50 over at least 10 post-change PR runs | 235-05 | COVERED | Tasks 1–2 preserve the honest 19-run/772-second miss and make its evidence source-bound. |
| REQ | GATE-05 | One before/after PR/main/nightly artifact proves nothing silently dropped | 235-05 | COVERED | Task 3 enforces exact owner/aggregate/receiver/receipt semantics for all 93 rows. |
| RESEARCH | — | Baseline-compatible wall mode, bounded capture, all conclusions | 235-05 | COVERED | Task 1 validates the canonical raw population without recapture or formula change. |
| RESEARCH | — | Binding-pole diagnosis from per-run job metadata | 235-05 | COVERED | Task 2 binds canonical jobs receipts and selection semantics. |
| RESEARCH | — | Phase 234 inventory remains the current Playwright input | 235-05 | COVERED | Task 3 derives current Playwright semantics from the pinned inventory. |
| RESEARCH | — | Direct executable owner differs from terminal aggregate | 235-05 | COVERED | Task 3 validates live job IDs, `needs`, receivers, and receipt registry. |
| RESEARCH | — | Contributor and closeout claims derive from live topology | 235-05 | COVERED | Task 3 composes existing contributor validation into closeout. |
| CONTEXT | D-01 | Immutable bounded wall-mode window and capture endpoint | 235-05 | COVERED | Task 1 pins the instant and canonical raw source receipt. |
| CONTEXT | D-02 | Queue-inclusive, all-outcome, sorted floor(n/2) semantics | 235-05 | COVERED | Tasks 1–2 retain the implemented metric and selection semantics. |
| CONTEXT | D-03 | Exact commands/real IDs and honest miss with binding pole | 235-05 | COVERED | Tasks 1–2 authenticate runs and job evidence while retaining 772 seconds. |
| CONTEXT | D-04 | Complete before/after ownership artifact | 235-05 | COVERED | Task 3 validates every semantic field for every declared family/event row. |
| CONTEXT | D-05 | Consume pinned Phase 234 inventory and reject silent drift | 235-05 | COVERED | Task 3 uses it as the Playwright mapping source. |
| CONTEXT | D-06 | Contributor direct-owner/aggregate/reproduction/non-PR truth | 235-05 | COVERED | Task 3 composes the existing topology validator into closeout. |
| CONTEXT | D-07 | Honest ledger-backed closeout/residual | 235-05 | COVERED | All tasks preserve the miss and Task 3 validates closeout composition. |
| CONTEXT | D-08 | No pending todos folded in | 235-05 | COVERED | Only the existing ledger and focused contract are modified. |

Deferred ideas and out-of-phase work remain excluded. The spec-less fallback has four applicable unresolved assumptions and all four are carried explicitly: `4 source = 4 planned + 0 silently dropped`. Prohibition recall keeps the two existing descriptor-less flagged-unverified entries: `2 source = 2 planned + 0 silently dropped`; no check descriptor is invented. External API coverage detector result: `detected:false` because this plan uses the established GitHub CLI only to authenticate existing evidence and adds no product API client, endpoint, or integration. Assumption-delta detector result: `detected:false`. No package install, schema file, schema push, UI, snapshot, manual UAT, sleep, CI watcher, or new external-service setup is introduced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical GitHub run-list receipt → retained measurements | Untrusted checked-in evidence crosses into the FAST-01 population and verdict. |
| Canonical per-run identity-plus-raw-jobs receipt → offline metrics replay → binding-pole diagnosis | A plausible fabricated or identity-detached job can misstate why the measured target missed. |
| Ownership ledger / evidence registry → live workflow | Nonempty but false owner, aggregate, receiver, or receipt values can mislead maintainers. |
| Contributor content → terminal closeout | A caller-supplied contradictory record can be ignored unless closeout composes topology validation. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-05-01 | Tampering | capture endpoint and source population | high | mitigate | Pin capture instant/digest, hash canonical raw receipt bytes, reconcile every retained source field, and reject coherent replacement mutations. |
| T-235-05-02 | Spoofing | retained run identities | high | mitigate | Require unique positive IDs, canonical URLs, and exact source-receipt identity/metadata equality. |
| T-235-05-03 | Tampering | binding-pole receipts | high | mitigate | Hash exact identity-plus-raw-jobs bytes, derive selection/identity/job timestamps from them, replay their jobs through the mandated metrics script offline, reconcile byte-exact metrics output, and mutation-test plausible substitutions. |
| T-235-05-04 | Tampering | GATE-05 ownership semantics | high | mitigate | Enforce complete declared family/event maps against parsed job IDs, aggregate dependencies, and receipt registry. |
| T-235-05-05 | Repudiation | contributor closeout composition | high | mitigate | Invoke contributor-topology validation from closeout and reject nil/contradictory supplied records. |
| T-235-05-06 | Information disclosure | raw run/job receipts | low | accept | Retain only public Actions metadata already named by the ledger; exclude logs, tokens, headers, artifacts, and authenticated payloads. |
| T-235-05-07 | Denial of service | evidence retrieval and deterministic parser | low | mitigate | Fetch only the fixed retained IDs once, obey GitHub rate-limit stops, then parse bounded checked-in receipts with no polling, sleeps, or recursive inputs. |

## ASVS L1 Automated Controls

- V4 access control: evidence retrieval uses the existing least-privilege authenticated `gh` context for public run metadata, performs no dispatch or mutation, and retains no credentials.
- V5 validation: closed schemas, exact constants, positive IDs, ISO-8601 timestamps, canonical URLs, digest checks, complete semantic maps, and adversarial mutations fail closed.
- V7 error handling: diagnostics identify the mismatched evidence dimension without printing credentials or authenticated content.
- V12 files/resources: parsers read fixed repository paths and bounded ledger fields only.
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs`, `bash scripts/ci/ci-run-metrics.test.sh`, `MIX_ENV=test mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs`, and `MIX_ENV=test mix test test/sigra/planning/`. All commands must exit 0. The focused contract must contain independent mutations for endpoint/source-population coherent forgery, nil/string/invented run identities, jobs receipt/selection/job fields, live-but-wrong ownership semantics, and supplied contributor closeout contradictions.
</verification>

<success_criteria>
- The immutable 19/2/2 run populations equal a pinned, digest-checked canonical run-list receipt field-for-field, and the strict PR result remains a 772-second miss.
- Both binding-pole diagnoses derive from canonical digest-checked identity-plus-raw-jobs receipts for retained PR runs, replay those bound jobs through the mandated duration instrument offline, and reject plausible fabrication or identity detachment.
- All 93 ownership rows equal their declared family/event semantics; `ci_gate_aggregate` points to live `ci-gate` destinations.
- Terminal closeout consumes the supplied contributor record and rejects nil or contradictory content.
- All four unresolved fallback assumptions and both descriptor-less prohibitions remain explicit with no silent drops, and every deterministic verification command passes.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-05-SUMMARY.md` when done.
</output>
