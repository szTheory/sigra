---
phase: 235-terminal-ratification-measured-not-read
plan: 12
type: execute
wave: 12
depends_on: [235-11]
files_modified:
  - scripts/ci/capture-fast-01-gap-closure.sh
  - scripts/ci/capture-fast-01-gap-closure.test.sh
  - .github/workflows/fast-01-gap-closure-evidence.yml
  - test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "FAST-01: the next population starts at the protected-main remediation merge recorded by 235-FAST-01-REMEDIATION.json, strictly after the immutable prior receipt's endpoint, and cannot contain any prior run ID."
    - "D-01/D-02/D-03: all terminal pull_request conclusions are retained; wall time is queue-inclusive; rows sort by {wall_seconds, run_id}; p50 uses floor(n/2); pass requires p50 < 720 and n>=10."
    - "Readiness is non-authoritative: 0–9 terminal rows produce insufficient_population with null statistics/verdict; ten or more produce ready with null statistics/verdict."
    - "The protected producer is main-only, input-free, independently fixes its endpoint at workflow start, proves contiguous pagination through a terminal empty page, and attests the exact new subject."
    - "The immutable n=13/p50=724/miss receipt remains unchanged, and GATE-05's 93-row protected proof remains independently Complete and green."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: exact 720 is a miss and the remediation cutoff itself is inclusive only for runs created at or after its fixed timestamp."
    - requirement_id: FAST-01
      category: empty
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: null/empty/one through nine populations retain readiness diagnostics and no verdict."
    - requirement_id: FAST-01
      category: ordering
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: equal durations use ascending run_id before floor(n/2), with canonical output in that order."
    - requirement_id: GATE-05
      category: unclassified
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: GATE-05 remains outside the performance subject and is reverified by its existing offline verifier and 93-row contract."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT reuse the old cutoff, endpoint, rows, or receipt; accept caller-selected endpoints; filter conclusions; or attest an undersized population."
      reason: "Descriptor-less flagged-unverified spec-less fallback; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT let the new evidence workflow join ci.yml, replace the ownership receipt, or change GATE-05 status."
      reason: "Descriptor-less flagged-unverified spec-less fallback; independent proof remains authoritative."
  artifacts:
    - path: scripts/ci/capture-fast-01-gap-closure.sh
      provides: "remediation-cutoff readiness/protected collector with independent-population rejection"
    - path: .github/workflows/fast-01-gap-closure-evidence.yml
      provides: "main-only input-free attestation producer for the new population"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json
      provides: "honest current population size, identities, rate state, and readiness diagnostics"
    - path: test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs
      provides: "cutoff independence, population, strict comparator, workflow, and GATE-05 non-regression contract"
  key_links:
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json
      to: scripts/ci/capture-fast-01-gap-closure.sh
      via: "exact protected-main cutoff SHA/time and immutable old-receipt digest"
    - from: scripts/ci/capture-fast-01-gap-closure.sh
      to: .github/workflows/fast-01-gap-closure-evidence.yml
      via: "separate non-authoritative readiness and protected-output interfaces"
    - from: .github/workflows/fast-01-gap-closure-evidence.yml
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-13-PLAN.md
      via: "one future attested n>=10 subject, never readiness bytes"
---

<objective>
Create the independent post-remediation population boundary and fail-closed protected producer, without treating automation or an undersized sample as completion.

Purpose: the old fixed-cutoff producer would overlap the immutable 13-run miss, so closure requires a separate cutoff, receipt namespace, readiness artifact, and protected workflow tied to the remediation merge.
Output: hermetic collector tests, a live honest readiness record, an input-free main-only producer, and external-call coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-11-SUMMARY.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json
@scripts/ci/capture-fast-01-remeasurement.sh
@scripts/ci/capture-fast-01-remeasurement.test.sh
@.github/workflows/fast-01-remeasurement-evidence.yml

<interfaces>
Cutoff source: `235-FAST-01-REMEDIATION.json.population_cutoff.{sha,timestamp}`; executor freezes those exact values into the new collector and contract.
Old immutable namespace: every `235-FAST-01-REMEASUREMENT*` file and its fixed a282b3de cutoff remain byte-identical/read-only.
New local interface: `bash scripts/ci/capture-fast-01-gap-closure.sh --readiness .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json`.
New protected interface: only `fast-01-gap-closure-evidence.yml` may invoke `--protected-output fast-01-gap-closure-remeasurement.json --endpoint <workflow-start-UTC>`.
</interfaces>
</context>

<flagged_planner_assumptions>
- All four supplied edge probes are resolved into explicit invariant/mutation behavior in this plan; none is auto-dismissed.
- The external GitHub evidence surface is already declared in `235-COVERAGE.md`; extend it with the new workflow/collector names and independent-cutoff calls rather than create a competing matrix.
- Assumption-delta detector is false; schema detector is false; no package install occurs.
</flagged_planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one post-remediation run page into honest readiness</name>
  <precondition>`235-FAST-01-REMEDIATION.json.population_cutoff.sha` is a 40-hex commit on origin/main, its timestamp matches Git, its recorded remediation blobs match origin/main, and it is later than `235-FAST-01-REMEASUREMENT.json.window.endpoint`.</precondition>
  <reversibility rating="costly">Changing the cutoff after any protected subject exists discards the entire new population; the new namespace isolates that cost from immutable prior evidence.</reversibility>
  <files>scripts/ci/capture-fast-01-gap-closure.sh, scripts/ci/capture-fast-01-gap-closure.test.sh, test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json</files>
  <read_first>
    - `235-FAST-01-REMEDIATION.json` — sole new cutoff authority and pre-remediation evidence.
    - `235-FAST-01-REMEASUREMENT.json` plus attestation/root files — immutable namespace, prior IDs, endpoint, strict miss, and digest that must never overlap/change.
    - `scripts/ci/capture-fast-01-remeasurement.sh` and `.test.sh` — proven pagination, chronology, rate, readiness, and comparator implementation to adapt into a separate namespace.
    - `test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs` — old cutoff/receipt invariants to preserve.
    - `scripts/ci/ci-run-metrics.sh` and `.test.sh` — mandatory wall/all-conclusion/stable median semantics.
    - `AGENTS.md` — API rate and retry constraints.
  </read_first>
  <behavior>
    - "RED first: using the old cutoff, any old run ID, an endpoint at/before cutoff, or a caller-controlled local endpoint fails."
    - "Pages are contiguous and retained through one terminal empty page; selected PR IDs are unique and absent from the immutable old receipt."
    - "Zero, one, nine rows yield insufficient_population; ten/eleven yield ready; local output always has null statistics/verdict and authority readiness_only."
    - "Protected calculations retain success/failure/cancelled conclusions, order {wall_seconds, run_id}, use floor(n/2), and classify 719 pass, 720/721 miss."
    - "The old receipt digest, n=13, p50=724, miss, and GATE-05 proof remain unchanged."
  </behavior>
  <action>
    Start RED with a production-script fake-gh suite covering 0/1/9/10/11 rows, mixed conclusions, equal-duration ties, 719/720/721 medians, duplicate/missing/noncontiguous pages, absent terminal page, chronology inversions, pre-cutoff/post-endpoint rows, old-population ID overlap, endpoint/cutoff argv and environment overrides, preseeded output, HTTP 403/429, and core remaining 250. Pin the immutable old receipt SHA-256 and fail if any old receipt ID appears.

    Create a separate collector by adapting the proven script, not editing it. Freeze the exact remediation SHA/timestamp from Plan 11; prove ancestry, timestamp, and remediation blob identity before page 1. Local `--readiness OUTPUT` derives current UTC internally from an absolute date binary, accepts no endpoint/cutoff, and writes the readiness schema declared in `Artifacts this phase produces` with authority/readiness source, source API, pages, rate state, exact public rows, diagnostics, null statistics/verdict, and status. The protected interface may accept only the workflow-supplied start endpoint and emits the protected schema declared in `<behavior>` only for n>=10.

    Run one live readiness invocation only after one rate-limit preflight. If population is undersized, atomically retain `insufficient_population` with exact IDs/diagnostics and complete only this infrastructure plan; do not dispatch, claim closure, edit FAST-01, or reuse old rows. If ready, still do not dispatch here—Plan 13 owns the sole protected result.
  </action>
  <verify>
    <automated>bash scripts/ci/capture-fast-01-gap-closure.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; jq -e '.schema_version == "sigra.fast-01-gap-closure-readiness/v1" and .authority == "readiness_only" and .statistics == null and .verdict == null and ((.eligible_pr_run_count &lt; 10 and .status == "insufficient_population") or (.eligible_pr_run_count &gt;= 10 and .status == "ready"))' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json</automated>
  </verify>
  <acceptance_criteria>
    - The new cutoff equals the remediation merge SHA/time and is later than the prior receipt endpoint.
    - All readiness/protected boundary, pagination, overlap, ordering, comparator, and rate-limit mutations fail deterministically.
    - Live readiness honestly reports the current independent count and exact rows with null statistics/verdict.
    - An undersized/empty sample blocks Plan 13's precondition and cannot be represented as progress on FAST-01.
    - Old FAST files and GATE-05 evidence/status remain unchanged and verified.
  </acceptance_criteria>
  <done>The repository can state exactly whether a genuinely independent population exists, without creating or implying a verdict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put the independent collector behind protected-main attestation</name>
  <files>.github/workflows/fast-01-gap-closure-evidence.yml, test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs, .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md</files>
  <read_first>
    - `.github/workflows/fast-01-remeasurement-evidence.yml` — pinned actions, main-only input-free producer pattern.
    - `.github/workflows/terminal-ratification-evidence.yml` — established least-privilege attestation producer.
    - Task 1 collector/contract/readiness — exact new namespace and cutoff.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md` — existing external call matrix to extend.
    - `.github/workflows/ci.yml` — prove the evidence workflow is not in required/aggregate topology.
  </read_first>
  <behavior>
    - "workflow_dispatch has no inputs; the sole job requires refs/heads/main and grants only contents:read, actions:read, id-token:write, attestations:write."
    - "All external actions use repository-approved immutable SHAs; the workflow fixes endpoint at start, invokes only the new collector/namespace, requires n>=10/measured, and attests/uploads byte-identical subject."
    - "The workflow neither triggers pull_request nor appears in ci.yml/aggregate needs, so it cannot create qualifying rows or alter GATE-05 ownership."
    - "Coverage lists rate preflight, paginated reads, dispatch correlation, one 60-second watcher, one summary/failure-only logs, artifact/bundle/root retrieval, and offline verification."
  </behavior>
  <action>
    Create the new input-free workflow from the existing producer pattern. Use a distinct name/artifact, `workflow_dispatch` only, main-ref job guard, bounded timeout, least privileges, and the already-approved immutable action pins. Derive the endpoint inside the workflow before collection, invoke only `capture-fast-01-gap-closure.sh --protected-output fast-01-gap-closure-remeasurement.json --endpoint ...`, require the new schema/status/n>=10, then attest and upload that exact path. Do not edit ci.yml or reference the old output path.

    Expand the focused contract with workflow mutations for inputs, non-main ref, pull_request trigger, mutable action refs, widened permissions, missing n gate, old collector/output path, changed subject between attest/upload, and any ci.yml/aggregate edge. Extend `235-COVERAGE.md` with the distinct workflow/collector/artifact names and every external call Plan 13 will make, preserving the existing matrix and hard-stop rules.
  </action>
  <verify>
    <automated>actionlint .github/workflows/fast-01-gap-closure-evidence.yml &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs &amp;&amp; grep -Fq 'fast-01-gap-closure-evidence.yml' .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - Workflow syntax is valid and every protected-producer mutation fails.
    - Only the new remediation-cutoff collector/output namespace is used.
    - The exact subject is both attested and uploaded; undersized populations fail before both operations.
    - ci.yml, required checks, old receipts, and GATE-05 ownership remain unchanged.
    - Coverage describes all calls and AGENTS.md bounds Plan 13 will execute.
  </acceptance_criteria>
  <done>A protected producer is ready for one future independent population, but no sample or completion is claimed.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/ci/capture-fast-01-gap-closure.sh` — new flags `--readiness OUTPUT` and protected-only `--protected-output OUTPUT --endpoint UTC`; new frozen remediation cutoff constants.
- `scripts/ci/capture-fast-01-gap-closure.test.sh` — new fake-gh boundary/pagination/overlap/rate suite.
- `.github/workflows/fast-01-gap-closure-evidence.yml` — new main-only input-free attestation producer.
- `test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs` — new `Sigra.Planning.Phase235Fast01GapClosureContractTest` module.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json` — new `sigra.fast-01-gap-closure-readiness/v1` fields: `authority`, `cutoff`, `window`, `requested_pages`, `rate_limit`, `runs`, `eligible_pr_run_count`, `statistics`, `verdict`, `status`, `diagnostics`.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md` — added external-call rows for the independent producer/capture; no new product API.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Prove headline claims from run data | 11–13 | COVERED | This plan establishes honest independent source; Plan 13 alone may reconcile. |
| REQ | FAST-01 | n>=10, strict p50<720 post-change PR runs | 12–13 | COVERED | Boundaries and producer here; protected result next. |
| REQ | GATE-05 | Preserve exact ownership proof | 12 | COVERED | Evidence workflow outside ci.yml; old verifier/contract retained. |
| RESEARCH | — | Immutable cutoff, complete pages, all conclusions, stable median | 12 | COVERED | Hermetic script and contract. |
| CONTEXT | D-01–D-03 | Honest method/result | 12–13 | COVERED | Null readiness; protected verdict only. |
| CONTEXT | D-04–D-06 | Ownership/docs remain true | 12 | COVERED | No CI ownership topology change. |
| CONTEXT | D-07 | Residual closes only from measured pass | 13 | COVERED | No record edits here. |
| CONTEXT | D-08 | No unrelated work | 12–13 | COVERED | Separate evidence namespace only. |
| VERIFICATION | FAST-01 gap | New protected independent population | 12–13 | COVERED | Remediation cutoff plus no-overlap guard. |

No source item is missing. Automation availability is not completion; an insufficient population remains a visible external precondition for Plan 13.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Remediation receipt → collector cutoff | Local planning data must match protected Git history and immutable blobs. |
| GitHub run pages → readiness/protected subject | External rows can be partial, duplicated, stale, overlapping, or malformed. |
| Readiness → protected dispatch | Non-authoritative state must never become a verdict or select an endpoint. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-12-01 | Tampering | cutoff/independence | high | mitigate | Protected ancestry/time/blob checks, prior endpoint comparison, old-ID disjointness, pinned old digest. |
| T-235-12-02 | Tampering | pagination/population | high | mitigate | Contiguous pages, terminal empty page, unique IDs, bounds, chronology, all conclusions, hermetic mutations. |
| T-235-12-03 | Spoofing | protected producer | high | mitigate | Input-free main-only workflow, immutable action pins, exact collector/output/schema, least privilege. |
| T-235-12-04 | Repudiation | readiness/verdict | high | mitigate | Separate schemas/namespaces; readiness has null statistics/verdict; protected n gate. |
| T-235-12-05 | Denial of service | GitHub API | medium | mitigate | One preflight, core>250, bounded pagination, one transient retry, hard stop on 403/429. |
| T-235-12-06 | Information disclosure | artifacts | low | accept | Public run metadata and cryptographic evidence only; exclude credentials/logs. |
</threat_model>

<verification>
Run the new collector's hermetic suite, old/new focused contracts, metrics semantics, actionlint, live readiness once, and the existing GATE-05 offline verifier. Confirm no old receipt byte or requirement status changed.
</verification>

<success_criteria>
- The independent cutoff is protected, later than the old endpoint, and population IDs cannot overlap.
- Readiness records the real state without statistics/verdict; under ten stays insufficient.
- The main-only producer can attest exactly one future n>=10 subject without joining CI topology.
- FAST-01 remains open and GATE-05 remains Complete after this infrastructure plan.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-12-SUMMARY.md` when done.
</output>
