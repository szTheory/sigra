---
phase: 235-terminal-ratification-measured-not-read
plan: 19
type: execute
wave: 13
depends_on: [235-18]
files_modified:
  - scripts/ci/verify-fast-01-source-complete-attestation-offline.sh
  - test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-02, the source-first oracle retains every literal terminal conclusion key, derives queue-inclusive wall seconds, sorts stably by {wall_seconds, run_id}, selects index floor(n/2), and agrees exactly with the authoritative scripts/ci/ci-run-metrics.sh statistics."
    - "A verifier-level population containing success, failure, and cancelled passes only when the complete source-derived statistics object and instrument statistics object are equal; collapsing cancelled into failure is rejected."
    - "Per D-03 and D-07, the authenticated 52-run result remains n=52 with p50=469 seconds and a strict 469 < 720 pass, while the earlier 772- and 724-second misses and rejected derived-only 466-second candidate remain immutable history."
    - "Per D-04, D-05, and D-06, the protected 93-row GATE-05 ownership artifact, its protected receipt, contributor topology, and independent offline proof remain unchanged and green."
    - "Per D-08, the gap closure changes no retained evidence, closeout record, CI topology, pending todo, product code, package, schema, or unrelated review finding."
  artifacts:
    - path: "scripts/ci/verify-fast-01-source-complete-attestation-offline.sh"
      provides: "Source-first semantic validator whose outcome map uses the authoritative literal-conclusion grouping semantics"
    - path: "test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs"
      provides: "Verifier-level positive and adverse fixtures covering success, failure, and cancelled through full statistics equality"
  key_links:
    - from: "source_collection.pages[*].runs[*].conclusion"
      to: "oracle statistics.outcomes"
      via: "literal conclusion group_by/map/from_entries aggregation identical to scripts/ci/ci-run-metrics.sh"
    - from: "oracle complete statistics"
      to: "statistics and instrument_receipt.output.statistics"
      via: "exact object equality, including mode, ordering, mean, p50, max, and every literal outcome key/count"
    - from: "zero-argument offline verifier path"
      to: "authenticated 52-run retained subject"
      via: "unchanged digest/provenance policy followed by the shared source-first semantic validator"
    - from: "FAST-01 verifier repair"
      to: "GATE-05 protected proof"
      via: "independent terminal-ratification verifier plus existing digest-pin contract"
  prohibitions:
    - "MUST NOT normalize, alias, merge, omit, or synthesize terminal conclusion keys."
    - "MUST NOT mutate retained subjects, attestations, trusted roots, ownership ledgers, protected receipts, contributor docs, requirements, residuals, SEED-005, or CI-PERF records to make verification pass."
---

<objective>
Close the sole current Phase 235 verifier gap by making the source-first offline oracle preserve the authoritative instrument's literal terminal-conclusion map and proving that behavior through the real verifier seam.

Purpose: Make the declared all-conclusion FAST-01 evidence contract true for every valid terminal population without disturbing the already-authenticated result or independently complete GATE-05 proof.
Output: A corrected shared source-first semantic validator and focused positive/adverse verifier fixtures.

Decision coverage: D-01: fixed bounded wall measurement; D-02: queue-inclusive duration, literal outcomes, stable ordering, and floor median; D-03: exact honest evidence; D-04: single ownership-artifact preservation; D-05: protected inventory/receipt preservation; D-06: contributor-topology preservation; D-07: evidence-limited closeout; D-08: fixed scope with no unrelated work.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-REVIEW.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-18-PLAN.md
@scripts/ci/ci-run-metrics.sh
@scripts/ci/verify-fast-01-source-complete-attestation-offline.sh
@test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry success, failure, and cancelled through the production source-first equality path (D-01, D-02, D-03)</name>
  <reversibility rating="reversible">The verifier/test refactor changes no signed evidence or published interface and can be reverted atomically.</reversibility>
  <files>scripts/ci/verify-fast-01-source-complete-attestation-offline.sh, test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</files>
  <read_first>
    - `235-VERIFICATION.md` sole `gaps` entry and Observable Truth 1 — authoritative statement of the current literal-outcome failure.
    - `235-REVIEW.md` CR-01 only — exact incompatible aggregation and required verifier-level fixture; do not fold CR-02 through CR-04 or WR-01 through WR-04 into this task.
    - `scripts/ci/ci-run-metrics.sh` source-page `SOURCE_RESULT` construction — canonical literal-conclusion `group_by`/`map`/`from_entries`, closed window, queue-inclusive wall seconds, `{wall_seconds, run_id}` ordering, `floor(n/2)`, and strict threshold behavior.
    - `scripts/ci/verify-fast-01-source-complete-attestation-offline.sh` source-first jq block — current provenance-first flow and the narrower outcome-map expression to replace.
    - `phase_235_fast_01_source_complete_contract_test.exs` retained-subject and verifier tests — existing fixed-path positive path and mutation conventions.
  </read_first>
  <behavior>
    - "A ten-run fixture containing literal success, failure, and cancelled conclusions produces three distinct outcome keys with their exact counts."
    - "The fixture's complete statistics object equals both the top-level source-derived statistics and instrument_receipt.output.statistics, including mode, ordering, mean_seconds, p50_seconds, max_seconds, and outcomes."
    - "The same source rows paired with a statistics map that folds cancelled into failure fail with source_first_semantic_validation_failed."
    - "Equal wall durations remain separate and ordered by ascending numeric run_id; index floor(n/2) is the p50; p50 719 passes and p50 720 does not."
    - "The ordinary zero-argument verifier still validates the pinned retained subject through provenance and the same semantic validator."
  </behavior>
  <action>
    Start RED by adding a verifier-level fixture in `phase_235_fast_01_source_complete_contract_test.exs` with at least ten bounded terminal PR rows and literal `success`, `failure`, and `cancelled` conclusions. Give it exact authoritative `runs`, `eligible_pr_run_count`, `statistics`, `selected_poles`, `verdict`, `status`, and `instrument_receipt.output` values. Invoke the shell verifier's production-shared semantic validation path on the fixture and assert success. Clone only the fixture's statistics/outcome fields into the formerly accepted two-bucket shape and assert the verifier rejects it with `source_first_semantic_validation_failed`. Keep the existing pinned, network-denied zero-argument verifier test intact.

    Refactor `verify-fast-01-source-complete-attestation-offline.sh` so one named function, `validate_source_first_semantics`, owns the source-first jq expression used by both normal attestation verification and the hermetic fixture seam. Add an explicit `--semantic-fixture PATH` test seam that accepts exactly one non-empty JSON path, runs only that same semantic validator, emits a distinct `source_complete_semantic_fixture_verified` banner on success, and never changes the default fixed subject/bundle/trusted-root path. Reject unknown arguments, missing values, and extra arguments deterministically.

    Inside the shared jq expression, derive `$outcomes` from `$oracle_runs` with the same literal-conclusion `group_by(.conclusion) | map({key: .[0].conclusion, value: length}) | from_entries` semantics as `ci-run-metrics.sh`. Construct the complete oracle statistics object once, require exact equality with both `.statistics` and `.instrument_receipt.output.statistics`, and preserve the existing exact runs, population, selected-pole, status, window, strict `<720`, and pass/miss binding-pole checks. Do not retain or introduce success/non-success bucket arithmetic anywhere in this validator.
  </action>
  <acceptance_criteria>
    - The production-shared verifier path passes a full ten-run fixture whose outcome map contains distinct success, failure, and cancelled keys.
    - The same verifier path rejects the equivalent source population when cancelled is counted as failure.
    - Full statistics equality covers mode, ordering, mean, p50, max, and the complete literal outcome map at both top-level and instrument receipt.
    - Stable `{wall_seconds, run_id}` ordering, `floor(n/2)`, and strict `<720` remain executable fixture predicates per D-02.
    - The pinned zero-argument authenticated verifier path remains intact and green per D-01 and D-03.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</automated>
    <fails_when>the command exits non-zero; ExUnit reports any failure or zero executed tests; the three literal outcome keys are not exercised through the shell verifier; or the collapsed-map adverse fixture is accepted</fails_when>
  </verify>
  <done>The offline verifier and its focused contract agree with the authoritative instrument for a real multi-conclusion population and reject the prior lossy mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock authenticated FAST-01 and independent GATE-05 non-regression (D-04, D-05, D-06, D-07, D-08)</name>
  <reversibility rating="reversible">This adds deterministic guard coverage around existing immutable artifacts and makes no external or data mutation.</reversibility>
  <files>scripts/ci/verify-fast-01-source-complete-attestation-offline.sh, test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</files>
  <read_first>
    - `phase_235_fast_01_source_complete_contract_test.exs` completed-ownership and contributor-topology immutability test — existing digest pins and exact 93-row GATE-05 assertions to preserve.
    - `scripts/ci/verify-terminal-ratification-attestation-offline.sh` — independent protected GATE-05 proof that must remain executable and unchanged.
    - `235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.json` — authenticated n=52, p50=469 subject consumed read-only by the default verifier path.
    - `235-TERMINAL-RATIFICATION.json` and `235-PROTECTED-RECEIPTS.json` — immutable 93-row ownership proof inputs consumed read-only.
  </read_first>
  <behavior>
    - "The zero-argument FAST-01 verifier still returns source_complete_offline_attestation_verified for the authenticated n=52, p50=469 retained subject."
    - "The metrics instrument self-test still proves all literal terminal conclusions, stable equal-duration ordering, floor median, and strict threshold behavior."
    - "The independent terminal-ratification verifier still proves the protected 93-row GATE-05 artifact."
    - "The focused ExUnit contract still rejects any digest drift in the protected ownership ledger, protected receipts, original GATE verifier, or contributor topology."
  </behavior>
  <action>
    Expand the focused contract only where needed to prove that fixture-mode parsing cannot bypass or alter the normal zero-argument provenance path: run both paths explicitly, assert their distinct success banners, and retain the existing subject/trusted-root/workflow/cutoff/endpoint pins. Keep all retained evidence and closeout files read-only.

    Run the authoritative metrics self-test, the corrected default FAST-01 offline verifier, the independent GATE-05 offline verifier, and the focused ExUnit contract together. Confirm the authenticated source-complete subject remains n=52/p50=469 with literal outcomes unchanged, the earlier misses/rejected candidate remain asserted by the existing contract, and the GATE-05 ledger stays exactly 93 rows with its existing digest pins. Per D-08, make no edits for adjacent review warnings or historical cross-check findings.
  </action>
  <acceptance_criteria>
    - The default authenticated FAST-01 verifier, the multi-conclusion fixture path, and the metrics self-test all pass in one deterministic verification run.
    - The independent GATE-05 verifier passes without any changed GATE-05 artifact, receipt, verifier, contributor document, or requirement record.
    - Existing contracts continue to preserve the authenticated 52-run result, the strict 469-second pass, both earlier misses, and the rejected derived-only candidate.
    - `git diff --check` reports no whitespace errors in the only two modified tracked source files.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs &amp;&amp; git diff --check -- scripts/ci/verify-fast-01-source-complete-attestation-offline.sh test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</automated>
    <fails_when>any command exits non-zero; either offline verifier omits its success banner; the metrics suite reports fewer than its existing passing contracts or any failure; ExUnit reports any failure or zero tests; an immutable FAST-01/GATE-05 assertion fails; or git reports whitespace errors</fails_when>
  </verify>
  <done>The literal-conclusion repair is proven against synthetic and authenticated source data while the completed GATE-05 proof and all historical evidence remain independently intact.</done>
</task>

</tasks>

## Flagged assumptions

- **FAST-01 adjacency:** Grounded by D-02 and the current instrument: the measurement window is inclusive at both declared timestamp bounds; p50 719 passes and p50 720 does not because the target is strict `<720`.
- **FAST-01 empty/degenerate:** Grounded by the current source-complete contract: fewer than ten eligible terminal PR rows are insufficient; this gap plan does not change population admission.
- **FAST-01 ordering/stability:** Grounded by D-02 and `ci-run-metrics.sh`: sort ascending by `{wall_seconds, run_id}` and select zero-based index `floor(n/2)`.
- **GATE-05 unclassified:** Flagged-unverified as a human descriptor judgment, but its wired safety predicates are exact and automated: the protected ownership universe remains 93 rows, immutable digest pins remain equal, and the independent offline verifier must pass. No GATE-05 classification is changed here.

## Artifacts this phase produces

- `scripts/ci/verify-fast-01-source-complete-attestation-offline.sh` — new `validate_source_first_semantics` shared function and `--semantic-fixture PATH` hermetic entry point; default attestation/provenance entry point remains unchanged.
- `test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs` — new `success`/`failure`/`cancelled` full-statistics positive fixture, collapsed-outcome adverse fixture, and fixture/default-path separation assertions.
- New files: None.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signed retained subject to local verifier | Attested external run data crosses into local jq validation and milestone claims. |
| Hermetic fixture CLI to shared validator | Test-controlled JSON exercises production semantic logic without authenticating a retained subject. |
| FAST-01 repair to GATE-05 proof | A focused verifier edit must not alter independently protected ownership evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-235-19-01 | Tampering | `validate_source_first_semantics` outcome aggregation | medium | mitigate | Derive literal keys with the authoritative group_by/from_entries expression and require complete object equality at both statistics locations. |
| T-235-19-02 | Elevation of Privilege | `--semantic-fixture` test seam | medium | mitigate | Keep fixture mode explicit and separate, reject malformed arguments, and prove the zero-argument path still performs pinned digest and attestation verification before shared semantic validation. |
| T-235-19-03 | Repudiation | authenticated FAST-01 result | medium | mitigate | Preserve the signed subject, bundle, trusted root, exact pins, n=52/p50=469 tuple, and existing provenance banner unchanged. |
| T-235-19-04 | Tampering | protected GATE-05 evidence | medium | mitigate | Run the independent GATE verifier and retain existing digest-pin and exact-93-row assertions; edit none of its evidence files. |
| T-235-19-SC | Tampering | package supply chain | low | accept | No npm, pip, cargo, Hex, or other package installation occurs in this plan. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Headline claims remain proven from run data and maintainers retain the exact ownership destination map | 235-19 | COVERED | Repairs the only failed measurement-verifier truth and preserves the completed ownership proof. |
| REQ | FAST-01 | Strict under-12-minute p50 across at least ten post-change runs | 235-19 | COVERED | Preserves authenticated n=52/p50=469 and corrects all-conclusion source equality. |
| REQ | GATE-05 | One before/after PR/main/nightly ownership artifact proves no silent drop | 235-19 | COVERED | Non-regression only: independent verifier, 93-row exactness, and digest pins remain green. |
| RESEARCH | — | `ci-run-metrics.sh` remains sole authority; offline replay is comparison-only | 235-19 | COVERED | Shared validator copies the instrument's literal grouping semantics without replacing authority. |
| RESEARCH | — | Machine-readable evidence fails closed under semantic disagreement | 235-19 | COVERED | Positive multi-conclusion fixture and lossy-map adverse fixture execute through the verifier. |
| CONTEXT | D-01 | Fixed bounded wall-mode terminal window | 235-19 | COVERED | Default authenticated path and recorded window remain unchanged. |
| CONTEXT | D-02 | Queue-inclusive duration, stable order, floor median, all conclusions | 235-19 | COVERED | Lifted into must-haves and executable fixture behavior. |
| CONTEXT | D-03 | Exact command/run evidence and honest strict verdict | 235-19 | COVERED | Existing authenticated tuple and historical outcomes remain immutable. |
| CONTEXT | D-04 | Single complete before/after ownership artifact | 235-19 | COVERED | Preserved through independent verifier and exact-row non-regression. |
| CONTEXT | D-05 | Phase 234 inventory, receivers, and execution receipts fail closed | 235-19 | COVERED | Existing protected GATE contract/digests remain untouched and green. |
| CONTEXT | D-06 | Contributor topology matches direct owners and aggregates | 235-19 | COVERED | Existing contributor digest assertion remains green; no docs edit. |
| CONTEXT | D-07 | Closeout limited to supported terminal measurement | 235-19 | COVERED | Repairs verification only; does not rewrite closure records. |
| CONTEXT | D-08 | No pending todos or unrelated work folded in | 235-19 | COVERED | Explicit two-file scope excludes adjacent review findings and deferred ideas. |

<verification>
Run the authoritative metrics self-test, the default authenticated FAST-01 offline verifier, the new multi-conclusion verifier fixture, the independent GATE-05 offline verifier, and the focused Phase 235 contract. The plan passes only when literal conclusion keys survive full statistics equality, the lossy adverse fixture fails, authenticated n=52/p50=469 remains valid, historical results remain immutable, and the 93-row ownership proof remains independently green.
</verification>

<success_criteria>
- `success`, `failure`, and `cancelled` remain distinct keys from signed-source-style rows through exact top-level and instrument statistics equality.
- A map that merges `cancelled` into `failure` is deterministically rejected by the production-shared offline verifier path.
- Queue-inclusive wall duration, stable `{wall_seconds, run_id}` ordering, `floor(n/2)`, and strict `<720` remain unchanged and tested.
- The existing authenticated 52-run, 469-second FAST-01 pass and all earlier misses/rejected evidence remain byte-for-value unchanged.
- GATE-05 remains independently Complete with its original protected 93-row artifact and verifier.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-19-SUMMARY.md` when done.
</output>
