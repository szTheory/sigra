---
phase: 235-terminal-ratification-measured-not-read
plan: 11
type: execute
wave: 11
depends_on: [235-10]
files_modified:
  - mix.exs
  - CONTRIBUTING.md
  - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "FAST-01: the measured binding pole is remediated before another population is opened: protected runs 30828457128/30844334551/30840458645 place Library tests shard at 673/692/698 seconds, and run 30844334551 spends 646 seconds in the literal MIX_ENV=test mix ci step."
    - "D-01/D-02/D-03: mix ci remains the single direct contributor/PR invocation, retains the seven locked legs and all conclusions, and never changes the strict observed_p50_seconds < 720 requirement."
    - "The six repository :scaffold modules execute exactly once: the broad test leg excludes scaffold and ci.install_golden owns the exact six-path receiver set, including golden diff and idempotency, with no missing, duplicate, or extra test path."
    - "A retry-free pull_request run at the remediation head proves the Library tests shard and protected Library tests aggregate execute successfully; the result is evidence for opening a new population, not a FAST-01 pass."
    - "GATE-05 remains independently complete: the direct owner, invocation, aggregate, 93-row ownership proof, protected receipt, and offline verifier do not regress."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: exactly 720 seconds remains a miss; only a p50 strictly below 720 passes."
    - requirement_id: FAST-01
      category: empty
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: remediation evidence with no qualifying PR run cannot authorize a population; later measurement still requires at least ten terminal rows."
    - requirement_id: FAST-01
      category: ordering
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: equal run durations remain ordered by {wall_seconds, run_id}; remediation does not alter measurement ordering."
    - requirement_id: GATE-05
      category: unclassified
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: exact-once ordinary/scaffold path equality and the existing protected ownership verifier are the non-regression surface."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT treat a faster single remediation PR run as FAST-01 completion or mutate/cherry-pick the immutable 13-run 724-second receipt."
      reason: "Descriptor-less flagged-unverified spec-less fallback; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT obtain speed by dropping, duplicating, path-gating, or moving a scaffold test outside the protected Library tests owner."
      reason: "Descriptor-less flagged-unverified spec-less fallback; exact-universe contracts and protected provenance remain mandatory."
  artifacts:
    - path: mix.exs
      provides: "exact-once ordinary/scaffold composition behind the unchanged mix ci interface"
    - path: test/sigra/planning/phase_233_library_economics_contract_test.exs
      provides: "live test-universe equality and single-owner coverage contract"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json
      provides: "source-bound current-pole diagnosis plus retry-free remediation PR receipt and protected-main merge identity"
  key_links:
    - from: mix.exs
      to: .github/workflows/ci.yml
      via: "unchanged library_tests_shard command MIX_ENV=test mix ci"
    - from: test/support/ci/library_test_partitions.exs
      to: mix.exs
      via: "canonical six-path scaffold universe reconciled to test tags and ci.install_golden argv"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-12-PLAN.md
      via: "protected-main remediation merge SHA/time becomes the unbiased lower cutoff"
---

<objective>
Remove the measured Library-tests duplication before opening another FAST-01 population, while preserving the direct `mix ci` contract and GATE-05 ownership.

Purpose: two independent populations missed the strict target (772 seconds and 724 seconds), and the newer median's 692-second Library-tests job spends 646 seconds in the contributor alias; a source-grounded exact-once fix is required before honest remeasurement.
Output: exact-once alias composition, fail-closed coverage contracts/documentation, and a retry-free protected-PR remediation receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-10-SUMMARY.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json
@.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md
@.planning/phases/233-library-suite-economics/233-RESEARCH.md
@.planning/phases/233-library-suite-economics/233-06-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md

<interfaces>
Locked direct command: `MIX_ENV=test mix ci` in `library_tests_shard`; protected aggregate job/name: `library_tests` / `Library tests`.
Current seven alias legs: format check, locked deps get, unused deps check, compile, broad test, ci.install_golden, sigra.dep_off.
Canonical scaffold universe: `Sigra.CI.LibraryTestPartitions` names exactly six `:scaffold` paths; all six tags exist in the live test tree.
Measured current pole: runs 30828457128/30844334551/30840458645 have Library tests shard durations 673/692/698 seconds; run 30844334551 has `Run contributor CI gate` duration 646 seconds and total wall 724 seconds.
Immutable prior authority: `235-FAST-01-REMEASUREMENT.json` remains n=13, p50=724, verdict miss.
</interfaces>
</context>

<flagged_planner_assumptions>
- Probe-source coverage: all four supplied edge rows are surfaced in `must_haves.assumptions`; none is dismissed or dropped.
- Prohibition fallback: two transparency items are descriptor-less and flagged-unverified; no mechanical descriptor is invented.
- API coverage detector: no new external API integration is introduced by Task 1; Task 2 uses the already-inventoried GitHub evidence surface in `235-COVERAGE.md`.
- Assumption-delta detector: `detected:false`; no identity model changes.
- Schema push detector: no ORM/schema path is modified.
</flagged_planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace every scaffold test through one exact-once mix ci execution</name>
  <reversibility rating="costly">The contributor alias is shared by local users and the protected PR owner; rollback is local, but must coordinate alias, docs, and two contracts.</reversibility>
  <files>mix.exs, CONTRIBUTING.md, test/sigra/planning/phase_198_contributor_dx_contract_test.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `mix.exs` — current seven-leg alias and separate `ci.install_golden` receiver.
    - `test/test_helper.exs` and the six files tagged `:scaffold` — prove the broad `mix test` currently includes the receiver class and enumerate actual coverage.
    - `test/support/ci/library_test_partitions.exs` — canonical exact six-path scaffold set and live-universe filters.
    - `test/sigra/planning/phase_198_contributor_dx_contract_test.exs` — locked seven-leg/DX-01 direct invocation contract.
    - `test/sigra/planning/phase_233_library_economics_contract_test.exs` — current sole-owner/aggregate contract.
    - `.github/workflows/ci.yml` — preserve the direct owner, required name, aggregate edge, cache, permissions, and timeout.
    - `CONTRIBUTING.md` — seven-leg explanation that must state exact-once ordinary/scaffold semantics.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md` — D-01 through D-03 non-negotiable local-parity decisions.
  </read_first>
  <behavior>
    - "RED first: a mutation that removes --exclude scaffold from the broad test leg or removes one of the six receiver paths fails."
    - "The ci alias still has exactly seven ordered legs and the workflow still invokes it exactly once with MIX_ENV=test."
    - "The broad test leg selects the ordinary suite with scaffold excluded; ci.install_golden invokes all and only the six live :scaffold paths in one Mix test process."
    - "Every live :scaffold module is present once in the receiver argv; missing/stale/duplicate/untagged/extra paths and a second broad execution fail with named diagnostics."
    - "GATE-05 direct owner library_tests_shard and aggregate library_tests remain unchanged, and no test becomes path-, docs-, or event-gated."
  </behavior>
  <action>
    Start RED in the Phase 198 and Phase 233 contracts. Parse the `ci` and `ci.install_golden` alias lists rather than checking loose tokens. Derive the live scaffold universe from tagged source modules and compare it to `Sigra.CI.LibraryTestPartitions`' canonical set. Add mutations for a missing receiver path, stale path, duplicate path, absent tag, broad leg without scaffold exclusion, reordered/removed locked alias leg, second workflow invocation, and changed required aggregate.

    Change only alias composition: make the fifth `test` leg exclude `:scaffold`, and expand the existing sixth `ci.install_golden` alias to run the exact six canonical repository-relative scaffold paths in one `mix test` process. Keep its name so D-02's installer-golden leg, the seven-leg public contract, and existing local invocation remain stable. Do not introduce a second CI job, matrix, retry, timeout change, success-only selection, or test deletion. Preserve the literal `MIX_ENV=test mix ci` workflow command, job IDs/names, service/cache/toolchain setup, and aggregate `needs` edge.

    Update CONTRIBUTING's seven-leg table to say the broad leg owns ordinary tests and the `ci.install_golden` leg owns the exact six scaffold modules (including golden/idempotency) once. Preserve its direct-owner versus aggregate language and all Playwright/non-PR topology. This implements the measured duplication fix without weakening D-01/D-02 or GATE-05.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; mix format --check-formatted mix.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The alias remains seven ordered legs and one direct workflow invocation.
    - The ordinary/scaffold universes are non-empty, disjoint, and their union equals the repository's current library-test universe after existing load filters.
    - All six tagged scaffold modules execute once through `ci.install_golden`; golden diff and idempotency remain included.
    - All named mutation cases fail, while the Phase 235 terminal ownership contract remains green.
    - `.github/workflows/ci.yml`, required job names, timeout/retry behavior, and immutable prior receipts are unchanged.
  </acceptance_criteria>
  <done>The protected library owner runs the same local `mix ci` interface with exact-once ordinary/scaffold coverage and no GATE-05 ownership change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the remediation on a retry-free PR and freeze its protected-main cutoff</name>
  <precondition>`gh auth status --hostname github.com` succeeds and one `gh api rate_limit` read reports `.resources.core.remaining > 250`.</precondition>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `AGENTS.md` — one-watcher, 60-second interval, summary/log, retry, and rate-limit rules.
    - Task 1's exact diff and focused contracts — subject that the PR must execute.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT.json` — immutable old population and strict miss.
    - `.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md` — owned performance residual and prior pole receipts.
    - `.planning/phases/233-library-suite-economics/233-EVIDENCE.json` — prior parallel/extracted suite observations used only as historical corroboration.
  </read_first>
  <behavior>
    - "The remediation receipt records the three source run/job/step observations, exact commands, source URLs, durations, and the immutable old n=13/p50=724/miss without rewriting it."
    - "One retry-free pull_request run at the exact remediation head has Library tests shard and Library tests success, the literal contributor step present once, and all required Phase 235/GATE-05 checks green."
    - "The remediation run must improve both the 692-second protected-median library job and the 724-second run wall; otherwise execution stops with evidence and does not establish a new cutoff."
    - "After squash auto-merge without bypass, the receipt records the exact origin/main merge SHA, commit timestamp, PR/run/job IDs, head SHA, and immutable file-blob digests."
  </behavior>
  <action>
    Add the closed remediation receipt and contract using the exact schema identifier declared in `Artifacts this phase produces`. Seed its `before` section from read-only structured `gh run view` data for runs 30828457128, 30844334551, and 30840458645, pinning the median run's 692-second Library-tests job, 646-second contributor step, and 724-second run wall. The contract recomputes durations from timestamps and rejects altered IDs, conclusions, timestamps, digests, thresholds, or a changed old receipt digest.

    Create/reuse one scoped evidence branch and PR containing Task 1 plus the receipt/contract, push without force, enable squash auto-merge with `--match-head-commit`, and select the exact `pull_request` CI run by head SHA/event. Use one REST preflight and one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; fetch one structured summary and failed logs only on failure. Do not rerun or manufacture PRs for a favorable result. Retain the exact run/job/step timestamps and require the Library-tests job duration to be strictly below 692 seconds and total run wall strictly below 724 seconds before allowing merge; a non-improvement is a durable remediation failure, not permission to open a new sample.

    After ruleset-governed merge, fetch origin/main, prove the Task 1 blobs match the merged commit, and record that merge SHA/time as `population_cutoff`. Preserve the old receipt byte-for-byte and run the existing terminal/GATE-05 offline verifier. The new cutoff is selected from change identity, never observed timing, and Plan 12 must use it unchanged.
  </action>
  <verify>
    <automated>jq -e '.schema_version == "sigra.fast-01-remediation/v1" and .before.protected_median.run_id == 30844334551 and .before.protected_median.library_job_seconds == 692 and .before.protected_median.run_wall_seconds == 724 and .after.event == "pull_request" and .after.conclusion == "success" and .after.library_job_seconds &lt; 692 and .after.run_wall_seconds &lt; 724 and (.population_cutoff.sha | test("^[0-9a-f]{40}$"))' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh</automated>
  </verify>
  <acceptance_criteria>
    - The receipt is closed-schema, source-bound, and preserves the immutable old receipt/digest and strict miss.
    - Exactly one retry-free remediation-head PR run is selected and watched under AGENTS.md constraints.
    - That run proves all library/scaffold coverage and protected aggregates green, with library job below 692 seconds and run wall below 724 seconds.
    - The protected-main cutoff is the exact merge commit carrying the remediation, not a favorable time/window chosen from results.
    - GATE-05's existing offline verifier and terminal ownership contract pass after merge.
  </acceptance_criteria>
  <done>A source-bound remediation is green on protected CI and its merge identity fixes the lower bound for an independent population; FAST-01 itself remains open.</done>
</task>

</tasks>

## Artifacts this phase produces

- `mix.exs` — modified `ci` leg value `test --exclude scaffold`; expanded `ci.install_golden` exact six-path argv; no new public CLI flag.
- `CONTRIBUTING.md` — updated exact-once ordinary/scaffold semantics under the existing seven-leg `mix ci` interface.
- `test/sigra/planning/phase_198_contributor_dx_contract_test.exs` — new alias-parser assertions for seven ordered legs and direct invocation.
- `test/sigra/planning/phase_233_library_economics_contract_test.exs` — new exact-universe, exact-once, mutation, and remediation-receipt validators; no runtime symbol.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json` — new `sigra.fast-01-remediation/v1` file with fields `before`, `after`, `immutable_prior_receipt`, `source_commands`, `file_digests`, and `population_cutoff`.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Prove headline from run data and show ownership | 11–13 | COVERED | This plan remediates measured pole; later plans collect protected population and reconcile. |
| REQ | FAST-01 | Strict p50 under 720 across at least ten post-change PR runs | 11–13 | COVERED | Remediation precedes new cutoff; no single run claims completion. |
| REQ | GATE-05 | No test silently dropped; exact destination visible | 11 | COVERED | Same job topology plus exact ordinary/scaffold universe and existing protected proof. |
| RESEARCH | — | Wall/all-conclusion/floor median semantics and real run IDs | 11–13 | COVERED | Old receipts retained; new cutoff comes from merge identity. |
| RESEARCH | — | Current binding pole and separate remediation residual | 11 | COVERED | Current structured jobs identify Library tests; residual explicitly assigns reduction. |
| CONTEXT | D-01–D-03 | Immutable honest measurements, strict miss | 11–13 | COVERED | Comparator/old evidence unchanged; no favorable reroll. |
| CONTEXT | D-04–D-06 | Ownership and contributor topology remain truthful | 11 | COVERED | Direct owner/aggregate unchanged; exact alias coverage becomes stronger. |
| CONTEXT | D-07 | Evidence-backed residual | 11–13 | COVERED | Remains open until protected population passes. |
| CONTEXT | D-08 | No unrelated todo/product/UI work | 11–13 | COVERED | Only measured alias duplication and evidence path are in scope. |
| VERIFICATION | FAST-01 gap | New protected independent n>=10 p50<720 population | 12–13 | COVERED | Explicit dependency after this remediation. |

No deferred idea, package install, schema change, UI work, timeout/retry change, test deletion, or preferred-window selection is planned.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Alias selectors → test universe | A selector change could silently omit or duplicate library/scaffold coverage. |
| GitHub PR run → remediation receipt | External run/job timing and identity could be misbound or cherry-picked. |
| Remediation merge → next cutoff | The new population boundary must be tied to protected change identity, not timing results. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-11-01 | Tampering | mix ci test selection | high | mitigate | Exact live ordinary/scaffold set equality, disjointness, union, tag, duplicate, and mutation contracts. |
| T-235-11-02 | Repudiation | coverage/performance claim | high | mitigate | Source-bound run/job/step timestamps and immutable old receipt digest in a closed JSON receipt. |
| T-235-11-03 | Spoofing | PR run identity | high | mitigate | Exact event/head SHA set selection, one watcher, persisted run/job IDs, protected aggregate results. |
| T-235-11-04 | Tampering | next cutoff | high | mitigate | Require origin/main ancestry, exact merged blobs, merge SHA/time, and no timing-derived selection. |
| T-235-11-05 | Denial of service | GitHub observation | medium | mitigate | One rate preflight, one 60-second watcher, one summary, failure-only logs, one transient retry, hard stop on 403/429/core<=250. |
| T-235-11-06 | Information disclosure | retained evidence | low | accept | Retain only public run metadata/digests; no tokens, headers, cookies, or raw authenticated state. |
</threat_model>

<verification>
Run the focused alias/ownership/terminal contracts, formatting, one exact retry-free PR run, and the existing terminal offline verifier. Confirm the immutable old receipt remains n=13/p50=724/miss and FAST-01 stays unchecked.
</verification>

<success_criteria>
- Exact-once test coverage removes the measured duplicate scaffold work without changing the direct `mix ci` interface or protected job topology.
- One protected PR run demonstrates a lower Library-tests duration and total wall, but does not claim FAST-01 completion.
- The remediation merge SHA/time becomes the deterministic cutoff for Plans 12–13.
- GATE-05 remains independently complete and machine-verified.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-11-SUMMARY.md` when done.
</output>
