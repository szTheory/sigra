---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 02
type: execute
wave: 2
depends_on: ["232-01"]
files_modified: []
autonomous: false
requirements: [PW-01]
must_haves:
  truths:
    - "D-03: a real retry-free PW-01 run exists before any PW-02/PW-03 workflow topology edit."
    - "The run preserves gallery assertion, snapshot, project, and readiness coverage and gives PW-01 its own attributable duration receipt."
    - "All five UI-SPEC covered states remain unchanged: gallery loading/populated/overflow and authenticated/responsive navigation loading/overflow."
  assumptions:
    - "FLAGGED/UNRESOLVED PW-01 adjacency, empty, and ordering probes remain recorded in plan 232-01; this gate does not silently resolve them."
  prohibitions:
    - statement: "MUST NOT accept a YAML/config read, retried run, changed assertion count, or post-shard run as PW-01 performance evidence."
      reason: "Bespoke evidence-integrity constraint from the spec-less prohibition probe; descriptor-less and flagged-unverified."
  artifacts: []
  key_links:
    - from: "completed 232-01 commit"
      to: "GitHub Actions design-gallery job"
      via: "an observed pull-request run before ci.yml topology restructuring"
---

<objective>
Pause after PW-01 implementation until the user supplies the ordered real-run receipt required by D-03.

Purpose: make authentication reuse measurable independently of sharding and shared-boot changes.
Output: user-confirmed BEFORE and AFTER-PW-01 run identifiers for plan 232-03.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the ordered PW-01-only CI run exists (D-03)</name>
  <read_first>
    - `232-EVIDENCE.md` — the captured BEFORE-PW-01 slot and exact baseline counts.
    - `232-01-SUMMARY.md` — the commit containing only PW-01 implementation and structural tests.
    - `.github/workflows/ci.yml` — confirm it has not been restructured by Phase 232 yet.
    - `232-VALIDATION.md` Manual-Only Verifications — exact observed-run requirement.
  </read_first>
  <what-built>Three project-specific Playwright setup states and removal of design-spec per-test registration, with no Phase 232 CI topology edit.</what-built>
  <action>Inspect the supplied run with the command in `<verify>`, compare its head ordering and design-gallery logs to the BEFORE slot, and approve only when the run is successful, retry-free, count-identical, and still on the pre-shard topology. This task records no file change and does not create, rename, or switch a branch.</action>
  <how-to-verify>
    Using the existing repository contribution/PR workflow without creating, renaming, or switching a branch in this task, provide:
    1. The BEFORE run id (expected ledger baseline `30390832059`, or a more appropriate same-topology replacement with reason).
    2. A completed AFTER-PW-01 pull-request run id whose head contains 232-01 and no 232-04/232-05 changes.
    3. Confirmation that the design-gallery step passed with `--retries=0` and the same assertion, snapshot, and project counts as BEFORE.
    4. Confirmation that `.github/workflows/ci.yml` still uses the pre-shard topology in that AFTER run.
  </how-to-verify>
  <verify>
    <automated>gh run view &lt;after-pw01-run-id&gt; --json databaseId,event,headSha,conclusion,jobs</automated>
  </verify>
  <acceptance_criteria>
    - The supplied AFTER run resolves, is a pull_request event, and concludes success.
    - Its head includes the completed 232-01 commit and predates every 232-04/232-05 workflow change.
    - The design-gallery command uses `--retries=0` and its passing assertion/snapshot/project counts equal BEFORE.
    - The response includes both run ids and any count/duration discrepancy; no discrepancy is waived.
  </acceptance_criteria>
  <resume-signal>Reply with the BEFORE run id, AFTER-PW-01 run id, and “counts identical”, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Security Enforcement Contract

**ASVS baseline: L1; block threshold: high.** Any high-or-critical finding blocks approval; medium/low findings follow the dispositions below.

| ASVS control area | Gate mitigation | Verification mapping |
|---|---|---|
| V2 Authentication | Accept only the UI-authenticated PW-01 implementation at the approved head. | `gh run view` head/event/conclusion inspection. |
| V3 Session Management | Require all three project states to execute successfully without retries or count drift. | AFTER-PW-01 design-gallery logs and count comparison. |
| V4 Access Control | Require the policy-valid design projects to reach the unchanged admin gallery. | Successful pre-shard design job with identical project/assertion counts. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub run metadata → evidence ledger | External CI observations become durable project claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-232-04 | Repudiation | PW-01 duration claim | high | mitigate | Require named run ids, exact commands, retry-zero execution, equal counts, and pre-topology head ordering. |
</threat_model>

## Artifacts this phase produces

- No files or symbols; this checkpoint produces two validated run identifiers and count confirmation for the next plan.

<verification>
The human supplies a successful pre-topology AFTER run and the automated `gh run view` command resolves it.
</verification>

<success_criteria>
PW-01 has a real, separately attributable observation before downstream topology work starts.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-02-SUMMARY.md` when approved.
</output>
