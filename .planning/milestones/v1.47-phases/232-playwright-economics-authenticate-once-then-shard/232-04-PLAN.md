---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 04
type: execute
wave: 4
depends_on: ["232-03"]
files_modified:
  - .github/actions/example-playwright-boot/action.yml
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_232_playwright_economics_test.exs
autonomous: true
requirements: [PW-03]
must_haves:
  truths:
    - "D-08: one workflow-level component owns checkout/toolchains/cache/compile, DB create/migrate, seeds, browser setup, app boot, and readiness for every app-booting Playwright consumer."
    - "Each consumer retains its event gate, services, permissions, timeout, artifacts, browser choice, port/base URL, log path, and test command."
    - "All five UI-SPEC covered states and Light/Dark/System behavior remain unchanged because no application UI files are edited."
  assumptions:
    - "FLAGGED/UNRESOLVED PW-03 unclassified probe: no plain SPEC defines additional edge semantics for shared-boot consumers; the plan preserves every current consumer and intentional variation exactly."
  prohibitions:
    - statement: "MUST NOT create a second boot definition, silently drop a boot consumer, or redesign an app's behavior while extracting repeated steps."
      reason: "Bespoke drift/coverage prohibition from the spec-less two-stage probe; descriptor-less and flagged-unverified."
  artifacts:
    - path: .github/actions/example-playwright-boot/action.yml
      provides: "sole reusable example Playwright boot prelude"
  key_links:
    - from: .github/workflows/ci.yml
      to: .github/actions/example-playwright-boot/action.yml
      via: "every app-booting Playwright job uses the local composite action with explicit inputs"
---

<objective>
Extract the duplicated example-app boot prelude into one parameterized local composite action after PW-01 evidence is sealed (D-08).

Purpose: prevent setup/migration/seed/browser/readiness drift across Playwright lanes.
Output: one shared boot action and all existing app-booting consumers wired to it.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the one-definition/every-consumer contract (D-08)</name>
  <files>test/sigra/planning/phase_232_playwright_economics_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` — all current app-booting Playwright job ids and their intentional setup variations.
    - `test/sigra/planning/phase_230_design_gallery_split_test.exs` — top-level job extraction and recapture preservation guards.
    - `232-PATTERNS.md` sections for the composite action and Phase 232 contract test.
    - The file listed in `<files>` before editing.
  </read_first>
  <behavior>
    - "exactly one local action defines database migration, seeds, app boot, and readiness."
    - "every enumerated app-booting Playwright job calls that action."
    - "no consumer retains a duplicated boot/migrate/seed/readiness sequence."
  </behavior>
  <action>
    Extend the Phase 232 contract test with the resolved exhaustive example-app-booting Playwright consumer set: `example_playwright_smoke`, `admin_design_recapture`, `admin_checkpoint_recapture`, and `admin_eval_render`. Assert these four literal job ids are present, the local action path exists, declares `runs.using: composite`, owns the canonical prelude markers exactly once, and is used by every enumerated consumer. Scope negative checks to each job body so comments elsewhere cannot satisfy or invalidate them. Run RED before creating the action. Per the resolved research inventory, exclude `example_http_smoke` (curl, not Playwright), `generated_admin_playwright_smoke` (boots a disposable generated host rather than `test/example`), and `install_matrix` (installer smoke).
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_economics_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The new assertions fail against the pre-extraction workflow for the expected missing-action/duplicate-consumer reasons.
    - Consumer enumeration is exactly `example_playwright_smoke`, `admin_design_recapture`, `admin_checkpoint_recapture`, and `admin_eval_render`.
    - Checks are job-region-scoped and non-vacuous; comments cannot make a duplicate-step assertion pass.
  </acceptance_criteria>
  <done>A failing contract precisely names the one boot definition and all required consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Extract and wire the shared boot action (D-08)</name>
  <files>.github/actions/example-playwright-boot/action.yml, .github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` current boot blocks in `example_playwright_smoke`, `admin_design_recapture`, `admin_checkpoint_recapture`, and `admin_eval_render` — transplant their common sequence and preserve variations.
    - `test/example/config/dev.exs` — PGDATABASE, PGPORT, and PORT environment seams.
    - `.github/workflows/ci.yml` Playwright cache logic — preserve browser-set/cache-key behavior from Phases 230-231.
    - `232-RESEARCH.md` Pattern 4 and `232-PATTERNS.md` composite action assignment.
    - Every file listed in `<files>` before editing.
  </read_first>
  <action>
    Create `.github/actions/example-playwright-boot/action.yml` with `runs.using: composite`. Define explicit inputs for database name, app port/base URL, server log path, browser set/install behavior, cache identity, and any current consumer variation proven by the source blocks. Transplant the common checkout/toolchains/cache/dependency/compile/database/migrate/seeds/npm/browser/app/readiness behavior once; use bounded curl readiness and retain route warm-up. Keep job-level PostgreSQL services, permissions, event conditions, timeout-minutes, artifact uploads, and Playwright commands in `ci.yml`.

    Replace the boot blocks in all four resolved consumers (`example_playwright_smoke`, `admin_design_recapture`, `admin_checkpoint_recapture`, `admin_eval_render`) with one use of the local action and explicit inputs. Ensure PORT, PGDATABASE, SIGRA_EXAMPLE_URL, curl target, compile/migration/seed/boot environment, and log path agree for each caller. Do not add packages, retries, `continue-on-error`, UI edits, or runtime auth-prefix configuration.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_economics_test.exs &amp;&amp; mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one composite action contains the canonical boot/migrate/seed/readiness sequence.
    - Every enumerated app-booting Playwright job calls it with internally consistent database/port/base-url/log/browser inputs.
    - No consumer job body retains the extracted sequence, and no event gate, service, artifact, timeout, permission, or Playwright command disappears.
    - The focused and planning suites exit 0; no package manifest/lockfile or application UI file changes.
  </acceptance_criteria>
  <done>PW-03's shared definition exists and every current boot consumer uses it.</done>
</task>

</tasks>

<threat_model>
## Security Enforcement Contract

**ASVS baseline: L1; block threshold: high.** Any high-or-critical finding blocks completion; medium/low findings follow the dispositions below.

| ASVS control area | Task mitigation | Verification mapping |
|---|---|---|
| V2 Authentication | Preserve the existing registration/authentication boot prerequisites in the one shared action. | Literal four-consumer contract plus full planning suite. |
| V3 Session Management | Keep Playwright state handling and artifact boundaries at callers; the action must not upload auth state. | Job-region assertions and unchanged caller artifact contracts. |
| V4 Access Control | Preserve seeds, admin policy inputs, and protected-route warm-up while extracting only duplicated orchestration. | Focused structural test over action markers and all four consumers. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow caller → composite action | Caller-controlled inputs configure DB, app port, cache, browsers, and logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-232-06 | Tampering | composite inputs | medium | mitigate | Explicit inputs, consistent env wiring, fixed local action path, contract-test every consumer. |
| T-232-07 | Denial of Service | readiness loop | low | accept | Preserve the existing bounded poll and job timeout; no unbounded wait introduced. |
</threat_model>

## Artifacts this phase produces

- New path: `.github/actions/example-playwright-boot/action.yml`.
- New action inputs: database, port/base URL, server log, browser set/install behavior, and cache identity required by existing consumers.
- New workflow links: each app-booting Playwright job uses the local action.
- No new application symbols, UI, schemas, runtime flags, or dependencies.

<verification>
Focused structural contracts and the full planning suite prove one definition and every consumer before live-run evidence.
</verification>

<success_criteria>
PW-03 is structurally complete with no consumer drift and no application behavior change.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-04-SUMMARY.md` when done.
</output>
