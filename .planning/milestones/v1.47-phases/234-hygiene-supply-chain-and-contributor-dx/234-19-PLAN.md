---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 19
type: execute
wave: 10
depends_on: [234-18]
files_modified:
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
  - test/sigra/planning/phase_234_playwright_inventory_contract_test.exs
autonomous: true
gap_closure: true
requirements: [DX-04]

must_haves:
  truths:
    - "D-08/D-09: every Playwright inventory row is bound to the exact spec named by that row, not merely to any existing marker in the claimed job."
    - "Direct workflow invocations must contain the row's own repository-relative spec marker; substituting another live spec marker fails closed."
    - "Only the explicitly documented admin-eval and generated-admin harness mappings may use an indirect workflow marker, and each harness must invoke its mapped exact spec."
  artifacts:
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      provides: "exact per-spec invocation ownership plus explicit harness mapping metadata"
    - path: test/sigra/planning/phase_234_playwright_inventory_contract_test.exs
      provides: "spec-aware direct/harness ownership validation and swapped-existing-marker mutations"
  key_links:
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      to: .github/workflows/ci.yml
      via: "the row spec is passed into validate_lane!/4 and matched to its own direct command marker"
    - from: .github/workflows/ci.yml
      to: scripts/ci/admin-eval-harness.sh
      via: "the documented admin-eval harness command resolves to tests/admin-eval.spec.ts"
    - from: .github/workflows/ci.yml
      to: scripts/ci/admin-acceptance-smoke.sh
      via: "the documented generated-admin harness command resolves to tests/admin-generated.spec.ts"
---

<objective>
Close the Playwright ownership hole by proving one inventory spec through its exact executable invocation, then extend that binding across direct and documented harness lanes (D-08, D-09).

Purpose: prevent a spec from borrowing another real marker in the same CI job while still supporting the repository's two deliberate harness indirections.
Output: spec-aware inventory validation, explicit harness metadata, and direct plus harness mutation coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-18-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/admin-eval-harness.sh
@scripts/ci/admin-acceptance-smoke.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind one direct inventory row to its own workflow argv (D-08, D-09)</name>
  <files>test/sigra/planning/phase_234_playwright_inventory_contract_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — `validate_spec!/3`, `validate_lane!/3`, and the current broken-token mutation table.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — direct rows such as `admin-audit.spec.ts` and other real markers available in the same job.
    - `.github/workflows/ci.yml` — anchored `example_playwright_shard` job and `admin_behavior` command block.
    - `234-VERIFICATION.md` — authoritative swapped-existing-marker gap and exact failure reason.
    - Every file listed in `<files>` — establish current behavior before editing.
  </read_first>
  <behavior>
    - "validate_spec!/3 passes the row's spec to lane validation instead of discarding it."
    - "A direct lane is valid only when its command marker equals the exact invocation marker derived from that row's repository-relative spec path and occurs in the claimed job/seam."
    - "Changing admin-audit's marker to the existing admin-theme marker fails with a diagnostic naming admin-audit and the mismatched marker."
  </behavior>
  <action>
    Start with a RED mutation that selects a direct inventory row, replaces only its `command_marker` with another existing spec marker from the same claimed job, and asserts validation fails with both the owned spec and substituted marker named. Pass `spec` from `validate_spec!/3` into a spec-aware lane validator. For ordinary lanes, derive the expected workflow marker by removing the fixed `test/example/priv/playwright/` prefix from the row's spec and require exact equality with `command_marker` before checking the marker inside the anchored job and seam. Retain the live-glob exact-set, duplicate, empty-owner, sorted-event, project, config, and workflow-job checks. Do not make file-wide marker presence sufficient and do not change workflow topology, specs, Playwright configuration, retries, workers, UI code, or snapshots.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The swapped-existing-marker mutation is observed RED before the validator change and GREEN afterward because the mutation is rejected.
    - A direct row's `command_marker` must equal its own `tests/*.spec.ts` path and occur in the anchored claimed job/seam.
    - Existing missing/stale/duplicate/unowned/missing-job/missing-command/missing-config mutations remain green as negative tests.
  </acceptance_criteria>
  <done>One direct spec-to-workflow path is proven end to end, and a valid sibling marker cannot impersonate it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admit only the two documented exact-spec harness mappings (D-08)</name>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json, test/sigra/planning/phase_234_playwright_inventory_contract_test.exs</files>
  <read_first>
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — the `admin-eval.spec.ts` and `admin-generated.spec.ts` indirect rows.
    - `scripts/ci/admin-eval-harness.sh` — exact `tests/admin-eval.spec.ts` Playwright invocation.
    - `scripts/ci/admin-acceptance-smoke.sh` — `PLAYWRIGHT_SPEC` binding to `tests/admin-generated.spec.ts` and the workflow's `--test all` entry.
    - `.github/workflows/ci.yml` — `admin_eval_render` and `generated_admin_playwright_smoke` anchored jobs.
    - `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — Task 1 direct-binding implementation.
    - Every file listed in `<files>` — establish current content before editing.
  </read_first>
  <behavior>
    - "The inventory explicitly labels the admin-eval and generated-admin rows with harness path and exact harness spec marker."
    - "An indirect row passes only if it is one of the two code-allowlisted mappings, the workflow invokes its exact harness command, and that harness source invokes the row's exact spec."
    - "Changing a harness row to another existing harness/spec marker or adding indirection to an ordinary row fails closed."
  </behavior>
  <action>
    Add explicit `harness_path` and `harness_spec_marker` metadata only to the `admin-eval.spec.ts` and `admin-generated.spec.ts` lane rows. In the contract, define the exact two allowed spec-to-harness mappings and branch validation only when those fields are present. Require the row spec, workflow `command_marker`, harness path, and harness spec marker to equal the allowlisted tuple; read the harness path from the repository and require its exact spec invocation marker. Reject missing/extra harness fields, any third indirect spec, and any swapped existing harness or spec marker. Keep direct rows on Task 1's strict equality path. Add mutations for swapped direct markers, swapped harness mapping, and unauthorized indirection; diagnostics must name the affected spec and expected invocation. This is a repository contract only: do not modify either harness, CI workflow, Playwright spec/config, or browser behavior.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs &amp;&amp; mix test test/sigra/planning/phase_232_playwright_economics_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Exactly two inventory lanes carry harness metadata, for `admin-eval.spec.ts` and `admin-generated.spec.ts` only.
    - Each allowed workflow command resolves through its named harness to that row's exact spec marker.
    - Swapped-existing direct markers, swapped harness mappings, unauthorized third-party indirection, and broken harness spec markers all fail with named diagnostics.
    - The full live-spec inventory and Phase 232 topology contracts pass without workflow/spec/config changes.
  </acceptance_criteria>
  <done>Every inventory row proves its exact invocation, with only two explicit and mechanically resolved harness exceptions.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — adds `harness_path` and `harness_spec_marker` only to the existing admin-eval and generated-admin lane objects; no new path is created.
- `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — modifies `validate_spec!`/lane validation to carry the spec, introduces the exact two-entry documented harness mapping, and adds swapped-existing-marker, swapped-harness, and unauthorized-indirection mutations.

## Multi-Source Coverage Audit

| Source | ID | Gap item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Playwright coverage must not drift silently | 19 | COVERED | Exact spec-to-invocation binding replaces job-wide marker presence. |
| REQ | DX-04 | Every Playwright spec has an executable CI owner | 19 | COVERED | Direct and explicit harness paths are checked end to end. |
| RESEARCH | — | Fail-closed live inventory and executable lane seams | 19 | COVERED | Existing inventory/ExUnit seam is strengthened without new tooling. |
| CONTEXT | D-08 | Mechanically checked exhaustive inventory | 19 | COVERED | Exact-set behavior remains and per-row ownership is made substantive. |
| CONTEXT | D-09 | Preserve and wire the two useful orphan specs | 19 | COVERED | Existing wiring remains; its exact invocation becomes mutation-protected. |

Deferred D-11/D-12 ideas remain excluded: this plan changes no canary, release-note, required-check, retry, UI, schema, or external API policy.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| inventory JSON → CI coverage claim | Repository metadata claims that a named browser spec executes in a particular lane. |
| workflow command → repository harness | Two indirect commands cross into shell harnesses that select the actual Playwright spec. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-19-01 | Spoofing | direct inventory row | high | mitigate | Derive the expected marker from the row's own spec and reject a different existing marker before job lookup. |
| T-234-19-02 | Tampering | harness mapping | high | mitigate | Permit exactly two code-allowlisted mappings and resolve both workflow-to-harness and harness-to-spec links. |
| T-234-19-03 | Repudiation | DX-04 ownership claim | high | mitigate | Mutation tests name swapped direct/harness markers and the affected spec; live-set equality remains mandatory. |
| T-234-19-04 | Denial of service | inventory parser | low | accept | The bounded 20-row local inventory uses repository files only; focused ExUnit execution limits resource exposure. |

## ASVS L1 Automated Controls

- V5.1 input validation: every untrusted JSON lane field is checked against an exact direct or allowlisted harness contract.
- V7.1 evidence integrity: a row cannot claim ownership from a sibling marker or unresolved harness.
</threat_model>

<verification>
The Phase 234 inventory contract and Phase 232 topology contract pass; direct-marker and both harness-indirection mutation families demonstrably fail closed.
</verification>

<success_criteria>
DX-04 is mechanically true: each live Playwright spec is tied to its exact direct or explicitly documented harness invocation, and a swapped real marker cannot preserve a green claim.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-19-SUMMARY.md` when done.
</output>
