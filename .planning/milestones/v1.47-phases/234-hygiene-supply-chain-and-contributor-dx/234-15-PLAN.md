---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 15
type: execute
wave: 7
depends_on: [234-14]
files_modified:
  - mix.exs
  - scripts/ci/sigra-dep-off.sh
  - scripts/ci/test-sigra-dep-off.sh
  - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
  - test/fixtures/install_golden/tree/config/dev.exs
autonomous: true
gap_closure: true
requirements: [DX-01]

must_haves:
  truths:
    - "D-01/D-02: a contributor can run the seven-leg MIX_ENV=test mix ci path without leaving mix.lock or the tracked worktree changed after the dep-off leg succeeds or fails."
    - "D-04: the committed config/dev.exs golden fixture is byte-identical to current locked-phx_new installer output and the golden/idempotency command exits zero."
    - "The dep-off cleanup restores the original lock bytes and a usable threadline dependency/build before returning the original leg result."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-01 unclassified edge: local safety is defined as byte-identical mix.lock, identical tracked/untracked status, and successful post-cleanup dependency resolution on both success and injected-failure paths; no broader platform guarantee is inferred."
  prohibitions:
    - "Contributor changes and the current branch are preserved; execution creates or switches no repository branch."
    - "A missing, red, malformed, skipped, or unproven gate result never becomes passing evidence."
    - "Credo, Dialyzer, mix_audit, retry machinery, and generated-tree formatter ownership are not added."
  artifacts:
    - path: scripts/ci/sigra-dep-off.sh
      provides: "failure-safe dep-off execution with dependency and lock restoration"
    - path: scripts/ci/test-sigra-dep-off.sh
      provides: "hermetic success/failure cleanup regression proof"
    - path: test/fixtures/install_golden/tree/config/dev.exs
      provides: "current canonical generated dev configuration"
  key_links:
    - from: mix.exs
      to: scripts/ci/sigra-dep-off.sh
      via: "the existing sigra.dep_off alias delegates its destructive leg to the cleanup harness"
      pattern: "sigra-dep-off\\.sh"
    - from: scripts/ci/sigra-dep-off.sh
      to: mix.lock
      via: "snapshot, trap/finalizer restoration, deps.get --check-locked, and threadline recompilation"
    - from: test/fixtures/install_golden/tree/config/dev.exs
      to: test/sigra/install/golden_diff_test.exs
      via: "byte-for-byte normalized generated-tree comparison"
---

<objective>
Repair the end-to-end contributor gate so one real `mix ci` path is non-destructive and golden-clean (D-01, D-02, D-04).

Purpose: remove the two local-gate failures that prevent a contributor from safely reproducing the PR gate.
Output: a cleanup-safe dep-off harness, hermetic failure-path coverage, the reconciled dev-config golden, and green focused gate commands.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-05-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-09-SUMMARY.md
@mix.exs
@lib/sigra/install/features/core.ex
@test/sigra/install/golden_diff_test.exs
@test/sigra/install/idempotency_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one dep-off run through cleanup and back to an unchanged contributor checkout (D-01, D-02)</name>
  <files>mix.exs, scripts/ci/sigra-dep-off.sh, scripts/ci/test-sigra-dep-off.sh, test/sigra/planning/phase_198_contributor_dx_contract_test.exs</files>
  <read_first>
    - `mix.exs` aliases/0 — preserve the seven ordered `ci` legs and the public `sigra.dep_off` alias name.
    - `test/sigra/planning/phase_198_contributor_dx_contract_test.exs` — extend the existing DX-01 contract rather than adding a competing parity definition.
    - `scripts/ci/` — follow existing `set -euo pipefail`, repository-root resolution, temporary-directory, and self-test conventions.
    - `234-VERIFICATION.md` — the root cause is both lockfile mutation and unrestored dependency/build state.
    - Every file listed in `<files>` — establish current content or confirmed absence before editing.
  </read_first>
  <behavior>
    - "A successful dep-off run returns zero, restores the exact pre-run mix.lock bytes, resolves the locked dependency set, and recompiles threadline."
    - "A deterministic injected command failure returns nonzero while still performing the same lock/dependency restoration."
    - "The public alias remains a leg of mix ci and no second dep-off implementation survives."
  </behavior>
  <action>
    Write `scripts/ci/test-sigra-dep-off.sh` first so it creates a temporary fixture repository, places a deterministic stub `mix` earlier on `PATH`, exercises both success and forced-failure command sequences, and asserts the original lock hash/content plus dependency-build marker are restored in both cases. It must also assert the failure exit status is preserved. Observe RED before the harness exists.

    Implement `scripts/ci/sigra-dep-off.sh` as the single destructive boundary. Resolve the repository root, require an existing `mix.lock`, copy its bytes and metadata into a `mktemp -d` directory, and install cleanup for normal exit, error, INT, and TERM. Run the existing unlock, build-clean, warnings-as-errors no-deps-check compile, and tagged threadline-guard test in their current order. Cleanup must atomically restore the saved lockfile, clear Mix dependency caches by using fresh Mix subprocesses, run `MIX_ENV=test mix deps.get --check-locked`, and compile `threadline`; if cleanup fails, return nonzero even when the guard passed, otherwise retain the original guard status. Remove only the harness-owned temporary directory.

    Keep `sigra.dep_off` in `mix.exs`, but make it delegate exactly once to the harness through Mix's `cmd` alias form; retain the seven ordered `ci` legs per D-02. Extend the Phase 198 contract to require that link, the cleanup/self-test files, and the absence of the old inline unlock sequence from `mix.exs`. Do not add retries or broaden the gate families per D-03/D-12.
  </action>
  <verify>
    <automated>bash scripts/ci/test-sigra-dep-off.sh &amp;&amp; mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The hermetic harness is observed RED before implementation and GREEN afterward.
    - Success and injected-failure cases both restore byte-identical lock content and a usable dependency/build marker; the injected failure remains nonzero.
    - `mix.exs` retains all seven ordered D-02 legs and delegates `sigra.dep_off` to exactly one cleanup harness.
    - No contributor branch, unrelated file, retry policy, or additional gate family is changed.
  </acceptance_criteria>
  <done>The contributor-visible dep-off path executes real checks and returns through cleanup without changing repository dependency state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile the generated dev-config golden and prove the installer gate (D-04)</name>
  <precondition>Docker/Postgres tooling is available and the documented phx_new 1.8.8 archive is installed; verify with read-only version/archive-list commands.</precondition>
  <files>test/fixtures/install_golden/tree/config/dev.exs</files>
  <read_first>
    - `test/fixtures/install_golden/tree/config/dev.exs` — stale expected bytes, including the Sigra Swoosh injection.
    - `lib/sigra/install/features/core.ex` `swoosh_instructions/2` — canonical Sigra-owned append operation.
    - `test/support/install_fixture.ex` — locked Phoenix scaffold setup and normalization path used by the golden test.
    - `test/sigra/install/golden_diff_test.exs` — byte-identity failure and regeneration runbook reference.
    - `test/sigra/install/idempotency_test.exs` — second-run zero-write/zero-touch invariant.
    - `.formatter.exs` — D-04 exclusion that keeps generated fixture bytes outside formatter ownership.
    - Every file listed in `<files>` — establish the stale content before editing.
  </read_first>
  <behavior>
    - "The locked phx_new 1.8.8 scaffold plus current Sigra installer produces config/dev.exs identical to the committed fixture."
    - "Running the installer twice leaves generated file content and mtimes unchanged."
    - "The golden reconciliation changes only the proven generated dev-config fixture bytes."
  </behavior>
  <action>
    Run the focused golden test first and retain its unified `config/dev.exs` diff as the RED proof. Regenerate the fixture through the repository's existing `InstallFixture.setup_tmp_app/1` and normalization/runbook seam using the documented phx_new 1.8.8 archive; do not hand-compose Phoenix scaffold content. Replace only `test/fixtures/install_golden/tree/config/dev.exs` with the normalized generated result containing the current Sigra Swoosh block. Review the scoped diff against `swoosh_instructions/2`; if any other generated path differs, keep it out of the commit and record the unexpected path as a blocking diagnostic rather than changing wider golden ownership.

    Run golden diff and idempotency together, then run the repository formatter and the dep-off self-test. Confirm the fixture remains excluded from formatter inputs and no source/template behavior was changed merely to match stale bytes.
  </action>
  <verify>
    <automated>mix test test/sigra/install/golden_diff_test.exs test/sigra/install/idempotency_test.exs &amp;&amp; mix format --check-formatted &amp;&amp; bash scripts/ci/test-sigra-dep-off.sh</automated>
  </verify>
  <acceptance_criteria>
    - The pre-change command fails on `config/dev.exs`; the post-change command exits zero for both golden and idempotency tests.
    - The fixture is produced through the locked scaffold/installer seam and contains the current Sigra Swoosh injection exactly once.
    - The scoped task commit changes only `test/fixtures/install_golden/tree/config/dev.exs`; formatter ownership remains excluded.
  </acceptance_criteria>
  <done>The required golden/idempotency proof exits zero against current deterministic generator output.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/ci/sigra-dep-off.sh` — new production cleanup boundary for the existing `sigra.dep_off` alias.
- `scripts/ci/test-sigra-dep-off.sh` — new hermetic success/failure cleanup regression harness.
- `mix.exs` — modifies `sigra.dep_off` implementation wiring while preserving its public alias and the ordered `ci` legs.
- `test/sigra/planning/phase_198_contributor_dx_contract_test.exs` — extends DX-01 structural coverage to the cleanup harness and removal of the inline destructive sequence.
- `test/fixtures/install_golden/tree/config/dev.exs` — updates the existing golden artifact to current locked-scaffold plus Sigra installer output; no new symbol is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| contributor checkout → dep-off subprocesses | The gate deliberately mutates dependency resolution and build state before returning control. |
| scaffold/installer output → committed golden fixture | Generated bytes become a repository trust anchor for later installer changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-15-01 | Tampering | `mix.lock` / dependency build | high | mitigate | Snapshot plus unconditional cleanup, locked resolution, threadline recompilation, and success/failure self-tests prove restoration. |
| T-234-15-02 | Denial of service | cleanup failure | high | mitigate | Cleanup failure overrides a successful guard result; signals are trapped and the original nonzero result is preserved when cleanup succeeds. |
| T-234-15-03 | Tampering | generated `config/dev.exs` | high | mitigate | Regenerate only through the locked scaffold/installer seam and require golden plus idempotency tests. |
| T-234-15-04 | Elevation of privilege | shell path/temporary files | high | mitigate | Resolve a fixed repository root, quote all paths, use `mktemp -d`, and remove only the harness-owned directory. |

## ASVS L1 Control Mapping and Automated Verification

| ASVS intent | Control | Automated assertion |
|---|---|---|
| V5.1 input validation | Fixed paths and explicit command sequences; no repository text is evaluated as shell. | Hermetic stub-runner test exercises success and failure without interpolating untrusted data. |
| V7.1 integrity logging | A failed check or cleanup cannot be reported as success. | Self-test asserts exact exit propagation and restoration invariants. |
</threat_model>

<verification>
The dep-off self-test, Phase 198 contract, repository formatter, and golden/idempotency command all pass; scoped diffs show no unrelated or generated-tree ownership change.
</verification>

<success_criteria>
DX-01's local gate is safe to run in a contributor checkout and its previously red generated-install proof is green.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-15-SUMMARY.md` when done.
</output>
