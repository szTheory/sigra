---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 18
type: execute
wave: 9
depends_on: [234-16]
files_modified:
  - test/sigra/planning/phase_234_evidence_contract_test.exs
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [DX-01, DX-02, DX-03, DX-04, DX-06]

must_haves:
  truths:
    - "Completion authorization and validation-artifact parsing call one shared validator for the exact five-command receipt inventory."
    - "Empty, missing, extra, duplicate, malformed, stale, or nonzero command receipts force status draft, nyquist_compliant false, and wave_0_complete false."
    - "234-VALIDATION.md changes to complete/true/true only after structural, local-command, and GitHub-service evidence is currently green."
    - "Already verified DX-02, DX-04, and DX-06 are consumed through their immutable existing receipts and focused contracts without reopening their implementation."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-01 edge: the recorded clean-checkout receipt and exact local commands are the bounded reproducibility proof."
    - "FLAGGED/UNRESOLVED DX-02 edge: the existing successful post-pin main job remains authoritative only while its immutable pin, merge SHA, run/job IDs, permissions, and hashes validate."
    - "FLAGGED/UNRESOLVED DX-03 edge: only the three authenticated processed-job rows from 234-17 count as service proof."
    - "FLAGGED/UNRESOLVED DX-04 concurrency edge: one commit snapshot is validated atomically; interrupted or parallel execution cannot authorize completion because missing/mismatched artifact and receipt sets fail closed."
    - "FLAGGED/UNRESOLVED DX-06 edge: the existing current and historical gallery receipts are consumed as recorded; contradictory evidence blocks rather than reopening UI remediation."
  prohibitions:
    - "No empty, partial, inferred, skipped, red, malformed, or stale evidence set authorizes complete/true/true."
    - "Tokens, cookies, environment values, and raw command/service logs are not committed; only sanitized SHA-256 receipts and immutable identifiers are retained."
    - "Verified action pinning, Playwright ownership, gallery behavior, required-check policy, UI behavior, and deferred todo boundaries are not modified."
  artifacts:
    - path: test/sigra/planning/phase_234_evidence_contract_test.exs
      provides: "shared exact command-receipt validator and fail-closed transition mutations"
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      provides: "machine-ratified final validation state or durable blocked diagnostics"
  key_links:
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      to: test/sigra/planning/phase_234_evidence_contract_test.exs
      via: "artifact parser and transition authorization share validate_command_receipts!/1"
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      to: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      via: "all required structural/local/service slots pass before frontmatter transition"
---

<objective>
Close the final authorization bypass and ratify Phase 234 only from the exact green evidence inventory.

Purpose: ensure the validation artifact cannot claim completion through vacuous or malformed command receipts.
Output: one shared receipt validator, empty/missing/malformed mutation coverage, fresh command receipts, and truthful final validation frontmatter.
</objective>

<execution_context>
@/Users/developer/.codex/gsd-core/workflows/execute-plan.md
@/Users/developer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-16-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-06-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-08-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share exact command-receipt validation with transition authorization</name>
  <files>test/sigra/planning/phase_234_evidence_contract_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` `@quick_run_paths`, `parse_command_receipts!/1`, `valid_command_receipt?/1`, and `assert_transition_allowed!/3` — remove the vacuous `Enum.all?/2` authorization path.
    - `234-VALIDATION.md` Command Receipts table — exact five-command order and field encoding.
    - `234-EVIDENCE.json` — all required service/local evidence schemas.
    - `234-VERIFICATION.md` — authoritative empty/missing/malformed mutation gap.
    - Every file listed in `<files>` — establish current behavior before editing.
  </read_first>
  <behavior>
    - "The exact ordered command keys are: final_evidence tagged test; six-file focused quick run; planning suite; repository formatter; clean-golden-tree plus golden/idempotency test."
    - "Every row has one exact command, UTC completion timestamp, exit 0, and lowercase 64-character output hash."
    - "Both parse_validation!/1 and assert_transition_allowed!/3 invoke the same validator and reject empty, missing, extra, duplicate, reordered, malformed, stale-command, and nonzero rows."
  </behavior>
  <action>
    Define one exact ordered command inventory constant from the five commands already listed in `234-VALIDATION.md`. Implement `validate_command_receipts!/1` so it requires list length five, exact command/order equality, unique commands, valid UTC timestamps, exit status `0`, and lowercase 64-character hashes. Call it from `parse_validation!/1` immediately after parsing and from `assert_transition_allowed!/3` before evaluating evidence/frontmatter; do not retain a separate weaker `Enum.all?/2` path.

    Add table-driven mutation tests for zero rows, each single missing command, extra command, duplicate, reordered rows, empty command, malformed timestamp, malformed hash, nonzero exit, and a stale-but-well-formed command. Each mutation must assert blocked draft fields or a named validation failure. Retain the exact quick-run, Wave 0, evidence-slot, immutable receipt, and stale-path gates already verified by Plan 14.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff</automated>
  </verify>
  <acceptance_criteria>
    - One validator owns both artifact parsing and transition authorization.
    - The exact five commands/order are required; every valid row is non-empty, current-shaped, timestamped, exit zero, and hashed.
    - Empty, each missing-row case, extra/duplicate/reordered, malformed-field, stale-command, and nonzero mutations fail.
    - The fully green in-memory case alone authorizes complete/true/true; every incomplete case requires draft/false/false.
  </acceptance_criteria>
  <done>Completion authorization has no vacuous command-receipt path and shares the artifact's exact inventory contract.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the exact proof inventory and ratify only a fully green validation artifact (D-01–D-12)</name>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md</files>
  <read_first>
    - `234-VALIDATION.md` — preserve the test map/Wave 0 rows and replace stale command receipts with current executions.
    - `234-EVIDENCE.json` — require every local, PR, release, Dependabot, gallery, and historical-gallery slot successful before mutation.
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` — Task 1 exact inventory and transition contract.
    - Existing Plan 06/08/10 summaries — consume verified DX-02/DX-04/DX-06 receipts without changing their implementation.
    - Every file listed in `<files>` — establish current draft/diagnostic state before editing.
  </read_first>
  <action>
    Run the exact five commands in the Task 1 inventory in their declared order. Capture UTC completion, exit status, and a SHA-256 of sanitized output for each; discard raw logs after hashing. Require every command to exit zero, including the clean-golden-tree plus golden/idempotency command. Re-run the tagged validation-signoff test after populating the receipt table.

    Validate all evidence slots: use the refreshed safe local receipt from 234-16, authenticated Dependabot rows from 234-17, and the existing immutable PR/release/Playwright/gallery receipts for DX-02/DX-04/DX-06. Only when all evidence validators, exact command inventory, Wave 0 rows, task rows, and mutation tests pass may `234-VALIDATION.md` change to `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true` with a machine-evidence approval timestamp/SHA. Otherwise retain draft/false/false and replace Approval with durable diagnostics naming every red command/slot; never waive a missing service receipt.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff &amp;&amp; mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only final_evidence</automated>
  </verify>
  <acceptance_criteria>
    - The Validation table contains exactly five fresh, unique, ordered, exit-zero rows with UTC timestamps and sanitized lowercase SHA-256 values.
    - All structural, local-command, and GitHub-service validators are green at one commit snapshot.
    - Frontmatter is complete/true/true only after those checks pass; otherwise it remains draft/false/false with durable diagnostics.
    - No DX-02, DX-04, DX-06 implementation, UI file, workflow topology, or deferred todo is modified.
  </acceptance_criteria>
  <done>Phase 234 validation is ratified from complete current evidence, or remains explicitly fail-closed with machine-readable diagnostics.</done>
</task>

</tasks>

## Artifacts this phase produces

- `test/sigra/planning/phase_234_evidence_contract_test.exs` — adds `validate_command_receipts!/1`, exact five-command inventory, and empty/missing/extra/duplicate/reordered/malformed/stale/nonzero mutation cases; existing evidence validators remain the service/local proof seams.
- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md` — replaces command receipts with fresh exact green rows and changes completion frontmatter only if the shared transition guard authorizes it.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Gap Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Contributor reproduces the gate and dependency/action surfaces do not drift silently | 15–18 | COVERED | Safe local execution, authenticated dependency receipts, and fail-closed ratification. |
| REQ | DX-01 | `mix ci` reproduces the PR gate including formatting and lock checks | 15, 16, 18 | COVERED | Repairs destructive dep-off behavior, golden drift, current local evidence, and sign-off. |
| REQ | DX-02 | Release-critical Actions are immutable | 18 | COVERED | Consumes the already verified exact pin/main receipt; implementation is not reopened. |
| REQ | DX-03 | Dependabot covers Actions, Mix, and npm | 17, 18 | COVERED | Captures exact authenticated processed-job rows and gates sign-off. |
| REQ | DX-04 | Every Playwright spec has an executable owner | 18 | COVERED | Consumes the already verified inventory receipt; concurrency assumption is explicit. |
| REQ | DX-06 | SEED-006 delivered or residual filed | 18 | COVERED | Consumes existing current/historical gallery receipts without UI remediation. |
| RESEARCH | — | Native Mix checks and alias-as-CI-call-through; no package install | 15, 16 | COVERED | Existing tools and direct alias remain authoritative. |
| RESEARCH | — | Structural proof is separate from managed-service proof | 16, 17, 18 | COVERED | Local execution and GitHub receipts remain independently typed. |
| RESEARCH | — | Fail-closed live-universe/exact-set reconciliation | 17, 18 | COVERED | Exact Dependabot tuples and command inventory reject drift. |
| CONTEXT | D-01–D-04 | Single seven-leg gate and golden-owned generator output | 15, 16, 18 | COVERED | Safety and golden gaps are directly repaired and re-executed. |
| CONTEXT | D-05–D-06 | Exact immutable action pin and pinning policy | 18 | COVERED | Existing verified receipt/contract are required by final sign-off. |
| CONTEXT | D-07 | Exact three weekly Dependabot tuples plus GitHub processing proof | 17, 18 | COVERED | Authenticated job receipts are mandatory. |
| CONTEXT | D-08, D-09, D-10 | Playwright inventory/orphans and SEED-006 closure | 18 | COVERED | Existing verified receipts are consumed, not reopened. |
| CONTEXT | D-11–D-12 | Exclude adjacent todos and retry machinery | 15, 17, 18 | COVERED | Scope fences and GitHub transient-only retry rule are explicit. |

Deferred Ideas from `234-CONTEXT.md` remain excluded: canary-recapture policy, release-note convention, absent `example_unit_smoke` aggregator ownership, and transient Hex/rebar retry work are not gap items.

## No-New-External-API Declaration

Phase 234 adds no product API client, endpoint, credential surface, webhook, schema capability, or external integration contract. GitHub Actions and Dependabot remain repository-managed delivery services used only to collect execution evidence, so no external-API capability matrix is applicable; their trust boundaries are fully represented in Plans 17–18 threat models and receipt schemas.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Markdown/JSON evidence → completion authorization | Repository-controlled artifacts may be empty, stale, malformed, or selectively edited. |
| local/service outputs → receipt table | Outputs can contain secrets and may not represent the exact required command or service tuple. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-18-01 | Tampering | command receipt inventory | high | mitigate | One exact-set validator is called by parsing and authorization; mutation tests cover every structural bypass. |
| T-234-18-02 | Repudiation | validation transition | high | mitigate | Require all immutable evidence IDs/hashes, five current command receipts, and one commit snapshot before frontmatter changes. |
| T-234-18-03 | Information disclosure | local/remote output | high | mitigate | Persist sanitized hashes and public identifiers only; discard raw logs and forbid session/environment material. |
| T-234-18-04 | Denial of service | interrupted/parallel sign-off | high | mitigate | Atomic exact-set validation makes partial tables/evidence block; only the final scoped commit changes authorization fields. |

## ASVS L1 Control Mapping and Automated Verification

| ASVS intent | Control | Automated assertion |
|---|---|---|
| V5.1 input validation | Shared strict parser for command and evidence fields. | Empty/missing/extra/duplicate/reordered/malformed/stale/nonzero mutations all fail. |
| V7.1 logging/evidence integrity | Immutable IDs, UTC timestamps, exact commands, and sanitized hashes. | Final tagged tests accept only a complete green snapshot. |
</threat_model>

<verification>
The validation-signoff and final-evidence tags pass against the persisted artifacts; the exact five commands are green and every negative mutation remains fail-closed.
</verification>

<success_criteria>
The completion bypass is closed, all five phase requirements remain evidenced, and `234-VALIDATION.md` is complete/true/true only from an exact current green inventory.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-18-SUMMARY.md` when done.
</output>
