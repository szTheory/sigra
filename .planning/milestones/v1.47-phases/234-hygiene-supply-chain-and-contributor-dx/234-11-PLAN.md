---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 11
type: execute
wave: 2
depends_on: [234-01]
files_modified:
  - lib/sigra/enterprise_routing.ex
  - lib/sigra/install/features/organizations.ex
  - lib/sigra/integrations/chimeway.ex
  - lib/sigra/jwt.ex
  - lib/sigra/jwt/refresh_token.ex
  - lib/sigra/mfa/trust.ex
  - lib/sigra/oauth.ex
  - lib/sigra/oauth/callback.ex
autonomous: true
requirements: [DX-01]
must_haves:
  truths:
    - "D-04: the complementary eight library sources are formatter-clean without routing, installation, integration, authentication, MFA, or OAuth behavior drift."
  artifacts:
    - path: .formatter.exs
      provides: "golden-safe formatter boundary from 234-01"
  key_links:
    - from: .formatter.exs
      to: files_modified
      via: "explicit scoped mix format batches"
---
<objective>Complete the library-source formatting cleanup in an independently reversible eight-file plan (D-04).</objective>
<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-01-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
</context>

## Artifacts this phase produces

- No new symbols or paths are created; the produced artifact is the formatter-clean state of these existing paths: `lib/sigra/enterprise_routing.ex`, `lib/sigra/install/features/organizations.ex`, `lib/sigra/integrations/chimeway.ex`, `lib/sigra/jwt.ex`, `lib/sigra/jwt/refresh_token.ex`, `lib/sigra/mfa/trust.ex`, `lib/sigra/oauth.ex`, and `lib/sigra/oauth/callback.ex`.
- The scoped D-04 contract is that these files retain their routing, installation, integration, JWT, refresh-token, MFA, and OAuth behavior while native formatter layout is applied; `test/fixtures/install_golden/tree` remains byte-for-byte outside this plan's ownership.

<tasks>
<task type="auto">
  <name>Task 1: Format routing/install/integration sources (D-04)</name>
  <files>lib/sigra/enterprise_routing.ex, lib/sigra/install/features/organizations.ex, lib/sigra/integrations/chimeway.ex</files>
  <reversibility rating="reversible">Rollback target: restore these three files from the task-start commit. Pre-change gate: record the scoped diff and empty golden-tree diff. Post-change gate: the exact formatter check and golden-tree check pass before advancing.</reversibility>
  <read_first>
    - `.formatter.exs` — D-04 golden-safe boundary.
    - Every file listed in `<files>` — current routing/install/integration behavior.
  </read_first>
  <action>Run native `mix format` against exactly these three paths. Preserve every function, operation, branch, and literal; do not touch generated golden files.</action>
  <acceptance_criteria>
    - `mix format --check-formatted lib/sigra/enterprise_routing.ex lib/sigra/install/features/organizations.ex lib/sigra/integrations/chimeway.ex` exits 0.
    - The task commit's path set is exactly `lib/sigra/enterprise_routing.ex`, `lib/sigra/install/features/organizations.ex`, and `lib/sigra/integrations/chimeway.ex`; `git diff --name-only &lt;task-start-sha&gt;..HEAD --` names no other path.
    - `git diff &lt;task-start-sha&gt;..HEAD -- lib/sigra/enterprise_routing.ex lib/sigra/install/features/organizations.ex lib/sigra/integrations/chimeway.ex` contains formatter-layout changes only: every pre-change function, operation, branch, and literal remains present with identical source meaning.
    - `test -z "$(git diff --name-only -- test/fixtures/install_golden/tree)"` exits 0, proving no generated golden path drift.
  </acceptance_criteria>
  <verify><automated>mix format --check-formatted lib/sigra/enterprise_routing.ex lib/sigra/install/features/organizations.ex lib/sigra/integrations/chimeway.ex &amp;&amp; test -z "$(git diff --name-only -- test/fixtures/install_golden/tree)"</automated></verify>
  <done>Three routing/install/integration sources are formatter-clean with no golden drift.</done>
</task>
<task type="auto">
  <name>Task 2: Format JWT/MFA/OAuth sources and compile (D-04)</name>
  <files>lib/sigra/jwt.ex, lib/sigra/jwt/refresh_token.ex, lib/sigra/mfa/trust.ex, lib/sigra/oauth.ex, lib/sigra/oauth/callback.ex</files>
  <reversibility rating="reversible">Rollback target: restore these five security-sensitive files from the task-start commit. Pre-change gate: record the scoped diff and baseline warnings-as-errors result. Post-change gate: formatter and warnings-as-errors compilation pass before advancing.</reversibility>
  <read_first>
    - `.formatter.exs` — D-04 boundary.
    - Every file listed in `<files>` — current security-sensitive control flow.
  </read_first>
  <action>Run native `mix format` against exactly these five paths. Preserve every condition, token/MFA/OAuth operation, error branch, and literal; then compile with warnings as errors.</action>
  <acceptance_criteria>
    - `mix format --check-formatted lib/sigra/jwt.ex lib/sigra/jwt/refresh_token.ex lib/sigra/mfa/trust.ex lib/sigra/oauth.ex lib/sigra/oauth/callback.ex` exits 0.
    - The task commit's path set is exactly `lib/sigra/jwt.ex`, `lib/sigra/jwt/refresh_token.ex`, `lib/sigra/mfa/trust.ex`, `lib/sigra/oauth.ex`, and `lib/sigra/oauth/callback.ex`; `git diff --name-only &lt;task-start-sha&gt;..HEAD --` names no other path.
    - `git diff &lt;task-start-sha&gt;..HEAD -- lib/sigra/jwt.ex lib/sigra/jwt/refresh_token.ex lib/sigra/mfa/trust.ex lib/sigra/oauth.ex lib/sigra/oauth/callback.ex` contains formatter-layout changes only: every condition, token/MFA/OAuth operation, error branch, and literal retains identical source meaning.
    - `mix compile --warnings-as-errors` exits 0 after the scoped formatting.
  </acceptance_criteria>
  <verify><automated>mix format --check-formatted lib/sigra/jwt.ex lib/sigra/jwt/refresh_token.ex lib/sigra/mfa/trust.ex lib/sigra/oauth.ex lib/sigra/oauth/callback.ex &amp;&amp; mix compile --warnings-as-errors</automated></verify>
  <done>The five security-sensitive sources are formatter-clean and warnings-clean.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| formatter → routing/security sources | Layout rewrites cross production control flow. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-11-01 | Tampering | routing/authentication sources | high | mitigate | Exact file lists, scoped diffs, formatter verification, and warnings-as-errors compilation. |
| T-234-11-02 | Tampering | golden fixtures | high | mitigate | D-04 exclusion plus an empty golden-tree diff gate. |
</threat_model>
<verification>All eight paths pass scoped formatting, production code compiles warnings-clean, and golden bytes remain untouched.</verification>
<success_criteria>The complementary D-04 library cleanup is complete in an eight-file recovery boundary.</success_criteria>
<output>Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-11-SUMMARY.md` when done.</output>
