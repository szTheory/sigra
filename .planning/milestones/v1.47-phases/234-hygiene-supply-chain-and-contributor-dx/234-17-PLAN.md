---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 17
type: execute
wave: 7
depends_on: [234-14]
files_modified:
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
  - test/sigra/planning/phase_234_evidence_contract_test.exs
  - .planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md
autonomous: true
gap_closure: true
requirements: [DX-03]

must_haves:
  truths:
    - "D-07: authenticated GitHub-processed evidence contains exactly github-actions:/, mix:/, and npm:/test/example/priv/playwright."
    - "Every tuple has a non-empty job ID, timestamp, successful processed status, GitHub log URL, and sanitized receipt hash; no-PR is acceptable only when the job receipt proves processing."
    - "Authentication, rate-limit, missing-row, processing-error, or malformed-receipt states remain failed with durable diagnostics and cannot authorize completion."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-03 unclassified edge: the latest authenticated successful version-update job for each exact tuple is authoritative even when it opens no PR; configuration presence and PR absence remain insufficient."
  prohibitions:
    - "Missing, unauthenticated, rate-limited, red, skipped, malformed, or inferred Dependabot evidence is never marked successful."
    - "Tokens, cookies, environment values, raw authenticated HTML, and unsanitized logs are not committed."
    - "HTTP 403/429 responses stop collection until GitHub's reported reset or retry time; they are not retried immediately."
  artifacts:
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      provides: "three authenticated GitHub Dependabot job receipts"
    - path: test/sigra/planning/phase_234_evidence_contract_test.exs
      provides: "exact tuple and valid processed-job schema"
  key_links:
    - from: .github/dependabot.yml
      to: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      via: "default-branch config hash plus exact per-tuple job-log receipts"
---

<objective>
Capture the missing authenticated GitHub Dependabot receipts for all three locked ecosystem/directory tuples (D-07).

Purpose: close DX-03 with managed-service execution evidence rather than YAML inference or absence of update PRs.
Output: exact successful receipt rows, strict mutation coverage, and a resolved-or-still-blocking durable diagnostic.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.github/dependabot.yml
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-07-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-10-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
@test/sigra/planning/phase_234_dependabot_contract_test.exs
@test/sigra/planning/phase_234_evidence_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate exact authenticated processed-job receipt rows before capture (D-07)</name>
  <files>test/sigra/planning/phase_234_evidence_contract_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` `:dependabot` contract — strengthen the existing success branch without weakening failed-state diagnostics.
    - `test/sigra/planning/phase_234_dependabot_contract_test.exs` — exact configuration tuple source.
    - `234-EVIDENCE.json` — current failed rows and preserved default-branch/config hashes.
    - `AGENTS.md` — rate-limit, single-watcher, structured-summary, and hard-stop rules.
    - Every file listed in `<files>` — establish current behavior before editing.
  </read_first>
  <behavior>
    - "The validator accepts exactly three unique ordered tuples with successful status, numeric job ID, UTC timestamp, tuple-specific GitHub job-log URL, and non-empty sanitized SHA-256."
    - "Empty slots, duplicate/missing/extra tuples, failed/skipped statuses, empty IDs, malformed timestamps/URLs/hashes, or a config/default-branch mismatch fail."
  </behavior>
  <action>
    Write failing in-memory success-schema and mutation tests first, then strengthen the existing `:dependabot` validator until those tests pass. Require each row's `processed_status` to name successful processing and require a non-empty `status_summary`; allow `associated_pr_url` to be absent only when the summary proves the successful no-update outcome. Keep the persisted failed rows valid only as durable diagnostics, never as completion evidence, so Task 1 can finish green without promoting the artifact before capture.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only dependabot</automated>
  </verify>
  <acceptance_criteria>
    - Exact successful schema and tuple set are centralized in one validator.
    - Empty/missing/duplicate/extra/malformed/red mutation cases are rejected with the tuple/field named.
    - The current artifact remains evidence-status failed until Task 2 captures authenticated rows, while its durable diagnostic schema still validates.
  </acceptance_criteria>
  <done>The contract is ready to accept only complete authenticated GitHub-processed receipts.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the three GitHub-processed Dependabot jobs and close the residual (D-07)</name>
  <precondition>The Task 1 validator exists, GitHub authentication is active, and the REST core rate-limit budget exceeds 250 requests.</precondition>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json, .planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md</files>
  <read_first>
    - `.github/dependabot.yml` — exact tuple order and directories.
    - `234-EVIDENCE.json` — preserve all non-Dependabot evidence verbatim.
    - `.planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md` — existing owner/diagnostic to resolve only after all rows pass.
    - `AGENTS.md` — inspect `gh api rate_limit` before polling; never duplicate a watcher; stop on 403/429.
    - Every file listed in `<files>` — establish current content before editing.
  </read_first>
  <action>
    Run `gh api rate_limit` once before collection and stop when REST `core.remaining` is 250 or lower until `core.reset`. Verify the exact default-branch SHA and hash the decoded `.github/dependabot.yml` at that SHA. Use the authenticated GitHub Dependabot job-log surface with deterministic browser automation: role/link selectors, explicit navigation readiness, selector waits, and no sleeps. Enumerate the exact three manifest rows, open each row's latest successful version-update job, and capture ecosystem, directory, job ID, UTC timestamp, processed status, status summary, full GitHub job-log URL, optional associated PR URL, and a SHA-256 of sanitized receipt text. Do not save session state or raw page/log content.

    The collection is bounded and serial: one active navigation/collector owns a tuple at a time. A transient transport failure may be retried once; a deterministic processing error is not retried, and HTTP 403/429 is a hard stop until the reported time. If authentication or any row remains unavailable, retain `dependabot.status: failed`, update the existing residual with exact diagnostics/reset time, and halt without completing the plan. Only after all three rows validate set `dependabot.status: success`, preserve the default-branch/config hashes, and mark the existing residual resolved with the three job IDs and evidence commit; do not delete its diagnostic history.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_dependabot_contract_test.exs test/sigra/planning/phase_234_evidence_contract_test.exs --only dependabot</automated>
  </verify>
  <acceptance_criteria>
    - Evidence contains exactly one valid successful row for each locked tuple, with job ID, UTC timestamp, processed status/summary, log URL, and sanitized SHA-256.
    - Any associated PR is linked; no-update rows are accepted only from a successful processed job summary.
    - Rate-limit and authentication rules are followed, raw authenticated material is absent, and every non-Dependabot evidence slot is preserved.
    - The residual is marked resolved only after the exact successful tuple contract passes; otherwise the plan halts red with durable diagnostics.
  </acceptance_criteria>
  <done>DX-03 has authenticated GitHub-owned processing evidence for Actions, Mix, and Playwright npm dependencies.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — replaces the three failed Dependabot rows with exact authenticated processed-job receipts; no other evidence slot is re-opened.
- `test/sigra/planning/phase_234_evidence_contract_test.exs` — strengthens the `:dependabot` success schema and adds tuple/field mutation coverage.
- `.planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md` — retains diagnostic history and transitions to resolved only after all three receipt rows validate.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated GitHub session → evidence collector | Remote UI content and navigation state are untrusted and session-bearing. |
| Dependabot job logs → committed JSON | Logs can contain ambiguous status text or sensitive request context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-17-01 | Spoofing | tuple/job association | high | mitigate | Resolve each exact config tuple through its authenticated manifest row and require tuple-specific job URL/ID/timestamp. |
| T-234-17-02 | Repudiation | no-update claim | high | mitigate | Require a successful processed-job summary; PR absence alone remains invalid. |
| T-234-17-03 | Information disclosure | authenticated page/log | high | mitigate | Persist only sanitized receipt hashes and public identifiers/URLs; never store cookies, tokens, or raw authenticated artifacts. |
| T-234-17-04 | Denial of service | GitHub polling/rate limit | high | mitigate | Check budget before collection, serialize ownership, bound waits, retry transport once, and hard-stop on 403/429. |
| T-234-17-05 | Tampering | evidence JSON | high | mitigate | Exact-set and malformed-field mutation tests gate the transition from failed to success. |

## ASVS L1 Control Mapping and Automated Verification

| ASVS intent | Control | Automated assertion |
|---|---|---|
| V5.1 input validation | Typed exact-set parser for untrusted GitHub-derived fields. | Duplicate/missing/extra/malformed mutations fail with named diagnostics. |
| V7.1 log integrity | Every claim is attributable without persisting secrets. | Job IDs, timestamps, URLs, summaries, and sanitized hashes are mandatory. |
</threat_model>

<verification>
The offline configuration and authenticated evidence contracts both pass for exactly three successful tuples, and the residual records closure rather than erasing prior diagnostics.
</verification>

<success_criteria>
DX-03 is supported by complete authenticated GitHub-processed evidence; no inferred or partial state is promotable.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-17-SUMMARY.md` when done.
</output>
