---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 16
type: execute
wave: 8
depends_on: [234-15, 234-17]
files_modified:
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
  - test/sigra/planning/phase_234_evidence_contract_test.exs
autonomous: true
gap_closure: true
requirements: [DX-01]

must_haves:
  truths:
    - "A clean detached checkout at the gap-fix commit runs MIX_ENV=test mix ci successfully and remains byte/status clean afterward."
    - "The local receipt proves pre/post mix.lock hashes, pre/post worktree status, dependency restoration, the seven ordered legs, and green golden/idempotency execution."
    - "A failed or dirty execution remains failed evidence and cannot overwrite the earlier diagnostic trail."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-01 unclassified edge: the Linux/Mix environment represented by the existing clean-checkout contract is the authoritative reproducibility target; no unrecorded operating-system equivalence is claimed."
  prohibitions:
    - "User changes and the current branch are preserved; only an explicit temporary detached worktree is created and removed."
    - "Raw command output, tokens, cookies, database credentials, and environment values are not committed."
    - "Missing, dirty, red, or malformed local evidence never receives success status."
  artifacts:
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      provides: "refreshed non-destructive local mix ci receipt"
    - path: test/sigra/planning/phase_234_evidence_contract_test.exs
      provides: "local receipt non-vacuity and clean-after contract"
  key_links:
    - from: scripts/ci/sigra-dep-off.sh
      to: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      via: "executed mix ci log markers plus pre/post lock and worktree measurements"
---

<objective>
Replace the stale local receipt with executed proof that the repaired contributor gate is green and non-destructive.

Purpose: turn the Plan 15 implementation into durable DX-01 evidence consumable by final sign-off.
Output: refreshed local evidence fields and mutation-tested receipt validation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-15-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-17-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
@test/sigra/planning/phase_234_evidence_contract_test.exs
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute and attest the repaired local contributor gate (D-01, D-02, D-04)</name>
  <precondition>Docker/Postgres tooling and the documented phx_new 1.8.8 archive are available; verify versions/archive presence with read-only commands before creating the detached worktree.</precondition>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json, test/sigra/planning/phase_234_evidence_contract_test.exs</files>
  <read_first>
    - `234-EVIDENCE.json` — preserve remote receipt rows delivered by 234-17 and all already verified release/gallery/PR identifiers.
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` — existing `:local_mix_ci` schema and remote receipt validation.
    - `234-15-SUMMARY.md` — exact cleanup harness and golden-fix commit to execute.
    - `CONTRIBUTING.md` — database/archive prerequisites and literal contributor command.
    - `scripts/db/up.sh` — deterministic Postgres startup contract.
    - Every file listed in `<files>` — establish current content before editing.
  </read_first>
  <behavior>
    - "The receipt validator rejects unequal pre/post lock hashes, unequal pre/post status snapshots, failed dependency restoration, a red golden command, or a nonzero mix ci result."
    - "The captured run is tied to the exact gap-fix commit and command output is represented only by a sanitized SHA-256."
  </behavior>
  <action>
    Extend the local receipt contract first with required `clean_after`, `mix_lock_sha256_before`, `mix_lock_sha256_after`, `git_status_sha256_before`, `git_status_sha256_after`, `dependency_restore_verified`, and `golden_idempotency_status` fields. Add hermetic mutations for mismatched lock hashes, mismatched status hashes, and false dependency restoration; each must make the tagged test fail. Preserve the exact existing remote evidence tuple schema from 234-17.

    Commit the Plan 15 gap fix before proof. Create a named `mktemp -d` location and a detached worktree at that exact commit without switching the user's branch. Verify it is clean, record the hash of `mix.lock` and normalized `git status --porcelain --untracked-files=all`, start the documented database, verify/install the documented phx_new archive, and run literal `MIX_ENV=test mix ci` once with output captured outside the repository. After it returns, run `MIX_ENV=test mix deps.get --check-locked`, confirm threadline compiles, re-hash the lockfile and normalized status, and require exact equality. Record start/end UTC, exit status, ordered legs, golden/idempotency success marker, hashes, exact commit, and sanitized log SHA-256. Remove only the explicit detached worktree/temp directory after capture.

    If the command, restoration, hash comparison, or cleanup fails, retain `status: failed` with sanitized diagnostics and do not alter any remote receipt or claim DX-01 complete.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only local_mix_ci &amp;&amp; mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - One clean detached execution at the exact gap-fix SHA exits zero and records all seven legs plus golden/idempotency success.
    - Pre/post lock hashes and status hashes are equal, `clean_after` and dependency restoration are true, and all mutation cases fail closed.
    - Remote Dependabot, release, PR, Playwright, and gallery receipts remain byte-semantically unchanged.
  </acceptance_criteria>
  <done>The evidence ledger proves a contributor can run the repaired full gate without dirtying repository or dependency state.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — extends/replaces the `local_mix_ci` receipt with clean-after, pre/post hash, dependency restoration, and golden status fields; no remote receipt is reinterpreted.
- `test/sigra/planning/phase_234_evidence_contract_test.exs` — extends the existing local receipt validator and adds lock/status/restoration mutations.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user repository → detached evidence worktree | Proof must execute exact committed code without consuming or changing user work. |
| process output → committed evidence | Local logs may contain paths or environment data and cannot be committed raw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-16-01 | Repudiation | local receipt | high | mitigate | Bind exact SHA, command, timestamps, ordered legs, exit status, and pre/post hashes in a mutation-tested schema. |
| T-234-16-02 | Tampering | user checkout | high | mitigate | Execute only in an explicit detached worktree and require identical normalized status plus lock hashes after completion. |
| T-234-16-03 | Information disclosure | captured logs | high | mitigate | Commit a sanitized hash and diagnostics only; remove raw output with the harness-owned temporary directory. |

## ASVS L1 Control Mapping and Automated Verification

| ASVS intent | Control | Automated assertion |
|---|---|---|
| V5.1 input validation | Typed receipt values and exact lowercase hashes. | Local mutations reject mismatched or malformed state. |
| V7.1 evidence integrity | Immutable commit and pre/post measurements. | Tagged contract accepts only a successful, clean, non-vacuous receipt. |
</threat_model>

<verification>
The tagged local evidence and Phase 198 parity contracts pass, while mutation cases demonstrate that dirty or unrestored runs cannot pass.
</verification>

<success_criteria>
DX-01 has current machine-readable proof of a green, non-dirty contributor `mix ci` execution.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-16-SUMMARY.md` when done.
</output>
