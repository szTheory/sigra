---
phase: "182"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens.json
  - brandbook/tokens.css
  - brandbook/README.md
  - brandbook/brand-book.md
  - brandbook/examples/landing-hero.svg
  - brandbook/examples/readme-header.svg
autonomous: true
requirements:
  - BRAND2-09
  - BRAND2-10

must_haves:
  truths:
    - "tokens.json version is 1.0.1 and meta.changed is set to an ISO date"
    - "tokens.css has a provenance header comment referencing tokens.json v1.0.1"
    - "README.md Files table no longer labels any logo entry as 'Rail Accent' and includes logo-primary-subtitle.svg and social-card-dark.svg"
    - "README.md has a Token Change Policy section documenting semver patch/minor/major semantics and the three-surface ember parity rule"
    - "brand-book.md no longer references Inter Display Black; wordmark font is documented as Space Grotesk v2.0"
    - "Both stale specimens (landing-hero.svg, readme-header.svg) no longer contain the v1 path signature M17 14v14"
  artifacts:
    - path: "brandbook/tokens.json"
      provides: "Version-bumped token source with meta.changed"
      contains: "1.0.1"
    - path: "brandbook/tokens.css"
      provides: "CSS custom properties with provenance header"
      contains: "tokens.json v1.0.1"
    - path: "brandbook/README.md"
      provides: "Updated Files table + Token Change Policy section"
      contains: "Token Change Policy"
    - path: "brandbook/brand-book.md"
      provides: "Updated logo system docs with Space Grotesk reference"
      contains: "Space Grotesk"
    - path: "brandbook/examples/landing-hero.svg"
      provides: "Specimen with D4 mark geometry"
    - path: "brandbook/examples/readme-header.svg"
      provides: "Specimen with D4 mark geometry"
  key_links:
    - from: "brandbook/tokens.css"
      to: "brandbook/tokens.json"
      via: "provenance header comment"
      pattern: "tokens\\.json v1\\.0\\.1"
    - from: "brandbook/README.md"
      to: "brandbook/tokens.json"
      via: "Token Change Policy section"
      pattern: "Token Change Policy"
---

<objective>
Upgrade the brand collateral documents: version-bump tokens, add a provenance header to tokens.css, refresh the README Files table (carry-forward from Phase 181), add the Token Change Policy section to README.md, update brand-book.md with Space Grotesk font reference and suite/ember-parity content, and replace the v1 stale mark geometry in both specimen SVGs with the ratified D4 Linked Rail geometry.

Purpose: Closes BRAND2-10 (tokens + policy + specimens) and the README carry-forward item from Phase 181. All doc/token work is done in this plan so Plan 02 can focus entirely on index.html expansion and the axe gate.

Output: Six files updated — tokens.json, tokens.css, README.md, brand-book.md, examples/landing-hero.svg, examples/readme-header.svg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-brand-book-v2-tokens/182-CONTEXT.md
@.planning/phases/182-brand-book-v2-tokens/182-RESEARCH.md
@.planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md

<interfaces>
<!-- Key source coordinates for the D4 mark geometry (from brandbook/logo-mark.svg) -->
<!-- viewBox="-70 -60 1040 1040"; logical extent x:180-720, y:-20-940 -->
<!-- Ink group (fill #151515 on light surface): -->
<!--   stem: rect x="540" y="360" width="180" height="580" -->
<!--   foot: rect x="180" y="760" width="540" height="180" -->
<!-- Rail block (fill #c2410c on light surface): -->
<!--   rect x="400" y="-20" width="320" height="320" -->
<!-- Scale to ~54px rendered height: scale(0.057), translate(70 60) to align to logical origin -->

<!-- landing-hero.svg stale group to replace (translate(98 96) offset): -->
<!--   <g transform="translate(98 96)"> ... M17 14v14 paths ... </g> -->

<!-- readme-header.svg stale group to replace (translate(88 88) scale(0.9) offset): -->
<!--   <g transform="translate(88 88) scale(0.9)"> ... M17 14v14 paths ... </g> -->

<!-- tokens.json current meta block: -->
<!-- "version": "1.0.0", "meta": { source, policy, license } -->
<!-- Change: version -> "1.0.1", add "changed": "2026-06-12" to meta -->

<!-- tokens.css: first line is `:root {` — prepend 5-line provenance comment block -->

<!-- README.md Files table pattern: | [`filename`](filename) | Purpose prose. | -->
<!-- Five entries still say "Rail Accent"; add logo-primary-subtitle.svg and social-card-dark.svg rows -->

<!-- brand-book.md stale font ref (line ~96): "Inter Display Black v4.1" -->
<!-- Replace with: "Space Grotesk v2.0 (OFL) wght=700, using opentype.js 2.0.0" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump tokens.json version + add meta.changed; add provenance header to tokens.css</name>
  <files>brandbook/tokens.json, brandbook/tokens.css</files>
  <read_first>
    - brandbook/tokens.json — full file (lines 1-10, the meta block; values unchanged)
    - brandbook/tokens.css — first 5 lines (confirm current first line is `:root {`, no existing header)
    - .planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md — "tokens.json (patch bump only)" section and "tokens.css (header addition only)" section for exact text
    - .planning/phases/182-brand-book-v2-tokens/182-RESEARCH.md — Section 3 (proposed version bump JSON) and Section 4 (provenance header text)
  </read_first>
  <action>
    Edit brandbook/tokens.json: change `"version": "1.0.0"` to `"version": "1.0.1"`. Add `"changed": "2026-06-12"` as the last key inside the `meta` object (after `"license"`). No other values change — do not touch any token value.

    Edit brandbook/tokens.css: prepend a 5-line block before `:root {`. The block reads:
    `/* Sigra brand tokens — CSS custom properties`
    ` * Derived from brandbook/tokens.json v1.0.1`
    ` * Hand-maintained sync. When tokens.json version changes, update this file`
    ` * and increment the version reference above.`
    ` * Do not add web fonts, external imports, or CDN links.`
    ` */`
    After the comment, the existing `:root {` block continues unchanged. Do not alter any CSS custom property values.
  </action>
  <verify>
    <automated>
      jq -r '.version' /Users/jon/projects/sigra/brandbook/tokens.json
      jq -r '.meta.changed' /Users/jon/projects/sigra/brandbook/tokens.json
      head -3 /Users/jon/projects/sigra/brandbook/tokens.css | grep -c 'tokens.json'
    </automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.version' brandbook/tokens.json` outputs `1.0.1`
    - `jq -r '.meta.changed' brandbook/tokens.json` outputs `2026-06-12` (not null)
    - `head -3 brandbook/tokens.css | grep -c 'tokens.json'` outputs `1`
    - `jq . brandbook/tokens.json` exits 0 (valid JSON)
    - No CSS custom property values changed: `grep -c 'c2410c' brandbook/tokens.css` returns same count as before edit
  </acceptance_criteria>
  <done>tokens.json at version 1.0.1 with meta.changed set; tokens.css has provenance header; no token values modified</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refresh README.md — Files table + Token Change Policy section</name>
  <files>brandbook/README.md</files>
  <read_first>
    - brandbook/README.md — full file (identify the Files table rows, their current "Rail Accent" labels, and the Maintenance Rules section location where Token Change Policy is appended)
    - .planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md — "README.md (documentation — extend in-place)" section: exact table row format, the two new rows to add, and the full Token Change Policy text block
    - .planning/STATE.md — Phase 181 carry-forward note confirming "Rail Accent" is the label to replace
  </read_first>
  <action>
    Edit brandbook/README.md in two parts:

    Part 1 — Files table refresh (carry-forward from Phase 181):
    - Locate the five rows where the Purpose column says "Rail Accent" (these describe logo-primary.svg, logo-primary-dark.svg, logo-mark.svg, logo-monochrome.svg, favicon.svg).
    - Replace the "Rail Accent" label in each row with language matching "D4 Linked Rail" (e.g. "D4 Linked Rail primary lockup for light surfaces.", "D4 Linked Rail primary lockup for dark surfaces.", "D4 Linked Rail abstract mark (standalone + favicon contexts).", "Single-ink monochrome typemark for emboss/print contexts.", "D4 Linked Rail favicon mark — light/dark via `prefers-color-scheme`.").
    - Add two new rows immediately after the social-card.svg row (or at the end of the table, whichever is logically consistent):
      `| [`logo-primary-subtitle.svg`](logo-primary-subtitle.svg) | D4 Linked Rail lockup with subtitle line for tall-format contexts. |`
      `| [`social-card-dark.svg`](social-card-dark.svg) | SVG social preview source for dark-background platforms. |`

    Part 2 — Token Change Policy section:
    - After the existing `## Maintenance Rules` section (add a blank line separator), append a new `## Token Change Policy` section with the full text from PATTERNS.md "New Token Change Policy section" block. Include the three bullet points (Patch/Minor/Major), the three-surface ember parity rule paragraph, and the meta.changed date note. The prose is verbatim from PATTERNS.md — do not shorten it.
  </action>
  <verify>
    <automated>
      grep -c 'Rail Accent' /Users/jon/projects/sigra/brandbook/README.md
      grep -c 'Token Change Policy' /Users/jon/projects/sigra/brandbook/README.md
      grep -c 'logo-primary-subtitle' /Users/jon/projects/sigra/brandbook/README.md
      grep -c 'social-card-dark' /Users/jon/projects/sigra/brandbook/README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Token Change Policy' brandbook/README.md` outputs `1` (the section heading is present exactly once)
    - `grep -c 'logo-primary-subtitle' brandbook/README.md` outputs at least `1` (new row present in Files table)
    - `grep -c 'social-card-dark' brandbook/README.md` outputs at least `1` (new row present in Files table)
    - Files table rows for the five existing logo assets no longer contain the substring "Rail Accent" (check: `grep -A 20 '| File' brandbook/README.md | grep -c 'Rail Accent'` outputs `0`, OR verify each of the five rows individually)
    - The Token Change Policy section contains the three-surface ember parity rule (`grep -c 'ember-700' brandbook/README.md` outputs at least `1`)
  </acceptance_criteria>
  <done>README.md Files table uses D4 Linked Rail language; logo-primary-subtitle.svg and social-card-dark.svg rows added; Token Change Policy section present with semver patch/minor/major semantics and three-surface ember parity rule</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update brand-book.md — font reference, logo file list, typemark anatomy, suite architecture + ember parity content</name>
  <files>brandbook/brand-book.md</files>
  <read_first>
    - brandbook/brand-book.md — full file (identify line ~96 with Inter Display Black, the file list at ~82-88, the Logo System section structure, and the end of the document where new sections can be appended)
    - .planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md — "brand-book.md (documentation — extend in-place)" section: stale font ref, updated file list, three ADD items
    - .planning/phases/182-brand-book-v2-tokens/182-RESEARCH.md — Section 5 (suite architecture skeleton, 7 libs, shared/per-library table, 3 onboarding rules)
    - brandbook/pressure-test-audit-v2.md — Section 6 (ADD/TIGHTEN list) and Section 8 (suite architecture verdicts) for content accuracy
  </read_first>
  <action>
    Edit brandbook/brand-book.md in three parts:

    Part 1 — Stale font reference fix:
    Find the line that reads approximately "The lockup wordmark is outlined from Inter Display Black v4.1." and replace it with: "The lockup wordmark is outlined from Space Grotesk v2.0 (OFL) wght=700, using opentype.js 2.0.0."

    Part 2 — Logo file list update:
    Find the Logo System section's file list (lines ~82-88) and update it to the 8-file D4 set (same format as existing list items):
    - Primary light lockup: `logo-primary.svg`
    - Primary dark lockup: `logo-primary-dark.svg`
    - Subtitle variant lockup: `logo-primary-subtitle.svg`
    - Free-standing mark: `logo-mark.svg`
    - Monochrome mark: `logo-monochrome.svg`
    - Favicon source: `favicon.svg`
    - Social card (light): `social-card.svg`
    - Social card (dark): `social-card-dark.svg`

    Part 3 — New content sections (extend, do not rewrite):
    After the existing Logo System section content, add the following subsections (or as a new section following the Logo System section, whichever fits the document flow):

    a) Integrated typemark anatomy: Document that the D4 Linked Rail mark uses two distinct visual elements — the rail-block tittle (ember-colored rect above the `i` stem) and the extended `g` tail aligned under the tittle, forming one bracketing rail system across the wordmark. These elements are functional, not decorative.

    b) Three-surface ember parity rule: State that `ember-700: #c2410c` is the canonical accent value consumed by three independent surfaces — `brandbook/tokens.json`, admin CSS (`--sg-color-brand`), and auth CSS (`--sigra-auth-light-accent`). Any new surface carrying the Sigra brand accent must reference the brandbook token rather than hardcoding `#c2410c`. This aligns with the Token Change Policy in README.md.

    c) Suite architecture (szTheory OSS suite): Add a `## Suite Architecture` section. Include: a brief framing sentence (seven libraries, one coherent visual system), the shared-vs-per-library table (typeface, design vocabulary, voice register, token naming convention — shared; mark/glyph, accent color, domain metaphor — per-library), the three new-library onboarding rules (inherit Space Grotesk v2.0 OFL, adopt a unique domain-metaphor mark, select an accent hue ≥15° from all existing siblings on the color wheel), and the seven library names with their domain metaphors (from RESEARCH.md Section 5). Keep this section concise — it documents the framework, not each library's identity.
  </action>
  <verify>
    <automated>
      grep -c 'Space Grotesk' /Users/jon/projects/sigra/brandbook/brand-book.md
      grep -c 'Inter Display Black' /Users/jon/projects/sigra/brandbook/brand-book.md
      grep -c 'logo-primary-subtitle' /Users/jon/projects/sigra/brandbook/brand-book.md
      grep -c 'Suite Architecture\|suite architecture' /Users/jon/projects/sigra/brandbook/brand-book.md
      grep -c 'ember-700\|three-surface' /Users/jon/projects/sigra/brandbook/brand-book.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Space Grotesk' brandbook/brand-book.md` outputs at least `1`
    - `grep -c 'Inter Display Black' brandbook/brand-book.md` outputs `0` (stale reference fully removed)
    - `grep -c 'logo-primary-subtitle' brandbook/brand-book.md` outputs at least `1` (subtitle variant in file list)
    - `grep -c 'Suite Architecture\|suite architecture' brandbook/brand-book.md` outputs at least `1`
    - `grep -c 'ember-700\|three-surface' brandbook/brand-book.md` outputs at least `1`
    - File is valid UTF-8 Markdown: no binary characters introduced
  </acceptance_criteria>
  <done>brand-book.md: stale Inter Display Black reference replaced with Space Grotesk v2.0; logo file list updated to 8 D4 assets; integrated-typemark anatomy, three-surface ember parity, and suite architecture sections added</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Replace stale v1 mark geometry in landing-hero.svg and readme-header.svg</name>
  <files>brandbook/examples/landing-hero.svg, brandbook/examples/readme-header.svg</files>
  <read_first>
    - brandbook/examples/landing-hero.svg — full file (locate the stale `<g transform="translate(98 96)">` group containing the M17 14v14 paths)
    - brandbook/examples/readme-header.svg — full file (locate the stale `<g transform="translate(88 88) scale(0.9)">` group containing the same v1 paths)
    - .planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md — "examples/landing-hero.svg" and "examples/readme-header.svg" sections: exact stale group lines, exact replacement group with D4 geometry, scale factors, transform chains
    - brandbook/logo-mark.svg — D4 geometry source (the three rects: stem rect540/360/180/580, foot rect180/760/540/180, rail-block rect400/-20/320/320)
  </read_first>
  <action>
    Edit brandbook/examples/landing-hero.svg:
    - Remove the entire stale `<g transform="translate(98 96)">` element that contains the three `<path>` elements with `d="M17 14v14..."` data.
    - Replace with the D4 mark group at the same translation offset. The replacement group uses inline light-surface colors (no CSS variables — SVG specimens are static files):
      `<g transform="translate(98 96) scale(0.057) translate(70 60)">`
        `<g fill="#151515">`
          `<rect x="540" y="360" width="180" height="580"/>`
          `<rect x="180" y="760" width="540" height="180"/>`
        `</g>`
        `<rect fill="#c2410c" x="400" y="-20" width="320" height="320"/>`
      `</g>`
    - Do not alter any other element in the file.

    Edit brandbook/examples/readme-header.svg:
    - Remove the entire stale `<g transform="translate(88 88) scale(0.9)">` element that contains the three `<path>` elements with `d="M17 14v14..."` data.
    - Replace with the D4 mark group, preserving the outer translate and scale:
      `<g transform="translate(88 88) scale(0.9) scale(0.057) translate(70 60)">`
        `<g fill="#151515">`
          `<rect x="540" y="360" width="180" height="580"/>`
          `<rect x="180" y="760" width="540" height="180"/>`
        `</g>`
        `<rect fill="#c2410c" x="400" y="-20" width="320" height="320"/>`
      `</g>`
    - Do not alter any other element in the file (the "Sigra" text at x="154" y="132" and other elements remain untouched).
  </action>
  <verify>
    <automated>
      grep -c 'M17 14v14' /Users/jon/projects/sigra/brandbook/examples/landing-hero.svg
      grep -c 'M17 14v14' /Users/jon/projects/sigra/brandbook/examples/readme-header.svg
      xmllint --noout /Users/jon/projects/sigra/brandbook/examples/landing-hero.svg && echo "landing-hero: valid XML"
      xmllint --noout /Users/jon/projects/sigra/brandbook/examples/readme-header.svg && echo "readme-header: valid XML"
      grep -c 'c2410c' /Users/jon/projects/sigra/brandbook/examples/landing-hero.svg
      grep -c 'c2410c' /Users/jon/projects/sigra/brandbook/examples/readme-header.svg
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'M17 14v14' brandbook/examples/landing-hero.svg` outputs `0`
    - `grep -c 'M17 14v14' brandbook/examples/readme-header.svg` outputs `0`
    - `xmllint --noout brandbook/examples/landing-hero.svg` exits 0 (valid XML)
    - `xmllint --noout brandbook/examples/readme-header.svg` exits 0 (valid XML)
    - Both files contain the D4 rail-block color: `grep -c 'c2410c' brandbook/examples/landing-hero.svg` outputs at least `1`
    - Both files contain the D4 rail-block color: `grep -c 'c2410c' brandbook/examples/readme-header.svg` outputs at least `1`
    - The other 7 examples/specimens have zero `M17 14v14` occurrences (confirm with: `grep -l 'M17 14v14' brandbook/examples/*.svg` — expected empty output)
  </acceptance_criteria>
  <done>Both stale specimens updated to D4 Linked Rail mark geometry; v1 path signature M17 14v14 gone from all examples/; both files parse as valid XML</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk read → file edit | Files read from local filesystem and edited in-place; no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01 | Tampering | tokens.json value edits | mitigate | Only `version` and `meta.changed` are edited; jq validation confirms values unchanged |
| T-182-02 | Tampering | SVG path replacement in specimens | mitigate | xmllint --noout confirms valid XML after edit; grep confirms stale path removed and D4 path present |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all edits are to local static files |
</threat_model>

<verification>
After all four tasks complete, run the full check suite:

```bash
# Tokens version bumped
jq -r '.version' brandbook/tokens.json          # expected: 1.0.1
jq -r '.meta.changed' brandbook/tokens.json     # expected: 2026-06-12

# tokens.css provenance header
head -3 brandbook/tokens.css | grep -c 'tokens.json'   # expected: 1

# README Files table refreshed
grep -c 'Token Change Policy' brandbook/README.md      # expected: 1
grep -c 'logo-primary-subtitle' brandbook/README.md    # expected: ≥1
grep -c 'social-card-dark' brandbook/README.md         # expected: ≥1

# brand-book.md updated
grep -c 'Space Grotesk' brandbook/brand-book.md        # expected: ≥1
grep -c 'Inter Display Black' brandbook/brand-book.md  # expected: 0

# Stale specimens cleaned
grep -c 'M17 14v14' brandbook/examples/landing-hero.svg   # expected: 0
grep -c 'M17 14v14' brandbook/examples/readme-header.svg  # expected: 0

# All SVGs parse clean
find brandbook -maxdepth 1 -name '*.svg' | xargs -n1 xmllint --noout
xmllint --noout brandbook/examples/landing-hero.svg
xmllint --noout brandbook/examples/readme-header.svg

# No font binaries introduced
git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2' | wc -l   # expected: 0
```
</verification>

<success_criteria>
- tokens.json version is 1.0.1 with meta.changed set
- tokens.css has provenance header referencing v1.0.1
- README.md Files table: no "Rail Accent" in logo description rows; two new rows added; Token Change Policy section present
- brand-book.md: Inter Display Black removed, Space Grotesk v2.0 referenced; file list is 8 D4 assets; suite architecture and ember parity sections present
- Both stale specimens: M17 14v14 path gone, D4 geometry present, valid XML
- No font binaries introduced
</success_criteria>

<output>
Create `.planning/phases/182-brand-book-v2-tokens/182-01-SUMMARY.md` when done
</output>
