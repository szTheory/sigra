---
phase: "179"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/brand/package.json
  - scripts/brand/outline-wordmark.mjs
  - scripts/brand/critique-render.mjs
  - .gitignore
  - brandbook/README.md
autonomous: true
requirements:
  - BRAND2-04

must_haves:
  truths:
    - "Running `node scripts/brand/outline-wordmark.mjs <font.ttf> <wght> /tmp/test-out.svg` exits 0 and produces a valid SVG file"
    - "The output SVG contains exactly 5 `<path>` elements (one per glyph of 'sigra') with index-based IDs"
    - "The output SVG `<desc>` records font name, version, OFL license, opentype.js version, and generation date"
    - "No font TTF/OTF/WOFF binaries appear in `git ls-files`"
    - "`scripts/brand/fonts/` and `scripts/brand/node_modules/` are covered by .gitignore"
    - "brandbook/README.md has a Font Provenance section listing all 5 OFL fonts with versions, licenses, and source URLs"
    - "`scripts/brand/critique-render.mjs` can launch headless Chromium, load a `file://` HTML page, and screenshot it at multiple viewport sizes"
  artifacts:
    - path: "scripts/brand/package.json"
      provides: "opentype.js 2.0.0 dependency declaration, type: module, private"
      contains: "opentype.js"
    - path: "scripts/brand/outline-wordmark.mjs"
      provides: "CLI glyph-outlining script"
      exports: []
    - path: "scripts/brand/critique-render.mjs"
      provides: "Playwright headless screenshot harness"
      exports: []
    - path: "brandbook/README.md"
      provides: "Font provenance section with OFL font table"
      contains: "Font Provenance"
  key_links:
    - from: "scripts/brand/outline-wordmark.mjs"
      to: "scripts/brand/node_modules/opentype.js"
      via: "ESM import"
      pattern: "loadSync|opentype"
    - from: "scripts/brand/critique-render.mjs"
      to: "test/example/priv/playwright/node_modules/playwright-core"
      via: "NODE_PATH or require resolution"
      pattern: "playwright-core"
    - from: "brandbook/README.md"
      to: "OFL font sources"
      via: "Font Provenance table"
      pattern: "OFL|opentype"
---

<objective>
Build and smoke-test the complete glyph-outlining + render-harness toolchain that Plan 02 depends on. Produces the BRAND2-04 deliverable: a committed, reproducible script that downloads OFL fonts to a gitignored location, outlines "sigra" into per-glyph SVG `<path>` elements with provenance `<desc>`, and a companion Playwright-based render harness for headless critique screenshots.

Purpose: Phase 180 requires pre-verified candidate SVGs; that verification depends on screenshots, which depend on the harness; both scripts depend on the toolchain being installable and runnable. This plan creates the infrastructure.

Output: scripts/brand/ toolchain (package.json, outline-wordmark.mjs, critique-render.mjs), .gitignore additions, brandbook/README.md font provenance section, and a smoke-run proof of end-to-end pipeline (outline "sigra" → valid SVG with ≥5 path elements and provenance desc → no binaries in git).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brandbook/logo-v2-design-brief.md
@.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-RESEARCH.md
@.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md

<interfaces>
<!-- Key references the executor needs. Extracted from codebase. -->

From brandbook/logo-primary.svg (accessibility shell — replicate in all output SVGs):
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="20 12 188 54" role="img" aria-labelledby="title desc">
    <title id="title">Sigra primary logo</title>
    <desc id="desc">The Sigra Rail Accent tight lockup for light surfaces. Wordmark outlined from Inter Display Black v4.1.</desc>

From brandbook/logo-mark.svg (ember palette source):
  ember-300: #fdba74  (accent soft — lighter rail bars, tittle replacements)
  ember-700: #c2410c  (primary ember anchor)
  ember-800: #9a3412  (deep ember — horizontal core line)

From test/example/priv/playwright/package.json (package.json format):
  { "name": "sigra-example-playwright", "private": true, "description": "..." }
  Use "private": true, "type": "module", "dependencies" (not "devDependencies") for opentype.js.

From .gitignore line 25 (existing /tmp/ coverage):
  /tmp/  ← already covered; render output to /tmp/sigra-renders/ needs no new entry.
  New entries: /scripts/brand/node_modules/ and /scripts/brand/fonts/

From RESEARCH.md Pattern 1 (opentype.js 2.0.0 API — confirmed):
  import { loadSync } from 'opentype.js';
  const font = loadSync(fontPath);
  if (font.variation) font.variation.set({ wght: N });
  font.forEachGlyph('sigra', 0, fontSize, fontSize, { kerning: true }, (glyph, x, y) => {
    const d = glyph.getPath(x, y, fontSize).toPathData({ decimalPlaces: 2 });  // flipY: true (default)
    // ...
  });
  // Total width: font.getAdvanceWidth('sigra', fontSize)

From RESEARCH.md Pitfall 1 (CRITICAL — upside-down output):
  NEVER call .toPathData(2) — integer shorthand sets flipY: false in v2.0 → upside-down wordmark.
  ALWAYS pass options object: .toPathData({ decimalPlaces: 2 }) which inherits flipY: true.

From RESEARCH.md Pitfall 6 (viewBox calculation):
  Work at fontSize = font.unitsPerEm for precision.
  vbPad = fontSize * 0.05; vbW = totalWidth + 2*vbPad; vbH = fontSize * 1.3

From RESEARCH.md Pattern 3 (Playwright standalone — confirmed working):
  import { chromium } from 'playwright-core';
  // playwright-core already installed at test/example/priv/playwright/node_modules/playwright-core
  // Chromium binary cached at ~/Library/Caches/ms-playwright/chromium-1223/
  const browser = await chromium.launch({ headless: true });
  await page.setViewportSize({ width: W, height: H });
  await page.emulateMedia({ colorScheme: 'dark' });  // or 'light'
  await page.goto(`file:///path/to/harness.html`);
  const buf = await page.screenshot({ type: 'png' });

Font download URLs (all verified from RESEARCH.md Standard Stack):
  Space Grotesk [wght].ttf: https://github.com/google/fonts/raw/main/ofl/spacegrotesk/SpaceGrotesk%5Bwght%5D.ttf
  Syne [wght].ttf: https://raw.githubusercontent.com/google/fonts/main/ofl/syne/Syne%5Bwght%5D.ttf
  Plus Jakarta Sans: https://github.com/tokotype/PlusJakartaSans/releases/download/2.7.1/PlusJakartaSans-2.7.1.zip (extract ExtraBold TTF)
  Geist: https://github.com/vercel/geist-font/releases/download/v1.7.2/geist-font-v1.7.2.zip (extract Black TTF)
  Inter v4.1: https://github.com/rsms/inter/releases/download/v4.1/Inter-4.1.zip (extract Inter-Black or variable TTF)

From brandbook/README.md lines 28–48 (Logo System section to extend — Font Provenance subsection goes AFTER Logo System paragraph, BEFORE Maintenance Rules):
  Pattern: "## Font Provenance" with OFL table: Font | Version | License | Source | Used in
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolchain scaffolding — gitignore, package.json, and font downloads</name>
  <files>
    .gitignore,
    scripts/brand/package.json,
    scripts/brand/fonts/ (gitignored, fonts downloaded here)
  </files>

  <read_first>
    - /Users/jon/projects/sigra/.gitignore (full file — must read before editing to avoid clobbering existing content)
    - /Users/jon/projects/sigra/brandbook/logo-v2-design-brief.md (OFL Typeface Candidates table — font names/versions/URLs)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md (gitignore pattern at "# Brand toolchain" section; package.json pattern from test/example/priv/playwright/package.json analog)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-RESEARCH.md (Standard Stack table — font download URLs; Environment Availability — missing deps)
  </read_first>

  <action>
  Create `scripts/brand/` directory. Append two new entries to `.gitignore` as a new comment-headed group at the end of the file (after the existing `/prompts/` entry): `# Brand toolchain — font binaries and render artifacts (never commit font TTFs).`, then `/scripts/brand/node_modules/`, then `/scripts/brand/fonts/`. Do NOT add `/tmp/sigra-renders/` — `/tmp/` is already covered on line 25.

  Create `scripts/brand/package.json` matching the pattern from `test/example/priv/playwright/package.json`: use `"private": true`, `"type": "module"`, `"description": "Brand toolchain scripts for Sigra (phase 179 BRAND2-04)"`, `"name": "sigra-brand-tools"`, `"version": "0.1.0"`, and `"dependencies": { "opentype.js": "^2.0.0" }`. No `"scripts"` entry needed.

  Run `npm install` from inside `scripts/brand/` to install opentype.js 2.0.0. Confirm `scripts/brand/node_modules/opentype.js/package.json` exists with version 2.0.0.

  Download the five OFL variable TTFs to `scripts/brand/fonts/` using `curl -L -o`:
  - `scripts/brand/fonts/SpaceGrotesk[wght].ttf` from https://github.com/google/fonts/raw/main/ofl/spacegrotesk/SpaceGrotesk%5Bwght%5D.ttf
  - `scripts/brand/fonts/Syne[wght].ttf` from https://raw.githubusercontent.com/google/fonts/main/ofl/syne/Syne%5Bwght%5D.ttf
  - `scripts/brand/fonts/Inter-VariableFont.ttf` from the Inter v4.1 release ZIP at https://github.com/rsms/inter/releases/download/v4.1/Inter-4.1.zip (unzip to /tmp/inter-dl/, find `Inter[opsz,wght].ttf` or `InterVariable.ttf`, copy to fonts/)
  - `scripts/brand/fonts/PlusJakartaSans-ExtraBold.ttf` from https://github.com/tokotype/PlusJakartaSans/releases/download/2.7.1/PlusJakartaSans-2.7.1.zip (unzip to /tmp/pjs-dl/, find ExtraBold static TTF, copy to fonts/)
  - `scripts/brand/fonts/Geist-Black.ttf` from https://github.com/vercel/geist-font/releases/download/v1.7.2/geist-font-v1.7.2.zip (unzip to /tmp/geist-dl/, find Black or variable TTF, copy to fonts/)

  After downloads, verify `git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'` returns empty output (gitignore is working). If any binary appears in git output, the gitignore entry is wrong — fix before proceeding.
  </action>

  <verify>
    <automated>
      cd /Users/jon/projects/sigra && node -e "require('/Users/jon/projects/sigra/scripts/brand/node_modules/opentype.js')" 2>/dev/null || node --input-type=module --eval "import opentype from '/Users/jon/projects/sigra/scripts/brand/node_modules/opentype.js/dist/opentype.js'; console.log('opentype.js loaded, version:', opentype.version || 'ok')" && ls scripts/brand/fonts/*.ttf | wc -l && git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'
    </automated>
  </verify>

  <acceptance_criteria>
    - `scripts/brand/node_modules/opentype.js/` exists and its package.json shows version ^2.0.0
    - `scripts/brand/fonts/` contains at least 5 TTF files (one per candidate font family)
    - `git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'` returns empty (no font binaries committed)
    - `.gitignore` contains `/scripts/brand/node_modules/` and `/scripts/brand/fonts/` (verify with `grep 'scripts/brand' .gitignore`)
    - `git status` shows `scripts/brand/package.json` and `scripts/brand/package-lock.json` as new committed files; `scripts/brand/fonts/` and `scripts/brand/node_modules/` appear under "Untracked files" or in the ignored list, never under "Changes to be committed"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: outline-wordmark.mjs — per-glyph SVG path generator</name>
  <files>
    scripts/brand/outline-wordmark.mjs
  </files>

  <read_first>
    - /Users/jon/projects/sigra/brandbook/logo-primary.svg (full file — SVG accessibility shell pattern, title/desc, viewBox convention, glyph group fill color #151515)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-RESEARCH.md (Pattern 1 opentype.js per-glyph outline; Pitfall 1 flipY CRITICAL; Pitfall 6 viewBox calculation; Pattern 2 variable font weight setting; Code Examples section — minimal outline-wordmark.mjs skeleton)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md (outline-wordmark.mjs section — header comment convention, SVG shell pattern, per-glyph id convention, fill="currentColor" guidance)
    - /Users/jon/projects/sigra/brandbook/logo-v2-design-brief.md (OFL Typeface Candidates table — font names for provenance desc)
  </read_first>

  <action>
  Create `scripts/brand/outline-wordmark.mjs` as a Node.js ESM script. Header comment per PATTERNS.md convention:
    - Shebang: `#!/usr/bin/env node`
    - Phase comment: `// Phase 179 (BRAND2-04): Outline OFL font glyphs for "sigra" into per-glyph SVG <path> elements.`
    - Usage comment: `// Usage: node scripts/brand/outline-wordmark.mjs <font.ttf> <wght> <output.svg>`
    - Run-context note: `// Run from repo root. Font must be in scripts/brand/fonts/ (gitignored). Run npm install in scripts/brand/ first.`

  Import `loadSync` from `opentype.js` using a `createRequire` pattern to handle the CJS/ESM boundary:
    `import { createRequire } from 'module'; const require = createRequire(import.meta.url); const opentype = require('opentype.js');`
  Then use `opentype.loadSync(fontPath)`.

  Parse CLI args: `const [,, fontPath, wghtStr, outPath] = process.argv;`. Validate all three are provided; exit 1 with usage message if not.

  Load font, set variation axis, outline glyphs:
  - `const font = opentype.loadSync(fontPath);`
  - `if (font.variation) font.variation.set({ wght: Number(wghtStr) || 700 });` (per Pitfall 4 guard)
  - Work at `fontSize = font.unitsPerEm` (native UPM for path precision — never a small pixel value)
  - Pass `y = fontSize` to `forEachGlyph` as the baseline (so paths are positive-Y after flipY)
  - Call `font.forEachGlyph('sigra', 0, fontSize, fontSize, { kerning: true }, (glyph, x, y) => { ... })`
  - Inside callback: call `glyph.getPath(x, y, fontSize).toPathData({ decimalPlaces: 2 })` — ALWAYS the object form, NEVER the integer shorthand — this is mandatory per Pitfall 1 to get `flipY: true`
  - Collect per-glyph path strings with index-based IDs: `id="g-0"` through `id="g-4"` (not glyph-name IDs per PATTERNS.md)
  - Accumulate total width using `font.getAdvanceWidth('sigra', fontSize)` for viewBox calculation

  Compute viewBox: `vbPad = fontSize * 0.05; vbX = -vbPad; vbW = getAdvanceWidth + 2*vbPad; vbH = fontSize * 1.3`

  Build provenance desc string: `Font: ${font.getEnglishName('fullName') || fontPath} wght=${wghtStr}. OFL licensed. Outlined with opentype.js 2.0.0. Generated ${new Date().toISOString().slice(0,10)}.`

  Emit SVG using the accessibility shell pattern from `brandbook/logo-primary.svg`: `role="img" aria-labelledby="title desc"`, `<title id="title">sigra wordmark outline</title>`, `<desc id="desc">...</desc>`. Use `fill="currentColor"` on the `<g id="glyphs">` element (not hardcoded `#151515`) so dark-mode SVGs inherit correctly via CSS.

  Write SVG to `outPath` with `writeFileSync`. Print `Wrote ${outPath} (${paths.length} glyphs, width ≈ ${totalWidth.toFixed(0)} UPM)` to stdout. Exit 0.

  After creating the script, run it for a smoke test on Space Grotesk to prove the pipeline end-to-end: `node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/SpaceGrotesk[wght].ttf 700 /tmp/sigra-sg-test.svg`
  </action>

  <verify>
    <automated>
      cd /Users/jon/projects/sigra && node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/SpaceGrotesk\[wght\].ttf 700 /tmp/sigra-sg-test.svg && python3 -c "import xml.etree.ElementTree as ET; ET.parse('/tmp/sigra-sg-test.svg'); print('SVG valid')" && grep -c 'id="g-' /tmp/sigra-sg-test.svg && grep -c 'Font:' /tmp/sigra-sg-test.svg
    </automated>
  </verify>

  <acceptance_criteria>
    - Script exits 0 when called with valid args: `node scripts/brand/outline-wordmark.mjs <font.ttf> <wght> /tmp/test.svg`
    - Script exits non-zero (not silently) when called without args
    - Output SVG passes `python3 -c "import xml.etree.ElementTree as ET; ET.parse('/tmp/sigra-sg-test.svg'); print('valid')"` (prints "valid")
    - `grep -c 'id="g-' /tmp/sigra-sg-test.svg` returns 5 (one path element per glyph of "sigra")
    - `grep -c 'Font:' /tmp/sigra-sg-test.svg` returns 1 (provenance desc present)
    - `grep 'toPathData' scripts/brand/outline-wordmark.mjs` shows only the object form `{ decimalPlaces: 2 }` — integer shorthand is absent (Pitfall 1 compliance)
    - `grep 'fill="currentColor"' scripts/brand/outline-wordmark.mjs` matches (not hardcoded hex on the glyph group)
    - Running smoke test on all 5 downloaded fonts exits 0 for each (Space Grotesk, Syne, Plus Jakarta Sans ExtraBold, Inter variable, Geist Black or nearest available weight)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: critique-render.mjs + brandbook/README.md provenance update</name>
  <files>
    scripts/brand/critique-render.mjs,
    brandbook/README.md
  </files>

  <read_first>
    - /Users/jon/projects/sigra/brandbook/README.md (full file — read before editing; identify Logo System section and Maintenance Rules section; Font Provenance subsection goes between them)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-RESEARCH.md (Pattern 3 Playwright standalone screenshot; Environment Availability — playwright-core path and chromium binary location)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md (critique-render.mjs section — "No Analog Found" note; Playwright resolution via NODE_PATH; brandbook/README.md — Font Provenance subsection format with OFL table)
    - /Users/jon/projects/sigra/brandbook/logo-v2-design-brief.md (OFL Typeface Candidates table — all 5 font names, versions, source URLs, and which candidates use each)
  </read_first>

  <action>
  Create `scripts/brand/critique-render.mjs`. This is a standalone headless Chromium screenshot script — no test framework, no server. Header comment: `// Phase 179 (BRAND2-04): Playwright headless render harness for per-candidate critique screenshots.` and `// Usage: node scripts/brand/critique-render.mjs <harness.html> <out-dir>`.

  Resolve `playwright-core` via `createRequire` pointing at the existing install: `import { createRequire } from 'module'; const require = createRequire('/Users/jon/projects/sigra/test/example/priv/playwright/'); const { chromium } = require('playwright-core');`. This avoids installing playwright-core again in scripts/brand/ — it reuses the confirmed-working install.

  The script accepts `<harnessHtmlAbsPath>` and `<outDir>` as CLI args. It screenshots at four scale profiles:
  - `{ name: '16px-favicon', width: 64, height: 64 }` — small viewport, logo rendered at 16px via CSS in harness
  - `{ name: '32px', width: 128, height: 128 }`
  - `{ name: '54px-topbar', width: 800, height: 160 }` — wide viewport so boundary-breaking designs don't clip
  - `{ name: 'hero', width: 1200, height: 300 }`

  For each scale, screenshot in both `light` and `dark` colorSchemes via `page.emulateMedia({ colorScheme: scheme })`. Write PNG to `${outDir}/${baseName}-${scale.name}-${scheme}.png` using `writeFileSync` from `fs`. Create `outDir` with `mkdirSync(outDir, { recursive: true })`.

  Await `browser.close()` before exiting. Print the list of files written. Exit 0.

  The harness HTML format (generated by Plan 02 tasks): a minimal HTML that embeds or references one candidate SVG and applies `@media (prefers-color-scheme: dark) { body { background: #151515; } }` plus a CSS class toggling the SVG's context color.

  After creating the script, do a smoke run against a minimal generated harness page to confirm Playwright launch succeeds:
  - Create `/tmp/sigra-harness-smoke.html` containing: `<!doctype html><html><body><img src="/Users/jon/projects/sigra/brandbook/logo-primary.svg" width="200" alt=""></body></html>`
  - Run: `node scripts/brand/critique-render.mjs /tmp/sigra-harness-smoke.html /tmp/sigra-renders/smoke`
  - Verify at least one PNG is created at `/tmp/sigra-renders/smoke/`

  Update `brandbook/README.md`: add a `## Font Provenance` section immediately after the Logo System section paragraph (before `## Maintenance Rules`). The section contains:
  - One sentence: "Round-3 candidate wordmarks are outlined using opentype.js 2.0.0 (MIT) from OFL-licensed variable TTFs. Font binaries are gitignored; only the resulting SVG path data is committed. Each candidate SVG `<desc>` records: font name, version, OFL license, opentype.js version, and generation date."
  - A pipe table with columns `Font | Version | License | Source | Used in` and rows for: Inter Display v4.1 (logo-primary.svg + round-3 A4), Space Grotesk v2.0.0 (round-3 A1, B1), Plus Jakarta Sans v2.7.1 (round-3 A2, C1), Syne latest (round-3 A3), Geist v1.7.2 (round-3 B2). Source URLs from the design brief OFL candidates table.
  </action>

  <verify>
    <automated>
      cd /Users/jon/projects/sigra && node scripts/brand/critique-render.mjs /tmp/sigra-harness-smoke.html /tmp/sigra-renders/smoke && ls /tmp/sigra-renders/smoke/*.png | wc -l && grep -c 'Font Provenance' brandbook/README.md && grep -c 'OFL\|opentype' brandbook/README.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/brand/critique-render.mjs /tmp/sigra-harness-smoke.html /tmp/sigra-renders/smoke` exits 0
    - `/tmp/sigra-renders/smoke/` contains at least 8 PNG files (4 scales × 2 color schemes)
    - Each PNG is > 500 bytes (not an empty or corrupt screenshot)
    - `grep -c 'Font Provenance' brandbook/README.md` returns 1
    - `grep -c 'OFL' brandbook/README.md` returns ≥ 3 (present in multiple rows of the provenance table)
    - `grep -c 'opentype' brandbook/README.md` returns ≥ 1
    - The Font Provenance section in README.md appears BEFORE `## Maintenance Rules` and AFTER `## Logo System` paragraph (verify visually by reading the surrounding context)
    - `git ls-files brandbook/README.md` is tracked and `git diff brandbook/README.md` shows only the Font Provenance addition (no deletions to existing content)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → scripts/brand/fonts/ | OFL font TTFs downloaded from external GitHub URLs; not from arbitrary sources |
| npm registry → scripts/brand/node_modules/ | opentype.js 2.0.0 installed from npm; postinstall scripts would run with local user privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-01 | Tampering | Font download (curl from github.com) | mitigate | Only download from the five verified OFL source repos enumerated in RESEARCH.md Standard Stack; curl to gitignored fonts/ directory only; no font binary ever reaches git |
| T-179-02 | Tampering | npm install opentype.js | mitigate | Pin `"opentype.js": "^2.0.0"` in package.json; opentype.js has no `scripts.postinstall` (verified in RESEARCH.md Package Legitimacy Audit); 13-year-old library, 1.34M weekly downloads, MIT, official opentypejs org |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck verification noted in RESEARCH.md as manually assessed; opentype.js is the only new package; disposition approved based on 13-year age + 1.34M downloads + no postinstall hooks |
</threat_model>

<verification>
Run the full machine-verifiable check suite from VALIDATION.md after completing all three tasks:

```bash
# 1. Script exits 0 and emits valid SVG
node /Users/jon/projects/sigra/scripts/brand/outline-wordmark.mjs \
  /Users/jon/projects/sigra/scripts/brand/fonts/SpaceGrotesk\[wght\].ttf 700 /tmp/sigra-final-smoke.svg

# 2. Output is valid XML
python3 -c "import xml.etree.ElementTree as ET; ET.parse('/tmp/sigra-final-smoke.svg'); print('SVG: valid')"

# 3. Contains ≥5 path elements (one per glyph)
grep -c 'id="g-' /tmp/sigra-final-smoke.svg

# 4. Contains provenance desc
grep -c 'Font:' /tmp/sigra-final-smoke.svg

# 5. No font binaries committed
cd /Users/jon/projects/sigra && git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'

# 6. README has font provenance
grep -c 'OFL\|outlined\|opentype' /Users/jon/projects/sigra/brandbook/README.md

# 7. Playwright smoke run produces screenshots
ls /tmp/sigra-renders/smoke/*.png | wc -l
```

All checks must pass before Plan 02 begins.
</verification>

<success_criteria>
- `scripts/brand/package.json` committed with `"opentype.js": "^2.0.0"` dependency
- `scripts/brand/outline-wordmark.mjs` committed; smoke run exits 0; output SVG has ≥5 path elements with index-based IDs and Font: provenance in `<desc>`
- `scripts/brand/critique-render.mjs` committed; Playwright smoke run exits 0; ≥8 PNGs written to /tmp/
- `.gitignore` covers `scripts/brand/node_modules/` and `scripts/brand/fonts/`; `git ls-files *.ttf *.otf` returns empty
- `brandbook/README.md` has Font Provenance section with 5-row OFL table including provenance for round-3 candidate fonts
- No font TTF/OTF/WOFF/WOFF2 binaries appear under `git ls-files` at any point during this plan
</success_criteria>

<output>
Create `.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-01-SUMMARY.md` when done.
</output>
