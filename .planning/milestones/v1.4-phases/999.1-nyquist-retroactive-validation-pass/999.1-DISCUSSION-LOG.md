# Phase 999.1: nyquist-retroactive-validation-pass - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.  
> Decisions are captured in **`999.1-CONTEXT.md`**.

**Date:** 2026-04-23  
**Phase:** 999.1 — nyquist-retroactive-validation-pass  
**Areas discussed:** `[--all]` auto-selected — (1) Pointer CONTEXT, (2) STATE vs VALIDATION, (3) Roadmap cross-link hygiene, (4) New Nyquist re-pass policy  
**Method:** Four parallel **generalPurpose** research subagents; parent synthesized into locked decisions.

---

## Area 1 — Formal `999.1-CONTEXT.md` (pointer-only)

| Option | Description | Selected |
|--------|-------------|----------|
| VALIDATION-only tombstone | Rely on `999.1-VALIDATION.md` only | |
| **Pointer CONTEXT + VALIDATION** | Short `CONTEXT.md` + peer `VALIDATION.md`, same supersession | ✓ |
| README-only in phase dir | GitHub-first “start here” | |
| Nothing beyond ROADMAP | Lowest file count | |

**User's choice:** **All areas** — adopt **pointer `CONTEXT.md`** aligned with GSD’s CONTEXT-centric discuss → plan flow; keep **VALIDATION** as peer with synced short truth.

**Notes:** Subagent compared Rust deprecation attrs, K8s API skew docs, RFC obsoletes, Hex `@deprecated` / changelog patterns; flagged footgun of agents treating missing CONTEXT as “not started.”

---

## Area 2 — `STATE.md` vs `999.1-VALIDATION.md` alignment

| Option | Description | Selected |
|--------|-------------|----------|
| Manual discipline only | Update STATE when closing phases | |
| **Demote STATE as roadmap** | STATE = narrative/handoff; queue = ROADMAP + phase attestation | ✓ |
| Auto-derive STATE | Generator from ROADMAP/git | |
| CI gate on contradiction | Fail if STATE points at superseded phase | (deferred — see CONTEXT deferred) |

**User's choice:** **STATE must not** be a second authoritative roadmap; remove false **999.1** “current focus” / “ready to plan” while card is superseded.

---

## Area 3 — ROADMAP / PROJECT cross-link hygiene

| Approach | Description | Selected |
|----------|-------------|----------|
| Duplicated deep tables | Repeat matrices in multiple files | |
| **Hub-and-spoke + thin summaries** | ROADMAP/archives hub; one canonical path per claim | ✓ |
| Archive-only | No tombstone in hub | |

**User's choice:** Strengthen **one canonical link per claim**; **999.x** as namespace with forward link to **36**; clarify Hex vs GitHub evidence split per existing **MAINTAINING** patterns.

---

## Area 4 — New Nyquist retro pass (vs archaeology)

| Policy | Description | Selected |
|--------|-------------|----------|
| Standing re-validation | Periodic full retro | |
| **Event-driven bounded work** | New phase only on triggers; prefer machine substitutes | ✓ |
| Promote 999.x casually | Re-open parking lot without driver | |

**User's choice:** Default **no** new work under **999.1**; future assurance via **bounded phases**, matrix deltas, waivers with substitutes — aligned with SEED-002 / SEED-001 / NYQ shipped culture.

---

## Claude's Discretion

_None recorded for implementation inside this directory._

## Deferred Ideas

- CI assertion: STATE “current focus” must not reference superseded-only phases.
- Optional `999.x-REDIRECT.md` hub if grep confusion persists.
