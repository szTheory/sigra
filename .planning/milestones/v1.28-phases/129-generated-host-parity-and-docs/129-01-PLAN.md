---
phase: 129-generated-host-parity-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/templates/settings_live_test.exs
  - test/sigra/install/isolation_test.exs
  - priv/templates/sigra.install/core/auth.ex
  - priv/templates/sigra.install/core/settings_live.ex
  - priv/templates/sigra.install/core/reactivation_live.ex
  - priv/templates/sigra.install/core/emails.ex
  - test/example/lib/example/accounts.ex
  - test/example/lib/example_web/live/settings_live.ex
  - test/example/lib/example_web/live/reactivation_live.ex
  - test/example/lib/example/accounts/emails.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/settings_live.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/reactivation_live.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts/emails.ex
autonomous: true
requirements: [HOST-01]
must_haves:
  truths:
    - "Generated host templates expose a thin auth-data export wrapper that delegates to Sigra.DataExport.export_auth_data/3 with only always-core schema defaults."
    - "Caller opts can supply or override optional export schemas so DataExport omission notes remain truthful when optional schemas are absent."
    - "Example app exposes a thin auth-data export wrapper without rebuilding payload shape, and may pass concrete optional schemas that exist in the example app."
    - "Generated, example, and golden lifecycle copy avoid broad hard-delete and permanent-removal claims for strategy-neutral flows."
    - "Install golden output mirrors the current generated templates after rebless."
  artifacts:
    - path: "priv/templates/sigra.install/core/auth.ex"
      provides: "Generated export_auth_data/2 wrapper, default_auth_export_opts/0, and existing lifecycle wrappers"
      contains: "Keyword.merge(default_auth_export_opts(), opts)"
    - path: "test/example/lib/example/accounts.ex"
      provides: "Example app export_auth_data/2 wrapper with existing sensitive-operation guard style preserved"
      contains: "Sigra.DataExport.export_auth_data"
    - path: "test/sigra/install/isolation_test.exs"
      provides: "Core isolation assertions preventing unguarded optional schema references in auth.ex"
      contains: "identity_schema:"
    - path: "test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex"
      provides: "Golden fixture generated wrapper evidence"
      contains: "def export_auth_data(user, opts \\\\ [])"
    - path: "test/sigra/templates/settings_live_test.exs"
      provides: "Template parity and copy truth assertions"
      contains: "Sigra.DataExport.export_auth_data"
  key_links:
    - from: "priv/templates/sigra.install/core/auth.ex"
      to: "lib/sigra/data_export.ex"
      via: "generated wrapper delegates to Sigra.DataExport.export_auth_data/3 with caller-mergeable defaults"
      pattern: "Keyword\\.merge\\(default_auth_export_opts\\(\\), opts\\)"
    - from: "test/example/lib/example/accounts.ex"
      to: "lib/sigra/data_export.ex"
      via: "example wrapper delegates to Sigra.DataExport.export_auth_data/3"
      pattern: "Sigra\\.DataExport\\.export_auth_data\\("
    - from: "priv/templates/sigra.install/core/*.ex"
      to: "test/fixtures/install_golden/tree/**"
      via: "MIX_ENV=test mix sigra.fixture.rebless_golden"
      pattern: "def export_auth_data\\(user, opts \\\\\\\\ \\[\\]\\)"
---

<objective>
Align generated host templates, the example app, and the install golden fixture with the bounded Phase 127/128 data-lifecycle contract.

Purpose: `HOST-01` requires generated host surfaces to preserve library-owned export and lifecycle semantics without duplicating payload or deletion truth.
Output: Template/example wrappers for `export_auth_data/2`, strategy-neutral lifecycle copy, and regenerated golden fixture evidence.

Scope note: this plan intentionally touches 14 files because the golden fixture mirrors the generated template and example app surfaces and the isolation test now pins optional-schema omission truth. Keep execution commits task-scoped: Task 1 tests, Task 2 template/example implementation, Task 3 golden rebless.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-generated-host-parity-and-docs/129-CONTEXT.md
@.planning/phases/129-generated-host-parity-and-docs/129-RESEARCH.md
@.planning/phases/129-generated-host-parity-and-docs/129-PATTERNS.md
@.planning/phases/129-generated-host-parity-and-docs/129-VALIDATION.md
@.planning/phases/127-versioned-auth-data-export/127-CONTEXT.md
@.planning/phases/127-versioned-auth-data-export/127-VERIFICATION.md
@.planning/phases/128-account-deletion-lifecycle-truth/128-CONTEXT.md
@.planning/phases/128-account-deletion-lifecycle-truth/128-01-SUMMARY.md
@CLAUDE.md
@test/example/AGENTS.md

<interfaces>
<!-- Key contracts the executor should use directly. -->

From `lib/sigra/data_export.ex`:
```elixir
@spec export_auth_data(module(), struct(), keyword()) :: {:ok, map()}
def export_auth_data(repo, user, opts \\ [])
```

Always-core schema defaults for the generated core wrapper:
```elixir
[
  session_schema: MyApp.Accounts.UserSession,
  audit_schema: MyApp.Accounts.AuditEvent,
  mfa_credential_schema: MyApp.Accounts.UserMFACredential,
  backup_code_schema: MyApp.Accounts.UserBackupCode
]
```

Optional/separately generated schema options that must not be hardcoded in the core generated wrapper:
```elixir
[
  identity_schema: MyApp.Accounts.UserIdentity,
  user_passkey_schema: MyApp.Accounts.UserPasskey,
  membership_schema: MyApp.Accounts.OrganizationMembership
]
```

The core generated wrapper must allow callers or separately generated code to supply those optional schemas via `opts`, so `Sigra.DataExport.omissions/1` continues to report omitted sections when those schemas are absent. The example app wrapper may pass concrete optional schemas that exist in `test/example` (for example `UserPasskey` and `OrganizationMembership`) because that is example-specific evidence, not the core template contract.

From generated lifecycle wrapper pattern:
```elixir
def schedule_deletion(user, opts \\ []) do
  Sigra.Auth.schedule_deletion(sigra_config(), user, Keyword.merge([...], opts))
end

def cancel_deletion(user, opts \\ []) do
  Sigra.Auth.cancel_deletion(sigra_config(), user, Keyword.merge([...], opts))
end

def deletion_status(user) do
  Sigra.Account.deletion_status(user)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 template assertions for export delegation and deletion-copy truth</name>
  <files>test/sigra/templates/settings_live_test.exs, test/sigra/install/isolation_test.exs</files>
  <read_first>
    test/sigra/templates/settings_live_test.exs
    test/sigra/install/isolation_test.exs
    priv/templates/sigra.install/core/auth.ex
    priv/templates/sigra.install/core/settings_live.ex
    priv/templates/sigra.install/core/reactivation_live.ex
    priv/templates/sigra.install/core/emails.ex
    .planning/phases/129-generated-host-parity-and-docs/129-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: raw `auth.ex` template contains `def export_auth_data(user, opts \\ [])`, `Sigra.DataExport.export_auth_data(Repo, user,`, `Keyword.merge(default_auth_export_opts(), opts)`, and `defp default_auth_export_opts`.
    - Test 2: raw `auth.ex` template default wrapper opts contain only always-core schema keys `session_schema:`, `audit_schema:`, `mfa_credential_schema:`, and `backup_code_schema:`.
    - Test 3: raw `auth.ex` template does not contain unguarded `identity_schema:`, `user_passkey_schema:`, or `membership_schema:` in the export default opts; those optional schemas must be supplied by caller opts or separately generated/gated code so omission notes are preserved when absent.
    - Test 4: raw `auth.ex` template proves caller opts can supply optional schemas by asserting `Keyword.merge(default_auth_export_opts(), opts)` is used, which lets `opts` add `identity_schema`, `user_passkey_schema`, `membership_schema`, or override any default schema option.
    - Test 5: raw `auth.ex` template does not construct library payload keys; refute `schema_version:`, `omissions:`, and `enterprise:` in the `export_auth_data` wrapper body.
    - Test 6: `test/sigra/install/isolation_test.exs` checks core `auth.ex` has no unguarded optional schema options by refuting `identity_schema:`, `user_passkey_schema:`, and `membership_schema:` unless a future implementation explicitly gates them with existing installer variables such as `<%= if passkeys? do %>` or `<%= if organizations? do %>`.
    - Test 7: raw `settings_live.ex`, `reactivation_live.ex`, and `emails.ex` templates do not contain `all associated data`, `account and data will be permanently removed`, or `account and associated data have been permanently removed`.
  </behavior>
  <action>
    Extend `Sigra.Templates.SettingsLiveTest` with a new `describe "auth.ex generated data export wrapper"` block that reads `auth.ex` from `@templates_dir`, plus copy-truth assertions in the existing settings/reactivation blocks and a new emails-template block.

    Write the failing assertions first. Keep them as raw file-content checks, matching the existing template test style. Use exact string assertions for `def export_auth_data(user, opts \\\\ [])`, `Sigra.DataExport.export_auth_data(Repo, user,`, `Keyword.merge(default_auth_export_opts(), opts)`, `defp default_auth_export_opts`, and the always-core schema option keys `session_schema:`, `audit_schema:`, `mfa_credential_schema:`, and `backup_code_schema:`.

    Add negative assertions proving omission truth is preserved for optional/separately generated schemas: the core template must not contain unguarded `identity_schema:`, `user_passkey_schema:`, or `membership_schema:` in the default export opts. The test may permit those strings only if a future executor implements exact EEx gating with existing installer variables (`<%= if passkeys? do %>` for passkeys, `<%= if organizations? do %>` for memberships, and an OAuth-generator/configured path for identities); otherwise the default expectation is no such keys in the core generated wrapper. Also assert that caller opts remain mergeable via `Keyword.merge(default_auth_export_opts(), opts)` so callers can supply `identity_schema`, `user_passkey_schema`, `membership_schema`, or override any default schema option.

    Add negative deletion-copy assertions with `refute content =~ "all associated data"`, `refute content =~ "account and data will be permanently removed"`, and `refute content =~ "account and associated data have been permanently removed"`.

    This implements D-03, D-04, D-05, D-07, D-08, D-12 and mitigates T-129-01/T-129-02 before production edits.
  </action>
  <acceptance_criteria>
    - `rg -n "def export_auth_data\\(user, opts \\\\\\\\ \\[\\]\\)|Sigra\\.DataExport\\.export_auth_data\\(Repo, user|Keyword\\.merge\\(default_auth_export_opts\\(\\), opts\\)|defp default_auth_export_opts|session_schema:|audit_schema:|mfa_credential_schema:|backup_code_schema:" test/sigra/templates/settings_live_test.exs` shows the added assertions.
    - `rg -n "identity_schema:|user_passkey_schema:|membership_schema:" test/sigra/templates/settings_live_test.exs test/sigra/install/isolation_test.exs` shows only omission-preservation/negative assertions, not instructions that the core wrapper must pass those defaults.
    - `rg -n "all associated data|account and data will be permanently removed|account and associated data have been permanently removed" test/sigra/templates/settings_live_test.exs` shows only negative/refute assertions.
    - The test fails before Task 2 implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/templates/settings_live_test.exs test/sigra/install/isolation_test.exs --max-failures 1</automated>
  </verify>
  <done>The tests pin the required thin export wrapper, preserve optional-schema omission truth, and reject broad generated deletion copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement thin generated/example export wrappers and soften lifecycle copy</name>
  <files>priv/templates/sigra.install/core/auth.ex, priv/templates/sigra.install/core/settings_live.ex, priv/templates/sigra.install/core/reactivation_live.ex, priv/templates/sigra.install/core/emails.ex, test/example/lib/example/accounts.ex, test/example/lib/example_web/live/settings_live.ex, test/example/lib/example_web/live/reactivation_live.ex, test/example/lib/example/accounts/emails.ex</files>
  <read_first>
    priv/templates/sigra.install/core/auth.ex
    priv/templates/sigra.install/core/settings_live.ex
    priv/templates/sigra.install/core/reactivation_live.ex
    priv/templates/sigra.install/core/emails.ex
    test/example/lib/example/accounts.ex
    test/example/lib/example_web/live/settings_live.ex
    test/example/lib/example_web/live/reactivation_live.ex
    test/example/lib/example/accounts/emails.ex
    lib/sigra/data_export.ex
    test/sigra/data_export_test.exs
    .planning/phases/127-versioned-auth-data-export/127-VERIFICATION.md
    .planning/phases/128-account-deletion-lifecycle-truth/128-01-SUMMARY.md
  </read_first>
  <action>
    Add `export_auth_data(user, opts \\ [])` beside the existing deletion lifecycle helpers in `priv/templates/sigra.install/core/auth.ex`. The function must call only:

    ```elixir
    def export_auth_data(user, opts \\ []) do
      Sigra.DataExport.export_auth_data(
        Repo,
        user,
        Keyword.merge(default_auth_export_opts(), opts)
      )
    end

    defp default_auth_export_opts do
      [
        session_schema: <%= context_module %>.UserSession,
        audit_schema: <%= context_module %>.AuditEvent,
        mfa_credential_schema: <%= context_module %>.UserMFACredential,
        backup_code_schema: <%= context_module %>.UserBackupCode
      ]
    end
    ```

    The generated core wrapper must not unconditionally pass `identity_schema`, `user_passkey_schema`, or `membership_schema`. `UserIdentity` is produced by `mix sigra.gen.oauth`, passkeys can be omitted with `--no-passkeys`, and organizations can be omitted with `--no-organizations`; hardcoding those schema options in core would suppress `Sigra.DataExport` omission notes and can break core-template isolation. Use `Keyword.merge(default_auth_export_opts(), opts)` so caller `opts` can provide `identity_schema`, `user_passkey_schema`, `membership_schema`, or override any default schema option.

    Mirror a wrapper in `test/example/lib/example/accounts.ex` using `Example.Repo`, wrapping the call in `with :ok <- forbid_sensitive_operation(opts, user, "account.data_export") do ... end` to preserve the example app sensitive-operation style. The example app wrapper may pass concrete optional schemas that exist in the example app, such as `Example.Accounts.UserPasskey` through `user_passkey_schema:` and `Example.Accounts.OrganizationMembership` through `membership_schema:`. Do not pass `identity_schema` unless an `Example.Accounts.UserIdentity` module exists. Make clear in code shape/tests that these optional schema defaults are example-specific and not required in the core generated wrapper.

    Example-specific wrapper shape:

    ```elixir
    def export_auth_data(user, opts \\ []) do
      with :ok <- forbid_sensitive_operation(opts, user, "account.data_export") do
        Sigra.DataExport.export_auth_data(
          Repo,
          user,
          Keyword.merge(default_auth_export_opts(), opts)
        )
      end
    end

    defp default_auth_export_opts do
      [
        session_schema: Example.Accounts.UserSession,
        audit_schema: Example.Accounts.AuditEvent,
        mfa_credential_schema: Example.Accounts.UserMFACredential,
        backup_code_schema: Example.Accounts.UserBackupCode,
        user_passkey_schema: Example.Accounts.UserPasskey,
        membership_schema: Example.Accounts.OrganizationMembership
      ]
    end
    ```

    Do not implement the old unconditional core wrapper shape that hardcodes OAuth identity, passkey, or organization membership schemas in the core generated default list.

    Preserve existing `schedule_deletion/2`, `cancel_deletion/2`, and `deletion_status/1` wrappers as thin calls into `Sigra.Auth.schedule_deletion/3`, `Sigra.Auth.cancel_deletion/3`, and `Sigra.Account.deletion_status/1` per D-01. Keep generated host ownership limited to repo, schema, scope, audit, token, and session context per D-02; do not move enqueue, active-scheduled, stale-worker, or finalization truth into generated/example code.

    Do not build or mention payload keys such as `schema_version`, `account`, `sessions`, `identities`, `audit`, `mfa`, `organizations`, `enterprise`, or `omissions` in either wrapper body. The library owns all payload semantics per D-04/D-05 and T-129-01.

    Soften generated and example lifecycle copy per D-03/D-12:
    - Replace `Permanently delete your account and all associated data.` with `Schedule account deletion according to your configured deletion strategy.`
    - Replace `This action cannot be undone after the grace period expires.` with `After the grace period expires, Sigra finalizes the account lifecycle according to that strategy.`
    - Replace `Are you sure? Your account will be deactivated immediately and permanently deleted. All sessions will be signed out.` with `Are you sure? Your account will be deactivated immediately, all sessions will be signed out, and finalization will follow your configured deletion strategy.`
    - Replace `Your account and data will be permanently removed on ...` with `Your account is scheduled for deletion on ... Finalization will follow the configured deletion strategy.`
    - Replace finalized email body text `Your %{app_name} account and associated data have been permanently removed...` with `Your %{app_name} account deletion has been finalized according to the configured deletion strategy. If you'd like to use %{app_name} again in the future, you're welcome to create a new account.`
  </action>
  <acceptance_criteria>
    - `rg -n "def export_auth_data\\(user, opts \\\\\\\\ \\[\\]\\)" priv/templates/sigra.install/core/auth.ex test/example/lib/example/accounts.ex` returns one match in each file.
    - `rg -n "Keyword\\.merge\\(default_auth_export_opts\\(\\), opts\\)" priv/templates/sigra.install/core/auth.ex test/example/lib/example/accounts.ex` returns one match in each file.
    - `rg -n "identity_schema:|user_passkey_schema:|membership_schema:" priv/templates/sigra.install/core/auth.ex` returns no matches unless each match is explicitly gated by existing installer EEx conditionals; the preferred implementation has no matches in the core generated wrapper.
    - `rg -n "user_passkey_schema: Example\\.Accounts\\.UserPasskey|membership_schema: Example\\.Accounts\\.OrganizationMembership" test/example/lib/example/accounts.ex` returns example-specific optional schema matches, and `rg -n "identity_schema:" test/example/lib/example/accounts.ex` returns no matches unless `Example.Accounts.UserIdentity` exists.
    - `rg -n "Sigra\\.DataExport\\.export_auth_data\\((Repo|Example\\.Repo),\\s*user" priv/templates/sigra.install/core/auth.ex test/example/lib/example/accounts.ex -U` returns wrapper delegation matches.
    - `rg -n "account\\.data_export" test/example/lib/example/accounts.ex` returns the example guard action.
    - `rg -n "all associated data|account and data will be permanently removed|account and associated data have been permanently removed" priv/templates/sigra.install/core test/example/lib/example test/example/lib/example_web` returns no matches.
    - `rg -n "Schedule account deletion according to your configured deletion strategy|Finalization will follow the configured deletion strategy|account deletion has been finalized according to the configured deletion strategy" priv/templates/sigra.install/core test/example/lib/example test/example/lib/example_web` returns matches in the changed template and example files.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/templates/settings_live_test.exs test/sigra/install/isolation_test.exs --max-failures 1</automated>
  </verify>
  <done>Generated templates and example app delegate auth export to the library, preserve optional-schema omission truth, and no longer make strategy-neutral permanent-removal claims.</done>
</task>

<task type="auto">
  <name>Task 3: Rebless and verify install golden parity</name>
  <files>test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/settings_live.ex, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/reactivation_live.ex, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts/emails.ex</files>
  <read_first>
    lib/mix/tasks/sigra.fixture.rebless_golden.ex
    test/sigra/install/golden_diff_test.exs
    priv/templates/sigra.install/core/auth.ex
    priv/templates/sigra.install/core/settings_live.ex
    priv/templates/sigra.install/core/reactivation_live.ex
    priv/templates/sigra.install/core/emails.ex
    test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex
    test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/settings_live.ex
    test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/reactivation_live.ex
    test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts/emails.ex
  </read_first>
  <action>
    Run `MIX_ENV=test mix sigra.fixture.rebless_golden` after Task 2 so the golden fixture is regenerated from current templates per D-07/D-08. Do not manually invent fixture-only behavior.

    Review the resulting fixture diff and keep only changes that mirror the current template behavior: the `export_auth_data/2` wrapper, strategy-neutral settings/reactivation copy, and finalized deletion email copy. The core-generated golden wrapper must mirror the core template defaults only: `session_schema`, `audit_schema`, `mfa_credential_schema`, and `backup_code_schema`, merged with caller `opts`. It must not unconditionally include `identity_schema`, `user_passkey_schema`, or `membership_schema`; callers or separately generated/gated code provide those optional schemas when present.
  </action>
  <acceptance_criteria>
    - `rg -n "def export_auth_data\\(user, opts \\\\\\\\ \\[\\]\\)|Sigra\\.DataExport\\.export_auth_data\\(" test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex` returns wrapper matches.
    - `rg -n "identity_schema:|user_passkey_schema:|membership_schema:" test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex` returns no unconditional generated-wrapper matches.
    - `rg -n "all associated data|account and data will be permanently removed|account and associated data have been permanently removed" test/fixtures/install_golden/tree` returns no matches.
    - `rg -n "Schedule account deletion according to your configured deletion strategy|Finalization will follow the configured deletion strategy|account deletion has been finalized according to the configured deletion strategy" test/fixtures/install_golden/tree` returns matches in golden LiveView/email files.
    - `git diff -- priv/templates/sigra.install/core test/fixtures/install_golden/tree` shows corresponding template and golden fixture changes rather than fixture-only behavior.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix sigra.fixture.rebless_golden && mix test test/sigra/install/golden_diff_test.exs test/sigra/templates/settings_live_test.exs test/sigra/install/isolation_test.exs --max-failures 1</automated>
  </verify>
  <done>Install golden output is regenerated evidence for the current generated-host behavior and passes golden diff verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated host -> Sigra.DataExport | Host app passes repo/user/schema modules into library-owned auth-data export. |
| generated UI/email -> user/operator | Strategy-neutral deletion copy communicates lifecycle consequences to users. |
| templates -> golden fixture | Installer templates become committed golden evidence for generated-host behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Information Disclosure | `auth.ex` and `accounts.ex` export wrappers | mitigate | Wrappers call `Sigra.DataExport.export_auth_data/3`; tests assert delegation and refute payload construction in generated host code. |
| T-129-04 | Information Disclosure / Repudiation | optional schema defaults in core generated wrapper | mitigate | Core wrapper defaults only always-core schemas and merges caller opts; tests refute unguarded `identity_schema`, `user_passkey_schema`, and `membership_schema` so missing optional schemas still produce DataExport omission notes. |
| T-129-02 | Repudiation / Information Disclosure | settings/reactivation/email copy | mitigate | Replace broad permanent-removal copy with configured-strategy wording; tests refute stale phrases. |
| T-129-03 | Tampering / Repudiation | install golden fixture | mitigate | Rebless from templates with `MIX_ENV=test mix sigra.fixture.rebless_golden` and verify `golden_diff_test.exs`. |
</threat_model>

<verification>
Run the plan-level focused checks:

```bash
mix test test/sigra/templates/settings_live_test.exs test/sigra/install/isolation_test.exs test/sigra/install/golden_diff_test.exs --max-failures 1
mix format --check-formatted test/sigra/templates/settings_live_test.exs test/sigra/install/isolation_test.exs priv/templates/sigra.install/core/auth.ex priv/templates/sigra.install/core/settings_live.ex priv/templates/sigra.install/core/reactivation_live.ex priv/templates/sigra.install/core/emails.ex test/example/lib/example/accounts.ex test/example/lib/example_web/live/settings_live.ex test/example/lib/example_web/live/reactivation_live.ex test/example/lib/example/accounts/emails.ex
```
</verification>

<success_criteria>
- `HOST-01` is satisfied for generated templates, example app, and install golden fixture.
- Export wrapper implementation is thin over `Sigra.DataExport.export_auth_data/3`.
- Core generated wrapper defaults include only always-core schemas and leave `identity_schema`, `user_passkey_schema`, and `membership_schema` to caller opts or separately generated/gated code.
- Optional-schema omission truth is preserved when OAuth, passkeys, or organizations are absent.
- Lifecycle copy is strategy-neutral and does not claim broad permanent removal outside the configured deletion strategy.
- Golden fixture is reblessed from templates and verified by `golden_diff_test.exs`.
</success_criteria>

<output>
After completion, create `.planning/phases/129-generated-host-parity-and-docs/129-01-SUMMARY.md`
</output>
