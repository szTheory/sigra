---
phase: 129-generated-host-parity-and-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/guides_dx02_test.exs
  - guides/flows/account-lifecycle.md
  - guides/flows/audit-logging.md
  - guides/recipes/testing.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Docs state that Sigra auth-data export covers Sigra-owned auth/account data, not all host application data."
    - "Docs state that host applications own host-domain export, retention policy, and legal interpretation."
    - "Docs explain explicit `omissions` for missing optional Sigra-owned schemas."
    - "Docs explain `:hard_delete`, `:soft_delete`, and `:anonymize` deletion strategy consequences without broad permanent-removal claims."
    - "Testing docs describe account-deletion assertions without implying soft-delete removes the user row."
  artifacts:
    - path: "guides/flows/account-lifecycle.md"
      provides: "Deletion strategy and host/Sigra data boundary truth"
      contains: ":soft_delete"
    - path: "guides/flows/audit-logging.md"
      provides: "Auth/account export boundary and omission behavior"
      contains: "Sigra.DataExport.export_auth_data/3"
    - path: "guides/recipes/testing.md"
      provides: "Testing-helper truth for deletion lifecycle and export omissions"
      contains: "assert_account_deleted"
    - path: "test/sigra/guides_dx02_test.exs"
      provides: "Automated guide truth assertions"
      contains: "Sigra-owned auth/account data"
  key_links:
    - from: "guides/flows/audit-logging.md"
      to: "lib/sigra/data_export.ex"
      via: "docs name Sigra.DataExport.export_auth_data/3 as bounded export API"
      pattern: "Sigra\\.DataExport\\.export_auth_data/3"
    - from: "guides/flows/account-lifecycle.md"
      to: "lib/sigra/account/deletion.ex"
      via: "docs describe strategy-specific finalization consequences"
      pattern: ":hard_delete.*:soft_delete.*:anonymize"
    - from: "test/sigra/guides_dx02_test.exs"
      to: "guides/flows/*.md and guides/recipes/testing.md"
      via: "file-content assertions"
      pattern: "Sigra-owned auth/account data"
---

<objective>
Update public docs and guide tests so Phase 129 documentation truth matches the bounded data-lifecycle contract.

Purpose: `DOC-01` requires lifecycle, audit export, and testing docs to distinguish Sigra-owned data from host-owned data, explain omission behavior, and state deletion strategy consequences.
Output: Guide assertions plus updated docs for account lifecycle, audit/auth export, and testing.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-generated-host-parity-and-docs/129-CONTEXT.md
@.planning/phases/129-generated-host-parity-and-docs/129-RESEARCH.md
@.planning/phases/129-generated-host-parity-and-docs/129-PATTERNS.md
@.planning/phases/129-generated-host-parity-and-docs/129-VALIDATION.md
@.planning/phases/127-versioned-auth-data-export/127-VERIFICATION.md
@.planning/phases/128-account-deletion-lifecycle-truth/128-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Documentation truth sources. -->

From Phase 127 verification:
```text
Sigra.DataExport.export_auth_data/3 returns schema_version, exported_at, account, sessions, identities, audit, MFA, organizations, enterprise, and omissions.
Missing optional schemas produce empty sections and explicit omission notes.
Enterprise connections are excluded because they are organization-scoped, not user-owned.
```

From Phase 128 summary:
```text
:hard_delete deletes the user row subject to host constraints.
:soft_delete preserves the user row and PII while clearing lifecycle staging fields.
:anonymize preserves the user row while clearing Sigra-owned PII.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 guide assertions for data boundary, omissions, and strategy truth</name>
  <files>test/sigra/guides_dx02_test.exs</files>
  <read_first>
    test/sigra/guides_dx02_test.exs
    guides/flows/account-lifecycle.md
    guides/flows/audit-logging.md
    guides/recipes/testing.md
    .planning/phases/129-generated-host-parity-and-docs/129-VALIDATION.md
    .planning/phases/127-versioned-auth-data-export/127-VERIFICATION.md
    .planning/phases/128-account-deletion-lifecycle-truth/128-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `guides/flows/audit-logging.md` contains `Sigra.DataExport.export_auth_data/3`, `Sigra-owned auth/account data`, `host-owned domain data`, and `omissions`.
    - Test 2: `guides/flows/account-lifecycle.md` contains strategy-specific text for `:hard_delete`, `:soft_delete`, and `:anonymize`, including `soft_delete preserves the user row and its PII`.
    - Test 3: `guides/recipes/testing.md` contains `:soft_delete preserves the user row`, `assert_account_deleted`, and `omissions`.
    - Test 4: the three guide files do not contain unscoped claims `exports all application data`, `all associated data is permanently removed`, or `guarantees compliance`.
  </behavior>
  <action>
    Extend `Sigra.GuidesDx02Test` with a new `describe "DATA-LIFECYCLE guide truth"` block. Add direct `File.read!/1` assertions for `@testing_guide`, and introduce local path variables inside tests for `guides/flows/account-lifecycle.md` and `guides/flows/audit-logging.md` if module attributes are not already present.

    Write tests before doc edits. Use exact expected strings: `Sigra.DataExport.export_auth_data/3`, `Sigra-owned auth/account data`, `host-owned domain data`, `omissions`, `:hard_delete`, `:soft_delete`, `:anonymize`, `soft_delete preserves the user row and its PII`, and `:soft_delete preserves the user row`.

    Add negative assertions that refute `exports all application data`, `all associated data is permanently removed`, and `guarantees compliance`. This implements D-09/D-10/D-11/D-12 and mitigates T-129-04/T-129-05 before prose edits.
  </action>
  <acceptance_criteria>
    - `rg -n "DATA-LIFECYCLE guide truth|Sigra\\.DataExport\\.export_auth_data/3|Sigra-owned auth/account data|host-owned domain data|omissions|soft_delete preserves the user row and its PII|guarantees compliance" test/sigra/guides_dx02_test.exs` shows the added assertions.
    - `mix test test/sigra/guides_dx02_test.exs --max-failures 1` fails before Task 2 doc edits and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/guides_dx02_test.exs --max-failures 1</automated>
  </verify>
  <done>Guide tests pin the required Phase 129 documentation truth and reject overbroad claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update lifecycle, audit export, and testing docs with bounded data-lifecycle truth</name>
  <files>guides/flows/account-lifecycle.md, guides/flows/audit-logging.md, guides/recipes/testing.md</files>
  <read_first>
    guides/flows/account-lifecycle.md
    guides/flows/audit-logging.md
    guides/recipes/testing.md
    test/sigra/guides_dx02_test.exs
    lib/sigra/data_export.ex
    test/sigra/data_export_test.exs
    lib/sigra/account/deletion.ex
    .planning/phases/127-versioned-auth-data-export/127-VERIFICATION.md
    .planning/phases/128-account-deletion-lifecycle-truth/128-01-SUMMARY.md
  </read_first>
  <action>
    Update `guides/flows/audit-logging.md` with a bounded auth/account export section that names `Sigra.DataExport.export_auth_data/3` and states these exact truths per D-06/D-09/D-10:
    - `Sigra-owned auth/account data`
    - `host-owned domain data`
    - `Host applications remain responsible for their own domain export, retention policy, and legal interpretation.`
    - `omissions` are explicit notes for missing optional Sigra-owned schemas, not silent completeness.
    - Sigra export includes Sigra-owned sections such as account lifecycle, sessions, identities, audit, MFA, passkeys, backup-code summary, and organization memberships when configured.

    Update `guides/flows/account-lifecycle.md` so the strategy section explicitly states these D-11 consequences:
    - `:hard_delete` removes the user row subject to host constraints and host-owned schema design.
    - `:soft_delete preserves the user row and its PII` while finalizing lifecycle state.
    - `:anonymize` preserves the row while clearing Sigra-owned PII.
    - Sigra lifecycle finalization does not claim to delete host-owned domain data.

    Update `guides/recipes/testing.md` so `assert_account_deleted(repo, user_schema, user_id)` no longer says only `the user row is gone (or anonymized)`. Replace it with strategy-aware wording that says `:hard_delete` expects the row to be gone, `:anonymize` expects Sigra-owned PII cleared, and `:soft_delete preserves the user row` while finalizing lifecycle state. Also mention testing auth export `omissions` when optional generated schemas are absent. Avoid broad copy per D-12.

    Do not add legal/compliance certification claims, generic BI export claims, SCIM claims, or hosted-control-plane claims.
  </action>
  <acceptance_criteria>
    - `rg -n "Sigra\\.DataExport\\.export_auth_data/3|Sigra-owned auth/account data|host-owned domain data|Host applications remain responsible for their own domain export, retention policy, and legal interpretation|omissions" guides/flows/audit-logging.md` returns matches.
    - `rg -n ":hard_delete|:soft_delete preserves the user row and its PII|:anonymize|host-owned domain data" guides/flows/account-lifecycle.md` returns matches.
    - `rg -n "assert_account_deleted|:soft_delete preserves the user row|:hard_delete|:anonymize|omissions" guides/recipes/testing.md` returns matches.
    - `rg -n "exports all application data|all associated data is permanently removed|guarantees compliance" guides/flows/account-lifecycle.md guides/flows/audit-logging.md guides/recipes/testing.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/guides_dx02_test.exs --max-failures 1</automated>
  </verify>
  <done>Lifecycle, audit export, and testing guides state the bounded Sigra-owned/host-owned data contract, omission behavior, and strategy consequences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator | Documentation shapes operator understanding of export completeness and deletion consequences. |
| library-owned data -> host-owned data | Sigra owns auth/account export; host app owns domain-data export and retention. |
| optional schemas -> export reader | Missing optional generated schemas must be communicated as explicit omissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04 | Information Disclosure / Repudiation | audit export and lifecycle docs | mitigate | Docs state Sigra export covers Sigra-owned auth/account data and host apps own domain export/retention/legal interpretation; guide tests assert these strings. |
| T-129-05 | Repudiation | audit export and testing docs | mitigate | Docs explain explicit `omissions` for missing optional Sigra-owned schemas; guide tests assert `omissions`. |
| T-129-02 | Repudiation | lifecycle and testing docs | mitigate | Docs describe `:hard_delete`, `:soft_delete`, and `:anonymize` consequences without broad permanent-removal claims. |
</threat_model>

<verification>
Run the plan-level focused checks:

```bash
mix test test/sigra/guides_dx02_test.exs --max-failures 1
mix docs
```
</verification>

<success_criteria>
- `DOC-01` is satisfied for account lifecycle, audit/auth export, and testing docs.
- Docs explicitly distinguish Sigra-owned auth/account data from host-owned domain data.
- Docs explain optional-schema `omissions` as explicit partial-export truth.
- Docs describe deletion strategy consequences for `:hard_delete`, `:soft_delete`, and `:anonymize`.
</success_criteria>

<output>
After completion, create `.planning/phases/129-generated-host-parity-and-docs/129-02-SUMMARY.md`
</output>
