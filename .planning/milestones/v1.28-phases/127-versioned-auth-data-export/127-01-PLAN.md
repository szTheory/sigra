---
phase: 127-versioned-auth-data-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/data_export_test.exs
autonomous: true
requirements:
  - EXP-01
  - EXP-02
must_haves:
  truths:
    - "Operator receives schema_version: 1 and exported_at on every auth data export"
    - "Operator receives account lifecycle raw fields plus lifecycle_status derived from Sigra.Account.Deletion.status/1"
    - "Configured optional schemas produce present sessions, identities, audit, MFA, passkey, backup-code summary, and organization membership sections"
    - "Missing optional schemas keep section keys present with empty values and explicit omission notes"
    - "Credential-related export sections exclude replay-relevant and secret-bearing fields"
  artifacts:
    - path: "test/sigra/data_export_test.exs"
      provides: "ExUnit coverage for versioned export shape, configured-schema safe serialization, omission truth, and sensitive-field exclusion"
      contains: "section: :sessions"
  key_links:
    - from: "test/sigra/data_export_test.exs"
      to: "Sigra.DataExport.export_auth_data/3"
      via: "DataExport.export_auth_data(repo, user, opts)"
      pattern: "DataExport\\.export_auth_data"
    - from: "test/sigra/data_export_test.exs"
      to: "Sigra.Account.Deletion.status/1"
      via: "assertion on lifecycle_status shape"
      pattern: "lifecycle_status"
---

<objective>
Create the executable proof surface for Phase 127 before changing production code.

Purpose: Lock the export contract required by EXP-01 and EXP-02 so implementation cannot accidentally preserve raw-struct export, silent partial exports, lifecycle drift, or host-domain overclaim.
Output: Focused ExUnit tests in `test/sigra/data_export_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/127-versioned-auth-data-export/127-CONTEXT.md
@.planning/phases/127-versioned-auth-data-export/127-RESEARCH.md
@.planning/phases/127-versioned-auth-data-export/127-PATTERNS.md
@.planning/phases/127-versioned-auth-data-export/127-VALIDATION.md
@lib/sigra/data_export.ex
@lib/sigra/account/deletion.ex
@test/sigra/data_export_test.exs

<interfaces>
From `lib/sigra/data_export.ex`:
```elixir
@spec export_auth_data(module(), struct(), keyword()) :: {:ok, map()}
def export_auth_data(repo, user, opts \\ [])
```

From `lib/sigra/account/deletion.ex`:
```elixir
@spec status(map()) :: {:scheduled, non_neg_integer()} | :not_scheduled | :deleted
def status(user)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add contract tests for lifecycle status and omission inventory</name>
  <files>test/sigra/data_export_test.exs</files>
  <read_first>
    - test/sigra/data_export_test.exs
    - lib/sigra/data_export.ex
    - lib/sigra/account/deletion.ex
    - .planning/phases/127-versioned-auth-data-export/127-CONTEXT.md
    - .planning/phases/127-versioned-auth-data-export/127-VALIDATION.md
  </read_first>
  <behavior>
    - `schema_version` remains exactly `1`.
    - `data.account.lifecycle_status` is `%{state: :scheduled, days_remaining: days}` when `Sigra.Account.Deletion.status(user)` returns `{:scheduled, days}`.
    - `data.account.lifecycle_status` is `%{state: :deleted}` when `Sigra.Account.Deletion.status(user)` returns `:deleted`.
    - `data.account.lifecycle_status` is `%{state: :not_scheduled}` when `Sigra.Account.Deletion.status(user)` returns `:not_scheduled`.
    - With no optional schemas, these values remain present and empty: `data.sessions == []`, `data.identities == []`, `data.audit == []`, `data.mfa.credentials == []`, `data.mfa.passkeys == []`, `data.mfa.backup_codes.count == 0`, and `data.organizations.memberships == []`.
    - With no optional schemas, `data.omissions` contains structured maps for every optional schema: `%{section: :sessions, schema_option: :session_schema}`, `%{section: :identities, schema_option: :identity_schema}`, `%{section: :audit, schema_option: :audit_schema}`, `%{section: :mfa_credentials, schema_option: :mfa_credential_schema}`, `%{section: :passkeys, schema_option: :user_passkey_schema}`, `%{section: :backup_codes, schema_option: :backup_code_schema}`, and `%{section: :memberships, schema_option: :membership_schema}`.
  </behavior>
  <action>Modify `test/sigra/data_export_test.exs` to replace the existing string-only omission assertion with exact structured omission assertions per D-07, D-08, and D-09. Add lifecycle assertions for scheduled, deleted, and not-scheduled users per D-05 and D-06. Use current `DateTime.utc_now()` plus `DateTime.add(..., 3, :day)` for scheduled status so the derived `days_remaining` assertion accepts an integer in `0..3` and asserts `state: :scheduled`. Keep the existing top-level section assertions for `:schema_version`, `:exported_at`, `:account`, `:sessions`, `:identities`, `:audit`, `:mfa`, `:organizations`, `:enterprise`, and `:omissions` per D-03 and D-04, and keep all assertions pointed at `Sigra.DataExport.export_auth_data/3` as the library-owned contract per D-01 and D-02.</action>
  <verify>
    <automated>mix test test/sigra/data_export_test.exs --max-failures 1</automated>
    <automated>rg -n "section: :sessions|schema_option: :session_schema|lifecycle_status|state: :scheduled|state: :not_scheduled|state: :deleted" test/sigra/data_export_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "lifecycle_status" test/sigra/data_export_test.exs` prints at least three matching lines.
    - `rg -n "section: :sessions" test/sigra/data_export_test.exs` prints a matching line.
    - `rg -n "schema_option: :backup_code_schema" test/sigra/data_export_test.exs` prints a matching line.
    - `mix test test/sigra/data_export_test.exs --max-failures 1` fails before Plan 02 with assertions for the new contract, not with syntax errors.
  </acceptance_criteria>
  <done>The test file pins version metadata, lifecycle status, stable empty sections, and structured omission notes for all seven optional schema options.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add configured-schema safe serialization tests</name>
  <files>test/sigra/data_export_test.exs</files>
  <read_first>
    - test/sigra/data_export_test.exs
    - lib/sigra/data_export.ex
    - .planning/phases/127-versioned-auth-data-export/127-CONTEXT.md
    - .planning/phases/127-versioned-auth-data-export/127-RESEARCH.md
    - .planning/phases/127-versioned-auth-data-export/127-PATTERNS.md
    - priv/templates/sigra.install/core/user_session.ex
    - priv/templates/sigra.gen.oauth/user_identity.ex
    - priv/templates/sigra.install/core/audit_event.ex
    - priv/templates/sigra.install/core/user_mfa_credential.ex
    - priv/templates/sigra.install/core/user_backup_code.ex
    - priv/templates/sigra.install/passkeys/user_passkey.ex
    - priv/templates/sigra.install/organizations/organization_membership.ex
  </read_first>
  <behavior>
    - Configured `:session_schema` rows serialize to maps that include `:id`, `:type`, `:ip`, `:user_agent`, `:last_active_at`, `:sudo_at`, `:active_organization_id`, `:inserted_at`, and `:updated_at`, and exclude `:hashed_token`.
    - Configured `:identity_schema` rows serialize to maps that include `:id`, `:provider`, `:provider_uid`, `:email`, `:name`, `:nickname`, `:avatar_url`, `:email_verified`, `:inserted_at`, and `:updated_at`, and exclude `:encrypted_access_token` and `:encrypted_refresh_token`.
    - Configured `:audit_schema` rows serialize to maps that include `:id`, `:action`, `:outcome`, `:actor_id`, `:effective_user_id`, `:target_id`, `:target_type`, `:organization_id`, `:ip`, `:user_agent`, `:metadata`, `:occurred_at`, and `:inserted_at`.
    - Configured `:mfa_credential_schema` rows serialize to maps that include `:id`, `:type`, `:enabled`, `:last_used_at`, `:locked_until`, `:failed_attempts`, `:inserted_at`, and `:updated_at`, and exclude `:encrypted_secret`.
    - Configured `:user_passkey_schema` rows serialize to maps that include `:id`, `:nickname`, `:device_type`, `:last_used_at`, `:sign_count`, `:rp_id`, `:aaguid`, `:inserted_at`, and `:updated_at`, and exclude `:credential_id` and `:public_key`.
    - Configured `:backup_code_schema` contributes only `data.mfa.backup_codes.count`; backup-code rows and `:hashed_code` never appear in the export.
    - Configured `:membership_schema` rows serialize to maps that include `:id`, `:organization_id`, `:user_id`, `:role`, `:inserted_at`, and `:updated_at`.
  </behavior>
  <action>Add self-contained in-test Ecto schema modules and a fake repo module inside `Sigra.DataExportTest` or nested under it. The fake repo must implement `all/1` and `aggregate/3`, return rows for schemas passed in opts, and be deterministic without a database. Add one configured-schema test that calls `DataExport.export_auth_data(FakeRepo, user, session_schema: TestSession, identity_schema: TestIdentity, audit_schema: TestAuditEvent, mfa_credential_schema: TestMfaCredential, backup_code_schema: TestBackupCode, user_passkey_schema: TestPasskey, membership_schema: TestMembership)`. Assert the exact safe fields listed in this task and refute every forbidden key: `:hashed_token`, `:encrypted_access_token`, `:encrypted_refresh_token`, `:encrypted_secret`, `:credential_id`, `:public_key`, and `:hashed_code` per D-10, D-11, and D-12.</action>
  <verify>
    <automated>mix test test/sigra/data_export_test.exs --max-failures 1</automated>
    <automated>rg -n "hashed_token|encrypted_access_token|encrypted_refresh_token|encrypted_secret|credential_id|public_key|hashed_code|TestSession|TestPasskey|aggregate" test/sigra/data_export_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "refute Map.has_key\\?.*:hashed_token" test/sigra/data_export_test.exs` prints a matching line.
    - `rg -n "refute Map.has_key\\?.*:encrypted_access_token" test/sigra/data_export_test.exs` prints a matching line.
    - `rg -n "refute Map.has_key\\?.*:hashed_code" test/sigra/data_export_test.exs` prints a matching line.
    - `rg -n "def aggregate\\(" test/sigra/data_export_test.exs` prints a matching line.
    - `mix test test/sigra/data_export_test.exs --max-failures 1` fails before Plan 02 with assertion failures for missing serializers, not with syntax errors or undefined test modules.
  </acceptance_criteria>
  <done>The test file proves configured optional schemas use curated safe export maps and backup codes remain summary-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated schemas -> library export | Host-generated Ecto structs may contain secret-bearing fields; the library must curate output. |
| missing schema config -> operator payload | Absent optional schemas cross into an operator-facing export where silence would misrepresent completeness. |
| account lifecycle storage -> export contract | Raw lifecycle fields must be interpreted through Sigra deletion semantics, not local guesses. |
| Sigra-owned data -> host domain expectations | Export payload must not imply BI, SCIM, legal certification, hosted control-plane, or host-domain data coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | `test/sigra/data_export_test.exs` safe serialization coverage | mitigate | Covers secret disclosure through raw auth structs: add regression assertions that exported session, identity, MFA, passkey, and backup-code sections do not contain `:hashed_token`, `:encrypted_access_token`, `:encrypted_refresh_token`, `:encrypted_secret`, `:credential_id`, `:public_key`, or `:hashed_code`. |
| T-127-02 | Repudiation | `test/sigra/data_export_test.exs` omission coverage | mitigate | Covers misleading partial exports/omission gaps: assert exact structured omission maps for `:session_schema`, `:identity_schema`, `:audit_schema`, `:mfa_credential_schema`, `:user_passkey_schema`, `:backup_code_schema`, and `:membership_schema`. |
| T-127-03 | Repudiation | `test/sigra/data_export_test.exs` lifecycle coverage | mitigate | Covers lifecycle misrepresentation: assert `data.account.lifecycle_status` exposes `:scheduled`, `:deleted`, and `:not_scheduled` states derived by production code from `Sigra.Account.Deletion.status/1`. |
| T-127-04 | Repudiation | `test/sigra/data_export_test.exs` enterprise/export boundary coverage | mitigate | Covers host-domain overclaim: keep assertions that enterprise connections are `[]`, `exported: false`, and include the organization-scoped non-export reason; do not add host-owned domain data assertions. |
</threat_model>

<verification>
Run `mix test test/sigra/data_export_test.exs --max-failures 1`. In Plan 01, new tests are expected to fail against current production code but must compile and fail on contract assertions only.
</verification>

<success_criteria>
- `test/sigra/data_export_test.exs` contains lifecycle status tests, omission inventory tests, configured-schema serialization tests, and sensitive-field exclusion tests.
- The focused ExUnit command compiles the test file.
- No production code is modified in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/127-versioned-auth-data-export/127-01-SUMMARY.md`.
</output>
