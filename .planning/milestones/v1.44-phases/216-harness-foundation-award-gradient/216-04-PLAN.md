---
phase: 216-harness-foundation-award-gradient
plan: 04
type: execute
wave: 2
depends_on: ["216-01", "216-02"]
files_modified:
  - scripts/ci/quality-findings-monotonic.sh
  - scripts/ci/quality-findings-monotonic.test.sh
  - scripts/ci/settled-findings-lint.sh
  - scripts/ci/settled-findings-lint.test.sh
  - scripts/ci/evidence-anchor-check.mjs
  - scripts/ci/evidence-anchor-check.test.mjs
autonomous: true
requirements: [HARNESS-02, RATCHET-02]

must_haves:
  truths:
    - "The findings-count guard FAILS when any cell's open-finding count increases vs merge-base, PASSES on no-change and on a decrease (down-ratchet), and is proven so by a hermetic .test.sh (D-21)."
    - "settled-findings.tsv is enforced sorted + deduped by finding_id; unsorted or duplicate rows FAIL the lint (D-22)."
    - "The evidence-anchor check rejects any finding whose structural DOM anchor is absent from the captured DOM — making cite-and-flip impossible by construction (D-09)."
  artifacts:
    - "scripts/ci/quality-findings-monotonic.sh + .test.sh"
    - "scripts/ci/settled-findings-lint.sh + .test.sh (+ --add regen helper)"
    - "scripts/ci/evidence-anchor-check.mjs + .test.mjs"
  key_links:
    - "admin-render-sha.json open_findings (Plan 02) → quality-findings-monotonic.sh diffs it vs merge-base."
    - "settled-findings.tsv (Plan 02) → settled-findings-lint.sh enforces order/dedup."
    - "cheerio (Plan 01) + captured dom.html (Plan 03 bundle) → evidence-anchor-check.mjs."
---

<objective>
Build the three forward-only / integrity guards that consume the Plan-02 ledgers and the Plan-03 bundles: (1) `quality-findings-monotonic.sh` — a structural clone of `quality-ledger-monotonic.sh` with the comparator INVERTED so open-finding counts ratchet DOWN (D-21); (2) `settled-findings-lint.sh` — enforces the sorted/deduped suppression set + ships a regen helper so humans never hand-edit ordering (D-22); (3) `evidence-anchor-check.mjs` — a cheerio-over-outerHTML check that rejects any finding whose anchor is absent, defeating cite-and-flip (D-09). Each guard ships with a hermetic self-test.

Purpose: These are the deterministic merge-blocking derivatives (RATCHET-02, HARNESS-02). Cloning the proven monotonic-guard idiom (git-base diff + associative-array + hermetic self-test) keeps them robust; inverting exactly one comparator keeps each guard to one ratchet direction.

Output: three guards + three self-tests under `scripts/ci/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md
@.planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md
@.planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md

# Clone targets (read verbatim before cloning):
@scripts/ci/quality-ledger-monotonic.sh
@scripts/ci/quality-ledger-monotonic.test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: quality-findings-monotonic.sh + hermetic .test.sh (clone + invert)</name>
  <files>scripts/ci/quality-findings-monotonic.sh, scripts/ci/quality-findings-monotonic.test.sh</files>
  <read_first>
    - scripts/ci/quality-ledger-monotonic.sh (header, arg-parse, fail(), extract_tiers awk, git-show-BASE associative-array diff, skip-on-empty-base, comparator at the `-lt` line)
    - scripts/ci/quality-ledger-monotonic.test.sh (the ENTIRE hermetic scaffold: mktemp throwaway repo, pass/fail counters, REAL_GUARD locate, git init, fixture commit, per-test run capture, Test-D decorated-cell lesson, summary block)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("### Pattern 7" + Test Map RATCHET-02 row: 3→4 FAIL, no-change PASS, 4→3 PASS)
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("CRITICAL skip-on-empty-base divergence (D-08/D-21)")
  </read_first>
  <action>
    Clone `quality-ledger-monotonic.sh` into `quality-findings-monotonic.sh`, renaming the FAIL/PASS prefixes to `quality-findings-monotonic`. Point `LEDGER` at `guides/reference/admin-render-sha.json` (the authoritative open-finding-count source, per Plan 02). Replace `extract_tiers` (awk over markdown) with an `extract_open_counts` that reads the JSON and emits `<surface>/<cell>\t<open_findings>` per cell — use `node -e` or `jq` for the JSON read (planner discretion; `node` avoids a jq dep). INVERT the comparator: FAIL when `head > base` (open findings INCREASED) — contrast the tier guard's `-lt` decrease check. Handle the skip-on-empty-base divergence per PATTERNS: the empty-base skip may fire ONLY when the ledger FILE is absent at base (initial commit); when the file exists with zero rows, an increase from 0 IS a regression — compare, do not skip. Read BASE via `git -C "$ROOT" show "${BASE}:${LEDGER}"` (BASE is now merge-base after Plan 01's D-10 fix). Author `quality-findings-monotonic.test.sh` by lifting the full hermetic scaffold: cases 3→4 FAIL, no-change PASS, 4→3 PASS (down-ratchet), plus a documented case proving the JSON positional/key parse can't be silently defeated (the Test-D lesson analog for this source format).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-findings-monotonic.test.sh</automated>
  </verify>
  <done>The self-test passes: open-count 3→4 FAILs, no-change PASSes, 4→3 PASSes, the parse-defeat case is documented; the guard reads merge-base via --base and admin-render-sha.json as its single source.</done>
</task>

<task type="auto">
  <name>Task 2: settled-findings-lint.sh + regen helper + hermetic .test.sh</name>
  <files>scripts/ci/settled-findings-lint.sh, scripts/ci/settled-findings-lint.test.sh</files>
  <read_first>
    - scripts/ci/quality-ledger-monotonic.test.sh (hermetic .test.sh scaffold to reuse)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("### `guides/reference/settled-findings.tsv`" — sorted/deduped/one-per-line rule; the --add regen helper; stale-anchor non-blocking prune note)
    - guides/reference/settled-findings.tsv (the committed 7-column header + zero data rows from Plan 02)
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-22)
  </read_first>
  <action>
    Author `settled-findings-lint.sh` using the shared bash-guard skeleton (`set -euo pipefail`, ROOT, fail()). Target `guides/reference/settled-findings.tsv`. Strip the `#` header/comment lines, then FAIL if (a) the data rows are not byte-identical to their `sort`ed form (unsorted → FAIL) or (b) any `finding_id` (column 1) appears more than once (duplicate → FAIL). An empty data set PASSES trivially. Add a `--add <finding_id> <surface> <class> <anchor> --disposition <waived|resolved> [--waived-by X] [--note ...]` mode that appends a row and re-sorts the file in place, so humans never hand-edit ordering (D-22). Author `settled-findings-lint.test.sh` (hermetic scaffold): sorted-empty PASS, a hand-crafted unsorted 2-row fixture FAIL, a duplicate-finding_id fixture FAIL, and an `--add`-then-lint round-trip PASS.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/settled-findings-lint.test.sh && bash scripts/ci/settled-findings-lint.sh</automated>
  </verify>
  <done>Self-test passes (sorted PASS, unsorted FAIL, dup FAIL, --add round-trip PASS); running the lint against the committed empty settled-findings.tsv PASSes.</done>
</task>

<task type="auto">
  <name>Task 3: evidence-anchor-check.mjs + node self-test (cheerio, defeats cite-and-flip)</name>
  <files>scripts/ci/evidence-anchor-check.mjs, scripts/ci/evidence-anchor-check.test.mjs</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("### Pattern 6: Evidence-anchor check" — cheerio load in HTML mode, `$(finding.anchor).length === 0` → exit 1; anchors are structural selectors never prose; geometry facts can't be recomputed here)
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-09: node + cheerio over captured outerHTML; anchor identity MUST be a structural selector / data-* hook; load in HTML mode not xmlMode)
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("`scripts/ci/evidence-anchor-check.mjs`" No-Analog row — design net-new)
  </read_first>
  <action>
    Author `evidence-anchor-check.mjs` (ESM node, no bash): `import { load } from 'cheerio'`. For each captured bundle under `test/example/priv/playwright/eval/<app_git_sha>/**/`, read `dom.html` + `findings.json`; `load(html)` in HTML mode (do NOT pass `{ xmlMode: true }`); for every finding assert `$(finding.anchor).length > 0`, else print `evidence-anchor-check: FAIL: anchor absent for <finding_id>: <anchor>` and set `process.exitCode = 1`. Skip findings whose class is a geometry-only class (misalignment/below-fold/focus-ring) with a note that those are browser-computed facts, not DOM-anchor claims (they can't be recomputed layout-free here, D-09) — but still require they carry a structural anchor field. Treat the finding JSON as untrusted input: JSON-parse it, run the anchor through cheerio `$()` ONLY (never shell/`eval` interpolation). Author `evidence-anchor-check.test.mjs`: a present-anchor fixture PASSes (exit 0), an absent-anchor fixture exits 1, and a finding whose `anchor` is prose-text rather than a selector is rejected as malformed.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node scripts/ci/evidence-anchor-check.test.mjs</automated>
  </verify>
  <done>Self-test passes: present anchor → exit 0, absent anchor → exit 1, prose-not-selector anchor rejected; anchors go through cheerio $() only (no shell interpolation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge-base ledger → findings guard | The "before" open-finding counts the down-ratchet trusts |
| captured DOM + findings → anchor check | Untrusted bundle JSON/HTML parsed to prove a finding's anchor exists |
| finding.anchor string → cheerio selector | User/harness-supplied selector executed against the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-04-CITE | Tampering | cite-and-flip (evaluate code not render) | high | mitigate | evidence-anchor-check asserts every finding's structural anchor exists in the CAPTURED DOM; a claim that cites code/prose with no rendered anchor fails by construction (D-09). Self-test proves absent-anchor → exit 1. |
| T-216-04-REGRESS | Tampering | silent findings-count regression | high | mitigate | quality-findings-monotonic.sh FAILs on any per-cell open-count increase vs merge-base; skip-on-empty-base narrowed to file-absent-only so an increase-from-0 is caught (D-08/D-21); hermetic self-test proves 3→4 FAIL. |
| T-216-04-WAIVE | Repudiation | settled-findings abuse (mass waiving) | medium | mitigate | settled-findings-lint enforces sorted/deduped; every row carries disposition + waived_by + note (attributable); --add regen prevents silent reordering that could hide a row; a waived finding still needs a real disposition. |
| T-216-04-INJECT | Injection (Tampering) | anchor/finding text into a guard | high | mitigate | Anchors run through cheerio `$()` only; findings JSON-parsed, never `bash`/`eval`-interpolated (V5). |
</threat_model>

<verification>
- `bash scripts/ci/quality-findings-monotonic.test.sh` green (3→4 FAIL, no-change PASS, 4→3 PASS).
- `bash scripts/ci/settled-findings-lint.test.sh` green; lint of committed empty TSV PASSes.
- `node scripts/ci/evidence-anchor-check.test.mjs` green (present PASS, absent exit 1, prose rejected).
</verification>

<success_criteria>
- Three guards + three hermetic self-tests exist and pass.
- Each guard reads ONE authoritative source and (where applicable) merge-base via --base.
- Cite-and-flip and silent count-regression are both provably caught.
</success_criteria>

<output>
Create `.planning/phases/216-harness-foundation-award-gradient/216-04-SUMMARY.md` when done.
</output>
