---
phase: 216-harness-foundation-award-gradient
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-award-ledger.json
  - guides/reference/settled-findings.tsv
  - guides/reference/admin-render-sha.json
  - guides/reference/admin-eval-schema.md
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements: [RATCHET-01, RATCHET-02]
user_setup: []

must_haves:
  truths:
    - "The award sub-score is an ordinal A0..A3 band above Tier-2, stored as a fixed 4-axis vector (token_fidelity/rhythm/a11y_polish/states) with band derived as min(axes) (D-17/D-18)."
    - "Award data lives ONLY in a new sibling JSON, never a 5th markdown column; the frozen tier column-4 grammar is untouched (D-19)."
    - "The settled-findings suppression key finding_id = sha256(surface \\0 class \\0 anchor) is written down as the deterministic-substrate contract that Phase 217's fix-queue key must match (D-22)."
    - "The open-finding-count source and the render_sha256 ledger are committed text/JSON with one authoritative source per guard (D-05/D-21)."
  artifacts:
    - "guides/reference/admin-award-ledger.json (schema_version + cells.<surface>.{axes,band,verified_at_sha,rendered,evidence_ref})"
    - "guides/reference/settled-findings.tsv (sorted, header-commented, D-22 columns)"
    - "guides/reference/admin-render-sha.json (render_sha256 + open-finding counts per surface×cell)"
    - "guides/reference/admin-eval-schema.md (human-readable schema doc + the finding_id key contract + cross-phase-217 seam)"
  key_links:
    - "admin-award-ledger.json → award-guard.mjs (Plan 05) reads it; band==min(axes) enforced there."
    - "settled-findings.tsv finding_id → settled-findings-lint.sh (Plan 04) AND Phase 217 AUTOFIX-01 fix-queue (cross-phase contract seam)."
    - "admin-render-sha.json open-finding counts → quality-findings-monotonic.sh (Plan 04)."
---

<objective>
Define and commit the small forward-only derived-signal ledgers this phase introduces, plus the schema documentation and the cross-phase `finding_id` key contract — all as data/text, with no executable code (guards that consume them land in Plans 04/05). This isolates the fragile, contract-bearing schema decisions (D-17..D-22) into one plan so the guards in Wave 2 can be written against a fixed, committed shape.

Purpose: The guards, the award ledger, and Phase 217's fix queue all key off shapes decided here. Locking the JSON/TSV grammar and the `finding_id` composition first — with the frozen markdown column-4 grammar explicitly preserved — prevents the awk-parse fragility that already forced a "bare-integer prohibition" contract in the persona rubric (D-19).

Output: `admin-award-ledger.json`, `settled-findings.tsv`, `admin-render-sha.json`, a schema doc, and a one-sentence cross-reference appended to the existing markdown ledger (column-4 grammar untouched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md
@.planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md
@.planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md

# Schema grounding (read the frozen grammar + cell inventory before writing):
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-persona-jtbd-rubric.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author admin-award-ledger.json + admin-render-sha.json with the two pilot surfaces</name>
  <files>guides/reference/admin-award-ledger.json, guides/reference/admin-render-sha.json</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("## Ledger / Award JSON Schema" — the full admin-award-ledger.json shape + A0..A3 band semantics + the 5 award-guard FAIL conditions)
    - guides/reference/admin-quality-ledger.md (the cell inventory; use the SAME cell keys, e.g. `users-index-live`, `user-show-live`)
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-17 A0..A3 ordinal + Tier-2-as-A0-entry, D-18 4-axis min() rollup, D-25 pilots cap at A2)
  </read_first>
  <action>
    Create `guides/reference/admin-award-ledger.json` per RESEARCH's schema: top-level `schema_version: 1` + `cells` object. Seed ONLY the two pilot surfaces (`users-index-live`, `user-show-live` — D-23) as initial entries at the floor band `A0` on all four axes (token_fidelity/rhythm/a11y_polish/states), `band: "A0"` (== min(axes)), `verified_at_sha: null` (not yet render-verified — Plan 07 sets this), `rendered: false`, `evidence_ref: []`. The band is the DERIVED min of the axis vector; author it consistent with that rule (all-A0 → A0). Do NOT set any axis above A0 here — climbing happens only against a fresh render in Plan 07. Add a top-level `"_doc"` or comment-free `"notes"` field pointing to admin-eval-schema.md.

    Create `guides/reference/admin-render-sha.json` as the render_sha256 + open-finding-count store: a `schema_version: 1` object with a `cells` map keyed `<surface>` → `<theme>-<viewport>-<state>` → `{ render_sha256: null, open_findings: 0 }` for the two pilots' chromium/DPR1 cells (leave populated by the harness in Plan 07; commit the empty-but-well-formed skeleton now so guards have a stable file to diff). Keep it strict JSON (guards read it via node/jq). Choose the consolidation per CONTEXT Discretion: keep render-sha + open-finding counts together in this one file so `quality-findings-monotonic.sh` has ONE authoritative source; document that choice in admin-eval-schema.md.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node -e "const a=require('./guides/reference/admin-award-ledger.json'); const r=require('./guides/reference/admin-render-sha.json'); const ord={A0:0,A1:1,A2:2,A3:3}; for(const [k,c] of Object.entries(a.cells)){const m=Math.min(...Object.values(c.axes).map(v=>ord[v])); const b=ord[c.band]; if(m!==b) throw new Error('band!=min(axes) for '+k); if(c.rendered!==false && c.rendered!==true) throw new Error('rendered must be bool for '+k);} if(!('users-index-live' in a.cells)||!('user-show-live' in a.cells)) throw new Error('missing pilot cell'); console.log('SCHEMA_OK');"</automated>
  </verify>
  <done>Both JSON files parse; award ledger has exactly the two pilot cells at A0 with band==min(axes) and rendered:false; render-sha file is well-formed with the pilots' chromium cells present.</done>
</task>

<task type="auto">
  <name>Task 2: Author settled-findings.tsv + the finding_id key contract doc (cross-phase-217 seam)</name>
  <files>guides/reference/settled-findings.tsv, guides/reference/admin-eval-schema.md</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("### `guides/reference/settled-findings.tsv`" — columns, key formula, sorted/one-per-line rule, Open-count definition; "## Open Questions #1" — the 216 class vs 217 lens+question reconciliation)
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-22 full text; the cross-phase MUST-match requirement)
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("Cross-phase contract flag (A3 / Open Q1)")
  </read_first>
  <action>
    Create `guides/reference/settled-findings.tsv` with a leading `#`-comment header line naming the seven tab-separated columns in order: `finding_id  surface  class  anchor  disposition  waived_by  note` (D-22). Seed it with ZERO data rows (no findings are settled yet); the file must exist so `settled-findings-lint.sh` (Plan 04) and the harness have a stable target. The file is sorted-by-`finding_id` and deduped by construction (trivially true when empty). Do NOT hand-author example data rows that reference nonexistent anchors — an empty settled set is correct at phase start.

    Create `guides/reference/admin-eval-schema.md` as the single human-readable contract doc: (1) document the `finding_id = sha256(surface + "\0" + class + "\0" + anchor)` key EXACTLY (byte-level: NUL separators, lowercase-hex digest); (2) explicitly state this is the deterministic-substrate key that **Phase 217 AUTOFIX-01's fix-queue key MUST match**, and surface RESEARCH Open Question #1 verbatim as an UNRESOLVED cross-phase seam — the 216 substrate uses `class` (probes have a class), while 217's requirement text says `lens+question`; recommend 217 map its `lens+question` into the same `class` slot, but DO NOT silently resolve it here (flag it for joint 216↔217 planning); (3) document each ledger file's authoritative role (award-ledger.json = award vector, render-sha.json = render_sha256 + open counts, settled-findings.tsv = suppression set); (4) restate the A0..A3 band semantics + the `band = min(axes)` and `Open = total − settled` rules; (5) reaffirm the anchor-identity rule: structural selector / `data-*` hook, NEVER prose/line-number (survives copy edits).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && head -1 guides/reference/settled-findings.tsv | grep -q 'finding_id' && awk -F'\t' 'NR==1{next} NF>0 && !/^#/{print "DATA_ROW"}' guides/reference/settled-findings.tsv | grep -qv DATA_ROW; grep -q 'sha256' guides/reference/admin-eval-schema.md && grep -qi 'Phase 217' guides/reference/admin-eval-schema.md && echo SETTLED_SCHEMA_OK</automated>
  </verify>
  <done>settled-findings.tsv has the 7-column header comment and zero data rows; admin-eval-schema.md documents the exact finding_id formula, flags the 217 cross-phase key seam as unresolved, and restates the band/Open/anchor rules.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-reference the JSON award ledger from the markdown ledger without touching column-4 grammar</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (lines 14-40: the frozen column-4 `[012]` awk-parse contract + "Decorators in column-4 are forbidden"; find the two pilot rows `users-index-live` / `user-show-live` evidence prose)
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-19: append at most one sentence to a cell's evidence prose cross-referencing the JSON; keep column-4 FROZEN)
  </read_first>
  <action>
    Append at most ONE sentence to the evidence-prose column of the two pilot cells (users-index-live, user-show-live) cross-referencing `guides/reference/admin-award-ledger.json` for the award sub-score (per D-19). Also add a short new prose subsection (NOT a table column) explaining that award data lives in the sibling JSON and the tier column-4 grammar remains frozen at bare `[012]`. Under NO circumstances add a 5th `|`-delimited column, a decorator, a footnote marker, or an asterisk to any tier cell — the `awk -F'|'` positional parse must keep working. Do NOT change any existing tier integer value.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-ledger-monotonic.sh --base HEAD && grep -q 'admin-award-ledger.json' guides/reference/admin-quality-ledger.md && echo LEDGER_XREF_OK</automated>
  </verify>
  <done>The markdown ledger references the award JSON in prose; the monotonic guard still parses every cell (unchanged column count, no decorators); no tier value changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed ledgers → CI guards | The award JSON / render-sha JSON / settled TSV are the trusted "before/after" the deterministic guards diff |
| 216 substrate → 217 fix queue | The finding_id key is a cross-phase contract; a mismatch silently breaks suppression + dedup in 217 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-02-DECOR | Tampering | markdown tier column-4 | high | mitigate | D-19: award data goes in sibling JSON only; column-4 grammar frozen; Task-3 verify re-runs quality-ledger-monotonic.sh to prove the awk parse still reads every cell after the prose edit. |
| T-216-02-KEY | Tampering | finding_id cross-phase key | high | mitigate | The key formula is written EXACTLY (NUL-separated sha256) in admin-eval-schema.md and flagged as a MUST-match contract with 217; Open Q #1 surfaced unresolved rather than silently decided, forcing joint reconciliation. |
| T-216-02-SETTLE | Repudiation | settled-findings.tsv abuse | medium | mitigate | Empty-at-start suppression set (no pre-waived findings); columns include `disposition` + `waived_by` + `note` so any future waiver is attributable; lint (Plan 04) enforces sorted/deduped. |
| T-216-02-BAND | Tampering | award band vs axes | high | mitigate | band == min(axes) authored and verified in Task 1; the derived-not-typed rule is documented; award-guard (Plan 05) enforces it in CI. |
</threat_model>

<verification>
- `node -e` schema check: award ledger band==min(axes), rendered is bool, both pilots present; render-sha well-formed.
- settled-findings.tsv has the 7-column header + zero data rows.
- admin-eval-schema.md documents the exact finding_id formula and flags the 217 seam.
- `bash scripts/ci/quality-ledger-monotonic.sh --base HEAD` still passes (column-4 grammar intact).
</verification>

<success_criteria>
- Four committed schema artifacts exist and parse.
- finding_id key contract documented + 217 seam flagged unresolved.
- Frozen markdown column-4 grammar provably intact.
</success_criteria>

<output>
Create `.planning/phases/216-harness-foundation-award-gradient/216-02-SUMMARY.md` when done.
</output>
