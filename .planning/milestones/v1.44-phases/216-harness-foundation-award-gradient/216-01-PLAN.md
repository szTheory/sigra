---
phase: 216-harness-foundation-award-gradient
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .gitignore
  - test/example/priv/playwright/package.json
  - test/example/priv/playwright/package-lock.json
autonomous: false
requirements: [HARNESS-01, HARNESS-02, RATCHET-02]
user_setup: []

must_haves:
  truths:
    - "The shared ci.yml `id: base` step emits a merge-base SHA (not the base-branch tip), so every down-ratchet guard diffs against the true fork point (D-10)."
    - "Ephemeral evaluation bundles under test/example/priv/playwright/ are gitignored and can never be accidentally committed (D-04, Pitfall 4)."
    - "parse5 and cheerio are installed as verified, legitimate devDependencies of the playwright subproject (D-06, D-09)."
  artifacts:
    - ".github/workflows/ci.yml (id: base step emits merge-base)"
    - ".gitignore (eval/ + playwright-report/ + test-results/ under the playwright subproject)"
    - "test/example/priv/playwright/package.json + package-lock.json (parse5, cheerio pinned)"
  key_links:
    - "id: base output → all --base consumers (snapshot-canary x2, quality-ledger-monotonic, and the new 216 guards) read the SAME merge-base."
    - "parse5 → canonicalize.ts (Plan 03); cheerio → evidence-anchor-check.mjs (Plan 04)."
---

<objective>
Lay the foundation the rest of Phase 216 builds on: (1) fix the shared `ci.yml` `id: base` step to emit the merge-base instead of the base-branch tip (D-10), which simultaneously corrects the new down-ratchet findings/award guards AND the two existing guards (tier monotonic, snapshot-canary); (2) add `.gitignore` entries so ephemeral evaluation bundles never get committed (D-04); (3) install and legitimacy-verify the two new parsers (parse5, cheerio) that the canonicalization and anchor-check code depend on.

Purpose: Every down-ratchet guard in this phase compares against merge-base; getting the base-ref semantics right once, in the shared step, is a prerequisite for correct guard behavior. The gitignore and deps are cheap prerequisites the render/probe engine and guards need.

Output: A corrected `ci.yml` base step, three new gitignore lines, and two verified new devDependencies with an updated lockfile.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md
@.planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md
@.planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md

# Foundation edit targets (read the exact current lines before editing):
@.github/workflows/ci.yml
@.gitignore
@test/example/priv/playwright/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify parse5 + cheerio package legitimacy (blocking, non-auto-approvable)</name>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("## Package Legitimacy Audit" table — parse5, cheerio both pre-verdicted OK/Approved but tagged [ASSUMED] pending install-time confirmation)
  </read_first>
  <what-built>
    RESEARCH.md's Package Legitimacy Audit pre-classified parse5 and cheerio as OK/Approved (ubiquitous parsers behind jsdom/cheerio), but both remain [ASSUMED] until confirmed against the live npm registry at install time. This checkpoint confirms the exact packages before the lockfile is committed.
  </what-built>
  <how-to-verify>
    Claude will run these read-only registry lookups and present the output:
    1. `cd test/example/priv/playwright && npm view parse5 version repository.url`
    2. `cd test/example/priv/playwright && npm view cheerio version repository.url`
    Confirm each resolves to the expected repo (github.com/inikulin/parse5, github.com/cheeriojs/cheerio) and a current version (parse5 7.x, cheerio 1.x). Cross-check against npmjs.com/package/parse5 and npmjs.com/package/cheerio if any doubt.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name the concern (wrong repo / suspicious version / typosquat).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install and pin parse5 + cheerio as playwright-subproject devDependencies</name>
  <files>test/example/priv/playwright/package.json, test/example/priv/playwright/package-lock.json</files>
  <read_first>
    - test/example/priv/playwright/package.json (existing devDependencies: @playwright/test ^1.48.0, @axe-core/playwright ^4.10.0 — mirror the dep shape)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Installation" + "Version verification" blocks)
  </read_first>
  <action>
    After the Task 1 checkpoint approves, run `cd test/example/priv/playwright && npm install --save-dev parse5 cheerio`. Confirm @axe-core/playwright and @playwright/test are already present (do NOT reinstall them — they are pre-existing). Pin the resolved caret ranges into devDependencies (parse5 ^7.x, cheerio ^1.x per RESEARCH). Commit the updated package-lock.json alongside package.json so CI cache keys stay deterministic. Do NOT touch the root-level mix deps or any other package.json.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npm ls parse5 cheerio @axe-core/playwright @playwright/test --depth=0</automated>
  </verify>
  <done>parse5 and cheerio appear in package.json devDependencies with resolved versions; package-lock.json updated; `npm ls` shows all four packages resolved with no missing/extraneous errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add .gitignore entries for ephemeral evaluation bundles</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (the `test/example/_build/` block near the end — mirror its comment style; note the playwright subproject is NOT currently ignored)
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md (".gitignore (config, modify)" section — exact three paths)
  </read_first>
  <action>
    Append a Phase-216-labeled block to .gitignore ignoring the three ephemeral evaluation-artifact paths under the playwright subproject (D-04, Pitfall 4): the per-app-git-sha evidence bundle directory `test/example/priv/playwright/eval/`, the subproject playwright HTML report directory `test/example/priv/playwright/playwright-report/`, and the subproject `test/example/priv/playwright/test-results/`. Use directory-trailing-slash form matching the existing `test/example/_build/` entry. Add a one-line comment citing Phase 216 / D-04. Do NOT touch the existing `/test-results/` root entry (that is the repo-root MCP path, a different location).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -qxF 'test/example/priv/playwright/eval/' .gitignore && grep -qxF 'test/example/priv/playwright/playwright-report/' .gitignore && grep -qxF 'test/example/priv/playwright/test-results/' .gitignore && echo GITIGNORE_OK</automated>
  </verify>
  <done>All three subproject paths are ignored; a `git status` in a populated `eval/<sha>/` tree shows no untracked bundle files; the root `/test-results/` entry is unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Fix the shared ci.yml `id: base` step to emit merge-base (D-10)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the `Resolve base ref` step under `fast_checks`, ~L65-75: `git fetch origin "${{ github.base_ref }}" --depth=1` then `echo "ref=origin/${{ github.base_ref }}"`; and the `fetch-depth: 0` checkout above it; and the installer-detect step's three-dot `origin/main...HEAD` for the merge-base precedent)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Harness Wiring §3" + "Pitfall 3" — the exact before/after)
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("D-10 base-ref fix" block)
  </read_first>
  <action>
    In the `fast_checks` `Resolve base ref` step, replace the pull_request branch so it (a) fetches the base branch WITHOUT `--depth=1` (merge-base needs history; `fetch-depth: 0` on checkout already provides HEAD's full history per D-10), computes `MB=$(git merge-base "origin/${{ github.base_ref }}" HEAD)`, and emits `echo "ref=${MB}" >> "$GITHUB_OUTPUT"` instead of the base-branch tip. Leave the non-PR else-branch (`ref=HEAD~1`) untouched. Change ONLY the `id: base` step — do NOT add the new-guard `run:` steps here (those land in Plan 07 after the guards exist). This one change re-points every existing `--base "${{ steps.base.outputs.ref }}"` consumer (snapshot-canary x2, quality-ledger-monotonic) to the merge-base at once; that is intended (per D-10) and the existing guards' behavior on a not-ahead PR is unchanged (merge-base == tip when main has not moved).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -q 'git merge-base' .github/workflows/ci.yml && ! grep -qE 'git fetch origin "\$\{\{ github\.base_ref \}\}" --depth=1' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML_OK')"</automated>
  </verify>
  <done>The `id: base` PR branch computes and emits a merge-base SHA; no `--depth=1` remains on the base fetch; ci.yml still parses as valid YAML; the `else` (push) branch is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | New third-party packages (parse5, cheerio) cross into the committed dependency tree |
| CI base-ref resolution → every down-ratchet guard | The `id: base` output is the reference every monotonic guard trusts as "before" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-01-SC | Tampering | npm install (parse5, cheerio) | high | mitigate | Blocking, non-auto-approvable human legitimacy checkpoint (Task 1) verifies exact package + repo against the live registry before the lockfile is committed; RESEARCH pre-audit + `npm view repository.url` cross-check. |
| T-216-01-BASE | Tampering | ci.yml `id: base` merge-base | high | mitigate | Emitting the merge-base (not the tip) prevents false-fail down-ratchets AND prevents a stale-tip base from masking a real regression when main is behind; YAML-validated; existing-guard behavior verified unchanged when main has not moved. |
| T-216-01-IGN | Information Disclosure | gitignored bundles | low | mitigate | Explicit per-subproject ignores keep post-hydration DOM / PNG evidence out of git history; `grep -qxF` verify confirms all three exact paths present. |
</threat_model>

<verification>
- `npm ls parse5 cheerio @axe-core/playwright @playwright/test --depth=0` resolves cleanly.
- `.gitignore` contains all three exact subproject paths (grep -qxF).
- `ci.yml` uses `git merge-base` in `id: base`, no `--depth=1` on the base fetch, and parses as valid YAML.
- Existing guard self-tests still pass unchanged: `bash scripts/ci/quality-ledger-monotonic.test.sh`.
</verification>

<success_criteria>
- parse5 + cheerio installed, pinned, lockfile committed, legitimacy human-approved.
- Three ephemeral-bundle paths gitignored.
- `id: base` emits merge-base; no `--depth=1`; YAML valid; push-branch untouched.
</success_criteria>

<output>
Create `.planning/phases/216-harness-foundation-award-gradient/216-01-SUMMARY.md` when done.
</output>
