---
phase: 216-harness-foundation-award-gradient
plan: 07
type: execute
wave: 4
depends_on: ["216-01", "216-02", "216-03", "216-04", "216-05", "216-06"]
files_modified:
  - scripts/ci/admin-eval-harness.sh
  - guides/reference/admin-award-ledger.json
  - guides/reference/admin-render-sha.json
  - guides/reference/admin-quality-ledger.md
  - guides/reference/admin-eval-runbook.md
  - .github/workflows/ci.yml
autonomous: false
requirements: [HARNESS-01, HARNESS-02, HARNESS-03, RATCHET-01, RATCHET-02]

must_haves:
  truths:
    - "A single orchestrator (scripts/ci/admin-eval-harness.sh) drives the render matrix + all derivative guards end-to-end (D-01)."
    - "The two pilot surfaces (users-index-live, user-show-live) complete render→probe→verify-then-climb, capped at A2, with user-show's modal ownership re-verified against rendered output (D-23/D-24/D-25)."
    - "The new deterministic guards are wired into the fast_checks lane reading committed ledgers + merge-base; the render+probe Playwright job is a SEPARATE job (never in fast_checks, never the LLM path — JUDGE-CI-01)."
  artifacts:
    - "scripts/ci/admin-eval-harness.sh (thin orchestrator)"
    - "admin-award-ledger.json + admin-render-sha.json (pilots verify-then-climbed to earned bands ≤ A2)"
    - "guides/reference/admin-eval-runbook.md (how to run one iteration)"
    - ".github/workflows/ci.yml (fast_checks guard steps + separate admin-eval render job)"
  key_links:
    - "harness → admin-eval.spec.ts (Plan 06) → bundles → stale-render + anchor + findings + award guards (Plans 04/05)."
    - "pilot verify-then-climb → award-guard PASS on the earned bands; findings-monotonic PASS."
    - "ci.yml id:base merge-base (Plan 01) → every new --base guard consumer."
---

<objective>
Converge the independent tracks into a working end-to-end harness proven on the two pilot surfaces, and wire the deterministic guards into CI. Build the thin bash orchestrator (D-01) — one bash entrypoint driving one Playwright project, with NO mix task and NO standalone Node CLI (D-02: a mix task's only value is Ecto Sandbox reuse, moot for the static /admin/_design gallery; a second orchestration brain has no payoff); run the full render→probe→verify-then-climb loop on `users-index-live` + `user-show-live` (D-23), re-verifying user-show's possibly-stale modal-ownership claim against rendered output (D-24) and climbing each pilot's award band to what the rendered evidence earns, capped at A2 (D-25); commit the earned award/render-sha ledger updates; write the iteration runbook; and attach the guard steps to the `fast_checks` lane plus a separate `admin-eval` render job.

Purpose: This is Success Criterion 5 — two pilots complete the full loop with zero guard regressions. It is the proof the substrate works, and the CI wiring that makes the forward-only signal merge-blocking while keeping the render (and the future LLM panel) off the merge path.

Output: the orchestrator, the climbed pilot ledgers, the runbook, and the CI wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md
@.planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md
@.planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md

# Orchestrator analog + wiring targets + pilot evidence:
@scripts/ci/snapshot-recapture-gate.sh
@.github/workflows/ci.yml
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-eval-schema.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin-eval-harness.sh — thin orchestrator (render → canonicalize → guards)</name>
  <files>scripts/ci/admin-eval-harness.sh</files>
  <read_first>
    - scripts/ci/snapshot-recapture-gate.sh (lines 14-18 ROOT/PW/SIGRA_EXAMPLE_URL header; the `( cd "$PW" && CI=true SIGRA_EXAMPLE_URL=… npx playwright test <spec> --project=<name> )` subshell; the labeled phase-marker echoes; downstream guard chaining)
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("`scripts/ci/admin-eval-harness.sh`" section — copy the header verbatim, invoke admin-eval + -mobile + -dark, chain the derivative guards, terminal PASS line; NOTE the guards ALSO attach to fast_checks independently)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Harness Wiring §2")
  </read_first>
  <action>
    Author `scripts/ci/admin-eval-harness.sh` by cloning `snapshot-recapture-gate.sh`'s shape: the `set -euo pipefail` + ROOT/PW/SIGRA_EXAMPLE_URL header verbatim. Phase (a): run `( cd "$PW" && CI=true SIGRA_EXAMPLE_URL=… npx playwright test tests/admin-eval.spec.ts --project=admin-eval --project=admin-eval-mobile --project=admin-eval-dark )` to render the matrix + write bundles (the spec computes render_sha256 + probes inline). Phase (b): run the derivative guards in sequence with labeled echoes — `bash scripts/ci/stale-render-guard.sh`, `node scripts/ci/evidence-anchor-check.mjs`, `bash scripts/ci/quality-findings-monotonic.sh`, `node scripts/ci/award-guard.mjs`, `bash scripts/ci/settled-findings-lint.sh`. Terminal `echo "admin-eval-harness: PASS …"`. This is the LOCAL/full-run driver; the merge gate is the guards attached to fast_checks (Task 4), which read the committed ledgers independently — the orchestrator is not itself the CI gate. Boot expectation: consume an already-booted `SIGRA_EXAMPLE_URL` (default http://localhost:4011) exactly like the recapture gate; do not embed a bespoke boot brain.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash -n scripts/ci/admin-eval-harness.sh && grep -q "admin-eval.spec.ts" scripts/ci/admin-eval-harness.sh && grep -q "stale-render-guard.sh" scripts/ci/admin-eval-harness.sh && grep -q "award-guard.mjs" scripts/ci/admin-eval-harness.sh && echo HARNESS_SH_OK</automated>
  </verify>
  <done>The orchestrator parses (bash -n), invokes the admin-eval spec across the three projects, and chains all five derivative guards in sequence with labeled phases + a terminal PASS line.</done>
</task>

<task type="auto">
  <name>Task 2: Two-pilot end-to-end render + verify-then-climb (users-index-live, user-show-live)</name>
  <files>guides/reference/admin-award-ledger.json, guides/reference/admin-render-sha.json, guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-23 pilots span all nine probe classes; D-24 user-show FIRST re-verify modal ownership — confirm overlay may have moved to UserSessionsLive; D-25 cap at A2)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Band semantics (D-17, additive)" A0..A3; "Pitfall 6" stale modal claim)
    - guides/reference/admin-award-ledger.json (the committed A0 pilot cells from Plan 02)
    - guides/reference/admin-quality-ledger.md (the users-index-live / user-show-live cells + their cited admin-modal-interaction.spec.ts APG claim)
  </read_first>
  <action>
    Boot the example app (`scripts/db/up.sh` then the example server on PORT=4011, or reuse a running SIGRA_EXAMPLE_URL) and run `scripts/ci/admin-eval-harness.sh` against the two pilots. For each pilot, from the RENDERED bundles determine the earned award band per the additive A0..A3 semantics (A0 Nominated = Tier-2 + every applicable probe has a rendered evidence key; A1 = + the 3 promoted manual proxies rendered; A2 = + adversarial zero/loading/error states rendered & axe-clean + content-equivalence proven) — and CAP at A2 (D-25; A3 needs the persona panel re-run, out of scope). For `user-show-live`, the FIRST step is to re-verify modal ownership against the rendered DOM: assert WHERE the confirm overlay actually renders — if the design contract's move to `UserSessionsLive` means user-show no longer owns the modal, RECORD that as a caught stale prior claim (this is a feature per D-24), do not force a modal onto user-show. Update `admin-award-ledger.json` per pilot with the earned axes (each raised axis backed by rendered:true + a resolving evidence_ref + a fresh `verified_at_sha` = the render's app_git_sha), band = min(axes) (guard-checked), and update `admin-render-sha.json` with the real render_sha256 + open-finding counts for the pilots' cells. Append at most one sentence to each pilot's markdown evidence cell cross-referencing the JSON award band (NO column-4 change — D-19). After updating, run `node scripts/ci/award-guard.mjs --base HEAD`, `bash scripts/ci/quality-findings-monotonic.sh --base HEAD`, and `bash scripts/ci/stale-render-guard.sh` and confirm all PASS (zero guard regressions — Success Criterion 5).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node scripts/ci/award-guard.mjs --base HEAD && node -e "const a=require('./guides/reference/admin-award-ledger.json'); const ord={A0:0,A1:1,A2:2,A3:3}; for(const k of ['users-index-live','user-show-live']){const c=a.cells[k]; if(ord[c.band]>2) throw new Error(k+' exceeds A2 cap'); const m=Math.min(...Object.values(c.axes).map(v=>ord[v])); if(m!==ord[c.band]) throw new Error(k+' band!=min'); if(ord[c.band]>0 && !c.verified_at_sha) throw new Error(k+' climbed without verified_at_sha');} console.log('PILOTS_OK');"</automated>
  </verify>
  <done>Both pilots verify-then-climbed to their earned band ≤ A2 with band==min(axes), a real verified_at_sha, and resolving evidence; user-show modal ownership re-verified against rendered output (stale claim caught if present); award + findings + stale-render guards all PASS.</done>
</task>

<task type="auto">
  <name>Task 3: admin-eval-runbook.md — how to run one iteration + where human sign-off sits</name>
  <files>guides/reference/admin-eval-runbook.md</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Harness Wiring" all sections; "Validation Architecture" quick-run commands)
    - guides/reference/admin-eval-schema.md (Plan 02 — the schema doc to cross-reference)
    - scripts/ci/admin-eval-harness.sh (Task 1 — the command to document)
  </read_first>
  <action>
    Author `guides/reference/admin-eval-runbook.md`: the one-command local iteration (`scripts/db/up.sh` → boot example on 4011 → `scripts/ci/admin-eval-harness.sh`), how bundles land under the gitignored `eval/<app_git_sha>/`, which guards gate merges (stale-render, evidence-anchor, quality-findings-monotonic, award-guard, settled-findings-lint) vs which run as the separate render job, how to add a settled finding (`settled-findings-lint.sh --add … --disposition`), how a cell climbs an award band (rendered probe evidence + fresh verified_at_sha, never prose), and WHERE the human sign-off sits (the milestone-terminal PR sign-off — the LLM panel is advisory/off-CI, arriving in Phase 217; this phase's loop is fully deterministic). Cross-reference admin-eval-schema.md for the finding_id key + band semantics.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && test -f guides/reference/admin-eval-runbook.md && grep -q "admin-eval-harness.sh" guides/reference/admin-eval-runbook.md && grep -q "settled-findings-lint.sh" guides/reference/admin-eval-runbook.md && echo RUNBOOK_OK</automated>
  </verify>
  <done>The runbook documents the single-command iteration, the gating guards, the settled-findings workflow, band-climb rules, and where the human sign-off sits.</done>
</task>

<task type="auto">
  <name>Task 4: Wire the deterministic guards into fast_checks + a separate admin-eval render job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the fast_checks lane; the `Quality ledger monotonic guard` + self-test steps ~L109-112 to append beside; the `id: base` step already fixed in Plan 01; the design-lane Playwright job partitioning for the separate render job's shape)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Harness Wiring §4" the exact guard `run:` steps; "Open Question #2" separate render job recommendation)
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("`.github/workflows/ci.yml`" — new guard step registration shape)
  </read_first>
  <action>
    In the `fast_checks` lane, append guard steps beside the existing `Quality ledger monotonic guard` steps, each following the `- name:` + `run:` shape and passing `--base "${{ steps.base.outputs.ref }}"` (merge-base, from Plan 01) where the guard takes it: Stale-render guard (`bash scripts/ci/stale-render-guard.sh`), Evidence anchor integrity check (`node scripts/ci/evidence-anchor-check.mjs`), Quality findings monotonic guard (`bash … --base …`) + its self-test, Award ledger verify-then-climb guard (`node scripts/ci/award-guard.mjs --base …`) + its self-test, Settled findings lint (`bash scripts/ci/settled-findings-lint.sh`) + its self-test, and the stale-render self-test. Do NOT put the expensive render+probe Playwright run in fast_checks (RESEARCH Open Q2 — JUDGE-CI-01 invariant): add it as a SEPARATE `admin_eval_render` job mirroring the existing design-lane Playwright job's partitioning (its own runner, boots the example, runs `scripts/ci/admin-eval-harness.sh` or the spec directly, uploads the gitignored bundles as CI artifacts like playwright-report/). The fast_checks guards read the COMMITTED derived ledgers, so they never depend on the render job's runtime. Do NOT add these guards to the 5 required-check contexts — they ride inside the existing `fast_checks` aggregator (confirm the required-check names are unchanged). Validate YAML.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML_OK')" && grep -q "stale-render-guard.sh" .github/workflows/ci.yml && grep -q "award-guard.mjs" .github/workflows/ci.yml && grep -q "quality-findings-monotonic.sh" .github/workflows/ci.yml && grep -q "evidence-anchor-check.mjs" .github/workflows/ci.yml && grep -q "settled-findings-lint.sh" .github/workflows/ci.yml</automated>
  </verify>
  <done>All five guards + their self-tests are wired into fast_checks reading merge-base; the render+probe runs as a separate admin_eval_render job (not in fast_checks); ci.yml is valid YAML; the 5 required-check names are unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify the end-to-end pilot loop + CI wiring locally before phase close</name>
  <what-built>
    The full deterministic harness: orchestrator drives render→probe→bundle→five guards; two pilots verify-then-climbed (capped A2); guards wired into fast_checks with a separate render job. This checkpoint confirms the loop actually runs green locally and the wiring is correct, since the render pass needs a booted example app that CI runs but a reviewer should sanity-check once.
  </what-built>
  <how-to-verify>
    1. Boot: `scripts/db/up.sh` then boot the example app on PORT=4011 (or point SIGRA_EXAMPLE_URL at a running instance).
    2. Run the full loop: `bash scripts/ci/admin-eval-harness.sh` — confirm it renders the matrix, writes bundles under `test/example/priv/playwright/eval/<sha>/`, and all five guards print PASS.
    3. Run every guard self-test: `for t in stale-render-guard quality-findings-monotonic settled-findings-lint; do bash scripts/ci/$t.test.sh; done` and `node scripts/ci/award-guard.test.mjs` && `node scripts/ci/evidence-anchor-check.test.mjs` — all green.
    4. Confirm the two pilot cells in `admin-award-ledger.json` show earned bands ≤ A2 with real verified_at_sha, and `git status` shows NO files under `eval/` staged.
    5. Confirm user-show modal-ownership finding (stale-claim-caught or confirmed-present) is recorded.
  </how-to-verify>
  <resume-signal>Type "approved" if the loop is green end-to-end and the pilots climbed correctly, or describe the failing guard / incorrect band.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed pilot ledgers → fast_checks guards | The merge-blocking forward-only signal |
| render+probe job → committed ledgers | The (expensive, off-gate) producer of the derived signal |
| CI required-check set | Must stay the 5 existing contexts; new guards ride inside fast_checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-07-STALECLAIM | Tampering | user-show stale modal claim | high | mitigate | The pilot's FIRST job re-verifies modal ownership against rendered output; a stale APG claim is caught and recorded, not silently trusted (D-24, Pitfall 6). |
| T-216-07-OPTFLIP | Tampering | optimistically-flipped pilot band | high | mitigate | Verify-then-climb: each raised axis needs rendered:true + fresh verified_at_sha + resolving evidence; award-guard PASS is re-run in-task (Success Criterion 5) and in fast_checks. |
| T-216-07-JUDGECI | Tampering | LLM/expensive render on merge path | high | mitigate | Render+probe is a SEPARATE job; only the deterministic committed-ledger guards gate merges (JUDGE-CI-01 invariant); required-check names unchanged. |
| T-216-07-BUNDLECOMMIT | Information Disclosure | bundles committed to git | low | mitigate | Plan 01 gitignore + checkpoint step 4 confirms `git status` shows nothing under eval/; bundles upload as CI artifacts only. |
</threat_model>

<verification>
- `bash -n scripts/ci/admin-eval-harness.sh` + it chains all five guards.
- `node scripts/ci/award-guard.mjs --base HEAD` PASS; pilots ≤ A2, band==min, verified_at_sha present.
- runbook documents iteration + sign-off location.
- ci.yml valid YAML; five guards in fast_checks; separate render job; required checks unchanged.
- Human checkpoint: full local loop green + pilots climbed + no eval/ files staged.
</verification>

<success_criteria>
- Success Criterion 5 met: two pilots complete render→probe→ratchet end-to-end with zero guard regressions.
- Orchestrator + runbook exist; guards wired into fast_checks; render job separate.
- user-show modal ownership re-verified; pilots capped at A2.
</success_criteria>

<output>
Create `.planning/phases/216-harness-foundation-award-gradient/216-07-SUMMARY.md` when done.
</output>
