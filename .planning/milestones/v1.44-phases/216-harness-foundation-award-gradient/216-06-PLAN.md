---
phase: 216-harness-foundation-award-gradient
plan: 06
type: execute
wave: 3
depends_on: ["216-01", "216-03", "216-05"]
files_modified:
  - test/example/priv/playwright/lib/eval/probes.ts
  - test/example/priv/playwright/tests/admin-eval.spec.ts
  - test/example/priv/playwright/playwright.config.ts
  - scripts/ci/stale-render-guard.sh
  - scripts/ci/stale-render-guard.test.sh
autonomous: true
requirements: [HARNESS-01, HARNESS-02, HARNESS-03]

must_haves:
  truths:
    - "The admin-eval Playwright project renders the /admin/_design gallery matrix (theme×viewport×state) after waitForLiveViewReady and writes bundles via writeBundle (D-01/D-03/D-04)."
    - "Nine in-browser probes run at capture via page.evaluate, reading the --sg-* scale live off :root (never a JS constant table), reading box longhands, diffing focus box-shadow not outline, and enabling axe target-size explicitly (D-11..D-16)."
    - "The gate-vs-warn split holds: #1/#4/#5/#6@24/#7/#8 hard-gate in the -chromium DPR1 project; #2/#3/#5-shadow/#6@44/#9 and all -mobile/-dark geometry runs warn-only (D-15)."
    - "The stale-render guard hard-FAILS when a bundle's app_git_sha != HEAD, when admin source is newer than the bundle, when a bundle sha is unreachable, and when bundles are absent (D-07/D-08)."
  artifacts:
    - "test/example/priv/playwright/lib/eval/probes.ts (nine probe implementations)"
    - "test/example/priv/playwright/tests/admin-eval.spec.ts (render+probe+bundle spec)"
    - "test/example/priv/playwright/playwright.config.ts (admin-eval + -mobile + -dark projects)"
    - "scripts/ci/stale-render-guard.sh + .test.sh"
  key_links:
    - "probes.ts probe ids → eval-probe-ids.mjs (Plan 05) — same nine ids."
    - "admin-eval.spec.ts → canonicalize.renderSha256 + bundle.writeBundle (Plan 03) → eval/<sha>/ tree → stale-render-guard.sh + evidence-anchor-check.mjs (Plan 04)."
    - "each probe seeds a fixture defect that the probe MUST flag + a clean cell that MUST pass."
---

<objective>
Build the render/probe engine: the nine deterministic visual probes (`probes.ts`), the Playwright spec that renders the gallery matrix and writes bundles (`admin-eval.spec.ts`), the `admin-eval`/`-mobile`/`-dark` project registration (`playwright.config.ts`, add — do not fork), and the stale-render trust guard (`stale-render-guard.sh`). This is HARNESS-01 (capture) + HARNESS-03 (probes) + the capture-side half of HARNESS-02 (stale-render trust).

Purpose: This is the largest single track — it turns the primitives (canonicalize, bundle, probe-ids) into an actual render matrix and enforces that only fresh renders are trusted. Every probe must be proven against a seeded defect (flags it) AND a clean cell (passes), per the Nyquist strategy.

Output: probes.ts, admin-eval.spec.ts, the three config projects, and the stale-render guard + self-test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md
@.planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md
@.planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md

# Analogs to lift + config target:
@test/example/priv/playwright/tests/admin-design.spec.ts
@test/example/priv/playwright/playwright.config.ts
@test/example/priv/static/assets/sigra_admin.css
@scripts/ci/quality-ledger-monotonic.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: probes.ts — nine in-browser visual probes (live --sg-* reads, box-shadow focus, axe target-size)</name>
  <files>test/example/priv/playwright/lib/eval/probes.ts</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("### Pattern 2" live token/box reads; "### Pattern 3" focus box-shadow diff; "### Pattern 4" axe target-size explicit enable; "### Pattern 5" card-in-card lift; Anti-Patterns: no toHaveCSS for custom props #12629, no duplicated token table, longhands not shorthand)
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-11 probes at capture via page.evaluate; D-12 live :root reads + rem→px ±0.5px epsilon + resolved values; D-13 focus box-shadow not outline; D-14 reuse axe target-size + verbatim card-in-card + data-sg-<probe>-audit-only suppression; D-16 promote the 3 manual proxies)
    - test/example/priv/playwright/tests/admin-design.spec.ts (lines ~349-361 card-in-card check to lift verbatim; getComputedStyle usage)
    - test/example/priv/static/assets/sigra_admin.css (the --sg-space/radius/control scale + --sg-focus-ring box-shadow at ~L148-150)
    - scripts/ci/lib/eval-probe-ids.mjs (the nine canonical probe ids — probes.ts MUST emit findings tagged with these exact ids)
  </read_first>
  <behavior>
    - Probe #1 off-token-spacing: an element with padding NOT on the live --sg-space-* scale (±0.5px) → finding; a padding exactly on-scale → no finding.
    - Probe #4 ember-reserved-for: ember accent used outside the reserved selected/ownership context → finding; on-brand ember → none.
    - Probe #5 off-scale radius + control-height: a radius/control-height off the live --sg-radius-*/--sg-control-* scale → finding (shadow-composite is WARN-only).
    - Probe #6 target-size: axe target-size (explicitly enabled) flags a deliberate <24×24 control; a ≥24×24 control passes.
    - Probe #7 focus-ring: .focus() then diff computed box-shadow (and outline) — PASS if either changes; a control with no focus style → finding.
    - Probe #8 card-in-card: a nested .sg-card .sg-card:not(.sg-skeleton) (no audit-only attr) → finding; suppressed nesting → none.
    - Probes #2 misalignment, #3 size/weight budget, #9 below-fold primary: emit findings as WARN-only (see D-15); geometry (fold line via documentElement.clientHeight).
  </behavior>
  <action>
    Implement `probes.ts` exporting the nine probes as functions run via `page.evaluate` (or returning strings to evaluate), each emitting `{ finding_id_inputs: {surface, class, anchor}, class, anchor, severity: 'gate'|'warn', ... }` where `class` is the canonical probe id from eval-probe-ids.mjs and `anchor` is a STRUCTURAL selector / data-* hook (never prose — feeds the anchor check). Non-negotiables: read the --sg-* scale via `getComputedStyle(document.documentElement).getPropertyValue('--sg-space-N')` and NEVER `toHaveCSS` (#12629); resolve root font-size and normalize rem→px with a ±0.5px epsilon; read box LONGHANDS (paddingTop/Right/Bottom/Left, four radius corners) never shorthand; treat clamp()/color-mix()/oklab as already-resolved concrete values. Focus-ring (#7): call `.focus()` and PASS if computed box-shadow OR outline changes (authored as `--sg-focus-ring: 0 0 0 3px …` box-shadow at sigra_admin.css:148-150). Target-size (#6): use `@axe-core/playwright` with `.options({ rules: { 'target-size': { enabled: true } } })` (rule is disabled by default — Pitfall 1). Card-in-card (#8): lift `admin-design.spec.ts:349-361` verbatim, honoring `data-sg-card-nesting-audit-only`. Every probe honors a `data-sg-<probe>-audit-only` suppression attr (D-14 — do NOT invent a new mechanism). Tag each probe's finding `severity` per the D-15 gate/warn split. Author the fixture-driven assertions in admin-eval.spec.ts (Task 2), but keep the probe logic testable/pure here.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsx -e "import('./lib/eval/probes.ts').then(()=>console.log('PROBES_TS_OK')).catch(e=>{console.error(e);process.exit(1)})" && grep -q "getPropertyValue" lib/eval/probes.ts && ! grep -q "toHaveCSS" lib/eval/probes.ts</automated>
  </verify>
  <done>probes.ts imports/transpiles cleanly (via tsx/esbuild — the runner the subproject actually has, not tsc), reads --sg-* via getPropertyValue (no toHaveCSS), diffs focus box-shadow, enables axe target-size, lifts card-in-card, and tags each finding with a canonical probe id + structural anchor + gate/warn severity.</done>
</task>

<task type="auto">
  <name>Task 2: admin-eval.spec.ts + playwright.config.ts projects (render matrix → bundles + fixture-defect proofs)</name>
  <files>test/example/priv/playwright/tests/admin-eval.spec.ts, test/example/priv/playwright/playwright.config.ts</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("`admin-eval.spec.ts`" section — imports, waitForLiveViewReady verbatim, admin-registration email-prefix, card-in-card lift, render-matrix data-testid convention; "`playwright.config.ts`" section — ADD_don't-fork, the ADMIN_EVAL_SPEC constant + testIgnore exclusion + the admin-eval/-mobile/-dark triple)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Harness Wiring §1" project registration; "§5 Gate-vs-warn split")
    - test/example/priv/playwright/tests/admin-design.spec.ts (waitForLiveViewReady L15-26; registerUser + platform-admin+…@example.test prefix L28-52; data-testid="mg-N-{…}" matrix L336-347)
    - test/example/priv/playwright/playwright.config.ts (the projects: [...] array + testIgnore + pathTemplate)
  </read_first>
  <action>
    Register three projects in `playwright.config.ts` by ADDING (never forking — D-03): a `const ADMIN_EVAL_SPEC = /admin-eval\.spec\.ts/;`, exclude it from the chromium/mobile `testIgnore` sets, then append `admin-eval` (Desktop Chrome, DPR1 — the HARD-GATE geometry project), `admin-eval-mobile` (iPhone 13), and `admin-eval-dark` (colorScheme:'dark'). Inherit the top-level `use` (baseURL, longpoll timeouts) and pathTemplate. Author `admin-eval.spec.ts` (sibling to admin-design.spec.ts): reuse `waitForLiveViewReady` VERBATIM before every capture; register a per-project-unique `platform-admin+…@example.test` admin; iterate the gallery's `data-testid="mg-N-{populated,zero,loading,error}"` × theme × viewport matrix; for each cell capture outerHTML + PNG + axe JSON, run all nine probes from probes.ts collecting findings, and call `writeBundle` (Plan 03) to emit the bundle under `eval/<app_git_sha>/…`. Apply the D-15 gate-vs-warn split: in the `admin-eval` (chromium DPR1) project the gate-tier probes (#1/#4/#5-radius+control/#6@24/#7/#8) hard-fail the test; in `-mobile`/`-dark` and for the warn-tier probes (#2/#3/#5-shadow/#6@44/#9) collect findings but do not fail. CRITICAL: also seed one deliberate fixture defect per probe class (a <24×24 control for #6, a nested card without the audit-only attr for #8, an off-scale padding for #1, a control with no focus style for #7, etc.) and assert the corresponding probe FLAGS it, plus assert a known-clean cell produces NO gate finding — this proves each probe actually fires (Nyquist: seeded-defect flagged + clean-cell pass). Use fold line `documentElement.clientHeight` for #9.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test --list --project=admin-eval 2>&1 | grep -q "admin-eval.spec.ts" && node -e "const c=require('./playwright.config.ts'); " 2>/dev/null; grep -q "admin-eval-dark" playwright.config.ts && grep -q "admin-eval-mobile" playwright.config.ts && echo EVAL_PROJECTS_OK</automated>
  </verify>
  <done>The three admin-eval projects are registered (add, not fork); `--list` shows the spec under admin-eval; the spec renders the matrix, writes bundles, applies the gate/warn split, and each probe's seeded-defect + clean-cell assertions are present.</done>
</task>

<task type="auto">
  <name>Task 3: stale-render-guard.sh + hermetic .test.sh (git plumbing, absence = FAIL)</name>
  <files>scripts/ci/stale-render-guard.sh, scripts/ci/stale-render-guard.test.sh</files>
  <read_first>
    - .planning/phases/216-harness-foundation-award-gradient/216-PATTERNS.md ("`scripts/ci/stale-render-guard.sh`" section — idiom to lift + the net-new git-plumbing core; the admin globs anchored to ci.yml:88 installer-detect paths)
    - .planning/phases/216-harness-foundation-award-gradient/216-RESEARCH.md ("Pitfall 5" absence=FAIL; D-07 git plumbing never mtime + `git cat-file -e` loud-error + `git diff --name-only <bundle_sha> HEAD -- <admin globs>`)
    - .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (D-07/D-08 full text)
    - scripts/ci/quality-ledger-monotonic.sh + .test.sh (header/arg-parse/fail() + hermetic scaffold)
  </read_first>
  <action>
    Author `stale-render-guard.sh` using the shared bash-guard skeleton. Core (D-07/D-08): locate the current-run bundles under `test/example/priv/playwright/eval/$(git rev-parse HEAD)/` (or scan `eval/*/*/*/bundle.json`); for each `bundle.json`, read `app_git_sha` and hard-FAIL if `!= $(git rev-parse HEAD)`. Run `git cat-file -e <bundle_sha>` and error LOUDLY (not skip) if the sha is unreachable. Run `git diff --name-only <bundle_sha> HEAD -- <admin globs>` and FAIL "admin source newer than bundle" if non-empty; anchor the globs to the SAME admin paths the installer-detect step uses (`lib/sigra/**/admin/**`, `sigra_admin.css`, admin LiveViews). ABSENCE of any bundle = hard FAIL (opposite of the tier guard's empty-base skip — D-08); the captured sha lives inside the current run's bundle so there is no first-phase bootstrap problem. Author `stale-render-guard.test.sh` (hermetic scaffold): mismatched-sha FAIL, matching-sha PASS, empty-eval-dir FAIL, unreachable-sha errors loudly, admin-source-newer FAIL; unit-test the admin-glob matching.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/stale-render-guard.test.sh</automated>
  </verify>
  <done>Self-test passes: sha-match PASS, sha-mismatch FAIL, empty-bundle-dir FAIL, unreachable-sha loud error, admin-source-newer FAIL, glob matching unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gallery render → bundle | The dev-only /admin/_design gallery output captured as the evidence of record |
| bundle app_git_sha → stale-render guard | Whether a render may be trusted as current-HEAD |
| CSS token scale (:root) → probes | Off-token judgments must read the LIVE scale, not a stale copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-06-STALE | Tampering | stale/absent bundle false-pass | high | mitigate | stale-render-guard uses git plumbing (never mtime — actions/checkout#468); app_git_sha != HEAD FAILs, admin-source-newer FAILs, unreachable sha errors loudly, ABSENCE = hard FAIL (D-07/D-08); self-test proves each. |
| T-216-06-CLIMBRENDER | Tampering | climb-without-render (capture side) | high | mitigate | Probes run at capture and their findings/evidence keys carry the render's app_git_sha; the award guard (Plan 05) then requires a fresh verified_at_sha. Capture is the trust root. |
| T-216-06-DRIFT | Tampering | off-token probe drift from a stale JS token table | medium | mitigate | Probes read --sg-* live off :root via getPropertyValue (D-12) — no duplicated constant table (the example↔source drift lesson); verify greps that no toHaveCSS/constant table is used. |
| T-216-06-FALSECLEAN | Tampering | probe silently never fires (false-clean gate) | high | mitigate | Each probe is proven against a seeded fixture defect it MUST flag + a clean cell it MUST pass (esp. axe target-size explicit-enable, Pitfall 1); a probe that can't flag its own seeded defect fails the spec. |
</threat_model>

<verification>
- `npx tsx -e "import('./lib/eval/probes.ts')"` imports clean (tsx/esbuild — the subproject's actual runner, not tsc); probes.ts uses getPropertyValue, no toHaveCSS.
- `npx playwright test --list --project=admin-eval` shows the spec; -mobile/-dark projects registered (add, not fork).
- `bash scripts/ci/stale-render-guard.test.sh` green (all D-07/D-08 cases).
- Per-probe seeded-defect + clean-cell assertions present in the spec.
</verification>

<success_criteria>
- Nine probes implemented with live token reads, box-shadow focus, explicit axe target-size, verbatim card-in-card.
- admin-eval/-mobile/-dark projects added (not forked); render matrix → bundles.
- Gate-vs-warn split enforced (chromium DPR1 hard-gates; mobile/dark warn).
- stale-render guard: absence + mismatch + source-newer all FAIL.
</success_criteria>

<output>
Create `.planning/phases/216-harness-foundation-award-gradient/216-06-SUMMARY.md` when done.
</output>
