---
phase: 220-terminal-ratification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/evidence-anchor-check.mjs
  - .planning/todos/pending/2026-07-09-fastchecks-cheerio-missing-dep.md
  - .planning/todos/done/2026-07-09-fastchecks-cheerio-missing-dep.md
autonomous: true
requirements: [RATIFY-01]

must_haves:
  truths:
    - "evidence-anchor-check.mjs resolves cheerio only AFTER the no-bundles exit-0 guard, so a bundle-free fast_checks checkout exits 0 without cheerio being installed at ci.yml:145 (SC-4, closes PI-2 step-ordering crash)"
    - "The gate stays fully armed wherever bundles exist (admin_eval_render): a broken cite/anchor still flips evidence-anchor-check to a non-zero exit"
    - "The folded cheerio TODO is closed — moved to .planning/todos/done/ with status: done and a resolution note citing D-10"
  artifacts:
    - "scripts/ci/evidence-anchor-check.mjs (cheerio require relocated below the no-bundles guard)"
    - ".planning/todos/done/2026-07-09-fastchecks-cheerio-missing-dep.md"
  key_links:
    - "Evidence anchor step (ci.yml:145) runs BEFORE the scoped npm ci that installs cheerio (ci.yml:206); the lazy-require relocation makes the step order-independent (D-10, PI-2)"
  prohibitions:
    - "No new npm dependency is added — cheerio already lives in test/example/priv/playwright/node_modules; the require is RELOCATED, not added (rejects D-10 root-package.json alternative)"
    - "evidence-anchor-check.mjs stays merge-blocking — it is NOT made advisory (D-09: deterministic cite-and-flip gate, not the LLM panel)"
    - "No job-reorder fix (D-10 rejected: fragile/positional); no LLM panel invocation added to any required-check job (SC-4 invariant)"
---

<objective>
Fix the SC-4 `fast_checks` cheerio break with the LOCKED D-10 lazy-require relocation, and
close the folded cheerio TODO.

`evidence-anchor-check.mjs` currently resolves `cheerio` at module top (line 47), ~60 workflow
lines before the scoped `npm ci` that installs it (ci.yml:145 runs the step; ci.yml:206 installs
deps). On a bundle-free `fast_checks` checkout this throws `Cannot find module 'cheerio'` and reds
the (non-required) `fast_checks` job. The fix is a clean ~2-line relocation of the runtime require
to AFTER the existing no-bundles → `exit(0)` guard, keeping the deterministic gate fully armed
wherever bundles exist.

Purpose: turn `fast_checks` green on a bundle-free checkout without weakening the D-09 deterministic
integrity gate, and retire the tracked TODO — a prerequisite for the caveat-free terminal ship PR.
Output: relocated require in evidence-anchor-check.mjs + closed TODO.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/220-terminal-ratification/220-CONTEXT.md
@.planning/phases/220-terminal-ratification/220-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- **Modified:** `scripts/ci/evidence-anchor-check.mjs` — the runtime `_require('cheerio')` call
  (currently line 47) relocated to immediately after the no-bundles early-exit guard (after line 103).
- **Closed:** the folded TODO moves from `.planning/todos/pending/2026-07-09-fastchecks-cheerio-missing-dep.md`
  to `.planning/todos/done/2026-07-09-fastchecks-cheerio-missing-dep.md` (status flipped to `done`).

<tasks>

<task type="auto">
  <name>Task 1: Relocate the cheerio require below the no-bundles guard (D-10)</name>
  <read_first>
    scripts/ci/evidence-anchor-check.mjs (lines 39-130) — the createRequire block (44-47), the
    no-bundles guard (98-103), and the first cheerioLoad use (line 129).
    .planning/phases/220-terminal-ratification/220-RESEARCH.md — Situational Reverification rows 7-8
    and PI-2 (step-ordering, not locally reproducible).
  </read_first>
  <files>scripts/ci/evidence-anchor-check.mjs</files>
  <action>
    Move ONLY the cheerio-load binding — currently line 47, `const { load: cheerioLoad } = _require('cheerio');`
    — out of the module-top block (lines 44-47) and place it immediately AFTER the no-bundles
    early-exit guard block that ends at line 103 (`if (bundleDirs.length === 0) { console.log(...); process.exit(0); }`),
    and before the `let checkedFindings = 0;` counter / the bundle loop that starts at line 108.
    Leave the `createRequire(...)` factory binding (`const _require = createRequire(path.join(PW, 'package.json'));`,
    line 46) and its explanatory comment in place — `createRequire` does not resolve cheerio, so it
    cannot throw; only `_require('cheerio')` triggers module resolution and must sit below the guard.
    After the move, `cheerioLoad` is declared in module scope after the guard and remains in scope at
    its first use (line 129, `const $ = cheerioLoad(html)`). Do NOT reorder the ci.yml npm-ci step
    (D-10 rejected as fragile/positional); do NOT add cheerio to a root package.json (D-10 rejected).
    Keep the file's existing behavior identical wherever bundles exist — this is a pure relocation.
  </action>
  <verify>
    <automated>node -e "const l=require('fs').readFileSync('scripts/ci/evidence-anchor-check.mjs','utf8').split('\n');const g=l.findIndex(x=>x.includes('bundleDirs.length === 0'));const r=l.findIndex(x=>x.includes(\"_require('cheerio')\"));if(g>=0&&r>g){console.log('OK: cheerio require ('+(r+1)+') is below no-bundles guard ('+(g+1)+')');process.exit(0)}else{console.error('FAIL: cheerio require not relocated below no-bundles guard (guard='+g+' require='+r+')');process.exit(1)}"</automated>
    <automated>rm -rf /tmp/eac-empty-$$; mkdir -p /tmp/eac-empty-$$; node scripts/ci/evidence-anchor-check.mjs --bundles-dir /tmp/eac-empty-$$; rc=$?; rm -rf /tmp/eac-empty-$$; test $rc -eq 0</automated>
    <automated>node scripts/ci/evidence-anchor-check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The `_require('cheerio')` line number is strictly greater than the `bundleDirs.length === 0`
      guard line number (relocation proven statically — this is the load-bearing local proof per PI-2).
    - A bundle-free invocation (`--bundles-dir` pointing at an empty dir) exits 0.
    - With local bundles present, the script still runs to completion and PASSes (gate stays armed).
    - HONEST FRAMING: the authoritative "no crash without cheerio installed at step 145" proof is a
      CI signal on the terminal PR's `fast_checks` job (cheerio is present locally, so the crash is
      not locally reproducible — PI-2). Local proof = static relocation + empty-dir exit-0.
  </acceptance_criteria>
  <done>Cheerio require sits below the no-bundles guard; empty-bundles run exits 0; bundles-present run PASSes.</done>
</task>

<task type="auto">
  <name>Task 2: Close the folded cheerio TODO (D-10)</name>
  <read_first>
    .planning/todos/pending/2026-07-09-fastchecks-cheerio-missing-dep.md — the folded TODO frontmatter + body.
  </read_first>
  <files>.planning/todos/pending/2026-07-09-fastchecks-cheerio-missing-dep.md, .planning/todos/done/2026-07-09-fastchecks-cheerio-missing-dep.md</files>
  <action>
    Move the folded TODO from `.planning/todos/pending/` to `.planning/todos/done/`
    (git mv; create the `done/` directory if it does not exist). Change the frontmatter `status:`
    from `pending` to `done` and append a short resolution note to the body stating the fix landed
    per D-10 (lazy-require relocation in scripts/ci/evidence-anchor-check.mjs; chosen over the
    npm-ci-reorder and root-package.json alternatives), referencing this phase (220). Do NOT delete
    the TODO — it is closed by moving to done/, preserving its history.
  </action>
  <verify>
    <automated>test -f .planning/todos/done/2026-07-09-fastchecks-cheerio-missing-dep.md && ! test -f .planning/todos/pending/2026-07-09-fastchecks-cheerio-missing-dep.md && grep -q 'status: done' .planning/todos/done/2026-07-09-fastchecks-cheerio-missing-dep.md</automated>
  </verify>
  <acceptance_criteria>
    - The TODO file exists under `done/`, no longer under `pending/`.
    - Its frontmatter reads `status: done`.
    - A resolution note cites D-10 and Phase 220.
  </acceptance_criteria>
  <done>TODO relocated to done/ with status: done and a D-10 resolution note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI merge gate → repo | The deterministic evidence-anchor gate decides whether a PR is mergeable; weakening it lets unverified cite-anchor findings through |
| dependency resolution → runtime | `evidence-anchor-check.mjs` resolves `cheerio` at runtime via createRequire from the playwright subproject |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-220-02 | Tampering | scripts/ci/evidence-anchor-check.mjs cheerio require | medium | mitigate | Fix RELOCATES an already-present runtime dep — adds no new package, no lockfile-drift surface, no root manifest. Cheerio stays resolved from the existing test/example/priv/playwright manifest. |
| T-220-04 | Tampering | evidence-anchor-check merge-blocking posture | high | mitigate | Gate stays merge-blocking and fully armed wherever bundles exist (D-09); the relocation only changes WHEN cheerio loads, never WHETHER the gate flips on a broken cite. Verified by the bundles-present PASS check + the static relocation check. |
| T-220-SC | Tampering | npm/pip/cargo installs | low | accept | No package install occurs in this plan — the require is relocated, not added. No `[ASSUMED]`/`[SUS]` package introduced; supply-chain surface unchanged. |
</threat_model>

<verification>
- Static: cheerio require line number > no-bundles guard line number in evidence-anchor-check.mjs.
- Functional: empty-bundles-dir run exits 0; bundles-present run PASSes (gate armed).
- TODO: moved to done/ with status: done.
- CI (authoritative, on terminal PR): `fast_checks` runs `evidence-anchor-check.mjs` at step 145 on a
  bundle-free checkout and exits 0 without `Cannot find module 'cheerio'` (PI-2 — not locally provable).
</verification>

<success_criteria>
SC-4 mechanically satisfied on the bundle-free path: the deterministic evidence-anchor gate no longer
crashes `fast_checks` for lack of cheerio, stays armed where bundles exist, and the LLM panel remains
absent from every required-check job. Folded TODO closed.
</success_criteria>

<output>
Create `.planning/phases/220-terminal-ratification/220-01-SUMMARY.md` when done.
</output>
