---
phase: 220-terminal-ratification
plan: 04
type: execute
wave: 2
depends_on: ["220-01", "220-02", "220-03"]
files_modified:
  - .planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md
  - .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md
autonomous: true
requirements: [RATIFY-01]

must_haves:
  truths:
    - "A quarantine-PR body draft scopes EXACTLY the 3 impersonation-banner PNGs (chromium/dark/mobile), explains the required Example Playwright smoke going green = positive amd64 byte-correctness proof while the non-required fast_checks reds as-expected on the canary-modify, and documents the D-08 GITHUB_TOKEN-no-retrigger footgun (push via PAT/manual)"
    - "A close-readiness record documents the four landed ratification commits (D-10 cheerio, D-04 runbook, D-02 live-guard, D-13 record) and the EXACT deferred ship sequence: quarantine PR merge-first -> release-please PR #66 (v1.2.0) -> terminal full-branch merge-commit PR (D-11) rebased -> /gsd-ship -> release-please v1.3.0 -> /gsd-complete-milestone"
    - "The close-readiness record maps each SC (SC-1..SC-4) to its observable signal and current status, and lists the pre-ship re-verify items (PNG drift still exactly 3; PR #66 still open)"
    - "SC-4's 'LLM panel off every required-check job' half is EVIDENCED (not merely prohibited): the record captures an affirmative panel-absence proof — panel-ci-isolation.test.sh PASS (5 required lanes wire no panel/auto-fix-loop orchestrator) + a zero-match grep of ci.yml run: lines for the panel/loop orchestrator basenames"
  artifacts:
    - ".planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md"
    - ".planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md"
  key_links:
    - "The quarantine PR merges FIRST so the terminal PR (rebased on the new main) sees zero PNG diff -> caveat-free 6/6-green milestone artifact (D-06->D-07 ordering seam)"
    - "release-please PR #66 (v1.2.0) is cut BEFORE the v1.44 ship so version cut-points stay milestone-aligned (D-12)"
  prohibitions:
    - "This phase MUST NOT self-merge any PR (quarantine, #66, or terminal) and MUST NOT archive the milestone — the actual merge is /gsd-ship, archival is /gsd-complete-milestone (D-13, 215 precedent). Tasks PREPARE and DOCUMENT only."
    - "The quarantine strategy MUST NOT weaken/allowlist/bypass snapshot-canary-guard.sh (D-05/D-06 never-allowlistable tripwire); the draft records the guard-red as expected-by-design, not something to suppress"
    - "NOT squash-merge and NOT /gsd-pr-branch filtered (D-11 rejected both); the terminal ship is ONE full-branch merge-commit PR with .planning/ included"
---

<objective>
Assemble the DEFERRED-ship documentation: (1) a quarantine-PR body draft for the 3-PNG
`impersonation-banner` canary reconciliation (D-05..D-08), and (2) the phase close-readiness record
(D-13) capturing the exact deferred ship sequence and the SC status.

Per D-13 and the 215 precedent, Phase 220 MUST NOT self-merge any PR or archive the milestone — the
actual merges are `/gsd-ship`, archival is `/gsd-complete-milestone`. This plan PREPARES and DOCUMENTS
those steps so the operator/`/gsd-ship` can execute them without re-deriving anything.

Purpose: hand a caveat-free, ordered ship runbook to the operator. Output: 220-QUARANTINE-PR-BODY.md
and 220-CLOSE-READINESS.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/220-terminal-ratification/220-CONTEXT.md
@.planning/phases/220-terminal-ratification/220-RESEARCH.md
@.planning/phases/220-terminal-ratification/220-VALIDATION.md
</context>

## Artifacts this phase produces (this plan)

- **New:** `.planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md` — a ready-to-paste
  PR body + operator checklist for the 3-PNG baselines-only quarantine PR (D-06/D-08). A DRAFT only —
  the PR is opened/merged later by the operator, NOT by this phase.
- **New:** `.planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md` — the D-13 record: the
  four landed ratification commits, the exact deferred ship sequence, the SC->signal status map, and
  the pre-ship re-verify checklist.

<tasks>

<task type="auto">
  <name>Task 1: Draft the quarantine-PR body + operator checklist (D-05..D-08)</name>
  <read_first>
    .planning/phases/220-terminal-ratification/220-CONTEXT.md — D-05..D-08 (lines 68-86) and the
    Coherent ship sequence (lines 117-124).
    .planning/phases/220-terminal-ratification/220-RESEARCH.md — the SC-3 row (line 135), the
    positive-byte-proof insight (lines 213-215), and Environment Availability A1 (PAT fallback).
    scripts/ci/snapshot-canary-guard.sh — the never-allowlistable + modify-forbid logic (so the draft
    describes the guard-red accurately as by-design).
  </read_first>
  <files>.planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md</files>
  <action>
    Confirm the current PNG drift is STILL exactly the 3 impersonation-banner PNGs before writing:
    run `git fetch origin main` then `git diff --name-status origin/main...HEAD -- '*.png'` and record
    the result in the draft (all three must be `M` impersonation-banner-admin-checkpoints-{chromium,dark,mobile}.png).
    Write 220-QUARANTINE-PR-BODY.md containing: (a) a ready-to-paste PR title/body for a
    baselines-only PR whose diff is ONLY those 3 PNGs (branch off main, cherry-pick/checkout just the
    3 files from HEAD); (b) a plain explanation that on this PR the REQUIRED `Example Playwright smoke
    (full lifecycle)` goes GREEN because the fresh amd64 render matches the fresh amd64 baselines
    (positive byte-correctness proof — stronger than a human eyeballing pixels), while the NON-REQUIRED
    `fast_checks` reds as EXPECTED on the intended canary-modify (snapshot-canary-guard.sh is
    never-allowlistable by design — D-05/D-06 — this red is not to be suppressed); (c) the D-08 footgun
    note: a GITHUB_TOKEN-authored PR does NOT retrigger workflow runs, so the PR must be opened/pushed
    via a PAT or a manual push so the 5 required checks actually run and can be confirmed green before
    merge; (d) an operator scope-check checklist: diff is exactly 3 files, required Example Playwright
    smoke green, then merge FIRST (before the terminal PR) so the terminal PR sees zero PNG diff (D-07);
    (e) the fallback (ship terminal PR with fast_checks red directly — D-06-compliant, worse DX) noted
    as the non-default. This is a DRAFT — do NOT open, push, or merge any PR in this task.
  </action>
  <verify>
    <automated>git fetch origin main --quiet; test "$(git diff --name-only origin/main...HEAD -- '*.png' | wc -l | tr -d ' ')" = "3" && git diff --name-only origin/main...HEAD -- '*.png' | grep -c impersonation-banner | grep -qx 3</automated>
    <automated>test -f .planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md && grep -q 'impersonation-banner' .planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md && grep -qiE 'PAT|GITHUB_TOKEN|retrigger' .planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md && grep -qiE 'Example Playwright smoke' .planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md</automated>
  </verify>
  <acceptance_criteria>
    - Current PNG drift re-verified as EXACTLY 3 `M` impersonation-banner PNGs and recorded in the draft.
    - Draft contains a ready-to-paste baselines-only PR body scoping only those 3 files.
    - Draft explains required Example Playwright smoke green = positive amd64 byte-proof, non-required
      fast_checks red = expected-by-design canary-modify (guard never-allowlisted).
    - Draft documents the D-08 GITHUB_TOKEN-no-retrigger footgun (open via PAT/manual push).
    - Draft includes the operator scope-check + merge-FIRST ordering and the noted fallback.
    - NO PR is opened, pushed, or merged by this task.
  </acceptance_criteria>
  <done>220-QUARANTINE-PR-BODY.md is a complete, ready-to-paste draft; no PR opened/merged.</done>
</task>

<task type="auto">
  <name>Task 2: Write the close-readiness record (D-13)</name>
  <read_first>
    .planning/phases/220-terminal-ratification/220-CONTEXT.md — D-11/D-12/D-13 (lines 102-124) and
    the Coherent ship sequence.
    .planning/phases/220-terminal-ratification/220-VALIDATION.md — the SC->signal map + Manual-Only
    Verifications (for the SC status table and the two human sign-off gates).
    .planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md — produced by Plan 03,
    referenced as the SC-1 evidence.
    .planning/phases/220-terminal-ratification/220-QUARANTINE-PR-BODY.md — produced by Task 1 above,
    referenced as the SC-3 mechanism.
    scripts/ci/panel-ci-isolation.test.sh — the negative-assertion CI-isolation self-test (asserts the
    5 required-check lanes wire no panel/auto-fix-loop orchestrator); its PASS is the SC-4 panel-half
    affirmative evidence.
    .github/workflows/ci.yml — to run the direct zero-match grep over run: lines for the panel/loop
    orchestrator basenames (the affirmative SC-4 panel-absence proof).
  </read_first>
  <files>.planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md</files>
  <action>
    Write 220-CLOSE-READINESS.md capturing: (a) the four ratification commits landed on THIS branch
    (D-10 cheerio lazy-require + closed TODO; D-04 runbook freshness notes; D-02 live-guard
    confirmation record; and this D-13 close-readiness record), each with a one-line status and a
    cross-ref to its plan/artifact; (b) the EXACT deferred ship sequence to be executed OUTSIDE this
    phase, in order: quarantine 3-PNG impersonation-banner PR (push via PAT — D-08 — 5 required green +
    fast_checks red-as-expected, human scope-check, merge FIRST — D-06/D-07) -> merge release-please
    PR #66 (v1.2.0 — D-12; note steps 1 and 2 are order-independent per Claude's Discretion, sequence
    for cleanest terminal rebase) -> open the terminal v1.44 milestone PR as ONE full-branch
    merge-commit PR with .planning/ included (D-11: NOT squash, NOT /gsd-pr-branch filtered), rebased
    on the new main -> 6/6 green + human sign-off -> `/gsd-ship` merge -> release-please regenerates a
    v1.3.0 release PR (publishes to Hex) -> `/gsd-complete-milestone` archival; (c) an SC-1..SC-4 ->
    observable-signal status map (cite 220-LIVE-GUARD-CONFIRMATION.md for SC-1, the runbook for SC-2,
    the quarantine draft for SC-3, the cheerio fix for SC-4), marking which proofs are landed on-branch
    vs pending on-PR. For the SC-4 row specifically, record BOTH halves and EVIDENCE the panel half (do
    NOT leave it as mere prohibition): (i) the cheerio deterministic-gate fix (cite 220-01); and (ii) an
    AFFIRMATIVE panel-absence proof — run `bash scripts/ci/panel-ci-isolation.test.sh` (asserts the 5
    required-check lanes wire no panel/auto-fix-loop orchestrator; expect PASS 3/3) AND a direct
    zero-match grep over `.github/workflows/ci.yml` `run:` lines for the panel/loop orchestrator
    basenames (assemble the basenames at runtime — `admin-` + `panel.sh` and `admin-` + `autofix-loop.sh`
    — for grep hygiene) -> expect zero matches. Capture the literal zero-match result and the
    `panel-ci-isolation.test.sh` PASS verbatim in the SC-4 evidence line, so SC-4's "LLM panel off every
    required-check job" half is EVIDENCED, not merely prohibited (the off-CI prohibition stays too).
    (d) a pre-ship RE-VERIFY checklist (research valid-until 7 days): PNG drift still
    exactly 3; PR #66 still OPEN; merge-base still origin/main HEAD. Also note as an EXECUTION-TIME step
    (gated on the live required-check greens, NOT performed in this phase): flipping 220-VALIDATION.md
    `nyquist_compliant: true` and completing its sign-off happens only once the live CI proof lands on
    the terminal PR — 220-VALIDATION.md stays `draft` until then (flipping it now would be dishonest).
    State explicitly that Phase 220
    does NOT self-merge or archive (D-13, 215 precedent) — this record is the handoff to the operator /
    `/gsd-ship`. Do NOT merge any PR or archive the milestone in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md && grep -qiE 'gsd-ship' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md && grep -qiE 'gsd-complete-milestone' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md && grep -q '#66' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md</automated>
    <automated>grep -qiE 'full-branch|merge-commit' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md && grep -qiE 'NOT squash|not squash' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md && grep -qE 'SC-1|SC-2|SC-3|SC-4' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md</automated>
    <automated>grep -qiE 'does not self-merge|must not self-merge|not self-merge|do not self-merge' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md</automated>
    <automated>bash scripts/ci/panel-ci-isolation.test.sh</automated>
    <automated>P="admin-"; test "$(grep -cE "run:.*${P}panel.sh|run:.*${P}autofix-loop.sh" .github/workflows/ci.yml)" = "0"</automated>
    <automated>grep -q 'panel-ci-isolation' .planning/phases/220-terminal-ratification/220-CLOSE-READINESS.md</automated>
  </verify>
  <acceptance_criteria>
    - Record lists the four landed ratification commits with plan/artifact cross-refs.
    - Record documents the exact ordered deferred ship sequence (quarantine -> #66 -> terminal
      full-branch merge-commit PR -> /gsd-ship -> v1.3.0 -> /gsd-complete-milestone).
    - Record maps SC-1..SC-4 to observable signals with landed-vs-pending status.
    - SC-4's panel half is EVIDENCED, not merely prohibited: the record captures the
      `panel-ci-isolation.test.sh` PASS AND the zero-match ci.yml `run:` grep for the panel/loop
      orchestrator basenames (affirmative panel-absence proof for the 5 required lanes).
    - Record includes the pre-ship re-verify checklist (3-PNG drift, PR #66 open, merge-base) and notes
      the 220-VALIDATION.md `nyquist_compliant: true` flip / sign-off as an execution-time step gated on
      the live required-check greens (stays `draft` until then).
    - Record states Phase 220 does NOT self-merge or archive (D-13). No PR merged/archived by this task.
  </acceptance_criteria>
  <done>220-CLOSE-READINESS.md documents the four commits, the ordered deferred ship sequence, the SC status map, and the pre-ship re-verify checklist; no merge/archival performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ship-sequence doc → operator merge actions | A wrong order or a suppressed guard-red instruction could merge a standing red on the artifact of record (normalization of deviance) or self-merge prematurely |
| canary guard → merge decision | The never-allowlistable canary guard must not be weakened to make the quarantine PR "green" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-220-03 | Repudiation | merging a standing red required check on the terminal PR | high | mitigate | The quarantine strategy isolates the canary-modify to a baselines-only PR so the terminal PR is caveat-free (D-06/D-07); the draft records fast_checks red as expected-by-design on the quarantine PR, never suppressed on the merge of record. |
| T-220-01 | Tampering | snapshot-canary-guard.sh never-allowlistable tripwire | high | mitigate | Docs explicitly forbid weakening/allowlisting the canary guard (D-05/D-06); the guard is the mitigation, the quarantine PR routes around it correctly without touching it. |
| T-220-08 | Elevation of Privilege | premature self-merge / self-archive within the phase | high | mitigate | Both tasks are DOCUMENT-only; the record states Phase 220 must not self-merge or archive (D-13, 215 precedent); merges deferred to /gsd-ship + /gsd-complete-milestone. |
| T-220-SC | Tampering | npm/pip/cargo installs | low | accept | No package install; docs-only plan. |
</threat_model>

<verification>
- Quarantine draft: PNG drift re-verified exactly 3; PAT footgun + green-byte-proof + scope-check present.
- Close-readiness: ordered deferred sequence, SC status map, pre-ship re-verify checklist, no-self-merge statement.
- SC-4 panel half evidenced: panel-ci-isolation.test.sh PASS + zero-match ci.yml run: grep captured in the record.
- No PR opened/pushed/merged and no milestone archived by this plan.
</verification>

<success_criteria>
SC-3 (mechanism) and the D-13 close-readiness are fully documented: the operator/`/gsd-ship` has a
caveat-free, ordered ship runbook (quarantine 3-PNG PR merge-first -> #66 -> terminal full-branch
merge-commit PR -> /gsd-ship -> v1.3.0 -> /gsd-complete-milestone) with no guard weakened and no
premature self-merge/archival.
</success_criteria>

<output>
Create `.planning/phases/220-terminal-ratification/220-04-SUMMARY.md` when done.
</output>