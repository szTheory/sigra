---
phase: 217-adversarial-panel-auto-fix-safety-rails
plan: 06
type: execute
wave: 3
depends_on: ["217-02", "217-05"]
files_modified:
  - scripts/panel/fix-apply.mjs
  - scripts/panel/fix-apply.test.mjs
  - scripts/panel/copy-rules.json
  - scripts/ci/admin-autofix-loop.sh
  - scripts/ci/admin-autofix-loop.test.sh
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
autonomous: true
requirements: [AUTOFIX-02]

must_haves:
  truths:
    - "fix-apply.mjs auto-applies ONLY copy-swap and token-swap classes, to admin LiveView .heex attributes/inline-style + the example only — never CSS, never component/judgment."
    - "Token-swap reuses the probe onScale/nearest logic reading facts.json scale_px; applies only within a tightened +/-1.0px band, preserving !important; ties or >1.0px downgrade to judgment."
    - "Copy-swap is a text-node-only edit from the fixed copy-rules.json ruleset; no free-form model text is ever written to source."
    - "The auto-fix loop commits one fix per commit (git add -A && git commit) and auto-reverts via git revert --no-edit HEAD (a NEW commit) if any of FOUR rails trips."
    - "The auto-fix loop reverts any fix that drifts a committed baseline PNG under the snapshot allowlist (rail 4 = scripts/ci/snapshot-canary-guard.sh vs the pre-loop sha), on the same git-revert footing as the other three rails."
    - "A reverted finding is written to settled-findings.tsv (waived, autofix-217) and added to a gitignored poison-set so it is never retried."
    - "The hermetic injected-regression test proves BOTH rails fire (a Revert commit exists, reflog clean, ledger restored, finding settled) AND quality-findings-monotonic.sh exits non-zero on the pre-revert commit."
  artifacts:
    - scripts/panel/fix-apply.mjs
    - scripts/ci/admin-autofix-loop.sh
    - scripts/ci/admin-autofix-loop.test.sh
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
  key_links:
    - "admin-autofix-loop.sh re-runs admin-eval-harness.sh after each fix and reads FOUR rails: quality-findings-monotonic.sh (count), award-guard.mjs min(axes) vs a pre-loop ledger snapshot, any deterministic-gate/anchor-resolution flip, AND scripts/ci/snapshot-canary-guard.sh (baseline-PNG drift vs the pre-loop sha) — because snapshot-canary-guard.sh is a fast_checks step the harness itself does NOT run, so a .heex/inline-style auto-fix that shifts a committed baseline PNG would otherwise pass the loop yet fail fast_checks on the PR carrying the fix commits."
    - "fix-apply.mjs consumes the auto_eligible fix-queue.json entries built by fix-queue-build.mjs (Plan 02)."
  prohibitions:
    - "No git reset / force-push / admin-merge — auto-revert is git revert --no-edit HEAD (a new commit) only."
    - "No model text reaches CSS/shell/HEEx — token-swap is deterministic arithmetic, copy-swap is a fixed ruleset."
    - "No CSS-token auto-apply (sigra_admin.css 3-lockstep-copy problem is scoped out of 217)."
    - "An auto-fix commit never lands a committed baseline-PNG drift — rail 4 reverts it before the loop advances."
    - "admin-autofix-loop.sh must NEVER be wired into any CI lane."
---

<objective>
Build the auto-fix safety rails (AUTOFIX-02): `fix-apply.mjs` (deterministic copy + token swaps to `.heex`/inline-style + example only — LLM strictly OUT of the apply path), the fixed `copy-rules.json` normalization ruleset, and the bash `admin-autofix-loop.sh` that applies one `auto_eligible` fix per commit, re-renders, and auto-reverts via `git revert` if any of FOUR rails trips (the fourth being a `snapshot-canary-guard.sh` baseline-PNG drift check, closing the hole where a `.heex`/inline-style fix passes the loop but fails `fast_checks` on the PR). Prove it with the hermetic `admin-autofix-loop.test.sh` (SC-4 — both rails fire on a seeded count-delta) plus the `board-autofix-seed` live-companion fixture.

Purpose: This is the auto-fix engine and its proof. The loop is an operator/nightly tool OFF the merge path; only its committed ledger diffs gate. The injected-regression test is the SC-4 guarantee that the rails actually catch a deliberately-clunky change.
Output: `fix-apply.mjs` (+ `.test`), `copy-rules.json`, `admin-autofix-loop.sh`, `admin-autofix-loop.test.sh`, the `board-autofix-seed` fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-CONTEXT.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-RESEARCH.md

@scripts/ci/admin-eval-harness.sh
@scripts/ci/quality-findings-monotonic.sh
@scripts/ci/quality-findings-monotonic.test.sh
@scripts/ci/award-guard.mjs
@scripts/ci/settled-findings-lint.sh
@scripts/ci/snapshot-canary-guard.sh
@test/example/priv/playwright/lib/eval/probes.ts
@test/example/priv/playwright/tests/admin-eval.spec.ts
@test/example/lib/example_web/live/admin/design_gallery_live.ex
@guides/reference/fix-queue.json
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-02-SUMMARY.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fix-apply.mjs + copy-rules.json — deterministic copy + token swaps</name>
  <read_first>test/example/priv/playwright/lib/eval/probes.ts (onScale/nearest logic + measured_px/scale_px), guides/reference/fix-queue.json</read_first>
  <files>scripts/panel/fix-apply.mjs, scripts/panel/fix-apply.test.mjs, scripts/panel/copy-rules.json</files>
  <behavior>
    - Test 1 (token): an off-scale numeric CSS value in a .heex inline style whose nearest scale_px token is within +/-1.0px is swapped to the nearest --sg-* token; !important is preserved.
    - Test 2 (token downgrade): a tie or a nearest >1.0px away is NOT applied (downgraded to judgment) — the file is untouched.
    - Test 3 (copy): a label needing sentence-case / terminal-period normalization per copy-rules.json is swapped as a text-node-only edit; a copy edit needing semantic judgment is refused (routed to judgment).
    - Test 4 (scope): fix-apply only ever edits admin LiveView .heex attributes/inline-style + the example; it refuses any CSS file, any component/judgment class.
  </behavior>
  <action>Per D-13: create `scripts/panel/copy-rules.json` (a fixed deterministic normalization ruleset: sentence-case labels, terminal-period consistency, house-style tokens) and `scripts/panel/fix-apply.mjs`. Token-swap reuses the probe `onScale`/nearest logic from `probes.ts` reading the LIVE `facts.json` `scale_px` + the finding's `measured_px` (NEVER a forked constant table); applies only when the nearest token is within a tightened +/-1.0px band, preserves `!important`, and downgrades ties or >1.0px to `judgment`. Copy-swap is a text-node-only edit from `copy-rules.json`; any edit needing semantic judgment routes to `judgment`. The apply surface is confined to admin LiveView `.heex` attributes/inline-`style=` + the example only — refuse CSS files (the 3-lockstep `sigra_admin.css` copies are scoped OUT) and refuse `component`/`judgment` findings. NO model text is ever read here — the input is the derived `auto_eligible` fix-queue entry + deterministic arithmetic/ruleset only. Author `fix-apply.test.mjs` per Behavior.</action>
  <verify>
    <automated>node scripts/panel/fix-apply.test.mjs</automated>
  </verify>
  <done>Self-test passes; token swap applies only within +/-1.0px preserving !important; ties/>1.0px and CSS/component/judgment are refused; copy swap is a fixed-ruleset text-node edit.</done>
</task>

<task type="auto">
  <name>Task 2: admin-autofix-loop.sh — apply/commit/re-render/revert with 4 rails</name>
  <read_first>scripts/ci/admin-eval-harness.sh, scripts/ci/award-guard.mjs (minBand), scripts/ci/quality-findings-monotonic.sh, scripts/ci/settled-findings-lint.sh, scripts/ci/snapshot-canary-guard.sh (rail 4 — --base &lt;pre-loop-sha&gt;, allowlist-scoped baseline-PNG drift, non-zero exit on any un-allowlisted change)</read_first>
  <files>scripts/ci/admin-autofix-loop.sh</files>
  <action>Per D-14 (plus RESEARCH.md Open Question #1, now resolved as rail 4): create the bash `scripts/ci/admin-autofix-loop.sh` (house idiom — bash orchestrator, Node at leaves). Snapshot the pre-loop sha + a copy of `admin-award-ledger.json`. For each `auto_eligible` finding in `fix-queue.json`, highest-priority-first (bounded by `--max-fixes N`, pick a defensible small default e.g. 5 and record it): run `fix-apply.mjs` -> `git add -A && git commit` (atomic, one fix per commit) -> re-run `admin-eval-harness.sh` (re-render + all guards). Auto-revert via `git revert --no-edit HEAD` (a NEW commit — NEVER `reset`/force-push/admin-merge, per the ruleset) if ANY of FOUR rails trips: (1) `quality-findings-monotonic.sh` shows a count increase vs the pre-loop sha, (2) a per-surface award-band floor breach — reuse `award-guard.mjs` `min(axes)` against the pre-loop `admin-award-ledger.json` snapshot (an already-elevated surface must not drop), (3) any deterministic gate flips / a cited anchor stops resolving, or (4) a committed baseline PNG under the snapshot allowlist drifts vs the pre-loop sha — assert via `scripts/ci/snapshot-canary-guard.sh --base <pre-loop-sha>` (it is a `fast_checks` step the harness does NOT itself run, so without this rail a `.heex`/inline-`style=` token/copy fix that perturbs a committed baseline PNG would pass the loop yet fail `fast_checks` later on the PR carrying the auto-fix commits). On revert (any rail, rail 4 included), write the finding to `settled-findings.tsv` (`disposition=waived, waived_by=autofix-217`) via `settled-findings-lint.sh --add` and add it to a persisted gitignored poison-set (`eval/autofix-state.json`) so it is never retried. The loop is resumable + idempotent. It is an operator/nightly tool OFF the merge path — do NOT wire it into any CI lane; ensure `eval/autofix-state.json` is gitignored.</action>
  <verify>
    <automated>bash -n scripts/ci/admin-autofix-loop.sh && grep -q 'git revert --no-edit' scripts/ci/admin-autofix-loop.sh && grep -q 'snapshot-canary-guard.sh' scripts/ci/admin-autofix-loop.sh && ! grep -q 'git reset --hard\|--force\|push --force' scripts/ci/admin-autofix-loop.sh</automated>
  </verify>
  <done>The loop syntax-checks; uses `git revert --no-edit` (never reset/force-push); reads all FOUR rails (including `snapshot-canary-guard.sh` baseline-PNG drift vs the pre-loop sha); writes waived findings to settled-findings.tsv + a gitignored poison-set; bounded by --max-fixes.</done>
</task>

<task type="auto">
  <name>Task 3: board-autofix-seed live fixture + admin-autofix-loop.test.sh (SC-4)</name>
  <read_first>test/example/lib/example_web/live/admin/design_gallery_live.ex (216-08 in-scope seeded-defect board pattern), scripts/ci/quality-findings-monotonic.test.sh (mktemp-hermetic idiom to clone)</read_first>
  <files>test/example/lib/example_web/live/admin/design_gallery_live.ex, scripts/ci/admin-autofix-loop.test.sh</files>
  <action>Per D-15: (a) add a `board-autofix-seed` board to `design_gallery_live.ex` — a deliberately-clunky, intentionally off-spec test fixture (off-token spacing / ember misuse / misalignment in board-scoped DOM, mirroring the 216-08 in-scope seeded-defect pattern) that the real harness renders; it is a TEST FIXTURE, not a product surface. (b) Create the hermetic `scripts/ci/admin-autofix-loop.test.sh` cloned from `quality-findings-monotonic.test.sh` (mktemp throwaway git repo, real guard binaries, browser-free, trap-clean, real repo untouched). Seed a deliberately-clunky COUNT-DELTA (the monotonic guard reads the committed count, not the DOM) and assert BOTH rails fire: (i) a `git revert` commit exists (`git log` shows a `Revert "autofix...` subject; reflog clean = no force-push), the ledger is restored, and the finding is in `settled-findings.tsv`; (ii) `quality-findings-monotonic.sh` exits non-zero on the pre-revert commit — causally linked to the revert via a harness test-double, not independently green. Scope note: SC-4's hermetic `.test.sh` proves the count-monotonic rail (rail 1) — a mktemp count-delta seed is the cheap, browser-free proof. Rail 4 (baseline-PNG drift) is NOT cheaply hermetic-testable here (it would need a committed-PNG + snapshot-allowlist fixture and a real render diff), so leave SC-4 as-is: rail 4 is proven instead by the Task-2 `<verify>` grep asserting `snapshot-canary-guard.sh` is wired into the loop, plus the OFF-CI `board-autofix-seed` live companion. Add `admin-autofix-loop.test.sh` to the `fast_checks` self-test list (the TEST attaches; the loop binary never does). Grep-hygiene: assemble any literal the CI-isolation test asserts-absent via a shell variable, do not embed the loop-orchestrator basename verbatim as a `run:`-shaped line.</action>
  <verify>
    <automated>bash scripts/ci/admin-autofix-loop.test.sh && bash scripts/ci/panel-ci-isolation.test.sh</automated>
  </verify>
  <done>The hermetic test proves both rails fire (Revert commit + clean reflog + restored ledger + settled finding, AND monotonic guard non-zero on the pre-revert commit); `board-autofix-seed` exists as a clunky fixture; the test attaches to fast_checks and the CI-isolation test still passes (loop not wired).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fix-queue entry -> source edit | A derived queue entry drives an automated edit to committed source; must be bounded + reversible |
| auto-commit -> git history | The loop writes commits; must obey the ruleset (no reset/force-push/admin-merge) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-217-06-UNSAFE | Tampering/DoS | auto-fix applying an unsafe/unbounded edit | high | mitigate | Only copy+token classes auto-apply (D-13); CSS + component/judgment refused; `--max-fixes N` bounds a run; token-swap is deterministic arithmetic, copy-swap a fixed ruleset — no model text reaches source |
| T-217-06-PNGDRIFT | Tampering | a `.heex`/inline-style auto-fix silently perturbs a committed baseline PNG (a `fast_checks` gate the harness itself does not run) | medium | mitigate | Rail 4: after each fix + re-render, `scripts/ci/snapshot-canary-guard.sh --base <pre-loop-sha>` asserts no un-allowlisted committed baseline PNG drifted; on drift, the fix is `git revert`ed + waived + poisoned like any other rail — so an auto-fix commit can never land a baseline-PNG drift that would fail `fast_checks` on its PR |
| T-217-06-HISTORY | Repudiation | auto-commit forging history / bypassing the ruleset | high | mitigate | `git revert --no-edit HEAD` = a new commit; the hermetic test asserts a `Revert "autofix...` subject + clean reflog; no reset/force-push/admin-merge (verified by a negative grep in Task 2) |
| T-217-06-LOOP | DoS | endless retry loop on a poison finding | medium | mitigate | Reverted findings enter a persisted poison-set + settled-findings.tsv so they are never retried (Renovate lesson); loop is idempotent + resumable |
| T-217-06-JUDGE | Elevation of Privilege | loop wired into a CI gate | high | mitigate | Loop stays OFF the merge path; the CI-isolation test (Plan 03) asserts no `run:` step invokes it |
</threat_model>

<verification>
- `node scripts/panel/fix-apply.test.mjs` passes.
- `bash -n scripts/ci/admin-autofix-loop.sh` clean; uses `git revert --no-edit`; wires `snapshot-canary-guard.sh` as rail 4; no reset/force-push.
- `bash scripts/ci/admin-autofix-loop.test.sh` passes (both rails fire).
- `bash scripts/ci/panel-ci-isolation.test.sh` still passes (loop unwired).
- `eval/autofix-state.json` is gitignored.
</verification>

<success_criteria>
The auto-fix loop applies only provably-safe copy/token swaps as atomic commits, re-renders, and auto-reverts via `git revert` on any of FOUR rail breaches (count-monotonic, award-band floor, deterministic-gate/anchor flip, and committed-baseline-PNG drift via `snapshot-canary-guard.sh`); the hermetic injected-regression test proves both rails fire; the loop is never wired into CI.
</success_criteria>

<output>
Create `.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-06-SUMMARY.md` when done
</output>
