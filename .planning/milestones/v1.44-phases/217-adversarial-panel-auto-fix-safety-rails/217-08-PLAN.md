---
phase: 217-adversarial-panel-auto-fix-safety-rails
plan: 08
type: execute
wave: 5
depends_on: ["217-05", "217-06", "217-07"]
gap_closure: true
autonomous: true
requirements: [PANEL-01, PANEL-02, AUTOFIX-01, AUTOFIX-02]
files_modified:
  - guides/reference/admin-render-sha.json
  - scripts/ci/admin-panel.sh
  - scripts/panel/judge.mjs
  - scripts/panel/judge-cli.test.mjs
  - guides/reference/fix-queue.json
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - guides/reference/admin-eval-runbook.md
  - .github/workflows/ci.yml
user_setup:
  - service: anthropic
    why: "OPTIONAL, post-merge only. The single un-automatable check is the TRUE-live SC-2 paid run (2nd admin-panel.sh run makes 0 calls with a real key). It is documented as an OPTIONAL operator confirmation in admin-eval-runbook.md and is NOT a task gate — the plan completes autonomously without it. Nothing in this plan's <verify> blocks needs a key: the SC-4 apply->revert->waive chain is deterministic and API-free (Postgres-only, and even that is avoided by running against a throwaway clone with --skip-render)."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (env-only; NEVER committed or logged). Needed ONLY for the OPTIONAL post-merge operator-only TRUE-live SC-2 confirmation, not for any task <verify>."

must_haves:
  truths:
    - "admin-panel.sh's PILOT_SURFACES and admin-render-sha.json's render_sha256 cells agree on the same concrete board-mg surfaces (board-mg-5-* and board-mg-9-*), so a live admin-panel.sh run enumerates cells that have real captured bundles on disk (closes the disjoint-surface-set gap)."
    - "The judge.mjs CLI reads dom.html + facts.json from the bundle output dir (eval/<sha>/<surface>/<cell>/) and hard-refuses paid API calls on an empty DOM BEFORE importing or constructing the Anthropic SDK — so the refuse path is provable key-free."
    - "A deterministic self-test drives runJudge against a REAL on-disk board-mg bundle's dom.html + facts.json (read via the --output-dir layout) with an injected SDK double, proving 0 API calls on a cache hit and k calls on a cache miss — no ANTHROPIC_API_KEY."
    - "One appliable, in-band SPACE-token finding is seeded on the board-autofix-seed surface (a 12.5px inline-style padding in design_gallery_live.ex; fix-queue entry measured_px:[12.5] + 10-entry scale_px resolving to var(--sg-space-12) with NO token_family hint). It survives fix-queue-lint.sh (auto_eligible correctly derives from fix_class:token) and is NOT a merge-gated render cell in admin-render-sha.json, so quality-findings-monotonic.sh stays green."
    - "The SC-4 apply->rail-trip->revert->waive chain runs AUTONOMOUSLY and API-free against the seeded in-band finding inside a throwaway git clone of the final committed HEAD (real repo history untouched): fix-apply rewrites 12.5px -> var(--sg-space-12), the loop commits it, a rail trips (count perturbation mirrored from the hermetic test), git revert --no-edit lands a Revert commit, admin-award-ledger.json is restored, the finding is written to settled-findings.tsv with disposition=waived, and the reflog shows no force-push / reset --hard. The outcome is asserted by an automated <verify> block (git log / reflog / ledger diff / settled-findings.tsv), NOT operator attestation."
    - "quality-findings-monotonic.sh --base <merge-base> PASSES with the new board-mg render_sha256 cells present (they carry open_findings: 0 at introduction, which the panel never reads), so committed state stays green."
    - "The runbook documents the OPTIONAL, post-merge, operator-only TRUE-live SC-2 paid run (2nd run = 0 calls + empty verdicts diff with a real key) as the single un-automatable check — it is NOT a task gate; the plan completes autonomously on the now-automated SC-4 chain + the deterministic judge-cli test."
  artifacts:
    - guides/reference/admin-render-sha.json
    - scripts/ci/admin-panel.sh
    - scripts/panel/judge.mjs
    - scripts/panel/judge-cli.test.mjs
    - guides/reference/fix-queue.json
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - guides/reference/admin-eval-runbook.md
  key_links:
    - "admin-panel.sh PILOT_SURFACES -> admin-render-sha.json cells (both name the same board-mg surfaces) -> on-disk eval/<sha>/<surface>/<cell>/ bundles."
    - "judge.mjs CLI --output-dir -> eval/<sha>/<surface>/<cell>/dom.html + facts.json (IN-01 bundle-read path, now SDK-free until a real call is warranted)."
    - "judge-cli.test.mjs -> runJudge (Plan 05) with an injected SDK double + a real on-disk board-mg bundle."
    - "fix-queue.json seeded board-autofix-seed finding (measured_px:[12.5], 10-entry scale_px) -> fix-apply.mjs applyTokenSwap resolves var(--sg-space-12) -> the 12.5px inline-style padding on #board-autofix-seed in design_gallery_live.ex (the exact string fix-apply rewrites)."
    - "admin-autofix-loop.sh surface_to_file('board-autofix-seed') -> design_gallery_live.ex; loop applies -> rail trips -> git revert --no-edit -> settled-findings.tsv (disposition=waived) + restored admin-award-ledger.json (asserted by Task 3's automated <verify>)."
  prohibitions:
    - "Do NOT implement Option 1 (rendering the pilot surfaces). Scope is LOCKED to Option 2 (repoint the panel at board-mg surfaces)."
    - "Do NOT set open_findings > 0 on the newly added board-mg cells in this commit — a nonzero value on a cell absent at merge-base trips quality-findings-monotonic (0 -> N increase). The panel reads only render_sha256, never open_findings."
    - "Do NOT run fix-queue-build.mjs as part of this commit's committed output (it recomputes the board-mg cells to 197/181, tripping the monotonic gate). If run for any reason, reset the newly added board-mg cells' open_findings back to 0 before committing."
    - "admin-panel.sh must NEVER be wired into any CI lane (JUDGE-CI-01). Only the new deterministic judge-cli.test.mjs is added to fast_checks."
    - "admin-autofix-loop.sh must NEVER be wired into any CI lane (JUDGE-CI-01). The SC-4 loop run happens inside a THROWAWAY git clone during Task 3 execution — it must NOT commit/revert to the real repo history and must NOT appear as a CI run: step."
    - "The seeded board-autofix-seed finding is a SPACE token (10-entry scale_px, NO token_family) so fix-apply APPLIES it (var(--sg-space-12)); do NOT seed it as a 4-entry radius/control finding (WR-03 refuses those without token_family, which would make the loop SKIP and prove nothing). Do NOT set surfaces_affected on it (single-surface -> priority must be 'normal', not 'systemic', per fix-queue-lint (d))."
    - "Do NOT add a board-autofix-seed cell to admin-render-sha.json for the seed finding — keeping it OFF the merge-gated render matrix is exactly what keeps quality-findings-monotonic green (the loop applies-then-reverts, so the tree is net-zero at final committed HEAD regardless)."
    - "ANTHROPIC_API_KEY must never be echoed/logged/committed."
---

<objective>
Close the two deferred Phase-217 human-verification items (SC-2 zero-calls live reality, SC-4 board-autofix-seed live companion) by (1) aligning the LLM panel's target surfaces with the board-mg surfaces the 217 render matrix actually renders (LOCKED Option 2), (2) confirming + hardening the judge.mjs CLI bundle-wiring and proving it with a NEW deterministic, key-free self-test, and (3) seeding ONE appliable in-band SPACE-token finding on the board-autofix-seed surface and driving the SC-4 apply->rail-trip->revert->waive chain AUTONOMOUSLY (API-free, inside a throwaway git clone of the final committed HEAD) with an AUTOMATED assertion block. The single un-automatable check — the TRUE-live SC-2 paid run — becomes an OPTIONAL post-merge operator confirmation in the runbook; it is NOT a task gate, so the plan completes autonomously.

Purpose: The panel currently targets pilot surfaces (users-index-live/user-show-live) that the 217 render matrix never renders, so a live admin-panel.sh run finds no bundles and makes 0 API calls for the WRONG reason (proving nothing). Aligning panel + render matrix + fix-queue + autofix on the same concrete board-mg surfaces makes SC-2 meaningful and shifts SC-2's proof as far LEFT as defensible (a deterministic CLI bundle-read test), leaving only the paid-key confirmation as an optional operator step. SC-4 was deferred because a live run against real committed data lands fix/revert commits on main; the committed fix-queue's only board-mg auto_eligible entries are 12 systemic radius findings with NO measured_px/scale_px/token_family, so fix-apply refuses every one and the loop SKIPs (0 commits/reverts/waives) — the SC-4 acceptance could never be met against committed data. This plan closes that GENUINELY: it seeds one appliable in-band SPACE finding fix-apply WILL apply, and runs the whole chain in a disposable clone so the real repo history stays clean AND the outcome is a repeatable automated assertion (zero human attestation, per the project's zero-human-UAT preference).
Output: aligned admin-render-sha.json + admin-panel.sh; a hardened, SDK-free-until-needed judge.mjs CLI; a new judge-cli.test.mjs wired into fast_checks; a seeded appliable board-autofix-seed finding (fix-queue.json + design_gallery_live.ex); an automated SC-4 apply->revert->waive proof; and an updated runbook documenting the optional operator-only SC-2 paid run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-CONTEXT.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-VERIFICATION.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-UAT.md
@.planning/todos/pending/2026-07-04-panel-pilot-surface-render-mismatch.md

@scripts/ci/admin-panel.sh
@scripts/panel/judge.mjs
@scripts/panel/judge.test.mjs
@scripts/ci/admin-autofix-loop.sh
@scripts/ci/admin-autofix-loop.test.sh
@scripts/panel/fix-apply.mjs
@scripts/ci/fix-queue-build.mjs
@scripts/ci/fix-queue-lint.sh
@scripts/ci/quality-findings-monotonic.sh
@guides/reference/admin-render-sha.json
@guides/reference/fix-queue.json
@guides/reference/admin-eval-runbook.md
@test/example/lib/example_web/live/admin/design_gallery_live.ex
@test/example/priv/playwright/tests/admin-eval.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align panel + render matrix on board-mg surfaces (LOCKED Option 2)</name>
  <read_first>
    guides/reference/admin-render-sha.json (existing cell schema: cells[surface][theme-viewport-state] = {render_sha256, open_findings}),
    scripts/ci/admin-panel.sh (line 91 PILOT_SURFACES; --all reads cells keys),
    test/example/priv/playwright/tests/admin-eval.spec.ts (line 350 surface = `${boardId}-${state}`; line 107 cell = `${theme}-${viewport}-${state}`; writeBundleLocal at line 105),
    scripts/ci/quality-findings-monotonic.sh (INVERTED guard: FAILS on open_findings increase vs merge-base; a cell absent at base defaults base=0)
  </read_first>
  <files>guides/reference/admin-render-sha.json, scripts/ci/admin-panel.sh</files>
  <action>
    Per the LOCKED Option-2 scoping decision (repoint the panel at the concrete board-mg surfaces the 217 render matrix actually renders): the pilot surfaces users-index-live and user-show-live were derived from board-mg-5 (user-results-pagination) and board-mg-9 (identity-header-summary-facts) respectively — the admin-render-sha.json `notes` already states this. Do NOT re-litigate; do NOT implement Option 1 (rendering the pilot surfaces).

    (a) In guides/reference/admin-render-sha.json, ADD render_sha256 cells for the board-mg-5-* and board-mg-9-* surfaces (the two boards the pilots were derived from), following the existing cell schema exactly. The on-disk bundle layout is surface=`board-mg-N-<state>`, cell=`<theme>-<viewport>-<state>`. Add the DESKTOP light+dark cells the panel judges, for the four states, i.e. eight surfaces total:
      - board-mg-5-populated : { light-desktop-populated, dark-desktop-populated }
      - board-mg-5-zero      : { light-desktop-zero, dark-desktop-zero }
      - board-mg-5-loading   : { light-desktop-loading, dark-desktop-loading }
      - board-mg-5-error     : { light-desktop-error, dark-desktop-error }
      - board-mg-9-populated : { light-desktop-populated, dark-desktop-populated }
      - board-mg-9-zero      : { light-desktop-zero, dark-desktop-zero }
      - board-mg-9-loading   : { light-desktop-loading, dark-desktop-loading }
      - board-mg-9-error     : { light-desktop-error, dark-desktop-error }
    Use these EXACT render_sha256 values (verified from the on-disk bundle.json manifests; identical across cells within a board because the board outerHTML is theme/state-invariant at this render):
      - all board-mg-5-* cells: 8b92c4714fa18812f2c022944ff64dcf9b6c316e2744398f4b71a2f947c4afb8
      - all board-mg-9-* cells: 088e9ab595a9bb3f5abae675ccf68a6c82907a025184bbad838c7985bb0eae8d
    Set open_findings: 0 on EVERY newly added board-mg cell. This is REQUIRED for gate-safety: quality-findings-monotonic.sh --base <merge-base> defaults a cell absent at base to 0 and FAILS on any increase; a nonzero introduction value (e.g. 197) would trip 0 -> 197. The panel path (judge.mjs / admin-panel.sh) reads ONLY render_sha256, never open_findings, so 0 is functionally correct for the panel. Do NOT run fix-queue-build.mjs on this commit — it would recompute these cells to 197/181 and trip the gate; if it is run for any reason, reset the added board-mg cells' open_findings back to 0 before committing.
    KEEP the existing users-index-live and user-show-live cells unchanged (do not delete them; removing keys is a decrease and safe, but retaining them avoids any verdicts-cache/orphan churn and keeps the existing open-findings ratchet intact).

    (b) In scripts/ci/admin-panel.sh, change PILOT_SURFACES from ("users-index-live" "user-show-live") to the board-mg subset the panel now judges:
      PILOT_SURFACES=("board-mg-5-populated" "board-mg-5-zero" "board-mg-5-loading" "board-mg-5-error" "board-mg-9-populated" "board-mg-9-zero" "board-mg-9-loading" "board-mg-9-error")
    Leave the --all fan-out, the freshness precondition, the Hammer no-op, and the estimated-call-count logic unchanged — they already read surfaces/cells generically from admin-render-sha.json. Do NOT wire admin-panel.sh into any CI lane.

    Note: the synthetic pilot-surface names in test fixtures (judge.test.mjs, panel-verdicts-lint.test.sh, quality-findings-monotonic.test.sh, evidence-anchor-check.test.mjs, etc.) are self-contained hermetic fixtures — they do NOT read the committed ledgers and MUST NOT be edited. Only admin-panel.sh's live PILOT_SURFACES and the committed admin-render-sha.json cells are load-bearing here.
  </action>
  <acceptance_criteria>
    - admin-render-sha.json has valid JSON with the eight board-mg-5-*/board-mg-9-* surfaces added, each desktop light+dark cell carrying the exact render_sha256 above and open_findings: 0; the existing pilot cells are untouched.
    - admin-panel.sh PILOT_SURFACES lists the eight board-mg surfaces; the two former pilot names no longer appear in PILOT_SURFACES.
    - quality-findings-monotonic.sh --base <merge-base-of-this-branch> exits 0 (the added board-mg cells at open_findings: 0 do not increase vs base).
    - admin-panel.sh with no key still exits 0 (Hammer no-op) and panel-ci-isolation.test.sh still passes (admin-panel.sh not wired into CI).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('guides/reference/admin-render-sha.json','utf8')); const need=['board-mg-5-populated','board-mg-5-zero','board-mg-5-loading','board-mg-5-error','board-mg-9-populated','board-mg-9-zero','board-mg-9-loading','board-mg-9-error']; for(const s of need){const c=d.cells[s]; if(!c) throw new Error('missing surface '+s); for(const [k,v] of Object.entries(c)){ if(!/^[0-9a-f]{64}$/.test(v.render_sha256)) throw new Error('bad sha '+s+'/'+k); if(v.open_findings!==0) throw new Error('open_findings must be 0 on new cell '+s+'/'+k); }} console.log('render-sha cells OK');" && grep -qF 'board-mg-5-populated' scripts/ci/admin-panel.sh && grep -qF 'board-mg-9-error' scripts/ci/admin-panel.sh && ! grep -qE 'PILOT_SURFACES=\("users-index-live"' scripts/ci/admin-panel.sh && MB=$(git merge-base origin/main HEAD 2>/dev/null || git rev-parse HEAD~1) && bash scripts/ci/quality-findings-monotonic.sh --base "$MB" && env -u ANTHROPIC_API_KEY bash scripts/ci/admin-panel.sh >/dev/null 2>&1; test $? -eq 0 && bash scripts/ci/panel-ci-isolation.test.sh</automated>
  </verify>
  <done>Panel and render matrix agree on the board-mg-5/9 surfaces; the monotonic gate stays green (board-mg cells at open_findings: 0); admin-panel.sh remains a no-op without a key and is not wired into CI. SC-2 is now unblocked (its cells have real captured bundles on disk).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm + harden judge.mjs CLI bundle-wiring; prove it with a deterministic key-free self-test</name>
  <read_first>
    scripts/panel/judge.mjs (CLI entry line 561+; IN-01 bundle-read at lines 612-638; SDK import at line 587 currently BEFORE the empty-DOM refuse),
    scripts/panel/judge.test.mjs (the SDK-double / callCount idiom at lines 124-165; runJudge signature: {surface, cell, renderSha256, verdictEntry, excerptDom, factsJson, sdkClient, outputDir, currentProvenance}),
    test/example/priv/playwright/tests/admin-eval.spec.ts (writeBundleLocal line 105 -> eval/<appGitSha>/<surface>/<cell>/{dom.html,facts.json,...}; admin-panel.sh passes --output-dir=${BUNDLE_BASE}/${surface}/${cell})
  </read_first>
  <files>scripts/panel/judge.mjs, scripts/panel/judge-cli.test.mjs, .github/workflows/ci.yml</files>
  <behavior>
    - Confirmed path convention: admin-panel.sh always passes --output-dir=eval/<HEAD-sha>/<surface>/<cell>; judge.mjs reads dom.html + facts.json from that dir (resolved against ROOT). The fallback path (no --output-dir) uses eval/<renderSha256>/<surface>/<cell>, which the live orchestrator never exercises — leave it as-is but keep the --output-dir path as the canonical one.
    - Refuse-on-empty-DOM path (IN-01) must be reachable WITHOUT importing/constructing the Anthropic SDK, so it is provable in a key-free deterministic test: with a bundle dir whose dom.html is empty/absent, the CLI exits non-zero with "refusing to make paid API calls" and makes 0 network calls.
    - Cache-hit path: given a REAL on-disk board-mg bundle's dom.html + facts.json (read via the --output-dir layout) and a matching verdict-cache entry, runJudge returns from_cache=true and makes 0 API calls (callCount === 0) with an injected SDK double.
    - Cache-miss path: with no matching verdict entry, runJudge makes exactly k API calls against the injected double (callCount === k), confirming the DOM/facts were wired through to the call.
  </behavior>
  <action>
    (a) Harden judge.mjs CLI ordering so the IN-01 refuse-on-empty-DOM decision is SDK-free: MOVE the `const { default: Anthropic } = await import('@anthropic-ai/sdk')` + `const client = new Anthropic()` construction (currently ~line 587, before the render_sha/verdicts/bundle-read block) to AFTER the empty-DOM refuse guard (the block ending at the `process.exit(1)` for "refusing to make paid API calls against an empty DOM"). The SDK is only needed to construct the client passed to runJudge for a real call; the render-sha lookup, verdicts-cache read, bundle dom.html/facts.json read, and the empty-DOM refuse must all run before the import. This makes the refuse path — and a bundle-present cache-hit path — reachable and deterministic without @anthropic-ai/sdk installed at root. Do NOT change runJudge, the key check (still exit 0 on missing ANTHROPIC_API_KEY, before any bundle work), or the --output-dir path convention.

    (b) Create scripts/panel/judge-cli.test.mjs — a deterministic, ANTHROPIC_API_KEY-free self-test that proves the CLI bundle-wiring + call/skip decision against a REAL on-disk board-mg bundle. Follow the judge.test.mjs SDK-double idiom (an sdkClient with messages.create incrementing a callCount, returning `{ content: [{ type:'text', text:'{}' }] }`). The test MUST:
      1. Locate a real on-disk board-mg-5 bundle under test/example/priv/playwright/eval/<sha>/board-mg-5-populated/light-desktop-populated/ (discover <sha> by scanning the eval dir for a dir containing board-mg-5-populated/light-desktop-populated/dom.html). If no such bundle exists (clean checkout / gitignored eval dir absent), SKIP with a clear "no on-disk board-mg bundle — run admin-eval-harness.sh first" message and exit 0 (do NOT fail — eval/ is gitignored and may be absent in CI). This keeps the test deterministic and non-flaky while still exercising the real path whenever bundles are present.
      2. Read dom.html + facts.json from that bundle dir exactly as the CLI does (the --output-dir layout), assert dom.html is non-empty, and pass them as excerptDom/factsJson to runJudge.
      3. Cache-hit case: build a matching verdict-cache entry (mirror makeCacheEntry in judge.test.mjs) for the bundle's render_sha256 with matching provenance {model:'claude-opus-4-8', k:3, quorum:2, rubric_version:'1.0', prompt_sha:...}; assert callCount === 0 and result.from_cache === true.
      4. Cache-miss case: pass verdictEntry=null; assert callCount === 3 (k), confirming the on-disk DOM/facts were wired into the k calls.
      5. Empty-DOM refuse case: call runJudge (or a small extracted helper) with excerptDom='' and assert it does NOT make a paid call (mirror the CLI's refuse contract); a static grep in this test asserting the CLI source still contains the "refusing to make paid API calls" string BEFORE the SDK import line is acceptable as the CLI-ordering guard.
    Use node:assert + node:test-style plain asserts consistent with judge.test.mjs. cheerio-backed imports resolve from the Playwright subproject node_modules (same as judge.test.mjs) — the fast_checks step below installs them first.

    (c) Wire judge-cli.test.mjs into fast_checks in .github/workflows/ci.yml immediately after the existing "Judge self-test" step (line ~201, `node scripts/panel/judge.test.mjs`), reusing the already-installed Playwright subproject deps. Add a step named "Judge CLI bundle-wiring self-test" running `node scripts/panel/judge-cli.test.mjs`. Do NOT add admin-panel.sh or admin-autofix-loop.sh to any lane (JUDGE-CI-01).
  </action>
  <acceptance_criteria>
    - In judge.mjs, the `import('@anthropic-ai/sdk')` + `new Anthropic()` now appear AFTER the empty-DOM refuse guard; the render-sha lookup, verdicts read, bundle dom.html/facts.json read, and refuse guard all precede the import.
    - scripts/panel/judge-cli.test.mjs exists and passes with ANTHROPIC_API_KEY unset: cache-hit -> callCount 0 + from_cache true; cache-miss -> callCount 3; empty-DOM -> no paid call; CLI-ordering grep passes. When no on-disk bundle is present it SKIPs cleanly (exit 0) rather than failing.
    - .github/workflows/ci.yml runs `node scripts/panel/judge-cli.test.mjs` in fast_checks after judge.test.mjs; neither admin-panel.sh nor admin-autofix-loop.sh appears as a CI run: step (panel-ci-isolation.test.sh still passes).
    - judge.test.mjs still passes 11/11 (no regression to runJudge).
  </acceptance_criteria>
  <verify>
    <automated>(cd test/example/priv/playwright && npm ci --ignore-scripts >/dev/null 2>&1 || npm install --ignore-scripts >/dev/null 2>&1); env -u ANTHROPIC_API_KEY node scripts/panel/judge-cli.test.mjs && env -u ANTHROPIC_API_KEY node scripts/panel/judge.test.mjs && grep -q 'judge-cli.test.mjs' .github/workflows/ci.yml && bash scripts/ci/panel-ci-isolation.test.sh && node -e "const s=require('fs').readFileSync('scripts/panel/judge.mjs','utf8'); const imp=s.indexOf(\"import('@anthropic-ai/sdk')\"); const refuse=s.indexOf('refusing to make paid API calls'); if(imp===-1||refuse===-1) throw new Error('markers missing'); if(refuse>imp) throw new Error('SDK import must come AFTER the empty-DOM refuse guard'); console.log('CLI ordering OK');"</automated>
  </verify>
  <done>The judge.mjs CLI bundle-read convention is confirmed and hardened so the refuse decision is SDK-free; a new deterministic key-free self-test proves the CLI reads a real on-disk board-mg bundle's DOM+facts and takes the correct call/skip decision (0 calls on cache hit, k on miss), shifting SC-2's live reality-check as far left as defensible; it is wired into fast_checks while the panel entrypoints stay off the merge path.</done>
</task>

<task type="auto">
  <name>Task 3: Seed an appliable in-band SPACE finding + prove the SC-4 apply->revert->waive chain autonomously (clone-isolated, API-free) + document the optional SC-2 paid run</name>
  <read_first>
    scripts/panel/fix-apply.mjs (applyTokenSwap: applies ONLY when nearest scale token is within +/-1.0px; resolveTokenRef resolves a 10-entry SPACE scale to var(--sg-space-N) with NO token_family hint, but REFUSES a 4-entry radius/control scale unless finding.token_family==='radius' — WR-03),
    scripts/ci/admin-autofix-loop.test.sh (the hermetic proof: seeds an in-band 12.5px SPACE fixture with measured_px:[12.5] + scale_px:[1,2,3,4,5,6,7,8,10,12] -> var(--sg-space-12), and installs a count-bumping post-commit hook to force rail-1 to trip on re-commit),
    scripts/ci/admin-autofix-loop.sh (surface_to_file maps ANY board-* surface -> design_gallery_live.ex; rail-1 = quality-findings-monotonic vs PRE_LOOP_SHA; git revert --no-edit HEAD; add_to_poison writes settled-findings.tsv disposition=waived; --skip-render skips the harness re-render),
    scripts/ci/fix-queue-lint.sh (recomputes derived fields: auto_eligible MUST equal fix_class in {copy,token}; single-surface entries must NOT be priority:systemic; it does NOT touch measured_px/scale_px/token_family — those pass through),
    guides/reference/fix-queue.json (the 12 committed board-mg auto_eligible entries all lack measured_px/scale_px/token_family -> fix-apply refuses every one -> loop SKIPs; this is why committed data alone yields 0 commits/reverts/waives),
    test/example/lib/example_web/live/admin/design_gallery_live.ex (line ~1425 #board-autofix-seed fixture; currently a 13px border-radius on a 4-entry scale — the exact WR-03 refuse case — with no board-autofix-seed entry in the queue),
    guides/reference/admin-eval-runbook.md (existing off-CI panel + loop section; "Pilot surfaces" note to update),
    .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (216-09 SC-5 committed-HEAD discipline: a pre-commit render sha is invalid; capture on a clean tree at the final committed HEAD)
  </read_first>
  <files>guides/reference/fix-queue.json, test/example/lib/example_web/live/admin/design_gallery_live.ex, guides/reference/admin-eval-runbook.md</files>
  <action>
    Make the SC-4 live apply->revert chain GENUINELY runnable and AUTONOMOUS (not the weak refuse path). The committed board-mg findings all lack measured_px/scale_px/token_family so fix-apply refuses them; and the board-autofix-seed fixture is a 4-entry-radius (WR-03 refuse) case that isn't even in the queue. Fix BOTH by seeding one appliable in-band SPACE finding, then prove the whole chain inside a throwaway clone so the real repo history stays clean and the assertion is repeatable.

    (a) Add an appliable in-band SPACE inline-style value to the #board-autofix-seed fixture in test/example/lib/example_web/live/admin/design_gallery_live.ex. Inside the existing `<div id="board-autofix-seed" ...>` block, add ONE child element whose inline style carries a 12.5px SPACE value that fix-apply will rewrite to var(--sg-space-12), modeled EXACTLY on the hermetic test fixture (12.5px is 0.5px from the space-12 token, inside the +/-1.0px band). Use a padding declaration, e.g. a `<div class="sg-stack" style="padding: 12.5px">...</div>` with a short muted label noting it is the SC-4 in-band space seed. Keep the existing 13px border-radius line and the other clunky defects intact — they are unrelated and stay as-is. Do NOT alter any board-mg-5/9 render cell or any product surface; this edit is confined to the board-autofix-seed fixture.

    (b) Seed ONE appliable finding into guides/reference/fix-queue.json for surface `board-autofix-seed`. It MUST be fix-queue-lint-clean and fix-apply-appliable:
      - finding_id: a fresh unique 64-hex string (NOT a duplicate of any existing entry).
      - surface: "board-autofix-seed"
      - class: "off-scale-radius-shadow-control" (any class is fine; fix_class drives applicability)
      - anchor: the anchor of the element you added in (a), e.g. ".sg-stack" scoped under the seed board (match what exists in the fixture DOM).
      - fix_class: "token"  -> auto_eligible MUST be true (fix-queue-lint (c) recomputes this; token => true).
      - auto_eligible: true
      - priority: "normal"  (single-surface: do NOT set surfaces_affected, and do NOT use "systemic" — fix-queue-lint (d) fails a non-multi-surface systemic entry).
      - measured_px: [12.5]
      - scale_px: [1,2,3,4,5,6,7,8,10,12]   (the 10-entry SPACE scale — resolveTokenRef maps 12 -> var(--sg-space-12) with NO token_family hint; this is why it APPLIES where the 4-entry radius findings refuse).
      - Do NOT set token_family (space resolution does not need it; radius resolution would — but this is space).
    After editing, run scripts/ci/fix-queue-lint.sh and confirm PASS (the seeded entry's derived auto_eligible/priority are consistent; measured_px/scale_px pass through untouched). Also confirm the seed finding does NOT require any admin-render-sha.json cell: board-autofix-seed is intentionally NOT a merge-gated render cell, so it never touches quality-findings-monotonic. (Reconciliation with Task 1: Task 1's board-mg-5/9 cells carry open_findings:0; this seed lives ONLY in fix-queue.json + the fixture DOM, on a surface absent from admin-render-sha.json, and the loop applies-then-reverts it — so the merge tree is net-zero at final committed HEAD and the monotonic gate stays green.)

    (c) The automated <verify> block (below) proves the SC-4 chain by running the REAL loop inside a THROWAWAY git clone of the final committed HEAD, using --skip-render (no browser, no Postgres, API-free) and mirroring the hermetic test's rail-1 perturbation (a one-shot post-commit hook that bumps a render-sha cell so quality-findings-monotonic trips vs PRE_LOOP_SHA). Because it runs in a disposable clone, the loop's fix commit + Revert commit + settled-findings.tsv row + ledger restore all happen in the clone and NEVER touch the real repo history — the executor's working tree stays clean, and the assertion is fully repeatable. The verify asserts, from the clone: a `Revert "autofix` commit in git log; admin-award-ledger.json restored to its pre-loop content; the seeded finding_id present in settled-findings.tsv with disposition=waived; and a reflog with NO force-push / NO reset --hard. This is the automated substance the checker required — no human attestation.
    ORDERING NOTE (load-bearing): load_eligible in admin-autofix-loop.sh sorts systemic-priority findings FIRST, then by finding_id, and caps at --max-fixes. The 12 committed board-mg findings are priority:systemic and refuse (no measured_px); the seed is priority:normal and therefore sorts LAST. So --max-fixes MUST be >= the total eligible count (13+) for the loop to reach and apply the seed — the verify uses --max-fixes 20. Do NOT use --max-fixes 5 (it stops after 5 refusing systemic findings and never reaches the seed → 0 reverts). Verified end-to-end in a real clone: at --max-fixes 20 the seed applies (var(--sg-space-12)), the hook trips rail-1, git revert lands a `Revert "autofix` commit, the ledger is restored, and the finding is settled=waived.

    (d) Update guides/reference/admin-eval-runbook.md:
      - Correct the stale pilot-surface note to name the board-mg-5-*/board-mg-9-* pilot surfaces (replacing "users-index-live, user-show-live").
      - Document the SC-4 chain as AUTONOMOUSLY PROVEN (clone-isolated, API-free) and point to Task 3's automated verify + the hermetic admin-autofix-loop.test.sh; note the board-autofix-seed in-band SPACE finding as the appliable seed.
      - Document the TRUE-live SC-2 paid run as an OPTIONAL, POST-MERGE, operator-only confirmation (NOT a gate): with `export ANTHROPIC_API_KEY=...` and a booted example server at the committed HEAD, run `bash scripts/ci/admin-panel.sh` twice; the 2nd run must report 0 API calls AND `git diff guides/reference/admin-panel-verdicts.json` must be empty. State plainly this is the ONLY step that cannot be automated (paid key + live server) and that its mechanism is already hermetically proven (judge.test.mjs callCount===0) and additionally proven against a real on-disk bundle by judge-cli.test.mjs (Task 2).
      - Restate the JUDGE-CI-01 invariant (neither admin-panel.sh nor admin-autofix-loop.sh is ever a merge gate).
    Record in the SUMMARY: the observed Revert commit sha, the restored-ledger confirmation, and the settled finding_id from the automated clone run.
  </action>
  <acceptance_criteria>
    - design_gallery_live.ex #board-autofix-seed fixture contains a 12.5px SPACE inline-style value (e.g. padding: 12.5px) that fix-apply rewrites to var(--sg-space-12); the pre-existing 13px radius + other clunky defects are unchanged; no board-mg-5/9 or product surface is touched.
    - fix-queue.json contains exactly one appliable board-autofix-seed finding (fix_class:token, auto_eligible:true, priority:normal, measured_px:[12.5], scale_px:[1,2,3,4,5,6,7,8,10,12], no token_family, unique finding_id); fix-queue-lint.sh PASSES.
    - The automated SC-4 clone run produces, IN THE CLONE: a `Revert "autofix` commit; a restored admin-award-ledger.json; the seeded finding in settled-findings.tsv with disposition=waived; a clean reflog (no force-push / reset --hard). The real repo working tree + history are unchanged by the run.
    - admin-eval-runbook.md names the board-mg-5-*/board-mg-9-* pilot surfaces, documents the SC-4 chain as autonomously proven, documents the OPTIONAL operator-only TRUE-live SC-2 paid run, and restates JUDGE-CI-01.
    - quality-findings-monotonic.sh --base <merge-base> still exits 0 (the seed adds no admin-render-sha.json cell; the clone run does not mutate the real ledger).
  </acceptance_criteria>
  <verify>
    <automated>set -e; node -e "const q=require('./guides/reference/fix-queue.json'); const s=q.filter(f=>f.surface==='board-autofix-seed'&&f.fix_class==='token'&&f.auto_eligible===true&&f.priority==='normal'&&Array.isArray(f.measured_px)&&f.measured_px[0]===12.5&&Array.isArray(f.scale_px)&&f.scale_px.length===10&&!f.token_family); if(s.length!==1) throw new Error('expected exactly 1 appliable board-autofix-seed SPACE finding, got '+s.length); console.log('seed finding OK');" && grep -qF '12.5px' test/example/lib/example_web/live/admin/design_gallery_live.ex && bash scripts/ci/fix-queue-lint.sh && MB=$(git merge-base origin/main HEAD 2>/dev/null || git rev-parse HEAD~1) && bash scripts/ci/quality-findings-monotonic.sh --base "$MB" && CLONE=$(mktemp -d) && git clone --quiet --no-hardlinks . "$CLONE/repo" && cp guides/reference/fix-queue.json "$CLONE/repo/guides/reference/fix-queue.json" && cp test/example/lib/example_web/live/admin/design_gallery_live.ex "$CLONE/repo/test/example/lib/example_web/live/admin/design_gallery_live.ex" && ( cd "$CLONE/repo" && git config user.email sc4@autofix.test && git config user.name "SC4 Clone" && git commit -aqm "seed working-tree state for SC-4 clone run" && LEDGER_PRE=$(cat guides/reference/admin-award-ledger.json) && printf '#!/usr/bin/env bash\nS=$(git log -1 --format=%%s)\nif [[ "$S" == autofix* ]] && [[ ! -f .git/hook-fired ]]; then touch .git/hook-fired; node -e "const fs=require(\\"fs\\");const p=\\"guides/reference/admin-render-sha.json\\";const d=JSON.parse(fs.readFileSync(p,\\"utf8\\"));const surf=Object.keys(d.cells)[0];const ck=Object.keys(d.cells[surf])[0];d.cells[surf][ck].open_findings=(d.cells[surf][ck].open_findings||0)+1;fs.writeFileSync(p,JSON.stringify(d,null,2)+\\"\\n\\");"; git add guides/reference/admin-render-sha.json; git commit --amend --no-edit -q || true; fi\n' > .git/hooks/post-commit && chmod +x .git/hooks/post-commit && env -u ANTHROPIC_API_KEY bash scripts/ci/admin-autofix-loop.sh --max-fixes 20 --skip-render >/dev/null 2>&1 || true && git log --oneline | grep -qi 'Revert "autofix' && test "$(cat guides/reference/admin-award-ledger.json)" = "$LEDGER_PRE" && SEED_ID=$(node -e "const q=require('./guides/reference/fix-queue.json'); console.log(q.find(f=>f.surface==='board-autofix-seed'&&f.fix_class==='token').finding_id)") && grep "$SEED_ID" guides/reference/settled-findings.tsv | grep -q waived && ! git reflog --format='%gs' | grep -iE 'force|reset.*hard' && echo "SC-4 clone chain OK" ) && rm -rf "$CLONE" && grep -qF 'board-mg-5' guides/reference/admin-eval-runbook.md && grep -qF 'board-mg-9' guides/reference/admin-eval-runbook.md && grep -qi 'JUDGE-CI-01' guides/reference/admin-eval-runbook.md && ! grep -qE 'pilot surfaces —? *users-index-live' guides/reference/admin-eval-runbook.md</automated>
  </verify>
  <done>An appliable in-band SPACE finding is seeded on board-autofix-seed (fixture DOM + fix-queue.json, lint-clean); the SC-4 apply->rail-trip->revert->waive chain is proven AUTONOMOUSLY inside a throwaway clone (Revert commit + restored ledger + settled(waived) finding + clean reflog) with an automated assertion block and NO human attestation; the real repo history stays clean and the merge gate stays green; the runbook names the board-mg pilot surfaces, records SC-4 as autonomously proven, and documents the OPTIONAL operator-only TRUE-live SC-2 paid run with JUDGE-CI-01 restated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env -> admin-panel.sh | ANTHROPIC_API_KEY gates the paid path; a missing key must fail-safe (no-op exit 0), never fail-open into a gate |
| committed ledgers -> merge gate | admin-render-sha.json feeds quality-findings-monotonic (merge-blocking); a nonzero open_findings on a from-zero cell is a false regression that would break the gate |
| on-disk bundle dir -> judge.mjs CLI | untrusted/empty DOM must not trigger paid API calls (IN-01 refuse) |
| autofix loop -> throwaway clone git history | the SC-4 run writes fix + Revert commits ONLY inside a disposable git clone (never the real repo); must use git revert only (no force-push / reset --hard) and the real working tree must be unchanged after the run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-217-08-GATE | Denial of Service | new board-mg cells trip quality-findings-monotonic (0 -> 197) | high | mitigate | Introduce board-mg cells at open_findings: 0 (panel reads only render_sha256); do NOT run fix-queue-build on this commit; Task 1 verify runs the real monotonic guard vs merge-base |
| T-217-08-KEY | Information Disclosure | ANTHROPIC_API_KEY echoed/logged | high | mitigate | admin-panel.sh no-op message names the var only; no task <verify> needs a key; the paid SC-2 step stays operator-only in the runbook |
| T-217-08-EMPTYDOM | Tampering | judge.mjs CLI fires k paid calls against an empty DOM | medium | mitigate | IN-01 refuse guard reordered BEFORE the SDK import; judge-cli.test.mjs proves the refuse path key-free |
| T-217-08-REVERT | Tampering | SC-4 loop mutates the REAL repo history unsafely | medium | mitigate | Task 3 runs the loop ONLY inside a throwaway `git clone --no-hardlinks` of HEAD; fix/Revert commits + settled row + ledger changes are confined to the clone (deleted after); reflog-clean assertion runs in the clone; git revert --no-edit only (already in admin-autofix-loop.sh); clone made at the final committed HEAD (216-09 SC-5 discipline) |
| T-217-08-SEED | Tampering | seeded board-autofix-seed finding trips the monotonic merge gate or corrupts the queue | medium | mitigate | Seed is fix_class:token (auto_eligible derives clean via fix-queue-lint), single-surface priority:normal, and lives ONLY in fix-queue.json + the fixture DOM on a surface absent from admin-render-sha.json — so it never touches quality-findings-monotonic; Task 3 verify runs fix-queue-lint + the real monotonic guard vs merge-base |
| T-217-08-CI | Elevation of Privilege | panel/loop entering a merge gate | high | mitigate | Only the deterministic judge-cli.test.mjs is added to fast_checks; the SC-4 loop runs only in a throwaway clone during Task 3 execution (never a CI run: step); panel-ci-isolation.test.sh re-asserted in Tasks 1 + 2 |
</threat_model>

<verification>
- admin-render-sha.json has the eight board-mg-5-*/board-mg-9-* surfaces with valid 64-hex render_sha256 and open_findings: 0; pilot cells untouched.
- quality-findings-monotonic.sh --base <merge-base> exits 0 with the new cells present.
- admin-panel.sh PILOT_SURFACES names the board-mg subset; no-key run exits 0; panel-ci-isolation.test.sh passes.
- judge.mjs CLI: SDK import is after the empty-DOM refuse guard (CLI-ordering grep passes).
- judge-cli.test.mjs passes key-free (cache-hit 0 calls, cache-miss k calls, empty-DOM refuse) or skips cleanly when no on-disk bundle; wired into fast_checks; judge.test.mjs still 11/11.
- fix-queue.json has one appliable board-autofix-seed SPACE finding; fix-queue-lint.sh PASSES; design_gallery_live.ex #board-autofix-seed carries a 12.5px SPACE inline-style value.
- SC-4 automated: the loop, run inside a throwaway clone of HEAD (--skip-render, API-free), produces a Revert commit + restored ledger + settled(waived) finding + clean reflog — asserted by Task 3's automated <verify>; the real repo history/tree is unchanged. Observed Revert sha + settled finding_id recorded in SUMMARY.
- Runbook names the board-mg pilot surfaces, records SC-4 as autonomously proven, and documents the OPTIONAL operator-only TRUE-live SC-2 paid step + JUDGE-CI-01.
</verification>

<success_criteria>
The panel and 217 render matrix agree on the board-mg-5/9 surfaces (SC-2 unblocked); the judge.mjs CLI bundle-wiring is confirmed, hardened SDK-free, and proven by a new deterministic key-free self-test in fast_checks (SC-2 shifted maximally left); an appliable in-band SPACE finding is seeded on board-autofix-seed (fixture + queue, lint-clean) so the SC-4 apply->rail-trip->revert->waive chain is proven AUTONOMOUSLY (clone-isolated, API-free) with an automated assertion block — a Revert commit + restored ledger + settled(waived) finding + clean reflog, with the real repo history untouched; the ONLY un-automatable check (the TRUE-live SC-2 paid run) is documented as an OPTIONAL post-merge operator confirmation in the runbook (not a gate), so the plan completes autonomously; committed state stays green (monotonic gate passes, JUDGE-CI-01 preserved).
</success_criteria>

<artifacts>
## Artifacts this phase produces

New:
- scripts/panel/judge-cli.test.mjs — deterministic, ANTHROPIC_API_KEY-free self-test proving the judge.mjs CLI reads a real on-disk board-mg bundle's dom.html + facts.json (via the --output-dir layout) and takes the correct call/skip decision (0 calls on cache hit, k on miss; refuse on empty DOM). Wired into fast_checks.

Modified:
- guides/reference/admin-render-sha.json — adds eight board-mg-5-*/board-mg-9-* surfaces (desktop light+dark cells) with real render_sha256 and open_findings: 0 (gate-safe; panel reads only render_sha256).
- scripts/ci/admin-panel.sh — PILOT_SURFACES repointed from the pilot surfaces to the board-mg-5/9 subset (LOCKED Option 2).
- scripts/panel/judge.mjs — CLI @anthropic-ai/sdk import moved AFTER the empty-DOM refuse guard so the IN-01 refuse decision is provable key-free.
- .github/workflows/ci.yml — adds a "Judge CLI bundle-wiring self-test" step in fast_checks after the existing judge self-test.
- guides/reference/fix-queue.json — adds ONE appliable in-band SPACE finding for surface board-autofix-seed (fix_class:token, measured_px:[12.5], 10-entry scale_px -> var(--sg-space-12), no token_family); fix-queue-lint clean; not a merge-gated render cell.
- test/example/lib/example_web/live/admin/design_gallery_live.ex — adds a 12.5px SPACE inline-style value inside #board-autofix-seed (the exact string fix-apply rewrites to var(--sg-space-12)); pre-existing clunky defects unchanged.
- guides/reference/admin-eval-runbook.md — names the board-mg-5-*/board-mg-9-* pilot surfaces, records the SC-4 chain as autonomously proven (clone-isolated, API-free), and documents the OPTIONAL post-merge operator-only TRUE-live SC-2 paid confirmation as the single un-automatable check; restates JUDGE-CI-01.

Automated verification recorded in SUMMARY (the SC-4 chain runs in a throwaway clone; NO commit lands in the real repo):
- SC-4 apply->revert->waive proven in a disposable clone of final committed HEAD — observed Revert commit sha, restored admin-award-ledger.json, settled (waived) finding_id in settled-findings.tsv, clean reflog; real repo working tree + history unchanged.
</artifacts>

<output>
Create `.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-08-SUMMARY.md` when done
</output>
