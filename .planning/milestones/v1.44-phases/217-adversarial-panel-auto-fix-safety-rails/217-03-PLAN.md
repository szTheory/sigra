---
phase: 217-adversarial-panel-auto-fix-safety-rails
plan: 03
type: execute
wave: 2
depends_on: ["217-01"]
files_modified:
  - scripts/ci/panel-forced-floor-check.mjs
  - scripts/ci/panel-forced-floor-check.test.mjs
  - scripts/ci/panel-ci-isolation.test.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [PANEL-01, PANEL-02]

must_haves:
  truths:
    - "panel-forced-floor-check.mjs asserts the 12-cell grid (4 lenses x 3 questions) is complete for every evaluated cell."
    - "Every non-keep finding's anchor is validated via the shared isStructuralAnchor from scripts/ci/lib/anchor.mjs; empty/vague NONE tokens are rejected."
    - "A CI-isolation self-test proves no workflow run: step invokes admin-panel.sh or admin-autofix-loop.sh, and the 5 required checks exclude the panel."
    - "The new self-tests attach to the fast_checks self-test list; the panel and loop binaries themselves never attach to any CI lane."
  artifacts:
    - scripts/ci/panel-forced-floor-check.mjs
    - scripts/ci/panel-ci-isolation.test.sh
  key_links:
    - "panel-forced-floor-check.mjs imports isStructuralAnchor from ./lib/anchor.mjs (the Plan 01 extraction)."
    - "panel-ci-isolation.test.sh greps .github/workflows/*.yml for admin-panel.sh / admin-autofix-loop.sh run: steps."
  prohibitions:
    - "admin-panel.sh and admin-autofix-loop.sh must NEVER appear as a run: step in any workflow."
    - "panel-forced-floor-check.mjs must read JSON only — never validate markdown (the retired panel-schema-check.sh column-4 hazard is moot)."
    - "No literal that a negative grep asserts absent may appear verbatim in an action body."
---

<objective>
Deliver the two deterministic panel guards that hold the forced-finding floor (SC-1) and prove JUDGE-CI-01 (SC-5): (1) `scripts/ci/panel-forced-floor-check.mjs` — cloned in shape from the retired `panel-schema-check.sh` but reading JSON — that asserts the 12-cell grid is complete, rejects empty/vague NONE tokens, and validates every non-keep anchor via the shared `isStructuralAnchor`; (2) a negative-assertion CI-isolation self-test that greps the workflows and proves the panel + loop are never wired into a merge gate. Both self-tests attach to `fast_checks`.

Purpose: These are the deterministic derivatives that MAY gate merges. The forced-floor check makes vague panel output impossible; the CI-isolation test is the suspenders to the Hammer no-op belt (Pitfall 4).
Output: `panel-forced-floor-check.mjs` (+ `.test`), `panel-ci-isolation.test.sh`, and their `fast_checks` wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-CONTEXT.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-RESEARCH.md

@scripts/ci/panel-schema-check.sh
@scripts/ci/evidence-anchor-check.mjs
@scripts/ci/lib/anchor.mjs
@.github/workflows/ci.yml
@guides/reference/admin-persona-jtbd-rubric.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: panel-forced-floor-check.mjs — 12-cell grid + NONE + anchor validation</name>
  <read_first>scripts/ci/panel-schema-check.sh (shape to clone), scripts/ci/lib/anchor.mjs, guides/reference/admin-persona-jtbd-rubric.md (question keys)</read_first>
  <files>scripts/ci/panel-forced-floor-check.mjs, scripts/ci/panel-forced-floor-check.test.mjs</files>
  <behavior>
    - Test 1: a panel-findings JSON missing one of the 12 cells (4 lenses x 3 questions) fails the check.
    - Test 2: a cell with a `keep` verdict but an empty or vague `none_searched_for` (missing the required literal token phrasing) fails.
    - Test 3: a non-keep finding whose anchor is prose (not structural) fails via the shared anchor validator.
    - Test 4: a complete, well-formed 12-cell grid with valid structural anchors and proper NONE tokens passes.
  </behavior>
  <action>Per D-06: create `scripts/ci/panel-forced-floor-check.mjs`, cloning the frontmatter/floor-validation STRUCTURE of the retired `panel-schema-check.sh` but reading the JSON `panel-findings.json` (never markdown — the column-4 hazard is moot). Enumerate the required 12 cells from the four lens ids (`platform_admin`, `support_investigator`, `org_admin`, `graphic_design`) x the three question keys per lens (persona: `earning_its_place`, `ia_muddy`, `redundant_coherent_surprising`; graphic-design: `salience`, `emphasis_ember`, `composition`). For each cell assert it is EITHER a `verdict != keep` finding carrying a structural `anchor` + `refutation`, OR a `keep` carrying a non-empty `none_searched_for` matching the required literal token shape. Validate every non-keep anchor with `isStructuralAnchor` imported from `./lib/anchor.mjs`. Reject empty/vague NONE tokens. Author `panel-forced-floor-check.test.mjs` per Behavior (grep-hygiene: build the required literal token via string concatenation in the test fixtures so no gate literal is echoed verbatim into a checked-in file).</action>
  <verify>
    <automated>node scripts/ci/panel-forced-floor-check.test.mjs</automated>
  </verify>
  <done>Self-test passes; missing cell, vague NONE, and prose anchor each fail; a valid 12-cell grid passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: panel-ci-isolation.test.sh — negative-assertion JUDGE-CI-01 proof (SC-5)</name>
  <read_first>.github/workflows/ci.yml (the 5 required checks + fast_checks self-test list)</read_first>
  <files>scripts/ci/panel-ci-isolation.test.sh</files>
  <behavior>
    - Test 1: greps every `.github/workflows/*.yml` and asserts NO `run:` line invokes the panel orchestrator or the auto-fix loop orchestrator (assert-absent).
    - Test 2: asserts the 5 required-check job names (Library tests, Example unit smoke, Install smoke, Example HTTP smoke, Example Playwright smoke) do not depend on any panel/loop step.
    - Test 3: a synthetic workflow fixture that DOES wire the panel orchestrator makes the test fail (proves the assertion has teeth).
  </behavior>
  <action>Per Pitfall 4 / SC-5: create `scripts/ci/panel-ci-isolation.test.sh` (hermetic where possible — grep the real `.github/workflows/*.yml` for the assert-absent case, and a mktemp synthetic-workflow fixture for the teeth case). It must fail if any workflow `run:` line invokes the panel orchestrator script or the auto-fix loop orchestrator script, and must confirm the 5 required checks exclude them. To keep grep hygiene, reference the two script basenames via shell variables assembled at runtime rather than embedding the exact literal a future negative grep would match. Do NOT place the assert-absent literals verbatim in prose in a checked-in non-test file.</action>
  <verify>
    <automated>bash scripts/ci/panel-ci-isolation.test.sh</automated>
  </verify>
  <done>Self-test passes on the real workflows (panel/loop absent) and fails on the synthetic wired fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Attach the two new self-tests to the fast_checks self-test list</name>
  <read_first>.github/workflows/ci.yml (existing self-test steps ~L112-140)</read_first>
  <files>.github/workflows/ci.yml</files>
  <action>Add two named steps to the existing `fast_checks` self-test list in `.github/workflows/ci.yml`, mirroring the existing `run: bash scripts/ci/<guard>.test.sh` / `run: node scripts/ci/<guard>.mjs` step pattern: (1) `node scripts/ci/panel-forced-floor-check.test.mjs`, (2) `bash scripts/ci/panel-ci-isolation.test.sh`. Do NOT add `panel-forced-floor-check.mjs` as a merge-gating run over live panel output (there is no committed panel-findings.json to gate on yet at this phase's scope); the SELF-TESTS are what attach. Critically: do NOT add any step that invokes `admin-panel.sh` or `admin-autofix-loop.sh` — the CI-isolation test would (correctly) fail. Confirm the 5 required-check names are byte-unchanged.</action>
  <verify>
    <automated>bash scripts/ci/panel-ci-isolation.test.sh && grep -c 'panel-forced-floor-check.test.mjs\|panel-ci-isolation.test.sh' .github/workflows/ci.yml</automated>
  </verify>
  <done>Both self-tests are named steps in `fast_checks`; the CI-isolation test still passes (confirming the additions did not wire the panel/loop); required-check names unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisory panel output -> merge gate | The forced-floor check is the one deterministic derivative of panel output that may gate; it must reject vague/unanchored findings |
| workflow config -> merge policy | A stray CI wiring of the panel/loop would breach JUDGE-CI-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-217-03-JUDGE | Elevation of Privilege | panel/loop wired into a CI gate | high | mitigate | `panel-ci-isolation.test.sh` grep-asserts absence in every workflow + a teeth fixture; attached to fast_checks so a future wiring fails CI |
| T-217-03-VAGUE | Tampering | vague/unanchored panel findings entering the queue | medium | mitigate | `panel-forced-floor-check.mjs` validates every non-keep anchor via shared `isStructuralAnchor` and rejects empty/vague NONE tokens |
| T-217-03-INJECT | Tampering | anchor text through cheerio | low | mitigate | Reuses the extracted anchor helper (cheerio `$()` only, never eval/shell) |
</threat_model>

<verification>
- `node scripts/ci/panel-forced-floor-check.test.mjs` passes.
- `bash scripts/ci/panel-ci-isolation.test.sh` passes.
- Both self-tests are named steps in `fast_checks`.
- `grep -rn 'admin-panel\|admin-autofix-loop' .github/workflows/*.yml` shows no `run:` invocation.
</verification>

<success_criteria>
The forced-floor check (12-cell grid + NONE + shared-anchor validation) and the CI-isolation negative-assertion test both exist, self-test green, and attach to `fast_checks`; the panel and loop remain unwired from any CI gate.
</success_criteria>

<output>
Create `.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-03-SUMMARY.md` when done
</output>
