---
phase: 217-adversarial-panel-auto-fix-safety-rails
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-graphic-design-lens.md
autonomous: true
requirements: [PANEL-01]

must_haves:
  truths:
    - "A new sibling guides/reference/admin-graphic-design-lens.md exists, NOT appended to the persona rubric."
    - "The lens defines exactly three refutation questions: salience (Q1), emphasis_ember (Q2), composition (Q3)."
    - "Each question maps to a named Sigra pillar with a brand-v2 / admin-ui-principles citation; every finding references a named pillar."
    - "The lens uses class = graphic_design:<key>, the keep|tighten|kill scale, and the literal NONE — searched for: token — same forced-floor contract as the persona rubric."
  artifacts:
    - guides/reference/admin-graphic-design-lens.md
  key_links:
    - "The three question keys (salience, emphasis_ember, composition) match the graphic_design cells enforced by panel-forced-floor-check.mjs (Plan 03)."
    - "Q1 defers to probe #9 above-fold/target-size geometry; Q2 defers to probe #4 ember structural allowlist; the lens owns only the perceptual half."
  prohibitions:
    - "The doc must obey the column-4 integer prohibition (no bare 0/1/2 in a table column that the ledger awk guard scans)."
    - "The lens must NOT own misalignment, radius/shadow/control-height, focus-ring, card-in-card, motion, or responsive reflow — those stay deterministic."
    - "Brand values must be v2 only: ember #c2410c (light) / #fdba74 (dark), Space Grotesk, Core Rails; ignore pre-v2 brand info."
---

<objective>
Author the new graphic-design lens (D-16/D-17/D-18): a sibling `guides/reference/admin-graphic-design-lens.md` that plugs into the same 4-lens panel via `class = "graphic_design:<key>"` with the same forced-floor + `keep|tighten|kill` + `NONE — searched for:` contract as the persona rubric. Three perceptual refutation questions no probe can own — `salience`, `emphasis_ember`, `composition` — each grounded in a named Sigra pillar and a brand-v2 citation, each deferring the measurable half to the probe/proxy that owns it.

Purpose: The graphic-design lens is the single biggest lever against generic-AI design critique — it must speak Sigra's language (7 named pillars, brand-v2 ember/Space Grotesk/Core Rails), not Nielsen boilerplate. This can be authored in parallel with the guards.
Output: `guides/reference/admin-graphic-design-lens.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-CONTEXT.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-RESEARCH.md

@guides/reference/admin-persona-jtbd-rubric.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-fractal-scorecard.md
@brandbook/brand-book.md
@brandbook/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author admin-graphic-design-lens.md (3 perceptual questions, pillar-grounded)</name>
  <read_first>guides/reference/admin-persona-jtbd-rubric.md (the authoring template: verdict scale, forced-finding floor, NONE token, output schema), guides/reference/admin-ui-principles.md, brandbook/brand-book.md, brandbook/tokens.css</read_first>
  <files>guides/reference/admin-graphic-design-lens.md</files>
  <action>Per D-16/D-17/D-18: create the sibling `guides/reference/admin-graphic-design-lens.md` (do NOT append to the persona rubric — mirror 216 D-19 sibling-file discipline). Structure it after `admin-persona-jtbd-rubric.md`: an IMPORTANT DISAMBIGUATION (this is a perceptual/visual-quality lens, judged on the rendered PNGs, distinct from the operator-JTBD persona lenses), the shared verdict scale (`keep`/`tighten`/`kill`, worst-verdict floor), the forced-finding floor standing instruction (adversarial framing; every cell holds a structural anchor OR the literal NONE token), and a machine-rollup-able output schema section using `class = graphic_design:<key>`.
  Define exactly three refutation questions:
  - Q1 `salience` — "does the eye land on the wrong thing first?" Perceived first-fixation dominance of the primary action. Defers the above-fold + target-size GEOMETRY to probe #9; owns only the perceptual salience judgment (picks up probe #9's deferred salience call). Named pillar: hierarchy/salience.
  - Q2 `emphasis_ember` — "is emphasis (esp. ember) earning its meaning, or decorating?" Semantic ember-as-ownership-boundary vs probe #4's structural allowlist; also flags UNDER-emphasis of meaning-bearing elements. Named pillars: restraint + ember-as-boundary; cite brand-v2 ember #c2410c (light) / #fdba74 (dark), Space Grotesk, Core Rails.
  - Q3 `composition` — "does grouping / type hierarchy / balance read coherently in BOTH themes?" Gestalt grouping, perceived type-hierarchy descent, compositional balance, and light-vs-dark EMPHASIS parity. Defers measured spacing/rhythm to probes/D-16 proxies; axe owns contrast correctness. Named pillars: consistency ("same job -> same component") + typographic coherence + dark/light emphasis parity.
  Map the lens to the 7 named Sigra pillars, each tied to a question and a brand-book v2 / `admin-ui-principles` citation, so every finding references a named pillar. Two-field finding shape per D-17: `observation` (perceptual prose) + `anchor` (structural selector from the existing `data-testid`/`sg-*` BEM vocabulary) + `evidence_cell` (which PNG). Note that this is the ONLY lens requiring `screenshot.png` (Q3 needs light AND dark PNGs). State the A3 award extension: A3 = all 4 lenses read `clean`. Explicitly list what the lens does NOT own (misalignment, radius/shadow/control-height, focus-ring, card-in-card, motion, responsive reflow — deterministic). Obey the column-4 integer prohibition (no bare 0/1/2 in any awk-scanned table column; use keep/tighten/kill vocabulary).</action>
  <verify>
    <automated>grep -q 'graphic_design:salience' guides/reference/admin-graphic-design-lens.md && grep -q 'graphic_design:emphasis_ember' guides/reference/admin-graphic-design-lens.md && grep -q 'graphic_design:composition' guides/reference/admin-graphic-design-lens.md && grep -q '#c2410c' guides/reference/admin-graphic-design-lens.md && grep -q '#fdba74' guides/reference/admin-graphic-design-lens.md</automated>
  </verify>
  <done>The doc exists with the three `graphic_design:<key>` classes, brand-v2 ember values, named-pillar grounding per question, the shared forced-floor contract, and the explicit not-owned list; no bare integer appears in an awk-scanned table column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored doc -> ledger awk guard | The lens markdown must not introduce a bare integer that false-matches the quality-ledger monotonic awk parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-217-04-COL4 | Tampering | ledger awk guard false-match | low | mitigate | Doc obeys the column-4 integer prohibition (keep/tighten/kill vocabulary only); verified by the existing quality-ledger-monotonic self-test on merge |
| T-217-04-BRAND | Spoofing | stale pre-v2 brand values | low | mitigate | Grep-verify brand-v2 ember values (#c2410c / #fdba74) present; pre-v2 info explicitly ignored |
</threat_model>

<verification>
- The three `graphic_design:<key>` classes are present and match the graphic_design cells the forced-floor check enforces.
- Brand-v2 ember values (#c2410c, #fdba74) are cited.
- No bare integer appears in an awk-scanned table column (quality-ledger-monotonic self-test stays green on merge).
</verification>

<success_criteria>
The graphic-design lens doc exists as a sibling, defines the three perceptual questions grounded in named Sigra pillars + brand-v2, uses the shared forced-floor contract, and defers all measurable/deterministic concerns to the probes.
</success_criteria>

<output>
Create `.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-04-SUMMARY.md` when done
</output>
