---
phase: 219-baseline-recapture-canary-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/components/core_components.ex
autonomous: true
requirements: [RECAP-01]
must_haves:
  truths:
    - "mix compile --warnings-as-errors succeeds inside test/example (no 'undefined attribute style for component icon/1')"
    - "The example icon/1 accepts and forwards arbitrary global attributes (style, data-*, aria-*)"
  artifacts:
    - "test/example/lib/example_web/components/core_components.ex with attr :rest, :global on icon/1 and {@rest} spread"
  key_links:
    - "icon/1 {@rest} spread consumes the inline style= passed at mfa_settings_live.ex:250 and :328"
---

<objective>
Fix the one hard compile prerequisite the Phase 218 elevation wave left behind (D-02): the
example `<.icon style=...>` call sites fail `mix compile --warnings-as-errors` because the
example's `icon/1` declares only `:name` and `:class`. This blocker kills EVERY recapture,
smoke, and parity job at the compile step before Playwright ever boots, so it must land first
on the Phase 219 working branch (branched off `main` per D-01 — see note below).

Purpose: Unblock all downstream in-CI recapture/smoke/parity jobs (they all run `mix compile
--warnings-as-errors` in `test/example` before booting).
Output: A durable, recurrence-proof `:global` attribute on the example `icon/1`.

Branch-base note (D-01): Phase 219 branches fresh off `main` (HEAD subsumes the stale wave
branch); PR #70 (`elevate-03-wave-v144-pr`) is closed as subsumed in plan 219-03. Do NOT rebase
or reconcile PR #70.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-baseline-recapture-canary-reconciliation/219-CONTEXT.md
@test/example/lib/example_web/components/core_components.ex
@test/example/lib/example_web/live/mfa_settings_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add :global attr + {@rest} spread to the example icon/1 (D-02)</name>
  <files>test/example/lib/example_web/components/core_components.ex</files>
  <read_first>
    - test/example/lib/example_web/components/core_components.ex:444-451 — the example icon/1
      declaring only `attr :name` and `attr :class`, rendering `<span class={[@name, @class]} />`.
    - test/example/lib/example_web/live/mfa_settings_live.ex:250 and :328 — the two `<.icon
      name=... class=... style="color:var(--vt-color-...)" />` call sites that trigger the
      compile failure.
  </read_first>
  <action>
    In the example core_components.ex icon/1 component (lines 444-451), add a global-attributes
    declaration `attr :rest, :global` alongside the existing `attr :name` and `attr :class`, and
    spread it onto the rendered element by adding `{@rest}` to the `<span class={[@name, @class]}
    />` node so inline `style` (and any future data-*/aria-*) attributes pass through. This is the
    ONE durable fix site (recurrence-proof) per D-02 — the `:global` approach is preferred over
    dropping the inline `style` from the call sites. EXAMPLE-ONLY: do NOT touch
    priv/templates/sigra.install/** and do NOT trigger a golden rebless (WR-01/02 are a separate
    /gsd-code-review 218 --fix concern, out of scope here). Do not modify the two mfa_settings_live
    call sites; the fix is entirely in icon/1.
  </action>
  <verify>
    <automated>cd test/example &amp;&amp; MIX_ENV=dev mix compile --warnings-as-errors 2>&amp;1 | tail -5; test ${PIPESTATUS[0]:-0} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` in test/example exits 0 (previously failed with
      "undefined attribute \"style\" for component ExampleWeb.CoreComponents.icon/1").
    - icon/1 declares `attr :rest, :global` and renders `{@rest}` on the span.
    - No changes under priv/templates/**; no golden fixture changes; mfa_settings_live.ex unchanged.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd test/example && MIX_ENV=dev mix compile --warnings-as-errors` exits 0.
- `git diff --name-only` shows ONLY test/example/lib/example_web/components/core_components.ex.
</verification>

<success_criteria>
The example host app compiles cleanly under warnings-as-errors, unblocking every in-CI
recapture/smoke/parity job for the rest of the phase.
</success_criteria>

<artifacts_produced>
- `attr :rest, :global` + `{@rest}` spread added to `ExampleWeb.CoreComponents.icon/1`
  (test/example/lib/example_web/components/core_components.ex).
</artifacts_produced>

<output>
Create `.planning/phases/219-baseline-recapture-canary-reconciliation/219-01-SUMMARY.md` when done.
</output>
