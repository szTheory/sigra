---
phase: 219-baseline-recapture-canary-reconciliation
plan: 05
type: execute
wave: 4
depends_on: [219-03]
files_modified: []
autonomous: true
requirements: [RECAP-01]
must_haves:
  truths:
    - "install-golden byte-diff is green (install_golden_contract lane succeeds; no rebless needed)"
    - "generated-host acceptance-smoke runtime render is green (admin-acceptance-smoke.sh --test all passes on a non-PR event run where the job actually executes)"
    - "The example-only icon fix leaves generated-host parity unaffected"
  artifacts: []
  key_links:
    - "install_golden_contract + generated_admin_playwright_smoke both conclude success on the authoritative CI run"
---

<objective>
Confirm generated-host parity stays green after the wave + recapture (D-08, SC-3). This is
essentially confirm-only: install-golden is already consistent on main (the wave touched no
templates; commit ec4dfd12 re-blessed it → no rebless), and acceptance-smoke consumes its own
generated app — not the 115 example baselines — so recapture does not touch it. The example-only
icon fix (219-01) leaves generated-host unaffected.

Purpose: SC-3 — prove both parity halves (install-golden byte-diff AND acceptance-smoke runtime
render) pass against the post-wave codebase.
Output: Recorded green conclusions for both parity lanes on the authoritative CI run.

IMPORTANT gating nuance (executor must honor): `generated_admin_playwright_smoke` carries a stale
`if:` at ci.yml:1320 (`github.event_name != 'pull_request' || github.head_ref ==
'ship/v1.42-ci-gate-remediation'`). On a plain 219→main PR the job is SKIPPED, and ci-gate treats
`skipped` as pass (ci.yml:1479). So the AUTHORITATIVE acceptance-smoke proof is a NON-PR event run
where the job actually executes — the workflow_dispatch recapture run from 219-03 (event =
workflow_dispatch, so the `if:` lets it run). Do NOT accept a `skipped` conclusion as the SC-3
proof; require an actual `success`. Out of scope: modifying the ci.yml:1320 condition (D-08 is
confirm-only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-baseline-recapture-canary-reconciliation/219-CONTEXT.md
@.github/workflows/ci.yml
@scripts/ci/admin-acceptance-smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm install-golden byte-diff is green (SC-3, D-08)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — `install_golden_contract` lane (in ci-gate.needs at :1442); it
      runs the byte-diff of `mix sigra.install` output against the committed golden fixture with the
      phx_new 1.8.8 pin.
    - CONTEXT.md D-08 — install-golden is consistent on main (ec4dfd12 already re-blessed it); no
      rebless this phase; the icon fix is example-only (priv/templates/** untouched).
    - CLAUDE.md — install golden tests require the phx_new 1.8.8 archive locally to match CI.
  </read_first>
  <action>
    Confirm the install-golden byte-diff is green on the Phase 219 branch's CI run: observe the
    `install_golden_contract` lane conclude `success` (`gh run view <run-id> --json jobs` and check
    that job). No rebless — if it reds, STOP and report (a red here would mean the wave/icon fix
    unexpectedly altered generated output, contradicting D-08; do not silently rebless). Optionally,
    if the phx_new 1.8.8 archive is installed locally, corroborate with the local install-golden
    test, but the CI lane conclusion is authoritative.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; gh run list --branch "$(git branch --show-current)" --workflow=CI --limit 1 --json databaseId | python3 -c "import json,sys,subprocess; rid=json.load(sys.stdin)[0]['databaseId']; jobs=json.loads(subprocess.check_output(['gh','run','view',str(rid),'--json','jobs']))['jobs']; g=[j for j in jobs if 'golden' in j['name'].lower()]; assert g and all(j['conclusion']=='success' for j in g), g; print('install_golden_contract success')"</automated>
  </verify>
  <acceptance_criteria>
    - The `install_golden_contract` lane concludes `success` on the Phase 219 branch CI run.
    - No golden fixture was re-blessed this phase.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Confirm generated-host acceptance-smoke runtime render is green (SC-3, D-08)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:1315-1369 — `generated_admin_playwright_smoke` → `scripts/ci/admin-acceptance-smoke.sh
      --test all`; note the stale `if:` at :1320 that skips it on a plain PR.
    - .github/workflows/ci.yml:1449, :1462, :1475-1481 — ci-gate consumes its result and treats
      `skipped` as pass; hence a plain-PR `skipped` is NOT a valid SC-3 proof.
    - scripts/ci/admin-acceptance-smoke.sh — the generated-host runtime parity target (scaffolds
      phx.new + sigra.install + boot + Playwright; consumes its OWN generated app, not the 115
      example baselines).
  </read_first>
  <action>
    Confirm acceptance-smoke actually EXECUTES and passes on a non-PR event run — the
    workflow_dispatch recapture run from 219-03 (or a fresh `gh workflow run "CI" --ref
    <phase-219-branch>` dispatch if needed). Observe `generated_admin_playwright_smoke` conclude
    `success` (NOT `skipped`) via `gh run view <dispatch-run-id> --json jobs`. This is the
    authoritative SC-3 runtime-parity proof (D-08). Do NOT modify the ci.yml:1320 condition (out of
    scope, confirm-only). If it reds, STOP and report.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; rid=$(gh run list --workflow=CI --event=workflow_dispatch --limit 1 --json databaseId | python3 -c "import json,sys; print(json.load(sys.stdin)[0]['databaseId'])") &amp;&amp; gh run view "$rid" --json jobs | python3 -c "import json,sys; jobs=json.load(sys.stdin)['jobs']; g=[j for j in jobs if 'generated admin playwright' in j['name'].lower()]; assert g and all(j['conclusion']=='success' for j in g), g; print('generated_admin_playwright_smoke success on dispatch run')"</automated>
  </verify>
  <acceptance_criteria>
    - `generated_admin_playwright_smoke` concludes `success` (executed, not skipped) on a non-PR
      event run (the workflow_dispatch recapture run).
    - The example-only icon fix did not perturb generated-host parity.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `install_golden_contract` lane concludes `success` on the Phase 219 branch CI run (no rebless).
- `generated_admin_playwright_smoke` concludes `success` (not skipped) on the workflow_dispatch run.
</verification>

<success_criteria>
SC-3: generated-host parity is green — install-golden byte-diff AND the acceptance-smoke runtime
render both pass against the post-wave codebase.
</success_criteria>

<artifacts_produced>
- Recorded green conclusions for `install_golden_contract` and `generated_admin_playwright_smoke`
  (no source/template artifacts produced — confirm-only, D-08).
</artifacts_produced>

<output>
Create `.planning/phases/219-baseline-recapture-canary-reconciliation/219-05-SUMMARY.md` when done.
</output>
