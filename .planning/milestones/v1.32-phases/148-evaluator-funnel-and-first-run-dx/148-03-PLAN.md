---
phase: 148-evaluator-funnel-and-first-run-dx
plan: 03
type: execute
wave: 2
depends_on:
  - 148-01
  - 148-02
files_modified:
  - guides/introduction/troubleshooting-install.md
  - test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-02
  - ADOPT-04
must_haves:
  truths:
    - "A developer can run `mix sigra.doctor` immediately after install and interpret success, available, missing, and misconfigured outcomes."
    - "The evaluator funnel cannot silently drift because the cross-surface doc contract is machine-checked."
  artifacts:
    - path: "guides/introduction/troubleshooting-install.md"
      provides: "canonical first-run doctor section with exact status and exit semantics"
      contains: "mix sigra.doctor"
    - path: "test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs"
      provides: "Nyquist-style docs contract assertions for routing, persona truth, proof language, and doctor wording"
      contains: "defmodule Sigra.Planning.Phase148EvaluatorFunnelAndFirstRunDxTest"
  key_links:
    - from: "guides/introduction/troubleshooting-install.md"
      to: "lib/mix/tasks/sigra.doctor.ex"
      via: "copied status labels and verdict lines"
      pattern: "OK: all configured features are properly wired."
    - from: "test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs"
      to: "README.md"
      via: "routing assertions"
      pattern: "Pick your lane"
    - from: "test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs"
      to: "guides/introduction/demo-showcase.md"
      via: "persona, screenshot, and limitation assertions"
      pattern: "demo-credentials-demo-showcase-chromium.png"
---

<objective>
Make `mix sigra.doctor` the canonical first-run verification surface and freeze the unified funnel with a fast structural test.

Purpose: satisfy D-07 and D-08 with exact status/exit semantics, then lock D-01 through D-06 against regression using a targeted Phase 148 docs-contract test.
Output: `guides/introduction/troubleshooting-install.md` with first-run doctor examples and `test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs` covering routing, persona proof, limitation language, and doctor wording.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/148-evaluator-funnel-and-first-run-dx/148-CONTEXT.md
@.planning/phases/148-evaluator-funnel-and-first-run-dx/148-RESEARCH.md
@.planning/phases/148-evaluator-funnel-and-first-run-dx/148-VALIDATION.md
@guides/introduction/troubleshooting-install.md
@lib/mix/tasks/sigra.doctor.ex
@lib/sigra/doctor.ex
@guides/recipes/deployment.md
@test/sigra/planning/phase_147_upgrade_migration_lanes_test.exs
@README.md
@mix.exs
@doc/llms.txt
@guides/introduction/demo-showcase.md
@test/example/README.md

<interfaces>
From `lib/mix/tasks/sigra.doctor.ex`:
- Visible states are rendered as `[ ] missing`, `[~] available`, `[✓] loaded`, and `[!] misconfigured`.
- Exit verdict lines are `OK: all configured features are properly wired.` and `ERROR: misconfigured features detected (see above). Fix the issues above before deploying.`

From `guides/recipes/deployment.md`:
- Exit contract text is already canonical: exit `0` means wired or unconfigured; exit `1` means configured-but-broken.

From `test/sigra/planning/phase_147_upgrade_migration_lanes_test.exs`:
- Use the same `root/0` + `read!/1` helper pattern and explicit cross-file assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical first-run doctor guidance with exact status and exit semantics</name>
  <files>guides/introduction/troubleshooting-install.md</files>
  <read_first>
guides/introduction/troubleshooting-install.md
.planning/phases/148-evaluator-funnel-and-first-run-dx/148-CONTEXT.md
lib/mix/tasks/sigra.doctor.ex
lib/sigra/doctor.ex
guides/recipes/deployment.md
  </read_first>
  <action>Add a new first-run section to `guides/introduction/troubleshooting-install.md` that makes `mix sigra.doctor` and `mix sigra.doctor --quiet` the immediate post-install verification commands per D-07. Reuse the exact CLI vocabulary from `Mix.Tasks.Sigra.Doctor`: explain `[ ] missing`, `[~] available`, `[✓] loaded`, and `[!] misconfigured`; quote the existing exit contract as exit `0` for wired or unconfigured features and exit `1` for configured-but-broken wiring; and include one success example that ends with `OK: all configured features are properly wired.` plus one failure example that ends with `ERROR: misconfigured features detected (see above). Fix the issues above before deploying.` Use existing doctor problem space for the failure example, such as configured OAuth without Assent or async email without supervised Oban/Swoosh, and keep absent optional deps explicitly non-fatal.</action>
  <acceptance_criteria>
- `guides/introduction/troubleshooting-install.md` contains both `mix sigra.doctor` and `mix sigra.doctor --quiet`.
- The page contains all four state labels: `[ ] missing`, `[~] available`, `[✓] loaded`, and `[!] misconfigured`.
- The page contains both exact verdict lines `OK: all configured features are properly wired.` and `ERROR: misconfigured features detected (see above). Fix the issues above before deploying.`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "mix sigra.doctor|mix sigra.doctor --quiet|\\[ \\] missing|\\[~\\] available|\\[✓\\] loaded|\\[!\\] misconfigured|OK: all configured features are properly wired\\.|ERROR: misconfigured features detected \\(see above\\)\\. Fix the issues above before deploying\\.|exit 0|exit 1" guides/introduction/troubleshooting-install.md</automated>
  </verify>
  <done>Troubleshooting now gives installers an exact first-run verification command, status legend, and success/failure interpretation backed by the real mix task.</done>
</task>

<task type="auto">
  <name>Task 2: Add a Phase 148 docs-contract test that locks routing, persona proof, and doctor wording</name>
  <files>test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs</files>
  <read_first>
test/sigra/planning/phase_147_upgrade_migration_lanes_test.exs
README.md
mix.exs
doc/llms.txt
guides/introduction/demo-showcase.md
test/example/README.md
guides/introduction/troubleshooting-install.md
.planning/phases/148-evaluator-funnel-and-first-run-dx/148-CONTEXT.md
  </read_first>
  <action>Create `test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs` using the Phase 147 test helper structure. Add one test for Plan `148-01` that asserts the README lane names, the `demo-showcase` route, `mix.exs` `main: "demo-showcase"`, and the `doc/llms.txt` demo/doctor entries. Add one test for Plan `148-02` that asserts the exact demo command block, the six persona emails, the four screenshot asset filenames, and explicit non-certification language in `guides/introduction/demo-showcase.md` plus the canonical showcase back-link in `test/example/README.md`. Add one test for Plan `148-03` that asserts the troubleshooting page contains the exact doctor status labels, verdict strings, and exit contract. Keep assertions precise enough that stale headings or comments do not satisfy them.</action>
  <acceptance_criteria>
- The new file defines `Sigra.Planning.Phase148EvaluatorFunnelAndFirstRunDxTest`.
- The file contains three tests covering Plan `148-01`, Plan `148-02`, and Plan `148-03`.
- The assertions mention `main: "demo-showcase"`, at least one locked screenshot filename, at least one persona email, and both doctor verdict lines.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs -x</automated>
  </verify>
  <done>The evaluator funnel is protected by a fast regression test that fails if routing, persona truth, screenshot proof boundaries, or doctor wording drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI diagnostic contract → public troubleshooting docs | Readers trust the docs to explain real `mix sigra.doctor` behavior. |
| docs contract test → future doc edits | Structural assertions gate later changes to public routing and proof wording. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-07 | Tampering | `guides/introduction/troubleshooting-install.md` | mitigate | Copy the exact status labels, verdict strings, and exit contract from `Mix.Tasks.Sigra.Doctor` / `guides/recipes/deployment.md` instead of paraphrasing them into new vocabulary. |
| T-148-08 | Denial of Service | first-run troubleshooting guidance | mitigate | Explicitly distinguish `missing` / `available` from `misconfigured` so adopters do not treat absent optional deps as hard failures and abandon a valid installation path. |
| T-148-09 | Repudiation | Phase 148 public-doc contract | mitigate | Add `phase_148_evaluator_funnel_and_first_run_dx_test.exs` so future edits cannot quietly break the canonical route, persona proof, screenshot boundary, or doctor semantics. |
</threat_model>

<verification>
- `mix test test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs -x`
- `mix docs --warnings-as-errors`
- Human-check: from `test/example`, run `mix setup && mix phx.server`, open `http://localhost:4000/demo/credentials`, log in as `alice@demo.sigra.dev`, and record whether the documented first path reaches a meaningful auth flow in ten minutes or less.
</verification>

<success_criteria>
- `mix sigra.doctor` is documented as the immediate first-run verification command with exact status and exit semantics.
- A targeted test locks the combined README/HexDocs/AI/demo/example/troubleshooting contract.
- Phase execution leaves a clear manual stopwatch trail for the 10-minute evaluator-path claim.
</success_criteria>

<output>
Create `.planning/phases/148-evaluator-funnel-and-first-run-dx/148-03-SUMMARY.md` when done
</output>
