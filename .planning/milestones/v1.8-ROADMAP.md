# Milestone v1.8: Adopter polish (diminishing returns)

**Status:** SHIPPED 2026-04-23  
**Phases:** 63–65  
**Total plans (on-disk):** 0 — all three phases satisfied via **shipped guides** + **`mix.exs`** ExDoc wiring (no discrete `.planning/phases/063-*` … `065-*` directories; traceability in archived [`v1.8-REQUIREMENTS.md`](v1.8-REQUIREMENTS.md)).

## Overview

Reduce remaining **first-week** friction for adopters with **documentation and light recipe polish** — **v1.8** upgrade entry, **cross-links** among install / walkthrough / troubleshooting / upgrade docs, and **companion-oauth-provider** clarity — without reopening **SEED-002** sweeps, **SEED-001** matrices, or **`sigra_lockspire`** glue.

## Phases (archived table)

| # | Phase | Goal | Requirements | Success criteria |
|---|-------|------|--------------|------------------|
| **63** | **Upgrade doc — v1.8** | Adopters and maintainers have a single honest **v1.8** upgrade entry that does not contradict **Hex SemVer** or **v1.7** maintainer context. | ADOPT-04 | **`guides/introduction/upgrading-to-v1.8.md`** exists; documents planning **v1.8** vs **`CHANGELOG.md`**; links **v1.7** upgrade guide for Nyquist/OA/CI; file appears in **`mix.exs`** `docs` extras immediately after **`upgrading-to-v1.7.md`**. |
| **64** | **Cross-link adoption path** | First-week readers can move between install, walkthrough, troubleshooting, and upgrade docs without hunting. | ADOPT-05 | **`getting-started.md`** faster path lists **v1.8** upgrade; **`first-hour.md`** reading map includes troubleshooting + both **v1.7** and **v1.8** upgrade links; **`troubleshooting-install.md`** has **“Upgrading between Sigra versions”** with **CHANGELOG** + both upgrade pages. |
| **65** | **Companion recipe polish** | Embedded-provider recipe states prerequisites and anti-patterns clearly. | INTG-02 | **`companion-oauth-provider.md`**: prerequisite block (Sigra login/session green before **AccountResolver**); **when not to use** includes explicit **B2C-only / no third-party API clients** line; **See also** includes **`upgrading-to-v1.8.html`**. |

**Coverage:** 3 requirements → 3 phases. Phase numbering continues from **v1.7** (last phase **62**).

### Success criteria (milestone)

1. A maintainer can find **v1.8** planning vs **Hex SemVer** truth in one upgrade doc with a clear pointer back to **v1.7** post–Nyquist/OA context.
2. **Getting started → first hour → troubleshooting → upgrades** reads as one navigation mesh, not isolated pages.
3. The **companion OAuth provider** recipe states prerequisites, explicit non-goals, and links the **v1.8** upgrade page for maintainer-facing navigation.

---

## Milestone summary

**Key decisions**

- **Diminishing returns:** Ship doc-only polish without expanding **SEED-002** / **SEED-001** or mandatory companion glue (see `.planning/decisions/001-defer-sigra-lockspire-glue-package.md`).

**Issues resolved**

- Missing **v1.8** upgrade narrative and ExDoc ordering after **v1.7** upgrade guide.
- Dead-end risk between adoption entry paths and upgrade / troubleshooting docs.
- Companion recipe ambiguity on when **AccountResolver** is appropriate vs **B2C-only** misuse.

**Technical debt / follow-ups**

- **`gsd-sdk query milestone.complete`** still fails (`version required for phases archive`); manual archival (v1.3–**v1.8** pattern).
- **No `063-*` / `064-*` / `065-*` phase directories** — execution history is prose + requirements traceability; optional retro packs if stricter per-phase disk history is desired later.

---

_For current roadmap, see [.planning/ROADMAP.md](../ROADMAP.md)._
