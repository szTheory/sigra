# v1.44 ADMIN-UX-RATCHET — Milestone Archive

**Status:** ✅ SHIPPED
**Phases:** 216–220 (5 phases, 36 plans)
**Shipped:** 2026-07-10 · terminal PR #73 merged to `origin/main` as `c0595e09` (full-branch merge-commit, `.planning/` included)
**Milestone audit:** Phase 220 terminal-ratification served as the audit — verification PASSED 4/4 must-haves; all 5 required CI checks + `fast_checks` + `ci-gate` green on the merged terminal PR.

**Milestone goal:** Automate the evaluation and forward-only ratcheting of admin/operator UI/UX quality (adversarial LLM persona/JTBD + graphic-design panels, deterministic-first with LLM-residual) so award-grade iteration is fast and only-moves-forward — needing a human only for a minimal final sign-off — then apply the harness in one broad wave to elevate every admin surface. **Invariant held throughout:** the merge-blocking forward-only signal stays 100% deterministic; the LLM panel is an issue-finder + judge-of-taste ONLY — never a CI gate, never flips a ledger cell.

## Phases

- [x] **Phase 216: Harness Foundation + Award Gradient** — render substrate, evidence-integrity + stale-render guards, deterministic visual probes, award sub-score ledger extension + verify-then-climb, end-to-end on 2 pilot surfaces (9/9) — completed 2026-07-04
- [x] **Phase 217: Adversarial Panel + Auto-Fix Safety Rails** — 4-lens LLM panel (3 persona/JTBD + 1 graphic-design), k=3 consensus, settled-findings suppression, findings-count-monotonic guard, fix queue, safe-class auto-apply with per-fix auto-revert (8/8) — completed 2026-07-04
- [x] **Phase 218: Elevation Wave + Nit Cleanup** — full loop across all 8 admin surfaces + L1/L2 component fractal; verify-then-climb each; fold in UI-01 (demo-DX nits) + UI-02 (Tasklane rebrand residuals); batched reviewable PR (10/10) — completed 2026-07-09
- [x] **Phase 219: Baseline Recapture + Canary Reconciliation** — ~115 PNG baselines recaptured in-CI (ubuntu), allowlists reset to empty steady-state, snapshot-canary + generated-host parity green (5/5) — completed 2026-07-09
- [x] **Phase 220: Terminal Ratification** — award sub-score cells locked forward under monotonic guard, harness runbook committed, milestone ships via PR gated on 5 required CI checks; LLM panel advisory/off-CI throughout (4/4) — completed 2026-07-10

## Phase Details

### Phase 216: Harness Foundation + Award Gradient

**Goal**: A single near-command renders every admin surface into tamper-proof evidence bundles, deterministic visual probes run and flag defects automatically, and the quality ledger gains a finer-grained award sub-score — all proven end-to-end on two pilot surfaces.
**Requirements**: HARNESS-01, HARNESS-02, HARNESS-03, RATCHET-01, RATCHET-02
**Plans**: 9/9 complete (incl. gap plans 216-08 board-scoped probes, 216-09 live end-to-end proof / SC-5)

### Phase 217: Adversarial Panel + Auto-Fix Safety Rails

**Goal**: The 4-lens LLM panel evaluates deterministically-clean surfaces under a forced-finding floor with k=3 consensus, deduplicates findings into a stable fix queue, and auto-applies only provably-safe fix classes with per-fix auto-revert on regression — all proven by an injected-regression test.
**Requirements**: PANEL-01, PANEL-02, AUTOFIX-01, AUTOFIX-02
**Plans**: 8/8 complete (7 + gap-closure 217-08: align panel + render matrix on board-mg-5/9, hardened judge.mjs CLI + key-free judge-cli.test.mjs, live SC-4 loop). Invariant JUDGE-CI-01 preserved — panel wired into NO required-check job.

### Phase 218: Elevation Wave + Nit Cleanup

**Goal**: Every admin surface and the L1/L2 component fractal runs through the full harness loop, existing Tier-2 claims re-verified and award sub-scores raised where earned, UI-01 and UI-02 nits folded in, landed as a single reviewable PR.
**Requirements**: ELEVATE-01, ELEVATE-02, ELEVATE-03
**Plans**: 10/10 complete (6 + gap-closure 218-07…10: deterministic systemic-rep selection, probe-ids drift wiring, demo LiveView crash fixes WR-01/02/03, up.sh reaper dual proxy-host union).

### Phase 219: Baseline Recapture + Canary Reconciliation

**Goal**: After the elevation wave, all ~115 committed PNG baselines are recaptured in-CI (ubuntu), allowlists reset to empty steady-state, snapshot-canary drift guard + generated-host parity green.
**Requirements**: RECAP-01
**Plans**: 5/5 complete. 115 baselines recaptured amd64-native in-CI (PASS 8/8); only the `impersonation-banner` canary drifted (reconciled at ship time via quarantine PR #72); a delete-rebirth ordering bug in the recapture path was found + fixed. Branch left unmerged (shipped in v1.44 terminal PR).

### Phase 220: Terminal Ratification

**Goal**: The award sub-score cells are locked forward under the monotonic guard, a harness runbook is committed, and the milestone ships via a PR gated on the five required CI checks — with the LLM panel advisory and off-CI throughout.
**Requirements**: RATIFY-01
**Plans**: 4/4 complete. Landed the cheerio lazy-require fix (SC-4, closes the 219 fast_checks handoff), three additive runbook freshness notes (SC-2), the live-guard confirmation record (SC-1), and the quarantine-PR body + close-readiness ship runbook (SC-3/D-13). Ship executed 2026-07-10 per the documented deferred sequence.

## Progress

| Phase | Plans | Status | Completed |
|-------|-------|--------|-----------|
| 216. Harness Foundation + Award Gradient | 9/9 | Complete | 2026-07-04 |
| 217. Adversarial Panel + Auto-Fix Safety Rails | 8/8 | Complete | 2026-07-04 |
| 218. Elevation Wave + Nit Cleanup | 10/10 | Complete | 2026-07-09 |
| 219. Baseline Recapture + Canary Reconciliation | 5/5 | Complete | 2026-07-09 |
| 220. Terminal Ratification | 4/4 | Complete | 2026-07-10 |

## Ship record

The v1.44 body was shipped 2026-07-10 via the Phase 220 `220-CLOSE-READINESS.md` deferred sequence:
quarantine 3-PNG `impersonation-banner` canary PR **#72** merged first (positive amd64 byte-proof green; `fast_checks` red-as-expected on the never-allowlistable canary) → release-please **#66** merged (**v1.2.0** cut) → terminal branch rebased onto new `main` (zero PNG diff) → terminal full-branch merge-commit PR **#73** (6/6 green) merged as `c0595e09` → release-please **#74** merged (**v1.3.0** cut). Close-out PR **#78** flipped `220-VALIDATION.md` to `nyquist_compliant: true`.

**Latent bug caught + fixed at the merge boundary:** the terminal PR's `fast_checks` (the branch's first full CI run) exposed a `fix-queue-lint.sh` bug — it treated the documented surface-level `proxy: true` marker as a cell and demanded `open_findings`; the generator already skipped it, the linter didn't. Fixed to mirror the generator + added a regression test (`993f7db4`). "Green self-tests ≠ live guard" (216 SC-5) surfacing on the first real CI run.

**Deferred (Jon's call, Option A):** v1.2.0 + v1.3.0 are git-tagged + GitHub-released but **not published to Hex** (Hex stays v1.1.0). Blocked by a pre-existing `Upgrade smoke` `<.button type>` warning-as-error (push/schedule-only job, red on every `main` push since the v1.43 ship). Tracked: `.planning/todos/pending/2026-07-10-upgrade-smoke-button-type-hex-publish.md`.

**No git tag** for v1.44 (internal milestone; avoids Hex-version collision — `v1.44` would outrank Hex's `1.3.0`).
