---
phase: 213-latest-phoenix-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/install_golden/tree/config/config.exs
  - test/fixtures/install_golden/STDOUT.txt
autonomous: true
requirements: [COMPAT-01, COMPAT-02]
user_setup:
  - service: phx_new (Hex archive)
    why: "Golden fixture + install-smoke must generate against phx.new >= 1.8.8; a cached 1.8.7 archive produces the wrong fixture bytes"
    dashboard_config:
      - task: "Ensure the local phx_new archive is the concrete pin target. Confirm current-latest with `mix hex.info phx_new`; if a patch newer than 1.8.8 shipped, pin to THAT concrete version and rebless against it (A3). Install with `mix archive.install --force hex phx_new 1.8.8` and confirm `mix phx.new --version` prints the matching installer version."
        location: "Local dev machine / CI runner (`~/.mix/archives/`)"
  - service: PostgreSQL
    why: "golden_diff_test runs a real `mix sigra.install` against a scaffolded app that boots Ecto; the install run needs live Postgres"
    dashboard_config:
      - task: "Boot the ephemeral test Postgres (`scripts/db/up.sh` then `direnv allow` or `source tmp/db.env`), or use a localhost:5432 Postgres with user=postgres password=postgres and a `sigra_test` database"
        location: "Local dev machine (Dockerized ephemeral PG per CLAUDE.md dev-prereq)"

must_haves:
  truths:
    - "Running `mix test test/sigra/install/golden_diff_test.exs` against the phx.new >= 1.8.8 archive passes with zero byte-diffs (per D-04, COMPAT-02)"
    - "The committed golden fixture `config/config.exs` contains the `config :phoenix_live_view, root_tag_attribute: \"phx-r\"` block (per D-05, the empirically-verified single delta)"
    - "A fresh phx.new >= 1.8.8 + `mix sigra.install` + `mix compile --warnings-as-errors` succeeds via install-smoke (per D-02, COMPAT-01)"
  artifacts:
    - "test/fixtures/install_golden/tree/config/config.exs (reblessed under >= 1.8.8, carrying the root_tag_attribute block)"
    - "test/fixtures/install_golden/STDOUT.txt (spot-checked; only changed if install output text shifted)"
  key_links:
    - "The reblessed fixture is the byte-parity baseline every downstream install CI job diffs against; it MUST be committed BEFORE any CI pin flips to 1.8.8 (D-09 load-bearing ordering — enforced by wave: Plan 02 depends on Plan 01)"
  prohibitions:
    - "No hand-editing of fixture bytes — rebless only via `mix sigra.fixture.rebless_golden`, review the git delta, then commit"
    - "The rebless delta MUST be confined to `config/config.exs` (and possibly STDOUT.txt text). If it touches `core_components.ex`, any `.heex`, or adds esbuild/tailwind blocks, STOP and review — that signals a real host-side drift and re-opens the button/template question (D-05 stop-and-review guard)"
    - "No CI workflow pin edits in this plan — those belong to Plan 02 and MUST follow the fixture commit (D-09)"
---

<objective>
Reconcile the install golden fixture with `phx.new` >= 1.8.8 output by reblessing it via the
existing `mix sigra.fixture.rebless_golden` task, then prove COMPAT-02 (fixture byte-parity) and
COMPAT-01 (fresh-host compile-clean) with the existing automated gates. This is the FIRST half of
the load-bearing D-09 ordering: the fixture must already contain the 1.8.8 `root_tag_attribute`
block before any CI pin is flipped, otherwise every install/golden job goes red on the byte-diff.

Purpose: Adopters on the latest `phx.new` (>= 1.8.8) can install Sigra with a golden fixture that
matches their generated output; the `phx_new 1.8.7` freeze is no longer needed to keep the fixture
green.
Output: A reblessed `test/fixtures/install_golden/` tree (single-block `config/config.exs` delta),
verified by `golden_diff_test` and `install-smoke.sh` against the >= 1.8.8 archive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/213-latest-phoenix-compatibility/213-CONTEXT.md
@.planning/phases/213-latest-phoenix-compatibility/213-RESEARCH.md
@.planning/phases/213-latest-phoenix-compatibility/213-VALIDATION.md

# The rebless mechanism (read before running — --check mode at lines 120-145)
@lib/mix/tasks/sigra.fixture.rebless_golden.ex
# The byte-parity gate this plan must turn green
@test/sigra/install/golden_diff_test.exs
# The fixture scaffolder — confirms --no-assets (lines ~15,53) which is WHY only root_tag_attribute drifts
@test/support/install_fixture.ex
# The COMPAT-01 compile-clean gate (--warnings-as-errors near line 64)
@scripts/ci/install-smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebless the install golden fixture under phx.new >= 1.8.8</name>
  <files>test/fixtures/install_golden/tree/config/config.exs, test/fixtures/install_golden/STDOUT.txt</files>
  <read_first>
    - lib/mix/tasks/sigra.fixture.rebless_golden.ex (the regenerator; understand it scaffolds a tmp app, normalizes, replaces the tree, and prints a git-backed delta report)
    - test/support/install_fixture.ex (confirms `mix phx.new --no-assets` — this is why esbuild/tailwind blocks are absent from the fixture and cannot appear in the delta)
    - RESEARCH.md "The Exact Golden Delta" section (the verbatim `root_tag_attribute` block to expect)
  </read_first>
  <action>
    First confirm the local archive is the concrete pin target: run `mix hex.info phx_new` to
    confirm current-latest, and `mix phx.new --version` to confirm the installed installer version.
    Per A3, the target is `1.8.8` unless a newer concrete patch has shipped — if so, pin to that
    concrete version and rebless against it (identical process); record the exact version used in
    the SUMMARY. If the installed archive is older than the target, install it with
    `mix archive.install --force hex phx_new <target>`.

    Ensure live Postgres is available (`scripts/db/up.sh` + `source tmp/db.env`, or a
    localhost:5432 Postgres with user=postgres/password=postgres and a `sigra_test` DB per CLAUDE.md).

    Regenerate the fixture with `MIX_ENV=test mix sigra.fixture.rebless_golden` (per D-04 — do NOT
    hand-edit fixture bytes). Review the printed delta report and `git diff test/fixtures/install_golden/`.

    Per D-05 the ONLY expected change is a single added block in
    `test/fixtures/install_golden/tree/config/config.exs`, inserted between the endpoint/live_view
    config and the mailer config, that configures phoenix_live_view with a root_tag_attribute value
    of the LiveView colocated-CSS default (`phx-r`). Spot-check whether STDOUT.txt also shifted
    (acceptable if it is only install output text).

    STOP-AND-REVIEW GUARD (D-05): if the delta touches `core_components.ex`, any `.heex` template,
    `assets/`, or adds `config :esbuild`/`config :tailwind`/`NODE_PATH` blocks, do NOT commit — that
    is not the expected single-block delta; halt and surface it (it would signal real host-side drift
    and re-open the template/button question, Area A). The fixture is generated `--no-assets` so
    asset-config blocks must never appear.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs</automated>
  </verify>
  <done>
    `golden_diff_test.exs` passes against the >= 1.8.8 archive with zero byte-diffs. `git diff
    --name-only test/fixtures/install_golden/` lists only `config/config.exs` (and optionally
    `STDOUT.txt`). The reblessed `config/config.exs` contains a `config :phoenix_live_view` block
    with `root_tag_attribute: "phx-r"`. No other fixture file changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove COMPAT-01 — fresh generated host compiles clean under --warnings-as-errors</name>
  <files>scripts/ci/install-smoke.sh</files>
  <read_first>
    - scripts/ci/install-smoke.sh (the ground-truth adopter simulation: `mix phx.new` + `mix sigra.install` + `mix compile --warnings-as-errors` at ~line 64)
    - CONTEXT.md D-01/D-02/D-03 (verification-first: templates already carry zero `<.button type=…>` callsites; `type` is a built-in LiveView global; no code change expected)
  </read_first>
  <action>
    This is verification-only per D-02 — no source edits are expected. Run the existing COMPAT-01
    gate against the >= 1.8.8 archive to prove a fresh generated host compiles clean under
    `--warnings-as-errors`: `GITHUB_WORKSPACE=$(pwd) scripts/ci/install-smoke.sh`. Live Postgres and
    the >= 1.8.8 archive from Task 1 must be present.

    Expected result: clean compile, no `undefined attribute "type"` warning and no other 1.8.8
    output-drift compile break — confirming COMPAT-01 is satisfied with zero code change (D-02).

    CONTINGENCY (D-03 — not expected to trigger): if a real `undefined attribute` error surfaces,
    the root cause is Sigra-side (a narrowed `:global` in a Sigra vendored/generated component). The
    fix is a Sigra-owned button declared in `priv/templates/sigra.install/core/sigra_auth_components.ex`
    (which already imports into the auth templates). Do NOT patch the host's `core_components.ex` —
    that violates "own your code". If this triggers, halt and surface it rather than silently patching.
  </action>
  <verify>
    <automated>GITHUB_WORKSPACE=$(pwd) scripts/ci/install-smoke.sh</automated>
  </verify>
  <done>
    `install-smoke.sh` exits 0 against the >= 1.8.8 archive: the fresh `phx.new` + `mix sigra.install`
    host compiles clean under `--warnings-as-errors` with no `undefined attribute "type"` warning
    (COMPAT-01 satisfied, no code change per D-02). No edits were made to `install-smoke.sh` or any
    template unless the D-03 contingency triggered (and if so it was surfaced, not silently applied).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phx.new Hex archive → generated fixture bytes | The generator version determines the committed fixture's byte shape; a silent version change tampers with the byte-parity baseline |
| local/CI toolchain → golden fixture | A cached/stale archive can produce a fixture that does not match adopters' real output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-213-01 | Tampering / supply-chain | phx.new archive → `test/fixtures/install_golden/` | medium | mitigate | Rebless only via `mix sigra.fixture.rebless_golden` (no hand-edited bytes) and confirm the concrete pin target with `mix hex.info phx_new` before reblessing; the D-05 stop-and-review guard rejects any delta beyond `config/config.exs` |
| T-213-02 | Repudiation (false-green) | golden fixture generated against stale local archive | low | accept | Local reblessing uses the developer's confirmed >= 1.8.8 archive; the cross-runner cached-archive case is mitigated by the D-11 smoke version-assert added in Plan 02 |
| T-213-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs no npm/pip/cargo packages; the only "package" is the already-vetted `phx_new` Hex archive, pinned to a concrete version (no floating install) |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs` — green against >= 1.8.8 (COMPAT-02).
- `GITHUB_WORKSPACE=$(pwd) scripts/ci/install-smoke.sh` — exit 0, compile-clean under `--warnings-as-errors` (COMPAT-01).
- `git diff --name-only test/fixtures/install_golden/` — only `config/config.exs` (and optionally `STDOUT.txt`).
- `grep -n 'root_tag_attribute' test/fixtures/install_golden/tree/config/config.exs` — the block is present in the committed fixture.
</verification>

<success_criteria>
- The install golden fixture is reblessed under phx.new >= 1.8.8 with a single-block
  `config/config.exs` delta (the `root_tag_attribute` block), and `golden_diff_test` passes without
  the 1.8.7 freeze (COMPAT-02).
- A fresh phx.new >= 1.8.8 + `mix sigra.install` host compiles clean under `--warnings-as-errors`
  (COMPAT-01) with no template/button code change (D-02).
- The fixture commit lands BEFORE any CI pin flip (D-09) — Plan 02 depends on this plan.
</success_criteria>

<output>
Create `.planning/phases/213-latest-phoenix-compatibility/213-01-SUMMARY.md` when done. Record the
exact phx.new version used for the rebless (A3), the reblessed file list, and confirmation that the
delta was the single `config/config.exs` `root_tag_attribute` block.
</output>
