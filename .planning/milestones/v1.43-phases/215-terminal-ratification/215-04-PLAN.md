---
phase: 215
plan: 04
type: execute
wave: 2
depends_on: [215-01, 215-02, 215-03]
files_modified: []
autonomous: false
requirements: [HEALTH-04]
must_haves:
  truths:
    - "The 110 uncommitted `.planning/` worktree deletions (v1.42 phase 205–212 archived dirs) are dispositioned cleanly BEFORE the PR — the ship PR carries only intended v1.43 content, not stray/accidental deletions. HEALTH-04 (D-01)."
    - "The never-CI'd local `main` body (~45 commits ahead of origin/main) is surfaced to GitHub Actions as a v1.43 ship/integration PR. HEALTH-04 (D-01)."
    - "All FIVE required GitHub checks pass green on the PR under ruleset 14941512 (strict): `Library tests`, `Example unit smoke (ExUnit + ConnTest)`, `Install smoke (fresh phx.new + sigra.install)`, `Example HTTP smoke (boot + curl critical routes)`, `Example Playwright smoke (full lifecycle)`. HEALTH-04 (D-01)."
    - "The PR branch is kept up-to-date with `origin/main` base (strict_required_status_checks_policy) so the required checks are not blocked as stale. HEALTH-04."
  artifacts:
    - ".planning/phases/215-terminal-ratification/215-04-SUMMARY.md — records the PR number/URL, the deletion disposition decision, the exact 5 required-check names + their green status, and the deferred merge/archival hand-off."
  key_links:
    - "The PR's GitHub Actions run is the HEALTH-04 signal of record (D-01) — NOT any local `mix test`/`act` pass. The five required contexts come from branch ruleset 14941512, not the workflow job list. This plan gets the checks green; /gsd-ship merges and /gsd-complete-milestone archives (deferred, D-01)."
  prohibitions:
    - "MUST NOT merge the PR to origin/main and MUST NOT archive the milestone — both are deferred to /gsd-ship + /gsd-complete-milestone (D-01). This plan gets the required checks GREEN only."
    - "MUST NOT treat a red CI lane as an environment excuse — any red required check is a REAL regression to fix under D-06 (e.g. genuine installer-template drift caught by the golden/acceptance lane). NEVER mask a gate by editing a committed fixture/baseline; a wrong local phx_new archive → install the 1.8.8 pin."
    - "MUST NOT recapture admin-design (board-*) PNG baselines on darwin to 'fix' a gallery diff — they are CI-native/ubuntu; a local recapture injects spurious px diffs and masks a real regression (D-06)."
    - "MUST NOT admin-merge, force-push, or self-approve to bypass the strict ruleset — the harness blocks these; keep the PR branch current with base instead."
    - "MUST NOT sweep the 110 uncommitted `.planning/` deletions into the PR via a naive `git add -A` — disposition them explicitly first."
---

<objective>
Surface the never-CI'd v1.43 body to GitHub Actions as a clean ship/integration PR and gate
HEALTH-04 on all FIVE required checks passing green under ruleset 14941512. This is the
load-bearing plan: the suites + ledger are already proven green (215-01/02/03), but local `main`
is ~45 commits ahead of origin/main (never CI'd) and the worktree carries 110 uncommitted
`.planning/` deletions that must not leak into the PR.

Purpose: HEALTH-04's signal of record is the PR's GitHub Actions run (D-01), not a local pass. The
five required contexts are defined by the branch ruleset (strict policy), so the PR branch must be
current with base and all five must be green. Any red required check is a REAL regression to fix
(D-06), never an environment excuse. Per D-01 this plan gets the checks GREEN only — the actual
merge to origin/main and milestone archival are deferred to /gsd-ship + /gsd-complete-milestone.

Output: a green-required-checks v1.43 ship PR, with the PR number, deletion disposition, and the
five green check names recorded in 215-04-SUMMARY.md.

DEPENDS ON: 215-01 (library green), 215-02 (example green), 215-03 (ledger reconciled) — the PR
goes up only once the suites + ledger are proven, so a red PR lane is unambiguously a CI-only
regression, not an unproven-locally artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-terminal-ratification/215-CONTEXT.md
@.planning/phases/215-terminal-ratification/215-RESEARCH.md
@CLAUDE.md
@.github/workflows/ci.yml
</context>

## Artifacts this phase produces

This is a verification/ratification plan — it produces **evidence + a PR**, not source symbols:
- The v1.43 ship/integration PR (its number/URL) with all five required checks green.
- `215-04-SUMMARY.md`: the deletion-disposition decision, the PR number/URL, the exact five required-check names + green status, and the deferred merge/archival hand-off to /gsd-ship + /gsd-complete-milestone.
- Possibly a `chore:`-style cleanup/restore commit for the 110 `.planning/` deletions (a git-state disposition, not product code). Any code fix would occur ONLY to repair a genuine CI-surfaced regression (D-06).

<tasks>

<task type="auto">
  <name>Task 1: Disposition the 110 uncommitted `.planning/` deletions and confirm the current git state (HEALTH-04 pre-req, D-01)</name>
  <files></files>
  <read_first>
    - .planning/phases/215-terminal-ratification/215-RESEARCH.md (§Runtime State Inventory + §Open Questions 1 + §Pitfall 4 — the 110 uncommitted `.planning/` deletions are the v1.42 phase 205–212 dirs; the archive commit archived ROADMAP/REQUIREMENTS but never committed the phase-dir cleanup; disposition options: commit as cleanup / restore / defer to /gsd-cleanup)
    - .planning/STATE.md ("Deferred Items" → v1.42 close: confirms the 205–212 dirs were archived at v1.42 ship, so the deletions are intended cleanup tail, not accidental loss — but CONFIRM before committing)
    - CLAUDE.md (GSD Workflow Enforcement; `/gsd-pr-branch` filters `.planning/` commits for a clean review PR)
  </read_first>
  <action>
    Confirm the live git state first: `git rev-list --count origin/main..main` (research saw ~43–45;
    record the actual number) and `git status --porcelain | grep '^ D .planning'` (research saw 110
    deletions of the v1.42 phase 205–212 dirs). Cross-check against STATE.md's v1.42-close "Deferred
    Items" table to confirm these dirs were intentionally archived at v1.42 ship (so the deletions
    are the un-committed cleanup tail, per research A2) — do NOT assume; if anything suggests
    accidental loss, `git restore` them and FLAG for the user instead of committing.

    Disposition the 110 deletions EXPLICITLY so they cannot leak into the PR via a naive `git add
    -A` (D-01, Pitfall 4). Recommended: commit them as an explicit `chore($PHASE): remove archived
    v1.42 phase-dirs (205–212 cleanup tail)` so the ship PR carries only intended content, OR
    `git restore` them if archival policy says /gsd-cleanup owns that sweep — planner-permitted
    either way, but the end state MUST be a clean worktree with no stray `.planning/` deletions
    hanging. Record which disposition was chosen and why in the SUMMARY.

    After disposition, confirm `git status` shows a clean worktree (no un-dispositioned deletions)
    and the branch is still `main` with the v1.43 body intact.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && echo "ahead=$(git rev-list --count origin/main..main)"; DELS=$(git status --porcelain | grep -c '^ D .planning' || true); echo "undispositioned_planning_deletions=$DELS"; [ "$DELS" = 0 ] && echo "WORKTREE_CLEAN_OF_STRAY_DELETIONS" || echo "STRAY_DELETIONS_REMAIN"</automated>
  </verify>
  <acceptance_criteria>
    - The live ahead-count and the `.planning/` deletion count are recorded (research saw ~43–45 ahead, 110 deletions).
    - The 110 deletions are confirmed as intended v1.42 cleanup (cross-checked against STATE.md) — or restored + flagged if they look accidental.
    - The deletions are dispositioned explicitly (committed as a `chore` cleanup or restored); `git status` shows a clean worktree with zero stray `.planning/` deletions.
    - The disposition decision + rationale is captured for the SUMMARY.
  </acceptance_criteria>
  <done>The 110 uncommitted `.planning/` deletions are dispositioned cleanly so the ship PR carries only intended content, with a clean worktree and the v1.43 body intact.</done>
</task>

<task type="auto">
  <name>Task 2: Cut and push the v1.43 ship PR to surface the never-CI'd body to GitHub Actions (HEALTH-04, D-01)</name>
  <files></files>
  <read_first>
    - .planning/phases/215-terminal-ratification/215-RESEARCH.md (§Objective 3/4 + §"Don't Hand-Roll" — use `/gsd-pr-branch` or `/gsd-ship` (the v1.42 PR #63 path), NOT a hand-crafted push; the required set comes from ruleset 14941512; open PRs #66 release-please / #61/#57 dependabot may move origin/main)
    - CLAUDE.md (GSD Workflow Enforcement + PR conventions; the harness blocks admin-merge/force-push/self-perms)
    - .planning/phases/215-terminal-ratification/215-01-SUMMARY.md, 215-02-SUMMARY.md, 215-03-SUMMARY.md (the proven-green suites + reconciled ledger this PR carries — reference them in the PR body)
  </read_first>
  <action>
    Cut the final v1.43 ship/integration PR from `main` using the sanctioned GSD path
    (`/gsd-pr-branch` to filter `.planning/` commits into a clean review branch, or `/gsd-ship`) —
    mirror the v1.42 ship (PR #63). Do NOT hand-craft a `git push` + ad-hoc `gh pr create` with a
    guessed base/head. The PR base is `origin/main`; the head is the v1.43 body (~45 commits). Push
    the branch so GitHub Actions runs the full required-lane matrix — this is the whole point of
    HEALTH-04 (the never-CI'd body must actually run remotely; local `mix test`/`act` is
    confidence-only, NOT the signal of record).

    In the PR body, reference the proven-green legs from 215-01/02/03 (library 0-fail, example
    0-fail, ledger reconciled) so reviewers see the local evidence. Do NOT merge and do NOT
    self-approve (D-01; harness blocks it). Record the PR number/URL for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && gh pr list --state open --search "v1.43 in:title OR STABILIZE in:title" --json number,title,headRefName 2>/dev/null | tee /tmp/215-04-pr.json; python3 -c "import json,sys; d=json.load(open('/tmp/215-04-pr.json')); print('PR_OPEN' if d else 'NO_PR_FOUND_CHECK_MANUALLY')" 2>/dev/null || echo "verify_pr_manually"</automated>
  </verify>
  <acceptance_criteria>
    - A v1.43 ship/integration PR is open against `origin/main` carrying the ~45-commit body, cut via the sanctioned GSD path (not a hand-crafted push).
    - The PR body references the 215-01/02/03 green evidence.
    - GitHub Actions has been triggered on the PR head (the required-lane matrix is running/queued).
    - The PR is NOT merged and NOT self-approved (D-01).
    - The PR number/URL is captured for the SUMMARY.
  </acceptance_criteria>
  <done>The never-CI'd v1.43 body is surfaced to GitHub Actions as an open ship PR against origin/main via the sanctioned GSD path, with the PR number recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm all five required CI checks green on the unmerged v1.43 PR (HEALTH-04)</name>
  <what-built>
    The v1.43 ship PR is open and GitHub Actions is running the required-lane matrix. Automated
    work done: suites proven green locally (215-01/02), ledger reconciled (215-03), `.planning/`
    deletions dispositioned (Task 1), PR cut + pushed via the sanctioned GSD path (Task 2). This
    checkpoint gates HEALTH-04 on the FIVE required checks going green — the CI-only lanes
    (Playwright/generated-host/install-golden) have never run on this exact body, so a red here is
    a REAL regression to fix (D-06), and the PR branch must be kept current with base (strict
    ruleset).
  </what-built>
  <how-to-verify>
    1. Get the PR number from 215-04-SUMMARY.md (or `gh pr list --state open`).
    2. Run `gh pr checks <PR#>` and confirm ALL FIVE required contexts are green (SUCCESS):
       - `Library tests`
       - `Example unit smoke (ExUnit + ConnTest)`
       - `Install smoke (fresh phx.new + sigra.install)`
       - `Example HTTP smoke (boot + curl critical routes)`
       - `Example Playwright smoke (full lifecycle)`
    3. Confirm the PR is NOT "out-of-date with base" — if `origin/main` moved (dependabot #61/#57,
       release-please #66), sync the branch to base (via /gsd-ship; NEVER admin-merge/force-push)
       and let the checks re-run.
    4. If ANY required check is RED: that is a real regression (D-06) — read the failing lane's
       log, fix the genuine cause (e.g. real installer-template drift → fix the template; wrong
       phx_new archive → install 1.8.8), push the fix, and re-run. NEVER edit a committed
       fixture/baseline to mask the gate, and NEVER recapture board-* PNGs on darwin.
    5. Confirm the PR remains UNMERGED (merge is deferred to /gsd-ship per D-01).
  </how-to-verify>
  <resume-signal>Type "approved" once all five required checks are green on the unmerged PR (branch current with base), or describe the red lane(s) and the fix applied.</resume-signal>
  <files></files>
  <action>Human-verify gate: pause execution until the operator confirms (via `gh pr checks <PR#>`) that all five required contexts under ruleset 14941512 are green on the unmerged PR, per the how-to-verify steps above. If any lane is red, fix the genuine cause (D-06 — never mask via fixture/baseline edits) and re-run before resuming.</action>
  <verify><human-check>All five required checks report SUCCESS on the unmerged v1.43 PR; branch current with base.</human-check></verify>
  <done>All five required GitHub checks are green on the unmerged v1.43 PR (HEALTH-04 signal of record); operator approved.</done>
</task>

<task type="auto">
  <name>Task 3: Record the HEALTH-04 signal and the deferred merge/archival hand-off (D-01)</name>
  <files></files>
  <read_first>
    - /tmp/215-04-pr.json (the PR number/URL from Task 2)
    - .planning/phases/215-terminal-ratification/215-CONTEXT.md (D-01: merge to origin/main + milestone archival are deferred to /gsd-ship + /gsd-complete-milestone; this phase proves close-readiness + gets the PR green only)
    - .planning/phases/215-terminal-ratification/215-RESEARCH.md (§Objective 3 — the exact five required-check context names from ruleset 14941512)
  </read_first>
  <action>
    Record the HEALTH-04 signal of record in the plan SUMMARY: the PR number/URL, and the five
    required-check context names each with their green (SUCCESS) status as observed via `gh pr
    checks <PR#>` at the checkpoint. State explicitly that the PR's GitHub Actions run IS the
    HEALTH-04 signal (local runs are confidence-only, D-01). Record the deletion-disposition
    decision from Task 1.

    Record the deferred hand-off per D-01: the actual merge to origin/main is deferred to
    /gsd-ship and milestone archival is deferred to /gsd-complete-milestone — Phase 215 proves
    close-readiness and gets the PR green; it does NOT merge or archive. Reference 215-03's
    close-readiness record so the RATIFY-01 "full suite + CI green" claim is fully evidenced across
    215-01/02 (suites) + this plan (CI green).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && S=.planning/phases/215-terminal-ratification/215-04-SUMMARY.md && test -f "$S" && grep -Eq 'Library tests' "$S" && grep -Eq 'Example Playwright smoke' "$S" && grep -Eiq 'defer' "$S" && grep -Eiq 'gsd-ship|complete-milestone' "$S" && echo "health04_signal_recorded" || echo "RECORD_INCOMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the PR number/URL and all five required-check context names with green status.
    - The SUMMARY states the PR's GitHub Actions run is the HEALTH-04 signal of record (D-01).
    - The SUMMARY records the deletion-disposition decision from Task 1.
    - The SUMMARY explicitly defers merge to /gsd-ship and archival to /gsd-complete-milestone (D-01).
  </acceptance_criteria>
  <done>The HEALTH-04 signal (PR + five green required checks) and the deferred merge/archival hand-off are recorded in the SUMMARY as the durable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by the phase's scope. The relevant boundary is process-level:
the v1.43 body crosses from never-reviewed/never-CI'd local `main` into GitHub's required-check +
PR-review gate. No new code paths, inputs, endpoints, schemas, or crypto are added.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-04 | Tampering / Repudiation | merging never-reviewed / never-CI'd v1.43 body | low | accept | The D-01 PR + required-check ruleset 14941512 (strict, 5 required contexts + `pull_request` + `deletion` + `non_fast_forward` rules) forces CI + PR review before any merge; the harness blocks admin-merge/force-push/self-perms. This plan only gets the checks green — merge is deferred to /gsd-ship. The control is the ruleset itself; no new attack surface introduced. |
| T-215-04-BM | Tampering | masking a real visual/behavioral regression by editing a fixture/baseline | low | accept | D-06 forbids "fixing" a gate by editing committed fixtures/baselines; design baselines are CI-native (ubuntu), never recaptured on darwin. Any red required check is fixed at its genuine cause. |
</threat_model>

<verification>
- The 110 uncommitted `.planning/` deletions are dispositioned; `git status` shows a clean worktree.
- The v1.43 ship PR is open against origin/main via the sanctioned GSD path; GitHub Actions ran the required-lane matrix.
- All FIVE required checks (`Library tests`, `Example unit smoke (ExUnit + ConnTest)`, `Install smoke (fresh phx.new + sigra.install)`, `Example HTTP smoke (boot + curl critical routes)`, `Example Playwright smoke (full lifecycle)`) are green on the PR (human-verified at the checkpoint).
- The PR remains UNMERGED; merge + archival are deferred to /gsd-ship + /gsd-complete-milestone (D-01).
- `215-04-SUMMARY.md` records the PR number, the five green check names, the deletion disposition, and the deferred hand-off.
</verification>

<success_criteria>
The never-CI'd v1.43 body is surfaced to GitHub Actions as a clean, unmerged ship PR (deletions
dispositioned, cut via the sanctioned GSD path), and all five required checks under ruleset
14941512 are green — with any red lane treated as a real regression fixed at its genuine cause
(D-06), the PR branch kept current with base, and merge + milestone archival explicitly deferred to
/gsd-ship + /gsd-complete-milestone (D-01).
</success_criteria>

<output>
Create `.planning/phases/215-terminal-ratification/215-04-SUMMARY.md` when done.
</output>