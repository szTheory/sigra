---
phase: 156-adopt-shared-components-on-baselined-screens
plan: 06
type: execute
wave: 3
depends_on:
  - 156-03
  - 156-04
  - 156-05
files_modified: []
autonomous: false
requirements:
  - COHR-01
  - COHR-02
  - COHR-03
  - COHR-04
  - COHR-05
  - COHR-06

must_haves:
  truths:
    - "All 5 lib admin LiveViews import `Sigra.Admin.Components` and have zero duplicate private component defp definitions"
    - "Full ExUnit suite passes (`mix test`)"
    - "All 15 Playwright checkpoint tests pass: 12 intentionally re-recorded (4 slugs × 3 projects), 3 impersonation-banner byte-green"
    - "admin-generated parity smoke (`admin-acceptance-smoke.sh`) exits 0"
    - "axe WCAG A/AA gates are green for all checkpoint pages (wired into assertCheckpointScreenshot)"
    - "COHR-01..06 are all satisfied as verified by source assertions and automated gates"
  artifacts:
    - path: lib/sigra/admin/live/users_index_live.ex
      provides: "import Sigra.Admin.Components; no duplicate defp"
      contains: "import Sigra.Admin.Components"
    - path: lib/sigra/admin/live/user_show_live.ex
      provides: "import; sg-page-header; page_back; scope_ribbon; notice; empty_state ×4"
      contains: "import Sigra.Admin.Components"
    - path: lib/sigra/admin/live/audit_index_live.ex
      provides: "import; scope_ribbon; applied_chip; empty_state"
      contains: "import Sigra.Admin.Components"
    - path: lib/sigra/admin/live/index_live.ex
      provides: "import; no defp metric_link/task_card"
      contains: "import Sigra.Admin.Components"
    - path: lib/sigra/admin/live/organization_live.ex
      provides: "import; no defp metric_link/task_card; notice for alert row"
      contains: "import Sigra.Admin.Components"
  key_links:
    - from: "all 5 LiveViews"
      to: "Sigra.Admin.Components"
      via: "import"
      pattern: "import Sigra.Admin.Components"
---

<objective>
Phase gate: confirm all COHR-01..06 requirements are satisfied across all 5 lib admin LiveViews, run the full ExUnit suite, run the full Playwright checkpoint spec (4 slugs re-recorded, impersonation-banner byte-green), and run the admin-generated parity smoke. This is a verification-only plan with no code changes — it proves the phase is complete.

Purpose: Final proof bundle before `/gsd:verify-work`. The phase ships only when all gates are simultaneously green.
Output: Phase 156 verified complete; all 5 LiveViews use shared components; 4 intended re-records committed; parity lane green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-CONTEXT.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-VALIDATION.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-01-SUMMARY.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-02-SUMMARY.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-03-SUMMARY.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-04-SUMMARY.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source assertions — COHR-01..06 per-screen verification</name>
  <files></files>
  <read_first>
    - .planning/phases/156-adopt-shared-components-on-baselined-screens/156-VALIDATION.md — the per-requirement gate commands; sample against all rows.
    - .planning/phases/156-adopt-shared-components-on-baselined-screens/156-CONTEXT.md — locked decisions D-01..D-09; verify each is implemented.
  </read_first>
  <action>
    Run source assertions for each COHR requirement. Do not make any code changes in this task — these are read-only checks.

    COHR-01 (no duplicate private defp component defs in any LiveView):
    For each of the 5 lib admin LiveViews, confirm zero duplicate defp:
    `grep -v "^#" lib/sigra/admin/live/users_index_live.ex | grep -c "defp summary_chip"` → 0
    `grep -v "^#" lib/sigra/admin/live/index_live.ex | grep -c "defp metric_link"` → 0
    `grep -v "^#" lib/sigra/admin/live/organization_live.ex | grep -c "defp metric_link"` → 0
    All 5 LiveViews have import: `grep -l "import Sigra.Admin.Components" lib/sigra/admin/live/*.ex` → 5 files

    COHR-02 (user-detail open sg-page-header):
    `grep -c "sg-page-header" lib/sigra/admin/live/user_show_live.ex` → 1

    COHR-03 (page_back on leaf screens only):
    `grep -c "<\.page_back" lib/sigra/admin/live/user_show_live.ex` → 1
    `grep -c "<\.page_back" lib/sigra/admin/live/users_index_live.ex` → 0 (not on list screen)
    `grep -c "<\.page_back" lib/sigra/admin/live/audit_index_live.ex` → 0 (not on list screen)

    COHR-04 (scope_ribbon on all list + leaf screens):
    `grep -c "<\.scope_ribbon" lib/sigra/admin/live/users_index_live.ex` → 1
    `grep -c "<\.scope_ribbon" lib/sigra/admin/live/user_show_live.ex` → 1
    `grep -c "<\.scope_ribbon" lib/sigra/admin/live/audit_index_live.ex` → 1
    `grep -c "sg-page-copy.*scope_copy" lib/sigra/admin/live/users_index_live.ex` → 0
    `grep -c "sg-page-copy.*scope_copy" lib/sigra/admin/live/audit_index_live.ex` → 0

    COHR-05 (notice component; no ad-hoc sg-list-row alert rows):
    `grep -c "<\.notice" lib/sigra/admin/live/user_show_live.ex` → 1
    `grep -c "<\.notice" lib/sigra/admin/live/organization_live.ex` → 1
    `grep -c "sg-list-row.*data-tone" lib/sigra/admin/live/user_show_live.ex` → 0
    `grep -c "sg-list-row.*data-tone" lib/sigra/admin/live/organization_live.ex` → 0
    `grep -A 20 "defp summary_alert" lib/sigra/admin/live/user_show_live.ex | grep -c '"risk"\|"warn"'` → 0

    COHR-06 (empty_state component):
    `grep -c "<\.empty_state" lib/sigra/admin/live/users_index_live.ex` → 1
    `grep -c "<\.empty_state" lib/sigra/admin/live/user_show_live.ex` → 4
    `grep -c "<\.empty_state" lib/sigra/admin/live/audit_index_live.ex` → 1

    D-08 (CSS tone-merge):
    `grep -c "sg-list-row\[data-tone=\"ok\"\].*sg-notice" test/example/priv/static/assets/css/app.css` → 1
    `grep -v "sg-list-row" test/example/priv/static/assets/css/app.css | grep -c "sg-notice\[data-tone"` → 0

    Run all assertions and report results. If any assertion fails, surface the specific gap.
  </action>
  <verify>
    <automated>
      # COHR-01: import in all 5 LiveViews
      grep -l "import Sigra.Admin.Components" lib/sigra/admin/live/*.ex | wc -l

      # No defp metric_link anywhere in lib admin live
      grep -v "^#" lib/sigra/admin/live/*.ex | grep -c "defp metric_link"

      # No defp summary_chip anywhere in lib admin live
      grep -v "^#" lib/sigra/admin/live/*.ex | grep -c "defp summary_chip"

      # COHR-05: No sg-list-row alert rows (tone-styled)
      grep -c "sg-list-row.*data-tone" lib/sigra/admin/live/user_show_live.ex
      grep -c "sg-list-row.*data-tone" lib/sigra/admin/live/organization_live.ex

      # Full ExUnit suite
      mix test
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "import Sigra.Admin.Components" lib/sigra/admin/live/*.ex | wc -l` returns 5.
    - `grep -v "^#" lib/sigra/admin/live/*.ex | grep -c "defp metric_link"` returns 0.
    - `grep -v "^#" lib/sigra/admin/live/*.ex | grep -c "defp summary_chip"` returns 0.
    - `grep -c "sg-list-row.*data-tone" lib/sigra/admin/live/user_show_live.ex` returns 0.
    - `grep -c "sg-list-row.*data-tone" lib/sigra/admin/live/organization_live.ex` returns 0.
    - `mix test` exits 0 (full ExUnit suite including component goldens and LiveView tests).
  </acceptance_criteria>
  <done>All COHR-01..06 source assertions pass; full ExUnit suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase-gate Playwright + parity smoke — final confirmation</name>
  <what-built>All 5 LiveViews have been migrated across Plans 01–05. This task confirms the full checkpoint spec is clean and the parity lane is green.</what-built>
  <how-to-verify>
    Prerequisite: the example app must be running on port 4000.
    Boot if needed: `cd test/example && mix phx.server`

    Step 1 — Run the full checkpoint spec (all 3 projects, all 5 slugs × 3 = 15 tests):
    ```bash
    cd test/example/priv/playwright
    SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-chromium \
      --project=admin-checkpoints-mobile \
      --project=admin-checkpoints-dark
    ```
    Expected result: 15/15 pass. Zero failures. Zero unexpected re-records needed.

    Confirm the re-record contract (SC-7):
    - `user-detail` ×3: re-recorded in Plan 04 — expected PASS
    - `global-user-index` ×3: re-recorded in Plan 03 — expected PASS
    - `org-scoped-admin` ×3: re-recorded in Plan 03 — expected PASS
    - `audit-explorer` ×3: re-recorded in Plan 05 — expected PASS
    - `impersonation-banner` ×3: byte-green, never re-recorded — expected PASS

    If any test fails unexpectedly: STOP. Do not run `--update-snapshots`. Investigate.

    Step 2 — Run the admin-generated parity smoke:
    ```bash
    GITHUB_WORKSPACE=$(pwd) scripts/ci/admin-acceptance-smoke.sh
    ```
    Expected: exits 0. This confirms the parity lane auto-tracked all lib LiveView changes.

    Step 3 — Confirm axe accessibility:
    The `assertCheckpointScreenshot` function calls `assertNoAxeViolations` before each snapshot.
    Step 1 above covers axe — if checkpoint spec passes, axe is green.

    Step 4 — Confirm snapshot file count:
    ```bash
    ls test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png | wc -l
    ```
    Expected: 15 files (5 slugs × 3 projects). If count differs, investigate.
  </how-to-verify>
  <resume-signal>Type "approved" once: (1) all 15 checkpoint tests pass, (2) admin-acceptance-smoke.sh exits 0, (3) snapshot count is 15. Or describe any failure with its test name and error message.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → snapshot files | Playwright writes snapshot .png files to disk; only deliberate --update-snapshots runs do this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — verification only |

**Security note:** This plan runs only verification commands against existing code. No security-relevant changes are introduced. The parity smoke scaffolds a test app using the installer template — standard test isolation.
</threat_model>

<verification>
This plan IS the verification. The phase is complete when:
1. All COHR-01..06 source assertions in Task 1 pass.
2. `mix test` exits 0.
3. All 15 Playwright checkpoint tests pass (15/15).
4. `admin-acceptance-smoke.sh` exits 0.
5. Snapshot file count is 15.
</verification>

<success_criteria>
All COHR-01..06 requirements verified by automated source assertions; `mix test` green; all 15 checkpoint baselines pass (4 slugs re-recorded deliberately, impersonation-banner byte-green); parity smoke green. Phase 156 is done.
</success_criteria>

<output>
Create `.planning/phases/156-adopt-shared-components-on-baselined-screens/156-06-SUMMARY.md` when done.
</output>
