---
phase: 156-adopt-shared-components-on-baselined-screens
plan: 03
type: execute
wave: 2
depends_on:
  - 156-01
  - 156-02
files_modified:
  - lib/sigra/admin/live/users_index_live.ex
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-user-index-admin-checkpoints-chromium.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-user-index-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-user-index-admin-checkpoints-mobile.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-scoped-admin-admin-checkpoints-chromium.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-scoped-admin-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-scoped-admin-admin-checkpoints-mobile.png
autonomous: true
requirements:
  - COHR-01
  - COHR-04
  - COHR-06

must_haves:
  truths:
    - "`users_index_live.ex` imports `Sigra.Admin.Components` and has no `defp summary_chip`"
    - "D-07: A `<.scope_ribbon copy={scope_copy(@admin_scope)} />` element appears immediately after the `<header class=\"sg-page-header\">` block (not nested inside it) — same scope-display component on every list/leaf screen"
    - "The `<p class=\"sg-page-copy\">` scope subtitle is removed from the header"
    - "Inline `sg-applied-chip` spans are replaced by `<.applied_chip :for={…}>` calls"
    - "The inline `sg-empty-state` div is replaced by `<.empty_state>` with inner_block body text preserved"
    - "D-03 / D-04: Playwright baselines `global-user-index` and `org-scoped-admin` are re-recorded (×3 projects) only after HTML-report confirmation that the delta is exactly the scope-ribbon addition (no blanket re-record; byte-green is the default)"
    - "`impersonation-banner` snapshot stays byte-green"
  artifacts:
    - path: lib/sigra/admin/live/users_index_live.ex
      provides: "import Sigra.Admin.Components; scope_ribbon after header; applied_chip; empty_state"
      contains: "import Sigra.Admin.Components"
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-user-index-admin-checkpoints-chromium.png"
      provides: "Updated baseline with scope ribbon"
  key_links:
    - from: "users_index_live.ex render/1"
      to: "Sigra.Admin.Components.scope_ribbon/1"
      via: "<.scope_ribbon copy={scope_copy(@admin_scope)} /> after </header>"
      pattern: "<\\.scope_ribbon"
    - from: "users_index_live.ex render/1"
      to: "Sigra.Admin.Components.applied_chip/1"
      via: "<.applied_chip :for={chip <- applied_chips(@current_params)} …>"
      pattern: "<\\.applied_chip"
    - from: "users_index_live.ex render/1"
      to: "Sigra.Admin.Components.empty_state/1"
      via: "<.empty_state title=\"No users match this view\">"
      pattern: "<\\.empty_state"
---

<objective>
Migrate `users_index_live.ex` to use `Sigra.Admin.Components`: add the import, delete `defp summary_chip` (COHR-01), add `<.scope_ribbon>` after the page header (COHR-04), migrate inline applied-chip spans to `<.applied_chip>` (COHR-01), and migrate the inline empty-state div to `<.empty_state>` (COHR-06). Then re-record the `global-user-index` and `org-scoped-admin` Playwright baselines after HTML-report confirmation.

Purpose: This screen drives two checkpoint slugs (`global-user-index`, `org-scoped-admin`). The scope-ribbon addition is an intended visual delta (COHR-04) — re-record is expected and deliberate. The `impersonation-banner` slug must stay byte-green (it is the canary).
Output: Migrated LiveView file + 6 updated `.png` baselines (2 slugs × 3 projects).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-CONTEXT.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-RESEARCH.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-UI-SPEC.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-VALIDATION.md

<interfaces>
<!-- From lib/sigra/admin/components.ex — component signatures needed for this migration -->

summary_chip/1 (replaces defp summary_chip/1 at line 336):
  attr :label, :string,  required: true  -- human-readable KPI label
  attr :value, :integer, required: true  -- numeric KPI value
  attr :class, :any,     default: nil
  attr :rest,  :global
  Emits: <div class="sg-metric"><dt>{@label}</dt><dd>{@value}</dd></div>
  Call sites in users_index_live.ex already use <.summary_chip label=… value=…> at lines 78-83.
  After import, simply DELETE the local defp (no call site changes needed).

applied_chip/1 (replaces inline :for span loop at lines 168-178):
  attr :label,       :string, required: true  -- filter label shown inside the chip
  attr :remove_href, :string, required: true  -- URL to remove this filter
  attr :class,       :any,    default: nil
  attr :rest,        :global
  Migration pattern:
    Before: <span :for={chip <- applied_chips(@current_params)} class="sg-applied-chip">
              <span>{chip.label}</span>
              <a class="sg-applied-chip__remove" href={remove_chip_path(@admin_scope, @current_params, chip.key)}
                 aria-label={"Remove filter " <> chip.label}>
                <span aria-hidden="true">&times;</span><span class="sr-only">remove</span>
              </a>
            </span>
    After:  <.applied_chip
              :for={chip <- applied_chips(@current_params)}
              label={chip.label}
              remove_href={remove_chip_path(@admin_scope, @current_params, chip.key)}
            />

scope_ribbon/1 (new — placed immediately after </header> on list screens):
  attr :copy,  :string, required: true  -- scope indicator copy
  attr :class, :any,    default: nil
  attr :rest,  :global
  Emits: <span class="sg-muted sg-text-sm">{@copy}</span>
  Placement: immediately after `</header>` (the closing tag of `<header class="sg-page-header">`),
  NOT nested inside the header. No sg-cluster--between wrapper (no page_back sibling on list screens).
  Remove the `<p class="sg-page-copy">{scope_copy(@admin_scope)}</p>` line 75 from inside the header.

empty_state/1 (replaces inline div at lines 285-302):
  attr :title, :string, required: true  -- primary heading
  attr :class, :any,    default: nil
  slot :inner_block  -- optional body (CTA, filter copy)
  Emits: <div class="sg-empty-state sg-stack sg-stack--3">…</div>
  The current inline div already has `sg-stack sg-stack--3` — migration is byte-neutral for this screen.
  Preserve the inner `<%= if any_filter_active?(@current_params) do %>` conditional as inner_block content.
  Body text (per CONTEXT.md discretion — preserve verbatim):
    - Filtered state: "No users match the active filters. Clear them to widen the result set."
    - Empty state: "Users appear here as people register and sign in. Once accounts exist, you can search, filter, and open any user."

<!-- Checkpoint snapshot files requiring re-record (produced by playwright --update-snapshots) -->
Snapshot dir: test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/
Slugs to re-record:
  global-user-index-admin-checkpoints-chromium.png
  global-user-index-admin-checkpoints-dark.png
  global-user-index-admin-checkpoints-mobile.png
  org-scoped-admin-admin-checkpoints-chromium.png
  org-scoped-admin-admin-checkpoints-dark.png
  org-scoped-admin-admin-checkpoints-mobile.png
MUST stay byte-green: impersonation-banner-*.png (×3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate users_index_live.ex — import, defp removal, scope_ribbon, applied_chip, empty_state</name>
  <files>lib/sigra/admin/live/users_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/users_index_live.ex — read the full file. Key sections: line 6 (use Phoenix.LiveView), lines 69-86 (header block with sg-page-copy at 75 and summary_chip calls at 78-83), lines 167-179 (applied_chip inline loop), lines 285-302 (empty_state block), lines 333-343 (defp summary_chip).
    - lib/sigra/admin/components.ex lines 133–220 — summary_chip, applied_chip, empty_state, scope_ribbon attr contracts.
    - .planning/phases/156-adopt-shared-components-on-baselined-screens/156-RESEARCH.md §"COHR-04: scope_ribbon Placement on List Screens" — exact placement rule (after </header>, not inside it).
    - .planning/phases/156-adopt-shared-components-on-baselined-screens/156-UI-SPEC.md §"scope_ribbon (COHR-04)" — confirms discrete element placement for list screens.
  </read_first>
  <action>
    Read `lib/sigra/admin/live/users_index_live.ex` in full before editing.

    Atomic edit — all changes in one pass:

    1. Add `import Sigra.Admin.Components` immediately after line 6 (`use Phoenix.LiveView`).

    2. Remove `<p class="sg-page-copy">{scope_copy(@admin_scope)}</p>` (line 75) from inside `<header class="sg-page-header">`. The scope job is now handled by the ribbon below.

    3. After the closing `</header>` tag (line 85), add:
       `<.scope_ribbon copy={scope_copy(@admin_scope)} />`
       This element is at the same indent level as `<header>`, outside it — NOT nested inside.

    4. Replace the inline applied-chip `:for` span loop (lines 167-178) with:
       `<.applied_chip :for={chip <- applied_chips(@current_params)} label={chip.label} remove_href={remove_chip_path(@admin_scope, @current_params, chip.key)} />`
       Keep the surrounding `<div :if={any_filter_active?(@current_params)} class="sg-cluster sg-cluster--start">` and the `<a href={index_path(@admin_scope)} class="sg-btn sg-btn--ghost sg-btn--sm">Clear all</a>` link — only the :for span loop changes.

    5. Replace the inline empty-state div (lines 285-302) with:
       ```heex
       <.empty_state :if={@rows == []} title="No users match this view">
         <%= if any_filter_active?(@current_params) do %>
           <p class="sg-muted sg-text-sm">No users match the active filters. Clear them to widen the result set.</p>
           <div class="sg-cluster sg-cluster--center">
             <a href={index_path(@admin_scope)} class="sg-btn sg-btn--secondary sg-btn--sm">Clear all filters</a>
           </div>
         <% else %>
           <p class="sg-muted sg-text-sm">Users appear here as people register and sign in. Once accounts exist, you can search, filter, and open any user.</p>
         <% end %>
       </.empty_state>
       ```
       Preserve the body text verbatim (per CONTEXT.md byte-faithful migration rule).

    6. Delete `defp summary_chip(assigns)` block (lines 333–343) and its `attr` declarations (lines 333–335) — the import now resolves `<.summary_chip>` to the shared module. The call sites at lines 78–83 use identical `label=` and `value=` attr names; no call site changes needed.

    Constraint: all other `defp` functions (quick_filter, status_pills, scope_copy, etc.) are NOT deleted — they are data/helper functions, not component duplicates.

    After writing, run `mix test test/sigra/admin/` to confirm green before proceeding to Task 2.
  </action>
  <verify>
    <automated>
      # 1. Import present
      grep -c "import Sigra.Admin.Components" lib/sigra/admin/live/users_index_live.ex

      # 2. No remaining defp summary_chip
      grep -v "^#" lib/sigra/admin/live/users_index_live.ex | grep -c "defp summary_chip"

      # 3. scope_ribbon present after header
      grep -c "<\.scope_ribbon" lib/sigra/admin/live/users_index_live.ex

      # 4. No sg-page-copy scope prose (removed from header)
      grep -c "sg-page-copy.*scope_copy" lib/sigra/admin/live/users_index_live.ex

      # 5. applied_chip component used (loop migrated)
      grep -c "<\.applied_chip" lib/sigra/admin/live/users_index_live.ex

      # 6. empty_state component used
      grep -c "<\.empty_state" lib/sigra/admin/live/users_index_live.ex

      # 7. Admin tests green
      mix test test/sigra/admin/
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import Sigra.Admin.Components" lib/sigra/admin/live/users_index_live.ex` returns 1.
    - `grep -v "^#" lib/sigra/admin/live/users_index_live.ex | grep -c "defp summary_chip"` returns 0.
    - `grep -c "<\.scope_ribbon" lib/sigra/admin/live/users_index_live.ex` returns 1.
    - `grep -c "sg-page-copy.*scope_copy" lib/sigra/admin/live/users_index_live.ex` returns 0.
    - `grep -c "<\.applied_chip" lib/sigra/admin/live/users_index_live.ex` returns 1 (the :for call).
    - `grep -c "<\.empty_state" lib/sigra/admin/live/users_index_live.ex` returns 1.
    - `mix test test/sigra/admin/` exits 0.
  </acceptance_criteria>
  <done>users_index_live.ex imports Sigra.Admin.Components, defp summary_chip is deleted, scope_ribbon is placed after the header block, applied_chip and empty_state components are wired, and admin tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review and re-record global-user-index and org-scoped-admin baselines</name>
  <what-built>Task 1 migrated users_index_live.ex. The COHR-04 scope ribbon addition is an intended visual delta — `global-user-index` and `org-scoped-admin` snapshots are expected to fail. The `impersonation-banner` snapshot must stay byte-green.</what-built>
  <how-to-verify>
    Prerequisite: the example app must be running on port 4000.
    Boot if needed: `cd test/example && mix phx.server`

    Step 1 — Run the checkpoint spec (expect 6 failures for the 2 affected slugs, 3 passes for impersonation-banner):
    ```bash
    cd test/example/priv/playwright
    SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-chromium \
      --project=admin-checkpoints-mobile \
      --project=admin-checkpoints-dark
    ```

    Step 2 — Review the HTML diff report:
    ```bash
    npx playwright show-report
    ```
    For each failing slug (`global-user-index`, `org-scoped-admin`):
    - Confirm the diff shows ONLY the scope-ribbon addition (a new `<span class="sg-muted sg-text-sm">…</span>` element appearing after the header block).
    - Confirm NO unexpected layout changes, color shifts, or extra elements.
    For `impersonation-banner` (×3): confirm it still passes (byte-green). If it fails, STOP — investigate before proceeding.

    Step 3 — Only after confirming the diff is the named delta: update snapshots:
    ```bash
    SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-chromium \
      --project=admin-checkpoints-mobile \
      --project=admin-checkpoints-dark \
      --update-snapshots
    ```

    Step 4 — Run again to confirm all 5 slugs (×3 projects = 15 tests) now pass:
    ```bash
    SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-chromium \
      --project=admin-checkpoints-mobile \
      --project=admin-checkpoints-dark
    ```
    Expected: 15/15 pass.
  </how-to-verify>
  <resume-signal>Type "approved" once all 15 checkpoint tests pass and the 6 updated .png files are committed alongside the users_index_live.ex change. Or describe any unexpected diff you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView render → browser | UI migration; scope_ribbon renders admin_scope which is set at mount from authenticated plug context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-03-01 | Information Disclosure | scope_ribbon copy | accept | `scope_copy(@admin_scope)` is already displayed in the `sg-page-copy` subtitle — moving it to a ribbon element does not disclose any new information. The admin scope is available only to authenticated admin users. |
| T-156-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

**Security note:** The `scope_ribbon` renders `scope_copy(@admin_scope)` — this string is already rendered on the page. The migration changes its visual presentation but not its exposure to the authenticated user. No new auth check is required.
</threat_model>

<verification>
After both tasks:
1. `grep -c "import Sigra.Admin.Components" lib/sigra/admin/live/users_index_live.ex` → 1
2. `grep -v "^#" lib/sigra/admin/live/users_index_live.ex | grep -c "defp summary_chip"` → 0
3. `grep -c "<\.scope_ribbon" lib/sigra/admin/live/users_index_live.ex` → 1
4. Checkpoint spec: all 15 tests pass (6 intended re-records accepted, 3 impersonation-banner byte-green).
5. `mix test test/sigra/admin/` exits 0.
</verification>

<success_criteria>
`users_index_live.ex` imports shared components and has no duplicate defp; scope ribbon appears after the header; applied_chip and empty_state are wired; `global-user-index` and `org-scoped-admin` baselines are re-recorded with only the COHR-04 ribbon delta; `impersonation-banner` remains byte-green.
</success_criteria>

<output>
Create `.planning/phases/156-adopt-shared-components-on-baselined-screens/156-03-SUMMARY.md` when done.
</output>
