---
phase: 155-shared-component-foundation-keystone
plan: 02
type: execute
wave: 2
depends_on: ["155-01"]
files_modified:
  - test/sigra/admin/components_test.exs
autonomous: true
requirements: [COMP-02]
must_haves:
  truths:
    - "render_component byte/structural equality proves each of the 10 components is a faithful drop-in for the markup it replaces"
    - "Each golden is bootstrapped from the ORIGINAL markup bytes, never authored from the new component (characterization fidelity, not tautology)"
    - "The proof test runs DB-free and endpoint-free in the library_tests lane via mix test"
    - "Each assertion carries a drift message naming the component and citing the design contract + do-not-re-record rule"
  artifacts:
    - path: "test/sigra/admin/components_test.exs"
      provides: "COMP-02 behavior-preservation proof: 7 strict byte-equal goldens + notice target golden + stat/skeleton structural asserts"
      contains: "use ExUnit.Case, async: true"
  key_links:
    - from: "test/sigra/admin/components_test.exs"
      to: "lib/sigra/admin/components.ex"
      via: "render_component(&Components.name/1, <fixed literal assigns>)"
      pattern: "render_component\\(&"
    - from: "test/sigra/admin/components_test.exs"
      to: "original defp/inline markup in lib/sigra/admin/live/*.ex"
      via: "goldens captured from the original output (D-11)"
      pattern: "== @\\w+_golden"
---

<objective>
Create `test/sigra/admin/components_test.exs` — the COMP-02 behavior-preservation proof. Prove each of the 10 components is a byte/pixel-faithful drop-in via `render_component/2` equality, run BEFORE Playwright. This is the real proof of the keystone: the module is unwired this phase, so Playwright renders the unchanged LiveViews and stays green trivially — the `render_component` equality test is what actually guards the extraction.

Purpose: Lock the extraction so any future drift in `components.ex` fails `mix test` (in the DB-free `library_tests` lane) before any Playwright baseline could be touched. Goldens are bootstrapped from the ORIGINAL markup (D-11) so the gate is characterization-faithful, not a tautology.

Output: One test file — `test/sigra/admin/components_test.exs` — with 7 strict byte-equal goldens, 1 full target golden (`notice`), and 2 structural assertions (`stat`, `skeleton`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-shared-component-foundation-keystone/155-CONTEXT.md
@.planning/phases/155-shared-component-foundation-keystone/155-PATTERNS.md
@.planning/phases/155-shared-component-foundation-keystone/155-VALIDATION.md
@.planning/phases/155-shared-component-foundation-keystone/155-01-SUMMARY.md

<interfaces>
<!-- Test harness idiom + the render_component invocation shape. -->
<!-- Source: test/sigra/admin/authorizer_test.exs (DB-free precedent, same dir) -->
<!--         test/example/test/example_web/admin_shell_test.exs:79-84 (render_component call shape) -->

Harness (D-10):
  defmodule Sigra.Admin.ComponentsTest do
    use ExUnit.Case, async: true
    @endpoint nil
    import Phoenix.LiveViewTest
    alias Sigra.Admin.Components
  end

Strict byte-equal assertion (D-11/D-13):
  @stat_link_golden "...captured ORIGINAL bytes..."
  assert render_component(&Components.stat_link/1, label: "...", value: 0, href: "...") ==
           @stat_link_golden,
         "stat_link drifted — see admin-design-contract.md; do not re-record Playwright baselines"

Slot-bearing call shape (notice, empty_state):
  inner_block: [%{inner_block: fn _, _ -> "..." end}]

Assertion tiers (D-11/D-12):
  Strict == (7): stat_link, task_card, summary_chip, applied_chip, empty_state, page_back, scope_ribbon
  Full target golden (1): notice  -> golden = TARGET sg-notice markup (deliberately ≠ current sg-list-row)
  Structural =~/refute (2): stat (sg-metric* present; refute "<a"; refute "sg-stat"), skeleton (=~ "sg-skeleton")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture original markup bytes as goldens (characterization bootstrap)</name>
  <files>test/sigra/admin/components_test.exs</files>
  <read_first>
    - 155-PATTERNS.md (lines 67-181) — VERBATIM original markup per component; the byte source for every strict golden
    - 155-CONTEXT.md (lines 40-45, 93-99) — D-10..D-13 proof harness rules; the string-tone verification note
    - 155-RESEARCH.md (lines 97-120, 198-216) — Q1 (tone is a STRING — verify the current call-site value), Pitfall 1 (capture from original, not new component), Pitfall 2 (notice tone string)
    - test/sigra/admin/authorizer_test.exs (lines 1-2) — DB-free `use ExUnit.Case, async: true` precedent in this exact dir
    - test/example/test/example_web/admin_shell_test.exs (lines 79-84) — `render_component` invocation shape (note: it uses ConnCase only for verified routes; leaf components here do not need it)
    - lib/sigra/admin/live/index_live.ex (lines 114-144) — original metric_link/task_card bytes
    - lib/sigra/admin/live/users_index_live.ex (lines 167-178, 285-302, 333-343) — applied_chip inline, empty_state inline, summary_chip defp bytes
    - lib/sigra/admin/live/user_show_live.ex (lines 90-95, 131-133, 488-504) — page_back/scope_ribbon inline bytes; notice current sg-list-row source; summary_alert/1 (confirms tone is a STRING)
  </read_first>
  <action>
    Create `test/sigra/admin/components_test.exs` as `defmodule Sigra.Admin.ComponentsTest do use ExUnit.Case, async: true`, set `@endpoint nil`, `import Phoenix.LiveViewTest`, `alias Sigra.Admin.Components`. NO ConnCase, NO endpoint, NO Postgres (mirror `authorizer_test.exs`). For the 7 strict-equal components (`stat_link`, `task_card`, `summary_chip`, `applied_chip`, `empty_state`, `page_back`, `scope_ribbon`), bootstrap each `@<name>_golden` module attribute by capturing the ORIGINAL `defp`/inline markup output (D-11, Pitfall 1) — the original `defp`s still exist in the LiveViews this phase, so render the ORIGINAL to obtain the exact bytes; paste those bytes as the golden. Use FIXED literal assigns only (no live query results). For `notice`, author `@notice_golden` as the TARGET `sg-notice` markup (deliberately ≠ current `sg-list-row`), driven with the value that reproduces the ORIGINAL's exact `data-tone` bytes — VERIFY the current call site passes tone as a STRING (`"risk"`), confirmed by `summary_alert/1` at `user_show_live.ex:488-504`, so the rendered attribute is `data-tone="risk"`. Pass slot bodies for `empty_state`/`notice` as `inner_block: [%{inner_block: fn _, _ -> "..." end}]`. Do NOT yet write the assertions repointed at the new component — this task only captures and pins the golden module attributes from the original output.
  </action>
  <verify>
    <automated>grep -q 'use ExUnit.Case, async: true' test/sigra/admin/components_test.exs && grep -q '@endpoint nil' test/sigra/admin/components_test.exs && grep -q 'import Phoenix.LiveViewTest' test/sigra/admin/components_test.exs && grep -Eq '@(stat_link|task_card|summary_chip|applied_chip|empty_state|page_back|scope_ribbon|notice)_golden' test/sigra/admin/components_test.exs && grep -q 'data-tone="risk"' test/sigra/admin/components_test.exs && ! grep -q 'use ExUnit.Case$' test/sigra/admin/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/sigra/admin/components_test.exs` contains `use ExUnit.Case, async: true`, `@endpoint nil`, `import Phoenix.LiveViewTest`
    - File defines golden module attributes for the 7 strict components plus `@notice_golden`
    - The `@notice_golden` string contains `class="sg-notice"` and `data-tone="risk"` (string tone, target form — NOT `sg-list-row`)
    - The 7 strict goldens reproduce the ORIGINAL markup bytes from 155-PATTERNS.md (e.g. `@stat_link_golden` contains `class="sg-metric-link"`, `@task_card_golden` contains `sg-card sg-card-hover sg-stack sg-stack--3`)
    - File uses NO ConnCase, NO endpoint, NO Postgres setup
  </acceptance_criteria>
  <done>The test file exists with all golden module attributes captured from the ORIGINAL markup (and the notice target form), driven by verified string tone — the characterization baseline is pinned before any assertion is repointed at the new component.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint assertions at the new components + structural asserts for stat/skeleton</name>
  <files>test/sigra/admin/components_test.exs</files>
  <read_first>
    - test/sigra/admin/components_test.exs — the file being modified (created in Task 1)
    - lib/sigra/admin/components.ex — the module under test (all 10 components from plan 01)
    - 155-PATTERNS.md (lines 209-221) — assertion-tier idiom + drift-message requirement
    - 155-CONTEXT.md (lines 42-45) — D-11 (7 strict), D-12 (notice full golden; stat/skeleton structural), D-13 (literal ==, drift message, no mneme)
    - 155-VALIDATION.md (lines 44-56) — per-component assertion/tier/command map
  </read_first>
  <action>
    Add the assertions to `test/sigra/admin/components_test.exs`, repointing each at the NEW component `Sigra.Admin.Components`. For the 7 strict components assert `render_component(&Components.<name>/1, <fixed literal assigns>) == @<name>_golden` with the SAME fixed assigns used to capture the golden — guaranteeing the new component reproduces the original bytes (D-11). For `notice` assert `render_component(&Components.notice/1, tone: ..., inner_block: [...]) == @notice_golden` where the golden is the TARGET `sg-notice` form (D-12). For `stat` and `skeleton` (no live analog) use structural asserts (D-12): `stat` — `=~` the required `sg-metric*` class(es), `refute html =~ "<a"`, `refute html =~ "sg-stat"`; `skeleton` — `=~ "sg-skeleton"`. Per D-13 use literal `==`/`=~`/`refute` only — NO `mneme`/`auto_assert`/snapshot library. Every assertion carries a drift message that names the component, cites `admin-design-contract.md`, and includes "do not re-record Playwright baselines". Run the test; it MUST pass (the new component is a faithful drop-in).
  </action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/sigra/admin/components_test.exs` exits 0
    - File asserts `render_component(&Components.stat_link/1, ...) == @stat_link_golden` (and the same `==` pattern for task_card, summary_chip, applied_chip, empty_state, page_back, scope_ribbon, notice)
    - `notice` is asserted against `@notice_golden` (target `sg-notice` form)
    - `stat` assertion contains `refute` of `<a` and of `sg-stat`, and `=~` of an `sg-metric` class
    - `skeleton` assertion contains `=~` of `sg-skeleton`
    - File contains NO `mneme`, `auto_assert`, or snapshot-library reference
    - Each assertion's message string contains the component name, `admin-design-contract.md`, and "do not re-record Playwright baselines"
  </acceptance_criteria>
  <done>All 10 components pass their tier-appropriate equality/structural assertion against the new module, proving behavior-preserving extraction with characterization-faithful goldens and drift messages — `mix test test/sigra/admin/components_test.exs` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → component | The test passes fixed literal assigns into the function components and compares rendered output. No untrusted input, no network, no DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-T01 | Tampering (gate weakening) | golden assertions | mitigate | Goldens are literal `==` strings bootstrapped from the ORIGINAL markup (D-11/D-13); NO `mneme`/snapshot "bless" affordance that could silently re-record drift. Drift messages explicitly forbid re-recording Playwright baselines. |
| T-155-T02 | Repudiation / silent failure | proof gate | mitigate | The test runs DB-free in the `library_tests` lane; plan 03 wires the Playwright job to `needs: [library_tests]` so a red byte-equality gate physically blocks the baseline guard from running. |
| T-155-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — `render_component/2` ships in the already-locked `phoenix_live_view 1.1.31`. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix test test/sigra/admin/components_test.exs` exits 0 (DB-free, async, < ~5s).
- All 10 components covered: 7 strict byte-equal, 1 notice target golden, 2 structural (stat/skeleton).
- Goldens captured from ORIGINAL markup (characterization), not authored from the new component.
- No snapshot library; every assertion carries a component-naming, contract-citing, do-not-re-record drift message.
</verification>

<success_criteria>
- COMP-02 markup proof satisfied: each component proven a faithful drop-in via `render_component` equality before Playwright.
- The gate runs in the DB-free `library_tests` lane (`mix test`).
- Tautology guard honored (D-11): goldens are the original's bytes, not the new component's.
</success_criteria>

<output>
Create `.planning/phases/155-shared-component-foundation-keystone/155-02-SUMMARY.md` when done
</output>
