---
phase: 155-shared-component-foundation-keystone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/components.ex
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "Sigra.Admin.Components compiles and exposes all 10 canonical components as public function components"
    - "Each component declares explicit semantic attrs, a merged class attr, and a global rest attr"
    - "Components emit only existing sg-* classes; no .sg-stat or any invented class is introduced"
    - "notice ships sg-notice + data-tone with NO live-region role by default"
  artifacts:
    - path: "lib/sigra/admin/components.ex"
      provides: "Sigra.Admin.Components module with 10 flat stateless function components in contract order"
      contains: "defmodule Sigra.Admin.Components"
  key_links:
    - from: "lib/sigra/admin/components.ex"
      to: "guides/reference/admin-design-contract.md"
      via: "@moduledoc reference + exact sg-* class strings per the contract"
      pattern: "sg-metric-link|sg-card|sg-applied-chip|sg-empty-state|sg-notice|sg-skeleton"
---

<objective>
Build `lib/sigra/admin/components.ex` — the lib-owned `Sigra.Admin.Components` module exposing all 10 canonical admin function components (COMP-01). This is the BUILD-ONLY deliverable: the module is defined but UNWIRED — no LiveView call site consumes it in this phase (that is Phase 156 / COHR-*). The original `defp`/inline markup in the admin LiveViews stays in place untouched.

Purpose: Consolidate the duplicated admin chrome (`metric_link`/`task_card` are byte-identical across two LiveViews) into a single lib-owned canonical set so design/a11y fixes propagate via `mix deps.update` (D-05). This module is the foundation the rest of the milestone builds on.

Output: One file — `lib/sigra/admin/components.ex` — with 10 documented function components in contract order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-shared-component-foundation-keystone/155-CONTEXT.md
@.planning/phases/155-shared-component-foundation-keystone/155-PATTERNS.md
@.planning/phases/155-shared-component-foundation-keystone/155-UI-SPEC.md
@guides/reference/admin-design-contract.md

<interfaces>
<!-- Phoenix.Component attr/slot/rest idiom the executor must follow. -->
<!-- Source: test/example/lib/example_web/components/core_components.ex (idiom only — do NOT copy flash/1's role="alert"). -->
<!-- 155-PATTERNS.md "VERBATIM current markup per component" (lines 67-181) is the AUTHORITATIVE markup source for each component. -->

Module head (D-01):
  defmodule Sigra.Admin.Components do
    @moduledoc "..."   # names lib-owned canonical set; points to admin-design-contract.md
    use Phoenix.Component
  end

Per-component idiom (D-02/D-04):
  - @doc with one-line job statement + ## Examples HEEx snippet
  - explicit attrs: required: / default: / doc:, plus values: for the notice tone enum
  - attr :class, :any, default: nil  ->  merged as class={["sg-…", @class]}
  - attr :rest, :global  ->  spread on the OUTERMOST element
  - slot :inner_block  ->  ONLY notice (required: true) and empty_state

Exact emitted root classes per contract (D-03) — see 155-UI-SPEC.md Component Contract Inventory (lines 140-151):
  stat_link    -> sg-metric-link (+ __label / __value)
  stat         -> reuse sg-metric* utilities; NO <a>, NO .sg-stat
  task_card    -> sg-card sg-card-hover sg-stack sg-stack--3
  summary_chip -> sg-metric (<dl>/<dt>/<dd> shape per source)
  applied_chip -> sg-applied-chip (+ __remove)
  empty_state  -> sg-empty-state sg-stack sg-stack--3
  page_back    -> sg-btn sg-btn--ghost sg-btn--sm (no dedicated class)
  scope_ribbon -> sg-muted sg-text-sm (no dedicated class)
  notice       -> sg-notice + data-tone={@tone} (NOT sg-list-row)
  skeleton     -> sg-skeleton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the module + the 8 components with a live markup source</name>
  <files>lib/sigra/admin/components.ex</files>
  <read_first>
    - 155-PATTERNS.md (lines 67-181) — VERBATIM current markup per component; this is the byte source the components must reproduce
    - 155-UI-SPEC.md (lines 133-151) — Component Contract Inventory: required attrs, slots, root class, ARIA per component
    - guides/reference/admin-design-contract.md — authoritative per-component markup/class/ARIA spec
    - test/example/lib/example_web/components/core_components.ex — attr/slot/rest/@doc idiom (lines ~29-90); do NOT copy flash/1's role="alert"
    - lib/sigra/admin/live/index_live.ex (lines 114-144) — original metric_link/1 + task_card/1 defp source bytes
    - lib/sigra/admin/live/users_index_live.ex (lines 167-178, 285-302, 333-343) — applied_chip inline, empty_state inline, summary_chip defp source bytes
    - lib/sigra/admin/live/user_show_live.ex (lines 90-95) — page_back + scope_ribbon inline source bytes
  </read_first>
  <action>
    Create `lib/sigra/admin/components.ex` as `defmodule Sigra.Admin.Components do use Phoenix.Component`. `@moduledoc` states this is the lib-owned canonical admin component set and points to `guides/reference/admin-design-contract.md` (D-04). Define these 8 components in contract order, each reproducing its VERBATIM markup from 155-PATTERNS.md: `stat_link` (rename of `metric_link`; attrs `href`/`label`/`value`, root `sg-metric-link`), `task_card` (attrs `title`/`body`/`href`/`action`, root `sg-card sg-card-hover sg-stack sg-stack--3`), `summary_chip` (attrs `label`/`value`, `sg-metric` `<dl>`-shape), `applied_chip` (attrs `label` + remove `href`; `aria-label` is `"Remove filter " <> label`; `&times;` glyph `aria-hidden`, `.sr-only` "remove"), `empty_state` (`slot :inner_block` for the variable body; title via attr per UI-SPEC; root `sg-empty-state sg-stack sg-stack--3` + `sg-empty-state__title`), `page_back` (attr `return_to`; fixed `<span aria-hidden="true">&larr;</span> ` prefix + caller-supplied label; root `sg-btn sg-btn--ghost sg-btn--sm`), `scope_ribbon` (caller-supplied copy; reuse `sg-muted sg-text-sm`, no dedicated class). Per D-02 every component declares explicit semantic `attr`s with `required:`/`default:`/`doc:`, adds `attr :class, :any, default: nil` merged as `class={["sg-…", @class]}`, and `attr :rest, :global` spread on the outermost element. Per D-04 each gets a `@doc` (one-line job + `## Examples`). Emit ONLY existing `sg-*` classes (D-03) — do NOT invent any new class. The original `defp`s in the LiveViews remain in place (deletion is Phase 156).
  </action>
  <verify>
    <automated>grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def stat_link\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def task_card\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def summary_chip\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def applied_chip\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def empty_state\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def page_back\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def scope_ribbon\(' && mix compile --warnings-as-errors 2>&1 | grep -vq 'error'</automated>
  </verify>
  <acceptance_criteria>
    - `lib/sigra/admin/components.ex` contains `defmodule Sigra.Admin.Components`
    - File contains `use Phoenix.Component`
    - File defines public `def stat_link(`, `def task_card(`, `def summary_chip(`, `def applied_chip(`, `def empty_state(`, `def page_back(`, `def scope_ribbon(`
    - File contains `slot :inner_block` for `empty_state` (variable body)
    - File contains `attr :rest, :global` and `attr :class, :any, default: nil`
    - File contains the exact class strings `sg-metric-link`, `sg-card sg-card-hover sg-stack sg-stack--3`, `sg-applied-chip`, `sg-empty-state sg-stack sg-stack--3`, `sg-btn sg-btn--ghost sg-btn--sm`, `sg-muted sg-text-sm`
    - `mix compile --warnings-as-errors` exits 0
    - File does NOT contain the string `.sg-stat` or any invented class
  </acceptance_criteria>
  <done>The 8 live-analog components compile cleanly, each declaring documented attr/slot contracts and emitting its verbatim contract markup using only existing sg-* classes.</done>
</task>

<task type="auto">
  <name>Task 2: Add stat, skeleton (no-analog), and notice (deliberate sg-notice fork)</name>
  <files>lib/sigra/admin/components.ex</files>
  <read_first>
    - lib/sigra/admin/components.ex — the file being modified (created in Task 1)
    - 155-PATTERNS.md (lines 156-181) — notice TARGET sg-notice markup (D-07), the string-tone note (Q1/Pitfall 2), stat structural rules, skeleton structural rules
    - 155-UI-SPEC.md (lines 142-172) — stat (no `<a>`, no `.sg-stat`), notice (sg-notice + data-tone, no role by default), skeleton (sg-skeleton) contract rows + the Notice deliberate-fork section
    - 155-RESEARCH.md (lines 97-120) — Q1 (tone is a STRING), Q3 (.sg-notice is a verified byte-clone of .sg-list-row)
    - guides/reference/admin-design-contract.md — notice + skeleton entries (notice entry will be amended in plan 02 per D-09; do NOT block on it)
  </read_first>
  <action>
    Add the remaining 3 components to `lib/sigra/admin/components.ex` in contract order. `stat`: read-only numeric KPI built fresh per contract — reuse `sg-metric*` utilities, attrs `label`/`value`, NO `<a>` element, NO `.sg-stat` class (D-03). `notice`: ship the FINAL TARGET form `<div class="sg-notice" data-tone={@tone} {@rest}><p class="sg-text-sm">{render_slot(@inner_block)}</p></div>` (D-07). Declare `attr :tone, :atom, values: [:ok, :warn, :risk, :info, nil], default: nil`, `attr :rest, :global`, `slot :inner_block, required: true`. Emit `sg-notice` (NOT `sg-list-row`). NO `role`/`aria-live` by default (D-08) — a live-region role is opt-in per call site via `:rest` only. Note the tone renders as a STRING in attribute position (`data-tone="risk"`), matching what the original `summary_alert/1` emits. `skeleton`: loading placeholder built fresh — root class `sg-skeleton`, no inline motion (motion is owned by CSS and stripped by `prefers-reduced-motion`). Each gets `@doc` (one-line job + `## Examples`), `attr :class, :any, default: nil` merged `class={["sg-…", @class]}`, and `attr :rest, :global` on the outer element per D-02/D-04.
  </action>
  <verify>
    <automated>grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def stat\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def notice\(' && grep -v '^#' lib/sigra/admin/components.ex | grep -Eq 'def skeleton\(' && grep -q 'class="sg-notice"' lib/sigra/admin/components.ex && grep -q 'values: \[:ok, :warn, :risk, :info, nil\]' lib/sigra/admin/components.ex && ! grep -q 'sg-list-row' lib/sigra/admin/components.ex && ! grep -q 'role="alert"' lib/sigra/admin/components.ex && ! grep -q 'sg-stat' lib/sigra/admin/components.ex && mix compile --warnings-as-errors 2>&1 | grep -vq 'error'</automated>
  </verify>
  <acceptance_criteria>
    - File defines public `def stat(`, `def notice(`, `def skeleton(`
    - `notice` emits `class="sg-notice"` and `data-tone={@tone}` and does NOT contain `sg-list-row`
    - `notice` declares `attr :tone, :atom, values: [:ok, :warn, :risk, :info, nil], default: nil` and `slot :inner_block, required: true`
    - File contains NO `role="alert"`, `role="status"`, or `aria-live` on the notice element (D-08)
    - `skeleton` emits the class `sg-skeleton` with no inline motion/transition style
    - File contains NO `.sg-stat` string and `stat` contains no `<a` element
    - `mix compile --warnings-as-errors` exits 0; all 10 components now defined
  </acceptance_criteria>
  <done>All 10 components are defined; notice ships the final sg-notice form with no default live-region role; stat and skeleton are built fresh per contract without inventing classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → component | Call sites pass attrs/slot content into the function components. In this phase there are NO call sites (module is unwired); the only "callers" are the proof tests with fixed literal assigns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-01 | Information disclosure / Tampering (XSS) | `render_slot/1` in `notice`/`empty_state`; interpolated attrs across all components | accept | HEEx auto-escapes all interpolated content and slot bodies; NO `raw/1` is used anywhere in the module. No new XSS sink is introduced — caller content is escaped by the template engine. |
| T-155-02 | Elevation of privilege | entire module | accept | Components are presentation-only stateless renderers — no auth logic, no data mutation, no token/session code, no routes. Zero security surface (D-05 rationale). |
| T-155-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — `Phoenix.Component` ships in the already-locked `phoenix_live_view 1.1.31`. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 with the new module present.
- All 10 components (`stat_link`, `stat`, `task_card`, `summary_chip`, `applied_chip`, `empty_state`, `page_back`, `scope_ribbon`, `notice`, `skeleton`) are defined as public functions in contract order.
- No invented CSS classes; `notice` uses `sg-notice` (not `sg-list-row`) with no default live-region role.
- No LiveView call site changed — the original `defp`s remain; module is unwired (Phase 156 wires it).
</verification>

<success_criteria>
- COMP-01 satisfied: `lib/sigra/admin/components.ex` exists with all 10 canonical components, each with documented `attr`/`slot` contracts.
- CSS boundary respected: only existing `sg-*` classes emitted; no `.sg-stat`, no new CSS.
- `notice` ships its final corrected form (sg-notice, string data-tone, no default role) per D-07/D-08.
</success_criteria>

<output>
Create `.planning/phases/155-shared-component-foundation-keystone/155-01-SUMMARY.md` when done
</output>
