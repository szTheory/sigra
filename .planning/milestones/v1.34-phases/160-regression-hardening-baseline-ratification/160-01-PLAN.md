---
phase: "160"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/static/assets/css/app.css
  - lib/sigra/admin.ex
  - lib/sigra/admin/live/index_live.ex
  - lib/sigra/admin/live/organization_live.ex
  - lib/sigra/admin/users/query.ex
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - "Dark admin pages pass WCAG-AA contrast — no brand-soft+brand-strong AA violation reported by axe"
    - "The needs-review deep-link reaches both locked AND deletion-scheduled accounts (union, not intersection)"
    - "Sigra.Admin.needs_review/1 is the single source of truth — no duplicate private needs_review/1 in index_live.ex or organization_live.ex"
    - "D-08 verify-only holds: notice slot wrapped in <div class=sg-text-sm>, NaiveDateTime head at organization_live.ex:194"
  artifacts:
    - path: "test/example/priv/static/assets/css/app.css"
      provides: "Dark :root block with --sg-color-brand-strong override"
      contains: "--sg-color-brand-strong"
    - path: "lib/sigra/admin.ex"
      provides: "Shared needs_review/1 helper"
      exports: ["needs_review"]
    - path: "lib/sigra/admin/users/query.ex"
      provides: "needs_review filter clause (OR: locked OR deleted)"
      contains: "apply_filter.*needs_review"
  key_links:
    - from: "lib/sigra/admin/live/index_live.ex"
      to: "Sigra.Admin.needs_review/1"
      via: "module call replacing defp"
      pattern: "Sigra.Admin.needs_review"
    - from: "lib/sigra/admin/live/organization_live.ex"
      to: "Sigra.Admin.needs_review/1"
      via: "module call replacing defp"
      pattern: "Sigra.Admin.needs_review"
    - from: "index_live.ex alarm notice"
      to: "?needs_review=true URL"
      via: "href attribute"
      pattern: "needs_review=true"
---

<objective>
Two code folds that prevent baseline ratification from being correct: a dark-mode WCAG-AA
contrast gap on brand chrome (D-06 CSS), and a needs-review count/link mismatch where the
alarm count sums locked+deleted but deep-links only to locked (D-07). Also: extract the
byte-identical needs_review/1 private helper into a shared Sigra.Admin module (IN-03). And:
confirm D-08 verify-only items still hold before closing.

Purpose: Close the two regression blockers before the ratification run; no un-fixed regression
can be certified "green." Dark baseline re-records come in Plan 03 after this CSS lands.
Output: Updated app.css, new lib/sigra/admin.ex, updated index_live.ex and organization_live.ex
call sites, updated query.ex with needs_review filter, and D-08 confirmation.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-regression-hardening-baseline-ratification/160-CONTEXT.md
@.planning/phases/160-regression-hardening-baseline-ratification/160-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from codebase. Executor reads these, not re-explores. -->

From lib/sigra/admin/live/index_live.ex (lines 54–57, 78, 115, 157–159):
- Line 54-57: alarm notice with href="/admin/users?locked=true" — fix to ?needs_review=true
- Line 78: capability card href="/admin/users?locked=true" — fix to ?needs_review=true
- Line 115: stat-card href="/admin/users?locked=true" — fix to ?needs_review=true
- Lines 157-159: defp needs_review(counts) — DELETE, replace with Sigra.Admin.needs_review(counts)

From lib/sigra/admin/live/organization_live.ex (lines 67, 114, 215–217):
- Line 67: href={users_path(@admin_scope) <> "?locked=true"} — fix to <> "?needs_review=true"
- Line 114: href={users_path(@admin_scope) <> "?locked=true"} — fix to <> "?needs_review=true"
- Lines 215-217: defp needs_review(counts) — DELETE, replace with Sigra.Admin.needs_review(counts)

From lib/sigra/admin/users/query.ex (lines 299–310):
- Line 299-304: apply_filter for :locked — AND-filter pattern to mirror
- Line 306-310: apply_filter for :deleted — AND-filter pattern to mirror
- Must add apply_filter for :needs_review using or_where / dynamic: (locked_at NOT NULL) OR (deleted_at NOT NULL)

From lib/sigra/admin/live/users_index_live.ex (line 14):
- @quick_filter_keys ~w(confirmed mfa passkeys locked deleted)
- Must add "needs_review" to this list so the filter is recognized by any_filter_active?/1 and filters_to_chips/1

From test/example/priv/static/assets/css/app.css (lines 160–184 dark :root block):
- The block ends with --sg-elev-3 and closing }
- Add --sg-color-brand-strong: <chosen value> inside this block, before closing brace
- Comment must reference the chip scoped fix it supersedes (lines 885–894)
- After adding global override, remove the scoped color: #fdba74 from .sg-filter-chip:has(input:checked) dark block (lines 885–894) to eliminate duplication

From lib/sigra/admin/authorizer.ex (module header pattern):
- defmodule Sigra.Admin.Authorizer do / @moduledoc "..." — mirror for new Sigra.Admin module
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dark brand-strong CSS override (D-06) + new Sigra.Admin shared helper (D-07/IN-03)</name>
  <files>
    test/example/priv/static/assets/css/app.css,
    lib/sigra/admin.ex
  </files>
  <read_first>
    - test/example/priv/static/assets/css/app.css — read the dark :root block (lines 160–184) and the chip dark override (lines 885–894) before editing
    - lib/sigra/admin/authorizer.ex — read lines 1–14 for the module header pattern to mirror
    - .planning/phases/160-regression-hardening-baseline-ratification/160-PATTERNS.md — "Pattern Assignments" section for app.css and the Sigra.Admin module template
  </read_first>
  <behavior>
    - CSS: dark :root block gains --sg-color-brand-strong: #fdba74 (or nearest value that passes AA on dark brand-soft rgba(243,90,16,0.16) — confirm empirically). Comment: "Dark-mode brand-strong: lightened to clear WCAG AA on dark brand-soft tint (~1.88:1 → >=4.5:1). Supersedes the scoped chip fix at the .sg-filter-chip block below."
    - CSS: after adding global override, delete the scoped color: #fdba74 line from .sg-filter-chip:has(input:checked) dark media block (lines 885–894); keep the wrapper block structure but with the comment updated to reference the global fix above.
    - lib/sigra/admin.ex: defmodule Sigra.Admin with @moduledoc describing shared admin utility helpers, one public def needs_review(counts) that returns Map.get(counts, :locked, 0) + Map.get(counts, :deleted, 0)
  </behavior>
  <action>
    In test/example/priv/static/assets/css/app.css:
    1. Open the dark :root block at line ~160–184. Inside the block, after --sg-color-brand-soft: rgba(243, 90, 16, 0.16) and before the comment about tone text lightening, insert:
       --sg-color-brand-strong: #fdba74;
       with the comment described above.
    2. In the .sg-filter-chip:has(input:checked) dark media block (~lines 885–894), remove the color: #fdba74 line. Keep the block with an updated comment noting the global :root override now applies. Do NOT remove the wrapping @media or selector — just remove the now-redundant scoped color rule.
    3. All edits must stay inside existing @layer sg-components context or within the existing dark :root block. NOTE: the `--sg-color-brand-strong` override is a CSS custom-property declaration in the dark `:root` block, NOT a component rule — `:root` token declarations cannot meaningfully live inside `@layer sg-components` and every existing dark-mode token override (lines 160–184) is already unlayered by design. This is the established token foundation, NOT a violation of the "all CSS in @layer sg-components" rule (which governs component rules). Do NOT add unlayered *component* rules; do NOT relocate the token override into the component layer.

    Create lib/sigra/admin.ex per the PATTERNS.md Sigra.Admin module template. The file must define defmodule Sigra.Admin with @moduledoc and a public needs_review/1.
  </action>
  <verify>
    <automated>
      grep -A2 "brand-strong" test/example/priv/static/assets/css/app.css | grep "fdba74"
      grep -c "Sigra.Admin" lib/sigra/admin.ex
    </automated>
  </verify>
  <done>
    - test/example/priv/static/assets/css/app.css contains --sg-color-brand-strong: #fdba74 (or equivalent AA-passing value) inside the dark @media :root block
    - lib/sigra/admin.ex exists and exports needs_review/1
    - The scoped chip color: #fdba74 rule is removed from the .sg-filter-chip dark block
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-07 needs-review link + filter fix — index_live.ex, organization_live.ex, query.ex, users_index_live.ex</name>
  <files>
    lib/sigra/admin/live/index_live.ex,
    lib/sigra/admin/live/organization_live.ex,
    lib/sigra/admin/users/query.ex,
    lib/sigra/admin/live/users_index_live.ex
  </files>
  <read_first>
    - lib/sigra/admin/live/index_live.ex — read the full file to locate alarm notice hrefs (lines ~54-57, 78, 115), the needs_review/1 defp (lines 157–159), and any existing module alias/import block
    - lib/sigra/admin/live/organization_live.ex — read the full file to locate hrefs (lines ~67, 114), needs_review/1 defp (lines 215–217)
    - lib/sigra/admin/users/query.ex — read lines 224–357 to see apply_filter pattern and the locked/deleted handlers (299–310)
    - lib/sigra/admin/live/users_index_live.ex — read lines 14 and 405–430 for @quick_filter_keys and filter chip assembly
    - .planning/phases/160-regression-hardening-baseline-ratification/160-PATTERNS.md — "index_live.ex (D-07 link fix + dedup)" and "organization_live.ex" sections
  </read_first>
  <behavior>
    - index_live.ex and organization_live.ex: replace all href=...?locked=true (for alarm notice + capability card + stat-card) with ...?needs_review=true; remove defp needs_review/1; add call Sigra.Admin.needs_review(counts) at both existing call sites
    - query.ex: add defp apply_filter(query, %Flop.Filter{field: :needs_review, value: true}, _helpers) that uses or_where to return users where (locked_at IS NOT NULL) OR (deleted_at IS NOT NULL). The false case returns the query unchanged (pass-through, OR-filter semantics: "not needs_review" is not a distinct filtered view)
    - users_index_live.ex: add "needs_review" to @quick_filter_keys so it is recognized by any_filter_active?/1 and filters_to_chips/1; add chip_label("needs_review", nil) clause returning "Needs review"
    - No visible text changes to the alarm headline (count number and copy unchanged) — so no checkpoint re-record is triggered
  </behavior>
  <action>
    In index_live.ex:
    1. Replace all three occurrences of href="...?locked=true" (lines ~56, 78, 115) with the needs_review=true equivalent.
    2. Replace the defp needs_review(counts) body (lines ~157–159) with a delegation: defp needs_review(counts), do: Sigra.Admin.needs_review(counts) — OR — remove the defp entirely and update the two call sites to call Sigra.Admin.needs_review(assigns.summary_counts) directly. Prefer the latter (no defp, direct call) for cleanliness.

    In organization_live.ex:
    1. Replace href={users_path(@admin_scope) <> "?locked=true"} on lines ~67 and ~114 with <> "?needs_review=true".
    2. Same defp removal as index_live.ex.

    In query.ex:
    1. After the :deleted apply_filter clause (line ~310), add a :needs_review clause:
       defp apply_filter(query, %Flop.Filter{field: :needs_review, value: true}, _helpers) do
         or_where(query, [user: user], not is_nil(user.locked_at) or not is_nil(user.deleted_at))
       end
       defp apply_filter(query, %Flop.Filter{field: :needs_review, value: false}, _helpers), do: query

    In users_index_live.ex:
    1. Add "needs_review" to @quick_filter_keys (append to the end).
    2. Add chip_label("needs_review", nil), do: "Needs review" clause before the fallback chip_label/2.
  </action>
  <verify>
    <automated>
      grep -c "needs_review=true" lib/sigra/admin/live/index_live.ex
      grep -c "needs_review=true" lib/sigra/admin/live/organization_live.ex
      grep -c "Sigra.Admin.needs_review" lib/sigra/admin/live/index_live.ex
      grep -c "Sigra.Admin.needs_review" lib/sigra/admin/live/organization_live.ex
      grep -c "needs_review" lib/sigra/admin/users/query.ex
      grep "quick_filter_keys" lib/sigra/admin/live/users_index_live.ex | grep "needs_review"
      mix test test/sigra/admin/ --no-start 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - All three locked=true alarm/card hrefs in index_live.ex replaced with needs_review=true
    - Both locked=true hrefs in organization_live.ex replaced with needs_review=true
    - No local defp needs_review/1 remains in either LiveView; both call Sigra.Admin.needs_review/1
    - query.ex has a :needs_review apply_filter clause using or_where for locked OR deleted
    - users_index_live.ex @quick_filter_keys includes "needs_review"; chip label "Needs review" defined
    - mix test test/sigra/admin/ passes with 0 failures
  </done>
</task>

<task type="auto">
  <name>Task 3: D-08 verify-only — confirm both fixes still hold, close todos</name>
  <files>
    lib/sigra/admin/components.ex,
    lib/sigra/admin/live/organization_live.ex
  </files>
  <read_first>
    - lib/sigra/admin/components.ex — read around line 303 to confirm notice/1 wraps slot in div.sg-text-sm not p
    - lib/sigra/admin/live/organization_live.ex — read lines 193–196 to confirm %NaiveDateTime{} head exists in format_date/1
    - .planning/phases/160-regression-hardening-baseline-ratification/160-CONTEXT.md — D-08 section
  </read_first>
  <action>
    This is a verify-only task — NO code changes expected.
    1. Assert components.ex line ~303–306: def notice(assigns) renders <div class="sg-notice"...><div class="sg-text-sm">. Confirm no <p> wrapper.
    2. Assert organization_live.ex lines ~193–194: defp format_date(%NaiveDateTime{} = ndt) head exists above the nil/fallback heads.
    If either assertion fails, fix the discrepancy (unexpected regression) and note it in the task output. If both hold, record "D-08: org-notice-nested-p confirmed resolved (div.sg-text-sm slot wrapper, line 306); admin-format-date-naivedatetime confirmed resolved (NaiveDateTime head, line 194)" in the execution summary.
    No checkpoint re-records expected from this task.
  </action>
  <acceptance_criteria>
    - grep "sg-text-sm" lib/sigra/admin/components.ex shows the div wrapper in notice/1 (NOT a p element)
    - grep "NaiveDateTime" lib/sigra/admin/live/organization_live.ex shows the guard head in format_date/1
    - mix test test/sigra/admin/components_test.exs passes (byte-golden for notice slot wrapping)
  </acceptance_criteria>
  <verify>
    <automated>
      grep -A3 "def notice(assigns)" lib/sigra/admin/components.ex | grep "sg-text-sm"
      grep "NaiveDateTime" lib/sigra/admin/live/organization_live.ex
      mix test test/sigra/admin/components_test.exs 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    - Both D-08 assertions confirmed: notice wraps slot in div.sg-text-sm; NaiveDateTime head present in format_date/1
    - mix test test/sigra/admin/components_test.exs passes
    - D-08 todos noted as resolved in execution summary
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin LiveView → Users query | Admin-only; already behind Sigra.Admin.Authorizer. The D-07 filter change must not broaden the visible user set beyond what the authorized admin can already see. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Information Disclosure | D-07 needs_review filter in query.ex | mitigate | The :needs_review OR-filter returns only users with locked_at IS NOT NULL OR deleted_at IS NOT NULL — a strict subset of the user table the admin is already authorized to query. No new data surface is opened. Verified by ExUnit admin query tests. |
| T-160-02 | Tampering | D-06 CSS dark override | accept | CSS token override affects visual rendering only; no input, auth, or session surface is touched. No new attack vector. |
| T-160-03 | Information Disclosure | D-08 verify-only | accept | No code change; reading-only confirms existing behavior. No new trust boundary. |
| T-160-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. No legitimacy gate needed. |
</threat_model>

<verification>
Run after both tasks complete:

1. mix test test/sigra/admin/ — must pass 0 failures
2. mix test test/sigra/admin/components_test.exs — byte-golden suite must pass
3. grep -c "needs_review=true" lib/sigra/admin/live/index_live.ex — must return >= 3
4. grep -c "needs_review=true" lib/sigra/admin/live/organization_live.ex — must return >= 2
5. grep -rn "defp needs_review" lib/sigra/admin/live/ — must return 0 (no local duplicates)
6. grep "brand-strong.*fdba74\|fdba74.*brand-strong" test/example/priv/static/assets/css/app.css — must match 1 line in dark :root block
7. mix compile --no-optional-deps 2>&1 | grep -i "warning\|error" — should be clean or pre-existing only
</verification>

<success_criteria>
- Dark :root block in app.css has --sg-color-brand-strong override using #fdba74-family value
- Scoped chip dark color rule removed (no duplicate)
- lib/sigra/admin.ex exists with public needs_review/1
- index_live.ex: all alarm/card links point to ?needs_review=true; no local defp needs_review/1
- organization_live.ex: same link fix + dedup
- query.ex: :needs_review apply_filter clause uses or_where for locked OR deleted union
- users_index_live.ex: "needs_review" in @quick_filter_keys; "Needs review" chip label defined
- D-08: both verify-only items confirmed still resolved; no unexpected regression
- mix test test/sigra/admin/ passes
</success_criteria>

<output>
Create .planning/phases/160-regression-hardening-baseline-ratification/160-01-SUMMARY.md when done
</output>
