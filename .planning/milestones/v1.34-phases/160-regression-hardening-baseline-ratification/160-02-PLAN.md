---
phase: "160"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/
autonomous: true
requirements:
  - GATE-02
must_haves:
  truths:
    - "admin-generated installer-parity lane runs clean with 0 failures"
    - "priv/templates/sigra.install/ mirrors test/example/ lib LiveViews (modulo namespace substitution)"
  artifacts:
    - path: "priv/templates/sigra.install/"
      provides: "Installer templates mirroring final lib LiveView state"
  key_links:
    - from: "priv/templates/sigra.install/ admin LiveView templates"
      to: "lib/sigra/admin/live/*.ex final implementations"
      via: "namespace substitution (Sigra.Admin → <%= @app_module %>.Admin)"
      pattern: "admin_live|index_live|organization_live"
---

<objective>
Close GATE-02 by confirming the admin-generated installer-parity lane is green. The
admin-acceptance-smoke.sh script scaffolds a throwaway Phoenix host, installs Sigra, and
runs admin-generated.spec.ts to verify templates mirror lib LiveViews. If installer-template
drift is found (priv/templates/sigra.install/ lagging test/example/), sync templates to
mirror the final lib state (modulo namespace substitution) before re-running.

Purpose: GATE-02 must be formally closed before the milestone proof bundle. Template drift
is a known recurring failure mode; this plan detects and fixes it in one pass.
Output: Green admin-generated parity lane run; installer templates updated if drift found.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-regression-hardening-baseline-ratification/160-CONTEXT.md

<interfaces>
<!-- Key contracts extracted from codebase. Executor reads these, not re-explores. -->

scripts/ci/admin-acceptance-smoke.sh:
- Scaffolds a throwaway Phoenix host app, installs Sigra, seeds a deterministic admin policy,
  runs tests/admin-generated.spec.ts via Playwright.
- Exits 0 on green, non-zero on failure.
- Does NOT require RUN_PARITY=1 — it is a standalone script.

Drift detection: if admin-acceptance-smoke.sh fails on a template mismatch, the fix is to
diff priv/templates/sigra.install/<admin-liveview-files> against the current
test/example/lib/<app>/admin/live/*.ex files and apply the delta with namespace substitution:
  - Module names: Sigra.Admin.* → <%= @app_module %>.Admin.*
  - Route helpers: Sigra.Admin routes → app-specific equivalents (check existing template form)
  - Internal call sites that reference Sigra.* helpers remain Sigra.* (library calls stay as-is)

The key difference between template files and lib files: template files use EEx template syntax
for module naming but call into Sigra library functions directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run admin-generated parity lane; sync templates if drift found (D-04)</name>
  <files>priv/templates/sigra.install/</files>
  <read_first>
    - scripts/ci/admin-acceptance-smoke.sh — read to understand what it scaffolds and how failure looks
    - priv/templates/sigra.install/ — list the admin-related template files present
    - test/example/lib/ — identify the example app's admin LiveView files to diff against templates if needed
    - .planning/phases/160-regression-hardening-baseline-ratification/160-CONTEXT.md — D-04 section and the "known recurring failure mode" note
    - MEMORY.md reference: reference_installer_template_drift.md — pattern for diagnosing template vs example drift
  </read_first>
  <action>
    Step 1 — Run the parity lane:
      bash scripts/ci/admin-acceptance-smoke.sh
    If it exits 0: record "GATE-02: admin-generated lane green — no template drift" and proceed to done.

    Step 2 (if failure on template mismatch) — Diagnose:
      diff priv/templates/sigra.install/<relevant-admin-file> test/example/lib/<app>/admin/live/<file>.ex
    Identify which template(s) lag behind the final lib LiveView state.

    Step 3 (if drift found) — Sync:
    For each drifted template file, apply the delta from the lib LiveView to the template file
    with namespace substitution:
      - Replace Sigra.Admin.SomeLive (the defmodule line) with EEx template form per existing template conventions
      - Library function calls (Sigra.Auth, Sigra.Admin, etc.) stay as-is — these are lib calls
      - Any app-module-specific paths (router helpers, app module name) use EEx interpolation per existing template form
    Do NOT change test/example/ files — only priv/templates/sigra.install/.

    Step 4 — Re-run after sync:
      bash scripts/ci/admin-acceptance-smoke.sh
    Must exit 0.
  </action>
  <acceptance_criteria>
    - bash scripts/ci/admin-acceptance-smoke.sh exits 0
    - If templates were updated: git diff --name-only priv/templates/sigra.install/ shows only the synced files; no test/example/ files changed
    - No admin-generated.spec.ts test failures
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/admin-acceptance-smoke.sh 2>&1 | tail -10</automated>
  </verify>
  <done>
    - admin-acceptance-smoke.sh exits 0 with 0 spec failures
    - GATE-02 can be marked Complete
    - If templates were synced: templates mirror final lib state (modulo namespace substitution)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer templates → scaffolded host app | Template sync is a one-way diff/apply operation on priv/templates/; no user input involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-P2-01 | Tampering | priv/templates/sigra.install/ sync | accept | Template sync is a mechanical diff/apply with no new behavior. The test harness (admin-generated.spec.ts) is the integrity gate — a green run proves templates produce correct output. |
| T-160-P2-02 | Information Disclosure | admin-generated lane scaffolds a throwaway Phoenix app | accept | The throwaway app is created in a temp directory, uses ephemeral test data, and is discarded after the run. No prod data touched. |
| T-160-P2-SC | Tampering | npm/pip/cargo installs | accept | No new installs; admin-acceptance-smoke.sh uses the existing node_modules under test/example/priv/playwright/. |
</threat_model>

<verification>
Run after task completes:

1. bash scripts/ci/admin-acceptance-smoke.sh — must exit 0 with 0 spec failures
2. If templates were updated: git diff --stat priv/templates/sigra.install/ — verify only intended files changed
3. Confirm no test/example/ source files were modified by this plan
</verification>

<success_criteria>
- scripts/ci/admin-acceptance-smoke.sh exits 0
- admin-generated.spec.ts reports 0 failures
- GATE-02 is proven green with evidence from the smoke run output
</success_criteria>

<output>
Create .planning/phases/160-regression-hardening-baseline-ratification/160-02-SUMMARY.md when done
</output>
