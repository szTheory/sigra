---
phase: "160"
plan: 03
type: execute
wave: 2
depends_on:
  - "160-01"
files_modified:
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/user-audit-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-user-index-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/user-detail-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-scoped-admin-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/audit-explorer-admin-checkpoints-dark.png
  - test/example/priv/playwright/snapshot-allowlist
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - "All 8 dark checkpoint baselines are re-recorded to reflect the brand-strong CSS fix"
    - "Light-mode and mobile baselines are byte-unchanged (zero unintended re-records)"
    - "impersonation-banner dark baseline is byte-unchanged (canary)"
    - "axe WCAG-AA reports 0 contrast violations on dark brand-soft+brand-strong combos after re-record"
    - "snapshot-allowlist is reset to steady-state (empty) after the re-records are confirmed"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/"
      provides: "Updated dark PNG baselines reflecting brand-strong fix"
    - path: "test/example/priv/playwright/snapshot-allowlist"
      provides: "Steady-state empty allowlist (no entries except permanent comments)"
  key_links:
    - from: "snapshot-allowlist"
      to: "snapshot-canary-guard.sh --require-all"
      via: "intended-delta manifest"
      pattern: "snapshot-canary-guard"
---

<objective>
Re-record all dark checkpoint baselines to incorporate the D-06 brand-strong CSS fix from Plan 01.
The CSS change affects brand chrome on every dark checkpoint, so all 8 existing dark PNGs will
legitimately differ. This is the one SANCTIONED re-record exception to the zero-re-record law.

After re-recording, run snapshot-canary-guard.sh --require-all with the 8 dark slugs allowlisted
to confirm ALL declared slugs changed (proving the CSS fix took effect) and NO undeclared slugs
changed (proving light-mode and mobile are untouched). Then reset snapshot-allowlist to its
steady-state empty form per D-03 (the Phase 158 user-audit/audit-explorer entries must be removed).

Also run an axe dark pass to confirm 0 WCAG-AA contrast violations on the updated brand chrome.

Purpose: Make the "axe green" claim in criterion 1 genuinely true — no latent dark-AA gap.
Output: 8 updated dark PNGs; steady-state empty snapshot-allowlist; axe dark confirmation.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-regression-hardening-baseline-ratification/160-CONTEXT.md
@.planning/phases/160-regression-hardening-baseline-ratification/160-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from codebase. Executor reads these, not re-explores. -->

Dark checkpoint slugs that render brand chrome (ALL must be re-recorded and allowlisted):
1. global-overview (new slug from Phase 157)
2. org-overview (new slug from Phase 157)
3. user-audit (new slug from Phase 158)
4. global-user-index (existing slug — scope ribbon brand chrome)
5. user-detail (existing slug — scope ribbon brand chrome)
6. org-scoped-admin (existing slug — scope ribbon brand chrome)
7. audit-explorer (existing slug — scope ribbon brand chrome)
8. impersonation-banner — CANARY, must NOT re-record (no brand chrome in impersonation UI)

Note: impersonation-banner is the one dark slug that must stay byte-green. The canary guard
enforces this — any change to impersonation-banner fails the gate regardless of allowlist.

snapshot-allowlist current contents (Phase 158 remnants to remove per D-03):
  user-audit
  audit-explorer

New allowlist content for this re-record: the 7 non-canary dark slugs listed above (1–7).
After the re-records are committed and the gate passes, reset to empty steady state (remove
all slug entries, leave only the comment block).

scripts/ci/snapshot-recapture-gate.sh usage:
  scripts/ci/snapshot-recapture-gate.sh <slug1> [<slug2> ...]
  Runs compare-mode admin checkpoints (all 3 projects), then snapshot-canary-guard with
  --require-all and the provided slugs as --allow args.
  SIGRA_EXAMPLE_URL env var points to the running example app (default http://localhost:4011).

Playwright --update-snapshots flag:
  cd test/example/priv/playwright && \
  CI=false SIGRA_EXAMPLE_URL=http://localhost:4011 \
  npx playwright test tests/admin-checkpoints.spec.ts \
    --project=admin-checkpoints-dark \
    --update-snapshots=all
  This re-records ALL dark baselines including any that were byte-green.
  Must restore the byte-green ones (chromium + mobile) after the dark re-record.
  Alternative: --update-snapshots=missing only adds missing; use "all" for a deliberate re-record.

axe verification: the assertCheckpointScreenshot function in admin-checkpoints.spec.ts already
calls assertNoAxeViolations before each screenshot, so a clean compare-mode run with 0 failures
IS the axe confirmation. The WCAG-AA contrast rules are enforced by axe-core on each page capture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate snapshot-allowlist with 7 dark slugs, re-record dark baselines, verify axe green</name>
  <files>
    test/example/priv/playwright/snapshot-allowlist,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-dark.png,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-dark.png,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/user-audit-admin-checkpoints-dark.png,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-user-index-admin-checkpoints-dark.png,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/user-detail-admin-checkpoints-dark.png,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-scoped-admin-admin-checkpoints-dark.png,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/audit-explorer-admin-checkpoints-dark.png
  </files>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist — read current contents (has Phase 158 user-audit + audit-explorer entries)
    - scripts/ci/snapshot-recapture-gate.sh — read to understand the --allow arg pattern and the REQUIRE_ALL behavior
    - scripts/ci/snapshot-canary-guard.sh — read the slug_of() function to confirm the strip pattern: basename | sed -E 's/-admin-checkpoints-(chromium|mobile|dark)\.png$//'
    - .planning/phases/160-regression-hardening-baseline-ratification/160-CONTEXT.md — D-06 (re-record list), D-03 (reset allowlist after merge)
    - .planning/phases/160-regression-hardening-baseline-ratification/160-PATTERNS.md — "All brand-soft+brand-strong combo surfaces" list
  </read_first>
  <action>
    Step 1 — Update snapshot-allowlist:
    Replace the current snapshot-allowlist content. Keep the existing comment block at the top verbatim.
    Replace the Phase 158 slug entries (user-audit, audit-explorer) with the 7 dark slugs for this re-record:
      global-overview
      org-overview
      user-audit
      global-user-index
      user-detail
      org-scoped-admin
      audit-explorer
    Add a comment above: "# Phase 160 D-06 dark brand-strong fix (remove after merge to main):"

    Step 2 — Ensure the example app is running at http://localhost:4011.
    If not running, start it: cd test/example && PORT=4011 MIX_ENV=dev mix phx.server &
    Wait for the server to be ready (mix phx.server output shows "Running SigraExample.Endpoint").

    Step 3 — Re-record dark baselines:
    cd test/example/priv/playwright && \
    CI=false SIGRA_EXAMPLE_URL=http://localhost:4011 \
    npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-dark \
      --update-snapshots=all

    Step 4 — Run the recapture gate to verify:
    bash scripts/ci/snapshot-recapture-gate.sh \
      global-overview org-overview user-audit \
      global-user-index user-detail org-scoped-admin audit-explorer

    This runs compare-mode for all 3 projects (including chromium + mobile in read-only compare mode),
    then snapshot-canary-guard with --require-all for the 7 slugs. Must exit 0.
    - If chromium or mobile baselines changed unexpectedly, the gate fails with "unintended snapshot change"
      — investigate and revert those changes before proceeding.
    - If impersonation-banner changed, the gate fails with "canary snapshot changed" — revert immediately.

    Step 5 — Verify axe: the gate run in Step 4 already runs axe on each checkpoint page via
    assertNoAxeViolations. A clean exit 0 from the gate IS the axe confirmation.
    Additionally confirm in the gate output: "0 violations" for any axe: assertions in the dark project run.
  </action>
  <acceptance_criteria>
    - snapshot-allowlist contains exactly 7 slug entries (global-overview, org-overview, user-audit, global-user-index, user-detail, org-scoped-admin, audit-explorer) plus the comment block
    - All 7 dark PNG files have been re-recorded (git diff shows them as modified)
    - impersonation-banner-admin-checkpoints-dark.png is NOT in git diff (canary byte-green)
    - No chromium or mobile PNG files are in git diff (light-mode and mobile unaffected)
    - scripts/ci/snapshot-recapture-gate.sh exits 0 with --require-all satisfied (all 7 declared slugs did change)
    - snapshot-canary-guard.sh output: "PASS (7 changed slug(s), all within allowlist)"
    - 0 axe WCAG-AA violations in the dark project run (confirmed by recapture-gate exit 0)
  </acceptance_criteria>
  <verify>
    <automated>
      git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep "dark" | wc -l
      git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -v "dark" | wc -l
      bash scripts/ci/snapshot-recapture-gate.sh global-overview org-overview user-audit global-user-index user-detail org-scoped-admin audit-explorer
    </automated>
  </verify>
  <done>
    - 7 dark PNG baselines re-recorded; 0 light/mobile PNG baselines changed; impersonation-banner unchanged
    - snapshot-recapture-gate.sh exits 0 (compare-mode green, canary guard PASS, ExUnit byte-goldens pass)
    - axe dark run: 0 WCAG-AA violations (confirmed via gate exit 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Reset snapshot-allowlist to steady-state (D-03)</name>
  <files>test/example/priv/playwright/snapshot-allowlist</files>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist — read current state (after Task 1, contains 7 slug entries)
    - .planning/phases/160-regression-hardening-baseline-ratification/160-CONTEXT.md — D-03 ("Reset snapshot-allowlist to its steady-state empty form after D-06 dark re-records are captured and merged")
    - scripts/ci/snapshot-canary-guard.sh — the STEADY STATE comment at line 7 describes the expected empty form
  </read_first>
  <action>
    Replace snapshot-allowlist content with steady-state form:
    - Keep the comment block at the top verbatim (lines 1–13 of the current file)
    - Remove ALL slug entries (the 7 Phase 160 D-06 entries added in Task 1, and the earlier Phase 158 user-audit/audit-explorer entries)
    - The file should contain only the comment block and no non-comment lines

    After reset, run the canary guard in compare mode to confirm it passes with 0 allowlist entries
    (the re-recorded PNGs are now committed, so the canary guard sees 0 changed PNGs vs HEAD and exits clean):
      bash scripts/ci/snapshot-canary-guard.sh --base HEAD
    This must exit 0 with "PASS (0 changed slug(s), all within allowlist)".
  </action>
  <acceptance_criteria>
    - snapshot-allowlist contains only comments (grep -v '^#' snapshot-allowlist | grep -v '^\s*$' | wc -l == 0)
    - bash scripts/ci/snapshot-canary-guard.sh --base HEAD exits 0
    - No slug entries remain from Phase 158 (user-audit, audit-explorer) or Phase 160 D-06
  </acceptance_criteria>
  <verify>
    <automated>
      grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^\s*$' | wc -l
      bash scripts/ci/snapshot-canary-guard.sh --base HEAD 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    - snapshot-allowlist is in steady-state: comment block only, 0 slug entries
    - Canary guard exits 0 in compare mode
    - impersonation-banner canary permanently protected (the file encodes this in its own comment)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → example app | Ephemeral test user fixtures via registerUser/createOrganization — no production data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-P3-01 | Tampering | Dark baseline PNG re-records | mitigate | The snapshot-canary-guard.sh --require-all gate enforces that: (1) only declared slugs change, (2) ALL declared slugs DID change, (3) the impersonation-banner canary is byte-green. This makes the re-record set auditable and reviewable in the diff. |
| T-160-P3-02 | Information Disclosure | axe WCAG-AA dark run | accept | axe runs in-browser against the example app test data; no PII, no production data. The finding is a CSS rendering correctness check. |
| T-160-P3-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; existing node_modules under test/example/priv/playwright/ are used. |
</threat_model>

<verification>
Run after both tasks complete:

1. git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep dark | wc -l — must equal 7
2. git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -v dark | wc -l — must equal 0
3. grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^\s*$' | wc -l — must equal 0 (steady state)
4. bash scripts/ci/snapshot-canary-guard.sh --base HEAD — must exit 0
5. Confirm impersonation-banner-admin-checkpoints-dark.png NOT in git diff
</verification>

<success_criteria>
- Exactly 7 dark PNG baselines re-recorded (global-overview, org-overview, user-audit, global-user-index, user-detail, org-scoped-admin, audit-explorer)
- 0 light-mode or mobile baselines changed
- impersonation-banner canary byte-green
- snapshot-recapture-gate.sh exits 0 (all 3 projects compare green, --require-all satisfied, axe 0 violations, byte-goldens pass)
- snapshot-allowlist reset to steady-state (0 slug entries)
- Dark brand chrome now passes WCAG-AA confirmed by axe gate exit 0
</success_criteria>

<output>
Create .planning/phases/160-regression-hardening-baseline-ratification/160-03-SUMMARY.md when done
</output>
