---
phase: 158-audit-mobile-per-user-audit-high-effort
plan: 05
type: execute
wave: 3
depends_on: ["158-02", "158-03", "158-04"]
files_modified:
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/
autonomous: false
requirements: [AUDX-01, AUDX-02, AUDX-03, GATE-01, GATE-02]
must_haves:
  truths:
    - "A new user-audit Playwright checkpoint passes with axe green across chromium/mobile/dark, waiting on a visible loaded audit row (not just .phx-connected)"
    - "The three audit-explorer baselines are deliberately re-recorded as intended deltas after HTML-report review"
    - "If targetEmail recent-audit contains impersonation rows, the user-detail baseline is re-recorded as a deliberate intended delta; otherwise it stays byte-green"
    - "D-08: The admin-generated installer-parity lane (GATE-02) stays green — these are lib-owned Sigra.Admin.Live.* modules routed directly (no generated-host LiveView copies, per 156 D-09); any admin HEEx mirrored to test/example/ stays in lockstep"
    - "No unintended baseline re-records exist — non-delta slugs stay byte-green"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts"
      provides: "new user-audit slug via captureAndVerify + assertCheckpointScreenshot after impersonation stop"
      contains: "user-audit"
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/"
      provides: "3 new user-audit PNGs + 3 re-recorded audit-explorer PNGs (+ possible user-detail)"
      contains: "user-audit"
  key_links:
    - from: "admin-checkpoints.spec.ts user-audit slug"
      to: "/admin/users/:id/audit for targetEmail"
      via: "page.goto after impersonation stop + loaded-row wait"
      pattern: "user-audit"
    - from: "assertCheckpointScreenshot"
      to: "axe WCAG 2a/2aa gate + toHaveScreenshot"
      via: "the two-call checkpoint pattern"
      pattern: "assertCheckpointScreenshot"
---

<objective>
Add the new `user-audit` Playwright checkpoint to the single authenticated journey, deliberately re-record the three `audit-explorer` baselines as intended deltas, decide+execute a possible `user-detail` re-record (impersonation to info tone), confirm axe green x3 on `user-audit`, and confirm the `admin-generated` parity lane stays green — all after the Wave-2 markup is final.

Purpose: AUDX-01/02/03 visual proof + GATE-01 (user-audit checkpoint x3 + axe) + GATE-02 (parity). Baselines are recorded LAST because the markup must be stable; every re-record is an intended, HTML-report-reviewed delta (156/157 discipline — no blanket "to be safe" resets).

Output: modified `admin-checkpoints.spec.ts` (new slug) + 3 new `user-audit-*` PNGs + 3 re-recorded `audit-explorer-*` PNGs (+ possibly 3 `user-detail-*` PNGs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-CONTEXT.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-UI-SPEC.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-PATTERNS.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-RESEARCH.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-04-SUMMARY.md

<interfaces>
The two-call checkpoint pattern to mirror (Source: admin-checkpoints.spec.ts audit-explorer block ~:267-276):
  page.goto('/admin/audit?action_prefix=admin.impersonation'); waitForLiveViewReady(page);
  expect(page.getByText('Impersonation').first()).toBeVisible();   // loaded-row wait, NOT just .phx-connected
  captureAndVerify(page, testInfo, 'audit-explorer');
  assertCheckpointScreenshot(page, testInfo, 'audit-explorer');     // axe WCAG 2a/2aa, then toHaveScreenshot, fullPage:false

Journey state: impersonation START ~:233-241, STOP ~:259-265 on targetEmail. The audit-explorer slug ~:267-276 relies on those rows. Insert user-audit AFTER stop (~:265) — between stop and audit-explorer, or after audit-explorer (~:276). targetEmail already has admin.impersonation rows — ZERO new seed.

Route: live "/admin/users/:id/audit" -> AuditUserLive (router.ex :260 global, :293 org).

New testids available after Wave 2 for the loaded-row wait:
  [data-testid="admin-audit-user-mobile-results"] article  |  [data-testid="admin-audit-user-desktop-results"] tbody tr  |  a stable "Impersonation" pill text.

Project names (playwright.config.ts ~:108-144): admin-checkpoints-chromium, admin-checkpoints-mobile, admin-checkpoints-dark, admin-generated.
</interfaces>

<run_mechanics>
From MEMORY (admin-checkpoint Playwright re-record procedure) — VERIFY exact invocation against the project's playwright config/justfile before running:
- Boot the example dev server on an ALT PORT (4000 = Rulestead Docker locally; use e.g. 4011).
- PRE-COMPILE the example app before recording to avoid a code-reload crash mid-capture.
- Re-record with --update-snapshots, then RESTORE any canary/unchanged-slug PNGs so only the intended deltas change.
- Review the Playwright HTML report before committing.
- Run one project at a time, e.g.: npx playwright test admin-checkpoints.spec.ts --project=admin-checkpoints-mobile --update-snapshots
- notice/1 wraps slot content in a p element — relevant if asserting notice text.
</run_mechanics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the user-audit slug to the journey and re-record/create baselines</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts, test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts (the file being modified: the captureAndVerify/assertCheckpointScreenshot helpers ~:94-145; the journey ~:147-278; impersonation START ~:233-241 and STOP ~:259-265; the existing audit-explorer slug ~:267-276 to mirror; the stale "five required pages" header comment)
    - .planning/phases/158-.../158-PATTERNS.md (admin-checkpoints.spec.ts section: insert AFTER impersonation STOP, the HARD-FAIL loaded-row-wait requirement, the D-07 re-record reasoning)
    - .planning/phases/158-.../158-UI-SPEC.md (## Interaction Contract -> "Playwright checkpoint: user-audit" + "Baseline re-records (D-07)")
    - .planning/phases/158-.../158-04-SUMMARY.md (whether targetEmail recent-audit contains impersonation rows -> determines if user-detail needs a deliberate re-record)
  </read_first>
  <action>
    Add a new `user-audit` checkpoint to the single authenticated journey in `admin-checkpoints.spec.ts`, inserted AFTER the impersonation STOP sequence (~:265) — either immediately before or after the existing `audit-explorer` block (~:267-276); both satisfy "after impersonation stop." Navigate to `/admin/users/:id/audit` for the journey's existing `targetEmail` user (reuse the id the journey already uses for impersonation — ZERO new seed). Mirror the two-call pattern: captureAndVerify(page, testInfo, 'user-audit') then assertCheckpointScreenshot(page, testInfo, 'user-audit'). HARD-FAIL (D-06): the wait before capture MUST assert a VISIBLE LOADED audit row — use a locator on [data-testid="admin-audit-user-desktop-results"] tbody tr (.first().toBeVisible()) for desktop projects and [data-testid="admin-audit-user-mobile-results"] article for the mobile project, OR a stable "Impersonation" pill text — NOT just .phx-connected/waitForLiveViewReady alone. Optionally update the stale "five required pages" header comment to reflect the now-8 checkpoints. Then RECORD baselines per the run mechanics: produce 3 new user-audit-admin-checkpoints-{chromium,mobile,dark}.png; deliberately re-record all 3 audit-explorer-* (the all-viewport chip row + mobile cards shift every project under fullPage:false — D-07); and per the 04-SUMMARY determination, re-record user-detail-* ONLY if targetEmail recent-audit gained data-tone="info" impersonation rows (otherwise leave user-detail byte-green). IMPORTANT — canary discipline on user-detail: the compact-row swap (Plan 04) is VISUALLY INERT on the UserShow "Recent Audit" block EXCEPT for the conditional impersonation `data-tone="info"` tone. Therefore the ONLY legitimate user-detail diff is the appearance of `data-tone="info"` on impersonation rows; any NON-impersonation user-detail PNG diff (layout, spacing, color, a non-impersonation row changing) is UNEXPECTED and MUST be investigated before any re-record — do NOT auto-accept it as part of the D-07 deltas. Restore all canary/unchanged-slug PNGs so ONLY the intended deltas change. Run one project at a time on the alt PORT after pre-compiling.
  </action>
  <verify>
    <automated>cd test/example && ls priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -E "user-audit-admin-checkpoints-(chromium|mobile|dark)" | wc -l | grep -q 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "user-audit" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` >= 2 (captureAndVerify + assertCheckpointScreenshot calls).
    - The slug navigates to the per-user audit route: `grep -c "/admin/users/" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` shows a goto to `:id/audit` for targetEmail, placed after the impersonation stop block.
    - The loaded-row wait is present (D-06 hard-fail): `grep -c "admin-audit-user-desktop-results\|admin-audit-user-mobile-results\|getByText('Impersonation')" test/example/priv/playwright/tests/admin-checkpoints.spec.ts` >= 1 in the user-audit block — NOT relying on .phx-connected alone.
    - 3 new PNGs exist: `ls test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -cE "user-audit-admin-checkpoints-(chromium|mobile|dark)"` == 3.
    - The 3 audit-explorer PNGs are re-recorded (git shows them modified): `git status --porcelain test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -cE "audit-explorer-admin-checkpoints-(chromium|mobile|dark)"` == 3.
    - Either user-detail PNGs are modified (if 04-SUMMARY said impersonation rows present, and only the data-tone=info treatment changed) OR they are unchanged (byte-green): the git status of user-detail-* matches the 04-SUMMARY determination — no surprise, and no NON-impersonation diff accepted.
    - No unrelated slug PNG is modified (canaries restored): `git status --porcelain test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/` lists ONLY user-audit (new), audit-explorer (3), and optionally user-detail (3) — nothing else.
  </acceptance_criteria>
  <done>The user-audit slug exists with a loaded-row wait; 3 new user-audit PNGs and 3 re-recorded audit-explorer PNGs committed; user-detail handled per the 04-SUMMARY determination (impersonation data-tone=info only; any non-impersonation diff investigated, not auto-accepted); no unintended baseline changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HTML-report review of intended baseline deltas before commit</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/</files>
  <action>
    This is a verification checkpoint, not implementation. The executor pauses here so the developer reviews the Playwright HTML report and confirms every PNG change is an intended delta before the snapshots are committed. See what-built / how-to-verify / resume-signal below.
  </action>
  <what-built>
    A new user-audit checkpoint (x3 projects, axe-gated) plus deliberately re-recorded audit-explorer baselines (x3) and a conditional user-detail re-record. Every PNG change is supposed to be an intended delta: mobile card layout (AUDX-01), the all-viewport quick-filter chip row above the fold (AUDX-02/D-05), and — only if applicable — impersonation rows gaining data-tone=info on user-detail (D-10/Pitfall #3).
  </what-built>
  <how-to-verify>
    1. Open the Playwright HTML report from the checkpoint run.
    2. For each re-recorded audit-explorer-* diff (chromium, mobile, dark): confirm the ONLY changes are (a) the new quick-filter chip row above the fold, and (b) on mobile, the card layout replacing the table. No unexpected layout/spacing/color drift.
    3. For each new user-audit-* PNG (chromium, mobile, dark): confirm it shows real impersonation audit rows (not an empty state), the shared chrome (page_back, scope_ribbon), and — on mobile — the card layout. Confirm axe reported 0 violations on all 3.
    4. For user-detail-*: the compact-row swap is visually inert EXCEPT for the conditional impersonation data-tone=info tone. If 158-04-SUMMARY said targetEmail recent-audit contains impersonation rows, confirm the diff is ONLY the new data-tone=info treatment on those rows; if it said none, confirm user-detail-* are unchanged (byte-green). Any NON-impersonation user-detail diff is unexpected — reject and investigate before commit.
    5. Confirm no other slug PNG changed (canaries intact).
    6. Run the admin-generated parity lane and confirm it is green (shell renders, Global+org scope labels visible, nav present, 403/404 copy intact).
  </how-to-verify>
  <verify>
    <human-check>HTML report shows only the intended audit-explorer + user-audit (+ conditional user-detail data-tone=info only) deltas; axe green x3 on user-audit; admin-generated lane green; canaries intact.</human-check>
  </verify>
  <resume-signal>Type "approved" to commit the reviewed deltas, or describe which diff is unexpected so it can be corrected before commit.</resume-signal>
  <done>Developer has reviewed the HTML report, confirmed every PNG change is an intended delta, confirmed axe green and parity green, and approved the snapshot commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser journey -> per-user audit route | The checkpoint navigates an authenticated admin to a subject user's audit page; the captured page must reflect correctly-scoped audit data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-11 | Information Disclosure | user-audit checkpoint capture | mitigate | The checkpoint captures the subject user's scoped audit page (scoping enforced by AuditUserLive/list_subject_events, unchanged in Plan 03). The axe gate and visual baseline confirm the rendered page is the intended scoped view, not a leaked global feed. N/A beyond confirming the existing scope renders. |
| T-158-12 | Tampering | committed PNG baselines | mitigate | Re-records are gated by a blocking human HTML-report review (Task 2) so only intended deltas land; canaries are restored. No blanket --update-snapshots reset. |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero packages installed; the Playwright/axe harness already exists. |
</threat_model>

<verification>
- 3 new user-audit PNGs (chromium/mobile/dark) committed; axe 0 violations on each.
- 3 audit-explorer PNGs re-recorded as reviewed intended deltas; user-detail handled per 04-SUMMARY (impersonation data-tone=info only).
- admin-generated parity lane green.
- No unintended baseline re-records (canaries intact).
</verification>

<success_criteria>
- The user-audit checkpoint passes x3 with a loaded-row wait and axe green (GATE-01).
- audit-explorer x3 re-recorded as deliberate, HTML-report-reviewed deltas (D-07); user-detail re-recorded only if applicable (and only for the impersonation data-tone=info delta).
- admin-generated parity lane stays green (GATE-02); no surprise baseline changes.
</success_criteria>

<output>
Create `.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-05-SUMMARY.md` when done.
</output>
</content>
