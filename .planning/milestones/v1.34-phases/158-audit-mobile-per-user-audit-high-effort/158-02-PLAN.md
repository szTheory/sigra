---
phase: 158-audit-mobile-per-user-audit-high-effort
plan: 02
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - lib/sigra/admin/live/audit_index_live.ex
autonomous: true
requirements: [AUDX-01, AUDX-02]
must_haves:
  truths:
    - "AuditIndexLive renders a mobile card layout below 1024px and the desktop table at/above it (dual-layout, no horizontal scroll)"
    - "Quick-filter chips set the real string params outcome=failure and action_prefix=admin.impersonation, render an active state when applied, and surface through the existing applied_chip row"
    - "The explorer's tone uses the unified audit_tone/1; the divergent local row_tone/1 is retired"
  artifacts:
    - path: "lib/sigra/admin/live/audit_index_live.ex"
      provides: "dual-layout wrappers + audit_row mobile cards + quick-filter chips + tone consolidation"
      contains: "admin-audit-mobile-results"
  key_links:
    - from: "audit_index_live.ex mobile wrapper"
      to: "Components.audit_row/1"
      via: "<.audit_row :for={row <- @rows} row={row} show_detail show_codes />"
      pattern: "\\.audit_row"
    - from: "quick-filter chips"
      to: "QueryParams @allowed_params (outcome, action_prefix)"
      via: "chip href/submission sets outcome=failure and action_prefix=admin.impersonation"
      pattern: "outcome=failure|action_prefix=admin.impersonation"
---

<objective>
Bring `AuditIndexLive` (the audit explorer) into the coherence milestone: wrap the desktop table in an `sg-show-desktop` dual-layout wrapper, add a sibling `sg-show-mobile` card list iterating `<.audit_row>` cards, add all-viewport quick-filter chips that set the real `outcome`/`action_prefix` params, and consolidate the local `row_tone/1` onto the unified tone derivation.

Purpose: AUDX-01 (mobile card layout) + AUDX-02 (quick-filter chips). Mirrors the proven `UsersIndexLive` dual-layout exactly so no second bespoke responsive pattern exists. The chips deliberately diverge from the users-index boolean-checkbox idiom because impersonation/failure are string param VALUES, not booleans (D-04).

Output: modified `audit_index_live.ex` with dual-layout, chips, and a single tone source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-CONTEXT.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-UI-SPEC.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-PATTERNS.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-RESEARCH.md

<interfaces>
<!-- audit_row/1 contract (frozen by Plan 01 goldens). Source: lib/sigra/admin/components.ex -->
<.audit_row row={row} show_detail show_codes />   <!-- explorer: both flags true -->
<!-- attr :row, :map, required; attr :show_detail, :boolean, default false; attr :show_codes, :boolean, default false -->
<!-- card root: <article class="sg-list-row sg-stack sg-stack--2" data-tone={audit_tone(row)}> -->

<!-- Real filter params (no query-layer change needed). Source: lib/sigra/admin/audit/query_params.ex @allowed_params -->
<!-- action_prefix and outcome are whitelisted; impersonation=true / failure=true are NOT — they are silently dropped by Map.take(@allowed_params). -->

<!-- Existing dual-layout idiom to MIRROR. Source: lib/sigra/admin/live/users_index_live.ex render/1 (~:179-280): -->
<!-- desktop: <div id="..." data-testid="admin-users-desktop-results" class="sg-table-panel sg-show-desktop"><table>...inline <tr>...</table></div> -->
<!-- mobile:  <div id="..." data-testid="admin-users-mobile-results" class="sg-stack sg-stack--3 sg-show-mobile"><article :for=...>...</article></div> -->

<!-- Existing chip CSS active-state LANDMINE. Source: app.css ~:858-882 -->
<!-- .sg-filter-chip:has(input:checked) lights the active brand-soft state. A plain <a> link has no <input>, so the active CSS never fires. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-layout wrappers + audit_row mobile cards + tone consolidation in AuditIndexLive</name>
  <files>lib/sigra/admin/live/audit_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_index_live.ex (the file being modified: the `<div :if={@rows != []} class="sg-table-panel">` table block at ~:125; the table `<tr>` data-tone source ~:136-163 currently using `row_tone(row)`; the local `defp row_tone/1` ~:206-208; the `defp format_timestamp/1` ~:265-268 used by the desktop `<tr>`; the `import`/alias header to confirm `Sigra.Admin.Components` is imported so `<.audit_row>` resolves)
    - lib/sigra/admin/live/users_index_live.ex (the dual-layout idiom to MIRROR exactly: render/1 ~:179-280 — keep BOTH `id=` and `data-testid=` on each wrapper)
    - .planning/phases/158-.../158-UI-SPEC.md (## Component Inventory → "Dual-layout wrappers: AuditIndexLive" table: exact classes `sg-table-panel sg-show-desktop` + testid `admin-audit-desktop-results`, and `sg-stack sg-stack--3 sg-show-mobile` + testid `admin-audit-mobile-results`)
    - .planning/phases/158-.../158-PATTERNS.md (audit_index_live.ex section: the MIRROR instructions, the HARD-FAIL no-horizontal-scroll boundary, and the "Tone consolidation (D-10)" block)
  </read_first>
  <action>
    Wrap the existing desktop table: change `<div :if={@rows != []} class="sg-table-panel">` to add `sg-show-desktop` and `id="admin-audit-desktop-results" data-testid="admin-audit-desktop-results"` (mirroring the users-index desktop wrapper). Add a sibling mobile block immediately after: `<div :if={@rows != []} id="admin-audit-mobile-results" data-testid="admin-audit-mobile-results" class="sg-stack sg-stack--3 sg-show-mobile">` containing `<.audit_row :for={row <- @rows} row={row} show_detail show_codes />`. The card uses `sg-list-row` via the component (NOT `sg-card`). Do NOT solve mobile via horizontal-scroll on the table (D-02 hard-fail). Retire the local `defp row_tone/1` (~:206-208): the desktop `<tr>` `data-tone={row_tone(row)}` must now call the unified tone source. Because the desktop `<tr>` is inline (not the card component) it still needs a tone function in this module — follow the consolidation approach: replace the local `row_tone/1` definition+callsites with the unified `audit_tone/1` body (same three clauses) so there is ONE body, OR if Plan 01 exposed a shared helper, call that. The body MUST be identical to Components' audit_tone/1 (outcome not in ["success",nil,""] → "risk"; action_badge present → "info"; else nil) so the desktop `<tr>` and the mobile card agree. Leave the desktop `<tr>` inline `format_timestamp/1` (seconds format) as-is for now — the desktop table timestamp is not migrated to the card helper (the `<tr>` is not an `<article>`); its baseline shift is owned by Plan 05's deliberate re-record.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/live/audit_index_live_test.exs || mix test test/sigra/admin/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "admin-audit-desktop-results" lib/sigra/admin/live/audit_index_live.ex` == 1 (or 2 if id+data-testid duplicated) and the wrapper class string contains `sg-table-panel sg-show-desktop`.
    - `grep -c "admin-audit-mobile-results" lib/sigra/admin/live/audit_index_live.ex` >= 1 and that wrapper class string contains `sg-stack sg-stack--3 sg-show-mobile`.
    - `grep -n "\.audit_row" lib/sigra/admin/live/audit_index_live.ex` shows `<.audit_row :for={row <- @rows} row={row} show_detail show_codes />` in the mobile block.
    - `grep -c "defp row_tone" lib/sigra/admin/live/audit_index_live.ex` == 0 (local divergent helper retired) — the inline `<tr>` data-tone now resolves through the unified audit_tone source.
    - No horizontal-scroll utility added: `grep -ci "overflow-x\|scroll" lib/sigra/admin/live/audit_index_live.ex` does not increase (D-02 hard-fail).
    - `mix compile --warnings-as-errors` succeeds (no unused-function warning from the retired row_tone).
  </acceptance_criteria>
  <done>Desktop table wrapped with sg-show-desktop + testid; sibling sg-show-mobile card list renders audit_row cards with show_detail+show_codes; local row_tone/1 retired in favor of the unified tone body; no horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 2: All-viewport quick-filter chips setting real outcome/action_prefix params</name>
  <files>lib/sigra/admin/live/audit_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_index_live.ex (the `<form method="get" ... class="sg-filter-panel sg-stack">` detailed filter form at ~:58 and the `<.applied_chip :for={chip <- applied_chips(@current_params)}>` row at ~:117-122 — chips go ABOVE the detailed form, BELOW the applied_chip row per UI-SPEC; the `@current_params` assign and the path helpers `index_path/1` used for links)
    - lib/sigra/admin/live/users_index_live.ex (the `defp quick_filter/1` ~:325-338 checkbox analog and `@quick_filter_keys` ~:14 — the structure to adapt, NOT copy verbatim, since audit chips are value-setters not booleans)
    - lib/sigra/admin/audit/query_params.ex (`@allowed_params` ~:9-21 confirming `action_prefix` + `outcome` are whitelisted and there is NO `impersonation`/`failure` boolean param)
    - .planning/phases/158-.../158-UI-SPEC.md (## Component Inventory → "Quick-filter chips: AuditIndexLive" table: "Failures" → outcome=failure, "Impersonation" → action_prefix=admin.impersonation; the "Chip active state" paragraph; ## Interaction Contract → "Quick-filter chips" + touch-target note)
    - .planning/phases/158-.../158-RESEARCH.md (## Common Pitfalls → Pitfall 1 LANDMINE: the `:has(input:checked)` active-state mismatch and recommended Option (a) checked-input-shell-setting-real-string-value)
    - test/example/priv/static/assets/css/app.css (`.sg-filter-chip` + `:has(input:checked)` ~:858-882 — confirm the active-state selector so the chip's active rendering actually fires)
  </read_first>
  <action>
    Add a quick-filter chip row using the existing `sg-filter-chip` class, rendered on ALL viewports (no `sg-show-*` gate — D-05), placed above the detailed filter form and below the `<.applied_chip>` row. Two chips: "Failures" sets `outcome=failure`; "Impersonation" sets `action_prefix=admin.impersonation`. RESOLVE THE ACTIVE-STATE LANDMINE explicitly per RESEARCH Option (a): render each chip as a `<label class="sg-filter-chip">` containing a checked-able input whose CHECKED state reflects whether the real string value is currently active in `@current_params`, and whose submission sets the REAL string param value (e.g. `<input type="checkbox" name="outcome" value="failure" checked={@current_params[:outcome] == "failure"}>` inside the GET filter form, or a same-shape construct that posts the literal value) — so the existing `:has(input:checked)` CSS lights the active state with ZERO new CSS. The chip MUST set the real param value, NOT a dead `impersonation=true`/`failure=true` boolean (D-04 hard-fail — those are silently dropped by QueryParams). Clearing is via the existing `<.applied_chip>` remove link that already surfaces these params (or re-clicking deactivates). Add a small `defp` (e.g. `audit_quick_filter/1` or inline markup) for the chip; microcopy exactly "Failures" and "Impersonation". Keep the detailed filter form intact — chips are convenience shortcuts onto the same params. Confirm the chips participate in the same GET form/path so navigating a chip yields a URL containing the real param. Document in the implementation which mechanism sets the value (checked-input-in-form vs link) so the acceptance criterion can assert it.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/live/audit_index_live_test.exs || mix test test/sigra/admin/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sg-filter-chip" lib/sigra/admin/live/audit_index_live.ex` >= 2 (two chips) and there is NO `sg-show-mobile`/`sg-show-desktop` class on the chip row (all-viewport, D-05).
    - The "Failures" chip sets `outcome` to `failure` and the "Impersonation" chip sets `action_prefix` to `admin.impersonation` — assert the literal strings exist: `grep -c "failure" lib/sigra/admin/live/audit_index_live.ex` and `grep -c "admin.impersonation" lib/sigra/admin/live/audit_index_live.ex` each >= 1.
    - NO dead boolean param: `grep -c "impersonation=true\|failure=true\|name=\"impersonation\"\|name=\"failure\"" lib/sigra/admin/live/audit_index_live.ex` == 0 (D-04 hard-fail boundary).
    - Active state renders via a checked input (Option a): the chip markup includes an `<input ... checked={...}>` whose checked expression compares against `@current_params` for the real value, so `.sg-filter-chip:has(input:checked)` can fire — `grep -c "checked=" lib/sigra/admin/live/audit_index_live.ex` increases by >= 2 within the chip row.
    - Chip labels are exactly "Failures" and "Impersonation": `grep -c ">Failures<\|>Impersonation<" lib/sigra/admin/live/audit_index_live.ex` >= 2 (allowing for surrounding markup).
    - A LiveView/render test asserts that requesting the page with `outcome=failure` (or activating the chip) produces the active chip state and a filtered result set; if no such test exists, add one to the audit_index test file.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>Two all-viewport quick-filter chips ("Failures", "Impersonation") set the real outcome/action_prefix string params, render an active state via a checked-input shell (preserving :has(input:checked) CSS with zero new CSS), and surface through the existing applied_chip row; no dead boolean param exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client query string → QueryParams.normalize | Chip clicks set `outcome`/`action_prefix` query params crossing into the query layer. |
| host audit data → audit_row/1 mobile cards | Audit metadata rendered into the mobile card list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-03 | Tampering / injection | quick-filter chip params | mitigate | Chips set values consumed only through the existing `QueryParams.normalize` whitelist (`Map.take(@allowed_params)`); no new param is plumbed and no value bypasses normalization. `outcome`/`action_prefix` already accept arbitrary strings and are used as scoped query filters — no new injection surface. |
| T-158-04 | Tampering (XSS) | audit_row/1 mobile cards | mitigate | Cards render via the Plan-01 `audit_row/1` whose fields are HEEx auto-escaped; confirm no `raw/1` added to audit_index_live.ex (`grep -c "raw(" lib/sigra/admin/live/audit_index_live.ex` does not increase). |
| T-158-05 | Information Disclosure | scope leak | accept | The explorer's scope (global vs org) is enforced by the existing `AdminScope` on_mount + `Explorer.list_events/3` scoping — this plan does not alter scoping or add params beyond existing `outcome`/`action_prefix`. No scope change. |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero packages installed; no CSS/JS deps added. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (retired row_tone leaves no unused-function warning).
- `mix test test/sigra/admin/` green.
- No `raw/1`, no horizontal-scroll utility, no dead boolean param introduced.
</verification>

<success_criteria>
- AuditIndexLive has a dual-layout (sg-show-desktop table + sg-show-mobile audit_row cards) with the exact data-testids; no horizontal scroll.
- Two all-viewport chips set the real outcome=failure / action_prefix=admin.impersonation params with a working active state and applied_chip surfacing.
- The explorer tone uses one unified body; local row_tone/1 is retired.
</success_criteria>

<output>
Create `.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-02-SUMMARY.md` when done.
</output>
