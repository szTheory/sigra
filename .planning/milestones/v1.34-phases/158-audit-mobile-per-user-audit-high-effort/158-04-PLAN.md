---
phase: 158-audit-mobile-per-user-audit-high-effort
plan: 04
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - lib/sigra/admin/live/user_show_live.ex
autonomous: true
requirements: [AUDX-03]
must_haves:
  truths:
    - "UserShowLive's 'Recent Audit' block renders through the shared compact audit_row/1 (show_detail=false, show_codes=false)"
    - "The old divergent audit_tone/1 in user_show_live.ex is retired — tone now comes from the unified audit_row tone source"
    - "Impersonation recent-audit rows now carry data-tone=info (the intended consolidation side effect), and this delta is anticipated, not a surprise"
  artifacts:
    - path: "lib/sigra/admin/live/user_show_live.ex"
      provides: "Recent Audit block routed through compact audit_row; old audit_tone/1 retired"
      contains: ".audit_row"
  key_links:
    - from: "user_show_live.ex Recent Audit block"
      to: "Components.audit_row/1 (compact)"
      via: "<.audit_row :for={row <- @detail.recent_audit} row={row} />"
      pattern: "\\.audit_row"
---

<objective>
Route the `UserShowLive` "Recent Audit" block through the shared compact `<.audit_row>` (defaults `show_detail=false`, `show_codes=false`), and retire the old divergent `audit_tone/1` so all three audit sites share one tone source. Explicitly account for the intended consolidation side effect: impersonation rows in this block will gain `data-tone="info"` where they had none (the old helper lacked the impersonation branch).

Purpose: AUDX-03 — the unified audit-row presentation is "also used by the user-detail 'recent audit' block." This is the third and final consumer site, and the one where the tone consolidation changes output (RESEARCH Pitfall #3 / D-10).

Output: modified `user_show_live.ex` with the compact audit_row block and the old audit_tone/1 removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-CONTEXT.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-UI-SPEC.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-PATTERNS.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-RESEARCH.md

<interfaces>
<!-- audit_row/1 compact variant (frozen by Plan 01 golden, characterized from THIS block's current bytes). -->
<!-- Source: lib/sigra/admin/components.ex -->
<.audit_row :for={row <- @detail.recent_audit} row={row} />   <!-- show_detail/show_codes default false -->

<!-- The block being replaced. Source: user_show_live.ex render/1 "Recent Audit" ~:264-272: -->
<!-- <article :for={row <- @detail.recent_audit} class="sg-list-row sg-stack sg-stack--2" data-tone={audit_tone(row)}> ... -->
<!--   inline Calendar.strftime(row.inserted_at, "%Y-%m-%d %H:%M") (no seconds, no helper) ... </article> -->

<!-- The old DIVERGENT tone helper to retire. Source: user_show_live.ex defp audit_tone/1 ~:437-440 -->
<!-- It returns nil for impersonation rows (no action_badge branch). The unified helper adds impersonation → "info". -->
<!-- => impersonation recent-audit rows GAIN data-tone="info". Possible user-detail baseline delta (handled in Plan 05). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Recent Audit block through compact audit_row and retire the old audit_tone/1</name>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex (the file being modified: the "Recent Audit" `<article>` block ~:264-272 with inline `Calendar.strftime(..., "%Y-%m-%d %H:%M")`; the `<.empty_state>` already wrapping the zero-rows case ~:273 — KEEP it; the "View full audit" CTA ~:259 — do not change; the old `defp audit_tone/1` ~:437-440; the import of `Sigra.Admin.Components`)
    - lib/sigra/admin/components.ex (the compact audit_row/1 from Plan 01 — its compact golden was characterized from THIS block's current bytes, so the swap is behavior-preserving EXCEPT for the impersonation tone change)
    - .planning/phases/158-.../158-PATTERNS.md (user_show_live.ex section: "Self-analog" swap instruction + the "Tone behavior change (Pitfall #3)" block)
    - .planning/phases/158-.../158-RESEARCH.md (## Common Pitfalls → Pitfall 3: the consolidation changes UserShow output for impersonation rows; flag a POSSIBLE user-detail re-record)
    - .planning/phases/158-.../158-UI-SPEC.md (## Copywriting Contract: "No recent audit activity" empty-state and "View full audit" CTA are existing — do not change)
  </read_first>
  <action>
    Replace the inline "Recent Audit" `<article :for={row <- @detail.recent_audit} ...>` block (~:264-272) with `<.audit_row :for={row <- @detail.recent_audit} row={row} />` (compact variant — show_detail and show_codes default false, so the Actor/Effective-user/id/action lines stay omitted and the timestamp stays "%Y-%m-%d %H:%M" via the component's format_date). Keep the surrounding `<.empty_state>` zero-rows wrapper (~:273) and the "View full audit" CTA (~:259) exactly as-is. Delete the now-unused `defp audit_tone/1` (~:437-440) — tone is owned by the component. EXPECT the intended side effect: any impersonation row in `@detail.recent_audit` now renders `data-tone="info"` (the old local helper returned nil for these). Record in the SUMMARY whether the seeded/journey `targetEmail` recent-audit actually contains impersonation rows so Plan 05 knows whether the `user-detail` baseline needs a deliberate re-record (RESEARCH Pitfall #3 / Assumption A2). Do NOT add any new tone helper here (a 4th helper re-creates the drift — D-10 hard-fail).
  </action>
  <verify>
    <automated>mix test test/sigra/admin/live/user_show_live_test.exs || mix test test/sigra/admin/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.audit_row" lib/sigra/admin/live/user_show_live.ex` shows `<.audit_row :for={row <- @detail.recent_audit} row={row} />` (compact — no `show_detail`/`show_codes` passed).
    - `grep -c "defp audit_tone" lib/sigra/admin/live/user_show_live.ex` == 0 (old divergent helper deleted).
    - The inline recent-audit `<article ... class="sg-list-row sg-stack sg-stack--2" data-tone={audit_tone(row)}>` is gone: `grep -c "data-tone={audit_tone" lib/sigra/admin/live/user_show_live.ex` == 0.
    - The empty-state wrapper and "View full audit" CTA are preserved: `grep -c "View full audit" lib/sigra/admin/live/user_show_live.ex` >= 1 and the recent-audit `<.empty_state` (or "No recent audit activity") remains.
    - No new tone helper added: `grep -c "defp row_tone\|defp audit_tone\|defp recent_tone" lib/sigra/admin/live/user_show_live.ex` == 0.
    - No `raw/1` introduced: `grep -c "raw(" lib/sigra/admin/live/user_show_live.ex` does not increase.
    - `mix compile --warnings-as-errors` succeeds (no unused-function warning from the deleted audit_tone/1).
    - The SUMMARY explicitly states whether targetEmail's recent-audit contains impersonation rows (the user-detail baseline-delta determination for Plan 05).
  </acceptance_criteria>
  <done>The Recent Audit block renders via the compact audit_row; the old audit_tone/1 is deleted; the empty-state and CTA are unchanged; the impersonation→info tone change is documented for Plan 05's baseline decision; no new tone helper exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host audit data → compact audit_row/1 | Recent-audit metadata rendered into the user-detail page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-09 | Tampering (XSS) | compact audit_row/1 in user-detail | mitigate | Recent-audit fields render via the golden-tested HEEx-auto-escaped `audit_row/1`; no `raw/1` introduced (acceptance criterion). The block is read-only. |
| T-158-10 | Information Disclosure | recent_audit_preview scope | accept | `@detail.recent_audit` is produced by the existing `Detail.recent_audit_preview/3` scoping — this plan changes only presentation, not the data source or its scope. |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero packages installed. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (deleted audit_tone/1 leaves no unused-function warning).
- `mix test test/sigra/admin/` green.
- No new tone helper; no `raw/1`; empty-state + CTA preserved.
</verification>

<success_criteria>
- The user-detail "Recent Audit" block is the third consumer of the single audit_row component.
- The old divergent audit_tone/1 is gone; tone is unified.
- The intended impersonation→info tone delta is documented so Plan 05 can decide on a deliberate user-detail re-record.
</success_criteria>

<output>
Create `.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-04-SUMMARY.md` when done.
</output>
