---
phase: 157-overview-landings-highest-effort
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/live/organization_live.ex
  - guides/reference/admin-design-contract.md
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "Org Overview mount/3 is split: disconnected path assigns loading=true, no DB queries; connected path calls Query.summary_counts/2 + Detail.member_roster/2 + Detail.pending_invitations/2, assigns loading=false"
    - "organization_name/1 helper is called BEFORE the connected? gate (no DB call — reads struct fields only)"
    - "old 'Scoped attention' card (organization_live.ex lines 59-84) is deleted entirely — this removes the nested-<p> notice at lines 73-78 (D-07 fold)"
    - "alarm <.notice> is the first child after <header class=sg-page-header> — above the task_card grid — with tone={:risk} when needs_review > 0 and tone={:ok} when 0"
    - "alarm notice renders only when NOT loading; no role=status in skeleton state"
    - "old sg-posture-strip__risk anchor block (lines 86-94) is deleted"
    - "posture strip <section> carries aria-busy on the section wrapping all deferred data regions when loading"
    - "5 skeleton shapes in posture strip cluster when loading (matching 5 stat_links: Users/Confirmed/MFA/Passkeys/Locked)"
    - "Members and Pending invitations sections show skeleton placeholder rows when loading (3 for Members, 2 for invitations)"
    - "Members roster (lines 125-141) and Pending invitations (lines 143-162) are preserved as demoted scoped-detail tail BELOW the shared archetype (D-05)"
    - "no block <p> children in any <.notice> slot content — inline only (D-07)"
    - "admin-design-contract.md Overview Archetype section updated with new archetype order and one-line Org-variant note"
  artifacts:
    - path: "lib/sigra/admin/live/organization_live.ex"
      provides: "Redesigned Org Overview with connected?-gate deferred load, front-door archetype, and preserved scoped-detail tail"
      contains: "connected?(socket)"
    - path: "guides/reference/admin-design-contract.md"
      provides: "Updated Overview Archetype section documenting Phase 157 redesign"
      contains: "Org appends a demoted scoped-detail tail"
  key_links:
    - from: "organization_live.ex mount/3"
      to: "Query.summary_counts/2 + Detail.member_roster/2 + Detail.pending_invitations/2"
      via: "connected? gate — all three queries only on connected mount"
      pattern: "if connected\\?\\(socket\\)"
    - from: "organization_live.ex render/1"
      to: "sg-posture-strip section + members section + invitations section"
      via: "aria-busy driven by @loading"
      pattern: "aria-busy"
---

<objective>
Redesign `lib/sigra/admin/live/organization_live.ex` (Org Overview) into the shared front-door archetype: split `mount/3` with `connected?(socket)` gate for deferred load of three queries (LAND-04), delete the old "Scoped attention" card + the nested-`<p>` notice it contained (D-07 fold), promote the needs-review alarm `<.notice>` above the task grid (LAND-01), reorder to the canonical archetype (LAND-02/03), preserve the Members + Pending invitations demoted tail (D-05), and update the design contract doc with the new archetype.

Purpose: Org Overview must share the identical front-door rhythm as Global Overview (LAND-03), with a deferred-load skeleton covering three data regions and a structural fix for the nested-`<p>` axe failure.

Output: Redesigned `organization_live.ex`; one-line Org-variant note in `admin-design-contract.md`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md

@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-UI-SPEC.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md

<interfaces>
<!-- Existing OrganizationLive — full source. -->

From lib/sigra/admin/live/organization_live.ex (current):
- mount/3 lines 14-28: synchronous — calls all three queries unconditionally. Must be split.
- render/1 lines 30-165:
  - HEADER: lines 35-42 (sg-page-header, org name kicker/title/copy — KEEP unchanged)
  - TASK CARD GRID: lines 44-57 (sg-grid sg-grid--2, 2 task_cards — KEEP, now second after alarm)
  - "SCOPED ATTENTION" CARD: lines 59-84 — DELETE ENTIRELY (D-07 fold; contains nested-<p> notice at lines 73-78)
  - sg-posture-strip__risk ANCHOR: lines 86-94 — DELETE (Landmine 4)
  - POSTURE STRIP sg-cluster: lines 96-118 with 5 stat_links (Users/Confirmed/MFA/Passkeys/Locked)
  - POSTURE STRIP tail copy: lines 120-122 (sg-section-copy "Bounded to…") — keep as-is
  - MEMBERS section: lines 125-141 — KEEP as demoted tail (D-05)
  - PENDING INVITATIONS section: lines 143-162 — KEEP as demoted tail (D-05)
- organization_name/1 lines 187-189: reads only struct fields — NO DB call; safe before gate (A1)
- users_path/1 lines 191-193, audit_path/1 lines 194-196: slug-only helpers; safe before gate
- needs_review/1 lines 197-199: locked + deleted; unchanged
- locked_summary/1 lines 201-202: used only in the "Scoped attention" card (being deleted); can be removed
- runtime_config!/0 lines 204-218: call BEFORE gate

From lib/sigra/admin/components.ex:
- notice/1 line 301-307: wraps slot in `<p class="sg-text-sm">` — INLINE slot content ONLY
  Pass `role="status"` via `:rest` only when NOT loading
- skeleton/1 line 327-330: `<div class={["sg-skeleton", @class]} {@rest}></div>`
- stat_link/1 line 48-55: renders .sg-metric-link__value span (Playwright wait selector)

Deferred data regions for Org (3 regions, wider than Global):
- Posture strip: 5 stat_links (Total/Confirmed/MFA/Passkeys/Locked) → 5 skeleton shapes when loading
- Members section: body list → 3 skeleton shapes when loading (representative count, discretion per CONTEXT.md)
- Pending invitations section: body list → 2 skeleton shapes when loading

aria-busy scope (D-02, RESEARCH open question resolved):
- Single aria-busy="true" on the outermost container wrapping all three deferred sections
  OR on the posture strip <section> only (posture strip is the most semantically clear boundary)
- Simplest approach: add aria-busy to the posture strip <section> AND add it to a wrapper <section>
  that contains the members + invitations tail, or add to each section individually
- Recommendation from RESEARCH.md: single top-level aria-busy is simpler — add it to the
  <section class="sg-stack sg-stack--6"> itself, or to a new wrapper <section> around the three
  deferred sections. Claude's discretion per CONTEXT.md.

admin-generated parity concern (Landmine 6):
- admin-generated.spec.ts:83-86 asserts `main` contains orgName — orgName persists in <h1>
- admin_shell_test.exs:55-60 asserts "Organization overview" (kicker, unchanged) + "Work inside this organization scope" (sg-page-copy, unchanged)
- The strings "Scoped attention", "Risk queue", "Evidence boundary" are NOT asserted in any existing test (A3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split mount/3 with connected? gate across three deferred queries</name>
  <files>lib/sigra/admin/live/organization_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/organization_live.ex — lines 1-28 (aliases + current mount to replace)
    - test/example/lib/example_web/live/confirmation_live.ex — line 103 (connected? gate precedent)
    - .planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md — Landmine 5 (correct mount/3 structure), Edit Sequence 1b
  </read_first>
  <action>
    Replace `mount/3` in `lib/sigra/admin/live/organization_live.ex` (lines 14-28) with a two-branch connected?-gate structure.

    Structure (per D-01, Landmine 5):
    1. `config = runtime_config!()` — BEFORE the gate; raises on misconfiguration for both mounts.
    2. `admin_scope = socket.assigns.admin_scope` — before the gate.
    3. `organization_name = organization_name(admin_scope)` — before the gate; reads struct fields only, no DB (A1 confirmed: lines 187-189 read only struct fields).
    4. Assign shared base assigns to socket before the gate: `sigra_config: config`, `organization_name: organization_name`, `page_title: "#{organization_name} overview"`.
    5. `if connected?(socket)` branch: call all three queries inline — `Query.summary_counts(config, admin_scope)`, `Detail.member_roster(config, admin_scope)`, `Detail.pending_invitations(config, admin_scope)` — assign results + `loading: false`. Return `{:ok, socket}`.
    6. `else` branch: assign `loading: true`, `summary_counts: %{}`, `members: []`, `pending_invitations: []`. Return `{:ok, socket}`.

    Hard-fail guards:
    - Do NOT call any of the three queries in the else branch.
    - Do NOT move `runtime_config!()`, `organization_name/1`, or base page assigns inside either branch.
    - `locked_summary/1` is only used by the "Scoped attention" card (being deleted in Task 2) — can be kept as dead code for now or deleted; safe either way.
  </action>
  <verify>
    <automated>mix test test/example/test/example_web/admin_shell_test.exs</automated>
  </verify>
  <done>
    mix test passes for admin_shell_test.exs. Existing assertions for "Organization overview", "Work inside this organization scope", and org name remain green. mount/3 contains `if connected?(socket)` with the three queries in the connected branch only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign render/1 — delete old card, front-door archetype, skeleton shapes for 3 regions</name>
  <files>lib/sigra/admin/live/organization_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/organization_live.ex — lines 30-165 (full render to replace)
    - lib/sigra/admin/components.ex — lines 264-331 (notice/1 at 301-307: inline slot only; skeleton/1 at 327-330)
    - .planning/phases/157-overview-landings-highest-effort/157-UI-SPEC.md — Component Inventory archetype rhythm; Copywriting Contract (alarm copy, members/invitations empty-state copy); Loading Contract
    - .planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md — Landmine 1 (nested-p), Landmine 3 (live-region), Landmine 4 (anchor deletion), Landmine 7 (dark mode), Landmine 8 (skeleton shapes — 5 posture, 3 members, 2 invitations)
  </read_first>
  <action>
    Rewrite `render/1` in `lib/sigra/admin/live/organization_live.ex` to implement the shared front-door archetype (D-04/D-05) and deferred load state (D-01/D-02).

    Keep the `assigns` precompute: `assigns = assign(assigns, :needs_review, needs_review(assigns.summary_counts))`

    Archetype structure inside `<section class="sg-stack sg-stack--6">`:

    ITEM 1 — `<header class="sg-page-header">` (unchanged from current lines 35-42):
    - kicker "Organization overview", title `{@organization_name}`, copy "Work inside this organization scope…" — all unchanged.

    ITEM 2 — alarm `<.notice>` (NEW per D-03 — before task grid):
    - `:if={not @loading}` — do NOT render alarm during skeleton state (Landmine 3)
    - `tone={if @needs_review > 0, do: :risk, else: :ok}`
    - `role="status"` via `:rest` (D-02 — post-load dynamic count)
    - Inline slot content ONLY (D-07, Landmine 1): when risk: `{@needs_review} accounts need review — <a href={users_path(@admin_scope) <> "?locked=true"}>Review now</a>`; when ok: `"All clear"`. No `<p>` as child.

    ITEM 3 — task_card grid (unchanged content, now clearly PRIMARY):
    - `<div class="sg-grid sg-grid--2">` with 2 `<.task_card>` (Support members, Investigate org events — unchanged from lines 44-57).

    DELETE — entire "Scoped attention" card (lines 59-84) — GONE. This is the D-07 fold: the nested-`<p>` `<.notice>` at lines 73-78 disappears with the card.

    ITEM 4 — demoted posture strip with skeleton shapes:
    - `<section class="sg-card sg-posture-strip sg-stack sg-stack--3">` — add `aria-busy={if @loading, do: "true"}` (D-02).
    - DELETE the `<a ... class="sg-cluster sg-cluster--start sg-posture-strip__risk">` block (lines 86-94) — DO NOT carry forward (Landmine 4).
    - `<div class="sg-cluster sg-cluster--3">`:
      - When `@loading`: render 5 `<.skeleton>` shapes (matching 5 stat_links: Users, Confirmed, MFA, Passkeys, Locked) — Landmine 8.
      - When NOT loading: render the 5 `<.stat_link>` components (from current lines 97-117, using `users_path(@admin_scope)` deep-links).
    - Keep the `<p class="sg-section-copy">` "Bounded to this org…" at lines 120-122 (not a deferred-data element).

    ORG-ONLY TAIL (D-05 — demoted scoped-detail, BELOW shared archetype):
    MEMBERS section `<section class="sg-card sg-stack sg-stack--3">`:
    - Heading `<h2 class="sg-section-heading">Members</h2>` (always visible — heading is not deferred data).
    - When `@loading`: render 3 `<.skeleton>` shapes as placeholder rows (Landmine 8).
    - When NOT loading: the existing `:if={@members == []}` empty copy and `:if={@members != []}` list (from current lines 127-140) — unchanged.

    PENDING INVITATIONS section `<section class="sg-card sg-stack sg-stack--3">`:
    - Heading `<h2 class="sg-section-heading">Pending invitations</h2>` (always visible).
    - When `@loading`: render 2 `<.skeleton>` shapes as placeholder rows (Landmine 8).
    - When NOT loading: the existing `:if={@pending_invitations == []}` empty copy and `:if={@pending_invitations != []}` list (from current lines 145-161) — unchanged.

    Hard-fail boundary checks:
    - `sg-posture-strip__risk` must NOT appear anywhere in rendered output.
    - "Scoped attention", "Risk queue", "Evidence boundary" must NOT appear (old card gone).
    - All `<.notice>` slot content is inline (no nested `<p>`).
    - The members + invitations sections are BELOW the posture strip (demoted tail).
    - `{@organization_name}` still appears in the `<h1>` (parity lane check, Landmine 6).
    - If `locked_summary/1` becomes unreferenced after the card deletion, either remove it or leave it as dead code (compiler will warn on unused private; remove to keep clean).
  </action>
  <verify>
    <automated>mix test test/example/test/example_web/admin_shell_test.exs && grep -v "^[[:space:]]*#" lib/sigra/admin/live/organization_live.ex | grep -c "sg-posture-strip__risk"</automated>
  </verify>
  <done>
    mix test passes for admin_shell_test.exs. grep count for sg-posture-strip__risk returns 0. "Scoped attention" card absent from file. Members and pending invitations sections present as demoted tail. mix test (full suite) passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update admin-design-contract.md Overview Archetype with Phase 157 archetype and Org-variant note</name>
  <files>guides/reference/admin-design-contract.md</files>
  <read_first>
    - guides/reference/admin-design-contract.md — lines 135-167 (Overview Archetype section to update)
    - .planning/phases/157-overview-landings-highest-effort/157-UI-SPEC.md — Component Inventory archetype rhythm block (the new canonical structure)
    - .planning/phases/157-overview-landings-highest-effort/157-CONTEXT.md — D-05 (Org-variant note wording)
  </read_first>
  <action>
    Update the "Overview Archetype" section of `guides/reference/admin-design-contract.md` (currently around lines 135-167) to reflect the Phase 157 redesign.

    Three changes only — this is a doc update, not a structural overhaul:

    1. Replace the "Current component composition" code block with the new Phase 157 archetype, matching the structure in UI-SPEC.md Component Inventory:
       ```
       <section class="sg-stack sg-stack--6">
         <header class="sg-page-header">                          [1] open header
           <p class="sg-page-kicker">
           <h1 class="sg-page-title">
           <p class="sg-page-copy">

         <.notice tone={:risk|:ok} role="status">                [2] LOUD ALARM — first after header
           ...inline alarm content (count + deep-link or "All clear")...
         </.notice>  (:if={not @loading})

         <div class="sg-grid sg-grid--{3|2}">                    [3] PRIMARY content — task_card grid
           task_card x N

         <section class="sg-card sg-posture-strip sg-stack sg-stack--3" aria-busy={...}>  [4] demoted stat_link strip
           <div class="sg-cluster sg-cluster--3">
             stat_link x N  (or skeleton x N when loading)

         <section class="sg-stack sg-stack--3">                  [5] Global only — capability matrix
           capability x N

         ── Org only — demoted scoped-detail tail (below shared archetype) ──
         <section class="sg-card sg-stack sg-stack--3">          [Org] Members roster
         <section class="sg-card sg-stack sg-stack--3">          [Org] Pending invitations
       ```

    2. Add one-line Org-variant note immediately after the archetype block (per D-05):
       "**Org variant:** Items 1–4 are byte-coherent across Global and Org Overviews. Org appends a demoted scoped-detail tail (Members roster + Pending invitations) below the shared front-door archetype. This tail is Org-only; it is NOT part of the shared archetype. No `page_back` on Overview screens (leaf-only constraint)."

    3. Update the "Notes" block below the archetype (currently lines 163-165) to remove the stale "Document current state" note about the needs-review alarm being a `sg-status-pill` inside `sg-posture-strip` — that is now the NEW state. Replace with a note that the alarm is now `<.notice tone={:risk|:ok}>` as the first child after the header, with `role="status"` opt-in for the post-load dynamic count (Phase 157 implementation).

    No other sections of the design contract file are modified.
  </action>
  <verify>
    <automated>grep -c "Org appends a demoted scoped-detail tail" guides/reference/admin-design-contract.md</automated>
  </verify>
  <done>
    grep returns 1 (the Org-variant note is present). The Overview Archetype code block in the contract reflects the new Phase 157 order (alarm before task grid). The stale "Document current state" note about sg-posture-strip__risk alarm is removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser → Org LiveView mount | HTTP/WS authenticated by existing on_mount hooks (scope: organization) |
| admin LiveView → DB queries (3) | All three queries run only on connected mount |
| alarm deep-link → filtered users route | Navigates within already-authenticated org admin scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-02a | Information Disclosure | Skeleton state — Org | accept | Loading state assigns members:[], pending_invitations:[], summary_counts:%{} — no org member data, invitation emails, or counts exposed on HTTP mount. |
| T-157-02b | Tampering | `users_path(@admin_scope) <> "?locked=true"` deep-link | accept | Link navigates within org-scoped admin, enforced by on_mount hooks on destination route. No session mutation. |
| T-157-02c | Information Disclosure | `organization_name/1` on disconnected mount | accept | Reads from admin_scope struct (already in assigns from on_mount hook). No DB query on disconnected mount; org name is already-authorized context from the session. |
| T-157-02d | Spoofing | Nested-<p> axe violation (D-07 fold) | mitigate | Delete the entire "Scoped attention" card containing the offending notice; new alarm notice carries inline content only. Axe WCAG A/AA gate in Playwright `assertNoAxeViolations` on `org-overview` slug catches any regression. |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
After all three tasks complete:
- `mix test test/example/test/example_web/admin_shell_test.exs` — existing assertions green ("Organization overview", "Work inside this organization scope", org name present)
- `mix test` — full suite green
- `grep -v "^[[:space:]]*#" lib/sigra/admin/live/organization_live.ex | grep "sg-posture-strip__risk"` — returns empty
- `grep "Scoped attention\|Risk queue\|Evidence boundary" lib/sigra/admin/live/organization_live.ex` — returns empty
- `grep "connected?(socket)" lib/sigra/admin/live/organization_live.ex` — returns match
- `grep "Org appends a demoted scoped-detail tail" guides/reference/admin-design-contract.md` — returns match
- `grep "organization_name" lib/sigra/admin/live/organization_live.ex | head -5` — `{@organization_name}` present in <h1> (parity lane guard)
</verification>

<success_criteria>
- `organization_live.ex` `mount/3` has connected? gate; three queries only in connected branch; disconnected assigns loading:true, empty structs.
- "Scoped attention" card (lines 59-84) is absent; nested-`<p>` notice gone (D-07 fold).
- `sg-posture-strip__risk` anchor absent from entire file.
- Alarm `<.notice>` with `tone={:risk|:ok}` and inline content is first child after header, rendered only when NOT loading.
- 5 skeleton shapes in posture strip cluster when loading; 3 skeleton shapes in members section; 2 in invitations section.
- Members roster + pending invitations preserved as demoted tail below posture strip.
- `admin-design-contract.md` Overview Archetype section reflects new order with Org-variant note.
- `mix test test/example/test/example_web/admin_shell_test.exs` green.
</success_criteria>

<output>
Create `.planning/phases/157-overview-landings-highest-effort/157-02-SUMMARY.md` when done.
</output>
