---
phase: 157-overview-landings-highest-effort
plan: 03
type: execute
wave: 2
depends_on:
  - 157-01
  - 157-02
files_modified:
  - test/example/test/example_web/admin_shell_test.exs
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "describe 'Phase 157 Overview redesign' block exists in admin_shell_test.exs"
    - "ConnCase GET /admin returns skeleton (sg-skeleton present, aria-busy present, no sg-metric-link__value)"
    - "live/2 /admin returns data (sg-metric-link__value present, no sg-skeleton, no aria-busy, alarm notice present with role=status)"
    - "ConnCase GET /admin/organizations/:slug returns skeleton (sg-skeleton present, aria-busy present, no sg-metric-link__value)"
    - "live/2 /admin/organizations/:slug returns data (sg-metric-link__value present, no sg-skeleton, alarm notice with role=status)"
    - "task_card grid appears BEFORE posture strip in rendered HTML offset order (both screens)"
    - "alarm notice HTML offset is BEFORE task grid HTML offset in connected render (both screens)"
    - "sg-posture-strip__risk is absent from both screens (GET and live/2)"
    - "full mix test suite green"
  artifacts:
    - path: "test/example/test/example_web/admin_shell_test.exs"
      provides: "describe block with two-state proof for LAND-04 and archetype-order assertions for LAND-01..03"
      contains: "Phase 157 Overview redesign"
  key_links:
    - from: "test describe block"
      to: "ConnCase GET /admin"
      via: "conn |> log_in_user(admin) |> get(~p\"/admin\") |> html_response(200)"
      pattern: "sg-skeleton"
    - from: "test describe block"
      to: "LiveViewTest live/2 /admin"
      via: "conn |> log_in_user(admin) |> live(~p\"/admin\")"
      pattern: "sg-metric-link__value"
---

<objective>
Add `describe "Phase 157 Overview redesign"` to `test/example/test/example_web/admin_shell_test.exs` with the full two-state proof for LAND-04 and archetype-order assertions for LAND-01..03 on both Global and Org Overview screens.

Purpose: Wave 2 of the edit sequence per RESEARCH.md — ExUnit coverage must be green before Playwright baselines are recorded. RESEARCH finding #1: zero `live/2` connected-mount tests exist for either Overview screen today; this plan closes that gap.

Output: New `describe` block with 4 test cases covering skeleton state (GET) + data state (live/2) for both screens, plus archetype-order and alarm-position assertions.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md

@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-VALIDATION.md

@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-01-SUMMARY.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-02-SUMMARY.md

<interfaces>
<!-- Existing test file structure -->

From test/example/test/example_web/admin_shell_test.exs (current):
- Uses `ExampleWeb.ConnCase, async: false`
- Imports: `Phoenix.LiveViewTest`, `AccountsFixtures`, `Scope`, `AdminShell`, `Layouts`, `AuthErrorHandler`
- Existing describe "admin shell scope chrome" — uses `conn |> log_in_user(user) |> get(path)` pattern
- `html_offset/2` helper (lines 149-153): returns byte offset of needle in html, or nil; used for order assertions
- Fixture helpers: `AccountsFixtures.user_fixture/1`, `AccountsFixtures.create_organization/1`, `AccountsFixtures.create_membership/3`
- Platform admin convention: email starting with "platform-admin+" (see line 157 in checkpoint spec)

From RESEARCH.md — Validation Architecture: "How to Prove LAND-04 Distinctly":
Two-mount semantics:
  | conn |> get(path) → html_response(200) | connected?(socket) = false | skeleton state |
  | conn |> live(path) → {:ok, _view, html} | connected?(socket) = true  | data state    |

Specific assertion pairs that distinguish done from looks-done:
  # Skeleton state (disconnected — HTTP GET):
  assert html =~ "sg-skeleton"
  assert html =~ ~s(aria-busy="true")
  refute html =~ "sg-metric-link__value"  # stat_link values not yet present

  # Data state (connected — live/2):
  refute html =~ "sg-skeleton"
  refute html =~ ~s(aria-busy="true")
  assert html =~ "sg-metric-link__value"  # stat_link values present
  assert html =~ "sg-notice"             # alarm present
  assert html =~ ~s(role="status")       # live-region on alarm

Additional assertions per RESEARCH.md Wave 0 Gaps:
- Archetype order: task_card grid BEFORE posture strip (use html_offset/2)
- Alarm notice BEFORE task grid in connected render (use html_offset/2)
- refute html =~ "sg-posture-strip__risk" — old anchor gone (both states)

html_offset/2 is a private helper already in the file — reuse it for order assertions.
Offset patterns for order assertions:
- sg-grid sg-grid--3 (or sg-grid--2 for Org) = task card grid start
- sg-card sg-posture-strip = posture strip start
- sg-notice = alarm notice start

Platform admin fixture note: the existing ConnCase tests use `AccountsFixtures.user_fixture/1` with a platform-admin+ email prefix. The `ExampleWeb.SigraAdminPolicy` grants platform_admin? to users whose email starts with "platform-admin+" — use the same convention.

For Org tests: need create_organization/1 + create_membership/3 (as in lines 36-44 of current file).
Org path: `~p"/admin/organizations/#{organization.slug}"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 157 Overview redesign describe block with two-mount tests for Global and Org</name>
  <files>test/example/test/example_web/admin_shell_test.exs</files>
  <read_first>
    - test/example/test/example_web/admin_shell_test.exs — full file (155 lines; understand existing describe structure, fixture helpers, html_offset/2 pattern, ConnCase setup)
    - .planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md — "How to Prove LAND-04 Distinctly" section (exact assertion patterns), Wave 0 Gaps checklist
    - .planning/phases/157-overview-landings-highest-effort/157-VALIDATION.md — Per-Task Verification Map (required proof checklist)
  </read_first>
  <action>
    Append a new `describe "Phase 157 Overview redesign"` block to `test/example/test/example_web/admin_shell_test.exs`, BEFORE the `describe "denied states are not blank pages"` block (i.e., after line 124). Do NOT modify any existing test.

    The new block contains 4 tests organized as two pairs (Global + Org), each pair covering the disconnected (GET) and connected (live/2) mount states.

    Test structure:

    --- Global Overview ---

    test "global overview disconnected mount (GET) renders skeleton, not stat values" do
      Platform admin fixture: `AccountsFixtures.user_fixture(%{email: "platform-admin+157-global-skel-#{System.unique_integer([:positive])}@example.com"})`
      conn = build_conn() |> log_in_user(platform_admin) |> get(~p"/admin")
      html = html_response(conn, 200)
      assert html =~ "sg-skeleton"                    # LAND-04: skeleton present
      assert html =~ ~s(aria-busy="true")             # D-02: busy signal present
      refute html =~ "sg-metric-link__value"          # A2: stat values absent in skeleton
      refute html =~ "sg-posture-strip__risk"         # LAND-01 cleanup: old anchor gone
    end

    test "global overview connected mount (live/2) renders data, not skeleton" do
      Platform admin fixture (same email convention + unique integer).
      {:ok, _view, html} = build_conn() |> log_in_user(platform_admin) |> live(~p"/admin")
      refute html =~ "sg-skeleton"                    # LAND-04: no skeleton in data state
      refute html =~ ~s(aria-busy="true")             # D-02: busy gone when loaded
      assert html =~ "sg-metric-link__value"          # A2: stat values present
      assert html =~ "sg-notice"                      # LAND-01: alarm notice present
      assert html =~ ~s(role="status")                # D-02: live-region on alarm
      refute html =~ "sg-posture-strip__risk"         # LAND-01 cleanup
      # Archetype order: alarm notice BEFORE task grid (LAND-01/02)
      assert html_offset(html, "sg-notice") < html_offset(html, "sg-grid sg-grid--3")
      # Archetype order: task grid BEFORE posture strip (LAND-02)
      assert html_offset(html, "sg-grid sg-grid--3") < html_offset(html, "sg-card sg-posture-strip")
    end

    --- Org Overview ---

    test "org overview disconnected mount (GET) renders skeleton, not stat values" do
      Create org_admin user + organization + membership (same pattern as lines 36-44 of existing tests).
      org_admin = AccountsFixtures.user_fixture(%{email: "org-admin+157-org-skel-#{System.unique_integer([:positive])}@example.com"})
      organization = AccountsFixtures.create_organization(%{name: "Test Org 157", slug: "test-org-157-skel-#{System.unique_integer([:positive])}"})
      AccountsFixtures.create_membership(org_admin, organization, :admin)
      conn = build_conn() |> log_in_user(org_admin) |> get(~p"/admin/organizations/#{organization.slug}")
      html = html_response(conn, 200)
      assert html =~ "sg-skeleton"                    # LAND-04: skeleton present
      assert html =~ ~s(aria-busy="true")             # D-02
      refute html =~ "sg-metric-link__value"          # A2
      refute html =~ "sg-posture-strip__risk"         # LAND-01 cleanup
      refute html =~ "Scoped attention"               # D-07: old card gone
    end

    test "org overview connected mount (live/2) renders data, not skeleton" do
      Same org_admin + organization + membership setup.
      {:ok, _view, html} = build_conn() |> log_in_user(org_admin) |> live(~p"/admin/organizations/#{organization.slug}")
      refute html =~ "sg-skeleton"                    # LAND-04
      refute html =~ ~s(aria-busy="true")             # D-02
      assert html =~ "sg-metric-link__value"          # A2
      assert html =~ "sg-notice"                      # LAND-01
      assert html =~ ~s(role="status")                # D-02
      refute html =~ "sg-posture-strip__risk"         # LAND-01 cleanup
      refute html =~ "Scoped attention"               # D-07: old card gone
      # Archetype order: alarm BEFORE task grid (LAND-01/02)
      assert html_offset(html, "sg-notice") < html_offset(html, "sg-grid sg-grid--2")
      # Archetype order: task grid BEFORE posture strip (LAND-02)
      assert html_offset(html, "sg-grid sg-grid--2") < html_offset(html, "sg-card sg-posture-strip")
      # LAND-03: same archetype structure as Global (same section class anchors present)
      assert html =~ "sg-posture-strip"
      # D-05: Org tail preserved (members + pending invitations)
      assert html =~ "Members"
      assert html =~ "Pending invitations"
    end

    Notes for executor:
    - `html_offset/2` is already defined as a private helper in this file (lines 149-153). Call it directly — no re-definition needed.
    - `Phoenix.LiveViewTest.live/2` is already imported (line 10). Use `live(conn, path)` form.
    - Platform admin email prefix "platform-admin+" is the SigraAdminPolicy grant convention (see checkpoint spec line 157).
    - Org admin does NOT need a platform-admin+ prefix — the membership with :admin role grants org admin scope.
    - The `async: false` setting on the ConnCase means tests can use the Sandbox safely with DB queries.
    - For Org live/2, the members and invitations lists will be empty (no fixture members beyond the org_admin) — that is fine; the test asserts the headings are present and stat_link values are rendered (not empty).
  </action>
  <verify>
    <automated>mix test test/example/test/example_web/admin_shell_test.exs</automated>
  </verify>
  <done>
    All 4 new tests in the "Phase 157 Overview redesign" describe block pass. All existing tests in the file continue to pass. mix test (full suite) is green. The two-mount proof (skeleton on GET, data on live/2) is established for both Global and Org Overview.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → LiveView HTTP mount | ConnCase GET — disconnected mount, no DB queries beyond fixture setup |
| test → LiveView WS mount | Phoenix.LiveViewTest live/2 — connected mount, queries fire against test DB sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-03a | Information Disclosure | Test fixture data in connected mount | accept | Tests run in SQL Sandbox (ExUnit async:false). Fixture data isolated per-test. No real user PII in test assertions. |
| T-157-03b | Tampering | live/2 role="status" assertion | accept | Asserting `role="status"` is present confirms D-02 live-region is opt-in only for post-load-dynamic content; not a security control itself but confirms the ARIA contract holds. |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No new packages. Test-only file addition — no runtime surface change. |
</threat_model>

<verification>
After task complete:
- `mix test test/example/test/example_web/admin_shell_test.exs` — all tests green including 4 new ones
- `mix test` — full suite green (confirms Wave 1 implementations are consistent)
- `grep -c "Phase 157 Overview redesign" test/example/test/example_web/admin_shell_test.exs` — returns 1
- `grep -c "sg-skeleton" test/example/test/example_web/admin_shell_test.exs` — returns > 0 (new assertions)
- `grep -c "sg-metric-link__value" test/example/test/example_web/admin_shell_test.exs` — returns > 0 (new assertions)
</verification>

<success_criteria>
- `describe "Phase 157 Overview redesign"` block exists in admin_shell_test.exs with 4 tests.
- GET /admin asserts skeleton + aria-busy + no stat values (LAND-04 disconnected proof).
- live/2 /admin asserts stat values + no skeleton + alarm notice + role=status + correct order (LAND-04 connected proof + LAND-01/02/03).
- GET /admin/organizations/:slug and live/2 /admin/organizations/:slug have equivalent proofs.
- All 4 tests and all pre-existing tests pass.
- `mix test` (full suite) is green — prerequisite for Playwright wave.
</success_criteria>

<output>
Create `.planning/phases/157-overview-landings-highest-effort/157-03-SUMMARY.md` when done.
</output>
