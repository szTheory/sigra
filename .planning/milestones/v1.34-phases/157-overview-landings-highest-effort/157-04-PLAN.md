---
phase: 157-overview-landings-highest-effort
plan: 04
type: execute
wave: 3
depends_on:
  - 157-03
files_modified:
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-chromium-1.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-mobile-1.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-dark-1.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-chromium-1.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-mobile-1.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-dark-1.png
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "global-overview and org-overview checkpoint slugs added to admin-checkpoints.spec.ts using captureAndVerify + assertCheckpointScreenshot pattern"
    - "screenshot wait goes beyond waitForLiveViewReady — additionally waits for .sg-metric-link__value to be visible before capture (perpetual-flake guard)"
    - "global-overview captured AFTER admin login and BEFORE navigating to /admin/users (logical journey order)"
    - "org-overview captured AFTER createOrganization and before org-scoped-admin checkpoint"
    - "6 new PNG baselines committed (2 slugs × 3 projects: chromium, mobile, dark)"
    - "existing 5 slugs are NOT re-recorded — their PNGs are unchanged"
    - "axe WCAG A/AA green on both new slugs across all 3 projects (assertCheckpointScreenshot calls assertNoAxeViolations)"
    - "admin-generated parity lane stays green"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts"
      provides: "Two new checkpoint blocks for global-overview and org-overview"
      contains: "global-overview"
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/"
      provides: "6 new baseline PNGs"
      contains: "global-overview-admin-checkpoints-chromium-1.png"
  key_links:
    - from: "admin-checkpoints.spec.ts global-overview block"
      to: ".sg-metric-link__value locator"
      via: "expect.toBeVisible() wait BEFORE captureAndVerify"
      pattern: "sg-metric-link__value"
    - from: "assertCheckpointScreenshot"
      to: "assertNoAxeViolations"
      via: "called inside assertCheckpointScreenshot (line 130)"
      pattern: "assertNoAxeViolations"
---

<objective>
Add the two new Playwright checkpoint slugs `global-overview` and `org-overview` to the single authenticated journey in `test/example/priv/playwright/tests/admin-checkpoints.spec.ts`, commit the 6 initial baseline PNGs, and verify axe green across all 3 projects.

Purpose: LAND-05 / D-06 — these screens need their first committed baselines so regressions are detectable in future phases. The wait-for-loaded-data guard (Landmine 2) is load-bearing: without it the baseline freezes a skeleton frame and every subsequent run diffs.

Output: Updated spec with 2 new checkpoint blocks; 6 committed PNG baselines.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md

@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md

@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-03-SUMMARY.md

<interfaces>
<!-- Existing Playwright spec structure -->

From test/example/priv/playwright/tests/admin-checkpoints.spec.ts (current):
- waitForLiveViewReady/1 at lines 40-44: waits for `[data-phx-session].phx-connected` attached
  CRITICAL: this alone is NOT sufficient after D-01 deferred load — WebSocket connects BEFORE
  connected mount completes DB queries. Must add content-visible assertion after this call.
- captureAndVerify/3 at lines 94-109: captures + asserts file exists and is non-empty
- assertCheckpointScreenshot/3 at lines 129-145: calls assertNoAxeViolations then toHaveScreenshot
- assertNoAxeViolations/2 at lines 112-123: scoped to wcag2a + wcag2aa tags only
- Journey flow (current): seed fixtures → login as admin → CP1 global-user-index → CP2 user-detail
  → CP3 org-scoped-admin (/admin/organizations/:slug/users) → impersonation → CP4 impersonation-banner
  → stop impersonation → CP5 audit-explorer
- orgSlug variable: `const orgSlug = \`checkpoint-org-${suffix}\`` (line 159); org is created by
  `createOrganization` call at line 164
- CP3 current comment at line 199-203: "Use /admin/organizations/:slug/users so reviewers see a
  dense data-rich org-scoped layout rather than the org landing stub." — Phase 157 creates the
  REAL org overview, so the NEW org-overview checkpoint uses /admin/organizations/:slug (root path)

Insertion points (per RESEARCH.md Journey Position recommendation):
- Insert `global-overview` BEFORE CP1 global-user-index (right after admin login at line 170):
  nav to /admin, waitForLiveViewReady, wait for .sg-metric-link__value visible, captureAndVerify, assertCheckpointScreenshot
- Insert `org-overview` AFTER createOrganization at line 164 AND after clearing the user session
  and logging in as admin — but the org exists from the target user's createOrganization call.
  Actually: the org exists from lines 162-164. After admin registers (line 169), the admin can
  nav to /admin/organizations/:slug. Insert org-overview AFTER global-overview capture, BEFORE
  navigating to CP1 global-user-index.
  OR: insert org-overview BETWEEN CP2 (user-detail) and CP3 (org-scoped-admin) — natural journey.
  
  RESEARCH.md recommendation (p.549): capture global-overview first (right after login, before
  /admin/users navigation), then org-overview after createOrganization is confirmed seeded.
  Since the org is seeded before admin login (line 164), and admin is logged in at line 169,
  org-overview CAN be captured immediately after global-overview.

Wait selector for loaded data (D-06, Landmine 2):
  ```typescript
  await waitForLiveViewReady(page);
  // D-06 HARD REQUIREMENT: wait for real loaded data (not skeleton frame)
  await expect(page.locator('.sg-metric-link__value').first()).toBeVisible();
  // THEN capture
  await captureAndVerify(page, testInfo, 'global-overview');
  await assertCheckpointScreenshot(page, testInfo, 'global-overview');
  ```
  The `.sg-metric-link__value` selector is guaranteed absent in skeleton state (A2) and
  present in data state. The 15,000ms expect.timeout in playwright.config.ts:55 is sufficient.

Snapshot recording approach (per RESEARCH.md Wave 3 step 3):
  Do NOT run --update-snapshots globally (would re-record existing 5 slugs).
  Use slug-specific approach: run `npx playwright test --grep "global-overview|org-overview"
  --update-snapshots` OR run the full spec with --update-snapshots then restore the existing
  5 PNGs from git. The safest approach: record ONLY the new slugs by running the spec normally
  (without --update-snapshots) and checking whether the new slugs fail with "missing snapshot"
  error — if so, run with --update-snapshots --grep pattern. Or: run with --update-snapshots
  for the full run, then `git checkout` the existing 5 PNGs to restore them.

Existing PNG count: 15 (5 slugs × 3 projects). After this plan: 21 (7 slugs × 3 projects).
Snapshot directory: test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/

admin-generated parity check:
  admin-generated.spec.ts:83-86 asserts org name in main — preserved by Task 3 of plan 02.
  Run: `cd test/example && npx playwright test tests/admin-generated.spec.ts --project admin-generated`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global-overview and org-overview checkpoint blocks to admin-checkpoints.spec.ts</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts — full file (256 lines); understand the journey flow, fixture seeding, captureAndVerify + assertCheckpointScreenshot two-call pattern, orgSlug variable scope
    - .planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md — Landmine 2 (perpetual-flake baseline), Landmine 6 (parity lane), Wave 3 step 2 code example, Journey Position recommendation (Open Questions item 3)
    - .planning/phases/157-overview-landings-highest-effort/157-CONTEXT.md — D-06 (wait-for-loaded-data hard requirement)
  </read_first>
  <action>
    Insert two new checkpoint blocks into the authenticated journey in `admin-checkpoints.spec.ts`.

    INSERTION POINT: After the admin login at line 169 (`await registerUser(page, adminEmail, password);`) and BEFORE the current Checkpoint 1 navigation at line 175 (`await page.goto(...)`).

    Block 1 — global-overview (navigates to /admin):
    ```
    // --- Checkpoint: Global overview (/admin) --------------------------------
    // Phase 157 LAND-01..04: front-door archetype with deferred load.
    // D-06 HARD REQUIREMENT: wait for loaded data (sg-metric-link__value visible),
    // not just .phx-connected — connected? gate defers queries to connected mount,
    // creating a brief window where .phx-connected is set but skeleton is still shown.
    await page.goto('/admin');
    await waitForLiveViewReady(page);
    await expect(page.locator('.sg-metric-link__value').first()).toBeVisible();
    await expect(page.locator('.sg-notice').first()).toBeVisible();
    await captureAndVerify(page, testInfo, 'global-overview');
    await assertCheckpointScreenshot(page, testInfo, 'global-overview');
    ```

    Block 2 — org-overview (navigates to /admin/organizations/:slug):
    Insert AFTER global-overview block and BEFORE the Checkpoint 1 navigation.
    The orgSlug variable is defined at line 159 — it is in scope here.
    ```
    // --- Checkpoint: Org overview (/admin/organizations/:slug) ---------------
    // Phase 157 LAND-01..04: same front-door archetype, org scope.
    // Org route: redesigned OrganizationLive (not the "org landing stub" referenced
    // in CP3 comment — that was before Phase 157 created this baseline).
    await page.goto(`/admin/organizations/${orgSlug}`);
    await waitForLiveViewReady(page);
    await expect(page.locator('.sg-metric-link__value').first()).toBeVisible();
    await expect(page.locator('.sg-notice').first()).toBeVisible();
    await captureAndVerify(page, testInfo, 'org-overview');
    await assertCheckpointScreenshot(page, testInfo, 'org-overview');
    ```

    Hard-fail guards:
    - Do NOT remove or modify any existing checkpoint block (CP1..CP5 at lines 171-253).
    - Do NOT add --update-snapshots to any npm script or CI config — that is handled separately in Task 2.
    - The `.sg-metric-link__value` wait MUST come after `waitForLiveViewReady` (Landmine 2) — never before.
    - The `.sg-notice` visibility wait confirms the alarm rendered (additional data-state confirmation).
    - Use `page.locator('.sg-metric-link__value').first()` — the `.first()` is necessary because multiple stat_links render after load and the locator must resolve to one element.
    - The `orgSlug` variable is declared at line 159 (`const orgSlug = ...`) and is in scope throughout the entire test function — confirm this before inserting.
  </action>
  <verify>
    <automated>grep -E "global-overview|org-overview" /Users/jon/projects/sigra/test/example/priv/playwright/tests/admin-checkpoints.spec.ts | wc -l</automated>
  </verify>
  <done>
    grep count is >= 8 (each slug appears in: page.goto, waitForLiveViewReady context, captureAndVerify call, assertCheckpointScreenshot call — 4 references × 2 slugs = 8 minimum). The spec file has no TypeScript errors (npx tsc --noEmit confirms). Existing checkpoint blocks (global-user-index, user-detail, org-scoped-admin, impersonation-banner, audit-explorer) are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record initial PNG baselines for global-overview and org-overview, verify axe + parity</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-chromium-1.png
test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-mobile-1.png
test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-overview-admin-checkpoints-dark-1.png
test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-chromium-1.png
test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-mobile-1.png
test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-overview-admin-checkpoints-dark-1.png</files>
  <read_first>
    - test/example/priv/playwright/playwright.config.ts — confirm 3 admin-checkpoints projects (chromium, mobile, dark) and their names (used for PNG filenames)
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ — list existing PNGs to confirm 15 exist before this task and that global-overview + org-overview PNGs are absent
    - .planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md — Wave 3 step 3 (snapshot recording approach); Landmine 2 (flake guard — already in place from Task 1)
  </read_first>
  <action>
    Record initial baselines for the two new slugs only, then verify the existing 5 slugs are unchanged and axe is green.

    Step 1 — Confirm pre-conditions:
    - Run `ls test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png | wc -l` — should be 15 (5 slugs × 3 projects).
    - Confirm `global-overview-*.png` and `org-overview-*.png` do NOT exist yet.

    Step 2 — Start the Phoenix dev server for the example app:
    - The Playwright tests require a running server. Check if there is a script (e.g. `scripts/uat/up.sh` or `mix phx.server` in `test/example/`). Run the dev server at the expected port (check `playwright.config.ts` for `baseURL`). Use a background process.

    Step 3 — Record new baselines (slug-specific):
    Run the checkpoint spec with `--update-snapshots` scoped to the new slugs only.
    Safest approach to avoid re-recording existing slugs:
    ```
    cd test/example && npx playwright test tests/admin-checkpoints.spec.ts \
      --project admin-checkpoints-chromium \
      --project admin-checkpoints-mobile \
      --project admin-checkpoints-dark \
      --update-snapshots
    ```
    This records all slugs. Then immediately restore the existing 5 slugs from git to prevent
    any drift from the current baselines:
    ```
    git checkout test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/global-user-index*.png
    git checkout test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/user-detail*.png
    git checkout test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/org-scoped-admin*.png
    git checkout test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner*.png
    git checkout test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/audit-explorer*.png
    ```
    Alternatively, if the Playwright version supports `--grep` for slug-specific capture:
    ```
    cd test/example && npx playwright test tests/admin-checkpoints.spec.ts \
      --update-snapshots 2>&1
    ```
    and then restore existing PNGs via git checkout (same list above).

    Step 4 — Verify PNG count and file sizes:
    ```
    ls test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png | wc -l
    ```
    Should now be 21 (7 slugs × 3 projects). Verify all 6 new PNGs are non-empty (size > 0).

    Step 5 — Run full checkpoint spec to confirm axe-green and no regressions:
    ```
    cd test/example && npx playwright test tests/admin-checkpoints.spec.ts \
      --project admin-checkpoints-chromium \
      --project admin-checkpoints-mobile \
      --project admin-checkpoints-dark
    ```
    All 3 projects must pass (axe violations → test failure via assertCheckpointScreenshot → assertNoAxeViolations).

    Step 6 — Run admin-generated parity lane:
    ```
    cd test/example && npx playwright test tests/admin-generated.spec.ts \
      --project admin-generated
    ```
    Must stay green (Landmine 6).

    Step 7 — Confirm no nested-<p> axe violation:
    The assertCheckpointScreenshot call on org-overview already runs assertNoAxeViolations with wcag2a+wcag2aa tags. A nested-<p> violation manifests as a wcag2a HTML parsing error. If axe passes on org-overview, D-07 is confirmed resolved.

    Hard-fail guards:
    - If assertNoAxeViolations fails on any new slug, check the notice content first (Landmine 1 — nested-<p>), then dark-mode contrast (Landmine 7).
    - If a perpetual-flake diff occurs (skeleton vs data diff run-to-run), the wait selector in Task 1 needs strengthening — check whether `.sg-metric-link__value` is actually absent in skeleton state (A2 assumption).
    - Do NOT proceed if the existing 5 slugs have diff failures — that means an unintended visual change from Wave 1 edits.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && npx playwright test tests/admin-checkpoints.spec.ts --project admin-checkpoints-chromium --project admin-checkpoints-mobile --project admin-checkpoints-dark 2>&1 | tail -5 && ls /Users/jon/projects/sigra/test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png | wc -l</automated>
  </verify>
  <done>
    Full admin-checkpoints spec passes across all 3 projects (chromium, mobile, dark) with axe green — spec exit code 0 is the load-bearing proof. PNG count is 21 (7 slugs × 3 projects). All 6 new PNGs exist and are non-empty. Existing 5 slug PNGs are unchanged (restored via git checkout if needed). admin-generated parity lane is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser → example app | Authenticated admin session; navigates within /admin scope |
| Playwright → snapshot directory | Writes PNG files; no network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-04a | Tampering | --update-snapshots re-recording existing baselines | mitigate | After recording, restore existing 5 slug PNGs via `git checkout` of specific filenames. Verify count = 21 before committing. |
| T-157-04b | Information Disclosure | Screenshot baselines contain rendered user data | accept | Baselines use fixture data (no real user emails or PII). The checkpoint spec seeds its own test users with random suffixes. |
| T-157-04c | Information Disclosure | Skeleton frame frozen in baseline | mitigate | Task 1 adds `.sg-metric-link__value` visibility wait BEFORE capture. If baseline is a skeleton, Task 2 step 5 run fails with diff on next run (detectable). The perpetual-flake guard is structural. |
| T-157-04d | Spoofing | `org-overview` route renders wrong LiveView | accept | Confirmed in RESEARCH.md: `/admin/organizations/:slug` maps to `OrganizationLive` per router.ex:287. The checkpoint comment in CP3 ("org landing stub") refers to the CP3 slug navigating to /users subpath — not this route. |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages. Uses existing @axe-core/playwright and Playwright already installed from prior phases. |
</threat_model>

<verification>
After both tasks complete:
- `ls test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png | wc -l` — 21
- All 6 new PNGs (global-overview × 3 + org-overview × 3) are non-empty files
- `cd test/example && npx playwright test tests/admin-checkpoints.spec.ts` — all 3 projects green
- axe violations = 0 for `axe:global-overview` and `axe:org-overview` across chromium/mobile/dark
- Existing 5 slug PNGs unchanged (git diff shows no changes to existing PNG files)
- `cd test/example && npx playwright test tests/admin-generated.spec.ts --project admin-generated` — green
</verification>

<success_criteria>
- `admin-checkpoints.spec.ts` has global-overview and org-overview checkpoint blocks with `.sg-metric-link__value` visibility wait before capture.
- 6 new PNG baselines committed to `admin-checkpoints.spec.ts-snapshots/` directory (2 slugs × 3 projects).
- Full admin-checkpoints spec (×3 projects) passes — including axe WCAG A/AA green on both new slugs.
- Existing 5 slug PNGs are unchanged.
- `admin-generated` parity lane is green.
- Phase 157 complete: `mix test` + Playwright admin-checkpoints all green.
</success_criteria>

<output>
Create `.planning/phases/157-overview-landings-highest-effort/157-04-SUMMARY.md` when done.
</output>
