---
phase: 159-cross-journey-coherence-sweep-seed-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example/demo/personas.ex
autonomous: true
requirements:
  - FIXT-02
  - FIXT-03
  - FIXT-04
  - FIXT-05

must_haves:
  truths:
    - "Personas.all/0 includes two new entries: pat (passkey-only, no MFA) and grace (deletion-scheduled Acme member)"
    - "feature_map/0 has matching entries for both 'pat' and 'grace' keys"
    - "Pat has totp: false, passkey: true — the only persona satisfying the Passkeys (no-MFA) pill condition"
    - "Grace has scheduled_deletion: true, org_member: :acme — the only Acme roster member with deleted_at"
  artifacts:
    - path: "test/example/lib/example/demo/personas.ex"
      provides: "all/0 with 9 personas, feature_map/0 with 9 entries"
      contains: "pat@demo.sigra.dev"
  key_links:
    - from: "personas.ex:all/0"
      to: "seeds.ex:seed_passkey/1"
      via: "persona map with passkey: true triggers passkey seed"
      pattern: "passkey: true"
    - from: "personas.ex:all/0"
      to: "seeds.ex:maybe_schedule_deletion/2"
      via: "persona map with scheduled_deletion: true routes to deletion clause"
      pattern: "scheduled_deletion: true"
    - from: "personas.ex:feature_map/0"
      to: "seeds.ex:print_credentials/0"
      via: "feature_map()[email_local_part] used to print persona description"
      pattern: "feature_map"
---

<objective>
Add two new demo personas to personas.ex: pat (passkey-only, no MFA) and grace
(deletion-scheduled Acme member). Update feature_map/0 with matching entries.

Purpose: Personas.all/0 is the single source of truth consumed by Seeds.run/0,
seeds_test.exs, and CredentialsLive. Both new personas are required before Plan 03
can wire them into the seed orchestrator (passkey insert, membership insert, audit rows).

Output: personas.ex with 9 personas in all/0 and 9 entries in feature_map/0.
No DB access — pure-data module.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-CONTEXT.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-PATTERNS.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From test/example/lib/example/demo/personas.ex (current state):
- all/0 returns a list of 7 persona maps, each with keys:
  email, display_name, password, confirmed, totp, passkey, locked,
  scheduled_deletion, identity_github, org_owner, org_admin, org_member
- feature_map/0 returns %{email_local_part => description_string} — 7 entries currently
- Keys are email local parts ("admin", "alice", "bob", "carol", "dave", "frank", "morgan")

Pill conditions in users_index_live.ex:
- "MFA + passkeys" = has_mfa AND passkey_count > 0 → admin (totp: true, passkey: true)
- "Passkeys" = NOT has_mfa AND passkey_count > 0 → NEW pat (totp: false, passkey: true)
- "No MFA" = NOT has_mfa AND passkey_count == 0 → alice, carol, dave, frank, morgan

Deletion-scheduled roster pill condition (after Plan 01):
- member.deletion_scheduled? = not is_nil(Map.get(user, :deleted_at))
- Existing frank has scheduled_deletion: true BUT org_member: nil — never in a roster
- NEW grace must have scheduled_deletion: true AND org_member: :acme
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add pat and grace personas to all/0 and feature_map/0</name>
  <files>test/example/lib/example/demo/personas.ex</files>

  <read_first>
    Read test/example/lib/example/demo/personas.ex lines 38–157 in full.
    The complete persona map shape (all 11 keys) must be replicated exactly for both new personas.
    Verify current offsets: highest-offset persona_audit_event is offset: 29 (Plan 03 must use 30+).
  </read_first>

  <action>
    Two edits to personas.ex:

    EDIT A — Add two personas to the list returned by all/0, appending after the morgan entry
    (the last current persona). Add pat first, then grace:

    Pat persona (per D-02 — passkey-only, no MFA):
      email: "pat@demo.sigra.dev"
      display_name: "Pat"
      password: "PatDemoPass1!Passkey"
      confirmed: true
      totp: false            (MUST be false — passkey-only means no TOTP)
      passkey: true          (triggers seed_passkey/1 in seeds.ex)
      locked: false
      scheduled_deletion: false
      identity_github: false
      org_owner: nil
      org_admin: nil
      org_member: nil

    Grace persona (per D-03 — deletion-scheduled Acme member):
      email: "grace@demo.sigra.dev"
      display_name: "Grace"
      password: "GraceDemoPass1!Acme"
      confirmed: true
      totp: false
      passkey: false
      locked: false
      scheduled_deletion: true   (routes to maybe_schedule_deletion/2 clause in seeds.ex)
      identity_github: false
      org_owner: nil
      org_admin: nil
      org_member: :acme          (MUST be :acme — org roster pill requires Acme membership)

    EDIT B — Add two entries to feature_map/0 (after the "morgan" entry). Keys are the email
    local parts (string before "@"):
      "pat"   => "Passkey-only user — no MFA, passkey display row, demonstrates Passkeys pill on users index"
      "grace" => "Deletion-scheduled Acme member — demonstrates in-roster Deletion scheduled pill"

    Critical: keys in feature_map/0 MUST match the email local part of every persona in all/0.
    Seeds.print_credentials/0 calls feature_map()[email_local_part] for each persona — a missing
    key prints nil instead of a description (no runtime crash, but broken output).
  </action>

  <verify>
    <automated>grep -n "pat@demo\|grace@demo\|\"pat\"\|\"grace\"" test/example/lib/example/demo/personas.ex</automated>
  </verify>

  <acceptance_criteria>
    - grep shows at least 4 lines: pat email in all/0, grace email in all/0, "pat" key in feature_map/0, "grace" key in feature_map/0
    - Pat persona has totp: false AND passkey: true (both on adjacent lines)
    - Grace persona has scheduled_deletion: true AND org_member: :acme (both on adjacent lines)
    - All 11 persona map keys are present for both new personas (no missing keys)
    - mix test exits 0 (pure-data module; any compile error is a key/value typo)
  </acceptance_criteria>

  <done>
    personas.ex all/0 returns 9 personas including pat (passkey-only) and grace
    (deletion-scheduled Acme member); feature_map/0 has 9 entries with "pat" and "grace" keys;
    mix test exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| personas.ex → seeds.ex | Pure-data module; persona maps drive seed behavior; no DB access here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-03 | Information Disclosure | New persona passwords (public-by-design demo credentials) | accept | All demo credentials are intentionally public; module docstring states "Never use in production"; no new risk beyond existing 7 personas |
| T-159-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After the task:
  mix test
Expected: exits 0, no compile errors.

Count check:
  grep -c "email:" test/example/lib/example/demo/personas.ex
Expected: 9 (one per persona in all/0)

feature_map count:
  grep -c "=>" test/example/lib/example/demo/personas.ex
Expected: 9 (one per persona in feature_map/0)
</verification>

<success_criteria>
- Personas.all/0 returns a list of exactly 9 maps (was 7; add pat and grace)
- Pat: totp: false, passkey: true, org_member: nil
- Grace: scheduled_deletion: true, org_member: :acme, totp: false, passkey: false
- feature_map/0 has "pat" and "grace" keys with descriptive strings
- mix test exits 0 (pure-data module, no DB interaction)
</success_criteria>

<output>
Create `.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-02-SUMMARY.md` when done.
</output>
