---
phase: 247-language-learning-digital-twin
plan: 06
type: execute
wave: 4
depends_on:
  - 247-04
  - 247-05
files_modified:
  - test/example/priv/static/assets/js/learning_twin.js
  - test/example/priv/static/assets/css/app.css
  - test/example/priv/playwright/tests/twin-offline.spec.ts
  - scripts/ci/phase-247-language-twin-proof.sh
  - .planning/phases/247-language-learning-digital-twin/247-EVIDENCE.json
autonomous: true
requirements:
  - TWIN-01
  - OFF-01
  - OFF-02
must_haves:
  truths:
    - "OFF-02 / D-09 / D-10 / D-13: reconnect transforms each queued row into its one stored accepted, rejected, or conflict result; retry/duplicate responses update or retain the same row and never show optimistic second success."
    - "Zero receipts render “No practice updates yet” and the documented body explaining when offline updates will be checked."
    - "An in-flight replay remains represented by its queued receipt until the host returns one durable accepted, rejected, or conflict result for that same row."
    - "Rejected and conflict rows use distinct visible text and recovery actions, never color alone, while retries retain the original terminal outcome."
    - "The populated state is a chronological semantic ordered list whose rows show learner-facing state and timestamps without exposing mutation IDs, partitions, credentials, or raw digests."
    - "Queued and terminal receipts may coexist, but every action has only one row and repeated replay updates or retains that row rather than adding a duplicate."
    - "Zero receipts show the empty-state heading and body, one receipt shows one labelled queued or terminal row, and many receipts remain a chronological ordered list with no duplicate action outcome."
  artifacts:
    - path: "test/example/priv/static/assets/js/learning_twin.js"
      provides: "Foreground reconnect replay reconciler and one-row terminal receipt state"
      contains: "Review lesson"
    - path: "test/example/priv/playwright/tests/twin-offline.spec.ts"
      provides: "Complete terminal replay, UI consideration, accessibility, overflow, and prohibition proof"
      contains: "replay"
    - path: "scripts/ci/phase-247-language-twin-proof.sh"
      provides: "Bounded full phase proof and atomic exact-key evidence writer"
      contains: "sigra.phase-247-language-twin/1"
    - path: ".planning/phases/247-language-learning-digital-twin/247-EVIDENCE.json"
      provides: "Credential-free source-bound machine-readable phase evidence"
      contains: "sigra.phase-247-language-twin/1"
  key_links:
    - from: "test/example/priv/static/assets/js/learning_twin.js"
      to: "POST /app/lesson/replay"
      via: "foreground reconnect with cookie/CSRF and current partition; no sync-now/background authority"
      pattern: "reconnect[\\s\\S]*replay"
    - from: "test/example/priv/static/assets/js/learning_twin.js"
      to: "ordered receipt row"
      via: "stable client mutation correlation updates the existing row once"
      pattern: "client_mutation_id[\\s\\S]*(accepted|rejected|conflict)"
    - from: "scripts/ci/phase-247-language-twin-proof.sh"
      to: "247-EVIDENCE.json"
      via: "all flags and source hashes validated before temporary-file atomic rename"
      pattern: "write_evidence_last"
  prohibitions:
    - requirement_id: TWIN-01
      category: transparency
      status: unresolved
      statement: "MUST NOT present offline availability or cached lesson presence as proof that the learner is signed in or authenticated"
    - requirement_id: TWIN-01
      category: accessibility
      status: unresolved
      statement: "MUST NOT communicate lesson availability, lease state, or replay outcome only through color, iconography, or audio"
    - requirement_id: TWIN-01
      category: user-agency
      status: unresolved
      statement: "MUST NOT autoplay lesson audio or introduce a timer-driven countdown that pressures the learner"
    - requirement_id: OFF-02
      category: user-agency
      status: unresolved
      statement: "MUST NOT automatically overwrite a learner’s saved answer when replay returns conflict or misrepresent rejected/conflict as accepted"
  backstops:
    - statement: "Playwright renders the complete lesson at a 320px viewport and asserts wrapping, reachable image and audio controls, fixed intrinsic image dimensions, and no horizontal page scrolling."
      verification: backstop
    - statement: "A browser fixture supplies long lesson prompts, transcripts, localized dates, and identifiers and asserts natural wrapping, visible focus, and no clipped critical copy or control names."
      verification: backstop
    - statement: "Playwright exercises long localized lease values and failure copy at 320px and proves the status, CTA, and retry affordance wrap without horizontal page scrolling."
      verification: backstop
    - statement: "A browser fixture supplies maximally long localized status and recovery copy and asserts that text reflows without clipping the actionable reason or control label."
      verification: backstop
    - statement: "A browser fixture supplies long prompt, learner input, validation, checkpoint, and control text and asserts wrapping, visible focus, and no clipped actionable copy."
      verification: backstop
    - statement: "Playwright supplies many receipts with long timestamps and recovery copy at 320px and asserts chronological order, reachable “Review lesson” navigation, wrapping, and no horizontal page scrolling."
      verification: backstop
    - statement: "A browser fixture uses long receipt messages and localized timestamps and asserts readable wrapping, visible “Review lesson” focus, and no clipped terminal state or recovery action."
      verification: backstop
---

<objective>
Finish the reconnect receipt experience and seal the complete phase in deterministic, credential-free, source-bound machine evidence.

Purpose: Prove terminal outcome UX, all approved UI considerations/backstops, and the full authority/integrity/isolation matrix without human UAT.
Output: One-row replay reconciliation, complete Chromium matrix, proof runner, and atomic `247-EVIDENCE.json`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/247-language-learning-digital-twin/247-CONTEXT.md
@.planning/phases/247-language-learning-digital-twin/247-RESEARCH.md
@.planning/phases/247-language-learning-digital-twin/247-PATTERNS.md
@.planning/phases/247-language-learning-digital-twin/247-VALIDATION.md
@.planning/phases/247-language-learning-digital-twin/247-UI-SPEC.md
@.planning/phases/247-language-learning-digital-twin/247-04-SUMMARY.md
@.planning/phases/247-language-learning-digital-twin/247-05-SUMMARY.md
@test/example/priv/static/assets/js/learning_twin.js
@test/example/priv/static/assets/css/app.css
@test/example/priv/playwright/tests/twin-offline.spec.ts
@scripts/ci/generated-app-login-runtime-proof.sh
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-02-SUMMARY.md

<interfaces>
Plan 04 returns one durable terminal `{client_mutation_id, status, terminal_at}` response under cookie/CSRF/current-Scope reauthorization. Plan 05 stores one partitioned queued row and renders the approved receipt container. Reconcile only by the stable client mutation correlation, keep terminal states immutable, and never display the correlation value.

The evidence script follows the repository's receipt-last pattern but is phase-local: it runs focused ExUnit and the complete `twin-offline.spec.ts`, validates exact boolean flags and source hashes, then atomically renames `247-EVIDENCE.json`. It does not invoke GitHub, watch CI, or retain browser/session values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile queued actions into one accessible terminal receipt row</name>
  <reversibility rating="reversible">Receipt rendering/reconciliation is internal UI logic behind the stable server outcome vocabulary.</reversibility>
  <files>test/example/priv/static/assets/js/learning_twin.js, test/example/priv/static/assets/css/app.css, test/example/priv/playwright/tests/twin-offline.spec.ts</files>
  <read_first>
    Read all three target files, Plans 04/05 summaries, D-09 through D-13, UI-SPEC replay state/copy/accessibility/automation/E4 contracts, the four descriptor-less prohibitions above, and existing receipt/list/focus CSS before editing.
  </read_first>
  <behavior>
    - Test 1: zero/one/many receipt states use one ordered semantic list, chronological timestamps, stable row count, and exact empty/queued/accepted copy.
    - Test 2: reconnect accepted response updates the matching queued row once; repeated and duplicate responses retain original status/timestamp and add no row.
    - Test 3: rejected response updates one row with exact recovery copy and never accepted styling/copy.
    - Test 4: conflict response updates one row, preserves learner input/current checkpoint, shows exact review copy, and “Review lesson” moves local focus without automatic overwrite.
    - Test 5: mixed queued/terminal rows remain chronological and expose no mutation ID, partition, credential, digest, or internal authorization detail.
  </behavior>
  <action>
    Add RED Chromium replay cases for accepted, duplicate accepted, rejected, conflict, zero/one/many, and mixed queued/terminal rows. Implement foreground reconnect selection only when online with the current valid partition/lease; send queued records one at a time through Plan 04's cookie/CSRF route. Match the response to the existing row by stable client mutation correlation, store the terminal status/timestamp, and remove/mark the outbox item only after the durable response. A repeated response must retain the original terminal row and timestamp; do not offer a bypassing “sync now” or background-sync path (D-09/D-10/D-11).

    Render an ordered semantic list with exact approved empty, queued, accepted, rejected, and conflict copy. Keep IDs/partitions/digests/credentials out of visible markup/hooks. Rejected and conflict use distinct text/recovery; conflict preserves entered data and “Review lesson” focuses the lesson/action context rather than overwriting. Apply status text plus semantic pill/focus behavior, not color alone; preserve invoking focus for in-place updates, native audio non-autoplay, and no timer/countdown (UI-SPEC/prohibitions). Use pre-armed response waits and direct IndexedDB/receipt assertions with no sleeps (D-12/D-13).
  </action>
  <acceptance_criteria>
    - Accepted/rejected/conflict each replace one queued row with exact approved copy and the host terminal timestamp; duplicate/retry adds no row and retains original result.
    - Conflict preserves learner state and “Review lesson” moves focus locally; no automatic overwrite or false accepted presentation occurs.
    - Zero, one, many, mixed, and chronological list states are semantic and deterministic; learner-facing content exposes none of the forbidden internal fields.
    - Text, ARIA semantics, focus, and non-autoplay behavior prove all four descriptor-less prohibitions remain respected in the rendered implementation even though they stay flagged for downstream review.
    - The focused Chromium replay command passes without sleeps, timing guesses, raw-cookie inspection, background sync, or human/visual-only approval.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright &amp;&amp; npm test -- twin-offline.spec.ts --project=chromium --grep "replay|receipt|accepted|rejected|conflict"</automated>
  </verify>
  <done>Foreground reconnect displays exactly one durable accessible terminal row per action, with correct accepted/rejected/conflict recovery behavior and no optimistic duplicate or automatic conflict overwrite (OFF-02; D-09 through D-13).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove all UI backstops and publish exact source-bound phase evidence</name>
  <reversibility rating="costly">The exact-key evidence schema is consumed as the phase's durable proof; changes require a versioned coordinated script/evidence update.</reversibility>
  <files>test/example/priv/playwright/tests/twin-offline.spec.ts, scripts/ci/phase-247-language-twin-proof.sh, .planning/phases/247-language-learning-digital-twin/247-EVIDENCE.json</files>
  <read_first>
    Read all three target files (create the missing script/evidence), final Task 1 spec, every `must_haves.backstops` row above, `247-VALIDATION.md` complete commands, AGENTS automation rules, Phase 246.2 proof script/summary receipt-last pattern, and all source files to be hash-bound before editing.
  </read_first>
  <behavior>
    - Test 1: 320px and long-content fixtures prove all seven approved backstops with no horizontal scroll, clipping, unreachable control, lost focus, or order error.
    - Test 2: the complete ExUnit and pinned Chromium matrix yields true flags for credential boundary, manifest integrity, write-failure closure, offline lesson/audio, lease boundary, partition/account isolation, and all replay outcomes/duplicates.
    - Test 3: evidence schema is exactly `sigra.phase-247-language-twin/1`, exact-key, all required booleans true, and source hashes match current production/test/proof files.
    - Test 4: evidence rejects missing/false/extra behavior fields, source mismatch, non-success result, and credential/account/mutation/digest/media payload fields.
    - Test 5: evidence temp file is created/renamed only after every command/flag/hash check passes; failure leaves no passing replacement.
  </behavior>
  <action>
    Extend the Playwright spec with deterministic long-content fixtures and 320px checks for all seven backstop statements in frontmatter: complete lesson media/control reachability and intrinsic dimensions; long lesson/transcript/date/identifier wrapping; long lease/failure/status/recovery copy; long form/validation/checkpoint/control copy; many chronological receipts plus reachable Review lesson; long receipt/timestamp/focus copy. Assert `document.documentElement.scrollWidth &lt;= clientWidth`, bounding boxes, computed visibility/wrapping, role/name reachability, and focus targets; do not use screenshots as the only assertion.

    Create `scripts/ci/phase-247-language-twin-proof.sh` to run migrations/focused ExUnit and the complete pinned Chromium twin spec once, capture only process outcomes, validate required markers, compute SHA-256 for the migration, host module, router, JS, CSS, ExUnit tests, Playwright spec, and proof script, then atomically write `.planning/phases/247-language-learning-digital-twin/247-EVIDENCE.json` last. The exact schema `sigra.phase-247-language-twin/1` contains `result: "pass"`, fixed true booleans `credential_boundary`, `manifest_integrity`, `short_corrupt_denied`, `cache_write_failure_denied`, `offline_media_usable`, `lease_boundary`, `account_isolation`, `accepted_once`, `rejected_once`, `conflict_once`, `duplicate_stable`, `ui_considerations`, `light_dark_system`, `no_sleep`, and an exact `sources` map. Reject extra keys and any credential/cookie/token/user/account/partition/mutation/digest/media bytes/content field. Write a temporary file only after all checks and rename last; no CI/GitHub watcher or external service is involved (D-12/D-13).
  </action>
  <acceptance_criteria>
    - All seven backstop fixtures pass at 320px/long content with semantic/geometry/focus assertions, not visual-only snapshots.
    - The script runs the required ExUnit and complete Chromium commands without watch mode, sleeps, human UAT, skip-on-missing dependency, or duplicate CI polling.
    - `247-EVIDENCE.json` has the exact schema-1 keys, all required booleans true, exact current source hashes, and no credential/account/partition/mutation/digest/media payload.
    - Mutating any required boolean, result, key set, or source hash causes validation failure; a failed command cannot publish a passing evidence replacement.
    - The complete proof script exits zero and `git diff --check` passes for all phase implementation/evidence files.
  </acceptance_criteria>
  <verify>
    <automated>scripts/ci/phase-247-language-twin-proof.sh &amp;&amp; git diff --check -- test/example scripts/ci/phase-247-language-twin-proof.sh .planning/phases/247-language-learning-digital-twin/247-EVIDENCE.json</automated>
  </verify>
  <done>All covered and backstop UI considerations plus every TWIN-01/OFF-01/OFF-02 security boundary pass one deterministic proof, sealed in credential-free exact-source `247-EVIDENCE.json` (D-01 through D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Outbox/response → receipt UI | Untrusted local/server correlation must update one row without exposing internal identity or inventing success. |
| Conflict/rejection → learner action | Terminal denial must preserve learner agency and avoid silent overwrite or misleading state. |
| Browser/ExUnit results → evidence flags | Evidence must derive from actual complete command outcomes, not caller assertion. |
| Repository sources → retained proof | Exact hashes bind the passing evidence to implementation/test/proof bytes. |
| Transient runtime values → retained evidence | Credentials, cookies, accounts, partitions, mutations, digests, and media content must not persist. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-247-24 | Tampering / Repudiation | Receipt reconciliation | high | mitigate | Match stable local correlation, accept only durable server terminal response, preserve first terminal status/timestamp, and assert one row. |
| T-247-25 | Information Disclosure | Receipt UI/hooks | high | mitigate | Learner-safe text/timestamps only; tests reject mutation IDs, partition, credentials, digest, and internal authorization data. |
| T-247-26 | Tampering / User harm | Conflict/rejection UX | high | mitigate | Distinct text/recovery, preserve learner input, Review lesson focus, no overwrite or false accepted state. |
| T-247-27 | Tampering / Repudiation | Evidence publication | high | mitigate | Run complete proof, require exact all-true flags/current hashes, validate exact keys, and atomically rename evidence last. |
| T-247-28 | Information Disclosure | Evidence/diagnostics | high | mitigate | Fixed booleans/source hashes only; exact-key validator rejects secret, account, mutation, digest, and media payload categories. |
| T-247-29 | Denial of Service | Long content/320px | medium | mitigate | Deterministic wrapping/geometry/focus/role assertions for every approved backstop. |
| T-247-SC | Tampering | Package installs | low | accept | No dependency is added; existing Playwright/axe availability is unchanged. |
</threat_model>

## Artifacts this phase produces

- Expanded `learning_twin.js` foreground functions for reconnect selection, replay POST, durable response reconciliation, and immutable one-row terminal state.
- Expanded `app.css` `vt-twin-*` receipt/status/recovery/focus/wrapping rules; no second theme/design system.
- Expanded `twin-offline.spec.ts` scenarios for accepted/rejected/conflict/duplicate/zero-one-many/mixed receipts, all four kept prohibitions, all seven UI backstops, full matrix helpers, and evidence markers.
- New executable `scripts/ci/phase-247-language-twin-proof.sh`; function/protocol names `run_exunit`, `run_chromium`, `validate_flags`, `hash_sources`, `write_evidence_last`.
- New `.planning/phases/247-language-learning-digital-twin/247-EVIDENCE.json`; schema `sigra.phase-247-language-twin/1`; exact behavior booleans and source-hash map listed in Task 2.
- No external API/SDK/service, package, schema/route, Crosswake adapter/released integration, native runtime/device claim, background sync, generic offline engine, additional lesson, or SDK surface.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Bounded account-safe offline lesson without cached authentication authority | 01-06 | COVERED | Full proof sealed here. |
| REQ | TWIN-01 | Authenticated structured/image/audio lesson, HttpOnly session, no JS/SW credential | 01,03,05,06 | COVERED | Server/browser/UI/evidence. |
| REQ | OFF-01 | Exact size/SHA before availability; corrupt/incomplete denied | 01-02,06 | COVERED | Complete media matrix and evidence. |
| REQ | OFF-02 | Seven-day lease, partitioned state/outbox, logout/switch, exactly-once three outcomes | 01,03-06 | COVERED | Server/client/concurrency/UI/evidence. |
| RESEARCH | — | Host-owned context/lease/replay, marker-last, partition-first, PostgreSQL terminal receipt, no packages | 01-06 | COVERED | All recommended constraints mapped. |
| CONTEXT | D-01 | Existing `/app` cookie session; no JS/SW credentials | 01,03,04,06 | COVERED | Tracer, surface tests, transport, evidence. |
| CONTEXT | D-02 | Host owns authorization/data/media/lease/partition/replay | 01,03,04 | COVERED | Current-Scope host context. |
| CONTEXT | D-03 | Immutable manifest URL/size/SHA for image/audio | 01-02 | COVERED | Host tracer plus failures. |
| CONTEXT | D-04 | Full read/size/SHA/cache then partition marker | 01-02 | COVERED | Exact browser order and direct proof. |
| CONTEXT | D-05 | Marker+cache required; every failure/orphan unavailable | 01-02 | COVERED | Complete failure matrix. |
| CONTEXT | D-06 | Opaque partition and configurable seven-day lease, non-authority | 01,03,05 | COVERED | Exact host/client boundary. |
| CONTEXT | D-07 | All local records partitioned; missing/expired/changed closed | 01,03,05 | COVERED | Schema/client tests. |
| CONTEXT | D-08 | Logout/switch blocks prior state; immutable bytes only unreachable reuse | 03,05 | COVERED | Same-context transition proof. |
| CONTEXT | D-09 | Crosswake vocabulary plus local partition | 01,04-06 | COVERED | Vocabulary only, no coupling. |
| CONTEXT | D-10 | Current Scope/authorization and durable exactly-one terminal outcome | 01,04,06 | COVERED | Transaction/controller/UI. |
| CONTEXT | D-11 | No Crosswake adapter/released/native proof until Phase 248 | 01,04-06 | COVERED | Explicit exclusions/source assertions. |
| CONTEXT | D-12 | Pinned deterministic Chromium, hooks, pre-armed waits, direct storage, offline, no UAT/sleeps | 01-02,05-06 | COVERED | Complete proof lane. |
| CONTEXT | D-13 | Valid/short/corrupt/write failure/offline/expiry/logout/switch/reconnect/duplicate/rejected/conflict | 02,04-06 | COVERED | All named paths and evidence flags. |
| UI-SPEC | 19 covered considerations | E1/E2/E3/E4 covered state rows | 01-03,05-06 | COVERED | Every resolution is lifted verbatim as a truth in the owning plan. |
| UI-SPEC | 7 backstop considerations | Overflow/long-text browser fixtures | 06 | COVERED | Every row is a flat `{statement, verification: backstop}` marker above. |
| VALIDATION | 247-01-01..247-02-02 | Auth lesson, integrity, lease/isolation, replay | 01-06 | COVERED | Expanded into focused and final commands. |

Edge-probe no-silent-drop audit: TWIN-01 unclassified appears once in Plan 01; OFF-01 unclassified appears once in Plan 02; OFF-02 boundary and precision appear once each in Plan 03. All four retain `status: unresolved` and explicit planned evidence. Prohibition recall no-silent-drop audit: raw candidates included credential leakage, cross-account disclosure, corrupt-media readiness, inaccessible status, autoplay/countdown, cached-auth implication, and conflict overwrite. Credential leakage/cross-account/corrupt integrity are canonical security/correctness concerns breadcrumbed to STRIDE/`$gsd-secure-phase` or OFF-01 tests; four bespoke transparency/accessibility/user-agency items were kept descriptor-less and appear above as unresolved. No `check_*` descriptor was invented.

External API coverage is decided in `COVERAGE.md`: only internal Phoenix/browser APIs exist. Assumption-delta remains absent. Ecto migration/test execution is planned normally; no foreign ORM schema-push command is introduced. Released Crosswake/native proof stays Phase 248, while generic sync/additional lessons/SDK and Electron runtime remain excluded.

<verification>
1. Run Task 1's focused terminal receipt browser matrix.
2. Run `scripts/ci/phase-247-language-twin-proof.sh` once; it must run focused ExUnit plus complete Chromium and atomically publish exact-key evidence.
3. Validate all 19 covered UI truths and seven structured backstops are represented across Plans 01-06, and all four edge flags/four kept prohibitions remain explicit.
4. Confirm `COVERAGE.md` declares no external integration and package manifests/lockfiles are unchanged.
</verification>

<success_criteria>
- All TWIN-01, OFF-01, and OFF-02 behavior passes one automated proof without human UAT.
- Every queued action becomes exactly one durable, accurately presented terminal row.
- All approved UI covered/backstop states pass across accessibility, 320px, long content, and Light/Dark/System.
- Exact-key source-bound evidence is credential/account/mutation/media-payload free and written only after complete success.
- All high-severity threats are mitigated with deterministic evidence and deferred/out-of-scope boundaries remain intact.
</success_criteria>

<output>
Create `.planning/phases/247-language-learning-digital-twin/247-06-SUMMARY.md` when done.
</output>
