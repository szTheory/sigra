---
phase: 247-language-learning-digital-twin
plan: 05
type: execute
wave: 3
depends_on:
  - 247-02
  - 247-03
files_modified:
  - test/example/priv/static/assets/js/learning_twin.js
  - test/example/priv/static/assets/css/app.css
  - test/example/priv/playwright/tests/twin-offline.spec.ts
autonomous: true
requirements:
  - TWIN-01
  - OFF-02
must_haves:
  truths:
    - "OFF-02 / D-07 / D-08 / D-12 / D-13: offline activation, action enqueue, logout, lease expiry, and account switch use only the current partition/lease; prior lesson, readiness, answer, outbox, and receipt state become immediately unreachable."
    - "An unfilled practice form shows the prompt, checkpoint context, and required labels and creates no receipt until a valid submission is made."
    - "A valid offline submission creates one queued receipt immediately; reconnect transitions that same row to exactly one terminal state without a timer or duplicate optimistic success."
    - "Rejected and conflict results retain one receipt row and show their distinct documented recovery copy; conflict provides “Review lesson” rather than overwriting automatically."
    - "An incomplete practice form retains entered values, shows inline validation, and creates no outbox entry until the submission is valid."
  artifacts:
    - path: "test/example/priv/static/assets/js/learning_twin.js"
      provides: "Partition-first activation/enqueue/reconnect/logout-switch state machine"
      contains: "partition"
    - path: "test/example/priv/static/assets/css/app.css"
      provides: "Tasklane `vt-twin-*` responsive Light/Dark/System lesson styles using shared tokens"
      contains: ".vt-twin-"
    - path: "test/example/priv/playwright/tests/twin-offline.spec.ts"
      provides: "Offline lease, invalid form, expiry, logout, and account-switch browser proof"
      contains: "account switch"
  key_links:
    - from: "current bootstrap partition/lease"
      to: "every IndexedDB reader/writer"
      via: "partition-leading compound keys and strict local lease validation"
      pattern: "partition[\\s\\S]*(media_markers|lesson_state|outbox)"
    - from: "logout/account-switch/expiry"
      to: "lesson UI"
      via: "immediate memory/DOM replacement before any prior storage read or replay"
      pattern: "clearCurrent[\\s\\S]*(expired|account)"
  prohibitions:
    - requirement_id: TWIN-01
      category: accessibility
      status: unresolved
      statement: "MUST NOT communicate lesson availability, lease state, or replay outcome only through color, iconography, or audio"
    - requirement_id: TWIN-01
      category: user-agency
      status: unresolved
      statement: "MUST NOT autoplay lesson audio or introduce a timer-driven countdown that pressures the learner"
    - requirement_id: OFF-02
      category: user-agency
      status: unresolved
      statement: "MUST NOT automatically overwrite a learner’s saved answer when replay returns conflict or misrepresent rejected/conflict as accepted"
---

<objective>
Complete the browser account-isolation and offline practice experience across invalid input, expiry, logout, and account switch while preserving Tasklane themes and accessibility.

Purpose: Prevent prior-account state resurrection and make bounded offline action behavior understandable and usable.
Output: Partition-first client state machine, `vt-twin-*` styles, and deterministic browser transition proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/247-language-learning-digital-twin/247-CONTEXT.md
@.planning/phases/247-language-learning-digital-twin/247-RESEARCH.md
@.planning/phases/247-language-learning-digital-twin/247-PATTERNS.md
@.planning/phases/247-language-learning-digital-twin/247-VALIDATION.md
@.planning/phases/247-language-learning-digital-twin/247-UI-SPEC.md
@.planning/phases/247-language-learning-digital-twin/247-02-SUMMARY.md
@.planning/phases/247-language-learning-digital-twin/247-03-SUMMARY.md
@test/example/priv/static/assets/js/learning_twin.js
@test/example/priv/static/assets/css/app.css
@test/example/priv/playwright/tests/twin-offline.spec.ts
@test/example/lib/example_web/live/app_live.ex

<interfaces>
Use Plan 02's media statuses/failure seam and Plan 03's exact lease/partition error contract. Preserve the approved hooks and role-first visible controls. All new styles are BEM-scoped `vt-twin-*` in the existing cascade order and consume existing `--sg-*`/`--vt-*` tokens; there is no local theme switch or admin `sg-*` component markup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove partition-first offline actions across expiry, logout, and account switch</name>
  <reversibility rating="reversible">Client state transitions and scoped CSS are internal behind the locked storage and visible-state contracts.</reversibility>
  <files>test/example/priv/static/assets/js/learning_twin.js, test/example/priv/static/assets/css/app.css, test/example/priv/playwright/tests/twin-offline.spec.ts</files>
  <read_first>
    Read all three target files, Plans 02/03 summaries, D-06 through D-08 and D-12/D-13, UI-SPEC composition/offline/practice/accessibility/theme contracts and E3 rows, existing `vt-*` CSS around `vt-page-intro`, `vt-panel`, `vt-card-grid`, status pills/buttons, and Playwright login/logout helpers before editing.
  </read_first>
  <behavior>
    - Test 1: a valid-lease offline reload retains structured lesson, verified image/audio, action form, and exact offline-until copy; a valid action creates one partitioned queued row.
    - Test 2: empty/incomplete action retains input and inline validation but creates no outbox/receipt; valid submission creates one row with Crosswake-compatible fields and no credentials.
    - Test 3: exact lease expiry suppresses lesson/action/receipt activation, shows the approved expired replacement, and focuses its heading.
    - Test 4: logout then account B login in the same BrowserContext never displays/activates/replays account A title, answer, lease, readiness, outbox, or receipt; A's immutable bytes may remain but are unreachable.
    - Test 5: Light, Dark, and System resolve through existing tokens; 320px layout has one-column panels, usable native audio/image/action controls, and no horizontal page scroll.
  </behavior>
  <action>
    Add failing Playwright scenarios using the same BrowserContext for account A logout/account B switch and a separate fresh-context isolation control. Before every local read, activation, enqueue, and replay selection, require the current bootstrap partition, strict unexpired lease, and a compound partition-leading key. On expiry/logout/switch, clear in-memory lesson/receipt state and replace the UI before querying local stores; do not delete immutable bytes solely for isolation, but prove they are unreachable without current matching metadata (D-06/D-07/D-08).

    Implement client validation for the bounded practice form: empty/incomplete values keep entered content, expose inline errors, and create no outbox row; valid offline submission creates one queued row with `client_mutation_id`, `idempotency_key`, `base_checkpoint`, partition, action, and local timestamp, with no credentials. Render exact valid-offline/expired/queued copy and focus behavior. Add only `vt-twin-*` rules using existing Tasklane/shared tokens, native audio, intrinsic image dimensions, one column below 768px, 44px interactive boxes, Light/Dark/System variables, existing reduced-motion behavior, and no autoplay/countdown/inline visual style/second design system (TWIN-01/UI-SPEC). Use role selectors and direct storage inspection; no sleeps (D-12/D-13).
  </action>
  <acceptance_criteria>
    - Valid lease offline reload shows/uses the lesson/media/action form and queues one partitioned credential-free action; invalid form creates no outbox/receipt.
    - Exactly expired lease replaces content, disables activation/replay, uses approved copy, and focuses the replacement heading.
    - Same-context A→logout→B proof shows no A lesson/title/answer/lease/readiness/outbox/receipt while direct storage inspection may observe immutable bytes only without activatable A metadata.
    - Light/Dark/System computed styles derive from existing variables; 320px has no horizontal scroll and controls remain reachable; status is textual and audio never autoplays.
    - Focused Chromium command passes with role-first selectors, stable approved hooks only, pre-armed waits, and no sleep/human/visual-only gate.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright &amp;&amp; npm test -- twin-offline.spec.ts --project=chromium --grep "lease|partition|logout|account switch|practice form|theme"</automated>
  </verify>
  <done>The browser permits bounded offline practice only for the current unexpired account partition, fails closed at expiry/logout/switch, and renders the approved accessible Tasklane experience across themes and 320px (TWIN-01/OFF-02; D-06/D-07/D-08/D-12/D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Current bootstrap → IndexedDB readers/writers | Every operation must carry the current partition and valid lease. |
| Logout/account switch → prior in-memory/DOM/storage state | Prior learner content must become unreachable before new account presentation. |
| Offline form → outbox | Only validated bounded action data is stored; no authentication authority enters storage. |
| Theme/responsive CSS → accessible status/actions | Visual states cannot hide meaning or controls across themes/viewports. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-247-19 | Information Disclosure | Account switch/local storage | high | mitigate | Partition-leading keys, current bootstrap check on every reader, immediate state replacement, same-context account-switch proof. |
| T-247-20 | Elevation of Privilege | Expired lease activation/replay | high | mitigate | Strict local expiry plus server contract, disable/suppress lesson/action/replay at exact expiry. |
| T-247-21 | Information Disclosure | Outbox record | high | mitigate | Exact bounded field schema and direct assertions excluding credential/cookie/token/digest fields. |
| T-247-22 | Tampering | Invalid offline action | medium | mitigate | Validate required bounded values before creating any outbox or receipt row. |
| T-247-23 | Denial of Service / Accessibility | Small viewport/theme states | medium | mitigate | Tokenized responsive CSS, native controls, text statuses, 320px and Light/Dark/System computed-style checks. |
| T-247-SC | Tampering | Package installs | low | accept | No dependency is added. |
</threat_model>

## Artifacts this phase produces

- Expanded `learning_twin.js` helpers for current-partition activation, exact lease expiry, form validation, outbox enqueue, and immediate account-transition replacement.
- Outbox record fields: partition-leading key, `client_mutation_id`, `idempotency_key`, `base_checkpoint`, bounded action/answer, `queued_at`; no authority/credential fields.
- `app.css` BEM blocks `vt-twin-*` for lesson media, offline status/action, practice form, receipt area, responsive grid, and focus/reduced-motion behavior using existing tokens.
- `twin-offline.spec.ts` scenarios for valid offline mode, invalid/valid practice form, expiry, logout, same-context account switch, fresh-context control, Light/Dark/System, and 320px.
- No new theme switch, admin markup, package, route/schema, generic sync/background sync, Crosswake integration, or native proof.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Account-safe bounded offline lesson | 01,03,05 | COVERED | Browser transitions completed here. |
| REQ | TWIN-01 | Usable structured/image/audio lesson | 01,05,06 | COVERED | Responsive/themed/accessibility behavior here. |
| REQ | OFF-02 | Lease, partitioned state/outbox, logout/switch | 01,03,05 | COVERED | Full client matrix here. |
| RESEARCH | — | Partition-first local activation, direct browser inspection | 05 | COVERED | Task 1. |
| CONTEXT | D-06..D-08 | Lease/local/account isolation | 05 | COVERED | Exact transition proof. |
| CONTEXT | D-09 | Crosswake-compatible local identity vocabulary | 05 | COVERED | Outbox only; no integration. |
| CONTEXT | D-12..D-13 | Deterministic offline/expiry/logout/switch proof | 05 | COVERED | Task 1 matrix. |
| UI-SPEC | E3 covered rows | Empty/loading/error/partial form states | 05 | COVERED | All four truths lifted verbatim above. |

Deferred generalized/offline/Crosswake/native work remains excluded. The three descriptor-less bespoke prohibitions remain flagged-unverified; canonical storage/credential/account isolation risks are in STRIDE.

<verification>
Run the focused Chromium transition matrix and confirm every account/expiry assertion includes direct storage plus visible-state evidence.
</verification>

<success_criteria>
- No prior-account state activates or appears after expiry/logout/switch.
- Valid offline practice remains usable and queues exactly one bounded credential-free action.
- Form, theme, viewport, focus, transcript/audio, and status behavior meet the approved UI contract.
</success_criteria>

<output>
Create `.planning/phases/247-language-learning-digital-twin/247-05-SUMMARY.md` when done.
</output>
