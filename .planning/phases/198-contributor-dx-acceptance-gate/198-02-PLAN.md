---
phase: 198-contributor-dx-acceptance-gate
plan: 02
type: execute
wave: 2
depends_on: ["198-01"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/todos/pending/2026-06-20-complete-d10-design-gallery-re-gate-after-recapture.md
  - .planning/todos/pending/2026-06-20-phase51-installer-milestone-audit-ci-contract-stale.md
autonomous: false
requirements: [GATE-02]
must_haves:
  truths:
    - "The design-gallery visual lane is hard-gated again: `continue-on-error: true` removed at ci.yml:1047 and `design_gallery` restored to the aggregator failure loop (D-06)"
    - "The hard re-gate is only landed after a confirm-green check against the new ubuntu baselines (PR #60 merged)"
    - "The 5 enforced required-check names are unchanged in the live ruleset 14941512"
    - "ci.yml contract-lock tests + snapshot-canary-guard stay green after the re-gate edit"
    - "The already-resolved Phase51 ci-contract stale todo is closed (D-07, not re-fixed)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "design_gallery step hard-gated (no continue-on-error) + restored to aggregator loop"
      contains: "design_gallery"
    - path: ".planning/todos/pending/2026-06-20-complete-d10-design-gallery-re-gate-after-recapture.md"
      provides: "the design-gallery re-gate deferral todo, now resolved/closed"
      contains: "design"
  key_links:
    - from: ".github/workflows/ci.yml design_gallery step"
      to: ".github/workflows/ci.yml Aggregate Playwright step outcomes loop"
      via: "design_gallery.outcome re-added to the failure loop so a visual regression re-fails the job"
      pattern: "steps.design_gallery.outcome"
    - from: ".github/workflows/ci.yml"
      to: "ruleset 14941512"
      via: "the 5 required job name: strings remain byte-stable after the edit"
      pattern: "name: Library tests"
---

<objective>
Deliver the GATE-02 no-regression guards and the D-06 design-gallery hard re-gate: remove the `TEMP SOFT-GATE continue-on-error: true` at ci.yml:1047 and restore `design_gallery` to the aggregator failure loop, behind a confirm-green check against the new ubuntu baselines, while proving the 5 required-check names are byte-stable, the ci.yml contract locks and snapshot-canary-guard still pass, and closing the already-resolved Phase51 stale todo (D-07).

Purpose: Make the milestone's "equal-or-greater quality signal" claim honest — the design-gallery is a correctness-critical visual gate that is currently non-blocking; this restores it without re-creating the bootstrap deadlock.
Output: hard-gated ci.yml design_gallery, confirmed-stable required names, green contract locks, closed D-07 todo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-contributor-dx-acceptance-gate/198-CONTEXT.md
@.planning/phases/198-contributor-dx-acceptance-gate/198-RESEARCH.md

# Files / context this plan touches
@MAINTAINING.md
@test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
@test/sigra/planning/phase_192_known_failure_contract_test.exs
@.planning/todos/pending/2026-06-20-complete-d10-design-gallery-re-gate-after-recapture.md
@.planning/todos/pending/2026-06-20-phase51-installer-milestone-audit-ci-contract-stale.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- Hard-gated `design_gallery` step in `.github/workflows/ci.yml` (no `continue-on-error`; restored to the `Aggregate Playwright step outcomes` loop).
- Closed todos: `2026-06-20-complete-d10-design-gallery-re-gate-after-recapture.md` and `2026-06-20-phase51-installer-milestone-audit-ci-contract-stale.md` (moved to resolved / annotated closed).
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm-green checkpoint — design-gallery lane green vs new ubuntu baselines before re-gate</name>
  <action>
    Before any ci.yml edit in Task 2, verify the design-gallery / `admin_design_recapture` lane is green on main against the new ubuntu-native baselines (PR #60 merged, commit 9eed3474) and that OQ3 cross-lane compare is clean. This is a blocking gate: if the lane is not green, HALT and do not remove the soft gate — the hard re-gate must never strand the REQUIRED `Example Playwright smoke` lane. See `<how-to-verify>` for the exact `gh` commands.
  </action>
  <what-built>
    A confirm-green check that the design-gallery lane is green against the new ubuntu-native baselines BEFORE the hard re-gate is committed. PR #60 ("recapture admin-design baselines in ubuntu CI", commit 9eed3474) is merged to main, so ubuntu-native PNGs exist. This step verifies the latest `admin_design_recapture` / design-gallery run on main is green and that OQ3 cross-lane compare is clean (no sibling-lane drift).
  </what-built>
  <how-to-verify>
    1. Run: `gh run list --workflow CI --branch main --json databaseId,conclusion,headSha,createdAt --limit 10` and identify the most recent push/schedule run that exercised the design-gallery / `admin_design_recapture` job.
    2. Run: `gh run view <id> --json jobs --jq '.jobs[] | select(.name | test("design|recapture")) | {name, conclusion}'` and confirm the design-gallery / recapture jobs concluded `success`.
    3. Confirm there is no pending `oq3-cross-lane` todo (cross-lane compare came back clean): `ls .planning/todos/pending/ | grep -i oq3 || echo "OQ3 clean"`.
    4. If the lane is NOT green against the new baselines, STOP — do not remove the soft gate; report the failing run so the baseline issue is fixed first (the hard re-gate must never strand the REQUIRED Example Playwright smoke lane).
  </how-to-verify>
  <resume-signal>Type "confirmed green" once the design-gallery lane is verified green against the new ubuntu baselines, or describe the failure to halt the re-gate.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Hard re-gate the design-gallery lane in ci.yml (D-06)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines ~1034-1070: the `Run design gallery boards` step (`id: design_gallery`) with the `TEMP SOFT-GATE` comment block and `continue-on-error: true` at line 1047.
    - .github/workflows/ci.yml lines ~1102-1127: the `Aggregate Playwright step outcomes` step — its `for o in` loop currently lists `admin_behavior`, `admin_checkpoints`, `non_admin_smoke`, `demo_showcase` but OMITS `design_gallery` (with a TEMP comment explaining why).
    - 198-RESEARCH.md §"Outstanding Residual: Design-Gallery Soft-Gate" — the exact two edits + the warning that a SECOND `continue-on-error: true` at ci.yml:1612 (inside `admin_design_recapture`, nightly OQ3 cross-lane) is intentional and MUST NOT be touched.
  </read_first>
  <action>
    Make exactly two edits to `.github/workflows/ci.yml`, per D-06: (1) Remove the `continue-on-error: true` line from the `design_gallery` step (the one at ~line 1047 under the `TEMP SOFT-GATE` comment), and remove/replace the now-stale `TEMP SOFT-GATE (bootstrap ordering ...)` comment block so the step's comments reflect that it is hard-gated again (the hard-gating rationale comment already present below it stays). (2) In the `Aggregate Playwright step outcomes` loop, add `"${{ steps.design_gallery.outcome }}"` back into the `for o in` list so a design-gallery `failure` re-fails the job, and remove the `TEMP (bootstrap ordering ... design_gallery is intentionally OMITTED ...)` comment that explained its absence. Do NOT touch the second `continue-on-error: true` at ~line 1612 inside `admin_design_recapture` (the nightly OQ3 cross-lane compare — intentional). Do NOT rename any job `name:` string. Do NOT alter the path-detector regex (it must still appear exactly twice — phase_51 lock) or the admin-design MG-5/6 `test.skip(` marker (phase_192 lock).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && test "$(grep -c 'continue-on-error: true' .github/workflows/ci.yml)" -eq 1 && grep -q 'steps.design_gallery.outcome' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE `continue-on-error: true` remains in ci.yml (the intentional nightly OQ3 one at ~1612); the design-gallery one is gone.
    - `steps.design_gallery.outcome` now appears in the aggregator failure loop.
    - No job `name:` string changed; the path-detector regex still appears exactly twice; the MG-5/6 `test.skip(` marker is untouched.
  </acceptance_criteria>
  <done>The design-gallery lane is hard-gated again and wired into the aggregator, with the second intentional soft-gate left intact.</done>
</task>

<task type="auto">
  <name>Task 3: Verify required-name stability + contract locks + snapshot determinism after the edit</name>
  <files>test/sigra/planning/phase_51_install_golden_ci_contract_test.exs, test/sigra/planning/phase_192_known_failure_contract_test.exs</files>
  <read_first>
    - MAINTAINING.md lines ~100-122: the 5 enforced required-check names + the `gh api ...rulesets/14941512` verification command.
    - scripts/ci/snapshot-canary-guard.sh — the canary/allowlist guard that must stay green.
  </read_first>
  <action>
    After the ci.yml edit, prove GATE-02's no-regression invariants hold (these are read-only assertions, not new code): (1) Assert the 5 enforced required-check names are byte-stable in the live ruleset via `gh api repos/szTheory/sigra/rulesets/14941512 --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'` and confirm the output is exactly the 5 known strings (`Library tests`, `Example unit smoke (ExUnit + ConnTest)`, `Install smoke (fresh phx.new + sigra.install)`, `Example HTTP smoke (boot + curl critical routes)`, `Example Playwright smoke (full lifecycle)`). (2) Run the ci.yml contract locks `mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs test/sigra/planning/phase_192_known_failure_contract_test.exs` and confirm green (the re-gate must not break the path-detector ×2 count or the MG-5/6 skip marker). (3) Run `bash scripts/ci/snapshot-canary-guard.sh` and confirm it passes (snapshot/baseline determinism preserved). Record the ruleset `gh api` output verbatim into the plan SUMMARY for the acceptance evidence (Plan 03) to cite. Do not modify the contract test files unless the re-gate legitimately changed a structural invariant they assert — if so, update the assertion to match the new ci.yml and note it.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs test/sigra/planning/phase_192_known_failure_contract_test.exs --no-start && bash scripts/ci/snapshot-canary-guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - `gh api ...rulesets/14941512` returns exactly the 5 known required-check name strings.
    - phase_51 + phase_192 contract locks pass after the ci.yml edit.
    - snapshot-canary-guard.sh passes.
    - The ruleset output is captured in the SUMMARY for Plan 03 to reference.
  </acceptance_criteria>
  <done>Required names are proven byte-stable, ci.yml contract locks are green, and snapshot determinism holds after the re-gate.</done>
</task>

<task type="auto">
  <name>Task 4: Close the already-resolved Phase51 stale todo + the design-gallery deferral todo (D-07, hygiene)</name>
  <files>.planning/todos/pending/2026-06-20-phase51-installer-milestone-audit-ci-contract-stale.md, .planning/todos/pending/2026-06-20-complete-d10-design-gallery-re-gate-after-recapture.md</files>
  <read_first>
    - .planning/todos/pending/2026-06-20-phase51-installer-milestone-audit-ci-contract-stale.md — describes a contract test that is ALREADY fixed (the test no longer asserts the removed job key). Per D-07, close as already-resolved; do NOT re-fix.
    - .planning/todos/pending/2026-06-20-complete-d10-design-gallery-re-gate-after-recapture.md — the re-gate deferral todo; Task 2 completes its final step, so close it too.
  </read_first>
  <action>
    Per D-07 and phase hygiene, resolve both pending todos. For the Phase51 stale todo: confirm via `git grep -n 'installer_milestone_audit:' .github/workflows/ci.yml` that the old job-key is gone and the contract test (phase_51) does NOT assert it — then close the todo as already-resolved (move it to `.planning/todos/resolved/` if that directory convention exists in this repo, else annotate it with a resolution note and the resolving phase/commit). Do NOT change the contract test or ci.yml to "fix" it — it is already correct. For the design-gallery deferral todo: now that Task 2 removed the soft gate, mark its final step complete and close it the same way. Use whatever todo-close convention the repo already uses (check for a `resolved/` sibling directory under `.planning/todos/`).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && ! git grep -qn 'installer_milestone_audit:' .github/workflows/ci.yml && (ls .planning/todos/resolved/ 2>/dev/null | grep -qiE 'phase51|design-gallery' || grep -qiE 'resolved|closed|already-resolved' .planning/todos/pending/2026-06-20-phase51-installer-milestone-audit-ci-contract-stale.md)</automated>
  </verify>
  <acceptance_criteria>
    - The old `installer_milestone_audit:` job key is confirmed absent from ci.yml (no re-fix needed).
    - Both todos are closed via the repo's todo-resolution convention (moved to resolved/ or annotated closed).
    - No contract test or ci.yml change was made to "fix" the already-resolved Phase51 todo.
  </acceptance_criteria>
  <done>Both stale/deferral todos are closed honestly; D-07 hygiene complete with no re-fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR diff → merge gate (ci.yml + ruleset) | The CI config and branch ruleset decide what blocks merge. A mis-edit could strand a required check (merge outage) or silently de-gate a correctness-critical lane (false green). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-04 | Elevation (gate bypass) | design-gallery soft-gate | mitigate | D-06 hard re-gate removes `continue-on-error` and restores the aggregator loop entry, so a visual regression again blocks merge; landed only behind the Task 1 confirm-green checkpoint to avoid stranding the REQUIRED lane. |
| T-198-05 | Denial of service (merge outage) | required `name:` strings | mitigate | Task 2 forbids renaming any of the 5 names; Task 3 asserts the live ruleset still resolves exactly those 5 contexts. |
| T-198-06 | Tampering (silent contract break) | path-detector regex / MG-5/6 skip | mitigate | phase_51 + phase_192 contract locks (Task 3) fail if the ci.yml edit disturbs the ×2 regex or the skip marker. |
| T-198-07 | Repudiation (determinism regression) | snapshot baselines | mitigate | snapshot-canary-guard.sh (Task 3) confirms no baseline drift introduced by the edit. |
| T-198-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only ci.yml YAML edits, read-only `gh api`/test assertions, and todo-file moves. |
</threat_model>

<verification>
- Exactly one `continue-on-error: true` remains in ci.yml (the nightly OQ3 one).
- `steps.design_gallery.outcome` present in the aggregator loop.
- `gh api ...rulesets/14941512` returns the 5 known required-check names.
- `mix test test/sigra/planning/phase_51... phase_192...` green.
- `bash scripts/ci/snapshot-canary-guard.sh` passes.
- Both target todos closed.
</verification>

<success_criteria>
- Design-gallery is hard-gated again (D-06) without re-creating the bootstrap deadlock (confirm-green first).
- The 5 required-check names are proven byte-stable; ci.yml contract locks + snapshot determinism preserved.
- The already-resolved Phase51 todo and the design-gallery deferral todo are closed (D-07).
</success_criteria>

<output>
Create `.planning/phases/198-contributor-dx-acceptance-gate/198-02-SUMMARY.md` when done. Include the verbatim `gh api ...rulesets/14941512` required-name output for Plan 03 to cite.
</output>
