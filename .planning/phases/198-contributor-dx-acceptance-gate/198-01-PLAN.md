---
phase: 198-contributor-dx-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - CONTRIBUTING.md
  - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
autonomous: true
requirements: [DX-01]
must_haves:
  truths:
    - "A contributor can run `mix ci` to reproduce the locally-faithful portion of the PR gate (D-01)"
    - "`mix ci` chains only PR-gate legs and is never stricter than CI (D-02, D-03)"
    - "CONTRIBUTING documents `mix ci`, its Postgres + phx_new 1.8.7 prereqs, the CI-only excluded lanes, and the optional heavy scaffold smokes (D-04)"
    - "A contract-lock test fails if `mix ci` drifts out of mix.exs or CONTRIBUTING"
  artifacts:
    - path: "mix.exs"
      provides: "`ci` alias in aliases/0 chaining compile --warnings-as-errors + test + ci.install_golden + sigra.dep_off"
      contains: "ci:"
    - path: "CONTRIBUTING.md"
      provides: "DX-01 section documenting mix ci + prereqs + CI-only caveats"
      contains: "mix ci"
    - path: "test/sigra/planning/phase_198_contributor_dx_contract_test.exs"
      provides: "Contract lock asserting mix ci presence + CONTRIBUTING mention"
      contains: "Phase198"
  key_links:
    - from: "test/sigra/planning/phase_198_contributor_dx_contract_test.exs"
      to: "mix.exs"
      via: "reads mix.exs and asserts the `ci:` alias chains the four PR-gate legs"
      pattern: "ci.install_golden"
    - from: "test/sigra/planning/phase_198_contributor_dx_contract_test.exs"
      to: "CONTRIBUTING.md"
      via: "asserts CONTRIBUTING mentions `mix ci` and the phx_new 1.8.7 prereq"
      pattern: "mix ci"
---

<objective>
Deliver DX-01: a single documented local command (`mix ci`) that mirrors the locally-faithful portion of the PR-fast required gate, plus the CONTRIBUTING documentation and a contract-lock test that keeps the alias and its docs from drifting apart.

Purpose: A contributor can reproduce a red PR check locally with one command, without it being stricter than CI (which would produce false reds where CI is green).
Output: `ci` alias in mix.exs, a DX-01 section in CONTRIBUTING.md, and `phase_198_contributor_dx_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-contributor-dx-acceptance-gate/198-CONTEXT.md
@.planning/phases/198-contributor-dx-acceptance-gate/198-RESEARCH.md

# Files this plan touches
@mix.exs
@CONTRIBUTING.md
@test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
- `ci` mix alias in `mix.exs` `aliases/0` (chains `compile --warnings-as-errors`, `test`, `ci.install_golden`, `sigra.dep_off`).
- CONTRIBUTING.md section anchor: `## Reproducing the PR gate locally (mix ci)`.
- New test module `Sigra.Planning.Phase198ContributorDxContractTest` in `test/sigra/planning/phase_198_contributor_dx_contract_test.exs`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the `mix ci` alias to mix.exs aliases/0</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs aliases/0 (lines ~137-161): the existing `ci.audit_45`, `ci.install_golden`, `sigra.dep_off`, `test.db` aliases. The new `ci:` entry joins this same keyword list.
  </read_first>
  <action>
    In `mix.exs` `aliases/0`, add a new `"ci": [...]` entry to the returned keyword list, in the existing `ci.*` namespace. Per D-01 and D-02, it chains exactly four legs in order, each byte-mirroring a real PR-gate command: `"compile --warnings-as-errors"`, `"test"`, `"ci.install_golden"`, `"sigra.dep_off"`. Per D-03, do NOT add `format --check-formatted`, `credo --strict`, or `dialyzer` — none are in the PR gate, and adding them would make `mix ci` stricter than CI. Per D-04, do NOT fold in the heavy scaffold lanes (`install_smoke`, `example_http_smoke`) or any nightly-broad lane. Add a short head comment above the entry naming it as the DX-01 local mirror of the PR-fast required gate and noting the Postgres + phx_new 1.8.7 prerequisites (mirror the style of the adjacent `sigra.dep_off` comment). Keep the comment concept-level; do not embed a literal that a negative grep elsewhere would match.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix run -e 'IO.puts(if Keyword.has_key?(Mix.Project.config()[:aliases], :ci), do: "CI_ALIAS_PRESENT", else: "MISSING")' 2>/dev/null | grep -q CI_ALIAS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` `aliases/0` returns a `:ci` key whose value is the four-leg list `["compile --warnings-as-errors", "test", "ci.install_golden", "sigra.dep_off"]`.
    - No `format`, `credo`, or `dialyzer` leg present in the `:ci` alias.
    - `mix compile --warnings-as-errors` succeeds (the alias addition does not break compilation).
  </acceptance_criteria>
  <done>`mix.exs` defines a `:ci` alias chaining the four PR-gate legs and nothing stricter; project still compiles warning-free.</done>
</task>

<task type="auto">
  <name>Task 2: Document `mix ci` + prereqs + CI-only caveats in CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md "## Developing" and "## CI overview" sections — the new DX-01 section is added after "## Developing", reusing existing links to CLAUDE.md and the admin Playwright artifacts section.
    - CLAUDE.md "Local development prerequisites" — the canonical Postgres (`scripts/db/up.sh` + `source tmp/db.env`) and phx_new 1.8.7 archive instructions to reference (do not duplicate verbatim, link/summarize).
    - 198-RESEARCH.md §"Recommended `mix ci` Composition" — the exact tradeoff list to document.
  </read_first>
  <action>
    Add a new CONTRIBUTING.md section titled `## Reproducing the PR gate locally (mix ci)` (the anchor downstream consumers reference). Document, per D-01..D-04: (1) the command is `mix ci`, and what its four legs mirror (compile-with-warnings-as-errors, library tests, install-golden contract, dep-off guard); (2) the hard prerequisites — a live Postgres (point to CLAUDE.md / `scripts/db/up.sh` + `source tmp/db.env`) and the phx_new **1.8.7** archive (`mix archive.install --force hex phx_new 1.8.7`, per SEED-004), stating that a newer archive produces a spurious golden-diff red; (3) per D-03, that `format --check-formatted`, `credo --strict`, and `dialyzer` are deliberately NOT part of `mix ci` because they are not in the PR gate — list them separately as optional "additional local hygiene"; (4) per D-04, which PR-gate lanes are CI/ubuntu-only and intentionally excluded — name the ubuntu-baselined Playwright visual snapshot lanes (local macOS runs show font-metric pixel diffs; direct contributors to the `admin-example-report` CI artifact) and the heavy scaffold smokes (`install_smoke`, `example_http_smoke`), giving the exact optional commands `scripts/ci/install-smoke.sh` and boot + `scripts/ci/http-smoke.sh`; (5) the known non-regression local `mix test` failures (the v1.40 env/golden set — phx_new archive drift + UpgradeIntegrationTest env-DB) so a contributor does not mistake them for regressions. Keep prose directive; no fenced blocks are required but exact command strings may appear inline.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -qF 'mix ci' CONTRIBUTING.md && grep -qF '1.8.7' CONTRIBUTING.md && grep -qiE 'install-smoke\.sh|http-smoke\.sh' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md contains a `mix ci` section naming the four mirrored legs.
    - The Postgres prereq and the phx_new 1.8.7 archive command both appear.
    - The CI-only lanes (ubuntu Playwright snapshots) and the optional heavy scaffold smoke commands are documented.
    - format/credo/dialyzer are explicitly framed as optional, not part of the gate mirror.
  </acceptance_criteria>
  <done>CONTRIBUTING.md has a complete, honest DX-01 section a contributor can follow to reproduce a red PR check locally.</done>
</task>

<task type="auto">
  <name>Task 3: Add a contract-lock test so mix ci can't drift from CONTRIBUTING</name>
  <files>test/sigra/planning/phase_198_contributor_dx_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs — the exact contract-lock pattern (`root/0`, `read!/1`, `use ExUnit.Case, async: true`, plain `File.read!` + string/regex asserts). Model the new module on it.
  </read_first>
  <action>
    Create `test/sigra/planning/phase_198_contributor_dx_contract_test.exs` defining module `Sigra.Planning.Phase198ContributorDxContractTest`, modeled byte-for-style on `phase_51_install_golden_ci_contract_test.exs` (same `root/0` + `read!/1` helpers, `use ExUnit.Case, async: true`). Assert: (1) `mix.exs` contains a `:ci` alias chaining all four legs — assert the file contains the leg strings `ci.install_golden` and `sigra.dep_off` and `compile --warnings-as-errors` within the aliases region, and that a `ci:` key is declared; (2) `mix.exs` `:ci` alias does NOT contain `credo`, `dialyzer`, or `format --check-formatted` (the not-stricter-than-CI invariant, D-03) — scope this negative check to the aliases region or to the `:ci` entry so it does not trip on `format` appearing elsewhere; (3) `CONTRIBUTING.md` mentions `mix ci` and the phx_new `1.8.7` prereq. Use `Sigra.Test`-free plain reads (no PG, no app boot) so it runs in the fast `mix test test/sigra/planning/` lane. Do not put any literal that one of these assertions negative-greps for into a comment in the same file.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs --no-start</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs` passes green.
    - The test fails if the `:ci` alias is removed from mix.exs, if a stricter leg (credo/dialyzer/format) is added, or if CONTRIBUTING drops the `mix ci` mention.
    - The test needs no Postgres and runs in the fast contract-lock lane.
  </acceptance_criteria>
  <done>A contract-lock test pins `mix ci` to both mix.exs and CONTRIBUTING and enforces the not-stricter-than-CI invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → local `mix ci` | A documented command shapes a contributor's belief about whether their change is gate-clean. A misleading command/doc could create false confidence (false green) or false reds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-01 | Spoofing (false-green) | `mix ci` alias composition | mitigate | Alias chains only real PR-gate commands (D-02); contract test (Task 3) locks the four legs so it cannot silently shed coverage and report green while CI would red. |
| T-198-02 | Tampering (false-red / scope creep) | `mix ci` adding stricter legs | mitigate | D-03 excludes format/credo/dialyzer; Task 3 negative-asserts they are absent, so `mix ci` cannot drift stricter than CI. |
| T-198-03 | Repudiation (silent doc drift) | CONTRIBUTING vs mix.exs | mitigate | Contract-lock test (Task 3) fails if the alias and its docs diverge. |
| T-198-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only a mix-alias + docs + ExUnit test. No `[ASSUMED]`/`[SUS]` packages introduced. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` succeeds.
- `mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs` passes.
- `mix test test/sigra/planning/` (full contract-lock lane) stays green (no regression to sibling locks).
- CONTRIBUTING grep checks (Task 2 verify) pass.
</verification>

<success_criteria>
- `mix ci` exists, chains exactly the four PR-gate legs, and is no stricter than CI.
- CONTRIBUTING documents `mix ci`, its prereqs, the CI-only excluded lanes, and the optional heavy smokes.
- A contract-lock test prevents drift between the alias and its documentation.
</success_criteria>

<output>
Create `.planning/phases/198-contributor-dx-acceptance-gate/198-01-SUMMARY.md` when done.
</output>
