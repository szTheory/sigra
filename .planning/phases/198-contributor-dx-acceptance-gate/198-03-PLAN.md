---
phase: 198-contributor-dx-acceptance-gate
plan: 03
type: execute
wave: 3
depends_on: ["198-02"]
files_modified:
  - .planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md
  - MAINTAINING.md
autonomous: false
requirements: [GATE-01, GATE-02]
must_haves:
  truths:
    - "A committed `198-ACCEPTANCE.md` diffs REAL post-197 CI timings against the 193 baseline (wall-clock, p95, flake-rate) — measured, not estimated (D-05, GATE-01)"
    - "The after-numbers demonstrate the PR path is meaningfully faster than the ~38m baseline with equal-or-greater required-gate quality signal"
    - "The 5 enforced required-check names are asserted unchanged and the flake-rate is recorded (GATE-02)"
    - "An ADD-only pointer to 198-ACCEPTANCE.md exists in MAINTAINING.md"
  artifacts:
    - path: ".planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md"
      provides: "Before/after acceptance evidence table mirroring 193-BASELINE.md shape"
      contains: "198-ACCEPTANCE"
    - path: "MAINTAINING.md"
      provides: "ADD-only pointer to the acceptance artifact"
      contains: "198-ACCEPTANCE.md"
  key_links:
    - from: ".planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md"
      to: ".planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md"
      via: "diffs after-numbers against the 193 baseline run-level wall-clock + per-long-pole p95 table"
      pattern: "193-BASELINE"
    - from: "MAINTAINING.md"
      to: ".planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md"
      via: "ADD-only pointer line referencing the acceptance evidence"
      pattern: "198-ACCEPTANCE"
---

<objective>
Deliver GATE-01 (and the evidence half of GATE-02): author a committed `198-ACCEPTANCE.md` that diffs REAL post-197 CI run timings (wall-clock, p95, flake-rate) — captured via `gh run view --json jobs`, not estimated — against the Phase 193 baseline, proving the PR path is meaningfully faster with equal-or-greater required-gate quality signal, and add an ADD-only pointer from MAINTAINING.md.

Purpose: Close the v1.40 CI-PERF milestone honestly with measured before/after evidence (zero-human-UAT: CI measures itself).
Output: `198-ACCEPTANCE.md` + a MAINTAINING.md pointer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-contributor-dx-acceptance-gate/198-CONTEXT.md
@.planning/phases/198-contributor-dx-acceptance-gate/198-RESEARCH.md
@.planning/phases/198-contributor-dx-acceptance-gate/198-02-SUMMARY.md

# Reference artifacts
@.planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md
@MAINTAINING.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- `.planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md` — the committed before/after acceptance evidence.
- ADD-only pointer line in `MAINTAINING.md` referencing `198-ACCEPTANCE.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Capture REAL post-197 PR-path CI timings via gh</name>
  <files>.planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md</files>
  <read_first>
    - 193-BASELINE.md "How wall-clock + p95 are measured" + the run-level wall-clock table (the exact `gh run view --json jobs` method and the n=6 2026-06-19 cohort to diff against: avg ~38.4m, p95 ~40m; per-long-pole example_playwright_smoke 22.2m, library_tests 16.0m, library_tests_dep_off 13.8m).
    - 198-RESEARCH.md "Acceptance Evidence Authoring" — the honest-after-capture protocol and the recommended `198-ACCEPTANCE.md` section list.
  </read_first>
  <action>
    Gather the after-numbers from REAL CI runs on the post-197 (post-re-gate) pipeline — do NOT substitute estimates (per D-05 and the zero-human-UAT contract). Steps: (1) `gh run list --workflow CI --json databaseId,event,conclusion,headSha,createdAt,updatedAt --limit 40` and select the PR-path runs on the current ci.yml (post the Plan 02 re-gate where possible; at minimum post-197). If too few PR-path runs exist to compute a meaningful p95, trigger/collect additional runs rather than estimating (note in the artifact how many runs the n= sample is). (2) For each selected run, `gh run view <id> --json jobs --jq '.jobs[] | {name, conclusion, startedAt, completedAt}'` and compute per-job durations and the run-level wall-clock. (3) Compute p95 across the sample and the flake-rate (failed-then-passed reruns / total). (4) Begin authoring `198-ACCEPTANCE.md` with the raw captured numbers in a per-long-pole + run-level wall-clock table mirroring 193-BASELINE's shape. Record the run IDs and capture date so the evidence is falsifiable.
  </action>
  <verify>
    <human-check>
      Author confirms `198-ACCEPTANCE.md` contains real captured run IDs and per-job/run-level durations from `gh run view --json jobs` (not estimates), with the sample size n= stated. (Manual per VALIDATION: real "after" CI timings require actual GitHub runs and cannot be synthesized locally.)
    </human-check>
  </verify>
  <acceptance_criteria>
    - `198-ACCEPTANCE.md` contains real run IDs, capture date, and per-job + run-level durations from `gh run view --json jobs`.
    - p95 and flake-rate are computed from a stated sample size, not estimated.
    - The after-numbers are drawn from the post-197 (ideally post-re-gate) pipeline.
  </acceptance_criteria>
  <done>Real post-197 PR-path CI timings are captured into 198-ACCEPTANCE.md with falsifiable run IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Author the before/after acceptance evidence + quality-signal parity proof</name>
  <files>.planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md</files>
  <read_first>
    - 198-02-SUMMARY.md — the verbatim `gh api ...rulesets/14941512` required-name output captured in Plan 02 (cite it here rather than re-running, or re-run to confirm).
    - 193-BASELINE.md — the before-numbers and table columns to diff against.
    - 198-RESEARCH.md "Recommended artifact: 198-ACCEPTANCE.md" — the required sections (before/after wall-clock+p95 table; quality-signal parity; flake-rate; tradeoffs ledger; SEED-004 + determinism attestation).
  </read_first>
  <action>
    Complete `198-ACCEPTANCE.md` per D-05, mirroring 193-BASELINE's table shape so the diff is mechanical. Required sections: (1) Before/after wall-clock + p95 table — run-level and per-long-pole job (example_playwright_smoke, library_tests, library_tests_dep_off) — showing the PR path is meaningfully faster than the ~38m baseline (GATE-01 target: well under ~22m, ideally under ~12m fast path); if the target is not met, state the actual delta honestly rather than overclaiming. (2) Quality-signal parity proof: the 5 enforced required name strings unchanged (paste the ruleset `gh api` output from 198-02-SUMMARY), the ci-gate `needs:` list unchanged, and a reference to the 196 D-08 per-moved-job proxy table confirming no correctness-critical test stranded nightly-only — plus the now-hard-gated design-gallery (D-06) as a strengthened, not weakened, signal. (3) Flake-rate: before vs after rerun rate; confirm FLAKE-01 (demo-showcase rgb) de-flaked and that retries are not masking real failures. (4) Tradeoffs ledger: any speed-for-trust move tiered to nightly (the 196 broad split) — reference MAINTAINING.md "CI cadence"; assert 198 moved nothing new to nightly. (5) SEED-004 + determinism attestation: phx_new 1.8.7 still pinned (grep-auditable in ci.yml), snapshot-canary-guard still green. Cite D-05/D-06 by ID in the relevant sections.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -qF '193-BASELINE' .planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md && grep -qiE 'p95|wall-clock' .planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md && grep -qF 'Library tests' .planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md && grep -qF '1.8.7' .planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `198-ACCEPTANCE.md` has a before/after wall-clock + p95 table diffed against 193-BASELINE.
    - The 5 required-check name strings are pasted/asserted unchanged; ci-gate needs-list parity noted.
    - Flake-rate, tradeoffs ledger, and SEED-004 + determinism attestation sections are present.
    - GATE-01's faster-with-equal-or-greater-signal claim is supported by real numbers or the shortfall is stated honestly.
  </acceptance_criteria>
  <done>198-ACCEPTANCE.md is a complete, falsifiable before/after acceptance artifact proving the GATE-01/GATE-02 claims.</done>
</task>

<task type="auto">
  <name>Task 3: Add an ADD-only MAINTAINING.md pointer to the acceptance artifact</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md "CI cadence — PR-fast vs nightly/main-broad (Phase 196)" section (~lines 124-139) — the ADD-only insertion point, matching how 196-04 appended its CI-cadence subsection.
  </read_first>
  <action>
    Per D-05, append (ADD-only — do not rewrite or delete existing content) a short pointer in MAINTAINING.md, near the "CI cadence" section, referencing `.planning/phases/198-contributor-dx-acceptance-gate/198-ACCEPTANCE.md` as the committed before/after acceptance evidence for the v1.40 CI-PERF milestone (measured wall-clock/p95/flake-rate vs the 193 baseline, with required-name stability attestation). Keep it to a sentence or two; do not duplicate the table. Preserve every existing required-check name string and the CI-cadence prose exactly (no edits to the 5 names — phase_51/GATE-02 invariants).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -qF '198-ACCEPTANCE.md' MAINTAINING.md && grep -qF 'Library tests' MAINTAINING.md && grep -qF 'Example Playwright smoke (full lifecycle)' MAINTAINING.md</automated>
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md contains an ADD-only pointer to 198-ACCEPTANCE.md.
    - All 5 required-check name strings in MAINTAINING.md are unchanged.
    - No existing MAINTAINING.md content was deleted or rewritten.
  </acceptance_criteria>
  <done>MAINTAINING.md points to the acceptance evidence via an ADD-only line, with required-check names intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acceptance evidence → milestone closeout | The committed before/after table is the durable record on which the "milestone shipped honestly" claim rests. Estimated or cherry-picked numbers would misrepresent the gate's real performance/quality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-08 | Repudiation (false evidence) | 198-ACCEPTANCE.md after-numbers | mitigate | Numbers come from real `gh run view --json jobs` runs with recorded run IDs (Task 1); estimates are forbidden (D-05); if the target is missed, the shortfall is stated honestly (Task 2). |
| T-198-09 | Information disclosure / overclaim | quality-signal parity proof | mitigate | The 5 required names are pasted from the live ruleset and the D-08 proxy table is referenced, so the "equal-or-greater signal" claim is verifiable, not asserted. |
| T-198-10 | Tampering (doc regression) | MAINTAINING.md required names | mitigate | Task 3 is ADD-only; verify asserts the 5 name strings remain present and unchanged. |
| T-198-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only read-only `gh` capture and Markdown authoring. |
</threat_model>

<verification>
- `198-ACCEPTANCE.md` exists with real run IDs, a before/after wall-clock+p95 table vs 193-BASELINE, the 5 required-name attestation, flake-rate, tradeoffs ledger, and SEED-004/determinism attestation.
- MAINTAINING.md has an ADD-only pointer to 198-ACCEPTANCE.md with the 5 required-check names unchanged.
- Task 2 + Task 3 grep checks pass.
</verification>

<success_criteria>
- A committed `198-ACCEPTANCE.md` proves, with measured numbers, the PR path is meaningfully faster than the 193 baseline with equal-or-greater required-gate quality signal (GATE-01).
- Required-check names asserted stable and flake-rate recorded (GATE-02 evidence half).
- MAINTAINING.md points to the artifact (ADD-only).
</success_criteria>

<output>
Create `.planning/phases/198-contributor-dx-acceptance-gate/198-03-SUMMARY.md` when done.
</output>
