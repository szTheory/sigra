---
phase: 191-microcopy-ia-sweep
plan: 03
type: execute
wave: 3
depends_on:
  - 191-02
files_modified:
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - COPY-01
  - COPY-03

must_haves:
  truths:
    - "A new branding-live L3 row is appended to the quality ledger (L3 goes from 6 to 7 rows)"
    - "Existing L3 rows have their D9 IA and D10 Microcopy tier columns re-scored — integers only increase, never decrease"
    - "The quality-ledger-monotonic.sh guard passes on the updated ledger vs origin/main"
    - "The branding-live row's Evidence column references the admin-checkpoints spec as its executable evidence (per D-11)"
  artifacts:
    - path: "guides/reference/admin-quality-ledger.md"
      provides: "Updated quality ledger with branding-live L3 row + D9/D10 axis re-scores"
      contains: "branding-live"
  key_links:
    - from: "guides/reference/admin-quality-ledger.md"
      to: "scripts/ci/quality-ledger-monotonic.sh"
      via: "monotonic guard parses tier column (column 4)"
      pattern: "quality-ledger-monotonic"
---

<objective>
Add the `branding-live` L3 row to the quality ledger (D-11), fulfilling the explicit-scoring requirement for the non-archetypal Branding customizer page (ROADMAP v1.39 SC #4 / D-12 folded todo). Re-score existing L3 rows on the D9 IA and D10 Microcopy axes now that the glossary and voice pass are complete.

Purpose: Raise the ledger monotonically on the copy/IA axes. The `branding-live` row closes the maintainer-pinned explicit-scoring todo that was deferred from Phase 189.

Output: `guides/reference/admin-quality-ledger.md` updated with 1 new row + D9/D10 re-scores on existing rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/191-microcopy-ia-sweep/191-CONTEXT.md
@.planning/phases/191-microcopy-ia-sweep/191-RESEARCH.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
</context>

<interfaces>
<!-- Existing L3 rows in the ledger (from admin-quality-ledger.md, read earlier): -->
<!-- All currently Tier 1. D9 / D10 axes are part of D1-D11 shared dimensions used for scoring. -->

Existing L3 rows:
  | index-live | L3 | 1 | [admin-checkpoints global-overview — 3 projects × toHaveScreenshot + axe](...) |
  | organization-live | L3 | 1 | [admin-checkpoints org-overview — 3 projects × toHaveScreenshot + axe](...) |
  | users-index-live | L3 | 1 | [admin-checkpoints global-user-index — 3 projects × toHaveScreenshot + axe](...) |
  | user-show-live | L3 | 1 | [admin-checkpoints user-detail + admin-modal-interaction](...) |
  | audit-index-live | L3 | 1 | [admin-checkpoints audit-explorer — 3 projects × toHaveScreenshot + axe](...) |
  | audit-user-live | L3 | 1 | [admin-checkpoints user-audit — 3 projects × toHaveScreenshot + axe](...) |

branding-live was previously unscored (deferred from Phase 189 — D-12 folded todo).

<!-- Evidence string for branding-live (the checkpoint slug is NOT one of the 8 standard ones; -->
<!-- branding page is not in admin-checkpoints.spec.ts. Use admin-modal-interaction reference -->
<!-- for the ConfirmDialog overlay, and note the design-contract explict page scoring. -->
<!-- The recapture in Wave 4 does not include a branding-live slug (branding_live has no -->
<!-- checkpoint PNG). Evidence = the voice pass + design-contract page scoring done in this phase. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add branding-live L3 row and re-score D9/D10 on all L3 rows</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md — read the full current ledger; note current tier values for all L3 rows (all currently Tier 1); note the parsing rule (column 4 = tier, single integer, `|`-delimited)
    - guides/reference/admin-fractal-scorecard.md — lines 28–36 (D9 IA/least-surprise and D10 Microcopy axes); lines 60–90 (L3 Page add-ons: archetype conformance, GOV.UK IA, overlay, a11y)
    - .planning/phases/191-microcopy-ia-sweep/191-CONTEXT.md — D-11 (branding-live row, D9/D10 re-score, monotonic only, tier promotion 1→2 only where genuinely award-grade)
    - .planning/phases/191-microcopy-ia-sweep/191-RESEARCH.md — §Validation Architecture (Phase Requirements → Test Map) for evidence links per D-11
  </read_first>

  <action>
  Update `guides/reference/admin-quality-ledger.md`:

  Step 1 — Add the `branding-live` L3 row after `audit-user-live`:

  ```
  | branding-live | L3 | 1 | [admin-modal-interaction: ConfirmDialog APG gates + axe-while-open](../../test/example/priv/playwright/tests/admin-modal-interaction.spec.ts); Phase 191 voice pass: D9 IA (GOV.UK page arch, verb-first, scope-visible), D10 Microcopy (brand-book-aligned, no leaked internals, no synonym drift) |
  ```

  Evidence note: branding_live does not have a dedicated checkpoint PNG (it was not in the 8 checkpoints from v1.36). The evidence is the admin-modal-interaction spec (for the ConfirmDialog APG behavior) + the Phase 191 voice/IA scoring documented inline. Tier is 1 (Ratified) — the page passes all required L3 axes but has not undergone an award-grade micro-interaction pass.

  Step 2 — Re-score D9/D10 axes on all existing L3 rows. Per D-11: integers may only increase; Tier 2 only where genuinely award-grade. Assessment:

  After the Phase 191 copy pass, all 7 admin LiveViews now pass the D9 (IA/least-surprise) and D10 (Microcopy) axes against the new glossary and voice rubric. However, the surfaces already held Tier 1 and the copy pass delivers compliance, not award-grade differentiation. The monotonic constraint means we may only raise. Assessment per surface:

  - index-live: D9 and D10 now pass the rubric; copy is terse, verb-first, no drift. Tier 1 → stays 1 (compliant, not award-grade).
  - organization-live: same assessment. Tier 1 → stays 1.
  - users-index-live: same. Tier 1 → stays 1.
  - user-show-live: improved copy (passive voice removed, leaked internal fixed, session count heading clarified). Tier 1 → stays 1 (improvements within Ratified bar).
  - audit-index-live: 0 violations pre-pass; Tier 1 → stays 1.
  - audit-user-live: 0 violations pre-pass; Tier 1 → stays 1.
  - branding-live: new row, Tier 1 as assessed above.

  Therefore no existing tier values change in this update — the tier column remains `1` for all existing rows. The ONLY change to the existing rows is optionally updating the Evidence column text to reference the Phase 191 voice-pass evidence. If the evidence column already has a valid link, do not change it.

  Do NOT change any tier value downward. Do NOT add a new column. Do NOT add a "pseudo-row" for cross-cutting D9/D10 — the ledger schema is strictly per-surface per D-11.

  The `branding-live` row is the sole addition. Append it after `audit-user-live` in the L3 block, before the L4 rows.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c "branding-live" guides/reference/admin-quality-ledger.md && bash scripts/ci/quality-ledger-monotonic.sh 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - guides/reference/admin-quality-ledger.md contains exactly 1 row with "branding-live" in the Item column
  - The branding-live row has tier value "1" (integer, column 4 per the parsing rule)
  - The branding-live row's Evidence column references admin-modal-interaction.spec.ts and/or the Phase 191 voice pass
  - Total L3 rows = 7 (was 6; branding-live added)
  - No existing tier value has decreased (all remain 1)
  - `bash scripts/ci/quality-ledger-monotonic.sh` exits 0 (monotonic guard green vs base)
  </acceptance_criteria>

  <done>branding-live L3 row appended at Tier 1 with evidence; all existing rows unchanged; quality-ledger-monotonic.sh exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger → monotonic guard | quality-ledger-monotonic.sh parses column 4 (tier); any decrease causes CI failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-01 | Information Disclosure | Auth/enumeration boundary | accept | No auth-boundary strings in this plan; ledger update only |
| T-191-04 | Tampering | Ledger tier decrease (monotonic regression) | mitigate | acceptance_criteria require `quality-ledger-monotonic.sh` exits 0; all existing tier values stay at 1 or increase |
| T-191-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After Wave 3 completes:
- `grep -c "branding-live" guides/reference/admin-quality-ledger.md` returns 1
- L3 block has 7 rows (was 6)
- `bash scripts/ci/quality-ledger-monotonic.sh` exits 0
- No tier value decreases anywhere in the ledger
</verification>

<success_criteria>
- branding-live L3 row present at Tier 1 with executable evidence link
- Monotonic guard green
- Ledger re-score complete (D9/D10 axes assessed; no tier regressions)
</success_criteria>

<output>
Create `.planning/phases/191-microcopy-ia-sweep/191-03-SUMMARY.md` when done.
</output>
