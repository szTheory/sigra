---
phase: 191-microcopy-ia-sweep
plan: 04
type: execute
wave: 4
depends_on:
  - 191-03
files_modified:
  - test/example/priv/playwright/snapshot-allowlist
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03

must_haves:
  truths:
    - "The 5 affected checkpoint slugs (global-overview, user-detail, org-overview, global-user-index, org-scoped-admin) have recaptured baselines matching the updated copy"
    - "The canary PNG files (impersonation-banner, board-notice) are restored to their pre-recapture state"
    - "snapshot-recapture-gate.sh passes: compare 3/3 projects + canary guard all-green"
    - "The snapshot-allowlist is empty after the gate passes (reset per D-10 phase-end protocol)"
    - "Full mix test suite exits 0"
  artifacts:
    - path: "test/example/priv/playwright/snapshot-allowlist"
      provides: "Empty after gate passes (steady-state for main)"
      contains: ""
  key_links:
    - from: "scripts/ci/snapshot-recapture-gate.sh"
      to: "test/example/priv/playwright/snapshot-allowlist"
      via: "canary guard reads allowlist to verify canaries never listed"
      pattern: "snapshot-allowlist"
    - from: "npx playwright test --update-snapshots"
      to: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts"
      via: "recapture updates PNG baselines for allowed slugs"
      pattern: "toHaveScreenshot"
---

<objective>
Recapture the 5 affected Playwright baseline PNGs whose visible copy changed in Wave 2. Uses the Phase 183-proven zero-human recapture sequence: declare slugs in allowlist → boot pre-compiled example dev server on PORT=4011 → npx playwright test --update-snapshots=all → restore canary PNGs → run snapshot-recapture-gate.sh → reset allowlist to empty.

Purpose: Keep the admin-checkpoints visual baselines in sync with the updated copy so CI stays green without human PNG review (D-10 / zero-human UAT).

Output: Recaptured PNG baselines for 5 slugs × 3 projects (15 PNG files updated). snapshot-allowlist reset to empty. Recapture gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/191-microcopy-ia-sweep/191-CONTEXT.md
@.planning/phases/191-microcopy-ia-sweep/191-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare affected slugs in snapshot-allowlist, boot example server, recapture baselines</name>
  <files>test/example/priv/playwright/snapshot-allowlist</files>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist — read current content to confirm it is empty (steady-state after Phase 190)
    - .planning/phases/191-microcopy-ia-sweep/191-RESEARCH.md — §Literal Text Assertions (affected slugs list: global-overview, user-detail, org-overview, global-user-index, org-scoped-admin); §Recapture Wave Sequence (Phase 183-proven procedure)
    - scripts/ci/snapshot-recapture-gate.sh — lines 1–50 to confirm gate invocation syntax
    - .planning/phases/191-microcopy-ia-sweep/191-CONTEXT.md — D-10 (exact recapture sequence + allowlist protocol; NEVER allowlist canaries impersonation-banner/board-notice)
  </read_first>

  <action>
  Execute the recapture sequence exactly as documented in D-10, mirroring Phase 183:

  Step 1 — Declare affected slugs in `test/example/priv/playwright/snapshot-allowlist`:
  Add these 5 lines (one slug per line, no duplicates):
  ```
  global-overview
  user-detail
  org-overview
  global-user-index
  org-scoped-admin
  ```
  CRITICAL: Do NOT add `impersonation-banner` or `board-notice` — these are canaries and must NEVER be allowlisted.

  Step 2 — Pre-compile and boot the example dev server on PORT=4011:
  ```bash
  cd test/example && PORT=4011 MIX_ENV=dev mix compile
  PORT=4011 MIX_ENV=dev mix phx.server &
  # Wait for server to be ready (check http://localhost:4011/admin/health or similar)
  ```
  The server must be pre-compiled before booting to avoid code-reload crashes during Playwright runs (lesson from Phase 183).

  Step 3 — Run Playwright with --update-snapshots=all across all 3 admin-checkpoints projects:
  ```bash
  cd test/example/priv/playwright
  npx playwright test tests/admin-checkpoints.spec.ts \
    --update-snapshots=all \
    --project=admin-checkpoints-chromium \
    --project=admin-checkpoints-mobile \
    --project=admin-checkpoints-dark
  ```
  This recaptures ALL slugs but the allowlist + canary guard will catch any unintended changes in Step 5.

  Step 4 — Restore the canary PNGs to their pre-recapture state:
  ```bash
  git checkout -- test/example/priv/playwright/tests/*.spec.ts-snapshots/impersonation-banner-*.png
  git checkout -- test/example/priv/playwright/tests/*.spec.ts-snapshots/board-notice-*.png
  ```
  The exact paths depend on the snapshot directory naming; adjust glob if necessary.

  Step 5 — Run the recapture gate:
  ```bash
  bash scripts/ci/snapshot-recapture-gate.sh \
    global-overview user-detail org-overview global-user-index org-scoped-admin
  ```
  Gate checks: (a) compare-mode admin-checkpoints 3 projects; (b) drift/canary guard --require-all for all 5 slugs; (c) compare-mode admin design gallery 3 projects (no design gallery slugs changed, so this should pass trivially).

  If the gate exits 0: proceed to Step 6.
  If the gate exits non-zero: read the error output and diagnose — most likely cause is a canary PNG accidentally recaptured (restore it) or a slug unexpectedly not changed (verify the copy edit reached that slug's viewport).

  Step 6 — Reset the allowlist to empty:
  Overwrite `test/example/priv/playwright/snapshot-allowlist` with only the header comment lines (the original empty state). Leave no slug entries.

  Step 7 — Stop the example dev server (kill the background phx.server process).

  Step 8 — Run the full test suite to confirm everything is green:
  ```bash
  mix test
  ```
  </action>

  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/snapshot-recapture-gate.sh global-overview user-detail org-overview global-user-index org-scoped-admin 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
  - `test/example/priv/playwright/snapshot-allowlist` is empty after Step 6 (only comment lines, no slug entries)
  - `scripts/ci/snapshot-recapture-gate.sh global-overview user-detail org-overview global-user-index org-scoped-admin` exits 0
  - `scripts/ci/snapshot-canary-guard.sh` confirms impersonation-banner and board-notice canary PNGs are UNCHANGED (git diff shows no PNG delta for canary files)
  - 15 PNG files (5 slugs × 3 projects) show changes in `git diff --stat` (the recaptured baselines)
  - 0 PNG files for canary slugs changed in `git diff --stat`
  - `mix test` exits 0 (full suite green including glossary_test, components_test, and ExUnit suite)
  </acceptance_criteria>

  <done>15 baseline PNGs recaptured; canaries restored and unchanged; snapshot-recapture-gate.sh exits 0; snapshot-allowlist reset to empty; mix test exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recapture → canary guard | impersonation-banner and board-notice canary PNGs must never be in the allowlist; the canary guard enforces this and the gate will fail if they drift |
| snapshot-allowlist → CI | The allowlist must be empty at merge time so main stays green between phases; non-empty allowlist would pass unexpected screenshot changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-01 | Information Disclosure | Auth/enumeration boundary | accept | No auth-boundary strings in this plan; Playwright recapture only |
| T-191-05 | Tampering | Canary PNG accidentally recaptured/allowlisted | mitigate | Step 4 explicitly restores canary PNGs via git checkout; Step 5 gate --require-all verifies canaries unchanged; acceptance_criteria require 0 canary PNG delta in git diff |
| T-191-06 | Tampering | Allowlist not reset to empty at end of phase | mitigate | Step 6 explicitly resets allowlist; acceptance_criteria verify empty allowlist at end |
| T-191-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; uses existing node_modules in test/example/priv/playwright/ |
</threat_model>

<verification>
After Wave 4 completes — the full phase 191 completion check:
- `test/example/priv/playwright/snapshot-allowlist` is empty (only comment header)
- `bash scripts/ci/snapshot-recapture-gate.sh global-overview user-detail org-overview global-user-index org-scoped-admin` exits 0
- `git diff --stat` shows ~15 changed PNG files for the 5 affected slugs and 0 canary PNG changes
- `mix test` exits 0 (full suite)
- `bash scripts/ci/quality-ledger-monotonic.sh` exits 0 (from Wave 3)
- `guides/reference/admin-glossary.md` exists (from Wave 1)
- `test/sigra/admin/glossary_test.exs` exists and passes (from Waves 1+2)
</verification>

<success_criteria>
- 15 PNG baselines recaptured for 5 affected slugs × 3 Playwright projects
- Canary PNGs (impersonation-banner, board-notice) unchanged
- Allowlist empty at phase completion
- Full mix test suite green
- Phase 191 is self-contained: copy + guard + ledger + recapture in one same-diff (per D-10, mirroring Phase 183)
</success_criteria>

<output>
Create `.planning/phases/191-microcopy-ia-sweep/191-04-SUMMARY.md` when done.
</output>
