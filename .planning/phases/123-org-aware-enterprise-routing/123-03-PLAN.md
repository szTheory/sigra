---
phase: 123-org-aware-enterprise-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 123-02
files_modified:
  - test/example/lib/example/organizations.ex
  - test/example/lib/example_web/controllers/session_controller.ex
  - test/example/lib/example_web/controllers/session_html.ex
  - test/example/lib/example_web/controllers/enterprise_sso_controller.ex
  - test/example/lib/example_web/router.ex
  - test/example/test/example_web/controllers/session_controller_test.exs
  - test/example/test/example_web/controllers/enterprise_sso_controller_test.exs
  - test/example/test/example_web/integration/enterprise_sso_routing_flow_test.exs
autonomous: true
requirements:
  - SSO-03
tags: [enterprise-sso, example-app, phoenix, routing, controller-tests]
must_haves:
  truths:
    - "The committed example app exposes the same enterprise login entry and callback contract as the generated templates."
    - "Example-app controller tests prove discovery redirects into the canonical org route and enterprise callback failures stay in the same mode."
    - "Example-app integration proof shows the resolved organization survives discovery, callback, and first authenticated scope hydration."
  artifacts:
    - path: test/example/lib/example_web/controllers/enterprise_sso_controller.ex
      provides: "example-app canonical org-scoped enterprise entry and callback implementation"
    - path: test/example/test/example_web/controllers/enterprise_sso_controller_test.exs
      provides: "Wave 0 controller proof for explicit org entry and same-mode recovery UI"
    - path: test/example/test/example_web/integration/enterprise_sso_routing_flow_test.exs
      provides: "Wave 0 integration proof for org discovery to callback to hydrated scope"
  key_links:
    - from: test/example/lib/example_web/controllers/session_controller.ex
      to: test/example/lib/example/organizations.ex
      via: "enterprise discovery delegates into library-owned routing before redirect"
      pattern: "discover_enterprise_connection|redirect\\("
    - from: test/example/lib/example_web/controllers/enterprise_sso_controller.ex
      to: lib/sigra/oauth.ex
      via: "example app persists enterprise session params, then calls `handle_callback`"
      pattern: "authorize_url|handle_callback|enterprise_context"
    - from: test/example/test/example_web/integration/enterprise_sso_routing_flow_test.exs
      to: lib/sigra/session_stores/ecto.ex
      via: "first authenticated request must hydrate the correct active organization"
      pattern: "active_organization_id|current_scope"
---

<objective>
Sync the committed example Phoenix app with the new enterprise routing surface and lock it with controller plus integration proof.

Purpose: make the generated-host contract executable and reviewable in the repo's example app so `SSO-03` is demonstrated in real controller and request flows, not only in templates.
Output: example app routing and controller updates, Wave 0 controller and end-to-end integration tests in the `test/example` Mix project, and committed proof that org truth survives discovery through the first authenticated request.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/123-org-aware-enterprise-routing/123-CONTEXT.md
@.planning/phases/123-org-aware-enterprise-routing/123-RESEARCH.md
@.planning/phases/123-org-aware-enterprise-routing/123-PATTERNS.md
@.planning/phases/123-org-aware-enterprise-routing/123-02-PLAN.md
@test/example/lib/example/organizations.ex
@test/example/lib/example_web/controllers/session_controller.ex
@test/example/lib/example_web/controllers/session_html.ex
@test/example/lib/example_web/router.ex
@test/example/test/example_web/controllers/session_controller_test.exs

<interfaces>
From `test/example/lib/example/organizations.ex`:
```elixir
def get_enterprise_connection(scope)
def change_enterprise_connection(scope, attrs \\ %{})
def save_enterprise_connection(scope, attrs)
def validate_enterprise_connection(scope, attrs)
def activate_enterprise_connection(scope, attrs)
def disable_enterprise_connection(scope)
```

From `test/example/lib/example_web/controllers/session_controller.ex`:
```elixir
def new(conn, _params)
def create(conn, %{"_action" => "registered"} = params)
def create(conn, %{"_action" => "confirmed"} = params)
def create(conn, %{"_action" => "password_updated"} = params)
def create(conn, %{"user" => user_params})
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example login form input -> org-scoped redirect | The example app must not begin OIDC from email-only discovery or leak domain inventory. |
| example callback handling -> rendered retry page | Example UX must preserve enterprise mode and resolved org truth on failure. |
| example session creation -> first authenticated request | Hydrated scope must reflect the initiating organization rather than a later inference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-10 | T | example `SessionController` | mitigate | Keep generic discovery as a redirect-only step into `/organizations/:org/sso`; do not call OIDC directly per D-02. |
| T-123-11 | I | example enterprise retry UX | mitigate | Keep generic discovery failures bounded and only show explicit org truth after the org is already resolved per D-11/D-12. |
| T-123-12 | R | example integration flow | mitigate | Assert the same org survives callback and first authenticated request via integration proof, not controller-only coverage. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Sync example-app enterprise entry controllers and controller tests</name>
  <files>test/example/lib/example/organizations.ex, test/example/lib/example_web/controllers/session_controller.ex, test/example/lib/example_web/controllers/session_html.ex, test/example/lib/example_web/controllers/enterprise_sso_controller.ex, test/example/lib/example_web/router.ex, test/example/test/example_web/controllers/session_controller_test.exs, test/example/test/example_web/controllers/enterprise_sso_controller_test.exs</files>
  <behavior>
    - Test 1: `GET /users/log_in` shows a separate enterprise discovery form and `POST /users/log_in` with `_action=enterprise` redirects to `/organizations/:org/sso` on one exact verified match.
    - Test 2: `GET /organizations/:org/sso` renders lightweight org truth before authorize, and `POST /organizations/:org/sso` starts OIDC with stored `enterprise_context`.
    - Test 3: callback failure branches return users to the same enterprise flow with bounded retry guidance instead of silently falling back to password, magic link, or passkey login.
  </behavior>
  <action>Follow the controller aliasing and render patterns from `test/example/lib/example_web/controllers/session_controller.ex` and the role-match guidance in `123-PATTERNS.md`. Update the committed example `Organizations` helper with the same thin delegates emitted by the templates so library-owned routing stays authoritative per D-03/D-13. Then wire the example login controller, login HTML, enterprise controller, and router to match the template contract from Plan 02: a separate enterprise discovery branch on `/users/log_in`, a canonical `/organizations/:org/sso` route family, lightweight org truth per D-11/D-12, and same-mode recovery per D-08/D-09. Add controller tests first in the `test/example` Mix lane, using `(cd test/example && mix test ...)`, and keep all authorize and callback steps anchored to the Plan 01 library APIs rather than duplicating security logic in the example app.</action>
  <verify>
    <automated>(cd test/example && mix test test/example_web/controllers/session_controller_test.exs test/example_web/controllers/enterprise_sso_controller_test.exs)</automated>
  </verify>
  <done>The committed example app matches the template-owned enterprise entry surface and has controller proof for bounded discovery, canonical org entry, and same-mode callback recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add example-app end-to-end enterprise routing flow proof</name>
  <files>test/example/test/example_web/integration/enterprise_sso_routing_flow_test.exs</files>
  <behavior>
    - Test 1: a work-email discovery POST resolves the expected organization, completes the enterprise callback path, and lands the user in an authenticated request whose hydrated scope reflects that organization.
    - Test 2: a mismatched or unavailable connection fails before the first authenticated request and preserves enterprise-mode recovery semantics.
  </behavior>
  <action>Follow the request-level proof style from `test/example/test/example_web/smoke/session_active_org_round_trip_test.exs` and the hydrated-scope link called out in `123-PATTERNS.md`. Add `test/example/test/example_web/integration/enterprise_sso_routing_flow_test.exs` as Wave 0 coverage for the full Phase 123 contract: discovery resolves one exact match, redirects into the canonical org route, callback completes with enterprise session metadata, and the first authenticated request sees the correct `active_organization_id`. Keep this test in the `test/example` Mix project lane and make the assertions about org truth explicit rather than inferred from redirect locations alone.</action>
  <verify>
    <automated>(cd test/example && mix test test/example_web/integration/enterprise_sso_routing_flow_test.exs)</automated>
  </verify>
  <done>The example app has an end-to-end proof that org truth survives discovery, callback, and first authenticated scope hydration.</done>
</task>
</tasks>

<verification>
- `(cd test/example && mix test test/example_web/controllers/session_controller_test.exs test/example_web/controllers/enterprise_sso_controller_test.exs)`
- `(cd test/example && mix test test/example_web/integration/enterprise_sso_routing_flow_test.exs)`
- `rg -n "/organizations/:org/sso|enterprise_context|Continue to .* enterprise sign-in|_action=enterprise" test/example/lib/example_web test/example/test/example_web`
</verification>

<success_criteria>
- The committed example app exposes the same org-scoped enterprise routing contract as the templates from Plan 02.
- Example controller and integration tests run in the `test/example` Mix project lane and prove both bounded discovery and first-session org truth.
- Discovery, callback, and retry UX stay in enterprise mode and preserve explicit org context once it has been resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/123-org-aware-enterprise-routing/123-03-SUMMARY.md`
</output>
