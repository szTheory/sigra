---
phase: 123-org-aware-enterprise-routing
plan: 04
type: execute
wave: 4
depends_on:
  - 123-02
  - 123-03
files_modified:
  - test/sigra/install/features/organizations_test.exs
  - test/sigra/install/golden_diff_test.exs
autonomous: true
requirements:
  - SSO-03
tags: [enterprise-sso, installer, regression, golden, generated-host]
must_haves:
  truths:
    - "Installer feature tests prove the enterprise routing templates are registered and emitted with the new org-scoped surface."
    - "Golden diff coverage proves template output and committed example code stay synchronized after the Phase 123 routing changes."
  artifacts:
    - path: test/sigra/install/features/organizations_test.exs
      provides: "installer feature assertions for enterprise controller template, routes, and login-page enterprise branch"
    - path: test/sigra/install/golden_diff_test.exs
      provides: "golden proof that emitted files match the committed example app after Phase 123 changes"
  key_links:
    - from: test/sigra/install/features/organizations_test.exs
      to: priv/templates/sigra.install/organizations/router_injection.ex
      via: "installer assertions must cover the new enterprise routes and controller template"
      pattern: "EnterpriseSSOController|/organizations/:org/sso"
    - from: test/sigra/install/golden_diff_test.exs
      to: test/example/lib/example_web/controllers/enterprise_sso_controller.ex
      via: "golden output must stay aligned with the committed example implementation"
      pattern: "enterprise_sso_controller|session_controller|session_html"
---

<objective>
Lock the generated-host routing contract with installer and golden regression coverage after the template and example changes land.

Purpose: keep the generated-host surface honest over time by proving the installer emits the new org-scoped enterprise routing files and that the committed example app remains the canonical generated output.
Output: installer feature assertions plus golden diff updates covering the enterprise controller, routes, login copy, and example synchronization for `SSO-03`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/123-org-aware-enterprise-routing/123-CONTEXT.md
@.planning/phases/123-org-aware-enterprise-routing/123-PATTERNS.md
@.planning/phases/123-org-aware-enterprise-routing/123-02-PLAN.md
@.planning/phases/123-org-aware-enterprise-routing/123-03-PLAN.md
@test/sigra/install/features/organizations_test.exs
@test/sigra/install/golden_diff_test.exs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer feature assertions -> emitted template surface | Missing assertions could let generated-host security or UX regress silently. |
| golden diff -> committed example app | Drift between templates and the example app would make generated-host proof unreliable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-13 | T | installer feature coverage | mitigate | Assert the enterprise controller template, login enterprise branch, and org-scoped routes are present in emitted output. |
| T-123-14 | R | golden diff coverage | mitigate | Update golden expectations only after the example app and templates match, so future diffs catch drift instead of papering over it. |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Extend installer feature assertions for enterprise routing output</name>
  <files>test/sigra/install/features/organizations_test.exs</files>
  <action>Follow the existing installer feature assertion style in `test/sigra/install/features/organizations_test.exs` and the template wiring called out in `123-PATTERNS.md`. Extend the feature test so it asserts the new controller template is registered, the emitted router includes `/organizations/:org/sso` and `/organizations/:org/sso/callback`, and the login surface includes the enterprise discovery branch and callback wiring introduced in Plan 02. Keep the assertions on emitted output concrete and bounded to the Phase 123 contract from D-01, D-02, and D-11.</action>
  <verify>
    <automated>mix test test/sigra/install/features/organizations_test.exs</automated>
  </verify>
  <done>Installer feature tests fail if the enterprise controller template, canonical routes, or bounded enterprise login branch drift from the Phase 123 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh golden diff proof for template and example synchronization</name>
  <files>test/sigra/install/golden_diff_test.exs</files>
  <action>Follow the golden-output synchronization pattern already used in `test/sigra/install/golden_diff_test.exs`. Update the expectations only after Plans 02 and 03 have aligned the templates and the committed example app. The golden diff must cover the enterprise controller, session controller, session HTML, organization delegates, and router changes so future generator drift is caught immediately. Do not add a separate one-off golden harness; keep the existing root-lane golden test authoritative.</action>
  <verify>
    <automated>mix test test/sigra/install/golden_diff_test.exs</automated>
  </verify>
  <done>Golden diff coverage proves the generated-host enterprise routing output and the committed example app stay in sync.</done>
</task>
</tasks>

<verification>
- `mix test test/sigra/install/features/organizations_test.exs`
- `mix test test/sigra/install/golden_diff_test.exs`
</verification>

<success_criteria>
- Installer feature coverage proves the new org-scoped enterprise routing files and login affordances are emitted.
- Golden diff coverage proves the generated-host templates and committed example app remain synchronized after the Phase 123 changes.
</success_criteria>

<output>
After completion, create `.planning/phases/123-org-aware-enterprise-routing/123-04-SUMMARY.md`
</output>
