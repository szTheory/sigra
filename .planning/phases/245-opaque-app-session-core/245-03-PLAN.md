---
phase: 245-opaque-app-session-core
plan: 03
type: tdd
wave: 2
depends_on: [245-01]
files_modified:
  - lib/sigra/app_session.ex
  - lib/sigra/app_session/refresh_token.ex
  - test/sigra/app_session_test.exs
autonomous: true
requirements: [APP-04]
must_haves:
  truths:
    - "D-07/D-08: every valid refresh locks and consumes the presented refresh row, supersedes the prior active access row, and appends a new access/refresh pair in one transaction."
    - "D-03: refresh idle advances by at most 30 days while the original 90-day family deadline remains fixed."
    - "D-09: a consumed refresh classifies as reuse and commits family revocation before returning a bounded reuse error."
    - "Raw replacement credentials are exposed only from a successful transaction result."
  artifacts:
    - path: "lib/sigra/app_session/refresh_token.ex"
      provides: "Locked refresh classification and persistence Multi builders"
    - path: "lib/sigra/app_session.ex"
      provides: "Public refresh orchestration and post-commit response"
      exports: ["refresh/2"]
    - path: "test/sigra/app_session_test.exs"
      provides: "Rotation, expiry, and reuse lifecycle proof"
  key_links:
    - from: "lib/sigra/app_session.ex"
      to: "lib/sigra/app_session/refresh_token.ex"
      via: "one Multi with locked classification before branch mutation"
      pattern: "build_locked_classify_multi"
    - from: "lib/sigra/app_session/refresh_token.ex"
      to: "PostgreSQL token rows"
      via: "digest-bound SELECT FOR UPDATE"
      pattern: "FOR UPDATE"
---

<objective>
Add the complete single-caller opaque refresh state machine on the proven host-schema seam.

Purpose: Guarantee rotation, idle/absolute expiry, and consumed-token reuse handling before concurrency and fault-injection expansion.
Output: Locked refresh Multi builders, the public refresh API, and PostgreSQL lifecycle tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-06-SUMMARY.md
@.planning/phases/245-opaque-app-session-core/245-CONTEXT.md
@.planning/phases/245-opaque-app-session-core/245-01-SUMMARY.md
@lib/sigra/jwt.ex
@lib/sigra/jwt/refresh_token.ex
@lib/sigra/app_session.ex
@test/sigra/jwt/refresh_token_test.exs

<interfaces>
Mirror the Phase 244 shape: a storage module appends `build_locked_classify_multi/4`, then the facade merges rotate/reuse persistence and interprets `Repo.transaction/1`. Unlike JWT JSON metadata, queries bind indexed `family_id`, `kind`, digest, and terminal columns from the host token schema.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rotate or revoke one family from a locked refresh classification</name>
  <precondition>`tmp/db.env` exists and supplies reachable PostgreSQL for row-lock and lifecycle verification.</precondition>
  <reversibility rating="costly">Phase 246 ceremonies will depend on the refresh result/error contract and terminal-state semantics, so later changes coordinate client-facing endpoints and persisted rows.</reversibility>
  <files>lib/sigra/app_session.ex, lib/sigra/app_session/refresh_token.ex, test/sigra/app_session_test.exs</files>
  <behavior>
    - Current refresh input yields new distinct access/refresh raw values; the input refresh and prior access become terminal, and exactly two new typed rows belong to the same family.
    - The replacement refresh expiry is `min(now + refresh_idle_ttl, family.absolute_expires_at)`; the family deadline is byte-for-byte unchanged.
    - Expired idle or absolute state returns bounded expiry without replacement rows or raw response.
    - Reusing a consumed refresh revokes its indexed family and returns `{:error, :reuse_detected}` only after commit; its current access fails `authenticate/2` immediately.
    - Missing, malformed, wrong-kind, or revoked inputs do not mutate another family.
  </behavior>
  <action>Start with failing public-behavior tests. Implement `Sigra.AppSession.RefreshToken` query/Multi helpers that hash and lock the exact refresh row using `FOR UPDATE` before classifying `consumed_at`, token expiry, family revocation, and family absolute expiry. For rotate, mark only terminal timestamps on prior credential rows and insert a new access/refresh pair in the existing family. For reuse, update the family by its indexed primary key and revoke all active family credentials in the same Multi. Orchestrate from `Sigra.AppSession.refresh/2`; create any raw replacement pair inside the transaction data flow but return it only from `{:ok, changes}`. Per D-03/D-07..D-09, do not use JSON/LIKE family matching, unlocked preflight reads, in-place digest replacement, separate audit/non-audit mutations, or extension of the absolute deadline.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_session_test.exs --trace</automated>
  </verify>
  <done>PostgreSQL tests prove rotate, idle/absolute expiry, reuse-family revocation, immediate access denial, and zero cross-family mutation through the public API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| raw refresh → locked row | Replayable attacker-held input selects a state transition. |
| transaction → response | Secrets must not escape a rolled-back mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-245-08 | Elevation of Privilege | refresh classification | critical | mitigate | Lock the digest-addressed refresh row before checking consumption or family state. |
| T-245-09 | Tampering | family revocation | high | mitigate | Use indexed family ID updates in the same transaction, never JSON/LIKE matching. |
| T-245-10 | Information Disclosure | refresh response | high | mitigate | Return replacement raw values only from a committed transaction result. |
| T-245-11 | Denial of Service | expiry handling | low | accept | Invalid/expired inputs perform bounded indexed lookups and return normalized errors. |
</threat_model>

<verification>
Run the focused refresh lifecycle tag, then the full `test/sigra/app_session_test.exs` file to retain the tracer contract.
</verification>

<success_criteria>
- APP-04 rotation/reuse semantics are correct for one caller on PostgreSQL.
- Idle and absolute clocks remain distinct and bounded.
- Reuse commits family revocation before the error becomes observable.
</success_criteria>

<output>
Create `.planning/phases/245-opaque-app-session-core/245-03-SUMMARY.md` when done.
</output>
