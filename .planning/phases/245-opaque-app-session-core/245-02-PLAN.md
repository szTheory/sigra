---
phase: 245-opaque-app-session-core
plan: 02
type: tdd
wave: 2
depends_on: [245-01]
files_modified:
  - lib/sigra/plug/fetch_app_session.ex
  - lib/sigra/plug/credential_auth.ex
  - test/sigra/plug/fetch_app_session_test.exs
autonomous: true
requirements: [APP-04, APP-05]
must_haves:
  truths:
    - "D-05: the explicit FetchAppSession pipeline authenticates only an opaque app access credential and rechecks durable token/family state plus the live user on every request."
    - "D-06: a valid credential builds the host's normal Scope while only bounded app-session facts enter conn.private[:sigra_auth]."
    - "Malformed, expired, revoked, refresh-kind, and deleted-user credentials fail closed without secret residue; an existing Scope remains untouched."
  artifacts:
    - path: "lib/sigra/plug/fetch_app_session.ex"
      provides: "Explicit app-session Bearer pipeline"
      exports: ["init/1", "call/2"]
    - path: "lib/sigra/plug/credential_auth.ex"
      provides: "Allowlisted private credential metadata projection"
    - path: "test/sigra/plug/fetch_app_session_test.exs"
      provides: "Plug boundary and redaction proof"
  key_links:
    - from: "lib/sigra/plug/fetch_app_session.ex"
      to: "lib/sigra/app_session.ex"
      via: "Sigra.AppSession.authenticate/2"
      pattern: "AppSession\.authenticate"
    - from: "lib/sigra/plug/fetch_app_session.ex"
      to: "lib/sigra/plug/credential_auth.ex"
      via: "normal Scope plus bounded private facts"
      pattern: "put_verified_scope"
---

<objective>
Activate the public Phase 243 app-session Plug without introducing credential guessing or a second Scope model.

Purpose: Make next-request expiry and revocation authoritative at the explicit request boundary.
Output: A tested `FetchAppSession` verifier path and an app-session-specific private fact allowlist.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-02-SUMMARY.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-VERIFICATION.md
@.planning/phases/245-opaque-app-session-core/245-CONTEXT.md
@.planning/phases/245-opaque-app-session-core/245-01-SUMMARY.md
@lib/sigra/plug/fetch_api_token.ex
@lib/sigra/plug/fetch_app_session.ex
@lib/sigra/plug/credential_auth.ex
@test/sigra/plug/fetch_app_session_test.exs

<interfaces>
`CredentialAuth.put_verified_scope/5` currently requires `id`, `scopes`, `auth_method`, and `assurance`; preserve PAT/JWT callers. The app-session branch must project `credential_kind: :app_session`, credential row ID, family/session ID, `scopes: []`, server-derived auth method and assurance. It must never project a raw value, digest, authorization header, client reference, or request-selected scope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authenticate explicit app-session requests into normal Scope</name>
  <precondition>`tmp/db.env` exists and supplies the PostgreSQL connection used by the representative app-session schemas.</precondition>
  <reversibility rating="costly">The private fact shape is shared by authorization and downstream observability, so changing it later requires coordinated Plug consumers, though it remains private rather than a wire format.</reversibility>
  <files>lib/sigra/plug/fetch_app_session.ex, lib/sigra/plug/credential_auth.ex, test/sigra/plug/fetch_app_session_test.exs</files>
  <behavior>
    - Valid explicit Bearer access credential plus an existing user produces `Scope.build/3` output and bounded `:sigra_auth` facts.
    - Missing/malformed Bearer input, refresh-kind credential, expired or revoked durable state, and a deleted user all assign `current_scope: nil` and no private credential facts.
    - An already assigned Scope bypasses parsing and database access unchanged.
    - Inspection of assigns/private/log capture never contains the raw credential, its digest, the authorization header, or client reference.
  </behavior>
  <action>Replace the Phase 243 inert body rather than adding another Plug. Parse exactly one `Authorization: Bearer` transport in `FetchAppSession`, call `Sigra.AppSession.authenticate/2`, reload `config.user_schema` through `config.repo`, and delegate Scope creation to `CredentialAuth`. Extend the helper with an explicit app-session fact projection while preserving the existing PAT/JWT allowlist and `RequireScopes` fail-closed empty-scope behavior. Per D-05/D-06, do not fall back to `FetchBearer`, PAT, JWT, cookies, or token-shape detection, and never place credential authority into Scope.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/plug/fetch_app_session_test.exs test/sigra/plug/fetch_api_token_test.exs test/sigra/plug/fetch_jwt_test.exs test/sigra/plug/require_scopes_test.exs --trace</automated>
  </verify>
  <done>All valid/failure/redaction cases pass, existing PAT/JWT projections remain green, and app sessions remain unscoped for `RequireScopes`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP Authorization header → Plug | Attacker-controlled bearer material enters the explicit pipeline. |
| verified row → host Scope | Credential facts must not become host authorization authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-245-04 | Spoofing | `FetchAppSession.call/2` | high | mitigate | Accept only `AppSession.authenticate/2` success and reload the live user before Scope construction. |
| T-245-05 | Elevation of Privilege | `CredentialAuth` | high | mitigate | Project empty scopes and bounded server facts outside Scope; retain `RequireScopes` kind/empty checks. |
| T-245-06 | Information Disclosure | Plug failure paths | high | mitigate | Normalize to nil Scope and assert no raw/header/digest/client data in assigns, private, or logs. |
| T-245-07 | Tampering | mixed credential pipelines | medium | mitigate | No autodetection or compatibility fallback in the explicit Plug. |
</threat_model>

<verification>
The focused command proves the new path and all adjacent explicit credential pipelines without sleeps or browser UAT.
</verification>

<success_criteria>
- Valid app access authenticates a live user into normal Scope.
- Every invalid/stale/revoked/missing-user path fails closed and redacts secrets.
- PAT, JWT, and `RequireScopes` behavior remains independent.
</success_criteria>

<output>
Create `.planning/phases/245-opaque-app-session-core/245-02-SUMMARY.md` when done.
</output>
