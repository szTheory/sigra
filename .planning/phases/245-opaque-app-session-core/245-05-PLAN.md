---
phase: 245-opaque-app-session-core
plan: 05
type: tdd
wave: 4
depends_on: [245-04]
files_modified:
  - lib/sigra/app_session.ex
  - lib/sigra/auth.ex
  - test/sigra/app_session_test.exs
autonomous: true
requirements: [APP-05]
must_haves:
  truths:
    - "D-11: owner-constrained single-family revocation changes exactly the selected active family; foreign, missing, and already-revoked IDs are indistinguishable no-ops."
    - "D-11: user-wide app-session revocation marks every active family for that user and no family for another user."
    - "D-05/D-12: explicit device and all-app revocation make access and refresh credentials fail on their next use."
    - "Explicit revoke state and optional audit commit atomically, with post-commit telemetry only."
  artifacts:
    - path: "lib/sigra/app_session.ex"
      provides: "Owner-bound family and user-wide revoke APIs"
      exports: ["revoke_family_for_user/3", "revoke_all_for_user/2", "append_revoke_all_multi/4"]
    - path: "lib/sigra/auth.ex"
      provides: "Stable Sigra.Auth facade delegates for host/generated callers"
    - path: "test/sigra/app_session_test.exs"
      provides: "Cross-account, idempotency, audit, and next-auth proof"
  key_links:
    - from: "lib/sigra/auth.ex"
      to: "lib/sigra/app_session.ex"
      via: "thin revoke delegates"
      pattern: "AppSession\.revoke"
    - from: "lib/sigra/app_session.ex"
      to: "family/token rows"
      via: "owner-bound active-family update and audit Multi"
      pattern: "revoke_family_for_user|revoke_all_for_user"
---

<objective>
Expose owner-safe one-device and all-app-session revocation through the library facade.

Purpose: Give Phase 246 ceremonies and host policy one stable security-event API without letting client selectors become authority.
Output: Transactional revoke APIs, Auth delegates, and PostgreSQL owner/isolation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/245-opaque-app-session-core/245-CONTEXT.md
@.planning/phases/245-opaque-app-session-core/245-04-SUMMARY.md
@lib/sigra/api_token.ex
@lib/sigra/auth.ex
@lib/sigra/app_session.ex
@test/sigra/api_token_test.exs

<interfaces>
Follow `Sigra.APIToken.revoke_for_user/3` and `revoke_all/2`: owner identity is a trusted user/Scope fact, queries bind both family ID and user ID, audit uses the same Multi, and foreign/missing/terminal outcomes reveal no row existence. `Sigra.Auth` exposes thin delegates; it does not accept a client-selected owner.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revoke one owned family or every family with durable next-auth denial</name>
  <precondition>`tmp/db.env` exists and supplies reachable PostgreSQL for owner/isolation and audit verification.</precondition>
  <reversibility rating="costly">These public facade signatures are the revoke contract Phase 246 will call, so changing them later coordinates generated delegates and host call sites.</reversibility>
  <files>lib/sigra/app_session.ex, lib/sigra/auth.ex, test/sigra/app_session_test.exs</files>
  <behavior>
    - `revoke_family_for_user/3` on an owned active family returns success, commits one `session.app_revoke` audit when configured, and denies all family access/refresh on next use.
    - The same call for foreign, absent, or already terminal family returns the same bounded not-found result, adds no audit, and changes no rows.
    - `revoke_all_for_user/2` returns the active-family count, commits one `session.app_revoke_all` audit, and leaves another user's families usable.
    - Audit constraint failure rolls back revocation; telemetry is emitted only after commit.
  </behavior>
  <action>Implement active-family queries constrained by both trusted owner ID and family ID, plus a user-wide indexed update. Provide `append_revoke_all_multi/4` as the schema-agnostic, transaction-composable primitive used by both the standalone revoke call and later security events. Revoke the family and its active token rows in one `Ecto.Multi` and append optional audit before commit. Add `Sigra.Auth.revoke_app_session/3` and `revoke_all_app_sessions/2` as thin delegates using the validated config. Treat the server-selected family ID as a selector only after binding it to the authenticated owner. Per D-05/D-11/D-12, prove immediate authenticate/refresh failure and cross-account isolation; do not add a controller, route, generated delegate, UI, or device registration endpoint.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_session_test.exs --trace</automated>
  </verify>
  <done>One-family and user-wide revocation are owner-safe, idempotent/fail-closed, audit-atomic, and observable on the next access/refresh attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated user → family selector | A caller selects a family ID but must not select its owner. |
| revoke API → audit | Revocation and its evidence must share a commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-245-16 | Elevation of Privilege | one-family revoke | high | mitigate | Query active family by both family ID and trusted owner ID. |
| T-245-17 | Information Disclosure | revoke result | medium | mitigate | Normalize foreign, missing, and terminal outcomes. |
| T-245-18 | Tampering | revoke-all | high | mitigate | Bind update to exact user ID and assert another user's rows and credentials remain active. |
| T-245-19 | Repudiation | revoke audit | medium | mitigate | Audit is a same-Multi step and constraint rejection rolls back revocation. |
</threat_model>

<verification>
Run the revocation tag and the complete app-session lifecycle/concurrency suite to prove revoked families cannot regain validity.
</verification>

<success_criteria>
- APP-05 one/all revocation behavior is public through `Sigra.Auth` and durable.
- Cross-account selectors disclose and mutate nothing.
- No Phase 246 delivery surface is added.
</success_criteria>

<output>
Create `.planning/phases/245-opaque-app-session-core/245-05-SUMMARY.md` when done.
</output>
