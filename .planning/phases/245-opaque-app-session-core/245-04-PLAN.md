---
phase: 245-opaque-app-session-core
plan: 04
type: tdd
wave: 3
depends_on: [245-03]
files_modified:
  - lib/sigra/app_session.ex
  - lib/sigra/app_session/refresh_token.ex
  - test/sigra/app_session_audit_cofate_test.exs
  - test/sigra/app_session/concurrency_test.exs
autonomous: true
requirements: [APP-04]
must_haves:
  truths:
    - "D-07: rotation, reuse-family revocation, and optional audit use the same Ecto.Multi transaction in audit-on and audit-off configurations."
    - "A rejected persistence or audit step rolls back every lifecycle mutation and returns no replacement credential."
    - "D-10: two barrier-released callers yield exactly one rotation winner and one serialized reuse result, leaving one deterministically revoked family without sleeps."
  artifacts:
    - path: "test/sigra/app_session_audit_cofate_test.exs"
      provides: "Audit-on/off and fault-injection rollback proof"
    - path: "test/sigra/app_session/concurrency_test.exs"
      provides: "Two-process PostgreSQL lock proof"
    - path: "lib/sigra/app_session.ex"
      provides: "Optional audit step and post-commit telemetry/response"
  key_links:
    - from: "lib/sigra/app_session.ex"
      to: "lib/sigra/audit.ex"
      via: "Sigra.Audit.log_multi_safe/3 appended before Repo.transaction"
      pattern: "log_multi_safe"
    - from: "test/sigra/app_session/concurrency_test.exs"
      to: "lib/sigra/app_session/refresh_token.ex"
      via: "real competing transactions serialized by FOR UPDATE"
      pattern: "Task\.async|Sandbox\.allow"
---

<objective>
Harden refresh with audit co-fate, rollback proof, and deterministic competing-caller evidence.

Purpose: Demonstrate that the locked lifecycle remains one transaction under both audit modes and cannot leak a credential from a failed commit.
Output: Audit/fault-injection coverage and a no-sleep PostgreSQL concurrency test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-06-PLAN.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-06-SUMMARY.md
@.planning/phases/245-opaque-app-session-core/245-03-SUMMARY.md
@lib/sigra/audit.ex
@lib/sigra/app_session.ex
@test/sigra/jwt_refresh_audit_cofate_test.exs

<interfaces>
Use `Sigra.Audit.log_multi_safe/3` inside the lifecycle Multi and `Audit.emit_telemetry_from_changes/2` only after commit. Reserve `session.app_issue`, `session.app_refresh`, `session.app_refresh_reuse`, `session.app_revoke`, and `session.app_revoke_all` under the existing `session.` namespace; never create an audit-only transaction branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove refresh persistence and audit share one rollback boundary</name>
  <precondition>`tmp/db.env` exists and supplies reachable PostgreSQL for constraint fault injection.</precondition>
  <reversibility rating="reversible">Audit append and telemetry placement are internal orchestration behind the stable refresh result contract.</reversibility>
  <files>lib/sigra/app_session.ex, lib/sigra/app_session/refresh_token.ex, test/sigra/app_session_audit_cofate_test.exs</files>
  <behavior>
    - Audit-on rotate commits lifecycle rows and exactly one `session.app_refresh` record; audit-off commits identical lifecycle state and no audit row.
    - Audit-on reuse commits family revocation and exactly one `session.app_refresh_reuse` record before returning reuse.
    - A CHECK-constraint rejection of the audit insert or replacement token insert rolls back consumption, inserts, and family mutation; the public result is `{:error, :app_session_refresh_aborted}` with no raw replacement values.
    - Audit metadata contains bounded actor/family/action facts and no raw credential, digest, client reference, or authorization material.
  </behavior>
  <action>Adapt the Phase 244 co-fate fixture to the explicit family/token tables. Append the optional audit step to the same rotate/reuse Multi with reserved `session.app_*` action names, and emit telemetry only after a successful commit. Exercise audit-on, audit-off, persistence rejection, audit rejection, and reuse rejection via temporary PostgreSQL constraints removed in `after` blocks. Preserve the same public abort outcome across storage and audit faults, and do not rescue database failures into a success-shaped result.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_session_audit_cofate_test.exs --trace</automated>
  </verify>
  <done>Audit-on/off state is identical apart from audit rows; all injected faults leave the original credential usable/state unchanged and expose no replacement secret.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize double refresh with barriers and row locks</name>
  <precondition>`tmp/db.env` exists and its PostgreSQL service permits two concurrent Sandbox clients.</precondition>
  <reversibility rating="reversible">The barrier fixture verifies the locked contract without becoming production API.</reversibility>
  <files>lib/sigra/app_session.ex, lib/sigra/app_session/refresh_token.ex, test/sigra/app_session/concurrency_test.exs</files>
  <behavior>
    - Two tasks announce readiness, receive the same explicit `:go`, and call refresh with the identical raw value.
    - In audit-on and audit-off modes, results contain exactly one `{:ok, replacement}` and one `{:error, :reuse_detected}`.
    - The final family is revoked, both the winning replacement access and all predecessor access credentials fail authentication, and row/audit counts are exact.
    - The test contains no `Process.sleep/1`, timer race, retry loop, or mocked lock.
  </behavior>
  <action>Copy the Phase 244 Sandbox allow + ready/go barrier pattern and run real concurrent `Repo.transaction` calls. Assert the multiset of results rather than task order. Verify exact final family/token/audit state and next-auth denial in audit-on and audit-off modes. If the test exposes an unlocked classification window, repair the production lock/query ordering rather than weakening the barrier or adding timing delays, per D-07/D-09/D-10.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_session/concurrency_test.exs --trace</automated>
  </verify>
  <done>The two-process proof deterministically produces one rotate, one reuse, and one revoked family in both audit modes with no sleeps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| lifecycle transaction → audit ledger | Security evidence must commit or roll back with state. |
| concurrent refresh callers → PostgreSQL | Two holders of one refresh secret contend for authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-245-12 | Repudiation | refresh audit | high | mitigate | Append audit to the state-changing Multi and prove constraint failure rolls back state. |
| T-245-13 | Elevation of Privilege | concurrent refresh | critical | mitigate | Real `FOR UPDATE` serialization plus exact final-family assertions in both audit modes. |
| T-245-14 | Information Disclosure | rollback response | high | mitigate | Normalize aborts without returning raw in-memory replacements and assert inspected results/logs. |
| T-245-15 | Tampering | concurrency test | medium | mitigate | Use explicit barriers and row counts; prohibit sleeps, retries, and mocked locks. |
</threat_model>

<verification>
Run both task commands, then `source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_session_test.exs test/sigra/app_session_audit_cofate_test.exs test/sigra/app_session/concurrency_test.exs --trace`.
</verification>

<success_criteria>
- Rotation/reuse state and audit are co-fated in both audit modes.
- Fault injection proves zero partial state and zero returned replacement secrets.
- Deterministic concurrency proves exactly one winner and family-wide reuse revocation.
</success_criteria>

<output>
Create `.planning/phases/245-opaque-app-session-core/245-04-SUMMARY.md` when done.
</output>
