---
phase: 245-opaque-app-session-core
plan: 08
type: execute
wave: 6
depends_on: [245-07]
files_modified:
  - lib/sigra/config.ex
  - test/sigra/app_session_security_event_test.exs
  - test/sigra/auth_test.exs
autonomous: true
requirements: [APP-04, APP-05]
gap_closure: true
must_haves:
  truths:
    - "The three Phase 245 files named by 245-VERIFICATION.md satisfy the repository formatter gate."
    - "APP-04 configuration defaults and APP-05 password-reset/sign-out revocation behavior remain green after formatting."
    - "The repository `mix ci` result is reported from a real run; a nonzero result blocks completion with the failing stage and files recorded."
  artifacts:
    - path: "lib/sigra/config.ex"
      provides: "Formatted app-session configuration validator"
    - path: "test/sigra/app_session_security_event_test.exs"
      provides: "Formatted PostgreSQL security-event revocation proof"
    - path: "test/sigra/auth_test.exs"
      provides: "Formatted reset-revocation composition test"
  key_links:
    - from: "lib/sigra/config.ex"
      to: "test/sigra/app_session_test.exs"
      via: "app-session TTL and paired-schema assertions"
      pattern: "access_ttl|refresh_idle_ttl|absolute_ttl"
    - from: "test/sigra/app_session_security_event_test.exs"
      to: "lib/sigra/auth.ex"
      via: "password-reset and sign-out-all lifecycle calls"
      pattern: "Auth\.(reset_password|delete_all_sessions)"
---

<objective>
Close the Phase 245 formatter-only verification gap without changing app-session behavior.

Purpose: Make the completed APP-04/APP-05 implementation satisfy the repository CI quality gate.
Output: Formatter-normalized versions of exactly the three verifier-identified files plus focused test and full CI evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/245-opaque-app-session-core/245-VERIFICATION.md
@.planning/phases/245-opaque-app-session-core/245-07-SUMMARY.md
@lib/sigra/config.ex
@test/sigra/app_session_test.exs
@test/sigra/app_session_security_event_test.exs
@test/sigra/auth_test.exs

<interfaces>
The verifier has already established all four Phase 245 behavioral truths and identified one integration failure: `mix format --check-formatted` rejects only the three files in this plan. This task must preserve behavior and must not modify implementation outside those files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the verifier-identified files and prove the repository gate</name>
  <precondition>`tmp/db.env` exists and supplies reachable PostgreSQL for the focused security-event tests and repository CI command.</precondition>
  <files>lib/sigra/config.ex, test/sigra/app_session_security_event_test.exs, test/sigra/auth_test.exs</files>
  <action>Run `mix format` with exactly the three listed paths. Inspect the resulting diff and retain formatter-only changes: no assertion, value, function call, SQL statement, or production behavior may change. Run the exact-path formatter check, then the focused app-session configuration, PostgreSQL security-event, and reset-composition tests. Finally run `source tmp/db.env &amp;&amp; MIX_ENV=test mix ci`. Record the real command, exit status, and relevant output in the SUMMARY. If `mix ci` is nonzero, diagnose the failing stage once; repair only a deterministic formatter issue within the three owned files, otherwise stop without claiming the gap closed and preserve durable diagnostics. Do not edit `.planning/config.json`, `.planning/research/.cache/`, or `245-VERIFICATION.md`.</action>
  <verify>
    <automated>mix format --check-formatted lib/sigra/config.ex test/sigra/app_session_security_event_test.exs test/sigra/auth_test.exs &amp;&amp; source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_session_test.exs test/sigra/app_session_security_event_test.exs test/sigra/auth_test.exs &amp;&amp; MIX_ENV=test mix ci</automated>
  </verify>
  <done>The three scoped files are formatted, focused APP-04/APP-05 behavior passes, and `MIX_ENV=test mix ci` exits zero; any nonzero CI result is reported as a blocker rather than completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source formatting → security-sensitive behavior | Mechanical rewrites touch configuration and revocation proofs but must not alter credential lifecycle semantics. |
| local verification → completion claim | The reported repository CI status must match the actual command exit status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-245-28 | Tampering | three scoped files | medium | mitigate | Restrict edits to formatter output, inspect the diff, and rerun focused APP-04/APP-05 tests. |
| T-245-29 | Repudiation | CI evidence | medium | mitigate | Record the exact `mix ci` command and exit result; block completion on nonzero status. |
| T-245-30 | Information Disclosure | test/CI logs | low | accept | Commands use the existing local test environment; SUMMARY records only relevant diagnostics and no credential values. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Opaque rotating app credentials remain bounded and revocable | Plans 245-01 through 245-07 delivered and verification proved the behavior; this plan closes the formatter/CI integration gap. | COVERED |
| REQ | APP-04 | Formatter normalization plus the focused app-session configuration/lifecycle suite preserves the verified implementation. | COVERED |
| REQ | APP-05 | Formatter normalization plus security-event and reset-composition tests preserves the verified revocation implementation. | COVERED |
| RESEARCH | Dedicated rows, locked rotation, authoritative authentication, lifecycle fan-out, and deterministic PostgreSQL proof | Delivered by Plans 245-01 through 245-07; this plan performs no semantic change and reruns the directly affected proofs. | COVERED |
| CONTEXT | All locked credential, authentication, rotation, revocation, and phase-boundary decisions | Delivered by Plans 245-01 through 245-07; formatter-only scope and focused tests prevent decision drift. | COVERED |
</source_audit>

<verification>
The exact-path formatter check, focused affected tests, and full repository `mix ci` command must all exit zero in sequence. `git diff --check` must also pass before commit.
</verification>

<success_criteria>
- The only verifier-reported gap is closed in exactly the three named files.
- APP-04 and APP-05 focused behavior remains green.
- Repository CI is proven by an actual zero exit, with no waived or hidden failure.
</success_criteria>

<output>
Create `.planning/phases/245-opaque-app-session-core/245-08-SUMMARY.md` when done.
</output>
