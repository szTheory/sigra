---
phase: 245-opaque-app-session-core
plan: 07
type: tdd
wave: 5
depends_on: [245-05]
files_modified:
  - lib/sigra/account/deletion.ex
  - test/sigra/app_session_account_deletion_test.exs
  - test/sigra/account/deletion_test.exs
autonomous: true
requirements: [APP-05]
must_haves:
  truths:
    - "D-12/D-13: scheduling account deletion revokes all app-session families in the same transaction as immediate account deactivation and token cleanup."
    - "A deletion-scheduling failure leaves user state, ordinary tokens, app-session families, hooks, and audit/effects uncommitted."
    - "Soft/anonymize/hard-delete execution cannot restore app credentials; hard deletion removes rows through configured cleanup or representative FK cascade."
  artifacts:
    - path: "lib/sigra/account/deletion.ex"
      provides: "Transactional app-session invalidation during deletion scheduling"
    - path: "test/sigra/app_session_account_deletion_test.exs"
      provides: "PostgreSQL schedule/rollback/finalization proof"
  key_links:
    - from: "lib/sigra/account/deletion.ex"
      to: "Sigra.Config.app_session"
      via: "the full config already threaded by Auth.schedule_deletion/3"
      pattern: "app_session"
    - from: "lib/sigra/account/deletion.ex"
      to: "lib/sigra/app_session.ex"
      via: "revoke-all Multi appended before schedule transaction"
      pattern: "append_revoke_all_multi"
---

<objective>
Make account deletion scheduling immediately and transactionally invalidate app sessions.

Purpose: Remove the existing post-commit revocation gap for the new credential kind while keeping deletion strategies and browser behavior intact.
Output: Account-deletion Multi composition and real PostgreSQL schedule/rollback/finalization evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/245-opaque-app-session-core/245-CONTEXT.md
@.planning/phases/245-opaque-app-session-core/245-05-SUMMARY.md
@lib/sigra/auth.ex
@lib/sigra/account/deletion.ex
@test/sigra/account/deletion_test.exs
@test/sigra/workers/account_deletion_test.exs

<interfaces>
`Auth.schedule_deletion/3` already threads the full config into `Sigra.Account.Deletion.schedule/3`. `Deletion.do_schedule/3` owns the deactivation `Ecto.Multi`; append the same schema-agnostic app-session revoke builder used by password reset before `repo.transaction/1`. Existing browser-session removal currently occurs post-commit and is outside this phase's app-session co-fate change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revoke app sessions inside account-deletion scheduling</name>
  <precondition>`tmp/db.env` exists and supplies reachable PostgreSQL for account-deletion transaction verification.</precondition>
  <reversibility rating="costly">Deletion transaction step names and schema threading are shared lifecycle contracts, though no generated schema exists until Phase 246.</reversibility>
  <files>lib/sigra/account/deletion.ex, test/sigra/app_session_account_deletion_test.exs, test/sigra/account/deletion_test.exs</files>
  <behavior>
    - Scheduling deletion for a user with active app families commits deleted/scheduled timestamps, ordinary token cleanup, hooks, and app-family revocation together; access and refresh fail next use.
    - A constraint/hook failure after the app revoke step rolls back user timestamps, token cleanup, and all app-family/token terminal fields.
    - Scheduling another user's deletion does not affect the first user's app sessions.
    - Subsequent soft-delete, anonymize, and hard-delete execution never makes a scheduled user's app credential valid; representative hard-delete FK behavior leaves no orphan family/token rows.
    - Hosts without app-session schemas retain existing deletion scheduling and worker tests.
  </behavior>
  <action>Read family/token schemas from the full `config` that `Auth.schedule_deletion/3` already threads into deletion options. In `do_schedule/3`, append Plan 245-05's app-session revoke-all Multi step alongside user update, ordinary token cleanup, and hooks, before the existing transaction. Add PostgreSQL success, cross-user, and fault-injection tests plus representative cascade/cleanup assertions; retain existing post-commit browser-session behavior and worker strategy contracts. Per D-12/D-13, do not place app revocation in `revoke_sessions/2`, a background job, or any post-commit callback, and do not emit Phase 246 schema/migration templates.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_session_account_deletion_test.exs test/sigra/account/deletion_test.exs test/sigra/workers/account_deletion_test.exs --trace</automated>
  </verify>
  <done>Deletion scheduling atomically deactivates the account and revokes app sessions; rollback, cross-user isolation, all strategies, and no-app-session compatibility are proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| deletion request → app credentials | Account deactivation must synchronously remove app authentication authority. |
| scheduling transaction → deletion worker | Later strategies must not revive or orphan active credential state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-245-24 | Elevation of Privilege | deletion schedule | critical | mitigate | Revoke app families in the same scheduling Multi and prove immediate next-auth denial. |
| T-245-25 | Tampering | rollback path | high | mitigate | Inject a later failure and assert all user/token/app state rolls back. |
| T-245-26 | Information Disclosure | cross-user deletion | high | mitigate | User-bound update queries plus adjacent-user credential assertions. |
| T-245-27 | Tampering | hard-delete cleanup | medium | mitigate | Representative FK cascade or explicit configured cleanup leaves no orphan app rows. |
</threat_model>

<verification>
Run the focused command, then the full app-session security-event suite after Plan 245-06 joins Wave 6.
</verification>

<success_criteria>
- Account deletion satisfies APP-05 at scheduling time, not eventual cleanup time.
- Fault injection proves app revocation co-fates with deactivation.
- Existing deletion strategies and hosts without app sessions remain green.
</success_criteria>

<output>
Create `.planning/phases/245-opaque-app-session-core/245-07-SUMMARY.md` when done.
</output>
