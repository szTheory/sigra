---
phase: 68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/deployment.md
autonomous: true
requirements:
  - ACF-01
  - ACF-04
must_haves:
  truths:
    - "**`guides/recipes/deployment.md`** contains **`## Production checklist (read first)`** immediately after the opening intro paragraph (before **`## Required environment variables`**), with a **≤12-row** markdown table (**Check** | **Why** | **Knob / where**) plus a **short triage** subsection (symptom → misconfiguration class) per **068-CONTEXT D-04–D-07**."
    - "Mail path guidance adds a **TL;DR decision tree** (bullets) naming **inline** default vs **Oban recommended** for production, **at-least-once** / retry double-send awareness, and a pointer to **`https://hexdocs.pm/oban`** — per **D-08–D-09, D-11**."
    - "No claim that following the checklist **certifies** security or compliance."
  artifacts:
    - path: "guides/recipes/deployment.md"
      provides: "ACF-01 checklist hub + ACF-04 Oban/inline semantics"
nyquist_compliant: false
---

<objective>
Extend **`guides/recipes/deployment.md`** with the **production checklist** block and **mail delivery / Oban** TL;DR per **068-CONTEXT.md** decisions **D-02–D-11** (hub-only truth; link out for Phoenix/Plug/OWASP mechanics).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-68-01 | Misrepresentation | Checklist read as **compliance certification** or universal secure posture | high | Framing: verification steps for **your** host; forbid words like *certified*, *guarantee*, *SOC*; link OWASP + Phoenix docs |
| T-68-02 | Misconfiguration | Wrong **cookie / `url:` / `check_origin`** guidance causes locked-out prod | medium | Table ties each check to config surface; triage box maps symptoms to classes; link `Phoenix.Endpoint` / `Plug.Session` |
| T-68-03 | Information disclosure / abuse | **Email** retries cause **duplicate** user-visible sends without warning | medium | One explicit sentence on at-least-once + idempotency; point to token patterns in existing docs if present |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Insert production checklist H2 and body</name>
  <files>guides/recipes/deployment.md</files>
  <read_first>
    - guides/recipes/deployment.md
    - .planning/phases/068-deploy-and-mail-confidence/068-CONTEXT.md
    - https://hexdocs.pm/phoenix/Phoenix.Endpoint.html (external — skim URL targets only)
  </read_first>
  <action>
    In **`guides/recipes/deployment.md`**, locate the first paragraph under the title `# Deployment` (ends before **`## Required environment variables`**).

    Immediately after that intro paragraph (preserve a single blank line before the next heading), insert:

    1. A level-2 heading with **exact** text: **`## Production checklist (read first)`**

    2. One short paragraph (2–3 sentences) stating this is a **pre-flight verification** list for **public HTTPS** hosts, not a compliance attestation.

    3. A **markdown table** with **exactly these three column headers** on the header row: **`Check`** | **`Why`** | **`Knob / where`**. Include **8–12** data rows covering at minimum these topic classes (merge into rows as needed): public origin URL, TLS termination / forwarded proto, redirect loops, **`Endpoint` `url:`** scheme/host/port, **`Plug.Session`** `secure` / `SameSite` / domain alignment with **`COOKIE_DOMAIN`**, LiveView / WebSocket **`check_origin`** or Origin alignment, staging parity, Sigra **`UserAuth` / `cookie_domain`** boot warning cross-reference (**link** to subdomain-auth recipe — do not paste full subdomain guide).

    4. A subsection with heading **`### Symptom triage`** containing **exactly 4** bullet lines; each bullet maps a **symptom** (plain language) to a **likely misconfiguration class** among: wrong **scheme/http vs https**, wrong **`url:` / host / port**, wrong **`check_origin` / WS origin**, wrong **cookie flags / domain**.

    5. One short paragraph with links (markdown) to **`https://hexdocs.pm/phoenix/deployment.html`**, **`https://hexdocs.pm/phoenix/Phoenix.Endpoint.html`**, **`https://hexdocs.pm/plug/Plug.Session.html`**, and **`https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html`** for deeper reading.

    Do **not** move or delete the existing **`## Required environment variables`** section — it must remain **after** the new checklist block.
  </action>
  <acceptance_criteria>
    - `grep -nF '## Production checklist (read first)' guides/recipes/deployment.md` exits **0** and the line number is **less than** the line number from `grep -nF '## Required environment variables' guides/recipes/deployment.md | head -1`.
    - `grep -F '| Check |' guides/recipes/deployment.md` exits **0** AND `grep -F '| Why |' guides/recipes/deployment.md` exits **0** AND `grep -F '| Knob / where |' guides/recipes/deployment.md` exits **0**.
    - `sed -n '/## Production checklist (read first)/,/## Required environment variables/p' guides/recipes/deployment.md | grep -cE '^\\|'` returns a count **≥ 9** (table rows within checklist section only; executor runs on macOS or Linux).
    - `grep -F '### Symptom triage' guides/recipes/deployment.md` exits **0**.
    - `grep -c '^- ' guides/recipes/deployment.md` is **≥ 4** in the file OR `grep -A20 '### Symptom triage' guides/recipes/deployment.md | grep -c '^- '` is **4** (four triage bullets under that heading — executor verifies with read).
    - `grep -iF 'Phoenix.Endpoint' guides/recipes/deployment.md` exits **0**.
    - `grep -iF 'Plug.Session' guides/recipes/deployment.md` exits **0**.
    - `grep -iF 'cheatsheetseries.owasp.org' guides/recipes/deployment.md` exits **0**.
    - `grep -Ei 'soc2|certif|compliance cert|guaranteed secure' guides/recipes/deployment.md` exits **1** (forbidden warranty phrasing in new prose — existing file must stay clean too).
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Oban vs inline TL;DR + install cross-link</name>
  <files>guides/recipes/deployment.md</files>
  <read_first>
    - guides/recipes/deployment.md
    - lib/mix/tasks/sigra.install.ex
    - .planning/phases/068-deploy-and-mail-confidence/068-CONTEXT.md
  </read_first>
  <action>
    In **`guides/recipes/deployment.md`**:

    1. **Immediately before** the existing heading **`## Oban for background jobs`** (keep that heading text unchanged), insert a new level-2 section **`## Mail delivery: inline vs Oban (TL;DR)`** containing:
       - **3–5** bullet lines forming a decision path: **dev/test** → acceptable inline/Swoosh adapter; **tiny single-node** → inline may be acceptable with tradeoffs stated; **remote SMTP / multi-node / burst / SLA** → **Oban** recommended.
       - One sentence that names **at-least-once delivery** and that **retries can double-send** emails unless messages are idempotent — point readers to existing Sigra token/email patterns (search repo for `deliver_later` / Oban mailer patterns in **`test/example/`** or guides; add **one** concrete relative link to **`test/example/`** tree or a named file if one exists, else state **CI example host** path **`test/example/`** as directory pointer without inventing filenames).
       - A markdown link to **`https://hexdocs.pm/oban`**.

    2. In the same new section (or immediately below it before **`## Oban for background jobs`**), add a bullet or table row listing **`mix sigra.install`** switches copied from **`mix help sigra.install`** output at execution time — at minimum the flags documented in **`lib/mix/tasks/sigra.install.ex`** `@moduledoc` **Options** list (`--live` / `--no-live`, `--binary-id` / `--no-binary-id`, `--api`, `--jwt`, `--admin` / `--no-admin`, `--passkeys` / `--no-passkeys`, `--yes`). If **`--no-organizations`** appears in help output, include it; if absent from help, omit rather than invent.

    3. Preserve the existing paragraph that contains the substring **`Strongly prefer Oban in production.`** (do not delete; may lightly edit for cross-reference to the new TL;DR section if needed).
  </action>
  <acceptance_criteria>
    - `grep -nF '## Mail delivery: inline vs Oban (TL;DR)' guides/recipes/deployment.md` exits **0** and line number is **less than** `grep -nF '## Oban for background jobs' guides/recipes/deployment.md | head -1`.
    - `grep -F 'https://hexdocs.pm/oban' guides/recipes/deployment.md` exits **0**.
    - `grep -iF 'at-least' guides/recipes/deployment.md` exits **0** OR `grep -iF 'double-send' guides/recipes/deployment.md` exits **0** OR `grep -iF 'idempotent' guides/recipes/deployment.md` exits **0** (at least one retry/dup-send warning present).
    - `grep -iF 'test/example' guides/recipes/deployment.md` exits **0**.
    - `grep -F 'Strongly prefer Oban in production.' guides/recipes/deployment.md` exits **0**.
    - `grep -iF 'mix sigra.install' guides/recipes/deployment.md` exits **0** AND `grep -E '\\-\\-(no-)?live|jwt|api|admin|passkeys|binary-id' guides/recipes/deployment.md` exits **0** (at least one install flag token present).
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
- Spot-check in generated **`doc/deployment.html`**: checklist appears before env-var section; new mail TL;DR appears before Oban jobs section.
</verification>

<success_criteria>
- **ACF-01:** Published checklist in canonical deployment recipe with discoverable heading.
- **ACF-04:** Oban vs inline semantics and install/example pointers started in hub doc (cross-file pointers completed in plan **02**).
</success_criteria>
