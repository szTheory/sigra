---
phase: 68
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - guides/introduction/getting-started.md
  - guides/introduction/first-hour.md
  - guides/introduction/installation.md
  - MAINTAINING.md
autonomous: true
requirements:
  - ACF-01
  - ACF-04
must_haves:
  truths:
    - "**Five** inbound surfaces (**README**, **getting-started**, **first-hour**, **installation**, **MAINTAINING**) each contain **one** navigational pointer toward the deployment recipe checklist anchor per **068-CONTEXT D-12**."
    - "**`guides/introduction/installation.md`** documents **`mix sigra.install`** switches consistent with **`mix help sigra.install`** (executor-captured) per **D-10**."
    - "**`MAINTAINING.md`** gains a single adopters-facing sentence: releases do not validate host TLS/mail; checklist lives in guides — **no** duplicated checklist table."
  artifacts:
    - path: "README.md"
      provides: "ACF-01 discovery — before-production pointer"
    - path: "guides/introduction/getting-started.md"
      provides: "ACF-01 post-tutorial handoff"
    - path: "guides/introduction/first-hour.md"
      provides: "ACF-01 reading-map handoff"
    - path: "guides/introduction/installation.md"
      provides: "ACF-04 install flags + prod mail pointer"
    - path: "MAINTAINING.md"
      provides: "ACF-01 maintainer breadcrumb"
nyquist_compliant: false
---

<objective>
Add **thin cross-links** and **installation flag** documentation so **ACF-01** checklist discovery and **ACF-04** install/mail semantics meet roadmap success criteria, per **068-CONTEXT.md D-10–D-13**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-68-04 | Tampering (doc drift) | Duplicated **env-var** matrix in **README** diverges from **`deployment.md`** | high | One-sentence + one-link strips only; grep rejects **`| Variable |`** in README |
| T-68-05 | Misrepresentation | **MAINTAINING** implies maintainers validate adopters' TLS/mail | medium | Explicit sentence: host validation is adopters' job; link **`guides/recipes/deployment.md`** only |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: README — Before production strip</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - guides/recipes/deployment.md
    - .planning/phases/068-deploy-and-mail-confidence/068-CONTEXT.md
  </read_first>
  <action>
    In **`README.md`**, within or immediately after the **`## Pick your lane`** section (before **`## Where code lives`**), add a compact block titled **`## Before production`** (exact heading).

    Body: **≤ 4 sentences** total plus **one** bullet list of **1–3** items. Must include a markdown link to the deployment recipe using repo-relative path **`guides/recipes/deployment.md`** and fragment **`#production-checklist-read-first`** (exact fragment string in the URL).

    Do **not** paste the env-var table from **`deployment.md`**.
  </action>
  <acceptance_criteria>
    - `grep -nF '## Before production' README.md` exits **0**.
    - `grep -F 'guides/recipes/deployment.md#production-checklist-read-first' README.md` exits **0**.
    - `grep -nF '| Variable |' README.md` exits **1** (table header from deployment env matrix must not be copied into README).
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix compile --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: getting-started.md — end-of-doc handoff</name>
  <files>guides/introduction/getting-started.md</files>
  <read_first>
    - guides/introduction/getting-started.md
    - guides/recipes/deployment.md
  </read_first>
  <action>
    Append a new final section **`## Before you ship to production`** at the **end** of **`guides/introduction/getting-started.md`** (after the existing **`## Troubleshooting`** section content — i.e. extend the file; do not insert at top).

    Section must be **≤ 6 lines** including heading and contain a markdown link to **`../recipes/deployment.html#production-checklist-read-first`** (relative path from **`guides/introduction/`**).

    Optional: one sentence referencing **Oban** vs inline pointing to the same deployment doc anchor **`#mail-delivery-inline-vs-oban-tl-dr`** if that fragment matches the inserted H2 slug from plan **01** (if slug differs, use link to **`../recipes/deployment.html`** without fragment for mail and keep checklist fragment only).
  </action>
  <acceptance_criteria>
    - `grep -nF '## Before you ship to production' guides/introduction/getting-started.md` exits **0** and the line number is **greater than** `grep -nF '## Troubleshooting' guides/introduction/getting-started.md | head -1`.
    - `grep -F '../recipes/deployment.html#production-checklist-read-first' guides/introduction/getting-started.md` exits **0** OR `grep -F 'recipes/deployment.html#production-checklist-read-first' guides/introduction/getting-started.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 3: first-hour.md — reading map pointer</name>
  <files>guides/introduction/first-hour.md</files>
  <read_first>
    - guides/introduction/first-hour.md
    - guides/recipes/deployment.md
  </read_first>
  <action>
    In **`guides/introduction/first-hour.md`**, under **`## Reading map (shortest path)`**, add **one** new numbered item (renumber subsequent items) that links to **`../recipes/deployment.html#production-checklist-read-first`** with anchor text containing the words **Production** and **checklist** (case-insensitive OK).
  </action>
  <acceptance_criteria>
    - `grep -iF 'checklist' guides/introduction/first-hour.md` exits **0** AND `grep -F 'recipes/deployment.html' guides/introduction/first-hour.md` exits **0**.
    - `grep -F '## Reading map' guides/introduction/first-hour.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 4: installation.md — generator flags + prod mail</name>
  <files>guides/introduction/installation.md</files>
  <read_first>
    - guides/introduction/installation.md
    - lib/mix/tasks/sigra.install.ex
  </read_first>
  <action>
    After **`## Run the generator`** (or within that section after the first code block), add **`### mix sigra.install flags (reference)`** containing a **markdown table**: columns **`Flag`** | **`Effect`** listing each switch from **`mix help sigra.install`** at execution time (minimum set from **`@moduledoc`**: `--live`/`--no-live`, `--binary-id`/`--no-binary-id`, `--table`, `--api`, `--jwt`, `--admin`/`--no-admin`, `--passkeys`/`--no-passkeys`, `--yes`).

    Add **one** paragraph with a link to **`../recipes/deployment.html#mail-delivery-inline-vs-oban-tl-dr`** if that heading exists after plan **01** execution; otherwise link to **`../recipes/deployment.html`** and name the section **Mail delivery: inline vs Oban (TL;DR)** in prose.

    Add **one** sentence pointing to **`test/example/`** as the CI-backed reference host.
  </action>
  <acceptance_criteria>
    - `grep -F '### mix sigra.install flags (reference)' guides/introduction/installation.md` exits **0**.
    - `grep -iF '| flag |' guides/introduction/installation.md` exits **0** OR `grep -iF '| Flag |' guides/introduction/installation.md` exits **0**.
    - `grep -iF '--no-live' guides/introduction/installation.md` exits **0** OR `grep -iF '--live' guides/introduction/installation.md` exits **0**.
    - `grep -iF 'test/example' guides/introduction/installation.md` exits **0**.
    - `grep -F 'recipes/deployment.html' guides/introduction/installation.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `mix help sigra.install` exits **0** (executor captures output while editing).
  </verify>
</task>

<task type="execute">
  <name>Task 5: MAINTAINING.md — adopters breadcrumb</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - .planning/phases/068-deploy-and-mail-confidence/068-CONTEXT.md
  </read_first>
  <action>
    After the first paragraph of **`MAINTAINING.md`** (the paragraph ending with **`CONTRIBUTING.md`** for drive-by contributors), insert **one** new paragraph (2–3 sentences) stating that **Hex releases do not validate** an adopter's production TLS, reverse proxy, or mail configuration, and pointing to **`guides/recipes/deployment.md#production-checklist-read-first`** as the adopters' checklist (markdown link). **Do not** paste the checklist table.
  </action>
  <acceptance_criteria>
    - `grep -iF 'guides/recipes/deployment.md#production-checklist-read-first' MAINTAINING.md` exits **0**.
    - `grep -iF 'adopter' MAINTAINING.md` exits **0** OR `grep -iF 'production' MAINTAINING.md` exits **0** (breadcrumb audience clear).
    - `grep -nF '| Check |' MAINTAINING.md` exits **1** (no checklist table pasted).
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
- `rg -l 'production-checklist-read-first' README.md guides/introduction/getting-started.md guides/introduction/first-hour.md guides/introduction/installation.md MAINTAINING.md | wc -l` outputs **5** (each file mentions the fragment or path — executor uses `rg` if available, else `grep -l` per file).
</verification>

<success_criteria>
- **ACF-01:** Checklist linked from README, intro paths, and MAINTAINING per roadmap (1).
- **ACF-04:** Oban vs inline + install flags + example host pointer satisfied per roadmap (2).
</success_criteria>
