---
phase: 240-alpha-operations-rehearsal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/install/features/core.ex
  - priv/templates/sigra.install/core/rate_limit.ex
  - test/sigra/install/generated_rate_limit_contract_test.exs
  - scripts/ci/passkeys-opt-out-smoke.sh
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "D-08: A fresh canonical B2C host owns a concrete, host-configurable Hammer limiter; production generation cannot silently rely on the Noop fallback."
    - "D-09: One sensitive POST path deterministically allows its configured bounded attempts, then returns generic 429 plus a positive integer Retry-After without sleeping across a limiter window."
    - "FLAGGED ASSUMPTION — OPS-01 boundary: conservative generated bounds must be selected and tested at zero/limit/limit+1 because CONTEXT/RESEARCH do not lock exact numeric defaults."
    - "FLAGGED ASSUMPTION — OPS-01 precision: Retry-After uses the existing integer ceiling contract `div(milliseconds + 999, 1000)`; tests cover the exact-second and one-millisecond-over boundaries because no alternative rounding rule is specified."
  artifacts:
    - path: priv/templates/sigra.install/core/rate_limit.ex
      provides: Host-owned Hammer ETS limiter module generated by the canonical core feature.
    - path: lib/sigra/install/features/core.ex
      provides: Idempotent dependency, supervision, config, and one route-level limiter injection.
    - path: test/sigra/install/generated_rate_limit_contract_test.exs
      provides: Deterministic source and boundary contracts for the generated limiter.
    - path: scripts/ci/passkeys-opt-out-smoke.sh
      provides: Credential-free fresh-host bounded limiter request proof.
  key_links:
    - from: lib/sigra/install/features/core.ex
      to: priv/templates/sigra.install/core/rate_limit.ex
      via: Core.files/1 renders the host-owned limiter and injects its dependency, child, and config.
    - from: generated router POST scope
      to: Sigra.Plug.RateLimit
      via: Explicit `limiter: Sigra.RateLimiters.Hammer`, stable key prefix, limit, window, and generated AuthErrorHandler.
    - from: scripts/ci/passkeys-opt-out-smoke.sh
      to: fresh generated host
      via: Bounded requests against one real generated POST route with no secret-backed provider.
---

<objective>
Deliver the production-quality end-to-end tracer for generated B2C rate limiting per D-08 and D-09: generation emits a real Hammer owner, a high-risk POST route consumes it, and the credential-free fresh-host smoke observes bounded exhaustion and the exact denial contract.

Purpose: Prove the phase's hardest architectural constraint before documentation or broader evidence work.
Output: A generated limiter module, idempotent host wiring, one protected POST slice, and deterministic boundary evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/240-alpha-operations-rehearsal/240-CONTEXT.md
@.planning/phases/240-alpha-operations-rehearsal/240-RESEARCH.md
@.planning/phases/240-alpha-operations-rehearsal/240-PATTERNS.md
@.planning/phases/240-alpha-operations-rehearsal/240-VALIDATION.md
@lib/sigra/install/features/core.ex
@lib/sigra/plug/rate_limit.ex
@lib/sigra/rate_limiters/hammer.ex
@lib/sigra/rate_limiters/noop.ex
@test/sigra/plug/rate_limit_test.exs
@scripts/ci/passkeys-opt-out-smoke.sh

<interfaces>
`Sigra.Plug.RateLimit.init/1` requires `:error_handler` and accepts `:limiter`, `:limit`, `:window`, and `:key_prefix`. `call/2` derives `#{key_prefix}:ip:#{conn.remote_ip}`, ignores GET/HEAD, and on deny rounds milliseconds upward to seconds, sets `Retry-After`, invokes `auth_error(:rate_limited, retry_after: seconds)`, and halts. `Sigra.RateLimiters.Hammer.check_rate/3` reads `config :sigra, :hammer_module` and calls the generated Hammer module as `hit(key, window_ms, limit)`. `Core.files/1` and `%Injection{target, marker, anchor, content}` are the existing generation/idempotency seams.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate and exercise one protected B2C POST path end to end</name>
  <reversibility rating="costly">The generated dependency/config/router contract is host-owned output consumed by adopters; changing it later requires coordinated generator and golden-output updates.</reversibility>
  <files>lib/sigra/install/features/core.ex, priv/templates/sigra.install/core/rate_limit.ex, test/sigra/install/generated_rate_limit_contract_test.exs, scripts/ci/passkeys-opt-out-smoke.sh</files>
  <read_first>Read D-08 and D-09 in `240-CONTEXT.md`; the explicit ownership and route patterns in `240-RESEARCH.md` and `240-PATTERNS.md`; `Core.files/1`, `Core.injections/1`, router/config/application injection helpers in `lib/sigra/install/features/core.ex`; `Sigra.Plug.RateLimit.init/1` and `call/2`; `Sigra.RateLimiters.Hammer.check_rate/3`; and the B2C generation, `assert_match`, boot, and cleanup sections of `scripts/ci/passkeys-opt-out-smoke.sh`.</read_first>
  <behavior>
    - Test 1: Core output includes `{:hammer, "~> 7.4"}`, a generated `<App>.RateLimit` using Hammer ETS, a supervised child before Endpoint, `config :sigra, hammer_module: <App>.RateLimit`, and a route plug whose limiter is explicitly `Sigra.RateLimiters.Hammer` per D-08.
    - Test 2: Reapplying the feature does not duplicate the dependency, module, child, config, or route plug.
    - Test 3: With an injected low test limit, attempts 1 through N proceed, attempt N+1 returns 429 with generic non-enumerating text and integer `Retry-After`; no wait or clock-window crossing occurs per D-09.
    - Test 4: Existing Retry-After ceiling behavior is exact at 1_000ms -> 1, 1_001ms -> 2, and 30_500ms -> 31, resolving the flagged precision assumption without changing the library contract.
  </behavior>
  <action>Start RED by creating `Sigra.Install.GeneratedRateLimitContractTest` with source/render assertions for the complete ownership chain and explicit zero/limit/limit+1 plus Retry-After rounding boundaries. Then add `priv/templates/sigra.install/core/rate_limit.ex` defining the generated `<App>.RateLimit` with `use Hammer, backend: :ets`; extend `lib/sigra/install/features/core.ex` through existing `files/1` and `%Injection{}` seams to add the exact Hex dependency `{:hammer, "~> 7.4"}`, the limiter child before `<AppWeb>.Endpoint`, `config :sigra, hammer_module: <App>.RateLimit`, and one real high-risk POST route using `Sigra.Plug.RateLimit` with explicit `limiter: Sigra.RateLimiters.Hammer`, `error_handler: <AppWeb>.AuthErrorHandler`, a stable flow-specific prefix, and documented integer limit/window values. Choose conservative defaults under the agent's discretion, record them in test names, and cover N-1/N/N+1 rather than treating untested constants as safe. Use unique injection markers so a second install is idempotent. Extend only the canonical B2C branch in `scripts/ci/passkeys-opt-out-smoke.sh` to run a bounded generated-host request/ExUnit probe; it must use a disposable test configuration, must not sleep, and must never print request bodies, tokens, or secret values. Do not alter the library-wide nil/Noop fallback: D-08 is closed at generated-host ownership, not by breaking hosts that deliberately omit Hammer.</action>
  <verify>
    <automated>mix test test/sigra/install/generated_rate_limit_contract_test.exs test/sigra/plug/rate_limit_test.exs &amp;&amp; bash -n scripts/ci/passkeys-opt-out-smoke.sh &amp;&amp; scripts/ci/passkeys-opt-out-smoke.sh</automated>
  </verify>
  <acceptance_criteria>D-08 is observable in a fresh B2C host through concrete dependency/module/child/config/route ownership; D-09 is observable as bounded allow/deny behavior with generic 429 and positive rounded Retry-After, distinct boundary assertions, zero sleeps, and repeat-install idempotency.</acceptance_criteria>
  <done>The fresh canonical B2C smoke proves one protected POST route end to end and the focused contracts pass at all stated threshold and rounding boundaries.</done>
</task>

</tasks>

## Artifacts this phase produces

- `priv/templates/sigra.install/core/rate_limit.ex` — generated host limiter owner.
- `test/sigra/install/generated_rate_limit_contract_test.exs` — limiter generation and threshold contract.
- Extended fresh B2C smoke evidence in `scripts/ci/passkeys-opt-out-smoke.sh`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted client -> generated Phoenix POST route | Attacker-controlled request frequency and remote address enter route limiter enforcement. |
| trusted proxy -> `conn.remote_ip` | Host proxy normalization determines whether IP keys identify clients or collapse shared traffic. |
| generated host -> Hammer ETS process | The route depends on explicit config and a supervised limiter owner rather than the library's fail-open fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240-01 | Denial of Service | generated sensitive POST route | high | mitigate | Per D-08, inject explicit Hammer ownership and a bounded route plug; prove N+1 denial in the disposable host. |
| T-240-02 | Spoofing | `conn.remote_ip` key | medium | mitigate | Keep the plug IP-based and require trusted-proxy normalization in the later operator tuple; do not trust arbitrary forwarding headers in library code. |
| T-240-03 | Information Disclosure | throttle response/logging | medium | mitigate | Per D-09, emit generic response text and only key-prefix/retry metadata; the smoke prints no tokens, bodies, or secrets. |
| T-240-04 | Tampering | generator reinvocation | medium | mitigate | Use stable markers and idempotency assertions so repeated generation cannot duplicate or bypass enforcement. |
</threat_model>

<verification>
The focused ExUnit suites pass, shell syntax is valid, and the fresh B2C smoke generates, compiles, boots, and observes the bounded denial contract without live credentials or sleeps.
</verification>

<success_criteria>
One canonical B2C POST path is protected end to end by a generated, supervised, explicitly configured Hammer limiter, with deterministic threshold and Retry-After proof.
</success_criteria>

<output>
Create `.planning/phases/240-alpha-operations-rehearsal/240-01-SUMMARY.md` when done.
</output>
