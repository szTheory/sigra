---
phase: 240-alpha-operations-rehearsal
plan: 02
type: execute
wave: 2
depends_on:
  - 240-05
  - 240-01
files_modified:
  - lib/sigra/install/features/core.ex
  - priv/templates/sigra.install/core/auth.ex
  - test/sigra/install/generated_rate_limit_context_test.exs
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex
  - test/fixtures/install_golden/tree/config/config.exs
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/rate_limit.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/application.ex
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "D-08: Every canonical sensitive identity/account flow has explicit Hammer ownership at its actual route or LiveView/context boundary."
    - "D-09: Route prefixes and mail-request keys are independent, bounded, generic, and deterministic; exhausting one flow cannot consume another flow's allowance."
    - "FLAGGED ASSUMPTION — OPS-01 boundary: exact per-flow bounds are not locked; each selected default is documented and tested at N-1/N/N+1 with no elapsed-window wait."
    - "FLAGGED ASSUMPTION — OPS-01 precision: all windows remain integer milliseconds and route Retry-After remains ceiling-rounded seconds; no floating-point or tie-breaking path is introduced."
  artifacts:
    - path: priv/templates/sigra.install/core/auth.ex
      provides: Explicit context-level limiter options for magic-link and password-reset mail requests.
    - path: lib/sigra/install/features/core.ex
      provides: Complete route-level sensitive-flow map with independent prefixes.
    - path: test/sigra/install/generated_rate_limit_context_test.exs
      provides: Independent-key, enumeration-safety, and bounded-flow contracts.
  key_links:
    - from: generated mail-request wrappers
      to: Sigra.Auth.request_magic_link/3 and Sigra.Auth.request_password_reset/3
      via: `rate_limiter: Sigra.RateLimiters.Hammer`, explicit max request count, and integer window options.
    - from: generated router
      to: high-risk controller POST flows
      via: Separate stable `key_prefix` values and the tracer's concrete limiter.
    - from: context limiter denial
      to: generated LiveView/controller UX
      via: Existing generic outcome branches; no account-existence distinction.
---

<objective>
Expand the proven tracer across every canonical B2C sensitive flow per D-08 and D-09, separating IP-owned controller throttles from context-owned mail-request throttles and sealing generated golden parity.

Purpose: Prevent a single protected login route from being mistaken for complete generated-host enforcement.
Output: A complete flow-to-limiter map, independent deterministic contracts, and regenerated exact host fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/240-alpha-operations-rehearsal/240-CONTEXT.md
@.planning/phases/240-alpha-operations-rehearsal/240-RESEARCH.md
@.planning/phases/240-alpha-operations-rehearsal/240-PATTERNS.md
@.planning/phases/240-alpha-operations-rehearsal/240-05-SUMMARY.md
@.planning/phases/240-alpha-operations-rehearsal/240-01-SUMMARY.md
@priv/templates/sigra.install/core/auth.ex
@lib/sigra/auth.ex
@priv/templates/sigra.install/core/session_controller.ex
@priv/templates/sigra.install/core/registration_live.ex
@priv/templates/sigra.install/core/reset_password_live.ex
@test/sigra/install/golden_diff_test.exs

<interfaces>
`Sigra.Auth.request_magic_link/3` accepts `:rate_limiter`, `:max_requests` (default 3), and `:window_ms` (default 900_000) and keys normalized email as `magic_link:<email>`. `Sigra.Auth.request_password_reset/3` accepts the same options and keys `sigra:reset:<email>`. Both return `{:error, :rate_limited}` while preserving `{:ok, :sent}` for absent users. Generated `auth.ex` wrappers are the host configuration seam; LiveView request events do not pass through distinct router POST routes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map and enforce every route and context limiter boundary</name>
  <reversibility rating="costly">Generated key prefixes and bounds become an operational contract across route, context, docs, and adopter override configuration.</reversibility>
  <files>lib/sigra/install/features/core.ex, priv/templates/sigra.install/core/auth.ex, test/sigra/install/generated_rate_limit_context_test.exs</files>
  <read_first>Read D-08 and D-09; the Architectural Responsibility Map and resolved Open Question 1 in `240-RESEARCH.md`; the Wave 0 context contract; the generated controller routes in `lib/sigra/install/features/core.ex`; `request_magic_link/2`, `deliver_magic_link/2`, and password-reset wrappers in `priv/templates/sigra.install/core/auth.ex`; the corresponding `Sigra.Auth` option/key contracts; and generic outcome handling in `session_controller.ex`, `registration_live.ex`, and `reset_password_live.ex`.</read_first>
  <behavior>
    - Test 1: Each real high-risk controller POST route has an explicit Hammer plug and a distinct IP prefix; safe GET/HEAD paths are unaffected.
    - Test 2: Magic-link and password-reset delivery calls pass the explicit Hammer adapter plus named integer max/window options through the generated Auth context.
    - Test 3: Exhausting route prefix A does not affect prefix B; exhausting a context email key does not consume the route IP key.
    - Test 4: Existing-user, absent-user, and limited mail requests retain generic outward results and never disclose account existence.
  </behavior>
  <action>Run the Wave 0 context contract first, then implement the exact mapping selected in resolved Open Question 1. Apply route-level IP limiting to each mutating controller route emitted by Core—login and sudo in the default LiveView output, plus registration, confirmation/resend, password-reset, and MFA controller variants when `--no-live` emits them—with action-specific stable prefixes and the tracer's concrete Hammer adapter. Keep safe GET/HEAD token-consumption and form-render routes unaffected. Apply context-level limiting to the repeatable anonymous mail requests that bypass a distinct controller boundary: generated `request_magic_link/2` and `deliver_user_reset_password_instructions/2` pass `rate_limiter: Sigra.RateLimiters.Hammer` and explicit integer max/window values into the existing library APIs. Preserve their existing normalized-email keys, make the magic-link branch demonstrably independent from its route-IP key, and do not claim a router pipeline protects `handle_event/3`. Preserve generic success/error handling; do not expose whether the account exists or name Hammer, queues, providers, or secrets to end users. Select and document conservative bounds under the agent's discretion, then test N-1/N/N+1 and cross-key independence without timers or elapsed-window waits.</action>
  <verify>
    <automated>mix test test/sigra/install/generated_rate_limit_context_test.exs test/sigra/install/generated_rate_limit_contract_test.exs test/sigra/plug/rate_limit_test.exs</automated>
  </verify>
  <acceptance_criteria>D-08 covers the complete canonical B2C route/context map, and D-09 proves bounded exhaustion, distinct route/context keys, generic anti-enumeration behavior, and integer window/rounding contracts deterministically.</acceptance_criteria>
  <done>Every sensitive generated B2C flow is assigned to an explicit route or context limiter with independent deterministic proof.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and verify exact installer golden parity</name>
  <files>test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts.ex, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex, test/fixtures/install_golden/tree/config/config.exs, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/rate_limit.ex, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/application.ex</files>
  <read_first>Read the snapshot regeneration contract in `test/sigra/install/golden_diff_test.exs`, the completed limiter implementation from Task 1, and the exact generated paths reported by the failing golden diff.</read_first>
  <action>Run `mix test test/sigra/install/golden_diff_test.exs --only snapshot` only after the source contracts are green. Review the resulting fixture diff and retain only deterministic output attributable to D-08/D-09: the Hammer module, application child, Sigra Hammer config, route plugs, and context options. If the snapshot reports another exact changed path required by those injections (for example generated `mix.exs`), add that path to this task's committed artifact list rather than suppressing the diff. Reject timestamps, environment-specific paths, credentials, unrelated formatting, or unrelated feature output. Then run the normal golden and idempotency suites.</action>
  <verify>
    <automated>mix test test/sigra/install/golden_diff_test.exs test/sigra/install/idempotency_test.exs</automated>
  </verify>
  <acceptance_criteria>The committed golden tree is byte-for-byte equal to a fresh install, rerunning the installer is idempotent, and every retained delta maps directly to the explicit limiter ownership contract.</acceptance_criteria>
  <done>Golden and idempotency suites pass with only the reviewed limiter ownership output changed.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete route/context limiter wiring in `core.ex` and generated `auth.ex`.
- Independent-key and anti-enumeration contracts in `generated_rate_limit_context_test.exs`.
- Exact generated-host golden output for the new limiter ownership chain.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted email input -> generated Auth context | Email-derived limiter keys and account lookup must not leak existence. |
| LiveView event -> Auth service | Mail-request events bypass route-specific POST plugs and require context enforcement. |
| generated source -> committed golden host | Snapshot update must not admit unrelated or secret-bearing output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240-05 | Denial of Service | mail-request LiveView flows | high | mitigate | Per D-08, pass explicit Hammer options at context call sites and prove bounded denial. |
| T-240-06 | Information Disclosure | magic-link/reset outcome | high | mitigate | Per D-09, retain normalized generic outcomes for existing, absent, and limited accounts. |
| T-240-07 | Tampering | shared limiter key space | medium | mitigate | Use stable distinct prefixes/keys and prove exhaustion independence. |
| T-240-08 | Repudiation | golden snapshot | low | accept | Snapshot provenance is the deterministic generator command and reviewed scoped diff; no runtime audit record is needed for repository test data. |
</threat_model>

<verification>
Focused context/plug tests, installer golden diff, and idempotency all pass without sleeps, secrets, or widened unrelated fixture churn.
</verification>

<success_criteria>
Every canonical B2C sensitive operation is limited at the layer that owns it, and generated fixture evidence proves the complete result is stable and repeatable.
</success_criteria>

<output>
Create `.planning/phases/240-alpha-operations-rehearsal/240-02-SUMMARY.md` when done.
</output>
