---
phase: 240-alpha-operations-rehearsal
plan: 03
type: execute
wave: 2
depends_on:
  - 240-01
files_modified:
  - guides/recipes/b2c-alpha.md
  - guides/recipes/deployment.md
  - test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "D-01 through D-07: The single B2C checklist has Library CI proof, Host pre-deploy, and Staging launch gate tiers; every row names owner, observable result, and must-not-claim boundary."
    - "D-02/D-03: Google callback registration is exact, while real Google, controlled confirmation/reset/magic-link delivery, and physical-iPhone hosted return remain mandatory, explicitly unpassable repository gates with outcome-only redacted receipts."
    - "D-04/D-05: The host records one literal canonical origin/proxy/cookie tuple and rehearses clean-browser session behavior with host-only Secure, HttpOnly, SameSite=Lax defaults."
    - "D-06/D-07: Wiring/boot and delivery gates are separate; Doctor is only configuration/dependency evidence and receipts/logs exclude sensitive values and payloads."
    - "FLAGGED ASSUMPTION — OPS-01 boundary: generated limiter thresholds are host-overridable and the recipe tests one below, at, and one above each configured bound; exact defaults come from Plans 01-02 rather than an unstated documentation constant."
    - "FLAGGED ASSUMPTION — OPS-01 precision: the recipe names millisecond windows and ceiling-rounded whole-second Retry-After behavior; it makes no claim about sub-second provider timing."
    - "FLAGGED ASSUMPTION — OPS-02 unclassified: because the probe supplied no classification, the contract test treats every CI-vs-host claim as fail-closed evidence vocabulary and rejects any sentence that promotes host-only proof to repository-passed status."
  artifacts:
    - path: guides/recipes/b2c-alpha.md
      provides: Single provider-neutral operator checklist and redacted receipt format.
    - path: guides/recipes/deployment.md
      provides: Cross-linked detailed deployment mechanics consistent with the canonical checklist.
    - path: test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs
      provides: Source contract for evidence tiers, configuration tuple, required host gates, redaction, and truthful claims.
  key_links:
    - from: guides/recipes/b2c-alpha.md
      to: guides/recipes/deployment.md
      via: Checklist rows link to detailed mechanics without creating a second competing launch checklist.
    - from: Host pre-deploy tuple
      to: clean-browser rehearsal
      via: One literal public origin/Endpoint/proxy/cookie/SameSite record.
    - from: Staging launch gate
      to: redacted receipt
      via: Outcome-only timestamps/environment/config fingerprints; no tokens, URLs, mail bodies, provider payloads, or secrets.
---

<objective>
Publish the single provider-neutral B2C operations checklist and mechanically enforce its three-tier evidence semantics per D-01 through D-07, while tying OPS-01 mechanics to OPS-02's unpassable host staging boundary.

Purpose: Give adopters an exact launch rehearsal without allowing repository evidence to overclaim provider, delivery, proxy, or device success.
Output: Canonical recipe, deployment cross-links, redacted receipt schema, and deterministic documentation contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/240-alpha-operations-rehearsal/240-CONTEXT.md
@.planning/phases/240-alpha-operations-rehearsal/240-RESEARCH.md
@.planning/phases/240-alpha-operations-rehearsal/240-01-SUMMARY.md
@guides/recipes/b2c-alpha.md
@guides/recipes/deployment.md
@guides/flows/oauth.md
@guides/recipes/subdomain-auth.md
@lib/sigra/doctor.ex
@lib/mix/tasks/sigra.doctor.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the three-tier launch checklist and redacted evidence contract</name>
  <reversibility rating="costly">The published checklist becomes the adopter-facing launch contract and must remain synchronized with generated configuration and CI claim boundaries.</reversibility>
  <files>guides/recipes/b2c-alpha.md, guides/recipes/deployment.md, test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs</files>
  <read_first>Read all D-01 through D-07 and D-09 through D-11; OPS-01/OPS-02 in ROADMAP/REQUIREMENTS; existing `b2c-alpha.md` and `deployment.md`; OAuth callback guidance; subdomain cookie constraints; Doctor implementation/task exit semantics; and the actual defaults and flow names recorded in `240-01-SUMMARY.md`.</read_first>
  <behavior>
    - Test 1: The recipe contains exactly the three D-01 tier headings and checklist rows with Owner, Expected result, and Must not claim fields.
    - Test 2: The literal host tuple includes canonical HTTPS public origin, matching Endpoint.url, trusted forwarded-scheme/client-IP policy, absent Domain attribute, Secure, HttpOnly, and SameSite=Lax per D-04/D-05.
    - Test 3: The wiring/boot tier names runtime-only SECRET_KEY_BASE, CLOAK_KEY, Google credentials, mailer config, Vault/app boot, and `mix sigra.doctor --quiet`; the delivery tier separately consumes confirmation/reset/magic-link mail in a clean browser per D-06/D-07.
    - Test 4: Exact Google callback is `https://<canonical-host>/auth/google/callback`; real Google, controlled mail, and physical-iPhone HTTPS hosted return are mandatory staging gates but have no repository-pass marker per D-02/D-03.
    - Test 5: Receipt fields are outcome-only and tests reject secret values, token-bearing URLs, mail bodies, provider payloads, and claims beyond Doctor/local evidence.
  </behavior>
  <action>Create the failing source-contract test first, parsing tier headings and row fields rather than relying on vague substring counts. Rewrite `guides/recipes/b2c-alpha.md` as the sole B2C checklist: Library CI proof, Host pre-deploy, Staging launch gate. For every item provide Owner, exact action/config key or command, observable expected result, must-not-claim boundary, and nearby recovery guidance. Record one literal D-04/D-05 tuple with one HTTPS public origin, matching `Endpoint.url`, trusted proxy forwarded scheme/client-IP policy, host-only cookie (no Domain), Secure, HttpOnly, SameSite=Lax; treat shared domains/SameSite=None only as explicitly reasoned host deviations. Separate D-06 wiring/boot from delivery. Describe Doctor exactly per D-07. Retain Google only for `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, and the exact callback per D-02. Make D-03's real Google, controlled-recipient confirmation/reset/magic-link consumption, and physical-iPhone hosted return visibly required but impossible for repository CI to mark passed. Add a redacted receipt template containing outcome, timestamp, environment label, non-sensitive config fingerprint, and operator sign-off only. Cross-link detailed mechanics from `deployment.md` while preventing it from becoming a competing checklist. Incorporate the limiter defaults/results from Plan 01, including boundary and rounding wording, without inventing new constants.</action>
  <verify>
    <automated>mix test test/sigra/planning/phase_240_alpha_operations_rehearsal_test.exs &amp;&amp; mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>D-01 through D-07 are each asserted by executable documentation contracts; OPS-01 contains all origin/session/Google/Cloak/rate-limit/mail rehearsal requirements; OPS-02's real-provider/mail/iPhone gates remain required, unpassable by CI, and recordable only through redacted host evidence.</acceptance_criteria>
  <done>The canonical recipe and deployment guide build cleanly and their source contract proves every evidence tier, operational tuple field, staging gate, and redaction boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- `guides/recipes/b2c-alpha.md` — canonical three-tier launch checklist.
- `guides/recipes/deployment.md` — aligned mechanics and non-competing cross-links.
- `phase_240_alpha_operations_rehearsal_test.exs` — deterministic claim and redaction contract.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository proof -> adopter launch decision | Local/CI evidence must not be interpreted as provider or deployment acceptance. |
| operator environment -> redacted receipt | Runtime secrets, token URLs, mail content, and provider payloads must not cross into durable evidence. |
| reverse proxy -> Phoenix endpoint/session | Canonical origin and client IP depend on explicit trusted proxy policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240-09 | Spoofing | forwarded scheme/client IP | high | mitigate | Per D-04/D-05, require one trusted-proxy policy tuple and staging observation of canonical URL/session/throttle behavior. |
| T-240-10 | Information Disclosure | launch receipt | high | mitigate | Per D-07, allow only outcome metadata and reject secrets, token URLs, bodies, and provider payloads in source contracts. |
| T-240-11 | Repudiation | host staging checks | medium | mitigate | Per D-03, require timestamped operator/environment outcomes while explicitly preventing repository auto-pass. |
| T-240-12 | Elevation of Privilege | insecure session posture | high | mitigate | Per D-04, default to host-only Secure/HttpOnly/SameSite=Lax and require explicit rationale for broader cookie posture. |
</threat_model>

<verification>
The phase contract test and warnings-as-errors docs build pass; negative assertions prove the recipe neither exposes evidence secrets nor claims host-only gates passed.
</verification>

<success_criteria>
An adopter can follow one exact checklist from credential-free library proof through pre-deploy wiring to mandatory staging launch evidence, with every claim boundary machine-enforced.
</success_criteria>

<output>
Create `.planning/phases/240-alpha-operations-rehearsal/240-03-SUMMARY.md` when done.
</output>
