---
phase: 38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.3-HUMAN-UAT.md
  - .planning/uat-evidence/v1.3.0/INDEX.md
  - .planning/uat-evidence/v1.3.0/REDACTION-CHECKLIST.md
autonomous: true
requirements:
  - UAT-02
must_haves:
  truths:
    - ".planning/v1.3-HUMAN-UAT.md exists and contains a markdown table with header row including literal substrings `SEED_item`, `Status`, `Evidence_link`."
    - ".planning/uat-evidence/v1.3.0/INDEX.md exists and links to each SEED item subdirectory name `item-01-lockout-mail` through `item-08-getting-started` (eight lines or bullet links)."
    - "REDACTION-CHECKLIST.md states no raw magic links, tokens, or .env paste — grep-verifiable strings from D-38-P04."
  artifacts:
    - path: ".planning/v1.3-HUMAN-UAT.md"
      provides: "Master UAT table scaffold for eight SEED-001 rows"
    - path: ".planning/uat-evidence/v1.3.0/INDEX.md"
      provides: "Evidence inventory root for v1.3.0"
nyquist_compliant: false
---

<objective>
Satisfy **UAT-02** scaffolding: create the **single milestone-visible** human UAT artifact (`.planning/v1.3-HUMAN-UAT.md`) plus the **v1.3.0** evidence tree root with `INDEX.md` and a **redaction checklist**, matching **D-38-08** through **D-38-10** and **D-38-05** / **D-38-06**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-38-01 | Information disclosure | Secrets pasted into `.planning/uat-evidence/` | high | REDACTION-CHECKLIST.md + forbid live tokens in INDEX |
| T-38-02 | Repudiation | Table rows without owner/date | medium | Master table columns per D-38-09 |
| T-38-03 | Tampering | Orphan evidence paths not listed in INDEX | low | INDEX enumerates eight `item-*` dirs |
</threat_model>

<context>
@.planning/phases/38-human-ga-uat-gate/38-CONTEXT.md
@.planning/phases/38-human-ga-uat-gate/38-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/seeds/SEED-001-v1.0-ga-human-uat-gate.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create evidence tree root + INDEX + redaction checklist</name>
  <files>.planning/uat-evidence/v1.3.0/INDEX.md,.planning/uat-evidence/v1.3.0/REDACTION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/38-human-ga-uat-gate/38-CONTEXT.md
    - .planning/phases/38-human-ga-uat-gate/38-RESEARCH.md
    - .planning/seeds/SEED-001-v1.0-ga-human-uat-gate.md
  </read_first>
  <action>
    1. Create directory `.planning/uat-evidence/v1.3.0/` if absent.
    2. Create eight empty evidence subdirectories: `item-01-lockout-mail`, `item-02-lifecycle-mail`, `item-03-gen-oauth-greenfield`, `item-04-google-oauth-e2e`, `item-05-provider-linking-ui`, `item-06-email-match-flash`, `item-07-backup-regenerate`, `item-08-getting-started` (each may contain a one-line `README.md` placeholder: "Evidence captured here; see parent INDEX.md").
    3. Write `INDEX.md` listing: release folder purpose, eight subdirectory links (relative paths), placeholder column for `sigra` Hex or git SHA to fill at execution, and a **Waiver cross-reference** subsection with anchor text `Waiver records` (empty list OK on first commit).
    4. Write `REDACTION-CHECKLIST.md` containing these literal checklist items as separate lines: `Crop secrets out of screenshots`, `Shorten or remove query tokens from URLs`, `Never commit .env`, `Prefer mailbox HTML source copy over full-window screenshot when possible`.
  </action>
  <acceptance_criteria>
    - `test -d .planning/uat-evidence/v1.3.0/item-08-getting-started` exits 0.
    - `grep -q "Waiver records" .planning/uat-evidence/v1.3.0/INDEX.md` exits 0.
    - `grep -q "Never commit .env" .planning/uat-evidence/v1.3.0/REDACTION-CHECKLIST.md` exits 0.
    - `grep -c "item-0" .planning/uat-evidence/v1.3.0/INDEX.md` prints a number **greater than or equal to 8** (eight item references).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .planning/uat-evidence/v1.3.0/INDEX.md && test -f .planning/uat-evidence/v1.3.0/REDACTION-CHECKLIST.md'</automated>
  </verify>
  <done>Evidence root is navigable and redaction rules are pinned before humans add captures.</done>
</task>

<task type="execute">
  <name>Task 2: Author v1.3-HUMAN-UAT.md master table (eight rows, Pending)</name>
  <files>.planning/v1.3-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/38-human-ga-uat-gate/38-CONTEXT.md
    - .planning/seeds/SEED-001-v1.0-ga-human-uat-gate.md
    - .planning/uat-evidence/v1.3.0/INDEX.md
  </read_first>
  <action>
    1. Create `.planning/v1.3-HUMAN-UAT.md` with title `# Sigra v1.3 — Human GA UAT (SEED-001)` as the first line.
    2. Include a short preamble (2–4 sentences) stating: execute-by-default per D-38-01; this file is canonical per D-38-08; evidence lives under `.planning/uat-evidence/v1.3.0/`.
    3. Add a **markdown table** with **exact** header cell text containing these substrings in order: `SEED_item`, `Status`, `Date`, `Owner`, `Environment`, `Evidence_link`, `Expiry`, `Notes`.
    4. Add **exactly eight** data rows with `SEED_item` values: `SEED-1`, `SEED-2`, …, `SEED-8` (digits only after hyphen), each with `Status` cell **`Pending`**, `Date` **`—`**, `Owner` **`—`**, `Environment` **`—`**, `Evidence_link` pointing to the matching `item-0N-*` path from Task 1 (relative path from repo root), `Expiry` **`—`**, `Notes` summarizing the SEED-001 one-line description for that item.
    5. Add section `## Changelog pointer` with literal sentence: `Release notes may add one line: Human UAT outcomes in v1.3-HUMAN-UAT.md.`
  </action>
  <acceptance_criteria>
    - First line of `.planning/v1.3-HUMAN-UAT.md` is exactly `# Sigra v1.3 — Human GA UAT (SEED-001)`.
    - `grep -q "SEED_item" .planning/v1.3-HUMAN-UAT.md` exits 0.
    - `grep -c "| SEED-" .planning/v1.3-HUMAN-UAT.md` prints **8**.
    - `grep -c "| Pending |" .planning/v1.3-HUMAN-UAT.md` prints **8** (Status column for all eight data rows).
    - `grep -q "Release notes may add one line" .planning/v1.3-HUMAN-UAT.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .planning/v1.3-HUMAN-UAT.md && grep -q "SEED-8" .planning/v1.3-HUMAN-UAT.md'</automated>
  </verify>
  <done>UAT-02 artifact exists in-repo with eight traceable rows ready for Plan 02 execution.</done>
</task>

</tasks>

<verification>
- All Task 1 and Task 2 `grep` / `test` acceptance criteria pass from repo root.
- Optional: `bash scripts/ci/milestone-verification-gate.sh` exits 0 if this phase is merged alongside other v1.3 gates requiring it.
</verification>

<success_criteria>
- **UAT-02** satisfied at scaffolding level: canonical file + evidence root + eight-row table live in git.
</success_criteria>
