---
phase: 38
plan: 02
type: execute
wave: 2
depends_on:
  - "38-01"
files_modified:
  - .planning/v1.3-HUMAN-UAT.md
  - .planning/uat-evidence/v1.3.0/INDEX.md
  - .planning/uat-evidence/v1.3.0/item-01-lockout-mail/
  - .planning/uat-evidence/v1.3.0/item-02-lifecycle-mail/
  - .planning/uat-evidence/v1.3.0/item-03-gen-oauth-greenfield/
  - .planning/uat-evidence/v1.3.0/item-04-google-oauth-e2e/
  - .planning/uat-evidence/v1.3.0/item-05-provider-linking-ui/
  - .planning/uat-evidence/v1.3.0/item-06-email-match-flash/
  - .planning/uat-evidence/v1.3.0/item-07-backup-regenerate/
  - .planning/uat-evidence/v1.3.0/item-08-getting-started/
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
autonomous: false
requirements:
  - UAT-01
must_haves:
  truths:
    - "Each of the eight rows in `.planning/v1.3-HUMAN-UAT.md` has `Status` of **Executed**, **Waived**, or **Blocked** (not `Pending`) with non-placeholder `Date` and `Owner` when status is Executed or Waived per D-38-03."
    - "Every Executed or Waived row has a non-empty `Evidence_link` path under `.planning/uat-evidence/v1.3.0/` that exists on disk at verification time."
    - "UAT-01 and UAT-02 checkboxes in `.planning/REQUIREMENTS.md` are flipped to `[x]` only after the above is true."
  artifacts:
    - path: ".planning/v1.3-HUMAN-UAT.md"
      provides: "Completed human GA matrix"
    - path: ".planning/uat-evidence/v1.3.0/INDEX.md"
      provides: "Final inventory + SHA anchors"
nyquist_compliant: false
---

<objective>
Satisfy **UAT-01**: for each **SEED-001** human-only item, produce **Executed** evidence (text-first per D-38-06) or a **Waived** / **Blocked** row meeting **D-38-03** minimum waiver fields in-table or in linked waiver markdown under the item directory; then close **REQUIREMENTS.md** checkboxes and add the **CHANGELOG** one-liner per **D-38-10**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-38-01 | Information disclosure | OAuth/mail captures leak secrets | high | Apply REDACTION-CHECKLIST.md before every `git commit` |
| T-38-03 | Elevation of privilege | Waivers on session/OAuth without compensation | high | D-38-07 fail-closed — stand-in IdP evidence if Google waived |
| T-38-04 | Denial of service | Blocked items undocumented | medium | Blocked rows must name blocking issue + GitHub issue link in `Notes` |
</threat_model>

<context>
@.planning/phases/38-human-ga-uat-gate/38-CONTEXT.md
@.planning/phases/38-human-ga-uat-gate/38-RESEARCH.md
@.planning/v1.3-HUMAN-UAT.md
@scripts/uat/RUNBOOK.md
@guides/introduction/getting-started.md
@test/example/priv/playwright/tests/golden-path.spec.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Items SEED-1 and SEED-2 — email HTML across clients</name>
  <files>.planning/uat-evidence/v1.3.0/item-01-lockout-mail,.planning/uat-evidence/v1.3.0/item-02-lifecycle-mail,.planning/v1.3-HUMAN-UAT.md</files>
  <read_first>
    - scripts/uat/RUNBOOK.md
    - .planning/uat-evidence/v1.3.0/REDACTION-CHECKLIST.md
    - .planning/phases/04-session-management-and-security-baseline/04-VERIFICATION.md
    - .planning/phases/08-account-lifecycle/08-VERIFICATION.md
  </read_first>
  <action>
    1. Follow `scripts/uat/RUNBOOK.md` to bring up `test/example` and mailbox preview at `http://localhost:4000/dev/mailbox`.
    2. For **SEED-1** (lockout + suspicious-login templates): trigger sends; capture **text-first** evidence (`steps.md` with URLs redacted, optional redacted screenshots) under `item-01-lockout-mail/`; for each of **Gmail, Outlook, Apple Mail** either record **Executed** in client or **Waived** with D-38-03 fields + compensation (e.g. HTML source diff) per D-38-04.
    3. For **SEED-2** (seven lifecycle templates): same pattern under `item-02-lifecycle-mail/`.
    4. Update `.planning/v1.3-HUMAN-UAT.md` rows **SEED-1** and **SEED-2**: set `Status`, `Date`, `Owner`, `Environment` tuple (Elixir/OTP, Postgres, sigra ref, host), `Evidence_link`, `Expiry` (if waived), `Notes`.
    5. Update `INDEX.md` with one-line pointers to new files and `sigra` version/git SHA used.
  </action>
  <acceptance_criteria>
    - Lines matching `\| SEED-1 \|` and `\| SEED-2 \|` in `.planning/v1.3-HUMAN-UAT.md` each contain `Executed`, `Waived`, or `Blocked` and do **not** contain `| Pending |`.
    - `test -f .planning/uat-evidence/v1.3.0/item-01-lockout-mail/steps.md` OR `test -f .planning/uat-evidence/v1.3.0/item-01-lockout-mail/waiver.md` exits 0.
    - Same OR condition holds for `item-02-lifecycle-mail/` with `steps.md` or `waiver.md`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep "| SEED-1 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"; grep "| SEED-2 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"'</automated>
  </verify>
  <done>Email-class human items have terminal status with evidence or compliant waiver.</done>
</task>

<task type="execute">
  <name>Task 2: Items SEED-3 and SEED-4 — OAuth generator + Google E2E</name>
  <files>.planning/uat-evidence/v1.3.0/item-03-gen-oauth-greenfield,.planning/uat-evidence/v1.3.0/item-04-google-oauth-e2e,.planning/v1.3-HUMAN-UAT.md</files>
  <read_first>
    - .planning/seeds/SEED-001-v1.0-ga-human-uat-gate.md
    - .planning/phases/05-oauth-and-social-login/05-VERIFICATION.md
    - guides/introduction/getting-started.md
  </read_first>
  <action>
    1. **SEED-3:** On a **fresh** `mix phx.new` app (per D-38-12 when installer/templates touched; otherwise document hotfix re-anchor per D-38-12), run `mix sigra.gen.oauth` (or current documented generator task name — use exact command from live docs); capture file list + `tree` or `find` transcript in `item-03-gen-oauth-greenfield/install-transcript.txt` noting **12+** files if true.
    2. **SEED-4:** Run Google OAuth register/login cycle with real credentials **or** mark **Waived** with stand-in IdP compensation per D-38-13; store captures under `item-04-google-oauth-e2e/`.
    3. Update master table rows **SEED-3**, **SEED-4** accordingly.
  </action>
  <acceptance_criteria>
    - Lines matching `\| SEED-3 \|` and `\| SEED-4 \|` do not contain `| Pending |`.
    - `item-03-gen-oauth-greenfield/install-transcript.txt` exists OR `item-03-gen-oauth-greenfield/waiver.md` exists.
    - `item-04-google-oauth-e2e/` contains at least one `.md` or `.txt` evidence file **or** `waiver.md` with literal substring `compensating`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep "| SEED-3 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"; grep "| SEED-4 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"'</automated>
  </verify>
  <done>OAuth-class items resolved with greenfield proof or documented waiver.</done>
</task>

<task type="execute">
  <name>Task 3: Items SEED-5 and SEED-6 — linking UI + email-match flash</name>
  <files>.planning/uat-evidence/v1.3.0/item-05-provider-linking-ui,.planning/uat-evidence/v1.3.0/item-06-email-match-flash,.planning/v1.3-HUMAN-UAT.md</files>
  <read_first>
    - scripts/uat/RUNBOOK.md
    - test/example/priv/playwright/tests/golden-path.spec.ts
  </read_first>
  <action>
    1. In browser against `test/example`, execute provider linking / last-method unlink prevention checks per SEED-001 item 5; capture notes under `item-05-provider-linking-ui/`.
    2. Execute email-match confirmation flash + redirect (item 6); capture under `item-06-email-match-flash/`.
    3. If Playwright already covers subset, state delta explicitly in `Notes` or waiver compensation.
    4. Update rows **SEED-5**, **SEED-6**.
  </action>
  <acceptance_criteria>
    - `grep "| SEED-5 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"` exits 0.
    - `grep "| SEED-6 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"` exits 0.
    - At least one file exists matching `item-05-provider-linking-ui/*.{md,txt}` or `waiver.md` via shell glob from repo root.
    - At least one file exists matching `item-06-email-match-flash/*.{md,txt}` or `waiver.md`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep "| SEED-5 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"'</automated>
  </verify>
  <done>OAuth-adjacent UX flows have terminal evidence.</done>
</task>

<task type="execute">
  <name>Task 4: Items SEED-7 and SEED-8 — backup regenerate + clean-machine guide</name>
  <files>.planning/uat-evidence/v1.3.0/item-07-backup-regenerate,.planning/uat-evidence/v1.3.0/item-08-getting-started,.planning/v1.3-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-multi-factor-authentication/06-VERIFICATION.md
    - guides/introduction/getting-started.md
  </read_first>
  <action>
    1. **SEED-7:** Verify backup code regeneration handler in a real MFA session; if wiring gap confirmed, set row **Blocked** with GitHub issue URL in `Notes` per SEED-001 Notes (bug ticket path); else **Executed** with transcript.
    2. **SEED-8:** Fresh machine or clean clone: follow `guides/introduction/getting-started.md` end-to-end; record wall-clock time in `item-08-getting-started/timing.txt` and friction list in `notes.md`.
    3. Update rows **SEED-7**, **SEED-8**.
  </action>
  <acceptance_criteria>
    - `grep "| SEED-7 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"` exits 0.
    - `grep "| SEED-8 |" .planning/v1.3-HUMAN-UAT.md | grep -vq "| Pending |"` exits 0.
    - File `.planning/uat-evidence/v1.3.0/item-08-getting-started/timing.txt` exists and contains substring `minutes` or `min` (lowercase).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .planning/uat-evidence/v1.3.0/item-08-getting-started/timing.txt && grep -qi min .planning/uat-evidence/v1.3.0/item-08-getting-started/timing.txt'</automated>
  </verify>
  <done>MFA + DX read-through items closed or explicitly blocked with issue pointer.</done>
</task>

<task type="execute">
  <name>Task 5: Close REQUIREMENTS checkboxes + CHANGELOG pointer</name>
  <files>.planning/REQUIREMENTS.md,CHANGELOG.md,.planning/uat-evidence/v1.3.0/INDEX.md</files>
  <read_first>
    - .planning/v1.3-HUMAN-UAT.md
    - .planning/REQUIREMENTS.md
    - CHANGELOG.md
  </read_first>
  <action>
    1. Confirm all eight rows in `.planning/v1.3-HUMAN-UAT.md` are terminal (no `Pending`). If any **Blocked**, ensure **UAT-01** interpretation is documented (blocked GA — do not check UAT-01 until unblocked) **or** split milestone scope per maintainer decision — default: **do not** check UAT-01 while Blocked exists without PM waiver.
    2. When all rows are **Executed** or **Waived** (no Blocked, or Blocked explicitly accepted by maintainer in `INDEX.md` under heading `Blocked items accepted`): set **UAT-01** and **UAT-02** lines in `.planning/REQUIREMENTS.md` from `[ ]` to `[x]`.
    3. Add under `CHANGELOG.md` **Unreleased** section a bullet containing literal substring `v1.3-HUMAN-UAT.md` describing human UAT completion pointer.
    4. Finalize `INDEX.md` with `git rev-parse HEAD` value recorded for the sigra repo at time of run.
  </action>
  <acceptance_criteria>
    - If (and only if) no `| Pending |` remains in `.planning/v1.3-HUMAN-UAT.md`: `grep -Fq '[x] **UAT-01**' .planning/REQUIREMENTS.md` AND `grep -Fq '[x] **UAT-02**' .planning/REQUIREMENTS.md`.
    - `grep -q "v1.3-HUMAN-UAT.md" CHANGELOG.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc '! grep -q "| Pending |" .planning/v1.3-HUMAN-UAT.md && grep -Fq "[x] **UAT-01**" .planning/REQUIREMENTS.md && grep -Fq "[x] **UAT-02**" .planning/REQUIREMENTS.md'</automated>
  </verify>
  <done>Requirements traceability reflects completed human gate.</done>
</task>

</tasks>

<verification>
- `! grep -q "| Pending |" .planning/v1.3-HUMAN-UAT.md`
- `grep -Fq '[x] **UAT-01**' .planning/REQUIREMENTS.md` (after all rows terminal per Task 5 rules)
- Secret scan: `! rg -n "BEGIN PRIVATE KEY|AKIA[0-9A-Z]{16}" .planning/uat-evidence/` (exit 1 means clean)
</verification>

<success_criteria>
- **UAT-01** satisfied: eight SEED-001 items each **Executed** or **Waived** (or **Blocked** with explicit maintainer acceptance documented in `INDEX.md`), with evidence paths live under `.planning/uat-evidence/v1.3.0/`.
</success_criteria>
