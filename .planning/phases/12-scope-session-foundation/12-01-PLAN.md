---
phase: 12-scope-session-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/session.ex
  - lib/sigra/session_stores/ecto.ex
  - test/support/test_user_session.ex
  - test/sigra/session_test.exs
  - test/sigra/session_stores/ecto_test.exs
autonomous: true
requirements:
  - ORG-SCOPE-02
must_haves:
  truths:
    - "%Sigra.Session{} carries a nullable :active_organization_id field that round-trips through SessionStore.Ecto"
    - "Sigra.Test.UserSession schema has the matching :binary_id field so Mox-driven tests do not crash"
    - "Existing Session tests continue to pass with the additive field"
  artifacts:
    - path: "lib/sigra/session.ex"
      provides: ":active_organization_id field on defstruct + @type"
      contains: "active_organization_id: nil"
    - path: "lib/sigra/session_stores/ecto.ex"
      provides: "round-trip of :active_organization_id in create/3 + to_session/1"
      contains: "active_organization_id"
    - path: "test/support/test_user_session.ex"
      provides: ":active_organization_id :binary_id field on the Mox test schema"
      contains: "field :active_organization_id, :binary_id"
  key_links:
    - from: "lib/sigra/session_stores/ecto.ex create/3"
      to: "user_sessions.active_organization_id column"
      via: "Map.get on attrs map"
      pattern: "active_organization_id: Map.get"
    - from: "lib/sigra/session_stores/ecto.ex to_session/1"
      to: "%Sigra.Session{}"
      via: "struct literal"
      pattern: "active_organization_id: .*record"
---

<objective>
Extend the `Sigra.Session` library struct with a first-class nullable `:active_organization_id` field and make `Sigra.SessionStores.Ecto` round-trip the field through the `user_sessions` row. This is the library-side write/read path that ORG-SCOPE-02 stands on.

Purpose: Sigra v1.1 is the Organizations milestone — the library is org-aware. Phase 14 plugs and Phase 16 LiveViews will pattern-match on `%Sigra.Session{active_organization_id: ...}`. This plan delivers the field with zero business logic so downstream phases can hydrate it.

Output: Updated session struct, updated Ecto session store, updated Mox test schema, and extended unit tests proving round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-scope-session-foundation/12-CONTEXT.md
@.planning/phases/12-scope-session-foundation/12-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Verified shapes from RESEARCH.md §Mechanical Reference 1, 3, 4 -->

From lib/sigra/session.ex (current shape — verified lines 48-78):
```elixir
@type t :: %__MODULE__{
        id: term(),
        user_id: term(),
        token: binary() | nil,
        hashed_token: binary(),
        type: session_type(),
        ip: String.t() | nil,
        user_agent: String.t() | nil,
        parsed_ua: map() | nil,
        geo_city: String.t() | nil,
        geo_country_code: String.t() | nil,
        last_active_at: DateTime.t() | nil,
        sudo_at: DateTime.t() | nil,
        inserted_at: DateTime.t() | nil
      }

defstruct [
  :id,
  :user_id,
  :token,
  :hashed_token,
  type: :standard,
  ip: nil,
  user_agent: nil,
  parsed_ua: nil,
  geo_city: nil,
  geo_country_code: nil,
  last_active_at: nil,
  sudo_at: nil,
  inserted_at: nil
]
```

From lib/sigra/session_stores/ecto.ex (verified):
- `create/3` (lines 22-49) builds an `attrs` map for `%session_schema{}` insert
- `to_session/1` (lines 146-168) constructs `%Sigra.Session{}` from a fetched record
- Pattern for optional fields uses `Map.get(record, :field)` / `Map.get(metadata, :field)`

From test/support/test_user_session.ex (verified — 22 lines):
- Ecto schema "user_sessions" used by `Sigra.SessionStores.EctoTest` with Mox
- Currently does NOT have `:active_organization_id`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend %Sigra.Session{} struct with :active_organization_id and update tests RED-first</name>
  <files>lib/sigra/session.ex, test/sigra/session_test.exs</files>
  <read_first>
    - lib/sigra/session.ex (full file — 78 lines)
    - test/sigra/session_test.exs (full file)
    - .planning/phases/12-scope-session-foundation/12-RESEARCH.md §Mechanical Reference /1 + Pitfall 1
  </read_first>
  <behavior>
    - Test 1 (RED): `%Sigra.Session{}` literal can be constructed with `active_organization_id: <uuid>` and the field is reflected on the resulting struct
    - Test 2 (RED): `%Sigra.Session{}` default has `active_organization_id: nil`
    - Test 3 (RED): `@type t` includes `active_organization_id: binary() | nil` (verified by Dialyzer-clean compilation under `mix compile --warnings-as-errors`)
  </behavior>
  <action>
    Step 1 — Add the failing tests FIRST in `test/sigra/session_test.exs`:
    - Add an assertion to the existing struct-construction test (or add a new test in the same describe block) verifying:
      ```elixir
      session = %Sigra.Session{active_organization_id: "0190b3a4-1234-7000-8000-000000000000"}
      assert session.active_organization_id == "0190b3a4-1234-7000-8000-000000000000"
      ```
    - Add an assertion that a default-constructed `%Sigra.Session{}` has `active_organization_id: nil`.
    Run `mix test test/sigra/session_test.exs` — both new assertions MUST fail with `KeyError` or `unknown key`.

    Step 2 — Edit `lib/sigra/session.ex`:
    - In the `@type t` block (around line 48-66), add `active_organization_id: binary() | nil,` immediately BEFORE the `inserted_at:` line. Per D-04 the field sits between `:sudo_at` and `:inserted_at`.
    - In the `defstruct` block (around line 67-78), add `active_organization_id: nil,` immediately BEFORE the `inserted_at: nil` line. CRITICAL — use the keyword form `active_organization_id: nil` (not a bare atom). The first four entries in defstruct are bare atoms; everything after `type: :standard` is keyword form. Inserting a bare atom here would trigger Pitfall 1 ("atoms must precede keyword pairs in defstruct").
    - In the `## Fields` doc block (around lines 29-41), add a one-line entry: `* `:active_organization_id` — Active organization the session is currently scoped to. Nullable; populated by Phase 14 plugs.`

    Step 3 — Run `mix test test/sigra/session_test.exs` (must pass) and `mix compile --warnings-as-errors` (must be clean).

    Per D-04 (CONTEXT.md). Do NOT add a `put_active_organization_id/2` setter — D-05 explicitly defers it.
  </action>
  <verify>
    <automated>mix test test/sigra/session_test.exs --color && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "active_organization_id: binary() | nil" lib/sigra/session.ex` returns 1 line in the @type block
    - `grep -n "active_organization_id: nil" lib/sigra/session.ex` returns 1 line in the defstruct block
    - `mix test test/sigra/session_test.exs` exits 0 with the new assertions present
    - `mix compile --warnings-as-errors` exits 0
    - `lib/sigra/session.ex` has no other diff outside the three insertions described above
  </acceptance_criteria>
  <done>
    %Sigra.Session{} carries the new field, default is nil, tests prove construction with explicit value, library compiles warning-free.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip :active_organization_id through Sigra.SessionStores.Ecto (Mox + test schema)</name>
  <files>test/support/test_user_session.ex, lib/sigra/session_stores/ecto.ex, test/sigra/session_stores/ecto_test.exs</files>
  <read_first>
    - test/support/test_user_session.ex (full file — 22 lines)
    - lib/sigra/session_stores/ecto.ex (full file — 169 lines, focus on `create/3` lines 22-49 and `to_session/1` lines 146-168)
    - test/sigra/session_stores/ecto_test.exs (full file — focus on the create/3 + fetch tests around line 56-103)
    - .planning/phases/12-scope-session-foundation/12-RESEARCH.md §Mechanical Reference /3 + /4
  </read_first>
  <behavior>
    - Test 1 (RED): Calling `Sigra.SessionStores.Ecto.create/3` with `%{active_organization_id: <uuid>}` in the metadata map results in a Repo insert whose attrs include `active_organization_id: <uuid>`
    - Test 2 (RED): `to_session/1` on a fetched `%Sigra.Test.UserSession{active_organization_id: <uuid>}` record returns a `%Sigra.Session{}` whose `active_organization_id` matches the input
    - Test 3 (RED): Default path — when no `:active_organization_id` is supplied, both `create/3` and `to_session/1` produce nil (no crash, no KeyError)
  </behavior>
  <action>
    Step 1 — Update the Mox test schema FIRST so tests can reference the field. Edit `test/support/test_user_session.ex`:
    - Inside the `schema "user_sessions" do` block, immediately after the `field :sudo_at, :utc_datetime_usec` line, add:
      ```elixir
      field :active_organization_id, :binary_id
      ```

    Step 2 — Add the failing tests to `test/sigra/session_stores/ecto_test.exs`:
    - In the `create/3` describe block, add a test that calls `Sigra.SessionStores.Ecto.create(...)` passing metadata containing `:active_organization_id`, sets up Mox to expect the Repo `insert` to receive a struct/changeset with `active_organization_id: <uuid>`, and asserts the Mox expectation is satisfied. Use the existing `Sigra.MockRepo` patterns from neighboring tests in the same file.
    - In the `fetch/2` (or equivalent reload) describe block, add a test where the Mox stub returns a `%Sigra.Test.UserSession{active_organization_id: <uuid>}` record and asserts the resulting `%Sigra.Session{}` has the same value. Use `Ecto.UUID.generate()` for the test UUID.
    - Add a "default nil" test asserting that omitting `:active_organization_id` from the input leaves `active_organization_id: nil` on the resulting Session struct.

    Run `mix test test/sigra/session_stores/ecto_test.exs` — new tests MUST fail.

    Step 3 — Edit `lib/sigra/session_stores/ecto.ex`:
    - In `create/3` (lines 22-49), inside the `attrs = %{...}` map, add a new key `active_organization_id: Map.get(metadata, :active_organization_id)`. Place it adjacent to other optional metadata fields (e.g., near `geo_city`, `geo_country_code`) for stylistic consistency.
    - In `to_session/1` (lines 146-168), inside the `%Sigra.Session{...}` literal, add `active_organization_id: Map.get(record, :active_organization_id)`. Place it in the same relative position to match the struct field ordering from Task 1.

    Step 4 — Run `mix test test/sigra/session_stores/ecto_test.exs` (must pass) and the broader `mix test test/sigra/session_stores/` (no regressions).

    Per D-06 — no behaviour callback signature changes. Per D-05 — no dedicated update function; `update_activity/3` and `update_sudo/3` stay untouched.
  </action>
  <verify>
    <automated>mix test test/sigra/session_test.exs test/sigra/session_stores/ecto_test.exs --color && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "field :active_organization_id, :binary_id" test/support/test_user_session.ex` returns exactly 1 line
    - `grep -n "active_organization_id: Map.get(metadata" lib/sigra/session_stores/ecto.ex` returns exactly 1 line
    - `grep -n "active_organization_id: Map.get(record" lib/sigra/session_stores/ecto.ex` returns exactly 1 line
    - `mix test test/sigra/session_stores/ecto_test.exs` exits 0 and contains at least 3 new assertions referencing `active_organization_id`
    - `mix test test/sigra/session_stores/` exits 0 (no regressions in sibling stores)
    - `mix compile --warnings-as-errors` exits 0
    - `lib/sigra/session_stores/ecto.ex` has NO changes to `update_activity/3` or `update_sudo/3` functions (verify with focused diff)
  </acceptance_criteria>
  <done>
    SessionStore.Ecto round-trips :active_organization_id through both write and read paths; Mox test schema supports the field; default-nil case works without crashing; sibling tests stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 12 changes do not introduce new trust boundaries. The new field is a nullable pointer column on an already-authenticated session row. No new request entry points, no new tokens, no new auth flows.

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 12 is purely additive data shape. Existing session-cookie boundary unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | %Sigra.Session{} :active_organization_id field | accept | Field is nullable and unindexed in Phase 12; no auth decision is made on its value in v1.1. Phase 14 LoadActiveOrganization plug is responsible for membership re-verification before any access decision. |
| T-12-02 | Information Disclosure | SessionStore.Ecto round-trip | accept | The column carries an opaque `binary_id` pointer; nothing PII. Read path is identical to existing optional fields (`geo_city`, `geo_country_code`) which already use the same Map.get pattern. |
| T-12-03 | Denial of Service | KeyError on nil read of new field | mitigate | All access paths use `Map.get/2` with default nil. Test 3 in Task 2 explicitly proves the default-nil case; absence would produce a 500. |
</threat_model>

<verification>
- `mix test test/sigra/session_test.exs test/sigra/session_stores/ecto_test.exs` exits 0
- `mix test test/sigra/session_stores/` exits 0 (sibling stores not regressed)
- `mix compile --warnings-as-errors` exits 0
- New field appears in `defstruct`, `@type t`, `## Fields` doc, `Sigra.Test.UserSession` schema, `create/3` attrs, `to_session/1` struct literal — total 6 modification sites.
</verification>

<success_criteria>
- ORG-SCOPE-02 (library write/read half) is satisfied: `%Sigra.Session{}` carries `:active_organization_id` and round-trips through `Sigra.SessionStores.Ecto`.
- VALIDATION.md sampling: `mix test test/sigra/session_test.exs test/sigra/session_stores/ecto_test.exs` runs in <5 seconds (well under 12s feedback latency).
- Pitfall 1 (defstruct atom-keyword ordering) avoided: keyword form used for new field.
- Phase 14 will be able to add a hydration plug that calls `%{session | active_organization_id: org_id}` without further library changes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-scope-session-foundation/12-01-SUMMARY.md` capturing:
- Files modified (6 sites)
- Test count delta (3+ new assertions)
- Confirmation that no `update_*` functions in SessionStore.Ecto were touched
- Confirmation that `Sigra.SessionStore` behaviour callbacks are unchanged
</output>
