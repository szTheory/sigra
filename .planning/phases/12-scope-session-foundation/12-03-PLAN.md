---
phase: 12-scope-session-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/core/scope.ex
  - priv/templates/sigra.install/core/user_session.ex
  - test/sigra/install/scope_template_invariants_test.exs
  - UPGRADE-v1.2.md
autonomous: true
requirements:
  - ORG-SCOPE-01
  - ORG-SCOPE-02
must_haves:
  truths:
    - "Generated %Scope{} defstruct has 4 fields: :user, :active_organization, :membership, :impersonating_from — all default nil"
    - "Scope template's for_user/1 and new/1 remain arity-1 (no signature change)"
    - "Generated user_session.ex Ecto schema has field :active_organization_id, :binary_id"
    - "Library-side invariant test fails LOUDLY if anyone removes :impersonating_from from the scope template"
    - "UPGRADE-v1.2.md exists at project root naming the reserved-field contract"
  artifacts:
    - path: "priv/templates/sigra.install/core/scope.ex"
      provides: "extended defstruct + @type + reserved-field doc comment"
      contains: "impersonating_from: nil"
    - path: "priv/templates/sigra.install/core/user_session.ex"
      provides: "active_organization_id field on generated Ecto schema"
      contains: "field :active_organization_id, :binary_id"
    - path: "test/sigra/install/scope_template_invariants_test.exs"
      provides: "two-assertion reserved-field invariant test (D-11)"
      contains: "impersonating_from"
    - path: "UPGRADE-v1.2.md"
      provides: "skeleton doc cited by D-11 failure messages"
      contains: "Reserved fields in v1.1"
  key_links:
    - from: "scope.ex template defstruct"
      to: "downstream pattern matching in user_auth.ex (Phase 14+)"
      via: "additive defstruct fields"
      pattern: "active_organization: nil.*membership: nil.*impersonating_from: nil"
    - from: "scope_template_invariants_test.exs"
      to: "UPGRADE-v1.2.md"
      via: "failure-message reference"
      pattern: "UPGRADE-v1.2.md"
---

<objective>
Extend the generated `Scope` and `UserSession` templates with the additive fields ORG-SCOPE-01 and ORG-SCOPE-02 require, lock the reserved `:impersonating_from` field with a library-side invariant test, and create the `UPGRADE-v1.2.md` skeleton doc the test cites.

Purpose: Phase 14 plugs and Phase 16 LiveViews pattern-match on `%Scope{active_organization: org, membership: m, impersonating_from: from}`. v1.2 impersonation will populate `:impersonating_from` purely additively if and only if the field exists in v1.1 generated code. The invariant test makes the reserved-field contract enforceable, not aspirational.

Output: Updated scope template, updated user_session template, new invariant test, new upgrade doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-scope-session-foundation/12-CONTEXT.md
@.planning/phases/12-scope-session-foundation/12-RESEARCH.md
@CLAUDE.md

<interfaces>
Verified shapes from RESEARCH.md sections 7, 8, 13, 14:

priv/templates/sigra.install/core/scope.ex (current — 38 lines, verified):
- defmodule wraps a Scope struct using `<%= context_module %>` and `<%= schema_alias %>` PLAIN bindings (NOT @assigns — see Pitfall 6)
- Current defstruct: `defstruct user: nil`
- Current @type t: `@type t :: %__MODULE__{user: %<%= schema_alias %>{} | nil}`
- for_user/1 and new/1 are arity-1 — D-08 keeps them arity-1
- alias line: `alias <%= context_module %>.<%= schema_alias %>`

priv/templates/sigra.install/core/user_session.ex (current — 35 lines, verified):
- schema "user_sessions" block contains fields ending with `field :sudo_at, :utc_datetime_usec`
- followed by `belongs_to :user, <%= context_module %>.<%= schema_alias %>`
- followed by `timestamps(...)`
- New field inserts between :sudo_at and belongs_to

EEx render harness precedent: test/sigra/install/generator_reset_test.exs lines 165-174 already evaluates EEx templates in tests (assigns: form). Phase 12's invariant test uses the plain bindings form because the scope template uses plain bindings.

Code.compile_string precedent: rendered scope source must compile after a dummy `TestApp.Accounts.User` module is pre-compiled (see Pitfall 7).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend scope.ex and user_session.ex generator templates with additive fields</name>
  <files>priv/templates/sigra.install/core/scope.ex, priv/templates/sigra.install/core/user_session.ex</files>
  <read_first>
    - priv/templates/sigra.install/core/scope.ex (full file — 38 lines)
    - priv/templates/sigra.install/core/user_session.ex (full file — 35 lines)
    - .planning/phases/12-scope-session-foundation/12-RESEARCH.md sections "Mechanical Reference /7" and "Mechanical Reference /8"
  </read_first>
  <behavior>
    - Rendered Scope module has defstruct with 4 fields all defaulting nil: user, active_organization, membership, impersonating_from
    - Rendered Scope module has @type t listing all 4 fields per D-09 / D-10 typespecs
    - for_user/1 and new/1 remain arity-1 (D-08 — DO NOT modify their signatures)
    - Scope template @moduledoc paragraph names the reserved field and cites UPGRADE-v1.2.md
    - Rendered UserSession schema includes `field :active_organization_id, :binary_id` between :sudo_at and belongs_to
  </behavior>
  <action>
    Step 1 — Edit `priv/templates/sigra.install/core/scope.ex`:

    A. Inside the `@moduledoc """ ... """` block, append a new paragraph (verbatim):

        ## Reserved fields

        `:impersonating_from` is reserved for v1.2 impersonation support and must
        not be removed. See `UPGRADE-v1.2.md` at the project root for the contract.

    B. Replace the existing `defstruct user: nil` line with this exact 5-line block (the comment line is part of the D-11 doc-comment enforcement):

        # Reserved for v1.2 impersonation. Do not remove — see UPGRADE-v1.2.md.
        defstruct user: nil,
                  active_organization: nil,
                  membership: nil,
                  impersonating_from: nil

    C. Replace the existing `@type t :: %__MODULE__{user: %<%= schema_alias %>{} | nil}` line with this exact block:

        @type t :: %__MODULE__{
                user: %<%= schema_alias %>{} | nil,
                active_organization: struct() | nil,
                membership: struct() | nil,
                impersonating_from: %<%= schema_alias %>{} | nil
              }

    Per D-09 — `struct() | nil` for `:active_organization` and `:membership` (Phase 13 will tighten to `%<%= context_module %>.Organization{} | nil` etc. when those modules exist). Per D-10 — `:impersonating_from` is `%<%= schema_alias %>{} | nil`. Per D-08 — for_user/1 and new/1 stay arity-1; do NOT touch them.

    Step 2 — Edit `priv/templates/sigra.install/core/user_session.ex`:

    Inside the `schema "user_sessions" do` block, immediately after the `field :sudo_at, :utc_datetime_usec` line, add a new line:

        field :active_organization_id, :binary_id

    Placement is before the `belongs_to :user, ...` line. Use `:binary_id` unconditionally (per D-03 — no FK in Phase 12, but the type is binary_id so Phase 14/18 can add the FK cleanly).

    Step 3 — Smoke render both templates via `mix run -e` to confirm they evaluate cleanly with EEx:

        mix run -e 'IO.puts EEx.eval_file("priv/templates/sigra.install/core/scope.ex", context_module: "TestApp.Accounts", schema_alias: "User")'

    Expect rendered output to contain literal `impersonating_from: nil`. Repeat for user_session.ex with `binary_id: true` in the bindings.

    Step 4 — Run `mix test test/sigra/install/` to confirm no neighboring template test breaks. The dedicated invariant test for these changes is built in Task 2.

    Pitfall 6 reminder: the scope template uses PLAIN bindings (`<%= context_module %>`), NOT `<%= @context_module %>`. Do NOT switch it to @assigns form.
  </action>
  <verify>
    <automated>elixir -e 'rendered = EEx.eval_file("priv/templates/sigra.install/core/scope.ex", context_module: "TestApp.Accounts", schema_alias: "User"); raise "missing impersonating_from" unless rendered =~ "impersonating_from: nil"; raise "missing active_organization" unless rendered =~ "active_organization: nil"; raise "missing membership" unless rendered =~ "membership: nil"; raise "for_user/1 lost" unless rendered =~ "def for_user("; rendered2 = EEx.eval_file("priv/templates/sigra.install/core/user_session.ex", context_module: "TestApp.Accounts", schema_alias: "User", binary_id: true); raise "missing active_organization_id field" unless rendered2 =~ "field :active_organization_id, :binary_id"; IO.puts("OK")' && mix test test/sigra/install/ --color</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "impersonating_from: nil" priv/templates/sigra.install/core/scope.ex` returns at least 1 line in the defstruct block
    - `grep -n "impersonating_from: %<%= schema_alias %>{}" priv/templates/sigra.install/core/scope.ex` returns 1 line in the @type block
    - `grep -n "active_organization: nil" priv/templates/sigra.install/core/scope.ex` returns 1 line
    - `grep -n "membership: nil" priv/templates/sigra.install/core/scope.ex` returns 1 line
    - `grep -n "Reserved for v1.2 impersonation" priv/templates/sigra.install/core/scope.ex` returns 1 line (the inline comment)
    - `grep -n "Reserved fields" priv/templates/sigra.install/core/scope.ex` returns 1 line (the @moduledoc paragraph header)
    - `grep -n "def for_user" priv/templates/sigra.install/core/scope.ex` returns 2 lines (clause for user struct + clause for nil) — both arity-1
    - `grep -n "def new" priv/templates/sigra.install/core/scope.ex` returns 2 lines — both arity-1
    - `grep -n "field :active_organization_id, :binary_id" priv/templates/sigra.install/core/user_session.ex` returns exactly 1 line
    - `mix test test/sigra/install/` exits 0
  </acceptance_criteria>
  <done>
    Both templates rendered, the four scope fields land in deterministic order, the reserved-field comment + moduledoc paragraph are present, user_session has the new binary_id field, and the existing install test directory stays green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scope template invariant test (D-11) + UPGRADE-v1.2.md skeleton (D-12)</name>
  <files>test/sigra/install/scope_template_invariants_test.exs, UPGRADE-v1.2.md</files>
  <read_first>
    - test/sigra/install/generator_reset_test.exs lines 150-200 (EEx eval test pattern precedent)
    - priv/templates/sigra.install/core/scope.ex (the file under invariant test — must already have Task 1 changes)
    - .planning/phases/12-scope-session-foundation/12-RESEARCH.md sections "Mechanical Reference /13" + "Mechanical Reference /14" + Pitfall 6 + Pitfall 7
  </read_first>
  <behavior>
    - Test 1 (source-level grep): reads the scope template file and asserts the source contains `impersonating_from: nil` somewhere in the defstruct block. Failure message names UPGRADE-v1.2.md.
    - Test 2 (compile-and-introspect): EEx-evaluates the template with a plain-binding keyword list, pre-compiles a dummy `TestApp.Accounts.User` module, then `Code.compile_string/1` on the rendered source and asserts `:impersonating_from in Map.keys(rendered_struct.__struct__())`. Failure message names UPGRADE-v1.2.md.
    - UPGRADE-v1.2.md exists at project root with the three-section skeleton from RESEARCH.md /14
    - The compile path may be brittle across Elixir versions — if compile-and-introspect proves fragile during execution, fall back to a second source-level assertion documented in a comment in the test file
  </behavior>
  <action>
    Step 1 — Create `UPGRADE-v1.2.md` at project root with this skeleton (verbatim):

        # Upgrading to Sigra v1.2

        > Status: skeleton. v1.2 has not shipped. This document defines the
        > forward contract that v1.1 generated code must respect so that
        > the v1.2 upgrade is purely additive.

        ## Reserved fields in v1.1

        Sigra v1.1 reserves the following fields in generated code so the
        v1.2 upgrade can be purely additive:

        - `%<YourApp>.Accounts.Scope{impersonating_from: nil}` — populated
          by v1.2 `Sigra.Plug.Impersonation`. Type: `%User{} | nil`.

        Do not remove these fields from your generated `scope.ex`. If you
        do, v1.2 will fail to compile against your generated `user_auth.ex`
        and associated plugs, and the library-side invariant test
        (`Sigra.Install.ScopeTemplateInvariantsTest`) will turn red on your
        next CI run.

        ## v1.2 population contract

        (Filled in when v1.2 ships.)

        ## If you need to remove a reserved field

        (Filled in when v1.2 ships.)

    Step 2 — Create `test/sigra/install/scope_template_invariants_test.exs` with a two-assertion test module under namespace `Sigra.Install.ScopeTemplateInvariantsTest` (CD-03 — planner discretion on naming, this is the chosen name).

    Test module skeleton (write this exactly):

        defmodule Sigra.Install.ScopeTemplateInvariantsTest do
          use ExUnit.Case, async: true

          @template_path Path.expand(
            "../../../priv/templates/sigra.install/core/scope.ex",
            __DIR__
          )

          describe "reserved :impersonating_from field (D-11)" do
            test "source-level grep — template defstruct mentions impersonating_from: nil" do
              source = File.read!(@template_path)

              assert source =~ ~r/impersonating_from:\s*nil/,
                """
                The generated Scope template at #{@template_path} must reserve
                the :impersonating_from field for v1.2 impersonation support.

                See UPGRADE-v1.2.md at the project root for the contract. If
                you intentionally removed this field, you are about to break
                the v1.1 -> v1.2 upgrade path for every Sigra-generated app.
                """
            end

            test "compile-and-introspect — rendered Scope module struct has :impersonating_from key" do
              # Pre-compile a dummy User module so the rendered scope can resolve
              # `alias TestApp.Accounts.User` and the `%User{}` pattern matches.
              # See Pitfall 7 in 12-RESEARCH.md.
              Code.compile_string("defmodule TestApp.Accounts.User, do: defstruct([:id])")

              # The scope template uses PLAIN bindings (`<%= context_module %>`),
              # NOT @assigns. See Pitfall 6 in 12-RESEARCH.md. Pass a plain
              # keyword list as the second argument to EEx.eval_file/2.
              rendered =
                EEx.eval_file(@template_path,
                  context_module: "TestApp.Accounts",
                  schema_alias: "User"
                )

              # Compile the rendered scope module and introspect its struct keys.
              [{mod, _bytecode} | _] = Code.compile_string(rendered)
              struct_keys = mod.__struct__() |> Map.keys()

              assert :impersonating_from in struct_keys,
                """
                The rendered Scope struct must contain :impersonating_from.
                Got keys: #{inspect(struct_keys)}.

                This field is reserved for v1.2 impersonation. See
                UPGRADE-v1.2.md at the project root for the contract.
                """
            end
          end
        end

    Per D-11 — both assertions must name UPGRADE-v1.2.md in their failure messages so a future contributor immediately understands what they are breaking.

    Step 3 — Run the test:

        mix test test/sigra/install/scope_template_invariants_test.exs

    Both assertions must PASS (the template was already updated in Task 1).

    Step 4 — Fallback path (only if Step 3 fails on the compile-and-introspect test): if `Code.compile_string` proves fragile (e.g., redefining the User module across test runs), replace Test 2 with a second source-level assertion: `assert source =~ ~r/@type t.*impersonating_from: %.*\{\} \| nil/s` and add a comment in the test explaining why the compile path was abandoned. Document the decision in the SUMMARY for downstream phases. Try the compile path FIRST.

    Step 5 — Run `mix test test/sigra/install/` (full directory) and `mix compile --warnings-as-errors` to confirm zero regressions and zero warnings.
  </action>
  <verify>
    <automated>mix test test/sigra/install/scope_template_invariants_test.exs --color && mix test test/sigra/install/ --color && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f UPGRADE-v1.2.md` succeeds
    - `grep -c "Reserved fields in v1.1" UPGRADE-v1.2.md` returns 1
    - `grep -c "impersonating_from" UPGRADE-v1.2.md` returns ≥1
    - `test -f test/sigra/install/scope_template_invariants_test.exs` succeeds
    - `grep -c "UPGRADE-v1.2.md" test/sigra/install/scope_template_invariants_test.exs` returns ≥2 (one in each test's failure message)
    - `grep -c "describe " test/sigra/install/scope_template_invariants_test.exs` returns 1
    - `grep -c "test \"" test/sigra/install/scope_template_invariants_test.exs` returns 2
    - `mix test test/sigra/install/scope_template_invariants_test.exs` exits 0 with 2 tests passing
    - `mix test test/sigra/install/` exits 0
    - `mix compile --warnings-as-errors` exits 0
    - SANITY CHECK: temporarily remove `impersonating_from: nil` from scope.ex (locally), re-run the invariant test, confirm BOTH assertions fail with the UPGRADE-v1.2.md citation in the message; then RESTORE the field. (Manual verification step — note in SUMMARY whether performed.)
  </acceptance_criteria>
  <done>
    UPGRADE-v1.2.md skeleton exists; scope template invariant test exists with two assertions both naming UPGRADE-v1.2.md in failure messages; both assertions pass against the Task 1 template state; library compiles warning-free.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 12 changes do not introduce new trust boundaries. The Scope template is generated into host apps and becomes their code; the invariant test runs at library build time only.

| Boundary | Description |
|----------|-------------|
| (none new) | Generator-time and test-time only. No request entry points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | Reserved :impersonating_from field | mitigate | This is the load-bearing security invariant of Phase 12. If a future contributor silently removes :impersonating_from from the scope template, v1.2 impersonation isolation breaks: the impersonator's identity can no longer be tracked through the scope, opening a path where impersonator actions are misattributed to the target user (audit confusion / forensics failure). The two-assertion library-side invariant test (D-11) catches the mutation at the template layer before it reaches a release. Doc comment + @moduledoc paragraph (D-11 mechanism A) make the reservation visible to humans reading the template. |
| T-12-08 | Repudiation | Removal of reserved field via refactoring | mitigate | Test 2 (compile-and-introspect) catches structural moves that the regex in Test 1 might miss (e.g., field renamed, field moved out of defstruct). Both failure messages name UPGRADE-v1.2.md so the contributor cannot claim ignorance of the contract. |
| T-12-09 | Information Disclosure | Generated user_session.ex column type | accept | `:binary_id` is opaque, no PII. Same trust profile as the existing `:user_id` foreign key column. |
</threat_model>

<verification>
- `mix test test/sigra/install/scope_template_invariants_test.exs` exits 0 with 2 tests
- `mix test test/sigra/install/` exits 0
- `mix compile --warnings-as-errors` exits 0
- Both EEx templates render cleanly via `EEx.eval_file/2`
- UPGRADE-v1.2.md exists at project root with the three-section skeleton
- Sanity-check (manual): removing the field locally turns BOTH assertions red with UPGRADE-v1.2.md cited
</verification>

<success_criteria>
- ORG-SCOPE-01 (template half) is satisfied: `%Scope{}` defstruct carries `:active_organization`, `:membership`, and reserved `:impersonating_from`.
- ORG-SCOPE-02 (template half) is satisfied: generated `user_session.ex` carries `field :active_organization_id, :binary_id`.
- D-11 reserved-field discipline mechanism is operational: doc-comment (A) + library-side invariant test (C) are both in place.
- D-12 UPGRADE-v1.2.md skeleton exists at project root and is the single citation target for D-11 failure messages.
- VALIDATION.md sampling: `mix test test/sigra/install/scope_template_invariants_test.exs` runs in <2 seconds.
- A future v1.2 phase can remove the "skeleton" disclaimer from UPGRADE-v1.2.md and fill in the two empty sections without touching the test or templates.
</success_criteria>

<output>
After completion, create `.planning/phases/12-scope-session-foundation/12-03-SUMMARY.md` capturing:
- Files modified / created (4 sites)
- Whether the compile-and-introspect path worked or fell back to a second source grep (with rationale)
- Result of the manual sanity-check (temporarily delete the field, confirm both assertions fail, restore)
- Confirmation that UPGRADE-v1.2.md is at project root (not under .planning/)
</output>
