---
phase: 212-v1-42-integration-merge-canary-reconciliation-gate-the-perso
plan: 04
type: execute
wave: 3
depends_on:
  - 212-01
  - 212-02
  - 212-03
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - test/example/priv/playwright/snapshot-allowlist
  - .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md
autonomous: false
requirements:
  - SC-4
  - GATE-01
  - FLOW-01
  - GATE-02
user_setup: []

must_haves:
  truths:
    - "The three gates are green on PR #63 (fast_checks snapshot guard passes, admin_behavior runs the 3 flow specs green, generated_admin_playwright_smoke RUNS and PASSES) and ci-gate is green"
    - "Local main is pushed to origin/main and PR #63 (ship/v1.42-ci-gate-remediation → main) is taken out of DRAFT and MERGED"
    - "ROADMAP v1.42 is flipped to shipped and STATE reconciled ONLY AFTER the merge lands — never before (D-10)"
    - "Post-merge: the checkpoint allowlist is reset to empty (D-03) and the stale phase-205 baseline-debt todo is deleted (D-11)"
  artifacts:
    - "origin/main containing the merged v1.42 backlog (PR #63 merged)"
    - ".planning/ROADMAP.md v1.42 marked shipped + Phase 212 plans checked off (post-merge)"
    - ".planning/STATE.md reconciled to reflect merged/shipped v1.42 (post-merge)"
    - "test/example/priv/playwright/snapshot-allowlist reset to comment-only steady state (post-merge, D-03)"
  key_links:
    - "gate greenness (Plans 01/02/03) ↔ PR #63 fast_checks + ci-gate on GitHub — merge is blocked until all required lanes are green"
    - "merge-landed ↔ the shipped-flag flip and allowlist reset — strict ordering: NOTHING flips or resets before merge (D-10/D-03)"
---

<objective>
Close SC-4 (merge + honest status) under STRICT ORDERING (D-10). The v1.42 milestone was
prematurely marked "shipped 2026-07-01" while local main is ~385 commits ahead of
origin/main (unpushed) and PR #63 is OPEN + DRAFT + failing. This plan lands the three
gates, merges, and only THEN reconciles status honestly.

Ordering (D-10) — do NOT reorder:
  1. Confirm the three gates (Plans 01/02/03) are green on PR #63.
  2. Push local `main` to origin (the ~385-commit backlog).
  3. Take PR #63 (ship/v1.42-ci-gate-remediation → main) out of DRAFT.
  4. Drive `fast_checks` + `ci-gate` green on GitHub (re-run the accepted-known NoopTest
     shard flake alone if it flares — Phase 211 D-05).
  5. Merge PR #63 to origin/main.
  6. ONLY THEN: flip ROADMAP v1.42 → shipped and reconcile STATE.
  7. Post-merge cleanup: reset the checkpoint allowlist to empty (D-03); delete the stale
     phase-205 baseline-debt todo (D-11); optionally backfill a Phase 208 VERIFICATION.md
     traceability pointer (a pointer, not re-verification).

Do NOT flip "shipped" before the merge lands (the premature-flag mistake the audit calls
out). Do NOT reset the allowlist before merge (its 4 slugs must be present in the merged
diff so the drift is reviewable; reset is a post-merge steady-state discipline).

Purpose: turn "every phase verified vs HEAD" into "the milestone is genuinely integrated,
gated, merged, and honestly flagged."
Output: merged origin/main + reconciled ROADMAP/STATE + swept bookkeeping.

NOTE (autonomous: false): several steps here are inherently human/interactive on GitHub —
pushing, taking a PR out of draft, watching CI go green, and merging. The executor performs
the git/gh operations it can and PAUSES at the human checkpoints below where confirmation
of live GitHub state is required.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-CONTEXT.md
@.planning/v1.42-MILESTONE-AUDIT.md
</context>

<artifacts_this_phase_produces>
This plan creates/modifies:
- `origin/main` — receives the pushed local backlog and the merged PR #63 (the v1.42
  ADMIN-DS-ELEVATION + CI-gate remediation work).
- `.planning/ROADMAP.md` — v1.42 flipped to shipped (post-merge) and the Phase 212 plan
  checklist marked complete.
- `.planning/STATE.md` — milestone/status/position reconciled to merged+shipped v1.42
  (post-merge).
- `test/example/priv/playwright/snapshot-allowlist` — reset to comment-only steady state
  (the 4 slug lines from Plan 01 removed) AFTER merge (D-03).
- `.planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md` — DELETED
  (its board-cfg PNGs now exist; 84 board files present) (D-11).
- (optional) `.planning/phases/208-.../208-VERIFICATION.md` — a traceability pointer noting
  Phase 208's work is covered by 208.1 + 210-02 (pointer, not re-verification).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm gates green locally + push local main to origin (D-10 steps 1-2)</name>
  <read_first>
    - .planning/phases/212-.../212-01-SUMMARY.md, 212-02-SUMMARY.md, 212-03-SUMMARY.md (confirm all three gate plans completed and their local verifications passed)
    - .planning/v1.42-MILESTONE-AUDIT.md §81/§158 (the premature-shipped-flag finding this plan must not repeat)
  </read_first>
  <files>(git operations — no tracked file edits in this task)</files>
  <action>
    First re-confirm the three gates locally, in order:
      - `git fetch origin main` then `bash scripts/ci/snapshot-canary-guard.sh --base origin/main`
        must exit 0 (GATE-01).
      - `grep` confirms the 3 admin-flow specs are in ci.yml's admin_behavior step (FLOW-01)
        and the generated_admin_playwright_smoke `if:` is branch-scoped (GATE-02).
    Then push the local backlog: `git push origin main`. This publishes the ~385-commit
    (currently ~387) unpushed backlog. Do NOT force-push. If the push is rejected
    (non-fast-forward or protected-branch), STOP and surface the rejection — do not rewrite
    history. This task does NOT flip any status flag and does NOT touch the PR draft state.
  </action>
  <verify>
    <automated>git fetch origin main -q; bash scripts/ci/snapshot-canary-guard.sh --base origin/main; echo "canary-exit=$?"; grep -q 'tests/admin-flow-platform-admin.spec.ts' .github/workflows/ci.yml && grep -q "head_ref == 'ship/v1.42-ci-gate-remediation'" .github/workflows/ci.yml && echo "gates-wired=ok"</automated>
  </verify>
  <acceptance_criteria>
    - snapshot-canary-guard --base origin/main exits 0.
    - The 3 flow specs and the branch-scoped smoke `if:` are present in ci.yml.
    - Local main is pushed to origin/main (or the rejection is surfaced, not forced).
  </acceptance_criteria>
  <done>Gates confirmed green locally and the backlog is on origin/main; ready to un-draft PR #63.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Un-draft PR #63 and drive all required lanes green on GitHub (D-10 steps 3-4)</name>
  <action>Take PR #63 out of draft (`gh pr ready 63` or GitHub UI) and observe the required lanes until all are green, then PAUSE for human confirmation of live GitHub state. See how-to-verify for the exact lanes and the accepted-known-flake re-run guidance. Do NOT merge in this step.</action>
  <what-built>
    The three gates are wired and green locally, and local main is pushed to origin/main.
    PR #63 (ship/v1.42-ci-gate-remediation → main) now needs to be taken out of DRAFT and
    driven green on GitHub — steps that require live GitHub state confirmation.
  </what-built>
  <how-to-verify>
    1. Take PR #63 out of draft (executor may run `gh pr ready 63`, or the human does it in
       the GitHub UI). Confirm PR #63 shows "Ready for review", not "Draft".
    2. Watch the required lanes on PR #63:
       - `fast_checks` — the "Snapshot drift guard (canary allowlist)" + design-lane steps
         are green (GATE-01).
       - `example_playwright_smoke` — the admin_behavior step ran the 3 admin-flow specs
         green (FLOW-01).
       - `Generated admin Playwright smoke` — shows as RUN (not skipping) and PASSED via
         admin-acceptance-smoke.sh --test all (GATE-02).
       - `ci-gate` — green.
       Use `gh pr checks 63` / `gh run list` to observe. If the accepted-known NoopTest
       shard-2 log-capture flake flares (Phase 211 D-05), re-run that job alone — it is not
       a gate failure.
    3. Confirm `golden_diff_test.exs` (installer↔example byte-parity) is green — do NOT
       regenerate the golden fixture to absorb any phx_new drift (SEED-004 pins 1.8.7).
    4. Confirm every required release lane (the 5 required-check contexts) is green and the
       PR is mergeable.
  </how-to-verify>
  <resume-signal>Type "gates green on PR #63" (with a link/run-id) once all required lanes are green and the PR is out of draft and mergeable, or describe which lane is red.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Merge PR #63 to origin/main (D-10 step 5 — point of no return)</name>
  <action>Merge PR #63 into origin/main (`gh pr merge 63 --merge` per repo policy, or GitHub UI), then PAUSE for human confirmation that the merge landed before any status flip. Do NOT touch ROADMAP/STATE in this step.</action>
  <what-built>
    All required lanes are green and PR #63 is mergeable. The merge itself is the
    point-of-no-return integration step and requires human confirmation before proceeding
    to the honest-status flip.
  </what-built>
  <how-to-verify>
    1. Merge PR #63 into origin/main (executor may run `gh pr merge 63 --merge` per repo
       merge policy, or the human merges in the UI). Confirm PR #63 shows "Merged".
    2. Confirm origin/main now contains the v1.42 backlog + the three gate fixes (the merge
       commit is on origin/main; `git fetch origin main` then `git log origin/main` shows
       the merge).
    3. Do NOT proceed to the ROADMAP/STATE flip until the merge is confirmed landed —
       this is the D-10 ordering guard against the premature-shipped-flag mistake.
  </how-to-verify>
  <resume-signal>Type "PR #63 merged" once the merge has landed on origin/main, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip ROADMAP v1.42 → shipped + reconcile STATE (D-10 step 6 — POST-MERGE ONLY)</name>
  <read_first>
    - .planning/ROADMAP.md Phase 212 section (lines ~305+; Success Criteria SC-1..SC-4) and the v1.42 milestone status line/header the audit flagged as prematurely "shipped"
    - .planning/STATE.md frontmatter (milestone: v1.42, status, current_phase, progress) and Current Position / Next
    - .planning/v1.42-MILESTONE-AUDIT.md "Recommended Closure" (the honest-completion definition)
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    ONLY after the previous checkpoint confirmed PR #63 merged: update planning bookkeeping
    to reflect honest, merged status.
    - ROADMAP.md: mark Phase 212 complete (check off its plan list entries), and set the
      v1.42 milestone status to shipped with today's date, now that it is TRUE (merged to
      origin/main). Use scoped edits only — do NOT rewrite the whole ROADMAP; touch only
      the Phase 212 block and the v1.42 milestone status line. Preserve every other phase
      entry.
    - STATE.md: reconcile frontmatter (status → the post-ship state, current_phase → 212,
      last_activity_desc → "v1.42 merged + shipped") and the Current Position / Next prose,
      removing the stale "PR #63 (draft) is open" / "385 commits ahead unpushed" lines now
      that both are resolved. Use scoped edits.
    Do NOT flip anything if the merge was not confirmed in the prior checkpoint.
  </action>
  <verify>
    <automated>grep -qi 'ship' .planning/ROADMAP.md && grep -q '212' .planning/STATE.md && python3 -c "import yaml; f=open('.planning/STATE.md').read(); yaml.safe_load(f.split('---')[1])"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP v1.42 reads shipped and Phase 212 plans are checked off — reflecting the
      actual merge, edited only after the merge checkpoint.
    - STATE.md frontmatter parses and reflects merged/shipped v1.42; stale draft/unpushed
      lines removed.
    - No unrelated ROADMAP phase entries were altered.
  </acceptance_criteria>
  <done>ROADMAP and STATE honestly reflect a merged, shipped v1.42 — the flip happened only after the merge landed (D-10).</done>
</task>

<task type="auto">
  <name>Task 3: Post-merge bookkeeping sweeps — reset allowlist (D-03) + delete stale todo (D-11)</name>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist (should currently hold the 4 slug lines added by Plan 01; the header states "Reset to empty once the re-recording PR merges")
    - .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md (the stale todo; its board-cfg PNGs now exist — 84 board files present, D-11)
    - .planning/phases/208-.../ (check whether a 208-VERIFICATION.md pointer is worth backfilling — optional, per D-11)
  </read_first>
  <files>test/example/priv/playwright/snapshot-allowlist, .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md</files>
  <action>
    POST-MERGE steady-state discipline (only after Task 2 confirmed the flip):
    - Reset test/example/priv/playwright/snapshot-allowlist back to comment-only (remove the
      4 slug lines audit-explorer / user-audit / global-user-index / org-scoped-admin added
      in Plan 01), restoring the empty steady state so the next PR's real drift on those
      slugs can't pass silently (D-03). Keep the comment header verbatim. Note: this reset
      is a follow-up change on origin/main after the merge — it is intentionally NOT part of
      the merged PR diff (the merged diff must contain the slugs so the drift was reviewable).
    - Delete .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md
      (git rm) — its 12 board-cfg PNGs now exist (84 board files present), so the debt is
      resolved (D-11).
    - Optionally backfill a short Phase 208 VERIFICATION.md traceability pointer stating its
      work is covered by 208.1 + 210-02 (a pointer, not re-verification) — skip if it adds
      churn without value.
  </action>
  <verify>
    <automated>test $(grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -c .) -eq 0 && test ! -f .planning/todos/pending/2026-06-28-phase205-debt-ci-native-board-baselines.md</automated>
  </verify>
  <acceptance_criteria>
    - The checkpoint allowlist is back to comment-only (0 slug lines) — steady state restored.
    - The stale phase-205 baseline-debt todo no longer exists in pending/.
    - Header comment preserved.
  </acceptance_criteria>
  <done>Steady-state allowlist discipline restored and stale bookkeeping swept; v1.42 is honestly closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main → origin/main (push) | Publishing the ~385-commit backlog; a force-push would rewrite shared history. |
| PR #63 → origin/main (merge) | The integration point; merging with a skipped/false-green gate would ship unproven parity. |
| planning bookkeeping → honest status | Flipping "shipped" before merge overstates reality (the audit's premature-flag finding). |

## STRIDE Threat Register

Integration/merge + bookkeeping; no runtime attack surface. Threats are process/integrity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-09 | Repudiation (dishonest status) | ROADMAP shipped flag | high | mitigate | D-10 strict ordering enforced by structure: the shipped flip (Task 2) sits AFTER a blocking merge-confirmation checkpoint; it cannot run before the merge is confirmed landed. |
| T-212-10 | Spoofing (false-green merge) | ci-gate skipped-need | high | mitigate | GATE-02 (Plan 03) makes the runtime smoke RUN on PR #63 so ci-gate reads a real result; the human-verify checkpoint confirms all required lanes green before merge. |
| T-212-11 | Tampering (silent future drift) | snapshot-allowlist | medium | mitigate | D-03 post-merge reset to empty restores the tripwire so a later PR's real drift on those slugs fails the guard. |
| T-212-12 | Tampering (history rewrite) | git push | high | mitigate | Task 1 forbids force-push; a rejected push is surfaced, not forced. |
| T-212-SC | Tampering | supply-chain / installs | low | accept | This phase adds no npm/pip/cargo installs and no new runtime deps; the generated-host smoke uses the already-pinned phx_new 1.8.7 (SEED-004). No new package legitimacy surface. |
</threat_model>

<verification>
- snapshot-canary-guard --base origin/main exits 0 before push.
- PR #63 out of draft; all required lanes (incl. RUN generated-host smoke) green; ci-gate green.
- PR #63 merged to origin/main BEFORE any ROADMAP/STATE flip.
- Post-merge: allowlist reset to empty; stale phase-205 todo deleted.
</verification>

<success_criteria>
SC-4 satisfied: local main pushed, PR #63 driven green and merged to origin/main, and ONLY
THEN v1.42 flipped to shipped + STATE reconciled, with the allowlist reset and stale todo
swept. Honest status — no premature shipped flag.
</success_criteria>

<output>
Create `.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-04-SUMMARY.md` when done
</output>
