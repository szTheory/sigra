---
phase: 212-v1-42-integration-merge-canary-reconciliation-gate-the-perso
plan: 03
type: execute
wave: 2
depends_on:
  - 212-02
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - GATE-02
user_setup: []

must_haves:
  truths:
    - "generated_admin_playwright_smoke RUNS (not skipping) on PR #63 because its head branch is ship/v1.42-ci-gate-remediation, and PASSES via admin-acceptance-smoke.sh --test all"
    - "The job still SKIPS on all other PRs (not un-skipped globally — no ~30-60m cold phx.new cost added to every future PR)"
    - "The branch-scoped condition is clearly written and trivially removable/generalizable later with no dead config"
  artifacts:
    - ".github/workflows/ci.yml generated_admin_playwright_smoke job `if:` condition (~line 1209) is branch-scoped so it runs on the integration branch's PR"
  key_links:
    - "the `if:` expression ↔ github.head_ref == 'ship/v1.42-ci-gate-remediation' (or equivalent label/dispatch), preserving `github.event_name != 'pull_request'` for the non-PR (push/nightly) path"
    - "the running job ↔ ci-gate aggregation (ci.yml:1338/1351 reads needs.generated_admin_playwright_smoke.result; a real run now contributes a real result instead of skipped)"
---

<objective>
Close GATE-02: make the "Generated admin Playwright smoke" job RUN (not `skipping`) and
PASS on PR #63, so generated-host runtime parity is proven IN CI, not just locally. The
audit found the runtime smoke was proven only on the maintainer's machine while the CI job
was `skipping` on the PR (and ci-gate treats a skipped need as not-failed → false-green).

Per D-08, relax the job's PR-skip (`ci.yml:1209: if: github.event_name != 'pull_request'`)
so it RUNS on the integration PR but **scoped to the ship/integration branch** — gate on
`github.head_ref == 'ship/v1.42-ci-gate-remediation'` (or an equivalent label /
workflow_dispatch), NOT un-skipped on all PRs. Rationale: this proves parity on PR #63 now
without adding the ~30-60m cold phx.new+compile+Playwright wall-clock (timeout-minutes: 60)
to every future PR — a known CI-PERF pole. Rejected: un-skip-all-PRs (30-60m on every PR
indefinitely); accept-post-merge-push (parity proven AFTER merge, weaker vs GATE-02 wording).

Per the specifics (D-08 clarity): write the condition so it clearly runs on PR #63's head
branch and is trivially removable/generalizable later without leaving dead config.

Exit (D-09): the job RUNS and PASSES via `scripts/ci/admin-acceptance-smoke.sh --test all`
(ci.yml:1258); the installer↔example byte-parity golden test (golden_diff_test.exs) stays
green (do NOT regenerate the golden fixture to absorb a phx_new drift — SEED-004 pins
phx_new 1.8.7).

Purpose: turn GATE-02 from a skipped (false-green) need into a real, passing runtime-parity
proof on the actual integration PR.
Output: an edited `if:` on the generated_admin_playwright_smoke job.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-CONTEXT.md
@scripts/ci/admin-acceptance-smoke.sh
</context>

<artifacts_this_phase_produces>
This plan modifies:
- `.github/workflows/ci.yml` — the `generated_admin_playwright_smoke` job's `if:` condition
  (currently `if: github.event_name != 'pull_request'` at ~line 1209) becomes a branch-scoped
  expression that ALSO runs when the PR head branch is the integration branch, e.g.
  `if: github.event_name != 'pull_request' || github.head_ref == 'ship/v1.42-ci-gate-remediation'`.
  No other job's `if:` is changed (the 5 unrelated `if: github.event_name != 'pull_request'`
  jobs at lines 520/573/624/754/1389/1690/1871 stay as-is). No other file changes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Branch-scope the generated_admin_playwright_smoke PR-skip so it runs on PR #63 (D-08)</name>
  <read_first>
    - .github/workflows/ci.yml lines 1206-1213 (the `generated_admin_playwright_smoke` job header: `name: Generated admin Playwright smoke`, `runs-on: ubuntu-latest`, `if: github.event_name != 'pull_request'`, `needs: release_ref_guard`, `timeout-minutes: 60`)
    - .github/workflows/ci.yml lines 1252-1258 (the smoke run step: `scripts/ci/admin-acceptance-smoke.sh --test all`) and 1327-1365 (ci-gate: needs list includes generated_admin_playwright_smoke; reads needs.generated_admin_playwright_smoke.result)
    - Confirm the integration branch name: run `git branch --show-current` and cross-check the PR #63 head branch is `ship/v1.42-ci-gate-remediation` per D-08/D-10; if the actual head_ref differs, use the real one and note the correction.
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Change ONLY the `generated_admin_playwright_smoke` job's `if:` (the one at ~line 1209,
    immediately under `name: Generated admin Playwright smoke`) from
    `if: github.event_name != 'pull_request'` to a branch-scoped disjunction that keeps the
    existing non-PR (push/nightly) behavior AND additionally runs on the integration PR's
    head branch:
    `if: github.event_name != 'pull_request' || github.head_ref == 'ship/v1.42-ci-gate-remediation'`.
    Add a short one-line comment directly above the `if:` explaining this is a temporary,
    integration-scoped relaxation for PR #63 (GATE-02 / D-08) that is trivially removable
    after merge — so it reads as intentional and non-permanent, not dead config. Do NOT
    change any other job's `if:` (the other `github.event_name != 'pull_request'` guards at
    lines ~520/573/624/754/1389/1690/1871 are unrelated and stay). Do NOT touch the smoke
    run step, the golden test, or the installer templates. If `git branch --show-current`
    shows a different integration branch name than `ship/v1.42-ci-gate-remediation`, use
    the real head-branch string and record the correction in the SUMMARY.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -q "head_ref == 'ship/v1.42-ci-gate-remediation'" .github/workflows/ci.yml && test $(grep -c "if: github.event_name != 'pull_request'" .github/workflows/ci.yml) -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml parses as valid YAML.
    - The generated_admin_playwright_smoke `if:` now includes the branch-scoped disjunct.
    - Exactly one job's `if:` gained the head_ref disjunct; the other pull_request-skip
      guards are unchanged (still ≥5 plain `github.event_name != 'pull_request'` occurrences).
    - A comment marks the relaxation as temporary/integration-scoped and removable.
  </acceptance_criteria>
  <done>generated_admin_playwright_smoke is wired to run on PR #63's head branch while still skipping on all other PRs; the actual RUN+PASS on GitHub is confirmed in Plan 04 (SC-4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR head branch → CI job gating | The `if:` expression decides whether the runtime-parity proof actually executes; a skipped job silently satisfies ci-gate. |

## STRIDE Threat Register

CI-config edit only; no runtime attack surface. The integrity risk is a false-green gate
from a skipped need.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-06 | Spoofing (false-green) | generated_admin_playwright_smoke gating | high | mitigate | The branch-scoped `if:` forces the job to RUN on PR #63 so ci-gate reads a real success/failure result instead of a skipped-as-not-failed pass. Confirmed green on GitHub in Plan 04. |
| T-212-07 | Denial of Service (CI cost) | future-PR wall-clock | low | accept | Scoping to the integration head branch avoids adding the ~30-60m cold phx.new run to every future PR; the disjunct is marked removable after merge so it does not become permanent dead config. |
| T-212-08 | Tampering | golden_diff_test.exs / phx_new pin | medium | mitigate | Task forbids regenerating the golden fixture; SEED-004's phx_new 1.8.7 pin keeps byte-parity green. Do not absorb a 1.8.8 diff. |
</threat_model>

<verification>
- ci.yml parses as valid YAML.
- Only the generated_admin_playwright_smoke `if:` gained the branch-scoped disjunct.
- The golden test and installer templates are untouched.
</verification>

<success_criteria>
GATE-02 wiring done: the runtime-parity smoke is configured to RUN on PR #63 (not skip);
the RUN+PASS is proven on GitHub in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-03-SUMMARY.md` when done
</output>
