---
phase: 212-v1-42-integration-merge-canary-reconciliation-gate-the-perso
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - FLOW-01
user_setup: []

must_haves:
  truths:
    - "The 3 persona-flow specs (admin-flow-platform-admin, admin-flow-support-investigator, admin-flow-org-admin) execute in the example_playwright_smoke `admin behavior browser truth` step on the chromium project"
    - "A flow-spec failure fails the example_playwright_smoke job via the existing outcome aggregator (fail-closed, no silent green)"
    - "The specs run on chromium ONLY — they match ADMIN_BEHAVIOR_SPECS and are excluded from the mobile project's testIgnore, so they are not silently skipped"
  artifacts:
    - ".github/workflows/ci.yml admin_behavior step (~line 993-997) lists the 3 admin-flow-*.spec.ts paths"
  key_links:
    - "the 3 appended spec paths ↔ the `--project=chromium` flag on the admin_behavior step (specs match ADMIN_BEHAVIOR_SPECS in playwright.config.ts:24-25)"
    - "admin_behavior step ↔ the outcome aggregator at ci.yml:1102-1107 (steps.admin_behavior.outcome is already summed)"
---

<objective>
Close FLOW-01: wire the 3 persona-flow Playwright specs into a CI job that actually
executes them, so the ledger's flow-* Tier-2 citations become execution-backed instead of
existence-backed. The audit found these specs exist and are substantive (6/5/5 tests) but
run in NO CI job and NO phase.

Per D-05, append the 3 spec paths to the explicit chromium spec list in the EXISTING
`example_playwright_smoke` "Run admin behavior browser truth (chromium)" step
(ci.yml ~991-997). This reuses the already-booted example app + seeded demo personas
(alice/dave/frank/morgan@demo.tasklane.test) at near-zero marginal cost — rejected
alternatives were a dedicated new job (duplicates the expensive boot+seed) and a written
waiver (contradicts the milestone's "proven in Playwright" acceptance clause).

Per D-06, they stay on the `chromium` project (they match ADMIN_BEHAVIOR_SPECS at
playwright.config.ts:24-25 and are excluded from `mobile` via testIgnore at
playwright.config.ts:98-99) — do NOT add them to any mobile list, or the testIgnore regex
would silently skip them ("wired but running nowhere").

Purpose: make the persona-flow acceptance real; the milestone can't honestly claim FLOW-01
until a gate runs these specs.
Output: an edited ci.yml admin_behavior step listing the 3 flow specs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-CONTEXT.md
@test/example/priv/playwright/playwright.config.ts
</context>

<artifacts_this_phase_produces>
This plan modifies:
- `.github/workflows/ci.yml` — the "Run admin behavior browser truth (chromium)" step
  (step id `admin_behavior`, run block currently ~993-997 listing
  admin-user-operations / impersonation / admin-audit / admin-modal-interaction) gains 3
  appended spec paths: `tests/admin-flow-platform-admin.spec.ts`,
  `tests/admin-flow-support-investigator.spec.ts`, `tests/admin-flow-org-admin.spec.ts`,
  before the trailing `--project=chromium` flag. No other file changes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Append the 3 persona-flow specs to the admin_behavior chromium step (D-05/D-06)</name>
  <read_first>
    - .github/workflows/ci.yml lines 978-997 (the `admin_behavior` step: `id: admin_behavior`, `if: ${{ !cancelled() }}`, and the `npx playwright test ... --project=chromium` run block listing the 4 existing behavior specs)
    - .github/workflows/ci.yml lines 1096-1112 (the outcome aggregator that already sums steps.admin_behavior.outcome and re-fails the job)
    - test/example/priv/playwright/playwright.config.ts lines 24-25 (ADMIN_BEHAVIOR_SPECS regex — already includes `admin-flow-`) and 88-99 (chromium project vs mobile project testIgnore excludes ADMIN_BEHAVIOR_SPECS)
    - test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts / admin-flow-support-investigator.spec.ts / admin-flow-org-admin.spec.ts (confirm filenames; they depend on seeded personas alice/dave/frank/morgan)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    In the `admin_behavior` step's `npx playwright test` invocation (the run block that
    currently lists tests/admin-user-operations.spec.ts, tests/impersonation.spec.ts,
    tests/admin-audit.spec.ts, tests/admin-modal-interaction.spec.ts then `--project=chromium`),
    append three more spec paths on their own continuation lines, matching the existing
    two-space indentation and trailing backslash continuation style, BEFORE the
    `--project=chromium` line: tests/admin-flow-platform-admin.spec.ts,
    tests/admin-flow-support-investigator.spec.ts, tests/admin-flow-org-admin.spec.ts.
    Keep the single `--project=chromium` flag as the final argument (D-06: chromium only —
    do NOT add a mobile project or a second invocation). Do NOT create a new job or step,
    and do NOT touch the mobile spec list (~1076-1081) or the generated-host smoke job.
    The existing outcome aggregator (ci.yml:1102-1107) already sums
    steps.admin_behavior.outcome, so a flow-spec failure fails the job fail-closed with no
    additional wiring (D-07). Optionally extend the step's explanatory comment to note the
    persona-flow specs are now gated, but do not alter the aggregator.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -q 'tests/admin-flow-platform-admin.spec.ts' .github/workflows/ci.yml && grep -q 'tests/admin-flow-support-investigator.spec.ts' .github/workflows/ci.yml && grep -q 'tests/admin-flow-org-admin.spec.ts' .github/workflows/ci.yml && test $(grep -c 'admin-flow-.*mobile\|--project=mobile.*admin-flow' .github/workflows/ci.yml) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml parses as valid YAML.
    - All 3 admin-flow-*.spec.ts paths appear in the admin_behavior step's run block.
    - The 3 flow specs are NOT wired to a mobile project.
    - No new CI job was added.
  </acceptance_criteria>
  <done>The 3 persona-flow specs run on chromium inside example_playwright_smoke and fail the job on failure via the existing aggregator; FLOW-01 execution is real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR diff → CI acceptance gate | The example_playwright_smoke job is a required release lane; adding specs to it changes what "green" attests. |

## STRIDE Threat Register

CI-config edit only; no runtime attack surface. The integrity risk is a false-green gate.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-04 | Spoofing (false-green) | admin_behavior step | high | mitigate | Specs are appended to a step whose outcome is already summed by the aggregator (ci.yml:1102-1107), so a flow-spec failure fails the job — no silent green. Verified by grep for the 3 paths in the aggregated step + YAML validity. |
| T-212-05 | Spoofing (wired-nowhere) | project selection | medium | mitigate | Specs match ADMIN_BEHAVIOR_SPECS and run only on `--project=chromium`; verify asserts they are NOT attached to a mobile project (mobile testIgnore would skip them silently). |
</threat_model>

<verification>
- ci.yml parses as valid YAML.
- The admin_behavior step lists all 3 flow specs on the chromium project.
- No new job/step added; mobile list untouched.
</verification>

<success_criteria>
FLOW-01 satisfied: the 3 persona-flow specs execute in a CI gate (fail-closed) rather than
existing unrun; ledger flow-* citations become execution-backed.
</success_criteria>

<output>
Create `.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-02-SUMMARY.md` when done
</output>
