---
phase: 212-v1-42-integration-merge-canary-reconciliation-gate-the-perso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/snapshot-allowlist
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-chromium.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-mobile.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-dark.png
autonomous: true
requirements:
  - GATE-01
user_setup: []

must_haves:
  truths:
    - "scripts/ci/snapshot-canary-guard.sh --base origin/main exits 0 (no unintended checkpoint drift, no forbidden canary modify)"
    - "The 4 legit v1.41-backlog drifted checkpoint slugs (audit-explorer, user-audit, global-user-index, org-scoped-admin) are declared in the checkpoint allowlist in the same PR diff"
    - "The impersonation-banner canary appears as `added` (not `modified`) vs origin/main, preserving the Phase 204-03 WCAG contrast fix (commit c96749fa) — canary stays ARMED, never allowlisted"
  artifacts:
    - "test/example/priv/playwright/snapshot-allowlist with 4 slug lines (audit-explorer, user-audit, global-user-index, org-scoped-admin)"
    - "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-{chromium,mobile,dark}.png re-added (identical WCAG-fixed bytes) so the merge diff classifies the canary as added"
  key_links:
    - "snapshot-allowlist slug lines ↔ the drifted PNG filenames (one slug covers all 3 projects)"
    - "canary delete-then-re-add ↔ snapshot-canary-guard.sh's `added`-tolerant / `modified`-forbidden classification (guard lines 92-104)"
---

<objective>
Close GATE-01 (canary reconciliation) so the v1.42 backlog can merge cleanly to
origin/main. Two mechanically-distinct drifts must be reconciled in the SAME PR diff:

1. Four **legitimate v1.41-backlog checkpoint drifts** (`audit-explorer`, `user-audit`,
   `global-user-index`, `org-scoped-admin`) — cumulative deltas from phases 200–204, not
   v1.42 regressions. These are exactly what the allowlist encodes (D-02). Add one slug
   line each to `test/example/priv/playwright/snapshot-allowlist`.
2. The **impersonation-banner canary**, whose Phase 204-03 WCAG contrast fix (commit
   `c96749fa`) left it `modified` vs origin/main. Per the human-ratified decision D-01,
   re-baseline + re-designate it so it appears `added` (delete-then-re-add the same
   WCAG-fixed bytes), keeping the tripwire ARMED on the new mobile bytes. The canary is
   NEVER allowlisted (rejected: waiver / revert-to-origin-bytes).

Exit condition (D-04): `scripts/ci/snapshot-canary-guard.sh --base origin/main` exits 0,
and both the fast_checks "Snapshot drift guard (canary allowlist)" step AND the design-lane
step (ci.yml:100-108) are conceptually satisfied (the checkpoint allowlist covers the
checkpoint lane; the design lane is untouched here).

Purpose: unblock PR #63's fast_checks + ci-gate, which are red solely on this drift.
Output: an edited checkpoint allowlist + a re-added canary that reads `added` vs origin/main.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-CONTEXT.md
@scripts/ci/snapshot-canary-guard.sh
@test/example/priv/playwright/snapshot-allowlist
</context>

<artifacts_this_phase_produces>
This plan creates/modifies:
- `test/example/priv/playwright/snapshot-allowlist` — 4 new slug lines appended below the
  comment header: `audit-explorer`, `user-audit`, `global-user-index`, `org-scoped-admin`
  (one line each; a slug covers chromium/mobile/dark at once).
- `test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-chromium.png`
- `test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-mobile.png`
- `test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-dark.png`
  — the 3 canary PNGs are git-removed and re-added (byte-identical WCAG-fixed content
  already committed at c96749fa) so the merge diff classifies the canary slug as `added`,
  which the guard tolerates, instead of `modified`, which it forbids.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Allowlist the 4 legit v1.41-backlog checkpoint drifts (D-02)</name>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist (current steady-state-empty file; comment header explains one-slug-per-line, covers all 3 projects, and that impersonation-banner must NEVER appear here)
    - scripts/ci/snapshot-canary-guard.sh lines 40-58 (allowlist load) and 92-114 (per-slug evaluation)
  </read_first>
  <files>test/example/priv/playwright/snapshot-allowlist</files>
  <action>
    Append exactly four slug lines below the existing comment header in
    test/example/priv/playwright/snapshot-allowlist, one per line, no inline comments:
    audit-explorer, user-audit, global-user-index, org-scoped-admin. These are the four
    checkpoint slugs the canary guard currently reports as unintended `modified` drift vs
    origin/main; they are cumulative v1.41 backlog deltas (phases 200–204, commits
    af735d75 / e7c5b0c7 / 4c3ce3cf), not v1.42 regressions — precisely what the allowlist
    encodes per D-02. Do NOT add impersonation-banner (the canary is never allowlistable —
    the header says so, and Task 2 handles it via re-designation). Do NOT touch the
    separate design-lane allowlist (snapshot-allowlist-design) — the design canary
    board-notice and design drifts are out of scope for this plan. Preserve the existing
    comment header verbatim.
  </action>
  <verify>
    <automated>test $(grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -c .) -eq 4 && grep -qx 'audit-explorer' test/example/priv/playwright/snapshot-allowlist && grep -qx 'user-audit' test/example/priv/playwright/snapshot-allowlist && grep -qx 'global-user-index' test/example/priv/playwright/snapshot-allowlist && grep -qx 'org-scoped-admin' test/example/priv/playwright/snapshot-allowlist && ! grep -qx 'impersonation-banner' test/example/priv/playwright/snapshot-allowlist</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 4 non-comment, non-blank lines in the file, being the 4 named slugs.
    - impersonation-banner is NOT present.
    - Comment header preserved.
  </acceptance_criteria>
  <done>The 4 legit drifted slugs are allowlisted in the same PR diff; the canary is not.</done>
</task>

<task type="auto">
  <name>Task 2: Re-designate the impersonation-banner canary so it reads `added` vs origin/main (D-01)</name>
  <read_first>
    - scripts/ci/snapshot-canary-guard.sh lines 70-104 (name-status classification: `A*`→added, `D*`→deleted, else→modified; canary tolerates `added`, forbids `modified`/`deleted`/`mixed`)
    - .github/workflows/ci.yml lines 1486-1498 (the design-lane precedent: delete canary PNGs BEFORE recapture so the guard sees `added` not `modified`) and 1802-1826 (the checkpoint-lane recapture-to-review-PR job; the impersonation-banner WCAG re-baseline rationale)
    - .planning/phases/212-.../212-CONTEXT.md D-01 (human-ratified: re-baseline + re-designate; preserve c96749fa; chromium + dark are byte-stable and must remain untouched in CONTENT — only their git-status changes from unchanged to added)
  </read_first>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-chromium.png, test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-mobile.png, test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-dark.png</files>
  <action>
    The impersonation-banner mobile PNG already carries the WCAG-fixed bytes (committed at
    c96749fa — verify: its slot is the ≥4.5:1 .vt-status-pill contrast, NOT origin bytes).
    Do NOT recapture on darwin (these are CI-native ubuntu baselines per project policy;
    a local darwin render would introduce spurious font-metric drift). Instead, perform a
    pure git re-designation so the merge diff classifies the canary slug as `added` (which
    the guard tolerates) rather than `modified` (which it forbids), while keeping the
    on-disk bytes byte-identical to what is committed at HEAD:
      1. `git rm --cached` all three impersonation-banner-admin-checkpoints-{chromium,mobile,dark}.png
         (remove from the index) but keep the working-tree files intact.
      2. `git add` the same three files back.
    Because origin/main's tree contains the pre-204-03 bytes for these paths and the
    rebaselined-plus-re-add makes the net `git diff --name-status origin/main` read the
    slug as added-when-deleted-first, confirm the guard's classification empirically in the
    verify step rather than reasoning about it — if the guard still reports the canary as
    `modified`, escalate to the sanctioned delete-then-recapture-on-ubuntu path (the ci.yml
    checkpoint-recapture job at lines 1802-1855 which opens a human-review PR) rather than
    recapturing locally. The canary MUST NOT be added to any allowlist. Preserve the
    committed WCAG-fixed content on all three projects.
  </action>
  <verify>
    <automated>git fetch origin main -q; bash scripts/ci/snapshot-canary-guard.sh --base origin/main; echo "guard-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/snapshot-canary-guard.sh --base origin/main` prints no FAIL lines and no
      "canary snapshot modified" line; it reports the canary as OK (added / first-established)
      or clean, and exits 0.
    - The 4 allowlisted slugs are reported as "OK intended ... delta", not FAIL.
    - The mobile PNG on disk is byte-identical to HEAD's c96749fa content (WCAG fix preserved).
    - impersonation-banner is NOT in any allowlist file.
  </acceptance_criteria>
  <done>GATE-01 exit condition (D-04) met locally: snapshot-canary-guard.sh --base origin/main exits 0 with the canary armed on the WCAG-fixed bytes and the 4 legit drifts allowlisted in the same diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR diff → CI merge gate | The snapshot-canary-guard tripwire is the control that keeps silent visual regressions out of origin/main. This plan edits the manifest it reads. |

## STRIDE Threat Register

This is a CI-manifest + baseline-PNG reconciliation. It adds no runtime attack surface;
the relevant threats are process/supply-chain integrity of the visual-regression tripwire.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-01 | Tampering | impersonation-banner canary | high | mitigate | Canary is re-designated (delete-then-re-add of WCAG-fixed bytes), NEVER allowlisted (D-01 rejected the waiver). Tripwire stays armed on the new mobile bytes; a future silent modify still fails the guard. Verified by guard exit 0 with the canary NOT in any allowlist. |
| T-212-02 | Repudiation | snapshot-allowlist | medium | mitigate | The 4 intended drifts are declared in the same PR diff (reviewable intent), and Plan 04 resets the allowlist to empty after merge (D-03) so a later PR's real drift on those slugs cannot pass silently. |
| T-212-03 | Tampering | .vt-status-pill WCAG fix (c96749fa) | high | mitigate | Task 2 forbids local darwin recapture and requires byte-identity with HEAD's WCAG-fixed content, preventing an accidental revert-to-origin-bytes that would reopen the 204-03 contrast regression. |
</threat_model>

<verification>
- `scripts/ci/snapshot-canary-guard.sh --base origin/main` exits 0.
- The checkpoint allowlist contains exactly the 4 legit slugs and NOT impersonation-banner.
- The impersonation-banner mobile PNG remains byte-identical to HEAD (c96749fa WCAG fix).
- The separate design-lane allowlist (snapshot-allowlist-design) is untouched.
</verification>

<success_criteria>
GATE-01 (D-04) satisfied: the canary guard passes vs origin/main; intended drifts are
allowlisted-in-diff; the canary is armed (not waived) on the WCAG-fixed bytes.
</success_criteria>

<output>
Create `.planning/phases/212-v1-42-integration-merge-canary-reconciliation-gate-the-perso/212-01-SUMMARY.md` when done
</output>
