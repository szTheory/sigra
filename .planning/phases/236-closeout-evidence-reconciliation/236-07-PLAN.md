---
phase: 236-closeout-evidence-reconciliation
plan: 07
type: execute
wave: 7
depends_on: [236-06]
files_modified:
  - scripts/planning/phase-236-audit-snapshot.exs
  - scripts/planning/phase-236-audit-snapshot-test.exs
  - test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "D-05: historical_verify!/4 first resolves and requires the exact freeze SHA 22dfd0880967dd84cdee153fa9b1dd1b083f83f5 and audit SHA a523575d4fb40baac371112d5a4476db5b153ae7, then accepts audit snapshot documents only when their exact bytes equal the fixed input/output blobs at those commits."
    - "A wholly forged but internally self-consistent manifest/output pair is rejected before its declarations can influence historical verification."
    - "D-04: the committed input/output snapshots, milestone audit, replay ledger, validation artifacts, verification artifacts, and retained runtime evidence remain byte-for-byte unchanged."
    - "The authorized rewritten history is proved from finalized Plan 06 amendment boundary 4aaa3a73 through completion boundary b6c67ed9; stale non-ancestor e9fee981 is not used as evidence."
    - "The Plan 07 introduction commit is a separately asserted pre-implementation boundary, so its full SHA can be pinned after that commit exists without making the fence self-referential."
    - "One identical allowlist governs the two committed execution ranges, tracked staged/unstaged changes, and untracked non-ignored paths; every other path is rejected."
    - "D-01, D-02, D-03, and D-06 remain exactly as executed, with no new ownership, lifecycle, debt, product, CI-topology, required-check, or runtime-evidence claim."
  artifacts:
    - path: "scripts/planning/phase-236-audit-snapshot.exs"
      provides: "Historical verifier bound to fixed committed input/output snapshot blobs"
    - path: "scripts/planning/phase-236-audit-snapshot-test.exs"
      provides: "Hermetic fully forged self-consistent replacement-pair adversarial coverage"
    - path: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      provides: "Deterministic rewritten-history, plan-amendment-boundary, and execution-scope contract"
  key_links:
    - from: "scripts/planning/phase-236-audit-snapshot.exs"
      to: ".planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json"
      via: "byte equality with git show of the fixed path at freeze commit 22dfd088 before JSON decoding"
      pattern: "git_show!.*236-AUDIT-INPUT-SNAPSHOT"
    - from: "scripts/planning/phase-236-audit-snapshot.exs"
      to: ".planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json"
      via: "byte equality with git show of the fixed path at audit commit a523575d before JSON decoding"
      pattern: "git_show!.*236-AUDIT-OUTPUT-SNAPSHOT"
    - from: "4aaa3a739b3d53b86de41136857ece240c0807a5"
      to: "b6c67ed987fae364bb8e41dab21a8942bbc952a1"
      via: "exact direct-parent chain and four-path Plan 06 execution range"
    - from: "the unique commit introducing 236-07-PLAN.md"
      to: "completed Plan 07 HEAD"
      via: "separately pinned plan-amendment boundary and identical fail-closed path allowlist"
---

<objective>
Close the two remaining Phase 236 verification blockers by binding historical audit verification to committed Git blobs and replacing the stale Plan 06 fence reference with explicit, non-self-referential amendment boundaries.

Purpose: Make the terminal audit provenance resistant to caller-forged documents and make the rewritten-history scope proof executable without changing any protected evidence or widening product/CI scope.
Output: Source-bound historical verification, a fully forged-pair regression test, and a deterministic contract for the finalized Plan 06 and Plan 07 execution boundaries.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md
@.planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md
@.planning/phases/236-closeout-evidence-reconciliation/236-REVIEW.md
@.planning/phases/236-closeout-evidence-reconciliation/236-06-PLAN.md
@.planning/phases/236-closeout-evidence-reconciliation/236-06-SUMMARY.md
@.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json
@.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json
@scripts/planning/phase-236-audit-snapshot.exs
@scripts/planning/phase-236-audit-snapshot-test.exs
@test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind historical verification to the committed snapshot blobs</name>
  <precondition>Commits 22dfd088 and a523575d and their fixed Phase 236 snapshot paths exist; the worktree has no edits to the two files owned by this task.</precondition>
  <reversibility rating="reversible">The change hardens a local read-only verifier and its tests without mutating the retained snapshots or any published/runtime contract.</reversibility>
  <files>scripts/planning/phase-236-audit-snapshot.exs, scripts/planning/phase-236-audit-snapshot-test.exs</files>
  <read_first>
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-REVIEW.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json,
    .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json,
    scripts/planning/phase-236-audit-snapshot.exs,
    scripts/planning/phase-236-audit-snapshot-test.exs,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
  </read_first>
  <behavior>
    - Test 1: the real committed input/output snapshot pair succeeds only for exact freeze commit 22dfd0880967dd84cdee153fa9b1dd1b083f83f5 and exact audit commit a523575d4fb40baac371112d5a4476db5b153ae7.
    - Test 2: immediately after rev-parsing each caller-supplied commit, historical verification asserts equality with its corresponding full expected SHA; both assertions occur before any Git-object read, caller-file read, or JSON decode.
    - Test 3: the real but wrong pair a523575d4fb40baac371112d5a4476db5b153ae7/a523575d4fb40baac371112d5a4476db5b153ae7 is rejected for the freeze-SHA mismatch before snapshot blobs are read, even though both arguments resolve to real commits and the fixed snapshot paths are retained.
    - Test 4: after the commit pins pass, historical verification loads the fixed input snapshot path from 22dfd0880967dd84cdee153fa9b1dd1b083f83f5 and the fixed output snapshot path from a523575d4fb40baac371112d5a4476db5b153ae7, then requires each supplied document to be raw-byte-identical to that Git blob before decoding or trusting any field.
    - Test 5: a wholly forged input with an empty source-file list, a recomputed canonical manifest digest, structurally valid claim limits/resolvers, and matching starting commit paired with a wholly forged output carrying the forged manifest links and real audit digest is rejected specifically as a committed-snapshot byte mismatch.
    - Test 6: the existing wrong-commit, source-digest, manifest-link, and audit-digest adverse cases remain rejected.
  </behavior>
  <action>
    Start RED in `scripts/planning/phase-236-audit-snapshot-test.exs` by constructing a replacement pair from decoded maps rather than by corrupting the real JSON text. Set the forged input `files` to an empty list, keep structurally admissible members/claim limits, use a structurally valid resolver list (an empty list is sufficient under the current contract), and recompute `manifest_sha256` with the same canonical key ordering and starting-commit exclusion used by the production utility. Build a separate forged output whose `input_manifest_sha256` and `post_audit_manifest_sha256` equal that forged digest while its audit path/digest and bounded provenance text remain otherwise valid. Demonstrate that this pair is internally self-consistent, then assert `historical-verify` exits nonzero and reports the committed input snapshot byte mismatch. This must be a replacement attack that would pass the pre-fix logic, not another one-field corruption.

    In `Phase236AuditSnapshot.historical_verify!/4`, pin `expected_freeze_sha` to `22dfd0880967dd84cdee153fa9b1dd1b083f83f5` and `expected_audit_sha` to `a523575d4fb40baac371112d5a4476db5b153ae7`. Rev-parse the freeze argument and immediately assert that resolved full SHA equals `expected_freeze_sha`; then rev-parse the audit argument and immediately assert that resolved full SHA equals `expected_audit_sha`. These equality checks must run before any `git_show!`, caller-file read, or JSON decoding. Add an adversarial invocation using the genuine supplied snapshot files and the real-but-wrong commit pair `a523575d4fb40baac371112d5a4476db5b153ae7`/`a523575d4fb40baac371112d5a4476db5b153ae7`; assert the deterministic freeze-SHA error and prove no later blob/path validation substitutes for the pin.

    Define fixed repository-relative paths for `236-AUDIT-INPUT-SNAPSHOT.json` and `236-AUDIT-OUTPUT-SNAPSHOT.json`. Only after both commit pins pass, load the input blob with `git_show!(resolved_freeze_sha, fixed_input_path)` and the output blob with `git_show!(resolved_audit_sha, fixed_output_path)`, read the caller-supplied files as diagnostic inputs, and require exact raw-byte equality with the corresponding committed blobs before JSON decoding. Decode and validate the committed bytes after equality succeeds. Keep the caller arguments for CLI compatibility, but do not trust their paths, declared file lists, manifest hashes, or output links as substitutes for the fixed Git objects. Preserve all existing ancestry, exact commit-scope, source-digest, unchanged-source-range, resolver-structure, manifest-link, audit-digest, and provenance-limit assertions. Do not edit either snapshot JSON, the audit report, any validation/replay artifact, or any retained runtime evidence per D-04/D-05.
  </action>
  <verify>
    <automated>elixir scripts/planning/phase-236-audit-snapshot-test.exs &amp;&amp; mix format --check-formatted scripts/planning/phase-236-audit-snapshot.exs scripts/planning/phase-236-audit-snapshot-test.exs &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The genuine historical pair passes only at the two full pinned SHAs; the real-but-wrong a523575d4fb40baac371112d5a4476db5b153ae7/a523575d4fb40baac371112d5a4476db5b153ae7 pair fails immediately for freeze-SHA mismatch before any Git-object read or JSON decode; the wholly forged but self-consistent pair fails because its raw bytes differ from the fixed committed input blob; existing adverse boundary cases still fail; both files are formatter-clean; no snapshot, audit, replay, validation, verification, product, CI, or runtime-evidence file changes.
  </acceptance_criteria>
  <done>D-05 historical audit provenance is bound to the committed source/output documents rather than caller-controlled self-consistency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ratify the rewritten Plan 06 boundary and seal one identical scope fence</name>
  <precondition>Before any Plan 07 execution, the workflow has committed the finalized 236-07-PLAN.md as a dedicated one-path planning commit; that unique introduction commit predates Task 1, changes only that plan file, is an ancestor of HEAD, and the worktree is clean. Task 1 is committed.</precondition>
  <reversibility rating="reversible">This adds local Git-history assertions and a scope gate; it neither rewrites the already-authorized history nor changes protected evidence.</reversibility>
  <files>test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</files>
  <read_first>
    .planning/phases/236-closeout-evidence-reconciliation/236-06-PLAN.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-06-SUMMARY.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-REVIEW.md,
    scripts/planning/phase-236-audit-snapshot.exs,
    scripts/planning/phase-236-audit-snapshot-test.exs,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
  </read_first>
  <behavior>
    - Test 1: 4aaa3a739b3d53b86de41136857ece240c0807a5 is an ancestor of b6c67ed987fae364bb8e41dab21a8942bbc952a1, changes only `236-06-PLAN.md`, and is the direct parent of c1a146d59b03528bcd1d8e4ac9cb3a97400e66c7.
    - Test 2: the exact rewritten Plan 06 chain is 4aaa3a73 → c1a146d5 → fcb55894 → c7ec527e → b6c67ed9, and the aggregate execution range contains exactly the three owned Elixir files plus `236-06-SUMMARY.md`.
    - Test 3: before execution, the finalized `236-07-PLAN.md` already exists in a dedicated committed one-path planning boundary; its unique introduction commit is then captured and pinned by full SHA in the later contract commit, changes only that plan file, and is an ancestor/direct pre-implementation boundary for Plan 07. The plan does not invent its SHA or require the planning commit to contain its own SHA.
    - Test 4: the union of finalized Plan 06 execution range and Plan 07 execution range, current tracked staged/unstaged changes, and untracked non-ignored paths is accepted only through one identical five-path allowlist; any sixth path fails.
    - Test 5: the full Phase 236 contract and the hardened snapshot tests pass together with no protected-evidence digest change.
  </behavior>
  <action>
    Treat `4aaa3a739b3d53b86de41136857ece240c0807a5` as the separately asserted finalized Plan 06 amendment boundary created by the authorized rebase, and `b6c67ed987fae364bb8e41dab21a8942bbc952a1` as the immutable Plan 06 completion boundary. Do not use stale non-ancestor `e9fee981`, edit `236-06-PLAN.md`, or perform another history rewrite. Add a `:scope_fence` contract case that proves the exact direct-parent chain through c1a146d5, fcb55894, c7ec527e, and b6c67ed9, asserts the amendment commit changed only `236-06-PLAN.md`, and requires the aggregate Plan 06 execution range to contain exactly the three implementation/test files plus `236-06-SUMMARY.md`.

    Fail the execution precondition unless the workflow has already introduced the finalized `236-07-PLAN.md` in a committed one-path planning commit before Task 1 starts. Task 2 must not create or amend that planning commit, invent a prospective SHA, or require the plan file to name the SHA of the commit that contains it. After the separate introduction commit exists, resolve its unique full SHA from history. Require its commit-local diff to contain only the added `236-07-PLAN.md`, require it to be an ancestor of Task 1 and the final HEAD, and pin that already-existing full SHA in the later contract commit as `plan_07_baseline_sha`. This is the separately asserted Plan 07 pre-implementation boundary; because the boundary commit already exists before the SHA is written into the later contract commit, the assertion is not self-referential. Reject an absent, ambiguous, rewritten, extra-path, non-ancestor, or interposed boundary.

    Define one five-path allowlist containing only `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs`, `scripts/planning/phase-236-audit-snapshot.exs`, `scripts/planning/phase-236-audit-snapshot-test.exs`, `236-06-SUMMARY.md`, and `236-07-SUMMARY.md` at their full repository paths. In the contract, require the historical Plan 06 range to equal its four expected members and require every path in `plan_07_baseline_sha..HEAD` to match the same five-path allowlist. In the final shell gate, enable pipeline failure propagation; collect the union of `git diff --name-only 4aaa3a73..b6c67ed9` and `git diff --name-only plan_07_baseline_sha..HEAD`, current tracked staged/unstaged paths from `git diff --name-only HEAD`, and untracked non-ignored paths from `git ls-files --others --exclude-standard`. Make every Git command succeed before filtering. Apply the exact same allowlist variable independently to all three collections and fail on any nonempty rejected set. `236-06-SUMMARY.md` and `236-07-SUMMARY.md` are the only bookkeeping paths admitted; all product, CI, config, requirement, snapshot JSON, replay JSON, audit, validation, verification, review, and runtime-evidence paths remain protected. Run the complete contract, snapshot suite, exact formatter gate, and scope fence after the code commits and again after the Plan 07 SUMMARY commit.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; elixir scripts/planning/phase-236-audit-snapshot-test.exs &amp;&amp; mix format --check-formatted test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs scripts/planning/phase-236-audit-snapshot.exs scripts/planning/phase-236-audit-snapshot-test.exs &amp;&amp; set -o pipefail &amp;&amp; plan_07_path='.planning/phases/236-closeout-evidence-reconciliation/236-07-PLAN.md' &amp;&amp; plan_07_baseline_sha="$(git log --diff-filter=A --format=%H -- "$plan_07_path")" &amp;&amp; test "$(printf '%s\n' "$plan_07_baseline_sha" | awk 'NF{n++} END{print n+0}')" -eq 1 &amp;&amp; git merge-base --is-ancestor 4aaa3a739b3d53b86de41136857ece240c0807a5 b6c67ed987fae364bb8e41dab21a8942bbc952a1 &amp;&amp; git merge-base --is-ancestor "$plan_07_baseline_sha" HEAD &amp;&amp; scope_allowlist='^(test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test\.exs|scripts/planning/phase-236-audit-snapshot\.exs|scripts/planning/phase-236-audit-snapshot-test\.exs|\.planning/phases/236-closeout-evidence-reconciliation/236-06-SUMMARY\.md|\.planning/phases/236-closeout-evidence-reconciliation/236-07-SUMMARY\.md)$' &amp;&amp; committed_changed="$({ git diff --name-only 4aaa3a739b3d53b86de41136857ece240c0807a5..b6c67ed987fae364bb8e41dab21a8942bbc952a1 &amp;&amp; git diff --name-only "$plan_07_baseline_sha"..HEAD; } | sort -u)" &amp;&amp; tracked_changed="$(git diff --name-only HEAD)" &amp;&amp; untracked_changed="$(git ls-files --others --exclude-standard)" &amp;&amp; for paths in "$committed_changed" "$tracked_changed" "$untracked_changed"; do test -z "$(printf '%s\n' "$paths" | awk -v allowed="$scope_allowlist" 'NF &amp;&amp; $0 !~ allowed')" || exit 1; done &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The contract proves the exact finalized Plan 06 amendment/execution chain and the separately committed one-path Plan 07 planning boundary that existed before execution; the later contract pins its actual full SHA without inventing it or making the planning commit self-referential; stale e9fee981 is nowhere in the active fence; both committed execution ranges plus tracked and untracked collections succeed and use one identical five-path allowlist; the full contract/snapshot/format suites pass; every non-allowlisted path remains unchanged.
  </acceptance_criteria>
  <done>The rewritten-history scope fence is non-self-referential, ancestor-valid, exact for Plan 06 and Plan 07, and fail-closed across committed, tracked, and untracked paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller-supplied JSON → historical audit verifier | Arbitrary files may be internally consistent while differing from the evidence committed at the asserted historical boundaries. |
| Rewritten Git history → scope-fence claim | A stale or self-referential baseline can make a scope gate reject valid work or silently inspect the wrong range. |
| Gap-closure implementation → protected milestone evidence | A convenient snapshot refresh, audit rewrite, or wider allowlist could hide provenance failure instead of repairing it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-236-26 | Tampering | `historical_verify!/4` input/output documents | critical | mitigate | Load fixed snapshot blobs from the asserted commits, require supplied raw bytes to equal them before decode, and reject a wholly forged self-consistent pair. |
| T-236-27 | Spoofing | Caller-declared snapshot paths and manifests | high | mitigate | Fixed repository paths and Git-object reads are authoritative; caller paths remain diagnostic-only compatibility arguments. |
| T-236-28 | Tampering | Rebased Plan 06 scope boundary | high | mitigate | Pin the finalized 4aaa3a73 amendment boundary, exact direct-parent chain, immutable b6c67ed9 end boundary, and four-path aggregate range. |
| T-236-29 | Repudiation | Plan 07 pre-implementation baseline | high | mitigate | Pin the already-existing unique plan-introduction commit in a later contract commit and assert its exact one-path scope and ancestry. |
| T-236-30 | Elevation of privilege | Scope expansion into protected artifacts | high | mitigate | One identical five-path allowlist covers committed, tracked, and untracked collections; all snapshot/audit/replay/validation/verification/review/product/CI/runtime-evidence paths are excluded. |
| T-236-31 | Denial of service | Git command failure interpreted as an empty collection | medium | mitigate | Require successful ancestry, diff, log, tracked, and untracked collection commands under pipeline failure propagation before evaluating results. |
| T-236-SC | Tampering | Package installation surface | high | accept | No npm, pip, cargo, Hex, or other package installation occurs. |
</threat_model>

<verification>
Run the full Phase 236 ExUnit contract, the standalone snapshot utility suite, exact check-only formatting for all three Elixir files, the two-range committed scope collection, current tracked staged/unstaged collection, untracked non-ignored collection, and `git diff --check`. The three path collections must all succeed and use the same allowlist variable. Phase 236 has no requirement IDs, so no probe ID or predicate is created.
</verification>

<success_criteria>
- Historical verification authenticates the exact committed input/output snapshot bytes before trusting JSON fields.
- A wholly forged but self-consistent manifest/output pair fails deterministically for committed-blob mismatch.
- The current authorized history proves finalized Plan 06 amendment boundary 4aaa3a73 through completion boundary b6c67ed9 without using e9fee981.
- The Plan 07 plan-introduction commit is asserted as a separate prior boundary, avoiding any attempt to embed a commit's own SHA in itself.
- The union of both committed execution ranges, tracked staged/unstaged changes, and untracked non-ignored paths passes one identical five-path allowlist; every other path is blocked.
- No product, CI topology, required-check policy, requirement metadata, historical snapshot/replay JSON, audit report, validation/verification/review artifact, or retained runtime evidence changes.
</success_criteria>

## Artifacts this phase produces

- Updated `scripts/planning/phase-236-audit-snapshot.exs` with fixed-blob binding for historical verification
- Updated `scripts/planning/phase-236-audit-snapshot-test.exs` with a wholly forged self-consistent replacement-pair regression
- Updated `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs` with exact rewritten-history and scope-boundary assertions
- `236-07-SUMMARY.md` recording the repair, pinned Plan 07 boundary, and final deterministic commands

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Close evidence bookkeeping without product, CI-topology, or retained runtime-evidence changes | 236-07 | COVERED | Repairs only the two current provenance/scope blockers. |
| REQ | — | No Phase 236 requirement IDs or probes | 236-07 | COVERED | `requirements: []`; no identifier or predicate is fabricated. |
| RESEARCH | — | Deterministic filesystem/Git contracts and provenance preservation | 236-07 | COVERED | Fixed Git blobs, replacement attack, exact ancestry, and fail-closed scope collection. |
| CONTEXT | D-01 | Exact four SUMMARY declarations only | 236-07 | COVERED | Existing assertion retained; historical SUMMARYs are protected. |
| CONTEXT | D-02 | Exact eight traceability reconciliations | 236-07 | COVERED | Existing 24-row contract retained; REQUIREMENTS is protected. |
| CONTEXT | D-03 | Canonical lifecycle evidence | 236-07 | COVERED | Existing replay assertions remain green; validation artifacts are protected. |
| CONTEXT | D-04 | Preserve all retained evidence | 236-07 | COVERED | Identical path fence and existing digest contract block every evidence mutation. |
| CONTEXT | D-05 | Fresh audit is the sole honest verdict | 236-07 | COVERED | Historical verifier is bound to fixed committed input/output documents. |
| CONTEXT | D-06 | Operational debt remains separate | 236-07 | COVERED | Existing audit classification remains asserted; no debt item is implemented. |

Deferred product/CI/debt work and all retained-evidence rewrites remain excluded and are not coverage gaps.

<output>
Create `.planning/phases/236-closeout-evidence-reconciliation/236-07-SUMMARY.md` when done.
</output>
