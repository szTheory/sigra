---
phase: 236-closeout-evidence-reconciliation
plan: 04
type: execute
wave: 4
depends_on: [236-03]
files_modified:
  - .planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md
  - .planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
  - .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json
  - test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "D-03: Phases 231, 232, and 234 make a fresh observable transition from their exact historical pre-transition lifecycle fields through successful `$gsd-validate-phase` skill runs."
    - "Phase 230 is verified as already validated/compliant/wave-0-complete at d93bb10a^ and is not falsely replayed or promoted."
    - "D-04: existing validation audit history and all retained Phase 230-235 runtime/verification evidence remain preserved; only canonical validator output may extend the three replayed VALIDATION artifacts."
    - "A failed validation skill run retains its diagnostics and stops the sequence before later validation or audit work."
    - "The committed replay proves a bounded forward lifecycle transition; it does not claim to prove the identity of an earlier LLM invocation or retroactively authenticate the disputed 2026-08-04 edits."
  artifacts:
    - path: ".planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json"
      provides: "Committed historical lifecycle baselines, source commits, pre-run hashes, and explicit provenance limitation"
    - path: ".planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md"
      provides: "Fresh draft/false/false to validated lifecycle transition with canonical appended audit trail"
    - path: ".planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md"
      provides: "Fresh ready/true/false to validated lifecycle transition with canonical appended audit trail"
    - path: ".planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md"
      provides: "Fresh complete/true/true to validated lifecycle transition with canonical appended audit trail"
    - path: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      provides: "Deterministic lifecycle-baseline, preservation, and forward-transition contract"
  key_links:
    - from: "d93bb10a^, 06f3734c^, 2e090f6a^"
      to: ".planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json"
      via: "git-object reads of the immediately preceding committed frontmatter"
    - from: "$gsd-validate-phase 231/232/234"
      to: "231/232/234-VALIDATION.md"
      via: "installed GSD skill orchestration, not a shell command or wrapper"
    - from: "236-VALIDATION-REPLAY-BASELINE.json"
      to: "phase_236_closeout_evidence_reconciliation_contract_test.exs"
      via: "exact baseline and post-transition assertions"
---

<objective>
Re-run the disputed Nyquist lifecycle transitions honestly: preserve Phase 230's pre-existing canonical state, stage exact historical baselines for 231/232/234, then invoke the supported validation skills and validate their forward mutations.

Purpose: Close D-03's provenance gap with an observable, reproducible forward transition while stating the limit of what repository evidence can prove.
Output: A committed replay baseline, three fresh skill-driven lifecycle transitions with canonical audit trails, and a deterministic bounded-claim contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md
@.planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md
@.planning/phases/236-closeout-evidence-reconciliation/236-03-SUMMARY.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md
@.planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
@test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
@/Users/jon/.codex/gsd-core/workflows/validate-phase.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Commit the honest validation replay baseline</name>
  <files>.planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md, .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md, .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md, .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json, test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</files>
  <read_first>
    AGENTS.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md,
    .planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md,
    .planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md,
    .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md,
    .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs,
    /Users/jon/.codex/gsd-core/workflows/validate-phase.md
  </read_first>
  <behavior>
    - Test 1: `git show d93bb10a^:.../230-VALIDATION.md` reports validated/true/true, so Phase 230 is classified as pre-existing canonical state and excluded from replay.
    - Test 2: the immediately preceding committed blobs report exactly 231 draft/false/false at `d93bb10a^`, 232 ready/true/false at `06f3734c^`, and 234 complete/true/true at `2e090f6a^`.
    - Test 3: resetting the three current files changes only the lifecycle values in the first YAML document; every line after the closing frontmatter delimiter, including all existing Validation Audit sections, is byte-identical to the pre-reset working-tree file.
    - Test 4: the baseline JSON records source commit/object IDs, expected fields, complete pre-reset and staged-baseline SHA-256 values, retained-body SHA-256 values, the baseline commit SHA once committed, and the statement that it proves a forward replay but cannot authenticate an earlier LLM invocation.
  </behavior>
  <action>
    Extend the existing Phase 236 ExUnit contract with baseline parsing and lifecycle helpers before editing validation files. Read the historical blobs directly from Git objects and fail unless they match the authoritative baselines: Phase 230 at `d93bb10a^` is `validated/true/true`; Phase 231 at `d93bb10a^` is `draft/false/false`; Phase 232 at `06f3734c^` is `ready/true/false`; Phase 234 at `2e090f6a^` is `complete/true/true`. Record the resolved blob IDs and hashes in `236-VALIDATION-REPLAY-BASELINE.json`; do not substitute recalled values.

    For 231, 232, and 234 only, change the current first-frontmatter lifecycle keys to those exact historical values. Preserve every other frontmatter field and preserve the complete Markdown body byte-for-byte, including the existing 2026-08-04 audit narratives; those sections remain historical claims, not proof of this replay. Do not modify Phase 230. Refuse to proceed if any target has an unrelated pre-existing working-tree edit. Run the focused contract, then create a dedicated baseline commit containing only the three lifecycle resets, the baseline JSON, and its test changes. Record the resulting commit SHA in the baseline artifact in the immediately following skill-transition commit; do not amend or rewrite the baseline commit. Assert that it is the direct ancestor from which the skill transitions run. Do not use checkout/reset/history rewrite and do not touch the user's unrelated `.planning/config.json`, Phase 235 readiness JSON, or `.gitkeep` changes.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs --only validation_replay_baseline &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    A dedicated commit exposes the exact three pre-transition lifecycle states; Phase 230's earlier canonical state is proven from `d93bb10a^` and untouched; all existing validation bodies and retained evidence remain byte-identical; the baseline artifact names both the forward proof and its retrospective limitation.
  </acceptance_criteria>
  <done>The thinnest honest replay path is established from committed historical states without fabricating a Phase 230 promotion.</done>
</task>

<task type="auto">
  <name>Task 2: Invoke the three canonical validation skills sequentially</name>
  <precondition>The committed `236-VALIDATION-REPLAY-BASELINE.json` matches HEAD ancestry and the three target frontmatter states; Phase 230 remains validated/true/true.</precondition>
  <files>.planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md, .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md, .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md</files>
  <read_first>
    /Users/jon/.codex/gsd-core/workflows/validate-phase.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json,
    .planning/phases/231-gate-honesty-nightly-revival/231-VERIFICATION.md,
    .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VERIFICATION.md,
    .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md
  </read_first>
  <action>
    Use the installed GSD skill/agent orchestration in this live runtime to invoke `$gsd-validate-phase 231`. This is an explicit skill execution step, not a terminal command: do not call `gsd-tools validate-phase`, do not create a wrapper, do not hash a loader as a substitute, and do not represent a PATH-resolved string as evidence of execution. Allow the skill to run its configured deterministic checks and update its canonical VALIDATION artifact. Continue only when the skill returns compliant and the file is `validated/true/true` with a newly appended canonical Validation Audit section relative to the committed baseline.

    Repeat the same supported orchestration for `$gsd-validate-phase 232`, then `$gsd-validate-phase 234`, in that order. At the first blocked, partial, or failed result, retain the skill's exact artifact diagnostics and stop; do not invoke later validation or the milestone audit, do not edit a lifecycle field, and do not waive a check. Do not invoke Phase 230 validation. Preserve Phase 233/235 validation files and every Phase 230-235 VERIFICATION/runtime receipt per D-04. In the execution SUMMARY, record that the live runtime invoked these three skills and describe their returned statuses, while explicitly stating that the committed repository can demonstrate the before/after transition and canonical artifact shape but cannot cryptographically prove which earlier LLM invocation made a historical edit.
  </action>
  <verify>
    <automated>for f in .planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md; do sed -n '1,14p' "$f" | grep -qx 'status: validated' &amp;&amp; sed -n '1,14p' "$f" | grep -qx 'nyquist_compliant: true' &amp;&amp; sed -n '1,14p' "$f" | grep -qx 'wave_0_complete: true'; done</automated>
  </verify>
  <acceptance_criteria>
    The live runtime completes `$gsd-validate-phase 231`, then 232, then 234 through the installed skills; each transition starts from the committed exact baseline, ends validated/compliant/wave-0-complete, and retains a newly appended canonical audit trail. Any failure stops the chain with exact diagnostics preserved.
  </acceptance_criteria>
  <done>D-03 has a fresh, observable, correctly bounded forward validation replay for the three disputed phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce the replay boundary and preserved evidence</name>
  <files>test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</files>
  <read_first>
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs,
    .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json,
    .planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md,
    .planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md,
    .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md,
    .planning/phases/233-library-suite-economics/233-VALIDATION.md,
    .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md,
    .planning/phases/235-terminal-ratification-measured-not-read/235-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: baseline commit ancestry, source Git objects, exact historical fields, and Phase 230's pre-existing canonical classification all recompute from Git rather than trusting JSON prose.
    - Test 2: each replayed file is validated/true/true and contains its complete pre-run body plus a later canonical audit section; Phase 230/233/235 and protected verification/runtime-evidence digests retain their D-04 values.
    - Test 3: wrong baseline commit, replaying Phase 230, missing/reordered target transition, lifecycle-only manual promotion without the expected appended artifact delta, truncated prior audit history, or mutation of protected evidence fails with a targeted assertion.
    - Test 4: the contract exposes the bounded claim verbatim: current Git history proves the staged baseline and forward file mutations, not cryptographic identity of the invoking LLM and not provenance of older disputed edits.
  </behavior>
  <action>
    Complete the Phase 236 ExUnit replay assertions. Recompute the baseline Git-object values and commit ancestry, compare the retained body prefix and newly appended audit suffix for each replayed validation file, and retain the existing exact digest fence for Phase 230-235 verification/runtime evidence and already-compliant Phase 233/235 validation. Add in-memory adverse mutations for every behavior above. Do not assert a shell command line, executable hash, transcript, nonce, or invented receipt as proof that the skill ran; those would exceed what the repository can establish. The deterministic contract instead establishes exact before state, forward mutation, canonical artifact shape, ordering, and preservation, while the execution SUMMARY truthfully records the live orchestration action.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; mix format --check-formatted test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The full contract passes; adverse baseline, ordering, audit-history, manual-promotion-shape, and protected-evidence mutations fail; no assertion overclaims retrospective or cryptographic provenance.
  </acceptance_criteria>
  <done>The validator gap is closed to the strongest honest repository claim: exact baseline plus canonical forward transition, with the proof limit explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical Git objects → replay baseline | Lifecycle values must come from the exact predecessor commits, not current prose or memory. |
| Committed baseline → installed validation skills | Only supported skill orchestration may perform the fresh lifecycle transition. |
| Skill-updated artifacts → repository claim | Tests may prove file history and artifact shape, but must not overclaim cryptographic proof of LLM identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-236-13 | Tampering | Historical lifecycle baseline | high | mitigate | Resolve exact Git blobs at d93bb10a^, 06f3734c^, and 2e090f6a^; commit and test the baseline before skill invocation. |
| T-236-14 | Repudiation | Fresh skill execution claim | high | mitigate | Record the live skill action in SUMMARY and restrict durable claims to committed before/after states and canonical appended artifacts; explicitly disclaim cryptographic LLM identity proof. |
| T-236-15 | Elevation of privilege | Manual lifecycle promotion | high | mitigate | Require supported `$gsd-validate-phase` orchestration, exact ordered transitions, appended canonical audit trails, and fail-closed stop semantics. |
| T-236-16 | Tampering | Phase 230/233/235 or retained evidence | high | mitigate | Exclude Phase 230 from replay and retain exact digest assertions for compliant validation and protected verification/runtime artifacts. |
| T-236-17 | Denial of service | Validation failure | medium | mitigate | Stop at the first canonical failure, preserve diagnostics, and prohibit later validation/audit execution. |
</threat_model>

<verification>
Run the focused baseline tag before skill invocation and the full Phase 236 contract afterward. Inspect the three target frontmatter blocks and appended audit sections, recompute historical Git values and baseline ancestry, and verify protected evidence digests. No manual UAT or fabricated Phase 236 requirement probe applies.
</verification>

<success_criteria>
- Phase 230 is proven already validated/true/true at `d93bb10a^` and is never replayed.
- Phase 231 begins from draft/false/false, Phase 232 from ready/true/false, and Phase 234 from complete/true/true in a dedicated baseline commit.
- The installed `$gsd-validate-phase` skills execute sequentially for 231, 232, and 234 and append canonical audit results.
- Every successful target ends validated/true/true; any failure stops later work with exact diagnostics retained.
- Existing audit history and D-04 protected evidence remain intact.
- Artifacts and tests state that the forward transition is observable but cannot retroactively prove an earlier LLM invocation.
- No product, CI topology, required-check, runtime-evidence, or operational-debt implementation changes.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json`
- Fresh appended canonical audit trails in Phase 231, 232, and 234 VALIDATION files
- Lifecycle replay and bounded-provenance assertions in `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs`

<output>
Create `.planning/phases/236-closeout-evidence-reconciliation/236-04-SUMMARY.md` when done.
</output>
