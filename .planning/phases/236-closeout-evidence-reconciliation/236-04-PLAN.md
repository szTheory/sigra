---
phase: 236-closeout-evidence-reconciliation
plan: 04
type: execute
wave: 4
depends_on: [236-03]
files_modified:
  - scripts/planning/capture-gsd-closeout-provenance.sh
  - scripts/planning/capture-gsd-closeout-provenance.test.sh
  - .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json
  - .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-TRANSCRIPT.ndjson
  - .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-BASELINE.json
  - test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
  - .planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md
  - .planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "D-03: every validator event starts from the planner-approved Wave-0 artifact digest and a target-clean git snapshot; hashing an arbitrary worktree pre-state is not sufficient."
    - "D-03: successful canonical validate-phase invocations for 230, 231, 232, and 234 are durably bound to the lifecycle artifacts they inspected or mutated."
    - "Each success contains a newly appended canonical Validation Audit entry and a bounded before/after patch proving the invoked workflow, rather than an earlier manual edit, authored the recorded lifecycle event."
    - "A nonzero validator event is recorded with exact diagnostics and prevents every later target in that invocation chain from starting."
    - "The Phase 234 formatter failure, stop boundary, formatter correction, and successful canonical resume are reproducible from immutable git objects rather than asserted only in Markdown."
    - "D-04: Phase 233/235 canonical validation and all retained runtime/verification evidence remain outside the mutation set."
  artifacts:
    - path: "scripts/planning/capture-gsd-closeout-provenance.sh"
      provides: "Fail-closed canonical workflow capture and Phase 234 historical replay"
    - path: "scripts/planning/capture-gsd-closeout-provenance.test.sh"
      provides: "Hermetic pass/fail/stop-order/receipt-integrity coverage"
    - path: ".planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json"
      provides: "Machine-readable four-phase validation and Phase 234 stop/resume chain"
    - path: ".planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-TRANSCRIPT.ndjson"
      provides: "Sanitized request/result envelopes emitted by the pinned validator dispatch"
    - path: ".planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-BASELINE.json"
      provides: "Wave-4-approved, pre-audit file/config/hook snapshot consumed by Plan 05"
    - path: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      provides: "Adversarial provenance-to-lifecycle contract"
  key_links:
    - from: "scripts/planning/capture-gsd-closeout-provenance.sh"
      to: "/Users/jon/.codex/gsd-core/workflows/validate-phase.md"
      via: "absolute realpath-pinned Node/gsd dispatch, executable and workflow SHA-256 values, nonce-bound request/result transcript, and validate-phase-specific lifecycle output"
      pattern: "validate-phase"
    - from: ".planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json"
      to: ".planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md"
      via: "planner-approved baseline SHA-256, target-clean git assertion, workflow-bounded patch, newly appended Validation Audit entry, after SHA-256, and canonical lifecycle postcondition"
      pattern: "artifact_sha256"
    - from: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      to: ".planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json"
      via: "exact phase set, hash chain, source digest, exit result, and adverse mutations"
      pattern: "VALIDATION-PROVENANCE"
---

<objective>
Close the two validator-provenance failures from `236-VERIFICATION.md` with a real capture seam, fresh canonical runs, and a reproducible Phase 234 stop/resume chain per D-03 and D-04.

Purpose: Make lifecycle provenance mechanically distinguishable from a hand-edited `status: validated` field while preserving the already-ratified product, CI topology, and runtime evidence.
Output: A tested capture utility, one machine-readable validation provenance manifest, fresh canonical validator events for 230/231/232/234, and an adversarial ExUnit contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md
@.planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md
@.planning/phases/236-closeout-evidence-reconciliation/236-02-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
@test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
@/Users/jon/.codex/gsd-core/workflows/validate-phase.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Capture one canonical lifecycle path and fail closed under a stubbed validator</name>
  <reversibility rating="reversible">The utility and its tests are repository-local evidence tooling with no published interface, schema migration, or runtime consumer.</reversibility>
  <files>scripts/planning/capture-gsd-closeout-provenance.sh, scripts/planning/capture-gsd-closeout-provenance.test.sh</files>
  <read_first>
    AGENTS.md,
    /Users/jon/.codex/gsd-core/workflows/validate-phase.md,
    scripts/ci/docs-only-receipt.sh,
    scripts/ci/docs-only-receipt.test.sh,
    scripts/ci/ci-run-metrics.test.sh,
    .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: a successful validate-phase stub produces one atomic JSON event containing the exact phase, exact invocation argv, workflow-source digest, start/end UTC values, exit 0, output digest, repository commit, artifact before/after digests, and validated/compliant/wave-0 postconditions.
    - Test 2: a nonzero stub produces a blocked event with the exact exit value and diagnostic digest, returns nonzero, leaves lifecycle postconditions unsatisfied, and records zero invocations for every later phase in the requested chain.
    - Test 3: missing artifact, unsupported phase, malformed prior manifest, missing canonical workflow source, or output that claims success without canonical lifecycle fields exits nonzero without a success receipt.
    - Test 4: a second event extends a SHA-256 predecessor chain; deletion, reordering, command substitution, or artifact-hash mutation invalidates the chain.
    - Test 5: production dispatch never resolves `gsd` from PATH: the receipt records the canonicalized absolute Node interpreter and gsd loader paths and SHA-256 values, while a PATH-first fake is not executed; changing either path/hash, the invocation nonce, the request envelope, or the result envelope invalidates the receipt.
    - Test 6: if any target's `status`, `nyquist_compliant`, or `wave_0_complete` field is edited after the approved baseline but before capture starts, capture stops before invoking the fixture validator and emits no successful event.
    - Test 7: a zero exit without one newly appended `## Validation Audit` section attributable to the bounded command interval, or with a patch outside the workflow-owned lifecycle/audit regions, is blocked even when the final lifecycle fields are green.
  </behavior>
  <action>
    Create `scripts/planning/capture-gsd-closeout-provenance.sh` with strict shell mode and a narrow `validate-chain` interface that accepts only the ordered subset `230,231,232,234`, resolves each existing phase directory and `*-VALIDATION.md`, and invokes the installed GSD runtime in print/no-session mode with the exact `$gsd-validate-phase N` capability request. Hard-code the planner-approved Wave-0 digest map for the four lifecycle artifacts: Phase 230 `a48e94fdee7981c93041b951c69a6e1fdd4ce0243c3ebbc25427be79b1c0518b`, Phase 231 `c3cef8f5f75513914c1331ab4a22e64883419f1beac4cd465205b925887f2036`, Phase 232 `aeaf2db1005b3066015dfd26055974d3e3cf5ac1c5a0f4e707f9191df07b8ec9`, and Phase 234 `cfcdbd03e4e2e4cb3e0b30a91d403da48ebb9da04448682c3e5fae8a23c5bc0d`. Before each target is invoked, require its current bytes to match that approved digest or the predecessor event's workflow-owned after-digest, as applicable, and require `git diff --quiet` plus `git diff --cached --quiet` for that target. Record the baseline commit and `git ls-tree` blob identity. A mismatch is a preflight failure: do not invoke the validator and do not create a success event. This prevents an arbitrary manually edited before-state from becoming trusted merely because it was hashed.

    Resolve the trusted runtime before execution to the real, absolute Node interpreter and real gsd loader (for the current asdf installation, resolve past both `/Users/jon/.asdf/shims/gsd` and the bin symlink); invoke those absolute paths directly and record their SHA-256 values. Never execute a bare `gsd`, search PATH after resolution, or accept a receipt whose dispatch path differs from the executed argv. Create a fresh nonce for each invocation and write a sanitized NDJSON transcript envelope containing the exact request bytes, nonce, argv, runner identities, start/end markers, exit result, and command-specific lifecycle fields; bind its SHA-256 into the JSON event. Capture combined command output to a temporary file, never environment variables or credentials. For every attempted phase, atomically append an event to the caller-supplied JSON manifest containing schema version, capability, target, exact argv, pinned dispatch identities, approved baseline digest and git blob, canonical workflow path and SHA-256, transcript path/range/digest/nonce, UTC bounds, exit status, sanitized output SHA-256, git HEAD before/after, artifact path and before/after SHA-256, a normalized patch digest, the newly appended Validation Audit section digest, parsed lifecycle fields, and the prior event digest. A zero exit is success only when the nonce-bound result envelope is complete, the target artifact exists and reads `status: validated`, `nyquist_compliant: true`, and `wave_0_complete: true`, and comparison with the approved pre-state proves exactly one new canonical `## Validation Audit` section was appended during the synchronous command interval. Permit only canonical lifecycle-header changes plus that appended audit section; any pre-existing green-field edit, missing/duplicate audit append, or out-of-region mutation is blocked. On any blocked event, stop the chain before invoking the next phase. Do not add a mode that edits lifecycle files directly.

    Create `scripts/planning/capture-gsd-closeout-provenance.test.sh` using a `mktemp -d` sandbox and an explicitly supplied absolute fixture runner plus a hostile PATH-first `gsd` fake. Drive RED then GREEN for every behavior above, including proof that only the absolute fixture ran, an argv log proving later targets were never called after failure, nonce-bound transcript assertions, and adverse JSON mutations proving dispatch identity and the event hash chain are substantive. Add three explicit pre-capture attacks that change `status`, `nyquist_compliant`, and `wave_0_complete` independently after fixture baseline creation; each must fail before the argv log is written. Add zero-exit attacks with no new audit entry, a pre-existing manually appended entry, and a mutation outside the allowed lifecycle/audit regions. The fixture override must be enabled only by an explicit test-only environment gate that production mode rejects. Keep the test offline, deterministic, and free of sleeps.
  </action>
  <verify>
    <automated>bash scripts/planning/capture-gsd-closeout-provenance.test.sh</automated>
  </verify>
  <acceptance_criteria>
    The hermetic suite exits 0; pass and failure receipts are both machine-readable; the failure case proves zero later invocations; the hostile PATH shim is never called; every pre-capture lifecycle edit is rejected before invocation; success cannot be recorded without the planner-approved baseline, target-clean git state, pinned executable identities, a nonce-bound request/result transcript, one newly appended canonical Validation Audit section, an allowed-region patch, and the three canonical lifecycle fields; the utility contains no lifecycle-edit operation and makes no GitHub/API call.
  </acceptance_criteria>
  <done>A production-quality tracer now binds one canonical validator invocation through command execution, artifact mutation, receipt emission, and deterministic verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture fresh canonical validation provenance for Phases 230 and 231</name>
  <reversibility rating="reversible">Fresh validation audit entries and their receipt events are reviewable repository evidence and can be regenerated from the same sources.</reversibility>
  <files>.planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json, .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-TRANSCRIPT.ndjson, .planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md, .planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md</files>
  <read_first>
    scripts/planning/capture-gsd-closeout-provenance.sh,
    scripts/planning/capture-gsd-closeout-provenance.test.sh,
    .planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md,
    .planning/phases/230-tier-1-critical-path-reclamation/230-VERIFICATION.md,
    .planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md,
    .planning/phases/231-gate-honesty-nightly-revival/231-VERIFICATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: the manifest contains exactly one successful fresh event for Phase 230 followed by exactly one for Phase 231.
    - Test 2: each event's workflow digest matches the installed canonical workflow and its after digest matches the current target VALIDATION artifact.
    - Test 3: the existing Phase 236 immutable-evidence checks stay green after both canonical invocations.
  </behavior>
  <action>
    Run the tested utility's `validate-chain` mode for exactly `230,231`, writing a new `236-VALIDATION-PROVENANCE.json`. The utility must first match each planner-approved Wave-0 digest and target-clean git state, then let the canonical capability alone update the lifecycle artifact per D-03. Require each success to carry the newly appended Validation Audit section and allowed-region patch evidence. If Phase 230 blocks, retain its blocked receipt and diagnostics and stop without Phase 231; if Phase 231 blocks, retain both the prior Phase 230 success and Phase 231 failure, then stop the plan. Do not translate failure into approval prose, manually edit lifecycle flags, collect new CI evidence, or touch Phase 233/235 per D-04. After both succeed, independently compare each receipt's baseline/after/patch/audit-entry digests to the approved and current artifacts and run the receipt utility's hermetic suite.
  </action>
  <verify>
    <automated>bash scripts/planning/capture-gsd-closeout-provenance.test.sh &amp;&amp; jq -e '.events | map(.target) == ["230","231"] and all(.[]; .capability == "validate-phase" and .exit_status == 0 and .result == "validated" and .lifecycle.status == "validated" and .lifecycle.nyquist_compliant == true and .lifecycle.wave_0_complete == true)' .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json</automated>
  </verify>
  <acceptance_criteria>
    The receipt contains exactly the ordered 230/231 success events, each is bound to the canonical workflow digest and current artifact bytes, and a failure would have left a durable blocked event while preventing continuation.
  </acceptance_criteria>
  <done>D-03 validator ownership is machine-verifiable for Phases 230 and 231 without changing D-04 protected proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete the validator chain and prove the Phase 234 stop/resume history</name>
  <reversibility rating="reversible">The new manifest and contract are reproducible evidence; historical git objects and protected runtime receipts are read-only inputs.</reversibility>
  <files>scripts/planning/capture-gsd-closeout-provenance.sh, scripts/planning/capture-gsd-closeout-provenance.test.sh, .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json, .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-TRANSCRIPT.ndjson, .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-BASELINE.json, .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md, .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md, test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</files>
  <read_first>
    .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json,
    scripts/planning/capture-gsd-closeout-provenance.sh,
    scripts/planning/capture-gsd-closeout-provenance.test.sh,
    .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md,
    .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-02-SUMMARY.md,
    .planning/ROADMAP.md,
    .planning/PROJECT.md,
    .planning/STATE.md,
    .planning/REQUIREMENTS.md,
    .planning/config.json,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs,
    /Users/jon/.codex/gsd-core/workflows/validate-phase.md
  </read_first>
  <behavior>
    - Test 1: the canonical event set is exactly 230, 231, 232, and 234 in order, with successful lifecycle/artifact/workflow bindings and an intact predecessor digest chain.
    - Test 2: the Phase 234 history section names existing commits `fd198c2b`, `40ceb739`, and `2e090f6a` in ancestry order and binds the blocked diagnostic bytes, three-file formatter set, correction diff, and resumed validated artifact to immutable git objects.
    - Test 3: a detached replay of `mix format --check-formatted` at the blocked snapshot exits nonzero with the exact three Phase 235 contract paths, while the correction snapshot exits zero; the replay outputs and exit values are stored in the manifest.
    - Test 4: wrong phase order, removed failure, a claimed audit start before the resume, altered workflow/artifact digest, or altered replay exit/result fails the ExUnit contract.
    - Test 5: hermetic `phase-234-history` fixtures reject a missing commit, broken ancestry, unexpected blocked-path set, blocked exit 0, correction exit nonzero, transcript mutation, and cleanup outside the created temporary worktree paths.
  </behavior>
  <action>
    First extend `scripts/planning/capture-gsd-closeout-provenance.sh` with an explicit narrow `phase-234-history` mode and extend `scripts/planning/capture-gsd-closeout-provenance.test.sh` with hermetic git-fixture coverage for that mode. The implementation must build the history section from git objects, not copied prose: require `fd198c2b` (blocked validation record), `40ceb739` (formatter correction), and `2e090f6a` (successful resume) to exist and be ancestor-ordered; hash the Phase 234 validation artifact at the relevant commits; create temporary detached worktrees for the blocked and correction snapshots; reuse the current repository's Mix dependency/build caches without changing either snapshot; execute `mix format --check-formatted`; require the blocked snapshot to return nonzero and name exactly the three Phase 235 planning contracts, and require the correction snapshot to return zero. Record exact exits/output digests in nonce-bound transcript/history entries and clean up only the explicit temporary worktrees. The hermetic suite must cover the successful history path and every failure in Test 5 before the real history capture is allowed.

    Then append fresh canonical validator events for exactly `232,234` through the tested `validate-chain` utility. Require each untouched target to match its planner-approved Wave-0 digest and clean git state before invocation, and require the resulting event to contain the workflow-bounded newly appended Validation Audit section. Stop at the first nonzero result and retain its exact receipt. On success, run the newly tested `phase-234-history` mode against the named immutable commits. This is a deterministic replay of retained repository state, not new runtime/CI evidence.

    Extend `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs` to parse the final manifest and transcript and enforce every behavior above, including independent recomputation of approved-baseline/git-blob/executable/workflow/transcript/artifact/patch/audit-entry hashes and deliberate in-memory mutations for pre-capture edits to each lifecycle field, manual lifecycle promotion, missing or pre-existing Validation Audit entries, out-of-region patches, missing/extra/reordered phase events, broken predecessor hashes, command/workflow substitution, nonce or transcript substitution, wrong artifact digests, absent failure, incorrect three-file diagnostic set, bad ancestry, and a downstream audit timestamp that precedes the successful resume. Preserve all existing D-01/D-02/D-04 tests. Run RED before the manifest is complete, then GREEN after the real receipts exist. Do not hard-code a new runtime evidence population or alter the historical Phase 234 narrative.

    After the four validator events and history replay are green, create `.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-BASELINE.json` for Plan 05. Inventory every canonical audit input named or resolved by `audit-milestone.md`: `AGENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, all scoped Phase 230-235 PLAN/SUMMARY/VERIFICATION/VALIDATION files, the canonical audit workflow and resolved gsd loader, `.planning/config.json`, and every optional local/global config source that the resolver reports present. Also record canonical normalized outputs and SHA-256 values for `init.milestone-op`, `phases.list`, each `find-phase` result, `loop render-hooks verify:post --raw`, `agent-skills gsd-integration-checker`, and `resolve-model gsd-integration-checker --raw`. Record path-sorted file entries, absent/present markers for optional inputs, a complete manifest digest, and the successful validation-chain digest. Require all target lifecycle files and all other clean inputs to match git blobs; if a configuration input is already dirty for unrelated work, bind its exact worktree SHA-256 plus staged/unstaged patch digests explicitly without modifying or staging it. This baseline is Plan 05's approved wave-boundary snapshot; Plan 05 must reject any later mutation.
  </action>
  <verify>
    <automated>bash scripts/planning/capture-gsd-closeout-provenance.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; mix format --check-formatted test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    The manifest and ExUnit contract prove four fresh canonical successes from the approved clean target baselines, exact workflow/artifact/patch/new-audit-entry bindings, an unbroken event chain, the reproducible Phase 234 formatter failure with no downstream invocation, the correction, and the later successful resume; the complete tamper-evident Plan 05 audit-input baseline exists; existing reconciliation and immutable-evidence tests remain green.
  </acceptance_criteria>
  <done>Both failed validator truths in `236-VERIFICATION.md` are closed with durable execution provenance and fail-closed stop/resume evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical GSD workflow → mutable VALIDATION artifact | The lifecycle claim is trusted only when the captured command, workflow source, outcome, and resulting bytes agree. |
| Historical git objects → Phase 234 replay receipt | Immutable commits are replayed in isolated worktrees; current product/runtime evidence is never modified. |
| Capture output → ExUnit verifier | JSON is untrusted input and must fail on substitution, deletion, reordering, or hash mismatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-236-13 | Tampering | Validator dispatch, receipt, transcript, or lifecycle artifact | high | mitigate | Absolute realpath-pinned interpreter/loader identities, exact argv, nonce-bound transcript, workflow SHA-256, before/after artifact SHA-256, lifecycle parsing, atomic writes, and adversarial mutations. |
| T-236-13A | Tampering | Validator pre-state | high | mitigate | Planner-approved Wave-0 hashes, target-clean index/worktree assertions, git blob binding, and pre-capture lifecycle-field attacks prevent laundering a manual edit as validator output. |
| T-236-14 | Repudiation | Phase 234 blocked/resumed sequence | high | mitigate | Git-object ancestry plus detached command replay pins the diagnostic, exit values, correction, and successful resume. |
| T-236-15 | Elevation of privilege | Manual `validated` field presented as workflow success | high | mitigate | Success requires a zero canonical invocation, canonical source digest, matching artifact bytes, and the three lifecycle postconditions. |
| T-236-16 | Denial of service | Validator chain or historical replay | medium | mitigate | Fixed four-phase allowlist, no sleeps/network polling, stop at first failure, and explicit temporary-worktree cleanup. |
| T-236-17 | Tampering | Scope expansion into CI/product/runtime evidence | high | mitigate | File allowlist and D-04 digest contract exclude workflows, product code, Phase 233/235 validation, VERIFICATION reports, and retained receipts. |
</threat_model>

<verification>
Run the hermetic capture suite, the expanded Phase 236 ExUnit contract, scoped formatter, and `git diff --check`. Confirm the manifest's target set is exactly 230/231/232/234 and all protected digests from Plan 01 still match.
</verification>

<success_criteria>
- Four fresh canonical validator events are bound to their installed workflow source and current lifecycle artifact bytes.
- Every event begins from its planner-approved Wave-0 digest and clean target state and proves a newly appended workflow-owned Validation Audit entry.
- A blocked validator event is durable and mechanically prevents later targets in the chain.
- Phase 234's exact formatter failure and later clean resume are reproducible from immutable git history with real exit outcomes.
- Phase 233/235 validation, Phase 230-235 verification, retained receipts, CI topology, and product/runtime files are unchanged.
</success_criteria>

## Artifacts this phase produces

- `scripts/planning/capture-gsd-closeout-provenance.sh`
- `scripts/planning/capture-gsd-closeout-provenance.test.sh`
- `.planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-PROVENANCE.json`
- `.planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-TRANSCRIPT.ndjson`
- `.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-BASELINE.json`
- `validate_provenance!/1`, `validate_event_chain!/1`, and `validate_phase_234_history!/1` private contract helpers in `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs`
- Fresh canonical audit-trail mutations, if emitted by the canonical validator, in `230-VALIDATION.md`, `231-VALIDATION.md`, `232-VALIDATION.md`, and `234-VALIDATION.md`

<output>
Create `.planning/phases/236-closeout-evidence-reconciliation/236-04-SUMMARY.md` when done.
</output>
