---
phase: 236-closeout-evidence-reconciliation
plan: 06
type: execute
wave: 6
depends_on: [236-05]
files_modified:
  - test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
  - scripts/planning/phase-236-audit-snapshot.exs
  - scripts/planning/phase-236-audit-snapshot-test.exs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "D-01 and D-02 remain reconciled exactly as executed: four SUMMARY declarations and eight traceability rows, with no new ownership or completion claim."
    - "D-03 validation replay proves the historical Phase 231 mixed boundary and the isolated Phase 232/234 transitions without requiring later HEAD metadata to equal historical blobs."
    - "D-04 protected Phase 230-235 verification, validation, receipt, attestation, audit-snapshot, and audit-report artifacts remain byte-for-byte unchanged."
    - "D-05 audit evidence has a deterministic historical pre/post boundary proof whose result remains green after legitimate later ROADMAP/STATE completion changes."
    - "D-06 operational debt remains outside the closeout gate unless the retained canonical audit classifies it as a gap."
    - "The contract and both snapshot scripts are formatter-clean, warning-free, deterministic, and fail closed on boundary, digest, scope, or provenance mutations."
  artifacts:
    - path: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      provides: "Green end-to-end ownership, traceability, protected-evidence, validation-replay, and historical audit-boundary contract"
    - path: "scripts/planning/phase-236-audit-snapshot.exs"
      provides: "Snapshot utility with deterministic historical audit-boundary verification"
    - path: "scripts/planning/phase-236-audit-snapshot-test.exs"
      provides: "Hermetic positive and adversarial coverage for snapshot creation and historical boundary verification"
  key_links:
    - from: ".planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json"
      to: "test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs"
      via: "Git-object assertions scoped to the recorded historical commits rather than current HEAD"
      pattern: "validation_replay_recovery"
    - from: ".planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json"
      to: ".planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json"
      via: "historical freeze-to-audit commit ancestry, input-manifest equality, source-path stability, and audit digest verification"
      pattern: "input_manifest_sha256"
---

<objective>
Repair the completed Phase 236 deterministic gate so it proves the intended historical validation and audit boundaries at current HEAD without weakening any fail-closed evidence guarantee.

Purpose: Close the sole verification gap caused by comparing legitimate post-audit metadata changes to historical blobs, while retaining the exact executed D-01 through D-06 outcomes and all protected evidence.
Output: Formatter-clean snapshot utilities and a green end-to-end Phase 236 reconciliation contract with durable historical pre/post audit-boundary proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md
@.planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md
@.planning/phases/236-closeout-evidence-reconciliation/236-04-SUMMARY.md
@.planning/phases/236-closeout-evidence-reconciliation/236-05-SUMMARY.md
@.planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json
@.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json
@.planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json
@test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs
@scripts/planning/phase-236-audit-snapshot.exs
@scripts/planning/phase-236-audit-snapshot-test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove validation replay and audit snapshots at their historical boundaries</name>
  <precondition>Commit a52dfecb exists, contains the completed 236-05 closeout, and the worktree has no edits to the three production/test files owned by this plan.</precondition>
  <reversibility rating="reversible">The repair changes only local deterministic test and snapshot utility logic; all retained evidence inputs and generated verdict artifacts stay immutable.</reversibility>
  <files>test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs, scripts/planning/phase-236-audit-snapshot.exs, scripts/planning/phase-236-audit-snapshot-test.exs</files>
  <read_first>
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VALIDATION-REPLAY-BASELINE.json,
    .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-INPUT-SNAPSHOT.json,
    .planning/phases/236-closeout-evidence-reconciliation/236-AUDIT-OUTPUT-SNAPSHOT.json,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs,
    scripts/planning/phase-236-audit-snapshot.exs,
    scripts/planning/phase-236-audit-snapshot-test.exs
  </read_first>
  <behavior>
    - Test 1: the Phase 231 replay still requires commit fe8e4305 to contain the recorded protected blob IDs, exact five-path mixed scope, direct parent, and lifecycle delta; later HEAD values for STATE/config/readiness/.gitkeep are allowed to differ because they are outside that historical assertion.
    - Test 2: Phase 232 commit 494d6765 and Phase 234 commit 0e008446 remain isolated one-path validator transitions with their recorded parents, lifecycle deltas, retained-body hashes, and protected-boundary checks.
    - Test 3: the input snapshot is introduced by freeze commit 22dfd088, binds starting commit 4396e74e, and the output snapshot plus audit report are introduced/modified by audit commit a523575d in the exact recorded ancestry and path scopes.
    - Test 4: every repository input listed by the input snapshot hashes to the recorded bytes at the snapshot starting commit, none changes before the audit-output commit, the output snapshot references the exact input manifest, and the audit report at the audit-output commit hashes to the recorded output digest.
    - Test 5: boundary verification is independent of current ROADMAP/STATE bytes and rejects an altered commit, parent, path set, input digest, output digest, audit digest, or extra source mutation.
    - Test 6: provenance text continues to state that repository history binds deterministic sources and outputs but does not authenticate an invoking LLM or fabricate an identity claim.
  </behavior>
  <action>
    Start RED by replacing the current Phase 231 `HEAD:path` equality with commit-scoped assertions that distinguish historical replay evidence from mutable completion metadata. Keep equality at fe8e4305 for the four recorded mixed-commit blobs and retain the exact parent, five-path scope, and lifecycle assertions. Do not delete or relax the Phase 232/234 isolated-transition checks. This implements D-03/D-04 at the boundary each claim actually covers, rather than treating current HEAD as the historical validator commit.

    Extend `Phase236AuditSnapshot` with a read-only historical-boundary verification entry point that accepts the committed input snapshot, committed output snapshot, freeze commit, and audit-output commit. Recompute the input snapshot's canonical manifest digest from its stored content; use Git objects to verify the snapshot starting commit, freeze/audit ancestry, exact commit path scopes, each repository input digest at the starting commit, absence of changes to those source paths through the audit-output commit, and the audit report digest at the audit-output commit. Verify the absolute installed workflow input against its recorded digest without claiming Git history for a file outside this repository. Verify stored resolver rows structurally and through the manifest binding; do not rerun them and misrepresent current resolver output as historical output. Keep the ordinary current-snapshot `compare` mode for callers that intentionally maintain a mutable snapshot, but the completed Phase 236 gate must use the historical-boundary mode so normal later STATE/ROADMAP changes cannot invalidate an already-frozen audit.

    Add hermetic positive and adverse tests for the historical mode. Construct adverse snapshot/output copies in a temporary directory and prove wrong ancestry, source digest, input-manifest link, audit digest, and unexpected changed-path scope all return nonzero. Do not edit the committed input/output snapshots, milestone audit, replay baseline, SUMMARYs, REQUIREMENTS, VALIDATION, VERIFICATION, runtime receipts, CI workflows, product code, or repository configuration. D-01/D-02/D-05 remain the executed exact claims, and D-06 remains debt because the retained audit says so; this task adds no requirement ID, probe predicate, runtime evidence, or identity assertion.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs --only validation_replay_recovery --only audit_input_snapshot --only audit_output_snapshot &amp;&amp; elixir scripts/planning/phase-236-audit-snapshot-test.exs</automated>
  </verify>
  <acceptance_criteria>
    Historical replay and audit-boundary tests pass from current HEAD; the tests fail for mutated boundaries or digests; current completion metadata may differ from historical blobs without weakening commit-scoped D-03/D-04 assertions; all protected evidence files remain unchanged.
  </acceptance_criteria>
  <done>The contract proves the executed validation chain and source-bound D-05 audit at their durable historical boundaries without fabricating provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal the formatter-clean end-to-end closeout gate and scope fence</name>
  <reversibility rating="reversible">Formatting and dead-helper cleanup are local mechanical changes in the same three owned files.</reversibility>
  <files>test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs, scripts/planning/phase-236-audit-snapshot.exs, scripts/planning/phase-236-audit-snapshot-test.exs</files>
  <read_first>
    AGENTS.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-CONTEXT.md,
    .planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION.md,
    test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs,
    scripts/planning/phase-236-audit-snapshot.exs,
    scripts/planning/phase-236-audit-snapshot-test.exs
  </read_first>
  <behavior>
    - Test 1: all seven existing end-to-end ownership, traceability, protected-evidence, replay, input-snapshot, and output-snapshot cases pass together at completed HEAD.
    - Test 2: all three owned Elixir files pass `mix format --check-formatted` and compile without unused private-helper warnings.
    - Test 3: the completed change set contains only the three owned implementation/test paths plus Phase 236 plan, summary, and verifier bookkeeping; product, CI topology, repository config, and retained evidence paths have no diff from completed Plan 05 commit a52dfecb.
  </behavior>
  <action>
    Remove the unused `retained_body_sha256!/1` and `retained_body!/1` helpers if Task 1 does not use them; otherwise exercise them directly in the isolated Phase 232/234 retained-body assertions. Run `mix format` on exactly the contract and two snapshot scripts, then use check-only formatting in the final gate. Preserve the existing exact D-01 SUMMARY ownership map, D-02 24-row traceability map, D-04 digest allowlist, D-05 24/24–6/6–8/8–7/7 audit assertions, and D-06 debt classification checks.

    Run the full focused contract and snapshot utility suite. Compare `a52dfecb..HEAD` names against the allowlist consisting only of these three files and Phase 236 `236-06-PLAN.md`, `236-06-SUMMARY.md`, and verifier bookkeeping. Treat any product path, `.github/workflows/**`, `.planning/config.json`, Phase 230-235 SUMMARY/VERIFICATION/VALIDATION artifact, Phase 235 receipt/attestation, Phase 236 replay/snapshot JSON, or v1.47 audit change as a blocking scope violation. Do not repair a violation by refreshing protected digests; restore the unintended edit and rerun the gate.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs &amp;&amp; elixir scripts/planning/phase-236-audit-snapshot-test.exs &amp;&amp; mix format --check-formatted test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs scripts/planning/phase-236-audit-snapshot.exs scripts/planning/phase-236-audit-snapshot-test.exs &amp;&amp; test -z "$(git diff --name-only a52dfecb..HEAD | grep -Ev '^(test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test\.exs|scripts/planning/phase-236-audit-snapshot\.exs|scripts/planning/phase-236-audit-snapshot-test\.exs|\.planning/phases/236-closeout-evidence-reconciliation/236-06-(PLAN|SUMMARY)\.md|\.planning/phases/236-closeout-evidence-reconciliation/236-VERIFICATION\.md)$')" &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The full contract reports zero failures and no compiler warnings; snapshot tests pass; all three files are formatter-clean; the post-Plan-05 diff contains no product, CI, config, retained runtime evidence, protected planning evidence, or regenerated-audit change.
  </acceptance_criteria>
  <done>Phase 236 has one green, deterministic, fail-closed end-to-end gate at completed HEAD with an explicit negative scope proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable repository HEAD → historical validation claims | Normal completion metadata can change after the validator commits and must not be mistaken for tampering with historical Git objects. |
| Stored snapshot JSON → historical audit verdict | Mutable JSON prose cannot attest itself; Git ancestry, file scopes, source hashes, manifest links, and output digest must be recomputed. |
| Test repair → protected evidence | A convenient snapshot refresh or digest update could erase the discrepancy instead of proving the original audit boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-236-21 | Tampering | Phase 231 protected mixed-commit blobs | high | mitigate | Assert blobs at fe8e4305 and exact commit scope/parent; remove only the invalid current-HEAD equality. |
| T-236-22 | Tampering | Input/output audit snapshots | high | mitigate | Recompute canonical manifest, historical source hashes, ancestry, unchanged source range, manifest linkage, and audit-output digest without editing snapshots. |
| T-236-23 | Repudiation | Audit/validator provenance | high | mitigate | Retain bounded claim-limit text and prove repository history only; reject any LLM identity or invocation-authentication claim. |
| T-236-24 | Elevation of privilege | Scope expansion into product/CI/config/evidence | high | mitigate | Exact post-Plan-05 changed-path allowlist blocks every out-of-scope path and prohibits digest refresh as remediation. |
| T-236-25 | Denial of service | Current mutable metadata invalidates frozen audit forever | medium | mitigate | Verify the historical freeze-to-output range; reserve current compare mode for explicitly maintained snapshots only. |
| T-236-SC | Tampering | Package installation surface | high | accept | No npm, pip, cargo, Hex, or other package installation occurs in this plan. |
</threat_model>

<verification>
Run the focused replay/snapshot tests first, then the complete Phase 236 contract, hermetic snapshot utility tests, exact formatter gate, changed-path scope fence, and `git diff --check`. Phase 236 has no requirement IDs, so no probe predicate is created.
</verification>

<success_criteria>
- Current HEAD passes the complete Phase 236 reconciliation contract with zero failures and no unused-helper warnings.
- Historical validation and audit claims are checked against their recorded commits/ranges, not mutable current metadata.
- Adverse boundary, source, manifest, output, and scope mutations fail deterministically.
- The three owned Elixir files pass exact check-only formatting.
- D-01 through D-06 remain exactly as executed, and every protected product, CI, config, runtime-evidence, and planning-evidence artifact remains byte-for-byte unchanged.
</success_criteria>

## Artifacts this phase produces

- Updated `test/sigra/planning/phase_236_closeout_evidence_reconciliation_contract_test.exs` with commit-scoped replay and historical audit-boundary assertions
- Updated `scripts/planning/phase-236-audit-snapshot.exs` with read-only historical boundary verification
- Updated `scripts/planning/phase-236-audit-snapshot-test.exs` with deterministic positive and adversarial boundary coverage
- `236-06-SUMMARY.md` recording the repair and exact verification commands

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reconcile and canonically validate closeout evidence without product, CI topology, or retained runtime-evidence changes | 236-06 | COVERED | Repairs the sole red completed-HEAD gate while preserving executed outputs. |
| REQ | — | No Phase 236 requirement IDs or probe predicates | 236-06 | COVERED | `requirements: []` remains explicit; no ID is fabricated. |
| RESEARCH | — | Deterministic filesystem/Git contracts and fail-closed provenance preservation | 236-06 | COVERED | Historical commit/range proof replaces invalid current-HEAD equality. |
| CONTEXT | D-01 | Exact four SUMMARY declarations only | 236-06 | COVERED | Existing ownership assertions retained; SUMMARY files are protected from edits. |
| CONTEXT | D-02 | Exact eight traceability reconciliations under three-source support | 236-06 | COVERED | Existing 24-row/three-source assertions retained; REQUIREMENTS is protected. |
| CONTEXT | D-03 | Canonical 230/231/232/234 lifecycle evidence | 236-06 | COVERED | Replay checks remain exact and commit-scoped. |
| CONTEXT | D-04 | Preserve compliant and retained evidence byte-for-byte | 236-06 | COVERED | Digest checks plus negative changed-path fence block refresh or mutation. |
| CONTEXT | D-05 | Fresh audit is the honest terminal verdict | 236-06 | COVERED | Historical source/output boundary and retained audit digest are verified. |
| CONTEXT | D-06 | Operational debt is nonblocking unless audited as a gap | 236-06 | COVERED | Retained audit classification is asserted; debt is not implemented. |

Deferred product/CI/debt work remains excluded and is not a coverage gap.

<output>
Create `.planning/phases/236-closeout-evidence-reconciliation/236-06-SUMMARY.md` when done.
</output>
