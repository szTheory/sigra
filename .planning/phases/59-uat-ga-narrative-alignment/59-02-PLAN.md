---
phase: 59
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/v1.4-GA-UAT.md
  - .planning/uat-evidence/v1.4/GA-03/waiver.md
  - .planning/uat-evidence/v1.4/INDEX.md
  - docs/ga-evidence.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - OA-02
must_haves:
  truths:
    - "**GA-03** row **`CI_substitute`** names **`Sigra.OAuthTest`** and **`Sigra.OAuthCeremonyAuditTest`** with **layered** machine story; **Notes** retain live Google waived + residual; no single **`MockStrategy`** claim covering both files unless code refactored (**D-59-02**)."
    - "**`GA-03/waiver.md`** **compensating_controls** cites **`oauth_ceremony_audit_test.exs`** / ceremony audit module in addition to **`Sigra.OAuthTest`**, still framed as **not** live Google (**D-59-04**)."
    - "**`docs/ga-evidence.md`** adds a **Where to read next** bullet with **absolute** `https://github.com/sztheory/sigra/blob/` URLs to **`docs/uat-ci-coverage.md`** (and matrix if per **D-59-08**)."
    - "**`.planning/PROJECT.md`** **Target features** bullet no longer uses ambiguous **`AUD-03 / OAuth ceremony not claimed`** hook; uses **OA-01 / OA-02** + **v1.3** qualifier vocabulary (**D-59-05**)."
  artifacts:
    - path: ".planning/v1.4-GA-UAT.md"
      provides: "GA-03 matrix honesty vs tests"
    - path: ".planning/uat-evidence/v1.4/GA-03/waiver.md"
      provides: "Waiver compensating controls aligned with OA-01"
    - path: "docs/ga-evidence.md"
      provides: "Hex reader path to OA-02 hub"
nyquist_compliant: false
---

<objective>
Align **GA-03** matrix, **waiver**, optional **evidence INDEX**, **`docs/ga-evidence.md`**, and living **`.planning/PROJECT.md`** / **`.planning/MILESTONES.md`** narrative with **`docs/uat-ci-coverage.md`** after Plan **01** — **OA-02** success criteria (2)(3) plus **AUD-03** disambiguation (**59-CONTEXT D-59-02–D-59-08**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-59-01 | Information disclosure | Matrix / waiver imply live Google or full E2E is CI-closed | high | Preserve **Waived** + residual; layer **contract** vs **audit persistence**; grep forbid phrases (**acceptance_criteria**). |
| T-59-02 | Tampering | Triple SHA drift across matrix / Notes / waiver | medium | Executor picks **one** SHA strategy or date-softening per **D-59-04** and applies consistently. |
| T-59-03 | Information disclosure | Bare **AUD-03** reused for OAuth story | medium | **PROJECT.md** uses **OA-01** / **v1.3 AUD-03 (OAuth deferred)** vocabulary; **MILESTONES** addendum time-versions v1.3 fact (**D-59-06**). |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Layer GA-03 CI_substitute + Notes in v1.4-GA-UAT.md</name>
  <files>.planning/v1.4-GA-UAT.md</files>
  <read_first>
    - .planning/v1.4-GA-UAT.md
    - docs/uat-ci-coverage.md
    - .planning/phases/59-uat-ga-narrative-alignment/59-CONTEXT.md
  </read_first>
  <action>
    Locate the markdown **pipe table** row where the first column is **GA-03**.

    Update the **`CI_substitute`** column to a **layered** form (**one line** or **two comma-separated clauses**):
    - (1) **`Sigra.OAuthTest`** — authorize/callback **contract**, in-process mock, **no HTTP to a real IdP**.
    - (2) **`Sigra.OAuthCeremonyAuditTest`** — **OA-01**: persisted **`audit_events`** on **successful** registration + **authorize** paths (cite **`test/sigra/oauth/oauth_ceremony_audit_test.exs`** path substring).

    **Notes** column: keep **live Google waived**; add **one** machine clause mirroring **OA-01** scope; retain residual (consent, refresh, provider-specific). Optionally pointer to **`GA-03/waiver.md`**.

    Add or tighten a **short pointer block** (paragraph or Phase-51-style callout) that names **OA-01** modules + paths and states **`docs/uat-ci-coverage.md`** is the **machine baseline** for expanded narrative — **mirror + pointer**, not a second SEED table (**D-59-08**).

    If a pinned SHA appears in this edit, follow **D-59-04** (single authoritative pin or soften waiver/matrix together).
  </action>
  <acceptance_criteria>
    - `grep -F 'Sigra.OAuthTest' .planning/v1.4-GA-UAT.md` exits **0**
    - `grep -F 'Sigra.OAuthCeremonyAuditTest' .planning/v1.4-GA-UAT.md` exits **0**
    - `grep -F 'test/sigra/oauth/oauth_ceremony_audit_test.exs' .planning/v1.4-GA-UAT.md` exits **0**
    - `grep -F 'docs/uat-ci-coverage.md' .planning/v1.4-GA-UAT.md` exits **0**
    - `grep -Fi 'OAuth fully verified in CI' .planning/v1.4-GA-UAT.md` exits **1**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 2: Refresh GA-03 waiver compensating_controls</name>
  <files>.planning/uat-evidence/v1.4/GA-03/waiver.md</files>
  <read_first>
    - .planning/uat-evidence/v1.4/GA-03/waiver.md
    - docs/uat-ci-coverage.md
    - .planning/phases/59-uat-ga-narrative-alignment/59-CONTEXT.md
  </read_first>
  <action>
    In the **Formal waiver table**, update the **`compensating_controls`** cell to cite **`Sigra.OAuthCeremonyAuditTest`** and path **`test/sigra/oauth/oauth_ceremony_audit_test.exs`** **in addition to** **`Sigra.OAuthTest`** / **`test/sigra/oauth/oauth_test.exs`**, framed as **contract + OA-01 audit persistence**, still **not** live Google.

    Align SHA / “as of date” wording with **Task 1** per **D-59-04** (avoid triple drift).

    Keep **`residual risk`** row accurate (consent UX, refresh, provider-specific).
  </action>
  <acceptance_criteria>
    - `grep -F 'oauth_ceremony_audit_test.exs' .planning/uat-evidence/v1.4/GA-03/waiver.md` exits **0**
    - `grep -F 'Sigra.OAuthCeremonyAuditTest' .planning/uat-evidence/v1.4/GA-03/waiver.md` exits **0**
    - `grep -F 'Sigra.OAuthTest' .planning/uat-evidence/v1.4/GA-03/waiver.md` exits **0**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 3: Optional INDEX.md pointer bullet</name>
  <files>.planning/uat-evidence/v1.4/INDEX.md</files>
  <read_first>
    - .planning/uat-evidence/v1.4/INDEX.md
    - docs/uat-ci-coverage.md
  </read_first>
  <action>
    Under an appropriate heading (e.g. after **GA directories** list or **Machine closure** paragraph), add **one bullet** linking **GA-03** evidence and **`docs/uat-ci-coverage.md`** OA subsection anchor — **no** duplicated requirement tables (**D-59-08** optional path).

    If the team prefers zero INDEX churn, replace this task with a no-op **only** after explicit maintainer decision recorded in phase **SUMMARY.md** — default is **add the bullet**.
  </action>
  <acceptance_criteria>
    - `grep -F 'docs/uat-ci-coverage.md' .planning/uat-evidence/v1.4/INDEX.md` exits **0**
    - `grep -F 'GA-03' .planning/uat-evidence/v1.4/INDEX.md` exits **0**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 4: docs/ga-evidence.md Hex router bullet</name>
  <files>docs/ga-evidence.md</files>
  <read_first>
    - docs/ga-evidence.md
    - docs/uat-ci-coverage.md
  </read_first>
  <action>
    Under **`## Where to read next`**, add **one bullet** with **absolute GitHub URLs** (same tag style as existing entries, e.g. **`https://github.com/sztheory/sigra/blob/v0.2.0/docs/uat-ci-coverage.md`**) pointing to:
    - **`docs/uat-ci-coverage.md`**
    - **`.planning/v1.4-GA-UAT.md`** matrix (tag snapshot path)

    Use **raw** `https://github.com/` URLs — **not** repo-relative links only.
  </action>
  <acceptance_criteria>
    - `grep -F 'https://github.com/sztheory/sigra/blob/' docs/ga-evidence.md` exits **0**
    - `grep -F 'uat-ci-coverage.md' docs/ga-evidence.md` exits **0**
    - `grep -F 'v1.4-GA-UAT.md' docs/ga-evidence.md` exits **0**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 5: PROJECT.md milestone narrative (retire AUD-03 hook)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/milestones/v1.3-REQUIREMENTS.md
    - .planning/phases/59-uat-ga-narrative-alignment/59-CONTEXT.md
  </read_first>
  <action>
    In **`## Current Milestone: v1.6 Nyquist closure + OAuth audit depth`**, replace the **Target features** bullet that currently says **`AUD-03 / OAuth ceremony not claimed`** (or equivalent ambiguous shorthand) with **OA-01 / OA-02** vocabulary and an explicit **v1.3** qualifier when referring to archived deferral, e.g. that **v1.3 AUD-03 integration smoke** deferred OAuth ceremony audit assertions per archived **`milestones/v1.3-REQUIREMENTS.md`**, while **v1.6** lands **OA-01** machine proof and **OA-02** documentation alignment.

    Do **not** introduce bare **`AUD-03`** for OAuth narrative (**D-59-06**).
  </action>
  <acceptance_criteria>
    - `grep -F 'OA-01' .planning/PROJECT.md` exits **0**
    - `grep -F 'OA-02' .planning/PROJECT.md` exits **0**
    - `grep -F 'AUD-03 / OAuth ceremony not claimed' .planning/PROJECT.md` exits **1** (exact legacy hook removed)
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 6: MILESTONES.md v1.3 AUD-03 boundary addendum</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md
    - .planning/phases/59-uat-ga-narrative-alignment/59-CONTEXT.md
  </read_first>
  <action>
    Near the existing **`AUD-03 boundary`** bullet under **v1.3** tech debt (line ~153 region), add **one** additive sentence or bullet **without rewriting** archived v1.3 facts — **time-versioned closure**, e.g. **As of v1.6 (OA-01):** merge-blocking ceremony audit assertions exist; see **`docs/uat-ci-coverage.md`** for machine vs human baseline (**D-59-07**).

    Do not claim v1.3 shipped **OA-01**.
  </action>
  <acceptance_criteria>
    - `grep -F 'As of v1.6' .planning/MILESTONES.md` exits **0**
    - `grep -F 'docs/uat-ci-coverage.md' .planning/MILESTONES.md` exits **0**
    - `grep -F 'OA-01' .planning/MILESTONES.md` exits **0**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

</tasks>

<verification>
1. Run every **acceptance_criteria** grep in Plans **01** and **02** from repo root.
2. `mix compile --warnings-as-errors` exits **0**.
3. Optional confidence: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/oauth/oauth_ceremony_audit_test.exs test/sigra/oauth/oauth_test.exs` exits **0** (unchanged code; catches accidental ecosystem breakage).
</verification>

<success_criteria>
- **GA-03** / **waiver** / **router** / **PROJECT** / **MILESTONES** tell the same machine vs human story as **`docs/uat-ci-coverage.md`**.
- **OA-02** checkbox in **`.planning/REQUIREMENTS.md`** can flip to complete after maintainer confirms wording (executor may tick in same PR if policy allows).
</success_criteria>
