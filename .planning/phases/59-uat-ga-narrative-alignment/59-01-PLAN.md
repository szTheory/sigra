---
phase: 59
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/uat-ci-coverage.md
autonomous: true
requirements:
  - OA-02
must_haves:
  truths:
    - "**`docs/uat-ci-coverage.md`** contains a **grep-friendly** subsection whose heading or first paragraph includes **all** literals **`OA-01`**, **`OA-02`**, **`library_tests`**, and **`oauth_ceremony`** (**59-CONTEXT D-59-01**)."
    - "That subsection names **`Sigra.OAuthCeremonyAuditTest`** + path **`test/sigra/oauth/oauth_ceremony_audit_test.exs`**, **`Sigra.Planning.Phase58OauthOa01CiContractTest`** + **`test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs`**, and **`Sigra.OAuthTest`** + **`test/sigra/oauth/oauth_test.exs`**, and states **no live IdP HTTP** for the machine proofs."
    - "**SEED-4** row and **GA-03** v1.4 bullet include a **short** pointer into the new subsection without duplicating a second authoritative CI table."
  artifacts:
    - path: "docs/uat-ci-coverage.md"
      provides: "OA-02 machine vs human hub for OAuth ceremony coverage"
nyquist_compliant: false
---

<objective>
Extend **`docs/uat-ci-coverage.md`** so **OA-02** is satisfied: one authoritative **OA-01 / OA-02** subsection (**machine** vs **human / live-provider**), literals for grep-first maintainers, and **tight** cross-links from the existing **SEED-4** row and **GA-03** line (**59-CONTEXT D-59-01**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-59-01 | Information disclosure | Docs **over-claim** machine proof → false assurance for adopters | high | Every machine bullet ties to **named module + path**; explicit **residual** bullets; forbid “OAuth fully E2E in CI” phrasing (**59-CONTEXT**). |
| T-59-02 | Repudiation | Subsection drifts from **OA-01** tests without grep anchors | medium | Require literals **`OA-01`**, **`OA-02`**, **`library_tests`**, **`oauth_ceremony`** in the subsection heading or first paragraph. |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add OA-01 / OA-02 machine baseline subsection</name>
  <files>docs/uat-ci-coverage.md</files>
  <read_first>
    - docs/uat-ci-coverage.md
    - .planning/phases/59-uat-ga-narrative-alignment/59-CONTEXT.md
    - .planning/phases/59-uat-ga-narrative-alignment/59-RESEARCH.md
    - test/sigra/oauth/oauth_ceremony_audit_test.exs
    - test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs
    - test/sigra/oauth/oauth_test.exs
  </read_first>
  <action>
    Insert a new **`##`** section in **`docs/uat-ci-coverage.md`** placed **immediately after** the SEED markdown table (before the existing **`## v1.4 GA (GA-02..GA-05)`** section) unless **59-CONTEXT** placement note requires otherwise — default is **after SEED table**.

    The section **title** (or first paragraph under it) MUST contain these **exact ASCII substrings** for grep stability: **`OA-01`**, **`OA-02`**, **`library_tests`**, **`oauth_ceremony`**.

    **Body structure (locked content):**

    1. **Machine (merge-blocking)** — bullets that:
       - Name **`Sigra.OAuthCeremonyAuditTest`** and path **`test/sigra/oauth/oauth_ceremony_audit_test.exs`**. State it proves **persisted** `audit_events` for **`oauth.register_via_oauth`** (registration ceremony) and **`oauth.authorize`** on the successful **`Sigra.OAuth.authorize_url/3`** path, **Postgres + Sandbox**, **in-process mock strategy**, **no live IdP HTTP**.
       - Name **`Sigra.Planning.Phase58OauthOa01CiContractTest`** and path **`test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs`**. State it is a **structural** CI gate: **`library_tests`** job runs plain **`mix test`** (does **not** replace integration assertions).
       - Name **`Sigra.OAuthTest`** and **`test/sigra/oauth/oauth_test.exs`** for **authorize/callback contract** / Assent-shaped mock coverage (complement, not duplicate of audit persistence proof).

    2. **Human / live-provider residual** — bullets explicitly listing **live Google** (consent UX, refresh, tenant policy) and any behavior **not** asserted by the modules above. MUST NOT imply full live-provider E2E in CI.

    Optional: one line cross-linking **`.github/workflows/ci.yml`** job name **`library_tests`** (literal) for discoverability.
  </action>
  <acceptance_criteria>
    - `grep -F 'OA-01' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'OA-02' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'library_tests' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'oauth_ceremony' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'Sigra.OAuthCeremonyAuditTest' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'test/sigra/oauth/oauth_ceremony_audit_test.exs' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'Sigra.Planning.Phase58OauthOa01CiContractTest' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'Sigra.OAuthTest' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'test/sigra/oauth/oauth_test.exs' docs/uat-ci-coverage.md` exits **0**
    - `grep -F 'no live IdP' docs/uat-ci-coverage.md || grep -F 'no live IdP HTTP' docs/uat-ci-coverage.md` — at least one exits **0**
    - `grep -Fi 'OAuth fully verified in CI' docs/uat-ci-coverage.md` exits **1** (phrase must **not** appear)
    - `grep -Fi 'OAuth fully E2E in CI' docs/uat-ci-coverage.md` exits **1**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 2: SEED-4 and GA-03 cross-links to the subsection</name>
  <files>docs/uat-ci-coverage.md</files>
  <read_first>
    - docs/uat-ci-coverage.md
    - .planning/phases/59-uat-ga-narrative-alignment/59-CONTEXT.md
  </read_first>
  <action>
    1. In the **SEED** table **row 4** (Google OAuth / **SEED-4**), append or extend the **CI / automated substitute** cell with **one short sentence** that points readers to the new OA subsection (use the **exact** section heading text so anchor or “see § …” style is valid Markdown).

    2. In **`## v1.4 GA`**, update the **GA-03** bullet so it **still** mentions **`Sigra.OAuthTest`** / mock contract, **and** adds **`Sigra.OAuthCeremonyAuditTest`** (audit persistence) with a **pointer** to the same OA subsection (subsection owns depth — **no** paragraph-length duplication in the GA-03 bullet).
  </action>
  <acceptance_criteria>
    - `grep -F 'Sigra.OAuthCeremonyAuditTest' docs/uat-ci-coverage.md` exits **0** (from Task 1 or this task)
    - SEED table row for **Google OAuth** / **SEED-4** still contains **`Sigra.OAuthTest`** substring
    - `grep -F '## v1.4 GA' docs/uat-ci-coverage.md` exits **0**
    - GA-03 bullet line contains both **`Sigra.OAuthTest`** and **`Sigra.OAuthCeremonyAuditTest`** substrings (same file, under v1.4 GA section)
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

</tasks>

<verification>
1. Run all **Task 1–2** `acceptance_criteria` grep commands from repo root.
2. `mix compile --warnings-as-errors` exits **0**.
</verification>

<success_criteria>
- **`docs/uat-ci-coverage.md`** is the **single depth hub** for OAuth machine vs human story (**OA-02**).
- No forbidden “full OAuth in CI” claims; module + path literals present for maintainers.
</success_criteria>
