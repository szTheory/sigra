---
phase: 57
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01-PLAN.md
files_modified:
  - test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs
autonomous: true
requirements:
  - NYQ-01
must_haves:
  truths:
    - "**`test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs`** encodes minimal structural guarantees: canonical matrix file exists, **`MAINTAINING.md`** links to it, and each **41–44** slug appears in the matrix (**D-11** “verify, don’t rewrite” style)."
    - "Tests remain **`async: true`** compatible with **`phase_50_nyquist_docs_contract_test.exs`** (no shared DB)."
  artifacts:
    - path: "test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs"
      provides: "CI-enforced anchors for matrix + MAINTAINING link"
nyquist_compliant: false
---

<objective>
Optional **D-11** hardening: add a small **ExUnit** contract so future edits cannot drop the canonical matrix link or phase rows without failing **`mix test`**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-57-04 | Tampering | Matrix file deleted but MAINTAINING still claims coverage | medium | Test **`test -f`** / `File.read!` on **`.planning/nyquist-phases-41-44-matrix.md`** |
| T-57-05 | Elevation of privilege (process) | Flaky test blocks unrelated PRs | low | Keep assertions to **string** presence checks only; no network |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: phase_57_nyquist_matrix_contract_test.exs</name>
  <files>test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_50_nyquist_docs_contract_test.exs
    - .planning/nyquist-phases-41-44-matrix.md
    - MAINTAINING.md
  </read_first>
  <action>
    Add **`test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs`** defining **`Sigra.Planning.Phase57NyquistMatrixContractTest`** with:

    1. `use ExUnit.Case, async: true`
    2. Private **`read!(rel)`** helper mirroring **phase_50** (`Path.expand("../../..", __DIR__)` pattern).
    3. **Test 1:** `File.read!` on **`.planning/nyquist-phases-41-44-matrix.md`** succeeds and body contains substring **`41-backup-codes-ga-product-closure`** and **`UNCHANGED`** (or documents all four disposition tokens — minimum bar: **four** slug substrings each appear at least once).
    4. **Test 2:** **`MAINTAINING.md`** contains **`.planning/nyquist-phases-41-44-matrix.md`** and still contains **`## Nyquist policy (phases 41-44)`**.

    Use **exact** path strings above so grep-based acceptance matches.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs` exits **0**.
    - `grep -F 'Sigra.Planning.Phase57NyquistMatrixContractTest' test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs` exits **0**.
    - `grep -F 'nyquist-phases-41-44-matrix.md' test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs` exits **0**.
    - `MIX_ENV=test mix test test/sigra/planning/phase_57_nyquist_matrix_contract_test.exs` exits **0**.
    - `MIX_ENV=test mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>
