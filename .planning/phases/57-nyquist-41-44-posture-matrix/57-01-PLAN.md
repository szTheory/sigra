---
phase: 57
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/nyquist-phases-41-44-matrix.md
  - MAINTAINING.md
autonomous: true
requirements:
  - NYQ-01
  - NYQ-02
must_haves:
  truths:
    - "**`.planning/nyquist-phases-41-44-matrix.md`** exists as the **canonical** full matrix; **`MAINTAINING.md`** keeps a **short** index under **`## Nyquist policy (phases 41-44)`** with a **prominent relative link** to that file and an explicit **precedence** sentence that the **`.planning/`** file wins on conflict (**57-CONTEXT D-01–D-03**)."
    - "Every **41–44** row uses primary disposition **`UNCHANGED`** (four rows, waiver + superseding evidence; `nyquist_compliant: false` in each phase’s `*-VALIDATION.md` frontmatter) **or** a different **D-12** label only if you have re-read that phase’s artifacts and can cite why in the matrix row — **no blank** disposition cells (**NYQ-02**)."
    - "Evidence columns use **repo-relative** paths from repo root (examples: **`.planning/phases/41-backup-codes-ga-product-closure/41-VERIFICATION.md`**, **`.planning/v1.4-GA-UAT.md`**); optional GitHub tag URLs are parenthetical convenience only (**D-07**)."
    - "Header includes **`ref:`** (or equivalent) with release tag **`v1.5`** and full commit SHA **`7b1001d05a2a749ca744bdcde28aee9d189828d2`** plus calendar date **2026-04-22** (update SHA if you advance commit before merge)."
    - "One note states **`.planning/`** evidence is **not** assumed in the **Hex** tarball unless packaged via **`mix.exs` `:files`** (**D-08**)."
  artifacts:
    - path: ".planning/nyquist-phases-41-44-matrix.md"
      provides: "Canonical Nyquist 41–44 posture grid + reopen triggers"
    - path: "MAINTAINING.md"
      provides: "Maintainer front door + link to canonical matrix"
nyquist_compliant: false
---

<objective>
Satisfy **NYQ-01** and **NYQ-02** by authoring the **canonical** markdown matrix and trimming **`MAINTAINING.md`**’s Nyquist subsection to an index that links to it, following **57-CONTEXT.md** decisions **D-01–D-17** and **57-RESEARCH.md**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-57-01 | Spoofing / misrepresentation | Matrix implies **`nyquist_compliant: true`** or “GA certified” contrary to **`*-VALIDATION.md`** | high | Copy **waiver** language from each phase **VALIDATION**; primary disposition **`UNCHANGED`** for all four rows unless artifacts changed; cite **`nyquist_compliant: false`** in narrative column |
| T-57-02 | Tampering (stale pointers) | Relative paths point at non-existent files | medium | Use exact paths from **57-CONTEXT** canonical_refs; grep `test -f` or `rg` file heads in acceptance |
| T-57-03 | Repudiation | Maintainer edits **`MAINTAINING.md`** summary that contradicts canonical matrix | low | **D-03** precedence sentence in **both** files |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Canonical matrix file</name>
  <files>.planning/nyquist-phases-41-44-matrix.md</files>
  <read_first>
    - .planning/phases/57-nyquist-41-44-posture-matrix/57-CONTEXT.md
    - .planning/phases/57-nyquist-41-44-posture-matrix/57-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/41-backup-codes-ga-product-closure/41-VALIDATION.md
    - .planning/phases/42-human-ga-matrix-evidence/42-VALIDATION.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md
  </read_first>
  <action>
    Create **`.planning/nyquist-phases-41-44-matrix.md`** with:

    1. Title **`# Nyquist posture matrix (phases 41–44)`** (or equivalent governance tone).
    2. A **short** preamble containing: precedence sentence (**`.planning/`** canonical wins), **Hex tarball** honesty note (**D-08**), and a **`ref:`** block with tag **`v1.5`**, SHA **`7b1001d05a2a749ca744bdcde28aee9d189828d2`**, date **2026-04-22** (adjust if your release engineering uses a different tag at merge time — then update this plan’s must_haves in the same PR).
    3. A **markdown table** with **exactly four** data rows (**41, 42, 43, 44**) and columns at minimum: **Phase slug** | **Primary disposition** (`UNCHANGED` / `COMPLIANT` / `DEFERRED` per **D-12**) | **Nyquist / waiver mode** | **`VERIFICATION.md` path** | **`VALIDATION.md` path** (use **`none`** + one-line why only if a file truly does not exist — **42** still has **`42-VALIDATION.md`**) | **Reopen trigger** (command-oriented per **D-17**) | **Optional tag-scoped URL** (parenthetical).

    For **primary disposition**, default all four rows to **`UNCHANGED`** with rationale bullets pointing at **`nyquist_compliant: false`** + waiver narrative in each phase’s **VALIDATION** (matches **57-RESEARCH** as of **2026-04-22**).

    Populate **slug** column with directory slugs: **`41-backup-codes-ga-product-closure`**, **`42-human-ga-matrix-evidence`**, **`43-audit-inventory-auth-atomic-batch`**, **`44-mfa-account-api-atomic-batches`**.
  </action>
  <acceptance_criteria>
    - `test -f .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -F 'v1.5' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -F '7b1001d05a2a749ca744bdcde28aee9d189828d2' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -Fi 'hex' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -F '41-backup-codes-ga-product-closure' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -F '42-human-ga-matrix-evidence' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -F '43-audit-inventory-auth-atomic-batch' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -F '44-mfa-account-api-atomic-batches' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -E 'UNCHANGED|COMPLIANT|DEFERRED' .planning/nyquist-phases-41-44-matrix.md` shows **at least one** primary disposition token per phase row (verify by reading file: **four** rows, **no** empty disposition cells).
    - `grep -F '.planning/phases/41-backup-codes-ga-product-closure/41-VERIFICATION.md' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
    - `grep -F '.planning/v1.4-GA-UAT.md' .planning/nyquist-phases-41-44-matrix.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: MAINTAINING.md index + link</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - .planning/nyquist-phases-41-44-matrix.md
    - .planning/phases/57-nyquist-41-44-posture-matrix/57-CONTEXT.md
    - test/sigra/planning/phase_50_nyquist_docs_contract_test.exs
  </read_first>
  <action>
    Under the existing heading **`## Nyquist policy (phases 41-44)`** (keep this **exact** string so **`phase_50_nyquist_docs_contract_test.exs`** test **`50-01-05`** stays green):

    1. Replace the **wide** GitHub-only table with a **short** explanation: what the matrix guarantees, that **canonical** detail lives under **`.planning/nyquist-phases-41-44-matrix.md`**, and **if this section disagrees with that file, the matrix file wins** (**D-03**).
    2. Insert **one** prominent relative markdown link to **`nyquist-phases-41-44-matrix.md`** using path **`.planning/nyquist-phases-41-44-matrix.md`** (link text is executor’s choice).
    3. Retain **one-line** reopen guidance pointing at **`mix ci.install_golden`** / scoped tests where still accurate — do **not** duplicate the full grid.

    Do **not** remove the **`41-backup-codes`** or **`44-mfa-account-api`** path substrings required by **`50-01-05`** (keep them in prose or in a minimal example row if needed).
  </action>
  <acceptance_criteria>
    - `grep -nF '## Nyquist policy (phases 41-44)' MAINTAINING.md` exits **0**.
    - `grep -F '.planning/nyquist-phases-41-44-matrix.md' MAINTAINING.md` exits **0**.
    - `grep -F '41-backup-codes' MAINTAINING.md` exits **0**.
    - `grep -F '44-mfa-account-api' MAINTAINING.md` exits **0**.
    - `MIX_ENV=test mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>
