---
phase: 61
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/mfa.ex
  - test/sigra/mfa_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-01
must_haves:
  truths:
    - "`Sigra.MFA.verify_backup/4` wrong-backup path uses **one** `Repo.transaction/1` that includes **`Lockout.increment`** and **`Sigra.Audit.log_multi_safe`** for **`mfa.verify.failure`** (and **`mfa.lockout`** when applicable), mirroring **`verify/4`** failure semantics."
    - "At least **one** test asserts **audit row(s)** on the invalid-backup success path (stable **`action`** strings) and **one** constraint-based rollback test proves domain counter changes do **not** commit when the audit step fails."
  artifacts:
    - path: "lib/sigra/mfa.ex"
      provides: "AUD-01 MFA verify_backup failure cluster — Multi + audit co-fate"
    - path: "test/sigra/mfa_audit_atomicity_test.exs"
      provides: "D-05/D-06 audit-aware + rollback coverage"
nyquist_compliant: false
---

<objective>
Implement **AUD-01** for the **bounded MFA cluster** **`verify_backup/4` invalid backup code** (**061-CONTEXT D-01, D-03, D-05, D-06**): move lockout counter updates from a naked post-transaction path into **`Ecto.Multi`** with **`log_multi_safe`**, matching **`verify/4`** failure behavior, and extend **`mfa_audit_atomicity_test.exs`** accordingly.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-61-01 | Repudiation | Wrong backup attempts leave **no** durable **`mfa.verify.failure`** row while **`failed_attempts`** increases | high | Bundle increment + audit in **same** `Multi` + success-path grep tests |
| T-61-02 | Tampering | Partial commit: counter changes without audit (or audit without counter) | high | Single `repo.transaction` per branch; rollback test with `CHECK` constraint on audit action |
| T-61-03 | Denial of service | Longer transaction holds row lock on MFA credential | medium | Keep `Multi` minimal — same steps as **`verify/4`** failure path (no extra queries) |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Multi + audit for verify_backup invalid code path</name>
  <files>lib/sigra/mfa.ex</files>
  <read_first>
    - lib/sigra/mfa.ex
    - lib/sigra/mfa.ex (lines 30–45 dispatch comment block)
    - lib/sigra/mfa.ex (verify/4 failure_multi pattern, ~lines 301–361)
    - lib/sigra/mfa.ex (verify_backup/4, ~lines 385–464)
    - lib/sigra/audit.ex
    - docs/audit-semantics.md
  </read_first>
  <action>
    In **`verify_backup/4`**, replace the **`{:error, :consume, :invalid_backup_code, _changes}`** branch body (currently **`Lockout.increment`** then threshold math **outside** any `Multi` audit).

    **Target behavior (must match `verify/4` patterns):**

    1. Build **`failure_multi = Multi.new()`** then **`Multi.run(:lockout_inc, fn r, _ -> Lockout.increment(r, mfa_credential_schema, credential.id, config) end)`**.
    2. Pipe **`Sigra.Audit.log_multi_safe("mfa.verify.failure", Keyword.merge(mfa_audit_opts(config), actor_id: user.id, target_id: user.id, outcome: \"failure\", audit_multi_step: :audit_mfa_backup_verify_failure, metadata_resolver: fn ch -> %{method: \"backup_code\", attempts: ch.lockout_inc.failed_attempts} end))`** — adjust **`audit_multi_step` atom** if it collides with existing atoms in **`emit_telemetry_from_changes`**; pick a **unique** atom name and use it consistently in tests if telemetry is asserted.
    3. **`Multi.merge`** conditional **`mfa.lockout`** **`log_multi_safe`** when **`lockout_inc.locked`** is true (copy structure from **`verify/4`** failure branch: duration from **`Keyword.get(config.mfa, :lockout_duration, 900)`**, metadata **`%{method: \"backup_code\", duration: duration}`**).
    4. **`case repo.transaction(failure_multi)`** — on **`{:ok, changes}`**: call **`Sigra.Audit.emit_telemetry_from_changes(changes, [:audit_mfa_backup_verify_failure, :audit_mfa_backup_lockout])`** (second element omitted if unused — mirror **`verify/4`** list exactly for lockout sub-step naming).
    5. Return tuples unchanged from today: **`{:error, :lockout, duration}`** or **`{:error, :invalid_backup_code, max(threshold - count, 0)}`** using **`changes.lockout_inc`** fields.
    6. On **`{:error, failed, reason, _changes}`**, preserve existing **`raise`** string prefix **`Sigra.MFA.verify_backup/4 unexpected transaction failure`**.

    Update the **file header dispatch comment** (lines ~35–42) to state **`verify_backup` failure** uses **`Multi` + `log_multi_safe`** (not “no audit”).
  </action>
  <acceptance_criteria>
    - `grep -nF 'invalid_backup_code' lib/sigra/mfa.ex` shows the failure branch contains **`Multi.new()`** before **`Lockout.increment`**.
    - `grep -nF '\"mfa.verify.failure\"' lib/sigra/mfa.ex` matches at least **one** occurrence inside **`verify_backup`** context (line numbers between **`def verify_backup`** and the next **`@doc`** for **`disable`**).
    - `grep -nF 'verify_backup' lib/sigra/mfa.ex` contains **`log_multi_safe`** in the same function as **`invalid_backup_code`** handling.
    - `mix format lib/sigra/mfa.ex` exits **0** (executor runs format after edits).
    - `MIX_ENV=test mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0** after Task 2 updates tests (run again after Task 2 if Task 1 alone fails compile only).
  </verify>
</task>

<task type="execute">
  <name>Task 2: Audit-aware tests for verify_backup failure + rollback</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/mfa_audit_atomicity_test.exs
    - lib/sigra/mfa.ex
  </read_first>
  <action>
    Add **two** tests to **`Sigra.MFAAuditAtomicityTest`**:

    1. **`verify_backup` wrong code emits `mfa.verify.failure`**: Seed user + credential + **no** backup code row (or wrong hash). Call **`MFA.verify_backup(config, user, \"not-a-valid-code\", mfa_credential_schema: MfaCredential, backup_code_schema: BackupCode)`** expecting **`{:error, :invalid_backup_code, _}`**. Assert **`count_where(repo, \"audit_events\", \"action = 'mfa.verify.failure'\") >= 1`** and that the most recent row’s **`metadata::text`** contains **`backup_code`** (substring match is fine).

    2. **Rollback when audit insert blocked**: After inserting credential, run **`verify_backup`** with wrong code under a temporary **`CHECK`** constraint on **`audit_events`** rejecting **`mfa.verify.failure`** (same **`try/after`** pattern as existing **`mfa_disable_atomicity_guard`** test). Assert **`failed_attempts`** on **`user_mfa_credentials`** returns to the pre-call value (or assert **0** failed attempts if starting from 0 and increment was rolled back — choose deterministic assertions documented in the test name).

    Optional third test: if lockout threshold is **5**, repeat failures until **`{:error, :lockout, _}`** and assert **`mfa.lockout`** row exists — only if stable with **`backup_code_count: 2`** fixture config (keep runtime low).
  </action>
  <acceptance_criteria>
    - `grep -nF 'verify_backup' test/sigra/mfa_audit_atomicity_test.exs` shows **≥ 2** new test blocks (or **`test \"`** lines) describing failure audit + rollback.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs --trace` exits **0** when debugging order-dependent failures.
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile --warnings-as-errors` exits **0**.
</verification>

<success_criteria>
- **AUD-01** satisfied for **`verify_backup`** failure cluster: **T1** **`Multi`** + audit co-fate + tests that would fail if audits drop on successful lockout accounting paths in this branch.
</success_criteria>
