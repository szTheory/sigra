---
phase: 61
plan: 02
type: execute
wave: 2
depends_on:
  - "061-01-PLAN.md"
files_modified:
  - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
  - .planning/phases/09-audit-logging/09-VERIFICATION.md
autonomous: true
requirements:
  - AUD-01
must_haves:
  truths:
    - "**`44-AUD-04-INVENTORY.md`** documents **`verify_backup`** failure-path mechanism consistently with **`lib/sigra/mfa.ex`** (Multi + **`mfa.verify.failure`** / lockout as implemented)."
    - "**`09-VERIFICATION.md`** C-1 matrix (or subsection) no longer implies **`verify_backup`** wrong-code lockout is unaudited hybrid **`log_safe`** post-commit — rows touched by this batch match merged code."
  artifacts:
    - path: ".planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md"
      provides: "AUD-04 traceability for backup verify failure"
    - path: ".planning/phases/09-audit-logging/09-VERIFICATION.md"
      provides: "C-1 honesty (061-CONTEXT D-08)"
nyquist_compliant: false
---

<objective>
Complete **061-CONTEXT D-08**: align **AUD-04** inventory and **Phase 9** **`09-VERIFICATION.md`** C-1 rows with the **post-61** **`verify_backup`** failure audit mechanism (**AUD-01** docs gate in same milestone as code).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-61-04 | Repudiation | C-1 claims hybrid **`log_safe`** while code is **`Multi`** | high | Diff inventory rows against **`grep log_multi_safe lib/sigra/mfa.ex`** before editing |
| T-61-05 | Information disclosure | Accidental paste of secrets in planning docs | low | Docs edits only — no credentials |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Refresh 44-AUD-04 inventory for verify_backup failure</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - lib/sigra/mfa.ex
    - .planning/phases/061-seed-002-bounded-batch/061-CONTEXT.md
  </read_first>
  <action>
    Add **or** update inventory row(s) so **`verify_backup`** **invalid backup** path is explicit:

    - Mechanism column: **`Multi` + `log_multi_safe`** for **`mfa.verify.failure`** when `:audit_schema` set.
    - If **`mfa.lockout`** is emitted from this path when threshold reached, add a second row or combined note referencing **`verify/4`** parity.
    - Fix any stale rows (**AUD-04-020–027**) whose mechanism column still says **`log_safe`** if **`grep`** confirms **`log_multi_safe`** in **`lib/sigra/mfa.ex`** for that action (bounded to rows this phase touched: at minimum **`verify_backup`**-related lines).

    Add a **Note** line under the MFA table: **Phase 61 (2026-04-23)** closed **`verify_backup`** failure hybrid gap — cite **`AUD-01`**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'verify_backup' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` exits **0** with at least one line mentioning **`mfa.verify.failure`** and **`backup`** (case-insensitive) OR **`invalid_backup`**.
    - `grep -nF 'Phase 61' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `test -f .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Update 09-VERIFICATION C-1 matrix for verify_backup failure</name>
  <files>.planning/phases/09-audit-logging/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-VERIFICATION.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - lib/sigra/mfa.ex
  </read_first>
  <action>
    In **`### C-1 — Phase 44 inventory`**:

    1. Update **`AUD-04-026`** and **`AUD-04-027`** mechanism + verdict columns to match current **`lib/sigra/mfa.ex`** (**`Multi` + `log_multi_safe`** success path — **T1**), if still showing **`log_safe`** / “target Multi”.
    2. Add a short **Phase 61** note (same subsection, paragraph or table footnote) stating **`verify_backup`** **invalid backup** attempts emit **`mfa.verify.failure`** via **`Multi`** (and **`mfa.lockout`** when threshold reached), with pointer to **`mfa_audit_atomicity_test.exs`**.

    Do **not** rewrite unrelated OAuth rows. No **`09-03-SUMMARY.md`** edits (**061-CONTEXT D-09**).
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-026' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0** and the matching line contains **`Multi`** or **`log_multi_safe`** (substring).
    - `grep -nF 'AUD-04-027' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0** and the matching line contains **`Multi`** or **`log_multi_safe`** (substring).
    - `grep -nF 'Phase 61' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `test -f .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `grep -nF 'verify_backup' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0**.
- Spot-check: inventory row IDs cited in **`09-VERIFICATION.md`** exist in **`44-AUD-04-INVENTORY.md`**.
</verification>

<success_criteria>
- **D-08** satisfied: C-1 / inventory truth matches **`verify_backup`** failure **`Multi`** implementation from **061-01**.
</success_criteria>
