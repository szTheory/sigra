---
phase: 189-page-compositions-l3
plan: 03
type: execute
wave: 2
depends_on: ["189-01", "189-02"]
files_modified:
  - test/example/priv/playwright/tests/admin-modal-interaction.spec.ts
  - test/example/priv/playwright/playwright.config.ts
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements: [PAGE-03, PAGE-05]
must_haves:
  truths:
    - "A deterministic, role-selector-based, sleep-free Playwright spec proves the ConfirmDialog APG behaviors on user_show_live"
    - "The modal spec asserts: initial focus on Cancel, Tab containment, Escape closes, focus returns to trigger, role/aria-modal/aria-labelledby present"
    - "axe wcag2a+wcag2aa runs WHILE the confirm dialog is open and reports 0 violations"
    - "All 8 admin checkpoints × 3 projects pass with axe clean"
    - "The 6 L3 ledger rows are ratified with executable evidence links (not '#' placeholders)"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-modal-interaction.spec.ts"
      provides: "Dedicated PAGE-03 modal interaction + axe-while-open assertions"
      contains: "ConfirmDialog"
    - path: "test/example/priv/playwright/playwright.config.ts"
      provides: "Modal interaction spec wired into the chromium behavior lane (or its own project)"
    - path: "guides/reference/admin-quality-ledger.md"
      provides: "6 ratified L3 rows with executable evidence links"
      contains: "index-live"
  key_links:
    - from: "admin-modal-interaction.spec.ts"
      to: "user_show_live ConfirmDialog hook"
      via: "click Revoke session trigger then keyboard/focus assertions"
      pattern: "Revoke session"
    - from: "guides/reference/admin-quality-ledger.md L3 rows"
      to: "admin-checkpoints + admin-modal-interaction specs"
      via: "evidence column links"
      pattern: "admin-checkpoints|admin-modal-interaction"
---

<objective>
Produce the executable RATIFICATION evidence for Phase 189 (PAGE-05, plus the PAGE-03 modal
behavior evidence): a dedicated deterministic modal-interaction spec proving the ConfirmDialog
APG behaviors, axe run WHILE the dialog is open, the existing 8×3 checkpoint lane still green,
and the 6 L3 ledger rows ratified with real evidence links. Evidence = behavior, not intent
(D-12/D-13).

Purpose: Phase 189 ratifies the 6 L3 ledger rows against the existing rubric. Ratification is
the existing `admin-checkpoints.spec.ts` (8 checkpoints × 3 projects + axe) PLUS new modal
interaction assertions. Per D-13, the planner chose a SMALL DEDICATED modal interaction spec
(role-selector, no-sleep, runs on the behavior lane) rather than bloating the capture-only
checkpoint journey with keyboard/focus steps.

Output: `admin-modal-interaction.spec.ts` + config wiring; checkpoint spec confirmed green
(recapture only if an intended visual delta appears, via the canary-guarded gate); 6 L3 ledger
rows ratified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-page-compositions-l3/189-CONTEXT.md
@.planning/phases/189-page-compositions-l3/189-PATTERNS.md
@.planning/phases/189-page-compositions-l3/189-UI-SPEC.md
@guides/reference/admin-quality-ledger.md
@.planning/phases/189-page-compositions-l3/189-01-SUMMARY.md
@.planning/phases/189-page-compositions-l3/189-02-SUMMARY.md

<interfaces>
<!-- Reuse these idioms from admin-checkpoints.spec.ts. The modal spec needs its OWN login/seed
     helpers (the checkpoints spec's registerUser/openUserDetail are file-local). Reuse the
     shared helpers/fixtures.ts TEST_PASSWORD and the same admin-email convention. -->

assertNoAxeViolations (admin-checkpoints.spec.ts L114-126) — AxeBuilder withTags
['wcag2a','wcag2aa'], expect violations.toHaveLength(0). Run it WHILE the dialog is open.

Admin platform-admin convention (admin-checkpoints.spec.ts L157-160): an email starting with
`platform-admin+` is granted platform-admin by Example.SigraAdminPolicy.

Trigger element (user_show_live.ex L178-187): button "Revoke session" (phx-click="open_revoke_session").
Confirm overlay after Plan 01: id="user-session-confirm-overlay" phx-hook="ConfirmDialog";
inner <section class="sg-confirm-dialog" role="dialog" aria-modal="true"
aria-labelledby="user-session-confirm-title">; Cancel button rendered first.

Shared helpers: test/example/priv/playwright/helpers/fixtures.ts (TEST_PASSWORD),
helpers/adminUsersIndex.ts (adminUsersEmailLocator). LiveView readiness: wait for
.phx-connected AND a loaded element (no waitForTimeout / no sleeps — 188 D-09).

playwright.config.ts (L84-86) chromium behavior lane uses testIgnore of the partitioned
specs; the default `chromium` project runs everything not ignored. Snapshot path template at
L60-63. Project blocks for admin-checkpoints at L108-145.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the dedicated ConfirmDialog modal-interaction spec + wire it into the config</name>
  <files>test/example/priv/playwright/tests/admin-modal-interaction.spec.ts, test/example/priv/playwright/playwright.config.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts (L1-148 imports + file-local helpers registerUser/openUserDetail/waitForLiveViewReady; L114-126 assertNoAxeViolations; L209-222 user-detail navigation idiom)
    - test/example/priv/playwright/playwright.config.ts (L24-27 spec regexes; L43-66 testDir/expect; L84-105 chromium/mobile lanes; L108-145 admin-checkpoints projects)
    - test/example/priv/playwright/helpers/fixtures.ts (TEST_PASSWORD)
    - test/example/priv/playwright/helpers/adminUsersIndex.ts (adminUsersEmailLocator)
    - .planning/phases/189-page-compositions-l3/189-UI-SPEC.md (L324-353 ratification contract; L339-349 the 7 required modal interaction assertions; L349 axe-while-open)
    - .planning/phases/189-page-compositions-l3/189-PATTERNS.md (section "admin-checkpoints.spec.ts" L232-267 role-selector + no-sleep idiom)
    - .planning/phases/189-page-compositions-l3/189-CONTEXT.md (D-07 hard gates, D-08 optional non-gates, D-12/D-13 evidence rules)
    - lib/sigra/admin/live/user_show_live.ex (L178-187 trigger; L315-333 overlay markup after Plan 01 wiring)
  </read_first>
  <action>
    Create `test/example/priv/playwright/tests/admin-modal-interaction.spec.ts`. It logs in as a
    platform admin (email prefix `platform-admin+`, TEST_PASSWORD from helpers/fixtures), seeds a
    target user with at least one active session (register a target user so a "Revoke session"
    button exists — mirror the checkpoints spec's registerUser + openUserDetail navigation idiom;
    define spec-local helpers since the checkpoints helpers are file-local), and navigates to that
    user's detail page (`/admin/users/:id`).

    Assert the PAGE-03 APG hard gates (UI-SPEC L339-349, D-07), all deterministic, role/selector
    based, NO `waitForTimeout`/sleeps (188 D-09):
    1. Click the "Revoke session" trigger (getByRole('button', { name: 'Revoke session' }).first()).
       Wait for the dialog via `page.locator('#user-session-confirm-overlay')` /
       `getByRole('dialog')` toBeVisible (LiveView readiness gate, not a sleep).
    2. Initial focus: assert `page.evaluate(() => document.activeElement?.textContent)` or an
       active-element role check shows focus is on the Cancel button inside `.sg-confirm-dialog`.
    3. Focus containment: from the last focusable (the danger confirm button), press Tab and
       assert focus wraps to the first focusable (Cancel); press Shift+Tab from Cancel and assert
       it wraps to the confirm button. Assert focus never lands on a background element.
    4. ARIA: assert `getByRole('dialog')` has aria-modal="true" and an aria-labelledby pointing to
       the visible title `#user-session-confirm-title` (toHaveAttribute checks).
    5. axe WHILE OPEN: run an AxeBuilder withTags(['wcag2a','wcag2aa']).analyze() with the dialog
       visible and assert 0 violations (reuse the assertNoAxeViolations shape from checkpoints L114-126).
    6. Escape closes: capture the trigger element handle BEFORE opening; press Escape; assert the
       overlay is hidden/removed (`#user-session-confirm-overlay` toHaveCount 0 or toBeHidden).
    7. Focus return: after Escape close, assert `document.activeElement` is the "Revoke session"
       trigger again (focus returned to trigger).
    Optionally (D-08, NOT a gate, do not fail the run on these): assert outside-click on the
    overlay closes too — only if it does not introduce flakiness.

    Wire the spec into `playwright.config.ts`: add a regex constant
    `const ADMIN_MODAL_SPEC = /admin-modal-interaction\.spec\.ts/;` and ensure the spec runs on the
    `chromium` behavior lane (it must NOT be added to that lane's `testIgnore`; if necessary add it
    to the lane that runs full behavior truth). Keep it OUT of the `mobile` lane's run if mobile
    excludes admin behavior specs (match the existing ADMIN_BEHAVIOR_SPECS exclusion convention).
    Do NOT add screenshots to this spec — it is interaction-only, no `toHaveScreenshot`.
  </action>
  <acceptance_criteria>
    - Spec exists and references the trigger + dialog: `grep -c 'Revoke session' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` >= 1 and `grep -c 'user-session-confirm-overlay\|getByRole(.dialog.)' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` >= 1
    - All 5 APG hard gates present (initial focus, Tab containment, Escape close, focus return, aria) — verified by grep for `activeElement`, `Escape`, `aria-modal`, `Tab` keyboard presses
    - NO sleeps: `grep -c 'waitForTimeout\|setTimeout' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` returns 0
    - axe-while-open present: `grep -c 'AxeBuilder\|wcag2a' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` >= 1
    - Config wires the spec: `grep -c 'admin-modal-interaction' test/example/priv/playwright/playwright.config.ts` >= 1
    - The spec PASSES against the example app (boot example on an alt PORT to avoid Docker 4000; pre-compile to avoid code-reload crash per MEMORY admin-checkpoint-playwright note): `npx playwright test admin-modal-interaction --project=chromium` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright && grep -c 'waitForTimeout' tests/admin-modal-interaction.spec.ts; npx playwright test admin-modal-interaction --project=chromium</automated>
  </verify>
  <done>Dedicated modal-interaction spec proves all PAGE-03 APG hard gates + axe-while-open, deterministic and sleep-free, wired into the config and passing on chromium.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm 8×3 checkpoint lane green (recapture only intended deltas) and ratify the 6 L3 ledger rows</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts, guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts (L150-345 the 8 checkpoints + axe; L132-148 assertCheckpointScreenshot tolerances)
    - guides/reference/admin-quality-ledger.md (L14-30 parsing rules + monotonic guard; L61-66 the 6 L3 rows currently Tier 1 with '#' evidence placeholders)
    - scripts/ci/snapshot-recapture-gate.sh, scripts/ci/snapshot-canary-guard.sh, scripts/ci/quality-ledger-monotonic.sh
    - .planning/phases/189-page-compositions-l3/189-UI-SPEC.md (L324-353 ratification contract; L351 recapture path; L353 monotonic guard)
    - .planning/phases/189-page-compositions-l3/189-CONTEXT.md (D-12 recapture vs accidental diff; D-13)
  </read_first>
  <action>
    Run the existing 8×3 checkpoint lane against the example app booted with Plan 01's hook + CSS
    in place. The ConfirmDialog hook + scroll-lock CSS should produce NO visual delta on the
    capture checkpoints (the dialogs are only visible when triggered, and captures are
    viewport-only on the page-default state). Confirm all 8 checkpoints × 3 projects pass with axe
    clean.

    IF an intended visual delta appears (e.g. a checkpoint that legitimately shows the overlay, or
    a rhythm fix from Plan 02 that changes a captured page), recapture ONLY those slugs via
    `scripts/ci/snapshot-recapture-gate.sh` with the affected slugs declared in the allowlist, then
    reset the allowlist to empty and confirm `scripts/ci/snapshot-canary-guard.sh` keeps the canary
    slug (impersonation-banner) untouched (D-12, UI-SPEC L351). NEVER commit an accidental diff. If
    NO delta appears, do not recapture — record "no visual delta; baselines unchanged".

    You MAY add a small confirmatory assertion to the existing `user-detail` checkpoint (e.g.
    assert the "Revoke session" trigger exists — already present at L216-217) but do NOT add the
    full keyboard/focus interaction journey here (that lives in Task 1's dedicated spec per D-13).

    Then ratify the 6 L3 ledger rows in `guides/reference/admin-quality-ledger.md` (L61-66).
    Replace the `(#)` placeholder evidence links with real executable evidence links following the
    L1/L2 row style:
    - index-live → admin-checkpoints global-overview
    - organization-live → admin-checkpoints org-overview
    - users-index-live → admin-checkpoints global-user-index/users-index
    - user-show-live → admin-checkpoints user-detail PLUS admin-modal-interaction.spec.ts
    - audit-index-live → admin-checkpoints audit-explorer/audit-index
    - audit-user-live → admin-checkpoints user-audit
    Tier MUST NOT decrease (monotonic guard floor is Tier 1 — D-12). Set the achieved tier per
    Plan 02's recorded evidence (Tier 1 ratified is the floor; do NOT claim Tier 2 award-grade
    unless a separate award-grade polish review was actually run — match Phase 188's posture,
    STATE.md "Tier 1 rather than claiming award-grade Tier 2"). Keep the tier column a single
    integer 0/1/2 with no decorators (parsing rules L14-30).
  </action>
  <acceptance_criteria>
    - 8×3 checkpoint lane passes with axe clean: `npx playwright test admin-checkpoints` exits 0 across the three admin-checkpoints projects (boot example on alt PORT, pre-compiled)
    - No accidental snapshot diff: `scripts/ci/snapshot-canary-guard.sh` exits 0 (canary slug untouched) and the snapshot allowlist is at empty steady-state after the run
    - All 6 L3 rows have real (non-`#`) evidence links: `grep -E '^\| (index-live|organization-live|users-index-live|user-show-live|audit-index-live|audit-user-live) ' guides/reference/admin-quality-ledger.md | grep -c '(#)'` returns 0
    - user-show-live row references the modal interaction spec: `grep 'user-show-live' guides/reference/admin-quality-ledger.md | grep -c 'admin-modal-interaction'` returns 1
    - Monotonic guard green (no tier decreased): `scripts/ci/quality-ledger-monotonic.sh` exits 0 versus the base ref
    - Tier column remains single-integer per parsing rules: each of the 6 rows' 4th `|`-field matches `^[012]$`
  </acceptance_criteria>
  <verify>
    <automated>(cd test/example/priv/playwright && npx playwright test admin-checkpoints) && bash scripts/ci/snapshot-canary-guard.sh && bash scripts/ci/quality-ledger-monotonic.sh && grep 'user-show-live' guides/reference/admin-quality-ledger.md | grep -c 'admin-modal-interaction'</automated>
  </verify>
  <done>8×3 checkpoint lane + axe green with no accidental diff (canary untouched, allowlist empty); all 6 L3 ledger rows ratified with real executable evidence links; user-show-live cites the modal spec; monotonic guard green; tiers are valid single integers and never decreased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ↔ example app | Read-only behavioral assertions + screenshots; no production code or auth surface changed. |
| ledger evidence ↔ CI monotonic guard | Evidence links must be executable, tiers must not decrease (merge-blocking guard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-08 | Repudiation | ledger evidence integrity | mitigate | Ratification cites executable specs (admin-checkpoints + admin-modal-interaction), not intent notes; acceptance criteria assert no `(#)` placeholders remain and the modal spec is referenced (D-12). |
| T-189-09 | Tampering | accidental snapshot baseline drift | mitigate | Recapture only via the canary-guarded gate; acceptance asserts the canary slug is untouched and the allowlist returns to empty steady-state (D-12, UI-SPEC L351). |
| T-189-10 | Tampering | monotonic guard regression | mitigate | `quality-ledger-monotonic.sh` must exit 0; tiers asserted as single integers that never decrease (Tier 1 floor). |
| T-189-11 | DoS (assistive-tech) | modal focus trap evidence | mitigate | The interaction spec proves Escape always closes and focus returns to the trigger — i.e. proves NO keyboard/AT lockout was introduced. |

Test-and-evidence phase: no new server endpoint, no untrusted input, no auth change. Threats
are proportionate.
</threat_model>

<verification>
- `npx playwright test admin-modal-interaction --project=chromium` exits 0.
- `npx playwright test admin-checkpoints` (3 projects) exits 0 with axe clean.
- `scripts/ci/snapshot-canary-guard.sh` exits 0; allowlist at empty steady-state.
- `scripts/ci/quality-ledger-monotonic.sh` exits 0.
- All 6 L3 rows carry real evidence links; user-show-live cites the modal spec.
</verification>

<success_criteria>
- Dedicated modal-interaction spec proves all PAGE-03 APG hard gates + axe-while-open, deterministic + sleep-free, wired in and passing.
- 8×3 checkpoint lane + axe green with no accidental visual diff (canary untouched, allowlist empty).
- 6 L3 ledger rows ratified with executable evidence; user-show-live cites the modal interaction spec; tiers never decreased; monotonic guard green.
</success_criteria>

<output>
Create `.planning/phases/189-page-compositions-l3/189-03-SUMMARY.md` when done
</output>
