---
phase: 189-page-compositions-l3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/live/index_live.ex
  - lib/sigra/admin/live/organization_live.ex
  - lib/sigra/admin/live/users_index_live.ex
  - lib/sigra/admin/live/audit_index_live.ex
  - lib/sigra/admin/live/audit_user_live.ex
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-04, PAGE-05]
must_haves:
  truths:
    - "index_live and organization_live present IA order: header → posture notice → task cards → (org tail), no page_back"
    - "users_index_live, audit_index_live, audit_user_live render pagination honestly — no phantom 'page 1 of 1' nav when results fit one page"
    - "Each of the 5 graded pages uses a single <h1> page title and <h2> section headings with no skipped levels, content within a <main> landmark"
    - "audit_user_live uses breadcrumbs for return context, not page_back"
    - "No flush adjacent sections and no double-gap on the Overview pages (consistent sg-stack--6 between major sections)"
  artifacts:
    - path: "lib/sigra/admin/live/index_live.ex"
      provides: "Overview archetype: header/posture/task-card IA + landmark/heading order"
    - path: "lib/sigra/admin/live/organization_live.ex"
      provides: "Overview-instance IA (D-04) + member roster/invitations tail last"
    - path: "lib/sigra/admin/live/users_index_live.ex"
      provides: "List archetype IA + honest multi_page? pagination guard (ratified)"
    - path: "lib/sigra/admin/live/audit_index_live.ex"
      provides: "PAGE-04 explorer IA + honest pagination guard"
    - path: "lib/sigra/admin/live/audit_user_live.ex"
      provides: "PAGE-04 explorer-leaf IA + breadcrumb return context + honest pagination"
  key_links:
    - from: "lib/sigra/admin/live/users_index_live.ex pagination nav"
      to: "multi_page?/1 guard"
      via: ":if guard on the pagination <nav>"
      pattern: "multi_page\\?\\(@meta\\)"
    - from: "lib/sigra/admin/live/audit_index_live.ex pagination nav"
      to: "single-page honesty guard"
      via: ":if guard hiding nav when one page"
      pattern: "multi_page\\?|show_pagination\\?"
---

<objective>
Audit and (where the audit surfaces a gap) correct the page-level IA ordering, vertical
rhythm, landmark/heading order, and honest-pagination affordances on the 5 admin LiveViews
NOT owned by Plan 01 — the Overview archetype (`index_live`, `organization_live`), the List
archetype (`users_index_live`), and the two PAGE-04 explorer pages (`audit_index_live`,
`audit_user_live`) — against the EXISTING L3 rubric. No new rubric is authored (D-01).

Purpose: PAGE-01 archetype conformance + vertical rhythm, PAGE-02 GOV.UK-derived IA
(general→specific, tasks-first/posture-second/capabilities-last), PAGE-04 explicit scoring of
the non-archetypal explorer pages on their own terms (D-05), and PAGE-05 landmark/heading
order. This is an AUDIT phase: most of these pages are already conformant from v1.34/188 — fix
only what the rubric flags (flush/double-gap, heading skips, phantom pagination); do NOT
rewrite conformant code.

Output: Page-local IA/rhythm/landmark corrections (if any) on 5 LiveViews; existing honest
pagination guards confirmed/extended to the audit explorers. Ratification evidence is owned by
Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-page-compositions-l3/189-CONTEXT.md
@.planning/phases/189-page-compositions-l3/189-PATTERNS.md
@.planning/phases/189-page-compositions-l3/189-UI-SPEC.md
@guides/reference/admin-fractal-scorecard.md
@guides/reference/admin-quality-ledger.md

<interfaces>
<!-- Honest-pagination guard to REUSE as the conformance anchor (do not rewrite). -->
users_index_live.ex multi_page?/1 (L513-517):
  defp multi_page?(nil), do: false
  defp multi_page?(meta), do: (meta.total_pages || 1) > 1 or not is_nil(meta.previous_page) or not is_nil(meta.next_page)

Render guards (users_index_live.ex L348-379): empty_state when rows == []; single-page all-results
label (no nav) when meta && rows != [] && not multi_page?(meta); real <nav> only when multi_page?(meta).

Scorecard L3 add-ons (admin-fractal-scorecard.md L81-101): archetype conformance, GOV.UK IA,
principle-of-least-surprise, page vertical rhythm (no flush / no double-gap, sg-stack--6 between
sections, sg-stack--4 inside cards), landmark/heading order, focus management on navigate/patch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit + fix Overview archetype pages (index_live, organization_live)</name>
  <files>lib/sigra/admin/live/index_live.ex, lib/sigra/admin/live/organization_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/index_live.ex (FULL render — page header, alarm notice, task-card grid, section wrappers)
    - lib/sigra/admin/live/organization_live.ex (FULL render — same archetype + member roster/invitations tail)
    - guides/reference/admin-fractal-scorecard.md (L81-101 L3 add-ons)
    - .planning/phases/189-page-compositions-l3/189-UI-SPEC.md (L139-159 PAGE-01 Overview IA order + vertical rhythm + no page_back)
    - .planning/phases/189-page-compositions-l3/189-CONTEXT.md (D-02 GOV.UK derived ordering; D-03/D-04 archetype mapping)
  </read_first>
  <action>
    Grade both pages against the L3 add-ons (scorecard L81-101) using the UI-SPEC PAGE-01 IA
    order (L141-148): [1] page header (h1 + kicker + copy), [2] alarm notice (posture),
    [3] task-card grid (tasks-first), [org only 4] member roster + invitations tail
    (capabilities-last). Apply the principle-of-least-surprise + GOV.UK-derived ordering from
    D-02 (general→specific, most-common-need-first, reference/config last).

    Fix ONLY rubric violations the audit surfaces; do not rewrite conformant markup:
    - Vertical rhythm: ensure `sg-stack--6` between major sections; remove any flush adjacency
      (missing spacer) or double-gap (a redundant outer sg-stack wrapper plus per-section
      spacing) per UI-SPEC L154-158.
    - Landmark/heading: page content inside a `<main>` landmark; exactly one `<h1>` page title;
      section headings `<h2>` with no skipped levels (PAGE-05, scorecard L97-99).
    - Confirm NO `page_back` on Overview screens (UI-SPEC L150 — Overview archetype contract);
      breadcrumb is a single "Overview"/"Org Overview" crumb.
    - organization_live is the org-scoped INSTANCE of Overview (D-04), NOT a fourth archetype:
      keep its member-roster + pending-invitations sections LAST (capabilities-last).

    If a page is already fully conformant, make NO code change and record "conformant, no edit"
    in the SUMMARY with the specific rubric rows checked (evidence, not intent).
  </action>
  <acceptance_criteria>
    - Exactly one h1 per page: `grep -c '<h1' lib/sigra/admin/live/index_live.ex` returns 1; same for organization_live.ex (a render-time single page title)
    - No page_back component used on either Overview page: `grep -c 'page_back\|<.page_back' lib/sigra/admin/live/index_live.ex` returns 0
    - No skipped heading levels (no <h3>/<h4> appearing before any <h2>) — verified by reading the render and noting heading order in the SUMMARY
    - Compiles clean: `mix compile --warnings-as-errors` exits 0
    - SUMMARY records, per page, which L3 scorecard rows (archetype conformance, GOV.UK IA, least-surprise, vertical rhythm, landmark/heading) pass and which (if any) required an edit
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors && test "$(grep -c 'page_back' lib/sigra/admin/live/index_live.ex)" = "0"</automated>
  </verify>
  <done>Both Overview pages conform to PAGE-01 IA + vertical rhythm + landmark/heading; no page_back; org tail is capabilities-last; edits (if any) are rubric-driven and recorded with evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Audit List + PAGE-04 explorer IA and confirm honest pagination on all three</name>
  <files>lib/sigra/admin/live/users_index_live.ex, lib/sigra/admin/live/audit_index_live.ex, lib/sigra/admin/live/audit_user_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/users_index_live.ex (L348-379 render guards; L513-517 multi_page?/1 — the honesty anchor)
    - lib/sigra/admin/live/audit_index_live.ex (FULL render — filter/results/export IA + its pagination block)
    - lib/sigra/admin/live/audit_user_live.ex (FULL render — breadcrumb return context + results + pagination)
    - guides/reference/admin-fractal-scorecard.md (L81-101 L3 add-ons)
    - .planning/phases/189-page-compositions-l3/189-UI-SPEC.md (L161-179 PAGE-02 List IA + pagination honesty + sticky no-shift + patch focus; L239-256 PAGE-04 explorer IA)
    - .planning/phases/189-page-compositions-l3/189-PATTERNS.md (section "users_index_live.ex" L199-228; "Honest affordances" L322-325)
    - .planning/phases/189-page-compositions-l3/189-CONTEXT.md (D-05 score explorers on own terms; D-09 honest pagination)
  </read_first>
  <action>
    `users_index_live.ex` is the List archetype anchor — its `multi_page?/1` guard (L513-517) and
    render guards (L348-379) are RATIFIED as evidence (D-09); do NOT rewrite them. Confirm the
    List IA order (UI-SPEC L163-173): header+summary chips → scope ribbon → filter panel →
    applied chips → results (table/card) → empty state → honest pagination. Verify the single-page
    case renders an all-results label, not a phantom "page 1 of 1" nav.

    `audit_index_live.ex` and `audit_user_live.ex` are the PAGE-04 explorer pages — score them on
    their OWN bespoke filter/export/explorer IA (D-05, UI-SPEC L249-256), NOT forced into the List
    archetype. Required corrections (only where the audit flags a gap):
    - Pagination honesty: each explorer's pagination `<nav>` must be hidden entirely when results
      fit one page (no phantom "page 1 of 1" with disabled controls), matching the
      `multi_page?`/`show_pagination?`-style guard pattern (D-09, PATTERNS L226). If an explorer
      already guards this, confirm + record it; if it renders a phantom single-page nav, add/extend
      the guard.
    - `audit_user_live.ex` uses breadcrumbs for return context, NOT `page_back` (UI-SPEC L253).
    - Landmark/heading order (PAGE-05): `<main>` wraps content; single `<h1>`; `<h2>` sections, no
      skips. Mobile table→card swap stays content-equivalent (already ratified in 188 — confirm).
    - Sticky/scroll: if any filter/results header is sticky, it must use `position: sticky; top:`
      with no layout shift (UI-SPEC L177) — do NOT introduce fixed positioning.

    Do NOT change filter-apply focus behavior to reset to h1 — on a PATCH (filter apply), focus
    stays on/returns to the filter input/submit (UI-SPEC L179); only full navigations reset focus
    to h1 (that behavior is verified in Plan 03).
  </action>
  <acceptance_criteria>
    - users_index_live multi_page? guard intact (not rewritten): `grep -c 'defp multi_page?' lib/sigra/admin/live/users_index_live.ex` returns 2 (nil clause + meta clause)
    - audit_user_live uses breadcrumbs, not page_back: `grep -c 'page_back' lib/sigra/admin/live/audit_user_live.ex` returns 0
    - Each explorer hides its pagination nav on single-page: verified by a guarded `:if` on the pagination nav in audit_index_live.ex and audit_user_live.ex (a `multi_page?`/`show_pagination?`/equivalent meta guard on the nav) — recorded in SUMMARY with the exact guard expression and line
    - Exactly one h1 per page (render-time single page title) on all three
    - Compiles clean: `mix compile --warnings-as-errors` exits 0
    - Relevant ExUnit/behavior assertions still pass: `mix test test/example` related admin tests (or the project's admin test scope) exit 0
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors && test "$(grep -c 'defp multi_page?' lib/sigra/admin/live/users_index_live.ex)" = "2" && test "$(grep -c 'page_back' lib/sigra/admin/live/audit_user_live.ex)" = "0"</automated>
  </verify>
  <done>List + both explorer pages conform to their IA contracts; pagination is honest (no phantom single-page nav) on all three; audit_user_live uses breadcrumbs; landmark/heading order correct; users_index multi_page? guard preserved; project compiles + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operator ↔ admin LiveView render | Page-level markup/IA changes only; no new input parsing, no auth/scope change. Scope gating and data queries are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-05 | Information Disclosure | pagination/scope rendering on explorer pages | mitigate | Pagination honesty edits change only whether the nav renders, not which rows the query returns; existing scope gating is untouched (acceptance criteria keep multi_page?/query logic intact). |
| T-189-06 | Tampering | accidental rewrite of ratified guards | mitigate | Acceptance asserts the users_index multi_page?/1 guard is preserved (count == 2); audit is additive/confirmatory, not a rewrite. |
| T-189-07 | Repudiation | claiming IA conformance without evidence | mitigate | SUMMARY must record per-page rubric rows checked with line evidence; Plan 03 supplies executable axe/Playwright proof. |

No new server endpoint, no new untrusted input, no auth-logic change — this is page-local
markup/IA auditing. Threats are proportionate to a build-free audit phase.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- users_index `multi_page?/1` guard preserved (count 2); audit_user_live has no page_back.
- Each explorer guards single-page pagination; recorded with evidence in SUMMARY.
- Admin ExUnit/behavior tests green.
- Executable axe + screenshot + focus-on-navigate ratification is owned by Plan 03.
</verification>

<success_criteria>
- Overview pages (index, organization) conform to PAGE-01 IA + vertical rhythm + landmark/heading; no page_back; org tail capabilities-last.
- List + both explorers conform to their IA; pagination honest on all three; audit_user_live breadcrumb return context.
- All edits are rubric-driven (no rewrite of conformant/ratified code) and recorded with line evidence in the SUMMARY.
- Project compiles warnings-as-errors clean; admin tests green.
</success_criteria>

<output>
Create `.planning/phases/189-page-compositions-l3/189-02-SUMMARY.md` when done
</output>
