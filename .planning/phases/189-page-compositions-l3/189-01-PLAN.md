---
phase: 189-page-compositions-l3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/admin_hooks.js
  - test/example/assets/js/admin_hooks.js
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - lib/sigra/admin/live/user_show_live.ex
  - lib/sigra/admin/live/branding_live.ex
autonomous: true
requirements: [PAGE-03]
must_haves:
  truths:
    - "Opening the session-revoke confirm dialog on user_show_live moves focus into the dialog (Cancel button)"
    - "Tab and Shift-Tab cycle only within the dialog and cannot reach background content"
    - "Escape closes the confirm dialog on both user_show_live and branding_live"
    - "Closing the dialog (Escape, Cancel, or confirm) returns focus to the element that opened it"
    - "The same ConfirmDialog behavior applies to the branding_live restore-defaults dialog"
    - "The ratified CmdK hook is unchanged"
  artifacts:
    - path: "priv/templates/sigra.install/admin/admin_hooks.js"
      provides: "Net-new ConfirmDialog hook + registration in window.SigraAdminHooks"
      contains: "ConfirmDialog"
    - path: "test/example/assets/js/admin_hooks.js"
      provides: "Byte-identical mirror of the canonical admin_hooks.js"
      contains: "ConfirmDialog"
    - path: "priv/templates/sigra.install/admin/sigra_admin.css"
      provides: "body.sg-body-scroll-locked rule in @layer sg-components"
      contains: "sg-body-scroll-locked"
    - path: "lib/sigra/admin/live/user_show_live.ex"
      provides: "phx-hook=ConfirmDialog + id on the :if-gated sg-confirm-overlay"
      contains: "phx-hook=\"ConfirmDialog\""
    - path: "lib/sigra/admin/live/branding_live.ex"
      provides: "phx-hook=ConfirmDialog + id on the restore-defaults sg-confirm-overlay"
      contains: "phx-hook=\"ConfirmDialog\""
  key_links:
    - from: "lib/sigra/admin/live/user_show_live.ex (sg-confirm-overlay)"
      to: "window.SigraAdminHooks.ConfirmDialog"
      via: "phx-hook attribute on the overlay div"
      pattern: "phx-hook=\"ConfirmDialog\""
    - from: "lib/sigra/admin/live/branding_live.ex (sg-confirm-overlay)"
      to: "window.SigraAdminHooks.ConfirmDialog"
      via: "phx-hook attribute on the overlay div"
      pattern: "phx-hook=\"ConfirmDialog\""
---

<objective>
Build the ONE net-new code slice of Phase 189: the `ConfirmDialog` LiveView hook (PAGE-03)
providing WAI-ARIA APG "Dialog (Modal)" behavior on the existing `sg-confirm-overlay` /
`sg-confirm-dialog` markup, wire it into both `user_show_live.ex` and `branding_live.ex`,
and land the one anticipated CSS rule (`body.sg-body-scroll-locked`) across all three parity
surfaces.

Purpose: The confirm dialogs are currently pure server-rendered markup with no `phx-hook`,
keydown, or focus management — Phase 188 explicitly deferred this to the L3 page-level
follow-up (188 D-13/D-14). This plan closes that gap so the modal traps focus, closes on
Escape, and returns focus to its trigger.

Output: Net-new `ConfirmDialog` hook in both JS surfaces; scroll-lock rule in all three CSS
surfaces; hook wired onto both LiveView overlays. The ratified `CmdK` hook is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-page-compositions-l3/189-CONTEXT.md
@.planning/phases/189-page-compositions-l3/189-PATTERNS.md
@.planning/phases/189-page-compositions-l3/189-UI-SPEC.md

<interfaces>
<!-- Reuse these verbatim from the existing CmdK hook (admin_hooks.js). Do NOT modify CmdK. -->

Module-level focusable selector (admin_hooks.js L115-116) — reuse as-is:
  var FOCUSABLE = 'a[href], button:not([disabled]), input, [tabindex]:not([tabindex="-1"])';

CmdK focus-trap shape (admin_hooks.js L316-328) — copy the Tab/Shift-Tab boundary logic,
but query the `.sg-confirm-dialog` element instead of `this.dialog`.

CmdK Escape + outside-click pattern (admin_hooks.js L196-202, L263-267, L301-303).

Hook registration object (admin_hooks.js L1009-1014):
  window.SigraAdminHooks = { AuthBrandingPreview, CmdK, CopyToClipboard, ThemeSwitch }

File-style constraints (header admin_hooks.js L1-21): plain JS, NO import/export, IIFE-scoped
`(function () { "use strict"; ... })()`, `function` (not arrow-only), never throw from a
click/keydown handler.

Existing confirm markup — user_show_live.ex L315-333 (gated by @confirm_action):
  <div :if={@confirm_action} class="sg-confirm-overlay" role="presentation"> ...
    <section class="sg-confirm-dialog" role="dialog" aria-modal="true"
             aria-labelledby="user-session-confirm-title"> ...
    Cancel button (phx-click="cancel_confirm") rendered FIRST, then confirm (phx-click="confirm_action")

Existing confirm markup — branding_live.ex L349-368 (gated by @restore_defaults_open?):
  same structure, aria-labelledby="restore-defaults-title", Cancel = phx-click="cancel_restore_defaults"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the ConfirmDialog hook to admin_hooks.js (both surfaces)</name>
  <files>priv/templates/sigra.install/admin/admin_hooks.js, test/example/assets/js/admin_hooks.js</files>
  <read_first>
    - priv/templates/sigra.install/admin/admin_hooks.js (FULL: header L1-21 style rules; CmdK hook L115-367 — FOCUSABLE L115-116, trapFocus L316-328, Escape/keydown L263-267/L301-303, close/focus-restore L335-354, lifecycle L120-151/L356-366; registration L1009-1014; throw-swallow L387-395)
    - .planning/phases/189-page-compositions-l3/189-PATTERNS.md (section "admin_hooks.js — net-new ConfirmDialog hook", excerpts 1-6)
    - .planning/phases/189-page-compositions-l3/189-UI-SPEC.md (L198-226 PAGE-03 behavior table; L224 CmdK non-interference; L226 trigger tracking)
    - .planning/phases/189-page-compositions-l3/189-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <action>
    Define a new `var ConfirmDialog = { ... }` hook in the IIFE of the CANONICAL template
    `priv/templates/sigra.install/admin/admin_hooks.js`, placed adjacent to the existing
    `CmdK` definition. Do NOT modify `CmdK` (it is ratified — UI-SPEC L224). You MAY share the
    module-level `FOCUSABLE` constant (L115-116) by reference, but duplicate-and-specialize the
    trap logic to avoid coupling to CmdK.

    The hook attaches to the `.sg-confirm-overlay` element (the `:if`-gated div). Implement APG
    Dialog behavior, scoring behavior not technique (D-07):
    - `mounted()`: the overlay just appeared via `:if`. Capture the trigger by storing
      `this._trigger = document.activeElement` BEFORE moving focus (UI-SPEC L226, PATTERNS excerpt 4).
      Query `.sg-confirm-dialog` inside `this.el`, find focusables via `FOCUSABLE`, and `focus()`
      the FIRST focusable (Cancel renders first in DOM in both LiveViews — user_show_live.ex L325,
      branding_live.ex L361 — so initial focus lands on Cancel per D-07). Add a document-level
      `keydown` listener (store the bound handler on `this` for cleanup) and add
      `sg-body-scroll-locked` to `document.body.classList`.
    - keydown handler: on `Escape`, `event.preventDefault()` then close the dialog by dispatching
      a synthetic click on the Cancel button found inside `.sg-confirm-dialog` (DO NOT hardcode a
      page-specific server event name like `cancel_confirm` vs `cancel_restore_defaults` —
      PATTERNS "branding_live" note; the generic hook must work for both). On `Tab`, run the
      focus-trap boundary logic from CmdK.trapFocus (excerpt 2) querying `.sg-confirm-dialog`.
    - Optional enhancement (D-08, NOT a gate): outside-click — clicking the overlay (event.target
      === this.el) dispatches the Cancel click. Implement it; it adds no ratification risk.
    - `destroyed()`: the overlay was removed via `:if` (server set the assign to nil/false).
      Remove the document keydown listener, remove `sg-body-scroll-locked` from
      `document.body.classList`, and restore focus: `if (this._trigger && this._trigger.focus) this._trigger.focus()`
      (PATTERNS excerpt 4 — KEY DIFFERENCE from CmdK which restores to `this.el`).
    - Never throw from the keydown/click handlers (follow the L387-395 swallow pattern).

    Register the hook in `window.SigraAdminHooks` (L1009-1014) in alphabetical order:
    `ConfirmDialog: ConfirmDialog,` between `CmdK` and `CopyToClipboard`.

    Then copy the ENTIRE canonical file verbatim to `test/example/assets/js/admin_hooks.js` so
    the two surfaces are byte-identical (D-10). Do not hand-edit the mirror separately.
  </action>
  <acceptance_criteria>
    - `grep -c 'var ConfirmDialog' priv/templates/sigra.install/admin/admin_hooks.js` returns 1
    - `grep -c 'ConfirmDialog: ConfirmDialog' priv/templates/sigra.install/admin/admin_hooks.js` returns 1
    - CmdK is unchanged: `git diff priv/templates/sigra.install/admin/admin_hooks.js` shows no lines removed/changed inside the `var CmdK = {` ... `}` block (only additions of the new ConfirmDialog block + the one registration line)
    - Byte-parity: `diff -q priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js` exits 0 (IDENTICAL)
    - No ESM: `grep -v '^//' priv/templates/sigra.install/admin/admin_hooks.js | grep -cE '\b(import|export)\b'` returns 0
  </acceptance_criteria>
  <verify>
    <automated>diff -q priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js && grep -c 'ConfirmDialog: ConfirmDialog' priv/templates/sigra.install/admin/admin_hooks.js</automated>
  </verify>
  <done>ConfirmDialog hook defined + registered in canonical admin_hooks.js, byte-identical mirror in example, CmdK untouched, no ESM.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add body.sg-body-scroll-locked rule across all three CSS surfaces</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</files>
  <read_first>
    - priv/templates/sigra.install/admin/sigra_admin.css (L15 cascade-layer declaration order; L637-675 .sg-confirm-* block in @layer sg-components — the placement analog)
    - .planning/phases/189-page-compositions-l3/189-PATTERNS.md (section "Three-surface CSS byte-parity"; analog block L284-301)
    - .planning/phases/189-page-compositions-l3/189-UI-SPEC.md (L228-237 CSS additions needed; L54 app.css keeps only vt-* glue)
  </read_first>
  <action>
    In the CANONICAL template `priv/templates/sigra.install/admin/sigra_admin.css`, inside
    `@layer sg-components` adjacent to the existing `.sg-confirm-overlay` block (around L637-675),
    add a single rule: `body.sg-body-scroll-locked { overflow: hidden; }` (UI-SPEC L232-235).
    No raw hex/px beyond `overflow: hidden` (a keyword, allowed). Do NOT touch any Phase 186
    token value and do NOT add this to `app.css`.

    Then copy the canonical file verbatim to both mirrors:
    `test/example/priv/static/assets/sigra_admin.css` and
    `test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css` (D-10/D-11 — leaving
    it example-only is a DIST-05 byte-parity failure).
  </action>
  <acceptance_criteria>
    - `grep -c 'sg-body-scroll-locked' priv/templates/sigra.install/admin/sigra_admin.css` returns 1
    - The new rule sits inside `@layer sg-components` (appears after the `@layer sg-components {` that contains `.sg-confirm-overlay`, before its closing brace)
    - Byte-parity (HARD CONSTRAINT D-10/D-11): both diffs exit 0:
      `diff -q priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css`
      `diff -q priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css`
    - No token-value change: `git diff priv/templates/sigra.install/admin/sigra_admin.css` shows no edits to any `--sg-*:` declaration line
  </acceptance_criteria>
  <verify>
    <automated>diff -q priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css && diff -q priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css && grep -c 'sg-body-scroll-locked' priv/templates/sigra.install/admin/sigra_admin.css</automated>
  </verify>
  <done>body.sg-body-scroll-locked rule present in @layer sg-components of all three byte-identical CSS surfaces; no token value changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire phx-hook=ConfirmDialog + id onto both LiveView overlays</name>
  <files>lib/sigra/admin/live/user_show_live.ex, lib/sigra/admin/live/branding_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex (L315-333 the :if={@confirm_action} sg-confirm-overlay block; L178-187 the open_revoke_session trigger button)
    - lib/sigra/admin/live/branding_live.ex (L349-368 the :if={@restore_defaults_open?} sg-confirm-overlay block)
    - .planning/phases/189-page-compositions-l3/189-PATTERNS.md (sections "user_show_live.ex" L120-171 and "branding_live.ex" L174-195)
    - .planning/phases/189-page-compositions-l3/189-UI-SPEC.md (L200-214 hook attachment + aria verification)
  </read_first>
  <action>
    In `lib/sigra/admin/live/user_show_live.ex`, on the `:if={@confirm_action}` overlay `<div
    class="sg-confirm-overlay" role="presentation">` (L315), add two attributes:
    `id="user-session-confirm-overlay"` and `phx-hook="ConfirmDialog"` (LiveView hooks require a
    DOM id). Do NOT change the `role="presentation"` on the overlay, the `role="dialog"`/
    `aria-modal="true"`/`aria-labelledby="user-session-confirm-title"` on the inner `<section>`
    (L317-320), the Cancel-first button order (L325 cancel_confirm before L328 confirm_action),
    or any server event names. Verify the `aria-labelledby` id matches the `<p id="user-session-confirm-title">`
    at L322 (it does — do not change it).

    In `lib/sigra/admin/live/branding_live.ex`, apply the SAME wiring to the
    `:if={@restore_defaults_open?}` overlay `<div class="sg-confirm-overlay">` (L349): add
    `id="restore-defaults-overlay"` and `phx-hook="ConfirmDialog"`. Leave the inner dialog aria
    attributes (L351-354, aria-labelledby="restore-defaults-title"), Cancel-first order
    (L361-366 cancel_restore_defaults), and event names unchanged. The hook is generic and
    relies on `:if`-driven `destroyed()` for focus restore — no page-specific event name is baked
    into it (PATTERNS "branding_live" note).

    Do NOT add new inline-style spacing — the existing `style="margin-top: var(--sg-space-3);"`
    is a documented prior-phase exception (UI-SPEC L78, PATTERNS L169-171).
  </action>
  <acceptance_criteria>
    - `grep -c 'phx-hook="ConfirmDialog"' lib/sigra/admin/live/user_show_live.ex` returns 1
    - `grep -c 'id="user-session-confirm-overlay"' lib/sigra/admin/live/user_show_live.ex` returns 1
    - `grep -c 'phx-hook="ConfirmDialog"' lib/sigra/admin/live/branding_live.ex` returns 1
    - `grep -c 'id="restore-defaults-overlay"' lib/sigra/admin/live/branding_live.ex` returns 1
    - aria attributes preserved: `grep -c 'aria-modal="true"' lib/sigra/admin/live/user_show_live.ex` returns 1 and same for branding_live.ex
    - Compiles clean: `mix compile --warnings-as-errors` exits 0
    - No new inline margin-top added: `grep -c 'style="margin-top' lib/sigra/admin/live/user_show_live.ex` returns 1 (unchanged)
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -c 'phx-hook="ConfirmDialog"' lib/sigra/admin/live/user_show_live.ex lib/sigra/admin/live/branding_live.ex</automated>
  </verify>
  <done>Both confirm overlays carry id + phx-hook="ConfirmDialog"; aria attributes and event names unchanged; project compiles warnings-as-errors clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM ↔ ConfirmDialog hook | Client-side keydown/click handling on already-rendered, server-gated overlay markup. No untrusted input is parsed; no server endpoint added. |
| installer template ↔ generated host | New JS/CSS must reach generated hosts byte-identically, not example-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-01 | Tampering | ConfirmDialog hook closing the destructive overlay | mitigate | Hook only DISPATCHES a click on the existing server-gated Cancel button / relies on `:if` toggling; it never auto-confirms and never bypasses the `phx-click="confirm_action"` server path. Confirm requires an explicit user click on the danger button. |
| T-189-02 | Denial of Service | Focus trap / keyboard handling | mitigate | Trap is scoped to the dialog while open and fully released in `destroyed()` (listener removed, body class removed, focus returned to trigger). No assistive-tech lockout: Escape always closes; focus returns to the trigger; APG-conformant containment. |
| T-189-03 | Repudiation/Integrity | Example-only CSS/JS drift | mitigate | Byte-parity diff gates on all 3 CSS + 2 JS surfaces (Tasks 1-2 acceptance criteria) prevent a DIST-05 generated-host-honesty regression (D-11). |
| T-189-04 | Spoofing/Elevation | ratified CmdK hook | accept | CmdK is not modified; acceptance criteria assert no edits inside the CmdK block. No new privilege or auth surface introduced. |

No new untrusted input, no new server endpoint, no auth-logic change — threat surface is a
client-side focus-trap hook + one CSS keyword rule. Threats above are proportionate; none are
fabricated for a build-free CSS/JS audit phase.
</threat_model>

<verification>
- `diff -q` byte-identity passes on all 3 CSS surfaces and both JS surfaces.
- `mix compile --warnings-as-errors` exits 0.
- ConfirmDialog registered; CmdK block unchanged in the diff.
- Manual/automated behavioral proof of focus-trap/Escape/return is owned by Plan 03 (the
  ratification lane) — this plan ships the mechanism, Plan 03 proves the behavior.
</verification>

<success_criteria>
- ConfirmDialog hook exists + registered in both byte-identical JS surfaces; CmdK untouched.
- `body.sg-body-scroll-locked` rule in @layer sg-components across all three byte-identical CSS surfaces.
- Both LiveView overlays carry `id` + `phx-hook="ConfirmDialog"` with all existing aria attributes and server event names preserved.
- No Phase 186 token value changed; no new inline-style spacing.
</success_criteria>

<output>
Create `.planning/phases/189-page-compositions-l3/189-01-SUMMARY.md` when done
</output>
