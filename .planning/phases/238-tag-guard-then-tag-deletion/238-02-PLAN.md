---
phase: 238-tag-guard-then-tag-deletion
plan: 02
type: execute
wave: 2
depends_on: ["238-01"]
files_modified:
  - .github/rulesets/tag-namespace.json
  - scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs
  - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
autonomous: true
requirements: [REL-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A tag ruleset named `tag-namespace` exists live with an active enforcement value, target tag, and an empty bypass list, created by API rather than by the Settings wizard."
    - "Pushing the scratch tag `v9.9` to origin is rejected by GitHub server-side, and pushing `v9.9.9-rulesettest` is accepted — both observed live, both recorded with their command output."
    - "The rejection is citable by rule-suite id with the matching rule evaluation entry, and the fetched JSON is embedded in the ledger rather than only the id, because the query window expires."
    - "`.github/rulesets/tag-namespace.json` is the verbatim by-id payload of the live ruleset, so a field-scoped diff against the live read is a byte comparison."
    - "The `p19` guard is green against the committed snapshot and returns a named failure — not a boolean — against an inline malformed ruleset object, proving falsifiability in the same commit it is born."
    - "Deleting the accepted scratch tag from origin answers empirically whether the ruleset blocks tag deletion, and the answer is recorded as its own slot."
    - "Both scratch tags are removed from origin and from the local repository at the end of the plan."
    - statement: "The accept-probe demonstrates the end anchor tolerates the trailing-newline shape that a git push or CLI flow can present, so a valid release tag is not rejected by the anchor."
      verification: backstop
  artifacts:
    - .github/rulesets/tag-namespace.json
    - scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs
  key_links:
    - "live `tag-namespace` ruleset → `.github/rulesets/tag-namespace.json` (the snapshot is the repo's only record of a Settings-managed object)"
    - "`.github/rulesets/tag-namespace.json` → `p19` via the single substitutable subject helper"
    - "the `ci.yml` prohibition glob → `p19` with zero workflow edits"
  prohibitions:
    - "MUST NOT add any entry to the ruleset's bypass actor list."
    - "MUST NOT include a deletion rule in the ruleset."
    - "MUST NOT create the ruleset through the Settings wizard, which selects restrict-deletions by default."
    - "MUST NOT edit `.github/workflows/ci.yml`; the existing prohibition glob picks the guard up."
    - "MUST NOT place the guard in the `release_ref_guard` job."
    - "MUST NOT add the guard to `mix ci`."
    - "MUST NOT call the GitHub API, use a token, or touch the network from inside the guard."
    - "MUST NOT assert the ruleset's bypass actor list inside the guard; that field is invisible at CI token level and the assertion would be vacuous."
    - "MUST NOT commit the guard before the snapshot file exists, because a missing subject is a broken run rather than an absent violation and would redden the fast checks lane."
---

<objective>
Land the guard end to end on one thin path: a live server-side ruleset, an observed rejection and
an observed acceptance, a committed snapshot of the live object, and a repo-side guard that is
green on the snapshot and red on a malformed one — all in the slice that proves the architecture
before any of it is expanded.

Purpose: REL-01 requires a server-side guard demonstrated RED, landing before any deletion. This
plan is where that becomes true. It is the phase's tracer.
Output: the live `tag-namespace` ruleset, `.github/rulesets/tag-namespace.json`, the first version
of `scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs`, and four captured evidence slots.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-PATTERNS.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by Phase 238 (new — exclude from drift / source-grounding verification):

- `.github/rulesets/tag-namespace.json` (new file; new `.github/rulesets/` directory)
- `scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs` (new file)
- `test/fixtures/prohibitions/p19-tag-ruleset-absent-or-altered.json` (new file)
- `scripts/maintainers/delete-planning-tags.sh` (new file)
- `.planning/decisions/003-tag-delete-list.tsv` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/COVERAGE.md` (new file)
- GitHub ruleset named `tag-namespace` (live GitHub configuration, not a repo file)
- CI job id `tag_ruleset_drift` in `.github/workflows/ci-observe.yml` (new job)
- Shell function names `fail` and `usage`, and the `--apply` flag, in `delete-planning-tags.sh`
- JS function name `rulesetShapeIssue` in the `p19` guard
- Scratch tag names `v9.9` and `v9.9.9-rulesettest` (created and removed within this plan)
- Evidence slot names: `BEFORE-RULESET-STATE`, `BEFORE-TAG-INVENTORY`,
  `BEFORE-RULESET-FEASIBILITY-PROBE`, `AFTER-RULESET-ACTIVE`, `AFTER-SC1-REJECT-ACCEPT`,
  `AFTER-DELETE-PROBE`, `AFTER-P19-RED`, `AFTER-LOCAL-DELETE`, `AFTER-REMOTE-DELETE`,
  `AFTER-RELEASE-SURFACE`

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — a non-SemVer `v*` tag is rejected server-side and the repo knows it</name>
  <precondition>`238-01-SUMMARY.md` records a selected tier of Tier 1 or Tier 2; if it records Tier 3, this plan does not run and the phase is awaiting an operator scope decision.</precondition>
  <read_first>
    - .planning/phases/238-tag-guard-then-tag-deletion/238-01-SUMMARY.md (the selected tier — this task's ruleset body depends on it)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md (the ledger to append to)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md (Architecture Patterns 1 and 2; Code Examples "SC-1 live proof"; Pitfall 2, 3, 7)
    - scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs (the guard template: header prose, pure checker returning a named string, non-vacuity floors, the negative-control test)
    - scripts/ci/prohibitions/p12-run-id-provenance.test.mjs (lines 1-20: the offline-by-design rationale this guard's header must mirror)
    - scripts/ci/prohibitions/_lib.mjs (the single-substitutable-subject rule and the subject/repo-file readers)
    - .github/workflows/ci.yml (lines 383-393: the prohibition glob that picks the guard up, and the comment explaining why the glob is load-bearing)
  </read_first>
  <action>
Wire one path from the GitHub server to a repo-side assertion. Do this as a single vertical slice;
do not build any layer beyond what this one path needs.

**1. Create the ruleset, active.** POST the body for the tier recorded by 238-01, with
`name: "tag-namespace"`, `target: "tag"`, an active enforcement value, and an empty
`bypass_actors` list. Tier 1 body: `conditions.ref_name.include` of `refs/tags/v*`, empty exclude,
one `tag_name_pattern` rule (`operator: "regex"`, `negate: false`, GitHub's published SemVer regex
with a `^v` prefix and an optional-newline end anchor). Tier 2 body: the same include,
`conditions.ref_name.exclude` of `refs/tags/v*.*.*`, one rule of type `creation`. Include no rule
of type `deletion` under either tier — a deletion rule would recreate the delete-deadlock D-06
exists to avoid, and the Settings wizard selects that rule by default, which is why creation goes
through the API. Record `## AFTER-RULESET-ACTIVE` with the fenced POST and the round-tripped by-id
payload.

**2. Observe the rejection and the acceptance.** Create and push scratch tag `v9.9` to origin;
expect a server-side rejection with GitHub's rule-violation error. Create and push scratch tag
`v9.9.9-rulesettest`; expect acceptance. Record `## AFTER-SC1-REJECT-ACCEPT` with both fenced
commands and their full output including the rejection text. If the accept is rejected under
Tier 1, that is the end-anchor defect from RESEARCH Pitfall 2 — stop and fix the pattern rather
than recording a partial pass. Note plainly in the slot that this probe exercises the git push
route only: release-please creates its tag through the Releases API, a different route, so this
observation is indicative for release-please under Tier 1 and moot under Tier 2 (where the release
tag shape is out of the ruleset's scope); Phase 242 is the true observation. Use the word
indicative, not proven.

**3. Make the rejection citable.** Inside the rule-suite query window (default one day), fetch the
failing rule suite for the rejected ref from the rule-suites endpoint, then fetch it by id to get
the rule evaluations array, and embed the fetched JSON in the same slot. An id alone is not
enough — the window expires and the id stops resolving. Scrub nothing but credentials; the actor
fields name the public repo owner.

**4. Commit the snapshot.** Write `.github/rulesets/tag-namespace.json` as the verbatim by-id
payload of the live ruleset. This is a new directory. The file is the repo's only record of a
Settings-managed object, and its verbatim shape is what makes the later live-versus-committed
comparison a byte comparison over the compared fields.

**5. Give the guard its thinnest true body.** Create
`scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs` following the `p17` template: a
header restating the prohibition, a line naming the substitutable subject, the offline-by-design
paragraph mirroring `p12`, and a paragraph on what silently breaks if the guard is deleted. Read
the snapshot through the single-subject helper — exactly one such call in the file — and parse it
as JSON. Implement `rulesetShapeIssue(obj)` as a pure checker that returns null when clean and a
named explanatory string otherwise; in this slice it asserts only that the target is the tag
target. Add a non-vacuity floor asserting the parse produced an object with a target field at all,
with the repo's message convention that a broken parse is not a pass. Add a negative-control test
that runs the checker against an inline object carrying the branch target and asserts a non-null
named message — this is the falsifiability proof that ships in the same commit as the guard. Do
not assert the bypass actor list. Do not touch `ci.yml`: the existing glob picks the file up.

Commit order within the task matters: the snapshot file must be committed no later than the guard,
never after it.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs</automated>
    <fails_when>non-zero exit, or the TAP output contains "not ok"</fails_when>
    <automated>bash -c 'set -eo pipefail; gh api repos/szTheory/sigra/rulesets --jq "[.[]|select(.name==\"tag-namespace\")][0].id" > /tmp/238-ts-id.txt; id=$(cat /tmp/238-ts-id.txt); test -n "$id" && test "$id" != "null" || { echo "tag-namespace ruleset ABSENT" >&2; exit 1; }; gh api "repos/szTheory/sigra/rulesets/$id" > /tmp/238-live.json; jq -e ".target == \"tag\" and .enforcement == \"active\"" /tmp/238-live.json >/dev/null; jq -e "[.rules[].type]|index(\"deletion\")|not" /tmp/238-live.json >/dev/null; jq -S "{target,enforcement,conditions,rules}" /tmp/238-live.json > /tmp/238-live-norm.json; jq -S "{target,enforcement,conditions,rules}" .github/rulesets/tag-namespace.json > /tmp/238-want-norm.json; diff -u /tmp/238-want-norm.json /tmp/238-live-norm.json'</automated>
    <fails_when>non-zero exit, stderr contains "tag-namespace ruleset ABSENT", or diff prints a hunk showing the committed snapshot differs from the live ruleset</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; for slot in AFTER-RULESET-ACTIVE AFTER-SC1-REJECT-ACCEPT; do sed -n "/^## $slot\$/,/^## /p" "$f" > /tmp/238-slot.txt; test -s /tmp/238-slot.txt || { echo "slot $slot empty — the parse broke, this is not a pass" >&2; exit 1; }; grep -qE "^Status: captured" /tmp/238-slot.txt || { echo "slot $slot not captured" >&2; exit 1; }; grep -q "```" /tmp/238-slot.txt || { echo "slot $slot has no fenced producing command" >&2; exit 1; }; done; grep -q "rule_evaluations" "$f" || { echo "no rule evaluations JSON embedded — the rejection is not citable after the window expires" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke", "not captured", "no fenced producing command", or "not citable"</fails_when>
    <automated>bash -c 'set -eo pipefail; ! grep -rn "p19-tag-namespace-ruleset" .github/workflows/ci.yml; grep -q "prohibitions/\*.test.mjs" .github/workflows/ci.yml'</automated>
    <fails_when>non-zero exit — either the guard was hard-wired into the workflow by name, or the load-bearing prohibition glob is no longer present</fails_when>
  </verify>
  <acceptance_criteria>
    - A live ruleset named `tag-namespace` exists with the tag target, an active enforcement value, and no rule of type deletion.
    - `.github/rulesets/tag-namespace.json` field-scoped-diffs clean against the live by-id payload over target, enforcement, conditions and rules.
    - `node --test` on the guard exits zero against the committed snapshot.
    - The guard file contains exactly one call to the substitutable-subject helper.
    - The guard contains a negative-control test that asserts a non-null named message from `rulesetShapeIssue` for an object with the wrong target.
    - `AFTER-RULESET-ACTIVE` and `AFTER-SC1-REJECT-ACCEPT` are captured, each with a fenced producing command and its output; the latter contains the rejection text for `v9.9` and the success output for `v9.9.9-rulesettest`.
    - The rule-suite JSON with its rule evaluations array is embedded in the ledger, not just an id.
    - `.github/workflows/ci.yml` is byte-identical to its state before this task.
  </acceptance_criteria>
  <done>A non-SemVer `v*` tag is rejected by GitHub, the rejection is citable by id, the live object is mirrored in git, and a falsifiable repo-side guard asserts that mirror — one path, working end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Answer the delete-deadlock question empirically and clear the scratch tags</name>
  <precondition>Both scratch tags from task 1 exist in the states task 1 observed: `v9.9.9-rulesettest` present on origin, `v9.9` absent from origin.</precondition>
  <read_first>
    - .planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md (D-06 — the delete probe and the enforcement-flip fallback)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md (Architecture Pattern 2 consequence 1; Pitfall 5 on why only active and disabled exist here)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
  </read_first>
  <action>
GitHub does not document whether a tag ruleset is evaluated on a delete push. If it is, this phase
deadlocks on exactly the tags it must remove, because those are precisely the names that fail the
guard. Answer it now, at zero extra cost, using a tag that already exists.

Delete `v9.9.9-rulesettest` from origin over git. That tag is inside the ruleset's include scope,
so the attempt is the probe. Record `## AFTER-DELETE-PROBE` with the fenced command and its full
output, and a one-line verdict naming whether ref deletion is governed by this ruleset.

- If the delete succeeded: record that deletion is unaffected, and that 238-05 therefore runs with
  enforcement left active — guard-first in the literal sense the phase title reads.
- If the delete was rejected: record the rejection text verbatim, then flip the ruleset's
  enforcement to disabled, repeat the delete, and flip enforcement back to active. Record the flip
  window with start time, end time and the reason. The guard object still landed before any
  deletion, which is what REL-01 requires. Note for 238-05 that its deletion passes must open and
  close the same window. Only active and disabled exist on this repo's plan; the evaluate
  enforcement status is enterprise-gated, so do not reach for it.

Under Tier 2 the answer is structurally known in advance — deletion is governed by a separate rule
type that this ruleset does not contain — but record the observation anyway rather than the
inference, and say which one you are recording.

Then remove both scratch tags from the local repository and confirm neither name survives locally
or on origin. Do not suppress errors on these commands; a swallowed error here would leave a
scratch tag in a namespace this phase exists to clean.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; git fetch --prune --prune-tags origin >/dev/null 2>&1 || true; git ls-remote --tags origin > /tmp/238-remote-tags.txt; test -s /tmp/238-remote-tags.txt || { echo "remote tag listing empty — the parse broke, this is not a pass" >&2; exit 1; }; ! grep -qE "refs/tags/v9\.9($|\^|[[:space:]])" /tmp/238-remote-tags.txt; ! grep -q "rulesettest" /tmp/238-remote-tags.txt; git tag > /tmp/238-local-tags.txt; test -s /tmp/238-local-tags.txt; ! grep -qxE "v9\.9" /tmp/238-local-tags.txt; ! grep -q "rulesettest" /tmp/238-local-tags.txt'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass" — a surviving scratch tag on either side, or an empty listing that would make the absence checks vacuous</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; sed -n "/^## AFTER-DELETE-PROBE$/,/^## /p" "$f" > /tmp/238-dp.txt; test -s /tmp/238-dp.txt || { echo "delete-probe slot empty — the parse broke, this is not a pass" >&2; exit 1; }; grep -qE "^Status: captured" /tmp/238-dp.txt; grep -q "```" /tmp/238-dp.txt; grep -qiE "deletion (is|was) (unaffected|blocked|rejected|governed|not governed)" /tmp/238-dp.txt || { echo "no verdict line on ref deletion" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke" or "no verdict line on ref deletion"</fails_when>
    <automated>bash -c 'set -eo pipefail; id=$(gh api repos/szTheory/sigra/rulesets --jq "[.[]|select(.name==\"tag-namespace\")][0].id"); gh api "repos/szTheory/sigra/rulesets/$id" --jq ".enforcement" | grep -qx active'</automated>
    <fails_when>non-zero exit, or the printed enforcement value is anything other than active — an enforcement flip window was left open</fails_when>
  </verify>
  <acceptance_criteria>
    - `AFTER-DELETE-PROBE` is captured with a fenced command, its full output, and an explicit verdict on whether ref deletion is governed by this ruleset.
    - If an enforcement flip was needed, the slot records the window's start, end and reason, and the live enforcement value is active again at task end.
    - Neither `v9.9` nor `v9.9.9-rulesettest` appears in `git tag` or in `git ls-remote --tags origin`.
    - The remote and local tag listings used for the absence checks are non-empty, so the absence assertions are not vacuous.
    - The slot states whether the recorded verdict is an observation or an inference, and it is an observation.
  </acceptance_criteria>
  <done>The phase knows, by observation rather than assumption, whether 238-05 can delete tags with the guard left active — and the `v*` namespace carries no scratch residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any pusher (owner, PAT, Actions token) → `refs/tags/*` on origin | Untrusted ref names cross here; the ruleset is the validator |
| GitHub Settings → the git repo | The ruleset is Settings-managed with no git-side record except the committed snapshot |
| pull_request CI lane → external network | The guard must not cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-06 | Spoofing | the `v*` release namespace | high | mitigate | Server-side ruleset with an empty bypass list, so a planning tag cannot masquerade as a release version; proven by the observed rejection of `v9.9` |
| T-238-07 | Elevation of Privilege | ruleset bypass actors | high | mitigate | The bypass list stays empty; the guard deliberately does not assert it (the field is invisible at CI token level and the assertion would be vacuous), so the assertion moves to the operator check documented in 238-06 |
| T-238-08 | Denial of Service | release-please's own tag creation | high | mitigate | Tier 2 excludes three-segment names from scope; under Tier 1 the accept-probe is the detector and a rejected accept-probe stops the task rather than recording a partial pass |
| T-238-09 | Tampering | silent ruleset removal in Settings | high | mitigate | The committed snapshot is the reference value; the live drift read lands in 238-03 |
| T-238-10 | Denial of Service | the ruleset blocking the deletions this phase owes | medium | mitigate | D-06's empirical delete probe in task 2, with a recorded enforcement-flip window as the fallback |
| T-238-11 | Denial of Service | `gh`, a token or a transient 5xx on the pull_request critical path | medium | mitigate | The guard is offline and structural; it reads a committed file and makes no network call |
| T-238-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages; no dependency manifest changes |
</threat_model>

<verification>
- The live ruleset exists with the tag target, an active enforcement value, no deletion rule, and an empty bypass list.
- `node --test --test-reporter=tap scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs` is green.
- The committed snapshot field-scoped-diffs clean against the live by-id payload.
- Four slots are captured: the ruleset creation, the reject/accept pair, the embedded rule-suite JSON, and the delete probe.
- No scratch tag survives locally or on origin, and `.github/workflows/ci.yml` is unchanged.
</verification>

<success_criteria>
- REL-01's server-side half is demonstrated live, not asserted: one rejection, one acceptance, both recorded with output.
- The rejection is citable after the rule-suite query window expires, because the JSON is embedded.
- A falsifiable repo-side guard exists and is green, and it was born in a commit that also carries its negative control.
- The delete-deadlock question is answered by observation, unblocking 238-05's sequencing.
</success_criteria>

<output>
Create `.planning/phases/238-tag-guard-then-tag-deletion/238-02-SUMMARY.md` when done. It must state
the delete-probe verdict explicitly, because 238-05 branches on whether an enforcement flip window
is required.
</output>

