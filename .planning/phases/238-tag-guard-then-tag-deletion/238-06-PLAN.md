---
phase: 238-tag-guard-then-tag-deletion
plan: 06
type: execute
wave: 5
depends_on: ["238-05"]
files_modified:
  - MAINTAINING.md
  - .planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
autonomous: true
requirements: [REL-01, REL-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The maintainer runbook documents how to read the live tag ruleset, including the operator-side check of the bypass actor list that the CI guard deliberately cannot make."
    - "The maintainer runbook documents the tag-deletion procedure by pointing at the committed script and the committed allowlist rather than restating their contents."
    - "ADR 003's guardrail 3 is corrected, not merely extended: it currently states milestone tagging stopped at a version that two later tags contradict, and both of those tags were minted after the ADR's own date."
    - "The ADR amendment carries the deletion date, the path to the committed allowlist, and the prescribed namespaces for future non-release tags."
    - "The ADR amendment states plainly what the guard does not cover, so a future reader does not over-read its reach."
    - "If the landed tier is not the literal shape REL-01 names, a recorded supersession appears in the requirements file — never a quiet re-scope."
    - "The supersession obligation triggers on the rule type that actually landed, read from the committed ruleset snapshot, not on REL-01's wording alone: a landed `creation` rule obliges a dated, explicit supersession whatever any requirement happens to say."
    - "A landed `creation` rule has its departure from CONTEXT D-04 — which forbids a `creation` rule absolutely — and from ROADMAP SC-1's named mechanism recorded as a dated supersession, in both the requirements file and the ADR amendment, within this phase's commit range."
    - "Every evidence slot is captured at the phase's final committed head on a clean tree, and none remains pending."
  artifacts:
    - MAINTAINING.md
    - .planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
  key_links:
    - "the ADR cites the allowlist by a path that survives milestone close, because phase directories move and the ADR outlives the phase"
    - "the runbook cites the script and the allowlist; the allowlist remains the single oracle for the delete set"
  prohibitions:
    - "MUST NOT restate the delete set in prose; the committed allowlist is the single source of truth and prose renders it."
    - "MUST NOT leave ADR 003's incorrect claim about when milestone tagging stopped standing alongside the amendment."
    - "MUST NOT change ADR 003's accepted status or create a superseding ADR; the amendment is in place and dated."
    - "MUST NOT re-scope REL-01 silently if the landed tier is not its literal shape."
    - "MUST NOT leave a landed `creation` rule without a dated, explicit supersession naming D-04 and ROADMAP SC-1; the decision to keep the rule is defensible, keeping it quietly is not."
    - "MUST NOT claim the observer-lane drift read has been proven working; it only executes from the default branch."
    - "MUST NOT claim the guard covers tag namespaces outside the scope the live conditions actually select."
---

<objective>
Make the phase legible to whoever reads the repository next: an operator runbook for both the live
guard and the deletion procedure, an ADR amendment that corrects a false claim rather than layering
on top of it, an honest requirements supersession if the landed guard is not REL-01's literal
shape, and an evidence ledger closed at the final head.

Purpose: SC-5 requires ADR 003 to carry the deletion date, the delete-list path and the prescribed
namespaces. D-02 requires any shortfall against REL-01's wording to be a recorded supersession.
Output: two new runbook subsections, a dated ADR amendment, a possibly-amended requirements entry,
and a fully captured ledger.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-02-SUMMARY.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-05-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by Phase 238 (new — exclude from drift / source-grounding verification):

- `.github/rulesets/tag-namespace.json` (new file; new `.github/rulesets/` directory)
- `scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs` (new file)
- `test/fixtures/prohibitions/p19-tag-ruleset-absent-or-altered.json` (new file)
- `scripts/maintainers/delete-planning-tags.sh` (new file)
- `.planning/decisions/003-tag-delete-list.tsv` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/COVERAGE.md` (new file)
- GitHub ruleset named `tag-namespace` (live GitHub configuration, not a repo file)
- CI job id `tag_ruleset_drift` in `.github/workflows/ci-observe.yml` (new job)
- Shell function names `fail` and `usage`, and the `--apply` and `--allowlist` flags, in `delete-planning-tags.sh`
- JS function name `rulesetShapeIssue` in the `p19` guard
- ADR 003 section heading `## Amendment — 2026-09-16 (Phase 238)`
- MAINTAINING.md headings for the live tag ruleset and the planning-tag deletion runbook
- Evidence slot names: `BEFORE-RULESET-STATE`, `BEFORE-TAG-INVENTORY`,
  `BEFORE-RULESET-FEASIBILITY-PROBE`, `AFTER-RULESET-ACTIVE`, `AFTER-SC1-REJECT-ACCEPT`,
  `AFTER-DELETE-PROBE`, `AFTER-P19-RED`, `AFTER-LOCAL-DELETE`, `AFTER-REMOTE-DELETE`,
  `AFTER-RELEASE-SURFACE`

<tasks>

<task type="auto">
  <name>Task 1: Two maintainer runbook subsections</name>
  <read_first>
    - MAINTAINING.md (lines 90-135: the existing live-ruleset subsection, which is the exact shape both new subsections mirror — heading naming the surface, a paragraph stating the enforced value, a bolded do-not-break warning, and a single-line operator command)
    - .github/rulesets/tag-namespace.json (the committed reference value the live check compares against)
    - scripts/maintainers/delete-planning-tags.sh (the passes and flags the runbook points at)
    - .planning/decisions/003-tag-delete-list.tsv (the allowlist the runbook cites rather than restates)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md (D-11, D-13)
  </read_first>
  <action>
Add two subsections to MAINTAINING.md, both following the existing live-ruleset subsection's shape
and placed as its siblings.

The first documents the live tag ruleset: the ruleset's name, that it is Settings-managed with the
committed snapshot as the repo's only record, and a single-line operator command that reads the
live ruleset and projects the fields worth eyeballing. That command selects the ruleset by the name
`tag-namespace` rather than by a numeric id — the id is minted at creation time and is not a
constant this document can carry, and selecting by name is also what distinguishes this command
from the one already in the file, which reads the branch ruleset `14941512` by id.

Include the operator-side bypass check that
the CI guard deliberately omits — GitHub returns the bypass actor list only to callers with write
access to the ruleset, so an operator running as the owner is the only one who can meaningfully
assert it is empty, and that emptiness is load-bearing because it is what binds the owner.
Include the bolded do-not-break warning in the style the neighbouring subsection uses.

The second documents the planning-tag deletion procedure. It points at
`scripts/maintainers/delete-planning-tags.sh` and `.planning/decisions/003-tag-delete-list.tsv` and
names the four separate invocations in order, without restating the delete set — the allowlist is
the single oracle and prose renders it. State that reporting is the default and mutation requires
the explicit flag, and that the script is an operator tool never wired into a CI lane.

Also record, in the live-ruleset subsection, the honest caveat about the observer-lane drift job: a
workflow-run lane only ever executes the default-branch copy of its file, so that job begins
reporting only after it merges, and it was not proven working during the phase that added it.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; f=MAINTAINING.md; grep -q "tag-namespace" "$f" || { echo "the live tag ruleset subsection does not name the ruleset" >&2; exit 1; }; grep -q "scripts/maintainers/delete-planning-tags.sh" "$f" || { echo "the runbook does not point at the deletion script" >&2; exit 1; }; grep -q ".planning/decisions/003-tag-delete-list.tsv" "$f" || { echo "the runbook does not cite the allowlist" >&2; exit 1; }; grep -E "gh api repos/szTheory/sigra/rulesets" "$f" | grep -q -- "tag-namespace" || { echo "no operator command for reading the live tag ruleset — the only ruleset read command in the file is the pre-existing one for the branch ruleset id" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "does not name the ruleset", "does not point at the deletion script", "does not cite the allowlist", or "no operator command for reading the live tag ruleset"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=MAINTAINING.md; tags=$(grep -cE "^v[0-9]+\.[0-9]+[[:space:]]*$" "$f" || true); test "$tags" -eq 0 || { echo "$tags delete-set tag names are restated in prose; the allowlist is the single source of truth" >&2; exit 1; }; grep -qi "default branch" "$f" || { echo "the observer-lane default-branch caveat is not recorded" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "restated in prose" or "default-branch caveat is not recorded"</fails_when>
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md contains a subsection naming the live tag ruleset, carrying a single-line operator read command and a bolded do-not-break warning.
    - That read command is distinguishable from the ruleset read command already in the file at HEAD: the pre-existing one addresses the branch ruleset by its numeric id, so the new one is asserted by requiring a line that both invokes the rulesets endpoint and selects the entry by the name `tag-namespace`. The assertion therefore fails before this task writes the subsection.
    - That subsection documents the operator-side bypass-list check and states why the CI guard cannot make it.
    - That subsection records the observer-lane default-branch caveat.
    - MAINTAINING.md contains a deletion runbook subsection citing both the script and the allowlist by path, naming the four invocations in order, and stating that reporting is the default and that the script is not wired into CI.
    - The delete set itself is not enumerated in prose.
  </acceptance_criteria>
  <done>An operator can verify the guard and rerun the procedure from the runbook alone, without reconstructing anything.</done>
</task>

<task type="auto">
  <name>Task 2: Amend ADR 003 — correct the false claim, record the deletion, prescribe the namespaces</name>
  <read_first>
    - .planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md (the whole file: its structure, its accepted status line, and guardrail 3 at line 36 — the clause being corrected)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md (Measured Starting State, "The recurrence is live — ADR 003 contains a factual error": the two tags minted after the ADR's date and their commit dates)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-05-SUMMARY.md (the deletion date)
    - .planning/decisions/003-tag-delete-list.tsv (the path the ADR must cite)
    - .github/rulesets/tag-namespace.json (the live conditions, which bound what the amendment may claim the guard covers)
  </read_first>
  <action>
Amend ADR 003 in place with a dated section headed `## Amendment — 2026-09-16 (Phase 238)`. Keep the
document's accepted status line unchanged; there is no superseding-ADR precedent in this directory
and this amendment establishes the in-place convention instead.

Correct guardrail 3 rather than merely extending it. It currently asserts that milestone tagging was
stopped after a particular version. Two milestone tags were minted after this ADR's own date —
`v1.47` on 2026-08-04, local only, and `v1.48` on 2026-08-12, which reached origin. Name both, with
their dates, and state the actual lesson: the guardrail was prose with no enforcement, which is
precisely what this phase's server-side guard replaces. An ADR that states a false fact about the
recurrence it exists to prevent is worse than no ADR.

The amendment records:

- The deletion date from 238-05, and the path `.planning/decisions/003-tag-delete-list.tsv` as the
  committed record of exactly which tags were removed. Cite the path; do not restate the set.
- The prescribed namespaces for future non-release tags: `milestone/` for milestone markers and
  `proof/` for phase proof tags. Guardrail 3 already gestures at the first of these, so this is an
  extension of existing ADR text rather than an invention. Note that the proof namespace exists
  because this phase deleted eleven tags from a differently-scoped earlier phase whose number
  collided with this one.
- What the guard does and does not cover, stated plainly and bounded by the live conditions. Its
  scope selector is fnmatch with path-segment semantics, so it governs only names under the `v`
  prefix in the tags namespace and leaves the archive, milestone and proof namespaces untouched.
  It therefore does not prevent a future phase-proof-style junk tag; it guarantees only that the
  release namespace means one thing. If the landed tier is the coarser shape, also state that it is
  a shape guard rather than a full version validator, and name the classes it lets through.
- The relationship to guardrail 2: guardrail 2 forbids a tag-triggered publish path. Nothing in
  this phase adds one. Say so, so a future reader does not misread the guard as a regression.

- **The decision departure, if a `creation` rule landed.** Read the rule type from the committed
  snapshot, not from a plan's expectation. If the landed ruleset's only rule is `creation`, the
  amendment carries a dated subsection recording the departure from CONTEXT D-04, whose text is
  absolute: the ruleset contains no `creation` rule and no `deletion` rule, ever. Record what was
  built instead, why D-04's objection dissolves under the shape that landed (the exclude list
  subtracts three-segment release names from the ruleset's scope, so the release automation is not
  blocked — which was D-02's entire reason for D-04's first clause), and what the departure costs
  (the rule constrains creation of names in scope rather than validating a version string, so it
  blocks the two-component recurrence class while admitting four-segment and non-numeric shapes).
  Record alongside it that ROADMAP SC-1 names the mechanism literally as a name-pattern rule and
  that this clause is superseded too, while SC-1's observable outcome still holds — a two-component
  scratch name carries one dot, so the three-dot-segment exclusion does not cover it and its
  creation is still refused. Note explicitly that D-04's second clause is untouched: no `deletion`
  rule was built, and the delete-deadlock D-06 exists to avoid is still avoided. This is the durable
  half of the supersession; task 3 records the requirements-file half, and both land in this
  phase's commit range alongside the rule itself.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; f=.planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md; grep -qE "^## Amendment — 2026-09-16 \(Phase 238\)$" "$f" || { echo "dated amendment heading absent" >&2; exit 1; }; grep -qE "^\*\*Status:\*\* Accepted" "$f" || { echo "the accepted status line was altered" >&2; exit 1; }; sed -n "/^## Amendment — 2026-09-16 (Phase 238)$/,\$p" "$f" > /tmp/238-amend-strings.txt; test -s /tmp/238-amend-strings.txt || { echo "amendment section empty — the parse broke, this is not a pass" >&2; exit 1; }; for s in "003-tag-delete-list.tsv" "milestone/" "proof/" "v1.47" "v1.48"; do grep -q -- "$s" /tmp/238-amend-strings.txt || { echo "amendment does not record: $s" >&2; exit 1; }; done'</automated>
    <fails_when>non-zero exit, or stderr contains "dated amendment heading absent", "the accepted status line was altered", "the parse broke, this is not a pass", or "amendment does not record:"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md; sed -n "/^## Amendment — 2026-09-16 (Phase 238)$/,\$p" "$f" > /tmp/238-amend.txt; test -s /tmp/238-amend.txt || { echo "amendment section empty — the parse broke, this is not a pass" >&2; exit 1; }; w=$(wc -w < /tmp/238-amend.txt); test "$w" -gt 150 || { echo "amendment is $w words; it cannot carry the correction, the date, the path, the namespaces and the coverage statement" >&2; exit 1; }; grep -qiE "does not (cover|prevent|guarantee)" /tmp/238-amend.txt || { echo "the amendment makes no statement about what the guard does not cover" >&2; exit 1; }; grep -qE "[0-9]{4}-[0-9]{2}-[0-9]{2}" /tmp/238-amend.txt'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass", "it cannot carry the correction", or "makes no statement about what the guard does not cover"</fails_when>
    <automated>bash -c 'set -eo pipefail; snap=.github/rulesets/tag-namespace.json; a=.planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md; types=$(jq -r ".rules[].type" "$snap" | sort -u); test -n "$types" || { echo "the snapshot exposes no rule types — the parse broke, this is not a pass" >&2; exit 1; }; if echo "$types" | grep -qx creation; then sed -n "/^## Amendment — 2026-09-16 (Phase 238)$/,\$p" "$a" > /tmp/238-amend.txt; grep -q "D-04" /tmp/238-amend.txt || { echo "a creation rule landed but the amendment does not record the D-04 departure" >&2; exit 1; }; grep -q "SC-1" /tmp/238-amend.txt || { echo "a creation rule landed but the amendment does not record the ROADMAP SC-1 departure" >&2; exit 1; }; grep -qiE "supersed" /tmp/238-amend.txt || { echo "the D-04 departure is recorded but not as a supersession" >&2; exit 1; }; fi; echo "rule types landed: $types"'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass", "does not record the D-04 departure", "does not record the ROADMAP SC-1 departure", or "not as a supersession"</fails_when>
  </verify>
  <acceptance_criteria>
    - ADR 003 carries a section headed exactly `## Amendment — 2026-09-16 (Phase 238)` and its accepted status line is unchanged.
    - The amendment names both post-ADR milestone tags with their dates and states that guardrail 3's claim was wrong because the guardrail was unenforced prose.
    - The amendment cites `.planning/decisions/003-tag-delete-list.tsv` by path and records the deletion date, without enumerating the set.
    - The amendment prescribes the `milestone/` and `proof/` namespaces and explains why the second exists. The namespace assertion is scoped to the amendment section, because guardrail 3 already mentions the first namespace at HEAD and a whole-file grep for it would pass before this task writes anything.
    - The amendment states explicitly what the guard does not cover, bounded by the live scope conditions.
    - The amendment states that nothing in this phase adds a tag-triggered publish path.
    - If the committed snapshot's rules include a `creation` rule, the amendment records a dated supersession of CONTEXT D-04's no-creation-rule clause and of ROADMAP SC-1's named mechanism, stating what was built instead, why the objection dissolves, what the departure costs, and that D-04's no-deletion-rule clause is untouched. The check reads the landed rule type, so it triggers on what shipped.
  </acceptance_criteria>
  <done>SC-5 is satisfied by an ADR that is now factually correct about the very recurrence it governs.</done>
</task>

<task type="auto">
  <name>Task 3: Record any requirements supersession and close the ledger at the final head</name>
  <read_first>
    - .planning/REQUIREMENTS.md (REL-01's current wording, and the existing supersession precedent referenced by D-02)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-02-SUMMARY.md (the landed tier)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md (every slot's current status)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md (D-02 — supersession, never a quiet re-scope)
  </read_first>
  <action>
Compare what landed against REL-01's literal wording. REL-01 names a GitHub tag ruleset that is
server-side, paired with a contract test, demonstrated red, landing before any deletion.

If the landed tier is the name-pattern shape, REL-01 is satisfied verbatim and the requirements file
needs no change beyond marking the requirement complete. Record that conclusion explicitly rather
than leaving it implied.

If the landed tier is the coarser creation-with-exclusion shape, REL-01's "server-side" survives but
its implied granularity does not. Record a supersession in the requirements file following the
existing precedent's form: leave the original text visible, add a dated supersession note stating
what was delivered instead, why, and what it does not cover — server-side prevention at coarser
granularity than a full version pattern, blocking the two-component recurrence class, letting
four-segment and non-numeric shapes through. Do not edit REL-01's original wording in place, and do
not narrow the requirement without the note.

**Trigger on the rule type, not on the wording.** Read the landed rule type from the committed
snapshot `.github/rulesets/tag-namespace.json`, not from a plan's expectation and not only from
REL-01's text. If the landed ruleset's only rule is `creation`, the supersession note is owed
regardless of how any requirement happens to be worded, because two locked source artifacts said
otherwise and both must be named in it:

- **CONTEXT D-04**, whose text is absolute — the ruleset contains no `creation` rule and no
  `deletion` rule, ever. The note records that a `creation` rule is exactly what landed, why D-04's
  stated reason for the first clause dissolves under this shape (the exclude list subtracts
  three-segment release names from the ruleset's scope, so the release automation is not blocked —
  which was the whole basis of the objection), and that D-04's second clause is untouched: no
  `deletion` rule was built, so the delete-deadlock D-06 exists to avoid is still avoided.
- **ROADMAP SC-1**, which names the mechanism literally as a name-pattern rule. Under the landed
  shape that clause is false as written, even though SC-1's observable outcome still holds — a
  two-component scratch name carries one dot, so the three-dot-segment exclusion does not cover it
  and its creation is still refused. Record the mechanism clause as superseded and the outcome as
  satisfied; they are different claims and conflating them is the quiet re-scope D-02 forbids.

Date the note. It lands in this phase's commit range, alongside the rule itself and alongside the
ADR amendment task 2 writes — the durable record and the requirements record are two halves of one
supersession, not alternatives. Keeping the `creation` rule is a defensible decision; keeping it
without this note is not.

Then close the ledger. Every slot must be captured; none may remain pending. Update the summary
table's status column to match the slot bodies, and set the observed-at-commit line to the phase's
final committed head. Because that head is not known until the last commit is made, capture the
observation, commit, then amend the line to the resulting head and state in the SUMMARY which
commit the ledger is pinned to — the milestone's standing constraint requires the observations to
be captured at the final committed head on a clean tree, and a ledger pinned to an earlier head
does not satisfy it.

Finally, run the full prohibition suite and the repository's standing quality alias so the phase's
commits go out on a green gate.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs</automated>
    <fails_when>non-zero exit, or the TAP output contains "not ok"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; total=$(grep -cE "^## (BEFORE|AFTER)-[A-Z0-9-]+$" "$f"); test "$total" -ge 10 || { echo "only $total slots — the parse broke, this is not a pass" >&2; exit 1; }; cap=$(grep -cE "^Status: captured" "$f"); test "$cap" -eq "$total" || { echo "$cap of $total slots are captured; the remainder are still pending" >&2; exit 1; }; grep -qE "^Observed at commit: [0-9a-f]{7,40}" "$f" || { echo "no observed-at-commit line pinning the ledger" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass", "slots are captured; the remainder are still pending", or "no observed-at-commit line"</fails_when>
    <automated>bash -c 'set -eo pipefail; tier=$(grep -oE "Tier [123]" .planning/phases/238-tag-guard-then-tag-deletion/238-02-SUMMARY.md | head -1); test -n "$tier" || { echo "the landed tier is not recorded in the 238-02 summary — the parse broke, this is not a pass" >&2; exit 1; }; if [ "$tier" != "Tier 1" ]; then grep -qiE "supersed" .planning/REQUIREMENTS.md || { echo "landed $tier but no supersession note appears in the requirements file" >&2; exit 1; }; fi; grep -q "REL-01" .planning/REQUIREMENTS.md'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass" or "no supersession note appears in the requirements file"</fails_when>
    <automated>bash -c 'set -eo pipefail; snap=.github/rulesets/tag-namespace.json; r=.planning/REQUIREMENTS.md; types=$(jq -r ".rules[].type" "$snap" | sort -u); test -n "$types" || { echo "the snapshot exposes no rule types — the parse broke, this is not a pass" >&2; exit 1; }; if echo "$types" | grep -qx creation; then grep -qiE "supersed" "$r" || { echo "a creation rule landed but the requirements file carries no supersession note" >&2; exit 1; }; grep -q "D-04" "$r" || { echo "a creation rule landed but the supersession does not name D-04" >&2; exit 1; }; grep -q "SC-1" "$r" || { echo "a creation rule landed but the supersession does not name ROADMAP SC-1" >&2; exit 1; }; grep -qE "[0-9]{4}-[0-9]{2}-[0-9]{2}" "$r" || { echo "the supersession note carries no date" >&2; exit 1; }; fi; echo "rule types landed: $types"'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass", "carries no supersession note", "does not name D-04", "does not name ROADMAP SC-1", or "carries no date"</fails_when>
    <automated>bash -c 'set -eo pipefail; test -z "$(git status --porcelain)" || { echo "working tree is dirty; the ledger head pin cannot be honest" >&2; exit 1; }; h=$(git rev-parse HEAD); grep -qE "^Observed at commit: ${h:0:7}" .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md || { echo "the ledger is pinned to a head other than the current one" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "working tree is dirty" or "pinned to a head other than the current one"</fails_when>
  </verify>
  <acceptance_criteria>
    - Every slot in the ledger is captured; the summary table's status column matches the slot bodies.
    - The observed-at-commit line resolves to the phase's final committed head, and the working tree is clean.
    - If the landed tier is not the name-pattern shape, a dated supersession note appears in the requirements file with REL-01's original wording left visible, naming what was delivered, why, and what it does not cover.
    - If the landed tier is the name-pattern shape, the SUMMARY records explicitly that REL-01 was satisfied verbatim and no supersession was needed.
    - If the committed snapshot's rules include a `creation` rule, the requirements file carries a dated supersession note that names both CONTEXT D-04 and ROADMAP SC-1 in addition to REL-01, distinguishing SC-1's superseded mechanism clause from its still-satisfied observable outcome. The check reads the landed rule type from the snapshot, so the obligation triggers on what shipped rather than on any requirement's wording.
    - The ADR amendment and the requirements note both land in this phase's commit range, so the supersession travels with the change rather than trailing it.
    - The full prohibition suite is green.
    - The repository's standing quality alias is green before the phase's commits are pushed.
  </acceptance_criteria>
  <done>The phase's claims match what it actually delivered, at a head that is clean and pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase artifacts → future readers | Documentation is where an over-claim survives longest and is hardest to detect |
| requirements file → milestone acceptance | A silently re-scoped requirement passes a gate it should not |
| ADR → future maintainers | An ADR carrying a false fact about a recurrence actively misleads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-30 | Repudiation | a requirement quietly narrowed to match what shipped | high | mitigate | A dated supersession note with the original wording left visible, gated by a verify that reads the landed tier and demands the note when the tier is not the literal shape |
| T-238-31 | Spoofing | an over-claim about the guard's coverage | high | mitigate | The amendment must state what the guard does not cover, asserted by the verify; the coverage claim is bounded by the live scope conditions |
| T-238-32 | Repudiation | an evidence ledger pinned to a head that predates the final commit | high | mitigate | The ledger's observed-at-commit line is asserted against the current head on a clean tree |
| T-238-33 | Tampering | the delete set restated in prose and drifting from the committed data | medium | mitigate | The runbook and the ADR cite the allowlist by path; a verify rejects enumerated delete-set names in the runbook |
| T-238-34 | Spoofing | claiming the observer-lane drift read is proven | medium | mitigate | The default-branch caveat is recorded in the runbook and restated in the SUMMARY |
| T-238-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages; no dependency manifest changes |
</threat_model>

<verification>
- MAINTAINING.md carries both subsections, citing the script, the allowlist and a live read command, without enumerating the delete set.
- ADR 003 carries the dated amendment with the correction, the deletion date, the allowlist path, the prescribed namespaces, and an explicit non-coverage statement.
- The requirements file reflects reality: either REL-01 satisfied verbatim and recorded as such, or a dated supersession note.
- If a `creation` rule landed, both the ADR amendment and the requirements note record the D-04 and ROADMAP SC-1 departures as a dated supersession, triggered by the landed rule type read from the committed snapshot.
- Every ledger slot is captured and the ledger is pinned to the final committed head on a clean tree.
- The full prohibition suite and the repository's standing quality alias are green.
</verification>

<success_criteria>
- SC-5 holds: ADR 003 records the deletion date, the delete-list path and the prescribed namespaces, and its guardrail 3 is now factually correct.
- Nothing in the phase's artifacts claims more than what was observed.
- An operator can verify and rerun everything this phase built from the runbook alone.
</success_criteria>

<output>
Create `.planning/phases/238-tag-guard-then-tag-deletion/238-06-SUMMARY.md` when done, stating the
landed tier, which rule type actually landed, whether a supersession was recorded and which source
artifacts it names (REL-01, CONTEXT D-04, ROADMAP SC-1), and the commit the evidence ledger is
pinned to.
</output>

