---
phase: 238-tag-guard-then-tag-deletion
plan: 05
type: execute
wave: 4
depends_on: ["238-03", "238-04"]
files_modified:
  - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
autonomous: false
requirements: [REL-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The guard landed before any deletion: the live ruleset, the committed snapshot and the red-proven contract test are all in git and in Settings before the first delete runs."
    - "The local pass, its verification, the remote pass and its verification run as four separate invocations, in that order."
    - "After the local pass, the local tag listing is set-equal to the local keep-set derived from the release expression union the archive prefix."
    - "After the remote pass, the remote tag listing is set-equal to the remote keep-set derived from the release expression alone."
    - "The release expression admits an optional prerelease or build suffix on the three numeric components, so a release-candidate tag minted by the release automation mid-window is a keep-set member rather than a drift failure."
    - "The release count is identical before and after, and the draft-release count is zero, both read from the REST releases route rather than from a field name that does not exist on the list command."
    - "A published documentation source link still resolves after deletion, proving no tag backing a release or the documentation source reference was touched."
    - "Each pass's command and full output is recorded as its own captured evidence slot, at the final committed head on a clean tree."
    - statement: "No keep-set cardinality appears in any assertion; both sides of each comparison are derived from the same expression at compare time."
      verification: backstop
    - statement: "A release tag created by the release automation during the deletion window is never passed to a delete invocation, because it is not an allowlist row, and it satisfies the keep expression so the post-deletion comparison still holds — including the prerelease shape D-05 explicitly contemplates, because the keep expression admits an optional prerelease or build suffix."
      verification: backstop
  artifacts:
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
  key_links:
    - "the committed allowlist drives every delete invocation; nothing else selects a ref"
    - "the pre-deletion SHA column is the forward-feed Phase 245 consumes before pruning the branch that holds the phase-proof tags"
  prohibitions:
    - "MUST NOT run any pass without the explicit apply flag being deliberately supplied by the operator-authorized executor."
    - "MUST NOT chain a mutation pass and its verification into one command."
    - "MUST NOT delete a remote ref for a row not flagged as present on the remote."
    - "MUST NOT suppress the exit status of any delete invocation."
    - "MUST NOT run garbage collection, reflog expiry, or a prune-now at any point."
    - "MUST NOT use the list command's non-existent draft field; the draft count comes from the REST releases route."
    - "MUST NOT proceed if the pre-deletion release count cannot be read, because the after-count would then have nothing to compare against."
---

<objective>
Execute the deletion — the one-way act this phase has been building toward — behind an explicit
operator gate, in four separate passes, with each pass's outcome recorded before the next begins.

Purpose: REL-02 and SC-3/SC-4 require the planning and proof tags gone from both sides, the keep-set
asserted by set equality rather than by count, and the release surface demonstrably untouched.
Output: the deleted tags, four captured evidence slots, and a proven-unchanged release surface.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-02-SUMMARY.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-04-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by Phase 238 (new — exclude from drift / source-grounding verification):

- `.github/rulesets/tag-namespace.json` (new file; new `.github/rulesets/` directory)
- `scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs` (new file)
- `test/fixtures/prohibitions/p19-tag-ruleset-absent-or-altered.json` (new file)
- `scripts/maintainers/delete-planning-tags.sh` (new file)
- `.planning/decisions/003-tag-delete-list.tsv` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/COVERAGE.md` (new file)
- GitHub ruleset named `tag-namespace` (live GitHub configuration, not a repo file)
- CI job id `tag_ruleset_drift` in `.github/workflows/ci-observe.yml` (new job)
- Shell function names `fail` and `usage`, and the `--apply` and `--allowlist` flags, in `delete-planning-tags.sh`
- Allowlist column names `tag`, `local`, `remote`, `class`, `pre_delete_sha`, `reason`
- JS function name `rulesetShapeIssue` in the `p19` guard
- Evidence slot names: `BEFORE-RULESET-STATE`, `BEFORE-TAG-INVENTORY`,
  `BEFORE-RULESET-FEASIBILITY-PROBE`, `AFTER-RULESET-ACTIVE`, `AFTER-SC1-REJECT-ACCEPT`,
  `AFTER-DELETE-PROBE`, `AFTER-P19-RED`, `AFTER-LOCAL-DELETE`, `AFTER-REMOTE-DELETE`,
  `AFTER-RELEASE-SURFACE`

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Authorize the one-way deletion of remote tags</name>
  <reversibility rating="one-way">Deleting a ref from origin removes the remote's only pointer to those objects. The pre-deletion SHA column makes recovery possible while the objects remain reachable and uncollected, but the remote refs themselves cannot be restored without re-pushing, and any third party who had fetched them loses the upstream reference. Treat it as a door that does not reopen.</reversibility>
  <read_first>
    - .planning/decisions/003-tag-delete-list.tsv (the exact set about to be destroyed — read it, do not summarise it from memory)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-04-SUMMARY.md (the scratch-clone observations proving the script's edge behaviors)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-02-SUMMARY.md (the delete-probe verdict: whether an enforcement flip window is required)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md (D-13, D-15)
  </read_first>
  <action>
Halt and present to the operator, before the first apply invocation:

(a) The exact set. Print the allowlist's row count, the per-class breakdown, and the subset flagged
present on the remote. State that the remote pass acts only on that subset because a remote delete
of a local-only tag errors.

(b) What is being kept, and how that is asserted. The three-component release tags and the archive
namespace survive on the local side; the three-component release tags survive on the remote side.
The assertion is a set comparison against an expression-derived keep-set on each side, not a count.

(c) Reversibility. Local deletion is recoverable from the recorded pre-deletion SHA while the
objects remain reachable. Remote deletion is the one-way half. This phase runs no garbage
collection, no reflog expiry and no prune-now, and Phase 245 carries the same constraint, so the
objects stay resolvable in the interim — but the remote refs do not come back on their own.

(d) The downstream coupling. The phase-proof rows are held by a branch Phase 245 lists among its
prune candidates. Deleting these tags is safe now and becomes lossy only if 245 later removes that
branch without consuming the recorded SHAs. Name that explicitly so the operator can weigh it.

(e) Whether an enforcement flip window is required, per the delete-probe verdict, and if so that
the window will be opened and closed around the remote pass and recorded.

Ask the operator to authorize the deletion, or to stop. If the operator declines, record the
decline and halt the plan — the guard has already landed and REL-01 stands on its own.
  </action>
  <verify>
    <human-check>Operator has explicitly authorized deleting the allowlisted tags from origin, having seen the exact set, the keep-set assertion method, and the Phase 245 coupling — or has explicitly declined and the plan is halted.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator's answer is recorded in the plan SUMMARY as an explicit authorize or decline, with the date and the row count presented.
    - No apply invocation of the deletion script has run before this checkpoint resolves.
    - The presentation cited the allowlist file's live contents, not a count copied from a planning document.
    - If declined: no further task runs and the SUMMARY states REL-02 is blocked on operator authorization.
  </acceptance_criteria>
  <done>The one-way act is authorized in writing, by an operator who saw the exact set.</done>
</task>

<task type="auto">
  <name>Task 2: Local pass, then its independent verification</name>
  <precondition>The live ruleset is active, `.github/rulesets/tag-namespace.json` and the red-proven guard are committed, and the working tree is clean — REL-01's "lands before any deletion" is a checkable state, not an intention.</precondition>
  <read_first>
    - scripts/maintainers/delete-planning-tags.sh (the passes and their flags, as committed)
    - .planning/decisions/003-tag-delete-list.tsv
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
    - .planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md (Code Examples "Per-side set-equality"; the positive-control requirement)
  </read_first>
  <action>
Assert the precondition first and record it: the live ruleset is present and active, the snapshot
and guard are committed, and the working tree is clean. If any of those is false, stop — REL-01
requires the guard to land before any deletion, and that is verifiable rather than assumed.

Run the local pass with the explicit apply flag, as its own invocation. Then, as a separate
invocation, run the local verification. The separation is the point: a verification that runs in
the same process as the mutation it checks is not an independent observation.

Record `## AFTER-LOCAL-DELETE` with the fenced apply command and its full per-row output, then the
fenced verification command and its full output, including the positive control's report that the
listing and the keep-set are both non-empty. Mark the slot captured.

The keep expression this pass and its verification use admits an optional prerelease or build
suffix on the three numeric components. That is not cosmetic: D-05 records that the release
automation may mint a release-candidate tag, and a bare three-component expression would classify
such a tag as drift and fail this pass for a benign reason. No allowlist row can match the widened
expression, so the delete set is unchanged by it.

Confirm, and record, that the release tags and the archive-namespace tag are all
still present locally and still resolve to the same objects they resolved to in the before
inventory. That is the assertion SC-3 actually cares about; the absence of the deleted names is the
easy half.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; git tag | sort -u > /tmp/238-local-after.txt; test -s /tmp/238-local-after.txt || { echo "local tag listing empty — the parse broke, this is not a pass" >&2; exit 1; }; grep -E "^(v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?|archive/.*)$" /tmp/238-local-after.txt | sort -u > /tmp/238-local-keep.txt; test -s /tmp/238-local-keep.txt || { echo "local keep-set empty — the parse broke, this is not a pass" >&2; exit 1; }; diff /tmp/238-local-after.txt /tmp/238-local-keep.txt || { echo "LOCAL DRIFT: a tag survives that is neither a release tag (three numeric components with an optional prerelease or build suffix) nor in the archive namespace" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass" or "LOCAL DRIFT"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/decisions/003-tag-delete-list.tsv; cut -f1,2 "$f" | grep -v "^#" | tr "\t" " " > /tmp/238-del-rows.txt; seen=0; checked=0; miss=0; while read -r tag loc; do case "$tag" in ""|"tag") continue;; esac; seen=$((seen+1)); if [ "$loc" = yes ]; then checked=$((checked+1)); if git rev-parse -q --verify "refs/tags/$tag" >/dev/null; then echo "allowlisted tag $tag still present locally" >&2; miss=1; fi; fi; done < /tmp/238-del-rows.txt; test "$seen" -gt 10 || { echo "the row loop visited $seen rows — the loop body never ran on real data, this is not a pass" >&2; exit 1; }; test "$checked" -gt 10 || { echo "only $checked local-flagged rows reached the absence check — the guarded branch is vacuous, this is not a pass" >&2; exit 1; }; test "$miss" -eq 0'</automated>
    <fails_when>non-zero exit, or stderr contains "still present locally", "the loop body never ran on real data", or "the guarded branch is vacuous"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; sed -n "/^## AFTER-LOCAL-DELETE$/,/^## /p" "$f" > /tmp/238-ld.txt; test -s /tmp/238-ld.txt || { echo "local-delete slot empty — the parse broke, this is not a pass" >&2; exit 1; }; grep -qE "^Status: captured" /tmp/238-ld.txt; n=$(grep -c "```" /tmp/238-ld.txt); test "$n" -ge 4 || { echo "slot carries $n fence markers; the apply and verify commands must both appear with their output" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass" or "fence markers"</fails_when>
  </verify>
  <acceptance_criteria>
    - The local tag listing is set-equal to the keep-set derived from the release expression union the archive prefix, compared as sorted sets, where the release expression admits an optional prerelease or build suffix.
    - No tag flagged present locally in the allowlist resolves locally any more.
    - Every three-component release tag present in the before inventory is still present and still resolves to the same object.
    - `AFTER-LOCAL-DELETE` is captured and carries both the apply invocation and the separate verification invocation, each with its output.
    - The positive control's non-empty listing and non-empty keep-set report appears in the recorded output.
    - The precondition assertion — ruleset active, snapshot and guard committed, tree clean — is recorded before the apply output.
  </acceptance_criteria>
  <done>The local side matches the keep-set exactly, and the passes that got it there are on the record as separate observations.</done>
</task>

<task type="auto">
  <name>Task 3: Remote pass, its verification, and the untouched release surface</name>
  <precondition>Task 2's local verification passed, and — if 238-02 recorded that ref deletion is governed by the ruleset — the enforcement flip window is open and its start time recorded.</precondition>
  <read_first>
    - scripts/maintainers/delete-planning-tags.sh
    - .planning/decisions/003-tag-delete-list.tsv (the remote-flagged subset the remote pass acts on)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md (Pitfall 1 — the list command's field name does not exist, so the draft count comes from the REST releases route; Pitfall 6 — a remote delete of a local-only tag errors)
    - mix.exs (lines 200-215: the documentation source reference and the comment warning about exactly this failure mode)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
  </read_first>
  <action>
Capture the before-state of the release surface first, from the REST releases route: the total
release count and the draft count. Do not use the list command's JSON field for drafts — that field
name does not exist on this tool version and the call errors rather than returning zero, which
would leave a swallowed error looking like a clean result. If the before-count cannot be read,
stop; an after-count with nothing to compare against proves nothing.

Run the remote pass with the explicit apply flag, as its own invocation, acting only on rows
flagged present on the remote. Then, as a separate invocation, run the remote verification. Do not
suppress any delete's exit status: a suppressed status would hide a server-side rejection just as
effectively as it hides a missing ref.

Record `## AFTER-REMOTE-DELETE` with the fenced apply command and its full per-row output, the
fenced verification command and its output, the positive control's report, and — if an enforcement
flip window was used — its start time, end time and reason, with the live enforcement value
confirmed active again afterwards.

Then record `## AFTER-RELEASE-SURFACE`: the after release count and draft count from the same REST
route, side by side with the before values; the assertion that the release-backing tag set is
unchanged; and a live check that a published documentation source link still resolves, which is
what proves the documentation source reference's tag was not among the deleted. State plainly which
tag that reference resolves to and that it is a keep-set member.

Confirm the working tree is clean and the ledger is committed at the head these observations were
made at, per the milestone's standing constraint.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; git ls-remote --tags origin | sed "s#.*refs/tags/##" | grep -v "\^{}" | sort -u > /tmp/238-remote-after.txt; test -s /tmp/238-remote-after.txt || { echo "remote tag listing empty — the parse broke, this is not a pass" >&2; exit 1; }; grep -E "^v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$" /tmp/238-remote-after.txt | sort -u > /tmp/238-remote-keep.txt; test -s /tmp/238-remote-keep.txt || { echo "remote keep-set empty — the parse broke, this is not a pass" >&2; exit 1; }; diff /tmp/238-remote-after.txt /tmp/238-remote-keep.txt || { echo "REMOTE DRIFT: a tag survives on origin that is not a release tag (three numeric components with an optional prerelease or build suffix)" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass" or "REMOTE DRIFT"</fails_when>
    <automated>bash -c 'set -eo pipefail; gh api repos/szTheory/sigra/releases --paginate > /tmp/238-releases-after.json; n=$(jq length /tmp/238-releases-after.json); test "$n" -gt 0 || { echo "release enumeration returned $n — the parse broke, this is not a pass" >&2; exit 1; }; d=$(jq "[.[]|select(.draft)]|length" /tmp/238-releases-after.json); test "$d" -eq 0 || { echo "$d draft releases found, expected 0" >&2; exit 1; }; jq -r ".[].tag_name" /tmp/238-releases-after.json | sort -u > /tmp/238-release-tags.txt; comm -23 /tmp/238-release-tags.txt /tmp/238-remote-after.txt > /tmp/238-untagged.txt; test ! -s /tmp/238-untagged.txt || { echo "a release lost its backing tag: $(cat /tmp/238-untagged.txt)" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass", "draft releases found", or "a release lost its backing tag"</fails_when>
    <automated>bash -c 'set -eo pipefail; v=$(grep -oE "@version \"[0-9.]+\"" mix.exs | head -1 | grep -oE "[0-9]+\.[0-9]+\.[0-9]+"); test -n "$v" || { echo "could not read the package version from mix.exs — the parse broke, this is not a pass" >&2; exit 1; }; git ls-remote --tags origin "refs/tags/v$v" > /tmp/238-srcref.txt; test -s /tmp/238-srcref.txt || { echo "the tag the documentation source reference resolves to (v$v) is absent from origin" >&2; exit 1; }'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass" or "is absent from origin"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; for slot in AFTER-REMOTE-DELETE AFTER-RELEASE-SURFACE; do sed -n "/^## $slot\$/,/^## /p" "$f" > /tmp/238-s.txt; test -s /tmp/238-s.txt || { echo "slot $slot empty — the parse broke, this is not a pass" >&2; exit 1; }; grep -qE "^Status: captured" /tmp/238-s.txt || { echo "slot $slot not captured" >&2; exit 1; }; grep -q "```" /tmp/238-s.txt || { echo "slot $slot has no fenced producing command" >&2; exit 1; }; done; grep -q "repos/szTheory/sigra/releases" "$f" || { echo "the release counts were not produced by the REST releases route" >&2; exit 1; }; test -z "$(git status --porcelain)"'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke", "not captured", "no fenced producing command", or "not produced by the REST releases route"; also fails when the working tree is dirty</fails_when>
  </verify>
  <acceptance_criteria>
    - The remote tag listing is set-equal to the keep-set derived from the release expression alone, compared as sorted sets, where the release expression admits an optional prerelease or build suffix.
    - No tag flagged present on the remote in the allowlist resolves on origin any more, and no remote delete was attempted for a row not so flagged.
    - The release count read from the REST releases route is identical before and after, and the draft count is zero.
    - Every release's backing tag is still present on origin.
    - The tag the documentation source reference resolves to is still present on origin, and the slot names that tag explicitly.
    - `AFTER-REMOTE-DELETE` and `AFTER-RELEASE-SURFACE` are both captured with fenced commands and output; the former records any enforcement flip window's start, end and reason, and the live enforcement value is active at task end.
    - The working tree is clean at the head where these observations were captured.
  </acceptance_criteria>
  <done>Both sides match their keep-sets exactly, the release surface is provably untouched, and the whole destructive sequence is on the record as four separate observations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator authorization → destructive execution | The gate between an authorized plan and an irreversible remote mutation |
| allowlist row → remote ref delete | The only path by which a name reaches a destructive command |
| pre-deletion SHA record → future recoverability | The sole record of where the deleted refs pointed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-24 | Denial of Service | a release-backing tag or the documentation source tag deleted | critical | mitigate | Allowlist-driven single-name deletes; post-pass assertion that every release still has a backing tag on origin and that the documentation source reference's tag is present |
| T-238-25 | Repudiation | an irreversible remote mutation with no authorization record | high | mitigate | Blocking decision checkpoint presenting the live allowlist contents, with the answer recorded in the SUMMARY |
| T-238-26 | Repudiation | deleted objects with no record of where they pointed | high | mitigate | The pre-deletion SHA column captured in 238-04; no garbage collection, reflog expiry or prune-now anywhere in this phase |
| T-238-27 | Spoofing | a swallowed tool error reported as a clean draft count | high | mitigate | The draft count comes from the REST releases route, whose field exists; the before-count must read successfully or the task stops |
| T-238-28 | Denial of Service | the ruleset blocking the deletions and the failure being read as a no-op | high | mitigate | Delete exit statuses are not suppressed; the enforcement flip window, if needed, is opened deliberately and closed with the live value re-asserted |
| T-238-29 | Elevation of Privilege | weakening the guard to unblock a stuck delete | high | mitigate | The documented remedy is the recorded enforcement flip window, never adding a bypass actor; the bypass list stays empty throughout |
| T-238-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages; no dependency manifest changes |
</threat_model>

<verification>
- Local listing set-equal to the local keep-set; remote listing set-equal to the remote keep-set; both compared as sorted sets with a non-empty positive control.
- Release count unchanged, zero drafts, every release still tagged, documentation source tag present.
- Four captured slots covering the local pass, the local verification, the remote pass, the remote verification and the release surface.
- Ruleset enforcement active at plan end; bypass list still empty.
- Working tree clean at the head the observations were captured at.
</verification>

<success_criteria>
- REL-02 holds on both sides, asserted by set equality against an expression-derived keep-set with no count hardcoded.
- SC-4 holds: the release surface is identical before and after, with zero drafts, read from a route whose fields exist.
- The one-way act was authorized by an operator who saw the exact set, and every pass is independently recorded.
</success_criteria>

<output>
Create `.planning/phases/238-tag-guard-then-tag-deletion/238-05-SUMMARY.md` when done, recording the
deletion date — 238-06's ADR amendment cites it.
</output>

