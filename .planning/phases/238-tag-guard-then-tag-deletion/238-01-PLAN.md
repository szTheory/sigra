---
phase: 238-tag-guard-then-tag-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md
  - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
autonomous: false
requirements: [REL-01]
user_setup:
  - service: github-repo-settings
    why: "POST/DELETE /repos/szTheory/sigra/rulesets is a repo-settings write; the harness permission classifier blocked it during discussion (D-01). Nothing in this phase can proceed without it."
    dashboard_config:
      - task: "Authorize the executing agent to perform repo-settings writes on szTheory/sigra (gh auth with admin scope, plus harness permission for `gh api -X POST repos/szTheory/sigra/rulesets`)"
        location: "Local gh auth + harness permission prompt"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A pre-change record exists in git naming the exact live ruleset state (one ruleset, `main` / `14941512`) before any ruleset write happens, so revert is by recorded value rather than reconstruction."
    - "Both feasibility probes (Tier 1 `tag_name_pattern`, Tier 2 `creation` + `ref_name.exclude`) are issued with enforcement set to disabled and deleted immediately, and each outcome is recorded verbatim as evidence."
    - "The chosen tier is recorded together with the API response that justifies it, never inferred from documentation alone."
    - "Wherever the tier is recorded, a Tier-2 selection also names its two departures from locked source artifacts — D-04's absolute 'no creation rule, ever' and ROADMAP SC-1's named mechanism — so the departure travels with the tier rather than being discovered later."
    - "A Tier-3 outcome is a named, accepted replanning trigger of this plan set, not an uncovered gap: D-02's pre-push hook and detection workflow are deliberately deferred out of this phase and the disposition is recorded as such."
    - "After the probes, the live ruleset list is back to exactly one ruleset and no probe ruleset survives."
    - "Every evidence slot heading matches the BEFORE-/AFTER- uppercase grammar and every slot body opens with a Status line."
  artifacts:
    - .planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md
  key_links:
    - "238-TAG-RULESET-RECORD.md carries the two payloads any revert must restore to"
    - "The recorded tier drives every downstream artifact shape in 238-02 / 238-03 / 238-06"
  prohibitions:
    - "MUST NOT issue a probe whose enforcement value enforces anything; probe bodies are disabled-only."
    - "MUST NOT leave a probe ruleset in place; each probe is deleted in the same task that created it."
    - "MUST NOT edit, patch, or delete ruleset 14941512."
    - "MUST NOT commit unredacted payloads; this is a public repo, so home-directory prefixes and token material are scrubbed before commit."
    - "MUST NOT record a probe outcome inferred from GitHub documentation instead of the observed API response."
    - "MUST NOT begin building Tier 3 infrastructure inside this plan."
    - "MUST NOT record a Tier-2 selection without naming the D-04 and ROADMAP SC-1 departures alongside it; D-02's own standard is a recorded supersession, never a quiet re-scope."
---

<objective>
Establish that this phase is authorized to write repo settings, capture the revert value for the
ruleset surface before anything is written, and settle the whole Tier-1 / Tier-2 / Tier-3 ladder
in a single write cycle so no downstream plan is blocked on an unanswered question.

This plan is the pre-tracer prerequisite: it performs no repo-side wiring and creates no enforcing
configuration. The phase's tracer is task 2 of 238-02, which cannot be written until this plan
records which ruleset shape the API actually accepts.

Purpose: D-01 records that repo-settings writes were blocked during discussion. Research resolves
the Tier-1 question to a near-certain NO on documentary evidence and proposes a Tier-2 shape the
CONTEXT did not consider. Neither is settled until the API answers.
Output: a committed pre-change settings record, a committed evidence ledger with three captured
slots, and a recorded tier selection.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md
@.planning/phases/238-tag-guard-then-tag-deletion/238-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols created by Phase 238 (new — exclude from drift / source-grounding verification):

- `.github/rulesets/tag-namespace.json` (new file; new `.github/rulesets/` directory)
- `scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs` (new file)
- `test/fixtures/prohibitions/p19-tag-ruleset-absent-or-altered.json` (new file)
- `scripts/maintainers/delete-planning-tags.sh` (new file)
- `.planning/decisions/003-tag-delete-list.tsv` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md` (new file)
- `.planning/phases/238-tag-guard-then-tag-deletion/COVERAGE.md` (new file)
- GitHub ruleset named `tag-namespace` (live GitHub configuration, not a repo file)
- CI job id `tag_ruleset_drift` in `.github/workflows/ci-observe.yml` (new job)
- Shell function names `fail` and `usage`, and the `--apply` and `--allowlist` flags, in `delete-planning-tags.sh`
- JS function name `rulesetShapeIssue` in the `p19` guard
- Evidence slot names: `BEFORE-RULESET-STATE`, `BEFORE-TAG-INVENTORY`,
  `BEFORE-RULESET-FEASIBILITY-PROBE`, `AFTER-RULESET-ACTIVE`, `AFTER-SC1-REJECT-ACCEPT`,
  `AFTER-DELETE-PROBE`, `AFTER-P19-RED`, `AFTER-LOCAL-DELETE`, `AFTER-REMOTE-DELETE`,
  `AFTER-RELEASE-SURFACE`

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize repo-settings writes and the live ruleset mutation</name>
  <decision>Is this phase authorized to perform repo-settings writes on `szTheory/sigra` — `POST /repos/szTheory/sigra/rulesets` and `DELETE /repos/szTheory/sigra/rulesets/{id}` — given that D-01 records the harness permission classifier blocking exactly this call class during discussion, and that the phase's whole deliverable is a live ruleset with an empty `bypass_actors` list that binds the repo owner?</decision>
  <options>
    <option name="(a) Authorize — proceed with repo-settings writes on szTheory/sigra">The executor may issue POST and DELETE against the rulesets endpoint. What that permits: the two disabled-enforcement feasibility probes in task 3, each deleted in the same task; and, in 238-02, the creation of a sibling ruleset named `tag-namespace` with `target: "tag"` and an empty `bypass_actors` list — empty per D-05, because binding the repo owner is the point. Ruleset `main` (id `14941512`) is never a write target: it is not read-modify-written, not patched and not deleted. The revert value for the whole surface is the pre-change record committed in task 2, and the revert operation is a DELETE of the new ruleset's id only. Blast radius if the shape is wrong: release-please's own `v1.5.1` tag creation (Phase 242 / REL-06) could be blocked — under Tier 2 that tag is out of the ruleset's scope entirely, and under Tier 1 the accept-probe in 238-02 is the detector.</option>
    <option name="(b) Decline — stop the phase here">No POST or DELETE is issued, no probe runs, and no ruleset is created. The plan halts at this task: tasks 2 and 3 do not run and no downstream plan in the set is dispatched. The decline is recorded in the SUMMARY with its date, and the SUMMARY states that the phase is blocked on operator authorization rather than on a technical finding. **The decline is not a route to a workaround.** The executor must not substitute a client-side or advisory mechanism, must not fall back to Tier 3 (D-02's pre-push hook and tag-push detection workflow) without a separate explicit instruction, and must not retry the blocked call under a different credential or tool. REL-01 stays open.</option>
  </options>
  <resume-signal>The operator answers `(a)` or `(b)`. On `(a)` — say "authorized" — execution continues into task 2 (the pre-change record) and then task 3 (the two probes). On `(b)` — say "declined" — the executor records the decline with the date and halts the plan immediately; it does not attempt any alternative mechanism, and phase 238 stops pending a new operator instruction.</resume-signal>
  <reversibility rating="costly">Creating a live active repository ruleset with an empty bypass list binds every actor including the repo owner. It is undoable by deleting the ruleset, but it is an outward-facing settings change on a public repo and while active it can block a legitimate push.</reversibility>
  <read_first>
    - .planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md (D-01, D-05, D-17)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md (Environment Availability, "Missing dependencies with no fallback")
  </read_first>
  <action>
Halt and present to the operator, before any API write is attempted:

(a) This phase requires `gh api -X POST repos/szTheory/sigra/rulesets` and
`gh api -X DELETE repos/szTheory/sigra/rulesets/{id}`. D-01 records that the harness permission
classifier blocked exactly this class of call during discussion. All read calls succeeded.

(b) What will be created: a sibling ruleset named `tag-namespace`, `target: "tag"`, with an empty
`bypass_actors` list. The existing ruleset `main` (id `14941512`) is not read-modify-written, not
deleted, and is never a write target. Per D-05 the empty bypass list is load-bearing — it binds
the repo owner, which is the point of the phase.

(c) Blast radius if the shape is wrong: release-please's own `v1.5.1` tag creation
(Phase 242 / REL-06) could be blocked. Under the Tier-2 shape that tag is out of the ruleset's
scope entirely; under Tier 1 the accept-probe in 238-02 is the detector. Either way the revert
is the recorded pre-change value captured in task 2.

(d) The one-way acts of this phase are not here. Remote tag deletion is gated separately by its
own checkpoint in 238-05.

Ask the operator to confirm: proceed with repo-settings writes on `szTheory/sigra`, or stop the
phase. If the operator declines, record the decline and halt — do not attempt a workaround and do
not fall back to Tier 3 without an explicit instruction.
  </action>
  <verify>
    <human-check>Operator has explicitly authorized repo-settings writes on szTheory/sigra for this phase, or explicitly declined and the phase is halted.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator's answer is recorded in the plan SUMMARY as an explicit authorize or decline, with the date.
    - No POST or DELETE call against the rulesets endpoint has been issued before this checkpoint resolves.
    - If declined: no further task in this plan runs, and the SUMMARY states the phase is blocked on operator authorization rather than on a technical finding.
  </acceptance_criteria>
  <done>Repo-settings write authorization is settled one way or the other, in writing.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the pre-change ruleset record and open the evidence ledger</name>
  <precondition>`gh auth status` reports an authenticated account with admin access to szTheory/sigra, and Task 1 returned an authorization.</precondition>
  <read_first>
    - .planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-PAGES-SETTING-RECORD.md (the exact frontmatter and body convention to copy, per D-17)
    - .planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-EVIDENCE.md (ledger structure: header, observed-at-commit line, summary table, slot bodies, per D-16)
    - .planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-GIT-OBJECT-SNAPSHOT.md (the public-repo redaction convention)
    - scripts/ci/prohibitions/_lib.mjs (lines 250-300: the slot-heading regex and the evidence-slot parser whose grammar the ledger must satisfy)
  </read_first>
  <action>
Create `.planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md` following
`237-PAGES-SETTING-RECORD.md` exactly. Frontmatter keys: `phase: 238-tag-guard-then-tag-deletion`,
`plan: 01`, `requirement: REL-01`, `decision: D-17`, and
`kind: outward-facing-settings-change-record`. Body: a title naming the surface as a pre-change
record and revert value; a paragraph stating the record exists so the change is reversible by a
recorded, known value rather than by a reconstruction, and that it is committed before the change
is performed; a paragraph naming D-01, D-03 and D-17 as the authorizing decisions; then a
`## PRE-CHANGE STATE — the value to revert to` section carrying the capture timestamp and both
fenced payloads, verbatim:

- `gh api repos/szTheory/sigra/rulesets` (the list call)
- `gh api repos/szTheory/sigra/rulesets/14941512` (the by-id call — the list endpoint omits
  `conditions`, `rules`, `bypass_actors` and `current_user_can_bypass`)

State in prose that the revert operation for this phase is a DELETE against the rulesets endpoint
for the new ruleset's id only, and that `14941512` is never a write target. Redact any
home-directory prefix as `<REDACTED_HOME>` and scrub any token material; the repo is public.

Create `.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md` following
`237-EVIDENCE.md`: a `# Phase 238 Evidence Ledger` title, an `Observed at commit:` line, a
paragraph asserting the observations were captured at that commit on a clean tree, a summary table
with columns `| Slot | What it is | How captured | Status |`, then one section per slot. Seed the
ledger with the ten slot names listed in this plan's "Artifacts this phase produces" block. Slots
not yet captured carry a pending status; two are captured now:

- `## BEFORE-RULESET-STATE` — fenced `gh api repos/szTheory/sigra/rulesets` with its output.
- `## BEFORE-TAG-INVENTORY` — fenced commands and output for the local and remote inventories and
  the per-class partition, using `git tag`, `git ls-remote --tags origin`, and the four class
  regexes `^v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$`, `^archive/`, `^v[0-9]+\.[0-9]+$`,
  `^phase-238-`. The release expression carries an optional prerelease or build suffix because D-05
  records that the release automation may mint a release-candidate tag; the same expression is the
  keep expression 238-04 and 238-05 use, and the two must not diverge. The partition must account
  for every tag on both sides with nothing unclassified.

Slot headings are exactly two hashes, a space, then `BEFORE-` or `AFTER-` followed by uppercase
letters, digits and hyphens only. The status line is the first non-blank line of each slot body
and begins at column one.

Record honestly in the ledger header (RESEARCH Pitfall 4) that no committed guard reads this
ledger at HEAD — every evidence-reading guard pins Phase 230's ledger — so this grammar is
convention at the moment of writing, and that 238-03 makes the claim true by having the `p19`
guard assert it as a secondary artifact.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; n=$(grep -cE "^## (BEFORE|AFTER)-[A-Z0-9-]+$" "$f"); test "$n" -ge 10 || { echo "only $n slot headings parsed — the parse broke, this is not a pass" >&2; exit 1; }; s=$(grep -cE "^Status:" "$f"); test "$s" -ge 10 || { echo "only $s Status lines — the parse broke, this is not a pass" >&2; exit 1; }; grep -qE "^## BEFORE-RULESET-STATE$" "$f"; grep -qE "^## BEFORE-TAG-INVENTORY$" "$f"'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-TAG-RULESET-RECORD.md; grep -q "^kind: outward-facing-settings-change-record$" "$f"; grep -q "14941512" "$f"; ! grep -qE "ghp_|gho_|github_pat_|/Users/" "$f"'</automated>
    <fails_when>non-zero exit — the frontmatter kind line is absent, the pre-change ruleset id is not recorded, or a token prefix or absolute home path survived redaction</fails_when>
  </verify>
  <acceptance_criteria>
    - `238-TAG-RULESET-RECORD.md` frontmatter contains `kind: outward-facing-settings-change-record` and `decision: D-17`.
    - The record embeds both the list-endpoint payload and the `14941512` by-id payload as fenced JSON, not as a prose summary.
    - `238-EVIDENCE.md` exposes at least 10 slot headings matching the BEFORE-/AFTER- grammar, each with a Status line at column one.
    - `BEFORE-RULESET-STATE` and `BEFORE-TAG-INVENTORY` are both marked captured and each carries a fenced producing command with its output.
    - The `BEFORE-TAG-INVENTORY` partition accounts for the full local tag count and the full remote tag count with nothing unclassified on either side.
    - Both files are committed before the first POST in task 3.
  </acceptance_criteria>
  <done>The revert value is in git and the ledger is machine-parseable, before the first write.</done>
</task>

<task type="auto">
  <name>Task 3: Probe Tier 1 and Tier 2 in one write cycle and record the tier</name>
  <precondition>Task 2's commit is on disk and the working tree is clean, so the pre-change record provably predates the first write in git history.</precondition>
  <read_first>
    - .planning/phases/238-tag-guard-then-tag-deletion/238-RESEARCH.md (Architecture Patterns 1 and 2 for the two exact request bodies; Code Examples "D-01 + Tier-2 feasibility probes, one cycle"; Pitfall 2; Pitfall 5)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-CONTEXT.md (D-01 through D-05)
    - .planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md (the ledger written in task 2)
  </read_first>
  <action>
Issue both probes against `POST /repos/szTheory/sigra/rulesets`, each with an enforcement value of
`disabled` (a disabled ruleset enforces nothing), capture the round-tripped by-id payload on
acceptance, then DELETE the created ruleset immediately in the same task. Neither probe body
carries a `deletion` rule, and both carry an empty `bypass_actors` list.

Probe A (Tier 1, D-03): name `tag-namespace-probe-tier1`, `target: "tag"`,
`conditions.ref_name.include` of `refs/tags/v*` with an empty exclude list, and exactly one
`tag_name_pattern` rule with `operator: "regex"` and `negate: false`, carrying GitHub's own
published SemVer regex prefixed with `^v`. Correction to D-03 carried from RESEARCH Pitfall 2:
terminate the pattern with an optional-newline end anchor rather than a bare end anchor, because
GitHub's metadata-restrictions note says a bare end anchor can fail to match a name carrying a
trailing newline from a git push or CLI flow — which would reject a valid release tag. Rulesets
are RE2; negative lookahead is unsupported, so do not reach for it.

Probe B (Tier 2, RESEARCH Architecture Pattern 2 — a shape the CONTEXT did not consider): name
`tag-namespace-probe-tier2`, `target: "tag"`, `conditions.ref_name.include` of `refs/tags/v*`,
`conditions.ref_name.exclude` of `refs/tags/v*.*.*`, and exactly one rule of type `creation`.
This is server-side and is documented as not enterprise-gated. `ref_name` matching is fnmatch with
FNM_PATHNAME and `.` is an ordinary literal, so the exclude pattern subtracts three-dot-segment
names from scope. This dissolves D-02's objection that a creation rule would block release-please.

Record `## BEFORE-RULESET-FEASIBILITY-PROBE` in the ledger with, for each probe: the fenced
request body, the fenced response including status and body (the error body verbatim when
rejected, so the rejection reason is citable), the round-tripped by-id payload when accepted, and
the fenced delete confirmation. Mark the slot captured.

Then select and record the tier, in this precedence:

- Probe A accepted → Tier 1. REL-01 is satisfiable verbatim; no requirements supersession.
- Probe A rejected, Probe B accepted → Tier 2. REL-01's "server-side" wording survives; the
  supersession 238-06 records narrows from "detection only, not server-side prevention" to
  "server-side prevention at coarser granularity than a SemVer regex". State the weakness in those
  terms: the shape guard admits four-segment and non-numeric names, and blocks the two-component
  recurrence class only.
- Both rejected → Tier 3. Do not begin building the pre-push hook or the detection workflow in
  this plan. Record the outcome, stop, and surface to the operator that Tier 3 is genuinely new
  infrastructure (no hooks infrastructure exists at HEAD; `core.hooksPath` is unset) plus a full
  REL-01 supersession — a scope decision, not an implementation detail.

Write the selection into the slot as a `Tier selected:` line naming the tier and citing the
response that justifies it. Downstream plans read that line. Do not state a tier that
documentation implies but the API did not demonstrate.

**The departure travels with the tier.** Tier 2's ruleset contains a `creation` rule, and two
locked source artifacts say otherwise. CONTEXT D-04 is absolute — the ruleset contains no
`creation` rule and no `deletion` rule, ever — and ROADMAP SC-1 names the mechanism literally as a
name-pattern rule. The Tier-2 reasoning (the exclude list subtracts three-segment names from scope,
so the D-02 objection that a creation rule would block the release automation does not apply, and
SC-1's observable outcome still holds because a two-component scratch name carries one dot and is
therefore not excluded) may well be right — but D-02's own standard for a decision that no longer
holds is a recorded supersession, never a quiet re-scope. So: when the selection is Tier 2, write a
second line into the same slot, immediately after the `Tier selected:` line, headed
`Departs from:` and naming both departures explicitly — D-04's no-creation-rule clause and
ROADMAP SC-1's named mechanism — each with the one-sentence reason the departure is safe and a
pointer to 238-06 task 3, which records the dated supersession. The obligation triggers on the
landed rule type, not on any requirement's wording: if a `creation` rule is what lands, the
departure is recorded, full stop.

**Tier 3 is an accepted disposition, not an uncovered gap.** D-02 decided a two-part Tier-3
fallback — a committed pre-push hook and a tag-push detection workflow — and no plan in this set
delivers either, by deliberate choice: neither exists at HEAD, both are genuinely new
infrastructure, and research resolves Tier 1 to a near-certain NO with Tier 2 as the expected
landing. This plan set therefore **accepts** Tier 3 as a named replanning trigger rather than
carrying contingent scope it is overwhelmingly unlikely to need. When Tier 3 is selected, record a
`Tier-3 disposition:` line in the slot stating that it is an accepted replanning trigger, that
D-02's pre-push hook and detection workflow are deliberately deferred out of this phase, and that
the phase halts here for an operator scope decision. Recording the disposition is the deliverable;
building the fallback is not.

Finally confirm no probe ruleset survives: the live list must be back to exactly the pre-change
payload recorded in task 2.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; gh api repos/szTheory/sigra/rulesets > /tmp/238-rulesets-after-probe.json; n=$(jq length /tmp/238-rulesets-after-probe.json); test "$n" -eq 1 || { echo "ruleset list has $n entries, expected 1 — a probe ruleset survived" >&2; exit 1; }; jq -e ".[0].id == 14941512" /tmp/238-rulesets-after-probe.json >/dev/null; ! jq -r ".[].name" /tmp/238-rulesets-after-probe.json | grep -q probe'</automated>
    <fails_when>non-zero exit, or stderr contains "a probe ruleset survived"</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; sed -n "/^## BEFORE-RULESET-FEASIBILITY-PROBE$/,/^## AFTER-/p" "$f" > /tmp/238-probe-slot.txt; test -s /tmp/238-probe-slot.txt || { echo "probe slot empty — the parse broke, this is not a pass" >&2; exit 1; }; grep -qE "^Status: captured" /tmp/238-probe-slot.txt; grep -qE "^Tier selected: Tier [123]" /tmp/238-probe-slot.txt; grep -q "tag-namespace-probe-tier1" /tmp/238-probe-slot.txt; grep -q "tag-namespace-probe-tier2" /tmp/238-probe-slot.txt'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass"; also fails when the slot lacks a captured status, lacks a Tier selected line, or does not name both probe rulesets</fails_when>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/238-tag-guard-then-tag-deletion/238-EVIDENCE.md; sed -n "/^## BEFORE-RULESET-FEASIBILITY-PROBE$/,/^## AFTER-/p" "$f" > /tmp/238-probe-slot.txt; tier=$(grep -oE "^Tier selected: Tier [123]" /tmp/238-probe-slot.txt | head -1); test -n "$tier" || { echo "no Tier selected line — the parse broke, this is not a pass" >&2; exit 1; }; case "$tier" in *"Tier 2") grep -qE "^Departs from:" /tmp/238-probe-slot.txt || { echo "Tier 2 selected but the slot records no Departs from line" >&2; exit 1; }; grep -q "D-04" /tmp/238-probe-slot.txt || { echo "the Tier-2 departure does not name D-04" >&2; exit 1; }; grep -q "SC-1" /tmp/238-probe-slot.txt || { echo "the Tier-2 departure does not name ROADMAP SC-1" >&2; exit 1; };; *"Tier 3") grep -qE "^Tier-3 disposition:" /tmp/238-probe-slot.txt || { echo "Tier 3 selected but the accepted-replanning-trigger disposition is not recorded" >&2; exit 1; };; esac; echo "tier-record obligations satisfied for $tier"'</automated>
    <fails_when>non-zero exit, or stderr contains "the parse broke, this is not a pass", "records no Departs from line", "does not name D-04", "does not name ROADMAP SC-1", or "the accepted-replanning-trigger disposition is not recorded"</fails_when>
  </verify>
  <acceptance_criteria>
    - Both probe request bodies and both responses are embedded verbatim in `BEFORE-RULESET-FEASIBILITY-PROBE`, including the full error body for any rejection.
    - The slot carries a `Tier selected:` line whose value is Tier 1, Tier 2 or Tier 3, and the line cites the response that justifies it.
    - After the task, `gh api repos/szTheory/sigra/rulesets` returns exactly one entry whose id is `14941512`.
    - If Tier 2 was selected, the same slot carries a `Departs from:` line naming both D-04's no-creation-rule clause and ROADMAP SC-1's named mechanism, each with its reason and a pointer to the dated supersession 238-06 records. The obligation triggers on the landed rule type, not on any requirement's wording.
    - If Tier 3 was selected, the plan stops here, the slot carries a `Tier-3 disposition:` line recording it as an accepted replanning trigger with D-02's fallback deliberately deferred, and the SUMMARY states the scope decision the operator must make; no hook or detection-workflow file was created.
    - The Tier-1 probe body's pattern terminates with an optional-newline end anchor, not a bare end anchor.
  </acceptance_criteria>
  <done>The ladder is settled by observation: a recorded tier, two verbatim API responses, and a ruleset list restored to its pre-change value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/agent → GitHub repo settings API | A privileged write crosses here; the credential is an admin-scoped token |
| GitHub settings → git repo | The ruleset exists only server-side; the repo has no record of it except what this phase commits |
| this repo (public) → the world | Everything committed here is world-readable, including embedded API payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-01 | Tampering | ruleset `14941512` (the `main` branch ruleset) | critical | mitigate | Every write in this plan is a POST creating a new ruleset or a DELETE of an id returned by that POST in the same task; `14941512` is never a write target, and task 3's verify asserts it is still present and still the only ruleset |
| T-238-02 | Denial of Service | probe ruleset left enforcing | high | mitigate | Both probe bodies use a disabled enforcement value, and each probe is deleted in the same task; the verify fails when the list is not back to one entry |
| T-238-03 | Information Disclosure | committed API payloads on a public repo | medium | mitigate | Redaction of home-directory prefixes and token material before commit, asserted by a grep in task 2's verify |
| T-238-04 | Repudiation | a settings change with no recorded prior value | high | mitigate | D-17 pre-change record committed before the first write, carrying both the list and by-id payloads verbatim |
| T-238-05 | Spoofing | a tier claimed from documentation rather than observed | medium | mitigate | The `Tier selected:` line must cite the observed response; task 3's acceptance criteria require both request/response pairs embedded verbatim |
| T-238-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero packages — no `mix.exs`, `package.json` or action change — so no package-legitimacy checkpoint applies |
</threat_model>

<verification>
- `gh api repos/szTheory/sigra/rulesets` returns exactly one entry, id `14941512`, matching the payload committed in `238-TAG-RULESET-RECORD.md`.
- `238-EVIDENCE.md` parses to at least 10 BEFORE-/AFTER- slots, with `BEFORE-RULESET-STATE`, `BEFORE-TAG-INVENTORY` and `BEFORE-RULESET-FEASIBILITY-PROBE` captured.
- The working tree is clean and all three artifacts are committed.
</verification>

<success_criteria>
- Repo-settings write authorization is recorded as an explicit operator decision.
- The revert value for the ruleset surface is in git before any write occurred.
- The Tier-1 / Tier-2 / Tier-3 ladder is settled by one observed write cycle, and the tier is recorded with its justifying response.
- No probe ruleset survives and ruleset `14941512` is untouched.
</success_criteria>

<output>
Create `.planning/phases/238-tag-guard-then-tag-deletion/238-01-SUMMARY.md` when done. The SUMMARY
must state the selected tier verbatim, because 238-02, 238-03 and 238-06 branch on it. It must also
carry the tier's departure or disposition line verbatim — the `Departs from:` line under Tier 2, the
`Tier-3 disposition:` line under Tier 3 — so the obligation travels with the tier into every
downstream plan that reads this SUMMARY.
</output>
