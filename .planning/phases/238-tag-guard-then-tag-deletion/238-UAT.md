---
status: complete
phase: 238-tag-guard-then-tag-deletion
source: [238-01-SUMMARY.md, 238-02-SUMMARY.md, 238-03-SUMMARY.md, 238-04-SUMMARY.md, 238-05-SUMMARY.md, 238-06-SUMMARY.md, prior observer-job evidence in 238-UAT.md]
started: 2026-09-17T16:12:12Z
updated: 2026-09-25T16:48:03Z
---

## Current Test

[testing complete]

## Tests

### 1. Tier-1 and Tier-2 GitHub ruleset feasibility probes issued and recorded; Tier 2 selected as the server-side tag-guard mechanism for the rest of Phase 238
expected: Tier-1 and Tier-2 GitHub ruleset feasibility probes issued and recorded; Tier 2 selected as the server-side tag-guard mechanism for the rest of Phase 238
result: pass
source: automated
coverage_id: D1

### 2. Live tag-namespace ruleset created (by operator, per plan's already_done_by_operator note) and read-back-validated: target=tag, enforcement=active, bypass_actors=[], one creation rule, no deletion rule
expected: Live tag-namespace ruleset created (by operator, per plan's already_done_by_operator note) and read-back-validated: target=tag, enforcement=active, bypass_actors=[], one creation rule, no deletion rule
result: pass
source: automated
coverage_id: D1

### 3. SC-1 live reject/accept proof: v9.9 rejected (GH013), v9.9.9-rulesettest accepted -- proves Assumptions Log A3 (exclude wins over include)
expected: SC-1 live reject/accept proof: v9.9 rejected (GH013), v9.9.9-rulesettest accepted -- proves Assumptions Log A3 (exclude wins over include)
result: pass
source: automated
coverage_id: D2

### 4. p19 offline contract guard: asserts committed snapshot target==tag, non-vacuity floor, inline negative control, and committed known-bad fixture -- all green, ci.yml untouched
expected: p19 offline contract guard: asserts committed snapshot target==tag, non-vacuity floor, inline negative control, and committed known-bad fixture -- all green, ci.yml untouched
result: pass
source: automated
coverage_id: D3

### 5. Delete-deadlock question (D-06) answered by observation: deleting the accepted scratch tag from origin succeeded -- deletion is unaffected by this ruleset; no enforcement flip needed; enforcement confirmed still active
expected: Delete-deadlock question (D-06) answered by observation: deleting the accepted scratch tag from origin succeeded -- deletion is unaffected by this ruleset; no enforcement flip needed; enforcement confirmed still active
result: pass
source: automated
coverage_id: D4

### 6. Scratch tag cleanup: both v9.9 and v9.9.9-rulesettest removed locally and remotely; tag counts return to exactly 52 local / 33 remote, matching BEFORE-TAG-INVENTORY
expected: Scratch tag cleanup: both v9.9 and v9.9.9-rulesettest removed locally and remotely; tag counts return to exactly 52 local / 33 remote, matching BEFORE-TAG-INVENTORY
result: pass
source: automated
coverage_id: D5

### 7. p19 expanded from a single target==\"tag\" assertion to the full Tier-2 contract: target, enforcement, include conditions, exclude conditions (the Tier-2 discriminating pattern), rule count, and rule type -- 10 named behavior cases, 3 non-vacuity floors, a bypass-actors-omission proof, a negative control, all green against the committed snapshot
expected: p19 expanded from a single target==\"tag\" assertion to the full Tier-2 contract: target, enforcement, include conditions, exclude conditions (the Tier-2 discriminating pattern), rule count, and rule type -- 10 named behavior cases, 3 non-vacuity floors, a bypass-actors-omission proof, a negative control, all green against the committed snapshot
result: pass
source: automated
coverage_id: D1

### 8. Committed known-bad fixture rewritten to clear every non-vacuity floor (object, target present, rules array non-empty) while carrying two Tier-2 violations (enforcement: disabled, exclude widened) -- guard header names the fixture by path and lists both violations
expected: Committed known-bad fixture rewritten to clear every non-vacuity floor (object, target present, rules array non-empty) while carrying two Tier-2 violations (enforcement: disabled, exclude widened) -- guard header names the fixture by path and lists both violations
result: pass
source: automated
coverage_id: D2

### 9. Fail-first RED proof: GSD_PROHIB_SUBJECT pointed at the known-bad fixture exits non-zero with exactly one `not ok`, whose failure message names a drifted `enforcement` field -- the shape checker, never a parse-broke floor. Unsubstituted run (the committed snapshot) exits 0, 19/19. Both directions recorded in AFTER-P19-RED.
expected: Fail-first RED proof: GSD_PROHIB_SUBJECT pointed at the known-bad fixture exits non-zero with exactly one `not ok`, whose failure message names a drifted `enforcement` field -- the shape checker, never a parse-broke floor. Unsubstituted run (the committed snapshot) exits 0, 19/19. Both directions recorded in AFTER-P19-RED.
result: pass
source: automated
coverage_id: D3

### 10. Phase 238 evidence ledger's slot grammar is now machine-enforced by p19 (secondary artifact, read via readRepoFile + archiveAwareRelPath), correcting RESEARCH Pitfall 4's false D-16 claim to a true one. Malformed-ledger negative control and a real-ledger green assertion both pass.
expected: Phase 238 evidence ledger's slot grammar is now machine-enforced by p19 (secondary artifact, read via readRepoFile + archiveAwareRelPath), correcting RESEARCH Pitfall 4's false D-16 claim to a true one. Malformed-ledger negative control and a real-ledger green assertion both pass.
result: pass
source: automated
coverage_id: D4

### 11. The delete set exists as a committed 39-row tab-separated allowlist derived from the live repository, with independent local/remote presence flags, class, pre-deletion SHA and a per-row reason
expected: The delete set exists as a committed 39-row tab-separated allowlist derived from the live repository, with independent local/remote presence flags, class, pre-deletion SHA and a per-row reason
result: pass
source: automated
coverage_id: D1

### 12. The deletion script is dry-run by default, refuses unknown arguments, parses the allowlist fail-closed, and can never express a glob at a delete call site
expected: The deletion script is dry-run by default, refuses unknown arguments, parses the allowlist fail-closed, and can never express a glob at a delete call site
result: pass
source: automated
coverage_id: D2

### 13. Every destructive edge behavior is observed on a disposable clone: prefix-collision adjacency, idempotency, emptiness vs zero-row, vacuity refusal, failure propagation — and nothing real was deleted
expected: Every destructive edge behavior is observed on a disposable clone: prefix-collision adjacency, idempotency, emptiness vs zero-row, vacuity refusal, failure propagation — and nothing real was deleted
result: pass
source: automated
coverage_id: D3

### 14. 39 allowlisted tags deleted locally; surviving local set is exactly the regex-derived keep-set
expected: 39 allowlisted tags deleted locally; surviving local set is exactly the regex-derived keep-set
result: pass
source: automated
coverage_id: D1

### 15. 21 remote-flagged tags deleted from origin; surviving remote set is exactly the regex-derived keep-set
expected: 21 remote-flagged tags deleted from origin; surviving remote set is exactly the regex-derived keep-set
result: pass
source: automated
coverage_id: D2

### 16. Release surface unchanged across the deletion window: 12 published, 0 drafts, v1.4.0 source tree still 200
expected: Release surface unchanged across the deletion window: 12 published, 0 drafts, v1.4.0 source tree still 200
result: pass
source: automated
coverage_id: D3

### 17. MAINTAINING.md documents the live `tag-namespace` ruleset — a by-name operator read command, the operator-side bypass_actors check the CI guard cannot make, the do-not-break warning, and the observer-lane default-branch caveat
expected: MAINTAINING.md documents the live `tag-namespace` ruleset — a by-name operator read command, the operator-side bypass_actors check the CI guard cannot make, the do-not-break warning, and the observer-lane default-branch caveat
result: pass
source: automated
coverage_id: D1

### 18. MAINTAINING.md documents the four-invocation deletion procedure by citing the committed script and allowlist, without enumerating the delete set
expected: MAINTAINING.md documents the four-invocation deletion procedure by citing the committed script and allowlist, without enumerating the delete set
result: pass
source: automated
coverage_id: D2

### 19. ADR 003 carries a dated in-place amendment: guardrail 3's false claim retracted and corrected with both post-ADR tags and their dates, the 2026-09-17 deletion date, the allowlist path, the milestone/ and proof/ namespaces, an explicit non-coverage statement, and the guardrail-2 no-regression note
expected: ADR 003 carries a dated in-place amendment: guardrail 3's false claim retracted and corrected with both post-ADR tags and their dates, the 2026-09-17 deletion date, the allowlist path, the milestone/ and proof/ namespaces, an explicit non-coverage statement, and the guardrail-2 no-regression note
result: pass
source: automated
coverage_id: D3

### 20. The D-04 and ROADMAP SC-1 supersessions are recorded in both the ADR amendment and REQUIREMENTS.md, dated, triggered by the `creation` rule type read from the committed snapshot
expected: The D-04 and ROADMAP SC-1 supersessions are recorded in both the ADR amendment and REQUIREMENTS.md, dated, triggered by the `creation` rule type read from the committed snapshot
result: pass
source: automated
coverage_id: D4

### 21. The Phase 238 evidence ledger is closed: 10 of 10 slots captured, table status column reconciled with the slot bodies, AFTER-RELEASE-SURFACE filled from the live REST releases route and a resolved HexDocs source link
expected: The Phase 238 evidence ledger is closed: 10 of 10 slots captured, table status column reconciled with the slot bodies, AFTER-RELEASE-SURFACE filled from the live REST releases route and a resolved HexDocs source link
result: pass
source: automated
coverage_id: D5

### 22. After merge to main, confirm the tag_ruleset_drift observer job executes and passes
expected: CI (observe) executes the tag namespace drift job (not skipped) and its field-scoped comparison matches the committed ruleset snapshot.
result: pass
source: automated
coverage_id: D5 (238-03)
evidence: Live structured result: workflow run 35249205910 (event=workflow_run, head=6b03af0553a13d17db487f06927dc0b990b90e3c) succeeded; job “Tag namespace ruleset drift (live vs committed, not a merge gate)” concluded success. Its captured stdout says ruleset 23574716 matches the committed snapshot.

### 23. The evidence ledger's head pin resolves to a real commit on a clean working tree
expected: The ledger pin resolves to an existing commit and matches the first parent of the ledger-close commit; that commit changes only the ledger, and a detached checkout is clean.
result: pass
source: automated
coverage_id: D6
evidence: |
  Ledger-close commit f7a987528d3ec1a9e0ba196461bbc39170c97bd4 has parent 31380c75a77a3044ebb644e7b3f15537ca7fb281, exactly matching the ledger pin. The commit changes only 238-EVIDENCE.md. A detached checkout at f7a987528d3ec1a9e0ba196461bbc39170c97bd4 has empty git status.

## Summary

total: 23
passed: 23
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
