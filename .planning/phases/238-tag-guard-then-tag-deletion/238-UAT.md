---
status: complete
phase: 238-tag-guard-then-tag-deletion
source: [238-VERIFICATION.md]
started: 2026-09-17T16:12:12Z
updated: 2026-09-17T17:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. After merge to `main`, confirm the `tag_ruleset_drift` job in `CI (observe)` actually executes and is green
expected: Job runs (not skipped); live-vs-committed field-scoped diff empty.
result: pass
source: automated
evidence: |
  Observed programmatically, not by eye. Merge commit 6b03af05 (PR #243) pushed to `main`;
  the push-triggered `CI` run completed, which fired `CI (observe)` run 35249205910
  (event=workflow_run, head=6b03af0553a13d17db487f06927dc0b990b90e3c, conclusion=success).

  Job conclusion, read per-job rather than from the run:
    success  Tag namespace ruleset drift (live vs committed, not a merge gate)
    success  Demotion receipt (non-PR executed-set evidence, not a merge gate)
    skipped  Docs-only receipt (FAST-05 true-branch evidence, not a merge gate)

  The job's own stdout, distinguishable from the `^[[36;1m` script-echo lines:
    2026-09-17T16:52:04Z live tag-namespace ruleset (id 23574716) matches the committed snapshot
  The id in that line (23574716) is the ruleset 238 created, so the job reached the live API
  and diffed it against `.github/rulesets/tag-namespace.json` -- the field-scoped diff was empty.

  Reproduce:
    gh run view 35249205910 --json conclusion,event,headSha,jobs \
      --jq '.jobs[] | {name, conclusion}'

  FALSE-GREEN TRAP, recorded because it nearly closed this item wrongly. Two EARLIER observe
  runs at this same head SHA (35247765541, 35247921328) reported run conclusion `success`
  while this job was `skipped` -- its guard is `github.event.workflow_run.event != 'pull_request'`
  and both were triggered by PR-event CI runs. A green observe RUN is not proof. Only the run
  triggered by the `push` CI executes the job, and only the JOB's conclusion plus its stdout
  line prove it. Nothing in CI asserts this today; see the follow-up todo.

**Why this cannot be shifted left.** A `workflow_run` lane only ever executes the default-branch
copy of its own file, so the job's first execution is necessarily post-merge — no in-phase command
can trigger it. 238-03 and 238-06 both carry an explicit prohibition against claiming it proven
in-phase, and the phase correctly did not claim it.

**What is already covered without this.** The offline half of the paired guard (`p19`) is 19/19
with a recorded RED proof and reads the committed snapshot. Code review IN-03 records two latent
defects in the drift job — an unpaginated ruleset list, and a `set -euo pipefail` abort at
`ID=$(…)` that pre-empts the job's own named `ABSENT` message on a transient API failure — deferred
to `.planning/todos/pending/2026-09-17-tag-ruleset-drift-observer-has-never-run-and-has-two-latent-defects.md`.
Both fail **closed** (false red or unlabelled red, never false green), so a deleted ruleset would
still be caught. This is a trust-and-noise item, not a silent hole.

**How to run it:** after this phase's commits reach `main`, let one push complete a `CI` run, then
`gh run list --workflow "CI (observe)" --limit 1` and open the `Tag namespace ruleset drift` job.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
