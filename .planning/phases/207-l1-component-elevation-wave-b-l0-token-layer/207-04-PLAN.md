---
phase: "207"
plan: "04"
type: execute
wave: 2
depends_on:
  - "207-01"
  - "207-02"
  - "207-03"
files_modified:
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "D-08: Exactly 6 ledger rows are flipped from 1 to bare 2 — token-layer (L0, line ~60), empty_state (line ~66), page_back (line ~67), scope_ribbon (line ~68), field_help (line ~71), skeleton (line ~72); no other rows modified"
    - "D-08: Every flipped tier value is a bare integer 2 in column 4 (awk -F'|' parse, /^[012]$/) — no decorated values (2*, 2 ✓, ~2) anywhere in the ledger, which would silently drop the row from the monotonic guard"
    - "D-08: Each of the 5 L1 rows' Evidence column carries the rich semicolon-delimited Tier-2 shape mirroring the 8 Wave-A rows (~:61-73): 3-project axe+screenshot citation (admin-design.spec.ts board-<name>); motion-tokens guarded (admin-css-conformance.sh, no transition:all, global reduced-motion strip ~:1467); target-size (reviewed value or N/A); token-conformance (admin-css-conformance.sh + admin-token-completeness.sh exit 0); N/A for inapplicable proxies"
    - "COMP-03: The token-layer L0 row's evidence leads with admin-token-completeness.sh exits 0 — 100/100 :root --sg-* tokens documented in admin-token-reference.md, plus the admin-css-conformance.sh hex/[+px] citation"
    - "After the flip, scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 and all 13 L1 cells + the L0 token-layer cell read bare 2 (the entire L0/L1 column is Tier-2)"
  artifacts:
    - guides/reference/admin-quality-ledger.md
  key_links:
    - "The monotonic guard reads column 4 with awk -F'|' and /^[012]$/ — bare 2 only; a decorated value loses regression protection (Pitfall 1)"
    - "Plan 02 SUMMARY supplies the per-component audit findings (interaction-state, target-size, light/dark) that become the 5 L1 evidence strings"
    - "Plan 01 produced admin-token-completeness.sh and Plan 03 refreshed admin-token-reference.md — both are the citable COMP-03 proofs for the token-layer L0 evidence string"
---

<objective>
Flip exactly 6 rows in guides/reference/admin-quality-ledger.md from tier 1 to bare 2 — the
5 L1 component rows (empty_state, page_back, scope_ribbon, field_help, skeleton) and the L0
token-layer row — each with a rich semicolon-delimited evidence string mirroring the 8
freshly-flipped Wave-A rows. Verify the monotonic guard exits 0 vs origin/main and the entire
L0/L1 ledger column now reads bare 2.

Purpose: This is the ledger-flip step — the final assertion locking Tier-2 status for the
remaining 5 L1 components and the L0 token layer under the monotonic guard's forward-only
protection (D-08). Every evidence string cites the automated gates (Plan 01's
admin-token-completeness.sh, the existing admin-css-conformance.sh + admin-design.spec.ts
axe/screenshot harness) and the documented-as-manual proxy outcomes from Plan 02's audit.

Output: guides/reference/admin-quality-ledger.md with 6 rows at bare tier 2; full L0/L1
column at Tier-2; monotonic guard green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-CONTEXT.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-01-SUMMARY.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-SUMMARY.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-03-SUMMARY.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
@scripts/ci/quality-ledger-monotonic.sh
</context>

## Artifacts this phase produces

- 6 flipped rows in `guides/reference/admin-quality-ledger.md` (token-layer L0 + empty_state / page_back / scope_ribbon / field_help / skeleton) — each bare tier `2` with a rich semicolon-delimited evidence string

<tasks>

<task type="auto">
  <name>Task 1: Flip 6 ledger rows (token-layer L0 + 5 L1) to bare tier 2 with rich evidence</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — read the full Quality Ledger; the 6 target rows are: token-layer (L0) at line ~60, empty_state at line ~66, page_back at line ~67, scope_ribbon at line ~68, field_help at line ~71, skeleton at line ~72 (all currently tier 1 with sparse link-style evidence). The Tier-2 exemplar evidence shape = the 8 Wave-A rows at ~:61-73 (stat, stat_link, task_card, summary_chip, applied_chip, notice, notice_link, audit_row). Also read any "Parsing Rules" / "Asserting Tier 2" section explaining the bare-2 requirement and forbidden decorators.
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-SUMMARY.md "Per-Component Audit" — per-component target-size and interaction-state findings (page_back 36px sg-btn--sm; field_help ~40×40 expanded ::before hit-area; empty_state/scope_ribbon/skeleton N/A) and the "CSS edited: yes/no" verdict
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-01-SUMMARY.md — the admin-token-completeness.sh guard name + PASS line (100 tokens) and the D-07/D-07a px decision, for the evidence-string citations
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-RESEARCH.md "Ledger evidence-string template" (~:302-306) — the exact bare-2 evidence shape for both an L1 component row and the L0 token-layer row
  </read_first>

  <action>
  Use Edit (scoped replacement) to update each of the 6 rows — one Edit call per row, using the row's existing sparse cell text as the unique old_string. Never Write the full ledger file.

  Each new row format: | {name} | {level} | 2 | {evidence-string} |
  The tier column MUST be the bare integer 2 — no asterisks, no decorators. The awk -F'|' parser reads column 4 and requires /^[012]$/ after trimming whitespace.

  L1 evidence template (semicolon-delimited; use N/A for inapplicable proxies), mirroring the Wave-A rows:
  "admin-design.spec.ts assertBoardScreenshot board-{name} — 3 projects (admin-design-chromium / -mobile / -dark) × toHaveScreenshot + assertNoAxeViolations (wcag2a/2aa/21a/21aa/22aa) — 0 violations; motion-tokens: guarded — scripts/ci/admin-css-conformance.sh exits 0; no transition: all; inherits global @media (prefers-reduced-motion: reduce) strip at sigra_admin.css:~1467; target-size: {value}; token-conformance: admin-css-conformance.sh exits 0 (no raw hex outside :root) + admin-token-completeness.sh exits 0 (100/100 :root tokens documented); interaction-state: {value}; content-equivalence: N/A; overlay-axe: N/A; APG: N/A"

  Per-row specifics (use board-<name> with the UNDERSCORE form the existing Wave-A rows use, e.g. board-empty_state, board-page_back, board-scope_ribbon, board-field_help, board-skeleton — matching the COMPONENT_BOARDS ids):

  - empty_state (L1): target-size: N/A — static display container; interaction-state: N/A — static (no hover/focus/active/disabled).
  - page_back (L1): target-size: reviewed — sg-btn--sm min-height var(--sg-control-sm) = 2.25rem = 36px (dense-admin precedent, D-08); interaction-state: confirmed present — hover/focus-visible/active/disabled inherited from base .sg-btn (sigra_admin.css:~449-462, --ghost hover ~:490-493).
  - scope_ribbon (L1): target-size: N/A — decorative inline span (no [href]/[role=link]/[tabindex]); interaction-state: N/A — static.
  - field_help (L1): target-size: reviewed — trigger ::before expands hit area to ~40×40 CSS px (visible 18px + inset -0.6875rem, sigra_admin.css:~889-892); interaction-state: confirmed present — trigger hover/[aria-expanded]/active/focus-visible (sigra_admin.css:~894-903) + Escape-close/focus-restore (admin-design.spec.ts:~695-711); add "tooltip APG: panel role=tooltip, aria-controls/aria-expanded wired" if mirroring Wave-A APG notes, else APG: N/A.
  - skeleton (L1): target-size: N/A — visual placeholder; interaction-state: N/A; ADD motion note: infinite shimmer (::after, sigra_admin.css:~1408-1420) stripped by global reduced-motion block (animation-iteration-count: 1 !important), passing assertion at admin-design.spec.ts:~639-677.

  token-layer (L0) row — lead with the COMP-03 token proofs:
  "admin-token-completeness.sh exits 0 — 100/100 :root --sg-* tokens documented in admin-token-reference.md (D-06 guard; merge-blocking, dual light/dark :root scanned); admin-css-conformance.sh exits 0 — no raw hex outside :root token defs{ + raw-px CHECK 3 in token-eligible contexts IF Plan 01 PATH A, ELSE + reviewed raw-px documented manual proxy in admin-token-reference.md (D-07a)}; light/dark/system parity — dual :root blocks (light + @media prefers-color-scheme: dark); admin-token-reference.md refreshed to cite this evidence; target-size: N/A; interaction-state: N/A; content-equivalence: N/A; overlay-axe: N/A; APG: N/A". Keep the Level column as L0.

  Pull the exact px values and the D-07/D-07a wording from the Plan 01/02 SUMMARYs. Preserve table column spacing. Do NOT decorate the tier value.
  </action>

  <verify>
    <automated>bash -c 'grep -E "^\| (token-layer|empty_state|page_back|scope_ribbon|field_help|skeleton) " guides/reference/admin-quality-ledger.md | awk -F"|" "{t=\$4; gsub(/^ +| +\$/,\"\",t); print t}" | grep -xc 2'</automated>
  </verify>

  <acceptance_criteria>
  - All 6 target rows show bare 2 in column 4: the verify command returns 6 (token-layer, empty_state, page_back, scope_ribbon, field_help, skeleton all == 2)
  - No decorated tier values anywhere in the ledger: grep -E '^\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$' returns empty output
  - Each of the 5 L1 rows cites the axe/screenshot harness: grep -E '^\| (empty_state|page_back|scope_ribbon|field_help|skeleton) ' guides/reference/admin-quality-ledger.md | grep -c 'assertBoardScreenshot' returns 5
  - The token-layer L0 row cites the completeness guard: grep -E '^\| token-layer ' guides/reference/admin-quality-ledger.md | grep -c 'admin-token-completeness' returns 1
  - The 5 L1 rows cite token-conformance: grep -E '^\| (empty_state|page_back|scope_ribbon|field_help|skeleton) ' guides/reference/admin-quality-ledger.md | grep -c 'admin-token-completeness\|admin-css-conformance' returns 5
  - No stale token name: grep -c 'sg-duration' guides/reference/admin-quality-ledger.md returns 0
  - Non-target rows are untouched (the 8 Wave-A L1 rows stay 2; L2 rows stay 1; L3 rows stay 2; L4 flow rows stay 1)
  </acceptance_criteria>

  <done>All 6 rows (token-layer L0 + 5 L1) show bare 2 with rich evidence strings; no decorators; non-target rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run monotonic guard to confirm no regressions and full L0/L1 Tier-2</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — re-read after edits to confirm all 6 rows are correct (bare 2, rich evidence) and no row was accidentally decorated before running the guard
  - scripts/ci/quality-ledger-monotonic.sh — confirm --base origin/main usage and that it parses column 4 with awk -F'|' and /^[012]$/; the PASS line reports a cell count
  </read_first>

  <action>
  Run the monotonic guard against origin/main to confirm:
  1. No tier values decreased vs main (the 6 rows moved 1 → 2, an allowed increase).
  2. The entire L0/L1 column now reads bare 2 — all 13 L1 component cells (8 Wave-A + 5 Wave-B) and the L0 token-layer cell.

  Command: bash scripts/ci/quality-ledger-monotonic.sh --base origin/main
  Expected: "quality-ledger-monotonic: PASS (N cells checked vs origin/main)".

  If the guard exits non-zero or the cell count is lower than expected, diagnose: a decorated tier value (2*, 2 ✓) makes a row invisible to the awk parse — find and replace with bare 2; or a row was accidentally decreased. Fix and re-run.

  Also run the guard self-test to confirm the guard itself is still green:
  bash scripts/ci/quality-ledger-monotonic.test.sh

  Confirm the full L0/L1 Tier-2 claim: grep the ledger for the L0 + 13 L1 rows and assert every tier cell is 2.
  </action>

  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && bash scripts/ci/quality-ledger-monotonic.test.sh</automated>
  </verify>

  <acceptance_criteria>
  - bash scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 and emits a PASS line
  - bash scripts/ci/quality-ledger-monotonic.test.sh exits 0 (guard self-test still green)
  - The PASS cell count is >= the baseline 36 (no bare-2 row became invisible to the awk parse via decorator corruption)
  - Every L0 token-layer + L1 component row reads bare 2: grep -E '^\| [a-z].* \| L[01] \|' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -vxc 2 returns 0 (no L0/L1 row at a tier other than 2)
  - git diff --stat shows only guides/reference/admin-quality-ledger.md modified in this plan
  </acceptance_criteria>

  <done>Monotonic guard exits 0 vs origin/main; all 13 L1 cells + the L0 cell read bare 2; guard self-test green; no other files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger edit → awk parse | A malformed tier column (e.g. "2*") silently removes the row from monotonic-guard protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-09 | Tampering | Ledger tier column format | high | mitigate | Acceptance criteria verify no decorated values exist (grep -vE '^[012]$' returns empty) and the guard cell count is >= baseline 36; any invisible row is caught |
| T-207-10 | Repudiation | Evidence string accuracy | low | accept | Evidence strings are human-curated, bounded by Plan 02 audit findings + Plan 01/03 guard outputs; the cited guards are independently runnable |
</threat_model>

<verification>
After both tasks complete:
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main   (exits 0)
  bash scripts/ci/quality-ledger-monotonic.test.sh                 (exits 0)
  grep -E '^\| (token-layer|empty_state|page_back|scope_ribbon|field_help|skeleton) ' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -xc 2   (returns 6)
  grep -c 'sg-duration' guides/reference/admin-quality-ledger.md   (returns 0)
</verification>

<success_criteria>
- 6 rows (token-layer L0 + empty_state, page_back, scope_ribbon, field_help, skeleton) at bare tier 2 with rich evidence
- No decorated tier values anywhere in the ledger
- Monotonic guard exits 0 vs origin/main; cell count >= baseline 36
- Entire L0/L1 column at Tier-2 (all 13 L1 cells + L0 cell == 2)
- Guard self-test green
</success_criteria>

<output>
Create .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-04-SUMMARY.md when done
</output>