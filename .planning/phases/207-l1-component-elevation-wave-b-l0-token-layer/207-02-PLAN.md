---
phase: "207"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
autonomous: true
requirements:
  - COMP-02

must_haves:
  truths:
    - "D-02: All 5 L1 components (empty_state, page_back, scope_ribbon, field_help, skeleton) have their interaction-state CSS, target-size, motion/reduced-motion, and light/dark token correctness audited and documented in the SUMMARY against the three Tier-2 L1 proxies — per the Phase 206 method (audit → cite gate → narrow-gap-fix)"
    - "D-04/D-05: The audit confirms — and the SUMMARY records — that all interaction-state CSS already exists (page_back inherits hover/focus-visible/active/disabled from base .sg-btn; field_help trigger has explicit hover/active/focus-visible; empty_state and scope_ribbon are static; skeleton shimmer is stripped by the global reduced-motion block), so the EXPECTED outcome is ZERO genuine CSS gaps and NO CSS edit"
    - "D-03: IF (and only if) the audit surfaces a genuine gap, the narrow fix lands in priv/templates/sigra.install/admin/sigra_admin.css ONLY, and the two generated copies (test/example/priv/static/assets/sigra_admin.css and test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css) are re-synced byte-for-byte so all 3 copies share an identical md5"
    - "D-05: No per-component @media (prefers-reduced-motion) block is added — the global block at sigra_admin.css:~1467 covers every transition/animation including the skeleton shimmer; the SUMMARY notes this as confirmed sufficient"
  artifacts:
    - priv/templates/sigra.install/admin/sigra_admin.css
  prohibitions:
    - statement: "D-01: Phase 207 touches only the 5 isolated board-* component boards + the L0 token layer; it does NOT make any page-composition / cross-page-placement edits — specifically NO scope_ribbon placement-across-pages, NO applied-chip DOM repositioning, NO More-filters work, NO quick-toggle Apply behavior, NO 'Effective user' filter work — all of which are Phase 209 page-recompose scope, not Phase 207"
      status: enforced
      verification: "git diff (changed-files scope-fence): the only files this plan may touch are priv/templates/sigra.install/admin/sigra_admin.css, its two generated copies (test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css), the 5 component defs, the CI guard scripts, the ledger, and admin-token-reference.md — the scope-fence grep returns NO Phase-209 page-composition tokens (page-recompose, cross-page placement, applied-chip reorder, More-filters, quick-toggle Apply, Effective-user filter) in any changed file"
  key_links:
    - "Per-component audit findings (interaction-state present/N-A, target-size CSS px, light/dark token correctness) are recorded in SUMMARY as the source material for Plan 04 ledger evidence strings"
    - "Whether a CSS edit landed (expected: none) gates Plan 03's recapture: zero edit → recapture is a no-op compare-mode verification; an edit → recapture the affected board-* slugs"
    - "admin-css-conformance.sh + admin-token-completeness.sh (Plan 01) must still exit 0 after this plan — any CSS edit must keep both guards green"
---

<objective>
Audit the 5 remaining L1 components — empty_state, page_back, scope_ribbon, field_help,
skeleton — against the three Tier-2 L1 proxies (interaction-state CSS, target-size, light/dark
token correctness) plus motion/reduced-motion conformance, on their isolated board-* gallery
boards. Cite the existing axe+screenshot gate (admin-design.spec.ts) and the motion/hex guard
(admin-css-conformance.sh). Land a narrow CSS fix ONLY if the audit surfaces a genuine gap;
the research-verified expected outcome is ZERO gaps and NO CSS edit.

Purpose: This is the audit-and-(maybe)-fix step of the audit → cite → narrow-gap-fix →
ledger-flip → recapture method (D-02). The component defs and sg-* CSS are already well-formed;
the audit genuinely re-verifies each component and documents what exists for Plan 04's evidence
strings. The phase is cite-and-flip, not build.

Output: A SUMMARY with a per-component audit section; an updated sigra_admin.css ONLY if a real
gap is found (with byte-coherent generated copies); a clear "CSS edited: yes/no" signal for
Plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-CONTEXT.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-RESEARCH.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-UI-SPEC.md
@lib/sigra/admin/components.ex
@priv/templates/sigra.install/admin/sigra_admin.css
@guides/reference/admin-design-contract.md
@guides/reference/admin-ui-principles.md
</context>

## Artifacts this phase produces

- (conditional) a narrow CSS edit in `priv/templates/sigra.install/admin/sigra_admin.css` — ONLY if the audit surfaces a genuine gap (expected: none); re-synced byte-coherently to the 2 generated copies
- a per-component audit section in the SUMMARY (5 components × interaction-state / target-size / motion-reduced-motion / light-dark) — the source material for Plan 04 ledger evidence
- a "CSS edited: yes/no" signal in the SUMMARY that gates Plan 03's recapture

<tasks>

<task type="auto">
  <name>Task 1: Audit 5 L1 components — interaction-state, target-size, motion, light/dark</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css</files>

  <read_first>
  - lib/sigra/admin/components.ex — read the 5 component function defs: empty_state (~:410), page_back (~:438), scope_ribbon (~:465), field_help (~:581-632), skeleton (~:655-659)
  - priv/templates/sigra.install/admin/sigra_admin.css — read the sg-* CSS blocks: empty_state .sg-empty-state (~:546-550) + __title (~:1086-1096); page_back base .sg-btn (~:447-462) + .sg-btn--ghost hover (~:486-493) + .sg-btn--sm sizing (~:509-510); scope_ribbon .sg-scope-ribbon (~:565-568); field_help __trigger hover/active/focus-visible (~:894-903) + ::before hit-area (~:889-892) + __panel (~:921-932); skeleton .sg-skeleton + ::after shimmer (~:1401-1425); the global @media (prefers-reduced-motion: reduce) block (~:1467-1484); both :root blocks (light ~:20, dark ~:176)
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-RESEARCH.md "Per-Component Audit" section — the HIGH-confidence per-component findings (verified line-by-line): zero genuine CSS gaps; cite the exact target-size values it documents (page_back sg-btn--sm = 36px; field_help expanded ::before hit-area ~40×40px; empty_state/scope_ribbon/skeleton N/A)
  - test/example/priv/playwright/tests/admin-design.spec.ts — COMPONENT_BOARDS (~:98-102) lists board-empty_state, board-page_back, board-scope_ribbon, board-field_help, board-skeleton; field_help Escape/focus-restore (~:695-711); skeleton reduced-motion assertion (~:639-677)
  - guides/reference/admin-fractal-scorecard.md — the L1 add-on proxy vocabulary (interaction-state, target-size, motion-token conformance)
  </read_first>

  <action>
  Perform a code-level audit of all 5 L1 components. For each, document in the SUMMARY (not inline in this PLAN) under a "Per-Component Audit" section:

  1. INTERACTION-STATE CSS — which states are CSS-addressable and present?
     - empty_state: static display container — N/A (no hover/focus/active/disabled).
     - page_back: anchor button (sg-btn sg-btn--ghost sg-btn--sm). Confirm it inherits from base .sg-btn: hover (.sg-btn--ghost:hover ~:490-493), focus-visible (.sg-btn:focus-visible ~:449-452 box-shadow var(--sg-focus-ring)), active (.sg-btn:active ~:453-456 transform scale(0.96)), disabled (~:457-462 opacity/pointer-events). Record "present (inherited from base .sg-btn)".
     - scope_ribbon: decorative inline span — N/A (spec asserts no [href]/[role=link]/[tabindex]).
     - field_help: trigger button with tooltip. Confirm hover/[aria-expanded=true] (~:894-896), active scale(0.96) (~:898-900), focus-visible box-shadow (~:901-903). Record "present".
     - skeleton: visual placeholder — N/A (no interactive states; aria-busy lives on the containing section).
     Record present/gap/N-A per state per component.

  2. TARGET-SIZE — minimum rendered CSS dimensions of any interactive sub-target:
     - page_back: sg-btn--sm min-height var(--sg-control-sm) = 2.25rem = 36px (cite 36px; acceptable per 206 D-08 dense-admin precedent).
     - field_help: visible trigger 1.125rem (18px) BUT ::before inset -0.6875rem expands the hit area to ~40×40 CSS px — cite the 40px expanded hit-area, not 18px.
     - empty_state / scope_ribbon / skeleton: N/A (not interactive).

  3. MOTION + REDUCED-MOTION:
     - Confirm zero "transition: all" in the file (grep).
     - skeleton ::after uses animation: ... infinite (~:1408-1420) — confirm it is stripped by the global block at ~:1467-1477 (animation-iteration-count: 1 !important covers *::after); cite the passing reduced-motion assertion at admin-design.spec.ts ~:639-677.
     - Do NOT add per-component @media (prefers-reduced-motion) blocks — the global block is sufficient (D-05). Note this in SUMMARY.

  4. LIGHT/DARK TOKEN CORRECTNESS — confirm each component's sg-* block uses var(--sg-color-*)/var(--sg-*) tokens only, zero raw hex (empty_state, page_back, scope_ribbon, field_help, skeleton all PASS per research).

  VERDICT: record the expected "ZERO genuine CSS gaps — cite-and-flip, no CSS edit" verdict, OR — if the audit genuinely surfaces a real gap not anticipated by research — describe the single narrow fix needed (Task 2 will apply it). Record "CSS edited: no" (expected) or "CSS edited: yes — <slug(s)>" in the SUMMARY for Plan 03.
  </action>

  <verify>
    <automated>bash -c '[ "$(grep -v "^#" priv/templates/sigra.install/admin/sigra_admin.css | grep -c "transition: all")" = "0" ] && grep -c "prefers-reduced-motion" priv/templates/sigra.install/admin/sigra_admin.css | grep -qE "^[1-9]" && echo OK'</automated>
  </verify>

  <acceptance_criteria>
  - SUMMARY contains a "Per-Component Audit" section covering all 5 components × 4 axes (interaction-state / target-size / motion-reduced-motion / light-dark)
  - Audit confirms zero "transition: all" (filtered): grep -v '^#' priv/templates/sigra.install/admin/sigra_admin.css | grep -c 'transition: all' returns 0
  - Audit confirms the global reduced-motion block is present: grep -c 'prefers-reduced-motion' priv/templates/sigra.install/admin/sigra_admin.css returns >= 1
  - The SUMMARY records an explicit verdict line: "CSS edited: no" (expected) or "CSS edited: yes — <board-slug(s)>"
  - The SUMMARY cites the documented target-size values: page_back 36px (sg-btn--sm), field_help ~40×40px expanded ::before hit-area
  </acceptance_criteria>

  <done>All 5 components audited across 4 axes; findings documented in SUMMARY; explicit "CSS edited: yes/no" verdict recorded for Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Apply narrow gap fix ONLY if found, and re-sync byte-coherence</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</files>

  <read_first>
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-SUMMARY.md (the in-progress SUMMARY from Task 1) — read the verdict; if "CSS edited: no", this task is a no-op verification only
  - priv/templates/sigra.install/admin/sigra_admin.css — the edit target IF a gap was found (Task 1 names the exact rule + line)
  - test/example/priv/static/assets/sigra_admin.css — generated copy 1; currently byte-identical to source (md5 7e60bc4c302d496d98f270ccba7d1766)
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css — generated copy 2; same md5
  - CLAUDE.md "Local development prerequisites" — the golden tests require phx_new 1.8.7 pinned (do NOT regenerate the fixture against a newer archive)
  </read_first>

  <action>
  IF Task 1's verdict is "CSS edited: no" (the research-verified expected outcome):
    No CSS edit. Verify the 3 copies remain byte-coherent (identical md5) and that both Plan-01 guards still exit 0. Record "no-op — zero gaps confirmed" in the SUMMARY. STOP.

  IF Task 1 surfaced a genuine gap requiring a narrow fix:
    EDIT the source ONLY: priv/templates/sigra.install/admin/sigra_admin.css — apply the single narrow fix Task 1 described (e.g. a missing focus-visible/active rule, or a raw-value→token swap). Use Edit (scoped replacement), never Write the whole file.

    PROPAGATE the identical character-for-character change to BOTH generated copies:
      - test/example/priv/static/assets/sigra_admin.css
      - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    Apply via Edit (scoped) to each — match the same surrounding context and apply the same replacement.

    Do NOT add per-component @media (prefers-reduced-motion) blocks (D-05). Do NOT introduce raw hex/px.

  VERIFY byte-coherence (always, both branches):
    - all 3 sigra_admin.css copies share one md5
    - bash scripts/ci/admin-css-conformance.sh exits 0
    - bash scripts/ci/admin-token-completeness.sh exits 0 (unaffected by component CSS, must stay 0)
    - mix test test/sigra/admin/components_test.exs exits 0 (component golden suite)

  Record the final verdict and (if edited) the affected board-* PNG slugs (hyphenated: board-empty-state, board-page-back, board-scope-ribbon, board-field-help, board-skeleton) in the SUMMARY for Plan 03's recapture.
  </action>

  <verify>
    <automated>bash -c 'set -e; a=$(md5 -q priv/templates/sigra.install/admin/sigra_admin.css); b=$(md5 -q test/example/priv/static/assets/sigra_admin.css); c=$(md5 -q test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css); [ "$a" = "$b" ] && [ "$b" = "$c" ] && echo "byte-coherent: $a"'</automated>
  </verify>

  <acceptance_criteria>
  - All 3 sigra_admin.css copies share an identical md5 (byte-coherent): the verify command prints "byte-coherent: <hash>"
  - bash scripts/ci/admin-css-conformance.sh exits 0 (no transition:all, no raw hex outside :root, +px if CHECK 3 landed)
  - bash scripts/ci/admin-token-completeness.sh exits 0 (100/100 token set unaffected)
  - mix test test/sigra/admin/components_test.exs exits 0 (golden byte-match preserved)
  - SUMMARY records the final "CSS edited: yes/no" verdict and, if yes, the exact affected board-slug list (hyphenated) for Plan 03
  </acceptance_criteria>

  <done>Either no CSS edit (3 copies byte-coherent, both guards green) or the single narrow fix is applied to source + re-synced to both generated copies byte-coherently; component golden suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template source → generated copies | Edits to source must be mirrored to the 2 generated copies or template drift breaks golden/install/Playwright tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-04 | Information Disclosure | Generated-copy drift | medium | mitigate | Any source edit is propagated to all 3 copies immediately; verified by md5 equality + components_test.exs golden byte-match (D-03) |
| T-207-05 | Tampering | Token/value swap on a component | low | accept | Audit-confirmed expected zero edits; any fix is a read-only semantic mapping re-verified by both conformance guards before close |
</threat_model>

<verification>
After both tasks complete:
  md5 of all 3 sigra_admin.css copies are identical
  bash scripts/ci/admin-css-conformance.sh                         (exits 0)
  bash scripts/ci/admin-token-completeness.sh                      (exits 0)
  mix test test/sigra/admin/components_test.exs                    (exits 0)
  grep -v '^#' priv/templates/sigra.install/admin/sigra_admin.css | grep -c 'transition: all'   (returns 0)
</verification>

<success_criteria>
- All 5 L1 components audited across 4 axes; findings in SUMMARY
- Expected outcome: zero CSS gaps, no edit (cite-and-flip) — OR a single narrow fix, byte-coherent across 3 copies
- No per-component @media (prefers-reduced-motion) blocks added (D-05 honored)
- Both Plan-01 guards exit 0; component golden suite exits 0
- "CSS edited: yes/no" verdict + (if yes) affected slug list recorded for Plan 03
</success_criteria>

<output>
Create .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-SUMMARY.md when done
</output>