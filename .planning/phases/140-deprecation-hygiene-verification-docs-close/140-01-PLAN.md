---
phase: 140-deprecation-hygiene-verification-docs-close
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/mfa/trust.ex
  - lib/sigra/account.ex
autonomous: true
requirements:
  - DEPR-01
  - DEPR-02
must_haves:
  truths:
    - "The @deprecated string for cookie_opts/0 (trust.ex:44) ends with ' Scheduled for removal in 0.4.0.' (D-02, D-04)"
    - "The @doc deprecated: string for cookie_opts/0 (trust.ex:43) ends with ' Scheduled for removal in 0.4.0.' (D-02, D-04)"
    - "The @deprecated string for audit_forced_password_change/2 (account.ex:543) ends with ' Scheduled for removal in 0.5.0.' (D-03, D-04)"
    - "No existing migration guidance is weakened or removed — only the removal-target phrase is appended (D-04)"
    - "Removal targets use Hex SemVer 0.x minors, not v1.x planning labels (D-01)"
  artifacts:
    - path: "lib/sigra/mfa/trust.ex"
      provides: "DEPR-02 annotation with removal target 0.4.0"
      contains: "Scheduled for removal in 0.4.0."
    - path: "lib/sigra/account.ex"
      provides: "DEPR-01 annotation with removal target 0.5.0"
      contains: "Scheduled for removal in 0.5.0."
  key_links:
    - from: "lib/sigra/mfa/trust.ex @deprecated/@doc deprecated:"
      to: "ExDoc-rendered deprecation badge"
      via: "mix docs"
      pattern: "Scheduled for removal in 0.4.0"
    - from: "lib/sigra/account.ex @deprecated"
      to: "ExDoc-rendered deprecation badge"
      via: "mix docs"
      pattern: "Scheduled for removal in 0.5.0"
---

<objective>
Append removal-target version strings to the deprecation annotations for the two live @deprecated public functions, closing the open-ended deprecation notices with concrete Hex SemVer 0.x removal targets.

Purpose: DEPR-01/DEPR-02 require that both deprecated functions carry real removal timelines. This phase closes open-ended @deprecated strings — adopters currently get a migration note with no timeline commitment. The appends make the timeline actionable without changing any runtime behavior.

Output: Two source-file edits (docstring-only, additive appends):
- lib/sigra/mfa/trust.ex:43-44 — both @doc deprecated: and @deprecated for cookie_opts/0 updated with "Scheduled for removal in 0.4.0." (D-02)
- lib/sigra/account.ex:543 — @deprecated for audit_forced_password_change/2 updated with "Scheduled for removal in 0.5.0." (D-03)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140-deprecation-hygiene-verification-docs-close/140-CONTEXT.md
@.planning/phases/140-deprecation-hygiene-verification-docs-close/140-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append removal target 0.4.0 to cookie_opts/0 deprecation annotations (DEPR-02, D-02)</name>
  <files>lib/sigra/mfa/trust.ex</files>

  <read_first>
    Read lib/sigra/mfa/trust.ex lines 42-57 to see the current annotation text before editing.
    Also read .planning/phases/140-deprecation-hygiene-verification-docs-close/140-RESEARCH.md
    section "DEPR-02: Sigra.MFA.Trust.cookie_opts/0" to confirm the exact current and target strings.
  </read_first>

  <action>
    Edit lib/sigra/mfa/trust.ex at lines 43-44 (the @doc deprecated: and @deprecated attributes
    for cookie_opts/0). Two attributes must both be updated — per D-02 and D-04, both strings must
    be kept in sync.

    Current text at line 43:
      @doc deprecated: "Use cookie_opts/1 with a %Sigra.Config{} so cookie_domain is honored."

    Replace with:
      @doc deprecated: "Use cookie_opts/1 with a %Sigra.Config{} so cookie_domain is honored. Scheduled for removal in 0.4.0."

    Current text at line 44:
      @deprecated "Use cookie_opts/1 with a %Sigra.Config{} so cookie_domain is honored."

    Replace with:
      @deprecated "Use cookie_opts/1 with a %Sigra.Config{} so cookie_domain is honored. Scheduled for removal in 0.4.0."

    Do NOT modify @doc since: "0.6.0" at line 42.
    Do NOT modify the @doc narrative block above lines 43-44 (the multi-line @doc """ ... """ with migration context).
    Do NOT change the function body — it is a no_return() raising stub; its runtime behavior is unchanged.
    The removal target is "0.4.0" (Hex SemVer 0.x minor) — never "v1.x" labels per D-01.
    Append to the END of each existing string; do not replace the migration guidance.
  </action>

  <verify>
    <automated>grep "Scheduled for removal in 0.4.0" lib/sigra/mfa/trust.ex</automated>
  </verify>

  <acceptance_criteria>
    - grep "Scheduled for removal in 0.4.0" lib/sigra/mfa/trust.ex returns exactly 2 matches (one for @doc deprecated: and one for @deprecated)
    - The full @deprecated string still begins with "Use cookie_opts/1 with a %Sigra.Config{} so cookie_domain is honored." (migration note not weakened)
    - @doc since: "0.6.0" is unchanged
    - No function body change; file compiles: MIX_ENV=test mix compile --no-deps-check exits 0
  </acceptance_criteria>

  <done>Both @doc deprecated: and @deprecated strings for cookie_opts/0 end with "Scheduled for removal in 0.4.0." and existing migration text is preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Append removal target 0.5.0 to audit_forced_password_change/2 deprecation annotation (DEPR-01, D-03)</name>
  <files>lib/sigra/account.ex</files>

  <read_first>
    Read lib/sigra/account.ex lines 540-556 to see the current annotation text before editing.
    Also read .planning/phases/140-deprecation-hygiene-verification-docs-close/140-RESEARCH.md
    section "DEPR-01: Sigra.Account.audit_forced_password_change/2" to confirm the exact current
    and target strings. Note: this function has ONLY @deprecated — NO @doc deprecated: attribute.
    Do not add @doc deprecated: (would cause duplicate display in ExDoc).
  </read_first>

  <action>
    Edit lib/sigra/account.ex at line 543 (the single @deprecated attribute for
    audit_forced_password_change/2). Per D-03 and D-04, only the @deprecated string is modified.

    Current text at line 543:
      @deprecated "Use clear_password_change_requirement/3 when :audit_schema is configured; do not call this function for the same forced-clear completion or you may duplicate audit rows."

    Replace with:
      @deprecated "Use clear_password_change_requirement/3 when :audit_schema is configured; do not call this function for the same forced-clear completion or you may duplicate audit rows. Scheduled for removal in 0.5.0."

    Do NOT add a @doc deprecated: attribute — this function uses only @deprecated, and adding
    @doc deprecated: would cause duplicate deprecation display in ExDoc.
    Do NOT modify @doc since: "0.9.0" at line 542.
    Do NOT change the function body — the function is still-functional (soft-deprecation only).
    The removal target is "0.5.0" (Hex SemVer 0.x minor; one minor of grace since function still works) per D-01/D-03.
    Append to the END of the existing string; do not replace or abbreviate the migration guidance.
  </action>

  <verify>
    <automated>grep "Scheduled for removal in 0.5.0" lib/sigra/account.ex</automated>
  </verify>

  <acceptance_criteria>
    - grep "Scheduled for removal in 0.5.0" lib/sigra/account.ex returns exactly 1 match (the @deprecated attribute)
    - grep "@doc deprecated:" lib/sigra/account.ex | grep "audit_forced_password_change" returns no match (confirm @doc deprecated: was not added)
    - The full @deprecated string still begins with "Use clear_password_change_requirement/3 when :audit_schema is configured;" (migration note not weakened)
    - @doc since: "0.9.0" is unchanged
    - No function body change; file compiles: MIX_ENV=test mix compile --no-deps-check exits 0
  </acceptance_criteria>

  <done>The single @deprecated string for audit_forced_password_change/2 ends with "Scheduled for removal in 0.5.0." and existing migration text is preserved verbatim. No @doc deprecated: attribute was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file edit | Planner modifies docstrings in lib/ files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01-SC | Tampering | lib/sigra/mfa/trust.ex, lib/sigra/account.ex | accept | No new attack surface: edits are docstring-only string appends to @deprecated annotations. No new inputs, auth paths, data flows, or runtime behavior change. The only risk is accidental mutation of non-target lines — mitigated by read_first requirements and grep acceptance criteria. |

**No new attack surface.** This plan makes docstring-only changes: additive string appends to @deprecated and @doc deprecated: attributes. No new inputs, auth paths, data flows, dependencies, or runtime behavior change. Existing compilation and test gates will catch any unintended modification.
</threat_model>

<verification>
After both tasks complete:

1. Both removal-target greps return matches:
   - grep "Scheduled for removal in 0.4.0" lib/sigra/mfa/trust.ex — expect 2 lines
   - grep "Scheduled for removal in 0.5.0" lib/sigra/account.ex — expect 1 line

2. Compilation is clean:
   MIX_ENV=test mix compile --no-deps-check

3. No @doc deprecated: added to audit_forced_password_change/2:
   grep -n "@doc deprecated:" lib/sigra/account.ex — should not contain the audit_forced line
</verification>

<success_criteria>
- DEPR-02: lib/sigra/mfa/trust.ex contains "Scheduled for removal in 0.4.0." in both @doc deprecated: and @deprecated strings for cookie_opts/0, with original migration guidance intact.
- DEPR-01: lib/sigra/account.ex contains "Scheduled for removal in 0.5.0." in the @deprecated string for audit_forced_password_change/2, with original migration guidance intact. No @doc deprecated: attribute was introduced.
- All removal targets use Hex SemVer 0.x notation per D-01.
- MIX_ENV=test mix compile --no-deps-check exits 0.
</success_criteria>

<output>
Create `.planning/phases/140-deprecation-hygiene-verification-docs-close/140-01-SUMMARY.md` when done.
</output>
