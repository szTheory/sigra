---
phase: 140-deprecation-hygiene-verification-docs-close
plan: "03"
type: execute
wave: 2
depends_on:
  - "140-01"
  - "140-02"
files_modified:
  - .planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md
autonomous: true
requirements:
  - PROOF-01
  - DEPR-01
  - DEPR-02
must_haves:
  truths:
    - "All 8 proof gates run and results recorded verbatim in 140-VERIFICATION.md (D-05/D-06)"
    - "Gate 1 (full mix test) exits 0 with 0 failures (PROOF-01)"
    - "Gate 2 (audit subtree) exits 0 with 0 failures (PROOF-01)"
    - "Gate 3 (dep-off lane: unlock/clean/compile/test/restore) exits 0 with 6 excluded, 0 failures, and mix.lock is restored (PROOF-01)"
    - "Gate 4 (test/example/ lane) exits 0 with ~236 tests, 0 failures (PROOF-01)"
    - "Gate 5 (mix docs --warnings-as-errors) exits 0 (PROOF-01)"
    - "Gate 6 (mix credo --only sigra enforced checks) exits 0; credo --strict advisory count recorded verbatim as non-blocking (D-08)"
    - "Gate 7 (mix sigra.doctor from test/example/ directory) exit code recorded verbatim — expected 0 (D-06)"
    - "Gate 8 (grep -r 'Scheduled for removal in 0.4.0' doc/ AND grep -r 'Scheduled for removal in 0.5.0' doc/) both return matches, proving DEPR notes render in published docs (D-07)"
    - "140-VERIFICATION.md filed with canonical YAML frontmatter (phase / verified / status / score / overrides_applied) per D-05"
    - "Actual output recorded verbatim — no assumed-green results (anti-overclaim)"
  artifacts:
    - path: ".planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md"
      provides: "Eight-gate proof bundle with verbatim results"
      contains: "phase: 140"
  key_links:
    - from: "140-VERIFICATION.md status field"
      to: "PROOF-01 requirement"
      via: "all 8 gates PASS"
      pattern: "status: passed"
    - from: "Gate 8 grep doc/"
      to: "DEPR-01 / DEPR-02 published proof"
      via: "mix docs output + grep"
      pattern: "Scheduled for removal in 0.4.0|0.5.0"
---

<objective>
Execute all 8 proof gates against the repo HEAD (with Plan 01 DEPR edits and Plan 02 docs sections already in place), record actual output verbatim, and file 140-VERIFICATION.md using the canonical Phase 136 proof-bundle pattern.

Purpose: PROOF-01 requires the full test suite, dep-off CI lane, mix docs, mix sigra.doctor, and the D-07 docs-render proof to all be green. This plan closes PROOF-01 and also provides the docs-render evidence that DEPR-01/DEPR-02 are not just in source but actually published in the generated docs. This plan MUST run in Wave 2 — after Plans 01 and 02 have landed — because Gates 5, 7, and 8 depend on the deprecation string appends being committed and the docs sections being present.

Output: .planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md with eight-gate results recorded verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140-deprecation-hygiene-verification-docs-close/140-CONTEXT.md
@.planning/phases/140-deprecation-hygiene-verification-docs-close/140-RESEARCH.md
@.planning/phases/136-verification-proof-bundle-narrative-honesty-corrigendum/136-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gates 1-5 and record results verbatim</name>
  <files>.planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md</files>

  <read_first>
    Read .planning/phases/136-verification-proof-bundle-narrative-honesty-corrigendum/136-VERIFICATION.md
    fully to internalize the exact format: YAML frontmatter, Result section, Goal Achievement table,
    Behavioral Spot-Checks table, Requirements Coverage, Anti-Overclaim Scan, and Gaps Summary.
    Read .planning/phases/140-deprecation-hygiene-verification-docs-close/140-RESEARCH.md sections
    "Proof Gate Commands" (Gates 1-5) for the exact command strings with PGUSER/PGPASSWORD/PGHOST
    prefixes. Confirm Plans 01 and 02 have been executed (grep "Scheduled for removal in 0.4.0"
    lib/sigra/mfa/trust.ex and grep "## Operator diagnostics" guides/recipes/deployment.md before
    running docs gates).
  </read_first>

  <action>
    Run gates 1-5 sequentially and collect actual output for filing in 140-VERIFICATION.md.
    Use exact command strings from 140-RESEARCH.md "Proof Gate Commands" section. Do NOT assume
    green — run each gate and record actual test counts, failure counts, and exit codes.

    Gate 1: Full library suite
      PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test
      Record: test count, doctest count, property count, failure count, elapsed time, exit code.
      Expected: 0 failures, exit 0.

    Gate 2: Audit subtree
      PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit/
      Record: test count, failure count, elapsed time, exit code.
      Expected: 60 tests (or current count), 0 failures, exit 0.

    Gate 3: Dep-off lane (three-step; MUST restore mix.lock afterward)
      Step 3a: mix deps.unlock threadline
      Step 3b: mix deps.clean threadline --build
      Step 3c: MIX_ENV=test mix compile --warnings-as-errors --no-deps-check
      Step 3d: PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test --exclude requires_threadline --no-deps-check
      Step 3e: mix deps.get    (RESTORE — mandatory; do not leave tree stripped)
      Record: compile exit code, test count, failure count, excluded count (expect 6), elapsed, exit code.
      Expected: compile exit 0, test exit 0, 6 excluded, 0 failures.

    Gate 4: test/example/ lane
      cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test --include example_app
      (Then cd back to repo root)
      Record: test count, failure count, elapsed, exit code.
      Expected: ~236 tests, 0 failures, exit 0.

    Gate 5: Docs build
      mix docs --warnings-as-errors
      Record: exit code, key output lines (files compiled, "View html docs", "View markdown docs").
      Expected: exit 0.
      NOTE: Run Gate 5 BEFORE Gate 8. Gate 8 (grep doc/) asserts against the doc/ tree produced
      by this run, which must reflect the DEPR edits from Plan 01.

    After running all five gates, begin drafting 140-VERIFICATION.md using the 136-VERIFICATION.md
    format as the verbatim template. Populate the YAML frontmatter and the Result section with
    actual findings. Leave Gate 6, 7, 8 rows as placeholders until Task 2 completes them.
    Do not record expected output in place of actual — record what each gate actually returned.
  </action>

  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit/</automated>
  </verify>

  <acceptance_criteria>
    - Gate 1 exits 0 with 0 failures (actual count recorded)
    - Gate 2 exits 0 with 0 failures
    - Gate 3 compile step exits 0 with no warnings; test step exits 0 with 6 excluded, 0 failures; mix.lock restored via mix deps.get
    - Gate 4 exits 0 with 0 failures from test/example/ directory
    - Gate 5 exits 0
    - 140-VERIFICATION.md draft exists in .planning/phases/140-deprecation-hygiene-verification-docs-close/ with YAML frontmatter and Gate 1-5 results filled in
    - Actual output recorded (test counts, elapsed times) — not placeholder text
  </acceptance_criteria>

  <done>Gates 1-5 all passed (exit 0, 0 failures) and results recorded with actual counts in the draft 140-VERIFICATION.md. mix.lock is restored after Gate 3. Gate 5 generated a fresh doc/ tree containing the DEPR-01/02 string appends from Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Run gates 6-8, complete 140-VERIFICATION.md, and file it</name>
  <files>.planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md</files>

  <read_first>
    Read the draft 140-VERIFICATION.md created in Task 1 to understand the current state before
    appending Gate 6-8 results.
    Read .planning/phases/140-deprecation-hygiene-verification-docs-close/140-RESEARCH.md sections
    "Gate 6: Credo advisory" (lines ~182-190), "Gate 7: mix sigra.doctor" (lines ~192-205),
    and "Gate 8: Deprecation notes render" (lines ~207-213) for exact command strings and
    expected behaviors.
  </read_first>

  <action>
    Run gates 6, 7, and 8. Record actual output verbatim. Then complete and finalize 140-VERIFICATION.md.

    Gate 6a: Credo advisory (non-blocking per D-08)
      mix credo --strict
      Record: exit code, total files, issue counts by category, Sigra library code issue count.
      This gate is NON-BLOCKING — record the advisory count verbatim; do not gate PROOF-01 on it.

    Gate 6b: Enforced custom checks (BLOCKING — must exit 0)
      mix credo --only sigra
      Record: exit code (must be 0).

    Gate 7: mix sigra.doctor against test/example/ (D-06)
      cd test/example && mix sigra.doctor
      (Then cd back to repo root)
      Record: exit code verbatim (expected 0). Record any printed output lines that indicate
      the feature-matrix state. If exit code is 1 (unexpected), record the non-zero exit and
      the printed output verbatim; note it as a finding rather than blocking the proof bundle
      (the misconfiguration would be a pre-existing test/example/ wiring issue, not caused by
      this phase's edits).
      CRITICAL: Run from test/example/ directory so the task picks up example app config, not
      the library's dev config. Matches Gate 4 pattern verbatim.

    Gate 8: Deprecation notes render in mix docs output (D-07)
      (doc/ tree already generated by Gate 5 in Task 1; do NOT re-run mix docs)
      grep -r "Scheduled for removal in 0.4.0" doc/
      grep -r "Scheduled for removal in 0.5.0" doc/
      Both greps must return at least one match. Record the matched file path and line verbatim.
      This proves DEPR-01/DEPR-02 notes are published in generated docs, not just in source.

    After all eight gates have results:

    Complete 140-VERIFICATION.md following the exact structure of 136-VERIFICATION.md:
    - YAML frontmatter: phase, verified (ISO 8601 timestamp), status (passed/failed),
      score ("8/8 must-haves verified" if all pass), overrides_applied (0 if no waivers),
      gaps [], deferred [], human_verification []
    - Result narrative (2-3 sentences describing all eight gates)
    - Goal Achievement table: 8 rows, one per gate, with Truth/Status/Evidence columns
    - Behavioral Spot-Checks table: one row per gate step with Command/Result/Status
    - Requirements Coverage table: DEPR-01, DEPR-02, PROOF-01, DOC-01 rows
    - Anti-Overclaim Scan section: list any advisory disclosures, no false-green claims
    - Gaps Summary section

    Score: count only PASS gates (not advisory gates) in the numerator.
    Credo --strict is recorded as ADVISORY regardless of exit code — consistent with D-08 and
    prior milestone-close phases. Gate 6b (--only sigra) IS a hard gate (must exit 0).

    Do not record "expected" output in place of actual output — record what the commands returned.
    Do not claim the milestone archive is done in this file — that is /gsd-complete-milestone.
  </action>

  <verify>
    <automated>grep -r "Scheduled for removal in 0.4.0" doc/ && grep -r "Scheduled for removal in 0.5.0" doc/</automated>
  </verify>

  <acceptance_criteria>
    - grep -r "Scheduled for removal in 0.4.0" doc/ returns at least 1 match (Gate 8 DEPR-02 docs render)
    - grep -r "Scheduled for removal in 0.5.0" doc/ returns at least 1 match (Gate 8 DEPR-01 docs render)
    - mix credo --only sigra exits 0 (Gate 6b hard gate)
    - cd test/example && mix sigra.doctor exit code recorded verbatim (Gate 7)
    - .planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md exists and is not empty
    - VERIFICATION.md contains valid YAML frontmatter with "phase: 140-deprecation-hygiene-verification-docs-close"
    - VERIFICATION.md contains a Behavioral Spot-Checks table with at least 8 gate rows
    - VERIFICATION.md contains a Requirements Coverage table with DEPR-01, DEPR-02, PROOF-01, DOC-01 rows
    - VERIFICATION.md Anti-Overclaim Scan section explicitly discloses credo advisory status (library NOT credo-clean unless it genuinely is)
    - No row in VERIFICATION.md shows assumed output — every row has actual measured results
  </acceptance_criteria>

  <done>140-VERIFICATION.md is filed with all 8 gates recorded verbatim. Gate 8 grep proves DEPR notes render in published docs. Gate 6b (--only sigra) exits 0. Gate 7 (doctor) exit code recorded. PROOF-01 is satisfied. DOC-01 coverage row states all four content areas are present. Anti-Overclaim Scan discloses credo advisory status honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test execution environment | Gates run mix test against live Postgres at localhost:5432 |
| dep-off lane | Temporarily mutates mix.lock (Threadline removed); must be restored |
| doc/ tree | Generated by mix docs; grep assertions read from it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-03-01 | Tampering | mix.lock (Gate 3 dep-off lane) | mitigate | Gate 3e (mix deps.get) is a mandatory restore step; task action and acceptance criteria both enforce it; the dep graph must NOT be left stripped |
| T-140-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all dependencies already in mix.lock |

**No new attack surface.** This plan is read-only verification: it runs existing test suites, an existing Mix task (mix docs), and an existing doctor command. The only mutation is filing 140-VERIFICATION.md (a new planning artifact) and temporarily modifying mix.lock during Gate 3 (immediately restored). No new inputs, auth paths, data flows, or library code changes.
</threat_model>

<verification>
After both tasks complete:

1. 140-VERIFICATION.md exists and has YAML frontmatter:
   grep "phase: 140" .planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md

2. Gate 8 docs-render proof (DEPR-01/DEPR-02 in published docs):
   grep -r "Scheduled for removal in 0.4.0" doc/
   grep -r "Scheduled for removal in 0.5.0" doc/

3. All hard gates passed (read from VERIFICATION.md status: passed)

4. mix.lock not stripped (Threadline restored):
   grep "threadline" mix.lock
</verification>

<success_criteria>
- PROOF-01: 140-VERIFICATION.md filed with eight gates — full suite (Gate 1), audit subtree (Gate 2), dep-off lane (Gate 3), test/example/ (Gate 4), mix docs (Gate 5), credo advisory (Gate 6a recorded, Gate 6b exit 0), mix sigra.doctor from test/example/ (Gate 7, exit recorded verbatim), and docs-render grep (Gate 8 both removal-version strings found in doc/).
- DEPR-01/DEPR-02 docs-render proof: Gate 8 confirms "Scheduled for removal in 0.4.0" and "Scheduled for removal in 0.5.0" both appear in the generated doc/ tree.
- Anti-overclaim: actual test counts and elapsed times recorded; credo advisory status honestly disclosed.
- mix.lock is in the same state as before Gate 3 (Threadline restored).
- 140-VERIFICATION.md status: passed (all hard gates green).
</success_criteria>

<output>
Create `.planning/phases/140-deprecation-hygiene-verification-docs-close/140-03-SUMMARY.md` when done.
</output>
