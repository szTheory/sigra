---
phase: 140-deprecation-hygiene-verification-docs-close
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/deployment.md
  - MAINTAINING.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "guides/recipes/deployment.md has a new '## Operator diagnostics' section documenting mix sigra.doctor usage (D-09)"
    - "MAINTAINING.md has three new ## sections: OptionalDeps SOT, recipe-contract fixture, and deprecation removal timeline (D-09)"
    - "The OptionalDeps maintainer note scopes its 'single source of truth' claim to runtime guards, documenting the narrow documented exceptions (D-11 / code_context)"
    - "The recipe-contract note documents test/sigra/recipes/companion_lib_contract_test.exs as maintainer-internal, NOT a Hex-facing recipe (D-11)"
    - "The deprecation-removal-timeline note lists both functions with their 0.4.0 and 0.5.0 removal targets (D-04)"
    - "ROADMAP.md lines 45/70/71 are flipped from [ ] to [x] and the progress-table row reflects 3/3 | Complete | 2026-05-29 (D-12)"
    - "No mix.exs edits made — MAINTAINING.md and guides/recipes/deployment.md are already registered (D-10)"
  artifacts:
    - path: "guides/recipes/deployment.md"
      provides: "Operator diagnostics section for mix sigra.doctor"
      contains: "## Operator diagnostics"
    - path: "MAINTAINING.md"
      provides: "Three maintainer-audience sections (OptionalDeps SOT, recipe-contract fixture, deprecation timeline)"
      contains: "OptionalDeps"
    - path: ".planning/ROADMAP.md"
      provides: "Reconciled Phase 137 completion status"
      contains: "[x] **Phase 137"
  key_links:
    - from: "guides/recipes/deployment.md ## Operator diagnostics"
      to: "ExDoc Recipes group"
      via: "mix.exs extras line 218 (already registered)"
      pattern: "## Operator diagnostics"
    - from: "MAINTAINING.md new ## sections"
      to: "ExDoc root-level page"
      via: "mix.exs extras line 184 (already registered)"
      pattern: "OptionalDeps"
---

<objective>
Append three documentation jobs to close DOC-01 and perform ROADMAP stale-state reconciliation (D-12):
1. Append an "## Operator diagnostics" section to guides/recipes/deployment.md documenting mix sigra.doctor usage.
2. Append three maintainer-audience ## sections to MAINTAINING.md: OptionalDeps SOT note, recipe-contract fixture note, and deprecation-removal-timeline note.
3. Reconcile stale ROADMAP.md Phase 137 entries — flip four lines from incomplete to complete.

Purpose: DOC-01 requires that guides/docs reflect everything shipped in v1.30 this milestone. The deployment guide gains operator-facing doctor usage; MAINTAINING.md gains architecture-maintenance notes for the three new capabilities shipped in Phases 137-139. The ROADMAP reconciliation makes the planning artifact reflect the work that has already landed.

Output: Three files edited (appends/targeted checkbox flips), no new files, no mix.exs changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140-deprecation-hygiene-verification-docs-close/140-CONTEXT.md
@.planning/phases/140-deprecation-hygiene-verification-docs-close/140-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ## Operator diagnostics section to deployment.md (DOC-01, D-09)</name>
  <files>guides/recipes/deployment.md</files>

  <read_first>
    Read guides/recipes/deployment.md fully to identify:
    - The existing section headings and their line numbers
    - The placement target: after "## Health check endpoint" (near line 183) and before "## Fly.io specifics" (near line 205)
    Also confirm from 140-RESEARCH.md "DOC-01 Content Placement" section the exact key facts to include.
  </read_first>

  <action>
    Insert a new "## Operator diagnostics" section into guides/recipes/deployment.md between
    the "## Health check endpoint" section and the "## Fly.io specifics" section (per D-09 and
    RESEARCH placement guidance).

    The section must document mix sigra.doctor as a pre-deploy verification command. Include:

    - Section heading: ## Operator diagnostics
    - Opening sentence: run mix sigra.doctor before deploying to verify all configured optional
      features are properly wired. Describes it as a complement to health-check smoke tests.
    - Standard invocation: mix sigra.doctor (human-readable matrix output)
    - CI/quiet invocation: mix sigra.doctor --quiet (machine-readable exit-code-only; suitable
      for pre-deploy CI gate)
    - Exit code contract: exit 0 = all configured features properly wired (or no optional features
      configured); exit 1 = at least one configured feature has broken wiring.
    - Nine features covered (totp_mfa, password_migration, oauth, rate_limiting, jwt, async_email,
      audit_forwarding, encryption, enterprise_connections) with four possible states per row:
      loaded / available / configured_but_missing / missing.
    - Note: absent optional deps are NOT an error — the command only exits 1 on
      configured-but-broken wiring. Safe to run in dep-off CI lanes.
    - Note: run from the application directory (not the Sigra library root) so the task picks up
      the application's own config.

    The section is informational prose (no fenced code blocks needed beyond the command examples,
    which are standard mix invocations). Keep it concise — 1-2 short paragraphs plus the key facts.

    Do NOT edit mix.exs — guides/recipes/deployment.md is already registered in mix.exs extras
    at line 218 and the Recipes groups_for_extras bucket. No new registration needed (D-10).
    Do NOT edit any other section — this is an append/insert to an existing file.
  </action>

  <verify>
    <automated>grep "## Operator diagnostics" guides/recipes/deployment.md</automated>
  </verify>

  <acceptance_criteria>
    - grep "## Operator diagnostics" guides/recipes/deployment.md returns at least 1 match
    - grep "mix sigra.doctor" guides/recipes/deployment.md returns at least 2 matches (standard + --quiet invocation)
    - grep "exit" guides/recipes/deployment.md returns at least 1 match in the new section (documenting exit 0/1 contract)
    - The section appears between "## Health check endpoint" and "## Fly.io specifics" (verify by reading the updated file)
    - No mix.exs changes (confirm mix.exs is NOT in the modified file set)
  </acceptance_criteria>

  <done>guides/recipes/deployment.md has a new "## Operator diagnostics" section documenting mix sigra.doctor invocations, exit codes, and nine-feature coverage after the health-check section.</done>
</task>

<task type="auto">
  <name>Task 2: Add three ## sections to MAINTAINING.md (DOC-01, D-09, D-11)</name>
  <files>MAINTAINING.md</files>

  <read_first>
    Read MAINTAINING.md lines 216-245 to see the existing section structure around the target
    insertion point: "## Semver for Sigra (pre-1.0)" (line 216) and "## Planning hygiene
    (without gsd-tools JSON)" (line 224). The three new sections go between them.
    Also read 140-RESEARCH.md section "MAINTAINING.md: three new ## sections" for content guidance.
  </read_first>

  <action>
    Insert three new ## sections into MAINTAINING.md immediately after the "## Semver for Sigra
    (pre-1.0)" section and before "## Planning hygiene (without gsd-tools JSON)" (per D-09 and
    RESEARCH placement guidance).

    Section 1: ## OptionalDeps single source of truth (Phase 137)
    - Documents lib/sigra/optional_deps.ex as the canonical module for runtime optional-dep checks.
    - States that all RUNTIME optional-dep guards delegate to Sigra.OptionalDeps — to add or audit
      an optional dependency, edit lib/sigra/optional_deps.ex; do not scatter new
      Code.ensure_loaded? guards across call sites.
    - Documents the narrow documented exceptions (out-of-scope of the SOT, not a Phase 137 gap):
      compile-time defmodule wrappers, dynamic host-schema atoms, boot-warning cond blocks, and
      the doctor task's dynamic-forwarder check. The "single source of truth" claim applies to
      runtime guards only.
    - Per D-11 guidance: the claim is explicitly scoped to runtime guards to avoid overclaiming.

    Section 2: ## Recipe-contract fixture (Phase 139)
    - Documents test/sigra/recipes/companion_lib_contract_test.exs as a maintainer-internal
      merge-blocking drift guard.
    - States the five required markers every companion-lib recipe under
      guides/recipes/companion-libs/ must carry: "Failure modes" section, "Non-goals" section,
      "Sigra works fully standalone" banner, validated_against: frontmatter, last_validated:
      frontmatter.
    - Explicitly states this is NOT a Hex-facing recipe or adopter-facing test — it is a CI
      contract assertion that runs in the standard mix test suite (per D-11).

    Section 3: ## Deprecation removal timeline
    - Documents the two live deprecated functions and their committed removal schedules:
      - Sigra.MFA.Trust.cookie_opts/0 — already raises at runtime (no_return() stub);
        stub deleted entirely in 0.4.0. Migration: use cookie_opts/1 with %Sigra.Config{}.
      - Sigra.Account.audit_forced_password_change/2 — still works (soft-deprecated);
        removed in 0.5.0. Migration: use clear_password_change_requirement/3.
    - States the removal process: each removal-target version will carry a CHANGELOG entry
      and the function body will be deleted (not just the annotation).
    - Removal targets are expressed as Hex SemVer 0.x minors consistent with the pre-1.0 policy
      above (D-01).

    Keep each section concise (1-2 paragraphs maximum). Match the terse, directive tone of
    existing MAINTAINING.md sections. No fenced code blocks needed unless showing a command.
    Do NOT edit mix.exs — MAINTAINING.md is already registered at line 184. (D-10)
  </action>

  <verify>
    <automated>grep -c "## OptionalDeps\|## Recipe-contract\|## Deprecation removal" MAINTAINING.md</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "## OptionalDeps\|## Recipe-contract\|## Deprecation removal" MAINTAINING.md returns 3
    - grep "runtime" MAINTAINING.md | grep -i "optional" returns at least 1 match (SOT claim scoped to runtime)
    - grep "companion_lib_contract_test" MAINTAINING.md returns at least 1 match (fixture documented)
    - grep "0.4.0" MAINTAINING.md returns at least 1 match (cookie_opts removal target)
    - grep "0.5.0" MAINTAINING.md returns at least 1 match (audit_forced removal target)
    - grep "NOT a Hex-facing\|not a Hex-facing\|maintainer-internal" MAINTAINING.md returns at least 1 match (D-11 scope note)
    - The three sections appear after "## Semver for Sigra (pre-1.0)" and before "## Planning hygiene"
  </acceptance_criteria>

  <done>MAINTAINING.md has three new ## sections after the Semver section: OptionalDeps SOT note (scoped to runtime guards with documented exceptions), recipe-contract fixture note (marked as maintainer-internal CI guard, not Hex-facing), and deprecation removal timeline (0.4.0 for cookie_opts/0, 0.5.0 for audit_forced_password_change/2).</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile stale ROADMAP.md Phase 137 entries (DOC-01, D-12)</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    Read .planning/ROADMAP.md lines 43-75 and lines 128-145 to see the current stale entries
    and the progress table row before making any edits.
    Also read 140-RESEARCH.md section "ROADMAP Reconciliation (D-12)" for the exact line
    content and required replacements.
  </read_first>

  <action>
    Make four targeted edits to .planning/ROADMAP.md to reflect that Phase 137 is fully complete.
    All three 137-SUMMARY.md files exist with completed: "2026-05-29" — this is cosmetic reconciliation
    only (D-12).

    Edit 1 — ROADMAP.md line 45:
      Current:  - [ ] **Phase 137: Optional-Dependency Source of Truth** —
      Replace:  - [x] **Phase 137: Optional-Dependency Source of Truth** —

    Edit 2 — ROADMAP.md line 70:
      Current:    - [ ] 137-02-PLAN.md — Delegate single-leaf runtime guards (Bcrypt, EQRCode, Joken, Hammer, Assent×5) to the SOT (OD-02)
      Replace:    - [x] 137-02-PLAN.md — Delegate single-leaf runtime guards (Bcrypt, EQRCode, Joken, Hammer, Assent×5) to the SOT (OD-02)

    Edit 3 — ROADMAP.md line 71:
      Current:    - [ ] 137-03-PLAN.md — Delegate compound-guard load-halves (Oban delivery/forwarders/deletion, Req validation), liveness/arity halves preserved (OD-02, D-06)
      Replace:    - [x] 137-03-PLAN.md — Delegate compound-guard load-halves (Oban delivery/forwarders/deletion, Req validation), liveness/arity halves preserved (OD-02, D-06)

    Edit 4 — ROADMAP.md line 141 (progress table row):
      Current:  | 137. Optional-Dependency Source of Truth | 1/3 | In Progress|  |
      Replace:  | 137. Optional-Dependency Source of Truth | 3/3 | Complete | 2026-05-29 |

    Do NOT edit STATE.md — the branch name drift and STATE.md inconsistencies are deferred to
    /gsd-complete-milestone (D-12). Only ROADMAP.md is in scope here.
    Do NOT change the Phase 140 "TBD" plans entry — that will be updated when this plan set
    is committed.
  </action>

  <verify>
    <automated>grep -n "\[x\].*Phase 137" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "\[x\].*Phase 137" .planning/ROADMAP.md returns at least 1 match (line 45 flipped)
    - grep -n "\[x\].*137-02-PLAN.md" .planning/ROADMAP.md returns at least 1 match (line 70 flipped)
    - grep -n "\[x\].*137-03-PLAN.md" .planning/ROADMAP.md returns at least 1 match (line 71 flipped)
    - grep "3/3.*Complete.*2026-05-29" .planning/ROADMAP.md returns at least 1 match (progress table row updated)
    - grep "1/3.*In Progress" .planning/ROADMAP.md returns 0 matches (old value gone)
    - grep "\[ \].*Phase 137" .planning/ROADMAP.md returns 0 matches (no remaining incomplete checkboxes for Phase 137 heading)
  </acceptance_criteria>

  <done>ROADMAP.md Phase 137 entries are fully reconciled: heading checkbox and both plan-level checkboxes are [x], and the progress table shows 3/3 | Complete | 2026-05-29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation append | Appending sections to existing registered docs files |
| Planning artifact edit | Checkbox flips in .planning/ROADMAP.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-02-SC | Tampering | guides/recipes/deployment.md, MAINTAINING.md, .planning/ROADMAP.md | accept | No new attack surface: edits are documentation appends and ROADMAP checkbox flips. No new inputs, auth paths, data flows, or runtime behavior change. Acceptance criteria verify section headings and grep patterns prevent accidental omission. |

**No new attack surface.** This plan makes documentation-only changes: new ## sections appended to existing registered ExDoc files and targeted [ ]->[x] checkbox flips in a planning artifact. No new dependencies, no runtime behavior change, no new inputs or auth paths.
</threat_model>

<verification>
After all three tasks complete:

1. Operator diagnostics section present:
   grep "## Operator diagnostics" guides/recipes/deployment.md

2. Three MAINTAINING.md sections present:
   grep -c "## OptionalDeps\|## Recipe-contract\|## Deprecation removal" MAINTAINING.md
   (expect 3)

3. ROADMAP Phase 137 fully reconciled:
   grep -n "\[x\].*Phase 137" .planning/ROADMAP.md
   grep "3/3.*Complete.*2026-05-29" .planning/ROADMAP.md

4. No mix.exs modification (would be wrong):
   git diff mix.exs — expect empty output
</verification>

<success_criteria>
- DOC-01 deployment.md: "## Operator diagnostics" section present documenting mix sigra.doctor standard + --quiet invocations, exit-code contract, and nine-feature coverage.
- DOC-01 MAINTAINING.md: Three new ## sections present — OptionalDeps SOT (scoped to runtime guards with documented exceptions), recipe-contract fixture (marked maintainer-internal, not Hex-facing), deprecation removal timeline (0.4.0 / 0.5.0).
- DOC-01 ROADMAP reconciliation (D-12): Lines 45/70/71 flipped to [x]; progress table row shows 3/3 | Complete | 2026-05-29.
- No mix.exs changes made (D-10).
</success_criteria>

<output>
Create `.planning/phases/140-deprecation-hygiene-verification-docs-close/140-02-SUMMARY.md` when done.
</output>
