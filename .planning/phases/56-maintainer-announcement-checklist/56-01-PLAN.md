---
phase: 56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
autonomous: true
requirements:
  - MAINT-01
must_haves:
  truths:
    - "**`MAINTAINING.md`** contains a new **`## First public launch (announcement checklist)`** section **after** **`## Release automation (default)`** and **before** **`## Manual release checklist (emergency or pre-automation)`**, with **Ship** vs **Announce** groupings, **durable roles** (no embedded `@github-handle` in checklist rows), and **optional** comms rows clearly marked **optional**."
    - "Evidence links for paths under **`.planning/`** use **tag-scoped** GitHub URLs with prefix **`https://github.com/sztheory/sigra/blob/v0.2.0/`** (aligned with **`mix.exs` `@version`**); packaged docs use **relative** links only (e.g. **`docs/uat-ci-coverage.md`**, **`docs/ga-evidence.md`**)."
    - "The checklist **links** to existing **Release automation**, **Manual release**, **Installer golden CI contract**, **`install_golden_contract`**, and **Nyquist policy** material — it does **not** duplicate numbered ship steps from those sections."
  artifacts:
    - path: "MAINTAINING.md"
      provides: "MAINT-01 first public announcement runbook + roster/assignment framing"
nyquist_compliant: false
---

<objective>
Deliver **MAINT-01**: extend **`MAINTAINING.md`** with a **First public launch (announcement checklist)** per **`.planning/phases/56-maintainer-announcement-checklist/56-CONTEXT.md` D-01–D-18** — ordered **Ship** vs **Announce** rows, role-based owners, installer golden + GA evidence pointers, optional comms rows, and a one-line **HexDocs vs tag URL** policy for maintainers.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-56-01 | Spoofing / misrepresentation | Checklist reads like **deployment certification** or **universal GA pass** for consumer apps | high | Forbid warranty language; require pointers to **Executed vs Waived** + *host* risk; include **D-16**-style “do not” bullets per **56-CONTEXT** |
| T-56-02 | Tampering (doc integrity) | **Bare** markdown links to **`.planning/`** break on **hexdocs.pm** | high | `grep` rejects `\]\(\.planning/`; mandate **`v0.2.0`** blob URLs for `.planning/*` evidence |
| T-56-03 | Information disclosure | Embedding personal **@handles** in committed checklist | low | Roles + per-run roster only (**D-09–D-10**) |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Maintainer intro — discoverability sentence</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - mix.exs
    - .planning/phases/56-maintainer-announcement-checklist/56-CONTEXT.md
  </read_first>
  <action>
    Under **`# Maintaining Sigra`**, in the **opening maintainer-facing** block (the first paragraphs before **`## Installer golden CI contract`**), add **one** new sentence (compose natural prose) that satisfies **all** of:

    1. Mentions **first public Hex release** (or **first loud public push** — pick one phrase).
    2. Mentions **`First public launch`** (exact words, backticks optional) as the section title readers should open.
    3. Mentions **`Release automation`** (exact substring) so Ctrl+F users land in the default ship path first.

    Do **not** duplicate the installer golden commands here.
  </action>
  <acceptance_criteria>
    - `grep -nF '# Maintaining Sigra' MAINTAINING.md` exits **0**.
    - `grep -Fi 'first public' MAINTAINING.md` exits **0**.
    - `grep -F 'First public launch' MAINTAINING.md` exits **0**.
    - `grep -F 'Release automation' MAINTAINING.md` exits **0**.
    - The line number of `grep -nF '## Installer golden CI contract' MAINTAINING.md | head -1` is **greater than** the line number of the last match of the four bullets above (intro sentence appears **before** the Installer golden heading).
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: First public launch (announcement checklist) section body</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - mix.exs
    - README.md
    - docs/ga-evidence.md
    - docs/uat-ci-coverage.md
    - .planning/phases/56-maintainer-announcement-checklist/56-CONTEXT.md
  </read_first>
  <action>
    Insert a new top-level section **immediately after** the **`## Release automation (default)`** block (after the **Recovery / one-off publish** paragraph, currently ending that section) and **immediately before** **`## Manual release checklist (emergency or pre-automation)`**.

    **Heading (exact):** `## First public launch (announcement checklist)`

    **Mandatory structure:**

    1. **Opening policy line** (one sentence, can be its own short paragraph): state that **relative links** in this file are for **in-repo / HexDocs-packaged** paths only; **evidence outside the Hex tarball** (under **`.planning/`**) must use **pinned tag** GitHub URLs — **not** `main` blob URLs.

    2. **`### Assignment`**: Explain that **Release captain** opens a **single tracking issue** (or equivalent) for the run and records a **Roster** table with **Comms DRI**, **Security / evidence reviewer** (and other roles you use) — **this run only**. Checklist steps reference **roles**, not GitHub handles (**56-CONTEXT D-09–D-10**).

    3. **`### Ship (artifact truth)`** — ordered checklist/table rows that **only link** into existing sections:
       - Link to **`## Release automation (default)`** and **`## Manual release checklist (emergency or pre-automation)`** using **in-doc** markdown anchors: `[Release automation (default)](#release-automation-default)` and `[Manual release checklist (emergency or pre-automation)](#manual-release-checklist-emergency-or-pre-automation)` (GitHub slug generation may vary — executor MUST verify anchors work on GitHub preview; if slugs differ, use the correct GitHub-generated `#...` fragment for those two headings in this file).
       - Link to **`## Installer golden CI contract (phase 50)`** via `[Installer golden CI contract (phase 50)](#installer-golden-ci-contract-phase-50)`.
       - Include the **exact** branch-protection check string in monospace: `` `Install golden + idempotency contract (subprocess harness)` `` (copy from existing MAINTAINING subsection).
       - Include tag-scoped links (full URL string prefix **`https://github.com/sztheory/sigra/blob/v0.2.0/`**) for **each** of:
         - `.planning/v1.4-GA-UAT.md`
         - `.planning/milestones/v1.4-REQUIREMENTS.md`
       - Include at least **one** relative link to **`docs/uat-ci-coverage.md`** using markdown `[docs/uat-ci-coverage.md](docs/uat-ci-coverage.md)`.
       - Include at least **one** relative link to **`docs/ga-evidence.md`** using markdown `[docs/ga-evidence.md](docs/ga-evidence.md)`.
       - Row or bullet that states human **GA-02..GA-05** matrix items may remain **waived** per **`v1.4-GA-UAT.md`** — do **not** imply they were re-run for a forum post (**56-CONTEXT D-15**).

    4. **`### Announce (attention budget — optional by default)`** — each row for **Elixir Forum**, **Slack/Discord**, **blog**, **HN**, or **short social** posts must include the word **`optional`** (case-insensitive) in the same row or immediately following line. Include **one** bullet of **D-16** tone: avoid security theater, comparative trash-talk, implied warranty, or heated realtime debate.

    5. **Cross-link integrator path once:** point to **`README.md`** GA / evidence entry (repo-relative `[README.md](README.md)` or anchor if present) without pasting README body.

    **Forbidden:** numbered steps that duplicate **`mix hex.publish`**, **`git tag`**, or the full manual release list — those stay in their existing sections.
  </action>
  <acceptance_criteria>
    - `grep -nF '## First public launch (announcement checklist)' MAINTAINING.md` exits **0** with line number **strictly less than** `grep -nF '## Manual release checklist (emergency or pre-automation)' MAINTAINING.md | head -1 | cut -d: -f1`.
    - `grep -nF '## Release automation (default)' MAINTAINING.md | head -1 | cut -d: -f1` is **strictly less than** the line number for `## First public launch (announcement checklist)`.
    - `grep -F '### Assignment' MAINTAINING.md` exits **0**.
    - `grep -F '### Ship (artifact truth)' MAINTAINING.md` exits **0**.
    - `grep -F '### Announce (attention budget' MAINTAINING.md` exits **0**.
    - `grep -F 'https://github.com/sztheory/sigra/blob/v0.2.0/.planning/v1.4-GA-UAT.md' MAINTAINING.md` exits **0**.
    - `grep -F 'https://github.com/sztheory/sigra/blob/v0.2.0/.planning/milestones/v1.4-REQUIREMENTS.md' MAINTAINING.md` exits **0**.
    - `grep -F '[docs/uat-ci-coverage.md](docs/uat-ci-coverage.md)' MAINTAINING.md` exits **0**.
    - `grep -F '[docs/ga-evidence.md](docs/ga-evidence.md)' MAINTAINING.md` exits **0**.
    - `grep -F 'Install golden + idempotency contract (subprocess harness)' MAINTAINING.md` exits **0**.
    - `grep -Ei 'optional' MAINTAINING.md` exits **0** (Announce subsection must mark optional comms).
    - `grep -E '\]\(\.planning/' MAINTAINING.md` exits **1** (no markdown link targets beginning with `.planning/`).
    - `grep -F 'Release captain' MAINTAINING.md` exits **0**.
    - `grep -F 'Roster' MAINTAINING.md` exits **0** (per-run roster table or equivalent wording).
  </acceptance_criteria>
  <verify>
    `mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>

<verification>
1. `mix compile --warnings-as-errors` — exit **0**.
2. `mix docs --warnings-as-errors` — exit **0**.
3. Re-run all **acceptance_criteria** greps from Task 1–2 — all succeed.
4. Manual: read **Ship** rows — each is a **link** or **pointer**, not a copy of **Manual release** numbered steps.
</verification>

<success_criteria>
- **MAINT-01** satisfied: ordered checklist with **roles**, **Ship** vs **Announce**, **install_golden_contract** + **GA matrix** references, optional rows explicitly **optional**.
- **`MAINTAINING.md`** remains the **single** maintainer runbook for this concern (no new `docs/maintainers/*.md` in this phase).
- Docs build is clean (`mix docs --warnings-as-errors`).
</success_criteria>
