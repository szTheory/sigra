---
phase: 244-playwright-test-1-59-1-1-62-1-alone
plan: 03
type: execute
wave: 3
depends_on: [244-02]
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/run-playwright-drift.sh
  - scripts/ci/measure-playwright-drift.mjs
  - scripts/ci/measure-playwright-drift.test.mjs
  - .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
autonomous: true
requirements: [QUEUE-02]
estimate: {tokens: 27000, raw_tokens: 27000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01: old and candidate captures are produced sequentially on one Ubuntu VM from the same source commit and complete tracked inventory."
    - "D-02/D-03: a committed JSON measurement names both package versions, tagged manifest revisions, run ID, source SHA and per-image results; full renders/diffs remain CI artifacts."
    - "D-07: live PR/main state and latest check identity are refreshed, with the August 8 failed run explicitly classified stale."
  artifacts:
    - .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
  key_links:
    - "Same `github.sha` -> separate old/new package and browser roots -> complete captures -> exact comparator -> immutable run artifact -> committed measurement JSON"
---

<objective>
Measure the full 1.59.1 versus 1.62.1 render set and commit the evidence whether the result is zero, drift, or inconclusive.
Purpose: make the conditional disposition depend on current measured evidence.
Output: one paired Ubuntu run and a validated machine-readable manifest.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-CONTEXT.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-RESEARCH.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-02-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/run-playwright-drift.sh
@scripts/ci/measure-playwright-drift.mjs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run full paired render and exact comparison on one Ubuntu VM</name>
  <files>.github/workflows/ci.yml, scripts/ci/run-playwright-drift.sh, scripts/ci/measure-playwright-drift.mjs, scripts/ci/measure-playwright-drift.test.mjs</files>
  <read_first>.github/workflows/ci.yml; scripts/ci/run-playwright-drift.sh; scripts/ci/measure-playwright-drift.mjs; .github/actions/example-playwright-boot/action.yml</read_first>
  <behavior>Both installs identify expected locked trio, manifest-derived Chromium revisions and separate browser roots. Exactly the full tracked path set from one source SHA is captured for each; any missing/extra/dimension/pixel difference produces drift or inconclusive. The candidate SHA's fast_checks cache guard and self-test both run successfully.</behavior>
  <action>Per D-01/D-02/D-03/D-05, run two sequential render passes in the isolated workflow job from identical source SHA. In disposable old root, change only npm package manifest/lock to exact 1.59.1; in disposable candidate root use committed exact 1.62.1. Use `npm ci`, assert the three Playwright packages are version-consistent, and set different `PLAYWRIGHT_BROWSERS_PATH` roots so a cache hit cannot cross versions. Fetch both tagged browsers.json files afresh, validate Chromium revision/version values and record URL plus content SHA-256; use the installed package's manifest/browser identity as a consistency check. Keep runner image, system dependencies, fonts, app source, fixture seed, project matrix and capture settings identical; reset/reseed the existing test data seam between passes. Render all tracked PNG paths, compare exact sets/dimensions/decoded AE counts, and upload both output trees, all diffs, raw manifest and logs on pass/fail. Do not write PNG baselines to the candidate source tree. Add a synthetic test ensuring one differing pixel makes the workflow result nonzero while retaining the manifest. Provide a `verify --validate-recorded-outcome` mode that accepts a complete and internally consistent zero, drift or inconclusive record while refusing to turn the latter two into merge eligibility. Dispatch a fresh run from the scoped candidate/evidence branch; reuse at most one watcher. Before a long watch check `gh api rate_limit`, stop polling if core remaining is at most 250 until reset, and on HTTP 403/429 stop until the reported reset/retry time. Use `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; a red measurement caused by recorded drift still proceeds to evidence collection. After completion fetch the structured run summary once and failure logs only if failed. Retry transient infrastructure failure once; diagnose and repair deterministic failure before another run.</action>
  <verify><automated>node --test scripts/ci/measure-playwright-drift.test.mjs && node scripts/ci/measure-playwright-drift.mjs verify --manifest "$PHASE244_MEASUREMENT_MANIFEST" --source-sha "$PHASE244_SOURCE_SHA" --validate-recorded-outcome</automated></verify>
  <acceptance_criteria>One CI-native run emits a schema-valid paired manifest and uploads old/new renders and diffs; the verifier reports zero_drift, drift, or inconclusive without converting unknown data into zero. The same candidate SHA's `fast_checks` job shows both cache guard steps successful; otherwise merge eligibility is false.</acceptance_criteria>
  <fails_when>Sources/runner environments differ, either complete set is unavailable, browser cache roots alias, comparator fails, or the manifest cannot be tied to the run and SHA.</fails_when>
  <done>A fresh paired measurement exists with a fail-closed verdict and inspectable CI artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Commit current PR state and measurement decision evidence</name>
  <files>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json</files>
  <read_first>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-CONTEXT.md; .planning/phases/240-green-main-evidence-honest-pages-script/240-GREEN-04-EVIDENCE.json; scripts/ci/capture-phase-241-final-head.sh</read_first>
  <action>Per D-02/D-03/D-07, re-read PR #213 state, mergedAt, headRefName/head SHA, current remote main SHA and the latest check run via structured GitHub reads. Start with the observed closed/unmerged, missing-head-ref state but do not assume it persisted. Explicitly distinguish the August 8 failed run from the new measurement and current checks; diagnose its failure as historical context without counting it as visual proof. Copy the verified run's schema-versioned measurement into a top-level `measurement` object in committed phase JSON on the scoped evidence branch, including source SHA, measuring run ID/URL, runner/image, comparator package/version, old/candidate package trio and manifest URLs/hashes/revisions, inventory digest/count, per-image path/dimensions/diff count/result, missing/extra lists, artifact identifiers, and current PR state. Include `decision_eligibility` with zero/drift/inconclusive and current candidate availability, but leave final PR disposition and main receipt for Plan 04. Commit even a drift/inconclusive outcome; never omit evidence because the measurement failed. Validate against the downloaded run manifest before atomic write.</action>
  <verify><automated>node scripts/ci/measure-playwright-drift.mjs verify --manifest .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json --source-sha "$PHASE244_SOURCE_SHA" --validate-recorded-outcome && gh pr view 213 --repo szTheory/sigra --json number,title,state,mergedAt,headRefName,headRefOid</automated></verify>
  <acceptance_criteria>The committed receipt exactly reflects a fresh measurement and names the latest PR state without claiming the old failed check establishes drift.</acceptance_criteria>
  <fails_when>Any required field is missing, the source/run identity differs, the PR state is inferred rather than read, or a failed measurement produces no committed diagnostic.</fails_when>
  <done>The measurement is durable and decision-ready regardless of outcome.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| GitHub API to decision receipt | PR/check metadata can be stale, incomplete or malformed. |
| CI artifact to committed evidence | Downloaded JSON must match source/run identity. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-06 | Spoofing | PR/check provenance | high | mitigate | Verify run ID, source SHA, PR number/head SHA and manifest hash from structured API fields. |
| T-244-07 | Tampering | paired render roots | high | mitigate | Isolate npm/browser roots and require exact source-tree equality outside npm lock state. |
| T-244-08 | Repudiation | failed measurement | medium | mitigate | Commit the diagnostic JSON even when verdict is drift/inconclusive; retain failed run artifacts. |
</threat_model>

<verification>Local contract tests plus one completed Ubuntu paired run and committed JSON. Historical PR checks are disclosed but never substituted.</verification>
<success_criteria>All tracked PNGs are accounted for under both versions on one source revision, with exact-pixel verdict and durable evidence.</success_criteria>
<output>Create `.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-03-SUMMARY.md`.</output>
