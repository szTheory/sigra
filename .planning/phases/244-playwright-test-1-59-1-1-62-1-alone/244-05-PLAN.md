---
phase: 244-playwright-test-1-59-1-1-62-1-alone
plan: 05
type: execute
wave: 5
depends_on: [244-04]
files_modified:
  - scripts/ci/capture-phase-244-final-main.sh
  - scripts/ci/capture-phase-244-final-main.test.sh
  - .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
autonomous: true
requirements: [QUEUE-02]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-08: one complete post-disposition CI run has successful ci-gate, five named shard browser steps, example smoke aggregation and generated-admin browser harness at the exact resulting main SHA."
    - "D-09: the committed receipt separates aggregate gate and each individual consumer, with run/job/step identities and conclusions; skipped jobs or browser bodies never count."
    - "The evidence branch commit can reference the exact remote main SHA without changing that SHA."
    - "QUEUE-02 spec-less edge probe remains flagged: unclassified/unresolved, applicable 1, resolved 0, unresolved 1."
  artifacts:
    - scripts/ci/capture-phase-244-final-main.sh
    - scripts/ci/capture-phase-244-final-main.test.sh
    - .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
  key_links:
    - "remote main SHA -> one CI run head_sha -> paginated job and steps inventory -> exact consumer/browser-step allowlist -> committed receipt"
---

<objective>
Capture the exact resulting-main CI proof and finish the durable phase receipt.
Purpose: prove each required Playwright consumer actually executed successfully after merge or defer.
Output: tested GitHub job collector and committed exact-SHA JSON receipt on a scoped evidence branch.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-CONTEXT.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-04-SUMMARY.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
@scripts/ci/capture-phase-241-final-head.sh
@.github/workflows/ci.yml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reject stale, skipped or incomplete final-main CI receipts</name>
  <files>scripts/ci/capture-phase-244-final-main.sh, scripts/ci/capture-phase-244-final-main.test.sh</files>
  <read_first>scripts/ci/capture-phase-241-final-head.sh; .github/workflows/ci.yml; .planning/phases/240-green-main-evidence-honest-pages-script/240-GREEN-04-EVIDENCE.json</read_first>
  <behavior>One completed run with run head SHA equal to remote main and exactly one successful `ci-gate`, five `Example Playwright shard (seam)` jobs, `Example Playwright smoke (full lifecycle)` and `Generated admin Playwright smoke` passes only when each mapped browser or aggregate step is present exactly once with completed/success. Example smoke is an aggregate job with no Playwright command of its own; its aggregate step plus all five shard browser steps are its execution proof. Missing, duplicate, skipped, queued, failed or wrong-SHA jobs or required steps fail. Paginated API totals must agree; a docs-only fixture with green aggregate/job conclusions but skipped browser steps fails.</behavior>
  <action>Per D-08/D-09, adapt the Phase 241 collector to query one specified CI workflow run and every job page through structured GitHub REST responses, including each job's `steps[]` fields (`name`, `number`, `status`, `conclusion`) from `/repos/szTheory/sigra/actions/runs/{run_id}/jobs?per_page=100&page=N`. Capture remote `main` SHA before and after collection and require both equal run `head_sha`; require completed run and each required job status/conclusion `completed`/`success`. Match all five actual matrix seam names, the two smoke jobs and `ci-gate` exactly once each. In the `steps[]` for each seam require exactly one completed/success browser body: `admin_behavior` → `Run admin behavior browser truth`; `admin_checkpoints` → `Run admin checkpoints`; `design_gallery` on final main push/dispatch → `Run design gallery behavior and snapshots` (the PR-only `Run design gallery behavior` does not satisfy this main receipt); `non_admin_smoke` → `Run non-admin example browser smoke`; `demo_showcase` → `Run demo showcase`. Require `example_playwright_smoke` step `Aggregate every Playwright shard result` completed/success as the aggregate execution signal, and `generated_admin_playwright_smoke` step `Run generated admin acceptance smoke` completed/success as the generated-host browser harness signal (`scripts/ci/admin-acceptance-smoke.sh --test all` calls Playwright). Record each required step's name, number, status and conclusion in its consumer object, with run/job IDs and exact SHA; keep `ci-gate` separate. Reject an absent `steps[]`, missing/duplicate/non-success step, ambiguous job, partial pagination or changed main; do not infer browser execution from a successful job, aggregate or artifact upload. Add fake-`gh` fixtures for all-success, wrong SHA, missing/duplicate shard, skipped smoke, failed gate, malformed API page, main advancing mid-read, and a docs-only run whose jobs/aggregate are green while all five browser steps are skipped. Emit canonical JSON atomically only after checks pass; expose an offline `verify --receipt PATH --main-sha SHA` mode that validates the same job/step contract before a receipt is committed. Treat API fields as structured data, never shell code.</action>
  <verify><automated>bash scripts/ci/capture-phase-244-final-main.test.sh</automated></verify>
  <acceptance_criteria>All success and failure fixture cases pass, including fail-first docs-only green-job/skipped-browser evidence and skipped or absent consumers.</acceptance_criteria>
  <fails_when>Any incomplete or stale job/step set can yield a successful receipt, a docs-only green job can pass without browser execution, or the collector can partially overwrite its output.</fails_when>
  <done>The final-main job collector is fail-closed and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Run final main CI and commit exact-SHA receipt</name>
  <files>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json</files>
  <read_first>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json; scripts/ci/capture-phase-244-final-main.sh; AGENTS.md</read_first>
  <action>Per D-08/D-09, after PR disposition read the exact remote main SHA. Find a fresh complete `push` CI run on that SHA with event `push` and `head_branch=main`, or use the Plan 01 routing change already present on default main to dispatch `gh workflow run ci.yml --repo szTheory/sigra --ref main -f phase_244_final_main=true` with `recapture_branch` empty and both failure probes false; capture that run ID. The scoped `release_ref_guard` exception accepts this exact `refs/heads/main` final-main dispatch, while both recapture jobs are skipped by their explicit job conditions and required release consumers plus `ci-gate` run. Reject an ordinary `ci.yml --ref main` dispatch without this input; the legacy guard rejects it. Require the run API event/ref/head SHA to match the selected route and remote main before and after collection, and require the run was created after PR disposition. Do not use an old run, docs-only skipped browser bodies, or green aggregate alone. Before a long watch, check GitHub REST core quota and stop polling at 250 remaining until reset; stop immediately on 403/429 until reset/retry time. Assign one watcher only: `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; then fetch the structured summary once and failed logs only on failure. Retry transient infrastructure failure once; diagnose and repair deterministic failures, then run fresh exact-SHA CI rather than waiving a lane. Run the collector against the complete run; require all eight named jobs and the seven required consumer steps successful at the same resulting main SHA. If main advances, recapture a new exact-main run. Merge the final receipt into existing phase JSON while preserving measurement and PR decision, then commit on a scoped evidence branch/PR so the receipt commit does not change the SHA it proves. Include run URL, event/ref, timestamp, main SHA, gate object, all five seam objects and two smoke objects with IDs/status/conclusions and required step evidence. Leave durable failure JSON if proof cannot be obtained; do not mark QUEUE-02 passed. Run the repository's `mix ci` before any push, as STATE.md requires.</action>
  <verify><automated>bash scripts/ci/capture-phase-244-final-main.test.sh && bash scripts/ci/capture-phase-244-final-main.sh verify --receipt .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json --main-sha "$PHASE244_MAIN_SHA"</automated></verify>
  <acceptance_criteria>The committed JSON binds the post-disposition main SHA to one valid push or explicit final-main dispatch with all five shard browser steps, example smoke aggregate step, generated-admin browser harness step and gate individually successful; each job has a distinct ID and exact same run/SHA.</acceptance_criteria>
  <fails_when>Main SHA differs from run head, run event/ref is invalid, any job or required step is missing/duplicate/skipped/non-success, a rate-limit stop is ignored, or the receipt is committed without exact-SHA validation.</fails_when>
  <done>The phase has a durable exact-main-SHA consumer receipt, or explicit diagnostics blocking completion.</done>
</task>
</tasks>

<prohibitions>
Descriptor-less adversarial prohibition recall for QUEUE-02, since no phase SPEC exists: (1) Never present a missing, skipped or stale consumer as successful execution; this is a transparency constraint enforced by the collector's negative fixtures. (2) Never present a closed, missing-head PR or an inconclusive visual measurement as a merge-ready zero-drift candidate; this is a safety/truth constraint enforced by decision checks. Routine parser/path correctness remains in the ordinary tests and STRIDE register. No additional product-values prohibition was surfaced by the precision pass.
</prohibitions>

<source_audit>
| Source | Item | Plan | Status |
|---|---|---|---|
| GOAL | isolated bump merges on proven zero or defers with measurement; no recapture; final main consumers | 01-05 | COVERED |
| REQ | QUEUE-02 | 01-05 | COVERED |
| RESEARCH | same-source sequential Ubuntu render and complete 115-current-path inventory | 01,03 | COVERED |
| RESEARCH | exact decoded pixel comparator pinned/verified, tested negative cases | 01-03 | COVERED |
| RESEARCH | flagged package legitimacy before candidate install | 02 | COVERED |
| RESEARCH | closed PR/head ref, stale August check and current checks | 03-04 | COVERED |
| RESEARCH | exact-SHA paginated final consumer receipt | 05 | COVERED |
| CONTEXT | D-01 exact full paired comparison | 01-03 | COVERED |
| CONTEXT | D-02 unchanged PNGs, committed measurement/artifacts | 01,03 | COVERED |
| CONTEXT | D-03 tagged manifest revisions | 02-03 | COVERED |
| CONTEXT | D-04 both cache-key variants without redesign | 02 | COVERED |
| CONTEXT | D-05 test-tooling/boot isolation boundary | 01-03 | COVERED |
| CONTEXT | D-06 conditional merge/defer, no recapture | 04 | COVERED |
| CONTEXT | D-07 refresh PR/main/check state | 03-04 | COVERED |
| CONTEXT | D-08 every required consumer success on final main | 05 | COVERED |
| CONTEXT | D-09 distinct aggregate/consumer receipt | 05 | COVERED |
</source_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| GitHub job API to final truth claim | Stale/malformed/partial responses cannot establish execution. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-11 | Spoofing | final run SHA | high | mitigate | Re-read main before/after and require exact run head SHA. |
| T-244-12 | Tampering | API job/step list | high | mitigate | Exhaust pagination and require each expected job and named browser/aggregate step exactly once with completed/success; reject green docs-only skipped steps. |
| T-244-13 | Denial of service | GitHub API quota | medium | mitigate | One watcher, 60-second interval, quota preflight and 403/429 hard stop. |
</threat_model>

<verification>Fake-GitHub collector tests including green docs-only skipped-browser rejection, plus a completed exact-main-SHA CI run and machine-validated committed JSON with required step outcomes.</verification>
<success_criteria>QUEUE-02 has both a durable merge/defer decision and distinct exact-SHA proof that every named Playwright consumer's required browser or aggregate step and `ci-gate` ran successfully.</success_criteria>
<output>Create `.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-05-SUMMARY.md`.</output>
