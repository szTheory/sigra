---
phase: 244-playwright-test-1-59-1-1-62-1-alone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/phase-244-playwright-measure.yml
  - scripts/ci/run-playwright-drift.sh
  - scripts/ci/measure-playwright-drift.mjs
  - scripts/ci/measure-playwright-drift.test.mjs
autonomous: true
requirements: [QUEUE-02]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01: one committed PNG can be rendered and compared through a real Ubuntu CI run with a source SHA and run ID."
    - "D-02: the authoritative checkout has no changed tracked PNGs; disposable render trees and CI artifacts hold captures."
    - "The early isolated CI capture proves the complete tracked path set can be materialized; an incomplete set is recorded as inconclusive."
    - "A scoped Phase 244 dispatch runs only read-only measurement; exact-main CI dispatch runs required consumers but no recapture jobs."
    - "QUEUE-02 spec-less edge probe remains unclassified and unresolved (applicable 1, resolved 0, unresolved 1); no more specific result is inferred."
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/phase-244-playwright-measure.yml
    - scripts/ci/run-playwright-drift.sh
    - scripts/ci/measure-playwright-drift.mjs
    - scripts/ci/measure-playwright-drift.test.mjs
  key_links:
    - "Phase 244 workflow_dispatch on refs/heads/phase-244/* -> isolated Ubuntu job -> existing example boot/readiness -> disposable screenshot output -> comparator -> JSON/artifact"
    - "CI workflow_dispatch phase_244_final_main=true on refs/heads/main -> release_ref_guard -> required consumers and ci-gate; recapture jobs excluded"
---

<objective>
Establish a CI-native screenshot measurement path and prove that it can render the whole committed corpus without changing baselines.
Purpose: expose any capture or runner obstacle before the candidate upgrade.
Output: runnable Ubuntu capture path, one-image tracer receipt, complete-corpus capture spike and diagnostics.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-CONTEXT.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-RESEARCH.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PATTERNS.md
@.github/workflows/ci.yml
@.github/actions/example-playwright-boot/action.yml
@test/example/priv/playwright/playwright.config.ts

Planning is local and read-only with respect to GitHub. The scoped workflow-routing PR, workflow dispatches, and any other external writes occur only during phase execution.
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Render and compare one real committed PNG on Ubuntu</name>
  <files>.github/workflows/ci.yml, .github/workflows/phase-244-playwright-measure.yml, scripts/ci/run-playwright-drift.sh, scripts/ci/measure-playwright-drift.mjs</files>
  <read_first>.github/workflows/ci.yml; .github/actions/example-playwright-boot/action.yml; test/example/priv/playwright/playwright.config.ts; scripts/ci/snapshot-canary-guard.sh</read_first>
  <behavior>Set the standalone workflow display name to `Phase 244 Playwright measurement`; its run identity and head SHA must match the one-path manifest. The separate final-main CI input succeeds only on exact main with empty recapture input and false probes.</behavior>
  <action>Per D-01/D-02/D-05, add `.github/workflows/phase-244-playwright-measure.yml` with only `workflow_dispatch`, a boolean `phase_244_measure` input defaulting false, `contents: read`, and one `phase_244_playwright_measure` Ubuntu job. Its first guard step requires `github.event_name == workflow_dispatch`, `inputs.phase_244_measure == true`, `github.ref_type == branch`, and `github.ref` beginning `refs/heads/phase-244/`; reject main, tags, other branches, and absent/false input before install or render. This standalone workflow has no release, recapture, or write-capable jobs. In `.github/workflows/ci.yml`, add boolean `phase_244_final_main` dispatch input defaulting false and narrowly allow it through `release_ref_guard` only when the event is `workflow_dispatch`, ref is exactly `refs/heads/main`, `recapture_branch` is empty, and both failure-probe inputs are false; keep ordinary manual release evidence restricted to `refs/tags/v*`. Make both `admin_design_recapture` and `admin_checkpoint_recapture` job conditions false for that exact final-main route while leaving required consumers and `ci-gate` enabled. Publish these two workflow route changes in a scoped workflow-only PR to default `main` before manual branch dispatch, since GitHub registers `workflow_dispatch` only for workflow files on the default branch. Then dispatch `gh workflow run phase-244-playwright-measure.yml --repo szTheory/sigra --ref phase-244/MEASUREMENT_BRANCH -f phase_244_measure=true` from an evidence branch containing these scripts and workflow; match manifest source SHA to the run API `head_sha`. Use Ubuntu 24.04, the existing Phoenix example boot/readiness, and disposable worktrees pinned to that SHA. Initially run the existing locked 1.59.1 package in two isolated install/browser roots and capture one tracked PNG from `admin-checkpoints.spec.ts` using its existing project, then compare the actual outputs by decoded pixels and write JSON with source SHA, workflow run ID, exact path, dimensions, metric, package/browser identity, runner/comparator version, and artifact paths. Select the input path only from `git ls-files` under `test/example/priv/playwright/tests/*-snapshots/*.png`; reject traversal and empty selection. Install/check the Ubuntu pixel comparator explicitly, parse its AE count strictly, and treat absence/malformed output as inconclusive. Upload both renders, diff, raw JSON and logs even when comparison fails. Inspect the actual run manifest and record its run ID. Never mutate canonical tracked PNGs. PR #213's closed state and missing head ref remain independent; this routing PR must not recreate or reopen it.</action>
  <verify><automated>node scripts/ci/measure-playwright-drift.mjs verify --manifest "$PHASE244_SPIKE_MANIFEST" --expect-count 1 --source-sha "$PHASE244_SOURCE_SHA" && gh run view "$PHASE244_SPIKE_RUN_ID" --repo szTheory/sigra --json workflowName,event,headBranch,headSha,conclusion | jq -e --arg sha "$PHASE244_SOURCE_SHA" '.workflowName == "Phase 244 Playwright measurement" and .event == "workflow_dispatch" and (.headBranch | startswith("phase-244/")) and .headSha == $sha and .conclusion == "success"'</automated></verify>
  <acceptance_criteria>One actual Ubuntu workflow run emits a valid one-path zero-diff JSON result and downloadable render/diff artifacts tied to its own source SHA and run ID; the canonical checkout has zero tracked PNG changes.</acceptance_criteria>
  <fails_when>There is no live run/artifact, output path differs from selected tracked path, comparator output is unavailable/ambiguous, source identity differs, or the tracked PNG worktree changes.</fails_when>
  <done>One complete source-to-render-to-comparator-to-artifact path is proven on Ubuntu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove complete baseline capture inventory in isolated CI</name>
  <files>.github/workflows/phase-244-playwright-measure.yml, scripts/ci/run-playwright-drift.sh, scripts/ci/measure-playwright-drift.mjs, scripts/ci/measure-playwright-drift.test.mjs</files>
  <read_first>scripts/ci/measure-playwright-drift.mjs; test/example/priv/playwright/tests/admin-checkpoints.spec.ts; test/example/priv/playwright/tests/admin-design.spec.ts; test/example/priv/playwright/tests/demo-showcase.spec.ts; .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-VALIDATION.md</read_first>
  <behavior>Complete capture yields exact tracked path-set equality; a missing/extra path or a non-rendered test produces an inconclusive manifest and nonzero validation. Current tracked inventory is 115 paths, but the script derives it from the measured SHA.</behavior>
  <action>Expand the standalone measurement job's baseline capture to every committed Playwright PNG, across the admin-checkpoints, admin-design and demo-showcase projects, retaining project-specific browser/fixture settings and readiness. Add deterministic workflow-route fixtures to the Node test covering valid exact-main, rejected evidence-branch CI dispatch, invalid ref/input combinations and recapture exclusion; the separate measurement workflow cannot invoke release jobs. Run update mode only in disposable worktrees to obtain actual screenshot files; compare their relative path set to `git ls-tree` at the measured SHA. Fail if any baseline appears changed in the authoritative checkout. Add a hermetic Node test for complete and incomplete path sets. Dispatch the isolated Ubuntu capture early and record the full inventory, output file list, comparator identity and failed test diagnostics as CI artifacts. If capture cannot produce all paths, leave a durable inconclusive manifest and stop downstream merge eligibility until capture is repaired; no visual UAT substitute.</action>
  <verify><automated>node --test scripts/ci/measure-playwright-drift.test.mjs && node scripts/ci/measure-playwright-drift.mjs verify --manifest "$PHASE244_FULL_CAPTURE_MANIFEST" --inventory-only --source-sha "$PHASE244_SOURCE_SHA"</automated></verify>
  <acceptance_criteria>One Ubuntu run proves both baseline output and tracked inventory have exactly the same complete path set, with no missing or extra paths; deterministic fixtures prove the branch measurement route and exact-main CI route reject invalid refs/inputs and recapture; artifacts retain all renders and any diagnostics.</acceptance_criteria>
  <fails_when>Any committed PNG has no rendered counterpart, an extra render appears, a required test fails to produce a capture, a branch measurement can invoke release/recapture jobs, a final-main CI dispatch can invoke recapture, or output cannot be bound to the measured SHA.</fails_when>
  <done>The complete capture seam is proven before the candidate package is installed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Workflow input to shell/filesystem | A selected PNG path and ref must be constrained to committed inventory. |
| CI runner to durable artifact | Runner output may be incomplete or malformed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-01 | Tampering | render inventory input | high | mitigate | Resolve paths from `git ls-tree` at source SHA; reject traversal, missing and extra results. |
| T-244-02 | Spoofing | measurement identity | medium | mitigate | Require run ID and SHA in manifest to match workflow context and verifier arguments. |
| T-244-03 | Information disclosure | uploaded artifacts | low | mitigate | Upload screenshot/log paths only; keep GitHub tokens and process env out of artifacts. |
| T-244-14 | Elevation of privilege | manual dispatch routes | high | mitigate | Dedicated read-only branch measurement workflow; exact-main CI exception rejects wrong ref/input and explicitly skips both write-capable recapture jobs. |
</threat_model>

<verification>Automated manifest verification and a real Ubuntu workflow run establish the capture path. Do not use local macOS renders as drift proof.</verification>
<success_criteria>The complete committed PNG path set is captured on one Ubuntu source revision, or an explicit inconclusive artifact blocks the later decision.</success_criteria>
<output>Create `.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-01-SUMMARY.md`.</output>
