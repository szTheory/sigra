---
phase: 244-playwright-test-1-59-1-1-62-1-alone
plan: 02
type: execute
wave: 2
depends_on: [244-01]
files_modified:
  - scripts/ci/measure-playwright-drift.mjs
  - scripts/ci/measure-playwright-drift.test.mjs
  - test/example/priv/playwright/package.json
  - test/example/priv/playwright/package-lock.json
  - .github/workflows/ci.yml
  - scripts/ci/playwright-cache-key-guard.sh
  - scripts/ci/playwright-cache-key-guard.test.sh
autonomous: false
requirements: [QUEUE-02]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-01: equal decoded pixels alone yield zero drift; one changed pixel, dimensions, missing/extra image or comparator failure cannot."
    - "D-03: browser revisions are taken from the exact upstream release manifests during execution."
    - "D-04: chromium and chromium-webkit cache-key families each match the selected lock version."
    - "D-05: package and cache changes stay in the isolated npm test-tooling candidate; existing example and generated-host boot/data seams remain intact."
  artifacts:
    - scripts/ci/measure-playwright-drift.test.mjs
    - scripts/ci/playwright-cache-key-guard.test.sh
    - test/example/priv/playwright/package-lock.json
  key_links:
    - "Tagged Playwright manifest -> recorded Chromium revision -> package-specific browser install"
    - "Candidate lock version -> every versioned cache key -> fast_checks guard"
---

<objective>
Make exact drift decisions fail closed and prepare the isolated 1.62.1 candidate behind its supply-chain gate.
Purpose: ensure package and cache provenance cannot be mistaken for visual proof.
Output: tested comparator/manifest rules and a candidate-only dependency/cache change.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-CONTEXT.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-RESEARCH.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-01-SUMMARY.md
@scripts/ci/playwright-cache-key-guard.sh
@scripts/ci/playwright-cache-key-guard.test.sh
@test/example/priv/playwright/package.json
@test/example/priv/playwright/package-lock.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce exact decoded-pixel and inventory verdicts</name>
  <files>scripts/ci/measure-playwright-drift.mjs, scripts/ci/measure-playwright-drift.test.mjs</files>
  <read_first>scripts/ci/measure-playwright-drift.mjs; scripts/ci/measure-playwright-drift.test.mjs; .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-RESEARCH.md</read_first>
  <behavior>Identical image sets and dimensions with AE zero yield zero_drift. A one-pixel change yields drift. Dimension mismatch, missing/extra path, comparator nonzero error, unavailable binary, or malformed metric yields drift or inconclusive, never zero_drift. Each path has width, height, pixel count, result.</behavior>
  <action>Per D-01/D-02, extend the tracer comparator from one path to the entire tracked path list. Compare two captured path sets to `git ls-tree` at the same source SHA and to each other before pixel work. Query decoded dimensions independently; invoke ImageMagick AE with zero fuzz and parse only a single nonnegative integer metric while allowing ImageMagick's documented different-image exit code. Fail closed for malformed stderr or execution failure. Pin ImageMagick to an explicit Ubuntu package version verified in the isolated CI spike; record package/version and refuse a version mismatch or missing command. Write stable schema-versioned JSON with expected inventory count/hash, every path, both dimensions, changed pixels, missing/extra lists, total and verdict. Preserve raw comparator diagnostics and diffs; never alter tracked baselines. Add hermetic tests with generated tiny PNGs for zero drift, one changed pixel, dimensions, missing, extra, malformed metric and unavailable comparator.</action>
  <verify><automated>node --test scripts/ci/measure-playwright-drift.test.mjs</automated></verify>
  <acceptance_criteria>All seven comparator cases pass, and no test depends on a browser or network.</acceptance_criteria>
  <fails_when>A positive or unknown difference can produce zero_drift, a path is omitted from the manifest, or comparator identity is not fixed and recorded.</fails_when>
  <done>The exact-pixel decision rule has positive and negative automated proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify the flagged Playwright package before candidate install</name>
  <read_first>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-RESEARCH.md</read_first>
  <action>The Package Legitimacy Audit marks `@playwright/test@1.62.1` `[SUS]` because it was too new for the research check. Refresh registry, official Microsoft source/tag and tarball identity. If a fresh legitimacy gate returns `OK`, preserve that result and continue; if it remains `[SUS]` or `[ASSUMED]`, require explicit blocking human verification against npmjs.com/package/@playwright/test and Microsoft's Playwright release before any candidate npm install. A `[SLOP]` verdict forbids installation. This supply-chain gate cannot be auto-approved by workflow automation.</action>
  <what-built>The baseline-only CI capture and comparator are ready; the candidate install has not begun.</what-built>
  <how-to-verify>Inspect the official npm package and Microsoft release identity for version 1.62.1, then approve only the named package/version if the refreshed audit remains flagged.</how-to-verify>
  <resume-signal>Record explicit approval of `@playwright/test@1.62.1` or reject it.</resume-signal>
  <verify><automated>npm view @playwright/test@1.62.1 version dist.tarball dist.integrity --json</automated></verify>
  <acceptance_criteria>A fresh OK verdict or explicit human legitimacy approval is recorded before candidate install.</acceptance_criteria>
  <fails_when>The package verdict is flagged without approval or is SLOP.</fails_when>
  <done>The supply-chain gate has a documented verdict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prepare the isolated candidate and guard both cache families</name>
  <files>test/example/priv/playwright/package.json, test/example/priv/playwright/package-lock.json, .github/workflows/ci.yml, scripts/ci/playwright-cache-key-guard.sh, scripts/ci/playwright-cache-key-guard.test.sh</files>
  <read_first>test/example/priv/playwright/package.json; test/example/priv/playwright/package-lock.json; .github/workflows/ci.yml; scripts/ci/playwright-cache-key-guard.sh; scripts/ci/playwright-cache-key-guard.test.sh</read_first>
  <behavior>Both chromium and chromium-webkit cache-key variants match the lock version; either variant absent/stale fails. `npm ls` reports `@playwright/test`, `playwright`, and `playwright-core` all at 1.62.1.</behavior>
  <action>Per D-03/D-04/D-05, in a scoped candidate worktree update package manifest and lockfile through npm to exact 1.62.1 without unrelated dependency changes. Re-fetch `browsers.json` from both official tagged versions and record URL, content hash, Chromium revision and browserVersion; do not maintain revisions as source constants. Change every existing `playwright-chromium-...` and `playwright-chromium-webkit-...` key in the five shard matrix entries to 1.62.1 while retaining each browser list, database, port and existing composite-action cache strategy. Extend the guard to enumerate every key and require both distinct browser-set variants and lock agreement; add fixtures where either family is stale or missing. Keep `fast_checks` invoking the guard and its self-test. Do not modify Elixir/Phoenix/runtime or PNG files.</action>
  <verify><automated>bash scripts/ci/playwright-cache-key-guard.test.sh && bash scripts/ci/playwright-cache-key-guard.sh && npm --prefix test/example/priv/playwright ls @playwright/test playwright playwright-core --all</automated></verify>
  <acceptance_criteria>All five versioned keys read 1.62.1, both families are exercised by negative guard tests, and npm's resolved Playwright package trio is consistent in the candidate worktree.</acceptance_criteria>
  <fails_when>Any cache-key variant is missed, npm package trio diverges, unrelated dependency diff appears, or a tracked PNG changes.</fails_when>
  <done>The isolated candidate is ready for paired CI measurement, without changing the mainline package until an authorized merge path exists.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| npm registry to CI dependency install | Candidate package is external supply-chain input. |
| Comparator process to decision JSON | Tool stderr and exit status are untrusted until parsed together. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-04 | Tampering | npm upgrade | high | mitigate | Official package identity checkpoint and lockfile integrity; forbid flagged install without approval. |
| T-244-05 | Tampering | image comparator | high | mitigate | Pin and check Ubuntu package version; strict AE parsing and negative fixture tests. |
| T-244-SC | Tampering | npm install | high | mitigate | Block on `[SUS]`/`[ASSUMED]` legitimacy checkpoint; reject `[SLOP]`. |
</threat_model>

<verification>Run focused comparator and cache guard self-tests plus the existing `fast_checks` lane when the candidate CI run is dispatched.</verification>
<success_criteria>A candidate package/cache diff exists only behind a documented legitimacy gate and every nonzero or unknown pixel outcome is ineligible for merge.</success_criteria>
<output>Create `.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-02-SUMMARY.md`.</output>
