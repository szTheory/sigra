---
phase: 244-playwright-test-1-59-1-1-62-1-alone
plan: 04
type: execute
wave: 4
depends_on: [244-03]
files_modified:
  - .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
  - .planning/todos/pending/2026-09-26-playwright-1-62-1-measured-deferred.md
autonomous: false
requirements: [QUEUE-02]
estimate: {tokens: 17000, raw_tokens: 17000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-06: PR #213 is merged only when zero pixel drift, a live authorized latest head and all current required checks are proven; otherwise it is deferred with actionable measurement."
    - "D-07: stale August 8 failures are diagnostic history only."
    - "A closed unmerged PR with missing head ref remains deferred by default; restoration requires a separate explicit live-candidate decision."
  artifacts:
    - .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
  key_links:
    - "Measurement verdict + refreshed PR/head/check data -> fail-closed merge or defer decision -> durable reason"
---

<objective>
Apply the exact merge-or-defer rule to PR #213 and record the current state honestly.
Purpose: prevent a stale failed run or closed branch from being misread as permission to merge.
Output: explicit PR disposition and committed decision evidence, plus a deferred todo when merge is unavailable.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-CONTEXT.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-03-SUMMARY.md
@.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-CONTEXT.md
</context>

<tasks>
<task type="checkpoint:decision" gate="blocking-human" condition="only if deliberately restoring a closed or missing-head PR for the merge route">
  <name>Task 1: Gate any requested restoration of PR #213 on a live candidate</name>
  <decision>Whether an intentionally restored live PR #213 candidate may enter the merge route after an exact-zero measurement.</decision>
  <context>The PR was closed unmerged and its head ref was absent at research time. The default is defer; restoration needs a deliberate authorization tied to a current ref and SHA.</context>
  <options>
    <option id="defer"><name>Defer current closed PR</name><pros>Uses the committed measurement and preserves the actual PR state.</pros><cons>The dependency bump does not land in this phase.</cons></option>
    <option id="restore"><name>Authorize live candidate restoration</name><pros>Allows fresh latest-head checks and conditional merge if zero drift.</pros><cons>Requires a restored ref, current diff and new CI; approval alone does not satisfy any evidence gate.</cons></option>
  </options>
  <read_first>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json</read_first>
  <action>Per D-06/D-07, inspect PR #213 and its head ref again. Research found it closed unmerged, its head ref missing, and only an old red August 8 run. Those facts make the default branch `deferred_missing_live_candidate`; skip this conditional checkpoint and continue to Task 2 on that branch. Enter this checkpoint only if a maintainer explicitly proposes restoration for a zero-drift result. Before permitting that route, require the actual restored live ref, current base, exact candidate diff limited to the Playwright package/cache scope, latest head SHA, and fresh required checks. This plan does not authorize reopening or recreating the PR; absent the live candidate, merge remains blocked and the deferred outcome proceeds.</action>
  <what-built>A complete measurement and current PR state are recorded in the phase JSON.</what-built>
  <how-to-verify>For a deliberately requested merge route, identify the restored live candidate ref and its exact current SHA; approve restoration only with a current diff and fresh checks. The default deferred route needs no human action.</how-to-verify>
  <resume-signal>Explicit authorization naming the live candidate ref and SHA, or continue with defer.</resume-signal>
  <verify><automated>gh pr view 213 --repo szTheory/sigra --json number,state,mergedAt,headRefName,headRefOid,mergeable</automated></verify>
  <acceptance_criteria>The merge route has an intentionally restored live candidate and current identity, or the default defer route is selected without a restoration action.</acceptance_criteria>
  <fails_when>A missing ref, closed state or old SHA is treated as a live merge candidate.</fails_when>
  <done>The external-state fork is explicit before any disposition write.</done>
</task>

<task type="auto">
  <name>Task 2: Merge only on complete zero proof, otherwise defer with details</name>
  <reversibility rating="costly">A merge can be reverted but changes shared main history; only the exact zero and current-check gate permits it.</reversibility>
  <files>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json, .planning/todos/pending/2026-09-26-playwright-1-62-1-measured-deferred.md</files>
  <read_first>.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json; .planning/REQUIREMENTS.md; .planning/ROADMAP.md</read_first>
  <action>Per D-06/D-07, refresh main, PR #213, current head SHA, mergeability and required checks immediately before deciding. If and only if the full measurement is zero_drift, a live PR head is present and intentionally authorized, the PR has been refreshed against current main, the diff is limited to the isolated Playwright bump, and every required check on the latest PR SHA succeeded, merge #213 and capture the merge commit. Diagnose any failed current checks and repair deterministic failures on the candidate before reconsidering; retry a transient failure once. On any drift, inconclusive result, missing live candidate, closed PR, conflict, stale/failed/skipped check, or absent authorization for restoration, keep/mark #213 deferred. Do not reopen the closed PR automatically. Create or update a pending todo with source SHA, measuring run/artifact links, pre/post browser revisions, per-image mismatch summary, current PR state and an actionable next step; attach the measured reason to PR #213 in one idempotent comment keyed by run ID, even if it is already closed. If it is already closed, record that state instead of issuing a duplicate close. Update and commit the phase JSON with `pr_disposition`, exact decision inputs, decision time and merged/deferred state. Do not open a PNG recapture path or change any baseline.</action>
  <verify><automated>gh pr view 213 --repo szTheory/sigra --json number,state,mergedAt,mergeCommit,headRefOid | jq -e --slurpfile e .planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-PLAYWRIGHT-EVIDENCE.json '($e[0].measurement.source_sha | test("^[0-9a-f]{40}$")) and ($e[0].measurement.run_id | type == "number") and ((($e[0].pr_disposition.kind == "merged") and .state == "MERGED" and .mergedAt != null) or (($e[0].pr_disposition.kind == "deferred") and .state == "CLOSED" and .mergedAt == null))'</automated></verify>
  <acceptance_criteria>A merge has complete exact-zero and latest-head evidence; a defer has an actionable pending todo and a committed JSON reason. The PR state matches the recorded outcome.</acceptance_criteria>
  <fails_when>A merge is attempted on unproven pixels or missing/currently failed checks, the closed PR is treated as mergeable, or a defer lacks durable details.</fails_when>
  <done>PR #213 has an evidence-based disposition with no recapture obligation.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI evidence to GitHub merge action | A stale or incomplete proof must never authorize an irreversible merge. |
| GitHub PR state to disposition | Closed/missing refs and race changes must be re-read. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-09 | Elevation of privilege | merge decision | high | mitigate | Require zero_drift, live authorized ref, refreshed base and successful latest-head required checks. |
| T-244-10 | Repudiation | defer outcome | medium | mitigate | Commit decision inputs/receipt and pending todo; add measured PR detail when necessary. |
</threat_model>

<verification>Read live PR state at disposition time and validate the committed JSON. The historical red run and closed head are never accepted as fresh proof.</verification>
<success_criteria>PR #213 is merged only on complete exact-zero proof, otherwise durably deferred with current actionable evidence.</success_criteria>
<output>Create `.planning/phases/244-playwright-test-1-59-1-1-62-1-alone/244-04-SUMMARY.md`.</output>
