---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 28
type: execute
wave: 25
depends_on: [235.1-27]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Exact candidate `9ae29062`/tree `5c9e7636` is reused with no tracked or authority change."
    - "The outer project uses its real default candidate `_build/test` because `MIX_BUILD_PATH` is absent from the inherited environment; nested fixture checkouts retain their own `<checkout>/_build/dev`."
    - "A fresh untimed run of the exact relocation test and receipt/BEAM containment probes passes before the timed harness."
    - "The contaminated Plan 27 diagnostic worktree is verified then removed non-forcibly; no other worktree or unrelated dirt changes."
    - "One fresh timed invocation yields exactly three local pairs, followed by one attempt-1 capture pair and one disjoint validation pair."
  artifacts:
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
      provides: "Fresh PR evidence"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
      provides: "Fresh dispatch evidence"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
      provides: "Distinct validation evidence"
  key_links:
    - from: "outer candidate process"
      to: "candidate/_build/test"
      via: "MIX_ENV=test with MIX_BUILD_PATH explicitly unset"
      pattern: "env -u MIX_BUILD_PATH|_build/test"
    - from: "nested relocated checkout"
      to: "checkout/_build/dev"
      via: "InstallFixture.command_options/1 supplies checkout-owned MIX_BUILD_PATH"
      pattern: "validated compiler manifest relocation preserves a clean private build"
    - from: "Plan 26 candidate"
      to: "live branch"
      via: "unchanged commit/tree/parent"
      pattern: "9ae290624e483129d2c294366f01a1a83feec82b"
---

<objective>
Execute the unchanged exact candidate with default outer build ownership and private nested fixture builds, then capture and validate live evidence.

Purpose: Plan 27 proved complete path containment, but exporting one candidate-global build path overrode the install fixture's checkout-local build contract. Plan 28 removes that environment override while retaining all other offline isolation.
Output: Untimed outer/nested build-ownership proof plus strict PR, dispatch, and validation receipts—or durable blocked diagnostics.

Decision coverage: D-01 preserves the PR owner; D-02/D-03/D-04 preserve exhaustive comparable partitions; D-05/D-06 preserve exact-six dispatch; D-07 admits fresh evidence only; D-08 reserves reconciliation for Plan 05; D-09 preserves history, source, calibration, audit, and protected facts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.47-MILESTONE-AUDIT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-21-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-22-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-26-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-27-SUMMARY.md
@test/support/install_fixture.ex
@test/support/install_fixture_performance_test.exs
@scripts/ci/library-partitions.sh
@scripts/ci/library-partitions.test.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove default outer and checkout-private nested build ownership (D-02–D-04, D-07, D-09)</name>
  <files>No tracked files; explicit Plan 27 temporary worktree cleanup and a fresh candidate-local ignored environment only.</files>
  <precondition>Candidate `9ae290624e483129d2c294366f01a1a83feec82b` still has tree `5c9e76366145c9f1619bc60778794f687157fa91` and sole parent `47f9578fc97edc63603953d446e79fac884326c3`; PR #234 remains at that parent.</precondition>
  <action>Snapshot main unrelated porcelain/index/content and the complete registered worktree list. Resolve the Plan 27 diagnostic path only from `git worktree list --porcelain`; require it is detached at exact candidate, tracked-clean, beneath `/private/tmp/sigra-plan27.VLVjvS/candidate`, and not any other registered worktree. Recompute the retained combined/timing diagnostic hashes when present and match Plan 27 summary. Do not reuse its ignored build/dependency state. Remove only explicit candidate-owned ignored outputs after containment checks, then remove that worktree with non-force `git worktree remove`; block rather than force if tracked/unexpected state exists. Remove its now-empty temporary parent only if exact and empty. Do not prune or alter other worktrees.

Create a new external temporary detached worktree at the same candidate. Copy locked dependency sources and required local Mix/Hex/Rebar caches by Plan 27's real byte-copy/distinct-inode method. Retain candidate-local `MIX_DEPS_PATH`, `MIX_HOME`, `HEX_HOME`, `REBAR_CACHE_DIR`, `XDG_CACHE_HOME`, `TMPDIR`, `MIX_ENV=test`, `ASDF_ERLANG_VERSION=28.4.1`, and `HEX_OFFLINE=1`. Explicitly unset `MIX_BUILD_PATH` before every Mix, shell-test, and timed command; do not export it globally or pass it through an env wrapper. Require `Mix.Project.build_path()` for the outer project resolves exactly to the real non-symlink candidate `<root>/_build/test`. Reject preexisting `_build`, external symlinks, shared regular-file inodes, fetch attempts, or paths outside candidate. Resolve locked dependencies offline and compile from scratch into default `_build/test`.

Before timing, run exactly `test/support/install_fixture_performance_test.exs:283` under this environment. Require the named `validated compiler manifest relocation preserves a clean private build` test to run (not merely a different line-selected test), pass, report zero failures, and prove from its existing assertions that the relocated checkout owns `<checkout>/_build/dev`, `checkout.manifest_mode == :relocated`, unchanged compile skips, changed source recompiles once, and unknown manifest falls back. Assert `InstallFixture.command_options/1` supplies nested `MIX_BUILD_PATH` equal to `checkout.build_path`, while the parent process remains unset before and after. Scan surviving nested diagnostic paths when available and require containment under candidate-local TMPDIR.

Repeat Plan 27's BEAM compile-info/debug scan, generated-metadata binary scan, and focused formatter raw/normalized receipt probe; require no main/external prefix. Run all untimed portability, routing/preflight, calibration/source/audit, frozen lifecycle, independent verifier, protected, Actionlint, and formatting gates. Assert candidate/tree/parent, tracked cleanliness, calibration, 21/24 audit, and seven pins exact. Recompare main dirt and all non-Plan-27 registered worktrees byte-for-byte. Do not invoke `library-partitions.test.sh`, a full partition, GitHub, or network. Record cleanup, fresh environment, unset/build-path assertions, relocation test identity/results, and containment proof.</action>
  <verify>
    <automated>env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/install_fixture_performance_test.exs:283 &amp;&amp; env -u MIX_BUILD_PATH bash scripts/ci/library-partitions-portability.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh --preflight . &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
    <fails_when>Wrong relocation test runs; parent MIX_BUILD_PATH exists; outer build is not real default candidate `_build/test`; nested build is not checkout `_build/dev`; any containment/tree/audit/calibration/dirt/worktree invariant changes; or timed/network/GitHub work occurs.</fails_when>
  </verify>
  <done>The exact relocation scenario and receipt/BEAM probes prove default outer plus private nested build ownership in a fresh contained environment.</done>
</task>

<task type="auto">
  <name>Task 2: Run one fresh local validation and capture the unchanged candidate (D-01–D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json</files>
  <action>Reuse Task 1's fresh worktree/environment with `MIX_BUILD_PATH` still explicitly unset. Reassert exact candidate/tree/parent, default outer `_build/test`, relocation test, candidate-local deps/homes/cache/tmp, offline state, containment scans, source/calibration/audit, seven pins, tracked cleanliness, main dirt, and other worktrees.

Invoke `bash scripts/ci/library-partitions.test.sh` exactly once here and nowhere in verify, through `env -u MIX_BUILD_PATH` while retaining the other candidate-local variables. Its synthetic cases plus exactly three real runner/verifier pairs are the entire fresh local budget. Require both children green, exhaustive/disjoint 225 paths, normalized candidate-contained receipts, private nested fixture builds, and ratio `&lt;= 2.0` all three times. Failure blocks before push without retry, rebuild/rematerialization, environment substitution, source/calibration/audit edit, alternate candidate, or GitHub.

After success, fast-forward-push the unchanged candidate once and pair one dispatch with the automatic PR run at identical SHA. Apply AGENTS.md rate floor, 403/429 hard stop, one 60-second watcher/run, one structured summary, and failure logs only on failure. Deterministic failure blocks. One durable non-code transient may rerun same ID once via sequential 60-second view; attempt above 1 is non-admissible and blocks. Only dual original attempt-1 success may verify/download three PR and two dispatch artifacts and atomically write both receipts. Preserve ten-run ledger/digest and audit.</action>
  <verify>
    <automated>env -u MIX_BUILD_PATH bash scripts/ci/library-partitions-portability.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; actionlint .github/workflows/ci.yml &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Timed count is not one; MIX_BUILD_PATH leaks; relocation/containment/tree authority changes; a local pair fails/exceeds 2.0; live evidence is reused/cross-SHA/red/above attempt 1; watcher policy is violated; or prior evidence is admitted.</fails_when>
  </verify>
  <done>The unchanged candidate yields exactly three local pairs and one attempt-1 capture pair under correct nested ownership, or blocks without retry/substitution/replacement/tuning/waiver.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the evidence commit on one distinct pair (D-07–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <action>Commit exactly two capture receipts atop candidate; prove parent, only-two-file diff, source/calibration/audit/runner authority/ledger. Fast-forward-push once; pair one dispatch with automatic PR at evidence SHA under Task 2 rate/watcher/failure rules. Only dual original attempt-1 success writes validation. Require disjoint IDs, exact SHA/jobs/commands/authority, no retry/waiver. Run receipt-only checks without local timed rerun and leave audit to Plan 05.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Any check fails; authority drifts; a run is reused/cross-SHA/red/above attempt 1; watcher policy is violated; retry is admitted; or local timing reruns.</fails_when>
  </verify>
  <done>One distinct attempt-1 pair validates the evidence commit, or blocks without alternate evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parent Mix environment to nested fixture | Global build path must not override checkout-owned build state. |
| Diagnostic worktree to fresh attempt | Contaminated ignored outputs must not be reused. |
| Candidate to live branch | Exact unchanged commit/tree/parent must be preserved. |
| GitHub artifacts to receipts | External evidence requires provenance and digest replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-28-01 | Tampering | nested build ownership | high | mitigate | Parent env unset plus exact relocation test and checkout path assertions. |
| T-235.1-28-02 | Tampering | diagnostic reuse | high | mitigate | Verify then non-force-remove only exact Plan 27 worktree; create fresh temp worktree. |
| T-235.1-28-03 | Elevation of Privilege | candidate replacement | high | mitigate | Reuse exact 9ae29062/tree/parent; no tracked changes. |
| T-235.1-28-04 | Spoofing | live provenance | high | mitigate | Exact attempt/event/SHA/job and disjoint IDs. |
| T-235.1-28-05 | Denial of Service | polling | medium | mitigate | Rate floor, hard stops, one watcher, 60-second observation. |
| T-235.1-28-SC | Tampering | packages | medium | mitigate | Offline copied locked dependencies/caches; no network. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Correctly isolated strict dual-route proof | 28 | COVERED | Default outer/private nested builds and live stages. |
| REQ | TEST-01 | PR timing evidence | 28 | COVERED | Normalized receipts/live artifacts. |
| REQ | TEST-02 | Comparable exhaustive partitions | 28 | COVERED | Unchanged calibration/three pairs. |
| REQ | TEST-03 | Exact-six non-PR success | 28 | COVERED | Dispatch evidence. |
| RESEARCH | — | Candidate/nested build ownership | 28 | COVERED | Exact relocation gate. |
| CONTEXT | D-01–D-06 | Preserve routing contracts | 28 | COVERED | No tracked change. |
| CONTEXT | D-07–D-09 | Fresh evidence/history/reconciliation | 28, 05 | COVERED | Strict live then final docs. |
| CONTEXT | Deferred ideas | Unrelated work | — | EXCLUDED | Out of scope. |

<verification>
Prove default candidate outer build plus nested checkout-private build without timing, then invoke one three-pair harness and capture two strict live stages from unchanged candidate.
</verification>

<success_criteria>
- Exact candidate/tree/parent and authority bytes remain unchanged.
- Plan 27 contaminated worktree is safely retired; fresh environment uses no global MIX_BUILD_PATH.
- Exact relocation test plus metadata/receipt containment pass.
- One timed invocation yields three pairs.
- Capture and validation prove exact lineage.
- Failure blocks without retry, rematerialization, substitution, replacement, tuning, or waiver.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-28-SUMMARY.md` with cleanup/worktree inventories, environment/build ownership, relocation/containment results, unchanged candidate, three local results, live provenance, and final status.
</output>
