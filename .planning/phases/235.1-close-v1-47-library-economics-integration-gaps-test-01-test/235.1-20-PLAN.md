---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 20
type: execute
wave: 19
depends_on: [235.1-22]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The capture candidate's tree OID is exactly the Plan 22 calibration authority tree; it is not produced by merge, rebase, cherry-pick, or summary exception."
    - "Candidate-local source/manifest/replay contracts and exactly three fresh factor-of-two validations pass before any push."
    - "Plan 20 adds only receipt artifacts; all three Plan 21 frozen ordinary transition tests and both Plan 22 calibration files remain byte-identical through capture and validation."
    - "Exactly one new attempt-1 PR/dispatch capture pair shares the candidate SHA and both selected routes succeed before two receipts are admitted."
    - "Exactly one later attempt-1 PR/dispatch validation pair shares the evidence commit and is disjoint from the capture pair and all ten Plan 10/12/14 negative runs."
    - "A deterministic failure blocks immediately; a required single transient retry remains non-admissible and blocks after diagnosis under the one-watcher/rate rules."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh attempt-1 PR partition provenance and three verified artifacts"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh attempt-1 exact-six dispatch provenance and two verified artifacts"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct attempt-1 dual-route validation at the evidence commit"
  key_links:
    - from: "Plan 22 calibration authority commit"
      to: "capture candidate"
      via: "deterministic `git commit-tree` with old evidence head as parent and exact Plan 22 authority tree"
      pattern: "commit-tree|tree"
    - from: "Plan 18 closed negative ledger"
      to: "capture and validation receipts"
      via: "canonical ten-run digest `847f97542bd110be6022771098a54db9b4d8b0f75db2c32b923ee81834acf962`"
      pattern: "negative_runs_sha256"
    - from: "capture receipts"
      to: "validation pair"
      via: "exact evidence commit SHA and run-ID disjointness"
      pattern: "VALIDATION-RUN"
---

<objective>
Create an exact-tree evidence candidate from Plan 22 calibration authority, then capture and validate one strict dual-route evidence set.

Purpose: Plan 18's five-file cherry-pick proved drift controls, Plan 19 exposed a stale fixed-placement contract, Plan 21 froze assignment-agnostic source, and Plan 22 rebuilds source-bound calibration without the circular bootstrap gate. Plan 20 eliminates merge/candidate ambiguity entirely by giving the old evidence head a new commit whose tree is exactly Plan 22's calibration authority tree.
Output: One verified PR receipt, one verified exact-six dispatch receipt, and one distinct validation receipt—or durable blocked diagnostics with no replacement or waiver.

Decision coverage: D-01 preserves the sole PR owner; D-02/D-03/D-04 preserve exhaustive comparable partitions and same-run receipts; D-05/D-06 preserve the separate exact-six dispatch route; D-07 admits TEST truth only from fresh evidence; D-08 reserves reconciliation for Plan 05; D-09 preserves all ten negative runs and protected facts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-18-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-19-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-21-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-22-SUMMARY.md
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-routing-evidence.test.sh
@scripts/ci/verify-library-validation-run.sh
@scripts/ci/verify-library-validation-run.test.sh
@test/support/ci/library_test_partitions.exs
@test/support/ci/library_test_partitions_test.exs
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Build the exact-tree candidate and capture one pair (D-01–D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json</files>
  <precondition>PR #234 head `ci/phase-235-1-evidence` is still exactly `47f9578fc97edc63603953d446e79fac884326c3`; otherwise stop without constructing or pushing a replacement lineage.</precondition>
  <action>Read Plans 19 and 21 as immutable blocked history, then read Plan 22 summary and verify its recorded calibration authority commit exists, its tree matches the recorded tree, its calibration manifest/artifact/source-index links replay against Plan 21's frozen source, none of Plan 19 or Plan 21 diagnostic payloads were reused, and its three local validations each passed the unchanged factor-of-two bound. Confirm PR #234 evidence head remains exactly Plan 14 SHA `47f9578fc97edc63603953d446e79fac884326c3`; any different external head blocks without rewriting history.

Create the capture candidate with `git commit-tree`: use the exact Plan 22 calibration authority tree, old evidence head `47f9578fc97edc63603953d446e79fac884326c3` as its sole parent, fixed message `Phase 235.1 Plan 20 exact-tree evidence candidate`, and author/committer identity plus timestamps copied exactly from the Plan 22 calibration authority commit. Do not merge, rebase, cherry-pick, synthesize files, or permit any summary exception. Generate the object twice and require the same commit OID. Assert `candidate^{tree}` equals the Plan 22 authority tree and a recursive diff between candidate and that commit is empty. Create a clean scoped worktree at that candidate and recheck tree equality before and after every local command. Record SHA-256/blob IDs for the shared evidence-state helper and frozen `phase_198_contributor_dx_contract_test.exs`, `phase_235_1_library_economics_contract_test.exs`, and `phase_235_terminal_ratification_contract_test.exs` from Plan 21, plus both calibration files from Plan 22, and require those exact bytes before/after every receipt operation. If ignored dependencies/build outputs are absent, materialize only the locked graph with `ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix deps.get --check-locked`, then prove the tracked tree is unchanged. Preflight Bash major version at least 5 and, on macOS only, prepend the established `/opt/homebrew/bin` when needed; neither environment bootstrap may alter the candidate tree.

Inside the candidate run the routing/validation mutation suites, calibration loader/source-byte/manifest mutations, Phase 233/235.1 contracts, the synthetic independent-verifier mutation suite, protected FAST-01/GATE-05 verifiers, Actionlint, and formatting. Invoke `bash scripts/ci/library-partitions.test.sh` exactly once here; this is the only invocation in Task 1, and its synthetic checks plus exactly three real candidate-local runner/verifier pairs are the complete candidate validation budget. Each real pair must have both children successful, exact exhaustive/disjoint coverage, surviving receipts, and raw ratio `&lt;= 2.0`. The automated verification below performs only standalone non-timed checks and must not invoke the self-test or runner again. On any local failure, block before push with no retry, tuning, source/artifact edit, alternate candidate, or GitHub action.

Only after the exact-tree local chain passes, fast-forward-push the deterministic candidate once to `ci/phase-235-1-evidence`; its old head is the candidate's parent, so no force is allowed. Dispatch exactly one `gh workflow run CI --repo szTheory/sigra --ref ci/phase-235-1-evidence` and pair it with the one automatic PR run at the same candidate SHA. Before observing each run, call `gh api rate_limit --jq '.resources.core | {remaining,reset}'`; at 250 or fewer, stop polling until reset. HTTP 403/429 is a hard stop until the reported time. Start exactly one watcher total per run ID across all attempts with `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; never add another watcher or concurrent poller. Fetch one supported structured summary after each completed attempt and failed logs only after failure.

On deterministic selected-route/repository failure, record exact SHA/run/job/artifact/log diagnosis and block immediately with no repair, rerun, or replacement. Only a durably classified non-code GitHub infrastructure transient receives exactly one `gh run rerun RUN_ID --repo szTheory/sigra`. Because the run ID is reused, do not start another watcher; after a fresh rate preflight, use one executor-owned sequential `gh run view RUN_ID --repo szTheory/sigra --json attempt,status` monitor no more often than every 60 seconds. Attempt above 1 is diagnostic only and the pair remains blocked regardless of retry outcome; no replacement is allowed.

Only if both original attempt-1 selected routes succeed, download the three PR and two dispatch run/attempt-qualified artifacts into separate validated `mktemp -d` roots, verify locally, and atomically write both existing evidence schemas. Preserve byte-identical canonical `negative_runs` arrays and digest `847f97542bd110be6022771098a54db9b4d8b0f75db2c32b923ee81834acf962`; all ten prior runs remain `pair_inadmissible`. No Plan 14/18 artifact or local receipt may be reused.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; actionlint .github/workflows/ci.yml &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any verifier/test fails or reports zero tests; candidate tree differs from Plan 22's authority tree; any local pair is red/above 2.0; either live run is reused/cross-SHA/above attempt 1/selected-route red; a rate/watcher rule is violated; or failed/prior artifacts are admitted.</fails_when>
  </verify>
  <done>The deterministic candidate tree exactly equals Plan 22's calibration authority and either one dual-success attempt-1 pair yields two verified receipts or the stage blocks durably without replacement, repair, or waiver.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the evidence commit on one distinct pair (D-07–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <action>After Task 1 dual success, commit exactly the two evidence receipts on top of the exact-tree candidate. Prove the commit's parent is the candidate, its only tree differences are those two files, both verifiers pass, all Plan 21 frozen ordinary bytes and Plan 22 calibration/manifest bytes remain exact, and the canonical ten-run arrays/digest are identical. Fast-forward-push that exact evidence commit once; dispatch exactly one workflow_dispatch and pair it with the one automatic PR run at the same evidence SHA.

Apply Task 1's exact rate preflight, 250-request pause, 403/429 hard stop, one watcher total per run ID across attempts, 60-second watcher, one structured summary per completed attempt, and failed-logs-only-on-failure rules. Deterministic failure records durable diagnosis and blocks immediately without repair/rerun/replacement. A durably classified non-code transient is rerun once under the same run ID, observed only by one sequential 60-second `gh run view` monitor after rate preflight; attempt above 1 remains non-admissible and blocks the pair regardless.

Only dual original attempt-1 selected-route success may atomically write `235.1-VALIDATION-RUN.json`. Require both validation IDs distinct from each other, both capture IDs, and all ten negative IDs; exact evidence SHA equality; PR owner/aggregate success; dispatch exact-six job success; no skipped selected job; exact command/watcher/rate records; source/calibration equality; and no retry/waiver fields. Run the frozen `:receipt_contract` tests against the completed triple and require success. Do not run or weaken the default `:document_transition` assertion after writing validation: its expected red state is the explicit handoff to Plan 05, which alone may update non-calibrated prose.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Any command fails or reports zero tests; receipt/source bytes drift; a run ID is reused, cross-SHA, selected-route red, or above attempt 1; watcher/rate policy is violated; transient retry is admitted; or deterministic failure is repaired/replaced/waived.</fails_when>
  </verify>
  <done>One distinct dual-success attempt-1 validation pair proves the exact evidence commit and passes the frozen receipt-only contract while the expected document-transition handoff remains for Plan 05, or the plan blocks with durable diagnostics and no alternate evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 22 calibration authority to evidence branch | Any merge-produced tree drift can invalidate local proof. |
| Local candidate to GitHub runs | Mutable branch/run state must match the exact candidate SHA and tree. |
| GitHub artifacts to tracked receipts | External artifacts are untrusted until identity and payload replay pass. |
| Capture receipts to validation | Validation must use a later exact evidence commit and disjoint run IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-20-01 | Tampering | candidate tree | high | mitigate | Deterministic `commit-tree`, exact Plan 22 authority tree OID, empty recursive diff, and repeated OID construction. |
| T-235.1-20-02 | Spoofing | live run provenance | high | mitigate | Exact repository/event/attempt/SHA/selected-job identity and capture/validation/history disjointness. |
| T-235.1-20-03 | Tampering | downloaded artifacts | high | mitigate | Run/attempt-qualified names, private roots, local SHA-256, and independent verifiers. |
| T-235.1-20-04 | Elevation of Privilege | rerun/replacement | high | mitigate | One pair per stage; deterministic hard block; one transient rerun diagnostic only and pair-blocking. |
| T-235.1-20-05 | Denial of Service | GitHub polling | medium | mitigate | Rate preflight, 250 floor, 403/429 stop, one watcher total per run ID, and sequential 60-second retry monitor. |
| T-235.1-20-SC | Tampering | package supply chain | low | accept | No package, dependency, workflow, or action-pin change is authorized. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Fresh strict dual-route proof from exact local authority | 20 | COVERED | Tree-identical candidate, one capture pair, one validation pair. |
| REQ | TEST-01 | PR same-run timing evidence | 20 | COVERED | Owner/aggregate and three partition artifacts. |
| REQ | TEST-02 | Comparable exhaustive PR partitions | 20 | COVERED | Plan 22 calibration plus candidate-local/live verification. |
| REQ | TEST-03 | Exact-six non-PR success | 20 | COVERED | Selected dispatch job and two artifacts. |
| RESEARCH | — | Eliminate evidence worktree merge drift | 20 | COVERED | Exact Plan 22 authority tree via deterministic commit object. |
| CONTEXT | D-01–D-06 | Preserve owner, partition, and route contracts | 20 | COVERED | No code/workflow/topology/bound change. |
| CONTEXT | D-07–D-09 | Fresh evidence, immutable history, later reconciliation | 20, 05 | COVERED | Ten-run ledger plus two strict attempt-1 stages. |
| CONTEXT | Deferred ideas | Unrelated CI/auth/admin/release work | — | EXCLUDED | Outside this recovery. |

<verification>
Prove candidate/source tree equality, run the full local contract and exactly three local validation pairs, then validate one attempt-1 capture pair and one distinct attempt-1 evidence-SHA pair under AGENTS.md rate/watcher rules.
</verification>

<success_criteria>
- Candidate commit construction is reproducible and its tree is exactly the Plan 22 calibration authority tree with no exception.
- Capture/validation commits alter only the three declared receipt files; Plan 21 frozen ordinary bytes and Plan 22 calibration bytes remain exact.
- Candidate-local contracts and exactly three fresh partition pairs pass before push.
- Exactly one original attempt-1 capture pair produces two independently verified receipts containing the immutable ten-run ledger.
- Exactly one later original attempt-1 pair validates the exact evidence commit and is disjoint from all prior IDs.
- Deterministic failure blocks immediately; one required transient retry is observed without a second watcher, remains non-admissible, and blocks the pair.
- No merge/rebase/cherry-pick, force push, source/calibration change, failed artifact reuse, waiver, or replacement candidate occurs.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-20-SUMMARY.md` with Plans 19/21 blocked-history pins, Plan 21 frozen source, Plan 22 calibration authority commit/tree, deterministic candidate commit/tree proof, candidate-local results, capture/validation run/job/artifact provenance, rate/watcher records, and final receipt status.
</output>
