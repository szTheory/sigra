---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 17
subsystem: test-infrastructure
tags: [exunit, oban, partitions, calibration, tdd]
status: complete
dependency_graph:
  requires: [235.1-16]
  provides: [identity-safe-global-oban-tests, three-green-calibrated-validation-pairs]
  affects: [235.1-14, 235.1-05]
tech_stack:
  added: []
  patterns: [cleanup-before-acquisition, per-invocation-monitor, immutable-calibration-replay]
key_files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-17-SUMMARY.md
  modified:
    - test/sigra/account/deletion_test.exs
    - test/sigra/delivery_test.exs
    - scripts/ci/library-partitions.test.sh
    - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
decisions:
  - Serialize the only two tests that register the VM-global Oban name.
  - Give every cleanup invocation its own bounded monitor and exact identity guard.
  - Keep Plan 16 calibration and 97/128 placement immutable; admit exactly three fresh validation pairs.
metrics:
  duration: 18m
  completed: 2026-09-10
actuals:
  tokens: 3851
  tasks: 2
  commits: 5
---

# Phase 235.1 Plan 17: Identity-Safe Oban Tests and Calibrated Validation Summary

The only two tests that own the global `Oban` name are synchronous and use cleanup-before-acquisition with a fresh local monitor per invocation; the unchanged calibrated partitions then passed exactly three independent factor-of-two validations.

## TDD Evidence

### Task 1: Global Oban ownership

- RED retained Plan 16's whole-source collision (partition 1: 37,517ms success; partition 2: 26,409ms failure) and its focused iteration-2 reproduction. New adversarial tests then failed compilation because `acquire_dummy_oban/1` and `cleanup_dummy_oban/1` did not exist.
- GREEN changed only `DeletionTest` and `DeliveryTest` from `async: true` to `async: false` and installed identical local ownership contracts.
- Cleanup is registered before `Process.register/2`. Each invocation creates its own `Process.monitor/1`, unregisters only when the current owner is the captured PID, kills only a live captured PID, and waits at most 1,000ms for its exact `:DOWN` with `:killed` or `:noproc`.
- Registration failure synchronously cleans and reraises. Cross-process cleanup, repeated cleanup, failed acquisition with a foreign owner, and cleanup after rebinding all passed.
- Ten seed-varied focused runs passed with 34 tests and zero failures each. The tracer feedback gate independently repeated all ten runs with the same result.

### Task 2: Immutable calibration and fresh validations

- RED required the exact three-run loop while the existing integration contract still had only its former single real execution.
- GREEN retained each run's combined and two timing receipts in a private test root, independently invoked `verify-library-partitions.sh`, and stopped on the first failure.
- The contract rejects async regression, external/reused monitors, unconditional unregister, missing kill, non-local or unbounded `:DOWN`, missing `:noproc`, cleanup-after-register ordering, and failed-acquisition leakage for both modules. It also rejects validation-count/runner mutations, immutable-byte drift, and missing manifest paths.

## Immutable Plan 16 Authority

- Task 2 GREEN commit: `121d25f2`.
- Blocked summary commit: `84550d73`; the working copy is byte-identical to that commit and still records 37,517ms success, 26,409ms failure, and zero completed validation pairs.
- Calibration: SHA-256 `975612d7f3cbfda75fb6857791ebfd9bcadd852451b86a6b917871b3ba092eeb`, 2,922,739 bytes, 225 paths/costs, three samples, and nine payloads.
- Replay remained 97 paths in partition 1 and 128 in partition 2, with modeled totals of 54,838,062us each. Both repaired tests remain in partition 2.
- Loader, loader tests, runner, verifier, workflow, and all `lib/` production sources stayed byte-identical to approved HEAD `fd97522d`.

## Whole-Source Prerequisite

The one post-fix prerequisite completed both children successfully and retained both timing receipts. It is not one of the validation pairs.

| Partition | Duration | Conclusion | Exit |
| --- | ---: | --- | ---: |
| 1 | 50,429ms | success | 0 |
| 2 | 21,524ms | success | 0 |

Receipt SHA-256 values: combined `743ca78e145f2ae65e2de112685e03f6088ae73ae66f5382a8a21c1a1831a961`; partition 1 `102ed7d07967e0188afb2f1517a069f19a625af80e4ff09f9a869a6ec71b667d`; partition 2 `9974642df765c2cc4bcffcc989452cd5285ed05aa35cb71c441baf9d343cb6a7`.

## Exactly Three Fresh Validations

Each pair passed both children, preserved both timing receipts, and independently satisfied `max/min <= 2.0`. No pair was retried, averaged, retuned, recalibrated, or substituted.

| Validation | Partition 1 | Partition 2 | max/min | Combined SHA-256 | P1 SHA-256 | P2 SHA-256 |
| ---: | ---: | ---: | ---: | --- | --- | --- |
| 1 | 45,839ms | 46,304ms | 1.010 | `2bbebe9c7d9764814c77840f09ca89d284c2abe8e9e8423f0d6aff84d5d60edb` | `6feb434c7d35f57835d297dd7897a113905a08406f6b62f4905bfc977fc24d9f` | `50c73456300a2efe01e55cb1323a4362d58d7a3fd00e961c263cc135c3bc5093` |
| 2 | 28,360ms | 26,675ms | 1.063 | `88c71d6805ea9c4cf8e9769d1f3657df5cd2ed81b4272fca0eddf7f2c2f8f908` | `88e688fbed6daa95151cbdde9774532a0c20c9f14f675a4883bd730d58ec0cde` | `744f82dd6979c8db12a87aef2796521bf5fd6948f67514f96804ab2851f935c1` |
| 3 | 30,167ms | 30,610ms | 1.015 | `7d3eaf862ace9821901fcae3f96d8aad227bc52c1b70aff02e861b69a173e21e` | `f047f19fcc2e9330e9477a33e5da70d304ccfcb6a0d8777203a9822589adc8dc` | `5d49f9d5e0c6e56817d3c850aa82f661c9c395a800860c2052ff17d1b2587ab0` |

## Verification

- `verify-library-partitions.test.sh`: PASS.
- Focused formatter/partition/Phase 233/Phase 235.1 suite: 73 tests, 0 failures.
- FAST-01 source-complete offline attestation: verified.
- GATE-05 terminal ratification offline attestation: verified.
- `mix format --check-formatted`: PASS.
- No GitHub workflow, API, branch, PR, or live-evidence action occurred.

## Commits

1. `bd9e8c45` — RED: adversarial Oban cleanup ownership tests.
2. `385a2e74` — GREEN: synchronous identity-safe dummy Oban ownership.
3. `4c9fc890` — RED: immutable exact-three validation contract.
4. `76f715d5` — GREEN: three fresh calibrated validation executions.
5. `5e9264cf` — REFACTOR: canonical contract formatting.

## Deviations from Plan

None. The unrelated `.tool-versions` worktree edit required explicit `ASDF_ERLANG_VERSION=28.4.1` on verifier commands; it was neither modified nor committed. The prerequisite's children and receipts were its only admission gate, while the unchanged ratio predicate was applied independently to each of the three subsequent validation pairs.

## Known Stubs

None.

## Self-Check: PASSED

- All four modified implementation/test files and this summary exist.
- All five task commits exist in order with both RED and GREEN gates plus the optional REFACTOR gate.
- Plan 16 immutable hashes/history, the prerequisite receipt, and all three validation records were re-read from disk/logs.
