---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 34
type: tdd
wave: 31
depends_on: [235.1-33]
files_modified:
  - test/sigra/auth_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-02]
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The authentication telemetry assertion accepts only its own user-5 login-stop event even while unrelated async tests emit the same global event name."
    - "A deterministic foreign user-1 event reproduces the old first-message failure and proves the filtered handler without sleeps or module-wide serialization."
    - "Only `test/sigra/auth_test.exs` changes from candidate `63910813`; production telemetry behavior, source membership, runner, workflow, audit, and active pins remain unchanged."
    - "A deterministic exact-source commit/tree with sole parent `47f9578` re-freezes all 225 ordinary test bytes before any new measurement."
    - "Plan 29 calibration becomes stale source history and cannot authorize later validation after the ordinary-source edit."
  artifacts:
    - path: test/sigra/auth_test.exs
      provides: "Identity-filtered async login telemetry assertion with deterministic foreign-event regression"
    - path: /private/tmp/sigra-plan34-source-authority.json
      provides: "Replayable exact source commit/tree and one-file-diff receipt"
  key_links:
    - from: "unique telemetry handler"
      to: "authenticate/3 login-stop assertion"
      via: "exact event plus user_id/failed_attempts_before filter"
      pattern: "telemetry.attach|user_id: 5|failed_attempts_before: 3"
    - from: "repaired ordinary source"
      to: "Plan 35 calibration"
      via: "exact 225-path commit/tree snapshot"
      pattern: "source_snapshot"
---

<objective>
Remove the deterministic async telemetry cross-test race and re-freeze the exact ordinary source that must be recalibrated.

Purpose: Plan 33's sole recovery run proved the PostgreSQL timeout transient, then stopped when `Sigra.AuthTest` consumed `Sigra.TelemetryTest`'s user-1 login-stop event instead of its own user-5 event. The event bus is global, but the assertion's identity is local and can be filtered without serializing the entire test module.
Output: One TDD-hardened ordinary test plus a deterministic source-authority commit/tree for fresh calibration.

Decision coverage: D-01–D-06 preserve routing and timing contracts; D-07 requires fresh evidence after the source edit; D-08 defers reconciliation; D-09 preserves all failure and protected history.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
@/Users/jon/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-17-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-29-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-33-SUMMARY.md
@test/sigra/auth_test.exs
@test/sigra/telemetry_test.exs
@lib/sigra/auth.ex
@lib/sigra/telemetry.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Correlate the async login telemetry assertion by identity (D-02–D-04, D-09)</name>
  <files>test/sigra/auth_test.exs</files>
  <behavior>
    - RED: after subscription but before `Auth.authenticate/3`, emit the same login-stop event for user 1; the old unfiltered `:telemetry_test` assertion consumes it and fails against expected user 5.
    - GREEN: a unique per-test handler forwards only the exact login-stop event whose metadata has `user_id: 5` and `failed_attempts_before: 3`; the injected user-1 event is ignored and the authenticating call's event is asserted.
    - The handler ID is unique, detaches through `on_exit`, its catch-all callback returns `:ok`, and the test process receives no uncorrelated login message.
    - `Sigra.AuthTest` and `Sigra.TelemetryTest` remain async; production auth/telemetry code and all other tests remain byte-identical.
  </behavior>
  <action>On exact candidate `63910813519db336d5e4556b885cdc0f3879799f`, preserve Plan 33's seed-884906 failure and user-5-versus-user-1 metadata as RED history. In the existing `authenticate/3 emits login telemetry event on success` test, first inject a foreign `[:sigra, :auth, :login, :stop]` event with user 1 and a non-target failed-attempt count after the subscription is active. Confirm the existing broad handler/assertion fails deterministically on that foreign event and commit RED.

For GREEN, replace only this test's `:telemetry_test.attach_event_handlers/2` subscription with a unique `:telemetry.attach/4` handler ID containing the module, test PID, and a fresh reference. Its callback must send to the test process only when both the event name and metadata identity equal user 5 plus `failed_attempts_before: 3`; all foreign/malformed metadata returns `:ok` without sending. Register exact-ID detach in `on_exit`, invoke the unchanged authentication path, assert the correlated message and exact metadata, and refute any uncorrelated handler message. Keep both modules `async: true`: serialization is not the fix because an identity-safe subscriber is sufficient and preserves suite parallelism. Do not add sleeps, mailbox flushing, recursive receives, timing changes, production edits, or shared global locks. Run the focused two-file seed-884906 command and commit GREEN separately.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/auth_test.exs test/sigra/telemetry_test.exs --seed 884906</automated>
    <fails_when>The focused run fails or reports zero tests; the foreign event can satisfy the target assertion; handler identity/filter/detach/catch-all is missing; either module becomes synchronous; a sleep/mailbox flush/global lock appears; or any production/other-test byte changes.</fails_when>
  </verify>
  <done>The deterministic foreign event fails before GREEN and is ignored after GREEN, while the exact user-5 event passes under unchanged async execution.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the one-file successor as new source authority (D-02, D-07, D-09)</name>
  <files>No additional tracked files; `/private/tmp/sigra-plan34-source-authority.json` is external evidence.</files>
  <precondition>Task 1 GREEN changes only `test/sigra/auth_test.exs` from candidate `63910813519db336d5e4556b885cdc0f3879799f`/tree `bd81cf764c901d3e2f728febe1240b57db7a2d7a`.</precondition>
  <action>Snapshot and preserve unrelated main dirt and registered worktrees. Starting from exact Plan 32 candidate tree `bd81cf764c901d3e2f728febe1240b57db7a2d7a`, overlay only Task 1's GREEN `test/sigra/auth_test.exs` blob and construct the source-authority commit twice with fixed author/committer/message/timestamps and sole parent `47f9578fc97edc63603953d446e79fac884326c3`. Require identical commit/tree results, exactly one recursive changed path from candidate `63910813`, the same 225 tracked ordinary paths, and byte identity for the other 224 source files plus every runner/loader/verifier/workflow/audit/pin file.

Write `/private/tmp/sigra-plan34-source-authority.json` with schema, base SHA/tree, new source SHA/tree, sole parent, both derivation results, exact changed-path array, ordinary count, source-index membership digest, new auth-test byte count/SHA-256, unchanged-path count, and booleans for production/routing/audit/pin preservation. Explicitly mark Plan 29 artifact `8d54ea4d...` and manifest `61ecf707...` as stale for the changed source snapshot: do not mutate or use them to validate the successor, collect timings, invoke either partition runner, change assignment, or perform GitHub action. Plan 35 alone rebuilds calibration from the new snapshot.</action>
  <verify>
    <automated>jq -e '.schema_version == "sigra.plan34.source-authority/v1" and .base.sha == "63910813519db336d5e4556b885cdc0f3879799f" and .base.tree == "bd81cf764c901d3e2f728febe1240b57db7a2d7a" and .source.sole_parent == "47f9578fc97edc63603953d446e79fac884326c3" and (.source.sha | test("^[0-9a-f]{40}$")) and (.source.tree | test("^[0-9a-f]{40}$")) and .derivations_match == true and .changed_paths == ["test/sigra/auth_test.exs"] and .ordinary_path_count == 225 and .unchanged_ordinary_path_count == 224 and (.ordinary_source_index_sha256 | test("^[0-9a-f]{64}$")) and (.auth_test.byte_count &gt; 0) and (.auth_test.sha256 | test("^[0-9a-f]{64}$")) and .production_unchanged == true and .routing_unchanged == true and .workflow_unchanged == true and .audit_unchanged == true and .active_pins_unchanged == true and .plan29_calibration_status == "stale_source_history" and .partition_runner_invocations == 0 and .github_actions == 0' /private/tmp/sigra-plan34-source-authority.json</automated>
    <fails_when>The receipt is missing/wrong; derivations differ; lineage, tree, membership, source hashes, or one-file diff fails; a protected byte changes; stale calibration is treated as authority; a partition runner executes; or any GitHub action occurs.</fails_when>
  </verify>
  <done>One exact 225-path source snapshot with a sole auth-test delta is reproducible and ready for fresh calibration; no timing payload or live action exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Global telemetry bus to test mailbox | Concurrent emitters share an event name but not assertion identity. |
| Test-only fix to source authority | One ordinary-byte change invalidates the prior source-bound calibration. |
| Dirty main tree to exact candidate | Unrelated user state must not enter the synthetic snapshot. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-34-01 | Spoofing | login telemetry assertion | high | mitigate | Handler filters exact event and user/attempt identity; foreign event is deterministic negative control. |
| T-235.1-34-02 | Tampering | source snapshot | high | mitigate | Two deterministic derivations, sole parent, one-path diff, and 225-file replay. |
| T-235.1-34-03 | Repudiation | stale calibration | high | mitigate | Receipt marks old artifact/manifest non-authoritative before measurement. |
| T-235.1-34-SC | Tampering | packages | low | accept | No package or dependency change. |
</threat_model>

## Multi-Source Coverage Audit
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reliable exhaustive ordinary execution | 34 → 35 | COVERED | Identity-safe test precedes fresh calibration. |
| REQ | TEST-02 | Comparable ordinary partitions | 34, 35 | COVERED | New source is frozen then measured. |
| RESEARCH | — | Test-global ownership/isolation | 34 | COVERED | Exact metadata correlation, analogous to Plan 17 identity safety. |
| CONTEXT | D-01–D-09 | Preserve routing/evidence/protected truth | 34, 35, 05 | COVERED | Only one test byte changes here. |

<verification>Deterministic RED/GREEN foreign-event coverage plus exact one-file source-authority replay; zero partition timing and GitHub action.</verification>
<success_criteria>- Foreign user-1 event cannot satisfy user-5 assertion.
- Both modules remain async and production is unchanged.
- Exact source commit/tree has sole parent and one changed path.
- All 225 paths are frozen; old calibration is stale history.
- Zero timing payloads and GitHub actions.</success_criteria>
<output>Create `235.1-34-SUMMARY.md` with RED/GREEN commits, exact handler contract, source-authority receipt/commit/tree, one-file diff, stale-calibration status, and zero timing/GitHub counts.</output>
