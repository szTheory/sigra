---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 19
type: execute
wave: 16
depends_on: [235.1-18]
files_modified:
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
  - test/support/ci/library_test_partitions.exs
  - test/support/ci/library_test_partitions_test.exs
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Scaffold discovery reads only tracked `test/**/*_test.exs` paths through NUL-delimited Git output and never traverses ignored or untracked trees."
    - "Every ordinary calibration path is bound to its exact current regular-file byte count and SHA-256; source drift, symlinks, missing paths, or digest substitutions fail closed."
    - "Stable contract code reads calibration hash/size from an external exact-schema manifest, so source code is finalized and committed before any calibration sample is collected."
    - "Exactly three new successful source triples produce one replayable nine-payload calibration, followed by exactly three fresh successful validation pairs that each satisfy the unchanged factor-of-two bound."
    - "Plan 18 remains blocked local history at 2,449ms/13,579ms with no GitHub run IDs or evidence receipts."
  artifacts:
    - path: "test/sigra/planning/phase_233_library_economics_contract_test.exs"
      provides: "Tracked NUL-safe scaffold discovery and ignored-tree regression"
    - path: "test/support/ci/library_test_partitions.exs"
      provides: "Fail-closed calibration loader with ordinary source-byte verification"
    - path: "test/support/ci/library_test_partitions_test.exs"
      provides: "Independent replay and source/artifact/manifest mutation coverage"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json"
      provides: "Three source triples, nine bounded raw payloads, exact source index, and derived medians"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json"
      provides: "External artifact digest/size and source-snapshot linkage"
  key_links:
    - from: "tracked scaffold discovery"
      to: "Phase 233/235.1 contracts"
      via: "`git -C ROOT ls-files -z -- :(glob)test/**/*_test.exs` parsed without shell/newline splitting"
      pattern: "ls-files|-z"
    - from: "ordinary source index"
      to: "calibration loader"
      via: "exact path/count/byte-count/SHA-256 recomputation against source snapshot and current bytes"
      pattern: "source_sha256|byte_count"
    - from: "calibration manifest"
      to: "calibration artifact"
      via: "exact relative path, byte count, SHA-256, source commit/tree, and canonical source-index digest"
      pattern: "PARTITION-CALIBRATION-MANIFEST"
---

<objective>
Remove ignored-tree timing pollution, bind calibration to exact ordinary source bytes, and rebuild comparable partitions from a finalized source snapshot.

Purpose: Plan 18 proved the candidate machinery but exposed a deterministic stale calibration: Phase 233's contract was costed at 49,731,475us because `Path.wildcard/1` traversed an ignored 363MB Playwright symlink tree, while the clean candidate executed it in 255,179us and measured 2,449ms/13,579ms partitions.
Output: Tracked-only scaffold discovery, source-byte-bound replayable calibration plus external digest manifest, and three independent local factor-of-two validations. No GitHub or live evidence action.

Decision coverage: D-01 preserves the single PR owner; D-02/D-03/D-04 preserve exhaustive ordinary partitions and same-run timings; D-07 keeps TEST truth closed until later live evidence; D-09 preserves protected history and prevents source drift.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-18-SUMMARY.md
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
@test/support/ci/library_test_partitions.exs
@test/support/ci/library_test_partitions_test.exs
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Discover scaffold tests only from tracked NUL-safe paths (D-02, D-04, D-09)</name>
  <files>test/sigra/planning/phase_233_library_economics_contract_test.exs, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - Phase 233's defective wildcard and Phase 235.1's newline-delimited variant both use the same tracked NUL-safe discovery contract; a tracked scaffold test is found exactly once while tracked ordinary and ignored/untracked scaffold-looking files are excluded.
    - Spaces and embedded newlines in tracked test filenames remain one path because discovery parses NUL delimiters, not lines or shell output.
    - A hermetic ignored directory containing a sparse 363MB sentinel, a nested scaffold-looking file, and a symlink self-loop is never traversed and cannot change discovery time/result.
    - Git failure, malformed/non-test paths, missing tracked files, symlinks, duplicates, or a scaffold-set mismatch fail explicitly.
    - Phase 235.1 source contracts retain Plan 18 summary `81afbf0c`, exact candidate/local failure values, ten-run history, and zero new GitHub/evidence facts before calibration begins.
  </behavior>
  <action>Start RED in the Phase 233 contract by extracting scaffold discovery behind a repository-root argument and exercising a temporary Git repository. Track ordinary/scaffold files including space and newline names; ignore a synthetic Playwright-like directory containing a sparse 363MB file, nested false scaffold files, and a self-loop symlink. Assert bounded completion and an exact tracked result without relying on sleeps. Replace `Path.wildcard("test/**/*_test.exs")` with `System.cmd/3` invoking `git`, `-C`, the validated root, `ls-files`, `-z`, `--`, and `:(glob)test/**/*_test.exs` as separate arguments. Require exit status zero, split only on NUL, validate repository-relative regular non-symlink paths, read only those tracked files, detect `@moduletag :scaffold`, deduplicate, and sort lexically. Do not invoke a shell, newline parser, filesystem glob, `find`, or ignored/untracked traversal.

Apply the same argument-vector `ls-files -z` and NUL parsing to Phase 235.1's existing newline-delimited `git ls-files test` helper so both source contracts remain safe for tracked newline paths; keep their exact scaffold result identical. Before any calibration work, update the Phase 235.1 contract to pin Plan 18 summary commit `81afbf0cafcf7dcf380d728a03053c42cdccdaa0`, GREEN commit `627428df5a20dc0479163b9993a49dccf5e6f92e`, candidate `39f53ca09f74e3bc5c385da7213bc1ce3953dcd8`/tree `71d14c82105cc8e48f018999bd3f02d2c9dc4a47`, validation 1 durations 2,449ms/13,579ms and ratio 5.544, both child successes, stopped validations 2/3, and zero Plan 18 pushes/runs/evidence receipts. Preserve the canonical ten-run digest `847f97542bd110be6022771098a54db9b4d8b0f75db2c32b923ee81834acf962`. Remove no prior negative facts. Run RED then GREEN and commit this bounded discovery/history slice before proceeding.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
    <fails_when>ExUnit exits nonzero or reports zero tests; discovery uses a glob/shell/newline split; an ignored/untracked/symlink path is read; the sparse/self-loop fixture changes the exact set; Git/path failure is masked; or any Plan 18 fact is rewritten as success.</fails_when>
  </verify>
  <done>Tracked NUL-safe discovery returns the exact scaffold set without touching the ignored 363MB/self-loop tree, and Plan 18's local block is immutable source-contract history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind calibration to a finalized ordinary-source snapshot (D-01–D-04, D-09)</name>
  <files>test/support/ci/library_test_partitions.exs, test/support/ci/library_test_partitions_test.exs, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - Calibration schema is exactly `sigra.library-partition-calibration/v2`; its source binds the frozen implementation commit and Plan 18 blocked summary `81afbf0cafcf7dcf380d728a03053c42cdccdaa0`.
    - Calibration schema contains one lexical source row for every exact ordinary path with exact keys `path`, `byte_count`, and `sha256`.
    - Loader independently recomputes every current/source-snapshot file's bytes and rejects missing, stale, duplicate, extra, symlink, nonregular, size, digest, universe, or commit/tree mutations.
    - Stable contract code obtains calibration path/hash/size and source snapshot from an external exact-schema manifest; no artifact digest or byte count is hardcoded in code that contributes to the calibration universe.
    - Manifest/artifact/source-index links reject every single-field, key, order, digest, size, path, commit, tree, and source-byte mutation.
  </behavior>
  <action>Start RED by evolving the artifact schema to exactly `sigra.library-partition-calibration/v2`. Retain source keys `implementation_commit`, `blocked_summary_commit`, `collection_command`, `sample_count`, and `aggregation`; bind the dynamic implementation commit to the external manifest and pin blocked summary to `81afbf0cafcf7dcf380d728a03053c42cdccdaa0`, sample count 3, the exact runner command, and `median(max(1,sum(time_us)))`. Make `current_ordinary_paths!/1` use the same argument-vector `git -C ROOT ls-files -z -- :(glob)test/**/*_test.exs` and NUL parser, with exact lexical uniqueness and regular non-symlink validation; include tracked space/newline and ignored-tree adversaries. Extend `ordinary_universe` with `source_files`, exactly one lexical row per ordinary path and exact row keys `path`, `byte_count`, `sha256`; retain exact set/disjointness and replace the ambiguous newline universe digest with SHA-256 over lexical paths joined by NUL plus a terminal NUL. Add a default external manifest path and injectable manifest/repository-root options for hermetic mutations. Define manifest schema `sigra.library-partition-calibration-manifest/v1` with exactly `schema_version`, `source_snapshot`, `calibration`, `ordinary_source_index_sha256`, `sample_count`, and `payload_count`; nested `source_snapshot` has exactly `commit` and `tree`, while `calibration` has exactly `path`, `byte_count`, and `sha256`. Require 40/64 lowercase hex forms, exact repository-relative calibration path, counts 3/9, regular non-symlink files, and canonical `jq -cS` plus LF source-index hashing.

Validate each ordinary current file against its source row and the same blob at manifest `source_snapshot.commit`, verify that commit's tree equals the manifest tree, and bind calibration `source.implementation_commit` to the manifest commit. The loader must independently decode/replay all nine retained payloads, direct per-file timing sums, `max(1,sum)`, medians, exact universe, and derived rows as before. In the Phase 235.1 contract replace the old in-code calibration byte-count/SHA literal with strict manifest parsing and artifact verification; retain fixed workflow/topology/threshold protections. Add hermetic mutations for one current source byte, snapshot blob, path, size, digest, source-index order/member, manifest key/value, artifact bytes, and symlink.

After all Task 1/2 test and loader code is GREEN under hermetic injected manifest/artifact fixtures, run formatting and the discovery/loader unit prerequisites, then create one source-snapshot commit. Assert it includes every intended Plan 18 summary/source-contract update and all loader/test code, leaves the prior calibration artifact untouched, excludes the new manifest, and leaves no tracked source change. Record its exact commit/tree and lexical ordinary path/digest index in a validated temporary handoff.

After that commit, generate a temporary bootstrap calibration at the default artifact/manifest paths so the stable default integration contract and runner are executable before new samples exist. Transform the prior artifact without changing its nine payload bytes or derived placement, add the new exact source index and source-snapshot identity, and generate a matching external manifest; mark both working-tree files `bootstrap_assignment_only` in the execution record and never commit them. This bootstrap is allowed only to choose exhaustive partitions for new collection; none of its timings, costs, hashes, or payloads may enter the final Plan 19 artifact. Run the full Phase 235.1/default-loader contract against it. On any failure, restore the prior tracked artifact, remove the untracked manifest, and block. No test/script/source file may change after the snapshot; a required change blocks and restarts Plan 19 before sampling.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any command exits nonzero or reports zero tests; a source/manifest/artifact mutation passes; digest/size remains hardcoded in an ordinary test; replay weakens; the snapshot omits a source update or commits a calibration output; bootstrap bytes enter final provenance; or source changes remain after commit.</fails_when>
  </verify>
  <done>Stable code and Plan 18 contracts are finalized in one source snapshot, and the loader fail-closes on any ordinary source, snapshot, manifest, artifact, or replay drift.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild replayable calibration and prove three fresh pairs (D-02–D-04, D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json</files>
  <action>Verify the Task 2 source-snapshot handoff, unchanged tracked source files, and explicitly marked uncommitted bootstrap artifact/manifest before collection. Preflight Bash major version at least 5 because the established runner uses `mapfile`; on macOS prepend the already-installed `/opt/homebrew/bin` only when that resolves Bash 5+, without changing tracked files. Discard every Plan 16 calibration sample as superseded history after using its placement only for bootstrap assignment; do not use its payloads or Plan 18's failed validation as a source sample. Execute `ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test bash scripts/ci/library-partitions.sh` exactly three times from the snapshot. Each collection must independently have two successful children, exhaustive/disjoint exact paths, both timing receipts, and one combined receipt. On any failure restore the prior tracked artifact/remove the bootstrap manifest and stop with zero new calibration output; do not retry, replace, average, or use a partial triple. Copy each successful run's one combined plus two timing payloads immediately into a validated private directory, recording ordinal/path/byte count/SHA-256 before the next overwrite.

Only after three successes, rebuild `235.1-PARTITION-CALIBRATION.json` with exactly those nine bounded payloads, the exact source snapshot commit, and one source-file digest row for every current ordinary path. Independently decode each payload, verify combined/timing linkage and exact coverage, sum raw `.tests[].time_us` rows per file, apply `max(1,sum)`, take the deterministic median across three, and compare every derived row. Create `235.1-PARTITION-CALIBRATION-MANIFEST.json` from the completed artifact with the exact Task 2 schema, artifact path/size/SHA-256, source commit/tree, canonical source-index digest, and counts 3/9. Re-run the loader/mutation suites, commit only the artifact and manifest as the final Plan 19 source/evidence-preflight commit, and record its exact commit/tree separately from the earlier source snapshot.

From that exact final commit execute the unchanged runner and independent verifier exactly three fresh times. Every run must have both children success/zero, surviving combined plus timing receipts, exact exhaustive/disjoint universe, and raw ratio `max(duration_ms)/min(duration_ms) &lt;= 2.0` independently. Retain each run's three payload hashes/durations in the Plan 19 summary. Any failure blocks with no retry, tuning, recollection, averaging, source edit, artifact rewrite, or live action.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; bash scripts/ci/library-partitions.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.sh &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any command exits nonzero or reports zero tests; collection is not exactly three complete triples/nine payloads; source bytes differ from snapshot; artifact/manifest/replay linkage fails; any of exactly three later pairs is missing/red/above 2.0; or a retry/tune/live action occurs.</fails_when>
  </verify>
  <done>One final commit contains a source-byte-bound three-sample/nine-payload calibration and external digest manifest, and exactly three later fresh pairs independently pass the unchanged factor-of-two bound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git index to test discovery | Filesystem content outside tracked test paths is untrusted and may be cyclic or huge. |
| Source snapshot to calibration | Timing samples are valid only for the exact ordinary test bytes they measured. |
| Raw receipts to derived costs | Embedded payloads and derived medians must be independently replayable. |
| Calibration artifact to stable contract | Artifact size/hash must be externally bound without source self-reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-19-01 | Denial of Service | scaffold discovery | high | mitigate | NUL-safe `git ls-files` discovery plus ignored sparse-tree/self-loop regression; no filesystem glob traversal. |
| T-235.1-19-02 | Tampering | ordinary source bytes | high | mitigate | Exact per-path size/SHA rows checked against current files and source-snapshot blobs. |
| T-235.1-19-03 | Repudiation | calibration provenance | high | mitigate | External exact-schema manifest binds source commit/tree, artifact path/size/hash, source-index digest, and 3/9 counts. |
| T-235.1-19-04 | Tampering | raw timing replay | high | mitigate | Retain nine payloads; independently verify hashes/schema/linkage/direct sums/max/median and mutate every pole. |
| T-235.1-19-05 | Elevation of Privilege | retry or retune | high | mitigate | Exactly three collection triples and three later validations; first failure blocks without substitution or rewrite. |
| T-235.1-19-SC | Tampering | package supply chain | low | accept | No package, dependency, or action-pin change is authorized. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Comparable exhaustive ordinary partitions without weakening evidence | 19 | COVERED | Correct discovery cost, exact source provenance, replay, and three validations. |
| REQ | TEST-01 | Same-run deterministic timing remains available | 19 | COVERED | Existing per-test receipts retained and replayed. |
| REQ | TEST-02 | Two exhaustive comparable ordinary partitions | 19 | COVERED | Unchanged factor-of-two bound over exact current universe. |
| RESEARCH | — | Avoid ignored Playwright traversal | 19 | COVERED | Tracked NUL-safe Git discovery with self-loop/large-tree regression. |
| RESEARCH | — | Prevent stale calibration after source drift | 19 | COVERED | Per-file byte digests plus source commit/tree. |
| CONTEXT | D-01–D-04 | Preserve owner, exhaustive partitions, and timing | 19 | COVERED | No topology, test-set, or bound change. |
| CONTEXT | D-07/D-09 | Fail closed and preserve history | 19 | COVERED | Local only; Plan 18 remains blocked and no evidence is admitted. |
| CONTEXT | Deferred ideas | Unrelated CI/auth/admin/release work | — | EXCLUDED | Outside this recovery. |

<verification>
Run the tracked-discovery regressions, exact source/manifest/artifact mutation suites, independent nine-payload replay, and exactly three fresh post-calibration partition validations. Confirm GitHub/evidence state is untouched.
</verification>

<success_criteria>
- Phase 233 scaffold discovery uses only NUL-delimited tracked test paths and ignores the hermetic sparse 363MB/self-loop tree.
- All Plan 18 summary/source contract updates and calibration code are committed before collection; no source byte changes afterward.
- Calibration includes exact byte count/SHA-256 for every ordinary path and independently rejects any current/snapshot/source-index mutation.
- An external exact-schema manifest binds the calibration artifact without an in-code artifact hash/size self-reference.
- Exactly three successful source triples create nine retained payloads, and exactly three later fresh validations each pass `&lt;= 2.0` without retry or tuning.
- Plan 18's 2,449ms/13,579ms block, ten negative runs, and zero new live/evidence facts remain intact.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-19-SUMMARY.md` with source snapshot commit/tree, all source-index/artifact/manifest digests, three collection triples, final source/evidence-preflight commit/tree, three validation pairs, and explicit no-GitHub status.
</output>
