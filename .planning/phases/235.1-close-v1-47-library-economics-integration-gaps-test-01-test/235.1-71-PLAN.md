---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 71
type: execute
gap_closure: true
wave: 68
depends_on: [235.1-69]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Plan 69 and /private/tmp/sigra-p2351-plan69-recovery.G9QrBacu remain immutable failed history; no Plan 69 launcher, allocator, collector, dry run, database, timing, terminal, or GitHub action is retried or reused."
    - "Before allocation, both pinned Python runtimes execute the same pure structural/TDD gate: the unchanged authenticated health SQL is accepted with exactly one E-string tab escape, and every other backslash, psql meta-command, or write-capable statement is rejected."
    - "The current collector test uses the Plan 67 parser boundary and a Plan 71 wrapper marker/receipt delimiter; stale Plan 46 wrapper naming cannot authorize current evidence."
    - "Per D-01 through D-04, the sole PR MIX_ENV=test mix ci owner runs ordinary partition 1 and ordinary partition 2 sequentially while ExUnit remains parallel inside each partition, and those same executions emit deterministic per-test timings."
    - "TEST-02 requires exactly two nonempty exhaustive ordinary receipts with positive integer durations, stable partition order, and independently recomputed raw max/min ratio at most 2.0; missing, duplicate, extra, reordered, zero, negative, or first-beyond-bound evidence fails closed."
    - "Per the D-05/D-06 operator supersession, schedule and workflow_dispatch alone execute the exact six scaffold modules; missing, skipped, duplicated, substituted, or PR-owned scaffold evidence fails closed."
    - "Only after the dual-runtime zero-allocation gate passes may one distinct Plan 71 root run one local evidence chain, followed by one fresh attempt-1 capture pair and one distinct attempt-1 validation pair under the unchanged no-retry rules."
    - "Per D-07 through D-09, the Plan 71 receipts prove TEST-01/02/03 without changing FAST-01 source-complete n=52/p50=469 facts or the 93-row GATE-05 ledger."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json"
      provides: "Canonical zero-allocation, dual-runtime source/SQL-policy proof and complete fixture receipts"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh automatic-PR attempt-1 partition evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh workflow_dispatch attempt-1 exact-six evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct attempt-1 PR/dispatch validation pair bound to the evidence commit"
    - path: "/private/tmp/sigra-p2351-plan71-sql-guard.XXXXXXXX"
      provides: "One private no-follow runtime root with a complete authenticated manifest and terminal local PASS"
  key_links:
    - from: "235.1-69-DRY-RUN-FAILURE.json"
      to: "Plan 71 pure SQL policy"
      via: "the exact offset-960 failure becomes the RED fixture; the GREEN policy authenticates the unchanged SQL and permits only its E-string tab escape"
    - from: "Plan 68 embedded sources and Plan 69 generated-source authority"
      to: "Plan 71 launcher/root package"
      via: "no-follow byte authentication, exact source-set regeneration, byte counts, SHA-256 rows, and current-namespace rebinding"
    - from: "collector/health.sql"
      to: "/opt/homebrew/bin/psql"
      via: "exact three-statement read-only framing, parsed escape/meta-command policy, authenticated SHA-256, fixed argv, and the Plan 67 output parser"
    - from: "ordinary partition runner"
      to: "Sigra.CI.ExUnitTimingFormatter"
      via: "same-run partition 1 then partition 2 invocations with stable lexical ordering for equal timings"
    - from: "GitHub run artifacts"
      to: "the three tracked Plan 71 evidence receipts"
      via: "exact repository, ref, SHA, event, attempt, workflow, job, artifact, partition, and exact-six checks"
  assumptions:
    - requirement_id: "TEST-01"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      statement: "FLAGGED/UNRESOLVED: the spec-less probe supplied no TEST-01 edge descriptor; D-01/D-03/D-04 and the same-run fail-closed receipt contracts remain authoritative."
    - requirement_id: "TEST-03"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      statement: "FLAGGED/UNRESOLVED: the spec-less probe supplied no TEST-03 edge descriptor; the exact-six non-PR receiver contract remains authoritative."
  prohibitions:
    - requirement_id: "TEST-01"
      status: "unverified"
      flagged: true
      statement: "Completion must not be claimed from a serial, second, or timing-only rerun instead of the same ordinary executions."
    - requirement_id: "TEST-02"
      status: "unverified"
      flagged: true
      statement: "Comparability must not be claimed from averaged, retried, relabeled, reordered, or threshold-relaxed timing evidence."
    - requirement_id: "TEST-03"
      status: "unverified"
      flagged: true
      statement: "CI speed must not be obtained by omitting or weakening any exact-six module or by counting a missing/skipped receiver as green."
---

<objective>
Repair Plan 69's overbroad SQL guard before allocation, then produce one fresh authenticated local and live library-economics evidence chain.

Purpose: Preserve Plan 69's deterministic failure as immutable evidence while replacing only the unsafe blanket backslash classification with a source-authenticated parsed allowlist, proving it under both pinned runtimes, and resuming the unchanged TEST-01/02/03 route once.
Output: A canonical Plan 71 preallocation proof, one distinct terminal-PASS root, two fresh capture receipts, and one distinct validation receipt.

Decision coverage: D-01 through D-09 and every operator/recovery addendum remain binding in Tasks 1–2.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-67-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-DRY-RUN-FAILURE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

## Artifacts this phase produces

- New tracked file: `235.1-71-PREALLOCATION-VALIDATION.json`.
- New tracked files after local admission: `235.1-PR-EVIDENCE.json`, `235.1-SCAFFOLD-EVIDENCE.json`, and `235.1-VALIDATION-RUN.json`.
- New runtime symbols: `validate_read_only_sql`, `build_plan71_authority`, `verify_plan71_terminal`, and Plan 71 launcher/driver/allocator/finalizer/verifier entry points.
- New private root: `/private/tmp/sigra-p2351-plan71-sql-guard.XXXXXXXX`, including exclusive allocation/finalization/action markers, source and artifact manifests, structural fixture receipts, the local evidence chain, and one terminal PASS.

<assumption_delta_decision>
Primary noun: library-economics evidence route.
Decision: no-change.
Rationale: the repair changes only the pre-psql classification of one authenticated SQL escape and current wrapper naming; it does not change the two ordinary partitions, exact-six route, evidence identity model, or D-01 through D-09.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the authenticated E-string escape through a zero-allocation dual-runtime gate (D-01–D-04, D-07, D-09)</name>
  <reversibility rating="costly">The pure policy is locally replaceable, but once its receipt authorizes allocation the successor consumes a one-shot evidence root and downstream action budget.</reversibility>
  <precondition>The 28,671-byte `235.1-69-PLAN.md` hashes to `fcd527d9f2de5997d39b8021b921b348814ab134447706a30bd8513bcae09dfb`; its 14,852-byte preallocation JSON hashes to `34e8eb84a2825f4aec308f1a68e6c46eb2ceab9f0dc140a6216e0b8e00e23f21`; its 2,489-byte dry-run failure JSON hashes to `843d0686d69a7729b2ee1a77ce237954f33999aace84f912e3d6d72aad023ebc`; its 11,108-byte summary hashes to `35559f07642d8bbd2f987b3da47d797a1f482b3b69d877e09951b0af0e99b677`; `/private/tmp/sigra-p2351-plan69-recovery.G9QrBacu` retains mode `0700` and dev/inode `16777229/296047679`, its 35-byte marker retains mode `0600` and dev/inode `16777229/296047680`, and no directory matches `/private/tmp/sigra-p2351-plan71-sql-guard.[A-Za-z0-9]{8}`.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json</files>
  <read_first>
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-SUMMARY.md` — treat its root, one allocation, one dry run, zero psql children, zero database/timing/GitHub actions, and no-retry decision as exact predecessor state.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-DRY-RUN-FAILURE.json` — use byte offset 960, forbidden-token classification, unchanged health SQL identity, and the independent 440-byte/zero-stderr read-only psql diagnosis as the RED authority.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-PREALLOCATION-VALIDATION.json` — authenticate all 23 inherited/generated source rows and both pinned runtime identities; do not treat its earlier PASS as SQL-policy success.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PLAN.md` and `235.1-68-PREALLOCATION-VALIDATION.json` — extract authenticated embedded source bytes and source-generation contracts without invoking any Plan 68 entry point.
    - `/private/tmp/sigra-p2351-plan69-recovery.G9QrBacu/collector/health-collector.sh`, `collector/health.sql`, and `collector/tests/run.sh` — inspect read-only only after matching the tracked failure/source identities; never execute, copy, chmod, touch, or otherwise reuse these frozen files.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md` — preserve D-01 through D-09, exact attempt-1/no-retry routing, and protected facts.
  </read_first>
  <behavior>
    - "RED: the authenticated Plan 69 collector policy rejects the unchanged 1,653-byte health SQL at byte offset 960 solely because the required E-string tab token contains a backslash; the structural test launches no subprocess and creates no root."
    - "GREEN: `validate_read_only_sql` accepts the exact SHA-256 `f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9` three-statement bundle and exactly one backslash token: the `\t` inside the complete `E'SIGRA_PLAN45_HEALTH_V1\t'` literal."
    - "The accepted-sentinel control passes; a second or displaced tab token, newline/hex/doubled/outside-literal backslash, psql copy/gexec/shell/include/set command, or COMMIT/INSERT/UPDATE/DELETE/CREATE/ALTER/DROP/TRUNCATE/COPY/DO/CALL/SELECT-INTO mutation fails before subprocess launch."
    - "The Plan 67 parser begin/end anchors remain the parser extraction authority, while the current exclusive collector wrapper marker and receipt use Plan 71 naming and reject stale Plan 46 delimiter bytes."
    - "Python 3.9.6 and Python 3.14.4 compile and run the same accepted/rejected matrix with identical semantic results, zero allocation/action counts, and unchanged Plan 69/71 root inventories."
  </behavior>
  <action>Authenticate the committed Plan 68 embedded source table, the Plan 69 plan/preallocation/failure/summary files, all 23 Plan 69 generated-source rows, both pinned interpreter executables, candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3`, and the complete 24-descendant Plan 69 root through lstat, `O_RDONLY|O_NOFOLLOW`, fstat dev/inode/type equality, byte counts, SHA-256, duplicate-key rejection, and canonical JSON. Treat the root only as immutable failure evidence. Reconstruct every successor source in memory from authenticated committed inputs and declared transformations; never import, execute, or copy an executable from the frozen Plan 69 root, never perform a blind namespace replacement, and regenerate an exact lexical source-authority table from the resulting bytes.

Write a structural RED test around the actual pre-psql policy entry: feed the authenticated health SQL bytes to the inherited blanket classifier, require the exact offset-960 rejection, replace subprocess launch with a counter that remains zero, and prove Plan 69/71 root inventories stay unchanged. In GREEN, extract the policy into one standard-library pure `validate_read_only_sql(bytes, expected_identity)` function used by the production dry-run and admission paths. First require authenticated `collector/health.sql` to remain exactly 1,653 bytes with SHA-256 `f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9`, and require the exact top-level sequence `BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ READ ONLY;`, one SELECT statement, and `ROLLBACK;`. Lex the SQL with explicit ordinary-string, E-string, quoted-identifier, line-comment, block-comment, and dollar-quote states; do not use a blanket byte-substring exception. Permit exactly one escape token, `\t`, only inside the complete E-string literal `E'SIGRA_PLAN45_HEALTH_V1\t'`; reject every other backslash or malformed lexical state. Reject all line-leading psql meta-commands and any top-level write-capable/transaction-ending statement beyond the exact framing. Keep `collector/health.sql` byte-identical.

Create one accepted fixture named `authenticated_e_string_tab_sentinel` and exact rejected fixtures named `second_tab_escape`, `non_sentinel_tab_escape`, `newline_escape`, `hex_escape`, `doubled_backslash`, `backslash_outside_e_string`, `psql_copy`, `psql_gexec`, `psql_shell`, `psql_include`, `psql_set`, `write_commit`, `write_insert`, `write_update`, `write_delete`, `write_create`, `write_alter`, `write_drop`, `write_truncate`, `write_copy`, `write_do`, `write_call`, and `select_into`. Each fixture calls the same production pure function, records accepted/status/failure class plus input bytes/hash, asserts zero subprocess calls, and rejects duplicate names or outcomes. Preserve the exact `PLAN67_PSQL_PARSER_BEGIN`/`PLAN67_PSQL_PARSER_END` extraction anchors. Change the current collector wrapper marker and test receipt delimiter to the exact bytes `plan71-collector-verify\n`, bind the surrounding receipt schema/ownership to Plan 71 while retaining the `PLAN67_PSQL_PARSER_BEGIN`/`PLAN67_PSQL_PARSER_END` parser authority, add a rejection for the stale exact bytes `plan46-collector-verify\n`, and require no current Plan 71 generated source or receipt to advertise Plan 46 ownership; historical Plan 46 records remain unchanged.

Run compile/import, source-table, policy RED/GREEN, fixture-matrix, definition-dominance, environment, host-path, finalizer-import, cache, no-follow, allocator-manifest, and validate-only launcher probes under both pinned interpreters. Reopen every generated source from its in-memory authority and require exact accepted/rejected fixture names and identical semantic digests. No probe may invoke allocator, mktemp, finalizer, psql, database, local evidence, timing, network, or GitHub work. Atomically write canonical `235.1-71-PREALLOCATION-VALIDATION.json` with the predecessor identities, full generated source table, SQL identity/allowed-token location, RED result, fixture rows, runtime receipts, action counts, before/after root inventories, and receipt digest; reopen and independently verify it before Task 1 completes.</action>
  <acceptance_criteria>The canonical authenticated SQL passes the current production policy under both runtimes; each of the exact 23 named hostile fixtures appears exactly once and fails with zero subprocess calls; stale Plan 46 wrapper bytes fail while the Plan 67 parser boundary and Plan 71 wrapper authority agree; the tracked validation receipt authenticates every inherited/generated source and reports all allocator/mktemp/root/psql/database/timing/network/GitHub counts as zero; Plan 69 is byte-identical and no Plan 71 root exists.</acceptance_criteria>
  <verify>
    <automated>PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json &amp;&amp; for PY in /Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/bin/python3.9 /opt/homebrew/Cellar/python@3.14/3.14.4/Frameworks/Python.framework/Versions/3.14/bin/python3.14; do "$PY" -c 'import json,sys; raw=open(sys.argv[1],"rb").read(); d=json.loads(raw,object_pairs_hook=lambda p: dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate"))); assert raw==(json.dumps(d,sort_keys=True,separators=(",",":"))+"\n").encode(); assert d["schema"]=="sigra.phase235.1-plan71-preallocation-validation/v1" and d["status"]=="PASS"; assert d["sql_policy"]["accepted"]==["authenticated_e_string_tab_sentinel"]; rejected=d["sql_policy"]["rejected"]; names=[r["name"] for r in rejected]; expected={"second_tab_escape","non_sentinel_tab_escape","newline_escape","hex_escape","doubled_backslash","backslash_outside_e_string","psql_copy","psql_gexec","psql_shell","psql_include","psql_set","write_commit","write_insert","write_update","write_delete","write_create","write_alter","write_drop","write_truncate","write_copy","write_do","write_call","select_into"}; assert len(rejected)==23 and len(names)==len(set(names))==23 and set(names)==expected; assert all(not r["accepted"] and r["subprocess_calls"]==0 for r in rejected); assert set(d["counts"].values())=={0}; assert d["root_inventory_before"]==d["root_inventory_after"]==[]; assert len(d["runtimes"])==2 and all(r["status"]==0 and r["stderr_bytes"]==0 for r in d["runtimes"])' "$PV" || exit; done &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan71-sql-guard.*' -print -quit)" &amp;&amp; test "$(shasum -a 256 /private/tmp/sigra-p2351-plan69-recovery.G9QrBacu/local/dry-run-failure.json | awk '{print $1}')" = 843d0686d69a7729b2ee1a77ce237954f33999aace84f912e3d6d72aad023ebc</automated>
    <fails_when>The receipt is missing/noncanonical/duplicate-keyed; a source/runtime identity differs; the accepted control fails; any hostile case passes or launches a subprocess; current wrapper naming is stale; any action count is nonzero; a Plan 71 root exists; or any Plan 69 byte/stat identity changes.</fails_when>
  </verify>
  <done>The exact collector defect is red-to-green under both runtimes with a closed authenticated escape policy and no one-shot allocation or evidence budget consumed.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate one fresh root, complete one local chain, and capture both attempt-1 route pairs (D-01–D-09)</name>
  <reversibility rating="costly">The runtime files are disposable, but the allocator, local observations, pushes, dispatches, and workflow attempts are unique evidence events that cannot be recreated under the same identities.</reversibility>
  <precondition>Task 1's canonical `235.1-71-PREALLOCATION-VALIDATION.json` independently passes, reports both pinned runtimes green and every action count zero, no Plan 71 root exists, the frozen Plan 69 root and failure receipt are unchanged, GitHub authentication is valid, and repo `szTheory/sigra`, PR #234, base `main`, head `refs/heads/ci/phase-235-1-evidence`, remote head `87338db7`, and candidate `3713dcd4` still match the inherited pre-remote contract.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <read_first>
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json` — authenticate the sole allocation authority and every generated source before invoking the launcher.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-SUMMARY.md` and `235.1-69-DRY-RUN-FAILURE.json` — preserve the frozen root and all consumed/unused budget facts; no current success may cite them.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PLAN.md` Task 3 and `235.1-69-PLAN.md` Task 2 — inherit exact candidate/evidence lineage, excluded historical IDs, run/job/artifact predicates, terminal verification, and four-run budget without invoking predecessor entry points.
    - `AGENTS.md` — enforce one watcher per workflow run, 60-second watch intervals, one structured summary, failure-only log fetches, and REST rate-limit hard stops.
    - `.github/workflows/ci.yml`, `scripts/ci/verify-library-routing-evidence.sh`, `scripts/ci/verify-library-validation-run.sh`, and `test/sigra/planning/phase_235_1_library_economics_contract_test.exs` — preserve current topology, schemas, exact-six and protected-value predicates.
  </read_first>
  <action>Authenticate and replay only the read-only Plan 71 preallocation receipt; do not rerun Task 1's fixture driver as an execution feedback hook. Reconstruct the Plan 71 package in memory from the same authenticated committed inputs and declared transformations, require every reconstructed byte count and SHA-256 to equal the receipt's complete source-authority table, and invoke that authenticated execute launcher exactly once. Its allocator may call mktemp exactly once with `/private/tmp/sigra-p2351-plan71-sql-guard.XXXXXXXX`, create exactly one mode-0700 root and one exclusive mode-0600 allocation marker, write canonical driver/handoff receipts, materialize every source from the authenticated Plan 71 source table, and finalize the complete no-follow authority before local work. Reopen the frozen Plan 69 root before and after each budgeted action and halt on any byte/type/mode/dev/inode change.

Inside the sole Plan 71 root, run one current local chain: the challenge-bound PostgreSQL 14 host gate, all 19 Plan 67 parser fixtures, the accepted SQL-policy control and 23 hostile cases through the production policy, the isolated environment matrix, one real dry run, one current candidate/database observation, one admission, one health sample, and one timing command that contains exactly three complete validation pairs. The dry run and admission use the unchanged authenticated health SQL, fixed psql argv, exact BEGIN/sentinel/ROLLBACK parser, and no current Plan 46 wrapper identity. Require six green partition children, the exact 225 ordinary paths split by the current manifests as 114/111, nine fresh ledger-disjoint payloads, positive integer raw durations, stable partition order, and independent ratio at most 2.0 for every pair. Require the non-PR route to retain exactly `Sigra.UpgradeIntegrationTest`, `Sigra.Install.GoldenDiffTest`, `Sigra.Install.Features.PasskeysJsTest`, `Sigra.Install.GeneratorPasskeysOptOutTest`, `Sigra.Install.IdempotencyTest`, and `Sigra.Install.VaultPromotionTest`. Seal every descendant exactly once in `local/artifact-manifest.json`, write one canonical terminal PASS, and make the independent `local/verify-plan71-terminal.py` reopen and validate the full root without mutation.

Only after the terminal verifier passes, query `gh api rate_limit` once before the watches. At REST core remaining 250 or less, record a durable blocked diagnostic with reset time and make no further polling request until reset; HTTP 403/429 is a hard stop until GitHub's reported reset/retry time. Give each run exactly one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`, fetch its structured summary once after completion, and fetch logs only for a failed conclusion. Retry exactly one durably classified non-rate-limit infrastructure transient once under the inherited policy; the retry is diagnostic/non-admissible and leaves its pair blocked. Never rerun, substitute, repair in place, waive, or auto-approve a deterministic failure.

Exclude every retained Plan 35–69 run/job ID. Push candidate `3713dcd4` once to the sole PR head, capture its new automatic pull_request attempt-1 run, and dispatch one distinct workflow_dispatch attempt-1 run at the same SHA. Require exact repository/PR/base/head/SHA/event/workflow/job/attempt identities, successful selected routes and required checks, independent artifact decoding, two exhaustive ordinary partitions with raw ratio at most 2.0, and the exact six modules only on workflow_dispatch. Atomically write and no-follow reopen the PR/scaffold capture receipts; commit exactly those two files as the candidate's direct child and push that evidence commit once. Capture one new automatic pull_request attempt-1 validation run and one distinct workflow_dispatch attempt-1 validation run at the evidence SHA. Write `235.1-VALIDATION-RUN.json` only after all four runs pass, binding capture/validation identities, candidate/evidence lineage, Plan 71 root/terminal identity, 225-path 114/111 topology, all ratios, and exact-six truth. Do not push another commit or reconcile prose.</action>
  <acceptance_criteria>Exactly one Plan 71 root and one local chain exist; the local manifest/terminal receipt authenticate every descendant and all three comparable pairs; Plan 69 remains unchanged; exactly four fresh distinct non-excluded attempt-1 runs prove capture PR, capture workflow_dispatch, validation PR, and validation workflow_dispatch; the evidence commit is the candidate's direct child with only the two capture JSON files changed; and both unchanged offline evidence verifiers pass.</acceptance_criteria>
  <verify>
    <automated>PLAN71_ROOT="$(/usr/bin/python3 -c 'import os,re; rows=sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan71-sql-guard\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False)); assert len(rows)==1; print(rows[0])')" &amp;&amp; /usr/bin/python3 "$PLAN71_ROOT/local/verify-plan71-terminal.py" --root "$PLAN71_ROOT" --preallocation-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json --predecessor-root /private/tmp/sigra-p2351-plan69-recovery.G9QrBacu &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Root discovery returns zero/multiple matches; Plan 69 changes; any source, policy, receipt, manifest, ledger, partition, ratio, child, or exact-six predicate fails; a one-shot action repeats; a GitHub run is stale/excluded/retried/wrong-ref/wrong-SHA/non-green; evidence lineage differs; or either offline verifier exits nonzero.</fails_when>
  </verify>
  <done>One fresh source-authenticated Plan 71 chain proves the complete local, PR, and non-PR routes and leaves canonical capture/validation receipts as the sole new authority for Plan 70 reconciliation.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Sole PR owner; two exhaustive comparable ordinary partitions; parallel same-run timing; exact-six non-PR scaffold; protect FAST-01/GATE-05 | Task 1 closes the pre-psql blocker without allocation; Task 2 runs the unchanged local and live route exactly once. |
| REQ | TEST-01, TEST-02, TEST-03 | All three are in frontmatter; Task 2 proves same-run timing, partition comparability, exact-six routing, and fresh capture/validation evidence. |
| RESEARCH | Fixed commands, independent raw verification, fail-closed artifacts, two-stage closure, no new dependency | Tasks 1–2 use standard-library parsing, existing source/verifier patterns, and delayed reconciliation. |
| CONTEXT | D-01 through D-09 plus all operator/recovery addenda | Task names/actions cite every decision set and preserve topology, exact-six routing, attempt-1 evidence, delayed truth updates, and protected facts. |
| VALIDATION | Structural adverse tests, local evidence, live capture, distinct validation, final receipt contract | Task 1 provides dual-runtime zero-action structural evidence; Task 2 provides the local terminal PASS and both live pairs. |
| LATEST EVIDENCE | Plan 69 rejected the required escape before psql; direct read-only psql proved healthy framing | Task 1 targets this exact root cause and authenticates both observations without reusing the failed root. |
| HISTORY | Plans 01–69 and every consumed root/run budget | Neither task edits/reclassifies predecessor plans or summaries; Plan 69 remains explicit frozen failure history. |
| CONTEXT deferred | Unrelated release, installer, auth, security, admin, and other CI work | Excluded; no deferred item is implemented. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated predecessor sources to generated Plan 71 code | Executable bytes are transformed into a new one-shot package. |
| SQL bundle to psql | A local SQL file crosses from static policy into a database client. |
| Private root to tracked evidence | Local receipts authorize remote capture and later completion claims. |
| GitHub run/artifact data to repository receipts | External identities/artifacts can be stale, substituted, malformed, or replayed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-71-01 | Tampering | SQL policy and health bundle | high | mitigate | Task 1 pins exact bytes/hash/framing, uses one production lexical policy, permits one location-bound E-string tab token, rejects named hostile mutations, and proves zero subprocess calls. |
| T-235.1-71-02 | Spoofing | Generated source/root authority | high | mitigate | Tasks 1–2 authenticate every inherited/generated source, interpreter, root, marker, and manifest path using no-follow stat identity plus SHA-256 and canonical JSON. |
| T-235.1-71-03 | Repudiation | One-shot action accounting | medium | mitigate | Task 2 uses exclusive markers, canonical action/sequence ledgers, exact counts, immutable Plan 69 comparisons, and a complete terminal manifest. |
| T-235.1-71-04 | Spoofing | GitHub run/job/artifact identity | high | mitigate | Task 2 binds exact repo/PR/ref/SHA/event/attempt/workflow/job/artifact identities, excludes all historical IDs, and runs both existing independent verifiers. |
| T-235.1-71-05 | Information Disclosure | Runtime and GitHub receipts | low | accept | Receipts persist only allowlisted identities, hashes, redacted environment facts, and public run metadata; no tokens, passwords, headers, or cookies are serialized. |
| T-235.1-71-06 | Denial of Service | CI polling and bounded evidence actions | low | accept | Four-run maximum, one watcher per run, 60-second interval, rate stops, and at most one transient GitHub API-request retry that never reruns or increments the attempt of a workflow bound operational exposure. |
</threat_model>

<verification>
Task 1 must finish with a canonical dual-runtime receipt and zero allocation/psql/database/timing/network/GitHub actions. Task 2 may then consume exactly one fresh root/local chain and the inherited four-run live budget. Final verification is read-only: the Plan 71 terminal verifier plus the unchanged routing, validation, and receipt-contract authorities; it does not repeat any allocator, dry run, database observation, admission, timing command, push, dispatch, or workflow run.
</verification>

<success_criteria>
Plan 71 succeeds only when the exact authenticated E-string tab escape passes the production SQL policy under both pinned runtimes, every hostile escape/meta/write fixture fails before subprocess launch, stale Plan 46 wrapper authority is gone from current sources, Plan 69 remains frozen, one distinct Plan 71 root carries a terminal local PASS, and four fresh attempt-1 runs produce canonical receipts accepted by both unchanged evidence verifiers.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-SUMMARY.md` when done.
</output>
