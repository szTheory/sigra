---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 69
type: execute
gap_closure: true
wave: 66
depends_on: [235.1-12, 235.1-68]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-PREALLOCATION-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 through D-04, the sole PR MIX_ENV=test mix ci owner runs ordinary partition 1 and ordinary partition 2 sequentially while ExUnit remains parallel inside each partition, and the same execution emits deterministic per-test timings."
    - "TEST-02 adjacency: equal positive durations and a raw max/min ratio exactly 2.0 pass, while the first representable integer-millisecond case beyond 2.0 fails closed."
    - "TEST-02 empty input: exactly two nonempty partition receipts with positive integer durations are required; null, absent, empty, zero, negative, duplicate, or extra partition evidence fails closed."
    - "TEST-02 ordering: partition 1 precedes partition 2 under the owner, path assignment is canonical, and equal timing observations retain the formatter's stable lexical tie-break."
    - "Per D-05 and D-06 as superseded by the operator addendum, only schedule and workflow_dispatch execute the exact six scaffold modules, and missing, skipped, duplicated, or substituted modules cannot produce green evidence."
    - "Plan 68 remains a frozen marker-only rejected predecessor; Plan 69 uses a distinct authenticated launcher/root namespace and fixes the definition-order defect before spending one allocation, one local route, or any GitHub budget."
    - "Per D-07 through D-09, fresh attempt-1 PR and workflow_dispatch capture runs plus a distinct attempt-1 validation pair prove TEST-01/02/03 without changing FAST-01 or the 93-row GATE-05 ledger."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-PREALLOCATION-VALIDATION.json"
      provides: "Canonical zero-allocation dual-runtime proof for the corrected Plan 69 successor package"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh automatic-PR attempt-1 run, job, artifact, timing, and partition evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh workflow_dispatch attempt-1 exact-six scaffold evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct PR/dispatch validation pair bound to the captured evidence commit"
    - path: "/private/tmp/sigra-p2351-plan69-recovery.XXXXXXXX"
      provides: "One private no-follow runtime root whose artifact manifest exhaustively names every generated descendant and whose terminal receipt is the sole local PASS authority"
  key_links:
    - from: "235.1-68-PLAN.md authenticated embedded sources"
      to: "Plan 69 successor launcher"
      via: "byte/hash verification, AST definition-order repair, current-namespace rebinding, and regeneration of source authority from the resulting bytes"
    - from: "ordinary partition runner"
      to: "Sigra.CI.ExUnitTimingFormatter"
      via: "same-run partition 1 then partition 2 invocations with stable lexical ordering for equal timings"
    - from: ".github/workflows/ci.yml"
      to: "six canonical scaffold modules"
      via: "the fail-closed schedule/workflow_dispatch receiver and exact module-set verification"
    - from: "GitHub run artifacts"
      to: "tracked evidence JSON"
      via: "exact repository, PR, ref, SHA, event, attempt, workflow, job, artifact, and semantic receipt checks"
  assumptions:
    - requirement_id: "TEST-01"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      statement: "FLAGGED/UNRESOLVED: the spec-less probe supplied no TEST-01 edge descriptor; the locked same-run timing and fail-closed receipt contracts remain the planning authority."
    - requirement_id: "TEST-03"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      statement: "FLAGGED/UNRESOLVED: the spec-less probe supplied no TEST-03 edge descriptor; the locked exact-six non-PR receiver contract remains the planning authority."
  prohibitions:
    - requirement_id: "TEST-01"
      status: "unverified"
      flagged: true
      statement: "Descriptor-less recall: completion must not be claimed from a serial, second, or timing-only rerun instead of the same ordinary executions."
    - requirement_id: "TEST-02"
      status: "unverified"
      flagged: true
      statement: "Descriptor-less recall: comparability must not be claimed from averaged, retried, relabeled, reordered, or threshold-relaxed timing evidence."
    - requirement_id: "TEST-03"
      status: "unverified"
      flagged: true
      statement: "Descriptor-less recall: CI speed must not be obtained by omitting or weakening any of the exact six modules, or by counting a missing/skipped receiver as green."
---

<objective>
Recover from Plan 68's pre-handoff definition-order halt and produce fresh, independently validated local and live library-economics evidence.

Purpose: Restore measurable TEST-01/02/03 truth without reusing a consumed root, replaying a failed evidence budget, weakening the two-partition/exact-six contract, or changing FAST-01/GATE-05.
Output: One authenticated Plan 69 runtime root with a terminal local PASS receipt, a preallocation validation record, two canonical capture receipts, and one distinct validation receipt.

Decision coverage: D-01 through D-09 and every operator/recovery addendum are binding in Tasks 1–2.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-12-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/library-partitions.test.sh
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
@test/support/ci/library_test_partitions_test.exs
@test/support/ci/ex_unit_timing_formatter_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

## Artifacts this phase produces

- New tracked file: `235.1-69-PREALLOCATION-VALIDATION.json`.
- New tracked files: `235.1-PR-EVIDENCE.json`, `235.1-SCAFFOLD-EVIDENCE.json`, and `235.1-VALIDATION-RUN.json`.
- New runtime symbols: `nodup`, `build_plan69_authority`, `verify_plan69_terminal`, and the Plan 69 launcher/driver/allocator/finalizer/verifier entry points.
- New private runtime files: `.allocation.once`, `preallocation-driver-run.json`, `preallocation-handoff.json`, Task 1 finalization/verification receipts, `local/verify-task2.py`, `local/verify-plan69-terminal.py`, the action and sequence ledgers, all local dry-run/database/admission/health/timing receipts, `local/artifact-manifest.json`, and `local/task2-terminal-pass.json`; `local/artifact-manifest.json` must enumerate every descendant exactly once.

<assumption_delta_decision>
Primary noun: library-economics evidence route.
Decision: no-change.
Rationale: detector signals such as historical additional-run counts and multiple-sentinel fixtures describe retained failure cases; they do not introduce a new domain variant, identity model, or route. D-01 through D-09 remain authoritative.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the definition-order repair through one terminal local PASS (D-01–D-04, D-07, D-09)</name>
  <reversibility rating="costly">The runtime files are disposable, but the authorized allocator, dry-run, database, and timing invocations are one-use evidence budgets whose repetition requires another recorded recovery decision.</reversibility>
  <precondition>The frozen root `/private/tmp/sigra-p2351-plan68-8716bf50.teZ26i0w` is still mode 0700 with dev 16777229/inode 294176944 and contains only `.allocation.once`, whose mode is 0600, dev/inode are 16777229/294176945, bytes are 35, and SHA-256 is `9f369b892d24ae7890a7486ef802a34d09417bd24b3f00ece08055c0da011729`; candidate commit `3713dcd4` and both pinned Python runtimes from Plan 68 still resolve.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-PREALLOCATION-VALIDATION.json</files>
  <read_first>
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-SUMMARY.md` — treat the undefined-`nodup` halt, consumed invocation counts, frozen root identity, and unused downstream budgets as exact predecessor state.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-12-SUMMARY.md` — treat the completed two-partition/exact-six implementation and verifier interfaces as the last successful execution dependency.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PLAN.md` — authenticate and reuse the embedded source contracts; do not execute its launcher, driver, allocator, finalizer, or verifier entry points.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PREALLOCATION-VALIDATION.json` — preserve the successful pure planning replay and its zero-allocation evidence as historical authority.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md` — enforce D-01 through D-09, the two ordinary partitions, exact-six non-PR receiver, attempt-1 evidence, and protected-value constraints.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md` and `235.1-RESEARCH.md` — reuse the approved parser, independent receipt validation, no-follow root, PostgreSQL 14 host-path, and first-failure patterns.
    - `.github/workflows/ci.yml`, `scripts/ci/library-partitions.test.sh`, `test/support/ci/library_test_partitions_test.exs`, and `test/support/ci/ex_unit_timing_formatter_test.exs` — keep the live owner, exhaustive assignment, stable ordering, and same-run timing contract unchanged.
  </read_first>
  <behavior>
    - "A structural RED check against the authenticated Plan 68 launcher proves `nodup` is referenced before any binding, without invoking the consumed Plan 68 chain or allocating a root."
    - "The GREEN successor defines `nodup` before every call site, rejects duplicate JSON keys, and passes compile/import plus validate-only probes under `/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/bin/python3.9` and `/opt/homebrew/Cellar/python@3.14/3.14.4/Frameworks/Python.framework/Versions/3.14/bin/python3.14` with zero Plan 69 roots before and after."
    - "One Plan 69 execute invocation creates one private root and one marker, finalizes authenticated verifier bytes, runs the inherited host gate, parser fixtures, one dry run, one current database observation, one admission, and one timing invocation, then emits a canonical terminal PASS."
    - "The local result contains three complete passing pairs, six green children, 225 exhaustive paths split 114/111, nine ledger-disjoint recovery payloads, positive integer durations, stable partition order, ratio at most 2.0, and the exact six-module non-PR route."
  </behavior>
  <action>First authenticate the committed Plan 68 plan, every embedded `bytes`/`sha256` attribute, the preallocation validation record, the summary, candidate `3713dcd4`, the two interpreter executable identities, and the frozen predecessor root using lstat, no-follow opens, fstat identity equality, duplicate-key rejection, canonical JSON, and SHA-256. Record a structural failing test that parses the authenticated launcher scope and identifies `nodup` as a load before its first binding; this test must not import or execute Plan 68 code and must leave all Plan 68 bytes and process budgets unchanged.

Construct a distinct Plan 69 successor package in memory from the authenticated Plan 68 sources. Insert one standard-library `nodup(pairs)` definition before its first use; it must reject duplicate object keys and otherwise return the mapping. Rebind only current launcher/driver/allocator/finalizer/verifier schema names, labels, and the root pattern to the Plan 69 recovery namespace. Preserve every historical Plan 45–68 label, hash, failure statement, and frozen-root identity. Do not perform a blind text replacement. Recompute each transformed source byte count and SHA-256, rebuild the embedded authority table and dominance/definition-order assertions from the resulting bytes, and require exact accepted/adverse row counts before execution. Materialize a Plan 69 terminal verifier that reopens the root and every manifest path with lstat plus `O_RDONLY|O_NOFOLLOW`, rejects duplicate keys and noncanonical bytes, checks every receipt preimage/digest and source/run identity, and has a read-only `--root`, `--preallocation-validation`, and `--predecessor-root` interface.

Run compile/import, definition-dominance, environment, host-path, finalizer-import, cache, no-follow, and allocator-manifest fixtures plus validate-only launcher probes under both pinned interpreters. Validation must report zero allocator, mktemp, root, marker, finalizer, local, timing, database, network, or GitHub actions and identical Plan 69 root inventories before/after. Write the canonical tracked preallocation validation record atomically with the transformed source identities, both runtime receipts, adverse cases, process counts, and zero-action inventories; reopen and verify it before allocation.

After every preallocation gate is green, invoke the new Plan 69 launcher exactly once. Its authenticated allocator may call mktemp exactly once with `/private/tmp/sigra-p2351-plan69-recovery.XXXXXXXX`, create exactly one 0700 root plus one 0600 exclusive marker, and write the driver/handoff receipts before finalization. Finalize and independently verify the root before local work. Under that root, carry the Plan 68 Task 2 semantic contract with Plan 69 current identities: run the host gate, all 19 inherited and three focused parser fixtures through the production parse entry, one isolated Perl environment fixture matrix, one real dry run, one candidate/current observation, one admission, and one timing command. Preserve the exact 225-path 114/111 partition universe and deterministic ordering; require both ordinary partitions to run sequentially under the sole owner with parallel ExUnit timing, and require the schedule/dispatch route to name exactly `Sigra.UpgradeIntegrationTest`, `Sigra.Install.GoldenDiffTest`, `Sigra.Install.Features.PasskeysJsTest`, `Sigra.Install.GeneratorPasskeysOptOutTest`, `Sigra.Install.IdempotencyTest`, and `Sigra.Install.VaultPromotionTest`. Seal the complete root in `local/artifact-manifest.json`, then use the independent terminal verifier to create `local/task2-terminal-pass.json`. Reopen the predecessor root before and after every budgeted action and halt on any change.</action>
  <acceptance_criteria>The original launcher defect is proven structurally without execution; the corrected successor passes both runtime probes with zero allocation, the validation JSON is canonical and authenticated, exactly one Plan 69 root exists, the Plan 68 root is byte-identical, all local budgets are 1/1 or their declared fixture counts, the three-pair/six-child/225-path/114-111/nine-payload facts agree across receipts, and an independently verified terminal PASS binds the complete manifest.</acceptance_criteria>
  <verify>
    <automated>PLAN69_ROOT="$(/usr/bin/python3 -c 'import os,re; rows=sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan69-recovery\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False)); assert len(rows)==1; print(rows[0])')" &amp;&amp; /usr/bin/python3 "$PLAN69_ROOT/local/verify-plan69-terminal.py" --root "$PLAN69_ROOT" --preallocation-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-PREALLOCATION-VALIDATION.json --predecessor-root /private/tmp/sigra-p2351-plan68-8716bf50.teZ26i0w</automated>
    <fails_when>Root discovery returns zero or multiple matches; the predecessor root changes; the corrected source still has a use-before-binding path; either interpreter/host/preallocation gate fails; any budget is duplicated; a receipt is missing, noncanonical, stale, symlinked, or digest-inconsistent; the partition universe/order/ratio differs; any child is non-green; or the terminal status is not PASS.</fails_when>
  </verify>
  <done>One production-quality tracer runs from authenticated repair through the real two-partition local path to a terminal PASS while Plan 68 remains immutable and no live evidence budget has yet been spent.</done>
</task>

<task type="auto">
  <name>Task 2: Capture two fresh attempt-1 route pairs and seal live evidence (D-01, D-05–D-09)</name>
  <reversibility rating="costly">The receipts are replaceable, but each push, dispatch, and workflow attempt is a unique external evidence event and cannot be recreated with the same run identity.</reversibility>
  <precondition>Task 1's terminal verifier returns PASS from the sole Plan 69 root; GitHub authentication is valid; repo `szTheory/sigra`, PR #234, base `main`, head `refs/heads/ci/phase-235-1-evidence`, remote head `87338db7`, candidate `3713dcd4`, and clean exact diff still match the Plan 68 pre-remote contract.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <read_first>
    - `AGENTS.md` — enforce one watcher per run, 60-second watch intervals, one structured summary, failure-only log fetches, and the REST rate-limit stop rules.
    - `.github/workflows/ci.yml` — use the existing automatic pull_request owner and fail-closed workflow_dispatch receiver; do not add or alter workflow topology.
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-68-PLAN.md` Task 3 — carry its candidate/evidence lineage, excluded historical run IDs, exact run/job/artifact predicates, and four-run budget, rebinding only current Plan 69 root/receipt authority.
    - `scripts/ci/verify-library-routing-evidence.sh` and `scripts/ci/verify-library-validation-run.sh` — produce receipts that their current fail-closed schemas accept without changing either verifier.
    - `test/sigra/planning/phase_235_1_library_economics_contract_test.exs` — preserve the receipt, exact-six, routing, and protected-value assertions.
    - `PLAN69_ROOT/local/task2-terminal-pass.json` and `PLAN69_ROOT/local/artifact-manifest.json` — generated by Task 1; authenticate these before every external action without invoking the local one-shot verifier again.
  </read_first>
  <action>Rediscover the sole Plan 69 root at entry and before/after every remote action. Run the read-only terminal verifier and independently reopen every manifest path; any mismatch sets all remote budgets to zero. Query `gh api rate_limit` once before watching. If REST core remaining is 250 or less, record a durable blocked diagnostic with the reported reset time and make no further GitHub polling request until that time. Treat HTTP 403/429 as a hard stop until GitHub's reported reset or retry time. Use exactly one watcher for each run with `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; after completion fetch its structured summary once, and fetch logs only for a failed conclusion. Retry one transient non-rate-limit failure once; never rerun, substitute, waive, or auto-approve a deterministic failure.

Carry the Plan 68 unused live budget exactly. Exclude every retained Plan 35–68 run/job ID. Push candidate `3713dcd4` once to the sole PR head ref, capture its new automatic pull_request attempt-1 run, and dispatch one distinct workflow_dispatch attempt-1 run. Require exact repo/PR/base/head/SHA/event/workflow/job/attempt identity, successful selected library job and all required checks, independent artifact decoding, two exhaustive ordinary partitions, stable same-run timing order, positive raw integer durations, raw ratio at most 2.0, and the exact six scaffold modules only on workflow_dispatch. Atomically write the two canonical capture receipts, reopen them no-follow, commit exactly those two files as the direct child of candidate with no other tracked byte, and push that evidence commit once. Capture its new automatic pull_request attempt-1 validation run and one distinct workflow_dispatch attempt-1 validation run under the same predicates. Write `235.1-VALIDATION-RUN.json` atomically only after all four runs pass; bind the capture and validation run/job/artifact identities, candidate/evidence lineage, root allocation and terminal receipt identities, 225 paths, 114/111 assignment, ratios, and exact-six truth. Do not push another commit and do not reconcile documentation in this task.</action>
  <acceptance_criteria>Exactly four fresh, distinct, non-excluded attempt-1 runs exist: capture PR, capture workflow_dispatch, validation PR, and validation workflow_dispatch. All four are successful and artifact-complete; the evidence commit has candidate `3713dcd4` as sole parent and only the two capture JSON files in its diff; the local validation JSON binds all four runs and the authenticated Plan 69 root; the two route verifiers pass unchanged.</acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh</automated>
    <fails_when>Either verifier exits nonzero; a file/key/run/job/artifact is missing or extra; any run is stale, excluded, retried, not attempt 1, not successful, or has the wrong repo/ref/SHA/event/workflow; partition timing/economics or exact-six semantics fail; candidate/evidence lineage is wrong; or any Plan 68/69 root byte changes during capture.</fails_when>
  </verify>
  <done>Fresh capture and validation pairs independently prove PR timing/economics and non-PR exact-six scaffold coverage, with canonical tracked receipts ready for reconciliation.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Sole PR owner; two exhaustive comparable ordinary partitions; parallel same-run timing; exact-six non-PR scaffold; protect FAST-01/GATE-05 | Task 1 proves the local end-to-end route and Task 2 proves both live routes without source changes. |
| REQ | TEST-01, TEST-02, TEST-03 | All three are in frontmatter; Task 1 covers timing, edge behavior, and exact local topology; Task 2 captures independent live evidence. |
| RESEARCH | Same-run receipts, independent raw verification, fixed predicates, fail-closed artifacts, two-stage closure | Tasks 1–2 reuse the current Plan 68/Plan 47 contracts and existing repository verifiers; no dependency or topology is introduced. |
| CONTEXT | D-01 through D-09 and operator/recovery addenda | Both task names/actions cite the locked decisions; single-owner sequencing, exact-six non-PR routing, delayed reconciliation, and protected facts remain binding. |
| VALIDATION | Parser fixtures, transition contracts, local series, fresh PR/dispatch evidence, distinct validation | Task 1 closes the parser/local series; Task 2 closes capture and distinct validation evidence. |
| LATEST EVIDENCE | Plan 68 planning replay passed; its sole execute root halted marker-only before handoff; downstream budgets unused | Task 1 preserves the frozen root and repairs only the definition-order defect under a new namespace before any fresh budget. |
| HISTORY | Plans 01–68, including every failed/blocked attempt | No prior PLAN or SUMMARY is edited or reclassified; Plan 68 is an explicit rejected predecessor. |
| CONTEXT deferred | Unrelated release, installer, auth, security, admin, and other CI work | Excluded; no deferred item is planned. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Committed Plan 68 authority to generated Plan 69 runtime | Embedded executable bytes cross into a newly executable private-root package. |
| Private runtime root to tracked evidence | Local files and timings can authorize remote evidence capture and later completion claims. |
| GitHub run/artifact data to repository receipts | Remote identities and artifacts are external input that can be stale, substituted, malformed, or replayed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-69-01 | Tampering | Successor source and private root | high | mitigate | Authenticate committed bytes, rebuild identities from transformed bytes, use exclusive 0700/0600 creation, lstat/no-follow/fstat checks, canonical JSON, complete manifests, and frozen-predecessor comparisons in Task 1. |
| T-235.1-69-02 | Spoofing | GitHub run/job/artifact identity | high | mitigate | Bind exact repository, PR, base/head ref, SHA, event, attempt, workflow, job, run, and artifact identities; exclude all historical IDs and verify capture/validation pairs independently in Task 2. |
| T-235.1-69-03 | Repudiation | Evidence lineage and one-shot actions | medium | mitigate | Append fsynced action/sequence ledgers, record process counts and receipt preimages, require candidate/evidence sole-parent lineage, and preserve every failed predecessor summary. |
| T-235.1-69-04 | Information Disclosure | GitHub environment and evidence JSON | medium | mitigate | Persist only allowlisted run/job/artifact metadata and economic facts; never serialize tokens, authorization headers, cookies, or the ambient environment. |
| T-235.1-69-05 | Denial of Service | CI polling and recovery budgets | low | accept | The bounded four-run route, one watcher per run, 60-second interval, one transient retry, and rate-limit hard stops bound resource use; residual GitHub outage risk is operationally tolerable. |
</threat_model>

<verification>
Task 1's read-only terminal verifier must pass once after the sole local route. Task 2 must then make both existing offline evidence verifiers pass without changing their scripts. No verification command may repeat an allocator, dry run, database observation, admission, timing command, push, dispatch, or workflow run.
</verification>

<success_criteria>
Plan 69 succeeds only when the definition-order defect is closed under both runtimes, Plan 68 remains marker-only and byte-identical, one Plan 69 root has a canonical terminal local PASS, four fresh attempt-1 live runs prove both routes, and the three tracked evidence files pass their unchanged independent verifiers.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-SUMMARY.md` when done.
</output>
