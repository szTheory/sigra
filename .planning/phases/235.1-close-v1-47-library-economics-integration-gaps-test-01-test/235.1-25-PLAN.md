---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 25
type: execute
wave: 22
depends_on: [235.1-24]
files_modified:
  - scripts/ci/library-partitions.sh
  - scripts/ci/library-partitions-portability.test.sh
  - scripts/ci/verify-library-routing-evidence.sh
  - scripts/ci/verify-library-routing-evidence.test.sh
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The partition runner works under macOS Bash 3.2 without mapfile while retaining NUL-safe tracked-file discovery in the partition oracle."
    - "The portability repair preserves the exact 225 ordinary paths, partition membership/order, weights, manifest hashes, argv boundaries, and receipt schema/meaning."
    - "Only the runner pin changes in the seven-entry active authority set; every pin remains independently mutation-tested without self-reference."
    - "Plan 22 calibration and Plan 24's committed 21/24 audit remain exact; no recalibration or prior validation is repeated."
    - "One candidate self-test invocation yields exactly three fresh local pairs before one attempt-1 capture pair and one disjoint attempt-1 validation pair."
  artifacts:
    - path: scripts/ci/library-partitions.sh
      provides: "Bash-3-compatible partition manifest loading"
    - path: scripts/ci/library-partitions-portability.test.sh
      provides: "Untimed portability and argv/receipt regression coverage"
    - path: scripts/ci/verify-library-routing-evidence.sh
      provides: "Active authority bound to the repaired runner"
    - path: scripts/ci/verify-library-routing-evidence.test.sh
      provides: "Closed-set and per-pin mutation proof"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
      provides: "Distinct live validation"
  key_links:
    - from: "LibraryTestPartitions.current_ordinary_paths!/1"
      to: "library-partitions.sh argv"
      via: "NUL-safe git discovery, validated manifests, Bash-3-compatible array append"
      pattern: "ls-files|-z|while|read"
    - from: "repaired runner blob"
      to: "CURRENT_ACTIVE_AUTHORITY"
      via: "computed SHA-256 copied exactly into verifier and adverse expected set"
      pattern: "scripts/ci/library-partitions.sh"
    - from: "Plan 24 audit authority"
      to: "Plan 25 candidate"
      via: "exact Task 1 tree and old evidence head sole parent"
      pattern: "bc5f4b29|commit-tree"
---

<objective>
Make partition execution portable to Bash 3 while preserving exact calibrated semantics, then capture and validate live evidence from the repaired authority tree.

Purpose: Plan 24's exact candidate passed every untimed gate but its sole self-test invocation stopped before a pair because `mapfile` is unavailable in the contributor's system Bash. This successor repairs that single portability seam instead of substituting an environment.
Output: A tested portable runner, refreshed active pin, and strict capture/validation receipts—or durable blocked diagnostics.

Decision coverage: D-01 preserves the sole PR owner; D-02/D-03/D-04 preserve exact exhaustive comparable partitions; D-05/D-06 preserve the exact-six route; D-07 admits only fresh evidence; D-08 reserves final reconciliation for Plan 05; D-09 preserves all history, audit, and protected provenance.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.47-MILESTONE-AUDIT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-21-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-22-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-23-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-24-SUMMARY.md
@scripts/ci/library-partitions.sh
@scripts/ci/library-partitions.test.sh
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-routing-evidence.test.sh
@test/support/ci/library_test_partitions.exs
@test/support/ci/library_test_partitions_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make exact partition argv construction portable and refresh its authority pin (D-02–D-04, D-07, D-09)</name>
  <files>scripts/ci/library-partitions.sh, scripts/ci/library-partitions-portability.test.sh, scripts/ci/verify-library-routing-evidence.sh, scripts/ci/verify-library-routing-evidence.test.sh</files>
  <behavior>
    - "System Bash 3.2 executes the synthetic two-partition path with exact argv boundaries and no mapfile dependency."
    - "Empty manifests fail; valid `test/..._test.exs` paths containing spaces and glob characters remain individual argv entries in lexical oracle order."
    - "The 225-path production oracle remains sourced from git ls-files -z and produces the same assignments, costs, and manifest digests as Plan 22."
    - "The active authority has seven entries and only its runner digest changes; isolated mutation of all seven paths still fails."
  </behavior>
  <action>RED first: add a new untimed portability shell test that uses a fake `mix` executable and never reaches real ExUnit. Require execution with `/bin/bash` on macOS (assert major version 3 there; otherwise use the oldest available Bash and also statically reject `mapfile`/`readarray`). Feed valid `test/..._test.exs` partition manifests containing spaces and literal glob characters; capture the fake child's NUL-delimited argv; prove every path is a separate unchanged argument in oracle order, both partitions execute sequentially, an empty manifest fails, and the combined receipt retains the existing schema, universe/partition paths, manifest SHA-256, conclusions, and exit statuses. Add an untimed production-oracle check proving `current_ordinary_paths!` still uses `git ls-files -z`, returns exactly the Plan 21 225-path source membership, and yields the Plan 22 partition path lists, measured weights, and newline-manifest hashes unchanged. Run this new test against the old runner and require RED specifically at `mapfile` under Bash 3.

GREEN: replace only the Bash-4-only manifest-to-array operation with a Bash-3-compatible `while IFS= read -r` array-append loop that preserves already-validated path bytes and order and handles the final record deterministically. Keep the NUL-safe tracked discovery in the Elixir oracle unchanged; do not replace it with `find`, glob expansion, newline-based `git` parsing, `eval`, or shell word splitting. Preserve quoted `mix test "${paths[@]}"` argv, fixed receipt paths, sequential lifecycle, timing normalization, error propagation, and receipt format.

Compute the repaired runner SHA-256 after GREEN. Update exactly the `scripts/ci/library-partitions.sh` digest in `CURRENT_ACTIVE_AUTHORITY` and the adverse suite's expected seven-entry tuple; all other six active entries and four inactive Plan 18 literals must remain exact. Do not pin the new portability test, verifier, or verifier test and do not add a bypass. Require pristine preflight and all seven isolated mutations. Prove the Task 1 execution diff is exactly four declared files; snapshot and compare all unrelated dirt/index state as Plan 24 did. Preserve the committed audit blob `b9c4c6614c776d8e8c192b2bfa941cb024244289e850066a4869f3120b4588dd`, all 225 frozen paths/source index, calibration artifact/manifest, and partition weights/assignment. Since neither source membership nor weights changed, do not recalibrate, recollect, rerun Plan 22 validations, invoke the timed self-test, or perform GitHub actions. Commit RED then GREEN and record GREEN authority commit/tree plus repaired runner digest.</action>
  <verify>
    <automated>bash scripts/ci/library-partitions-portability.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh --preflight . &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
    <fails_when>Any untimed check fails; real ExUnit/timed pairs run; mapfile/readarray remains; argv/path/order/receipt differs; source count/weights/assignment changes; active set is not seven or more than runner pin changes; audit/calibration/unrelated dirt drifts.</fails_when>
  </verify>
  <done>The runner's exact semantics work under Bash 3, only its active pin changes, calibration remains valid, and no measurement/GitHub budget is consumed.</done>
</task>

<task type="auto">
  <name>Task 2: Build the portable exact candidate and capture one pair (D-01–D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json</files>
  <precondition>PR #234 evidence head remains exactly `47f9578fc97edc63603953d446e79fac884326c3`; otherwise stop without replacement.</precondition>
  <action>Verify Task 1 authority, four-file execution diff, new runner pin, seven mutation outcomes, unrelated-dirt preservation, exact audit, and unchanged source/calibration assignments. Create the candidate twice with `git commit-tree`, Task 1's tree, old evidence head as sole parent, fixed message `Phase 235.1 Plan 25 exact-tree evidence candidate`, and Task 1 identity/timestamps. Require identical OID, exact tree, sole parent, and empty recursive diff. Use a clean worktree, materialize only locked dependencies if absent, and prove no tracked drift.

Run the new portability test and all established untimed adverse, preflight, calibration, frozen, independent, protected, Actionlint, and formatting checks. Invoke `bash scripts/ci/library-partitions.test.sh` exactly once here and nowhere in verify; its synthetic cases plus exactly three real runner/verifier pairs are the entire new validation budget. Require both children successful, exact exhaustive/disjoint 225 paths, three receipts, and ratio `&lt;= 2.0` each time. Any failure blocks before push without retry, environment substitution, tuning, source/calibration/audit edit, alternate candidate, or GitHub action.

After success, fast-forward-push once, dispatch one CI run, and pair it with the one automatic PR run at the same SHA. Apply AGENTS.md rate floor, 403/429 hard stop, one 60-second watcher per run, one structured summary, and failure-logs-only rules. Deterministic failure blocks without repair/rerun/replacement. One durable non-code transient may rerun the same ID once through a sequential 60-second view monitor; attempt above 1 remains non-admissible and blocks. Only dual original attempt-1 success may download/verify three PR plus two dispatch artifacts and atomically write both receipts. Preserve the ten-run ledger/digest and the 21/24 audit.</action>
  <verify>
    <automated>bash scripts/ci/library-partitions-portability.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; actionlint .github/workflows/ci.yml &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any standalone check fails; timed self-test count is not one; tree/audit/calibration differs; a local pair fails/exceeds 2.0; evidence is reused/cross-SHA/red/above attempt 1; watcher policy is violated; or prior evidence is admitted.</fails_when>
  </verify>
  <done>The exact portable authority yields one attempt-1 capture pair after exactly three local pairs, or blocks without replacement, tuning, repair, or waiver.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the evidence commit on one distinct pair (D-07–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <action>Commit exactly the two capture receipts on the candidate; prove parent, only-two-file diff, portable authority, 21/24 audit, source/calibration, and ledger unchanged. Fast-forward-push once and pair one dispatch with the automatic PR run at the evidence SHA. Apply Task 2's exact rate/watcher/failure rules. Only dual original attempt-1 success may write validation; require all run-ID disjointness, exact SHA/jobs/commands/authority, and no retry/waiver authority. Run receipt-only checks without rerunning the timed self-test. Leave the audit pre-evidence for Plan 05.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Any check fails; authority bytes drift; a run is reused/cross-SHA/red/above attempt 1; watcher policy is violated; retry is admitted; or local timing reruns.</fails_when>
  </verify>
  <done>One distinct attempt-1 pair validates the evidence commit, or the plan blocks durably without alternate evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git tracked discovery to shell argv | Path boundaries/order must survive without expansion or splitting. |
| Repaired runner to active authority | One computed digest changes without self-reference or bypass. |
| Dirty worktree to authority | Unrelated user state must remain untouched. |
| Local candidate to GitHub receipts | Exact SHA and attempt identity govern admission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-25-01 | Tampering | path argv | high | mitigate | Fake mix captures NUL-delimited argv for valid paths with spaces/globs; quoted arrays only. |
| T-235.1-25-02 | Tampering | runner authority | high | mitigate | Exact seven-key map and isolated mutation of every pin. |
| T-235.1-25-03 | Elevation of Privilege | calibration reuse | high | mitigate | Exact 225 membership/weights/assignments and unchanged artifact/manifest hashes. |
| T-235.1-25-04 | Spoofing | live provenance | high | mitigate | Exact event/attempt/SHA/job identity and disjoint IDs. |
| T-235.1-25-05 | Denial of Service | polling | medium | mitigate | Rate floor, hard stops, one watcher, 60-second observation. |
| T-235.1-25-SC | Tampering | packages | low | accept | No package or dependency change. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Portable strict dual-route proof | 25 | COVERED | Runner repair plus exact live lineage. |
| REQ | TEST-01 | PR timing evidence | 25 | COVERED | Three artifacts and owner/aggregate. |
| REQ | TEST-02 | Comparable exhaustive partitions | 25 | COVERED | Unchanged calibration and three pairs. |
| REQ | TEST-03 | Exact-six non-PR success | 25 | COVERED | Dispatch job/artifacts. |
| RESEARCH | — | Portable harness/no tree drift | 25 | COVERED | Bash-3 test, exact pin, exact candidate. |
| CONTEXT | D-01–D-06 | Preserve routing contracts | 25 | COVERED | Semantics unchanged. |
| CONTEXT | D-07–D-09 | Fresh evidence/history/reconciliation | 25, 05 | COVERED | Strict live evidence then final docs. |
| CONTEXT | Deferred ideas | Unrelated work | — | EXCLUDED | Out of scope. |

<verification>
Prove portable path handling and unchanged calibration semantics without timing, then invoke the timed harness once for three pairs and run two strict live evidence stages.
</verification>

<success_criteria>
- Bash 3 executes exact manifest paths without mapfile/readarray or argv corruption.
- Only the runner active pin changes; all seven pins remain mutation-sensitive.
- Source membership/weights/assignments, calibration, audit, and unrelated dirt remain exact.
- One self-test invocation yields exactly three candidate-local pairs.
- One capture pair and one disjoint validation pair prove the exact portable lineage.
- Failure blocks without environment substitution, alternate evidence, tuning, replacement, or waiver.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-25-SUMMARY.md` with RED/GREEN commits, Bash versions, argv cases, runner digest/pin mutations, unchanged membership/calibration/audit/dirt, authority/candidate identity, three local results, live provenance, and final status.
</output>
