---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 36
subsystem: ci-evidence
tags: [shallow-checkout, source-index, tdd, exact-candidate, github-evidence]
status: halted
dependency_graph:
  requires: [235.1-35]
  provides: [shallow-safe-runtime-source-authorization, three-pair-local-validation, deterministic-live-capture-diagnostic]
  affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
tech-stack:
  added: []
  patterns: [immutable-provenance-metadata, exhaustive-runtime-source-index, deterministic-commit-tree, attempt-one-live-capture]
key_files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-36-SUMMARY.md
  modified:
    - test/support/ci/library_test_partitions.exs
    - test/support/ci/library_test_partitions_test.exs
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json
    - scripts/ci/verify-library-routing-evidence.sh
    - scripts/ci/verify-library-routing-evidence.test.sh
decisions:
  - Authorize runtime partition loading from exact immutable provenance metadata plus the exhaustive current 225-row source index, without resolving or fetching the provenance Git object.
  - Stop permanently after the original attempt-1 PR owner failed because the reachable-provenance test depended on a full local object store; do not repair, retry, substitute, or write receipts in Plan 36.
requirements-completed: []
actuals:
  tokens: 5613
  tasks: 2
  commits: 5
plan_head_before: b867620fe4a4615297f6187fcc313e5ccf8af46b
metrics:
  duration: 28m
  completed: 2026-09-11
---

# Phase 235.1 Plan 36: Shallow Source Authorization and Attempt-1 Capture Stop Summary

The loader now admits an unreachable immutable provenance object only when all 225 current ordinary paths and bytes exactly match the externally bound source index, and three fresh local pairs passed; the sole live PR capture nevertheless failed because the test's reachable-provenance control tried to fetch that unavailable object from the shallow checkout itself, so the plan stopped without receipts or validation runs.

## Task 1 — TDD Source Authorization Complete

RED commit `38ad8587` created a repository fixture with the exact current 225 ordinary files, rebound artifact and manifest, and no `b37ac116...` provenance object. The target test failed only with `invalid partition calibration: source snapshot commit/tree`. The persisted RED record passed `gsd-tools check tdd-red-evidence` with `RED_EVIDENCE_OK` / `target_test_failed`.

GREEN commit `efadc05e` removed runtime `git rev-parse`, `git show`, fetch, and ancestry dependence. Admission now has two independent layers:

- immutable metadata must be source commit `b37ac1164cee96be11a5cdeb60b31db560018e04`, source tree `02bb907f075b1428d89bb87e519c0e0d8810fcd9`, and an artifact `source.implementation_commit` cross-link to that commit;
- runtime authorization discovers tracked ordinary files and requires the exact sorted 225-path universe, ordinary regular non-symlink files, per-row positive byte count and SHA-256, canonical unique row order, exhaustive membership, and manifest source-index digest.

The focused matrix passed for unreachable and locally reachable provenance controls. Mutated provenance commit, provenance tree, artifact cross-link, current bytes, added path, removed path, renamed path, source-row order, row hash, index digest, symlink, non-regular file, and duplicate row all failed closed.

Rebind commit `475039b2`, followed by the formatting correction in `d7d4cee0`, changed only the loader-test row to 30,278 bytes / SHA-256 `f68aae0c524a662b7d6cacabea151e9d3d09fbc82a59b96714af661428289ab8`, manifest source-index digest `83b278d7d7ecd37ef19b869a92a19435bd10644986e3264fb527217fb2bf466d`, artifact SHA-256 `f5208c0c1f9078cedd37cde001fe26a0c1bed4f6e84ddd3b42eeab19910efc19`, and manifest SHA-256 `b80effe8b59d5d454dcb5064aacdad1892893861e775dcbd3dc7fad4f8c8abc6`. The artifact remained 2,987,511 bytes and the manifest remained 623 bytes.

An independent decoded comparison against Plan 35 proved equality after deleting exactly that source row and the manifest calibration/index bindings. Source provenance, all three samples, nine embedded payloads, limits, 225 paths, NUL-manifest digest, all 225 derived timing weights, and the 114/111 assignment remained unchanged. No collection runner or timing calibration was invoked.

The final focused command executed 15 tests with zero failures.

## Task 2 — Exact Candidate and Local Validation Complete

Commit `eb49130e` refreshed exactly the loader, loader test, artifact, and manifest entries in both active authority tuples. The workflow, runner, independent partition verifier, historical authority, and seven-entry topology remained unchanged; the routing verifier and its adverse test remain outside their own authority. Pristine preflight and every isolated one-byte mutation/restoration case passed.

Two fixed-metadata `git commit-tree` derivations produced the same exact candidate:

| Field | Value |
| --- | --- |
| Candidate commit | `0bf7b8e9369592f7781f2bc51a93d9db62e13483` |
| Candidate tree | `24476020cf4814db600f3cd8b0bfa8745a12bead` |
| Sole parent | `f179e3afc8b328849a3c12ba9c74abe1356d2664` |
| Recursive diff | Exactly loader, loader test, artifact, manifest, and two pin files |
| Executable modes | Both verifier scripts remain `100755` |

A fresh physical worktree copied 3,369 dependency files and 800 Mix/Hex cache files with no symlinks or shared inodes, compiled into its own default `_build/test`, used candidate-local homes/cache/tmp, retained `MIX_BUILD_PATH` unset, and stayed tracked-clean. Portability under system Bash 3 and modern Bash 5, routing adverse/preflight, partition synthetics, 111 focused loader/auth/telemetry tests, source-complete and terminal attestations, Actionlint, formatting, and tree identity all passed.

`library-partitions.test.sh` was invoked exactly once. Its exactly three fresh pairs all passed independently:

| Pair | Partition 1 | Partition 2 | Ratio |
| --- | ---: | ---: | ---: |
| 1 | 17,176ms | 11,439ms | 1.502 |
| 2 | 14,068ms | 11,731ms | 1.199 |
| 3 | 14,712ms | 14,390ms | 1.022 |

All six children were green. Every pair was exhaustive and disjoint across 225 paths with unchanged 114/111 assignment. The nine new combined/P1/P2 hashes were unique and disjoint from the canonical 92 earlier hashes plus Plan 35's nine collection and nine validation hashes. No timing retry, recalibration, source-row retune, reassignment, alternate candidate, or GitHub action occurred before this gate passed.

## Task 3 — Deterministic Attempt-1 Live-Capture Hard Stop

The exact candidate fast-forwarded the existing PR branch once, and CI was dispatched once at the same SHA. After a 5,000-request rate floor, each new run received exactly one watcher at the required 60-second interval, one structured summary after completion, and one failed-log fetch because both overall workflows concluded failure.

| Route | Run | Event | Selected job | Result |
| --- | ---: | --- | --- | --- |
| PR owner | `34564383170` | `pull_request` | `Library tests shard` (`103153401354`) | failure |
| PR aggregate | `34564383170` | `pull_request` | `Library tests` (`103153833289`) | failure |
| Exact-six | `34564379273` | `workflow_dispatch` | `Library install golden (non-PR)` (`103153379008`) | success |

Both IDs are attempt 1, exact-SHA bound to `0bf7b8e9...`, mutually disjoint, and distinct from Plan 35 runs `34562007714` and `34562004702`. The dispatch selected job passed; its overall workflow remained red on the known branch release and aggregate guards.

The PR owner ran 1 doctest, 3 properties, and 1,315 tests with one failure. The exact failing test was `reachable provenance remains a non-authorizing compatibility control`: its fixture executed `git fetch <current-checkout> b37ac116...`, and GitHub's shallow checkout returned `upload-pack: not our ref b37ac116...`. Partition 2 therefore never ran, the verifier correctly rejected its absent receipt, and its artifact upload correctly failed closed.

This is a test-fixture reachability bug, not a loader rejection: the local linked worktree inherited the parent repository's full object store, which made the control pass locally, while the GitHub checkout contained only candidate history. Plan 36 prohibits any post-live repair or replacement run. Therefore no PR/scaffold receipts, evidence commit, validation pair, or validation receipt was created.

External diagnostic SHA-256 values:

| File | SHA-256 |
| --- | --- |
| PR structured summary | `92be1d88ae3d6137166cfa007bac60c8c0177ddb5c0b92c34679a9d202f65bac` |
| PR failed log | `e8f64fb5c92b651096e1499dc754fd3ea67399d5595abe3450ee0655c557ad99` |
| Dispatch structured summary | `587a9953c2705c954f5e384f5e0d38417b53855d0c0a08563285a8b3c5fca82b` |
| Dispatch failed log | `85405fdd09149a93e2aebc764a81d4471f643761885e5ac6dfc0ae6368825247` |

## Budget and External-Action Accounting

- local validation script invocations / real pairs / payloads: exactly 1 / 3 / 9;
- candidate successful pushes / capture dispatches: exactly 1 / 1;
- capture runs / watchers / structured summaries / failed logs: 2 / 2 / 2 / 2;
- evidence commits, validation runs, live receipt files, reruns, retries, substitutions, and waivers: zero.

## TDD Gate Compliance

| Gate | Commit | Evidence |
| --- | --- | --- |
| RED | `38ad8587` | Target shallow fixture failed at Git provenance resolution; `RED_EVIDENCE_OK` |
| GREEN | `efadc05e` | Unreachable/reachable controls and provenance/runtime mutation matrix passed |
| REBIND | `475039b2`, `d7d4cee0` | Sole source row/bindings changed; Plan 35 samples, weights, and assignment equal |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Seeded private Mix/Hex caches before isolated compilation**

- **Found during:** Task 2 isolation setup
- **Issue:** Empty private Mix/Hex homes did not contain Hex, so the offline dependency graph could not identify package SCMs.
- **Fix:** Byte-copied the existing locked Mix/Hex cache into candidate-local directories, rejected symlinks/shared inodes, and restarted only compile preconditioning before any timed validation.
- **Files modified:** None.
- **Commit:** Not applicable.

**2. [Rule 1 - Bug] Applied the authoritative formatter and rebound dependent authority**

- **Found during:** Task 2 first untimed candidate gate
- **Issue:** The initial local `mix format` ran without a selected Erlang version and did not format either edited Elixir file; the isolated formatter gate rejected both.
- **Fix:** Ran the formatter with `ASDF_ERLANG_VERSION=28.4.1`, rebound only the changed loader-test row plus manifest/artifact bindings and four pins, re-ran all focused/adverse tests, derived a new exact candidate, and repeated fresh isolation before timing.
- **Files modified:** The planned six candidate files.
- **Commit:** `d7d4cee0`.

**3. [Rule 1 - Bug] Corrected a local refspec interpolation error before the sole successful push**

- **Found during:** Task 3 push command
- **Issue:** zsh parsed `$CAND:refs/...` as a modifier and rejected the local source refspec before contacting/updating the remote branch.
- **Fix:** Used `${CAND}:refs/...`; the remote then accepted exactly one successful fast-forward update.
- **Files modified:** None.
- **Commit:** Not applicable.

## Deferred Issue

The reachable-provenance test must construct or import the exact `b37ac116...` object hermetically from bytes that do not depend on the checkout already possessing that object. A successor plan must fix that test without restoring any runtime Git-object dependency, derive a new direct fast-forward child of `0bf7b8e9...`, and obtain wholly new attempt-1 capture/validation evidence. Plan 36 has no authority to repair or retry after its live failure.

## Known Stubs

None.

## Threat Review

No endpoint, authentication behavior, schema, dependency, workflow, runner, threshold, audit, or receipt schema changed. Immutable metadata blocks provenance spoofing; the 225-row runtime index blocks path/content tampering; decoded equality blocks timing recalibration; exact-six lineage and disjoint run IDs block evidence substitution. The live boundary failed closed, and no receipt was fabricated from the successful dispatch job alone.

## Self-Check: PASSED

- Task commits `38ad8587`, `efadc05e`, `475039b2`, `eb49130e`, and `d7d4cee0` resolve in order from plan base `b867620f`.
- Candidate `0bf7b8e9...` / tree `24476020...` is the exact six-file direct child of pushed head `f179e3af...` and is now the remote PR head.
- The one local timing log contains exactly three passing pairs and nine hashes disjoint from all 110 prior hashes.
- Both attempt-1 summaries and failed logs replay to the recorded identities and SHA-256 values.
- PR, scaffold, and validation receipt files are absent, as required after the capture failure.
