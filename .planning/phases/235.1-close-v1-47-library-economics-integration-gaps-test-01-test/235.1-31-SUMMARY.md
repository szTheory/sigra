---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 31
subsystem: ci-evidence
tags: [epoch-clock, portability, exact-tree, deterministic-block]
status: halted
dependency_graph:
  requires: [235.1-30, 235.1-29]
  provides: [cross-process-epoch-clock, four-file-exact-successor, containment-diagnostic]
  affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
tech-stack:
  added: []
  patterns: [controlled-cross-process-clock, real-delay-clock-probe, deterministic-commit-tree]
key_files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-31-SUMMARY.md
  modified:
    - scripts/ci/library-partitions.sh
    - scripts/ci/library-partitions-portability.test.sh
    - scripts/ci/verify-library-routing-evidence.sh
    - scripts/ci/verify-library-routing-evidence.test.sh
decisions:
  - Use Python epoch milliseconds so separately launched clock processes share one coordinate.
  - Preserve the first local validation failure and stop before GitHub because the one-invocation budget forbids retry or rematerialization.
requirements-completed: []
actuals:
  tokens: 8200
  tasks: 1
  commits: 3
metrics:
  duration: 18m
  completed: 2026-09-10
---

# Phase 235.1 Plan 31: Cross-Process Epoch Clock Summary

The production runner now records truthful cross-process epoch milliseconds under Bash 3 and Bash 5, but the exact successor's sole timed invocation stopped on a macOS `/tmp` versus `/private/tmp` containment alias before completing its first local pair.

## Task 1 — Complete

RED commit `d4407614` extended the production-runner portability suite under distinct system Bash 3 and modern Bash 5 runtimes. A controlled fake Python executable atomically consumed exact cross-process timestamps; malformed, regressing, and exhausted sequences failed closed. Real `/usr/bin/python3` around delayed children demonstrated the old process-relative clock defect. Existing exact NUL argv/order, spaces and glob characters, absolute timing paths, empty-partition diagnostics, nounset protection, receipt schema, sequential ownership, and first-child status propagation remained covered.

GREEN commit `09e5b478` changed only `clock_ms` from `time.monotonic_ns() // 1000000` to `time.time_ns() // 1_000_000`. Controlled receipts reproduced exact `125ms` and `225ms` differences; real delayed children produced positive durations equal to `end_ms - start_ms`, above the conservative delay bound and below 5 seconds. Failure status `42` propagated from partition 1 while partition 2 remained `not_run`.

Authority commit `9cb590ab` refreshed only the runner digest in `CURRENT_ACTIVE_AUTHORITY` and the adverse suite's exact expected list, from `f2a75570...` to `22dac275dac16cda0a52224b704397e9a836f644667cc949967997feeca489e7`. The other six entries, historical authority, ten-run ledger, sizes, and containment rules were unchanged. Pristine preflight, all seven isolated mutation/restoration cases, and historical-only non-authority passed. The superseded digest is absent from active scripts, tests, and workflow files.

The full tracer gate passed: dual-shell portability, routing adverse suite, active preflight, partition verifier, and 35 focused loader/economics tests. The unavailable Erlang version in the user's dirty `.tool-versions` required only the already-approved `ASDF_ERLANG_VERSION=28.4.1` environment override.

## Task 2 — Deterministic Hard Stop

Plan 29 authority remained byte-exact:

- calibration: `8d54ea4ddb09b7aa5b6b0a5acec462f6e57ba2218a3466c31cf2cad76a562603`, 2,986,763 bytes;
- manifest: `61ecf707ee0535da9995767984c8afa5abd75eff00b3dcd774d1f800d0e09ef9`, 623 bytes;
- three samples, nine payloads, 225 paths, and 110/115 assignment at 19,142,933us/19,142,928us.

An independent replay decoded only the six per-partition timing payloads, summed each test's `time_us`, applied `median(max(1, sum(time_us)))`, and exactly reproduced all 225 recorded costs. Replacing every combined receipt's start, end, and duration fields did not change the replay, proving the clock repair requires no recalibration.

Two independent commit-tree derivations over exact Plan 30 candidate `33a4f235` produced identical successor commit `34931b9cb8e2edd6ee68dd81c0ce18acf8a5e0a4` and tree `462bb0594e7c1f843401693eb5d72639c524ce38`, with sole parent `47f9578fc97edc63603953d446e79fac884326c3`. Its diff is exactly four files from Plan 30 and six paths from `9ae29062`: the two Plan 29 artifacts plus runner, portability test, routing verifier, and routing adverse test.

A fresh detached worktree at `/tmp/sigra-plan31.NlChsF/successor` received independent copies of 4,186 dependency/cache files with zero symlinks and zero shared regular-file inodes. Candidate-local Mix, Hex, Rebar, XDG, and tmp state, offline mode, absent `MIX_BUILD_PATH`, and a fresh real `_build/test` were used. The copied Hex registry lacked an `h2` solver entry, so the source/lock set was validated with `mix deps` plus `mix deps.unlock --check-unused`; no network fetch or package mutation occurred. Dependencies and the project compiled from scratch.

All untimed gates passed before measurement: exact relocation (1 test, 0 failures, 27 excluded), dual-shell clock portability, routing mutations/preflight, validation synthetic suite, independent partition verifier, 57 calibration/lifecycle/terminal contracts, both protected offline attestations, Actionlint, formatting, exact frozen-byte comparison, candidate cleanliness, and unrelated-dirt preservation.

`library-partitions.test.sh` was invoked exactly once. Its synthetic cases passed, and pair 1's partition 1 ran 1,254 tests with zero failures. The child produced a valid timing receipt, but normalization rejected its physical paths:

- runner lexical root: `/tmp/sigra-plan31.NlChsF/successor`;
- formatter physical path prefix: `/private/tmp/sigra-plan31.NlChsF/successor`;
- diagnostic: `timing receipt contains a path outside the repository test tree`.

The combined receipt remained structurally truthful:

| Partition | Paths | Start | End | Duration | Conclusion | Status |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| 1 | 110 | 1789093230843 | 1789093243956 | 13,113ms | failure | 1 |
| 2 | 115 | 0 | 0 | 0ms | not_run | 0 |

Diagnostic digests are: timed log `dd3851e13b2ce02f0421fe95a2a5d067caa1d29bfda6bd0d499c750c65fdd762`, combined receipt `8214695d81bd1bc05606fa74c152056b0d11e58d521cca3009411455c5b8a0b5`, and partition-1 timing `498c73132525835c047c05f04a26b95515ba8473991c61771250ad458f44ce67`.

The plan requires the first local failure to block without retry, rematerialization, clock padding, recalibration, reassignment, threshold change, alternate candidate, or GitHub. Therefore zero local pairs were admitted and the successor was not pushed.

## Task 3 — Not Run

No automatic PR run, workflow dispatch, watcher, structured summary, artifact download, capture receipt, evidence commit, validation run, or validation receipt occurred. The milestone audit remains in its pre-evidence state.

## Budget and External-Action Accounting

- `library-partitions.test.sh` invocations: exactly one.
- Admitted real local pairs: zero of three; pair 1 stopped after partition 1 normalization.
- Recalibrations, assignment changes, threshold changes, timed retries, and rematerializations: zero.
- GitHub pushes, dispatches, watchers, summaries, logs, downloads, and live receipts: zero.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Environment] Used copied locked dependency sources when offline solver metadata was incomplete**

- **Found during:** Task 2 compile preconditioning.
- **Issue:** The independently copied Hex cache lacked the registry record for locked package `h2`, so offline `mix deps.get --check-locked` could not solve despite complete copied source.
- **Fix:** Validated every copied locked source with `mix deps`, ran `mix deps.unlock --check-unused`, and compiled dependencies/project from scratch without network access or package changes.
- **Files modified:** None.

## Deferred Issue

`normalize_timing_paths` compares lexical prefixes and does not canonicalize the repository root and receipt paths before containment. On macOS, a worktree created beneath `/tmp` may emit formatter paths beneath its physical `/private/tmp` alias and be rejected despite remaining inside the same worktree. A successor plan must regression-test and repair canonical containment before consuming a new timed budget.

## Known Stubs

None.

## Threat Review

No endpoint, authentication path, schema, dependency version, package, ordinary source, workflow, calibration, manifest, loader, lifecycle, audit, factor-two threshold, or evidence receipt changed. The clock successor retained its exact sole parent and permitted four-file overlay; no live provenance was admitted.

## Self-Check: PASSED

- Task commits `d4407614`, `09e5b478`, and `9cb590ab` resolve.
- Exact successor `34931b9c` resolves to tree `462bb059` with sole parent `47f9578` and the required four-/six-path diffs.
- The isolated candidate is tracked-clean; the diagnostic log and receipts have the recorded digests.
- The timed budget was consumed once, the first failure was not retried, and all three live-evidence artifacts remain absent.
