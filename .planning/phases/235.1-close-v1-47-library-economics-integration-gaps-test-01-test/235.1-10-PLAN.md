---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 10
type: execute
wave: 8
depends_on: [235.1-09]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
  - scripts/ci/verify-library-routing-evidence.sh
  - scripts/ci/verify-library-routing-evidence.test.sh
  - scripts/ci/verify-library-validation-run.sh
  - scripts/ci/verify-library-validation-run.test.sh
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh attempt-1 PR run proves the sole owner and byte-stable aggregate green with two exhaustive comparable ordinary-partition artifacts."
    - "A fresh attempt-1 workflow_dispatch run proves the exact six-module non-PR job green with required receipt and diagnostics artifacts."
    - "After both capture receipts are committed, distinct attempt-1 PR and dispatch runs execute the evidence commit and independently validate both routes."
    - "The failed fixed-bound runs remain negative history; no current evidence contains or relies on the impossible ordinary-vs-scaffold non-dominance predicate."
  artifacts:
    - path: 235.1-PR-EVIDENCE.json
      provides: raw PR partition provenance and artifact digests
    - path: 235.1-SCAFFOLD-EVIDENCE.json
      provides: raw non-PR six-receiver provenance and artifact digests
    - path: 235.1-VALIDATION-RUN.json
      provides: distinct evidence-SHA PR and dispatch validation
  key_links:
    - from: PR evidence
      to: partition verifier
      via: downloaded raw artifacts and recomputed exact-set/ratio predicates
    - from: scaffold evidence
      to: install verifier
      via: downloaded exact-six receipt and diagnostic artifact
    - from: validation receipt
      to: both evidence receipts
      via: exact evidence commit SHA and distinct run IDs
---

<objective>
Capture real attempt-1 evidence for the separated PR and non-PR routes, then validate their committed bytes on independent runs.

Purpose: Replace Plan 04's impossible admission contract without rewriting its failure history, and provide the machine authority Plan 05 needs for honest TEST closure.
Output: Two capture receipts, one dual-route validation receipt, offline verifiers, and adverse tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-08-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-09-SUMMARY.md
@scripts/ci/verify-library-partitions.sh
@scripts/ci/verify-library-install-golden.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind fresh PR partition and non-PR scaffold capture runs (D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, scripts/ci/verify-library-routing-evidence.sh, scripts/ci/verify-library-routing-evidence.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - PR evidence admits only pull_request attempt 1 with successful `Library tests shard` and `Library tests`, exact head SHA, and three same-run downloaded artifacts passing the partition verifier.
    - Scaffold evidence admits only workflow_dispatch attempt 1 with an executed successful `Library install golden (non-PR)` job and two same-run downloaded artifacts passing the install verifier.
    - Run-level conclusions are recorded exactly; selected-job success is the admission authority so an unrelated non-gating dispatch job cannot be mislabeled or erase valid scaffold execution.
  </behavior>
  <action>Start with hermetic valid fixtures and one-pole mutations for repository, PR/event, run attempt, head SHA, job identity/status/conclusion/skipped state, artifact ID/name/digest, duplicate/extra keys, raw receipt values, receiver set, partition coverage/ratio, protected invariants, and the supersession ledger. Push Plans 08/09 to the scoped evidence branch. Resolve one fresh automatic PR run and one fresh `gh workflow run CI --ref BRANCH` dispatch run at the same implementation SHA; do not rerun either attempt. For each run, preflight `gh api rate_limit --jq '.resources.core | {remaining,reset}'`, hard-stop at remaining at or below 250 or on 403/429, use one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`, fetch one structured summary with the locally supported `attempt` field, and fetch failed logs only when the overall run fails. Download only exact run/attempt-qualified authoritative artifacts into validated `mktemp -d` directories, run the corresponding production verifier, and compute SHA-256 locally.

Atomically create `235.1-PR-EVIDENCE.json` with schema `sigra.library-partitions-evidence/v1`, exact repository/PR/run/owner/aggregate/artifacts/partitions/protected_invariants/commands/supersession keys. Record all raw durations and exact manifest/test counts; recompute `max * 1000 &lt;= min * 2000`. Atomically create `235.1-SCAFFOLD-EVIDENCE.json` with schema `sigra.library-install-golden-evidence/v1`, exact repository/run/job/artifacts/receivers/diagnostics/protected_invariants/commands/supersession keys. Its supersession object must cite attempt-1 run 34435818106 (28,671ms ordinary, 79,614ms install), safe optimization commits `1fa788fc` and `488f4fa1`, the bounded clean-local 56–78s range, and the final 57,389ms hard stop as failed evidence, not passing predicates. Implement one offline verifier over both tracked receipts that reruns raw arithmetic/exact-set/provenance checks and rejects any retained `install_not_dominant`, ordinary-vs-scaffold `comparable`, or producer-owned pass authority.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; jq -e '.schema_version == "sigra.library-partitions-evidence/v1" and .run.event == "pull_request" and .run.attempt == 1 and .owner.conclusion == "success" and .aggregate.conclusion == "success" and (.artifacts | length) == 3' .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json &amp;&amp; jq -e '.schema_version == "sigra.library-install-golden-evidence/v1" and .run.event == "workflow_dispatch" and .run.attempt == 1 and .job.conclusion == "success" and (.artifacts | length) == 2 and (.receivers | length) == 6' .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json</automated>
  </verify>
  <done>Fresh real capture runs prove both routes at one implementation SHA, while every failed fixed-bound observation remains explicitly negative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate the committed dual-route evidence on distinct runs (D-07–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json, scripts/ci/verify-library-validation-run.sh, scripts/ci/verify-library-validation-run.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - Validation contains exactly one pull_request and one workflow_dispatch run at the commit containing both capture receipts and their verifier.
    - Both validation runs are attempt 1, distinct from both capture runs and each other; their head SHA equals the evidence commit SHA.
    - PR required jobs and the non-PR scaffold job are completed, executed, and successful; provenance/SHA/run-reuse/missing/skipped/red/extra-key mutations fail.
  </behavior>
  <action>Commit Task 1 receipts and verifier, push that exact evidence commit, then collect the automatically triggered PR validation run and one new workflow_dispatch validation run at the evidence commit. Apply the same rate-limit and one-watcher policy independently to each run; never create evidence from rerun attempts. Write `235.1-VALIDATION-RUN.json` schema `sigra.library-routing-validation/v1` with exact top-level keys `schema_version`, `repository`, `evidence_commit_sha`, `capture_run_ids`, `validations`, and `commands`. Require exactly two ordered validations: PR with raw `Library tests shard` and `Library tests` states, and dispatch with raw `Library install golden (non-PR)` state. Record run URL/event/attempt/head SHA/conclusion without masking unrelated job failures. Rework the existing validation verifier/test into an offline cross-receipt authority: exact keys/types, two route identities, distinctness, SHA equality, selected job execution/success, exact rate/watch/summary command shapes, and no stored pass boolean. Mutation-test every admission pole before committing.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
  </verify>
  <done>Independent live PR and dispatch executions prove the committed evidence contract and both current routes without human UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub API to tracked JSON | External mutable run data becomes milestone authority. |
| Downloaded artifacts to verdict | Untrusted bytes must be digest-bound and independently parsed. |
| Capture commit to validation runs | Validation must execute the exact evidence bytes it claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-10-01 | Spoofing | run provenance | high | mitigate | Exact repo/event/attempt/SHA/job IDs and run-qualified artifacts. |
| T-235.1-10-02 | Tampering | artifact bytes | high | mitigate | Local SHA-256 plus production verifier and mutation suite. |
| T-235.1-10-03 | Repudiation | failed fixed-bound history | high | mitigate | Required supersession ledger with exact failed observations. |
| T-235.1-10-04 | Elevation of Privilege | evidence self-validation | high | mitigate | Two distinct post-commit validation runs at exact evidence SHA. |
| T-235.1-10-05 | Denial of Service | API polling | medium | mitigate | One 60-second watcher per run and hard rate-limit stop. |
| T-235.1-10-SC | Tampering | package supply chain | low | accept | No package installation changes occur in evidence capture. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Real evidence for measurable separated routes | 10 | COVERED | Capture plus independent validation. |
| REQ | TEST-01 | Parallel timing observed on PR | 10 | COVERED | Two partition artifacts and owner job. |
| REQ | TEST-02 | Comparable partitions observed on PR | 10 | COVERED | Raw ratio recomputation. |
| REQ | TEST-03 | Exact scaffold coverage observed non-PR | 10 | COVERED | Dispatch job and two artifacts. |
| RESEARCH | — | Rate-safe attempt-1 evidence | 10 | COVERED | AGENTS command policy. |
| CONTEXT | D-07, D-08, D-09 | Evidence-first closure and terminal preservation | 10 | COVERED | No truth docs change here; protected verifiers run. |
| CONTEXT | D-02, D-05, D-06 | Fixed-bound admission | 10 | SUPERSEDED | Exact negative history retained; impossible predicate removed from current authority. |

<verification>
Run both offline mutation suites, focused phase contract, protected verifiers, and exact jq schema probes over all three receipts.
</verification>

<success_criteria>
- Two fresh attempt-1 capture runs prove the PR and scaffold routes.
- Two distinct attempt-1 validation runs execute the exact evidence commit.
- Artifact IDs, names, digests, raw values, selected job states, and commands are durable and mutation-tested.
- Plan 04 remains failed historical evidence and is not reclassified.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-10-SUMMARY.md` when done.
</output>
