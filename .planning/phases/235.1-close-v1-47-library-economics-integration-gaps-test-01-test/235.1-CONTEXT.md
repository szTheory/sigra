# Phase 235.1: Close v1.47 Library Economics Integration Gaps - Context

**Gathered:** 2026-09-09 (assumptions mode)
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the v1.47 milestone audit blockers TEST-01, TEST-02, and TEST-03 without
reversing the proven FAST-01 improvement or weakening the byte-stable `Library tests`
required context. This phase owns library-test timing/economics evidence, the explicit
TEST-02 requirement reinterpretation needed by the one-owner topology, and the directly
related traceability corrections. It does not absorb unrelated CI, release, installer,
security, auth UI, or admin UI backlog.

</domain>

<decisions>
## Implementation Decisions

### Consolidated library topology

- **D-01:** Preserve the current single `library_tests_shard` execution owner invoking
  `MIX_ENV=test mix ci`; do not restore the slower Phase 233 two-job matrix plus the
  dedicated scaffold receiver.
- **D-02:** Explicitly redefine TEST-02 around comparable, separately timed work classes
  within the one-owner parity run instead of retaining a literal requirement for two CI
  jobs. The evidence must keep library economics measurable rather than merely deleting
  the old claim. — **Reversibility:** costly — reverting this decision requires restoring
  the matrix, its aggregate dependencies, the partition consumer, and protected evidence.

### Same-run slow-test visibility

- **D-03:** Restore TEST-01 by producing deterministic slow-test timing from the same
  parallel ExUnit execution owned by `mix ci`. A serial `--slowest` run, a second full-suite
  invocation, or timing derived from a separate rerun is prohibited.
- **D-04:** Reuse `Sigra.CI.ExUnitTimingFormatter` unless implementation research finds an
  equivalent existing seam with stronger determinism. The CI owner must set the receipt
  path, preserve the artifact, and prove the formatter is a production consumer rather
  than a self-test-only module.

### Install and scaffold economics

- **D-05:** Keep ordinary tests and `ci.install_golden` in the contributor-facing `mix ci`
  alias and on every pull request. Do not demote upgrade, golden-diff, idempotency, or
  scaffold coverage to a non-PR lane.
- **D-06:** Add explicit same-run timing around the ordinary-test and install/scaffold
  portions and retain named exact-once coverage evidence. TEST-03 passes only when the
  measured receipt shows the subprocess-heavy class is not the dominant leg, or when a
  targeted remediation changes that measured result and a fresh receipt proves it.

### Traceability and terminal truth

- **D-07:** Update TEST-01/02/03 requirement wording/status only after the new deterministic
  contracts and same-run evidence pass. Record the TEST-02 reinterpretation explicitly;
  do not silently turn the old two-shard sentence green.
- **D-08:** Reconcile the already-verified DX-01/02/03/04/06 traceability rows from
  `Gaps Found` to `Complete`, and make the Phase 235 verification body agree with its
  passing frontmatter and 11/11 deterministic result.
- **D-09:** Preserve FAST-01's source-complete n=52/p50=469 attestation, its immutable miss
  history, and the protected 93-row GATE-05 ledger byte-for-byte unless a fail-closed
  ownership reconciliation is strictly required by the new timing artifact.

### the agent's Discretion

- Exact receipt schema, helper names, and step labels, following existing machine-readable
  Phase 233/235 evidence patterns.
- Whether work-class timing is implemented by a small Mix task, shell wrapper, or alias
  helper, provided `mix ci` remains the single contributor-facing command and timing cannot
  mask a failed leg.
- The quantitative comparability bound for TEST-02, provided it is justified from current
  same-run measurements and encoded in a deterministic contract rather than prose alone.

### Operator Decision Addendum — 2026-09-10

This addendum is the current authority for the empirical fallback. It supersedes only
**D-02, D-05, and D-06**; D-01, D-03, D-04, and D-07–D-09 remain binding.

- **D-02 superseded:** TEST-02 returns to two exhaustive ordinary-test partitions inside
  the single `library_tests_shard` / `MIX_ENV=test mix ci` owner. The partitions run
  sequentially at the owner level, retain parallel ExUnit execution within each, exclude
  all scaffold modules, emit deterministic per-test receipts, and must have independently
  verified comparable raw durations. This does not restore the retired two-job matrix.
- **D-05 superseded:** The exact six subprocess-heavy install/scaffold modules leave PR
  `mix ci` and move to one named hard-signal job that executes on every `schedule` and
  `workflow_dispatch` event. The job must upload exact receipt and diagnostic artifacts,
  fail on missing evidence, and never enter the byte-stable required PR aggregate.
- **D-06 superseded:** Ordinary-vs-scaffold comparability and `install_scaffold <= ordinary`
  are retained only as failed historical predicates. Current authority is (a) comparable
  exhaustive ordinary partitions on a fresh attempt-1 PR run and (b) exact-six successful
  scaffold execution on a fresh attempt-1 non-PR dispatch, followed by distinct validation
  runs at the evidence commit.

The evidence basis is attempt-1 runs `34431739531` and `34435818106`, including the latter's
`28,671ms` ordinary and `79,614ms` install/scaffold measurements, plus the final `57,389ms`
content-rebase hard stop after safe optimizations. These are negative diagnostics, not a
passing claim. Further reduction would weaken required compile/runtime coverage. The
original TEST-03 wording explicitly authorizes moving subprocess-heavy install tests to a
non-PR lane with recorded justification, so this is requirement-conforming scope evolution.
Plans 01/02/03/06/07 and their source optimizations remain completed history; Plan 04 remains
failed history; Plans 08/09 are completed implementation history and Plan 10 is blocked
evidence history. Implementation and live evidence are still pending, so this addendum
makes no terminal success claim.

### CI Root-Cause Recovery Addendum — 2026-09-10

This addendum does not supersede any D-01–D-09 decision or the operator fallback above.
It records the two confirmed deterministic causes of Plan 10's final failed capture pair
and authorizes only the bounded repair/evidence sequence `11 → 12 → revised 05`.

- The partition-2 assignment includes
  `test/support/ci/ex_unit_timing_formatter_test.exs`. Its write test hardcoded and later
  removed `/tmp/sigra-library-1-timings.json`, erasing the already-successful partition-1
  receipt. The repair uses the already-allowed scaffold timing path and must prove both
  ordinary receipt bytes survive the complete sequential run.
- `test/support/install_fixture.ex` seals graph `shared_deps` read-only before variant
  compilation. Rebar 3.25.1 persists its compiler DAG below dependency source
  `_build/prod/lib/.rebar3`; creating `<MIX_BUILD_PATH>/lib/<dep>/.mix` cannot satisfy that
  write and reproduced `{badmatch,{error,enoent}}`. The single exceptional retry must use
  a validated private writable copy of dependency sources through retry-only
  `MIX_DEPS_PATH`, while canonical shared deps stay immutable and the exact two-worker
  ceiling remains unchanged.
- Plan 10 and attempt-1 runs `34466384009`, `34466384470`, `34467186749`,
  `34467189602`, `34468109536`, and `34468110161` are immutable negative history. They
  cannot become evidence for Plan 12.
- Plan 12 may observe at most three new tree-equivalent candidate commits. Each candidate
  gets exactly one attempt-1 PR run and one attempt-1 workflow_dispatch run at the same
  SHA; no reruns are allowed. Any deterministic selected-route/repository failure records
  diagnostics and blocks for replanning immediately. Only a durably proven non-code
  GitHub infrastructure transient may advance to another byte-identical-tree candidate.
  After one admissible capture pair, the evidence commit gets one distinct attempt-1
  validation pair. Either failed validation route blocks for replanning.

### Plan 12 Recovery Addendum — 2026-09-10

This addendum does not supersede D-01–D-09 or either earlier addendum. Plan 12 followed its
hard-stop contract: PR run `34493873867` and workflow_dispatch run `34493911924` were
attempt 1 at SHA `a2d1840dfe17640184bebbe2bba786a53fdbf39b`, failed deterministic repository
checks, were not rerun, and admitted no evidence. Plans 10/12 and all eight failed run IDs
remain immutable negative history. The route recorded at that checkpoint was
`13 → 14 → revised 05`; the later Plan 13 comparability addendum supersedes that routing.

- The Plan 11 formatter test still wrote both ordinary receipt files and removed them in
  `on_exit`; its assertions ran before cleanup, while the shell regression used a fake
  child that never executed the real test. Plan 13 must remove every ordinary-path
  filesystem mutation/cleanup from that ExUnit source, reject reintroduction through a
  whole-source AST or mutation contract, and prove the real post-partition-2 lifetime.
- The no-compile entrypoint emitted exactly two canonical unavailable-app warning blocks
  for `:sigra_install_golden_tmp` followed by one for `:phoenix_live_view`. Plan 13 may
  remove only contiguous leading canonical blocks for those two explicit atoms; every
  other application, warning shape, position, and installer byte remains authoritative.
  Real compile and warnings-as-errors gates do not change.
- The install evidence verifier depended on unavailable `rg`. Plan 13 must use
  deterministic find/grep ownership discovery, validate safe paths/order, and prove the
  verifier under a hermetic PATH with no ripgrep.
- Plan 14 owns exactly one new attempt-1 PR/dispatch capture pair at one implementation
  SHA and, only after success, exactly one distinct attempt-1 validation pair. A
  deterministic selected-route failure records durable diagnosis and blocks immediately;
  implementation repair requires a new plan. A durably classified non-code GitHub
  transient must be retried exactly once under the same rate safeguards, but each run ID
  receives at most one `gh run watch` across all attempts. The retry is observed by one
  non-concurrent 60-second `gh run view` monitor, never a second watcher. Its
  attempt-above-1 result is diagnostic only and can never become evidence; the pair then
  blocks with no replacement candidate, repair, fabrication, or waiver.

### Plan 13 Comparability Recovery Addendum — 2026-09-10

This addendum does not supersede D-01–D-09, the operator fallback, or the earlier recovery
addenda. Plan 13 completed and focused-greened all three Plan 12 repository repairs, but
its terminal local gate remained red: two unchanged real runs measured partition 1 / 2 as
`9,103/2,430ms` and `10,963/4,242ms` (plus the recorded S4 observation), above the fixed
`max/min <= 2.0` TEST-02 bound. Plan 13 and summary commit `265b0874` are blocked history;
the formatter, warning-normalization, and no-ripgrep repairs remain implementation
authority, while none of its timing pairs is passing or live evidence.

- The confirmed cause is the partition oracle's drift handling. Its Phase 233 cost source
  predates fourteen current ordinary files. `build_partitions!/1` appends every missing
  path with `time_us: 0`, and LPT assigns exact total ties to partition 1. Since zero never
  increases that total, all post-snapshot paths accumulate in partition 1. Those paths
  include `test/support/install_fixture_performance_test.exs` (about 4.5–4.9 seconds in
  the observed receipts) and the Phase 235/235.1 planning contracts.
- Plan 15 is local-only TDD authority to replace zero-cost fallback with a tracked,
  bounded exact-schema full-current calibration containing all three real local combined
  receipts and all six timing receipts as complete canonical base64 payloads with exact
  source path, byte count, and SHA-256. The loader must decode and validate every retained
  payload, link each combined manifest to its two timing test-row sets, recompute every
  per-file raw sum directly from retained rows, apply `max(1,sum)`, then take the
  deterministic three-sample median. Missing, stale, duplicate, scaffold, zero, oversized,
  opaque-hash-only, or forged calibration blocks manifest generation. Hand-moving lists is
  not authorized.
- Plan 15 must finish with three consecutive fresh real local runs that each preserve both
  receipts, pass both partitions, and independently satisfy the unchanged factor-of-two
  verifier. It may not drop/duplicate/move a test outside the two exhaustive manifests,
  alter the single PR owner or aggregate, touch live evidence, or dispatch GitHub.
- Plan 14 remains the live capture/validation authority because it has not consumed its
  one admissible pair. Its evidence and watcher policy is unchanged, but it now depends on
  successful Plan 15. The active route is blocked Plan 13 → Plan 15 → Plan 14 → revised
  Plan 05.

### Plan 15 Source-Collection Blocker Addendum — 2026-09-10

This addendum preserves Plan 15 and summary commit `2d854758` as immutable blocked
history. Plan 15 stopped before RED and admitted zero calibration samples and zero raw
payloads. Its replayable calibration design remains authoritative, but its route is
superseded by Plan 16.

- The exact source command failed because
  `test/support/install_fixture_performance_test.exs` constructed a real prepared graph
  and passed literal `1_000` to `diagnostic_receipt/2`. After Plan 11 correctly included
  preparation work, the exact positive diagnostic phase sum may exceed 1,000ms, so the
  production validator correctly rejected the synthetic receipt.
- Plan 16 first changes only that test fixture. It must construct the no-raw diagnostic
  receipt, sum its actual exact six positive phase durations, set
  `raw_install_duration_ms` to that exact sum, and prove sum-minus-one is rejected.
  Production `diagnostic_receipt/2` and `validate_diagnostics!/1` are immutable; arbitrary
  padding, multipliers, threshold increases, retries, or validation weakening are not
  authorized.
- Only after the focused and whole performance-test source pass may Plan 16 restart
  calibration collection from zero. None of Plan 15's failed-run bytes may enter the
  artifact. Plan 16 then carries forward unchanged the bounded nine-payload replay,
  direct per-file raw sums, `max(1,sum)`, three-sample median, exact-universe fail-closed
  loader, and three consecutive independent local <=2.0 validations.
- Plan 14 retains exactly the same single capture-pair and distinct validation-pair live
  authority and AGENTS.md watcher/rate/transient policy, but now depends on successful
  Plan 16. The active route is blocked Plan 13 → blocked Plan 15 → Plan 16 → Plan 14 →
  revised Plan 05.

### Plan 16 Global-Registrant Blocker Addendum — 2026-09-10

Plan 16 summary `84550d73` is immutable blocked history at its first post-calibration
whole-source prerequisite. Its exact-bound fixture, 2,922,739-byte calibration artifact,
three retained source samples/nine payloads, strict replay loader, 225 positive costs,
97/128 placement, and equal 54,838,062us modeled totals remain implementation authority.
The failed prerequisite recorded partition 1 success at 37,517ms and partition 2 failure
at 26,409ms; it completed zero of three validation pairs and is not passing evidence.

- Repository discovery found only two async modules that register the VM-global `Oban`
  name: `test/sigra/account/deletion_test.exs` and `test/sigra/delivery_test.exs`. They are
  co-located in calibrated partition 2, and a focused repeated two-file run failed again
  on iteration 2. The blocker is test-global ownership, not calibration or placement.
- Plan 17 is test-only authority to mark both modules `async: false` and replace open-coded
  dummy ownership with a cleanup closure installed before registration. Every cleanup
  invocation—including synchronous registration-failure cleanup and later ExUnit
  `on_exit`—must create its own monitor in the invoking process, exact-PID-check before
  unregister, kill if alive, and await its local bounded `:DOWN`; repeated cleanup of an
  already-dead PID receives `:noproc`. No monitor reference may cross processes.
  Adversarial cross-process/on-exit-equivalent coverage must prove failed acquisition
  leaks no sleeper and cleanup never removes or kills an unrelated/replacement owner.
- Plan 17 may not edit production, calibration, replay loader, derived costs, partition
  manifests/placement, runner topology, or the <=2.0 predicate. After focused repeated
  coverage, it runs one fresh whole-source prerequisite and then exactly three later
  fresh calibrated validations. Each validation must pass independently; no retry,
  averaging, retuning, or prerequisite substitution is permitted.
- Plan 14 retains its live evidence and GitHub policy unchanged, now depending on Plan 17.
  The active route is blocked Plan 13 → blocked Plan 15 → blocked Plan 16 (calibration
  retained) → Plan 17 → Plan 14 → revised Plan 05.

### Plan 14 Candidate-Byte Blocker Addendum — 2026-09-10

Plan 17 completed its identity-safe cleanup and exactly three local calibrated validations.
Plan 14 then consumed its sole capture pair and halted with summary `8d463e5a`: PR run
`34520992740` failed the immutable workflow-byte contract, while dispatch run
`34520986751` succeeded on the selected exact-six route. Both were attempt 1 at
`47f9578fc97edc63603953d446e79fac884326c3`; the pair is negative history, no artifacts
were admitted, and no validation pair ran.

- The PR cause is exactly one missing blank line. Candidate `.github/workflows/ci.yml`
  hashed `cf46fc226daec325db1d3191c61158f9da55edb24b2f5cddac60bcb429aeb3f1`, while Plan 17
  source authority is `ae1e2b519a433720aeb8f7a598d3869e3a5d73c871092f4e6df60456ee413682`.
  The partition-2 and verifier failures were cascade from this immutable-contract test,
  not new implementation/calibration defects.
- Plan 18 supersedes Plan 14. Its first task must construct the canonical ordered ten-run
  ledger from immutable Plan 10/12/14 summary commits; no prior machine-readable eight-run
  records exist. The ledger pins a closed schema and every available field for all ten,
  appends PR `34520992740` then dispatch `34520986751`, marks every record
  `pair_inadmissible`, and records dispatch `34520986751` as overall failure with selected
  exact-six success. Both capture receipts carry byte-identical arrays, validation binds
  their digest/order, and mutation coverage rejects every field, null, member, and order.
- Candidate authority is exactly Plan 17 commit
  `f0b5af4080325b71dbced7637f86adf76f075e79` plus the exact Task 1 GREEN commit. Because
  that commit and its hashes do not exist at planning time, Task 1 must generate and verify
  a canonical handoff manifest after commit with the commit ID, exactly its five changed
  files, post-commit SHA-256/blob IDs, and the ten-run ledger/digest. Task 2 must verify the
  manifest and apply that named commit to the scoped evidence worktree before candidate
  preflight; it cannot independently recreate the changes.
- Before any push, the candidate must restore authoritative workflow bytes without
  semantic/pin weakening, permit exactly the five manifest-pinned Task 1 files to differ
  from Plan 17, reject every other truth-bearing drift, run the focused Phase 235.1
  contract, and run the complete Plan 17 selected/local chain inside that exact worktree.
  No Phase 235.1 Plan 10/12/14/15/16/17/18 summary is eligible for a historical-summary
  exception; any other summary needs recorded path/digests and proof of no truth consumer.
- Plan 18 owns exactly one new attempt-1 capture pair and, only after dual selected-route
  success, one distinct attempt-1 validation pair. Plan 14's rate/watcher/403/429 and
  transient-retry non-admission rules apply unchanged. Deterministic failure blocks
  immediately; no repair between live candidates, replacement, waiver, or failed artifact
  reuse is authorized.
- The active route is completed Plan 17 → blocked Plan 14 → Plan 18 → revised Plan 05.

### Plan 18 Local-Calibration Blocker Addendum — 2026-09-10

Plan 18 summary commit `81afbf0cafcf7dcf380d728a03053c42cdccdaa0` is immutable blocked
history. Its ten-run ledger and verifier implementation are retained, but its clean
candidate stopped before push: both children passed, partition 1 measured 2,449ms,
partition 2 measured 13,579ms, and ratio 5.544 violated the unchanged `<= 2.0` bound.
Validations 2/3 never ran. Plan 18 created no GitHub run/job ID and no evidence receipt.

- The exact calibration defect is the 49,731,475us derived cost assigned to
  `test/sigra/planning/phase_233_library_economics_contract_test.exs`. During the three
  source samples its `live_scaffold_paths` helper used `Path.wildcard/1`, which traversed
  an ignored Playwright symlink/363MB tree. The clean candidate's actual file time was
  255,179us, so LPT placement was based on non-source environmental cost.
- Plan 19 is local-only. Replace scaffold discovery with tracked NUL-delimited
  `git ls-files` test paths and prove ignored/untracked sparse/self-loop trees are never
  traversed. Keep the exact tracked scaffold set and fail closed on Git/path errors.
- Every Plan 18 summary assertion and every test/script/loader change must be finalized and
  committed before new timing collection. Calibration must contain exact byte-count and
  SHA-256 rows for every current ordinary test path and reject any later current/snapshot
  source drift. Move the calibration artifact path/hash/size pin into an external
  exact-schema machine manifest read by stable contract code; code must not hardcode the
  new artifact digest and thereby make its own source snapshot circular. After the source
  commit, a temporary uncommitted schema-upgraded copy of the prior calibration may choose
  exhaustive bootstrap placement only; its old timings/payloads never enter final output.
- Plan 19 was authorized to collect exactly three wholly successful triples and retain all
  nine raw payloads before three later validations. Its actual fixed-placement block and
  Plan 21's fresh replacement collection are recorded below; neither local plan performs
  GitHub or evidence action.
- The exact-tree live plan must consume the latest completed local source/evidence-preflight
  tree OID. Prefer a reproducible `git commit-tree` object
  with old evidence head `47f9578fc97edc63603953d446e79fac884326c3` as sole parent; merge,
  rebase, cherry-pick, tree drift, and summary exceptions are forbidden. Candidate-local
  contracts and three fresh comparable pairs pass before push.
- Plan 20 owns exactly one attempt-1 capture PR/dispatch pair and, after dual selected-route
  success, exactly one distinct attempt-1 validation pair. It reuses Plan 18's canonical
  ten-run digest and exact AGENTS.md rate/watcher/transient policy: deterministic failure
  blocks immediately; one required non-code transient rerun is diagnostic/non-admissible,
  observed without a second watcher, and leaves the pair blocked with no replacement.
- That recovery route reached blocked Plan 19 and is superseded by the addendum below.

### Plan 19 Fixed-Placement Blocker Addendum — 2026-09-10

Plan 19 summary commit `d405755f078be8eefc8be1abcc2c387a4b6c8589` is immutable
blocked history. Its tracked NUL-safe discovery and v2 source-bound loader remain completed
implementation, but the mandatory frozen-source post-build suite passed only 38/39. The
Phase 235.1 contract hardcoded `test/sigra/account/deletion_test.exs` and
`test/sigra/delivery_test.exs` in partition 2; valid measured LPT moved delivery to
partition 1. Assignment number and co-partition placement are not safety requirements.

- Preserve the real invariant: those are the only two global Oban registrants, both modules
  remain `async: false` with identity-safe synchronous cleanup, and both paths occur exactly
  once in the exhaustive disjoint ordinary universe. Deterministic positive cost-injection
  tests must prove either path may occupy either partition while the exact set, two-owner
  topology, and unchanged `<= 2.0` bound remain enforced. Audit every other pathname-to-
  partition-number assertion before freezing source.
- Plan 19's three duration pairs were `3,174/60,072`, `3,995/41,711`, and
  `3,351/42,354` milliseconds. All nine payload hashes and the attempted artifact/manifest
  hashes in its summary are diagnostic only. The tracked v1 artifact was restored at
  `975612d7f3cbfda75fb6857791ebfd9bcadd852451b86a6b917871b3ba092eeb`, the v2 manifest
  is absent, zero samples/validations were admitted, and no GitHub action or evidence ran.
- Plan 21 supersedes Plan 19 locally. Complete the assignment-agnostic source contracts,
  and before that snapshot finalize all three ordinary tests that Plan 05 previously intended
  to edit: Phase 198 contributor DX, Phase 235.1 economics, and Phase 235 terminal ratification.
  They share a frozen helper and recognize two semantic states without future literal IDs:
  absent validation is pre-evidence (including valid capture-only intermediates) and a valid
  PR/scaffold/validation triple is post-evidence. Present captures are always schema-checked;
  only the full cross-linked triple permits Complete/passed prose. Generated mutations cover
  missing/reused/cross-SHA runs, wrong jobs/artifacts/digests, and incorrect citations.
- Commit a new frozen snapshot only after those lifecycle and assignment contracts pass,
  restart collection from zero through the existing v2 loader
  bootstrap, retain exactly three wholly new triples/nine payloads, rebuild and independently
  replay the v2 artifact/manifest, then run exactly three later fresh `<= 2.0` validations.
  Plan 19 payloads cannot be reused; no failed collection/validation is retried or tuned.
  Plan 21 performs no GitHub or evidence action.
- Plan 20 remains the live plan but now consumes the exact final Plan 21 source/evidence-
  preflight tree. Its capture-only commit remains the validated pre-evidence state. After it
  writes the validation receipt, the receipt-only frozen contract must pass while the full
  document transition is intentionally pending Plan 05. Plan 20 and Plan 05 may not alter
  the shared helper or any of the three ordinary transition tests.
- Plan 05 owns only non-calibrated documents/artifacts. Its three tasks move the frozen
  post-evidence contract green in document-sized steps, then run all three default transition
  modules and verify their bytes still match Plan 21. Active routing is blocked Plan 19 →
  local Plan 21 → live Plan 20 → revised Plan 05.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Milestone truth and closure findings

- `.planning/v1.47-MILESTONE-AUDIT.md` — current 21/24 fail-gate and exact TEST blockers.
- `.planning/REQUIREMENTS.md` — v1.47 requirement wording and traceability table.
- `.planning/ROADMAP.md` — milestone definition of done and inserted Phase 235.1 boundary.
- `.planning/METHODOLOGY.md` — decisive-default and proof-truth escalation rules.

### Prior phase decisions and evidence

- `.planning/phases/233-library-suite-economics/233-CONTEXT.md` — original timing,
  two-shard, receiver, and hard-fail decisions.
- `.planning/phases/233-library-suite-economics/233-EVIDENCE.json` — original shard and
  scaffold timing evidence.
- `.planning/phases/233-library-suite-economics/233-VERIFICATION.md` — phase-local TEST
  verdict before the later integration regression.
- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md` — `mix ci`
  parity contract and one-owner intent.
- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md` — passing
  DX evidence and current parity topology.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — terminal
  FAST/GATE proof plus stale body verdict to reconcile.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json`
  — protected before/after library-owner and PR-wall measurements.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json`
  — current protected ownership ledger.

### Implementation seams

- `.github/workflows/ci.yml` — current library execution owner and terminal aggregate.
- `mix.exs` — `ci`, `ci.install_golden`, and exact-once coverage aliases.
- `test/support/ci/ex_unit_timing_formatter.ex` — reusable parallel timing formatter.
- `test/support/ci/library_test_partitions.ex` — orphaned former two-shard partition export.
- `test/sigra/planning/phase_233_library_economics_contract_test.exs` — current topology and
  coverage contract that must be deliberately evolved.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `Sigra.CI.ExUnitTimingFormatter`: deterministic, atomic per-test timing receipt already
  proven by focused tests; it needs a real CI consumer again.
- `mix ci` / `ci.install_golden`: existing one-command parity boundary with ordinary and
  scaffold-heavy work already separated into measurable alias legs.
- Phase 235 offline attestation and metrics helpers: established patterns for signed or
  fail-closed machine-readable evidence.

### Established Patterns

- `Library tests` is a byte-stable required aggregate; direct execution owners may change,
  but skipped/green aggregates never substitute for observed work.
- Performance claims require real same-run measurements and retained failure outcomes.
- Coverage ownership is exact-set and mutation-tested; a timing refactor must not create a
  second suite owner or silently drop scaffold/golden modules.

### Integration Points

- The library owner in `.github/workflows/ci.yml` invokes `MIX_ENV=test mix ci` and feeds the
  `library_tests` aggregate, which feeds `ci-gate`.
- ExUnit formatter configuration and receipt upload attach to that same owner.
- Work-class timing attaches at the `mix ci` alias boundary and must preserve nonzero exit
  status from either ordinary or install/scaffold work.
- REQUIREMENTS, Phase 235 verification prose, and the milestone audit consume the final
  evidence only after contracts pass.

</code_context>

<specifics>
## Specific Ideas

- Keep one contributor command and one direct library owner; make the internals observable
  enough to prove the economics claims.
- Treat the original two-shard wording as a changed requirement with an explicit rationale,
  not as an implementation detail that can be silently reinterpreted.
- Preserve the p50=469 FAST-01 result while adding timing; any material regression blocks
  closure and must be measured rather than inferred from YAML.

</specifics>

<deferred>
## Deferred Ideas

- The automatic todo matcher returned unrelated auth UI, admin UI, installer, security,
  release, Playwright, and repository-administration work based on generic words such as
  “phase,” “run,” and “test.” None were folded into this fixed TEST-01/02/03 closure phase.
- Adjacent CI todos such as `example_unit_smoke` aggregation, recapture mirror reliability,
  and `admin_eval_render` history remain separately tracked because they do not close the
  library-economics flow identified by the fresh audit.

</deferred>

---

*Phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test*
*Context gathered: 2026-09-09*
