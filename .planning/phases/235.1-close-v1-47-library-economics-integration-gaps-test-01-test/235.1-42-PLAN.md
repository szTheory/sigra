---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 42
type: execute
wave: 39
depends_on: [235.1-41]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate: {tokens: 27000, raw_tokens: 27000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Plan 41's canonical 132-database cleanup is consumed as immutable authority and is never repeated."
    - "Only the exact Plan 41 candidate database/environment may be removed, and only after candidate/tree/owner/path provenance passes."
    - "A plan-local untracked collector is fixture-tested and read-only-dry-run-tested before it captures one admission sample; tests cannot consume live admission metrics."
    - "The collector uses `vm_stat -c 2 1` and one robust psql SQL-file REPEATABLE READ READ ONLY snapshot with no multiline meta-command."
    - "Candidate `3713dcd4`/tree `94fca667`, calibration, 114/111 assignment, threshold, ledger137, and audit state remain unchanged."
    - "One complete healthy sample permits one three-pair series and four fresh original-attempt-1 live runs; any failure blocks without retry."
  artifacts:
    - path: /private/tmp/plan42-collector/health-collector.sh
      provides: "Untracked deterministic fixture/dry-run/admission health collector"
    - path: /private/tmp/plan42-collector-tests.json
      provides: "Canonical fixture and read-only dry-run test receipt"
    - path: /private/tmp/plan42-preflight-health.json
      provides: "Only Plan 42 admission receipt"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
      provides: "Fresh live evidence validation"
  key_links:
    - from: "Plan 41 cleanup receipt `399b2455`"
      to: "Plan 42 environment baseline"
      via: "duplicate-safe canonical replay; zero residual allowlisted databases"
    - from: "fixture and dry-run collector receipt"
      to: "one-shot admission invocation"
      via: "same script SHA and fixed mode separation"
    - from: "canonical ledger137"
      to: "Plan 42 new9"
      via: "strict sorted lowercase-hex disjointness"
      pattern: "0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08"
---

<objective>
Repair the health collection mechanism without changing candidate bytes, then perform one new health-gated recovery and strict live route.

Purpose: Plan 41 completed the destructive cleanup safely. Its only health capture was incomplete because two collector commands were malformed, so repeating cleanup or treating partial host values as evidence would be incorrect.
Output: Verified temporary collector, one complete admission receipt, one bounded timing series, and fresh capture/validation receipts.

Decision coverage: D-01 preserves exact PR ownership; D-02–D-04 preserve calibrated partitions/threshold; D-05/D-06 preserve exact-six routing; D-07 requires fresh evidence; D-08 defers reconciliation; D-09 retains all failed history.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-39-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-40-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-41-SUMMARY.md
@scripts/ci/library-partitions.test.sh
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
</context>

## Accepted Cleanup and Failed-Capture Authority

Independently replay regular non-symlink `/private/tmp/plan41-ref-authority.json` and `/private/tmp/plan41-resource-cleanup.json`. Reject duplicate keys, require exact `LC_ALL=C jq -S -c` bytes plus one LF, and reconstruct their omitted-`receipt_sha256` preimages. Exact accepted digests are `8dd749aea9f8d1a6a4e14fc938e38ce7f9bc625b11b3355bf72cc74392232780` and `399b24553b3b00b80f1967a5ed41492f4ef26c7104714c97f5c5c4b00f9d42f9`. Cleanup proves 132/132 drops, zero terminations/failures/residual allowlisted databases/sessions, and unchanged 1,667-name nonmatching set SHA `fc90950d4c7467e3dd91a19ccbbdc6672e74fea25f14db10d6344775a52c6a02`. Never reconstruct or execute that allowlist again.

Plan 41 then created only candidate database `sigra_plan41_0ad0ed669063` and accepted isolation `/private/tmp/sigra-plan41.XaQFR7`, whose candidate tree is `94fca667d3f5226347548bdeea625bc737a4348e`. Current nonmatching state must equal the cleanup-preserved exact names plus that one database—no unexplained delta. Its only health marker exists, but health DB raw data, preimage, final receipt, timed marker/receipts, and recovery ledger do not. Canonical failure diagnostic SHA is `798c86bc581cacc2e33ec2496ee373c08fbe7b18ac309a7c4f54ee4d8aa77110`; it records bad `vm_stat 1 2`, corrected platform syntax not admitted, a multiline `\copy` parse error, and zero timing/GitHub. Candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3` remains unpushed over PR head/sole parent `87338db7c2678601e20ed3140fd8de1981dfbeb1`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Consume cleanup authority and prove a deterministic collector (D-01, D-07, D-09)</name>
  <files>/private/tmp/plan42-collector/health-collector.sh, /private/tmp/plan42-collector/health.sql, /private/tmp/plan42-collector/tests/run.sh, /private/tmp/plan42-collector/fixtures/vm-stat-wide.txt, /private/tmp/plan42-collector/fixtures/vm-stat-lines.txt, /private/tmp/plan42-collector/fixtures/psql-valid.txt, /private/tmp/plan42-collector/fixtures/psql-malformed.txt, /private/tmp/plan42-collector/fixtures/json-duplicate.txt, /private/tmp/plan42-collector-tests.json, /private/tmp/plan42-collector-verify.once, /private/tmp/plan42-environment-disposition.json</files>
  <behavior>
    - Fixture argv: Darwin collector invokes exactly `vm_stat -c 2 1`; reversed/positional count syntax is rejected.
    - Fixture parsing: wide and line-oriented two-reading vm_stat fixtures yield deterministic pageout/swapout deltas without a live observation.
    - Fixture SQL: tuples-only fixture maps all required database/activity/lock/snapshot fields; malformed/missing/extra tuples and psql meta-commands fail.
    - Live dry run: executes the SQL file read-only, validates shape/transaction facts, labels output inadmissible, and cannot create the admission marker/receipt.
    - Canonical JSON: duplicate keys, noncanonical bytes, included digest fields, and reconstructed digest mismatch fail.
  </behavior>
  <precondition>Both Plan 41 accepted receipts and failure diagnostic independently replay at exact digests, all 132 allowlisted databases remain absent, and Plan 41 timing/GitHub counts remain zero.</precondition>
  <action>First consume—not repeat—the accepted cleanup. Replay every receipt identity/digest/snapshot/owner/132-action/nonmatching-set assertion plus Plan 41 summary and failure diagnostic. Recheck exact PR head/candidate/tree/parent, absent health raw/preimage/final receipt/timed marker/recovery ledger/evidence receipts, and zero candidate/evidence workflow runs or remote refs. This exact replay transfers only the unspent health/timing/live budget; never issues an allowlist query that becomes cleanup authority and never drops any of the 132 names again.

Handle the one Plan 41 candidate environment deterministically. Require exact database name `sigra_plan41_0ad0ed669063`, owner equality through the same resolved candidate test DB identity, zero sessions after immediate PID/database/user recheck, and configuration provenance inside physical root `/private/tmp/sigra-plan41.XaQFR7`; require that root has no live process, resolves beneath `/private/tmp`, has no symlink traversal, and its candidate commit reproduces tree `94fca667...`. If every fact passes, server-quote and drop only that exact database, prove the current nonmatching set returns byte-for-byte to the cleanup receipt's 1,667 names/SHA `fc90950d...`, then remove only that exact physical root after device/inode/path recheck. If provenance is incomplete, retain both and bind them as an explicit expected database/path exclusion in later health; do not partially remove or guess. Record the chosen single branch, before/action/after or retained identities, and canonical digest in `/private/tmp/plan42-environment-disposition.json`.

Create one exact untracked candidate-external bundle rooted at `/private/tmp/plan42-collector` containing exactly eight regular non-symlink files and no others: `health-collector.sh`, `health.sql`, `tests/run.sh`, and fixtures `vm-stat-wide.txt`, `vm-stat-lines.txt`, `psql-valid.txt`, `psql-malformed.txt`, `json-duplicate.txt`. No bundle byte may enter candidate/workspace Git state. The collector has disjoint `--fixture`, `--dry-run`, and `--admission` modes, but all three bind the same exact `health.sql` path, size, and SHA—no generated, copied, substituted, templated, or mode-specific SQL. On Darwin, production argv is exactly `vm_stat -c 2 1`; a fake fixture shim records argv and supplies fixed outputs. The one SQL file begins `BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ READ ONLY`, contains one fixed statement returning a versioned/count-delimited tuples-only record for runtime identity, backend PID, xid, `pg_current_snapshot()`/xmin/xmax/xip, activity, wait/block, lock, and scratch facts, and ends exactly with `ROLLBACK;`. Invoke it with `psql -X --no-psqlrc --set=ON_ERROR_STOP=1 --tuples-only --no-align --file`; forbid `\copy`, every other psql meta-command, command interpolation, COMMIT, writes, and multiple connections/snapshots.

RED: write `tests/run.sh` so its fixture/adverse cases reproduce and reject both Plan 41 bugs before collector GREEN. GREEN: implement corrected argv, strict tuple parser, validation, duplicate-safe sorted-compact-one-LF JSON, and omitted-digest preimage. `tests/run.sh --verify-once` must atomically create `/private/tmp/plan42-collector-verify.once` with exclusive-create semantics and refuse any prior marker; it then invokes the fixture/adverse suite exactly once without live `vm_stat`/PostgreSQL, followed by exactly one PostgreSQL-only read-only `--dry-run` using the same SQL bytes. Dry-run may validate connection/transaction/output shape only; it cannot call `vm_stat`, calculate admission, create a health marker/receipt, retain measured health values, or authorize timing. Require ROLLBACK/no writes and mark output `admissible:false`. Do not invoke this wrapper in the action; the `<automated>` command below is its sole invocation.

The wrapper writes canonical `/private/tmp/plan42-collector-tests.json` only after success. Its `bundle.files` is an exact sorted eight-entry manifest with absolute path, byte size, and SHA-256; include an exhaustive physical-root file count/list, aggregate manifest digest, exact wrapper argv, exclusive marker inode/time/content hash, fixture/adverse results, sole dry-run result, identical SQL hash observed by every mode, and receipt digest. Independent verification must enumerate the physical root itself and hash every file rather than trust booleans or wrapper output.</action>
  <verify>
    <automated>bash /private/tmp/plan42-collector/tests/run.sh --verify-once &amp;&amp; python3 -c 'import hashlib,json,os,sys; nodup=lambda p: (_ for _ in ()).throw(ValueError("duplicate key")) if len(p)!=len({k for k,_ in p}) else dict(p); r=json.load(open("/private/tmp/plan42-collector-tests.json"),object_pairs_hook=nodup); root="/private/tmp/plan42-collector"; paths=[root+"/health-collector.sh",root+"/health.sql",root+"/tests/run.sh",root+"/fixtures/vm-stat-wide.txt",root+"/fixtures/vm-stat-lines.txt",root+"/fixtures/psql-valid.txt",root+"/fixtures/psql-malformed.txt",root+"/fixtures/json-duplicate.txt"]; actual=sorted(os.path.join(d,f) for d,_,fs in os.walk(root) for f in fs); got={x["path"]:(x["bytes"],x["sha256"]) for x in r["bundle"]["files"]}; assert actual==sorted(paths)==sorted(got); assert all(os.path.isfile(p) and not os.path.islink(p) for p in paths); assert all(got[p]==(os.path.getsize(p),hashlib.sha256(open(p,"rb").read()).hexdigest()) for p in paths)' &amp;&amp; python3 -c 'import json,sys; json.load(open(sys.argv[1]),object_pairs_hook=lambda x: (_ for _ in ()).throw(ValueError("duplicate key")) if len(x)!=len({k for k,_ in x}) else dict(x))' /private/tmp/plan42-environment-disposition.json &amp;&amp; cmp -s /private/tmp/plan42-collector-tests.json &lt;(LC_ALL=C jq -S -c . /private/tmp/plan42-collector-tests.json) &amp;&amp; cmp -s /private/tmp/plan42-environment-disposition.json &lt;(LC_ALL=C jq -S -c . /private/tmp/plan42-environment-disposition.json) &amp;&amp; TEST_EXPECTED="$(jq -r '.receipt_sha256' /private/tmp/plan42-collector-tests.json)" &amp;&amp; TEST_ACTUAL="$(LC_ALL=C jq -S -c 'del(.receipt_sha256)' /private/tmp/plan42-collector-tests.json | shasum -a 256 | awk '{print $1}')" &amp;&amp; test "$TEST_ACTUAL" = "$TEST_EXPECTED" &amp;&amp; jq -e '.wrapper.invocation_count == 1 and .wrapper.argv == ["--verify-once"] and .fixture.vm_stat_argv == ["-c","2","1"] and .fixture.valid == true and .fixture.adverse == true and .fixture.live_observations == 0 and .dry_run.invocation_count == 1 and .dry_run.admissible == false and .dry_run.writes == 0 and .dry_run.health_values_retained == false and .dry_run.health_marker_created == false and .dry_run.health_receipt_created == false and .sql.same_sha_all_modes == true and .sql.repeatable_read == true and .sql.read_only == true and .sql.ends_rollback == true and .sql.meta_commands == 0 and (.receipt_sha256 | test("^[0-9a-f]{64}$"))' /private/tmp/plan42-collector-tests.json</automated>
    <fails_when>Either accepted receipt/diagnostic/zero-action fact fails; cleanup allowlist is reconstructed for action or any of 132 names is touched; Plan 41 candidate DB/root lacks exact provenance yet is changed; another database/path/process is touched; current nonmatching set is neither exact cleanup baseline nor baseline plus the retained candidate DB; collector enters tracked state; bundle contains other/missing/symlink files or any independently computed size/hash differs from its exact receipt row; wrapper marker preexists, wrapper/fixture/adverse/dry-run invocation count differs from one, or rerun succeeds; fixture mode samples live state; dry run calls vm_stat, retains admission values, creates marker/receipt, writes DB state, or authorizes timing; Darwin argv differs; any mode substitutes SQL; SQL lacks exact RR/RO/ROLLBACK, contains meta-commands/COMMIT/writes, or uses multiple snapshots; fixture adverse case passes; canonical/digest/hash proof fails; or admission/timing/GitHub begins.</fails_when>
  </verify>
  <done>Accepted cleanup remains untouched, the sole Plan 41 candidate environment is exactly removed or explicitly retained, and one pinned untracked collector passes fixtures plus an inadmissible read-only dry run.</done>
</task>

<task type="auto">
  <name>Task 2: Capture one complete health sample and run one recovery series (D-02–D-04, D-07, D-09)</name>
  <files>/private/tmp/plan42-admission.once, /private/tmp/plan42-admission-invocation.json, /private/tmp/plan42-preflight-health.json, /private/tmp/plan42-recovery-payloads.txt</files>
  <precondition>Task 1 receipts are canonical, collector/SQL hashes remain pinned, no Plan 42 admission marker exists, and PR head is exact `87338db7`.</precondition>
  <action>Create a wholly fresh standalone archive-only isolation of exact candidate/tree, with private copied locked deps/cache, homes/tmp/database, unset `MIX_BUILD_PATH`, new default `_build/test`, offline dependency/compile-only/full untimed gates, clean 225-source tree, exact Plan 35 calibration and 114/111 assignment. Do not reuse Plan 35–41 build/database/timing state. Reprove canonical ledger137 at 137 lines/8,905 bytes/SHA `0d31c343...`.

After every test/dry-run/gate is complete, independently re-enumerate and hash the exact eight-file bundle and reconstruct the exact collector-tests receipt digest; require equality to Task 1's manifest/digest. Exclusively create `/private/tmp/plan42-admission.once`, then invoke the pinned collector exactly once with argv `[/private/tmp/plan42-collector/health-collector.sh,--admission,--sql,/private/tmp/plan42-collector/health.sql]` and a closed, sorted environment allowlist/digest. It must execute `vm_stat -c 2 1`, collect all other fixed host/disk/process facts, and execute the same exact `health.sql` bytes once in one REPEATABLE READ READ ONLY psql connection ending ROLLBACK. No copied/generated/mode-specific SQL is permitted.

Before reading metrics, the collector records its own resolved `$0`, argv, PID/start time, closed environment key/value digest with secrets reduced to presence/hashes, exact eight-file pre-manifest, collector-tests receipt preimage digest/full-file SHA, and marker inode/time/content SHA. After collection it records the same file manifest again and requires byte equality. Write duplicate-safe canonical `/private/tmp/plan42-admission-invocation.json` with those bindings, raw-output hashes/counts, exit status, and final health-file SHA; compute its omitted-field receipt digest independently. Emit `/private/tmp/plan42-preflight-health.json` atomically only after every raw field validates. Reject duplicate keys; require exact byte equality to `LC_ALL=C jq -S -c` plus one LF; independently reconstruct SHA from the preimage with `receipt_sha256` omitted. Include the exact bundle manifest/aggregate digest, collector-tests preimage digest and file SHA, invocation receipt digest, SQL hash observed by fixture/dry-run/admission, environment/disposition/Plan41 receipt hashes, isolation/candidate identities, sample count one, and any predictably retained Plan 41 candidate exclusion. Admission requires load1 <=18, normal memory/no swap growth, >=10 GiB free, zero competing candidate process, DB probes <=1,000ms, zero foreign non-idle sessions/waiters/blockers/ungranted locks, zero allowlisted databases, and exact expected nonmatching/candidate state. Incomplete/unhealthy blocks with no repair/resample.

With no intervening work, invoke `bash scripts/ci/library-partitions.test.sh` exactly once. Require exactly three fresh pairs/nine hashes, six green children, exhaustive/disjoint 225 paths, unchanged weights/114-111 assignment, truthful contained receipts, and ratio <=2.0 each. Persist new9 as lowercase64 `LC_ALL=C` sorted unique, exactly 9 lines/585 bytes/one final LF, zero intersection with canonical137. Any failure permanently blocks without retry, cleanup, resample, tuning, recalibration, source/candidate change, or GitHub.</action>
  <verify>
    <automated>python3 -c 'import hashlib,json,os,sys; nodup=lambda p: (_ for _ in ()).throw(ValueError("duplicate key")) if len(p)!=len({k for k,_ in p}) else dict(p); t=json.load(open("/private/tmp/plan42-collector-tests.json"),object_pairs_hook=nodup); h=json.load(open("/private/tmp/plan42-preflight-health.json"),object_pairs_hook=nodup); i=json.load(open("/private/tmp/plan42-admission-invocation.json"),object_pairs_hook=nodup); root="/private/tmp/plan42-collector"; paths=[root+"/health-collector.sh",root+"/health.sql",root+"/tests/run.sh",root+"/fixtures/vm-stat-wide.txt",root+"/fixtures/vm-stat-lines.txt",root+"/fixtures/psql-valid.txt",root+"/fixtures/psql-malformed.txt",root+"/fixtures/json-duplicate.txt"]; actual=sorted(os.path.join(d,f) for d,_,fs in os.walk(root) for f in fs); manifest=[{"path":p,"bytes":os.path.getsize(p),"sha256":hashlib.sha256(open(p,"rb").read()).hexdigest()} for p in paths]; assert actual==sorted(paths); assert manifest==t["bundle"]["files"]==h["collector_bundle"]["files"]==i["pre_files"]==i["post_files"]; test_file_sha=hashlib.sha256(open("/private/tmp/plan42-collector-tests.json","rb").read()).hexdigest(); test_digest=t["receipt_sha256"]; assert h["collector_tests_receipt_sha256"]==i["collector_tests_receipt_sha256"]==test_digest; assert h["collector_tests_file_sha256"]==i["collector_tests_file_sha256"]==test_file_sha' &amp;&amp; cmp -s /private/tmp/plan42-preflight-health.json &lt;(LC_ALL=C jq -S -c . /private/tmp/plan42-preflight-health.json) &amp;&amp; cmp -s /private/tmp/plan42-admission-invocation.json &lt;(LC_ALL=C jq -S -c . /private/tmp/plan42-admission-invocation.json) &amp;&amp; TEST_EXPECTED="$(jq -r '.receipt_sha256' /private/tmp/plan42-collector-tests.json)" &amp;&amp; TEST_ACTUAL="$(LC_ALL=C jq -S -c 'del(.receipt_sha256)' /private/tmp/plan42-collector-tests.json | shasum -a 256 | awk '{print $1}')" &amp;&amp; INV_EXPECTED="$(jq -r '.receipt_sha256' /private/tmp/plan42-admission-invocation.json)" &amp;&amp; INV_ACTUAL="$(LC_ALL=C jq -S -c 'del(.receipt_sha256)' /private/tmp/plan42-admission-invocation.json | shasum -a 256 | awk '{print $1}')" &amp;&amp; HEALTH_EXPECTED="$(jq -r '.receipt_sha256' /private/tmp/plan42-preflight-health.json)" &amp;&amp; HEALTH_ACTUAL="$(LC_ALL=C jq -S -c 'del(.receipt_sha256)' /private/tmp/plan42-preflight-health.json | shasum -a 256 | awk '{print $1}')" &amp;&amp; test "$TEST_ACTUAL" = "$TEST_EXPECTED" &amp;&amp; test "$INV_ACTUAL" = "$INV_EXPECTED" &amp;&amp; test "$HEALTH_ACTUAL" = "$HEALTH_EXPECTED" &amp;&amp; jq -e '.argv == ["/private/tmp/plan42-collector/health-collector.sh","--admission","--sql","/private/tmp/plan42-collector/health.sql"] and .marker.count == 1 and .exit_status == 0 and .environment.closed == true and .pre_files == .post_files' /private/tmp/plan42-admission-invocation.json &amp;&amp; jq -e '.admission.overall == true and .sample_count == 1 and .collector.vm_stat_argv == ["-c","2","1"] and .snapshot.isolation == "repeatable read" and .snapshot.read_only == true and .sql.ends_rollback == true and .collector_tests_receipt_sha256 != null and .invocation_receipt_sha256 != null and .cleanup_receipt_sha256 == "399b24553b3b00b80f1967a5ed41492f4ef26c7104714c97f5c5c4b00f9d42f9"' /private/tmp/plan42-preflight-health.json &amp;&amp; awk 'length($0) != 64 || $0 !~ /^[0-9a-f]+$/ { bad=1 } END { exit bad }' /private/tmp/plan42-recovery-payloads.txt &amp;&amp; test "$(wc -l &lt; /private/tmp/plan42-recovery-payloads.txt | tr -d ' ')" = 9 &amp;&amp; test "$(wc -c &lt; /private/tmp/plan42-recovery-payloads.txt | tr -d ' ')" = 585 &amp;&amp; test "$(tail -c 1 /private/tmp/plan42-recovery-payloads.txt | od -An -tuC | tr -d ' ')" = 10 &amp;&amp; cmp -s /private/tmp/plan42-recovery-payloads.txt &lt;(LC_ALL=C sort -u /private/tmp/plan42-recovery-payloads.txt) &amp;&amp; test "$(LC_ALL=C comm -12 /private/tmp/plan39-prior-payloads-137.txt /private/tmp/plan42-recovery-payloads.txt | wc -l | tr -d ' ')" = 0</automated>
    <fails_when>Candidate/tree/source/calibration/assignment/ledger changes or prior state is reused; actual eight-file paths/sizes/hashes differ from collector-tests, admission, or invocation manifests; collector-tests preimage digest/full-file SHA differs across recomputation/admission/invocation; tests or dry run occur after marker; more/fewer than one admission invocation/sample occurs; argv/environment/marker/PID/start/log/raw-output/final-receipt binding is absent or inconsistent; pre/post file manifests differ; raw/receipt/invocation JSON is incomplete, duplicate-keyed, noncanonical, self-referential, or digest-invalid; wrong vm_stat argv or substituted/non-ROLLBACK/fragile psql SQL appears; fixed health predicate fails; work intervenes before timing; runner count differs from one; any pair/new9/path/ratio/containment/disjointness proof fails; or retry/tuning/GitHub occurs.</fails_when>
  </verify>
  <done>One complete healthy sample immediately yields exactly one passing three-pair series/new9, or Plan 42 blocks permanently.</done>
</task>

<task type="auto">
  <name>Task 3: Capture and validate strict live evidence (D-01, D-05–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <precondition>Task 2 health and all three pairs passed; exact PR #234 head `ci/phase-235-1-evidence` remains `87338db7`, and candidate remains unpushed.</precondition>
  <action>Recheck PR #234 repository/base/head name/OID and negative planning-ref control. Fast-forward-push exact candidate once only to `refs/heads/ci/phase-235-1-evidence`; dispatch exact-six once at identical SHA. Pair only the resulting new automatic PR/new dispatch original-attempt-1 successes. Then commit exactly the two capture receipts as direct child of candidate, push once to the same PR ref, and pair one new automatic PR/new dispatch original-attempt-1 success at evidence SHA. Require all four IDs mutually disjoint and outside Plans 35–41; Plans 38–41 contribute no live IDs. Apply AGENTS.md rate floor/403/429 stop, one 60-second watcher/run, one structured summary, and failed logs only after failure. Any failure blocks without retry/waiver/substitution. Plan 05 owns reconciliation.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>PR/ref/base/candidate/evidence lineage or exact diff fails; wrong ref advances; run is reused/retried/nonautomatic/unsuccessful/wrong SHA/event/job; IDs overlap; receipt/artifact/digest/path/source provenance fails; watcher/rate rule breaks; extra timing or retry/waiver/substitution occurs.</fails_when>
  </verify>
  <done>Four new disjoint original-attempt-1 successes prove candidate and evidence routes, or live evidence blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Completed cleanup to collector repair | Accepted destructive work must not be repeated. |
| Fixture/dry-run to admission | Test observations must never become selectable health evidence. |
| Temporary collector to candidate | Collector must remain untracked/external so source/calibration stays frozen. |
| Local recovery to live evidence | Only exact candidate and new attempt1 runs may cross. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-42-01 | Elevation of Privilege | prior cleanup/environment | critical | mitigate | Consume canonical receipt; only exact candidate DB/root may be dispositioned. |
| T-235.1-42-02 | Tampering | health collector | high | mitigate | Fixture/adverse tests, pinned script/SQL hashes, robust argv/SQL-file contract. |
| T-235.1-42-03 | Repudiation | admission boundary | high | mitigate | Mode separation, one marker, duplicate-safe canonical receipt. |
| T-235.1-42-04 | Tampering | evidence budget | high | mitigate | One sample, one timed invocation, ledger137 disjointness. |
| T-235.1-42-05 | Spoofing | live evidence | high | mitigate | Exact PR ref and four fresh original-attempt-1 IDs. |
| T-235.1-42-SC | Tampering | packages | low | accept | No package/source change; locked private copies only. |
</threat_model>

## Multi-Source Coverage Audit
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Exact ordinary/exact-six routes | 42 | COVERED | Collector repair then bounded local/live route. |
| REQ | TEST-01 | PR timing evidence | 42 | COVERED | Exact candidate PR capture. |
| REQ | TEST-02 | Comparable exhaustive partitions | 42 | COVERED | Frozen assignment and one series. |
| REQ | TEST-03 | Exact-six receiver | 42 | COVERED | Fresh dispatch capture/validation. |
| RESEARCH | — | No retry/tuning/outcome selection | 42 | COVERED | Test/admission isolation and one-shot budget. |
| CONTEXT | D-01–D-09 | Preserve route/history/order | 42, 05 | COVERED | Plan 41 cleanup stays positive; health failure stays negative. |

<verification>Replay accepted cleanup without repeating it; prove collector fixtures/dry-run; admit one health sample; run one local series and four live attempt1 routes.</verification>
<success_criteria>- Plan 41 cleanup remains canonical and is never rerun.
- Only exact Plan 41 candidate environment is removed or predictably retained.
- Collector tests prove correct vm_stat argv and robust single-snapshot SQL before admission.
- Exactly one sample and one three-pair series occur; new9 are outside137.
- Four fresh disjoint attempt1 runs prove candidate/evidence routes.</success_criteria>
<output>Create `235.1-42-SUMMARY.md` with consumed cleanup digests, environment disposition, collector/SQL/test hashes, Plan 41 failure diagnostic, one-shot health/timing accounting, and live provenance.</output>
