---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 33
subsystem: ci-evidence
tags: [postgres-diagnostic, transient-classification, exact-tree, deterministic-block]
status: halted
dependency_graph:
  requires: [235.1-32, 235.1-31, 235.1-30, 235.1-29]
  provides: [query-index-transient-classification, redacted-postgres-receipt, one-recovery-diagnostic]
  affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
tech-stack:
  added: []
  patterns: [single-invocation-classification, execution-local-hmac, redacted-pg-snapshots, fail-closed-recovery]
key_files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-33-SUMMARY.md
  modified: []
decisions:
  - Classify Plan 32's QueryIndexTest timeout as transient only after every explicit exact-candidate, database, result, cleanup, and no-leak input passed.
  - Stop permanently after the sole fresh recovery series reproduced a different ordinary-suite failure in the second pair; do not retry, change source, or contact GitHub.
requirements-completed: []
actuals:
  tokens: 2087
  tasks: 1
  commits: 0
plan_head_before: 59e0b06de3fbfd7160681c167d5b3147d3f01954
metrics:
  duration: 15m
  completed: 2026-09-10
---

# Phase 235.1 Plan 33: PostgreSQL Classification and Recovery Stop Summary

One exact targeted PostgreSQL test proved Plan 32's setup timeout transient, but the only authorized fresh three-pair recovery series stopped on pair 2 when an async authentication telemetry assertion consumed another async test's event.

## Task 1 — Complete

The Plan 32 diagnostic history replayed before any new test:

- candidate `63910813519db336d5e4556b885cdc0f3879799f`, tree `bd81cf764c901d3e2f728febe1240b57db7a2d7a`, sole parent `47f9578fc97edc63603953d446e79fac884326c3`;
- partial pair combined receipt `d8c859d6...`, partition-1 timing `4db90914...`, and failure log `36d6b529...` matched the Plan 32 summary;
- the prior failure remained 1,254 tests, zero failures, one invalid `Sigra.Audit.QueryIndexTest`, and 30,216ms; no prior pair or payload was admitted into Plan 33.

A new detached worktree at `/private/tmp/sigra-plan33.lXnUhZ/candidate` used the exact candidate with 3,369 independently copied dependency files, zero copied symlinks, zero shared regular-file inodes, candidate-local Home/Mix/Hex/Rebar/XDG/tmp paths, offline mode, an unset `MIX_BUILD_PATH`, and a fresh default `_build/test`. Locked sources, unused-lock validation, dependency compilation, and warnings-as-errors project compilation passed.

All eleven untimed gates then passed: exact relocation; dual-shell portability, containment, and epoch clock; routing adverse cases and preflight; validation synthetics; independent partition verification; the calibration/lifecycle/terminal contract set; both protected offline attestations; Actionlint; and formatting. The candidate stayed tracked-clean.

The exact target command ran once and only once:

`MIX_ENV=test mix test test/sigra/audit/query_index_test.exs`

It completed one named test with zero failures and zero invalids. The module setup created its scratch storage, the composite-index assertion executed successfully, teardown removed the scratch database, and no candidate session remained. Read-only PostgreSQL snapshots immediately before and after both contained zero scratch-database activity rows, locks, and blockers; query text was never captured.

The external `/private/tmp/sigra-plan33-query-index-diagnostic.json` receipt uses schema `sigra.plan33.query-index-diagnostic.v1`. It records exact candidate/environment/argv/timestamps, the 146-byte target log at SHA-256 `cd7adceb32251a476534374f011c88ee252b6ccb547c59c5ba23afd8aa1d687c`, redacted database identity, password presence plus HMAC-SHA-256 identity, and only a non-secret key ID. The execution-local HMAC key was never persisted. Every explicit classification input is true, its plan-provided `jq` replay passes, and receipt SHA-256 is `e9ae3f7d11c355d3c5b1f8139dfb3dd3c7c5992fec682da6380e338814fc615d`. Outcome: `transient`.

## Task 2 — Deterministic Hard Stop

After deleting only the three fixed `/tmp` receipt files, the exact candidate, calibration `8d54ea4d...`, manifest `61ecf707...`, runner pin `deae1229...`, clean tree, target receipt, and PostgreSQL readiness were re-proven. `library-partitions.test.sh` was invoked exactly once from 2026-09-11T03:04:47Z to 2026-09-11T03:07:05Z. Its recovery log is 30,835 bytes with SHA-256 `243fd16e59f23601d53a583d8d31db34eca671a3bf49f994467f742120f04c75`.

Pair 1 completed both exhaustive/disjoint 110/115 partitions green:

| Pair | Partition 1 | Partition 2 | Ratio | Result |
| --- | ---: | ---: | ---: | --- |
| 1 | 40,958ms | 27,019ms | 1.516 | green diagnostic |
| 2 | 40,746ms | not run | — | blocked |

Pair 1 hashes were combined `3e63e93f...`, partition 1 `984da9d2...`, and partition 2 `9e6ae348...`. Because the required fresh series did not reach three green pairs, it is not completion evidence.

Pair 2 partition 1 ran 1,254 tests and produced one failure with zero invalids. `Sigra.AuthTest`'s `authenticate/3 emits login telemetry event on success` expected `metadata.user_id == 5` but received `1`. The failing run used ExUnit seed `884906`. Both `Sigra.AuthTest` and `Sigra.TelemetryTest` are async and observe the same `[:sigra, :auth, :login, :stop]` global event; the observed metadata is the value emitted by the concurrent telemetry test. This is a reproducible ordinary-suite isolation failure, not a PostgreSQL timeout.

The final truthful receipts report partition 1 `failure`, exit status 2, 40,746ms, 110 paths, and partition 2 `not_run`, 115 paths. Their hashes are combined `69280bb3785460dfd89f72b6dfb82aabdfb4ec02d9d44c501def678c843b7796` and partition-1 timing `f463893f78c3a18102c89b9ac578992cdfe653c45905a0191b53282d206e6065`; no partition-2 timing receipt exists.

Plan 33 requires any recovery failure to stop permanently. There was no second recovery invocation, rematerialization, recalibration, reassignment, timeout or threshold change, source edit, alternate candidate, push, dispatch, CI watcher, or GitHub API action.

## Task 3 — Not Run

No capture PR or dispatch run occurred, no PR/scaffold evidence receipt was written, no evidence commit was created, and no disjoint validation pair or validation receipt exists. The milestone audit remains in its pre-evidence state.

## Budget and External-Action Accounting

- targeted `QueryIndexTest` invocations: exactly one;
- `library-partitions.test.sh` recovery invocations: exactly one;
- complete fresh local pairs: one of three; pair 2 stopped after partition 1;
- local retries, candidate rematerializations, recalibrations, source changes, and alternate candidates: zero;
- GitHub pushes, dispatches, watchers, summaries, logs, downloads, and evidence receipts: zero.

## Deviations from Plan

None. The targeted pass authorized exactly one recovery invocation, and the first recovery failure followed the plan's mandatory fail-closed route.

## Deferred Issue

`Sigra.AuthTest` and `Sigra.TelemetryTest` may run concurrently while observing the same global login-stop telemetry event. The auth test's handler can receive the telemetry test's `user_id: 1` event before its own expected `user_id: 5` event. A separate plan must make this global-event assertion identity-safe or serialize the relevant tests before any new evidence budget is authorized.

## Known Stubs

None.

## Threat Review

No endpoint, auth behavior, schema, dependency, ordinary source, workflow, calibration, manifest, assignment, audit, timeout, threshold, or evidence artifact changed. The HMAC key and plaintext password were not persisted; PostgreSQL query text was redacted. Historical partial pairs remained non-admitted, and no live provenance was attempted.

## Self-Check: PASSED

- Candidate `63910813`, tree `bd81cf76`, and sole parent `47f9578` resolve and the isolated worktree is clean.
- The external diagnostic receipt and target log replay to their recorded hashes and the exact plan `jq` contract passes.
- The target and recovery invocation ledgers each contain exactly one line.
- The failed recovery log and final fixed receipts replay to the recorded hashes.
- PR, scaffold, and validation evidence files remain absent, and no GitHub action occurred.
