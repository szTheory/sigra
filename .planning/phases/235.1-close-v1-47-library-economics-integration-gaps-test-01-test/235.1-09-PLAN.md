---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 09
type: execute
wave: 7
depends_on: [235.1-08]
files_modified:
  - scripts/ci/install-golden.sh
  - scripts/ci/install-golden.test.sh
  - scripts/ci/verify-library-install-golden.sh
  - scripts/ci/verify-library-install-golden.test.sh
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The exact six scaffold/install modules are absent from PR `mix ci` and execute on every scheduled or manually dispatched CI run in one named non-PR job."
    - "The non-PR job cannot silently skip on schedule/workflow_dispatch and cannot report success when a receiver, receipt, or diagnostic artifact is absent."
    - "All source and fixture-DAG optimizations from Plans 06/07 remain active in the receiving lane; coverage, assertions, two-worker ceiling, and private checkout isolation are unchanged."
  artifacts:
    - path: scripts/ci/install-golden.sh
      provides: exact six-receiver execution receipt
    - path: scripts/ci/verify-library-install-golden.sh
      provides: independent exact-set and outcome authority
    - path: .github/workflows/ci.yml
      provides: schedule/workflow_dispatch receiving lane
  key_links:
    - from: non-PR workflow job
      to: scripts/ci/install-golden.sh
      via: one fixed command on schedule/workflow_dispatch only
    - from: install receipt
      to: diagnostic receipt
      via: same run/job and fixed paths
---

<objective>
Move the empirically dominant six-module install/scaffold class off pull requests into an automation-verifiable non-PR receiver.

Purpose: TEST-03 explicitly permits a named non-PR lane with recorded justification. The operator's fallback supersedes Phase 234 D-02's installer-in-`mix ci` clause and Phase 235.1 D-05/D-06's PR non-dominance experiment after genuine attempt-1 evidence (28,671ms ordinary vs 79,614ms scaffold and a later 57,389ms hard stop) proved it infeasible without weakening compile coverage.
Output: A hard-signal schedule/dispatch lane, exact receipts, and fail-closed contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-06-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-07-SUMMARY.md
@scripts/ci/install-golden.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry all six receivers through a fail-closed non-PR receipt (D-05/D-06 empirically superseded)</name>
  <files>scripts/ci/install-golden.sh, scripts/ci/install-golden.test.sh, scripts/ci/verify-library-install-golden.sh, scripts/ci/verify-library-install-golden.test.sh</files>
  <behavior>
    - The runner executes exactly the six current scaffold modules once through the optimized prepared-fixture path and writes a receipt even when a child fails.
    - The verifier rejects a missing, skipped, duplicated, reordered, substituted, failed, or unproven receiver and rejects missing/unsafe diagnostics.
  </behavior>
  <action>Evolve the existing fixed runner without reverting Plans 06/07. Atomically write `/tmp/sigra-library-install-golden.json` with schema `sigra.library-install-golden/v1` and exact keys: schema version, receiver paths, start/end/duration, exit status, conclusion, worker ceiling, prepared-fixture flag, and diagnostic path. Keep the receiver list literal, lexical, exact, and single-owned; retain `SIGRA_INSTALL_GOLDEN_PREPARED=1`, private checkout/build isolation, the graph-global maximum of two workers, real warnings-as-errors compile checks, cancellation, cleanup, and all behavioral assertions. Create an independent verifier that recomputes exact receiver equality from live `@moduletag :scaffold` ownership, raw duration arithmetic, success state, diagnostics schema/phases, and the two-worker ceiling. It must never accept a producer pass boolean. Mutation-test one pole at a time.</action>
  <verify>
    <automated>bash scripts/ci/install-golden.test.sh &amp;&amp; bash scripts/ci/verify-library-install-golden.test.sh &amp;&amp; shellcheck scripts/ci/install-golden.sh scripts/ci/install-golden.test.sh scripts/ci/verify-library-install-golden.sh scripts/ci/verify-library-install-golden.test.sh</automated>
  </verify>
  <done>The optimized exact six-module class produces independently checkable raw outcome and diagnostic evidence on both pass and failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add the non-skippable schedule/dispatch receiving job</name>
  <files>.github/workflows/ci.yml, test/sigra/planning/phase_233_library_economics_contract_test.exs, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <action>Add `library_install_golden_non_pr` to the existing `CI` workflow so branch-scoped evidence can be dispatched before merge. Give it name `Library install golden (non-PR)`, a bounded timeout, Postgres service, the repository's pinned checkout/setup/cache/upload actions, locked beam versions, Hex/Rebar, phx_new 1.8.8, and locked deps. Its exact job condition must be `${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}` with no additional input gate, `continue-on-error`, or producer-owned success override; therefore it executes on every one of those two event types and is intentionally skipped on PR/push. Run `MIX_ENV=test bash scripts/ci/install-golden.sh`, validate both fixed receipts under `if: always()`, and upload exactly `library-install-golden-${{ github.run_id }}-${{ github.run_attempt }}` and `library-install-diagnostics-${{ github.run_id }}-${{ github.run_attempt }}` with the immutable v7 action pin, `if-no-files-found: error`, and seven-day retention. Do not add this non-PR job to `ci-gate.needs` or the required `Library tests` aggregate. Update contracts to prove event eligibility in both directions, exact six-path ownership, hard failure semantics, pinned uploads, retained optimized receiver assertions, and explicit empirical supersession of the former PR non-dominance authority.</action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml &amp;&amp; bash scripts/ci/install-golden.test.sh &amp;&amp; bash scripts/ci/verify-library-install-golden.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh</automated>
  </verify>
  <done>Schedule and manual dispatch always execute the exact six modules as a red-on-failure hard signal, while pull requests run none of them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PR to non-PR routing | Coverage leaves the merge-critical event and must have a real receiver. |
| Runner to artifact upload | Missing or partial execution must not look green. |
| Prepared fixture graph to receivers | Reused inputs must not create shared writable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-09-01 | Spoofing | non-PR execution claim | high | mitigate | Exact event condition, fresh dispatch evidence, job state, and artifact digests. |
| T-235.1-09-02 | Tampering | receiver list | high | mitigate | Exact live scaffold-set equality and duplicate/order mutations. |
| T-235.1-09-03 | Repudiation | demotion rationale | high | mitigate | Preserve failed Plan 04 plus later measured hard-stop values in tracked evidence/docs. |
| T-235.1-09-04 | Elevation of Privilege | missing artifacts | high | mitigate | `if: always()` verification and error-on-missing uploads. |
| T-235.1-09-SC | Tampering | Actions/package supply chain | high | mitigate | Existing immutable action SHAs and locked phx_new version; no new package. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Prevent install/scaffold from dominating PR wall clock | 09 | COVERED | Move exact coverage to a named non-PR receiver. |
| REQ | TEST-03 | Share setup or move to non-PR with justification | 09 | COVERED | Uses the requirement's explicit fallback. |
| RESEARCH | — | Exact six-path fail-closed ownership | 09 | COVERED | Preserved with raw receipts. |
| CONTEXT | D-05, D-06 | Keep and compare scaffold on PR | 09 | SUPERSEDED | Repeated genuine evidence established infeasibility; no success is fabricated. |
| CONTEXT | D-09 | Preserve protected terminal evidence | 09 | COVERED | Protected verifiers remain gates. |

<verification>
Run actionlint, runner/verifier adverse suites, focused topology contracts, and protected verifiers.
</verification>

<success_criteria>
- PR `mix ci` contains no six-module scaffold execution.
- Every schedule/workflow_dispatch run has one hard-signal exact-six receiver.
- Receipt and diagnostics are required artifacts and independently verified.
- Plans 06/07 optimizations and behavior coverage remain intact.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-09-SUMMARY.md` when done.
</output>
