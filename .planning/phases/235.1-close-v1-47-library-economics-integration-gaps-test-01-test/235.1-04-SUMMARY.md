---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 04
subsystem: ci-evidence
tags: [github-actions, library-economics, retry-free, blocked]
status: blocked
requires:
  - 235.1-01
  - 235.1-02
  - 235.1-03
provides:
  - durable fixed-bound failure diagnostic for retry-free PR run 34395477605
  - durable protected-contract race diagnostic for retry-free PR run 34425218446
  - durable Linux verifier-cleanup diagnostic for retry-free PR run 34426392394
  - durable post-remediation fixed-bound diagnostic for retry-free PR run 34427315287
  - durable Linux reflink-fallback cleanup diagnostic for retry-free PR run 34430473966
affects:
  - TEST-01
  - TEST-02
  - TEST-03
tech-stack:
  added: []
  patterns: [single-watcher CI observation, raw-artifact predicate recomputation]
key-files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md
  modified:
    - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
    - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
decisions:
  - Do not admit run 34395477605 because its raw durations fail both locked economics predicates.
  - Do not relax the 2.0 comparability or install non-dominance bounds; route receiver optimization to a scoped remediation replan.
metrics:
  completed: 2026-09-09
  tasks: 0/2
actuals:
  tokens: 3080
  tasks: 0
  commits: 3
---

# Phase 235.1 Plan 04: Retry-Free Library Economics Evidence Checkpoint

A real attempt-1 pull-request observation reached the independent economics verifier, but the downloaded raw receipt proved the install/scaffold class was 26.23 times slower than the ordinary class, so closure stopped without creating authoritative evidence.

## Checkpoint Status

- **Plan status:** Blocked during Task 1; Task 2 was not started.
- **Pull request:** [#234](https://github.com/szTheory/sigra/pull/234)
- **Evidence branch:** `ci/phase-235-1-evidence`
- **Passing evidence:** Intentionally absent. `235.1-EVIDENCE.json` was not created.
- **Required remediation:** Optimize the six install/scaffold receivers in a separately scoped plan, then collect a fresh attempt-1 pull-request run. The fixed predicates must remain unchanged.

## Durable Capture Diagnostic

### Linux reflink-fallback cleanup diagnostic — run 34430473966

The scoped Linux tmpfs/sealed-template optimization was pushed at `e30c4ecb`, and the resulting
fresh attempt-1 pull-request run exposed a deterministic cleanup defect before an economics
observation could complete. Five concurrent `InstallFixturePerformanceTest` cases each failed at
`copy_tree!/3` line 1480 while removing a partial `cp --reflink=always` target before physical-copy
fallback:

```text
** (File.Error) could not remove files and directories recursively from
"/tmp/sigra_install_golden.unit-*/installer_builds/default_installed/dev": file already exists
```

The failures were the diagnostics-bound test, private-checkout isolation test, exact-six-variants
test, cleanup-boundary test, and subprocess-helper test. The ordinary class therefore finished
with `2591 tests, 5 failures, 12 skipped (20 excluded)`; install/scaffold correctly did not run,
and the production verifier rejected `install_leg_ran=false`. This is a deterministic source
failure rather than a timing miss and is not retryable.

| Field | Raw value |
| --- | --- |
| Repository / PR | `szTheory/sigra` / `234` |
| Run ID / attempt | `34430473966` / `1` |
| Event / conclusion | `pull_request` / `failure` |
| Head SHA | `e30c4ecb6428497cc8747a6d365f928750051137` |
| Owner job | `Library tests shard` (`102724765621`) |
| Owner interval | `2026-09-10T02:42:19Z`–`2026-09-10T02:44:27Z` |
| Rate preflight | `remaining=5000`, `reset=1789011743` |
| Authoritative economics artifact | `library-economics-34430473966-1`, ID `10134329819`, GitHub digest `sha256:8839f37aa22db5c1c5e953bad1c680189932cf6addba666868607292058edfae` — not downloaded or admitted |
| Authoritative timings artifact | `library-test-timings-34430473966-1`, ID `10134329449`, GitHub digest `sha256:cd2fc87c1d9012504c5fc68fd62969052be7b56832f55d3d5d2ffd9c166752ee` — not downloaded or admitted |
| Explanatory artifact | `library-install-diagnostics-34430473966-1`, ID `10134330195`, GitHub digest `sha256:c4c32798c08c73a7eeb6e85708ff2254b188b7cc4245414fefde38510ced6bef` |
| Explanatory file SHA-256 | `3356052df94adc0e9abfeaf608197d019fc4b2eefecd0e6274db5465a1b8b7eb` |

The explanatory receipt is non-authoritative and remains outside evidence `.artifacts`. It reports
`copy_mode=copy`, empty `failed_paths`, and phases `phx_new=1`, `deps_get=1`,
`baseline_compile=1`, `installer=66`, `checkout_copy=38`, and
`receiver_compile_runtime=1`. The run used exactly one 60-second watcher and one structured
summary with executable field `attempt`; failed logs were fetched only after the failure.

### Post-remediation measured diagnostic — run 34427315287

After both protected FAST-01 contract repairs, this fresh attempt-1 run executed the complete
fixed producer successfully. The ordinary and install/scaffold classes were both green, but the
downloaded raw durations independently fail both locked economics predicates. This is a genuine
fixed-bound miss, not a transient failure, so it is not retryable and no field from this run may
populate `235.1-EVIDENCE.json`.

| Field | Raw value |
| --- | --- |
| Repository | `szTheory/sigra` |
| PR | `234` |
| Run ID | `34427315287` |
| Run URL | `https://github.com/szTheory/sigra/actions/runs/34427315287` |
| Event | `pull_request` |
| Attempt | `1` |
| Head SHA | `ad62ec88a3393224fbe6de653e82eb3e9e42ef2a` |
| Conclusion | `failure` |
| Owner job | `Library tests shard` (`102715213595`) |
| Owner result | `completed` / `failure` |
| Contributor CI gate | `success` |
| Per-test timing validation | `success` |
| Economics validation | `failure` |

The economics artifact contains these raw values:

```json
{
  "ordinary": {"duration_ms": 17161, "conclusion": "success", "exit_status": 0},
  "install_scaffold": {"duration_ms": 93033, "conclusion": "success", "exit_status": 0}
}
```

Independent arithmetic from those integers gives:

- Comparability: `93033 / 17161 = 5.4211876`, failing locked `max/min <= 2.0`.
- Install non-dominance: `93033 <= 17161` is false.
- Production verifier: `verify-library-economics: FAIL: comparability predicate failed: max/min exceeds 2.0`.

The timing artifact independently reports `2645` total tests, `2613` passed, `0` failed, `12`
skipped, and `20` excluded.

Neither downloaded artifact embeds the diagnostic fixture's per-phase breakdown or aggregate
copy mode: the economics receipt contains only its schema/path/install-leg/class fields, and the
timing receipt contains only partition/test-count/test-record fields. No phase values or copy mode
are inferred, and no second log fetch was made.

| Artifact | Artifact ID | File | SHA-256 |
| --- | ---: | --- | --- |
| `library-economics-34427315287-1` | `10133225658` | `sigra-library-economics.json` | `1e2ea4d31c7293e3a63415068e0aade11b4e6136dabffc80edfda944c7ffc902` |
| `library-test-timings-34427315287-1` | `10133225181` | `sigra-library-1-timings.json` | `52619d64edc2fb0d736203ad0382d56eaa6f23fc7c4e850ad6432e35251091a8` |

This run used one watcher after a rate-limit preflight of
`{"remaining":5000,"reset":1789008863}`, exactly one executable structured-summary command
using `attempt`, and one failed-log fetch after the terminal failure.

### Serialized follow-up diagnostic — run 34426392394

The Phase 235 contract was serialized before this fresh attempt-1 run, but its two authenticated
default-path checks still invoked the protected verifier as the non-root parent of a
passwordless-sudo network namespace. On GitHub-hosted Linux, `gh` created
`$work/home/.local/state/gh/device-id` as root; the verifier printed its PASS banner and then its
non-root EXIT trap failed to remove that file. Both tests therefore correctly returned failure.
This is a deterministic Linux cleanup defect in the prerequisite contract, not a timing
observation, so install/scaffold did not run and no evidence is admissible.

| Field | Raw value |
| --- | --- |
| Repository | `szTheory/sigra` |
| PR | `234` |
| Run ID | `34426392394` |
| Run URL | `https://github.com/szTheory/sigra/actions/runs/34426392394` |
| Event | `pull_request` |
| Attempt | `1` |
| Head SHA | `576d61aac303b38f9309459ce6846174958415f9` |
| Conclusion | `failure` |
| Owner job | `Library tests shard` (`102712459565`) |
| Owner result | `completed` / `failure` |
| Ordinary class | `23927 ms`, `failure`, exit status `2` |
| Install/scaffold class | `not_run`, `0 ms`, exit status `0` |

The two failed tests remained:

- `semantic fixture parsing stays isolated from the authenticated default path`
- `authenticated strict pass is reconciled exactly into FAST-01 and its residual`

Both failures carried the same exact post-success cleanup diagnostic:

```text
source_complete_offline_attestation_verified
rm: cannot remove '<private-work>/home/.local/state/gh/device-id': Permission denied
```

The existing Phase 235.1 contract already proves the scoped repair pattern: on Linux, execute the
whole authenticated verifier with `sudo -n bash` so its own EXIT cleanup retains ownership;
semantic-fixture and malformed-argument paths remain unprivileged. That same-file follow-up must
land before another fresh run.

| Artifact | Artifact ID | File | SHA-256 |
| --- | ---: | --- | --- |
| `library-economics-34426392394-1` | `10132867081` | `sigra-library-economics.json` | `f5aaaa4ab283af9d2ac01673adc9e39683f98069204a756a85e65fdf662511fa` |
| `library-test-timings-34426392394-1` | `10132866815` | `sigra-library-1-timings.json` | `24482f20c3380f4e288ba3e8bdd1da2abf345e0a9846c1bd1c5daeab7b5093f5` |

This run used one watcher after a rate-limit preflight of
`{"remaining":5000,"reset":1789008021}`, one structured-summary fetch, and one failed-log fetch.
The summary used the executable `attempt` field; because this was a rejected diagnostic, its
additional local `--jq` projection is not admitted to the future evidence command record.

### Post-remediation prerequisite diagnostic — run 34425218446

The fresh post-Plan-06/07 attempt-1 run did not produce an economics observation. Its ordinary
class stopped after two protected FAST-01 contract tests failed concurrently, so the fixed
producer correctly set `install_leg_ran=false` and never ran the install/scaffold class. This is
a deterministic prerequisite race, not a timing-predicate miss or retryable observation, and no
field from this run may populate `235.1-EVIDENCE.json`.

| Field | Raw value |
| --- | --- |
| Repository | `szTheory/sigra` |
| PR | `234` |
| Run ID | `34425218446` |
| Run URL | `https://github.com/szTheory/sigra/actions/runs/34425218446` |
| Event | `pull_request` |
| Attempt | `1` |
| Head SHA | `3003d4a26a663c7aade5983523dcb5642b64f0c9` |
| Conclusion | `failure` |
| Owner job | `Library tests shard` (`102708921720`) |
| Owner result | `completed` / `failure` |
| Ordinary class | `21046 ms`, `failure`, exit status `2` |
| Install/scaffold class | `not_run`, `0 ms`, exit status `0` |

The exact two ordinary failures, independently identified in the downloaded per-test timing
receipt, were:

- `semantic fixture parsing stays isolated from the authenticated default path`
- `authenticated strict pass is reconciled exactly into FAST-01 and its residual`

Both live in `test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs`, whose
`async: true` tests concurrently execute the same protected offline verifier. That Phase 235
contract is outside Plan 04's fixed evidence ownership and must be repaired before another fresh
attempt-1 observation can be admitted.

| Artifact | Artifact ID | File | SHA-256 |
| --- | ---: | --- | --- |
| `library-economics-34425218446-1` | `10132424376` | `sigra-library-economics.json` | `07a7919bcf352895cadf213c010375ad4d81c10e8acfcec8df7e4d6564ce1599` |
| `library-test-timings-34425218446-1` | `10132424071` | `sigra-library-1-timings.json` | `b24f19b8550825c4920f47ad270d48e96e94a66b0b64568ffd7ffca5fb6dc3ca` |

This run used one watcher after a rate-limit preflight of
`{"remaining":5000,"reset":1789006969}`, one structured-summary fetch, and one failed-log fetch.
The installed GitHub CLI again required the executable field name `attempt`; it does not support
`runAttempt`. The later evidence contract must pin the actually executed command and translate
`attempt` to tracked `run_attempt` without weakening attempt-1 or retry-free admission.

### Selected retry-free observation

| Field | Raw value |
| --- | --- |
| Repository | `szTheory/sigra` |
| PR | `234` |
| Run ID | `34395477605` |
| Run URL | `https://github.com/szTheory/sigra/actions/runs/34395477605` |
| Event | `pull_request` |
| Attempt | `1` |
| Head SHA | `3abb96d597c92d0b8ddc2cc4c062bfa1fb188b61` |
| Conclusion | `failure` |
| Owner job | `Library tests shard` (`102614055824`) |
| Owner result | `completed` / `failure` |
| Failing step | `Validate library economics receipt` |

The owner executed both classes successfully. The verifier then emitted `comparability predicate failed: max/min exceeds 2.0`; the downstream `Library tests` aggregate and `ci-gate` failed closed as designed.

### Downloaded raw artifacts

| Artifact | Artifact ID | File | SHA-256 |
| --- | ---: | --- | --- |
| `library-economics-34395477605-1` | `10121760830` | `sigra-library-economics.json` | `89d1258cbbe7081505ee2bb7b9f5a9f29e07b2ac191f243956b2e998206c3c86` |
| `library-test-timings-34395477605-1` | `10121760326` | `sigra-library-1-timings.json` | `a5fcc7f39d50174eae13dcc55005d99a4d8e899ebe27c45ad6ceca15127dc221` |

The timing artifact records `2613` total tests, `2579` passed, `0` failed, `12` skipped, and `22` excluded.

The economics artifact contains these raw values:

```json
{
  "schema_version": "sigra.library-economics/v1",
  "timing_receipt_path": "/tmp/sigra-library-1-timings.json",
  "install_leg_ran": true,
  "classes": {
    "ordinary": {
      "start_ms": 265882,
      "end_ms": 286932,
      "duration_ms": 21050,
      "conclusion": "success",
      "exit_status": 0
    },
    "install_scaffold": {
      "start_ms": 286950,
      "end_ms": 839171,
      "duration_ms": 552221,
      "conclusion": "success",
      "exit_status": 0
    }
  }
}
```

Independent arithmetic from those raw integers gives:

- Comparability: `552221 / 21050 = 26.2337767`, which fails the locked `max/min <= 2.0` predicate.
- Install non-dominance: `552221 <= 21050` is false.

### Exact GitHub commands and results

```text
gh api rate_limit --jq '.resources.core | {remaining,reset}'
=> {"remaining":5000,"reset":1788985848}

gh run watch 34395477605 --repo szTheory/sigra --compact --interval 60 --exit-status
=> one watcher; exit 1 after the owner economics validation failed

gh run view 34395477605 --repo szTheory/sigra --json databaseId,event,headSha,conclusion,attempt,url,jobs
=> one structured summary; run attempt 1, pull_request, exact head SHA above, conclusion failure

gh run view 34395477605 --repo szTheory/sigra --log-failed
=> one failed-log fetch; exact primary diagnostic: verify-library-economics: FAIL: comparability predicate failed: max/min exceeds 2.0

gh api repos/szTheory/sigra/actions/runs/34395477605/artifacts --paginate --jq '.artifacts[] | {id,name,size_in_bytes,expired,archive_download_url}'
=> artifact IDs 10121760830 and 10121760326, both unexpired

gh run download 34395477605 --repo szTheory/sigra --name library-economics-34395477605-1 --dir <validated-mktemp-dir>/library-economics-34395477605-1
gh run download 34395477605 --repo szTheory/sigra --name library-test-timings-34395477605-1 --dir <validated-mktemp-dir>/library-test-timings-34395477605-1
=> both exact run-qualified artifacts downloaded and SHA-256 hashed locally
```

The installed GitHub CLI exposes the structured-summary field as `attempt`, not the plan's `runAttempt`; an earlier exact `runAttempt` invocation failed locally before producing a summary. No second watcher was started.

## Earlier Deterministic Diagnostics and Repairs

| Run | Head | Result | Diagnostic / disposition |
| ---: | --- | --- | --- |
| `34392048404` | `53474b8f2064724af45962362c3c6b370b329ae8` | failure | Fresh PR branch lacked retained source-complete prerequisite evidence; repaired by applying the existing prerequisite commits. |
| `34393085749` | `ed57aabc2708c0f5d8cecbdb6e2a00877912b56a` | failure | Protected offline verifier cleanup encountered root-owned GitHub CLI state, and retained FAST-01 history was stale; repaired deterministically. |
| `34394247564` | `a5ecc9519791b40bceeca25f814b68fa2179e8fd` | failure | Downloaded timing receipt identified three stale contract assertions; repaired without changing production thresholds. |
| `34395477605` | `3abb96d597c92d0b8ddc2cc4c062bfa1fb188b61` | failure | All contract repairs held; genuine raw economics miss blocks evidence admission. |
| `34425218446` | `3003d4a26a663c7aade5983523dcb5642b64f0c9` | failure | Ordinary class stopped on two concurrent protected FAST-01 contract failures; install/scaffold correctly did not run. |
| `34426392394` | `576d61aac303b38f9309459ce6846174958415f9` | failure | Serialization held, but both authenticated verifier calls failed after PASS because sudo-unshare left root-owned GitHub CLI state for the non-root EXIT trap. |
| `34427315287` | `ad62ec88a3393224fbe6de653e82eb3e9e42ef2a` | failure | Both classes and all tests executed successfully, but raw 17161ms/93033ms durations fail comparability and install non-dominance. |
| `34430473966` | `e30c4ecb6428497cc8747a6d365f928750051137` | failure | Linux physical-copy fallback could not remove partial read-only reflink targets; five ordinary-suite fixture contracts failed before install/scaffold ran. |

Each run used at most one watcher, a rate-limit preflight above 250, one post-completion structured summary, and failed logs only after failure.

## Completed Repair Commits

- `6a78916dc4891d260a2e8ddfe5afdfa91109acbf` — `fix(235.1-04): preserve offline verifier cleanup in CI`
- `2e52c611753ee1fe4bb8ec45788365479c3df7ea` — `fix(235.1-04): reconcile contributor gate contract`

The corresponding pushed evidence-branch commits are `a5ecc9519791b40bceeca25f814b68fa2179e8fd` and `3abb96d597c92d0b8ddc2cc4c062bfa1fb188b61`.

## Verification Performed

```text
ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test \
  test/sigra/planning/phase_198_contributor_dx_contract_test.exs \
  test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs \
  test/sigra/planning/phase_235_1_library_economics_contract_test.exs
=> 20 tests, 0 failures
```

Task 1's full verification cannot pass because the raw economics receipt correctly fails. Task 2 and its validation receipt/verifier were not started.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved protected-verifier cleanup under GitHub-hosted Linux**

- **Found during:** Task 1, run `34393085749`
- **Issue:** The passwordless-sudo fallback created root-owned GitHub CLI state that the verifier's EXIT trap could not remove, turning a successful offline verification into exit status 1.
- **Fix:** Invoke the complete protected verifier under the same privilege on Linux so its own cleanup remains effective; retain the Darwin path unchanged.
- **File:** `test/sigra/planning/phase_235_1_library_economics_contract_test.exs`
- **Commit:** `6a78916d`

**2. [Rule 3 - Blocking] Reconciled stale Phase 198 contributor-gate topology**

- **Found during:** Task 1, run `34394247564`
- **Issue:** The old contract still required separate ordinary and scaffold alias legs after Phase 235.1 established one timed owner.
- **Fix:** Require the ordered six-leg alias with exactly one `library-economics.sh` owner and reject the superseded split legs.
- **File:** `test/sigra/planning/phase_198_contributor_dx_contract_test.exs`
- **Commit:** `2e52c611`

## Blocker

Run `34430473966` exposed a deterministic Linux fallback-cleanup bug in the new sealed-template
optimization: a failed `cp --reflink=always` leaves a partial read-only target that `File.rm_rf!/1`
cannot remove before physical-copy fallback. That fixture source is owned by Plan 06, outside
Plan 04's evidence files, and must be repaired and locally verified before another fresh attempt-1
observation may be collected. Until a fresh observation executes both classes and satisfies both
locked predicates, TEST-01, TEST-02, and TEST-03 remain open and no validation run can be
authoritative.

## Self-Check: PASSED

- The checkpoint summary exists and contains the exact selected run, PR, SHA, job, artifact IDs/names/files/digests, raw receipt values, commands, and fixed-bound arithmetic.
- Both completed repair commits exist in local history.
- Run `34425218446` is recorded as diagnostic-only with its exact failed class, owner, artifact
  IDs, digests, two failing tests, rate preflight, and single-fetch command behavior.
- Run `34426392394` is recorded as diagnostic-only with the exact Linux cleanup failure, raw
  non-observation, artifact IDs/digests, and rate-safe command history.
- Run `34427315287` is recorded as diagnostic-only with both successful class durations, exact
  failed predicates, owner job, test totals, artifact IDs/digests, rate preflight, and
  single-watcher/single-summary/single-failed-log behavior.
- Run `34430473966` is recorded as diagnostic-only with its exact five Linux cleanup failures,
  owner job interval, explanatory diagnostic receipt/digests, non-admitted authoritative artifact
  IDs/digests, and rate-safe single-watcher/single-summary command behavior.
- `235.1-EVIDENCE.json` and `235.1-VALIDATION-RUN.json` remain absent, as required for a failed observation.
