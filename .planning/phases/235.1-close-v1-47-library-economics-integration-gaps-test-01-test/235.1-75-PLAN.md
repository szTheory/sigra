---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 75
type: execute
gap_closure: true
wave: 76
depends_on: [235.1-12]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREALLOCATION-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-HOME-PROBE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREREQUISITE-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Plans 69, 71, 72, 73, and 74 remain immutable failed history; no predecessor root, partial success, process, or consumed budget can authorize reuse, retry, or TEST completion."
    - "Before allocation, both pinned Python runtimes prove one complete closed Plan 75 runtime environment for the timing wrapper, canonical exact-six wrapper, and Mix home probe, including literal HOME=/Users/jon and no inherited or extra key."
    - "The exact Plan 75 runtime environment has eleven C-sorted keys with fixed PATH, MIX_ENV, ASDF runtime, locale, PostgreSQL values, and one nonempty redacted password; Plan 74's separate seven-key stdin prerequisite environment remains byte/field exact and is not widened."
    - "One pre-root Mix process durably proves System.user_home! resolves to /Users/jon while Hex dependency loading, local cache state, pinned archives, cwd, argv, environment, stdout, stderr, and status remain authenticated."
    - "Plan 74's passing stdin prerequisite, Plan 73's canonical exact-six policy, Plan 72's path-keyed health-SQL authority, and Plan 71/72's parsed SQL policy carry forward without becoming predecessor completion evidence."
    - "Only after both pre-root receipts pass may one distinct Plan 75 root run one local chain, three timing pairs, one canonical exact-six action, one capture pair, and one distinct validation pair under attempt-1/no-retry budgets."
    - "TEST-01/02/03 remain incomplete until Plan 75 evidence passes; the two sequential ordinary partitions, exact six, FAST-01 source-complete n=52/p50=469, and all 93 GATE-05 rows do not weaken or change."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREALLOCATION-VALIDATION.json"
      provides: "Dual-runtime zero-effect proof of the complete closed runtime environment and inherited execution policies"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-HOME-PROBE.json"
      provides: "Durable unparsed capture of the sole Mix/Hex home-resolution process"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREREQUISITE-VALIDATION.json"
      provides: "Exact pre-allocation readiness decision for HOME, Hex/cache/archive state, timing argv, and canonical wrapper"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh attempt-1 automatic-PR two-partition evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh attempt-1 workflow_dispatch canonical exact-six evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct attempt-1 validation pair bound to Plan 75 local and capture authority"
  key_links:
    - from: "Plan75ClosedRuntimeEnvironment"
      to: "scripts/ci/library-partitions.sh and scripts/ci/install-golden.sh"
      via: "the same newly constructed eleven-key env object passed with shell=false, exact cwd, and complete stream persistence"
    - from: "HOME=/Users/jon"
      to: "Mix/Hex and pinned local archives/cache"
      via: "one exact pre-root mix run --no-compile --no-start home-resolution probe plus no-follow before/after manifests"
    - from: "Plan 74 stdin prerequisite authority"
      to: "Plan 75 generated package"
      via: "byte/field-equal seven-key probe contract and raw five-line capture, without rerunning psql"
    - from: "Plan 73/72/71 entrypoint, source, and SQL policies"
      to: "Plan 75 local chain"
      via: "exact accepted/adverse identity sets and Plan 75-only root/schema/delimiter substitutions"
    - from: "both Plan 75 pre-root PASS receipts"
      to: "the sole Plan 75 allocator"
      via: "independent reopen, exact one-probe accounting, unchanged local-state manifests, and empty Plan 75 root inventory"
    - from: "Plan 75 capture and validation receipts"
      to: "Plan 70 reconciliation"
      via: "unchanged offline verifiers; Plans 68/69/71/72/73/74 supply history only"
  prohibitions:
    - requirement_id: "TEST-01"
      status: "unverified"
      flagged: true
      statement: "Do not relabel predecessor timing, a serial pass, a rerun, or timing without same-run formatter evidence as TEST-01 completion."
    - requirement_id: "TEST-02"
      status: "unverified"
      flagged: true
      statement: "Do not relax raw max/min <=2.0, tune after observation, reorder partitions, or admit an incomplete pair."
    - requirement_id: "TEST-03"
      status: "unverified"
      flagged: true
      statement: "Do not use raw mix test, caller-supplied prepared mode, skipped/duplicated/substituted receivers, or predecessor output as exact-six evidence."
---

<objective>
Repair Plan 74's under-specified closed runtime environment before allocation, then produce one fresh authenticated local and live library-economics evidence chain.

Purpose: Make HOME, Mix/Hex resolution, pinned tools, local cache/archive state, and both canonical wrapper environments explicit and fail-closed before a new one-shot root can exist.
Output: Dual-runtime zero-effect runtime-contract receipt, one raw home-resolution capture and decision, one distinct terminal Plan 75 root, and fresh canonical PR/scaffold/validation evidence for Plan 70.

Decision coverage: D-01 through D-09 are implemented across Tasks 1–3. Plans 69/71/72/73/74 remain immutable failed evidence only; Plan 75 is the sole possible new TEST completion authority.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-POSTGRESQL-PROBE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREREQUISITE-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-LOCAL-FAILURE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-SUMMARY.md
@/private/tmp/sigra-p2351-plan74-stdin-probe._55jsnhr/local/timing/pair-1/process.json
@scripts/ci/library-partitions.sh
@scripts/ci/install-golden.sh
@scripts/ci/install-golden.test.sh
@scripts/ci/verify-library-install-golden.sh
@scripts/ci/verify-library-install-golden.test.sh
@test/test_helper.exs
@test/support/install_fixture.ex
@.github/workflows/ci.yml
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

## Artifacts this phase produces

- New tracked zero-effect receipt: `235.1-75-PREALLOCATION-VALIDATION.json`.
- New tracked raw home probe: `235.1-75-HOME-PROBE.json`.
- New tracked pre-root decision: `235.1-75-PREREQUISITE-VALIDATION.json`.
- Replaced tracked evidence receipts only after fresh passing capture/validation: `235.1-PR-EVIDENCE.json`, `235.1-SCAFFOLD-EVIDENCE.json`, and `235.1-VALIDATION-RUN.json`.
- New private evidence root: exactly one `/private/tmp/sigra-p2351-plan75-runtime-home.XXXXXXXX`.

## Interface contracts

### Plan75ClosedRuntimeEnvironment

- The launcher constructs a fresh environment object with exactly the C-sorted keys `ASDF_ELIXIR_VERSION,ASDF_ERLANG_VERSION,HOME,LC_ALL,MIX_ENV,PATH,PGDATABASE,PGHOST,PGPASSWORD,PGPORT,PGUSER`.
- Fixed values are exactly `ASDF_ELIXIR_VERSION=1.19.5-otp-28`, `ASDF_ERLANG_VERSION=28.4.1`, `HOME=/Users/jon`, `LC_ALL=C`, `MIX_ENV=test`, `PATH=/Users/jon/.asdf/shims:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin`, `PGDATABASE=sigra_test`, `PGHOST=localhost`, `PGPORT=5432`, and `PGUSER=postgres`. `PGPASSWORD` is the only nonliteral input, must be nonempty, and is persisted only as `<redacted-present>` plus SHA-256.
- `/Users/jon` must be the absolute non-symlink directory that resolves to itself. `HEX_HOME`, `MIX_HOME`, `MIX_ARCHIVES`, `TMPDIR`, process-environment copies, fallback reads, merges, updates, overrides, and any twelfth key are forbidden. Canonical scripts use fixed `/tmp`; Hex resolves beneath `/Users/jon/.hex`; the exact selected Hex 2.5.1 and phx_new 1.8.8 archives resolve beneath the pinned ASDF Elixir installation.
- The same environment object is passed to exact timing argv `[bash,scripts/ci/library-partitions.sh]`, exact-six argv `[bash,scripts/ci/install-golden.sh]`, and the pre-root Mix home probe. The literal PATH independently resolves `bash` to `/opt/homebrew/bin/bash`; each process uses `shell=False`, cwd `/Users/jon/projects/sigra`, both output pipes, and persistence of raw streams/status before assertions.
- Exact PATH resolution is authenticated without executing the evidence commands: `bash=/opt/homebrew/bin/bash`, `mix=/Users/jon/.asdf/shims/mix`, `elixir=/Users/jon/.asdf/shims/elixir`, `erl=/Users/jon/.asdf/shims/erl`, `python3=/opt/homebrew/bin/python3`, `jq=/usr/bin/jq`, `psql=/opt/homebrew/bin/psql`, `env=/usr/bin/env`, `awk=/usr/bin/awk`, `sort=/usr/bin/sort`, `shasum=/usr/bin/shasum`, `mktemp=/usr/bin/mktemp`, `cat=/bin/cat`, `chmod=/bin/chmod`, `mv=/bin/mv`, `rm=/bin/rm`, `sh=/bin/sh`, `stat=/usr/bin/stat`, `test=/bin/test`, `df=/bin/df`, `pwd=/bin/pwd`, `find=/usr/bin/find`, `cp=/bin/cp`, and `git=/opt/homebrew/bin/git`; `sha256sum` is absent so the authenticated `shasum` fallback is authoritative; every symlink is resolved and authenticated to a regular executable target.

### Plan75HomeResolutionProbe

- The sole pre-root argv is exactly `mix run --no-compile --no-start -e 'IO.write(System.user_home!() &lt;> "\n")'` under `Plan75ClosedRuntimeEnvironment`; stdin is DEVNULL, both output streams are pipes, shell is false, and cwd is the repository root.
- Expected stdout is exactly 11 bytes `/Users/jon\n`, base64 `L1VzZXJzL2pvbgo=`, SHA-256 `9d75bf0190e2e157453723af13843a699ec75794f6f175ca48a8e7e024980297`; expected stderr is empty and status is zero.
- The raw process receipt is written and reopened before output/status interpretation. The decision also binds unchanged before/after identities for `/Users/jon`, `.hex/hex.config` without exposing its bytes, `.hex/cache.ets`, Hex 2.5.1, phx_new 1.8.8, pinned ASDF runtimes, `mix.lock`, `deps`, `_build/test`, both wrappers, their required tools, and every failed predecessor root.

### InheritedExecutionAuthority

- Plan 74's exact seven-key stdin prerequisite contract, 126-byte SQL, 64-byte output, raw CAPTURED process, exact five-line PASS, and 37-hostile matrix remain byte/field equal and are not rerun.
- Plan 73's canonical exact-six entrypoint remains the sole receiver authority: exact ordered unique six, unconditional `SIGRA_INSTALL_GOLDEN_PREPARED=1`, two-worker ceiling, diagnostic/receipt ownership, both-stream persistence, phx_new 1.8.8, and exact 15 hostile cases.
- Plan 72's path-keyed health-SQL source authority and Plan 71/72's one accepted/23-hostile parsed SQL allowlist remain exact. Plan 75 changes only plan/root/schema/delimiter identities and the complete runtime/home contract.

<assumption_delta_decision>
Primary noun: closed local runtime environment.
Decision: corrected.
Rationale: Plan 74 passed its separate seven-key PostgreSQL prerequisite, then its first timing process inherited a ten-key launcher map that omitted HOME. Hex failed before a partition artifact existed. Canonical wrapper inspection proves a fresh eleven-key literal projection is sufficient: HOME supplies Mix/Hex resolution while fixed `/tmp` paths and the pinned ASDF archive location require no ambient key.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the complete Plan 75 runtime environment with zero downstream effects (D-01–D-07, D-09)</name>
  <reversibility rating="reversible">This task runs only pure in-memory fixtures under two pinned interpreters and writes one canonical receipt; it cannot allocate a root or invoke Mix, Hex, timing, exact-six, database, or network actions.</reversibility>
  <precondition>Plan 74 plan is 51,247 bytes/SHA-256 `c557e722d29f2e9aecea292edf79a06a3edc7f28cb59efb77192d8bfdf86db93`; its 28,253-byte PASS preallocation receipt hashes to `81ed3aa1770a478ed2805a06095ec3dcba469fd6767ec5befe56eb27e7875eb8`; its 2,937-byte CAPTURED PostgreSQL receipt hashes to `773f806cdbab51e0f0525981389dc9fc70b4116174407a5bbce0998e70e89e26`; its 4,984-byte PASS prerequisite receipt hashes to `e0589e73b6a3854cac80ff302355315af4915e6b220f886a7c541305b60faab8`; its 386-byte BLOCKED local failure hashes to `1a34024af0b903e5d978abb7dfc9384c0d1c7d0a5058bb9edf84b4f5eb8b0283`; its 13,266-byte halted summary hashes to `0dda018fdcb4986c4099a6d5fb2f94dc27504653337316d2367bc68839330de7`; frozen root `/private/tmp/sigra-p2351-plan74-stdin-probe._55jsnhr` retains the recorded identity and 44-descendant inventory; and no Plan 75 receipt or root exists.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREALLOCATION-VALIDATION.json</files>
  <read_first>
    - `235.1-74-SUMMARY.md`, `235.1-74-LOCAL-FAILURE.json`, and frozen root `local/timing/pair-1/process.json` — preserve the one timing call, status 1, zero stdout, 1,765-byte Hex stderr, absent timing artifact, and zero exact-six/network/GitHub continuation.
    - Plan 74 plan and all three pre-root receipts — carry the successful exact stdin/raw transcript contract and identify the distinct seven-key prerequisite environment that must not be widened.
    - Frozen root `local/environment-matrix.json` and Plan 74 generated sources — identify the ten-key runtime map, source construction seam, and all current root/source identities without execution.
    - `scripts/ci/library-partitions.sh`, `scripts/ci/install-golden.sh`, their tests/verifiers, `test/test_helper.exs`, and `test/support/install_fixture.ex` — derive exact wrapper argv, PATH tool set, fixed temporary paths, prepared-graph entrypoint, archive/cache consumers, and persistence ordering.
    - Plan 73 prerequisite receipt, Plan 72 source receipt, and Plan 71 SQL receipt — retain pinned ASDF/archive/dependency, exact-six, path-keyed source, and parsed-SQL authorities without rerunning their processes.
    - `/Users/jon`, its `.hex` config/cache, pinned ASDF installations/archives, repository `mix.lock`, `deps`, `_build/test`, resolved PATH tools, and Plans 69/71/72/74 roots — authenticate by no-follow identity/manifests only; never expose config or secret bytes.
  </read_first>
  <behavior>
    - "RED: Plan 74's exact frozen timing failure is classified home_missing_before_hex and cannot authorize a retry, source edit, or descendant reuse."
    - "GREEN: only authenticated_closed_runtime_bundle accepts the exact eleven-key redacted environment, literal HOME directory identity, two wrapper argv values, home-probe argv, repository cwd, shell false, complete capture/persist order, tool resolution table, and local cache/archive manifests."
    - "Exact 47 rejected names: home_absent, home_empty, home_wrong, home_symlink, home_relative, home_inherited, home_overridden, hex_home_resolution_mismatch, mix_home_resolution_mismatch, path_missing_asdf_shims, path_missing_homebrew_bin, path_missing_usr_local_bin, path_missing_usr_bin, path_missing_bin, path_reordered, asdf_erlang_missing, asdf_erlang_wrong, asdf_elixir_missing, asdf_elixir_wrong, mix_env_missing, mix_env_wrong, lc_all_missing, lc_all_wrong, pg_environment_missing, pg_environment_wrong, password_absent, password_empty, extra_environment_key, inherited_environment_key, environment_copy_projection, timing_argv_changed, exact_six_argv_changed, repository_cwd_changed, stdin_env_home_added, stdin_env_runtime_key_added, stdin_env_pg_value_changed, stdin_env_key_missing, stdin_env_copy_projection, hex_cache_missing, hex_config_symlink, hex_config_stale_hash, hex_archive_missing, phx_new_archive_missing, tool_resolution_mismatch, stdout_capture_missing, stderr_capture_missing, status_capture_missing."
    - "Every rejected row is a unique one-field deep-copy mutation with accepted=false and allocator_calls=timing_calls=exact_six_calls=network_calls=github_calls=0; exact name-set equality and uniqueness prevent omission, duplication, or substitution."
    - "Python 3.9.6 and 3.14.4 produce identical environment, home, argv, tool, local-state, inherited stdin/entrypoint/source/SQL, and generated-package semantics with every forbidden effect count zero."
  </behavior>
  <action>Authenticate the exact Plan 74 artifacts and frozen root pinned in the precondition with lstat, no-follow opens, fstat identity equality, duplicate-key rejection, canonical JSON, byte counts, hashes, and an exact before manifest. Verify its timing process remains one immutable status-1 call with argv `bash scripts/ci/library-partitions.sh`, empty stdout, 1,765-byte stderr/SHA-256 `e30eb1e9ee6bd4c579e18101ca6b21ded935bfaa0f5cf609216d0ecc1eb7b7ec`, no combined timing receipt, and no exact-six or remote descendant. Plans 69/71/72/73/74 are history only and may not be executed, copied, touched, relabeled, or used as fresh evidence.

Define `Plan75ClosedRuntimeEnvironment` from eleven explicit assignments only. Ten values are the literals in the interface; the password slot accepts only a nonempty opaque binding and emits only `<redacted-present>` plus its digest. Require exact C-sorted key equality and source AST dominance proving no process-environment read/copy, dictionary merge/update, fallback, late override, deletion, additional assignment, `setdefault`, or child-specific environment divergence. Explicitly require `HOME=/Users/jon` to be absolute, non-symlink, self-resolving, owned by uid 501, and a directory; no alternate home or home-derived override is accepted.

Structurally parse the canonical timing/exact-six scripts, test helper, fixture graph, Plan 73 prerequisite receipt, and PATH. Require the two exact wrapper argv values and the exact tool-resolution table in the interface, fixed `/tmp` receipt paths, prepared export owned by the wrapper, exact ordered unique six, worker ceiling two, phx_new 1.8.8, ASDF Erlang 28.4.1/Elixir 1.19.5-otp-28, repository locked deps/build state, and no need for `TMPDIR`, `HEX_HOME`, `MIX_HOME`, or `MIX_ARCHIVES`. Create no-follow reduced manifests for `/Users/jon/.hex/hex.config` without raw content, `.hex/cache.ets`, required repository-lock package cache rows, Hex 2.5.1, phx_new 1.8.8, ASDF runtime roots, `mix.lock`, `deps`, and `_build/test`; bind exact paths/types/owners/bytes/hashes and reject symlinks or stale hashes. Resolve tools by scanning only the five literal PATH components; do not invoke them.

Keep Plan 74's `probe_contract` and raw PostgreSQL/prerequisite receipts byte/field equal, including its separate exact seven-key redacted map. Keep Plan 73 `entrypoint_policy`, Plan 72 `source_authority`, and Plan 71/72 `sql_policy` byte/field equal. Generate the Plan 75 source package in memory with only Plan 75 schema/root/delimiter/runtime-home substitutions, preserving source names, parser anchors, policy spans, definition order, dominance, and recomputed source-table hashes.

Write RED/GREEN fixtures entirely in memory. Accept only `authenticated_closed_runtime_bundle`; apply exactly the 47 named mutations from `behavior` to independent deep copies, require exact set/count/uniqueness, one changed field, expected failure class, accepted=false, and all five named side-effect counters zero per row. Run the pure gate under exact pinned Python 3.9.6 and 3.14.4 with `PYTHONDONTWRITEBYTECODE=1`; require identical semantics. It may write only canonical `235.1-75-PREALLOCATION-VALIDATION.json` after both pass, then no-follow reopen and independently verify its receipt digest, omitted-field digest, unchanged predecessor/local-state manifests, empty Plan 75 root inventory, and zero Mix/Hex/home-probe/psql/database/allocator/mktemp/root/source-materialization/timing/exact-six/network/GitHub counts.</action>
  <acceptance_criteria>Both runtimes prove the complete exact eleven-key closed runtime map and all 47 unique hostile cases; HOME, PATH tools, Mix/Hex cache/archive state, two wrapper commands, capture order, and inherited Plan 74 stdin/Plan 73 entrypoint/Plan 72 source/Plan 71 SQL policies are exact; every rejected row and global forbidden counter is zero; predecessor roots are unchanged; and no Plan 75 process or root exists.</acceptance_criteria>
  <verify>
    <automated>PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREALLOCATION-VALIDATION.json &amp;&amp; P74=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json &amp;&amp; for PY in /Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/bin/python3.9 /opt/homebrew/Cellar/python@3.14/3.14.4/Frameworks/Python.framework/Versions/3.14/bin/python3.14; do "$PY" -c 'import hashlib,json,sys; nodup=lambda p:dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate")); load=lambda p:(lambda b:(b,json.loads(b,object_pairs_hook=nodup)))(open(p,"rb").read()); raw,d=load(sys.argv[1]); raw74,p74=load(sys.argv[2]); canon=lambda v:(json.dumps(v,sort_keys=True,separators=(",",":"))+"\n").encode(); assert raw==canon(d) and d["schema"]=="sigra.phase235.1-plan75-preallocation-validation/v1" and d["status"]=="PASS"; rc=d["runtime_contract"]; keys=["ASDF_ELIXIR_VERSION","ASDF_ERLANG_VERSION","HOME","LC_ALL","MIX_ENV","PATH","PGDATABASE","PGHOST","PGPASSWORD","PGPORT","PGUSER"]; env={"ASDF_ELIXIR_VERSION":"1.19.5-otp-28","ASDF_ERLANG_VERSION":"28.4.1","HOME":"/Users/jon","LC_ALL":"C","MIX_ENV":"test","PATH":"/Users/jon/.asdf/shims:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin","PGDATABASE":"sigra_test","PGHOST":"localhost","PGPASSWORD":"<redacted-present>","PGPORT":"5432","PGUSER":"postgres"}; tools={"awk":"/usr/bin/awk","bash":"/opt/homebrew/bin/bash","cat":"/bin/cat","chmod":"/bin/chmod","cp":"/bin/cp","df":"/bin/df","elixir":"/Users/jon/.asdf/shims/elixir","env":"/usr/bin/env","erl":"/Users/jon/.asdf/shims/erl","find":"/usr/bin/find","git":"/opt/homebrew/bin/git","jq":"/usr/bin/jq","mix":"/Users/jon/.asdf/shims/mix","mktemp":"/usr/bin/mktemp","mv":"/bin/mv","psql":"/opt/homebrew/bin/psql","pwd":"/bin/pwd","python3":"/opt/homebrew/bin/python3","rm":"/bin/rm","sh":"/bin/sh","sha256sum":None,"shasum":"/usr/bin/shasum","sort":"/usr/bin/sort","stat":"/usr/bin/stat","test":"/bin/test"}; argv={"exact_six":["bash","scripts/ci/install-golden.sh"],"home_probe":["/Users/jon/.asdf/shims/mix","run","--no-compile","--no-start","-e","IO.write(System.user_home!() &lt;> \"\\n\")"],"timing":["bash","scripts/ci/library-partitions.sh"]}; capture={"persisted_before_parse":True,"stderr_pipe":True,"stdin":"DEVNULL","stdout_pipe":True}; assert rc["accepted"]==["authenticated_closed_runtime_bundle"] and rc["environment_keys"]==keys and rc["observed_redacted"]==env and rc["cwd"]=="/Users/jon/projects/sigra" and rc["shell"] is False and rc["consumer_argv"]==argv and rc["capture_contract"]==capture and rc["tool_resolution"]==tools; rejected=rc["rejected"]; names=[r["name"] for r in rejected]; exact={"home_absent","home_empty","home_wrong","home_symlink","home_relative","home_inherited","home_overridden","hex_home_resolution_mismatch","mix_home_resolution_mismatch","path_missing_asdf_shims","path_missing_homebrew_bin","path_missing_usr_local_bin","path_missing_usr_bin","path_missing_bin","path_reordered","asdf_erlang_missing","asdf_erlang_wrong","asdf_elixir_missing","asdf_elixir_wrong","mix_env_missing","mix_env_wrong","lc_all_missing","lc_all_wrong","pg_environment_missing","pg_environment_wrong","password_absent","password_empty","extra_environment_key","inherited_environment_key","environment_copy_projection","timing_argv_changed","exact_six_argv_changed","repository_cwd_changed","stdin_env_home_added","stdin_env_runtime_key_added","stdin_env_pg_value_changed","stdin_env_key_missing","stdin_env_copy_projection","hex_cache_missing","hex_config_symlink","hex_config_stale_hash","hex_archive_missing","phx_new_archive_missing","tool_resolution_mismatch","stdout_capture_missing","stderr_capture_missing","status_capture_missing"}; assert len(rejected)==len(names)==len(set(names))==47 and set(names)==exact and all(r["accepted"] is False and r["allocator_calls"]==r["timing_calls"]==r["exact_six_calls"]==r["network_calls"]==r["github_calls"]==0 for r in rejected); assert raw74==canon(p74) and len(raw74)==28253 and hashlib.sha256(raw74).hexdigest()=="81ed3aa1770a478ed2805a06095ec3dcba469fd6767ec5befe56eb27e7875eb8" and d["inherited"]["stdin_probe_contract"]==p74["probe_contract"] and d["inherited"]["entrypoint_policy"]==p74["entrypoint_policy"] and d["inherited"]["source_authority"]==p74["source_authority"] and d["inherited"]["sql_policy"]==p74["sql_policy"]; zero={"allocator","database","exact_six","github","home_probe","mix","mktemp","network","psql","roots","source_materializations","timing"}; assert d["counts"]=={k:0 for k in zero} and d["local_state"]["ready"] is True and len(d["runtimes"])==2 and all(r["status"]==0 and r["stderr_bytes"]==0 for r in d["runtimes"]) and len({r["semantic_sha256"] for r in d["runtimes"]})==1 and d["root_inventory_before"]==d["root_inventory_after"]==[]' "$PV" "$P74" || exit; done &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan75-runtime-home.*' -print -quit)"</automated>
    <fails_when>The receipt is absent/noncanonical/duplicate-keyed; any exact environment/home/argv/tool/cache/archive/inherited-policy field differs; a hostile name is absent, duplicated, substituted, or accepted; runtimes disagree; a forbidden count is nonzero; a predecessor/local-state manifest changes; or a Plan 75 process/root exists.</fails_when>
  </verify>
  <done>One canonical dual-runtime receipt proves the exact Plan 75 closed runtime environment, 47-hostile matrix, local Mix/Hex prerequisites, and inherited execution package without invoking a live prerequisite or allocating evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the sole Mix/Hex home-resolution prerequisite before allocation (D-05–D-07, D-09)</name>
  <reversibility rating="costly">This read-only Mix process is a one-shot pre-root prerequisite whose raw result is append-only; failure freezes the attempt and requires a successor.</reversibility>
  <precondition>Task 1 passes under both runtimes with all forbidden counts zero; its HOME/tool/cache/archive/script manifests still match; Plan 74's six tracked artifacts, frozen root, and timing process remain exact; no Plan 75 probe/root exists; and a nonempty authorized PGPASSWORD binding is available.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-HOME-PROBE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREREQUISITE-VALIDATION.json</files>
  <read_first>
    - `235.1-75-PREALLOCATION-VALIDATION.json` — sole environment, home-probe argv/output, PATH tool, local-state manifest, generated-package, and zero-effect authority.
    - Plan 74 summary/failure/root process — preserve the consumed timing failure and prove no predecessor action is being repeated.
    - `/Users/jon`, `.hex/hex.config`, `.hex/cache.ets`, Hex/phx_new archives, pinned ASDF runtimes, repository deps/build/lock, and both canonical wrappers — reauthenticate immediately before and after the one process.
    - Plan 74 PostgreSQL capture/decision, Plan 73 prerequisite, Plan 72 source, and Plan 71 SQL receipts — retain successful prerequisite policies and frozen failed history without rerunning any process.
  </read_first>
  <action>Independently reopen Task 1, regenerate the exact eleven-key runtime map, home-probe argv, expected raw output, cwd, binary/path identities, and controller source, and require byte/field equality before any process. Read the authorized password once into an opaque scalar, require nonempty, and construct the environment by eleven explicit assignments; do not read/copy/merge/update/iterate over the ambient environment or pass a twelfth key. Reopen all Plan 74 artifacts/root and all local-state manifests before action.

Invoke exactly one process with argv `[/Users/jon/.asdf/shims/mix,run,--no-compile,--no-start,-e,IO.write(System.user_home!() &lt;> "\n")]`, stdin DEVNULL, stdout/stderr PIPE, shell false, cwd `/Users/jon/projects/sigra`, and the exact fresh environment. This is a read-only dependency-loader/home-resolution prerequisite, not a timing, exact-six, psql, database, allocator, setup, or TEST action. Record wall-clock bounds and immediately capture immutable stdout, stderr, and status. Before checking any result, atomically write duplicate-safe canonical `235.1-75-HOME-PROBE.json` with schema/status `CAPTURED`, exact argv/cwd/shell/stdin/pipe fields, full stdout/stderr base64/bytes/SHA, process count one, exact complete redacted environment/key order/password SHA, Mix/Elixir/Erlang/Hex identities, Task 1 digest, predecessor/local-state before manifests, timestamps, and omitted-field receipt digest. No-follow reopen and reproduce all bytes/digests before parsing.

Only after durable reopen require status zero, stderr empty, stdout exactly the 11 authenticated bytes `/Users/jon\n`, and direct raw equality before `splitlines(keepends=True)`. Reauthenticate `/Users/jon`, Hex config/cache, both archives, ASDF roots, deps/build/lock, canonical scripts, PATH tools, and every predecessor after the process; require exact before/after equality and no new cache/config/archive/package/build file. Require the Task 1 static first timing argv/environment/cwd and exact-six argv/environment/cwd contracts unchanged; do not invoke either wrapper.

Atomically write and no-follow reopen canonical `235.1-75-PREREQUISITE-VALIDATION.json`, binding Task 1 and raw-probe hashes, exact home parse, complete redacted environment equality, immutable local-state/predecessor manifests, and counts `home_probe=1`, `mix=1`, with psql/database/allocator/mktemp/root/source-materialization/timing/exact-six/network/GitHub all zero. On any capture, identity, output, cache, or parse failure, write status BLOCKED with the frozen raw capture, consume the probe, allocate no root, and require a successor. Never retry, rewrite, normalize, substitute another query, or treat status alone as readiness.</action>
  <acceptance_criteria>Exactly one Mix/Hex home process is durably captured before parse; its exact argv, cwd, shell, stdin, pipes, complete eleven-entry redacted environment, status, empty stderr, and `/Users/jon\n` stdout pass; local cache/archive/tool/script and predecessor manifests are unchanged; the decision is PASS; and no allocator, timing, exact-six, network, GitHub, or Plan 75 root exists.</acceptance_criteria>
  <verify>
    <automated>RAW=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-HOME-PROBE.json &amp;&amp; PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREREQUISITE-VALIDATION.json &amp;&amp; /usr/bin/python3 -c 'import base64,hashlib,json,sys; nodup=lambda p:dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate")); load=lambda p:(lambda b:(b,json.loads(b,object_pairs_hook=nodup)))(open(p,"rb").read()); rr,r=load(sys.argv[1]); vr,v=load(sys.argv[2]); canon=lambda x:(json.dumps(x,sort_keys=True,separators=(",",":"))+"\n").encode(); assert rr==canon(r) and vr==canon(v) and r["schema"]=="sigra.phase235.1-plan75-home-probe/v1" and r["status"]=="CAPTURED" and v["schema"]=="sigra.phase235.1-plan75-prerequisite-validation/v1" and v["status"]=="PASS"; env={"ASDF_ELIXIR_VERSION":"1.19.5-otp-28","ASDF_ERLANG_VERSION":"28.4.1","HOME":"/Users/jon","LC_ALL":"C","MIX_ENV":"test","PATH":"/Users/jon/.asdf/shims:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin","PGDATABASE":"sigra_test","PGHOST":"localhost","PGPASSWORD":"<redacted-present>","PGPORT":"5432","PGUSER":"postgres"}; keys=sorted(env); out=base64.b64decode(r["stdout_base64"],validate=True); err=base64.b64decode(r["stderr_base64"],validate=True); assert r["argv"]==["/Users/jon/.asdf/shims/mix","run","--no-compile","--no-start","-e","IO.write(System.user_home!() <> \"\\n\")"] and r["cwd"]=="/Users/jon/projects/sigra" and r["shell"] is False and r["stdin"]=="DEVNULL" and r["stdout_pipe"] is True and r["stderr_pipe"] is True and r["environment"]["keys"]==keys and r["environment"]["observed_redacted"]==env and len(r["environment"]["password_sha256"])==64 and r["process_count"]==1 and r["persisted_before_parse"] is True; assert out==b"/Users/jon\n" and len(out)==r["stdout_bytes"]==11 and hashlib.sha256(out).hexdigest()==r["stdout_sha256"]=="9d75bf0190e2e157453723af13843a699ec75794f6f175ca48a8e7e024980297" and err==b"" and r["stderr_bytes"]==0 and r["stderr_sha256"]=="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855" and r["status_code"]==0; assert v["probe_file_sha256"]==hashlib.sha256(rr).hexdigest() and v["home_resolution"]=={"exact_raw_equal":True,"home":"/Users/jon","lines_base64":["L1VzZXJzL2pvbgo="],"normalization":[],"persisted_before_parse":True} and v["local_state_before"]==v["local_state_after"] and v["predecessors_before"]==v["predecessors_after"]; assert v["counts"]=={"allocator":0,"database":0,"exact_six":0,"github":0,"home_probe":1,"mix":1,"mktemp":0,"network":0,"psql":0,"roots":0,"source_materializations":0,"timing":0} and v["root_inventory_before"]==v["root_inventory_after"]==[]' "$RAW" "$PV" &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan75-runtime-home.*' -print -quit)"</automated>
    <fails_when>Either receipt is absent/noncanonical/duplicate-keyed; call count differs from one; any argv/cwd/env/input/stream/status/home byte or hash differs; capture did not precede parsing; local state or a predecessor changes; any forbidden count is nonzero; or a Plan 75 root exists.</fails_when>
  </verify>
  <done>One frozen pre-root Mix process proves the exact closed environment resolves HOME and loads the current Hex/dependency state, with raw evidence and unchanged manifests authorizing the sole Plan 75 allocation.</done>
</task>

<task type="auto">
  <name>Task 3: Allocate one Plan 75 root and run the unchanged local/live evidence route (D-01–D-09)</name>
  <reversibility rating="costly">The root, timing/exact-six processes, evidence commit, and GitHub runs are append-only one-shot evidence; any failure is durable and cannot be repaired or retried in place.</reversibility>
  <precondition>Tasks 1–2 PASS and independently reopen exactly; the raw home probe, complete environment, cache/archive/tool/script manifests, and inherited stdin/entrypoint/source/SQL policies agree; no Plan 75 root exists; all failed predecessor roots/artifacts remain unchanged; GitHub authentication is valid; and repo `szTheory/sigra`, PR #234, base `main`, head `refs/heads/ci/phase-235-1-evidence`, remote head `87338db7`, and candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3` match inherited authority.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <read_first>
    - All three Plan 75 pre-root receipts — sole runtime environment, generated package, home-resolution, local-state, allocator, and zero/one-call accounting authority.
    - Plan 74 plan/pre-root receipts/summary/failure/root — carry the successful stdin prerequisite and partial local policies while freezing its root and failed timing action forever.
    - Plan 73 canonical wrapper/prerequisite authority, Plan 72 source authority, and Plan 71 SQL authority — inherit unchanged accepted/adverse matrices and exact source identities.
    - `scripts/ci/library-partitions.sh`, `scripts/ci/install-golden.sh`, their verifiers/tests, workflow, routing/validation verifiers, and Phase 235.1 contract — reauthenticate immediately before each sole local or live action.
    - Plans 69/71/72 roots and all Plans 68/69/71/72/73/74 summaries/failures — preserve every used/unused budget and prohibit partial-evidence reuse.
  </read_first>
  <action>Authenticate all Plan 75 pre-root receipts and independently reconstruct the eleven-key runtime map, one home-probe accounting row, generated package, Plan 74 exact stdin/five-line authority, Plan 73 entrypoint policy, Plan 72 source locator, Plan 71/72 parsed SQL policy, local-state manifests, and current canonical files. Require exact equality before invoking the execute launcher once. Its allocator may call mktemp once with `/private/tmp/sigra-p2351-plan75-runtime-home.XXXXXXXX`, create one mode-0700 root and one exclusive mode-0600 Plan 75 marker, write canonical driver/handoff/finalizer receipts, materialize each authenticated source once, and reopen authority. Reauthenticate all failed predecessor roots/artifacts before and after every budgeted action.

Run one fresh local chain under Plan 75 identity: challenge-bound PostgreSQL 14 host gate; all 19 Plan 67 parser fixtures; Plan 72's accepted source control/ten hostile source cases; Plan 71/72's accepted SQL control/23 hostile SQL cases; the new 47-hostile runtime environment matrix; one real dry run; candidate/current observation; one admission; one health sample; and exactly one `bash scripts/ci/library-partitions.sh` timing process. Pass only the exact fresh eleven-key environment from Task 1, whose authenticated PATH resolves `bash` to `/opt/homebrew/bin/bash`. Persist its complete stdout/stderr/status/argv/cwd/redacted environment and time bounds in `local/timing/pair-1/process.json` before any assertion; never repeat Plan 74's process or read from its partial outputs. Require status zero and then exactly three complete pairs, six green timing children, 225 exhaustive disjoint paths in deterministic 114/111 partitions, sequential partition order inside each pair, positive integer raw durations, unchanged parallel ExUnit formatter behavior, independently recomputed raw ratio <=2.0, and nine fresh ledger-disjoint payloads. Use `plan75-collector-verify\n` with Plan 67 parser anchors.

After timing and before terminal publication, invoke exactly one `bash scripts/ci/install-golden.sh` process from authenticated repository cwd with the same exact eleven-key environment and authenticated bash resolution. Do not add prepared mode in the parent or invoke raw `mix test`; the canonical wrapper must overwrite `SIGRA_INSTALL_GOLDEN_PREPARED=1` and own its one exact ordered child. Persist complete stdout/stderr/status/argv/cwd/redacted environment and time bounds before any assertion; then preserve/no-follow reopen its receipt and diagnostics. Require status zero, exact unique six, prepared fixture true, worker ceiling two, complete positive phases, six variants, unique partitions/ports, zero failed paths, and the unchanged install-golden verifier PASS. Any local failure writes a durable full-stream failure receipt, freezes the root, performs no remote action, and requires a successor without retry.

Seal every descendant once in `local/artifact-manifest.json`, publish one terminal PASS, and make `local/verify-plan75-terminal.py` independently reopen every Plan 75 receipt, predecessor root/history artifact, source/policy/environment/entrypoint/prerequisite/process/wrapper/diagnostic/timing receipt, ledger, and manifest. Terminal authority requires one home probe before allocation, no new psql prerequisite, one root/local chain, one timing wrapper, three pairs/six children/225 paths, one real canonical exact-six wrapper, full stream capture, and all attempt budgets exact.

Only after terminal PASS, follow the inherited GitHub route once: query rate limit once; hard-stop at REST core <=250 or HTTP 403/429 until reset; use at most one 60-second watcher per run, one structured summary after completion, and failed logs only on failure. At most one non-rate-limit API transient may be retried; never rerun a workflow or increment attempt. Exclude all retained Plan 35–74 run/job IDs. Push candidate `3713dcd4` once, capture one automatic pull_request attempt-1 run and one workflow_dispatch attempt-1 run at the same SHA, require exact route/job/artifact identities and decoded two-partition/exact-six predicates, write/reopen the two evidence receipts, commit only them as candidate's direct child, and push once. Capture one automatic PR attempt-1 validation and one distinct dispatch attempt-1 validation at the evidence SHA. Write `235.1-VALIDATION-RUN.json` only after all four pass, bind Plan 75 root/terminal/home/runtime/stdin/entrypoint/timing/exact-six authority, and run unchanged offline verifiers. Do not push again or reconcile prose.</action>
  <acceptance_criteria>Exactly one Plan 75 root/local chain exists; terminal verification authenticates the one pre-root home probe, complete runtime environment, frozen Plan 74 stdin authority, inherited source/SQL/entrypoint policies, three timing pairs, six green children, 225 paths, and one canonical exact-six process with both streams preserved; every failed predecessor remains unchanged; exactly four fresh distinct non-excluded attempt-1 runs prove capture/validation PR/dispatch; the evidence commit is candidate's direct child with only two capture JSON files changed; and both unchanged evidence verifiers pass.</acceptance_criteria>
  <verify>
    <automated>PLAN75_ROOT="$(/usr/bin/python3 -c 'import os,re; rows=sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan75-runtime-home\.[A-Za-z0-9_]{8}",e.name) and e.is_dir(follow_symlinks=False)); assert len(rows)==1; print(rows[0])')" &amp;&amp; /usr/bin/python3 "$PLAN75_ROOT/local/verify-plan75-terminal.py" --root "$PLAN75_ROOT" --preallocation-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREALLOCATION-VALIDATION.json --home-probe .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-HOME-PROBE.json --prerequisite-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PREREQUISITE-VALIDATION.json --plan69-root /private/tmp/sigra-p2351-plan69-recovery.G9QrBacu --plan71-root /private/tmp/sigra-p2351-plan71-sql-guard.knJsmWsF --plan72-root /private/tmp/sigra-p2351-plan72-source-authority.Q1orfipi --plan74-root /private/tmp/sigra-p2351-plan74-stdin-probe._55jsnhr &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Root discovery returns zero/multiple; a predecessor changes; any home/environment/stdin/source/policy/entrypoint/prerequisite/stream/manifest/ledger/partition/ratio/timing/exact-six predicate fails; an action repeats; a run is stale/excluded/retried/wrong-ref/wrong-SHA/non-green; lineage differs; or an offline verifier fails.</fails_when>
  </verify>
  <done>One fresh home-authenticated Plan 75 chain proves complete local, PR, and non-PR routes and leaves canonical capture/validation receipts as Plan 70's sole new TEST authority.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | Items | Coverage |
|---|---|---|
| GOAL | Two measured exhaustive PR partitions plus exact-six non-PR coverage without protected regression | Task 3 produces both routes from one fresh Plan 75 chain; no predecessor partial evidence is promoted. |
| REQ | TEST-01, TEST-02, TEST-03 | All three are in frontmatter; Task 3 preserves their exact predicates and produces their only possible new evidence. |
| RESEARCH | Independent fail-closed receipts, same-run timing, deterministic receiver ownership, pinned phx_new/cache prerequisites | Tasks 1–3 authenticate the runtime/home/cache boundary, preserve canonical owners, and require independent replay. |
| CONTEXT | D-01 through D-09 and operator/recovery addenda | Tasks cite every decision, preserve two partitions/exact-six/attempt-1 budgets, delay reconciliation, and retain FAST-01/GATE-05. |
| LATEST EVIDENCE | Plan 74 Tasks 1–2 passed; its first timing command failed because HOME was absent | Task 1 freezes that exact process/root and proves the complete successor environment; Task 2 proves home resolution before Task 3 can allocate. |
| HISTORY | Plans 68/69/71/72/73/74 and blocked Plan 70 | Every failed plan remains immutable; only Plan 75 can supply new completion evidence, and Plan 70 remains reconciliation-only. |
| CONTEXT deferred | Unrelated release, installer-product, auth, security, admin, and other CI work | Excluded; no deferred item is implemented. |

</source_coverage_audit>

<recovery_decisions>

- `/private/tmp/sigra-p2351-plan74-stdin-probe._55jsnhr`, its one failed timing process, and every descendant are permanently frozen; its passing stdin/pre-root receipts remain authenticated history, never completion authority.
- Plan 75 uses an eleven-key literal runtime projection, not Plan 74's map plus a late mutation and not an ambient-environment copy. The distinct Plan 74 seven-key PostgreSQL prerequisite map remains unchanged.
- One exact pre-root Mix dependency-loader probe must resolve `/Users/jon` and leave cache/archive/build manifests unchanged before allocation; it is counted once and never reused as timing or TEST evidence.
- Plan 75 retains the canonical exact-six runner, path-keyed health source, parsed SQL allowlist, two ordinary partitions, exact-six receiver, TEST predicates, FAST-01, GATE-05, and every failed predecessor without weakening or relabeling.
</recovery_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Frozen Plan 74 root → Plan 75 pure gate | The missing-HOME process informs fixtures but cannot be retried or reused. |
| Closed environment → Mix/Hex | Only the exact eleven-key projection may cross into the one pre-root home probe and later wrapper processes. |
| Home/cache/archive state → allocator | Local-state identities must remain unchanged across the home probe before a root can exist. |
| Prior stdin/runner/source/SQL policies → Plan 75 package | Accepted and hostile matrices remain exact while only successor identity changes. |
| Local terminal → GitHub | Remote actions remain blocked until home, cache, stdin, timing, entrypoint, exact-six, and manifest predicates pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-75-01 | Spoofing | HOME/Mix/Hex resolution | high | mitigate | Tasks 1–2 require literal non-symlink HOME, exact raw `/Users/jon\n`, pinned runtime identities, and 47 named adverse fixtures. |
| T-235.1-75-02 | Tampering | Runtime environment | high | mitigate | Exact eleven-key map, AST proof of fresh construction, no override/inheritance, full redacted equality, and identical env for both wrappers. |
| T-235.1-75-03 | Information Disclosure | Password and Hex config | high | mitigate | Persist only redacted password plus SHA and no-follow config identity/hash; never persist raw secret/config bytes. |
| T-235.1-75-04 | Repudiation | Home prerequisite process | high | mitigate | Atomic raw receipt and no-follow reopen precede status/output interpretation; failure consumes the attempt and blocks allocation. |
| T-235.1-75-05 | Tampering | Cache/archive/tool authority | high | mitigate | Exact before/after reduced manifests, pinned ASDF/Hex/phx_new identities, literal PATH, and no tool execution in the dual-runtime gate. |
| T-235.1-75-06 | Repudiation | Failed predecessor budgets | high | mitigate | Every task authenticates Plans 69/71/72/74 roots and Plans 68/69/71/72/73/74 artifacts and forbids retry/reuse/relabeling. |
| T-235.1-75-07 | Spoofing | GitHub evidence | high | mitigate | Exact repo/ref/SHA/event/workflow/job/attempt/artifact identities, excluded IDs, direct-child lineage, and unchanged offline verifiers. |
| T-235.1-75-08 | Denial of Service | Local/live budget | medium | mitigate | One home probe, one root, one timing wrapper, one exact-six wrapper, one watcher per run at 60 seconds, and no workflow reruns. |
</threat_model>

<verification>
Task 1 proves the complete eleven-key runtime and 47-hostile matrix under both pinned runtimes with zero downstream effects. Task 2 spends one read-only Mix home-resolution process, persists full raw evidence before parsing, and authorizes allocation only on exact `/Users/jon\n` plus unchanged local-state manifests. Task 3 independently authenticates both gates, runs one canonical local chain, and only after terminal PASS spends four fresh attempt-1 live runs. Plan 70 remains unavailable until Plan 75's summary and unchanged evidence verifiers pass.
</verification>

<success_criteria>
Plan 75 succeeds only when the dual-runtime environment receipt, raw home-probe capture, prerequisite decision, sole fresh root, three complete timing pairs, canonical exact-six process, local terminal, capture pair, and distinct validation pair all independently PASS; the exact 47 runtime, inherited 37 stdin, 15 entrypoint, ten source, and 23 SQL hostile cases remain complete and unique; Plans 69/71/72/73/74 stay frozen; no action is retried; and unchanged routing/validation verifiers accept the resulting evidence.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-SUMMARY.md` when done.
</output>
