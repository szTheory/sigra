---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 06
type: execute
wave: 4
depends_on: [235.1-03]
files_modified:
  - test/support/install_fixture.ex
  - test/test_helper.exs
  - mix.exs
  - scripts/ci/install-golden.sh
  - scripts/ci/install-golden.test.sh
  - test/upgrade_test.exs
  - test/sigra/install/golden_diff_test.exs
  - test/sigra/install/idempotency_test.exs
  - scripts/ci/library-economics.sh
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
  - test/sigra/install/features/passkeys_js_test.exs
  - test/sigra/install/generator_passkeys_opt_out_test.exs
  - test/sigra/install/vault_promotion_test.exs
  - test/support/install_fixture_performance_test.exs
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-05, the single `MIX_ENV=test mix ci` owner retains all six canonical install/scaffold receiver paths exactly once on every PR."
    - "Per D-06, one timed immutable fixture DAG replaces twelve independent Phoenix bootstraps while fixture preparation remains after `install_start` and before `install_end`."
    - "Every mutable scenario runs from a private copy-on-write checkout with a unique database partition and server port; no writable generated app, Mix manifest, or build output is shared."
    - "Compatible root dependency sources and `_build/test` bytes are reused safely, with private build trees and deterministic copy fallback accounting."
    - "Exactly two workers execute independent scenarios; first failure cancels outstanding work, retains diagnostic checkout paths, and returns nonzero."
    - "All existing compile/runtime assertions and the fixed `max/min <= 2.0` plus `install_scaffold <= ordinary` predicates remain authoritative."
    - "The blocked Plan 04 summary is a measured diagnostic and checkpoint input only; it is never admitted as completion or passing evidence."
  artifacts:
    - path: "test/support/install_fixture.ex"
      provides: "Prepared immutable fixture graph, private checkout API, unique partitions/ports, bounded two-worker runner, phase timing, and one safe cleanup owner"
    - path: "scripts/ci/install-golden.sh"
      provides: "Fixed-command six-receiver runner enabling prepared-fixture mode inside the measured install leg"
    - path: "scripts/ci/install-golden.test.sh"
      provides: "Hermetic exact-path, one-invocation, timing-boundary, status, and diagnostic-receipt contract"
    - path: "test/support/install_fixture_performance_test.exs"
      provides: "Focused fixture DAG compatibility, immutability, copy isolation, concurrency, cancellation, and diagnostic tests"
    - path: "test/sigra/planning/phase_235_1_library_economics_contract_test.exs"
      provides: "Fixed-predicate, preparation-boundary, topology, and omission/duplication performance contract"
  key_links:
    - from: "mix ci"
      to: "scripts/ci/library-economics.sh"
      via: "one existing economics harness under the sole library owner"
    - from: "scripts/ci/library-economics.sh install_start/install_end"
      to: "scripts/ci/install-golden.sh"
      via: "the complete prepared-fixture construction and six-receiver execution occur inside the timed install command"
    - from: "test/test_helper.exs"
      to: "test/support/install_fixture.ex"
      via: "fixed prepared-mode environment flag creates one graph and registers one validated after-suite cleanup"
    - from: "immutable prepared variants"
      to: "six canonical receiver files"
      via: "named read-only snapshots or private copy-on-write scenario checkouts with unique partition/port manifests"
    - from: "root deps and compatible _build/test"
      to: "private generated checkouts"
      via: "read-only dependency-source reuse plus fingerprint-checked reflink/copy seeding into per-checkout build directories"
  prohibitions:
    - "MUST NOT relax, bypass, round, or replace either fixed economics predicate."
    - "MUST NOT prepare or restore install-only fixtures before `install_start` or after `install_end`."
    - "MUST NOT share writable Mix manifests, build outputs, generated applications, databases, or server ports between scenarios."
    - "MUST NOT delete compile/runtime assertions, omit or duplicate a receiver, add a second suite owner, or change the protected `Library tests` aggregate."
    - "MUST NOT use caller-provided commands, `eval`, unrestricted concurrency, or more than two workers."
    - "MUST NOT create `235.1-EVIDENCE.json`, update requirement status, or treat run 34395477605 as passing evidence."
---

<objective>
Remediate the measured install/scaffold economics miss by replacing twelve duplicated Phoenix bootstraps with one timed immutable prepared-fixture DAG and isolated two-worker scenario execution.

Purpose: Make the existing install/scaffold coverage economically comparable without moving work outside the measured class, laundering coverage, or weakening either fixed predicate.
Output: Fixed install runner, reusable fixture graph, migrated receivers, focused diagnostics/contracts, and a locally passing fixed-bound gate ready for fresh PR evidence capture in Plan 04.

Decision coverage: D-01 through D-06 and D-09 constrain implementation and non-regression; D-07 and D-08 remain blocked for Plans 04–05.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md
@test/support/install_fixture.ex
@test/test_helper.exs
@mix.exs
@scripts/ci/library-economics.sh
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one default-install scenario through the timed prepared-fixture DAG (D-01, D-05, D-06)</name>
  <reversibility rating="costly">This changes the shared install-test fixture lifecycle and contributor alias, but remains source-revertible with focused contracts.</reversibility>
  <files>test/support/install_fixture.ex, test/test_helper.exs, scripts/ci/install-golden.sh, scripts/ci/install-golden.test.sh, test/sigra/install/golden_diff_test.exs</files>
  <read_first>
    - `235.1-RESEARCH.md` Measured Miss Remediation — implement its graph, timing, compatibility, isolation, and bounded-concurrency contracts exactly.
    - Blocked `235.1-04-SUMMARY.md` — use 21,050ms/552,221ms and run 34395477605 only as the durable miss diagnostic; do not import it as evidence.
    - `test/support/install_fixture.ex` — preserve current installer/upgrade/Mix command behavior while threading a unique `MIX_TEST_PARTITION` through every subprocess.
    - `test/test_helper.exs` and generated `config/test.exs` fixture — reuse the existing test database partition suffix and register exactly one after-suite cleanup owner.
    - `mix.exs` `ci` and `ci.install_golden` — preserve one public owner and move the canonical six paths into one fixed runner without copying them elsewhere.
  </read_first>
  <behavior>
    - "The fixed runner contains each canonical receiver path exactly once, sets one fixed prepared-mode flag, preserves the child status, and accepts no caller-selected command."
    - "One Phoenix source base derives a named immutable default-installed state; one golden scenario consumes captured tree/stdout without a second installer invocation."
    - "Graph preparation starts only after `library-economics.sh` records `install_start` and finishes before `install_end`."
    - "A diagnostic receipt records positive integer milliseconds for phx_new, deps_get, baseline_compile, installer, receiver_compile_runtime, and checkout_copy; exact keys occur once and their sum does not exceed the install total."
  </behavior>
  <action>
    Start RED in `install-golden.test.sh` with a shadowed fixed child and clock. Implement `install-golden.sh` with the six canonical receiver paths once, a fixed prepared-mode environment flag, exact argument ordering, first-child-status propagation, and no dynamic executable/eval seam. Exercise this fixed runner directly for the tracer; Task 2 wires the already-proven seam into `ci.install_golden`.

    Add the smallest production-quality prepared graph path to `InstallFixture`: create one canonical Phoenix source base under a validated `mktemp -d` root, reuse root dependency sources without modifying them, fingerprint the Elixir/OTP/lock/compiler inputs before seeding compatible root `_build/test` bytes, and copy those bytes with `cp --reflink=auto` on Linux plus deterministic normal-copy fallback. Every scenario gets its own build directory and Mix manifests; reject symlink/hardlink/write sharing. Derive an immutable default-installed snapshot with captured normalized stdout, expose named read-only and private-checkout access, propagate a unique partition token and port through every Mix/installer/upgrade child, and register one validated `ExUnit.after_suite/1` cleanup when the fixed flag is present.

    Update golden diff to consume the named default snapshot and its captured tree/stdout without a second installer run. Emit `/tmp/sigra-install-golden-diagnostics.json` atomically with exact phase keys, positive integer durations, copy mode, variant/scenario counts, unique partitions/ports, and retained failed-checkout paths. The tracer executes golden tree/stdout assertions from the one default install, proving runner→timed graph→private/read-only consumer→diagnostic output end to end.
  </action>
  <acceptance_criteria>
    - The hermetic runner contract proves six exact paths, one invocation, fixed flag propagation, child-status preservation, and no preparation before the timed child.
    - One default fixture is prepared once inside the measured leg and supplies golden tree plus stdout without a second bootstrap/install.
    - Root dependency/build reuse is fingerprint-compatible and leaves root bytes unchanged; each checkout owns writable manifests/build output.
    - The exact diagnostic schema is atomic, bounded, non-self-authorizing, and rejects omitted/duplicate/zero/negative phase values.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/install-golden.test.sh &amp;&amp; shellcheck scripts/ci/install-golden.sh scripts/ci/install-golden.test.sh &amp;&amp; MIX_ENV=test SIGRA_INSTALL_GOLDEN_PREPARED=1 mix test test/sigra/install/golden_diff_test.exs</automated>
    <fails_when>any command exits non-zero; the runner PASS banner is absent; ShellCheck reports a diagnostic; a receiver is missing/duplicated; preparation occurs outside the child timing boundary; the base/install runs more than once; root dependency/build bytes change; a writable manifest/build output is shared; or diagnostic schema/timing validation fails</fails_when>
  </verify>
  <done>One real default-install path traverses the fixed timed runner and immutable fixture graph with isolated mutable state and attributable diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand private graph states across upgrade, golden, and idempotency receivers (D-02, D-05, D-06)</name>
  <reversibility rating="costly">Receiver setup changes share the fixture API, so rollback requires coordinated receiver and topology-contract edits.</reversibility>
  <files>mix.exs, test/upgrade_test.exs, test/sigra/install/idempotency_test.exs, scripts/ci/library-economics.sh, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `test/upgrade_test.exs` — preserve all three upgrade variants, database seeding/migration, rerun checks, compile assertions, and server/runtime behavior.
    - `golden_diff_test.exs` output from Task 1 and `idempotency_test.exs` — share the same captured default install only read-only for golden; clone privately before idempotency mutation.
    - `mix.exs` `ci.install_golden` alias — replace its inline receiver list with the fixed runner once and preserve the surrounding `mix ci` order.
    - `library-economics.sh` `install_start`/`install_end` region — retain the complete install runner inside the authoritative duration.
    - Phase 233 contract — move exact path ownership to the fixed runner while preserving live `:scaffold` equality and one-owner assertions.
  </read_first>
  <behavior>
    - "Golden tree/stdout uses the immutable default snapshot, idempotency mutates a private clone, and upgrade scenarios clone the matching installed state before database/filesystem changes."
    - "Three upgrade scenarios retain their compile, migration, rerun, and runtime assertions with unique partitions and ports."
    - "The economics harness measures fixture preparation, checkout copies, and all receiver work between the existing install markers."
    - "The fixed runner remains the sole owner of six live `:scaffold` paths, each exactly once, with no second library execution owner."
  </behavior>
  <action>
    Preserve Task 1's golden read-only consumer, convert idempotency to a private copy-on-write checkout before its second invocation, and convert all three upgrade variants to private checkouts of the correct default or no-organizations installed states. Preserve every database seed, upgrade, rerun, compile, server-start, and runtime assertion. Assign unique manifest partition tokens and ports before dispatch.

    Replace the inline `ci.install_golden` receiver list in `mix.exs` with one call to the fixed runner, preserving the surrounding single `mix ci` owner and order. Keep `library-economics.sh` timing the entire fixed `mix ci.install_golden` runner after `install_start` and before `install_end`; add only the validation/link needed to bind the diagnostic total to that raw install duration. Evolve the Phase 233 contract so canonical receiver ownership is parsed from `install-golden.sh`, the exact live tagged set remains equal and unique, the alias invokes the runner once, and no receiver list or suite owner is duplicated.
  </action>
  <acceptance_criteria>
    - Golden and idempotency share preparation but never mutable state; all upgrade scenarios are filesystem/database/port isolated.
    - Every pre-existing compile/runtime assertion in these three receiver files remains executable and passes.
    - Preparation and diagnostic copy cost remain included in the install duration, whose verifier predicates are unchanged.
    - Phase 233 contract proves one fixed runner, six exact receiver paths, live-set equality, and one `MIX_ENV=test mix ci` owner.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test SIGRA_INSTALL_GOLDEN_PREPARED=1 mix test test/upgrade_test.exs test/sigra/install/golden_diff_test.exs test/sigra/install/idempotency_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
    <fails_when>the command exits non-zero; ExUnit reports any failure or zero tests; any compile/runtime assertion disappears or is bypassed; a snapshot is mutated; a partition/port is reused; a receiver is omitted/duplicated; the install timing excludes preparation/copy work; or owner topology changes</fails_when>
  </verify>
  <done>Default, idempotency, and upgrade coverage consume isolated prepared states exactly once with all behavioral proofs retained inside the measured install class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete variant fan-out, enforce two-worker isolation, and prove the fixed bounds (D-02–D-06, D-09)</name>
  <reversibility rating="costly">This completes the shared scenario scheduler and changes three receiver setups, but all behavior remains protected by focused and full parity contracts.</reversibility>
  <files>test/sigra/install/features/passkeys_js_test.exs, test/sigra/install/generator_passkeys_opt_out_test.exs, test/sigra/install/vault_promotion_test.exs, test/support/install_fixture_performance_test.exs, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <read_first>
    - Passkeys JS, passkeys opt-out, and vault promotion receiver files — preserve standard/non-standard app.js, no-passkeys, no-organizations combinations, compile sentinels, rerun/idempotency, and runtime assertions.
    - `InstallFixture` graph/scheduler from Tasks 1–2 — consume named variants; do not add a second cache, manifest owner, or worker pool.
    - Phase 235.1 contract — pin fixed thresholds, timing boundary, immutable variant names, exact scenario coverage, and protected FAST/GATE evidence.
    - Blocked Plan 04 summary — retain run 34395477605 and its 26.23x miss as diagnostic history only.
  </read_first>
  <behavior>
    - "Passkeys standard/non-standard, no-passkeys, no-organizations/no-passkeys, no-organizations, and vault scenarios consume the exact named graph states while retaining every compile/runtime/wiring assertion."
    - "At most two independent scenario checkouts run concurrently; each has unique partition and port values, and first failure cancels outstanding work while retaining failed checkout diagnostics."
    - "Focused performance tests reject mutable snapshot reuse, shared manifests/builds, incompatible root build reuse, phase omission/duplication, work before install_start, worker count other than two, and swallowed child failures."
    - "The final local run keeps one library owner/exact-once receivers and passes unchanged max/min <= 2.0 plus install_scaffold <= ordinary predicates; otherwise Plan 06 remains blocked."
  </behavior>
  <action>
    Migrate the remaining receivers onto the named passkeys, non-standard app.js, no-passkeys, no-organizations/no-passkeys, and no-organizations snapshots. Use private checkouts for rerun, vault, compile, or runtime mutation and read-only access only for nonmutating assertions. Preserve every existing JS wiring, opt-out, rerun/idempotency, compile, vault promotion, and runtime assertion.

    Add `install_fixture_performance_test.exs` for graph cardinality, immutable snapshot enforcement, compatible root-cache fingerprinting, copy-on-write/fallback isolation, unique partitions/ports, exact two-worker maximum, first-failure cancellation/status propagation, retained failed-checkout diagnostics, exact diagnostic phase keys, and `sum(phases) <= install_scaffold.duration_ms`. Expand the Phase 235.1 contract to pin exact named variants/scenarios, preparation between `install_start` and `install_end`, fixed receiver ownership, absence of environment/threshold bypasses, and the unchanged two economics predicates. The blocked Plan 04 summary remains an asserted failed observation, never a passing source.

    Run the focused receiver/contracts and complete contributor command. If either fixed predicate still misses, preserve the diagnostic receipt, keep Plan 06 blocked, and use its largest duplicated phase for the next scoped remediation; never create evidence or relax thresholds. A local passing result authorizes only Plan 04 to collect a fresh attempt-1 PR observation.
  </action>
  <acceptance_criteria>
    - All six receiver files preserve their prior behavioral assertions and execute exactly once through one fixed runner.
    - Twelve independent bootstraps are replaced by one source base and the six named installed-state variants; mutable scenarios always use private checkouts.
    - Concurrency is exactly bounded at two with deterministic cancellation, failure propagation, and unique isolation values.
    - Diagnostic phases are complete, unique, positive, included in the timed install total, and retain checkout-copy fallback cost.
    - Full local contributor parity and the independent verifier pass both unchanged economics predicates; failure leaves durable diagnostics and no evidence/status updates.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/install-golden.test.sh &amp;&amp; MIX_ENV=test SIGRA_INSTALL_GOLDEN_PREPARED=1 mix test test/sigra/install/features/passkeys_js_test.exs test/sigra/install/generator_passkeys_opt_out_test.exs test/sigra/install/vault_promotion_test.exs test/support/install_fixture_performance_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; MIX_ENV=test mix ci &amp;&amp; bash scripts/ci/verify-library-economics.sh</automated>
    <fails_when>any command exits non-zero; either shell/verifier PASS banner is absent; ExUnit reports any failure or zero tests; any compile/runtime assertion is absent; graph/scenario cardinality, immutability, private-build, partition/port, two-worker, cancellation, diagnostic, timing-boundary, exact-once, owner, or protected-invariant assertion fails; or either fixed economics predicate misses</fails_when>
  </verify>
  <done>The measured duplication is removed with attributable diagnostics, complete isolated receiver coverage, bounded concurrency, and a locally green unchanged economics gate ready for Plan 04's fresh PR evidence.</done>
</task>

</tasks>

## Flagged assumptions

- **PR authority:** Plan 06 proves implementation and local performance diagnostics. Plan 04 alone captures the fresh attempt-1 PR evidence required for TEST closure.
- **Copy portability:** Linux uses `cp --reflink=auto`; unsupported filesystems use a measured normal copy. Either path remains inside the install duration.
- **Root build compatibility:** Reuse is admitted only on an exact Elixir/OTP/lock/compiler fingerprint; incompatible bytes fail closed to a private rebuild whose cost remains measured.

## Artifacts this phase produces

- `scripts/ci/install-golden.sh` and `.test.sh` — fixed six-receiver runner and hermetic topology/timing/status contract.
- `test/support/install_fixture.ex` plus `test/test_helper.exs` — immutable prepared-fixture graph, safe reuse, private checkouts, two-worker scheduling, diagnostics, and cleanup.
- Six canonical receiver files — named graph-state consumers retaining every behavioral assertion.
- `test/support/install_fixture_performance_test.exs` — focused isolation, scheduler, compatibility, diagnostic, and failure-direction tests.
- Phase 233/235.1 contracts — exact-once owner, timing boundary, fixed predicate, and historical-miss ratchets.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root dependency/build cache to generated fixtures | Reused bytes cross from the contributor checkout into isolated scenario build trees. |
| Immutable prepared variants to mutable scenarios | Shared setup fans out to private filesystem/database/runtime state. |
| Fixed runner to six receiver paths | One shell entry point owns exact coverage and exit propagation. |
| Diagnostic receipt to economics authority | Profiling data explains remediation but must not replace raw class predicates. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-235.1-06-01 | Tampering | root deps/build reuse | high | mitigate | Exact compatibility fingerprint, root-byte immutability checks, private copied build trees, and no writable symlink/hardlink sharing. |
| T-235.1-06-02 | Tampering | prepared snapshots | high | mitigate | Immutable variants, private copy-on-write scenario roots, unique partitions/ports, and mutation tests. |
| T-235.1-06-03 | Repudiation | omitted or duplicated coverage | high | mitigate | Fixed runner owns six exact live-tagged receiver paths once; Phase 233/235.1 contracts reject set/count drift. |
| T-235.1-06-04 | Denial of Service | subprocess fan-out | medium | mitigate | Exactly two workers, first-failure cancellation, child-status propagation, and retained failed checkout diagnostics. |
| T-235.1-06-05 | Spoofing | warm-cache performance claim | high | mitigate | Graph preparation, build seeding, copy fallback, and all receiver work remain between install markers; diagnostic sum is bounded by raw install duration. |
| T-235.1-06-06 | Elevation of Privilege | dynamic shell command | high | mitigate | Repository-owned fixed command/receiver arrays only; no caller executable, eval string, or arbitrary cleanup root. |
| T-235.1-06-07 | Tampering | diagnostic as authority | medium | mitigate | Diagnostic receipt carries no passing verdict; unchanged independent economics verifier remains sole predicate authority. |
| T-235.1-06-SC | Tampering | package supply chain | low | accept | No dependency, action, archive, or package installation is added. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Comparable one-owner library economics without terminal regression | 235.1-06 | COVERED | Timed DAG remediation precedes fresh evidence. |
| REQ | TEST-01 | Same-run parallel slow visibility remains intact | 235.1-06 | COVERED | Existing sole ordinary run/formatter is preserved by topology contracts. |
| REQ | TEST-02 | Two work classes meet unchanged comparability bound | 235.1-06 | COVERED | Full local gate uses the production verifier; PR authority remains Plan 04. |
| REQ | TEST-03 | Install/scaffold becomes non-dominant with exact-once coverage | 235.1-06 | COVERED | Setup deduplication plus six-path receiver ratchet. |
| RESEARCH | measured miss | 21,050ms/552,221ms diagnostic drives setup deduplication | 235.1-06 | COVERED | Summary retained as failed checkpoint input. |
| RESEARCH | prepared graph | One base, six named states, private checkouts, unique partitions | 235.1-06 | COVERED | Tasks 1–3. |
| RESEARCH | safe reuse | Root deps plus compatible private `_build/test` seeding | 235.1-06 | COVERED | Task 1 and focused tests. |
| RESEARCH | batching | Two-worker maximum, cancellation, retained diagnostics | 235.1-06 | COVERED | Task 3. |
| RESEARCH | fresh PR proof | Attempt-1 fixed-bound observation | 235.1-04 | COVERED | Remains downstream evidence authority by explicit topology. |
| CONTEXT | D-01–D-06 | One owner, same-run observations, PR coverage, fixed raw proof | 235.1-06 | COVERED | All implementation tasks cite constraints. |
| CONTEXT | D-07–D-08 | Evidence-first truth reconciliation | 235.1-04/05 | COVERED | Prohibited in Plan 06. |
| CONTEXT | D-09 | Preserve FAST-01/GATE-05 | 235.1-06 | COVERED | Phase contract and no protected artifact changes. |

<verification>
Run the hermetic fixed-runner contract, all six receiver files, fixture performance/isolation tests, Phase 233/235.1 contracts, complete `MIX_ENV=test mix ci`, and the unchanged production economics verifier. Any miss preserves diagnostics and blocks Plan 04 evidence capture.
</verification>

<success_criteria>
- One timed immutable graph replaces twelve fresh bootstraps without moving preparation outside the install interval.
- Private checkouts, private build manifests, unique partitions/ports, and exactly two workers keep scenarios deterministic.
- Every receiver and compile/runtime assertion remains present and executes exactly once under one owner.
- Diagnostic phase timing attributes the reduction while the unchanged class verifier alone decides pass/fail.
- A green local gate advances to Plan 04 for fresh retry-free PR evidence; no evidence or truth status is created here.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-06-SUMMARY.md` when done. If either predicate misses, record the diagnostic receipt and return a blocked summary without completing Plan 04.
</output>
