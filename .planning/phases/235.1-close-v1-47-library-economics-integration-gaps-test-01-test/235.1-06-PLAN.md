---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 06
type: execute
wave: 4
depends_on: [235.1-03]
files_modified:
  - test/support/install_fixture.ex
  - test/test_helper.exs
  - mix.exs
  - scripts/ci/install-golden.sh
  - scripts/ci/install-golden.test.sh
  - scripts/ci/library-economics.sh
  - scripts/ci/library-economics.test.sh
  - test/support/install_fixture_performance_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-01/D-05, one `MIX_ENV=test mix ci` owner routes one timed install leg through a fixed six-path runner; no receiver or suite owner is added, removed, or duplicated."
    - "Per D-06, one manifest-backed Phoenix source base derives six exact immutable variants and private copy-on-write scenario checkouts inside `install_start`/`install_end`."
    - "Compatible root dependency sources and `_build/test` bytes seed private build trees without shared writable Mix manifests, symlinks, hardlinks, databases, or ports."
    - "Exactly two workers execute independent scenarios; first failure cancels outstanding work, returns nonzero, and retains failed-checkout diagnostics."
    - "Diagnostic phase timing measures all preparation/copy work but never replaces or relaxes the fixed comparability and non-dominance predicates."
  artifacts:
    - path: "test/support/install_fixture.ex"
      provides: "Immutable six-variant DAG, private checkout API, safe cache reuse, isolation, two-worker runner, timings, and cleanup"
    - path: "scripts/ci/install-golden.sh"
      provides: "Fixed-command canonical receiver runner with prepared-mode activation and child-status propagation"
    - path: "scripts/ci/install-golden.test.sh"
      provides: "Hermetic exact-path, one-invocation, timing-boundary, flag, and failure contract"
    - path: "test/support/install_fixture_performance_test.exs"
      provides: "Fixture immutability, compatibility, isolation, concurrency, cancellation, and diagnostic mutation contract"
  key_links:
    - from: "mix ci"
      to: "scripts/ci/library-economics.sh"
      via: "the existing economics harness remains the sole contributor/CI entry point"
    - from: "library-economics.sh install_start/install_end"
      to: "mix ci.install_golden and install-golden.sh"
      via: "fixture preparation, copies, and receiver invocation remain inside the timed child"
    - from: "test/test_helper.exs"
      to: "InstallFixture"
      via: "fixed prepared-mode flag creates one graph and one validated after-suite cleanup"
    - from: "root deps/_build/test"
      to: "private scenario builds"
      via: "exact compatibility fingerprint and reflink/copy seeding into checkout-owned writable directories"
  prohibitions:
    - "MUST NOT prepare/restore install-only fixtures outside the install interval or omit copy fallback cost."
    - "MUST NOT share writable generated apps, Mix manifests/builds, databases, ports, symlinks, or hardlinks."
    - "MUST NOT use caller commands, eval strings, arbitrary cleanup roots, unrestricted concurrency, or more than two workers."
    - "MUST NOT change the workflow aggregate, receiver universe, fixed predicates, protected evidence, requirements, or audit status."
---

<objective>
Build the prepared-fixture and measured-runner infrastructure that replaces repeated Phoenix bootstrap/setup work without moving install cost outside the authoritative timing boundary.

Purpose: Establish the stable fixture API and runner contract consumed by Plan 07 receiver migration.
Output: Exact eight-file Pattern Map Plan A boundary with focused performance/security diagnostics.

Decision coverage: D-01 through D-06 and D-09 constrain infrastructure; D-07/D-08 remain evidence-gated downstream.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md
@test/support/install_fixture.ex
@test/test_helper.exs
@mix.exs
@scripts/ci/library-economics.sh
@scripts/ci/library-economics.test.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one private scenario through the immutable fixture DAG (D-05, D-06)</name>
  <files>test/support/install_fixture.ex, test/test_helper.exs, test/support/install_fixture_performance_test.exs</files>
  <read_first>
    - `235.1-PATTERNS.md` mappings for `install_fixture.ex`, `test_helper.exs`, and the performance test — extend fixed argv/status and conditional global setup; model adverse tests on the timing formatter contract.
    - `235.1-RESEARCH.md` Prepared-fixture graph and Reuse sections — use one base, six exact variants, private checkouts, and compatible private build copies.
    - Existing `InstallFixture.run_sigra_install/2`, `run_sigra_upgrade/2`, and `run_mix/2` — preserve `{:ok, stdout}` and immediate failure while propagating partition identity.
  </read_first>
  <behavior>
    - "One canonical Phoenix base derives exact variants default_installed, passkeys_standard, passkeys_nonstandard_app_js, no_passkeys, no_org_no_passkeys, and no_org_installed."
    - "Read-only consumers may use immutable snapshots; each rerun, upgrade, database, compile, or server action receives a private checkout/build plus unique partition and port."
    - "Root deps remain unchanged and `_build/test` reuse requires exact Elixir/OTP/lock/compiler fingerprint; reflink and measured copy fallback both produce private writable outputs."
    - "One after-suite owner deletes only its validated graph root; failed scenario paths remain in diagnostics."
  </behavior>
  <action>
    Following the concrete `InstallFixture` fixed-argv/status analog in `235.1-PATTERNS.md`, start RED in `install_fixture_performance_test.exs`, then add one manifest-backed graph under a validated `mktemp -d` root. Generate one canonical Phoenix source base and derive only the six mapped immutable variants. Store absolute snapshot path, normalized captured stdout where applicable, unique partition, unique port, compatibility fingerprint, and copy mode.

    Reuse root dependency sources read-only and seed compatible root `_build/test` bytes into checkout-owned build trees with `cp --reflink=auto` on Linux or measured normal copy. Reject shared writable manifests/builds, symlinks, hardlinks, partitions, or ports. Thread `MIX_TEST_PARTITION` through the three mapped subprocess helpers without changing their return shape. Following the Pattern Map conditional-bootstrap analog, prepare only under the fixed flag and register one root-validating after-suite cleanup. Prove one private default scenario end to end.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/support/install_fixture_performance_test.exs</automated>
    <fails_when>the command exits non-zero; ExUnit reports any failure or zero tests; base/variant cardinality differs; a snapshot is mutable; a checkout shares writable filesystem/build/database/port state; incompatible bytes are reused; copy fallback is unmeasured; status is masked; or cleanup escapes its root</fails_when>
  </verify>
  <done>The stable API produces six immutable states and isolated private scenarios with compatible reusable inputs and one cleanup owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the fixed six-receiver runner under the single owner (D-01, D-03, D-05)</name>
  <files>mix.exs, scripts/ci/install-golden.sh, scripts/ci/install-golden.test.sh</files>
  <read_first>
    - `235.1-PATTERNS.md` runner mappings — copy repository-owned arrays, captured status, and hermetic shadow-command mutations from `library-economics.sh` and `.test.sh`.
    - `mix.exs` aliases — preserve ordered `mix ci` ownership and make `ci.install_golden` invoke one runner.
    - `235.1-RESEARCH.md` tracked-file scope — the runner contains six canonical paths once and no caller executable/eval seam.
  </read_first>
  <behavior>
    - "The runner contains each canonical receiver path exactly once, enables prepared mode, invokes one Mix child, and returns its original status."
    - "`ci.install_golden` invokes the runner once; `mix ci` still invokes one economics harness and remains the only contributor/CI owner."
    - "Missing/duplicate/reordered path, missing flag, caller command, eval, signal, or status-masking mutations fail."
  </behavior>
  <action>
    Following the Pattern Map `library-economics.sh`/`.test.sh` analogs, start with hermetic shadowed-Mix tests, then implement `install-golden.sh` using one repository-owned array of the six paths. Set the fixed prepared flag, propagate child status/signals, and expose no caller executable, dynamic command, or eval. Replace only the inline `ci.install_golden` path list with one runner call; preserve every other `mix ci` leg and order.
  </action>
  <verify>
    <automated>bash scripts/ci/install-golden.test.sh &amp;&amp; shellcheck scripts/ci/install-golden.sh scripts/ci/install-golden.test.sh</automated>
    <fails_when>either command exits non-zero; the PASS banner is absent; ShellCheck reports a diagnostic; a path/flag/invocation is missing, duplicated, or reordered; child/signal failure is masked; a dynamic command seam exists; or `mix ci` ownership/order changes</fails_when>
  </verify>
  <done>One fixed runner owns the exact install leg under the unchanged single contributor/CI command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind preparation and diagnostics inside the install interval (D-02, D-06, D-09)</name>
  <files>scripts/ci/library-economics.sh, scripts/ci/library-economics.test.sh, test/support/install_fixture_performance_test.exs</files>
  <read_first>
    - `235.1-PATTERNS.md` Timing boundary and authority — keep graph construction, copies, fallback cost, and receivers between `install_start` and `install_end`.
    - Mapped `library-economics.sh` producer/tests — preserve first-failure/atomic receipt behavior and unchanged class totals.
    - Blocked `235.1-04-SUMMARY.md` — retain 21,050ms/552,221ms as failed diagnostics, never evidence or threshold authority.
  </read_first>
  <behavior>
    - "The prepared runner starts after install_start and returns before install_end; no warm setup/cache restore occurs earlier."
    - "Diagnostic keys phx_new, deps_get, baseline_compile, installer, receiver_compile_runtime, and checkout_copy are unique positive integers and sum <= raw install duration."
    - "Exactly two workers are enforced with first-failure cancellation, nonzero propagation, and retained failed checkout paths."
    - "Economics schema and fixed `max/min <= 2.0` plus `install_scaffold <= ordinary` arithmetic remain unchanged."
  </behavior>
  <action>
    Using the Pattern Map economics producer/test analog, keep the fixed runner inside existing timing markers and expand hermetic clock/command fixtures to reject pre-timing preparation/cache restoration. Emit an atomic diagnostic receipt from the fixture service with exact phase keys, positive milliseconds, copy mode, counts, unique isolation values, and failed paths; store no verdict. Expand the performance contract for omission/duplication, sum overflow, worker-count drift, cancellation failure, cleanup masking, and threshold/environment bypass. Diagnostics explain cost only; `verify-library-economics.sh` remains unchanged authority.
  </action>
  <verify>
    <automated>bash scripts/ci/library-economics.test.sh &amp;&amp; bash scripts/ci/install-golden.test.sh &amp;&amp; MIX_ENV=test mix test test/support/install_fixture_performance_test.exs</automated>
    <fails_when>any command exits non-zero; either PASS banner is absent; ExUnit reports failure or zero tests; preparation/copy falls outside the interval; a phase is absent/duplicated/non-positive; sum exceeds install duration; concurrency differs from two; cancellation/status/diagnostics are lost; or a fixed predicate/schema can be bypassed</fails_when>
  </verify>
  <done>The graph and all measurable cost sit inside the install class with deterministic diagnostics and unchanged admission predicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root cache to fixtures | Reused bytes cross into private builds. |
| Immutable variants to scenarios | Shared setup fans out to isolated state. |
| Fixed runner to Mix | Receiver input controls subprocess coverage/status. |
| Diagnostics to evidence | Profiling is explanatory but non-authoritative. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-235.1-06-01 | Tampering | root cache | high | mitigate | Exact fingerprint, root-byte immutability, private copies, no writable links. |
| T-235.1-06-02 | Tampering | snapshots | high | mitigate | Immutable variants, private builds, unique partition/port mutation tests. |
| T-235.1-06-03 | Repudiation | runner | high | mitigate | Six fixed paths once, fixed flag, child status/signal tests, no dynamic command. |
| T-235.1-06-04 | Denial of Service | concurrency | medium | mitigate | Exactly two workers, cancellation, retained failed checkout diagnostics. |
| T-235.1-06-05 | Spoofing | timing | high | mitigate | Marker-order tests include all preparation/copy cost. |
| T-235.1-06-06 | Elevation of Privilege | roots/commands | high | mitigate | Validated temp root, fixed argv, no caller executable/eval. |
| T-235.1-06-07 | Tampering | diagnostics | medium | mitigate | Non-verdict schema and unchanged independent verifier. |
| T-235.1-06-SC | Tampering | supply chain | low | accept | No package, dependency, action, or archive install. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Timed safe setup deduplication under one owner | 235.1-06 | COVERED | Exact Pattern Map Plan A boundary. |
| REQ | TEST-01/02/03 | Same-run owner, measurable classes, exact install work | 235.1-06 | COVERED | Timing and fixed-runner infrastructure. |
| RESEARCH | DAG/reuse/batching | One base, six variants, private copies, two workers | 235.1-06 | COVERED | Tasks 1–3. |
| PATTERNS | Plan A | Exact eight infrastructure files and concrete analogs | 235.1-06 | COVERED | No receiver ownership. |
| CONTEXT | D-01–D-06 | One owner, timing, every-PR exact coverage | 235.1-06 | COVERED | Infrastructure contracts. |
| CONTEXT | D-07–D-08 | Evidence-first truth | 235.1-04/05 | COVERED | Prohibited here. |
| CONTEXT | D-09 | Protected FAST/GATE evidence | 235.1-06 | COVERED | No protected edit. |

<verification>
Run both runner/economics shell suites and the focused fixture performance/security contract. This establishes the exact eight-file API boundary for Plan 07 without creating evidence.
</verification>

<success_criteria>
- One base/six variants/private checkouts replace repeated bootstrap infrastructure safely.
- Root cache reuse is compatible and produces private writable trees.
- One fixed runner and exact two-worker failure semantics are deterministic.
- All setup/copy cost remains timed and fixed predicates stay unchanged.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-06-SUMMARY.md` when done.
</output>
