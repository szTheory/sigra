---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 16
type: tdd
wave: 12
depends_on: [235.1-13]
files_modified:
  - test/support/install_fixture_performance_test.exs
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
  - test/support/ci/library_test_partitions.exs
  - test/support/ci/library_test_partitions_test.exs
  - scripts/ci/library-partitions.test.sh
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The diagnostics performance test derives its valid synthetic raw bound exactly from the receipt's measured positive phases, while production validation and a one-millisecond-too-low rejection remain unchanged."
    - "Every current ordinary path has one positive calibrated assignment cost independently replayed from three retained raw combined/timing receipt sets; no missing path enters LPT at zero cost."
    - "The two exhaustive ordinary partitions remain non-empty, exact-set, disjoint, scaffold-free, and inside the sole PR owner while three consecutive raw-duration pairs each satisfy the unchanged max/min <= 2.0 predicate."
    - "Plan 15 remains immutable blocked history with zero admitted samples and zero payloads; Plan 16 restarts collection rather than treating the failed run as calibration input."
  artifacts:
    - path: "test/support/install_fixture_performance_test.exs"
      provides: "Host-speed-independent synthetic diagnostic bound and exact low-bound rejection"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json"
      provides: "Bounded exact-schema calibration with three complete base64-embedded raw receipt sets and derived positive per-file costs"
    - path: "test/support/ci/library_test_partitions.exs"
      provides: "Fail-closed calibrated LPT oracle with no zero-cost missing-path fallback"
    - path: "test/support/ci/library_test_partitions_test.exs"
      provides: "Pure replay, aggregation, exact-universe, provenance, zero-cost, tie-starvation, and assignment contracts"
    - path: "scripts/ci/library-partitions.test.sh"
      provides: "Repeated real-run comparability and receipt-lifetime regression"
    - path: "test/sigra/planning/phase_235_1_library_economics_contract_test.exs"
      provides: "Fixture-bound, topology, replayability, and immutable-bound integration contract"
  key_links:
    - from: "InstallFixture.diagnostic_receipt/2 synthetic test setup"
      to: "InstallFixture.validate_diagnostics!/1"
      via: "exact sum of the no-raw receipt's actual positive phase durations"
      pattern: "raw_install_duration_ms|Enum.sum"
    - from: "235.1-PARTITION-CALIBRATION.json"
      to: "Sigra.CI.LibraryTestPartitions.build_partitions!/1"
      via: "decode/hash/size/schema/path replay of retained source receipts and exact current ordinary-universe equality"
      pattern: "calibrat|ordinary|time_us"
    - from: "scripts/ci/library-partitions.test.sh"
      to: "scripts/ci/verify-library-partitions.sh"
      via: "three fresh real receipts independently checked against the unchanged factor-of-two bound"
      pattern: "verify-library-partitions"
---

<objective>
Unblock source-complete partition calibration by correcting only the timing-sensitive synthetic diagnostics fixture, then execute Plan 15's replayable calibration and repeated local proof without weakening TEST-02 or changing CI/evidence topology.

Purpose: Plan 15 admitted zero samples because its required exhaustive source run deterministically failed an existing test that supplied a fixed 1,000ms raw ceiling after Plan 11 correctly increased measured preparation work. Production validation is correct; the synthetic valid fixture must derive its exact bound from the phases it actually contains.
Output: One narrow test-only TDD fix, the unchanged bounded replayable calibration design, adversarial oracle contracts, and three consecutive green real local partition runs.

Decision coverage: D-01 preserves the single PR owner; D-02 preserves two exhaustive partitions and the unchanged raw bound; D-03/D-04 retain same-run formatter timing and receipt ownership; D-05/D-06 leave the exact-six non-PR route and predicates unchanged; D-07 creates no live truth; D-08 reserves reconciliation for Plan 05; D-09 verifies FAST-01/GATE-05 without rewriting them.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
@/Users/jon/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-13-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-15-SUMMARY.md
@.planning/phases/233-library-suite-economics/233-EVIDENCE.json
@test/support/install_fixture.ex
@test/support/install_fixture_performance_test.exs
@test/support/ci/library_test_partitions.exs
@test/support/ci/library_test_partitions_test.exs
@scripts/ci/library-partitions.sh
@scripts/ci/library-partitions.test.sh
@scripts/ci/verify-library-partitions.sh
@scripts/ci/verify-library-partitions.test.sh
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Derive the synthetic diagnostic bound from its actual phases (D-05, D-06, D-09)</name>
  <files>test/support/install_fixture_performance_test.exs</files>
  <behavior>
    - RED: preserve Plan 15's deterministic failure as the baseline showing that literal 1,000ms is not a valid host-independent bound for a real prepared graph.
    - GREEN: call `diagnostic_receipt(graph)` without a raw value, assert the raw key is absent, sum the exact six positive integer phase durations, and add that exact sum as `raw_install_duration_ms`; validation succeeds at equality.
    - GREEN: the same receipt with `raw_install_duration_ms` set to the derived sum minus one is rejected even though that lower value remains positive.
    - Production `diagnostic_receipt/2`, `validate_diagnostics!/1`, phase membership, positivity, count, worker, partition, port, copy-mode, failed-path, and no-verdict contracts remain unchanged.
  </behavior>
  <action>Per D-05/D-06, change only the existing test named `diagnostics require exact positive phases bounded by raw install duration`; do not edit `test/support/install_fixture.ex` or any production validation rule. Treat the exact Plan 15 failure and isolated reproduction in summary `2d854758` as the retained RED baseline; rerun the focused line once before editing and require the same validator rejection. For GREEN, construct the receipt first with `InstallFixture.diagnostic_receipt(graph)` so it has no producer-supplied raw key, retain the existing exact-six phase-key and positive-integer assertions, calculate `phase_sum` from those actual receipt phases, assert it is positive, then create the valid fixture with exactly `Map.put(receipt_without_raw, :raw_install_duration_ms, phase_sum)`. Keep the successful equality-bound assertion and replace the arbitrary `1` rejection with the precise positive lower boundary `phase_sum - 1`; six positive phases guarantee that value remains positive. Preserve the zero-phase, unexpected-phase, variant/worker, and producer-verdict checks. Do not add slack, a multiplier, a host threshold, a retry, or a production escape hatch. Commit the test-only GREEN correction separately and record the retained RED command/output in the Plan 16 summary before collecting any calibration sample.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/install_fixture_performance_test.exs:610 &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/install_fixture_performance_test.exs</automated>
    <fails_when>Either command exits nonzero or reports zero tests; the valid raw bound differs from the exact actual phase sum; the one-lower positive bound is accepted; production code changes; or any existing diagnostics invariant is removed.</fails_when>
  </verify>
  <done>The focused diagnostic contract and whole performance-test source pass with an exact measured-phase equality bound and exact low-bound rejection, while production validation bytes remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restart source collection and build the replayable positive calibration (D-01, D-02, D-03)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json, test/support/ci/library_test_partitions.exs, test/support/ci/library_test_partitions_test.exs</files>
  <behavior>
    - RED: two or more uncalibrated or zero-cost paths in either input order are rejected instead of repeatedly accumulating in partition 1.
    - RED: malformed payload/base64/hash/count/schema/path linkage, a missing/stale/duplicate/scaffold/non-test path, wrong provenance, over-limit bytes, mismatched universe, or altered derived cost fails closed.
    - RED: each path's raw sample cost is independently summed from decoded timing `.tests[].time_us`, effective cost is `max(1, raw_sum)`, and assignment cost is the deterministic median of exactly three samples; row/sample order cannot change the result.
    - GREEN: LPT consumes only exact positive derived current costs and produces two non-empty lexical manifests whose disjoint union is the current ordinary universe.
  </behavior>
  <action>Begin only after Task 1's focused and whole-source commands pass and its isolated commit exists. Record that fixture-fix commit as the sample tree; verify Plan 13 implementation commit `997a1bb5` still owns the runner/oracle/formatter bytes, Plan 13 summary `265b0874` and blocked Plan 15 summary `2d854758` are present, `235.1-PARTITION-CALIBRATION.json` is absent, and Plan 15 admitted zero samples/payloads. Restart collection from zero: run the unchanged `ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test bash scripts/ci/library-partitions.sh` exactly three times from one validated `mktemp -d`, and after each successful pair retain the exact combined and both timing receipt bytes before `/tmp` is overwritten. Admit only regular non-symlinks with successful children, exact membership, recorded byte count/SHA-256, each decoded payload at most 1,048,576 bytes, and no bytes from Plan 15's failed run.

Carry forward Plan 15's schema unchanged. Create `sigra.library-partition-calibration/v1` with exact top-level keys `schema_version`, `source`, `ordinary_universe`, `limits`, `samples`, and `derived_costs`, capped at 8,388,608 bytes. `source` has exactly `implementation_commit`, `blocked_summary_commit`, `collection_command`, `sample_count`, and `aggregation`; bind the implementation to Task 1's commit, record blocked Plan 15 summary `2d854758`, the literal command, three, and `median(max(1,sum(time_us)))`. `ordinary_universe` has exact lexical `paths`, `count`, and `newline_manifest_sha256`. `limits` has exact artifact/combined/timing byte caps, sample count three, one combined and two timings per sample, and partitions `[1,2]`. `derived_costs` is a lexical exact-universe array whose rows have only `path` and positive-integer `time_us`.

For ordinals `[1,2,3]`, retain all nine exact original payloads as canonical base64. Each sample has only `ordinal`, `combined`, and `timings`; each embedded receipt has only `role`, `source_path`, `byte_count`, `sha256`, and `payload_base64`. Require roles `combined`, `timing-1`, `timing-2`; fixed paths `/tmp/sigra-library-partitions.json`, `/tmp/sigra-library-1-timings.json`, `/tmp/sigra-library-2-timings.json`; timing order `[1,2]`; lowercase 64-hex hashes; exact decoded byte counts; and no duplicate/extra/missing keys at any level. Validate the full existing decoded receipt schemas, arithmetic, outcomes, IDs, path syntax, unique tests, and manifests. Link each combined partition manifest to the matching timing receipt's unique `.tests[].file` set and its union to the ordinary universe exactly once with no scaffold path.

Start RED only after all three immutable source triples are admitted. Do not store self-asserted sample sums: derive raw per-file sums from retained timing rows, apply `max(1, raw_sum)`, and independently recompute each three-sample median and every `derived_costs` row before LPT. Reject repeated zero-cost missing paths and reversed order adversarially. Remove the default zero-cost append behavior while preserving the two-manifest API and deterministic descending-cost/lexical LPT. No handwritten allocation, opaque hash, producer pass boolean, environment/threshold override, live evidence, or GitHub action is permitted. Commit RED and GREEN separately.</action>
  <verify>
    <automated>test "$(wc -c &lt; .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json | tr -d ' ')" -le 8388608 &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
    <fails_when>The command exits nonzero or reports zero tests; any payload/hash/size/schema/path/raw-sum/median/provenance mutation passes; a Plan 15 byte is admitted; a zero/missing/stale/duplicate/scaffold cost is accepted; or manifests are empty, overlapping, non-lexical, or not exactly exhaustive.</fails_when>
  </verify>
  <done>Exactly three new successful source triples are retained and replayable; the default oracle uses only independently recomputed positive current costs and emits exact-set, disjoint, lexical, non-empty, scaffold-free manifests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove whole-source and repeated real comparability (D-02, D-04, D-07, D-09)</name>
  <files>scripts/ci/library-partitions.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - The whole ordinary source, including `install_fixture_performance_test.exs`, passes before repeated calibration validation begins.
    - Three consecutive real executions run partition 1 then partition 2; both children pass, both timing receipts survive partition 2 teardown, and each combined receipt independently passes exact-set/disjointness and max/min <= 2.0.
    - Mutating fixture-bound derivation, any retained payload/replay link, the unchanged factor-of-two arithmetic, sequential owner, formatter pair, or receipt lifetime makes the focused chain fail.
    - Plans 13 and 15 remain negative history and no local success writes PR/scaffold/validation evidence.
  </behavior>
  <action>First rerun the complete source command and require the formerly failing diagnostics test plus both partitions to pass. Then carry forward Plan 15's validation unchanged: extend the shell integration contract after fast fake-child cases to run the real fixed runner exactly three consecutive times. After each run require both regular timing receipts after partition 2 teardown, copy/digest the combined and timing receipts into its private root, invoke the independent verifier, and require the unchanged integer ratio independently. Do not average, retry a red validation, or retune after validation begins.

Expand the phase contract to pin the test-only fixture derivation, unchanged production validator bytes, blocked Plan 15 summary `2d854758` with zero samples/payloads, new sample-tree commit, bounded embedded-payload artifact, complete byte/hash/schema/path replay, exact-universe loader, runner/formatter topology, and `maximum * 1000 &lt;= minimum * 2000`. Mutation-test exact-sum versus inflated/lower bounds, production-validator drift, payload bytes/base64/count/hash/schema/linkage, receipt arithmetic/outcomes, direct sum/median recomputation, zero fallback, size ceilings, exact set/disjointness, and no-live-evidence boundary. Run focused formatter/partition/Phase 233/235.1 and protected FAST-01/GATE-05 checks. If the whole source or any repetition is red, retain exact local diagnostics in the Plan 16 summary and block before Plan 14; do not weaken the bound, omit/move/duplicate a test, edit topology, or dispatch GitHub.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test bash scripts/ci/library-partitions.sh &amp;&amp; bash scripts/ci/library-partitions.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/install_fixture_performance_test.exs test/support/ci/ex_unit_timing_formatter_test.exs test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any command exits nonzero or any PASS/test signal is absent; the prior diagnostics failure recurs; fewer or more than three validation executions occur; a child/receipt is missing; any pair exceeds 2.0; a red result is averaged/retried; protected bytes/topology drift; or live evidence appears.</fails_when>
  </verify>
  <done>The fixed diagnostic test and whole source pass, three consecutive fresh local partition pairs independently meet the unchanged <=2.0 bound with both receipts intact, all adversarial/protected checks pass, and no live evidence action occurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Real graph timings to synthetic valid fixture | Host-dependent phase duration must determine the exact test bound without weakening production validation. |
| Local timing receipts to tracked calibration | Mutable `/tmp` measurements must be retained byte-for-byte, digest-bound, exhaustive, and independently recomputable. |
| Calibration to partition oracle | A stale, incomplete, zero, or forged cost can silently skew exhaustive manifests. |
| Partition manifests to TEST-02 claim | Passing tests alone are insufficient; every raw-duration pair must satisfy the unchanged verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-16-01 | Tampering | synthetic diagnostic bound | high | mitigate | Test derives equality from exact positive phases and rejects sum-minus-one; production validator cannot change. |
| T-235.1-16-02 | Spoofing | calibration provenance | high | mitigate | Exact fixture-fix/sample commit, blocked-summary identity, and three complete embedded receipt sets replay from decoded bytes. |
| T-235.1-16-03 | Tampering | retained receipts/cost universe | high | mitigate | Exact schemas/linkage plus direct raw-sum/median recomputation; stale, duplicate, scaffold, or nonpositive costs hard-fail. |
| T-235.1-16-04 | Repudiation | blocked history/comparability | high | mitigate | Plan 15 zero-admission summary remains immutable; three new raw pairs are independently verified without averaging/retry. |
| T-235.1-16-05 | Denial of Service | embedded receipts/LPT ties | medium | mitigate | 8 MiB artifact and 1 MiB payload caps, strict counts, positive costs, and repeated-zero rejection. |
| T-235.1-16-06 | Elevation of Privilege | topology escape | high | mitigate | Phase contract pins one PR owner, two exhaustive sequential manifests, parallel ExUnit, formatter paths, and unchanged bound. |
| T-235.1-16-SC | Tampering | package supply chain | low | accept | No package, dependency, or action installation/change is authorized. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Comparable exhaustive ordinary partitions without protected regression | 16 → 14 → 05 | COVERED | Narrow fixture correction precedes calibrated assignment, live proof, and reconciliation. |
| REQ | TEST-01 | Same-run parallel slow-test visibility | 13, 14 | COVERED | Plan 13 repair retained; Plan 14 captures it. |
| REQ | TEST-02 | Two comparable exhaustive ordinary partitions | 16, 14 | COVERED | Exact-bound fixture, positive current calibration, repeated local proof, then fresh PR proof. |
| REQ | TEST-03 | Exact-six non-PR scaffold coverage | 13, 14 | COVERED | Plan 13 repair retained; Plan 14 captures it. |
| RESEARCH | — | Preserve <=2.0 and remediate measured blockers | 16 | COVERED | Exact synthetic bound plus replayable full-current median calibration; no relaxation. |
| RESEARCH | — | Exact-set/disjoint/scaffold-free fail-closed manifests | 16 | COVERED | Calibration and output equal the current ordinary universe. |
| CONTEXT | D-01–D-06 | Preserve owner, formatters, routes, and predicates | 16 | COVERED | Test-only predecessor fix and oracle assignment repair leave topology intact. |
| CONTEXT | D-07–D-09 | Evidence-first truth and protected invariants | 14, 05 | COVERED | Plan 16 creates no live evidence and runs both protected verifiers. |
| CONTEXT | Deferred ideas | Unrelated CI/auth/admin/release work | — | EXCLUDED | Explicitly deferred and outside this recovery. |

<verification>
Run the focused and whole diagnostics source first, collect exactly three new successful raw triples, run pure replay/oracle mutations, execute exactly three fresh real local partition validations, and run focused Phase 233/235.1 plus protected verifiers. Do not run GitHub workflows or create live evidence.
</verification>

<success_criteria>
- The only predecessor change is the synthetic diagnostics test: equality uses the exact actual positive phase sum and sum-minus-one remains rejected; production validation is byte-identical.
- Plan 15 remains blocked with zero samples/payloads; Plan 16 retains exactly three new complete source samples and all nine bounded payloads with exact path/count/hash metadata.
- The loader independently validates and links every receipt, recomputes raw sums, `max(1,sum)`, and three-sample medians, and rejects every provenance/universe/derived mutation.
- Three consecutive real local runs preserve both receipts and independently pass the unchanged <=2.0 verifier.
- No test path, PR owner, aggregate topology, formatter behavior, live evidence, FAST-01 value, or GATE-05 ledger is weakened or rewritten.
- Plan 14 becomes reachable only after Plan 16 completes green.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-16-SUMMARY.md` when done, including the fixture RED/GREEN commit, sample-tree commit, calibration commits, all three validation duration pairs, Plan 15 zero-admission preservation, and confirmation that no GitHub action occurred.
</output>
