---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 12
type: execute
wave: 10
depends_on: [235.1-11]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
  - scripts/ci/verify-library-routing-evidence.sh
  - scripts/ci/verify-library-routing-evidence.test.sh
  - scripts/ci/verify-library-validation-run.sh
  - scripts/ci/verify-library-validation-run.test.sh
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A newly budgeted fresh attempt-1 PR/dispatch pair at one remediated implementation SHA proves both selected routes and all five authoritative artifacts."
    - "At most three new candidate implementation SHAs may each receive one attempt-1 PR run and one attempt-1 dispatch; no run is rerun and every failed candidate remains negative history."
    - "After admissible capture receipts are committed, one distinct attempt-1 PR/dispatch pair at the exact evidence SHA validates both tracked evidence routes."
    - "Plan 10 and runs 34466384009, 34466384470, 34467186749, 34467189602, 34468109536, and 34468110161 remain immutable, non-authoritative failure history."
  artifacts:
    - path: 235.1-PR-EVIDENCE.json
      provides: fresh PR partition provenance and three artifact digests
    - path: 235.1-SCAFFOLD-EVIDENCE.json
      provides: fresh dispatch exact-six provenance and two artifact digests
    - path: 235.1-VALIDATION-RUN.json
      provides: distinct evidence-SHA PR and dispatch validation
  key_links:
    - from: remediated implementation SHA
      to: PR and dispatch capture
      via: same exact head SHA, attempt 1, selected-job success, and run-qualified artifacts
    - from: committed capture receipts
      to: validation runs
      via: distinct run IDs and exact evidence commit SHA
    - from: GitHub observation
      to: durable JSON
      via: one watcher, one structured summary, digest-bound downloads, and offline verifier replay
---

<objective>
Capture and validate live evidence for the remediated separated routes under a newly explicit finite attempt budget.

Purpose: Replace the missing authority from blocked Plan 10 without rerolling or rewriting its failures, then provide Plan 05 with independently verified PR and non-PR evidence.
Output: Fresh PR/scaffold capture receipts, one dual-route validation receipt, and budget-aware offline admission contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-10-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-10-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-11-SUMMARY.md
@scripts/ci/verify-library-partitions.sh
@scripts/ci/verify-library-install-golden.sh
@scripts/ci/verify-library-routing-evidence.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Capture one admissible remediated PR/dispatch pair within three candidate SHAs (D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, scripts/ci/verify-library-routing-evidence.sh, scripts/ci/verify-library-routing-evidence.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - Each candidate is one exact implementation SHA with exactly one automatic pull_request run and exactly one `workflow_dispatch` run; both must be run attempt 1 and neither may be rerun.
    - No more than three new candidate SHAs/pairs are permitted after Plan 11. A deterministic failure may be diagnosed and repaired before the next candidate; a third failed pair blocks and requires a new plan.
    - PR admission requires successful selected owner and aggregate plus three complete artifacts passing the partition verifier; dispatch admission requires successful selected install owner plus two complete artifacts passing the install verifier.
    - Historical Plan 10 run IDs remain required negative entries and cannot satisfy, substitute for, or reduce the new budget.
  </behavior>
  <action>Begin with the complete local Plan 11 verification chain. Reuse PR #234 and the scoped evidence branch/worktree only after confirming their exact refs and clean ownership boundary; push the remediated implementation SHA. For candidate 1, resolve the one automatic PR run and dispatch `gh workflow run CI --repo szTheory/sigra --ref ci/phase-235-1-evidence`, binding both to the same exact SHA. Before each long watch, run `gh api rate_limit --jq '.resources.core | {remaining,reset}'`; if remaining is 250 or lower, make no further polling request until reset. Treat HTTP 403/429 as a hard stop until GitHub's reported reset/retry time. Give each run at most one watcher using exactly `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; after completion fetch its structured summary exactly once using the locally supported `attempt` field, and fetch failed logs only when conclusion is failure. Never poll a run from a second agent or rerun an attempt.

If a selected route fails deterministically, retain its run/SHA/job/artifact diagnosis as negative history, make only an in-scope repair with local regression coverage, and advance to the next candidate SHA/pair. Transient infrastructure failure may consume the candidate but is never waived or relabeled. Stop immediately after the first admissible pair or after candidate 3 fails; do not create a fourth candidate. For the admitted pair, download only exact run/attempt-qualified artifacts into validated `mktemp -d` roots, run both production verifiers, compute local SHA-256, and atomically create the same exact Plan 10 schemas and key contracts. Extend the routing verifier/test so its supersession/history object requires all six Plan 10 run IDs and an ordered `recovery_attempts` array of one to three new candidate pairs, with exactly one final admitted pair and every earlier new pair explicitly failed; reject missing, reordered, duplicated, rerun, cross-SHA, over-budget, or history-substitution mutations. Preserve the failed fixed-bound economics ledger and the prohibition on old non-dominance predicates.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; jq -e '.schema_version == "sigra.library-partitions-evidence/v1" and .run.event == "pull_request" and .run.attempt == 1 and .owner.conclusion == "success" and .aggregate.conclusion == "success" and (.artifacts | length) == 3' .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json &amp;&amp; jq -e '.schema_version == "sigra.library-install-golden-evidence/v1" and .run.event == "workflow_dispatch" and .run.attempt == 1 and .job.conclusion == "success" and (.artifacts | length) == 2 and (.receivers | length) == 6' .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json</automated>
  </verify>
  <done>One of at most three new attempt-1 candidate pairs proves both routes at one implementation SHA, with every failed candidate and all six Plan 10 runs retained as negative history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate committed remediated evidence on one distinct attempt-1 pair (D-07–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json, scripts/ci/verify-library-validation-run.sh, scripts/ci/verify-library-validation-run.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - Validation starts only after both Task 1 receipts and their verifier are committed; one automatic PR run and one dispatch run execute that exact evidence commit.
    - Both validation runs are attempt 1 and are distinct from every historical, recovery-capture, and each other run ID.
    - PR selected owner/aggregate and dispatch selected install owner execute successfully; no rerun or replacement validation pair is allowed without changing evidence bytes and replanning.
  </behavior>
  <action>Commit Task 1 evidence and verifier together and push that exact evidence commit. Observe the one automatically triggered PR run and dispatch one workflow_dispatch run for the exact commit, applying the identical rate preflight, hard-stop, single-watcher, one-summary, and failed-log-only rules from Task 1. Do not rerun a validation run. Atomically write `235.1-VALIDATION-RUN.json` using Plan 10's `sigra.library-routing-validation/v1` exact key contract, ordered PR/dispatch entries, exact evidence SHA, raw selected job states, and capture-run distinctness. Expand the existing offline validation verifier/test to cross-check the recovery attempt ledger, all historical/new run-ID disjointness, exact SHA equality, attempt 1, route identity, selected job execution/success, exact command shapes, and absence of stored pass authority. If either validation route fails, retain the diagnostics, create no passing validation receipt, and block for a new plan rather than consuming another hidden attempt.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
  </verify>
  <done>One distinct attempt-1 PR/dispatch pair proves the exact committed evidence bytes and both current routes without human UAT or rerun selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub API to tracked receipts | Mutable external run data becomes milestone evidence. |
| Candidate attempts to selection | Multiple observations could enable rerolling or omission of failures. |
| Downloaded artifacts to verdict | Untrusted bytes require exact run binding, digests, and independent replay. |
| Evidence commit to validation | Validation must execute the exact tracked bytes it claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-12-01 | Spoofing | run provenance | high | mitigate | Exact repository/event/attempt/SHA/job/artifact identities. |
| T-235.1-12-02 | Tampering | artifact bytes | high | mitigate | Run-qualified downloads, local SHA-256, and production verifier replay. |
| T-235.1-12-03 | Repudiation | failed attempts | high | mitigate | Ordered required history, explicit maximum three, and mutation-tested no-omission contract. |
| T-235.1-12-04 | Elevation of Privilege | selective reroll | high | mitigate | One attempt-1 run per route/candidate, no reruns, final validation pair fixed. |
| T-235.1-12-05 | Denial of Service | GitHub polling | medium | mitigate | Core-budget preflight, hard 403/429 stop, and one 60-second watcher per run. |
| T-235.1-12-SC | Tampering | package supply chain | low | accept | Evidence capture changes no dependency declarations or packages. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Fresh dual-route machine authority without protected regression | 12 | COVERED | Capture plus distinct validation. |
| REQ | TEST-01 | Same-run parallel timing observed on PR | 12 | COVERED | Selected owner and both per-test artifacts. |
| REQ | TEST-02 | Comparable exhaustive partitions observed on PR | 12 | COVERED | Raw duration and exact-set recomputation. |
| REQ | TEST-03 | Exact-six successful non-PR scaffold execution | 12 | COVERED | Selected dispatch job and both required artifacts. |
| RESEARCH | — | Rate-safe attempt-1 two-stage evidence | 12 | COVERED | Exact AGENTS watcher/rate rules and distinct validation. |
| CONTEXT | D-07, D-08, D-09 | Evidence-first truth and immutable terminal history | 12 | COVERED | No truth docs change; protected verifiers run. |
| CONTEXT | D-02, D-05, D-06 | Empirical fallback route authority | 12 | COVERED | Current separated predicates only; old failed predicates remain history. |

<verification>
Run both offline mutation suites, focused phase contracts, exact schema probes, and protected verifiers over fresh attempt-1 captures and the distinct evidence-SHA validation pair.
</verification>

<success_criteria>
- At most three newly budgeted candidate SHA pairs are observable and none is rerun or omitted.
- One admitted pair supplies complete verified PR and scaffold receipts at the same implementation SHA.
- One distinct attempt-1 pair validates the exact committed evidence SHA.
- Plan 10, its six failed runs, fixed-bound misses, FAST-01, and GATE-05 remain intact.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-12-SUMMARY.md` when done.
</output>
