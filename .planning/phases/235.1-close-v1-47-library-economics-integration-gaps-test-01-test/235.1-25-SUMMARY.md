---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 25
subsystem: ci-evidence
tags: [bash-portability, routing-authority, exact-tree, deterministic-block]
status: halted
dependency_graph:
  requires: [235.1-24, 235.1-22, 235.1-21]
  provides: [bash3-portable-runner, refreshed-seven-pin-authority, exact-tree-candidate-diagnostic]
  affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
tech_stack:
  added: []
  patterns: [nul-safe-discovery, quoted-array-argv, ordered-read-loop, per-pin-mutation]
key_files:
  created:
    - scripts/ci/library-partitions-portability.test.sh
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-25-SUMMARY.md
  modified:
    - scripts/ci/library-partitions.sh
    - scripts/ci/verify-library-routing-evidence.sh
    - scripts/ci/verify-library-routing-evidence.test.sh
decisions:
  - Replace mapfile with an ordered Bash-3-compatible read loop while retaining quoted array argv and NUL-safe tracked discovery.
  - Stop after the sole permitted timed self-test exposed a Bash-5 nounset incompatibility; do not repair, retry, substitute, push, or seek alternate evidence.
requirements-completed: []
metrics:
  duration: 14m
  completed: 2026-09-10
actuals:
  tokens: 2700
  tasks: 1
  commits: 2
---

# Phase 235.1 Plan 25: Bash Portability Recovery Summary

The runner became portable to macOS Bash 3.2 with exact argv preservation and refreshed fail-closed authority, but the one permitted timed self-test exposed a separate Bash 5 `set -u` empty-array failure before any local pair or GitHub action.

## Task 1 — Complete

RED commit `4ef1fc79e49cae535ce03e6c37635c53ca4d3730` introduced an untimed portability/adverse test. Under `/bin/bash` 3.2.57 it failed specifically at the old runner's `mapfile` call:

```text
scripts/ci/library-partitions.sh: line 78: mapfile: command not found
library-partitions: FAIL: partition 1 is empty
```

GREEN commit `820ef36f0d0641870d87cbeabf44923f2f5ed7f6` has tree `f34d606bf7a16b6e024f74fbbf803953d71a500c`. It replaced `mapfile` with `while IFS= read -r path || [[ -n "$path" ]]` plus ordered array append, while preserving the NUL-safe `git ls-files -z` oracle and quoted `mix test "${paths[@]}"` boundary.

The portability suite proved:

- `/bin/bash` 3.2 executes two partitions sequentially;
- paths containing spaces and literal `[abc]*` glob characters remain separate unchanged argv entries in lexical oracle order;
- an empty first manifest fails before any child test process;
- combined receipt schema, universe/partition paths, manifest SHA-256 values, conclusions, exit statuses, and normalized timing paths retain their prior meaning;
- no `mapfile`, `readarray`, `eval`, word splitting, glob discovery, or newline-based Git parsing enters the runner.

The repaired runner SHA-256 is `605f2a2c351bffbde446e8330a22675ae2985377441a7361911bed734e5fdb60`. Exactly that runner entry changed in the ordered seven-entry `CURRENT_ACTIVE_AUTHORITY`; the other six active pins and four historical Plan 18 literals remained byte-exact. Pristine preflight and isolated one-byte mutation of each of all seven active paths passed. The portability test, verifier, and verifier test remain outside the pinned authority set.

## Frozen Calibration and Source Proof

Untimed replay retained all Plan 22 facts:

| Fact | Exact value |
| --- | --- |
| Ordinary membership | 225 exhaustive, disjoint paths |
| Partition counts | 111 / 114 |
| Derived weights | 22,878,707us / 22,878,706us |
| Newline manifest SHA-256 | `8e5c31efc1ca73ca05939c70b423e579d86944ecfc49b693fb93d6e2c356aabc` / `1740e9fee3f3670d64ad8a843dda90935797b7204640e83e979cb18d212e044a` |
| NUL universe SHA-256 | `78a70051f7aa55f91de5849327b86dca7915028b15d263230efb5c217b1907f7` |
| Source-index SHA-256 | `f4ffd881ff99ce65bc1bfef576188de24ed99064f0e7132306d0831fe8dc4485` |
| Calibration SHA-256 | `f54316873b2e66cb39277e063accf1168736eb9d3f74ee6c61183a845244e99b` |
| Manifest SHA-256 | `1168cda99d40277253a0eb4a1c2e7ce3debdb3f92d77f3ca88c304690539e424` |
| Pre-evidence audit SHA-256 | `b9c4c6614c776d8e8c192b2bfa941cb024244289e850066a4869f3120b4588dd` |

Every source-index byte count and SHA-256 matched the current 225 files. The focused loader/source/lifecycle suite passed 35 tests. No calibration collection, recalibration, or Plan 22 validation was run. The Task 1 execution diff from plan authority `55949522` is exactly the four declared files, and the unrelated dirty path set and cached index remained unchanged.

## Task 2 — Deterministic Hard Stop

PR #234 remained open at exact evidence head `47f9578fc97edc63603953d446e79fac884326c3`.

Two independent `git commit-tree` invocations used Task 1's frozen tree, the old evidence head as sole parent, fixed message `Phase 235.1 Plan 25 exact-tree evidence candidate`, and Task 1 identity/timestamps. Both produced:

- Candidate: `634c4d5a92a3b0883174a1c1baa251ca906777f3`
- Tree: `f34d606bf7a16b6e024f74fbbf803953d71a500c`
- Sole parent: `47f9578fc97edc63603953d446e79fac884326c3`
- Recursive diff against Task 1: empty

In a detached candidate worktree, the portability suite, routing adverse suite, seven-pin preflight, validation adverse suite, independent partition-verifier suite, focused five-file contract suite (74 tests), FAST-01 source-complete verifier, terminal-ratification verifier, Actionlint, formatting, and tracked-cleanliness checks all passed. The first bare shell verifier initially lacked the pinned Erlang selection because of the intentionally dirty project `.tool-versions`; rerunning only the not-yet-completed untimed gates with `ASDF_ERLANG_VERSION=28.4.1` followed the established Plan 22/24 pattern and changed no tracked byte.

The command `bash scripts/ci/library-partitions.test.sh` was then invoked exactly once. It failed before producing its first real candidate-local pair:

```text
scripts/ci/library-partitions.sh: line 79: paths: unbound variable
library-partitions: FAIL: partition 1 timing receipt is not a regular non-symlink file
```

The failure occurs under Bash 5 with `set -u`: `local -a paths` declares but does not initialize the array before `${#paths[@]}` is evaluated. The Bash 3 execution accepted that expression, so the new test did not expose the cross-version nounset edge before the single-use gate.

Plan 25 forbids repair, retry, environment substitution, tuning, recalibration, alternate candidate construction, or GitHub action after any failure of the sole timed invocation. Execution therefore stopped durably.

## Budget and External-Action Accounting

- `bash scripts/ci/library-partitions.test.sh`: exactly one invocation total.
- Successful new real candidate-local pairs: zero of three.
- Pushes and workflow dispatches: zero.
- CI watchers, structured summaries, failed-log fetches, and artifact downloads: zero.
- PR, scaffold, and validation receipts: absent and unwritten.
- The audit remains the committed 21/24 pre-evidence document.
- The ten-run negative ledger and digest `847f97542bd110be6022771098a54db9b4d8b0f75db2c32b923ee81834acf962` remain unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking environment] Supplied the pinned Erlang version to unfinished untimed shell checks**

- **Found during:** Task 2 untimed candidate preflight
- **Issue:** Nested `mix` could not select Erlang from the unrelated dirty `.tool-versions`.
- **Fix:** Set `ASDF_ERLANG_VERSION=28.4.1` and `MIX_ENV=test` only for the not-yet-completed untimed commands.
- **Files modified:** None.
- **Commit:** None.

No repair was attempted after the timed self-test invocation failed.

## TDD Gate Compliance

- RED: `4ef1fc79`.
- GREEN: `820ef36f`.
- The tracer's untimed end-to-end verification passed before candidate expansion.

## Known Stubs

None.

## Threat Review

No network endpoint, authentication path, schema, dependency, package, calibration, workflow, or ordinary-source surface changed. Path boundaries were tested with adverse names, all current pins remained fail-closed, unrelated worktree state stayed untouched, and no untrusted GitHub artifact crossed into tracked authority.

## Self-Check: PASSED

- Both Task 1 commits and the frozen tree resolve.
- The exact candidate commit/tree/sole-parent claims replay from Git.
- The repaired runner digest, exact seven-entry authority, all seven mutation outcomes, audit/calibration/source invariants, and clean candidate checks were observed.
- The single timed invocation stopped before push or receipt creation, and no timed retry ran.
