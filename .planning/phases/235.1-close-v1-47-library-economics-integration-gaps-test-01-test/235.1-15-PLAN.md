---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 15
type: tdd
wave: 12
depends_on: [235.1-13]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
  - test/support/ci/library_test_partitions.exs
  - test/support/ci/library_test_partitions_test.exs
  - scripts/ci/library-partitions.test.sh
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-02]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The two exhaustive ordinary partitions remain non-empty, exact-set, disjoint, scaffold-free, and inside the sole PR owner while their unchanged raw-duration ratio is at most 2.0."
    - "Every current ordinary path has one positive calibrated assignment cost replayed directly from three retained raw combined/timing receipt sets; no missing path can enter LPT with zero cost or win repeated ties into one partition."
    - "Three consecutive real local partition runs at the Plan 15 tree preserve both timing receipts, pass all tests, and independently satisfy the unchanged max/min <= 2.0 predicate."
    - "Plan 13 remains blocked negative history: its three repairs are retained as implementation authority, but its 9,103/2,430ms and 10,963/4,242ms measurements never become passing evidence."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json"
      provides: "Bounded exact-schema calibration with three complete base64-embedded raw receipt sets and derived positive per-file costs"
    - path: "test/support/ci/library_test_partitions.exs"
      provides: "Fail-closed calibrated LPT oracle with no zero-cost missing-path fallback"
    - path: "test/support/ci/library_test_partitions_test.exs"
      provides: "Pure aggregation, exact-universe, provenance, zero-cost, tie-starvation, and assignment contracts"
    - path: "scripts/ci/library-partitions.test.sh"
      provides: "Repeated real-run comparability and receipt-lifetime regression"
    - path: "test/sigra/planning/phase_235_1_library_economics_contract_test.exs"
      provides: "Topology and immutable-bound integration contract"
  key_links:
    - from: "235.1-PARTITION-CALIBRATION.json"
      to: "Sigra.CI.LibraryTestPartitions.build_partitions!/1"
      via: "decode/hash/size/schema/path replay of retained source receipts and exact current ordinary-universe equality"
      pattern: "calibrat|ordinary|time_us"
    - from: "Sigra.CI.LibraryTestPartitions.build_partitions!/1"
      to: "scripts/ci/library-partitions.sh"
      via: "unchanged manifest emission consumed by the fixed sequential runner"
      pattern: "LibraryTestPartitions.partition"
    - from: "scripts/ci/library-partitions.test.sh"
      to: "scripts/ci/verify-library-partitions.sh"
      via: "three fresh real receipts independently checked against the unchanged factor-of-two bound"
      pattern: "verify-library-partitions"
---

<objective>
Make the two exhaustive ordinary partitions robustly comparable by replacing zero-cost missing-path fallback with a provenance-bound full-current calibration, without weakening TEST-02 or changing CI/evidence topology.

Purpose: Plan 13 proved both partitions and both receipts work, but also proved that the Phase 233 cost snapshot cannot safely assign post-snapshot tests: all missing paths receive zero, exact LPT ties always select partition 1, and newly heavy tests create deterministic imbalance. Plan 15 repairs only that local assignment seam.
Output: A committed local calibration artifact, a fail-closed calibrated partition oracle, adversarial unit/integration contracts, and repeated real local runs green at the unchanged bound.

Decision coverage: D-01: preserve the single PR owner; D-02: preserve the superseding
two-exhaustive-partition requirement and unchanged raw bound; D-03: retain same-run
parallel formatter timing; D-04: retain the existing formatter; D-05: leave the
superseding exact-six non-PR route unchanged; D-06: leave the separated current predicates
unchanged; D-07: make no truth claim before live evidence; D-08: reserve DX/Phase 235
reconciliation for dependent Plan 05; D-09: verify FAST-01/GATE-05 without rewriting them.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
@/Users/jon/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-13-SUMMARY.md
@.planning/phases/233-library-suite-economics/233-EVIDENCE.json
@test/support/ci/library_test_partitions.exs
@test/support/ci/library_test_partitions_test.exs
@scripts/ci/library-partitions.sh
@scripts/ci/library-partitions.test.sh
@scripts/ci/verify-library-partitions.sh
@scripts/ci/verify-library-partitions.test.sh
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace zero-cost drift with a provenance-bound current calibration (D-01, D-02, D-03)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json, test/support/ci/library_test_partitions.exs, test/support/ci/library_test_partitions_test.exs</files>
  <behavior>
    - RED: two or more uncalibrated/zero-cost paths presented in either input order are rejected instead of repeatedly accumulating in partition 1.
    - RED: calibration samples with a missing, stale, duplicate, scaffold, non-test, negative, non-integer, or mismatched-universe path fail closed.
    - RED: exactly three retained source samples over the same current ordinary universe derive each path's raw sample cost by summing that sample's decoded timing `.tests[].time_us`, derive its effective cost as max(1, raw sum), then derive its assignment cost as the deterministic median of the three effective values; sample/test row order cannot change the result.
    - RED: malformed base64/JSON, duplicate/extra/missing keys, wrong source tree `997a1bb5`, wrong Plan 13 summary `265b0874`, altered payload byte count/hash, over-limit artifact/payload, missing receipt/sample, combined-to-timing partition/path mismatch, derived-cost mismatch, or a current-universe change is rejected.
    - GREEN: LPT consumes only the exact positive derived current costs, retains descending-cost/lexical ordering, and produces two non-empty lexical manifests whose union equals the current ordinary universe exactly once.
  </behavior>
  <action>Per D-01 and the superseding D-02 decision, keep the existing two-manifest API and LPT algorithm but remove its default zero-cost append behavior. Before modifying a source/test file, assert the relevant runner, oracle, formatter, and current ordinary test bytes still match final Plan 13 implementation commit `997a1bb5`; the later summary/planning commits must be the only intervening changes. Then collect exactly three successful local source samples: use one validated `mktemp -d`, run `ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test bash scripts/ci/library-partitions.sh` three times, and after each run copy the exact combined receipt plus both timing receipt byte payloads before the next run overwrites `/tmp`. Admit a sample only when both child conclusions are success, all three files are regular non-symlinks, each combined payload is at most 1,048,576 bytes, each timing payload is at most 1,048,576 bytes, receipt membership is exact, and every payload has recorded byte count and SHA-256. The current ratio may remain red during source collection and is not calibration admission authority.

Start RED after those immutable inputs are captured: add failing unit tests for the behavior list, including an adversarial batch of repeated zero-cost missing paths and reversed input order. Preserve `assign!/1` as deterministic LPT, but require strictly positive costs; make the default build load one repository-owned calibration whose schema is exact and whose paths equal `current_ordinary_paths!/1` exactly after the six scaffold paths are excluded. Missing or stale calibration must stop manifest generation rather than choose a partition.

Create `sigra.library-partition-calibration/v1` with exact top-level keys `schema_version`, `source`, `ordinary_universe`, `limits`, `samples`, and `derived_costs`, and reject the complete artifact above 8,388,608 bytes. Require `source` to have exactly `implementation_commit`, `blocked_summary_commit`, `collection_command`, `sample_count`, and `aggregation`; pin their values to `997a1bb5`, `265b0874`, the literal collection command, integer three, and `median(max(1,sum(time_us)))`. Require `ordinary_universe` to have exactly `paths`, `count`, and `newline_manifest_sha256`, with lexical unique paths and matching count/digest. Require `limits` to have exactly `artifact_bytes`, `combined_payload_bytes`, `timing_payload_bytes`, `sample_count`, `combined_per_sample`, `timings_per_sample`, and `partitions`, pinned respectively to 8,388,608; 1,048,576; 1,048,576; three; one; two; and `[1,2]`. Require `derived_costs` to be a lexical array covering the universe once, each row having exactly `path` and positive-integer `time_us`.

For each fixed ordinal 1–3, retain the exact original bytes inside the tracked artifact rather than discarding them: use exact sample keys `ordinal`, `combined`, and `timings`; require ordinals `[1,2,3]` once in that order; use exact embedded-receipt keys `role`, `source_path`, `byte_count`, `sha256`, and `payload_base64`; require roles `combined`, `timing-1`, and `timing-2`, the combined `source_path` `/tmp/sigra-library-partitions.json`, timing paths `/tmp/sigra-library-1-timings.json` and `/tmp/sigra-library-2-timings.json`, timing partition order `[1,2]`, and strict canonical base64. Decode each payload, check byte count and lowercase 64-hex SHA-256 against those exact decoded bytes, reject duplicate/extra/missing JSON keys at every calibration and decoded-receipt object, and validate the full existing combined/timing receipt schemas, count arithmetic, successful outcomes, fixed partition IDs, path syntax, unique test identities, and lexical universe/manifests. Require each combined manifest to equal its matching timing receipt's unique `.tests[].file` set and require the combined union to equal `ordinary_universe.paths` exactly once with no scaffold path.

Do not store self-asserted per-file sample sums. Recompute each sample's per-file raw sum directly from the retained decoded timing `.tests` rows, use `max(1, raw_sum)` only to convert legitimate all-zero formatter rows to a positive assignment weight, and recompute `derived_costs` as the lexical exact universe of deterministic three-sample medians. `LibraryTestPartitions` may consume derived costs only after the entire retained-payload replay succeeds. No producer-owned pass boolean, handwritten partition list, opaque hash without payload, environment override, threshold override, or GitHub evidence is permitted. Commit RED and GREEN separately per the TDD workflow.</action>
  <verify>
    <automated>test "$(wc -c &lt; .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json | tr -d ' ')" -le 8388608 &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
    <fails_when>The command exits nonzero or reports zero tests; an embedded payload, byte/hash/size/schema/path linkage, zero/missing/stale/duplicate/scaffold cost, sample provenance, universe, raw-sum, or median mutation passes; or output manifests are empty, overlapping, non-lexical, or not exactly exhaustive.</fails_when>
  </verify>
  <done>The default oracle is source-complete and deterministic over positive, independently recomputed current costs; every zero/missing/stale/provenance/median mutation fails, and the two output manifests remain exact-set, disjoint, lexical, non-empty, and scaffold-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove repeated real comparability without changing the bound or topology (D-02, D-04, D-07, D-09)</name>
  <files>scripts/ci/library-partitions.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - Three consecutive real executions run the complete calibrated partition 1 then partition 2 path; each execution has two successful children and both post-partition-2 timing receipts.
    - Each of the three combined receipts independently passes `verify-library-partitions.sh`, including exact-set/disjointness, membership, arithmetic, and max/min <= 2.0.
    - Mutating any retained payload byte/base64/hash/count/schema/path linkage, the calibration universe, any calibrated cost, sample provenance, the factor-of-two verifier arithmetic, sequential owner, formatter pair, or receipt lifetime makes the focused chain fail.
    - Plan 13's two red duration pairs remain diagnostics only and no local success writes PR/scaffold/validation evidence.
  </behavior>
  <action>Extend the existing shell integration contract after the fast fake-child cases so it runs the real fixed runner exactly three consecutive times at the current working tree. After every run, require both regular timing receipts after partition 2 teardown, copy/digest the combined and timing receipts into its private test root, and invoke the existing independent verifier before continuing. This repetition is required because Plan 13 observed material run-to-run spread (`9,103/2,430ms` and `10,963/4,242ms` plus the S4 run); all three must satisfy the unchanged integer predicate independently. Do not average wall durations, retry a red sample, or tune the calibration after seeing one of these validation runs.

Expand the phase contract to bind the bounded embedded-payload calibration artifact, full byte/hash/schema/path replay, strict-positive/exact-universe loader, fixed sequential runner, both formatters, the unchanged `maximum * 1000 <= minimum * 2000` verifier arithmetic, and absence of additional ordinary invocations or PR owner changes. Mutation-test payload bytes and base64, byte counts, hashes, duplicate/extra/missing keys, receipt counts/outcomes/arithmetic, combined-to-timing path linkage, provenance commits, sample count/order, direct raw-sum/median recomputation, zero-cost rejection, exact set/disjointness, size ceilings, and no-live-evidence boundary. Run formatter, partition, Phase 233/235.1, and protected FAST-01/GATE-05 checks; preserve Plan 13 repairs and D-09 digests. If any real repetition is red, retain its exact local receipt values in the Plan 15 summary and block before Plan 14—do not weaken the bound, drop/move/duplicate a test, hand-edit manifests, or dispatch GitHub. Commit the expansion only after all three fresh repetitions pass.</action>
  <verify>
    <automated>bash scripts/ci/library-partitions.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/ex_unit_timing_formatter_test.exs test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any command exits nonzero or any PASS/test signal is absent; fewer or more than three fresh real executions occur; either child/receipt is missing or changed; any raw duration pair exceeds 2.0; a red sample is averaged/retried; topology or protected digest changes; or a live evidence artifact/action appears.</fails_when>
  </verify>
  <done>Three consecutive real local runs are independently green at the unchanged <=2.0 bound, both receipts survive every complete run, all exact-set/provenance/adversarial contracts pass, protected invariants remain unchanged, and no live evidence action occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local timing receipts to tracked calibration | Mutable `/tmp` measurements must be retained byte-for-byte, digest-bound, exhaustive, and independently recomputable before becoming assignment input. |
| Calibration to partition oracle | A stale, incomplete, zero, or forged cost can silently skew the exhaustive manifests. |
| Partition manifests to TEST-02 claim | Passing tests alone are insufficient; every real raw-duration pair must satisfy the unchanged verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-15-01 | Spoofing | calibration provenance | high | mitigate | Exact Plan 13 identifiers plus three complete embedded combined/timing payload sets whose decoded bytes, counts, paths, and SHA-256 values replay. |
| T-235.1-15-02 | Tampering | retained receipts/cost universe | high | mitigate | Exact schemas and combined-to-timing linkage; direct raw-sum/median recomputation; missing, stale, duplicate, scaffold, and nonpositive costs hard-fail. |
| T-235.1-15-03 | Repudiation | comparability | high | mitigate | Three independently verified real combined receipts; no averages, retries, or trusted pass field. |
| T-235.1-15-04 | Denial of Service | embedded receipts / LPT tie starvation | medium | mitigate | 8 MiB artifact and 1 MiB payload limits plus strict counts; positive costs and repeated-zero rejection prevent one-sided accumulation. |
| T-235.1-15-05 | Elevation of Privilege | topology escape | high | mitigate | Phase contract pins one PR owner, two exhaustive sequential manifests, parallel ExUnit, fixed formatter paths, and unchanged bound. |
| T-235.1-15-SC | Tampering | package supply chain | low | accept | No package, dependency, or action installation/change is authorized. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Comparable exhaustive ordinary partitions without FAST-01/GATE-05 regression | 15 → 14 → 05 | COVERED | Local calibrated assignment precedes unchanged live proof and reconciliation. |
| REQ | TEST-01 | Same-run parallel slow-test visibility | 13, 14 | COVERED | Plan 13 repair retained; Plan 14 captures it. |
| REQ | TEST-02 | Two comparable exhaustive ordinary partitions | 15, 14 | COVERED | Positive current calibration plus repeated local and fresh PR proof. |
| REQ | TEST-03 | Exact-six non-PR scaffold coverage | 13, 14 | COVERED | Plan 13 repairs retained; Plan 14 captures it. |
| RESEARCH | — | Preserve <=2.0 and remediate a measured miss | 15 | COVERED | Full-current median calibration; no threshold relaxation. |
| RESEARCH | — | Exact-set/disjoint/scaffold-free fail-closed manifests | 15 | COVERED | Calibration and output both equal the discovered ordinary universe. |
| CONTEXT | D-01, D-03, D-04 | One owner and same-run formatter timing | 15 | COVERED | Oracle-only assignment change; runner/formatter topology remains fixed. |
| CONTEXT | D-02, D-05, D-06 | Approved empirical fallback | 15, 14 | COVERED | Plan 15 repairs ordinary balance only; separated scaffold route remains unchanged. |
| CONTEXT | D-07, D-08, D-09 | Evidence-first truth and protected invariants | 14, 05 | COVERED | Plan 15 changes no truth/evidence docs and runs both protected verifiers. |
| CONTEXT | Deferred ideas | Unrelated CI/auth/admin/release work | — | EXCLUDED | Explicitly deferred and outside this recovery. |

<verification>
Run the pure calibration/oracle contracts, hermetic script mutations, exactly three fresh real local partition runs, focused Phase 233/235.1 contracts, formatter tests, protected offline verifiers, and formatting. Do not run GitHub workflows or create live evidence.
</verification>

<success_criteria>
- The tracked calibration stays within 8 MiB and contains exactly three complete source samples, each with one <=1 MiB combined and two <=1 MiB timing payloads retained as base64 plus exact byte count/path/SHA-256 metadata.
- The loader decodes and independently validates every retained receipt's exact keys/schema/counts/outcomes/paths, proves combined-to-timing linkage, recomputes per-file raw sums directly from test rows, applies max(1,sum), and recomputes the three-sample median before LPT.
- The oracle rejects any payload/hash/size/linkage mutation, zero-cost fallback, missing/stale/duplicate/scaffold path, malformed provenance, or derived-cost mutation before emitting a manifest.
- Three consecutive real local runs preserve both receipts and independently pass the unchanged <=2.0 verifier.
- No test path, PR owner, aggregate topology, formatter behavior, evidence receipt, FAST-01 value, or GATE-05 ledger is weakened or rewritten.
- Plan 13 remains blocked history; Plan 14 becomes reachable only after Plan 15 completes green.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-15-SUMMARY.md` when done, including RED/GREEN commits, calibration provenance, all three real local duration pairs, and confirmation that no GitHub action occurred.
</output>
