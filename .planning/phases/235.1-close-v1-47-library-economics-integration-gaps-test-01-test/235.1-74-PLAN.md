---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 74
type: execute
gap_closure: true
wave: 74
depends_on: [235.1-12]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-POSTGRESQL-PROBE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREREQUISITE-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Plans 69, 71, 72, and 73 remain immutable failed history; none of their roots, partial successes, or consumed attempts can authorize retry, reuse, live continuation, or TEST completion."
    - "Before any live PostgreSQL prerequisite or evidence-root allocation, both pinned Python runtimes prove a byte-exact stdin-fed Plan 74 probe with exact argv, pipes, cwd, closed environment, and raw five-line output semantics while every hostile mutation rejects with zero psql calls."
    - "The sole Plan 74 PostgreSQL prerequisite process persists its exact stdin, stdout, stderr, status, argv, cwd, and closed redacted environment before parsing; only byte equality to the authenticated five-line transcript passes."
    - "Plan 73's proven canonical exact-six entrypoint/prerequisite policy, Plan 72's path-keyed health-SQL authority, and Plan 71/72's parsed SQL allowlist matrix carry forward without weakening or becoming predecessor completion evidence."
    - "Only after the zero-call receipt and the fresh PostgreSQL prerequisite receipts pass may one distinct Plan 74 root execute one local chain, the canonical prepared exact-six wrapper, one automatic-PR/dispatch attempt-1 capture pair, and one distinct attempt-1 validation pair."
    - "The PR owner remains two sequential exhaustive ordinary partitions with parallel ExUnit timing; schedule/workflow_dispatch remains the fail-closed exact-six receiver; TEST-01/02/03 remain incomplete until Plan 74 evidence passes."
    - "FAST-01 source-complete n=52/p50=469, its immutable miss history, every disclosure, and all 93 GATE-05 rows remain unchanged."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json"
      provides: "Dual-runtime zero-call proof of the exact stdin probe plus inherited exact-six/source/SQL policies"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-POSTGRESQL-PROBE.json"
      provides: "Durable unparsed capture of the sole live prerequisite process"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREREQUISITE-VALIDATION.json"
      provides: "Exact raw-transcript readiness decision bound to the frozen live capture"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh attempt-1 automatic-PR two-partition capture"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh attempt-1 workflow_dispatch canonical exact-six capture"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct attempt-1 validation pair bound to Plan 74 local and capture authority"
  key_links:
    - from: "Plan 74 PROBE_SQL bytes"
      to: "the sole PostgreSQL prerequisite process"
      via: "subprocess input=PROBE_SQL with shell=false, exact argv, both pipes, authenticated cwd, and a newly constructed closed environment"
    - from: "235.1-74-POSTGRESQL-PROBE.json"
      to: "235.1-74-PREREQUISITE-VALIDATION.json"
      via: "atomic raw persistence and no-follow reopen before byte-exact five-line parsing"
    - from: "Plan 73 canonical entrypoint policy"
      to: "scripts/ci/install-golden.sh"
      via: "byte-equal accepted/15-hostile policy, fixed prepared export, exact ordered six, worker ceiling, diagnostics, and full-stream dominance"
    - from: "Plan 72 health source and Plan 71/72 parsed SQL policy"
      to: "Plan 74 generated local package"
      via: "byte/field-equal inherited matrices plus Plan 74-only delimiter/root/schema substitutions"
    - from: "all three Plan 74 pre-root PASS receipts"
      to: "the sole Plan 74 allocator"
      via: "independent reopen, exact one-probe accounting, empty root inventory, and one execute-launcher authorization"
    - from: "Plan 74 capture and validation receipts"
      to: "Plan 70 reconciliation"
      via: "unchanged routing/validation verifiers; Plans 69/71/72/73 never supply new completion evidence"
  prohibitions:
    - requirement_id: "TEST-01"
      status: "unverified"
      flagged: true
      statement: "Do not relabel predecessor timing pairs, a serial pass, a rerun, or missing same-run evidence as TEST-01 completion."
    - requirement_id: "TEST-02"
      status: "unverified"
      flagged: true
      statement: "Do not relax raw max/min <=2.0, tune after observation, reorder partitions, or admit an incomplete pair."
    - requirement_id: "TEST-03"
      status: "unverified"
      flagged: true
      statement: "Do not use raw mix test, caller-supplied prepared mode, skipped/duplicated/substituted receivers, or predecessor output as exact-six evidence."
---

<objective>
Repair Plan 73's PostgreSQL transcript boundary before allocation, then produce one fresh authenticated local and live library-economics evidence chain.

Purpose: Replace the grouped `--command` and normalized parser with one authenticated stdin-fed session whose exact raw input and five-line output are durable before interpretation, while retaining every already-proven runner, source, and SQL policy.
Output: Dual-runtime zero-call probe receipt, one frozen raw prerequisite capture, one prerequisite decision receipt, one distinct terminal Plan 74 root, and fresh canonical PR/scaffold/validation evidence for Plan 70.

Decision coverage: D-01 through D-09 are implemented across Tasks 1–3. Plans 69/71/72/73 remain immutable failed evidence only; Plan 74 is the sole possible new TEST completion authority.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-43-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-45-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-SUMMARY.md
@scripts/ci/install-golden.sh
@scripts/ci/install-golden.test.sh
@scripts/ci/verify-library-install-golden.sh
@scripts/ci/verify-library-install-golden.test.sh
@test/test_helper.exs
@test/support/install_fixture.ex
@.github/workflows/ci.yml
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

## Artifacts this phase produces

- New tracked zero-call receipt: `235.1-74-PREALLOCATION-VALIDATION.json`.
- New tracked raw prerequisite capture: `235.1-74-POSTGRESQL-PROBE.json`.
- New tracked prerequisite decision: `235.1-74-PREREQUISITE-VALIDATION.json`.
- Replaced tracked evidence receipts only after fresh passing capture/validation: `235.1-PR-EVIDENCE.json`, `235.1-SCAFFOLD-EVIDENCE.json`, and `235.1-VALIDATION-RUN.json`.
- New private evidence root: exactly one `/private/tmp/sigra-p2351-plan74-stdin-probe.XXXXXXXX`.

## Interface contracts

### Plan74StdinProbe

- `PROBE_SQL` is exactly 126 bytes/SHA-256 `0e15239b5748bc0fef0409baaef0b43d46ccc25eb06d4d9add1971278b048222`, RFC-4648 base64 `QkVHSU4gVFJBTlNBQ1RJT04gUkVBRCBPTkxZOwpTRUxFQ1QgJ1NJR1JBX1BMQU43NF9QUkVSRVFVSVNJVEVfVjEnOwpTRUxFQ1QgY3VycmVudF9kYXRhYmFzZSgpOwpTRUxFQ1QgY3VycmVudF91c2VyOwpST0xMQkFDSzsK`. Decoding yields exactly five LF-terminated statements in order: `BEGIN TRANSACTION READ ONLY;`, `SELECT 'SIGRA_PLAN74_PREREQUISITE_V1';`, `SELECT current_database();`, `SELECT current_user;`, `ROLLBACK;`.
- `EXPECTED_STDOUT` is exactly 64 bytes/SHA-256 `47c71f229461f62d7213a980b8489dc6d25d1d05ad9cbf9910a8d8ba9a8c90a9`, RFC-4648 base64 `QkVHSU4KU0lHUkFfUExBTjc0X1BSRVJFUVVJU0lURV9WMQpzaWdyYV90ZXN0CnBvc3RncmVzClJPTExCQUNLCg==`. Its exact LF-retaining lines are `BEGIN`, `SIGRA_PLAN74_PREREQUISITE_V1`, `sigra_test`, `postgres`, `ROLLBACK`; there are no blank, padded, CRLF, leading, or trailing bytes.
- The sole argv is exactly `[/opt/homebrew/bin/psql,-X,--no-psqlrc,--set=ON_ERROR_STOP=1,--tuples-only,--no-align]`. It has no `--command`, `--file`, quiet, echo, variable, or extra argument.
- The subprocess call uses `input=PROBE_SQL`, `stdout=PIPE`, `stderr=PIPE`, `shell=False`, and `cwd=/Users/jon/projects/sigra`. It passes a newly constructed environment with exactly C-sorted keys `LC_ALL,PATH,PGDATABASE,PGHOST,PGPASSWORD,PGPORT,PGUSER`; no process-environment copy, merge, update, iteration, fallback, or inherited key is allowed.
- Closed nonsecret values are exactly `LC_ALL=C`, `PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin`, `PGDATABASE=sigra_test`, `PGHOST=localhost`, `PGPORT=5432`, `PGUSER=postgres`. `PGPASSWORD` is required nonempty from the authorized secret binding, passed as the only nonliteral value, and persisted only as `<redacted-present>` plus its SHA-256.
- `/opt/homebrew/bin/psql` is the 38-byte symlink to `/opt/homebrew/Cellar/postgresql@14/14.17/bin/psql`; the target is a 634,640-byte regular executable with SHA-256 `ba4e92f3a55a7f8d8220692e0971dd23d9c17857701cb6914518efee53796d5d`.

### InheritedExecutionAuthority

- Plan 73's `entrypoint_policy` remains byte/field equal: sole accepted canonical wrapper, exact ordered unique six receivers, unconditional prepared export, two-worker ceiling, diagnostics/receipt ownership, stream persistence before assertion, and exact 15 unique hostile entrypoint cases.
- Plan 73's successful pinned runtime/archive/dependency and wrapper-stub rows remain immutable historical prerequisite inputs. Plan 74 does not rerun or relabel those consumed processes; it authenticates their receipt and current no-follow source/archive manifests, then adds only its own fresh PostgreSQL readiness authority.
- Plan 72's `source_authority` remains exact; Plan 71/72's `sql_policy` remains one accepted authenticated E-string tab sentinel plus 23 unique rejected cases. Plan 74 changes only plan identity, root/schema paths, current delimiter `plan74-collector-verify\n`, and the corrected prerequisite controller.

<assumption_delta_decision>
Primary noun: PostgreSQL prerequisite transcript.
Decision: corrected.
Rationale: Plan 73 sent five statements as one 103-byte `--command`; psql treated that string as one server request and exposed only the final `ROLLBACK` command tag. A single authenticated stdin script preserves one session while yielding the exact five-line transcript, and byte equality avoids Plan 73's unauthenticated `.strip()` normalization.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove exact stdin framing with zero psql or downstream calls (D-01–D-07, D-09)</name>
  <reversibility rating="reversible">This task authenticates immutable inputs, runs only pure in-memory fixtures under two interpreters, and writes one canonical receipt without a database process or private root.</reversibility>
  <precondition>The Plan 73 plan is 49,918 bytes/SHA-256 `9a09a60d588272d1873cf61ec838819c92ea2204867946638dd9f4eefec004e2`; its 19,903-byte PASS preallocation receipt hashes to `2835ad27d99a2f00ce4efdce1fe7a2dfdf85e016f0873945912ab4782def05db`; its 21,018-byte BLOCKED prerequisite receipt hashes to `070b3ca8118475da7fa874566896ee89c119d1cae112db378a962b892d446692`; its 12,076-byte halted summary hashes to `c0982bf7328707dfc546ac7a0abf5a0bdf28a81ffc478d29a0bd9a15dfacf4b0`; no Plan 73 evidence root exists; Plans 69/71/72 roots retain their frozen identities; and no Plan 74 probe, prerequisite, setup, or evidence-root artifact exists.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json</files>
  <read_first>
    - `235.1-73-SUMMARY.md` and `235.1-73-PREREQUISITE-VALIDATION.json` — preserve the one consumed psql process, exact `ROLLBACK\n` output, five other successful prerequisite records, zero root/live work, and halted status.
    - `235.1-73-PLAN.md` and `235.1-73-PREALLOCATION-VALIDATION.json` — carry forward the canonical exact-six entrypoint policy and generated runner, source, and SQL contracts without rerunning Task 1.
    - `235.1-43-SUMMARY.md`, `235.1-44-PLAN.md`, and `235.1-45-SUMMARY.md` — preserve the identical grouped-command failure and the later proven exact client/output/file-mode framing pattern; do not reuse any consumed marker or process.
    - `235.1-72-PREALLOCATION-VALIDATION.json` and `235.1-71-PREALLOCATION-VALIDATION.json` — retain exact path-keyed health-SQL source and accepted/23-hostile parsed SQL authorities.
    - `/opt/homebrew/bin/psql`, its resolved 14.17 target, canonical exact-six sources/tests, workflow, and Phase 233/235.1 contracts — authenticate all files through no-follow identity before deriving execution inputs.
    - Plans 69/71/72 roots and every Plan 73 receipt — immutable history only; never import, execute, retry, copy, chmod, touch, or relabel a predecessor action or descendant.
  </read_first>
  <behavior>
    - "RED: Plan 73's grouped_command_flag case uses the frozen `--command` record and is rejected with psql_calls=0; no reproduction occurs."
    - "GREEN: only authenticated_stdin_probe accepts with exact Plan 74 SQL/output bytes, argv, input binding, two pipes, shell false, repository cwd, closed environment, and raw-equality parser."
    - "Exact 37 rejected names: grouped_command_flag, stdin_absent, stdin_inherited, stdin_wrong_bytes, stdin_crlf, stdin_truncated, quiet_flag, echo_flag, missing_tuples_only, missing_no_align, missing_on_error_stop, wrong_on_error_stop, missing_begin_sql, missing_rollback_sql, commit_sql, write_sql, meta_command_sql, shell_true, stdout_not_pipe, stderr_not_pipe, wrong_cwd, stdout_missing_line, stdout_extra_line, stdout_reordered_lines, stdout_duplicate_line, stdout_blank_line, stdout_whitespace_line, stdout_wrong_sentinel, stdout_wrong_database, stdout_wrong_user, stdout_strip_normalization, nonzero_status, nonempty_stderr, env_missing_key, env_extra_key, env_wrong_value, env_copy_projection."
    - "Every rejected row is a unique one-field deep-copy mutation, accepted=false, psql_calls=0, and carries the expected rejection class; exact name-set equality and uniqueness prevent substitution or count masking."
    - "Python 3.9.6 and 3.14.4 produce identical probe, entrypoint, source, SQL-policy, name-set, and generated-source semantics with zero psql/database/allocator/root/marker/mktemp/source-materialization/mix/phx_new/prepare-graph/exact-six/timing/network/GitHub effects."
  </behavior>
  <action>Authenticate with lstat, `O_RDONLY|O_NOFOLLOW`, fstat dev/inode/type equality, exact byte count/SHA-256, duplicate-key rejection, and canonical JSON where applicable: all four Plan 73 artifacts pinned in the precondition; Plan 71/72 policy receipts; Plan 43/45 summaries; canonical exact-six wrapper/test/verifiers, test helper, fixture graph, workflow, Phase 233/235.1 contracts; both pinned Python executables; the psql symlink and resolved executable; and all frozen predecessor roots/manifests. Require the Plan 73 prerequisite record to remain BLOCKED with exactly five fully captured subprocesses, exact PostgreSQL argv containing one 103-byte grouped `--command` whose SHA-256 is `0e61228c395edc73522566117a0aaa927173019db78d06ebf998b0ab700b4dab`, status 0, stdout exactly 9-byte `ROLLBACK\n`/SHA `3faad9faa0472bfb904bae84d6c020dbb4d9f3bd57ebc1b1d3f25abfd4369e5f`, empty stderr, no Plan 73 root, and zero downstream counts.

Define `PROBE_SQL` and `EXPECTED_STDOUT` only by strict base64 decode of the pinned values in `Plan74StdinProbe`; independently require the stated byte counts, hashes, statement/line arrays, final LF, and exact Plan 74 identity. Build one pure `validate_plan74_stdin_probe(inputs)` used by the generated prerequisite controller. It must structurally require exact argv order and absence of every command/file/quiet/echo/variable option; `input is PROBE_SQL`; both pipes; `shell is False`; exact cwd; one newly constructed seven-key environment object with the six fixed nonsecret values plus one nonempty password; exact raw stdout/status/stderr; and persistence dominance before parsing. Reject any parser path containing `.strip()`, whitespace filtering, line omission, newline conversion, or decoded-text normalization. Reject any environment path containing `os.environ.copy()`, `dict(os.environ)`, copy/update/merge/inheritance, or keys outside the literal allowlist.

Require the SQL parser to recognize exactly five statements and no comments, variables, psql meta-commands, transaction replacement, COMMIT, DML, DDL, COPY, DO, function call beyond the two identity selectors, or extra bytes. Require raw output equality before interpreting exact `splitlines(keepends=True)` bytes. Do not accept semantic equivalence, reordered statements/lines, CRLF, blank lines, padded lines, alternate database/user, or any normalization.

Write RED/GREEN tests entirely in memory. The sole accepted name is `authenticated_stdin_probe`. Apply exactly the 37 named mutations from `behavior` to independent deep copies. Require exact rejected-name set/count/uniqueness, one changed field per row, accepted=false, expected failure class, and `psql_calls=0`. The missing/extra/reordered/duplicate/blank/whitespace and wrong sentinel/database/user output mutations independently exercise raw parser edges; missing BEGIN/ROLLBACK, COMMIT/write/meta cases mutate SQL bytes; env-copy is a source-structure mutation. RED records Plan 73 facts without invoking psql.

Carry `entrypoint_policy`, `source_authority`, and `sql_policy` byte/field equal from authenticated Plan 73/72 receipts, including accepted/rejected identities and zero-call rows. Carry successful Plan 73 runtime/archive/stub/dependency records as frozen inputs and authenticate their current source/archive manifests without a subprocess. Generate the Plan 74 package in memory from Plan 73 generated sources with only Plan 74 schema/path/root/delimiter/prerequisite substitutions; preserve all source names, parser anchors, policy spans, dominance, definition order, and recomputed byte/SHA/table digests. The controller source must contain the exact `subprocess.run` call contract and persistence-before-parse dominance proven above.

Run this pure gate under the exact pinned Python 3.9.6 and 3.14.4 interpreters with `PYTHONDONTWRITEBYTECODE=1`, capture both outputs/statuses, and require semantic equality. It may write only canonical `235.1-74-PREALLOCATION-VALIDATION.json` after both pass. Independently reopen it and verify omitted-field receipt digest. No psql, database, prerequisite subprocess, allocator, mktemp, root, marker, setup workspace, source materialization, Mix, phx_new, prepared graph, exact-six, timing, network, or GitHub action may occur.</action>
  <acceptance_criteria>Both runtimes prove the sole exact stdin probe and all 37 unique named hostile cases; SQL, expected raw output, argv, pipes, shell, cwd, the complete exact seven-entry redacted environment map, parser, binary, and persistence order are exact; Plan 73 entrypoint/prerequisite policy and inherited source/SQL policies remain exact; every rejected row and global forbidden-effect count has psql_calls=0; predecessors are unchanged; and no Plan 74 live or private artifact exists.</acceptance_criteria>
  <verify>
    <automated>PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json &amp;&amp; P73=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json &amp;&amp; for PY in /Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/bin/python3.9 /opt/homebrew/Cellar/python@3.14/3.14.4/Frameworks/Python.framework/Versions/3.14/bin/python3.14; do "$PY" -c 'import base64,hashlib,json,sys; nodup=lambda p:dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate")); load=lambda p:(lambda b:(b,json.loads(b,object_pairs_hook=nodup)))(open(p,"rb").read()); raw,d=load(sys.argv[1]); raw73,p73=load(sys.argv[2]); canon=lambda v:(json.dumps(v,sort_keys=True,separators=(",",":"))+"\n").encode(); assert raw==canon(d) and d["schema"]=="sigra.phase235.1-plan74-preallocation-validation/v1" and d["status"]=="PASS"; assert len(raw73)==19903 and hashlib.sha256(raw73).hexdigest()=="2835ad27d99a2f00ce4efdce1fe7a2dfdf85e016f0873945912ab4782def05db" and raw73==canon(p73); pc=d["probe_contract"]; sql=base64.b64decode(pc["probe_sql_base64"],validate=True); out=base64.b64decode(pc["expected_stdout_base64"],validate=True); assert len(sql)==126 and hashlib.sha256(sql).hexdigest()=="0e15239b5748bc0fef0409baaef0b43d46ccc25eb06d4d9add1971278b048222" and len(out)==64 and hashlib.sha256(out).hexdigest()=="47c71f229461f62d7213a980b8489dc6d25d1d05ad9cbf9910a8d8ba9a8c90a9"; argv=["/opt/homebrew/bin/psql","-X","--no-psqlrc","--set=ON_ERROR_STOP=1","--tuples-only","--no-align"]; keys=["LC_ALL","PATH","PGDATABASE","PGHOST","PGPASSWORD","PGPORT","PGUSER"]; expected_env={"LC_ALL":"C","PATH":"/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin","PGDATABASE":"sigra_test","PGHOST":"localhost","PGPASSWORD":"<redacted-present>","PGPORT":"5432","PGUSER":"postgres"}; assert pc["accepted"]==["authenticated_stdin_probe"] and pc["argv"]==argv and pc["input_binding"]=="PROBE_SQL" and pc["shell"] is False and pc["stdout_pipe"] is True and pc["stderr_pipe"] is True and pc["cwd"]=="/Users/jon/projects/sigra" and pc["environment_keys"]==keys and pc["observed_redacted"]==expected_env and pc["normalization"]==[] and pc["persisted_before_parse"] is True; rejected=pc["rejected"]; names=[r["name"] for r in rejected]; exact={"grouped_command_flag","stdin_absent","stdin_inherited","stdin_wrong_bytes","stdin_crlf","stdin_truncated","quiet_flag","echo_flag","missing_tuples_only","missing_no_align","missing_on_error_stop","wrong_on_error_stop","missing_begin_sql","missing_rollback_sql","commit_sql","write_sql","meta_command_sql","shell_true","stdout_not_pipe","stderr_not_pipe","wrong_cwd","stdout_missing_line","stdout_extra_line","stdout_reordered_lines","stdout_duplicate_line","stdout_blank_line","stdout_whitespace_line","stdout_wrong_sentinel","stdout_wrong_database","stdout_wrong_user","stdout_strip_normalization","nonzero_status","nonempty_stderr","env_missing_key","env_extra_key","env_wrong_value","env_copy_projection"}; assert len(rejected)==len(names)==len(set(names))==37 and set(names)==exact and all(r["accepted"] is False and r["psql_calls"]==0 for r in rejected); assert d["entrypoint_policy"]==p73["entrypoint_policy"] and d["source_authority"]==p73["source_authority"] and d["sql_policy"]==p73["sql_policy"]; zero={"allocator","database","exact_six","github","markers","mix","mktemp","network","phx_new","prepare_graph","psql","roots","source_materializations","timing"}; assert set(d["counts"])==zero and set(d["counts"].values())=={0} and len(d["runtimes"])==2 and all(r["status"]==0 and r["stderr_bytes"]==0 for r in d["runtimes"]) and len({r["semantic_sha256"] for r in d["runtimes"]})==1 and d["root_inventory_before"]==d["root_inventory_after"]==[]' "$PV" "$P73" || exit; done &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan74-stdin-probe.*' -print -quit)"</automated>
    <fails_when>The receipt is absent/noncanonical/duplicate-keyed; either exact byte/hash/argv/input/pipe/shell/cwd/environment/parser/persistence contract differs; any accepted or rejected name is missing, duplicated, substituted, reordered into acceptance, or has nonzero calls; inherited policy differs; runtimes disagree; any forbidden count is nonzero; a predecessor changes; or a Plan 74 live/private artifact exists.</fails_when>
  </verify>
  <done>One canonical dual-runtime receipt proves the exact stdin prerequisite mechanism, all 37 hostile cases, inherited execution policies, and generated Plan 74 package without invoking psql or allocating evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Capture and parse the sole Plan 74 PostgreSQL prerequisite before allocation (D-05–D-07, D-09)</name>
  <reversibility rating="costly">The live psql process is an append-only one-shot prerequisite attempt; its raw receipt is frozen before interpretation and failure requires a successor.</reversibility>
  <precondition>Task 1 independently passes under both runtimes with all forbidden counts zero; Plan 73's exact artifacts and successful non-PostgreSQL prerequisite rows remain unchanged; psql symlink/target and current canonical exact-six/archive/source manifests match Task 1; no Plan 74 probe/prerequisite/root exists; and a nonempty authorized `PGPASSWORD` binding is available.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-POSTGRESQL-PROBE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREREQUISITE-VALIDATION.json</files>
  <read_first>
    - `235.1-74-PREALLOCATION-VALIDATION.json` — sole SQL, output, argv, environment, controller-source, generated-package, and zero-call authority.
    - `235.1-73-PREREQUISITE-VALIDATION.json` and summary — authenticate the successful wrapper/archive/dependency records and frozen grouped-command failure; none is rerun or relabeled.
    - `/opt/homebrew/bin/psql`, its resolved 14.17 target, Plan 73 canonical wrapper/archive/source manifests, and frozen Plans 69/71/72 roots — reauthenticate immediately before and after the one live process.
    - Plan 43 and Plan 45 summaries — retain the failed grouped request and successful exact raw framing history without consuming their commands or files.
  </read_first>
  <action>Independently reopen the Task 1 receipt, regenerate `PROBE_SQL`, `EXPECTED_STDOUT`, argv, cwd, and controller source, and require byte/field equality before any live action. Reopen Plan 73 artifacts, the five frozen prerequisite subprocess rows, current archive/source manifests, and all predecessor roots before action. Read the authorized `PGPASSWORD` once into a scalar, require nonempty, and construct a new environment object by seven explicit assignments in exact C-sorted key order: the six fixed values from `Plan74StdinProbe` plus that password. Do not copy, merge, update, iterate, inherit, or pass any other process-environment field.

Invoke exactly one process using `subprocess.run(PSQL_ARGV, input=PROBE_SQL, stdout=subprocess.PIPE, stderr=subprocess.PIPE, shell=False, cwd=REPOSITORY_ROOT, env=PROBE_ENV, check=False)`. There is no shell, file, command, quiet, echo, or text-mode seam. Record start/finish wall-clock bounds around the call. Immediately capture the returned status and immutable raw bytes; before checking status, stderr, length, lines, identity, or content, atomically write duplicate-safe canonical `235.1-74-POSTGRESQL-PROBE.json` with schema/status `CAPTURED`, exact stdin/stdout/stderr base64/bytes/SHA, argv, cwd, `shell:false`, pipe/input flags, exact redacted environment/key order and password SHA, process count 1, binary identities, timestamps, Task 1 digest, predecessor manifests, and omitted-field receipt digest. Nofollow reopen that file and independently reproduce every byte/digest. Any inability to persist/reopen is a hard stop and cannot authorize parsing or retry.

Only after durable reopen, require status exactly 0, stderr exactly empty bytes, stdin exactly the 126 authenticated bytes, and stdout exactly the 64 authenticated bytes. Parse only by direct raw equality and `splitlines(keepends=True)` against the five exact LF-terminated byte strings; `.strip()`, `.rstrip()`, `splitlines()` without retained endings, filtering, decode/re-encode, CRLF conversion, or whitespace normalization is forbidden. Require exact sentinel, configured/current database `sigra_test`, configured/current user `postgres`, BEGIN first, ROLLBACK last, and no extra byte.

Atomically write and independently reopen canonical `235.1-74-PREREQUISITE-VALIDATION.json`, binding Task 1 and raw-probe file/receipt hashes, exact parse assertions, Plan 73 successful non-PostgreSQL prerequisite rows, current archive/source manifests, predecessor before/after manifests, and counts `psql=1`, `database_queries=1`, with allocator/root/mktemp/exact-six/prepare-graph/phx_new/timing/network/GitHub all zero. On any process or parse failure, write `status:BLOCKED` with the same frozen raw capture, consume the attempt, allocate no root, and require a successor. Never retry, rewrite, substitute, or treat process status alone as readiness.</action>
  <acceptance_criteria>Exactly one live psql process is durably captured before parse; its input/output/empty stderr, argv, cwd, pipes, shell flag, binary, complete exact seven-entry redacted environment map, status, and five LF-retaining lines match Task 1 byte-for-byte; Plan 73's successful runner/archive/dependency policies and all predecessor manifests remain exact; the prerequisite decision is PASS; and no Plan 74 root or downstream action exists.</acceptance_criteria>
  <verify>
    <automated>RAW=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-POSTGRESQL-PROBE.json &amp;&amp; PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREREQUISITE-VALIDATION.json &amp;&amp; /usr/bin/python3 -c 'import base64,hashlib,json,sys; nodup=lambda p:dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate")); load=lambda p:(lambda b:(b,json.loads(b,object_pairs_hook=nodup)))(open(p,"rb").read()); rr,r=load(sys.argv[1]); vr,v=load(sys.argv[2]); canon=lambda x:(json.dumps(x,sort_keys=True,separators=(",",":"))+"\n").encode(); assert rr==canon(r) and vr==canon(v) and r["schema"]=="sigra.phase235.1-plan74-postgresql-probe/v1" and r["status"]=="CAPTURED" and v["schema"]=="sigra.phase235.1-plan74-prerequisite-validation/v1" and v["status"]=="PASS"; sql=base64.b64decode(r["stdin_base64"],validate=True); out=base64.b64decode(r["stdout_base64"],validate=True); err=base64.b64decode(r["stderr_base64"],validate=True); assert len(sql)==r["stdin_bytes"]==126 and hashlib.sha256(sql).hexdigest()==r["stdin_sha256"]=="0e15239b5748bc0fef0409baaef0b43d46ccc25eb06d4d9add1971278b048222"; assert len(out)==r["stdout_bytes"]==64 and hashlib.sha256(out).hexdigest()==r["stdout_sha256"]=="47c71f229461f62d7213a980b8489dc6d25d1d05ad9cbf9910a8d8ba9a8c90a9" and err==b"" and r["stderr_bytes"]==0 and r["stderr_sha256"]=="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855" and r["status_code"]==0; lines=[b"BEGIN\n",b"SIGRA_PLAN74_PREREQUISITE_V1\n",b"sigra_test\n",b"postgres\n",b"ROLLBACK\n"]; expected_env={"LC_ALL":"C","PATH":"/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin","PGDATABASE":"sigra_test","PGHOST":"localhost","PGPASSWORD":"<redacted-present>","PGPORT":"5432","PGUSER":"postgres"}; assert out==b"".join(lines) and r["argv"]==["/opt/homebrew/bin/psql","-X","--no-psqlrc","--set=ON_ERROR_STOP=1","--tuples-only","--no-align"] and r["input_binding"]=="PROBE_SQL" and r["stdout_pipe"] is True and r["stderr_pipe"] is True and r["shell"] is False and r["cwd"]=="/Users/jon/projects/sigra" and r["environment"]["keys"]==["LC_ALL","PATH","PGDATABASE","PGHOST","PGPASSWORD","PGPORT","PGUSER"] and r["environment"]["observed_redacted"]==expected_env and len(r["environment"]["password_sha256"])==64 and r["psql_calls"]==1 and r["persisted_before_parse"] is True; assert v["probe_file_sha256"]==hashlib.sha256(rr).hexdigest() and v["parse"]=={"exact_raw_equal":True,"lines_base64":[base64.b64encode(x).decode() for x in lines],"normalization":[],"persisted_before_parse":True}; assert v["counts"]=={"allocator":0,"database_queries":1,"exact_six":0,"github":0,"mktemp":0,"network":0,"phx_new":0,"prepare_graph":0,"psql":1,"roots":0,"timing":0} and v["root_inventory_before"]==v["root_inventory_after"]==[]' "$RAW" "$PV" &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan74-stdin-probe.*' -print -quit)"</automated>
    <fails_when>Either receipt is absent/noncanonical/duplicate-keyed; raw capture was not persisted/reopened before parse; call count differs from one; any input/output/stderr/status/argv/cwd/pipe/shell/binary/environment byte or hash differs; parsing normalizes or filters; predecessor or inherited prerequisite policy changes; any forbidden downstream count is nonzero; a root exists; or the decision is not PASS.</fails_when>
  </verify>
  <done>One fresh one-shot stdin-fed PostgreSQL process proves exact Plan 74 readiness through an immutable raw capture and byte-exact PASS decision before allocation.</done>
</task>

<task type="auto">
  <name>Task 3: Allocate one Plan 74 root, run the canonical local chain, and capture both attempt-1 route pairs (D-01–D-09)</name>
  <reversibility rating="costly">The evidence root, local actions, exact-six process, and GitHub runs are append-only one-shot evidence; every failure is durable and cannot be retried or repaired in place.</reversibility>
  <precondition>Tasks 1–2 PASS; the raw PostgreSQL capture and prerequisite decision reopen exactly; their SQL/output/controller/policy/manifests agree; no Plan 74 evidence root exists; Plans 69/71/72 roots and all Plan 73 artifacts remain unchanged; GitHub authentication is valid; and repo `szTheory/sigra`, PR #234, base `main`, head `refs/heads/ci/phase-235-1-evidence`, remote head `87338db7`, and candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3` match inherited authority.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <read_first>
    - All three Plan 74 pre-root receipts — sole allocator, generated-package, stdin transcript, exact-six entrypoint, runtime/archive/dependency, and PostgreSQL authority.
    - Plan 73 plan/preallocation/prerequisite/summary — carry its proven runner and successful prerequisite policies, while treating its grouped-command attempt and all counts only as immutable failed history.
    - `scripts/ci/install-golden.sh`, its verifier, and their tests — authenticate again immediately before the single real canonical exact-six action.
    - Plan 72 summary/failure/root and Plans 69/71 summaries/failures/roots — preserve every consumed/unused budget and immutable manifest; no partial timing, source, or process output is reusable.
    - Plan 73 Task 3 route contract, workflow, routing/validation verifiers, and Phase 235.1 receipt contract — inherit unchanged terminal, lineage, excluded-ID, run/job/artifact, partition, and exact-six predicates with Plan 74 identity only.
  </read_first>
  <action>Authenticate all three Plan 74 pre-root receipts and independently reconstruct the generated package, stdin probe, exact-five-line transcript, entrypoint policy, source locator, parsed SQL policy, runtime/archive/dependency policy, and current manifests. Require exact equality before invoking the execute launcher once. Its allocator may call mktemp once with `/private/tmp/sigra-p2351-plan74-stdin-probe.XXXXXXXX`, create one mode-0700 root and one exclusive mode-0600 marker, write canonical driver/handoff receipts, materialize each authenticated source once, and finalize no-follow authority. Reopen Plans 69/71/72 roots and all Plan 73 artifacts before/after every budgeted action; any drift halts.

Run one fresh local chain under Plan 74 identity: challenge-bound PostgreSQL 14 host gate; all 19 Plan 67 parser fixtures; Plan 72's accepted source control/ten hostile source cases; Plan 71/72's accepted SQL control/23 hostile SQL cases; isolated environment matrix; one real dry run; candidate/current observation; one admission; one health sample; and one timing command with exactly three complete pairs. Require six green timing children, 225 exhaustive disjoint paths in deterministic 114/111 partitions, sequential partition order inside each pair, positive integer raw durations, unchanged parallel ExUnit formatter behavior, independently recomputed raw ratio <=2.0, and nine fresh ledger-disjoint payloads. Use `plan74-collector-verify\n` with Plan 67 parser anchors. The Task 2 readiness probe is separate prerequisite history and is not rerun or counted as local health evidence.

After timing and before terminal publication, invoke the real exact-six action exactly once using only `bash scripts/ci/install-golden.sh` from authenticated repository cwd with the inherited exact ASDF runtime, selected phx_new 1.8.8 archive, `MIX_ENV=test`, PostgreSQL environment, LC_ALL, PATH, and redacted password binding. Never invoke raw `mix test`, set prepared mode in the parent as a substitute, or call `prepare_graph!` directly. The wrapper itself must overwrite prepared mode and own its sole exact ordered child.

Capture the wrapper child's complete stdout and stderr concurrently plus status/argv/cwd/redacted environment and start/finish boundaries. Atomically publish and no-follow reopen `local/install-golden-process.json` before any exit assertion. Then preserve/reopen `/tmp/sigra-library-install-golden.json` and `/tmp/sigra-install-golden-diagnostics.json` into the root before checking outcome. Require status 0, complete stream hashes/bytes, exact unique six receivers, prepared_fixture=true, worker_ceiling=2, complete positive diagnostic phases, six variants, unique partitions/ports, zero failed paths, and existing install-golden verifier PASS. On failure, publish a durable full-stream local failure receipt, freeze root, perform no remote action, and require a successor.

Seal every descendant once in `local/artifact-manifest.json`, publish one terminal PASS, and make `local/verify-plan74-terminal.py` independently reopen all predecessor roots, Plan 73 artifacts, Plan 74 pre-root receipts, source/policy/entrypoint/prerequisite/process/wrapper/diagnostic/timing receipts, ledgers, and manifests without mutation. Terminal authority requires exactly one Plan 74 prerequisite psql process, one local chain, one real canonical wrapper process, zero raw exact-six invocations, three pairs/six timing children/225 paths, and the exact-six process/receipt/diagnostic chain.

Only after terminal PASS, query `gh api rate_limit` once. At REST core <=250, record reset and issue no polling until reset; HTTP 403/429 is a hard stop. Give each run one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`, fetch one structured summary after completion, and fetch failed logs only for failure. At most one non-rate-limit API transient may be retried; never rerun a workflow or increment attempt. Deterministic/workflow failure blocks without rerun, substitution, in-place repair, waiver, or auto-approval.

Exclude every retained Plan 35–73 run/job ID. Push candidate `3713dcd4` once, capture one automatic pull_request attempt-1 run, and dispatch one workflow_dispatch attempt-1 run at the same SHA. Require exact repo/PR/base/head/SHA/event/workflow/job/attempt identities, successful selected routes/checks, independent artifact decoding, two partitions/ratios, and canonical exact-six only on dispatch. Write/reopen PR/scaffold receipts; commit exactly those two files as candidate's direct child and push once. Capture one automatic pull_request attempt-1 validation and one distinct workflow_dispatch attempt-1 validation at evidence SHA. Write `235.1-VALIDATION-RUN.json` only after all four pass, binding run identities, lineage, Plan 74 root/terminal/stdin prerequisite/entrypoint/process, 225-path 114/111 topology, ratios, and exact-six. Run unchanged offline verifiers. Do not push again or reconcile prose.</action>
  <acceptance_criteria>Exactly one Plan 74 root/local chain exists; terminal verification authenticates the raw prerequisite capture, exact stdin transcript, inherited source/SQL/entrypoint policies, three timing pairs, six green timing children, 225 paths, and one canonical exact-six wrapper process with both streams preserved; Plans 69/71/72 and Plan 73 remain unchanged; exactly four fresh distinct non-excluded attempt-1 runs prove capture/validation PR/dispatch; the evidence commit is candidate's direct child with only two capture JSON files changed; and both unchanged evidence verifiers pass.</acceptance_criteria>
  <verify>
    <automated>PLAN74_ROOT="$(/usr/bin/python3 -c 'import os,re; rows=sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan74-stdin-probe\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False)); assert len(rows)==1; print(rows[0])')" &amp;&amp; /usr/bin/python3 "$PLAN74_ROOT/local/verify-plan74-terminal.py" --root "$PLAN74_ROOT" --preallocation-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json --postgresql-probe .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-POSTGRESQL-PROBE.json --prerequisite-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREREQUISITE-VALIDATION.json --plan69-root /private/tmp/sigra-p2351-plan69-recovery.G9QrBacu --plan71-root /private/tmp/sigra-p2351-plan71-sql-guard.knJsmWsF --plan72-root /private/tmp/sigra-p2351-plan72-source-authority.Q1orfipi &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Root discovery returns zero/multiple; a predecessor changes; any stdin, transcript, source, policy, entrypoint, prerequisite, stream, receipt, manifest, ledger, partition, ratio, timing-child, or exact-six predicate fails; an action repeats; a run is stale/excluded/retried/wrong-ref/wrong-SHA/non-green; lineage differs; or an offline verifier fails.</fails_when>
  </verify>
  <done>One fresh stdin-authenticated Plan 74 chain proves complete local, PR, and non-PR routes and leaves canonical capture/validation receipts as Plan 70's sole new TEST authority.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | Items | Coverage |
|---|---|---|
| GOAL | Two measured exhaustive PR partitions plus exact-six non-PR coverage without protected regression | Task 3 proves both routes from one fresh local/live chain; no predecessor partial evidence is promoted. |
| REQ | TEST-01, TEST-02, TEST-03 | All three are in frontmatter; Task 3 preserves unchanged predicates and produces their only possible new evidence. |
| RESEARCH | Independent fail-closed receipts, same-run timing, deterministic receiver ownership | Tasks 1–3 authenticate raw bytes, persist before parse, preserve exact topology, and require independent replay at every boundary. |
| CONTEXT | D-01 through D-09 and recovery addenda | Tasks cite every decision, preserve partitions/exact-six/attempt-1 budgets, delay reconciliation, and retain FAST-01/GATE-05. |
| LATEST EVIDENCE | Plan 73 proved the canonical runner and non-PostgreSQL prerequisites, then grouped `--command` yielded only `ROLLBACK` | Task 1 records without rerun; Task 2 uses authenticated stdin once; Task 3 proceeds only from byte-exact PASS. |
| HISTORY | Plans 43/45 framing and Plans 69/71/72/73 recovery history | Working raw framing informs the gate; every consumed command/root remains frozen and non-passing. |
| CONTEXT deferred | Unrelated release, installer-product, auth, security, admin, and other CI work | Excluded; no deferred item is implemented. |
</source_coverage_audit>

<recovery_decisions>

- Plan 73's grouped-command psql attempt and all its artifacts are permanently frozen. Its successful structural/runner/archive/dependency rows remain authenticated policy/history, not TEST completion evidence.
- The confirmed one-request behavior and safe stdin reproduction are diagnostic authority only; Plan 73 is never retried. Plan 74 gets one separately identified stdin prerequisite attempt after the dual-runtime zero-call gate passes.
- Raw input/output authority precedes parsing: full stdin/stdout/stderr/status/argv/cwd/environment are persisted and no-follow reopened before any content/status decision; no normalization is allowed.
- The environment is constructed from a closed seven-key literal projection with one explicit secret binding. `os.environ.copy()` and every inherited/extra key path are rejected structurally before psql.
- Plan 74 retains Plan 73's canonical exact-six runner/prerequisite policies, Plan 72's source authority, Plan 71/72's SQL policy, TEST predicates, two partitions, exact-six, FAST-01, GATE-05, and all historical failures without weakening or relabeling.
</recovery_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Frozen Plan 73 failure → Plan 74 pure gate | Grouped-command diagnostics inform fixtures but cannot trigger a retry or database call. |
| Authenticated bytes → psql process | Only exact stdin, argv, pipes, cwd, binary, and closed environment may cross into the sole live prerequisite. |
| psql process → parser | Complete raw streams/status are durable before exact byte parsing; normalization cannot hide drift. |
| Prior runner/source policies → Plan 74 package | Inherited accepted/adverse matrices remain byte/field equal while only Plan 74 identity changes. |
| Prerequisite PASS → allocator | The one-shot root is forbidden until all three pre-root receipts independently reopen and agree. |
| Local terminal → GitHub | Network actions remain blocked until stdin, source, timing, entrypoint, exact-six, and manifest predicates pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-74-01 | Spoofing | PostgreSQL transcript | high | mitigate | Tasks 1–2 pin exact input/output bytes, raw five-line equality, current database/user, status, stderr, and Plan 74 sentinel. |
| T-235.1-74-02 | Tampering | psql argv/input | high | mitigate | Exact stdin-only argv, shell false, both pipes, cwd, binary identity, and 37 named zero-call hostile mutations. |
| T-235.1-74-03 | Information Disclosure | Closed environment | high | mitigate | Seven exact keys, redacted nonempty password plus hash, no environment copy/inheritance, and env missing/extra/wrong/copy fixtures. |
| T-235.1-74-04 | Repudiation | Prerequisite process | high | mitigate | Atomic raw receipt and no-follow reopen before parsing; failure freezes the one attempt and blocks allocation. |
| T-235.1-74-05 | Tampering | Exact-six/source/SQL policy | high | mitigate | Byte/field equality to Plan 73 entrypoint, Plan 72 source, and Plan 71/72 SQL matrices under both runtimes. |
| T-235.1-74-06 | Repudiation | Failed predecessor budgets | high | mitigate | All tasks authenticate Plans 69/71/72 roots and Plan 73 artifacts and prohibit retry/reuse/relabeling. |
| T-235.1-74-07 | Spoofing | GitHub evidence | high | mitigate | Exact repo/ref/SHA/event/workflow/job/attempt/artifact identities, excluded IDs, and unchanged offline verifiers. |
| T-235.1-74-08 | Denial of Service | Local/live budget | medium | mitigate | One prerequisite psql, one evidence root, one wrapper process, one watcher/run at 60 seconds, rate hard stops, and no workflow reruns. |
</threat_model>

<verification>
Task 1 verifies the exact stdin/37-hostile matrix and inherited runner/source/SQL policies under both pinned runtimes with zero psql or downstream effects. Task 2 spends one PostgreSQL prerequisite call, preserves all raw process evidence before parsing, and authorizes allocation only on exact five-line equality. Task 3 independently authenticates all three receipts, executes one canonical local/exact-six chain, verifies the terminal, and only then spends four fresh attempt-1 live runs. Plan 70 remains unavailable until the Plan 74 summary and unchanged evidence verifiers pass.
</verification>

<success_criteria>
Plan 74 succeeds only when the zero-call receipt, raw PostgreSQL capture, prerequisite decision, sole fresh root, local terminal, canonical exact-six process/receipt/diagnostics, three ordinary timing pairs, capture pair, and distinct validation pair all independently PASS; the exact 37 stdin and inherited 15/ten/23 safety mutations remain complete and unique; Plans 69/71/72/73 stay frozen; no action is retried; and unchanged routing/validation verifiers accept the resulting evidence.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-SUMMARY.md` when done.
</output>
