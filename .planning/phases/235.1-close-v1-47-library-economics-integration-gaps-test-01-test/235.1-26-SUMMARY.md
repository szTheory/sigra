---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 26
subsystem: ci-evidence
tags: [bash-portability, nounset, exact-tree, deterministic-block]
status: halted
dependency_graph:
  requires: [235.1-25, 235.1-24, 235.1-22, 235.1-21]
  provides: [dual-bash-production-loader-contract, initialized-runner-array, refreshed-seven-pin-authority]
  affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
tech_stack:
  added: []
  patterns: [whole-runner-fake-child, dual-shell-nounset-contract, deterministic-commit-tree]
key_files:
  created: [.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-26-SUMMARY.md]
  modified:
    - scripts/ci/library-partitions.sh
    - scripts/ci/library-partitions-portability.test.sh
    - scripts/ci/verify-library-routing-evidence.sh
    - scripts/ci/verify-library-routing-evidence.test.sh
decisions:
  - Initialize the production loader path array with `local -a paths=()` so nounset behavior is identical under Bash 3 and Bash 5.
  - Stop after the sole timed invocation exposed candidate-worktree path contamination from a shared build symlink; do not retry, substitute, push, or admit evidence.
requirements-completed: []
metrics:
  duration: 10m
  completed: 2026-09-10
actuals:
  tokens: 3808
  tasks: 1
  commits: 2
---

# Phase 235.1 Plan 26: Dual-Shell Loader Recovery Summary

The actual production loader is now nounset-safe under macOS Bash 3.2 and Homebrew Bash 5.2 with an exact refreshed authority pin, but the sole permitted timed candidate invocation stopped before producing a local pair because shared build metadata contained paths rooted in the main checkout.

## Task 1 — Complete

RED commit `e46ebc568946e2ea0157dad186a4d29087fee9b9` refactored the portability contract into a reusable driver that executes `scripts/ci/library-partitions.sh` as a whole. It resolved distinct `/bin/bash` 3.2.57 and `/opt/homebrew/bin/bash` 5.2.37 runtimes, supplied only a fake `mix` child, and isolated each shell's nonempty/empty logs and receipts. The unchanged runner passed its Bash 3 nonempty case and RED failed specifically with:

```text
library-partitions-portability.test: FAIL: modern production loader hit paths: unbound variable
```

GREEN commit `105498a76a0cd05d24451ea8f87cc60a1778e2b8` changed only the declaration `local -a paths` to `local -a paths=()` and refreshed the matching runner digest in the verifier and adverse suite. Both shell versions then proved:

- identical NUL-delimited child argv and `1` then `2` execution order;
- preservation of paths containing spaces and literal `[abc]*` glob characters;
- identical universe, partition paths, manifest hashes, conclusions, statuses, and fixed timing paths;
- normalization of an absolute repository timing path back to `test/...`;
- an empty first partition exits nonzero with exactly `library-partitions: FAIL: partition 1 is empty`, no child invocation, and no nounset diagnostic.

The final runner SHA-256 is `f2a75570b5cfc050312571c8d78aaadbff503bfff2c24074f8e0b9ce430c79c7`. Only that pin changed among the seven ordered active entries; pristine preflight and all seven isolated mutations passed. The four-file Task 1 tree is `5c9e76366145c9f1619bc60778794f687157fa91`.

## Frozen Source, Calibration, and Audit

The untimed production replay preserved all required facts:

| Fact | Exact value |
| --- | --- |
| Ordinary membership | 225 exhaustive, disjoint paths |
| Partition counts | 111 / 114 |
| Derived weights | 22,878,707us / 22,878,706us |
| Newline manifest SHA-256 | `8e5c31efc1ca73ca05939c70b423e579d86944ecfc49b693fb93d6e2c356aabc` / `1740e9fee3f3670d64ad8a843dda90935797b7204640e83e979cb18d212e044a` |
| NUL universe SHA-256 | `78a70051f7aa55f91de5849327b86dca7915028b15d263230efb5c217b1907f7` |
| Source-index SHA-256 | `f4ffd881ff99ce65bc1bfef576188de24ed99064f0e7132306d0831fe8dc4485` |
| Calibration SHA-256 | `f54316873b2e66cb39277e063accf1168736eb9d3f74ee6c61183a845244e99b` |
| Calibration manifest SHA-256 | `1168cda99d40277253a0eb4a1c2e7ce3debdb3f92d77f3ca88c304690539e424` |
| Pre-evidence audit SHA-256 | `b9c4c6614c776d8e8c192b2bfa941cb024244289e850066a4869f3120b4588dd` |

The dual-shell suite, routing adverse suite, seven-pin preflight, and focused loader/lifecycle command passed 35 tests. The execution diff from approved planning commit `88eccdb676b680c5cc0229cfa5cae38d1030be58` contained exactly the four declared Task 1 files. Unrelated dirty paths and the cached index were byte-identical before and after. No recalibration or Plan 22 validation ran.

## Task 2 — Deterministic Hard Stop

PR #234 remained open at exact evidence head `47f9578fc97edc63603953d446e79fac884326c3`.

Two independent `git commit-tree` invocations used Task 1's exact tree, the old evidence head as sole parent, fixed message `Phase 235.1 Plan 26 exact-tree evidence candidate`, and Task 1 identity/timestamps. Both produced:

- Candidate: `9ae290624e483129d2c294366f01a1a83feec82b`
- Tree: `5c9e76366145c9f1619bc60778794f687157fa91`
- Sole parent: `47f9578fc97edc63603953d446e79fac884326c3`
- Recursive diff against Task 1: empty

In a detached candidate worktree, dual-shell portability, routing adverse/preflight, validation adverse, independent partition-verifier, the focused five-file contract command (74 tests), FAST-01, terminal ratification, Actionlint, formatting, and tracked cleanliness all passed.

The candidate used dependency and build symlinks to the main checkout. The command `bash scripts/ci/library-partitions.test.sh` was then invoked exactly once. Validation 1's complete ordinary suite ran green (32 doctests, 3 properties, 1,274 tests, 0 failures, 4 skipped), but its timing receipt included absolute paths rooted at `/Users/jon/projects/sigra` rather than the detached candidate root. The production runner correctly rejected that receipt:

```text
jq: error (at /tmp/sigra-library-1-timings.json:1): timing receipt contains a path outside the repository test tree
```

The raw combined receipt records 225 ordinary paths, partition 1 `failure`/status 1 at 4,740ms, and partition 2 `not_run`; its SHA-256 is `94788e60f7d53cf090cb3d8f406d0915a6e2fb205221b0ed03eab8742e774f1e`. The partition-1 timing payload SHA-256 is `c402dfcae87309c486b955544a1b1ab3c25dcf05549586d91a35f79de163e1cb`.

Plan 26 permits exactly one timed self-test invocation and forbids retry, build substitution, alternate candidate construction, source/calibration/audit edits, tuning, or GitHub work after failure. Execution therefore stopped durably.

## Budget and External-Action Accounting

- `bash scripts/ci/library-partitions.test.sh`: exactly one invocation total.
- Successful new real candidate-local pairs: zero of three.
- Pushes and workflow dispatches: zero.
- CI watchers, structured summaries, failed-log fetches, and artifact downloads: zero.
- PR, scaffold, and validation receipts: absent and unwritten.
- The audit remains the committed 21/24 pre-evidence document.
- The ten-run negative ledger and digest `847f97542bd110be6022771098a54db9b4d8b0f75db2c32b923ee81834acf962` remain unchanged.

## Deviations from Plan

### Blocking environment defect

**1. Candidate build materialization leaked main-checkout absolute timing paths**

- **Found during:** Task 2's sole timed invocation.
- **Issue:** Symlinking the candidate `_build` directory to the main checkout preserved compiled test metadata whose absolute file paths were outside the candidate repository root.
- **Outcome:** The production normalization boundary failed closed after the first child suite. The no-retry budget made repair in this plan prohibited.
- **Files modified:** None.
- **Commit:** None.

An exploratory pre-receipt call to the final routing verifier also rejected the intentionally absent PR/scaffold receipts. It was untimed, made no change, and was excluded from the remaining pre-receipt gate chain.

## TDD Gate Compliance

- RED: `e46ebc56`.
- GREEN: `105498a7`.
- The tracer's complete untimed end-to-end verification passed before candidate expansion.

## Known Stubs

None.

## Threat Review

No endpoint, authentication path, schema, dependency, package, workflow, ordinary source, calibration, or audit surface changed. Whole-runner tests cover the Bash 3/5 trust boundary, all seven active authority entries remain fail-closed, and no GitHub artifact entered tracked authority.

## Self-Check: PASSED

- Both Task 1 commits, the frozen tree, and exact candidate commit resolve.
- The runner digest, active pin mutation proof, dual-shell outcomes, source/calibration/audit facts, and unchanged unrelated worktree state were observed.
- The sole timed invocation and its failing receipt diagnostics are recorded; no retry, push, run, or receipt write occurred.
