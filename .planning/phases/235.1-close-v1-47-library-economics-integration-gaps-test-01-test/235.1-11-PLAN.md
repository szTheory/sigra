---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 11
type: execute
wave: 9
depends_on: [235.1-10]
files_modified:
  - test/support/ci/ex_unit_timing_formatter_test.exs
  - test/support/install_fixture.ex
  - test/support/install_fixture_performance_test.exs
  - scripts/ci/library-partitions.test.sh
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The ordinary partition-1 receipt remains present and byte-identical after partition 2 executes every assigned test, including the timing formatter contract."
    - "A failed Rebar dependency compile retries once against a validated private writable copy of dependency source state while the shared dependency graph remains immutable."
    - "Recovery state stays inside the fixture graph lifetime, is removed by graph cleanup, shares no writable filesystem identity with the source, and never increases the exact two-worker ceiling."
    - "All six Plan 10 capture failures remain immutable negative history and no local fixture result is promoted to live evidence."
  artifacts:
    - path: test/support/ci/ex_unit_timing_formatter_test.exs
      provides: collision-free formatter contract using the dedicated scaffold timing path
    - path: test/support/install_fixture.ex
      provides: bounded private dependency-source recovery for Rebar compilation
    - path: test/support/install_fixture_performance_test.exs
      provides: source immutability, private-copy integrity, lifetime, cleanup, and one-retry contracts
  key_links:
    - from: partition 2 timing formatter test
      to: ordinary partition receipts
      via: dedicated scaffold receipt path and regression assertions over both ordinary receipt bytes
    - from: failed dependency compile
      to: Rebar DAG writer
      via: retry-only MIX_DEPS_PATH replacement pointing at a writable graph-private dependency copy
    - from: private retry dependency copy
      to: graph cleanup
      via: validated graph-member placement under the existing graph root
---

<objective>
Repair the two independently confirmed CI-only failures that blocked Plan 10 without changing route topology, evidence predicates, or protected aggregates.

Purpose: Make the already-passing ordinary suites retain both receipts and let Rebar persist its source-owned DAG during the single exceptional compile retry while preserving immutable shared dependencies and the two-worker ceiling.
Output: Collision-free timing tests, bounded private dependency-source recovery, and deterministic adverse coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-08-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-09-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-10-SUMMARY.md
@test/support/ci/ex_unit_timing_formatter.ex
@test/support/ci/ex_unit_timing_formatter_test.exs
@scripts/ci/library-partitions.sh
@scripts/ci/library-partitions.test.sh
@test/support/install_fixture.ex
@test/support/install_fixture_performance_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve both ordinary timing receipts across the complete sequential run (D-03, D-04)</name>
  <files>test/support/ci/ex_unit_timing_formatter_test.exs, scripts/ci/library-partitions.test.sh</files>
  <behavior>
    - The formatter write test uses `/tmp/sigra-library-scaffold-timings.json`, the already allowlisted non-ordinary path, and cleans only that path.
    - A regression seeds distinct bytes at both ordinary timing paths, exercises the formatter contract assigned to partition 2, and proves neither ordinary file is removed, replaced, or truncated.
    - The sequential runner contract proves a successful partition 1 receipt still exists with its original digest after the partition 2 child finishes.
  </behavior>
  <action>Start RED by extending the formatter and runner contracts to reproduce the cross-partition lifetime collision: seed both ordinary receipt paths with distinguishable valid bytes, run the partition-2/formatter-test path or its hermetic child analog, and assert partition 1 remains a regular non-symlink file with the same SHA-256. Then move the formatter unit test's write target and cleanup ownership to the dedicated scaffold timing path already accepted by `Sigra.CI.ExUnitTimingFormatter`; keep production partition paths, formatter allowlist, per-test schema, and D-03 same-run parallel timing unchanged. Do not add sleeps, timing reruns, global cleanup, or a fourth receipt path. Add an adverse/source mutation assertion that changing the unit test back to either ordinary path fails the focused contract. Preserve Plan 10's missing-partition diagnostic as historical truth rather than editing its summary.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/ex_unit_timing_formatter_test.exs &amp;&amp; bash scripts/ci/library-partitions.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 bash scripts/ci/verify-library-partitions.test.sh</automated>
  </verify>
  <done>Partition 2 can execute the timing formatter contract without changing either ordinary receipt, and the runner contract detects any future cross-partition clobber.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retry Rebar compilation with graph-private writable dependency sources (D-05 superseded, D-09)</name>
  <files>test/support/install_fixture.ex, test/support/install_fixture_performance_test.exs</files>
  <behavior>
    - Only an output naming one failed dependency enters the existing single retry; unrelated failures retain their original status and output without copy creation.
    - Recovery copies the complete validated read-only `shared_deps` tree into a unique graph-member directory, makes only that private copy writable, proves no shared writable identity, and replaces `MIX_DEPS_PATH` only in retry options.
    - The original shared dependency source tree and its digest/modes remain unchanged before and after retry, including the dependency source `_build/prod/lib/.rebar3` location Rebar may create.
    - The private copy exists for the retry/graph lifetime, is removed by normal and failure cleanup, and retry execution remains within the existing graph-global two-worker lease.
  </behavior>
  <action>Start RED with an injected-command contract that exactly reproduces the first compile failure, writes a marker beneath the retry dependency source's `_build/prod/lib/.rebar3`, and succeeds only when retry `MIX_DEPS_PATH` is writable and distinct from the sealed shared path. Replace the incorrect build-output `.mix` recovery in `retry_failed_dependency_compile/4` with one bounded helper that: extracts the failed dependency name; resolves and revalidates the original `MIX_DEPS_PATH` as the graph's canonical read-only `shared_deps`; creates a unique validated child beneath a fixed graph-private recovery directory; copies the whole dependency tree using the existing copy primitive; removes inherited ACLs/makes only the copy writable; rejects symlinks, path escape, missing dependency source, partial copy, or shared writable filesystem identity; and swaps `MIX_DEPS_PATH` only for the one retry while retaining `MIX_BUILD_PATH`, partition, port, diagnostic flag, captured output, and original failure authority. Do not make shared deps writable, ignore Rebar write errors, mutate dependency build outputs as a substitute, add another retry, or spawn outside `with_worker/1`. Retain successful recovery copies until graph cleanup so subsequent Mix use cannot reference deleted source state; require both normal cleanup and preparation-failure cleanup to remove them. Test exact one-retry count, no-copy nonmatching failure, source byte/mode integrity, private `.rebar3` writeability, copy independence, path validation, cleanup, status propagation, and the unchanged maximum of two concurrent workers.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/install_fixture_performance_test.exs &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/ex_unit_timing_formatter_test.exs test/support/install_fixture_performance_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; bash scripts/ci/install-golden.test.sh &amp;&amp; bash scripts/ci/verify-library-install-golden.test.sh &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh</automated>
  </verify>
  <done>The exceptional compile retry gives Rebar private writable source-owned state, leaves shared dependencies byte- and mode-immutable, cleans all private state safely, and preserves one retry plus two workers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Partition-local test to shared `/tmp` receipts | One test process can otherwise mutate evidence owned by an earlier partition. |
| Read-only dependency graph to Rebar compiler | Rebar writes DAG state below dependency source paths during compilation. |
| Failed compiler output to recovery path | Parsed dependency identity must not authorize path escape or arbitrary copying. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-11-01 | Tampering | ordinary timing receipts | high | mitigate | Dedicated scaffold test path plus byte/digest preservation regression. |
| T-235.1-11-02 | Elevation of Privilege | failed dependency name | high | mitigate | Strict dependency token, canonical graph-root validation, and exact child containment. |
| T-235.1-11-03 | Tampering | shared dependency source | high | mitigate | Read-only canonical source, private full copy, identity/integrity assertions. |
| T-235.1-11-04 | Denial of Service | compile recovery | medium | mitigate | Existing one-retry branch and graph-global two-worker lease remain authoritative. |
| T-235.1-11-05 | Repudiation | failed capture history | medium | mitigate | Plan 10 summary remains unchanged and no evidence receipt is created locally. |
| T-235.1-11-SC | Tampering | package supply chain | low | accept | No package versions or dependency declarations change; only existing local dependency bytes are copied. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Measurable single-owner PR partitions plus exact-six non-PR receiver | 11 | COVERED | Repairs both blocked execution paths without topology change. |
| REQ | TEST-01 | Same-run parallel timing survives complete PR owner execution | 11 | COVERED | Partition receipts cannot be clobbered by assigned tests. |
| REQ | TEST-02 | Two independently verifiable ordinary partition receipts | 11 | COVERED | Both artifacts persist for the existing verifier. |
| REQ | TEST-03 | Exact-six scaffold execution can compile without shared mutable state | 11 | COVERED | Retry-only private dependency source supports Rebar. |
| RESEARCH | — | Fail-closed receipts and immutable prepared fixture graph | 11 | COVERED | Dedicated path and private-copy recovery preserve both constraints. |
| CONTEXT | D-01, D-03, D-04 | One owner and same-run formatter authority | 11 | COVERED | Task 1 leaves production topology and timing semantics intact. |
| CONTEXT | D-05, D-06 | Exact-six non-PR execution under fallback addendum | 11 | COVERED | Task 2 repairs execution without restoring superseded predicates. |
| CONTEXT | D-07, D-08, D-09 | Evidence-first closure and protected history | 11 | COVERED | No truth status changes; protected verifiers gate remediation. |

<verification>
Run focused TDD contracts for both root causes, both production receipt mutation suites, the phase topology contracts, and protected FAST-01/GATE-05 verifiers. This plan dispatches no GitHub workflow and creates no live evidence receipt.
</verification>

<success_criteria>
- Partition 1 receipt bytes survive every partition 2 test and cleanup path.
- Rebar's one exceptional retry writes only to a validated private dependency-source copy.
- Shared dependency bytes/modes, exact two-worker ceiling, route topology, and protected aggregate remain unchanged.
- Plan 10 and all six failed runs remain immutable negative history.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-11-SUMMARY.md` when done.
</output>
