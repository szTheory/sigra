---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 02
type: execute
wave: 2
depends_on: [235.1-01]
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-05, exactly one `library_tests_shard` owns exactly one `MIX_ENV=test mix ci`, and the bare `Library tests` aggregate remains byte-stable."
    - "The `ci` alias invokes the economics harness once while the six-path `ci.install_golden` alias remains the sole exact receiver list."
    - "CI validates and uploads both authoritative receipts under `if: always()` and treats missing files as errors."
    - "The current PR path retains ordinary, upgrade, golden-generation, idempotency, and scaffold coverage exactly once."
  artifacts:
    - path: "mix.exs"
      provides: "Contributor parity alias wired through one economics harness"
    - path: ".github/workflows/ci.yml"
      provides: "Single owner, fixed receipt paths, validation, and fail-closed uploads"
    - path: "test/sigra/planning/phase_233_library_economics_contract_test.exs"
      provides: "Updated one-owner topology and exact-once receiver contract"
  key_links:
    - from: "mix ci"
      to: "scripts/ci/library-economics.sh"
      via: "one fixed `cmd bash` alias leg replacing the adjacent ordinary/install legs"
    - from: "library_tests_shard"
      to: "mix ci"
      via: "the sole `MIX_ENV=test mix ci` workflow command"
    - from: "library_tests_shard"
      to: "both timing artifacts"
      via: "always-run validation and pinned upload-artifact steps with missing-file errors"
    - from: "library_tests"
      to: "library_tests_shard"
      via: "unchanged `needs: [library_tests_shard]` aggregate contract"
  prohibitions:
    - "MUST NOT restore a two-job matrix, add another library execution owner, rename the protected aggregate, or duplicate the full suite."
    - "MUST NOT copy the six scaffold file paths into the harness or remove upgrade, golden, idempotency, or scaffold coverage from pull requests."
    - "MUST NOT make upload or validation failures advisory."
---

<objective>
Wire the proven economics tracer into contributor parity and the sole GitHub library owner while updating the historical topology contract to the deliberate one-owner interpretation.

Purpose: Make the new evidence a production consumer without regressing the fast single-owner design or exact-once coverage.
Output: Updated Mix alias, CI workflow, and Phase 233 structural contract.

Decision coverage: D-01, D-02, D-04, D-05, and D-06 are wired; D-03's prohibited reruns are contract-locked; D-09's protected aggregate is preserved.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@mix.exs
@.github/workflows/ci.yml
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@scripts/ci/library-economics.sh
@scripts/ci/verify-library-economics.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route contributor parity through the single economics harness (D-01, D-03, D-05, D-06)</name>
  <reversibility rating="costly">Changing the `mix ci` contract affects contributors, CI, exact-once tests, and later evidence, though it remains source-revertible.</reversibility>
  <files>mix.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `mix.exs` aliases `ci` and `ci.install_golden` — preserve every non-test leg and the six-path receiver byte-for-purpose.
    - `phase_233_library_economics_contract_test.exs` topology, `ci_legs/1`, live `:scaffold` set, and `install_golden_paths/1` helpers — retain exhaustive equality and duplicate rejection while replacing retired shard assumptions.
    - `scripts/ci/library-economics.sh` from Plan 01 — consume its fixed public seam once; do not duplicate its internal commands in `mix.exs`.
  </read_first>
  <behavior>
    - "The `ci` alias order remains format, locked-dependency checks, warnings-as-errors compile, one economics harness, then dependency-off."
    - "The six `ci.install_golden` paths equal the live upgrade/golden/idempotency/scaffold universe and are unique."
    - "The harness names `mix ci.install_golden` once and contains no copied receiver path."
    - "No serial timing flag or second ordinary command is present in the contributor path."
  </behavior>
  <action>
    Start by revising the Phase 233 contract to describe the deliberate one-owner model. In `mix.exs`, replace only the adjacent ordinary-test and `ci.install_golden` legs of the `ci` alias with one `cmd bash scripts/ci/library-economics.sh` leg. Leave `ci.install_golden` as the sole six-path list and preserve all other parity legs and their order. Update assertions to require one harness call, one internal `mix ci.install_golden`, exact live-tagged receiver equality, uniqueness, and absence of copied receiver paths in the harness. Retain a structural assertion that the ordinary runner uses two formatters in one invocation and no duplicate full-suite path exists.
  </action>
  <acceptance_criteria>
    - `mix ci` has one economics harness leg and no direct duplicate ordinary/install legs.
    - The six required install/scaffold paths remain exact, unique, and PR-owned through `ci.install_golden`.
    - The focused Phase 233 contract passes against the new topology and mutation directions.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
    <fails_when>the command exits non-zero; ExUnit reports any failure or zero tests; the harness/receiver is omitted or duplicated; the live tagged universe differs; or a prohibited execution shape is accepted</fails_when>
  </verify>
  <done>The contributor command owns both work classes exactly once through one fixed harness while all non-test parity and receiver coverage remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach validated receipts to the sole CI owner without changing the aggregate (D-01, D-02, D-04, D-09)</name>
  <reversibility rating="costly">The workflow contract and protected evidence consumers depend on exact job names and ownership, so reversal requires coordinated workflow/test updates.</reversibility>
  <files>.github/workflows/ci.yml, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` `library_tests_shard` and `library_tests` blocks — modify only the direct owner; preserve the aggregate block byte-for-byte.
    - Existing pinned `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` steps — reuse the pin and retention convention.
    - `test/support/ci/ex_unit_timing_formatter.ex` path allowlist — use `/tmp/sigra-library-1-timings.json` exactly.
    - Plan 01 producer/verifier paths and exit contracts — validation is authoritative and uploads remain diagnostic under failure.
  </read_first>
  <behavior>
    - "Workflow contains one `library_tests_shard`, one bare `Library tests`, and one `MIX_ENV=test mix ci` across library jobs."
    - "The owner sets exact per-test and economics receipt paths, validates both after the contributor command even on failure, and uploads each separately even on failure."
    - "Both upload steps use the pinned action and `if-no-files-found: error`."
    - "The aggregate still depends only on `library_tests_shard`, uses `if: always()`, and fails unless that owner succeeded."
  </behavior>
  <action>
    Add the two fixed receipt environment values to the current contributor step, then add always-run validation and separate upload steps inside `library_tests_shard`. Validate the per-test receipt's exact schema, nonempty bounded rows, unique identities, and deterministic sort using the existing formatter contract; invoke `verify-library-economics.sh` for class evidence. Upload with stable run-qualified names, the existing pinned upload action, seven-day retention, and missing-file errors. Keep upload diagnostic behavior from masking the contributor/verifier exit. Do not edit the `library_tests` aggregate block. Expand the Phase 233 contract to assert exact owner count, one contributor invocation, pinned uploads, fail-closed missing-file behavior, fixed paths, and aggregate byte stability.
  </action>
  <acceptance_criteria>
    - Both receipts are validated and uploaded from the sole owner on success and retained diagnostically on failure.
    - Missing or malformed receipt data makes the owner red; artifact absence is not a warning.
    - The workflow contains no second owner, matrix, serial flag, duplicate ordinary command, or renamed aggregate.
    - Actionlint and the focused topology contract pass.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/ci.yml &amp;&amp; bash scripts/ci/library-economics.test.sh &amp;&amp; bash scripts/ci/verify-library-economics.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
    <fails_when>any command exits non-zero; Actionlint reports a diagnostic; either shell PASS banner is absent; ExUnit reports any failure or zero tests; artifact validation is advisory; or the aggregate/owner topology drifts</fails_when>
  </verify>
  <done>The sole CI owner executes contributor parity once and publishes independently validated slow-test and two-class economics evidence without changing the protected aggregate.</done>
</task>

</tasks>

## Artifacts this phase produces

- `mix.exs` — `ci` alias consumes one `library-economics.sh` leg; `ci.install_golden` remains the sole six-path receiver.
- `.github/workflows/ci.yml` — sole owner validates and uploads two exact receipts; protected aggregate remains unchanged.
- `test/sigra/planning/phase_233_library_economics_contract_test.exs` — one-owner, exact-once, formatter, upload, and aggregate regression contract.
- New files: None.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mix ci` alias to shell harness | Contributor and CI execution enters the new orchestration seam. |
| CI filesystem to uploaded artifacts | Local receipt bytes become durable GitHub evidence. |
| Direct owner to protected aggregate | Job result controls the required `Library tests` context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-235.1-02-01 | Tampering | `mix ci` execution ownership | high | mitigate | Structural contract requires one harness, one install alias, exact receiver universe, and one workflow owner. |
| T-235.1-02-02 | Repudiation | missing/malformed artifacts | high | mitigate | Always-run independent validation plus `if-no-files-found: error` uploads and exact schema checks. |
| T-235.1-02-03 | Tampering | protected aggregate | high | mitigate | Byte-stable aggregate assertion and exact `needs`/result contract. |
| T-235.1-02-04 | Denial of Service | duplicate suite execution | medium | mitigate | Count commands and owners; reject matrix restoration, duplicate ordinary commands, and copied receiver paths. |
| T-235.1-02-SC | Tampering | GitHub Action supply chain | low | accept | Reuse the repository's already-pinned upload-artifact SHA; add no package installation. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Single owner with production evidence and preserved aggregate | 235.1-02 | COVERED | Alias/workflow wiring and contract. |
| REQ | TEST-01 | Timing formatter has a production consumer | 235.1-02 | COVERED | Fixed path, validation, upload. |
| REQ | TEST-02 | Comparable work classes exist in current owner | 235.1-02 | COVERED | Harness is current `mix ci` test leg. |
| REQ | TEST-03 | Install coverage remains exact-once on PR | 235.1-02 | COVERED | Sole receiver and live tagged-universe equality. |
| RESEARCH | — | Preserve aggregate; attach evidence to direct owner | 235.1-02 | COVERED | Workflow contract enforces both. |
| CONTEXT | D-01 | Preserve one owner | 235.1-02 | COVERED | One job/one invocation assertions. |
| CONTEXT | D-02 | One-owner work-class reinterpretation | 235.1-02 | COVERED | Two observables replace retired job pair. |
| CONTEXT | D-04 | CI sets and preserves formatter receipt | 235.1-02 | COVERED | Fixed env, validation, upload. |
| CONTEXT | D-05 | Keep all coverage on PR | 235.1-02 | COVERED | `mix ci` and receiver unchanged in scope. |
| CONTEXT | D-06 | Time both portions and retain exact-once | 235.1-02 | COVERED | Harness plus topology tests. |
| CONTEXT | D-09 | Preserve protected aggregate | 235.1-02 | COVERED | Byte-stable contract. |

<verification>
Run Actionlint, both hermetic shell suites, and the revised Phase 233 topology contract. The plan passes only with one owner/one contributor invocation, exact receiver coverage, two fail-closed artifacts, and an unchanged protected aggregate.
</verification>

<success_criteria>
- `mix ci` remains the only contributor-facing command and reaches each required test exactly once.
- CI is a real consumer of both receipts.
- Missing/malformed evidence fails the direct owner.
- `Library tests` retains its exact protected name and dependency semantics.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-02-SUMMARY.md` when done.
</output>
