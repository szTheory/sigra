---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 13
type: execute
wave: 11
depends_on: [235.1-12]
files_modified:
  - test/support/ci/ex_unit_timing_formatter_test.exs
  - scripts/ci/library-partitions.sh
  - scripts/ci/library-partitions.test.sh
  - test/support/install_fixture.ex
  - test/support/install_fixture_performance_test.exs
  - scripts/ci/verify-library-install-golden.sh
  - scripts/ci/verify-library-install-golden.test.sh
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The formatter ExUnit test owns only the scaffold timing path and contains no filesystem write, remove, or cleanup ownership for either ordinary receipt path."
    - "A real complete partition-2 ExUnit lifetime finishes without changing or deleting the already-produced partition-1 receipt."
    - "No-compile installer stdout removes only contiguous leading canonical unavailable-app warning blocks for `:sigra_install_golden_tmp` and `:phoenix_live_view`; every other warning and installer byte remains authoritative."
    - "The install-golden verifier discovers the exact scaffold owner set deterministically with POSIX find/grep primitives and passes when `rg` is absent."
    - "Plans 10/12 and all eight failed runs remain immutable negative history; this local plan creates no live evidence."
  artifacts:
    - path: test/support/ci/ex_unit_timing_formatter_test.exs
      provides: scaffold-only test receipt ownership
    - path: scripts/ci/library-partitions.test.sh
      provides: source-ownership mutation gate and real post-partition-2 lifetime regression
    - path: test/support/install_fixture.ex
      provides: strict allowlisted leading unavailable-app warning normalization
    - path: scripts/ci/verify-library-install-golden.sh
      provides: portable exact scaffold ownership discovery
  key_links:
    - from: formatter test source
      to: ordinary receipt lifetime
      via: AST/source ownership rejection plus real sequential runner execution
    - from: no-compile Mix stdout
      to: golden stdout assertion
      via: canonical leading-block parser with explicit application allowlist
    - from: tracked scaffold tests
      to: install evidence verifier
      via: deterministic safe-path find/grep discovery and lexical ordering
---

<objective>
Repair the three deterministic repository defects exposed by Plan 12 without changing CI topology, test coverage, warning policy, or evidence predicates.

Purpose: Make both separated routes executable on the GitHub Linux environment while preventing another locally invisible receipt-lifetime or tool-availability gap.
Output: Scaffold-only formatter test ownership, strict no-compile stdout normalization, portable scaffold discovery, and adversarial integration contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-10-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-11-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-12-SUMMARY.md
@test/support/ci/ex_unit_timing_formatter_test.exs
@scripts/ci/library-partitions.sh
@scripts/ci/library-partitions.test.sh
@test/support/install_fixture.ex
@test/support/install_fixture_performance_test.exs
@scripts/ci/verify-library-install-golden.sh
@scripts/ci/verify-library-install-golden.test.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove ordinary receipt ownership from the formatter test and prove the real lifetime boundary (D-03, D-04)</name>
  <files>test/support/ci/ex_unit_timing_formatter_test.exs, scripts/ci/library-partitions.sh, scripts/ci/library-partitions.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - The formatter write test names, writes, reads, and cleans only `/tmp/sigra-library-scaffold-timings.json`.
    - A whole-source AST or equivalent mutation gate rejects `File.write`, `File.rm`, `File.rm_rf`, or `on_exit` ownership involving either exact ordinary timing path, including aliases assigned before the call.
    - The production `library-partitions.sh` runs both real ordinary partitions; after partition 2 and all ExUnit `on_exit` callbacks finish, partition 1 remains a regular non-symlink file with the digest captured immediately after partition 1.
  </behavior>
  <action>Start RED against the current lines that write both ordinary receipts and remove them in `on_exit`. Build a whole-source ownership checker by parsing the formatter test AST (or an equivalently exhaustive mutation contract) rather than checking only its `path =` line: resolve literal/variable references to both exact ordinary paths and reject their use as a filesystem mutation target or inside cleanup callbacks. Include mutations for direct literals, aliases, `File.write!`, `File.rm`, `File.rm!`, `File.rm_rf`, and `on_exit`. Then delete every ordinary receipt variable, write, assertion, and cleanup from the ExUnit test; retain only scaffold-path setup/write/assertion/cleanup. Harden the production runner itself: immediately after successful partition 1, require its timing receipt to be a regular non-symlink file and capture its digest; after partition 2 and all ExUnit callbacks exit, require the same file and digest before publishing success. Extend the runner regression beyond the fake `mix` child by executing the production runner with the real partition oracle and real ExUnit suites, then independently validate both final receipts. Keep the hermetic fake-child status tests as fast unit coverage, but do not treat them as the lifetime proof. Preserve same-run parallel formatter behavior, partition manifests, timing schemas, and the historical Plan 12 missing-receipt diagnosis.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; bash scripts/ci/library-partitions.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test bash scripts/ci/library-partitions.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 bash scripts/ci/verify-library-partitions.sh</automated>
  </verify>
  <done>The test source has no ordinary receipt mutation authority, and a real completed partition-2 lifecycle leaves partition 1 present and byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize only allowlisted leading no-compile unavailable-app warnings (D-05 superseded, D-09)</name>
  <files>test/support/install_fixture.ex, test/support/install_fixture_performance_test.exs</files>
  <behavior>
    - Zero canonical leading warning blocks return the input byte-for-byte.
    - One or repeated contiguous canonical blocks for only `:sigra_install_golden_tmp` and `:phoenix_live_view` are removed, including the observed two-plus-one sequence.
    - A forbidden application, mismatched application names within a block, altered wording/spacing, a non-leading allowed block, or any other warning remains byte-for-byte visible.
    - Compile commands and warnings-as-errors checks are unchanged; normalization applies only to captured stdout from the existing no-start/no-compile installer entrypoint.
  </behavior>
  <action>Start RED with exact fixtures for zero, one, repeated, and mixed allowed leading blocks, plus forbidden-app, mismatched-name, noncanonical, non-leading, and trailing installer-output mutations. Replace the single phoenix-only anchored regex with a strict prefix consumer for the canonical Mix unavailable-application block. Extract the application atom from each complete leading block, require every repeated mention in that block to match, and remove it only when it is in the fixed allowlist `sigra_install_golden_tmp` or `phoenix_live_view`; continue while the next complete leading block is allowed. Stop without consuming at the first forbidden or noncanonical byte and preserve that byte plus the entire remaining stdout exactly. Do not broadly filter warning lines, normalize internal whitespace, strip unknown applications, or change `compile_variant!` warning rejection, compile commands, golden installer output, or warnings-as-errors gates. Expose a narrow test seam if needed so focused tests exercise the production parser directly.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/install_fixture_performance_test.exs &amp;&amp; bash scripts/ci/install-golden.test.sh</automated>
  </verify>
  <done>The observed no-compile environment prefix is removed exactly, while every non-allowlisted/noncanonical warning and all installer-owned bytes remain observable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make exact scaffold ownership verification portable without ripgrep (D-05 superseded, D-09)</name>
  <files>scripts/ci/verify-library-install-golden.sh, scripts/ci/verify-library-install-golden.test.sh, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <behavior>
    - The verifier derives only tracked test-shaped files containing an exact line-level `@moduletag :scaffold` declaration using find/grep primitives available on the runner.
    - Discovered paths are validated against the repository-relative safe path alphabet, deduplicated, and sorted under `LC_ALL=C` before JSON conversion.
    - The valid receipt passes under a hermetic PATH containing required POSIX tools and no `rg`; missing, duplicate, unsafe, reordered, extra, or non-exact tag ownership still fails closed.
  </behavior>
  <action>Start RED with a hermetic PATH/no-rg execution of the production verifier and ownership fixtures covering spaces/newlines or other unsafe names, misleading comments, substring tags, duplicates, and lexical-order drift. Replace `rg` with a fixed `find test -type f -name '*_test.exs'` traversal plus `grep` exact line-expression matching. Quote every path, terminate option parsing, require every match to resolve as an exact tracked path through `git ls-files --error-unmatch`, validate each repository-relative path before newline aggregation, fail on unsafe delimiter-bearing paths rather than ambiguously serializing them, sort with `LC_ALL=C`, reject duplicates, and convert only the validated ordered list to JSON. Do not add package installation, an `rg` fallback, shell glob ownership, a copied six-path allowlist, or trust the producer receipt as the live oracle. Ensure the hermetic test PATH exposes only explicitly enumerated required commands and cannot accidentally resolve `rg` from the host.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-install-golden.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; actionlint .github/workflows/ci.yml &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh</automated>
  </verify>
  <done>The production exact-six verifier passes without ripgrep and rejects every unsafe or inexact ownership mutation deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test helper to shared receipt path | A unit-test cleanup callback can delete evidence from an earlier partition. |
| No-compile runtime noise to golden stdout | Narrow environment warnings must be separated without laundering installer warnings. |
| Repository paths to shell/JSON | Filesystem names and tag text become exact ownership evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-13-01 | Tampering | ordinary receipts | high | mitigate | Whole-source mutation rejection and real post-partition-2 digest assertion. |
| T-235.1-13-02 | Repudiation | stdout warnings | high | mitigate | Canonical prefix grammar, repeated atom equality, two-atom allowlist, adverse fixtures. |
| T-235.1-13-03 | Spoofing | scaffold ownership | high | mitigate | Exact tag expression, safe relative paths, deduplication, deterministic sort. |
| T-235.1-13-04 | Denial of Service | verifier dependency | medium | mitigate | POSIX find/grep discovery and hermetic no-rg execution. |
| T-235.1-13-05 | Elevation of Privilege | broad normalization | high | mitigate | Stop at first forbidden/noncanonical byte; compile warning gates remain unchanged. |
| T-235.1-13-SC | Tampering | package supply chain | low | accept | No package/action/dependency change; verifier uses existing runner tools. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Executable measurable PR and exact-six non-PR routes | 13 | COVERED | Repairs all three Plan 12 deterministic failures. |
| REQ | TEST-01 | Same-run parallel timing receipt survives | 13 | COVERED | Source ownership plus real lifetime proof. |
| REQ | TEST-02 | Both ordinary receipts reach independent verifier | 13 | COVERED | Post-partition-2 digest and full verifier. |
| REQ | TEST-03 | Exact-six non-PR run and verifier execute on Linux | 13 | COVERED | Strict stdout prefix and no-rg discovery. |
| RESEARCH | — | Fail-closed exact evidence without warning laundering | 13 | COVERED | Parser and path mutations preserve all unknown input. |
| CONTEXT | D-01, D-03, D-04 | Single owner and same-run formatter | 13 | COVERED | Task 1 changes only test ownership/verification. |
| CONTEXT | D-05, D-06 | Exact-six non-PR fallback | 13 | COVERED | Tasks 2/3 preserve route and receiver set. |
| CONTEXT | D-07, D-08, D-09 | Evidence-first closure and protected history | 13 | COVERED | No truth status/evidence created; protected verifiers run. |

<verification>
Run the real two-partition producer and verifier, focused formatter/fixture/adverse tests, the hermetic no-rg verifier suite, topology contracts, Actionlint, and both protected offline verifiers. Dispatch no GitHub workflow.
</verification>

<success_criteria>
- Formatter tests cannot write or clean either ordinary receipt path.
- A real partition-2 lifecycle preserves partition-1 evidence.
- Only exact allowed leading no-compile warning blocks are stripped.
- Exact-six ownership verification succeeds without ripgrep and remains fail-closed.
- Plans 10/12 and all failed runs remain unchanged and non-authoritative.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-13-SUMMARY.md` when done.
</output>
