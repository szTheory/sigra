---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 08
type: execute
wave: 6
depends_on: [235.1-07]
files_modified:
  - test/support/ci/library_test_partitions.exs
  - test/support/ci/library_test_partitions_test.exs
  - scripts/ci/library-partitions.sh
  - scripts/ci/library-partitions.test.sh
  - scripts/ci/verify-library-partitions.sh
  - scripts/ci/verify-library-partitions.test.sh
  - mix.exs
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The sole PR `library_tests_shard` still invokes literal `MIX_ENV=test mix ci` exactly once, and `Library tests` remains byte-identical to the ruleset context."
    - "One owner runs two non-empty, exhaustive, disjoint ordinary-test partitions sequentially; ExUnit remains parallel inside each partition and no scaffold path leaks into either partition."
    - "Each partition emits a deterministic per-test receipt and the owner emits independently verified raw wall durations whose ratio is at most 2.0."
  artifacts:
    - path: scripts/ci/library-partitions.sh
      provides: fixed two-partition runner and raw combined receipt
    - path: scripts/ci/verify-library-partitions.sh
      provides: fail-closed independent coverage and comparability authority
    - path: test/support/ci/library_test_partitions.exs
      provides: exhaustive deterministic partition manifests
  key_links:
    - from: mix.exs
      to: scripts/ci/library-partitions.sh
      via: one fixed `cmd bash` alias leg
    - from: library_tests_shard
      to: mix ci
      via: one direct literal invocation
    - from: partition manifests
      to: timing receipts
      via: exact file ownership and per-test file membership
---

<objective>
Restore TEST-01/02 through two measured ordinary partitions inside the existing single PR owner.

Purpose: The operator's 2026-09-10 fallback supersedes Phase 235.1 D-02's ordinary-vs-scaffold comparison after repeated real evidence proved that topology infeasible without weakening coverage. D-01, D-03, and D-04 remain binding: one direct `mix ci` owner, parallel ExUnit timing, and deterministic receipts.
Output: A fixed partition runner/verifier, PR wiring, and mutation-tested ownership contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-06-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-07-SUMMARY.md
@test/support/ci/library_test_partitions.exs
@test/support/ci/ex_unit_timing_formatter.ex
@mix.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one exhaustive partition pair through deterministic receipts (D-01, D-03, D-04)</name>
  <files>test/support/ci/library_test_partitions.exs, test/support/ci/library_test_partitions_test.exs, scripts/ci/library-partitions.sh, scripts/ci/library-partitions.test.sh</files>
  <behavior>
    - The current ordinary universe is exactly partition 1 union partition 2; both are non-empty, disjoint, lexical, and contain none of the six scaffold paths.
    - The fixed runner executes partition 1 then partition 2, each through `mix test` with both `ExUnit.CLIFormatter` and `Sigra.CI.ExUnitTimingFormatter`; it never uses `--slowest`, `--trace`, a bare full-suite command, `eval`, or caller-selected commands.
    - A child failure writes the raw combined receipt and propagates the first non-zero status; partition 2 does not conceal partition 1 failure.
  </behavior>
  <action>First add failing tests for exhaustive live-universe equality, scaffold exclusion, deterministic longest-processing-time assignment, exact tie behavior, fixed receipt paths `/tmp/sigra-library-1-timings.json`, `/tmp/sigra-library-2-timings.json`, and sequential execution. Evolve `Sigra.CI.LibraryTestPartitions` from inert Phase 233 history into the fail-closed production test manifest consumer requested by the operator; retain its measured-cost assignment and reject missing/stale/duplicate paths. Implement `library-partitions.sh` with fixed commands and a monotonic millisecond clock. Emit `/tmp/sigra-library-partitions.json` using schema `sigra.library-partitions/v1`: exact top-level keys `schema_version`, `execution_mode`, `ordinary_universe`, and `partitions`; `execution_mode` is `sequential`; `ordinary_universe` contains sorted current paths/count and zero-valued missing/stale/duplicate/scaffold-leak arrays; each of exactly two ordered partition objects contains id, sorted paths, manifest SHA-256, fixed timing receipt path, start/end/duration integers, conclusion, and exit status. Do not alter the six receiver modules or revert the fixture-DAG optimizations from Plans 06/07.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/support/ci/ex_unit_timing_formatter_test.exs &amp;&amp; bash scripts/ci/library-partitions.test.sh</automated>
  </verify>
  <done>One production-quality runner executes the exact ordinary universe once across two deterministic partitions and preserves raw receipts on success and failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute partition coverage and comparability independently (D-03)</name>
  <files>scripts/ci/verify-library-partitions.sh, scripts/ci/verify-library-partitions.test.sh</files>
  <behavior>
    - The verifier accepts only exact schema, two successful positive-duration partitions, exhaustive disjoint ownership, exact manifest digests, and a maximum/minimum duration ratio no greater than 2.0.
    - Missing, symlinked, malformed, duplicate-key, oversized, extra-key, failed-child, path-leak, universe-drift, digest, receipt-membership, arithmetic, ordering, and ratio mutations each fail with no PASS banner.
  </behavior>
  <action>Build an offline verifier over the three fixed receipts. Bound file type and size, reject duplicate JSON keys, and compare combined-receipt manifests with both per-test receipts and a freshly computed `Sigra.CI.LibraryTestPartitions` universe. Recompute durations from end minus start and compare integers as `maximum * 1000 &lt;= minimum * 2000`; do not trust a producer verdict boolean. Require every per-test receipt's partition id, exact schema, unique `{file,module,name}`, deterministic descending-time/lexical ordering, zero failed/invalid outcomes, and file membership in only its manifest. Mutation-test every authority field.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; shellcheck scripts/ci/library-partitions.sh scripts/ci/library-partitions.test.sh scripts/ci/verify-library-partitions.sh scripts/ci/verify-library-partitions.test.sh</automated>
  </verify>
  <done>Stored producer claims cannot turn an incomplete, overlapping, failed, or imbalanced pair into passing evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the partition pair into the sole PR alias owner (D-01, D-03)</name>
  <files>mix.exs, .github/workflows/ci.yml, test/sigra/planning/phase_198_contributor_dx_contract_test.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <action>Replace the obsolete `library-economics.sh` alias leg with exactly one fixed `library-partitions.sh` leg while preserving format, locked-dependency, warnings-as-errors compile, and `sigra.dep_off` in order. In `library_tests_shard`, preserve one literal `MIX_ENV=test mix ci`, remove PR-only phx_new setup and ordinary-vs-scaffold validation/uploads, validate the combined and both timing receipts under `if: always()`, and upload three run/attempt-qualified artifacts with the existing pinned `actions/upload-artifact` SHA, `if-no-files-found: error`, and seven-day retention. Keep the entire `library_tests` job byte-identical, including `needs: [library_tests_shard]`, and retain its SHA-256 ratchet. Update all three topology contracts to assert exactly two exhaustive sequential ordinary partitions, ExUnit parallelism within each, no scaffold leakage, one direct owner, no matrix, no serial slowest rerun, and no change to FAST-01/GATE-05 protected evidence. Mark the old ordinary-vs-scaffold non-dominance predicate as empirically superseded, never passed.</action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml &amp;&amp; bash scripts/ci/library-partitions.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh</automated>
  </verify>
  <done>The PR owner remains singular and ruleset-compatible while producing two exhaustive, comparable ordinary-partition receipts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Partition oracle to Mix commands | Generated paths control which tests execute. |
| ExUnit events to CI artifacts | Test lifecycle data becomes completion evidence. |
| Required aggregate to ruleset | Any byte/name drift can strand PRs pending. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-08-01 | Tampering | partition manifests | high | mitigate | Live-universe equality, disjointness, scaffold exclusion, and digest recomputation. |
| T-235.1-08-02 | Spoofing | timing receipts | high | mitigate | Exact fixed paths/schema plus cross-receipt membership and arithmetic verification. |
| T-235.1-08-03 | Denial of Service | sequential partition runner | medium | mitigate | Positive bounded durations, child-status propagation, existing job timeout. |
| T-235.1-08-04 | Elevation of Privilege | aggregate context | high | mitigate | Preserve byte hash and sole-owner dependency exactly. |
| T-235.1-08-SC | Tampering | package supply chain | low | accept | No package install or dependency change is introduced. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Measurable PR library economics under one owner | 08 | COVERED | Two ordinary partitions restore the meaningful comparison. |
| REQ | TEST-01 | Slow visibility without serial ExUnit | 08 | COVERED | Same-run formatter on parallel ExUnit partitions. |
| REQ | TEST-02 | Two comparable exhaustive library partitions | 08 | COVERED | Ratio recomputed from raw wall durations. |
| RESEARCH | — | Fail-closed receipts and immutable aggregate | 08 | COVERED | Reuses established formatter/artifact patterns. |
| CONTEXT | D-01, D-03, D-04 | One owner and deterministic same-run timing | 08 | COVERED | Preserved. |
| CONTEXT | D-02 | Compare ordinary vs scaffold | 08 | SUPERSEDED | Operator directive follows repeated genuine infeasibility evidence; history remains in Plan 04 summary. |

<verification>
Run runner/verifier mutation suites, focused ExUnit topology contracts, Actionlint, and both protected terminal verifiers.
</verification>

<success_criteria>
- The exact ordinary universe executes once across two non-empty partitions.
- ExUnit remains parallel within each partition and both deterministic receipts upload fail-closed.
- Raw wall durations satisfy the fixed 2.0 comparison on live evidence before TEST-02 closes.
- The PR owner, required aggregate bytes, FAST-01, and GATE-05 remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-08-SUMMARY.md` when done.
</output>
