---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/library-economics.sh
  - scripts/ci/library-economics.test.sh
  - scripts/ci/verify-library-economics.sh
  - scripts/ci/verify-library-economics.test.sh
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-03 and D-04, one ordinary parallel ExUnit execution emits the existing deterministic per-test timing receipt; no timing-only rerun or serial test mode exists."
    - "Per D-06, the same harness records raw positive integer milliseconds for exactly two classes, ordinary and install_scaffold, while returning the first failing child status."
    - "The independent verifier recomputes max/min <= 2.0 and install_scaffold <= ordinary from raw durations and never trusts producer verdict booleans."
    - "Missing, malformed, zero, negative, duplicate, oversized, or incomplete evidence fails with a diagnostic non-zero exit."
  artifacts:
    - path: "scripts/ci/library-economics.sh"
      provides: "Fixed-command two-leg producer and exact-schema diagnostic receipt"
    - path: "scripts/ci/verify-library-economics.sh"
      provides: "Independent exact-schema economics verifier"
    - path: "scripts/ci/library-economics.test.sh"
      provides: "Hermetic producer status, command, path, and receipt tests"
    - path: "scripts/ci/verify-library-economics.test.sh"
      provides: "Boundary and adverse mutation tests for the verifier"
  key_links:
    - from: "scripts/ci/library-economics.sh"
      to: "Sigra.CI.ExUnitTimingFormatter"
      via: "ordinary mix test receives both ExUnit.CLIFormatter and Sigra.CI.ExUnitTimingFormatter with the fixed timing path"
    - from: "scripts/ci/library-economics.sh"
      to: "mix ci.install_golden"
      via: "one fixed second-leg command after ordinary succeeds"
    - from: "scripts/ci/verify-library-economics.sh"
      to: "/tmp/sigra-library-economics.json"
      via: "exact key/type validation and integer cross-product recomputation"
  prohibitions:
    - "MUST NOT execute a second ordinary suite, a serial timing mode, or a timing-only rerun."
    - "MUST NOT accept caller-selected executables, arbitrary output paths, eval-built commands, or producer-owned pass booleans as authority."
    - "MUST NOT accept missing classes, non-positive durations, duplicate keys, extra keys, or unbounded per-test evidence."
---

<objective>
Create the production-quality tracer from the single contributor command through both real library work classes to deterministic per-test and class-economics receipts, then independently verify the raw result.

Purpose: Restore the measurable TEST-01/02/03 observability seam before changing CI topology or milestone truth.
Output: A fixed-command fail-closed producer, independent verifier, and hermetic positive/adverse self-tests.

Decision coverage: D-03, D-04, and D-06 are implemented directly; D-01, D-02, D-05, D-07, D-08, and D-09 remain protected constraints for later plans.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@test/support/ci/ex_unit_timing_formatter.ex
@scripts/ci/sigra-dep-off.sh
@scripts/ci/docs-only-receipt.sh
@scripts/ci/docs-only-receipt.test.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one successful ordinary/install execution through both receipts (D-03, D-04, D-06)</name>
  <reversibility rating="reversible">The new scripts are isolated CI helpers with no database, package, or published API mutation.</reversibility>
  <files>scripts/ci/library-economics.sh, scripts/ci/library-economics.test.sh, scripts/ci/verify-library-economics.sh, scripts/ci/verify-library-economics.test.sh</files>
  <read_first>
    - `test/support/ci/ex_unit_timing_formatter.ex` — reuse its fixed `/tmp/sigra-library-1-timings.json` allowlist, atomic rename, `:test_finished` collection, descending duration order, and lexical tie behavior.
    - `scripts/ci/sigra-dep-off.sh` — copy the strict root resolution, status preservation, signal handling, and cleanup discipline.
    - `scripts/ci/docs-only-receipt.sh` and `.test.sh` — copy exact input validation, accumulated diagnostics, shadowed-command fixtures, and counted terminal PASS/FAIL banners.
    - `235.1-RESEARCH.md` Code Examples and Pitfalls 1–4 — fixed commands, exact class names, positive millisecond values, first-failure propagation, and no duplicated receiver paths.
  </read_first>
  <behavior>
    - "A shadowed successful ordinary command runs first with `MIX_TEST_PARTITION=ordinary`, the fixed timing path, CLI formatter, and Sigra timing formatter; one `mix ci.install_golden` follows."
    - "The producer writes schema `sigra.library-economics/v1` with exact top-level and class key sets, integer monotonic start/end/duration values, conclusions, exit statuses, and install_leg_ran=true."
    - "The independent verifier accepts equality at the 2.0 ratio boundary and equality at install_scaffold == ordinary."
    - "The per-test receipt is generated by the ordinary command itself and is not synthesized by the harness."
  </behavior>
  <action>
    Start RED with hermetic shell tests that shadow `mix` and the millisecond clock through a temporary `PATH`, then implement `library-economics.sh` with repository-owned command arrays only. Run the ordinary command exactly once as `mix test --exclude scaffold` with both formatter arguments and the existing fixed timing path, then run `mix ci.install_golden` exactly once only after ordinary succeeds. Record monotonic raw integer milliseconds for `ordinary` and `install_scaffold`; always atomically write a bounded diagnostic JSON receipt at the exact allowlisted `/tmp/sigra-library-economics.json` path, use private temporary permissions, and exit with the first non-zero child status.

    Implement `verify-library-economics.sh` as a separate authority over the fixed receipt. Require exact object keys, schema and class names, integer positive durations, matching start/end arithmetic, successful conclusions, zero exit statuses, and a true install-leg execution fact. Recompute comparability with integer cross-products (`max * 1000 <= min * 2000`) and non-dominance (`install_scaffold <= ordinary`); do not consume a stored pass field. The happy-path self-test must execute producer then verifier end to end with no network, database, or real Mix runtime.
  </action>
  <acceptance_criteria>
    - One hermetic invocation demonstrates the fixed ordinary command, formatter environment, one install alias call, raw receipt, and independent successful verification end to end.
    - Exactly two work classes exist; both durations are positive raw integers and equality passes both applicable boundaries.
    - The harness returns child failures rather than the receipt writer's status and never manufactures the per-test receipt.
    - Temporary cleanup validates its target before recursive deletion and output publication is atomic.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/library-economics.test.sh &amp;&amp; bash scripts/ci/verify-library-economics.test.sh</automated>
    <fails_when>either script exits non-zero; either terminal PASS banner is absent; the stub records a different command count/order/environment; a receipt key/type differs; or either equality-boundary fixture is rejected</fails_when>
  </verify>
  <done>The thinnest real path executes both fixed work classes once, preserves same-run timing, emits both receipts, and passes independent raw-value verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand failure propagation and adversarial evidence rejection (D-03, D-06)</name>
  <reversibility rating="reversible">All changes remain within new scripts and hermetic tests.</reversibility>
  <files>scripts/ci/library-economics.sh, scripts/ci/library-economics.test.sh, scripts/ci/verify-library-economics.sh, scripts/ci/verify-library-economics.test.sh</files>
  <read_first>
    - The four files completed by Task 1 — preserve the happy-path interface and terminal banners.
    - `235.1-RESEARCH.md` Security Domain and Common Pitfalls — required fixed-path, forged-boolean, hidden-failure, malformed-evidence, injection, size, and duplication defenses.
  </read_first>
  <behavior>
    - "An ordinary failure records install_leg_ran=false, never invokes install, retains diagnostics, and exits with the ordinary status."
    - "An install failure records both legs and exits with the install status; receipt writing cannot turn it green."
    - "Zero/negative/fractional durations, missing/extra keys, absent/duplicate classes, malformed JSON, failed conclusions, false execution facts, ratio above 2.0, and install dominance all fail."
    - "A forged producer pass boolean and a mutated threshold do not influence the verifier's recomputation."
    - "At most the bounded deterministic per-class payload is accepted; oversized or duplicate evidence is rejected."
  </behavior>
  <action>
    Expand the runner self-test with one-pole-at-a-time fixtures for ordinary failure, install failure, skipped second leg, missing per-test output, signal exit, and receipt-write failure. Assert invocation count/order and the original child status in every case. Expand the verifier test with locally generated JSON mutations for every exact-schema, type, positivity, arithmetic, conclusion, execution, comparability, non-dominance, and bounded-size predicate. Include a fixture carrying a misleading producer verdict and prove it cannot override raw values. Keep tests hermetic and avoid error-suppressing fallback pipelines.
  </action>
  <acceptance_criteria>
    - Every listed failure direction is exercised independently and produces a non-zero status plus a diagnostic naming the failed predicate.
    - No failure path invokes a hidden duplicate ordinary run or allows receipt/upload success to mask child failure.
    - The verifier rejects 2.0-plus-one-millisecond comparability, install one millisecond above ordinary, empty input, one-class input, null input, unstable/duplicate class representation, and extra schema keys.
    - ShellCheck passes on all four scripts without suppressing a relevant warning.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/library-economics.test.sh &amp;&amp; bash scripts/ci/verify-library-economics.test.sh &amp;&amp; shellcheck scripts/ci/library-economics.sh scripts/ci/library-economics.test.sh scripts/ci/verify-library-economics.sh scripts/ci/verify-library-economics.test.sh</automated>
    <fails_when>any command exits non-zero; a listed mutation is accepted; a child failure is masked; a duplicate command is observed; either PASS banner is absent; or ShellCheck reports a diagnostic</fails_when>
  </verify>
  <done>The producer and verifier fail closed in every required direction while retaining useful diagnostics and the successful tracer path.</done>
</task>

</tasks>

## Flagged assumptions

- **TEST-01 unclassified:** Resolved as an executable predicate: the ordinary command includes the existing lifecycle formatter in its sole parallel run, and the harness contains no second ordinary invocation.
- **TEST-02 adjacency:** Exactly equal class durations pass; a ratio exactly 2.0 passes; one millisecond beyond either boundary fails.
- **TEST-02 empty/single/null:** Only an exact two-class object is valid. Empty, null, one-class, duplicate-class, and extra-class representations fail closed.
- **TEST-02 ordering:** Work classes are fixed object keys, so array order is non-authoritative. Per-test rows retain the formatter's existing descending-duration and lexical-tie ordering.
- **TEST-03 unclassified:** Resolved as exact successful install execution plus raw `install_scaffold <= ordinary`; prose or a producer boolean is insufficient.

## Artifacts this phase produces

- `scripts/ci/library-economics.sh` — fixed two-leg runner producing `sigra.library-economics/v1`.
- `scripts/ci/library-economics.test.sh` — hermetic runner command/status/receipt contract.
- `scripts/ci/verify-library-economics.sh` — independent raw-duration verifier.
- `scripts/ci/verify-library-economics.test.sh` — exact-schema, boundary, mutation, and failure-direction contract.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell harness to child Mix processes | Child exit status and output cross into the evidence producer. |
| Producer receipt to independent verifier | Untrusted local JSON must not be allowed to assert its own validity. |
| Shared `/tmp` namespace to repository evidence | Fixed paths may be pre-created or malformed by another process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-235.1-01-01 | Tampering | fixed receipt publication | high | mitigate | Exact allowlist, private temp file, same-directory atomic rename, exact key/type checks, and symlink-safe replacement behavior. |
| T-235.1-01-02 | Repudiation | child process failures | high | mitigate | Capture each child status, record whether install ran, retain diagnostic receipt, and return the first failing child status. |
| T-235.1-01-03 | Tampering | producer verdict fields | high | mitigate | Independent verifier ignores derived trust booleans and recomputes both predicates from positive integer raw durations. |
| T-235.1-01-04 | Elevation of Privilege | command construction | high | mitigate | Repository-owned fixed command arrays only; no eval, caller executable, or arbitrary path input. |
| T-235.1-01-05 | Denial of Service | oversized/duplicate evidence | medium | mitigate | Exact bounded schema, exactly two unique classes, bounded per-test artifact delegated to the existing formatter, and mutation tests. |
| T-235.1-01-SC | Tampering | package supply chain | low | accept | The plan adds no package dependency or package-manager install. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Same-run slow visibility and separately timed work classes | 235.1-01 | COVERED | Production tracer and independent verifier. |
| REQ | TEST-01 | Parallel slow-test visibility | 235.1-01 | COVERED | Existing formatter on sole ordinary run. |
| REQ | TEST-02 | Comparable work classes | 235.1-01 | COVERED | Raw ratio recomputation with exact boundary tests. |
| REQ | TEST-03 | Install/scaffold non-dominance | 235.1-01 | COVERED | Raw non-dominance recomputation and exact execution fact. |
| RESEARCH | — | Fixed paths/commands, first-failure propagation, exact schemas | 235.1-01 | COVERED | Implemented and mutation-tested. |
| CONTEXT | D-03 | Same parallel run; prohibited reruns | 235.1-01 | COVERED | Sole ordinary invocation. |
| CONTEXT | D-04 | Reuse ExUnitTimingFormatter | 235.1-01 | COVERED | Existing formatter is the producer. |
| CONTEXT | D-06 | Same-run class timing and exact-once evidence | 235.1-01 | COVERED | Two fixed commands and raw receipt. |

<verification>
Run both hermetic suites and ShellCheck. Passing means the happy path works end to end and every required malformed, forged, missing, boundary, dominance, duplication, injection, and child-failure direction is rejected.
</verification>

<success_criteria>
- The sole ordinary parallel run emits deterministic per-test timing.
- Exactly two real work classes emit raw positive millisecond timings.
- Independent arithmetic accepts equality boundaries and rejects one-unit violations.
- Child failures, missing evidence, and malformed evidence cannot appear green.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-01-SUMMARY.md` when done.
</output>
