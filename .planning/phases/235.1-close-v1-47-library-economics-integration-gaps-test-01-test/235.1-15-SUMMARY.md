---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 15
subsystem: ci-evidence
tags: [exunit, calibration, deterministic-tests, blocked]
status: blocked
requires:
  - phase: 235.1-13
    provides: repaired receipt ownership, warning normalization, and portable scaffold verification
provides:
  - durable pre-calibration blocker diagnostics
  - zero-admission provenance record for the failed source collection
affects: [TEST-02, 235.1-14]
actuals:
  tokens: 0
  tasks: 0
  commits: 0
tech-stack:
  added: []
  patterns: [fail-closed source-sample admission, deterministic blocker reproduction]
key-files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-15-SUMMARY.md
  modified: []
key-decisions:
  - Admit no calibration sample when either ordinary child fails, regardless of whether receipt files were partially written.
  - Stop before RED or calibration implementation because the exact required source command is deterministically red in an out-of-plan test.
requirements-completed: []
coverage:
  - id: D1
    description: Exactly three successful raw source receipt triples precede calibration implementation.
    requirement: TEST-02
    verification:
      - kind: integration
        ref: scripts/ci/library-partitions.sh#source collection
        status: fail
    human_judgment: false
duration: 13m
completed: 2026-09-10
---

# Phase 235.1 Plan 15: Provenance-Bound Partition Calibration Summary

**Plan 15 is blocked before RED: the first exact source-collection execution failed an existing install-fixture diagnostics test, and an isolated retry reproduced the same deterministic failure, so zero receipt triples were admitted and no calibration was created.**

## Status

- **Plan status:** Blocked before Task 1 RED; Task 2 was not started.
- **Source samples admitted:** 0 of 3.
- **Raw payloads retained in calibration:** 0 of 9.
- **Implementation/test files changed:** None.
- **GitHub actions:** None dispatched or inspected.
- **Passing evidence:** Intentionally absent.

## Source-Collection Failure

The required command was invoked unchanged:

```text
ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test bash scripts/ci/library-partitions.sh
```

The first child suite stopped with:

```text
1) test diagnostics require exact positive phases bounded by raw install duration
   (Sigra.Test.InstallFixturePerformanceTest)
   test/support/install_fixture_performance_test.exs:610
   ** (ArgumentError) invalid install fixture diagnostics
   code: assert :ok = InstallFixture.validate_diagnostics!(receipt)
   stacktrace:
     (sigra 1.5.0) test/support/install_fixture.ex:461:
       Sigra.Test.InstallFixture.validate_diagnostics!/1
     test/support/install_fixture_performance_test.exs:614: (test)

Finished in 81.0 seconds (62.7s async, 18.3s sync)
1 doctest, 1288 tests, 1 failure, 12 skipped
```

Because both child conclusions were not successful, the run did not meet Plan 15 admission rules. No combined or timing payload from it was copied into a repository artifact or treated as a sample.

## Deterministic Reproduction

Following the project instruction to retry a potentially transient failure once, the exact failing test was run in isolation with the ignored self-referential Playwright `node_modules/node_modules` symlink temporarily moved out of the tree and restored afterward:

```text
ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test \
  mix test test/support/install_fixture_performance_test.exs:610 --seed 305972
```

It failed at the same assertion and stack location:

```text
Finished in 2.2 seconds (0.00s async, 2.2s sync)
1 test, 1 failure (27 excluded)
```

This excludes the known ignored Playwright dependency loop as the cause and establishes a deterministic repository-test blocker rather than a transient collection failure.

## Root Cause Boundary

`test/support/install_fixture_performance_test.exs:611-614` constructs a real prepared graph and passes a hard-coded `1_000` millisecond raw duration to `InstallFixture.diagnostic_receipt/2`. `validate_diagnostics!/1` correctly requires the sum of all positive measured phase durations to be at most that supplied raw duration. On this machine the prepared graph's measured phases exceed the artificial 1,000ms ceiling, so the test is timing-sensitive and blocks every exhaustive partition run before Plan 15 can obtain a successful source triple.

The validator behavior is not a Plan 15 calibration defect, and the failing test is outside Plan 15's authorized file ownership. The executor therefore did not change it, exclude it from the exhaustive universe, alter the runner command, weaken the validator, or fabricate source receipts.

## Narrow Predecessor Replan

A bounded predecessor should make the diagnostics unit contract independent of host wall-clock speed while preserving production validation semantics. The narrow repair is to derive the valid fixture's `raw_install_duration_ms` from its measured positive phase sum (and retain explicit below-bound mutation coverage), rather than assert that a real prepared graph always completes within 1,000ms. After that focused test passes, Plan 15 must restart source collection from zero and obtain exactly three fresh successful triples before writing RED tests or a calibration artifact.

## Immutable Negative History

Plan 13 remains blocked history. Its `9,103/2,430ms` and `10,963/4,242ms` measurements were not imported as calibration samples or passing evidence. All eight failed live runs remain unchanged: `34466384009`, `34466384470`, `34467186749`, `34467189602`, `34468109536`, `34468110161`, `34493873867`, and `34493911924`.

## Deviations from Plan

None. The plan required three successful source samples before RED and prohibited incomplete receipt admission. The executor stopped at that boundary.

## TDD Gate Compliance

- RED: Not started.
- GREEN: Not started.
- Reason: the immutable-input source collection prerequisite failed before any source/test modification was permitted.

## Known Stubs

None.

## Self-Check: PASSED

- Plan 13 implementation target files still match commit `997a1bb5` byte-for-byte.
- Zero calibration samples and zero raw payloads were admitted.
- `235.1-PARTITION-CALIBRATION.json` remains absent.
- All five Plan 15 implementation/test targets remain unchanged.
- The temporarily moved ignored Playwright symlink was restored.
- No GitHub workflow was dispatched and no live evidence artifact was created.

---
*Phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test*
*Blocked: 2026-09-10*
