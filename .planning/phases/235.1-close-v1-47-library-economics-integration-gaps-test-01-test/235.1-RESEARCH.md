# Phase 235.1: Close v1.47 Library Economics Integration Gaps - Research

**Researched:** 2026-09-09
**Domain:** Elixir/ExUnit CI observability, fail-closed timing evidence, and milestone traceability
**Confidence:** HIGH for repository architecture; MEDIUM for the proposed quantitative bound

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

### Consolidated library topology

- **D-01:** Preserve the current single `library_tests_shard` execution owner invoking
  `MIX_ENV=test mix ci`; do not restore the slower Phase 233 two-job matrix plus the
  dedicated scaffold receiver.
- **D-02:** Explicitly redefine TEST-02 around comparable, separately timed work classes
  within the one-owner parity run instead of retaining a literal requirement for two CI
  jobs. The evidence must keep library economics measurable rather than merely deleting
  the old claim. — **Reversibility:** costly — reverting this decision requires restoring
  the matrix, its aggregate dependencies, the partition consumer, and protected evidence.

### Same-run slow-test visibility

- **D-03:** Restore TEST-01 by producing deterministic slow-test timing from the same
  parallel ExUnit execution owned by `mix ci`. A serial `--slowest` run, a second full-suite
  invocation, or timing derived from a separate rerun is prohibited.
- **D-04:** Reuse `Sigra.CI.ExUnitTimingFormatter` unless implementation research finds an
  equivalent existing seam with stronger determinism. The CI owner must set the receipt
  path, preserve the artifact, and prove the formatter is a production consumer rather
  than a self-test-only module.

### Install and scaffold economics

- **D-05:** Keep ordinary tests and `ci.install_golden` in the contributor-facing `mix ci`
  alias and on every pull request. Do not demote upgrade, golden-diff, idempotency, or
  scaffold coverage to a non-PR lane.
- **D-06:** Add explicit same-run timing around the ordinary-test and install/scaffold
  portions and retain named exact-once coverage evidence. TEST-03 passes only when the
  measured receipt shows the subprocess-heavy class is not the dominant leg, or when a
  targeted remediation changes that measured result and a fresh receipt proves it.

### Traceability and terminal truth

- **D-07:** Update TEST-01/02/03 requirement wording/status only after the new deterministic
  contracts and same-run evidence pass. Record the TEST-02 reinterpretation explicitly;
  do not silently turn the old two-shard sentence green.
- **D-08:** Reconcile the already-verified DX-01/02/03/04/06 traceability rows from
  `Gaps Found` to `Complete`, and make the Phase 235 verification body agree with its
  passing frontmatter and 11/11 deterministic result.
- **D-09:** Preserve FAST-01's source-complete n=52/p50=469 attestation, its immutable miss
  history, and the protected 93-row GATE-05 ledger byte-for-byte unless a fail-closed
  ownership reconciliation is strictly required by the new timing artifact.

### the agent's Discretion

- Exact receipt schema, helper names, and step labels, following existing machine-readable
  Phase 233/235 evidence patterns.
- Whether work-class timing is implemented by a small Mix task, shell wrapper, or alias
  helper, provided `mix ci` remains the single contributor-facing command and timing cannot
  mask a failed leg.
- The quantitative comparability bound for TEST-02, provided it is justified from current
  same-run measurements and encoded in a deterministic contract rather than prose alone.

### Deferred Ideas (OUT OF SCOPE)

- The automatic todo matcher returned unrelated auth UI, admin UI, installer, security,
  release, Playwright, and repository-administration work based on generic words such as
  “phase,” “run,” and “test.” None were folded into this fixed TEST-01/02/03 closure phase.
- Adjacent CI todos such as `example_unit_smoke` aggregation, recapture mirror reliability,
  and `admin_eval_render` history remain separately tracked because they do not close the
  library-economics flow identified by the fresh audit.
</user_constraints>

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| TEST-01 | Slow-test visibility no longer forces the library suite to run serially. | Reconnect the existing formatter to the ordinary test command inside the sole `mix ci` owner; reject `--trace`, `--slowest`, and any second ordinary-suite invocation. |
| TEST-02 | The current requirement says two library shards must finish comparably; D-02 replaces the obsolete two-job topology with comparable ordinary and install/scaffold work classes inside the one owner. | Record both leg durations in one fail-closed receipt, calculate the ratio from raw integer durations, and commit the reinterpretation with the observed evidence. |
| TEST-03 | Subprocess-heavy install tests must not dominate library wall-clock. | Keep the exact six install/scaffold test files on every PR, time that leg separately, and require `install_scaffold_ms <= ordinary_ms` in the independent verifier. |
</phase_requirements>

## Summary

The smallest correct implementation is to keep the current job graph and instrument the two test legs that already exist inside `mix ci`. The current alias contains the ordered values `"test --exclude scaffold"` and `"ci.install_golden"`, and the latter names exactly six test paths. The current workflow has one direct command, `"MIX_ENV=test mix ci"`, under `library_tests_shard`; the required `Library tests` aggregate depends only on that owner. [VERIFIED: mix.exs:143-168; .github/workflows/ci.yml:497-576]

Reuse `Sigra.CI.ExUnitTimingFormatter` unchanged if possible. It already consumes `{:test_finished, %ExUnit.Test{}}`, sorts by descending `time_us` with lexical tie-breaks, writes atomically, and accepts the fixed path `"/tmp/sigra-library-1-timings.json"`. Set `MIX_TEST_PARTITION=ordinary` and `SIGRA_EXUNIT_TIMING_PATH=/tmp/sigra-library-1-timings.json` on the sole owner, and make the ordinary command include both `ExUnit.CLIFormatter` and `Sigra.CI.ExUnitTimingFormatter`. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:6-10,21-63,80-87]

Instrument the two existing test commands with a project-local, fixed-command Bash harness invoked once by the `ci` alias. This matches the repository's established nested-Mix harness pattern: `scripts/ci/sigra-dep-off.sh` runs fixed `mix` commands from a `cmd bash` alias while preserving failure status and cleanup. The new harness must emit a raw work-class receipt even on a failed leg, then return the failing child status; a separate verifier must recompute all ratio and dominance predicates instead of trusting booleans stored in the receipt. [VERIFIED: mix.exs:164-168; scripts/ci/sigra-dep-off.sh:1-64]

**Primary recommendation:** add one fail-closed `library-economics` harness plus verifier, reconnect the existing same-run formatter, retain two separately uploaded receipts, and use a fresh retry-free PR run to authorize traceability changes only after `ratio <= 2.0` and `install_scaffold_ms <= ordinary_ms` both pass. The factor-of-two comparability ceiling is a prescriptive phase recommendation, not a discovered ecosystem standard. [ASSUMED]

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Per-test slow visibility | ExUnit runtime | CI artifact layer | ExUnit owns lifecycle events and microsecond test durations; CI only configures and preserves the receipt. [CITED: https://hexdocs.pm/ex_unit/main/ExUnit.Formatter.html] [CITED: https://hexdocs.pm/ex_unit/main/ExUnit.Test.html] |
| Work-class duration measurement | Contributor command harness | Mix aliases | The harness surrounds the two existing commands while `mix ci` remains the only public entry point. [VERIFIED: mix.exs:143-168] |
| Comparable/non-dominant verdict | Offline deterministic verifier | Phase evidence contract | The verifier recomputes predicates from raw durations; the evidence contract binds those results to a real PR run. [VERIFIED: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json:76-126] |
| Required check continuity | GitHub Actions workflow | Repository ruleset | The workflow preserves the bare `Library tests` aggregate; the live active ruleset currently requires that exact context. [VERIFIED: gh api repos/szTheory/sigra/rulesets/14941512, 2026-09-09] |
| Milestone truth reconciliation | Planning artifacts | Milestone audit | REQUIREMENTS and Phase 235 prose change only after machine evidence passes; the milestone audit is regenerated afterward. [VERIFIED: .planning/REQUIREMENTS.md:62-91; .planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md:1-38] |

## Project Constraints (from AGENTS.md)

- Replace human UAT with deterministic tests, browser/CI automation, and committed machine-readable evidence inside the authorized scope. [VERIFIED: AGENTS.md]
- Retry a transient failure once; diagnose and repair deterministic failures; never waive or mark missing evidence passed. [VERIFIED: AGENTS.md]
- Do not expand into unrelated phases or product scope. [VERIFIED: AGENTS.md]
- Use one CI watcher per workflow run, with `gh run watch <run-id> --repo szTheory/sigra --compact --interval 60 --exit-status`; inspect `gh api rate_limit` before a long watch; stop on 403/429 until reset. [VERIFIED: AGENTS.md]
- After completion, fetch the structured run summary once and fetch failed logs only for a failed conclusion. [VERIFIED: AGENTS.md]
- Admin UI design-system constraints are not applicable because this phase changes CI/test instrumentation and planning evidence only. [VERIFIED: 235.1-CONTEXT.md]

## Standard Stack

### Core

| Library / Tool | Version | Purpose | Why Standard |
|----------------|---------|---------|--------------|
| Elixir / Mix | `1.19.5-otp-28` | Run the project, aliases, ExUnit, and formatter | The project declares `"elixir 1.19.5-otp-28"`. [VERIFIED: .tool-versions:2] |
| Erlang/OTP | `28.5` | BEAM runtime | The project declares `"erlang 28.5"`. [VERIFIED: .tool-versions:1] |
| `Sigra.CI.ExUnitTimingFormatter` | repository module, schema `1` | Deterministic per-test timing receipt | It already implements the required lifecycle consumer, stable ordering, exact outcomes, path allowlist, and atomic rename. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:21-151] |
| Bash | repository CI harness convention | Time fixed child commands and preserve their exit status | The existing dep-off harness demonstrates fixed nested Mix commands and exit preservation. [VERIFIED: scripts/ci/sigra-dep-off.sh:1-64] |
| `actions/upload-artifact` | pinned SHA `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (`v7.0.1`) | Preserve raw timing receipts | The workflow already uses this immutable pin. [VERIFIED: .github/workflows/ci.yml:1357-1364] |

### Supporting

| Tool | Version | Purpose | When to Use |
|------|---------|---------|-------------|
| `jq` | local probe `1.7.1` | Exact schema checks and CI summaries | Use in the verifier and summary step, never as the only evidence authority. [VERIFIED: local `jq --version`, 2026-09-09] |
| `gh` | local probe `2.95.0` | One-run observation and artifact retrieval | Use only after rate-limit inspection and with the project watcher constraints. [VERIFIED: local `gh --version`, 2026-09-09] |
| Existing Phase 235 offline verifiers | repository scripts | Guard FAST-01 and GATE-05 invariants | Run before and after this phase; do not modify protected evidence. [VERIFIED: .planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md:60-80] |

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Fixed-command Bash harness | Small Mix task | A Mix task gives stronger types but complicates running two ExUnit invocations and preserving the first failure inside one Mix VM; Bash already has an accepted nested-Mix precedent. [VERIFIED: scripts/ci/sigra-dep-off.sh:25-34] |
| Existing lifecycle formatter | `mix test --slowest` | Prohibited: official Mix docs state `--slowest` enables `--trace`, and `--trace` sets `--max-cases` to `1`. [CITED: https://mix.hexdocs.pm/Mix.Tasks.Test.html] |
| One owner with timed classes | Restore two matrix shards plus scaffold job | Locked out by D-01/D-02 and would reintroduce the topology Phase 234 intentionally consolidated. [VERIFIED: 235.1-CONTEXT.md] |
| Integer cross-multiplication | Floating-point ratios stored as truth | Integer math is deterministic and avoids rounding-dependent verdicts. [ASSUMED] |

**Installation:** no new package, dependency, action, or runtime is required. [VERIFIED: repository implementation seams above]

## Package Legitimacy Audit

Not applicable. This phase should install no external packages; it reuses repository modules, Bash, Mix, `jq`, `gh`, and the already pinned artifact action. [VERIFIED: .github/workflows/ci.yml:517-548; mix.exs:143-168]

## Architecture Patterns

### System Architecture Diagram

```text
PR / local contributor
        |
        v
MIX_ENV=test mix ci                      (single public command / single CI owner)
        |
        +--> fixed library-economics harness
        |       |
        |       +--> ordinary mix test --exclude scaffold
        |       |       +--> CLI formatter
        |       |       +--> ExUnitTimingFormatter --> per-test receipt
        |       |
        |       +--> mix ci.install_golden ---------> exact six-path receiver
        |       |
        |       +--> raw integer leg durations + conclusions --> economics receipt
        |                                                        |
        |                                                        v
        |                                              independent verifier
        |                                      ratio <= 2.0 AND install <= ordinary
        |
        +--> remaining ci legs
        |
        v
library_tests_shard result --> bare `Library tests` aggregate --> ci-gate
        |
        +--> fail-closed artifact uploads --> retained PR evidence --> traceability update
```

The current aggregate values are `name: Library tests`, `needs: [library_tests_shard]`, `if: always()`, and an exact success check on the owner result; preserve them byte-for-byte where D-09 requires. [VERIFIED: .github/workflows/ci.yml:550-576]

### Recommended Project Structure

```text
scripts/ci/
├── library-economics.sh                 # fixed two-leg runner; raw receipt; exit propagation
├── library-economics.test.sh            # hermetic success/failure/omission tests
└── verify-library-economics.sh           # exact schema + recomputed predicates
test/support/ci/
├── ex_unit_timing_formatter.ex           # existing same-run per-test producer
└── ex_unit_timing_formatter_test.exs      # existing formatter contract
test/sigra/planning/
├── phase_233_library_economics_contract_test.exs # evolve topology/coverage assertions
└── phase_235_1_library_economics_contract_test.exs # receipt/evidence/traceability mutations
.planning/phases/235.1-.../
└── 235.1-EVIDENCE.json                   # retained real-PR observation and protected digests
```

These new filenames are recommended, not existing source-of-truth paths. [ASSUMED]

### Pattern 1: Same-run lifecycle receipt

Configure the ordinary test child exactly once with the existing formatter and no serializing flag:

```bash
MIX_TEST_PARTITION=ordinary \
SIGRA_EXUNIT_TIMING_PATH=/tmp/sigra-library-1-timings.json \
mix test --exclude scaffold \
  --formatter ExUnit.CLIFormatter \
  --formatter Sigra.CI.ExUnitTimingFormatter
```

The fixed path above is quoted verbatim from the formatter allowlist; `ordinary` is a recommended receipt identity. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:6-10,23-30] [ASSUMED]

Official ExUnit documentation says formatter processes receive `test_finished` events and that `ExUnit.Test.time` is the test runtime in microseconds; official Mix documentation says the normal test runner supports async concurrency while `--slowest` enables serializing trace mode. [CITED: https://hexdocs.pm/ex_unit/main/ExUnit.Formatter.html] [CITED: https://hexdocs.pm/ex_unit/main/ExUnit.Test.html] [CITED: https://mix.hexdocs.pm/Mix.Tasks.Test.html]

### Pattern 2: Raw receipt, independent verdict

The runner should record only measured integer durations, exact fixed commands, exact outcomes, and whether the second leg ran. The verifier should require the exact key set and recompute:

```text
both conclusions == "success"
ordinary_ms > 0
install_scaffold_ms > 0
max(ordinary_ms, install_scaffold_ms) * 1000
  <= min(ordinary_ms, install_scaffold_ms) * 2000
install_scaffold_ms <= ordinary_ms
```

The `2.0` ratio and `50%` non-dominance boundary are the recommended operational definitions for this phase. The first is an agent-selected comparability bound; the second directly encodes D-06's “not the dominant leg.” Do not loosen either after seeing a failing final receipt; remediate and collect a new run. [ASSUMED]

### Pattern 3: Fail-closed artifact production

Run receipt validation and upload under `if: always()` so failures retain diagnostics, but use separate uploads with `if-no-files-found: error` so absence of either receipt is itself red. The official action defaults missing files to a warning, so the explicit error mode is load-bearing. [CITED: https://github.com/actions/upload-artifact/blob/main/README.md]

### Pattern 4: Two-stage evidence closure

First land instrumentation and deterministic contracts on an evidence branch, observe one retry-free `pull_request` run, fetch its structured summary once, download the two named artifacts, and commit a digest-bound `235.1-EVIDENCE.json`. Then run the focused contract against that committed evidence and allow REQUIREMENTS/verification prose updates only after it passes. [VERIFIED: AGENTS.md; .planning/phases/233-library-suite-economics/233-EVIDENCE.json]

### Anti-Patterns to Avoid

- **A second ordinary-suite timing run:** violates D-03 even if the first run stays parallel. [VERIFIED: 235.1-CONTEXT.md]
- **`--slowest`, `--slowest-modules`, or `--trace`:** all destroy the required parallel behavior. [CITED: https://mix.hexdocs.pm/Mix.Tasks.Test.html]
- **Timing the entire `mix ci` step only:** cannot prove TEST-02 or TEST-03 because it does not separate the two work classes. [VERIFIED: .planning/v1.47-MILESTONE-AUDIT.md]
- **Trusting receipt booleans:** a mutated `pass: true` must not override raw durations; recompute every predicate. [VERIFIED: test/sigra/planning/phase_233_library_economics_contract_test.exs:77-97]
- **Using artifact upload default behavior:** missing evidence would warn and still succeed. [CITED: https://github.com/actions/upload-artifact/blob/main/README.md]
- **Reactivating `LibraryTestPartitions`:** that module exports the retired two-shard model and is not a current CI consumer. Keep it historical or delete it only if the plan proves no live reference; do not wire it back. [VERIFIED: test/support/ci/library_test_partitions.exs:1-160; .planning/v1.47-MILESTONE-AUDIT.md]
- **Changing `Library tests`:** the active ruleset requires that exact string, so renaming it risks a merge outage. [VERIFIED: gh api repos/szTheory/sigra/rulesets/14941512, 2026-09-09]

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Per-test timing | Log regex or custom test rerun | Existing `Sigra.CI.ExUnitTimingFormatter` | It consumes authoritative lifecycle data, sorts deterministically, and atomically writes JSON. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:33-151] |
| Normal CI output | Replacement formatter | Both `ExUnit.CLIFormatter` and the timing formatter | Prior repository history already used the pair in the same invocation; retain ordinary operator output. [VERIFIED: git object 6974bd1e2e4214fd5b9d519a987dfef2c3e89b89, .github/workflows/ci.yml:546-562] |
| Test ownership discovery | A second path list | Existing `ci.install_golden` alias plus the live `@moduletag :scaffold` exact-set contract | Current tests already enforce receiver equality and no duplicates. [VERIFIED: mix.exs:161-163; test/sigra/planning/phase_233_library_economics_contract_test.exs:50-75] |
| Required-check replacement | New protected context | Existing thin `Library tests` aggregate | The live ruleset already protects it. [VERIFIED: .github/workflows/ci.yml:550-576; gh api ruleset result, 2026-09-09] |
| CI polling | Custom loop | Required `gh run watch ... --interval 60 --exit-status` command | Project instructions prescribe rate-safe observation. [VERIFIED: AGENTS.md] |

**Key insight:** this phase is an observability repair, not another CI topology redesign. Preserve ownership and attach evidence at the existing boundaries. [VERIFIED: 235.1-CONTEXT.md]

## Runtime State Inventory

| Category | Items Found | Action Required |
|----------|-------------|------------------|
| Stored data | No application/database record stores the retired shard topology. The durable evidence is repository JSON plus GitHub run artifacts. [VERIFIED: repository artifact search and Phase 233/235 evidence files] | No data migration; create a new phase-owned evidence file and do not rewrite historical receipts. |
| Live service config | GitHub ruleset `14941512`, named `main`, is active and requires the exact context `"Library tests"`. [VERIFIED: live `gh api` response, 2026-09-09] | Preserve the check name; no repository-admin mutation is needed. |
| OS-registered state | None. No launchd/systemd/task-scheduler registration participates in the library CI flow. [VERIFIED: workflow and alias entry points] | None. |
| Secrets/env vars | Existing `SIGRA_EXUNIT_TIMING_PATH` is workflow-local; the proposed economics path is also workflow-local and must be fixed/allowlisted. No repository secret is needed. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:23-30; .github/workflows/ci.yml:57-59] | Add explicit job env only; validate fixed paths before writes. |
| Build artifacts / installed packages | CI caches `deps` and `_build`; local/CI test execution requires the pinned `phx_new 1.8.8` archive for install/upgrade coverage. [VERIFIED: .github/workflows/ci.yml:523-540; test/test_helper.exs:7-20] | No cache migration; retain the archive-install step and current cache key. New source recompiles normally. |

## Common Pitfalls

### Pitfall 1: Formatter configured globally for both ExUnit legs
**What goes wrong:** `ci.install_golden` starts a second ExUnit run and overwrites the ordinary per-test receipt. [VERIFIED: mix.exs:149-163]
**Why it happens:** a job-wide formatter configuration applies to both child commands. [ASSUMED]
**How to avoid:** pass formatter flags only to the ordinary `mix test`; keep the install alias unchanged and measure it only at the harness boundary. [ASSUMED]
**Warning signs:** receipt identity says `scaffold`, test count equals only the six receiver files, or the file modification time follows the second leg. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:23-30; mix.exs:161-163]

### Pitfall 2: Receipt loss hides a failed producer
**What goes wrong:** the main test step fails and later upload warns about a missing file while the evidence path appears green. [CITED: https://github.com/actions/upload-artifact/blob/main/README.md]
**How to avoid:** write a failure receipt before exiting, validate each receipt independently under `if: always()`, and use `if-no-files-found: error`. [ASSUMED]
**Warning signs:** no artifact ID, no digest, or a successful summary with an absent file. [CITED: https://github.com/actions/upload-artifact/blob/main/README.md]

### Pitfall 3: Ratio math accepts zero or rounded values
**What goes wrong:** integer-second measurements can produce zero in hermetic runs or floating rounding can move a boundary. [ASSUMED]
**How to avoid:** record monotonic integer milliseconds, require both values positive, and compare with integer cross-products. [ASSUMED]
**Warning signs:** division in shell/jq, decimal ratios stored as the authority, or zero-duration “passing” fixtures. [ASSUMED]

### Pitfall 4: Exact-once coverage drifts during wrapper insertion
**What goes wrong:** the wrapper calls a shortened path set or leaves the original alias call in place, creating omission or duplication. [VERIFIED: test/sigra/planning/phase_233_library_economics_contract_test.exs:50-75]
**How to avoid:** keep `ci.install_golden` as the sole six-path list; assert the `ci` alias invokes the harness once and the harness invokes `ci.install_golden` once. [ASSUMED]
**Warning signs:** more than one `mix ci.install_golden`, direct install file paths in the harness, or receiver/live scaffold set inequality. [VERIFIED: mix.exs:161-163; test/sigra/planning/phase_233_library_economics_contract_test.exs:50-75]

### Pitfall 5: Reconciliation precedes proof
**What goes wrong:** checked boxes and “Complete” rows advertise success before a real receipt passes. The current file already illustrates this split: TEST checkboxes are checked while traceability says `Gaps Found`. [VERIFIED: .planning/REQUIREMENTS.md:47-49,83-91]
**How to avoid:** make docs reconciliation the last plan after the evidence contract and protected verifiers pass. [VERIFIED: 235.1-CONTEXT.md]

## Code Examples

### Fixed-command failure propagation skeleton

```bash
# Recommended shape; exact helper names are agent discretion. [ASSUMED]
set -uo pipefail

ordinary_status=0
run_timed ordinary \
  env MIX_TEST_PARTITION=ordinary \
      SIGRA_EXUNIT_TIMING_PATH=/tmp/sigra-library-1-timings.json \
  mix test --exclude scaffold \
      --formatter ExUnit.CLIFormatter \
      --formatter Sigra.CI.ExUnitTimingFormatter \
  || ordinary_status=$?

if (( ordinary_status != 0 )); then
  write_receipt_and_exit "$ordinary_status"
fi

install_status=0
run_timed install_scaffold mix ci.install_golden || install_status=$?
write_receipt_and_exit "$install_status"
```

The path is from the current formatter allowlist; all command arguments are fixed rather than interpolated from untrusted input. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:6-10] [ASSUMED]

### Independent integer predicates

```bash
jq -e '
  .schema_version == "sigra.library-economics/v1" and
  .classes.ordinary.conclusion == "success" and
  .classes.install_scaffold.conclusion == "success" and
  (.classes.ordinary.duration_ms | type == "number" and . > 0) and
  (.classes.install_scaffold.duration_ms | type == "number" and . > 0) and
  ([.classes[].duration_ms] | max * 1000 <= min * 2000) and
  (.classes.install_scaffold.duration_ms <= .classes.ordinary.duration_ms)
' "$SIGRA_LIBRARY_ECONOMICS_PATH"
```

The schema name and ratio constants are recommendations and must be mutation-tested before use as evidence authority. [ASSUMED]

### Rate-safe evidence observation

```bash
gh api rate_limit --jq '.resources.core | {remaining,reset}'
gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status
gh run view RUN_ID --repo szTheory/sigra \
  --json databaseId,event,headSha,conclusion,runAttempt,url,jobs
gh run download RUN_ID --repo szTheory/sigra \
  --name library-test-timings --dir RECEIPT_DIR
gh run download RUN_ID --repo szTheory/sigra \
  --name library-economics --dir RECEIPT_DIR
```

Use exactly one watcher and one structured summary fetch for the run. [VERIFIED: AGENTS.md]

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| Two ordinary matrix shards plus dedicated scaffold receiver | One `library_tests_shard` running `MIX_ENV=test mix ci` | Phase 234 | Cut the measured library job from 692s to 148s, but removed the separate economics observations. [VERIFIED: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json:76-110; .planning/v1.47-MILESTONE-AUDIT.md] |
| `--slowest`/trace timing | Lifecycle-event formatter | Phase 233 | Keeps per-test timing in the parallel run. [CITED: https://mix.hexdocs.pm/Mix.Tasks.Test.html] [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:33-63] |
| Job topology as TEST-02 evidence | Separately timed work classes inside one owner | Phase 235.1 decision D-02 | Preserves measurable economics without restoring runner duplication. [VERIFIED: 235.1-CONTEXT.md] |

**Deprecated/outdated:** `Sigra.CI.LibraryTestPartitions` is historical for this milestone closure; its values `1` and `2` model the retired matrix and must not become production CI consumers again. [VERIFIED: test/support/ci/library_test_partitions.exs:18-33; 235.1-CONTEXT.md]

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | A factor-of-two maximum/minimum duration ratio is an appropriate operational definition of “comparable.” | Summary / Architecture Patterns | A real clean PR may fail despite acceptable economics, requiring targeted remediation or an explicitly justified tighter/different bound before final evidence. |
| A2 | A Bash harness can obtain monotonic millisecond timing portably in both GitHub Ubuntu and the supported contributor environment without adding a dependency. | Patterns / Pitfalls | If the clock seam is not portable, use a small test-only Mix task while preserving the same receipt/verifier contract. |

## Open Questions (RESOLVED)

1. **Initial same-run duration observation — resolved as an execution-time admission policy.**
   - What we know: the consolidated owner measured 148s and its `mix ci` step 108s, but the retained receipt does not split the two classes. [VERIFIED: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json:85-110]
   - Resolution: execution admits evidence only when the first eligible instrumented retry-free PR observation satisfies the already-fixed `max/min <= 2.0` and `install_scaffold <= ordinary` predicates. A miss is a red diagnostic, not calibration authority: keep the bounds unchanged, perform a scoped implementation remediation, and collect a fresh attempt-1 PR run. The first failing observation is retained in diagnostics but never becomes passing evidence. [ASSUMED]

2. **Retired partition module disposition — resolved as inert historical source.**
   - What we know: it has no current workflow consumer and reads historical Phase 233 evidence. [VERIFIED: test/support/ci/library_test_partitions.exs:1-160; .planning/v1.47-MILESTONE-AUDIT.md]
   - Resolution: Plans 01–04 neither delete nor reactivate `Sigra.CI.LibraryTestPartitions`. Leave it inert as historical Phase 233 support, assert that it is not a workflow or `mix ci` production consumer, and keep the one-owner work-class implementation independent of its retired shard exports. Any later cleanup is outside this closure phase. [ASSUMED]

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Project Erlang/Elixir | Local focused tests | ✗ through current asdf shims | project requires OTP `28.5`, Elixir `1.19.5-otp-28`; shim reports no matching `erl` | Use CI's existing `erlef/setup-beam` exact `.tool-versions` setup. [VERIFIED: local probe; .github/workflows/ci.yml:517-522] |
| PostgreSQL | Full `mix ci` | ✓ | local server accepting on `/tmp:5432`; client 14.17 | CI PostgreSQL 15 service is canonical. [VERIFIED: local `pg_isready`/`psql --version`; .github/workflows/ci.yml:505-515] |
| Docker | Optional local parity | ✓ | 29.5.2 | Direct local PostgreSQL or CI service. [VERIFIED: local `docker info`, 2026-09-09] |
| `gh` | Real PR evidence | ✓ | 2.95.0 | None; CI evidence closure requires authenticated GitHub data. [VERIFIED: local probe, 2026-09-09] |
| `jq` | Receipt verification | ✓ | 1.7.1 | Elixir `JSON` exact parser if shell portability becomes a problem. [VERIFIED: local probe; test/support/ci/library_test_partitions.exs:133-138] |

**Missing dependencies with no fallback:** none for implementation because CI provisions the declared runtime; local focused Elixir verification is currently blocked by the user's asdf runtime installation state. [VERIFIED: local probe, 2026-09-09]

**Missing dependencies with fallback:** local exact Erlang/Elixir runtime → existing CI setup. [VERIFIED: .github/workflows/ci.yml:517-522]

## Validation Architecture

### Test Framework

| Property | Value |
|----------|-------|
| Framework | ExUnit on project Elixir `1.19.5-otp-28`; Bash hermetic self-tests for the harness/verifier. [VERIFIED: .tool-versions:1-2; existing `scripts/ci/*.test.sh`] |
| Config file | `test/test_helper.exs` [VERIFIED: test/test_helper.exs:1-35] |
| Quick run command | `mix test test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs` [ASSUMED] |
| Full suite command | `MIX_ENV=test mix ci` [VERIFIED: mix.exs:143-168; .github/workflows/ci.yml:543-548] |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| TEST-01 | Sole owner invokes one ordinary parallel suite with both formatters; receipt is nonempty, exact-schema, sorted, and uploaded fail-closed; no `--trace`/`--slowest`/second suite. | unit + workflow contract | `mix test test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs` | ✅ evolve existing |
| TEST-02 | Raw positive durations independently satisfy max/min `<= 2.0`; altered duration, trusted boolean, zero, missing class, or changed threshold fails. | contract + mutation | `mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs` | ❌ Wave 0 |
| TEST-03 | Exact six live scaffold modules execute once through `ci.install_golden`; install duration is `<=` ordinary duration; omission/duplication/dominance mutations fail. | contract + real PR evidence | `mix test test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs` | ✅ evolve + ❌ Wave 0 |
| TEST-01/02/03 | Runner preserves child failures and emits diagnostic receipt; verifier rejects missing/malformed/incomplete artifacts. | hermetic shell | `bash scripts/ci/library-economics.test.sh && bash scripts/ci/verify-library-economics.test.sh` | ❌ Wave 0 |
| Regression | FAST-01 and GATE-05 protected evidence remains valid and byte-stable. | offline verifier | `bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh && bash scripts/ci/verify-terminal-ratification-attestation-offline.sh` | ✅ existing |

### Sampling Rate

- **Per task commit:** formatter unit test, new harness/verifier shell tests, and the focused Phase 233/235.1 contract touched by that task. [ASSUMED]
- **Per wave merge:** all focused commands above plus `mix format --check-formatted`. [ASSUMED]
- **Phase gate:** one retry-free real PR run with `Library tests` green, two retained artifacts, exact structured run summary, passing independent predicates, and both protected offline verifiers green. [VERIFIED: AGENTS.md; 235.1-CONTEXT.md]

### Wave 0 Gaps

- [ ] `scripts/ci/library-economics.sh` and `scripts/ci/library-economics.test.sh` — fixed two-leg execution, raw receipt, and failure propagation.
- [ ] `scripts/ci/verify-library-economics.sh` and `.test.sh` — exact schema and adverse ratio/dominance/omission fixtures.
- [ ] `test/sigra/planning/phase_235_1_library_economics_contract_test.exs` — bind final PR evidence, reinterpretation, protected digests, and mutation directions.
- [ ] `.planning/phases/235.1-.../235.1-EVIDENCE.json` — populated only after a real PR run; never pre-seed as passing.

## Security Domain

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | No | No user authentication surface changes. [VERIFIED: 235.1-CONTEXT.md] |
| V3 Session Management | No | No session state changes. [VERIFIED: 235.1-CONTEXT.md] |
| V4 Access Control | Yes, CI evidence authority | GitHub ruleset plus exact required aggregate; do not grant write permissions or mutate ruleset. [VERIFIED: .github/workflows/ci.yml:32-37; live ruleset query] |
| V5 Input Validation | Yes | Fixed output paths, exact JSON key sets/types, positive integer durations, exact class names, and recomputed predicates. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:69-87; existing Phase 233 mutation pattern] |
| V6 Cryptography | Yes, evidence integrity only | SHA-256 digests and existing offline attestation verifiers; do not hand-roll signing. [VERIFIED: .planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md:60-80] |

### Known Threat Patterns for CI Evidence

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Caller-controlled output path overwrites arbitrary files | Tampering | Exact fixed path allowlist plus atomic same-directory rename. [VERIFIED: test/support/ci/ex_unit_timing_formatter.ex:6-10,69-87] |
| Receipt says pass while raw durations fail | Tampering | Independent verifier recomputes ratios from exact positive integers; mutation tests alter each pole. [ASSUMED]
| Failed leg masked by instrumentation | Repudiation / Tampering | Capture child status, write failure receipt, exit with nonzero status, and retain artifacts under `if: always()`. [ASSUMED]
| Missing evidence silently accepted | Repudiation | `if-no-files-found: error`, nonempty checks, artifact ID/digest recorded. [CITED: https://github.com/actions/upload-artifact/blob/main/README.md]
| Dynamic command/env injection into harness | Tampering | Fixed command/argument arrays; only allowlisted paths; no `eval`, shell-built command strings, or caller-provided executable. [ASSUMED]
| Oversized or duplicate receipt content | Denial of Service / Tampering | Exact schema, nonempty bounded top-N summary, unique test identity check, and raw artifact retention. [VERIFIED: .planning/phases/233-library-suite-economics/233-EVIDENCE.json]

## Sources

### Primary (HIGH confidence)

- Repository source: `mix.exs`, `.github/workflows/ci.yml`, formatter/partition modules, Phase 233 contract/evidence, Phase 234 parity evidence, Phase 235 remediation and terminal verification.
- Live GitHub ruleset `14941512` queried 2026-09-09; active required context remains `Library tests`.

### Secondary (MEDIUM confidence)

- [Official Mix test documentation](https://mix.hexdocs.pm/Mix.Tasks.Test.html) — formatter, async scheduling, slowest/trace behavior.
- [Official ExUnit.Formatter documentation](https://hexdocs.pm/ex_unit/main/ExUnit.Formatter.html) — lifecycle events and suite timing.
- [Official ExUnit.Test documentation](https://hexdocs.pm/ex_unit/main/ExUnit.Test.html) — test fields and microsecond runtime.
- [Official upload-artifact README](https://github.com/actions/upload-artifact/blob/main/README.md) — missing-file failure mode and artifact outputs.

### Tertiary (LOW confidence)

- None. The only low-confidence items are explicit agent recommendations in the Assumptions Log.

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — current project declarations, source, local probes, and live ruleset were read directly.
- Architecture: HIGH — it preserves locked decisions and reconnects an implementation previously proven in repository history.
- Quantitative bound: MEDIUM — `2.0` is a deliberate operational definition that still needs a clean same-run observation.
- Pitfalls: HIGH except clock portability — most are direct consequences of current aliases, formatter semantics, and artifact behavior.

**Research date:** 2026-09-09
**Valid until:** 2026-10-09 for repository architecture; re-query the GitHub ruleset immediately before workflow edits.

## Measured Miss Remediation

### Finding and fixed boundary

The retry-free PR observation is a real implementation miss, not a receipt defect. Run
`34395477605` measured `ordinary=21,050ms` and `install_scaffold=552,221ms`; therefore
`max/min=26.2337767` and `552,221 <= 21,050` is false. Plan 06 must retain the existing
verifier predicates `maximum * 1000 <= minimum * 2000` and `install <= ordinary`, and it
must not create evidence until a fresh attempt-1 PR run satisfies both. [VERIFIED:
.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md:37-70;
scripts/ci/verify-library-economics.sh:60-73]

The largest visible structural duplication is fixture construction. The receiver set is exactly the six
paths named by `ci.install_golden`, but those files construct substantially more than six
fresh applications: upgrade constructs three, golden diff constructs two, passkey JS
constructs three, passkey opt-out constructs two, idempotency constructs one, and vault
promotion constructs one. Each current construction repeats `mix phx.new --no-assets
--no-install`, path-dependency patching, `mix deps.get`, and a baseline `mix compile` before
the receiver-specific assertion begins. [VERIFIED: mix.exs:160-162;
test/upgrade_test.exs:38-184; test/sigra/install/golden_diff_test.exs:51-88;
test/sigra/install/features/passkeys_js_test.exs:34-88,389-398;
test/sigra/install/generator_passkeys_opt_out_test.exs:32-83;
test/sigra/install/idempotency_test.exs:28-101;
test/sigra/install/vault_promotion_test.exs:10-27;
test/support/install_fixture.ex:43-160] Whether this is the dominant measured component
must be confirmed by the Plan 06 diagnostic receipt. [ASSUMED]

**Recommended Plan 06 approach:** replace twelve independent bootstraps with one counted,
immutable prepared-fixture graph, then fan out private copy-on-write checkouts to the same
six receiver files. Build the graph inside the timed `ci.install_golden` command—not in a
workflow setup step—so the economics receipt continues to measure all work needed only by
the install/scaffold class. Run independent variant construction and receiver scenarios
with a bounded concurrency of two on the GitHub-hosted runner. [ASSUMED]

### Prepared-fixture graph

Use one source base with the canonical application name and derive only the six installer
states actually needed:

```text
Phoenix source base (one phx.new; no installer)
├── default installed
│   ├── golden tree + captured stdout (read-only checkout)
│   ├── idempotency second invocation (private checkout)
│   ├── upgrade default/backfill-off (private checkout)
│   └── upgrade backfill-on + rerun (private checkout)
├── passkeys installed with standard app.js
│   ├── passkey wiring assertions (read-only checkout)
│   ├── passkey rerun/idempotency (private checkout)
│   └── vault promotion compile sentinel (private checkout)
├── passkeys installed with non-standard app.js
├── no-passkeys installed
├── no-organizations + no-passkeys installed
└── no-organizations installed (upgrade zero-org checkout)
```

This preserves every behavioral axis currently asserted while eliminating repeated
Phoenix generation, dependency resolution, baseline compilation, default installation,
and passkey installation. Golden diff's tree and stdout assertions must consume the same
captured default-install result instead of running the installer twice. Idempotency must
mutate a private clone of that result. Upgrade tests must clone the matching installed
state before seeding or migrating, so neither database nor filesystem state is shared.
[VERIFIED: test/sigra/install/golden_diff_test.exs:51-88;
test/sigra/install/idempotency_test.exs:28-101; test/upgrade_test.exs:38-184]

Implement the graph as a test-only manifest under one `mktemp -d` root. The manifest should
carry each variant's absolute checkout path, captured normalized stdout where applicable,
and a unique partition token. `InstallFixture.run_sigra_install/2`,
`run_sigra_upgrade/2`, and `run_mix/2` should always propagate that token as
`MIX_TEST_PARTITION`; the generated test database already suffixes its name from that
environment variable. Delete the validated temp root from one `ExUnit.after_suite/1`
callback. Never let receiver modules delete the immutable prepared variants. [VERIFIED:
test/support/install_fixture.ex:186-262;
test/fixtures/install_golden/tree/config/test.exs:8-14] [ASSUMED]

### Reuse the compilation work already owned by `mix ci`

The public alias performs `compile --warnings-as-errors` before the economics harness, so
Plan 06 should reuse the root checkout's already-downloaded dependency sources rather than
resolving and compiling the same dependency graph for every generated app. The prepared
base should point `MIX_DEPS_PATH` at the repository `deps` directory and seed one private
fixture build tree from the compatible root `_build/test` output using a platform-checked
copy-on-write operation (`cp --reflink=auto` on the Linux evidence runner; a normal copy as
the deterministic fallback). Each mutable checkout then receives its own build directory;
shared writable Mix manifests or symlinked build outputs are prohibited. [VERIFIED:
mix.exs:149-168; test/support/install_fixture.ex:43-160] [ASSUMED]

Keep compile/runtime proofs only where they are behavioral assertions: the upgrade paths,
the two passkey opt-out variants, and vault promotion. Golden diff, passkey JavaScript
wiring, and idempotency should not add a redundant baseline compile beyond the prepared
base. Do not replace compile/runtime assertions with source regexes. [VERIFIED:
test/upgrade_test.exs:80-184;
test/sigra/install/generator_passkeys_opt_out_test.exs:35-80;
test/sigra/install/vault_promotion_test.exs:10-27]

### Batching and isolation

After the immutable graph is ready, schedule independent private checkouts with a bounded
two-worker supervisor. Multiple tests in one ExUnit module are serial even when the module
is asynchronous, so the three upgrade scenarios and generated-app portions of the passkey
and opt-out receivers need either separate modules in their existing receiver file or an
explicit two-worker scenario runner. Keep all six canonical file paths in one fixed
`ci.install_golden` invocation exactly once. [ASSUMED]

Do not use unrestricted ExUnit concurrency: generated projects launch Mix, touch databases,
and one upgrade assertion starts a Phoenix server. Every checkout needs a unique database
partition and server port; the fixture graph must assert that no two scenario manifests
share either value. A failing scenario must cancel outstanding work, retain its checkout
path in diagnostics, and return nonzero from `mix ci.install_golden`. [VERIFIED:
test/upgrade_test.exs:320-466] [ASSUMED]

### Exact tracked-file scope for Plan 06

| File | Required change |
|------|-----------------|
| `test/support/install_fixture.ex` | Add prepared-base creation, immutable variant snapshots, private checkout, partition propagation, bounded scenario execution, phase timings, and one safe cleanup owner. |
| `test/test_helper.exs` | When a fixed install-golden environment flag is present, prepare the graph inside the measured ExUnit invocation and register after-suite cleanup. |
| `mix.exs` | Preserve the single `mix ci` owner and one `ci.install_golden` leg; route the latter through a fixed runner without duplicating receiver paths. |
| `scripts/ci/install-golden.sh` | New fixed-command runner containing the canonical six paths once and enabling prepared-fixture mode; no caller-provided command or `eval`. |
| `scripts/ci/install-golden.test.sh` | New hermetic contract for exact receiver paths, one invocation, flag propagation, child-status preservation, and no pre-timing preparation. |
| Six current receiver files | Consume named private/read-only graph states, merge golden tree/stdout onto one install, split only scenario execution needed for bounded concurrency, and keep all existing behavioral assertions. |
| `test/sigra/planning/phase_233_library_economics_contract_test.exs` | Move exact-once ownership checks to the fixed runner while continuing to prove the six live `:scaffold` paths occur once and no second suite owner exists. |
| `test/sigra/planning/phase_235_1_library_economics_contract_test.exs` | Pin preparation inside the measured leg, exact immutable variant names, partition uniqueness, fixed predicates, and absence of threshold/environment bypasses. |
| `scripts/ci/library-economics.sh` | Continue timing the complete `mix ci.install_golden` call; do not move fixture preparation before `install_start`. |

No workflow topology, package, action, requirement status, milestone audit score, FAST-01
artifact, or GATE-05 ledger change belongs in Plan 06. [VERIFIED: 235.1-CONTEXT.md]

### Required profiling and acceptance sequence

Before replacing setup, add a private diagnostic receipt that records cumulative
`phx_new`, `deps_get`, baseline `compile`, installer, receiver compile/runtime, and checkout
copy durations. It is diagnostic only: the authoritative economics verifier continues to
consume the two existing class totals. Require the diagnostic sum to be less than or equal
to the measured install/scaffold total and mutation-test omission/duplicate phase keys.
[ASSUMED]

Plan 06 is complete only when all of the following hold:

1. `bash scripts/ci/install-golden.test.sh`, both existing economics shell suites, the
   Phase 233/235.1 contracts, and `MIX_ENV=test mix ci` pass locally without a skipped or
   duplicated receiver.
2. A source contract proves the fixed six paths appear exactly once, every existing test
   assertion remains present, prepared variants are immutable, mutable scenarios use
   private checkouts, and preparation occurs after `install_start` and before `install_end`.
3. One fresh attempt-1 `pull_request` run under the existing single
   `MIX_ENV=test mix ci` owner produces successful raw durations satisfying both
   `max/min <= 2.0` and `install_scaffold <= ordinary` with no threshold relaxation.
4. The same run proves all six receiver paths executed exactly once and retains the
   diagnostic phase receipt so the improvement is attributable to removed setup
   duplication rather than omitted work.
5. If the fresh run still exceeds either bound, Plan 06 remains blocked and the diagnostic
   receipt selects the next concrete duplicated compile/install phase to remove; no
   evidence or traceability document is admitted.

### Risks and rejection criteria

- **Warm-cache theater:** preparing or restoring install-only fixtures before
  `install_start` makes the comparison invalid. Reject any such change. [VERIFIED:
  scripts/ci/library-economics.sh:136-151]
- **Mutable cache contamination:** sharing a writable generated app, build manifest, or
  database can make tests order-dependent. Require private checkouts and unique partitions.
  [ASSUMED]
- **Coverage laundering:** deleting compile/runtime assertions or moving any receiver off
  the PR lane is not optimization. Reject it even if timing turns green. [VERIFIED:
  235.1-CONTEXT.md]
- **Unbounded subprocess fan-out:** more workers can increase wall time or exhaust the
  two-core runner. Start at two and change only from retained diagnostic measurements.
  [ASSUMED]
- **Copy fallback cost:** if the Linux filesystem cannot reflink, the diagnostic receipt
  must include checkout-copy time. A fallback that makes the class miss remains a blocker,
  not grounds to exclude copy time. [ASSUMED]

This approach resolves the blocked planning question without claiming the unobserved
outcome: it attacks the measured source of waste, preserves exact receiver ownership and
end-to-end assertions, and leaves the fixed verifier as the sole authority for whether the
remediation is sufficient. [VERIFIED:
.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md:172-194]

## Approved Empirical Fallback — 2026-09-10

This section supersedes the earlier recommendations that `LibraryTestPartitions` remain
inert, that all six scaffold modules remain in PR `mix ci`, and that ordinary-vs-scaffold
non-dominance remain the current admission predicate. Those recommendations remain useful
history for Plans 01–07 but are not authority for Plans 08–10 or revised Plan 05.

Attempt-1 runs `34431739531` and `34435818106` tested the fixed-bound path after safe
fixture/storage optimizations. Run `34435818106` measured `ordinary=28,671ms` and
`install_scaffold=79,614ms`; its diagnostic phases were baseline `16,183ms`, installer
`25,254ms`, and receiver compile/runtime `28,763ms`. The final content-rebase experiment
still hard-stopped at `57,389ms`. The remaining cost is truth-bearing compile/runtime
coverage, so additional reductions cannot be assumed safe. [VERIFIED: 235.1-06-SUMMARY.md;
operator decision addendum in 235.1-CONTEXT.md]

The approved topology follows TEST-03's explicit alternative:

1. Keep one PR `library_tests_shard` directly invoking literal `MIX_ENV=test mix ci`, and
   keep the `Library tests` aggregate/ruleset edge byte-identical.
2. Reactivate `Sigra.CI.LibraryTestPartitions` as a fail-closed manifest consumer for two
   non-empty, disjoint, exhaustive ordinary partitions. Execute them sequentially inside
   the owner while ExUnit remains parallel within each. Emit two deterministic timing
   receipts plus one combined raw-duration receipt; independently verify exact universe,
   no scaffold leakage, per-test membership, and `max/min <= 2.0`.
3. Remove only the exact six scaffold/install modules from PR `mix ci`. Run them unchanged
   through the optimized prepared-fixture runner in `library_install_golden_non_pr` on every
   `schedule` and `workflow_dispatch`; require exact receipt and diagnostic uploads and
   hard-fail missing/skipped/failed execution.
4. Capture a fresh attempt-1 PR run and a fresh attempt-1 dispatch at one implementation
   SHA, commit digest-bound receipts, then capture distinct PR/dispatch validation runs at
   the evidence commit. No human UAT or producer-owned pass boolean is authoritative.

### Current verifier and evidence map

| Current artifact | Role | Closest existing analog |
|---|---|---|
| `scripts/ci/library-partitions.sh` | fixed sequential partition runner | `scripts/ci/library-economics.sh` fixed-command/raw-receipt pattern |
| `scripts/ci/verify-library-partitions.sh` | independent exact-set/ratio authority | `scripts/ci/verify-library-economics.sh` exact-schema arithmetic verifier |
| `scripts/ci/verify-library-install-golden.sh` | exact-six non-PR receipt authority | `scripts/ci/verify-library-economics.sh` plus Phase 233 receiver equality contract |
| `235.1-PR-EVIDENCE.json` | attempt-1 PR partition provenance | `233-EVIDENCE.json` and Phase 235 source-complete receipt patterns |
| `235.1-SCAFFOLD-EVIDENCE.json` | attempt-1 dispatch receiver provenance | Phase 232 non-PR receiving-lane evidence and `234-EVIDENCE.json` job-specific proof |
| `235.1-VALIDATION-RUN.json` | distinct evidence-SHA dual-route validation | prior Plan 04 distinct-run design, evolved to two routes |

Plans 08 and 09 implemented this fallback. Plan 10 is retained as blocked negative
evidence after six attempt-1 failures; Plans 11 → 12 → revised 05 now own deterministic
root-cause remediation, bounded fresh evidence, and reconciliation.

### Confirmed Plan 10 root causes and constrained remedies

The final PR owner passed both test invocations but lost partition 1 because the formatter
unit test assigned to partition 2 owned `/tmp/sigra-library-1-timings.json` and removed it
in `on_exit`. This is a receipt-lifetime collision, not a partition execution failure.
Use the already allowlisted `/tmp/sigra-library-scaffold-timings.json` for the formatter
write contract and prove both ordinary receipts remain byte-identical across partition 2.

The final dispatch failed after compiling all `telemetry_poller` sources because Rebar
3.25.1 stores compiler DAG state beneath dependency source `_build/prod/lib/.rebar3`.
The existing recovery's `<MIX_BUILD_PATH>/lib/<dep>/.mix` directory is the wrong ownership
location. Keep canonical `shared_deps` read-only; for the existing single retry, copy the
complete validated dependency tree to a unique graph-private writable directory, replace
only retry `MIX_DEPS_PATH`, retain it for graph lifetime, and test source integrity,
filesystem independence, cleanup, failure authority, and the unchanged two-worker limit.
Do not accept ignored Rebar writes or shared writable dependency state.

### Confirmed Plan 12 root causes and constrained remedies

Plan 12's attempt-1 pair `34493873867`/`34493911924` exposed three repository defects.
First, the formatter unit test itself unconditionally created and later removed both
ordinary timing paths; because its assertions preceded `on_exit` and the shell runner
stub never ran real ExUnit, local coverage could not observe final-lifetime deletion.
Remove all ordinary-path mutation ownership from the test, parse/mutate the whole source
to prohibit recurrence, and run the production partition runner through completed
partition 2 before comparing partition-1 bytes.

Second, the no-compile installer entrypoint emits canonical Mix unavailable-app warning
blocks before installer output. The observed allowed prefix is two blocks for
`:sigra_install_golden_tmp` and one for `:phoenix_live_view`. Replace the phoenix-only
regex with a strict repeated leading-block parser limited to those two atoms. Zero, one,
and repeated allowed blocks are environment noise; any forbidden atom, noncanonical
block, non-leading warning, or subsequent installer byte must remain exact.

Third, GitHub's runner did not provide `rg` to the verifier. Exact scaffold ownership can
be derived with fixed POSIX find/grep primitives, safe repository-relative path
validation, `LC_ALL=C` ordering, and exact tag-line matching. A hermetic PATH test must
prove no accidental host ripgrep dependency. No package installation or copied receiver
allowlist is appropriate.

## Plan 13 Comparability Diagnosis and Approved Local Calibration — 2026-09-10

Plan 13 repaired all three Plan 12 defects and both exhaustive ordinary partitions pass
with both receipts surviving. The unchanged verifier nevertheless measured
`9,103/2,430ms` and `10,963/4,242ms` (plus S4), so TEST-02 remains red at the fixed 2.0
ceiling. This is a partition-cost drift defect, not permission to relax the bound.

`Sigra.CI.LibraryTestPartitions.build_partitions!/1` reads Phase 233 per-file costs, then
gives every current path absent from that snapshot `time_us: 0`. Its deterministic LPT
tie rule selects partition 1 when totals are equal. A zero assignment never changes the
selected total, so every consecutive missing path follows the first into partition 1.
Fourteen current ordinary paths are absent from the snapshot; observed heavy examples are
`test/support/install_fixture_performance_test.exs` at roughly 4.5–4.9 seconds and the
Phase 235/235.1 planning contracts. The old static totals remain almost exactly equal
while real work is strongly skewed, explaining why source-level balance and live wall
balance disagree.

Plan 15 should preserve LPT but replace the stale/zero seam with a repository-owned
`sigra.library-partition-calibration/v1` artifact built from exactly three real local
receipt pairs at the final Plan 13 implementation tree (`997a1bb5`, summary `265b0874`).
Retain the exact bytes of all three combined receipts and all six timing receipts as
canonical base64 in that single bounded artifact, with fixed source path, byte count, and
SHA-256 for each payload. Bound the whole artifact at 8 MiB and every decoded payload at
1 MiB. The loader must decode and validate exact receipt keys/schema/counts/outcomes,
prove each combined manifest equals the matching timing receipt's unique test-file set,
and recompute every per-file raw sum directly from retained `.tests[].time_us` rows.
Effective sample costs are `max(1, sum)` and assignment costs are the deterministic
per-path median. Exact current-universe equality, sample count/order, positive integers,
and no scaffold leakage remain mandatory. Any payload/hash/size/linkage mutation, later
path addition/removal, or malformed provenance fails closed; opaque hashes or self-hashed
derived rows are insufficient replay authority.

This is preferable to hand-moving files because the partition lists remain derived,
reviewable, and exhaustive. Three further consecutive real runner executions at the new
tree are justified by the observed S4/run-to-run spread; every run must independently
pass the unchanged integer ratio, membership, receipt-lifetime, and outcome verifier.
No average, retry, threshold change, test omission/duplication, topology edit, or live
evidence action is calibration authority.
