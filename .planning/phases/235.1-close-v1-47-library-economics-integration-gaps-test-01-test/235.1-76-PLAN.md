---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 76
type: execute
gap_closure: true
wave: 78
depends_on: [235.1-12]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREALLOCATION-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Plans 69, 71, 72, 73, 74, and 75 remain immutable failed history; none of their roots, partial successes, processes, or budgets can authorize reuse, retry, or TEST completion."
    - "Before allocation, Python 3.9.6 and 3.14.4 accept one exact immutable 34-row build-link whitelist while requiring the deps and _build/test trust-anchor roots themselves to be non-symlink directories."
    - "The whitelist contains exactly 24 repo_build_link rows and ten retained_fixture_residue rows: eight bound live external targets and two exact dangling targets that remain absent and are never followed."
    - "The exact eleven-key HOME=/Users/jon runtime environment, exact 47 runtime-hostile cases, Plan 74 stdin authority, Plan 73 canonical entrypoint, Plan 72 path-selected source, and Plan 71/72 parsed SQL policy remain unchanged."
    - "One raw pre-root Mix process is persisted before parse, is never retried, resolves System.user_home! to /Users/jon, and leaves the full build-link/external-target authority unchanged."
    - "Only after both pre-root receipts pass may one Plan 76 root run one local chain, three timing pairs, one canonical exact-six process, one capture pair, and one distinct validation pair; every authorized route preserves same-schema before/after build state."
    - "Every canonical fixture copy returns only after its existing in-process materialize/omit, hardlink-breaking, and first_unsafe_link checks succeed; authenticated receipt/diagnostic success and the post-run verifier prove that contract without an invented pre-receiver handshake or repository cleanup."
    - "TEST-01/02/03 remain incomplete until Plan 76 evidence passes; the protected two ordinary partitions, exact-six receiver, FAST-01 source-complete n=52/p50=469, and all 93 GATE-05 rows remain unchanged."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREALLOCATION-VALIDATION.json"
      provides: "Dual-runtime, zero-effect proof of the exact runtime and immutable build-link authority"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json"
      provides: "Durable unparsed capture of the sole Mix home-resolution process"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json"
      provides: "Exact pre-allocation decision over HOME plus same-schema before/after build authority"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh attempt-1 two-partition pull-request evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh attempt-1 canonical exact-six dispatch evidence"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct attempt-1 validation pair bound to Plan 76 local and capture authority"
  key_links:
    - from: "Plan76BuildStateAuthority"
      to: "the timing, home-probe, and exact-six routes"
      via: "same-schema before/after equality including eight live external target-tree digests and two absent dangling targets"
    - from: "Plan76ClosedRuntimeEnvironment"
      to: "scripts/ci/library-partitions.sh and scripts/ci/install-golden.sh"
      via: "one fresh exact eleven-key env object passed with shell=false, exact cwd, and complete stream persistence"
    - from: "test/support/install_fixture.ex materialize_or_omit_links!/2, break_hardlinks!/1, and first_unsafe_link/1"
      to: "the prepared exact-six private graph"
      via: "the existing in-process per-copy sanitizer gate plus authenticated receipt/diagnostics and independent post-run source/receipt verification"
    - from: "Plan 74/73/72/71 stdin, entrypoint, source, and SQL authorities"
      to: "Plan 76 local chain"
      via: "byte/field-equal inherited accepted/adverse sets without predecessor execution"
    - from: "Plan 76 capture and validation receipts"
      to: "Plan 70 reconciliation"
      via: "unchanged offline verifiers; Plans 68/69/71/72/73/74/75 supply history only"
  prohibitions:
    - requirement_id: "TEST-01"
      status: "unverified"
      flagged: true
      statement: "Do not relabel predecessor timing, a serial pass, a rerun, or timing without same-run formatter evidence as TEST-01 completion."
    - requirement_id: "TEST-02"
      status: "unverified"
      flagged: true
      statement: "Do not relax raw max/min <=2.0, tune after observation, reorder partitions, or admit an incomplete pair."
    - requirement_id: "TEST-03"
      status: "unverified"
      flagged: true
      statement: "Do not use raw mix test, caller-supplied prepared mode, skipped/duplicated/substituted receivers, or predecessor output as exact-six evidence."
---

<objective>
Correct Plan 75's overbroad descendant-link rejection by proving the current build state against one exact immutable whitelist, then produce one fresh authenticated local/live evidence chain.

Purpose: Distinguish trusted non-symlink roots from explicitly authenticated build links, preserve external and dangling residue without cleanup or traversal, and retain the already-correct HOME/runtime and canonical execution contracts.
Output: One dual-runtime build/runtime receipt, one raw home probe and prerequisite decision, one fresh terminal Plan 76 root, and canonical PR/scaffold/validation evidence for Plan 70.

Decision coverage: D-01 through D-09 are implemented across Tasks 1–3. Plans 69/71/72/73/74/75 remain immutable failed evidence only; Plan 76 is the sole possible new TEST completion authority.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-POSTGRESQL-PROBE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-PREREQUISITE-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-74-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-75-SUMMARY.md
@scripts/ci/library-partitions.sh
@scripts/ci/install-golden.sh
@scripts/ci/install-golden.test.sh
@scripts/ci/verify-library-install-golden.sh
@scripts/ci/verify-library-install-golden.test.sh
@test/test_helper.exs
@test/support/install_fixture.ex
@.github/workflows/ci.yml
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

## Artifacts this phase produces

- New tracked zero-effect receipt: `235.1-76-PREALLOCATION-VALIDATION.json`.
- New tracked raw home probe: `235.1-76-HOME-PROBE.json`.
- New tracked pre-root decision: `235.1-76-PREREQUISITE-VALIDATION.json`.
- Replaced tracked evidence receipts only after fresh passing capture/validation: `235.1-PR-EVIDENCE.json`, `235.1-SCAFFOLD-EVIDENCE.json`, and `235.1-VALIDATION-RUN.json`.
- New private evidence root: exactly one `/private/tmp/sigra-p2351-plan76-build-links.XXXXXXXX`.

## Interface contracts

### Plan76ClosedRuntimeEnvironment

- Construct one fresh object with exactly the C-sorted keys `ASDF_ELIXIR_VERSION,ASDF_ERLANG_VERSION,HOME,LC_ALL,MIX_ENV,PATH,PGDATABASE,PGHOST,PGPASSWORD,PGPORT,PGUSER`.
- Fixed values are `1.19.5-otp-28`, `28.4.1`, `/Users/jon`, `C`, `test`, `/Users/jon/.asdf/shims:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin`, `sigra_test`, `localhost`, `5432`, and `postgres` in their corresponding slots. `PGPASSWORD` is the only opaque nonempty input and is persisted only as `<redacted-present>` plus SHA-256.
- `HOME=/Users/jon` is an absolute, uid-501, non-symlink directory resolving to itself. No `HEX_HOME`, `MIX_HOME`, `MIX_ARCHIVES`, `TMPDIR`, ambient environment copy/read, merge/update, fallback, late override, deletion, `setdefault`, child divergence, or twelfth key is permitted.
- Pass the identical environment object to `[bash,scripts/ci/library-partitions.sh]`, `[bash,scripts/ci/install-golden.sh]`, and `[/Users/jon/.asdf/shims/mix,run,--no-compile,--no-start,-e,IO.write(System.user_home!() &lt;> "\\n")]`, with cwd `/Users/jon/projects/sigra`, shell false, stdin DEVNULL, both output pipes, and raw persistence before parse.
- Preserve Plan 75's exact 24-tool PATH-resolution table and absent `sha256sum`; resolve without executing evidence commands.

### Plan76BuildStateAuthority

- Trust anchors `/Users/jon/projects/sigra/deps` and `/Users/jon/projects/sigra/_build/test` themselves must be present non-symlink directories. Task 1 derives and persists their full current lstat rows; no volatile inode, timestamp, or retained-temporary suffix is hardcoded as policy. Later gates compare exact same-schema observations to that authenticated baseline.
- Descendant links are permitted only by exact immutable 34-row C-byte-sorted path equality. Preserve Plan 75's earlier diagnostic digest `2c842c40d8c907f32d3f74257e92365efc9a15d4ebf6579dcd61372095aed5d9` as frozen history. The debugger-authenticated compact 34-row observed-manifest is exactly 11,627 bytes/SHA-256 `055c5457505fd7b7084071af5e2f129a23dba8bd9ac34d4d9ca71202a0c85935`. Each row is the fixed-position array `[relpath,link_text,sha256(link_text UTF-8),link_lstat_identity,classification,target_class,target_exists,target_lstat_or_null]`; each identity is `[type_code,dev,ino,uid,gid,mode_04o,size,mtime_ns]` with type code `l|d|f|o`. Target classes are exactly `repo_dependency_dir`, `repo_priv_dir`, `external_fixture_shared_dep_dir`, or `external_fixture_source_missing`; classification is `repo_build_link` for either repo class and `retained_install_fixture_residue` otherwise. Walk `_build/test` top-down without following links, byte-sort dirs/files and final relpaths, resolve targets by lexical `normpath` rather than realpath, and encode `json.dumps(rows,separators=(",",":"),ensure_ascii=True).encode()` with no newline/BOM. Task 1 persists the observed volatile identities rather than embedding them in source policy.
- The 24 `repo_build_link` rows are exactly: `certifi/{priv,src}`, `chimeway/priv`, `h2/{include,src}`, `hackney/{include,src}`, `jose/{include,priv}`, `mailglass/priv`, `metrics/src`, `mimerl/src`, `parse_trans/{include,src}`, `phoenix_live_view/priv`, `quic/{include,priv,src}`, `sigra/priv`, `sourceror/priv`, `tzdata/priv`, `unicode_util_compat/src`, and `webtransport/{include,src}`. Each link path is `_build/test/lib/{app}/{segment}`; each target is the present non-symlink directory `deps/{app}/{segment}`, except `sigra/priv` targets repository `priv`.
- The eight live `retained_fixture_residue` rows are `file_system/priv`, `idna/src`, `phoenix/priv`, `phoenix_html/priv`, `phoenix_live_reload/priv`, `swoosh/priv`, `telemetry/src`, and `telemetry_poller/src`. Derive their exact current external target strings and full top-level target lstats without hardcoding the volatile retained-workspace suffix. Each separate target-tree digest includes root `.` and bytewise-sorted relative paths with `followlinks=false`; each tree row is exactly `{path,lstat:{type,mode,size}}`, where type is `dir|file|symlink|other` and mode is four-digit octal text. Never include dev/ino/uid/gid/mtime in a tree row. A regular file adds `sha256` of bytes; a symlink adds `link_text` and `link_sha256` without traversal. Hash `json.dumps(rows,sort_keys=True,separators=(",",":"),ensure_ascii=True).encode()` with no newline or BOM. Exact `{rows,sha256}` values in the listed order are `{3,37c08bb25f35a1cf02032735f71de5ec48d17d3a3b63027b5dc0a70180eade2b}`, `{11,aa2ee0e199ae81e0c564c2a2e8250d92df1642f979b5baded75eefa50bbd1333}`, `{105,a65e18af015c97cc0abbc66c1375b5ebefa57dbd701059f2268d1cf6f7215a00}`, `{3,b3707c112b5fae3bfc16a3550dc99d9acaf01b6e260d1f59f6f75a4186fff988}`, `{3,25563ce69b74f73f0dca832be21a12204583b7a0a28497997ed84d83177ca31f}`, `{4,7f01d3417ae35e67adc45d3f6d3dc9374b9cdfd3f6fcb59ed8d00e7886f0ea3a}`, `{10,7a29389b0a92d763362f360bc8527fe0fcd177569aabcb472799074b87940c1b}`, and `{6,72f283fdfb07c58199229d8673f5e4d5c3fda0c214da01f8c76f849b734ab72c}`.
- The two dangling residue rows are exactly `_build/test/lib/sigra_install_golden_tmp/priv` with link hash `da4ef4cc80409a80e378e3fe3437618daf8f4858b695c6ce4a1de41a7f3832c0` and `_build/test/phoenix-colocated/sigra_install_golden_tmp/node_modules` with link hash `6deff47b8463b8dfa2d4835b4197f0b10ca4cae56ae1e405e1596c95d2aa61f4`. Their exact external target strings are retained, `target_exists=false`, `target_lstat=null`, no traversal is attempted, and their becoming present is rejection.
- Every authorized home-probe, timing, exact-six, local terminal, capture, and validation boundary stores the same row schema before and after and requires exact equality, including the eight external target trees. The build anchors may not be cleaned, rebuilt, rewritten, normalized, or repaired.
- Repository build links are input authority only. Authenticate the canonical `copy_tree!/3` source in `test/support/install_fixture.ex`: each copy runs `materialize_or_omit_links!/2`, `break_hardlinks!/1`, then its existing in-process `first_unsafe_link/1` fail-closed check before returning to its caller. Do not invent an independent whole-graph pre-receiver scan or handshake. Successful canonical receipt/diagnostics plus the post-run verifier must prove the authenticated sanitizer source executed without an unsafe-link exception.

### InheritedExecutionAuthority

- Plan 74's exact seven-key stdin prerequisite, 126-byte SQL, 64-byte five-line output, raw CAPTURED process, PASS decision, and 37-hostile matrix remain byte/field equal and are not rerun.
- Plan 73's canonical exact-six entrypoint remains the sole receiver authority: exact ordered unique six, unconditional wrapper-owned `SIGRA_INSTALL_GOLDEN_PREPARED=1`, worker ceiling two, both-stream persistence, and exact 15 hostile cases.
- Plan 72's source authority remains the unique row selected by exact path `/private/tmp/plan46-collector/health.sql` from `pure_preflight.result.observed.plan46`; its exact ten hostile cases remain unchanged.
- Plan 71/72's parsed SQL policy retains the one accepted E-string tab sentinel and exact 23 hostile cases. Plan 76 changes only successor plan/root/schema/delimiter identities and the build-link policy.

<assumption_delta_decision>
Primary noun: authenticated repository build state.
Decision: corrected.
Rationale: Plan 75 incorrectly treated every descendant symlink as unsafe although the canonical prepared-fixture copier already materializes allowed repository content, omits disallowed/dangling sources, breaks hardlinks, and rejects any link left in its private graph. The safe input contract is an immutable no-follow allowlist with exact target authority, not mutation or cleanup of the repository build.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the exact build-link and runtime authority with zero effects (D-01–D-07, D-09)</name>
  <reversibility rating="reversible">Only in-memory mutations and read-only no-follow inventory are allowed before one canonical receipt is written.</reversibility>
  <precondition>Plan 75 plan is 52,432 bytes/SHA-256 `490a97fa7218139d523dc1c18eb3bf72e4e6f6553a897cc896f0493d509f8af6`; halted summary commit is `e8ea40b7`, and its 11,160-byte summary hashes to `301822c779687be477fd885405dba324a66f0d657030fc1dbcef99836f23ecf9`; no Plan 75 JSON receipt/root exists; all Plan 69/71/72/73/74 roots and artifacts retain their frozen identities; and no Plan 76 receipt or root exists.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREALLOCATION-VALIDATION.json</files>
  <read_first>
    - `235.1-75-SUMMARY.md` and `235.1-75-PLAN.md` — preserve the exact zero-action link-gate halt, legacy digest, unused budgets, and complete eleven-key/47-case runtime contract.
    - `deps`, `_build/test`, and all 34 exact link rows — use only lstat/readlink/no-follow file reads; never follow a dangling row or mutate either trust anchor.
    - `test/support/install_fixture.ex` — authenticate the existing materialize/omit, break-hardlinks, and unsafe-link rejection order that must yield a link-free private graph.
    - Plans/receipts/summaries 71–74 — inherit SQL, source, canonical entrypoint, stdin prerequisite, and immutable failure authority without process reuse.
    - `scripts/ci/library-partitions.sh`, `scripts/ci/install-golden.sh`, their tests/verifiers, `test/test_helper.exs`, Hex/cache/archive/runtime roots, repository `mix.lock`, and PATH tools — retain Plan 75's exact runtime and canonical-wrapper contracts.
  </read_first>
  <behavior>
    - "RED: Plan 75's all-descendant-link rejection fails against the exact frozen 34-row diagnostic while producing no process, allocation, mutation, cleanup, or network effect."
    - "GREEN: only authenticated_build_and_runtime_bundle accepts non-symlink trust anchors, the exact 34-row rich whitelist, 24 repo mappings, eight live external no-follow tree digests, two absent dangling targets, the exact eleven-key environment, wrapper/probe argv, capture ordering, and inherited policies."
    - "Exact 47 runtime rejected names: home_absent, home_empty, home_wrong, home_symlink, home_relative, home_inherited, home_overridden, hex_home_resolution_mismatch, mix_home_resolution_mismatch, path_missing_asdf_shims, path_missing_homebrew_bin, path_missing_usr_local_bin, path_missing_usr_bin, path_missing_bin, path_reordered, asdf_erlang_missing, asdf_erlang_wrong, asdf_elixir_missing, asdf_elixir_wrong, mix_env_missing, mix_env_wrong, lc_all_missing, lc_all_wrong, pg_environment_missing, pg_environment_wrong, password_absent, password_empty, extra_environment_key, inherited_environment_key, environment_copy_projection, timing_argv_changed, exact_six_argv_changed, repository_cwd_changed, stdin_env_home_added, stdin_env_runtime_key_added, stdin_env_pg_value_changed, stdin_env_key_missing, stdin_env_copy_projection, hex_cache_missing, hex_config_symlink, hex_config_stale_hash, hex_archive_missing, phx_new_archive_missing, tool_resolution_mismatch, stdout_capture_missing, stderr_capture_missing, status_capture_missing."
    - "Exact build-state rejected names: deps_root_symlink, build_root_symlink, whitelist_row_missing, whitelist_row_extra, whitelist_row_duplicate, link_path_changed, link_text_changed, link_text_hash_changed, link_lstat_changed, repo_app_segment_mismatch, repo_target_missing, repo_target_symlink, repo_target_lstat_changed, external_target_graph_changed, external_target_lstat_changed, dangling_target_became_present, new_external_target, new_dangling_target, dangling_follow_attempted, before_after_state_drift, prepared_graph_symlink, prepared_graph_hardlink."
    - "The canonical sorted-name JSON digests are runtime `45352a36648117dfaa852b909dfe7540baa6d21881fb6abf204283e6b1d20a36` and build-state `b452b57a2cca55a3fa6129bdec92405f7675cc6441cb0a7cbd23a48c0b9a26b0`; count alone cannot satisfy either set."
    - "Every adverse row is a unique one-field deep-copy mutation with accepted=false and allocator_calls=home_probe_calls=mix_calls=psql_calls=timing_calls=exact_six_calls=network_calls=github_calls=cleanup_calls=rebuild_calls=0."
    - "Python 3.9.6 and 3.14.4 produce identical exact name sets, row schemas/digests, inherited-policy semantics, and all-zero effect counts."
  </behavior>
  <action>Authenticate Plan 75 and every earlier frozen root/artifact with lstat, no-follow open, fstat equality, exact bytes, SHA-256, and canonical duplicate-key-free JSON. RED must intentionally prove that the predecessor's global descendant-link rejection rejects the observed 34-link control for that planned reason; validate the RED receipt with `gsd_run check tdd-red-evidence` before GREEN and commit the intentional failure as `test(235.1-76): add failing build-link authority contract`.

Implement a pure `Plan76BuildStateAuthority` evaluator over an already-captured in-memory object. Require exact C-sorted equality for every field and set in the interface, with the two roots checked without following, 34 links read by lstat/readlink, 24 repo targets opened only after exact repo class/path matching, eight live external targets traversed by a deterministic no-follow walker, and two dangling target strings checked only with lstat/lexists and never traversed. Recompute and accept the exact 34-row compact manifest identity before opening any resolved repo or live external target, so a new or changed external/dangling route rejects before traversal. Full lstat is permitted only for each anchor, link row, and top-level resolved target row. External target-tree descendant rows use only reduced `{type,mode,size}` plus content hash for regular files or link text/hash for untraversed symlinks. Record an attempted dangling traversal as immediate rejection. Do not authorize cleanup, rebuild, relink, compile, or normalization.

Persist `build_state_contract.anchors`, `whitelist`, `live_external_targets`, and `dangling_targets`, plus identical `before` and `after` objects containing those exact four keys. Also persist the exact 11,627 debugger-compatible compact observed-manifest bytes as base64 and `observed_manifest_sha256=055c5457505fd7b7084071af5e2f129a23dba8bd9ac34d4d9ca71202a0c85935`; the decoded fixed-position row array must equal `whitelist`. This is evidence of the current observation, never a source-code predicate over volatile values. The independent automated verifier below must reconstruct both anchors, every link/target row, every live external reduced tree, and both dangling absences directly from the filesystem and compare them to the receipt; count fields or a self-declared digest cannot pass alone.

Carry forward `Plan76ClosedRuntimeEnvironment` byte/field-identically from Plan 75, including the exact 47 rejected names and full literal environment/tool/argv/capture objects. Statically parse canonical scripts and `test/support/install_fixture.ex`; require the prepared route calls materialization before hardlink breaking before final unsafe-link rejection and requires zero unsafe links. Reconstruct inherited Plan 74 stdin, Plan 73 entrypoint, Plan 72 source, and Plan 71/72 SQL policy objects exactly. Run the accepted control plus every runtime/build adverse case as independent deep copies under both pinned Python runtimes. The pure gate may write only canonical `235.1-76-PREALLOCATION-VALIDATION.json` after both pass. No-follow reopen it and independently verify exact schemas, digests, omitted-field digest, identical runtime semantics, unchanged before/after local/predecessor manifests, empty Plan 76 root inventory, and all effect counters zero. Commit GREEN as `feat(235.1-76): authenticate immutable build links and runtime`.</action>
  <acceptance_criteria>One canonical dual-runtime receipt proves exact non-symlink roots, 34 rich whitelist rows, 24 repo links, eight live external target trees, two absent never-followed dangling targets, exact runtime/build adverse sets, sanitizer order, inherited policies, and zero effects; Plan 75 and all earlier history are unchanged; no Plan 76 root exists.</acceptance_criteria>
  <verify>
    <automated>PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREALLOCATION-VALIDATION.json &amp;&amp; for PY in /Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/bin/python3.9 /opt/homebrew/Cellar/python@3.14/3.14.4/Frameworks/Python.framework/Versions/3.14/bin/python3.14; do PYTHONDONTWRITEBYTECODE=1 "$PY" -c '
import base64,hashlib,json,os,stat,sys
nodup=lambda p:dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate"))
canon=lambda x:(json.dumps(x,sort_keys=True,separators=(",",":"))+"\n").encode()
raw=open(sys.argv[1],"rb").read(); d=json.loads(raw,object_pairs_hook=nodup); assert raw==canon(d) and d["schema"]=="sigra.phase235.1-plan76-preallocation-validation/v1" and d["status"]=="PASS"
repo="/Users/jon/projects/sigra"
def kind(s): return "l" if stat.S_ISLNK(s.st_mode) else "d" if stat.S_ISDIR(s.st_mode) else "f" if stat.S_ISREG(s.st_mode) else "o"
def full(p):
 s=os.lstat(p); return [kind(s),s.st_dev,s.st_ino,s.st_uid,s.st_gid,format(stat.S_IMODE(s.st_mode),"04o"),s.st_size,s.st_mtime_ns]
def reduced(p):
 s=os.lstat(p); t="symlink" if stat.S_ISLNK(s.st_mode) else "dir" if stat.S_ISDIR(s.st_mode) else "file" if stat.S_ISREG(s.st_mode) else "other"; return {"type":t,"mode":format(stat.S_IMODE(s.st_mode),"04o"),"size":s.st_size}
def file_sha(p):
 before=os.lstat(p); fd=os.open(p,os.O_RDONLY|os.O_NOFOLLOW); after=os.fstat(fd); assert (before.st_dev,before.st_ino)==(after.st_dev,after.st_ino); h=hashlib.sha256()
 while True:
  chunk=os.read(fd,1048576)
  if not chunk: break
  h.update(chunk)
 os.close(fd); return h.hexdigest()
def tree(root):
 paths=[root]
 for dp,dns,fns in os.walk(root,topdown=True,followlinks=False):
  dns.sort(key=lambda x:x.encode()); fns.sort(key=lambda x:x.encode()); paths.extend(os.path.join(dp,n) for n in dns+fns)
 rows=[]
 for p in sorted(set(paths),key=lambda x:("." if x==root else os.path.relpath(x,root)).encode()):
  rel="." if p==root else os.path.relpath(p,root); row={"path":rel,"lstat":reduced(p)}; t=row["lstat"]["type"]
  if t=="file": row["sha256"]=file_sha(p)
  elif t=="symlink": text=os.readlink(p); row.update({"link_text":text,"link_sha256":hashlib.sha256(text.encode()).hexdigest()})
  rows.append(row)
 tree_raw=json.dumps(rows,sort_keys=True,separators=(",",":"),ensure_ascii=True).encode(); return {"rows":len(rows),"sha256":hashlib.sha256(tree_raw).hexdigest()}
repo_map={"_build/test/lib/certifi/priv":"deps/certifi/priv","_build/test/lib/certifi/src":"deps/certifi/src","_build/test/lib/chimeway/priv":"deps/chimeway/priv","_build/test/lib/h2/include":"deps/h2/include","_build/test/lib/h2/src":"deps/h2/src","_build/test/lib/hackney/include":"deps/hackney/include","_build/test/lib/hackney/src":"deps/hackney/src","_build/test/lib/jose/include":"deps/jose/include","_build/test/lib/jose/priv":"deps/jose/priv","_build/test/lib/mailglass/priv":"deps/mailglass/priv","_build/test/lib/metrics/src":"deps/metrics/src","_build/test/lib/mimerl/src":"deps/mimerl/src","_build/test/lib/parse_trans/include":"deps/parse_trans/include","_build/test/lib/parse_trans/src":"deps/parse_trans/src","_build/test/lib/phoenix_live_view/priv":"deps/phoenix_live_view/priv","_build/test/lib/quic/include":"deps/quic/include","_build/test/lib/quic/priv":"deps/quic/priv","_build/test/lib/quic/src":"deps/quic/src","_build/test/lib/sigra/priv":"priv","_build/test/lib/sourceror/priv":"deps/sourceror/priv","_build/test/lib/tzdata/priv":"deps/tzdata/priv","_build/test/lib/unicode_util_compat/src":"deps/unicode_util_compat/src","_build/test/lib/webtransport/include":"deps/webtransport/include","_build/test/lib/webtransport/src":"deps/webtransport/src"}
live={"_build/test/lib/file_system/priv":(3,"37c08bb25f35a1cf02032735f71de5ec48d17d3a3b63027b5dc0a70180eade2b"),"_build/test/lib/idna/src":(11,"aa2ee0e199ae81e0c564c2a2e8250d92df1642f979b5baded75eefa50bbd1333"),"_build/test/lib/phoenix/priv":(105,"a65e18af015c97cc0abbc66c1375b5ebefa57dbd701059f2268d1cf6f7215a00"),"_build/test/lib/phoenix_html/priv":(3,"b3707c112b5fae3bfc16a3550dc99d9acaf01b6e260d1f59f6f75a4186fff988"),"_build/test/lib/phoenix_live_reload/priv":(3,"25563ce69b74f73f0dca832be21a12204583b7a0a28497997ed84d83177ca31f"),"_build/test/lib/swoosh/priv":(4,"7f01d3417ae35e67adc45d3f6d3dc9374b9cdfd3f6fcb59ed8d00e7886f0ea3a"),"_build/test/lib/telemetry/src":(10,"7a29389b0a92d763362f360bc8527fe0fcd177569aabcb472799074b87940c1b"),"_build/test/lib/telemetry_poller/src":(6,"72f283fdfb07c58199229d8673f5e4d5c3fda0c214da01f8c76f849b734ab72c")}
dangling={"_build/test/lib/sigra_install_golden_tmp/priv":"da4ef4cc80409a80e378e3fe3437618daf8f4858b695c6ce4a1de41a7f3832c0","_build/test/phoenix-colocated/sigra_install_golden_tmp/node_modules":"6deff47b8463b8dfa2d4835b4197f0b10ca4cae56ae1e405e1596c95d2aa61f4"}
b=d["build_state_contract"]; anchors={"deps":full(repo+"/deps"),"_build/test":full(repo+"/_build/test")}; assert all(v[0]=="d" and not os.path.islink(repo+"/"+p) for p,v in anchors.items()) and b["anchors"]==anchors
observed=[]
for dp,dns,fns in os.walk(repo+"/_build/test",topdown=True,followlinks=False):
 dns.sort(key=lambda x:x.encode("utf-8")); fns.sort(key=lambda x:x.encode("utf-8"))
 for name in dns+fns:
  p=os.path.join(dp,name)
  if not os.path.islink(p): continue
  rel=os.path.relpath(p,repo); text=os.readlink(p); target=os.path.normpath(os.path.join(os.path.dirname(p),text)); exists=os.path.lexists(target)
  if target.startswith(repo+"/deps/"): target_class="repo_dependency_dir"
  elif target==repo+"/priv" or target.startswith(repo+"/priv/"): target_class="repo_priv_dir"
  elif exists: target_class="external_fixture_shared_dep_dir"
  else: target_class="external_fixture_source_missing"
  classification="repo_build_link" if target_class.startswith("repo_") else "retained_install_fixture_residue"
  observed.append([rel,text,hashlib.sha256(text.encode("utf-8")).hexdigest(),full(p),classification,target_class,exists,full(target) if exists else None])
observed.sort(key=lambda r:r[0].encode("utf-8")); rows=b["whitelist"]; assert rows==observed and len(rows)==len({r[0] for r in rows})==34 and {r[0] for r in rows}==set(repo_map)|set(live)|set(dangling)
manifest=base64.b64decode(b["observed_manifest_base64"],validate=True); expected_manifest=json.dumps(rows,separators=(",",":"),ensure_ascii=True).encode(); assert manifest==expected_manifest and len(manifest)==11627 and hashlib.sha256(manifest).hexdigest()==b["observed_manifest_sha256"]=="055c5457505fd7b7084071af5e2f129a23dba8bd9ac34d4d9ca71202a0c85935"
for r in rows:
 path,text,link_sha,link_id,classification,target_class,exists,target_id=r; p=repo+"/"+path; target=os.path.normpath(os.path.join(os.path.dirname(p),text)); assert os.path.islink(p) and link_sha==hashlib.sha256(text.encode("utf-8")).hexdigest() and link_id==full(p)
 if path in repo_map:
  expected=repo+"/"+repo_map[path]; expected_class="repo_priv_dir" if path=="_build/test/lib/sigra/priv" else "repo_dependency_dir"; assert target==expected and classification=="repo_build_link" and target_class==expected_class and exists is True and not os.path.islink(target) and os.path.isdir(target) and target_id==full(target)
 elif path in live:
  assert not target.startswith(repo+"/") and classification=="retained_install_fixture_residue" and target_class=="external_fixture_shared_dep_dir" and exists is True and not os.path.islink(target) and os.path.isdir(target) and target_id==full(target) and tree(target)=={"rows":live[path][0],"sha256":live[path][1]}
 else:
  assert classification=="retained_install_fixture_residue" and target_class=="external_fixture_source_missing" and link_sha==dangling[path] and not os.path.lexists(target) and exists is False and target_id is None
trees=[{"path":p,"rows":v[0],"sha256":v[1]} for p,v in sorted(live.items())]; misses=[{"follow_attempted":False,"link_text_sha256":dangling[p],"path":p,"target_exists":False,"target_lstat":None} for p in sorted(dangling)]; current={"anchors":anchors,"dangling_targets":misses,"live_external_targets":trees,"whitelist":rows}; assert b["live_external_targets"]==trees and b["dangling_targets"]==misses and b["before"]==b["after"]==current
rc=d["runtime_contract"]; keys=["ASDF_ELIXIR_VERSION","ASDF_ERLANG_VERSION","HOME","LC_ALL","MIX_ENV","PATH","PGDATABASE","PGHOST","PGPASSWORD","PGPORT","PGUSER"]; env={"ASDF_ELIXIR_VERSION":"1.19.5-otp-28","ASDF_ERLANG_VERSION":"28.4.1","HOME":"/Users/jon","LC_ALL":"C","MIX_ENV":"test","PATH":"/Users/jon/.asdf/shims:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin","PGDATABASE":"sigra_test","PGHOST":"localhost","PGPASSWORD":"&lt;redacted-present&gt;","PGPORT":"5432","PGUSER":"postgres"}; assert rc["environment_keys"]==keys and rc["observed_redacted"]==env
rn=sorted(r["name"] for r in rc["rejected"]); bn=sorted(r["name"] for r in b["rejected"]); assert len(rn)==len(set(rn))==47 and hashlib.sha256(canon(rn)).hexdigest()=="45352a36648117dfaa852b909dfe7540baa6d21881fb6abf204283e6b1d20a36" and len(bn)==len(set(bn))==22 and hashlib.sha256(canon(bn)).hexdigest()=="b452b57a2cca55a3fa6129bdec92405f7675cc6441cb0a7cbd23a48c0b9a26b0" and all(not r["accepted"] for r in rc["rejected"]+b["rejected"])
assert b["prepared_graph_policy"]=={"in_process_first_unsafe_link":True,"post_run_verifier":True,"sanitizer_source_authenticated":True} and set(d["counts"].values())=={0} and d["root_inventory_before"]==d["root_inventory_after"]==[] and len(d["runtimes"])==2 and len({r["semantic_sha256"] for r in d["runtimes"]})==1
' "$PV" || exit; done &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan76-build-links.*' -print -quit)"</automated>
    <fails_when>RED is invalid; receipt/schema/set/digest/root/link/target/environment/policy equality fails; a dangling target is followed or becomes present; runtimes disagree; any effect count is nonzero; a predecessor changes; or a Plan 76 root exists.</fails_when>
  </verify>
  <done>The corrected immutable build-link boundary and unchanged closed runtime are proven before any process or allocation budget is spent.</done>
</task>

<task type="auto">
  <name>Task 2: Spend one raw Mix home probe without build-state drift (D-01–D-07, D-09)</name>
  <reversibility rating="costly">The single process capture is immutable one-shot evidence; failure consumes it and requires another successor.</reversibility>
  <precondition>Task 1 RED and GREEN commits exist in order; its receipt independently reopens as PASS; both roots and all 34 links/targets equal its canonical before state; no Plan 76 probe/decision/root exists; and all predecessor roots/artifacts remain frozen.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json</files>
  <read_first>
    - `235.1-76-PREALLOCATION-VALIDATION.json` — sole build/runtime schema and exact before authority.
    - `235.1-75-PLAN.md` — retain the exact argv, eleven-key environment, raw stream persistence, cache/archive/tool manifests, and expected `/Users/jon\n` bytes.
    - `235.1-74-POSTGRESQL-PROBE.json` and `235.1-74-PREREQUISITE-VALIDATION.json` — carry exact stdin authority without rerunning psql.
    - Canonical wrappers, Mix/Hex state, both build anchors, all whitelist targets, and every failed predecessor — reauthenticate before and after the process.
  </read_first>
  <action>Reopen and independently prove Task 1, then capture a same-schema build-state before object. Launch exactly once with the exact Plan 75 home-probe argv, cwd, shell=false, DEVNULL, both pipes, and the exact fresh eleven-key Plan 76 environment. Persist complete raw status/stdout/stderr/environment/time metadata atomically as `235.1-76-HOME-PROBE.json`, no-follow reopen it, and only then interpret output or status.

Require status zero, empty stderr, and stdout exactly the 11 bytes `/Users/jon\n` with base64 `L1VzZXJzL2pvbgo=` and SHA-256 `9d75bf0190e2e157453723af13843a699ec75794f6f175ca48a8e7e024980297`; no normalization is allowed. Reauthenticate HOME, Hex config/cache, pinned archives/runtimes, lock/deps/build state, scripts/tools, every predecessor, all 34 whitelist rows, 24 repo targets, eight external target trees, and two absent dangling targets. Write canonical `235.1-76-PREREQUISITE-VALIDATION.json` only when its same-schema before/after objects are exactly equal and counts are `home_probe=1,mix=1` with every other action zero. On any failure, persist BLOCKED with the raw capture, allocate no root, freeze all state, and require a successor without retry.</action>
  <acceptance_criteria>Exactly one raw Mix process is durably captured before parse and proves `/Users/jon`; all local, build-link, external-target, dangling, and predecessor manifests are unchanged; the decision is PASS; and no allocator, timing, exact-six, psql, database, network, GitHub, cleanup, rebuild, or Plan 76 root exists.</acceptance_criteria>
  <verify>
    <automated>RAW=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json &amp;&amp; PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json &amp;&amp; /usr/bin/python3 -c 'import base64,hashlib,json,sys; r=json.load(open(sys.argv[1],"rb")); v=json.load(open(sys.argv[2],"rb")); out=base64.b64decode(r["stdout_base64"],validate=True); err=base64.b64decode(r["stderr_base64"],validate=True); assert r["schema"]=="sigra.phase235.1-plan76-home-probe/v1" and r["status"]=="CAPTURED" and r["process_count"]==1 and r["persisted_before_parse"] is True and r["cwd"]=="/Users/jon/projects/sigra" and r["environment"]["observed_redacted"]["HOME"]=="/Users/jon" and len(r["environment"]["keys"])==11 and r["status_code"]==0 and err==b"" and out==b"/Users/jon\n" and hashlib.sha256(out).hexdigest()=="9d75bf0190e2e157453723af13843a699ec75794f6f175ca48a8e7e024980297"; assert v["schema"]=="sigra.phase235.1-plan76-prerequisite-validation/v1" and v["status"]=="PASS" and v["build_state_before"]==v["build_state_after"] and v["counts"]["home_probe"]==v["counts"]["mix"]==1 and set(x for k,x in v["counts"].items() if k not in {"home_probe","mix"})=={0}' "$RAW" "$PV" &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan76-build-links.*' -print -quit)"</automated>
    <fails_when>Either receipt is absent/noncanonical; call count differs; raw capture did not precede parse; status/stream/HOME/env differs; build/local/predecessor state drifts; a dangling target is followed/present; another counter is nonzero; or a Plan 76 root exists.</fails_when>
  </verify>
  <done>One frozen pre-root Mix capture proves HOME under the corrected immutable build-state contract and authorizes exactly one Plan 76 allocation.</done>
</task>

<task type="auto">
  <name>Task 3: Allocate one Plan 76 root and run the unchanged local/live route (D-01–D-09)</name>
  <reversibility rating="costly">Root, local processes, evidence commit, and GitHub runs are append-only one-shot evidence; any failure must remain durable.</reversibility>
  <precondition>Tasks 1–2 independently PASS; exact runtime/build before/after, one home probe, cache/archive/tool/script manifests, and inherited stdin/entrypoint/source/SQL policies agree; no Plan 76 root exists; failed predecessor state is unchanged; GitHub authentication is valid; and repo `szTheory/sigra`, PR #234, base `main`, head `refs/heads/ci/phase-235-1-evidence`, remote head `87338db7`, and candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3` match inherited authority.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <read_first>
    - All three Plan 76 pre-root receipts — sole runtime, build-state, home, allocator, and zero/one-call accounting authority.
    - Plan 75 summary and all Plans 68/69/71/72/73/74 summaries/failures/roots — freeze every failed budget and prohibit partial-evidence reuse.
    - Plan 74 stdin prerequisite, Plan 73 canonical entrypoint, Plan 72 source authority, and Plan 71/72 SQL authority — inherit exact accepted/adverse sets without rerun.
    - Canonical timing/exact-six wrappers, sanitizer code, workflow, routing/validation verifiers, and Phase 235.1 contract — reauthenticate immediately before and after every authorized route.
  </read_first>
  <action>Authenticate both pre-root gates and capture the same-schema build state before allocation. Invoke one execute launcher. Its allocator calls mktemp once with `/private/tmp/sigra-p2351-plan76-build-links.XXXXXXXX`, creates one mode-0700 root and exclusive mode-0600 marker, materializes authenticated sources once, and writes/reopens canonical driver/handoff/finalizer authority. Never clean or rebuild repository `deps` or `_build/test`; never touch predecessor roots.

Run one local chain under the exact eleven-key environment: inherited challenge-bound PostgreSQL 14 host gate and Plan 74 five-line stdin authority without rerunning its probe; all 19 parser fixtures; Plan 72 accepted/ten-hostile source set; Plan 71/72 accepted/23-hostile SQL set; exact 47 runtime cases; exact 22 build-state cases; one dry run, candidate/current observation, admission, health sample, and exactly one `bash scripts/ci/library-partitions.sh` process. Persist the timing process before parse. Require exactly three complete pairs, six green children, all 225 disjoint paths in deterministic 114/111 sequential partitions, positive raw durations, same-run parallel formatter evidence, independently recomputed ratio <=2.0, and nine ledger-disjoint payloads. Surround each authorized process with same-schema exact build-state before/after equality.

Invoke exactly one `bash scripts/ci/install-golden.sh` from authenticated cwd/environment. The wrapper, not its parent, owns `SIGRA_INSTALL_GOLDEN_PREPARED=1`; raw `mix test` and caller-prepared mode are forbidden. Persist both streams/status/argv/env/time before parse. Authenticate the canonical fixture source and require each existing `copy_tree!/3` call to run `materialize_or_omit_links!/2`, `break_hardlinks!/1`, and its in-process `first_unsafe_link/1` rejection before returning. Do not invent a whole-graph pre-receiver scan, handshake, or code change. Require exact ordered unique six, worker ceiling two, phx_new 1.8.8, complete positive phases, six variants, unique partitions/ports, zero failed paths, and unchanged receipt/diagnostic verifier PASS; those successes prove no per-copy unsafe-link exception occurred. Same-schema repository/external build state must remain exactly equal afterward. Any local failure writes a full-stream failure receipt, freezes the root, performs no remote action, and requires a successor without retry.

Seal the local manifest once and require an independent Plan 76 terminal verifier to reopen every receipt, authenticated sanitizer source, exact-six diagnostics, policy, process, whitelist, target tree, predecessor, and budget. It proves the per-copy sanitizer contract from exact source plus successful canonical receipt/diagnostics; it does not claim a separate pre-receiver graph scan. Only after terminal PASS may remote actions begin. Query `gh api rate_limit` once; at REST core <=250, make no further CI polling request until reset; HTTP 403/429 is a hard stop until reported reset/retry time. Give every primary run exactly one watcher using `gh run watch &lt;run-id&gt; --repo szTheory/sigra --compact --interval 60 --exit-status`; fetch one structured summary after completion and failed logs only on failure. Never poll the same run/attempt from multiple agents.

If and only if one attempt-1 workflow failure is durably classified from its summary/logs as a non-code GitHub infrastructure transient, consume the single retry budget on exactly one diagnostic workflow rerun. Persist the original failure, classification, rerun command, run ID/attempt, and non-admissible status before interpreting the diagnostic result. The diagnostic run/attempt receives its own single watcher with the same exact 60-second command and no duplicate watcher; fetch its structured summary once and failed logs only if it fails. The diagnostic result can explain the failure but can never replace attempt-1 evidence, authorize a receipt, or complete TEST-01/02/03. No deterministic/code failure, HTTP 403/429, low-rate-budget stop, unclassified failure, or second transient can rerun; there is no second retry.

Exclude retained Plan 35–75 run/job IDs. Push candidate once; capture one automatic pull_request attempt-1 and one workflow_dispatch attempt-1 run at the same SHA, requiring exact route/job/artifact/two-partition/exact-six predicates. Write/reopen the two evidence receipts, commit only them as candidate's direct child, and push once. Capture one automatic PR attempt-1 validation and one distinct dispatch attempt-1 validation at the evidence SHA. Write `235.1-VALIDATION-RUN.json` only after all four pass, binding Plan 76 root/terminal/home/runtime/build/stdin/entrypoint/timing/exact-six authority; run unchanged offline verifiers. Do not push again or reconcile prose.</action>
  <acceptance_criteria>Exactly one Plan 76 root/local chain exists; every route preserves exact repository and external build state; authenticated successful in-process per-copy sanitizer outcomes and the post-run verifier prove that the prepared exact-six private graph has zero symlinks and zero multiply-linked regular files; terminal verification authenticates one home probe, three timing pairs, six green children, 225 paths, and one canonical exact-six process; all predecessors remain unchanged; four fresh distinct non-excluded attempt-1 runs prove capture/validation PR/dispatch; evidence lineage is exact; both offline verifiers pass.</acceptance_criteria>
  <verify>
    <automated>PLAN76_ROOT="$(/usr/bin/python3 -c 'import os,re; r=sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan76-build-links\.[A-Za-z0-9_]{8}",e.name) and e.is_dir(follow_symlinks=False)); assert len(r)==1; print(r[0])')" &amp;&amp; /usr/bin/python3 "$PLAN76_ROOT/local/verify-plan76-terminal.py" --root "$PLAN76_ROOT" --preallocation-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREALLOCATION-VALIDATION.json --home-probe .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json --prerequisite-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json --plan69-root /private/tmp/sigra-p2351-plan69-recovery.G9QrBacu --plan71-root /private/tmp/sigra-p2351-plan71-sql-guard.knJsmWsF --plan72-root /private/tmp/sigra-p2351-plan72-source-authority.Q1orfipi --plan74-root /private/tmp/sigra-p2351-plan74-stdin-probe._55jsnhr &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Root discovery differs; any predecessor/build/environment/stdin/source/SQL/entrypoint/stream/manifest/partition/ratio/exact-six/sanitizer predicate fails; an action repeats outside the single classified diagnostic allowance; an admitted run is stale/excluded/retried/wrong-ref/wrong-SHA/non-green; a diagnostic rerun is admitted, unclassified, rate-limited, deterministically failed, multiply watched, or followed by another retry; lineage differs; or an offline verifier fails.</fails_when>
  </verify>
  <done>One fresh Plan 76 chain proves the complete local, PR, and non-PR routes while leaving verified capture/validation receipts as Plan 70's sole new TEST authority.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | Items | Coverage |
|---|---|---|
| GOAL | Two measured exhaustive PR partitions plus exact-six non-PR coverage without protected regression | Task 3 produces both routes from one fresh Plan 76 chain; no failed partial evidence is promoted. |
| REQ | TEST-01, TEST-02, TEST-03 | All three are in frontmatter and retain their exact protected predicates in Task 3. |
| RESEARCH | Independent fail-closed receipts, same-run timing, deterministic receiver ownership, pinned fixture/runtime state | Tasks 1–3 authenticate the build/runtime boundary, canonical owner, and distinct evidence route. |
| CONTEXT | D-01 through D-09 and recovery addenda | Tasks cite every decision, preserve two partitions/exact-six/attempt-1 budgets, delay reconciliation, and retain FAST-01/GATE-05. |
| LATEST EVIDENCE | Plan 75 halted before RED because its descendant-link ban rejected the existing 34-link build | Task 1 replaces only that policy with an exact immutable whitelist and preserves all unused budgets. |
| HISTORY | Plans 68/69/71/72/73/74/75 and blocked Plan 70 | Every failed plan stays immutable; only Plan 76 can supply new completion evidence, and Plan 70 remains reconciliation-only. |
| CONTEXT deferred | Unrelated release, installer-product, auth, security, admin, and other CI work | Excluded; no deferred item is implemented. |

</source_coverage_audit>

<recovery_decisions>

- Plan 75's plan, halted summary, zero-action diagnosis, and unused budgets are immutable; its all-descendant-link ban is history, not an instruction to clean or rebuild.
- Build trust-anchor roots themselves must be non-symlinks. Their descendant links are accepted only by exact 34-row whitelist and exact target authority; new, missing, duplicated, mutated, followed-dangling, or drifted rows fail closed.
- The canonical copier's authenticated in-process per-copy sanitizer remains authoritative: each copy materializes/omits links, breaks hardlinks, then calls `first_unsafe_link/1` before returning; receipt/diagnostic success and the post-run verifier prove that source contract without a new handshake.
- Plan 76 retains the exact eleven-key HOME environment, one raw pre-root probe, Plan 74 stdin framing, Plan 73 canonical wrapper, Plan 72 path-selected source, Plan 71/72 SQL policy, protected partitions/exact-six/TEST predicates, FAST-01, GATE-05, and one diagnostic-only retry for one durably classified non-code GitHub infrastructure transient; attempt-1 alone remains admissible and there is no second retry.
</recovery_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository build links → preallocation | Existing links are accepted only by exact row and target authority. |
| External residue → local route | Eight live targets are no-follow hashed; two dangling targets are never traversed. |
| Repository seed → prepared private graph | Existing sanitizer must leave no symlink or hardlink before receiver execution. |
| Closed environment → Mix/Hex | Only the exact eleven-key projection may reach the probe and wrappers. |
| Local terminal → GitHub | Remote budgets stay blocked until all build/runtime/local predicates pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-76-01 | Spoofing | Build whitelist | high | mitigate | Exact 34-row rich schema, 24/10 classification, target class/existence/lstat, and dual-runtime adverse cases in Task 1. |
| T-235.1-76-02 | Tampering | External targets | high | mitigate | No-follow tree digests before/after every route; two dangling strings remain absent and untraversed. |
| T-235.1-76-03 | Tampering | Prepared private graph | high | mitigate | Authenticate existing per-copy sanitizer call order, require canonical receipt/diagnostic success, and have the post-run verifier bind those outcomes to the unchanged source; no invented handshake. |
| T-235.1-76-04 | Information Disclosure | Password and Hex config | high | mitigate | Persist only redacted password/digest and config identity/hash, never secret/config bytes. |
| T-235.1-76-05 | Repudiation | One-shot processes | high | mitigate | Raw capture precedes parse; same-schema manifests bracket every action; failure consumes the action and freezes state. |
| T-235.1-76-06 | Repudiation | Failed predecessor history | high | mitigate | Authenticate every frozen root/artifact and prohibit retry, reuse, relabeling, cleanup, or reconstruction from partial output. |
| T-235.1-76-07 | Spoofing | GitHub evidence | high | mitigate | Exact repo/ref/SHA/event/workflow/job/attempt/artifact identities, excluded IDs, direct-child lineage, and offline verifiers. |
| T-235.1-76-08 | Denial of Service | Local/live budget | medium | mitigate | One probe/root/timing/exact-six action and one 60-second watcher per run/attempt; at most one classified infrastructure failure gets one diagnostic non-admissible rerun, with no second retry. |
</threat_model>

<verification>
Task 1 proves the exact immutable 34-link input and eleven-key runtime under both pinned interpreters with zero effects. Task 2 spends one raw Mix probe and proves same-schema build-state equality before allocation. Task 3 runs one canonical local/live route, authenticates the existing in-process per-copy sanitizer through its source and successful receipts/diagnostics, and brackets every action with the same immutable input authority. Plan 70 remains unavailable until Plan 76's summary and unchanged evidence verifiers pass.
</verification>

<success_criteria>
Plan 76 succeeds only when the dual-runtime build/runtime receipt, raw home-probe capture, prerequisite decision, sole fresh root, three timing pairs, canonical exact-six process with authenticated successful per-copy sanitizer outcomes, local terminal, capture pair, and distinct validation pair all PASS; the 47 runtime, 22 build-state, 37 stdin, 15 entrypoint, ten source, and 23 SQL hostile cases remain exact and unique; Plans 69/71/72/73/74/75 stay frozen; only one classified diagnostic infrastructure rerun is permitted and never admitted; repository/external state is unchanged; and unchanged routing/validation verifiers accept the resulting evidence.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-SUMMARY.md` when done.
</output>
