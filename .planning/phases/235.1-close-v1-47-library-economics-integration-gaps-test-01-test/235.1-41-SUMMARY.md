---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 41
subsystem: ci-evidence
tags: [ref-authority, scoped-cleanup, archive-only, health-gate, deterministic-block]
status: halted
requires:
  - phase: 235.1-39
    provides: rejected one-shot health history and canonical 137-payload ledger
  - phase: 235.1-40
    provides: hardened cleanup contract and zero-action precondition stop
provides:
  - exact PR-head authority proof and explicit planning-ref negative control
  - canonical scoped cleanup receipt for the exact 132-name PostgreSQL allowlist
  - fail-closed record of an incomplete single health capture with zero timing or GitHub work
affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
actuals:
  tokens: 5541
  tasks: 1
  commits: 1
plan_head_before: e4c932235dbb98bc498e52ad58a4b3bb6262ea50
tech-stack:
  added: []
  patterns: [fully-qualified-ref-authority, repeatable-read-cleanup-snapshot, per-row-server-quoted-drop, incomplete-health-fail-closed]
key-files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-40-SUMMARY.md
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-41-SUMMARY.md
  modified: []
key-decisions:
  - "Authorize delivery only from PR #234 head ci/phase-235-1-evidence at 87338db7; keep ci/phase-235-16-source-complete as a rejected negative control."
  - "Drop only the immutable 1+131 PostgreSQL allowlist after repeatable-read owner proof; preserve every unidentified, nonmatching, retained, or ambiguous resource."
  - "Treat the incomplete one-shot health collection as a permanent Plan 41 stop; do not time, push, dispatch, watch, or write live evidence."
patterns-established:
  - "A partially collected health observation never becomes an admissible receipt, even when its observed predicates look healthy."
requirements-completed: []
coverage:
  - id: D1
    description: Exact PR-head authority and hardened scoped cleanup are proven canonically.
    requirement: TEST-01
    verification:
      - kind: integration
        ref: /private/tmp/plan41-ref-authority.json and /private/tmp/plan41-resource-cleanup.json
        status: pass
    human_judgment: false
  - id: D2
    description: One complete healthy sample authorizes one three-pair local recovery series.
    requirement: TEST-02
    verification:
      - kind: integration
        ref: /private/tmp/plan41-preflight-health.json
        status: fail
    human_judgment: false
    rationale: The fixed sample was incomplete and no canonical health receipt exists.
  - id: D3
    description: Four fresh original-attempt-1 live runs prove candidate and evidence routes.
    requirement: TEST-03
    verification:
      - kind: e2e
        ref: PR and workflow_dispatch capture/validation
        status: fail
    human_judgment: false
    rationale: The health hard stop prohibited all timing and live actions.
duration: 20m
completed: 2026-09-11
---

# Phase 235.1 Plan 41: Corrected Ref and Scoped Cleanup Summary

**The corrected PR authority and exact 132-database cleanup passed, but the recovery stopped permanently because its sole health collection did not produce the required atomic PostgreSQL snapshot or canonical admission receipt.**

## Task 1 — Complete

### Plan 40 zero-action transfer

Before any Plan 41 mutation, independent replay proved that Plan 40 had no cleanup, health, timing, evidence, or GitHub artifact. The unchanged database inventory was exactly 132 lines/6,819 bytes with SHA-256 `bebf055763feb7682e543f0c31a534f0b9721bbc61bd5c722c1e36552b8ad8a4`, split one base plus 131 prepared names and zero names outside the two anchored grammars. Candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3` had no remote ref or workflow run.

`235.1-40-SUMMARY.md` records `status: halted`, `requirements-completed: []`, and zero tasks/commits/actions. Its commit `a069ea30` changes exactly that one file. A complete post-write replay passed before the Plan 40 budget was transferred.

### Exact remote-ref authority

The two remote refs were fetched and resolved separately:

| Role | Fully qualified ref | SHA | Admission |
| --- | --- | --- | --- |
| PR #234 delivery head | `refs/heads/ci/phase-235-1-evidence` | `87338db7c2678601e20ed3140fd8de1981dfbeb1` | sole authority |
| planning publication | `refs/heads/ci/phase-235-16-source-complete` | `e932a2c84fd356b5a3fe561fb9c77925fa836b34` | ignored negative control |

PR #234 was open in `szTheory/sigra`, based on `main`, and exposed the exact delivery head name/OID. Candidate `3713dcd4...` had exactly that OID as its sole parent and exact tree `94fca667d3f5226347548bdeea625bc737a4348e`. Swapping either the ref name or OID made authorization false.

`/private/tmp/plan41-ref-authority.json` is duplicate-key-safe, sorted compact JSON with one LF and non-self-referential receipt SHA-256 `8dd749aea9f8d1a6a4e14fc938e38ce7f9bc625b11b3355bf72cc74392232780`.

### Hardened scoped cleanup

One `REPEATABLE READ READ ONLY` PostgreSQL transaction captured backend PID `59559`, unassigned xid, snapshot `32478575:32478575:`, parsed xmin/xmax/xip, all non-template database names/owners, and activity. Runtime identity resolved from candidate configuration to localhost:5432, database `sigra_test`, configured/session/current user `postgres`, password presence only, and all allowlisted owners equal to the current user.

The immutable allowlist was exactly one base plus 131 prepared names, 132 lines, 6,819 bytes, SHA-256 `bebf0557...`. Candidate source provenance was bound to:

- `test/support/install_fixture.ex`: blob `e34ddfe185a79cbccfe82047894e8cf2fcffc55d`, 88,320 bytes, SHA-256 `e31568e6ff5103d775afdf546420644493e4bdbc8ecf5a40bb3b27e1926157dc`;
- `test/support/install_fixture_performance_test.exs`: blob `ea60c732f8e3058f82e4a948b2f0f7638bfae63d`, 38,993 bytes, SHA-256 `4a24b7b0359457e9a282be5fbc3f665f0ef90f0047246db63ec410fddcb1479f`.

Each row received a fresh exact name/owner recheck, a server-produced quoted identifier, an immediate PID/database/user/owner session recheck, a force-free drop, and an immediate absence check. Results:

| Fact | Exact result |
| --- | ---: |
| Allowlisted databases dropped | 132/132 |
| Failed drops | 0 |
| Sessions terminated | 0 |
| Residual allowlisted databases/sessions | 0/0 |
| Nonmatching database names | 1,667 before and after |
| Nonmatching set SHA-256 | `fc90950d4c7467e3dd91a19ccbbdc6672e74fea25f14db10d6344775a52c6a02` before and after |
| Path/process cleanup actions | 0/0 |

Recorded Plan 37–39 evidence roots were retained. Unrecorded Plan 35/36/38/39 attempt roots were ambiguous and left untouched. No PostgreSQL service, agent, current workspace, unidentified session, nonmatching database, or retained evidence input was touched.

`/private/tmp/plan41-resource-cleanup.json` contains the before/allowlist/actions/exclusions/after proof, all 132 per-row actions, source/runtime/snapshot identities, and its ref-receipt link. It is duplicate-key-safe, canonical, non-self-referential, and independently reconstructs receipt SHA-256 `399b24553b3b00b80f1967a5ed41492f4ef26c7104714c97f5c5c4b00f9d42f9`. The Task 1 verifier and auto-mode tracer replay both passed.

## Task 2 — Deterministic Hard Stop

### Fresh isolation and untimed gates

A first pre-build isolation attempt was abandoned after its historical-object absence check was mistakenly run after the local content commit, where shared source blobs correctly existed. It had no dependency copy, database, build, sample, or timing. The accepted fresh root `/private/tmp/sigra-plan41.XaQFR7` checked historical/candidate object absence while its object database was still empty, then created one local content commit reproducing tree `94fca667...`.

The accepted isolation had no remote, alternates, common-dir/worktree linkage, or bundle. It physically copied 3,369 locked dependency files and 801 Mix/Hex cache files with no symlinks or shared inodes, used candidate-local Mix/Hex/cache/tmp roots, a dedicated database `sigra_plan41_0ad0ed669063`, an unset `MIX_BUILD_PATH`, and a new default `_build/test`. Offline dependency status covered 59 dependencies and compile-only preconditioning passed.

Every untimed gate passed:

- Bash 3/5 portability, seven routing mutations, exact authority preflight, and partition synthetics;
- exact 114/111 assignment over 225 disjoint/exhaustive paths, modeled totals 19,974,682us / 19,974,684us;
- 136 focused tests, zero failures;
- complete ordinary manifest: 225 paths, 33 doctests, three properties, 2,637 tests, zero failures, 12 skips;
- source-complete and terminal-ratification offline attestations, Actionlint, formatting, clean tracked tree, and zero returned allowlisted scratch databases.

The 137-entry denial ledger remained exactly 8,905 bytes with SHA-256 `0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08`.

### Incomplete one-shot health collection

The only health marker was created at `2026-09-11T16:41:58Z`. Its first `vm_stat` command used an unsupported argument order and exited with usage text. Without re-reading already captured load/CPU/memory facts, the command was corrected to the platform-supported fixed two-reading form. The collection then captured a one-minute load of 7.60 against 18 logical CPUs, 58% free memory, zero competing candidate processes, `pg_isready` in 32ms, and `SELECT 1` in 21ms.

The required single-transaction PostgreSQL snapshot did not materialize: the client rejected the long `\copy` meta-command with a parse error. Consequently:

- `/private/tmp/plan41-health-db-snapshot.raw` is absent;
- `/private/tmp/plan41-preflight-health.preimage.json` is absent;
- `/private/tmp/plan41-preflight-health.json` is absent;
- no health receipt SHA-256 exists and `admission.overall` cannot be proven.

The plan forbids a second sample, resample, wait, substitution, or incomplete receipt. Execution therefore stopped without retrying the database observation or promoting the partial healthy-looking host facts.

`/private/tmp/plan41-health-capture-failure.json` preserves the exact two command errors, marker and partial-artifact paths, current database state, and zero downstream-action counts as canonical duplicate-safe JSON. Its independently reconstructed receipt SHA-256 is `798c86bc581cacc2e33ec2496ee373c08fbe7b18ac309a7c4f54ee4d8aa77110`. Current state has zero allowlisted databases and one exact Plan 41 candidate database; the resulting 1,668 nonmatching non-template databases are the cleanup-preserved 1,667 plus that newly created candidate database.

## Task 3 — Not Run

The incomplete health gate prohibited the runner and all remote work. PR #234 remains at `87338db7...`; no remote ref points at candidate `3713dcd4...`.

## Budget and External-Action Accounting

- scoped cleanup: one accepted snapshot, 132 exact drops, zero terminations, zero path/process actions;
- health markers: exactly one; complete health samples/admission receipts: zero;
- health waits, resamples, or replacement observations: zero;
- `library-partitions.test.sh` invocations: zero;
- Plan 41 timing pairs/payloads: zero/zero;
- candidate/evidence pushes, workflow dispatches, CI watchers, structured summaries, failed-log fetches, and artifact downloads: zero;
- PR/scaffold/validation evidence receipts and evidence commits: zero.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] Used literal-safe value comparison after `psql -c` rejected client variables**

- **Found during:** Task 1, before the first database action.
- **Issue:** `psql` did not expand `:'db'` variables inside a `-c` argument.
- **Fix:** Because every immutable name had already passed either complete anchored grammar, value predicates used those strict lowercase/underscore names and drop identifiers still came only from server `format('%I', name)` output.
- **Verification:** 132/132 owner/name/action/absence checks passed; the nonmatching set remained byte-identical.
- **Files modified:** temporary cleanup receipts only.

**2. [Rule 3 - Blocking issue] Moved historical-object absence proof before the standalone content commit**

- **Found during:** Task 2 initial isolation.
- **Issue:** The first check ran after importing current source, so an unchanged historical source blob was correctly present.
- **Fix:** Abandoned that pre-build root without reuse and created a wholly fresh root where candidate/historical absence was proven in the empty object database before importing content.
- **Verification:** Fresh local content commit reproduced exact tree `94fca667...` and passed every untimed gate.
- **Files modified:** no repository files.

### Unresolved Gate Failure

**3. Health snapshot capture remained incomplete**

- **Found during:** Task 2 one-shot health collection.
- **Issue:** The fixed `vm_stat` syntax was initially wrong, then the required PostgreSQL `\copy` snapshot meta-command parse-failed.
- **Disposition:** Stopped permanently under the one-sample/no-retry contract. No health receipt, timing, or GitHub action was produced.

## Known Stubs

None.

## Threat Review

No endpoint, auth path, schema, dependency, workflow byte, source, calibration, assignment, threshold, candidate, or remote ref changed. Destructive scope stayed confined to 132 exact owner-proven install-fixture databases. The incomplete health capture was not laundered into evidence, preserving the local-to-live trust boundary.

## Self-Check: PASSED

- Commit `a069ea30` exists and changes only the Plan 40 halted summary.
- Both canonical Task 1 receipts parse without duplicate keys and independently reproduce digests `8dd749ae...` and `399b2455...`.
- The cleanup receipt records 132 successful drops, zero terminations, zero residual allowlisted resources, and unchanged nonmatching digest `fc90950d...`.
- The accepted isolated tree is exact and every untimed gate passed.
- The health database snapshot, health receipt, timing marker/ledger, runner receipts, and all three repository evidence receipts are absent.
- The canonical failure diagnostic reconstructs digest `798c86bc...` and records zero downstream actions.
- PR #234 remains at `87338db7...`, and no remote ref points at the candidate.
