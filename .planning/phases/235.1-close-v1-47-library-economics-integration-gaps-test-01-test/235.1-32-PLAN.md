---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 32
type: execute
wave: 29
depends_on: [235.1-31]
files_modified:
  - scripts/ci/library-partitions.sh
  - scripts/ci/library-partitions-portability.test.sh
  - scripts/ci/verify-library-routing-evidence.sh
  - scripts/ci/verify-library-routing-evidence.test.sh
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The runner resolves repository root physically, so macOS `/tmp` and `/private/tmp` aliases normalize to one containment boundary under Bash 3 and Bash 5."
    - "Only physical candidate `test/` paths or already-normalized safe `test/...` paths are accepted; external lookalikes, prefix collisions, dot traversal, and malformed paths fail closed."
    - "Only the runner pin and its expected mutation entry refresh; Plan 29 calibration, Plan 30 relational assertions, Plan 31 epoch clock, workflow, source, lifecycle, and audit remain exact."
    - "A deterministic four-file successor retains sole parent `47f9578`, then one fresh timed invocation produces exactly three local pairs before strict live capture/validation."
  artifacts:
    - path: scripts/ci/library-partitions.sh
      provides: "Physical-root timing receipt normalization"
    - path: scripts/ci/library-partitions-portability.test.sh
      provides: "Dual-shell alias and hostile-path regression"
    - path: scripts/ci/verify-library-routing-evidence.sh
      provides: "Refreshed runner active pin"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
      provides: "Distinct live validation evidence"
  key_links:
    - from: "script location"
      to: "formatter absolute paths"
      via: "physical repository root from `pwd -P`"
      pattern: "pwd -P|root_prefix"
    - from: "runner bytes"
      to: "CURRENT_ACTIVE_AUTHORITY"
      via: "digest refresh plus seven mutation failures"
      pattern: "scripts/ci/library-partitions.sh"
    - from: "exact successor"
      to: "live receipts"
      via: "three local pairs then capture and disjoint validation"
      pattern: "run_attempt|duration_ms"
---

<objective>
Canonicalize the production runner's repository root without weakening receipt containment, then prove and capture the exact successor.

Purpose: Plan 31's epoch clock measured partition 1 truthfully at 13,113ms, but the runner used lexical `/tmp/...` while formatter metadata used physical `/private/tmp/...`. This safe macOS alias was rejected before a complete pair; physical root resolution removes the false mismatch while hostile paths remain fail-closed.
Output: RED/GREEN dual-shell alias regression, refreshed runner authority, exact successor, three local validations, and strict live capture/validation—or durable diagnostics.

Decision coverage: D-01 preserves the PR owner; D-02/D-03/D-04 preserve exhaustive measured partitions and factor-two bound; D-05/D-06 preserve exact-six dispatch; D-07 admits fresh evidence only; D-08 leaves reconciliation to Plan 05; D-09 preserves calibration/history/protected facts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.47-MILESTONE-AUDIT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-29-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-30-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-31-SUMMARY.md
@scripts/ci/library-partitions.sh
@scripts/ci/library-partitions.test.sh
@scripts/ci/library-partitions-portability.test.sh
@scripts/ci/verify-library-partitions.sh
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-routing-evidence.test.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED/GREEN physical-root containment and refresh the runner pin (D-02–D-04, D-07, D-09)</name>
  <files>scripts/ci/library-partitions.sh, scripts/ci/library-partitions-portability.test.sh, scripts/ci/verify-library-routing-evidence.sh, scripts/ci/verify-library-routing-evidence.test.sh</files>
  <behavior>
    - RED: Invoking the actual runner through a lexical symlink alias while fake formatter rows use the physical path reproduces Plan 31's containment rejection under Bash 3 and Bash 5.
    - GREEN: Physical and lexical aliases normalize to identical safe `test/...` rows, with truthful epoch durations and unchanged receipt/child semantics.
    - HOSTILE: Sibling prefix lookalikes, absolute external paths, `..`/`.` traversal, doubled separators, and non-test paths are rejected without publishing a successful child.
    - AUTHORITY: Only the runner digest changes in the seven-entry active authority and all isolated mutations still fail.
  </behavior>
  <action>Start from Plan 31 successor `34931b9cb8e2edd6ee68dd81c0ce18acf8a5e0a4`/tree `462bb0594e7c1f843401693eb5d72639c524ce38`. Extend the existing unpinned portability test first. Create a physical temporary repository fixture and a distinct symlink alias to it; invoke the actual runner by the alias under system Bash 3 and modern Bash 5 while the fake formatter emits the physical repository path. Record RED from the current lexical-root rejection. Preserve controlled/real epoch-clock, argv/order, empty/nounset, failure propagation, relational calibration, and active-authority mutation cases.

Add isolated hostile rows for an external absolute path, a sibling whose name merely begins with the root, root-contained strings with `../` or `./`, doubled separators, and paths outside `test/`. Require each case nonzero, never normalized into an admissible `test/...` row, and a truthful failure/not-run combined receipt. Include safe already-relative paths and physical absolute candidate paths as positive controls. Tests must drive the production `normalize_timing_paths`, not a duplicate helper.

GREEN changes runner root initialization to enter the script-relative repository directory and emit its physical path using portable `pwd -P`; do not depend on GNU `realpath`, Python path resolution, or macOS-only flags. Tighten normalization so accepted output is exactly a canonical repository-relative `test/..._test.exs` path with no empty, `.` or `..` component, while absolute input must begin with physical `ROOT/test/` at a path-component boundary. Keep epoch clock, sequential children, fixed receipt paths, status propagation, and schema unchanged.

Refresh only the new runner digest in `CURRENT_ACTIVE_AUTHORITY` and its adverse expected list; preserve the other six entries and all history. Prove pristine preflight and seven isolated mutations. Scan active scripts/tests/workflow for superseded digest `22dac275dac16cda0a52224b704397e9a836f644667cc949967997feeca489e7`; it may remain only in historical plans/summaries. Commit RED, GREEN, and pin refresh by concern. Do not change calibration/manifest/loader/workflow/ordinary source/audit/lifecycle/receipts/threshold.</action>
  <verify>
    <automated>env -u MIX_BUILD_PATH bash scripts/ci/library-partitions-portability.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh --preflight . &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
    <fails_when>Alias tests do not exercise production normalization under both shells; hostile paths pass; safe physical paths fail; clock/child/receipt semantics regress; pins/mutations fail; or frozen authority changes.</fails_when>
  </verify>
  <done>The actual runner accepts only canonical physical-candidate timing paths across `/tmp` aliases and both Bash versions, rejects hostile paths, and has one refreshed active pin.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the containment successor and run one three-pair validation (D-01–D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json</files>
  <precondition>Plan 31 successor `34931b9cb8e2edd6ee68dd81c0ce18acf8a5e0a4` has tree `462bb0594e7c1f843401693eb5d72639c524ce38` and sole parent `47f9578fc97edc63603953d446e79fac884326c3`.</precondition>
  <action>Verify Task 1 changes exactly runner, portability test, routing verifier, and routing verifier test. Reassert Plan 29 calibration/manifest/110–115 assignment, Plan 30 relational contract, Plan 31 epoch clock, source index, workflow, audit, lifecycle, and no recalibration. Derive the successor twice from Plan 31 tree plus exactly four Task 1 blobs, with sole parent `47f9578fc97edc63603953d446e79fac884326c3` and fixed metadata; require identical commit/tree, exact four-file diff from Plan 31, and unchanged six unique changed paths from `9ae29062`.

Create a fresh external detached worktree using its resolved physical `/private/tmp/...` path, independently copied locked deps/caches, candidate-local homes/cache/tmp, offline resolution, absent `MIX_BUILD_PATH`, and default `_build/test`. Do not reuse earlier worktrees. Run compile-only preconditioning and every untimed portability/alias/hostile, clock, routing/preflight, calibration/source/lifecycle, verifier, relocation/containment, protected, Actionlint, formatting, cleanliness, and unrelated-dirt check.

Invoke `bash scripts/ci/library-partitions.test.sh` exactly once here and nowhere in verification. Its synthetic cases plus exactly three real pairs are the entire fresh budget. Require green children, exhaustive/disjoint 225 paths at 110/115, only canonical timing rows, truthful epoch wall durations, and ratio at or below 2.0 three times. First failure blocks without retry/rematerialization/recalibration/reassignment/threshold or code change/alternate candidate/GitHub.

After local success, fast-forward-push once and pair one schedule/workflow-dispatch exact-six run with the automatic PR run at identical SHA. Apply AGENTS.md rate floor, 403/429 stop, one 60-second watcher/run, one structured summary, and failure logs only on failure. Deterministic failure blocks; one durable non-code transient may rerun same ID once, but attempt above 1 is inadmissible. Only dual attempt-1 success writes both capture receipts.</action>
  <verify>
    <automated>env -u MIX_BUILD_PATH bash scripts/ci/library-partitions-portability.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; actionlint .github/workflows/ci.yml &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Tree/authority drifts; timed count is not one; normalization or wall time is false; any pair is missing/red/above 2.0; live provenance is invalid; or watcher/rate rules are violated.</fails_when>
  </verify>
  <done>The exact containment successor yields three truthful local pairs and one strict attempt-1 capture pair, or blocks without retry/substitution/tuning/waiver.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the evidence commit on one disjoint live pair (D-07–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <action>Commit exactly two capture receipts atop successor; prove parent and only-two-file diff. Fast-forward-push once and pair distinct automatic PR plus schedule/workflow-dispatch runs at evidence SHA under Task 2 rules. Only dual attempt-1 success writes validation. Require four disjoint IDs, exact SHA/events/jobs/commands/digests, canonical timing paths, truthful durations, no retry/waiver, and no local timed rerun. Leave audit to Plan 05.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Receipt/provenance/path/duration/lineage fails; run reused/cross-SHA/red/above attempt 1; policy violated; evidence partial; or local timing reruns.</fails_when>
  </verify>
  <done>One distinct attempt-1 pair validates the evidence commit, or blocks without alternate evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Lexical alias to physical worktree | Equivalent paths must canonicalize once. |
| Timing row to normalized receipt | External/traversal lookalikes must fail closed. |
| Runner repair to candidate | Four declared blobs only; calibration unchanged. |
| GitHub artifacts to receipts | Exact attempt/event/SHA/job provenance. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-32-01 | Tampering | timing path normalization | high | mitigate | Physical root plus component-safe hostile cases. |
| T-235.1-32-02 | Spoofing | prefix lookalike | high | mitigate | Exact `ROOT/test/` boundary and canonical relative grammar. |
| T-235.1-32-03 | Elevation of Privilege | successor tree | high | mitigate | Double derivation and exact diffs. |
| T-235.1-32-04 | Spoofing | live provenance | high | mitigate | Exact attempt/SHA/events and disjoint IDs. |
| T-235.1-32-05 | Denial of Service | polling | medium | mitigate | Rate floor, hard stops, one 60-second watcher. |
</threat_model>

## Multi-Source Coverage Audit
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Comparable ordinary plus exact-six routes | 32 | COVERED | Safe normalization, local/live proof. |
| REQ | TEST-01 | PR timing evidence | 32 | COVERED | Canonical exact-candidate receipts. |
| REQ | TEST-02 | Exhaustive comparable partitions | 32 | COVERED | Three truthful pairs. |
| REQ | TEST-03 | Exact-six success | 32 | COVERED | Dispatch capture/validation. |
| RESEARCH | — | macOS alias-safe containment | 32 | COVERED | `pwd -P`, dual-shell hostile matrix. |
| CONTEXT | D-01–D-09 | Preserve contracts/fresh proof/history | 32, 05 | COVERED | No scope drift. |

<verification>
RED/GREEN actual-run production normalization through symlink aliases and hostile rows, refresh runner pin, freeze exact successor, run one three-pair validation, then strict disjoint live proof.
</verification>

<success_criteria>
- Physical candidate paths normalize under `/tmp`/`/private/tmp`; hostile paths fail.
- Bash 3/5, epoch clock, child receipts, relational authority, and seven mutations pass.
- No recalibration or frozen source/workflow/audit change.
- Exactly one timed invocation yields three local pairs before capture/validation.
</success_criteria>

<output>Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-32-SUMMARY.md` with RED/GREEN alias matrix, hostile cases, pins/tree/diffs, local/live evidence, budgets, and status.</output>
