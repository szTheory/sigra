---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 77
type: execute
gap_closure: true
wave: 79
depends_on:
  - 235.1-76
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-PREALLOCATION-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Plan 76 remains immutable: its one green canonical timing pair is authenticated and admitted, while its consumed formatter-path failure is retained only as negative history and is never rerun or treated as evidence (TEST-01, TEST-02; D-02, D-03, D-04, D-06)."
    - "Four newly allocated repeat actions run exactly once under fresh Plan 77 identities; each formatter receives only its partition's fixed receipt path, and each successful receipt is no-follow reopened, validated, and archived before that fixed path is reused (TEST-01, TEST-02; D-02, D-03, D-04)."
    - "The admitted ordinary authority consists of three comparable parallel ExUnit pairs—one immutable Plan 76 pair plus two fresh Plan 77 pairs—with six receipts, 225 unique paths per pair, exhaustive 114/111 partition cardinalities, and no serial timing pass or second suite execution (TEST-01, TEST-02; D-01, D-02, D-03)."
    - "The exact six-module install-golden scaffold runs once on the named schedule/workflow_dispatch receiver, fails closed, publishes both required artifacts, and remains outside the protected pull-request aggregate (TEST-03; D-05, D-06)."
    - "A fresh attempt-1 pull-request capture and workflow_dispatch capture at one candidate SHA plus a distinct attempt-1 validation pair at the direct evidence child SHA prove the final routing; process success never substitutes for a missing receipt (TEST-01, TEST-02, TEST-03; D-06, D-07)."
    - "The FAST-01 baseline remains n=52 and p50=469 seconds, all 93 GATE-05 rows remain intact, and no product, CI topology, formatter, workflow, Mix alias, or test source is edited (D-01, D-05, D-09)."
    - "Plan 70 and TEST-01/TEST-02/TEST-03 reconciliation remain blocked until all five tracked Plan 77 receipts are green and the unchanged offline verifiers pass (D-07, D-08)."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-PREALLOCATION-VALIDATION.json"
      provides: "Zero-effect fixed-path schedule proof, immutable Plan 76 authority pins, five unresolved edge assumptions, and one-shot budget ledger"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json"
      provides: "Authenticated three-pair ordinary timing authority plus the one-shot exact-six local terminal receipt"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh protected pull-request capture receipt for the sole ordinary library owner"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh non-PR exact-six receiver capture receipt"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct direct-child attempt-1 validation pair and exact evidence lineage"
  key_links:
    - from: "test/support/ci/ex_unit_timing_formatter.ex"
      to: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json"
      via: "the formatter allowlist controls child output paths; fresh Plan 77 archive labels are storage destinations only and never formatter inputs"
      pattern: "allowed_paths -> fixed_receipt -> no_follow_reopen -> validate -> immediate_archive"
    - from: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-SUMMARY.md"
      to: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json"
      via: "the successor imports only Plan 76's authenticated green canonical pair and explicitly excludes the consumed failed repeat action"
      pattern: "canonical_pair_digest + excluded_failure_digest + fresh_repeat_digests"
    - from: ".github/workflows/ci.yml"
      to: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      via: "literal MIX_ENV=test mix ci ownership and the byte-stable protected aggregate"
      pattern: "library_tests_shard -> Library tests"
    - from: ".github/workflows/ci.yml"
      to: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      via: "schedule/workflow_dispatch exact-six job and its two fail-closed artifacts"
      pattern: "library_install_golden_non_pr -> Library install golden (non-PR)"
    - from: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      to: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      via: "candidate capture SHA, direct evidence-child SHA, excluded run IDs, and distinct attempt-1 validation runs"
      pattern: "capture_runs -> evidence_commit -> validation_runs"
  assumptions:
    - id: EDGE-TEST-01-UNCLASSIFIED
      status: unverified
      flagged: true
      statement: "With no phase SPEC, TEST-01 behavior for an otherwise valid timing receipt containing an unclassified test path remains unspecified; this plan records the gap and does not invent admission semantics."
    - id: EDGE-TEST-02-ADJACENCY
      status: unverified
      flagged: true
      statement: "With no phase SPEC, TEST-02 behavior when valid partition receipts are not adjacent in capture order remains unspecified; this plan requires the existing evidence schema and does not invent adjacency semantics."
    - id: EDGE-TEST-02-EMPTY
      status: unverified
      flagged: true
      statement: "With no phase SPEC, TEST-02 behavior for an empty otherwise well-formed partition remains unspecified; this plan admits only the known non-empty 114/111 partition universe."
    - id: EDGE-TEST-02-ORDERING
      status: unverified
      flagged: true
      statement: "With no phase SPEC, TEST-02 behavior for reordered otherwise valid timing rows remains unspecified; this plan preserves the formatter's deterministic ordering contract."
    - id: EDGE-TEST-03-UNCLASSIFIED
      status: unverified
      flagged: true
      statement: "With no phase SPEC, TEST-03 behavior for an unclassified scaffold module remains unspecified; this plan admits exactly the six named modules and nothing else."
  prohibitions:
    - id: PROHIBIT-TEST-01-SERIAL-SUBSTITUTE
      status: unverified
      flagged: true
      statement: "A serial timing pass, second full-suite execution, or process exit code without the exact receipt cannot satisfy TEST-01."
    - id: PROHIBIT-TEST-01-RETRY
      status: unverified
      flagged: true
      statement: "No Plan 76 process identity or consumed Plan 76 action may be rerun, and no failed diagnostic may enter admitted evidence."
    - id: PROHIBIT-TEST-02-INCOMPLETE
      status: unverified
      flagged: true
      statement: "An empty, overlapping, duplicated, missing, reordered, or non-comparable ordinary partition set cannot satisfy TEST-02."
    - id: PROHIBIT-TEST-03-PR-ROUTING
      status: unverified
      flagged: true
      statement: "The exact-six scaffold cannot run in, influence, or satisfy the protected pull-request aggregate."
    - id: PROHIBIT-STATUS-RECONCILIATION
      status: unverified
      flagged: true
      statement: "Requirements, roadmap, audit, and Plan 70 status cannot be reconciled from local success or partial remote evidence."
---

<objective>
Close the Plan 76 formatter-path gap without mutating or rerunning Plan 76: authenticate its immutable canonical timing pair, allocate two fresh repeat pairs whose children use the formatter's fixed receipt paths and immediately archive validated bytes, then spend the still-unused exact-six and GitHub evidence budgets once to prove TEST-01, TEST-02, and TEST-03.

Purpose: Restore the measurable library-suite economics required by the phase goal while preserving the single `MIX_ENV=test mix ci` pull-request owner (D-01), parallel same-run timing (D-02, D-03), the production formatter contract (D-04), non-PR exact-six routing (D-05), fresh attempt-1 authority (D-06), delayed status reconciliation (D-07, D-08), and FAST-01/GATE-05 invariants (D-09).

Output: A zero-effect Plan 77 preallocation receipt, one local terminal receipt joining the immutable Plan 76 pair to four fresh fixed-path repeat receipts and one exact-six receipt, and the three existing-schema capture/validation evidence receipts. Plan 70 remains blocked until these outputs pass.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.47-MILESTONE-AUDIT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/COVERAGE.md
@test/support/ci/ex_unit_timing_formatter.ex
@test/support/ci/ex_unit_timing_formatter_test.exs
@scripts/ci/library-partitions.sh
@scripts/ci/verify-library-partitions.sh
@scripts/ci/install-golden.sh
@scripts/ci/verify-library-install-golden.sh
@scripts/ci/library-economics.sh
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
@.github/workflows/ci.yml
@mix.exs
</context>

<gap_contract>
Plan 76 is halted, not failed-over or superseded. Its frozen root and all consumed process records are historical read-only authority. The canonical pair is green at 114/111 paths and 12,056/12,949 ms. The consumed repeat child exited zero but produced no receipt because it was assigned a pair-specific archive path rejected by `Sigra.CI.ExUnitTimingFormatter`; its exit status is therefore negative evidence only. Exact-six, GitHub capture, GitHub validation, and offline evidence budgets were not spent. Plan 77 corrects only the repeat formatter-path schedule and does not edit runtime sources or reopen any Plan 76 action.
</gap_contract>

<scope_gate_results>
## Scope Detector Results

- API detector result: API has no integration.
- UI/frontend detector result: UI/frontend is false.
- Schema detector result: no schema push applies.
- Binding boundary: these detector verdicts preserve Plan 77's evidence-only surface. The plan may write only the five declared evidence JSON artifacts and must make no runtime source change, including no product, API, UI/frontend, schema, migration, CI topology, formatter, workflow, Mix alias, or test-source edit.

</scope_gate_results>

<assumption_delta>
Decision: no-change. The primary noun remains the formatter's fixed receipt-path authority. A fresh Plan 77 action identity may produce a receipt at a fixed formatter path and then copy authenticated bytes to a distinct archive; archive multiplicity does not pluralize formatter authority. This is one bounded recovery allocation, not a singular-to-plural identity-model change.
</assumption_delta>

<tracer_boundary>
The full path cannot be used as the tracer because each Mix/GitHub action is costly and one-shot. Task 1 is therefore a production zero-effect tracer: it proves the exact transformation `formatter allowlist -> fresh child fixed path -> immediate immutable archive` against real tracked sources and the frozen failure before Task 2 crosses the local execution boundary. It creates the permanent preallocation authority consumed by Tasks 2 and 3; it is not a prototype.
</tracer_boundary>

<interface_contracts>

## FixedReceiptSchedule

- Derive the two ordinary fixed formatter paths independently from `test/support/ci/ex_unit_timing_formatter.ex` and `scripts/ci/library-partitions.sh`; the sets and partition mapping must agree exactly.
- Allocate exactly four fresh action IDs: two Plan 77 repeat-pair identities multiplied by partitions 1 and 2. None may equal a Plan 76 action/process identity.
- Each action's `SIGRA_EXUNIT_TIMING_PATH` is its partition's fixed formatter path. A pair-specific archive destination is never supplied to the child.
- Before each child, remove only that fixed output receipt. After raw process persistence, reopen the fixed receipt with no-follow semantics, authenticate it, and copy those exact bytes to its unique append-only archive before another child may reuse the fixed path.
- Missing, empty, stale, symlinked, malformed, swapped-partition, wrong-universe, unsorted, duplicate, or failed receipts halt admission even when the child status is zero.

## Plan77LocalTerminal

- Schema symbol: `sigra.phase235.1-plan77-local-terminal/v1`.
- Pin Plan 76 plan, summary, preallocation, HOME, and prerequisite receipts to SHA-256 `9521f1263e242204dc27406fa46ca0df6168f168009072f465ba93cedaa6a84b`, `da8632f927ede7e6054f2d44aa3713a93a1a7c4d227171a977a8cdcdb454ca0c`, `1f18e5eea1d75e05107d5cfb12bbf84bffb874f2baad13c588b7e318dab7e17f`, `c6ec362e1fc99f62ca22238c9937129829631a306c8e450ab266e309f35f9fc7`, and `e7df44f4c5165c5c04320bfeb094217a439f03c3bcd340264e2327dc8d11b213` respectively.
- Pin the formatter and ordinary wrapper tracked sources to SHA-256 `e9ad6dbaaba24528c60be38f54ddd8d1e946ab539943dd744a1602eba4a29d0c` and `deae1229e1bfabea6924d2512db1293026d6904497c2ac804f0478afc04471dd` before allocation; any runtime-affecting source drift halts before an execution.
- Admit exactly three ordinary pairs: immutable Plan 76 canonical pair plus two Plan 77 repeat pairs. Each pair has two green receipts, exhaustive disjoint 114/111 paths, 225 unique paths total, parallel ExUnit durations from the same partition executions, and `max(duration_ms) * 1000 <= min(duration_ms) * 2000`.
- Retain the Plan 76 failed repeat process and every excluded predecessor ID as negative history. Neither a status code nor a diagnostic can satisfy a missing receipt.
- Exact-six runs once after ordinary terminal validation and proves exactly `AuditLog`, `CrossValidation`, `DataManager`, `ERIAdapter`, `ERIClient`, and `Settings`, with authenticated diagnostics and zero symlink/multiply-linked prepared files.

## RemoteEvidenceLineage

- The candidate SHA receives exactly one automatic pull_request capture and one workflow_dispatch capture, both attempt 1. The former proves the sole ordinary owner and protected aggregate; the latter proves the exact-six non-PR receiver.
- Commit only `235.1-PR-EVIDENCE.json` and `235.1-SCAFFOLD-EVIDENCE.json` as the candidate's direct child, then push once and capture one new pull_request attempt-1 validation and one distinct workflow_dispatch attempt-1 validation at that evidence SHA.
- `235.1-VALIDATION-RUN.json` binds the local terminal digest, the two capture run IDs, all negative IDs, the direct-child evidence commit, the two validation run IDs, and the unchanged evidence schemas. No subsequent push or prose reconciliation occurs in this plan.

</interface_contracts>

<artifacts_this_phase_produces>

## Artifacts this phase produces

- `235.1-77-PREALLOCATION-VALIDATION.json` — schema `sigra.phase235.1-plan77-preallocation-validation/v1`; permanent zero-effect fixed-path and budget proof.
- `235.1-77-LOCAL-TERMINAL.json` — schema `sigra.phase235.1-plan77-local-terminal/v1`; permanent local ordinary/exact-six terminal authority.
- `235.1-PR-EVIDENCE.json` — existing `sigra.library-partitions-evidence/v1` contract populated from the fresh Plan 77 capture.
- `235.1-SCAFFOLD-EVIDENCE.json` — existing exact-six scaffold evidence contract populated from the fresh Plan 77 dispatch capture.
- `235.1-VALIDATION-RUN.json` — existing `sigra.library-routing-validation/v1` contract populated from the distinct direct-child validation pair.
- Symbols introduced only in evidence: `FixedReceiptSchedule`, `Plan77LocalTerminal`, and `RemoteEvidenceLineage`; no production module, workflow job, Mix alias, or CI owner is introduced.

</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the fixed formatter-path recovery schedule with zero effects</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-PREALLOCATION-VALIDATION.json</files>
  <read_first>
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PLAN.md`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-SUMMARY.md`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREALLOCATION-VALIDATION.json`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json`
    - `test/support/ci/ex_unit_timing_formatter.ex`
    - `test/support/ci/ex_unit_timing_formatter_test.exs`
    - `scripts/ci/library-partitions.sh`
    - `scripts/ci/verify-library-partitions.sh`
  </read_first>
  <precondition>Plan 76's plan, halted summary, three tracked prerequisite receipts, frozen root inventory, canonical pair, and local failure receipt still match their recorded identities and digests; no Plan 77 execution root or process record exists.</precondition>
  <reversibility rating="reversible">This task performs no Mix, database, network, GitHub, root allocation, or fixed-receipt write; deleting its new tracked JSON returns to the prior state.</reversibility>
  <action>Implement D-02/D-03/D-04/D-06 as a deterministic, zero-effect evaluator and write `235.1-77-PREALLOCATION-VALIDATION.json` only after it passes. Authenticate the five pinned Plan 76 tracked artifacts, then no-follow inspect the single frozen Plan 76 root without changing timestamps or contents. Record its one admitted canonical pair and its consumed missing-receipt child as disjoint authorities, with retry false and the failed child excluded from every positive count. Independently extract the ordinary formatter allowlist from `test/support/ci/ex_unit_timing_formatter.ex` and the partition-to-path mapping from `scripts/ci/library-partitions.sh`; require the formatter and wrapper source digests in `Plan77LocalTerminal` and exact agreement between both tracked sources.

Model exactly four fresh one-shot action IDs—two successor repeat identities for each of partitions 1 and 2. Each row must bind partition ID, fixed formatter path, unique archive label, removal-before-child, raw-process-persisted-before-parse, no-follow reopen, receipt validation, immediate archive, and fixed-path-reuse prohibition. Reject the exact Plan 76 pair-specific formatter-path schedule and negative cases for a scaffold path used by an ordinary partition, swapped partition paths, missing/extra schedule rows, reused Plan 76 IDs, duplicate archive labels, an archive label equal to a formatter path, archive-after-reuse, parse-before-process-persistence, missing/empty/stale/symlinked receipt, wrong partition, wrong universe, non-green receipt, unordered rows, or status-only admission. Record effect counters for Mix, PostgreSQL, filesystem root allocation, receipt output, exact-six, git, GitHub API, watcher, capture, validation, and reconciliation; every counter must remain zero.

Store the preallocation schema declared under Artifacts, status `PASS`, `assumption_delta: no-change`, the primary noun `formatter fixed receipt-path authority`, the five flagged unresolved edge assumptions exactly as declared in frontmatter, source/artifact digests, before/after frozen-root inventory, fresh action schedule, negative-case results, unspent one-shot budgets, and zero effects. This is the durable tracer consumed by Tasks 2 and 3, not a substitute timing receipt.</action>
  <acceptance_criteria>The tracked preallocation receipt independently proves the real formatter/wrapper fixed-path contract, rejects Plan 76's pair-specific child-path mistake, allocates four distinct unconsumed Plan 77 action identities, keeps every execution/evidence counter at zero, preserves all Plan 76 bytes and processes, records the no-change assumption decision, and carries the five unresolved SPEC-fallback edge assumptions without inventing behavior.</acceptance_criteria>
  <verify>
    <automated>PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-PREALLOCATION-VALIDATION.json &amp;&amp; /usr/bin/python3 -c 'import json,sys; v=json.load(open(sys.argv[1],"rb")); assert v["schema"]=="sigra.phase235.1-plan77-preallocation-validation/v1" and v["status"]=="PASS" and v["assumption_delta"]=="no-change" and v["primary_noun"]=="formatter fixed receipt-path authority"; assert len(v["schedule"])==4 and {(x["repeat"],x["partition_id"]) for x in v["schedule"]}=={(1,1),(1,2),(2,1),(2,2)}; assert len({x["action_id"] for x in v["schedule"]})==4 and len({x["archive_label"] for x in v["schedule"]})==4; assert all(x["fixed_formatter_path"] in v["fixed_formatter_paths"] and x["archive_label"] not in v["fixed_formatter_paths"] for x in v["schedule"]); assert set(v["flagged_assumptions"])=={"EDGE-TEST-01-UNCLASSIFIED","EDGE-TEST-02-ADJACENCY","EDGE-TEST-02-EMPTY","EDGE-TEST-02-ORDERING","EDGE-TEST-03-UNCLASSIFIED"}; assert set(v["effects"].values())=={0} and v["plan76"]["retry"] is False and v["plan76"]["canonical_pair_admitted"] is True and v["plan76"]["failed_repeat_admitted"] is False' "$PV" &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan77-fixed-receipts.*' -print -quit)"</automated>
    <fails_when>The receipt is absent or malformed; any pinned predecessor/source digest or frozen-root inventory differs; the fixed-path sources disagree; the fresh schedule is not exactly four unique actions; a Plan 76 ID is reused; a negative case is accepted; an assumption is omitted/resolved; an effect counter is nonzero; or any Plan 77 execution root exists.</fails_when>
  </verify>
  <done>The permanent zero-effect tracer proves the only allowed recovery schedule and leaves all local/remote one-shot budgets untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Complete two fresh fixed-path repeat pairs and the exact-six local terminal</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json</files>
  <read_first>
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-PREALLOCATION-VALIDATION.json`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-SUMMARY.md`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json`
    - `test/support/ci/ex_unit_timing_formatter.ex`
    - `scripts/ci/library-partitions.sh`
    - `scripts/ci/verify-library-partitions.sh`
    - `scripts/ci/install-golden.sh`
    - `scripts/ci/verify-library-install-golden.sh`
    - `scripts/ci/library-economics.sh`
    - `mix.exs`
  </read_first>
  <precondition>The Task 1 receipt is PASS with zero effects, all pinned runtime-affecting sources still match, the authenticated Plan 76 canonical-pair and failure records are immutable, PostgreSQL prerequisites remain valid, and none of the four Plan 77 repeat action IDs or exact-six action ID has been consumed.</precondition>
  <reversibility rating="costly">The tracked terminal JSON can be reverted, but four Mix child allocations and the exact-six allocation are one-shot evidence actions whose raw process records must be preserved even on failure.</reversibility>
  <action>Implement TEST-01/TEST-02 under D-01/D-02/D-03/D-04/D-06 and TEST-03 under D-05/D-06. Allocate exactly one fresh append-only Plan 77 evidence root using the namespace asserted by Task 1; never modify, link into, execute within, or reopen an action in the frozen Plan 76 root. Materialize the authenticated external build-link graph and exact HOME/environment contract established by Plan 76, persist every command/environment/stream/status record before interpreting it, and create the terminal verifier before any child runs. Halt before execution on source, SQL, build, HOME, stdin, entrypoint, manifest, wrapper, formatter, or preallocation drift.

Import Plan 76's green canonical pair by no-follow reading and digest authentication only. Keep its consumed missing-receipt repeat process in the negative ledger and do not execute the canonical wrapper or any Plan 76 action again. For each of the four fresh schedule rows, remove only the row's fixed formatter output, launch one direct `MIX_ENV=test mix test` partition child with the exact authenticated environment plus `MIX_TEST_PARTITION` and `SIGRA_EXUNIT_TIMING_PATH` set to that partition's fixed formatter path, and persist raw stdout/stderr/status before parsing. Immediately no-follow reopen the fixed output and require a green receipt whose partition ID, sorted unique test paths, cardinality, aggregate duration, per-test timing rows, and manifest digest match the authenticated partition. Before any later child can reuse the fixed output, copy the exact validated bytes to the row's unique append-only archive, fsync, reopen, compare byte length and SHA-256, and seal the archive. If status is zero but the fixed receipt is missing or invalid, freeze the root and halt exactly as Plan 76 did; do not retry, substitute the status, or consume another row.

After all four fresh children pass, construct repeat-pair receipts from their archived bytes and raw in-process durations. Join those two fresh pairs to the immutable Plan 76 canonical pair and require exactly three comparable pairs, exactly six admitted partition receipts, 114/111 paths per pair, 225 exhaustive disjoint unique paths, unchanged path membership across pairs, all green statuses, same-run parallel ExUnit timing, and the two-times ratio bound for every pair. The Plan 76 failed repeat stays outside all admitted counts.

Only after ordinary terminal verification, run the still-unspent exact-six install-golden local action once through the existing `mix ci.install_golden` route. Require exactly the six named scaffold modules, authenticated discovery/install/compile/test diagnostics, the tracked wrapper/verifier contract, sanitized prepared copies with no symlinks or multiply-linked regular files, and one green process. Persist and authenticate its receipt before admission. Run the terminal verifier in a clean environment and write/reopen `235.1-77-LOCAL-TERMINAL.json` only after the verifier passes; bind all pinned predecessor/source digests, Task 1 digest, root inventory, raw/archive digests, negative ledger, three-pair metrics, exact-six receipt, command/environment counts, and one-shot accounting. Do not run git, GitHub, capture, validation, offline reconciliation, or Plan 70.</action>
  <acceptance_criteria>Plan 76 remains byte-for-byte frozen; its canonical pair contributes two admitted receipts and its failed repeat contributes zero. Exactly four fresh Plan 77 children each execute once with a fixed allowed formatter path and an immediate authenticated archive. The resulting three pairs each prove 114/111 exhaustive partitions, 225 unique paths, green same-run parallel timing, and the ratio bound. Exactly one exact-six process proves the named modules and sanitizer invariants. The tracked local terminal receipt passes independently; no remote/evidence/status action has occurred.</acceptance_criteria>
  <verify>
    <automated>LT=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json &amp;&amp; ROOT="$(/usr/bin/python3 -c 'import json,sys; v=json.load(open(sys.argv[1],"rb")); assert v["schema"]=="sigra.phase235.1-plan77-local-terminal/v1" and v["status"]=="PASS"; assert v["ordinary"]["pair_count"]==3 and v["ordinary"]["receipt_count"]==6 and v["ordinary"]["partition_cardinalities"]==[114,111] and v["ordinary"]["unique_paths_per_pair"]==225; assert v["ordinary"]["plan76_canonical_pairs"]==1 and v["ordinary"]["plan77_repeat_pairs"]==2 and v["ordinary"]["fresh_child_count"]==4 and v["ordinary"]["excluded_plan76_failure_count"]==1; assert v["exact_six"]["process_count"]==1 and v["exact_six"]["modules"]==["AuditLog","CrossValidation","DataManager","ERIAdapter","ERIClient","Settings"]; print(v["evidence_root"])' "$LT")" &amp;&amp; /usr/bin/python3 "$ROOT/local/verify-plan77-terminal.py" --root "$ROOT" --terminal "$LT" --preallocation-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-PREALLOCATION-VALIDATION.json --plan76-summary .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-SUMMARY.md --plan76-home .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-HOME-PROBE.json --plan76-prerequisites .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-PREREQUISITE-VALIDATION.json</automated>
    <fails_when>The terminal receipt/root/verifier is missing; any predecessor/source/build/HOME/SQL/stdin/entrypoint/manifest identity differs; Plan 76 changed or any Plan 76 action reran; a fresh child ran more or less than once; a child received a non-fixed formatter path; raw process persistence, no-follow validation, or immediate archive ordering fails; any exact receipt is absent/invalid despite process success; any partition/cardinality/universe/parallel-timing/ratio predicate fails; exact-six differs or reruns; or any git/GitHub/reconciliation counter is nonzero.</fails_when>
  </verify>
  <done>The local terminal durably proves the three-pair ordinary economics and exact-six scaffold while preserving Plan 76 and every one-shot boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Capture and validate fresh attempt-1 PR and non-PR evidence</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <read_first>
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-PREALLOCATION-VALIDATION.json`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-76-SUMMARY.md`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md`
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/COVERAGE.md`
    - `.planning/v1.47-MILESTONE-AUDIT.md`
    - `.github/workflows/ci.yml`
    - `scripts/ci/verify-library-routing-evidence.sh`
    - `scripts/ci/verify-library-validation-run.sh`
    - `test/sigra/planning/phase_235_1_library_economics_contract_test.exs`
  </read_first>
  <precondition>The local terminal receipt and its private verifier pass from a clean environment, its remote counters are zero, the candidate branch/PR target is authorized and not already allocated to another watcher, and the GitHub REST core budget exceeds 250 requests.</precondition>
  <reversibility rating="costly">Evidence JSON and commits are revertible, but pushes, pull-request runs, dispatch runs, watcher allocation, and run-attempt identities are externally visible one-shot actions.</reversibility>
  <action>Implement D-01/D-05/D-06/D-07/D-09 using the unchanged evidence schemas and automation-first GitHub protocol. Before each remote allocation inspect `gh api rate_limit`; if REST core remaining is 250 or lower, stop until the reported reset. Treat 403/429 as a hard stop until GitHub's reported reset/retry time. Maintain one watcher per run, always `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; fetch each completed run's structured summary once and fetch failed logs only for a failure. Retry one transient failure at most once as diagnostic-only, preserve both run identities, and never admit the diagnostic. A deterministic failure, unclassified failure, second transient failure, or missing receipt halts with durable diagnostics.

Authenticate the Task 2 local terminal and ensure no source/topology/evidence input drift. Exclude every retained Plan 35–76 run/job ID and all diagnostic IDs. Commit the candidate without changing product, CI, formatter, Mix, test, requirement, roadmap, audit, or Plan 70 files; push once. Capture exactly one automatic pull_request attempt-1 run and one workflow_dispatch attempt-1 run at the same candidate SHA. Require repository `szTheory/sigra`, exact ref/SHA/event/workflow identity, green selected jobs, literal sole `MIX_ENV=test mix ci` owner, unchanged protected aggregate, exactly three ordinary artifacts containing the comparable 114/111 partitions, and exactly two non-PR artifacts containing the six named modules. Reject stale, excluded, wrong-attempt, wrong-ref, wrong-SHA, incomplete, or status-only evidence.

Write and no-follow reopen `235.1-PR-EVIDENCE.json` and `235.1-SCAFFOLD-EVIDENCE.json`; run `scripts/ci/verify-library-routing-evidence.sh`. Commit only those two receipts as the candidate's direct child and push once. Capture exactly one new automatic pull_request attempt-1 validation and one distinct workflow_dispatch attempt-1 validation at that evidence SHA, with nothing intervening between the open-time property check and the PR allocation. Require their exact selected jobs and artifact predicates. Write and reopen `235.1-VALIDATION-RUN.json` only after both pass, binding candidate/evidence lineage, local terminal digest, capture/validation IDs, negative IDs and digest, watcher/summary commands, and attempt-1 identities. Run the unchanged routing, validation, and receipt-contract verifiers. Do not push again, edit prose/status files, mark any requirement complete, or execute Plan 70; D-07/D-08 leave reconciliation to the already-planned downstream gate.</action>
  <acceptance_criteria>Four fresh, distinct, non-excluded attempt-1 GitHub runs prove the candidate capture and direct-child validation routes. The pull-request evidence proves one sole ordinary owner, the byte-stable aggregate, three comparable timing pairs, and its three artifacts. The dispatch evidence proves the named fail-closed exact-six job and its two artifacts outside the aggregate. Evidence lineage is exact, one-watcher/rate-limit policy is honored, diagnostics are excluded, all three tracked receipts reopen and pass unchanged offline verifiers, and FAST-01/GATE-05 remain n=52/p50=469 and 93 rows.</acceptance_criteria>
  <verify>
    <automated>LT=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-LOCAL-TERMINAL.json &amp;&amp; /usr/bin/python3 -c 'import json,sys; v=json.load(open(sys.argv[1],"rb")); assert v["schema"]=="sigra.phase235.1-plan77-local-terminal/v1" and v["status"]=="PASS" and v["ordinary"]["pair_count"]==3 and v["ordinary"]["receipt_count"]==6 and v["exact_six"]["process_count"]==1' "$LT" &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>The local terminal is absent/invalid; a capture or validation run is missing, reused, excluded, retried, diagnostic, stale, wrong-attempt, wrong-event, wrong-ref, wrong-SHA, non-green, multiply watched, or incompletely summarized; repository/job/artifact/partition/exact-six/aggregate/lineage predicates differ; a process status substitutes for a receipt; REST limits are violated; an evidence file fails no-follow reopen or an unchanged verifier; FAST-01 differs from n=52/p50=469; any of 93 GATE-05 rows differs; or a forbidden prose/status/source change occurs.</fails_when>
  </verify>
  <done>The three evidence receipts establish fresh attempt-1 TEST-01/TEST-02/TEST-03 authority without changing FAST-01, GATE-05, Plan 70, or predecessor history.</done>
</task>

</tasks>

<threat_model>

## Security Enforcement

Active policy: `ASVS L1` with `block high` enforcement. Every high-severity threat in this register is mitigated; any unresolved high-severity threat blocks execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tracked formatter/wrapper -> fixed runtime receipt | A trusted allowlist controls where an untrusted child process may publish timing evidence. |
| Fixed receipt -> append-only archive | A reusable output name crosses into permanent per-action evidence and is vulnerable to stale, swapped, linked, or overwritten bytes. |
| Frozen Plan 76 authority -> Plan 77 terminal | Historical positive and negative records are imported read-only into a new decision. |
| Repository/build/HOME -> external Mix/PostgreSQL command | Source, linked build products, environment, stdin, SQL state, and child output cross an external-process boundary. |
| Local evidence -> GitHub Actions | Local claims become remote workflow/job/artifact evidence at specific commits and attempts. |
| GitHub API -> tracked evidence receipts | Mutable remote responses become durable local identity/lineage records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-235.1-77-01 | Spoofing | Formatter output path | high | mitigate | Derive the allowlist independently from two tracked sources, pin their digests, reject every non-fixed child path, and validate partition identity after no-follow reopen. |
| T-235.1-77-02 | Tampering | Fixed receipt/archive handoff | high | mitigate | Remove before child, persist raw process first, no-follow reopen, validate exact universe, archive/fsync/reopen/digest before reuse, and halt on stale/link/missing bytes. |
| T-235.1-77-03 | Repudiation | Process-success admission | high | mitigate | Preserve raw streams/status before parse and require an authenticated exact receipt; retain Plan 76's zero-exit missing-receipt child as negative evidence. |
| T-235.1-77-04 | Tampering | Frozen Plan 76 root | high | mitigate | Pin tracked artifacts and root inventory, import the canonical pair read-only, exclude the failed child, compare before/after digests, and prohibit any Plan 76 action reuse. |
| T-235.1-77-05 | Elevation of privilege | External Mix/PostgreSQL execution | high | mitigate | Use the authenticated eleven-key environment, exact HOME/build/SQL/stdin/entrypoint contracts, isolated append-only evidence, and pre-created terminal verifier. |
| T-235.1-77-06 | Information disclosure | Command streams and GitHub logs | medium | mitigate | Persist redacted environment metadata, never serialize secrets, inspect structured summaries once, and fetch failure logs only after a failed conclusion. |
| T-235.1-77-07 | Denial of service | GitHub API/watchers | medium | mitigate | Preflight REST budget, stop at 250 remaining or on 403/429, allocate one 60-second watcher per run, and allow at most one classified transient diagnostic. |
| T-235.1-77-08 | Spoofing | GitHub evidence identity | high | mitigate | Require exact repository/ref/SHA/event/workflow/job/attempt/artifact identities, excluded IDs, direct-child lineage, fresh attempt-1 runs, and unchanged offline verifiers. |
| T-235.1-77-09 | Tampering | Protected PR aggregate | high | mitigate | Require the sole literal owner and byte-stable aggregate; prove exact-six only on schedule/workflow_dispatch and outside every protected aggregate input. |
| T-235.1-77-SC | Tampering | Package supply chain | low | accept | No npm, pip, cargo, Mix dependency, or other package installation/change is introduced; the plan executes only already-tracked dependencies and existing CI routes. |

</threat_model>

<source_coverage_audit>

| Source | Item | Coverage | Plan location |
|--------|------|----------|---------------|
| GOAL | Two exhaustive comparable ordinary partitions with parallel timing under sole `MIX_ENV=test mix ci` owner | COVERED | Task 2 local authority; Task 3 remote owner/artifact evidence |
| GOAL | Exact-six scaffold on fail-closed schedule/workflow_dispatch receiver | COVERED | Task 2 exact-six terminal; Task 3 dispatch evidence |
| GOAL | Preserve FAST-01 and GATE-05 | COVERED | Task 3 acceptance and verifier |
| REQ | TEST-01 measurable per-test parallel timing | COVERED | Tasks 1–3 |
| REQ | TEST-02 two exhaustive comparable ordinary partitions | COVERED | Tasks 1–3 |
| REQ | TEST-03 exact six-module scaffold coverage | COVERED | Tasks 2–3 |
| RESEARCH | Production formatter fixed paths and immediate per-child archive | COVERED | `FixedReceiptSchedule`; Tasks 1–2 |
| RESEARCH | Same-execution ExUnit timing; no serial or second suite | COVERED | `Plan77LocalTerminal`; Task 2 |
| RESEARCH | Non-PR exact-six, hard signal, artifacts, fresh attempt-1 evidence | COVERED | `RemoteEvidenceLineage`; Task 3 |
| CONTEXT | D-01 sole PR owner and no matrix | COVERED | Objective; Tasks 2–3 |
| CONTEXT | D-02 exhaustive ordinary partitions, sequential owner, parallel ExUnit, deterministic receipts, comparable raw durations | COVERED | Truths; `Plan77LocalTerminal`; Task 2 |
| CONTEXT | D-03 same parallel execution timing | COVERED | Truths; Task 2 |
| CONTEXT | D-04 reuse production fixed-path formatter | COVERED | Tasks 1–2 |
| CONTEXT | D-05 exact-six fail-closed non-PR receiver | COVERED | Tasks 2–3 |
| CONTEXT | D-06 fresh attempt-1 PR/non-PR capture and distinct validation | COVERED | Task 3 |
| CONTEXT | D-07 status changes only after complete evidence | COVERED | Truths; Task 3 prohibition |
| CONTEXT | D-08 later DX traceability | COVERED | Explicitly preserved for downstream; Task 3 does not reconcile prose |
| CONTEXT | D-09 FAST-01/GATE-05 invariants | COVERED | Truths; Task 3 verifier |
| AUTHORITATIVE GAP | Freeze Plan 76 root/processes, admit its green pair, exclude missing receipt, correct only repeat path authority | COVERED | `gap_contract`; Tasks 1–2 |
| SPEC FALLBACK | Five deterministic edge probes unresolved because no SPEC exists | COVERED | `must_haves.assumptions`; Task 1 receipt |
| PROHIBITIONS | Descriptor-less safety/value prohibitions remain explicit and unverified | COVERED | `must_haves.prohibitions`; task rejection rules |
| DEFERRED | Unrelated UI, release, roadmap, audit, and DX work | EXCLUDED | No task implements a deferred idea; Plan 70 remains blocked |

No GOAL, REQ, RESEARCH, CONTEXT, authoritative-gap, edge-assumption, or prohibition item is missing from this plan.

</source_coverage_audit>

<verification>

1. `gsd_run query frontmatter.validate` and `gsd_run query verify.plan-structure` pass for this gap-closure plan before execution.
2. Task 1 proves the formatter-path correction without allocating or consuming any execution/evidence action.
3. Task 2's terminal verifier authenticates the immutable Plan 76 pair plus four fresh fixed-path children as three comparable pairs, then one exact-six local action.
4. Task 3's unchanged routing/validation verifiers authenticate four fresh attempt-1 runs, direct-child evidence lineage, sole-owner/non-PR routing, all artifacts, FAST-01, and all 93 GATE-05 rows.
5. `git diff --name-only` for the plan's execution contains only the five declared evidence JSON files and the eventual Plan 77 summary; no runtime source, prior plan/summary, requirement, roadmap, audit, workflow, or Plan 70 file changes.

</verification>

<success_criteria>

- TEST-01: Six admitted timing receipts across three comparable pairs contain real parallel ExUnit per-test durations; no serial/second execution or status-only substitute exists.
- TEST-02: Every pair contains exhaustive, disjoint 114/111 ordinary partitions over the same 225 paths and satisfies the two-times duration bound under the sole PR owner.
- TEST-03: The exact six named scaffolds pass once locally and once through the fail-closed non-PR receiver with two required artifacts, outside the protected aggregate.
- Evidence: Candidate PR/dispatch capture and direct-child PR/dispatch validation are four distinct fresh attempt-1 runs with exact lineage and unchanged offline verifier success.
- Preservation: Plan 76, all predecessor history, FAST-01 n=52/p50=469, all 93 GATE-05 rows, CI topology, product/test sources, and Plan 70 remain unchanged.
- Governance: Five SPEC-fallback edge assumptions and all descriptor-less prohibitions remain explicitly flagged; requirement/status reconciliation is not performed in this plan.

</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-77-SUMMARY.md` when done.
</output>
