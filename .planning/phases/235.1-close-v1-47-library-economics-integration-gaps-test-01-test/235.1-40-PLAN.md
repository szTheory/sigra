---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 40
type: execute
wave: 37
depends_on: [235.1-39]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Only PostgreSQL databases whose complete names match one of the two observed install-fixture grammars and whose owner equals the resolved repository test role are terminated/dropped; unidentified or nonmatching sessions and databases are untouched."
    - "Only completed Plan 35–39 resources with recorded path, process, candidate, and ownership identity are cleaned; ambiguous resources block rather than being removed."
    - "Candidate `3713dcd4`/tree `94fca667`, its sole parent `87338db7`, the 114/111 calibrated assignment, and the exact 137-entry denied-payload ledger remain unchanged."
    - "A fresh isolated candidate receives one fixed no-wait/no-resample health sample after cleanup; an unhealthy sample stops before timing."
    - "One healthy sample permits exactly one complete three-pair series and then only fresh, disjoint, original-attempt-1 live evidence."
  artifacts:
    - path: /private/tmp/plan40-resource-cleanup.json
      provides: "Canonical before/allowlist/actions/after cleanup receipt"
    - path: /private/tmp/plan40-preflight-health.json
      provides: "Canonical single-sample host and database admission receipt"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
      provides: "Fresh successful PR route receipt"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
      provides: "Fresh successful exact-six dispatch receipt"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
      provides: "Four-way-disjoint evidence-SHA validation"
  key_links:
    - from: "Plan 39 failed health receipt"
      to: "Plan 40 cleanup allowlist"
      via: "read-only exact-name/session/resource inventory"
    - from: "cleanup after-state"
      to: "fresh health admission"
      via: "zero allowlisted scratch databases and no unidentified destructive action"
    - from: "128-line prior ledger plus Plan 38 local nine"
      to: "Plan 40 payload admission"
      via: "unchanged 137-line canonical set"
      pattern: "0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08"
---

<objective>
Remove only proven test-owned environmental residue, then give the unchanged candidate one final health-gated recovery and strict live-evidence route.

Purpose: Plan 39 correctly rejected its only health sample before timing. Read-only inspection proves its 132 matches are one base install-fixture database plus 131 prepared install-fixture databases with source-derived complete names; its one foreign idle-in-transaction session retained no identity and must never be guessed at or terminated.
Output: Canonical scoped-cleanup evidence, one new health receipt, at most one three-pair recovery, and—only after success—new capture/validation receipts.

Decision coverage: D-01 preserves the sole PR owner; D-02–D-04 preserve the calibrated partitions and threshold; D-05/D-06 preserve exact-six routing; D-07 requires fresh disjoint evidence; D-08 defers reconciliation; D-09 preserves all failed history.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-35-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-36-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-37-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-38-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-39-SUMMARY.md
@scripts/ci/library-partitions.test.sh
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
</context>

## Frozen Authority and Budget

Candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3`, tree `94fca667d3f5226347548bdeea625bc737a4348e`, and sole parent/current remote PR head `87338db7c2678601e20ed3140fd8de1981dfbeb1` remain exact. The candidate is unpushed. No source, calibration, manifest, pin, runner, workflow, assignment, or threshold byte may change.

Plan 39 recorded one failed health sample, zero runner invocations, zero timing pairs/payloads, and zero GitHub actions. Preserve its receipt SHA-256 `9b3d89e4c160e945cf171ec9f8d750a16c37d5649e6ca050baee830deeac4a2f`: load 25.32 exceeded 18 logical CPUs, one unidentified foreign session was idle in transaction, and 132 database names matched the recorded test-scratch prefix expression; candidate processes, waiters, blockers, ungranted locks, and scratch schemas were zero. The exact 137-line/8,905-byte denied-payload ledger remains `/private/tmp/plan39-prior-payloads-137.txt`, SHA-256 `0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08`.

Plan 40 authorizes one scoped environmental cleanup, one new fixed health sample, and—only on health success—one full recovery invocation yielding exactly three pairs/nine new local payloads. Cleanup is remediation before the sample, not a sample retry. Any subsequent local failure exhausts recovery permanently.

## Read-Only Database Naming Authority

The current read-only inventory contains exactly 132 names selected by Plan 39's broad prefix: one `sigra_install_golden_tmp_dev` and 131 matching `sigra_install_golden_tmp_dev_prepared_[0-9a-f]{10}_[0-9]+`. Their `LC_ALL=C` sorted, one-name-per-line, one-final-LF bytes are 132 lines/6,819 bytes with SHA-256 `bebf055763feb7682e543f0c31a534f0b9721bbc61bd5c722c1e36552b8ad8a4`. No current `sigra_audit_query_index_scratch*` or `sigra_plan39_scratch*` database exists, and no other broad-prefix match falls outside those two sets. Therefore the only cleanup-admissible complete-name grammars are `^sigra_install_golden_tmp_dev$` and `^sigra_install_golden_tmp_dev_prepared_[0-9a-f]{10}_[0-9]+$`. Arbitrary suffixes, backup/copy names, uppercase variants, and even otherwise test-looking names from the two absent families are not authorized.

The name provenance is exact candidate source: `test/support/install_fixture.ex` patches the base literal to `sigra_install_golden_tmp_dev_` plus `MIX_TEST_PARTITION`; `checkout_partition/2` emits `prepared_` plus the first ten lowercase hexadecimal SHA-256 characters of the prepared graph root plus a positive monotonic integer; and `test/support/install_fixture_performance_test.exs` asserts that complete partition form. At execution, bind these source paths to their exact candidate source-index hashes. The inspected candidate runtime contract resolves to localhost port 5432 with configured role `postgres` and password present; a connection through that contract proved `configured username == session_user == current_user`, and all 132 `datdba` owners equal that role. Re-resolve hostname, port, username, password presence, and database from the candidate's `SIGRA_TEST_PG_*`/`config/test.exs` contract and require those equivalences before cleanup. Record host/user/owner as domain-separated SHA-256 values and password presence only; never record the password.

<tasks>

<task type="tracer">
  <name>Task 1: Inventory and clean only proven test-owned residue (D-07, D-09)</name>
  <files>/private/tmp/plan40-resource-cleanup.json, /private/tmp/plan40-db-allowlist.txt</files>
  <precondition>Plan 39 health receipt is regular, canonical, digest-valid, and records exactly the frozen failed predicates above; candidate and remote identities remain exact.</precondition>
  <action>Before mutation, open one explicit `BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ READ ONLY` session through the exact resolved repository test database contract. Within that single transaction and snapshot, collect configuration/current-user equivalence, database catalog, `pg_stat_activity`, and allowlist rows together; do not claim stability across separate connections or transactions. Include exact database name/owner, backend PID, state, wait-event type/event, transaction/query start times, application name, client address, and credential-free identity hashes. Record transaction isolation/read-only state, backend PID, `txid_current_if_assigned()` result, `pg_current_snapshot()` plus parsed xmin/xmax/xip facts, snapshot timestamp, the full non-template database-name set hash/count, and the separately sorted candidate list.

Derive `/private/tmp/plan40-db-allowlist.txt` only from complete names matching `^sigra_install_golden_tmp_dev$` or `^sigra_install_golden_tmp_dev_prepared_[0-9a-f]{10}_[0-9]+$`. Require exact candidate source-index hashes for the name-producing/asserting files, base count one, prepared count 131, total 132, exact 6,819 bytes and SHA-256 `bebf055763feb7682e543f0c31a534f0b9721bbc61bd5c722c1e36552b8ad8a4`, configured username/session_user/current_user equality, and every row's owner equality. Explicitly prove all 132 original broad-prefix rows fall in these two full grammars and no nonmatching name enters the allowlist. Any arbitrary suffix/backup/copy name or absent-family name is excluded. The unidentified Plan 39 foreign session is context only: never infer its PID or terminate it. A currently observed session may be terminated only when its exact database name is already in the immutable verified allowlist and its current PID/database/user tuple is rechecked immediately before action.

Commit the read-only transaction only after the snapshot and allowlist bytes are materialized and hashed. Generate identifiers through PostgreSQL `format('%I', name)`/`quote_ident`, never string interpolation. Because `DROP DATABASE` cannot run inside a transaction, process each immutable allowlist row separately afterward: re-query exact name/owner through the same resolved runtime identity, terminate only sessions attached to that exact allowlisted database, issue a force-free quoted drop only for that row, then prove the name absent before advancing. Do not use a wildcard, broad prefix command, shell glob, cascade outside the database, or any cleanup command against a non-allowlisted identity.

Inventory filesystem/process residue independently. A removable worktree/tmp/build/cache/process must be under an exact retained Plan 35–39 temporary root named in its summary or receipt, resolve physically beneath `/private/tmp`, and carry matching completed-plan, candidate/tree, PID command/root, and no-live-owner evidence. Recheck PID start time and full command before termination; recheck physical path, device/inode, and ownership marker before removal. Exclude the current workspace, PostgreSQL service, agent processes, the exact retained Plan 38/39 evidence inputs, and anything ambiguous. Clean only verified completed-task residue; do not make load fall by killing unrelated work.

Write canonical `/private/tmp/plan40-resource-cleanup.json` using sorted compact JSON plus one LF. Include schema `sigra.phase235.1-plan40-resource-cleanup/v1`, frozen/source/runtime identities, transaction/isolation/xid/snapshot facts, before snapshot hashes/counts, allowlist hash/count/names and exact grammar counts, every termination/drop/path action and its recheck, excluded sessions/resources with reason, after snapshot hashes/counts, zero remaining allowlisted names/sessions, and `receipt_sha256`. Reject duplicate JSON keys while parsing via Python `json.load(..., object_pairs_hook=...)`; compare the complete file byte-for-byte to `LC_ALL=C jq -S -c` output plus one LF. Compute the receipt digest over the exact sorted/compact/one-LF preimage with `receipt_sha256` omitted, insert it afterward, and independently reconstruct by duplicate-safe parsing, deleting the field, canonicalizing, and hashing. Require nonmatching database names unchanged as a set, and record—not alter—any remaining unidentified/nonmatching foreign activity.</action>
  <verify>
    <automated>python3 -c 'import json,sys; json.load(open(sys.argv[1]),object_pairs_hook=lambda p: (_ for _ in ()).throw(ValueError("duplicate key")) if len(p)!=len({k for k,_ in p}) else dict(p))' /private/tmp/plan40-resource-cleanup.json &amp;&amp; cmp -s /private/tmp/plan40-resource-cleanup.json &lt;(LC_ALL=C jq -S -c . /private/tmp/plan40-resource-cleanup.json) &amp;&amp; jq -e '.schema == "sigra.phase235.1-plan40-resource-cleanup/v1" and .candidate == "3713dcd4408b2a280a53b541f973b4acdb79aff3" and .tree == "94fca667d3f5226347548bdeea625bc737a4348e" and .snapshot.isolation == "repeatable read" and .snapshot.read_only == true and .allowlist.base_count == 1 and .allowlist.prepared_count == 131 and .allowlist.count == 132 and .runtime_identity.configured_user_equals_session_user == true and .runtime_identity.session_user_equals_current_user == true and .runtime_identity.all_owners_equal_current_user == true and .after.allowlisted_database_count == 0 and .after.allowlisted_session_count == 0 and .safety.nonmatching_database_set_unchanged == true and .safety.unidentified_session_terminated == false and (.receipt_sha256 | test("^[0-9a-f]{64}$"))' /private/tmp/plan40-resource-cleanup.json &amp;&amp; CLEANUP_EXPECTED="$(jq -r '.receipt_sha256' /private/tmp/plan40-resource-cleanup.json)" &amp;&amp; CLEANUP_ACTUAL="$(LC_ALL=C jq -S -c 'del(.receipt_sha256)' /private/tmp/plan40-resource-cleanup.json | shasum -a 256 | awk '{print $1}')" &amp;&amp; test "$CLEANUP_ACTUAL" = "$CLEANUP_EXPECTED"</automated>
    <fails_when>Plan 39 receipt/candidate/remote or observed 1+131=132 inventory facts differ; the source-index naming provenance or runtime DB contract is absent/mismatched; configured username, session_user, current_user, or owner equivalence fails; snapshot is not one explicit repeatable-read read-only transaction or lacks backend/isolation/xid/snapshot facts; catalog/activity/allowlist comes from inconsistent snapshots; allowlist input is ambiguous, outside either full anchored grammar, has arbitrary suffix/backup/copy form, is non-test-owned, duplicated, mutable, or includes an absent-family name; an identifier is not server-quoted; a terminate/drop targets a tuple not rechecked immediately; a non-allowlisted database/session, unidentified session, service, workspace, agent, retained evidence input, or ambiguous resource is touched; a matching database/session remains; nonmatching database set changes; before/after/action hashes/counts fail; either receipt has duplicate keys, differs bytewise from sorted compact JSON plus one LF, includes its digest in the preimage, or fails strict independent digest reconstruction; or timing/GitHub occurs.</fails_when>
  </verify>
  <done>Only exact allowlisted test-owned scratch databases and exact completed-task resources are removed, with every other identity preserved and canonically accounted for.</done>
</task>

<task type="auto">
  <name>Task 2: Take one fresh health sample and run one final recovery series (D-02–D-04, D-07, D-09)</name>
  <files>/private/tmp/plan40-preflight-health.json, /private/tmp/plan40-recovery-payloads.txt</files>
  <precondition>Task 1 receipt is canonical and proves zero remaining allowlisted databases/sessions without touching any unidentified or nonmatching resource.</precondition>
  <action>Create a wholly fresh physical archive-only isolation from the exact candidate using Plan 39's accepted standalone-object, copied locked-dependency/cache, candidate-local home/tmp/database, unset-`MIX_BUILD_PATH`, default `_build/test`, offline dependency, compile-only, clean-tree, and full untimed-gate contract. Do not reuse Plan 35–39 worktrees, build products, databases, or timing receipts. Reprove exact tree and 225-source/calibration/114-111 assignment integrity. Revalidate the regular non-symlink 137 ledger at its exact count/bytes/digest before timing.

Immediately after untimed gates, collect exactly one new health sample into `/private/tmp/plan40-preflight-health.json`; do not wait for favorable load, resample, clean after observation, or select among observations. Use schema `sigra.phase235.1-plan40-preflight-health/v1` and Plan 39's complete host/disk/process/redacted-DB/activity/lock/cleanup fields and fixed bounds. Add the Task 1 cleanup receipt digest, fresh database identity, and proof that no allowlisted database/session returned. Reject duplicate keys with Python `object_pairs_hook`, require exact byte equality to `LC_ALL=C jq -S -c` plus one LF, and use the same non-self-referential digest contract: `receipt_sha256` omitted from the preimage, field inserted afterward, and strict independent duplicate-safe delete-field reconstruction. Admission requires load1 <=18, normal memory pressure/no swap growth, at least 10 GiB free on each relevant volume, zero competing candidate processes, both database probes <=1,000ms, zero foreign non-idle sessions, zero waiters/blockers/ungranted locks, and complete scratch cleanup. An unidentified or unrelated foreign non-idle session remains a hard stop, not cleanup authority.

With no intervening compile, suite, cleanup, wait, or unrelated work, invoke `bash scripts/ci/library-partitions.test.sh` exactly once. Require exactly three fresh pairs/nine unique payload hashes, six green children, exhaustive/disjoint 225 paths, unchanged 114/111 assignment and weights, truthful contained receipts, and independently recomputed ratio <=2.0 for every pair. Persist `/private/tmp/plan40-recovery-payloads.txt` as strict lowercase 64-hex, `LC_ALL=C` sorted-unique bytes: exactly 9 lines/585 bytes with no duplicate collapse and exactly one final LF. Prove that exact canonical new9 file has zero `comm -12` intersection with the already canonical exact137. Any unhealthy sample or timed failure permanently blocks: no further cleanup, sample, recovery, partial admission, recalibration, reassignment, threshold/source/pin/candidate change, or GitHub.</action>
  <verify>
    <automated>python3 -c 'import json,sys; json.load(open(sys.argv[1]),object_pairs_hook=lambda p: (_ for _ in ()).throw(ValueError("duplicate key")) if len(p)!=len({k for k,_ in p}) else dict(p))' /private/tmp/plan40-preflight-health.json &amp;&amp; cmp -s /private/tmp/plan40-preflight-health.json &lt;(LC_ALL=C jq -S -c . /private/tmp/plan40-preflight-health.json) &amp;&amp; jq -e '.schema == "sigra.phase235.1-plan40-preflight-health/v1" and .admission.overall == true and .cleanup_receipt_sha256 != null and (.receipt_sha256 | test("^[0-9a-f]{64}$"))' /private/tmp/plan40-preflight-health.json &amp;&amp; HEALTH_EXPECTED="$(jq -r '.receipt_sha256' /private/tmp/plan40-preflight-health.json)" &amp;&amp; HEALTH_ACTUAL="$(LC_ALL=C jq -S -c 'del(.receipt_sha256)' /private/tmp/plan40-preflight-health.json | shasum -a 256 | awk '{print $1}')" &amp;&amp; test "$HEALTH_ACTUAL" = "$HEALTH_EXPECTED" &amp;&amp; awk 'length($0) != 64 || $0 !~ /^[0-9a-f]+$/ { bad=1 } END { exit bad }' /private/tmp/plan40-recovery-payloads.txt &amp;&amp; test "$(wc -l &lt; /private/tmp/plan40-recovery-payloads.txt | tr -d ' ')" = 9 &amp;&amp; test "$(wc -c &lt; /private/tmp/plan40-recovery-payloads.txt | tr -d ' ')" = 585 &amp;&amp; test "$(tail -c 1 /private/tmp/plan40-recovery-payloads.txt | od -An -tuC | tr -d ' ')" = 10 &amp;&amp; cmp -s /private/tmp/plan40-recovery-payloads.txt &lt;(LC_ALL=C sort -u /private/tmp/plan40-recovery-payloads.txt) &amp;&amp; test "$(LC_ALL=C comm -12 /private/tmp/plan39-prior-payloads-137.txt /private/tmp/plan40-recovery-payloads.txt | wc -l | tr -d ' ')" = 0</automated>
    <fails_when>Isolation/tree/source/calibration/assignment differs or reuses prior state; cleanup linkage fails; health receipt is incomplete, has duplicate keys, differs bytewise from sorted compact JSON plus one LF, includes its digest in the preimage, or fails strict independent digest reconstruction; more or fewer than one health sample exists; load exceeds CPUs, foreign non-idle activity persists, or any other fixed predicate fails; work occurs between sample and timing; runner invocation is not exactly one; recovery ledger has non-lowercase/non-64-hex data, is unsorted, collapses duplicates, differs from 9 lines/585 bytes/one final LF, or intersects exact137; pair/path/ratio/containment proof fails; or any retry/tuning/GitHub occurs.</fails_when>
  </verify>
  <done>One healthy post-cleanup sample immediately yields one and only one passing three-pair series with nine payloads outside the frozen ledger, or recovery stops permanently.</done>
</task>

<task type="auto">
  <name>Task 3: Capture and validate strict live routes (D-01, D-05–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <precondition>Task 2's sole health sample and all three fresh recovery pairs passed, and candidate remains exact/unpushed.</precondition>
  <action>Fast-forward-push exact candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3` once from remote head `87338db7c2678601e20ed3140fd8de1981dfbeb1`. Pair only the resulting new automatic PR run with one new exact-six workflow_dispatch at the identical candidate SHA. Exclude every historical ID, explicitly including Plans 35–37 (`34562007714`, `34562004702`, `34564383170`, `34564379273`, `34594098613`, `34594093081`); Plans 38–39 had zero live IDs. Apply the AGENTS.md rate floor, 403/429 stop, one 60-second watcher per run, one structured summary, and failed logs only after failure. Only dual original-attempt-1 success writes the two capture receipts.

Require the evidence commit to have the candidate as direct parent and change exactly the two capture receipts. Push it once, then pair one new automatic PR run with one new exact-six dispatch at the evidence SHA under identical rules. Only dual original-attempt-1 success writes validation. Require all four Plan 40 IDs mutually disjoint and outside all history, exact SHA/event/job/command/artifact/digest/path/source facts, and correct candidate/evidence lineage. Any live failure blocks without retry, waiver, replacement, or substitution. Leave audit reconciliation to Plan 05.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Push/evidence lineage or exact diff fails; a selected run is reused, retried, nonautomatic, unsuccessful, or at the wrong SHA/event/job/command; IDs overlap current/history; artifact/digest/path/source/receipt linkage fails; watcher/rate rules break; a receipt follows partial/failure evidence; extra local timing occurs; or retry/waiver/substitution appears.</fails_when>
  </verify>
  <done>Four new disjoint original-attempt-1 successes prove candidate and evidence routes, or live evidence blocks without substitution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Failed health state to cleanup authority | Environmental residue must be proven test-owned before destructive action. |
| Process/path identity to cleanup | Completed-task ownership must be exact; ambiguity is fail-closed. |
| Cleanup after-state to health admission | Cleanup cannot select or alter the later fixed sample. |
| Local recovery to live evidence | Only unchanged candidate and fresh attempt-1 runs may cross. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-40-01 | Elevation of Privilege | PostgreSQL cleanup | critical | mitigate | Immutable exact-name allowlist, owner proof, quoted identifiers, per-row recheck. |
| T-235.1-40-02 | Tampering | process/filesystem cleanup | high | mitigate | Physical path/PID/start/command/receipt ownership checks; ambiguity blocks. |
| T-235.1-40-03 | Repudiation | environmental remediation | high | mitigate | Canonical before/action/after hashes and non-self-referential receipt digest. |
| T-235.1-40-04 | Tampering | recovery budget | high | mitigate | One health sample, one timed invocation, frozen 137-entry denylist. |
| T-235.1-40-05 | Spoofing | live evidence | high | mitigate | Exact fast-forward lineage and four fresh original-attempt-1 IDs. |
| T-235.1-40-SC | Tampering | packages | low | accept | No package/source change; only locked private copies. |
</threat_model>

## Multi-Source Coverage Audit
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Exact ordinary/exact-six routes | 40 | COVERED | Scoped remediation, bounded recovery, strict live. |
| REQ | TEST-01 | PR evidence | 40 | COVERED | New attempt-1 PR routes only. |
| REQ | TEST-02 | Comparable exhaustive partitions | 40 | COVERED | Unchanged assignment and one final series. |
| REQ | TEST-03 | Exact-six receiver | 40 | COVERED | Fresh dispatch capture/validation. |
| RESEARCH | — | Fail-closed cleanup and no tuning | 40 | COVERED | Exact identities and one-shot budgets. |
| CONTEXT | D-01–D-09 | Preserve routes/history/evidence ordering | 40, 05 | COVERED | Plan 39 remains failed health history. |

<verification>Prove exact scoped cleanup, one canonical healthy sample, one three-pair recovery, and four new live attempt-1 runs.</verification>
<success_criteria>- Cleanup touches only exact test-owned scratch databases and completed-task resources.
- Unidentified/nonmatching activity is never terminated; persistent foreign non-idle activity blocks.
- Candidate/tree/source/calibration/assignment/threshold and ledger137 remain exact.
- One new health sample permits at most one three-pair series with new9 outside137.
- Four new disjoint original-attempt-1 live IDs prove candidate and evidence routes.</success_criteria>
<output>Create `235.1-40-SUMMARY.md` with before/allowlist/action/after cleanup proof, canonical receipt digests, exact isolation/health/timing budgets, frozen candidate/ledger, Plan 39 failure history, and new live provenance.</output>
