---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 48
type: execute
wave: 45
depends_on: [235.1-47]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Plans 45–47 and the three exact Plan 47 collision files remain immutable; no prior command, marker, receipt, or temporary is rerun, deleted, or changed."
    - "One collision-resistant, receipt-authorized private root owns every Plan 48 temporary, marker, script, receipt, candidate, and timing payload without symlinks or path escape."
    - "The complete Plan 47 parser/source-diff, 22-call confinement, dry-run, candidate, adjacency, admission, health, ledger137, and three-pair controls pass under root-relative paths."
    - "Exactly one dry run, one admitted health sample, and one production timing command produce three passing pairs, six green children, exhaustive 225-path coverage, and nine payloads disjoint from ledger137."
    - "Four fresh original-attempt-1 GitHub runs prove the unchanged candidate and evidence commits on PR and dispatch routes."
  artifacts:
    - path: /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/allocation.json
      provides: "Canonical authority for the sole private Plan 48 root"
    - path: /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/preflight.json
      provides: "Marker-free parser/source-diff/confinement proof"
    - path: /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing-evidence.json
      provides: "One admitted three-pair local series"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
      provides: "Strict local/live provenance and disjointness validation"
  key_links:
    - from: "Plan 47 halted summary"
      to: "Plan 48 allocation"
      via: "exact collision inventory and zero Plan 47 task-action transfer"
    - from: "allocation.json"
      to: "every Plan 48 action"
      via: "canonical realpath/dev/inode/owner/mode plus repeated receipt replay"
    - from: "Plan 47 approved verifier contract"
      to: "root-relative Plan 48 evidence"
      via: "path-token substitution only; semantic assertions remain unchanged"
    - from: "ledger137"
      to: "Plan 48 new9"
      via: "actual-file authentication immediately before C-sorted intersection"
---

<objective>
Replace Plan 47's globally fixed temporary namespace with one collision-resistant, receipt-authorized private root, then resume its unchanged parser and bounded local/live evidence route.

Purpose: Plan 47 spent no action budget; it halted because planning/executor diagnostics occupied three names matched by an overbroad freshness glob. Plan 48 preserves those bytes as history and makes allocation identity—not a global name prefix—the authority for all new evidence.
Output: One canonical private-root receipt, root-relative parser/local evidence, and four fresh live-route receipts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-45-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-46-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-47-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-47-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Allocate and authenticate the sole Plan 48 root (D-07, D-09)</name>
  <files>/private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/.allocation.once, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/allocation.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/history.json</files>
  <precondition>Plan 47 halted summary commit is exact `ad01e4f15e0131b2b5d5940dfeac93ec055b3cf5`; its three collision files and every Plan 45–47 namespace identity replay exactly; no directory matches the strict allocation pattern `/private/tmp/sigra-p2351-plan48-ad01e4f1.[A-Za-z0-9]{8}`.</precondition>
  <action>First create no output. Independently replay Plan 45/46 accepted and halted authorities, Plan 47 plan commit `43aec33ea5c4280d3c4c9facec6f3c82202c23b5`, and the 6,468-byte Plan 47 summary SHA `e4c81ce8ea922297e3803c9a176def27e78c4c92c5d93ea302bf72aa4135085f` at commit `ad01e4f15e0131b2b5d5940dfeac93ec055b3cf5`. Freeze the exact collision rows: pre-existing `/private/tmp/plan47-schema.out` 222 bytes/mode `0644`/inode `284721198`/SHA `0e87d156e98b3c1f421ff67644a0b1293d2a7ef40e37753af503821b2d13d281`; pre-existing `/private/tmp/plan47-structure.out` 987/mode `0644`/inode `284721211`/SHA `c9b78febd4d09efbd129b0afb3e81f9185e9309652663ac8bc1c0dd77d1951e2`; executor-created approved-plan comparison `/private/tmp/plan47-approved.md` 76,925/mode `0644`/inode `284841668`/SHA `a949d987bff558947b5b8122403692c64fdc19b7122e9ac73990e105bf7c0c6c`. Require every other Plan 47 task artifact/marker absent exactly as the summary states. Preserve all three files and all Plan 45–47 paths byte/type/mode/inode/mtime identical throughout Plan 48; never execute, copy over, chmod, touch, rename, or delete them.

With `umask 077`, use `/usr/bin/python3` `os.scandir("/private/tmp")` without following symlinks to record a C-byte-sorted snapshot of direct children whose basenames match exact regex `^sigra-p2351-plan48-ad01e4f1\.[A-Za-z0-9]{8}$`; require zero paths. Invoke exactly `/usr/bin/mktemp -d /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX` once, with argv recorded before invocation. Reject empty/multiline output, nonzero status, stderr, or a result not matching exact regex `^/private/tmp/sigra-p2351-plan48-ad01e4f1\.[A-Za-z0-9]{8}$`. Canonicalize using `cd -- "$PLAN48_ROOT" && pwd -P`; require lexical and physical paths identical, immediate parent physical `/private/tmp`, `lstat` type directory, no symlink component, owner UID equal `/usr/bin/id -u`, group recorded, mode exactly `0700`, link count two, and a previously absent dev/inode tuple. Require a second `os.scandir` snapshot's delta is exactly this one directory and no other matching direct child exists.

Inside that authenticated empty directory, use `/usr/bin/python3` `os.open` with `O_CREAT|O_EXCL|O_WRONLY|O_NOFOLLOW`, mode `0600`, to create `.allocation.once`; write exactly `sigra-phase235.1-plan48-allocation\n`, fsync, close, and require a second create fails `EEXIST` without truncation. Write `allocation.json` by the same exclusive/no-follow discipline. Its exact keys are `schema,plan47,allocator,root,marker,pre_snapshot,post_snapshot,namespace_policy,receipt_sha256`; canonical duplicate-safe compact C-key JSON plus one LF; receipt SHA is over the identical object with the digest field omitted. Bind exact mktemp argv/status/stdout/stderr hashes, start/finish RFC3339 UTC order, lexical/physical path, basename regex, parent, owner/group/mode/dev/inode/nlink, marker stat/content/SHA/reacquire errno, complete pre/post ledgers and hashes, zero-before/one-after matching counts, and policy that all Plan 48 untracked paths are descendants while only the three named tracked evidence files may be outside. Create `history.json` inside the root with complete Plan 45–47 inventory and the three collision provenance labels. Later tasks rediscover the root only by a one-result `find -P` plus independent allocation-receipt replay; no fixed pointer, symlink, environment persistence, newest-directory choice, glob-first selection, cleanup, or second allocation is allowed. Planning/schema/structure outputs must use ordinary tool output or names outside the strict regex and are never execution preconditions.</action>
  <verify>
    <automated>PLAN48_ROOT="$(/usr/bin/python3 -c 'import os,re; print("\n".join(sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan48-ad01e4f1\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False))))')" &amp;&amp; test -n "$PLAN48_ROOT" &amp;&amp; test "$(printf '%s\n' "$PLAN48_ROOT" | wc -l | tr -d ' ')" = 1 &amp;&amp; PLAN48_ROOT="$PLAN48_ROOT" /usr/bin/python3 -c 'import hashlib,json,os,re,stat; p=os.environ["PLAN48_ROOT"]+"/allocation.json"; pairs=lambda x:(_ for _ in ()).throw(ValueError("duplicate key")) if len(x)!=len({k for k,_ in x}) else dict(x); raw=open(p,"rb").read(); d=json.loads(raw,object_pairs_hook=pairs); assert raw==(json.dumps(d,sort_keys=True,separators=(",",":"))+"\n").encode(); h=d.pop("receipt_sha256"); assert h==hashlib.sha256((json.dumps(d,sort_keys=True,separators=(",",":"))+"\n").encode()).hexdigest(); root=os.environ["PLAN48_ROOT"]; s=os.lstat(root); assert d["schema"]=="sigra.phase235.1-plan48-allocation/v1" and re.fullmatch(r"/private/tmp/sigra-p2351-plan48-ad01e4f1\.[A-Za-z0-9]{8}",root) and os.path.realpath(root)==root and os.path.dirname(root)=="/private/tmp" and stat.S_ISDIR(s.st_mode) and stat.S_IMODE(s.st_mode)==0o700 and s.st_uid==os.getuid() and d["root"]["dev"]==s.st_dev and d["root"]["inode"]==s.st_ino and d["post_snapshot"]["matching_count"]==1 and d["pre_snapshot"]["matching_count"]==0; m=open(root+"/.allocation.once","rb").read(); assert m==b"sigra-phase235.1-plan48-allocation\n" and d["marker"]["sha256"]==hashlib.sha256(m).hexdigest() and d["marker"]["reacquire_errno"]=="EEXIST"'</automated>
  </verify>
  <fails_when>Plan 47 summary/plan/collision bytes or provenance differ; any Plan 45–47 path changes; any strict-prefix directory predates allocation; mktemp runs other than once; root output is ambiguous, nonphysical, nonprivate, symlinked, wrongly owned, or outside `/private/tmp`; pre/post delta contains anything else; marker/receipt is nonexclusive/noncanonical; or a fixed alias/pointer/global Plan 48 artifact is used.</fails_when>
  <done>Exactly one private root is allocated once and independently rediscoverable only through its canonical receipt; all prior collision bytes remain immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the unchanged Plan 47 parser and local evidence route under root authority (D-02–D-04, D-07, D-09)</name>
  <files>/private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/health-collector.sh, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/health.sql, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/tests/run.sh, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/fixtures/vm-stat-wide.txt, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/fixtures/vm-stat-lines.txt, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/fixtures/psql-valid.txt, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/fixtures/psql-malformed.txt, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/collector/fixtures/json-duplicate.txt, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/preflight.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/verify-task2.py, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/collector-verify.once, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/collector-verify.log, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/collector-tests.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/candidate.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/current-databases.sql, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/current-databases.once, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/current-databases.raw, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/current-databases.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/task2-sequence.jsonl, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/admission.once, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/admission-invocation.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/preflight-health.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing.once, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-1/combined.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-1/partition-1-timings.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-1/partition-2-timings.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-2/combined.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-2/partition-1-timings.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-2/partition-2-timings.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-3/combined.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-3/partition-1-timings.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing/validation-3/partition-2-timings.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/timing-evidence.json, /private/tmp/sigra-p2351-plan48-ad01e4f1.XXXXXXXX/local/recovery-payloads.txt</files>
  <precondition>Exactly one allocation receipt passes Task 1's independent replay; its directory initially contains only `.allocation.once`, `allocation.json`, and `history.json`; all Plan 45–47 identities remain exact.</precondition>
  <behavior>
    - The exact frozen Plan 46 collector normalizes from the root-relative Plan 48 collector with only allocated-root/comment literals and the approved sentinel-before-LF predicate block changed.
    - All 19 inherited and three focused fixture cases run only through the production parse entry, with no reachable live/database/network/marker action.
    - One dry run, one candidate/current observation, one admitted health sample, and one timing invocation yield three complete passing pairs and nine ledger-disjoint payloads.
  </behavior>
  <action>At every action and gate, rediscover `PLAN48_ROOT` using the exact one-result `find -P` predicate, export it only to that child command, replay `allocation.json` against current `lstat`, and require every referenced Plan 48 path resolves by `realpath` beneath that root. Build paths by `os.path.join(root, fixed_relative_component)` or double-quoted shell `"$PLAN48_ROOT/relative"`; reject `..`, empty components, newline/NUL, shell evaluation, unquoted expansion, unresolved variables, absolute child components, symlinks, foreign dev/inode, and any second matching root. Use exact relative layout `collector/{health-collector.sh,health.sql,tests/run.sh,fixtures/*}`, `preflight.json`, and `local/{verify-task2.py,collector-verify.once,collector-verify.log,collector-tests.json,dry-home,dry-tmp,candidate,candidate.json,current-databases.sql,current-databases.once,current-databases.raw,current-databases.json,task2-sequence.jsonl,admission.once,admission-invocation.json,preflight-health.json,timing.once,timing/**,timing-evidence.json,recovery-payloads.txt}`. No Plan 48 untracked byte exists outside the root. Materialize `verify-task2.py` before any one-shot action from the complete approved Plan 47 inline semantic verifier with only a typed `PLAN48_ROOT` path resolver substituted; record its bytes/SHA in `preflight.json`, independently AST-scan it for exact receipt reconstruction and absence of evidence writes/actions, and require it unchanged before and after every later gate. It is a read-only replay implementation, not an evidence producer.

Carry Plan 47 Task 1's approved parser proof exactly, replacing fixed `/private/tmp/plan47-*` literals only with receipt-derived root-relative paths. Pin frozen Plan 46 collector 6,284/SHA `c5c06f49c90faa88ee7930910134d4808803b303942e6489382f0ab12f32963c`, runner SHA `e1239a3671a6b652f15e79fdaee11fe7b3e304f10650e6d060d65bc0f5f5d7ed`, SQL `f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9`, and five fixture authorities. Normalize the exact allocated collector-root literal to Plan 46 root, `PLAN48_PSQL_PARSER_` to `PLAN46_PSQL_PARSER_`, and the exact approved new predicate byte block to the old block once; require remaining full bytes, tokens, and embedded-Python AST equal Plan 46. Prove the sole delta is nonempty -> sentinel count -> embedded-LF ordering. Reconstruct Plan 46 cases 1–7 and exact first mismatch case 8 (`multiple_sentinel`, expected `76/sentinel`, actual `75/framing`) without executing Plan 46. Run all exact Plan 48 19+3 cases once through the production `--parse-fixture` entry. Preserve unique raw stdout/stderr/status, argv, exit, class, parser SHA, and output hashes. Prove shell `set -euo`, sole unconditional `exec /usr/bin/python3`, and all-path `sys.exit` dominance; every live `open`/subprocess/socket/psql/createdb/git/gh/network node is after and unreachable from fixture mode. Independently inventory the entire root before/after preflight and require only `preflight.json` plus explicitly materialized collector files change; all exclusive/live targets remain absent. Do not trust aggregate booleans or a self-produced child log.

Then carry every Plan 47 Task 2 control unchanged with root-relative paths. Spend exactly one exclusive wrapper and one real dry run under the exact explicit 11-key environment; independently bind psql argv/framing, redacted environment/password SHA, same-environment verifier, configured/current/session auth, writes zero, inadmissibility, and no retained health. Build the unchanged exact candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3`/tree `94fca667d3f5226347548bdeea625bc737a4348e`/sole parent `87338db7c2678601e20ed3140fd8de1981dfbeb1` inside `$PLAN48_ROOT/local/candidate`, with real contained build/deps/home/tmp, offline locked compile-only preconditioning, clean status, no symlink/shared inode/external path, and no exported `MIX_BUILD_PATH`. Derive the sole candidate database name as `sigra_plan48_` plus the first 12 lowercase hex of the allocation receipt SHA; exact owner `postgres`. Persist and independently validate the closed creation receipt, exact createdb argv/env/output/timestamps, lineage, containment, and receipt cross-links.

Immediately before admission, authenticate Plan 45 protected1675 and take one closed RR/RO current observation. Require rows equal protected1675 plus exactly the candidate row, count 1,676, with exact auth/env/framing/canonical sequence. Spend one admission and one health sample only; retain Plan 47 thresholds and reject wait/resample/cleanup. With no intervening work, authenticate actual `/private/tmp/plan39-prior-payloads-137.txt` as 137 rows/8,905 bytes/SHA `0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08`, lowercase64/C-sorted unique/final LF. Acquire timing once and invoke exact `bash scripts/ci/library-partitions.test.sh` once with `LIBRARY_PARTITION_PAIRS=3`. Preserve nine raw receipts beneath `$PLAN48_ROOT/local/timing`; independently reconstruct exact script/argv/env, candidate/source/calibration/health/ledger bindings, 3 sequential pairs, 6 green children, 225 disjoint exhaustive paths, 114/111 assignment, commands, epoch arithmetic, zero failed/invalid, ratios <=2, and new9 exact 585-byte sorted unique final-LF with zero `comm` intersection against the authenticated ledger. Timing evidence binds the actual ledger path/bytes/count/SHA and allocation receipt file/preimage SHA. All approved Plan 47 inline semantic verifiers remain primary; any generated replay checker is supplemental only.</action>
  <verify>
    <automated>PLAN48_ROOT="$(/usr/bin/python3 -c 'import os,re; print("\n".join(sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan48-ad01e4f1\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False))))')" &amp;&amp; test -n "$PLAN48_ROOT" &amp;&amp; test "$(printf '%s\n' "$PLAN48_ROOT" | wc -l | tr -d ' ')" = 1 &amp;&amp; PLAN48_ROOT="$PLAN48_ROOT" /usr/bin/python3 "$PLAN48_ROOT/local/verify-task2.py" --allocation "$PLAN48_ROOT/allocation.json" --preflight "$PLAN48_ROOT/preflight.json" --timing "$PLAN48_ROOT/local/timing-evidence.json"</automated>
    <automated>PLAN48_ROOT="$(/usr/bin/python3 -c 'import os,re; print("\n".join(sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan48-ad01e4f1\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False))))')" &amp;&amp; test -n "$PLAN48_ROOT" &amp;&amp; test "$(printf '%s\n' "$PLAN48_ROOT" | wc -l | tr -d ' ')" = 1 &amp;&amp; test "$(wc -l &lt; /private/tmp/plan39-prior-payloads-137.txt | tr -d ' ')" = 137 &amp;&amp; test "$(wc -c &lt; /private/tmp/plan39-prior-payloads-137.txt | tr -d ' ')" = 8905 &amp;&amp; test "$(shasum -a 256 /private/tmp/plan39-prior-payloads-137.txt | awk '{print $1}')" = 0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08 &amp;&amp; cmp -s /private/tmp/plan39-prior-payloads-137.txt &lt;(LC_ALL=C sort -u /private/tmp/plan39-prior-payloads-137.txt) &amp;&amp; test "$(LC_ALL=C comm -12 /private/tmp/plan39-prior-payloads-137.txt "$PLAN48_ROOT/local/recovery-payloads.txt" | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <fails_when>Root discovery is zero/multiple or receipt/lstat changes; any Plan 48 path escapes the root or is symlinked; any Plan 45–47 identity changes; parser normalization differs beyond allocated-root/comment/predicate blocks; confinement or any exact 22-case result fails; wrapper/dry-run/candidate/observation/admission/health/timing counts differ from one; candidate/source/calibration/assignment/auth/adjacency changes; actual ledger137 or its timing binding differs; timing is not exactly 3 pairs/6 green children/225 paths/new9 disjoint/ratio <=2; or any retry, wait, resample, cleanup, tuning, recalibration, source edit, alternate root, or GitHub action occurs.</fails_when>
  <done>The unchanged Plan 47 semantic contract passes entirely beneath one receipt-authorized root and produces one admissible three-pair local series.</done>
</task>

<task type="auto">
  <name>Task 3: Capture four fresh live runs and seal evidence (D-01, D-05–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <precondition>The sole root allocation and every Task 1/2 raw receipt independently replay; PR #234 head remains exact `87338db7`; candidate is its clean fast-forward child `3713dcd4`; no Plan 48 GitHub identity exists.</precondition>
  <action>Rediscover and authenticate the one Plan 48 root at entry and before/after every remote action. Revalidate complete Plan 45–47 immutable inventory, allocation receipt/current root `lstat`, parser/local raw evidence, actual ledger137, and exact candidate. Confirm repo `szTheory/sigra`, PR #234, base `main`, exact head ref `refs/heads/ci/phase-235-1-evidence`, remote head `87338db7`, candidate sole parent and clean exact diff. Exclude every retained Plan 35–47 run/job ID. Follow AGENTS.md rate-limit and sole-watcher rules exactly.

Carry Plan 47 Task 3's live budget unchanged: push candidate `3713dcd4` once to only the PR head ref; capture its new automatic PR attempt-1 run and one distinct workflow_dispatch attempt-1 run. Require exact SHA/event/workflow/job identity, selected library job and all required checks successful, independent artifact decoding, and semantic parity. Commit exactly the two canonical tracked PR/scaffold capture receipts as the direct child of candidate, with no root/untracked path or other tracked byte; push that evidence commit once; capture its new automatic PR attempt-1 run plus one distinct dispatch attempt-1 run. No retry/rerun/substitute. Write local canonical `235.1-VALIDATION-RUN.json` only after all four runs pass, without another commit/push. Bind all runs/jobs/artifacts, candidate/evidence lineage, allocation receipt file/preimage SHA, current root dev/inode, every Task 2 receipt, ledger137/new9 disjointness, 225 paths, 114/111 assignment, ratios, and six-module scaffold truth. The three tracked evidence JSON files are the only Plan 48 outputs permitted outside the root. Preserve Plan 47 collisions and all root bytes; no auto-merge, waiver, reconciliation, cleanup, or human substitution.</action>
  <verify>
    <automated>PLAN48_ROOT="$(/usr/bin/python3 -c 'import os,re; print("\n".join(sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan48-ad01e4f1\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False))))')" &amp;&amp; test -n "$PLAN48_ROOT" &amp;&amp; test "$(printf '%s\n' "$PLAN48_ROOT" | wc -l | tr -d ' ')" = 1 &amp;&amp; PLAN48_ROOT="$PLAN48_ROOT" /usr/bin/python3 "$PLAN48_ROOT/local/verify-task2.py" --allocation "$PLAN48_ROOT/allocation.json" --preflight "$PLAN48_ROOT/preflight.json" --timing "$PLAN48_ROOT/local/timing-evidence.json" &amp;&amp; bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
  </verify>
  <fails_when>Root/allocation/history/local replay drifts; a Plan 45–47 path changes; repo/PR/ref/base/candidate/evidence lineage differs; push/dispatch counts differ from two each; a run is reused/retried/nonautomatic/unsuccessful/wrong SHA/event/job/attempt or overlaps Plans 35–47; artifact/provenance/watcher/rate-limit rules fail; evidence omits root allocation bindings or is noncanonical/incomplete/not disjoint; or extra timing/retry/waiver/substitution/merge/cleanup occurs.</fails_when>
  <done>Four fresh attempt-1 runs and canonical tracked evidence prove both routes while every untracked Plan 48 byte remains owned by the one authenticated root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shared `/private/tmp` -> Plan 48 | Untrusted ambient names are excluded by one atomic allocation and canonical root receipt. |
| allocation receipt -> task paths | Every path must resolve beneath the same current dev/inode root without symlinks. |
| fixture bytes -> parser | Adversarial framing crosses only the statically confined production parse entry. |
| local evidence -> GitHub | Exact candidate/evidence commits and fresh run IDs cross the remote boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-48-01 | Spoofing | temporary root | high | mitigate | Atomic mktemp allocation, strict basename, canonical realpath/lstat, owner/mode, dev/inode, exclusive marker, canonical receipt. |
| T-235.1-48-02 | Tampering | root-relative evidence | high | mitigate | Replayed allocation authority, no-follow exclusive writes, descendant checks, canonical receipt/preimage hashes. |
| T-235.1-48-03 | Repudiation | one-shot budgets | high | mitigate | Exclusive markers and exact invocation/run ledgers with immutable Plan 47 zero-action history. |
| T-235.1-48-04 | Information Disclosure | PostgreSQL password | high | mitigate | Closed child environments and password hash/presence only; never argv/log/raw secret. |
| T-235.1-48-05 | Denial of Service | timing/live routes | medium | mitigate | Fixed one-sample/one-series/four-run budgets and fail-closed no-retry rules. |
| T-235.1-48-06 | Elevation of Privilege | parser preflight | high | mitigate | Full source normalization plus shell/Python reachability proof excludes live subprocess/network branches. |
</threat_model>

<verification>
Plan 48 succeeds only when the allocation receipt identifies the sole strict-prefix root, all Plan 45–47 history remains byte-identical, the complete approved Plan 47 semantic gates pass with root-relative paths, one local three-pair series passes, and four fresh live runs produce canonical evidence.
</verification>

<success_criteria>
- Exactly one Plan 48 root was allocated by one mktemp call and remains mode-0700, owner-bound, physical, and uniquely receipt-authorized.
- The three Plan 47 collision files retain exact provenance/type/mode/inode/bytes/SHA and no Plan 47 action was rerun.
- All untracked Plan 48 artifacts are descendants of the authenticated root; only three named tracked evidence files exist outside it.
- Plan 47 parser/source/confinement/local controls pass unchanged after path indirection.
- One timing invocation yields three passing factor-of-two pairs and nine payloads outside authenticated ledger137.
- Four fresh attempt-1 GitHub runs prove candidate and evidence commits on both routes.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-48-SUMMARY.md` when done.
</output>
