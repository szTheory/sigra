---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 04
type: execute
wave: 6
depends_on: [235.1-07]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
  - scripts/ci/verify-library-validation-run.sh
  - scripts/ci/verify-library-validation-run.test.sh
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One retry-free pull-request run executes `Library tests shard`, succeeds, and yields both authoritative artifacts from the same job/run."
    - "The prior blocked Plan 04 summary remains a failed diagnostic checkpoint; only a fresh post-Plan-06 attempt-1 run can enter evidence."
    - "Downloaded raw durations independently satisfy max/min <= 2.0 and install_scaffold <= ordinary; a miss blocks closure and triggers scoped diagnosis/repair rather than threshold relaxation."
    - "Committed evidence binds exact run, PR, SHA, job, attempt, artifact IDs/names/files/digests, raw receipt values, and rate-safe commands."
    - "A committed validation-run receipt proves a distinct follow-up PR run tested the evidence commit SHA and both `Library tests shard` and `Library tests` executed to success."
    - "An offline verifier rejects missing, skipped, red, wrong-SHA, same-run, malformed provenance, or any mutation of the exact rate-limit/watch/structured-summary command values without reusing a watcher."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-EVIDENCE.json"
      provides: "Observed retry-free PR economics and artifact provenance"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct PR validation run, evidence commit SHA, required job states, and exact rate-safe capture commands"
    - path: "scripts/ci/verify-library-validation-run.sh"
      provides: "Offline exact-schema and cross-receipt validation-run verifier"
    - path: "scripts/ci/verify-library-validation-run.test.sh"
      provides: "Hermetic provenance, missing/skipped/red-job, distinct-run, SHA, and one-pole exact-command mutation tests"
    - path: "test/sigra/planning/phase_235_1_library_economics_contract_test.exs"
      provides: "Exact evidence-envelope, recomputation, digest, and mutation contract"
  key_links:
    - from: "GitHub PR run"
      to: "downloaded timing artifacts"
      via: "one structured summary fetch and exact artifact download from the same run/job"
    - from: "235.1-EVIDENCE.json"
      to: "phase_235_1_library_economics_contract_test.exs"
      via: "exact keys, immutable provenance, raw recomputation, and one-pole mutations"
    - from: "evidence branch"
      to: "CI validation run"
      via: "pushed committed evidence, one watcher for that distinct run, and a tracked exact-schema validation receipt"
    - from: "235.1-VALIDATION-RUN.json"
      to: "235.1-EVIDENCE.json"
      via: "offline verifier requires a different run ID and validation head SHA equal to the recorded evidence commit SHA"
  prohibitions:
    - "MUST NOT alter the 2.0 comparability or install non-dominance predicates to fit an observation."
    - "MUST NOT accept a retry attempt, skipped job, missing artifact, failed child, derived-only boolean, or hand-entered duration as passing evidence."
    - "MUST NOT treat run 34395477605 or the existing blocked 235.1-04-SUMMARY.md as task completion or passing evidence."
    - "MUST NOT exceed one watcher per run, poll at three-second cadence, continue after 403/429, or fetch failed logs for a successful run."
---

<objective>
After Plan 06 remediation, capture and bind one fresh real retry-free PR observation, prove both economics predicates from downloaded raw artifacts, and validate the committed evidence through a subsequent CI run.

Purpose: Convert the tested implementation into requirement-authoritative evidence without assuming the first observation passes.
Output: A machine-readable evidence artifact and final evidence mutation contract, plus a green PR validation receipt.

Decision coverage: D-02, D-03, D-05, D-06, and D-09 govern evidence admission; D-07 and D-08 remain blocked until this plan succeeds.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-06-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-07-SUMMARY.md
@.planning/phases/233-library-suite-economics/233-EVIDENCE.json
@.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEDIATION.json
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture one retry-free PR run and bind raw artifacts (D-02, D-03, D-05, D-06)</name>
  <reversibility rating="costly">Creating/pushing an evidence branch and PR is externally visible; commits are revertible, but run history is permanent.</reversibility>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`; before each long watch, `gh api rate_limit` reports REST core remaining above 250.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-EVIDENCE.json, test/sigra/planning/phase_235_1_library_economics_contract_test.exs</files>
  <read_first>
    - `AGENTS.md` GitHub API usage — one watcher per run, 60-second interval, one structured summary, conditional failed logs, hard rate-limit stop.
    - `233-EVIDENCE.json` retry-free run/artifact provenance shape and exact mutation precedent.
    - `235-FAST-01-REMEDIATION.json` current one-owner job/step receipt shape.
    - Blocked `235.1-04-SUMMARY.md` — retain run 34395477605 and its measured miss as checkpoint diagnostics only, never completion.
    - `235.1-06-SUMMARY.md` and `235.1-07-SUMMARY.md` — require both exact 8-file remediation boundaries and the final local fixed-bound gate before selecting a fresh run.
    - `235.1-RESEARCH.md` resolved admission policy and rate-safe commands — never tune bounds to an observation.
  </read_first>
  <behavior>
    - "Evidence is admitted only for event pull_request, conclusion success, run_attempt 1, retry_free true, exact head SHA, and a successful `Library tests shard` job."
    - "Both artifacts share the run/job, have distinct exact names/files, and carry computed SHA-256 digests."
    - "Raw receipt independently passes exact schema, positive durations, max/min <= 2.0, and install_scaffold <= ordinary."
    - "A deterministic predicate miss produces a durable diagnostic and no passing EVIDENCE.json; a transient failure is retried once at most and never admitted as retry-free evidence."
  </behavior>
  <action>
    Treat the existing blocked `235.1-04-SUMMARY.md`, run 34395477605, and its 21,050ms/552,221ms values only as a failed diagnostic checkpoint; they do not complete any task and must not populate evidence. Require completed Plans 06 and 07 plus their unchanged local predicate gate, then ensure Plans 01–03, 06, and 07 commits are on the scoped evidence branch and open or reuse one PR against `main`. Resolve the exact new pull_request run once. Before watching, query `gh api rate_limit --jq '.resources.core | {remaining,reset}'`; hard-stop on remaining <=250 or HTTP 403/429 until the reported time. Use exactly one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`, then fetch `databaseId,event,headSha,conclusion,runAttempt,url,jobs` once. Fetch failed logs only if conclusion is failure. Retry a transient failure once; diagnose and repair deterministic failures within the fixed Phase 235.1 ownership/evidence scope, push a new commit, and require a new attempt-1 run. Never admit a rerun attempt as evidence.

    Download the two exact run-qualified artifacts to a validated `mktemp -d` directory, verify both with the production scripts, calculate SHA-256, and populate `235.1-EVIDENCE.json` only if the raw predicates pass. Use schema `sigra.library-economics-evidence/v1` with exact provenance, job, artifact, class, receipt-validation, exact-once coverage, protected-invariant, and commands fields. Record raw observed values and the exact rate-check/watch/view/download commands. Extend the phase contract to require exact key sets and values, recompute both predicates, bind the one owner and six receiver paths, and mutation-test run attempt, event, SHA, job conclusion, artifact ID/digest, raw durations, class execution, and protected invariant fields.
  </action>
  <acceptance_criteria>
    - One attempt-1 pull_request run and successful owner are bound to two downloaded same-run artifacts by IDs and SHA-256.
    - Independent verification proves both raw timing predicates and exact-once coverage; no trusted boolean substitutes for recomputation.
    - Failure/rate-limit paths halt with durable diagnostics and never create passing evidence.
    - The evidence contract rejects every provenance, artifact, raw-value, coverage, and invariant mutation.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify-library-economics.sh &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; jq -e '.schema_version == "sigra.library-economics-evidence/v1" and .run.event == "pull_request" and .run.conclusion == "success" and .run.run_attempt == 1 and .run.retry_free == true and (.artifacts | length) == 2 and .economics.comparable == true and .economics.install_not_dominant == true' .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-EVIDENCE.json</automated>
    <fails_when>any command exits non-zero; either protected verifier banner is absent; ExUnit reports any failure or zero tests; evidence is absent/malformed; provenance is not retry-free PR attempt 1; an artifact is missing/unbound; or either raw economics predicate is false</fails_when>
  </verify>
  <done>A real attempt-1 PR run proves same-run slow visibility, comparable work classes, install non-dominance, and exact-once coverage with immutable artifact provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate the committed evidence contract on the PR (D-07, D-09)</name>
  <reversibility rating="costly">The validation run is permanent external evidence, although the branch and PR remain normally revertible.</reversibility>
  <precondition>Task 1 evidence and contract are committed locally, the PR remains open, and REST core remaining is above 250 before the watch.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json, scripts/ci/verify-library-validation-run.sh, scripts/ci/verify-library-validation-run.test.sh</files>
  <read_first>
    - Task 1 commit and `235.1-EVIDENCE.json` — the validation run must test these exact committed bytes.
    - `AGENTS.md` watcher and structured-summary constraints.
    - `.github/workflows/ci.yml` job names — require the bare `Library tests` context and its direct owner to execute successfully, not skip.
    - `233-EVIDENCE.json` and `235-FAST-01-REMEDIATION.json` — reuse exact-key provenance and adverse-mutation patterns, not their historical run values.
  </read_first>
  <behavior>
    - "The validation receipt has schema `sigra.library-economics-validation-run/v1`, exact repository/PR/run/command/job keys, and no producer-owned pass boolean."
    - "Its run is a pull_request run distinct from the capture run, and its head SHA exactly equals the full commit SHA containing `235.1-EVIDENCE.json` plus its contract."
    - "Both exact jobs have status `completed`, conclusion `success`, and skipped=false; missing, skipped, cancelled, neutral, timed_out, action_required, or failure states fail."
    - "The verifier cross-checks the validation receipt against `235.1-EVIDENCE.json`; provenance, SHA, distinct-run, missing-job, and red-job mutations each fail independently."
    - "The command record contains exactly `gh api rate_limit --jq '.resources.core | {remaining,reset}'`, a watch command for the recorded run with `--repo szTheory/sigra --compact --interval 60 --exit-status`, and one structured-summary command for that run with `--repo szTheory/sigra --json databaseId,event,headSha,conclusion,runAttempt,url,jobs`."
    - "One-pole changes to the rate-limit command, repository, `--compact`, interval value, `--exit-status`, structured-summary verb/options, or an extra structured-summary command each fail."
  </behavior>
  <action>
    Start RED by creating hermetic fixtures for a valid distinct validation run and one-pole mutations covering repository/event/PR provenance, capture-run reuse, wrong evidence commit SHA, run attempt, missing job, skipped job, non-completed job, every non-success conclusion, extra keys, and malformed JSON. Add separate one-pole fixtures that alter only one recorded command value at a time: the literal `gh api rate_limit --jq '.resources.core | {remaining,reset}'`; `szTheory/sigra`; presence of `--compact`; `--interval 60`; presence of `--exit-status`; and the single literal structured-summary shape `gh run view RUN_ID --repo szTheory/sigra --json databaseId,event,headSha,conclusion,runAttempt,url,jobs`. Include mutations for a wrong run ID and a duplicated/second summary command. Implement `verify-library-validation-run.sh` as an offline authority over the two fixed tracked receipt paths. It must require exact key sets and types, compare the validation run ID against the capture run ID in `235.1-EVIDENCE.json`, compare validation `head_sha` with `evidence_commit_sha`, require pull_request provenance, require exactly the two named jobs with `status=completed`, `conclusion=success`, and `skipped=false`, and reconstruct the only accepted watch/summary command strings using the recorded validation run ID. Recompute every admission predicate from raw fields and reject any stored verdict field.

    Commit Task 1 evidence and its contract, record that full commit SHA, push it to the same PR, and resolve only the distinct run triggered by that SHA. Before the long watch execute `gh api rate_limit --jq '.resources.core | {remaining,reset}'`; stop without another polling request when remaining is 250 or less, and hard-stop until GitHub's reported reset/retry time on HTTP 403 or 429. Use exactly `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status` once for this run. After completion fetch exactly one structured summary with `gh run view RUN_ID --repo szTheory/sigra --json databaseId,event,headSha,conclusion,runAttempt,url,jobs`; fetch failed logs once only when the conclusion is failure. On failure, distinguish transient from deterministic failure, retry a transient once, and repair deterministic failures without altering raw evidence or thresholds.

    From that single structured response, atomically create `235.1-VALIDATION-RUN.json` with schema `sigra.library-economics-validation-run/v1`, repository, PR number, evidence commit SHA, capture run ID, validation run ID/URL/event/head SHA/attempt/conclusion, exact raw state for `Library tests shard` and `Library tests`, capture timestamp, and exactly three command fields: the literal rate-limit command, the literal watch command with the recorded run ID, and the literal single structured-summary command with that same run ID. Do not store a pass boolean or a list that could hide repeated summary fetches. Run the offline verifier and its command/provenance/job-state mutation suite before committing the receipt; the receipt is the durable validation result and the summary only cites it.
  </action>
  <acceptance_criteria>
    - A tracked machine-readable receipt binds the committed evidence/contract SHA to a distinct successful PR run with executing direct owner and protected aggregate.
    - One watcher and one structured summary fetch are used for this run; rate-limit policy is honored.
    - Offline verification rejects malformed provenance, capture-run reuse, wrong SHA, and every missing/skipped/red required-job mutation.
    - Exact literal command assertions pass, and independent one-pole mutations of the rate-limit command, repository, compact flag, 60-second interval, exit-status flag, run ID, structured-summary field list, and summary-command cardinality all fail.
    - No raw duration, threshold, artifact digest, protected receipt, requirement status, or audit verdict changes during validation.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; bash scripts/ci/verify-library-economics.sh &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs</automated>
    <fails_when>any command exits non-zero; either validation-verifier PASS banner or either protected verifier banner is absent; ExUnit reports any failure or zero tests; the tracked receipt does not prove a distinct successful PR run at the evidence commit SHA; either required job is skipped/missing/red; any one-pole mutation of the literal rate-limit command, repository `szTheory/sigra`, `--compact`, `--interval 60`, `--exit-status`, recorded run ID, or single structured-summary command is accepted; or GitHub rate policy is violated</fails_when>
  </verify>
  <done>The committed machine evidence is itself CI-validated on the PR, with a mutation-tested tracked receipt that independently authorizes final truth-document reconciliation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-EVIDENCE.json` — `sigra.library-economics-evidence/v1` attempt-1 PR provenance, job/artifact bindings, raw durations, exact-once coverage, independent verdicts, protected invariants, and commands.
- `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json` — exact-schema distinct-run proof tying the evidence commit SHA to two executing successful required jobs and the prescribed rate-safe commands.
- `scripts/ci/verify-library-validation-run.sh` — offline cross-receipt admission verifier.
- `scripts/ci/verify-library-validation-run.test.sh` — hermetic provenance and job-state mutation suite.
- `test/sigra/planning/phase_235_1_library_economics_contract_test.exs` — final committed-evidence envelope and adverse mutation checks.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API/run metadata to local evidence | External metadata and job state become milestone authority. |
| Downloaded artifacts to committed JSON | Untrusted bytes must be bound to exact IDs/digests and independently validated. |
| Evidence commit to validation run | The PR must execute the committed contract, not only pre-evidence implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-235.1-04-01 | Spoofing | selected GitHub run/job | high | mitigate | Bind repo, PR, event, attempt, head SHA, run ID/URL, exact job name/ID, and successful execution. |
| T-235.1-04-02 | Tampering | artifact bytes | high | mitigate | Exact artifact ID/name/file, SHA-256, schema, and production verifier before commit. |
| T-235.1-04-03 | Repudiation | missing/retried evidence | high | mitigate | Attempt-1/retry-free admission, explicit commands, one watcher, structured summary, and fail-closed absence rules. |
| T-235.1-04-04 | Denial of Service | API polling/rate limits | medium | mitigate | REST preflight, 60-second watch interval, one watcher per run, one summary, and hard stop on 403/429. |
| T-235.1-04-05 | Tampering | threshold fitting | high | mitigate | Fixed integer predicates from context; a miss blocks and triggers scoped repair, never bound changes. |
| T-235.1-04-06 | Spoofing | validation-run summary and command provenance | high | mitigate | Commit exact-schema run receipt; cross-check distinct run ID, evidence commit SHA, PR provenance, raw required-job states, and exactly one literal rate-safe watcher/summary command set with one-pole mutation-tested offline code. |
| T-235.1-04-SC | Tampering | package supply chain | low | accept | No new package is installed by this plan; CI uses existing pinned setup. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Real PR evidence without FAST/GATE regressions | 235.1-04 | COVERED | Capture plus committed validation run. |
| REQ | TEST-01 | Current same-run slow receipt | 235.1-04 | COVERED | Downloaded formatter artifact. |
| REQ | TEST-02 | Current comparable raw durations | 235.1-04 | COVERED | Fixed ratio recomputation. |
| REQ | TEST-03 | Current non-dominant exact-once install | 235.1-04 | COVERED | Raw duration and receiver binding. |
| RESEARCH | — | Rate-safe attempt-1 observed evidence | 235.1-04 | COVERED | AGENTS-prescribed commands and hard stops. |
| CONTEXT | D-02, D-03, D-05, D-06 | Equivalent classes, same run, PR coverage, raw proof | 235.1-04 | COVERED | Live artifacts and mutations. |
| CONTEXT | D-07 | Reconcile only after passing evidence | 235.1-04 | COVERED | The tracked validation-run receipt and verifier are the prerequisite gate. |
| CONTEXT | D-09 | Preserve terminal proofs | 235.1-04 | COVERED | Independent verifiers in both tasks. |

<verification>
Require a retry-free capture run, two same-job downloaded artifacts, fixed raw predicates, exact provenance and mutation contracts, both protected offline verifiers, and a tracked independently verified distinct successful PR run over the committed evidence contract.
</verification>

<success_criteria>
- One attempt-1 PR run produces valid same-run timing artifacts.
- Raw values pass both fixed economics predicates without threshold fitting.
- Evidence provenance and every truth-bearing value are mutation-tested.
- A later PR run validates the committed evidence bytes and required aggregate, with its SHA and raw job states retained in a mutation-tested receipt.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-04-SUMMARY.md` when done, citing both tracked receipts and their verifier commands.
</output>
