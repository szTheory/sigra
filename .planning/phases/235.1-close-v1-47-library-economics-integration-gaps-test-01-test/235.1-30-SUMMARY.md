---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 30
subsystem: ci-evidence
tags: [relational-authority, exact-tree, deterministic-block]
status: halted
dependency_graph:
  requires: [235.1-29, 235.1-21]
  provides: [relational-portability-contract, one-file-exact-successor, three-local-pair-diagnostics]
  affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
tech-stack:
  added: []
  patterns: [dynamic-authority-replay, isolated-negative-controls, deterministic-commit-tree]
key_files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-30-SUMMARY.md
  modified:
    - scripts/ci/library-partitions-portability.test.sh
decisions:
  - Derive portability expectations only from the current loader, calibration, manifest, source index, and active-authority output.
  - Preserve the third local pair's factor-two failure and stop before all GitHub work, as required by the one-invocation budget.
requirements-completed: []
actuals:
  tokens: 4400
  tasks: 1
  commits: 2
metrics:
  duration: 25m
  completed: 2026-09-10
---

# Phase 235.1 Plan 30: Relational Portability Recovery Summary

The portability contract now follows current calibration authority without stale assignment pins, and the exact one-file successor completed two local pairs before the sole timed invocation stopped on pair 3's raw factor-two failure.

## Task 1 — Complete

RED was reproduced on exact Plan 29 candidate `1f19b0c9f93c7b439f440682a02eff3d3dfb988c`: both Bash 3 and Bash 5 synthetic cases reached the final Python authority block, which rejected only the superseded Plan 22 literal map.

Commit `d7f092dfe59464e8a89081909e6a0a0b323b5d6a` changed only `scripts/ci/library-partitions-portability.test.sh`. The final block now reads the current oracle TSV, calibration bytes, manifest bytes, current source files, and `--print-active-authority` output. It proves:

- exactly two non-empty, sorted partitions whose disjoint union equals the calibration universe and source index;
- count, derived-cost total, and newline manifest digest for each exact path list;
- NUL universe digest, canonical source-index digest, and each current source file's byte count and SHA-256;
- calibration-to-manifest byte binding and unique calibration/manifest entries in active authority;
- actual artifact/manifest SHA-256 and size equality without including the portability test in active authority.

Independent copied-value mutations reject count, total, partition manifest, dropped/duplicated/swapped membership, calibration bytes, manifest calibration digest/size, and active calibration/manifest digest/size. A pristine replay runs after every mutation. Existing exact argv/order, spaces/glob characters, absolute-path normalization, empty-partition fail-closed behavior, nounset diagnostics, Bash 3/Bash 5 parity, and the `mapfile`/`readarray` prohibition remain unchanged.

The direct portability test, routing authority mutation suite, `--preflight`, and 35 focused loader/Phase 235.1 tests passed. A repository scan found no active Plan 22 totals, partition manifests, or artifact identities outside historical plans and summaries.

## Task 2 — Deterministic Hard Stop

The successor was independently derived twice by overlaying only the Task 1 test blob on tree `a96510e190063039702958687aa318ec2a0dd6dd`, retaining sole parent `47f9578fc97edc63603953d446e79fac884326c3` and fixed identity/message/timestamps. Both derivations produced:

- commit `33a4f2350e465ecba75296085dc7b2edbba60260`;
- tree `dad81a73a168c1742e9743d93ec5417d036586ca`;
- exactly one changed file from candidate `1f19b0c9`;
- exactly five recursive changes from candidate `9ae29062`: Plan 29's two calibration artifacts, two active-pin files, and the portability test.

Plan 29 authority remained exact: calibration `8d54ea4ddb09b7aa5b6b0a5acec462f6e57ba2218a3466c31cf2cad76a562603` / 2,986,763 bytes, manifest `61ecf707ee0535da9995767984c8afa5abd75eff00b3dcd774d1f800d0e09ef9` / 623 bytes, three samples/nine payloads, 225 source-indexed paths, seven active entries, and unchanged runner, workflow, source, audit, and lifecycle bytes.

A fresh detached worktree at `/private/tmp/sigra-plan30.WI8TKP/successor` received independent byte copies of 3,369 dependency files and 817 cache files. The copies contained no symlinks and shared zero regular-file device/inode pairs with their source. Candidate-local home/Mix/Hex/Rebar/XDG/tmp paths, offline Hex resolution, absent `MIX_BUILD_PATH`, and a real default `_build/test` were used.

Compile-only preconditioning, exact relocation (1 test, 0 failures, 27 excluded), Bash portability, routing/preflight, validation synthetic suite, independent partition verifier, 74 calibration/lifecycle tests, protected offline attestations, Actionlint, formatting, BEAM metadata containment, and a focused 21-test timing-receipt normalization probe all passed before timing. The candidate remained tracked-clean and the main checkout's unrelated dirty state remained unchanged.

`library-partitions.test.sh` was invoked exactly once. Its synthetic cases passed and it produced exactly three later pairs:

| Pair | Partition 1 | Partition 2 | Receipt duration | Result |
| --- | --- | --- | --- | --- |
| 1 | 1,254 tests, 0 failures | 33 doctests, 3 properties, 1,378 tests, 0 failures, 12 skipped | 1ms / 1ms | PASS |
| 2 | 1,254 tests, 0 failures | 33 doctests, 3 properties, 1,378 tests, 0 failures, 12 skipped | 1ms / 1ms | PASS |
| 3 | 1,254 tests, 0 failures | 33 doctests, 3 properties, 1,378 tests, 0 failures, 12 skipped | 3ms / 1ms | FAIL: raw 3.0 ratio |

Pair 3 remained exhaustive/disjoint over 110/115 paths with both child conclusions `success` and exit statuses `0`. Its final receipts are:

- combined `172866a8935d2ec2dda7dd3bc8bd6ef0dad2981fb06d500580acba79376115b4`;
- partition 1 `ab59cf4da0f06576c7060ff7633f6b8c3765c079a86414cd3caf3285d6dc1623`;
- partition 2 `2fe21117c0df077a6b9d4956728dba0a2ae93368e8070fdd5e4f8534b395a62c`.

The raw admission arithmetic was `3 * 1000 > 1 * 2000`. The immediate mechanism is the frozen runner's `/usr/bin/python3` clock: on this macOS host, each fresh Apple Python 3.9 process reports process-relative `time.monotonic_ns()`, so separately spawned start/end clocks yielded 7/10ms and 4/5ms instead of wall elapsed time. The plan forbids runner edits, timing retries, rematerialization, recalibration, threshold changes, or alternate candidates, so this is a deterministic stop.

## Task 3 — Not Run

No successor push, workflow dispatch, watcher, structured GitHub summary, artifact download, PR/scaffold receipt, evidence commit, validation pair, or validation receipt occurred. All three planned receipt files remain absent and the milestone audit remains in its pre-evidence state.

## Budget and External-Action Accounting

- `library-partitions.test.sh` invocations: exactly one.
- Real later local pairs: exactly three; two admitted and one rejected.
- Recalibrations, rematerializations, assignment changes, threshold changes, and timed retries: zero.
- GitHub pushes, dispatches, watchers, summaries, failed-log fetches, and downloads: zero.
- PR/scaffold/validation receipts: zero.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Environment] Supplied the frozen Erlang version to an untimed nested verifier**

- **Found during:** Task 2 untimed independent-verifier gate.
- **Issue:** The exact candidate's `.tool-versions` names unavailable Erlang 28.5, and the initially bare nested script could not resolve `erl`.
- **Fix:** Re-ran only the untimed remaining gate under the already-planned isolated `ASDF_ERLANG_VERSION=28.4.1` environment. No timed command had started.
- **Files modified:** None.

**2. [Rule 1 - Probe] Used the formatter's allowlisted receipt path**

- **Found during:** Task 2 supplemental focused containment probe.
- **Issue:** The first probe requested a candidate-local filename that the frozen formatter correctly refuses; no receipt was emitted.
- **Fix:** Re-ran only that untimed focused test using its allowlisted partition-1 receipt, copied it into candidate-local state, and proved all raw/normalized source paths were contained.
- **Files modified:** None.

**3. [Rule 1 - Observation wrapper] Preserved the timed failure masked by `tee`**

- **Found during:** Task 2 sole timed invocation.
- **Issue:** The outer observation pipeline did not set `pipefail`, so `tee` returned zero after the inner verifier emitted the factor-two failure.
- **Fix:** Read the already-produced receipt directly, confirmed raw `3000 > 2000`, and treated the run as failed. The timed harness was not invoked again.
- **Files modified:** None.

## Deferred Issue

The frozen cross-process macOS clock source does not represent wall time under `/usr/bin/python3`, making millisecond admission unstable even though both partitions pass. Correcting that runner is outside Plan 30's exact-tree authority and retry budget.

## Known Stubs

None.

## Threat Review

No endpoint, auth path, schema, dependency, ordinary source, workflow, runner, loader, calibration, manifest, active pin, audit, threshold, or receipt authority was changed. The relational test remains outside active authority and every adverse check operates on copied/in-memory values.

## Self-Check: PASSED

- Task commit `d7f092df` and exact successor `33a4f235` resolve.
- The successor has the required tree, sole parent, one-file delta from Plan 29 authority, and five-file delta from `9ae29062`.
- The isolated candidate is tracked-clean; the three final timing receipts are regular files with the recorded digests.
- The timed budget was consumed once, the failing third pair was not retried, and all live-evidence artifacts remain absent.
