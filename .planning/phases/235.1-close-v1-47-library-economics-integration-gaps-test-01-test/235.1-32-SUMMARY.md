---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 32
subsystem: ci-evidence
tags: [physical-path, containment, bash-portability, exact-tree, deterministic-block]
status: halted
dependency_graph:
  requires: [235.1-31, 235.1-30, 235.1-29]
  provides: [physical-root-containment, hostile-path-regression, four-file-exact-successor, local-timeout-diagnostic]
  affects: [TEST-01, TEST-02, TEST-03, 235.1-05]
tech-stack:
  added: []
  patterns: [portable-pwd-physical-root, canonical-relative-path-grammar, deterministic-commit-tree]
key_files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-32-SUMMARY.md
  modified:
    - scripts/ci/library-partitions.sh
    - scripts/ci/library-partitions-portability.test.sh
    - scripts/ci/verify-library-routing-evidence.sh
    - scripts/ci/verify-library-routing-evidence.test.sh
decisions:
  - Resolve the script-relative repository root with portable `pwd -P` before timing-path containment.
  - Admit only canonical `test/..._test.exs` paths and fail closed on absolute lookalikes or malformed relative components.
  - Preserve the second-pair database timeout and stop without retry, rematerialization, tuning, or GitHub activity.
requirements-completed: []
actuals:
  tokens: 2141
  tasks: 1
  commits: 3
metrics:
  duration: 15m
  completed: 2026-09-10
---

# Phase 235.1 Plan 32: Physical-Root Containment Summary

The production runner now treats macOS lexical and physical temporary-directory aliases as one repository boundary while rejecting hostile timing paths, but the sole timed invocation stopped on pair 2 when an ordinary audit test's database setup command timed out.

## Task 1 — Complete

RED commit `f6c59e48` copied the actual runner into a physical temporary repository, invoked it through a distinct lexical symlink, and made the fake formatter identify the repository by its physical path. The current Plan 31 runner failed the first Bash 3 positive control with the reproduced `timing receipt contains a path outside the repository test tree` containment rejection. The same test matrix drives production `normalize_timing_paths` under system Bash 3 and modern Bash 5; it does not duplicate the normalization helper.

GREEN commit `99641dcc` changed repository initialization to `cd` to the script-relative root and emit `pwd -P`. Timing rows now normalize only when they are either a physical absolute path beginning at the exact `$ROOT/test/` component boundary or an already-relative canonical `test/..._test.exs` path. Empty, `.`, and `..` components, doubled separators, non-test paths, wrong suffixes, external absolute paths, and sibling-prefix lookalikes fail closed. Each hostile case returned nonzero, left partition 1 as `failure`, and left partition 2 as `not_run`; both safe controls normalized to `test/safe_test.exs`. Existing epoch-clock, sequential-child, fixed-receipt, exact argv/order, nounset, empty-partition, and first-child status semantics remained green.

Authority commit `03aec79e` refreshed only the runner entry from `22dac275...` to `deae1229e1bfabea6924d2512db1293026d6904497c2ac804f0478afc04471dd` in `CURRENT_ACTIVE_AUTHORITY` and its adverse expected list. The other six entries, sizes, negative-run ledger, and historical authority were unchanged. Pristine preflight and all seven isolated mutation/restoration cases passed. The superseded digest is absent from active scripts, tests, and workflow files and remains only in historical planning artifacts.

The tracer gate passed with `ASDF_ERLANG_VERSION=28.4.1` applied to the whole command to isolate the user's unrelated dirty `.tool-versions`: Bash 3/Bash 5 portability, routing adverse and active-authority suites, preflight, the independent partition verifier, and 35 focused partition/economics tests all passed.

## Task 2 — Deterministic Hard Stop

Plan 31's required precondition was re-proven: commit `34931b9cb8e2edd6ee68dd81c0ce18acf8a5e0a4` has tree `462bb0594e7c1f843401693eb5d72639c524ce38` and sole parent `47f9578fc97edc63603953d446e79fac884326c3`.

Two independent index and commit-tree derivations over that tree, using the same fixed author, committer, timestamp, message, and sole parent, produced exact successor:

- commit `63910813519db336d5e4556b885cdc0f3879799f`;
- tree `bd81cf764c901d3e2f728febe1240b57db7a2d7a`;
- exactly four changes from Plan 31: runner, portability test, routing verifier, and routing adverse test;
- exactly six recursive changes from `9ae29062`: those four files plus Plan 29's calibration and manifest artifacts.

Plan 29 authority remained exact: calibration `8d54ea4d...` at 2,986,763 bytes, manifest `61ecf707...` at 623 bytes, 225 exhaustive paths assigned 110/115 at 19,142,933us/19,142,928us. No calibration, manifest, assignment, threshold, ordinary source, workflow, audit, lifecycle, receipt, or protected-attestation byte changed.

A fresh detached worktree at `/private/tmp/sigra-plan32.WOL3NM/successor` used 4,169 independently copied dependency/cache files with zero symlinks, candidate-local home/Mix/Hex/Rebar/XDG/tmp state, offline mode, absent inherited `MIX_BUILD_PATH`, and a fresh real `_build/test`. Locked dependency sources, unused-lock validation, dependency compilation, and project compilation passed.

All untimed gates passed before measurement: exact relocation (1 test, 0 failures, 27 excluded), Bash 3/Bash 5 alias and hostile-path portability, routing mutations/preflight, validation synthetics, independent partition verifier, 57 calibration/lifecycle/terminal contracts, both protected offline attestations, Actionlint, formatting, exact tree/frozen-byte checks, candidate cleanliness, and unrelated-dirt preservation.

`library-partitions.test.sh` was invoked exactly once. Its synthetic cases passed. Real pair 1 completed successfully:

| Pair | Partition 1 | Partition 2 | Ratio | Result |
| --- | ---: | ---: | ---: | --- |
| 1 | 36,153ms | 65,828ms | 1.8208 | admitted |
| 2 | 30,216ms | not run | — | blocked |

Pair 1 digests were combined `0a9a5ba33fd1767734e82c1b0e27ecf10f5296d7b2c58f599e35e2328e2a94a2`, partition 1 `5f2ac8a9cbc2ab646a1815c890aa5be502447de044bca8acbbf3314be0bd989a`, and partition 2 `ebde710f27bc2d16de21a18171a237282c64f1ab10249b10d2115c0e12560a07`.

Pair 2 partition 1 completed 1,254 tests with zero failures but one invalid test after `Sigra.Audit.QueryIndexTest`'s `setup_all` received `{:error, "command timed out"}` from a PostgreSQL query held longer than 15 seconds. The runner truthfully published partition 1 `failure`, exit status 2, duration 30,216ms and partition 2 `not_run`. Diagnostic digests are timed log `36d6b52991fd4601722fc4e9ce3d1f5397db46787d5cc084fffeaa69b04f6ce1`, combined receipt `d8c859d6bf9b76d3743ff0983a5ec97c2dde9131b4ff33a470f2d256bb0527b6`, and partition-1 timing `4db90914e521c7b12b0cfedd5aecca2f308d88b773c195005bd172526b637bdd`.

The plan makes the first timed failure a hard stop. There was no retry, rematerialization, recalibration, reassignment, threshold change, code change, alternate candidate, or GitHub action. The successor was not pushed and no capture receipt was written.

## Task 3 — Not Run

No capture evidence commit, automatic PR run, workflow dispatch, watcher, structured GitHub summary, artifact download, validation pair, or validation receipt occurred. All three evidence artifacts remain absent, and the milestone audit remains in its pre-evidence state.

## Budget and External-Action Accounting

- `library-partitions.test.sh` invocations: exactly one.
- Admitted real local pairs: one of three; pair 2 stopped after partition 1.
- Recalibrations, assignment changes, threshold changes, timed retries, and rematerializations: zero.
- GitHub pushes, dispatches, watchers, summaries, logs, downloads, and live receipts: zero.

## Deviations from Plan

None. The environment-specific Erlang override was the established isolation for unrelated user dirt, and the timed failure followed the plan's mandatory stop path.

## Deferred Issue

`Sigra.Audit.QueryIndexTest` can invalidate an otherwise green ordinary partition when its setup query queues and holds a PostgreSQL connection beyond 15 seconds. The failed timed budget cannot be retried within this plan; a successor must diagnose and deterministically isolate this contention before consuming new evidence.

## Known Stubs

None.

## Threat Review

No endpoint, authentication path, schema, dependency, ordinary source, workflow, calibration, manifest, loader, lifecycle, audit, factor-two threshold, or evidence receipt changed. Strict physical containment rejects path spoofing, the candidate retained its exact sole parent and permitted four-file overlay, and no live provenance was admitted.

## Self-Check: PASSED

- Task commits `f6c59e48`, `99641dcc`, and `03aec79e` resolve.
- Exact successor `63910813` resolves to tree `bd81cf76`, sole parent `47f9578`, and the required four-/six-path diffs.
- The isolated candidate is tracked-clean and the local failure log/receipts have the recorded digests.
- The timed budget was consumed exactly once, the pair-2 failure was not retried, and all three live-evidence artifacts remain absent.
