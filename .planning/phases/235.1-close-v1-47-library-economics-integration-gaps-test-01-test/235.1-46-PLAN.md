---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 46
type: execute
wave: 43
depends_on: [235.1-45]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Plan 45 remains immutable failed history: its successful snapshot proof is consumed as authority and its collector wrapper is never rerun."
    - "The production collector parser accepts exactly BEGIN, one authenticated sentinel JSON row, and ROLLBACK, while rejecting every contaminated framing."
    - "One fresh dry run and one fresh admission use byte-identical SQL/parser/argv/environment contracts without changing the protected database set."
    - "Exactly one production timing command yields three passing pairs and nine payloads disjoint from ledger137."
    - "Four fresh original-attempt-1 GitHub runs prove candidate and evidence commits on both routes."
    - "Candidate `3713dcd4`, tree `94fca667`, calibration, 114/111 assignment, ledger137, audit, and ordinary source remain unchanged."
  artifacts:
    - path: /private/tmp/plan46-collector-tests.json
      provides: "Fresh hash-bound framing fixture and sole dry-run proof"
    - path: /private/tmp/plan46-preflight-health.json
      provides: "Sole admitted environment and protected-state health sample"
    - path: /private/tmp/plan46-recovery-payloads.txt
      provides: "Nine fresh local validation payload hashes"
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
      provides: "Strict local/live route validation"
  key_links:
    - from: "Plan 45 protected snapshot"
      to: "Plan 46 admission"
      via: "canonical receipt replay and exact name+owner ledger preservation"
      pattern: "7fb6b3978fbbd44c5bdc74aa84441db97757e5f3288a3f32481b39cfb2a42c1d"
    - from: "framing fixtures"
      to: "dry run and admission"
      via: "the same production parse function and byte-identical SQL"
    - from: "ledger137"
      to: "Plan 46 new9"
      via: "strict lowercase64 C-sorted uniqueness and zero intersection"
      pattern: "0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08"
---

<objective>
Repair only the collector's real-psql transaction framing, then resume the bounded health, local, and live evidence route.

Purpose: Plan 45 proved the native Perl fixture and authoritative protected snapshot, then its sole collector dry run correctly stopped because the parser expected one line although real file-mode psql emits `BEGIN`, the sentinel row, and `ROLLBACK`. Plan 46 authenticates that exact framing under a fresh namespace without altering or rerunning Plan 45.
Output: One framing receipt, one health receipt, one three-pair validation series, and strict live evidence. Plan 05 remains the docs-only downstream reconciliation plan and is not executed or edited here.

Decision coverage: D-01 preserves PR ownership; D-02–D-04 preserve partitions and threshold; D-05/D-06 preserve exact-six routing; D-07 requires fresh evidence; D-08 keeps reconciliation in Plan 05; D-09 preserves every failed attempt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-41-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-45-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-45-SUMMARY.md
@scripts/ci/library-partitions.test.sh
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
</context>

## Frozen authority and exact failure

Plan 45 summary commit is `6a60788dfca75923151b5cd07d98be8e7384b706`; its summary is 11,805 bytes/SHA-256 `51682ebe1a21d9bd2f4e9641b04f30db0fd00114eaeee8cb92ae6a312c0679b2`. Replay its complete machine evidence before creating a Plan 46 path. `/private/tmp/plan45-snapshot-tests.json`, `/private/tmp/plan45-live-probe.json`, and `/private/tmp/plan45-protected-databases.json` remain accepted, canonical, and immutable. The authoritative protected receipt file SHA is `7fb6b3978fbbd44c5bdc74aa84441db97757e5f3288a3f32481b39cfb2a42c1d`; its protected ledger is exactly 1,675 names, 48,446 bytes, SHA `97f14d8d3805e79f92b41aa2b0f56955a88144e14ad5b48adb83a1f7875d1c72`, with identical before/after name+owner ledgers and zero mutation. The eight-call Perl proof, call log 5,405 bytes/SHA `153a67f88e9c9b564d12638df79fd17d5d4ba378f4f8385212f9dc2e08598a03`, and all snapshot/probe markers are complete; consume them, do not rerun them.

The Plan 45 collector source is 2,392 bytes/SHA `37ffa87aee4ac7209b532e6d5b24fc91abcead040c3df08e1435c056830f3857`; its SQL is 1,653 bytes/SHA `f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9`. The SQL/psql contract deliberately omits `--quiet`, so successful real output is byte-framed as `BEGIN\n`, exactly one `SIGRA_PLAN45_HEALTH_V1<TAB>{JSON}\n`, then `ROLLBACK\n`. Plan 45 instead filtered nonempty lines and asserted length one. Its consumed marker is 24 bytes/SHA `9c37c03193de251f7e181543e0036ae40ae22d96ce0118820841e86766e8b0d4`; its incomplete 134-byte log SHA is `a1fc3afefa9fb22d2a71346bd9c6cfd1aa4cd375c4b54f2cbc6187aa7bd9fef1` and ends at `dry_run_start<TAB>1\n`; its collector receipt, admission, health, timing, and GitHub artifacts are absent. Hash the complete retained Plan 45 namespace before and after Plan 46 and require byte/inode/mtime equality. Never invoke a Plan 45 script, acquire a Plan 45 marker, delete a Plan 45 temporary, or promote its failed dry run.

Candidate remains `3713dcd4408b2a280a53b541f973b4acdb79aff3`, tree `94fca667d3f5226347548bdeea625bc737a4348e`, sole parent and current PR #234 head `87338db7c2678601e20ed3140fd8de1981dfbeb1`. Canonical ledger137 remains exactly 137 LF-terminated lowercase hashes, 8,905 bytes, SHA `0d31c343f3f7b642d9ad20486940f9a848454249c0c7cfaaf1587b03dd48bd08`. No recalibration, assignment change, ordinary-source change, candidate rewrite, database cleanup, or protected-state disposition is authorized.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove exact transaction framing and spend one fresh dry run (D-07, D-09)</name>
  <files>/private/tmp/plan46-plan45-immutable.json, /private/tmp/plan46-collector/health-collector.sh, /private/tmp/plan46-collector/health.sql, /private/tmp/plan46-collector/tests/run.sh, /private/tmp/plan46-collector/fixtures/vm-stat-wide.txt, /private/tmp/plan46-collector/fixtures/vm-stat-lines.txt, /private/tmp/plan46-collector/fixtures/psql-valid.txt, /private/tmp/plan46-collector/fixtures/psql-malformed.txt, /private/tmp/plan46-collector/fixtures/json-duplicate.txt, /private/tmp/plan46-collector-verify.once, /private/tmp/plan46-collector-verify.log, /private/tmp/plan46-collector-tests.json</files>
  <behavior>
    - RED: the frozen Plan 45 one-line parser rejects the exact valid `BEGIN`/sentinel/`ROLLBACK` fixture at its line-count assertion.
    - GREEN: the production parser accepts exactly three LF-terminated lines in that order, one tab-delimited sentinel, duplicate-free JSON, and empty stderr at exit zero.
    - Adverse: missing/reordered/duplicate command tags, missing/multiple sentinel, blank/extra/trailing output, CR/NUL, malformed/duplicate-key JSON, stderr warning/error, and nonzero psql status all fail closed.
    - The fixture path and real dry-run path call the same parser function; fixture-only success cannot create a health marker or retain live health values.
  </behavior>
  <precondition>Plan 45 summary/receipts/markers/log/failure absences replay exactly, candidate authority is unchanged, and no `/private/tmp/plan46-*` path exists.</precondition>
  <action>First record `/private/tmp/plan46-plan45-immutable.json`: a duplicate-safe canonical receipt containing a recursive C-sorted `lstat` inventory of every retained `/private/tmp/plan45-*` root and descendant, with path, type, mode, byte size, inode, mtime_ns, and SHA-256 for each regular file, plus the exact Plan 45 summary/receipt/failure facts and strict omitted-field preimage digest. Prove the summary's exact successful and failed facts. Make the namespace read-only for this plan by policy; all later automated gates independently reconstruct the inventory and require exact object and canonical-byte equality. A mismatch or any existing Plan 46 path blocks before action.

Create a fresh untracked candidate-external `/private/tmp/plan46-collector` containing exactly the same eight relative paths as Plan 45, all regular non-symlink files. Copy `health.sql` byte-for-byte: exactly 1,653 bytes/SHA `f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9`; retain its `SIGRA_PLAN45_HEALTH_V1` sentinel/schema because these bytes are the immutable health authority. Preserve full live psql argv exactly as `/opt/homebrew/bin/psql -X --no-psqlrc --set=ON_ERROR_STOP=1 --tuples-only --no-align --file /private/tmp/plan46-collector/health.sql` and the closed admission environment keys `ASDF_ERLANG_VERSION,HOME,LC_ALL,MIX_ENV,PATH,PGDATABASE,PGHOST,PGPASSWORD,PGPORT,PGUSER,TMPDIR`; authenticate complete nonsecret values, password presence/hash/redaction, and configured/current/session user/database equivalence. Do not add `--quiet`, use stdin/`--command`, change SQL, or weaken authentication.

Refactor only the collector parser into one shell-invoked Python region used by both `--parse-fixture` and the real `--dry-run`/`--admission` modes. It reads stdout and stderr as bytes plus the child status. Success requires status zero, stderr exactly empty, no CR or NUL, and stdout exactly `BEGIN\n` + one nonempty sentinel row + `\nROLLBACK\n`, with no blank, prefix, suffix, warning, or fourth line. Split the middle row on exactly one first tab; require sentinel bytes exactly `SIGRA_PLAN45_HEALTH_V1`; parse JSON with duplicate-key rejection. Require exact top-level keys `schema,runtime,snapshot,activity,locks,blockers,allowlist`; exact schema `sigra.plan45.health.v1`; runtime keys/types `database:string,user:string,backend_pid:positive integer`; snapshot keys/types `isolation:string,read_only:boolean,value:string,xmin:string,xmax:string`; activity integer keys `foreign_non_idle,foreign_waiters`; locks integer key `ungranted`; blockers integer key `blockers`; and allowlist integer key `allowlisted`, with no extra nested keys. Serialize the parsed object with `json.dumps(sort_keys=True,separators=(",",":"))` plus one LF and SHA-256 it. The fixture receipt may retain fixture bytes, parsed fixture values, and canonical JSON; the live dry-run receipt retains only raw/payload/canonical hashes, sizes, framing/schema facts, argv/environment hashes, and `admissible:false`, never health metric values. Extraction boundaries are fixed comments `# PLAN46_PSQL_PARSER_BEGIN` and `# PLAN46_PSQL_PARSER_END`; every consumer and receipt independently hashes that exact byte interval, and Task 2 must use the identical bundle manifest/parser SHA.

Build the deterministic fixture/adverse matrix inside the fresh one-shot wrapper. The valid file contains exact real framing and a schema-valid payload. Run the old Plan 45 parser logic against it once as the in-process RED control and require rejection at `len(lines)==1`; this does not invoke a Plan 45 file or external client. Then invoke the actual production `--parse-fixture` path for valid and every listed adverse case. Independently bind case name, exact stdout/stderr base64/hash/size, simulated child status, parser argv/exit, parsed canonical hash if accepted, and zero live observations. Exactly one valid case exits zero; every adverse exits the prescribed nonzero framing/JSON/client class. No aggregate boolean substitutes for reconstructing every row.

The wrapper atomically creates `/private/tmp/plan46-collector-verify.once` with `plan46-collector-verify\n`, writes an ordered append-only log, runs the fixture matrix, then invokes `health-collector.sh --dry-run --sql /private/tmp/plan46-collector/health.sql` exactly once. Capture exact psql argv/environment/status/stdout/stderr before deletion of raw temporaries. Require the real output's exact three-line framing and authenticated canonical JSON, but mark it inadmissible with writes zero, no health marker/receipt, and no retained metric values. Guard-only marker reacquisition must fail EEXIST before script/psql execution. Emit duplicate-safe canonical compact `/private/tmp/plan46-collector-tests.json` with one LF and a `receipt_sha256` computed over the identical object with that field omitted. Any failure blocks permanently with no rerun, repair, admission, timing, or GitHub.</action>
  <verify>
    <automated>python3 -c 'import glob,hashlib,json,os,stat; nodup=lambda p: (_ for _ in ()).throw(ValueError("duplicate key")) if len(p)!=len({k for k,_ in p}) else dict(p); h=json.load(open("/private/tmp/plan46-plan45-immutable.json"),object_pairs_hook=nodup); roots=sorted(glob.glob("/private/tmp/plan45-*"),key=os.fsencode); paths=[]; [(paths.append(q),paths.extend(os.path.join(d,n) for d,ds,fs in os.walk(q,followlinks=False) for n in ds+fs)) if os.path.isdir(q) and not os.path.islink(q) else paths.append(q) for q in roots]; paths=sorted(set(paths),key=os.fsencode); kind=lambda s:"file" if stat.S_ISREG(s.st_mode) else "dir" if stat.S_ISDIR(s.st_mode) else "symlink" if stat.S_ISLNK(s.st_mode) else "other"; rows=[]; [(lambda s,p:rows.append({"path":p,"type":kind(s),"mode":stat.S_IMODE(s.st_mode),"bytes":s.st_size,"inode":s.st_ino,"mtime_ns":s.st_mtime_ns,**({"sha256":hashlib.sha256(open(p,"rb").read()).hexdigest()} if stat.S_ISREG(s.st_mode) else {})}))(os.lstat(p),p) for p in paths]; assert rows==h["paths"] and h["summary"]=={"bytes":11805,"commit":"6a60788dfca75923151b5cd07d98be8e7384b706","sha256":"51682ebe1a21d9bd2f4e9641b04f30db0fd00114eaeee8cb92ae6a312c0679b2"}; b=open("/private/tmp/plan46-plan45-immutable.json","rb").read(); assert b==(json.dumps(h,sort_keys=True,separators=(",",":"))+"\n").encode(); want=h.pop("receipt_sha256"); assert want==hashlib.sha256((json.dumps(h,sort_keys=True,separators=(",",":"))+"\n").encode()).hexdigest()'</automated>
    <automated>python3 -c 'import hashlib,json,os; nodup=lambda p: (_ for _ in ()).throw(ValueError("duplicate key")) if len(p)!=len({k for k,_ in p}) else dict(p); load=lambda p:json.load(open(p),object_pairs_hook=nodup); r=load("/private/tmp/plan46-collector-tests.json"); root="/private/tmp/plan46-collector"; rel=["fixtures/json-duplicate.txt","fixtures/psql-malformed.txt","fixtures/psql-valid.txt","fixtures/vm-stat-lines.txt","fixtures/vm-stat-wide.txt","health-collector.sh","health.sql","tests/run.sh"]; paths=[root+"/"+x for x in sorted(rel,key=os.fsencode)]; actual=sorted((os.path.join(d,f) for d,_,fs in os.walk(root) for f in fs),key=os.fsencode); files=[{"path":p,"bytes":os.path.getsize(p),"sha256":hashlib.sha256(open(p,"rb").read()).hexdigest()} for p in paths]; assert actual==paths and all(os.path.isfile(p) and not os.path.islink(p) for p in paths) and files==r["bundle"]["files"]; sql=open(root+"/health.sql","rb").read(); assert len(sql)==1653 and hashlib.sha256(sql).hexdigest()=="f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9"; src=open(root+"/health-collector.sh","rb").read(); parser=src.split(b"# PLAN46_PSQL_PARSER_BEGIN\n",1)[1].split(b"# PLAN46_PSQL_PARSER_END\n",1)[0]; assert r["parser"]["bytes"]==len(parser) and r["parser"]["sha256"]==hashlib.sha256(parser).hexdigest(); assert r["red_control"]=={"assertion":"len(lines)==1","exit_status":1,"source_sha256":"37ffa87aee4ac7209b532e6d5b24fc91abcead040c3df08e1435c056830f3857"}; cases=r["cases"]; assert [x["name"] for x in cases]==["valid","missing_begin","missing_rollback","reordered_tags","duplicate_begin","duplicate_rollback","missing_sentinel","multiple_sentinel","blank_line","extra_prefix","extra_suffix","trailing_bytes","cr","nul","malformed_json","duplicate_key_json","stderr_warning","stderr_error","nonzero"] and [x["accepted"] for x in cases]==[True]+[False]*18 and all(x["live_observations"]==0 for x in cases); d=r["dry_run"]; assert d["invocation_count"]==1 and d["psql_argv"]==["/opt/homebrew/bin/psql","-X","--no-psqlrc","--set=ON_ERROR_STOP=1","--tuples-only","--no-align","--file",root+"/health.sql"] and d["exit_status"]==0 and d["stderr_bytes"]==0 and d["framing"]==["BEGIN","sentinel_json","ROLLBACK"] and d["sentinel_count"]==1 and d["admissible"] is False and d["writes"]==0 and d["health_values_retained"] is False and d["health_marker_created"] is False and d["health_receipt_created"] is False; env=d["environment"]; allowed=["ASDF_ERLANG_VERSION","HOME","LC_ALL","MIX_ENV","PATH","PGDATABASE","PGHOST","PGPASSWORD","PGPORT","PGUSER","TMPDIR"]; assert env["keys"]==allowed and env["unexpected_keys"]==[] and env["secrets_redacted"] is True and env["password_present"] is True; raw=open("/private/tmp/plan46-collector-verify.once","rb").read(); assert raw==b"plan46-collector-verify\n" and r["wrapper"]["invocation_count"]==1 and r["wrapper"]["reacquire"]=="EEXIST" and r["wrapper"]["psql_count_before_reacquire"]==r["wrapper"]["psql_count_after_reacquire"]==1' &amp;&amp; python3 -c 'import hashlib,json; p="/private/tmp/plan46-collector-tests.json"; pairs=lambda x: (_ for _ in ()).throw(ValueError("duplicate key")) if len(x)!=len({k for k,_ in x}) else dict(x); b=open(p,"rb").read(); d=json.loads(b,object_pairs_hook=pairs); assert b==(json.dumps(d,sort_keys=True,separators=(",",":"))+"\n").encode(); want=d.pop("receipt_sha256"); assert want==hashlib.sha256((json.dumps(d,sort_keys=True,separators=(",",":"))+"\n").encode()).hexdigest()' &amp;&amp; test "$(shasum -a 256 /private/tmp/plan45-collector-verify.once | awk '{print $1}')" = 9c37c03193de251f7e181543e0036ae40ae22d96ce0118820841e86766e8b0d4 &amp;&amp; test "$(shasum -a 256 /private/tmp/plan45-collector-verify.log | awk '{print $1}')" = a1fc3afefa9fb22d2a71346bd9c6cfd1aa4cd375c4b54f2cbc6187aa7bd9fef1 &amp;&amp; test ! -e /private/tmp/plan45-collector-tests.json</automated>
    <fails_when>Plan 45 summary/receipt/failure replay differs; any Plan 45 path, marker, inode, mtime, or byte changes or a Plan 45 command runs; a Plan 46 path preexists; bundle membership/type/order/hash differs; SQL differs from 1,653/f35c; parser is duplicated/bypassed or parser-region authority is absent; RED does not fail at the old assertion; fixture names/order/bytes/status/argv/results are not independently reconstructed; valid framing is not exact or any adverse is accepted; stdout has missing/extra/reordered tags, blank/CR/NUL/extra/trailing bytes, multiple sentinel, malformed/duplicate JSON, or stderr/status contamination; dry-run psql argv/environment/auth differs; wrapper/dry-run count differs from one; marker reacquisition executes psql; receipt is noncanonical/self-referential/duplicate-keyed or retains live values; or admission/timing/GitHub occurs.</fails_when>
  </verify>
  <done>One fresh fixture matrix and one fresh inadmissible dry run prove the same production parser accepts only exact real psql transaction framing, with every Plan 45 byte untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admit one healthy isolation and run one three-pair series (D-02–D-04, D-07, D-09)</name>
  <files>/private/tmp/plan46-admission.once, /private/tmp/plan46-admission-invocation.json, /private/tmp/plan46-preflight-health.json, /private/tmp/plan46-recovery-payloads.txt</files>
  <behavior>
    - The exact tested parser/SQL bundle crosses once from inadmissible dry run to admission without byte drift.
    - Plan 45's complete dynamic protected name+owner set is unchanged; only the fresh Plan 46 candidate database may be outside it.
    - One production runner command produces three complete green pairs or blocks without a second series.
  </behavior>
  <precondition>Task 1 receipt and immutable Plan 45 manifest pass; no Plan 46 admission/isolation/database/health/timing artifact exists.</precondition>
  <action>Create one fresh standalone archive-only candidate worktree from exact `3713dcd4`, with real candidate-local `_build`, private copied locked deps/cache/homes/tmp, a fresh exact-provenance candidate database, unset `MIX_BUILD_PATH`, offline dependencies, compile-only preconditioning, and the full untimed gates. Reject any symlink/external path, copied/prewarmed build, network, source dirt, noncandidate commit/tree, or deviation from 225 exhaustive ordinary paths, Plan 35 weights, 114/111 assignment, and ledger137.

Immediately before admission, independently replay the canonical Plan 45 protected receipt and its 1,675-row name+owner ledger. In one read-only observation require every protected name still exists exactly once with the same owner and zero unauthorized mutation; arbitrary additions remain protected, not cleaned. Require no database outside that set except the exact fresh Plan 46 candidate database whose name/owner/source/worktree provenance is fully bound. Any other delta blocks without disposition.

Rehash all eight collector files, SQL, parser region, Task 1 canonical receipt, psql argv, and closed 11-key environment. Atomically acquire fresh `/private/tmp/plan46-admission.once` containing `plan46-admission\n`; a second acquisition is guard-only EEXIST. Invoke `/private/tmp/plan46-collector/health-collector.sh --admission --sql /private/tmp/plan46-collector/health.sql` exactly once. The same parser must authenticate exact `BEGIN`/single sentinel/`ROLLBACK`, empty stderr, exit zero, duplicate-safe JSON, complete runtime/snapshot/activity/locks/blockers/allowlist schema, and canonical payload hash. Persist a canonical invocation receipt binding pre/post bundle manifests, marker stat, exact argv/environment, raw hashes, and exit. Persist one canonical health receipt with sample_count one and strict preimage digest. Require load1 <= 18, correct CPU count, normal memory with no swap/pageout growth, at least 10 GiB free on every relevant volume, zero competing candidate process, database probes <= 1,000 ms, zero foreign non-idle/waiting/blocking/ungranted-lock state, allowlist count zero, protected name+owner digest unchanged, and only the candidate DB addition. No resample, wait, cleanup, repair, or alternate observation.

With no intervening work invoke exact `bash scripts/ci/library-partitions.test.sh` once. This is the only production timing command and authorizes exactly three pairs/nine payloads. Require six green children, exhaustive/disjoint 225 paths, unchanged weights and 114/111 assignment, truthful candidate-contained receipts, and ratio <= 2 in every pair. Write `/private/tmp/plan46-recovery-payloads.txt` as exactly nine lowercase64 lines, 585 bytes, C-sorted unique, one final LF, with zero intersection against exact ledger137; bind each hash to its pair/partition command and receipt. A failure blocks permanently: no second series, retry, sample, cleanup, tuning, recalibration, source/candidate change, or GitHub.</action>
  <verify>
    <automated>python3 -c 'import hashlib,json,os; nodup=lambda p: (_ for _ in ()).throw(ValueError("duplicate key")) if len(p)!=len({k for k,_ in p}) else dict(p); load=lambda p:json.load(open(p),object_pairs_hook=nodup); t=load("/private/tmp/plan46-collector-tests.json"); i=load("/private/tmp/plan46-admission-invocation.json"); h=load("/private/tmp/plan46-preflight-health.json"); p=load("/private/tmp/plan45-protected-databases.json"); assert i["pre_files"]==i["post_files"]==t["bundle"]["files"] and i["manifest_sha256"]==h["collector_bundle"]["manifest_sha256"]==t["bundle"]["manifest_sha256"]; assert i["parser"]==h["parser"]==t["parser"] and i["sql_sha256"]==h["sql_sha256"]=="f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9"; assert i["argv"]==["/private/tmp/plan46-collector/health-collector.sh","--admission","--sql","/private/tmp/plan46-collector/health.sql"] and i["invocation_count"]==1 and i["exit_status"]==0 and i["stderr_bytes"]==0 and i["framing"]==["BEGIN","sentinel_json","ROLLBACK"] and i["sentinel_count"]==1; assert h["sample_count"]==1 and h["admission"]["overall"] is True and h["snapshot"]["isolation"]=="repeatable read" and h["snapshot"]["read_only"] is True and h["protected"]["count"]==1675 and h["protected"]["sha256"]=="97f14d8d3805e79f92b41aa2b0f56955a88144e14ad5b48adb83a1f7875d1c72" and h["protected"]["mutations"]==0 and h["protected"]["rows"]==p["rows"]; assert h["load1"]&lt;=18 and h["disk_free_min_bytes"]&gt;=10737418240 and h["database_probe_max_ms"]&lt;=1000 and h["competing_candidate_processes"]==h["foreign_non_idle"]==h["foreign_waiters"]==h["blockers"]==h["ungranted_locks"]==h["allowlisted_databases"]==0; marker=open("/private/tmp/plan46-admission.once","rb").read(); assert marker==b"plan46-admission\n" and i["marker"]["reacquire"]=="EEXIST" and i["marker"]["invocation_count_before"]==i["marker"]["invocation_count_after"]==1' &amp;&amp; for f in /private/tmp/plan46-admission-invocation.json /private/tmp/plan46-preflight-health.json; do python3 -c 'import hashlib,json,sys; p=sys.argv[1]; pairs=lambda x: (_ for _ in ()).throw(ValueError("duplicate key")) if len(x)!=len({k for k,_ in x}) else dict(x); b=open(p,"rb").read(); d=json.loads(b,object_pairs_hook=pairs); assert b==(json.dumps(d,sort_keys=True,separators=(",",":"))+"\n").encode(); want=d.pop("receipt_sha256"); assert want==hashlib.sha256((json.dumps(d,sort_keys=True,separators=(",",":"))+"\n").encode()).hexdigest()' "$f" || exit 1; done &amp;&amp; awk 'length($0) != 64 || $0 !~ /^[0-9a-f]+$/ { bad=1 } END { exit bad }' /private/tmp/plan46-recovery-payloads.txt &amp;&amp; test "$(wc -l &lt; /private/tmp/plan46-recovery-payloads.txt | tr -d ' ')" = 9 &amp;&amp; test "$(wc -c &lt; /private/tmp/plan46-recovery-payloads.txt | tr -d ' ')" = 585 &amp;&amp; test "$(tail -c 1 /private/tmp/plan46-recovery-payloads.txt | od -An -tuC | tr -d ' ')" = 10 &amp;&amp; cmp -s /private/tmp/plan46-recovery-payloads.txt &lt;(LC_ALL=C sort -u /private/tmp/plan46-recovery-payloads.txt) &amp;&amp; test "$(LC_ALL=C comm -12 /private/tmp/plan39-prior-payloads-137.txt /private/tmp/plan46-recovery-payloads.txt | wc -l | tr -d ' ')" = 0</automated>
    <fails_when>Task 1 or Plan 45 authority drifts; isolation is reused/noncandidate/external/prewarmed/networked/dirty; protected names/owners change or any delta other than the exact candidate DB exists; collector/SQL/parser/argv/environment/auth bytes differ; admission marker or invocation/sample count differs from one; framing/schema/canonical/preimage checks fail; health is incomplete or unhealthy; runner count differs from one; any pair/path/payload/ratio/containment/disjointness proof fails; or any retry/resample/wait/cleanup/tuning/recalibration/source/candidate/GitHub action occurs.</fails_when>
  </verify>
  <done>One fresh admitted health sample authorizes one passing three-pair series and nine payloads disjoint from ledger137, with protected and frozen authority unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Capture four fresh live runs and validate evidence (D-01, D-05–D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <precondition>Task 2's sole health sample and all three pairs pass; PR #234 is open against `main`, `refs/heads/ci/phase-235-1-evidence` remains exact `87338db7`, candidate is unpushed, and every prior run ID is reconstructed from Plans 35–45.</precondition>
  <action>Execute the frozen strict live contract. Recheck repo `szTheory/sigra`, PR #234, base `main`, exact head ref, remote SHA, and negative control proving `ci/phase-235-16-source-complete` is irrelevant. Fast-forward push exact candidate once to `ci/phase-235-1-evidence`; dispatch exact-six once at candidate SHA. Pair only the new automatic PR and dispatch original-attempt-1 successful selected jobs. Commit exactly the two capture receipts as the direct candidate child, with no other bytes, push that evidence commit once to the same PR ref, dispatch exact-six once at the evidence SHA, and pair only the new automatic PR and dispatch original-attempt-1 successes. The four run IDs and four selected job IDs are mutually disjoint and outside the complete Plans 35–45 historical sets; Plan 45 contributed zero IDs. Validate exact SHA/event/workflow/job/attempt/timestamps/artifact hashes and candidate-to-evidence lineage.

Before each long watch inspect `gh api rate_limit`; stop at core <=250 or HTTP 403/429 until the reported reset/retry time. Use one watcher per run, exactly `gh run watch &lt;run-id&gt; --repo szTheory/sigra --compact --interval 60 --exit-status`; after completion fetch one structured summary, and fetch failed logs only on failure. Do not poll a run from another agent, reuse/retry/substitute a run, waive failure, amend/rebase/force-push, add a third push/dispatch, or run any additional local timing command. Any failure blocks. Plan 05 alone performs later docs reconciliation.</action>
  <verify>
    <automated>bash scripts/ci/verify-library-routing-evidence.test.sh &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.test.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; env -u MIX_BUILD_PATH ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Repo/PR/ref/base/candidate/evidence lineage or exact diff fails; wrong ref advances; push/dispatch counts differ from two each; a run is reused/retried/nonautomatic/unsuccessful/wrong SHA/event/job/attempt; any run/job ID overlaps Plans 35–45 or another current lane; artifact/provenance/watcher/rate rules fail; or extra timing/retry/waiver/substitution/reconciliation occurs.</fails_when>
  </verify>
  <done>Four new mutually disjoint original-attempt-1 successes prove candidate and evidence commits on both routes, or the plan blocks without claiming evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Plan 45 history to Plan 46 | Accepted snapshot authority is read-only; consumed collector evidence is inadmissible. |
| Fixture to real psql | Synthetic framing cannot authorize admission; the sole dry run must prove the same parser with real bytes. |
| Dry run to admission | Only an immutable tested bundle and a fresh marker may cross. |
| Protected database state | Every prior protected name/owner remains untouched; only the exact candidate DB may be added. |
| Local to GitHub | Only exact candidate/evidence commits and fresh attempt-1 runs cross. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-46-01 | Tampering | psql framing | high | mitigate | Byte-exact three-line grammar, unique sentinel, duplicate-safe canonical JSON, stderr/status rejection, adverse fixtures. |
| T-235.1-46-02 | Repudiation | one-shot budgets | high | mitigate | Fresh exclusive markers, exact invocation logs, immutable Plan 45 namespace, fail-closed receipts. |
| T-235.1-46-03 | Elevation of Privilege | database state | critical | mitigate | RR/RO/ROLLBACK SQL, complete protected ledger equality, zero mutation, exact sole candidate delta. |
| T-235.1-46-04 | Spoofing | GitHub evidence | high | mitigate | Exact PR/ref/SHA lineage and four fresh disjoint original-attempt-1 IDs. |
| T-235.1-46-SC | Tampering | packages | low | accept | No dependency install or tracked implementation change; locked offline copies only. |
</threat_model>

## Multi-Source Coverage Audit
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Exact ordinary and exact-six routes | 46 | COVERED | Framing repair then bounded local/live evidence. |
| REQ | TEST-01 | PR timing evidence | 46 | COVERED | Fresh candidate/evidence PR captures. |
| REQ | TEST-02 | Comparable partitions | 46 | COVERED | Frozen calibration/assignment and one series. |
| REQ | TEST-03 | Exact-six receiver | 46 | COVERED | Fresh dispatch capture and validation. |
| RESEARCH | — | No retry, tuning, or outcome selection | 46 | COVERED | Fresh one-shot markers and fixed budgets. |
| CONTEXT | D-01–D-09 | Preserve routing, order, and history | 46, 05 | COVERED | Plan 45 remains immutable negative history; Plan 05 remains docs-only downstream. |

<verification>Replay Plan 45 byte authority and zero downstream action; prove exact three-line psql framing with the production parser; run one dry run, one health admission, one three-pair series, and four fresh live routes.</verification>
<success_criteria>- Every Plan 45 path and marker remains byte/stat identical and no Plan 45 command reruns.
- Exact `BEGIN`/one sentinel JSON/`ROLLBACK` framing passes; every contamination case fails.
- Exactly one dry run is inadmissible and exactly one healthy admission is authoritative.
- Protected names/owners and frozen source/calibration/assignment/candidate remain unchanged.
- Exactly one timing command produces three passing pairs and nine disjoint payloads.
- Four fresh original-attempt-1 runs validate both candidate and evidence commits.</success_criteria>
<output>Create `235.1-46-SUMMARY.md` with Plan 45 immutability replay, parser/SQL/bundle authorities, exact fixture/dry-run/admission/timing budgets, protected-state proof, and live provenance.</output>
