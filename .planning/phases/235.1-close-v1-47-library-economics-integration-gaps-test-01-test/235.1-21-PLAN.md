---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 21
type: execute
wave: 17
depends_on: [235.1-19]
files_modified:
  - test/support/ci/phase_235_1_evidence_state_contract.exs
  - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
  - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
  - test/support/ci/library_test_partitions_test.exs
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03, DX-01, DX-02, DX-03, DX-04, DX-06]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All three ordinary transition-contract tests are finalized before calibration and accept exactly two evidence-derived states without hardcoding future IDs."
    - "Absent validation evidence, including intermediate capture-only state, preserves the exact current gaps/stale prose; only a fully valid cross-linked capture-plus-validation set permits Complete/passed reconciliation."
    - "No contract assigns either global-Oban test path to a fixed ordinary partition; measured LPT may validly place either path in either partition."
    - "Both global-Oban registrant modules remain async false and identity-safe, and both paths occur exactly once in the exhaustive disjoint ordinary universe."
    - "Plan 19 remains blocked history with its exact three diagnostic triples, restored v1 artifact, absent v2 manifest, zero admitted samples, zero validations, and zero GitHub actions."
    - "A new frozen source snapshot produces exactly three wholly new source triples, one replayable v2 artifact/manifest, and exactly three later independent validation pairs at the unchanged factor-of-two bound."
  artifacts:
    - path: test/support/ci/phase_235_1_evidence_state_contract.exs
      provides: shared pure two-state receipt and dynamic-fact validator required by all three ordinary transition tests
    - path: test/sigra/planning/phase_198_contributor_dx_contract_test.exs
      provides: frozen pre/post contributor and DX document-transition contract
    - path: test/sigra/planning/phase_235_1_library_economics_contract_test.exs
      provides: central receipt-state validator, assignment-agnostic Oban safety, and immutable Plan 19 blocked-history contract
    - path: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      provides: frozen pre/post Phase 235 body and protected-value transition contract
    - path: test/support/ci/library_test_partitions_test.exs
      provides: adversarial cost-injection proof that either Oban path may occupy either partition without losing topology or ratio guarantees
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json
      provides: three-sample v2 calibration bound to the new frozen source
    - path: .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json
      provides: external artifact/source-index binding for Plan 20
  key_links:
    - from: PR, scaffold, and validation receipts
      to: docs, requirements, audit, and Phase 235 body state
      via: exact-schema dynamic cross-link validator with pre/intermediate/post state machine
      pattern: "VALIDATION-RUN|Complete|11/11"
    - from: injected positive cost maps
      to: ordinary partition membership
      via: LibraryTestPartitions.build_partitions!/1 and validate_current_universe!/2
      pattern: "costs|ordinary_paths"
    - from: Plan 21 source snapshot
      to: fresh calibration
      via: per-path source bytes plus three new combined/timing triples
      pattern: "source_snapshot|ordinary_source_index_sha256"
---

<objective>
Freeze the evidence-to-prose lifecycle and remove stale partition-number ownership before rebuilding source-bound calibration from a new source snapshot.

Purpose: Plan 19 correctly changed measured placement, but its post-build contract still treated an old partition number as behavior; Plan 05 also intended to edit calibrated ordinary tests after evidence. Plan 21 makes both transitions stable before timing so deterministic LPT may follow current costs and later receipt/prose changes cannot invalidate calibration.
Output: One shared future-value receipt-state helper, three frozen ordinary document adapters, assignment-agnostic mutations, a fresh replayable v2 calibration/manifest, and three new passing local validation pairs—or a durable local block with no live action.

Decision coverage: D-01 preserves the sole PR owner; D-02/D-03/D-04 preserve two exhaustive, disjoint, comparable ordinary partitions and same-run receipts; D-07 forbids completion without fresh proof; D-08 freezes the later reconciliation gate; D-09 preserves every blocked result and protected fact.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PATTERNS.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-19-SUMMARY.md
@test/support/ci/library_test_partitions.exs
@test/support/ci/library_test_partitions_test.exs
@test/support/ci/phase_235_1_evidence_state_contract.exs
@test/sigra/planning/phase_198_contributor_dx_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
@test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
@test/sigra/account/deletion_test.exs
@test/sigra/delivery_test.exs
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/v1.47-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Freeze an evidence-derived two-state reconciliation contract (D-07–D-09)</name>
  <files>test/support/ci/phase_235_1_evidence_state_contract.exs, test/sigra/planning/phase_198_contributor_dx_contract_test.exs, test/sigra/planning/phase_235_1_library_economics_contract_test.exs, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <behavior>
    - With no `235.1-VALIDATION-RUN.json`, zero or any valid subset of capture receipts remains pre-evidence: every present capture receipt is strictly validated, current gap/stale document values are exact, and no TEST/DX Complete or Phase 235 reconciled-body claim is accepted.
    - Post-evidence requires valid PR, scaffold, and validation receipts together; exact schemas/types/keys, attempt-1/event/conclusion, SHA relationships, distinct run IDs, job identities/conclusions, artifact identities/digests, receipt digests, ten-ledger equality, and validation-to-capture links are derived from receipt content rather than future literals.
    - In post-evidence state, CONTRIBUTING, REQUIREMENTS, ROADMAP, Phase 234/235 verification, Phase 235.1 validation, and the milestone audit must cite the dynamically derived receipt values and exact Complete/passed/11-of-11 states while retaining protected FAST-01/GATE-05 facts.
    - Generated fixtures prove missing receipts/keys, malformed schemas, reused IDs, cross-SHA pairs, wrong jobs/artifacts/digests, premature Complete prose, and incorrect/missing post-evidence citations fail.
  </behavior>
  <action>RED: add pure validation helpers and generated mutation fixtures before changing any document. Create `test/support/ci/phase_235_1_evidence_state_contract.exs` with public pure module `Sigra.Planning.Phase2351EvidenceStateContract`; have all three ordinary test files `Code.require_file/2` that exact support file and alias the helper, so each module remains independently runnable while all three exercise one semantic contract without loading or duplicating another ExUnit module. The state discriminator is structural: validation receipt absent means pre-evidence even when one or both capture receipts exist; validation present requires both capture receipts and a fully valid post-evidence triple. Do not key state on a future run ID, SHA, job ID, artifact ID, digest, timestamp, or prose token.

Define closed validators for `sigra.library-partitions-evidence/v1`, `sigra.library-install-golden-evidence/v1`, and `sigra.library-routing-validation/v1`. Validate exact top/nested key sets and types; attempt 1, retry-free/original-attempt admission, correct PR versus workflow_dispatch events, selected job names and success conclusions, artifact cardinalities/names/IDs/SHA-256s, receipt byte digests, candidate/evidence SHA relationships, pairwise run-ID distinctness, validation run IDs disjoint from capture and ten-ledger IDs, byte-identical ordered ten-run arrays/digest, and validation links to both capture receipt digests. Read future numeric/string values from the validated maps and return a normalized facts map for prose checks; constants are limited to schemas, job/artifact contract names, protected history, and required statuses. Tag pure receipt/state/mutation tests `:receipt_contract` and actual repository-prose assertions `:document_transition`, so Plan 20 can validate a newly written validation receipt before Plan 05 changes prose without weakening the default full contract.

Construct synthetic valid receipts with generated unique positive IDs, 40-hex SHAs, 64-hex digests, artifact values, partition durations/ratio, and six receiver paths. Mutate every required field and relationship: absent receipt/key, extra key, wrong type/schema/event/attempt/conclusion/job/artifact/digest, reused run ID, capture cross-SHA, validation cross-SHA, capture/validation overlap, ledger order/member/digest drift, and receipt-link drift. Build pre/intermediate/post document fixtures from generated facts and mutate premature Complete/passed/11-of-11, retained stale/gap prose after post evidence, missing or incorrect run/job/artifact/digest/duration/ratio citations, and FAST-01/GATE-05 protected values. Require focused RED failure and commit as `test(235.1-21): define evidence-driven reconciliation states`.

GREEN: wire all three ordinary tests to the stable helpers. Pre-evidence must pin the repository's exact current state: all eight TEST-01/02/03 and DX-01/02/03/04/06 REQUIREMENTS traceability rows are `Gaps Found`; the audit is `status: gaps_found`, scores `21/24`, `6/6`, `7/10`, `5/6`, TEST-01/02/03 are unsatisfied, and the library flow is broken; Phase 235 frontmatter remains `passed`/`11/11` while its body remains exactly `human_needed`, `11/11 truths verified (0 present, behavior-unverified)`, and the disclosed judgment-only conclusion; Phase 235.1 validation remains non-green; and no document claims absent validation proof. If valid capture receipts are present without validation, validate each present receipt fully but require the same pre-evidence prose; capture alone cannot authorize green. Post-evidence uses the shared normalized facts but divides document ownership so Plan 05 stays executable task-by-task: Phase 198 asserts CONTRIBUTING/REQUIREMENTS/ROADMAP; Phase 235 terminal asserts Phase 234 route evolution plus the Phase 235 body and protected FAST-01/GATE-05 values/disclosures; Phase 235.1 asserts Phase 235.1 validation, milestone audit, cross-document final scores, and the complete receipt chain. Collectively require all Plan 05 target documents to cite normalized values and exact TEST-01/02/03 plus DX-01/02/03/04/06 `Complete`, Phase 235 `passed` and `11/11`, and explicit Phase 234 route evolution. Keep receipt paths optional only for the pre/intermediate state; a malformed present receipt always fails. Run all three focused modules green in current pre-evidence state and commit as `test(235.1-21): freeze pre and post evidence contracts`.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any module fails or reports zero tests; a future identity is hardcoded; malformed/intermediate receipts authorize green; valid post evidence cannot derive citations; missing/reused/cross-SHA/job/artifact/digest/prose mutations pass; or protected facts change.</fails_when>
  </verify>
  <done>All three ordinary transition tests are final, future-value-capable, strict in both states, and prove that capture-only evidence validates but cannot reconcile documents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Oban safety invariant under valid measured placement (D-02–D-04, D-09)</name>
  <files>test/sigra/planning/phase_235_1_library_economics_contract_test.exs, test/support/ci/library_test_partitions_test.exs</files>
  <behavior>
    - The two and only two global Oban registrants are `test/sigra/account/deletion_test.exs` and `test/sigra/delivery_test.exs`; each module remains `async: false` and satisfies the existing identity-safe local-monitor cleanup mutations.
    - Each registrant path occurs exactly once across the two non-empty, exhaustive, disjoint ordinary partitions, with no required partition number or co-partition relationship.
    - Two deterministic positive cost fixtures move each Oban path across partition 1 and partition 2 while preserving the exact ordinary set, disjointness, positive totals, unchanged two-partition topology, and ratio at or below 2.0.
    - Any fixed membership assertion tying either path to partition 1/2, any duplicate/omission, async regression, unsafe cleanup, third registrant, or weakened ratio/topology assertion fails.
  </behavior>
  <action>RED: extend the now-stable Phase 235.1 contract with an assignment-agnostic expectation that fails against the two literal `partitions[2].paths` assertions at lines 295–296. Preserve Task 1's state machine, the AST-based registrant discovery, and every async/identity-safe cleanup mutation. Assert the two exact registrant paths each have occurrence count one in `partitions[1].paths ++ partitions[2].paths`, the union equals `current_ordinary_paths!/0`, the sets are disjoint/non-empty, and neither scaffold membership nor PR owner topology changes. Run the focused command, require the intended assertion failure, and commit only the failing tests as `test(235.1-21): expose fixed partition ownership`.

Add a table-driven cost-injection adversary in `library_test_partitions_test.exs`. Supply exact positive costs and the matching `ordinary_paths` to `build_partitions!/1` for multiple balanced fixtures such that each Oban path is observed in partition 1 in one fixture and partition 2 in another. For every fixture call the real validation path, assert exact-set/disjointness/non-empty/positive totals, calculate `max(total_us)/min(total_us)`, and require it is at most 2.0. Mutate the phase contract in-memory to reintroduce each fixed partition-number membership or co-partition assumption and require the source-policy helper to reject it; do not forbid numeric partition assertions needed for the two-partition runner itself.

GREEN: delete only the fixed delivery/deletion placement and co-partition ownership assumptions. Audit all tracked Phase 235.1 partition tests/contracts for an ordinary test pathname coupled to literal partition membership; convert every such ownership assertion to exact-once/exhaustive invariants or document why it is runner topology rather than measured assignment. Do not change `LibraryTestPartitions.assign!/1`, costs, the current artifact, either Oban module, CI workflow, runner, receipt schema, owner topology, or the `<= 2.0` bound. Run the focused suite green and commit the contract implementation as `fix(235.1-21): make measured placement assignment agnostic`.

Pin Plan 19 summary commit `d405755f078be8eefc8be1abcc2c387a4b6c8589` as blocked history in the phase contract: frozen source `75a4798faf50c29af5d849ff0ba2ee8a83f4e5ed`/tree `02384b6410c371959dfeb976d29dd1e61acc3e4a`, 225 paths, source-index digest `39f26999db7160124dd61a65427155a7f2e78f60c50f3f42e7af211a9ee6ed1c`, restored v1 artifact digest `975612d7f3cbfda75fb6857791ebfd9bcadd852451b86a6b917871b3ba092eeb`, absent v2 manifest, and zero admitted samples/validations/GitHub actions. Pin all three diagnostic triples exactly: durations `3,174/60,072` milliseconds with combined `25,178`/`173786d4150e730598f89e65f4cbac960ee8bb3c3081f25f6da44e0ecce20fb0`, timing 1 `273,229`/`8632d5825bcaf1ccfde79cd7907f2c124d4036a27648b81cc7ed07d5e8244934`, timing 2 `425,980`/`e7570371f8475af78eb111007c25b699675dbcb72cf3a897a4c77f4dc03da23d`; durations `3,995/41,711` with combined `25,178`/`31764c67e85ed0b091c27053d0c3035976ed035e036beb22e30e8b3796b2d41e`, timing 1 `273,257`/`3f432a2f84144d04194309eb5c4b5fda9e17356081c9fef4335ca916397ae29a`, timing 2 `425,963`/`c036ba202bb8840258e6525aea0b5e9dc5ebbe657f2afec0955f7b00ceaa8993`; and durations `3,351/42,354` with combined `25,178`/`ee8bd47263266a3765e721a8bef56fbc7e15bc71580759e7b1ffaec8533df957`, timing 1 `273,247`/`55487aac6fa03c3319d26b9131b3c7eb6bce80ed324038a177ef4bd45e225274`, timing 2 `426,048`/`df205cecf2fa21bc423e098f79419b8d209869c1771d6f58443eaa74b1203932`. Also pin attempted artifact `2,959,930`/`2d45b1db1ae2ffd614dc0c5b3692748504583ef4465545ee614117ea5e2b8a30` and manifest `561`/`68ef4aa43c93cca6e70b2769adf6fc9f4b09bbd1dd57f4531cc3e89cb06c7028`; mutation of any duration, byte count, hash, zero-admission fact, or promotion of those payloads must fail.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs &amp;&amp; mix format --check-formatted</automated>
    <fails_when>ExUnit exits nonzero or reports zero tests; either Oban path is missing/duplicated or tied to a fixed partition; a cost fixture cannot place each path on both sides while staying exhaustive/disjoint and within 2.0; cleanup/async/topology weakens; or any Plan 19 blocked fact mutates.</fails_when>
  </verify>
  <done>Oban ownership remains fully safe and exactly covered, measured assignment is partition-number agnostic under adversarial costs, every other pathname-to-partition assumption is removed or proven topology-only, and Plan 19 is immutable blocked history.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze all transition sources and rebuild calibration from zero (D-02–D-04, D-07, D-09)</name>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PARTITION-CALIBRATION-MANIFEST.json</files>
  <behavior>
    - The source snapshot is taken only after Tasks 1–2 and all three Plan 05 transition tests are final; every ordinary path is byte-bound to that commit/tree.
    - Exactly three wholly new successful collection triples produce exactly nine newly retained payloads, none reused from Plan 19.
    - Independent replay derives each path's `max(1,sum(time_us))` per sample and median across three, with exact manifest/artifact/source-index linkage.
    - Exactly three later fresh validation pairs independently pass both child statuses, receipt survival, exhaustive/disjoint coverage, and raw ratio at or below 2.0 without retry or tuning.
  </behavior>
  <action>First run all three frozen transition modules from Task 1, the assignment suite from Task 2, both partition shell adverse suites, Phase 233 discovery contract, protected verifiers, and formatting. Require a clean worktree for every source path and designate Task 2's final GREEN commit as the new frozen source snapshot; record its commit/tree rather than creating an empty or mixed calibration commit. Explicitly hash and include `phase_198_contributor_dx_contract_test.exs`, `phase_235_1_library_economics_contract_test.exs`, and `phase_235_terminal_ratification_contract_test.exs` in the ordinary source index, and pin the shared helper's blob/SHA-256 in the frozen Phase 235.1 contract and Plan 21 summary. No later plan may edit any of those four files without invalidating the frozen transition contract. No calibration artifact, manifest, payload, or validation result may be part of this source snapshot. Recompute the lexical exact 225-path ordinary universe, every path/source-file byte row, helper binding, and source-index digest against this commit; any count, path, helper, or source-byte mismatch blocks.

The tracked state starts with the restored v1 artifact SHA-256 `975612d7f3cbfda75fb6857791ebfd9bcadd852451b86a6b917871b3ba092eeb` and no v2 manifest. Use the existing Plan 19 v2 loader bootstrap procedure only after the new source commit to create an uncommitted default-path assignment bootstrap. It may use the old placement solely to run exhaustive collection; no Plan 19 diagnostic receipt, duration, payload, derived cost, attempted artifact/manifest byte, or hash may enter the new calibration. Allocate a new private `mktemp -d` collection root and assert every retained payload digest differs from the nine forbidden Plan 19 digests.

With Bash major version at least 5 (`/opt/homebrew/bin` may be prepended on macOS without tracked changes), execute exactly three fresh `ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test bash scripts/ci/library-partitions.sh` collections from the unchanged snapshot. Each collection must have both successful children, surviving combined plus two timing receipts, exhaustive/disjoint exact source universe, and exact metadata; copy all three payloads before overwrite. First failure restores the v1 artifact, removes the bootstrap manifest, preserves diagnostics, and blocks with no retry or partial admission.

Build the v2 artifact from only those nine payloads. Independently decode each timing receipt, verify exact keys/types/counts/paths/source bytes, sum all rows per file per sample, apply `max(1,sum)`, take the median across three, and compare every derived row. Emit the external exact-schema manifest with new source commit/tree, artifact path/byte count/SHA-256, source-index digest, sample count 3, and payload count 9. Run the focused Phase 233/235.1/loader suite, artifact/manifest/source mutation suite, partition shell suites, protected verifiers, and format before committing exactly the artifact plus manifest as the final Plan 21 source/evidence-preflight commit.

From that unchanged final commit run exactly three later fresh `library-partitions.sh` plus independent verifier pairs. For each record partition milliseconds, ratio, child conclusions, receipt digests, and exact-set/disjointness; each ratio must independently be `<= 2.0`. No collection or validation may be retried, averaged, tuned, substituted, or reused. Perform no push, dispatch, CI watch/poll, artifact download, or evidence-receipt write. The Plan 21 summary must record source and final commit/tree OIDs, manifest/artifact/source-index identities, all nine new collection payload identities, and all three validation results for Plan 20.</action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/support/ci/library_test_partitions_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs test/sigra/planning/phase_235_1_library_economics_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/library-partitions.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.test.sh &amp;&amp; bash scripts/ci/verify-library-partitions.sh &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; mix format --check-formatted</automated>
    <fails_when>Any command fails or reports zero tests; source changes after freeze; a Plan 19 payload/value is reused; collection is not exactly three new complete triples/nine payloads; replay or source/manifest/artifact linkage differs; any of exactly three later pairs is missing/red/above 2.0; or a retry/tune/live action occurs.</fails_when>
  </verify>
  <done>A new finalized source snapshot includes all three frozen transition tests, has one independently replayable v2 artifact/manifest from exactly three wholly new triples, and exactly three later fresh validation pairs pass the unchanged factor-of-two bound with no retry, tuning, reuse, GitHub action, or downstream ordinary-test edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical samples to new calibration | Diagnostic Plan 19 payloads must never become admissible Plan 21 inputs. |
| Measured costs to partition membership | Valid LPT output may change membership without changing safety or coverage. |
| Frozen source to generated artifact | Every calibrated source byte must match the recorded snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-21-01 | Tampering | assignment contract | high | mitigate | Cost-injection mutations prove both placements while exact-set/disjointness/topology/ratio remain mandatory. |
| T-235.1-21-02 | Spoofing | calibration provenance | high | mitigate | New private payloads, denylisted Plan 19 hashes, source-byte binding, and independent replay. |
| T-235.1-21-03 | Repudiation | blocked history | high | mitigate | Pin summary commit, all diagnostic tuples, restored state, and zero-admission/live facts. |
| T-235.1-21-04 | Elevation of Privilege | premature live route | high | mitigate | Local-only plan; exactly three post-build validations gate Plan 20. |
| T-235.1-21-05 | Elevation of Privilege | evidence-to-prose transition | high | mitigate | Strict two-state helper, dynamic cross-links, capture-only non-admission, and missing/reuse/cross-SHA/prose mutations. |
| T-235.1-21-SC | Tampering | package supply chain | low | accept | No package or dependency change occurs. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Comparable exhaustive ordinary partitions | 21 | COVERED | Assignment-agnostic contracts plus fresh calibration. |
| REQ | TEST-01 | Two ordinary partitions in sole owner | 21 | COVERED | Topology and exact universe unchanged. |
| REQ | TEST-02 | Independently comparable partitions | 21 | COVERED | Fresh three-sample replay and three validations. |
| REQ | TEST-03 | Separate scaffold evidence transition | 21 | COVERED | Frozen dynamic scaffold receipt validator; live evidence remains Plan 20. |
| REQ | DX-01/02/03/04/06 | Evidence-gated reconciliation | 21 | COVERED | Frozen pre/post adapters; document completion remains Plan 05. |
| RESEARCH | — | Source-bound v2 calibration | 21 | COVERED | Existing loader/bootstrap, wholly new samples. |
| CONTEXT | D-02, D-03, D-04 | Exhaustive/disjoint/comparable receipts | 21 | COVERED | Exact-set and ratio mutations preserved. |
| CONTEXT | D-07, D-09 | Evidence-first and immutable history | 21 | COVERED | No live action; Plan 19 pinned blocked. |
| CONTEXT | D-08 | Reconcile only after complete evidence | 21 | COVERED | Frozen pre/post adapters gate Plan 05 without later test edits. |

<verification>
Run the assignment-agnostic Oban mutations before freezing source; then admit only a newly replayed 3/9 calibration and three distinct post-build `<= 2.0` pairs.
</verification>

<success_criteria>
- Both Oban registrant paths remain exact-once, async false, identity-safe, and free to occupy either measured partition.
- All three Plan 05 transition tests and their shared helper are frozen before sampling and derive future evidence values dynamically.
- Capture-only intermediate state validates present receipts but keeps current gap/stale prose; only the full valid triple permits post-evidence Complete/passed prose.
- No tracked contract treats an ordinary pathname's partition number as stable behavior.
- Plan 19's three diagnostic triples and zero-admission/no-live result remain exact blocked history.
- Plan 21 owns a new source snapshot containing all transition contracts, new v2 artifact/manifest, exactly three new collection triples, and exactly three later passing validations.
- No ratio, exhaustive-set, disjointness, receipt, PR-owner, or protected-history requirement is weakened.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-21-SUMMARY.md` with lifecycle RED/GREEN commits, shared-helper and all three adapter hashes, assignment mutation results, Plan 19 pinned history, new source/final commit/tree identities, 3/9 payload identities, three validation pairs, and explicit zero-GitHub status.
</output>
