---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 73
type: execute
gap_closure: true
wave: 72
depends_on: [235.1-12]
files_modified:
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json
  - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Plans 69, 71, and 72 and their private roots remain immutable failed history; their partial structural/timing proofs cannot authorize retry, root reuse, live continuation, or TEST completion."
    - "Before allocation, both pinned Python runtimes prove that canonical scripts/ci/install-golden.sh—not raw mix test—is the sole exact-six entrypoint, fixes SIGRA_INSTALL_GOLDEN_PREPARED=1, owns the exact ordered unique receiver set, and durably captures both child streams before outcome assertions."
    - "Before allocation, an independently verified prerequisite receipt proves pinned Erlang/Elixir selection, phx_new 1.8.8 task availability, repository dependencies, and PostgreSQL readiness; any required archive setup is isolated, bounded, fully captured, and completed before the one-shot evidence root exists."
    - "The zero-effect gate retains Plan 72's path-keyed health-SQL authority and Plan 71's parsed accepted-sentinel/exact-23-hostile SQL policy without regression."
    - "Only after both pre-root receipts pass may one distinct Plan 73 root execute one local chain, canonical exact-six wrapper, automatic-PR/dispatch attempt-1 capture pair, and distinct attempt-1 validation pair."
    - "The PR owner still runs two sequential exhaustive ordinary partitions with parallel ExUnit timing, schedule/workflow_dispatch remains the fail-closed exact-six receiver, and TEST-01/02/03 remain incomplete until Plan 73 evidence passes."
    - "FAST-01 source-complete n=52/p50=469, its miss history, and all 93 GATE-05 rows/disclosures remain unchanged."
  artifacts:
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json"
      provides: "Canonical dual-runtime exact-six entrypoint/mutation proof plus inherited health-SQL and SQL-policy authority with zero forbidden effects"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json"
      provides: "Durable pre-root archive/runtime/dependency/PostgreSQL readiness and optional bounded setup accounting"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json"
      provides: "Fresh attempt-1 automatic-PR two-partition capture"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json"
      provides: "Fresh attempt-1 workflow_dispatch canonical exact-six capture"
    - path: ".planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json"
      provides: "Distinct attempt-1 validation pair bound to Plan 73 local and capture authority"
  key_links:
    - from: "scripts/ci/install-golden.sh"
      to: "Sigra.Test.InstallFixture.prepare_graph!/0"
      via: "the script's fixed prepared-mode export before its sole ordered receiver invocation and test/test_helper.exs exact environment branch"
    - from: "pinned phx_new 1.8.8 prerequisite"
      to: "prepare_graph! build_source_base!/2"
      via: "authenticated mix phx.new task availability before the evidence root is allocated"
    - from: "235.1-73-PREALLOCATION-VALIDATION.json"
      to: "235.1-73-PREREQUISITE-VALIDATION.json"
      via: "exact setup argv/environment and entrypoint policy; no raw receiver substitute"
    - from: "both Plan 73 pre-root PASS receipts"
      to: "the sole Plan 73 allocator"
      via: "independent reopen, exact counts, no Plan 73 root before setup completion, and one execute-launcher authorization"
    - from: "canonical exact-six process receipt"
      to: "Plan 73 local terminal PASS"
      via: "stdout/stderr/status persisted before assertion plus wrapper receipt/diagnostics and exact receiver equality"
    - from: "Plan 73 capture and validation receipts"
      to: "Plan 70 reconciliation"
      via: "unchanged routing/validation verifiers; Plans 69/71/72 never supply new completion evidence"
  prohibitions:
    - requirement_id: "TEST-01"
      status: "unverified"
      flagged: true
      statement: "Do not relabel Plan 72's three local timing pairs, a serial pass, a rerun, or missing same-run evidence as TEST-01 completion."
    - requirement_id: "TEST-02"
      status: "unverified"
      flagged: true
      statement: "Do not relax raw max/min <=2.0, tune after observation, reorder partitions, or admit an incomplete pair."
    - requirement_id: "TEST-03"
      status: "unverified"
      flagged: true
      statement: "Do not use raw mix test, caller-supplied prepared mode, skipped/duplicated/substituted receivers, or predecessor output as exact-six evidence."
---

<objective>
Repair Plan 72's exact-six invocation before allocation, then produce one fresh authenticated local and live library-economics evidence chain.

Purpose: Use the repository's canonical prepared-fixture receiver and prove every required tool/service prerequisite before consuming another one-shot root, while retaining the already-proven health-SQL and SQL-policy gates.
Output: Dual-runtime zero-effect entrypoint receipt, pre-root prerequisite receipt, one distinct terminal Plan 73 root, and fresh canonical PR/scaffold/validation evidence for Plan 70.

Decision coverage: D-01 through D-09 are implemented across Tasks 1–3. Plans 69/71/72 remain immutable failed evidence only; Plan 73 is the sole possible new TEST completion authority.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-CONTEXT.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-RESEARCH.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-69-DRY-RUN-FAILURE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-SUMMARY.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-71-LOCAL-FAILURE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-PLAN.md
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-PREALLOCATION-VALIDATION.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-LOCAL-FAILURE.json
@.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-SUMMARY.md
@scripts/ci/install-golden.sh
@scripts/ci/install-golden.test.sh
@scripts/ci/verify-library-install-golden.sh
@scripts/ci/verify-library-install-golden.test.sh
@test/test_helper.exs
@test/support/install_fixture.ex
@.github/workflows/ci.yml
@scripts/ci/verify-library-routing-evidence.sh
@scripts/ci/verify-library-validation-run.sh
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@test/sigra/planning/phase_235_1_library_economics_contract_test.exs
</context>

## Artifacts this phase produces

- New tracked zero-effect receipt: `235.1-73-PREALLOCATION-VALIDATION.json`.
- New tracked pre-root readiness receipt: `235.1-73-PREREQUISITE-VALIDATION.json`.
- Replaced tracked evidence receipts only after fresh passing capture/validation: `235.1-PR-EVIDENCE.json`, `235.1-SCAFFOLD-EVIDENCE.json`, and `235.1-VALIDATION-RUN.json`.
- New private evidence root: exactly one `/private/tmp/sigra-p2351-plan73-install-entrypoint.XXXXXXXX`.
- Optional setup workspace only when the exact archive is absent: exactly one `/private/tmp/sigra-p2351-plan73-phx-new.XXXXXXXX`, recorded separately and completed before evidence-root allocation.

## Interface contracts

### ExactSixEntrypoint

- `entrypoint_argv` is exactly `["bash","scripts/ci/install-golden.sh"]` from repository root; direct `mix test` is never an admissible entrypoint.
- `receiver_paths` is the exact ordered unique list: `test/sigra/install/features/passkeys_js_test.exs`, `test/sigra/install/generator_passkeys_opt_out_test.exs`, `test/sigra/install/golden_diff_test.exs`, `test/sigra/install/idempotency_test.exs`, `test/sigra/install/vault_promotion_test.exs`, `test/upgrade_test.exs`.
- The canonical script exports `SIGRA_INSTALL_GOLDEN_PREPARED=1` after changing to authenticated repository root and before its sole `mix test "${receiver_paths[@]}"`; caller values, including attacker text, are overwritten.
- Prepared mode maps in `test/test_helper.exs` only from exact value `1` to `Sigra.Test.InstallFixture.prepare_graph!/0`. The graph has six variants, a global two-worker ceiling, complete positive diagnostics, unique partitions/ports, and fail-closed cleanup/retention.
- The Plan 73 parent persists full child stdout and stderr, byte counts, hashes, status, argv, cwd, redacted environment, and timing in `local/install-golden-process.json` before inspecting the exit status or wrapper receipts.

### PrerequisiteAuthority

- Runtime selection is explicit `ASDF_ERLANG_VERSION=28.4.1`, `ASDF_ELIXIR_VERSION=1.19.5-otp-28`; archive discovery uses exact `mix archive`, and task discovery uses exact `mix help phx.new`.
- Required archive version is exactly `phx_new-1.8.8`; the only authorized absent-archive setup argv is `["mix","archive.install","--force","hex","phx_new","1.8.8"]`, executed at most once in a separately recorded archive directory before evidence-root allocation.
- PostgreSQL prerequisites are `/opt/homebrew/bin/psql` 14.17, the authenticated PostgreSQL 14 host authority retained by Plan 72, and one successful read-only `sigra_test`/`postgres` framing probe under the exact allowlisted PG environment.
- Repository dependencies must already satisfy `MIX_ENV=test mix deps.get --check-locked`; no dependency or archive setup runs after the evidence root exists.

### InheritedSafetyAuthority

- Health SQL remains Plan 72's exact collection `pure_preflight.result.observed.plan46`, selector `path=/private/tmp/plan46-collector/health.sql`, 1,653 bytes/SHA-256 `f35c4ea7a402130d766dceb9a428f0438de822f8a0f774ed3692092cc1b754d9`.
- SQL policy remains Plan 71/72's sole accepted `authenticated_e_string_tab_sentinel` plus the exact 23 unique rejected names; `PLAN67_PSQL_PARSER_BEGIN`/`PLAN67_PSQL_PARSER_END` remain extraction authority and current wrapper delimiter becomes `plan73-collector-verify\n`.

<assumption_delta_decision>
Primary noun: exact-six local entrypoint.
Decision: corrected.
Rationale: Plan 72 bypassed the canonical receiver with raw `mix test`, omitted the prepared-fixture export, and asserted on empty stderr before preserving the 8,182-byte stdout that contained the five prepared-graph failures. The existing wrapper already owns preparation, exact order, worker ceiling, diagnostics, and failure receipt semantics.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the canonical exact-six entrypoint and inherited safety policies with zero forbidden effects (D-01–D-07, D-09)</name>
  <reversibility rating="reversible">The gate reads authenticated sources, executes only pure in-memory probes, and writes one tracked canonical receipt while creating no runtime root or external state.</reversibility>
  <precondition>The 39,990-byte Plan 72 plan hashes to `ac484ada3e6b80d771fff59914d0d9fa0e6722b7fbfc706b51d58179bb88df39`; its 16,611-byte preallocation receipt hashes to `b9be3a7f805093b5ba960089f8150c087a98356cbc1b74290f0ab3cc7bba8814`; its 202-byte failure receipt hashes to `65bb0775378c2003cef32a4fee1837b5fb03fec691a83c44b232b40466c0570b`; its 9,645-byte summary hashes to `d6c67ba3d805ba1d7fa21a43fc31beb44b7aa3f2aa3c73fbff056ed1447bfc18`; frozen root `/private/tmp/sigra-p2351-plan72-source-authority.Q1orfipi` retains mode/dev/inode `0700/16777229/297260113`, marker inode `297260114` and SHA-256 `731a1d74f18c47824f3cd81d48694638b30d320dfca720f9290cdf97dd498d89`, 52 descendants, and inventory digest `d84eb0f6b431b3d72d0565df1844cbf21c4f3c93a141cb7df275b279126dc961`; Plans 69/71 retain their frozen identities; and no directory matches the exact Plan 73 evidence-root or setup-workspace patterns.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json</files>
  <read_first>
    - `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-SUMMARY.md`, `235.1-72-LOCAL-FAILURE.json`, and the frozen Plan 72 root — preserve exact consumed budgets, three green timing pairs, failed exact-six action, missing durable stdout, zero remote work, and no TEST completion.
    - `scripts/ci/install-golden.sh` and `scripts/ci/install-golden.test.sh` — authenticate the canonical fixed invocation, prepared export, ordered receivers, status/signal propagation, diagnostics, receipt schema, and attacker-value overwrite.
    - `test/test_helper.exs` and `test/support/install_fixture.ex` — bind exact prepared value `1` to `prepare_graph!`, six immutable variants, two-worker ceiling, unique scenario state, diagnostic phases, and retained failure behavior.
    - `.github/workflows/ci.yml` — derive the proven setup order: locked Beam, Hex/Rebar, exact `mix archive.install --force hex phx_new 1.8.8`, locked deps, PostgreSQL service/environment, then `MIX_ENV=test bash scripts/ci/install-golden.sh`.
    - `scripts/ci/verify-library-install-golden.sh`, its adverse test, and Phase 233/235.1 contract modules — preserve exact receiver, worker, prepared, diagnostic, and route predicates.
    - `235.1-72-PREALLOCATION-VALIDATION.json` and `235.1-71-PREALLOCATION-VALIDATION.json` — carry forward exact source-location and SQL-policy rows rather than recreating or weakening them.
    - `235.1-69-SUMMARY.md`, `235.1-71-SUMMARY.md`, and both older roots — immutable failed history only; never import, execute, retry, copy, chmod, or touch a predecessor entrypoint or descendant.
  </read_first>
  <behavior>
    - "RED: the exact Plan 72 raw mix-test argv with absent SIGRA_INSTALL_GOLDEN_PREPARED is rejected; the diagnostic records exit 2, stdout 8,182 bytes, stderr 0, 20 tests/5 failures/7 invalid, and prepared install fixture graph unavailable at install_fixture.ex:351 without repeating the command."
    - "GREEN: only canonical_install_golden_entrypoint accepts, with exact script identity, argv, cwd, ordered unique six paths, fixed prepared export, worker ceiling 2, receipt/diagnostic ownership, pinned setup/prerequisite contract, and persist-both-streams-before-assert ordering."
    - "Exact 15 rejected names: raw_mix_test_entrypoint, missing_prepared_env_export, attacker_prepared_env_passthrough, reordered_receivers, missing_receiver, duplicate_receiver, substituted_receiver, missing_phx_new_pin, changed_phx_new_pin, unavailable_phx_new_task, missing_postgresql_binary, unavailable_postgresql_server, missing_database_environment, stdout_not_persisted, stderr_not_persisted."
    - "Plan 72's one accepted source locator/ten exact source adverses and Plan 71/72's one accepted SQL sentinel/23 exact hostile SQL cases remain exact and unique."
    - "Python 3.9.6 and 3.14.4 produce identical entrypoint, source, SQL, wrapper, name-set, and generated-source semantics with zero allocator/root/marker/mktemp/source-materialization/mix/archive-install/phx_new/psql/database/timing/network/GitHub effects."
  </behavior>
  <action>Authenticate through lstat, `O_RDONLY|O_NOFOLLOW`, fstat dev/inode/type equality, exact byte count/SHA-256, duplicate-key rejection, and canonical JSON where applicable: Plan 72 plan/preallocation/failure/summary, Plan 71 preallocation, canonical wrapper/test/verifier files, `test_helper.exs`, `install_fixture.ex`, workflow, Phase 233/235.1 contracts, both pinned Python executables, and all frozen Plan 69/71/72 roots/manifests. Pin current identities: `install-golden.sh` 5,052 bytes/SHA-256 `b68dfd02687486459b4fd1fb04bddc23844e4301e68a94373d5005967d29c89a`; its test 6,328/`9240eed665210ab2316cb83f1325222571c55bcc43b56ff1f94485073a1f1f9b`; `test_helper.exs` 2,193/`014dafd3e0c52ee9367e32f002399afa6f55bde67860d3a46e0a9988b4c94322`; `install_fixture.ex` 88,320/`e31568e6ff5103d775afdf546420644493e4bdbc8ecf5a40bb3b27e1926157dc`; workflow 112,777/`ae1e2b519a433720aeb8f7a598d3869e3a5d73c871092f4e6df60456ee413682`; install verifier 5,369/`2d8e838951229c406d4295844d87c48f148c2743139dc93289deee6e210e6aa4`; and its test 6,912/`2e008e515f2be91dd1c98ef9f363541bc7cb21cee846a002b3167dc2975eaa20`.

Build one pure `validate_exact_six_entrypoint(inputs)` predicate used by the generated Plan 73 launcher. It must parse—not substring-count alone—the canonical shell array/command order, exact `ROOT`/`cd`, unconditional prepared export, one background child plus wait/status preservation, exact receiver receipt, diagnostic closed schema, worker ceiling, and stdout/stderr persistence dominance in the generated launcher. It must cross-check the test-helper `"1"` branch calls exactly `Sigra.Test.InstallFixture.prepare_graph!()` before ExUnit receivers and validates diagnostics after suite; inspect `prepare_graph!`/`WorkerPool` structure for six exact variants and maximum two leases. It must cross-check the workflow's exact archive pin, locked dependency prerequisite, PostgreSQL service/env, wrapper command, and always-run verifier/uploads. Direct `mix test`, caller-controlled prepared values, an unpinned task, or assertion-before-persistence can never satisfy the predicate.

Write RED/GREEN tests around that production predicate entirely in memory. Record sole accepted name `canonical_install_golden_entrypoint`. Apply exactly the 15 named one-field mutations from `behavior` to independent deep copies. Require exact rejected-name set and count, uniqueness, one mutation per row, accepted=false, expected failure class, and `action_calls=0`; the two stream mutations independently move or omit each persistence edge so a combined count cannot mask one missing channel. RED records the already-authorized reproduction facts without executing it.

Carry Plan 72's `source_authority` and Plan 71/72's `sql_policy` byte/field equal, including exact accepted/rejected sets, identities, and zero subprocess calls. Generate the Plan 73 package in memory from authenticated Plan 72 source rows with only current schema/path/root/wrapper/entrypoint/prerequisite substitutions; preserve 23 unique source names, parser anchors, policy-body spans, dominance, definition order, and recomputed byte/SHA/table digests. Set current delimiter to `plan73-collector-verify\n` and reject Plan 46/71/72 delimiters as current ownership while retaining history.

Run the pure entrypoint/mutation, inherited source/SQL, wrapper, compile/import, source-table, dominance, environment, host-path, finalizer-import, cache, no-follow, allocator-manifest, and validate-only launcher probes under both pinned runtimes. No probe may invoke real shell/mix/archive/phx_new/psql/database, allocate a setup/evidence directory, materialize generated sources, or perform timing/network/GitHub work. Atomically write and independently reopen canonical `235.1-73-PREALLOCATION-VALIDATION.json` with exact predecessor/source identities, RED facts, entrypoint control/15 rejection rows, source/SQL controls/adverses, generated-source/wrapper policies, runtime receipts, before/after root/workspace inventories, closed zero counts, and receipt digest.</action>
  <acceptance_criteria>Both runtimes prove one canonical wrapper entrypoint and the exact 15 unique hostile mutations; the accepted receiver list/order, prepared mapping, two-worker/diagnostic contract, phx_new/PostgreSQL prerequisites, and stream-persistence dominance are exact; Plan 72 source and Plan 71/72 SQL policies remain exact; all forbidden-effect counts are zero; predecessor roots are byte/stat identical; and no Plan 73 setup or evidence root exists.</acceptance_criteria>
  <verify>
    <automated>PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json &amp;&amp; P72=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-72-PREALLOCATION-VALIDATION.json &amp;&amp; for PY in /Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/bin/python3.9 /opt/homebrew/Cellar/python@3.14/3.14.4/Frameworks/Python.framework/Versions/3.14/bin/python3.14; do "$PY" -c 'import hashlib,json,sys; nodup=lambda p:dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate")); load=lambda p:(lambda b:(b,json.loads(b,object_pairs_hook=nodup)))(open(p,"rb").read()); raw,d=load(sys.argv[1]); raw72,p72=load(sys.argv[2]); canon=lambda v:(json.dumps(v,sort_keys=True,separators=(",",":"))+"\n").encode(); assert raw==canon(d) and d["schema"]=="sigra.phase235.1-plan73-preallocation-validation/v1" and d["status"]=="PASS"; assert len(raw72)==16611 and hashlib.sha256(raw72).hexdigest()=="b9be3a7f805093b5ba960089f8150c087a98356cbc1b74290f0ab3cc7bba8814" and raw72==canon(p72) and p72["receipt_sha256"]=="ff77a5b14f70c99307ea1566ef4885cb5b76d67b4422a1d7367d737fda1c9dc1"; ep=d["entrypoint_policy"]; expected=["test/sigra/install/features/passkeys_js_test.exs","test/sigra/install/generator_passkeys_opt_out_test.exs","test/sigra/install/golden_diff_test.exs","test/sigra/install/idempotency_test.exs","test/sigra/install/vault_promotion_test.exs","test/upgrade_test.exs"]; assert ep["accepted"]==["canonical_install_golden_entrypoint"] and ep["entrypoint_argv"]==["bash","scripts/ci/install-golden.sh"] and ep["receiver_paths"]==expected and len(expected)==len(set(expected))==6 and ep["prepared_export"]=={"name":"SIGRA_INSTALL_GOLDEN_PREPARED","value":"1","overwrites_caller":True} and ep["worker_ceiling"]==2 and ep["persist_before_assert"]==["stdout","stderr","exit_status"]; rejected=ep["rejected"]; names=[r["name"] for r in rejected]; exact={"raw_mix_test_entrypoint","missing_prepared_env_export","attacker_prepared_env_passthrough","reordered_receivers","missing_receiver","duplicate_receiver","substituted_receiver","missing_phx_new_pin","changed_phx_new_pin","unavailable_phx_new_task","missing_postgresql_binary","unavailable_postgresql_server","missing_database_environment","stdout_not_persisted","stderr_not_persisted"}; assert len(rejected)==len(names)==len(set(names))==15 and set(names)==exact and all(r["accepted"] is False and r["action_calls"]==0 for r in rejected); assert d["source_authority"]==p72["source_authority"] and d["sql_policy"]==p72["sql_policy"] and d["structural_red"]=={"command":"raw mix test over exact six","exit_status":2,"stdout_bytes":8182,"stderr_bytes":0,"tests":20,"failures":5,"invalid":7,"failure_class":"prepared install fixture graph is unavailable","source":"test/support/install_fixture.ex:351","action_calls":0}; gs=d["generated_sources"]; gn=[r["name"] for r in gs]; assert len(gs)==len(gn)==len(set(gn))==23 and gn==sorted(gn,key=lambda s:s.encode()) and all(set(r)=={"bytes","name","sha256"} and type(r["bytes"]) is int and r["bytes"]&gt;0 and len(r["sha256"])==64 for r in gs); zero={"allocator","archive_install","database","github","markers","mix","mktemp","network","phx_new","psql","roots","setup_workspaces","source_materializations","timing"}; assert set(d["counts"])==zero and set(d["counts"].values())=={0}; assert d["evidence_root_inventory_before"]==d["evidence_root_inventory_after"]==[] and d["setup_workspace_inventory_before"]==d["setup_workspace_inventory_after"]==[] and len(d["runtimes"])==2 and all(r["status"]==0 and r["stderr_bytes"]==0 for r in d["runtimes"]) and len({r["semantic_sha256"] for r in d["runtimes"]})==1' "$PV" "$P72" || exit; done &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d \( -name 'sigra-p2351-plan73-install-entrypoint.*' -o -name 'sigra-p2351-plan73-phx-new.*' \) -print -quit)"</automated>
    <fails_when>The receipt is missing/noncanonical/duplicate-keyed; any source identity differs; the accepted fixture/entrypoint/order/export/prerequisite/persistence contract differs; any one of the exact 15 names is omitted, duplicated, substituted, or accepted; inherited source/SQL rows differ; runtimes disagree; any forbidden count is nonzero; a Plan 73 directory exists; or a predecessor changes.</fails_when>
  </verify>
  <done>One canonical dual-runtime receipt proves the exact prepared-fixture entrypoint, prerequisite contract, stream preservation, inherited source/SQL policies, and full generated package without allocating or executing local/live evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Prove or prepare every exact-six prerequisite before evidence-root allocation (D-05–D-07, D-09)</name>
  <reversibility rating="costly">A missing pinned archive may require one isolated network-backed installation; it is bounded, captured, and completed before the evidence root, and cannot be retried within this plan.</reversibility>
  <precondition>Task 1's receipt passes independently under both runtimes; all Task 1 forbidden counts are zero; no Plan 73 setup/evidence directory exists; the repository worktree and frozen Plans 69/71/72 remain unchanged; `/Users/jon/.asdf/installs/erlang/28.4.1` and `/Users/jon/.asdf/installs/elixir/1.19.5-otp-28` exist; and the PostgreSQL 14 host-path authority still matches Plan 72.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json</files>
  <read_first>
    - `235.1-73-PREALLOCATION-VALIDATION.json` — sole authority for exact setup commands, receiver entrypoint, source package, and zero initial effects.
    - `.tool-versions`, `.github/workflows/ci.yml`, and Plan 09 summary/plan — derive the existing exact Beam/archive/dependency/PostgreSQL/wrapper preparation order without guessing.
    - `scripts/ci/install-golden.test.sh` — run only its stubbed wrapper contract, never the real exact-six receiver, and require cleanup leaves no test residue.
    - `test/test_helper.exs` and `test/support/install_fixture.ex` — confirm task availability and database readiness before prepared graph construction can start.
    - Plan 72 summary/failure/root and Plans 69/71 roots — immutable one-shot history checked before and after every prerequisite action.
  </read_first>
  <action>Run one prerequisite controller from authenticated Task 1 generated bytes. It must set explicit `ASDF_ERLANG_VERSION=28.4.1`, `ASDF_ELIXIR_VERSION=1.19.5-otp-28`, `LC_ALL=C`, authenticated PATH, repository cwd, and no shell evaluation. Before each subprocess, reopen the Task 1 receipt and all predecessor manifests; after return, capture complete stdout/stderr/status/argv/cwd/redacted env plus byte counts/SHA-256 into controller state before any assertion.

Run `bash scripts/ci/install-golden.test.sh` exactly once. It may exercise only its temporary stub `mix`, must report its exact PASS suite, must prove attacker prepared input is overwritten, and must leave neither its temporary directory nor `/tmp/sigra-install-golden-diagnostics.json` nor `/tmp/sigra-library-install-golden.json`. It does not consume the real exact-six budget.

Discover archives using exact `mix archive`, never the nonexistent `mix archive.list`, under the pinned Beam environment. If and only if exactly one authenticated `phx_new-1.8.8` archive is already selected and `mix help phx.new` succeeds, record `setup_decision=authenticated_existing`, `archive_install_count=0`, `network_count=0`, and its no-follow manifest. If absent, allocate exactly one mode-0700 `/private/tmp/sigra-p2351-plan73-phx-new.XXXXXXXX` setup workspace, set `MIX_ARCHIVES` to its fixed `archives` child, and invoke exact `mix archive.install --force hex phx_new 1.8.8` once; capture both streams/status before assertion, reject redirect/version substitution or a second attempt, then require exact `mix archive` and `mix help phx.new` success from that isolated archive. Record setup workspace/all descendants and `archive_install_count=1`; any install/network failure freezes setup, writes BLOCKED prerequisite receipt, allocates no evidence root, and requires a successor.

Run `MIX_ENV=test mix deps.get --check-locked` exactly once as the canonical locked dependency prerequisite; this is setup/readiness, never timing or TEST evidence. Authenticate `/opt/homebrew/bin/psql` and the PostgreSQL 14 server authority, then run exactly one fixed read-only BEGIN/current_database-current_user/ROLLBACK prerequisite probe with `PGDATABASE=sigra_test`, `PGHOST=localhost`, `PGPORT=5432`, `PGUSER=postgres`, and present redacted `PGPASSWORD`; persist full streams/status before requiring green framing and identities. Do not run `prepare_graph!`, `mix phx.new`, the actual wrapper, raw exact-six tests, source materialization, timing, or GitHub work.

Atomically write and independently reopen canonical `235.1-73-PREREQUISITE-VALIDATION.json`. Bind Task 1 receipt digest, runtime/archive location and manifest, setup decision/workspace/counts, stub-run transcript, dependency transcript, PostgreSQL transcript, exact environment projection, zero real-exact-six/timing/root/GitHub counts, unchanged predecessor manifests, empty evidence-root inventory before/after, and receipt digest. The exact selected archive location and environment become immutable Task 3 inputs.</action>
  <acceptance_criteria>The canonical wrapper's adverse suite passes without real receiver execution; pinned Beam and exactly phx_new 1.8.8 are callable; locked dependencies and PostgreSQL `sigra_test`/`postgres` readiness pass; any archive installation is isolated, exactly once, fully captured, and completed before root allocation; all subprocess streams/statuses are durable; predecessors remain unchanged; and no Plan 73 evidence root exists.</acceptance_criteria>
  <verify>
    <automated>PV=.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json &amp;&amp; /usr/bin/python3 -c 'import base64,hashlib,json,sys; nodup=lambda p:dict(p) if len(p)==len(dict(p)) else (_ for _ in ()).throw(ValueError("duplicate")); raw=open(sys.argv[1],"rb").read(); d=json.loads(raw,object_pairs_hook=nodup); canon=lambda v:(json.dumps(v,sort_keys=True,separators=(",",":"))+"\n").encode(); assert raw==canon(d) and d["schema"]=="sigra.phase235.1-plan73-prerequisite-validation/v1" and d["status"]=="PASS"; assert d["runtime"]=={"erlang":"28.4.1","elixir":"1.19.5-otp-28"} and d["archive"]["name"]=="phx_new-1.8.8" and d["archive"]["task_help_status"]==0 and d["setup_decision"] in ["authenticated_existing","isolated_install"] and d["archive_install_count"] in [0,1] and d["network_count"]==d["archive_install_count"]; assert d["entrypoint_test"]["argv"]==["bash","scripts/ci/install-golden.test.sh"] and d["entrypoint_test"]["status"]==0 and d["entrypoint_test"]["stderr_bytes"]==0 and base64.b64decode(d["entrypoint_test"]["stdout_base64"],validate=True).endswith(b"install-golden.test: PASS\n"); assert d["dependency_probe"]["argv"][-3:]==["mix","deps.get","--check-locked"] and d["dependency_probe"]["status"]==0; pg=d["postgresql_probe"]; assert pg["status"]==0 and pg["framing"]==["BEGIN","SIGRA_PLAN73_PREREQUISITE_V1","ROLLBACK"] and pg["current_database"]=="sigra_test" and pg["current_user"]=="postgres" and pg["stderr_bytes"]==0; assert all("stdout_base64" in r and "stderr_base64" in r and "status" in r for r in d["subprocesses"]); assert d["counts"]["real_exact_six"]==d["counts"]["prepare_graph"]==d["counts"]["phx_new"]==d["counts"]["timing"]==d["counts"]["evidence_roots"]==d["counts"]["github"]==0 and d["evidence_root_inventory_before"]==d["evidence_root_inventory_after"]==[]' "$PV" &amp;&amp; test -z "$(find /private/tmp -maxdepth 1 -type d -name 'sigra-p2351-plan73-install-entrypoint.*' -print -quit)"</automated>
    <fails_when>The receipt is missing/noncanonical/duplicate-keyed; wrapper stub tests, pinned runtime/archive/task, locked deps, or PostgreSQL readiness fail; an unrecorded or repeated setup occurs; a subprocess stream/status is missing; real exact-six, prepared graph, timing, evidence-root, or GitHub count is nonzero; any predecessor changes; or an evidence root exists.</fails_when>
  </verify>
  <done>One canonical prerequisite receipt proves the exact wrapper can reach prepared-graph construction with pinned tools, locked deps, and PostgreSQL ready before the sole evidence root is allocated.</done>
</task>

<task type="auto">
  <name>Task 3: Allocate one Plan 73 root, execute the canonical exact-six chain, and capture both attempt-1 route pairs (D-01–D-09)</name>
  <reversibility rating="costly">The root, exact-six process, and GitHub runs are append-only one-shot evidence; every failure is durable and cannot be retried, repaired in place, replaced, or relabeled.</reversibility>
  <precondition>Tasks 1–2 independently PASS; their source/package/archive/runtime/dependency/PostgreSQL facts agree; no Plan 73 evidence root exists; optional setup is complete and immutable; frozen Plans 69/71/72 and all receipts are unchanged; GitHub authentication is valid; and repo `szTheory/sigra`, PR #234, base `main`, head `refs/heads/ci/phase-235-1-evidence`, remote head `87338db7`, and candidate `3713dcd4408b2a280a53b541f973b4acdb79aff3` match inherited authority.</precondition>
  <files>.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-PR-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-SCAFFOLD-EVIDENCE.json, .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-VALIDATION-RUN.json</files>
  <read_first>
    - `235.1-73-PREALLOCATION-VALIDATION.json` and `235.1-73-PREREQUISITE-VALIDATION.json` — sole allocation, generated-source, exact-six entrypoint, runtime/archive, dependency, and database authority.
    - `scripts/ci/install-golden.sh`, `scripts/ci/verify-library-install-golden.sh`, and their tests — authenticate again immediately before the single real exact-six action and its receipt verification.
    - Plan 72 summary/failure/root — retain the three timing pairs and exact-six failure only as frozen history; no entrypoint, source, receipt, or partial timing output is reusable.
    - Plans 69/71 summaries/failures/roots — preserve their consumed/unused budgets and immutable manifests.
    - Plan 72 Task 3 route contract, `.github/workflows/ci.yml`, routing/validation verifiers, and Phase 235.1 receipt contract — inherit unchanged local terminal, lineage, excluded-ID, run/job/artifact, partition, and exact-six predicates.
  </read_first>
  <action>Authenticate both Plan 73 pre-root receipts and independently reconstruct the package, entrypoint policy, source locator, SQL policy, selected archive, environment, and prerequisite transcripts. Require exact equality before invoking the execute launcher once. Its allocator may call mktemp once with `/private/tmp/sigra-p2351-plan73-install-entrypoint.XXXXXXXX`, create one mode-0700 root and one exclusive mode-0600 marker, write canonical driver/handoff receipts, materialize each authenticated source once, and finalize no-follow authority. Reopen Plans 69/71/72 roots and setup workspace before/after every budgeted action; any drift halts.

Run one local chain: challenge-bound PostgreSQL 14 host gate; all 19 Plan 67 parser fixtures; Plan 72's accepted source control/ten hostile source cases; Plan 71/72's accepted SQL control/23 hostile SQL cases; isolated environment matrix; one real dry run; candidate/current observation; one admission; one health sample; and one timing command with exactly three complete pairs. Require six green timing children, 225 exhaustive disjoint paths in deterministic 114/111 partitions, sequential partition order inside each pair, positive integer raw durations, unchanged parallel ExUnit formatter behavior, independently recomputed raw ratio <=2.0, and nine fresh ledger-disjoint payloads. Use `plan73-collector-verify\n` with Plan 67 parser anchors.

After timing and before terminal publication, invoke the real exact-six action exactly once using only `bash scripts/ci/install-golden.sh` from authenticated repository cwd with Task 2's exact `ASDF_ERLANG_VERSION`, `ASDF_ELIXIR_VERSION`, selected `MIX_ARCHIVES`, `MIX_ENV=test`, PG environment, LC_ALL, PATH, and redacted password binding. Never invoke raw `mix test`, set prepared mode in the parent as a substitute, or call `prepare_graph!` directly. The wrapper itself must overwrite prepared mode and own its sole child.

Capture the wrapper child's complete stdout and stderr concurrently plus status/argv/cwd/redacted environment and start/finish boundaries. Atomically publish and no-follow reopen `local/install-golden-process.json` before any exit assertion. Then no-follow read and atomically preserve `/tmp/sigra-library-install-golden.json` and `/tmp/sigra-install-golden-diagnostics.json` as `local/install-golden-receipt.json` and `local/install-golden-diagnostics.json`; preserve failure artifacts even when nonzero. Only afterward require process status 0, full stream hashes/bytes, exact ordered unique six receivers, prepared_fixture=true, worker_ceiling=2, complete positive diagnostic phases, six variants, unique partitions/ports, zero failed paths, and existing install-golden verifier PASS. On failure, publish a durable full-stream local failure receipt, freeze root, perform no remote action, and require a successor—never assert from stderr alone.

Seal every descendant once in `local/artifact-manifest.json`, publish one terminal PASS, and make `local/verify-plan73-terminal.py` independently reopen all roots, pre-root receipts, source/policy/entrypoint/prerequisite/process/wrapper/diagnostic/timing receipts, ledgers, and manifests without mutation. Terminal authority requires exactly one real wrapper process, zero raw exact-six invocations, three pairs/six timing children/225 paths, and the exact-six process/receipt/diagnostic chain.

Only after terminal PASS, query `gh api rate_limit` once. At REST core <=250, record reset and issue no polling until reset; HTTP 403/429 is a hard stop. Give each run one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`, fetch one structured summary after completion, and fetch failed logs only for failure. At most one non-rate-limit API transient may be retried; never rerun a workflow or increment attempt. Deterministic/workflow failure blocks without rerun, substitution, in-place repair, waiver, or auto-approval.

Exclude every retained Plan 35–72 run/job ID. Push candidate `3713dcd4` once, capture one automatic pull_request attempt-1 run, and dispatch one workflow_dispatch attempt-1 run at the same SHA. Require exact repo/PR/base/head/SHA/event/workflow/job/attempt identities, successful selected routes/checks, independent artifact decoding, two partitions/ratios, and canonical exact-six only on dispatch. Write/reopen PR/scaffold receipts; commit exactly those two files as candidate's direct child and push once. Capture one automatic pull_request attempt-1 validation and one distinct workflow_dispatch attempt-1 validation at evidence SHA. Write `235.1-VALIDATION-RUN.json` only after all four pass, binding run identities, lineage, Plan 73 root/terminal/entrypoint/process, 225-path 114/111 topology, ratios, and exact-six. Run unchanged offline verifiers. Do not push again or reconcile prose.</action>
  <acceptance_criteria>Exactly one Plan 73 evidence root/local chain exists; terminal verification authenticates both pre-root receipts, actual health SQL, exact parsed policy, three timing pairs, six green timing children, 225 paths, and one canonical exact-six wrapper process with both streams preserved; Plans 69/71/72 remain unchanged; exactly four fresh distinct non-excluded attempt-1 runs prove capture/validation PR/dispatch; the evidence commit is candidate's direct child with only two capture JSON files changed; and both unchanged evidence verifiers pass.</acceptance_criteria>
  <verify>
    <automated>PLAN73_ROOT="$(/usr/bin/python3 -c 'import os,re; rows=sorted(e.path for e in os.scandir("/private/tmp") if re.fullmatch(r"sigra-p2351-plan73-install-entrypoint\.[A-Za-z0-9]{8}",e.name) and e.is_dir(follow_symlinks=False)); assert len(rows)==1; print(rows[0])')" &amp;&amp; /usr/bin/python3 "$PLAN73_ROOT/local/verify-plan73-terminal.py" --root "$PLAN73_ROOT" --preallocation-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREALLOCATION-VALIDATION.json --prerequisite-validation .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-PREREQUISITE-VALIDATION.json --plan69-root /private/tmp/sigra-p2351-plan69-recovery.G9QrBacu --plan71-root /private/tmp/sigra-p2351-plan71-sql-guard.knJsmWsF --plan72-root /private/tmp/sigra-p2351-plan72-source-authority.Q1orfipi &amp;&amp; bash scripts/ci/verify-library-routing-evidence.sh &amp;&amp; bash scripts/ci/verify-library-validation-run.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_1_library_economics_contract_test.exs --only receipt_contract</automated>
    <fails_when>Root discovery returns zero/multiple; a predecessor/setup artifact changes; any source, policy, entrypoint, prerequisite, stream, receipt, manifest, ledger, partition, ratio, timing-child, or exact-six predicate fails; an action repeats; a run is stale/excluded/retried/wrong-ref/wrong-SHA/non-green; lineage differs; or an offline verifier fails.</fails_when>
  </verify>
  <done>One fresh prepared-entrypoint Plan 73 chain proves complete local, PR, and non-PR routes and leaves canonical capture/validation receipts as Plan 70's sole new TEST authority.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | Items | Coverage |
|---|---|---|
| GOAL | Two measured exhaustive PR partitions plus exact-six non-PR coverage without protected regression | Task 3 proves both routes from one fresh local/live chain; no predecessor partial evidence is promoted. |
| REQ | TEST-01, TEST-02, TEST-03 | All three are in frontmatter; Task 3 preserves unchanged predicates and produces their only possible new evidence. |
| RESEARCH | Independent fail-closed receipts, same-run timing, deterministic receiver ownership | Tasks 1–3 authenticate inputs, preserve exact topology, and require independent replay at each boundary. |
| CONTEXT | D-01 through D-09 and recovery addenda | Tasks cite every decision, preserve partitions/exact-six/attempt-1 budgets, delay reconciliation, and retain FAST-01/GATE-05. |
| LATEST EVIDENCE | Plan 72 passed source/SQL and three timing pairs, then raw exact-six lacked prepared mode and lost stdout | Task 1 records the root cause without rerun; Task 2 proves prerequisites; Task 3 uses the canonical wrapper and persists both streams before assertion. |
| HISTORY | Plans 68/69/71/72 and their roots/budgets | Every task authenticates and freezes them; no failed evidence becomes completion authority. |
| CONTEXT deferred | Unrelated release, installer-product, auth, security, admin, and other CI work | Excluded; no deferred item is implemented. |
</source_coverage_audit>

<recovery_decisions>

- Plan 72 and `/private/tmp/sigra-p2351-plan72-source-authority.Q1orfipi` are permanently frozen. Its exact-six command, root, timing pairs, and sources are never retried or reused.
- The confirmed direct reproduction is diagnostic authority only: raw exact-six `mix test`, missing prepared env, exit 2, stdout 8,182, stderr 0, 20 tests/5 failures/7 invalid, repeated prepared-graph RuntimeError at `install_fixture.ex:351`. It is not repeated.
- The fix is invocation recovery, not product/test modification: canonical `bash scripts/ci/install-golden.sh` already owns exact receivers, prepared export, worker ceiling, diagnostics, and receipt.
- Missing prerequisites are resolved or durably blocked before evidence allocation. Optional archive setup has its own one-attempt budget and cannot authorize a second evidence root.
- Plan 73 retains Plan 72 source-location authority, Plan 71/72 SQL policy, TEST predicates, two partitions, exact-six, FAST-01, GATE-05, and all historical failures without weakening or relabeling.
</recovery_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository source → pure preallocation receipt | Shell/Elixir/YAML/generated-source bytes become execution authority only after exact parsing, hashes, mutation matrices, and dual-runtime agreement. |
| Host tools/services → prerequisite receipt | Beam, Mix archive, dependencies, and PostgreSQL are external mutable prerequisites proven before evidence allocation. |
| Prerequisite receipt → allocator | The one-shot root is forbidden until exact setup/readiness state is canonical and independently reopened. |
| Canonical wrapper → prepared fixture graph | Only the fixed wrapper may set prepared mode and invoke the exact ordered receivers; caller-controlled/raw substitutes fail. |
| Exact-six child → local terminal | Both output streams and status cross an untrusted process boundary and are persisted before assertions. |
| Local terminal → GitHub | Network actions remain blocked until all local source, timing, entrypoint, exact-six, and manifest predicates pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235.1-73-01 | Spoofing | Exact-six entrypoint | high | mitigate | Task 1 pins canonical script/argv/cwd/export/order and exact accepted/15-rejected unique fixture sets. |
| T-235.1-73-02 | Tampering | Prepared fixture and receiver set | high | mitigate | Authenticate wrapper/test-helper/fixture graph plus exact six ordered paths, two-worker ceiling, diagnostics, and existing adverse contracts. |
| T-235.1-73-03 | Tampering | phx_new/dependency setup | high | mitigate | Task 2 allows exact 1.8.8 setup at most once before allocation, captures both streams, and authenticates resulting archive/task. |
| T-235.1-73-04 | Repudiation | Child failure output | high | mitigate | Task 3 atomically persists full stdout, stderr, status, argv, cwd, environment, and wrapper receipts before checking success. |
| T-235.1-73-05 | Repudiation | Failed predecessor budgets | high | mitigate | All tasks authenticate Plans 69/71/72 manifests and prohibit retry/reuse/relabeling. |
| T-235.1-73-06 | Spoofing | Health SQL/parsed policy | high | mitigate | Task 1 requires byte/field equality to Plan 72 source and Plan 71/72 accepted/23-hostile policy. |
| T-235.1-73-07 | Spoofing | GitHub evidence | high | mitigate | Exact repo/ref/SHA/event/workflow/job/attempt/artifact identities, excluded IDs, and unchanged offline verifiers. |
| T-235.1-73-08 | Information Disclosure | Process/prerequisite receipts | low | accept | Store complete command output needed for diagnosis but redact passwords and private environment values. |
| T-235.1-73-09 | Denial of Service | Setup/live budget | medium | mitigate | One optional archive install, one evidence root, one wrapper process, one watcher/run, 60-second polling, rate hard stops, and no workflow reruns. |
</threat_model>

<verification>
Task 1 verifies the exact entrypoint/15-hostile matrix and inherited source/SQL policies under both pinned runtimes without forbidden effects. Task 2 proves or prepares every actual runtime/archive/dependency/PostgreSQL prerequisite before allocation with full-stream transcripts. Task 3 independently authenticates both receipts, executes one canonical local chain, persists exact-six output before assertions, verifies the terminal, and only then spends four fresh attempt-1 live runs. Plan 70 remains unavailable as reconciliation authority until the Plan 73 summary and unchanged evidence verifiers pass.
</verification>

<success_criteria>
Plan 73 succeeds only when the zero-effect receipt, prerequisite receipt, sole fresh root, local terminal, canonical exact-six process/receipt/diagnostics, three ordinary timing pairs, capture pair, and distinct validation pair all independently PASS; the exact 15 entrypoint and inherited ten/23 safety mutations remain complete and unique; Plans 69/71/72 stay frozen; no action is retried; and unchanged routing/validation verifiers accept the resulting evidence.
</success_criteria>

<output>
Create `.planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-73-SUMMARY.md` when done.
</output>
