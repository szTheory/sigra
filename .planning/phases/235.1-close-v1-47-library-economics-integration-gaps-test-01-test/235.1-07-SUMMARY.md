---
phase: 235.1-close-v1-47-library-economics-integration-gaps-test-01-test
plan: 07
subsystem: testing
tags: [elixir, exunit, fixture-dag, concurrency, library-economics, blocked]
status: halted
requires:
  - phase: 235.1-06
    provides: immutable prepared fixture DAG, private checkout isolation, and bounded scenario runner
provides:
  - six canonical scaffold receivers migrated to immutable reads or isolated private checkouts
  - exact receiver-universe, assertion-retention, and two-worker topology contracts
  - retained fixed-bound local economics diagnostic showing the remaining non-dominance miss
affects: [TEST-01, TEST-02, TEST-03, 235.1-04, 235.1-05]
actuals:
  tokens: 10413
  tasks: 2
  commits: 9
tech-stack:
  added: []
  patterns: [immutable prepared variants, private mutation checkouts, two-worker scenario fan-out, source-contract topology ratchets]
key-files:
  created:
    - .planning/phases/235.1-close-v1-47-library-economics-integration-gaps-test-01-test/235.1-07-SUMMARY.md
  modified:
    - test/upgrade_test.exs
    - test/sigra/install/golden_diff_test.exs
    - test/sigra/install/features/passkeys_js_test.exs
    - test/sigra/install/generator_passkeys_opt_out_test.exs
    - test/sigra/install/idempotency_test.exs
    - test/sigra/install/vault_promotion_test.exs
    - test/sigra/planning/phase_233_library_economics_contract_test.exs
    - test/sigra/planning/phase_235_1_library_economics_contract_test.exs
key-decisions:
  - Golden assertions read the immutable default variant, while every rerun, compile, migration, database, or runtime mutation owns a private checkout.
  - Upgrade and opt-out scenario groups execute through the fixture runner with exactly two workers and preserve their real behavioral assertions.
  - The six-path receiver universe is derived from the fixed shell runner and compared with the live tagged set instead of copying a retired list.
  - The locked economics predicates remain unchanged; the local non-dominance miss halts the plan without evidence admission.
patterns-established:
  - "Prepared receiver ownership: immutable reads for observation and private checkout/build/database/port state for mutation."
  - "Coverage ratchet: derive the runner-owned receiver set, prove equality with live :scaffold modules, and pin executable assertion sentinels."
requirements-completed: []
coverage:
  - id: D1
    description: Six canonical receivers consume the prepared fixture DAG with retained compile, runtime, migration, JavaScript, golden, idempotency, opt-out, and vault assertions.
    requirement: TEST-01
    verification:
      - kind: integration
        ref: "SIGRA_INSTALL_GOLDEN_PREPARED=1 mix test for the Task 1 and Task 2 receiver groups"
        status: pass
    human_judgment: false
  - id: D2
    description: Exact-once receiver ownership, private mutation isolation, two-worker cancellation, and unchanged verifier predicates are contract-tested.
    requirement: TEST-02
    verification:
      - kind: integration
        ref: "mix test test/support/install_fixture_performance_test.exs plus Phase 233 and Phase 235.1 contracts"
        status: pass
    human_judgment: false
  - id: D3
    description: The complete local contributor path satisfies comparability and install non-dominance with unchanged bounds.
    requirement: TEST-03
    verification:
      - kind: integration
        ref: "MIX_ENV=test mix ci && bash scripts/ci/verify-library-economics.sh"
        status: fail
    human_judgment: false
duration: 1h 1m
completed: 2026-09-09
---

# Phase 235.1 Plan 07: Prepared Receiver Economics Remediation Summary

Six canonical receivers now preserve their real assertions on Plan 06's isolated fixture DAG, but the unchanged local economics verifier still rejects install non-dominance at 115.041 seconds versus 63.892 seconds, so the plan halts without authorizing evidence.

## Checkpoint Status

- **Plan status:** Halted during Task 3; Tasks 1 and 2 are complete.
- **Receiver coverage:** All six canonical paths pass through one runner with immutable reads or private mutation state and exactly two workers for independent scenarios.
- **Fixed predicates:** Comparability passes at approximately `1.8006 <= 2.0`; install non-dominance fails because `115041 <= 63892` is false.
- **Passing evidence:** Intentionally absent. No evidence or truth-status artifact was created or changed.
- **Required remediation:** Reduce the remaining measured install phases, led by receiver compile/runtime, without weakening assertions, owner topology, or either fixed predicate; then rerun Plan 07's complete command before Plan 04 captures fresh PR evidence.

## Completed Receiver Migration

- Golden tree/stdout checks read the immutable `default_installed` state.
- Idempotency reruns operate in a private `default_installed` checkout and retain hash, new-file, mtime, and skip-output checks.
- Three upgrade scenarios use matching private default/no-org states, execute through the two-worker runner, and retain seed, migration, rerun, warnings-as-errors compile, server, and runtime checks.
- Standard and nonstandard passkey JavaScript checks read exact named variants; the rerun owns a private checkout.
- Both opt-out variants compile in private checkouts through the two-worker runner and resolve their generated OTP application names from each checkout manifest.
- Vault promotion compiles and validates a private passkeys checkout with its real source sentinels.
- Phase 233 and 235.1 contracts pin six-path equality, one runner/owner, private-state mappings, assertion sentinels, cancellation, diagnostics, timing boundaries, and unchanged integer predicates.

## Retained Local Diagnostic

The exact raw class receipt remains at `/tmp/sigra-library-economics.json`, and the non-authoritative phase diagnostic remains at `/tmp/sigra-install-golden-diagnostics.json`.

| Class | Duration | Conclusion | Exit status |
| --- | ---: | --- | ---: |
| ordinary | `63892 ms` | success | `0` |
| install_scaffold | `115041 ms` | success | `0` |

Independent arithmetic from those raw integers gives:

- Comparability: `115041 / 63892 = 1.80056`, which passes the locked `max/min <= 2.0` predicate.
- Install non-dominance: `115041 <= 63892` is false, exceeding ordinary by `51149 ms`.

The retained install diagnostic reports raw install duration `114746 ms` and this complete positive phase breakdown:

| Phase | Duration |
| --- | ---: |
| `receiver_compile_runtime` | `55216 ms` |
| `baseline_compile` | `19742 ms` |
| `checkout_copy` | `17541 ms` |
| `deps_get` | `4517 ms` |
| `installer` | `3580 ms` |
| `phx_new` | `279 ms` |

`receiver_compile_runtime` is the largest remaining measured phase. Its `55216 ms` alone exceeds the full `51149 ms` class gap, while preparation phases owned by the fixture infrastructure add another `45659 ms`.

## Verification Performed

```text
bash scripts/ci/install-golden.test.sh
=> 6 tests, 0 failures

ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test \
  test/support/install_fixture_performance_test.exs \
  test/sigra/planning/phase_233_library_economics_contract_test.exs \
  test/sigra/planning/phase_235_1_library_economics_contract_test.exs
=> 27 tests, 0 failures

ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix ci
=> ordinary: 33 doctests, 3 properties, 2581 tests, 0 failures, 12 skipped, 20 excluded
=> prepared receivers: 20 tests, 0 failures
=> dependency-off lane: 65 tests, 0 failures

bash scripts/ci/verify-library-economics.sh
=> FAIL: non-dominance predicate failed: install_scaffold exceeds ordinary
```

Focused prepared verification also passed Task 1 with 13 tests and Task 2 with 26 tests, both with zero failures. No suite reported zero tests.

## Task Commits

1. **Task 1 RED contract:** `c7748d15` — define prepared receiver ownership contract.
2. **Task 1 receiver migration:** `251dcee7` — migrate default, idempotency, and upgrade receivers.
3. **Task 2 RED contract:** `88bbdf67` — define remaining receiver isolation contract.
4. **Task 2 receiver migration:** `94ad441d` — migrate passkey, opt-out, and vault receivers.
5. **Task 1 correctness repair:** `9c2849bf` — remove the globally excluded upgrade tag so prepared scenarios execute.
6. **Task 3 contract ratchet:** `ba73cfb9` — pin exact receiver assertions and economics proofs.
7. **Task 2 concurrency:** `9805b26e` — run opt-out compile scenarios through the two-worker runner.
8. **Task 2 identity repair:** `5ea3362d` — resolve generated OTP application identity from checkout manifests.
9. **Task 1 concurrency:** `6d7315c2` — run isolated upgrade scenarios through the two-worker runner.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical coverage] Executed upgrade scenarios hidden by a global exclusion**

- **Found during:** Task 1 prepared verification.
- **Issue:** `@moduletag :upgrade` matched the global `test_helper` exclusion, so the planned command could exit successfully while running zero upgrade scenarios.
- **Fix:** Removed the excluded tag and added a Phase 233 contract that rejects its reintroduction.
- **File:** `test/upgrade_test.exs`, `test/sigra/planning/phase_233_library_economics_contract_test.exs`
- **Commit:** `9c2849bf`

**2. [Rule 1 - Bug] Resolved generated application identity independently of checkout names**

- **Found during:** Task 2 prepared verification.
- **Issue:** Private checkout basenames are isolation tokens, not OTP application names, so compile verification targeted a nonexistent application.
- **Fix:** Parse the generated application name from each checkout's `mix.exs` manifest.
- **File:** `test/sigra/install/generator_passkeys_opt_out_test.exs`
- **Commit:** `5ea3362d`

**Total deviations:** 2 auto-fixed (one Rule 1, one Rule 2). Both preserve executable coverage and add no product scope.

## Issues Encountered

Plan 06's prepared fixture required several integration repairs while these real receivers exercised it: portable copy semantics, prepared build seeding, materialized Sigra `priv/` assets, executable-bit and database/port isolation, and canonical Phoenix endpoint configuration. The Plan 06 owner committed those repairs in `713ee2da`, `c1dd0162`, `87e650a5`, `f85b5042`, and `745bcb3b`; Plan 07 did not modify fixture infrastructure.

One full-gate retry initially encountered local build state left by the dependency-off lane. Restoring and compiling the locked Threadline dependency repaired that local state without source changes; the fresh full rerun then completed every suite and produced the genuine economics miss above.

## Known Stubs

None.

## Blocker

Task 3 cannot satisfy its done condition because the production verifier correctly rejects the raw non-dominance result. The receiver migration, exact-once ownership, isolation, cancellation, diagnostics, and fixed-verifier contracts are green, but TEST-01, TEST-02, and TEST-03 remain open until a fresh complete local run satisfies both locked predicates. No threshold, environment, receipt, evidence, or status mutation was used to bypass the result.

## Self-Check: PASSED

- The checkpoint summary exists and records the exact retained raw receipt and phase diagnostic values.
- All nine Plan 07 source commits exist in local history.
- All eight owned source/contract files are clean after their atomic commits.
- No evidence or truth-status artifact was created or changed by Plan 07.

