---
phase: 242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/live/app_live.ex
  - test/example/test/example_web/live/app_live_test.exs
  - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
  - test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs
autonomous: true
requirements:
  - XW-01
  - XW-02
must_haves:
  truths:
    - "An authenticated user can see and operate a plainly named Tasklane control on /app that performs the existing CSRF-protected POST /crosswake/start (D-01, D-02, D-03)."
    - "The rendered control submits no user-selected Crosswake, session, evaluator, route, destination, or navigation values; all protocol authority remains server-owned (D-04, D-05, D-06, D-07)."
    - "The real browser cookie jar reaches the existing Crosswake return and fixed /app destination by clicking the rendered control, without DOM fabrication (D-08, D-09)."
    - "The focused proof remains role-driven, readiness-driven, sleep-free, serial with one worker, and zero-retry (D-10)."
  artifacts:
    - path: test/example/lib/example_web/live/app_live.ex
      provides: "Visible native Crosswake POST control on the authenticated Tasklane account hub"
      contains: "Continue to Crosswake"
    - path: test/example/test/example_web/live/app_live_test.exs
      provides: "Rendered route, method, accessible-name, and zero-protocol-input contract"
      contains: "crosswake/start"
    - path: test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
      provides: "Role-driven real-cookie-jar Crosswake journey"
      contains: "getByRole"
    - path: test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs
      provides: "Repository source guard for the rendered entry and preserved browser/security contract"
      contains: "@app_live"
  key_links:
    - from: test/example/lib/example_web/live/app_live.ex
      to: test/example/lib/example_web/router.ex
      via: "Phoenix.Component action-bearing POST form delegates to the existing authenticated controller route"
      pattern: "action=\\{~p\"/crosswake/start\"\\}.*method=\"post\""
    - from: test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
      to: test/example/lib/example_web/live/app_live.ex
      via: "Accessible role/name locator clicks the rendered native submit button"
      pattern: "getByRole\\('button'.*Continue to Crosswake"
    - from: test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
      to: test/example/lib/example_web/controllers/crosswake_controller.ex
      via: "Observed native POST follows the existing 303 return chain using the real cookie jar"
      pattern: "waitForRequest.*crosswake/return"
---

<objective>
Close XW-01/XW-02's remaining integration edge by giving an authenticated example-host user a rendered native Crosswake start control and proving the unchanged hosted return flow through that control.

Purpose: Replace crafted browser-side submission with a discoverable, accessible user action while preserving the Phase 240.3 controller, continuation, session, evaluator, secrecy, and fixed-navigation boundaries (D-01–D-10).
Output: One Tasklane `/app` action, focused LiveView/browser contracts, and a durable repository source guard.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-CONTEXT.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-RESEARCH.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-PATTERNS.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-VALIDATION.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-04-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-08-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-09-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-10-SUMMARY.md

<interfaces>
Existing contracts the executor must preserve:

- `ExampleWeb.AppLive.render/1` owns the authenticated `/app` Tasklane surface and its stable `data-testid="app-account-home"` and `data-testid="app-quick-actions"` hooks.
- `ExampleWeb.Router` already exposes `post "/crosswake/start", CrosswakeController, :start` inside `[:browser, :require_authenticated]`; the `:browser` pipeline already calls `protect_from_forgery`.
- `Phoenix.Component.form/1` with `for={%{}}`, an `action`, and `method="post"` emits the ordinary HTTP form and standard CSRF mechanics; no LiveView event is required.
- The browser proof already registers `/crosswake/return` and final document `/app` request observers before submission and owns the exact callback-key, absent-Referer, fixed destination, cookie, correlation, and secret assertions.
- `Sigra.Planning.Phase2403HostedCrosswakeRuntimeTest.read!/1` and module attributes are the existing source-inspection seam for browser/config/host contracts.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render and drive the complete native Crosswake start journey</name>
  <reversibility rating="reversible">The host-only label, panel placement, and test locator can be changed locally without changing a published contract, schema, or protocol.</reversibility>
  <read_first>
    - test/example/lib/example_web/live/app_live.ex
    - test/example/test/example_web/live/app_live_test.exs
    - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
    - test/example/lib/example_web/router.ex
    - test/example/lib/example_web/controllers/confirmation_html.ex
    - test/example/lib/example_web/controllers/crosswake_controller.ex
    - test/example/priv/playwright/playwright.config.ts
    - scripts/ci/hosted-session-interop-proof.sh
    - scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
    - .planning/v1.48-MILESTONE-AUDIT.md
    - .planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-CONTEXT.md
    - .planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-VALIDATION.md
  </read_first>
  <files>test/example/lib/example_web/live/app_live.ex, test/example/test/example_web/live/app_live_test.exs, test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts</files>
  <behavior>
    - RED: the authenticated `/app` render contract fails until a visible button named `Continue to Crosswake` belongs to an ordinary POST form targeting `/crosswake/start` (XW-01; D-01–D-03).
    - GREEN: the form contains only Phoenix's standard CSRF submission mechanics and the submit control; it has no application-defined inputs and no LiveView submission event (XW-01, XW-02; D-04–D-07).
    - END-TO-END: after rendered `/app` readiness and pre-registered request observers, the role/name click traverses the existing real-cookie-jar flow and retains exact return keys, absent Referer, final `/app`, and non-disclosure assertions (XW-01, XW-02; D-08–D-10).
  </behavior>
  <action>
First extend `ExampleWeb.AppLiveTest`'s authenticated standard-user case, or add one adjacent focused case using the same `user_fixture/1`, `log_in_user/2`, and `live/2` setup, so RED asserts one visible native submit control named `Continue to Crosswake`, its form action `/crosswake/start`, POST method, and CSRF hidden field. Parse/select the rendered form narrowly enough to prove it has no application-defined input names for continuation, state, PKCE verifier, binding, session identity, route, destination, evaluator, or navigation, and no LiveView submission binding. Preserve the existing unauthenticated redirect and operator-hiding assertions (XW-01, XW-02; D-01–D-07).

Then add a fourth action panel inside `data-testid="app-quick-actions"` in `ExampleWeb.AppLive.render/1`. Use the existing Tasklane classes `vt-panel`, `vt-kicker`, `vt-panel__title`, `vt-copy`, and `vt-btn vt-btn--primary`; render `<.form for={%{}} action={~p"/crosswake/start"} method="post">` with a native `button type="submit"` whose exact visible and accessible name is `Continue to Crosswake`. Give the form stable hook `data-testid="app-crosswake-start"`. Keep `data-testid="app-account-home"` and `app-quick-actions` unchanged. The form must contain no application-owned inputs and must delegate directly to the existing authenticated router/controller boundary. Do not add `handle_event/3`, route/controller/template changes, client orchestration, `sg-*` styling, generated-host output, Sigra-core behavior, Crosswake package changes, dependencies, schema changes, or protocol/navigation configuration (D-01–D-07).

Finally, in `crosswake-hosted-runtime.spec.ts`, retain the rendered login, real `_example_key` cookie assertions, both pre-registered request observers, exact sorted `continuation`/`state` callback keys, absent Referer, final `/app` URL and `app-account-home`/welcome readiness, and the entire correlation/secret sentinel matrix. Replace only the DOM-fabricated submission block with `page.getByRole('button', { name: 'Continue to Crosswake' }).click()` after an explicit visible assertion on that locator. Do not reconstruct, log, attach, store, or replay observed correlation values; do not use direct request APIs, cookie mutation, arbitrary delays, or retry overrides. Leave the `crosswake-hosted-runtime` Playwright project and proof runner unchanged so it remains serial, one-worker, zero-retry, and bounded (D-08–D-10).
  </action>
  <acceptance_criteria>
    - Authenticated `/app` HTML contains one stable `app-crosswake-start` native form targeting `/crosswake/start` with POST method, standard CSRF hidden input, and a visible submit button accessible as `Continue to Crosswake` (XW-01; D-01–D-03).
    - The Crosswake form has no application-defined protocol, identity, evaluator, route, destination, or navigation inputs and no LiveView event binding; `router.ex`, `crosswake_controller.ex`, Sigra core, generated-host templates, Crosswake packages, manifests, schemas, and admin UI have no phase diff (XW-01, XW-02; D-04–D-07).
    - The browser spec contains a role/name click on `Continue to Crosswake`, contains no DOM-fabricated submission, and preserves the existing real-cookie, request-observer, exact callback-key, absent-Referer, fixed `/app`, and correlation/secret non-disclosure assertions (XW-01, XW-02; D-08, D-09).
    - `playwright.config.ts` still declares `fullyParallel: false`, `workers: 1`, and `retries: 0`; the focused spec adds no arbitrary delay and runs through the existing `--browser-only` wrapper (D-10).
    - `cd test/example && mix test test/example_web/live/app_live_test.exs` and `scripts/ci/hosted-session-interop-proof.sh --browser-only` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example_web/live/app_live_test.exs &amp;&amp; cd ../.. &amp;&amp; scripts/ci/hosted-session-interop-proof.sh --browser-only</automated>
  </verify>
  <done>An authenticated user can click the rendered Tasklane `Continue to Crosswake` button and complete the existing secure, fixed-destination Crosswake journey with every Phase 240.3 browser assertion still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract-lock the rendered edge without duplicating protocol ownership</name>
  <reversibility rating="reversible">The focused source assertions extend an internal planning test and can be revised locally with the host markup.</reversibility>
  <read_first>
    - test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs
    - test/example/lib/example_web/live/app_live.ex
    - test/example/test/example_web/live/app_live_test.exs
    - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
    - test/example/priv/playwright/playwright.config.ts
    - test/example/lib/example_web/router.ex
    - scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
    - scripts/ci/hosted-session-interop-proof.sh
    - .planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-PATTERNS.md
    - .planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-VALIDATION.md
  </read_first>
  <files>test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs</files>
  <behavior>
    - The existing planning contract fails if `AppLive` loses the native `/crosswake/start` POST, stable hook, Tasklane class family, or exact accessible button name (XW-01; D-01–D-05).
    - The contract fails if the browser proof stops using the role/name click or if the callback-key, absent-Referer, fixed `/app`, secret checks, one-worker, or zero-retry guarantees disappear (XW-01, XW-02; D-06, D-08–D-10).
    - Existing controller, continuation, evaluator, and P14 assertions remain the sole server-security authority rather than being recopied into the new rendered-edge test (XW-02; D-06, D-07).
  </behavior>
  <action>
Extend `Sigra.Planning.Phase2403HostedCrosswakeRuntimeTest` in place. Add `@app_live "test/example/lib/example_web/live/app_live.ex"`, read it alongside `@browser_test` in the existing `continuation, controller, browser, and serial project guards name the real security matrix` test, and add focused stable-marker assertions for `data-testid="app-crosswake-start"`, `action={~p"/crosswake/start"}`, `method="post"`, `type="submit"`, `Continue to Crosswake`, and the `vt-*` panel/button classes. Require the browser source marker for `page.getByRole('button', { name: 'Continue to Crosswake' }).click()` and reject the removed synthetic submission API by source inspection. Retain the existing controller/continuation denial markers, `page.waitForRequest`, Playwright project name, `workers: 1`, `retries: 0`, exact callback-key browser assertions, absent-Referer assertion, final `/app` readiness, and verifier/correlation non-disclosure markers (XW-01, XW-02; D-01–D-10).

Keep these assertions in the existing Phase 240.3 source-contract module because it already owns the host/browser/security wiring. Do not create a second guard, change the P14 fact schema, duplicate evaluator/replay cases, rewrite the controller/protocol internals, alter the proof runner or evidence receipt, or touch application production code in this task (D-06, D-07, D-09, D-10). Use exact stable markers and actionable assertion messages, not broad vocabulary counts or formatting-sensitive whole-file snapshots.
  </action>
  <acceptance_criteria>
    - The source contract reads `@app_live` and fails if the native POST action, stable hook, Tasklane styling markers, submit type, or `Continue to Crosswake` accessible name is removed or redirected through a LiveView event (XW-01; D-01–D-05).
    - The source contract requires the exact role/name click and rejects reintroduction of the synthetic browser submission mechanism while preserving request observers (D-08, D-10).
    - Existing assertions continue to require exact callback keys, absent Referer, fixed `/app`, secret/correlation non-disclosure, controller/continuation denial markers, `crosswake-hosted-runtime`, one worker, and zero retries (XW-01, XW-02; D-06, D-09, D-10).
    - The task changes only `test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs`; P14, controller, continuation, adapter, runner, receipt, dependencies, schemas, generated output, Sigra core, and admin UI are unchanged (D-06, D-07).
    - `MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` and `scripts/ci/hosted-session-interop-proof.sh --browser-only` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs &amp;&amp; scripts/ci/hosted-session-interop-proof.sh --browser-only</automated>
  </verify>
  <done>The repository mechanically rejects loss or bypass of the rendered native Crosswake entry while preserving the established controller/protocol and secrecy proof ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated `/app` document → `POST /crosswake/start` | A user-visible browser action crosses into the existing CSRF-protected authenticated controller pipeline. |
| Browser-observed return navigation → host continuation/controller | Opaque correlation values appear transiently in an observed server-generated request but must not become browser authority, logs, final navigation, or DOM content. |
| Source-contract test → established Phase 240.3 security owners | The new rendered-edge guard must preserve controller, continuation, adapter, evaluator, and P14 ownership rather than creating a competing security implementation. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-01 | Spoofing / Tampering | Native `/app` start form | high | mitigate | Use Phoenix's action-bearing POST form and standard CSRF mechanics inside the already authenticated router pipeline; assert the action, method, CSRF field, and absence of a LiveView event (D-01, D-02, D-05). |
| T-242-02 | Elevation of Privilege | Rendered Crosswake control | high | mitigate | Submit no application-defined protocol, binding, identity, evaluator, route, destination, or navigation values; preserve fresh server-side session resolution and evaluator ownership in the unchanged controller path (D-04–D-07). |
| T-242-03 | Information Disclosure | Browser return journey | high | mitigate | Preserve exact callback keys, encrypted HttpOnly transport, absent Referer, fixed final `/app`, and the existing URL/DOM sentinel matrix; the test observes values only in memory and does not log or replay them (D-06, D-09). |
| T-242-04 | Repudiation | XW-01/XW-02 closure evidence | medium | mitigate | Require both the focused LiveView contract and real role-driven one-worker/zero-retry Chromium proof; source-contract assertions reject a return to fabricated submission (D-08–D-10). |
| T-242-05 | Denial of Service | Focused browser proof | low | accept | The bounded existing runner owns readiness, port selection, server cleanup, and timeout behavior; this phase adds no poller, retry, worker, or delay and therefore adds no material availability exposure (D-10). |
</threat_model>

## Artifacts this phase produces

| Category | Artifact | Disposition |
|----------|----------|-------------|
| Modified committed file | `test/example/lib/example_web/live/app_live.ex` | Adds the host-only Tasklane native Crosswake action. |
| Modified committed file | `test/example/test/example_web/live/app_live_test.exs` | Adds the rendered route/method/name/zero-input contract. |
| Modified committed file | `test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts` | Replaces fabricated submission with the role-driven rendered journey while retaining downstream assertions. |
| Modified committed file | `test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` | Adds durable source-contract coverage for the rendered entry and role click. |
| Created symbol, route, controller action, LiveView event, public API, schema, migration, dependency, fixture, generated-host output, admin UI asset, or external-API coverage artifact | None | The phase adds markup and assertions only; existing protocol/controller ownership remains unchanged (D-02, D-05–D-07). |

## Capability detector outcomes

- Schema-push detection: no schema-relevant path is in phase scope; no schema push task is generated.
- Assumption-delta detector: `detected: false`; no assumption-delta decision is recorded.
- API-coverage detector: no external API/SDK/service integration is in phase scope; no coverage matrix is generated.
- Spec-less edge probe: visibly skipped because ROADMAP/init maps no requirement IDs to Phase 242; no probe-derived predicates were generated. XW-01 and XW-02 are covered directly from REQUIREMENTS.md, CONTEXT.md, and RESEARCH.md.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Authenticated users can initiate the existing Crosswake flow from a rendered `/app` control and complete the unchanged secure return | 242-01 | COVERED | Tracer spans rendered form through role-driven browser proof. |
| REQ | XW-01 | Project a backend-validated personal session without organization invention or credential/token exposure | 242-01 | COVERED | Zero-authority-input form plus retained cookie-jar and non-disclosure proof. |
| REQ | XW-02 | Missing/stale/switched session state fails closed and return data alone never grants access | 242-01 | COVERED | Existing server security owners stay unchanged and source-guarded; rendered edge supplies no authority. |
| RESEARCH | — | Native action-bearing Phoenix POST form with automatic CSRF | 242-01 | COVERED | Tracer implements and LiveView test verifies route/method/CSRF. |
| RESEARCH | — | Pre-register navigation observers and use role/name click | 242-01 | COVERED | Tracer replaces only the fabricated submission block. |
| RESEARCH | — | Preserve Tasklane `vt-*`, stable readiness, no sleeps, one worker, zero retries | 242-01 | COVERED | Both tasks and browser verification enforce these constraints. |
| RESEARCH | — | Preserve controller/P14 security ownership and avoid protocol duplication | 242-01 | COVERED | Scope fence plus existing source-contract expansion. |
| CONTEXT | D-01 | Render on authenticated `ExampleWeb.AppLive` `/app` | 242-01 | COVERED | Task 1. |
| CONTEXT | D-02 | Native CSRF POST to existing `/crosswake/start` controller route | 242-01 | COVERED | Task 1 and T-242-01. |
| CONTEXT | D-03 | Visible accessible Tasklane `vt-*` control | 242-01 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-04 | No user-supplied Crosswake/navigation values | 242-01 | COVERED | Task 1 zero-input contract. |
| CONTEXT | D-05 | Only CSRF mechanics; all protocol values server-owned | 242-01 | COVERED | Task 1 and T-242-02. |
| CONTEXT | D-06 | Preserve Phase 240.3 security/navigation contract | 242-01 | COVERED | Both tasks retain existing assertions and ownership. |
| CONTEXT | D-07 | No core/generated/published-successor/responsibility expansion | 242-01 | COVERED | File fence and both task criteria. |
| CONTEXT | D-08 | Replace `page.evaluate()` fabrication with role interaction | 242-01 | COVERED | Tracer browser change and source guard. |
| CONTEXT | D-09 | Retain real-cookie, return, keys, Referer, `/app`, and non-disclosure proof | 242-01 | COVERED | Tracer acceptance and Task 2 contract. |
| CONTEXT | D-10 | Stable readiness, roles, no sleeps, one worker, zero retries | 242-01 | COVERED | Tracer and source-contract criteria. |

No CONTEXT.md deferred ideas exist. RESEARCH.md scope exclusions—controller/protocol changes, generated output, admin styling, dependencies, and unresolved successor work—remain excluded.

<verification>
1. Run `cd test/example && mix test test/example_web/live/app_live_test.exs` for the native rendered form contract.
2. Run `MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` for durable source ownership and deterministic browser-contract guards.
3. Run `scripts/ci/hosted-session-interop-proof.sh --browser-only` for the complete role-driven real-cookie-jar journey.
4. Confirm the implementation diff is limited to the four frontmatter paths; no controller, continuation, adapter, P14, runner, receipt, core, generated-output, dependency, schema, or admin UI file changed.
</verification>

<success_criteria>
- Authenticated `/app` visibly renders a Tasklane button named `Continue to Crosswake` inside a native CSRF POST form targeting the existing route.
- The control contributes no Crosswake authority or navigation input and the existing controller/session/evaluator boundaries remain unchanged.
- Chromium clicks the rendered control by role/name and preserves the real cookie jar, exact callback keys, absent Referer, fixed final `/app`, and all correlation/secret non-disclosure assertions.
- Focused LiveView, source-contract, and browser commands pass deterministically with no sleeps, one worker, and zero retries.
- XW-01, XW-02, and D-01 through D-10 are all mechanically covered without scope expansion.
</success_criteria>

<output>
Create `.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-01-SUMMARY.md` when done.
</output>
