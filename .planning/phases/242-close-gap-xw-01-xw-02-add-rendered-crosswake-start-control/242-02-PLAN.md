---
phase: 242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control
plan: "02"
type: execute
wave: 2
depends_on: ["242-01"]
files_modified:
  - test/example/test/example/accounts/crosswake_continuations_test.exs
autonomous: true
gap_closure: true
requirements:
  - XW-01
  - XW-02
must_haves:
  truths:
    - "The continuation cleanup proof starts from a known sandbox-local row baseline, so committed residue in the shared example_test database cannot change its expected bounded-cleanup result (XW-02; D-06, D-10)."
    - "The cleanup case still creates 501 expired continuations plus one live continuation, deletes at most the 500 oldest terminal rows, preserves the live claim, and proves that claim can complete through the unchanged evaluator path (XW-01, XW-02; D-06)."
    - "The complete adapter, continuation, controller, and P14 real/bad/clean matrix exits successfully without weakening the rendered role-driven browser journey delivered by Plan 242-01 (XW-01, XW-02; D-06-D-10)."
  artifacts:
    - path: test/example/test/example/accounts/crosswake_continuations_test.exs
      provides: "Sandbox-isolated Crosswake continuation cleanup and replay evidence"
      contains: "cleanup removes at most 500 oldest terminal rows without weakening live claims"
  key_links:
    - from: test/example/test/example/accounts/crosswake_continuations_test.exs
      to: test/example/test/support/conn_case.ex
      via: "ConnCase checks out the SQL sandbox before the continuation test setup clears only its transaction-visible continuation rows"
      pattern: "setup_sandbox"
    - from: test/example/test/example/accounts/crosswake_continuations_test.exs
      to: test/example/lib/example/accounts/crosswake_continuations.ex
      via: "The isolated test exercises cleanup_expired/2 and then completes the surviving live continuation"
      pattern: "CrosswakeContinuations\\.(cleanup_expired|complete)"
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: the spec-less edge probe left XW-01 unclassified; this gap plan preserves the owner-owned classification question while restoring deterministic evidence."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: the spec-less edge probe left XW-02 unclassified; a passing cleanup/security matrix does not fabricate an edge classification."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT manufacture organization authority for a personal SIGRA session."
      reason: "Descriptor-less fallback prohibition retained for review; this plan does not invent a wired check descriptor."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT expose a session credential, token digest, provider payload, or PKCE verifier through the rendered or return journey."
      reason: "Descriptor-less fallback prohibition retained for review; existing mechanical evidence is rerun without being repackaged as a fallback descriptor."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or return data select a session, replace fresh host resolution, admit a route, choose a destination, or independently grant access."
      reason: "Descriptor-less fallback prohibition retained for review; no automatic dismissal or fabricated enforcement path is permitted."
---

<objective>
Close the remaining Phase 242 verification gap by making the terminal-continuation cleanup proof independent of committed residue in the shared example test database, then rerunning the unchanged Crosswake security matrix.

Purpose: Supply deterministic passing XW-02 evidence without changing production cleanup semantics, the rendered `/app` control, or any Phase 240.3 authority/security boundary (D-01-D-10).
Output: One sandbox-isolated continuation test file and a clean aggregate adapter/continuation/controller/P14 result.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-CONTEXT.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-RESEARCH.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-PATTERNS.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-VERIFICATION.md
@.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-01-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-02-SUMMARY.md

<interfaces>
- `ExampleWeb.ConnCase` invokes `Example.DataCase.setup_sandbox/1` before each test, so cleanup in test setup is transaction-local and rolled back after the case.
- `Example.Accounts.CrosswakeContinuations.cleanup_expired/2` selects at most 500 terminal/expired IDs ordered by expiry and ID; this production query remains unchanged.
- The failing case intentionally creates 501 expired rows and one live row, then proves a 500-row delete leaves exactly one expired row plus the live row.
- Plan 242-01 already proved the rendered `/app` control, native CSRF POST, role-driven browser interaction, fixed return, and disclosure sentinels; this plan only restores the aggregate XW-02 evidence gate.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Isolate the continuation cleanup proof and pass the complete Crosswake matrix</name>
  <reversibility rating="reversible">The change is confined to sandboxed test setup and can be removed without a schema, protocol, public-contract, or production-code migration.</reversibility>
  <read_first>
    - test/example/test/example/accounts/crosswake_continuations_test.exs
    - test/example/test/support/conn_case.ex
    - test/example/test/support/data_case.ex
    - test/example/lib/example/accounts/crosswake_continuations.ex
    - test/example/lib/example/accounts/crosswake_continuation.ex
    - test/example/test/example/accounts/crosswake_session_adapter_test.exs
    - test/example/test/example_web/controllers/crosswake_controller_test.exs
    - scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
    - .planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-VERIFICATION.md
    - .planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-01-SUMMARY.md
    - .planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-02-SUMMARY.md
  </read_first>
  <files>test/example/test/example/accounts/crosswake_continuations_test.exs</files>
  <behavior>
    - RED: With committed `crosswake_continuations` residue present in the configured shared `example_test` database, the existing global row-count assertion reproduces the verifier's expected-two/observed-extra-rows failure (XW-02).
    - GREEN: After `ConnCase` has checked out the SQL sandbox, module test setup deletes transaction-visible `CrosswakeContinuation` rows and asserts the table is empty before each case; the rollback boundary prevents the test cleanup from mutating committed evidence outside its transaction (XW-02; D-06, D-10).
    - BOUND: The cleanup case still issues 501 one-second rows and one sixty-second live row at the fixed clock, receives exactly `{500, nil}`, observes exactly two owned rows, and successfully completes the preserved live continuation (XW-01, XW-02; D-06).
    - END-TO-END: The original aggregate adapter/continuation/controller test command plus P14 repository, expected-failing bad fixture, and passing clean fixture completes successfully with no production file changes (XW-01, XW-02; D-06-D-10).
  </behavior>
  <action>
Add a module-level test `setup` in `Example.Accounts.CrosswakeContinuationsTest` that runs after `ExampleWeb.ConnCase` has established the SQL sandbox. Delete all transaction-visible `CrosswakeContinuation` rows through `Example.Repo.delete_all/1`, assert that a subsequent existence/count check proves the local baseline is empty, and return `:ok`. This directly removes the verifier-documented dependence on committed shared-database residue while keeping every test mutation inside the sandbox rollback boundary (XW-02; D-06, D-10).

Keep the production `cleanup_expired/2` implementation, schema, migration, timestamps, 500-row bound, oldest-first ordering, one-time claim logic, and cleanup test scenario unchanged. Do not replace the exact two-row postcondition with a baseline-relative or weaker inequality: once setup establishes an empty transaction-local baseline, the existing exact assertion is the deterministic proof that 500 of the 501 expired rows were deleted and the live row survived. Do not truncate tables, disable the sandbox, reset the database globally, add sleeps/retries, alter the aggregate command, or touch the rendered `/app` control, controller, adapter, P14 guard, generated output, Sigra core, admin UI, evidence receipts, dependencies, or external services (XW-01, XW-02; D-01-D-10).

Run the focused continuation file first, then the full unchanged aggregate matrix. Apply `mix format` only to the modified continuation test and verify it with `--check-formatted`; the unrelated Plan 242-01 formatter warnings in REVIEW.md are not entries in VERIFICATION.md `gaps:` and are therefore not expanded into this gap-closure task.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; set -a; source tmp/db.env; set +a; cd test/example; MIX_ENV=test mix ecto.migrate --quiet; MIX_ENV=test mix format --check-formatted test/example/accounts/crosswake_continuations_test.exs; MIX_ENV=test mix test test/example/accounts/crosswake_continuations_test.exs; MIX_ENV=test mix test test/example/accounts/crosswake_session_adapter_test.exs test/example/accounts/crosswake_continuations_test.exs test/example_web/controllers/crosswake_controller_test.exs --include example_app; cd ../..; node --test scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs; ! GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json node --test scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs; GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json node --test scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs'</automated>
  </verify>
  <acceptance_criteria>
    - The continuation test file establishes and asserts an empty transaction-local `CrosswakeContinuation` baseline after sandbox checkout, and the focused file passes against a configured database that previously contained committed continuation rows (XW-02; D-06, D-10).
    - The cleanup case continues to create 501 expired rows plus one live row, delete exactly 500 terminal rows, observe exactly two rows, preserve the live digest, and complete the live claim through the evaluator (XW-01, XW-02; D-06).
    - The unchanged adapter, continuation, and controller suites pass together with `--include example_app`; P14 passes on the repository, fails on its known-bad fixture, and passes on its clean fixture (XW-01, XW-02; D-06, D-09, D-10).
    - `mix format --check-formatted` passes for the only modified file.
    - `git diff --name-only` for this plan contains only `test/example/test/example/accounts/crosswake_continuations_test.exs`; no production, schema, migration, route, controller, browser, core, generated-host, admin UI, receipt, dependency, or external-API artifact changes (D-01-D-10).
  </acceptance_criteria>
  <done>The verifier's expected-two/observed-extra-rows failure is eliminated by sandbox-local isolation, and the full unchanged Crosswake fail-closed matrix supplies deterministic passing evidence for XW-01/XW-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed shared `example_test` rows → sandboxed continuation test | Pre-existing database residue is untrusted test state and must not influence deterministic cleanup evidence. |
| Test-only isolation → production continuation behavior | A test repair must not weaken or alter the production 500-row cleanup, claim, replay, expiry, evaluator, or navigation contracts. |
| Aggregate test result → XW-01/XW-02 verification claim | A green receipt is trustworthy only if the real suites and non-vacuous P14 real/bad/clean controls all execute and propagate failures. |

## STRIDE Threat Register — OWASP ASVS L1, blocking threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242G-01 | Tampering / Repudiation | Shared-database cleanup test | medium | mitigate | Clear and assert only transaction-visible continuation rows after sandbox checkout, then retain the exact row-count and live-claim assertions so residue cannot falsify the result. |
| T-242G-02 | Elevation of Privilege | Production continuation authority | high | mitigate | Restrict the diff to the test file and rerun the unchanged adapter, continuation, controller, and P14 real/bad/clean matrix; no production authority or callback input changes are allowed (D-04-D-07). |
| T-242G-03 | Information Disclosure | Crosswake security evidence | high | mitigate | Preserve P14 repository/fail-first/clean enforcement and the existing no-secret authority contracts; test setup handles rows only and emits no continuation values (D-05, D-06, D-09). |
| T-242G-04 | Denial of Service | Bounded cleanup proof | low | accept | The test retains the production maximum of 500 rows and uses no retry, polling, delay, or unbounded data generation; the bounded local fixture cost is acceptable (D-10). |
</threat_model>

## Artifacts this phase produces

| Category | Artifact | Disposition |
|----------|----------|-------------|
| Modified committed file | `test/example/test/example/accounts/crosswake_continuations_test.exs` | Adds sandbox-local continuation-row isolation and preserves the exact cleanup/live-claim proof. |
| New production symbol, route, controller action, LiveView event, public API, schema, migration, dependency, fixture, generated-host output, admin UI asset, external-API coverage artifact, or evidence receipt | None | The gap is a deterministic test-isolation defect only. |

## Capability detector outcomes

- Schema-push detection: no schema-relevant file is in `files_modified`; no schema push task is generated.
- Assumption-delta detector: `detected: false`; no assumption-delta decision is generated.
- API-coverage detector: this plan integrates no external API, SDK, service, webhook, or provider; no API coverage work is fabricated.
- Spec-less edge probe: XW-01 and XW-02 remain `unclassified` / `unresolved` and are carried above as explicit flagged assumptions.
- Prohibition recall: routine engineering items were dropped; canonical CSRF/auth/injection concerns route to the ASVS threat model. The three product-specific authority/secret prohibitions are retained above as descriptor-less `unverified` items and are not auto-dismissed.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Rendered authenticated `/app` Crosswake start and unchanged secure return journey | 242-01, 242-02 | COVERED | Plan 01 delivered and proved the user journey; Plan 02 restores the aggregate fail-closed evidence required to certify it. |
| REQ | XW-01 | Backend-validated personal session projects without organization invention or credential/token exposure | 242-01, 242-02 | COVERED | Plan 01 browser/P14 proof remains intact; Plan 02 reruns the full adapter and P14 matrix. |
| REQ | XW-02 | Missing, expired, revoked, or switched state fails closed; return data alone grants no access | 242-02 | COVERED | The only verification gap is the contaminated cleanup-test baseline; the tracer isolates it and reruns the complete denial matrix. |
| RESEARCH | — | Native Phoenix POST, role-driven browser proof, and server-owned protocol inputs | 242-01 | COVERED | Executed and verified; Plan 02 deliberately does not reopen the rendered edge. |
| RESEARCH | — | Deterministic ExUnit/Playwright evidence with stable readiness, no sleeps, one worker, zero retries | 242-01, 242-02 | COVERED | Plan 01 owns browser determinism; Plan 02 adds sandbox-local database determinism with no sleeps/retries. |
| RESEARCH | — | Preserve controller/P14 security ownership and avoid protocol duplication | 242-01, 242-02 | COVERED | Plan 02 changes only the test baseline and reruns the existing owners. |
| CONTEXT | D-01-D-05 | Existing authenticated host surface, native POST, accessible Tasklane control, zero client authority inputs | 242-01 | COVERED | Already implemented and verified; no gap task reimplements or alters these locked decisions. |
| CONTEXT | D-06 | Preserve the complete Phase 240.3 security/navigation contract | 242-01, 242-02 | COVERED | Plan 02 restores deterministic evidence for the unchanged contract. |
| CONTEXT | D-07 | No Sigra core, generated output, successor, or responsibility-split change | 242-01, 242-02 | COVERED | File fence permits only the continuation test. |
| CONTEXT | D-08-D-10 | Role interaction, retained return/secrecy assertions, deterministic no-sleep serial proof | 242-01, 242-02 | COVERED | Plan 01 remains the browser owner; Plan 02 reruns its security owners without weakening these contracts. |

No CONTEXT.md deferred ideas exist. REVIEW.md WR-01 through WR-03 are formatter warnings rather than entries in VERIFICATION.md `gaps:`; gap-closure mode therefore leaves them outside this plan instead of silently treating them as certification failures.

<verification>
1. Run the focused continuation suite against the configured shared test database and require all six cases to pass from the sandbox-local baseline.
2. Run the aggregate adapter/continuation/controller suite with `--include example_app` and require zero failures.
3. Run P14 against the repository, its known-bad fixture, and its clean fixture, requiring green/red/green respectively.
4. Confirm the only implementation diff is `test/example/test/example/accounts/crosswake_continuations_test.exs` and no schema/API/admin UI path appears.
</verification>

<success_criteria>
- Shared committed continuation residue cannot change the cleanup test result.
- The exact 500-row bound, oldest terminal cleanup behavior, two-row postcondition, and surviving live claim remain proven.
- The complete unchanged Crosswake adapter/continuation/controller/P14 matrix passes and supplies the missing XW-02 evidence.
- XW-01/XW-02 edge assumptions and product-specific prohibitions remain explicitly flagged without fabricated classification or descriptors.
- No production, schema, API, generated-host, admin UI, or external-service scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/242-close-gap-xw-01-xw-02-add-rendered-crosswake-start-control/242-02-SUMMARY.md` when done.
</output>
