---
phase: 154-design-contract-sg-notice
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/static/assets/css/app.css
autonomous: true
requirements:
  - COMP-04

must_haves:
  truths:
    - "D-04: sg-notice CSS is added in exactly ONE place — test/example/priv/static/assets/css/app.css inside @layer sg-components (the sole tracked app.css; no lib/template copy)"
    - "D-05: no mirrored CSS copy is created — the admin-generated parity lane probes routes/markup and never diffs app.css, so this edit cannot affect it"
    - "D-06: sg-notice reuses the existing [data-tone] token set (--sg-color-{ok,warn,risk,info}-soft/base, --sg-radius-sm, --sg-space-4, --sg-color-panel, --sg-transition-tone, --sg-elev-inset) — behavior-preserving vs sg-list-row[data-tone], no new token or motion primitive"
    - "test/example/priv/static/assets/css/app.css contains .sg-notice selector inside @layer sg-components"
    - "The sg-notice block has a base rule and all four tone variants (ok, warn, risk, info)"
    - "ok tone uses 18% ring opacity; warn/risk/info use 20% — preserving the sg-list-row asymmetry"
    - "No new !important is introduced in app.css"
    - "No new --sg-* CSS custom property definitions are introduced (existing tokens only)"
    - "The insertion is between @layer sg-components { (line ~203) and its closing brace (line ~1418)"
    - "No LiveView files under lib/sigra/admin/live/ are modified"
    - "No Playwright baseline snapshots are changed"
  artifacts:
    - path: "test/example/priv/static/assets/css/app.css"
      provides: "sg-notice CSS class definition inside @layer sg-components"
      contains: ".sg-notice"
  key_links:
    - from: "test/example/priv/static/assets/css/app.css .sg-notice"
      to: "test/example/priv/static/assets/css/app.css .sg-list-row[data-tone]"
      via: "behavior-preserving token parity (same tokens, same formula, same opacity asymmetry)"
      pattern: "sg-notice.*data-tone"
---

<objective>
Add ~15 lines of sg-notice CSS inside @layer sg-components in test/example/priv/static/assets/css/app.css. The new block is a behavior-preserving selector-rename of the existing .sg-list-row[data-tone] pattern — same tokens, same color-mix formula, same ok=18% vs warn/risk/info=20% ring-opacity asymmetry. Insertion point: after line 967 (closing brace of .sg-list-row[data-tone="info"]), before .sg-kv at line ~969.

Purpose: Provides the .sg-notice CSS class so Phase 156 (COHR-05) can swap the two ad-hoc sg-list-row[data-tone] alert call sites in user_show_live.ex:131 and organization_live.ex:71 to <.notice> without any visual delta. This phase only adds the CSS class definition — no LiveView renders .sg-notice until Phase 156.

Output: test/example/priv/static/assets/css/app.css (edited in place, ~16 lines added after line 967)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
@/Users/jon/projects/sigra/.planning/phases/154-design-contract-sg-notice/154-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/154-design-contract-sg-notice/154-RESEARCH.md
@/Users/jon/projects/sigra/.planning/phases/154-design-contract-sg-notice/154-PATTERNS.md

<interfaces>
<!-- Key CSS contracts the executor needs. Verified from codebase. -->

From test/example/priv/static/assets/css/app.css (verified):
  @layer declaration (line 15): @layer sg-base, sg-components, sg-overrides;
  @layer sg-components opens: line ~203
  @layer sg-components closes: line ~1418
  .sg-list-row block (base rule): lines 945–948 (border-radius, background, box-shadow, padding, transition)
  .sg-list-row[data-tone] variants: lines 950–967 (ok=18% ring, warn/risk/info=20% ring)
  Insertion point: after line 967 (closing } of .sg-list-row[data-tone="info"]), before .sg-kv at line ~969
  prefers-reduced-motion block (ONLY !important in file): lines ~1437–1447

Token set for sg-notice (all confirmed in :root):
  --sg-radius-sm       (line 84)
  --sg-color-panel-alt (line 62)   — base background (neutral, no tone)
  --sg-color-panel     (line 61)   — used in color-mix formula
  --sg-elev-inset      (line 99)   — 1px inset ring
  --sg-space-4         (line 25)   — 16px padding
  --sg-transition-tone (lines 127–130) — color/bg/box-shadow 140ms ease
  --sg-color-ok-soft   (line 78)   — ok tone soft fill
  --sg-color-warn-soft (line 76)   — warn tone soft fill
  --sg-color-risk-soft (line 74)   — risk tone soft fill
  --sg-color-info-soft (line 80)   — info tone soft fill
  --sg-color-ok        (line 77)   — ok tone accent
  --sg-color-warn      (line 75)   — warn tone accent
  --sg-color-risk      (line 73)   — risk tone accent
  --sg-color-info      (line 79)   — info tone accent

Critical asymmetry: ok ring = 18%, warn/risk/info ring = 20%. Copy verbatim from sg-list-row source.

Dark-mode and reduced-motion are inherited automatically — no additional rules needed.
No .sg-notice__body {} stub — that is a Phase 155 concern (per RESEARCH.md open question).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert sg-notice CSS block into app.css</name>
  <files>test/example/priv/static/assets/css/app.css</files>

  <read_first>
    - test/example/priv/static/assets/css/app.css lines 940–975 — the .sg-list-row[data-tone] template block (lines 945–967) and the surrounding context (.sg-kv at ~969); confirm exact indentation style used inside @layer sg-components
    - test/example/priv/static/assets/css/app.css lines 1–20 — @layer declaration at line 15; confirm layer order
    - test/example/priv/static/assets/css/app.css lines 1415–1450 — confirm @layer sg-components closing brace at ~1418 and prefers-reduced-motion block at ~1437–1447
    - .planning/phases/154-design-contract-sg-notice/154-PATTERNS.md — exact CSS block to insert (verbatim in the "Exact CSS to insert" section); confirm ok=18%, warn/risk/info=20%
  </read_first>

  <action>
    Edit test/example/priv/static/assets/css/app.css to insert the sg-notice block immediately after the closing brace of .sg-list-row[data-tone="info"] (line ~967), before .sg-kv.

    The block to insert (read from 154-PATTERNS.md "Exact CSS to insert" section — do NOT deviate from the token names or the 18%/20% opacity values):

      A blank line, then the comment header:
        /* NOTICE — behavior-preserving consolidation of sg-list-row[data-tone] alert pattern.
         * Phase 156 (COHR-05) migrates call sites to <.notice>. Source: Phase 154 COMP-04. */

      Then the base .sg-notice rule using the same 5 properties as .sg-list-row:
        border-radius: var(--sg-radius-sm);
        background: var(--sg-color-panel-alt);
        box-shadow: var(--sg-elev-inset);
        padding: var(--sg-space-4);
        transition: var(--sg-transition-tone);

      Then all four tone variants using the same color-mix formula as .sg-list-row[data-tone]:
        ok variant: 18% ring opacity (color-mix(in oklab, var(--sg-color-ok) 18%, transparent))
        warn variant: 20% ring opacity
        risk variant: 20% ring opacity
        info variant: 20% ring opacity

    Match the indentation of the surrounding .sg-list-row rules exactly (2-space indent inside @layer sg-components block).

    Do NOT modify any other part of app.css. Do NOT change any existing rule. Do NOT add any new --sg-* custom property definitions. Do NOT add any !important. Do NOT touch any file under lib/sigra/admin/live/.
  </action>

  <verify>
    <automated>
      grep -c "sg-notice" /Users/jon/projects/sigra/test/example/priv/static/assets/css/app.css &&
      git -C /Users/jon/projects/sigra diff -- test/example/priv/static/assets/css/app.css | grep "^+" | grep "!important" &&
      git -C /Users/jon/projects/sigra diff -- test/example/priv/static/assets/css/app.css | grep "^+.*--sg-.*:.*;" | grep -v "var(" &&
      git -C /Users/jon/projects/sigra diff --name-only -- lib/sigra/admin/live/
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -c "sg-notice" test/example/priv/static/assets/css/app.css returns at least 5 (base rule + 4 tone variants each contain "sg-notice")
    - git diff app.css | grep "^+" | grep "!important" returns empty — no new !important introduced
    - git diff app.css | grep "^+.*--sg-.*:.*;" | grep -v "var(" returns empty — no new custom property definitions (only references via var())
    - git diff --name-only -- lib/sigra/admin/live/ returns empty — no LiveView modified
    - git diff --name-only -- 'test/example/priv/playwright/tests/**-snapshots/' returns empty — no baselines changed
    - Confirm ok tone uses 18%: grep "data-tone=\"ok\"" -A2 app.css | grep "18%" returns a hit
    - Confirm warn/risk/info use 20%: grep "data-tone=\"warn\"" -A2 app.css | grep "20%" returns a hit
    - The inserted block is adjacent to .sg-list-row[data-tone="info"] closing brace (verify by inspecting diff context lines)
    - grep -n "@layer" app.css confirms layer declaration is unchanged (still line 15: @layer sg-base, sg-components, sg-overrides;)
  </acceptance_criteria>

  <done>test/example/priv/static/assets/css/app.css contains .sg-notice with base rule + 4 tone variants inside @layer sg-components, inserted after line 967, with no !important and no new token definitions. No LiveView or Playwright baseline files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static asset | app.css is a hand-authored static CSS file served to browsers; no server-side processing, no user input, no authentication boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; threat not applicable |
| T-154-02-01 | Tampering | app.css cascade layer | accept | The CSS edit introduces no !important and stays inside @layer sg-components. Cascade order is unchanged. No specificity conflicts — .sg-notice is a new class with no competing rules. This is a maintainability invariant (documented in RESEARCH.md common pitfalls), not a security vulnerability. Verified by: git diff | grep "!important" must be empty. |
| T-154-02-02 | Tampering | Playwright baselines | accept | No LiveView renders .sg-notice yet (Phase 156); app.css change is purely additive CSS (new class definition). Playwright tests screenshot rendered HTML, not CSS files. Risk of baseline churn is zero for this phase. Verified by: git diff --name-only -- *-snapshots/ must be empty. |

**Summary:** This is a ~15-line additive CSS edit to a static file. There are no new attack surfaces, no user input handling, no authentication logic, no secrets, and no privilege boundaries touched. No high-severity threats exist. The only relevant invariant is cascade safety: no !important introduced, insertion stays inside @layer sg-components. Both are verified by automated diff checks.
</threat_model>

<verification>
After the task completes:

1. sg-notice present: grep -c "sg-notice" test/example/priv/static/assets/css/app.css (expect ≥5)
2. No !important added: git diff -- test/example/priv/static/assets/css/app.css | grep "^+" | grep "!important" (expect empty)
3. No new token defs: git diff -- test/example/priv/static/assets/css/app.css | grep "^+.*--sg-.*:.*;" | grep -v "var(" (expect empty)
4. ok ring opacity: grep "data-tone=\"ok\"" -A2 test/example/priv/static/assets/css/app.css | grep "18%" (expect hit)
5. warn ring opacity: grep "data-tone=\"warn\"" -A2 test/example/priv/static/assets/css/app.css | grep "20%" (expect hit)
6. No LiveView modified: git diff --name-only -- lib/sigra/admin/live/ (expect empty)
7. No baselines changed: git diff --name-only -- 'test/example/priv/playwright/tests/**-snapshots/' (expect empty)
8. Scope boundary: git diff --name-only (expect only test/example/priv/static/assets/css/app.css plus any .planning/ files)
</verification>

<success_criteria>
- .sg-notice CSS block exists inside @layer sg-components in test/example/priv/static/assets/css/app.css with base rule + 4 tone variants (ok 18%, warn/risk/info 20%)
- No new !important; no new --sg-* custom property definitions
- Insertion placed after .sg-list-row[data-tone="info"] closing brace (~line 967), before .sg-kv (~line 969)
- No LiveView files modified; no Playwright baselines changed
- COMP-04 requirement satisfied
</success_criteria>

<output>
Create .planning/phases/154-design-contract-sg-notice/154-02-SUMMARY.md when done
</output>
