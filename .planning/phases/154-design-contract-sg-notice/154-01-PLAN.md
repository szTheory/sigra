---
phase: 154-design-contract-sg-notice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-design-contract.md
  - mix.exs
autonomous: true
requirements:
  - COMP-03

must_haves:
  truths:
    - "guides/reference/admin-design-contract.md exists at the committed path and is tracked by git"
    - "The doc covers all 10 canonical components: stat_link, stat, task_card, summary_chip, applied_chip, empty_state, page_back, scope_ribbon, notice, skeleton — each with job, winning markup/CSS, ARIA role(s), motion spec (including explicit 'not animated' entries), and when-NOT-to-use"
    - "The doc documents 3 page archetypes (Overview / List / Detail) as explicit component compositions — not prose descriptions"
    - "mix.exs extras: list contains 'guides/reference/admin-design-contract.md' immediately after 'guides/reference/generator-options.md'"
    - "mix docs exits 0 — ExDoc renders the new guide without error"
    - "No LiveView file under lib/sigra/admin/live/ is changed"
    - "No Playwright baseline snapshots are changed"
  artifacts:
    - path: "guides/reference/admin-design-contract.md"
      provides: "Job->Component mapping governance doc (COMP-03 deliverable)"
      contains: "## Job"
    - path: "mix.exs"
      provides: "ExDoc extras registration for admin-design-contract.md"
      contains: "admin-design-contract"
  key_links:
    - from: "mix.exs extras:"
      to: "guides/reference/admin-design-contract.md"
      via: "ExDoc build (mix docs)"
      pattern: "admin-design-contract"
    - from: "guides/reference/admin-design-contract.md"
      to: "lib/sigra/admin/live/*.ex (all 6)"
      via: "documentation cross-reference (cited sources)"
      pattern: "stat_link|task_card|summary_chip|applied_chip|empty_state|page_back|scope_ribbon|notice|skeleton"
---

<objective>
Create guides/reference/admin-design-contract.md — the governance doc that documents the Job->Component mapping for all 10 canonical admin components and 3 page archetypes (Overview / List / Detail). Register it in the mix.exs ExDoc extras list so it appears in hexdocs alongside generator-options.md.

Purpose: This doc is the durable, citable authority for phases 155–160. It commits "same job -> same component" as a locked convention, including ARIA roles, motion specs with explicit "not animated" entries, and when-NOT-to-use guidance. Phase 155 (COMP-01) and Phase 156 (COHR-01..06) reference it.

Output:
- guides/reference/admin-design-contract.md (new file, ~150–200 lines Markdown)
- mix.exs (one-line addition to extras: list, line 198+1)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
@/Users/jon/projects/sigra/.planning/phases/154-design-contract-sg-notice/154-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/154-design-contract-sg-notice/154-RESEARCH.md
@/Users/jon/projects/sigra/.planning/phases/154-design-contract-sg-notice/154-PATTERNS.md
@/Users/jon/projects/sigra/.planning/phases/154-design-contract-sg-notice/154-UI-SPEC.md

<interfaces>
<!-- Key source patterns the executor needs. Verified from codebase. -->

From mix.exs lines 186–248 (ExDoc extras: and groups_for_extras: confirmed):
  extras: list at line 186; "guides/reference/generator-options.md" at line 198.
  INSERT "guides/reference/admin-design-contract.md" immediately after line 198
  (as a new line 199, pushing all subsequent entries down by 1).
  groups_for_extras: Reference: ~r{guides/reference/.?} at line 243 — already matches; NO change needed.

Analog doc shape from guides/reference/generator-options.md:
  - No YAML frontmatter. Title is first # H1. ExDoc infers title from H1.
  - Tables preferred over prose bullet lists for structured data.
  - Cross-reference links use relative .html paths (e.g. ../introduction/getting-started.html).

From lib/sigra/admin/live/ (all 6 LiveViews — component locations for doc accuracy):
  Component           LiveView source file(s)                  CSS class(es)
  ──────────────────────────────────────────────────────────────────────────
  summary_chip        users_index_live.ex:78–84,336–343        sg-metric-grid, sg-metric
  stat_link           index_live.ex:118–125                    sg-metric-link, sg-metric-link__label, sg-metric-link__value
                      organization_live.ex:169–176
  stat                (no dedicated impl — D-08: markup-consolidation target, deferred Phase 155)
  task_card           index_live.ex:132–144                    sg-card sg-card-hover sg-stack
                      organization_live.ex:183–195
  applied_chip        users_index_live.ex:167–180              sg-applied-chip, sg-applied-chip__remove
                      audit_user_live.ex:137–152
  empty_state         users_index_live.ex:285                  sg-empty-state sg-stack sg-stack--3
                      user_show_live.ex:188,222,252,282
  page_back           user_show_live.ex:90–95                  sg-btn sg-btn--ghost sg-btn--sm
                      audit_user_live.ex:62–67
  scope_ribbon        user_show_live.ex:94 (as sg-muted span)  (no dedicated class — D-07: canonical form Phase 155)
                      audit_user_live.ex:66
  notice              user_show_live.ex:131                    sg-list-row[data-tone] (consolidation target)
                      organization_live.ex:71
  skeleton            app.css lines 1395–1417                  sg-skeleton (CSS defined; no LiveView renders it yet)

Archetype source LiveViews:
  Overview: index_live.ex (global), organization_live.ex (org)
  List:     users_index_live.ex
  Detail:   user_show_live.ex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin-design-contract.md</name>
  <files>guides/reference/admin-design-contract.md</files>

  <read_first>
    - guides/reference/generator-options.md — analog doc shape: H1 title, no frontmatter, tables over prose, .html cross-ref links
    - lib/sigra/admin/live/index_live.ex — global Overview archetype (task_card markup, stat_link markup, archetype structure)
    - lib/sigra/admin/live/organization_live.ex — org Overview (notice at line 71, task_card, stat_link)
    - lib/sigra/admin/live/users_index_live.ex — List archetype (summary_chip at 78–84,336; applied_chip at 167–180; empty_state at 285)
    - lib/sigra/admin/live/user_show_live.ex — Detail archetype (page_back at 90–95; notice at 131; empty_state; scope as sg-muted span at 94)
    - lib/sigra/admin/live/audit_user_live.ex — page_back at 62–67; applied_chip at 137–152; scope span at 66
    - .planning/phases/154-design-contract-sg-notice/154-RESEARCH.md — full 10-component inventory with exact markup; 3 archetype compositions; ARIA table
    - .planning/phases/154-design-contract-sg-notice/154-UI-SPEC.md — ARIA contract table (lines 198–209); copywriting contract; 10-component required-coverage table
  </read_first>

  <action>
    Create guides/reference/admin-design-contract.md. The file follows the same shape as guides/reference/generator-options.md: plain Markdown, H1 title, no YAML frontmatter, Markdown tables for structured data.

    Required top-level structure (per D-03 and UI-SPEC lines 232–244):
      # Sigra Admin Design Contract
      (One-sentence authority statement. Cite REQUIREMENTS.md COMP-03 and note this doc constrains Phases 155–160.)

      ## Job -> Component Mapping
        ### Canonical Component: stat_link
        ### Canonical Component: stat
        ... (all 10, in this order: stat_link, stat, task_card, summary_chip, applied_chip, empty_state, page_back, scope_ribbon, notice, skeleton)

      ## Page Archetypes
        ### Overview Archetype
        ### List Archetype
        ### Detail Archetype

    For each of the 10 components, include a property table with these rows:
      | Property | Value |
      | Job | ... |
      | Winning markup / CSS | ... |
      | ARIA role(s) | ... |
      | Motion spec | ... |
      | When NOT to use | ... |

    Accuracy requirements — use the RESEARCH.md inventory directly, do not invent:

    stat_link: job = numeric KPI deep-link (posture strip); markup = <a class="sg-metric-link"> with sg-metric-link__label + sg-metric-link__value spans (index_live.ex:118–125); ARIA = native <a>; motion = hover box-shadow lift 140ms; NOT for non-navigating stats.

    stat: job = numeric KPI read-only (no navigation); markup = markup-consolidation target — no dedicated impl yet; cite sg-metric-link as closest analog; motion = not animated; include note "Executable form deferred to Phase 155 (COMP-01)." per D-08. Do NOT invent a .sg-stat CSS class.

    task_card: job = verb-first action prompt; markup = <article class="sg-card sg-card-hover sg-stack sg-stack--3"> with h2.sg-section-heading + p.sg-section-copy + a.sg-btn.sg-btn--primary (index_live.ex:132–144); ARIA = <article> + <h2> + <a>; motion = hover lift translateY(-1px) + box-shadow, pointer-device only via @media (hover:hover); NOT animated on keyboard focus; NOT for informational (non-actionable) cards.

    summary_chip: job = aggregate count / boolean-state posture badge (non-interactive); markup = <dl class="sg-metric-grid"><div class="sg-metric"><dt>...</dt><dd>...</dd></div></dl> (users_index_live.ex:78–84); ARIA = definition list semantics; motion = NOT animated (static read-only); NOT for interactive filter state (use applied_chip).

    applied_chip: job = active filter indicator with clear affordance; markup = <span class="sg-applied-chip"> with <a class="sg-applied-chip__remove" aria-label="Remove filter {chip.label}"> + <span aria-hidden="true">&times;</span> (users_index_live.ex:167–180); ARIA = remove link has explicit aria-label; motion = sg-applied-chip__remove color change on hover (--sg-transition-tone); NOT animated on filter-apply (keyboard-frequent, per GATE-03); NOT for non-removable state badges (use summary_chip).

    empty_state: job = dashed-border centred placeholder when list has zero rows; markup = <div class="sg-empty-state sg-stack sg-stack--3"><p class="sg-empty-state__title">...</p></div> (users_index_live.ex:285); ARIA = no additional ARIA; motion = NOT animated (static); NOT for loading states (use skeleton); NOT in non-list contexts.

    page_back: job = single-step back to list screen, consuming return_to param; markup = <a class="sg-btn sg-btn--ghost sg-btn--sm" href={@return_to}> with <span aria-hidden="true">&larr;</span> and descriptive link text (user_show_live.ex:90–95); ARIA = native <a>, arrow is aria-hidden; motion = standard button hover/active transitions, NOT animated on keyboard navigation; NOT for multi-level hierarchy (use breadcrumb for >1 step back); leaf screens only.

    scope_ribbon: job = persistent in-body scope indicator (Global vs org name); markup = currently rendered as <span class="sg-muted sg-text-sm"> alongside page_back (user_show_live.ex:94, audit_user_live.ex:66); include note "Canonical component form deferred to Phase 155 (COMP-01); present on every list and leaf screen." per D-07; ARIA = no additional ARIA (decorative scope copy); motion = NOT animated; NOT for primary navigation; NOT in place of the topbar scope pill (sg-scope-pill is a different element).

    notice: job = block-level contextual alert with semantic tone (ok/warn/risk/info); markup = currently uses .sg-list-row[data-tone] (user_show_live.ex:131, organization_live.ex:71); sg-notice CSS class added in Phase 154 (COMP-04); call-site migration Phase 156 (COHR-05); ARIA = role="alert" for risk (implicit live assertive), role="status" aria-live="polite" for warn/ok/info — applied in Phase 155 HEEx markup (per UI-SPEC ARIA contract table); motion = --sg-transition-tone on base rule; NOT animated on initial render, keyboard navigation, or filter-apply; NOT for form field validation (use sg-error); NOT for persistent navigation context (use scope_ribbon).

    skeleton: job = loading placeholder matching the shape of content it will replace; markup = <div class="sg-skeleton"> (CSS defined app.css:1395–1417; no LiveView renders it yet — Phase 157 adds async overview data per LAND-04); motion = shimmer animation via sg-skeleton-shimmer @keyframes using translateX only (composite-safe); prefers-reduced-motion strips animation via universal rule; NOT for error or empty states (use empty_state or notice).

    For the 3 Page Archetypes section (per RESEARCH.md verified archetype compositions):

    Overview Archetype: Document the composition from index_live.ex / organization_live.ex. Structure: sg-page-header (open, no sg-card wrapper) -> task_card grid (sg-grid sg-grid--3) -> sg-posture-strip card containing needs-review status pill + stat_link strip -> capability section (lowest priority). Note: current needs-review alarm is inside sg-posture-strip (Phase 157 LAND-01 will promote it); no sg-notice in current Overview pages.

    List Archetype: Document from users_index_live.ex. Structure: sg-page-header (OPEN, not boxed — locked winner per COHR-02) -> summary_chip strip (sg-metric-grid in header) -> filter form with quick_filter chips -> applied_chip row (when filters active) -> sg-table-panel (desktop) / sg-show-mobile card stack -> empty_state -> pagination nav. Note scope indicator is shown alongside page header.

    Detail Archetype: Document from user_show_live.ex. Structure: page_back + scope copy (sg-cluster sg-cluster--between) -> Identity card (CURRENTLY boxed sg-card — documented as outlier; COHR-02 locks open sg-page-header as winner, reconciled Phase 156) -> notice (summary alert, sg-list-row data-tone at line 131 — COHR-05 migration target) -> Sessions card -> sg-detail-grid (2-col) -> Organizations card -> Recent Audit card -> Danger Zone -> dialog. Include a note that the boxed Identity card header is the current state and is the COHR-02 reconciliation target for Phase 156.

    Authoring rules (from D-07, D-08):
    - Document current reality. Name locked winners. Do NOT invent new design calls.
    - Open sg-page-header is the locked winner for list/detail headers (cite COHR-02).
    - Do NOT invent a .sg-stat CSS class.
    - Motion spec entries MUST include explicit "not animated" items for keyboard-frequent interactions (seeds Phase 159 GATE-03 audit).
    - Per RESEARCH.md open question: do NOT include .sg-notice__body {} CSS stub — that is a Phase 155 concern.
  </action>

  <verify>
    <automated>
      test -f /Users/jon/projects/sigra/guides/reference/admin-design-contract.md &&
      grep -c "## Job" /Users/jon/projects/sigra/guides/reference/admin-design-contract.md &&
      grep -c "stat_link\|task_card\|summary_chip\|applied_chip\|empty_state\|page_back\|scope_ribbon\|skeleton" /Users/jon/projects/sigra/guides/reference/admin-design-contract.md &&
      grep -c "### Overview Archetype\|### List Archetype\|### Detail Archetype" /Users/jon/projects/sigra/guides/reference/admin-design-contract.md &&
      grep -c "not animated\|NOT animated\|Not animated" /Users/jon/projects/sigra/guides/reference/admin-design-contract.md
    </automated>
  </verify>

  <acceptance_criteria>
    - File exists at guides/reference/admin-design-contract.md
    - grep -c "## Job" returns at least 1
    - A grep for stat_link, task_card, summary_chip, applied_chip, empty_state, page_back, scope_ribbon, skeleton all return hits (all 10 components present)
    - grep -c "### Overview Archetype\|### List Archetype\|### Detail Archetype" returns 3
    - At least 3 "not animated" entries present (motion spec requirement for keyboard-frequent interactions)
    - No line reads ".sg-stat {" — no invented CSS class
    - Content is consistent with RESEARCH.md markup inventory (executor spot-checks stat_link against index_live.ex:118–125 and notice against user_show_live.ex:131)
  </acceptance_criteria>

  <done>guides/reference/admin-design-contract.md exists, covers all 10 components with required property rows, and documents 3 archetypes as component compositions including explicit "not animated" motion entries.</done>
</task>

<task type="auto">
  <name>Task 2: Register in mix.exs ExDoc extras</name>
  <files>mix.exs</files>

  <read_first>
    - mix.exs lines 186–248 — the full extras: list and groups_for_extras:; confirm "guides/reference/generator-options.md" is at line 198 and the Reference regex at line 243 already covers guides/reference/.?
  </read_first>

  <action>
    Edit mix.exs to add one entry to the extras: list. The existing entry at line 198 is:
      "guides/reference/generator-options.md",

    Insert the new entry immediately after line 198 as a new line, so it reads:
      "guides/reference/generator-options.md",
      "guides/reference/admin-design-contract.md",

    This is a single-line insertion. No other mix.exs changes. The groups_for_extras: Reference: ~r{guides/reference/.?} at line 243 already matches the new path — do NOT touch groups_for_extras:.

    The new entry is a bare string (no options map), consistent with every other extras: entry in the list.
  </action>

  <verify>
    <automated>
      grep -n "admin-design-contract" /Users/jon/projects/sigra/mix.exs &&
      cd /Users/jon/projects/sigra && mix docs 2>&1 | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "admin-design-contract" mix.exs returns exactly one hit on the extras: line (no other mentions)
    - The line immediately before it in the file is "guides/reference/generator-options.md", (verified by grep -A1 "generator-options")
    - mix docs exits 0 (ExDoc renders without error; the new guide appears under the Reference group)
    - git diff --name-only shows only mix.exs changed in this task (no other files)
    - grep -c "groups_for_extras" mix.exs diff produces no changed lines (groups_for_extras: is not modified)
  </acceptance_criteria>

  <done>mix.exs extras: contains "guides/reference/admin-design-contract.md" on the line after "guides/reference/generator-options.md", and mix docs exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository artifact | Both files (a Markdown doc and a mix.exs config edit) are committed source artifacts; no runtime behavior, no user input, no HTTP boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; threat not applicable |
| T-154-01-01 | Tampering | mix.exs extras: edit | accept | The extras: list is a build-time config consumed only by mix docs (no runtime impact). Malicious content in admin-design-contract.md would only affect generated hexdocs HTML — not the running library. Low risk; no PII, no secrets, no privilege boundary. |
| T-154-01-02 | Information Disclosure | admin-design-contract.md | accept | The doc describes internal CSS/markup conventions already visible in the open-source repo. No new information is exposed that was not already public. |

**Summary:** This is a pure artifacts-only plan — a Markdown file and a one-line config edit. There are no new attack surfaces, no untrusted input paths, no secrets, no authentication logic, and no privilege boundaries touched. No high-severity threats exist. The only relevant invariant is scope correctness: no LiveView files are modified (verified by git diff gate) and ExDoc registration is valid (verified by mix docs exit 0).
</threat_model>

<verification>
After both tasks complete:

1. File exists: test -f guides/reference/admin-design-contract.md
2. Section count: grep -c "## " guides/reference/admin-design-contract.md (expect ≥13: "Job -> Component Mapping" + 10 component subsections + "Page Archetypes" + 3 archetype subsections)
3. ExDoc registration: grep "admin-design-contract" mix.exs (expect exactly 1 hit)
4. ExDoc build: mix docs (must exit 0)
5. Scope boundary — no LiveView modified: git diff --name-only -- lib/sigra/admin/live/ (expect empty)
6. Scope boundary — no Playwright baselines changed: git diff --name-only -- 'test/example/priv/playwright/tests/**-snapshots/' (expect empty)
7. Scope boundary — only expected files changed: git diff --name-only (expect only guides/reference/admin-design-contract.md and mix.exs plus any .planning/ files)
</verification>

<success_criteria>
- guides/reference/admin-design-contract.md exists and covers all 10 canonical components (each with job, markup/CSS, ARIA, motion spec with "not animated" entries, when-NOT-to-use) and 3 page archetypes as explicit component compositions
- mix.exs extras: list contains "guides/reference/admin-design-contract.md" immediately after "guides/reference/generator-options.md"
- mix docs exits 0
- No LiveView files modified (git diff gate empty)
- No Playwright baselines changed (git diff gate empty)
- COMP-03 requirement satisfied
</success_criteria>

<output>
Create .planning/phases/154-design-contract-sg-notice/154-01-SUMMARY.md when done
</output>
