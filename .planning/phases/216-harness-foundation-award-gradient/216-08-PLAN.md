---
phase: 216-harness-foundation-award-gradient
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/lib/eval/probes.ts
  - test/example/priv/playwright/tests/admin-eval.spec.ts
autonomous: true
gap_closure: true
requirements: [HARNESS-02, HARNESS-03]
user_setup: []

must_haves:
  truths:
    - "Every element-scanning probe queries only within the board root subtree, never document, so no finding references gallery chrome or a sibling board."
    - "The seeded-defect Nyquist tests inject defect nodes inside the board scope and the board-scoped probes still detect them, proving scoping did not blind the probes."
    - "Design-token reads (--sg-* scale from :root) remain global — scoping moves only the element-scan loops, not the token reads (D-12)."
  artifacts:
    - "test/example/priv/playwright/lib/eval/probes.ts (probe engine accepts and threads a board root)"
    - "test/example/priv/playwright/tests/admin-eval.spec.ts (passes '#'+boardId as root; seeded defects injected in-scope)"
  key_links:
    - "runAllProbes(page, { root }) → each of the 9 probes → rootEl.querySelectorAll(...) instead of document.querySelectorAll(...)"
    - "admin-eval.spec.ts captureSurface → runAllProbes(page, { root: '#'+boardId }) matches the board-scoped dom.html capture"
  prohibitions:
    - "MUST NOT switch dom.html / render_sha256 to full-page capture — that would flip every board's SHA when any board changes and destroy per-surface SHA granularity (D-05/D-06, admin-render-sha.json, quality-findings-monotonic.sh depend on it)."
    - "MUST NOT move the --sg-* token reads (probe #1/#5 scale, probe #4 ember color) off :root/document.documentElement — those read design tokens, not findings."
---

<objective>
Close BLOCKER Gap 1 (probe-scope vs DOM-scope mismatch). Thread a board-element root
through the probe engine so all nine probes scan only within the board subtree —
matching the board-scoped `dom.html` outerHTML capture — instead of scanning the
whole `/admin/_design` gallery page. This restores the D-09 "cite-and-flip
impossible by construction" guarantee: every finding's anchor now resolves inside
the already-board-scoped captured DOM.

Root cause (established by verification): 8 of 9 probes hard-code
`document.querySelectorAll('[class*="sg-"]')` and scan the entire page, attributing
gallery chrome (`.sg-nav-link`, `.sg-breadcrumb__item`, `.sg-scope-pill`) and every
OTHER board's elements into the currently-captured single board's bundle. Result:
526/869 (60.5%) of MG-5 findings have anchors absent from its board-scoped `dom.html`,
so `evidence-anchor-check.mjs` exits 1 on every real bundle.

Directed fix (locked, do not re-derive): scope the probes to the board element.
Full-page capture is DISQUALIFIED — all boards share one page, so a full-page
`render_sha256` would flip every board's SHA whenever any board changes.

Purpose: Make evidence-anchor-check able to exit 0 on real bundles (proven live in 216-09).
Output: A board-scoped probe engine + in-scope seeded-defect tests that still pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md
@.planning/phases/216-harness-foundation-award-gradient/216-VERIFICATION.md
@test/example/priv/playwright/lib/eval/probes.ts
@test/example/priv/playwright/tests/admin-eval.spec.ts
@scripts/ci/evidence-anchor-check.mjs
@scripts/ci/lib/eval-probe-ids.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread a board root through the probe engine so every element-scan is board-scoped</name>
  <files>test/example/priv/playwright/lib/eval/probes.ts</files>
  <read_first>
    - test/example/priv/playwright/lib/eval/probes.ts (all 9 probes + runAllProbes; note the exact document.querySelectorAll lines: 91, 146, 200, 262/272, 354, 449 axe, 525, 647)
    - test/example/priv/playwright/tests/admin-eval.spec.ts L239-242 (how runAllProbes is called today) and L313-315 (board outerHTML capture — the scope the probes must match)
    - scripts/ci/evidence-anchor-check.mjs L172-230 (how anchors are resolved against dom.html via cheerio; why absent anchors fail)
    - scripts/ci/lib/eval-probe-ids.mjs (canonical PROBE_IDS — for the D-12 fold in Task 2's warning, keep in mind here)
  </read_first>
  <behavior>
    - Given a board root selector (e.g. '#board-mg-5'), probeOffTokenSpacing scans ONLY '#board-mg-5'-subtree sg-* elements; an off-token element inside a DIFFERENT board (or in gallery chrome) produces NO finding.
    - Given the same root, an off-token element INSIDE '#board-mg-5' still produces a finding whose anchor is a structural selector present in that board's outerHTML.
    - Probe #1 and #5 still read the --sg-space/radius/control-* scale from :root globally (design tokens), independent of the board root.
    - Probe #4 still reads --sg-color-ember* from :root globally; only its sg-* element scan is board-scoped.
    - Probe #6 (axe target-size) constrains analysis to the board subtree via AxeBuilder .include(rootSelector), not whole-page .analyze().
    - Probe #9 (below-fold-primary): board-scoped element scan; because the board is an isolated gallery component (fold is page-level and semantically N/A for gallery boards), it emits no cross-board anchors. Warn-only (D-15) — it must never leave an unresolvable anchor.
    - runAllProbes(page, { root: '#board-mg-5', isGateProject }) forwards the root to all nine probes.
  </behavior>
  <action>
    Add a `root` field to the probe options and thread it into every probe so the element-scan queries the board subtree instead of the whole document. Concretely:

    1. Extend `ProbeRunOptions` and the `runAllProbes` options object with `root?: string` (a CSS selector for the board container, e.g. `'#board-mg-5'`). Keep `boardSelector` for probe #8's existing behavior, but default `boardSelector` to `root` when `root` is provided so #8 also scopes to the board. Default `root` to `':root'`-equivalent whole-document behavior ONLY if a caller omits it (preserve backward-compatible self-tests), but the spec MUST always pass a real board root.

    2. Change the signature of each element-scanning probe (probeOffTokenSpacing, probeMisalignment, probeSizeWeightBudget, probeEmberReservedFor, probeOffScaleRadiusShadowControl, probeFocusRing, probeBelowFoldPrimary) to accept a `root` selector argument, and pass that selector INTO the `page.evaluate(...)` call as an argument. Inside each `page.evaluate`, resolve `const rootEl = root ? document.querySelector(root) : document; if (!rootEl) return [];` and replace `document.querySelectorAll('[class*="sg-"]')` with `rootEl.querySelectorAll('[class*="sg-"]')` (and likewise the interactive-selector scan in #7 and the primary-selector scan in #9).

    3. KEEP the design-token reads global: `document.documentElement` / `getComputedStyle(root).getPropertyValue('--sg-*')` for the space/radius/control scale (probe #1 L73-84, probe #5 L323-347) and the ember color read (probe #4 L266-268) stay exactly as-is. Only the sg-* element-scan loop moves under the board root. Do NOT rename the local `root` variable used for `document.documentElement` in those token reads to collide with the new board-root arg — use a distinct name (e.g. `boardRoot` for the selector arg, keep `root`/`document.documentElement` for tokens) to avoid shadowing.

    4. Probe #4 ember: the reserved-context set (EMBER_RESERVED_SELECTORS querying `document.querySelectorAll(sel)`) may stay document-wide for reserved-context membership, but the CANDIDATE scan (L272) must be board-scoped. Reserved membership is a containment test — scoping candidates to the board is sufficient to eliminate cross-board false anchors.

    5. Probe #6 target-size: replace `new AxeBuilder({ page }).options(...).analyze()` with `new AxeBuilder({ page }).include(boardRoot).options(...).analyze()` when a board root is provided, so axe only evaluates the board subtree. The suppression re-check page.evaluate stays as-is (it queries by the axe-reported node selector, which is already inside the board).

    6. Probe #9 below-fold-primary: board-scope the primary-selector candidate scan (`rootEl.querySelectorAll(primarySelectors)`). Keep the fold read (`document.documentElement.clientHeight`) global — the fold is a viewport property. Since gallery boards are isolated components, this will typically produce zero findings; that is correct and it never emits a cross-board anchor. Add a one-line code comment noting the fold is page-level and below-fold is semantically N/A for isolated gallery-board surfaces (warn-only).

    7. In `runAllProbes`, forward `root` (renamed local, e.g. `boardRoot`) to every probe call, and pass `boardRoot ?? boardSelector` to `probeCardInCard` so #8 also scopes to the board.

    Do NOT capture full-page DOM and do NOT change render_sha256 — board-scoping does not touch the dom.html capture or the hash (see prohibitions). NEVER inline code fences in this action; the identifiers, line anchors, and the .include()/.querySelector() shape above are the spec.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -E 'probes\.ts' && exit 1 || echo "probes.ts typechecks clean")</automated>
  </verify>
  <acceptance_criteria>
    - probes.ts compiles with no type errors attributable to probes.ts.
    - grep shows zero remaining `document.querySelectorAll('[class*="sg-"]')` element-scan candidate loops (the token reads and ember-reserved-context read may still reference document); every candidate loop now uses a board-root-scoped querySelectorAll. Verify with: grep -n "document.querySelectorAll(\['\"]\?\[class" test/example/priv/playwright/lib/eval/probes.ts returns no candidate-loop hits.
    - runAllProbes accepts `root` and forwards it to all nine probes.
    - Token reads (--sg-space/radius/control/ember) still read from document.documentElement / :root.
  </acceptance_criteria>
  <done>Every element-scanning probe scopes to a board root passed by the caller; design-token reads remain global; probes.ts typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Board-scope the spec call + move seeded-defect injections in-scope; prove probes still fire (+ fold D-12 import, add probe #4 seeded test)</name>
  <files>test/example/priv/playwright/tests/admin-eval.spec.ts, test/example/priv/playwright/lib/eval/probes.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-eval.spec.ts L239-242 (runAllProbes call), L291-320 (render-matrix capture per board), L322-545 (seeded-defect + clean-baseline tests that inject on document.body)
    - test/example/priv/playwright/lib/eval/probes.ts (the new `root` option from Task 1; PROBE_IDS array L32-42 duplicated vs scripts/ci/lib/eval-probe-ids.mjs)
    - scripts/ci/lib/eval-probe-ids.mjs (the single-source PROBE_IDS to import)
  </read_first>
  <behavior>
    - Render-matrix capture passes `root: '#'+boardId` into runAllProbes so probe findings for that board reference only that board's DOM.
    - probe #1 seeded test: inject the off-token node INSIDE the board under test (or a test-created board root), pass that root to probeOffTokenSpacing, and the defect is still flagged; an on-scale node in the same scope is not flagged.
    - probe #5, #7, #8 seeded tests: same in-scope injection pattern; each seeded defect is still detected by the board-scoped probe.
    - NEW probe #4 seeded test: inject an ember-misuse node (an `.sg-ember`/ember-classed element OUTSIDE any reserved selected/ownership context) inside the board scope; probeEmberReservedFor flags it (gate severity). A reserved-context ember element in the same scope is NOT flagged.
    - clean-baseline test scans board roots (not the whole document) and still asserts zero gate findings.
  </behavior>
  <action>
    1. In `captureSurface` (spec L239-242), change the runAllProbes call to pass the board root:
       `runAllProbes(page, { isGateProject: gateProject, root: '#' + <boardId in scope> })`.
       captureSurface currently receives `surface` (e.g. 'board-mg-5-populated') but not the raw boardId — thread the boardId (available at the call site L302-317 as `boardId`) into captureSurface as a new parameter and build the root selector `'#'+boardId`. Keep boardSelector defaulting to that root for #8.

    2. For each seeded-defect test (probe #1 L324, #5 L360, #7 L434, #8 L478): instead of appending the injected node to `document.body`, create-or-reuse an in-page board root the probe will scan, append the defect node INSIDE it, and pass that root selector to the probe call. Simplest robust approach: give the injected wrapper a stable id (e.g. `id="probe-scope-root"`) appended to document.body, put the defect node inside it, and call the probe with `root: '#probe-scope-root'`. This keeps the defect within the scanned scope. Update the clean-element assertions the same way (inject the clean node inside the same root). Update cleanup to remove the wrapper.

    3. Add a NEW seeded-defect test for probe #4 (ember-reserved-for, gate-severity) mirroring the pattern: inject an ember-misuse element (class includes `ember`, e.g. `sg-ember`, NOT inside any `[data-selected]`/`[aria-selected]`/`[data-owned]`/`[data-tone="ember"]` reserved context) inside `#probe-scope-root`; assert probeEmberReservedFor returns a finding for it with severity 'gate'. Then inject a reserved-context ember element (`[data-selected="true"]` wrapping an ember element) in the same root and assert it is NOT flagged. Clean up.

    4. Update the clean-baseline test (L521-545) to scan board roots rather than whole-document: it already scopes probeCardInCard to the board ids; scope probeOffTokenSpacing and probeFocusRing to the board roots too by calling them per-board with `root: '#'+id` (or by passing a combined board-root selector if the new `root` option accepts a group — if it only accepts a single element root, iterate GROUP_BOARDS and aggregate). Assert zero gate findings as before.

    5. D-12 fold (WARNING remediation): make probes.ts import PROBE_IDS from the single source `scripts/ci/lib/eval-probe-ids.mjs` instead of duplicating the array (probes.ts L32-42). If the import path/module-interop does not cleanly resolve inside the Playwright CJS transform without risk to the scoping change, DO NOT force it — instead leave a `// FOLLOW-UP(216): import PROBE_IDS from scripts/ci/lib/eval-probe-ids.mjs (D-12 single-source)` marker and note it in the SUMMARY as deferred. Do not entangle the D-12 fold with the scoping fix.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -E 'admin-eval\.spec\.ts|probes\.ts' && exit 1 || echo "spec + probes typecheck clean")</automated>
  </verify>
  <acceptance_criteria>
    - admin-eval.spec.ts render-matrix capture passes a board root ('#'+boardId) into runAllProbes.
    - Every seeded-defect test injects its defect node WITHIN the scanned board scope and still asserts the probe fires (`toBeGreaterThan(0)`); on-scale/reserved counterparts still assert zero.
    - A new probe #4 (ember-reserved-for) seeded-defect test exists and asserts a gate finding on ember misuse.
    - clean-baseline test scans board roots and asserts zero gate findings.
    - spec + probes typecheck clean.
    - D-12: either probes.ts imports PROBE_IDS from eval-probe-ids.mjs, OR a FOLLOW-UP marker is present and the deferral is noted in the SUMMARY.
  </acceptance_criteria>
  <done>The spec drives board-scoped probes, seeded defects are injected in-scope and still detected (including a new probe #4 test), the clean baseline passes board-scoped, and the D-12 duplication is either folded or explicitly deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered board DOM → probe findings | probes read live DOM/computed-style; findings become evidence anchors |
| findings.json → evidence-anchor-check | anchors are resolved via cheerio against captured dom.html |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-08-01 | Tampering | Board-scoping could accidentally blind a probe (false-negative), letting a real defect pass unflagged | high | mitigate | Seeded-defect Nyquist tests inject WITHIN the board scope and assert every scoped probe still fires; a blinded probe fails its seeded test. |
| T-216-08-02 | Information disclosure | Cross-board/gallery-chrome anchors leaking into a board's findings (the exact bug) | high | mitigate | Every element-scan now queries the board root subtree only; verified by evidence-anchor-check exiting 0 on real bundles in 216-09. |
| T-216-08-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan (parse5/cheerio/axe already vetted in 216-01). |
</threat_model>

<verification>
- probes.ts and admin-eval.spec.ts typecheck clean (no probes.ts/spec type errors).
- No element-scan candidate loop references `document.querySelectorAll('[class*="sg-"]')`; every candidate loop is board-root-scoped.
- Design-token reads (--sg-* scale, ember color) still read from :root / document.documentElement.
- Seeded-defect tests inject in-scope and still detect defects (proof scoping did not blind probes) — asserted by the spec; full run executed in 216-09.
</verification>

<success_criteria>
- Gap 1 root cause removed: probe scan scope == board dom.html capture scope.
- All nine probes accept and honor a board root; token reads remain global.
- Seeded-defect coverage still green in-scope, plus a new probe #4 seeded test.
- Ready for 216-09 to prove evidence-anchor-check exits 0 on freshly-rendered real bundles.
</success_criteria>

<artifacts_produced>
- test/example/priv/playwright/lib/eval/probes.ts — board-root-scoped probe engine (9 probes + runAllProbes).
- test/example/priv/playwright/tests/admin-eval.spec.ts — board-scoped runAllProbes call; in-scope seeded-defect tests incl. new probe #4; board-scoped clean baseline.
</artifacts_produced>

<output>
Create `.planning/phases/216-harness-foundation-award-gradient/216-08-SUMMARY.md` when done
</output>
