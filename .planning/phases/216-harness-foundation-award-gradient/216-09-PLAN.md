---
phase: 216-harness-foundation-award-gradient
plan: 09
type: execute
wave: 2
depends_on: [216-08]
files_modified:
  - guides/reference/admin-render-sha.json
  - guides/reference/admin-award-ledger.json
autonomous: true
gap_closure: true
requirements: [HARNESS-01, HARNESS-02, RATCHET-01, RATCHET-02]
user_setup: []

must_haves:
  truths:
    - "evidence-anchor-check.mjs exits 0 when run against freshly-rendered real bundles at current HEAD, and admin-eval-harness.sh completes with all 5 guards green."
    - "The live harness run used a trusted example server with correct compile-time PORT/PG provenance — no stale/unknown server owned the port."
    - "render_sha256 values are stable under board-scoping (capture is already board-scoped); admin-render-sha.json only changes if a SHA legitimately shifts, and any shift is investigated before commit."
  artifacts:
    - "Captured command output evidencing admin-eval-harness.sh exit 0 with real bundles present under eval/<HEAD-sha>/"
    - "guides/reference/admin-render-sha.json (regenerated only if SHAs legitimately changed)"
    - "guides/reference/admin-award-ledger.json (verified-at-sha refreshed only if a legitimate change occurred)"
  key_links:
    - "trusted example server (PORT=4011, MIX_ENV, live PG) → admin-eval-harness.sh → 3 playwright projects → eval/<HEAD-sha>/ bundles → evidence-anchor-check.mjs exit 0"
    - "admin-eval-harness.sh phases (b1..b5) → all 5 guards exit 0 end-to-end"
  prohibitions:
    - "MUST NOT accept a self-test pass as proof of SC-5 — the acceptance criterion is a real harness run producing real bundles and exiting 0, evidenced by captured output."
    - "MUST NOT trust harness output produced against a server of unknown provenance (a stale server on port 4011). Verify server ownership/provenance before trusting bundles."
    - "MUST NOT switch dom.html/render_sha256 to full-page capture (breaks per-surface SHA granularity)."
---

<objective>
Close BLOCKER Gap 2 (SC-5 "proven end-to-end, zero guard regressions" was never
verified with trusted provenance). With Gap 1 fixed in 216-08, boot a trusted
example server and run `scripts/ci/admin-eval-harness.sh` end-to-end at current HEAD,
regenerating real bundles, and CONFIRM `evidence-anchor-check.mjs` exits 0 on those
real bundles AND all 5 harness guards pass. Refresh the committed render-SHA / award
ledgers only if a SHA legitimately shifted (board-scoping does not change render_sha256,
which is already board-scoped — so SHAs should be stable; investigate any change).

The prior SUMMARY admitted the live render was never re-run with trusted provenance
(a stale unknown Phoenix server occupied port 4011). Self-tests were green while the
REAL run failed. Therefore the acceptance gate for this fix is an ACTUAL live harness
run with captured output — not a self-test pass.

Purpose: Make SC-5 observably true — the render-probe-ratchet loop is green end-to-end.
Output: Captured harness exit-0 evidence + any legitimately-updated committed ledgers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-harness-foundation-award-gradient/216-VERIFICATION.md
@CLAUDE.md
@scripts/ci/admin-eval-harness.sh
@scripts/ci/evidence-anchor-check.mjs
@guides/reference/admin-eval-runbook.md
@guides/reference/admin-render-sha.json
@guides/reference/admin-award-ledger.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot a trusted example server and run admin-eval-harness.sh end-to-end; capture exit-0 with real bundles</name>
  <files>guides/reference/admin-render-sha.json, guides/reference/admin-award-ledger.json</files>
  <read_first>
    - scripts/ci/admin-eval-harness.sh (the 5-guard orchestrator; boot expectation: consume already-booted SIGRA_EXAMPLE_URL default http://localhost:4011; phase b2 = evidence-anchor-check)
    - scripts/ci/evidence-anchor-check.mjs (walks eval/<app_git_sha>/**; exits 1 if any anchor absent)
    - CLAUDE.md "Local development prerequisites" (scripts/db/up.sh ephemeral test PG on dynamic port + tmp/db.env; the compile-time PORT gotcha)
    - guides/reference/admin-eval-runbook.md (documented local iteration recipe: scripts/db/up.sh → boot example on PORT=4011 → run harness)
    - MEMORY notes on admin Playwright boot: compile-time PORT/PG provenance, pre-compile to avoid code-reload crash, a stale server may already own 4011 (verify before trusting output)
  </read_first>
  <action>
    Run the full render-probe-ratchet loop against a TRUSTED server and prove it green.

    1. Ensure a clean, trusted server on the harness URL. First verify NOTHING of unknown provenance owns port 4011: check `lsof -ti tcp:4011` (or the platform equivalent). If an unknown process holds it, either stop it or boot the example on a different free port and export `SIGRA_EXAMPLE_URL=http://localhost:<port>` (the harness honors SIGRA_EXAMPLE_URL). Record which port you used and that you own the process.

    2. Boot the test database and the example app with correct compile-time provenance per CLAUDE.md + runbook:
       - `bash scripts/db/up.sh` (ephemeral test PG, writes tmp/db.env), then source tmp/db.env (or `direnv allow`).
       - Pre-compile the example to avoid the code-reload crash: from test/example run a compile step (e.g. `MIX_ENV=dev PORT=<port> mix compile`) BEFORE starting the server, because the compile-time PORT is baked in (validate_compile_env aborts on a stale build with a different port — see host-run compile-env memory).
       - Start the server in the background: `MIX_ENV=dev PORT=<port> mix phx.server` and health-gate the URL (poll until it responds) before running the harness. Register a user path is handled inside the spec (adminEvalEmail prefix).

    3. Run the harness end-to-end at current HEAD and CAPTURE the full output to a log:
       `SIGRA_EXAMPLE_URL=http://localhost:<port> bash scripts/ci/admin-eval-harness.sh 2>&1 | tee /tmp/216-09-harness.log`
       The harness runs phase (a) render matrix across 3 projects, then phases (b1..b5): stale-render-guard, evidence-anchor-check, quality-findings-monotonic, award-guard, settled-findings-lint. It must print "admin-eval-harness: PASS — all phases green" and exit 0.

    4. Independently confirm the decisive guard on the freshly-produced real bundles:
       `node scripts/ci/evidence-anchor-check.mjs 2>&1 | tee -a /tmp/216-09-harness.log` → must print PASS and exit 0. Confirm real bundles exist under `test/example/priv/playwright/eval/$(git rev-parse HEAD)/` (non-empty; dom.html + findings.json per cell).

    5. Ledger integrity: board-scoping does NOT change render_sha256 (the dom.html capture was already board-scoped), so `guides/reference/admin-render-sha.json` SHAs should be STABLE. Compare freshly-produced bundle render_sha256 values against the committed admin-render-sha.json. If they match → do not modify the ledgers (leave them as-is). If any SHA legitimately shifted → INVESTIGATE why before committing (a shift means the captured DOM changed; confirm it is a real, intended change and not a scoping regression), then regenerate admin-render-sha.json and refresh admin-award-ledger.json verified_at_sha via the runbook's documented regen path. Do not blindly overwrite.

    6. If the harness fails at any phase, treat it as an unclosed gap: capture the failing output, diagnose (most likely a residual unscoped probe path from 216-08 or a boot/provenance issue), fix within scope, and re-run until green. Do NOT declare success on a self-test.

    Attach the captured /tmp/216-09-harness.log evidence (exit code + PASS line + bundle path) to the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/ci/evidence-anchor-check.mjs; test -d "test/example/priv/playwright/eval/$(git rev-parse HEAD)"</automated>
  </verify>
  <acceptance_criteria>
    - OBSERVABLE (not a self-test): `scripts/ci/admin-eval-harness.sh` was run against a trusted server and printed "admin-eval-harness: PASS — all phases green" and exited 0, evidenced by captured /tmp/216-09-harness.log in the SUMMARY.
    - Real bundles exist under test/example/priv/playwright/eval/<HEAD-sha>/ (dom.html + findings.json present per cell).
    - `node scripts/ci/evidence-anchor-check.mjs` exits 0 on those real bundles (PASS printed).
    - All 5 harness guards (b1..b5) passed in the same run.
    - Server provenance was verified (no stale/unknown process owned the port; the executor owned the booted process/port).
    - admin-render-sha.json / admin-award-ledger.json are unchanged UNLESS a SHA legitimately shifted, in which case the shift was investigated and the regen documented in the SUMMARY.
  </acceptance_criteria>
  <done>The full harness exits 0 against a trusted server with freshly-rendered real bundles, evidence-anchor-check passes on those bundles, and the committed ledgers are correct (stable or legitimately-updated). SC-5 is observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booted example server → captured bundles | server provenance determines whether bundles/anchors are trustworthy |
| freshly-rendered bundles → committed ledgers | render_sha256 values feed admin-render-sha.json + monotonic guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-09-01 | Spoofing | Bundles rendered against a stale/unknown server (the original SC-5 failure) — untrustworthy provenance | high | mitigate | Verify no unknown process owns the harness port; boot with known compile-time PORT/PG; the executor owns the booted process before trusting output. |
| T-216-09-02 | Tampering | Committing shifted render SHAs without investigating (masking a scoping regression) | high | mitigate | SHAs must be stable under board-scoping; any shift is investigated and justified before regen/commit, never blindly overwritten. |
| T-216-09-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs — parse5/cheerio/axe already vetted in 216-01. |
</threat_model>

<verification>
- admin-eval-harness.sh prints "PASS — all phases green" and exits 0 against a trusted server (captured log).
- evidence-anchor-check.mjs exits 0 on freshly-rendered real bundles at HEAD.
- Real bundles present under eval/<HEAD-sha>/ with dom.html + findings.json.
- admin-render-sha.json SHAs stable (or any change investigated + documented).
</verification>

<success_criteria>
- SC-5 observably true: two-pilot render-probe-ratchet loop completes end-to-end with zero guard regressions, proven by a real harness run (not self-tests).
- HARNESS-02 evidence-anchor integrity holds on real bundles ("cite-and-flip impossible by construction" restored).
- Committed ledgers remain correct.
</success_criteria>

<artifacts_produced>
- Captured harness run evidence (/tmp/216-09-harness.log referenced in SUMMARY) proving exit 0 with real bundles.
- guides/reference/admin-render-sha.json — regenerated only if a SHA legitimately shifted (else unchanged).
- guides/reference/admin-award-ledger.json — verified_at_sha refreshed only on a legitimate change (else unchanged).
</artifacts_produced>

<output>
Create `.planning/phases/216-harness-foundation-award-gradient/216-09-SUMMARY.md` when done
</output>
