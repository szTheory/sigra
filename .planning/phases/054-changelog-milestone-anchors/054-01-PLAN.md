---
phase: 54
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - PUB-02
must_haves:
  truths:
    - "**`CHANGELOG.md`** uses **only** `## [Unreleased]` and **`## [0.x.y] - YYYY-MM-DD`** as version headings — **no** `## [v1.4]`-style headings implying a parallel Hex axis."
    - "Each shipped **`[0.1.0]`** and **`[0.2.0]`** release includes a **`### Roadmap traceability`** subsection whose **dates and archive paths** match **`.planning/MILESTONES.md`** for **v1.2** / **v1.3** respectively."
    - "**`[Unreleased]`** includes a **`### Roadmap traceability`** block that names **v1.4** as a **planning milestone** (not a Hex semver) and points to **`.planning/MILESTONES.md`** and **`.planning/milestones/v1.4-*`** / **`.planning/v1.4-GA-UAT.md`** without contradicting shipped dates in **`MILESTONES.md`**."
    - "A short glossary section titled **`## Planning milestones vs Hex releases`** appears after the SemVer intro and before **`## [Unreleased]`**."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "PUB-02 milestone anchors + honest dual-axis story for integrators"
nyquist_compliant: false
---

<objective>
Implement **PUB-02**: reshape **`CHANGELOG.md`** so Hex semver lines stay canonical, **planning milestones v1.2–v1.4** are explicitly anchored (traceability blocks + glossary), and wording cannot contradict **`.planning/MILESTONES.md`**, per **`054-CONTEXT.md` D-01–D-20`** and **`054-RESEARCH.md`**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-54-01 | Spoofing / misrepresentation | Readers infer **`v1.4`** is an installable Hex range | high | Grep forbid `## [v1.` headings; glossary states **0.x.y = Hex**, **v1.x = planning** only |
| T-54-02 | Repudiation | Changelog dates or archive links disagree with **`MILESTONES.md`** | medium | Executor reads **`MILESTONES.md`** first; traceability bullets copy **Shipped:** dates verbatim |
| T-54-03 | Information disclosure | Over-claiming GA / audit certification in changelog prose | low | Traceability links only; no SOC/waiver/certification language (**grep** forbid list) |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add planning-milestones glossary</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/phases/054-changelog-milestone-anchors/054-CONTEXT.md
    - .planning/MILESTONES.md
  </read_first>
  <action>
    Immediately **after** the line `and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).` and **before** the line `## [Unreleased]`, insert a **blank line** then the following **exact** markdown block (preserve line breaks):

    ```markdown
    ## Planning milestones vs Hex releases

    This changelog uses **[Semantic Versioning](https://semver.org/spec/v2.0.0.html)** headings like **`[0.2.0]`** for **published Hex releases**. Separately, maintainers track **planning milestones** labeled **v1.0–v1.4** in **`.planning/MILESTONES.md`** — those **v1.x** labels describe shipped *tranches* of work, **not** a second installable version axis on Hex (this repo remains **0.x** on Hex until a real **1.0.0**). Each dated release below may include a **`### Roadmap traceability`** subsection linking back to the milestone narrative. When in doubt, treat **`MILESTONES.md`** as canonical for dates and archive paths.
    ```
  </action>
  <acceptance_criteria>
    - `grep -nF '## Planning milestones vs Hex releases' CHANGELOG.md` exits 0 and prints a line number **less than** the line number from `grep -nF '## [Unreleased]' CHANGELOG.md | head -1`.
    - `grep -F '**not** a second installable version axis on Hex' CHANGELOG.md` exits 0.
    - `grep -F '.planning/MILESTONES.md' CHANGELOG.md` exits 0.
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Add Roadmap traceability under 0.1.0, 0.2.0, and Unreleased</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/MILESTONES.md
    - .planning/phases/054-changelog-milestone-anchors/054-CONTEXT.md
  </read_first>
  <action>
    1. Under **`## [0.1.0] - 2026-04-17`**, after the one-line preamble paragraph (`First library version line...`) and **before** the first existing `### ...` heading, insert:

    ```markdown
    ### Roadmap traceability

    Planning milestone **v1.2** (admin dashboard tranche; **not** a Hex version): shipped **2026-04-17** per `.planning/MILESTONES.md` — see `.planning/milestones/v1.2-ROADMAP.md`, `.planning/milestones/v1.2-REQUIREMENTS.md`, and `.planning/milestones/v1.2-MILESTONE-AUDIT.md`.
    ```

    2. Under **`## [0.2.0] - 2026-04-19`**, after the date line and **before** the first existing `### ...` heading, insert:

    ```markdown
    ### Roadmap traceability

    Planning milestone **v1.3** (cleanup & hardening tranche; **not** a Hex version): shipped **2026-04-19** per `.planning/MILESTONES.md` — see `.planning/milestones/v1.3-ROADMAP.md`, `.planning/milestones/v1.3-REQUIREMENTS.md`, and `.planning/milestones/v1.3-MILESTONE-AUDIT.md`.
    ```

    3. Under **`## [Unreleased]`**, **after** the last bullet that is part of the rolling unreleased notes and **before** the `## [0.2.0]` heading, insert:

    ```markdown
    ### Roadmap traceability

    Planning milestone **v1.4** (GA readiness & audit trail completeness; **not** a Hex version): shipped **2026-04-22** per `.planning/MILESTONES.md` — see `.planning/milestones/v1.4-ROADMAP.md`, `.planning/milestones/v1.4-REQUIREMENTS.md`, `.planning/milestones/v1.4-MILESTONE-AUDIT.md`, and the GA matrix framing in `.planning/v1.4-GA-UAT.md` (Executed / Waived language; do not duplicate the matrix here).
    ```

    If bullets already exist below `[Unreleased]`, keep **all** existing bullets; place the new `### Roadmap traceability` subsection **after** those bullets (still before `## [0.2.0]`).
  </action>
  <acceptance_criteria>
    - `grep -c '### Roadmap traceability' CHANGELOG.md` outputs **`3`** when run as: `test "$(grep -c '### Roadmap traceability' CHANGELOG.md)" -eq 3`; equivalently `grep -c '### Roadmap traceability' CHANGELOG.md` prints `3`.
    - `grep -F 'Planning milestone **v1.4**' CHANGELOG.md` exits 0.
    - `grep -F 'shipped **2026-04-22**' CHANGELOG.md` exits 0.
    - `grep -Ei '## \[v1\.' CHANGELOG.md` exits **1** (no `## [v1.x]` pseudo-version headings).
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Reorder 0.1.0 Keep a Changelog sections (risk-weighted)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/phases/054-changelog-milestone-anchors/054-CONTEXT.md
  </read_first>
  <action>
    Within **`## [0.1.0] - 2026-04-17`**, reorder top-level `###` sections so the **display** order is exactly:

    1. `### Changed` (keep all existing **Changed** bullets, with **BREAKING** bullets first inside that section)
    2. `### Fixed` (the single Hex `files` list bullet)
    3. `### Added` (all existing **Added** bullets)

    Do **not** edit bullet text except moving whole bullet blocks between sections. Preserve the **`### Roadmap traceability`** block immediately under the `## [0.1.0]` heading (before `### Changed`).
  </action>
  <acceptance_criteria>
    - From repo root: `diff -q <(printf '%s\n' '### Roadmap traceability' '### Changed' '### Fixed' '### Added') <(sed -n '/^## \[0.1.0\]/,/^## \[0.2.0\]/p' CHANGELOG.md | grep '^###' | head -4)` exits 0 (proves the first four `###` headings in the **0.1.0** section are **Roadmap traceability → Changed → Fixed → Added**).
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 4: Keep a Changelog compare links footer</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - mix.exs
  </read_first>
  <action>
    Append a **final newline** after the last content line of `CHANGELOG.md`, then append this **exact** reference-style compare block (URLs use **`https://github.com/sztheory/sigra`** — must match **`@source_url`** in **`mix.exs`**):

    ```markdown
    [Unreleased]: https://github.com/sztheory/sigra/compare/v0.2.0...HEAD
    [0.2.0]: https://github.com/sztheory/sigra/compare/v0.1.0...v0.2.0
    [0.1.0]: https://github.com/sztheory/sigra/releases/tag/v0.1.0
    ```
  </action>
  <acceptance_criteria>
    - `grep -F '[Unreleased]: https://github.com/sztheory/sigra/compare/v0.2.0...HEAD' CHANGELOG.md` exits 0.
    - `grep -F '[0.2.0]: https://github.com/sztheory/sigra/compare/v0.1.0...v0.2.0' CHANGELOG.md` exits 0.
    - `grep -F '[0.1.0]: https://github.com/sztheory/sigra/releases/tag/v0.1.0' CHANGELOG.md` exits 0.
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits 0.
  </verify>
</task>

</tasks>

<verification>
- `grep -Ei 'soc2|pen[- ]?test|audit[- ]certified' CHANGELOG.md` exits **1** unless a **Security**-classified finding truly exists (none expected for this edit).
- `mix compile --warnings-as-errors` exits 0.
- Spot-check: open **`.planning/MILESTONES.md`** next to **`CHANGELOG.md`** traceability paragraphs — **v1.2**, **v1.3**, **v1.4** **Shipped** dates match (**2026-04-17**, **2026-04-19**, **2026-04-22**).
</verification>

<success_criteria>
- **PUB-02** satisfied: v1.3 / v1.4 (and prior lines via **0.1.0 → v1.2**) have deliberate anchors; no contradiction vs **`MILESTONES.md`** on inspection.
- CI/docs unchanged or greener: only **`CHANGELOG.md`** modified in this phase’s plans.
</success_criteria>
