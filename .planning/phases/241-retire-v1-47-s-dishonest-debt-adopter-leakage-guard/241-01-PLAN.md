---
phase: 241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md
  - test/support/ci/ex_unit_timing_formatter.ex
  - test/support/ci/ex_unit_timing_formatter_test.exs
  - .planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-01-EVIDENCE.md
autonomous: true
requirements: [DEBT-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md` exists and records that TEST-01/TEST-02 are superseded by the single-owner `mix ci` topology, why, and what the replacement guarantee is (per D-04, the number and content `PITFALLS.md:303` already prescribes)."
    - "The ADR names **all ten** refs carrying a live non-`test/support` consumer of `SIGRA_EXUNIT_TIMING_PATH` — not only the parked `235.1` family but `gsd/phase-232-playwright-economics` as well (D-03) — each listed with the paths that would fail to compile after the deletion."
    - "`test/support/ci/ex_unit_timing_formatter.ex` and `test/support/ci/ex_unit_timing_formatter_test.exs` are deleted and **nothing else** is deleted (D-01)."
    - "A repo-wide grep for `ExUnitTimingFormatter` and `SIGRA_EXUNIT_TIMING`, excluding `_build/ deps/ node_modules/ .git/ .gsd/ .planning/`, returns zero hits after the deletion."
    - "`mix.exs`'s `ci:` alias is byte-unchanged and `test/sigra/planning/phase_233_library_economics_contract_test.exs`'s alias test still passes (D-02) — the test file is not edited by this plan."
    - "The `mix ci` green claim is recorded as a CI claim, not a local one, and the 6 environmental `Sigra.Audit.Forwarders.ThreadlineTest` failures are named in writing rather than excluded from the suite (D-05)."
  artifacts:
    - .planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md
    - .planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-01-EVIDENCE.md
  key_links:
    - "The ADR's `## Replacement guarantee` section is the text plan 241-02 implements as an assertion. 241-02 `depends_on: [241-01]` exists for exactly this reason — the guarantee is written down first, then asserted, never inferred backwards from whatever the rewritten test happens to say."
    - "`mix.exs:55` (`elixirc_paths(:test)` includes `test/support`) is why the module compiled at all; removing one `.ex` from that tree changes nothing else (D-02)."
  prohibitions:
    - "MUST NOT edit `mix.exs`'s `ci:` alias or `test/sigra/planning/phase_233_library_economics_contract_test.exs`. That test is re-run as **evidence** for SC-1, never modified (D-02). Editing it reopens the exact v1.47 wound this milestone closes."
    - "MUST NOT delete a third file. The sweep proved the blast radius is exactly two files; anything beyond that is unmeasured."
---

<objective>
Record ADR 004 — TEST-01/TEST-02 superseded by the single-owner `mix ci` topology — and delete the
orphaned `Sigra.CI.ExUnitTimingFormatter` and its test, with the cross-ref consumer sweep run and
embedded so the deletion is a recorded decision rather than silent data loss.

Purpose: SC-1. A formatter no alias names is a guard that asserts nothing; deleting it without the
ADR converts ten branches' worth of live re-wiring commits into silent cherry-pick failures.
Output: one ADR, two deletions, one evidence file.

### Decisions implemented by this plan

- **D-01:** the deletion is exactly `test/support/ci/ex_unit_timing_formatter.ex` and
  `test/support/ci/ex_unit_timing_formatter_test.exs` and nothing else; Task 1 re-proves the
  two-file blast radius by command before removing anything.
- **D-02:** the `mix ci` alias is never edited and `phase_233_library_economics_contract_test.exs`
  is not touched by this plan — its alias test is re-run as evidence in Task 3.
- **D-03:** the ADR's affected-consumers list carries all ten refs the sweep finds, including the
  `gsd/phase-232-playwright-economics` family D-03 does not itself name.
- **D-04:** the ADR is `.planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md`,
  written in the committed `001`/`003` convention: `# ADR 004: …` H1 plus bold `**Status:** /
  **Date:** / **Context:**` lines, no YAML frontmatter.
- **D-05:** the green claim is made against CI at the phase's final HEAD, not against a local
  `mix ci`; the 6 environmental `Sigra.Audit.Forwarders.ThreadlineTest` failures
  (`Threadline.attach/1 undefined`) are named in the evidence file and are **not** excluded from
  the suite.
</objective>

<!-- planner-discipline-allow: ExUnitTimingFormatter -->
<!-- planner-discipline-allow: SIGRA_EXUNIT_TIMING -->

Both literals above are named verbatim in this plan's `<action>` bodies **and** negative-grepped by
Task 1's `<verify>`. That is safe rather than self-invalidating: the verification grep passes
`--exclude-dir=.planning`, and this plan file lives under `.planning/`, so the plan text is outside
the scanned surface by construction.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-CONTEXT.md
@.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-RESEARCH.md
@.planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — sweep, record, delete, prove clean</name>
  <files>.planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md, test/support/ci/ex_unit_timing_formatter.ex, test/support/ci/ex_unit_timing_formatter_test.exs</files>
  <read_first>
    Read `.planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md` for the ADR
    convention (D-04) and `.planning/research/PITFALLS.md` around the "write an ADR (004)" line —
    locate it **by content**, not by the `:303` citation, per the phase's standing re-location rule.
  </read_first>
  <action>
    One end-to-end slice: measure, record, delete, re-measure.

    Step A — run the cross-ref consumer sweep and capture its output verbatim. Iterate every local
    and remote ref via `git for-each-ref --format="%(refname)" refs/heads refs/remotes`, `git grep -l
    SIGRA_EXUNIT_TIMING_PATH "$ref"` on each, filtering out `test/support` paths and `.planning`
    paths. Expect ten refs (D-03). If the count differs from ten at execution HEAD, record the
    actual set — the ADR lists what the sweep finds, never what the plan predicted.

    Step B — re-prove the two-file blast radius (D-01) before deleting: a repo-wide grep for
    `ExUnitTimingFormatter` and `SIGRA_EXUNIT_TIMING` excluding `_build/ deps/ node_modules/ .git/
    .gsd/ .planning/` must return exactly the two files named in `<files>`. Stale `_build/*.beam`
    artifacts are not source and are excluded by the filter.

    Step C — write `.planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md` in the
    `001`/`003` convention (D-04): `# ADR 004: TEST-01/TEST-02 are superseded by the single-owner
    `mix ci` topology` as H1, then bold `**Status:** Accepted`, `**Date:**`, `**Context:** Phase 241
    (v1.48 CLEAN-BASELINE) — …`. Sections: the problem (a per-test timing formatter that no alias
    leg ever names is a measurement apparatus with no measurement); `## Decision`; `## Replacement
    guarantee` — state in one paragraph that the guarantee the `mix ci` topology provides is
    **exactly one owner of the full library suite**, and that plan 241-02 asserts it as a derived
    property rather than as a restatement of HEAD's `ci.yml` shape; `## Affected refs` — the sweep
    output from Step A as a table of ref to affected paths, with the sentence that a cherry-pick
    from any of them will fail `compile --warnings-as-errors` against a deleted module for a reason
    unrelated to its own diff; `## Consequences`; `## Revisit triggers`.

    Step D — delete the two files with `git rm`. Nothing else.

    Step E — re-run the Step B grep. It must now return zero.

    Amendments to this ADR, if ever needed, follow the in-place dated-strikethrough retraction style
    the committed `003` uses; do not silently edit it.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; test -f .planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md; test ! -e test/support/ci/ex_unit_timing_formatter.ex; test ! -e test/support/ci/ex_unit_timing_formatter_test.exs; n=$(grep -rIl -e ExUnitTimingFormatter -e SIGRA_EXUNIT_TIMING . --exclude-dir=_build --exclude-dir=deps --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.gsd --exclude-dir=.planning | wc -l | tr -d " "); test "$n" = "0" || { echo "still referenced in $n file(s)"; exit 1; }'</automated>
  </verify>
  <done>ADR 004 exists in the committed convention and carries the sweep output; both formatter files are gone; a source-tree grep for the module and its env var returns zero.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the alias and the compile are untouched</name>
  <files>(no files modified — verification only)</files>
  <action>
    Prove D-02 by command rather than by claim. Confirm `mix.exs`'s `ci:` alias is byte-unchanged
    against `git show HEAD~:mix.exs` for the alias region, that
    `test/sigra/planning/phase_233_library_economics_contract_test.exs` has no diff in this plan's
    commits, and that the alias test in that file still passes. Then confirm the tree still compiles
    under the alias's own strictness — `MIX_ENV=test mix compile --warnings-as-errors` — which is
    what catches the class of error a careless deletion produces (an orphaned reference or an unused
    helper left behind).

    Run everything through `bash -c`; zsh has produced a confident false negative in this repo.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; git diff --name-only "$(git merge-base HEAD origin/main)" -- mix.exs test/sigra/planning/phase_233_library_economics_contract_test.exs | tee /dev/stderr | wc -l | grep -qx "0"; MIX_ENV=test mix compile --warnings-as-errors; mix test test/sigra/planning/phase_233_library_economics_contract_test.exs'</automated>
  </verify>
  <done>`mix.exs` and `phase_233_library_economics_contract_test.exs` are untouched by this plan's diff, the tree compiles warnings-free, and the alias contract test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Record the SC-1 evidence, including the honest green claim</name>
  <files>.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-01-EVIDENCE.md</files>
  <action>
    Write the plan-scoped evidence file. Per-plan evidence files (`241-NN-EVIDENCE.md`) are
    deliberate: four plans in this phase run in the same wave, and one shared `241-EVIDENCE.md`
    would be a write collision.

    Record, each as the literal `bash -c` invocation plus its captured output: the Step A sweep, the
    before/after Step B/E greps, the compile, and the alias contract-test run. Then record the green
    claim per D-05 in these words rather than a bare "green": the full-suite claim for SC-1 is made
    against **CI** at this phase's final committed HEAD; a local `MIX_ENV=test mix ci` currently
    carries 6 environmental `Sigra.Audit.Forwarders.ThreadlineTest` failures
    (`Threadline.attach/1 undefined`) that are unrelated to this phase and are green in CI at the
    same commit. Those tests are **not** excluded from the suite and no `--exclude` is added; the
    failure mode is named instead of hidden. State the CI run URL once it exists, or mark it
    `PENDING — filled at phase verification` if the push has not happened yet.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-01-EVIDENCE.md; test -f "$f"; grep -q "bash -c" "$f"; grep -q "ThreadlineTest" "$f"; grep -q "SIGRA_EXUNIT_TIMING_PATH" "$f"'</automated>
  </verify>
  <done>`241-01-EVIDENCE.md` carries every SC-1 command as a reproducible `bash -c` invocation with its output, and the green claim is explicitly a CI claim with the 6 environmental failures named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → adopter | nothing in this plan ships; the deleted module lived under `test/support` and was never packaged |
| branch → `main` | ten refs carry live consumers of the deleted module; a later cherry-pick crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-241-01 | Repudiation | deletion of a module ten branches still reference | medium | mitigate | ADR 004 `## Affected refs` lists every ref the sweep finds, with the compile consequence stated (D-03) |
| T-241-02 | Tampering | an over-broad deletion removing a live support module | low | mitigate | blast radius re-proved by grep before and after the delete; `compile --warnings-as-errors` is the backstop |
| T-241-03 | Information disclosure | ADR prose leaking adopter data | low | accept | the repo is public and the ADR content is CI topology only; no adopter PII and no brand hex values are written |
</threat_model>

<verification>
- ADR 004 exists, is numbered 004, and matches the `001`/`003` convention.
- Both formatter files are gone; source grep is zero.
- `mix.exs` and `phase_233_library_economics_contract_test.exs` untouched by this plan's diff.
- `MIX_ENV=test mix compile --warnings-as-errors` succeeds.
- `241-01-EVIDENCE.md` carries the commands, the outputs, and the honest green claim.
</verification>

<success_criteria>
SC-1's first half is satisfied: the supersession is recorded, the orphan is gone, the alias topology
is provably unchanged, and the green claim is honest about where it was measured.
</success_criteria>

<output>
Create `.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-01-SUMMARY.md` when done
</output>
