---
phase: 241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard
plan: 07
type: execute
wave: 3
depends_on: [241-06]
files_modified:
  - scripts/ci/prohibitions/_p18-lib.mjs
  - scripts/ci/prohibitions/p18-doc-range.test.mjs
  - test/fixtures/prohibitions/p18-doc-range-python-language.ex
  - test/fixtures/prohibitions/p18-doc-range-parity.tsv
  - .planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-07-EVIDENCE.md
autonomous: true
gap_closure: true
requirements: [SURF-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Per D-30, the JavaScript R1 scanner recognizes exactly the committed Phase-237 Python language: ordinary and uppercase-`S` triple-quoted doc attributes plus ordinary and uppercase-`S` one-line quoted doc attributes; lowercase, pipe-delimited, paired-delimiter, escaped-delimiter, and generalized sigil parsing are outside the locked language."
    - "The committed counterexample `p18-doc-range-delimited-sigils.ex` measures Python 0 and JavaScript 0; `p18-doc-range-lowercase-sigil.ex` likewise measures 0/0 rather than preserving the rejected widening."
    - "Every relevant committed fixture has a permanent JS regression expectation sourced from the Phase-237 reference: doc-range leak 1, lowercase sigil 0, delimited sigils 0, and the accepted-language corpus equal to the Python result measured before the expectation is committed."
    - "The real `lib/` corpus still equals the committed R1 baseline in `p18-ratchet-baseline.tsv`, and a one-time Python-versus-JS evidence sweep proves parity on the real corpus and every fixture at the same commit."
    - "The doc-range hard-fail still rejects `p18-doc-range-leak.ex`; restoring the bounded scanner does not make the actual protected language vacuous."
  artifacts:
    - path: scripts/ci/prohibitions/_p18-lib.mjs
      provides: "Exact JavaScript reproduction of the Phase-237 Python scanner state machine"
    - path: scripts/ci/prohibitions/p18-doc-range.test.mjs
      provides: "Permanent fixture-corpus and R1-baseline parity regression tests"
    - path: test/fixtures/prohibitions/p18-doc-range-python-language.ex
      provides: "Accepted-language control for ordinary and uppercase-S heredoc/one-line forms"
    - path: test/fixtures/prohibitions/p18-doc-range-parity.tsv
      provides: "Machine-readable expected totals for every relevant committed fixture"
    - path: .planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-07-EVIDENCE.md
      provides: "Same-commit Python/JS parity transcript for fixtures and real R1 baseline"
  key_links:
    - from: .planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-docs-attribute-scan.py
      to: scripts/ci/prohibitions/_p18-lib.mjs
      via: "literal state-machine port required by D-30"
      pattern: "start|oneline|in_block"
    - from: test/fixtures/prohibitions/p18-doc-range-parity.tsv
      to: scripts/ci/prohibitions/p18-doc-range.test.mjs
      via: "fixture path and expected total loaded by the permanent Node test"
      pattern: "p18-doc-range-(leak|lowercase-sigil|delimited-sigils|python-language)"
    - from: scripts/ci/prohibitions/p18-ratchet-baseline.tsv
      to: scripts/ci/prohibitions/p18-doc-range.test.mjs
      via: "R1 real-tree baseline, read rather than duplicated"
      pattern: "R1"
  prohibitions:
    - "Do not broaden the grammar. Generalized, escaped, lowercase, pipe-delimited, or paired-delimiter sigils remain outside D-30 even if Elixir accepts them."
    - "Do not re-baseline R1. A changed real-tree total is a regression to diagnose, not permission to edit `p18-ratchet-baseline.tsv`."
    - "Do not add Python to the `fast_checks` runtime. Python is the one-time evidence oracle; the permanent CI guard remains Node-only per D-30."
---

<objective>
Restore D-30 semantic parity by replacing the broadened JavaScript sigil parser with the exact
Phase-237 Python scanner language, then bind every relevant fixture and the real R1 baseline to a
machine-checkable parity corpus.

Purpose: Close the verifier's SURF-04 gap. The existing 337/337 real-tree equality is coincidental
while the committed counterexample returns Python 0 / JavaScript 2.
Output: bounded scanner, permanent parity corpus, accepted-language fixture, and same-commit evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-CONTEXT.md
@.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-VERIFICATION.md
@.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-PATTERNS.md
@.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-06-SUMMARY.md
@.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-docs-attribute-scan.py
@scripts/ci/prohibitions/_p18-lib.mjs
@scripts/ci/prohibitions/p18-doc-range.test.mjs
@scripts/ci/prohibitions/p18-ratchet-baseline.tsv

<interfaces>
The existing public helper contract stays synchronous:
- `docRangeScan(relDir)` returns `{totalHits, docRanges, distinctSites, distinctFiles, tokenHits}`.
- `docRangeTotal(relDir)` returns `docRangeScan(relDir).totalHits` as a number.
- `GSD_PROHIB_SUBJECT` substitutes exactly one fixture while real tier resolution remains unchanged.

The Phase-237 reference contract is defined by two regexes and one boolean state:
- heredoc opener: doc attribute followed by either a plain triple quote or uppercase `~S` triple quote;
- one-line opener: doc attribute followed by either a plain quote or uppercase `~S` quote, excluding triple-quoted lines;
- `in_block` remains true until the next triple quote; opener and closer lines are counted.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end D-30 parity — RED counterexample to exact bounded scanner</name>
  <files>scripts/ci/prohibitions/_p18-lib.mjs, scripts/ci/prohibitions/p18-doc-range.test.mjs, test/fixtures/prohibitions/p18-doc-range-python-language.ex, test/fixtures/prohibitions/p18-doc-range-parity.tsv</files>
  <read_first>
    Read `237-docs-attribute-scan.py` in full and treat its `start`, `oneline`, and `in_block`
    behavior as the specification. Read `_p18-lib.mjs` only for its existing result shape and
    synchronous file traversal. Read all three existing `p18-doc-range-*.ex` fixtures and the R1 row
    in `p18-ratchet-baseline.tsv`. Per D-30, no review suggestion may widen the Python language.
  </read_first>
  <behavior>
    - The existing leak fixture produces one token hit and remains a hard-fail subject.
    - The lowercase-sigil fixture produces zero token hits; its ordinary heredoc remains a positive range-control.
    - The pipe/paired counterexample produces zero token hits; its ordinary heredoc remains a positive range-control.
    - A new accepted-language fixture proves plain heredoc, uppercase-S heredoc, plain one-line quoted, and uppercase-S one-line quoted attributes are counted exactly as Python counts them.
    - The real `lib/` total equals the R1 baseline read from `p18-ratchet-baseline.tsv`; the test does not duplicate or rewrite that number.
  </behavior>
  <action>
    Start RED by replacing the two widened-behavior tests in `p18-doc-range.test.mjs` with a
    manifest-driven parity test whose rows name the existing leak, lowercase-sigil, and
    delimited-sigil fixtures plus `p18-doc-range-python-language.ex`. Generate each expected fixture
    total by running the committed Python reference against a temporary one-file directory before
    writing the TSV; record the measured total in `p18-doc-range-parity.tsv` with columns
    `subject`, `expected_total_hits`, and `reference`. Add a separate test that reads the R1 value
    from `p18-ratchet-baseline.tsv` and compares the real JS total to it. Confirm the broadened
    implementation makes at least the lowercase and delimited rows fail for their own totals.

    Then implement GREEN by porting the Python state machine literally per D-30. Replace the generic
    doc-attribute value parser and delimiter table with the reference's two opener regexes and
    boolean triple-quote block state. Count token matches on opener lines, every in-block line, and
    closing lines exactly as Python does. Preserve sorted `*.ex` followed by sorted `*.exs`
    traversal, the synchronous return contract, `GSD_PROHIB_SUBJECT`, token vocabulary, and
    `P18InstrumentFailure` behavior. Do not recognize lowercase string sigils or any delimiter other
    than the exact quote forms the Python regex accepts.

    Make the accepted-language fixture format-clean and intentionally include both accepted and
    rejected forms so the regression proves the boundary in both directions. The permanent Node
    suite consumes only committed expected totals and requires no Python runtime in CI.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; mix format --check-formatted test/fixtures/prohibitions/p18-doc-range-python-language.ex; node --test --test-reporter=tap scripts/ci/prohibitions/p18-doc-range.test.mjs; GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p18-doc-range-leak.ex node --test scripts/ci/prohibitions/p18-doc-range.test.mjs >/tmp/241-07-leak.txt 2>&1 && { echo "known-bad doc leak passed" >&2; exit 1; } || grep -q "P18 DIRTY SURFACE: doc-range planning artifact" /tmp/241-07-leak.txt'</automated>
    <fails_when>The parity test does not cover all four fixture rows; either rejected-sigil fixture reports a nonzero JS total; the accepted-language fixture disagrees with its Python-measured total; the real `lib/` total differs from the committed R1 row; or the known-bad leak no longer produces the guard's own dirty-surface diagnostic.</fails_when>
  </verify>
  <acceptance_criteria>
    - `p18-doc-range.test.mjs` passes without invoking Python and reads all four manifest rows.
    - The delimited counterexample and lowercase fixture each assert zero, while the leak fixture asserts one.
    - The real-tree assertion reads R1 from the existing baseline file and does not mutate it.
    - The known-bad leak still produces a nonzero test run with the attributable p18 diagnostic.
  </acceptance_criteria>
  <done>The permanent Node guard implements the exact D-30 language, covers every relevant fixture and R1, and still fails the genuine doc-range leak.</done>
</task>

<task type="auto">
  <name>Task 2: Capture full Python/JS corpus parity and gate all prohibitions</name>
  <files>.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-07-EVIDENCE.md</files>
  <read_first>
    Read `241-06-EVIDENCE.md` for the command-transcript convention and `241-VERIFICATION.md` for the
    exact failed counterexample. Read the newly committed parity TSV before running any evidence so
    the evidence population and permanent-test population are set-equal.
  </read_first>
  <action>
    Write `241-07-EVIDENCE.md` with the candidate SHA and a table containing every parity subject:
    real `lib/`, the leak fixture, the lowercase fixture, the delimited counterexample, and the new
    accepted-language fixture. For each fixture, copy it into its own `mktemp -d` directory, run the
    committed Python reference on that directory, run the JS scanner through
    `GSD_PROHIB_SUBJECT`, and require equal totals. For real `lib/`, require Python total equals JS
    total equals the R1 value parsed from `p18-ratchet-baseline.tsv`. Use a trap scoped to the
    temporary directory; never delete a broad or unresolved path.

    Record the commands, exit codes, measured totals, and the statement that generalized,
    lowercase, escaped, pipe-delimited, and paired-delimiter sigils remain outside the locked
    language per D-30. Then run the full prohibition glob so the repaired helper is proven against
    all of its consumers, including the independent R1 ratchet.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; tmp=$(mktemp -d); trap '\''rm -rf -- "$tmp"'\'' EXIT; baseline=$(awk -F "\t" '\''$1=="R1" {print $2}'\'' scripts/ci/prohibitions/p18-ratchet-baseline.tsv); test -n "$baseline"; py=$(python3 .planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-docs-attribute-scan.py lib | sed -n "s/^total_hits=//p"); js=$(node -e '\''import("./scripts/ci/prohibitions/_p18-lib.mjs").then(m => console.log(m.docRangeTotal("lib")))'\''); test "$py" = "$js"; test "$js" = "$baseline"; for f in p18-doc-range-leak.ex p18-doc-range-lowercase-sigil.ex p18-doc-range-delimited-sigils.ex p18-doc-range-python-language.ex; do rm -f -- "$tmp/fixture.ex"; cp "test/fixtures/prohibitions/$f" "$tmp/fixture.ex"; py=$(python3 .planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-docs-attribute-scan.py "$tmp" | sed -n "s/^total_hits=//p"); js=$(GSD_PROHIB_SUBJECT="test/fixtures/prohibitions/$f" node -e '\''import("./scripts/ci/prohibitions/_p18-lib.mjs").then(m => console.log(m.docRangeTotal("lib")))'\''); test "$py" = "$js" || { echo "$f python=$py js=$js" >&2; exit 1; }; done; node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs; test -s .planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-07-EVIDENCE.md'</automated>
    <fails_when>Any fixture is absent from the evidence population; Python and JS differ for any row; real-tree parity differs from R1; the full prohibition suite fails; or the evidence omits the candidate SHA and exact commands.</fails_when>
  </verify>
  <acceptance_criteria>
    - Evidence contains five parity rows: real `lib/` plus four committed fixture subjects.
    - The delimited counterexample is explicitly recorded as Python 0 / JavaScript 0.
    - The lowercase fixture is explicitly recorded as Python 0 / JavaScript 0.
    - The real-tree row is equal on all three authorities: Python, JavaScript, and R1 baseline.
    - The entire prohibition glob passes at the evidence SHA.
  </acceptance_criteria>
  <done>D-30 parity is machine-verifiable across the complete fixture corpus and the real baseline, and every p18 consumer remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed Elixir text → scanner verdict | Tracked source and adversarial fixtures drive the public-doc leakage guard. |
| Python reference → JavaScript port | A semantic translation can silently widen or narrow the guarded language while preserving one aggregate count. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-241-G07-01 | Tampering | `_p18-lib.mjs` grammar | high | mitigate | Manifest-driven behavioral parity over every relevant fixture plus the real R1 baseline; D-30 forbids unreviewed grammar widening. |
| T-241-G07-02 | Repudiation | coincidental 337/337 equality | high | mitigate | Same-commit Python/JS comparison covers counterexamples and accepted controls, not only the aggregate. |
| T-241-G07-03 | Denial of service | Python added to `fast_checks` | medium | mitigate | Python is evidence-only; permanent tests consume committed expected totals under Node. |
| T-241-G07-04 | Information disclosure | missed planning reference in an accepted doc range | medium | mitigate | Genuine leak fixture must still fail with the guard's own diagnostic after parity restoration. |
</threat_model>

<verification>
- Permanent Node parity tests pass for every fixture and the committed R1 baseline.
- Same-commit Python and JavaScript totals agree for real `lib/` and all fixture subjects.
- The known-bad leak retains an attributable RED.
- The full prohibition suite passes without a workflow or `mix ci` topology edit.
</verification>

<success_criteria>
The verifier's D-30 gap is closed: the JavaScript scanner is semantically equivalent to the
committed Phase-237 reference on accepted forms, rejected counterexamples, and the real baseline.
</success_criteria>

<output>
Create `.planning/phases/241-retire-v1-47-s-dishonest-debt-adopter-leakage-guard/241-07-SUMMARY.md` when done.
</output>
