---
phase: 243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending
  - .planning/todos/completed
  - .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-INVENTORY.json
  - .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-TRIAGE-VALIDATION.json
autonomous: true
requirements: [QUEUE-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "The execution-start sorted pending todo inventory is frozen and every path has exactly one keep/close/defer disposition and a reason (D-01, D-02, D-05)."
    - "Earlier-phase-owned todos remain open unless their owning phase supplies completion evidence; triage itself fixes zero todos (D-03)."
    - "Required FUT-01…FUT-05 and the two adjacent gap records exist with accurate attached diagnoses, reusing matching identities and avoiding duplicates; Dependabot groups is filed only (D-04)."
    - "The triage commit changes only `.planning/todos/` and carries deterministic inventory/disposition evidence (D-05)."
  artifacts:
    - path: .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-INVENTORY.json
      provides: "Exact sorted execution-start inventory and per-file disposition rows"
    - path: .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-TRIAGE-VALIDATION.json
      provides: "Coverage, exclusivity, allowed-path, and commit-scope validation results"
---

<objective>
Freeze and disposition every pending todo present at execution start, create or correct the required future-work records, and commit the triage with no non-todo path changes.

Purpose: Complete QUEUE-04 while preserving issue ownership and avoiding implementation scope creep.
Output: Reproducible inventory, exact-one disposition ledger, required FUT records, and todo-only triage commit.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/METHODOLOGY.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-CONTEXT.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-RESEARCH.md
@.planning/todos/pending
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze exact execution-start inventory and disposition ledger</name>
  <files>.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-INVENTORY.json</files>
  <read_first>Read D-01 through D-05 and the whole pending todo inventory. At execution start, sort all markdown paths and snapshot before moving, editing, or creating any todo. Treat today's 66 count as planning context, not an execution-time invariant.</read_first>
  <behavior>Each frozen path appears once in the inventory and receives exactly one allowed disposition (`keep`, `close`, or `defer`), a specific reason, and evidence or named future owner/milestone; no file is closed solely from `resolves_phase` metadata or resolved-sounding prose.</behavior>
  <action>Write the exact sorted execution-start path list and stable content fingerprints to the inventory artifact. Inspect every file and assign one disposition plus reason; close only when direct completion evidence exists, defer only to a named future milestone/owner, otherwise keep with current rationale. Keep the approximately 12 earlier-phase-owned records open until owner evidence exists. Preserve the diagnosis and do not repair described issues (D-01 through D-03, D-05).</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; find .planning/todos/pending -maxdepth 1 -type f -name "*.md" -print | LC_ALL=C sort; jq -e ".inventory | length == (map(.path) | unique | length) and all(.[]; ([.disposition] | length == 1) and (.reason | length > 0))" .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-INVENTORY.json'</automated>
    <fails_when>The snapshot is not sorted/unique, a frozen input lacks exactly one valid disposition and reason, or fingerprint/path reconciliation fails.</fails_when>
  </verify>
  <acceptance_criteria>Snapshot has sorted unique paths and content fingerprints. Each entry has one and only one disposition, rationale, and evidence/owner field. Recount may differ from 66 and must be explained by actual execution-start files.</acceptance_criteria>
  <done>A fixed, reproducible inventory ledger covers every pending markdown todo present at execution start.</done>
</task>

<task type="auto">
  <name>Task 2: File accurate FUT-01 through FUT-05 and adjacent-gap records</name>
  <files>.planning/todos/pending, .planning/todos/completed, .planning/todos/resolved, .planning/todos/done</files>
  <read_first>Read D-04 and inspect existing FUT records, the example_unit_smoke todo, the launch-pack-contract caller gap, and release-related pending records. Verify each diagnosis and its canonical identity before editing.</read_first>
  <behavior>FUT-01 through FUT-05 and both adjacent gap diagnoses are represented by accurate records, reusing/correcting matching todo identities and creating only missing records. Dependabot grouping is recorded as future policy work and is not implemented.</behavior>
  <action>Map each required FUT identity to the best matching existing todo by diagnosis, not merely keyword. Correct existing records when they overlap; otherwise create one focused todo with the diagnosis and evidence. Ensure records cover absent `example_unit_smoke` from `ci-gate.needs` and the `launch-pack-contract.sh` workflow-caller gap. File Dependabot `groups:` only as backlog work. Apply the assigned disposition to each frozen item while ensuring zero described todos are fixed by changing product, CI, workflow, or documentation behavior; all changes remain within `.planning/todos/` (D-02 through D-05).</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; rg -l "FUT-0[1-5]|example_unit_smoke|launch-pack-contract|Dependabot.*groups" .planning/todos/pending .planning/todos/completed .planning/todos/resolved .planning/todos/done; git diff --name-only -- . ":!.planning/todos/"'</automated>
    <fails_when>Any of the seven required records is missing or duplicated despite an existing matching identity, a diagnosis is unverified, or any issue outside `.planning/todos/` is modified.</fails_when>
  </verify>
  <acceptance_criteria>Each of the seven required records is discoverable and has an attached verified diagnosis; no duplicate identity was created where an existing record matched. No implementation files changed.</acceptance_criteria>
  <done>All required future records exist accurately as todo records without implementing their contents.</done>
</task>

<task type="auto">
  <name>Task 3: Validate exact coverage and commit todo-only triage</name>
  <files>.planning/todos/pending, .planning/todos/completed, .planning/todos/resolved, .planning/todos/done, .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-INVENTORY.json, .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-TRIAGE-VALIDATION.json</files>
  <read_first>Read the frozen inventory, all changed todo files, and the repository's current diff. Compare paths as files may have moved from pending to completed/deferred but must still match their frozen identity.</read_first>
  <behavior>Validation proves every frozen input has exactly one disposition; every disposition has reason/evidence; no todo fix was included; required additions/corrections are confined to todo directories; and the triage commit's changed paths are only under `.planning/todos/`.</behavior>
  <action>Run deterministic jq/shell validation that compares the frozen snapshot with dispositions and checks uniqueness, allowed values, reason/evidence presence, required FUT coverage, and no out-of-scope file changes. If validation fails, repair only planning-record bookkeeping. Commit the triage separately with only `.planning/todos/` paths; inventory and validation receipts remain phase evidence and are committed with the relevant phase evidence commit, not in the todo-only triage commit. Record triage commit SHA and computed path scope in validation JSON (D-05).</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; git diff --check; git show --pretty=format: --name-only HEAD | awk '\''NF && $0 !~ /^\.planning\/todos\// { bad=1; print } END { exit bad }'\''; jq -e ".passed == true" .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TODO-TRIAGE-VALIDATION.json'</automated>
    <fails_when>The triage commit contains a path outside `.planning/todos/`, any frozen todo lacks exactly one reasoned disposition, a required future record is missing, or a todo issue was fixed during triage.</fails_when>
  </verify>
  <acceptance_criteria>The committed triage diff touches only `.planning/todos/`; validation reports complete one-to-one coverage and zero in-phase issue fixes. Evidence records the exact commit SHA and input inventory count.</acceptance_criteria>
  <done>QUEUE-04 is satisfied by a machine-validated inventory and a separately scoped todo-only triage commit.</done>
</task>

</tasks>

<verification>
Run the validator against the frozen inventory and final disposition ledger; compare the triage commit's complete changed-path list to `.planning/todos/`; verify every required FUT/adjacent record and confirm no todo subject was implemented.
</verification>

<success_criteria>
- Every execution-start pending markdown todo has exactly one reasoned disposition.
- Earlier-owner items remain open without owner-supplied completion evidence.
- FUT-01…FUT-05 and adjacent gap records are accurate and non-duplicative.
- The triage commit changes only `.planning/todos/` and fixes no issue.
</success_criteria>
