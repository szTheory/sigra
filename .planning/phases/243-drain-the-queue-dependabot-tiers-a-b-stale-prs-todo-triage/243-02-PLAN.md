---
phase: 243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage
plan: 02
type: execute
wave: 2
depends_on: ["243-01"]
files_modified:
  - .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-EVIDENCE.json
  - .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-FAILURE.json
  - mix.lock
  - test/example/priv/playwright/package-lock.json
  - .github/workflows
autonomous: true
requirements: [QUEUE-01]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Tier B PRs are handled individually in order: oban #225, hammer #229, flop_phoenix #230, threadline #226, credo #183, and @axe-core/playwright #216; #213 is untouched (D-06)."
    - "After every Tier B merge, `ci-gate` is observed green on `main` before the next PR begins; Threadline #226 also has `library_tests_dep_off` green (D-07)."
    - "Post-merge lockfile values match PR titles, and `hackney ~> 4.7` is re-evaluated against Threadline 0.9.0 and dropped or retained with a recorded reason (D-06, D-07)."
    - "Failures stop the sequence and retain causal evidence; unrelated Credo findings become todo records, not fixes (D-08, D-07)."
  artifacts:
    - path: .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-EVIDENCE.json
      provides: "Per-PR ordered merge, lockfile, gate, and special Threadline evidence"
    - path: .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-FAILURE.json
      provides: "Stop record with causal evidence and explicit resume condition"
  key_links:
    - from: "Tier B PR title"
      to: "mix.lock or Playwright package-lock"
      via: "Post-merge resolved version comparison"
      pattern: "locked_version"
    - from: "Tier B merge SHA"
      to: "GitHub Actions run on main"
      via: "Exact SHA and named required jobs"
      pattern: "ci-gate"
---

<objective>
Process the six named Tier B Dependabot PRs one at a time, requiring exact main CI evidence between merges and stopping safely on any mismatch or failure.

Purpose: Complete QUEUE-01's individually attributable dependency sequence.
Output: Ordered machine-readable merge receipts, or a causal failure receipt that blocks the next PR.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-CONTEXT.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-RESEARCH.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-01-PLAN.md
@scripts/ci/wait-for-ci-gate.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge and prove the first three Tier B updates in order</name>
  <files>.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-EVIDENCE.json, .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-FAILURE.json, mix.lock</files>
  <read_first>Read D-06 through D-08 and the Tier A evidence from plan 01. Re-read live PRs #225, #229, and #230 before each operation; confirm title/package/base and current state.</read_first>
  <behavior>Oban #225, hammer #229, and flop_phoenix #230 are each merged only after the previous PR's exact-main `ci-gate` is green, and each merged lock version matches its PR title.</behavior>
  <action>For each PR in fixed order, capture the pre-merge main SHA and baseline, verify required checks, merge only that PR, then read its merge SHA and exact `mix.lock` resolved version. Wait for `ci-gate` on the resulting main SHA before beginning the next PR. Follow AGENTS.md: check rate limit before a long watch, use one watcher per run with `--interval 60`, retrieve one structured summary after completion, and fetch failure logs only for a failure. On a red gate, capture PR, merge SHA, version, failing job/run, and baseline comparison; stop. Do not apply an unrelated fix or proceed while red. Record any new Credo finding as a new pending todo, not a code fix (D-07, D-08).</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; git diff --check; for n in 225 229 230; do gh pr view "$n" --repo szTheory/sigra --json number,title,state,mergedAt,mergeCommit,statusCheckRollup; done; jq -e ".items | length >= 3" .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-EVIDENCE.json'</automated>
    <fails_when>Any PR merges out of order, its lockfile value fails title comparison, or its `ci-gate` is not green on the exact main SHA before the next merge.</fails_when>
  </verify>
  <acceptance_criteria>Evidence rows preserve strict order and include before/after SHA, PR title, actual resolved lock version, run URL, and `ci-gate` conclusion for every completed step; an interrupted sequence clearly names the first blocked item and resume condition.</acceptance_criteria>
  <done>The first three Tier B updates have a verified green boundary after each, or execution is stopped with causal evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Merge and prove Threadline, Credo, and Axe updates individually</name>
  <files>.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-EVIDENCE.json, .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-FAILURE.json, mix.lock, test/example/priv/playwright/package-lock.json, .github/workflows</files>
  <read_first>Read completed Task 1 evidence and re-query PRs #226, #183, and #216 before each merge. Inspect the current Threadline compatibility override and workflow lockfile entries.</read_first>
  <behavior>Threadline #226 is followed by exact-main `ci-gate` and `library_tests_dep_off` success before Credo; Credo is followed by green `ci-gate` before Axe; every version matches its PR title; #213 remains open/unmerged.</behavior>
  <action>Process #226, #183, then #216 individually using the same before/merge/lockfile/green-gate boundary. For Threadline 0.9.0, verify the `hackney ~> 4.7` override against the merged dependency's requirements and tests; remove it only if proven unnecessary, otherwise retain it and write the evidence-based reason in the receipt. Require both `ci-gate` and `library_tests_dep_off` on Threadline's resulting main SHA. New Credo findings become todo records without fixing them. Do not merge, close, or otherwise advance Playwright #213, which belongs to Phase 244. Stop and record D-08 diagnostics at the first failed gate or version mismatch.</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; for n in 226 183 216; do gh pr view "$n" --repo szTheory/sigra --json number,title,state,mergedAt,mergeCommit,statusCheckRollup; done; gh pr view 213 --repo szTheory/sigra --json number,title,state,mergedAt,headRefName; git diff --check; jq -e ".items | length == 6" .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-B-EVIDENCE.json'</automated>
    <fails_when>Threadline lacks either required green job or a documented override verdict, any later merge starts before its predecessor's green boundary, a title/lock mismatch occurs, Credo findings are fixed instead of filed, or #213 is advanced.</fails_when>
  </verify>
  <acceptance_criteria>Evidence proves the order and green boundary after each merge. Threadline includes lock version, override verdict/reason, and both required job results. #213 is recorded as untouched for Phase 244.</acceptance_criteria>
  <done>All six Tier B bumps are individually verified with green main gates, or a precise blocker prevents claiming completion.</done>
</task>

</tasks>

<verification>
Validate strict sequence order, exact post-merge lock versions versus PR titles, `ci-gate` after every Tier B merge, Threadline's additional job and override disposition, and the untouched status of #213. One missing or red required result blocks subsequent merges and phase success.
</verification>

<success_criteria>
- Six Tier B PRs are individually processed in the fixed order, with a green main gate between each.
- Threadline #226 has both required jobs green and a reasoned override disposition.
- Every merged version is verified from the resulting lockfile.
- #213 remains for Phase 244 and Credo findings are filed, not fixed.
</success_criteria>
