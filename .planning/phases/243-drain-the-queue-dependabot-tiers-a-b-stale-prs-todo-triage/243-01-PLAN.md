---
phase: 243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-EVIDENCE.json
  - .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-FAILURE.json
  - mix.lock
  - test/example/priv/playwright/package-lock.json
  - .github/workflows/hex-publish.yml
autonomous: true
requirements: [QUEUE-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The four Tier A PRs (attest-build-provenance #215, @anthropic-ai/sdk #227, zod #228, otplib #220) are integrated as one batch or an attributable blocker record is produced (D-06, D-08)."
    - "For every merged Tier A dependency, the resulting lockfile value matches its PR title, and the exact merge SHA and required CI result are recorded (D-06)."
    - "The Tier A work preserves PR #215's same-line `# vX.Y.Z` action pin comment (D-07)."
  artifacts:
    - path: .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-EVIDENCE.json
      provides: "Machine-readable PR, merge SHA, lock-version, and CI evidence for the Tier A batch"
    - path: .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-FAILURE.json
      provides: "Causal diagnostic and resume condition if the batch cannot complete"
  key_links:
    - from: "Tier A PR metadata"
      to: mix.lock and test/example/priv/playwright/package-lock.json
      via: "Post-merge lockfile versions are compared to PR titles"
      pattern: "locked_version"
    - from: "PR #215 action reference"
      to: test/sigra/planning/phase_234_action_pinning_contract_test.exs
      via: "Same-line version annotation remains machine-checkable"
      pattern: "# vX.Y.Z"
---

<objective>
Merge the four named Tier A Dependabot updates as one batch, with a verified lockfile and CI receipt; if any check blocks the batch, stop with enough causal evidence to resume safely.

Purpose: Complete the Tier A portion of QUEUE-01 without losing version or failure attribution.
Output: Tier A merge evidence, or a failure receipt and concrete resume condition.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-CONTEXT.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-RESEARCH.md
@.github/workflows/ci.yml
@scripts/ci/wait-for-ci-gate.sh
@test/sigra/planning/phase_234_action_pinning_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight and merge the fixed Tier A batch</name>
  <files>.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-EVIDENCE.json, .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-FAILURE.json</files>
  <read_first>Read phase context D-06 through D-08, the project GitHub API rules in AGENTS.md, and the current CI wait helper. Query PRs #215, #227, #228, and #220 immediately before acting; verify they remain open, target the expected base, and retain the expected title/package identity.</read_first>
  <behavior>All four PRs are either merged together under the required batch policy or none are advanced as a partial batch; changed identities, blocked required checks, or a failed merge create a diagnostic record and halt further queue work.</behavior>
  <action>Capture each PR's current head SHA, title, base, checks, and relevant lockfile state. Merge the four named Tier A PRs together as the roadmap's one batch, preserving #215's same-line `# vX.Y.Z` comment. Do not merge any Tier B PR in this plan. If GitHub requires sequential API calls to realize one batch, execute the preflight and merge in one bounded batch window and stop on the first failure; do not claim a fully merged batch when only a subset landed. Record failed job/run URL and baseline evidence if blocked (D-06 through D-08).</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; for n in 215 227 228 220; do gh pr view "$n" --repo szTheory/sigra --json number,title,state,mergedAt,mergeCommit,statusCheckRollup; done; rg -n "attest-build-provenance|# v[0-9]" .github/workflows'</automated>
    <fails_when>Any named PR is not merged as part of the Tier A batch, an expected identity or title differs, required checks are incomplete/red, or #215 lacks its same-line action version comment.</fails_when>
  </verify>
  <acceptance_criteria>Evidence identifies each PR, exact merged commit or blocked state, and the batch outcome. #215's pin comment is present in the merged workflow line. No Tier B PR is merged by this task.</acceptance_criteria>
  <done>The fixed Tier A batch is merged and identified, or a durable failure receipt blocks continuation.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Tier A lock versions and required CI on the resulting main head</name>
  <files>mix.lock, test/example/priv/playwright/package-lock.json, .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-EVIDENCE.json</files>
  <read_first>Read every Tier A PR title and merge SHA from Task 1. Inspect exact lockfile entries rather than Dependabot branch names. Check GitHub rate-limit guidance before any long wait.</read_first>
  <behavior>Each merged dependency's lockfile version equals the PR title's target version; the required checks for the resulting main commit conclude successfully.</behavior>
  <action>Read `mix.lock` for `attest-build-provenance`'s workflow pin evidence as applicable and each Hex package, and `test/example/priv/playwright/package-lock.json` for the four npm/action updates. Compare exact resolved values to PR titles. Observe the required `ci-gate` on the resulting `main` SHA, using at most one watcher per run at 60-second intervals. Fetch structured run summary once after completion, and failed logs only on failure. If a mismatch or red gate appears, follow D-08 and preserve diagnostics rather than repairing unrelated behavior.</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; git diff --check; for n in 215 227 228 220; do gh pr view "$n" --repo szTheory/sigra --json mergeCommit,title; done; jq -e ".tier_a | length == 4" .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-TIER-A-EVIDENCE.json'</automated>
    <fails_when>Any merged lockfile version differs from its PR title, required CI is not green on the exact main SHA, or the evidence receipt omits a required value.</fails_when>
  </verify>
  <acceptance_criteria>Tier A evidence contains title, target version parsed from title, actual lockfile version, merge SHA, main SHA, exact `ci-gate` run URL/conclusion, and an explicit pass/block verdict for each item.</acceptance_criteria>
  <done>All four Tier A versions and required CI are verified, or the phase has an attributable stop condition.</done>
</task>

</tasks>

<verification>
Validate the receipt against the four named PRs and lockfiles; confirm the batch outcome and successful required CI on the observed `main` SHA. No conclusion may be inferred from branch names or an aggregate status without the required job.
</verification>

<success_criteria>
- Tier A batch is complete or durably blocked with factual evidence.
- Each merged package lock value equals its PR title target.
- #215's same-line version annotation remains intact.
- Required CI is green on the exact resulting main commit.
</success_criteria>
