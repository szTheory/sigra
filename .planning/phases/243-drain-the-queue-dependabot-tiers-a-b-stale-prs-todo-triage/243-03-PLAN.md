---
phase: 243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-STALE-PR-EVIDENCE.json
autonomous: true
requirements: [QUEUE-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Exactly eight stale phase/recapture PRs are identified, reconciled to recorded evidence, and closed with an accurate individual public reason (D-09)."
    - "The stale PR evidence records number, head/base refs, disposition, reason, close state, and verification for each PR (D-09, D-10)."
    - "No branch is deleted, and required head/base refs including #211/#219's relevant base remain resolvable through this phase (D-10)."
  artifacts:
    - path: .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-STALE-PR-EVIDENCE.json
      provides: "Identity manifest and per-PR closing/comment/ref verification"
---

<objective>
Resolve the stale phase/recapture PR set from current GitHub state and repository evidence, then close each of exactly eight with a specific reason while preserving every branch.

Purpose: Complete QUEUE-03 before Phase 245 branch pruning.
Output: Machine-readable stale PR closure evidence or a hard stop if the eight identities cannot be reconciled.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-CONTEXT.md
@.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-RESEARCH.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile and freeze the exact eight stale PR identities</name>
  <files>.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-STALE-PR-EVIDENCE.json</files>
  <read_first>Read D-09 and D-10, the live open PR list, and all references to stale phase/recapture PRs in `.planning/research/SUMMARY.md`, `.planning/research/PITFALLS.md`, and relevant phase summaries. The eight planning candidates are #211, #172, #124, #174, #219, #234, #261, and #254; verify each live identity and stale classification before any mutation.</read_first>
  <behavior>Before any close action, a frozen manifest contains exactly eight unique PR numbers (#211, #172, #124, #174, #219, #234, #261, #254), each verified as an open stale phase/recapture PR with current title/head/base; if any candidate is active or not stale, stop without closing any until the discrepancy is reconciled.</behavior>
  <action>Query each of the eight candidate PRs individually and inspect the live state, title, head/base, history, and successor evidence. Freeze the reconciled eight-entry manifest before closing anything; if any candidate is no longer open or is not stale, stop and record the mismatch rather than substituting another PR based on age. Exclude active Dependabot work, release PRs, and #213. Specifically record #211's base and #219's head/base relationship because Phase 245 needs those refs (D-09/D-10).</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; gh pr list --repo szTheory/sigra --state open --json number,title,headRefName,baseRefName,url; jq -e ".prs | length == 8 and (map(.number) | unique | length == 8)" .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-STALE-PR-EVIDENCE.json'</automated>
    <fails_when>The candidate set cannot be reconciled to exactly eight open stale phase/recapture PRs or any identity/ref differs from the frozen manifest.</fails_when>
  </verify>
  <acceptance_criteria>The manifest contains exactly the reconciled eight candidates, each with recorded title, number, head/base refs, stale rationale, and evidence source. An unresolved identity mismatch is a blocking outcome and no PR is closed.</acceptance_criteria>
  <done>The exact eight are frozen from evidence, or no mutation occurs and a blocker is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Close each stale PR with an individual reason and prove refs remain</name>
  <files>.planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-STALE-PR-EVIDENCE.json</files>
  <read_first>Read the frozen eight-entry manifest and re-query each PR to ensure its identity and refs have not changed. Consult Phase 245 boundary in ROADMAP.md before any action involving a branch.</read_first>
  <behavior>Each selected PR is closed only after an accurate public reason is prepared and posted; the close is verified in `gh pr list --state closed` and its closing comment is inspected. No branch deletion occurs.</behavior>
  <action>For each manifest PR, choose and document whether the work is superseded, obsolete, or deferred, with a successor/evidence link where available. Post an individually accurate GitHub comment, close the PR, then read back state and comment into the evidence. Verify all required head/base refs remain resolvable, especially #211/#219 relationships. Do not delete local or remote branches and do not close #213 or active dependency PRs. Stop if the PR changed identity since preflight (D-09, D-10).</action>
  <verify>
    <automated>bash -c 'set -eo pipefail; gh pr list --repo szTheory/sigra --state closed --json number,title,headRefName,baseRefName,closedAt; git diff --check; git branch -a --list "*generated-auth-runtime-proof-evidence*"; jq -e ".prs | length == 8 and all(.[]; .closed == true and (.comment | length > 0))" .planning/phases/243-drain-the-queue-dependabot-tiers-a-b-stale-prs-todo-triage/243-STALE-PR-EVIDENCE.json'</automated>
    <fails_when>Any of the eight is not closed with an inspected specific reason, required refs are missing, or a branch deletion occurred.</fails_when>
  </verify>
  <acceptance_criteria>All eight frozen entries show closed state, public comment body, reason/disposition, exact refs, and verification. No deletion operation or deleted branch appears; retained refs are git/GitHub-resolvable.</acceptance_criteria>
  <done>All eight stale PRs have public reasons and verified closure while all branches remain intact.</done>
</task>

</tasks>

<verification>
Check that the exact frozen set contains eight unique stale phase/recapture PRs, each is closed with a specific inspected public comment, and no required head/base branch was deleted.
</verification>

<success_criteria>
- Exactly eight evidenced stale PRs are closed with individual reasons.
- Closed state and comments are read back from GitHub.
- No branch is deleted; #211/#219 refs remain available for Phase 245.
</success_criteria>
