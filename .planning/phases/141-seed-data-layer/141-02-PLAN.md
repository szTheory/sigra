---
phase: 141-seed-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/config/dev.exs
  - test/example/lib/example/demo/personas.ex
autonomous: true
requirements: [SEED-02, SEED-05, SEED-06]
must_haves:
  truths:
    - "The dev seed path hashes passwords with real Argon2id at a reduced dev cost (t_cost: 2, m_cost: 12), isolated to dev"
    - "A single source-of-truth module enumerates the six personas with fixed @demo.sigra.dev emails and policy-passing passwords"
    - "The demo TOTP secret is deterministic, derived as specified, and carries the exact 'Never use in production' label"
  artifacts:
    - path: "test/example/config/dev.exs"
      provides: "Argon2 dev cost override t_cost: 2, m_cost: 12 (D-04)"
      contains: "config :argon2_elixir, t_cost: 2, m_cost: 12"
    - path: "test/example/lib/example/demo/personas.ex"
      provides: "Example.Demo.Personas pure-data module: 6 persona definitions + deterministic TOTP secret attr"
      contains: "defmodule Example.Demo.Personas"
  key_links:
    - from: "test/example/lib/example/demo/personas.ex"
      to: "Sigra.PasswordPolicy.validate/1"
      via: "every persona password is policy-conformant (DemoAdmin1! format)"
      pattern: "@demo.sigra.dev"
    - from: "test/example/config/dev.exs"
      to: "argon2_elixir"
      via: "config override consumed only in MIX_ENV=dev"
      pattern: "config :argon2_elixir"
---

<objective>
Lay the two non-DB foundations that the seed orchestrator (plan 03) depends on:

1. The **dev-only Argon2 cost override** (`t_cost: 2, m_cost: 12`) in `test/example/config/dev.exs` (D-04). This makes `mix setup`'s 6 persona hashes fast (~20-50ms/hash) while keeping a *credible* security posture — NOT the test-env `t_cost: 1, m_cost: 8`. Isolated to dev; never copied to prod.
2. The **`Example.Demo.Personas` pure-data module** (D-01, D-05): the single source of truth for the six personas (fixed `@demo.sigra.dev` emails, policy-passing passwords, role-descriptive display names, per-persona auth-state metadata) plus the deterministic demo-only TOTP secret module attribute consumed by admin and bob.

Purpose: Establishes the deterministic, security-posture-correct data and config layer. `Personas` is consumed both by plan 03's `Seeds` orchestrator and (next milestone phase) the `/demo/credentials` LiveView — keep it pure data with no DB or cross-module deps.
Output: `dev.exs` override + `personas.ex` module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-seed-data-layer/141-CONTEXT.md
@.planning/phases/141-seed-data-layer/141-PATTERNS.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Argon2 dev cost override to dev.exs (D-04)</name>
  <files>test/example/config/dev.exs</files>
  <read_first>
    - test/example/config/test.exs (line 46-47) — the analog: `# Speed up password hashing in tests` / `config :argon2_elixir, t_cost: 1, m_cost: 8`. Adapt the COST upward to the dev values; do NOT reuse the test cost.
    - test/example/config/dev.exs — current file has NO argon2 override; append the dev override at the end (after the existing live_reload config block).
  </read_first>
  <action>
    Append to `test/example/config/dev.exs` the Argon2 dev cost override per D-04: a 2-line comment then `config :argon2_elixir, t_cost: 2, m_cost: 12`. The comment must state the security rationale explicitly — that the demo seed path uses real Argon2id at a reduced DEV cost so `mix setup` stays fast (~20-50ms/hash), that the cost must NOT be lowered further, and must NOT be copied to prod. Use the DEV cost `t_cost: 2, m_cost: 12` — do NOT use the test-env `t_cost: 1, m_cost: 8`. This override is isolated to dev: `test.exs` keeps `t_cost: 1, m_cost: 8`, prod is untouched (no argon2 line added to prod config). Implements D-04 / SEED-06.
  </action>
  <acceptance_criteria>
    - `grep -c 'config :argon2_elixir, t_cost: 2, m_cost: 12' test/example/config/dev.exs` returns exactly 1.
    - `grep 'argon2' test/example/config/test.exs` still shows `t_cost: 1, m_cost: 8` (test override unchanged).
    - `grep -rn 'argon2' test/example/config/prod.exs` returns NO match (prod untouched).
    - The override is preceded by a comment containing the word "production" or "prod" warning against copying.
    - `cd test/example && MIX_ENV=dev mix loadconfig` / compile succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'config :argon2_elixir, t_cost: 2, m_cost: 12' test/example/config/dev.exs; grep -q 't_cost: 1, m_cost: 8' test/example/config/test.exs && echo TEST_UNCHANGED; ! grep -q argon2 test/example/config/prod.exs 2>/dev/null && echo PROD_CLEAN</automated>
  </verify>
  <done>dev.exs carries `t_cost: 2, m_cost: 12` with an explicit no-copy-to-prod comment; test.exs and prod.exs Argon2 posture are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create Example.Demo.Personas pure-data module (D-01, D-05)</name>
  <files>test/example/lib/example/demo/personas.ex</files>
  <read_first>
    - .planning/phases/141-seed-data-layer/141-PATTERNS.md (lines 120-141) — the persona roster table (handle / email / state-to-patch) and the EXACT TOTP secret derivation + label (lines 124-129). No codebase analog exists; this is the first `Example.Demo.*` module.
    - .planning/research/SUMMARY.md (lines 94-107) — the canonical 6-persona roster table with auth states and features demonstrated.
    - .planning/phases/141-seed-data-layer/141-UI-SPEC.md (Demo Content Contract, persona roster & display naming) — display names (Admin (operator), Alice, Bob, Carol, Dave, Frank) and the single auth feature each demonstrates.
  </read_first>
  <action>
    Create `Example.Demo.Personas` as a PURE-data module: no DB calls, no deps on other Example modules (single source of truth, per D-01 + Claude's Discretion). Define the deterministic demo TOTP secret as a module attribute, derived EXACTLY as `@demo_totp_secret :crypto.hash(:sha256, "sigra-demo-admin-totp-v1") |> binary_part(0, 20)` (D-05), preceded by the verbatim comment line `# Demo-only — intentionally deterministic. Never use in production.` Expose the six personas as a list (e.g. `all/0` returning maps/structs) — handles admin, alice, bob, carol, dave, frank, all on `@demo.sigra.dev`. Each persona carries: `email`, `display_name` (per UI-SPEC: "Admin (operator)", "Alice", "Bob", "Carol", "Dave", "Frank"), `password` (policy-passing, `DemoAdmin1!` format — 12+ chars, mixed case, digit, symbol; per-persona distinct, public-by-design), and auth-state metadata flags the orchestrator will read (e.g. `:confirmed`, `:totp`, `:passkey`, `:locked`, `:scheduled_deletion`, `:identity_github`, org memberships). Expose the demo TOTP secret via an accessor (e.g. `demo_totp_secret/0`) so plan 03's Seeds module reads it without re-deriving. Exact function decomposition / struct shape is implementation detail (below methodology escalation threshold per Claude's Discretion). Implements D-01, D-05, SEED-02.
  </action>
  <acceptance_criteria>
    - File defines `defmodule Example.Demo.Personas`.
    - Contains the exact label line `# Demo-only — intentionally deterministic. Never use in production.`
    - Contains the exact derivation `:crypto.hash(:sha256, "sigra-demo-admin-totp-v1") |> binary_part(0, 20)`.
    - All six handles present, each email ending `@demo.sigra.dev`; `grep -c '@demo.sigra.dev' lib/example/demo/personas.ex` >= 6.
    - NO email in the file uses `@example.test` (domain segregation invariant): `! grep -q '@example.test' lib/example/demo/personas.ex`.
    - Every persona password satisfies `Sigra.PasswordPolicy.validate/1` — verified at runtime: iterate the persona list, call `Sigra.PasswordPolicy.validate/1` on each password, assert all return :ok (no error tuple).
    - `cd test/example && mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors 2>&1 | tail -3; grep -q 'Demo-only — intentionally deterministic. Never use in production.' lib/example/demo/personas.ex && echo LABEL_OK; ! grep -q '@example.test' lib/example/demo/personas.ex && echo DOMAIN_OK; MIX_ENV=dev mix run -e 'Example.Demo.Personas.all() |> Enum.each(fn p -> :ok = Sigra.PasswordPolicy.validate(p.password) end); IO.puts("PASSWORDS_OK")'</automated>
  </verify>
  <done>Personas module compiles; deterministic TOTP secret derivation + exact label present; six `@demo.sigra.dev` personas with policy-passing passwords; zero `@example.test` leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev config -> hashing cost | The Argon2 cost override defines how strongly seeded passwords are hashed; an over-lowered cost would model a weak posture to adopters reading the seed code. |
| seed data module -> committed source | Persona passwords + TOTP secret are public-by-design demo data; the risk is they leak into a non-dev path or are read as a production recommendation. |
| email domain -> CI fixture | `@demo.sigra.dev` (seeded) vs `@example.test` (golden-path) segregation keeps `mix test` deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-04 | Tampering | Argon2 dev cost leaking to prod | mitigate | Override lives ONLY in `dev.exs` (D-04). Acceptance criteria assert prod.exs has no argon2 line and test.exs is unchanged. Comment explicitly forbids copying to prod. |
| T-141-05 | Spoofing/Repudiation | weak seed passwords read as a recommendation | mitigate | All passwords satisfy `Sigra.PasswordPolicy.validate/1` (runtime-verified), use the `DemoAdmin1!` 12+ char mixed-case+digit+symbol format, and are documented public-by-design. Never `password`/`admin`/`demo123`. |
| T-141-06 | Information disclosure | deterministic demo TOTP secret | accept | Public-by-design fixture data behind the `Mix.env()==:test` guard (enforced in plan 04) + the passthrough Cloak type. Carries the exact "Never use in production" label (D-05). |
| T-141-07 | Tampering | email-domain contamination of CI fixture | mitigate | All persona emails use `@demo.sigra.dev`; acceptance criteria assert zero `@example.test` occurrences (segregation invariant). |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | No new packages; no `Faker` dep added (D-01 / research Conflict 1). `argon2_elixir` already pinned. |
</threat_model>

<verification>
- `dev.exs` contains `config :argon2_elixir, t_cost: 2, m_cost: 12` (D-04); test.exs and prod.exs Argon2 posture unchanged.
- `Example.Demo.Personas` compiles, derives the TOTP secret exactly per D-05 with the verbatim label.
- All six personas use `@demo.sigra.dev`; zero `@example.test`.
- Every persona password returns `:ok` from `Sigra.PasswordPolicy.validate/1` at runtime.
</verification>

<success_criteria>
- Real Argon2id hashing is enabled in the dev seed path at the safe dev cost `t_cost: 2, m_cost: 12`, isolated from test and prod (SEED-05, SEED-06).
- A single-source-of-truth `Example.Demo.Personas` module enumerates the six personas with deterministic, policy-passing, `@demo.sigra.dev` credentials (SEED-02).
- The deterministic demo TOTP secret is derived and labeled exactly as D-05 mandates (SEED-06).
</success_criteria>

<output>
Create `.planning/phases/141-seed-data-layer/141-02-SUMMARY.md` when done.
</output>
