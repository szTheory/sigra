---
phase: 141-seed-data-layer
plan: 04
type: execute
wave: 3
depends_on: ["141-03"]
files_modified:
  - test/example/priv/repo/seeds.exs
autonomous: true
requirements: [SEED-01, SEED-05, SEED-06]
must_haves:
  truths:
    - "Running `mix run priv/repo/seeds.exs` twice in test/example completes exit 0 both times with identical persona row counts (no duplicates)"
    - "Running `MIX_ENV=test mix run priv/repo/seeds.exs` raises immediately with a clear message BEFORE any DB write"
    - "The dev seed path uses real Argon2id hashing and the labeled deterministic TOTP secret (security posture preserved)"
  artifacts:
    - path: "test/example/priv/repo/seeds.exs"
      provides: "Mix.env()==:test raise-guard + Example.Demo.Seeds.run/0 invocation"
      contains: "Mix.env() == :test"
  key_links:
    - from: "test/example/priv/repo/seeds.exs"
      to: "test/example/lib/example/demo/seeds.ex"
      via: "calls Example.Demo.Seeds.run() after the raise-guard"
      pattern: "Example.Demo.Seeds.run"
    - from: "test/example/priv/repo/seeds.exs"
      to: "test mix alias (mix.exs:85)"
      via: "two-layer defense: guard raises in test AND test alias never calls seeds.exs"
      pattern: "Mix.env() == :test"
---

<objective>
Wire the populated seed run as the final integration step: add the `Mix.env() == :test` raise-guard to the very top of `test/example/priv/repo/seeds.exs` (D-03, two-layer defense), then call `Example.Demo.Seeds.run/0`. This is where the phase's end-to-end Success Criteria are verified: the seed-twice idempotency check (SC#1), the test-env raise-guard (SC#2), the six-persona auth-state coverage (SC#3 amended per D-10), the audit liveness target (SC#4), and the security-posture invariants (SC#5).

Purpose: The raise-guard is the primary defense against CI-fixture contamination — it must raise BEFORE any DB access. The `test` mix alias already never calls `seeds.exs` (`mix.exs:85`); this is the second defensive layer — do NOT change the alias. This plan closes SEED-01, SEED-05, SEED-06 and validates the full phase against all five Success Criteria.
Output: populated `seeds.exs` + end-to-end verification evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-seed-data-layer/141-CONTEXT.md
@.planning/phases/141-seed-data-layer/141-PATTERNS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add raise-guard + wire Example.Demo.Seeds.run/0 in seeds.exs (D-03)</name>
  <files>test/example/priv/repo/seeds.exs</files>
  <read_first>
    - .planning/phases/141-seed-data-layer/141-PATTERNS.md (lines 211-221) — the exact raise-guard placement (at the very top, before any DB access) and the `Example.Demo.Seeds.run()` call.
    - test/example/priv/repo/seeds.exs — current file is header-comment-only; the guard goes at the top of the executable section.
    - test/example/mix.exs (line ~85) — confirm the `test` alias does NOT call `run priv/repo/seeds.exs` (it does not; do NOT change it). The `setup`/`ecto.setup` aliases DO call it for dev.
  </read_first>
  <action>
    Modify `test/example/priv/repo/seeds.exs`: keep the existing header comment, then add the D-03 raise-guard as the FIRST executable statement, before any DB access: `if Mix.env() == :test do raise "<clear message>" end`. The message must clearly state that seeds.exs must not run in `MIX_ENV=test` because it would contaminate the sandboxed CI fixture DB, and direct the operator to run with `MIX_ENV=dev`. After the guard, call `Example.Demo.Seeds.run()`. Do NOT add any `Repo`/DB call before the guard (the guard must short-circuit before touching the DB — two-layer defense with the `test` alias at mix.exs:85, which already never calls this file and must stay that way). Implements D-03, SEED-05.
  </action>
  <acceptance_criteria>
    - `seeds.exs` contains the literal `Mix.env() == :test` guard as the first executable statement.
    - The guard's raise message mentions test/MIX_ENV and the contamination rationale.
    - `seeds.exs` calls `Example.Demo.Seeds.run()` after the guard.
    - No `Example.Repo.` / DB call appears before the `if Mix.env() == :test` line.
    - `test/example/mix.exs` `test` alias still does NOT reference `priv/repo/seeds.exs` (unchanged).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Mix.env() == :test' test/example/priv/repo/seeds.exs && grep -q 'Example.Demo.Seeds.run' test/example/priv/repo/seeds.exs && echo WIRED_OK; ! grep -A30 'test:' test/example/mix.exs | grep -q 'priv/repo/seeds.exs' && echo TEST_ALIAS_CLEAN</automated>
  </verify>
  <done>seeds.exs raises in MIX_ENV=test before any DB access and calls `Example.Demo.Seeds.run()` in dev; the `test` mix alias is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end Success Criteria verification (SC#1–SC#5)</name>
  <files>test/example/priv/repo/seeds.exs</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 141 Success Criteria 1-5) — the five testable criteria this task asserts. Note SC#3 amended per D-10 (drop the admin "API token observable in admin UI" claim).
    - .planning/phases/141-seed-data-layer/141-CONTEXT.md (D-10) — the SC#3 amendment: admin's distinguishing features are TOTP + multi-org + passkey display + rich audit trail (NO API token surface).
  </read_first>
  <action>
    Run and capture the end-to-end Success Criteria checks against the wired `seeds.exs` (no new product code — this is the integration verification task; if any check fails, the fix belongs in the relevant earlier plan's file, but `seeds.exs` wiring corrections land here):
    SC#1 (idempotency): run `MIX_ENV=dev mix run priv/repo/seeds.exs` TWICE; both exit 0; persona-row counts identical between runs (no duplicates).
    SC#2 (test guard): run `MIX_ENV=test mix run priv/repo/seeds.exs`; assert non-zero exit AND the raise message is printed BEFORE any DB write (no persona rows created in example_test).
    SC#3 (six personas, amended): assert all six `@demo.sigra.dev` users exist with distinct states — admin (TOTP + multi-org + passkey display), alice (confirmed standard), bob (TOTP + Beta owner), carol (github identity), dave (locked: failed_login_attempts=5 + locked_at), frank (scheduled-deletion). Do NOT assert any API-token row (dropped per D-10).
    SC#4 (audit liveness): assert `audit_events` >= 15 rows across >= 6 distinct `action` values.
    SC#5 (security posture): assert seeded passwords pass `Sigra.PasswordPolicy.validate/1`; passwords are real Argon2id hashes (hashed_password starts with `$argon2id$`) consistent with the dev cost `t_cost: 2, m_cost: 12`; the demo TOTP secret constant in personas.ex carries the exact label `# Demo-only — intentionally deterministic. Never use in production.`
    Record the captured outputs as evidence in the SUMMARY.
  </action>
  <acceptance_criteria>
    - SC#1: two consecutive `MIX_ENV=dev mix run priv/repo/seeds.exs` runs both return exit 0; `@demo.sigra.dev` user count identical across runs (no duplicate personas/orgs/memberships/audit rows).
    - SC#2: `MIX_ENV=test mix run priv/repo/seeds.exs` exits non-zero and prints the raise message; no `@demo.sigra.dev` users exist in the example_test DB afterward.
    - SC#3: six personas present with their distinct lifecycle/auth-state columns as enumerated (no API-token assertion per D-10).
    - SC#4: `SELECT count(*) FROM audit_events` >= 15 and `SELECT count(DISTINCT action) FROM audit_events` >= 6.
    - SC#5: a sampled seeded `hashed_password` begins with `$argon2id$`; `Sigra.PasswordPolicy.validate/1` returns :ok for every persona password; the exact demo-only TOTP label string is present in personas.ex.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example && MIX_ENV=dev mix run priv/repo/seeds.exs && MIX_ENV=dev mix run priv/repo/seeds.exs && echo SC1_TWICE_OK; (MIX_ENV=test mix run priv/repo/seeds.exs; test $? -ne 0 && echo SC2_RAISES_OK); MIX_ENV=dev mix run -e 'import Ecto.Query; r=Example.Repo; alias Example.Accounts.{User,AuditEvent}; n=r.aggregate(from(u in User, where: like(u.email,"%@demo.sigra.dev")), :count); a=r.aggregate(AuditEvent,:count); d=r.one(from x in AuditEvent, select: count(x.action,:distinct)); adm=Example.Accounts.get_user_by_email("admin@demo.sigra.dev"); IO.inspect({:personas,n,:audit,a,:distinct,d,:argon2,String.starts_with?(adm.hashed_password || "", "$argon2id$")})'; grep -q 'Demo-only — intentionally deterministic. Never use in production.' lib/example/demo/personas.ex && echo SC5_LABEL_OK</automated>
  </verify>
  <done>All five Success Criteria pass with captured evidence: seed-twice idempotent (SC#1), test-env raise (SC#2), six personas with distinct states (SC#3 amended), >=15 audit rows / >=6 actions (SC#4), real Argon2id + labeled demo TOTP secret + policy-passing passwords (SC#5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeds.exs entry -> DB | The raise-guard is the gate; it must fire before any DB connection is used in MIX_ENV=test. |
| dev seed run -> CI fixture DB | The dev and test databases (example_dev vs example_test) are separate; the guard prevents the dev seed path from writing to the test DB even if mistakenly invoked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-12 | Tampering | seeds.exs contaminating the CI fixture DB | mitigate | `Mix.env() == :test` raise-guard at the very top, before any DB access (D-03), verified by SC#2 (raises + zero rows in example_test). Second layer: the `test` mix alias never calls seeds.exs (mix.exs:85, unchanged). |
| T-141-13 | Information disclosure | demo secrets/passwords surfacing in non-dev env | mitigate | The guard confines the seed run to dev; the Argon2 dev cost (plan 02) and demo TOTP label (plan 02) are dev-only; SC#5 asserts real `$argon2id$` hashing and the verbatim demo label. |
| T-141-14 | Denial of service / corruption | non-idempotent re-run breaking CI/setup | mitigate | SC#1 verifies seed-twice exits 0 with identical counts (all upserts are on_conflict/count-guarded per plan 03). |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this phase. |
</threat_model>

<verification>
- SC#1: `mix run priv/repo/seeds.exs` twice in test/example → exit 0 both times, identical persona row counts.
- SC#2: `MIX_ENV=test mix run priv/repo/seeds.exs` → non-zero exit, raise message printed before any DB write, no rows in example_test.
- SC#3: six personas with distinct auth states (no API-token assertion per D-10).
- SC#4: audit_events >= 15 rows, >= 6 distinct actions.
- SC#5: real Argon2id hashes (`$argon2id$`), policy-passing passwords, exact demo-only TOTP label.
</verification>

<success_criteria>
- `seeds.exs` raises immediately in `MIX_ENV=test` before any DB access and runs `Example.Demo.Seeds.run/0` in dev (SEED-05, D-03).
- The full phase passes all five Success Criteria with captured evidence (SEED-01, SEED-05, SEED-06).
- The example app's demo security posture matches production: real Argon2id at the safe dev cost, no committed real secrets, the demo-only TOTP secret clearly labeled.
</success_criteria>

<output>
Create `.planning/phases/141-seed-data-layer/141-04-SUMMARY.md` when done.
</output>
