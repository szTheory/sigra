---
phase: 141-seed-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example/accounts/user_identity.ex
  - test/example/priv/repo/migrations/20260529000000_create_user_identities.exs
autonomous: true
requirements: [SEED-02, SEED-03]
must_haves:
  truths:
    - "Carol's GitHub OAuth identity is observable in the library admin user-detail Identities panel (no longer the 'not available' fallback)"
    - "The example app exposes Example.Accounts.UserIdentity so the library admin auto-detects identity_schema via optional_schema/2"
  artifacts:
    - path: "test/example/lib/example/accounts/user_identity.ex"
      provides: "UserIdentity schema with changeset/2 matching Detail.list_identities/3 contract"
      contains: "schema \"user_identities\""
    - path: "test/example/priv/repo/migrations/20260529000000_create_user_identities.exs"
      provides: "user_identities table with [:user_id, :provider] unique index (on_conflict target)"
      contains: "create unique_index(:user_identities, [:user_id, :provider])"
  key_links:
    - from: "test/example/lib/example/accounts/user_identity.ex"
      to: "lib/sigra/admin/users/detail.ex"
      via: "module named Example.Accounts.UserIdentity resolved by optional_schema(accounts_module, :UserIdentity)"
      pattern: "defmodule Example.Accounts.UserIdentity"
    - from: "test/example/lib/example/accounts/user_identity.ex"
      to: "test/example/lib/example/accounts/user.ex"
      via: "belongs_to :user FK"
      pattern: "belongs_to :user"
---

<objective>
Add the one new schema this milestone requires: `Example.Accounts.UserIdentity` plus its migration, so Carol's seeded GitHub OAuth identity becomes genuinely observable in the library-owned admin user-detail surface (D-09). The library admin (`Sigra.Admin.Users.Detail` -> `Sigra.Admin.Live.UserShowLive`) already renders linked identities and auto-detects the schema via `optional_schema(accounts_module, :UserIdentity)`; without this schema the Identities panel shows the literal fallback `Linked identities are not available for this app.` (`user_show_live.ex:163`). No library change is needed — only the example-app schema, its migration, and (in a later plan) a seeded row.

Purpose: Makes Carol's OAuth-linked auth state honest and admin-visible, satisfying SC#3 / SEED-02 / SEED-03. This is a hard dependency for the Carol seed row in plan 03 (`on_conflict:` keyed on the `[:user_id, :provider]` unique index per D-02).
Output: `user_identity.ex` schema + `create_user_identities` migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-seed-data-layer/141-CONTEXT.md
@.planning/phases/141-seed-data-layer/141-PATTERNS.md
@.planning/phases/141-seed-data-layer/141-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Example.Accounts.UserIdentity schema</name>
  <files>test/example/lib/example/accounts/user_identity.ex</files>
  <read_first>
    - priv/templates/sigra.gen.oauth/user_identity.ex — the library generator template; de-template the EEx tags. This is the field-set + changeset source of truth (PATTERNS.md lines 44-77).
    - test/example/lib/example/accounts/user_mfa_credential.ex — example-app schema header convention to copy verbatim (lines 12-21): `@primary_key {:id, :binary_id, autogenerate: true}`, `@foreign_key_type :binary_id`, and the `Example.Accounts.Encrypted.Binary` passthrough Cloak type usage.
    - test/example/lib/example/accounts/user_passkey.ex — second concrete analog for the same header/Encrypted.Binary conventions.
    - lib/sigra/admin/users/detail.ex — `list_identities/3` (lines 120-133) is the CONTRACT this schema must satisfy: query is `from(identity in identity_schema, where: identity.user_id == ^user.id, order_by: [asc: identity.provider, asc: identity.inserted_at])`. Also note `optional_schema(accounts_module, :UserIdentity)` auto-detection at lines 274,278 — the module name `Example.Accounts.UserIdentity` is what wires it in.
    - test/example/lib/example/accounts/user.ex — confirm `Example.Accounts.User` is the belongs_to target module name.
  </read_first>
  <action>
    Create `Example.Accounts.UserIdentity` (de-templated from the sigra.gen.oauth template, NOT a `use Sigra.*` macro). Header: `use Ecto.Schema`, `import Ecto.Changeset`, `@primary_key {:id, :binary_id, autogenerate: true}`, `@foreign_key_type :binary_id` (copy from user_mfa_credential.ex). Schema table `"user_identities"` with fields: `provider :string`, `provider_uid :string`, `encrypted_access_token Example.Accounts.Encrypted.Binary`, `encrypted_refresh_token Example.Accounts.Encrypted.Binary`, `token_expires_at :utc_datetime`, `provider_email :string`, `provider_name :string`, `provider_avatar_url :string`, `metadata :map default %{}`, `last_used_at :utc_datetime`, `belongs_to :user, Example.Accounts.User`, `timestamps(type: :utc_datetime)`. The changeset MUST be named `changeset/2` (template convention, NOT `create_changeset/2`) — this is load-bearing for plan 03's Carol seed call. It casts `[:provider, :provider_uid, :encrypted_access_token, :encrypted_refresh_token, :token_expires_at, :provider_email, :provider_name, :provider_avatar_url, :metadata, :last_used_at, :user_id]`, `validate_required([:provider, :provider_uid, :user_id])`, `update_change(:provider, &String.downcase/1)`, `unique_constraint([:user_id, :provider])`, `unique_constraint([:provider, :provider_uid])`, `foreign_key_constraint(:user_id)`. Per D-09, expose `provider`, `provider_uid`, `provider_email`, `user_id`, and `inserted_at` (via timestamps) so the row matches `Detail.list_identities/3`.
  </action>
  <acceptance_criteria>
    - File defines `defmodule Example.Accounts.UserIdentity`.
    - `grep -c 'schema "user_identities"' test/example/lib/example/accounts/user_identity.ex` returns 1.
    - The public function is named `changeset/2` (grep `def changeset(` returns a match; grep `create_changeset` returns NO match).
    - Schema declares `field :provider`, `field :provider_uid`, `field :provider_email`, and `belongs_to :user, Example.Accounts.User`.
    - Header contains `@primary_key {:id, :binary_id, autogenerate: true}` and `@foreign_key_type :binary_id`.
    - `cd test/example && mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors 2>&1 | tail -5; grep -q 'def changeset(' lib/example/accounts/user_identity.ex && ! grep -q 'create_changeset' lib/example/accounts/user_identity.ex && echo CHANGESET_OK</automated>
  </verify>
  <done>UserIdentity schema compiles, defines `changeset/2`, table `user_identities`, and matches the `list_identities/3` field contract (provider, provider_uid, provider_email, user_id, inserted_at).</done>
</task>

<task type="auto">
  <name>Task 2: Create create_user_identities migration</name>
  <files>test/example/priv/repo/migrations/20260529000000_create_user_identities.exs</files>
  <read_first>
    - test/example/priv/repo/migrations/20260415000002_create_user_passkeys.exs — primary analog for the binary_id table DDL style (`create table(..., primary_key: false)`, `add :id, :binary_id, primary_key: true`, `references(:users, type: :binary_id, on_delete: :delete_all)`).
    - priv/templates/sigra.gen.oauth/oauth_migration.exs (lines 4-25) — field-list source for the user_identities columns.
    - test/example/priv/repo/migrations/ (latest existing stamp is 20260528152139) — the new filename stamp 20260529000000 must sort AFTER it.
  </read_first>
  <action>
    Create migration module `Example.Repo.Migrations.CreateUserIdentities` (matching the `Example.Repo.Migrations.*` namespace). Filename stamp `20260529000000` (later than the latest existing `20260528152139`). `change/0` creates table `:user_identities` with `primary_key: false`, then: `add :id, :binary_id, primary_key: true`; `add :user_id, references(:users, type: :binary_id, on_delete: :delete_all), null: false`; `add :provider, :string, null: false`; `add :provider_uid, :string, null: false`; `add :encrypted_access_token, :binary`; `add :encrypted_refresh_token, :binary`; `add :token_expires_at, :utc_datetime`; `add :provider_email, :string`; `add :provider_name, :string`; `add :provider_avatar_url, :string`; `add :metadata, :map, default: %{}`; `add :last_used_at, :utc_datetime`; `timestamps(type: :utc_datetime)`. After the table: `create unique_index(:user_identities, [:user_id, :provider])` (this is the `on_conflict:` target for D-02 Carol upsert), `create unique_index(:user_identities, [:provider, :provider_uid])`, `create index(:user_identities, [:user_id])`.
  </action>
  <acceptance_criteria>
    - Filename is `20260529000000_create_user_identities.exs` and sorts after `20260528152139_threadline_governance_schema.exs`.
    - Module is `Example.Repo.Migrations.CreateUserIdentities`.
    - `grep -q 'create unique_index(:user_identities, \[:user_id, :provider\])'` matches (the D-02 conflict target exists).
    - `add :user_id, references(:users, type: :binary_id, on_delete: :delete_all), null: false` present.
    - `cd test/example && MIX_ENV=dev mix ecto.migrate` runs the migration with exit 0 (table created); re-running `mix ecto.migrate` is a no-op (already migrated).
  </acceptance_criteria>
  <verify>
    <automated>cd test/example && MIX_ENV=dev mix ecto.migrate 2>&1 | tail -8 && MIX_ENV=dev mix run -e 'IO.inspect Example.Repo.query!("SELECT to_regclass(\x27user_identities\x27)").rows'</automated>
  </verify>
  <done>Migration applies cleanly in MIX_ENV=dev; `user_identities` table exists with the `[:user_id, :provider]` unique index; re-migrate is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed/dev DB -> admin UI render | The seeded identity row crosses into the library admin surface; a malformed row would either crash the panel or silently fail to render. |
| schema field contract -> library query | `list_identities/3` queries fixed columns; a field-name mismatch is a runtime/compile failure, not a security leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering | user_identities migration applied to wrong DB | mitigate | Migration is run only via `MIX_ENV=dev mix ecto.migrate`; the example app's `example_dev` vs `example_test` DBs are already separate. No test-DB write here. |
| T-141-02 | Information disclosure | encrypted_access_token / encrypted_refresh_token columns | accept | These columns use the example app's documented passthrough `Encrypted.Binary` Cloak type (no real encryption, documented as not-production). The seeded Carol row (plan 03) stores NO real token — fabricated/empty. Public-by-design demo data. |
| T-141-03 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No CLAUDE.md dep delta. |
| T-141-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced; `argon2_elixir`, `ecto`, etc. already pinned in `test/example/mix.exs`. |
</threat_model>

<verification>
- `cd test/example && mix compile --warnings-as-errors` succeeds with the new schema.
- `cd test/example && MIX_ENV=dev mix ecto.migrate` applies `20260529000000_create_user_identities` and creating `user_identities` succeeds; re-run is a no-op.
- `Example.Accounts.UserIdentity.changeset/2` exists (not `create_changeset/2`).
- The `[:user_id, :provider]` unique index exists (the plan-03 Carol `on_conflict:` target).
</verification>

<success_criteria>
- New `Example.Accounts.UserIdentity` schema compiles and exposes `changeset/2` with the field set required by `Sigra.Admin.Users.Detail.list_identities/3` (provider, provider_uid, provider_email, user_id, inserted_at).
- `user_identities` table is created in the dev database via an idempotent migration with the `[:user_id, :provider]` unique index.
- No changes to `lib/sigra/` (library is unchanged; only the example app gains the schema, per the milestone "no new Sigra library code" decision).
</success_criteria>

<output>
Create `.planning/phases/141-seed-data-layer/141-01-SUMMARY.md` when done.
</output>
