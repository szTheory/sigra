---
phase: 44
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - lib/sigra/mfa.ex
  - test/sigra/mfa_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-06
must_haves:
  truths:
    - "Listed MFA success paths that mutate DB + audit use Ecto.Multi + log_multi_safe (or __log_internal__) in one repo.transaction per D-44-03 ordering."
    - "verify_backup success emits mfa.verify.success and mfa.backup_code_used in one transaction when :audit_schema is set."
  artifacts:
    - path: "lib/sigra/mfa.ex"
      provides: "Atomic MFA + audit boundaries"
nyquist_compliant: true
---

<objective>
Deliver **AUD-06** for **`Sigra.MFA`**: convert inventoried success paths from post-commit **`log_safe`** to **`Ecto.Multi` + `Audit.log_multi_safe` / `__log_internal__`** per **D-44-03**, using **`audit_multi_step`** from plan **44-02** where two audit rows share one transaction; add **Postgres atomicity tests** per **D-44-06**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-44-03 | Repudiation | MFA state commits without audit row | high | Atomicity tests force audit insert failure → domain mutation rolled back |
| T-44-04 | Elevation | Lockout / verify failure mishandled | medium | Inventory-driven coverage; tests for counter + `mfa.verify.failure` paths per rows marked for Multi |
</threat_model>

<context>
@.planning/phases/44-mfa-account-api-atomic-batches/44-CONTEXT.md
@.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
@lib/sigra/mfa.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement MFA Multis per inventory</name>
  <files>lib/sigra/mfa.ex</files>
  <read_first>
    - lib/sigra/mfa.ex
    - lib/sigra/audit.ex
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-CONTEXT.md
  </read_first>
  <action>
    1. For **each** inventory row tagged **`AUD-06`** with target mechanism **`Multi`**, refactor the matching `def` / clause to build an **`Ecto.Multi`** that includes domain writes + **`Sigra.Audit.log_multi_safe`** (or **`__log_internal__`**) in **one** `config.repo.transaction/1` call.
    2. Implement **D-44-03** order: (a) **`verify/4`** TOTP success — `last_verified_step` update + **`mfa.verify.success`** shared txn; (b) **`verify_backup/4`** success — consume + lockout reset + **two** audits using **distinct** `audit_multi_step` atoms; (c) **`confirm_enrollment/4`** success — promote audit into credential + backup insert Multi; (d) **`disable/4` / `disable!/4`** — append audit to **`cleanup_mfa/5`** Multi; (e) lockout / verify-failure **writes** per inventory — prefer Multi where **`Lockout.increment`** or MFA row updates pair with **`mfa.verify.failure`** / **`mfa.lockout`**.
    3. Leave **pure validation** and **read-only** exits as **`log_safe`** or silent per **EX-** rows in **`44-AUD-04-INVENTORY.md`** — do not expand scope beyond inventory without updating inventory appendix.
    4. After each successful `repo.transaction` that uses `log_multi_safe`, call **`Sigra.Audit.emit_telemetry_from_changes/1`** (or the extended arity from **44-02**) from the **`{:ok, changes}`** branch only.
    5. Update the **D-26 dispatch comment block** at top of **`mfa.ex`** to reflect new mechanisms (mirror **`auth.ex`** style).
  </action>
  <acceptance_criteria>
    - `grep -q "log_multi_safe" lib/sigra/mfa.ex` exits 0.
    - `grep -q "emit_telemetry_from_changes" lib/sigra/mfa.ex` exits 0.
    - `mix format lib/sigra/mfa.ex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "log_multi_safe" lib/sigra/mfa.ex && grep -q "emit_telemetry_from_changes" lib/sigra/mfa.ex'</automated>
  </verify>
  <done>MFA module uses Multi-bound audit for converted rows.</done>
</task>

<task type="execute">
  <name>Task 2: MFA audit atomicity tests</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/api_token_audit_atomic_test.exs
    - guides/recipes/testing.md
    - lib/sigra/mfa.ex
  </read_first>
  <action>
    1. Create **`test/sigra/mfa_audit_atomicity_test.exs`** following **`api_token_audit_atomic_test.exs`**: real Postgres, minimal schemas or existing test schemas required for MFA tables used by `Sigra.MFA` in tests.
    2. Add **one deep test per converted boundary** from inventory (minimum: **verify TOTP success**, **verify_backup success** with **two** audit actions ordered by `id`, **confirm_enrollment success** if converted).
    3. Each test must include a **rollback path**: invalid audit row / constraint guard (same pattern as API token atomic test) proving **no** partial domain commit without audit when `:audit_schema` is set.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/mfa_audit_atomicity_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs'</automated>
  </verify>
  <done>MFA atomicity bounded by Postgres tests.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs`
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit_multi_step_test.exs`
</verification>

<success_criteria>
- **AUD-06** MFA slice implemented per **`44-AUD-04-INVENTORY.md`** with atomicity tests.
</success_criteria>
