---
phase: 44
plan: 05
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - lib/sigra/api_token.ex
  - test/sigra/api_token_audit_atomic_test.exs
autonomous: true
requirements:
  - AUD-07
must_haves:
  truths:
    - "revoke/2 uses Multi + log_multi_safe(api.token_revoke) in one transaction when audit_schema is set."
    - "revoke_all/2 emits summary api.token_revoke_all (or inventory-agreed name) with count metadata and audited in same transaction as bulk revoke."
  artifacts:
    - path: "lib/sigra/api_token.ex"
      provides: "Symmetric token revoke auditing with create/"
nyquist_compliant: true
---

<objective>
Close **AUD-07** API token remainder per **D-44-05**: atomic **`revoke/2`** mirroring **`create/3`**; add **`revoke_all/2`** summary audit; extend **`api_token_audit_atomic_test.exs`** for revoke paths; keep **`api.token_verify.failure`** as documented **`log_safe`** hybrid unless inventory says otherwise.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-44-05 | Repudiation | Token revoked in DB without audit row | high | Atomicity test: invalid audit forces rollback → token still unrevoked |
| T-44-06 | Information disclosure | revoke_all metadata leaks raw tokens | high | Metadata contains **count** only — grep test forbids `hashed_token` in audit metadata map literals |
</threat_model>

<context>
@.planning/phases/44-mfa-account-api-atomic-batches/44-CONTEXT.md
@.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
@lib/sigra/api_token.ex
@test/sigra/api_token_audit_atomic_test.exs
</context>

<tasks>

<task type="execute">
  <name>Task 1: Atomic revoke + revoke_all</name>
  <files>lib/sigra/api_token.ex</files>
  <read_first>
    - lib/sigra/api_token.ex
    - lib/sigra/audit.ex
    - .planning/phases/44-mfa-account-api-atomic-batches/44-CONTEXT.md
  </read_first>
  <action>
    1. Refactor **`revoke/2`** ( arity and module per current source ) to use **`Ecto.Multi.update`** (or equivalent) for `revoked_at` + **`Sigra.Audit.log_multi_safe("api.token_revoke", audit_opts)`** in **one** `repo.transaction`, mirroring **`create/3`** pattern including **`emit_telemetry_from_changes/1`** on success.
    2. Implement **`revoke_all/2`** with **one** summary audit action **`api.token_revoke_all`** (if inventory names differently, follow inventory) using **`Multi.run`** or **`update_all`** inside **`Ecto.Multi`** + **`log_multi_safe`**, with **`metadata`** including integer **`count`** of rows affected — **no** token material.
    3. Do **not** change **`api.token_verify.failure`** hybrid (`log_safe`) unless **`44-AUD-04-INVENTORY.md`** documents a row requiring change.
    4. Update moduledoc or inline comments noting **`maybe_update_last_used`** observability stance per **D-44-05**.
  </action>
  <acceptance_criteria>
    - `grep -n "api.token_revoke" lib/sigra/api_token.ex | head -1` returns a line containing **`api.token_revoke`**.
    - `grep -q "api.token_revoke_all" lib/sigra/api_token.ex` exits 0.
    - `grep -q "log_multi_safe" lib/sigra/api_token.ex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "api.token_revoke_all" lib/sigra/api_token.ex'</automated>
  </verify>
  <done>Revoke paths emit durable audit in same txn as DB mutation.</done>
</task>

<task type="execute">
  <name>Task 2: Extend api_token_audit_atomic_test</name>
  <files>test/sigra/api_token_audit_atomic_test.exs</files>
  <read_first>
    - test/sigra/api_token_audit_atomic_test.exs
    - lib/sigra/api_token.ex
  </read_first>
  <action>
    1. Add test: **`revoke/2`** with `:audit_schema` set — assert audit row **`action`** == **`api.token_revoke`** on success; force audit failure and assert token **not** revoked (`revoked_at` still `nil`).
    2. Add test: **`revoke_all/2`** — assert exactly **one** audit row with action **`api.token_revoke_all`** and metadata count matches revoked rows.
  </action>
  <acceptance_criteria>
    - `grep -q "api.token_revoke" test/sigra/api_token_audit_atomic_test.exs` exits 0.
    - `grep -q "api.token_revoke_all" test/sigra/api_token_audit_atomic_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/api_token_audit_atomic_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/api_token_audit_atomic_test.exs'</automated>
  </verify>
  <done>API token audit tests cover revoke + revoke_all atomicity.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/api_token_audit_atomic_test.exs`
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/` (if time permits in CI; else defer to final phase verify)
</verification>

<success_criteria>
- **AUD-07** API token lifecycle symmetry: create + revoke + revoke_all audited per **D-44-05**.
</success_criteria>
