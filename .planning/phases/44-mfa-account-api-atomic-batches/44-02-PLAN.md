---
phase: 44
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/sigra/audit.ex
  - test/sigra/audit_multi_step_test.exs
autonomous: true
requirements:
  - AUD-06
  - AUD-07
must_haves:
  truths:
    - "`Sigra.Audit.log_multi_safe/3` and `__log_internal__/3` accept a distinct Ecto.Multi operation name so two audit inserts compose without `:audit` key collision."
    - "`emit_telemetry_from_changes/1` fires `[:sigra, :audit, :log]` once per committed audit insert produced by those steps (and never on rollback); backward compatible when only `:audit` exists."
  artifacts:
    - path: "lib/sigra/audit.ex"
      provides: "Named Multi audit step support + telemetry"
nyquist_compliant: true
---

<objective>
Implement **D-44-02**: extend **`Sigra.Audit`** so a single `Ecto.Multi` can append **multiple** internal audit inserts with **distinct operation names**, and telemetry reflects **all** committed audit rows — unblocking MFA backup-verify dual audit in plan **44-03**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-44-02 | Repudiation | Second audit silently dropped | high | ExUnit proves two rows inserted in one transaction; grep shows both actions in DB |
| T-44-03 | Tampering | Telemetry fires on rolled-back txn | high | Test asserts zero `[:sigra, :audit, :log]` events when transaction returns `{:error, _}` |
</threat_model>

<context>
@.planning/phases/44-mfa-account-api-atomic-batches/44-CONTEXT.md
@.planning/phases/44-mfa-account-api-atomic-batches/44-RESEARCH.md
@lib/sigra/audit.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Named Multi audit step + telemetry</name>
  <files>lib/sigra/audit.ex</files>
  <read_first>
    - lib/sigra/audit.ex
    - lib/sigra/auth.ex
    - lib/sigra/api_token.ex
  </read_first>
  <action>
    1. Add optional keyword **`audit_multi_step`** (atom) to `log_multi_safe/3` and `__log_internal__/3` **opts**, default **`:audit`**, passed through to `do_log_multi/4` as the **Ecto.Multi** operation name (replace hard-coded `:audit` in `Ecto.Multi.insert/3`).
    2. Update **`emit_telemetry_from_changes/1`** to emit telemetry for **every** audit event in the post-transaction `changes` map: either (a) accept an optional second argument **`audit_step_names`** list defaulting to **`[:audit]`** for backward compatibility, or (b) scan `changes` for values that are `struct(audit_schema)` — pick one approach and document in `@moduledoc` under **Telemetry responsibility**.
    3. Ensure **all existing callers** without `audit_multi_step` behave identically (same `:audit` key, same single telemetry event) — run full **`mix test`** expectation.
    4. Add **`@doc`** examples showing two-step usage: `|> log_multi_safe("mfa.verify.success", Keyword.put(opts, :audit_multi_step, :audit_mfa_verify))` then second step with `:audit_mfa_backup` (exact atom names are implementer choice but must be **distinct**).
  </action>
  <acceptance_criteria>
    - `grep -q "audit_multi_step" lib/sigra/audit.ex` exits 0.
    - `grep -q "emit_telemetry_from_changes" lib/sigra/audit.ex` exits 0.
    - `mix format lib/sigra/audit.ex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "audit_multi_step" lib/sigra/audit.ex'</automated>
  </verify>
  <done>Audit module supports multiple named audit inserts per Multi.</done>
</task>

<task type="execute">
  <name>Task 2: Regression tests for multi-step audit + telemetry</name>
  <files>test/sigra/audit_multi_step_test.exs</files>
  <read_first>
    - lib/sigra/audit.ex
    - test/sigra/api_token_audit_atomic_test.exs
    - test/support/postgres_test_repo.ex
  </read_first>
  <action>
    1. Add **`test/sigra/audit_multi_step_test.exs`** using **`Sigra.Test.PostgresRepo`** (same style as `api_token_audit_atomic_test.exs`): minimal `audit_events` table, **no** production schema deps beyond existing test helpers.
    2. Test A: In one `Repo.transaction`, compose a trivial `Multi` with **two** `log_multi_safe` calls using **different** `audit_multi_step` atoms — assert **two** rows in `audit_events` with distinct `action` strings passed in test.
    3. Test B: Attach telemetry handler for `[:sigra, :audit, :log]` — assert **two** events on success, **zero** events when second insert is forced to fail (invalid attrs / constraint) and transaction rolls back.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/audit_multi_step_test.exs` exits 0.
    - `grep -q "audit_multi_step" test/sigra/audit_multi_step_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit_multi_step_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit_multi_step_test.exs'</automated>
  </verify>
  <done>Multi-step audit behavior and telemetry are pinned by tests.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit_multi_step_test.exs`
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/api_token_audit_atomic_test.exs` (guard regressions on `api.token_create`)
</verification>

<success_criteria>
- **D-44-02** satisfied in library with tests; MFA plan can depend on named steps.
</success_criteria>
