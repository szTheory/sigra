---
phase: 44
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - lib/sigra/account.ex
  - test/sigra/account_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-07
must_haves:
  truths:
    - "Each in-scope Account success path from D-44-04 uses Multi + log_multi_safe when audit_schema is set; execute_deletion preserves pre-delete audit in same rollback unit."
    - "No SMTP or Oban enqueue inside Repo.transaction for Account flows."
  artifacts:
    - path: "lib/sigra/account.ex"
      provides: "Atomic Account + audit boundaries"
nyquist_compliant: true
---

<objective>
Deliver **AUD-07** for **`Sigra.Account`**: convert inventoried **`log_safe` after `{:ok, _}`** success paths to **`Ecto.Multi` + `Audit.log_multi_safe`** per **D-44-04** priority stack; **`execute_deletion`** audit + delete share fate; add **Postgres atomicity tests**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-44-04 | Repudiation | Password/email change commits without audit | high | Atomicity tests per boundary with forced audit failure |
| T-44-05 | Denial of service | Long txn holds email delivery | medium | Code review + grep: no `Swoosh` / mailer calls inside `Repo.transaction` blocks |
</threat_model>

<context>
@.planning/phases/44-mfa-account-api-atomic-batches/44-CONTEXT.md
@.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
@lib/sigra/account.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Account Multi conversions per inventory</name>
  <files>lib/sigra/account.ex</files>
  <read_first>
    - lib/sigra/account.ex
    - lib/sigra/audit.ex
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-CONTEXT.md
  </read_first>
  <action>
    1. For each **`AUD-07`** inventory row in **`lib/sigra/account.ex`** targeting **`Multi`**, refactor to **`Ecto.Multi`** + **`Sigra.Audit.log_multi_safe`** inside **`config.repo.transaction/1`**, emitting **`Sigra.Audit.emit_telemetry_from_changes/1`** only on **`{:ok, changes}`**.
    2. Follow **D-44-04** priority: (1) `change_password` / forced password change, (2) **`confirm_email_change`**, (3) request/cancel email change, (4) schedule/cancel deletion, (5) **`execute_deletion`** last — **`execute_deletion`** MUST insert audit and perform hard delete steps in **one** Multi/transaction so audit failure rolls back delete.
    3. Keep **`Sigra.Workers.AccountDeletion`** out of scope unless **`44-AUD-04-INVENTORY.md`** contains an explicit AUD-07 row promoting it (default defer **AUD-08**).
    4. Update **D-26-style dispatch comments** at top of **`account.ex`** to list `Multi` vs `log_safe` per action string.
    5. Code review: ensure no new **`Swoosh`**, **`Mailer`**, or **`deliver`** calls appear inside **`Repo.transaction`** / **`Multi`** lambdas; keep email enqueue post-commit per **D-44-04**.
  </action>
  <acceptance_criteria>
    - `grep -q "log_multi_safe" lib/sigra/account.ex` exits 0.
    - `grep -q "emit_telemetry_from_changes" lib/sigra/account.ex` exits 0.
    - `grep -q "account.deletion_execute" lib/sigra/account.ex` exits 0 (audit action still present in module after refactor).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "log_multi_safe" lib/sigra/account.ex'</automated>
  </verify>
  <done>Account public entrypoints use Multi-bound audit per inventory.</done>
</task>

<task type="execute">
  <name>Task 2: Account audit atomicity tests</name>
  <files>test/sigra/account_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/api_token_audit_atomic_test.exs
    - lib/sigra/account.ex
  </read_first>
  <action>
    1. Add **`test/sigra/account_audit_atomicity_test.exs`** with Postgres repo and minimal user/account tables as required by existing Sigra test helpers.
    2. Minimum tests: **password change** (or **`set_password`**) happy path + audit rollback; **`execute_deletion`** proves audit insert + delete **both** absent when audit insert invalid.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/account_audit_atomicity_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/account_audit_atomicity_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/account_audit_atomicity_test.exs'</automated>
  </verify>
  <done>Account atomicity tests green.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/account_audit_atomicity_test.exs`
- Spot-check: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit_multi_step_test.exs`
</verification>

<success_criteria>
- **AUD-07** Account slice implemented per inventory with tests; **`execute_deletion`** semantics preserved or strengthened.
</success_criteria>
