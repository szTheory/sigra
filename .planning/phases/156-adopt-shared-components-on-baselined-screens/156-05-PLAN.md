---
phase: 156-adopt-shared-components-on-baselined-screens
plan: 05
type: execute
wave: 2
depends_on:
  - 156-01
  - 156-02
files_modified:
  - lib/sigra/admin/live/audit_index_live.ex
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/audit-explorer-admin-checkpoints-chromium.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/audit-explorer-admin-checkpoints-dark.png
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/audit-explorer-admin-checkpoints-mobile.png
autonomous: false
requirements:
  - COHR-01
  - COHR-04
  - COHR-06

must_haves:
  truths:
    - "`audit_index_live.ex` imports `Sigra.Admin.Components` (no duplicate component defp)"
    - "D-07: A `<.scope_ribbon copy={scope_copy(@admin_scope)} />` element appears immediately after the `<header class=\"sg-page-header\">` block (not nested inside it) — same scope-display component on every list/leaf screen"
    - "The `<p class=\"sg-page-copy\">{scope_copy(@admin_scope)}</p>` line inside the header is removed"
    - "The inline `sg-applied-chip` `:for` span loop is replaced by `<.applied_chip :for={…}>`"
    - "The inline `sg-empty-state` div is replaced by `<.empty_state>` with inner_block body text preserved verbatim"
    - "D-03 / D-04: Playwright baseline `audit-explorer` is re-recorded (×3 projects) only after HTML-report confirmation showing ONLY the COHR-04 scope-ribbon delta (byte-green default; no blanket re-record)"
    - "`impersonation-banner` snapshot stays byte-green"
  artifacts:
    - path: lib/sigra/admin/live/audit_index_live.ex
      provides: "import Sigra.Admin.Components; scope_ribbon after header; applied_chip; empty_state"
      contains: "import Sigra.Admin.Components"
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/audit-explorer-admin-checkpoints-chromium.png"
      provides: "Updated baseline with scope ribbon"
  key_links:
    - from: "audit_index_live.ex render/1"
      to: "Sigra.Admin.Components.scope_ribbon/1"
      via: "<.scope_ribbon copy={scope_copy(@admin_scope)} /> after </header>"
      pattern: "<\\.scope_ribbon"
    - from: "audit_index_live.ex render/1"
      to: "Sigra.Admin.Components.applied_chip/1"
      via: "<.applied_chip :for={chip <- applied_chips(@current_params)} …>"
      pattern: "<\\.applied_chip"
    - from: "audit_index_live.ex render/1"
      to: "Sigra.Admin.Components.empty_state/1"
      via: "<.empty_state title=\"No audit events match this view\">"
      pattern: "<\\.empty_state"
---

<objective>
Migrate `audit_index_live.ex` to use `Sigra.Admin.Components`: add the import, add `<.scope_ribbon>` after the page header (COHR-04), remove the `sg-page-copy` scope subtitle from the header, migrate inline applied-chip spans to `<.applied_chip>` (COHR-01), and migrate the inline empty-state div to `<.empty_state>` (COHR-06). Then re-record the `audit-explorer` Playwright baseline.

`audit_index_live.ex` has no shadowing `defp` for components (no `defp summary_chip`, `defp applied_chip`, etc.) — the import hazard is lower than other screens, but the import is still required for COHR-01 ("all screens import shared components").

Purpose: This screen drives the `audit-explorer` checkpoint slug. The scope-ribbon addition is the only intended visual delta (COHR-04) — the applied-chip and empty-state migrations are byte-neutral (already has `sg-stack sg-stack--3`).
Output: Migrated LiveView file + 3 updated `.png` baselines (1 slug × 3 projects).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-CONTEXT.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-RESEARCH.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-UI-SPEC.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-VALIDATION.md

<interfaces>
<!-- From lib/sigra/admin/components.ex -->

applied_chip/1 (replaces inline :for span loop at audit_index_live.ex:114-123):
  attr :label,       :string, required: true
  attr :remove_href, :string, required: true
  attr :class,       :any,    default: nil
  attr :rest,        :global
  Migration pattern (identical to users_index_live.ex):
    Before: <span :for={chip <- applied_chips(@current_params)} class="sg-applied-chip">…</span>
    After:  <.applied_chip
              :for={chip <- applied_chips(@current_params)}
              label={chip.label}
              remove_href={remove_chip_path(@admin_scope, @current_params, chip.key)}
            />

scope_ribbon/1 (new — placed immediately after </header> on list screens, per D-07/UI-SPEC):
  attr :copy, :string, required: true
  Emits: <span class="sg-muted sg-text-sm">{@copy}</span>
  Placement: immediately after `</header>` (closing tag of `<header class="sg-page-header">`),
  NOT nested inside the header. No sg-cluster wrapper (no page_back sibling on list screens).
  Remove: `<p class="sg-page-copy">{scope_copy(@admin_scope)}</p>` (line 53) from inside the header.

empty_state/1 (replaces inline div at audit_index_live.ex:172-188):
  attr :title, :string, required: true
  slot :inner_block
  Note: the current audit inline div already has `sg-stack sg-stack--3` — migration is byte-neutral.
  Body text to preserve verbatim (per CONTEXT.md):
    Filtered state: "No audit events match the active filters. Clear one or more to widen the timeline."
    Empty state: "Audit events appear here as activity is recorded. Adjust the filters above to focus on a specific actor, outcome, or time range."

<!-- Current header structure at audit_index_live.ex:49-54 (verified) -->
<header class="sg-page-header">
  <p class="sg-page-kicker">Audit evidence</p>
  <h1 class="sg-page-title">Audit</h1>
  <p class="sg-page-copy">{scope_copy(@admin_scope)}</p>   ← REMOVE THIS (line 53)
</header>
↓ (insert here)
<.scope_ribbon copy={scope_copy(@admin_scope)} />

<!-- Applied chips location at audit_index_live.ex:114-123 (verified) -->
<div :if={any_filter_active?(@current_params)} class="sg-cluster sg-cluster--start">
  <span :for={chip <- applied_chips(@current_params)} class="sg-applied-chip">
    <span>{chip.label}</span>
    <a class="sg-applied-chip__remove" href={remove_chip_path(@admin_scope, @current_params, chip.key)}
       aria-label={"Remove filter " <> chip.label}>
      <span aria-hidden="true">&times;</span>
      <span class="sr-only">remove</span>
    </a>
  </span>
</div>
After: replace inner :for span with <.applied_chip :for=… label=… remove_href=… />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate audit_index_live.ex — import, scope_ribbon, applied_chip, empty_state</name>
  <files>lib/sigra/admin/live/audit_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_index_live.ex — read the full file. Key sections: line 6 (use Phoenix.LiveView), lines 48-54 (header block with scope_copy at 53), lines 113-123 (applied_chip inline loop), lines 171-188 (empty_state block).
    - lib/sigra/admin/components.ex lines 161–220 — applied_chip, empty_state, scope_ribbon attr contracts.
    - .planning/phases/156-adopt-shared-components-on-baselined-screens/156-RESEARCH.md §"COHR-04: scope_ribbon Placement on List Screens" — exact placement rule confirmed for audit_index_live.
  </read_first>
  <action>
    Read `lib/sigra/admin/live/audit_index_live.ex` in full before editing.

    Atomic edit — all changes in one pass:

    1. Add `import Sigra.Admin.Components` immediately after line 6 (`use Phoenix.LiveView`).

    2. Remove `<p class="sg-page-copy">{scope_copy(@admin_scope)}</p>` (line 53) from inside `<header class="sg-page-header">`.

    3. After the closing `</header>` tag (line 54 area, after removing line 53), add:
       `<.scope_ribbon copy={scope_copy(@admin_scope)} />`
       This element is at the same indent level as `<header>`, outside it.

    4. Replace the inline applied-chip `:for` span loop (lines 114-122 area) with:
       `<.applied_chip :for={chip <- applied_chips(@current_params)} label={chip.label} remove_href={remove_chip_path(@admin_scope, @current_params, chip.key)} />`
       Keep the surrounding `<div :if={any_filter_active?(@current_params)} class="sg-cluster sg-cluster--start">` div.

    5. Replace the inline empty-state div (lines 172-188 area) with:
       ```heex
       <.empty_state :if={@rows == []} title="No audit events match this view">
         <%= if any_filter_active?(@current_params) do %>
           <p class="sg-muted sg-text-sm">No audit events match the active filters. Clear one or more to widen the timeline.</p>
           <div class="sg-cluster sg-cluster--center">
             <a href={index_path(@admin_scope)} class="sg-btn sg-btn--secondary sg-btn--sm">Clear all filters</a>
           </div>
         <% else %>
           <p class="sg-muted sg-text-sm">Audit events appear here as activity is recorded. Adjust the filters above to focus on a specific actor, outcome, or time range.</p>
         <% end %>
       </.empty_state>
       ```
       Preserve body text verbatim.

    `audit_index_live.ex` has no private `defp` component definitions (no defp summary_chip, applied_chip, or empty_state) — no defp deletion is required.

    After writing, run `mix test test/sigra/admin/` to confirm green before proceeding to Task 2.
  </action>
  <verify>
    <automated>
      # 1. Import present
      grep -c "import Sigra.Admin.Components" lib/sigra/admin/live/audit_index_live.ex

      # 2. scope_ribbon present after header
      grep -c "<\.scope_ribbon" lib/sigra/admin/live/audit_index_live.ex

      # 3. sg-page-copy scope prose removed
      grep -c "sg-page-copy.*scope_copy" lib/sigra/admin/live/audit_index_live.ex

      # 4. applied_chip component used
      grep -c "<\.applied_chip" lib/sigra/admin/live/audit_index_live.ex

      # 5. empty_state component used
      grep -c "<\.empty_state" lib/sigra/admin/live/audit_index_live.ex

      # 6. Admin tests green
      mix test test/sigra/admin/
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import Sigra.Admin.Components" lib/sigra/admin/live/audit_index_live.ex` returns 1.
    - `grep -c "<\.scope_ribbon" lib/sigra/admin/live/audit_index_live.ex` returns 1.
    - `grep -c "sg-page-copy.*scope_copy" lib/sigra/admin/live/audit_index_live.ex` returns 0.
    - `grep -c "<\.applied_chip" lib/sigra/admin/live/audit_index_live.ex` returns 1.
    - `grep -c "<\.empty_state" lib/sigra/admin/live/audit_index_live.ex` returns 1.
    - `mix test test/sigra/admin/` exits 0.
  </acceptance_criteria>
  <done>audit_index_live.ex imports Sigra.Admin.Components; scope_ribbon is placed after the header; applied_chip and empty_state components are wired; and admin tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review and re-record audit-explorer baseline</name>
  <what-built>Task 1 migrated audit_index_live.ex. The scope-ribbon addition is the intended visual delta for `audit-explorer`. The applied-chip and empty-state changes are byte-neutral (empty-state already had sg-stack sg-stack--3).</what-built>
  <how-to-verify>
    Prerequisite: the example app must be running on port 4000.
    Boot if needed: `cd test/example && mix phx.server`

    Step 1 — Run the checkpoint spec (expect 3 failures for audit-explorer ×3 projects):
    ```bash
    cd test/example/priv/playwright
    SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-chromium \
      --project=admin-checkpoints-mobile \
      --project=admin-checkpoints-dark
    ```
    Expected: audit-explorer ×3 fail, global-user-index/org-scoped-admin/user-detail (already re-recorded in 03/04) and impersonation-banner pass.

    Step 2 — Review the HTML diff report:
    ```bash
    npx playwright show-report
    ```
    For `audit-explorer` (×3 projects), confirm the diff shows:
    - A new `<span class="sg-muted sg-text-sm">…</span>` scope ribbon element appearing after the `</header>` tag.
    - The `<p class="sg-page-copy">` scope text inside the header is gone.
    - NO other layout changes. The audit table, filter form, applied chips, empty state are visually unchanged.
    If `impersonation-banner` fails: STOP — investigate before accepting.

    Step 3 — Only after confirming the diff shows ONLY the ribbon delta: update snapshots:
    ```bash
    SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-chromium \
      --project=admin-checkpoints-mobile \
      --project=admin-checkpoints-dark \
      --update-snapshots
    ```

    Step 4 — Run again to confirm all 15 tests pass:
    ```bash
    SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/admin-checkpoints.spec.ts \
      --project=admin-checkpoints-chromium \
      --project=admin-checkpoints-mobile \
      --project=admin-checkpoints-dark
    ```
    Expected: 15/15 pass.
  </how-to-verify>
  <resume-signal>Type "approved" once all 15 checkpoint tests pass and the 3 updated audit-explorer .png files are committed alongside the audit_index_live.ex change. Or describe any unexpected diff.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView render → browser | UI migration; all data is from authenticated admin context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-05-01 | Information Disclosure | scope_ribbon copy on audit screen | accept | `scope_copy(@admin_scope)` is already rendered in the `sg-page-copy` subtitle. Moving it to a ribbon element does not disclose new information. Admin auth guards are at the plug level, unchanged. |
| T-156-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

**Security note:** This plan is a UI migration. Audit data rendering logic is unchanged. The scope ribbon renders the scope string which is already visible to the authenticated admin. No new input surfaces, data flows, or auth paths introduced.
</threat_model>

<verification>
After both tasks:
1. `grep -c "import Sigra.Admin.Components" lib/sigra/admin/live/audit_index_live.ex` → 1
2. `grep -c "<\.scope_ribbon" lib/sigra/admin/live/audit_index_live.ex` → 1
3. `grep -c "sg-page-copy.*scope_copy" lib/sigra/admin/live/audit_index_live.ex` → 0
4. Checkpoint spec: 15/15 pass (3 audit-explorer re-recorded, impersonation-banner byte-green).
5. `mix test test/sigra/admin/` exits 0.
</verification>

<success_criteria>
`audit_index_live.ex` imports Sigra.Admin.Components; scope ribbon appears after the header; applied_chip and empty_state are wired; `audit-explorer` baseline is re-recorded with only the COHR-04 ribbon delta; `impersonation-banner` remains byte-green.
</success_criteria>

<output>
Create `.planning/phases/156-adopt-shared-components-on-baselined-screens/156-05-SUMMARY.md` when done.
</output>
