---
phase: 156-adopt-shared-components-on-baselined-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/static/assets/css/app.css
autonomous: true
requirements:
  - COHR-05

must_haves:
  truths:
    - "The `.sg-list-row[data-tone]` and `.sg-notice[data-tone]` tone rules are expressed as a single shared-selector block inside `@layer sg-components`"
    - "Zero rendered bytes change — the merged selectors produce identical visual output to the two separate blocks"
    - "The `.sg-list-row` base (non-tone) rules remain intact for audit/invitation rows that use it as a non-alert layout primitive"
  artifacts:
    - path: test/example/priv/static/assets/css/app.css
      provides: "Merged tone selector block replacing the two duplicate `[data-tone]` blocks at lines 952-967 and 978-993"
      contains: ".sg-list-row[data-tone=\"ok\"], .sg-notice[data-tone=\"ok\"]"
  key_links:
    - from: "app.css @layer sg-components"
      to: ".sg-list-row[data-tone] + .sg-notice[data-tone]"
      via: "shared-selector merge"
      pattern: "sg-list-row.*data-tone.*sg-notice.*data-tone"
---

<objective>
Merge the duplicate `.sg-list-row[data-tone]` and `.sg-notice[data-tone]` CSS tone rules from two identical blocks into one shared-selector block (D-08). This is a CSS-only, zero-rendered-byte change that eliminates the drift risk now that COHR-05 is closing the notice-migration window.

Purpose: Once `UserShowLive` migrates its `sg-list-row` alert row to `<.notice>`, both selectors must stay in sync. Merging them now before the LiveView migration ensures no drift is ever possible.
Output: One merged tone block in `app.css` replacing two byte-clone blocks, all inside `@layer sg-components`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-CONTEXT.md
@.planning/phases/156-adopt-shared-components-on-baselined-screens/156-RESEARCH.md

<interfaces>
<!-- CSS blocks to merge — both extracted from test/example/priv/static/assets/css/app.css. -->

Existing sg-list-row tone block (lines 952–967):
```
.sg-list-row[data-tone="ok"]   { background: color-mix(…); box-shadow: inset 3px 0 0 … }
.sg-list-row[data-tone="warn"] { … }
.sg-list-row[data-tone="risk"] { … }
.sg-list-row[data-tone="info"] { … }
```

Existing sg-notice tone block (lines 978–993) — byte-clone of the above:
```
.sg-notice[data-tone="ok"]   { background: color-mix(…); box-shadow: inset 3px 0 0 … }
.sg-notice[data-tone="warn"] { … }
.sg-notice[data-tone="risk"] { … }
.sg-notice[data-tone="info"] { … }
```

Target (replaces both blocks):
```
.sg-list-row[data-tone="ok"],  .sg-notice[data-tone="ok"]   { … }
.sg-list-row[data-tone="warn"], .sg-notice[data-tone="warn"] { … }
.sg-list-row[data-tone="risk"], .sg-notice[data-tone="risk"] { … }
.sg-list-row[data-tone="info"], .sg-notice[data-tone="info"] { … }
```

The `.sg-list-row` base block (lines 945–951: border-radius, background, box-shadow, padding, transition) is NOT touched — it is preserved verbatim as the non-alert layout primitive contract.
The `.sg-notice` base block (lines 971–977) is the byte-clone of `.sg-list-row` base — it is also preserved verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge sg-list-row and sg-notice tone rules into shared-selector block</name>
  <files>test/example/priv/static/assets/css/app.css</files>
  <read_first>
    - test/example/priv/static/assets/css/app.css lines 940–1000 — read the full context of both tone blocks and the base rules surrounding them before editing. Confirm the exact byte content of each `[data-tone]` value block so the merged selector matches precisely.
    - .planning/phases/156-adopt-shared-components-on-baselined-screens/156-RESEARCH.md §"D-08: Notice Tone CSS Merge" — verified line numbers and merge target format.
    - .planning/phases/156-adopt-shared-components-on-baselined-screens/156-CONTEXT.md §D-08 — locked decision: shared-selector merge inside `@layer sg-components`, zero new class, zero rendered-byte change.
  </read_first>
  <action>
    Read `test/example/priv/static/assets/css/app.css` lines 940–1000 to confirm current content.

    The edit is confined to the `[data-tone]` sub-blocks only (lines approximately 952–967 and 978–993). The base blocks for `.sg-list-row` (lines 945–951) and `.sg-notice` (lines 971–977) are untouched.

    Replace the four `.sg-list-row[data-tone="X"]` rules and the four `.sg-notice[data-tone="X"]` duplicate rules with four shared-selector rules of the form:
    `.sg-list-row[data-tone="ok"], .sg-notice[data-tone="ok"] { … }` — one rule per tone value (`ok`, `warn`, `risk`, `info`), each using the identical declaration block that was previously duplicated.

    The merged block stays inside the existing `@layer sg-components { }` boundary — do not move it outside or add a new layer declaration.

    Constraint: no new CSS class is introduced; no value in any declaration changes; the only change is selector consolidation. The `.sg-list-row` base block and the `.sg-notice` base block each remain as separate rules with their original single-class selectors.
  </action>
  <verify>
    <automated>
      # 1. Confirm merged selector pattern is present (check both orderings count as one)
      grep -c "sg-list-row\[data-tone=\"ok\"\].*sg-notice" test/example/priv/static/assets/css/app.css

      # 2. Confirm no remaining lone .sg-notice[data-tone] rules exist (should be 0 lone-notice tone lines)
      grep -v "sg-list-row" test/example/priv/static/assets/css/app.css | grep -c "sg-notice\[data-tone"

      # 3. Full ExUnit still green (component goldens must not be disturbed by a CSS change)
      mix test test/sigra/admin/components_test.exs
    </automated>
  </verify>
  <acceptance_criteria>
    - `app.css` contains exactly four shared-selector tone rules of the form `.sg-list-row[data-tone="X"], .sg-notice[data-tone="X"] { … }` (one per tone value).
    - No lone `.sg-notice[data-tone="X"]` rules exist in the file (the duplicates are gone).
    - The `.sg-list-row` base block (non-tone: border-radius, background, box-shadow, padding, transition) is intact and unchanged.
    - The `.sg-notice` base block is intact and unchanged.
    - All rules are inside `@layer sg-components { }`.
    - `mix test test/sigra/admin/components_test.exs` exits 0.
  </acceptance_criteria>
  <done>The two byte-clone `[data-tone]` blocks are replaced by one shared-selector block; zero rendered bytes change; `components_test.exs` stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS asset → browser | app.css is a static file served by the host; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — CSS-only edit, no new deps |

**Security note:** This plan is a CSS-only refactor. No new input surfaces, no data flows, no auth/session/token code paths are touched. ASVS L1 has no applicable controls for selector consolidation within a static stylesheet. No security threat applies.
</threat_model>

<verification>
After the plan executes:
1. `grep -c "sg-list-row\[data-tone=\"ok\"\].*sg-notice" test/example/priv/static/assets/css/app.css` returns 1.
2. `grep -v "sg-list-row" test/example/priv/static/assets/css/app.css | grep -c "sg-notice\[data-tone"` returns 0.
3. `mix test test/sigra/admin/components_test.exs` exits 0.
4. The full Playwright checkpoint spec (run after Phase 156 is complete at the phase-gate plan) confirms all 5 slug snapshots are byte-green for the CSS merge — `impersonation-banner`, `global-user-index`, `org-scoped-admin`, `audit-explorer`, `user-detail` render identical tone colors.
</verification>

<success_criteria>
The `.sg-list-row[data-tone]` and `.sg-notice[data-tone]` tone rules share a single declaration per tone, inside `@layer sg-components`, with no rendered-byte change, and `components_test.exs` stays green.
</success_criteria>

<output>
Create `.planning/phases/156-adopt-shared-components-on-baselined-screens/156-01-SUMMARY.md` when done.
</output>
