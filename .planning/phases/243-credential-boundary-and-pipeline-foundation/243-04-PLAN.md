---
phase: 243-credential-boundary-and-pipeline-foundation
plan: 04
type: execute
wave: 3
depends_on: [243-01, 243-02]
files_modified:
  - lib/sigra/plug/fetch_bearer.ex
  - test/sigra/plug/fetch_bearer_test.exs
autonomous: true
requirements: [API-01]
must_haves:
  truths:
    - "Installed FetchBearer callers retain deterministic prefix/JWT/default dispatch and receive normal Scope plus bounded credential facts."
    - "FetchBearer is explicitly deprecated and is not presented as a primary credential pipeline."
    - "The compatibility dispatcher does not rewrite host routers or invent a new autodetection contract."
  artifacts:
    - path: "lib/sigra/plug/fetch_bearer.ex"
      provides: "Deprecated compatibility dispatcher over explicit credential pipelines"
    - path: "test/sigra/plug/fetch_bearer_test.exs"
      provides: "Legacy dispatch, normal-Scope, metadata, and deprecation evidence"
  key_links:
    - from: "lib/sigra/plug/fetch_bearer.ex"
      to: "Sigra.Plug.FetchAPIToken"
      via: "legacy prefix/default branch delegation"
      pattern: "FetchAPIToken"
    - from: "lib/sigra/plug/fetch_bearer.ex"
      to: "Sigra.Plug.FetchJWT"
      via: "legacy enabled-JWT branch delegation"
      pattern: "FetchJWT"
  prohibitions:
    - "No raw credential in assigns, private data, logs, or telemetry."
    - "No client-derived scopes."
    - "No app-session storage or endpoints."
    - "No generator route changes."
    - "No Lockspire or Crosswake source changes."
---

<objective>
Turn `FetchBearer` into a deprecated compatibility dispatcher over the explicit credential-kind pipelines without breaking installed hosts (API-01).

Purpose: Existing routers remain deterministic while all successful authentication now obeys the normal-Scope/private-facts boundary.
Output: Refactored compatibility Plug and focused compatibility/deprecation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-CONTEXT.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-PATTERNS.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-VALIDATION.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-01-SUMMARY.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-02-SUMMARY.md

<interfaces>
- Preserve `FetchBearer.init/1`, `call/2`, required `:config`/`:scope_module`, existing-Scope skip, configured-prefix-first dispatch, enabled-`eyJ` JWT dispatch, and opaque default.
- Delegate successful branches through `FetchAPIToken` and `FetchJWT`; do not reconstruct token-shaped Scope maps.
</interfaces>
</context>

## Artifacts this phase produces

- `lib/sigra/plug/credential_auth.ex` — internal `Sigra.Plug.CredentialAuth` with `put_verified_scope/5` and bounded `build_scope/2` compatibility adapter.
- `lib/sigra/plug/fetch_api_token.ex` — public `Sigra.Plug.FetchAPIToken.init/1` and `call/2`.
- `lib/sigra/plug/fetch_jwt.ex` — public `Sigra.Plug.FetchJWT.init/1` and `call/2`.
- `lib/sigra/plug/fetch_app_session.ex` — public fail-closed `Sigra.Plug.FetchAppSession.init/1` and `call/2`.
- `lib/sigra/plug/fetch_session.ex` — updated public `Sigra.Plug.FetchSession` with full-user Scope construction and bounded legacy compatibility.
- `lib/sigra/plug/fetch_bearer.ex` — updated deprecated `Sigra.Plug.FetchBearer` compatibility dispatcher.
- `lib/sigra/plug/require_scopes.ex` — updated public `Sigra.Plug.RequireScopes` with trusted private-facts enforcement.
- `conn.private[:sigra_auth]` — stable bounded credential-facts map keyed by credential kind.
- `test/sigra/plug/fetch_api_token_test.exs`, `fetch_jwt_test.exs`, `fetch_app_session_test.exs` — explicit-pipeline contract modules.
- `test/sigra/plug/fetch_session_test.exs`, `fetch_bearer_test.exs`, `require_scopes_test.exs` — focused browser compatibility, legacy dispatch, and fail-closed scope-enforcement contracts.
- `test/sigra/credential_boundary_docs_test.exs` — normative ownership and primary-pipeline documentation guard.
- `guides/introduction/contract.md`, `guides/flows/api-authentication.md`, `guides/recipes/companion-libs/lockspire.md` — normative ownership and usage contract.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve legacy dispatch while delegating to explicit credential kinds (API-01)</name>
  <reversibility rating="costly">Compatibility behavior is relied on by installed routers; removal or precedence changes require a declared coordinated migration.</reversibility>
  <files>test/sigra/plug/fetch_bearer_test.exs, lib/sigra/plug/fetch_bearer.ex</files>
  <read_first>
Read the current production/test pair completely, then the completed explicit PAT/JWT Plugs. Record the existing prefix derivation and branch precedence in the test before editing. Check current project deprecation style with `rg -n '@deprecated|deprecated:' lib test | head -40` and use that established form.
  </read_first>
  <behavior>
    - RED first: configured prefix selects PAT even when bytes resemble another type; enabled `eyJ` selects JWT; every other input takes the historical opaque/PAT default.
    - Both successful branches now assign a full-user normal Scope and the same exact credential-kind facts as their explicit Plug.
    - Failure is nil Scope/no facts; existing Scope skips all work.
    - Module/function documentation exposes deterministic migration guidance to `FetchAPIToken` or `FetchJWT` without removing the callable API.
  </behavior>
  <action>
Rewrite expected behavior away from token-shaped Scope maps before changing production. Keep the legacy classifier exactly bounded to its existing three-way precedence, then dispatch to the already-tested explicit Plug paths so verifier, live-user, Scope, and facts behavior cannot diverge. Mark the compatibility surface deprecated using the repository's compiler/doc convention and name both replacements. The generalized primary noun is credential kind; avoid adding new token-type abstractions. Preserve current opts and skip semantics. Do not edit installer templates, generated routers, or primary guides in this task.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/plug/fetch_bearer_test.exs test/sigra/plug/fetch_api_token_test.exs test/sigra/plug/fetch_jwt_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
Every pre-existing dispatch branch is deterministic and green, successful callers receive normal Scope plus exact bounded facts, failure/skip behavior is preserved, and deprecation guidance names explicit replacements.
  </acceptance_criteria>
  <done>FetchBearer remains a safe compatibility dispatcher only; explicit credential kinds are the primary public model.</done>
</task>

</tasks>

<threat_model>
## ASVS posture

Level 1; blocking threshold high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy bearer bytes → compatibility classifier | Untrusted bytes select one historical verification branch. |
| Dispatcher → explicit Plugs | Compatibility must not bypass the new verified credential seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-243-13 | Spoofing | FetchBearer branch selection | high | mitigate | Lock the existing precedence in tests and delegate each result to exactly one explicit verifier path. |
| T-243-14 | Tampering | Legacy Scope creation | high | mitigate | Remove token-shaped construction and reuse explicit normal-Scope/private-facts behavior. |
| T-243-15 | Information Disclosure | Compatibility results | high | mitigate | Reuse explicit facts allowlists; prove raw credential absence in compatibility tests. |
| T-243-16 | Repudiation | Deprecation contract | low | accept | Stable compiler/documentation guidance makes legacy use observable without changing runtime behavior. |
</threat_model>

<verification>
Run the complete PAT/JWT/bearer focused command in the task plus `mix format --check-formatted lib/sigra/plug/fetch_bearer.ex test/sigra/plug/fetch_bearer_test.exs`.
</verification>

<success_criteria>
- Existing FetchBearer installations retain deterministic behavior.
- Compatibility success obeys the same normal-Scope and bounded-facts contract as explicit Plugs.
- The public migration direction is unambiguously toward credential-kind-specific pipelines.
</success_criteria>

<output>
Create `.planning/phases/243-credential-boundary-and-pipeline-foundation/243-04-SUMMARY.md` when done.
</output>
