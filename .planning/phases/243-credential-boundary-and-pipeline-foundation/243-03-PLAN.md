---
phase: 243-credential-boundary-and-pipeline-foundation
plan: 03
type: execute
wave: 2
depends_on: [243-01]
files_modified:
  - lib/sigra/plug/fetch_session.ex
  - test/sigra/plug/fetch_session_test.exs
autonomous: true
requirements: [API-01]
must_haves:
  truths:
    - "A valid browser session reloads the full current user and constructs the host's normal Scope."
    - "Generated struct Scope modules use canonical Scope construction while existing non-struct modules with new/1 remain supported."
    - "A session whose user was deleted is unauthenticated, and browser sessions never gain delegated scopes."
  artifacts:
    - path: "lib/sigra/plug/fetch_session.ex"
      provides: "Full-user browser-session Scope construction with bounded compatibility"
    - path: "test/sigra/plug/fetch_session_test.exs"
      provides: "Generated-struct and legacy non-struct compatibility evidence"
  key_links:
    - from: "lib/sigra/plug/fetch_session.ex"
      to: "config.repo.get(config.user_schema, session.user_id)"
      via: "live current-user resolution"
      pattern: "repo\\.get"
    - from: "lib/sigra/plug/fetch_session.ex"
      to: "Sigra.Plug.CredentialAuth.build_scope/2"
      via: "canonical struct construction plus bounded legacy adapter"
      pattern: "CredentialAuth\\.build_scope"
  prohibitions:
    - "No raw credential in assigns, private data, logs, or telemetry."
    - "No client-derived scopes."
    - "No app-session storage or endpoints."
    - "No generator route changes."
    - "No Lockspire or Crosswake source changes."
---

<objective>
Correct the browser-cookie path to load a full current user while preserving its established host Scope compatibility (API-01).

Purpose: The normal-Scope invariant must cover browser sessions without breaking installed hosts whose non-struct scope modules expose only `new/1`.
Output: Updated `FetchSession` and deterministic compatibility/deleted-user coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-CONTEXT.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-PATTERNS.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-VALIDATION.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-01-SUMMARY.md

<interfaces>
- Existing `FetchSession` options and timeout/activity/remember-me behavior are source-compatible contracts.
- `Sigra.Plug.CredentialAuth.build_scope/2` uses `Sigra.Scope.build/3` for generated struct Scope modules and `scope_module.new(user)` only for legacy non-struct modules.
- Browser-session state remains in `conn.private[:sigra_session]`; it does not become scoped credential metadata.
</interfaces>
</context>

## Artifacts this phase produces

- `lib/sigra/plug/credential_auth.ex` — internal `Sigra.Plug.CredentialAuth` with `put_verified_scope/5` and bounded `build_scope/2` compatibility adapter.
- `lib/sigra/plug/fetch_api_token.ex` — public `Sigra.Plug.FetchAPIToken.init/1` and `call/2`.
- `lib/sigra/plug/fetch_jwt.ex` — public `Sigra.Plug.FetchJWT.init/1` and `call/2`.
- `lib/sigra/plug/fetch_app_session.ex` — public fail-closed `Sigra.Plug.FetchAppSession.init/1` and `call/2`.
- `conn.private[:sigra_auth]` — stable bounded credential-facts map keyed by credential kind.
- `test/sigra/plug/fetch_api_token_test.exs`, `fetch_jwt_test.exs`, `fetch_app_session_test.exs` — explicit-pipeline contract modules.
- `test/sigra/credential_boundary_docs_test.exs` — normative ownership and primary-pipeline documentation guard.
- `guides/introduction/contract.md`, `guides/flows/api-authentication.md`, `guides/recipes/companion-libs/lockspire.md` — normative ownership and usage contract.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load full browser-session users through the compatibility adapter (API-01)</name>
  <reversibility rating="costly">The adapter preserves two installed-host Scope forms; changing its dispatch later can break existing host modules across call sites.</reversibility>
  <files>test/sigra/plug/fetch_session_test.exs, lib/sigra/plug/fetch_session.ex</files>
  <read_first>
Read the complete current `fetch_session.ex` and its test once, the generated Scope template at `priv/templates/sigra.install/core/scope.ex`, and the completed `credential_auth.ex` from 243-01. Inventory existing timeout, deletion, activity, remember-me, MFA-pending, cookie, and private-session assertions so none are lost.
  </read_first>
  <behavior>
    - RED first: a valid session performs `repo.get(user_schema, session.user_id)` and a generated-like struct Scope contains that exact user.
    - A non-struct test Scope exporting `new/1` receives the loaded user and remains supported.
    - A valid session row pointing to a missing user assigns nil Scope and does not install authenticated private state.
    - Existing idle/absolute expiry, activity throttling, remember-me, MFA-pending, cookie, and session-private behavior remains green.
    - No browser path writes delegated scope authority to `sigra_auth`.
  </behavior>
  <action>
Add the generated-like struct, legacy non-struct, and deleted-user tests before changing production code. Then update only the successful browser-session branch: resolve the full current user and construct Scope through the 243-01 adapter. Per the locked compatibility clarification, struct modules receive canonical `Sigra.Scope.build/3` construction while existing non-struct modules continue through their `new(user)` contract. Missing users fail as unauthenticated. Preserve session validation, activity updates, MFA-pending propagation, remember-me behavior, public options, and `conn.private[:sigra_session]`. Do not write credential scopes or create `sigra_auth` for browser identity.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/plug/fetch_session_test.exs test/sigra/plug/fetch_api_token_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
Both Scope-module shapes pass using a loaded full user, deleted-user sessions fail closed, and every pre-existing browser-session behavior remains covered and green.
  </acceptance_criteria>
  <done>FetchSession emits normal current-user Scope state without a source-compatibility break or delegated-scope confusion.</done>
</task>

</tasks>

<threat_model>
## ASVS posture

Level 1; blocking threshold high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Session-store record → current user | A valid session reference must resolve to a currently existing user. |
| Sigra → host Scope module | Canonical struct construction and legacy non-struct compatibility meet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-243-09 | Spoofing | `FetchSession.call/2` | high | mitigate | Reload the current user and return nil when it no longer exists. |
| T-243-10 | Elevation of Privilege | Browser Scope projection | high | mitigate | Keep browser sessions identity-only; write no delegated scopes or scoped credential facts. |
| T-243-11 | Tampering | Scope compatibility adapter | medium | mitigate | Bound dispatch to struct canonical build or existing non-struct `new(user)` and cover both shapes. |
| T-243-12 | Denial of Service | Existing session lifecycle | low | accept | Preserve already-tested timeout/activity behavior; this plan adds one bounded Repo lookup per valid session. |
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/sigra/plug/fetch_session_test.exs test/sigra/plug/fetch_api_token_test.exs test/sigra/plug/require_scopes_test.exs --trace`.
</verification>

<success_criteria>
- FetchSession loads a full live user before assigning a Scope.
- Generated and legacy Scope forms remain compatible.
- Deleted users and browser attempts to imply delegated scopes fail closed.
</success_criteria>

<output>
Create `.planning/phases/243-credential-boundary-and-pipeline-foundation/243-03-SUMMARY.md` when done.
</output>
