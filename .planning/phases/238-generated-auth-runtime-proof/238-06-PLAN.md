---
phase: 238-generated-auth-runtime-proof
plan: 06
type: execute
wave: 5
depends_on:
  - 238-05
files_modified:
  - scripts/ci/generated-auth-runtime-proof.sh
  - test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
  - .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "AUTH-01 / D-01 / D-02 / D-05: the direct Generated auth runtime proof job completes the serial registration, confirmation, password sign-in/logout, magic-link, and password-reset journey against the fresh generated B2C host."
    - "AUTH-02 / D-03: that exact job completes the focused and full-suite Google start/callback/account-link-collision paths through the credential-free loopback provider double."
    - "AUTH-03 / D-04 / D-06: that exact job executes every named rendered-state Axe, label/control, and duplicate-ID assertion with role/label selectors, LiveView readiness, zero Playwright retries, and no browser sleeps."
    - "The harness resolves its retained script from the already-computed CI_DIR after entering the disposable app; the current working directory cannot break its focused-probe contract check."
    - "A green workflow and green direct job are correlated to the immutable repair implementation SHA; local source checks, human UAT, unrelated SHAs, and skipped/cancelled/red results remain insufficient."
  artifacts:
    - path: scripts/ci/generated-auth-runtime-proof.sh
      provides: "Working-directory-independent retained-harness self-reference"
      contains: "${CI_DIR}/generated-auth-runtime-proof.sh"
    - path: test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
      provides: "Regression contract for the post-cd CI_DIR self-reference and unchanged deterministic browser lane"
      contains: "CI_DIR"
    - path: .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
      provides: "Green exact-implementation-SHA workflow/job receipt with retained failed-attempt provenance"
      contains: "generated_auth_runtime_proof"
  key_links:
    - from: scripts/ci/generated-auth-runtime-proof.sh
      to: scripts/ci/generated-auth-runtime-proof.sh
      via: "assert_locked_contract reads the retained harness through CI_DIR after boot_and_run_spec changes into APP_DIR"
      pattern: "CI_DIR.*generated-auth-runtime-proof\\.sh"
    - from: .github/workflows/generated-auth-runtime-proof.yml
      to: scripts/ci/generated-auth-runtime-proof.sh
      via: "the explicitly dispatched PostgreSQL/Chromium workflow executes the corrected retained harness with --all"
      pattern: "generated-auth-runtime-proof.sh --all"
    - from: .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
      to: .github/workflows/ci.yml
      via: "one immutable head SHA identifies the successful CI workflow and Generated auth runtime proof job"
      pattern: "head_sha"
---

<objective>
Close the exact-SHA runtime blocker for AUTH-01, AUTH-02, and AUTH-03 by repairing the harness's post-`cd` self-reference and recording a machine-validated green direct `Generated auth runtime proof` job for the corrected implementation SHA.

Purpose: Plans 238-01 through 238-04 installed the generated-host journeys and CI lane, but Plan 238-05 proved that a working-directory-dependent harness assertion stops both browser specs before any runtime truth can be credited. This gap plan targets that execution miss without changing D-01 through D-06 or widening product scope.
Output: A working-directory-independent harness, a regression lock for that path, and durable exact-SHA success evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md
@.planning/phases/238-generated-auth-runtime-proof/238-RESEARCH.md
@.planning/phases/238-generated-auth-runtime-proof/238-VALIDATION.md
@.planning/phases/238-generated-auth-runtime-proof/238-VERIFICATION.md
@.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json
@.planning/phases/238-generated-auth-runtime-proof/238-04-SUMMARY.md
@.planning/phases/238-generated-auth-runtime-proof/238-05-SUMMARY.md
@scripts/ci/generated-auth-runtime-proof.sh
@test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs
@.github/workflows/ci.yml
</context>

<gap_closure>
## Existing verification gaps closed by this plan

| Requirement | Failed observable truth | Exact missing proof |
|-------------|-------------------------|---------------------|
| AUTH-01 | Registration, confirmation, password sign-in/logout, magic-link request/verification, and password-reset completion have no successful generated-host browser execution. | Correct the retained-harness self-reference and require a green exact-SHA `--all` CI execution. |
| AUTH-02 | Both exact-SHA attempts failed before the focused/full Google start, callback, and collision probes could execute. | Require the focused OAuth probe and full generated-auth suite to pass in the same green direct job. |
| AUTH-03 | Axe, stable label/control, and duplicate-ID assertions exist but remain unexecuted against the generated host. | Require the complete browser job to exercise the named auth states successfully. |

Root cause category: execution miss. The harness computed `CI_DIR` before changing directories but `assert_locked_contract` later used the invocation path while the process was inside `APP_DIR`. The fix must consume the already-established absolute directory instead of deriving a path from the disposable host or an unavailable repository object.
</gap_closure>

<assumptions>
## Explicit unresolved fallback-edge assumptions

- **A-GAP-01 / AUTH-01:** concurrency fallback probing is unresolved. The actionable verifier gap requires the existing one-serial-journey runtime lane to execute successfully; this plan does not claim concurrency behavior from that result.
- **A-GAP-02 / AUTH-02:** unclassified fallback probing is unresolved. The green job proves only the specified Google start/callback/existing-email collision contract and does not silently classify additional provider outcomes.
- **A-GAP-03 / AUTH-03:** adjacency, empty-value, encoding, and ordering fallback probes are unresolved. The green job proves the committed named-state Axe, label/control, and duplicate-ID predicates; it does not claim these additional edge dimensions.

These assumptions are recorded because no Phase 238 SPEC defines those fallback probes and `238-VERIFICATION.md` does not list them as actionable gaps. They remain unclaimed rather than being invented, waived, or represented as covered by the exact-SHA receipt.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair the retained harness and prove all three auth requirements on its exact SHA</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`; the executor can push a scoped evidence ref and dispatch the existing `CI` workflow; if GitHub reports REST core remaining at 250 or fewer, or returns HTTP 403/429, execution halts until the reported reset/retry time without further polling.</precondition>
  <files>scripts/ci/generated-auth-runtime-proof.sh, test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs, .planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json</files>
  <read_first>
    - `AGENTS.md` for deterministic Playwright, one-watcher, 60-second interval, rate-limit, structured-summary, failed-log, retry, and durable-evidence rules.
    - `.planning/phases/238-generated-auth-runtime-proof/238-VERIFICATION.md` for all three failed truths and the authoritative requirement that source checks or unrelated SHAs cannot close them.
    - `.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json` and `238-05-SUMMARY.md` for failed run IDs `31019501361` and `31021611104`, the blocked status, and the exact self-reference diagnosis to preserve as prior-attempt provenance.
    - `scripts/ci/generated-auth-runtime-proof.sh` for `CI_DIR`, `assert_locked_contract`, the later `cd "${APP_DIR}"`, the explicit `--all` allowlist, and the zero-retry dedicated-project invocation.
    - `test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs` for the existing source-lock style and its D-01 through D-06 browser/OAuth/accessibility predicates.
    - `.github/workflows/generated-auth-runtime-proof.yml` for the workflow-dispatch-only exact-ref contract, direct display name, PostgreSQL/Chromium prerequisites, harness command, and diagnostic artifacts.
  </read_first>
  <behavior>
    - Test 1: after the harness has entered the disposable generated application, `assert_locked_contract` reads the retained repository script at `${CI_DIR}/generated-auth-runtime-proof.sh` and finds the focused-probe entry point.
    - Test 2: the Phase 238 source-contract test fails if the self-reference stops using the already-computed CI directory, while continuing to pin serial Playwright, retries 0, observable mailbox/LiveView readiness, scoped Axe, label/control, duplicate-ID, and no fixed browser-delay behavior per D-01 through D-06.
    - Test 3: the corrected implementation commit's direct `Generated auth runtime proof` job runs `--all` and concludes success, proving AUTH-01, AUTH-02, and AUTH-03 at runtime without provider credentials or external provider traffic.
    - Test 4: the evidence receipt is rejected unless its workflow run head SHA equals the frozen repair commit, the workflow and direct job both conclude success, and all post-repair-commit changes are limited to evidence/planning artifacts.
  </behavior>
  <action>
    First add a failing regression assertion to `Sigra.Planning.Phase238GeneratedAuthRuntimeProofTest` that pins the retained harness path used by `assert_locked_contract`. Then change that function to resolve its supporting script through the already-computed absolute `CI_DIR`, specifically `${CI_DIR}/generated-auth-runtime-proof.sh`; do not reconstruct the path from the current directory, a git object, or the invocation string. Keep the existing generated-host lifecycle, guarded cleanup, loopback OIDC double, role/label selectors, LiveView readiness, `expect.poll` mailbox synchronization, scoped Axe/DOM gates, one worker, and Playwright retries 0 unchanged per D-01 through D-06. Run `bash -n scripts/ci/generated-auth-runtime-proof.sh` and `MIX_ENV=test mix test test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs`, then commit the repair and record that immutable 40-character SHA before editing evidence.

    Push the repair commit on one scoped evidence branch/ref. Immediately before any long wait, run one `gh api rate_limit` preflight and record `resources.core.remaining` and `reset`; do not start or continue GitHub polling at 250 or fewer remaining requests. Dispatch `.github/workflows/generated-auth-runtime-proof.yml` explicitly with `gh workflow run "Generated auth runtime proof" --ref EXACT_REF -f evidence_ref=EXACT_REF`; the workflow must be workflow-dispatch-only and `evidence_ref` must equal the dispatched ref. Correlate one new workflow by workflow identity, event, ref, dispatch time, and exact `headSha`; reject ambiguity. Use exactly one active watcher for that run with the literal command `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; no second agent or polling loop may watch it. After completion, fetch one structured summary containing run and jobs metadata. Fetch failed logs only when the run fails. A deterministic failure blocks with diagnostics; a transient infrastructure failure may be dispatched once more at the same implementation SHA, with one 60-second watcher for that new run and no overlapping watcher. Treat HTTP 403/429 as a hard stop until GitHub's reported reset/retry time.

    Replace the blocked top-level receipt in `238-EVIDENCE.json` only after the exact-SHA workflow and the direct `Generated auth runtime proof` job both conclude `success`. Preserve the two prior failed attempt identifiers and blocker codes in a `previous_attempts` array, then record `status: "passed"`, schema version, immutable repair `head_sha`, workflow database ID/name/path, run ID/URL/event/ref/timestamps/conclusion, job database ID/stable ID/name/URL/timestamps/conclusion, the represented `GITHUB_WORKSPACE="$PWD" scripts/ci/generated-auth-runtime-proof.sh --all` command, and the rate-limit preflight metadata. Store no raw logs, credentials, environment values, mailbox URLs, OAuth codes, or tokens. Commit the receipt and any Phase 238 plan/summary updates, leave the worktree clean, and prove that the committed diff from `head_sha` through `HEAD` contains only `.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json` and Phase 238 `238-NN-PLAN.md` / `238-NN-SUMMARY.md` artifacts. If that range contains a harness, workflow, browser, library, or any other non-allowlisted path, freeze the resulting implementation SHA and obtain a new green run whose `headSha` exactly equals it before replacing the receipt.
  </action>
  <acceptance_criteria>
    - The source-contract ExUnit test and `bash -n` pass with `assert_locked_contract` targeting `${CI_DIR}/generated-auth-runtime-proof.sh` after the disposable-host directory change.
    - One explicitly dispatched `Generated auth runtime proof` workflow at the immutable repair SHA concludes `success`, and its job named `Generated auth runtime proof` with stable ID `generated_auth_runtime_proof` concludes `success` after executing both allowlisted specs.
    - `238-EVIDENCE.json` has top-level `status: "passed"`, retains both previous red attempts, records the green workflow/job metadata, and one live `gh run view` result has the same implementation SHA, successful workflow conclusion, and exactly the recorded job database ID with name `Generated auth runtime proof` and conclusion `success`.
    - The final verification runs from a clean worktree, and every committed path from the receipt's `head_sha` through `HEAD` is either `238-EVIDENCE.json` or a Phase 238 `238-NN-PLAN.md` / `238-NN-SUMMARY.md` artifact; any other path invalidates the receipt and requires a fresh exact-SHA green run.
    - AUTH-01, AUTH-02, and AUTH-03 are not credited from local source checks, human UAT, a different SHA, or any skipped, cancelled, neutral, timed-out, ambiguous, or failed run/job.
    - A-GAP-01 through A-GAP-03 remain explicitly unresolved and are not described as covered by the receipt.
  </acceptance_criteria>
  <verify>
    <automated>evidence_path='.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json' &amp;&amp; bash -n scripts/ci/generated-auth-runtime-proof.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs &amp;&amp; jq -e '.schema_version == 1 and .status == "passed" and (.head_sha | test("^[0-9a-f]{40}$")) and (.previous_attempts | map(.run_id) | index(31019501361) != null) and (.previous_attempts | map(.run_id) | index(31021611104) != null) and .workflow.name == "Generated auth runtime proof" and .workflow.path == ".github/workflows/generated-auth-runtime-proof.yml" and .run.event == "workflow_dispatch" and .run.conclusion == "success" and (.job.database_id | type == "number") and .job.id == "generated_auth_runtime_proof" and .job.name == "Generated auth runtime proof" and .job.conclusion == "success" and .harness_command == "GITHUB_WORKSPACE=\"$PWD\" scripts/ci/generated-auth-runtime-proof.sh --all"' "$evidence_path" &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)" &amp;&amp; tested_sha="$(jq -r '.head_sha' "$evidence_path")" &amp;&amp; git cat-file -e "${tested_sha}^{commit}" &amp;&amp; git merge-base --is-ancestor "$tested_sha" HEAD &amp;&amp; test -z "$(git diff --name-only "${tested_sha}..HEAD" -- | awk '$0 == ".planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json" { next } $0 ~ /^\.planning\/phases\/238-generated-auth-runtime-proof\/238-[0-9][0-9]-(PLAN|SUMMARY)\.md$/ { next } { print }')" &amp;&amp; gh run view "$(jq -r '.run.id' "$evidence_path")" --repo szTheory/sigra --json headSha,conclusion,jobs,url | jq -e --arg sha "$tested_sha" --argjson job_id "$(jq -r '.job.database_id' "$evidence_path")" '.headSha == $sha and .conclusion == "success" and any(.jobs[]; .databaseId == $job_id and .name == "Generated auth runtime proof" and .conclusion == "success")'</automated>
  </verify>
  <done>The retained harness survives its post-scaffold directory change, and one durable receipt proves the complete credential-free generated B2C email, Google collision, and per-state accessibility lane green on the exact repair implementation SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained repository harness → disposable generated host | The process changes directories, so relative invocation paths can accidentally resolve inside untrusted/generated workspace state. |
| GitHub API/workflow → evidence artifact | Remote metadata must identify the intended immutable repair commit, workflow, and direct job without ambiguity. |
| evidence artifact → requirement completion | A stale, red, skipped, wrong-SHA, or post-run-mutated implementation must not be represented as passing runtime proof. |

## STRIDE Threat Register — OWASP ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-21 | Tampering | `assert_locked_contract` retained-script lookup | high | mitigate | Resolve the retained script from immutable `CI_DIR` after `cd`, and pin that exact target in the Phase 238 ExUnit contract before dispatch. |
| T-238-22 | Spoofing | workflow/run/job correlation | high | mitigate | Match workflow identity, exact repair SHA, ref, event, dispatch window, unique run, stable job ID, display name, and success conclusions; re-query the recorded run during verification. |
| T-238-23 | Repudiation | exact-SHA completion receipt | high | mitigate | Preserve prior failed attempts, store typed run/job URLs and timestamps, and allow only evidence/planning diffs after the tested repair commit. |
| T-238-24 | Information Disclosure | CI logs and evidence JSON | medium | mitigate | Keep credential-free loopback inputs; store metadata only and fetch failed logs solely for diagnosis without embedding logs, bearer links, codes, tokens, or environment values. |
| T-238-25 | Denial of Service | GitHub API/watch lifecycle | low | accept | The bounded evidence operation uses one preflight, one non-overlapping watcher per run at 60-second intervals, one structured summary, and a hard stop on low budget or 403/429. |
| T-238-SC | Tampering | package supply chain | low | accept | No dependency or action version changes are planned; the existing locked workflow installs the already-audited repository dependencies. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Deterministic browser and accessibility proof for generated B2C email and Google journeys without credentials | 238-06 | COVERED | Corrected retained harness plus exact-SHA green runtime receipt. |
| REQ | AUTH-01 | Complete generated-host email authentication journey | 238-06 | COVERED | Direct `--all` job must execute successfully; source-only evidence is rejected. |
| REQ | AUTH-02 | Local Google start/callback/account-link collision | 238-06 | COVERED | Same green job includes focused and full-suite loopback provider probes. |
| REQ | AUTH-03 | Per-rendered-state Axe, label/control, and duplicate IDs | 238-06 | COVERED | Same green job executes committed named-state accessibility gates. |
| RESEARCH | — | Phase 237 fresh-host lifecycle, Assent loopback seam, and no external credentials | 238-06 | COVERED | Repair changes path resolution only and reruns the existing canonical lane. |
| RESEARCH | A1 | Observable bounded mailbox polling | 238-06 | COVERED | Existing ExUnit contract remains green and direct runtime job exercises `expect.poll`. |
| RESEARCH | A2 | Generated host configures local Assent `base_url` | 238-06 | COVERED | Direct job exercises the already-built loopback double; no real-Google fallback is allowed. |
| RESEARCH | — | Missing local PostgreSQL/Chromium requires exact-commit CI | 238-06 | COVERED | Green correlated workflow/job receipt is mandatory. |
| CONTEXT | D-01 | Fresh canonical generated B2C host boundary | 238-06 | COVERED | The corrected retained harness runs the existing fresh-host lifecycle. |
| CONTEXT | D-02 | One serial rendered email journey and deterministic mailbox | 238-06 | COVERED | `--all` includes the existing serial journey; no direct context calls substitute. |
| CONTEXT | D-03 | Credential-free Google double and collision semantics | 238-06 | COVERED | Exact job includes both Google probes without changing the costly seam. |
| CONTEXT | D-04 | Per-state Axe/DOM checks, stable selectors/readiness, no browser sleeps | 238-06 | COVERED | Regression contract stays green and runtime job executes the named checks. |
| CONTEXT | D-05 | Complete AUTH-01 journey replaces login-only coverage | 238-06 | COVERED | Exact job runs the complete generated-auth spec. |
| CONTEXT | D-06 | Auth-surface Axe coverage folded into AUTH-03 | 238-06 | COVERED | Exact job executes scoped accessibility assertions. |

## Artifacts this phase produces

- `scripts/ci/generated-auth-runtime-proof.sh` — retained harness whose post-`cd` source assertion resolves through `CI_DIR`.
- `test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs` — regression lock for that absolute supporting-path contract and the unchanged deterministic browser proof surface.
- `.planning/phases/238-generated-auth-runtime-proof/238-EVIDENCE.json` — machine-readable green workflow/direct-job receipt for the exact repair implementation SHA, retaining prior red-attempt provenance.
- `.planning/phases/238-generated-auth-runtime-proof/238-06-SUMMARY.md` — execution summary created after the plan completes.

<verification>
1. `bash -n scripts/ci/generated-auth-runtime-proof.sh` passes, and `MIX_ENV=test mix test test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs` pins the `CI_DIR` lookup plus D-01 through D-06 deterministic test contracts.
2. The implementation repair is committed before dispatch; its immutable SHA is the workflow `headSha`, and later implementation changes force a new run.
3. One watcher per run uses `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status` after a REST rate-limit preflight; one structured summary is fetched after completion, and failed logs are fetched only for a failed run.
4. The direct `Generated auth runtime proof` job and its workflow both conclude `success` after `GITHUB_WORKSPACE="$PWD" scripts/ci/generated-auth-runtime-proof.sh --all` executes both dedicated-project specs.
5. `238-EVIDENCE.json` passes the local `jq` predicate and one live `gh run view` exact-SHA/job-success predicate. No local check, human UAT, wrong SHA, or non-success conclusion substitutes.
</verification>

<success_criteria>
- The self-reference blocker from runs `31019501361` and `31021611104` is eliminated using the harness's own already-computed CI directory.
- AUTH-01, AUTH-02, and AUTH-03 each have machine-verifiable runtime evidence from one green exact-implementation-SHA workflow and direct job.
- D-01 through D-06 remain intact: fresh generated host, one serial email journey, loopback Google collision, per-state accessibility, no credentials/network, stable selectors/readiness, and no browser sleeps.
- High-severity T-238-21 through T-238-23 mitigations pass; absent, ambiguous, rate-limited, or red evidence leaves the phase blocked.
- A-GAP-01 through A-GAP-03 remain explicitly unresolved and unclaimed pending a source artifact that defines those fallback probes.
</success_criteria>

<output>
Create `.planning/phases/238-generated-auth-runtime-proof/238-06-SUMMARY.md` when done.
</output>
