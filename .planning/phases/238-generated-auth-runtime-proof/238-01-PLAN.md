---
phase: 238-generated-auth-runtime-proof
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/ci/generated-auth-runtime-proof.sh
  - test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
autonomous: true
requirements:
  - AUTH-02
must_haves:
  truths:
    - "AUTH-02 / D-01 / D-03: a fresh canonical B2C host reaches its real generated /auth/google request and callback routes through an entirely local Assent 0.3.1 OIDC double."
    - "The provider double proves the locked Google configuration: local base_url, dummy credentials, id_token_signed_response_alg HS256, and code_verifier true, while preserving signed Sigra state and PKCE data without nonce, userinfo, or JWKS."
    - "A password account with the double's normalized email reaches the generated link_confirmation_required collision UI; no direct callback invocation, real credential, or external network substitutes."
  artifacts:
    - path: scripts/ci/generated-auth-runtime-proof.sh
      provides: "Fresh-host lifecycle and ephemeral local OIDC-double installation"
      contains: "--probe-oauth"
    - path: test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
      provides: "Focused browser proof of the generated Google request/callback/collision path"
      contains: "/auth/google"
  key_links:
    - from: scripts/ci/generated-auth-runtime-proof.sh
      to: scripts/ci/passkeys-opt-out-smoke.sh
      via: "copies Phase 237's exact scaffold, installer, cloak_ecto, Google-generator, migration, free-port, cleanup, and boot lifecycle"
      pattern: "--no-admin --no-organizations --no-passkeys"
    - from: test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts
      to: priv/templates/sigra.gen.oauth/oauth_controller.ex
      via: "browser starts at /auth/google and follows the generated controller redirect/callback"
      pattern: "auth/google"
    - from: scripts/ci/generated-auth-runtime-proof.sh
      to: Assent.Strategy.Google
      via: "ephemeral generated-host Google config supplies local base_url and dummy client values"
      pattern: "base_url"
---

<objective>
Retain executable end-to-end proof of the locked Assent 0.3.1 local-`base_url` OIDC-double contract through the generated Google routes, implementing D-01 and D-03 for AUTH-02.

Purpose: The complete email and accessibility suite is irrelevant if the credential-free Google boundary cannot preserve the real generated controller/state/PKCE path.
Output: A reusable fresh-host harness and focused OAuth browser probe that later plans expand without changing the proven architecture.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md
@.planning/phases/238-generated-auth-runtime-proof/238-RESEARCH.md
@.planning/phases/238-generated-auth-runtime-proof/238-PATTERNS.md
@.planning/phases/238-generated-auth-runtime-proof/238-VALIDATION.md
@.planning/phases/237-canonical-b2c-generator-contract/237-CONTEXT.md
@.planning/phases/237-canonical-b2c-generator-contract/237-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the local Assent Google OIDC seam through generated routes</name>
  <precondition>Phase 237's `scripts/ci/passkeys-opt-out-smoke.sh` contains the canonical B2C install-then-Google lifecycle; a full local run additionally requires reachable PostgreSQL and a Playwright Chromium binary, otherwise this task remains unproven until the same command runs in CI.</precondition>
  <reversibility rating="costly">Per D-03, changing this seam later would require coordinated changes to generated-host callback evidence and the provider-double contract.</reversibility>
  <files>scripts/ci/generated-auth-runtime-proof.sh, test/example/priv/playwright/tests/generated-auth-oauth-probe.spec.ts</files>
  <read_first>
    - `.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md` for D-01, D-03, the exact collision outcome, and excluded scopes.
    - `.planning/phases/238-generated-auth-runtime-proof/238-RESEARCH.md` resolved Open Question 1 for the exact Assent 0.3.1 local-Google contract and security threats.
    - `.planning/phases/238-generated-auth-runtime-proof/238-PATTERNS.md` for the shell lifecycle and generated controller analogs.
    - `scripts/ci/passkeys-opt-out-smoke.sh` for the authoritative Phase 237 scaffold/install/generate/migrate/boot and guarded-delete implementation.
    - `priv/templates/sigra.gen.oauth/oauth_controller.ex` for the request session keys, callback inputs, collision flash, and login redirect.
    - `lib/sigra/oauth/strategies/google.ex`, `deps/assent/lib/assent/strategies/google.ex`, `deps/assent/lib/assent/strategies/oidc.ex`, and `deps/assent/lib/assent/strategies/oidc/base.ex` for the exact `base_url`, discovery, token, PKCE, HS256 ID-token, and configuration-merge contracts.
    - `test/sigra/oauth/callback_test.exs` for the existing-email `:link_confirmation_required` domain predicate.
    - `test/example/priv/playwright/playwright.config.ts` for serial, zero-retry browser defaults.
  </read_first>
  <behavior>
    - Test 1: starting in the browser at the generated `/auth/google` route redirects to the local authorization endpoint, not a Google hostname.
    - Test 2: local discovery advertises only the loopback issuer, authorization, and token endpoints; authorization relays the exact state and requires a PKCE challenge; token receives the matching verifier and returns the dummy-secret-signed HS256 ID token used by the generated callback.
    - Test 3: when the deterministic provider email already belongs to a password account, the browser lands on generated login with the account-link collision message and no identity is silently attached.
    - Test 4: the harness fails if any provider request leaves loopback, if a real Google credential variable is required, if callback is entered without the generated start route, or if the double grows nonce, userinfo, or JWKS dependencies outside the locked contract.
  </behavior>
  <action>
    Start with a failing focused Playwright probe, then create `scripts/ci/generated-auth-runtime-proof.sh` as a retained acceptance harness. Copy the Phase 237 lifecycle rather than calling its three-leg script: invocation-owned `mktemp -d`, fixed-prefix guarded cleanup, free ports, assets-enabled `mix phx.new`, local Sigra path dependency, exact `mix sigra.install Accounts User users --no-admin --no-organizations --no-passkeys --yes`, direct `cloak_ecto`, `mix sigra.gen.oauth --providers google`, warning-free compile, database creation/migration, bounded boot, and server-log diagnostics. Add `--probe-oauth` as the focused entry point and make later spec selection an explicit allowlisted harness option so subsequent plans can reuse the same generated host.

    Patch only the temporary generated application with a narrow dev/test OIDC controller/router and generated-host configuration. Set provider `:google` to loopback `base_url`, dummy `client_id`/`client_secret`, generated callback `redirect_uri`, `id_token_signed_response_alg: "HS256"`, and `code_verifier: true`. Serve discovery, authorization, and token endpoints entirely on loopback: discovery supplies local issuer/authorization/token endpoints and `client_secret_post`; authorization preserves signed state and verifies a PKCE challenge; token verifies the returned PKCE verifier and supplies a short-lived HS256 ID token whose issuer, audience, subject, and collision-email claims match the configured dummy values. Do not add `nonce`, `userinfo`, `jwks_uri`, or an alternate strategy: the locked Google wrapper validates the ID token and the generated controller persists only the PKCE verifier. Enforce loopback endpoints in the harness before boot and prevent inherited Google credential values from becoming proof inputs.

    In the browser probe, create the password account through the generated registration form, use the real generated logout form if authentication must be cleared, then begin at `/auth/google`. Assert the authorization redirect targets the local double, preserve the returned state, follow the generated callback, and assert the exact visible existing-account collision outcome at the generated login route. Use roles/labels, URL/flash predicates, and LiveView's connected-root signal only. Keep admin, organizations, passkeys, real email providers, Crosswake, and physical-device behavior absent.
  </action>
  <verify>
    <automated>bash -n scripts/ci/generated-auth-runtime-proof.sh &amp;&amp; GITHUB_WORKSPACE="$PWD" scripts/ci/generated-auth-runtime-proof.sh --probe-oauth</automated>
  </verify>
  <done>The fresh generated B2C host completes the real Google start/callback/collision path through Assent 0.3.1 using only a loopback `base_url` double, and the focused command fails closed on state, PKCE, HS256 ID-token, endpoint, or collision regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → generated OAuth controller | Untrusted route parameters enter generated request/callback state handling. |
| generated host → local OIDC double | Authorization, token, discovery, and claims cross an HTTP boundary that must remain loopback-only. |
| harness → filesystem/process/PostgreSQL | Disposable source, database, and server processes require bounded cleanup and collision-free ports. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-238-01 | Tampering | OAuth state/PKCE/HS256 ID token | high | mitigate | Always enter through generated `/auth/google`; relay exact state, require the generated callback session and PKCE verifier, validate the dummy-secret-signed ID token, and make altered/missing transaction data fail the probe. |
| T-238-02 | Information Disclosure | provider configuration and logs | high | mitigate | Clear inherited Google credentials, use dummy values, constrain all endpoints to loopback, and never print token/client-secret bodies. |
| T-238-03 | Elevation of Privilege | existing-email collision | high | mitigate | Precreate the matching password account and assert the generated collision message plus absence of silent identity attachment. |
| T-238-04 | Denial of Service | temporary host/process lifecycle | medium | mitigate | Preserve invocation-owned temp roots, validated cleanup, free ports, bounded readiness, and failure logs. |
| T-238-SC | Tampering | package supply chain | low | accept | No install or upgrade is introduced; reuse repository-locked Assent, Playwright, and Axe dependencies. |
</threat_model>

<verification>
Run the exact focused harness command. A missing local PostgreSQL/browser prerequisite is blocked evidence, never a pass; the same command must later succeed in the exact-commit CI lane.
</verification>

<success_criteria>
- The locked Assent 0.3.1 local `base_url` + dummy credentials + HS256 ID token + `code_verifier: true` seam is executable before broader work begins.
- D-01 and D-03 are implemented through the real generated host and routes.
- No real credentials, external provider network, callback shortcut, admin/org/passkey surface, or human-UAT claim is introduced.
</success_criteria>

<output>
Create `.planning/phases/238-generated-auth-runtime-proof/238-01-SUMMARY.md` when done.
</output>
