---
phase: 133-suite-narrative-ecosystem-diagram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/introduction/suite-integration.md
  - README.md
  - mix.exs
  - guides/recipes/companion-libs/threadline.md
autonomous: true
requirements: [NX-01]

must_haves:
  truths:
    - "A reader landing on `README.md` can follow a single link to `guides/introduction/suite-integration.md` and see, on one page: the ASCII ecosystem diagram with Unicode box glyphs, the fan-out matrix (6 audit-action rows × 5 sink columns), the 'Sigra works fully standalone' banner with the verbatim D-16 wording, an ARIA-flat one-sentence diagram caption, and a 'Diminishing Returns Wall' subsection that quotes `MILESTONE-ARC.md:215-220` verbatim."
    - "The narrative cross-links to all six companion-lib recipes (Threadline, Mailglass, Accrue, Lockspire, Relyra, Rulestead) so ExDoc renders the suite as a coherent unit. `mix docs --warnings-as-errors` exits 0 — the four not-yet-shipped recipe filenames (Accrue/Lockspire/Relyra/Rulestead) are listed in `skip_undefined_reference_warnings_on:` under a Phase 133 comment header so the cross-link warnings stay green in the 133→134 window."
    - "The narrative carries none of the four banned marketing phrases ('seamlessly', 'just works', 'production-ready out of the box', 'the recommended way') and the D-19 pre-commit grep gate is non-interactive automation that the verification task re-runs against the merged tree."
  artifacts:
    - path: "guides/introduction/suite-integration.md"
      provides: "Canonical narrative entry point for the szTheory companion-library suite — banner + 'What this is' prose + ASCII ecosystem diagram + ARIA caption + role-table (7 rows: Sigra core + 6 companions) + fan-out matrix (6 audit actions × 5 sinks) + Diminishing Returns Wall subsection (verbatim quote of MILESTONE-ARC.md:215-220) + 'Where to next' cross-links + closing one-sentence banner echo."
      contains:
        - "Sigra works fully standalone"
        - "Diminishing Returns Wall"
        - "Last validated: 2026-05-27"
        - "Opinionated Authorization"
        - "Billing & Subscription Integration"
        - "Frontend / UI Component Libraries"
        - "../recipes/companion-libs/threadline.html"
        - "../recipes/companion-libs/mailglass.html"
        - "../recipes/companion-libs/accrue.html"
        - "../recipes/companion-libs/lockspire.html"
        - "../recipes/companion-libs/relyra.html"
        - "../recipes/companion-libs/rulestead.html"
    - path: "README.md"
      provides: "Single-link discoverability — one new row in the existing `## Topic map → guides` table at position 2 (immediately below `First happy path`)."
      contains: "introduction/suite-integration.md"
    - path: "mix.exs"
      provides: "ExDoc registration — one new `extras:` entry between `upgrading-to-v1.1.md` (line 198) and `flows/registration.md` (line 199); four new `skip_undefined_reference_warnings_on:` entries (Accrue/Lockspire/Relyra/Rulestead) under a Phase 133 comment header; ZERO `groups_for_extras:` edits (the existing `Introduction:` regex at line 223 absorbs the new file)."
      contains:
        - "guides/introduction/suite-integration.md"
        - "guides/recipes/companion-libs/accrue.md"
        - "guides/recipes/companion-libs/lockspire.md"
        - "guides/recipes/companion-libs/relyra.md"
        - "guides/recipes/companion-libs/rulestead.md"
    - path: "guides/recipes/companion-libs/threadline.md"
      provides: "Reciprocal 'See also' pointer up to `../introduction/suite-integration.html`, matching the pointer `mailglass.md` already carries (deferred-item default YES per CONTEXT.md `<deferred>` section)."
      contains: "../introduction/suite-integration.html"
  key_links:
    - from: "README.md (Topic map row 2)"
      to: "guides/introduction/suite-integration.md"
      via: "Markdown link in topic-map table cell"
      pattern: "introduction/suite-integration\\.md"
    - from: "guides/introduction/suite-integration.md (role-table + Where to next)"
      to: "guides/recipes/companion-libs/{threadline,mailglass,accrue,lockspire,relyra,rulestead}.md"
      via: "Relative `.html` cross-links from the narrative into all six recipe pages"
      pattern: "\\.\\./recipes/companion-libs/(threadline|mailglass|accrue|lockspire|relyra|rulestead)\\.html"
    - from: "guides/introduction/suite-integration.md (Diminishing Returns Wall blockquote)"
      to: ".planning/MILESTONE-ARC.md:215-220"
      via: "Verbatim 3-bullet quote (NO cross-link — planning tree stays maintainer-private per D-14)"
      pattern: "Opinionated Authorization.*RBAC.*Zanzibar"
    - from: "mix.exs (extras: list)"
      to: "guides/introduction/suite-integration.md"
      via: "New `extras:` entry between `upgrading-to-v1.1.md` and `flows/registration.md`"
      pattern: "guides/introduction/suite-integration\\.md"
    - from: "mix.exs (skip_undefined_reference_warnings_on: list)"
      to: "guides/recipes/companion-libs/{accrue,lockspire,relyra,rulestead}.md"
      via: "Four new entries under a Phase 133 comment header keeping `mix docs --warnings-as-errors` green in the 133→134 window"
      pattern: "guides/recipes/companion-libs/(accrue|lockspire|relyra|rulestead)\\.md"
---

<objective>
Ship the v1.29 SUITE-INTEGRATION canonical narrative entry point: publish `guides/introduction/suite-integration.md` as the single page that orients adopters to the szTheory companion-library suite, plus the surgical `README.md` Topic-map pointer that makes it discoverable from the repo root, plus the three-block `mix.exs` edit that registers it under the existing `Introduction` ExDoc group and pre-emptively silences cross-link warnings for the four not-yet-shipped companion-lib recipes (Accrue / Lockspire / Relyra / Rulestead) that Phase 134 lands.

Purpose: Without this narrative the six companion-lib recipes (Threadline, Mailglass + the four Phase 134 recipes) read as six unrelated islands. The narrative is the load-bearing orientation page: it ships the ASCII ecosystem diagram + role-table + fan-out matrix + Diminishing Returns Wall quote that make the suite legible as a coherent picture. Without the README Topic-map row, the narrative is undiscoverable from project root. Without the `mix.exs` edit, `mix docs --warnings-as-errors` fails closed on the four forward cross-links.

Output: One new guide file (180–260 lines per D-11) + one new README Topic-map row + a three-block `mix.exs` edit + a one-line `threadline.md` reciprocal-pointer addition + a passing `mix docs --warnings-as-errors` gate + a passing banned-phrase grep gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONE-ARC.md
@.planning/phases/133-suite-narrative-ecosystem-diagram/133-CONTEXT.md

# Phase 132 analog plan — mirror frontmatter shape, task XML shape, verification gate placement.
@.planning/phases/132-threadline-recipe-mailglass-cross-link-recipe/132-01-PLAN.md

# Phase 132 recipes — banner shape (line 7) + validated-against HTML comment form + section ordering
# the narrative must remain stylistically consistent with. Task 1 pins voice against these.
@guides/recipes/companion-libs/threadline.md
@guides/recipes/companion-libs/mailglass.md

# Neighbor introduction-group pages — voice register + length envelope + cross-link patterns Task 1 mirrors.
@guides/introduction/getting-started.md
@guides/introduction/intermediate-production-path.md
@guides/introduction/installation.md

# Recipe template — voice / section-ordering convention Task 1 mirrors at a higher tier.
@guides/recipes/companion-oauth-provider.md

# Canonical audit-action list — Task 1 fan-out matrix rows pin against this.
@guides/flows/audit-logging.md

# Diagram prototype Task 1 tightens (NOT a from-scratch redraw — D-01).
@.planning/research/ARCHITECTURE.md

# Edit targets — Task 2 (README) and Task 3 (mix.exs) modify exact line regions.
@README.md
@mix.exs

<diagram_prototype>
<!-- D-01 ships a tightened version of this prototype. From .planning/research/ARCHITECTURE.md:204-222. -->
<!-- "Tighten" per CONTEXT.md <specifics>: heavier glyphs for the central Phoenix-app box vs satellites; -->
<!-- one-word primary-role labels on each satellite; the optional [:sigra, :audit, :log] telemetry tap -->
<!-- shown as a labeled edge, not a separate box. The prototype already uses Unicode box glyphs (┌─┐│└─┘), -->
<!-- not plain `+--+` — preserve that. -->

```
                ┌─────────────────────────────────┐
                │       Sigra (auth core)         │
                │  sessions · MFA · passkeys ·    │
                │  audit · webhooks · SSO         │
                └────────┬────────────────────────┘
                         │
    ┌────────────────────┼───────────────────────────┐
    │                    │                           │
┌───▼────┐          ┌────▼─────┐               ┌─────▼─────┐
│ Email  │          │  Audit   │               │  OAuth    │
│ outbox │          │  sink    │               │  AS       │
│Mailglass│         │Threadline│               │ Lockspire │
└────────┘          └──────────┘               └───────────┘

Other adopter-side companions (recipe-only):
Accrue (billing webhook consumer) · Relyra (relationships)
Rulestead (rule engine)
```
</diagram_prototype>

<banner_text_top>
<!-- D-15 + D-16 final form. Task 1 quotes this verbatim as the "Standalone banner" section directly under H1 + last-validated line. -->

> **Sigra works fully standalone.** Threadline, Mailglass, Accrue, Lockspire, Relyra, and Rulestead are all optional integrations; Sigra ships without any of them, and removing the integration sections below returns Sigra to standalone operation with no further changes.
</banner_text_top>

<end_echo_text>
<!-- D-15 final form. Task 1 places this as the closing one-sentence bookend on the very last line of the page. -->

> **Reminder:** every integration above is opt-in; Sigra runs fully standalone without any of them.
</end_echo_text>

<drw_quote>
<!-- D-13 + D-14: quote MILESTONE-ARC.md:215-220 byte-for-byte as a `> blockquote` 3-bullet list. -->
<!-- NO link into .planning/ — the planning tree stays maintainer-private. Task 1 inlines these three bullets verbatim. -->

> - **Opinionated Authorization (RBAC / Zanzibar):** Sigra provides the identity (`user_id`, `organization_id`); the host app must own the *policy* (what the user can do). We provide seams, but we will not build a built-in roles engine.
> - **Billing & Subscription Integration:** Direct mappings to Stripe or other billing providers are domain logic. Sigra provides webhook egress for syncing identity state, not billing logic.
> - **Frontend / UI Component Libraries:** Sigra generates functional HTML (`mix sigra.install`); it will not ship a heavy CSS framework or React component library. Aesthetics belong to the adopter.
</drw_quote>

<fan_out_matrix_legend>
<!-- D-03 cell legend. Task 1 places this directly under the fan-out matrix table. -->

```
✓      = Sigra fans this event into the named sink by default.
(host) = Host code may opt into this sink (e.g. Mailglass receives email side-effects).
—      = This sink does not receive this event.
n/a    = This event is host-emitted; Sigra does not own it.
```
</fan_out_matrix_legend>

<fan_out_matrix_rows>
<!-- D-03 row labels — curated representative subset of audit `action` strings (NOT every action). -->
<!-- Verify rows match the canonical action strings published at guides/flows/audit-logging.md:36-51. -->

- `auth.login.success`
- `auth.login.failure`
- `auth.password_reset.success`
- `mfa.challenge.success`
- `account.deletion_execute`
- `billing.subscription.upgraded`   ← custom host-emitted row (demonstrates matrix generalizes; `n/a` in Sigra audit DB column)
</fan_out_matrix_rows>

<fan_out_matrix_columns>
<!-- D-03 column headers — five sinks. -->

1. `Sigra audit DB row`
2. `[:sigra, :audit, :log]` telemetry
3. Sigra webhooks
4. Threadline forwarder
5. Mailglass
</fan_out_matrix_columns>

<readme_topic_map_row>
<!-- D-09 exact form. Task 2 inserts this as the SECOND row of the Topic map table, between `First happy path` (current line 144) and `Upgrade notes` (current line 145). -->

| Companion library suite | [introduction/suite-integration.md](guides/introduction/suite-integration.md) |
</readme_topic_map_row>

<mix_exs_extras_insertion>
<!-- D-05 exact position: append after `"guides/introduction/upgrading-to-v1.1.md",` (mix.exs:198), before `"guides/flows/registration.md",` (mix.exs:199). Match the surrounding two-space indent and trailing-comma style. -->

```elixir
"guides/introduction/suite-integration.md",
```
</mix_exs_extras_insertion>

<mix_exs_skip_warnings_insertion>
<!-- D-06 exact entries: append to the block at mix.exs:160-174 under a Phase 133 comment header. -->
<!-- The block already carries Phase 131 entries (165-169) and Phase 132 entries (172-173); D-06 adds four more. -->
<!-- Phase 134's executor removes these four entries in the same commit that lands the four recipes. -->

```elixir
# Phase 133: companion-lib recipes pending Phase 134 — remove these four
# entries when accrue/lockspire/relyra/rulestead recipes land.
"guides/recipes/companion-libs/accrue.md",
"guides/recipes/companion-libs/lockspire.md",
"guides/recipes/companion-libs/relyra.md",
"guides/recipes/companion-libs/rulestead.md"
```

The final entry in the list (Rulestead) must NOT carry a trailing comma — it is the last element of the keyword-list value before the closing `]`. The current last entry (`"guides/recipes/companion-libs/mailglass.md"` at line 173) is trailing-comma-less; preserve that pattern after the Phase 133 additions move it into the middle of the list.
</mix_exs_skip_warnings_insertion>

<banned_phrases>
<!-- D-19 + REQUIREMENTS.md:65-73. Tasks 1, 4, and 5 all gate on absence of these in `suite-integration.md`. -->

- "seamlessly"
- "just works"
- "production-ready out of the box"
- "the recommended way"
</banned_phrases>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `guides/introduction/suite-integration.md` end-to-end</name>
  <files>guides/introduction/suite-integration.md</files>

  <read_first>
    - `guides/recipes/companion-libs/threadline.md` — banner shape (line 7), validated-against HTML-comment form (lines 1-2), section-ordering convention, voice register the narrative must remain stylistically consistent with
    - `guides/recipes/companion-libs/mailglass.md` — identical banner shape (line 7); also the "See also → suite-integration.html" forward pointer at lines 129-130 the narrative implicitly closes the loop on
    - `guides/introduction/getting-started.md` — neighbor introduction-group page; voice register + length envelope (318 lines) — Task 1 lands at 180–260 lines per D-11
    - `guides/introduction/intermediate-production-path.md` — closest precedent for cross-linking from `introduction/` into `recipes/`
    - `guides/introduction/installation.md` — table-of-prerequisites pattern
    - `guides/recipes/companion-oauth-provider.md` — voice register + section-ordering convention (lines 1-52)
    - `guides/flows/audit-logging.md` (lines 36-51) — canonical Sigra audit `action` strings the fan-out matrix rows pin against
    - `.planning/MILESTONE-ARC.md` (lines 213-220) — Diminishing Returns Wall canonical text Task 1 quotes verbatim per D-13 (the three bullets ARE in the `<drw_quote>` block above; re-read MILESTONE-ARC to byte-confirm before pasting)
    - `.planning/research/ARCHITECTURE.md` (lines 204-222) — ASCII diagram prototype Task 1 tightens (NOT a from-scratch redraw per D-01)
    - `.planning/phases/133-suite-narrative-ecosystem-diagram/133-CONTEXT.md` (full file — D-01 through D-21 + `<specifics>` block + `<deferred>` block)
    - `.planning/REQUIREMENTS.md` (line 40 — NX-01; lines 65-73 — banned marketing phrases)
    - `.planning/ROADMAP.md` (lines 100-110 — Phase 133 goal + success criteria; line 161 — 133‖134 parallelism note)
  </read_first>

  <action>
    Create `guides/introduction/suite-integration.md` as a new file under the existing `Introduction` ExDoc group. Open with two HTML comment lines (D-12 step 2 — HTML comments, NOT YAML `---`):

    Line 1: `<!-- validated_against: sigra v1.29 -->`
    Line 2: `<!-- last_validated: 2026-05-27 -->`

    Then an H1: `# Suite Integration`.

    Then a human-visible single-line statement directly under H1: `Last validated: 2026-05-27.`

    Then the ten sections in the EXACT order from D-12. The contracts for each section:

    **Section 1 — Standalone banner (D-15, D-16):** Quote the verbatim text from the `<banner_text_top>` block in this plan's context. One `>` blockquote line. No deviation from the wording.

    **Section 2 — "What this is" (D-12 step 4):** Two paragraphs of orientation. Paragraph 1 names the six companion libraries (Threadline, Mailglass, Accrue, Lockspire, Relyra, Rulestead) and the one-sentence summary of what each owns. Paragraph 2 frames the page's purpose: "this is the page you read before any individual recipe; the recipes show wiring, this page shows the picture." Discretion call (CONTEXT.md `<decisions>` final block — pick decisively): plain prose (NOT a TL;DR callout) — the diagram immediately below already serves the at-a-glance role.

    **Section 3 — ASCII ecosystem diagram + ARIA caption (D-01, D-02):** Fenced ```` ``` ```` code block. Ship a TIGHTENED version of the `<diagram_prototype>` from this plan's context. Tightening targets from CONTEXT.md `<specifics>`:
    - Heavier glyphs / double-line box for the central Phoenix-app box vs satellites (visually distinct).
    - One-word primary-role labels on each satellite (`Threadline → audit`, `Mailglass → email`, `Accrue → seats`, `Lockspire → OAuth-provider`, `Relyra → SAML`, `Rulestead → feature-flags`).
    - Show the optional `[:sigra, :audit, :log]` telemetry tap as a labeled edge from Sigra (e.g. `─[:sigra, :audit, :log]─►`), NOT a separate box.
    - Preserve Unicode box glyphs (`┌─┐│└─┘`). NOT plain `+--+`.
    - Show all six satellites (the prototype only shows three in the upper-row; the four "recipe-only" companions in the prototype's footer must be elevated into the diagram proper or the diagram fails to deliver on success criterion #1's "ecosystem diagram" promise — keep the layout legible at terminal width ~80 cols).

    Directly under the closing ```` ``` ```` of the diagram, place a one-sentence ARIA-flat caption italicized as plain prose (NOT inside the fence). Example (rephrase for register; the semantic content is fixed): *"Diagram: a host Phoenix app sits at the center; Sigra owns auth; Threadline, Mailglass, Accrue, Lockspire, Relyra, and Rulestead are six optional satellites the host wires in. The dashed edge labeled `[:sigra, :audit, :log]` shows the telemetry tap Threadline subscribes to."*

    **Section 4 — "Who owns what" role table (D-12 step 6):** 7 rows, in this order (mirrors REQUIREMENTS.md:31-36 + PROJECT.md:30-31 — Sigra core anchor row, then Threadline first because it's the only library-code companion, then Mailglass because it's the other Phase 132 recipe, then the four Phase 134 recipes alphabetically: Accrue → Lockspire → Relyra → Rulestead).

    Discretion call (CONTEXT.md `<decisions>` final block — pick decisively): 3-column table `Library | Owns | Recipe` (the banner already made the optional point; a 4th `Optional` column would be redundant and visually noisier).

    Each `Recipe` cell links via relative `.html` path: `[Threadline recipe](../recipes/companion-libs/threadline.html)`, etc. The Sigra-core row's `Recipe` cell points at `[Getting started](./getting-started.html)` (the closest neighbor anchor — there is no Sigra-core "recipe"). The "Owns" cells pin facts (planner voice, but the semantics are fixed):
    - Sigra core: auth identity, sessions, MFA, passkeys, audit DB row (source-of-truth), webhooks, SSO
    - Threadline: audit row projection / queryable timeline (idempotency by Sigra audit UUID + `occurred_at`)
    - Mailglass: transactional auth-email dispatch + `NoTrackingOnAuthStream` privacy guard
    - Accrue: seat-based billing webhook consumer (host-emitted `billing.*` events; not on Sigra's audit fan-out by default)
    - Lockspire: OAuth 2.0 / OIDC authorization-server (Sigra is the *identity* source; Lockspire is the *AS*)
    - Relyra: SAML 2.0 + enterprise SSO IdP federation
    - Rulestead: rule-engine / feature-flag evaluation surface

    **Section 5 — Fan-out matrix (D-03):** Markdown table with 6 rows (from `<fan_out_matrix_rows>` block in this plan's context) and 5 columns (from `<fan_out_matrix_columns>`). Cells use the `✓ / — / (host) / n/a` legend.

    Specifically:
    - `auth.login.success`: ✓ ✓ ✓ (host) — — — (matrix is row × column; Sigra audit DB ✓, telemetry ✓, webhooks `(host)`, Threadline ✓, Mailglass —)
    - `auth.login.failure`: ✓ ✓ ✓ (host) — — (same shape; Mailglass — because login-failure does not send email by default)
    - `auth.password_reset.success`: ✓ ✓ ✓ (host) ✓ (host) — (Mailglass `(host)` because Mailglass receives the *email side-effect*, not the audit event itself — this is the doctrinal "inoculate against the common misread" cell per D-03)
    - `mfa.challenge.success`: ✓ ✓ — — — (no webhook by default; no email)
    - `account.deletion_execute`: ✓ ✓ ✓ (host) ✓ — (deletion is forwarded; email is host-discretion)
    - `billing.subscription.upgraded`: n/a — — — — (custom host-emitted row; n/a in Sigra audit DB because Sigra does not own this event — the doctrinal "matrix generalizes" cell per D-03)

    Directly under the table, place the cell legend from the `<fan_out_matrix_legend>` block in this plan's context (fenced code block, exact contents).

    **Section 6 — "The Diminishing Returns Wall" subsection (D-13, D-14):** `## The Diminishing Returns Wall` heading. ONE prose lead-in sentence (planner discretion — semantic content fixed: "Sigra owns identity, not policy, not billing, not aesthetics — see the **Diminishing Returns Wall** below."). Then the `> blockquote` with THREE bullets quoted byte-for-byte from MILESTONE-ARC.md:215-220 — the `<drw_quote>` block in this plan's context contains the exact text; re-read MILESTONE-ARC before pasting to byte-confirm (D-14 flags this as cheap and Phase 136 PROOF-01 may grep-assert it byte-for-byte). Then ONE trailer sentence (planner discretion — semantic content fixed: "Each companion library above respects this boundary by owning the surface Sigra deliberately doesn't.").

    Do NOT link to `.planning/MILESTONE-ARC.md` from this section (D-14 explicit: the planning tree is maintainer-private). The quoted bullets become first-class adopter content; the source attribution is implicit.

    **Section 7 — "Where to next" (D-12 step 9):** Six bullets, each one a relative `.html` cross-link to a companion-lib recipe. Order: Threadline → Mailglass → Accrue → Lockspire → Relyra → Rulestead (mirrors role-table order). Each bullet: one-line framing of what the recipe ships (e.g. "**Threadline recipe** — wire audit row projection to a queryable timeline with idempotency by Sigra audit UUID."). The four not-yet-shipped recipe links (Accrue/Lockspire/Relyra/Rulestead) resolve once Phase 134 lands; Task 3's `skip_undefined_reference_warnings_on:` edits keep `mix docs --warnings-as-errors` green in the meantime.

    **Section 8 — Closing one-sentence banner echo (D-15):** The verbatim text from the `<end_echo_text>` block in this plan's context. ONE `>` blockquote line. NOT a second full banner — top is doctrinal anchor, bottom is cognitive bookend.

    Voice register (D-19): pragmatic, role-table-led, prerequisites-first — same register as `guides/recipes/companion-oauth-provider.md` and the Phase 132 recipes (`threadline.md`, `mailglass.md`). Declarative sentences. AVOID all four banned phrases listed in the `<banned_phrases>` block.

    Target length per D-11: 180–260 lines (longer than `mailglass.md` ~130 lines, shorter than `getting-started.md` ~318 lines). The verify gate allows ±20% slack (150–290 lines) to absorb register polish.

    Do NOT include any cross-link into `.planning/` (D-14). Do NOT add a "Roadmap" / "Release log" / "Coming in v1.29" section (CONTEXT.md `<deferred>` explicit reject — the narrative is timeless adopter orientation). Do NOT add a Mermaid variant of the diagram (D-01: ASCII-only).
  </action>

  <acceptance_criteria>
    - File `guides/introduction/suite-integration.md` exists.
    - Opens with `<!-- validated_against: sigra v1.29 -->` on line 1 and `<!-- last_validated: 2026-05-27 -->` on line 2 (HTML comments, NOT YAML `---`).
    - `# Suite Integration` H1 on line 3 (or within first 5 lines if blank lines intervene).
    - Contains the literal string `Last validated: 2026-05-27.` somewhere in the first 10 lines.
    - Contains the literal string `**Sigra works fully standalone.**` (top banner — D-16 verbatim).
    - Contains the literal string `Threadline, Mailglass, Accrue, Lockspire, Relyra, and Rulestead are all optional integrations` (D-16 verbatim continuation).
    - Contains the literal string `**Reminder:** every integration above is opt-in` (end-echo — D-15 verbatim).
    - Contains a fenced code block (` ``` ` … ` ``` `) with at least one Unicode box glyph (any of `┌ ┐ │ └ ┘ ─`).
    - Diagram references all six companion libraries by name inside the fence: Threadline, Mailglass, Accrue, Lockspire, Relyra, Rulestead.
    - Contains an italicized ARIA-flat one-sentence caption directly under the closing diagram fence (line begins with `_` or `*` and mentions "Diagram").
    - Contains an `## The Diminishing Returns Wall` (or `## Diminishing Returns Wall`) section heading.
    - The DRW section contains all THREE phrase anchors from MILESTONE-ARC.md:215-220: `Opinionated Authorization (RBAC / Zanzibar)`, `Billing & Subscription Integration`, `Frontend / UI Component Libraries`.
    - The DRW section contains the policy-clause anchor `the host app must own the *policy*` (verbatim from MILESTONE-ARC).
    - The DRW section contains the `mix sigra.install` anchor (verbatim from MILESTONE-ARC line 219).
    - Contains relative cross-links to all SIX companion-lib recipes: `../recipes/companion-libs/threadline.html`, `../recipes/companion-libs/mailglass.html`, `../recipes/companion-libs/accrue.html`, `../recipes/companion-libs/lockspire.html`, `../recipes/companion-libs/relyra.html`, `../recipes/companion-libs/rulestead.html`.
    - Contains a 7-row role table covering Sigra core + the six companions (verify: count of `Threadline`, `Mailglass`, `Accrue`, `Lockspire`, `Relyra`, `Rulestead`, and `Sigra` as table-row anchors — each appears at least once in a table context).
    - Contains a fan-out matrix table with all SIX audit action strings: `auth.login.success`, `auth.login.failure`, `auth.password_reset.success`, `mfa.challenge.success`, `account.deletion_execute`, `billing.subscription.upgraded`.
    - Contains the fan-out matrix cell legend: literal anchors `Sigra fans this event into the named sink by default`, `Host code may opt into this sink`, `This sink does not receive this event`, `This event is host-emitted`.
    - NEGATIVE — does NOT contain any of the four banned phrases (case-insensitive): `seamlessly`, `just works`, `production-ready out of the box`, `the recommended way`.
    - NEGATIVE — does NOT contain any link into `.planning/` (no `](.planning/` substring, no `](../../.planning/` substring) per D-14.
    - NEGATIVE — does NOT contain a YAML frontmatter delimiter (`! grep -qE "^---$" "$F"`).
    - NEGATIVE — does NOT contain a Mermaid code fence (`! grep -qE "^\`\`\`mermaid" "$F"`) per D-01.
    - Line count is between 150 and 290 (D-11 target 180–260 with ±20% slack absorbed in the verify gate).
  </acceptance_criteria>

  <verify>
    <automated>
      bash -lc '
      F=guides/introduction/suite-integration.md
      set -e
      test -f "$F"
      # Frontmatter (HTML-comment form, NOT YAML)
      head -2 "$F" | grep -qE "^<!-- validated_against: sigra v1\.29 -->$"
      head -2 "$F" | grep -qE "^<!-- last_validated: 2026-05-27 -->$"
      ! grep -qE "^---$" "$F"
      # H1 + visible last-validated line in first 10 lines
      head -10 "$F" | grep -qE "^# Suite Integration$"
      head -10 "$F" | grep -qE "Last validated: 2026-05-27"
      # Banner (D-16 verbatim)
      grep -qF "**Sigra works fully standalone.**" "$F"
      grep -qF "Threadline, Mailglass, Accrue, Lockspire, Relyra, and Rulestead are all optional integrations" "$F"
      # End-echo (D-15 verbatim)
      grep -qF "**Reminder:** every integration above is opt-in" "$F"
      # ASCII diagram presence — Unicode box glyph inside a fence (heuristic: any of ┌─┐│└┘ in the file)
      grep -qE "[┌┐│└┘─]" "$F"
      # All six companion library names present in the file
      grep -qF "Threadline" "$F"
      grep -qF "Mailglass" "$F"
      grep -qF "Accrue" "$F"
      grep -qF "Lockspire" "$F"
      grep -qF "Relyra" "$F"
      grep -qF "Rulestead" "$F"
      # ARIA caption (heuristic — italicized line mentioning "Diagram" with leading _ or *)
      grep -qE "^[_*].*[Dd]iagram" "$F"
      # DRW section anchors (D-13 verbatim — three bullets from MILESTONE-ARC.md:215-220)
      grep -qE "^## (The )?Diminishing Returns Wall" "$F"
      grep -qF "Opinionated Authorization (RBAC / Zanzibar)" "$F"
      grep -qF "Billing & Subscription Integration" "$F"
      grep -qF "Frontend / UI Component Libraries" "$F"
      grep -qF "the host app must own the *policy*" "$F"
      grep -qF "mix sigra.install" "$F"
      # Cross-links to all six recipes (relative .html form)
      grep -qF "../recipes/companion-libs/threadline.html" "$F"
      grep -qF "../recipes/companion-libs/mailglass.html" "$F"
      grep -qF "../recipes/companion-libs/accrue.html" "$F"
      grep -qF "../recipes/companion-libs/lockspire.html" "$F"
      grep -qF "../recipes/companion-libs/relyra.html" "$F"
      grep -qF "../recipes/companion-libs/rulestead.html" "$F"
      # Fan-out matrix — all six audit action rows present
      grep -qF "auth.login.success" "$F"
      grep -qF "auth.login.failure" "$F"
      grep -qF "auth.password_reset.success" "$F"
      grep -qF "mfa.challenge.success" "$F"
      grep -qF "account.deletion_execute" "$F"
      grep -qF "billing.subscription.upgraded" "$F"
      # Fan-out matrix cell legend anchors
      grep -qF "Sigra fans this event into the named sink by default" "$F"
      grep -qF "Host code may opt into this sink" "$F"
      grep -qF "This sink does not receive this event" "$F"
      grep -qF "This event is host-emitted" "$F"
      # NEGATIVE — banned marketing phrases (case-insensitive)
      ! grep -iqE "(seamlessly|just works|production-ready out of the box|the recommended way)" "$F"
      # NEGATIVE — no .planning/ cross-links (D-14)
      ! grep -qE "\]\(\.\.?/?\.?\.?/?\.planning/" "$F"
      # NEGATIVE — no Mermaid fence (D-01: ASCII-only)
      ! grep -qE "^\`\`\`mermaid" "$F"
      # Length target (D-11: 180-260; allow 150-290 slack)
      LINES=$(wc -l < "$F" | tr -d " ")
      test "$LINES" -ge 150 && test "$LINES" -le 290
      echo OK
      '
    </automated>
  </verify>

  <done>
    `guides/introduction/suite-integration.md` exists with the ten-section ordered contract from D-12: HTML-comment frontmatter (no YAML); H1 + `Last validated: 2026-05-27` line; verbatim D-16 top banner; "What this is" two-paragraph orientation; tightened ASCII ecosystem diagram + ARIA-flat italicized caption; 7-row "Who owns what" role table (Sigra core + 6 companions) with relative `.html` recipe cross-links; 6-row × 5-column fan-out matrix + cell legend; "The Diminishing Returns Wall" subsection with the three MILESTONE-ARC.md:215-220 bullets quoted verbatim and NO link into `.planning/`; six-bullet "Where to next" with relative cross-links to all six companion-lib recipes; verbatim D-15 end-echo. All four banned marketing phrases absent. Line count within 150–290 (D-11 ±20% slack). `mix docs --warnings-as-errors` is NOT run here — Task 5 owns that gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Topic-map row in `README.md` at position 2</name>
  <files>README.md</files>

  <read_first>
    - `README.md` (lines 139-160 — the `## Topic map → guides` table; insertion point is between current line 144 `First happy path` and current line 145 `Upgrade notes`)
    - `guides/introduction/suite-integration.md` (must exist — Task 1 output; the README row links to it)
    - `.planning/phases/133-suite-narrative-ecosystem-diagram/133-CONTEXT.md` (D-09, D-10 — position-2 rationale)
  </read_first>

  <action>
    Insert exactly ONE new row into the existing `## Topic map → guides` Markdown table at `README.md:139-160`. The row goes at position 2 — immediately after the `First happy path` row (current line 144), immediately before the `Upgrade notes` row (current line 145). The exact row text (from `<readme_topic_map_row>` in this plan's context):

    `| Companion library suite | [introduction/suite-integration.md](guides/introduction/suite-integration.md) |`

    Pipe spacing matches the existing rows (single space pad inside cells). Do NOT add a new H2 section. Do NOT add a `Pick your lane` row. Do NOT reorder existing rows. Do NOT modify the table header (`| Topic | Guide |`) or the separator line (`|-------|--------|`). Do NOT touch any other region of `README.md` (D-09: ONE row, NO other edits).

    After the insertion, the row order in the Topic-map table reads:
    1. `Installation & env`
    2. `First happy path`
    3. **`Companion library suite`** ← new
    4. `Upgrade notes`
    5. (existing rows continue in current order)

    Per D-10, position 2 is intentional: the suite narrative is conceptual orientation, semantically closer to "getting started" than to topic-specific deep-dive. Position-2 placement satisfies ROADMAP.md:108 success criterion #1 ("a reader landing on the README can follow a single link"). Do NOT insert at position 1 (Installation must stay first — it gates everything else).
  </action>

  <acceptance_criteria>
    - `README.md` contains the literal substring `| Companion library suite | [introduction/suite-integration.md](guides/introduction/suite-integration.md) |`.
    - The new row appears AFTER the `| First happy path |` row and BEFORE the `| Upgrade notes |` row (verify by line-number ordering: `grep -n` results show `First happy path` line < `Companion library suite` line < `Upgrade notes` line).
    - Topic-map table still contains all 18 pre-existing rows (verify: `Installation & env`, `First happy path`, `Upgrade notes`, `Registration`, `Login / logout`, `Password reset`, `Account lifecycle`, `OAuth`, `MFA`, `API / JWT`, `Audit logging`, `Deployment`, `Testing`, `Passkeys`, `Multi-tenant patterns`, `Custom user fields`, `Subdomain auth` — all 17 row labels grep-present; the 18th would be the new row).
    - The Topic-map H2 heading `## Topic map → guides` is unchanged.
    - NO new H2 section is added anywhere in `README.md` (verify `grep -c "^## " README.md` is unchanged from pre-edit count or +0).
    - NO Mermaid hero diagrams modified (verify `README.md:39-54` and `README.md:63-69` regions are byte-identical to pre-edit).
  </acceptance_criteria>

  <verify>
    <automated>
      bash -lc '
      F=README.md
      set -e
      # New row present, exact form
      grep -qF "| Companion library suite | [introduction/suite-integration.md](guides/introduction/suite-integration.md) |" "$F"
      # Ordering: First happy path < Companion library suite < Upgrade notes
      FH=$(grep -n "First happy path" "$F" | head -1 | cut -d: -f1)
      CL=$(grep -n "Companion library suite" "$F" | head -1 | cut -d: -f1)
      UN=$(grep -n "Upgrade notes" "$F" | head -1 | cut -d: -f1)
      test -n "$FH" && test -n "$CL" && test -n "$UN"
      test "$FH" -lt "$CL"
      test "$CL" -lt "$UN"
      # Topic-map H2 heading unchanged
      grep -qF "## Topic map → guides" "$F"
      # All 17 pre-existing row labels still present
      for label in "Installation & env" "First happy path" "Upgrade notes" "Registration" "Login / logout" "Password reset" "Account lifecycle" "OAuth" "MFA" "API / JWT" "Audit logging" "Deployment" "Testing" "Passkeys" "Multi-tenant patterns" "Custom user fields" "Subdomain auth"; do
        grep -qF "$label" "$F" || { echo "missing row label: $label"; exit 1; }
      done
      echo OK
      '
    </automated>
  </verify>

  <done>
    `README.md` Topic-map table at the existing `## Topic map → guides` section carries one new row at position 2 (between `First happy path` and `Upgrade notes`) with the exact text from `<readme_topic_map_row>`. All 17 pre-existing rows preserved. No new H2 sections added. No other regions of `README.md` modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply three-block `mix.exs` edit — extras: entry + skip-warnings entries + zero groups_for_extras edits</name>
  <files>mix.exs</files>

  <read_first>
    - `mix.exs` (lines 156-229 — `docs/0` private function; lines 160-174 `skip_undefined_reference_warnings_on:` block; lines 180-221 `extras:` list; lines 222-229 `groups_for_extras:` block — three regions form the edit target)
    - `guides/introduction/suite-integration.md` (must exist — Task 1 output; the new `extras:` entry registers it)
    - `.planning/phases/133-suite-narrative-ecosystem-diagram/133-CONTEXT.md` (D-04, D-05, D-06, D-17 — three-block edit summary + `<specifics>` exact insertion text)
  </read_first>

  <action>
    Apply the three-block surgical edit to `mix.exs` per D-17. All three blocks land in a single commit alongside Tasks 1, 2, and 4.

    **Block 1 — `extras:` list (currently lines 180-221):**
    Insert exactly ONE new entry between current line 198 (`"guides/introduction/upgrading-to-v1.1.md",`) and current line 199 (`"guides/flows/registration.md",`). The new entry text (from `<mix_exs_extras_insertion>` in this plan's context):

    `        "guides/introduction/suite-integration.md",`

    (Match the surrounding 8-space indent — count the indent of the line above, replicate it.) Preserve the trailing comma. Do NOT reorder existing entries. Do NOT insert anywhere else in the `extras:` list — D-05 is explicit about position.

    **Block 2 — `skip_undefined_reference_warnings_on:` list (currently lines 160-174):**
    Append FOUR new entries to the end of the list (after the current last entry `"guides/recipes/companion-libs/mailglass.md"` at line 173), under a comment header. The exact insertion text (from `<mix_exs_skip_warnings_insertion>` in this plan's context):

    ```
            # Phase 133: companion-lib recipes pending Phase 134 — remove these four
            # entries when accrue/lockspire/relyra/rulestead recipes land.
            "guides/recipes/companion-libs/accrue.md",
            "guides/recipes/companion-libs/lockspire.md",
            "guides/recipes/companion-libs/relyra.md",
            "guides/recipes/companion-libs/rulestead.md"
    ```

    Two trailing-comma rules to enforce:
    1. The PRE-EXISTING last entry `"guides/recipes/companion-libs/mailglass.md"` (currently line 173) is trailing-comma-less because it WAS the last element. After the Phase 133 additions, it is no longer the last element — ADD a trailing comma to it.
    2. The NEW last entry `"guides/recipes/companion-libs/rulestead.md"` must NOT carry a trailing comma — it is the new last element before the closing `]`.

    Net effect on the keyword-list: comma added to `mailglass.md` line, four new entries appended (the last of which is comma-less).

    **Block 3 — `groups_for_extras:` list (currently lines 222-229):**
    ZERO edits. Per D-04 the existing `Introduction: ~r{guides/introduction/.?}` regex at line 223 already absorbs `guides/introduction/suite-integration.md`. Verify by inspection (not by edit) that line 223 reads `Introduction: ~r{guides/introduction/.?},` — do not modify.

    Do NOT edit any other region of `mix.exs`. Do NOT touch `deps`, `aliases`, `no_warn_undefined`, `groups_for_modules`, `source_ref`, `source_url`, `formatters`, or the `main:` setting. Do NOT touch the `# Companion Libraries` group entry at line 226 (Phase 132 already landed it; Phase 134 will populate it with the four new recipes).
  </action>

  <acceptance_criteria>
    - **Block 1:** `mix.exs` contains the literal string `"guides/introduction/suite-integration.md"`.
    - **Block 1 ordering:** The new `suite-integration.md` line appears AFTER `upgrading-to-v1.1.md` and BEFORE `flows/registration.md` in line-number order.
    - **Block 2:** `mix.exs` contains all four new `skip_undefined_reference_warnings_on:` entries: `guides/recipes/companion-libs/accrue.md`, `guides/recipes/companion-libs/lockspire.md`, `guides/recipes/companion-libs/relyra.md`, `guides/recipes/companion-libs/rulestead.md`.
    - **Block 2 comment header:** `mix.exs` contains the literal substring `Phase 133: companion-lib recipes pending Phase 134` exactly once.
    - **Block 2 trailing-comma rule:** The PRE-EXISTING entry `"guides/recipes/companion-libs/mailglass.md"` now carries a trailing comma; the NEW last entry `"guides/recipes/companion-libs/rulestead.md"` does NOT carry a trailing comma. (Verify the file parses as valid Elixir via `mix compile` — Task 5 covers the full ExDoc gate.)
    - **Block 2 ordering:** The four new entries appear AFTER the existing Phase 132 entry `"guides/recipes/companion-libs/mailglass.md"`.
    - **Block 3 (zero edits):** The line `Introduction: ~r{guides/introduction/.?},` is byte-identical to pre-edit (D-04). The line `"Companion Libraries": ~r{guides/recipes/companion-libs/.?},` is byte-identical to pre-edit (Phase 132 landed it).
    - NO new `extras:` entries other than the single `suite-integration.md` line.
    - NO `groups_for_extras:` edits.
    - Compilation gate: `mix compile --warnings-as-errors` exits 0 (catches syntax errors from trailing-comma mistakes immediately).
  </acceptance_criteria>

  <verify>
    <automated>
      bash -lc '
      set -e
      F=mix.exs
      # Block 1 — extras entry present
      grep -qF "\"guides/introduction/suite-integration.md\"" "$F"
      # Block 1 ordering: upgrading-to-v1.1 < suite-integration < flows/registration
      UV=$(grep -n "upgrading-to-v1\.1\.md" "$F" | head -1 | cut -d: -f1)
      SI=$(grep -n "guides/introduction/suite-integration\.md" "$F" | head -1 | cut -d: -f1)
      FR=$(grep -n "guides/flows/registration\.md" "$F" | head -1 | cut -d: -f1)
      test -n "$UV" && test -n "$SI" && test -n "$FR"
      test "$UV" -lt "$SI"
      test "$SI" -lt "$FR"
      # Block 2 — four skip-warnings entries present
      grep -qF "guides/recipes/companion-libs/accrue.md" "$F"
      grep -qF "guides/recipes/companion-libs/lockspire.md" "$F"
      grep -qF "guides/recipes/companion-libs/relyra.md" "$F"
      grep -qF "guides/recipes/companion-libs/rulestead.md" "$F"
      # Block 2 comment header — exactly once
      HC=$(grep -cE "Phase 133: companion-lib recipes pending Phase 134" "$F")
      test "$HC" -eq 1
      # Block 2 ordering — Phase 133 entries appear AFTER the Phase 132 mailglass entry
      MG=$(grep -n "\"guides/recipes/companion-libs/mailglass\.md\"" "$F" | head -1 | cut -d: -f1)
      AC=$(grep -n "\"guides/recipes/companion-libs/accrue\.md\"" "$F" | head -1 | cut -d: -f1)
      test -n "$MG" && test -n "$AC"
      test "$MG" -lt "$AC"
      # Block 3 — Introduction: regex byte-identical (D-04 zero edits)
      grep -qE "Introduction: ~r\{guides/introduction/\.\?\}," "$F"
      grep -qE "\"Companion Libraries\": ~r\{guides/recipes/companion-libs/\.\?\}," "$F"
      # Compilation gate — catches trailing-comma syntax errors immediately
      mix compile --warnings-as-errors
      echo OK
      '
    </automated>
  </verify>

  <done>
    `mix.exs` carries the single new `extras:` entry `"guides/introduction/suite-integration.md"` between `upgrading-to-v1.1.md` and `flows/registration.md`. The `skip_undefined_reference_warnings_on:` block carries the four new entries (Accrue/Lockspire/Relyra/Rulestead) under a `# Phase 133: companion-lib recipes pending Phase 134` comment header, with the pre-existing `mailglass.md` entry now trailing-comma'd and the new `rulestead.md` entry trailing-comma-less. `groups_for_extras:` is byte-identical to pre-edit (D-04 zero edits). `mix compile --warnings-as-errors` exits 0. `mix docs --warnings-as-errors` is NOT run here — Task 5 owns that gate.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add reciprocal "Suite integration overview" pointer to `threadline.md`</name>
  <files>guides/recipes/companion-libs/threadline.md</files>

  <read_first>
    - `guides/recipes/companion-libs/threadline.md` (the existing "See also" section near end-of-file — currently lacks the suite-integration forward pointer)
    - `guides/recipes/companion-libs/mailglass.md` (lines 129-130 — the EXISTING reciprocal pointer this task mirrors verbatim modulo register; the entry reads `[Suite integration overview](../introduction/suite-integration.html) — companion-library ecosystem diagram (Phase 133 NX-01; link ships ahead of the page)`)
    - `.planning/phases/133-suite-narrative-ecosystem-diagram/133-CONTEXT.md` (`<deferred>` block — "Reciprocal cross-links from the Phase 132 recipes back to `suite-integration.html`" item, default YES)
  </read_first>

  <action>
    Add ONE new bullet to the existing `## See also` section of `guides/recipes/companion-libs/threadline.md`. The new bullet mirrors the equivalent bullet that already exists in `mailglass.md` lines 129-130; place it as the LAST bullet of the `## See also` section (immediately before the `### Custom forwarders` subsection that closes the file).

    Exact bullet text:

    `- [Suite integration overview](../introduction/suite-integration.html) — companion-library ecosystem diagram, fan-out matrix, and Diminishing Returns Wall framing`

    (Note: the mailglass.md form ends with a parenthetical "(Phase 133 NX-01; link ships ahead of the page)" — Phase 132 needed that hedge because Phase 133 had not yet shipped. Phase 133 IS this phase, and once Task 1 lands the page the hedge becomes inaccurate. Use the cleaner suffix shown above; do not copy the hedge.)

    Do NOT modify any other section of `threadline.md`. Do NOT modify the banner, the validated-against HTML comment, the `forwarders:` config block, the failure-modes section, the non-goals section, or the `### Custom forwarders` closing subsection. Do NOT add a banner-shape change. Do NOT touch the audit DB row anchor lines.

    If the reciprocal-pointer addition is reconsidered (CONTEXT.md `<deferred>` allows the planner to default to YES but flags it as a discretion call), the rationale for shipping it is: zero cost (one-line edit), completes the symmetry with `mailglass.md`, and makes the cross-link graph between the four Phase 132/133 docs (threadline ↔ mailglass ↔ suite-integration ↔ threadline) a closed loop. The CONTEXT default is YES and there is no countervailing signal.
  </action>

  <acceptance_criteria>
    - `guides/recipes/companion-libs/threadline.md` contains the literal substring `[Suite integration overview](../introduction/suite-integration.html)`.
    - The new bullet appears AFTER the existing `## See also` heading and BEFORE the `### Custom forwarders` subsection (line-number ordering: `## See also` < new bullet < `### Custom forwarders`).
    - The pre-existing two bullets in the `## See also` section (the `../flows/audit-logging.html` bullet and the `./mailglass.html` bullet) are preserved unchanged.
    - The `### Custom forwarders` closing subsection at end-of-file is preserved unchanged (verify: file ends with the existing `Mox.defmock(MyForwarder, for: Sigra.Audit.Forwarder)` anchor).
    - The banner (line 7), validated-against HTML comment (line 1), and all other sections are byte-identical to pre-edit (verify: `git diff guides/recipes/companion-libs/threadline.md` shows ONLY additions in or adjacent to the `## See also` section).
    - File line count increases by exactly 1 (the one new bullet line) — verify `wc -l` delta is 1.
  </acceptance_criteria>

  <verify>
    <automated>
      bash -lc '
      F=guides/recipes/companion-libs/threadline.md
      set -e
      # New bullet present
      grep -qF "[Suite integration overview](../introduction/suite-integration.html)" "$F"
      # Ordering: ## See also < new bullet < ### Custom forwarders
      SA=$(grep -n "^## See also\$" "$F" | head -1 | cut -d: -f1)
      NB=$(grep -n "Suite integration overview" "$F" | head -1 | cut -d: -f1)
      CF=$(grep -n "^### Custom forwarders\$" "$F" | head -1 | cut -d: -f1)
      test -n "$SA" && test -n "$NB" && test -n "$CF"
      test "$SA" -lt "$NB"
      test "$NB" -lt "$CF"
      # Pre-existing bullets preserved
      grep -qF "../flows/audit-logging.html" "$F"
      grep -qF "./mailglass.html" "$F"
      # Custom forwarders closing subsection intact
      grep -qF "Mox.defmock(MyForwarder, for: Sigra.Audit.Forwarder)" "$F"
      # Banner intact (line 7 anchor — D-15/D-16 verbatim form Phase 132 locked)
      grep -qF "Sigra works fully standalone" "$F"
      # Validated-against HTML comment intact
      head -3 "$F" | grep -qE "^<!-- validated_against: threadline ~> 0.5 -->$"
      echo OK
      '
    </automated>
  </verify>

  <done>
    `guides/recipes/companion-libs/threadline.md` carries one new bullet in the `## See also` section pointing at `../introduction/suite-integration.html` with a one-line framing. The closing `### Custom forwarders` subsection, banner, validated-against HTML comment, and all other content are byte-identical to pre-edit. Reciprocal-pointer symmetry with `mailglass.md` is now complete: both Phase 132 recipes point up to the Phase 133 narrative.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run final verification gates — `mix docs --warnings-as-errors` + banned-phrase grep + cross-link coverage sanity</name>
  <files></files>

  <read_first>
    - `guides/introduction/suite-integration.md` (must exist — Task 1 output)
    - `README.md` (must contain Task 2's new Topic-map row)
    - `mix.exs` (must carry Task 3's three-block edit; ExDoc gate runs against the merged state)
    - `guides/recipes/companion-libs/threadline.md` (must carry Task 4's reciprocal pointer)
    - `.planning/phases/133-suite-narrative-ecosystem-diagram/133-CONTEXT.md` (D-18, D-19 — the two verification gates this task fires)
  </read_first>

  <action>
    Run the THREE pre-commit verification gates on the merged tree (all four edit tasks 1-4 already applied). All three gates must pass before commit.

    **Gate 1 — ExDoc warnings-as-errors (D-18, REQUIRED).** From repo root:

    `mix docs --warnings-as-errors`

    This must exit 0 cleanly. The four `skip_undefined_reference_warnings_on:` entries added in Task 3 keep the four forward cross-links to not-yet-shipped Accrue/Lockspire/Relyra/Rulestead recipes from failing the gate. The two cross-links to the already-shipped Threadline/Mailglass recipes resolve today. If `mix docs --warnings-as-errors` fails, the failure mode is almost certainly one of: (a) a typo'd cross-link target that does not match any of the four suppressed paths, (b) the `skip_undefined_reference_warnings_on:` block has a syntax error from Task 3's trailing-comma manipulation, (c) a cross-link to a `.md` file the executor pasted instead of the `.html` form ExDoc expects. Fix in the originating task's file; do NOT add silencing entries beyond the four Task 3 added.

    Post-gate sanity: confirm the generated HTML page exists at `doc/guides/introduction/suite-integration.html`.

    **Gate 2 — Banned marketing phrase grep (D-19, REQUIRED — NX-01 success criterion #3).** From repo root:

    `rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/introduction/suite-integration.md`

    This MUST exit NON-ZERO (no matches). If `rg` is unavailable on the executor's PATH, the equivalent is `grep -iEqv` returning 1 (no match). The grep is case-insensitive (`-i`) by design.

    **Gate 3 — Cross-link coverage sanity (NX-01 success criterion #2).** From repo root, run six greps against `guides/introduction/suite-integration.md`, one per companion-lib recipe filename:

    - `grep -qF "threadline.html" guides/introduction/suite-integration.md`
    - `grep -qF "mailglass.html" guides/introduction/suite-integration.md`
    - `grep -qF "accrue.html" guides/introduction/suite-integration.md`
    - `grep -qF "lockspire.html" guides/introduction/suite-integration.md`
    - `grep -qF "relyra.html" guides/introduction/suite-integration.md`
    - `grep -qF "rulestead.html" guides/introduction/suite-integration.md`

    All six must exit 0. This is a coverage assertion, not just a link-existence check — it proves the narrative cross-links to ALL SIX companion-lib recipes (not just five) and therefore satisfies ROADMAP.md:109 success criterion #2 ("no orphan pages" in the suite).

    Do NOT run `mix test` (docs-only phase; no test additions per D-20). Do NOT run `mix credo --strict` (the docs change does not affect library code; Phase 136 PROOF-01 owns full lint discipline at milestone close). Do NOT skip Gate 1 even if the local executor's Postgres is unavailable — `mix docs` does not require Postgres.
  </action>

  <acceptance_criteria>
    - `mix docs --warnings-as-errors` exits 0.
    - Generated HTML page exists at `doc/guides/introduction/suite-integration.html`.
    - `rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/introduction/suite-integration.md` exits non-zero (no matches).
    - Six greps for `{threadline,mailglass,accrue,lockspire,relyra,rulestead}.html` against `guides/introduction/suite-integration.md` all exit 0.
    - `git diff --stat lib/` shows zero changes (docs + `mix.exs` only).
    - `git diff --stat test/` shows zero changes (no test additions).
    - `git diff --stat` shows changes ONLY in `guides/introduction/suite-integration.md`, `README.md`, `mix.exs`, and `guides/recipes/companion-libs/threadline.md` (exactly four files modified, one of them new).
  </acceptance_criteria>

  <verify>
    <automated>
      bash -lc '
      set -e
      # Gate 1: ExDoc warnings-as-errors (D-18)
      mix docs --warnings-as-errors
      test -f doc/guides/introduction/suite-integration.html
      # Gate 2: banned-phrase grep (D-19) — MUST exit non-zero (no matches)
      F=guides/introduction/suite-integration.md
      if grep -iqE "(seamlessly|just works|production-ready out of the box|the recommended way)" "$F"; then
        echo "FAIL: banned marketing phrase found in $F"
        grep -inE "(seamlessly|just works|production-ready out of the box|the recommended way)" "$F"
        exit 1
      fi
      # Gate 3: cross-link coverage — all six recipes referenced
      for name in threadline mailglass accrue lockspire relyra rulestead; do
        grep -qF "${name}.html" "$F" || { echo "missing cross-link: ${name}.html"; exit 1; }
      done
      # Library-tree non-regression
      LIB_CHANGES=$(git diff --stat lib/ | wc -l | tr -d " ")
      test "$LIB_CHANGES" -eq 0
      # Test-tree non-regression
      TEST_CHANGES=$(git diff --stat test/ | wc -l | tr -d " ")
      test "$TEST_CHANGES" -eq 0
      # File-count assertion — exactly four files in the staged diff (count includes the new untracked file once `git add`-ed)
      git add -A
      CHANGED=$(git diff --cached --name-only | sort -u)
      EXPECTED="README.md
guides/introduction/suite-integration.md
guides/recipes/companion-libs/threadline.md
mix.exs"
      ACTUAL=$(echo "$CHANGED")
      if [ "$ACTUAL" != "$EXPECTED" ]; then
        echo "UNEXPECTED FILE SET"
        echo "expected:"
        echo "$EXPECTED"
        echo "actual:"
        echo "$ACTUAL"
        exit 1
      fi
      echo OK
      '
    </automated>
  </verify>

  <done>
    `mix docs --warnings-as-errors` passes; generated `doc/guides/introduction/suite-integration.html` exists. Banned-phrase grep returns zero matches in the new narrative. All six companion-lib recipe filenames are referenced in the narrative (cross-link coverage proven). Library tree and test tree are unchanged. The phase's total file-change set is exactly four: `guides/introduction/suite-integration.md` (new), `README.md`, `mix.exs`, `guides/recipes/companion-libs/threadline.md`.
  </done>
</task>

</tasks>

<threat_model>
**Surface:** Documentation + ExDoc compile-time config edit. No runtime auth code.

**Assets touched:** None. (No secrets, no PII, no auth state, no DB migrations.)

**Threats considered:**
- Documentation injection / XSS via markdown → N/A: ExDoc renders Markdown via Earmark which escapes HTML by default. The HTML comments used for `<!-- last_validated: ... -->` are explicitly stripped by ExDoc.
- Supply-chain via `mix.exs` edit → no new dependencies added; only `extras:` list + `skip_undefined_reference_warnings_on:` list extended.
- Banned marketing phrase leak → mitigated by D-19 pre-commit grep gate (NX-01 success criterion #3).
- Stale cross-link to not-yet-shipped recipe → mitigated by D-06 `skip_undefined_reference_warnings_on:` entries; Phase 134 removes them.

**ASVS L1 mapping:** No ASVS controls touched (no auth code, no session handling, no validation surface).

**Block-on-high outcome:** No HIGH severity threats identified. Phase clears the gate.
</threat_model>

<verification>
After all five tasks complete, the merged tree must satisfy:

1. **File presence:** `test -f guides/introduction/suite-integration.md` succeeds.
2. **ExDoc gate (D-18):** `mix docs --warnings-as-errors` exits 0; `doc/guides/introduction/suite-integration.html` exists.
3. **Group assignment:** Generated `doc/index.html` lists `Suite Integration` under the `Introduction` sidebar group (via the unchanged `~r{guides/introduction/.?}` regex at `mix.exs:223`). NO new ExDoc group created. NO orphan companion-lib placement.
4. **Banned-phrase audit (D-19, NX-01 SC#3):** `rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/introduction/suite-integration.md` exits non-zero (no matches).
5. **Cross-link coverage (NX-01 SC#2):** All six companion-lib recipe `.html` paths referenced in the narrative (Threadline, Mailglass, Accrue, Lockspire, Relyra, Rulestead). The four not-yet-shipped recipes are suppressed in `skip_undefined_reference_warnings_on:` so the ExDoc gate stays green; Phase 134 removes the suppressions.
6. **Single-link discoverability (NX-01 SC#1):** `README.md` Topic-map table position-2 row links to `guides/introduction/suite-integration.md`.
7. **DRW byte-fidelity:** The narrative's Diminishing Returns Wall subsection contains the three bullets quoted byte-for-byte from `MILESTONE-ARC.md:215-220` (Phase 136 PROOF-01 may grep-assert this; Phase 133 does not).
8. **Banner verbatim:** The narrative carries the verbatim D-16 top banner AND the verbatim D-15 end-echo.
9. **No library regressions:** `git diff --stat lib/` returns no changes. `git diff --stat test/` returns no changes. Phase is docs-only modulo the `mix.exs` ExDoc config edit.
10. **File set:** Exactly four files in the staged diff: `guides/introduction/suite-integration.md` (new), `README.md`, `mix.exs`, `guides/recipes/companion-libs/threadline.md`.
</verification>

<success_criteria>
ROADMAP.md Phase 133 success criteria (lines 100-110), mapped to plan output:

1. **SC-1 (single-link README discoverability + one-page picture):** Met by Task 2's position-2 Topic-map row in `README.md` + Task 1's narrative carrying the ASCII ecosystem diagram, the 6×5 fan-out matrix, the verbatim D-16 "Sigra works fully standalone" banner, and the Diminishing Returns Wall subsection quoting MILESTONE-ARC.md:215-220.

2. **SC-2 (cross-links to all six companion-lib recipes; no orphan pages):** Met by Task 1's role-table + "Where to next" sections, both linking to all six recipes via relative `.html` paths. Task 3's four `skip_undefined_reference_warnings_on:` entries keep the ExDoc gate green for the four not-yet-shipped Phase 134 recipes; Phase 134 removes the suppressions in the same commit that lands the four recipes. Task 5's Gate 3 grep coverage check proves all six filenames are referenced.

3. **SC-3 (no banned marketing phrases):** Met by Task 1's voice-register discipline (mirrors `companion-oauth-provider.md` and Phase 132 recipes per D-19) + Task 5's Gate 2 banned-phrase grep against the new file. The four banned phrases (`seamlessly`, `just works`, `production-ready out of the box`, `the recommended way`) MUST exit non-zero on `rg -i`.

Requirement coverage:
- **NX-01** (REQUIREMENTS.md line 40): covered by Tasks 1, 2, 3, 5. Task 4 is a deferred-default-YES touch-up that completes the cross-link symmetry between Phase 132 recipes and the Phase 133 narrative.
</success_criteria>

<output>
On completion of all five tasks, create `.planning/phases/133-suite-narrative-ecosystem-diagram/133-01-SUMMARY.md` per the standard execute-plan summary template. The summary MUST record:

- Which of the seven `must_haves.truths` items above are now observably true (all seven should be).
- Which of the five `must_haves.artifacts` paths exist and contain the asserted anchors.
- The `mix docs --warnings-as-errors` and banned-phrase grep results from Task 5.
- The exact set of files modified (expect 4: new narrative + README + mix.exs + threadline reciprocal pointer).
- Any deviation from the locked decisions D-01 through D-21 (expect: none).
- Any text register / phrasing choices made within the explicit "Claude's Discretion" surface from CONTEXT.md's final `<decisions>` block (the TL;DR-vs-prose call, the 3-vs-4-column role-table call, the two DRW-prose-sentence reword call).
</output>
