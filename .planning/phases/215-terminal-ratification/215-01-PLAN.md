---
phase: 215
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [HEALTH-01]
must_haves:
  truths:
    - "The full library test suite runs green against live Postgres via the CLAUDE.md DB discovery (`source tmp/db.env` or localhost:5432 fallback): `mix test` finishes with 0 failures. HEALTH-01 (D-02)."
    - "The exact release-signal command AND its observed counts (tests / failures / skipped / excluded / doctests / properties) are recorded verbatim in the plan SUMMARY as the trustworthy release signal. HEALTH-01 (D-03)."
    - "Every skipped/excluded test is classified as a legitimate gate (intentional `:skip` planning-contract stubs + the conditional `:upgrade` exclusion when the phx_new archive is absent) — there is ZERO residual accepted-failure set. HEALTH-01 (D-02)."
  artifacts:
    - ".planning/phases/215-terminal-ratification/215-01-SUMMARY.md — records the exact release-signal command + observed pass/skip/exclude counts (the durable D-03 artifact)."
  key_links:
    - "The library `mix test` result IS the HEALTH-01 release signal; it is mirrored remotely by the CI `Library tests` required check (gated in 215-04). The SUMMARY's recorded command+counts is the durable artifact that D-03 requires."
  prohibitions:
    - "MUST NOT reintroduce a 'green modulo known set' posture — no accepted-failures list, no tolerated `Sigra.UpgradeIntegrationTest` env-DB failures, no tolerated `Chimeway.Repo` noise. Those are resolved/gated by HEALTH-03 (Phase 214). Any failure outside a legitimately gated/skipped test is a REAL regression to escalate (D-02)."
    - "MUST NOT edit product code, tests, or fixtures to make the suite pass, and MUST NOT add or delete tests to 'improve' the signal (D-06). If a real regression surfaces, escalate it — do not mask it."
    - "MUST NOT record 'it passed' as the signal — the reproducible command and the observed counts are the artifact (D-03)."
---

<objective>
Re-prove the full library test suite green against live Postgres and record the exact
release-signal command + observed counts as the trustworthy HEALTH-01 signal. Research ran this
live on-branch this session: `mix test` = 2404 tests, 0 failures, 12 skipped, 3 excluded, 33
doctests, 3 properties — so this is a re-prove-and-record pass, not a fix.

Purpose: HEALTH-01 is the milestone's core "the foundation is demonstrably green" claim. Per D-03
"it passed" is insufficient — the reproducible command and the observed pass/skip/exclude counts
are the durable artifact. Per D-02 there is NO accepted-failure residue: the Phase 211-era known
set (3 upgrade env-DB failures + Chimeway noise) was resolved/gated by HEALTH-03 in Phase 214, so
green means zero failures.

Output: the library suite green with 0 failures; the exact command + counts recorded verbatim in
215-01-SUMMARY.md; every skip/exclude classified as a legitimate gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-terminal-ratification/215-CONTEXT.md
@.planning/phases/215-terminal-ratification/215-RESEARCH.md
@CLAUDE.md
@test/test_helper.exs
</context>

## Artifacts this phase produces

This is a verification/ratification plan — it produces **evidence**, not source symbols:
- `215-01-SUMMARY.md` recording the exact release-signal command and its observed counts (the D-03 artifact of record).
- No code, test, or fixture changes (D-06). `git status` for tracked source stays clean.

<tasks>

<task type="auto">
  <name>Task 1: Boot live Postgres and run the full library suite green (HEALTH-01, D-02)</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Local development prerequisites: `scripts/db/up.sh` writes `tmp/db.env` with `SIGRA_TEST_PG_*` on a dynamic port; readers fall back to `localhost:5432` when `tmp/db.env` is absent; `mix test` sets `MIX_ENV=test` itself; phx_new 1.8.8 archive pin for install golden)
    - test/test_helper.exs (the conditional `:upgrade` exclude preflight — `:upgrade` tests run only when the phx_new 1.8.8 archive is present; `Sigra.Test.PostgresRepo` sandbox `:manual` mode)
    - .planning/phases/215-terminal-ratification/215-RESEARCH.md (§Objective 1 — the verified live command + the 2404/0-fail/12-skip/3-excl counts; §Common Pitfalls for the exclusion classification)
  </read_first>
  <action>
    Discover the test database per CLAUDE.md: `source tmp/db.env` if present (dynamic ephemeral PG
    port), else rely on the automatic `localhost:5432` fallback. If neither is reachable, boot the
    ephemeral test Postgres with `scripts/db/up.sh` first (it writes `tmp/db.env`); then `source
    tmp/db.env`. Confirm the DB is reachable before running the suite (`pg_isready` on the resolved
    host/port, or the ephemeral port from `tmp/db.env`).

    Run the canonical full-suite release-signal command: `source tmp/db.env 2>/dev/null; mix test`
    (mix sets `MIX_ENV=test` itself — do NOT pass a stale/watch flag; run the WHOLE suite, not
    `--stale`). Confirm it finishes with **0 failures**. Research observed 2404 tests, 0 failures,
    12 skipped, 3 excluded, 33 doctests, 3 properties — treat those as the expected shape but
    RECORD the actual observed numbers from this run (counts may drift by ±small as the suite
    evolves; the invariant is 0 failures).

    If ANY failure appears outside a legitimately gated/skipped test, STOP and escalate it as a
    real regression (D-02/D-06) — do NOT edit product code, tests, or fixtures to mask it, and do
    NOT add it to an accepted-failures list. A byte-diff style failure from a wrong local phx_new
    archive is an environment issue → install the 1.8.8 pin (`mix archive.install --force hex
    phx_new 1.8.8`), never edit a fixture.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && source tmp/db.env 2>/dev/null; mix test 2>&1 | tee /tmp/215-01-libtest.log | tail -5; echo "libtest_exit=${PIPESTATUS[0]}"; grep -Eq '0 failures' /tmp/215-01-libtest.log && echo "ZERO_FAILURES_CONFIRMED" || echo "FAILURES_PRESENT_ESCALATE"</automated>
  </verify>
  <acceptance_criteria>
    - The resolved test Postgres is reachable (ephemeral port from `tmp/db.env`, or localhost:5432 fallback) before the suite runs.
    - `source tmp/db.env 2>/dev/null; mix test` completes with `0 failures` (the log line contains `0 failures`).
    - The run's summary line (tests / failures / skipped / excluded / doctests / properties) is captured to `/tmp/215-01-libtest.log`.
    - No product code, test file, or fixture was modified to achieve green (`git status` clean for tracked source under `lib/`, `test/`, `priv/`).
  </acceptance_criteria>
  <done>The full library suite runs green (0 failures) against live Postgres via the CLAUDE.md DB discovery, with the summary counts captured.</done>
</task>

<task type="auto">
  <name>Task 2: Classify all skips/excludes and record the exact release signal (HEALTH-01, D-03)</name>
  <files></files>
  <read_first>
    - /tmp/215-01-libtest.log (the Task 1 run output — the summary line with tests/failures/skipped/excluded counts)
    - test/test_helper.exs (the two legitimate gates: intentional `@moduletag :skip` planning-contract stubs, and the conditional `:upgrade` exclusion keyed on phx_new-archive presence)
    - .planning/phases/214-debt-robustness-clear/214-VERIFICATION.md (HEALTH-03 resolution: Chimeway.Repo config stanza + conditional :upgrade skip — the reason there is no accepted-failure residue)
  </read_first>
  <action>
    Classify every skipped and excluded test from the Task 1 run against the two documented gates:
    (1) intentional `:skip` planning-contract stubs, and (2) the conditional `:upgrade` exclusion
    (runs in CI where the phx_new 1.8.8 archive is present; cleanly excluded locally when it is
    absent — HEALTH-03, Phase 214). Confirm NO skip/exclude is a disguised failure and that there
    is ZERO residual accepted-failure set (per D-02, the Phase 211-era 3 upgrade env-DB failures +
    Chimeway noise are resolved/gated, not tolerated).

    Then record the trustworthy release signal in the plan SUMMARY per D-03: the EXACT reproducible
    command (`source tmp/db.env 2>/dev/null; mix test`, with the CLAUDE.md DB-discovery note) AND
    the observed counts (tests / failures / skipped / excluded / doctests / properties) verbatim
    from `/tmp/215-01-libtest.log`. "It passed" is NOT the artifact — the command + counts are.
    Note in the SUMMARY that this local result is the HEALTH-01 signal and is mirrored remotely by
    the CI `Library tests` required check (gated in 215-04).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && test -f .planning/phases/215-terminal-ratification/215-01-SUMMARY.md && grep -Eq 'source tmp/db.env' .planning/phases/215-terminal-ratification/215-01-SUMMARY.md && grep -Eq '0 failures' .planning/phases/215-terminal-ratification/215-01-SUMMARY.md && echo "release_signal_recorded" || echo "SIGNAL_NOT_RECORDED"</automated>
  </verify>
  <acceptance_criteria>
    - Every skipped and excluded test is classified against the two legitimate gates (intentional `:skip` stubs + conditional `:upgrade` exclusion); none is a disguised failure.
    - The SUMMARY records the exact command (`source tmp/db.env 2>/dev/null; mix test`) AND the observed counts verbatim (tests/failures/skipped/excluded/doctests/properties).
    - The SUMMARY states there is ZERO residual accepted-failure set (no 'green modulo known set' posture).
  </acceptance_criteria>
  <done>All skips/excludes are classified as legitimate gates and the exact release-signal command + counts are recorded in the SUMMARY as the durable HEALTH-01 artifact (D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan runs the existing library test suite against a
live test Postgres and reads git status — no new code paths, inputs, endpoints, schemas, or crypto.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-01 | Tampering / Repudiation | library test suite result (release signal) | low | accept | Verification-only phase (D-06); no new attack surface. The relevant control is process-level: recording the exact reproducible command + counts (D-03) makes the release signal auditable rather than a bare "it passed" claim. The green-suite result is itself a regression tripwire. No ASVS category newly engaged; threat model unchanged from the v1.42/Phase-211 close. |
</threat_model>

<verification>
- `source tmp/db.env 2>/dev/null; mix test` finishes with `0 failures` against live Postgres.
- Every skip/exclude is classified as a legitimate gate (`:skip` stubs + conditional `:upgrade`); zero residual accepted-failure set.
- `215-01-SUMMARY.md` records the exact command + observed counts verbatim (D-03 artifact).
- No product code, test, or fixture was modified (`git status` clean for tracked source).
</verification>

<success_criteria>
The full library suite is proven green (0 failures) against live Postgres via the CLAUDE.md DB
discovery; every skip/exclude is a classified legitimate gate with no accepted-failure residue
(D-02); and the exact reproducible command + observed counts are recorded in the SUMMARY as the
trustworthy HEALTH-01 release signal (D-03) — with no source/test/fixture edits (D-06).
</success_criteria>

<output>
Create `.planning/phases/215-terminal-ratification/215-01-SUMMARY.md` when done.
</output>