---
phase: 215
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [RATIFY-01]
must_haves:
  truths:
    - "Every v1.43-pulled deferred-items-ledger entry (COMPAT-*, DEBT-*, HEALTH-03) reads *resolved* — the six resolved todos (oban-enqueue, phase209-code-review, phase200-code-review, app-css-comment-corruption, v142-integration-snapshot-canary-drift, stale-known-failure-contract-tests) are all under `.planning/todos/resolved/`. RATIFY-01 (D-04)."
    - "The six explicitly out-of-scope pending todos (4 v2/UI/SEED features + Tasklane rebrand residuals + per-shard-DB Playwright) remain deferred under `.planning/todos/pending/` — they are NOT unreconciled milestone debt. RATIFY-01 (D-04)."
    - "The 2026-07-02 `app-css-corruption-guard-blind-spot` todo is CONFIRMED classified as an accepted low-severity DEBT-05 follow-up (live app.css clean + browser-verified; regression-guard hardening gap only) and left TRACKED — 'no new blocking debt' holds. RATIFY-01 (D-05)."
    - "A milestone close-READINESS / audit record is produced enumerating the reconciled ledger state; it explicitly states merge + archival are DEFERRED to /gsd-ship + /gsd-complete-milestone (D-01). RATIFY-01."
  artifacts:
    - ".planning/phases/215-terminal-ratification/215-03-SUMMARY.md — the milestone close-readiness / ledger-reconciliation audit record (resolved vs deferred vs accepted-classified)."
  key_links:
    - "The `.planning/todos/resolved/` vs `.planning/todos/pending/` split IS the RATIFY-01 ledger reconciliation; the SUMMARY audit record ties each v1.43-pulled entry to resolved status and each deferred item to its out-of-scope disposition (D-04/D-05)."
  prohibitions:
    - "MUST NOT fix or move the `app-css-corruption-guard-blind-spot` todo — it is CONFIRMED accepted/tracked (D-05), not resolved in this phase. Confirm the classification; do NOT edit the awk guard or the todo status."
    - "MUST NOT re-triage, resolve, or pull in the out-of-scope v2/UI/SEED pending todos — they stay deferred (D-04); touching them would expand scope beyond v1.43."
    - "MUST NOT perform milestone archival or move any phase directory — archival is deferred to /gsd-complete-milestone (D-01). This plan proves close-READINESS only."
    - "MUST NOT edit product code, tests, or fixtures (D-06) — this is a pure bookkeeping cross-check."
---

<objective>
Reconcile the deferred-items ledger for the v1.43 milestone close and confirm no new blocking
debt, producing the milestone close-readiness audit record. Research cross-checked this live: all
v1.43-pulled items are in `.planning/todos/resolved/`; the six v2/UI/SEED + the app.css-guard
follow-up remain correctly deferred in `.planning/todos/pending/`. This is a confirm-and-record
pass, not a fix.

Purpose: RATIFY-01 requires that every deferred-items-ledger entry pulled INTO v1.43 (the COMPAT-*
items resolved in Phase 213, the DEBT-* + HEALTH-03 items resolved in Phase 214) reads *resolved*,
that the explicitly out-of-scope items stay deferred (not counted as unreconciled debt), and that
the one 2026-07-02 app.css-guard follow-up is CONFIRMED as an accepted low-severity DEBT-05
follow-up so "no new blocking debt" holds. Per D-01 this plan proves close-READINESS only — the
actual merge + archival are deferred to /gsd-ship + /gsd-complete-milestone.

Output: a milestone close-readiness / ledger-reconciliation audit record in 215-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/215-terminal-ratification/215-CONTEXT.md
@.planning/phases/215-terminal-ratification/215-RESEARCH.md
@.planning/todos/pending/2026-07-02-app-css-corruption-guard-blind-spot.md
</context>

## Artifacts this phase produces

This is a verification/ratification plan — it produces **evidence/records**, not source symbols:
- `215-03-SUMMARY.md`: the milestone close-readiness audit record — the resolved-ledger cross-check, the deferred-items disposition, and the app.css-guard accepted-classification confirmation.
- No code, todo-status, or fixture changes (D-06). `git status` for tracked source and todos stays clean.

<tasks>

<task type="auto">
  <name>Task 1: Cross-check the v1.43-pulled ledger reads resolved; deferred items stay deferred (RATIFY-01, D-04)</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md (the v1.43 requirement set + the v2/UI/SEED "Out of Scope" list — the authoritative scope boundary for what counts as v1.43-pulled vs deferred)
    - .planning/STATE.md ("Deferred Items" → "Acknowledged at v1.42 close": the exact table of items pulled INTO v1.43 (oban=DEBT-01, phase209=DEBT-02, phase200=DEBT-03, SEED-004=COMPAT, app-css-comment-corruption=DEBT-05) vs items deferred beyond v1.43)
    - .planning/phases/215-terminal-ratification/215-RESEARCH.md (§Objective 5 / "Don't Hand-Roll" — the resolved/ vs pending/ cross-check; the D-04 scope list of the six deferred pending todos)
  </read_first>
  <action>
    Cross-check `.planning/todos/resolved/` against `.planning/todos/pending/` using the D-04 scope
    boundary from REQUIREMENTS.md + STATE.md. Confirm every v1.43-pulled entry reads *resolved*: the
    DEBT/COMPAT/HEALTH-03 work landed in Phases 213/214, so its ledger todos must be under
    `resolved/` — specifically confirm these are present in `resolved/`: `oban-enqueue-unguarded-...`
    (DEBT-01), `phase209-code-review-deferred` (DEBT-02), `phase200-code-review-deferred` (DEBT-03),
    `app-css-comment-corruption-cleanup` (DEBT-05), plus the v1.42-integration-snapshot-canary-drift
    and stale-known-failure-contract-tests items that HEALTH-03/Phase-214 cleared.

    Confirm the SIX explicitly out-of-scope pending todos remain in `pending/` and are NOT counted
    as unreconciled milestone debt: `uat-demo-dx-polish-nits` (UI-01), `vaultr-authed-rebrand-residuals`
    (UI-02), `runtime-auth-prefix-override` (FEAT-01), `mix-sigra-migrate-schema-helper` (FEAT-02),
    `white-label-auth-email-theming` (FEAT-03), `playwright-parallelization-per-shard-db` (SEED-005).
    Do NOT resolve, re-triage, or pull these in (D-04) — they are correctly deferred.

    Record the reconciliation result as a table in the SUMMARY (v1.43-pulled → resolved; deferred →
    out-of-scope disposition + which v2/UI/SEED bucket). Do NOT move any todo file.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && MISS=0; for f in 2026-06-24-oban-enqueue-unguarded-when-compiled-but-unsupervised 2026-07-01-phase209-code-review-deferred 2026-06-25-phase200-code-review-deferred 2026-06-21-app-css-comment-corruption-cleanup 2026-06-30-v142-integration-snapshot-canary-drift 2026-06-26-stale-known-failure-contract-tests; do test -f ".planning/todos/resolved/$f.md" || { echo "MISSING_RESOLVED: $f"; MISS=1; }; done; for f in 2026-06-19-uat-demo-dx-polish-nits 2026-06-22-vaultr-authed-rebrand-residuals 2026-06-20-runtime-auth-prefix-override 2026-06-20-mix-sigra-migrate-schema-helper 2026-06-22-white-label-auth-email-theming 2026-06-20-playwright-parallelization-per-shard-db; do test -f ".planning/todos/pending/$f.md" || { echo "MISSING_PENDING: $f"; MISS=1; }; done; [ "$MISS" = 0 ] && echo "LEDGER_RECONCILED" || echo "LEDGER_MISMATCH_ESCALATE"</automated>
  </verify>
  <acceptance_criteria>
    - All six v1.43-pulled ledger todos are present under `.planning/todos/resolved/` (none stranded in `pending/`).
    - The six explicitly out-of-scope todos remain under `.planning/todos/pending/` and are not counted as unreconciled debt.
    - The SUMMARY contains a reconciliation table (v1.43-pulled → resolved; deferred → out-of-scope disposition).
    - No todo file was moved, resolved, or re-triaged by this task (`git status` clean for `.planning/todos/`).
  </acceptance_criteria>
  <done>The v1.43-pulled ledger reads resolved and the out-of-scope items stay correctly deferred, recorded as a reconciliation table (D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the app.css-guard follow-up is accepted low-severity, tracked; produce close-readiness record (RATIFY-01, D-05/D-01)</name>
  <files></files>
  <read_first>
    - .planning/todos/pending/2026-07-02-app-css-corruption-guard-blind-spot.md (severity: low; source: phase-214 verification; the live app.css is clean + browser-parse-verified — this is a regression-guard hardening gap, NOT a live defect)
    - .planning/phases/215-terminal-ratification/215-CONTEXT.md (D-05: this item is an accepted low-severity DEBT-05 follow-up, the phase must CONFIRM the classification not fix it; D-01: merge + archival deferred to /gsd-ship + /gsd-complete-milestone)
    - .planning/phases/214-debt-robustness-clear/214-VERIFICATION.md (the DEBT-05 live app.css cleanup + the CI corruption-guard script; the context that makes the guard blind-spot non-blocking)
  </read_first>
  <action>
    CONFIRM (do not fix) the classification of `2026-07-02-app-css-corruption-guard-blind-spot`:
    read the todo and verify it is a low-severity regression-guard hardening gap (the live app.css
    is clean + browser-parse-verified; the guard's awk `last_was_prop` blind-spot only misses a
    mid-block orphan that does not exist in the live file). Confirm it is correctly left TRACKED in
    `pending/` and that RATIFY-01's "no new blocking debt" holds WITH this item classified —
    because it is a hardening follow-up, not a live defect. Do NOT edit the awk guard, do NOT change
    the todo status, do NOT move it to resolved (D-05).

    Produce the milestone close-READINESS audit record in the SUMMARY: (a) the v1.43-pulled ledger
    reads resolved (from Task 1), (b) the deferred items are correctly out-of-scope, (c) the
    app.css-guard follow-up is accepted low-severity + tracked → no new BLOCKING debt, and (d) an
    explicit statement that the actual merge to origin/main and milestone archival are DEFERRED to
    /gsd-ship + /gsd-complete-milestone (D-01) — this phase proves close-READINESS only. Note that
    the full suite + CI green legs of RATIFY-01 are carried by plans 215-01/02 (suites) and 215-04
    (required CI checks), which this audit record references.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && test -f .planning/todos/pending/2026-07-02-app-css-corruption-guard-blind-spot.md && S=.planning/phases/215-terminal-ratification/215-03-SUMMARY.md && test -f "$S" && grep -Eiq 'accepted low-severity|no new blocking debt' "$S" && grep -Eiq 'defer' "$S" && grep -Eiq 'complete-milestone|gsd-ship' "$S" && echo "close_readiness_recorded" || echo "RECORD_INCOMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - The `app-css-corruption-guard-blind-spot` todo is UNCHANGED and still in `pending/` (confirmed accepted, not fixed/moved).
    - The SUMMARY states the app.css-guard item is an accepted low-severity DEBT-05 follow-up, tracked, → no new BLOCKING debt (D-05).
    - The SUMMARY explicitly defers merge + milestone archival to /gsd-ship + /gsd-complete-milestone (D-01) and references 215-01/02/04 for the suite + CI-green legs of RATIFY-01.
    - No awk guard, todo, or source file was edited (`git status` clean).
  </acceptance_criteria>
  <done>The app.css-guard follow-up is confirmed accepted low-severity + tracked, and the milestone close-readiness audit record is produced with merge/archival explicitly deferred (D-05/D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan cross-checks planning-doc todo files and produces a
markdown audit record — no code, inputs, endpoints, schemas, or crypto.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-03 | Repudiation | milestone ledger reconciliation record | low | accept | Verification/bookkeeping-only phase (D-06); no attack surface. The relevant control is process-level: the resolved/ vs pending/ cross-check + the explicit accepted-classification of the app.css-guard follow-up make the "no new blocking debt" claim auditable rather than asserted. The app.css guard blind-spot is a low-severity regression-guard gap on an already-clean, browser-verified file — accepted + tracked, not blocking. No ASVS category newly engaged. |
</threat_model>

<verification>
- All six v1.43-pulled ledger todos read *resolved*; the six out-of-scope todos stay deferred (D-04).
- The `app-css-corruption-guard-blind-spot` todo is confirmed accepted low-severity + tracked, unchanged (D-05).
- `215-03-SUMMARY.md` contains the reconciliation table + close-readiness record with merge/archival explicitly deferred to /gsd-ship + /gsd-complete-milestone (D-01).
- No todo file, source, or fixture was modified (`git status` clean).
</verification>

<success_criteria>
The v1.43 deferred-items ledger is reconciled — every pulled entry reads resolved, the out-of-scope
items stay correctly deferred, and the app.css-guard follow-up is confirmed accepted low-severity +
tracked so "no new blocking debt" holds (D-04/D-05) — with a milestone close-readiness audit record
that explicitly defers merge + archival to /gsd-ship + /gsd-complete-milestone (D-01) and no source
edits (D-06).
</success_criteria>

<output>
Create `.planning/phases/215-terminal-ratification/215-03-SUMMARY.md` when done.
</output>
