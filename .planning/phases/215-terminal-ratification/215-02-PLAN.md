---
phase: 215
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [HEALTH-02]
must_haves:
  truths:
    - "The example app test suite (`test/example`) runs green against live Postgres in the CI-parity env (`PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost`, localhost:5432): `mix test --include example_app` finishes with 0 failures. HEALTH-02 (D-02)."
    - "The example-suite command + observed counts (tests / failures) are recorded in the plan SUMMARY as the generated-host parity evidence."
    - "The `Jetstream #907 structural defense` `[error]` log lines are recognized as the TESTED deny-path firing (a passing test), NOT a failure. HEALTH-02 (D-02)."
  artifacts:
    - ".planning/phases/215-terminal-ratification/215-02-SUMMARY.md — records the example-suite command + observed counts (generated-host parity evidence)."
  key_links:
    - "The `test/example` `mix test` result is the HEALTH-02 generated-host parity signal; it is mirrored remotely by the CI `Example unit smoke (ExUnit + ConnTest)` required check (gated in 215-04)."
  prohibitions:
    - "MUST NOT reintroduce a 'green modulo known set' posture — the example suite passes with 0 failures, no tolerated failures (D-02)."
    - "MUST NOT edit product code, example templates, tests, or fixtures to make the suite pass, and MUST NOT add/delete tests to 'improve' the signal (D-06). A real regression is escalated, not masked."
    - "MUST NOT misread the tested `Jetstream #907 structural defense` deny-path `[error]` log lines as a failure — the test asserting that deny-path passes."
---

<objective>
Re-prove the example app test suite green against live Postgres and record the command + observed
counts as the HEALTH-02 generated-host parity evidence. Research ran this live on-branch this
session (CI-parity env): `cd test/example && mix test --include example_app` = 323 tests, 0
failures — so this is a re-prove-and-record pass, not a fix.

Purpose: HEALTH-02 proves the generated host (the example app that mirrors `mix sigra.install`
output) is demonstrably green. Per D-02 green means zero failures; the Phase 211-era accepted set
is resolved. The example suite emits tested-deny-path `[error]` log lines (`Jetstream #907
structural defense`) that are the passing accept-invitation deny branch firing — not failures.

Output: the example suite green with 0 failures; the command + counts recorded in 215-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-terminal-ratification/215-CONTEXT.md
@.planning/phases/215-terminal-ratification/215-RESEARCH.md
@CLAUDE.md
@test/example/test/test_helper.exs
</context>

## Artifacts this phase produces

This is a verification/ratification plan — it produces **evidence**, not source symbols:
- `215-02-SUMMARY.md` recording the example-suite command + observed counts (generated-host parity evidence).
- No code, template, test, or fixture changes (D-06). `git status` for tracked source stays clean.

<tasks>

<task type="auto">
  <name>Task 1: Run the example app suite green against live Postgres in CI-parity env (HEALTH-02, D-02)</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Local development prerequisites: the example lane reads libpq `PG*` vars; CI uses `localhost:5432`; the example Playwright boot notes and the phx_new 1.8.8 archive pin)
    - test/example/test/test_helper.exs (the example suite's ExUnit config + `example_app` tag inclusion)
    - .planning/phases/215-terminal-ratification/215-RESEARCH.md (§Objective 2 — the verified CI-parity command + the 323/0-fail counts; the note that `Jetstream #907 structural defense` `[error]` lines are the TESTED deny-path, not a failure)
  </read_first>
  <action>
    Run the example suite in the exact CI-parity environment the `Example unit smoke` lane uses.
    Ensure a live Postgres is reachable at `localhost:5432` (the example lane targets libpq `PG*`
    vars → `localhost:5432`, NOT the ephemeral `tmp/db.env` port; do NOT `source tmp/db.env` for
    this lane). If a local `localhost:5432` Postgres is not available, boot one (Homebrew PG or a
    disposable container) with credentials `postgres`/`postgres`.

    From `test/example`, export `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost`, create and
    migrate the test DB (`MIX_ENV=test mix ecto.create && MIX_ENV=test mix ecto.migrate`), then run
    `mix test --include example_app`. Confirm it finishes with **0 failures**. Research observed
    323 tests, 0 failures — treat that as the expected shape but RECORD the actual observed counts
    from this run.

    Recognize the `[error] ... Jetstream #907 structural defense ...` log lines as the TESTED
    deny-path firing (the accept_invitation invalid/already-accepted branch) — the emitting test
    PASSES; those lines are expected output, not a failure. If a genuine failure appears, escalate
    it as a real regression (D-02/D-06) — do NOT edit example templates, tests, or fixtures to mask
    it.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && export PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost && MIX_ENV=test mix ecto.create >/dev/null 2>&1; MIX_ENV=test mix ecto.migrate >/dev/null 2>&1; mix test --include example_app 2>&1 | tee /tmp/215-02-exampletest.log | tail -5; echo "exampletest_exit=${PIPESTATUS[0]}"; grep -Eq '0 failures' /tmp/215-02-exampletest.log && echo "ZERO_FAILURES_CONFIRMED" || echo "FAILURES_PRESENT_ESCALATE"</automated>
  </verify>
  <acceptance_criteria>
    - A live Postgres is reachable at `localhost:5432` (CI-parity target) and the example test DB is created + migrated.
    - `mix test --include example_app` (from `test/example`, CI-parity `PG*` env) completes with `0 failures`.
    - The `Jetstream #907 structural defense` `[error]` lines (if present) are noted as the tested deny-path, not a failure.
    - No example template, test, or fixture was modified to achieve green (`git status` clean for tracked source under `test/example/`).
  </acceptance_criteria>
  <done>The example app suite runs green (0 failures) against live `localhost:5432` Postgres in the CI-parity env, with the summary counts captured.</done>
</task>

<task type="auto">
  <name>Task 2: Record the example-suite parity signal in the SUMMARY (HEALTH-02)</name>
  <files></files>
  <read_first>
    - /tmp/215-02-exampletest.log (the Task 1 example-suite output — the summary line with tests/failures counts)
    - .planning/phases/215-terminal-ratification/215-RESEARCH.md (§"Phase Requirements → Test Map" — the HEALTH-02 command + the mirroring CI `Example unit smoke` lane)
  </read_first>
  <action>
    Record the HEALTH-02 evidence in the plan SUMMARY: the EXACT reproducible example-suite command
    (`cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test --include
    example_app`) AND the observed counts (tests / failures) verbatim from
    `/tmp/215-02-exampletest.log`. Note that this local result is the HEALTH-02 generated-host
    parity signal and is mirrored remotely by the CI `Example unit smoke (ExUnit + ConnTest)`
    required check (gated in 215-04). State explicitly that there is no accepted-failure residue
    (0 failures, D-02) and that the `Jetstream #907` `[error]` lines are the tested deny-path.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && test -f .planning/phases/215-terminal-ratification/215-02-SUMMARY.md && grep -Eq 'example_app' .planning/phases/215-terminal-ratification/215-02-SUMMARY.md && grep -Eq '0 failures' .planning/phases/215-terminal-ratification/215-02-SUMMARY.md && echo "parity_signal_recorded" || echo "SIGNAL_NOT_RECORDED"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the exact example-suite command AND the observed counts (tests/failures) verbatim.
    - The SUMMARY states 0 failures / no accepted-failure residue (D-02) and notes the `Jetstream #907` deny-path as tested-passing.
    - The SUMMARY links the local result to the CI `Example unit smoke (ExUnit + ConnTest)` required check.
  </acceptance_criteria>
  <done>The example-suite parity signal (command + counts) is recorded in the SUMMARY as the durable HEALTH-02 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan runs the existing example app test suite against a
live test Postgres and reads git status — no new code paths, inputs, endpoints, schemas, or crypto.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-02 | Tampering / Repudiation | example app test suite result (parity signal) | low | accept | Verification-only phase (D-06); no new attack surface. Recording the exact command + counts makes the generated-host parity signal auditable. The green example suite is itself a regression tripwire; the tested `Jetstream #907` deny-path proves the structural-defense control still fires. No ASVS category newly engaged; threat model unchanged from the Phase-211 close. |
</threat_model>

<verification>
- `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test --include example_app` finishes with `0 failures`.
- The `Jetstream #907 structural defense` `[error]` lines are recognized as the tested deny-path (not a failure).
- `215-02-SUMMARY.md` records the exact command + observed counts, linked to the CI `Example unit smoke` lane.
- No example template, test, or fixture was modified (`git status` clean for tracked source under `test/example/`).
</verification>

<success_criteria>
The example app suite is proven green (0 failures) against live `localhost:5432` Postgres in the
CI-parity env; the tested `Jetstream #907` deny-path is correctly recognized as passing; and the
exact command + observed counts are recorded in the SUMMARY as the durable HEALTH-02 generated-host
parity evidence — with no source/template/test/fixture edits (D-06).
</success_criteria>

<output>
Create `.planning/phases/215-terminal-ratification/215-02-SUMMARY.md` when done.
</output>
