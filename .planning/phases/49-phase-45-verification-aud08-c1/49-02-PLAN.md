---
phase: 49
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/phases/09-audit-logging/09-VERIFICATION.md
  - .planning/phases/09-audit-logging/09-03-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - AUD-08
must_haves:
  truths:
    - "**`REQUIREMENTS.md`** **AUD-08** checkbox and traceability row flip only when **`45-VERIFICATION.md`** reads **`status: passed`**."
    - "**`09-VERIFICATION.md` C-1** uses **three** explicit inventory subsections (**43**, **44**, **45**) with exhaustive row coverage — no **“representative only”** matrix without excluded-population listing."
    - "**`ROADMAP.md`** row **49** / phase **45** prose stays aligned with **phase 50** Nyquist ownership if touched."
  artifacts:
    - path: ".planning/phases/09-audit-logging/09-VERIFICATION.md"
      provides: "C-1 reconciliation vs 43/44/45 AUD-04 inventories"
nyquist_compliant: false
---

<objective>
After **`45-VERIFICATION.md`** proves **`status: passed`**, reconcile **`.planning/phases/09-audit-logging/09-VERIFICATION.md` C-1** with exhaustive per-inventory matrices, refresh **`09-03-SUMMARY.md`** if needed, and flip **`.planning/REQUIREMENTS.md`** **AUD-08** + traceability — with optional **`ROADMAP.md`** narrative fixes only where drift exists.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-49-10 | Tampering | **AUD-08** marked complete without passing verification | high | Task 1 aborts **49-02** unless **`status: passed`** |
| T-49-11 | Repudiation | C-1 rows disagree with canonical inventories | medium | Task 2 reads inventory tables as source of truth; IDs must match |
| T-49-12 | Information disclosure | Accidental paste of secrets while editing Phase 9 docs | low | Task 3 grep for bearer/client_secret patterns |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Pre-flight — 45-VERIFICATION gate</name>
  <files>.planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md
    - .planning/phases/49-phase-45-verification-aud08-c1/49-CONTEXT.md
  </read_first>
  <action>
    If **`45-VERIFICATION.md`** YAML does **not** contain **`status: passed`**, **stop** — do not edit **`REQUIREMENTS.md`**, **`09-VERIFICATION.md`**, or **`09-03-SUMMARY.md`**. Return to **49-01** Task 3 until the merge gate is green.
  </action>
  <acceptance_criteria>
    - `grep -E "^status: passed" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0 before any other task in this plan modifies tracked files.
  </acceptance_criteria>
  <verify>
    `head -n 8 .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 2: Rebuild 09-VERIFICATION.md C-1 (three exhaustive subsections)</name>
  <files>.planning/phases/09-audit-logging/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-VERIFICATION.md
    - docs/audit-semantics.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md
    - .planning/phases/49-phase-45-verification-aud08-c1/49-CONTEXT.md
  </read_first>
  <action>
    Under **`## C-1`** in **`.planning/phases/09-audit-logging/09-VERIFICATION.md`**:

    1. Keep the existing **Scope** / **Intentional hybrid** / **Oban / session store** explanatory prose if still accurate; update only where inventories contradict.

    2. **Remove** the old subsection whose heading contains **`Matrix (representative`** and any table that claims “representative” coverage without listing excluded populations.

    3. Add **`### C-1 completeness preamble`** containing:
       - Total **AUD-04** row counts copied from mechanical counts on the three inventory files (**19** rows in **`43-AUD-04-INVENTORY.md`**, **30** in **`44-AUD-04-INVENTORY.md`**, **12** in **`45-AUD-04-INVENTORY.md`** — re-count with `rg '^\| AUD-04-'` at execution time in case of drift).
       - One documented mechanical check: e.g. `rg '^\| AUD-04-' .planning/phases/09-audit-logging/09-VERIFICATION.md | wc -l` must be **≥ 61** after edit (same total as inventories), **or** explain any intentional delta with explicit excluded IDs + owner (should be **empty** for default closure).

    4. Add **`### C-1 — Phase 43 inventory`**: markdown table columns **`AUD-04-id | mechanism | tier | verdict | evidence pointer`** — **one data row per inventory row** from **`43-AUD-04-INVENTORY.md`** main **AUD-04** table (IDs **AUD-04-001** … through the auth slice). **Evidence pointer** is a concrete test path (e.g. **`test/sigra/auth/register_audit_atomicity_test.exs`**) or **`43-VERIFICATION.md`** / plan summary path — never vague “see tests”.

    5. Add **`### C-1 — Phase 44 inventory`**: same table shape — **one data row per** **`44-AUD-04-INVENTORY.md`** **AUD-04** row (**AUD-04-020** … **AUD-04-049** range).

    6. Add **`### C-1 — Phase 45 inventory`**: same table shape — **one data row per** **`45-AUD-04-INVENTORY.md`** **AUD-04** row (**AUD-04-050+**).

    Preserve relative links to **`docs/audit-semantics.md`** and inventory paths that already work from **`09-VERIFICATION.md`**.
  </action>
  <acceptance_criteria>
    - `grep -F "### C-1 — Phase 43 inventory" .planning/phases/09-audit-logging/09-VERIFICATION.md` exits 0.
    - `grep -F "### C-1 — Phase 44 inventory" .planning/phases/09-audit-logging/09-VERIFICATION.md` exits 0.
    - `grep -F "### C-1 — Phase 45 inventory" .planning/phases/09-audit-logging/09-VERIFICATION.md` exits 0.
    - `grep -F "Matrix (representative" .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **1**.
    - `rg -c '^\| AUD-04-[0-9]+' .planning/phases/09-audit-logging/09-VERIFICATION.md` prints an integer **≥ 61** (re-run `rg -c '^\| AUD-04-'` on each inventory; the **sum** is the required minimum for **`09-VERIFICATION.md`** unless Task 2 action documents explicit exclusions).
    - `grep -iE "client_secret|Bearer [A-Za-z0-9_-]{20,}" .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **1**.
  </acceptance_criteria>
  <verify>
    `wc -l .planning/phases/09-audit-logging/09-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: Align 09-03-SUMMARY.md with C-1 + inventories</name>
  <files>.planning/phases/09-audit-logging/09-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-03-SUMMARY.md
    - .planning/phases/09-audit-logging/09-VERIFICATION.md
  </read_first>
  <action>
    Update **`09-03-SUMMARY.md`** so the **Trust model** / **Inventory pointers** section does not contradict the new **C-1** exhaustive matrices (e.g. remove any language implying “representative only” global matrix). Add a one-line pointer: **C-1 row-level matrices** live under **`09-VERIFICATION.md`** with per-phase inventory anchors.
  </action>
  <acceptance_criteria>
    - `grep -i "09-VERIFICATION" .planning/phases/09-audit-logging/09-03-SUMMARY.md` exits 0.
    - `grep -i "representative" .planning/phases/09-audit-logging/09-03-SUMMARY.md` exits **1** **or** the word appears only inside a sentence explicitly negating representative-only coverage (e.g. “not representative-only”); if the latter, the sentence must also reference exhaustive C-1.
  </acceptance_criteria>
  <verify>
    `head -n 25 .planning/phases/09-audit-logging/09-03-SUMMARY.md`
  </verify>
</task>

<task type="execute">
  <name>Task 4: Flip AUD-08 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md
  </read_first>
  <action>
    Set the **AUD-08** checklist item to **`[x]`** only now that **`45-VERIFICATION.md`** is **`status: passed`**.

    In the **Traceability** table, set **AUD-08** **Status** to **`Complete`** with date **`2026-04-21`** (or the date matching **`45-VERIFICATION.md`** **`verified:`** field if different).

    Ensure **Coverage** counts at the bottom remain arithmetically consistent with the edited rows (update **v1.4 requirements** totals if the template requires it).
  </action>
  <acceptance_criteria>
    - `grep -F "[x] **AUD-08**" .planning/REQUIREMENTS.md` exits 0 (checkbox line must read **`[x]`** immediately before **` **AUD-08**`**).
    - `grep -F "AUD-08" .planning/REQUIREMENTS.md | grep -i "Complete"` exits 0.
    - `grep -F "Pending" .planning/REQUIREMENTS.md | grep "AUD-08"` exits **1** (no **Pending** left on the **AUD-08** traceability line).
  </acceptance_criteria>
  <verify>
    `grep -n "AUD-08" .planning/REQUIREMENTS.md | head`
  </verify>
</task>

<task type="execute">
  <name>Task 5: ROADMAP touch-up (only if drift)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/49-phase-45-verification-aud08-c1/49-CONTEXT.md
  </read_first>
  <action>
    If rows **45**, **49**, or **50** contradict **`49-CONTEXT.md`** or the new artifacts (e.g. imply **AUD-08** still open, or assign Nyquist **41–44** to phase **49**), apply minimal prose edits. If already aligned, make **no** edit and record “no change” in the wave summary.
  </action>
  <acceptance_criteria>
    - `grep -n "^| \\*\\*49\\*\\*" .planning/ROADMAP.md` shows criterion text still referencing **`45-VERIFICATION.md`**, **C-1**, and **REQUIREMENTS** — or unchanged file if no drift.
    - `grep -i "phase 50" .planning/ROADMAP.md` exits 0 (Nyquist batch ownership remains visible somewhere in the roadmap table or notes for **49**/**50** context).
  </acceptance_criteria>
  <verify>
    `grep "49" .planning/ROADMAP.md | head -n 5`
  </verify>
</task>

</tasks>

<verification>
1. Task 1 gate passes before REQ / C-1 edits.
2. C-1 acceptance counts and heading greps pass.
3. **AUD-08** traceability shows **Complete** with no stale **Pending** on that line.
</verification>

<success_criteria>
- **Phase 9** narrative matches **row-level** inventory truth across **43** / **44** / **45**.
- **AUD-08** bookkeeping reflects **`45-VERIFICATION.md`** merge gate reality per **D-49-04**.
</success_criteria>
