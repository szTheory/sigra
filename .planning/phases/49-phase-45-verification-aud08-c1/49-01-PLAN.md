---
phase: 49
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md
autonomous: true
requirements:
  - AUD-08
must_haves:
  truths:
    - "`mix ci.audit_45` exists in `mix.exs` and lists the same scoped test paths documented in **`45-06-SUMMARY.md`** (OAuth subtree + ops/worker/lockout/impersonation/suspicious-login + MFA/API/account proofs)."
    - "`45-VERIFICATION.md` mirrors **`43-VERIFICATION.md`** / **`44-VERIFICATION.md`**: YAML frontmatter, **Must-haves**, **Merge gate** citing **`mix ci.audit_45`**, **Automated checks run**, **Notes** with SHA + **phase 50** Nyquist deferral."
    - "No `status: passed` until merge gate exits 0 on the recorded tree."
  artifacts:
    - path: ".planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md"
      provides: "Dated AUD-08 verification snapshot for phase 45"
nyquist_compliant: false
---

<objective>
Add **`mix ci.audit_45`** to **`mix.exs`**, publish **`.planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md`** as the frozen AUD-08 closure document, and run the merge gate with Postgres so frontmatter can read **`status: passed`** with honest **PASS** lines and test counts.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-49-01 | Tampering | `45-VERIFICATION.md` claims PASS without running tests | high | Task 3 runs commands and records exit codes + counts before `status: passed` |
| T-49-02 | Repudiation | Merge gate omits OAuth or worker paths from **45-06** set | medium | Task 1 lists exact paths; Task 2 cross-checks against **`45-06-SUMMARY.md`** |
| T-49-03 | Elevation | Vacuous `mix test` (zero files) exits 0 | medium | Task 1 uses non-empty path list; Task 3 acceptance requires substring `tests,` with `0 failures` in **Automated checks run** |
| T-49-04 | Information disclosure | Secrets pasted into verification | low | Task 2 forbids `client_secret` / long `Bearer` patterns |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add mix ci.audit_45 alias</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - .planning/phases/49-phase-45-verification-aud08-c1/49-CONTEXT.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-06-SUMMARY.md
  </read_first>
  <action>
    In **`Mix.Project.project/0`**, add **`aliases: aliases()`** to the keyword list (alongside **`deps: deps()`**).

    Add **`defp aliases/0`** returning (nested form so **`mix ci.audit_45`** is registered):

    ```elixir
    defp aliases do
      [
        ci: [
          audit_45: [
            "test",
            "test/sigra/oauth/",
            "test/sigra/workers/account_deletion_test.exs",
            "test/sigra/account/deletion_test.exs",
            "test/sigra/account_audit_atomicity_test.exs",
            "test/sigra/auth/login_and_lockout_audit_atomicity_test.exs",
            "test/sigra/impersonation_test.exs",
            "test/sigra/suspicious_login_test.exs",
            "test/sigra/lockout_test.exs",
            "test/sigra/mfa_audit_atomicity_test.exs",
            "test/sigra/api_token_audit_atomic_test.exs"
          ]
        ]
      ]
    end
    ```

    Place **`defp aliases/0`** after **`defp deps/0`** (or before **`end`** of the module) following existing **`defp`** style. If **`ci:`** collides with an existing top-level alias key, nest under a unique namespace per **`49-CONTEXT.md`** (e.g. **`sigra_ci:`**) and document the rename in **`49-DISCUSSION-LOG.md`**; update **`45-VERIFICATION.md`** merge gate text to match the resulting **`mix …`** invocation.
  </action>
  <acceptance_criteria>
    - `grep -q "audit_45" mix.exs` exits 0.
    - `grep -q "ci:" mix.exs` exits 0 **or** the documented rename namespace appears in **`mix.exs`** and **`45-VERIFICATION.md`** merge gate lines match it.
    - `grep -q "test/sigra/oauth/" mix.exs` exits 0.
    - `grep -q "account_deletion_test.exs" mix.exs` exits 0.
    - `grep -q "aliases: aliases()" mix.exs` exits 0.
    - `MIX_ENV=test mix compile` exits 0 from repo root after the edit.
  </acceptance_criteria>
  <verify>
    `mix format --check-formatted mix.exs` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Create 45-VERIFICATION.md (draft → pass path)</name>
  <files>.planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-01-SUMMARY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-02-SUMMARY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-03-SUMMARY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-04-SUMMARY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-05-SUMMARY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-06-SUMMARY.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-VALIDATION.md
  </read_first>
  <action>
    Create **`.planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md`** modeled on **`43-VERIFICATION.md`** / **`44-VERIFICATION.md`**:

    YAML frontmatter (start **`status: draft`** until Task 3 passes; then **`status: passed`**): `phase: "45"`, `verified:` ISO date when passed.

    Sections:
    1. **## Must-haves (AUD-08 — OAuth + ops + workers)** — table **Item | Evidence** citing **`45-AUD-04-INVENTORY.md`** row ranges (e.g. **AUD-04-050+**), **`test/sigra/oauth/`** atomicity coverage (**`oauth_audit_atomicity_test.exs`** etc. as appropriate), **`45-02-SUMMARY.md`** through **`45-06-SUMMARY.md`** for closure narratives, and **`45-VALIDATION.md`** for the living-map cross-reference.
    2. **## Merge gate** — two verbatim lines for humans:
       - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile`
       - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.audit_45` (or the renamed alias from Task 1)
    3. **## Release attestation (optional)** — full root `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test` — state **Not run in this closure** unless actually executed.
    4. **## Automated checks run** — leave placeholders until Task 3 fills **PASS/FAIL**, exit codes, and **`N tests, 0 failures`** from real output.
    5. **## Notes** — paragraph: Nyquist batch **41–44** remains **phase 50**; include **`git rev-parse HEAD`** shell output when finalized.

    Do not paste OAuth client secrets or long bearer tokens.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0.
    - `grep -E "^---$" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md | wc -l | grep -qE "^[[:space:]]*2"` exits 0.
    - `grep -F "mix ci.audit_45" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0 **or** matches the renamed alias string present in **`mix.exs`**.
    - `grep -F "45-AUD-04-INVENTORY.md" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0.
    - `grep -i "AUD-08" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0.
    - `grep -i "phase 50" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0.
    - `grep -iE "client_secret|Bearer [A-Za-z0-9_-]{20,}" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits **1**.
  </acceptance_criteria>
  <verify>
    `head -n 16 .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: Run merge gate and finalize 45-VERIFICATION.md</name>
  <files>.planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md
    - mix.exs
  </read_first>
  <action>
    With Postgres at **`localhost:5432`** with credentials **`postgres`/`postgres`** per **`CLAUDE.md`**, run:

    1. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile`
    2. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.audit_45` (use renamed task if Task 1 renamed it)

    If both exit **0**: set YAML **`status: passed`**, set **`verified:`** to today’s ISO date; in **## Automated checks run**, record **PASS (exit 0)** for each command with the literal lines above; copy the **`N tests, 0 failures`** fragment from the **`mix ci.audit_45`** stdout into that section.

    If either fails: keep **`status: draft`**, document **FAIL** with the first stderr/stdout error line — **do not** run plan **49-02**.
  </action>
  <acceptance_criteria>
    - If **`status: passed`**: `grep -E "^status: passed" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0.
    - If **`status: passed`**: `grep -F "0 failures" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0.
    - If **`status: passed`**: `grep -E "^verified: 20" .planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md` exits 0.
    - If **`status: draft`** after failure: body contains substring **`FAIL`** (honest failure path).
  </acceptance_criteria>
  <verify>
    When **`status: passed`**, re-run Task 3 step 2; must exit 0.
  </verify>
</task>

</tasks>

<verification>
1. Task 1–3 acceptance greps pass (or honest **`draft`** / **`FAIL`** path documented).
2. When **`status: passed`**, `mix ci.audit_45` with **`PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test`** exits 0.
3. No secret patterns in **`45-VERIFICATION.md`** per Task 2.
</verification>

<success_criteria>
- **`45-VERIFICATION.md`** is the authoritative dated AUD-08 evidence bundle for phase **45** work, aligned with **`49-CONTEXT.md`** **D-49-01** / **D-49-03**.
- Merge gate is **`mix compile`** + **`mix ci.audit_45`** with Postgres env vars documented verbatim.
</success_criteria>
