---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .formatter.exs
  - mix.exs
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
  - CONTRIBUTING.md
autonomous: true
requirements: [DX-01]

must_haves:
  truths:
    - "D-01: mix ci is the sole contributor-facing library-gate command and the existing library_tests_dep_off PR lane invokes MIX_ENV=test mix ci directly."
    - "D-02: the alias includes format --check-formatted, deps.get --check-locked, deps.unlock --check-unused, compile --warnings-as-errors, test, ci.install_golden, and sigra.dep_off."
    - "D-03: the Phase 198 contract requires the seven legs and direct workflow call while rejecting Credo, Dialyzer, and mix_audit."
    - "D-04: formatter inputs exclude test/fixtures/install_golden/tree/** before any tree-wide cleanup."
    - "The byte-stable Library tests aggregator, shard jobs, required context, and ci-gate dependency remain intact."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-01 unclassified edge: source artifacts define no additional boundary taxonomy; the plan assumes parity means the locked seven alias legs plus an observed direct CI invocation, and proves both rather than inventing a fallback."
  prohibitions: []
  artifacts:
    - path: test/sigra/planning/phase_198_contributor_dx_contract_test.exs
      provides: "DX-01 alias-to-workflow parity contract"
    - path: mix.exs
      provides: "seven-leg mix ci contributor CLI"
  key_links:
    - from: .github/workflows/ci.yml
      to: mix.exs
      via: "library_tests_dep_off executes MIX_ENV=test mix ci"
      pattern: "MIX_ENV=test mix ci"
    - from: .formatter.exs
      to: test/fixtures/install_golden/tree
      via: "formatter input exclusion keeps generated bytes outside formatter ownership"
---

<objective>
Prove the contributor command reaches the real PR gate through one production-quality tracer path (D-01–D-04).

Purpose: stop local and CI library-gate definitions from drifting independently.
Output: the seven-leg alias, golden-safe formatter boundary, direct existing-lane call-through, fail-closed contract, and contributor instructions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-RESEARCH.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PATTERNS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire one local-to-PR alias path and make drift fail closed (D-01, D-02, D-03, D-04)</name>
  <files>.formatter.exs, mix.exs, .github/workflows/ci.yml, test/sigra/planning/phase_198_contributor_dx_contract_test.exs</files>
  <read_first>
    - `.formatter.exs` — current broad fixture input and golden-tree ownership hazard.
    - `mix.exs` aliases/0 — current four-leg `ci`, `ci.install_golden`, and `sigra.dep_off` contracts.
    - `.github/workflows/ci.yml` — `library_tests_shard`, `library_tests_scaffold`, byte-stable `library_tests`, `library_tests_dep_off`, and `ci-gate` jobs.
    - `test/sigra/planning/phase_198_contributor_dx_contract_test.exs` — stale no-format assertion to replace, not duplicate.
    - `test/sigra/planning/phase_233_library_economics_contract_test.exs` — anchored `job_body/2` extraction and protected aggregation assertions.
    - `.planning/phases/233-library-suite-economics/233-06-SUMMARY.md` — live-universe and selector fail-closed obligations that must remain intact.
    - Every file listed in `<files>` — establish current contents before editing.
  </read_first>
  <behavior>
    - "The Phase 198 contract fails before the alias/CI edits and passes after them."
    - "The selected job contains one literal MIX_ENV=test mix ci invocation and no expanded dep-off command sequence."
    - "The Library tests name, aggregator needs, shard commands, scaffold receiver, and ci-gate link remain byte/behavior stable."
  </behavior>
  <action>
    First revise the Phase 198 tests to require the ordered seven-leg alias, the golden-tree formatter exclusion, and a fail-closed extraction of `library_tests_dep_off` containing exactly one `MIX_ENV=test mix ci` call. Retain negative assertions for the three out-of-scope gate families per D-03 and assert the protected `library_tests` name/needs plus the Phase 233 shard/scaffold commands remain present. Run the focused test and record RED.

    In `.formatter.exs`, replace the fixture-inclusive input with explicit non-golden fixture coverage so `test/fixtures/install_golden/tree/**` is not selected per D-04 while other intended test inputs remain selected. In `mix.exs`, prepend the three locked native checks to `ci` without changing the existing four coverage legs per D-02.

    In `.github/workflows/ci.yml`, keep `library_tests_dep_off` as the single non-sharded, non-ruleset call-through host: retain checkout, beam/cache, Hex/Rebar, archive, locked dependency fetch prerequisites, Postgres service, docs check, docs-only gate, timeout, and ci-gate membership; replace its separately expanded Threadline removal/compile/test sequence with one `MIX_ENV=test mix ci` step. Do not edit the `Library tests` job id/name, its needs, either Phase 233 receiver, required contexts, or skip semantics. Workflow expressions must stay in `env:`/`if:` fields rather than interpolated inside mutable shell text.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs &amp;&amp; mix format --check-formatted .formatter.exs mix.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The focused test is observed RED before implementation and GREEN after implementation.
    - The alias contains each locked leg exactly once and no Credo, Dialyzer, or mix_audit invocation.
    - The workflow contract resolves `library_tests_dep_off`, finds one literal direct alias invocation, and confirms its expanded dep-off sequence is gone.
    - The golden-tree path is not selected by formatter inputs, while `test/sigra/**` and intended non-golden fixtures remain selected.
    - `library_tests` remains named `Library tests`, depends on `library_tests_shard` and `library_tests_scaffold`, and still fails unless both succeed.
  </acceptance_criteria>
  <done>A focused automated contract proves contributor command → existing PR lane → protected aggregate wiring without golden ownership or topology drift.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the exact contributor reproduction contract (D-01, D-02, D-03, D-12)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - `CONTRIBUTING.md` — existing local gate, Postgres/archive prerequisites, optional tools, and Playwright reproduction guidance.
    - `mix.exs` — final ordered alias legs and exact archive prerequisite.
    - `.github/workflows/ci.yml` — selected alias call-through job and preserved topology.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md` — D-01/D-02/D-03/D-12 scope boundaries.
    - Every file listed in `<files>` — establish current content before editing.
  </read_first>
  <action>
    Update the existing `mix ci` section to list the seven ordered legs and state that `library_tests_dep_off` invokes the same alias while `Library tests` remains the protected aggregation. Preserve the Postgres and phx_new 1.8.8 prerequisites. Remove formatting from the non-gating list; keep Credo and Dialyzer explicitly optional and omit mix_audit from the gate per D-03. State that retries are not part of local parity per D-12 and point to the existing deterministic Playwright seam without inventing UI work.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING names all seven alias legs, Postgres setup, phx_new 1.8.8, and the direct CI host.
    - Documentation does not claim Credo, Dialyzer, mix_audit, retries, example-app Playwright, or release workflows are inside `mix ci`.
    - The Phase 198 test checks the contributor text and passes.
  </acceptance_criteria>
  <done>A contributor has one exact, truthful command and prerequisites matching the CI call-through.</done>
</task>

</tasks>

## Artifacts this phase produces

- New paths: none.
- Modified contract: `mix.exs` exposes the seven-leg `mix ci` alias.
- Modified contract: `phase_198_contributor_dx_contract_test.exs` enforces alias order, formatter exclusion, direct `library_tests_dep_off` invocation, and protected aggregation.
- Modified contributor surface: `CONTRIBUTING.md` documents the exact local reproduction path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| contributor shell → Mix tasks | Local command executes dependency, formatter, compile, and test operations against repository state. |
| GitHub event/workflow → shell | Managed event data enters a secrets-capable CI runner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-01-01 | Tampering | `mix ci` / `ci.yml` | high | mitigate | One direct alias invocation plus Phase 198 ordered-leg/workflow extraction prevents two independently mutable gate definitions. |
| T-234-01-02 | Tampering | `.formatter.exs` / golden tree | high | mitigate | Exclude generated golden bytes and run focused golden ownership checks before the full formatter cleanup. |
| T-234-01-03 | Elevation of privilege | workflow shell | high | mitigate | Keep GitHub expressions in fixed `env:`/`if:` mappings and use literal task commands; do not interpolate untrusted event text into shell. |
| T-234-01-04 | Denial of service | required `Library tests` context | high | mitigate | Preserve byte-stable name, shard/scaffold receivers, always-running aggregator, timeout, and ci-gate link. |
</threat_model>

<verification>
Focused Phase 198 contract passes, scoped formatter check passes, and the protected aggregation/source assertions prove the tracer without relying on a human review.
</verification>

<success_criteria>
DX-01 has an executable direct alias path, a fail-closed drift ratchet, golden-safe formatter ownership, and truthful contributor documentation per D-01–D-04/D-12.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-01-SUMMARY.md` when done.
</output>
