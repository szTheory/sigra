---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 20
type: execute
wave: 11
depends_on: [234-19]
files_modified:
  - test/sigra/planning/phase_234_evidence_contract_test.exs
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [DX-01, DX-02, DX-03, DX-04, DX-06]

must_haves:
  truths:
    - "assert_transition_allowed!/3 validates each concrete local, PR, release, Dependabot, gallery, and historical-gallery receipt before authorizing complete/true/true."
    - "A bare success map or a success-shaped receipt missing any slot-specific field is rejected through the production transition path."
    - "Every command receipt carries the reviewed commit SHA and a UTC timestamp inside one deterministic 30-minute window ending at reviewed_at; wrong-SHA and stale-time transition mutations fail."
    - "234-VALIDATION.md can remain complete/true/true only after the exact five commands and all concrete evidence validators pass at the same reviewed commit after Plan 19."
    - "The Per-Task Verification Map contains exactly one mechanically validated green row for each gap-closing task 234-19-01, 234-19-02, 234-20-01, and 234-20-02, with that task's exact deterministic automated command."
  artifacts:
    - path: test/sigra/planning/phase_234_evidence_contract_test.exs
      provides: "shared concrete evidence authorization plus revision/freshness-bound command receipt validation"
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      provides: "reviewed SHA/time, exact fresh five-command receipts, and exact green Per-Task Verification Map coverage for all four Plans 19–20 tasks"
  key_links:
    - from: assert_transition_allowed!/3
      to: local/PR/release/Dependabot/gallery/historical validators
      via: "all six concrete validators run before completion fields are accepted"
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      to: validate_command_receipts!/3
      via: "reviewed_commit_sha and reviewed_at bind every command row to one snapshot and bounded interval"
    - from: test/sigra/planning/phase_234_playwright_inventory_contract_test.exs
      to: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      via: "the exact five-command inventory re-executes the Plan 19 ownership contract before sign-off"
    - from: 234-19-PLAN.md and 234-20-PLAN.md
      to: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      via: "an exact-set parser binds task IDs 234-19-01 through 234-20-02 to their declared automated commands and green statuses before sign-off"
---

<objective>
Carry one real complete evidence set through concrete slot validation, then bind the final command receipts to the reviewed revision and a deterministic freshness window.

Purpose: prevent malformed or historical success-shaped data from authorizing Phase 234 completion.
Output: shared concrete receipt validators, wrong-shape/SHA/time mutations, and a freshly ratified validation artifact after the Playwright ownership repair.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-19-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate every concrete receipt through the production transition path</name>
  <files>test/sigra/planning/phase_234_evidence_contract_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` — current per-slot tests, private validators, inline assertions, bare-success fixture, and `assert_transition_allowed!/3`.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — the real six-slot successful receipt set to use as the green fixture.
    - `234-VERIFICATION.md` — concrete receipt bypass and required malformed-success production-path mutations.
    - `234-VALIDATION.md` — current frontmatter and exact command receipt inventory.
    - Every file listed in `<files>` — establish current behavior before editing.
  </read_first>
  <behavior>
    - "The green transition fixture uses the decoded real evidence ledger, not six fabricated status-only maps."
    - "Local, PR, release, Dependabot, gallery, and historical-gallery receipts each have a named validator that returns :ok only for the full concrete schema."
    - "assert_transition_allowed!/3 calls every named validator before accepting complete/true/true."
    - "For each slot, replacing its receipt with a bare success map or deleting one required concrete field fails through assert_transition_allowed!/3 with the slot/field named."
  </behavior>
  <action>
    Extract the existing inline PR, release, gallery, and historical-gallery assertions into named receipt validators consistent with the existing local and Dependabot validators. Preserve every current field, exact immutable value, URL/ID/hash shape, skip distinction, and sanitization assertion; do not weaken a validator while refactoring. Add one shared `validate_required_evidence!/1` that verifies the exact six-slot set and calls all six concrete validators. Make `assert_transition_allowed!/3` call this function before it considers completion frontmatter, while retaining the blocked draft behavior for explicitly failed or missing slots.

    Replace the fabricated green fixture in the transition mutation test with the decoded successful `234-EVIDENCE.json`. For every slot, add two production-path mutations: a bare `%{"status" => "success"}` map and a success receipt missing a representative mandatory field. Both must be rejected with a diagnostic naming the slot or missing field; they must never return `:complete`. Retain command inventory mutations and final-evidence tagged tests. Do not modify the evidence ledger, remote receipts, workflows, inventory, UI, or service state in this task.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff --only final_evidence</automated>
  </verify>
  <acceptance_criteria>
    - The real six-slot ledger authorizes completion only when every named validator returns `:ok`.
    - Twelve malformed-success transition mutations (bare and missing-field for each of six slots) fail with named diagnostics.
    - Existing local cleanup, immutable release, exact Dependabot tuple, gallery, historical-gallery, URL/ID/hash, and sanitization checks remain enforced.
    - Missing or explicitly failed evidence keeps draft/false/false behavior; malformed success is never treated as ordinary blocked evidence or accepted.
  </acceptance_criteria>
  <done>The same production transition that changes validation state now proves every concrete evidence receipt end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind command and task receipts to the reviewed snapshot, then ratify</name>
  <files>test/sigra/planning/phase_234_evidence_contract_test.exs, .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md</files>
  <read_first>
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` — `@command_receipt_commands`, parser, validator, green receipt helper, and transition mutation table after Task 1.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md` — frontmatter, Command Receipts table, Approval line, Validation Sign-Off checklist, and Per-Task Verification Map (which currently stops at Plan 14).
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-19-PLAN.md` — exact task IDs and `<automated>` commands for 234-19-01 and 234-19-02.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-20-PLAN.md` — exact task IDs and `<automated>` commands for 234-20-01 and 234-20-02.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-19-SUMMARY.md` — exact Plan 19 implementation commit and verification result to include in the reviewed snapshot.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — immutable service/local receipts that must remain valid.
    - `AGENTS.md` — automation-first evidence and no-waiver requirements.
    - Every file listed in `<files>` — establish current content before editing.
  </read_first>
  <behavior>
    - "Validation frontmatter has one lowercase 40-character reviewed_commit_sha and one UTC reviewed_at timestamp."
    - "Every one of the exact five command rows carries the identical reviewed commit SHA."
    - "Every command completion time is at or before reviewed_at, no more than 1,800 seconds earlier, and rows remain chronologically nondecreasing."
    - "A well-formed wrong SHA and a well-formed timestamp older than the 30-minute window both fail through assert_transition_allowed!/3."
    - "Commands that collect receipts do not execute the validation-signoff test that consumes those same receipts; sign-off runs separately after all five rows are populated."
    - "The Per-Task Verification Map has exactly one row apiece for 234-19-01, 234-19-02, 234-20-01, and 234-20-02; each row carries its task's exact automated command and an exact green status."
    - "Omitting or duplicating any one of the four Plans 19–20 task rows fails validation-signoff through the production parser."
  </behavior>
  <action>
    Add `reviewed_commit_sha` and `reviewed_at` to validation frontmatter and a `Commit SHA` column to the five-row Command Receipts table. Extend parsing so every command receipt includes its SHA. Change the shared command validator to accept the reviewed SHA/time, require exact command order/cardinality/uniqueness, require every receipt SHA to equal the reviewed SHA, parse all UTC times, require nondecreasing receipt order, and require each time to fall within the closed interval from `reviewed_at - 1,800 seconds` through `reviewed_at`. Use this artifact-relative interval rather than current wall-clock time so the committed proof remains reproducible while an old receipt cannot authorize a later reviewed snapshot.

    Pass the frontmatter review fields into the shared validator from both `parse_validation!/1` and `assert_transition_allowed!/3`. Extend the transition mutation table with a different valid 40-character SHA on one row and a valid UTC timestamp 1,801 seconds before `reviewed_at`; require named SHA and freshness failures. Also cover a receipt after `reviewed_at` and non-monotonic order. Keep the existing empty/missing/extra/duplicate/reordered/malformed/nonzero cases.

    Remove the existing self-reference in the exact five-command inventory: add `--exclude validation_signoff` to the focused multi-file command and the `test/sigra/planning/` command, because those commands produce receipt rows that the validation-signoff tests consume. Keep the final-evidence, formatter, and golden/idempotency command meanings unchanged. The post-population validation-signoff run remains mandatory but is not permitted to attest itself.

    Extend the validation Markdown parser to read the Per-Task Verification Map without collapsing duplicate task IDs. Define an exact required gap-task tuple set and require the artifact to contain exactly one row for each of these IDs with these literal `Automated Command` and `Status` values: `234-19-01` → `mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` → `✅ green`; `234-19-02` → `mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs && mix test test/sigra/planning/phase_232_playwright_economics_test.exs` → `✅ green`; `234-20-01` → `mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff --only final_evidence` → `✅ green`; and `234-20-02` → `mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff && mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only final_evidence && mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` → `✅ green`. Preserve all earlier task rows. Add exact-set mutation assertions that independently remove and duplicate each of the four required rows and prove validation-signoff rejects all eight mutations with the affected task ID named; also reject a substituted command or non-green status for each required tuple. Wire this check into `parse_validation!/1` and `assert_transition_allowed!/3`, not only a standalone table test.

    Commit the Task 1, command-inventory code, and Plan 19 changes, record that exact `HEAD` as `reviewed_commit_sha`, and run the revised exact five command inventory in declared order against that snapshot while validation remains draft. Capture sanitized output SHA-256, UTC completion, exit, and the identical reviewed SHA for each row; raw output is discarded after hashing. Set `reviewed_at` to a UTC instant at or after the last row and within the 30-minute bound. Add the four exact Plans 19–20 rows to the Per-Task Verification Map, mark them green only from their successful deterministic commands, add a checked Validation Sign-Off item naming exact coverage of 234-19-01, 234-19-02, 234-20-01, and 234-20-02, and update the Approval statement to cover those four rows explicitly. Populate task rows, five command receipts, sign-off, and approval atomically, then run validation-signoff separately. Preserve complete/true/true only if the Plan 19 Playwright contract, all six concrete evidence validators, every mutation, exact Plans 19–20 task-row set, prior task/Wave 0 rows, exact command receipts, sign-off coverage, and approval SHA/time pass together; otherwise restore draft/false/false and write durable diagnostics without waiving a slot.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff &amp;&amp; mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only final_evidence &amp;&amp; mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Validation and all five rows name one identical reviewed commit SHA containing Plans 19–20 Task 1 code.
    - All five timestamps are nondecreasing and inside the deterministic 1,800-second interval ending at reviewed_at.
    - Wrong-SHA, 1,801-second-stale, future-time, and non-monotonic transition mutations fail with named diagnostics.
    - The exact five commands are re-executed after Plan 19; only their sanitized hashes, status, timestamps, and reviewed SHA are committed.
    - The two receipt-producing test commands exclude `validation_signoff`; the separate post-population validation-signoff run passes, so no command receipt attests itself.
    - The Per-Task Verification Map contains exactly one row each for `234-19-01`, `234-19-02`, `234-20-01`, and `234-20-02`; their Automated Command cells exactly equal the four literal task commands specified in this action and their Status cells exactly equal `✅ green`.
    - Eight exact-set mutations—one omission and one duplicate for each Plans 19–20 task ID—fail through both validation parsing and transition authorization with the affected ID named; substituted-command and non-green-status mutations also fail.
    - Validation Sign-Off contains a checked item explicitly covering all four Plans 19–20 task IDs, and Approval explicitly attests their exact-row, exact-command, green-status coverage.
    - Completion fields remain complete/true/true only when the concrete evidence set and fresh reviewed-revision command set both pass; otherwise they are draft/false/false with durable diagnostics.
  </acceptance_criteria>
  <done>Phase 234 is ratified only from concrete evidence, exact green verification rows for all four gap-closing tasks, and commands executed for one identified, time-bounded reviewed snapshot.</done>
</task>

</tasks>

## Artifacts this phase produces

- `test/sigra/planning/phase_234_evidence_contract_test.exs` — introduces named PR, release, gallery, historical-gallery, and aggregate evidence validators; wires them into `assert_transition_allowed!/3`; extends command receipts with commit SHA and deterministic freshness enforcement; parses exact Plans 19–20 task verification tuples without duplicate collapse; and adds malformed-success, wrong-SHA, stale/future-time, non-monotonic, missing-task-row, duplicate-task-row, substituted-command, and non-green-status mutations.
- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md` — adds `reviewed_commit_sha`, `reviewed_at`, per-command `Commit SHA` values, exactly one green command-bound row for each of 234-19-01, 234-19-02, 234-20-01, and 234-20-02, plus explicit sign-off/approval coverage; then replaces the five receipts with executions against the gap-closed reviewed snapshot; no new path is created.

## Multi-Source Coverage Audit

| Source | ID | Gap item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Gate/action/dependency/spec state cannot drift behind historical or malformed proof | 19–20 | COVERED | Exact Playwright invocation plus concrete revision-bound authorization. |
| REQ | DX-01 | Reproducible local gate | 20 | COVERED | Concrete local receipt and reviewed command executions gate transition. |
| REQ | DX-02 | Immutable release-critical Actions | 20 | COVERED | Full immutable release receipt validator is invoked by authorization. |
| REQ | DX-03 | Dependabot covers three ecosystems | 20 | COVERED | Exact processed-job validator is invoked by authorization. |
| REQ | DX-04 | Every Playwright spec has an executable owner | 19, 20 | COVERED | Exact invocation contract is re-run in the reviewed command set. |
| REQ | DX-06 | SEED-006 delivered or residual filed | 20 | COVERED | Current and historical gallery receipts are concretely validated. |
| RESEARCH | — | Structural proof separated from managed-service receipts | 20 | COVERED | Each receipt keeps its own concrete schema; command proof is independently SHA/time-bound. |
| RESEARCH | — | Fail-closed exact-set/non-vacuity contracts | 19, 20 | COVERED | Marker swaps, malformed success, wrong revision, stale time, and missing/duplicate gap-task rows all fail. |
| CONTEXT | D-01–D-07 | Local/action/Dependabot contracts | 20 | COVERED | Existing implementations are not reopened; their evidence is made mandatory. |
| CONTEXT | D-08–D-10 | Inventory/orphans/gallery closure | 19, 20 | COVERED | Exact spec invocation and concrete gallery evidence gate final state. |
| CONTEXT | D-11–D-12 | Exclude adjacent todos and retry machinery | 19, 20 | COVERED | No deferred policy or retry behavior enters either plan. |

No deferred idea is planned. No package install, external API, schema, UI, snapshot, required-check, retry, canary, or release-note change is introduced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| evidence JSON → completion transition | Repository-controlled success-shaped maps can be malformed, partial, or selectively edited. |
| command output → validation receipt | A green command may belong to a different commit or an earlier review interval. |
| validation Markdown → authorization | Frontmatter and table fields jointly control the complete/true/true claim. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-20-01 | Spoofing | evidence slots | high | mitigate | Named full-schema validators and exact six-slot aggregation reject status-only impersonation. |
| T-234-20-02 | Tampering | command receipt SHA | high | mitigate | Every row must equal one 40-character reviewed commit SHA; wrong-SHA mutation runs through authorization. |
| T-234-20-03 | Repudiation | command receipt time | high | mitigate | UTC parsing, monotonic order, and a closed 1,800-second artifact-relative window bind commands to reviewed_at. |
| T-234-20-04 | Information disclosure | command/service output | high | mitigate | Persist only sanitized hashes and public immutable identifiers; raw logs, tokens, cookies, and environment values remain forbidden. |
| T-234-20-05 | Denial of service | final proof suite | medium | mitigate | Five fixed commands and focused tags are bounded, sequential, and use existing repository timeouts; deterministic failures block with diagnostics. |

## ASVS L1 Automated Controls

- V5.1 input validation: all evidence, SHA, timestamp, URL/ID, and receipt-table fields are parsed and mutation-tested before state transition.
- V7.1 evidence integrity: concrete receipt provenance plus reviewed SHA/time bind the completion claim to one reproducible snapshot.
</threat_model>

<verification>
The validation-signoff, final-evidence, and Playwright inventory contracts pass against persisted artifacts; malformed success, wrong SHA, stale/future time, non-monotonic order, swapped-marker, and missing/duplicate/substituted Plans 19–20 task-row mutations remain demonstrably red.
</verification>

<success_criteria>
All three verifier gaps are closed and Nyquist coverage is current: exact Playwright invocation is enforced, every concrete evidence slot is validated through the transition, every final command receipt belongs to one reviewed revision inside a deterministic freshness interval, and all four gap-closing tasks have exact green command-bound verification rows.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-20-SUMMARY.md` when done.
</output>
