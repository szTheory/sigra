---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release-please.yml
  - test/sigra/planning/phase_234_action_pinning_contract_test.exs
autonomous: true
requirements: [DX-02]

must_haves:
  truths:
    - "D-05: Release Please resolves to googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 with trailing # v5.0.0."
    - "D-06: every third-party uses reference in the enumerated secrets-bearing/release-critical workflow set is a lowercase 40-character commit SHA with a same-line version comment."
    - "The annotated v5 tag-object SHA 0dfd8538845b8e92600d271a895a5372865d4062 is rejected."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-02 unclassified edge: the source artifacts do not define nested reusable-workflow/action ownership beyond release-critical workflows; the plan uses an explicit scoped workflow universe and fails on missing/stale members rather than silently widening or narrowing it."
  prohibitions: []
  artifacts:
    - path: test/sigra/planning/phase_234_action_pinning_contract_test.exs
      provides: "release-critical third-party action inventory and immutable-pin policy"
  key_links:
    - from: .github/workflows/release-please.yml
      to: test/sigra/planning/phase_234_action_pinning_contract_test.exs
      via: "scoped uses-line enumeration"
      pattern: "[0-9a-f]{40} # v"
---

<objective>
Make the secrets-bearing release action immutable and install a fail-closed policy against future mutable-ref drift (D-05, D-06).

Purpose: reviewed release code must not change underneath a mutable tag.
Output: exact dereferenced Release Please pin and a scoped action-inventory contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-RESEARCH.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin Release Please to the locked dereferenced commit (D-05)</name>
  <files>.github/workflows/release-please.yml, test/sigra/planning/phase_234_action_pinning_contract_test.exs</files>
  <read_first>
    - `.github/workflows/release-please.yml` — permissions, secret-bearing token path, checkout convention, and mutable Release Please line.
    - `.github/workflows/publish-hex.yml` and `.github/workflows/release-lane-rot.yml` if present — adjacent release-critical action-pin conventions and scope.
    - `test/sigra/planning/phase_230_ci_timeouts_test.exs` — source-contract failure diagnostics.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md` — exact D-05 commit/tag-object distinction.
    - Every file listed in `<files>` — current contents/absence before editing.
  </read_first>
  <behavior>
    - "The locked dereferenced SHA plus v5.0.0 comment passes."
    - "The annotated tag-object SHA, v5 tag, short SHA, missing comment, or mismatched version comment fails with workflow/line diagnostics."
  </behavior>
  <action>
    Create the focused async ExUnit contract first and observe RED. Define the explicit release-critical workflow universe from the repository's secrets/contents-write/publish paths and assert every member exists. Extract third-party `uses:` lines while excluding repository-local `./` actions; require exactly 40 lowercase hex characters and a non-empty same-line semantic-version comment. Add exact assertions for the Release Please repository, locked commit, and `v5.0.0` comment, and reject the annotated object SHA.

    Replace only `googleapis/release-please-action@v5` with `googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5.0.0`. Do not alter permissions, token expressions, release conditions, release-note behavior, or the deferred orphan-unreleased policy.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_action_pinning_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The test is observed RED before the workflow pin and GREEN after it.
    - Release Please uses the exact locked commit and comment; the annotated object SHA and tag ref are absent.
    - Every scoped third-party action is inventoried, immutable, and commented; local actions are deliberately excluded.
    - Release permissions, secret/token flow, conditions, and outputs are unchanged.
  </acceptance_criteria>
  <done>The live release path uses the reviewed dereferenced commit and future mutable action drift fails the fast contract.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the action policy is non-vacuous and workflow-valid (D-06)</name>
  <files>test/sigra/planning/phase_234_action_pinning_contract_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_234_action_pinning_contract_test.exs` — Task 1 universe/extractor.
    - `.github/workflows/release-please.yml` — positive subject.
    - `test/sigra/planning/phase_232_playwright_economics_test.exs` — non-vacuous count and source mutation fixture patterns.
    - Every file listed in `<files>` — current content before editing.
  </read_first>
  <action>
    Add hermetic mutation cases that feed the extractor synthetic release workflow bodies containing a floating tag, short SHA, uppercase SHA, absent version comment, annotated-object SHA, and repository-local action. Assert every invalid third-party case fails with the action/workflow named while the local action is accepted as out of third-party scope. Assert the production inventory count is positive so an empty parser cannot green.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_action_pinning_contract_test.exs &amp;&amp; git diff --check -- .github/workflows/release-please.yml test/sigra/planning/phase_234_action_pinning_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Each invalid synthetic ref fails for its intended reason and the production inventory is non-empty.
    - The test does not execute workflows, access secrets, install a YAML parser, or interpolate workflow context into shell.
  </acceptance_criteria>
  <done>The pin policy has demonstrated teeth rather than a vacuous source scan.</done>
</task>

</tasks>

## Artifacts this phase produces

- New path: `test/sigra/planning/phase_234_action_pinning_contract_test.exs`.
- New contract: explicit release-critical workflow universe and third-party `uses:` validator requiring `owner/repo@<40 lowercase hex> # <version>`.
- Modified release contract: exact `release-please-action` dereferenced v5.0.0 pin.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| third-party action source → secrets-bearing release runner | External action code receives release permissions/token access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-03-01 | Tampering | release-please action ref | high | mitigate | Locked dereferenced 40-character commit and explicit rejection of tag/tag-object alternatives. |
| T-234-03-02 | Elevation of privilege | release workflow permissions/secrets | high | mitigate | Change only the ref; preserve permission/token boundaries and inventory every release-critical third-party action. |
| T-234-03-03 | Repudiation | version provenance | medium | mitigate | Require same-line version comment and non-vacuous mutation-tested contract. |
</threat_model>

<verification>
Focused contract passes all production and negative mutation cases; the release workflow diff is limited to the pin.
</verification>

<success_criteria>
DX-02 and D-05/D-06 are structurally complete: immutable exact pin today and a fail-closed ratchet for future release-critical action drift.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-06-SUMMARY.md` when done.
</output>
