---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 21
type: execute
wave: 12
depends_on: [234-20]
files_modified:
  - test/sigra/planning/phase_234_evidence_contract_test.exs
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [DX-01, DX-02, DX-03, DX-04, DX-06]

must_haves:
  truths:
    - "The production completion transition authorizes the real six-slot Phase 234 evidence ledger only when its key set equals the required local_mix_ci, pr_ci, release, dependabot, gallery, and historical_gallery set."
    - "Adding any seventh evidence slot rejects complete/true/true even when all six required receipts remain valid."
    - "Both an extra failed receipt and an extra malformed success-shaped receipt are rejected through assert_transition_allowed!/3, not merely through an isolated helper assertion."
    - "All already-green required-slot validators, command-receipt checks, and final-evidence behaviors remain unchanged and deterministic."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-01 edge: the recorded clean-checkout receipt and exact local commands remain the bounded reproducibility proof; this gap plan changes only aggregate evidence-set closure."
    - "FLAGGED/UNRESOLVED DX-02 edge: the existing successful post-pin main job remains authoritative only while its immutable pin, merge SHA, run/job IDs, permissions, and hashes validate."
    - "FLAGGED/UNRESOLVED DX-03 edge: only the three authenticated processed-job rows already ratified by Phase 234 count as service proof."
    - "FLAGGED/UNRESOLVED DX-04 concurrency edge: one commit snapshot is validated atomically; an interrupted or parallel producer that adds a seventh slot must fail the exact-set transition."
    - "FLAGGED/UNRESOLVED DX-06 edge: the existing current and historical gallery receipts are consumed as recorded; contradictory evidence blocks rather than reopening UI remediation."
  prohibitions:
    - statement: "MUST NOT authorize complete/true/true when the evidence map contains any unvalidated slot outside the exact required six-slot contract."
      reason: "Bespoke evidence-honesty constraint kept by the spec-less fallback probe; descriptor-less and flagged-unverified."
  artifacts:
    - path: test/sigra/planning/phase_234_evidence_contract_test.exs
      provides: "exact required-evidence key-set equality plus production-transition mutations for extra failed and malformed success-shaped slots"
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
      provides: "Wave 12 Nyquist row, refreshed reviewed-snapshot receipts, and explicit 234-21-01 sign-off/approval coverage"
  key_links:
    - from: assert_transition_allowed!/3
      to: validate_required_evidence!/1
      via: "the all-required-slots-green completion branch invokes exact key-set validation before returning :complete"
    - from: Map.keys(receipts)
      to: required six-slot MapSet
      via: "MapSet equality rejects missing and additional evidence keys symmetrically"
    - from: production-transition mutation test
      to: assert_transition_allowed!/3
      via: "the real green ledger is extended with one failed slot and one malformed success-shaped slot, and both complete transitions must raise"
    - from: 234-21-01 Per-Task Verification Map row
      to: T-234-21-01 and the post-execution validation sign-off
      via: "the Wave 12 row records the exact composite automated command and remains green only when exact-set, full evidence-contract, and Playwright ownership checks all pass"
---

<objective>
Close the remaining Phase 234 verifier gap by making the claimed six-slot evidence contract exact and proving both classes of extra slot fail through the real completion transition.

Purpose: prevent a new failed, malformed, or otherwise unvalidated receipt from drifting into the phase evidence map while completion silently ignores it.
Output: exact MapSet equality in `validate_required_evidence!/1`, deterministic production-transition mutation coverage for extra failed and malformed success-shaped slots, and a re-ratified validation artifact with explicit Wave 12 coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-RESEARCH.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PATTERNS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-20-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject every seventh evidence slot through the production completion transition</name>
  <reversibility rating="reversible">This changes one repository-local ExUnit contract helper and its mutation coverage; reverting the task restores the prior behavior without migration or published-contract breakage.</reversibility>
  <files>test/sigra/planning/phase_234_evidence_contract_test.exs, .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md</files>
  <read_first>
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` — modify `validate_required_evidence!/1` and extend the existing `validation_signoff` production-transition mutation coverage without duplicating the transition helper.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VERIFICATION.md` — authoritative remaining gap: intersection accepts arbitrary extra failed or malformed slots.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — real six-slot green ledger used by `evidence/0` as the baseline fixture.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md` — update its Per-Task Verification Map, reviewed-snapshot receipts, Validation Sign-Off, and Approval so the artifact explicitly covers 234-21-01 after the exact-set repair.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-20-SUMMARY.md` — established concrete receipt validators and production transition behavior to preserve.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md` — preserve D-01 through D-10 evidence meaning and the D-11/D-12 no-scope-expansion boundaries.
    - Every file listed in `<files>` — establish current behavior before editing.
  </read_first>
  <behavior>
    - "RED: the real green ledger plus an extra `%{\"status\" => \"failed\"}` slot currently returns `:complete`; add a transition mutation that requires rejection."
    - "RED: the real green ledger plus an extra malformed `%{\"status\" => \"success\"}` slot currently returns `:complete`; add a separate transition mutation that requires rejection."
    - "GREEN: the unchanged real six-slot ledger still returns `:complete` with green command receipts."
    - "GREEN: the receipt key set must equal, not merely contain, the six required keys, and rejection diagnostics identify the exact-set violation or added key."
  </behavior>
  <action>
    Start with the two failing production-transition mutations inside the existing `@tag :validation_signoff` coverage. Use `evidence()` and `green_command_receipts()` as the real green baseline and the existing complete/true/true frontmatter shape. Create two independent seventh-slot mutations: one extra failed receipt and one extra malformed success-shaped receipt. Pass each entire mutated map to `assert_transition_allowed!/3`; require `ExUnit.AssertionError` and a stable diagnostic that identifies the exact six-slot contract or unexpected key. Keep a positive assertion showing the unmodified six-slot fixture still returns `:complete`.

    Then change `validate_required_evidence!/1` so the required six keys are represented as a `MapSet` and the comparison is exactly `MapSet.new(Map.keys(receipts)) == required`. Remove the intersection-based containment check. Preserve the existing six named validators and their order; the repair is aggregate key-set closure, not a rewrite of receipt schemas or command validation. Exercise the exact helper only through `assert_transition_allowed!/3` in the new mutations so the verifier sees the real production authorization path.

    Bring `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md` into correspondence with this Wave 12 work. Add exactly one Per-Task Verification Map row for task `234-21-01`, Plan `21`, Wave `12`, requirements `DX-01/DX-02/DX-03/DX-04/DX-06`, threat reference `T-234-21-01`, and secure behavior stating that the production completion transition admits exactly the six named evidence keys and rejects every additional slot. Its Automated Command cell must exactly match this task's three-command `<automated>` verification, and its Status must remain pending until all three commands complete with zero failures. Extend the validation-contract parser's required gap-task tuple set so this row is mandatory exactly once, retains its literal command and green status, and is rejected when omitted, duplicated, command-substituted, or marked non-green, following the existing Plans 19–20 row conventions.

    After the implementation is committed, use that implementation commit as the new `reviewed_commit_sha`, rerun the validation artifact's existing exact five-command receipt inventory in declared order, and replace each receipt timestamp, exit, sanitized output SHA-256, and commit SHA with the new run. Set `reviewed_at` within the existing deterministic freshness bound after the final receipt. Then run the focused `validation_signoff`, the complete evidence-contract file, and the deterministic Playwright ownership contract; only after all three report zero failures may the executor mark `234-21-01` green, add a checked Validation Sign-Off item explicitly naming it, and update Approval to attest the Wave 12 row, exact six-slot behavior, refreshed reviewed-snapshot receipts, and zero-failure post-population verification. If any command fails, keep validation draft/false/false and persist the diagnostic without waiving or approving the row.

    Preserve every locked Phase 234 decision while closing only the aggregate evidence-set gap. D-01: keep `mix ci` as the single contributor/CI call-through. D-02: retain all seven ratified alias legs. D-03: retain the fail-closed parity contract and do not add Credo, Dialyzer, or `mix_audit`. D-04: keep generated install-golden bytes outside formatter ownership. D-05: preserve the dereferenced immutable Release Please SHA and continue rejecting the annotated tag-object SHA. D-06: preserve the release-critical action inventory contract. D-07: preserve the exact three Dependabot ecosystems/directories and service-owned evidence semantics. D-08: preserve the committed exact Playwright inventory. D-09: keep both formerly orphaned specs deliberately wired. D-10: preserve the evidence-backed SEED-006 closeout without reopening gallery remediation. D-11: add none of the four adjacent policy todos. D-12: add no retry machinery for the transient Hex/rebar incident. Apart from the explicitly required Wave 12 row, parser tuple, refreshed receipts, sign-off item, approval, and review metadata, preserve the validation artifact's established structure and earlier rows. Do not alter the ledger, workflows, inventory, service state, required-check policy, UI, or receipt semantics; do not weaken existing bare/partial required-slot mutations or replace the real evidence fixture with fabricated status-only maps.
  </action>
  <acceptance_criteria>
    - `validate_required_evidence!/1` compares `MapSet.new(Map.keys(receipts))` directly with the required six-slot `MapSet`; no intersection or one-way containment remains in that exact-set assertion.
    - The unmodified real six-slot evidence fixture still authorizes `:complete` through `assert_transition_allowed!/3` with the existing green command receipts.
    - A seventh slot whose receipt status is failed raises through `assert_transition_allowed!/3` before completion can be authorized.
    - A seventh slot containing only a malformed success-shaped map raises through `assert_transition_allowed!/3` before completion can be authorized.
    - Both mutations are distinct, deterministic, tagged for focused `validation_signoff` execution, and assert a diagnostic tied to the exact-set violation or unexpected slot.
    - The Per-Task Verification Map contains exactly one `234-21-01` / Plan `21` / Wave `12` row with requirements `DX-01/DX-02/DX-03/DX-04/DX-06`, threat reference `T-234-21-01`, exact six-slot secure behavior, the literal three-command task verification, and `✅ green` only after that verification reports zero failures.
    - Omitting, duplicating, command-substituting, or marking the `234-21-01` row non-green is rejected through validation parsing and the production completion transition with the task ID named.
    - The exact five command receipts are refreshed against the implementation commit with exit 0, sanitized hashes, one identical reviewed commit SHA, and timestamps inside the existing freshness window; Validation Sign-Off and Approval explicitly cover `234-21-01` and the exact six-slot repair.
    - Existing missing/bare/partial required-slot, wrong-SHA, stale/future timestamp, command-inventory, final-evidence, concrete receipt-validator, and Playwright ownership tests report zero failures without changing persisted evidence.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff &amp;&amp; mix test test/sigra/planning/phase_234_evidence_contract_test.exs &amp;&amp; mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs</automated>
  </verify>
  <done>The exact six-slot ledger remains accepted, both extra-slot production mutations reject complete/true/true with stable diagnostics, all three deterministic verification commands report zero failures, and the refreshed validation artifact explicitly ratifies 234-21-01.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new file or production symbol is created.
- `validate_required_evidence!/1` in `test/sigra/planning/phase_234_evidence_contract_test.exs` is modified to enforce exact six-slot key-set equality.
- One new `validation_signoff` ExUnit production-transition test case (or two independently named cases) is created to cover an extra failed slot and an extra malformed success-shaped slot.
- `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md` is updated with one exact Wave 12 row for 234-21-01, refreshed reviewed-snapshot command receipts, and explicit post-execution sign-off/approval coverage.

## Planner Contribution Detector Results

- API coverage: deterministic detector returned `detected: true` only because an existing plan states that there is no external API integration. The reasoned declaration in `234-COVERAGE.md` remains valid: this plan changes a repository-local evidence contract and adds no Sigra product API/SDK/service capability, so no capability matrix or API checkpoint is added.
- Assumption delta: deterministic detector returned `detected: false`; no identity-model decision block is needed.
- Schema push: deterministic path scan found no supported ORM schema file in the phase section, context, research, or plans; no schema task is needed.
- UI safety: this gap changes no admin UI, CSS, assets, themes, browser behavior, or Playwright runtime scope.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Local gate reproduction and action/dependency evidence cannot drift silently | 01–21 | COVERED | Plan 21 closes the only remaining exact-evidence-set drift path. |
| REQ | DX-01 | `mix ci` reproduces the PR gate | 01–05, 09, 11–16, 18, 20–21 | COVERED | Existing implementation/evidence remains intact; Plan 21 closes its aggregate receipt set. |
| REQ | DX-02 | Release-critical Actions are immutable | 06, 10, 14, 18, 20–21 | COVERED | Existing release receipt remains required; an unknown seventh slot cannot bypass exact-set authorization. |
| REQ | DX-03 | Dependabot covers Actions, Mix, and npm | 07, 10, 14, 17–18, 20–21 | COVERED | Existing exact Dependabot receipt remains required; no extra unvalidated dependency receipt is tolerated. |
| REQ | DX-04 | Every Playwright spec has an executable owner | 08, 10, 14, 18–21 | COVERED | Existing exact inventory evidence remains required; the concurrency edge remains explicitly flagged. |
| REQ | DX-06 | SEED-006 is delivered or a residual is filed | 10, 14, 18, 20–21 | COVERED | Existing current/historical gallery receipts remain exact required members. |
| RESEARCH | — | Extend existing ExUnit seams; add no package or second topology | 21 | COVERED | One repository-local helper, its production-transition mutations, and the corresponding validation contract are changed. |
| RESEARCH | — | Enumerate then reconcile with exact set equality | 08, 19, 21 | COVERED | Plan 21 applies the same fail-closed equality pattern to evidence keys. |
| RESEARCH | — | Structural proof and service receipts remain separate | 09–10, 17–18, 20–21 | COVERED | Plan 21 changes only aggregate authorization; individual receipt validators remain unchanged. |
| CONTEXT | D-01–D-04 | Single local gate and golden-safe formatter boundary | 01–05, 09, 11–16, 18, 20–21 | COVERED | Plan 21 preserves the ratified local receipt without reopening gate or formatter implementation. |
| CONTEXT | D-05–D-07 | Exact action pin/policy and three Dependabot ecosystems | 06–07, 10, 14, 17–18, 20–21 | COVERED | Plan 21 preserves concrete validators and closes the aggregate extra-slot bypass. |
| CONTEXT | D-08–D-10 | Exact Playwright ownership and evidence-backed gallery closeout | 08, 10, 14, 18–21 | COVERED | Inventory/gallery evidence stays unchanged and exactly admitted. |
| CONTEXT | D-11–D-12 | Exclude adjacent todos and retry machinery | 01, 10, 14, 18, 20–21 | COVERED | The task explicitly forbids scope expansion and retry behavior. |

No source item is missing. Deferred ideas, adjacent policy todos, UI remediation, service mutation, retry behavior, package installation, and schema work remain excluded.

## Prohibition Recall Disposition

The spec-less two-stage recall/precision fallback keeps one bespoke evidence-honesty prohibition: completion must not ignore an unvalidated seventh slot. It is carried descriptor-less and flagged-unverified in `must_haves.prohibitions`; the executor adds deterministic regression coverage but does not silently rewrite the fallback probe lifecycle. Canon supply-chain and input-validation concerns remain in the threat model rather than being minted as additional prohibitions. Count equality: 1 kept = 1 authored + 0 unresolved omissions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository evidence JSON → completion transition | Repository-controlled keys and receipt maps cross into `assert_transition_allowed!/3`, which authorizes complete/true/true. |
| aggregate evidence set → six slot validators | The aggregate key set decides whether every admitted receipt has a named schema validator. |
| validation Markdown → completion authorization | The Wave 12 task row, reviewed-snapshot receipts, sign-off, and approval must correspond exactly to the code and commands that closed the gap. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-21-01 | Tampering | `validate_required_evidence!/1` key-set gate | high | mitigate | Replace intersection containment with direct MapSet equality and mutation-test both extra-slot shapes through the production transition. |
| T-234-21-02 | Spoofing | extra success-shaped evidence receipt | high | mitigate | Reject every key without one of the six named validators before a success-shaped map can influence or evade authorization. |
| T-234-21-03 | Repudiation | completion-transition diagnostic | medium | mitigate | Require deterministic assertion diagnostics that identify the exact-set violation or unexpected slot for both mutations. |
| T-234-21-04 | Denial of service | evidence key-set construction | low | accept | The repository-owned evidence map is small and MapSet construction is linear; focused tests bound the exercised inputs. |
| T-234-21-05 | Tampering | `234-21-01` validation row and reviewed receipts | high | mitigate | Require one exact Wave 12 tuple, refresh all five receipts against one reviewed implementation commit, and ratify only after the focused, full evidence, and Playwright contracts report zero failures. |

## ASVS L1 Automated Controls

- V5.1 input validation: exact key-set equality and per-slot validators reject unrecognized, missing, failed, and malformed evidence before completion.
- V7.1 evidence integrity: the production transition is mutation-tested with the real persisted six-slot baseline and two distinct seventh-slot adversaries.
</threat_model>

<verification>
Run `mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only validation_signoff` for the focused production-transition and validation-row mutations, `mix test test/sigra/planning/phase_234_evidence_contract_test.exs` for the complete Phase 234 evidence contract including final-evidence and untagged regressions, and `mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` for deterministic ownership continuity. Each command must exit 0 with zero failures before 234-21-01 is marked green or sign-off/approval is restored. No sleeps, browser UAT, network calls, service mutation, or human approval is used.
</verification>

<success_criteria>
- The exact six-slot real evidence ledger authorizes completion and no seven-slot ledger does.
- Extra failed and extra malformed success-shaped slots both fail through `assert_transition_allowed!/3` with deterministic diagnostics.
- The focused validation-signoff suite, complete evidence-contract file, and Playwright inventory contract each report zero failures.
- The validation artifact contains one exact green 234-21-01 Wave 12 row tied to T-234-21-01, refreshed reviewed-snapshot receipts, and explicit post-execution sign-off/approval coverage.
- The verifier's sole remaining 6/7 gap is mechanically closed without reopening any completed requirement implementation.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-21-SUMMARY.md` when done.
</output>
