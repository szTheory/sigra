---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 09
type: execute
wave: 3
depends_on: [234-01, 234-02, 234-03, 234-04, 234-05]
files_modified:
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
  - test/sigra/planning/phase_234_evidence_contract_test.exs
autonomous: true
requirements: [DX-01]

must_haves:
  truths:
    - "A detached clean worktree runs the complete `mix ci` successfully with the documented Postgres and phx_new prerequisites."
    - "A pull_request CI run at the implementation SHA executes the `library_tests_dep_off` step named for `mix ci` successfully and preserves the exact `Library tests` required context."
    - "Missing local or GitHub evidence yields a failed contract with durable command/output diagnostics; no absent/skipped receipt is passed."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-01 unclassified edge remains explicit: clean-checkout success plus a real PR invocation is the chosen proof; no unstated platform/ordering fallback is inferred."
  prohibitions: []
  artifacts:
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      provides: "machine-readable local and GitHub receipt ledger"
    - path: test/sigra/planning/phase_234_evidence_contract_test.exs
      provides: "receipt schema and non-vacuity gate"
  key_links:
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      to: .github/workflows/ci.yml
      via: "run id, head SHA, job id, step name, conclusion"
---

<objective>
Execute—not merely read—the new local/CI parity path and commit machine-readable proof.

Purpose: close DX-01 with clean-checkout and real pull-request evidence.
Output: evidence schema/contract, local receipt, and observed CI receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-01-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-02-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-03-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-04-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-05-SUMMARY.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove `mix ci` from a detached clean checkout</name>
  <precondition>Docker/Postgres tooling and the phx_new 1.8.8 archive installation command are available; verify with read-only version/archive-list checks.</precondition>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json, test/sigra/planning/phase_234_evidence_contract_test.exs</files>
  <read_first>
    - `CONTRIBUTING.md` — exact prerequisites and alias claim produced by 234-01.
    - `mix.exs` — final alias order.
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — immutable receipt conventions.
    - `scripts/db/up.sh` — deterministic Postgres startup/env output.
    - Every file listed in `<files>` — current absence/content before editing.
  </read_first>
  <behavior>
    - "Evidence rejects dirty source, nonzero alias status, missing seven-leg markers, absent golden-tree checksum, or omitted command output path."
    - "The run occurs at the exact implementation commit in a detached worktree and cannot consume unrelated user changes."
  </behavior>
  <action>
    Create `234-EVIDENCE.json` with schema version and empty `local_mix_ci`, `pr_ci`, `dependabot`, and `gallery` objects, plus an ExUnit contract requiring typed non-empty fields and success conclusions before a slot can be complete. Observe RED.

    Commit implementation prerequisites before proof. Create an explicit `mktemp -d` directory, add a detached worktree at that commit, start the documented database, install/check phx_new 1.8.8, and run `MIX_ENV=test mix ci` with stdout/stderr captured to an evidence log outside the repository. Record commit SHA, clean-before status, command, start/end UTC timestamps, exit status, ordered leg names, formatter/golden result markers, and SHA-256 of the log in JSON; do not record environment values or secrets. Remove only the explicit detached worktree/temp directory after capture. A nonzero run records `status: failed` plus sanitized tail diagnostics and blocks completion.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only local_mix_ci</automated>
  </verify>
  <acceptance_criteria>
    - Evidence contract is RED with empty slots and GREEN for the completed local slot only.
    - Receipt names exact detached commit, clean status, literal command, timestamps, status 0, seven legs, and log hash.
    - Golden-tree checksum/diff indicates no generated-byte modification.
    - Failure cannot be converted to success; sanitized diagnostics are durable and secrets/env values are absent.
  </acceptance_criteria>
  <done>A clean checkout executes the full contributor gate successfully with a non-vacuous machine receipt.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the direct alias and protected aggregate on a real PR run</name>
  <precondition>`gh auth status` succeeds with access to `szTheory/sigra`, and the implementation commit is pushed to a scoped phase branch or existing authorized phase PR.</precondition>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json, test/sigra/planning/phase_234_evidence_contract_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` — workflow name, selected job/step, PR trigger, and protected aggregate name.
    - `scripts/ci/ci-run-metrics.sh` — existing fail-closed run/job metric collector.
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md` — PR run lookup and receipt fields.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — Task 1 schema.
    - Every file listed in `<files>` — current content before editing.
  </read_first>
  <action>
    Reuse an existing authorized phase PR or create a scoped branch/PR for the implementation commits. Locate the exact pull_request run by head SHA using `gh run list --workflow ci.yml --event pull_request --commit <sha> --json databaseId,headSha,event,status,conclusion,url`; require exactly one candidate or record all ambiguity and block. Use `gh run watch <id> --exit-status` for deterministic polling without a sleep loop; retry a transient CLI/network failure once, but never retry a deterministic job failure.

    Capture `gh run view <id> --json jobs` and the selected `library_tests_dep_off` job log. Require event `pull_request`, matching implementation SHA, overall success, a successful step whose name/command evidence identifies the direct `mix ci` invocation, successful `Library tests` terminal job, non-skipped shard/scaffold receivers, and no `continue-on-error`. Write run/job IDs, URLs, conclusions, timestamps, and sanitized log hashes to `pr_ci`. If any evidence is absent/skipped/red, write `status: failed`, exact diagnostics and URLs, file a scoped tracked defect if the cause is outside phase-owned code, and leave DX-01 incomplete.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs --only local_mix_ci --only pr_ci</automated>
  </verify>
  <acceptance_criteria>
    - Evidence names a pull_request run at the exact implementation SHA and its immutable URLs/IDs.
    - Direct alias step and protected Library tests aggregate both executed (not skipped) and succeeded.
    - Shard and scaffold receivers remain present/non-skipped; no topology rename is accepted.
    - Missing/red evidence fails the contract and produces diagnostics or a tracked defect, never a passed slot.
  </acceptance_criteria>
  <done>DX-01 is proven by both clean local execution and the same alias executing successfully on a real PR.</done>
</task>

</tasks>

## Artifacts this phase produces

- New path: `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json`.
- New evidence fields: `schema_version`, `local_mix_ci`, `pr_ci`, `dependabot`, `gallery`, receipt `status`, `commit_sha`, `run_id`, `job_id`, `url`, `command`, `started_at`, `completed_at`, `conclusion`, `log_sha256`, `diagnostics`.
- New path: `test/sigra/planning/phase_234_evidence_contract_test.exs` with `:local_mix_ci` and `:pr_ci` tagged receipt gates.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local repository → detached worktree/process | Clean-checkout evidence must not consume unrelated user state. |
| GitHub API/job logs → committed evidence | Remote metadata/log text is untrusted and may contain sensitive values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-06-01 | Repudiation | evidence JSON | high | mitigate | Exact SHAs/IDs/URLs/timestamps/log hashes and schema contract; skipped/absent data fails. |
| T-234-06-02 | Information disclosure | CI logs/secrets | high | mitigate | Store sanitized metadata/hashes, never raw token/env values; release secrets are not queried. |
| T-234-06-03 | Tampering | shell arguments | high | mitigate | Fixed literal commands and validated numeric run/job IDs; no untrusted log/event text is evaluated or interpolated. |
| T-234-06-04 | Denial of service | polling | medium | mitigate | `gh run watch --exit-status`, one transient retry, workflow timeout; no unbounded/sleep polling. |
</threat_model>

<verification>
Receipt contract accepts only successful non-skipped local and PR evidence at the implementation SHA; all failures remain durable and red.
</verification>

<success_criteria>
DX-01 is closed by executed clean-checkout and PR evidence, not source resemblance.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-09-SUMMARY.md` when done.
</output>
