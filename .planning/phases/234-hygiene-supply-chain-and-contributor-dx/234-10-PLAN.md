---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 10
type: execute
wave: 5
depends_on: [234-06, 234-07, 234-08, 234-09]
files_modified:
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-COVERAGE.md
  - .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md
  - test/sigra/planning/phase_234_evidence_contract_test.exs
  - .planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md
autonomous: true
requirements: [DX-02, DX-03, DX-04, DX-06]

must_haves:
  truths:
    - "The pinned release workflow executes successfully after the exact pin reaches main; missing/skipped/red evidence remains failed with diagnostics."
    - "D-07: GitHub's Dependabot job-log surface shows processed update jobs for github-actions, mix, and npm at the locked directories, or a tracked defect records the missing ecosystem/log/error without claiming success."
    - "D-10: a current non-PR gallery run succeeds and is recorded alongside run 30659282026's 126-design-test receipt; contradictory evidence becomes a tracked residual."
    - "SEED-006 records the corrected Phase 197 root cause/remediation and delivered evidence without reopening gallery implementation."
    - "D-11/D-12: no reviewed adjacent todo, retry machinery, required-check policy, release-note policy, canary policy, UI scope, or schema/API capability is folded into the phase."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-01 unclassified edge remains explicit: clean-checkout success plus a real PR invocation is the chosen proof; no unstated platform/ordering fallback is inferred."
    - "FLAGGED/UNRESOLVED DX-02 unclassified edge remains explicit: release-workflow green is judged on the post-pin main SHA; no tag or source-only backstop is invented."
    - "FLAGGED/UNRESOLVED DX-03 unclassified edge remains explicit: GitHub job logs, not absence of PRs, determine processing."
    - "FLAGGED/UNRESOLVED DX-04 concurrency edge remains explicit: atomic inventory/guard commit plus exact-set validation is the stated parallel/interruption guarantee."
    - "FLAGGED/UNRESOLVED DX-06 unclassified edge remains explicit: current gallery execution either corroborates delivered status or produces a named defect; no UI remediation is assumed."
  prohibitions: []
  artifacts:
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-COVERAGE.md
      provides: "reasoned no-external-API/no-schema capability declaration and source-audit disposition"
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      provides: "final GitHub-managed release, Dependabot, gallery, and inventory receipts"
    - path: .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md
      provides: "delivered closure or linked tracked residual"
  key_links:
    - from: .github/dependabot.yml
      to: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
      via: "default-branch SHA plus per-ecosystem GitHub job-log receipts"
    - from: .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
      to: .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md
      via: "run 30659282026 and current non-PR gallery receipt"
---

<objective>
Ratify the GitHub-owned surfaces and close SEED-006 with executed, durable evidence rather than source inspection.

Purpose: finish supply-chain/dependency/gallery claims honestly after configuration reaches the managed service.
Output: post-main release/Dependabot/gallery receipts, SEED-006 delivered closure or tracked residual, and reasoned coverage/source-audit declaration.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-RESEARCH.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-VALIDATION.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
@.planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Observe the immutable release path on main (D-05, D-06)</name>
  <precondition>`gh auth status` succeeds with scoped repository write/read access and all implementation PR checks are successful.</precondition>
  <reversibility rating="costly">Rollback target: revert the scoped phase PR by merge SHA and restore the three evidence/residual files from the task-start commit. Pre-change gate: required checks, exact PR head SHA, permissions/token-region hashes, and merge strategy are recorded. Post-change gate: the release evidence contract proves the post-merge run and unchanged privilege boundaries before advancing.</reversibility>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json, test/sigra/planning/phase_234_evidence_contract_test.exs, .planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md</files>
  <read_first>
    - `.github/workflows/release-please.yml` — pinned job, push trigger, and sensitive release conditions.
    - `test/sigra/planning/phase_234_action_pinning_contract_test.exs` — exact pin policy.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — prior local/PR receipts and final schema.
    - `AGENTS.md` — authorization for scoped evidence PRs, CI polling, and auto-merge; missing evidence must block.
    - Every file listed in `<files>` — current content/absence before editing.
  </read_first>
  <action>
    Confirm every phase implementation/evidence commit is on the scoped PR and required checks are successful via `gh pr checks --watch --fail-fast` (one transient CLI retry only). Enable or perform the repository-authorized merge using the existing merge strategy; record PR number, merge SHA, and URL. Do not dispatch Release Please manually on a branch because that could create release state from a non-main ref.

    Poll the push-triggered `Release Please` run for the exact merge SHA using `gh run list --workflow release-please.yml --event push --commit <merge-sha> --json ...` and `gh run watch <id> --exit-status`; capture `gh run view --json jobs` and sanitized job-log hashes. Require the `release-please` job/step to execute (not skip due missing step) and the workflow conclusion to be success; acceptable downstream release jobs may be conditionally skipped only when no release was created, and that condition/result must be recorded. Extend the evidence contract with `:release` schema/non-vacuity assertions plus ASVS V4.1/V10.3 checks tying the receipt to the exact immutable ref, merge SHA, preserved permissions/token source, and release conditions. Any red/absent run writes failed diagnostics and the fixed residual todo, then blocks the success claim.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_action_pinning_contract_test.exs test/sigra/planning/phase_234_evidence_contract_test.exs --only release</automated>
  </verify>
  <acceptance_criteria>
    - Evidence identifies PR, merge SHA, push run/job URLs/IDs, exact pinned action step, success conclusion, and log hashes.
    - Release Please action step executed successfully; no secret value/raw token is committed.
    - Conditional no-release downstream skips are distinguished from a missing/rotted action step.
    - Any failure produces the fixed tracked-defect path and a red evidence contract.
  </acceptance_criteria>
  <done>The immutable action pin is proven on its real secrets-bearing main-branch workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Capture GitHub-processed Dependabot jobs for all ecosystems (D-07)</name>
  <precondition>The Phase 234 Dependabot configuration exists on the recorded default-branch merge SHA and the authenticated account has write access required to view Dependabot job logs.</precondition>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json, test/sigra/planning/phase_234_evidence_contract_test.exs, .planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md</files>
  <reversibility rating="reversible">Rollback target: restore the evidence JSON, contract, and conditional residual file from the task-start commit; remote update jobs are observed, not mutated. Pre-change gate: main SHA and authenticated log access are proven. Post-change gate: the exact three-tuple Dependabot evidence contract passes before advancing.</reversibility>
  <read_first>
    - `.github/dependabot.yml` — exact three ecosystem/directory pairs.
    - `test/sigra/planning/phase_234_dependabot_contract_test.exs` — offline proof boundary.
    - GitHub's official `Dependabot job logs` and `Viewing Dependabot job logs` docs — job ID/timestamp/associated PR/error semantics and write-access requirement.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — final slot schema.
    - Every file listed in `<files>` — current content/absence before editing.
  </read_first>
  <action>
    First prove `.github/dependabot.yml` at the recorded main SHA with `gh api repos/szTheory/sigra/contents/.github/dependabot.yml?ref=<sha>` and hash the decoded content. Use authenticated deterministic browser automation (role/link selectors and navigation readiness; no sleeps) against GitHub Insights → Dependency graph → Dependabot to enumerate the three manifest rows and each `Recent update jobs` list. For each of `github-actions:/`, `mix:/`, and `npm:/test/example/priv/playwright`, capture manifest/directory, latest version-update job ID, timestamp, status/error summary, full-log URL, and associated PR URL when present. Poll page state with selector waits until the row/job list appears or the bounded automation timeout expires; save sanitized text/HTML hashes, not cookies/tokens.

    Record a successful processed job even when it found no update; never use lack of a Dependabot PR as proof. Extend the evidence contract to require exactly the three tuples, non-empty job IDs/timestamps/log URLs, no processing errors for a passing slot, sanitized evidence hashes, and ASVS V5.1/V7.1 rejection of malformed tuple/status/URL/hash fields. If GitHub has not scheduled an ecosystem yet, access is unavailable, or the job errors, record `status: failed`, exact tuple, URL/error/timeout diagnostics, and the residual todo with owner/recheck command; do not mark DX-03 complete.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_dependabot_contract_test.exs test/sigra/planning/phase_234_evidence_contract_test.exs --only dependabot</automated>
  </verify>
  <acceptance_criteria>
    - Main SHA serves byte-equivalent config and evidence has exactly three processed ecosystem/directory rows.
    - Each passing row has job ID, timestamp, log URL, processing result, artifact hash, and optional PR URL.
    - No-PR is accepted only with a successful job log showing the ecosystem was checked.
    - Missing/error/access evidence creates durable diagnostics/tracked defect and remains red.
  </acceptance_criteria>
  <done>GitHub—not local YAML inference—has processed all three dependency ecosystems, or the exact unresolved service defect is durably owned.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the gallery, close SEED-006, and seal source/capability coverage (D-08, D-10, D-11, D-12)</name>
  <precondition>`gh auth status` succeeds and `ci.yml` on main includes the shared-boot `admin_design_recapture` consumer.</precondition>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json, .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-COVERAGE.md, .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md, test/sigra/planning/phase_234_evidence_contract_test.exs, .planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md</files>
  <reversibility rating="costly">Rollback target: restore the five closeout/evidence files from the task-start commit; the dispatched evidence run is immutable and remains as an auditable receipt. Pre-change gate: exact main SHA, historical receipt, and gallery workflow identity are recorded. Post-change gate: gallery/final-slot contracts and the planning suite pass before advancing.</reversibility>
  <read_first>
    - `.github/workflows/ci.yml` — workflow_dispatch and `admin_design_recapture` job/shared boot/test command.
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — run 30659282026 exact receipt.
    - `.planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md` — corrected Phase 197 root cause/remediation and acceptance criteria.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — Phase 235 input to cite.
    - `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `234-CONTEXT.md`, `234-RESEARCH.md` — final source audit.
    - Every file listed in `<files>` — current content/absence before editing.
  </read_first>
  <action>
    Dispatch `ci.yml` on the recorded main SHA with `gh workflow run ci.yml --ref main`, resolve the unique workflow_dispatch run by head SHA/time, and use `gh run watch --exit-status`. Capture `admin_design_recapture` job metadata/log and require shared boot success, non-skipped job, `--retries=0`, and the observed design test count/result from the log; also retain run 30659282026's immutable receipt (126 design tests) as historical corroboration. Extend the evidence contract with `:gallery` and final all-slot assertions. If current execution contradicts delivery, record failed diagnostics and the residual todo instead of editing UI/snapshots or weakening the claim.

    Update SEED-006 with a delivered closeout citing the corrected Phase 197 host-OS font-metric root cause, self-hosted font/in-CI recapture/hard-gate remediation, run 30659282026, and the current run. If current evidence is red, leave the seed unresolved and link the fixed residual todo with owner/evidence/command.

    Create `234-COVERAGE.md`: declare `frontend=false`, no schema-relevant ORM change, and no external API capability rows because GitHub Actions/Dependabot are repository-managed CI services rather than a product API integration. Include the complete source audit below, all five flagged fallback assumptions, zero kept bespoke prohibitions, and canon breadcrumbs for action tampering/secrets/shell injection to the plan threat models and `$gsd-secure-phase`. Explicitly list D-11's four excluded todos and D-12 retry exclusion; do not modify them.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_evidence_contract_test.exs test/sigra/planning/phase_234_playwright_inventory_contract_test.exs &amp;&amp; mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - Current gallery job at main is non-skipped/successful with shared boot and retry-zero evidence, or a red tracked residual exists and DX-06 is not claimed complete.
    - SEED-006 retains the corrected root cause/remediation and cites both historical/current immutable receipts.
    - COVERAGE declares no UI/API/schema invention, contains the source audit, five flagged assumptions, prohibition no-silent-drop result, D-11 exclusions, and D-12 retry boundary.
    - Final evidence contract rejects every absent/skipped/red required slot and names diagnostics.
  </acceptance_criteria>
  <done>SEED-006 and every GitHub-owned claim are honestly closed with machine evidence or durably tracked as unresolved, and Phase 235 receives the inventory.</done>
</task>

</tasks>

## Artifacts this phase produces

- New path: `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-COVERAGE.md`.
- Final evidence fields: `release`, Dependabot ecosystem job receipts, `gallery`, `historical_gallery`, and per-slot `status`/diagnostics.
- Modified seed contract: delivered closeout with corrected root cause and two immutable gallery receipts, or linked residual.
- Conditional new path: `.planning/todos/pending/2026-08-01-phase-234-github-evidence-residual.md` only when required evidence is absent/red; its presence blocks the affected completion claim.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Contributor reproduces the gate; action/dependency/spec ownership cannot drift silently | 01–14 | COVERED | Local/PR/service evidence, ratchets, and fail-closed validation sign-off. |
| REQ | DX-01 | `mix ci` reproduces PR gate | 01–05, 09, 11–14 | COVERED | Exact single-owner invocation, bounded cleanup, two executed receipts, and final sign-off. |
| REQ | DX-02 | Release-critical Actions immutable | 06, 10 | COVERED | Exact pin/policy plus main workflow receipt. |
| REQ | DX-03 | Dependabot Actions/Hex/npm | 07, 10 | COVERED | Offline exact set plus GitHub job logs. |
| REQ | DX-04 | Every Playwright spec owned or removed | 08, 10 | COVERED | Exact inventory and both orphans wired. |
| REQ | DX-06 | SEED-006 closed or residual filed | 10 | COVERED | Historical/current gallery evidence or defect. |
| RESEARCH | — | Native Mix checks; no packages; alias call-through | 01–05, 09, 11–14 | COVERED | Existing tools only. |
| RESEARCH | — | Fail-closed action/Dependabot/inventory contracts | 06–08 | COVERED | Mutation/non-vacuity tests. |
| RESEARCH | — | Structural proof separated from service receipts | 09, 10 | COVERED | Local/PR/main/Dependabot/gallery evidence. |
| CONTEXT | D-01–D-04 | Local gate and golden-safe formatting | 01–05, 09, 11–14 | COVERED | Every decision cited in tasks and validation sign-off. |
| CONTEXT | D-05–D-07 | Exact action pin/policy and three Dependabot ecosystems | 06, 07, 10 | COVERED | Every decision cited in tasks. |
| CONTEXT | D-08–D-10 | Inventory, two orphans, SEED-006 | 08, 10 | COVERED | Every decision cited in tasks. |
| CONTEXT | D-11–D-12 | Exclude adjacent todos/retry machinery | 01, 10 | COVERED | Explicit negative scope; deferred items are exclusions, not gaps. |

## Prohibition Recall Disposition

The descriptor-less two-stage recall produced no bespoke values/safety/ethics prohibitions for these repository utilities. Raw candidates about mutable actions, secret exposure, unsafe interpolation, malformed parsing, coverage deletion, and retries are routine engineering or canon supply-chain/security constraints; they are handled by the plan threat models, deterministic contracts, AGENTS.md, and `$gsd-secure-phase`, not minted as descriptor-less product prohibitions. Count equality: 0 kept = 0 authored + 0 unresolved.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| merged repository config → GitHub release/Dependabot/gallery services | Main-branch files trigger secrets-bearing and managed automation. |
| authenticated GitHub pages/API/logs → evidence | Remote service output is untrusted and may contain secrets or ambiguous absence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-07-01 | Tampering | release main workflow | high | mitigate | Exact merge SHA, immutable action contract, observed post-pin main run, unchanged permissions. |
| T-234-07-02 | Information disclosure | release/Dependabot/gallery logs | high | mitigate | Sanitize and hash logs; never store tokens/cookies/env values; fixed selectors/IDs only. |
| T-234-07-03 | Repudiation | Dependabot absence | high | mitigate | Require per-ecosystem job IDs/timestamps/log URLs; absence of PR is never proof. |
| T-234-07-04 | Tampering | evidence parser/shell | high | mitigate | Typed fail-closed JSON contract, validated IDs/SHAs, fixed commands, and no evaluation of remote text. |
| T-234-07-05 | Denial of service | service polling | medium | mitigate | Bounded `gh ... --watch`/selector waits and one transient retry; no sleeps/unbounded loops. |

## ASVS L1 Control Mapping and Automated Verification

| ASVS control intent | Managed-service/evidence control | Automated assertion |
|---|---|---|
| V4.1 access-control design | Release receipt must preserve the reviewed permissions, token source, trigger, and condition boundaries. | Release evidence contract binds merge SHA/run/job/step to the exact pin and extracts the unchanged privileged regions; any mismatch is red. |
| V5.1 input validation | Treat GitHub API/browser fields and repository evidence JSON as untrusted input. | Typed contracts reject malformed SHAs, IDs, tuple names, statuses, URLs, timestamps, hashes, duplicate/missing rows, and unknown fields required for a pass. |
| V7.1 log/evidence integrity | Evidence must be attributable, sanitized, complete, and non-vacuous. | Per-slot run/job IDs, timestamps, immutable URLs, sanitized log hashes, success/non-skipped conclusions, and diagnostics are mandatory; tokens/cookies/env values are forbidden. |
| V10.3 application integrity | Privileged execution must use the reviewed action revision on the recorded main SHA. | Action-pinning and evidence contracts jointly require the exact dereferenced SHA and a successful post-merge step; source-only or branch-only evidence fails. |
</threat_model>

<verification>
All focused structural contracts and the full planning suite pass; final evidence JSON contains successful non-skipped receipts or explicit failed slots linked to a tracked defect, never silent absence.
</verification>

<success_criteria>
DX-02/DX-03/DX-04/DX-06 and every D-05–D-12 decision are closed with main/service evidence or an explicit blocking tracked residual; no unrelated UI/API/schema/todo/retry scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-10-SUMMARY.md` when done.
</output>
