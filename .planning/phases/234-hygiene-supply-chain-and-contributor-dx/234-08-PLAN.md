---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 08
type: execute
wave: 2
depends_on: [234-01]
files_modified:
  - .github/workflows/ci.yml
  - test/example/priv/playwright/playwright.config.ts
  - .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
  - test/sigra/planning/phase_234_playwright_inventory_contract_test.exs
autonomous: true
requirements: [DX-04]

must_haves:
  truths:
    - "D-08: the JSON inventory has exactly one row per live tests/*.spec.ts file and each row names at least one workflow, job, seam, event set, explicit command marker, and config/project seam."
    - "D-09: admin-theme.spec.ts and admin-coherence-sweep.spec.ts execute explicitly in the existing admin_behavior shard with --project=chromium and --retries=0."
    - "Inventory/spec sets reconcile in both directions; missing, stale, duplicate, empty-owner, missing job/command/config seam, and changed lane cases fail with named diagnostics."
    - "DX-04 concurrency guarantee: parallel phase execution cannot publish a partial inventory because this plan owns both ci.yml wiring and the complete inventory/guard atomically after 234-01."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-04 concurrency edge: sources do not define partial-write recovery semantics; the plan assumes atomic git commits plus exact-set validation are the guarantee and does not invent runtime locking."
  prohibitions: []
  artifacts:
    - path: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      provides: "Phase 235-consumable spec-to-CI ownership map"
    - path: test/sigra/planning/phase_234_playwright_inventory_contract_test.exs
      provides: "live-universe and lane-seam reconciliation"
  key_links:
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      to: test/example/priv/playwright/tests
      via: "exact normalized spec set"
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      to: .github/workflows/ci.yml
      via: "workflow/job/seam/command markers"
---

<objective>
Account for every Playwright spec in an executable CI lane and resolve the two known orphans without deleting useful coverage (D-08, D-09).

Purpose: browser specs must not remain present while silently unexecuted.
Output: two explicit admin-behavior routes, a complete machine-readable inventory, and a fail-closed reconciliation contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-RESEARCH.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route both useful orphan specs through admin_behavior (D-09)</name>
  <files>.github/workflows/ci.yml, test/example/priv/playwright/playwright.config.ts, test/sigra/planning/phase_234_playwright_inventory_contract_test.exs</files>
  <read_first>
    - `test/example/priv/playwright/tests/admin-theme.spec.ts` — theme, accessibility, session, role/stable-hook, and readiness coverage distinct from flow specs.
    - `test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts` — six-screen seeded behavior contract and static/runtime motion checks.
    - `test/example/priv/playwright/playwright.config.ts` — ADMIN_BEHAVIOR_SPECS and chromium/mobile exclusions.
    - `.github/workflows/ci.yml` — `example_playwright_shard` matrix, shared boot, seeding/warmup, explicit admin_behavior command, retry-zero policy, and terminal aggregator.
    - `test/sigra/planning/phase_232_playwright_economics_test.exs` — exhaustive five-seam/shared-boot and retry-zero contracts.
    - Every file listed in `<files>` — current content/absence before editing.
  </read_first>
  <behavior>
    - "Both orphan filenames appear exactly once in the admin_behavior command and are selected by chromium."
    - "The existing shard still seeds/boots the isolated example app, uses the shared action, and runs retry-zero."
    - "No UI source, snapshot, theme behavior, worker setting, or other seam command changes."
  </behavior>
  <action>
    Add focused RED assertions that extract `example_playwright_shard`, its `admin_behavior` run block, and the `chromium` config. Require both exact filenames, `--project=chromium`, `--retries=0`, shared boot action, and the existing matrix seam; require the terminal `Example Playwright smoke (full lifecycle)` aggregation unchanged.

    Append `tests/admin-theme.spec.ts` and `tests/admin-coherence-sweep.spec.ts` to the existing admin_behavior Playwright argv. Extend `ADMIN_BEHAVIOR_SPECS` to include `admin-coherence-sweep` explicitly; retain existing `admin-theme` matching and every project setting. Do not edit either spec, snapshots, workers, retries, setup/readiness, database ownership, UI implementation, or other lanes.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs test/sigra/planning/phase_232_playwright_economics_test.exs &amp;&amp; cd test/example/priv/playwright &amp;&amp; npx playwright test --list tests/admin-theme.spec.ts tests/admin-coherence-sweep.spec.ts --project=chromium --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - Focused contract is RED before wiring and GREEN after wiring.
    - Both exact filenames occur once in admin_behavior and list successfully under chromium.
    - Phase 232 topology/shared-boot/retry-zero contracts remain green.
    - No UI/spec/snapshot/runtime behavior file is modified.
  </acceptance_criteria>
  <done>Both previously uninvoked, useful admin behavior specs have one explicit existing-lane owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish and reconcile the complete Playwright ownership inventory (D-08, D-09)</name>
  <files>.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json, test/sigra/planning/phase_234_playwright_inventory_contract_test.exs, .github/workflows/ci.yml, test/example/priv/playwright/playwright.config.ts</files>
  <read_first>
    - `test/example/priv/playwright/tests/*.spec.ts` — live spec universe (20 files at planning time; derive again during execution).
    - `.github/workflows/ci.yml` — every explicit Playwright invocation in CI, Pages, recapture, and eval jobs.
    - `.github/workflows/playwright-github-pages.yml` — separate Pages publisher invocations if present.
    - `test/example/priv/playwright/playwright.config.ts` — project/testMatch/testIgnore seams.
    - `test/support/ci/library_test_partitions.exs` — exact missing/stale set diagnostics.
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — observed event/seam ownership.
    - Every file listed in `<files>` — current content/absence before editing.
  </read_first>
  <behavior>
    - "Sorted inventory spec paths equal sorted live glob paths exactly."
    - "Each row has non-empty lanes and every lane's workflow/job/seam/event/command/config tokens resolve."
    - "A synthetic missing/stale/duplicate/unowned/missing-lane case fails with the exact path or token."
  </behavior>
  <action>
    Create `234-PLAYWRIGHT-INVENTORY.json` with schema/version fields and one sorted row per current repository-relative `test/example/priv/playwright/tests/*.spec.ts`. Each lane object must record `workflow`, `job`, `seam`, sorted `events`, an exact stable `command_marker`, `project`, and `config_seam`; list multiple lanes when a spec genuinely runs in PR/non-PR/recapture/Pages surfaces. Use explicit workflow commands as execution truth, not config regex membership alone.

    Extend the ExUnit contract to decode JSON with the available runtime, derive the live glob, compare MapSets both directions, reject duplicate rows and empty lanes, anchored-extract each workflow job, and require every claimed seam/command marker plus project/config seam. Include synthetic mutation cases for missing/stale/duplicate/unowned/missing job/missing command/missing config. Assert the two D-09 filenames resolve to admin_behavior. Keep diagnostics sorted and fail closed on malformed JSON or zero rows.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs &amp;&amp; mix test test/sigra/planning/phase_232_playwright_economics_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Inventory has one and only one row for every live spec and no stale row.
    - Every ownership token resolves to current workflow/config source and every row owns at least one lane.
    - Both known orphans map to `example_playwright_shard` / `admin_behavior` / chromium with retry-zero command evidence.
    - Mutation tests prove missing, stale, duplicate, unowned, and broken-token cases red.
    - JSON is explicitly labeled the Phase 235 GATE-05 input.
  </acceptance_criteria>
  <done>A complete, mechanically enforced spec-to-lane artifact is ready for Phase 235 and cannot drift silently.</done>
</task>

</tasks>

## Artifacts this phase produces

- New path: `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json`.
- New JSON fields: `schema_version`, `generated_from`, `spec`, and lane `workflow`, `job`, `seam`, `events`, `command_marker`, `project`, `config_seam`.
- New path: `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs`.
- Modified CI contract: `admin_behavior` explicitly owns `admin-theme.spec.ts` and `admin-coherence-sweep.spec.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository spec/config inventory → CI coverage claim | Static artifacts claim that browser behavior executes on named managed runners. |
| GitHub workflow/event → Playwright shell argv | Workflow expressions and matrix values select browser commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-05-01 | Repudiation | Playwright inventory | high | mitigate | Exact live-universe equality, non-empty owners, anchored workflow/config resolution, and mutation-tested failure diagnostics. |
| T-234-05-02 | Tampering | workflow Playwright argv | high | mitigate | Literal repository-owned filenames/project/retry flag; no event text or user input is interpolated into command construction. |
| T-234-05-03 | Denial of service | parallel shard topology | medium | mitigate | Reuse existing isolated admin_behavior shard/shared boot and preserve terminal aggregate/timeouts. |
</threat_model>

<verification>
Focused inventory and Phase 232 topology contracts pass; Playwright list proves both added specs resolve under the exact project without a browser sleep or retry.
</verification>

<success_criteria>
DX-04 and D-08/D-09 are complete: every live spec has mechanically resolvable named CI ownership and the two known orphans execute in the existing lane.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-08-SUMMARY.md` when done.
</output>
