---
phase: 234-hygiene-supply-chain-and-contributor-dx
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - test/sigra/planning/phase_234_dependabot_contract_test.exs
autonomous: true
requirements: [DX-03]

must_haves:
  truths:
    - "D-07: Dependabot uniquely covers weekly github-actions at /, mix at /, and npm at /test/example/priv/playwright."
    - "Each configured directory resolves to the expected manifest and lockfile; malformed, duplicate, missing, or stale entries fail with diagnostics."
    - "A missing update PR is never promoted to semantic proof; GitHub update-job evidence is owned by 234-07."
  assumptions:
    - "FLAGGED/UNRESOLVED DX-03 unclassified edge: requirements define three exact ecosystems/directories but no grouping semantics; the plan preserves independent weekly entries and requires GitHub-owned evidence rather than assuming grouping behavior."
  prohibitions: []
  artifacts:
    - path: test/sigra/planning/phase_234_dependabot_contract_test.exs
      provides: "offline Dependabot ecosystem/directory/manifest reconciliation"
  key_links:
    - from: .github/dependabot.yml
      to: mix.exs
      via: "mix ecosystem at repository root"
    - from: .github/dependabot.yml
      to: test/example/priv/playwright/package-lock.json
      via: "npm ecosystem at Playwright subproject"
---

<objective>
Extend Dependabot across all locked dependency ecosystems and make configuration drift fail locally (D-07).

Purpose: Hex and npm dependencies must not silently age outside the existing Actions updater.
Output: three weekly entries and a hermetic structure/filesystem contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-RESEARCH.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add and reconcile the three weekly Dependabot entries (D-07)</name>
  <files>.github/dependabot.yml, test/sigra/planning/phase_234_dependabot_contract_test.exs</files>
  <reversibility rating="reversible">Rollback target: restore dependabot.yml and its contract from the task-start commit. Pre-change gate: preserve/hash the existing github-actions block and observe missing Mix/npm cases RED. Post-change gate: exact three-tuple validation passes before advancing.</reversibility>
  <read_first>
    - `.github/dependabot.yml` — existing github-actions weekly entry, labels, and prefix.
    - `mix.exs` and `mix.lock` — root Mix manifest/lock targets.
    - `test/example/priv/playwright/package.json` and `package-lock.json` — npm target pair.
    - `test/sigra/planning/phase_233_library_economics_contract_test.exs` — exact-set diagnostics pattern.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-CONTEXT.md` — D-07 exact identifiers/directories and proof split.
    - Every file listed in `<files>` — current content/absence before editing.
  </read_first>
  <behavior>
    - "Exactly one entry exists for each locked ecosystem/directory pair and every interval is weekly."
    - "Wrong ecosystem names, directories, missing manifests/locks, duplicates, or absent existing github-actions coverage fail."
  </behavior>
  <action>
    Write the focused async ExUnit contract first and observe RED. Parse the small locked YAML surface fail closed without adding a dependency: require `version: 2`, extract update blocks, normalize quoted scalars, compare the exact ecosystem/directory set, require weekly schedule, and assert the root/subproject manifest-lock pairs exist. The parser must reject an unrecognized or incomplete block instead of skipping it.

    Preserve the existing `github-actions` entry. Add weekly `mix` at `/` and weekly `npm` at `/test/example/priv/playwright`; use the existing `dependencies` label and ecosystem-appropriate commit prefixes (`deps` for mix/npm, existing `ci` for actions). Do not add multi-ecosystem grouping, registries, ignore rules, or packages.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_dependabot_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The contract is observed RED before entries and GREEN after them.
    - Exact set is `github-actions:/`, `mix:/`, `npm:/test/example/priv/playwright`, each unique and weekly.
    - `mix.exs`, `mix.lock`, `package.json`, and `package-lock.json` existence is asserted.
    - Existing Actions coverage remains; no grouping/registry/ignore/package dependency is introduced.
  </acceptance_criteria>
  <done>All three dependency ecosystems are configured and locally guarded with exact filesystem ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Demonstrate fail-closed Dependabot parsing (D-07)</name>
  <files>test/sigra/planning/phase_234_dependabot_contract_test.exs</files>
  <reversibility rating="reversible">Rollback target: restore the parser contract from the task-start commit. Pre-change gate: the production YAML parses to three rows. Post-change gate: every malformed fixture fails with a non-empty named diagnostic before advancing.</reversibility>
  <read_first>
    - `test/sigra/planning/phase_234_dependabot_contract_test.exs` — Task 1 parser and production assertions.
    - `.github/dependabot.yml` — positive subject.
    - `test/support/ci/library_test_partitions.exs` — missing/stale/duplicate diagnostics style.
    - Every file listed in `<files>` — current content before editing.
  </read_first>
  <action>
    Add table-driven malformed fixtures covering missing directory, missing interval, duplicate ecosystem/directory, unknown ecosystem, malformed indentation/block, and nonexistent manifest directory. Assert each returns a non-empty named diagnostic. Add a positive production count assertion so parsing zero blocks cannot pass.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_234_dependabot_contract_test.exs &amp;&amp; git diff --check -- .github/dependabot.yml test/sigra/planning/phase_234_dependabot_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every malformed fixture fails and identifies the offending/missing ecosystem or field.
    - The positive subject reports three parsed entries.
    - Offline proof is labeled structural; no test claims GitHub processed an update job.
  </acceptance_criteria>
  <done>The offline configuration gate fails closed and leaves service-owned proof for explicit live evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- New path: `test/sigra/planning/phase_234_dependabot_contract_test.exs`.
- New contract fields: exact ecosystem/directory tuples, weekly interval, manifest path, and lockfile path.
- Modified service config: weekly `mix` and Playwright `npm` entries alongside preserved `github-actions`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository Dependabot YAML → GitHub-managed updater | GitHub parses repository configuration and opens dependency PRs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-234-04-01 | Tampering | dependency update coverage | high | mitigate | Exact three-entry set and manifest/lock reconciliation prevent silent ecosystem removal or directory drift. |
| T-234-04-02 | Repudiation | YAML parser/evidence | medium | mitigate | Malformed fixtures and positive count make parsing fail closed; service proof remains explicitly separate. |
| T-234-04-03 | Information disclosure | private registries/secrets | low | accept | No registry or credential configuration is added; updater uses public existing manifests. |

## ASVS L1 Control Mapping and Automated Verification

| ASVS control intent | Config-parser control | Automated assertion |
|---|---|---|
| V5.1 input validation | Parse the locked YAML subset fail closed and validate ecosystem/directory fields against exact allowed values. | Table-driven malformed, duplicate, unknown, incomplete, indentation, and nonexistent-directory fixtures must all fail with named diagnostics. |
| V7.1 evidence integrity | Distinguish local structural proof from GitHub service proof. | Contract rejects zero rows and contains no assertion that an absent PR proves successful processing. |
</threat_model>

<verification>
Focused contract passes production and malformed fixtures; live GitHub processing remains a declared downstream evidence requirement, never inferred from absent PRs.
</verification>

<success_criteria>
DX-03 structural coverage is complete per D-07, with exact offline proof and no false semantic claim.
</success_criteria>

<output>
Create `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-07-SUMMARY.md` when done.
</output>
