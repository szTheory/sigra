---
phase: 181-ratified-logo-system-buildout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/logo-primary.svg
  - brandbook/logo-primary-dark.svg
  - brandbook/logo-primary-subtitle.svg
  - brandbook/logo-mark.svg
  - brandbook/logo-monochrome.svg
  - brandbook/favicon.svg
autonomous: true
requirements:
  - BRAND2-08

must_haves:
  truths:
    - "logo-primary.svg, logo-primary-dark.svg, logo-primary-subtitle.svg, logo-mark.svg, logo-monochrome.svg, and favicon.svg all exist at their canonical brandbook/ paths with D4 Linked Rail geometry"
    - "Every file parses as valid XML (xmllint exits 0)"
    - "Every typemark SVG <desc> contains 'Space Grotesk' font provenance"
    - "favicon.svg contains a prefers-color-scheme media query"
    - "logo-primary-dark.svg and logo-mark.svg contain no ember-700 (#c2410c) in any fill attribute (dark/auto-theme files must use ember-300 #fdba74 via CSS)"
    - "The favicon passes the 16px kill test: executor reads /tmp/v2-renders/ PNGs and both the ink stem+foot and the ember block are visually distinct elements"
    - "logo-primary.svg renders legibly at 54px topbar scale without clipping"
    - "logo-monochrome.svg ember rect uses #151515 (solid ink), not #c2410c"
  artifacts:
    - path: "brandbook/logo-primary.svg"
      provides: "D4 Linked Rail typemark, light surface"
      contains: "rail-tittle"
    - path: "brandbook/logo-primary-dark.svg"
      provides: "D4 Linked Rail typemark, dark surface"
      contains: "f4f1eb"
    - path: "brandbook/logo-primary-subtitle.svg"
      provides: "D4 Linked Rail typemark with subtitle text, light surface"
      contains: "Phoenix auth that ships"
    - path: "brandbook/logo-mark.svg"
      provides: "D4 abstract rail glyph mark, auto light/dark"
      contains: "prefers-color-scheme"
    - path: "brandbook/logo-monochrome.svg"
      provides: "D4 typemark in single ink"
      contains: "rail-tittle"
    - path: "brandbook/favicon.svg"
      provides: "D4 abstract rail glyph favicon with dark-mode media query"
      contains: "Sigra</title>"
  key_links:
    - from: "brandbook/logo-primary-dark.svg"
      to: "ember-300 fill"
      via: "hardcoded fill attribute"
      pattern: "fdba74"
    - from: "brandbook/favicon.svg"
      to: "prefers-color-scheme"
      via: "<style> block"
      pattern: "prefers-color-scheme: dark"
    - from: "brandbook/logo-monochrome.svg"
      to: "single-ink ember rect"
      via: "rail-tittle fill"
      pattern: "rail-tittle.*fill=\"#151515\""
---

<objective>
Produce the six typemark and mark SVG assets of the D4 Linked Rail production set, render-verify each at its required display sizes, and commit all files.

Purpose: These six files are the foundation of the v2 logo system. They derive directly from the ratified round-4 source geometry — the only authoring work per file is updating accessibility text and fills per variant. Every file must be render-verified before commit.

Output: brandbook/logo-primary.svg, brandbook/logo-primary-dark.svg, brandbook/logo-primary-subtitle.svg, brandbook/logo-mark.svg, brandbook/logo-monochrome.svg, brandbook/favicon.svg — all containing D4 geometry, passing xmllint, and verified at intended sizes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
@/Users/jon/projects/sigra/.planning/phases/181-ratified-logo-system-buildout/181-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/181-ratified-logo-system-buildout/181-RESEARCH.md
@/Users/jon/projects/sigra/.planning/phases/181-ratified-logo-system-buildout/181-PATTERNS.md
</context>

<interfaces>
<!-- Source SVGs to read before writing production files. Read each one fully before authoring. -->

Source: brandbook/logo-options/round-4/d4-linked-rail-typemark.svg
  - viewBox: "0 220 2410 1026"
  - <g id="glyphs" fill="#151515"> with 5 path elements (g-0 through g-4)
  - <rect id="rail-tittle" x="557" y="246" width="200" height="200" fill="#c2410c" />
  - Key invariant: g-2 path includes g-tail terminating at x=557; rail-tittle left edge also at x=557

Source: brandbook/logo-options/round-4/d4-linked-rail-typemark-dark.svg
  - Identical structure to light variant
  - Glyph fill: #f4f1eb (NOT #151515)
  - Ember rect fill: #fdba74 (NOT #c2410c)

Source: brandbook/logo-options/round-4/d4-linked-rail-favicon.svg
  - viewBox: "-70 -60 1040 1040" (square: width=1040 height=1040)
  - <style>#glyphs { fill: #151515; } #rail-block { fill: #c2410c; } @media (prefers-color-scheme: dark) { #glyphs { fill: #f4f1eb; } #rail-block { fill: #fdba74; } }</style>
  - <g id="glyphs"> with two rects: stem (x=540 y=360 w=180 h=580) and foot (x=180 y=760 w=540 h=180)
  - <rect id="rail-block" x="400" y="-20" width="320" height="320" />

Accessibility shell required on every file:
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="..." role="img" aria-labelledby="title desc">
    <title id="title">...</title>
    <desc id="desc">...</desc>

Render harness: scripts/brand/critique-render.mjs
  Usage: node scripts/brand/critique-render.mjs /tmp/v2-harness.html /tmp/v2-renders/
  Harness HTML pattern: file:// inline img tags with explicit sizes; background #f6f5f2 (light test)
  Render profiles: 16px-favicon (64×64), 32px (128×128), 54px-topbar (800×160), hero (1200×300)
  Both light and dark color schemes per viewport.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write logo-primary.svg, logo-primary-dark.svg, and logo-primary-subtitle.svg</name>
  <read_first>
    brandbook/logo-options/round-4/d4-linked-rail-typemark.svg (full file — copy glyph paths verbatim)
    brandbook/logo-options/round-4/d4-linked-rail-typemark-dark.svg (full file — copy glyph paths verbatim)
    brandbook/logo-primary.svg (v1, to be overwritten — check <title>/<desc> accessibility pattern, then discard v1 content)
    .planning/phases/181-ratified-logo-system-buildout/181-PATTERNS.md sections for logo-primary, logo-primary-dark, logo-primary-subtitle
  </read_first>
  <files>brandbook/logo-primary.svg, brandbook/logo-primary-dark.svg, brandbook/logo-primary-subtitle.svg</files>
  <action>
    Write three SVG files. The only differences from the round-4 source are the accessibility text (title, desc) and, for the subtitle variant, an extended viewBox and appended text element. Do NOT re-outline paths; copy the path d= values from the source without modification.

    logo-primary.svg:
    - Copy all content from d4-linked-rail-typemark.svg.
    - Update <title> to: "Sigra primary logo"
    - Update <desc> to: "The Sigra D4 Linked Rail typemark for light surfaces. Font: Space Grotesk v2.0 (OFL) wght=700, outlined with opentype.js 2.0.0. Ember-700 (#c2410c) rail tittle and g-tail bracket. See brandbook/README.md for usage rules."
    - All path d= attributes: identical to source (do not alter by even one character).
    - viewBox stays "0 220 2410 1026".

    logo-primary-dark.svg:
    - Copy all content from d4-linked-rail-typemark-dark.svg.
    - Update <title> to: "Sigra primary logo for dark surfaces"
    - Update <desc> to: "The Sigra D4 Linked Rail typemark for dark surfaces. Font: Space Grotesk v2.0 (OFL) wght=700, outlined with opentype.js 2.0.0. Ember-300 (#fdba74) rail tittle on dark. See brandbook/README.md for usage rules."
    - Glyph fill MUST be #f4f1eb; ember rect fill MUST be #fdba74.
    - After writing, run: grep '#c2410c' brandbook/logo-primary-dark.svg — must return empty (0 hits).

    logo-primary-subtitle.svg:
    - Start from d4-linked-rail-typemark.svg glyph content (identical light surface fills and paths).
    - Change viewBox to "0 220 2410 1380" (extends bottom by 354 units to accommodate subtitle; tune slightly after render if needed — keep x-origin 0, y-origin 220, width 2410).
    - Update <title> to: "Sigra primary logo with subtitle"
    - Update <desc> to: "The Sigra D4 Linked Rail typemark with tagline for light surfaces. Font: Space Grotesk v2.0 (OFL) wght=700, outlined with opentype.js 2.0.0. Subtitle in system font. See brandbook/README.md for usage rules."
    - After the glyph group and ember rect, add a subtitle <text> element:
      x="60" y="1320"
      font-family="ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"
      font-size="200"
      fill="#686868"
      letter-spacing="0"
      Content: "Phoenix auth that ships"
    - Do NOT run outline-wordmark.mjs for the subtitle text. It uses live SVG <text> with system font fallback, not outlined paths.

    Render verification (build harness, run, Read PNGs, self-critique before committing):
    - Write a harness HTML to /tmp/sigra-plan01-task1-harness.html that shows all three files at:
      54px-topbar (logo-primary, logo-primary-dark as inline img height=54),
      hero (logo-primary, logo-primary-dark, logo-primary-subtitle at width=600),
      light background (#f6f5f2) and dark background (#151515) sections.
    - Use file:// absolute paths to the brandbook/ files.
    - Run: node scripts/brand/critique-render.mjs /tmp/sigra-plan01-task1-harness.html /tmp/sigra-plan01-task1-renders/
    - Read every PNG in /tmp/sigra-plan01-task1-renders/ using the Read tool.
    - Self-critique against the brief's rubric: wordmark legible at 54px, no clipping, subtitle text legible at hero scale, dark variant uses warm white wordmark + amber ember, monochrome distinction not applicable to these files.
    - If any render fails critique: fix the SVG, re-run, re-read, re-critique. Do not commit until all three pass.
  </action>
  <verify>
    <automated>
      xmllint --noout brandbook/logo-primary.svg brandbook/logo-primary-dark.svg brandbook/logo-primary-subtitle.svg 2>&amp;1 | grep -c 'error' | grep -q '^0$' &amp;&amp; echo "PASS: all 3 parse"
      grep -l 'Space Grotesk' brandbook/logo-primary.svg brandbook/logo-primary-dark.svg brandbook/logo-primary-subtitle.svg | wc -l | grep -q '3' &amp;&amp; echo "PASS: font provenance present in all 3"
      grep -c '#c2410c' brandbook/logo-primary-dark.svg | grep -q '^0$' &amp;&amp; echo "PASS: no ember-700 in dark variant"
      grep -c 'Phoenix auth that ships' brandbook/logo-primary-subtitle.svg | grep -q '^1$' &amp;&amp; echo "PASS: subtitle text present"
    </automated>
    <human-check>
      Read /tmp/sigra-plan01-task1-renders/ PNGs. Verify:
      (a) logo-primary at 54px: "sigra" wordmark fully legible, no clipping, ember block distinct above i.
      (b) logo-primary-dark at hero: warm white wordmark + amber ember on dark background.
      (c) logo-primary-subtitle at hero: subtitle "Phoenix auth that ships" below the wordmark, legible grey text.
    </human-check>
  </verify>
  <done>Three SVG files exist at canonical paths; xmllint passes on all three; font provenance present; dark variant has zero #c2410c hits; subtitle text present; all PNGs read and self-critiqued per the render rubric.</done>
</task>

<task type="auto">
  <name>Task 2: Write logo-mark.svg, logo-monochrome.svg, and favicon.svg</name>
  <read_first>
    brandbook/logo-options/round-4/d4-linked-rail-favicon.svg (full file — source for mark and favicon)
    brandbook/logo-options/round-4/d4-linked-rail-typemark.svg (glyph paths needed for monochrome)
    brandbook/logo-mark.svg (v1, to be overwritten — inspect pattern only)
    brandbook/logo-monochrome.svg (v1, to be overwritten — inspect pattern only, do NOT copy opacity trick)
    brandbook/favicon.svg (v1, to be overwritten — inspect pattern only)
    .planning/phases/181-ratified-logo-system-buildout/181-PATTERNS.md sections for logo-mark, logo-monochrome, favicon
  </read_first>
  <files>brandbook/logo-mark.svg, brandbook/logo-monochrome.svg, brandbook/favicon.svg</files>
  <action>
    Write three SVG files. All three derive from the favicon source for mark geometry; monochrome additionally takes glyph paths from the typemark source.

    logo-mark.svg:
    - Copy all content from d4-linked-rail-favicon.svg.
    - KEEP the prefers-color-scheme <style> block (the mark is used as <img> in HTML; the media query makes it auto-theme without needing a separate dark variant).
    - Update <title> to: "Sigra mark"
    - Update <desc> to: "Sigra D4 Linked Rail free-standing mark. Abstract rail glyph: ink stem, leftward foot, ember-700 (#c2410c) rail block on light; ember-300 (#fdba74) on dark via prefers-color-scheme. Use as UI accent on any surface."
    - viewBox MUST remain exactly "-70 -60 1040 1040" — the 3rd and 4th values are both 1040 (square). Do not alter.
    - id="glyphs" and id="rail-block" must remain on their respective elements (the CSS selects by id).

    logo-monochrome.svg:
    - Start from d4-linked-rail-typemark.svg (same viewBox "0 220 2410 1026", same 5 glyph paths).
    - Glyph group fill: #151515 (same as light typemark).
    - Ember rect fill: #151515 (NOT #c2410c — this collapses ember to ink, creating the monochrome variant).
    - No prefers-color-scheme, no media query — single-ink file used in restricted-color contexts.
    - Update <title> to: "Sigra monochrome logo"
    - Update <desc> to: "Sigra D4 Linked Rail typemark in single ink for restricted-color contexts. No color distinction between wordmark and rail accents. Font: Space Grotesk v2.0 (OFL) wght=700, outlined with opentype.js 2.0.0."
    - After writing, verify: grep -c '#c2410c' brandbook/logo-monochrome.svg must return 0. grep -c '#fdba74' brandbook/logo-monochrome.svg must return 0. Both ember values must be absent.

    favicon.svg:
    - Copy all content from d4-linked-rail-favicon.svg.
    - KEEP the <style> media query block verbatim (correct production behavior for browser tab rendering).
    - KEEP viewBox "-70 -60 1040 1040" exactly.
    - KEEP all rect coordinates and id attributes unchanged.
    - Update <title> to: "Sigra"
    - Update <desc> to: "Sigra D4 Linked Rail favicon mark. Abstract rail glyph: ink stem, leftward foot, ember-700 (#c2410c) rail block on light; adapts to dark mode via prefers-color-scheme."

    Render verification:
    - Write harness HTML to /tmp/sigra-plan01-task2-harness.html:
      Favicon section: favicon.svg as img at 16px (kill test), 32px — on both light (#f6f5f2) and dark (#151515) backgrounds (use two separate divs, not emulateMedia).
      Mark section: logo-mark.svg at 32px and hero (128px height), on both light and dark backgrounds.
      Monochrome section: logo-monochrome.svg at 54px and hero, on light background only (single-ink — dark not applicable).
    - Use file:// absolute paths.
    - Run: node scripts/brand/critique-render.mjs /tmp/sigra-plan01-task2-harness.html /tmp/sigra-plan01-task2-renders/
    - Read every PNG in /tmp/sigra-plan01-task2-renders/ using the Read tool.
    - Kill test criteria (MUST PASS before commit):
      (a) favicon at 16px light: ink stem+foot group and ember block are visually two distinct elements.
      (b) favicon at 16px dark: ember block switches to amber (#fdba74) via media query — check the dark section div render.
      (c) logo-mark at 32px: mark elements readable.
      (d) logo-monochrome at 54px: rail block above i reads as spatially distinct from the i stem even though both are solid ink (#151515). If they appear as a merged blob, increase the viewBox top padding by ~50 units or verify y=246 rect position has adequate separation from cap height at y~490.
    - If any render fails: fix the SVG, re-run, re-read, re-critique. Do not commit until all pass.
  </action>
  <verify>
    <automated>
      xmllint --noout brandbook/logo-mark.svg brandbook/logo-monochrome.svg brandbook/favicon.svg 2>&amp;1 | grep -c 'error' | grep -q '^0$' &amp;&amp; echo "PASS: all 3 parse"
      grep -c 'prefers-color-scheme' brandbook/favicon.svg | grep -qv '^0$' &amp;&amp; echo "PASS: favicon has media query"
      grep -c 'prefers-color-scheme' brandbook/logo-mark.svg | grep -qv '^0$' &amp;&amp; echo "PASS: mark has media query"
      grep -c '#c2410c' brandbook/logo-monochrome.svg | grep -q '^0$' &amp;&amp; echo "PASS: no ember-700 in monochrome"
      grep -c '#fdba74' brandbook/logo-monochrome.svg | grep -q '^0$' &amp;&amp; echo "PASS: no ember-300 in monochrome"
      grep -v '^#' brandbook/favicon.svg | grep -c 'rail-tittle\|rail-block' | grep -qv '^0$' &amp;&amp; echo "PASS: favicon has rail-block id"
    </automated>
    <human-check>
      Read /tmp/sigra-plan01-task2-renders/ PNGs. Verify:
      (a) favicon 16px light PNG: two distinct visual elements (stem+foot vs ember block). Kill test — DO NOT skip.
      (b) favicon dark section: ember block is amber, not brick-red.
      (c) monochrome 54px: rail block spatially distinct from i stem (solid ink, position-based separation).
    </human-check>
  </verify>
  <done>Three SVG files exist at canonical paths; xmllint passes; favicon has prefers-color-scheme; logo-mark has prefers-color-scheme; logo-monochrome has zero hits for both #c2410c and #fdba74; all renders read and self-critiqued — favicon 16px kill test passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system read | Round-4 source SVGs are read inputs; no external network calls |
| Harness rendering | Playwright opens file:// URLs; no remote content loaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-01 | Tampering | SVG path data copy | mitigate | Read source file in full before writing; grep verification confirms key geometry attributes (rail-tittle, viewBox) are preserved |
| T-181-02 | Information Disclosure | Font binaries | mitigate | `git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'` must return 0; scripts/brand/fonts/ is gitignored |
| T-181-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase; toolchain pre-established in Phase 179 |
</threat_model>

<verification>
After both tasks complete:

```bash
# All 6 files exist
ls brandbook/{logo-primary,logo-primary-dark,logo-primary-subtitle,logo-mark,logo-monochrome,favicon}.svg

# All parse as valid XML
find brandbook -maxdepth 1 -name '*.svg' | xargs -n1 xmllint --noout

# Font provenance in all 6 typemark/mark files
grep -l 'Space Grotesk' brandbook/{logo-primary,logo-primary-dark,logo-primary-subtitle,logo-mark,logo-monochrome,favicon}.svg | wc -l
# Expected: 6

# Dark variant has no ember-700
grep -c '#c2410c' brandbook/logo-primary-dark.svg
# Expected: 0

# Monochrome has no color fills
grep -c '#c2410c' brandbook/logo-monochrome.svg; grep -c '#fdba74' brandbook/logo-monochrome.svg
# Expected: 0, 0

# Favicon and mark have media query
grep -l 'prefers-color-scheme' brandbook/favicon.svg brandbook/logo-mark.svg | wc -l
# Expected: 2

# No font binaries committed
git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2' | wc -l
# Expected: 0

# No SVG exceeds 250KB
find brandbook -maxdepth 1 -name '*.svg' -size +250k -print
# Expected: empty
```
</verification>

<success_criteria>
- logo-primary.svg: D4 Linked Rail typemark, light surface, correct viewBox, font provenance in desc, xmllint clean, render-verified at 54px and hero.
- logo-primary-dark.svg: same geometry with #f4f1eb glyphs and #fdba74 ember rect; zero #c2410c hits; render-verified.
- logo-primary-subtitle.svg: typemark + "Phoenix auth that ships" subtitle in grey system-font text; extended viewBox; render-verified at hero.
- logo-mark.svg: D4 abstract rail glyph with prefers-color-scheme; viewBox "-70 -60 1040 1040"; render-verified at 32px and hero.
- logo-monochrome.svg: typemark with both glyph group and rail-tittle filled #151515; zero color hits; render-verified at 54px (rail block spatially distinct from i stem).
- favicon.svg: D4 abstract rail glyph, <title>="Sigra", prefers-color-scheme; 16px kill test passed (two distinct elements visible).
</success_criteria>

<output>
Create .planning/phases/181-ratified-logo-system-buildout/181-01-SUMMARY.md when done.
</output>
