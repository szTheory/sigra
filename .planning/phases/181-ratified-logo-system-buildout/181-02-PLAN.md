---
phase: 181-ratified-logo-system-buildout
plan: 02
type: execute
wave: 2
depends_on:
  - 181-01
files_modified:
  - brandbook/social-card.svg
  - brandbook/social-card-dark.svg
  - brandbook/README.md
  - brandbook/logo-options/archive-v1/README.md
  - brandbook/logo-options/archive-v1/logo-primary.svg
  - brandbook/logo-options/archive-v1/logo-primary-dark.svg
  - brandbook/logo-options/archive-v1/logo-mark.svg
  - brandbook/logo-options/archive-v1/logo-monochrome.svg
  - brandbook/logo-options/archive-v1/favicon.svg
  - brandbook/logo-options/archive-v1/social-card.svg
autonomous: true
requirements:
  - BRAND2-08

must_haves:
  truths:
    - "social-card.svg and social-card-dark.svg exist at brandbook/ with D4 mark geometry, OG-standard viewBox 0 0 1200 630, and no CDN font links"
    - "Dark social card uses #fdba74 ember accent and #f4f1eb glyph fills; no #c2410c in the mark or glyph fills"
    - "brandbook/logo-options/archive-v1/ contains the six v1 files with a deprecation README"
    - "brandbook/index.html img src references remain live (all canonical brandbook/*.svg paths resolve to v2 content)"
    - "brandbook/README.md has a Logo System Usage Rules section with clearspace rule, minimum-sizes table, and ≥4 misuse examples"
    - "Space Grotesk v2.0 font provenance row is present in the Font Provenance table of brandbook/README.md"
    - "Social cards render legibly at thumbnail scale (600×315); wordmark and tagline readable"
  artifacts:
    - path: "brandbook/social-card.svg"
      provides: "D4 Linked Rail light OG social card"
      contains: "1200"
    - path: "brandbook/social-card-dark.svg"
      provides: "D4 Linked Rail dark OG social card"
      contains: "171614"
    - path: "brandbook/logo-options/archive-v1/README.md"
      provides: "v1 deprecation notice"
      contains: "Do not use"
    - path: "brandbook/README.md"
      provides: "Clearspace, min-size, misuse rules"
      contains: "Misuse"
  key_links:
    - from: "brandbook/social-card-dark.svg"
      to: "ember-300 accent"
      via: "fill attribute on mark rects"
      pattern: "fdba74"
    - from: "brandbook/logo-options/archive-v1/"
      to: "six v1 SVG files"
      via: "git mv (copy to archive path + overwrite canonical)"
      pattern: "archive-v1"
    - from: "brandbook/index.html"
      to: "brandbook/*.svg"
      via: "img src attributes"
      pattern: "src=\"(logo-primary|logo-primary-dark|logo-mark|favicon)\\.svg\""
---

<objective>
Produce the two social card SVGs, archive the six v1 assets atomically (so brandbook/index.html img src references never break), and add usage rules documentation to brandbook/README.md.

Purpose: Completes BRAND2-08 — all eight production SVGs exist and are verified, v1 assets are archived with a deprecation note, and clearspace/min-size/misuse rules are documented. The archive must be atomic: old content moves to archive-v1/ and new v2 content lands at the same canonical paths in a single commit.

Output: brandbook/social-card.svg, brandbook/social-card-dark.svg, brandbook/logo-options/archive-v1/{6 files + README.md}, brandbook/README.md (updated).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
@/Users/jon/projects/sigra/.planning/phases/181-ratified-logo-system-buildout/181-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/181-ratified-logo-system-buildout/181-RESEARCH.md
@/Users/jon/projects/sigra/.planning/phases/181-ratified-logo-system-buildout/181-PATTERNS.md
@/Users/jon/projects/sigra/.planning/phases/181-ratified-logo-system-buildout/181-01-SUMMARY.md
</context>

<interfaces>
<!-- Read these files before authoring -->

Source: brandbook/social-card.svg (v1 — read the ENTIRE file before writing v2)
  - viewBox: "0 0 1200 630" with explicit width="1200" height="630"
  - <defs><style> with CSS classes: .bg .panel .ink .muted .accent .strong .warm .soft .type .mono
  - Background rect: class="bg" (fill #f6f5f2)
  - Top bar: <path d="M0 0h1200v22H0z" fill="#151515"/>
  - Panel: <rect x="72" y="70" width="1056" height="488" rx="24" class="panel"/>
  - v1 mark geometry (to be replaced with D4 abstract glyph)
  - Text elements using .type and .mono classes
  - No CDN links — system fonts only

Source: brandbook/logo-primary.svg (v2, just written in Plan 01 — read to verify it has D4 geometry before archiving v1)

Source: brandbook/README.md (existing — read full file before adding sections)

Archive strategy (CRITICAL — atomicity constraint):
  Do NOT use git mv command-line for the archive. Instead:
  1. Read each v1 canonical file.
  2. Write its content to brandbook/logo-options/archive-v1/{filename}.
  3. Then overwrite the canonical path with v2 content (already done in Plan 01 for the 5 typemark/mark/favicon files; only social-card.svg needs archive-then-replace in this plan).
  This preserves git history naturally (git sees the archive path as a new addition and the canonical path as modified).
  Do NOT commit the archive without the canonical v2 replacement in the same commit — brandbook/index.html must never point to a missing file.

D4 abstract rail glyph geometry (for embedding in social cards):
  Stem:   <rect x="540" y="360" width="180" height="580" />
  Foot:   <rect x="180" y="760" width="540" height="180" />
  Block:  <rect x="400" y="-20" width="320" height="320" />
  (These are the 1040-unit viewBox coordinates from d4-linked-rail-favicon.svg)
  Scale down with a transform for social card embedding.
  Use unique IDs (sc-glyphs, sc-rail-block) to prevent id collision in HTML pages.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write social-card.svg (light) and social-card-dark.svg</name>
  <read_first>
    brandbook/social-card.svg (v1, FULL FILE — read before writing v2 to preserve composition)
    brandbook/logo-options/round-4/d4-linked-rail-typemark.svg (for D4 glyph path data — embed inline in social card)
    .planning/phases/181-ratified-logo-system-buildout/181-PATTERNS.md sections for social-card.svg and social-card-dark.svg
  </read_first>
  <files>brandbook/social-card.svg, brandbook/social-card-dark.svg</files>
  <action>
    Write two OG-standard social card SVGs. Both use the exact v1 layout composition (background, topbar, panel, tagline, code snippet, right panel) but replace the v1 mark geometry with the D4 abstract rail glyph.

    social-card.svg (v2, light surface):
    - Root: <svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630" role="img" aria-labelledby="title desc">
    - <title id="title">Sigra social preview card</title>
    - <desc id="desc">A social preview card for Sigra featuring the D4 Linked Rail typemark.</desc>
    - Preserve all CSS classes from v1 (.bg, .panel, .ink, .muted, .accent, .strong, .warm, .soft, .type, .mono). Do NOT add CDN links or @import.
    - Background: <rect class="bg" width="1200" height="630"/>
    - Top bar: <path d="M0 0h1200v22H0z" fill="#151515"/>
    - Panel: <rect x="72" y="70" width="1056" height="488" rx="24" class="panel"/>
    - Left panel "Sigra" wordmark: <text x="186" y="154" class="type ink" font-size="56" font-weight="850">Sigra</text>
    - D4 mark in the upper-left area (near wordmark): embed the abstract glyph at roughly 60–64px rendered height using a scale transform. Use unique IDs sc-glyphs and sc-rail-block. Example: <g transform="translate(104 106) scale(0.075)"><g id="sc-glyphs" fill="#151515"><rect x="540" y="360" width="180" height="580"/><rect x="180" y="760" width="540" height="180"/></g><rect id="sc-rail-block" x="400" y="-20" width="320" height="320" fill="#c2410c"/></g>
    - Tagline (read from v1 and preserve or adapt for brand voice): retain the v1 tagline lines verbatim if they match current brand voice; acceptable to use "Auth you can keep patching after install." per v1 prose OR "Phoenix auth that ships" if brand-book.md voice section recommends it. Check brandbook/brand-book.md voice section during read phase. Do not invent new copy.
    - Code snippet block: <g transform="translate(104 448)"><rect width="510" height="64" rx="10" fill="#151515"/><text x="24" y="40" class="mono" fill="#f4f1eb" font-size="22">mix sigra.install Accounts User users</text></g>
    - Right panel: <g transform="translate(768 128)"><rect x="0" y="0" width="286" height="356" rx="18" class="soft"/><g transform="translate(23 20) scale(0.24)"><g fill="#151515"><rect x="540" y="360" width="180" height="580"/><rect x="180" y="760" width="540" height="180"/></g><rect x="400" y="-20" width="320" height="320" fill="#c2410c"/></g></g>
    - Scale values (0.075 and 0.24) are starting points — tune after render so the mark reads cleanly at both positions.

    social-card-dark.svg (v2, dark surface):
    - Mirror the entire light card structure.
    - Override CSS classes for dark: .bg → fill: #171614; .panel → fill: #1e1c1a; stroke: rgba(244,241,235,0.10); .ink → fill: #f4f1eb; .muted → fill: #a0998f; .accent → fill: #fdba74; .strong → fill: #c2410c; .soft → fill: #2a2520.
    - Top bar fill: #f4f1eb (light bar on dark card) OR keep #151515 and reduce opacity — your judgment, pick what looks best after render.
    - D4 mark fills: glyphs fill="#f4f1eb"; ember rail-block fill="#fdba74" (ember-300 on dark).
    - Pitfall guard: after writing, run grep '#c2410c' brandbook/social-card-dark.svg — must return only one hit (the .strong class definition), never in the mark rects or glyph fills.
    - <title id="title">Sigra social preview card (dark)</title>
    - <desc id="desc">A dark-surface social preview card for Sigra featuring the D4 Linked Rail typemark.</desc>

    Render verification:
    - Write harness HTML to /tmp/sigra-plan02-task1-harness.html:
      Show social-card.svg and social-card-dark.svg each at three sizes: full (1200×630 container), 600×315 (50% / GitHub thumbnail scale), 300×158 (25% small thumbnail).
      Use file:// absolute paths.
    - Run: node scripts/brand/critique-render.mjs /tmp/sigra-plan02-task1-harness.html /tmp/sigra-plan02-task1-renders/
    - Read every PNG in /tmp/sigra-plan02-task1-renders/ using the Read tool.
    - Thumbnail legibility test (self-critique before committing):
      (a) At 600×315: D4 mark visible in left and right panels, "Sigra" wordmark readable, tagline readable, code block present.
      (b) Dark card at 600×315: light wordmark on dark background, amber ember accents, no invisible elements.
      (c) D4 mark in the right panel: the three geometric elements (stem, foot, ember block) are distinguishable at thumbnail scale.
    - If any render fails: fix the SVG, re-run, re-read. Do not commit until both cards pass at 600×315 legibility.
  </action>
  <verify>
    <automated>
      xmllint --noout brandbook/social-card.svg brandbook/social-card-dark.svg 2>&amp;1 | grep -c 'error' | grep -q '^0$' &amp;&amp; echo "PASS: both parse"
      grep -c 'prefers-color-scheme\|@import\|http' brandbook/social-card.svg | grep -q '^0$' &amp;&amp; echo "PASS: no CDN links in light card"
      grep -c 'prefers-color-scheme\|@import\|http' brandbook/social-card-dark.svg | grep -q '^0$' &amp;&amp; echo "PASS: no CDN links in dark card"
      python3 -c "import re,open; content=open('brandbook/social-card-dark.svg').read(); fills=re.findall(r'&lt;rect[^&gt;]*fill=\"#c2410c\"',content); assert not fills, f'ember-700 in mark fill: {fills}'; print('PASS: dark card mark has no ember-700')"
      grep -c '1200' brandbook/social-card.svg | grep -qv '^0$' &amp;&amp; echo "PASS: social-card has width=1200"
      grep -c '171614' brandbook/social-card-dark.svg | grep -qv '^0$' &amp;&amp; echo "PASS: dark card has dark background"
    </automated>
    <human-check>
      Read /tmp/sigra-plan02-task1-renders/ PNGs. Verify:
      (a) social-card.svg at 600×315 (thumbnail): mark, wordmark, tagline, code block all visible and readable.
      (b) social-card-dark.svg at 600×315: dark background, light text, amber accents, mark elements distinguishable.
    </human-check>
  </verify>
  <done>Both social card SVGs exist at brandbook/; xmllint passes; no CDN links; dark card has no #c2410c in mark fills; both render legibly at 600×315 thumbnail scale per visual read of PNGs.</done>
</task>

<task type="auto">
  <name>Task 2: Archive v1 assets and update brandbook/README.md usage rules</name>
  <read_first>
    brandbook/logo-primary.svg (v1 content — read BEFORE overwriting; this IS the v1 content to archive)
    brandbook/logo-primary-dark.svg (v1 — read to archive)
    brandbook/logo-mark.svg (v1 — read to archive)
    brandbook/logo-monochrome.svg (v1 — read to archive)
    brandbook/favicon.svg (v1 — read to archive)
    brandbook/social-card.svg (v1 content — need to confirm the current content before writing v2 ALREADY done in Task 1; at this point canonical path has v2 content — archive the v1 content recorded during Task 1's read phase)
    brandbook/README.md (full file — read before adding sections)
    brandbook/index.html (lines 320-445 — verify img src paths; do not modify)
    .planning/phases/181-ratified-logo-system-buildout/181-PATTERNS.md sections for README.md and archive-v1/README.md
  </read_first>
  <files>
    brandbook/logo-options/archive-v1/README.md,
    brandbook/logo-options/archive-v1/logo-primary.svg,
    brandbook/logo-options/archive-v1/logo-primary-dark.svg,
    brandbook/logo-options/archive-v1/logo-mark.svg,
    brandbook/logo-options/archive-v1/logo-monochrome.svg,
    brandbook/logo-options/archive-v1/favicon.svg,
    brandbook/logo-options/archive-v1/social-card.svg,
    brandbook/README.md
  </files>
  <action>
    CRITICAL ATOMICITY REQUIREMENT: The archive write and the final commit must happen together. At the end of this task, both the archive-v1/ files AND the canonical v2 brandbook/*.svg files (from Plan 01 Task 1+2 and Plan 02 Task 1) must be in a single git commit. This ensures brandbook/index.html never has a broken img src reference.

    Step 1 — Archive v1 content.
    The v1 content is the content of the canonical files BEFORE Plan 01 replaced them. However, since Plan 01 has already overwritten the canonical files with v2 content, the v1 archive must use git to recover the pre-Plan01 content, or the files must be read from the Plan 01 SUMMARY which records what was replaced. Use: git show HEAD~N:brandbook/logo-primary.svg (find the correct commit where v1 content existed) to recover v1 content for archiving. Alternatively: check if Plan 01 SUMMARY records the v1 content or confirms the round-4 source files are the v1-replacements; if the archive content is simply the v1 Rail Accent files that existed before this phase, use git to recover them:

    Recovery command sequence:
      git log --oneline brandbook/logo-primary.svg -- | head -5  (find the commit before Phase 181 changes)
      git show <sha>:brandbook/logo-primary.svg > /tmp/v1-logo-primary-backup.svg

    Repeat for each of the 6 v1 files. Then write those recovered contents to brandbook/logo-options/archive-v1/{filename}.

    Create brandbook/logo-options/archive-v1/ directory if it doesn't exist (Write tool will create it implicitly).

    Write brandbook/logo-options/archive-v1/README.md with the deprecation note:
    ```
    # Sigra Brandbook — v1 Archive

    These files are the original Rail Accent logo assets (v1) superseded by the D4 Linked Rail
    production set in Phase 181 (2026-06-12). They are preserved here for historical reference.

    **Do not use these files.** The canonical production assets are in `brandbook/` (one level up).

    ## Archived Files

    | File | v1 Concept | Superseded by |
    |------|-----------|---------------|
    | logo-primary.svg | Rail Accent staircase mark + Inter Display Black wordmark | brandbook/logo-primary.svg (D4) |
    | logo-primary-dark.svg | Same, dark wordmark #f4f1eb | brandbook/logo-primary-dark.svg (D4) |
    | logo-mark.svg | 3-bar Rail Accent staircase mark | brandbook/logo-mark.svg (D4 abstract rail glyph) |
    | logo-monochrome.svg | Monochrome Rail Accent mark, opacity=0.72 secondary | brandbook/logo-monochrome.svg (D4 solid ink) |
    | favicon.svg | 3-bar Rail Accent mark (same geometry as logo-mark) | brandbook/favicon.svg (D4 abstract rail glyph) |
    | social-card.svg | OG 1200x630 Rail Accent + tagline | brandbook/social-card.svg (D4) |
    ```

    Step 2 — Verify index.html references.
    Run: grep 'src=".*\.svg"' brandbook/index.html
    Confirm the referenced filenames (favicon.svg, logo-primary-dark.svg, logo-primary.svg, logo-mark.svg) are all present as v2 files at brandbook/. Do NOT modify index.html — it is Phase 182 scope.

    Step 3 — Update brandbook/README.md.
    Read the existing README.md in full. Then add the following sections. Do NOT remove or modify any existing content.

    After the "## Logo System" section, insert a new "## Logo System Usage Rules" section:

    ### Clearspace
    Minimum clearspace around the **mark** = 0.25 × rendered mark height.
    Minimum margin around the **typemark** = 0.15 × rendered typemark height.

    ### Minimum Sizes

    | Context | Minimum rendered height | Notes |
    |---------|------------------------|-------|
    | Favicon (kill test) | 16px | Abstract rail glyph; ember block legible from 16px |
    | UI accent (mark only) | 24px | Block + stem readable |
    | Topbar lockup | 32px | Wordmark legible; 54px is the recommended target |
    | Marketing / docs | 120px | Full typemark with subtitle; subtitle text hard to read below 120px |

    ### Misuse Examples

    1. **Incorrect surface:** Using `logo-primary.svg` on a dark background — ink wordmark becomes invisible. Use `logo-primary-dark.svg` on dark surfaces.
    2. **Scaling below minimum:** Displaying the typemark below 32px — "sigra" letters are no longer distinguishable; use `favicon.svg` (the abstract mark) instead.
    3. **Rectangular container:** Wrapping the mark in a box, badge, or pill — the D4 mark is designed for transparent/colored surfaces without containers.
    4. **Wrong ember hue:** Substituting `#c2410c` with a bright orange or red outside hue 15–40° — breaks brand differentiation from Phoenix Framework and Ash.
    5. **Rotating or reflecting:** The rail direction (vertical stem, leftward foot, upper-right block) is the spatial system — rotation breaks the visual metaphor.
    6. **Live text wordmark:** Recreating "sigra" in any font at runtime — the wordmark is outlined paths that must render identically at all sizes without font loading.

    ### Palette Values (v2 / D4)

    | Context | Glyph fill | Ember accent |
    |---------|-----------|--------------|
    | Light surface | `#151515` (ink) | `#c2410c` (ember-700) |
    | Dark surface | `#f4f1eb` (warm white) | `#fdba74` (ember-300) |
    | Monochrome | `#151515` | `#151515` (ember block collapses to solid ink) |
    | Favicon / mark | CSS via prefers-color-scheme | CSS via prefers-color-scheme |

    Also add a row to the "## Font Provenance" table:
    | Space Grotesk | v2.0.0 | OFL | github.com/floriankarsten/space-grotesk | logo-primary.svg (v2), all D4 production assets |

    Step 4 — Atomic commit.
    Stage and commit ALL Phase 181 changes together:
      - brandbook/logo-primary.svg (v2, from Plan 01)
      - brandbook/logo-primary-dark.svg (v2, from Plan 01)
      - brandbook/logo-primary-subtitle.svg (new, from Plan 01)
      - brandbook/logo-mark.svg (v2, from Plan 01)
      - brandbook/logo-monochrome.svg (v2, from Plan 01)
      - brandbook/favicon.svg (v2, from Plan 01)
      - brandbook/social-card.svg (v2, from Plan 02 Task 1)
      - brandbook/social-card-dark.svg (new, from Plan 02 Task 1)
      - brandbook/logo-options/archive-v1/ (all 7 files)
      - brandbook/README.md (updated)
    This single commit ensures index.html img src references are never broken by having the archive exist without the v2 replacements.

    If Plan 01 and Plan 02 Task 1 changes were committed incrementally during execution (which is acceptable), this task's commit adds only the archive and README changes. The important invariant is that brandbook/index.html's referenced img paths always point to existing v2 files — verify this is true after every commit.
  </action>
  <verify>
    <automated>
      ls brandbook/logo-options/archive-v1/*.svg | wc -l | grep -q '6' &amp;&amp; echo "PASS: 6 v1 files archived"
      test -f brandbook/logo-options/archive-v1/README.md &amp;&amp; echo "PASS: archive README exists"
      grep -c 'Do not use' brandbook/logo-options/archive-v1/README.md | grep -qv '^0$' &amp;&amp; echo "PASS: deprecation notice present"
      grep -c 'Misuse' brandbook/README.md | grep -qv '^0$' &amp;&amp; echo "PASS: misuse section in README"
      grep -c 'Minimum\|clearspace\|Clearspace' brandbook/README.md | grep -qv '^0$' &amp;&amp; echo "PASS: clearspace/min-size in README"
      grep -c 'Space Grotesk' brandbook/README.md | grep -qv '^0$' &amp;&amp; echo "PASS: Space Grotesk provenance in README"
      ls brandbook/{logo-primary,logo-primary-dark,logo-primary-subtitle,logo-mark,logo-monochrome,favicon,social-card,social-card-dark}.svg | wc -l | grep -q '8' &amp;&amp; echo "PASS: all 8 production SVGs present"
      find brandbook -maxdepth 1 -name '*.svg' -size +250k -print | grep -q '.' &amp;&amp; echo "WARN: SVG exceeds 250KB" || echo "PASS: no oversized SVGs"
      git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2' | wc -l | grep -q '^0$' &amp;&amp; echo "PASS: no font binaries"
    </automated>
  </verify>
  <done>brandbook/logo-options/archive-v1/ contains 6 v1 SVGs and a README with deprecation notice; brandbook/README.md has Logo System Usage Rules section with clearspace, min-size table, and ≥4 misuse examples plus Space Grotesk provenance row; all 8 production SVGs exist at brandbook/; brandbook/index.html img src references all resolve to v2 content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system read/write | Archive reads v1 content from git history; no external network |
| Harness rendering | Playwright opens file:// URLs; no remote content loaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-03 | Tampering | Archive atomicity — index.html broken window | mitigate | Both archive write and v2 canonical file write must exist before commit; verify with `grep 'src=".*\.svg"' brandbook/index.html` then `ls` referenced files |
| T-181-04 | Tampering | Dark social card ember-700 in mark fill | mitigate | Post-write grep: `grep '#c2410c' brandbook/social-card-dark.svg` — only .strong class definition should match, never mark rects |
| T-181-05 | Information Disclosure | Font binaries committed | mitigate | `git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'` must return 0 at phase gate |
| T-181-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase |
</threat_model>

<verification>
Phase gate — run after both plans complete:

```bash
# All 8 production SVGs exist
ls brandbook/{logo-primary,logo-primary-dark,logo-primary-subtitle,logo-mark,logo-monochrome,favicon,social-card,social-card-dark}.svg

# All brandbook SVGs parse as valid XML
find brandbook -maxdepth 1 -name '*.svg' | xargs -n1 xmllint --noout

# Font provenance in all 6 typemark/mark files
grep -l 'Space Grotesk' brandbook/{logo-primary,logo-primary-dark,logo-primary-subtitle,logo-mark,logo-monochrome,favicon}.svg | wc -l
# Expected: 6

# Dark variants have no ember-700 in glyph/mark fills
grep -c '#c2410c' brandbook/logo-primary-dark.svg
# Expected: 0

# Monochrome uses solid ink only
grep -c '#c2410c' brandbook/logo-monochrome.svg; grep -c '#fdba74' brandbook/logo-monochrome.svg
# Expected: 0, 0

# Favicon and mark have media query
grep -l 'prefers-color-scheme' brandbook/favicon.svg brandbook/logo-mark.svg | wc -l
# Expected: 2

# Archive exists with 6 files + README
ls brandbook/logo-options/archive-v1/*.svg | wc -l
# Expected: 6
test -f brandbook/logo-options/archive-v1/README.md && echo "README exists"

# Usage rules in README
grep -c 'Misuse\|clearspace\|Minimum' brandbook/README.md
# Expected: ≥3

# No font binaries committed
git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2' | wc -l
# Expected: 0

# No SVG exceeds 250KB
find brandbook -maxdepth 1 -name '*.svg' -size +250k -print
# Expected: empty

# index.html referenced files all exist
for f in favicon.svg logo-primary-dark.svg logo-primary.svg logo-mark.svg; do
  test -f "brandbook/$f" && echo "OK: $f" || echo "MISSING: $f"
done
```
</verification>

<success_criteria>
- social-card.svg: D4 mark geometry embedded, OG-standard 1200×630, no CDN links, renders legibly at 600×315 thumbnail.
- social-card-dark.svg: dark surface variant with #fdba74 ember, dark background, renders legibly at 600×315.
- brandbook/logo-options/archive-v1/: 6 v1 SVGs + README with "Do not use these files" deprecation notice.
- brandbook/index.html img src references: all 4 referenced canonical SVG paths (favicon.svg, logo-primary-dark.svg, logo-primary.svg, logo-mark.svg) resolve to v2 content — no broken references.
- brandbook/README.md: Logo System Usage Rules section with clearspace rule, minimum-sizes table, ≥4 misuse examples, palette values table; Space Grotesk provenance row in Font Provenance table.
- All 8 production SVGs present at brandbook/ with correct content.
- BRAND2-08 fully satisfied: all seven required SVGs exist (logo-primary, logo-primary-dark, logo-primary-subtitle, logo-mark, logo-monochrome, favicon, social-card + social-card-dark bonus); render-verified; usage rules documented; v1 assets archived.
</success_criteria>

<output>
Create .planning/phases/181-ratified-logo-system-buildout/181-02-SUMMARY.md when done.
</output>
