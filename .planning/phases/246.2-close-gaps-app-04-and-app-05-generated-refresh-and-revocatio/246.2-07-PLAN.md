---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
plan: 07
type: execute
wave: 7
depends_on:
  - 246.2-06
files_modified:
  - .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json
  - .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json
  - .planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-RUNTIME-PROOF-RUN-FAILED.json
autonomous: true
gap_closure: true
requirements:
  - APP-04
  - APP-05
must_haves:
  truths:
    - "APP-04 / D-08/D-09: one fresh exact-head PostgreSQL workflow artifact proves direct-budget exhaustion immediately before consumed-refresh replay, family revocation, and next-auth denial."
    - "APP-05 / D-08/D-09: the same run retains all one/all revocation, owner-isolation, and next-auth flags before receipt-last publication."
    - "The canonical receipt is byte-identical to artifact `generated-app-login-runtime-proof` from the selected successful run; run URL is exact and implementation_ref equals head_sha."
    - "APP-04/APP-05 empty, adjacency, and ordering assumptions remain flagged exactly as recorded in Plans 04–06; publication adds no new lifecycle semantics."
  artifacts:
    - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json"
      provides: "Byte-identical credential-free v5 receipt from one successful exact-head generated-host run"
      contains: "direct_budget_exhausted_before_refresh_replay"
    - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json"
      provides: "Exact selected run URL, immutable implementation SHA, artifact name, and receipt hash"
      contains: "generated-app-login-runtime-proof"
  key_links:
    - from: "GitHub workflow run"
      to: "canonical receipt"
      via: "one watcher, artifact download, byte comparison, exact v5/offline validation"
      pattern: "workflow_dispatch|receipt_sha256"
    - from: "canonical provenance implementation_ref"
      to: "source hashes and selected run head"
      via: "one exact 40-hex SHA value shared by implementation_ref and head_sha"
      pattern: "implementation_ref|head_sha"
  assumptions:
    - id: A-246.2-07-EDGE-CARRY
      status: flagged
      statement: "The unresolved APP-04/APP-05 adjacency, empty, and ordering probes remain the explicit assumptions recorded in Plans 04–06; this publication task verifies those planned boundaries without inventing broader semantics."
  prohibitions:
    - id: P-246.2-07-01
      requirement_id: APP-04
      category: transparency
      status: unverified
      flagged: true
      statement: "Canonical evidence must not claim that a local run, a different head, or a receipt lacking the causal direct-budget flag proved APP-04."
      reason: "Descriptor-less spec-less prohibition recall item; exact-head CI publication is planned, but no plain SPEC supplied an author-resolved prohibition descriptor."
    - id: P-246.2-07-02
      requirement_id: APP-05
      category: transparency
      status: unverified
      flagged: true
      statement: "Canonical provenance must not overstate the artifact name, run URL, immutable implementation reference, or later evidence-only commit coverage."
      reason: "Descriptor-less spec-less prohibition recall item; semantic provenance validation is planned, but no plain SPEC supplied an author-resolved prohibition descriptor."
---

<objective>
Publish one fresh exact-head v5 artifact that closes both authoritative blockers and turns the canonical APP-04/APP-05 evidence contract green (D-08/D-09).

Purpose: Replace stale pre-fix evidence only after the separated-limiter runtime path and semantic provenance contract succeed together.
Output: Byte-identical canonical v5 receipt, exact immutable provenance, or bounded failure-only diagnostics with no passing claim.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-05-SUMMARY.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-06-SUMMARY.md
@scripts/ci/generated-app-login-runtime-proof.sh
@.github/workflows/generated-app-login-runtime-proof.yml
@test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
@test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json

<interfaces>
The workflow artifact name is exactly `generated-app-login-runtime-proof`. Provenance `run_url` must equal `https://github.com/szTheory/sigra/actions/runs/{run_id}` and `implementation_ref` must be the same exact immutable SHA as `head_sha`; the mutable remote dispatch ref is selection metadata only and must not occupy `implementation_ref`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Retain one exact-head causal runtime artifact</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`; Plans 04–06 are committed, their exact HEAD is pushed to the existing scoped evidence ref without creating or switching branches, one `gh api rate_limit` read reports REST core remaining above 250, and no watcher is polling the selected workflow run.</precondition>
  <reversibility rating="costly">Superseding a retained evidence claim requires another independently selected exact-head workflow run and coordinated canonical replacement.</reversibility>
  <files>.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json, .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json, .planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-RUNTIME-PROOF-RUN-FAILED.json</files>
  <read_first>
    - `AGENTS.md` — apply the one-watcher, 60-second interval, structured-summary-once, failed-logs-only-on-failure, transient retry, and rate-limit hard-stop rules exactly.
    - `.github/workflows/generated-app-login-runtime-proof.yml` — confirm dispatch event, exact v5 validator, and exact artifact name.
    - `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` — use Plan 06's offline exact-key, identity, immutable-head, receipt-hash, and source-hash validator.
    - `scripts/ci/generated-app-login-runtime-proof.sh` — confirm the causal flag is gated after the direct-429/replay/state/denial path and receipt is written last.
    - Existing canonical receipt/provenance — preserve their vocabulary while replacing values only after complete validation.
  </read_first>
  <behavior>
    - Exactly one new workflow_dispatch run is selected by workflow, pushed ref, and Plans 04–06 exact head SHA, then observed by exactly one 60-second watcher.
    - A successful artifact has exact schema v5, status passed, every previous lifecycle flag true, the new direct-budget causal flag true, exact source keys/hashes at the implementation head, and no credential/session/CSRF fields.
    - Canonical receipt bytes equal downloaded receipt bytes; provenance records exact run URL, exact artifact name, `implementation_ref == head_sha`, one dispatch, one watcher interval, success, receipt hash, and later evidence-only non-coverage.
    - Any rate-limit, selection, run, artifact, redaction, identity, source-hash, or offline-validation failure leaves canonical files untouched and writes bounded credential-free failure diagnostics only.
  </behavior>
  <action>
    Read REST rate limit once. If it is 250 or lower, or GitHub returns 403/429, hard-stop until the reported reset/retry time without immediate retry. Push the exact Plans 04–06 implementation HEAD to the already-established scoped evidence ref without creating or switching branches, dispatch the generated-app-login workflow exactly once on that ref, and identify exactly one new workflow_dispatch run whose head SHA equals the recorded implementation SHA. Use exactly `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`. Fetch the structured summary once after completion; fetch failed logs only when conclusion is failure. Allow one retry only for a transient non-rate-limit read-only query or artifact download after unique run selection; never redispatch.

    On success, download artifact `generated-app-login-runtime-proof` once to a temporary directory. Validate exact v5 keys, every all-true behavior including `direct_budget_exhausted_before_refresh_replay`, exact source-key/hash bindings at the immutable head, redaction, and receipt SHA. Construct temporary provenance with the selected canonical run URL, exact artifact name, and both `implementation_ref` and `head_sha` set to the exercised SHA; run the Plan 06 offline validator before canonical replacement. Copy the receipt byte-for-byte and write exact provenance, then run the full canonical tests. Commit only the two canonical JSON files in a distinct evidence-only commit. If any gate fails, keep canonical files unchanged, write only bounded non-secret diagnostics/non-coverage to the Phase 246.2 failure JSON, and stop without a passing claim.
  </action>
  <acceptance_criteria>
    - One and only one selected successful workflow_dispatch run exercises the exact Plans 04–06 implementation head with one 60-second watcher.
    - Downloaded and canonical receipt bytes match and exact v5 includes every prior flag plus the new causal flag true.
    - Provenance run URL, artifact name, head SHA, implementation reference, receipt hash, dispatch count, watcher interval, and coverage statement all pass Plan 06's semantic validator.
    - Source hashes bind router, runtime script, workflow, runtime source test, and evidence contract test to the exercised head.
    - Failure leaves old canonical evidence untouched and cannot be represented as success.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace &amp;&amp; jq -e '.schema == "sigra.generated-app-login-runtime-proof/v5" and .status == "passed" and .direct_budget_exhausted_before_refresh_replay and .refresh_reuse_family_revoked and .refresh_reuse_denied_next_access and .revoke_family_owner_isolated and .revoke_all_denied_next_access' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json &amp;&amp; jq -e '.repository == "szTheory/sigra" and .event == "workflow_dispatch" and .dispatch_attempts == 1 and .watch_interval_seconds == 60 and .conclusion == "success" and .artifact_name == "generated-app-login-runtime-proof" and .implementation_ref == .head_sha and .run_url == ("https://github.com/szTheory/sigra/actions/runs/" + (.run_id | tostring))' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json &amp;&amp; git diff --check</automated>
  </verify>
  <done>Canonical exact-head evidence proves the direct-budget isolation replay path and fully semantic provenance; APP-04 and APP-05 are ready for re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local implementation commit → GitHub workflow run | The selected run must exercise the exact pushed implementation SHA. |
| GitHub artifact → canonical repository evidence | External bytes are untrusted until exact v5, redaction, identity, receipt-hash, and source-hash gates pass. |
| Evidence-only commit → claimed runtime coverage | Provenance must explicitly exclude the later canonical-file commit. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246.2-27 | Spoofing / Repudiation | workflow run selection | high | mitigate | Select exactly one run by workflow/event/ref/exact head and watch it once at 60 seconds. |
| T-246.2-28 | Tampering | downloaded/canonical artifact | high | mitigate | Require byte identity, exact v5 flags/keys, immutable source hashes, receipt hash, and offline semantic provenance validation. |
| T-246.2-29 | Information Disclosure | artifact/failure diagnostics | high | mitigate | Reject credential/session/CSRF fields and retain only allowlisted flags, hashes, run metadata, and bounded failure classes. |
| T-246.2-30 | Denial of Service | GitHub API usage | medium | mitigate | Precheck rate limit, use one watcher, one structured summary, failed logs only on failure, one scoped transient retry, and hard-stop on 403/429. |
| T-246.2-SC | Tampering | package installs | low | accept | No package-manager install occurs. |
</threat_model>

## Artifacts this phase produces

- Replaced canonical receipt: `.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json`, exact schema `sigra.generated-app-login-runtime-proof/v5` with new field `direct_budget_exhausted_before_refresh_replay`.
- Replaced canonical provenance: `.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json`, with exact selected `run_url`, artifact name, and immutable `implementation_ref == head_sha`.
- Conditional failure-only diagnostic: `246.2-RUNTIME-PROOF-RUN-FAILED.json`, created only on a blocked run and containing no credentials or passing claim.
- No new production route, controller, lifecycle API, config flag, schema, migration, UI field, or package.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Secure generated refresh/revocation with receipt-last PostgreSQL evidence | 04–07 | COVERED | Limiter isolation, causal runtime proof, semantic provenance, and fresh publication close the remaining gaps. |
| REQ | APP-04 | Atomic rotation and consumed-refresh family revocation | 04–07 | COVERED | Direct traffic cannot consume refresh capacity; direct 429 precedes proven replay revocation/denial. |
| REQ | APP-05 | Owner-safe one/all revocation with subsequent-auth effect | 05–07 | COVERED | Existing runtime flags remain mandatory and exact evidence identity becomes fail closed. |
| VERIFICATION GAP | — | Separate direct and refresh budgets | 04 | COVERED | Dedicated generated limiter/scope and rendered route contract. |
| VERIFICATION GAP | — | Exhaust direct budget, then prove replay revocation and denial | 05, 07 | COVERED | Causal generated-host path, dedicated receipt flag, and fresh artifact. |
| VERIFICATION GAP | — | Validate run_url, implementation_ref, artifact_name | 06, 07 | COVERED | Semantic negative mutations plus matching canonical provenance. |
| VERIFICATION WARNING | — | Literal contract-stage interpolation / unused variable | 05 | COVERED | Concrete inventory/case assertions replace the weakened loop. |
| RESEARCH | — | Public rate-limited refresh, real PostgreSQL routes, redacted receipt-last evidence | 04–07 | COVERED | Existing stack/patterns retained; no package install or lifecycle reimplementation. |
| CONTEXT | D-01–D-03 | Public refresh boundary and committed four-field response | 04–07 | COVERED | Existing behavior preserved; route reachability and exact evidence re-proven. |
| CONTEXT | D-04 | Bounded lifecycle failure and rate-limited public pipeline | 04–05 | COVERED | Refresh stays bounded while direct receives an independent bounded pipeline. |
| CONTEXT | D-05–D-07 | Owner-derived browser revocation and scope exclusions | 04–07 | COVERED | Existing routes/flags stay mandatory; no deferred listing/UI/admin/API work enters plans. |
| CONTEXT | D-08 | Generated source and disposable HTTP/PostgreSQL proof | 04–07 | COVERED | Rendered tracer, causal runtime route, and exact artifact. |
| CONTEXT | D-09 | Automation-first, no sleeps/secrets, truthful current-head evidence | 05–07 | COVERED | Bounded harness, semantic validator, and controlled one-run publication. |
| SPEC-LESS EDGE | APP-04 | adjacency, empty, ordering | 04–07 | COVERED | Explicit flagged assumptions A-246.2-04/05/06 and deterministic boundary tests. |
| SPEC-LESS EDGE | APP-05 | adjacency, empty, ordering | 04–07 | COVERED | Explicit flagged carry-forward assumptions; no new revocation semantics invented. |
| SPEC-LESS PROHIBITION | APP-04 | unrelated traffic/evidence overclaim | 04, 05, 07 | COVERED | Descriptor-less flagged safety/transparency items retained. |
| SPEC-LESS PROHIBITION | APP-05 | provenance overclaim | 06–07 | COVERED | Descriptor-less flagged transparency item retained. |

Deferred device/session listing, management UI, admin/operator controls, PWA/native/Crosswake/Electron work, and new lifecycle APIs remain excluded. Canon security/privacy concerns are handled by the concrete ASVS threat models rather than duplicated as bespoke prohibitions. No external packages, ORM/schema changes, UI work, or API coverage artifact is required.

<verification>
1. Plans 04–06 focused tests prove independent scopes, causal harness ordering/schema, and all provenance rejection mutations before external evidence is accepted.
2. Plan 07 uses one exact-head workflow run/watcher and validates artifact bytes, exact v5 flags, semantic identity, redaction, and immutable source hashes before replacement.
3. The complete focused suite passes against canonical files and the failure-only diagnostic is absent on success or non-successful and bounded on failure.
4. Re-run phase verification; no human UAT, sleeps, unrelated scope, or missing evidence is marked passed.
</verification>

<success_criteria>
- Direct-login throttling cannot preempt refresh replay-family revocation in generated routing or the retained runtime proof.
- Arbitrary run URL, mutable implementation reference, or wrong artifact name cannot pass canonical validation.
- Fresh canonical evidence is byte-identical, exact-head/source-bound, credential-free, and causal.
- Both blockers and the concrete-stage warning are closed without changing deferred or already-proven lifecycle scope.
</success_criteria>

<output>
Create `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-07-SUMMARY.md` when done.
</output>
