---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
plan: 05
type: execute
wave: 5
depends_on:
  - 246.2-04
files_modified:
  - scripts/ci/generated-app-login-runtime-proof.sh
  - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
  - .github/workflows/generated-app-login-runtime-proof.yml
autonomous: true
gap_closure: true
requirements:
  - APP-04
  - APP-05
must_haves:
  truths:
    - "APP-04 / D-08/D-09: a disposable direct-enabled generated host reaches a direct-login 429 first, then replays an already-consumed refresh credential through `/refresh`, revokes only that family, and denies its replacement access on next authentication."
    - "APP-04 adjacency assumption (flagged): no wait or window reset separates direct-budget exhaustion from refresh replay."
    - "APP-04 ordering assumption (flagged): the retained behavior flag becomes true only after direct 429, refresh 401, typed family/control checks, and next-auth denial complete in that order."
    - "APP-05 empty/adjacency/ordering assumptions (flagged): existing one/all revocation proof semantics remain unchanged and must still pass before receipt publication."
    - "The source-contract suite names every post-ceremony contract concretely without unused loop interpolation."
  artifacts:
    - path: "scripts/ci/generated-app-login-runtime-proof.sh"
      provides: "Direct-budget-exhaustion-before-refresh-replay generated-host proof and receipt-last flag"
      contains: "DIRECT_BUDGET_EXHAUSTED_BEFORE_REFRESH_REPLAY"
    - path: "test/sigra/planning/phase_246_generated_app_login_runtime_test.exs"
      provides: "Source-order, redaction, limiter-isolation, and concrete stage inventory regressions"
      contains: "direct budget"
    - path: ".github/workflows/generated-app-login-runtime-proof.yml"
      provides: "Exact receipt validation for the new causal behavior flag"
      contains: "direct_budget_exhausted_before_refresh_replay"
  key_links:
    - from: "direct-enabled disposable host"
      to: "consumed-refresh replay transaction"
      via: "bounded direct requests reach 429 immediately before POST /api/app-login/refresh"
      pattern: "exhaust_direct.*budget[\\s\\S]*prove_refresh_reuse"
    - from: "typed family state and next-auth denial"
      to: "runtime-proof.json"
      via: "flag set after assertions and required before atomic receipt rename"
      pattern: "DIRECT_BUDGET_EXHAUSTED_BEFORE_REFRESH_REPLAY=true|direct_budget_exhausted_before_refresh_replay"
  assumptions:
    - id: A-246.2-05-APP04-ADJACENCY
      status: flagged
      statement: "The proof's adjacent 429-to-refresh sequence is the required limiter-isolation boundary; it intentionally performs no delay or rate-window reset."
    - id: A-246.2-05-APP04-EMPTY
      status: flagged
      statement: "Malformed direct bodies are sufficient to consume the direct budget because controller success is irrelevant to limiter isolation; their bodies remain unretained."
    - id: A-246.2-05-APP04-ORDERING
      status: flagged
      statement: "The new receipt flag certifies a fixed causal order rather than merely the presence of independent successful checks."
    - id: A-246.2-05-APP05-EDGES
      status: flagged
      statement: "APP-05 one/all adjacency, empty-owner-set semantics, and stable order are preserved from the existing runtime proof and are not redefined here."
  prohibitions:
    - id: P-246.2-05-01
      requirement_id: APP-04
      category: safety
      status: unverified
      flagged: true
      statement: "The proof must not report replay-family revocation without first demonstrating that unrelated direct traffic exhausted its own budget in the same generated-host run."
      reason: "Descriptor-less spec-less prohibition recall item; the causal harness and receipt flag are planned, but no plain SPEC supplied an author-resolved prohibition descriptor."
---

<objective>
Prove the separated budgets through a direct-enabled generated host and retain the causal outcome in the receipt contract (APP-04/APP-05; D-08/D-09).

Purpose: Turn the rendered route separation into runtime evidence that the security-critical replay transaction remains reachable after direct-login throttling.
Output: A bounded no-sleep generated-host scenario, source regressions, an exact receipt behavior flag, and workflow validation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-04-SUMMARY.md
@scripts/ci/generated-app-login-runtime-proof.sh
@test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
@.github/workflows/generated-app-login-runtime-proof.yml

<interfaces>
Reuse the harness's `prove_refresh_rotation`, `prove_refresh_reuse_revocation`, `assert_family_state`, `assert_access_denied`, generated FetchAppSession proof route, bounded curl patterns, and receipt-last writer. The new retained boolean is `direct_budget_exhausted_before_refresh_replay`; advance the exact receipt schema to `sigra.generated-app-login-runtime-proof/v5` consistently in the script, workflow, and source contract. Plan 06 updates the offline evidence parser before canonical evidence is republished.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Exhaust direct capacity before routed refresh replay</name>
  <reversibility rating="reversible">The proof/schema contract can be superseded by another exact receipt revision without production data migration.</reversibility>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs, .github/workflows/generated-app-login-runtime-proof.yml</files>
  <read_first>
    - `scripts/ci/generated-app-login-runtime-proof.sh` — reuse existing fixture issuance, rotation/reuse, typed state, access denial, redacted diagnostics, direct-host ceremony, and receipt-last seams.
    - `test/sigra/planning/phase_246_generated_app_login_runtime_test.exs` — add failing causal-order/source-contract tests and repair the post-ceremony loop warning with concrete inventory assertions.
    - `.github/workflows/generated-app-login-runtime-proof.yml` — update the exact schema/required-field validator in lockstep with the writer.
    - `priv/templates/sigra.install/app_sessions/router_injection.ex` — consume Plan 04's `:app_login_direct` / `:app_login_refresh` contract without changing it here.
  </read_first>
  <behavior>
    - A fresh direct-enabled host issues and rotates an isolated refresh family, then bounded malformed requests across `/direct` and `/direct/mfa` reach a direct-pipeline 429.
    - Without sleeping or restarting the server, replay of the consumed refresh returns the bounded 401, marks only its family revoked, leaves a control family active, and denies replacement access on the protected generated route.
    - The new receipt flag is false by default, set only after the full ordered sequence, required by `write_receipt_last`, emitted under exact schema v5, and required by workflow validation.
    - Diagnostics retain only allowlisted status/result classes and never raw credentials, family IDs, challenge values, cookies, CSRF values, emails, or request bodies.
    - The post-ceremony source test asserts the exact contract inventory/case arms and has no unused loop variable or literal non-interpolation regression.
  </behavior>
  <action>
    Start with failing source-contract assertions for the dedicated direct limiter markers, a bounded direct-exhaustion helper, the exact 429-before-refresh-replay call order, the post-assertion flag assignment, receipt gate/schema, workflow field, redaction, and absence of sleeps. Implement the scenario in the direct host after its normal direct ceremony so a real generated router includes both direct and refresh pipelines. Issue a separate replay family and active control family, rotate once, exhaust direct capacity using a fixed maximum attempt loop that requires a 429, then immediately invoke the existing typed refresh-reuse proof and protected-route denial. Advance writer/workflow to exact schema v5 with the new boolean. Repair the contract-stage warning by asserting the concrete loop inventory and each concrete case arm while separately asserting the shared dynamic stage format; do not leave an unused `contract` variable or assert a literal interpolation. Preserve all existing APP-05 flags and D-09 redaction/receipt-last rules.
  </action>
  <acceptance_criteria>
    - The runtime source contract proves a direct 429 precedes the replay request, and replay/state/next-auth assertions precede the new true flag.
    - The attempt loop is bounded, uses no sleep, and fails if direct capacity never reaches 429.
    - Exact schema v5 contains `direct_budget_exhausted_before_refresh_replay: true`; both writer and workflow reject its absence/false value.
    - Existing refresh rotation, one/all revocation, owner-isolation, source-binding, atomic rename, and redaction assertions remain green.
    - The former unused-variable warning is removed and every post-ceremony contract name has concrete regression coverage.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs --trace &amp;&amp; bash -n scripts/ci/generated-app-login-runtime-proof.sh &amp;&amp; mix format --check-formatted test/sigra/planning/phase_246_generated_app_login_runtime_test.exs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The generated-host harness deterministically proves direct-limit exhaustion cannot preempt consumed-refresh family revocation, and its exact causal result is ready for offline validation and fresh publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated direct traffic → direct limiter | Untrusted direct requests must reach 429 without affecting refresh capacity. |
| Refresh replay → PostgreSQL lifecycle transaction | The consumed credential must reach the controller/library before family state and next-auth checks. |
| Runtime state → retained receipt | Only completed, ordered, credential-free assertions may set the new behavior flag. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246.2-19 | Denial of Service / Elevation | direct-versus-refresh runtime route path | high | mitigate | Require direct 429 immediately before routed refresh replay and prove replay transaction effects plus next-auth denial. |
| T-246.2-20 | Repudiation / Tampering | receipt behavior claim | high | mitigate | Add a dedicated false-by-default flag set after assertions, gate atomic publication on it, and require it in workflow validation. |
| T-246.2-21 | Information Disclosure | curl bodies and diagnostics | high | mitigate | Retain only allowlisted status/result classes and source hashes; source tests reject credential/session/CSRF fields. |
| T-246.2-22 | Denial of Service | proof attempt loop | medium | mitigate | Use a fixed maximum attempt count with immediate failure and no sleeps/window resets. |
| T-246.2-SC | Tampering | package installs | low | accept | No package-manager install is added; the existing pinned workflow dependencies remain unchanged. |
</threat_model>

## Artifacts this phase produces

- New harness state flag: `DIRECT_BUDGET_EXHAUSTED_BEFORE_REFRESH_REPLAY`.
- New receipt field: `direct_budget_exhausted_before_refresh_replay`.
- New exact receipt schema identifier: `sigra.generated-app-login-runtime-proof/v5`.
- New bounded direct-budget exhaustion/refresh-replay helper(s) and stage names in `generated-app-login-runtime-proof.sh`.
- Repaired concrete post-ceremony contract inventory assertions; no new production route, controller, lifecycle API, schema, migration, UI field, or package.

<verification>
Run focused source contracts, shell syntax, formatter, and diff hygiene. The full fresh-host workflow is intentionally deferred to Plan 07 after Plan 06 makes canonical v5 provenance fail closed.
</verification>

<success_criteria>
- Runtime source and workflow contracts encode direct 429 → refresh replay → family revocation → next-auth denial as one causal proof.
- Receipt publication remains atomic, exact-key, source-bound, and credential-free.
- The contract-stage warning is eliminated with concrete stage inventory coverage.
</success_criteria>

<output>
Create `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-05-SUMMARY.md` when done.
</output>
