---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
verified: 2026-08-18T19:29:03Z
status: passed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/8
  gaps_closed:
    - "A direct-login request can consume the refresh limiter, preventing consumed-refresh replay from reaching family revocation."
    - "The retained-evidence validator accepts arbitrary run_url, implementation_ref, and artifact_name values."
  gaps_remaining: []
  regressions: []
---

# Phase 246.2: Generated Refresh and Revocation Surfaces Verification Report

**Phase Goal:** A fresh app-session host exposes secure generated HTTP refresh and owner-derived one/all revocation routes whose rotation, replay-family revocation, owner isolation, and next-auth denial are proven by receipt-last PostgreSQL evidence.
**Verified:** 2026-08-18T19:29:03Z
**Status:** passed
**Re-verification:** Yes — after gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A first-party client can rotate exactly one refresh credential at a public bounded generated route and receives only the committed access token, refresh token, family ID, and access expiry. | ✓ VERIFIED | `router_injection.ex:64-68` wires only `POST /refresh` through `[:api, :app_login_refresh]`; `app_login_controller.ex:68-78` accepts exactly the singleton binary body. `Sigra.AppSession.refresh/2` returns its four-key map only from the successful database transaction branch (`lib/sigra/app_session.ex:102-145`). The exact-head generated-host run exercised the route. |
| 2 | Refresh requires neither browser nor access authentication and cannot broaden ordinary access authentication. | ✓ VERIFIED | The refresh scope has only API and refresh-limiter pipelines; the controller contains no `FetchAppSession`. The runtime proof rejects the refresh credential at the protected access-only route after rotation. |
| 3 | A consumed-refresh replay reaches the lifecycle transaction despite exhausted direct-login budget, returns one bounded unauthenticated failure, revokes only its family, and denies the replacement access credential on next authentication. | ✓ VERIFIED | Direct and refresh routes use exclusive `app_login_direct` and `app_login_refresh` scopes (`router_injection.ex:16-37,64-76`); the route test and runtime source test pass. The CI proof exhausts direct budget immediately before replay, checks HTTP 401, PostgreSQL family states, and next-auth denial before setting the causal receipt flags. |
| 4 | A recently authenticated browser user can revoke one owned family or all owned families through CSRF-protected mutations whose owner derives only from current Scope; foreign families stay unchanged. | ✓ VERIFIED | Browser routes require `[:browser, :require_authenticated, :require_sudo]` (`router_injection.ex:51-58`); controller actions derive `conn.assigns.current_scope.user` and call owner-bound facade methods (`app_login_controller.ex:80-108`). The generated-host PostgreSQL proof checks owned revocation, foreign-family activity, and next-auth outcomes. |
| 5 | The generated lifecycle proof uses twice-installed disposable PostgreSQL hosts, real HTTP routes, bounded readiness, and receipt-last publication. | ✓ VERIFIED | `generated-app-login-runtime-proof.sh` creates separate generated hosts, uses HTTP `curl` and typed `mix run` PostgreSQL assertions, uses `wait_for_http` rather than fixed sleeps, and gates every lifecycle flag before temp-file/atomic `mv` publication (`899-932`). Successful exact-head CI job executed this lane. |
| 6 | Retained v5 receipt is credential-free, contains every APP-04/APP-05 lifecycle flag, and binds 16 source hashes to its immutable implementation head. | ✓ VERIFIED | Canonical receipt is schema v5, status `passed`, has all required lifecycle booleans true and 16 sources. The passing retained-evidence test recomputes each hash with `git show <head>:<path>` for provenance head `ce96d537b3b1427d9c12ac0899ec9538e53afca6`. |
| 7 | Receipt publication occurs only after every lifecycle assertion passes and is atomic. | ✓ VERIFIED | `write_receipt_last` hard-checks all APP-04/APP-05 flags before writing `runtime-proof.json.tmp` and atomically renaming it; the workflow validates the receipt before upload. |
| 8 | The retained receipt and provenance are exact-head, exact-run, and fail closed for run URL, artifact name, immutable implementation reference, receipt hash, source hashes, schema, and flag mutations. | ✓ VERIFIED | `phase_246_runtime_evidence_contract_test.exs:58-134,204-235` rejects forged URL/ref/artifact fields and validates the canonical files unconditionally. GitHub run `32173051169` is successful at the exact recorded head, its retained artifact is unexpired, and its downloaded `runtime-proof.json` is byte-identical to canonical receipt (SHA-256 `a0f35e…47b241`). |

**Score:** 8/8 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `priv/templates/sigra.install/app_sessions/router_injection.ex` | Exclusive public refresh/direct boundaries and secure revoke routes | ✓ VERIFIED | Substantive templates, rendered by tests, with three exclusive API scopes and protected browser mutations. |
| `priv/templates/sigra.install/app_sessions/app_login_controller.ex` | Strict refresh and owner-derived revoke actions | ✓ VERIFIED | Singleton validation, facade calls, bounded failures, and Scope-derived owner. |
| `priv/templates/sigra.install/app_sessions/auth_app_sessions.ex` | Generated facade to lifecycle authority | ✓ VERIFIED | Delegates refresh and owner-bound one/all revocation to `Sigra.AppSession`. |
| `scripts/ci/generated-app-login-runtime-proof.sh` | Routed PostgreSQL proof and receipt-last writer | ✓ VERIFIED | Executed successfully by the retained exact-head CI artifact; causal direct-budget, replay, ownership, and next-auth checks are substantive. |
| `test/sigra/install/app_sessions_routes_test.exs` | Rendered route/controller contracts | ✓ VERIFIED | Seven deterministic rendered-template tests include direct-versus-refresh scope isolation. |
| `test/sigra/planning/phase_246_generated_app_login_runtime_test.exs` | Runtime source contracts | ✓ VERIFIED | Passing source contracts cover the causal proof ordering, routes, readiness, receipt, and CSRF revocation paths. |
| `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` | Fail-closed v5 receipt/provenance validation | ✓ VERIFIED | Passing canonical validation plus URL/ref/artifact mutation tests. |
| `.github/workflows/generated-app-login-runtime-proof.yml` | PostgreSQL CI proof before artifact upload | ✓ VERIFIED | Successful `workflow_dispatch` run executes proof, validates flags/source hashes, then uploads named artifact. |
| `246-RUNTIME-PROOF.json` and `246-RUNTIME-PROOF-RUN.json` | Credential-free receipt and exact provenance | ✓ VERIFIED | Valid v5 canonical pair; provenance matches selected run and artifact bytes. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `POST /api/app-login/refresh` | `Auth.AppSessions.refresh/1` | strict controller validation | ✓ WIRED | Router → `AppLoginController.refresh/2` → generated facade → `Sigra.AppSession.refresh/2`; test and CI route proof cover it. |
| Direct-login traffic | Refresh replay transaction | distinct limiter pipelines | ✓ WIRED | `:app_login_direct` owns only `/direct` and `/direct/mfa`; `:app_login_refresh` owns only `/refresh`; exact-head proof exhausts the former before successful replay handling. |
| Revoke POST routes | owner-bound facade | authenticated/sudo Scope plus CSRF | ✓ WIRED | Controller never accepts an owner selector and proof verifies foreign-family preservation. |
| Consumed refresh replay | next protected-route denial | HTTP → PostgreSQL family state → `FetchAppSession` | ✓ WIRED | CI proof performs the replay, typed family checks, and denied replacement-access request in order. |
| Runtime flags | v5 receipt | final flag gate → temp file → atomic rename | ✓ WIRED | No receipt is published before all lifecycle flags are true. |
| Canonical receipt/provenance | selected CI artifact and immutable source | exact validator plus downloaded artifact comparison | ✓ WIRED | Provenance URL/ref/name/receipt hash are semantically checked; artifact bytes match canonical receipt. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Refresh action | `credentials` | committed `Sigra.AppSession.refresh/2` transaction result | Yes — four-key success map | ✓ FLOWING |
| Revocation actions | owner and mutation outcome | trusted Scope plus owner-bound locked database operations | Yes — generated-host PostgreSQL assertions | ✓ FLOWING |
| Receipt | lifecycle flags/source hashes | generated HTTP and PostgreSQL checks plus SHA-256 at receipt-last write | Yes — selected exact-head artifact | ✓ FLOWING |
| Provenance validator | run/artifact/head identity | canonical JSON, selected GitHub metadata, and immutable source lookup | Yes — semantic field/mutation checks | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Generated routes, causal proof contracts, and retained evidence validation | `MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace` | 55 tests, 0 failures (inactive local PostgreSQL emitted non-fatal connection-refused logs) | ✓ PASS |
| Exact-head PostgreSQL lifecycle proof | GitHub Actions run `32173051169` | `workflow_dispatch`, exact head `ce96d537…`, all job steps successful | ✓ PASS |
| Canonical artifact identity | `gh run download …; cmp -s artifact/runtime-proof.json 246-RUNTIME-PROOF.json` | Byte-identical; both SHA-256 `a0f35e112f3c839f82a8b60b860dab30a5cfb8b362b7f41b34dd32baba47b241` | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| Generated app-login runtime proof | CI `scripts/ci/generated-app-login-runtime-proof.sh --all` in run `32173051169` | Successful PostgreSQL generated-host artifact retained | PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| --- | --- | --- | --- |
| APP-04 | 246.2-01 through 246.2-07 | Atomic refresh rotation and family revocation on consumed-token reuse. | ✓ SATISFIED | Strict public route, separate direct budget, routed replay/typed PostgreSQL/next-auth proof, and exact-head receipt. |
| APP-05 | 246.2-01 through 246.2-07 | Owner-safe one/all revocation with subsequent-authentication effect. | ✓ SATISFIED | CSRF/sudo Scope-derived routes, owner-bound persistence, foreign-control verification, and exact-head receipt. |

All plan-declared IDs occur in `REQUIREMENTS.md`; no Phase 246.2 plan requirement is orphaned. The older traceability table still names Phase 245 for these two requirements, but the current inserted roadmap phase explicitly assigns APP-04 and APP-05 and all seven Phase 246.2 plans declare both IDs.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `scripts/ci/generated-app-login-runtime-proof.sh` | 399 | Runtime diagnostic checks that all expected response keys occur but does not parse and compare the entire JSON key set. | ⚠️ WARNING | Review finding WR-01 weakens the proof harness's regression sensitivity for an extra JSON field; it does not defeat this phase's must-have because `Sigra.AppSession.refresh/2` constructs only the committed four-field map after its successful transaction. |

No modified phase file has an unreferenced `TBD`, `FIXME`, or `XXX` marker. The `mktemp` occurrence is deliberate disposable-host setup, not an empty implementation.

### Advisory Review Adjudication

WR-01 is retained as a warning. It is not a phase blocker: the HTTP harness's string check would miss a future extra JSON key, but the actual lifecycle success branch constructs exactly `access_token`, `refresh_token`, `family_id`, and `expires_in`, and the controller serializes that committed map. The warning should be tightened in follow-up coverage, but it does not falsify the current APP-04/APP-05 goal.

### Predecessor Regression Inventory

The documented nine historical Phase 234–236 evidence-contract failures remain outside this phase's requirement evidence and are not represented as passed. They are retained in `.planning/debug/phase-246-2-predecessor-regressions.md`; no observed failure connects them to APP-04 or APP-05 implementation, retained proof, or the scoped 55-test verification.

### Gaps Summary

No actionable Phase 246.2 gaps remain. The two prior blockers are closed by exclusive limiter scopes plus the causal direct-budget runtime proof, and by semantic v5 provenance validation plus a new exact-head, byte-identical retained artifact.

---

_Verified: 2026-08-18T19:29:03Z_
_Verifier: the agent (gsd-verifier)_
