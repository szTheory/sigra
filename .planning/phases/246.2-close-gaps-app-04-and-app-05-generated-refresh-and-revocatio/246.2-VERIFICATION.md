---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
verified: 2026-08-18T15:49:29Z
status: gaps_found
score: 6/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/8
  gaps_closed:
    - "Canonical retained evidence is now schema v4 and is passed unconditionally through validate/2."
  gaps_remaining:
    - "A direct-login request can consume the refresh limiter, preventing consumed-refresh replay from reaching family revocation."
    - "The retained-evidence validator accepts arbitrary run_url, implementation_ref, and artifact_name values."
  regressions: []
gaps:
  - truth: "Consumed-refresh replay returns one bounded unauthenticated failure, revokes only its session family, and denies that family's replacement access credential on its next generated-route authentication."
    status: failed
    reason: "Direct-login routes share the IP-keyed app_login_refresh limiter with /refresh. Four direct requests can yield 429 before the refresh controller runs, so a consumed refresh cannot execute its required family-revocation transaction."
    artifacts:
      - path: "priv/templates/sigra.install/app_sessions/router_injection.ex"
        issue: "The :app_login_refresh scope contains /refresh, /direct, and /direct/mfa."
      - path: "test/sigra/install/app_sessions_routes_test.exs"
        issue: "The rendered-route contract proves only exchange-versus-refresh isolation, not direct-versus-refresh budget isolation."
    missing:
      - "Put /direct and /direct/mfa behind an independent limiter/pipeline or give refresh a scope containing only /refresh."
      - "Add a generated-route/runtime test that exhausts the direct budget then proves consumed-refresh replay still revokes its family."
  - truth: "The retained receipt/provenance is a fail-closed, exact-head, matching proof artifact for the generated PostgreSQL run."
    status: partial
    reason: "The canonical files and the selected GitHub run currently agree, but valid_provenance?/2 never validates run_url, implementation_ref, or artifact_name beyond requiring the keys. A forged provenance map with a valid historical head and recomputed receipt hash is accepted."
    artifacts:
      - path: "test/sigra/planning/phase_246_runtime_evidence_contract_test.exs"
        issue: "Lines 166-188 do not constrain run_url, implementation_ref, or artifact_name, and mutation coverage omits all three."
    missing:
      - "Require run_url to equal the selected run URL and artifact_name to equal generated-app-login-runtime-proof."
      - "Bind implementation_ref to the immutable head (or verify its trusted remote resolution) and add rejection tests for each field."
---

# Phase 246.2: Generated Refresh and Revocation Surfaces Verification Report

**Phase Goal:** A fresh app-session host exposes secure generated HTTP refresh and owner-derived one/all revocation routes whose rotation, replay-family revocation, owner isolation, and next-auth denial are proven by receipt-last PostgreSQL evidence.
**Verified:** 2026-08-18T15:49:29Z
**Status:** gaps_found
**Re-verification:** Yes — after gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Public refresh accepts exactly one refresh credential, is rate limited, requires neither browser nor access authentication, and returns only committed credentials. | ✓ VERIFIED | `router_injection.ex:49-54` exposes `POST /refresh` through `[:api, :app_login_refresh]`; `app_login_controller.ex:68-78` enforces the singleton binary body and returns the facade result or bounded error. `app_sessions_routes_test.exs` passed its refresh contract. |
| 2 | Refresh does not broaden ordinary access authentication. | ✓ VERIFIED | `AppLoginController.refresh/2` contains no `FetchAppSession`; the generated proof explicitly rejects a refresh credential at the access-only proof route. |
| 3 | Consumed-refresh replay always reaches the lifecycle transaction, revokes only that family, and denies replacement access on next authentication. | ✗ FAILED — BLOCKER | `/direct` and `/direct/mfa` are in the same `:app_login_refresh` scope as `/refresh` (`router_injection.ex:50-54`). The IP-keyed four-request budget can therefore return 429 before replay reaches `AppSessions.refresh/1`. The existing proof does not consume direct budget first. |
| 4 | Browser one/all revocation is CSRF-protected, derives owner only from current Scope, and leaves foreign families unchanged. | ✓ VERIFIED | Routes use `[:browser, :require_authenticated, :require_sudo]` (`router_injection.ex:36-41`); controller actions source only `conn.assigns.current_scope.user` and call the owner-bound facade (`app_login_controller.ex:80-108`). The rendered route test passed. |
| 5 | Runtime lifecycle proof uses generated HTTP routes, PostgreSQL assertions, bounded readiness, and no fixed sleeps. | ✓ VERIFIED | The harness uses `curl`, typed family/token state assertions, `wait_for_http`, and no `sleep`; its 44 focused source/evidence tests passed. The retained CI workflow run is successful. |
| 6 | The retained receipt is credential-free v4 evidence with all APP-04/APP-05 lifecycle flags and hashes bound to its immutable implementation head. | ✓ VERIFIED | Canonical receipt is v4 with all seven lifecycle flags true. All 16 listed source hashes were recomputed from `139959b66af452e71d56e5fe1b32285f92909ec6` and match. |
| 7 | Receipt publication is last and atomic after all lifecycle flags pass. | ✓ VERIFIED | `write_receipt_last` checks every lifecycle flag before writing `runtime-proof.json.tmp`, then atomically renames it (`generated-app-login-runtime-proof.sh:861-895`). |
| 8 | Canonical receipt/provenance validation is fully fail-closed for the claimed exact workflow artifact and implementation reference. | ✗ FAILED — BLOCKER | The v4/schema gap is closed (`validate/2` now runs for any present receipt), but `valid_provenance?/2` does not validate `run_url`, `implementation_ref`, or `artifact_name`; its mutation test never exercises them. |

**Score:** 6/8 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `priv/templates/sigra.install/app_sessions/router_injection.ex` | Public refresh and secure browser revoke routes | ⚠️ PARTIAL | Substantive and wired, but direct routes share refresh's limiter. |
| `priv/templates/sigra.install/app_sessions/app_login_controller.ex` | Strict refresh and owner-derived revoke actions | ✓ VERIFIED | Exact request shapes, facade-only calls, bounded JSON responses. |
| `priv/templates/sigra.install/app_sessions/auth_app_sessions.ex` | Generated facade to lifecycle authority | ✓ VERIFIED | `refresh/1`, `revoke_family/2`, and `revoke_all/1` delegate to `Sigra.AppSession`. |
| `scripts/ci/generated-app-login-runtime-proof.sh` | Routed PostgreSQL proof and receipt-last writer | ⚠️ PARTIAL | Implements causal proof and atomic writer, but lacks direct-budget-exhaustion coverage. |
| `test/sigra/install/app_sessions_routes_test.exs` | Rendered route/controller contracts | ⚠️ PARTIAL | Six tests pass but omit direct-versus-refresh limiter isolation. |
| `test/sigra/planning/phase_246_generated_app_login_runtime_test.exs` | Runtime source contracts | ⚠️ PARTIAL | 40 tests pass, but the contract-stage loop repeats a literal interpolation and emits an unused-variable warning. This is a coverage warning, not the goal blocker. |
| `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` | Exact-key receipt/provenance contract | ✗ PARTIAL | 4 tests pass and reject stale v3; three provenance fields remain unconstrained. |
| `.github/workflows/generated-app-login-runtime-proof.yml` | PostgreSQL CI proof before artifact upload | ✓ VERIFIED | Runs the proof then validates v4 flags/source hashes before upload. |
| `246-RUNTIME-PROOF.json` | Canonical v4 receipt | ✓ VERIFIED | Exact v4 keys, all-true flags, no secret fields. |
| `246-RUNTIME-PROOF-RUN.json` | Exact-run provenance | ⚠️ PRESENT | Its fields match GitHub run `32154454396`, but the local validator does not fail closed for every claimed identity field. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `POST /api/app-login/refresh` | `Auth.AppSessions.refresh/1` | strict controller validation | ✓ WIRED | Router → `refresh/2` → facade → `Sigra.AppSession.refresh/2`. |
| Revoke POST routes | owner-bound facade | browser/authenticated/sudo Scope | ✓ WIRED | Controller uses `current_scope.user`, not request owner data. |
| Consumed refresh replay | next protected-route denial | HTTP → PostgreSQL state → FetchAppSession | ✗ NOT WIRED FOR ALL REQUIRED TRAFFIC | Shared direct limiter can halt the route before the handler. |
| Runtime flags | v4 receipt | final flag gate → tmp file → rename | ✓ WIRED | All flags precede publication. |
| Canonical receipt/provenance | exact CI artifact identity | unconditional `validate/2` | ⚠️ PARTIAL | Schema, status, source hashes, head, and receipt hash validate; URL/ref/artifact identity does not. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Refresh action | `credentials` | committed `Sigra.AppSession.refresh/2` result | Yes | ✓ FLOWING |
| Revocation actions | owner and mutation outcome | trusted Scope plus owner-bound persistence | Yes | ✓ FLOWING |
| Receipt | lifecycle flags/source hashes | generated HTTP checks and SHA-256 at receipt-last write | Yes | ✓ FLOWING |
| Provenance validator | run/artifact/reference identity | retained JSON only | Incomplete validation | ✗ HOLLOW VALIDATION |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Generated refresh/revoke template contracts | `MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs --trace` | 6 tests, 0 failures | ✓ PASS |
| Focused runtime/evidence contracts | `MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace` | 44 tests, 0 failures; one unused `contract` warning | ✓ PASS with warning |
| Immutable source binding | Recomputed 16 receipt source hashes from `139959b66af452e71d56e5fe1b32285f92909ec6` | 16/16 match | ✓ PASS |
| Selected GitHub run/artifact metadata | `gh run view 32154454396 …` and artifact listing | successful `workflow_dispatch`, exact head/ref/URL, named artifact present and unexpired | ✓ PASS |
| Direct limiter interference | Static route/pipeline trace | `/direct` and `/direct/mfa` share `:app_login_refresh` | ✗ FAIL |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| APP-04 | 246.2-01, 246.2-02, 246.2-03 | Atomic refresh rotation and family revocation on consumed-token reuse. | ✗ BLOCKED | Normal-path proof exists, but direct traffic can rate-limit a replay before revocation. Retained provenance also has an unvalidated identity surface. |
| APP-05 | 246.2-01, 246.2-02, 246.2-03 | Owner-safe one/all revocation with next-auth effect. | ✗ BLOCKED | Owner-bound functional routes are present, but phase-required retained proof provenance is not fully fail-closed. |

Both plan-declared IDs appear in `REQUIREMENTS.md`; no Phase 246.2 requirement is orphaned. No later roadmap phase explicitly covers either limiter separation or the missing provenance validation, so neither gap is deferred.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `router_injection.ex` | 50-54 | Direct and refresh routes share the refresh limiter | 🛑 BLOCKER | A security replay cannot reliably reach its required revocation transaction. |
| `phase_246_runtime_evidence_contract_test.exs` | 166-188 | Provenance fields accepted without semantic validation | 🛑 BLOCKER | Exact run/artifact/ref claims are forgeable within the retained-evidence contract. |
| `phase_246_generated_app_login_runtime_test.exs` | 617-621 | Unused loop variable; each iteration asserts the same literal string | ⚠️ WARNING | Stage coverage can miss a deleted concrete stage name. |

No phase-modified `TBD`, `FIXME`, or `XXX` markers were found. The `mktemp` use in the proof harness is an intentional disposable-directory mechanism, not a stub.

### Advisory Review Adjudication

1. **Shared direct/refresh limiter interference — confirmed BLOCKER.** The review describes the current routing exactly: the EEx direct routes are inside the refresh-limited scope. The later isolation commit separated exchange from refresh, not direct from refresh.
2. **Incomplete provenance field validation — confirmed BLOCKER.** The actual canonical values match GitHub metadata, and source hashes are genuinely exact-head-bound; nevertheless the retained-evidence validator accepts arbitrary `run_url`, `implementation_ref`, and `artifact_name`. That makes the contract weaker than the phase's claimed exact workflow-artifact proof.
3. **Contract-stage loop — confirmed WARNING.** The source test emits the unused-variable warning and does not test each literal stage. The harness itself dynamically emits those stages, so this does not invalidate the implemented refresh/revocation behavior, but it reduces diagnostic regression coverage.

### Gaps Summary

The former stale-v3 receipt blocker is closed: canonical evidence is v4, validates unconditionally, matches all source hashes at the immutable execution head, and GitHub confirms the recorded successful run/artifact. The phase goal is still not achieved because a replay may be rate-limited by unrelated direct login before it can revoke its family, and because retained proof provenance is not wholly fail-closed. These are implementation/evidence gaps, not missing human UAT.

**Next action:** separate the direct and refresh limiter budgets; then validate and test every claimed provenance identity field before re-verification.

**Next command:** `$gsd-plan-phase 246.2 --gaps`

---

_Verified: 2026-08-18T15:49:29Z_
_Verifier: the agent (gsd-verifier)_
