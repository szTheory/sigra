---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
verified: 2026-08-16T23:55:00-04:00
status: gaps_found
score: 7/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "A twice-installed disposable PostgreSQL host proves routed rotation, replay, one/all revocation, owner isolation, and next-auth denial before atomically publishing a credential-free, exact-source-bound receipt."
    status: failed
    reason: "The committed canonical receipt is v3 and lacks every Phase 246.2 lifecycle flag, but the canonical-evidence test accepts any non-v4 receipt rather than failing closed. Consequently stale or malformed retained evidence can pass the phase gate."
    artifacts:
      - path: "test/sigra/planning/phase_246_runtime_evidence_contract_test.exs"
        issue: "Lines 90-94 only refute v4 in the non-v4 branch; they do not require schema v4 or call validate/2."
      - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json"
        issue: "Committed canonical evidence has schema sigra.generated-app-login-runtime-proof/v3 and no APP-04/APP-05 v4 behavior flags."
    missing:
      - "Require v4 and validate the canonical receipt/provenance whenever a canonical receipt exists."
      - "Produce and retain a current v4 receipt/provenance after the fail-closed validator is repaired."
---

# Phase 246.2: Generated Refresh and Revocation Surfaces Verification Report

**Phase Goal:** A fresh app-session host exposes secure generated HTTP refresh and owner-derived one/all revocation routes whose rotation, replay-family revocation, owner isolation, and next-auth denial are proven by receipt-last PostgreSQL evidence.
**Verified:** 2026-08-16T23:55:00-04:00
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A first-party client can use a public, rate-limited generated refresh route without browser or access-token authentication and receives the committed four-field response. | ✓ VERIFIED | `router_injection.ex:29-34` places only `POST /refresh` in `[:api, :app_login_public]`; `AppLoginController.refresh/2` strictly accepts only one binary `refresh_token` and delegates to the facade. `Sigra.AppSession.refresh/2` returns exactly `access_token`, `refresh_token`, `family_id`, and `expires_in` after the transaction commits. |
| 2 | Refresh does not broaden ordinary access authentication. | ✓ VERIFIED | The refresh controller contains no `FetchAppSession` dependency; the full generated-host proof exercised the refresh credential rejection through its access-only proof route. |
| 3 | A replayed consumed refresh has one bounded failure, revokes only its family, and denies the replacement access token on next authentication. | ✓ VERIFIED | `prove_refresh_reuse_revocation` sends the original credential to the generated route, requires 401/`invalid_refresh`, verifies typed family/token state and a control family, then calls `assert_access_denied` for the replacement access credential. The full `--all` proof completed successfully. |
| 4 | A recently authenticated browser user can revoke one owned family or all owned families through CSRF-protected JSON mutations whose owner is current Scope only; foreign families remain unchanged. | ✓ VERIFIED | `router_injection.ex:22-27` uses `[:browser, :require_authenticated, :require_sudo]`; controller actions use only `conn.assigns.current_scope.user`. The routed proof checks missing CSRF denial, foreign 404/control access survival, owned revocation/denial, then current-user-only revoke-all. |
| 5 | All lifecycle checks use generated routes and PostgreSQL state with bounded readiness and no fixed sleeps/library-only substitutes. | ✓ VERIFIED | The harness uses `curl`, typed generated schemas, `assert_family_state`, `prove_fetch_app_session`/`assert_access_denied`, and bounded `wait_for_http`; source contracts reject `sleep`. The full proof completed. |
| 6 | The runtime proof does not retain credentials, family selectors, browser cookies, CSRF values, or lifecycle internals in its receipt. | ✓ VERIFIED | The v4 writer emits boolean outcomes plus source SHA-256 values only; the evidence validator has negative credential/CSRF field tests; the full proof completed with its disposable directory cleaned. |
| 7 | The proof script writes a v4 receipt only after all APP-04/APP-05 flags pass, atomically, and CI validates exact v4 keys/current source hashes before upload. | ✓ VERIFIED | `write_receipt_last` checks all flags before writing `runtime-proof.json.tmp` then atomically renames it; the workflow validates v4 flags and source hashes before artifact upload. |
| 8 | The committed receipt-last PostgreSQL evidence is itself fail-closed, current, exact-source-bound v4 evidence for this phase. | ✗ FAILED — BLOCKER | The committed canonical receipt is v3, and `phase_246_runtime_evidence_contract_test.exs:90-94` accepts that stale schema without validating it. The green local v4 fixture test therefore does not prove the retained evidence contract. |

**Score:** 7/8 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `priv/templates/sigra.install/app_sessions/router_injection.ex` | Public refresh plus browser/sudo revocation routes | ✓ VERIFIED | Substantive 36-line template; generated by feature wiring and used by route tests/runtime host. |
| `priv/templates/sigra.install/app_sessions/app_login_controller.ex` | Thin bounded refresh/revoke actions | ✓ VERIFIED | Strict request shapes, Scope-derived owner, facade calls, bounded JSON only. |
| `test/sigra/install/app_sessions_routes_test.exs` | Rendered route/controller contracts | ✓ VERIFIED | Six relevant rendered-source tests passed in the scoped suite. |
| `scripts/ci/generated-app-login-runtime-proof.sh` | Routed refresh/revocation matrix and v4 writer | ✓ VERIFIED | Substantive, invoked by workflow and successfully run with `--all`. |
| `test/sigra/planning/phase_246_generated_app_login_runtime_test.exs` | Causal proof/redaction source contract | ⚠️ PARTIAL | Substantive and passing, but Plan 02's literal `refresh_reuse_family_revoked` artifact pattern is absent (the test checks its uppercase shell flag). It also contains the review's unused-loop-variable warning at line 523. |
| `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` | Exact-key v4 receipt/provenance parser | ✗ FAILED | Fixtures are fail-closed, but the canonical retained-evidence branch fails open for non-v4 receipts. |
| `.github/workflows/generated-app-login-runtime-proof.yml` | CI receipt validator before upload | ✓ VERIFIED | Validates v4 flags/source hashes before upload. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `POST /api/app-login/refresh` | `Auth.AppSessions.refresh/1` | `AppLoginController.refresh/2` after exact key/binary validation | ✓ WIRED | Router → controller → facade → `Sigra.AppSession.refresh/2` trace verified. |
| Browser revoke routes | `Auth.AppSessions.revoke_family/2` / `revoke_all/1` | browser/authenticated/sudo pipeline and `current_scope.user` | ✓ WIRED | Both routes/actions and trusted-owner facade calls verified. |
| Consumed refresh replay | Subsequent FetchAppSession denial | HTTP replay → PostgreSQL state → protected-route request | ✓ WIRED | `prove_refresh_reuse_revocation` has the required causal sequence and ran in the proof. |
| Browser revocation requests | Owner-bound generated facade | Cookie jar + rendered CSRF + persisted outcome | ✓ WIRED | `prove_revoke_family_owner_isolation` and `prove_revoke_all` use generated HTTP routes and state/access controls. |
| Runtime flags | v4 receipt and CI validator | flag gate → temp write/rename → CI source validation | ⚠️ PARTIAL | New-run path is wired; canonical retained-receipt validation accepts v3 instead of failing closed. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Refresh action | `credentials` | `AppSessions.refresh/1` → transactionally committed `Sigra.AppSession.refresh/2` map | Yes | ✓ FLOWING |
| Revocation actions | `owner`, facade result | `conn.assigns.current_scope.user` plus owner-bound persistence methods | Yes | ✓ FLOWING |
| Runtime receipt | lifecycle flags/source hashes | Real routed checks plus `sha256sum` at write time | New-run path yes; canonical artifact is stale v3 | ✗ FAIL-CLOSED GAP |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Rendered routes/controllers and evidence contracts | `MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_generator_test.exs test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace` | 56 tests, 0 failures; compiler warned that `contract` is unused at runtime source test line 523. | ✓ PASS (with warning) |
| Generated host refresh/replay/revocation matrix | `scripts/ci/generated-app-login-runtime-proof.sh --all` | Completed successfully in this verification environment. | ✓ PASS |
| Canonical receipt fail-closed enforcement | `jq '.schema' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json` plus the scoped evidence-contract test | Canonical receipt is `/v3`; its presence still lets the test pass because of the non-v4 branch. | ✗ FAIL |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| APP-04 | 246.2-01, 246.2-02 | Atomic every-use refresh rotation and family revocation on consumed-token reuse. | ✗ BLOCKED | Generated route, replay/family isolation, and next-auth denial work in the fresh-host proof, but the required retained v4 receipt evidence can be stale/unvalidated. |
| APP-05 | 246.2-01, 246.2-02 | Owner-safe one/all revocation with next-auth effect. | ✗ BLOCKED | Generated owner-bound routes and routed controls work in the fresh-host proof, but the phase's required retained v4 receipt evidence can be stale/unvalidated. |

Both plan-declared requirement IDs are present in `.planning/REQUIREMENTS.md`; no Phase 246.2 requirement is orphaned. No later roadmap phase explicitly schedules repair of the receipt validator or publication of v4 APP-04/APP-05 evidence, so this gap is not deferred.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` | 90-94 | Non-v4 canonical receipt is accepted | 🛑 BLOCKER | Fail-closed receipt evidence claim is false. |
| `scripts/ci/generated-app-login-runtime-proof.sh` | 930 | `kill` does not wait for the prior fixed-port server | ⚠️ WARNING | Direct-host proof can race the hosted server and become intermittent/misleading. |
| `priv/templates/sigra.install/app_sessions/app_login_controller.ex` | 160 | Callback query is appended after a possible URI fragment | ⚠️ WARNING | A fragment-bearing configured callback can lose the approval response as query parameters; this is outside APP-04/APP-05 route behavior but remains unresolved review debt. |
| `test/sigra/planning/phase_246_generated_app_login_runtime_test.exs` | 523-524 | Loop variable is unused due literal interpolation | ⚠️ WARNING | Per-contract stage source assertion checks the same literal five times. |

No phase-modified `TBD`, `FIXME`, or `XXX` markers were found.

### Gaps Summary

The generated production routes and fresh-host behavior are materially present and the full disposable PostgreSQL proof succeeded. The phase nevertheless misses its outcome because its retained proof gate is observably fail-open: the committed canonical evidence is schema v3, missing all v4 APP-04/APP-05 flags, and its supposed validator accepts it. That is the committed code-review blocker (CR-01), not a summary-only concern.

**Next action:** repair CR-01, publish fresh v4 receipt/provenance through the authorized proof workflow, then re-verify.

**Next command:** `$gsd-plan-phase 246.2 --gaps`

---

_Verified: 2026-08-16T23:55:00-04:00_
_Verifier: the agent (gsd-verifier)_
