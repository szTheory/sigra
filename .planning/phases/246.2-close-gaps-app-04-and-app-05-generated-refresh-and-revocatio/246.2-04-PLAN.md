---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
plan: 04
type: execute
wave: 4
depends_on:
  - 246.2-03
files_modified:
  - priv/templates/sigra.install/app_sessions/router_injection.ex
  - test/sigra/install/app_sessions_routes_test.exs
autonomous: true
gap_closure: true
requirements:
  - APP-04
  - APP-05
must_haves:
  truths:
    - "APP-04 / D-04: generated refresh requests have a bounded limiter budget that direct password and direct MFA requests cannot consume."
    - "APP-04 adjacency assumption (flagged): the last allowed direct request and first rejected direct request affect only the direct budget; an immediately adjacent refresh presentation remains dispatchable."
    - "APP-04 empty assumption (flagged): malformed or empty direct input can consume only direct-login capacity and cannot consume refresh capacity."
    - "APP-04 ordering assumption (flagged): request arrival order across the two pipelines does not merge their counters because their limiter keys and configuration keys are distinct."
    - "APP-05 remains unchanged: browser/sudo/CSRF revocation routes keep their existing owner-derived boundary while the public credential scopes are separated."
  artifacts:
    - path: "priv/templates/sigra.install/app_sessions/router_injection.ex"
      provides: "Independent generated direct-login and refresh limiter pipelines and route scopes"
      contains: "pipeline :app_login_direct"
    - path: "test/sigra/install/app_sessions_routes_test.exs"
      provides: "Rendered-template regression proving direct and refresh routes cannot share a limiter scope"
      contains: "direct and refresh budgets"
  key_links:
    - from: "POST /api/app-login/refresh"
      to: ":app_login_refresh"
      via: "a scope containing only the refresh route"
      pattern: "pipe_through \[:api, :app_login_refresh\][\\s\\S]*post \"/refresh\""
    - from: "POST /api/app-login/direct and /direct/mfa"
      to: ":app_login_direct"
      via: "an opt-in scope with a distinct key prefix and config keys"
      pattern: "pipeline :app_login_direct|key_prefix: \"app_login_direct\""
  assumptions:
    - id: A-246.2-04-APP04-ADJACENCY
      status: flagged
      statement: "Exactly touching direct-limit exhaustion and refresh replay remain independent because rate-limit identity is pipeline/key-prefix based, not route-order based."
    - id: A-246.2-04-APP04-EMPTY
      status: flagged
      statement: "Empty and malformed direct requests count against the direct limiter only; existing bounded controller failures remain unchanged."
    - id: A-246.2-04-APP04-ORDERING
      status: flagged
      statement: "Interleaving direct and refresh requests does not couple their counters; no cross-pipeline ordering guarantee is otherwise introduced."
    - id: A-246.2-04-APP05-EDGES
      status: flagged
      statement: "APP-05 adjacency, empty-owner-set, and equal-order behavior are unchanged because this plan does not alter revocation routes, owner selection, or lifecycle code."
  prohibitions:
    - id: P-246.2-04-01
      requirement_id: APP-04
      category: safety
      status: unverified
      flagged: true
      statement: "Unrelated direct-login traffic must not silently suppress the security-critical consumed-refresh family-revocation path."
      reason: "Descriptor-less spec-less prohibition recall item; the route contract is mechanically planned, but no plain SPEC supplied an author-resolved prohibition descriptor."
---

<objective>
Separate the generated direct-login and refresh rate-limit budgets so direct traffic cannot prevent consumed-refresh replay from reaching the lifecycle transaction (APP-04/APP-05; D-04).

Purpose: Restore reachability of the existing replay-family revocation guarantee without changing refresh, direct-login, or revocation lifecycle semantics.
Output: A dedicated generated direct-login limiter/scope and a rendered-template regression contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-CONTEXT.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-03-SUMMARY.md
@priv/templates/sigra.install/app_sessions/router_injection.ex
@test/sigra/install/app_sessions_routes_test.exs

<interfaces>
Keep `POST /api/app-login/refresh` on `[:api, :app_login_refresh]`. Add `:app_login_direct` using `Sigra.Plug.RateLimit`, key prefix `app_login_direct`, and host config keys `:app_login_direct_rate_limit` / `:app_login_direct_rate_limit_window`; route both opt-in direct actions through `[:api, :app_login_direct]`. Do not change controller actions or browser revocation scopes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render independently budgeted direct and refresh routes</name>
  <reversibility rating="reversible">The generated router/config surface can be reverted without a data migration or published storage change.</reversibility>
  <files>priv/templates/sigra.install/app_sessions/router_injection.ex, test/sigra/install/app_sessions_routes_test.exs</files>
  <read_first>
    - `priv/templates/sigra.install/app_sessions/router_injection.ex` — preserve the hosted public, refresh-only, browser revocation, and opt-in EEx boundaries while relocating only direct routes.
    - `test/sigra/install/app_sessions_routes_test.exs` — extend the rendered-template assertions and use the existing `render_template/2` helper with `app_password_login: true`.
    - `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md` — implement only the direct-versus-refresh limiter blocker.
  </read_first>
  <behavior>
    - The rendered refresh scope contains `POST /refresh` and no direct route.
    - The rendered direct scope exists only for `app_password_login: true`, contains both direct actions, and uses only `:app_login_direct`.
    - Direct and refresh pipelines have distinct key prefixes and distinct rate/window config keys while retaining the established default limit/window and error handler.
    - Hosted exchange stays on `:app_login_public`; browser one/all revocation stays on browser/authenticated/sudo.
  </behavior>
  <action>
    First add a failing rendered-route test that extracts each `/api/app-login` scope and proves exclusive membership: exchange-only public, refresh-only refresh, and direct/direct-MFA-only direct. Assert the direct limiter's exact key prefix and configuration keys and refute either direct action from the refresh scope. Then add the dedicated direct limiter pipeline and move both opt-in direct routes to their own EEx-guarded scope. Preserve D-01 through D-07 delivery boundaries and avoid controller, lifecycle, UI, schema, package, and revocation changes.
  </action>
  <acceptance_criteria>
    - A generated direct-enabled router has three independent public credential scopes: exchange, refresh, and direct.
    - The refresh scope contains only `/refresh`; exhausting direct capacity cannot address the refresh limiter key.
    - A generated hosted-only router contains no direct routes while its refresh route remains present.
    - Existing owner-derived browser revocation route assertions remain green (APP-05; D-05/D-06).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs --trace &amp;&amp; mix format --check-formatted priv/templates/sigra.install/app_sessions/router_injection.ex test/sigra/install/app_sessions_routes_test.exs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The generated route contract makes direct and refresh budgets structurally independent while preserving every already-proven route boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted direct requests → generated limiter | Direct password/MFA traffic can exhaust only the direct ceremony budget. |
| Untrusted refresh requests → generated limiter/controller | Refresh retains an independent bounded budget before the existing strict controller. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246.2-16 | Denial of Service / Elevation | generated direct and refresh scopes | high | mitigate | Assign distinct pipelines, key prefixes, and config keys; rendered tests prove exclusive route membership. |
| T-246.2-17 | Tampering | EEx opt-in route generation | medium | mitigate | Render both hosted-only and direct-enabled variants and assert direct routes appear only in the latter. |
| T-246.2-18 | Information Disclosure | rate-limit error boundary | low | accept | Existing bounded `AuthErrorHandler` behavior and response vocabulary are unchanged. |
| T-246.2-SC | Tampering | package installs | low | accept | No package-manager install occurs. |
</threat_model>

## Artifacts this phase produces

- New generated pipeline symbol: `:app_login_direct`.
- New host configuration keys: `:app_login_direct_rate_limit` and `:app_login_direct_rate_limit_window`.
- Modified generated route ownership: `/api/app-login/direct` and `/api/app-login/direct/mfa` move from the refresh scope to the direct scope; `/api/app-login/refresh` remains refresh-only.
- New rendered regression test named for independent direct and refresh budgets; no new controller, lifecycle function, schema, migration, UI field, or package.

<verification>
Run the focused rendered-template suite, formatter check, and diff hygiene gate. Inspect failures by rendered scope; do not weaken exact route/pipeline assertions.
</verification>

<success_criteria>
- Direct traffic and refresh traffic cannot address the same generated limiter counter.
- Direct opt-in, hosted exchange, refresh, and browser revocation route boundaries remain deterministic and independently asserted.
- All high-severity threats have concrete automated mitigations.
</success_criteria>

<output>
Create `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-04-SUMMARY.md` when done.
</output>
