---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
plan: 02
type: execute
wave: 2
depends_on:
  - 246.2-01
files_modified:
  - scripts/ci/generated-app-login-runtime-proof.sh
  - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
  - test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
  - .github/workflows/generated-app-login-runtime-proof.yml
autonomous: true
requirements:
  - APP-04
  - APP-05
must_haves:
  truths:
    - "APP-04 / D-08: generated-host HTTP proof shows every-use rotation and proves replay of the consumed refresh credential revokes only its family and denies that family's next access authentication."
    - "APP-05 / D-05/D-06/D-08: authenticated CSRF-bearing HTTP proof shows a foreign family selector cannot be revoked, an owned family can be revoked, and revoke-all affects the current user's applicable families, with denial on subsequent access."
    - "D-09: all lifecycle checks use real generated routes and PostgreSQL state with bounded readiness/deterministic state assertions, never fixed sleeps or library-only substitutes."
    - "D-09: a versioned exact-key receipt is written atomically last only after every APP-04/APP-05 flag passes, binds current source hashes, and rejects credential or unexpected fields."
  artifacts:
    - path: "scripts/ci/generated-app-login-runtime-proof.sh"
      provides: "Complete routed refresh/revocation matrix and v4 receipt-last writer"
      contains: "sigra.generated-app-login-runtime-proof/v4"
    - path: "test/sigra/planning/phase_246_generated_app_login_runtime_test.exs"
      provides: "Source contract for causal HTTP/PostgreSQL proof and redaction"
      contains: "refresh_reuse_family_revoked"
    - path: "test/sigra/planning/phase_246_runtime_evidence_contract_test.exs"
      provides: "Exact-key v4 receipt/provenance parser and mutation tests"
      contains: "revoke_all_denied_next_access"
    - path: ".github/workflows/generated-app-login-runtime-proof.yml"
      provides: "CI-side exact receipt flag/source validation before artifact upload"
      contains: "sigra.generated-app-login-runtime-proof/v4"
  key_links:
    - from: "consumed refresh replay"
      to: "subsequent FetchAppSession denial"
      via: "real HTTP refresh request, PostgreSQL family state assertion, then protected-route request"
      pattern: "prove_refresh_reuse_revocation[\\s\\S]*assert_access_denied"
    - from: "browser revocation HTTP requests"
      to: "owner-bound generated facade"
      via: "authenticated cookie jar plus rendered CSRF token/header; foreign and owned families are distinguished only by persisted outcome"
      pattern: "prove_revoke_family_owner_isolation[\\s\\S]*prove_revoke_all"
    - from: "runtime proof flags"
      to: "v4 receipt and CI validator"
      via: "all flags checked before temp-file write/atomic rename, then exact keys and current hashes validated"
      pattern: "write_receipt_last[\\s\\S]*runtime-proof/v4"
---

<objective>
Prove the complete APP-04/APP-05 generated HTTP lifecycle and seal it in receipt-last, source-bound evidence.

Purpose: Replace the audit's two partial requirement findings with deterministic causal proof across generated routes, browser security boundaries, PostgreSQL state, and next-auth outcomes.
Output: Refresh-replay and owner-safe revocation runtime proofs plus a fail-closed v4 evidence contract validated identically by shell, ExUnit, and CI.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.49-v1.49-MILESTONE-AUDIT.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-CONTEXT.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-RESEARCH.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-PATTERNS.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VALIDATION.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-01-SUMMARY.md
@.planning/phases/245-opaque-app-session-core/245-03-SUMMARY.md
@.planning/phases/245-opaque-app-session-core/245-05-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-17-SUMMARY.md
@scripts/ci/generated-app-login-runtime-proof.sh
@test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
@test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
@.github/workflows/generated-app-login-runtime-proof.yml

<interfaces>
Plan 01 produces `POST /api/app-login/refresh`, `POST /users/app-sessions/revoke`, and `POST /users/app-sessions/revoke-all` plus `AppLoginController.refresh/2`, `revoke_family/2`, and `revoke_all/2`. The revocation routes require an authenticated recent browser session and Phoenix CSRF token; no access credential supplies their owner.

The proof harness already owns disposable host creation, two installer runs, PostgreSQL setup, bounded HTTP readiness, browser cookie/CSRF flows, an access-only `/api/app-login-proof` route, transient cleanup, and atomic receipt publication. Extend these seams rather than creating a second harness.

The receipt parser and workflow enforce exact top-level keys, all-true required behavior flags, exact source-key sets, SHA-256 bindings, and immutable-run provenance. Any new flag requires a coordinated shell fixture/parser/workflow change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove consumed-refresh replay revokes its family over generated HTTP</name>
  <reversibility rating="reversible">The proof extends an internal disposable-host harness around the already-locked APP-04 lifecycle and can be locally reorganized without changing production behavior.</reversibility>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs</files>
  <read_first>
    Read APP-04; D-03/D-04/D-08/D-09; the audit's broken refresh flow; Plan 01 summary; `Sigra.AppSession.refresh/2`; the complete current harness around `prove_refresh_rotation`, `prove_fetch_app_session`, `prove_host`, and receipt flags; and runtime-source tests around replay, redaction, and receipt-last ordering before editing.
  </read_first>
  <behavior>
    - Test 1 (APP-04): the first presentation of a generated refresh credential rotates it and the replacement access credential authenticates.
    - Test 2 (APP-04, D-04): replaying the consumed original over `POST /api/app-login/refresh` returns the same 401/`invalid_refresh` class as invalid/expired/internal lifecycle failures.
    - Test 3 (APP-04, D-08): PostgreSQL assertions prove the replay marks only the indexed family terminal and revokes its credential rows; an unrelated family remains active.
    - Test 4 (D-08): the previously valid replacement access credential receives bounded unauthenticated denial on the next real `/api/app-login-proof` request.
    - Test 5 (D-09): no replay diagnostic or source contract includes raw credentials, family identifiers, body dumps, or fixed sleeps.
  </behavior>
  <action>
    Add `prove_refresh_reuse_revocation` to the existing hosted generated-host flow after successful rotation. Replay the consumed original refresh credential through the emitted refresh route, require exactly 401 and `{error: "invalid_refresh"}`, then query typed generated PostgreSQL schemas to assert that family and all of its credential rows are revoked while a separately issued control family remains active (APP-04/D-04/D-08). Call `assert_access_denied` (introduced as a bounded-status counterpart to `prove_fetch_app_session`) with the rotated access credential and require the protected route's normal unauthenticated response; do not substitute a direct library call or database state alone. Use deterministic persisted-state queries and existing bounded readiness, not elapsed waits (D-09).

    Extend the source-contract test to require this exact causal order: routed rotation, routed consumed-token replay, typed family/token state, next-access denial, and unrelated-family survival. Retained diagnostics may record only labels such as `refresh_replay=unauthenticated`, `family=revoked`, `control_family=active`, and `next_access=denied`; keep credential values and selectors confined to transient variables/files removed by harness cleanup (D-09).
  </action>
  <acceptance_criteria>
    - The proof invokes the generated refresh URL twice with the same original credential, observes success then the bounded 401 failure, and never exposes `:reuse_detected` or another internal lifecycle label over HTTP (D-04/D-08).
    - Typed PostgreSQL assertions prove the replayed family/token rows are revoked and a control family is still active; family-count-only or captured-log assertions are insufficient (APP-04/D-08).
    - The replacement access credential is denied by the real FetchAppSession route after replay, while the control family's access remains accepted (APP-04).
    - Runtime-source contracts fail if the request/database/next-auth order is removed, a direct library proof replaces HTTP, a fixed sleep appears, or retained diagnostics contain a credential/family/body value (D-09).
    - The focused ExUnit command and complete generated-host script pass.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_generator_test.exs test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace</automated>
  </verify>
  <done>Consumed refresh replay through the generated route produces one bounded failure, revokes only its family in PostgreSQL, and denies the replacement access credential on its next routed authentication (APP-04; D-03/D-04/D-08/D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove owner-isolated one/all revocation and next-auth denial</name>
  <reversibility rating="reversible">This task adds deterministic proof around locked D-05 through D-07 routes and can change its fixture arrangement without changing the public revocation contract.</reversibility>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs</files>
  <read_first>
    Read APP-05; D-05 through D-09; the audit's broken user-revocation flow; Plan 01 summary; `Sigra.AppSession.revoke_family_for_user/3` and `revoke_all_for_user/2`; generated browser login/sudo/CSRF helpers already in the harness; and the final Task 1 proof before editing.
  </read_first>
  <behavior>
    - Test 1 (D-06): missing browser authentication, missing recent-auth status, or missing/invalid CSRF cannot successfully reach either revocation action.
    - Test 2 (APP-05, D-05): a logged-in owner presenting another user's family selector receives 404/`not_found`; PostgreSQL state and the foreign access route prove no mutation.
    - Test 3 (APP-05): the same owner revokes an owned family through HTTP, receives `{ok: true}`, and that family's next access is denied while another owned family remains active.
    - Test 4 (APP-05, D-05): revoke-all for that browser owner revokes all remaining active families for that owner, does not revoke a different user's family, and denies each affected access credential on the next routed request.
    - Test 5 (D-07/D-09): proof uses mutation routes only and retains no session listing, UI, administrator, credential, cookie, CSRF, email, or family value.
  </behavior>
  <action>
    Add `prove_revoke_family_owner_isolation` and `prove_revoke_all` to the disposable hosted flow. Reuse the generated browser login/recent-auth cookie jar and extract its rendered CSRF token into a transient variable; POST JSON requests with the CSRF header to the two Plan 01 routes (D-06). Arrange typed PostgreSQL fixtures with at least two active families for the authenticated owner and one for a different user. First submit the foreign selector and require 404/`not_found`, unchanged foreign rows, and successful foreign protected-route access. Then revoke one owned family, require 200 `{ok: true}`, typed family/token revocation, denial of its access, and continued access for the owner's second family (D-05/D-08).

    Finally call revoke-all as the same browser owner, require 200 `{ok: true}`, assert every remaining active family/token for that owner is revoked, assert the other user's family remains active, and prove all affected access credentials fail on the next real FetchAppSession request while the foreign control still succeeds (APP-05). Add negative requests for unauthenticated, non-sudo, and missing/invalid CSRF boundaries without printing or retaining cookies/tokens. Extend runtime-source tests to pin real routes, browser cookie + CSRF transport, trusted-owner fixtures, typed state checks, next-auth outcomes, and mutation-only scope; never introduce a list request or human/browser UAT (D-07/D-09).
  </action>
  <acceptance_criteria>
    - Both successful HTTP mutations carry an authenticated recent browser cookie and valid CSRF header; unauthenticated, stale/non-sudo, and invalid-CSRF requests cannot return success (D-06).
    - Foreign one-family selection returns the normalized 404 and leaves the foreign family/access usable; no request field or route segment can override the Scope owner (D-05).
    - Owned one-family revocation denies only that family's access, and revoke-all then denies every remaining owner family while preserving the other user's control family (APP-05/D-05/D-08).
    - Each denial/continued-access assertion goes through the real generated FetchAppSession route after typed PostgreSQL state checks; direct library-only evidence cannot pass (D-08).
    - Source contracts reject listing/UI/admin additions, fixed sleeps, and retained credential/cookie/CSRF/email/family values; the full script passes without human UAT (D-07/D-09).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_generator_test.exs test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace</automated>
  </verify>
  <done>Real authenticated/CSRF generated-host requests prove foreign-family isolation, owned-family revocation, current-user revoke-all, control-user survival, and denial on each affected credential's next authentication (APP-05; D-05 through D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seal APP-04/APP-05 proof in an exact-key v4 receipt</name>
  <reversibility rating="costly">The exact-key retained evidence schema is consumed by shell, ExUnit, CI, and provenance; changing it requires a coordinated versioned update across all validators, so this task bumps v3 to v4.</reversibility>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs, test/sigra/planning/phase_246_runtime_evidence_contract_test.exs, .github/workflows/generated-app-login-runtime-proof.yml</files>
  <read_first>
    Read D-08/D-09; `246.2-VALIDATION.md` receipt row; the final Task 1/2 harness flags; the complete `write_receipt_last`; `@required_behaviors`, `@sources`, fixture/mutation/validate functions in the evidence contract; and the workflow's receipt validation/upload ordering before editing any file.
  </read_first>
  <behavior>
    - Test 1: schema is exactly `sigra.generated-app-login-runtime-proof/v4` and requires all prior v3 behaviors plus the seven APP-04/APP-05 flags listed below.
    - Test 2: every new behavior must equal boolean true; missing, false, renamed, extra, or credential-bearing top-level fields fail closed in ExUnit and CI.
    - Test 3: source bindings remain exact and cover the controller, router, facade, runtime script, workflow, runtime source contract, and evidence contract at the proven current head.
    - Test 4: `write_receipt_last` checks all lifecycle flags before writing a temporary file and atomically renaming it; failed proof cannot publish a passed receipt.
    - Test 5: artifact upload remains after validation and receives only the bounded receipt/diagnostics contract, never raw credentials.
  </behavior>
  <action>
    Version the exact-key receipt from `sigra.generated-app-login-runtime-proof/v3` to `/v4` because APP-04/APP-05 add required fields to a closed schema. Add exactly these boolean receipt fields: `refresh_rotated`, `refresh_reuse_family_revoked`, `refresh_reuse_denied_next_access`, `revoke_family_owner_isolated`, `revoke_family_denied_next_access`, `revoke_all_current_user_only`, and `revoke_all_denied_next_access`. Set each causal shell flag only after its corresponding HTTP, typed PostgreSQL, and next-auth assertions have passed; `write_receipt_last` must require all seven plus all prior behavior flags before writing the temp file and atomic rename (D-08/D-09).

    Update `@required_behaviors`, the valid fixture, negative mutations, exact-key parser, runtime-source markers, and workflow Python validator in one commit so all three consumers require v4 and the identical flag set. Preserve the existing exact source-key map, which already binds `app_login_controller`, `router_injection`, `auth_app_sessions`, `runtime_script`, `workflow`, `runtime_source_contract_test`, and `runtime_evidence_contract_test`; verify hashes against current source/immutable provenance rather than adding redundant keys. Add negative assertions for a false new flag, an omitted flag, an unexpected lifecycle detail, and top-level `access_token`, `refresh_token`, `family_id`, `cookie`, or `csrf_token` fields. Keep validation before upload and do not write/update retained Phase 246 evidence files during implementation; fresh evidence is produced only by the authorized proof workflow (D-09).
  </action>
  <acceptance_criteria>
    - Shell, ExUnit, and workflow all require schema `/v4`, every prior behavior, and exactly the seven named APP-04/APP-05 booleans; their sets are byte-for-byte equivalent after normalization.
    - A missing/false/extra flag, unexpected lifecycle detail, credential/session/CSRF field, source-key drift, source SHA mismatch, invalid provenance, or non-success conclusion fails closed before artifact validation succeeds (D-09).
    - Every new shell flag is assigned only after its routed HTTP, typed persisted-state, and next-auth assertion; `write_receipt_last` checks all flags before temp-file creation and atomic rename (D-08/D-09).
    - Exact sources bind all modified production/proof/validator files at the proven head, and upload remains ordered after receipt validation.
    - The focused evidence/source suite and complete generated-host proof pass; no human approval, skip, or unavailable-PostgreSQL waiver is accepted.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_generator_test.exs test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace</automated>
  </verify>
  <done>A v4 exact-key receipt is atomically published only after all routed APP-04/APP-05 lifecycle flags pass, remains credential-free and current-source-bound, and is identically validated by shell, ExUnit, and CI (D-08/D-09).</done>
</task>

</tasks>

<wave_gate>
  <name>Wave 2 final generated-host proof and evidence gate</name>
  <action>After all three Wave 2 task commits pass their focused ExUnit checks, run the complete disposable-host proof once as the final phase gate. Require routed refresh replay, owner-isolated one/all revocation, typed PostgreSQL state, next-auth denial/control survival, and atomic exact-key v4 receipt publication to pass together (APP-04/APP-05; D-03 through D-09).</action>
  <verify>
    <automated>scripts/ci/generated-app-login-runtime-proof.sh --all</automated>
  </verify>
  <acceptance_criteria>The complete APP-04/APP-05 matrix and credential-free current-source-bound v4 evidence pass in one final run; unavailable PostgreSQL/Phoenix setup, missing proof flags, or receipt/provenance failure blocks phase completion.</acceptance_criteria>
</wave_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated HTTP responses → lifecycle proof | Status/body checks must be causally tied to persisted mutation and next-auth outcome, not treated as sufficient alone. |
| Browser cookie/CSRF → revocation routes | Transient browser authority is required for proof but must never enter retained evidence. |
| PostgreSQL rows → proof classifications | Typed state establishes exact family isolation while identifiers remain redacted. |
| Shell flags → receipt publisher | A passed artifact must be impossible until every required transition is true. |
| Repository files/provenance → CI evidence claim | Source hashes and immutable-head metadata bind the evidence to the implementation actually proven. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246.2-06 | Repudiation / Tampering | Refresh replay proof | high | mitigate | Require real HTTP result, typed family/token state, unrelated-family control, and subsequent FetchAppSession denial before setting receipt flags. |
| T-246.2-07 | Elevation of Privilege | Browser one/all revocation proof | high | mitigate | Prove browser auth, recent-auth, CSRF, Scope-derived ownership, foreign selector denial, foreign control survival, and next-auth denial. |
| T-246.2-08 | Information Disclosure | Harness diagnostics/artifacts | high | mitigate | Permit only outcome classifications and boolean receipt fields; exact-key validators reject credentials, selectors, cookies, CSRF, emails, and lifecycle internals. |
| T-246.2-09 | Tampering / Repudiation | Receipt publication | high | mitigate | Version to v4, require all prior/new flags before temp write, atomic rename last, exact current-head source hashes, and immutable provenance validation. |
| T-246.2-10 | Denial of Service | Runtime readiness/concurrency | medium | mitigate | Reuse bounded readiness polling and deterministic typed state/barriers; no fixed sleeps, hidden retries, or skip-on-missing-PostgreSQL paths. |
| T-246.2-SC | Tampering | Package installs | low | accept | No dependency is added; existing pinned CI setup remains under its current source-bound workflow contract. |
</threat_model>

## Artifacts this phase produces

- No new production module, route, controller action, facade, lifecycle function, schema, migration, or UI file; Plan 01 owns all production surface symbols.
- New shell proof functions in `scripts/ci/generated-app-login-runtime-proof.sh`: `assert_access_denied`, `prove_refresh_reuse_revocation`, `prove_revoke_family_owner_isolation`, and `prove_revoke_all`. Existing `prove_refresh_rotation` is expanded only as needed for causal flag handoff.
- New receipt schema identifier: `sigra.generated-app-login-runtime-proof/v4`.
- New receipt fields: `refresh_rotated`, `refresh_reuse_family_revoked`, `refresh_reuse_denied_next_access`, `revoke_family_owner_isolated`, `revoke_family_denied_next_access`, `revoke_all_current_user_only`, and `revoke_all_denied_next_access`.
- No new receipt source key: the existing exact keys already bind the controller, router, facade, script, workflow, runtime source test, and evidence test. The values change to current-file SHA-256 digests when the fresh proof runs.
- Modified files: `scripts/ci/generated-app-login-runtime-proof.sh`, `test/sigra/planning/phase_246_generated_app_login_runtime_test.exs`, `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs`, and `.github/workflows/generated-app-login-runtime-proof.yml`.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| ROADMAP/GOAL | — | Secure generated refresh/revocation with causal fresh-host evidence | 01-02 | COVERED | Plan 01 ships/traces; this plan completes lifecycle/evidence. |
| REQUIREMENTS | APP-04 | Opaque defaults, atomic rotation, consumed-reuse family revoke | 01-02 | COVERED | Existing lifecycle plus public consumer, routed rotation/replay, family isolation, next-auth denial. |
| REQUIREMENTS | APP-05 | One/all and security-event revocation take effect next auth | 01-02 | COVERED | Existing security events remain; generated user consumers and routed one/all proof close reachability. |
| MILESTONE AUDIT | APP-04 | Refresh facade lacks generated HTTP consumer/proof | 01-02 | COVERED | Plan 01 consumer/tracer; Task 1 replay closure. |
| MILESTONE AUDIT | APP-05 | One/all facades lack generated consumer/proof | 01-02 | COVERED | Plan 01 consumers; Task 2 owner/CSRF/denial closure. |
| CONTEXT | D-01 | Public POST accepts only refresh_token and delegates | 01 | COVERED | Plan 01 Task 1. |
| CONTEXT | D-02 | No browser/access auth; refresh never ordinary auth | 01 | COVERED | Plan 01 tracer proves both sides. |
| CONTEXT | D-03 | Four-field committed credential shape | 01 | COVERED | Plan 01 controller/runtime assertions. |
| CONTEXT | D-04 | Request/lifecycle error bounds plus rate limit | 01-02 | COVERED | Plan 01 mapping; this plan replay check. |
| CONTEXT | D-05 | Scope owner for one/all revocation | 01-02 | COVERED | Plan 01 actions; Task 2 foreign/current-owner proof. |
| CONTEXT | D-06 | Browser/authenticated/sudo/CSRF boundary | 01-02 | COVERED | Plan 01 routes; Task 2 negative/positive requests. |
| CONTEXT | D-07 | Mutation-only, no listing/UI/admin/new lifecycle | 01-02 | COVERED | Source/runtime contracts enforce scope fence. |
| CONTEXT | D-08 | Generated source + disposable HTTP/PostgreSQL closure | 01-02 | COVERED | Tracer, replay, one/all, next-auth matrix. |
| CONTEXT | D-09 | Deterministic, no sleeps/UAT/raw retained credentials | 01-02 | COVERED | Bounded harness plus exact-key v4 receipt. |
| RESEARCH | — | Reuse existing limiter/controller/facade/harness | 01-02 | COVERED | No new subsystem or dependency. |
| RESEARCH | — | Real route + PostgreSQL + exact source-bound receipt | 01-02 | COVERED | All three tasks require this causal chain. |
| PATTERNS | — | Bounded readiness, typed state, receipt-last atomic publish | 02 | COVERED | Tasks 1-3 extend the exact analogs. |
| VALIDATION | 246.2-02-01 | Rotation/replay/family revoke/next denial | 02 | COVERED | Task 1. |
| VALIDATION | 246.2-02-02 | Owner isolation, one/all revoke, next denial | 02 | COVERED | Task 2. |
| VALIDATION | 246.2-02-03 | Credential-free exact source-bound receipt | 02 | COVERED | Task 3. |

Deferred device/session listing, management UI, admin/operator controls, PWA/native/Crosswake/Electron work, and new lifecycle APIs are explicit exclusions, not gaps. Research's package audit is satisfied because no install task exists.

Spec-less probe fallback skipped: phase init had no mapped requirement IDs to probe (visible skip). APP-04 and APP-05 are canonical from REQUIREMENTS.md and the milestone audit and appear in plan frontmatter/tasks.

External API detector remains `detected:false`; no COVERAGE.md is created. Assumption-delta detector remains `detected:false`; no checkpoint is added. Schema-push scan remains empty; no schema task is added. UI gate remains `frontend:false`; no UI work is planned.

<verification>
1. Run the focused ExUnit command from `246.2-VALIDATION.md`; all rendered/runtime/evidence contracts must pass.
2. Run `scripts/ci/generated-app-login-runtime-proof.sh --all`; unavailable PostgreSQL/Phoenix setup is a blocking failure, never a skip or waiver.
3. Confirm v4 receipt exact keys and current source hashes are validated before artifact upload, and that no raw credential/session/CSRF value exists in retained output.
4. Confirm `git diff --` over the four plan files contains no product UI, schema, lifecycle-library, external-service, or unrelated ceremony expansion.
</verification>

<success_criteria>
- APP-04 is proven through generated HTTP rotation, consumed-token replay family revocation, unrelated-family survival, and next-access denial.
- APP-05 is proven through authenticated/sudo/CSRF one/all requests, owner isolation, current-user-only mutation, control-user survival, and next-access denial.
- The exact-key v4 receipt cannot publish until all prior and new proof flags pass and all source/provenance bindings validate.
- Every high-severity threat is deterministically mitigated; no human UAT, secret-bearing evidence, or scope expansion remains.
</success_criteria>

<output>
Create `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-02-SUMMARY.md` when done.
</output>
