---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
plan: 03
type: execute
wave: 3
depends_on:
  - 246.2-02
files_modified:
  - test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
  - .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json
  - .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json
  - .planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-RUNTIME-PROOF-RUN-FAILED.json
autonomous: true
gap_closure: true
requirements:
  - APP-04
  - APP-05
must_haves:
  truths:
    - "APP-04 adjacency: the retained proof requires the already-proven immediate second presentation of a consumed refresh credential to revoke exactly that family and deny its replacement access on next authentication (D-08)."
    - "APP-04 empty input: malformed, missing, or null refresh transport remains a bounded request failure and cannot contribute a successful v4 lifecycle flag (D-04/D-08)."
    - "APP-04 ordering: JSON object key order is irrelevant, but exact key membership, all-true lifecycle flags, successful immutable provenance, and current source hashes must all validate before canonical evidence passes (D-08/D-09)."
    - "APP-05 adjacency: one-family revocation followed by revoke-all preserves the foreign control family while denying each affected owned family on its next authentication (D-05/D-08)."
    - "APP-05 ordering: retained evidence is valid only when the routed one/all mutations, PostgreSQL outcomes, and next-auth checks completed before receipt-last publication (D-08/D-09)."
    - "Whenever the canonical receipt exists, it is required to be exact schema v4 and is passed with its provenance through the same fail-closed validator as the v4 fixture; stale v3 or malformed evidence cannot satisfy APP-04 or APP-05."
  artifacts:
    - path: "test/sigra/planning/phase_246_runtime_evidence_contract_test.exs"
      provides: "Unconditional canonical v4 receipt/provenance validation and stale-schema regression"
      contains: "canonical retained evidence uses the same fail-closed parser when present"
    - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json"
      provides: "Byte-identical credential-free v4 artifact from one successful exact-head PostgreSQL workflow run"
      contains: "sigra.generated-app-login-runtime-proof/v4"
    - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json"
      provides: "Exact workflow run, immutable head, artifact, and receipt SHA-256 provenance for canonical v4 evidence"
      contains: "prior immutable implementation head"
  key_links:
    - from: "canonical retained receipt and provenance"
      to: "test/sigra/planning/phase_246_runtime_evidence_contract_test.exs"
      via: "unconditional decode plus validate/2 whenever the receipt exists"
      pattern: "File.exists.*@receipt_path[\\s\\S]*validate\\(receipt, decode!\\(@provenance_path\\)\\)"
    - from: "GitHub workflow artifact"
      to: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json"
      via: "byte-for-byte copy after v4 exact-key, source-SHA, provenance, and redaction validation"
      pattern: "sigra.generated-app-login-runtime-proof/v4"
    - from: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json"
      to: "the pushed Task 1 implementation commit"
      via: "one workflow_dispatch run selected by event, ref, and exact head SHA"
      pattern: "workflow_dispatch|head_sha|receipt_sha256"
  assumptions:
    - id: A-246.2-03-APP05-EMPTY
      status: flagged
      statement: "APP-05 does not specify whether revoke-all over an owner with zero active families reports success or not-found; this gap closure preserves existing semantics and proves only the required non-empty one/all lifecycle without adding a new claim."
  prohibitions:
    - id: P-246.2-03-01
      requirement_id: APP-04
      category: transparency
      status: unverified
      flagged: true
      statement: "The retained receipt must not represent stale, schema-downgraded, source-mismatched, or locally synthesized evidence as successful APP-04 proof."
      reason: "Descriptor-less spec-less prohibition recall item; canonical parsing and exact-head CI provenance are planned, but no plain SPEC author-resolved a prohibition check descriptor."
    - id: P-246.2-03-02
      requirement_id: APP-05
      category: transparency
      status: unverified
      flagged: true
      statement: "The provenance must not imply that the later evidence-only commit, a different branch head, or a second dispatch was exercised by the selected APP-05 runtime run."
      reason: "Descriptor-less spec-less prohibition recall item; exact selected-run provenance is planned, but no plain SPEC author-resolved a prohibition check descriptor."
---

<objective>
Close the retained-evidence blocker for APP-04 and APP-05 by rejecting every non-v4 canonical receipt and publishing one fresh exact-head v4 PostgreSQL artifact with matching provenance.

Purpose: Make the phase's final receipt-last claim fail closed against the committed canonical files, not only against synthetic v4 fixtures.
Output: A repaired canonical evidence validator plus byte-identical v4 receipt and exact successful-run provenance.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-CONTEXT.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-02-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json
@test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
@test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
@scripts/ci/generated-app-login-runtime-proof.sh
@.github/workflows/generated-app-login-runtime-proof.yml

<interfaces>
`validate/2` in `phase_246_runtime_evidence_contract_test.exs` already requires exact v4 top-level keys, every APP-04/APP-05 behavior boolean, the exact source-key set, a successful one-dispatch provenance record, receipt SHA-256 equality, and source hashes read from `provenance["head_sha"]`. The gap is only the canonical test's non-v4 branch, which refutes v4 instead of invoking this validator.

The workflow emits artifact `generated-app-login-runtime-proof` containing `runtime-proof.json`. The canonical files remain `246-RUNTIME-PROOF.json` and `246-RUNTIME-PROOF-RUN.json`; evidence proves the immutable implementation commit before the later evidence-only commit.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make canonical retained evidence fail closed on stale schema</name>
  <reversibility rating="reversible">The change is confined to an evidence-contract test and can be revised without changing production data, routes, or public APIs.</reversibility>
  <files>test/sigra/planning/phase_246_runtime_evidence_contract_test.exs</files>
  <read_first>
    - `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md` — read the single actionable gap and ignore warnings/deferred work.
    - `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` — read the canonical test, `validate/2`, `valid_provenance?/2`, and exact-key helpers together.
    - `.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json` and `246-RUNTIME-PROOF-RUN.json` — confirm the current canonical schema/head relationship before changing the gate.
  </read_first>
  <behavior>
    - A synthetic v3 canonical-shaped receipt is rejected by the same validation contract that accepts the exact v4 fixture.
    - When the canonical receipt exists, missing provenance fails and every receipt schema, including stale v3, is decoded and passed to `validate/2` without a permissive branch.
    - The currently committed v3 receipt makes the canonical retained-evidence test fail for the intended stale-schema reason until Task 2 supplies exact v4 evidence.
    - Receipt absence retains the existing fail-closed rule that successful provenance cannot exist without a receipt.
  </behavior>
  <action>
    Add a focused stale-schema regression tagged `:stale_schema_regression` that feeds a v3 mutation of `valid_receipt()` to `validate/2` and requires `{:error, :invalid_runtime_evidence}`. Tag the canonical retained-evidence test `:canonical_retained`, then replace its schema conditional with one unconditional `assert :ok = validate(receipt, decode!(@provenance_path))` whenever the canonical receipt exists, per APP-04/APP-05 and D-08/D-09. Preserve exact-key, all-true behavior, receipt-hash, immutable-head, and source-SHA checks; do not relax them to make the current v3 files green. Preserve the receipt-absent/successful-provenance guard. Commit this test-only implementation, push the exact non-detached branch head used for Task 2, and record the 40-character implementation SHA and remote ref in the executor summary.
  </action>
  <acceptance_criteria>
    - A direct v3 input to `validate/2` returns `{:error, :invalid_runtime_evidence}`.
    - The canonical receipt-present branch has exactly one outcome: the decoded receipt and provenance must satisfy `validate/2`; no schema fallback, skip, refutation-only branch, or alternate parser remains.
    - The focused stale-schema regression passes while the canonical retained-evidence test fails specifically because the committed receipt is v3; Task 1 does not overwrite or manually edit either canonical JSON file.
    - The validator implementation commit is pushed on a non-detached remote ref before any workflow dispatch, so Task 2 can select one run by exact head SHA.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --only stale_schema_regression --trace &amp;&amp; ! MIX_ENV=test mix test test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --only canonical_retained --trace &amp;&amp; jq -e '.schema == "sigra.generated-app-login-runtime-proof/v3"' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json</automated>
  </verify>
  <done>The canonical evidence path is fail-closed and the stale v3 receipt is an explicit red gate pending one fresh exact-head v4 workflow artifact (APP-04/APP-05; D-08/D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch once and retain exact v4 PostgreSQL evidence</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`; Task 1's complete test-only commit is pushed on a non-detached remote ref and its exact SHA is recorded; one `gh api rate_limit` read reports REST core remaining above 250; no watcher is polling the selected workflow run; and the canonical files still contain the pre-dispatch v3 receipt/provenance.</precondition>
  <reversibility rating="costly">A retained evidence claim can be superseded, but doing so requires another independently selected immutable-head CI run and coordinated receipt/provenance replacement.</reversibility>
  <files>.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json, .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json, .planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-RUNTIME-PROOF-RUN-FAILED.json</files>
  <read_first>
    - `AGENTS.md` — apply the one-watcher, 60-second interval, structured-summary-once, failed-logs-only-on-failure, and rate-limit hard-stop rules.
    - `.github/workflows/generated-app-login-runtime-proof.yml` — confirm workflow name, dispatch event, exact v4 validator, and artifact name.
    - `scripts/ci/generated-app-login-runtime-proof.sh` — confirm receipt-last publication and credential-free artifact layout.
    - `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` at Task 1's pushed SHA — use its v4 field/source/provenance contract as the offline validator.
    - `.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json` — preserve the established provenance vocabulary while replacing run/head/artifact facts truthfully.
  </read_first>
  <behavior>
    - Exactly one workflow_dispatch run is created and selected by workflow, event, remote ref, and Task 1's exact head SHA.
    - Exactly one watcher observes that run with the required 60-second interval; no second watcher or redispatch occurs.
    - A successful run's named artifact is downloaded once, validated as exact v4 against the immutable implementation head, redaction-checked, and copied byte-for-byte to the canonical receipt.
    - Canonical provenance records the selected run, exact implementation head/ref, one dispatch, artifact identity, receipt SHA-256, and the statement that the later evidence-only commit is outside coverage.
    - Any dispatch, selection, rate-limit, workflow, artifact, redaction, or offline-validation failure blocks passing evidence and writes only bounded durable failure diagnostics to `246.2-RUNTIME-PROOF-RUN-FAILED.json`.
  </behavior>
  <action>
    Read the REST rate limit once, then dispatch `.github/workflows/generated-app-login-runtime-proof.yml` exactly once on Task 1's pushed ref. Identify exactly one new `workflow_dispatch` run whose head SHA equals the recorded implementation SHA. Follow AGENTS.md with one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; after completion fetch the structured run summary once, and fetch failed logs only if the conclusion is failure. Do not redispatch after failure, cancellation, timeout, ambiguous run selection, missing artifact, validation failure, HTTP 403/429, or rate-limit exhaustion. Allow one retry only for a transient non-rate-limit read-only query or artifact-download transport failure after unique run selection.

    On success, download artifact `generated-app-login-runtime-proof` to a temporary directory. Require the receipt to be exact schema v4, status passed, all existing and APP-04/APP-05 booleans true, the exact source-key set, and SHA-256 values equal to files at Task 1's immutable implementation head. Reject any unexpected or credential/session/CSRF field. Run the Task 1 offline evidence contract against a temporary receipt/provenance pair before replacing canonical evidence. Copy the validated receipt byte-for-byte to `246-RUNTIME-PROOF.json`; write `246-RUNTIME-PROOF-RUN.json` with exact run/head/ref/artifact/receipt-hash facts, dispatch count one, watcher interval 60, success conclusion, and prior-immutable-head coverage statement. Then run the full canonical test so the Task 1 red gate turns green. Commit only the two successful canonical JSON files in a distinct evidence-only commit. On any failure, do not alter the canonical receipt/provenance; write only bounded non-secret diagnostics and the non-coverage statement to the Phase 246.2 failed-run JSON, then stop.
  </action>
  <acceptance_criteria>
    - The selected successful run is unique, uses `workflow_dispatch`, and has head SHA exactly equal to Task 1's pushed implementation commit; provenance records one dispatch and one 60-second watcher.
    - The retained receipt is byte-identical to the downloaded artifact, has exact schema v4 and exact required keys, and its SHA-256 matches provenance.
    - Every source binding resolves against the immutable implementation head, including the repaired evidence-contract test; no local proof, manually synthesized receipt, or different-head artifact can pass.
    - The canonical evidence-only commit changes only `246-RUNTIME-PROOF.json` and `246-RUNTIME-PROOF-RUN.json`, and provenance explicitly excludes that later commit from runtime coverage.
    - The full focused evidence contract passes with the committed canonical files; a blocked run leaves old canonical files untouched and produces no passing claim.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace &amp;&amp; jq -e '.schema == "sigra.generated-app-login-runtime-proof/v4" and .status == "passed" and .refresh_rotated and .refresh_reuse_family_revoked and .refresh_reuse_denied_next_access and .revoke_family_owner_isolated and .revoke_family_denied_next_access and .revoke_all_current_user_only and .revoke_all_denied_next_access' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json &amp;&amp; jq -e '.event == "workflow_dispatch" and .dispatch_attempts == 1 and .watch_interval_seconds == 60 and .conclusion == "success" and (.head_sha | test("^[0-9a-f]{40}$")) and (.receipt_sha256 | test("^[0-9a-f]{64}$")) and (.coverage_statement | contains("prior immutable implementation head"))' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json &amp;&amp; git diff --check</automated>
  </verify>
  <done>One successful exact-head PostgreSQL run is retained as byte-identical v4 receipt/provenance, and the canonical fail-closed gate now proves APP-04/APP-05 without stale-evidence escape hatches (D-08/D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical JSON → offline evidence validator | Durable evidence is untrusted until exact v4 keys, booleans, provenance, receipt hash, and source bindings all pass. |
| GitHub workflow run → local retained evidence | Mutable external CI output becomes canonical only when selected by event/ref/exact head and downloaded from one successful run. |
| Runtime artifact → repository commit | The downloaded receipt must remain byte-identical and credential-free; the later evidence-only commit is not part of runtime coverage. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246.2-11 | Tampering / Repudiation | canonical receipt validator | high | mitigate | Remove the non-v4 acceptance branch and require every present canonical receipt/provenance pair to pass the existing exact v4 `validate/2` contract. |
| T-246.2-12 | Spoofing / Repudiation | workflow run selection | high | mitigate | Dispatch once and select one run by workflow, event, pushed ref, and exact Task 1 head SHA before starting the sole 60-second watcher. |
| T-246.2-13 | Tampering | artifact retention | high | mitigate | Validate exact v4 keys, all-true behaviors, immutable-head source SHAs, redaction, and receipt SHA-256 before byte-identical canonical replacement. |
| T-246.2-14 | Information Disclosure | artifact and durable diagnostics | high | mitigate | Reject credential/session/CSRF fields, retain only allowlisted booleans/hashes/provenance, and keep failure diagnostics bounded and secret-free. |
| T-246.2-15 | Denial of Service | GitHub API/watch operations | medium | mitigate | Read rate limit before the one watcher, use a 60-second interval, allow only one scoped transient read/download retry, and hard-stop on 403/429 or exhausted budget. |
| T-246.2-SC | Tampering | package installs | low | accept | No package-manager install occurs; the existing pinned workflow and proof dependencies are unchanged. |
</threat_model>

## Artifacts this phase produces

- Modified validator file: `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs`; no new public symbol, route, controller action, lifecycle function, schema, migration, configuration flag, or UI field.
- Replaced canonical receipt: `.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json`, exact schema `sigra.generated-app-login-runtime-proof/v4`; no new receipt field beyond the seven v4 lifecycle fields already defined by Plan 02.
- Replaced canonical provenance: `.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json`, carrying the existing exact run/head/ref/artifact/hash/coverage fields with current values.
- Conditional failure-only diagnostic: `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-RUNTIME-PROOF-RUN-FAILED.json`; created only if Task 2 blocks, containing no credentials or passing claim.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Receipt-last PostgreSQL evidence proves routed refresh/revocation outcomes | 246.2-03 | COVERED | Tasks 1-2 make canonical evidence fail closed and current. |
| REQ | APP-04 | Rotation and consumed-refresh family revocation | 246.2-03 | COVERED | Existing behavior remains verified; current v4 retained evidence becomes mandatory. |
| REQ | APP-05 | One/all revocation affects next authentication | 246.2-03 | COVERED | Existing behavior remains verified; current v4 retained evidence becomes mandatory. |
| VERIFICATION GAP | — | Require v4 and validate canonical receipt/provenance whenever present | 246.2-03 | COVERED | Task 1 removes the permissive non-v4 branch. |
| VERIFICATION GAP | — | Produce and retain current v4 receipt/provenance | 246.2-03 | COVERED | Task 2 dispatches once and retains exact-head CI evidence. |
| RESEARCH | — | Exact-key source-bound receipt and immutable provenance | 246.2-03 | COVERED | Existing v4 validator/workflow contract is reused without a new dependency. |
| CONTEXT | D-01–D-07 | Generated refresh/revocation production boundaries | 246.2-01–02 | COVERED | Already verified; this plan changes no production surface. |
| CONTEXT | D-08 | Disposable generated HTTP/PostgreSQL proof | 246.2-03 | COVERED | Task 2 retains the exact successful workflow artifact. |
| CONTEXT | D-09 | Deterministic, receipt-last, credential-free evidence | 246.2-03 | COVERED | Both tasks fail closed; one watcher, no sleeps/UAT, exact redacted evidence. |
| SPEC-LESS EDGE | APP-04 | adjacency, empty, ordering | 246.2-03 | COVERED | Three explicit must-have truths preserve replay, malformed transport, and receipt ordering/exactness. |
| SPEC-LESS EDGE | APP-05 | adjacency, ordering | 246.2-03 | COVERED | Two explicit truths preserve one/all isolation and causal publication order. |
| SPEC-LESS EDGE | APP-05 | empty | 246.2-03 | COVERED | Explicit flagged assumption A-246.2-03-APP05-EMPTY; no new semantic claim is invented. |
| SPEC-LESS PROHIBITION | APP-04 | stale or synthesized evidence | 246.2-03 | COVERED | Descriptor-less P-246.2-03-01 is carried flagged-unverified. |
| SPEC-LESS PROHIBITION | APP-05 | provenance overclaim | 246.2-03 | COVERED | Descriptor-less P-246.2-03-02 is carried flagged-unverified. |

Deferred device/session listing, management UI, admin/operator controls, PWA/native/Crosswake/Electron work, and new lifecycle APIs remain excluded. Verification warnings about server shutdown and unrelated callback/runtime-test cleanup are not actionable `gaps:` items and are not planned here. Canon security/privacy candidates were referred to the ASVS threat model rather than minted as bespoke prohibitions. No package install, external API matrix, schema/ORM, assumption-delta decision, or UI work is required.

<verification>
1. Task 1 proves the current v3 canonical receipt is rejected by the same validator used for v4 fixtures.
2. Task 2 selects and watches exactly one exact-head workflow run, validates the downloaded v4 artifact before canonical replacement, and makes the complete canonical evidence test green.
3. Confirm the successful evidence-only commit changes only the two canonical JSON files, or that a blocked run leaves them untouched and records bounded diagnostics without a passing claim.
4. Run `git diff --check`; no production route, controller, lifecycle, schema, UI, package, or unrelated warning repair enters this plan.
</verification>

<success_criteria>
- A present canonical v3, malformed, stale-source, bad-provenance, or unexpected-field receipt cannot pass the evidence contract.
- Canonical v4 receipt/provenance comes byte-for-byte from one successful workflow_dispatch run for Task 1's exact pushed implementation head.
- APP-04 and APP-05 retained evidence contains every required true lifecycle flag and current source binding without credentials, cookies, CSRF values, or family selectors.
- Every high-severity threat has a deterministic mitigation; no human UAT, redispatch, second watcher, local-evidence substitute, or deferred-scope work remains.
</success_criteria>

<output>
Create `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-03-SUMMARY.md` when done.
</output>
