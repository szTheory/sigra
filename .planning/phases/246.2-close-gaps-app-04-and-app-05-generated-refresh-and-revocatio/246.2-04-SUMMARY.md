---
phase: 246.2
plan: 04
subsystem: generated app-login router
tags: [app-sessions, refresh, direct-login, rate-limit, generated-router]
requires:
  - 246.2-03 exact-head runtime evidence
provides:
  - independent generated direct-login and refresh limiter budgets
  - rendered route-scope regression coverage
affects: [APP-04, APP-05, generated app-login routes]
tech-stack:
  added: []
  patterns: [EEx opt-in limiter pipeline, exclusive public credential scopes]
key-files:
  created:
    - .planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-04-SUMMARY.md
  modified:
    - priv/templates/sigra.install/app_sessions/router_injection.ex
    - test/sigra/install/app_sessions_routes_test.exs
decisions:
  - "Generated direct login uses an opt-in dedicated rate-limit pipeline, leaving refresh replay revocation reachable through its independent limiter."
metrics:
  duration: "about 4 minutes"
  completed: "2026-08-18"
  tasks_completed: 1
  files_modified: 2
status: complete
---

# Phase 246.2 Plan 04: Independent direct and refresh limiter budgets Summary

**Generated direct-password and MFA traffic now consumes a dedicated limiter budget, so refresh replay reaches its existing family-revocation transaction independently.**

## Accomplishments

- Added the opt-in `:app_login_direct` rate-limit pipeline with a unique key prefix and host-configurable limit/window keys.
- Moved `/api/app-login/direct` and `/api/app-login/direct/mfa` into their own EEx-gated scope.
- Kept hosted exchange public-only, refresh refresh-only, and browser/sudo revocation routes unchanged.
- Added rendered-template regression coverage for the three exclusive credential scopes and the hosted-only variant.

## Verification

- RED: `MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs --trace` failed because `pipeline :app_login_direct` was absent.
- GREEN: `MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs --trace` — 7 tests passed.
- `mix format --check-formatted test/sigra/install/app_sessions_routes_test.exs` — passed.
- `git diff --check` — passed.
- The plan command’s formatter invocation cannot parse `router_injection.ex` because it is an EEx template containing embedded expressions. This pre-existing command incompatibility is recorded in `.planning/WINDOWS.md`; the rendered-template suite remains the deterministic verification for that file.

## Decisions Made

- Generated direct login uses an opt-in dedicated rate-limit pipeline, leaving refresh replay revocation reachable through its independent limiter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Formatting] Formatted the expanded rendered-route test**
- **Found during:** Task 1 verification.
- **Issue:** The newly extended test required one blank line under the repository formatter.
- **Fix:** Ran the formatter on the test and re-ran the focused suite.
- **Files modified:** `test/sigra/install/app_sessions_routes_test.exs`
- **Commit:** `c0b77ce0`

### Verification Exception

The explicit EEx router formatter command is unrun because `mix format` parses the template as raw Elixir and fails at its established `<%= web_module %>` expressions. The focused rendered suite, formatted test file, and diff hygiene checks passed; the exception is tracked as open ledger entry 18.

## Known Stubs

None.

## Self-Check: PASSED

- Verified the router template, rendered-route test, and this summary exist.
- Verified commits `07faaa65`, `01fd21ba`, and `c0b77ce0` exist in Git history.
