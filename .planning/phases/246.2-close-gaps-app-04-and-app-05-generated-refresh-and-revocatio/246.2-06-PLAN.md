---
phase: 246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio
plan: 06
type: execute
wave: 6
depends_on:
  - 246.2-05
files_modified:
  - test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
autonomous: true
gap_closure: true
requirements:
  - APP-04
  - APP-05
must_haves:
  truths:
    - "APP-04/APP-05 / D-08/D-09: retained provenance accepts only artifact name `generated-app-login-runtime-proof`, a run URL derived exactly from repository plus positive run ID, and an implementation reference equal to the immutable 40-hex head."
    - "APP-04 adjacency/ordering assumptions (flagged): evidence identity fields are validated together before any source hash is trusted; equal-looking but non-identical values are rejected."
    - "APP-04 empty assumption (flagged): missing, null, empty, or wrong-typed identity fields fail the exact-key/provenance contract."
    - "APP-05 edge assumptions (flagged): provenance validation changes no one/all revocation semantics, but APP-05 proof cannot pass through an ambiguous artifact identity."
    - "Canonical v4/current provenance is intentionally red until Plan 07 publishes exact v5 evidence for the new immutable implementation head."
  artifacts:
    - path: "test/sigra/planning/phase_246_runtime_evidence_contract_test.exs"
      provides: "Exact v5 receipt and semantic run URL, artifact name, and immutable implementation-ref validation"
      contains: "generated-app-login-runtime-proof"
  key_links:
    - from: "provenance run_id"
      to: "provenance run_url"
      via: "exact GitHub Actions URL construction for szTheory/sigra"
      pattern: "actions/runs|run_id"
    - from: "provenance implementation_ref"
      to: "provenance head_sha"
      via: "exact equality after 40-hex head validation"
      pattern: "implementation_ref.*head_sha|head_sha.*implementation_ref"
    - from: "provenance artifact_name"
      to: "workflow artifact"
      via: "exact constant match"
      pattern: "generated-app-login-runtime-proof"
  assumptions:
    - id: A-246.2-06-APP04-ADJACENCY
      status: flagged
      statement: "A run URL is authentic within this offline contract only when every character equals the canonical repository/run-id URL; redirects and alternate hostnames are not accepted."
    - id: A-246.2-06-APP04-EMPTY
      status: flagged
      statement: "Empty, null, absent, or non-string run URL, implementation reference, and artifact name are invalid evidence."
    - id: A-246.2-06-APP04-ORDERING
      status: flagged
      statement: "Semantic provenance validation precedes source-hash acceptance; map key order itself remains irrelevant."
    - id: A-246.2-06-APP05-EDGES
      status: flagged
      statement: "APP-05 one/all lifecycle edge semantics remain those already proven; this plan only fail-closes the identity of their retained evidence."
  prohibitions:
    - id: P-246.2-06-01
      requirement_id: APP-05
      category: transparency
      status: unverified
      flagged: true
      statement: "Retained evidence must not imply that an arbitrary URL, mutable branch label, or differently named artifact is the exact APP-05 workflow proof."
      reason: "Descriptor-less spec-less prohibition recall item; semantic mutation tests are planned, but no plain SPEC supplied an author-resolved prohibition descriptor."
---

<objective>
Fail-close every retained provenance identity field and align the offline validator with the new causal v5 receipt (APP-04/APP-05; D-08/D-09).

Purpose: Prevent a forged provenance map with valid historical source hashes from impersonating the selected run, artifact, or immutable implementation reference.
Output: An exact v5 evidence parser with rejection mutations for run URL, implementation reference, and artifact name.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md
@.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-05-SUMMARY.md
@test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
@.github/workflows/generated-app-login-runtime-proof.yml
@scripts/ci/generated-app-login-runtime-proof.sh

<interfaces>
`valid_provenance?/2` already checks exact provenance keys, repository/workflow/event, one dispatch, watcher interval, positive run ID, success, 40-hex head, receipt hash, and coverage statement. Extend the same `with` contract with exact `run_url`, exact artifact name, and `implementation_ref == head_sha`. Add `direct_budget_exhausted_before_refresh_replay` to `@required_behaviors` and accept only schema `sigra.generated-app-login-runtime-proof/v5`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject forged retained-evidence identity fields</name>
  <reversibility rating="reversible">This is a test-local evidence contract with no production or storage migration.</reversibility>
  <files>test/sigra/planning/phase_246_runtime_evidence_contract_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_246_runtime_evidence_contract_test.exs` — modify `@required_behaviors`, fixture schema/provenance, mutation coverage, `validate/2`, and `valid_provenance?/2` as one contract.
    - `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-VERIFICATION.md` — close only the three unconstrained provenance fields plus the new causal receipt revision.
    - `.github/workflows/generated-app-login-runtime-proof.yml` — match Plan 05's exact schema/behavior vocabulary.
  </read_first>
  <behavior>
    - The exact synthetic v5 receipt/provenance fixture passes, including the new direct-budget causal flag.
    - Mutating run URL, implementation reference, or artifact name independently makes validation return `{:error, :invalid_runtime_evidence}`.
    - Run URL must equal `https://github.com/szTheory/sigra/actions/runs/` plus the validated positive run ID; artifact name is exact; implementation reference equals the validated head SHA.
    - Missing/null/empty/wrong-typed identity values and unexpected keys continue to fail closed.
    - Current canonical v4 evidence fails the canonical-retained tag until fresh v5 evidence replaces it in Plan 07.
  </behavior>
  <action>
    Write the three independent mutation assertions first and confirm they expose the permissive validator. Update the valid fixture so `implementation_ref` is the exact immutable head, then extend `valid_provenance?/2` with exact derived run URL, exact artifact constant, and implementation-ref/head equality. Update the required behavior set, valid receipt, schema checks, stale-schema regression, and test names to exact v5. Preserve exact key sets, receipt hashing, source hashes at the immutable head, and the unconditional canonical path. Do not resolve identity by making network calls inside ExUnit; Plan 07 independently selects and validates the trusted remote run before writing the values.
  </action>
  <acceptance_criteria>
    - Each of `run_url`, `implementation_ref`, and `artifact_name` has a distinct rejecting mutation test.
    - A matching synthetic v5 receipt/provenance pair passes; every old v4 or absent/forged identity field fails.
    - `implementation_ref` is an immutable SHA, not the dispatch branch/ref label.
    - Canonical evidence remains red rather than receiving a local workaround before the fresh exact-head run.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --exclude canonical_retained --trace &amp;&amp; ! MIX_ENV=test mix test test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --only canonical_retained --trace &amp;&amp; mix format --check-formatted test/sigra/planning/phase_246_runtime_evidence_contract_test.exs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The offline contract rejects every forged run/artifact/ref mutation and deliberately requires one fresh exact-head v5 artifact to turn canonical evidence green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Retained provenance JSON → offline validator | All fields are attacker-editable until exact semantic validation succeeds. |
| Mutable dispatch ref → immutable implementation identity | The retained implementation reference must collapse to the exact exercised head SHA. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246.2-23 | Spoofing / Repudiation | run URL | high | mitigate | Derive and require the exact repository/run-ID GitHub Actions URL; add an independent rejection mutation. |
| T-246.2-24 | Spoofing / Tampering | implementation reference | high | mitigate | Require exact equality with the validated immutable 40-hex head and reject branch labels/different SHAs. |
| T-246.2-25 | Spoofing | artifact name | high | mitigate | Require exact `generated-app-login-runtime-proof` and independently mutate it red. |
| T-246.2-26 | Tampering | receipt schema/behavior set | high | mitigate | Require exact v5 keys and the new causal behavior before source hashes are accepted. |
| T-246.2-SC | Tampering | package installs | low | accept | No package-manager install occurs. |
</threat_model>

## Artifacts this phase produces

- Updated required behavior: `direct_budget_exhausted_before_refresh_replay`.
- Updated accepted receipt schema: `sigra.generated-app-login-runtime-proof/v5`.
- New semantic provenance invariants: derived exact `run_url`, exact `artifact_name`, and `implementation_ref == head_sha`.
- Three independent rejection mutations for `run_url`, `implementation_ref`, and `artifact_name`; no new production symbol, route, schema, migration, UI field, or package.

<verification>
Run all non-canonical evidence tests green, then require the canonical-only tag to stay red until Plan 07. Formatter and diff checks must pass.
</verification>

<success_criteria>
- No arbitrary provenance identity field can accompany otherwise valid historical hashes.
- Exact v5 synthetic evidence passes and current stale canonical evidence fails for the intended reason.
- All high-severity spoofing/tampering threats have deterministic negative tests.
</success_criteria>

<output>
Create `.planning/phases/246.2-close-gaps-app-04-and-app-05-generated-refresh-and-revocatio/246.2-06-SUMMARY.md` when done.
</output>
