---
phase: 237-clean-working-tree-green-pages-clean-lib-docs-surface
plan: 05
type: execute
wave: 3
depends_on:
  - 237-04
files_modified:
  - guides/introduction/upgrading-to-v1.10.md
  - guides/introduction/upgrading-to-v1.11.md
  - mix.exs
  - doc/llms.txt
autonomous: true
requirements:
  - SURF-02

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The three dead relative links to non-existent planning documents in the two upgrade guides are gone, and the surrounding claims each guide was making still stand (D-03)."
    - "The documentation-warning suppression list shrinks from 9 entries to 7, and the two removed entries are exactly the two whose only justification was the dead links fixed in this plan — a reduction earned by removing the underlying rot, not by hiding it."
    - "The comment above that list no longer claims the removed paths are intentionally relative for repository navigation. That claim is false — the targets do not exist — and a comment that misdescribes its own code is the defect class this milestone exists to remove."
    - "The remaining 7 entries are shown to still be load-bearing by a remove-and-retest that produces warnings, after which `mix.exs` is restored and proven byte-identical."
    - "The documentation build with warnings treated as errors still exits zero with the shorter list — the prune did not simply move a failure into CI."
    - "The already-satisfied half of the requirement is recorded as already satisfied, not re-done: the warnings-as-errors invocation is a gate in three workflows today and the build is already warning-free."
  artifacts:
    - guides/introduction/upgrading-to-v1.10.md
    - guides/introduction/upgrading-to-v1.11.md
    - mix.exs
  key_links:
    - "dead relative links in published guides → the warnings they raise → the two suppression entries that existed only to hide those warnings → removing the links is the only non-vacuous route to a shorter list"
    - "the suppression list length → the warnings-as-errors gate already running in three workflows → a prune that is wrong shows up there, not here"
  prohibitions:
    - statement: "MUST NOT remove any suppression entry whose warnings have not first been eliminated at the source."
      status: pending
      verification: automated
      reason: "All nine entries were measured load-bearing. Removing one without fixing its underlying reference turns the already-green documentation gate red in three workflows."
    - statement: "MUST NOT add a fourth documentation-warning gate to any workflow."
      status: pending
      verification: automated
      reason: "The gate clause of the requirement is already satisfied in three workflows. Adding a fourth is duplicate machinery presented as progress."
    - statement: "MUST NOT replace the dead links with absolute URLs to the same non-existent documents."
      status: pending
      verification: review
      reason: "The target files do not exist anywhere; an absolute URL would be a 404 instead of a build warning, which is worse — it removes the signal without removing the rot."
    - statement: "MUST NOT strip the prose sentence that mentions the planning directory as a coordination-label concept."
      status: pending
      verification: automated
      reason: "That sentence is a correct, load-bearing explanation of how milestone labels relate to published library versions. Only the dead LINKS are in scope."
---

<objective>
Retire the four dead documentation-link warnings at their source, then take the only non-vacuous
reduction available in the warning-suppression list and correct the false comment that sits above
it.

Purpose: SURF-02's remaining half. The warnings-as-errors gate is already wired in three workflows
and the build is already warning-free — that clause needs recording, not rebuilding. What is real
is that four of the warnings the suppression list hides are dead links to planning documents that
no longer exist, published in library documentation that adopters read. Fixing them lets two
entries go and turns a proof that the list is load-bearing into a list that is actually smaller.

Output: three corrected guide lines, a 7-entry suppression list, a corrected comment, and a
remove-and-retest showing the survivors still earn their place.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-CONTEXT.md
@.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-RESEARCH.md
@mix.exs
</context>

<planner_decisions_closed_here>
**The roadmap's SURF-02 clause "make the documentation build warning-free as a gate" is already
true and is NOT re-done here.** The warnings-as-errors invocation runs in the main CI workflow and
in both release workflows, and a measured build at the phase's base commit emitted zero warnings.
The plan records this as already-satisfied so the verifier does not grade the phase on machinery it
was right not to build.

**The roadmap's "prune the suppression list to what is still needed" is also already answered, and
the answer was "nothing is prunable".** An exhaustive remove-and-retest at research time produced
14 warnings covering all 9 entries: zero were dead. A naive prune would have been vacuous. The
reduction taken here is earned differently — by deleting the dead links that generated four of
those warnings (D-03), which is the only route to a genuinely shorter list.

**These three lines sit in `guides/`, while the requirement's wording scopes to `lib/`.** They are
fixed here anyway because they are the same defect class, they render in published library
documentation, and they are the enabling condition for the list reduction the same requirement
asks for. The scope extension is recorded rather than silent.

**Every scope fence in this plan is taken against the phase's MERGE-BASE with `origin/main`, and
bounded on BOTH sides.** A revisionless `git diff` reports unstaged changes only, so after a commit
it reports nothing and an upper-bound-only assertion passes vacuously at zero changed files. The
live branch tip is wrong in the other direction: this phase runs in parallel with Phase 236, so a
tip-based fence can fail this phase because a sibling merged. And the fence is scoped to the two
files THIS plan owns, not to the whole introduction-guides directory — plan 237-02 rewrites the
walkthrough guide in an earlier wave, so a directory-wide count of two would be a deterministic red
here. A separate assertion covers the directory: nothing may change in it beyond the three files
this phase owns.

**The dead links are removed, not redirected.** The three target documents do not exist anywhere —
not in the repository, not under an archive path. Rewriting them as absolute URLs would convert a
loud build warning into a silent 404 for a reader.
</planner_decisions_closed_here>

<tasks>

<task type="auto">
  <name>Task 1: Remove the three dead planning-document links from the two upgrade guides, keeping each sentence's claim</name>
  <files>guides/introduction/upgrading-to-v1.10.md, guides/introduction/upgrading-to-v1.11.md</files>
  <read_first>
    - `guides/introduction/upgrading-to-v1.10.md:1-12` — the two affected sentences at lines 5 and 9, and the correct prose sentence at line 3 that mentions the planning directory as a concept and must NOT be touched
    - `guides/introduction/upgrading-to-v1.11.md:1-10` — the affected sentence at line 7
    - `guides/introduction/upgrading-to-v1.12.md:1-10` — the house style for referring to planning material without a broken relative link
    - `.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-RESEARCH.md` §E.4 — the four warnings and the verbatim source lines
    - `.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-CONTEXT.md` D-03
  </read_first>
  <action>
Three sentences across two files carry Markdown links whose relative targets do not exist: an
adopter-scope document referenced from both guides, a triage document, and an archived roadmap.

For each, rewrite the sentence so the link is gone and the claim survives. Two shapes work and both
are acceptable — drop the pointer and keep the statement as prose, or restate what the reader
should know inline. Prefer the shortest rewrite that leaves the paragraph reading naturally. Do not
invent a replacement destination, and do not point at a different document as a stand-in.

Leave everything else in these files alone. In particular the sentence explaining that milestone
labels are coordination labels and not a second installable version axis is correct and
load-bearing — it mentions the planning directory as a CONCEPT, not as a link target, and it must
survive untouched. The distinction that matters is a dead link versus a correct prose mention; only
the former is in scope.

Do not touch any other guide. The wider set of planning-directory mentions across the guides
directory raises no warnings and is out of scope for this phase.

<!-- planner-discipline-allow: ](../../.planning/ -->
  </action>
  <verify>
    <automated>bash -c '! grep -Fq "](../../.planning/" guides/introduction/upgrading-to-v1.10.md guides/introduction/upgrading-to-v1.11.md'</automated>
    <fails_when>non-zero exit from the negation — at least one dead relative link to a planning document survives in the two upgrade guides</fails_when>
    <automated>bash -c 'set -eo pipefail; for f in guides/introduction/upgrading-to-v1.10.md guides/introduction/upgrading-to-v1.11.md; do grep -q "planning milestone" "$f"; done'</automated>
    <fails_when>non-zero exit — at least one of the two guides yields no milestone prose, meaning that file was gutted rather than edited and the absence result above is not evidence for it. Asserted per file on purpose: a single `grep -c f1 f2` pipeline passes when only ONE file is non-zero</fails_when>
    <automated>bash -c 'grep -q "second installable version axis" guides/introduction/upgrading-to-v1.10.md && grep -q "second installable version axis" guides/introduction/upgrading-to-v1.11.md'</automated>
    <fails_when>non-zero exit — the correct prose sentence explaining milestone labels versus published versions was removed along with the dead links</fails_when>
    <automated>bash -c 'set -eo pipefail; git fetch -q origin main; base=$(git merge-base origin/main HEAD); mine=$(git diff --name-only "$base" HEAD -- ":/guides/introduction/upgrading-to-v1.10.md" ":/guides/introduction/upgrading-to-v1.11.md"); test "$(printf "%s" "$mine" | grep -n . | wc -l | tr -d " ")" -eq 2; all=$(git diff --name-only "$base" HEAD -- ":/guides/introduction/"); extra=$(printf "%s\n" "$all" | grep -v "^$" | grep -vx "guides/introduction/upgrading-to-v1.10.md" | grep -vx "guides/introduction/upgrading-to-v1.11.md" | grep -vx "guides/introduction/code-walkthrough.md" || true); test -z "$extra"'</automated>
    <fails_when>non-zero exit — this plan did not change both named upgrade guides (a bound that only caps the count would pass at zero), or some OTHER file under the introduction guides changed that no plan in this phase owns. Scoped to the two named files rather than the whole directory: plan 237-02 legitimately rewrites `code-walkthrough.md` in an earlier wave, so a directory-wide count of two would be a deterministic red by the time this plan runs, and the obvious local remedy — raising the bound — is how a fence stops fencing</fails_when>
  </verify>
  <acceptance_criteria>
    - No dead relative link to a planning document remains in either upgrade guide.
    - Each of the three rewritten sentences still reads as a complete claim.
    - The milestone-labels-versus-versions sentence survives verbatim in both files.
    - No absolute URL was introduced pointing at any of the three non-existent documents.
    - Both named upgrade guides appear in this plan's diff against the phase merge-base, and no file under the introduction guides changed beyond the three this phase owns (the two upgrade guides plus the walkthrough that plan 237-02 rewrites).
  </acceptance_criteria>
  <done>The four dead-link warnings no longer have a source; the guides still say what they said.</done>
  <reversibility rating="reversible">Prose edits to two documentation files.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Drop the two now-unjustified suppression entries and correct the false comment above the list</name>
  <files>mix.exs</files>
  <read_first>
    - `mix.exs:188-206` — the documentation configuration function, the comment block immediately above the suppression list, and all nine entries with their two inline group comments
    - `.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-RESEARCH.md` §E.4 — the full 14-warning attribution table mapping each warning to the entry it justifies
    - `.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-CONTEXT.md` D-03
  </read_first>
  <action>
Remove exactly the two suppression entries for the upgrade guides Task 1 just fixed. Leave the
other seven entries — the five source modules and the two companion-library recipe pages — exactly
as they are, including their two inline group comments explaining that they suppress references to
hidden application helpers and to a behaviour callback. Those seven are measured load-bearing and
are not in scope for removal.

Then correct the comment block above the list. Its current claim — that maintainer paths under the
planning directory are intentionally relative from the guide for repository navigation — is false:
the three targets do not exist, which is why they warned. Replace it with a comment that accurately
describes what the remaining seven entries suppress: references that the documentation tool cannot
resolve because the targets are hidden or are behaviour callbacks rather than public functions.
Write the comment so that it describes the list as it now is, not as a history of what was removed.

Do not change any other part of the documentation configuration — not the main page, not the
source reference, not the extras list. Do not add a warnings-as-errors key: the enforcement is the
command-line flag already used by three workflows, and duplicating it in configuration would create
two places to be wrong.
  </action>
  <verify>
    <automated>bash -c 'test "$(sed -n "/skip_undefined_reference_warnings_on/,/\]/p" mix.exs | grep -n "^ *\"" | wc -l | tr -d " ")" -eq 7'</automated>
    <fails_when>the count is not exactly 7 — the suppression list holds a different number of entries than the earned reduction from nine to seven</fails_when>
    <automated>bash -c '! sed -n "/skip_undefined_reference_warnings_on/,/\]/p" mix.exs | grep -q "upgrading-to-v1.1[01].md"'</automated>
    <fails_when>non-zero exit from the negation — one or both upgrade-guide entries survive in the suppression list</fails_when>
    <automated>bash -c 'sed -n "/skip_undefined_reference_warnings_on/,/\]/p" mix.exs | grep -q "companion-libs"'</automated>
    <fails_when>non-zero exit — the positive control found no companion-library recipe entry, meaning entries beyond the two intended ones were removed</fails_when>
    <automated>bash -c '! grep -q "intentionally relative" mix.exs'</automated>
    <fails_when>non-zero exit from the negation — the false comment claiming the removed paths are intentionally relative is still present somewhere in the build file. Searched over the whole file rather than a fixed line window, because removing two list entries shifts every line number in the region this task edits</fails_when>
    <automated>bash -c 'grep -q "skip_undefined_reference_warnings_on" mix.exs'</automated>
    <fails_when>non-zero exit — the positive control found no suppression-list key at all, meaning the search machinery or the path is wrong and the absence result above is not evidence</fails_when>
    <automated>mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit — the project no longer compiles cleanly after the build-file edit</fails_when>
  </verify>
  <acceptance_criteria>
    - The suppression list has exactly seven entries and neither upgrade guide is among them.
    - The five source-module entries and the two companion-library recipe entries all survive, with their group comments.
    - The comment above the list describes the current seven entries and makes no false claim about relative paths.
    - No warnings-as-errors key was added to the documentation configuration.
    - The project compiles cleanly.
  </acceptance_criteria>
  <done>The suppression list is genuinely shorter because its underlying rot is gone, and its comment tells the truth about what it does.</done>
  <reversibility rating="reversible">Two list entries and a comment in the build file; restoring them restores the previous suppression behaviour exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Prove the surviving seven still earn their place, and that the build and tree are clean with the shorter list</name>
  <files>doc/llms.txt</files>
  <read_first>
    - `.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-RESEARCH.md` §E.4 — the method used at research time: copy the build file, empty the list, build, capture, restore, and verify the restore byte-for-byte with a checksum and a clean diff
  </read_first>
  <action>
Demonstrate the shorter list is not decorative, using the same method the research used and
restoring with the same rigour.

(1) Take a byte copy of the build file. Empty the suppression list to an empty list. Build the
documentation and capture the full output. Warnings must appear — that is the RED that proves the
seven survivors are doing work. Record how many warnings appeared and which entries they attribute
to, and check the four dead-link warnings from the guides are NOT among them: their absence is the
evidence that Task 1 actually fixed something.

(2) Restore the build file from the byte copy. Prove the restore, do not assume it: a clean diff on
that file AND a checksum comparison against the copy. The research established this discipline
precisely because a half-restored build file is a silent, wide-radius defect.

(3) With the list restored to seven, run the documentation build with warnings treated as errors —
the same invocation the three workflows run — and confirm it exits zero. That is the statement that
the prune did not simply relocate a failure into CI.

(4) Re-check the committed documentation index for drift and, if it moved, regenerate and commit it,
keeping the clean-tree property established earlier in the phase.

Record in the summary, in plain words, that the warnings-as-errors gate already existed in three
workflows before this phase and that no fourth gate was added.
  </action>
  <verify>
    <automated>bash -c 'git diff --exit-code -- mix.exs'</automated>
    <fails_when>non-zero exit — the build file is dirty relative to the last commit, meaning the temporary emptied-list edit from the remove-and-retest was not fully restored</fails_when>
    <automated>mix docs --warnings-as-errors</automated>
    <fails_when>non-zero exit — the documentation build emits a warning with the seven-entry list, so the two removed entries were still needed</fails_when>
    <automated>bash -c 'mix docs >/dev/null 2>&1 && git diff --exit-code -- doc/llms.txt'</automated>
    <fails_when>non-zero exit — the committed documentation index is dirtied by a build, regressing the clean-tree property</fails_when>
    <automated>bash -c 'test -z "$(git status --porcelain --untracked-files=all)"'</automated>
    <fails_when>non-zero exit — the working tree is not clean at this plan's end</fails_when>
    <automated>bash -c 'set -eo pipefail; git fetch -q origin main; base=$(git merge-base origin/main HEAD); out=$(git diff --name-only "$base" HEAD -- ":/.github/workflows/"); test -z "$out"'</automated>
    <fails_when>non-zero exit — a workflow file was modified, meaning a fourth documentation gate or other CI change was introduced against the prohibition</fails_when>
  </verify>
  <acceptance_criteria>
    - The emptied-list build produced warnings, and the count and attributions are recorded.
    - None of the four dead-link warnings appeared in that capture.
    - The build file is byte-identical to its committed state after the retest, proven by both a clean diff and a checksum match.
    - The documentation build with warnings-as-errors exits zero with seven entries.
    - The working tree and committed documentation index are clean.
    - No workflow file was modified.
  </acceptance_criteria>
  <done>The seven survivors are proven load-bearing, the four dead-link warnings are proven gone, and the build file is proven restored.</done>
  <reversibility rating="reversible">A temporary, checksum-verified edit-and-restore plus verification runs.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published library documentation → adopters | Broken links and stale claims in upgrade guides are read by people deciding whether to adopt. |
| warning-suppression configuration → the CI documentation gate | A wrong suppression list turns three workflows red, or hides a real problem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-237-05-01 | Spoofing (of green) | suppression-list prune | high | mitigate | Entries are removed only after their underlying warnings are eliminated at source; proven by a remove-and-retest and by the warnings-as-errors build exiting zero with the shorter list. |
| T-237-05-02 | Tampering | temporary edit to the build file | high | mitigate | Byte copy before, restore after, proven by both a clean diff and a checksum comparison. |
| T-237-05-03 | Repudiation | a comment that misdescribes its own configuration | medium | mitigate | The false relative-path claim is replaced with an accurate description, and an automated check asserts the false phrase is gone. |
| T-237-05-04 | Information Disclosure | dead internal links in published guides | low | mitigate | Links removed rather than redirected; a positive control proves the search ran. |
| T-237-05-05 | Tampering | package installs | low | accept | No registry packages are installed by this plan; no legitimacy gate applies. |
</threat_model>

<verification>
- No dead relative planning-document link remains in either upgrade guide, alongside a positive control.
- The suppression list has exactly seven entries; the two upgrade-guide entries are gone and the other seven survive.
- The false comment claim is gone.
- Emptying the list still produces warnings (RED), and the build file restores byte-identically.
- The warnings-as-errors build exits zero with seven entries; no workflow file changed.
- Working tree and committed documentation index are clean.
</verification>

<success_criteria>
SURF-02 is fully satisfied: the rendered documentation surface carries no dead planning-document
links in scope, the suppression list is smaller for an earned reason with its survivors proven
load-bearing, its comment tells the truth, and the already-existing warning gate was recorded rather
than duplicated.
</success_criteria>

<output>
Create `.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-05-SUMMARY.md` when done.
</output>
