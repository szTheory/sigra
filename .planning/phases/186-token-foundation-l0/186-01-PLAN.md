---
phase: 186-token-foundation-l0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-token-reference.md
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-03

must_haves:
  truths:
    - "Every --sg-* token in sigra_admin.css :root has a rationale + brand reference entry in admin-token-reference.md"
    - "Motion tokens (5 durations + 4 easings + 3 composed) are documented with emilkowal.ski validation as rationale"
    - "The L0 ledger row in admin-quality-ledger.md has tier integer 1 (bare, no decorators)"
    - "The monotonic guard reads token-layer:1 from the ledger without skipping the row"
  artifacts:
    - path: "guides/reference/admin-token-reference.md"
      provides: "Per-token rationale + brand-ref table for entire :root token layer"
      contains: "--sg-color-brand"
    - path: "guides/reference/admin-quality-ledger.md"
      provides: "Machine-parseable L0 tier row"
      contains: "token-layer"
  key_links:
    - from: "guides/reference/admin-token-reference.md"
      to: "brandbook/tokens.json"
      via: "Brand Ref column JSON paths"
      pattern: "semantic\\.light\\.color"
    - from: "guides/reference/admin-quality-ledger.md"
      to: "guides/reference/admin-token-reference.md"
      via: "Evidence link in L0 row"
      pattern: "\\[admin-token-reference"
---

<objective>
Author the new per-token rationale document (`guides/reference/admin-token-reference.md`) and add the L0 row to the quality ledger (`guides/reference/admin-quality-ledger.md`), making the implicit `:root` token layer explicit, ratified, and machine-tracked.

Purpose: Phase 186 is the ONLY phase permitted to change token values. Ratifying here means phases 187–192 build on a documented, frozen foundation. Every token must carry a written rationale and brand reference so future auditors know why each value exists, not just what it is.

Output: `guides/reference/admin-token-reference.md` (new file, all :root token categories); `admin-quality-ledger.md` updated with L0 row, tier 1, evidence link to the new doc.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-token-foundation-l0/186-CONTEXT.md
@.planning/phases/186-token-foundation-l0/186-RESEARCH.md
@.planning/phases/186-token-foundation-l0/186-PATTERNS.md

<interfaces>
<!-- Key files the executor MUST read before writing anything. -->

Read these in full before authoring the token reference:
- priv/templates/sigra.install/admin/sigra_admin.css (lines 20–165 for :root tokens; lines 117–137 for motion; lines 167–204 for dark @media block)
- brandbook/tokens.json (semantic token paths; dark values lines 69–98; motion subset lines 155–160)
- guides/reference/admin-quality-ledger.md (current state; L0 row goes ABOVE the L1 rows, before the first "stat" row)
- guides/reference/admin-fractal-scorecard.md (heading/table conventions to follow)

Existing ledger row format (exact pattern to match for L0 row):
```
| token-layer | L0 | 1 | [admin-token-reference.md](admin-token-reference.md) |
```
Tier column (col 4) MUST be the bare integer `1`. The monotonic guard awk uses `tier ~ /^[012]$/` — any decoration silently skips the row.

Token categories to cover in admin-token-reference.md (from RESEARCH.md token survey):
- Color neutrals: --sg-color-ink, --sg-color-muted, --sg-color-subtle, --sg-color-panel, --sg-color-panel-alt, --sg-color-line, --sg-color-line-strong
- Color brand: --sg-color-brand, --sg-color-brand-strong, --sg-color-brand-solid, --sg-color-brand-fill-hover, --sg-color-brand-fill-active, --sg-color-brand-soft, --sg-color-on-brand, --sg-color-on-brand-solid, plus logo tokens
- Color semantic status: --sg-color-risk, --sg-color-risk-soft, --sg-color-warn, --sg-color-warn-soft, --sg-color-ok, --sg-color-ok-soft, --sg-color-info, --sg-color-info-soft
- Spacing: --sg-space-1 through --sg-space-12 (4px base grid)
- Type scale: sizes + weights + leading + tracking
- Radii: --sg-radius-xs through --sg-radius-full
- Control heights: --sg-control-xs through --sg-control-lg (no tokens.json equivalent; document as "admin-layer decision")
- Elevation/shadow: --sg-elev-inset, --sg-elev-1, --sg-elev-2, --sg-elev-3
- Motion: 5 durations + 4 easings + 3 composed transitions (emilkowal.ski validation as rationale per D-08)
- Focus ring: --sg-focus-ring, --sg-focus-ring-offset
- Z-index: --sg-z-base through --sg-z-toast (no tokens.json equivalent; admin-layer decision)
- Layout: --sg-container-max, --sg-breakpoint-lg, --sg-measure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/reference/admin-token-reference.md</name>
  <files>guides/reference/admin-token-reference.md</files>
  <read_first>
    - priv/templates/sigra.install/admin/sigra_admin.css (full :root block lines 20–165, motion lines 117–137, dark @media lines 167–204)
    - brandbook/tokens.json (full file — semantic paths, dark values, motion subset)
    - guides/reference/admin-fractal-scorecard.md (H1/H2 heading conventions, table column style, cross-reference footer pattern)
    - guides/reference/admin-quality-ledger.md (document structure model)
    - .planning/phases/186-token-foundation-l0/186-PATTERNS.md (table column spec, footer pattern, per-category list)
  </read_first>
  <action>
    Create guides/reference/admin-token-reference.md as a new standalone reference document following the guides/reference/*.md sibling convention (admin-quality-ledger.md and admin-fractal-scorecard.md are the style models).

    Document structure: H1 title ("Admin Token Reference") + one-sentence role statement. Then one H2 section per token category, each containing a four-column pipe table: Token | Value | Rationale | Brand Ref.

    Table conventions (per D-01 and PATTERNS.md):
    - Token column: backtick-wrapped CSS custom property name (e.g. `--sg-color-brand`)
    - Value column: backtick-wrapped hex or CSS value
    - Rationale column: one sentence stating design intent; motion tokens cite emilkowal.ski validation explicitly (per D-08)
    - Brand Ref column: JSON dot-path into brandbook/tokens.json (e.g. `semantic.light.color.accent`), or `admin-layer decision` for tokens with no tokens.json equivalent (control heights, z-index ladder, layout tokens, component sizing)

    Categories and Brand Ref sourcing:
    - Color (neutrals, brand, status): trace each token value to its matching entry in brandbook/tokens.json using the semantic.light.color.* and raw.color.ember-* paths
    - Dark override tokens: note the dark value and cite `semantic.dark.color.*` path or `raw.color.ember-*`
    - Spacing: tokens.json space.* entries
    - Type: tokens.json typography.* entries
    - Radius: tokens.json radius.* entries
    - Control heights: no tokens.json equivalent → "admin-layer decision"
    - Elevation/shadow: partial match in tokens.json shadow.*
    - Motion: 5 durations (press=120ms, pop=180ms, fast=140ms, medium=220ms, slow=300ms) + 4 easings + 3 composed transitions. Rationale for each duration MUST cite emilkowal.ski validation: press ALIGNED (100–160ms range), pop ALIGNED (180ms = his cited dropdown sweet-spot), fast ALIGNED (100–200ms), medium ALIGNED (150–250ms dropdown range), slow ALIGNED/MARGINAL (at his "under 300ms" ceiling; acceptable for full modal overlays; exit-asymmetry refinement deferred to Phase 187 per D-09). Easings: --sg-ease-out is for enters (per Emil: ease-out for enters), --sg-ease-in is for exits only (Emil cautions against ease-in for UI enters; acceptable exit easing). --sg-ease-spring is pointer-gated micro-delight. Motion budget ratification verdict: ALIGNED, Tier 1 Ratified.
    - Focus ring: tokens.json semantic.light.color.focus path
    - Z-index: no tokens.json equivalent → "admin-layer decision" for each rung (base=0, overlay=10, modal=20, drawer=30, toast=40)
    - Layout: no tokens.json equivalent → "admin-layer decision"

    Dark color note: add a subsection under the Color section noting that the dark override block (sigra_admin.css @media lines 167–204) lightens brand-strong to `#fdba74` for WCAG AA compliance (v1.34 remediation); cite admin-design-contract.md ~line 207 as the dark AA resolution record.

    Document footer: cross-reference links matching the fractal-scorecard footer convention:
    Cross-reference: `admin-design-contract.md` (dark AA resolution note ~line 207), `brandbook/tokens.json` (brand source of truth), `guides/reference/admin-quality-ledger.md` (L0 row).

    Do NOT add verbose inline CSS prose to sigra_admin.css (per D-02). This document IS the rationale home.
  </action>
  <verify>
    <automated>grep -c '\-\-sg-color-brand' guides/reference/admin-token-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at guides/reference/admin-token-reference.md
    - Contains a row for `--sg-color-brand` with value `#c2410c` and brand ref `semantic.light.color.accent` (or equivalent tokens.json path)
    - Contains a motion section with all 5 duration tokens and emilkowal.ski validation cited
    - Contains --sg-color-brand-strong with dark value noted as `#fdba74` with AA remediation rationale
    - Motion budget ratification verdict "ALIGNED, Tier 1 Ratified" appears in the motion section
    - Cross-reference footer present linking to admin-design-contract.md and brandbook/tokens.json
    - Document uses H2 sections per category (not one giant flat table)
  </acceptance_criteria>
  <done>admin-token-reference.md exists with every --sg-* :root token category covered, each row having a one-sentence rationale + accurate brand-ref JSON path; motion section cites emilkowal.ski validation per D-08.</done>
</task>

<task type="auto">
  <name>Task 2: Add L0 row to guides/reference/admin-quality-ledger.md</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (full file — current row structure, column ordering, table header)
    - scripts/ci/quality-ledger-monotonic.sh (verify the awk tier-parsing rule: tier ~ /^[012]$/)
    - .planning/phases/186-token-foundation-l0/186-PATTERNS.md (L0 row format, verified awk parsing rules)
  </read_first>
  <action>
    Insert the L0 row into the Quality Ledger table in guides/reference/admin-quality-ledger.md, ABOVE the existing L1 rows (before the first `| stat |` row). The ledger is ordered from lowest fractal level to highest, so L0 belongs at the top of the data rows.

    Exact row to insert:
    | token-layer | L0 | 1 | [admin-token-reference.md](admin-token-reference.md) |

    Column rules (CRITICAL — monotonic guard awk reads column 4):
    - Column 2 (Item): `token-layer` — must start with a lowercase letter
    - Column 3 (Level): `L0`
    - Column 4 (Tier): bare integer `1` — NO text, NO asterisks, NO parenthetical. The awk uses `tier ~ /^[012]$/` which fails on any decoration.
    - Column 5 (Evidence): markdown link to the new admin-token-reference.md sibling file

    Evidence link path: since admin-token-reference.md is a sibling in the same directory (guides/reference/), the link is `[admin-token-reference.md](admin-token-reference.md)` — relative path, no subdirectory prefix.

    After inserting, run the validation command to confirm the guard reads the new row correctly.
  </action>
  <verify>
    <automated>grep -v '^#' guides/reference/admin-quality-ledger.md | grep -E '^\| [a-z]' | awk -F'|' '{tier=gensub(/^ +| +$/, "", "g", $4); if (tier ~ /^[012]$/) print $2 ":" tier}' | grep 'token-layer'</automated>
  </verify>
  <acceptance_criteria>
    - admin-quality-ledger.md contains a row starting with `| token-layer | L0 |`
    - The tier cell (column 4) contains exactly `1` with no surrounding text or decorators
    - Running `grep -v '^#' guides/reference/admin-quality-ledger.md | grep -E '^\| [a-z]' | awk -F'|' '{tier=gensub(/^ +| +$/, "", "g", $4); if (tier ~ /^[012]$/) print $2 ":" tier}'` outputs a line containing `token-layer:1`
    - The L0 row appears above the first L1 row (before `| stat |`)
    - Evidence column contains a link to admin-token-reference.md
  </acceptance_criteria>
  <done>admin-quality-ledger.md has a new L0 row with tier integer 1 and evidence link; the monotonic guard awk reads it as token-layer:1 without skipping the row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates one documentation file and edits another. No user input, no auth flows, no server-side logic, no CSS value changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; documentation-only work |

**Security domain assessment:** This plan is documentation-only (markdown file creation + markdown file edit). No auth flows, no user data, no session management, no cryptography, no user input surfaces are touched. All ASVS categories are N/A. The honest finding is: no security-relevant surface.
</threat_model>

<verification>
After both tasks complete:

1. Confirm admin-token-reference.md exists and covers all token categories:
   `grep -c '^\| \`--sg-' guides/reference/admin-token-reference.md`
   (should be a substantial count of token rows)

2. Confirm the ledger L0 row parses correctly:
   `grep -v '^#' guides/reference/admin-quality-ledger.md | grep -E '^\| [a-z]' | awk -F'|' '{tier=gensub(/^ +| +$/, "", "g", $4); if (tier ~ /^[012]$/) print $2 ":" tier}' | grep 'token-layer'`
   Must output `token-layer:1` (with possible leading/trailing whitespace around the item name — that's fine).

3. Confirm the monotonic guard does not error on the ledger:
   `bash scripts/ci/quality-ledger-monotonic.sh`
   Should pass (or report no regressions; the L0 row is new so it has no prior tier to compare against).
</verification>

<success_criteria>
- admin-token-reference.md covers every --sg-* :root category from sigra_admin.css with token · value · rationale · brand-ref per row
- Motion section carries emilkowal.ski validation rationale and "ALIGNED, Tier 1 Ratified" verdict
- admin-quality-ledger.md L0 row reads as `token-layer:1` via the monotonic guard awk
- No sigra_admin.css modifications (per D-02 and D-04 — documentation-only, no CSS prose added)
</success_criteria>

<output>
Create `.planning/phases/186-token-foundation-l0/186-01-SUMMARY.md` when done.
</output>
