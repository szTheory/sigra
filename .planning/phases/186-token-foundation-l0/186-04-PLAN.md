---
phase: 186-token-foundation-l0
plan: 04
type: execute
wave: 2
depends_on:
  - 186-01
  - 186-02
  - 186-03
files_modified:
  - test/example/priv/playwright/snapshot-allowlist
  - test/example/priv/playwright/snapshot-allowlist-design
autonomous: false
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-04
  - THEME-01

must_haves:
  truths:
    - "mix test exits 0 (all ExUnit + D-11 parity assertions green)"
    - "Playwright axe lane exits 0 violations in all three admin-design projects (chromium, mobile, dark)"
    - "The monotonic guard reads token-layer:1 from the ledger and exits 0"
    - "Both snapshot allowlists are empty if no token values changed (steady-state)"
    - "If a value change was made, the affected board slugs are declared in BOTH allowlists and both canary boards are green"
    - "The snapshot-canary-guard.sh exits 0"
  artifacts:
    - path: "test/example/priv/playwright/snapshot-allowlist"
      provides: "Steady-state empty (or slug-declared if token change occurred)"
    - path: "test/example/priv/playwright/snapshot-allowlist-design"
      provides: "Steady-state empty (or slug-declared if token change occurred)"
  key_links:
    - from: "Playwright admin-design-dark project"
      to: "sigra_admin.css @media dark block"
      via: "axe wcag2a+wcag2aa on rendered components"
      pattern: "admin-design-dark"
---

<objective>
Run the full verification suite to ratify the token layer as Tier 1 and handle the conditional value-change path: if the adversarial axe audit surfaces a genuine AA failure (expected finding: "already compliant"), apply the four-surface update procedure and declare board slugs in both allowlists. If no failure is found, close Phase 186 with both allowlists at empty steady-state.

Purpose: This is the gate that makes the token-layer ratification real — the audit instruments (axe, contrastRatio(), D-11 parity, monotonic guard) are run together, their combined output is the evidence for Tier 1, and any token-value delta that does get made is properly ring-fenced before Phase 187 consumes the token layer.

Output: All CI gates green; both allowlists at steady-state empty (or with declared slugs if a value change was necessary); phase summary committed.

PATH SELECTION (automated, no human gate on PATH A):
- PATH A (expected): axe 0 violations AND all contrastRatio() >= 4.5 AND mix test exit 0 → token layer is already compliant; phase closes automatically; both allowlists stay empty; skip Task 2 entirely.
- PATH B (genuine defect found): any axe violation OR any contrastRatio() < 4.5 on an --sg-* token pair → Task 2 checkpoint fires for editorial approval of the proposed replacement hex value before the four-surface change proceeds.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-token-foundation-l0/186-CONTEXT.md
@.planning/phases/186-token-foundation-l0/186-RESEARCH.md
@.planning/phases/186-token-foundation-l0/186-VALIDATION.md
@.planning/phases/186-token-foundation-l0/186-01-SUMMARY.md
@.planning/phases/186-token-foundation-l0/186-02-SUMMARY.md
@.planning/phases/186-token-foundation-l0/186-03-SUMMARY.md

<interfaces>
<!-- Verification commands and conditional procedure the executor needs -->

Full ExUnit suite command:
  mix test

Playwright axe lane (3 projects, admin-design boards):
  cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts tests/admin-theme.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark

Monotonic guard:
  bash scripts/ci/quality-ledger-monotonic.sh

Snapshot canary guard (requires a running example server):
  bash scripts/ci/snapshot-canary-guard.sh

Allowlist files (should be empty at steady-state):
  test/example/priv/playwright/snapshot-allowlist
  test/example/priv/playwright/snapshot-allowlist-design

Value-change blast radius (D-05, D-12 — only if a genuine AA failure is found):
  Files to update TOGETHER:
    1. priv/templates/sigra.install/admin/sigra_admin.css (change in :root + @media dark block)
    2. test/example/priv/static/assets/sigra_admin.css (byte-identical copy of installer template)
    3. test/example/priv/static/assets/css/app.css (update .sg-admin-shell[data-theme="light"/"dark"] blocks)
    4. priv/templates/sigra.install/core/sigra_auth.css (update auth ember-family if affected)
    5. brandbook/tokens.json (update corresponding semantic value)
  Then declare affected board slugs in BOTH allowlists.
  Then run: mix test + Playwright lane to verify all assertions pass with new values.
  Then update the value cell in admin-token-reference.md with the new value and a note.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full ExUnit suite + Playwright axe lane adversarial audit; select PATH A or PATH B</name>
  <files></files>
  <read_first>
    - .planning/phases/186-token-foundation-l0/186-VALIDATION.md (per-task verification map; phase gate criteria)
    - .planning/phases/186-token-foundation-l0/186-RESEARCH.md (Validation Architecture; blast-radius procedure D-05/D-12 if a genuine failure is found)
    - test/example/priv/playwright/snapshot-allowlist (current state — should be empty)
    - test/example/priv/playwright/snapshot-allowlist-design (current state — should be empty)
  </read_first>
  <action>
    Run the full verification suite in order:

    Step 1: Run mix test to confirm all ExUnit tests pass, including the D-11 parity assertion from Plan 02 and the existing DIST-05 test:
      mix test
    If mix test fails on the D-11 describe block, diagnose and fix the extraction logic before proceeding. The most likely failure modes are: (a) line-range offset for the dark blocks shifted (verify line numbers against current file state), or (b) whitespace normalization producing an empty list (check that --sg- is present in the extracted slice).

    Step 2: Boot the example app on port 4000 (or the SIGRA_EXAMPLE_URL-configured port):
      cd test/example && mix phx.server
    (Run in background; verify the base URL is reachable before Playwright.)

    Step 3: Run the Playwright axe lane against all three admin-design projects:
      cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts tests/admin-theme.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark

    Adversarial interpretation: if axe reports violations, treat each as a genuine finding (not "probably a false positive"). For each violation:
    - Check the token value involved (which CSS property, which element)
    - Determine if the violation is on a --sg-* token pair (not a gallery-only artifact)

    Step 4: Run the new contrastRatio() tone-soft assertions from Plan 03:
      cd test/example/priv/playwright && npx playwright test tests/admin-theme.spec.ts --project=chromium --grep "tone notice"
    If any tone pair fails (< 4.5:1), note the exact pair, current computed values, and whether a token change is needed.

    Step 5: Run the monotonic guard:
      bash scripts/ci/quality-ledger-monotonic.sh
    Must exit 0 (L0 token-layer row is new, has no prior tier to compare against; no existing row should have decreased).

    PATH SELECTION — evaluate atomically after Steps 1–5:

    PATH A criteria (all must be true):
      - mix test (Step 1) exits 0
      - axe reports 0 violations across all 3 admin-design projects (Step 3)
      - All contrastRatio() tone-soft assertions pass >= 4.5:1 (Step 4)
      - bash scripts/ci/quality-ledger-monotonic.sh exits 0 (Step 5)

    If ALL PATH A criteria are met:
      → PATH A selected automatically. Record finding as "already compliant — all gates green."
      → Skip Task 2 (the checkpoint:human-verify does NOT fire).
      → Proceed directly to Task 3 (steady-state close).

    PATH B criteria (any of the following):
      - Any axe violation on an --sg-* token pair (Step 3)
      - Any contrastRatio() assertion < 4.5:1 (Step 4)
      - mix test non-zero exit (Step 1, after diagnosing and confirming it is token-related, not a pre-existing unrelated failure)

    If any PATH B criterion is met:
      → PATH B selected. Record the specific failing pair(s): token name, current value, computed contrast ratio.
      → Propose a replacement hex value for the failing pair(s) that achieves >= 4.5:1 in both light and dark.
      → Pause at Task 2 for editorial approval of the proposed value before applying the four-surface change.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <acceptance_criteria>
    - mix test exits 0 (all ExUnit tests pass including D-11)
    - Playwright axe lane reports 0 violations across all 3 admin-design projects
    - contrastRatio() tone-soft assertions pass (>= 4.5:1 for all pairs)
    - bash scripts/ci/quality-ledger-monotonic.sh exits 0
    - PATH selected explicitly: either "PATH A — all gates green, proceeding to Task 3" or "PATH B — [specific pair] fails at [value], proposed replacement [hex], pausing at Task 2"
    - If PATH A: Task 2 is skipped; proceed to Task 3
    - If PATH B: Task 2 fires before any token values are changed
  </acceptance_criteria>
  <done>Full audit run complete; PATH selected by automated criteria; either proceeding directly to Task 3 (PATH A) or pausing at Task 2 for value approval (PATH B).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PATH B ONLY — this checkpoint fires only when Task 1's automated gates surface a genuine AA failure on an --sg-* token pair. If Task 1 selected PATH A (all gates green), skip this task entirely and proceed to Task 3.

    Task 1 found a genuine AA violation: a specific --sg-* token pair fails axe or produces a contrastRatio() < 4.5. Claude has identified the failing pair, its current value, and a proposed replacement hex value that achieves >= 4.5:1 in both light and dark contexts. The four-surface change (D-05: sigra_admin.css installer + example copy + app.css explicit-toggle + sigra_auth.css ember-family + brandbook/tokens.json) has NOT been applied yet — this checkpoint gates it.

    You are approving an editorial/aesthetic judgment: the proposed replacement hex value. Claude's criteria are AA compliance (>= 4.5:1) and minimal deviation from the existing brand palette.
  </what-built>
  <how-to-verify>
    1. Review Task 1 output for the specific failing pair(s): token name, current hex, computed contrast ratio.
    2. Review Claude's proposed replacement hex value and its computed contrast ratio (Claude will provide both actual and target ratios).
    3. Verify the proposed value passes AA using your preferred contrast checker if desired.
    4. Optionally propose a different value if the replacement deviates too far from brand intent.
  </how-to-verify>
  <resume-signal>
    PATH B approval: type "approved — change [token-name] to [proposed-value]" OR "reject — use [different-value] instead"
    (If you are seeing this checkpoint but Task 1 selected PATH A, type "skip — PATH A already selected" to continue to Task 3.)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close phase — declare allowlist state and confirm snapshot canary</name>
  <files>
    test/example/priv/playwright/snapshot-allowlist
    test/example/priv/playwright/snapshot-allowlist-design
  </files>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist (current state)
    - test/example/priv/playwright/snapshot-allowlist-design (current state)
    - .planning/phases/186-token-foundation-l0/186-RESEARCH.md (Blast Radius section — if PATH B was taken, all four surfaces were updated in Task 2)
  </read_first>
  <action>
    This task handles the phase close for both PATH A and PATH B outcomes.

    PATH A — no token changes (expected):
    Both allowlists are already empty. Confirm by reading both files. Run the snapshot canary guard to confirm the canary board is still green:
      bash scripts/ci/snapshot-canary-guard.sh
    If the canary guard passes, no file changes are needed. Both allowlists stay empty (steady-state). The phase is clean.

    PATH B — token value change was applied:
    The four parity surfaces were updated in Task 2 (triggered by the checkpoint:human-verify approval). Now:
    1. Run mix test to confirm DIST-05 still passes (installer ↔ example byte-parity still holds after the value change).
    2. Run the Playwright axe lane again to confirm 0 violations with the new value.
    3. Identify which gallery/checkpoint board PNGs will visually change (the boards that render the affected token pair).
    4. Declare the affected board slugs in BOTH allowlists:
       - test/example/priv/playwright/snapshot-allowlist: add the admin-checkpoints slug (e.g. global-overview, org-overview) one per line
       - test/example/priv/playwright/snapshot-allowlist-design: add the admin-design board slug (e.g. board-notice, board-summary-chip) one per line
       Note: snapshot-allowlist-design uses hyphenated slug forms derived from PNG filename stems (board-notice not board_notice).
    5. Re-record the affected Playwright boards using the recapture gate or --update-snapshots on the affected slug.
    6. Run snapshot-canary-guard.sh to confirm the canary is still green.
    7. Update the admin-token-reference.md value cell with the new value and a note "(updated Phase 186 — AA remediation)".

    In both paths, confirm the final state: mix test green + axe 0 violations + both allowlists at their correct state + canary green + monotonic guard green.
  </action>
  <verify>
    <automated>mix test && bash scripts/ci/quality-ledger-monotonic.sh</automated>
  </verify>
  <acceptance_criteria>
    PATH A:
    - Both allowlists are empty (zero non-comment lines)
    - bash scripts/ci/snapshot-canary-guard.sh exits 0
    - mix test exits 0
    - bash scripts/ci/quality-ledger-monotonic.sh exits 0

    PATH B (additionally):
    - BOTH allowlists contain the affected board slug(s) from the value-change
    - mix test exits 0 (DIST-05 still passes — all four parity surfaces were updated together)
    - Playwright axe 0 violations with the new value
    - admin-token-reference.md updated with new value and change note
    - canary board is green (not in allowlist)
  </acceptance_criteria>
  <done>Phase 186 token layer ratified: mix test green, axe 0 violations, monotonic guard green, both allowlists at correct steady-state, canary green. Token foundation is frozen and documented for Phase 187.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → example app (dev server) | Playwright navigates to the example app running locally. The /admin/_design gallery is dev-only (example-only, never templated to installer per INFRA-01 contract guard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; verification-only run + conditional CSS token value edit |

**Security domain assessment:** This plan runs verification tooling (mix test, Playwright, bash scripts) and conditionally edits CSS token values and allowlist text files. No auth flows, user data, session management, cryptographic operations, or user input surfaces. All ASVS categories are N/A. Security-relevant surface: none.
</threat_model>

<verification>
Terminal state for Phase 186:

1. mix test — exits 0 (includes D-11 dark-block parity, brand-strong assertion, auth ember cross-check)
2. npx playwright test tests/admin-design.spec.ts tests/admin-theme.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark — exits 0, 0 axe violations
3. bash scripts/ci/quality-ledger-monotonic.sh — exits 0 (token-layer:1 row present, no tier decreased)
4. bash scripts/ci/snapshot-canary-guard.sh — exits 0 (canary green)
5. Both allowlists: empty (PATH A) or with correctly declared slugs (PATH B)
</verification>

<success_criteria>
- All five phase success criteria from ROADMAP.md are met:
  1. Every token has rationale + brand ref (admin-token-reference.md exists with all categories)
  2. All color pairs pass AA light+dark (axe 0 violations; contrastRatio tone-soft >= 4.5)
  3. Motion tokens ratified (emilkowal.ski rationale in admin-token-reference.md; motion section Tier 1 Ratified)
  4. Auth coherence preserved (D-11 parity test green; any token delta declared in both allowlists)
  5. Tokens render correctly in Light/Dark/System (axe all-3-projects; contrastRatio dark brand-strong #fdba74 verified)
- Phase 186 is the last phase permitted to change token values; if PATH A was taken, the token layer is frozen; if PATH B was taken, the changes are fully declared and guarded.
</success_criteria>

<output>
Create `.planning/phases/186-token-foundation-l0/186-04-SUMMARY.md` when done.
</output>
