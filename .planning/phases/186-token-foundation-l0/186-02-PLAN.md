---
phase: 186-token-foundation-l0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/install/features/admin_test.exs
autonomous: true
requirements:
  - TOKEN-04
  - THEME-01

must_haves:
  truths:
    - "mix test exits 0 after adding the D-11 parity describe block"
    - "The D-11 test extracts and sorts --sg-* declarations from both dark blocks and asserts they are equal"
    - "The test asserts dark brand-strong value is #fdba74 (not the light #9a3412)"
    - "The test covers auth ember-family parity (risk/warn/ok/info values in both light and dark)"
    - "The D-11 describe block follows the DIST-05 pattern (File.read!, assert with descriptive message)"
  artifacts:
    - path: "test/sigra/install/features/admin_test.exs"
      provides: "D-11 dark-block parity assertion + auth ember cross-check"
      contains: "D-11 System↔explicit-toggle dark-block parity"
  key_links:
    - from: "test/sigra/install/features/admin_test.exs"
      to: "priv/templates/sigra.install/admin/sigra_admin.css"
      via: "File.read! in D-11 describe block"
      pattern: "File\\.read!.*sigra_admin\\.css"
    - from: "test/sigra/install/features/admin_test.exs"
      to: "test/example/priv/static/assets/css/app.css"
      via: "File.read! in D-11 describe block"
      pattern: "File\\.read!.*app\\.css"
---

<objective>
Add the D-11 System↔explicit-toggle dark-block parity assertion to `test/sigra/install/features/admin_test.exs`, closing the highest-value structural gap in Phase 186: a silent desync between the System dark path and the explicit-toggle dark path would currently pass all CI gates undetected.

Purpose: The 27 `--sg-*` dark token values are currently identical between `sigra_admin.css` `@media (prefers-color-scheme: dark)` (lines 167–204) and `app.css` `html[data-sg-admin-theme="dark"] .sg-admin-shell` (lines 1512–1543), but nothing enforces this. The D-11 test closes the gap. It also includes a lightweight auth ember-family cross-check (risk/warn/ok/info) and a direct assertion that dark brand-strong is `#fdba74`.

Decisions covered (CONTEXT.md): D-10 (closes the unguarded four-surface parity gap — System dark path vs explicit-toggle dark path, plus tokens.json↔admin↔auth drift), D-11 (the lightweight automated parity assertion itself).

Output: `test/sigra/install/features/admin_test.exs` modified with a new `describe "D-11 ..."` block appended after the DIST-05 block; `mix test test/sigra/install/features/admin_test.exs` exits 0.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-token-foundation-l0/186-CONTEXT.md
@.planning/phases/186-token-foundation-l0/186-RESEARCH.md
@.planning/phases/186-token-foundation-l0/186-PATTERNS.md

<interfaces>
<!-- Exact analog the executor MUST read before modifying admin_test.exs -->

Read test/sigra/install/features/admin_test.exs fully before editing — the D-11 block goes AFTER line 328 (the closing `end` of the DIST-05 describe block) and BEFORE the closing `end` of the module (line 350 currently).

DIST-05 block (exact pattern to model D-11 on — lines 317–328):
```elixir
describe "DIST-05 example≡template byte-parity (sigra_admin.css)" do
  test "example copy is byte-identical to the installer template" do
    template = File.read!("priv/templates/sigra.install/admin/sigra_admin.css")
    example = File.read!("test/example/priv/static/assets/sigra_admin.css")

    assert byte_size(template) == byte_size(example),
           "size mismatch — resync with: cp priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css"

    assert template == example,
           "content mismatch — example copy has diverged from the installer template; ..."
  end
end
```

Existing private helpers available in the module (lines 330–349 — these are module-level helpers; the D-11 tests can use them or define their own private helpers inside the describe block):
```elixir
defp source_fragment(source, needle, len) do
  case :binary.match(source, needle) do
    {start, _} -> binary_part(source, start, min(len, byte_size(source) - start))
    :nomatch -> ""
  end
end

defp source_offset(source, needle) do
  case :binary.match(source, needle) do
    {offset, _} -> offset
    :nomatch -> nil
  end
end
```

Dark block anchors (VERIFIED in 186-RESEARCH.md):
- sigra_admin.css anchor: "@media (prefers-color-scheme: dark)" at line 167; 27 --sg-* properties inside, 4-space indent
- app.css anchor: "html[data-sg-admin-theme=\"dark\"] .sg-admin-shell" at line 1513; 27 --sg-* properties inside, 2-space indent

Anti-pattern: DO NOT use byte-equality on the block content — the selectors and indentation differ. Extract and compare sorted --sg-* declarations (value-equivalence, not byte-equivalence).

Auth ember values (VERIFIED in 186-RESEARCH.md — ember parity table):
- Light: --sg-color-risk=#b42318 ↔ --sigra-auth-risk=#b42318; --sg-color-warn=#a15c00 ↔ --sigra-auth-warn=#a15c00; --sg-color-ok=#176b43 ↔ --sigra-auth-ok=#176b43; --sg-color-info=#1d4ed8 ↔ --sigra-auth-info (verify exact token name in sigra_auth.css)
- Dark: --sg-color-risk=#f8a39c ↔ --sigra-auth-risk dark; --sg-color-warn=#f5c451 ↔ --sigra-auth-warn dark; --sg-color-ok=#5dd1a0 ↔ --sigra-auth-ok dark
- Near-match (known intentional difference): --sg-color-panel=#1f1d1a vs --sigra-auth-surface=#211f1c — do NOT assert these; document the near-match instead
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D-11 dark-block parity describe block to admin_test.exs</name>
  <files>test/sigra/install/features/admin_test.exs</files>
  <read_first>
    - test/sigra/install/features/admin_test.exs (full file — module structure, DIST-05 block lines 317–328, existing private helpers lines 330–349, module closing end)
    - priv/templates/sigra.install/admin/sigra_admin.css (dark @media block lines 167–204 — note exact anchor string and the 27 --sg-* declarations to understand the expected sorted set)
    - test/example/priv/static/assets/css/app.css (explicit-toggle dark block lines 1512–1543 — note the anchor string "html[data-sg-admin-theme=\"dark\"] .sg-admin-shell" and 2-space indented --sg-* declarations)
    - priv/templates/sigra.install/core/sigra_auth.css (auth ember-family values lines 1–103 — confirm exact --sigra-auth-risk, --sigra-auth-warn, --sigra-auth-ok, --sigra-auth-info token names and values in light + dark blocks)
    - .planning/phases/186-token-foundation-l0/186-PATTERNS.md (D-11 describe block pattern section — the recommended ExUnit approach)
    - .planning/phases/186-token-foundation-l0/186-RESEARCH.md (Pattern 2: ExUnit dark-block parity; Pitfall 3: byte-equality anti-pattern; ember parity table)
  </read_first>
  <behavior>
    - Test 1 (dark parity): extracting --sg-* properties from the sigra_admin.css dark @media block and from the app.css explicit-toggle dark block, trimming whitespace, sorting both lists, and asserting equality produces no difference
    - Test 2 (brand-strong assertion): the sorted --sg-* set from the sigra_admin.css dark block contains `--sg-color-brand-strong: #fdba74;` (not `#9a3412`)
    - Test 3 (auth ember light parity): the --sg-color-risk, --sg-color-warn, --sg-color-ok, --sg-color-info values extracted from sigra_admin.css :root match the corresponding --sigra-auth-* values in sigra_auth.css light block
    - Test 4 (auth ember dark parity): the dark --sg-color-risk, --sg-color-warn, --sg-color-ok, --sg-color-info values from the sigra_admin.css dark block match the corresponding --sigra-auth-* dark values
  </behavior>
  <action>
    Append a new describe block to test/sigra/install/features/admin_test.exs, inserted AFTER the closing `end` of the DIST-05 describe block (after line 328) and BEFORE the module-level private helpers and the final module `end`.

    The describe block label is: "D-11 System↔explicit-toggle dark-block parity"

    Implement two tests inside the describe block:

    TEST 1 — dark token value parity:
    Read both CSS files using File.read!. Extract --sg-* declarations from each dark block using the approach below. Assert the sorted sets are equal with a descriptive failure message instructing how to fix (update BOTH dark blocks together).

    Extraction approach: use :binary.match to locate the anchor string in each file. For sigra_admin.css, anchor on "@media (prefers-color-scheme: dark)" and scan forward to find and extract the inner :root block. For app.css, anchor on "html[data-sg-admin-theme=\"dark\"] .sg-admin-shell" and scan forward to extract the block. Then filter lines containing "--sg-", String.trim each, sort, and compare.

    Practical implementation: since the exact line ranges are stable (167–204 for sigra_admin.css; 1512–1543 for app.css), a robust approach is to split the file on newlines and slice the known line range (zero-indexed: lines 166..203 for sigra_admin.css, lines 1511..1542 for app.css), then filter for "--sg-", trim, sort. This avoids brace-counting complexity and directly targets the verified ranges. Include a brief comment noting the line ranges were verified 2026-06-14.

    The extracted and processed list from each block MUST be a sorted list of strings like ["--sg-color-brand-strong: #fdba74;", "--sg-color-brand: #fdba74;", ...] (trimmed, semicolon-terminated, sorted). Assert the two lists are equal.

    Include the #fdba74 assertion as a separate assert: assert "--sg-color-brand-strong: #fdba74;" in admin_dark_props (where admin_dark_props is the extracted sorted list from sigra_admin.css) with message "dark brand-strong must be #fdba74 (WCAG AA lightened value from v1.34); if changed, update all four parity surfaces and both snapshot allowlists".

    TEST 2 — auth ember-family light + dark parity:
    Read sigra_admin.css and sigra_auth.css using File.read!. For each of the four ember tone values (risk, warn, ok, info), extract the light value from sigra_admin.css :root and from sigra_auth.css light block, and assert equality. Then do the same for the dark values.

    Token mapping (from RESEARCH.md ember parity table):
    - Admin: --sg-color-risk; Auth: verify exact name in sigra_auth.css (likely --sigra-auth-risk)
    - Admin: --sg-color-warn; Auth: --sigra-auth-warn
    - Admin: --sg-color-ok; Auth: --sigra-auth-ok
    - Admin: --sg-color-info; Auth: verify exact name (may differ)

    Use a simple extraction helper (private function in the describe block or module-level): given a CSS string and a property name, find the line matching the property, extract the value. String pattern: property name followed by `: ` and a hex value.

    Include a comment noting the near-match exclusion: "Note: --sg-color-panel vs --sigra-auth-surface intentionally differ (#1f1d1a vs #211f1c); not asserted here."

    Failure messages must include "update sigra_auth.css to restore ember parity" so the developer knows the fix direction.

    Module structure: both tests live inside one `describe "D-11 System↔explicit-toggle dark-block parity" do ... end` block. Private helpers (extract_dark_media_props/1, extract_explicit_dark_props/1, extract_token_value/2) can be private functions at the module level alongside the existing source_fragment/3 and source_offset/2 helpers.
  </action>
  <verify>
    <automated>mix test test/sigra/install/features/admin_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/sigra/install/features/admin_test.exs exits 0 with the new describe block present
    - The describe block label is "D-11 System↔explicit-toggle dark-block parity"
    - Contains an assertion that the sorted --sg-* sets from both dark blocks are equal
    - Contains an assert that "--sg-color-brand-strong: #fdba74;" is in the extracted admin dark props
    - Contains assertions for the four ember tone values (risk, warn, ok, info) in both light and dark against auth surface
    - Failure messages include actionable fix instructions (which files to update)
    - Uses File.read! with the same path conventions as DIST-05 (no Path.join(__DIR__, ...))
    - Does NOT use byte-equality on the full dark blocks (only compares sorted extracted --sg-* lines)
  </acceptance_criteria>
  <done>D-11 describe block in admin_test.exs; mix test exits 0; dark-block parity, brand-strong #fdba74, and auth ember-family all asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies one ExUnit test file. It reads local CSS files from the filesystem (no user input, no network, no auth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; test-file edit only |

**Security domain assessment:** This plan edits one ExUnit test file. No auth flows, no user data, no session management, no cryptographic operations, no user input surfaces, no CSS value changes. The test reads local filesystem files (CSS assets). All ASVS categories are N/A. Security-relevant surface: none.
</threat_model>

<verification>
After Task 1 completes:

1. Confirm the new describe block is present:
   `grep -c "D-11 System" test/sigra/install/features/admin_test.exs`
   (should output 1)

2. Run the full ExUnit test file:
   `mix test test/sigra/install/features/admin_test.exs`
   Must exit 0. All DIST-05 and D-11 tests must pass.

3. Confirm the brand-strong assertion is present:
   `grep -c "fdba74" test/sigra/install/features/admin_test.exs`
   (should be >= 1)
</verification>

<success_criteria>
- mix test test/sigra/install/features/admin_test.exs exits 0
- D-11 describe block present with at minimum: System↔toggle dark parity test + brand-strong #fdba74 assertion + auth ember-family cross-check
- The existing DIST-05 test is undisturbed (still passes)
- No new files created; only admin_test.exs modified
</success_criteria>

<output>
Create `.planning/phases/186-token-foundation-l0/186-02-SUMMARY.md` when done.
</output>
