---
phase: 186-token-foundation-l0
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/admin-theme.spec.ts
autonomous: true
requirements:
  - TOKEN-02

must_haves:
  truths:
    - "contrastRatio() assertions cover all four tones (ok/warn/risk/info) on notice backgrounds in light AND dark"
    - "contrastRatio() assertion covers brand-strong text on brand-soft background in dark mode"
    - "New assertions use expect.poll (not direct expect) for computed-style reliability across theme switches"
    - "New assertions navigate to /admin/_design and call waitForLiveViewReady before reading computed styles"
    - "The added assertions pass (>= 4.5 contrast ratio) against the current CSS token values"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-theme.spec.ts"
      provides: "contrastRatio() tone-on-soft computed-style assertions (light + dark)"
      contains: "data-tone=\"ok\""
  key_links:
    - from: "test/example/priv/playwright/tests/admin-theme.spec.ts"
      to: "/admin/_design"
      via: "page.goto('/admin/_design') in new test block"
      pattern: "goto.*_design"
---

<objective>
Extend the existing `contrastRatio()` computed-style assertions in `admin-theme.spec.ts` to cover the four tone-on-soft pairs (ok/warn/risk/info on notice + brand-strong on brand-soft) that axe skips due to alpha-composited `color-mix()` backgrounds.

Purpose: axe cannot evaluate WCAG AA on computed `color-mix(in oklab, var(--sg-color-*-soft) 62%, var(--sg-color-panel))` backgrounds because the alpha is resolved at render time. TOKEN-02 requires "including text on brand-soft surfaces" — these are the pairs that fall through axe's coverage. The `contrastRatio()` computed-style pattern (already established in the spec for notice_link) is the proven mechanism.

Output: `admin-theme.spec.ts` with a new test block covering 5 pairs × 2 themes = 10 contrast ratio assertions; `npx playwright test tests/admin-theme.spec.ts` passes the new assertions.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-token-foundation-l0/186-CONTEXT.md
@.planning/phases/186-token-foundation-l0/186-RESEARCH.md
@.planning/phases/186-token-foundation-l0/186-PATTERNS.md

<interfaces>
<!-- Key patterns the executor MUST read before modifying admin-theme.spec.ts -->

Read test/example/priv/playwright/tests/admin-theme.spec.ts before editing. Key anchors:

File-level imports (lines 1–7):
```typescript
import AxeBuilder from "@axe-core/playwright";
import { test, expect, type Page } from "@playwright/test";
import { TEST_PASSWORD } from "../helpers/fixtures";

const DESKTOP_VIEWPORT = { width: 1280, height: 900 };
const MOBILE_VIEWPORT = { width: 390, height: 844 };
```

waitForLiveViewReady helper (lines 8–12) — call after every page.goto():
```typescript
async function waitForLiveViewReady(page: Page) {
  await page.waitForSelector("[data-phx-session].phx-connected", { state: "attached" });
}
```

contrastRatio() helper (lines 183–190) — do NOT re-define; reference directly in new tests.

Established notice_link contrastRatio pattern (lines 500–521 — EXACT model for new assertions):
- Read computed styles via page.locator(...).evaluate(el => getComputedStyle(...))
- Assert with expect.poll(() => contrastRatio(...)).toBeGreaterThanOrEqual(4.5)
- Theme switch via page.getByRole("radio", { name: "Dark" }).click() + await expect(page.locator(".sg-admin-shell")).toHaveAttribute("data-theme", "dark")

Metric contrast pattern (lines 662–677) — per-element evaluate with querySelector:
```typescript
const metricContrast = async () =>
  metric.evaluate((el) => {
    const value = el.querySelector(".sg-metric__value") as HTMLElement;
    return { color: getComputedStyle(value).color, background: getComputedStyle(el).backgroundColor };
  });
await expect.poll(async () => { const s = await metricContrast(); return contrastRatio(s.color, s.background); }).toBeGreaterThanOrEqual(4.5);
```

Tone pairs to cover (from 186-RESEARCH.md Pattern 3 — axe misses all of these):
- .sg-notice[data-tone="ok"] text (--sg-color-ok) on computed color-mix(ok-soft 62%, panel) bg — light and dark
- .sg-notice[data-tone="warn"] text (--sg-color-warn) on computed warn-soft bg — light and dark
- .sg-notice[data-tone="risk"] text (--sg-color-risk) on computed risk-soft bg — light and dark
- .sg-notice[data-tone="info"] text (--sg-color-info) on computed info-soft bg — light and dark
- .sg-summary-chip brand-strong text on brand-soft bg — dark only (the critical #fdba74 AA surface)

Gallery URL: /admin/_design renders all components including all four tone notices and the summary chip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tone-on-soft contrastRatio() test block to admin-theme.spec.ts</name>
  <files>test/example/priv/playwright/tests/admin-theme.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-theme.spec.ts (full file — especially lines 183–190 for contrastRatio helper, lines 490–576 for notice_link pattern, lines 662–700 for metric contrast pattern, and the end of the file to find the correct append location)
    - .planning/phases/186-token-foundation-l0/186-PATTERNS.md (Pattern 3: contrastRatio() extension; pairs to cover; expect.poll pattern; theme-switch pattern; navigation pattern)
    - .planning/phases/186-token-foundation-l0/186-RESEARCH.md (Pattern 3: exact tone pairs; color values for each light/dark pair; anti-pattern: cartesian product calculator)
    - test/example/priv/playwright/playwright.config.ts (confirm admin-design projects include a "chromium" project the new test can run under; note test file routing)
  </read_first>
  <action>
    Append a new test block to admin-theme.spec.ts after the last existing test, following the established patterns exactly.

    New test name: "tone notice and status chip pairs meet WCAG AA in light and dark (axe-skipped soft backgrounds)"

    The test navigates to /admin/_design (not a live admin page like /admin/users), so all four tone notices and the summary chip are present in the same view without needing fixture users.

    Test structure:
    1. page.goto("/admin/_design") + waitForLiveViewReady(page)
    2. For each of the four tones, create a readStyles async function scoped to that tone's locator:
       - Locator: page.locator(`.sg-notice[data-tone="${tone}"]`) — verify the element is visible first
       - Evaluate: read getComputedStyle() for the notice element's backgroundColor and the inner text element's color. The text element inside .sg-notice is likely .sg-notice__body or direct text — read the notice HTML from the gallery to confirm the correct child selector.
       - Assert: expect.poll(() => contrastRatio(textColor, bgColor)).toBeGreaterThanOrEqual(4.5) with a label like `ok tone text on ok-soft background (light)`
    3. After light assertions for all four tones, switch to dark mode: page.getByRole("radio", { name: "Dark" }).click() + await expect(page.locator(".sg-admin-shell")).toHaveAttribute("data-theme", "dark")
    4. Repeat the four tone contrastRatio assertions in dark mode with dark labels
    5. Summary chip brand-strong assertion (dark mode only — this is the #fdba74 critical pair):
       - Locator: page.locator(".sg-summary-chip").first() (or a more specific locator if the gallery renders multiple chips)
       - Evaluate: read backgroundColor of the chip and color of the inner value element (.sg-metric__value or equivalent)
       - Assert: expect.poll(() => contrastRatio(textColor, chipBg)).toBeGreaterThanOrEqual(4.5)

    Conventions to follow exactly:
    - Use expect.poll (not direct expect) for all contrastRatio assertions — this is mandatory for computed-style reliability after theme switches
    - Call await waitForLiveViewReady(page) after page.goto and after theme switch if re-navigating
    - The contrastRatio() function is already defined in the file — do NOT redefine it
    - Use expect(locator).toBeVisible() before reading computed styles on each element
    - Structure the inner evaluate callback the same way as the metric pattern: evaluate((el) => { const inner = el.querySelector(".sg-notice__body") as HTMLElement; return { color: getComputedStyle(inner).color, background: getComputedStyle(el).backgroundColor }; })

    Failure message format: use test.info().annotations or just the .toBeGreaterThanOrEqual(4.5) — Playwright will show the actual vs expected values. No custom message needed beyond the test name.

    If the /admin/_design gallery does not render .sg-notice components (confirm by checking the gallery LiveView implementation), fall back to navigating to /admin/users which renders the summary chip and notices in the real UI. Check test/example/lib/example_web/live/admin_design_live.ex to confirm notice rendering before choosing the URL.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-theme.spec.ts --project=admin-checkpoints-chromium --grep "tone notice" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - admin-theme.spec.ts contains a new test named "tone notice and status chip pairs meet WCAG AA in light and dark"
    - The test has contrastRatio assertions for all four tones (ok, warn, risk, info) in both light and dark modes (8 assertions)
    - The test has a contrastRatio assertion for the summary chip in dark mode (1 assertion, the #fdba74 surface)
    - All 9 assertions use expect.poll(...).toBeGreaterThanOrEqual(4.5)
    - The test navigates with page.goto and calls waitForLiveViewReady before reading computed styles
    - Running the test against the current CSS (no token changes expected) passes >= 4.5 for all pairs
    - Existing tests in admin-theme.spec.ts are undisturbed
  </acceptance_criteria>
  <done>New test block in admin-theme.spec.ts covers all 4 tone-soft pairs × 2 themes + brand-soft dark pair; npx playwright test tests/admin-theme.spec.ts passes the new assertions; axe gap closed for alpha-composited soft backgrounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → example app | Playwright navigates to /admin/_design, which is the example-only gallery (no installer template). The gallery is dev-gated (not accessible in production generated hosts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; admin-theme.spec.ts edit only using existing @playwright/test and @axe-core/playwright |

**Security domain assessment:** This plan modifies one Playwright test file to add computed-style contrast ratio assertions. No auth flows, no user data, no session management, no cryptographic operations, no user input surfaces are modified. The test reads computed CSS from a dev-only gallery page. All ASVS categories are N/A. Security-relevant surface: none.
</threat_model>

<verification>
After Task 1 completes:

1. Confirm the new test block is present:
   `grep -c "tone notice" test/example/priv/playwright/tests/admin-theme.spec.ts`
   (should output >= 1)

2. Run the new test in isolation (requires booted example server on port 4002 or configured port):
   `cd test/example/priv/playwright && npx playwright test tests/admin-theme.spec.ts --grep "tone notice"`
   All assertions should pass (4.5:1 met by current token values per RESEARCH.md finding).

3. Confirm existing tests still pass:
   `cd test/example/priv/playwright && npx playwright test tests/admin-theme.spec.ts`
   All tests pass including the new additions.
</verification>

<success_criteria>
- admin-theme.spec.ts contains tone-on-soft contrastRatio assertions for ok/warn/risk/info in both light and dark
- Contains brand-strong on brand-soft assertion in dark mode (the #fdba74 pair)
- All new assertions use expect.poll
- npx playwright test tests/admin-theme.spec.ts passes (all new assertions >= 4.5:1 with current token values)
- Existing tests undisturbed
</success_criteria>

<output>
Create `.planning/phases/186-token-foundation-l0/186-03-SUMMARY.md` when done.
</output>
