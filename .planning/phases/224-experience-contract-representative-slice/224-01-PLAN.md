---
phase: 224-experience-contract-representative-slice
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [EXPR-01, EXPR-02]
files_modified:
  - .planning/phases/224-experience-contract-representative-slice/224-CONTEXT.md
  - .planning/phases/224-experience-contract-representative-slice/224-UI-SPEC.md
  - priv/templates/sigra.install/core/sigra_auth_components.ex
  - priv/templates/sigra.install/core/sigra_auth.css
  - priv/templates/sigra.install/core/login_html.ex
  - priv/templates/sigra.install/organizations/live/invitation_accept_live.ex
  - lib/sigra/admin/live/audit_index_live.ex
autonomous: true
---

# Plan 224-01: Contract and Representative Slice

## Objective

Turn the approved milestone research into an executable generated-host contract and prove it across login, invitation mismatch, backup-code recovery, and audit active-filter state.

## Tasks

1. Commit the JTBD/domain/ownership contract and UI specification.
2. Add the bounded semantic `sigra-auth-*` vocabulary and retire broad inference selectors from the canonical CSS.
3. Recompose the representative generated templates without changing security semantics.
4. Replace duplicate audit quick-filter inputs with shareable preset URLs and label the applied-filter region.
5. Add deterministic template/admin tests for class ownership, zero-action mismatch, unique filter keys, and stable URL presets.
6. Run formatter, focused ExUnit, template syntax/render, and accessibility/static-contract gates.

## Must Hold

- `sg-*`, `sigra-auth-*`, and `vt-*` lanes do not cross.
- Invitation mismatch renders no acceptance control.
- Audit GET semantics and existing `applied_chip` component remain intact.
- Generated code stays host-owned and does not require a runtime UI dependency.
- No sleeps or broad text-only Playwright selectors.
