---
phase: 219-baseline-recapture-canary-reconciliation
plan: 03
type: execute
wave: 3
depends_on: [219-02]
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots
  - test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots
autonomous: false
requirements: [RECAP-01]
must_haves:
  truths:
    - "All 115 committed PNG baselines (84 admin-design + 27 admin-checkpoints + 4 demo-showcase) are recaptured in-CI on ubuntu/amd64 (never darwin-local) and land on the Phase 219 branch"
    - "example_playwright_smoke compare-mode is green against the recaptured baselines"
    - "The recapture PR names the changed slugs so the operator scope-check is possible"
    - "PR #70 (elevate-03-wave-v144-pr) is closed as subsumed (D-01)"
  artifacts:
    - "Recaptured 115-PNG set committed onto the Phase 219 branch via the bot recapture PR(s)"
    - "The recapture PR(s) with base = the Phase 219 branch"
  key_links:
    - "workflow_dispatch (recapture_branch=Phase219 branch) → admin_design_recapture + admin_checkpoint_recapture (incl demo-showcase) → bot PR → merge into 219 branch → example_playwright_smoke green"
---

<objective>
Run the branch-scoped recapture (D-04) that produces true CI-native amd64 pixels for all 115
baselines, land them on the Phase 219 branch via the bot review PR, and pass the ONE retained
human touch: the D-05 git-diff scope check (named slugs — not per-pixel eyeballing). This is the
SC-1 deliverable: a reviewable recapture landing with zero spurious drift.

Purpose: Replace darwin/stale committed baselines with amd64-native pixels so the required
example_playwright_smoke compare gate is green-before-merge.
Output: 115 recaptured PNGs on the Phase 219 branch; PR #70 closed as subsumed.

Zero-human on PIXELS (D-05): snapshot-recapture-gate / snapshot-canary-guard / example_playwright_smoke
compare-mode are the automated approval. The single human touch is the cheap scope check below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-baseline-recapture-canary-reconciliation/219-CONTEXT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch the branch-scoped recapture and poll both jobs to green (SC-1, D-04)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — the recapture_branch dispatch input + relaxed release_ref_guard
      + retargeted recapture jobs landed by plan 219-02.
    - CONTEXT.md canonical_refs — the recapture jobs boot the example on :4000, run
      `--update-snapshots` for the 3 admin-design + 3 admin-checkpoints projects + demo-showcase,
      then open a PR whose base is the dispatched branch.
  </read_first>
  <action>
    Push the Phase 219 branch (with plans 219-01 and 219-02 landed) to origin. Trigger the
    branch-scoped recapture: `gh workflow run "CI" --ref <phase-219-branch> -f
    recapture_branch=<phase-219-branch>` (substitute the actual current phase branch name for both
    — the `--ref` selects the branch whose ci.yml runs, and the input drives the release_ref_guard
    relaxation + PR base retarget). Poll the run with `gh run watch` / `gh run view` until
    `admin_design_recapture` and `admin_checkpoint_recapture` (which now also recaptures
    demo-showcase per 219-02) both conclude `success`. These jobs run on ubuntu/amd64 in CI — do NOT
    recapture any admin-native baseline on darwin-local (arm64↔amd64 sub-pixel AA parity is
    unproven). Confirm the bot opened the recapture PR(s) with base = the Phase 219 branch and that
    the design job's self-gate + the checkpoint job's newly-added snapshot-canary-guard self-gate
    both passed (canaries born `added`, non-canary slugs `--allow`ed).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; gh run list --workflow=CI --event=workflow_dispatch --limit 1 --json conclusion,databaseId,headBranch | python3 -c "import json,sys; r=json.load(sys.stdin); assert r and r[0]['conclusion'] in ('success',None), r; print('latest dispatch run:', r[0])"</automated>
  </verify>
  <acceptance_criteria>
    - A workflow_dispatch CI run exists for the Phase 219 branch with `recapture_branch` set.
    - `admin_design_recapture` and `admin_checkpoint_recapture` both conclude `success`.
    - Bot recapture PR(s) exist with base = the Phase 219 branch, listing changed slugs in the body.
    - No admin baseline was recaptured on darwin-local.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator git-diff scope check of the recapture PR (D-05 — the one retained human touch)</name>
  <action>Pause for the operator's git-diff scope check of the bot recapture PR before merge. Present the PR diff (files + named slugs) and wait for approval. This is the single retained human touch (D-05) — a scope check by named slug, not per-pixel eyeballing. Do not merge until approved.</action>
  <what-built>
    The bot recapture PR(s) carrying the amd64-native recapture of the 115 baselines (84
    admin-design + 27 admin-checkpoints + 4 demo-showcase), targeting the Phase 219 branch, with
    the changed slugs named in the PR body and the automated gates (snapshot-canary-guard /
    snapshot-recapture-gate) green.
  </what-built>
  <how-to-verify>
    1. Open the recapture PR(s) (`gh pr view <n> --web` or `gh pr diff <n> --name-only`).
    2. Confirm the changed files are ONLY under the three `*.spec.ts-snapshots/` dirs — no source,
       config, or template files snuck in.
    3. Confirm the changed slugs match the expected recapture scope (admin-design boards,
       admin-checkpoints slugs, the 4 demo-showcase PNGs). This is a SCOPE check by named slug —
       NOT per-pixel eyeballing (D-05: 218 already visually verified the elevated renders).
    4. Confirm NEITHER canary (`board-notice`, `impersonation-banner`) appears as a forbidden
       `modified` in the guard output — they must be `added` (delete-reborn).
  </how-to-verify>
  <resume-signal>Type "approved" if the diff scope is only the expected snapshot slugs, or describe the unexpected files/slugs.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge the recapture PR(s) into the Phase 219 branch, confirm gates, close PR #70 (SC-1, D-01)</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts-snapshots</files>
  <read_first>
    - .github/workflows/ci.yml — example_playwright_smoke compare lanes (design_gallery,
      admin_checkpoints, demo_showcase aggregator) that re-render and pixel-diff against the
      committed PNGs; fast_checks snapshot drift guards (ci.yml:101-109).
    - CONTEXT.md D-01 — PR #70 (`elevate-03-wave-v144-pr`) is stale/subsumed by main; close it.
  </read_first>
  <action>
    After operator approval, merge the bot recapture PR(s) into the Phase 219 branch (`gh pr merge
    <n> --squash` or as the repo ruleset requires). Re-run / confirm CI on the updated Phase 219
    branch so `example_playwright_smoke` compare-mode is green against the recaptured baselines and
    `fast_checks` snapshot drift guards pass (canaries born `added`, no forbidden modify). Then
    close PR #70 as subsumed per D-01 (`gh pr close 70 --comment "Subsumed by main + Phase 219
    recapture; branched fresh off main."`) — do NOT rebase or reconcile it. Do not merge the Phase
    219 branch to main here (allowlist-reset + parity confirm are plans 219-04/219-05).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/*.png | wc -l | grep -qx "84" &amp;&amp; ls test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png | wc -l | grep -qx "27" &amp;&amp; ls test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/*.png | wc -l | grep -qx "4" &amp;&amp; echo "115 baselines present (84/27/4)"</automated>
  </verify>
  <acceptance_criteria>
    - The recapture PR(s) are merged into the Phase 219 branch; the 115-PNG set (84/27/4) is present.
    - example_playwright_smoke compare-mode is green on the updated Phase 219 branch.
    - fast_checks snapshot drift guards pass (canaries `added`, not `modified`).
    - PR #70 is closed as subsumed.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- The workflow_dispatch recapture run's two recapture jobs conclude `success`.
- Operator scope-check approved (only expected snapshot slugs changed).
- 115 baselines present (84 admin-design + 27 admin-checkpoints + 4 demo-showcase).
- example_playwright_smoke compare-mode green on the Phase 219 branch; PR #70 closed.
</verification>

<success_criteria>
SC-1: all ~115 committed PNG baselines are recaptured via the in-CI ubuntu recapture jobs (not
darwin-local), producing a reviewable PR that lands on the Phase 219 branch with zero spurious drift.
</success_criteria>

<artifacts_produced>
- Recaptured 115-PNG amd64-native baseline set on the Phase 219 branch (84/27/4).
- Bot recapture PR(s) (base = Phase 219 branch) recording the changed slugs.
- PR #70 closed as subsumed (D-01).
</artifacts_produced>

<output>
Create `.planning/phases/219-baseline-recapture-canary-reconciliation/219-03-SUMMARY.md` when done.
</output>
