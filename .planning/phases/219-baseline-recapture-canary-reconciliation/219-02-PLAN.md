---
phase: 219-baseline-recapture-canary-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [219-01]
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/snapshot-canary-guard.sh
autonomous: true
requirements: [RECAP-01]
must_haves:
  truths:
    - "The recapture jobs can be triggered via a branch-scoped workflow_dispatch against the Phase 219 branch (a non-tag ref) without release_ref_guard failing"
    - "release_ref_guard STILL requires refs/tags/v* for every dispatch that is NOT a recapture dispatch (no general bypass)"
    - "The recaptured PNG PRs target the dispatched branch, not main"
    - "demo-showcase (4 PNGs) has an in-CI recapture path"
    - "The admin-checkpoints canary (impersonation-banner) is delete-reborn so recapture yields 'added' (guard-tolerated), not 'modified' (guard-forbidden)"
    - "A canary slug can never be silenced by an allowlist — snapshot-canary-guard.sh fails if the canary appears in the allowlist"
  artifacts:
    - "ci.yml workflow_dispatch input recapture_branch"
    - "narrowed release_ref_guard branch that treats a recapture dispatch as not-applicable"
    - "demo-showcase --update-snapshots recapture step folded into admin_checkpoint_recapture"
    - "impersonation-banner delete-rebirth step in admin_checkpoint_recapture"
    - "canary-in-allowlist assertion in snapshot-canary-guard.sh"
  key_links:
    - "recapture_branch input value flows into release_ref_guard bypass condition AND the gh pr create --base of both recapture jobs"
    - "impersonation-banner delete precedes --update-snapshots so the checkpoint job can self-gate via snapshot-canary-guard"
---

<objective>
Perform the CI recapture-trigger surgery (D-04) and close the three recapture-mechanism gaps
(D-03) plus the canary-hardening enforcement (D-06), so the existing ubuntu/amd64 recapture jobs
can run branch-scoped BEFORE merge and produce a complete, self-gated, zero-human-defensible
115-PNG recapture on the Phase 219 branch.

Purpose: Yield true CI-native amd64 pixels AND green-before-merge (no red on the required
example_playwright_smoke gate; no merge deadlock) — the core enabling machinery for SC-1/SC-2.
Output: A surgical, least-surface ci.yml dispatch path + demo-showcase recapture + checkpoint
canary delete-rebirth + comment corrections + a never-allowlistable-canary assertion.

All Claude's-Discretion choices take the LEAST-SURFACE correct option, stated inline per action.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-baseline-recapture-canary-reconciliation/219-CONTEXT.md
@.github/workflows/ci.yml
@scripts/ci/snapshot-canary-guard.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch-scoped recapture dispatch + narrow release_ref_guard relaxation + PR base retarget (D-04)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:3-20 — the `on:` block; workflow_dispatch currently exposes only
      `force_fail_probe`. push watches only [main]; pull_request watches [main].
    - .github/workflows/ci.yml:34-52 — release_ref_guard: on workflow_dispatch it requires
      GITHUB_REF to match refs/tags/v* and exits 1 otherwise.
    - .github/workflows/ci.yml:1497-1504 — admin_design_recapture (needs: release_ref_guard;
      if: github.event_name != 'pull_request'; permissions contents:write + pull-requests:write).
    - .github/workflows/ci.yml:1688-1692 — admin_design gh pr create with hardcoded `--base main`.
    - .github/workflows/ci.yml:1798-1805 — admin_checkpoint_recapture header (same needs/if/perms).
    - .github/workflows/ci.yml:1961-1965 — admin_checkpoint gh pr create with hardcoded `--base main`.
  </read_first>
  <action>
    LEAST-SURFACE CHOICE (D-04): add a single new `workflow_dispatch` string input named
    `recapture_branch` (description: the branch to run branch-scoped baseline recapture against;
    required false; default empty string) to the `on.workflow_dispatch.inputs` map at ci.yml:6-12,
    leaving `force_fail_probe` intact. Then narrow release_ref_guard (ci.yml:38-52): when
    `github.event_name` is `workflow_dispatch` AND `github.event.inputs.recapture_branch` is
    non-empty, echo a "recapture dispatch; release-ref guard not applicable" line and exit 0 BEFORE
    the `refs/tags/v*` case — the existing tag requirement MUST remain unchanged for every other
    dispatch (recapture-only relaxation, NOT a general bypass of the release-evidence guard).
    Retarget both recapture jobs' `gh pr create --base` from the hardcoded `main` (ci.yml:1691 and
    :1964) to the dispatched `recapture_branch` when set, falling back to `main` when empty — so
    recaptured PNGs PR into the Phase 219 branch and land green-before-merge. Reference the input as
    `${{ github.event.inputs.recapture_branch }}`. Do NOT widen the recapture jobs' `permissions:`
    and do NOT add either recapture job to ci-gate.needs (they stay one-time utilities). No new
    jobs, no step reordering.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'recapture_branch' in d['on']['workflow_dispatch']['inputs']; print('recapture_branch input present')"</automated>
  </verify>
  <acceptance_criteria>
    - `on.workflow_dispatch.inputs.recapture_branch` exists (string, default empty).
    - release_ref_guard exits 0 for a recapture dispatch (non-empty recapture_branch) and STILL
      exits 1 for a non-tag dispatch with empty recapture_branch (tag path unchanged).
    - Both recapture jobs' `gh pr create --base` resolve to the dispatched branch when set, else main.
    - No recapture job added to ci-gate.needs; no `permissions:` widened.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Close the three recapture gaps — demo-showcase recapture, checkpoint canary delete-rebirth, stale comments (D-03)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:1597-1609 — admin_design_recapture's board-notice canary
      delete-rebirth step (the exact `find ... -name 'board-notice-admin-design-*.png' -delete`
      pattern to mirror for impersonation-banner).
    - .github/workflows/ci.yml:1896-1910 — admin_checkpoint_recapture's `--update-snapshots` step
      (3 projects; NO canary delete-rebirth precedes it today).
    - .github/workflows/ci.yml:1911-1966 — checkpoint commit step (currently "human visual review
      is the gate", runs NO snapshot-canary-guard; git add of the checkpoints snap dir only).
    - .github/workflows/ci.yml:1490 and :1611 — stale "72 admin-design / 24 boards" comments.
    - test/example/priv/playwright/playwright.config.ts:168 — `demo-showcase-chromium` project.
    - test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/ — the 4 committed PNGs
      (admin-user-detail, admin-user-list, audit-explorer, demo-credentials).
  </read_first>
  <action>
    Three gaps (D-03), cheapest-correct wiring:
    (1) demo-showcase recapture (D-03.1) — LEAST-SURFACE CHOICE: fold into admin_checkpoint_recapture
    (it already boots the example on :4000). After the checkpoint `--update-snapshots` step
    (ci.yml:1896-1910), add one recapture step running `npx playwright test tests/demo-showcase.spec.ts
    --project=demo-showcase-chromium --update-snapshots` (working-directory the playwright dir, same
    CI/SIGRA_EXAMPLE_URL env). Include the demo-showcase snapshots dir in the job's `git add` so the
    4 PNGs commit onto the recapture branch. No canary/allowlist choreography for demo-showcase (the
    lane has neither guard nor canary).
    (2) checkpoint canary delete-rebirth (D-03.2) — BEFORE the checkpoint `--update-snapshots` step,
    add a step (mirroring the design job's board-notice pattern at ci.yml:1597-1609) that runs `find
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots -name
    'impersonation-banner-admin-checkpoints-*.png' -delete` so the canary re-appears as `added`
    (guard-tolerated) rather than `modified` (guard-forbidden). Then convert the checkpoint commit
    step to self-gate like the design job: compute changed slugs, build `--allow` args for every
    non-canary changed slug, and run `bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist
    test/example/priv/playwright/snapshot-allowlist --canary impersonation-banner` with those
    `--allow` flags BEFORE committing — replacing the "human visual review is the gate" posture with
    the automated gate (D-05 zero-human posture). Keep committing to a `ci/recapture-admin-checkpoints-<run_id>`
    branch with `[skip ci]`.
    (3) stale comments (D-03.3) — correct ci.yml:1490 and :1611 from "72 admin-design / 24 boards"
    to "84 admin-design PNGs (28 boards × chromium/mobile/dark)".
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('ci.yml parses')" &amp;&amp; grep -c "demo-showcase.spec.ts --project=demo-showcase-chromium --update-snapshots" .github/workflows/ci.yml &amp;&amp; grep -c "impersonation-banner-admin-checkpoints-\*.png' -delete" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml still parses as valid YAML.
    - admin_checkpoint_recapture runs a demo-showcase `--update-snapshots` step and git-adds the
      demo-showcase snapshots dir (4 PNGs land on the recapture branch).
    - An `impersonation-banner-*.png -delete` step precedes the checkpoint `--update-snapshots`.
    - The checkpoint commit step runs snapshot-canary-guard.sh with `--canary impersonation-banner`
      and per-slug `--allow` flags before committing (no longer human-review-only).
    - ci.yml:1490 and :1611 read 84/28, not 72/24.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Enforce canary-is-never-allowlistable in snapshot-canary-guard.sh (D-06)</name>
  <files>scripts/ci/snapshot-canary-guard.sh</files>
  <read_first>
    - scripts/ci/snapshot-canary-guard.sh:40-51 — allowlist load into the ALLOWED map (committed
      manifest lines + `--allow` flags).
    - scripts/ci/snapshot-canary-guard.sh:89-112 — the per-slug evaluation; the canary
      (line 93) already fires on modify/delete/mixed but there is NO check that the canary is
      absent from ALLOWED.
    - test/example/priv/playwright/snapshot-allowlist and snapshot-allowlist-design — both carry a
      comment "The <canary> canary must NEVER appear here" but nothing enforces it.
  </read_first>
  <action>
    Add an assertion (D-06): after the ALLOWED map is fully populated (after
    scripts/ci/snapshot-canary-guard.sh:51, before the CHANGED_SLUGS collection), if
    `${ALLOWED[$CANARY]:-}` is set, call the existing `fail` helper with a message that the canary
    slug must never be allowlisted (naming the canary and the allowlist path). This closes the
    zero-human-safety hole: an operator cannot silence the sentinel by adding it (via committed
    allowlist OR a stray `--allow`). Keep it a pure guard addition — do not change the added-vs-
    modified canary semantics at lines 93-105.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; bash -n scripts/ci/snapshot-canary-guard.sh &amp;&amp; ! bash scripts/ci/snapshot-canary-guard.sh --base HEAD --canary board-notice --allow board-notice --allowlist /dev/null 2>/dev/null; echo "guard rejects canary-in-allowlist (exit nonzero expected above)"</automated>
  </verify>
  <acceptance_criteria>
    - snapshot-canary-guard.sh exits non-zero with a clear FAIL message when the canary slug is in
      the allowlist or passed via `--allow`.
    - Existing added-tolerated / modified-forbidden canary behavior is unchanged.
    - `bash -n` parses clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow_dispatch actor → CI | A repo actor with dispatch rights supplies `recapture_branch`, choosing which branch the amd64 recapture jobs run against |
| github-actions[bot] (GITHUB_TOKEN, contents:write) → repo | The recapture jobs commit recaptured PNGs and open a PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-219-01 | Elevation of Privilege | release_ref_guard relaxation (ci.yml:34-52) | high | mitigate | Relaxation is gated on a NON-EMPTY `recapture_branch` dispatch input only; the `refs/tags/v*` requirement stays intact for every other dispatch (no general bypass). Only the two recapture jobs (`admin_design_recapture`, `admin_checkpoint_recapture`) consume this path; neither is added to ci-gate.needs, so no required release lane becomes triggerable off a non-tag ref. Dispatch itself already requires repo write access. Confirm the diff does not broaden `permissions:` on any release-affecting job. |
| T-219-02 | Tampering | bot-committed recaptured PNGs | medium | mitigate | Recaptured PNGs land on a `ci/recapture-*` branch and PR into the Phase 219 branch (never a silent push to main); the operator scope-check (D-05, plan 219-03) names the changed slugs; the required `example_playwright_smoke` gate re-renders and pixel-diffs against the committed PNGs, so a wrong/tampered baseline reds a required gate. |
| T-219-03 | Spoofing / Repudiation | canary silenced via allowlist | high | mitigate | Task 3 makes the canary never-allowlistable — snapshot-canary-guard.sh hard-fails if the sentinel slug appears in a committed allowlist or a `--allow` flag, so the zero-human safety tripwire cannot be disarmed. |
| T-219-SC | Tampering | npm/browser installs in recapture jobs | high | accept | No NEW npm/pip/cargo packages are added this phase — recapture reuses lockfile-pinned `npm ci` + `npx playwright install` in existing jobs. No package-legitimacy checkpoint required (no new deps). Digest-pinning the Playwright/browser image is noted as deferred CI-hardening (CONTEXT Deferred). |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` succeeds.
- `bash -n scripts/ci/snapshot-canary-guard.sh` succeeds; guard rejects a canary-in-allowlist.
- Optional if available: `actionlint .github/workflows/ci.yml` reports no new errors.
- `git diff --name-only` shows ONLY .github/workflows/ci.yml and scripts/ci/snapshot-canary-guard.sh.
</verification>

<success_criteria>
The branch-scoped recapture path exists (D-04), all three recapture gaps are closed (D-03), and the
canary is provably never-allowlistable (D-06) — the mechanism is ready to run a complete 115-PNG,
self-gated recapture on the Phase 219 branch.
</success_criteria>

<artifacts_produced>
- ci.yml `on.workflow_dispatch.inputs.recapture_branch` (new string input).
- Narrowed release_ref_guard branch (recapture-dispatch = not-applicable, exit 0; tag path intact).
- Retargeted `gh pr create --base` (dispatched branch, fallback main) in both recapture jobs.
- demo-showcase `--update-snapshots` recapture step + git-add folded into admin_checkpoint_recapture.
- impersonation-banner delete-rebirth step + snapshot-canary-guard self-gate in the checkpoint job.
- Corrected ci.yml:1490/:1611 comments (84 PNGs / 28 boards).
- Canary-never-allowlistable assertion in scripts/ci/snapshot-canary-guard.sh.
</artifacts_produced>

<output>
Create `.planning/phases/219-baseline-recapture-canary-reconciliation/219-02-SUMMARY.md` when done.
</output>
