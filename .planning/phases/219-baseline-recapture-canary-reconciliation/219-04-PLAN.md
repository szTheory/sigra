---
phase: 219-baseline-recapture-canary-reconciliation
plan: 04
type: execute
wave: 4
depends_on: [219-03]
files_modified:
  - test/example/priv/playwright/snapshot-allowlist
  - test/example/priv/playwright/snapshot-allowlist-design
autonomous: true
requirements: [RECAP-01]
must_haves:
  truths:
    - "Both snapshot allowlists are at empty steady-state (comment-only, no active slug lines)"
    - "The snapshot-canary drift guard exits zero on a clean re-run against the post-recapture base for both lanes"
    - "Neither canary slug (board-notice, impersonation-banner) appears in either allowlist"
  artifacts:
    - "test/example/priv/playwright/snapshot-allowlist at empty steady-state"
    - "test/example/priv/playwright/snapshot-allowlist-design at empty steady-state"
  key_links:
    - "post-recapture base has no PNG diff → snapshot-canary-guard.sh exits 0 with empty allowlists"
---

<objective>
Reconcile the allowlists to SC-2's empty steady-state and prove the snapshot-canary drift guard
exits zero on a clean re-run (D-07). Because the recapture path (219-02/03) used the jobs' runtime
`--allow` flags — not committed allowlist entries — both committed allowlists should already be
comment-only; this plan confirms/enforces that and proves the guard is green with no PNG diff vs the
post-recapture base.

Purpose: SC-2 — no slugs suppressed by allowlist entries; guard exits zero on a clean re-run.
Output: Both allowlists at empty steady-state + a guard-green proof for both lanes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-baseline-recapture-canary-reconciliation/219-CONTEXT.md
@scripts/ci/snapshot-canary-guard.sh
@test/example/priv/playwright/snapshot-allowlist
@test/example/priv/playwright/snapshot-allowlist-design
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset both allowlists to empty steady-state (D-07)</name>
  <files>test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/snapshot-allowlist-design</files>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist — admin-checkpoints allowlist (comment-only;
      "The impersonation-banner canary must NEVER appear here").
    - test/example/priv/playwright/snapshot-allowlist-design — admin-design allowlist (comment-only;
      "The board-notice canary must NEVER appear here").
    - scripts/ci/snapshot-canary-guard.sh:42-48 — a line is active only if it has non-comment,
      non-blank content after stripping `#...`.
  </read_first>
  <action>
    Confirm both allowlists carry ZERO active (non-comment, non-blank) slug lines — the D-07
    empty-steady-state. If the recapture landing left any temporary slug entries (it should not, per
    D-07 the jobs use runtime `--allow`), remove them, preserving the existing header comments
    (including the "canary must NEVER appear here" lines). Do NOT add any slug. Verify neither
    canary slug appears as an active line in either file.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; for f in test/example/priv/playwright/snapshot-allowlist test/example/priv/playwright/snapshot-allowlist-design; do n=$(sed 's/#.*//' "$f" | grep -v '^[[:space:]]*$' | wc -l | tr -d ' '); echo "$f active lines: $n"; test "$n" -eq 0; done &amp;&amp; echo "both allowlists empty steady-state"</automated>
  </verify>
  <acceptance_criteria>
    - Both allowlist files have 0 active (non-comment, non-blank) lines.
    - Header comments (incl. the "canary must NEVER appear here" lines) are preserved.
    - Neither `board-notice` nor `impersonation-banner` appears as an active line.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove the canary drift guard exits zero on a clean re-run for both lanes (SC-2)</name>
  <files>scripts/ci/snapshot-canary-guard.sh</files>
  <read_first>
    - scripts/ci/snapshot-canary-guard.sh — `--base`, `--allowlist`, `--canary` semantics; PASS
      when 0 changed slugs (or all changed slugs within allowlist) and no forbidden canary drift.
    - .github/workflows/ci.yml:101-109 — fast_checks invokes both guard lanes with the empty
      committed allowlists; the SC-2 steady state is exactly this passing with no active slugs.
  </read_first>
  <action>
    With the recapture landed (219-03) and the allowlists empty (Task 1), run the guard for BOTH
    lanes against a base where the recaptured PNGs already match (0 changed slugs), exactly as
    fast_checks does: the admin-checkpoints lane `bash scripts/ci/snapshot-canary-guard.sh --base
    <post-recapture-base>` (default SNAP_DIR + default `--canary impersonation-banner` + default
    allowlist), and the admin-design lane with `SNAP_DIR=test/example/priv/playwright/tests/admin-design.spec.ts-snapshots
    bash scripts/ci/snapshot-canary-guard.sh --base <post-recapture-base> --allowlist
    test/example/priv/playwright/snapshot-allowlist-design --canary board-notice`. Both must exit
    zero. Use the merge-base of the Phase 219 branch against main (or HEAD if the recapture commit
    is the tip and the tree is clean) as `<post-recapture-base>` so there is no PNG diff — the SC-2
    empty-steady-state proof. This is the confirm-only follow-up (D-07): base already has the PNGs →
    no diff → guard exits zero with empty allowlists.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra &amp;&amp; bash scripts/ci/snapshot-canary-guard.sh --base HEAD &amp;&amp; SNAP_DIR=test/example/priv/playwright/tests/admin-design.spec.ts-snapshots bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice &amp;&amp; echo "both lanes: guard exits zero on clean re-run"</automated>
  </verify>
  <acceptance_criteria>
    - snapshot-canary-guard.sh exits 0 for the admin-checkpoints lane with the empty committed allowlist.
    - snapshot-canary-guard.sh exits 0 for the admin-design lane with the empty committed allowlist.
    - No slug is suppressed by an allowlist entry (0 active allowlist lines).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Both allowlists have 0 active lines (comment-only steady-state).
- snapshot-canary-guard.sh exits 0 for both the admin-checkpoints and admin-design lanes on a clean
  re-run against the post-recapture base.
</verification>

<success_criteria>
SC-2: both snapshot allowlists are reset to empty steady-state — no slugs suppressed by allowlist
entries — and the snapshot-canary drift guard exits zero on a clean re-run.
</success_criteria>

## Downstream note (Phase 220) — record-for-handoff, NOT a Phase 219 task

The branch-scoped recapture (219-02/03, D-04) solves the canary/guard deadlock for LANDING on the
Phase 219 branch: PNGs land amd64-native, canaries are delete-reborn as `added`, and the guard
exits zero against the post-recapture base. Phase 219 fully achieves its goal on the branch.

BUT the same deadlock re-emerges harder at the eventual **219→main** merge step, and it is now
Phase 220's problem to reconcile: once 219-02 T3 makes the canary **never-allowlistable** AND this
plan resets both allowlists **empty**, the 219→main PR diffs all 115 slugs — including BOTH canaries
(`board-notice`, `impersonation-banner`) — as `modified` vs main's OLD darwin baselines. A `modified`
canary is hard-forbidden with no allowlist escape hatch, so `snapshot-canary-guard.sh` / `fast_checks`
would RED on the 219→main merge.

Scope/severity: `fast_checks` is NOT one of Phase 220's five named required checks (ruleset 14941512:
`Library tests`, `Example unit smoke`, `Install smoke`, `Example HTTP smoke`, `Example Playwright
smoke`), so the ship may still be technically mergeable — but it surfaces as a red gate. Phase 220's
ship planner MUST choose a reconciliation strategy BEFORE executing the milestone-ship PR, e.g.:
(a) the ship PR merges the recaptured canaries as the accepted new baseline (a deliberate one-time
canary re-baseline at the main boundary), or (b) run the canary guard post-merge against the new
main baseline. Do NOT attempt to solve this in Phase 219 — it is captured here purely for handoff.

<artifacts_produced>
- test/example/priv/playwright/snapshot-allowlist at empty steady-state.
- test/example/priv/playwright/snapshot-allowlist-design at empty steady-state.
- Guard-green proof for both lanes (0 changed slugs, empty allowlists).
</artifacts_produced>

<output>
Create `.planning/phases/219-baseline-recapture-canary-reconciliation/219-04-SUMMARY.md` when done.
</output>
