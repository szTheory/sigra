# 230-09 AFTER-CANCEL probe (throwaway)

This file exists only to give the AFTER-CANCEL double-push probe (230-09-PLAN.md Task 3) a
Markdown-only commit to push. The branch and its pull request are closed and deleted at the end
of the probe; this file never lands on `main`.
