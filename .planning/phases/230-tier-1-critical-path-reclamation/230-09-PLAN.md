---
phase: 230-tier-1-critical-path-reclamation
plan: 09
type: execute
wave: 8
depends_on: ["230-08"]
files_modified:
  - .planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md
autonomous: true
requirements: [FAST-02, FAST-03, FAST-04, FAST-05, FAST-06, FAST-07]

must_haves:
  truths:
    - "`230-EVIDENCE.md` records AFTER-PR, AFTER-NONPR, AFTER-DOCSONLY and AFTER-CANCEL with verbatim run IDs and the verbatim command that produced each block, so every claim in this phase is independently re-fetchable."
    - "On AFTER-PR, `Admin eval render + probe (evidence only, not a merge gate)` reports `conclusion: skipped` with a duration under 5 seconds, and the design-gallery step reports 39 executed tests while the snapshot step reports skipped."
    - "On AFTER-PR, the `example_playwright_smoke` step summary reports the Playwright browser cache-hit value, and both the install-step duration and the `actions/cache` post-step duration are recorded verbatim so the FAST-06 net is stated honestly rather than as a headline."
    - "On AFTER-NONPR, `admin_eval_render` executes with a real duration, `design_gallery_snapshots` executes 84 tests, and `admin_design_recapture` executes the full 123-test inventory — the Pitfall 1 regression guard."
    - "On AFTER-DOCSONLY, `gh pr checks` shows all five ruleset-required contexts in a merge-eligible state, `fast_checks` and `library_tests` show realistic non-zero durations, and the Playwright seam aggregator logs the docs-only line."
    - "On AFTER-CANCEL, the superseded run reports `conclusion: cancelled` and the newer run runs to completion; no push-to-main, scheduled or dispatched run in the captured set reports `cancelled` or a queued start."
    - "The AFTER-PR wall-clock is recorded against the 29.5m mean / 27.3m p50 baseline using the committed measurement script, not a hand-computed number."
    - "AFTER-PUSH is recorded as an explicit post-merge obligation with its exact capture command and a pending marker — it is not claimed, inferred, or quietly dropped."
    - "No job in any captured AFTER run concludes with a timeout, confirming the FAST-07 integers do not truncate a real run."
  prohibitions:
    - statement: "MUST NOT narrow or weaken a success criterion at verification time to make it pass; SC-2's restatement was made before the check was written and is recorded with its evidence, and any further restatement must be recorded the same way rather than applied silently."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT record a wall-clock or per-job claim without the verbatim run ID and the command that produced it; a claim without a run ID is not evidence."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT report a lane green on the basis that it was skipped; every demotion in this phase is recorded together with the receiving lane observed executing the demoted work with a non-zero test count."
      status: unresolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md"
      provides: "The completed observed-run evidence ledger for all six requirements"
      contains: "AFTER-PR"
  key_links:
    - from: ".planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md"
      to: "scripts/ci/ci-run-metrics.sh"
      via: "every per-job evidence block is the verbatim stdout of the committed measurement script"
      pattern: "ci-run-metrics\\.sh"
---

<objective>
Capture the observed-run half of the phase — the half the phase is actually judged on.

Every success criterion here is a claim about what a run *did*.
`.planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md` records a milestone that passed audits which were
"code-level reads that never executed the specs" while the required Playwright check carried about 15
real failures. This plan is the correction: four real runs, each captured with a verbatim run ID
through the script committed in plan 01, plus one explicitly pending post-merge slot.

Purpose: make the before/after pair real and re-fetchable.
Output: a completed `230-EVIDENCE.md`.

D-24 binds the FAST-02 evidence specifically: it is proven by each run's own executed-test count, not
by reading YAML. D-21 binds every duration: it is produced by the committed measurement script, so
the AFTER numbers are computed by the same method as the baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-RESEARCH.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md
@.planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture AFTER-PR on the phase branch's final commit</name>
  <files>.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md</files>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`, and the phase branch has an open pull request against `main` (`gh pr view --json number` succeeds); if no PR exists, open one with `gh pr create` before capturing.</precondition>
  <read_first>
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md` as created by plan 01 — the slot table and the BEFORE blocks whose format the AFTER blocks must match exactly so the two are diffable.
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md` § "The Observed-Run Evidence Contract" and § "Restated Success Criterion (SC-2)".
    - `230-RESEARCH.md` § "FAST-02 — Design-Gallery Axe/Snapshot Split" for the expected executed-test integers, and § "FAST-03" for the expected skipped-with-zero-duration shape.
  </read_first>
  <action>
    Push every commit from plans 01-08 to the phase branch and let CI run against that final commit.
    Wait for the run to complete rather than sampling it mid-flight.

    Resolve the run id for that commit, then capture the per-job table verbatim with the committed
    measurement script in `--jobs` mode and paste it into the AFTER-PR slot of `230-EVIDENCE.md`,
    preceded by the exact command line, in the same format as the BEFORE blocks.

    Alongside the per-job table, pull and record four step-level facts from the run that the per-job
    table does not carry:
    - the executed-test count reported by the design-gallery step's Playwright list-reporter tail
      (expected 39) and the conclusion of the snapshot step (expected skipped);
    - the `changes` job's emitted `docs_only` value (expected false — this PR touches non-Markdown
      files, which is the mixed-diff case);
    - the `Cache hit summary` step's Playwright browser cache-hit value, plus the
      `Install Playwright browsers` step duration and the `actions/cache` post-step duration;
    - the conclusion and duration of `Admin eval render + probe (evidence only, not a merge gate)`
      (expected skipped, under 5 seconds).

    Also capture the windowed table with the script's aggregate mode and record it next to the
    REQUIREMENTS.md baseline table, stating plainly which rows are already influenced by this change
    and which are not — the window slides and will contain a mix of pre- and post-change runs until
    Phase 235's dedicated measurement window.

    Record every number as the script printed it. Do not round, recompute, or summarise into a
    headline. If any expected integer does not match, record the actual value and mark that row as a
    discrepancy rather than adjusting the expectation.
  </action>
  <verify>
    <automated>python3 -c "
import re
m=open('.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md').read()
i=m.index('AFTER-PR')
sec=m[i:]
assert re.search(r'ci-run-metrics\.sh --jobs \d{6,}', sec), 'no verbatim --jobs command recorded'
assert re.search(r'\b\d{8,}\b', sec), 'no run id recorded'
for tok in ['Admin eval render','design gallery','cache-hit','docs_only']:
    assert tok.lower() in sec.lower(), 'missing AFTER-PR fact: '+tok
assert 'pending' not in sec.split('AFTER-NONPR')[0].lower(), 'AFTER-PR still marked pending'
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The AFTER-PR slot in `230-EVIDENCE.md` carries a run id of at least 8 digits and the verbatim `scripts/ci/ci-run-metrics.sh --jobs <id>` command that produced its block.
    - `gh run view <that id> --json jobs` re-fetched by a reviewer reproduces the pasted table.
    - The recorded conclusion for `Admin eval render + probe (evidence only, not a merge gate)` is `skipped` with a duration under 5 seconds.
    - The recorded executed-test count for the design-gallery step is 39, and the snapshot step's recorded conclusion is `skipped`.
    - The recorded `docs_only` value for this run is `false`.
    - The Playwright browser cache-hit value, the `Install Playwright browsers` step duration, and the `actions/cache` post-step duration are all recorded as printed.
    - No job in the recorded table concludes with a timeout.
    - The AFTER-PR row of the slot table is marked captured, not pending.
  </acceptance_criteria>
  <reversibility rating="reversible">Evidence capture only; the push is the phase's own work being published.</reversibility>
  <done>The PR-lane half of the before/after pair exists with a verbatim run ID and per-step facts.</done>
</task>

<task type="auto">
  <name>Task 2: Capture AFTER-NONPR via a workflow_dispatch on the phase branch</name>
  <files>.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md</files>
  <precondition>`gh auth status` succeeds and the phase branch is pushed; the dispatch is run with `force_fail_probe` false and `recapture_branch` empty.</precondition>
  <read_first>
    - `.github/workflows/ci.yml` lines 3-25 — the `workflow_dispatch` inputs (`force_fail_probe`, `recapture_branch`), both of which must be left at their defaults.
    - `.github/workflows/ci.yml` lines 1688-1700 — `admin_design_recapture`'s commit-and-open-PR step, which is gated to `workflow_dispatch`. **A dispatch run will open a recapture pull request if any baseline drifts.** That is expected, is not a defect, and must be cleaned up by this task.
    - `230-RESEARCH.md` § "Open Questions" item 4 — why a green scheduled run may be unobtainable inside this phase and why a dispatch proxy plus the structural run-id argument is the honest substitute.
  </read_first>
  <action>
    Trigger a `workflow_dispatch` run of the CI workflow against the phase branch with both inputs at
    their defaults, and wait for it to complete.

    Capture the per-job table verbatim with the measurement script's `--jobs` mode into an AFTER-NONPR
    slot, preceded by the exact command line. Record four facts from that run:
    - `admin_eval_render` executes: a non-skipped conclusion with a real duration in the ~18 minute
      band, confirming the demotion moved the work rather than deleting it. Note that its conclusion
      may be `failure` under its retained `continue-on-error: true` — that is GATE-04's scope in
      Phase 231 and does not invalidate this observation; record the conclusion as printed.
    - `design_gallery_snapshots` executes and its Playwright list-reporter tail reports 84 tests.
    - `admin_design_recapture`'s recapture step executes the full ungrepped inventory: **123 tests**,
      not the 120 recorded in RESEARCH.md and VALIDATION.md — that figure predates the axe test added
      in plan 02, which executes once per design project. A report of 36 or 39 there is the Pitfall 1
      regression and must be recorded as a discrepancy.
    - No job in the run reports `cancelled`, and the run's earliest job start is within a few seconds
      of the run's creation, i.e. it was not queued — the structural half of SC-3.

    Then clean up: if the dispatch opened a baseline recapture pull request, record its number in the
    ledger, close it, and delete its branch. Do not merge it — baseline changes are out of this
    phase's scope.

    Record explicitly in the ledger that the scheduled-run half of SC-3 is proven by this dispatch
    proxy plus the structural run-id grouping argument, and not by an observed green nightly: the
    nightly has been 0-pass over the whole sampled window and reviving it is Phase 231's GATE-01.
    State that substitution in words rather than letting it pass silently.
  </action>
  <verify>
    <automated>python3 -c "
import re
m=open('.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md').read()
i=m.index('AFTER-NONPR'); sec=m[i:]
assert re.search(r'ci-run-metrics\.sh --jobs \d{6,}', sec)
for tok in ['admin_eval_render','design_gallery_snapshots','admin_design_recapture','123','workflow_dispatch','GATE-01']:
    assert tok in sec, 'missing AFTER-NONPR fact: '+tok
assert '84' in sec
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The AFTER-NONPR slot carries a run id and the verbatim `--jobs` command that produced its block.
    - `admin_eval_render`'s recorded duration on that run exceeds 900 seconds and its conclusion is recorded exactly as printed.
    - `design_gallery_snapshots` is recorded as executing with 84 tests.
    - `admin_design_recapture`'s recapture step is recorded as executing 123 tests; any other value is recorded as a discrepancy with the observed number.
    - No job in the recorded table has conclusion `cancelled`, and the run's queue delay is recorded.
    - Any recapture pull request opened by the dispatch is recorded by number and is closed, with its branch deleted; `gh pr view <n> --json state` reports `CLOSED`.
    - The ledger states in words that the schedule half of SC-3 is proven by a dispatch proxy plus the structural run-id argument, and names GATE-01 / Phase 231 as the owner of nightly greenness.
  </acceptance_criteria>
  <reversibility rating="reversible">A dispatch run plus closing any PR it opens; no branch state is merged.</reversibility>
  <done>The non-PR half of every demotion is observed executing, and the recapture lane is proven not to have been silently emptied.</done>
</task>

<task type="auto">
  <name>Task 3: Capture AFTER-DOCSONLY and AFTER-CANCEL, then close the ledger</name>
  <files>.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md</files>
  <precondition>`gh auth status` succeeds and the phase branch's changes are on the pull request being probed — a throwaway branch cut from the phase branch inherits the new workflow, whereas one cut from `main` would not.</precondition>
  <read_first>
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md` with the AFTER-PR and AFTER-NONPR slots filled.
    - `MAINTAINING.md` lines 100-117 — the five byte-exact ruleset-required check names to look for in `gh pr checks`.
    - `230-RESEARCH.md` § "FAST-04 — Concurrency" → the SC-3 proof recipe.
  </read_first>
  <action>
    Cut a throwaway branch from the phase branch, add a single trivial Markdown-only commit (touching
    one `.md` file and nothing else), push it, and open a pull request against `main`. Cutting from
    the phase branch rather than from `main` is essential — a branch cut from `main` would run the
    pre-change workflow and prove nothing.

    Capture AFTER-DOCSONLY from that pull request:
    - `gh pr checks` output verbatim, showing the state of all five ruleset-required contexts; none of
      them may be pending.
    - the `changes` job's emitted `docs_only` value (expected true);
    - the durations of `fast_checks` and `library_tests`, which must be realistic rather than near
      zero, proving the two doc-contract lanes were not gated off;
    - the `Example Playwright smoke (full lifecycle)` job's conclusion together with its seam
      aggregator's log line, which must be the docs-only line rather than the ordinary
      all-seams-passed line;
    - the whole-run wall-clock, against the 29.5m mean baseline.

    Then capture AFTER-CANCEL on that same throwaway pull request: push a second trivial commit
    roughly 30 seconds after the first run starts, while the run is still in its library-tests leg, and
    record `gh run list --branch <throwaway> --json databaseId,headSha,status,conclusion,createdAt`
    showing the earlier run with conclusion `cancelled` and the later run completing.

    Close the throwaway pull request and delete its branch, recording its number in the ledger.

    Finally, close the ledger:
    - mark AFTER-PR, AFTER-NONPR, AFTER-DOCSONLY and AFTER-CANCEL captured with their run ids;
    - record AFTER-PUSH as an explicit post-merge obligation with a pending marker, its exact capture
      command, and one sentence stating that it cannot be captured before the merge commit exists and
      is therefore neither claimed nor inferred here;
    - add a per-requirement summary table mapping FAST-02 through FAST-07 to the slot and the specific
      recorded number that proves it, so a reviewer never has to search the ledger;
    - restate, in the FAST-06 row, the honest net: a cache hit was logged, install step took X, the
      cache post-step took Y — never a headline saving figure;
    - list any discrepancy found during capture as an explicit open item rather than adjusting the
      expectation it failed to meet.
  </action>
  <verify>
    <automated>python3 -c "
import re
m=open('.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md').read()
for slot in ['AFTER-PR','AFTER-NONPR','AFTER-DOCSONLY','AFTER-CANCEL','AFTER-PUSH']:
    assert slot in m, slot
d=m[m.index('AFTER-DOCSONLY'):m.index('AFTER-CANCEL')]
for tok in ['Library tests','Example Playwright smoke (full lifecycle)','fast_checks','docs_only']:
    assert tok in d, 'missing AFTER-DOCSONLY fact: '+tok
c=m[m.index('AFTER-CANCEL'):]
assert 'cancelled' in c
p=m[m.index('AFTER-PUSH'):]
assert 'pending' in p.lower() and 'ci-run-metrics.sh' in p
for r in ['FAST-02','FAST-03','FAST-04','FAST-05','FAST-06','FAST-07']:
    assert r in m, r
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The AFTER-DOCSONLY slot carries the verbatim `gh pr checks` output for the throwaway pull request, in which all five required contexts — `Library tests`, `Example unit smoke (ExUnit + ConnTest)`, `Install smoke (fresh phx.new + sigra.install)`, `Example HTTP smoke (boot + curl critical routes)`, `Example Playwright smoke (full lifecycle)` — are in a merge-eligible state and none is pending.
    - The recorded `docs_only` value for that run is `true`, and the recorded `fast_checks` and `library_tests` durations are non-zero and consistent with a full run.
    - The recorded seam-aggregator log line for that run is the docs-only line.
    - The AFTER-CANCEL slot carries `gh run list` output in which the earlier of two runs on the throwaway branch has conclusion `cancelled` and the later completed.
    - The throwaway pull request is closed and its branch deleted; `gh pr view <n> --json state` reports `CLOSED`.
    - The AFTER-PUSH row is marked pending with its exact capture command and an explicit statement that it is a post-merge obligation.
    - The ledger contains a per-requirement table covering FAST-02 through FAST-07, each row naming the slot and the specific number that proves it.
    - The FAST-06 row states the measured install-step and cache post-step durations rather than a headline saving.
    - Every discrepancy encountered during capture is listed as an open item.
  </acceptance_criteria>
  <reversibility rating="costly">The probes create a throwaway branch, a pull request and two runs. Cleanup is closing the pull request and deleting the branch, both scripted in the task, but the runs and the closed pull request remain in the repository's history as artifacts. That residue is deliberate — it is the evidence.</reversibility>
  <done>All four capturable slots are filled with verbatim run IDs, AFTER-PUSH is an explicit pending obligation, and every requirement maps to a recorded number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recorded evidence → phase verdict | The ledger is what `/gsd-verify-work` and Phase 235 read; a fabricated or rounded number is a false verdict |
| Throwaway probe branch → repository state | A probe branch and pull request are created against the real repository |
| Dispatch run → baseline recapture | A `workflow_dispatch` run can open a baseline recapture pull request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-230-09-01 | Repudiation | Evidence ledger | critical | mitigate | Every block is the verbatim stdout of the committed measurement script, preceded by the command line and carrying a run id, so a reviewer can re-fetch and re-verify each claim independently |
| T-230-09-02 | Repudiation | Expectation adjustment | critical | mitigate | The action and acceptance criteria require any mismatch to be recorded as a discrepancy with the observed value; weakening an expectation to make it pass is bound by an explicit prohibition |
| T-230-09-03 | Tampering | Baseline PNGs via a dispatch-opened recapture pull request | high | mitigate | The recapture pull request is recorded, closed and its branch deleted; it is explicitly never merged |
| T-230-09-04 | Denial of service | Probe branch residue | low | accept | The throwaway branch and pull request are deleted and closed; the closed pull request and its runs remain as evidence, which is intended |
| T-230-09-05 | Information disclosure | Pasted CI output | low | mitigate | Only job/step names, conclusions and durations are pasted; the measurement script never echoes a token and `ci.yml` reads no secret beyond `GITHUB_TOKEN` |
| T-230-09-06 | Repudiation | Unobtainable scheduled-run observation | medium | mitigate | The dispatch proxy substitution is stated in words in the ledger with GATE-01 / Phase 231 named as the owner of nightly greenness, rather than allowed to pass as an observed nightly |
| T-230-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package added; evidence capture only |
</threat_model>

## Artifacts this phase produces

See `230-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide symbol list. This plan
creates no new code symbol; it completes
`.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md`.

<verification>
- All five slot names present in the ledger; four captured with run ids, AFTER-PUSH explicitly pending.
- Each captured block preceded by its verbatim command line.
- Per-requirement table covering FAST-02 through FAST-07, each row naming a recorded number.
- Throwaway pull request closed and branch deleted; any dispatch-opened recapture pull request closed.
- Discrepancies listed as open items rather than absorbed into adjusted expectations.
</verification>

<success_criteria>
- The phase's before/after pair exists as four real runs plus one declared post-merge obligation.
- Every FAST-0x requirement maps to a specific recorded number from a named run.
- The FAST-06 claim is a logged cache hit with measured durations, not a saving figure.
- SC-2 is verified against its recorded restatement (skipped with a sub-5-second duration), not against
  the unsatisfiable ROADMAP wording.
</success_criteria>

<output>
Create `.planning/phases/230-tier-1-critical-path-reclamation/230-09-SUMMARY.md` when done
</output>
