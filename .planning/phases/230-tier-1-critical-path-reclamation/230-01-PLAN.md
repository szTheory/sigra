---
phase: 230-tier-1-critical-path-reclamation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/ci-run-metrics.sh
  - scripts/ci/ci-run-metrics.test.sh
  - .github/workflows/ci.yml
  - .planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md
autonomous: true
requirements: [FAST-02, FAST-03, FAST-04, FAST-05, FAST-06, FAST-07]

must_haves:
  truths:
    - "`bash scripts/ci/ci-run-metrics.sh --jobs 30390832059` prints a per-job table for the pre-change PR run in which `Admin eval render + probe (evidence only, not a merge gate)` carries a duration of 17m33s (1053s)."
    - "`bash scripts/ci/ci-run-metrics.sh --limit 40 --format table` emits the exact column shape `trigger | n | mean | p50 | max | outcomes` used by REQUIREMENTS.md:9-13, so an AFTER table is diffable against the committed baseline."
    - "A skipped job whose `completedAt` precedes its `startedAt` is reported as duration 0, never as a negative number."
    - "The script computes per-job durations without filtering on `conclusion == \"success\"`, so `admin_eval_render`'s 17m33s under `continue-on-error: true` is counted as real runner time."
    - "`bash scripts/ci/ci-run-metrics.test.sh` exits 0 with zero network access and no GH_TOKEN, and exits non-zero with zero recorded `gh` invocations when a required input is missing."
    - "`fast_checks` executes `scripts/ci/ci-run-metrics.test.sh` on every PR and push, so the measurement contract cannot silently rot."
    - "`230-EVIDENCE.md` records the BEFORE-PR and BEFORE-PUSH slots with verbatim run IDs 30390832059 and 30389700235 and the verbatim command that produced each block."
  prohibitions:
    - statement: "MUST NOT state a CI performance win that was not produced by the committed measurement method; every wall-clock or per-job claim carries a verbatim run ID and the `ci-run-metrics.sh` invocation that produced it."
      status: unresolved
      verification: judgment
  artifacts:
    - path: "scripts/ci/ci-run-metrics.sh"
      provides: "Committed CI wall-clock / per-job measurement instrument (D-21)"
      contains: "--jobs"
    - path: "scripts/ci/ci-run-metrics.test.sh"
      provides: "Hermetic PATH-stub self-test pinning the four behavioural requirements"
      contains: "ci-run-metrics.test: PASS"
    - path: ".planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md"
      provides: "The phase's observed-run evidence ledger with BEFORE slots filled"
      contains: "30390832059"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/ci-run-metrics.test.sh"
      via: "a `fast_checks` step running the self-test on every PR and push"
      pattern: "ci-run-metrics\\.test\\.sh"
    - from: "scripts/ci/ci-run-metrics.sh"
      to: "gh CLI"
      via: "bare `gh run list` / `gh run view` calls resolved through PATH (so the self-test can shadow them)"
      pattern: "gh run (list|view)"
---

<objective>
Land the committed measurement instrument that every other plan in this phase is proven with, and
fill the two BEFORE slots of the observed-run evidence ledger.

D-21 is an ordering constraint, not a nice-to-have: no measurement may be taken before the script
that defines the measurement exists in git. Nothing in the repo today records how the 2026-07-28
baseline table was windowed, how mean/p50/max were computed, or whether wall-clock is
queue-inclusive — so without this plan, Phase 230's "after" numbers and Phase 235's FAST-01 verdict
would be measured differently from the baseline and the milestone's headline claim would be
unfalsifiable.

Purpose: make every FAST-0x proof in this phase reproducible from a verbatim run ID.
Output: `scripts/ci/ci-run-metrics.sh` + its hermetic self-test, the self-test wired into
`fast_checks`, and `230-EVIDENCE.md` carrying BEFORE-PR and BEFORE-PUSH.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-CONTEXT.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-RESEARCH.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-PATTERNS.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md
@scripts/ci/notify-failure-issue.sh
@scripts/ci/notify-failure-issue.test.sh
@scripts/ci/quality-ledger-monotonic.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end per-run measurement — one path, `--jobs <run_id>`, wired and self-tested</name>
  <files>scripts/ci/ci-run-metrics.sh, scripts/ci/ci-run-metrics.test.sh, .github/workflows/ci.yml</files>
  <read_first>
    - `scripts/ci/notify-failure-issue.sh` — the header/provenance-comment convention (shebang, phase/decision-ID attribution, contract paragraph, named consumer list, Security note when it touches `gh`, then `set -euo pipefail`), the fail-closed `: "${VAR:?msg}"` idiom, and the bare `gh` invocation form (resolved via PATH — this is what makes the self-test hermetic).
    - `scripts/ci/notify-failure-issue.test.sh` — read in full. This is the structural template: `mktemp -d` sandbox, a recording `gh` stub written into `$TMPDIR/bin` and prepended to PATH, per-case `set +e` invocation, assertions on BOTH exit code and the stub's argv log, `pass`/`fail` counters, `trap cleanup EXIT`, and the fixed summary block.
    - `scripts/ci/quality-ledger-monotonic.sh` — the `ROOT=` resolution, the `while [[ $# -gt 0 ]] ... case` flag loop with **unknown-arg exit 2**, the `fail()` helper, and the `<script-name>: FAIL|INFO|PASS` output vocabulary.
    - `.github/workflows/ci.yml` lines 110-226 — the `fast_checks` guard/self-test adjacency convention and the `actions/setup-node` boundary at line 213.
    - `230-RESEARCH.md` § "D-21 — The Measurement Script" — the flag contract table, the four behavioural requirements, and the verified `gh run list --json` / `gh run view --json jobs` field lists.
  </read_first>
  <behavior>
    - `--jobs 30390832059` prints one row per job of that run: job name, conclusion, duration in seconds and in `NmSSs` form.
    - `Admin eval render + probe (evidence only, not a merge gate)` appears in that output with duration 1053s (17m33s) and conclusion `failure` — it is NOT filtered out despite not concluding `success`.
    - A job whose `completedAt` is earlier than its `startedAt` (observed on `Upgrade smoke` and `notify_release_lane_rot`) reports duration `0`, never a negative value.
    - Invoked with an unknown flag, the script exits 2 and prints `ci-run-metrics: FAIL: unknown arg: <arg>` to stderr.
    - Invoked when `gh` is absent from PATH, or when `gh` exits non-zero, or when the run has an empty job list, the script exits non-zero and prints a `ci-run-metrics: FAIL:` line — it never emits a partial or empty-but-successful table.
    - The self-test drives all of the above through a PATH-shadowing recording `gh` stub returning canned JSON; it makes no network call and requires no `GH_TOKEN`.
  </behavior>
  <action>
    Create `scripts/ci/ci-run-metrics.sh` implementing the single-run mode only: flag `--jobs <run_id>`
    plus `--repo <owner/name>` (default `szTheory/sigra`) and `--format table|json` (default `table`).
    Follow the header/provenance convention from `notify-failure-issue.sh` verbatim in shape: shebang,
    an attribution line reading `Phase 230 (D-21 / SC-5)`, a contract paragraph, the named consumer
    list (`fast_checks` self-test; the phase's observed-run evidence ledger), a Security note stating
    that no token or secret is ever echoed, then `set -euo pipefail`. Copy the `ROOT=`, flag-loop and
    `fail()` shape from `quality-ledger-monotonic.sh`, keeping unknown-arg exit 2 distinct from the
    exit-1 failure code, and keep the `ci-run-metrics: FAIL|INFO|PASS` output vocabulary.

    Data source is exactly `gh run view "$RUN_ID" --repo "$REPO" --json jobs`, called bare so PATH can
    shadow it. Per job read `name`, `conclusion`, `startedAt`, `completedAt`. Duration is
    `completedAt - startedAt` clamped at 0. Do NOT filter jobs by conclusion — `admin_eval_render`
    concludes `failure` under `continue-on-error: true` and its 1053s is real runner time.
    Fail closed: if `gh` is not on PATH, if `gh` exits non-zero, or if the returned job array is
    empty, print a `ci-run-metrics: FAIL:` line to stderr and exit 1.

    Create `scripts/ci/ci-run-metrics.test.sh` structurally copied from `notify-failure-issue.test.sh`:
    the same docblock case list in prose, the same `mktemp -d` sandbox, the same recording `gh` stub in
    `$TMPDIR/bin` prepended to PATH, the same `pass`/`fail` counters, `trap cleanup EXIT`, summary
    block and exit convention (only the script name changes). The stub returns canned
    `gh run view --json jobs` JSON that MUST include (a) a job with a negative raw duration, and
    (b) a job concluding `failure` with a large real duration. Cover the six behaviours listed in
    `<behavior>`, including a fail-closed case asserting **zero** recorded stub invocations.

    Wire only the self-test into `fast_checks` (the script itself needs live `gh`, so it follows the
    `ci.yml:146-149` split convention where only the self-test is wired and the comment says so).
    Add one step named `CI run metrics self-test` running `bash scripts/ci/ci-run-metrics.test.sh`,
    placed among the existing bash-only self-tests and **before** the `actions/setup-node` step at
    `ci.yml:213` — everything after that line is the cheerio-dependent block. Carry a
    `# Phase 230 (D-21 / SC-5): ...` comment stating what it proves and that the measuring script
    itself is not run here because it requires a live `gh`.
  </action>
  <verify>
    <automated>bash scripts/ci/ci-run-metrics.test.sh && actionlint -shellcheck= .github/workflows/ci.yml && bash scripts/ci/ci-run-metrics.sh --jobs 30390832059 | grep -qE 'Admin eval render.*1053'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/ci-run-metrics.test.sh` exits 0 and its summary line reports 0 failed.
    - `bash scripts/ci/ci-run-metrics.test.sh` exits 0 when run with `GH_TOKEN` unset and with no network reachable (it never calls the real `gh`).
    - `bash scripts/ci/ci-run-metrics.sh --jobs 30390832059` exits 0 and its output contains a row for `Admin eval render + probe (evidence only, not a merge gate)` whose duration is `1053` seconds / `17m33s`.
    - That same output contains a row for `Upgrade smoke` with duration `0` (not a negative number).
    - `bash scripts/ci/ci-run-metrics.sh --bogus-flag` exits 2 and prints `ci-run-metrics: FAIL: unknown arg: --bogus-flag` on stderr.
    - `actionlint -shellcheck= .github/workflows/ci.yml` exits 0 (it exits 0 on the pre-change file, so any non-zero exit is attributable to this edit).
    - `grep -c 'ci-run-metrics.test.sh' .github/workflows/ci.yml` returns 1.
    - `mix test test/sigra/planning/` reports the same pass/fail counts as the pre-change baseline captured before this task started.
  </acceptance_criteria>
  <reversibility rating="reversible">Two new files plus one additive `fast_checks` step; reverting is `git rm` plus deleting one step.</reversibility>
  <done>The measurement instrument exists in git, runs end-to-end against a real pre-change run, and its contract is executed on every PR and push by `fast_checks`.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to the windowed baseline-reproduction mode</name>
  <files>scripts/ci/ci-run-metrics.sh, scripts/ci/ci-run-metrics.test.sh</files>
  <read_first>
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` as written in Task 1.
    - `.planning/REQUIREMENTS.md` lines 9-13 — the exact baseline table this mode must reproduce, column for column.
    - `230-RESEARCH.md` § "D-21 — The Measurement Script" — the reconstructed baseline command, the flag contract table, and the note that `gh run list --json` does **not** expose a `jobs` field.
  </read_first>
  <behavior>
    - `--limit 40` with no `--event` groups the last 40 runs by `event` and prints one row per trigger.
    - The emitted table's header is exactly `trigger | n | mean | p50 | max | outcomes`, matching REQUIREMENTS.md:9-13 so an AFTER table is a plain text diff against the committed baseline.
    - `--mode wall` (default) computes `updatedAt - createdAt` (queue-inclusive, the baseline's method); `--mode jobspan` computes `max(job.completedAt) - min(job.startedAt)`.
    - p50 is computed by sorting ascending and taking index `floor(n/2)` on a 0-based array; the rule is stated in the script header so it can never be silently reinterpreted.
    - `--since <ISO8601>` restricts the window to runs created at or after that timestamp; without it the window can straddle the change under measurement.
    - `--format json` emits the same aggregates as machine-readable JSON.
    - `--jobs` remains a single-run mode and is never combined with the window sweep (a 40-run per-job sweep would be 40 API round-trips).
  </behavior>
  <action>
    Extend the flag loop with `--workflow <file>` (default `ci.yml`), `--limit <n>` (default 40),
    `--since <ISO8601>` (unset by default), `--event <name>` (unset = all), and
    `--mode wall|jobspan` (default `wall`). The window data source is
    `gh run list --repo "$REPO" --workflow "$WORKFLOW" --limit "$LIMIT" --json event,createdAt,updatedAt,conclusion`
    with `--jq` doing the projection, exactly the reconstructed command in RESEARCH.

    Emit the table with header `trigger | n | mean | p50 | max | outcomes` and an outcomes cell in the
    committed baseline's `N pass / M fail` form. Durations render in the `NN.Nm` form REQUIREMENTS.md
    uses. State the p50 rule (sort ascending, 0-based index `floor(n/2)`) as a comment in the script
    header — an undefined p50 is precisely the ambiguity D-21 exists to remove. Keep the clamp-at-zero
    rule and the no-conclusion-filter rule from Task 1 applying to both modes.

    Extend the self-test with cases pinning: the exact header string; the p50 index rule against a
    fixed odd-length and a fixed even-length canned window; the clamp for a negative `updatedAt -
    createdAt`; that `--mode jobspan` produces a different (job-span) number than `--mode wall` on the
    same canned run; and fail-closed behaviour on an empty run list. Keep every case hermetic through
    the same PATH-shadowing `gh` stub.
  </action>
  <verify>
    <automated>bash scripts/ci/ci-run-metrics.test.sh && bash scripts/ci/ci-run-metrics.sh --limit 40 --format table | grep -qE 'trigger *\| *n *\| *mean *\| *p50 *\| *max *\| *outcomes'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/ci-run-metrics.test.sh` exits 0 and reports at least 8 passing cases, 0 failed.
    - `bash scripts/ci/ci-run-metrics.sh --limit 40 --format table` exits 0 and emits a header line whose fields are exactly `trigger`, `n`, `mean`, `p50`, `max`, `outcomes` in that order.
    - That same invocation emits a `pull_request` row, a `push` row and a `schedule` row.
    - `bash scripts/ci/ci-run-metrics.sh --limit 40 --format json` exits 0 and its output parses as JSON (`python3 -c "import json,sys; json.load(sys.stdin)"` exits 0).
    - `bash scripts/ci/ci-run-metrics.sh --limit 40 --mode jobspan --format table` exits 0 and produces mean values strictly smaller than the `--mode wall` run for the same window (job-span excludes queue time).
    - The script header contains a stated p50 definition naming both the sort direction and the index rule.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive flags on a file created in Task 1.</reversibility>
  <done>The committed script reproduces the baseline table's exact column shape, so the AFTER table is diffable against REQUIREMENTS.md:9-13.</done>
</task>

<task type="auto">
  <name>Task 3: Open the observed-run evidence ledger with both BEFORE slots</name>
  <files>.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-VALIDATION.md` § "The Observed-Run Evidence Contract" — the six-slot table this ledger materialises, and the restated SC-2.
    - `.planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md` — the precedent failure mode ("code-level reads that never executed the specs") that makes a run ID mandatory on every claim.
    - `scripts/ci/ci-run-metrics.sh` as written in Tasks 1-2.
  </read_first>
  <action>
    Create `230-EVIDENCE.md` as the phase's evidence ledger. Open with a one-paragraph binding
    statement: a claim without a verbatim run ID is not evidence, and static reads of `ci.yml` or
    `admin-design.spec.ts` are necessary-but-not-sufficient pre-checks only.

    Add a slot table with the six rows from VALIDATION.md (BEFORE-PR, BEFORE-PUSH, AFTER-PR,
    AFTER-PUSH, AFTER-DOCSONLY, AFTER-CANCEL) plus one row VALIDATION.md does not enumerate but the
    phase needs: **AFTER-NONPR**, a `workflow_dispatch` run on the phase branch, which is how the
    demoted `admin_eval_render` and the event-gated snapshot step are observed *executing* inside the
    phase window rather than only after merge. Each row carries its run ID (or `pending`), the exact
    command that captures it, and a captured/pending marker.

    Fill BEFORE-PR from run `30390832059` and BEFORE-PUSH from run `30389700235` by pasting the
    verbatim stdout of `bash scripts/ci/ci-run-metrics.sh --jobs <id>` into a fenced block under each
    slot, above it the exact command line that produced it. Also paste the verbatim
    `bash scripts/ci/ci-run-metrics.sh --limit 40 --format table` output as the re-fetched window
    baseline, noting the fetch date and that the 40-run window slides so `n` will differ from
    REQUIREMENTS.md:9-13 while the `max` values should match exactly.

    Add a section reproducing VALIDATION.md's restated SC-2 verbatim, with a one-line note that the
    ROADMAP wording ("absent from the job list") is unsatisfiable because a false `if:` produces a
    `skipped` job record, and citing the six such jobs observed on run `30390832059`.

    Leave the AFTER slots as explicit `pending` rows with their capture commands filled in; plan 09
    fills them.
  </action>
  <verify>
    <automated>test -f .planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md && grep -q '30390832059' .planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md && grep -q '30389700235' .planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md && grep -c 'AFTER-' .planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `230-EVIDENCE.md` exists and contains the literal run IDs `30390832059` and `30389700235`.
    - It contains five rows whose slot names begin with `AFTER-` (AFTER-PR, AFTER-NONPR, AFTER-PUSH, AFTER-DOCSONLY, AFTER-CANCEL), each marked `pending` with a capture command.
    - The BEFORE-PR block contains a line for `Admin eval render + probe (evidence only, not a merge gate)` with `1053` seconds.
    - The BEFORE-PUSH block contains a line for `Example Playwright smoke (full lifecycle)` with a duration above 1500 seconds.
    - The restated SC-2 section states both halves: `conclusion == "skipped"` with duration under 5s on a PR run, and a non-skipped conclusion with a real duration on a non-PR run.
    - Every fenced evidence block is immediately preceded by the verbatim command line that produced it.
  </acceptance_criteria>
  <reversibility rating="reversible">A new planning document; no runtime effect.</reversibility>
  <done>The evidence ledger exists with both BEFORE slots captured by the committed instrument, and the four AFTER slots enumerated as explicitly pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions context → `run:` shell | Workflow context strings (branch, actor, ref, run id) can reach a shell if inlined into `${{ }}` inside `run:` |
| `gh` CLI → local shell | The measurement script executes an external binary resolved through PATH and parses its JSON output |
| Third-party action → runner | Any `uses:` in `ci.yml` executes third-party code with the runner's token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-230-01-01 | Tampering | `fast_checks` new step `CI run metrics self-test` | medium | mitigate | The step is a bare `bash scripts/ci/…test.sh` with no `${{ }}` interpolation into the shell; it introduces no new `uses:` and no new `permissions:` |
| T-230-01-02 | Information disclosure | `scripts/ci/ci-run-metrics.sh` | high | mitigate | Script never echoes `GH_TOKEN` or any secret; it reads only `gh run list`/`gh run view` public run metadata. Header carries an explicit Security note per the `notify-failure-issue.sh:14-18` convention |
| T-230-01-03 | Elevation of privilege | Workflow `permissions:` | high | mitigate | Workflow default stays `contents: read` (`ci.yml:31-32`); this plan adds no job and no job-level `permissions:` override |
| T-230-01-04 | Spoofing | `gh` binary resolution in the self-test | low | accept | PATH shadowing is deliberate and is the hermeticity mechanism; the self-test runs only in CI and locally, never against a real token |
| T-230-01-05 | Repudiation | Evidence ledger | high | mitigate | Every evidence block is preceded by the verbatim command and carries a run ID, so any claim is independently re-fetchable |
| T-230-01-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package in any ecosystem. RESEARCH § Package Legitimacy Audit records "not applicable" for the whole phase; no `mix.exs`, `mix.lock`, `package.json` or `package-lock.json` change is in scope |
</threat_model>

## Artifacts this phase produces

New symbols created by Phase 230 (all plans). Newly-created symbols are excluded from drift
verification against the pre-phase tree.

- **Scripts:** `scripts/ci/ci-run-metrics.sh`, `scripts/ci/ci-run-metrics.test.sh`,
  `scripts/ci/playwright-cache-key-guard.sh`, `scripts/ci/playwright-cache-key-guard.test.sh`
- **CLI flags on `ci-run-metrics.sh`:** `--repo`, `--workflow`, `--limit`, `--since`, `--event`,
  `--mode` (`wall`|`jobspan`), `--format` (`table`|`json`), `--jobs`
- **ExUnit modules:** `Sigra.Planning.Phase230DesignGallerySplitTest`
  (`test/sigra/planning/phase_230_design_gallery_split_test.exs`),
  `Sigra.Planning.Phase230CiTimeoutsTest` (`test/sigra/planning/phase_230_ci_timeouts_test.exs`)
- **New CI job id:** `changes` (job `name:` `Detect docs-only change`)
- **New CI job output:** `changes.outputs.docs_only`
- **New CI step ids:** `design_gallery_snapshots`, `playwright_browsers_cache`, `detect` (inside `changes`)
- **New CI step names:** `Run design gallery board snapshots (non-PR)`, `Cache Playwright browsers`,
  `CI run metrics self-test`, `Playwright cache key guard`, `Playwright cache key guard self-test`
- **New cache key:** `${{ runner.os }}-playwright-chromium-webkit-1.59.1-v1`
  (restore-keys prefix `${{ runner.os }}-playwright-chromium-webkit-`)
- **New Playwright tag:** `@snapshot`
- **New Playwright test title:** `axe: full-page WCAG 2.1/2.2 AA on the design gallery`
- **New documents:** `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md`;
  `MAINTAINING.md` section `### Honest-skip set after Phase 230 (v1.47 FAST-02/FAST-03/FAST-05)`
- **New git tags:** none

<verification>
- `bash scripts/ci/ci-run-metrics.test.sh` exits 0 with zero network access.
- `bash scripts/ci/ci-run-metrics.sh --jobs 30390832059` reproduces the pre-change per-job table.
- `bash scripts/ci/ci-run-metrics.sh --limit 40 --format table` reproduces the REQUIREMENTS.md
  column shape; the `pull_request` and `push` `max` values match 41.7m and 42.3m.
- `actionlint -shellcheck= .github/workflows/ci.yml` exits 0.
- `mix test test/sigra/planning/` matches the pre-change baseline (Pitfall 5 — capture it before the
  first edit so a pre-existing red is not misattributed to this phase).
</verification>

<success_criteria>
- The measurement script and its hermetic self-test are committed **before** any other plan measures
  anything (D-21 ordering constraint satisfied).
- `fast_checks` executes the self-test on every PR and push.
- `230-EVIDENCE.md` carries BEFORE-PR and BEFORE-PUSH with verbatim run IDs and commands, and lists
  the four AFTER slots as explicitly pending.
</success_criteria>

<output>
Create `.planning/phases/230-tier-1-critical-path-reclamation/230-01-SUMMARY.md` when done
</output>
