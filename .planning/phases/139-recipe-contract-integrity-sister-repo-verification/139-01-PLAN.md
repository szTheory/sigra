---
phase: 139
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/recipes/companion_lib_contract_test.exs
autonomous: true
requirements:
  - RCT-01
must_haves:
  truths:
    - "Running `mix test test/sigra/recipes/companion_lib_contract_test.exs` exits 0 (all six recipes currently pass)"
    - "Removing any one of the five required markers from any companion-lib recipe causes the fixture to fail with a message naming the recipe and the missing marker (D-05 / Success Criteria #2)"
    - "The non-empty-glob guard test fails independently when the glob path is wrong or the directory is empty — 'empty glob' and 'marker missing' are distinct failures"
    - "The fixture is merge-blocking with no CI tag or separate job (runs in the standard suite; CLAUDE.md has no tag exclusions)"
  artifacts:
    - path: "test/sigra/recipes/companion_lib_contract_test.exs"
      provides: "Recipe contract fixture (RCT-01)"
      min_lines: 30
      exports: []
  key_links:
    - from: "test/sigra/recipes/companion_lib_contract_test.exs"
      to: "guides/recipes/companion-libs/*.md"
      via: "Path.wildcard via root() helper"
      pattern: "guides/recipes/companion-libs"
---

<objective>
Create `test/sigra/recipes/companion_lib_contract_test.exs` — a pure-ExUnit merge-blocking fixture that asserts every companion-lib recipe under `guides/recipes/companion-libs/` carries all five required contract markers (per D-02), plus a standalone non-empty-glob guard (D-05) so the fixture cannot pass vacuously.

Purpose: RCT-01 — companion-lib recipe docs cannot silently drift out of their required shape; the fixture is the merge gate.
Output: One new test file. All six existing recipes already carry all five markers (confirmed in RESEARCH.md), so the fixture will be green on first run. The negative-test verification (removing a marker → assert red → revert) proves Success Criteria #2 without shipping a permanently failing test.
</objective>

<execution_context>
@/Users/jon/projects/sigra/.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-PATTERNS.md
@/Users/jon/projects/sigra/.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-VALIDATION.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create companion-lib recipe contract fixture (RCT-01)</name>
  <files>test/sigra/recipes/companion_lib_contract_test.exs</files>

  <read_first>
    Read these files before writing a single line:
    1. `test/sigra/planning/phase_50_nyquist_docs_contract_test.exs` — primary analog; copy the module header shape, `root()`/`read!()` helper, and flat `for`-loop-inside-test assertion style.
    2. `test/sigra/guides_dx02_test.exs` (first 50 lines) — secondary analog; copy the `Path.wildcard/1` + `Path.join/2` glob idiom and per-file `File.read!` pattern.
    3. `guides/recipes/companion-libs/accrue.md` (lines 1-10) — canonical marker format; confirm the exact strings for `validated_against:`, `last_validated:`, and `Sigra works fully standalone.`.
    4. `PATTERNS.md` ("Complete recommended shape for the new file" section) — the full recommended module, already adapted for the two-`..` path depth.
  </read_first>

  <behavior>
    - The fixture lives at `test/sigra/recipes/companion_lib_contract_test.exs`, two levels deep from repo root: `Path.expand("../..", __DIR__)` resolves to repo root (NOT three `..` like the phase-50 analog which lives one level deeper).
    - Test 1 (non-empty-glob guard, D-05): standalone test asserting `recipe_files() != []` with a descriptive failure message naming the glob pattern. Fails independently from marker assertions.
    - Test 2 (five-marker sweep): a single test block that calls `recipe_files()`, asserts `files != []` (second guard), then iterates `for path <- files` and `for {marker, label} <- @required_markers` — each failed assertion names the recipe file and the missing marker string.
    - Module attribute `@required_markers` holds exactly five tuples per D-02: `{"## Failure modes", "## Failure modes section"}`, `{"## Non-goals", "## Non-goals section"}`, `{"Sigra works fully standalone.", "standalone banner"}`, `{"validated_against:", "validated_against: frontmatter"}`, `{"last_validated:", "last_validated: frontmatter"}`.
    - Do NOT add `{:sigra, "~> 0.2"}` version-pin assertion or date-parsing of `last_validated:` (D-03 — declined).
    - Do NOT use a describe block or compile-time for loop — follow phase-50 style: flat `test` blocks with runtime iteration.
    - `use ExUnit.Case, async: true` — no DB, no tags, no setup, no Oban.
    - Module name mirrors file path: `Sigra.Recipes.CompanionLibContractTest`.
  </behavior>

  <action>
    Create `test/sigra/recipes/companion_lib_contract_test.exs`. The `test/sigra/recipes/` subdirectory is new; create it implicitly by writing the file at that path.

    Shape (derive exact text from PATTERNS.md "Complete recommended shape" excerpt — do not invent; copy verbatim and verify path arithmetic):
    - `defmodule Sigra.Recipes.CompanionLibContractTest`
    - `@moduledoc` stating RCT-01 purpose, D-05 guard, and "Fails loudly on missing markers AND on empty glob"
    - `use ExUnit.Case, async: true`
    - `@recipes_glob "guides/recipes/companion-libs/*.md"` (module attribute used in both the helper and failure messages)
    - `@required_markers` keyword list with the exact five pairs from D-02
    - `defp root, do: Path.expand("../..", __DIR__)` — TWO `..`, not three
    - `defp recipe_files, do: root() |> Path.join(@recipes_glob) |> Path.wildcard()`
    - Test 1: `"companion-libs glob is non-empty (D-05 guard)"` — asserts `recipe_files() != []` with message `"#{@recipes_glob} matched no files — directory missing or glob wrong"`
    - Test 2: `"each companion-lib recipe carries all five required contract markers"` — asserts `files != []`, then nested `for` with `assert String.contains?(content, marker), "#{name}: missing #{label} (#{inspect(marker)})"`

    Negative-test verification (MANDATORY, per VALIDATION.md and Success Criteria #2):
    After the fixture file is written and `mix test test/sigra/recipes/companion_lib_contract_test.exs` is green:
    1. Temporarily remove the string `## Failure modes` from `guides/recipes/companion-libs/accrue.md` (or any recipe of choice).
    2. Run `mix test test/sigra/recipes/companion_lib_contract_test.exs`.
    3. Assert the output contains a failure message naming `accrue.md` and `## Failure modes section`.
    4. Revert the recipe file to its original state (do NOT commit the temporary change).
    5. Run the fixture again — must be green before committing.
    Record the observed failure message in the task evidence (SUMMARY.md section).

    Do NOT place fenced code blocks in this action section — the full shape is in PATTERNS.md.
  </action>

  <verify>
    <automated>mix test test/sigra/recipes/companion_lib_contract_test.exs --seed 0</automated>
  </verify>

  <acceptance_criteria>
    - `test/sigra/recipes/companion_lib_contract_test.exs` exists
    - `mix test test/sigra/recipes/companion_lib_contract_test.exs` exits 0 with all tests passing
    - The fixture contains exactly two tests: the D-05 glob guard and the five-marker sweep
    - `Path.expand("../..", __DIR__)` is the root helper (two dots, not three — confirmed by running the fixture and observing no "no such file" errors)
    - `@required_markers` contains exactly the five marker pairs from D-02: `"## Failure modes"`, `"## Non-goals"`, `"Sigra works fully standalone."`, `"validated_against:"`, `"last_validated:"`
    - No `{:sigra, "~> 0.2"}` version-pin assertion (D-03)
    - Negative-test verification completed: a fixture failure was observed when a marker was temporarily removed; the failure message named the recipe file and the missing marker label; the recipe was reverted; the fixture is green again
    - `mix test` (full suite) exits 0 after the new file is committed
  </acceptance_criteria>

  <done>
    `test/sigra/recipes/companion_lib_contract_test.exs` is committed, `mix test test/sigra/recipes/companion_lib_contract_test.exs` is green, negative-test verification is documented in the SUMMARY, and the full suite (`mix test`) is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan has no trust boundaries. The fixture is a pure ExUnit test that reads files from the local filesystem. No auth logic, no token handling, no HTTP endpoints, no data processing, no user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01-NA | N/A | ExUnit file-read fixture | accept | No STRIDE-applicable threats. Changes are a test file that reads Markdown from the local repo tree. ASVS L1 categories do not apply. RESEARCH.md Security Domain confirms: "Not applicable." |

No blocking threats. No slopcheck required (no package installs).
</threat_model>

<verification>
1. `mix test test/sigra/recipes/companion_lib_contract_test.exs` exits 0 (six recipes × five markers = 30 assertions, all green)
2. Negative-test evidence documented in SUMMARY: fixture failed with a named-recipe + named-marker message when a marker was temporarily removed
3. `mix test` (full suite with live Postgres) exits 0
</verification>

<success_criteria>
- The RCT-01 fixture exists at `test/sigra/recipes/companion_lib_contract_test.exs` and passes green
- Removing any one of the five required markers from any companion-lib recipe makes the fixture fail loudly (drift caught at merge time — Success Criteria #2)
- The D-05 non-empty-glob guard test is a standalone assertion that fails independently from the marker sweep
- The full suite is green; no CI wiring changes needed (CLAUDE.md: no tag exclusions)
</success_criteria>

<output>
Create `.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-01-SUMMARY.md` when done.
</output>
