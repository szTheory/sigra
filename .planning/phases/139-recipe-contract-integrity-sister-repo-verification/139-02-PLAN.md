---
phase: 139
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/companion-libs/lockspire.md
  - guides/recipes/companion-libs/rulestead.md
  - .planning/todos/pending/2026-05-28-phase-134-recipe-residual-findings.md
autonomous: true
requirements:
  - RCV-01
  - RCV-02
must_haves:
  truths:
    - "lockspire.md resolve_account/2 example returns {:ok, account} | {:error, :not_found} — not bare user-or-nil (RCV-01 / D-08)"
    - "rulestead.md policy example declares @behaviour Rulestead.Admin.Policy and @impl true on can?/4 (RCV-02 / D-12)"
    - "rulestead.md prose attributes the behaviour to policy.ex:121, not authorizer.ex; notes authorizer.ex:149 as the dispatch site (D-11)"
    - "rulestead.md includes a short prose note that change_request_required?/4 and allow_self_approval?/4 are optional callbacks (D-13 resolved)"
    - "Both recipes cite the verified sister-repo ref and have last_validated: updated to 2026-05-29 (D-15/D-16); rulestead.md cites the verified rulestead v0.1.3 (`0a18360`) ref and WR-05 is corrected in the todo with the right module name (D-14)"
    - "mix docs --warnings-as-errors exits 0 after both recipe edits"
    - "The folded todo 2026-05-28-phase-134-recipe-residual-findings.md is marked resolved with WR-02/WR-05/IN-01 dispositions (D-17)"
    - "The RCT-01 fixture (139-01 plan) still passes after these edits — edited recipes keep all five required markers"
  artifacts:
    - path: "guides/recipes/companion-libs/lockspire.md"
      provides: "Fixed Lockspire resolve_account/2 example (correct return shape)"
      contains: "{:ok, account}"
    - path: "guides/recipes/companion-libs/rulestead.md"
      provides: "Fixed Rulestead policy example (@behaviour + @impl + optional-callback note)"
      contains: "@behaviour Rulestead.Admin.Policy"
    - path: ".planning/todos/pending/2026-05-28-phase-134-recipe-residual-findings.md"
      provides: "Resolved todo (WR-02/WR-05/IN-01 all closed)"
      contains: "resolved"
  key_links:
    - from: "guides/recipes/companion-libs/lockspire.md"
      to: "/Users/jon/projects/lockspire/lib/lockspire/host/account_resolver.ex:17-18"
      via: "verified callback contract"
      pattern: "def616d"
    - from: "guides/recipes/companion-libs/rulestead.md"
      to: "/Users/jon/projects/rulestead/rulestead/lib/rulestead/admin/policy.ex:121"
      via: "verified @callback can?/4"
      pattern: "0a18360"
---

<objective>
Fix two confirmed recipe contract bugs (RCV-01/RCV-02) uncovered by verifying the sister-repo sources in-tree, and close the folded Phase-134 todo (D-17).

- RCV-01: `guides/recipes/companion-libs/lockspire.md` line 93 returns a bare `user-or-nil` from `get_user/1`, causing a MatchError in Lockspire's `with {:ok, account} <- resolver.resolve_account(...)` dispatch at `token_exchange.ex:1223` and `userinfo.ex:147`. Fix: wrap in `case` returning `{:ok, user}` / `{:error, :not_found}`. Cite verified ref (Lockspire v1.2.0 `def616d`).
- RCV-02: `guides/recipes/companion-libs/rulestead.md` policy example lacks `@behaviour Rulestead.Admin.Policy` and `@impl true`; prose incorrectly attributes the behaviour to `authorizer.ex` (it is the dispatch site). The real behaviour is `Rulestead.Admin.Policy` at `policy.ex:121`. Fix: add `@behaviour`/`@impl`, correct prose, note optional callbacks (`change_request_required?/4`, `allow_self_approval?/4` at `policy.ex:142`). Cite verified ref (Rulestead v0.1.3 `0a18360`).
- D-17: Fold and close `2026-05-28-phase-134-recipe-residual-findings.md` — WR-02 resolved, WR-05 resolved, IN-01 already resolved (mark done).

Purpose: RCV-01/RCV-02 close the verified sister-repo contracts; D-17 closes the tracked todo.
Output: Two corrected recipe files and one marked-resolved todo file.
</objective>

<execution_context>
@/Users/jon/projects/sigra/.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-PATTERNS.md
@/Users/jon/projects/sigra/.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-RESEARCH.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lockspire.md resolve_account/2 return shape (RCV-01 / D-06..D-09)</name>
  <files>guides/recipes/companion-libs/lockspire.md</files>

  <read_first>
    Read ALL of these before editing:
    1. `guides/recipes/companion-libs/lockspire.md` — full file; understand the structure before making any change. Note lines 1-2 (`last_validated:` and `validated_against:`), line 5 (prose validation line), and lines 92-95 (the buggy `resolve_account/2` implementation block).
    2. `/Users/jon/projects/lockspire/lib/lockspire/host/account_resolver.ex` lines 17-18 — the canonical `@callback resolve_account/2` contract: `@callback resolve_account(account_reference :: term(), context()) :: {:ok, account()} | {:error, :not_found | term()}`. Copy the exact return shape.
    3. `/Users/jon/projects/lockspire/lib/lockspire/protocol/token_exchange.ex` line 1223 — confirms the `with {:ok, account} <- resolver.resolve_account(...)` dispatch that breaks with a bare user-or-nil return.
    4. PATTERNS.md "lockspire.md (edit, documentation)" section — contains the exact current-vs-fixed excerpt with the `case` expression.
    5. RESEARCH.md "RCV-01 Recipe Fix: Lockspire `resolve_account/2`" section — contains the citation format and which lines need updating.
  </read_first>

  <action>
    Make exactly three changes to `guides/recipes/companion-libs/lockspire.md`:

    CHANGE 1 — Line 2 (`last_validated:`): Update the HTML-comment date from `2026-05-28` to `2026-05-29`.
    Current: `<!-- last_validated: 2026-05-28 -->`
    New: `<!-- last_validated: 2026-05-29 -->`

    CHANGE 2 — Line 5 (prose validation line): Add the git ref to the citation.
    Current: `Validated against: \`lockspire ~> 1.2\` as of 2026-05-28`
    New: `Validated against: \`lockspire ~> 1.2\` (\`def616d\`) as of 2026-05-29`
    (If the exact current wording differs from above, match the existing wording style but add `(ref)` and update the date — do NOT change other prose on that line.)

    CHANGE 3 — Lines 92-95 (the `resolve_account/2` implementation): Replace the bare `get_user` call with a `case` expression that wraps the result in the correct tuple shape.
    Current (per D-08 and PATTERNS.md):
      `def resolve_account(account_reference, _context) do`
      `  MyApp.Accounts.get_user(account_reference)`
      `end`
    New (per D-07 contract and PATTERNS.md replacement excerpt):
      `def resolve_account(account_reference, _context) do`
      `  case MyApp.Accounts.get_user(account_reference) do`
      `    nil -> {:error, :not_found}`
      `    user -> {:ok, user}`
      `  end`
      `end`
    The `@impl Lockspire.Host.AccountResolver` line immediately above stays unchanged.

    Do NOT change: validated_against: line (D-16 — already accurate at `lockspire ~> 1.2`), any other prose, section headings, or code blocks elsewhere in the file.
    Do NOT add `@behaviour Lockspire.Host.AccountResolver` — the recipe module-example structure likely already has `@impl` and the host behaviour wiring is not this task's scope.

    Confirm after editing: the five required RCT-01 markers (`## Failure modes`, `## Non-goals`, `Sigra works fully standalone.`, `validated_against:`, `last_validated:`) are still present in the file.
  </action>

  <verify>
    <automated>mix docs --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `guides/recipes/companion-libs/lockspire.md` line 93 area contains `{:ok, user}` and `{:error, :not_found}` (no bare `MyApp.Accounts.get_user(account_reference)` as the sole return)
    - The `case MyApp.Accounts.get_user(account_reference) do` pattern is present
    - Line 2 reads `<!-- last_validated: 2026-05-29 -->`
    - Line 5 (prose) includes `def616d` and date `2026-05-29`
    - `mix docs --warnings-as-errors` exits 0
    - All five RCT-01 markers remain present in lockspire.md (run `mix test test/sigra/recipes/companion_lib_contract_test.exs` if 139-01 plan has already been executed; otherwise verify by grep)
  </acceptance_criteria>

  <done>
    `lockspire.md` updated with correct `{:ok, account}` / `{:error, :not_found}` return shape, sister-repo citation added, `last_validated:` refreshed, `mix docs --warnings-as-errors` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix rulestead.md @behaviour + @impl + optional-callback note + todo closure (RCV-02 / D-10..D-17)</name>
  <files>
    guides/recipes/companion-libs/rulestead.md
    .planning/todos/pending/2026-05-28-phase-134-recipe-residual-findings.md
  </files>

  <read_first>
    Read ALL of these before editing:
    1. `guides/recipes/companion-libs/rulestead.md` — full file; understand the structure. Note lines 1-2 (frontmatter), line 5 (prose validation), lines 123-153 (the policy section: prose attribution, module example).
    2. `/Users/jon/projects/rulestead/rulestead/lib/rulestead/admin/policy.ex` lines 115-145 — confirms `@callback can?/4` at line 121 and `@optional_callbacks change_request_required?: 4, allow_self_approval?: 4` at line 142. This is the host-facing behaviour module.
    3. `/Users/jon/projects/rulestead/rulestead/lib/rulestead/admin/authorizer.ex` line 149 — confirms the dispatch site (`policy.can?(actor, action, resource, environment_key)`) — this is NOT the behaviour definition.
    4. PATTERNS.md "rulestead.md (edit, documentation)" section — exact prose replacement and module replacement with `@behaviour`/`@impl`/optional-callback comment block.
    5. RESEARCH.md "RCV-02 Recipe Fix: Rulestead @behaviour and @impl" section — contains the wrong-behaviour attribution description, the optional callback context, and the citation format.
    6. `.planning/todos/pending/2026-05-28-phase-134-recipe-residual-findings.md` — full file; read the WR-02, WR-05, IN-01 entries before updating.
  </read_first>

  <action>
    Make exactly four changes to `guides/recipes/companion-libs/rulestead.md`:

    CHANGE 1 — Line 2 (`last_validated:`): Update date from `2026-05-28` to `2026-05-29`.
    Current: `<!-- last_validated: 2026-05-28 -->`
    New: `<!-- last_validated: 2026-05-29 -->`

    CHANGE 2 — Line 5 (prose validation line): Add the git ref and update date.
    Current: `Validated against: \`rulestead ~> 0.1\` as of 2026-05-28`
    New: `Validated against: \`rulestead ~> 0.1\` (\`0a18360\`) as of 2026-05-29`

    CHANGE 3 — Prose attribution (around line 123-124): Correct the wrong behaviour source.
    Current (per PATTERNS.md/RESEARCH.md):
      The prose says the admin UI calls `policy.can?/4` "at `authorizer.ex:146-150`" and implies `Authorizer` is the behaviour source.
    New prose (adapt the exact wording from PATTERNS.md "Prose Fix" excerpt):
      State that the host module implements the `Rulestead.Admin.Policy` behaviour defined at `policy.ex:121` (verified against rulestead v0.1.3 `0a18360` on 2026-05-29), dispatched from `authorizer.ex:149`. The prose must make clear that `policy.ex` defines the behaviour and `authorizer.ex` is the dispatch site — not the reverse.

    CHANGE 4 — Module example (lines ~140-153): Add `@behaviour Rulestead.Admin.Policy`, add `@impl Rulestead.Admin.Policy` before `def can?/4`, and add a commented optional-callback block.
    Current module example (per PATTERNS.md "Current example module"):
      `defmodule MyApp.RulesteadPolicy do`
        `@doc "..."`
        `def can?(%{roles: roles}, action, _resource, _environment_key) do ... end`
      `end`
    New (per PATTERNS.md "Replace with" and RESEARCH.md D-13 resolution):
      `defmodule MyApp.RulesteadPolicy do`
        `@behaviour Rulestead.Admin.Policy` (after `do`, before `@doc`)
        blank line
        `@doc "..."`
        `@impl Rulestead.Admin.Policy` (before `def can?/4`)
        `def can?(%{roles: roles}, action, _resource, _environment_key) do ... end`
        blank line
        commented block noting `change_request_required?/4` and `allow_self_approval?/4` as optional callbacks for governance-action workflows (implement only if using governance actions — publish_ruleset, advance_rollout, etc.; if omitted, Rulestead falls back to internal defaults)
      `end`
    The `@doc` body and `def can?/4` implementation body stay unchanged.

    Do NOT change: `validated_against:` marker (D-16 — already accurate at `rulestead ~> 0.1`), any other prose or section headings, any sections outside the policy section.

    After editing rulestead.md, update `.planning/todos/pending/2026-05-28-phase-134-recipe-residual-findings.md` (D-17):
    - Add a header or section `## Resolution (Phase 139, 2026-05-29)` at the top or bottom of the file.
    - Document each finding's disposition:
      - WR-02: resolved — Lockspire `resolve_account/2` contract confirmed against v1.2.0 `def616d`; recipe fixed in RCV-01 (139-02 plan, task 1)
      - WR-05: resolved — Rulestead behaviour is `Rulestead.Admin.Policy` at `policy.ex:121`, NOT `Admin.Authorizer`; module corrected in WR-05 note; recipe fixed in RCV-02 (139-02 plan, task 2)
      - IN-01: already resolved — all six recipes now pin `{:sigra, "~> 0.2"}` (fixed in quick task 260528-sbn); no sweep needed; marked done
    - Move the file to `.planning/todos/resolved/2026-05-28-phase-134-recipe-residual-findings.md` OR add a `status: resolved` / `resolved: 2026-05-29` field to the frontmatter — use whichever pattern exists in the todos directory. Check for a `resolved/` sibling directory first.

    Confirm after editing: the five required RCT-01 markers are still present in rulestead.md.
  </action>

  <verify>
    <automated>mix docs --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `guides/recipes/companion-libs/rulestead.md` contains the string `@behaviour Rulestead.Admin.Policy`
    - `guides/recipes/companion-libs/rulestead.md` contains `@impl Rulestead.Admin.Policy` immediately before `def can?/4`
    - `rulestead.md` prose attributes the behaviour to `policy.ex:121`, NOT to `authorizer.ex` as the behaviour source (authorizer.ex may still be mentioned as the dispatch site)
    - `rulestead.md` contains a prose note about `change_request_required?/4` and `allow_self_approval?/4` as optional callbacks
    - Line 2 of rulestead.md reads `<!-- last_validated: 2026-05-29 -->`
    - Line 5 (prose) includes `0a18360` and date `2026-05-29`
    - `mix docs --warnings-as-errors` exits 0
    - All five RCT-01 markers remain present in rulestead.md (grep or run the fixture if 139-01 is already executed)
    - The todo file `2026-05-28-phase-134-recipe-residual-findings.md` is marked resolved with WR-02/WR-05/IN-01 dispositions documented
  </acceptance_criteria>

  <done>
    `rulestead.md` updated with `@behaviour Rulestead.Admin.Policy`, `@impl true` on `can?/4`, corrected prose attribution, optional-callback comment block, sister-repo citation, and refreshed `last_validated:`. The folded todo is resolved and closed. `mix docs --warnings-as-errors` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan has no trust boundaries. Changes are documentation-only Markdown edits and a todo status update. No auth logic, no token handling, no HTTP endpoints, no data processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-02-NA | N/A | Markdown recipe edits + todo file update | accept | No STRIDE-applicable threats. RESEARCH.md Security Domain confirms: "Not applicable — no auth logic, no token handling, no HTTP endpoints, no data processing." ASVS L1 categories do not apply to documentation fixes. |

No blocking threats. No slopcheck required (no package installs).
</threat_model>

<verification>
1. `mix docs --warnings-as-errors` exits 0 after both recipe edits
2. `grep "@behaviour Rulestead.Admin.Policy" guides/recipes/companion-libs/rulestead.md` returns a match
3. `grep -c "{:ok, user}" guides/recipes/companion-libs/lockspire.md` returns a non-zero count
4. Both recipes contain `last_validated: 2026-05-29`
5. If 139-01 plan has been executed: `mix test test/sigra/recipes/companion_lib_contract_test.exs` exits 0 (edited recipes still carry all five markers)
6. `mix test` (full suite) exits 0
</verification>

<success_criteria>
- RCV-01: `lockspire.md` example returns `{:ok, account}` / `{:error, :not_found}` with a `case` expression; verified ref cited as Lockspire v1.2.0 `def616d`
- RCV-02: `rulestead.md` declares `@behaviour Rulestead.Admin.Policy` and `@impl true` on `can?/4`; prose attributes behaviour to `policy.ex:121`; optional callbacks noted; verified ref cited as Rulestead v0.1.3 `0a18360`
- D-17: `2026-05-28-phase-134-recipe-residual-findings.md` resolved — WR-02 fixed, WR-05 fixed (with corrected module name), IN-01 confirmed already done
- `mix docs --warnings-as-errors` exits 0
- Full suite (`mix test`) exits 0
</success_criteria>

<output>
Create `.planning/phases/139-recipe-contract-integrity-sister-repo-verification/139-02-SUMMARY.md` when done.
</output>
