---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/components/admin_shell.ex
  - test/sigra/templates/installer_drift_test.exs
  - test/example/test/example_web/admin_shell_test.exs
autonomous: true
requirements:
  - USER-05
tags:
  - generator
  - installer
  - admin
  - template
  - navigation
  - mobile
must_haves:
  truths:
    - "A host app generated with `mix sigra.install` exposes a live `href={users_link(@admin_scope)}` anchor in the top-bar scope-switch, desktop sidebar Operations group, and mobile bottom-nav of the admin shell — matching `test/example/lib/example_web/components/admin_shell.ex`."
    - "The generated admin shell's desktop sidebar renders Operations before Overview (D-11 reorder); the Operations group's first entry is the live Users link."
    - "The generated admin shell's mobile bottom-nav contains a Users entry with the `btm-nav-label` span as its first `<a>`, preserving Global, Organization (conditional), and Audit entries after it."
    - "The previously dead `<li><span class=\"text-base-content/60\">Users</span></li>` is removed from the generator template — `installer_drift_test.exs` fails if it ever reappears on either side (template or example-app source)."
    - "`mix test test/sigra/templates/installer_drift_test.exs` covers the Users nav parity fixture and fails if the generator template and example-app shell drift on `defp users_link(`, `href={users_link(@admin_scope)}`, or `btm-nav-label\">Users<` markers."
  artifacts:
    - path: "priv/templates/sigra.install/admin/components/admin_shell.ex"
      provides: "Generator template for the host-owned admin shell; after Phase 33 exposes live Users navigation (top-bar, desktop sidebar, mobile bottom-nav) plus `users_link/1` and `users_active?/1` private helpers."
      contains: "defp users_link("
      min_lines: 190
    - path: "test/sigra/templates/installer_drift_test.exs"
      provides: "Drift test appended with one new `@fixtures` entry guarding the admin shell Users nav parity (matching Phase 32's fix+guard discipline)."
      contains: "admin_shell users nav"
  key_links:
    - from: "priv/templates/sigra.install/admin/components/admin_shell.ex"
      to: "priv/templates/sigra.install/admin/router_injection.ex (UsersIndexLive mounts emitted by Phase 32)"
      via: "~p\"/admin/users\" route rendered by users_link/1"
      pattern: "~p\"/admin/users\""
    - from: "test/sigra/templates/installer_drift_test.exs"
      to: "test/example/lib/example_web/components/admin_shell.ex"
      via: "side-A/side-B fixture regex parity"
      pattern: "defp users_link\\("
---

<objective>
Phase 33 / Plan 01 — INT-04 Admin Shell Users Navigation Port (generator template + drift guard).

The generated admin shell template at `priv/templates/sigra.install/admin/components/admin_shell.ex` currently renders "Users" as a dead `<span class="text-base-content/60">Users</span>` — there is no live link to the UsersIndexLive route that Phase 32 wired in the router template. A freshly installed host therefore exposes admin Users pages at reachable routes but with no in-shell navigation to them.

This plan ports the Users navigation chrome verbatim from the example-app source of truth (`test/example/lib/example_web/components/admin_shell.ex`) into the generator template:
- New `users_link/1` private helper (global + org-scoped).
- New `users_active?/1` private helper (always-true stub, path-aware logic deferred per D-10).
- New top-bar `<.scope_switch_link>` Users entry, inserted before Global.
- Replacement of the dead `<span>` with a live anchor as the first entry in the Operations sidebar group.
- Reorder desktop sidebar sections so Operations renders before Overview (D-11).
- New mobile bottom-nav Users entry as the first `<a>` in `.btm-nav`.

A new drift fixture in `test/sigra/templates/installer_drift_test.exs` guarantees the template and example never diverge on the Users nav markers (Phase 32 "fix + guard" discipline).

Purpose: close INT-04 from the v1.2 milestone audit so a freshly installed host has a live, mobile-friendly Users navigation matching the example app (USER-05).

Output:
- Updated `admin_shell.ex` generator template with live Users nav (top-bar, sidebar, bottom-nav) + `users_link/1` + `users_active?/1`.
- One new `@fixtures` entry in `installer_drift_test.exs` (test assertion update — not test infra redesign).
- Optional one-line assertion extension in `test/example/test/example_web/admin_shell_test.exs` asserting `html =~ "href=\"/admin/users\""` to make the assertion sensitive to link liveness, not just the word "Users" being present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-admin-shell-navigation-and-audit-preview-polish/33-CONTEXT.md
@.planning/phases/33-admin-shell-navigation-and-audit-preview-polish/33-PATTERNS.md

# Verbatim source for the port
@test/example/lib/example_web/components/admin_shell.ex

# Destination generator template (currently broken)
@priv/templates/sigra.install/admin/components/admin_shell.ex

# Drift-guard harness to extend
@test/sigra/templates/installer_drift_test.exs

# Optional assertion extension target
@test/example/test/example_web/admin_shell_test.exs

<interfaces>
<!-- Helper signatures and component signatures the executor relies on. -->
<!-- Extracted verbatim from the example-app source of truth. -->

From test/example/lib/example_web/components/admin_shell.ex (the verbatim port source):

```elixir
# Private helpers to port (example lines 191-194, 201):
defp users_link(%{mode: :organization, organization_slug: slug}) when is_binary(slug),
  do: ~p"/admin/organizations/#{slug}/users"

defp users_link(_admin_scope), do: ~p"/admin/users"

defp users_active?(_admin_scope), do: true

# Existing scope_switch_link (both files share this component - no change):
attr :href, :string, required: true
attr :active, :boolean, default: false
slot :inner_block, required: true
defp scope_switch_link(assigns)

# Existing nav_item_class/1 and bottom_nav_class/1 (both files share - no change):
defp nav_item_class(true), do: "active rounded-md"
defp nav_item_class(false), do: "rounded-md"
defp bottom_nav_class(true), do: "active"
defp bottom_nav_class(false), do: ""
```

The `@admin_scope` assign is already declared at the top of `admin_shell/1`. No new `attr :...` declarations, no new EEx bindings (`<%= web_module %>`), no new assigns required (D-13).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Users navigation chrome into admin_shell generator template</name>
  <files>priv/templates/sigra.install/admin/components/admin_shell.ex</files>
  <read_first>
    - priv/templates/sigra.install/admin/components/admin_shell.ex (CURRENT STATE — the file being modified; read in full before editing)
    - test/example/lib/example_web/components/admin_shell.ex (VERBATIM SOURCE — port from this file; read in full)
    - .planning/phases/33-admin-shell-navigation-and-audit-preview-polish/33-CONTEXT.md (user decisions D-10 through D-13, D-18; Deferred Ideas)
    - .planning/phases/33-admin-shell-navigation-and-audit-preview-polish/33-PATTERNS.md (§ "priv/templates/sigra.install/admin/components/admin_shell.ex" — exact diff targets + example-line numbers)
  </read_first>
  <action>
    Edit `priv/templates/sigra.install/admin/components/admin_shell.ex` to land four verbatim ports + one structural reorder from the example (`test/example/lib/example_web/components/admin_shell.ex`). Do NOT touch `audit_link/1`, the `~p` vs literal-string mix, or any other existing helper (D-12). No new EEx bindings or assigns (D-13).

    **Change 1 — Top-bar scope-switch Users entry (per D-10 bullet 2 and PATTERNS.md).**
    In the top-bar `<div class="flex items-center gap-2">` block (currently template lines 23-38), insert this `<.scope_switch_link>` **before** the existing Global `<.scope_switch_link>`:
    ```elixir
    <.scope_switch_link href={users_link(@admin_scope)} active={users_active?(@admin_scope)}>
      Users
    </.scope_switch_link>
    ```
    This Users link is unguarded (no `:if={...}` attribute) — matches example line 24. The existing Global and Organization `<.scope_switch_link>` entries are untouched.

    **Change 2 — Desktop sidebar: swap section ordering (D-11) and replace dead `<span>` with live link (D-10 bullet 3).**
    The template currently emits `<div class="rounded-lg bg-base-200 p-3">` Overview (template lines 47-62) then Operations (template lines 64-74). Swap these two blocks so Operations renders **first**, Overview second — exactly matching the example (example lines 50-84).

    Inside the Operations block (after the reorder), replace the current dead entry:
    ```elixir
    <li><span class="text-base-content/60">Users</span></li>
    ```
    with the live anchor (as the **first** `<li>` in the Operations `<ul>`, above the existing Audit `<li>`):
    ```elixir
    <li>
      <a
        class={nav_item_class(users_active?(@admin_scope))}
        href={users_link(@admin_scope)}
      >
        Users
      </a>
    </li>
    ```
    The existing Audit `<li>` stays exactly as it was. Overview block contents (Global link + conditional Organization link) stay exactly as they were — only the block position in the `<nav>` changes.

    **Change 3 — Mobile bottom-nav Users entry (D-10 bullet 4).**
    In the `<nav aria-label="Admin bottom nav" class="btm-nav ...">` block (currently template lines 83-100), insert this `<a>` as the **first** entry (above the existing Global `<a>`):
    ```elixir
    <a
      href={users_link(@admin_scope)}
      class={bottom_nav_class(users_active?(@admin_scope))}
    >
      <span class="btm-nav-label">Users</span>
    </a>
    ```
    The existing Global, Organization (conditional), and Audit `<a>` entries stay exactly as they were. Final `.btm-nav` children order: Users → Global → Organization (conditional) → Audit — matches example lines 96-115.

    **Change 4 — `users_link/1` private helper (D-10 bullet 1).**
    Place adjacent to the existing `audit_link/1` helper (currently template lines 165-168). Matching example helper ordering, insert these clauses **before** `audit_link/1`:
    ```elixir
    defp users_link(%{mode: :organization, organization_slug: slug}) when is_binary(slug),
      do: ~p"/admin/organizations/#{slug}/users"

    defp users_link(_admin_scope), do: ~p"/admin/users"
    ```
    (Note: `users_link/1` uses `~p` sigil routes — matches example lines 191-194 and is consistent with `organization_link/1` above it. The fact that `audit_link/1` uses literal strings is an existing inconsistency and stays unchanged per D-12.)

    **Change 5 — `users_active?/1` private helper (D-10 bullet 5).**
    Place near the other `*_active?/1` clauses (currently template lines 170-174 for `global_active?/1`, `organization_active?/1`). Insert verbatim from example line 201:
    ```elixir
    defp users_active?(_admin_scope), do: true
    ```
    This always-true stub is intentional — path-aware active state is explicitly deferred (D-10 last bullet; Deferred Ideas).

    **Do NOT change:**
    - `audit_link/1` (literal string vs `~p` inconsistency stays — D-12).
    - `organization_link/1`, `global_active?/1`, `organization_active?/1`, `scope_switch_link/1`, `scope_label/1`, `scope_chip_class/1`, `impersonating?/1`, `user_label/1`, `show_global_link?/1`, `nav_item_class/1`, `bottom_nav_class/1`, `impersonation_banner/1`.
    - The `<%= web_module %>` EEx binding or any of the template's existing parameterization surface.
    - Any class names, spacing, or HTML structure outside the listed insertion/replacement points.

    After editing, run `mix compile --warnings-as-errors` to catch any mis-pasted syntax. The template is EEx; it does NOT compile as plain Elixir (the `<%= web_module %>` substitution is required first), so a direct `mix compile` will not touch this file. Verify the file parses by reading it back and confirming:
    - `defp users_link(` appears once with the two-clause signature above.
    - `defp users_active?(_admin_scope), do: true` appears once.
    - `<li><span class="text-base-content/60">Users</span></li>` is ABSENT (regression marker).
    - `href={users_link(@admin_scope)}` appears at least three times (top-bar, sidebar, bottom-nav).
    - `btm-nav-label">Users<` appears exactly once.
    - The Operations `<div class="rounded-lg bg-base-200 p-3">` block precedes the Overview block in the `<nav aria-label="Admin navigation">`.
  </action>
  <verify>
    <automated>bash -lc 'grep -qE "^\s*defp users_link\(" priv/templates/sigra.install/admin/components/admin_shell.ex && grep -qE "^\s*defp users_active\?\(" priv/templates/sigra.install/admin/components/admin_shell.ex && ! grep -qE "<li><span class=\"text-base-content/60\">Users</span></li>" priv/templates/sigra.install/admin/components/admin_shell.ex && [ "$(grep -c "href={users_link(@admin_scope)}" priv/templates/sigra.install/admin/components/admin_shell.ex)" -ge 3 ] && grep -q "btm-nav-label\">Users<" priv/templates/sigra.install/admin/components/admin_shell.ex'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^\s*defp users_link\(' priv/templates/sigra.install/admin/components/admin_shell.ex` returns 0.
    - `grep -qE '^\s*defp users_active\?\(' priv/templates/sigra.install/admin/components/admin_shell.ex` returns 0.
    - `grep -c "defp users_link(" priv/templates/sigra.install/admin/components/admin_shell.ex` equals 2 (two clauses: org + fallback).
    - `grep -c "defp users_active?(" priv/templates/sigra.install/admin/components/admin_shell.ex` equals 1.
    - `grep -qE '<li><span class="text-base-content/60">Users</span></li>' priv/templates/sigra.install/admin/components/admin_shell.ex` returns 1 (ABSENT — this pattern must NOT appear).
    - `grep -c 'href={users_link(@admin_scope)}' priv/templates/sigra.install/admin/components/admin_shell.ex` is ≥ 3 (top-bar + sidebar + bottom-nav).
    - `grep -c 'btm-nav-label">Users<' priv/templates/sigra.install/admin/components/admin_shell.ex` equals 1.
    - `grep -n '<p class="mb-2 text-xs font-semibold uppercase text-base-content/60">Operations</p>' priv/templates/sigra.install/admin/components/admin_shell.ex` reports a line number **smaller** than `grep -n '<p class="mb-2 text-xs font-semibold uppercase text-base-content/60">Overview</p>' priv/templates/sigra.install/admin/components/admin_shell.ex` (Operations before Overview, D-11).
    - No new `<%= ... %>` EEx bindings were introduced (no new substitution points).
    - No changes to `audit_link/1`, `impersonation_banner/1`, `user_label/1`, `scope_switch_link/1`, or any other helper untouched by D-10.
  </acceptance_criteria>
  <done>
    Generator template exposes live Users navigation in top-bar, desktop sidebar (as first Operations entry), and mobile bottom-nav; sidebar sections ordered Operations-then-Overview; dead `<span>Users</span>` removed; `users_link/1` and `users_active?/1` helpers defined; no other helpers touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append drift fixture guarding admin shell Users nav parity</name>
  <files>test/sigra/templates/installer_drift_test.exs, test/example/test/example_web/admin_shell_test.exs</files>
  <read_first>
    - test/sigra/templates/installer_drift_test.exs (CURRENT STATE — read in full to match existing fixture shape + comment rhythm)
    - test/example/test/example_web/admin_shell_test.exs (CURRENT STATE — to optionally extend)
    - .planning/phases/33-admin-shell-navigation-and-audit-preview-polish/33-CONTEXT.md (D-14, D-15, D-16, D-17, D-18, Claude's Discretion)
    - .planning/phases/33-admin-shell-navigation-and-audit-preview-polish/33-PATTERNS.md (§ "test/sigra/templates/installer_drift_test.exs" — exact fixture shape)
    - priv/templates/sigra.install/admin/components/admin_shell.ex (post-Task-1 state — fixture regex must match)
    - test/example/lib/example_web/components/admin_shell.ex (fixture regex must match this side too)
  </read_first>
  <action>
    Two edits, both test-side. Neither changes production code.

    **Edit 1 — Append the Users nav drift fixture to `test/sigra/templates/installer_drift_test.exs`.**

    Locate the `@fixtures [` list (currently starts at line 29). Append one new `%{...}` entry **after** the existing `"fix #17 — auth.ex passes :user_token_schema..."` entry (currently ending at line 265 with a `}` closing the `%{}`). The new entry must match the existing file's comment-on-top style, `~r/.../` regex verbosity, and `must_have` / `must_not` rhythm (D-14, D-15, D-16). Insert verbatim:

    ```elixir
    %{
      # Phase 33 — INT-04 fix + guard: the generator's admin shell template must
      # expose a live Users link (desktop sidebar + top-bar + mobile bottom-nav)
      # matching the example-app shell. Catches the "dead <span>Users</span>"
      # class of navigation drift (WCAG SC 1.3.1).
      id: "fix #18 — admin_shell users nav + mobile bottom-nav",
      template: "priv/templates/sigra.install/admin/components/admin_shell.ex",
      example: "test/example/lib/example_web/components/admin_shell.ex",
      must_have: [
        {"users_link/1 helper defined",
         ~r/defp users_link\(/,
         ~r/defp users_link\(/},
        {"at least one href={users_link(@admin_scope)} usage",
         ~r/href=\{users_link\(@admin_scope\)\}/,
         ~r/href=\{users_link\(@admin_scope\)\}/},
        {"mobile bottom-nav Users label present",
         ~r/btm-nav-label">Users</,
         ~r/btm-nav-label">Users</}
      ],
      must_not: [
        {"dead <span>Users</span> navigation item absent",
         ~r/<li>\s*<span[^>]*>Users<\/span>\s*<\/li>/,
         ~r/<li>\s*<span[^>]*>Users<\/span>\s*<\/li>/}
      ]
    }
    ```

    The closing `]` of `@fixtures` follows this new entry as before (remember to add a comma after the previous `%{}` entry so the list is well-formed). Do NOT introduce any new test infrastructure (D-17) — this is a pure declarative fixture append. Do NOT add a template-rendering harness, EEx evaluation, or verified-routes stub (Phase 35 scope).

    **Edit 2 (Claude's Discretion — one-line assertion extension) — `test/example/test/example_web/admin_shell_test.exs`.**

    The existing "renders Admin and Global for the global admin shell" test at lines 13-34 asserts `html =~ "Users"` (line 31), which passed BEFORE Phase 33 because the dead `<span>Users</span>` contained the literal word. To make the assertion sensitive to link liveness (not just word presence), add one line **after** the existing `assert html =~ "Users"` at line 31:

    ```elixir
    assert html =~ "href=\"/admin/users\""
    ```

    This mirrors the existing `assert html =~ "href=\"/admin/audit\""` assertion on line 33. Keep the new assertion adjacent to the existing `"Users"` assertion (not at the end of the test) so the test reads coherently. Do NOT extend the other test(s) in this file — the organization-scope test at lines 36-65 already asserts against `html =~ "Organization"` and `href=\"/admin/organizations/#{slug}/audit\"`; the optional parity for `href=\"/admin/organizations/#{slug}/users\"` is a nice-to-have but NOT required by this phase (planner chose minimal scope per D-18 and Claude's Discretion).

    After both edits, run both tests and confirm they pass:
    ```
    mix test test/sigra/templates/installer_drift_test.exs
    cd test/example && mix test test/example_web/admin_shell_test.exs
    ```
    Both require live Postgres on localhost:5432 (CLAUDE.md local-dev-prereqs). If either test was already passing before Task 1 on the example-app side due to the dead `<span>` containing "Users", Task 1 must have landed first — verify by checking the `priv/templates/` file for the absence of the dead `<span>` marker (dependency on Task 1).
  </action>
  <verify>
    <automated>bash -lc 'grep -q "fix #18 — admin_shell users nav" test/sigra/templates/installer_drift_test.exs && grep -q "btm-nav-label\">Users<" test/sigra/templates/installer_drift_test.exs && grep -q "href=\\\\\"/admin/users\\\\\"" test/example/test/example_web/admin_shell_test.exs && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/templates/installer_drift_test.exs --only describe:"installer template drift"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'fix #18 — admin_shell users nav' test/sigra/templates/installer_drift_test.exs` returns 0 (new fixture entry present).
    - `grep -q 'btm-nav-label">Users<' test/sigra/templates/installer_drift_test.exs` returns 0 (mobile bottom-nav regex present on both sides — template and example columns).
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/templates/installer_drift_test.exs` exits 0 (new fixture passes AND existing 17 fixtures still pass).
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/admin_shell_test.exs` exits 0 (the new `href="/admin/users"` assertion passes against the example-app live shell).
    - No new file created. No new test module created. No new helper, macro, or module attribute introduced.
    - Total `@fixtures` list length in `installer_drift_test.exs` is 18 (was 17).
  </acceptance_criteria>
  <done>
    Drift test catches Users nav divergence between generator template and example-app shell; example-app shell test asserts link liveness (not just label presence); no new test infrastructure created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes sit on the generator-template / test sides of already-established boundaries:
- Generator template → host application at installation time (existing boundary; governed by `Sigra.Install.Features.Admin.files/1`, unchanged).
- Generated admin shell → admin router pipeline (existing boundary; already guarded by `require_admin_plug` per Phase 27).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.01-01 | Information Disclosure | Users nav link to `/admin/users` | accept | The nav link destination is already authz-gated by the admin live_session pipeline (Phase 27 `require_admin_plug` + Phase 28 `Sigra.Admin.Scope` resolution). Rendering a link does not grant access; clicking a link without admin privileges still fails closed server-side at the route boundary. Adding a link exposes no new attack surface. |
| T-33.01-02 | Tampering | Drift fixture bypass | accept | `installer_drift_test.exs` is a CI gate, not a production code path. Bypass requires modifying the test file and the template in a commit that disables the new fixture — covered by code review, not runtime controls. |
| T-33.01-03 | Spoofing / Elevation of Privilege | Users link in org scope resolving to cross-org path | mitigate | `users_link/1` pattern-matches on `%{mode: :organization, organization_slug: slug}` and only ever emits paths containing that exact `slug`. The org-scope route `/admin/organizations/:org_slug/users` is protected server-side by `Sigra.Admin.Scope` resolution (Phase 27 D-06) and `Authorizer.authorize_organization!/2` (Phase 28). A malicious or mis-rendered slug in the link does not bypass server-side scope checks. |

**Summary:** This plan is navigation chrome only. The Users link destinations already exist and are already authz-protected by the admin pipeline. No new threat surface is introduced.
</threat_model>

<verification>
- `mix test test/sigra/templates/installer_drift_test.exs` exits 0 with 18 fixtures covered (17 pre-existing + 1 new Phase 33 entry).
- `cd test/example && mix test test/example_web/admin_shell_test.exs` exits 0, including the new `href="/admin/users"` assertion.
- `grep -c 'href={users_link(@admin_scope)}' priv/templates/sigra.install/admin/components/admin_shell.ex` ≥ 3.
- `grep -q '<li><span class="text-base-content/60">Users</span></li>' priv/templates/sigra.install/admin/components/admin_shell.ex` returns 1 (dead marker absent).
- `grep -n 'Operations</p>' priv/templates/sigra.install/admin/components/admin_shell.ex` precedes `grep -n 'Overview</p>' priv/templates/sigra.install/admin/components/admin_shell.ex` (sidebar section reorder verified).
</verification>

<success_criteria>
- INT-04 closed: generator template exposes live Users nav in top-bar, desktop sidebar (Operations first, as first entry), and mobile bottom-nav, matching the example-app shell exactly.
- Drift fixture added (17→18 fixtures) guaranteeing future divergence is caught at test time.
- No new EEx bindings, no new helpers beyond `users_link/1` + `users_active?/1`, no unrelated refactors (audit_link/1 inconsistency stays per D-12).
- Phase 35's generalized dead-text detector, template-render harness, and a11y baselines remain out of scope (D-17).
- Phase 34's generated-host browser smoke for the Users nav remains out of scope (D-18).
</success_criteria>

<output>
After completion, create `.planning/phases/33-admin-shell-navigation-and-audit-preview-polish/33-01-SUMMARY.md` documenting:
- Which of the four port points landed (top-bar, sidebar, bottom-nav, helpers + reorder).
- The drift fixture ID chosen (`"fix #18 — admin_shell users nav + mobile bottom-nav"` or refinement).
- Whether the optional `admin_shell_test.exs` one-line extension landed (planner chose: YES, per Task 2).
- Confirmation that no unrelated code (audit_link/1, other helpers) was touched (D-12).
- Any deviation from PATTERNS.md's recommended shape with rationale.
</output>
