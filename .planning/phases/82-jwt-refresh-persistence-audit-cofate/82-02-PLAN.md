---
phase: 82
plan: 02
type: execute
wave: 2
depends_on:
  - 82-01-PLAN.md
files_modified:
  - test/sigra/jwt_refresh_audit_cofate_test.exs
autonomous: true
requirements:
  - AUD-19-03
must_haves:
  truths:
    - "**`test/sigra/jwt_refresh_audit_cofate_test.exs`** proves persistence + audit co-fate (happy, audit-off, fault injection, reuse rollback) with **`async: false`**."
    - "**`@moduledoc`** references **`api_token_audit_atomic_test.exs`** for audit-only **`APIToken.audit_jwt_refresh*`** coverage (Phase **81**)."
  artifacts:
    - path: test/sigra/jwt_refresh_audit_cofate_test.exs
      provides: "AUD-19-03 automated evidence"
nyquist_compliant: false
---

<objective>
Implement **AUD-19-03**: dedicated Postgres integration tests for JWT refresh persistence + audit co-fate, mirroring Phase **79**/**81** fault-injection style per **82-CONTEXT.md** D-82-04.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-82-01 | Repudiation | Missing tests for co-fate rollback | high | CHECK / constraint fault injection + row counts |
| T-82-04 | Elevation | Flaky async tests mutating global DB | low | **`async: false`** module attribute |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add `jwt_refresh_audit_cofate_test.exs`</name>
  <files>test/sigra/jwt_refresh_audit_cofate_test.exs</files>
  <read_first>
    - test/sigra/api_token_audit_atomic_test.exs (CHECK injection helpers, `sigra_config/1`, telemetry attach patterns)
    - lib/sigra/jwt.ex (`refresh/3` after plan 01)
    - lib/sigra/jwt/refresh_token.ex (`create/4`, rotation semantics)
    - .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-CONTEXT.md (D-82-04 coverage list)
  </read_first>
  <action>
    1. Create **`test/sigra/jwt_refresh_audit_cofate_test.exs`** with **`use Sigra.DataCase`** (or the same case module **`api_token_audit_atomic_test.exs`** uses), **`@moduletag :capture_log`**, and **`async: false`**.

    2. Implement named tests (minimum set aligned with **D-82-04**):
       - **Happy path (audit on):** create user + refresh token via **`RefreshToken.create`** / fixtures used in atomic tests; call **`Sigra.JWT.refresh(cfg, raw, opts)`**; assert **`audit_events`** row **`action = 'api.jwt_refresh'`** count **1** and **`user_tokens`** shows rotated state (new raw token differs, old superseded).
       - **Audit off:** config without **`:audit_schema`** — refresh succeeds, **zero** **`api.jwt_refresh`** rows.
       - **Fault injection (happy branch):** add CHECK constraint on **`audit_events`** rejecting **`api.jwt_refresh`** (unique name e.g. **`jwt_refresh_cofate_guard`**); call **`JWT.refresh`**; assert **`{:error, :jwt_refresh_aborted}`**; assert **no** orphaned partial **`user_tokens`** state vs baseline (use SQL counts or **`repo.get_by`** expectations documented in test comments); detach handler in **`after`**; **`Repo.query!`** to drop constraint in **`after`**.
       - **Reuse + audit:** simulate reuse; assert **`api.jwt_refresh_reuse`** row + **`{:error, :reuse_detected}`** still returned **after** successful commit when audit on.
       - **Reuse audit failure rolls back revocations:** fault injection on reuse audit action; assert **`{:error, :jwt_refresh_aborted}`** or **`{:error, :reuse_detected}`** per **D-82-02** final contract (test name documents chosen tuple — must match **82-CONTEXT**; if reuse path returns **`:reuse_detected`** only after full success, audit failure case should be **`:jwt_refresh_aborted`**).

    3. **`@moduledoc`** paragraph pointing readers to **`test/sigra/api_token_audit_atomic_test.exs`** for standalone **`audit_jwt_refresh*`**.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/jwt_refresh_audit_cofate_test.exs` exits **0**.
    - First line of module contains **`async: false`** OR **`@moduletag :integration`** with **`async: false`** on **`use`** line: `grep -n "async: false" test/sigra/jwt_refresh_audit_cofate_test.exs` exits **0**.
    - `grep -nF "api_token_audit_atomic_test" test/sigra/jwt_refresh_audit_cofate_test.exs` exits **0** (moduledoc pointer).
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/jwt_refresh_audit_cofate_test.exs` exits **0** with live Postgres.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/jwt_refresh_audit_cofate_test.exs`
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/api_token_audit_atomic_test.exs test/sigra/jwt_refresh_audit_cofate_test.exs` exits **0**.
</verification>

<success_criteria>
- **AUD-19-03** satisfied: automated tests prove co-fate and rollback posture.
</success_criteria>
