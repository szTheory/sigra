---
phase: 82
plan: 03
type: execute
wave: 3
depends_on:
  - 82-02-PLAN.md
files_modified:
  - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
  - .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md
  - .planning/phases/09-audit-logging/09-VERIFICATION.md
  - .planning/phases/09-audit-logging/09-03-SUMMARY.md
  - CHANGELOG.md
  - .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md
autonomous: true
requirements:
  - AUD-19-04
must_haves:
  truths:
    - "**44** / **45** / **09-VERIFICATION** rows **048–049** cite persistence + audit **co-fate** when **`:audit_schema`** set, with dated footnote **81** → audit-only txn; **82** → **AUD-08** persistence closure per **D-82-05**."
    - "**`CHANGELOG.md` [Unreleased]** contains one operator-facing bullet for JWT refresh co-fate (no false **AUD-18**-only claim)."
    - "**`82-VERIFICATION.md`** exists with merge gate table mirroring phase **81** style."
  artifacts:
    - path: .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md
      provides: "Phase 82 merge gate"
nyquist_compliant: false
---

<objective>
Implement **AUD-19-04**: surgical planning truth + **`82-VERIFICATION.md`** + **`CHANGELOG`** per **82-CONTEXT.md** D-82-05.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-82-05 | Repudiation | Docs claim co-fate without matching code/tests | medium | **`82-VERIFICATION.md`** ties checklist to **`jwt_refresh_audit_cofate_test.exs`** + **`lib/sigra/jwt.ex`** |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Inventories **44** / **45** — rows 048–049</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md, .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md (lines for AUD-04-048 / 049)
    - .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md (JWT appendix rows if present)
    - .planning/phases/81-jwt-refresh-audit-atomicity/81-03-PLAN.md (wording patterns)
    - .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-CONTEXT.md (D-82-05)
  </read_first>
  <action>
    For **AUD-04-048** and **AUD-04-049** table rows, update **mechanism** / **notes** / **Phase** cells so the **current** story is: **`user_tokens`** refresh persistence + **`api.jwt_refresh*`** share **one** **`Repo.transaction`** + **`Ecto.Multi`** + **`Audit.log_multi_safe`** when **`:audit_schema`** set (**Phase 82**). Include a **short dated supersession footnote**: Phase **81** = audit-only helper transaction; Phase **82** = **AUD-08** persistence co-fate closure. Preserve **Nyquist** trace — surgical edits, not table rewrite.
  </action>
  <acceptance_criteria>
    - `grep -nF "82" .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md | grep -E "048|049"` exits **0** OR inventory rows contain substring **`82`** in the **AUD-04-048** / **049** lines: `grep "AUD-04-048" .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md | grep "82"` exits **0** and same for **049**.
    - `grep -nF "co-fate" .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` exits **0** OR mechanism column contains **`user_tokens`** and **`api.jwt_refresh`** in the **048** line (case-insensitive OK): `grep -i "user_tokens" .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md | grep "048"` exits **0**.
    - If **45-AUD-04-INVENTORY.md** contains **048/049** or **EX-45-JWT** appendix lines for JWT refresh, those lines reference **phase 82** co-fate: `grep -i "82" .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md` exits **0** when appendix exists; if no JWT rows, skip with comment in commit message (executor documents **N/A** in **`82-VERIFICATION.md`** notes).
  </acceptance_criteria>
  <verify>
    `grep -n "AUD-04-048" .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md`
  </verify>
</task>

<task type="execute">
  <name>Task 2: **09-VERIFICATION.md** C-1 rows 048–049 + **09-03-SUMMARY.md**</name>
  <files>.planning/phases/09-audit-logging/09-VERIFICATION.md, .planning/phases/09-audit-logging/09-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-VERIFICATION.md (C-1 table around AUD-04-048 / 049)
    - .planning/phases/09-audit-logging/09-03-SUMMARY.md (tail — where to add one paragraph)
    - .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-CONTEXT.md (D-82-05)
  </read_first>
  <action>
    1. Update **C-1** lines **AUD-04-048** / **049** so **mechanism** / **verdict** / **evidence** reference **persistence + audit co-fate** for **`Sigra.JWT.refresh/3`** when **`:audit_schema`** set, **`test/sigra/jwt_refresh_audit_cofate_test.exs`**, and **phase 82** closure; keep **T1** language honest (bounded to described evidence).

    2. Append **one short paragraph** to **`09-03-SUMMARY.md`** linking **JWT refresh co-fate** verification to **`.planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md`**.
  </action>
  <acceptance_criteria>
    - `grep -nF "jwt_refresh_audit_cofate_test" .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0**.
    - `grep -nF "82-VERIFICATION.md" .planning/phases/09-audit-logging/09-03-SUMMARY.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `grep -n "AUD-04-048" .planning/phases/09-audit-logging/09-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: **`CHANGELOG.md`** + **`82-VERIFICATION.md`**</name>
  <files>CHANGELOG.md, .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md</files>
  <read_first>
    - CHANGELOG.md (`[Unreleased]` section)
    - .planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md (table template)
    - .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-CONTEXT.md (D-82-05 CHANGELOG bullet)
  </read_first>
  <action>
    1. Under **`[Unreleased]`**, add **one** **Changed** (or **Fixed**) bullet: JWT refresh persistence shares transactional boundary with **`api.jwt_refresh`** / **`api.jwt_refresh_reuse`** when audit schema configured; mention **`:jwt_refresh_aborted`** for operators.

    2. Write **`.planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md`** with frontmatter **`status: passed`** only if executor has run tests locally — otherwise **`pending`** and checklist rows **`pending`**. Template: merge gate rows for **`mix test test/sigra/jwt_refresh_audit_cofate_test.exs`**, planning truth files touched, **`CHANGELOG`**, code pointers (**`lib/sigra/jwt.ex`**, **`lib/sigra/api_token.ex`**), maintainer sign-off **pending**.
  </action>
  <acceptance_criteria>
    - `grep -A120 "## [Unreleased]" CHANGELOG.md | grep -iE "Sigra\\.JWT\\.refresh|jwt_refresh_aborted|user_tokens.*jwt|co-fate|persistence.*audit"` exits **0** (new **Changed** bullet for phase **82** appears before the next **`## [`** release heading).
    - `test -f .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md` exits **0**.
    - `grep -nF "jwt_refresh_audit_cofate_test" .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `head -5 .planning/phases/82-jwt-refresh-persistence-audit-cofate/82-VERIFICATION.md`
  </verify>
</task>

</tasks>

<verification>
- Planning grep checks in **acceptance_criteria** all succeed from repo root.
</verification>

<success_criteria>
- **AUD-19-04** satisfied; merge gate doc exists for human PR.
</success_criteria>

## PLANNING COMPLETE
