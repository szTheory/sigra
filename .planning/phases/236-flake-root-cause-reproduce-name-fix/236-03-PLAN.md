---
phase: 236-flake-root-cause-reproduce-name-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 236-01
files_modified:
  - scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs
  - test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts
  - test/sigra/planning/phase_236_retry_wrapper_prohibition_test.exs
  - .github/workflows/ci.yml
  - .planning/research/STACK.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - GREEN-02

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Adding a retry wrapper to the Playwright config — `retries` above 0, a `retries` read from `process.env`, a per-project `retries:` override, a `test.describe.configure({ retries`, a `waitForTimeout(`, or a `test.slow(` — fails `scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs` with a NAMED message explaining what it masks (GREEN-02, SC-4)."
    - "That failure has been OBSERVED, not asserted: the guard exits non-zero against the committed known-bad fixture `test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts` and exits zero against the real `test/example/priv/playwright/playwright.config.ts`, and both exit codes plus the RED's failure message are recorded in the phase evidence ledger."
    - "The guard cannot pass vacuously: it fails with a `the parse broke, this is not a pass` message if the config parse locates no `retries:` line, or if the spec walk finds fewer than 10 `tests/*.spec.ts` files (there are 20 at HEAD)."
    - "The guard runs in CI with ZERO workflow edits — the existing `.github/workflows/ci.yml:393` glob inside `fast_checks` auto-discovers `scripts/ci/prohibitions/*.test.mjs` — and is never added to `mix ci`."
    - "The dead `PLAYWRIGHT_RETRIES: 1` env key is gone from `.github/workflows/ci.yml`, and its absence is proven by a parsed contract test with a non-vacuity floor rather than a bare grep count."
    - "`.planning/research/STACK.md` no longer claims that traces for the flaky runs already exist and can be harvested."
  artifacts:
    - scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs
    - test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts
    - test/sigra/planning/phase_236_retry_wrapper_prohibition_test.exs
  key_links:
    - "`ci.yml:393` shell glob → auto-discovery of `p17-*.test.mjs` inside `fast_checks`. A bare directory arg is NOT valid on node 22; the glob is load-bearing and must not be 'tidied'."
    - "PLAN frontmatter `check_violation_fixture`/`check_clean_fixture` → `GSD_PROHIB_SUBJECT` → `readSubject()` in the guard. The fixture path lives in the frontmatter, never in the guard file."
    - "`stripJsComments` → every content assertion. `playwright.config.ts:15-16` documents its own compliance in prose containing the asserted token; a naive match reds the compliant file."
  prohibitions:
    - statement: "MUST NOT add a retry wrapper to the Playwright surface — `retries` above 0, a `retries` value read from `process.env`, a per-project `retries:` override, `test.describe.configure({ retries ... })`, `page.waitForTimeout(`, or `test.slow(` — anywhere in `test/example/priv/playwright/playwright.config.ts` or its specs. Recovering a failed attempt masks the isolation evidence the flake investigation depends on."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts
      check_clean_fixture: test/example/priv/playwright/playwright.config.ts
      reason: "ROADMAP SC-4 and GREEN-02 both forbid closing the flake by retrying it into silence; D-33 makes retry-wrapping never the fallback. ARCHITECTURE.md:275-277 already states the prohibition in prose — this makes it mechanical."
    - statement: "MUST NOT have the `p17` guard assert anything about `trace`."
      status: resolved
      verification: review
      reason: "`trace` is not a retry wrapper, and a leftover diagnostic `trace: 'on'` from the SC-1 reproduction would red `fast_checks` during the very diagnosis this phase performs. Scope the guard to `retries` / `waitForTimeout` / `test.slow` / `test.describe.configure({ retries`."
    - statement: "MUST NOT wire `PLAYWRIGHT_RETRIES` to `playwright.config.ts`; delete it."
      status: resolved
      verification: test
      reason: "SC-4 permits wire-or-delete, but wiring it would make the flagship generated-host parity job actually retry — the precise retry-wrap GREEN-02, SC-4 and `ARCHITECTURE.md:275-277` prohibit, and it would instantly violate the `p17` guard written in the same phase."
    - statement: "MUST NOT split the guard's checks into 'config patterns' and 'spec patterns'; all six patterns apply to the substituted subject text."
      status: resolved
      verification: review
      reason: "Only ONE artifact is substitutable (`_lib.mjs:11-17`). When the fixture is injected as `GSD_PROHIB_SUBJECT`, the `tests/*.spec.ts` walk still reads the real, clean specs — so a split checker makes the committed known-bad unable to go RED, and the guard could never be observed red."
---

<objective>
Make "do not retry the flake into silence" mechanical, and delete the one dishonest surface that
would let someone do it by accident.

Purpose: ROADMAP SC-4. A prose prohibition in `ARCHITECTURE.md` is not a gate. This plan adds
`p17-no-playwright-retry-wrapper.test.mjs`, proves it RED against a committed known-bad fixture
(standing constraint 6: a guard never observed RED does not count), deletes the dead
`PLAYWRIGHT_RETRIES: 1` at `ci.yml:1460`, and corrects the two `STACK.md` claims that traces already
exist — claims this phase's own reproduction work disproves.

Output: one guard, one fixture, one ExUnit contract test, one deleted env line, two corrected doc
claims, and one guard-number collision pre-resolved.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-PATTERNS.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-RESEARCH.md
@scripts/ci/prohibitions/_lib.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the `p17` guard and its committed known-bad fixture, and observe the RED</name>
  <files>scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs, test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts</files>
  <read_first>
    - `scripts/ci/prohibitions/_lib.mjs` — the whole file (315 lines); specifically `:11-17` (the one-substitutable-subject doctrine), `:20-24` (the `the parse broke, this is not a pass` message convention and where it was borrowed from), `:33-78` (`subjectPath` / `archiveAwareRelPath` / `readSubject` — `readSubject` THROWS on a missing subject, because a missing subject is a broken run and never an absent violation), `:147-177` (`stripJsComments`)
    - `scripts/ci/prohibitions/p02-axe-signal-not-reduced.test.mjs` — `:1-12` (the four-section header comment) and `:14-21` (imports + the `stripJsComments(readSubject(...))` idiom). This is the only guard whose subject is a TypeScript file.
    - `scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs` — `:1-29` (header, including the why-comments-are-stripped paragraph), `:59-85` (the pure `leniencyIssue(body)` checker returning `null` or a named explanatory string, never a boolean), `:87-105` (the non-vacuity floor test plus the `assert.equal(issue, null, issue ?? '')` assertion shape), `:124-137` (the inline negative control)
    - `test/fixtures/prohibitions/p02-axe-test-tagged-snapshot.ts` — the only committed `.ts` fixture; copy its conventions (opening comment starting `// KNOWN-BAD fixture for PNN (<plan-id>).`, naming the defect explicitly, and being structurally valid so the RED comes from the substantive check and not from a parse-broke message)
    - `test/example/priv/playwright/playwright.config.ts` — `:15-16` (the compliance prose that a naive `/retries/` match would red), `:59` (`retries: 0`), and the `projects:` array so the per-project override check targets the real shape
  </read_first>
  <action>
Create `scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs`.

Header comment, four sections, matching `p02:1-12` and `p16:1-29`: the `P17 (236-03-PLAN.md) —
mechanical enforcement.` line; the indented MUST-NOT statement; a `Subject:` line naming
`test/example/priv/playwright/playwright.config.ts` (via `GSD_PROHIB_SUBJECT`) plus a `Secondary:`
line naming the `tests/*.spec.ts` walk; and a `What silently breaks if this guard is deleted:`
paragraph. Add `p16`'s why-comments-are-stripped paragraph too, and make it concrete: the shipped
config documents its own compliance at `:15-16` with prose containing the word `retries`, so a naive
match reds the file precisely for explaining that it is correct.

Import `readSubject`, `readRepoFile` and `stripJsComments` from `./_lib.mjs`. Declare exactly ONE
substitutable subject constant for `test/example/priv/playwright/playwright.config.ts` and read it
through `stripJsComments(readSubject(...))`. Read every `tests/*.spec.ts` from its REAL location via
a `readdirSync` walk plus `readRepoFile` — never through `readSubject`.

D-24 fixes this guard's whole contract, and the paragraphs above and below are its implementation:
the file name `scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs`; exactly ONE
substitutable subject (`test/example/priv/playwright/playwright.config.ts`, via `subjectPath()`) per
the `_lib.mjs` contract; every `tests/*.spec.ts` as SECONDARY artifacts read from their real
locations; a pure `retryWrapperIssue(text)` checker; all assertions taken over COMMENT-STRIPPED
content via `stripJsComments`; and the two mandatory non-vacuity floors (the config parse found a
`retries:` line; the spec walk found ≥10 spec files). None of those six elements is optional.

Export a pure `retryWrapperIssue(text)` that returns `null` when clean and a NAMED, explanatory
string when violating — never a boolean. Six patterns, ALL applied to the same text, because only
one artifact is substitutable and a split checker makes the fixture unable to go RED:

  1. no `retries:` line found at all → the parse-broke message
  2. a `retries:` line whose value is not literally `0`
  3. `retries` read from `process.env`
  4. a per-project `retries:` override inside the `projects:` array
  5. `test.describe.configure({ retries`
  6. `waitForTimeout(` or `test.slow(`

Do NOT assert anything about `trace`. `trace` is not a retry wrapper, and a leftover diagnostic
`trace: 'on'` from the plan 236-01 reproduction would red `fast_checks` during the very diagnosis
this phase performs — that leak is caught by review, not by a guard that would have blocked the
investigation.

Two non-vacuity floor tests, each with a message ending in `the parse broke, this is not a pass`:
the config parse located a `retries:` line at all, and the spec walk found at least 10 spec files
(there are 20 at HEAD, so 10 is honest with headroom). Assert the substantive check with
`assert.equal(issue, null, issue ?? '')`, matching `p16:100-104`. Add `p16`'s inline negative
control as well — it costs nothing and proves the checker fires.

Create `test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts`. Opening comment starts literally
`// KNOWN-BAD fixture for P17 (236-03).`, names the defect, and says what silently breaks. It must
be STRUCTURALLY VALID for the guard's parse — a recognisable `defineConfig({...})` with a locatable
`retries:` line — so that every structural assertion passes and the RED comes from
`retryWrapperIssue`, not from a parse-broke message. Per D-26 it carries all three violations at
once: `retries: Number(process.env.PLAYWRIGHT_RETRIES ?? 1)` inside the `defineConfig`, a
`page.waitForTimeout(500)`, and a `test.slow()`. It is never imported or executed — it is read as
text.

Then OBSERVE both halves and capture their exit codes and the RED's message text for the evidence
ledger. Asserting the guard was run does not earn the claim.
  </action>
  <verify>
    <automated>GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts node --test --test-reporter=tap scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs</automated>
    <fails_when>exit 0 — the RED half is REQUIRED to exit non-zero. It is also a failure if it exits non-zero with a `the parse broke, this is not a pass` message, because that means the fixture broke the parse instead of tripping the substantive check.</fails_when>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs</automated>
    <fails_when>non-zero exit, or `# fail 0` absent from the TAP summary</fails_when>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs</automated>
    <fails_when>non-zero exit, or `# fail 0` absent from the TAP summary — the new guard must not break any existing guard in the shared glob</fails_when>
  </verify>
  <acceptance_criteria>
    - The RED invocation exits non-zero and its failure message names which of the six patterns fired, in prose that explains what the wrapper masks — not a bare boolean assertion message.
    - The GREEN invocation exits 0 against the unmodified `test/example/priv/playwright/playwright.config.ts`.
    - `grep -n 'trace' scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs` returns no assertion on `trace`.
    - `grep -n 'test/fixtures/prohibitions' scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs` returns nothing — the fixture path lives in this PLAN's frontmatter, matching every other guard in the directory.
    - The guard imports `stripJsComments` from `./_lib.mjs` and does not define its own comment-stripping regex.
    - `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0.
    - Both exit codes and the RED's failure message are appended to `236-EVIDENCE.md`.
  </acceptance_criteria>
  <done>`p17` exists, is observed RED against a committed known-bad and GREEN against the real config, and the shared glob still passes.</done>
  <reversibility rating="costly">Phase 236 claims the `p17` number ahead of Phase 241's SURF-04, which currently names `p17-*`. Two colliding `p17-*` files would both run and break nothing functionally, but the numbering would stop being a stable id. Mitigated in Task 3 by renumbering SURF-04 to `p18-*` in the two planning documents; flagged, not gated.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Delete the dead `PLAYWRIGHT_RETRIES` env key and prove its absence with a parsed contract test</name>
  <files>.github/workflows/ci.yml, test/sigra/planning/phase_236_retry_wrapper_prohibition_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml:1454-1462` — the `Run generated admin acceptance smoke` step's `env:` block; `PLAYWRIGHT_RETRIES: 1` is the fifth and last key, the four survivors are `PGUSER`, `PGPASSWORD`, `PGHOST`, `GITHUB_WORKSPACE`
    - `.github/workflows/ci.yml:1401-1421` — the `generated_admin_playwright_smoke` job, which has `needs: release_ref_guard` and deliberately no `if:`, with an 11-line comment forbidding reintroduction
    - `test/sigra/planning/phase_230_ci_timeouts_test.exs` — `:9-10` (the no-YAML-parser rationale), `:24-45` (the `String.split(~r/\njobs:\s*\n/, parts: 2)` + `~r/(?=^  [a-zA-Z0-9_-]+:\s*$)/m` job-block walk), `:53-59` (the non-vacuity floor)
    - `test/sigra/planning/phase_232_playwright_economics_test.exs:1-18` — the module header and `File.read!` + `=~` assertion idiom
  </read_first>
  <action>
Delete the single line `          PLAYWRIGHT_RETRIES: 1` from `.github/workflows/ci.yml`. Delete only
that line — the four surviving `env:` keys stay, and no other workflow edit is made anywhere in this
phase. The `p17` guard needs no wiring: the existing `ci.yml:393` glob
(`node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs`, inside `fast_checks`)
auto-discovers it, which is also what keeps it correctly outside `mix ci` per standing constraint 5.
Do not "tidy" that glob into a directory argument — the comment two lines above records that a bare
directory arg is not valid on node 22.

D-27: that auto-discovery is the decision, and it is why this phase makes ZERO workflow additions.
The `ci.yml:393` glob inside `fast_checks` already picks up any new
`scripts/ci/prohibitions/*.test.mjs`, so `p17` needs no wiring at all — and being reached through
`fast_checks` rather than through the root alias is exactly what keeps it outside `mix ci`, per
standing constraint 5. Criterion 5 below asserts the glob survives, because it is the guard's only
route into CI.

D-28: DELETE the dead key, do not wire it. It has zero readers repo-wide outside `.planning/` prose;
wiring it would make the flagship generated-host parity job actually retry — the precise retry-wrap
GREEN-02, ROADMAP SC-4 and `ARCHITECTURE.md:275-277` prohibit — and would instantly violate the
`p17` guard written in the same phase. SC-4 permits wire-or-delete; deletion is the only option that
does not contradict the rest of the phase.

Create `test/sigra/planning/phase_236_retry_wrapper_prohibition_test.exs` defining
`Sigra.Planning.Phase236RetryWrapperProhibitionTest` with `use ExUnit.Case, async: true`. It is a
separate module from `phase_236_audit_url_ownership_test.exs` (owned by plan 236-02) so the two
plans can run in the same wave without contending for a file.

It asserts, as a PARSED contract and not a bare grep count — standing constraint 1 rejects
count-only acceptance:

  1. `.github/workflows/ci.yml` contains zero occurrences of `PLAYWRIGHT_RETRIES`.
  2. Non-vacuity floor A: the job-block walk (copy the `phase_230_ci_timeouts_test.exs:24-45` shape,
     including its no-YAML-parser rationale comment) locates a `generated_admin_playwright_smoke`
     block at all, failing with `— the parse broke, this is not a pass` if it does not.
  3. Non-vacuity floor B: that block still declares at least one `env:` key — so a future edit that
     deletes the whole step, rather than the one dead key, is caught instead of reported green.
  4. `scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs` and
     `test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts` both exist on disk.
  5. `.github/workflows/ci.yml` still contains the literal
     `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` glob — the guard's only
     route into CI, and the thing that must survive for `p17` to run at all.
  6. `mix.exs`'s `ci:` alias list contains no reference to `scripts/ci/prohibitions` — standing
     constraint 5, asserted rather than trusted.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_retry_wrapper_prohibition_test.exs</automated>
    <fails_when>non-zero exit, or the output does not report `0 failures`</fails_when>
    <automated>grep -c 'PLAYWRIGHT_RETRIES' .github/workflows/ci.yml; test "$(grep -c 'PLAYWRIGHT_RETRIES' .github/workflows/ci.yml)" = "0"</automated>
    <fails_when>non-zero exit from the `test` comparison — any surviving `PLAYWRIGHT_RETRIES` occurrence in the workflow</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff origin/main...HEAD -- .github/workflows/ci.yml` shows exactly one deleted line and zero added lines.
    - The `Run generated admin acceptance smoke` step still declares `PGUSER`, `PGPASSWORD`, `PGHOST` and `GITHUB_WORKSPACE`.
    - `.github/workflows/ci.yml` still contains the literal string `scripts/ci/prohibitions/*.test.mjs`.
    - `phase_236_retry_wrapper_prohibition_test.exs` exits 0 and its non-vacuity floor messages end with the literal `— the parse broke, this is not a pass`.
    - `grep -rn 'PLAYWRIGHT_RETRIES' --include='*.yml' --include='*.ts' --include='*.exs' --include='*.mjs' .` returns hits only inside the new fixture and `.planning/` prose.
  </acceptance_criteria>
  <done>The dead env key is gone, its absence is a parsed contract, and the guard's CI route is asserted.</done>
  <reversibility rating="reversible">A one-line workflow deletion with zero readers; restoring it is a one-line revert.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Correct STACK.md's two false trace claims and pre-resolve the `p17`/`p18` number collision</name>
  <files>.planning/research/STACK.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/research/STACK.md` — `:82` ("traces for the flaky runs already exist as artifacts. Harvest them before adding instrumentation") and `:197` (diagnosis step 1, "Harvest the traces that already exist.")
    - `test/example/priv/playwright/playwright.config.ts:59` (`retries: 0`) and `:81` (`trace: 'on-first-retry'`) — the two facts that make both claims false
    - `.planning/REQUIREMENTS.md:37` (SURF-04, currently naming `p17-*`) and `.planning/ROADMAP.md:170` (Phase 241 SC-5, also naming `p17-*`)
    - `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-01-SUMMARY.md` — what the reproduction actually had to do, which is the correction's evidence
  </read_first>
  <action>
D-29: correct the two now-false claims at `.planning/research/STACK.md:81-82,195-204` that traces
already exist (`SUMMARY.md:33` K5 already records the contradiction). Concretely:

Correct `.planning/research/STACK.md` at `:82` and `:197` in place. Both claim traces for the flaky
runs already exist and can be harvested. Both are false: `playwright.config.ts:59` hardcodes
`retries: 0`, so `trace: 'on-first-retry'` at `:81` never fires, and `PLAYWRIGHT_RETRIES` had zero
readers even before Task 2 deleted it. Replace each with the true statement — no trace artifact
exists and none can be harvested; a RED must be manufactured deliberately, as plan 236-01 did —
and cite the reproduction path that was actually used. `SUMMARY.md:33` K5 already records the
contradiction; this closes it rather than leaving two documents disagreeing.

Then pre-resolve the guard-number collision this phase creates. Phase 236 claims `p17`. Phase 241's
SURF-04 currently also names `p17-*`, in two places: `.planning/REQUIREMENTS.md:37` and
`.planning/ROADMAP.md:170`. Change both to `p18-*`, one line each, changing nothing else about
either requirement's text or scope. This is not a found-while-cleaning fix — it is the direct
consequence of this plan's own deliverable taking the number, and leaving it would mean two
colliding `p17-*` files that both run while the numbering stops being a stable id. Phase 241 is the
later, still-unwritten requirement, so it moves.
  </action>
  <verify>
    <automated>test "$(grep -c 'traces that already exist\|traces for the flaky runs already exist' .planning/research/STACK.md)" = "0"</automated>
    <fails_when>non-zero exit — either false-trace claim survives verbatim in STACK.md</fails_when>
    <automated>grep -n 'p17-\|p18-' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>the output shows `p17-` on `.planning/REQUIREMENTS.md:37` or `.planning/ROADMAP.md:170`, or shows no `p18-` on either line</fails_when>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs</automated>
    <fails_when>non-zero exit, or `# fail 0` absent from the TAP summary — several guards read `.planning/` documents, so the doc edits must not break them</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/research/STACK.md` contains no sentence asserting that flaky-run traces already exist or can be harvested.
    - The replacement text at both sites cites `playwright.config.ts:59` (`retries: 0`) as the reason.
    - `.planning/REQUIREMENTS.md:37` names `scripts/ci/prohibitions/p18-*.test.mjs` for SURF-04; `.planning/ROADMAP.md:170` names `p18-*` for Phase 241 SC-5. Neither requirement's scope, wording or checkbox state is otherwise changed.
    - `git diff origin/main...HEAD -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows exactly two changed lines total.
  </acceptance_criteria>
  <done>STACK.md tells the truth about traces, and the `p17`/`p18` numbering is unambiguous before Phase 241 is written.</done>
  <reversibility rating="reversible">Documentation and requirement-id edits; no shipped code, no external artifact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo contributor → `fast_checks` prohibition glob | A guard is the only thing standing between a well-meant retry wrapper and a silently-masked flake |
| `GSD_PROHIB_SUBJECT` env → `readSubject()` | The fail-first producer substitutes the subject path; a guard that hardcodes its path cannot be proven RED |
| planning documents → CI guards | Several guards read `.planning/` files, so doc edits are not inert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-236-10 | Tampering | `p17-no-playwright-retry-wrapper.test.mjs` | high | mitigate | A guard that passes vacuously is worse than no guard — it reports protection that does not exist. Mitigated by two non-vacuity floors (a `retries:` line was located; the spec walk found ≥10 of the 20 spec files) whose messages end in `the parse broke, this is not a pass`, and by the observed RED against a committed known-bad fixture. |
| T-236-11 | Repudiation | the SC-4 "guard works" claim | high | mitigate | Standing constraint 6: a guard never observed RED does not count. Mitigated by recording BOTH exit codes and the RED's verbatim failure message in `236-EVIDENCE.md`; `gsd_run check prohibition-enforcement` re-runs the same pair from this plan's `check_violation_fixture`/`check_clean_fixture` frontmatter. |
| T-236-12 | Elevation of Privilege | `.github/workflows/ci.yml` | high | mitigate | Any workflow edit is a supply-chain-adjacent change. Mitigated by constraining the diff to exactly one deleted line with zero additions, asserted by the Task 2 acceptance criteria and by the contract test's floor that the step still declares ≥1 `env:` key — so deleting the whole step is caught rather than reported green. |
| T-236-13 | Tampering | `test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts` | medium | mitigate | The fixture contains a deliberately malicious-looking config. It must never be imported or executed — it is read as text only. Mitigated by the `// KNOWN-BAD fixture for P17 (236-03).` header convention and by placing it under `test/fixtures/prohibitions/`, which no build or test path loads. |
| T-236-14 | Denial of Service | `fast_checks` runtime | low | accept | One more `node:test` file in an existing glob; the guard reads ~21 small files and runs in under a second. |
| T-236-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages. The guard uses only `node:test`, `node:assert/strict` and the existing `./_lib.mjs`; no `package.json` or lockfile is in `files_modified`, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts node --test scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs` exits NON-ZERO with a named message.
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0.
- `MIX_ENV=test mix ci` exits 0.
- `grep -c 'PLAYWRIGHT_RETRIES' .github/workflows/ci.yml` returns 0.
- Both `p17` exit codes and the RED's message are in `236-EVIDENCE.md`.
</verification>

<success_criteria>
- ROADMAP SC-4 is satisfied: a retry wrapper fails a `scripts/ci/prohibitions/*.test.mjs` guard,
  demonstrated RED against a committed known-bad fixture, and the dead `PLAYWRIGHT_RETRIES: 1` is
  deleted.
- Zero workflow edits beyond the one deletion; `mix ci` topology unchanged.
- No planning document still claims flaky-run traces already exist.
</success_criteria>

<output>
Create `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-03-SUMMARY.md` when done.
</output>
