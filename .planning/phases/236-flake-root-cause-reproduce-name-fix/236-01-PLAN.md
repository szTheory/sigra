---
phase: 236-flake-root-cause-reproduce-name-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md
  - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-DIAGNOSIS.md
autonomous: false
requirements:
  - GREEN-01

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Playwright attempt that FAILED `expect(page).toHaveURL(/actor=/)` at test/example/priv/playwright/tests/admin-generated.spec.ts:459 has been captured, and its trace.zip absolute path, its --repeat-each index, and the verbatim received URL string are recorded in the phase evidence ledger (GREEN-01, SC-1)."
    - "The recorded received URL discriminates D-05's three branches on the record: the diagnosis states which branch it is (a `actor=` absent / b `actor=` present-but-empty / c no keydown reached the document) and cites the trace observation that settles it."
    - "A written differential diagnosis names the cause as exactly one of harness race, DB collision, or product race, and rules out the other two with a cited observation rather than an assertion (SC-2 first half)."
    - "`.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md` parses under `parseEvidenceSlots`: a `## BEFORE-FLAKE-RED` heading with a `Status:` line matching the `p12` grammar, and at least one fenced block invoking a committed instrument."
  artifacts:
    - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md
    - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-DIAGNOSIS.md
  key_links:
    - "236-EVIDENCE.md `## BEFORE-FLAKE-RED` slot → the D-05 branch call in 236-DIAGNOSIS.md → which fix shape plan 236-02 is permitted to implement"
    - "The captured trace's received URL string → the branch call. Without the verbatim string the branch call is unfalsifiable."
  prohibitions:
    - statement: "MUST NOT accept, begin, or commit any change to `lib/sigra/admin/live/audit_index_live.ex` before the SC-1 RED is captured and recorded. Repro-before-fix is a hard ROADMAP gate."
      status: resolved
      verification: review
      reason: "ROADMAP SC-1: 'no fix is accepted without it'. D-05 keeps the diagnosis falsifiable; a fix written first makes the RED decorative."
    - statement: "MUST NOT leave a temporary diagnostic `trace: 'on'` (or any other temporary edit) in `test/example/priv/playwright/playwright.config.ts` at commit time if the Path-B dispatch route is used."
      status: resolved
      verification: review
      reason: "D-21/Pitfall 3: a leftover `trace: 'on'` survives into the shipped config. Path A is preferred precisely because it needs no config edit."
    - statement: "MUST NOT record a trace from a PASSING attempt, or from a run where the generated host failed to boot, as the SC-1 RED."
      status: resolved
      verification: review
      reason: "A passing attempt proves nothing and a boot failure is not a falsification — both would make GREEN-01 a green gate that verified nothing, the exact failure mode this milestone exists to close."
---

<objective>
Manufacture the `Generated admin Playwright smoke` failure deliberately, capture it as a RED with a
trace, and name its cause in a written differential diagnosis.

Purpose: ROADMAP SC-1 is a hard gate — no fix to `lib/` is accepted without a captured reproduction,
and D-05 makes the trace (not the CONTEXT.md diagnosis) the decider of which fix shape is legal.
This plan is the phase's tracer: it is the thinnest end-to-end slice that touches the whole stack
(generated host boot → Playwright driver → the failing assertion → the evidence ledger), and it
carries the phase's real risk. Everything downstream is expansion from what this slice proves.

Output: `236-EVIDENCE.md` with a parseable `## BEFORE-FLAKE-RED` slot, `236-DIAGNOSIS.md` with the
differential, and a recorded D-05 branch call that gates plan 236-02.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-RESEARCH.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-PATTERNS.md
@.planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md
</context>

<planner_decisions_closed_here>
**Closed question 1 — SC-3's n is `n = 5`.** Stated here for the whole phase; executed in plan
236-04. Five sequential `pull_request` pushes to the fix PR, each allowed to conclude before the
next is pushed, because `.github/workflows/ci.yml:52-54` sets
`concurrency.cancel-in-progress: true` keyed on the PR number — push N+1 cancels push N's entire
run — and `push:` fires only on `branches: [main]` (`ci.yml:26-29`), so a feature-branch push
produces no `push` run at all. Budget ~4-5 min per repeat (`ci.yml:1395` records the job at ~3.7m).
Phase 240 / GREEN-04 owns the n≥20 bar on `main`; Phase 236 deliberately carries the lighter n so
it does not duplicate 240's work. An unstated n would be count-only acceptance by another name
(standing constraint 1), which is why it is written down here rather than left to execution.

**Closed question 2 — SC-5's quarantine schema is SPECIFIED, not BUILT.** See
`<sc5_contingency_design>` in plan 236-04. `.github/ci-skip-manifest.tsv` is an enumeration of
`ci.yml` constructs, enforced in four directions by
`scripts/ci/prohibitions/p10-no-undocumented-demotion.test.mjs` (`:72-89` id-resolution, `:101-116`
display-name byte match, `:118-175` gate-column match, `:211-226` reverse set-equality). A
Playwright test name satisfies none of them, and Phase 241 explicitly depends on this file's job
ids. Building the amendment speculatively hands Phase 241 a moving target — the exact reason the
ROADMAP ordered 236 → 241. It is built only if the D-05 branch call in this plan comes out (c).

**Closed question 3 — the manifest's stale pointer to
`scripts/ci/prohibitions/honest-skip-parity.test.mjs` (a file that does not exist) is OUT OF SCOPE
here.** It is already a tracked requirement owned by another phase: DEBT-03 /
`### Phase 241` SC-3 names exactly this correction, and it is ALSO already filed as a pending todo,
`.planning/todos/pending/2026-09-15-honest-skip-parity-guard-does-not-exist.md` (verified present at
plan time). So it is tracked twice already; filing a third record would be noise, and fixing it here
would be a found-while-cleaning in-phase fix, which standing constraint 4 forbids (the one
pre-authorized exception is the `lib/` product race, not this). Recorded, not ignored, not fixed.

**Closed question 4 — `test/example/priv/playwright/tests/admin-generated.spec.ts` is NOT edited in
this phase.** RESEARCH open question 3 floated adding `waitForLiveViewReady(page)` after the two
preset clicks. Rejected: after the 236-02 fix a preset click is a live patch, so the socket never
goes dead and `waitForLiveViewReady` would assert a condition that is already true — it adds no
information. Worse, it would make SC-3's repeated green attributable to either the harness change
or the `lib/` fix, and the milestone's whole thesis is that green must be attributable. Leaving the
spec byte-unchanged is what makes SC-3 evidence for the `lib/` fix alone.
</planner_decisions_closed_here>

<planner_assumptions>
**FLAGGED, UNRESOLVED — edge-coverage row GREEN-01 / `unclassified`.** The probe returned
`unclassified — review manually` and it is left `unresolved` deliberately, not auto-resolved and not
dropped. Reason: GREEN-01's acceptance is the capture of a *stochastic* event. There is no
defensible acceptance criterion of the form "running X always yields Y", because if the flake were
deterministic it would not be a flake. Its evidence is a manual artifact (a trace zip plus a
verbatim received-URL string) reviewed by a human, not a test that can be re-run to the same
verdict. Surfacing this rather than inventing a backstop is the honest disposition.

**Edge-coverage no-silent-drop accounting for the phase:** 3 probe-surfaced items →
2 authored into `must_haves.truths` (GREEN-02 idempotency + GREEN-02 concurrency, both in plan
236-02) + 1 surfaced as this flagged assumption (GREEN-01 unclassified). 2 + 1 = 3. No drops.

**Inherited, already-closed assumptions (do NOT re-open):** PATTERNS.md closed RESEARCH A4
(`append_query/2` at `audit_index_live.ex:298-308` already rejects `nil`/`""`/`false`) and A2
(`applied_chip` is `lib/sigra/admin/components.ex:372`, `audit_pagination_nav` is `:827`, both
shared with the D-30-excluded views). RESEARCH A3 (`gh` authenticated) was probed at plan time:
`gh auth status` reports logged in as `szTheory`. A6 (`gsd_run check prohibition-enforcement`
availability) is handled in plan 236-03 by making the manual RED/GREEN pair the primary evidence.
</planner_assumptions>

<assumption_delta_decision>
**Noun now plural:** "closing Phase 236" has two accepted terminal paths — the root-cause fix
(SC-2 + SC-3) and the SC-5 dated, attributed quarantine entry.

**Decision: `no-change`.** The root-cause fix remains the sole *primary* close; the quarantine stays
a strictly subordinate contingency that fires only on D-05 branch (c). Rationale: RESEARCH C-2
establishes the contingency is not merely expensive but structurally awkward — it needs a real
schema change to a file Phase 241 depends on — so treating it as co-primary would invite
speculative construction and hand 241 a moving target. Naming it as a peer would also soften the
gate that makes SC-1 meaningful.

**What would force a promote:** the SC-1 trace landing on branch (c) — no keydown reaching the
document at all. At that point no `lib/` change is warranted, the quarantine becomes the only
available close, and plan 236-02 is replaced by the `<sc5_contingency_design>` build.

*Advisory only — this block records and informs; it changes no scope.*
</assumption_delta_decision>

<artifacts_this_phase_produces>
Symbols and files created by Phase 236 (excluded from drift verification — they do not exist at
HEAD):

| Artifact | Kind | Owning plan |
|---|---|---|
| `.planning/phases/236-.../236-EVIDENCE.md` | evidence ledger (machine-parsed) | 236-01 (created), 236-04 (extended) |
| `.planning/phases/236-.../236-DIAGNOSIS.md` | written differential diagnosis | 236-01 |
| `handle_event("apply_filters", params, socket)` | new `handle_event/3` clause in `Sigra.Admin.Live.AuditIndexLive` | 236-02 |
| `@filter_param_keys` | new module attribute (param whitelist) in `Sigra.Admin.Live.AuditIndexLive` | 236-02 |
| `test/sigra/planning/phase_236_audit_url_ownership_test.exs` / `Sigra.Planning.Phase236AuditUrlOwnershipTest` | new ExUnit source-contract module | 236-02 |
| `scripts/ci/prohibitions/p17-no-playwright-retry-wrapper.test.mjs` | new prohibition guard | 236-03 |
| `retryWrapperIssue(text)` | new pure checker function inside the `p17` guard | 236-03 |
| `test/fixtures/prohibitions/p17-playwright-retry-wrapper.ts` | new committed known-bad fixture | 236-03 |
| `test/sigra/planning/phase_236_retry_wrapper_prohibition_test.exs` / `Sigra.Planning.Phase236RetryWrapperProhibitionTest` | new ExUnit contract module (`PLAYWRIGHT_RETRIES` gone) | 236-03 |
| `.planning/todos/pending/2026-09-15-get-form-in-liveview-race-on-users-index-and-user-audit.md` | new deferral todo (D-30) | 236-04 |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Capture the RED — drive the failing assertion end-to-end against a booted generated host</name>
  <files>.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md</files>
  <precondition>PostgreSQL is reachable with the credentials `scripts/ci/admin-acceptance-smoke.sh` defaults to (`PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost`); the `phx_new` 1.8.8 archive is installed (`mix archive.install --force hex phx_new 1.8.8`); and Chromium is installed for Playwright (`cd test/example/priv/playwright && npm ci && npx playwright install --with-deps chromium`). Assert each before starting; halt and report if any is unmet rather than recording a boot failure as a RED.</precondition>
  <read_first>
    - `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-RESEARCH.md` — the whole "Reproduction Mechanics (SC-1)" section, plus Pitfalls 1 and 2
    - `scripts/ci/admin-acceptance-smoke.sh` — `:20-76` (the `trap cleanup EXIT` at `:71-76` kills `SERVER_PID`), `:28` (`PORT=4017`), `:31` (`PLAYWRIGHT_SPEC`), `:361-404` (the closed `case` that builds `PLAYWRIGHT_ARGS` with no pass-through), `:406-407` (the platform-admin revoke)
    - `test/example/priv/playwright/tests/admin-generated.spec.ts:415-470` — the failing test; declaration at `:428`, `press("Enter")` at `:458`, failing `toHaveURL` at `:459`
    - `test/example/priv/playwright/playwright.config.ts` — `:15-16` (the compliance prose), `:59` (`retries: 0`), `:68-69` (`expect.timeout: 15_000`), `:81` (`trace: 'on-first-retry'`), `:98,111` (`testIgnore`), `:160-167` (the `admin-generated` project)
  </read_first>
  <action>
Use Path A from RESEARCH §Reproduction Mechanics. Do NOT edit `playwright.config.ts` — Path B's
temporary `trace: 'on'` is the fallback only, and only if Path A cannot boot.

Boot the generated host in a backgrounded-but-alive shell:
`GITHUB_WORKSPACE=$(pwd) scripts/ci/admin-acceptance-smoke.sh --test chrome`. Two script behaviors
are load-bearing and must be worked around, not fought: `trap cleanup EXIT` at `:71-76` kills the
server when the script exits, so the script must stay alive for the whole repeat run; and
`mix sigra.admin.revoke` at `:406-407` removes the platform admin immediately after its own
Playwright block, so every repeat must happen BEFORE that line runs. If the script reaches the
revoke before the repeats finish, the resulting failures are login failures, not the flake, and are
unattributable — discard them and restart.

Drive the failing test directly against the booted port `4017`, 30 attempts, one worker, zero
retries, trace on every attempt, with `--project=admin-generated` (required: the spec is
`testIgnore`d from the `chromium` and `mobile` projects at `playwright.config.ts:98,111` and is
matched only by the `admin-generated` project at `:160-167`). Grep the failing attempt for its
`toHaveURL` error and record the `Received string` verbatim — that string, not the pass/fail, is the
diagnostic payload that discriminates D-05's three branches.

Record three things: the ABSOLUTE path of the failing attempt's `trace.zip`, the `--repeat-each`
index at which it failed, and the received URL string verbatim. If 30 attempts produce no failure,
re-run with a higher `--repeat-each` before concluding anything; do not record a passing run as the
RED and do not proceed to Task 2 without a genuine failure.
  </action>
  <verify>
    <automated>test -s "$(grep -m1 -oE '/[^ ]*trace\.zip' .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md 2>/dev/null || echo /nonexistent)"</automated>
    <fails_when>non-zero exit — no absolute trace.zip path is recorded in the ledger yet, or the recorded path does not resolve to a non-empty file on disk</fails_when>
  </verify>
  <acceptance_criteria>
    - A Playwright attempt exists whose console output contains `Error: expect(page).toHaveURL` and whose `Received string:` line is captured verbatim.
    - `npx playwright show-trace <recorded path>` opens a trace for that failing attempt (the trace is a real zip, not a zero-byte stub).
    - The recorded attempt is a FAILURE of the `:459` assertion — not a boot failure, not a login failure, not a timeout on `page.goto`.
    - `git status --porcelain test/example/priv/playwright/playwright.config.ts` is empty: the config was not edited.
  </acceptance_criteria>
  <done>A failing attempt's trace.zip absolute path, its `--repeat-each` index, and the verbatim received URL are in hand and written into the ledger stub.</done>
  <reversibility rating="reversible">Reproduction is pure observation; nothing shipped changes and a bad capture is simply re-run.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Write 236-EVIDENCE.md with a parseable BEFORE-FLAKE-RED slot, and 236-DIAGNOSIS.md with the differential</name>
  <files>.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md, .planning/phases/236-flake-root-cause-reproduce-name-fix/236-DIAGNOSIS.md</files>
  <read_first>
    - `.planning/milestones/v1.47-phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — the ledger shape to copy verbatim (H1, summary table with anchor links, slot headings, `Status:` line, fenced command blocks)
    - `scripts/ci/prohibitions/_lib.mjs:254` (`SLOT_HEADING_RE`) and `:256-295` (`parseEvidenceSlots` — the `Status:` regex, the `captured` prefix rule, the 8-12 digit run-id harvest, the fenced-block harvest, and the throw when zero slots parse)
    - `scripts/ci/prohibitions/p12-run-id-provenance.test.mjs:38-48` (the enforced status grammar) and `:66-80` (the committed-instrument requirement on fenced blocks)
    - `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-RESEARCH.md` — C-6 (the `live_socket.js:1181-1197` → `unload()` mechanism), C-5 (why `admin-audit.spec.ts` is NOT the same form), D-02/D-03 as recorded in CONTEXT.md
  </read_first>
  <action>
Create `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md`. H1
`# Phase 236 Evidence Ledger`, then a summary table linking each slot by anchor. Create exactly one
slot now: `## BEFORE-FLAKE-RED` (uppercase + hyphens only — `SLOT_HEADING_RE` rejects lowercase).
Give it a bare `Status:` line in the enforced grammar. Because this slot's evidence is a LOCAL
artifact with no GitHub run id, and `p12`'s grammar requires an 8-12 digit run id inside a
`captured (...)` status, use the `pending (...)` form with a one-line reason pointing at the trace
path, and record the trace path, the repeat index and the verbatim received URL in the slot body —
then have plan 236-04 promote it or leave it pending deliberately. Include at least one fenced block
carrying the exact `npx playwright test ... --repeat-each ... --trace=on` invocation that produced
the artifact. Reserve `## AFTER-FIX-GREEN` for plan 236-04 — do not create it here, an empty slot
with no `Status:` line makes the parse ambiguous.

Create `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-DIAGNOSIS.md`. It must name the
cause as exactly ONE of: harness race, DB collision, product race — and rule out the other two with
a cited observation. Ground the writeup in these, in this order of weight:

1. The captured received URL string from Task 1 — the decider (D-05).
2. The `unload()` mechanism (RESEARCH C-6): `live_socket.js:1181-1187` shows that submitting a form
   with NO `phx-submit` calls `this.unload()`, and `:247-257` shows `unload()` is a one-way latch
   that sets `unloaded = true`, runs `destroyAllViews()` and `disconnect()`. Pressing Enter in the
   audit filter form therefore tears the LiveSocket down irreversibly in anticipation of a native
   navigation; if that navigation is suppressed or raced against the join, the page is left
   socket-dead with the URL frozen at the prior preset's query — the reported symptom. This is the
   strongest mechanism-level support and it comes from LiveView's own source.
3. DB collision ruled out: the assertion is URL-only and needs no matching audit rows; the
   `Actor: <uuid>` chip renders from params, not rows (D-02).
4. Value-wipe ruled in or out by the received URL: a form that submitted with a wiped input would
   still serialize `actor=` empty, so total absence of the key means no submission occurred (D-03).

Two honesty constraints on the writeup, both non-negotiable:
- Do NOT claim `admin-generated.spec.ts`'s form has a working "identical" control. RESEARCH C-5:
  `admin-audit.spec.ts:127-146` drives a DIFFERENT LiveView (`audit_user_live.ex`, the file D-30
  excludes) and clicks a button rather than pressing Enter. No spec anywhere drives
  `audit_index_live.ex`'s filter form except the failing one — state that as the finding it is.
- Do NOT write that `<.link patch>` / `push_patch` / `live_patch` has zero hits in
  `lib/sigra/admin/`. It is false: `lib/sigra/admin/live/branding_live.ex:126-148` ships three
  `<.link ... patch={panel_path(...)}>` tabs. The narrow TRUE claim is that server-side
  `push_patch/2` is 0-hit in `lib/`. Write that one.

Close the diagnosis with an explicit D-05 branch call: (a), (b) or (c), with the trace observation
that settles it quoted verbatim.
  </action>
  <verify>
    <automated>node -e "import('./scripts/ci/prohibitions/_lib.mjs').then(m=>{const s=m.parseEvidenceSlots(require('fs').readFileSync('.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md','utf8'));if(!s.some(x=>x.name==='BEFORE-FLAKE-RED'))throw new Error('BEFORE-FLAKE-RED slot not parsed');console.log('slots',s.map(x=>x.name).join(','))})"</automated>
    <fails_when>non-zero exit, or the printed slot list does not contain `BEFORE-FLAKE-RED`</fails_when>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs</automated>
    <fails_when>non-zero exit, or `# fail 0` absent from the TAP summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `236-EVIDENCE.md` contains a literal `## BEFORE-FLAKE-RED` heading followed by a line matching `^Status:\s*(captured \((run|runs) .+\)|pending \(.+\))$`.
    - The `BEFORE-FLAKE-RED` body contains the absolute `trace.zip` path, the `--repeat-each` index, and the received URL string.
    - `236-EVIDENCE.md` contains at least one fenced block whose content invokes `npx playwright test` or `gh run`.
    - `236-DIAGNOSIS.md` contains exactly one of the literal strings `harness race`, `DB collision`, `product race` as its named cause, and rules out the other two with a file:line citation each.
    - `236-DIAGNOSIS.md` cites `live_socket.js:1181` and `live_socket.js:247` for the `unload()` mechanism.
    - `236-DIAGNOSIS.md` contains a line naming the D-05 branch as one of `(a)`, `(b)`, `(c)`.
    - `grep -n 'zero-hit' .planning/phases/236-flake-root-cause-reproduce-name-fix/236-DIAGNOSIS.md` returns nothing, and the file does not assert that `<.link patch>` is absent from `lib/sigra/admin/`.
  </acceptance_criteria>
  <done>Both artifacts exist, the ledger parses under `parseEvidenceSlots`, and the diagnosis names one cause with a recorded D-05 branch call.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: Branch call — confirm which fix shape Phase 236 is permitted to build</name>
  <files>.planning/phases/236-flake-root-cause-reproduce-name-fix/236-01-SUMMARY.md</files>
  <decision>Which of D-05's three branches does the captured trace show, and therefore which downstream path is Phase 236 authorized to build?</decision>
  <options>
    <option name="(a) product race — `actor=` absent from the URL entirely, no navigation and no `live_patch` frame after the Enter keydown">Confirms D-01 and the C-6 `unload()` mechanism. Plan 236-02 proceeds exactly as written: six anchors to `<.link patch>`, `phx-submit="apply_filters"` on the form, one new `handle_event/3` returning `push_patch`. This is the expected branch.</option>
    <option name="(b) connect-patch value wipe — `actor=` present but EMPTY">A DIFFERENT cause. The form would need `phx-change` value ownership, not just submit ownership — which D-09 currently rejects on evidence that does not apply under this branch. Plan 236-02 is invalid as written. HALT and re-plan 236-02; do not improvise a `phx-change` into it. Plan 236-03 still proceeds — SC-4 is branch-independent.</option>
    <option name="(c) harness actionability — no `keydown` reaches the document at all">No `lib/` change is warranted. Plan 236-02 is dropped entirely and GREEN-02 goes to the SC-5 quarantine branch, whose design is recorded in `<sc5_contingency_design>` in plan 236-04 and would then be BUILT rather than specified. Plan 236-03 still proceeds and is owed MORE, not less: D-33 makes retry-wrapping never the fallback, so on this branch the `p17` guard is the thing standing between a quarantine and a retry-wrap.</option>
  </options>
  <resume-signal>The developer states the branch letter — `(a)`, `(b)` or `(c)` — and confirms the downstream path. On `(a)` wave 2 is unblocked and execution continues with plans 236-02 and 236-03. On `(b)` plan 236-02 halts for re-planning. On `(c)` plan 236-02 is dropped and the SC-5 contingency is built. Plan 236-03 is UNBLOCKED ON ALL THREE BRANCHES and is never dropped: SC-4 (the `p17` retry-wrapper guard and the `PLAYWRIGHT_RETRIES: 1` deletion) is branch-independent, and on `(c)` it matters more rather than less, because D-33 makes retry-wrapping never the fallback even when the quarantine is the close. On `(b)` and `(c)`, 236-03 proceeds while 236-02 is re-planned or dropped.</resume-signal>
  <read_first>
    - `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-DIAGNOSIS.md` (written in Task 2)
    - `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-RESEARCH.md` — "What to read out of the trace (D-05's decision tree)"
  </read_first>
  <action>
Present the captured received URL string, the trace path, and the Task 2 branch call to the
developer, and confirm which downstream path is authorized. The three branches and their
consequences, stated so the decision is legible:

- **(a) No `actor=` key in the URL at all; no navigation / no `live_patch` frame after the Enter
  keydown.** → D-01 product race, mechanism C-6. Plan 236-02 proceeds exactly as written (six
  anchors to `<.link patch>`, `phx-submit` on the form, one new `handle_event` → `push_patch`).
  This is the expected branch.
- **(b) `actor=` present but EMPTY.** → connect-patch value wipe, a DIFFERENT cause. The form needs
  `phx-change` value ownership, not just submit ownership — which D-09 currently rejects on
  evidence that no longer applies under this branch. **Plan 236-02 is invalid as written and the
  phase must be re-planned.** Halt; do not improvise a `phx-change` into 236-02.
- **(c) No `keydown` reaches the document at all.** → pure harness actionability. **No `lib/` change
  is warranted.** Plan 236-02 is dropped entirely and GREEN-02 goes to the SC-5 quarantine branch
  (`<sc5_contingency_design>` in plan 236-04), which is then BUILT rather than specified.

This is a `checkpoint:decision` and not merely informational because branches (b) and (c) invalidate
an already-written plan, and because the ROADMAP makes SC-1 a gate rather than a formality. Wait for
the developer's confirmation before any wave-2 work starts.
  </action>
  <verify>
    <human-check>Developer confirms the branch letter and authorizes (a) → proceed to 236-02 / (b) → halt and re-plan / (c) → drop 236-02 and build the SC-5 contingency.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer has seen the verbatim received URL string and the trace path.
    - One branch letter is recorded in the plan SUMMARY as the authorized path.
    - No file under `lib/` has been modified at this point: `git status --porcelain lib/` is empty.
  </acceptance_criteria>
  <done>The authorized branch is recorded and wave 2 is unblocked (or the phase is halted for re-planning).</done>
  <reversibility rating="costly">Choosing branch (a) commits waves 2-3 to the D-08 fix shape. Wrong on (b) and the work is discarded and re-planned; wrong on (c) and a `lib/` change ships for a harness problem — which is why the trace, not the diagnosis, decides.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → generated host on `:4017` | The repro boots a real Phoenix app with a seeded platform admin; credentials live in the smoke script's defaults |
| repo working tree → committed evidence | A temporary diagnostic edit (Path B's `trace: 'on'`) could leak into a commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-236-01 | Information Disclosure | `236-EVIDENCE.md` / `236-DIAGNOSIS.md` | medium | mitigate | The received URL and trace metadata may contain a seeded admin UUID and a local absolute path. Sigra is a PUBLIC repo — record the seeded demo UUID (it is ephemeral fixture data, not adopter PII) but redact nothing else by reflex; before committing, grep the two new files for `password`, `secret`, `token`, `gho_`, `HEX_API_KEY` and confirm zero hits. |
| T-236-02 | Tampering | `test/example/priv/playwright/playwright.config.ts` | medium | mitigate | Path B's temporary `trace: 'on'` must never reach a commit. Path A is mandated precisely because it needs no config edit; the Task 1 acceptance criterion asserts `git status --porcelain` on the config is empty. |
| T-236-03 | Repudiation | the SC-1 RED claim | high | mitigate | A prose claim of "reproduced" with no artifact is unfalsifiable. Mitigated by requiring the absolute trace.zip path, the repeat index and the verbatim received URL in the ledger, parsed by `parseEvidenceSlots` and gated by `p12`. |
| T-236-04 | Denial of Service | local Postgres / port 4017 | low | accept | The repro contends for a local port and DB. Accepted: single-developer local run, documented in CLAUDE.md, no shared resource. |
| T-236-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs **zero** new packages (RESEARCH §Package Legitimacy Audit: "Not applicable"). `npm ci` and `npx playwright install` in the precondition restore already-locked versions from the committed `package-lock.json`; no new dependency enters the lockfile, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `236-EVIDENCE.md` parses under `parseEvidenceSlots` with a `BEFORE-FLAKE-RED` slot.
- `node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` exits 0.
- `git status --porcelain lib/ test/example/priv/playwright/` is empty — this plan ships evidence, not code.
- The developer has confirmed the D-05 branch letter.
</verification>

<success_criteria>
- ROADMAP SC-1 is satisfied: a captured RED exists, recorded with its local artifact path.
- ROADMAP SC-2's first half is satisfied: a written differential diagnosis names the cause.
- Wave 2 is authorized (or the phase is halted with a stated reason).
</success_criteria>

<output>
Create `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-01-SUMMARY.md` when done.
</output>
