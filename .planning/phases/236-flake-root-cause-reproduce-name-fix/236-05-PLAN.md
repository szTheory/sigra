---
phase: 236-flake-root-cause-reproduce-name-fix
plan: 05
type: execute
wave: 4
depends_on: [236-04]
files_modified:
  - scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
  - test/fixtures/prohibitions/p12-phase236-claim-without-run-id.md
  - test/sigra/planning/phase_236_evidence_provenance_guard_test.exs
  - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md
  - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-SECURITY.md
autonomous: true
gap_closure: true
gap_ids: [T-236-03, T-236-15]
requirements: [GREEN-01, GREEN-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The default `p12-run-id-provenance.test.mjs` invocation validates both the Phase 230 and Phase 236 evidence ledgers, so Phase 236's RED and five-run GREEN claims are mechanically checked by the existing `fast_checks` prohibition glob (T-236-03, T-236-15)."
    - "Phase 230's contract is not weakened: its ledger still has a >=4-slot floor, a >=3-captured-slot floor, run IDs in every captured `Status:` line, a producing command in every captured slot, and status/body run-ID corroboration."
    - "Phase 236's three slots all satisfy the same captured-evidence grammar; `BEFORE-FLAKE-RED` and `AFTER-FIX-GREEN` retain their real run IDs, while `AFTER-P17-GUARD-OBSERVED` gains an honestly verified `Fast checks` CI receipt instead of earning an exception to the grammar."
    - "The existing Phase 230 known-bad fixture and a new Phase 236-shaped known-bad fixture both make `p12` exit non-zero with a named provenance failure; each clean ledger exits zero when substituted directly."
    - "The guard remains deterministic and offline in CI: no GitHub API call is added to the Node test, no workflow file changes, and no `mix ci` topology change."
    - "After the provenance guard and its independent contract pass, the authoritative Phase 236 security artifact closes T-236-03 and T-236-15, records zero blocking threats, and carries verified sign-off."
  artifacts:
    - path: scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
      provides: "Multi-ledger provenance enforcement with ledger-specific non-vacuity floors"
    - path: test/fixtures/prohibitions/p12-phase236-claim-without-run-id.md
      provides: "Committed Phase 236-shaped negative control for an unproven AFTER-FIX-GREEN claim"
    - path: test/sigra/planning/phase_236_evidence_provenance_guard_test.exs
      provides: "Independent source contract pinning both ledgers and the existing CI route"
    - path: .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md
      provides: "Run-backed provenance for all three captured Phase 236 slots"
    - path: .planning/phases/236-flake-root-cause-reproduce-name-fix/236-SECURITY.md
      provides: "Authoritative verified security disposition with T-236-03 and T-236-15 closed and `threats_open: 0`"
  key_links:
    - from: .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md
      to: scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
      via: "Phase 236 entry in the guard's default ledger-spec table"
      pattern: "236-flake-root-cause-reproduce-name-fix/236-EVIDENCE\\.md"
    - from: scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
      to: scripts/ci/prohibitions/_lib.mjs
      via: "archive-aware `readSubject()` plus `parseEvidenceSlots()`; injected fixtures remain the single substitutable subject"
      pattern: "readSubject|parseEvidenceSlots"
    - from: scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
      to: .github/workflows/ci.yml
      via: "the existing `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` fast-checks glob"
      pattern: "scripts/ci/prohibitions/\\*\\.test\\.mjs"
    - from: .planning/phases/236-flake-root-cause-reproduce-name-fix/236-SECURITY.md
      to: scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
      via: "Task 3 records verified closure only after the default provenance guard and Phase 236 contract both pass"
      pattern: "T-236-03|T-236-15|threats_open: 0|status: verified"
  prohibitions:
    - statement: "MUST NOT let either Phase 236's captured RED or its five-run GREEN claim lose the run ID, producing command, or in-slot corroboration that makes the claim reproducible."
      status: resolved
      verification: test
      check_kind: node-test
      check_target: scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
      check_violation_fixture: test/fixtures/prohibitions/p12-phase236-claim-without-run-id.md
      check_clean_fixture: .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md
      reason: "T-236-03 and T-236-15 are high-severity repudiation blockers: direct parsing during a one-time verification does not put the Phase 236 ledger on CI's standing enforcement path."
    - statement: "MUST NOT relax Phase 230's p12 grammar or replace its existing known-bad control while adding Phase 236 coverage."
      status: resolved
      verification: test
      reason: "The remediation is additive. The Phase 230 ledger and `p12-claim-without-run-id.md` remain explicit clean/RED subjects in the verification matrix."
    - statement: "MUST NOT add network access to the Node guard, edit `.github/workflows/ci.yml`, or add prohibition guards to `mix ci`."
      status: resolved
      verification: review
      reason: "The standing guard must remain hermetic and already has a CI route through the fast-checks glob; workflow or alias edits would expand scope and reopen the single-owner topology constraint."
---

<objective>
Close security blockers T-236-03 and T-236-15 by making Phase 236's evidence ledger a first-class
subject of the standing `p12` provenance guard without weakening Phase 230.

Purpose: Phase 236's verifier parsed `236-EVIDENCE.md` directly, but the CI guard still hard-codes
the Phase 230 ledger. That leaves the captured RED and five-run GREEN claims mechanically
unprotected after verification. The repair must remain offline, fail non-vacuously, and use the
existing `fast_checks` glob rather than creating another CI path.

Output: a multi-ledger `p12` guard, an honestly run-backed third Phase 236 slot, a Phase 236-shaped
known-bad fixture, an ExUnit contract pinning the two ledgers and their CI route, and a verified
Phase 236 security artifact with both blocking provenance threats closed.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-CONTEXT.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-RESEARCH.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-SECURITY.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-VERIFICATION.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-01-SUMMARY.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-03-SUMMARY.md
@.planning/phases/236-flake-root-cause-reproduce-name-fix/236-04-SUMMARY.md
@scripts/ci/prohibitions/_lib.mjs
@scripts/ci/prohibitions/p01-committed-method-provenance.test.mjs
@scripts/ci/prohibitions/p12-run-id-provenance.test.mjs
@test/fixtures/prohibitions/p12-claim-without-run-id.md
@test/sigra/planning/phase_236_retry_wrapper_prohibition_test.exs
@.github/workflows/ci.yml

<interfaces>
- `readSubject(defaultRelPath)` honors one `GSD_PROHIB_SUBJECT` override and otherwise resolves a
  live phase path or its archived milestone copy. Preserve that single-subject fail-first seam.
- `parseEvidenceSlots(text)` returns every `## BEFORE-*` / `## AFTER-*` slot with `statusRaw`,
  `captured`, `pending`, `statusRunIds`, all body `runIds`, and fenced blocks; it throws on zero
  parsed slots. Do not duplicate or fork this parser.
- Phase 230 currently needs floors of at least four slots and at least three captured slots.
- Phase 236 has exactly three captured slots: `BEFORE-FLAKE-RED`,
  `AFTER-P17-GUARD-OBSERVED`, and `AFTER-FIX-GREEN`.
- `.github/workflows/ci.yml` already runs every `scripts/ci/prohibitions/*.test.mjs` file in the
  `Fast checks (milestone/installer/contracts/snapshot/ledger guards)` job. No wiring edit exists.
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage | Status |
|--------|------|----------|--------|
| GOAL | Named, fixed flake rather than retry-wrapped green | 236-01..04 deliver it; 236-05 makes the RED/GREEN provenance durable in CI and Task 3 records authoritative security closure | COVERED |
| REQ | GREEN-01 captured RED before the fix | Task 1 registers `BEFORE-FLAKE-RED` under p12 and preserves its real run receipt | COVERED |
| REQ | GREEN-02 fixed race and retry-wrapper prohibition | Task 1 preserves the p17 local RED/GREEN record and adds its real fast-checks run receipt | COVERED |
| RESEARCH | Evidence ledger uses `parseEvidenceSlots`; guards run through the fast-checks glob; zero new dependencies | Tasks 1-2 reuse those exact seams and prohibit workflow/alias/package changes | COVERED |
| CONTEXT | D-05, D-20, D-21: the trace and API observations decide the claim and carry run/artifact provenance | Task 1 validates those already-recorded receipts without changing their conclusions | COVERED |
| CONTEXT | D-27 and standing constraints 5-6: guards use the existing Node glob and are observed RED against committed fixtures | Tasks 1-2 keep the glob untouched and prove both Phase 230 and Phase 236 negative controls RED | COVERED |
| SECURITY | T-236-03 and T-236-15 remain blocking until Phase 236 is under the standing provenance guard | Tasks 1-2 implement and prove the guard; Task 3 then closes both rows and records `threats_open: 0` with verified sign-off | COVERED |
| CONTEXT | D-01..D-33 remainder | Already implemented or explicitly deferred by 236-01..04; this additive security closure does not reopen or alter any product decision | COVERED |
| CONTEXT | Deferred ideas | Excluded: no other LiveView surface, template parity, admin-filter convergence, or interface work appears here | EXCLUDED |
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire Phase 236 through p12 end-to-end without weakening Phase 230</name>
  <precondition>`gh run view 35034938082 --json jobs` still resolves and contains the exact job `Fast checks (milestone/installer/contracts/snapshot/ledger guards)` with conclusion `success`; halt rather than attaching an unverified run receipt if that fact is false.</precondition>
  <files>scripts/ci/prohibitions/p12-run-id-provenance.test.mjs, test/fixtures/prohibitions/p12-phase236-claim-without-run-id.md, .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md</files>
  <behavior>
    - RED 1: after Phase 236 is added to the default ledger table but before its evidence is corrected, the default p12 invocation fails specifically on `AFTER-P17-GUARD-OBSERVED`'s bare `Status: captured`, absent status run ID, and lack of a `gh`/committed-instrument receipt.
    - RED 2: substituting `p12-claim-without-run-id.md` exits non-zero, preserving the Phase 230 negative control.
    - RED 3: substituting `p12-phase236-claim-without-run-id.md` exits non-zero and names `AFTER-FIX-GREEN`'s missing run ID rather than a parse-floor failure.
    - GREEN 1: the default invocation validates both real ledgers and exits zero.
    - GREEN 2: substituting either clean Phase 230 or clean Phase 236 ledger exits zero.
    - Invariant: Phase 230 retains >=4 parsed slots and >=3 captured slots; Phase 236 uses >=3 and >=3; an injected fixture uses >=3 and >=3 so both committed negative controls reach the substantive checks.
  </behavior>
  <action>
Start with the guard/fixture half so the newly exposed Phase 236 defect is observed RED before the
ledger is repaired.

Refactor `p12-run-id-provenance.test.mjs` from one `LEDGER` constant and one module-level `slots`
array into an explicit default ledger-spec table. The Phase 230 entry names
`.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md` with `minSlots: 4` and
`minCaptured: 3`. The Phase 236 entry names
`.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md` with `minSlots: 3` and
`minCaptured: 3`. When `GSD_PROHIB_SUBJECT` is set, validate only that injected subject with
`minSlots: 3` and `minCaptured: 3`, preserving the one-substitutable-subject contract used by
`check prohibition-enforcement`; when it is absent, validate both default ledgers through
`readSubject()` so milestone archival remains transparent.

Keep every substantive p12 rule strict for every ledger: allowed statuses remain
`captured (run ...)`, `captured (runs ...)`, or `pending (...)`; every captured slot has at least
one 8-12 digit run ID in its Status; every captured slot has a fenced command invoking
`ci-run-metrics.sh` or `gh`; and every Status run ID appears at least twice in that slot body.
Keep the pending-obligation rule. Do not add a path-wide or slot-name exception, do not make bare
`Status: captured` valid, and do not add a network call to the guard. Include the ledger label and
slot name in every failure so a two-ledger CI failure is immediately actionable.

Create `test/fixtures/prohibitions/p12-phase236-claim-without-run-id.md` as a structurally valid,
three-slot Phase 236 ledger. Give the first two slots valid captured run statuses, repeated body IDs,
and fenced `gh run` commands. Make only `AFTER-FIX-GREEN` defective: it claims capture while its
Status lacks a run ID. The fixture must clear the >=3 slot and >=3 captured floors so the observed
RED comes from the provenance violation, not `the parse broke, this is not a pass`.

Run the refactored default guard now, before editing the real ledger, and require a non-zero exit
naming `AFTER-P17-GUARD-OBSERVED` for the three defects listed in RED 1. If it passes, Phase 236 is
still not in the default path; if it fails only on a floor, the per-ledger configuration is wrong.

Then make the real third slot honestly satisfy the unchanged grammar. First execute the precondition
query and retain its JSON output. In `236-EVIDENCE.md`, change both the summary-table status and the
slot's Status line from bare `captured` to `captured (run `35034938082`)`. In that same slot, add a
fenced `gh run view 35034938082 --json jobs --jq ...` command selecting the exact Fast checks job,
record its `success` conclusion, and mention run `35034938082` again in prose so status/body
corroboration is real. State precisely what the receipt proves: that the existing prohibition glob,
which includes p17, passed in CI. Preserve the local known-bad RED command, exit code, and named
failure message because the CI receipt proves the GREEN route but does not replace the observed RED.

Finish by running the default guard, both clean-ledger substitutions, and both committed bad-fixture
substitutions. The two bad-fixture commands must exit non-zero with named run-provenance messages;
an unexpected zero or a floor-only failure is a task failure.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs</automated>
    <fails_when>non-zero exit, or the TAP output does not show all Phase 230 and Phase 236 subtests passing</fails_when>
    <automated>GSD_PROHIB_SUBJECT=.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs &amp;&amp; GSD_PROHIB_SUBJECT=.planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs</automated>
    <fails_when>either clean ledger substitution exits non-zero</fails_when>
  </verify>
  <done>The standing p12 guard validates both real ledgers offline, Phase 236's three captured slots are run-backed, and both Phase 230 and Phase 236 negative controls have been observed failing for substantive provenance reasons.</done>
  <reversibility rating="reversible">The guard, fixture, and evidence receipt are four local files with no schema, dependency, workflow, or published-interface change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the dual-ledger contract and execute the full deterministic matrix</name>
  <files>test/sigra/planning/phase_236_evidence_provenance_guard_test.exs</files>
  <behavior>
    - Test 1: the p12 source names both exact ledger paths; removing either path fails the contract.
    - Test 2: the p12 source retains explicit Phase 230 floors of 4/3 and Phase 236 floors of 3/3, and the new Phase 236 known-bad fixture exists.
    - Test 3: `236-EVIDENCE.md` has exactly the three expected slot headings and `AFTER-P17-GUARD-OBSERVED` has a run-backed Status naming `35034938082`.
    - Test 4: `.github/workflows/ci.yml` retains the literal prohibition glob under fast checks, while `mix.exs` contains no `scripts/ci/prohibitions` reference.
    - Test 5: each structural count has a positive floor and a failure message ending `the parse broke, this is not a pass`.
  </behavior>
  <action>
Create `test/sigra/planning/phase_236_evidence_provenance_guard_test.exs` following
`phase_236_retry_wrapper_prohibition_test.exs`: `use ExUnit.Case, async: true`, `File.read!/1`,
named path module attributes, and explanatory assertion messages. Keep it a source/route contract;
do not shell out to Node from ExUnit and create cross-runtime coupling in the library-test job.

Assert the exact Phase 230 and Phase 236 ledger paths are both present in p12; the ledger-spec
floors remain explicit and asymmetric (4/3 for Phase 230, 3/3 for Phase 236); the new fixture exists;
and the real Phase 236 ledger contains exactly the expected three parsed headings with the p17 slot
run-backed by `35034938082`. Assert the workflow still contains the literal
`node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` command and `mix.exs` still has
no reference to `scripts/ci/prohibitions`. Count only parsed `## BEFORE-*` / `## AFTER-*` headings,
not every Markdown heading, and include a >=3 positive floor so a parser matching nothing cannot
report success.

After the ExUnit contract is green, run the behavioral matrix outside ExUnit: default p12 GREEN;
clean Phase 230 and Phase 236 substitutions GREEN; existing Phase 230 fixture RED; new Phase 236
fixture RED naming `AFTER-FIX-GREEN`; then the complete prohibition glob GREEN. Capture the two RED
outputs before their temporary files disappear and reject a failure whose only message is a parse
floor. This is deterministic and offline; the one live `gh` observation was already recorded in
Task 1 and is data consumed by the guard, never an operation performed by CI.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_236_evidence_provenance_guard_test.exs</automated>
    <fails_when>non-zero exit, any failed assertion, or the slot parser matches fewer than three headings</fails_when>
    <automated>phase230_red_log=$(mktemp -t phase230-p12-red); if GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p12-claim-without-run-id.md node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs &gt;"$phase230_red_log" 2&gt;&amp;1; then echo "Phase 230 negative control unexpectedly passed"; exit 1; fi; grep -Eq 'BEFORE-PR.*(run ID|Status)' "$phase230_red_log"</automated>
    <fails_when>the existing Phase 230 fixture exits zero, or its failure is not the named missing-run provenance violation</fails_when>
    <automated>phase236_red_log=$(mktemp -t phase236-p12-red); if GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p12-phase236-claim-without-run-id.md node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs &gt;"$phase236_red_log" 2&gt;&amp;1; then echo "Phase 236 negative control unexpectedly passed"; exit 1; fi; grep -Eq 'AFTER-FIX-GREEN.*(run ID|Status)' "$phase236_red_log"</automated>
    <fails_when>the Phase 236 fixture exits zero, reports only a parse-floor failure, or does not name AFTER-FIX-GREEN's provenance defect</fails_when>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs &amp;&amp; mix format --check-formatted test/sigra/planning/phase_236_evidence_provenance_guard_test.exs</automated>
    <fails_when>any prohibition test fails, or the new ExUnit contract is not formatted</fails_when>
  </verify>
  <done>The independent contract pins both ledgers and the unchanged CI route, both negative controls are observed RED, both clean ledgers and the full prohibition suite are GREEN, and no workflow/alias change exists.</done>
  <reversibility rating="reversible">The contract is a local test file and can be removed with the accompanying guard generalization in one revert.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Synchronize the authoritative security disposition after provenance verification</name>
  <precondition>The default p12 provenance guard and `phase_236_evidence_provenance_guard_test.exs` both exit zero against the current worktree; assert both at task start and halt without editing `236-SECURITY.md` if either command fails.</precondition>
  <files>.planning/phases/236-flake-root-cause-reproduce-name-fix/236-SECURITY.md</files>
  <action>
Run `node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` and
`MIX_ENV=test mix test test/sigra/planning/phase_236_evidence_provenance_guard_test.exs` before
changing the security artifact. Treat either non-zero exit as a hard stop: retain the artifact's
blocked state and do not claim mitigation closure from source inspection alone.

Only after both commands pass, update the authoritative `236-SECURITY.md` disposition. Set
frontmatter `status: verified` and `threats_open: 0`. Change the T-236-03 and T-236-15 threat-register
rows from `open` to `closed` while preserving their high severity, mitigate disposition, and stated
controls. Leave the documented low-severity accepted entries intact; they are non-blocking risk
records, not failed mitigations. Append a Security Audit Trail row dated on execution that reports
20 total threats, 15 closed threats, and 5 low-severity entries with 0 blocking threats, and identify
the automated p12 plus Phase 236 contract run as the verification authority.

Replace the stale Blocking Findings text with a resolved-blocking-findings record that names the
two successful commands, explains that Phase 236 is now a default p12 subject without weakening
Phase 230, and states that T-236-03/T-236-15 are closed. Mark every Sign-Off checkbox complete and
set Approval to verified with the same automated evidence basis. Do not alter unrelated threat
dispositions or erase the original audit row; the artifact must retain the review history that led
to this remediation.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_236_evidence_provenance_guard_test.exs &amp;&amp; elixir -e 'path = ".planning/phases/236-flake-root-cause-reproduce-name-fix/236-SECURITY.md"; text = File.read!(path); checks = [{"verified status", Regex.match?(~r/^status: verified$/m, text)}, {"zero open blockers", Regex.match?(~r/^threats_open: 0$/m, text)}, {"T-236-03 closed", Regex.match?(~r/^\| T-236-03 \|.*\| closed \|$/m, text)}, {"T-236-15 closed", Regex.match?(~r/^\| T-236-15 \|.*\| closed \|$/m, text)}, {"blocking sign-off", String.contains?(text, "- [x] Blocking mitigations verified")}, {"zero-threat sign-off", String.contains?(text, "- [x] `threats_open: 0` confirmed")}, {"verified approval", String.contains?(text, "**Approval:** verified")}]; failed = for {name, false} &lt;- checks, do: name; case failed do [] -&gt; :ok; _ -&gt; IO.puts(:stderr, "missing security closure: #{Enum.join(failed, ", ")}"); System.halt(1) end'</automated>
    <fails_when>either automated provenance command fails, either blocking threat remains open, frontmatter does not report verified/zero, or the sign-off and approval are incomplete</fails_when>
  </verify>
  <done>`236-SECURITY.md` retains its audit history, closes T-236-03 and T-236-15 only after automated proof, reports `status: verified` and `threats_open: 0`, and contains complete verified sign-off with no unresolved blocking threat.</done>
  <reversibility rating="reversible">The task updates one documentation artifact from blocked to verified based on repeatable local commands; rerunning the audit can restore a blocked disposition if the guard regresses.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence Markdown → p12 parser | Human-authored claims become machine-enforced run provenance through `parseEvidenceSlots()` |
| `GSD_PROHIB_SUBJECT` → p12 guard | A committed known-bad or clean fixture replaces the default subjects during fail-first enforcement |
| p12 guard → fast-checks result | An offline Node test gates CI through the existing prohibition glob |
| GitHub Actions receipt → committed ledger | One already-recorded public run identity substantiates the p17 slot's CI GREEN route |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-236-03 | Repudiation | `BEFORE-FLAKE-RED` provenance | high | mitigate | Register Phase 236 in p12's default subjects; require its Status run ID, producing command, and duplicate in-slot occurrence on every CI run; then synchronize the passing result into `236-SECURITY.md`. |
| T-236-15 | Repudiation | `AFTER-FIX-GREEN` five-run claim | high | mitigate | Apply the same strict rules to all five Status run IDs and their `gh run` command/body receipts; the new Phase 236 fixture proves a missing run ID fails non-vacuously, after which `236-SECURITY.md` records closure. |
| T-236-20 | Tampering | Phase 230 p12 coverage | high | mitigate | Preserve the 4/3 floors and every original assertion; execute the existing Phase 230 bad fixture and clean ledger alongside the Phase 236 pair. |
| T-236-21 | Spoofing | p17 CI receipt | medium | mitigate | Before recording run `35034938082`, query its exact Fast checks job and require `success`; repeat the run ID in the slot body so p12 can corroborate it structurally. |
| T-236-22 | Denial of Service | fast-checks runtime | low | accept | One guard reads two small Markdown ledgers instead of one and remains offline; no network, subprocess, dependency, or workflow fan-out is added. |
| T-236-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages or lockfiles change; the implementation uses built-in `node:test`, existing `_lib.mjs`, and ExUnit. |
</threat_model>

<verification>
- `node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` exits 0 while reporting both default ledgers.
- Clean substitutions for Phase 230 and Phase 236 each exit 0.
- The existing Phase 230 fixture and new Phase 236 fixture each exit non-zero on a named missing-run provenance defect, not a parse floor.
- `MIX_ENV=test mix test test/sigra/planning/phase_236_evidence_provenance_guard_test.exs` exits 0.
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` and the targeted `mix format --check-formatted` command exit 0.
- After those commands pass, `236-SECURITY.md` reports `status: verified`, `threats_open: 0`, closed T-236-03/T-236-15 rows, and completed verified sign-off.
- The union of `git show --name-only --format= <task-commit>` for this plan's task commits contains only the five declared files; `.github/workflows/ci.yml`, `mix.exs`, product code, and lockfiles are unchanged.
</verification>

<success_criteria>
- T-236-03 is closed: Phase 236's RED provenance is mechanically enforced in the standing CI guard.
- T-236-15 is closed: the five-run GREEN claim cannot lose its Status run IDs, producing commands, or body corroboration without failing CI.
- The authoritative security artifact agrees with the passing controls: both blocking rows are closed, `threats_open` is zero, and the phase security status and approval are verified.
- Phase 230 retains its original p12 coverage and its original known-bad fixture remains demonstrably RED.
- Both new and existing negative controls are observed RED, all clean/default paths are GREEN, and the guard remains hermetic.
</success_criteria>

<output>
Create `.planning/phases/236-flake-root-cause-reproduce-name-fix/236-05-SUMMARY.md` when done.
</output>
