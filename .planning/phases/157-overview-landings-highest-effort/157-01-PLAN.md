---
phase: 157-overview-landings-highest-effort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/live/index_live.ex
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "Global Overview mount/3 is split: disconnected path assigns loading=true, no DB query; connected path calls Query.summary_counts/2, assigns loading=false"
    - "alarm <.notice> is the first child after <header class=sg-page-header> — above the task_card grid — with tone={:risk} when needs_review > 0 and tone={:ok} when 0"
    - "alarm notice carries role=status via :rest only in the non-loading branch; skeleton state does NOT render the alarm notice (or renders without role)"
    - "old sg-posture-strip__risk anchor block (lines 61-65) is deleted"
    - "posture strip <section> carries aria-busy=true when loading; the attribute is absent when data renders"
    - "skeleton shapes in the posture strip cluster replace the 6 stat_link elements during loading (6 <.skeleton> children in the sg-cluster sg-cluster--3)"
    - "task_card grid appears BEFORE posture strip in rendered HTML (archetype order: header → alarm → task grid → posture strip → capability)"
    - "sg-posture-strip__risk is absent from rendered HTML"
  artifacts:
    - path: "lib/sigra/admin/live/index_live.ex"
      provides: "Redesigned Global Overview with connected?-gate deferred load and front-door archetype"
      contains: "connected?(socket)"
  key_links:
    - from: "lib/sigra/admin/live/index_live.ex mount/3"
      to: "Query.summary_counts/2"
      via: "connected? gate — called only on connected mount"
      pattern: "if connected\\?\\(socket\\)"
    - from: "lib/sigra/admin/live/index_live.ex render/1"
      to: "sg-posture-strip section"
      via: "aria-busy attribute driven by @loading assign"
      pattern: "aria-busy"
---

<objective>
Redesign `lib/sigra/admin/live/index_live.ex` (Global Overview) into the front-door archetype: split `mount/3` with `connected?(socket)` gate for deferred load (LAND-04), promote the needs-review alarm `<.notice>` as the first element above the task grid (LAND-01), reorder to the canonical archetype (LAND-02/03), and delete the old `sg-posture-strip__risk` alarm anchor.

Purpose: LAND-01 through LAND-04 require this screen to lead with a loud/calm alarm, verb-first task cards as primary content, a demoted posture strip, and a skeleton loading state — not an empty flash.

Output: Redesigned `index_live.ex` with two-state render (skeleton → data), front-door archetype, and no old alarm anchor.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md

@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-UI-SPEC.md
@/Users/jon/projects/sigra/.planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md

<interfaces>
<!-- Existing GlobalOverview LiveView — full source. -->

From lib/sigra/admin/live/index_live.ex (current):
- mount/3 lines 12-22: synchronous — calls Query.summary_counts/2 unconditionally. Must be split.
- render/1 lines 25-114: current archetype order: header(30-37) → task_card grid(39-58) → posture strip with sg-posture-strip__risk anchor(60-95) → capability section(97-111)
- DELETE lines 61-65: `<a href="/admin/users?locked=true" class="sg-cluster sg-cluster--start sg-posture-strip__risk"> ... </a>`
- needs_review/1 lines 128-130: locked + deleted count; unchanged.
- runtime_config!/0 lines 132-145: call BEFORE connected? gate (needed for both mounts).

From lib/sigra/admin/components.ex (compose only):
- skeleton/1 (line 327): `<div class={["sg-skeleton", @class]} {@rest}></div>` — pass class for sizing
- notice/1 (line 301-307): `<div class="sg-notice" data-tone={@tone} {@rest}><p class="sg-text-sm">{render_slot(@inner_block)}</p></div>`
  CRITICAL: slot content must be INLINE only — notice wraps slot in <p>; any block <p> child = nested <p><p> = axe violation (D-07)
  Pass role="status" via {:rest} spread: `<.notice tone={...} role="status">` — only when not loading (D-02)
- stat_link/1 (line 48-55): renders `<a class="sg-metric-link"><span class="sg-metric-link__label">...</span><span class="sg-metric-link__value">...</span></a>`
  .sg-metric-link__value is the Playwright wait selector — ABSENT in skeleton, PRESENT in data state (A2)

CSS boundaries (compose only — no new classes):
- sg-stack sg-stack--6: top-level section container (unchanged)
- sg-grid sg-grid--3: task card grid (unchanged)
- sg-card sg-posture-strip sg-stack sg-stack--3: posture strip section class (unchanged)
- sg-cluster sg-cluster--3: stat_link cluster inside posture strip (unchanged)
- sg-skeleton: shimmer class (prefers-reduced-motion safe via app.css:1463-1473)
- sg-notice[data-tone="risk"]: inset risk bar + risk-soft fill (app.css:960-967,972-978)
- sg-notice[data-tone="ok"]: ok-soft fill

Connected?-gate precedent:
- test/example/lib/example_web/live/confirmation_live.ex:103 uses `if connected?(socket) do ... else ... end`
- In mount/3 both branches return {:ok, socket}
- runtime_config!() MUST be called OUTSIDE the if block (before the gate) — it raises on misconfiguration and must fire on both mounts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split mount/3 with connected? gate and add loading assign</name>
  <files>lib/sigra/admin/live/index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/index_live.ex — lines 12-22 (current mount to replace)
    - test/example/lib/example_web/live/confirmation_live.ex — line 103 (connected? gate precedent; mount branch structure)
  </read_first>
  <action>
    Replace the current `mount/3` in `lib/sigra/admin/live/index_live.ex` (lines 12-22) with a two-branch structure using `connected?(socket)`.

    Structure (per D-01 and Landmine 5 in RESEARCH.md):
    1. Call `config = runtime_config!()` BEFORE the `if connected?(socket)` check — config is needed for both paths and raises fast on misconfiguration regardless of mount type.
    2. Assign `admin_scope = socket.assigns.admin_scope` before the gate.
    3. `if connected?(socket)` branch (connected mount): call `Query.summary_counts(config, admin_scope)`, assign `loading: false` and `summary_counts` to the result, assign `page_title: "Global overview"`, assign `sigra_config: config`. Return `{:ok, socket}`.
    4. `else` branch (disconnected/HTTP mount): assign `loading: true`, `summary_counts: %{}`, `page_title: "Global overview"`, `sigra_config: config`. Return `{:ok, socket}`.

    Hard-fail guards:
    - Do NOT call `Query.summary_counts/2` in the else (disconnected) branch — that would make LAND-04 a no-op.
    - Do NOT move `runtime_config!()` inside either branch — it must be called unconditionally.
    - Both branches must return `{:ok, socket}` (not `{:noreply, socket}`).
  </action>
  <verify>
    <automated>mix test test/example/test/example_web/admin_shell_test.exs</automated>
  </verify>
  <done>
    mix test passes for admin_shell_test.exs. No KeyError on @summary_counts in existing tests. The `if connected?(socket)` pattern is present in mount/3 with runtime_config!() before the branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign render/1 — front-door archetype, alarm, skeleton shapes, delete old anchor</name>
  <files>lib/sigra/admin/live/index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/index_live.ex — lines 25-114 (full render to replace)
    - lib/sigra/admin/components.ex — lines 264-331 (notice/1 and skeleton/1 contracts; notice wraps slot in `<p class="sg-text-sm">` at line 304 — inline slot content ONLY)
    - .planning/phases/157-overview-landings-highest-effort/157-UI-SPEC.md — Component Inventory archetype rhythm block; Copywriting Contract table (alarm copy); Interaction & Loading Contract (skeleton shapes section)
    - .planning/phases/157-overview-landings-highest-effort/157-RESEARCH.md — Landmine 1 (nested-p), Landmine 2 (flake), Landmine 3 (live-region), Landmine 4 (sg-posture-strip__risk deletion), Landmine 8 (skeleton shapes)
  </read_first>
  <action>
    Rewrite `render/1` in `lib/sigra/admin/live/index_live.ex` to implement the front-door archetype (D-04) and deferred load state (D-01/D-02).

    The `assigns` precompute at the top of render/1 keeps the `needs_review` derivation:
    `assigns = assign(assigns, :needs_review, needs_review(assigns.summary_counts))`

    Archetype structure inside `<section class="sg-stack sg-stack--6">`:

    ITEM 1 — `<header class="sg-page-header">` (unchanged from current lines 30-37):
    - kicker "Admin overview", title "What do you need to do?", copy unchanged.

    ITEM 2 — alarm `<.notice>` (NEW — before task grid, per D-03):
    - Render only when NOT loading: `:if={not @loading}`
    - `tone={if @needs_review > 0, do: :risk, else: :ok}` (per D-03)
    - Live-region role via `:rest`: `role="status"` (per D-02 — valid because count is post-load-dynamic)
    - Inline slot content ONLY (D-07, Landmine 1): e.g. `{@needs_review} accounts need review — <a href="/admin/users?locked=true">Review now</a>` when risk; `"All clear"` when ok. No `<p>` as child.
    - WARNING: `notice/1` wraps its slot in `<p class="sg-text-sm">`. Adding any block `<p>` inside the slot creates `<p><p>…</p></p>` (invalid HTML, axe violation). Text nodes + `<a>` + `<span>` are the only valid children.

    ITEM 3 — task_card grid (moved BEFORE posture strip — this is the PRIMARY content):
    - `<div class="sg-grid sg-grid--3">` with 3 `<.task_card>` components (unchanged content from current lines 39-58).

    ITEM 4 — demoted posture strip:
    - `<section class="sg-card sg-posture-strip sg-stack sg-stack--3">` with `aria-busy={if @loading, do: "true"}` on this element (D-02, Landmine 5).
    - DELETE the old alarm anchor `<a ... class="sg-cluster sg-cluster--start sg-posture-strip__risk">` (lines 61-65) entirely — DO NOT carry it forward (Landmine 4).
    - `<div class="sg-cluster sg-cluster--3">`:
      - When `@loading` is true: render 6 `<.skeleton>` elements inside the cluster, each matching the footprint of a stat_link (Landmine 8 — match count to avoid layout jump). Apply a width/height class appropriate to a `sg-metric-link` column shape; per Claude's discretion but must not cause layout jump.
      - When `@loading` is false: render the 6 `<.stat_link>` elements (Total, Confirmed, MFA, Passkeys, Locked, Deleted) exactly as in current lines 68-93.
    - Per UI-SPEC: `sg-posture-strip` CSS already encodes "demoted metrics… below the jobs" (app.css:1172).

    ITEM 5 — capability section (lowest priority, unchanged):
    - `<section class="sg-stack sg-stack--3">` with heading "What Sigra can do", `<div class="sg-capability sg-grid sg-grid--3">` with 7 `<.capability>` items (unchanged from current lines 97-111).

    Critical checks before finishing:
    - `sg-posture-strip__risk` must NOT appear anywhere in the rendered output (Landmine 4). Run a local grep to confirm deletion.
    - All `<.notice>` slot content is inline (no nested `<p>`).
    - `aria-busy` is on the posture strip `<section>`, not the top-level section.
    - The `<.notice>` alarm is rendered only when `not @loading` (Landmine 3 — no inert live-region during skeleton).
    - Skeleton is rendered INSIDE the sg-cluster sg-cluster--3, NOT replacing the entire posture strip section.
  </action>
  <verify>
    <automated>mix test test/example/test/example_web/admin_shell_test.exs && grep -v "^[[:space:]]*#" lib/sigra/admin/live/index_live.ex | grep -c "sg-posture-strip__risk"</automated>
  </verify>
  <done>
    mix test passes. grep count for sg-posture-strip__risk returns 0 (anchor deleted). render/1 contains connected?-gated skeleton state (6 .skeleton elements in posture strip cluster) and the <.notice> alarm above the task grid. All existing admin_shell_test assertions remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser → LiveView mount | HTTP/WS requests authenticated by existing on_mount hooks (unchanged) |
| admin LiveView → DB aggregate queries | Query.summary_counts/2 called only on connected mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-01 | Information Disclosure | Skeleton state | accept | Skeleton shows no user data — loading=true assigns empty %{} for summary_counts. No PII or count values leak in the HTTP mount response. |
| T-157-02 | Tampering | Alarm deep-link `/admin/users?locked=true` | accept | Link navigates within the already-authenticated admin scope. `on_mount` hooks (unchanged) enforce scope authorization on the destination route. The link itself carries no session-mutating side effect. |
| T-157-03 | Spoofing | `connected?(socket)` gate | accept | Two-state render difference is cosmetic (skeleton vs data). Attacker cannot force the connected state via HTTP; WebSocket upgrade is gated by the same session auth as the HTTP mount. No security boundary crossed by the UI state difference. |
| T-157-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. No legitimacy gate required. |
</threat_model>

<verification>
After both tasks complete:
- `mix test test/example/test/example_web/admin_shell_test.exs` — all existing assertions green
- `mix test` — full suite green
- `grep -v "^[[:space:]]*#" lib/sigra/admin/live/index_live.ex | grep "sg-posture-strip__risk"` — returns empty (deleted)
- `grep "connected?(socket)" lib/sigra/admin/live/index_live.ex` — returns match (gate present in mount/3)
- Visual inspection of rendered HTML offset: `.sg-notice` offset < task grid `sg-grid--3` offset (alarm above task cards)
</verification>

<success_criteria>
- `lib/sigra/admin/live/index_live.ex` `mount/3` has `if connected?(socket)` gate; disconnected branch assigns `loading: true, summary_counts: %{}`; connected branch calls `Query.summary_counts/2` and assigns `loading: false`.
- Rendered HTML on HTTP GET: skeleton present in posture strip cluster, no `.sg-metric-link__value`, no `sg-posture-strip__risk`, no alarm notice.
- Rendered HTML on connected mount: `.sg-metric-link__value` values present, alarm `<.notice>` with correct tone above task grid, no skeleton, `sg-posture-strip__risk` absent.
- `mix test test/example/test/example_web/admin_shell_test.exs` green.
</success_criteria>

<output>
Create `.planning/phases/157-overview-landings-highest-effort/157-01-SUMMARY.md` when done.
</output>
