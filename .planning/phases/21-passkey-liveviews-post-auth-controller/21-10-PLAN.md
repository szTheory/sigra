---
phase: 21-passkey-liveviews-post-auth-controller
plan: 10
type: execute
wave: 8
depends_on: ["21-09"]
files_modified:
  - lib/sigra/plug/passkey_challenge.ex
  - lib/sigra/passkeys/registration.ex
  - lib/sigra/passkeys/authentication.ex
  - lib/sigra/session_stores/ecto.ex
  - test/sigra/plug/passkey_challenge_test.exs
  - test/sigra/session_stores/ecto_test.exs
autonomous: true
gap_closure: true
requirements:
  - PK-UX-01
  - PK-UX-02
  - PK-UX-03
  - PK-UX-04
  - PK-UX-05
  - PK-UX-06
  - PK-UX-07
  - PK-UX-08
  - PK-UX-09
  - PK-UX-10
  - PK-UX-11
  - PK-UX-12
must_haves:
  truths:
    - "Normal passkey registration, MFA, primary-login, and conditional-login options calls can issue server-side challenges without caller-supplied bytes."
    - "Issued passkey challenge bytes are non-empty, signed into the Plug session token, and reconstructed unchanged during verification."
    - "Persisted user_sessions rows with type \"mfa_pending\" hydrate as %Sigra.Session{type: :mfa_pending}, not :standard."
  artifacts:
    - path: "lib/sigra/plug/passkey_challenge.ex"
      provides: "Plug-session challenge issue/verify adapter that never signs nil challenge bytes"
      contains: "Base.url_encode64"
    - path: "lib/sigra/passkeys/registration.ex"
      provides: "Registration challenge builder with server-generated fallback bytes"
      contains: "Wax.new_registration_challenge"
    - path: "lib/sigra/passkeys/authentication.ex"
      provides: "Authentication challenge builder with server-generated fallback bytes"
      contains: "Wax.new_authentication_challenge"
    - path: "lib/sigra/session_stores/ecto.ex"
      provides: "Ecto session hydration preserving mfa_pending type"
      contains: "mfa_pending"
  key_links:
    - from: "lib/sigra/plug/passkey_challenge.ex"
      to: "lib/sigra/passkeys/registration.ex"
      via: "build_challenge(:registration, config, opts)"
      pattern: "Registration\\.new_challenge"
    - from: "lib/sigra/plug/passkey_challenge.ex"
      to: "lib/sigra/passkeys/authentication.ex"
      via: "build_challenge(:authentication, config, opts)"
      pattern: "Authentication\\.new_challenge"
    - from: "lib/sigra/session_stores/ecto.ex"
      to: "test/example/lib/example_web/controllers/session_controller.ex"
      via: "conn.private[:sigra_session].type used by complete_mfa_passkey/2"
      pattern: "\"mfa_pending\"\\s*->\\s*:mfa_pending"
---

<objective>
Close the Phase 21 runtime blockers that make real options endpoints crash and make route-time MFA passkey success impossible.

Purpose: Phase 21 cannot be verified end-to-end while challenge bytes are nil during normal options initiation or while persisted MFA-pending sessions hydrate as standard sessions.
Output: Library/runtime fixes with focused tests, ready for route and browser verification in Plan 21-11.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-RESEARCH.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-VERIFICATION.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-09-SUMMARY.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-01-SUMMARY.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-04-SUMMARY.md
@lib/sigra/plug/passkey_challenge.ex
@lib/sigra/passkeys/registration.ex
@lib/sigra/passkeys/authentication.ex
@lib/sigra/session_stores/ecto.ex
@test/sigra/plug/passkey_challenge_test.exs
@test/sigra/session_stores/ecto_test.exs

<interfaces>
Existing issue/verify boundary:
```elixir
@spec issue(Plug.Conn.t(), ceremony(), Sigra.Config.t(), keyword()) ::
        {Plug.Conn.t(), Wax.Challenge.t()}
def issue(%Plug.Conn{} = conn, ceremony, %Sigra.Config{} = config, opts \\ [])

@spec verify(Plug.Conn.t(), ceremony(), Sigra.Config.t(), keyword(), verifier()) ::
        {:ok, Plug.Conn.t(), term()} | {:error, Plug.Conn.t(), term()}
def verify(%Plug.Conn{} = conn, ceremony, %Sigra.Config{} = config, opts \\ [], callback)
```

Existing challenge builders:
```elixir
def new_challenge(%Sigra.Config{} = config, opts \\ []) do
  Wax.new_registration_challenge(..., bytes: Keyword.get(opts, :bytes))
end

def new_challenge(%Sigra.Config{} = config, opts \\ []) do
  Wax.new_authentication_challenge(..., bytes: Keyword.get(opts, :bytes))
end
```

Existing session type hydration:
```elixir
case record.type do
  "standard" -> :standard
  "remember_me" -> :remember_me
  type when is_atom(type) -> type
  _ -> :standard
end
```
</interfaces>
</context>

<source_audit>
GOAL: Phase 21 end-to-end passkey enrollment/authentication is blocked by runtime options issuance and MFA session hydration. COVERED by Task 1 and Task 2.
REQ: PK-UX-01 through PK-UX-12 all remain phase requirements; this plan covers the runtime prerequisites shared by every options-started passkey flow. COVERED in requirements frontmatter.
RESEARCH: Controller-owned session completion, server-stored challenges, and progressive-enhancement browser flows require working real options endpoints. COVERED by Task 1.
CONTEXT: D-03, D-11, D-23 require final auth/session mutation at controller boundaries; route-time MFA success requires persisted :mfa_pending hydration. COVERED by Task 2.
VERIFICATION gaps: nil challenge bytes and mfa_pending hydration. COVERED by Task 1 and Task 2.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate non-empty challenge bytes for normal options issuance</name>
  <files>lib/sigra/plug/passkey_challenge.ex, lib/sigra/passkeys/registration.ex, lib/sigra/passkeys/authentication.ex, test/sigra/plug/passkey_challenge_test.exs</files>
  <behavior>
    - Test 1: Calling `Sigra.Plug.PasskeyChallenge.issue(conn, :registration, config)` with no `bytes:` option returns a `%Wax.Challenge{bytes: bytes}` where `is_binary(bytes)` and `byte_size(bytes) >= 32`.
    - Test 2: Calling `issue(conn, :authentication, config)` with no `bytes:` option returns the same non-empty bytes encoded in the signed session token payload.
    - Test 3: Explicit `bytes:` passed by existing tests remains respected exactly, preserving deterministic completion-test setup from earlier plans.
  </behavior>
  <action>Add the failing assertions first in `test/sigra/plug/passkey_challenge_test.exs`. Then implement server-side fallback byte generation so normal route calls do not rely on Wax returning bytes. Prefer putting the fallback in `Registration.new_challenge/2` and `Authentication.new_challenge/2` with a shared local expression such as `Keyword.get(opts, :bytes) || :crypto.strong_rand_bytes(32)`; keep explicit `bytes:` behavior unchanged for deterministic tests and verification reconstruction. Do not change session slot names, token purpose, max age, delete-on-success behavior, or callback-error preservation.</action>
  <verify>
    <automated>mix test test/sigra/plug/passkey_challenge_test.exs</automated>
  </verify>
  <done>Real registration and authentication challenge issuance without explicit bytes produces non-empty challenge bytes, signs those bytes into the Plug session token, and all existing PK-06 challenge tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve mfa_pending session type during Ecto hydration</name>
  <files>lib/sigra/session_stores/ecto.ex, test/sigra/session_stores/ecto_test.exs</files>
  <behavior>
    - Test 1: Fetching a `%Sigra.Test.UserSession{type: "mfa_pending"}` returns `%Sigra.Session{type: :mfa_pending}`.
    - Test 2: Existing `"standard"`, `"remember_me"`, atom, and unknown fallback behavior remains unchanged.
  </behavior>
  <action>Add a focused test in the `fetch/2` describe block of `test/sigra/session_stores/ecto_test.exs` for a persisted `"mfa_pending"` row. Update only `to_session/1` in `lib/sigra/session_stores/ecto.ex` to map `"mfa_pending" -> :mfa_pending`. Keep unknown string fallback to `:standard` so older or malformed rows remain fail-closed instead of creating arbitrary atoms.</action>
  <verify>
    <automated>mix test test/sigra/session_stores/ecto_test.exs</automated>
  </verify>
  <done>Persisted MFA-pending sessions hydrate as `:mfa_pending`, enabling `SessionController.complete_mfa_passkey/2` to pass its route-time guard through normal session loading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> options endpoint | Untrusted request initiates a WebAuthn ceremony and receives public options JSON. |
| Plug session cookie -> controller | Signed/encrypted session state carries the server-authoritative challenge token and session token reference. |
| Database -> session hydration | Persisted session row type controls whether an MFA-pending request may upgrade to a standard session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10-01 | Tampering | `Sigra.Plug.PasskeyChallenge.issue/4` | mitigate | Generate cryptographically random challenge bytes before signing; tests assert signed payload matches returned challenge bytes. |
| T-21-10-02 | Elevation of Privilege | `Sigra.SessionStores.Ecto.to_session/1` | mitigate | Whitelist `"mfa_pending"` explicitly and keep unknown strings mapped to `:standard`; do not convert arbitrary strings to atoms. |
| T-21-10-03 | Denial of Service | options endpoints | mitigate | Remove nil-byte crash path by ensuring `Base.url_encode64/2` only receives binaries. |
</threat_model>

<verification>
Run both focused library suites:

```bash
mix test test/sigra/plug/passkey_challenge_test.exs test/sigra/session_stores/ecto_test.exs
```
</verification>

<success_criteria>
- `Sigra.Plug.PasskeyChallenge.issue/4` works without caller-supplied bytes for both ceremonies.
- Explicit deterministic bytes still round-trip for existing tests.
- `Sigra.SessionStores.Ecto.fetch/2` returns `%Sigra.Session{type: :mfa_pending}` for persisted `"mfa_pending"` rows.
- No user-facing passkey route tests are changed in this plan; Plan 21-11 owns route/browser proof after these prerequisites land.
</success_criteria>

<output>
After completion, create `.planning/phases/21-passkey-liveviews-post-auth-controller/21-10-SUMMARY.md`
</output>
