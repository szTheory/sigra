---
phase: 21-passkey-liveviews-post-auth-controller
plan: 11
type: execute
wave: 9
depends_on: ["21-10"]
files_modified:
  - test/example/test/example_web/controllers/passkey_session_controller_test.exs
  - test/example/priv/playwright/tests/passkey-login.spec.ts
autonomous: true
gap_closure: true
requirements:
  - PK-UX-01
  - PK-UX-02
  - PK-UX-03
  - PK-UX-04
  - PK-UX-05
  - PK-UX-06
  - PK-UX-07
  - PK-UX-08
  - PK-UX-09
  - PK-UX-10
  - PK-UX-11
  - PK-UX-12
must_haves:
  truths:
    - "Real example-app options endpoints return 200 JSON with non-empty challenges for enrollment, MFA passkey, conditional primary login, and email-specific primary login."
    - "MFA passkey success is proven through `POST /users/mfa/passkey` and normal router/session-store loading, not direct controller invocation."
    - "Playwright no longer intercepts `/users/log_in/passkey/options` and therefore observes the real server-side options behavior."
  artifacts:
    - path: "test/example/test/example_web/controllers/passkey_session_controller_test.exs"
      provides: "Route-backed options and MFA success coverage"
      contains: "/users/log_in/passkey/options"
    - path: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      provides: "Real-server browser smoke without options interception"
      contains: "waitForResponse"
  key_links:
    - from: "test/example/test/example_web/controllers/passkey_session_controller_test.exs"
      to: "test/example/lib/example_web/controllers/session_controller.ex"
      via: "Phoenix.ConnTest.post/3 real routes"
      pattern: "post\\(conn, ~p\"/users/mfa/passkey\""
    - from: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      to: "/users/log_in/passkey/options"
      via: "real browser click and network response observation"
      pattern: "waitForResponse"
---

<objective>
Close the remaining Phase 21 verification blockers by replacing route and browser workarounds with real server-backed proof.

Purpose: Plan 21-10 fixes the runtime prerequisites; this plan proves the end-to-end Phase 21 routes and browser entrypoint now exercise those real paths.
Output: ExUnit route tests and Playwright smoke that no longer seed, bypass, or intercept the behavior under verification.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-RESEARCH.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-VERIFICATION.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-09-SUMMARY.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-10-SUMMARY.md
@test/example/lib/example_web/controllers/session_controller.ex
@test/example/test/example_web/controllers/passkey_session_controller_test.exs
@test/example/priv/playwright/tests/passkey-login.spec.ts

<interfaces>
Existing example controller options endpoints:
```elixir
def passkey_registration_options(conn, _params)
def passkey_authentication_options(conn, %{"conditional" => "true"})
def passkey_authentication_options(conn, %{"user" => %{"email" => email}})
def passkey_mfa_options(conn, _params)
```

Existing completion route under test:
```elixir
def complete_mfa_passkey(conn, %{"passkey" => passkey_params}) do
  with true <- get_session(conn, :mfa_pending) == true,
       %{type: :mfa_pending} <- old_session,
       ...
```

Existing test helpers:
```elixir
passkey_fixture(user)
encoded_passkey_response(%{credential_id: credential_id})
stub_passkey_ceremony(fn {:authenticate, user, response, opts} -> ... end)
log_in_with_mfa_pending_session(conn, user)
```
</interfaces>
</context>

<source_audit>
GOAL: User can enroll and authenticate with passkeys end-to-end through example app routes and browser entrypoints. COVERED by Task 1 and Task 2.
REQ: PK-UX-01 through PK-UX-12 all remain Phase 21 requirements; this plan provides the final route/browser proof for the blockers affecting those requirements. COVERED in requirements frontmatter.
RESEARCH: Browser hooks may own recoverable ceremony state, but terminal session mutation must stay controller POST. COVERED by route-backed MFA success test.
CONTEXT: D-04, D-08, D-11, D-22, D-23 require explicit CTA, controller-rendered login, and controller-owned completion. COVERED by ExUnit and Playwright tests.
VERIFICATION gaps: MFA success bypasses route and Playwright intercepts options. COVERED by Task 1 and Task 2.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove real options endpoints and MFA success through routes</name>
  <files>test/example/test/example_web/controllers/passkey_session_controller_test.exs</files>
  <behavior>
    - Test 1: `POST /users/settings/mfa/passkeys/options` as a sudo-authenticated user returns 200 JSON with `options.challenge` as a non-empty string and stores a signed registration challenge session token.
    - Test 2: `POST /users/mfa/passkey/options` with a real persisted `mfa_pending` session returns 200 JSON with `options.challenge`, `allowCredentials`, and no crash.
    - Test 3: `POST /users/log_in/passkey/options` with `%{"conditional" => "true"}` returns 200 JSON with `options.challenge`, `allowCredentials: []`, and `useBrowserAutofill: true`.
    - Test 4: `POST /users/log_in/passkey/options` with an existing confirmed user's email and passkey row returns 200 JSON with non-empty `options.challenge` and a non-empty `allowCredentials` list.
    - Test 5: The MFA success test uses `post(conn, ~p"/users/mfa/passkey", ...)` and normal session loading; it must not call `ExampleWeb.SessionController.complete_mfa_passkey/2` directly or inject `conn.private[:sigra_session]`.
  </behavior>
  <action>Extend `passkey_session_controller_test.exs` with route-level options tests that call the actual Phoenix routes after Plan 21-10. Replace the direct-controller MFA success test with a real `POST /users/mfa/passkey` sequence: create a persisted `mfa_pending` session, set Plug session keys via the same helpers used by the failure test, call `POST /users/mfa/passkey/options` first to seed the real challenge, stub only the WebAuthn ceremony module, then post the encoded response to `/users/mfa/passkey`. Remove the `mfa_pending_sigra_session/1` helper if it becomes unused. Keep deterministic WebAuthn hardware stubbing; do not stub the options route, router, session store, or controller action.</action>
  <verify>
    <automated>cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/example_web/controllers/passkey_session_controller_test.exs --max-failures 1</automated>
  </verify>
  <done>Focused controller tests prove all real options endpoints return usable challenge JSON and MFA passkey success upgrades the session through the router.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove Playwright options interception and observe the real server response</name>
  <files>test/example/priv/playwright/tests/passkey-login.spec.ts</files>
  <behavior>
    - Test 1: The Playwright spec contains no `page.route('**/users/log_in/passkey/options'...)` or `route.fulfill(...)` for the options endpoint.
    - Test 2: Clicking `#passkey_login_button` causes one real `/users/log_in/passkey/options` response from the server with HTTP 200.
    - Test 3: The browser page still retains identifier, password, and magic-link fallbacks and does not render raw `AbortError` or `NotAllowedError` strings.
  </behavior>
  <action>Update `passkey-login.spec.ts` so it no longer intercepts `/users/log_in/passkey/options`. Keep the `SigraPasskeys.authenticate` browser stub only as a hardware-avoidance shim: it may call `fetch(optionsUrl, ...)` with the email, read the real JSON response, and return `null` to exercise the recoverable no-credential path. Use `page.waitForResponse()` or equivalent to assert the real options response URL, status 200, and JSON shape. Do not fulfill or fake the options response.</action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test tests/passkey-login.spec.ts</automated>
  </verify>
  <done>The browser smoke uses the real example server options endpoint and would fail if `/users/log_in/passkey/options` crashed again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> login options route | Real browser click triggers a server route that returns WebAuthn options JSON. |
| ExUnit conn -> Phoenix router | Tests cross router pipelines, Plug session loading, and controller action dispatch. |
| WebAuthn hardware -> deterministic test stub | Only authenticator hardware is stubbed; server options and session behavior remain real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11-01 | Repudiation | route-backed tests | mitigate | Replace direct controller invocation with Phoenix `post/3` calls so logs, plugs, and session transitions are exercised. |
| T-21-11-02 | Tampering | Playwright options response | mitigate | Remove `route.fulfill` interception so the test cannot mask server-side options crashes. |
| T-21-11-03 | Elevation of Privilege | MFA session upgrade | mitigate | Prove success requires a persisted `mfa_pending` session and clears pending keys after controller upgrade. |
</threat_model>

<verification>
Run the focused route and browser checks:

```bash
cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/example_web/controllers/passkey_session_controller_test.exs --max-failures 1
cd test/example/priv/playwright && npx playwright test tests/passkey-login.spec.ts
```
</verification>

<success_criteria>
- No Phase 21 test calls `ExampleWeb.SessionController.complete_mfa_passkey/2` directly for the success path.
- No Playwright test intercepts or fulfills `/users/log_in/passkey/options`.
- Route tests prove options JSON challenge generation for registration, MFA, conditional login, and email-specific login.
- Focused ExUnit and Playwright commands pass against the real example app routes.
</success_criteria>

<output>
After completion, create `.planning/phases/21-passkey-liveviews-post-auth-controller/21-11-SUMMARY.md`
</output>
