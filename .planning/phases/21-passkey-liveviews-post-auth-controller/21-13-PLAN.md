---
phase: 21-passkey-liveviews-post-auth-controller
plan: 13
type: execute
wave: 11
depends_on: ["21-12"]
files_modified:
  - test/example/priv/playwright/tests/passkey-login.spec.ts
autonomous: true
gap_closure: true
requirements:
  - PK-UX-01
  - PK-UX-02
  - PK-UX-03
  - PK-UX-04
  - PK-UX-05
  - PK-UX-06
  - PK-UX-07
  - PK-UX-08
  - PK-UX-09
  - PK-UX-10
  - PK-UX-11
  - PK-UX-12
user_setup: []
must_haves:
  truths:
    - "Playwright proves the example app served bundle defines the passkey runtime without using page.addInitScript."
    - "Playwright proves LiveSocket sees PasskeyRegister and PasskeyAuthenticate in its app-owned hook registry."
    - "Clicking passkey-primary login reaches the real options route through app-owned runtime, with no route interception or test-defined passkey global."
    - "Static regression checks fail if the served bundle loses passkey runtime symbols or the Playwright test reintroduces injection/fulfillment."
  artifacts:
    - path: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      provides: "Browser proof for app-owned passkey runtime and real options route behavior"
      contains: "SigraPasskeyRuntime"
  key_links:
    - from: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      to: "test/example/priv/static/assets/js/app.js"
      via: "page evaluates window.SigraPasskeyRuntime and window.liveSocket.hooks"
      pattern: "SigraPasskeyRuntime"
    - from: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      to: "/users/log_in/passkey/options"
      via: "real browser click and waitForResponse"
      pattern: "waitForResponse"
    - from: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      to: "test/example/lib/example_web/controllers/session_html.ex"
      via: "interacts with #passkey_login_form and #passkey_login_button"
      pattern: "passkey_login_button"
---

<objective>
Replace the masked Playwright proof with a browser and static-bundle regression test for the app-owned passkey runtime.

Purpose: Phase 21 verification failed because the Playwright smoke injected `window.SigraPasskeys`, allowing tests to pass while the real served app lacked passkey hooks and login runtime. This plan makes that failure mode impossible to miss.
Output: Playwright coverage and static assertions proving the served bundle is active without test-created globals or route fulfillment.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-VERIFICATION.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-11-SUMMARY.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-12-PLAN.md
@test/example/priv/playwright/playwright.config.ts
@test/example/priv/playwright/tests/passkey-login.spec.ts
@test/example/priv/static/assets/js/app.js
@test/example/lib/example_web/controllers/session_html.ex

<interfaces>
Runtime exposed by Plan 21-12:
```javascript
window.SigraPasskeyRuntime = {
  PasskeyRegister,
  PasskeyAuthenticate,
  attachPasskeyLogin
}
```

Forbidden Playwright masking patterns:
```typescript
page.addInitScript(...)
page.route(...)
route.fulfill(...)
(window as any).SigraPasskeys = ...
```
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 21 | End-to-end passkey UX in the example app browser | 21-13 | COVERED | Browser proof verifies runtime presence and real options request from the served page.
REQ | PK-UX-01 | Sudo-gated enrollment from account settings | 21-13 | COVERED | Runtime proof includes PasskeyRegister hook registration.
REQ | PK-UX-02 | Registration notification email | 21-13 | COVERED | Enrollment browser runtime is proven active; existing route tests cover completion side effects.
REQ | PK-UX-03 | Friendly passkey names | 21-13 | COVERED | Existing list behavior remains covered; this plan verifies the browser runtime blocker is removed.
REQ | PK-UX-04 | Rename/delete and cap | 21-13 | COVERED | Existing management tests remain valid once runtime proof is app-owned.
REQ | PK-UX-05 | Passkey MFA alongside TOTP | 21-13 | COVERED | Runtime proof includes PasskeyAuthenticate hook registration.
REQ | PK-UX-06 | Config-gated passkey-primary login | 21-13 | COVERED | Browser click reaches real options route through app-owned runtime.
REQ | PK-UX-07 | Mandatory magic-link recovery | 21-13 | COVERED | Browser test preserves fallback assertions.
REQ | PK-UX-08 | Conditional UI/autofill | 21-13 | COVERED | Runtime exposes attachPasskeyLogin and browser proof verifies it is loaded.
REQ | PK-UX-09 | Duplicate credential friendly copy | 21-13 | COVERED | Existing route coverage remains; runtime blocker to reaching that path is removed.
REQ | PK-UX-10 | Phoenix JS hooks wrap browser library | 21-13 | COVERED | Playwright inspects app-owned LiveSocket hooks.
REQ | PK-UX-11 | Auth completion POSTs to plain controller | 21-13 | COVERED | Browser click still uses form/controller path and real options route.
REQ | PK-UX-12 | Abort/timeout/cancel recovery | 21-13 | COVERED | Browser test asserts no raw browser error strings and runtime exposes safe status path.
RESEARCH | Browser proof must avoid route fulfillment | Real app-owned runtime and server response only | 21-13 | COVERED | Test forbids addInitScript, route interception, and route fulfillment.
CONTEXT | D-11 | Controller POST owns terminal auth mutation | 21-13 | COVERED | Browser test drives existing form and controller route.
CONTEXT | D-16-D-20 | Guided recovery and no raw browser exception strings | 21-13 | COVERED | Browser test keeps raw browser error leak assertion.
CONTEXT | D-22 | Conditional UI progressive enhancement | 21-13 | COVERED | Fallback controls stay visible in the test.
Deferred | Rich device-management console | Not applicable | EXCLUDED | Deferred in CONTEXT.md and not part of latest verifier gaps.
</source_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove Playwright-injected passkey runtime and assert app-owned runtime is loaded</name>
  <files>test/example/priv/playwright/tests/passkey-login.spec.ts</files>
  <behavior>
    - The spec contains no `addInitScript`, `page.route`, `route.fulfill`, or `(window as any).SigraPasskeys`.
    - The first test still proves identifier field, passkey CTA, password fallback, and magic-link recovery are visible.
    - A browser assertion proves `window.SigraPasskeyRuntime.PasskeyRegister`, `window.SigraPasskeyRuntime.PasskeyAuthenticate`, and `window.SigraPasskeyRuntime.attachPasskeyLogin` are functions.
    - A browser assertion proves `window.liveSocket.hooks.PasskeyRegister` and `window.liveSocket.hooks.PasskeyAuthenticate` are present.
  </behavior>
  <action>
    Edit `test/example/priv/playwright/tests/passkey-login.spec.ts` to remove the `page.addInitScript` block that defines `window.SigraPasskeys`. Add app-owned runtime assertions after `page.goto("/users/log_in")` using `page.evaluate`.

    Assert both the explicit marker created by Plan 21-12 (`window.SigraPasskeyRuntime`) and LiveSocket's hook registry. Keep the fallback visibility assertions from the existing smoke. Do not add route mocks, route fulfillment, static HTML fallback, or test-created WebAuthn globals.
  </action>
  <verify>
    <automated>! rg -n "addInitScript|page\\.route|route\\.fulfill|SigraPasskeys|setContent|gotoLoginOrFixture" test/example/priv/playwright/tests/passkey-login.spec.ts</automated>
    <automated>rg -n "SigraPasskeyRuntime|liveSocket\\.hooks|PasskeyRegister|PasskeyAuthenticate|attachPasskeyLogin" test/example/priv/playwright/tests/passkey-login.spec.ts</automated>
  </verify>
  <done>Playwright can no longer mask a missing app-owned runtime; the spec explicitly proves the runtime and hook registry are loaded from the served bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove passkey login click uses the served runtime and real options route</name>
  <files>test/example/priv/playwright/tests/passkey-login.spec.ts</files>
  <behavior>
    - Clicking `#passkey_login_button` triggers a real POST to `/users/log_in/passkey/options`.
    - The test uses `page.waitForResponse` only; no route interception or response fulfillment.
    - For the unknown-email smoke path, the server response remains `{ error: "unavailable" }` and the page stays recoverable with password and magic-link controls visible.
    - Raw `AbortError` and `NotAllowedError` text are absent.
  </behavior>
  <action>
    Update the click-path test so the only browser runtime used is the code from `test/example/priv/static/assets/js/app.js`. Before clicking, fill the `autocomplete="username webauthn"` field. Wait for a POST response whose URL includes `/users/log_in/passkey/options`.

    Because this smoke does not enroll a real hardware credential, assert the existing unknown-email `{ error: "unavailable" }` response for the current fixture email only after proving the request originated from the app-owned click handler. Keep the focused ExUnit route tests as the challenge-shape proof for existing users; do not recreate route fulfillment in Playwright.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -n "PasskeyRegister|PasskeyAuthenticate|attachPasskeyLogin|SigraPasskeyRuntime" test/example/priv/static/assets/js/app.js; ! rg -n "addInitScript|page\\.route|route\\.fulfill|SigraPasskeys|setContent|gotoLoginOrFixture" test/example/priv/playwright/tests/passkey-login.spec.ts'</automated>
    <automated>bash -lc 'set -euo pipefail; cd test/example; MIX_ENV=dev PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix ecto.setup; MIX_ENV=dev PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix phx.server &gt; /tmp/sigra-example-playwright.log 2&gt;&amp;1 &amp; server_pid=$!; cleanup(){ kill "$server_pid" 2&gt;/dev/null || true; wait "$server_pid" 2&gt;/dev/null || true; }; trap cleanup EXIT; for i in {1..60}; do if curl -fsS http://localhost:4000/users/log_in &gt;/dev/null; then break; fi; if ! kill -0 "$server_pid" 2&gt;/dev/null; then cat /tmp/sigra-example-playwright.log; exit 1; fi; sleep 1; done; curl -fsS http://localhost:4000/users/log_in &gt;/dev/null; cd priv/playwright; SIGRA_EXAMPLE_URL=http://localhost:4000 npx playwright test tests/passkey-login.spec.ts'</automated>
  </verify>
  <done>The browser proof uses the real served app bundle, observes a real options request, and contains static guards against reintroducing injected passkey runtime or route fulfillment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser to example server | Browser automation exercises real server routes and static assets. |
| Test code to browser page | Test code can accidentally inject globals or intercept routes, masking product bugs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13-01 | Repudiation | Playwright proof | mitigate | Add static guards forbidding `addInitScript`, route interception, and fulfillment so test evidence is attributable to app-owned runtime. |
| T-21-13-02 | Tampering | Browser options response | mitigate | Use `waitForResponse` only and inspect the real server response; do not alter network traffic in the test. |
| T-21-13-03 | Information Disclosure | Browser error rendering | mitigate | Keep raw browser exception leak assertions in the smoke. |
| T-21-13-04 | Denial of Service | Dev server startup in test command | accept | Command runs a local dev server for one focused spec and tears it down with a shell trap. |
</threat_model>

<verification>
Run the task-level commands plus:

```bash
rg -n "PasskeyRegister|PasskeyAuthenticate|attachPasskeyLogin|SigraPasskeyRuntime" test/example/priv/static/assets/js/app.js
! rg -n "addInitScript|page\\.route|route\\.fulfill|SigraPasskeys|setContent|gotoLoginOrFixture" test/example/priv/playwright/tests/passkey-login.spec.ts
cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/example_web/controllers/passkey_session_controller_test.exs --max-failures 1
```
</verification>

<success_criteria>
Phase 21 has a browser-level proof that passkey assets are served and active from the example app's own bundle, and the Playwright smoke cannot pass by injecting `window.SigraPasskeys` or fulfilling options routes.
</success_criteria>

<output>
After completion, create `.planning/phases/21-passkey-liveviews-post-auth-controller/21-13-SUMMARY.md`.
</output>
