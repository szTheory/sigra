---
phase: 21-passkey-liveviews-post-auth-controller
plan: 04
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - priv/templates/sigra.install/core/auth.ex
  - priv/templates/sigra.install/core/confirmation_controller.ex
  - priv/templates/sigra.install/core/session_controller.ex
  - priv/templates/sigra.install/core/login_html.ex
  - priv/templates/sigra.install/core/registration_html.ex
  - priv/templates/sigra.install/core/registration_live.ex
  - priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js
  - priv/templates/sigra.install/passkeys/passkey_browser.js
  - priv/templates/sigra.install/passkeys/passkey_hooks.js
  - test/sigra/install/generator_passkey_primary_login_test.exs
  - test/sigra/install/features/passkeys_js_test.exs
autonomous: true
requirements: [PK-UX-06, PK-UX-07, PK-UX-08, PK-UX-10, PK-UX-11, PK-UX-12]
user_setup: []
must_haves:
  truths:
    - "Passkey-primary mode keeps one identifier-first controller-rendered login screen"
    - "Email field uses autocomplete username webauthn"
    - "Continue with passkey leads visually while password and magic-link recovery remain visible"
    - "Signup flow exposes config-gated passkey enrollment wiring without allowing unconfirmed-email primary passkey accounts"
    - "Passkey-primary mode requires confirmed email and keeps magic-link recovery available"
    - "Conditional UI/autofill is progressive enhancement, not required for login comprehension"
    - "Controller-rendered passkey-primary login has a plain-page DOMContentLoaded initializer, not only LiveView hooks"
    - "Hook teardown and conditional UI aborts do not leak raw browser errors"
  artifacts:
    - path: "priv/templates/sigra.install/core/login_html.ex"
      provides: "Identifier-first passkey-primary login markup"
      contains: "autocomplete=\"username webauthn\""
    - path: "priv/templates/sigra.install/core/registration_live.ex"
      provides: "Config-gated signup-time passkey enrollment handoff"
      contains: "enroll_passkey"
    - path: "priv/templates/sigra.install/core/auth.ex"
      provides: "Passkey-primary recovery and confirmed-email invariants"
      contains: "passkey_primary_user_eligible?"
    - path: "priv/templates/sigra.install/passkeys/passkey_browser.js"
      provides: "Conditional mediation helper plus controller-page passkey login initializer"
      exports: ["startAuthentication", "attachPasskeyLogin"]
    - path: "priv/templates/sigra.install/passkeys/passkey_hooks.js"
      provides: "PasskeyAuthenticate hook support for conditional UI"
      contains: "useBrowserAutofill"
    - path: "priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js"
      provides: "Generated app.js import and DOMContentLoaded wiring for controller login pages"
      contains: "attachPasskeyLogin"
  key_links:
    - from: "priv/templates/sigra.install/core/login_html.ex"
      to: "priv/templates/sigra.install/core/session_controller.ex"
      via: "POST /users/log_in/passkey"
      pattern: "/users/log_in/passkey"
    - from: "priv/templates/sigra.install/passkeys/passkey_hooks.js"
      to: "priv/templates/sigra.install/passkeys/passkey_browser.js"
      via: "startAuthentication({ optionsJSON, signal, useBrowserAutofill })"
      pattern: "useBrowserAutofill"
    - from: "priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js"
      to: "priv/templates/sigra.install/passkeys/passkey_browser.js"
      via: "DOMContentLoaded calls attachPasskeyLogin() on controller-rendered pages"
      pattern: "DOMContentLoaded.*attachPasskeyLogin"
---

<objective>
Add the opt-in passkey-primary login surface and conditional UI browser helper while preserving password and magic-link recovery.

Purpose: Deliver PK-UX-06 through PK-UX-08 without breaking the Phase 10.1.1 controller-rendered login invariant or Phase 20 hook contract.
Output: Updated login template, conditional mediation helper, hook tests, and template assertions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-RESEARCH.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-UI-SPEC.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-01-SUMMARY.md
@priv/templates/sigra.install/core/confirmation_controller.ex
@priv/templates/sigra.install/core/login_html.ex
@priv/templates/sigra.install/core/session_controller.ex
@priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js
@priv/templates/sigra.install/passkeys/passkey_browser.js
@priv/templates/sigra.install/passkeys/passkey_hooks.js
@test/sigra/install/features/passkeys_js_test.exs
</context>

<source_audit>
REQ PK-UX-06: COVERED by Task 1 login UI and Task 3 signup-time passkey enrollment wiring.
REQ PK-UX-06: COVERED by Task 3 signup-time passkey enrollment wiring.
REQ PK-UX-07: COVERED by Task 1 visible magic-link fallback and Task 3 confirmed-email + mandatory magic-link recovery enforcement.
REQ PK-UX-08: COVERED by Task 2 conditional mediation helper and controller-page login initializer.
REQ PK-UX-10: COVERED by Task 2 existing hook extension.
REQ PK-UX-11: COVERED by Task 1 POST `/users/log_in/passkey` handoff.
REQ PK-UX-12: COVERED by Task 2 abort/error normalization.
CONTEXT D-08, D-09, D-10, D-11, D-16, D-19, D-20, D-22: COVERED.
Deferred chooser-first/split-screen auth: EXCLUDED.
</source_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Update controller-rendered login template for passkey-primary mode</name>
  <files>priv/templates/sigra.install/core/login_html.ex, test/sigra/install/generator_passkey_primary_login_test.exs</files>
  <read_first>
    priv/templates/sigra.install/core/login_html.ex
    priv/templates/sigra.install/core/session_controller.ex
    priv/templates/sigra.install/core/auth.ex
    test/example/test/example_web/controllers/session_controller_test.exs
    .planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
    .planning/phases/21-passkey-liveviews-post-auth-controller/21-UI-SPEC.md
  </read_first>
  <behavior>
    - Login template remains a controller HTML template with no `phx-submit` and no LiveView container requirement.
    - In passkey-primary mode the email input has exact `autocomplete="username webauthn"`.
    - Visible actions include `Continue with passkey`, `Use password instead`, and `Email me a magic link`.
    - The passkey completion form posts to `/users/log_in/passkey`.
    - There is no separate route/screen or chooser-first copy.
  </behavior>
  <action>
    In `priv/templates/sigra.install/core/login_html.ex`, keep `use <%= web_module %>, :html` and the `new(assigns)` controller template.

    Add a passkey-primary block guarded by `<%%= if @passkey_primary_enabled do %>`. Plan 01 updates `SessionController.new/2` to pass `passkey_primary_enabled: Auth.passkey_primary_enabled?()`. The final template must contain concrete markup:
    - one email input with `autocomplete="username webauthn"` for the passkey form.
    - passkey form `id="passkey_login_form"` with `action={~p"/users/log_in/passkey"}` and `method="post"`.
    - a button inside the form with `id="passkey_login_button"` and text `Continue with passkey`.
    - a hidden input named `passkey[response]` that the plain-page initializer fills before submitting the form.
    - primary button text `Continue with passkey`.
    - fallback button/link text `Use password instead`.
    - magic-link fallback button text `Email me a magic link`.
    - Keep password form on the same page; hide/show is allowed but do not navigate to a separate screen.

    Preserve the existing non-primary login forms for default config. The default non-passkey mode must still render `id="magic_link_form"` and `id="login_form"`.

    Extend template tests to assert exact strings: `autocomplete="username webauthn"`, `id="passkey_login_form"`, `id="passkey_login_button"`, `passkey[response]`, `/users/log_in/passkey`, `/users/log_in/passkey/options`, `Continue with passkey`, `Use password instead`, `Email me a magic link`, and refute `phx-submit`.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/install/generator_passkey_primary_login_test.exs test/sigra/install/generator_mfa_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "@passkey_primary_enabled|autocomplete=\\\"username webauthn\\\"|id=\\\"passkey_login_form\\\"|id=\\\"passkey_login_button\\\"|passkey\\[response\\]|/users/log_in/passkey|/users/log_in/passkey/options|Continue with passkey|Use password instead|Email me a magic link" priv/templates/sigra.install/core/login_html.ex` returns every string.
    - `! rg -n "phx-submit" priv/templates/sigra.install/core/login_html.ex` succeeds.
    - `! rg -n "choose your sign-in method|passkey screen|password screen" priv/templates/sigra.install/core/login_html.ex` succeeds.
  </acceptance_criteria>
  <done>Passkey-primary login is identifier-first, controller-rendered, and fallback-preserving.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend passkey browser helper, hooks, and app.js wiring for conditional UI/autofill</name>
  <files>priv/templates/sigra.install/passkeys/passkey_browser.js, priv/templates/sigra.install/passkeys/passkey_hooks.js, priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js, test/sigra/install/features/passkeys_js_test.exs</files>
  <read_first>
    priv/templates/sigra.install/passkeys/passkey_browser.js
    priv/templates/sigra.install/passkeys/passkey_hooks.js
    priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js
    test/sigra/install/features/passkeys_js_test.exs
    .planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-03-SUMMARY.md
    .planning/phases/21-passkey-liveviews-post-auth-controller/21-RESEARCH.md
  </read_first>
  <behavior>
    - `startAuthentication` accepts `useBrowserAutofill`.
    - Hooks accept `optionsUrl` and `completeUrl`, fetch options from the controller, and submit success payloads to controller completion URLs as hidden form JSON at `passkey[response]`.
    - Conditional mediation is only attempted if `PublicKeyCredential.isConditionalMediationAvailable()` exists and returns true.
    - `attachPasskeyLogin()` binds `#passkey_login_form` and `#passkey_login_button` on controller-rendered pages, fetches `/users/log_in/passkey/options`, calls `startAuthentication({ optionsJSON, useBrowserAutofill })`, writes `passkey[response]`, and submits `/users/log_in/passkey`.
    - Generated app.js wiring imports `attachPasskeyLogin` and calls it on `DOMContentLoaded`.
    - Unsupported conditional UI returns a normalized non-fatal unsupported result.
    - Unsupported, abort, and timeout errors map to user-safe UI states without raw browser exception strings.
    - Existing `destroyed()` and `disconnected()` abort behavior remains covered.
    - Tests use Node stubs and do not require real WebAuthn hardware.
  </behavior>
  <action>
    In `passkey_browser.js`, update `startAuthentication({ optionsJSON, signal })` to `startAuthentication({ optionsJSON, signal, useBrowserAutofill = false })`.
    - Add `async function conditionalMediationAvailable()` that checks `window.PublicKeyCredential`, `PublicKeyCredential.isConditionalMediationAvailable`, and awaits it.
    - If `useBrowserAutofill` is true and conditional mediation is unavailable, throw `new WebAuthnError("unsupported", "ERROR_PASSKEY_UNSUPPORTED")`.
    - When available, call `navigator.credentials.get({ publicKey: ..., mediation: "conditional", signal })`.
    - Keep explicit-click mode unchanged when `useBrowserAutofill` is false.

    In `passkey_browser.js`, add and export `attachPasskeyLogin(options = {})` for non-LiveView controller pages:
    - Resolve `form = options.form || document.querySelector("#passkey_login_form")` and `button = options.button || document.querySelector("#passkey_login_button")`; return `{ attached: false }` if either is missing so normal pages are unaffected.
    - Resolve the email input from `form.querySelector("input[name='user[email]']")`, the hidden response input from `form.querySelector("input[name='passkey[response]']")`, `optionsUrl` from `form.dataset.optionsUrl || "/users/log_in/passkey/options"`, and `completeUrl` from `form.action || "/users/log_in/passkey"`.
    - Bind the `Continue with passkey` button click and form submit to prevent default, fetch `optionsUrl` with method `POST`, `content-type: application/json`, `accept: application/json`, `x-csrf-token` from `meta[name='csrf-token']`, and body `JSON.stringify({ user: { email: emailInput.value } })`.
    - Call `startAuthentication({ optionsJSON: json.options, useBrowserAutofill: options.useBrowserAutofill === true })` for explicit submit/click and use the same helper for optional conditional mediation when feature detection allows it.
    - Write `JSON.stringify(response)` into `passkey[response]`, set `form.action = completeUrl`, and submit the form with `HTMLFormElement.prototype.submit.call(form)` so the terminal auth mutation remains `POST /users/log_in/passkey`.
    - Map unsupported/abort/timeout into user-safe status values such as `unsupported`, `canceled`, and `timeout`; update an optional `[data-passkey-login-status]` element using safe copy from UI-SPEC. Do not render raw `AbortError`, `NotAllowedError`, browser exception `message`, or stack strings.
    - Keep magic-link and password fallback controls untouched and visible when an error occurs.

    In `passkey_hooks.js`, add shared helpers:
    - `async fetchOptions(optionsUrl)` posts to `optionsUrl` with `x-csrf-token` from `document.querySelector("meta[name='csrf-token']")?.content` and returns `json.options`.
    - `submitCompletion(completeUrl, response, extra = {})` creates and submits a hidden POST form with `_csrf_token`, `passkey[response]` containing `JSON.stringify(response)`, and any `extra` values. For login passkey, extra includes `user[email]` from the login email input. This is the single completion contract; controllers decode `passkey[response]` JSON before calling generated Auth normalizers.
    - If `payload.options` is present, keep existing behavior; if `payload.optionsUrl` is present, fetch options before starting the ceremony.
    - If `payload.completeUrl` is present, submit the successful response to that URL after pushing the existing success event. Preserve existing event names exactly.
    - Pass `payload.useBrowserAutofill === true` into `startAuthentication` for the authenticate hook only.

    In `priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js`, extend the existing marker-wrapped app.js injection:
    - import `PasskeyHooks` from `./passkey_hooks` and `attachPasskeyLogin` from `./passkey_browser`.
    - keep the existing `hooks: { ...colocatedHooks, ...PasskeyHooks }` line intact.
    - add `document.addEventListener("DOMContentLoaded", () => attachPasskeyLogin())` so controller-rendered login pages get the static binding.
    - If the app.js marker injection cannot be applied, the installer's existing manual-action path must include the exact import/call instructions for `attachPasskeyLogin`; do not silently rely on LiveView hooks for controller pages.

    Extend `test/sigra/install/features/passkeys_js_test.exs` with Node-stub tests asserting:
    - template contains `optionsUrl`
    - template contains `completeUrl`
    - template contains `passkey[response]`
    - template contains `JSON.stringify`
    - template contains `x-csrf-token`
    - template contains `attachPasskeyLogin`
    - template contains `DOMContentLoaded`
    - template contains `#passkey_login_form`
    - template contains `#passkey_login_button`
    - template contains `/users/log_in/passkey/options`
    - template contains `/users/log_in/passkey`
    - template contains `useBrowserAutofill`
    - template contains `isConditionalMediationAvailable`
    - template contains `mediation: "conditional"`
    - unsupported path emits `sigra:passkey-authenticate:error` with code `ERROR_PASSKEY_UNSUPPORTED`
    - static `attachPasskeyLogin` tests prove unsupported, abort, and timeout update user-safe status/copy without exposing `AbortError`, `NotAllowedError`, or raw exception messages
    - existing destroyed teardown test still emits one `sigra:passkey-register:aborted`.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/install/features/passkeys_js_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "optionsUrl|completeUrl|passkey\\[response\\]|JSON.stringify|x-csrf-token|useBrowserAutofill|isConditionalMediationAvailable|mediation: \\\"conditional\\\"|ERROR_PASSKEY_UNSUPPORTED|attachPasskeyLogin|#passkey_login_form|#passkey_login_button|/users/log_in/passkey/options|/users/log_in/passkey" priv/templates/sigra.install/passkeys/passkey_browser.js priv/templates/sigra.install/passkeys/passkey_hooks.js` returns every string.
    - `rg -n "import \\{ PasskeyHooks \\} from \\\"\\./passkey_hooks\\\"|import \\{ attachPasskeyLogin \\} from \\\"\\./passkey_browser\\\"|DOMContentLoaded|attachPasskeyLogin\\(\\)|hooks: \\{ \\.\\.\\.colocatedHooks, \\.\\.\\.PasskeyHooks \\}" priv/templates/sigra.install/passkeys/app_js_passkeys_injection.js` returns imports, DOMContentLoaded call, and preserved LiveView hook registration.
    - `rg -n "attachPasskeyLogin|DOMContentLoaded|#passkey_login_form|/users/log_in/passkey/options|/users/log_in/passkey" test/sigra/install/features/passkeys_js_test.exs` returns static login binding assertions.
    - `rg -n "sigra:passkey-authenticate:error|ERROR_PASSKEY_UNSUPPORTED|destroyed teardown emits a single aborted event" test/sigra/install/features/passkeys_js_test.exs` returns all strings.
    - `rg -n "destroyed\\(\\)|disconnected\\(\\)|cancelPasskeyCeremony" priv/templates/sigra.install/passkeys/passkey_hooks.js` still returns all teardown functions.
  </acceptance_criteria>
  <done>Conditional UI is implemented as progressive enhancement in the existing helper/hook seam, controller-rendered passkey-primary login has static DOMContentLoaded binding, and abort-safe teardown plus user-safe error mapping are preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add signup-time passkey enrollment wiring and passkey-primary recovery invariants</name>
  <files>priv/templates/sigra.install/core/auth.ex, priv/templates/sigra.install/core/confirmation_controller.ex, priv/templates/sigra.install/core/session_controller.ex, priv/templates/sigra.install/core/registration_html.ex, priv/templates/sigra.install/core/registration_live.ex, test/sigra/install/generator_passkey_primary_login_test.exs</files>
  <read_first>
    priv/templates/sigra.install/core/auth.ex
    priv/templates/sigra.install/core/confirmation_controller.ex
    priv/templates/sigra.install/core/session_controller.ex
    priv/templates/sigra.install/core/registration_html.ex
    priv/templates/sigra.install/core/registration_live.ex
    priv/templates/sigra.install/core/login_html.ex
    priv/templates/sigra.install/core/user.ex
    priv/templates/sigra.install/core/user_auth.ex
    .planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
    .planning/phases/21-passkey-liveviews-post-auth-controller/21-RESEARCH.md
  </read_first>
  <behavior>
    - When `Auth.passkey_primary_enabled?/0` is true, registration UI contains a config-gated signup enrollment control named `user[enroll_passkey]`.
    - Selecting signup enrollment preserves the normal email-confirmation flow and redirects/continues to `/users/settings/mfa#passkeys` only after the user is confirmed and freshly authenticated.
    - Passkey-primary completion refuses users whose `confirmed_at` is nil.
    - Passkey-primary mode cannot hide or disable magic-link recovery; the login page and controller keep `Email me a magic link` available when primary mode is enabled.
    - Tests assert generator content for enrollment, confirmed-email enforcement, and magic-link invariant strings.
  </behavior>
  <action>
    In `priv/templates/sigra.install/core/auth.ex`, add generated helpers:
    - `passkey_primary_user_eligible?(user)` returns true only when `passkey_primary_enabled?()` is true and `user.confirmed_at != nil`.
    - `ensure_passkey_primary_user_eligible(user)` returns `:ok` for eligible users and `{:error, :email_not_confirmed}` for nil `confirmed_at`.
    - `magic_link_recovery_available?()` returns true when `passkey_primary_enabled?()` is true, even if a host configuration option would otherwise hide magic-link login. Add a short comment that PK-UX-07 makes magic-link recovery mandatory for passkey-primary accounts.

    In `priv/templates/sigra.install/core/session_controller.ex`, update `complete_passkey/2` so the passkey-primary POST path checks `Auth.ensure_passkey_primary_user_eligible(user)` before `Auth.authenticate_passkey/2`. If the user is unconfirmed, redirect to `~p"/users/log_in"` with the generic mapped passkey failure copy and keep the submitted email in flash, capped at 160 characters. Do not create a session for unconfirmed passkey-primary users.

    In `priv/templates/sigra.install/core/registration_live.ex`, add config-gated signup-time enrollment wiring:
    - `mount/3` assigns `passkey_primary_enabled: <%= context_module %>.passkey_primary_enabled?()` and `enroll_passkey_after_signup: false`.
    - Render an optional checkbox/control only when `@passkey_primary_enabled` is true with exact copy `Add a passkey after creating your account` and input name `user[enroll_passkey]`.
    - In `handle_event("save", %{"user" => user_params}, socket)`, read `enroll_passkey = Map.get(user_params, "enroll_passkey") in ["true", true, "on", "1"]`. When selected, build `confirmation_url_fun` so the confirmation link is `url(socket, ~p"/users/confirm/#{token}?enroll_passkey=1")`. Preserve the existing submit/login flow and do not expose the passkey enrollment ceremony before email confirmation succeeds.

    In `priv/templates/sigra.install/core/confirmation_controller.ex`, update `confirm/2`:
    - Add `alias <%= web_module %>.UserAuth`.
    - On `{:ok, user}` and `%{"enroll_passkey" => "1"}`, set `:user_return_to` in the Plug session to `~p"/users/sudo?return_to=/users/settings/mfa#passkeys"` before calling `UserAuth.log_in_user(user, %{})` after the `Your email has been confirmed.` flash. Do not pass `%{"return_to" => ...}` to `UserAuth.log_in_user/3`; that helper intentionally redirects from `get_session(conn, :user_return_to)` and ignores arbitrary redirect params. This logs in the now-confirmed user, then forces the fresh sudo boundary before enrollment.
    - On normal confirmation links, preserve the existing redirect behavior.

    In `priv/templates/sigra.install/core/registration_html.ex`, add equivalent controller-mode markup:
    - conditionally render the same `user[enroll_passkey]` control when `@passkey_primary_enabled` is true.
    - keep the existing password field and registration form behavior intact.

    Extend `test/sigra/install/generator_passkey_primary_login_test.exs` with assertions for `passkey_primary_user_eligible?`, `ensure_passkey_primary_user_eligible`, `magic_link_recovery_available?`, `confirmed_at != nil`, `{:error, :email_not_confirmed}`, `user[enroll_passkey]`, `Add a passkey after creating your account`, `?enroll_passkey=1`, `put_session(:user_return_to`, `UserAuth.log_in_user(user, %{})`, `/users/sudo?return_to=/users/settings/mfa#passkeys`, `/users/settings/mfa#passkeys`, and `Email me a magic link`.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/install/generator_passkey_primary_login_test.exs test/sigra/install/generator_wiring_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "passkey_primary_user_eligible\\?|ensure_passkey_primary_user_eligible|magic_link_recovery_available\\?|confirmed_at != nil|:email_not_confirmed" priv/templates/sigra.install/core/auth.ex priv/templates/sigra.install/core/session_controller.ex` returns every invariant.
    - `rg -n "user\\[enroll_passkey\\]|Add a passkey after creating your account|\\?enroll_passkey=1|passkey_primary_enabled" priv/templates/sigra.install/core/registration_live.ex priv/templates/sigra.install/core/registration_html.ex` returns signup enrollment wiring in both registration modes.
    - `rg -n "put_session\\(:user_return_to|UserAuth.log_in_user\\(user, %\\{\\}\\)|/users/sudo\\?return_to=/users/settings/mfa#passkeys|enroll_passkey" priv/templates/sigra.install/core/confirmation_controller.ex` returns confirmed-email handoff to the sudo-gated passkey enrollment surface.
    - `! rg -n "return_to.*UserAuth.log_in_user|UserAuth.log_in_user\\(user, %\\{\\\"return_to\\\"" priv/templates/sigra.install/core/confirmation_controller.ex` succeeds; unsupported redirect params are not passed to `UserAuth.log_in_user/3`.
    - `rg -n "Email me a magic link|magic_link_recovery_available\\?" priv/templates/sigra.install/core/login_html.ex priv/templates/sigra.install/core/auth.ex` proves magic-link recovery remains available in passkey-primary mode.
  </acceptance_criteria>
  <done>Passkey-primary signup enrollment is config-gated and passkey-primary accounts cannot bypass confirmed-email or magic-link recovery invariants.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Login HTML -> SessionController | Untrusted identifier and passkey response post into auth completion. |
| Browser conditional UI -> hook | Browser-generated autofill credentials and errors cross into hook code. |
| Hook -> controller POST | Successful browser ceremony leads to controller-owned session rotation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Spoofing | passkey-primary login | mitigate | Keep identifier-first email field and POST to `/users/log_in/passkey`; Plan 01 verifies assertion against the requested user and server challenge before session creation. |
| T-21-04-02 | Tampering | conditional UI payload | mitigate | Browser helper serializes credential fields; server verification remains authoritative. |
| T-21-04-03 | Information Disclosure | unknown email/passkey failure | mitigate | Login template preserves magic-link fallback and Plan 01 completion uses enumeration-safe generic passkey failure copy. |
| T-21-04-04 | Denial of Service | conditional mediation unsupported | mitigate | Helper throws normalized `ERROR_PASSKEY_UNSUPPORTED`; UI maps to fallback instead of stuck loading. |
| T-21-04-05 | Elevation of Privilege | session fixation | mitigate | Final login remains a plain POST into `UserAuth.log_in_user/3`; no LiveView event creates sessions. |
| T-21-04-06 | Repudiation | passkey login audit | mitigate | Completion routes call `Auth.authenticate_passkey/2`, preserving Phase 19 passkey authentication audit behavior. |
| T-21-04-07 | Elevation of Privilege | unconfirmed passkey-primary login | mitigate | `complete_passkey/2` must call `Auth.ensure_passkey_primary_user_eligible/1` and reject nil `confirmed_at` before authentication/session creation. |
| T-21-04-08 | Denial of Service | lost-device lockout | mitigate | `magic_link_recovery_available?/0` is mandatory in passkey-primary mode and login UI must keep `Email me a magic link` visible. |
| T-21-04-09 | Denial of Service | controller-page JS binding | mitigate | `attachPasskeyLogin()` maps unsupported/abort/timeout to recoverable UI states, leaves password and magic-link fallback visible, and never shows raw browser exception strings. |
</threat_model>

<verification>
Run `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/install/generator_passkey_primary_login_test.exs test/sigra/install/generator_wiring_test.exs test/sigra/install/features/passkeys_js_test.exs --max-failures 1`.
</verification>

<success_criteria>
Plan succeeds when passkey-primary mode remains one controller-rendered page, signup enrollment is config-gated through the registration flow, conditional UI is enhancement-only, and confirmed-email plus magic-link recovery invariants are enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/21-passkey-liveviews-post-auth-controller/21-04-SUMMARY.md`.
</output>
