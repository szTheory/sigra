---
phase: 21-passkey-liveviews-post-auth-controller
plan: 06
type: execute
wave: 5
depends_on: [21-04, 21-05, 21-07]
files_modified:
  - test/example/test/support/fixtures/auth_fixtures.ex
  - test/example/test/example_web/controllers/confirmation_controller_test.exs
  - test/example/test/example_web/controllers/passkey_session_controller_test.exs
  - test/example/test/example_web/live/registration_live_test.exs
  - test/example/test/example_web/live/passkey_mfa_challenge_live_test.exs
  - test/example/test/example_web/live/passkey_settings_live_test.exs
  - test/example/priv/playwright/tests/passkey-login.spec.ts
autonomous: true
requirements: [PK-UX-01, PK-UX-02, PK-UX-03, PK-UX-04, PK-UX-05, PK-UX-06, PK-UX-07, PK-UX-08, PK-UX-09, PK-UX-10, PK-UX-11, PK-UX-12]
user_setup: []
must_haves:
  truths:
    - "Example app tests prove passkey-primary login completion creates or rotates a logged-in session through UserAuth.log_in_user/3"
    - "Example app tests prove MFA passkey completion clears pending MFA session state, writes an upgraded session token, and redirects to the saved MFA return path"
    - "Example app tests prove sudo-gated passkey enrollment adds a passkey and triggers the registration notification path"
    - "Example app tests keep invalid/error-path, signup enrollment handoff, confirmation handoff, and fallback-preserving UI coverage"
    - "Playwright browser smoke proves the controller-rendered passkey-primary login JS binding leaves identifier and fallback UI reachable while handling unsupported/abort without a dead end"
    - "Focused generator, hook, example integration, compile, and example precommit gates are documented as final verification"
  artifacts:
    - path: "test/example/test/support/fixtures/auth_fixtures.ex"
      provides: "Passkey fixture rows and deterministic ceremony stub helpers"
      contains: "stub_passkey_ceremony"
    - path: "test/example/test/example_web/controllers/passkey_session_controller_test.exs"
      provides: "Passkey-primary login and MFA controller completion tests"
      contains: "/users/log_in/passkey"
    - path: "test/example/test/example_web/live/passkey_settings_live_test.exs"
      provides: "Sudo-gated passkey enrollment and management tests"
      contains: "/users/settings/mfa/passkeys"
    - path: "test/example/test/example_web/live/registration_live_test.exs"
      provides: "Signup-time passkey enrollment selection assertions"
      contains: "user[enroll_passkey]"
    - path: "test/example/test/example_web/controllers/confirmation_controller_test.exs"
      provides: "Confirmed-email passkey enrollment handoff assertions"
      contains: "/users/sudo?return_to=/users/settings/mfa#passkeys"
    - path: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      provides: "Browser smoke coverage for passkey-primary controller-page JS binding and fallback UI"
      contains: "passkey_login_form"
  key_links:
    - from: "test/example/test/example_web/controllers/passkey_session_controller_test.exs"
      to: "test/example/lib/example_web/controllers/session_controller.ex"
      via: "POST /users/log_in/passkey and POST /users/mfa/passkey"
      pattern: "/users/(log_in|mfa)/passkey"
    - from: "test/example/test/example_web/live/passkey_settings_live_test.exs"
      to: "test/example/lib/example_web/controllers/session_controller.ex"
      via: "POST /users/settings/mfa/passkeys"
      pattern: "/users/settings/mfa/passkeys"
    - from: "test/example/test/support/fixtures/auth_fixtures.ex"
      to: "test/example/lib/example/accounts.ex"
      via: "Application.put_env(:example, :passkey_ceremony_module, StubModule)"
      pattern: ":passkey_ceremony_module"
    - from: "test/example/priv/playwright/tests/passkey-login.spec.ts"
      to: "test/example/assets/js/passkey_browser.js"
      via: "browser runtime exercises attachPasskeyLogin DOM binding on #passkey_login_form"
      pattern: "attachPasskeyLogin|passkey_login_form"
---

<objective>
Add example app integration tests and final Phase 21 verification gates.

Purpose: Prove the mirrored example app behaves end-to-end for success paths, invalid/error paths, signup enrollment handoff, and fallback-preserving UI without requiring real WebAuthn hardware during ExUnit.
Output: Example integration tests, passkey fixtures/stubs, and final focused verification commands.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-UI-SPEC.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-05-PLAN.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-07-PLAN.md
@test/example/AGENTS.md
@test/example/test/example_web/controllers/session_controller_test.exs
@test/example/test/support/fixtures/auth_fixtures.ex
@test/example/test/support/conn_case_helpers.ex
@test/example/lib/example/accounts.ex
@test/example/lib/example_web/controllers/confirmation_controller.ex
@test/example/lib/example_web/controllers/session_controller.ex
@test/example/lib/example_web/controllers/session_html.ex
@test/example/lib/example_web/live/registration_live.ex
@test/example/lib/example_web/live/mfa_settings_live.ex
@test/example/lib/example_web/live/mfa_challenge_live.ex
@test/example/priv/playwright/playwright.config.ts
@test/example/priv/playwright/tests/passkeys-hooks.spec.ts
</context>

<source_audit>
GOAL example app end-to-end passkey UX: COVERED by Tasks 1-3.
REQ PK-UX-01: COVERED by sudo-gated enrollment success and settings tests.
REQ PK-UX-02: COVERED by registration notification assertion in enrollment success test.
REQ PK-UX-03: COVERED by passkey fixture label/default-name assertions in settings tests.
REQ PK-UX-04: COVERED by rename/delete UI coverage and sudo posture assertions inherited from settings tests.
REQ PK-UX-05: COVERED by successful MFA passkey completion and challenge fallback tests.
REQ PK-UX-06: COVERED by passkey-primary login success and signup enrollment handoff tests.
REQ PK-UX-07: COVERED by magic-link fallback assertions and passkey failure recovery tests.
REQ PK-UX-08: COVERED by `autocomplete="username webauthn"`, conditional UI hook-focused verification, and Playwright smoke coverage for the controller-page login initializer.
REQ PK-UX-09: COVERED by duplicate/invalid enrollment error-path tests.
REQ PK-UX-10: COVERED by LiveView hook marker assertions and generator hook verification.
REQ PK-UX-11: COVERED by controller POST completion success tests.
REQ PK-UX-12: COVERED by abort/unsupported/error-path tests, generator JS verification, and Playwright smoke assertions that unsupported/abort fallback does not dead-end the login page.
CONTEXT D-01 through D-23: COVERED by settings, challenge, login, confirmation, controller, hook, and fallback assertions.
</source_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add passkey fixtures and deterministic ceremony stubs</name>
  <files>test/example/test/support/fixtures/auth_fixtures.ex</files>
  <read_first>
    test/example/test/support/fixtures/auth_fixtures.ex
    test/example/lib/example/accounts.ex
    test/example/lib/example/accounts/user_passkey.ex
    test/example/lib/example_web/controllers/session_controller.ex
  </read_first>
  <behavior>
    - Tests can insert a valid `user_passkeys` row without real WebAuthn hardware.
    - Tests can stub registration and authentication ceremony verification through the `:passkey_ceremony_module` seam planned in 21-05.
    - Stubbed success returns concrete passkey rows and users; stubbed failure returns exact atoms used by controller error mapping.
    - Stubs are reset after each test and never persist across async test boundaries.
  </behavior>
  <action>
    Extend `Example.AccountsFixtures` with:
    - `passkey_fixture(user, attrs \\ %{})`: inserts a `Example.Accounts.UserPasskey` row with fields matching the generated schema: `user_id`, `credential_id`, `public_key`, `sign_count`, `aaguid`, `nickname`, `device_hint`, `transports`, `rp_id`, `last_used_at`, timestamps. Defaults must include `credential_id: "credential-" <> Integer.to_string(System.unique_integer([:positive]))`, encrypted/accepted `public_key` value compatible with the schema type, `sign_count: 0`, `aaguid: "00000000-0000-0000-0000-000000000000"`, `nickname: "Test passkey"`, `device_hint: "Test Device"`, `transports: ["internal"]`, and `rp_id: "localhost"`.
    - `stub_passkey_ceremony(result_fun)`: creates or reuses a nested test module such as `Example.AccountsFixtures.PasskeyCeremonyStub`, stores `result_fun` in `:persistent_term` under a unique module key, sets `Application.put_env(:example, :passkey_ceremony_module, Example.AccountsFixtures.PasskeyCeremonyStub)`, and returns an `on_exit` cleanup function that deletes the persistent term and resets the app env.
    - The stub module must expose the same functions the 21-05 seam calls, with exact names `register_passkey(user, response, opts)` and `authenticate_passkey(email_or_user, response, opts)`. These functions call `result_fun.({:register, user, response, opts})` or `result_fun.({:authenticate, email_or_user, response, opts})`.
    - Helper `encoded_passkey_response(attrs \\ %{})` returning a JSON string suitable for controller params under `passkey["response"]`. Include at least `"id"`, `"rawId"`, `"type" => "public-key"`, and `"response"` keys.
  </action>
  <verify>
    <automated>cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def passkey_fixture|def stub_passkey_ceremony|def encoded_passkey_response|:passkey_ceremony_module|register_passkey\\(|authenticate_passkey\\(" test/example/test/support/fixtures/auth_fixtures.ex` returns all helper/seam strings.
    - `rg -n "credential_id|public_key|sign_count|aaguid|nickname|device_hint|transports|rp_id|last_used_at" test/example/test/support/fixtures/auth_fixtures.ex` returns all passkey fixture fields.
  </acceptance_criteria>
  <done>Example tests have deterministic passkey fixture and ceremony-stub helpers that use the 21-05 seam instead of fake browser hardware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add example app integration tests and browser smoke for success, error, and handoff flows</name>
  <files>test/example/test/example_web/controllers/confirmation_controller_test.exs, test/example/test/example_web/controllers/passkey_session_controller_test.exs, test/example/test/example_web/live/registration_live_test.exs, test/example/test/example_web/live/passkey_mfa_challenge_live_test.exs, test/example/test/example_web/live/passkey_settings_live_test.exs, test/example/priv/playwright/tests/passkey-login.spec.ts</files>
  <read_first>
    test/example/test/example_web/controllers/session_controller_test.exs
    test/example/test/support/fixtures/auth_fixtures.ex
    test/example/test/support/conn_case_helpers.ex
    test/example/lib/example_web/controllers/confirmation_controller.ex
    test/example/lib/example_web/controllers/session_controller.ex
    test/example/lib/example_web/controllers/session_html.ex
    test/example/lib/example_web/live/registration_live.ex
    test/example/lib/example_web/live/mfa_settings_live.ex
    test/example/lib/example_web/live/mfa_challenge_live.ex
    test/example/priv/playwright/playwright.config.ts
    test/example/priv/playwright/tests/passkeys-hooks.spec.ts
  </read_first>
  <behavior>
    - `POST /users/log_in/passkey` success creates or rotates a logged-in session through `UserAuth.log_in_user/3` or the planned helper seam, redirects to `/`, and sets a session cookie.
    - `POST /users/mfa/passkey` success clears `:mfa_pending`, writes the upgraded token via `UserAuth.put_user_session_token/2`, clears `:mfa_remember_me`, and redirects to `:mfa_return_to`.
    - `POST /users/settings/mfa/passkeys` under sudo adds a passkey and triggers the registration notification path.
    - Invalid/error-path tests remain: passkey login failure, MFA failure, duplicate enrollment, unsupported/abort copy, and metadata hiding.
    - Signup/confirmation enrollment handoff tests remain: `user[enroll_passkey]`, `?enroll_passkey=1`, and confirmed-email redirect through `/users/sudo?return_to=/users/settings/mfa#passkeys`.
    - Playwright smoke proves the generated passkey-primary login page has `#passkey_login_form`, keeps the identifier field visible, keeps `Use password instead` and `Email me a magic link` visible, feature-detects conditional mediation before using it, and maps unsupported/abort to fallback UI without hiding recovery controls.
  </behavior>
  <action>
    Create or extend tests:
    - `passkey_session_controller_test.exs`:
      - GET `/users/log_in` remains dead-rendered: refute `phx-submit` and `data-phx-session`; assert `/users/log_in/passkey`, `Continue with passkey`, `Use password instead`, `Email me a magic link`, and `autocomplete="username webauthn"`.
      - Successful passkey-primary login: create a confirmed user and a passkey fixture; call `stub_passkey_ceremony(fn {:authenticate, email, _response, _opts} -> assert email == user.email; {:ok, user, passkey} end)`; POST `/users/log_in/passkey` with `%{"user" => %{"email" => user.email}, "passkey" => %{"response" => encoded_passkey_response()}}`; assert redirect to `/`, non-empty `set-cookie`, and a standard session token row for the user. The assertion string must include `UserAuth.log_in_user/3` in the test name or comment so the controller-owned helper path is explicit.
      - Invalid passkey-primary login: stub `{:error, :invalid_passkey}`; assert redirect to `/users/log_in` with flash `We couldn't finish passkey sign-in. Try again or use another way to continue.` and no new standard session.
      - Successful MFA passkey completion: create `mfa_pending_session_fixture`, insert a passkey fixture, put session keys `:mfa_pending`, `:mfa_return_to` set to `"/dashboard"`, and `:mfa_remember_me` set to true; stub `{:ok, user, passkey}`; POST `/users/mfa/passkey`; assert redirect to `"/dashboard"`, `get_session(conn, :mfa_pending) == nil`, `get_session(conn, :mfa_return_to) == nil`, `get_session(conn, :mfa_remember_me) == nil`, and the upgraded session token was written by asserting a `type: "standard"` session row exists for the user. The assertion string must include `UserAuth.put_user_session_token/2` in the test name or comment.
      - Invalid MFA passkey completion: stub `{:error, :invalid_passkey}`; assert redirect back to `/users/mfa`, pending keys remain, and generic recovery copy is shown.
    - `passkey_settings_live_test.exs`:
      - Logged-in sudo-fresh user visiting `/users/settings/mfa` sees `id="passkeys"`, `Passkeys`, `Add passkey`, `No passkeys added yet`, `Use Face ID, Touch ID, Windows Hello`; refute raw `credential_id`, `rp_id`, and `transports`.
      - Successful sudo-gated enrollment: log in with a fresh sudo session; call `stub_passkey_ceremony(fn {:register, ^user, _response, _opts} -> {:ok, passkey_fixture(user, nickname: "MacBook Touch ID")} end)`; POST `/users/settings/mfa/passkeys` with `%{"passkey" => %{"response" => encoded_passkey_response(), "nickname" => "MacBook Touch ID"}}`; assert redirect or live patch back to `/users/settings/mfa#passkeys`, database passkey count increases to 1, rendered page includes `MacBook Touch ID`, and the registration notification path fired. Assert the notification by using the example app's Swoosh test mailbox or existing email capture helper and checking subject/copy contains `New passkey added` or `A new passkey was added to your account`.
      - Duplicate/error enrollment: stub `{:error, :duplicate_credential}`; assert copy `This passkey is already registered` and count does not increase.
      - Stale-sudo delete rejection: render the settings page while sudo-fresh, then mutate/expire the session freshness or build a POST without fresh sudo to `/users/settings/mfa/passkeys/:id/delete`; assert `Sigra.Plug.RequireSudo` redirects/challenges instead of deleting, passkey count stays unchanged, and the test name/comment includes `stale sudo delete is rejected at request time`.
    - `registration_live_test.exs`:
      - Enable passkey-primary config in test setup; render `/users/register`; assert `Add a passkey after creating your account`, `user[enroll_passkey]`, and password signup fields remain.
      - Submit valid user params with `"enroll_passkey" => "true"` and assert the captured confirmation URL includes `?enroll_passkey=1`.
      - Submit without enrollment and assert the captured confirmation URL does not include `enroll_passkey=1`.
    - `confirmation_controller_test.exs`:
      - Create an unconfirmed user and confirmation token; GET `/users/confirm/:token?enroll_passkey=1`; assert redirect to `/users/sudo?return_to=/users/settings/mfa#passkeys` after login/session handoff and flash `Your email has been confirmed.`.
      - Assert normal `/users/confirm/:token` preserves the existing non-enrollment redirect behavior.
    - `passkey_mfa_challenge_live_test.exs`:
      - Create/log in an MFA pending user with at least one passkey fixture row; visit `/users/mfa`; assert `Continue with passkey`, `Use authenticator code instead`, `Use a backup code`, `PasskeyAuthenticate`, `/users/mfa/passkey`, and refute `role="tablist"`.
      - Assert unsupported/abort guidance contains neutral copy `Passkey sign-in was canceled.` and fallback action `Use another way`.
    - `test/example/priv/playwright/tests/passkey-login.spec.ts`:
      - Author this browser smoke spec in Task 2, but do not run Playwright in Task 2's task-local `<verify>` command; Playwright is reserved for Task 3/final phase gate to keep fast feedback in ExUnit/Node-stub checks.
      - Use the existing Playwright server/auth fixture style from `passkeys-hooks.spec.ts` or `golden-path.spec.ts`; do not require real WebAuthn hardware.
      - Open `/users/log_in` with passkey-primary enabled and assert `#passkey_login_form`, `input[autocomplete="username webauthn"]`, `#passkey_login_button`, `Continue with passkey`, `Use password instead`, and `Email me a magic link` are visible at the same time.
      - Stub `window.PublicKeyCredential.isConditionalMediationAvailable` to record calls and return false; assert the page remains usable and the password/magic-link fallbacks stay visible.
      - Stub unsupported/abort behavior from the WebAuthn path; click `Continue with passkey`; assert user-safe copy such as `Passkeys aren't available in this browser.` or `Passkey sign-in was canceled.`, assert no raw `AbortError` or `NotAllowedError` text appears, and assert the identifier field plus fallback actions remain visible.
      - Assert the browser attempts the options request to `/users/log_in/passkey/options` when the passkey button is used and does not navigate away before `/users/log_in/passkey` form submission is prepared.
  </action>
  <verify>
    <automated>cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/example_web/controllers/confirmation_controller_test.exs test/example_web/controllers/passkey_session_controller_test.exs test/example_web/live/registration_live_test.exs test/example_web/live/passkey_settings_live_test.exs test/example_web/live/passkey_mfa_challenge_live_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "UserAuth.log_in_user/3|/users/log_in/passkey|set-cookie|We couldn't finish passkey sign-in|autocomplete=\\\"username webauthn\\\"" test/example/test/example_web/controllers/passkey_session_controller_test.exs` returns all login success and failure strings.
    - `rg -n "UserAuth.put_user_session_token/2|/users/mfa/passkey|:mfa_pending|:mfa_return_to|:mfa_remember_me|standard" test/example/test/example_web/controllers/passkey_session_controller_test.exs` returns MFA success assertions.
    - `rg -n "/users/settings/mfa/passkeys|New passkey added|A new passkey was added to your account|This passkey is already registered|MacBook Touch ID|credential_id|rp_id|transports" test/example/test/example_web/live/passkey_settings_live_test.exs` returns enrollment success, notification, duplicate, and metadata assertions.
    - `rg -n "stale sudo delete is rejected at request time|/users/settings/mfa/passkeys/.*/delete|RequireSudo|count stays unchanged|Passkey deleted" test/example/test/example_web/live/passkey_settings_live_test.exs` returns stale-sudo delete rejection coverage.
    - `rg -n "user\\[enroll_passkey\\]|Add a passkey after creating your account|\\?enroll_passkey=1|enroll_passkey" test/example/test/example_web/live/registration_live_test.exs` returns signup enrollment assertions.
    - `rg -n "/users/sudo\\?return_to=/users/settings/mfa#passkeys|Your email has been confirmed|enroll_passkey" test/example/test/example_web/controllers/confirmation_controller_test.exs` returns confirmation handoff assertions.
    - `rg -n "Continue with passkey|Use authenticator code instead|Use a backup code|Passkey sign-in was canceled|Use another way|role=\\\"tablist\\\"" test/example/test/example_web/live/passkey_mfa_challenge_live_test.exs` returns challenge/fallback assertions.
    - `rg -n "passkey_login_form|username webauthn|Use password instead|Email me a magic link|isConditionalMediationAvailable|/users/log_in/passkey/options|/users/log_in/passkey|AbortError|NotAllowedError|Passkeys aren't available in this browser|Passkey sign-in was canceled" test/example/priv/playwright/tests/passkey-login.spec.ts` returns browser-smoke assertions and raw-error refutations.
  </acceptance_criteria>
  <done>Example integration tests cover successful passkey login, successful MFA completion, successful sudo enrollment notification, invalid/error paths, signup/confirmation enrollment handoff, fallback UI, and Playwright smoke coverage is authored for the final gate only.</done>
</task>

<task type="auto">
  <name>Task 3: Run final focused Phase 21 verification gates</name>
  <files>test/example/test/support/fixtures/auth_fixtures.ex, test/example/test/example_web/controllers/confirmation_controller_test.exs, test/example/test/example_web/controllers/passkey_session_controller_test.exs, test/example/test/example_web/live/registration_live_test.exs, test/example/test/example_web/live/passkey_mfa_challenge_live_test.exs, test/example/test/example_web/live/passkey_settings_live_test.exs, test/example/priv/playwright/tests/passkey-login.spec.ts</files>
  <read_first>
    test/example/test/support/fixtures/auth_fixtures.ex
    test/example/test/example_web/controllers/confirmation_controller_test.exs
    test/example/test/example_web/controllers/passkey_session_controller_test.exs
    test/example/test/example_web/live/registration_live_test.exs
    test/example/test/example_web/live/passkey_mfa_challenge_live_test.exs
    test/example/test/example_web/live/passkey_settings_live_test.exs
    test/example/priv/playwright/tests/passkey-login.spec.ts
    test/sigra/install/generator_passkeys_foundation_test.exs
    test/sigra/install/generator_passkey_management_test.exs
    test/sigra/install/generator_passkey_mfa_challenge_test.exs
    test/sigra/install/generator_passkey_primary_login_test.exs
    test/sigra/install/features/passkeys_js_test.exs
  </read_first>
  <action>
    Run the final focused verification commands:
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/install/generator_passkeys_foundation_test.exs test/sigra/install/generator_passkey_management_test.exs test/sigra/install/generator_passkey_mfa_challenge_test.exs test/sigra/install/generator_passkey_primary_login_test.exs test/sigra/install/features/passkeys_js_test.exs --max-failures 1`
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/example_web/controllers/confirmation_controller_test.exs test/example_web/controllers/passkey_session_controller_test.exs test/example_web/live/registration_live_test.exs test/example_web/live/passkey_settings_live_test.exs test/example_web/live/passkey_mfa_challenge_live_test.exs --max-failures 1`
    - `cd test/example/priv/playwright && npx playwright test tests/passkey-login.spec.ts`
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix compile --warnings-as-errors`
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix precommit`

    Follow `test/example/AGENTS.md`: use `mix precommit` as the final example-app gate after all changes and keep happy-path coverage for the example-app passkey flows. Keep the Node-stub unit tests from Plan 21-07; the Playwright smoke is additive browser coverage for the generated/controller-page binding and fallback visibility.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/install/generator_passkeys_foundation_test.exs test/sigra/install/generator_passkey_management_test.exs test/sigra/install/generator_passkey_mfa_challenge_test.exs test/sigra/install/generator_passkey_primary_login_test.exs test/sigra/install/features/passkeys_js_test.exs --max-failures 1 && (cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/example_web/controllers/confirmation_controller_test.exs test/example_web/controllers/passkey_session_controller_test.exs test/example_web/live/registration_live_test.exs test/example_web/live/passkey_settings_live_test.exs test/example_web/live/passkey_mfa_challenge_live_test.exs --max-failures 1) && (cd test/example/priv/playwright && npx playwright test tests/passkey-login.spec.ts) && (cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix compile --warnings-as-errors) && (cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix precommit)</automated>
  </verify>
  <acceptance_criteria>
    - Generator focused command exits with status 0.
    - Example integration command exits with status 0.
    - Playwright passkey login smoke exits with status 0 and confirms identifier/fallback visibility plus unsupported/abort recovery.
    - Example `mix compile --warnings-as-errors` exits with status 0.
    - Example `mix precommit` exits with status 0.
    - No command output contains `undefined function`, `undefined route`, or `undefined variable`.
  </acceptance_criteria>
  <done>Focused generator, hook, example-app integration, compile, and final example `mix precommit` gates pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client -> Phoenix controllers | Tests submit untrusted passkey completion params over controller POST routes. |
| Test ceremony stub -> Accounts wrappers | Stubbed verifier bypasses real WebAuthn only in ExUnit through app config. |
| Controller POST -> session cookie | Passkey completion can create or promote a session token. |
| Sudo session -> passkey registration | Enrollment mutation must require fresh sudo and notify the account owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06-01 | Spoofing | passkey-primary login tests | mitigate | Success test proves only the controller completion path creates a session; failure test proves invalid passkey responses do not create one. |
| T-21-06-02 | Elevation of Privilege | MFA passkey completion tests | mitigate | Success test asserts pending MFA keys are cleared and the upgraded token is written through `UserAuth.put_user_session_token/2`; failure test keeps pending state. |
| T-21-06-03 | Elevation of Privilege | passkey enrollment tests | mitigate | Enrollment success test runs under sudo and checks passkey count plus notification path; duplicate failure does not increase count. |
| T-21-06-04 | Information Disclosure | settings UI tests | mitigate | Tests refute raw credential metadata strings on the management surface. |
| T-21-06-05 | Denial of Service | JS hook and abort handling | mitigate | Final verification includes Phase 21 JS hook tests, challenge fallback copy assertions, and Playwright smoke proving unsupported/abort on the passkey-primary login page leaves fallback controls visible. |
| T-21-06-06 | Repudiation | registration notification path | mitigate | Enrollment success test asserts the registration notification email/copy fires. |
</threat_model>

<verification>
Run the Task 3 focused verification command, including generator tests, example integration tests, Playwright passkey-primary login smoke, example compile, and `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix precommit`.
</verification>

<success_criteria>
Plan succeeds when example happy-path passkey login, MFA passkey completion, sudo enrollment notification tests, and Playwright passkey-primary login smoke exist and pass alongside invalid/error-path, signup/confirmation handoff, focused generator/hook, compile, and example precommit gates.
</success_criteria>

<output>
After completion, create `.planning/phases/21-passkey-liveviews-post-auth-controller/21-06-SUMMARY.md`.
</output>
