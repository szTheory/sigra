---
phase: "179"
plan: "02"
type: execute
wave: 2
depends_on:
  - "179-01"
files_modified:
  - brandbook/logo-options/round-3/index.html
  - brandbook/logo-options/round-3/README.md
  - brandbook/logo-options/round-3/a1-rail-i-typemark.svg
  - brandbook/logo-options/round-3/a1-rail-i-typemark-dark.svg
  - brandbook/logo-options/round-3/a1-rail-i-favicon.svg
  - brandbook/logo-options/round-3/a2-descender-rail-typemark.svg
  - brandbook/logo-options/round-3/a2-descender-rail-typemark-dark.svg
  - brandbook/logo-options/round-3/a2-descender-rail-favicon.svg
  - brandbook/logo-options/round-3/a3-crossbar-s-typemark.svg
  - brandbook/logo-options/round-3/a3-crossbar-s-typemark-dark.svg
  - brandbook/logo-options/round-3/a3-crossbar-s-favicon.svg
  - brandbook/logo-options/round-3/a4-ember-dot-typemark.svg
  - brandbook/logo-options/round-3/a4-ember-dot-typemark-dark.svg
  - brandbook/logo-options/round-3/a4-ember-dot-favicon.svg
  - brandbook/logo-options/round-3/b1-redesigned-mark-primary.svg
  - brandbook/logo-options/round-3/b1-redesigned-mark-primary-dark.svg
  - brandbook/logo-options/round-3/b1-redesigned-mark-mark.svg
  - brandbook/logo-options/round-3/b1-redesigned-mark-favicon.svg
  - brandbook/logo-options/round-3/b2-letterform-sub-primary.svg
  - brandbook/logo-options/round-3/b2-letterform-sub-primary-dark.svg
  - brandbook/logo-options/round-3/b2-letterform-sub-favicon.svg
  - brandbook/logo-options/round-3/c1-stacked-primary.svg
  - brandbook/logo-options/round-3/c1-stacked-primary-dark.svg
  - brandbook/logo-options/round-3/c1-stacked-favicon.svg
autonomous: true
requirements:
  - BRAND2-05
  - BRAND2-06

must_haves:
  truths:
    - "7 candidates exist (A1–A4, B1, B2, C1), each with at minimum a primary SVG (light), primary-dark SVG, and favicon SVG"
    - "Every candidate has passed the render-critique loop: executor has read actual PNG screenshots (Read tool on generated /tmp/*.png files) and written a critique note per candidate per rubric row before the candidate was added to the gallery"
    - "At least 2 candidates are fully-integrated typemarks (A1, A2, A3, or A4 — motif worked INTO letterforms, no separate mark element)"
    - "All 7 candidates show a with-subtitle preview block in the round-3 gallery"
    - "The gallery index.html links `../../tokens.css` exactly once and parses as valid HTML"
    - "The gallery README.md rationale table has ≥7 data rows (one per candidate) plus header and separator"
    - "No candidate SVG encodes a subtitle/slogan in its primary lockup path data"
    - "All ember accent values fall within the hue 15–40° warm amber range (anchor: #c2410c)"
    - "The 16px favicon for every candidate has a readable silhouette (critique note confirms this per candidate)"
  artifacts:
    - path: "brandbook/logo-options/round-3/index.html"
      provides: "Round-3 gallery HTML"
      contains: "../../tokens.css"
    - path: "brandbook/logo-options/round-3/README.md"
      provides: "Rationale table"
      min_lines: 12
    - path: "brandbook/logo-options/round-3/a1-rail-i-typemark.svg"
      provides: "A1 integrated typemark — rail-i motif (Space Grotesk Bold)"
      contains: "Font:"
    - path: "brandbook/logo-options/round-3/a2-descender-rail-typemark.svg"
      provides: "A2 integrated typemark — descender rail (Plus Jakarta Sans ExtraBold)"
      contains: "Font:"
    - path: "brandbook/logo-options/round-3/b1-redesigned-mark-primary.svg"
      provides: "B1 refined combination lockup (Space Grotesk Bold + redesigned mark)"
      contains: "Font:"
  key_links:
    - from: "brandbook/logo-options/round-3/index.html"
      to: "brandbook/tokens.css"
      via: "relative link"
      pattern: "../../tokens.css"
    - from: "brandbook/logo-options/round-3/index.html"
      to: "brandbook/logo-options/round-3/*.svg"
      via: "<img src>"
      pattern: "img src"
    - from: "scripts/brand/outline-wordmark.mjs"
      to: "scripts/brand/fonts/*.ttf"
      via: "loadSync(fontPath)"
      pattern: "loadSync"
---

<objective>
Generate 7 logo candidates (A1–A4 Direction A integrated typemarks, B1–B2 Direction B refined lockups, C1 Direction C wildcard), run each through the Playwright render-critique loop at 16/32/54px and hero scale in light and dark, self-critique against the brief's 6-row rubric, iterate until clean, then assemble the `brandbook/logo-options/round-3/` gallery (index.html + README.md) for Phase 180 human ratification.

Purpose: The gallery is the deliverable that enables Phase 180's human selection gate. Candidate quality is gate-checked here via the render-critique loop before any file lands in the gallery — the 16px favicon legibility test is the kill test.

Output: 7 candidate SVG sets (21–28 SVG files total across light/dark/favicon variants), brandbook/logo-options/round-3/index.html (matching round-2 format), brandbook/logo-options/round-3/README.md (rationale table). Render screenshots and harness HTML stay in /tmp/ — never committed.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brandbook/logo-v2-design-brief.md
@.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-CONTEXT.md
@.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-RESEARCH.md
@.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md
@.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-01-SUMMARY.md

<interfaces>
<!-- Key references the executor needs. -->

Candidate set (7 candidates; executor builds all 7):

Direction A — Integrated typemarks (≥2–3 required; produce all 4):
  A1 — Rail-i typemark (Space Grotesk Bold wght=700)
       Integration: i tittle replaced by ember-300 (#fdba74) rectangular block (wide enough to survive 16px);
       g descender elongated, curves leftward for visual mass; all else monochrome #151515.
       Favicon: wordmark silhouette — the colored tittle block is the sole accent.
  A2 — Descender-rail typemark (Plus Jakarta Sans ExtraBold)
       Integration: g descender continues as horizontal rail bar with round cap below baseline (echoing
       Rail Accent stroke-linecap geometry); s entry/exit terminals cut flat (horizontal); r shoulder flat.
       ViewBox must include descender overflow (see Pitfall 3 mitigation in RESEARCH.md).
       Favicon: mark-only — rail bar motif extracted as standalone square 64×64 SVG.
  A3 — Crossbar-s typemark (Syne ExtraBold wght=800)
       Integration: s entry and exit terminals replaced with flat horizontal bars (two outer columns of
       Rail Accent mark); body of s bridges them (sinuous connector echoing rail structure).
       Note: If s-mod reduces legibility at 32px, fall back to terminal-only flat cuts — rubric word
       legibility is pass/fail, not gradable. Document the iteration in critique notes.
       Favicon: wordmark silhouette — s modification must read as s-like, not broken.
  A4 — Ember-dot minimal typemark (Inter Display Black wght=900, variable opsz=32)
       Integration: i tittle replaced by ember-700 (#c2410c) dot, 2–3× standard tittle size, positioned
       slightly above cap-height. Everything else is path-accurate Inter Display Black.
       This is the most conservative candidate — include to benchmark distinctiveness vs the rubric.
       Favicon: enlarged ember dot on minimal wordmark silhouette.

Direction B — Refined combination lockups (produce both):
  B1 — Redesigned mark + Space Grotesk tight lockup
       Mark redesign: three staggered bars simplified to TWO bars, heavier stroke (stroke-width:10 → 12),
       mark baseline extends 12px below wordmark baseline (boundary-breaking). No rectangular container.
       Mark placed left of wordmark with overlap into first letter's vertical stroke.
       Wordmark: Space Grotesk Bold wght=700.
       Files: b1-redesigned-mark-primary.svg, b1-redesigned-mark-primary-dark.svg,
              b1-redesigned-mark-mark.svg (mark alone, 64×64), b1-redesigned-mark-favicon.svg.
  B2 — Letterform-substitution lockup (Geist Black wght=900)
       The first "s" letterform is replaced by a custom glyph shaped like the Rail Accent mark's
       three-bar column (staggered bars as glyph-width shape). Rest of word is Geist Black outlined.
       The s-mark glyph width must match the Geist s-advance-width for spacing coherence.
       Favicon: the s-mark glyph extracted as favicon.

Direction C — Wildcard (produce 1):
  C1 — Stacked wordmark (Plus Jakarta Sans ExtraBold)
       "si" (top) / "gra" (bottom), tightly tracked, Rail Accent mark centered vertically between lines.
       Mark is embedded in interline space as a vertical accent.
       Favicon: mark alone (not the stacked wordmark — stacked is illegible at 16px).

Ember palette locked values (from brandbook/logo-mark.svg; tunable within hue 15–40°):
  ember-300: #fdba74   (lighter accents, i tittle for A1)
  ember-700: #c2410c   (primary anchor — tittle for A4, B2 s-mark accent bar)
  ember-800: #9a3412   (deep ember — horizontal rail core for A2/A3 terminal bars)

Light/dark fill values (from brandbook/logo-primary.svg + round-2 gallery):
  Light surface wordmark fill: #151515
  Dark surface wordmark fill:  #f4f1eb  (NOT white — this exact value)
  Dark surface background: #151515 (as per gallery .surface.dark CSS)

opentype.js usage (from Plan 01 toolchain):
  node scripts/brand/outline-wordmark.mjs <font.ttf> <wght> <output.svg>
  Output: per-glyph <path id="g-0"...id="g-4"> with fill="currentColor" on parent <g>

Round-2 gallery HTML classes (must replicate exactly):
  .option, .option-head, .preview, .lockups, .surface.light, .surface.dark, .lockup,
  .mark-lab, .mark-stage, .favicons, .favicon-sample, .notes, .scores, .score, .meter, .test
  Status pill: class="status" with text "Under review"
  Breadcrumbs: class="crumbs" linking ../round-2/index.html and ../../index.html

With-subtitle preview block (required per CONTEXT.md for all finalist-grade options):
  Second .lockups div showing img + span.word > strong + span with subtitle "Phoenix auth that ships"
  Uses style="margin-top: var(--sigra-space-2)" on the container div

Critique workpad file (quality mechanism — created during critique loop):
  /tmp/sigra-critique-notes.md  (throwaway — not committed)
  One section per candidate per iteration:
  ```
  ## A1 Rail-i — Iteration 1
  - 16px: [pass/fail + observation]
  - 32px: [pass/fail + observation]
  - 54px topbar: [pass/fail + observation]
  - hero: [pass/fail + observation]
  - Integration coherence: [note]
  - Ecosystem distinctiveness: [note]
  - Ember anchor: [pass/fail]
  - Tattoo test: [pass/fail]
  - Decision: [approved for gallery / iterate: change X]
  ```
  The SUMMARY.md for this plan must include a "Critique Summary" section with ≥1 iteration note
  per candidate, quoting the final pass decision for each.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate candidate SVGs via outline script + path surgery (A1–A4 integrated typemarks)</name>
  <files>
    brandbook/logo-options/round-3/a1-rail-i-typemark.svg,
    brandbook/logo-options/round-3/a1-rail-i-typemark-dark.svg,
    brandbook/logo-options/round-3/a1-rail-i-favicon.svg,
    brandbook/logo-options/round-3/a2-descender-rail-typemark.svg,
    brandbook/logo-options/round-3/a2-descender-rail-typemark-dark.svg,
    brandbook/logo-options/round-3/a2-descender-rail-favicon.svg,
    brandbook/logo-options/round-3/a3-crossbar-s-typemark.svg,
    brandbook/logo-options/round-3/a3-crossbar-s-typemark-dark.svg,
    brandbook/logo-options/round-3/a3-crossbar-s-favicon.svg,
    brandbook/logo-options/round-3/a4-ember-dot-typemark.svg,
    brandbook/logo-options/round-3/a4-ember-dot-typemark-dark.svg,
    brandbook/logo-options/round-3/a4-ember-dot-favicon.svg
  </files>

  <read_first>
    - /Users/jon/projects/sigra/brandbook/logo-v2-design-brief.md (full file — letterform integration anatomy: g descender, i tittle, s terminals, r shoulder; integration pitfalls; render-critique rubric 6 rows; OFL typeface candidates table)
    - /Users/jon/projects/sigra/brandbook/logo-primary.svg (full file — v1 reference: glyph group structure, per-glyph paths, viewBox, title/desc accessibility shell, fill colors)
    - /Users/jon/projects/sigra/brandbook/logo-mark.svg (full file — ember palette hex values, stroke-linecap:round, stroke-width values for the Rail Accent bars)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md (SVG accessibility shell; Ember Color Palette shared pattern; Light/Dark Wordmark Fill Colors)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-RESEARCH.md (Candidate Concept Space A1–A4 descriptions; Common Pitfalls 1–6; Pattern 1 forEachGlyph; Pitfall 3 viewBox padding for boundary-breaking overflow)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-01-SUMMARY.md (confirm toolchain is working; note font file paths)
  </read_first>

  <action>
  Create `brandbook/logo-options/round-3/` directory if it does not exist.

  For each of the four Direction A candidates, the workflow is: (1) generate wordmark base SVG via outline script, (2) hand-integrate the motif via SVG path/element surgery, (3) run critique-render.mjs to produce /tmp/ screenshots, (4) read the screenshots with the Read tool, (5) write critique notes to /tmp/sigra-critique-notes.md, (6) iterate if any rubric row fails, (7) once approved, save the final SVG to brandbook/logo-options/round-3/.

  STEP 1 — Generate base wordmark SVGs for each font:
  - `node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/SpaceGrotesk[wght].ttf 700 /tmp/a1-base.svg`
  - `node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/PlusJakartaSans-ExtraBold.ttf 800 /tmp/a2-base.svg` (use the ExtraBold static TTF; if Plus Jakarta Sans variable is available, use wght=800)
  - `node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/Syne[wght].ttf 800 /tmp/a3-base.svg`
  - `node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/Inter-VariableFont.ttf 900 /tmp/a4-base.svg` (opsz axis: if available, set opsz=32 for display variant; check font.variation properties after load)

  STEP 2 — Path surgery per candidate (edit the /tmp base SVGs with Write/Edit tools):

  A1 Rail-i: Open /tmp/a1-base.svg. Identify the `g-1` path (the `i` glyph). Locate its tittle subpath (the dot portion of the i) within the compound path `d` attribute — it is typically the second contour (second M command) in Space Grotesk Bold's i glyph. Replace the tittle contour with a rectangular subpath: a filled rectangle centered on the tittle position, sized approximately 70% of the glyph width × 70% wide (wide enough to survive 16px), in ember-300 (#fdba74). Implement as a separate `<rect>` element with `fill="#fdba74"` overlaying the tittle region, OR as a separate `<path>` element with the rectangle path data. Adjust the g-2 path (g glyph): extend the descender curve leftward by approximately 15% of the glyph's advance width for visual mass — this means modifying the final curve segment of the descender in the path d attribute. All other glyphs (g-0 s, g-3 r, g-4 a) remain as-is. Update `<desc>` to include `A1 Rail-i integration: i tittle → ember-300 block, g descender extended.`

  A2 Descender-rail: Open /tmp/a2-base.svg. Identify the `g-2` path (g glyph). Find the descender terminal point (lowest y coordinate in the g-2 path). Add a new SVG element after the `<g id="glyphs">` close tag: a `<line>` or `<path>` element representing the horizontal rail bar at the descender terminus, extending approximately 1.5× the glyph's x-width to the right, with `stroke="#9a3412"` (ember-800), `stroke-width` matching the font's stroke weight at the used UPM scale, `stroke-linecap="round"`. Flat-cut s terminals: identify the g-0 (s) path entry and exit terminal bezier curves and replace them with horizontal flat lines by editing the first and last curve segments of the s contour (set tangent handles to horizontal). For r shoulder terminal: edit the last segment of g-3 (r)'s arm path to terminate with a horizontal flat cut. ViewBox: extend the bottom padding to accommodate the descender rail bar — add at least 15% × fontSize to the vbH value, and update viewBox on the root svg element accordingly.

  A3 Crossbar-s: Open /tmp/a3-base.svg. Identify g-0 (s glyph). The modification: the s entry stroke (top-left terminal of the s spine) and exit stroke (bottom-right terminal) are flat-cut horizontal bars. Edit the first bezier in the s contour to terminate horizontally (adjust the final control point handle to be horizontal). Do the same for the exit terminal. Add two horizontal `<line>` elements as accent bars at the entry and exit positions with `fill="none" stroke="#9a3412" stroke-width="[match font stroke weight]" stroke-linecap="square"` — NOT round (to echo the rail bar's flat geometry). If the s-modification makes the letterform unreadable at 32px during critique, fall back: use only the flat-cut terminal modification without the accent bar strokes, and document this in critique notes.

  A4 Ember-dot: Open /tmp/a4-base.svg. Identify g-1 (i glyph). Find the tittle circle subpath. Replace it with a single `<circle>` element (or circular `<path>`) centered at the tittle's center position, with radius 2.5–3× the original tittle radius, `fill="#c2410c"` (ember-700). Position it 1.5× the standard tittle clearance above the i stem's cap-height. All other glyphs unchanged. Update desc: `A4 Ember-dot: i tittle → ember-700 circle, 3× size, above cap-height.`

  STEP 3 — Generate dark variants: For each candidate typemark SVG, create the dark variant by copying the file and changing wordmark fill to `#f4f1eb`. Ember accent values remain the same (they read fine on both surfaces). Filename: `[id]-typemark-dark.svg`.

  STEP 4 — Generate favicon SVGs: For each candidate, create a `[id]-favicon.svg` at a 64×64 viewBox:
  - A1: crop to show only the i+g portion of the typemark with the colored tittle block prominent
  - A2: extract the descender rail bar motif as a 64×64 square (the bar + descender hook)
  - A3: crop to show only the s glyph with the horizontal bar treatment
  - A4: the enlarged ember-700 dot on a white/transparent background (minimal — the dot IS the favicon)

  STEP 5 — Critique loop: For EACH candidate A1–A4:
  - Create a throwaway harness HTML at /tmp/sigra-harness-[id].html: minimal HTML with the candidate SVG embedded at natural width, plus a dark-mode media query. The harness displays the SVG at multiple zoom levels via CSS: .scale-16 { width: 16px }, .scale-32 { width: 32px }, etc.
  - Run: `node scripts/brand/critique-render.mjs /tmp/sigra-harness-[id].html /tmp/sigra-renders/[id]`
  - USE THE READ TOOL to read each generated PNG: `/tmp/sigra-renders/[id]/[id]-16px-favicon-light.png`, `/tmp/sigra-renders/[id]/[id]-16px-favicon-dark.png`, `/tmp/sigra-renders/[id]/[id]-54px-topbar-light.png`, `/tmp/sigra-renders/[id]/[id]-hero-light.png`
  - After reading each PNG, write a critique note to /tmp/sigra-critique-notes.md covering all 6 rubric rows: wordmark legibility, integration coherence, ecosystem distinctiveness, ember anchor, scale durability (16px favicon), tattoo test. RECORD the decision: "approved for gallery" or "iterate: [specific change]".
  - If the critique results in "iterate", make the SVG change and repeat from the critique-render step. Document the iteration in /tmp/sigra-critique-notes.md as "Iteration 2", etc.
  - Only move the SVG to brandbook/logo-options/round-3/ after achieving "approved for gallery" status.

  STEP 6 — Write all approved A1–A4 SVGs to their final destinations in brandbook/logo-options/round-3/.

  ACCESSIBILITY REQUIREMENT: All candidate SVGs must include `role="img" aria-labelledby="title desc"`, `<title id="title">[Candidate name]</title>`, `<desc id="desc">Font: [name] v[ver] (OFL) wght=[N]. [integration description]. Outlined with opentype.js 2.0.0.</desc>`.
  </action>

  <verify>
    <automated>
      ls /Users/jon/projects/sigra/brandbook/logo-options/round-3/a{1,2,3,4}-*.svg | wc -l && python3 -c "
import xml.etree.ElementTree as ET, glob
files = glob.glob('/Users/jon/projects/sigra/brandbook/logo-options/round-3/a*.svg')
for f in files:
    ET.parse(f)
print(f'All {len(files)} A-candidate SVGs parse as valid XML')
" && grep -l 'Font:' /Users/jon/projects/sigra/brandbook/logo-options/round-3/a*.svg | wc -l
    </automated>
  </verify>

  <acceptance_criteria>
    - `ls brandbook/logo-options/round-3/a*.svg | wc -l` returns 12 (4 candidates × 3 files: typemark + typemark-dark + favicon)
    - All 12 SVG files parse as valid XML (python3 ET.parse passes for each)
    - `grep -l 'Font:' brandbook/logo-options/round-3/a*.svg | wc -l` returns 12 (provenance in every file)
    - `grep -rn 'ember\|#fdba74\|#c2410c\|#9a3412' brandbook/logo-options/round-3/a*.svg | wc -l` returns ≥ 4 (ember accent present in each candidate)
    - QUALITY GATE: The executor has used the Read tool to inspect PNG screenshots for every A-candidate at all 4 scales × 2 color schemes (minimum 32 Read calls across the 4 candidates), and has written critique notes to /tmp/sigra-critique-notes.md covering all 6 rubric rows per candidate, with an explicit "approved for gallery" decision recorded for each
    - CRITIQUE EVIDENCE: /tmp/sigra-critique-notes.md exists and contains entries for A1, A2, A3, A4 with rubric rows and pass decisions (verify existence: `ls /tmp/sigra-critique-notes.md`)
    - The A2 SVG viewBox Y dimension is larger than the A1/A3/A4 viewBox Y dimension (descender rail requires extra bottom padding — verify with `grep viewBox brandbook/logo-options/round-3/a2-*.svg`)
    - Dark variants use `#f4f1eb` for wordmark fill (not `#ffffff`): `grep -L '#f4f1eb' brandbook/logo-options/round-3/a*-dark.svg` returns empty
    - No candidate primary SVG contains a subtitle/slogan string (grep for "Phoenix auth" in primary SVGs returns empty): `grep -l 'Phoenix auth' brandbook/logo-options/round-3/a*-typemark.svg` returns empty
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Generate B1, B2, C1 candidates + complete critique loop for all 7</name>
  <files>
    brandbook/logo-options/round-3/b1-redesigned-mark-primary.svg,
    brandbook/logo-options/round-3/b1-redesigned-mark-primary-dark.svg,
    brandbook/logo-options/round-3/b1-redesigned-mark-mark.svg,
    brandbook/logo-options/round-3/b1-redesigned-mark-favicon.svg,
    brandbook/logo-options/round-3/b2-letterform-sub-primary.svg,
    brandbook/logo-options/round-3/b2-letterform-sub-primary-dark.svg,
    brandbook/logo-options/round-3/b2-letterform-sub-favicon.svg,
    brandbook/logo-options/round-3/c1-stacked-primary.svg,
    brandbook/logo-options/round-3/c1-stacked-primary-dark.svg,
    brandbook/logo-options/round-3/c1-stacked-favicon.svg
  </files>

  <read_first>
    - /Users/jon/projects/sigra/brandbook/logo-v2-design-brief.md (Direction B and C guidance; all 7 hard constraints; render-critique rubric)
    - /Users/jon/projects/sigra/brandbook/logo-mark.svg (full file — current Rail Accent mark paths, stroke values, viewBox 64×64 — B1 redesigns this; B2 extracts a variant as a glyph substitute)
    - /Users/jon/projects/sigra/brandbook/logo-primary.svg (full file — combination lockup composition reference; mark+wordmark positioning)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-RESEARCH.md (B1 concept: mark simplification to 2 bars, boundary-breaking extension below wordmark baseline; B2 concept: Geist Black s-replacement with 3-bar column; C1 concept: stacked si/gra with mark in interline)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md (SVG accessibility shell; mark viewBox 64×64 convention; stroke attributes from logo-mark.svg)
  </read_first>

  <action>
  Follow the same outline → path surgery → critique loop → write-to-gallery workflow from Task 1.

  B1 — Redesigned mark + Space Grotesk tight lockup:
  - Generate Space Grotesk base wordmark (reuse /tmp/a1-base.svg or re-run outline script at wght=700)
  - Build the redesigned mark as a new SVG group: TWO staggered vertical bars (not three), with heavier stroke-width=12 (vs v1's stroke-width=8), stroke-linecap="round". Bar heights: tall bar `M17 10v22`, short bar `M32 20v22` (staggered). Add the horizontal cross bar at the midpoint of the tall bar: `M11 21h16` with stroke-width=4 stroke="#9a3412". Ember tone on the taller bar: stroke="#fdba74", shorter bar: stroke="#c2410c". ViewBox for the mark: 64×64.
  - Composition: the mark is placed LEFT of the wordmark (conventional side), but extends 12px BELOW the wordmark's baseline (boundary-breaking — the mark bottom y exceeds the wordmark bottom y). The mark's right edge overlaps or touches the first stroke of the "s" glyph. No rectangular container behind mark or wordmark. The combined SVG viewBox must include the vertical overflow from the mark extension.
  - Files: b1-redesigned-mark-primary.svg (light: #151515 wordmark + ember mark), b1-redesigned-mark-primary-dark.svg (#f4f1eb wordmark + ember mark), b1-redesigned-mark-mark.svg (64×64 mark alone, for favicon row), b1-redesigned-mark-favicon.svg (64×64 favicon: simplified two-bar mark, heavier strokes).

  B2 — Letterform-substitution lockup (Geist Black):
  - Generate Geist Black base wordmark: `node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/Geist-Black.ttf 900 /tmp/b2-base.svg` (or nearest available Geist weight)
  - Replace the g-0 path (s glyph) with a custom Rail Accent-styled glyph: three vertical bars arranged within the advance width of the original s glyph. Bar spacing: divide the s advance width into thirds. Heights: left bar tall, middle bar medium (staggered), right bar short — mirroring the Rail Accent pattern at glyph scale. Bar width: match the Geist Black stroke weight. Color: bars use fill, not stroke, to match the rest of the outlined wordmark. Use ember-700 (#c2410c) for the bars.
  - The resulting wordmark looks like: [bar-column][igra in Geist Black]. The "s" is implicitly read from the mark-like glyph at the start of the word.
  - Files: b2-letterform-sub-primary.svg, b2-letterform-sub-primary-dark.svg, b2-letterform-sub-favicon.svg (the s-mark column extracted to 64×64).

  C1 — Stacked wordmark (Plus Jakarta Sans ExtraBold):
  - Generate Plus Jakarta Sans base: `node scripts/brand/outline-wordmark.mjs scripts/brand/fonts/PlusJakartaSans-ExtraBold.ttf 800 /tmp/c1-base.svg`
  - The composition is hand-built SVG (not from a single script run): two text-as-path rows "si" and "gra", each outlined at wght=800. The script is run twice — once with text "si" and once with "gra" — but the outline-wordmark.mjs script targets "sigra" hardcoded. Therefore: run with full wordmark, then extract g-0 (s) + g-1 (i) paths into one group and g-2 (g) + g-3 (r) + g-4 (a) into another group, positioning them as two staggered lines. OR: write the stacked composition manually as two sub-groups with the Rail Accent mark (from logo-mark.svg, scaled down ~50%) centered in the interline space.
  - Stacking parameters: track each line to -5% letter-spacing for tighter appearance; the two rows are left-aligned; interline gap = approximately 1× cap-height; the mark SVG group is scaled to ~40% of its normal size and centered between the lines, offset to the right by ~50% of the wordmark width.
  - Files: c1-stacked-primary.svg, c1-stacked-primary-dark.svg, c1-stacked-favicon.svg (IMPORTANT: the favicon for C1 is the Rail Accent mark ALONE, not the stacked wordmark — per RESEARCH.md "C1: 16px risk HIGH — stacked wordmark not legible at favicon scale").

  CRITIQUE LOOP for B1, B2, C1 (same protocol as Task 1):
  - Create throwaway harness HTML for each candidate at /tmp/sigra-harness-[id].html
  - Run: `node scripts/brand/critique-render.mjs /tmp/sigra-harness-[id].html /tmp/sigra-renders/[id]`
  - USE THE READ TOOL to read generated PNGs for each scale and color scheme
  - Write critique notes to /tmp/sigra-critique-notes.md (append to existing A1–A4 notes)
  - Critical test for B2: at 16px, does the bar-column s-substitute read as a letter-like shape? Critique note must explicitly address this. If it reads as pure decoration, iterate.
  - Critical test for C1 stacked at 54px: does the topbar slot clip the top or bottom of the composition? If so, adjust viewBox padding and re-render.
  - APPROVED DECISION required before any candidate SVG is written to brandbook/logo-options/round-3/

  After all 7 candidates are approved:
  - Verify `ls brandbook/logo-options/round-3/*.svg | wc -l` returns ≥ 21 (7 candidates × ≥3 files each)
  - B1 has 4 files (primary + primary-dark + mark + favicon); all others have 3 minimum
  </action>

  <verify>
    <automated>
      ls /Users/jon/projects/sigra/brandbook/logo-options/round-3/*.svg | wc -l && python3 -c "
import xml.etree.ElementTree as ET, glob
files = glob.glob('/Users/jon/projects/sigra/brandbook/logo-options/round-3/*.svg')
errors = []
for f in files:
    try:
        ET.parse(f)
    except Exception as e:
        errors.append(f'{f}: {e}')
if errors:
    print('INVALID:', errors)
else:
    print(f'All {len(files)} SVGs parse as valid XML')
" && git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'
    </automated>
  </verify>

  <acceptance_criteria>
    - `ls brandbook/logo-options/round-3/*.svg | wc -l` returns ≥ 22 (at minimum: 4×3 for A-set + 4 for B1 + 3 for B2 + 3 for C1 = 22)
    - All SVGs in round-3/ parse as valid XML
    - `git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'` returns empty (no regressions)
    - QUALITY GATE: Executor has used Read tool on PNG screenshots for B1, B2, C1 (minimum 24 additional Read calls — 3 candidates × 4 scales × 2 color schemes) and written critique notes with pass decisions to /tmp/sigra-critique-notes.md
    - B1's primary SVG viewBox is taller than a standard wordmark viewBox (mark extends below baseline): `grep viewBox brandbook/logo-options/round-3/b1-*-primary.svg` shows a Y-dimension ≥ 15% larger than typical
    - C1's favicon SVG is the Rail Accent mark (not the stacked wordmark): `grep 'stagger\|M17\|M32\|M47' brandbook/logo-options/round-3/c1-stacked-favicon.svg` returns ≥ 1 match (the bar paths from logo-mark.svg are present)
    - All ember accents are within hue 15–40° (visual check during critique: documented in critique notes)
    - /tmp/sigra-critique-notes.md contains entries for all 7 candidates (B1, B2, C1 appended in this task): `grep -c '^## ' /tmp/sigra-critique-notes.md` returns ≥ 7
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Round-3 gallery index.html + README.md</name>
  <files>
    brandbook/logo-options/round-3/index.html,
    brandbook/logo-options/round-3/README.md
  </files>

  <read_first>
    - /Users/jon/projects/sigra/brandbook/logo-options/round-2/index.html (full file — EXACT structural template to replicate: head/CSS link, topbar/crumbs/status-pill, header section, per-option article structure, .option/.option-head/.preview/.lockups/.surface/.mark-lab/.favicons/.notes/.scores/.meter/.test class names, inline score meter percentages, favicon-sample sizes, CSS rules for .surface.light and .surface.dark)
    - /Users/jon/projects/sigra/brandbook/logo-options/round-2/README.md (full file — format template: heading, description line, Files table columns, separator row format, closing link)
    - /Users/jon/projects/sigra/.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-PATTERNS.md (Gallery index.html section — full pattern for topbar crumbs update, per-option article, with-subtitle preview variant, favicon row; README.md section — column order, separator format)
    - /Users/jon/projects/sigra/brandbook/tokens.css (confirm the file exists and the relative path ../../tokens.css from round-3/ is valid)
    - /tmp/sigra-critique-notes.md (strength/risk notes and score estimates for each candidate — use to populate .notes section and .meter percentages in the gallery)
  </read_first>

  <action>
  Create `brandbook/logo-options/round-3/index.html` replicating the round-2 structure EXACTLY: same HTML skeleton, same CSS rules inlined in `<style>`, same class names. Differences from round-2:

  HEAD:
  - Title: `Sigra Logo Options — Round 3`
  - CSS link path stays `../../tokens.css` (unchanged — correct relative path for this location)

  TOPBAR/CRUMBS:
  - Crumbs: `<a href="../round-2/index.html">Round 2 archive</a>`, `<a href="../../index.html">Brandbook</a>`, `<a href="README.md">Round 3 notes</a>`
  - Status pill: `<span class="status">Under review</span>` (not "Archive study")

  HEADER:
  - Code tag: `Round 3 exploration / integrated typemarks / rail motif`
  - H1: something appropriate like `Typemarks, not icon + text.`
  - Lead paragraph: brief description of what round 3 explores vs round 2 (integrated typemarks, boundary-breaking, no rectangular container)
  - Rules list: the 7 hard constraints from the design brief (abbreviated for the gallery header)

  PER-OPTION ARTICLES — produce one `<article class="option" id="option-[id]">` for each of the 7 candidates in order: A1, A2, A3, A4, B1, B2, C1. Each article follows the EXACT round-2 per-option structure:
  - `.option-head` div: code tag (A1/A2/etc.), h2 (candidate name), p (one-sentence concept)
  - `.preview` div containing:
    - First `.lockups` div: `.surface.light` and `.surface.dark` each with `.lockup` containing `<img src="[candidate]-typemark.svg" alt="">` for A-candidates; `<img src="[candidate]-primary.svg" alt="">` for B/C-candidates
    - Second `.lockups` div with `style="margin-top: var(--sigra-space-2)"`: SAME structure but with a subtitle span added: `<span class="word"><strong>sigra</strong><span>Phoenix auth that ships</span></span>` (the with-subtitle variant block required per CONTEXT.md for all candidates)
    - `.mark-lab` div with `.mark-stage` and `.favicons`: for A-candidates (no separate mark), `.mark-stage` shows the favicon SVG. For B1, shows `b1-redesigned-mark-mark.svg`. Favicon row: three `.favicon-sample` spans with inline `style="width:32px;height:32px"`, `style="width:24px;height:24px"`, `style="width:16px;height:16px"` — all three sizes required
  - `.notes` div: strength paragraph, risk paragraph, `.scores` div with ≥3 `.score` rows (Appeal, Distinctive, Concept), `.test` paragraph stating what hypothesis each candidate tests against the rubric. Populate meter percentages from critique notes.

  B1 gets an additional `.mark-lab` section with a mark-stage showing `b1-redesigned-mark-mark.svg` at natural size alongside the favicon row.

  C1 note in `.notes`: "Favicon uses mark alone — stacked wordmark is illegible at 16px; the mark IS the favicon for this candidate."

  IMPORTANT: the with-subtitle lockup block shows the candidate SVG (not a separate subtitle-variant SVG file — no subtitle-variant SVG files are generated this phase, per deferred scope). The subtitle text "Phoenix auth that ships" is in the surrounding HTML, not baked into the SVG path.

  Create `brandbook/logo-options/round-3/README.md` following the round-2 format:
  - H1: `Sigra Logo Options — Round 3`
  - One-sentence description of what round 3 explores
  - Paragraph: "The active Sigra logo source set lives in the parent `brandbook/` directory."
  - `## Files` heading
  - Pipe table with columns `Option | File | What it tests` and one row per SVG group (use the primary/typemark SVG as the file link per option — not all variants): A1, A2, A3, A4, B1, B2, C1 (7 data rows + header + separator = ≥9 pipe rows)
  - Separator: `| --- | --- | --- |` (three cells, no alignment markers per PATTERNS.md)
  - Closing line: `Open [index.html](index.html) to compare mark-only, light/dark lockup, and favicon previews.`
  </action>

  <verify>
    <automated>
      python3 -c "
from html.parser import HTMLParser
p = HTMLParser()
p.feed(open('/Users/jon/projects/sigra/brandbook/logo-options/round-3/index.html').read())
print('index.html: valid HTML')
" && grep -c 'tokens.css' /Users/jon/projects/sigra/brandbook/logo-options/round-3/index.html && grep -c '^\|' /Users/jon/projects/sigra/brandbook/logo-options/round-3/README.md && grep -c 'Under review' /Users/jon/projects/sigra/brandbook/logo-options/round-3/index.html && grep -c 'favicon-sample' /Users/jon/projects/sigra/brandbook/logo-options/round-3/index.html
    </automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "from html.parser import HTMLParser; p=HTMLParser(); p.feed(open('brandbook/logo-options/round-3/index.html').read()); print('valid')"` prints "valid"
    - `grep -c 'tokens.css' brandbook/logo-options/round-3/index.html` returns 1 (linked exactly once)
    - `grep -c '../../tokens.css' brandbook/logo-options/round-3/index.html` returns 1 (exact relative path)
    - `grep -c 'Under review' brandbook/logo-options/round-3/index.html` returns 1 (status pill correct)
    - `grep -c 'favicon-sample' brandbook/logo-options/round-3/index.html` returns ≥ 21 (3 sizes × 7 candidates = 21)
    - `grep -c 'margin-top.*sigra-space' brandbook/logo-options/round-3/index.html` returns 7 (with-subtitle blocks, one per candidate)
    - `grep -c '^\|' brandbook/logo-options/round-3/README.md` returns ≥ 9 (header + separator + 7 data rows)
    - `grep -c 'round-2' brandbook/logo-options/round-3/index.html` returns ≥ 1 (crumb link to round-2 archive present)
    - The gallery opens correctly in a browser (verify by running `node scripts/brand/critique-render.mjs brandbook/logo-options/round-3/index.html /tmp/sigra-renders/gallery-check` and using Read tool on the hero screenshot to confirm all 7 option sections are visible and no layout is broken)
    - FINAL GALLERY SMOKE: Read tool inspection of `/tmp/sigra-renders/gallery-check/` PNG shows a recognizable multi-option gallery layout; no 404 boxes or broken image icons visible
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /tmp/sigra-harness-*.html → Playwright Chromium | Throwaway harness HTML loaded via file:// in headless browser; content is executor-authored, not user-supplied |
| brandbook/logo-options/round-3/*.svg → git commit | SVG files with hand-edited path data; must not contain font binaries or executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-03 | Information Disclosure | SVG path data | accept | SVG path data from OFL fonts is public by definition (OFL allows any use including derivative works); no proprietary path data is committed |
| T-179-04 | Tampering | Harness HTML file:// load | accept | Harness HTML is executor-created and ephemeral in /tmp/; no user-controlled input; Playwright runs headless in local dev context only |
| T-179-SC | Tampering | npm/cargo installs | accept | No new npm packages installed in Plan 02; uses existing opentype.js from Plan 01 and playwright-core from test/example; no additional supply-chain surface |
</threat_model>

<verification>
Run the full machine-verifiable check suite from VALIDATION.md after completing all three tasks:

```bash
# 1. Candidate SVG count (≥22 SVG files)
ls /Users/jon/projects/sigra/brandbook/logo-options/round-3/*.svg | wc -l

# 2. All SVGs parse as valid XML
python3 -c "
import xml.etree.ElementTree as ET, glob
files = glob.glob('/Users/jon/projects/sigra/brandbook/logo-options/round-3/*.svg')
[ET.parse(f) for f in files]
print(f'All {len(files)} SVGs valid')
"

# 3. Gallery HTML parses
python3 -c "from html.parser import HTMLParser; p=HTMLParser(); p.feed(open('/Users/jon/projects/sigra/brandbook/logo-options/round-3/index.html').read()); print('Gallery HTML valid')"

# 4. tokens.css linked exactly once
grep -c 'tokens.css' /Users/jon/projects/sigra/brandbook/logo-options/round-3/index.html

# 5. README table rows (≥9)
grep -c '^\|' /Users/jon/projects/sigra/brandbook/logo-options/round-3/README.md

# 6. No font binaries in git
git -C /Users/jon/projects/sigra ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'

# 7. brandbook/README.md has font provenance (from Plan 01)
grep -c 'OFL\|outlined\|opentype' /Users/jon/projects/sigra/brandbook/README.md

# 8. Critique notes exist for all 7 candidates
grep -c '^## ' /tmp/sigra-critique-notes.md
```
</verification>

<success_criteria>
- 7 candidates authored (A1, A2, A3, A4, B1, B2, C1) with ≥3 SVG files each (primary light, primary dark, favicon); B1 has 4 files (adds stand-alone mark SVG)
- Every candidate SVG includes provenance `<desc>` with Font: line and opentype.js version
- Every candidate passed the render-critique loop: PNG screenshots read via Read tool at 16/32/54/hero × light/dark, critique notes written to /tmp/sigra-critique-notes.md, "approved for gallery" decision recorded per candidate
- The SUMMARY.md critique summary section quotes the final pass decision for each of the 7 candidates
- `brandbook/logo-options/round-3/index.html` is valid HTML, links `../../tokens.css` exactly once, shows a with-subtitle preview block and favicon row for every candidate, uses "Under review" status pill
- `brandbook/logo-options/round-3/README.md` has the Files table with ≥7 data rows
- `git ls-files -- '*.ttf' '*.otf' '*.woff' '*.woff2'` returns empty
- Gallery is ready for Phase 180 human ratification gate
</success_criteria>

<output>
Create `.planning/phases/179-outlining-toolchain-logo-concept-exploration/179-02-SUMMARY.md` when done.
The SUMMARY.md MUST include a "Critique Summary" section containing:
- One entry per candidate (A1–A4, B1, B2, C1)
- At minimum: final-iteration scale observations at 16px, 54px, and hero; decision ("approved for gallery")
- Note any candidates that required more than 1 iteration and what changed
</output>
