---
phase: 149-launch-evidence-and-announcement-pack
plan: 02
type: execute
wave: 2
depends_on:
  - 149-01
files_modified:
  - README.md
  - CHANGELOG.md
  - mix.exs
  - docs/NEXT-STEPS-MANUAL.md
  - doc/llms.txt
  - llms.txt
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-03
  - LAUNCH-04
must_haves:
  truths:
    - "Public entry surfaces converge on the canonical launch pack instead of inventing separate release narratives."
    - "HexDocs publishes the launch docs and AI-facing indexes route to the same canonical 1.0 paths."
    - "The GitHub Release drafting step points maintainers at the launch pack instead of a divergent hand-written summary."
  artifacts:
    - path: "mix.exs"
      provides: "ExDoc publication of launch docs"
      contains: "docs/launch/v1.0/announcement.md"
    - path: "doc/llms.txt"
      provides: "canonical AI-consumption map including launch docs"
      contains: "announcement.html"
    - path: "llms.txt"
      provides: "root discoverability pointer without a second taxonomy"
      contains: "doc/llms.txt"
  key_links:
    - from: "README.md"
      to: "docs/launch/v1.0/announcement.md"
      via: "top-level launch/evidence routing"
      pattern: "docs/launch/v1\\.0/announcement\\.md"
    - from: "docs/NEXT-STEPS-MANUAL.md"
      to: "docs/launch/v1.0/announcement.md"
      via: "GitHub Release body drafting guidance"
      pattern: "docs/launch/v1\\.0/announcement\\.md"
    - from: "llms.txt"
      to: "doc/llms.txt"
      via: "pointer-only AI routing"
      pattern: "doc/llms\\.txt"
---

<objective>
Publish and route the launch pack through every top-level surface that adopters, maintainers, and AI tools actually use.

Purpose: implement D-01, D-16, D-18, D-19, D-20, and D-21 so README, changelog guidance, ExDoc, the GitHub Release drafting step, and AI indexes all converge on one canonical launch pack.
Output: updated routing in `README.md`, `CHANGELOG.md`, `mix.exs`, `docs/NEXT-STEPS-MANUAL.md`, `doc/llms.txt`, and a pointer-only root `llms.txt`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/149-launch-evidence-and-announcement-pack/149-CONTEXT.md
@.planning/phases/149-launch-evidence-and-announcement-pack/149-RESEARCH.md
@.planning/phases/149-launch-evidence-and-announcement-pack/149-PATTERNS.md
@.planning/phases/149-launch-evidence-and-announcement-pack/149-VALIDATION.md
@docs/launch/v1.0/announcement.md
@docs/launch/v1.0/alternatives.md
@docs/launch/v1.0/evidence.md
@README.md
@CHANGELOG.md
@mix.exs
@docs/NEXT-STEPS-MANUAL.md
@doc/llms.txt

<interfaces>
From `mix.exs`:
- Launch docs must be added to `docs.extras`; the existing `Docs: ~r{^docs/|^SECURITY\.md$}` group should keep owning them.

From `docs/NEXT-STEPS-MANUAL.md`:
- `## 3. GitHub Release` is the concrete maintainer step that currently tells maintainers what to paste into a hand-cut release body.

From `doc/llms.txt`:
- The file is the AI-facing table of contents; keep one canonical vocabulary and route it toward current 1.0 docs first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the launch pack through README, changelog guidance, ExDoc, and the GitHub Release drafting step</name>
  <files>README.md, CHANGELOG.md, mix.exs, docs/NEXT-STEPS-MANUAL.md</files>
  <read_first>
README.md
CHANGELOG.md
mix.exs
docs/NEXT-STEPS-MANUAL.md
.planning/phases/149-launch-evidence-and-announcement-pack/149-CONTEXT.md
.planning/phases/149-launch-evidence-and-announcement-pack/149-RESEARCH.md
.planning/phases/149-launch-evidence-and-announcement-pack/149-PATTERNS.md
docs/launch/v1.0/announcement.md
docs/launch/v1.0/alternatives.md
docs/launch/v1.0/evidence.md
docs/release-runbook-v1-0.md
  </read_first>
  <action>Update `mix.exs`, `README.md`, `CHANGELOG.md`, and `docs/NEXT-STEPS-MANUAL.md` per D-01, D-16, and D-18 so the new launch pack is the only canonical public 1.0 story. In `mix.exs`, add all three `docs/launch/v1.0/*.md` files to `docs.extras` and preserve the existing `Docs` grouping instead of inventing a new ExDoc vocabulary. In `README.md`, extend the existing launch or evidence routing so announcement, alternatives, and evidence are visible from the top-level map. In `CHANGELOG.md`, add one version-clear `Unreleased` documentation bullet set that routes readers to the launch pack rather than duplicating who-should-upgrade guidance. In `docs/NEXT-STEPS-MANUAL.md`, change `## 3. GitHub Release` so the canonical GitHub Release body source is `docs/launch/v1.0/announcement.md`, with `docs/launch/v1.0/evidence.md` and the matching changelog section as supporting links; do not leave “paste the changelog section” as the only guidance.</action>
  <acceptance_criteria>
- `mix.exs` contains `docs/launch/v1.0/announcement.md`, `docs/launch/v1.0/alternatives.md`, and `docs/launch/v1.0/evidence.md` in `docs.extras`, and `groups_for_extras` still contains `Docs: ~r{^docs/|^SECURITY\\.md$}`.
- `README.md` contains at least one top-level link each to `docs/launch/v1.0/announcement.md` and `docs/launch/v1.0/evidence.md`.
- `CHANGELOG.md` `## [Unreleased]` routes to `docs/launch/v1.0/announcement.md` and keeps the new copy Hex-version-clear rather than using `v1.32` as the public label.
- `docs/NEXT-STEPS-MANUAL.md` `## 3. GitHub Release` references `docs/launch/v1.0/announcement.md` and `docs/launch/v1.0/evidence.md`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -n "docs/launch/v1\\.0/announcement\\.md|docs/launch/v1\\.0/alternatives\\.md|docs/launch/v1\\.0/evidence\\.md|Docs: ~r\\{\\^docs/\\|\\^SECURITY\\\\\\.md\\$\\}" mix.exs; rg -n "docs/launch/v1\\.0/announcement\\.md|docs/launch/v1\\.0/evidence\\.md" README.md CHANGELOG.md docs/NEXT-STEPS-MANUAL.md; ! rg -n "v1\\.32" README.md CHANGELOG.md docs/NEXT-STEPS-MANUAL.md; mix docs --warnings-as-errors'</automated>
  </verify>
  <done>Every human-facing router in this plan scope now points at the same launch pack, and HexDocs publishes the launch docs.</done>
</task>

<task type="auto">
  <name>Task 2: Curate the AI indexes around the canonical 1.0 paths and add a pointer-only root `llms.txt`</name>
  <files>doc/llms.txt, llms.txt</files>
  <read_first>
doc/llms.txt
llms.txt
.planning/phases/149-launch-evidence-and-announcement-pack/149-CONTEXT.md
.planning/phases/149-launch-evidence-and-announcement-pack/149-RESEARCH.md
.planning/phases/149-launch-evidence-and-announcement-pack/149-PATTERNS.md
docs/launch/v1.0/announcement.md
docs/launch/v1.0/alternatives.md
docs/launch/v1.0/evidence.md
guides/introduction/installation.md
guides/introduction/contract.md
guides/introduction/upgrading-to-v1.0.md
guides/introduction/migrating-from-phx-gen-auth.md
guides/introduction/migrating-from-pow-guardian-ueberauth.md
guides/introduction/demo-showcase.md
SECURITY.md
CHANGELOG.md
  </read_first>
  <action>Update `doc/llms.txt` per D-19 and D-21 so the generated ExDoc AI-consumption map routes first to installation, the 1.0 contract, migration lanes, demo showcase, launch announcement, alternatives, evidence, changelog guidance, and security/non-goals. Keep one canonical vocabulary; do not create separate maintainer versus adopter taxonomies. Then add a root `llms.txt` per D-20 as a pointer-only discoverability file that links to `doc/llms.txt` and the published HexDocs `llms.txt` path, without recreating a second `## Pages` tree or adding launch-policy prose that can drift. Because `doc/llms.txt` lives under the ignored `doc/` tree, note in the summary that execution must stage it with force if commits are created.</action>
  <acceptance_criteria>
- `doc/llms.txt` contains entries for `installation`, `contract`, `upgrading-to-v1-0`, `migrating-from-phx-gen-auth`, `migrating-from-pow-guardian-ueberauth`, `demo-showcase`, `announcement`, `alternatives`, `evidence`, `changelog`, and `security`.
- `llms.txt` exists at the repo root and contains `doc/llms.txt` and/or `https://hexdocs.pm/sigra/llms.txt`.
- `llms.txt` does not contain `## Pages` and does not duplicate the full table-of-contents taxonomy from `doc/llms.txt`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test -f llms.txt; rg -n "installation|contract|upgrading-to-v1-0|migrating-from-phx-gen-auth|migrating-from-pow-guardian-ueberauth|demo-showcase|announcement|alternatives|evidence|changelog|security" doc/llms.txt; rg -n "doc/llms\\.txt|https://hexdocs\\.pm/sigra/llms\\.txt" llms.txt; ! rg -n "^## Pages$" llms.txt'</automated>
  </verify>
  <done>AI-facing discovery points to the same canonical 1.0 docs, and the repo root gains a minimal pointer without introducing a second routing vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README / CHANGELOG / GitHub Release guidance → public launch readers | Divergent entry points can create conflicting launch claims or routing. |
| `doc/llms.txt` / `llms.txt` → AI consumers | Multiple vocabularies or stale ordering can steer generated guidance toward obsolete paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-04 | Tampering | `README.md`, `CHANGELOG.md`, `mix.exs`, `docs/NEXT-STEPS-MANUAL.md` | mitigate | Route every edited surface to the same `docs/launch/v1.0/` files per D-01 and D-16 instead of creating second release narratives. |
| T-149-05 | Spoofing | launch routing copy | mitigate | Keep new public routing keyed to Hex `1.0.0` per D-18 and avoid introducing internal milestone labels as the public version story. |
| T-149-06 | Information Disclosure | `doc/llms.txt`, `llms.txt` | mitigate | Per D-19 through D-21, keep `doc/llms.txt` as the only full taxonomy and make root `llms.txt` pointer-only so AI consumers cannot drift onto a second policy vocabulary. |
</threat_model>

<verification>
- `mix docs --warnings-as-errors`
- `rg -n "docs/launch/v1\\.0/announcement\\.md|docs/launch/v1\\.0/alternatives\\.md|docs/launch/v1\\.0/evidence\\.md" README.md CHANGELOG.md mix.exs docs/NEXT-STEPS-MANUAL.md`
- `rg -n "announcement|alternatives|evidence|security|changelog" doc/llms.txt llms.txt`
</verification>

<success_criteria>
- HexDocs publishes the launch pack through `mix.exs` extras without creating a new docs taxonomy.
- README, changelog guidance, and the GitHub Release drafting step all point at the same canonical announcement and evidence docs.
- AI-consumption discovery routes through one canonical index plus a minimal root pointer.
</success_criteria>

<output>
Create `.planning/phases/149-launch-evidence-and-announcement-pack/149-02-SUMMARY.md` when done
</output>
