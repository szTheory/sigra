---
phase: 69
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/sigra.install.ex
  - guides/reference/generator-options.md
  - guides/introduction/intermediate-production-path.md
  - mix.exs
  - guides/introduction/installation.md
  - guides/introduction/first-hour.md
  - guides/introduction/getting-started.md
autonomous: true
requirements:
  - ACF-02
  - ACF-03
must_haves:
  truths:
    - "**`guides/introduction/intermediate-production-path.md`** ships a **numbered** narrative path (install → confirm → session login → **MFA/TOTP exemplar** + short **password-change / session revocation** sidebar) that names the **v1.10 default bundle** in prose, links **`.planning/v1.10-ADOPTER-SCOPE.md`** (visible path string) **and** a resolvable **GitHub `blob/main`** URL to the same file, links **deployment** checklist anchor only (no duplicated prod tables), and satisfies **069-CONTEXT** D-01–D-04, D-16–D-18."
    - "**`guides/reference/generator-options.md`** is the **canonical** flag matrix (table **Flag | Default | One-line effect | See also**) covering every entry in **`@switches`** / **`@default_opts`**, plus footer: if this page disagrees with **`mix help sigra.install`**, treat help as authoritative."
    - "**`mix.exs`** lists both new paths in **`extras`** and introduces a **`Reference`** (or **`CLI & generators`**) entry in **`groups_for_extras`** for **`guides/reference/`**, ordered **after Introduction** and **before Flows** per D-12."
    - "**`getting-started.md`** and **`first-hour.md`** each contain **at least one** markdown link to **`generator-options.html`**; both link **`intermediate-production-path.html`** per D-14–D-15."
    - "**`lib/mix/tasks/sigra.install.ex`** `@moduledoc` **Options** lists **`--organizations` / `--no-organizations`** with correct default semantics matching **`@default_opts`**."
  artifacts:
    - path: guides/introduction/intermediate-production-path.md
      provides: ACF-02 intermediate narrative
    - path: guides/reference/generator-options.md
      provides: ACF-03 optional-feature index
nyquist_compliant: true
---

<objective>
Ship **ACF-02** and **ACF-03**: maintainer-quality **HexDocs** — (1) **`intermediate-production-path`** narrative + cross-links, (2) **`generator-options`** canonical index + **`Mix.Tasks.Sigra.Install`** `@moduledoc` alignment, (3) **`mix.exs`** ExDoc registration and intro-guide cross-links — per **069-CONTEXT.md** and **069-RESEARCH.md**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-69-01 | Misrepresentation | Intermediate path read as **compliance** or guaranteed secure production | medium | Prose: pre-flight / confidence wording only; no *certified*, *SOC*, *guarantee*; checklist remains in **deployment** only |
| T-69-02 | Misconfiguration | Wrong or missing **CLI flag** docs lead to accidental **`--no-passkeys`** / org stripping | medium | Matrix defaults copied from **`@default_opts`**; footer defers to **`mix help sigra.install`**; sync `@moduledoc` with `@switches` |
| T-69-03 | Information disclosure | Implies secrets in docs | low | No real tokens; use **`alice@example.com`**-style examples only |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Align Mix.Tasks.Sigra.Install @moduledoc with @switches</name>
  <files>lib/mix/tasks/sigra.install.ex</files>
  <read_first>
    - lib/mix/tasks/sigra.install.ex
    - .planning/phases/069-intermediate-path-optional-features/069-CONTEXT.md
    - .planning/phases/069-intermediate-path-optional-features/069-RESEARCH.md
  </read_first>
  <action>
    In **`lib/mix/tasks/sigra.install.ex`**, inside the **`## Options`** section of **`@moduledoc`** (the bulleted list under **Options**), add **one** new bullet **after** the `--live` / `--no-live` bullet and **before** `--binary-id`, with **exact** phrasing:

    * `--organizations` / `--no-organizations` — Generate organizations scaffolding (default: true)

    Ensure no duplicate contradictory bullets exist for the same switch. Do **not** change **`@switches`**, **`@default_opts`**, or **`run/1`** logic — documentation only unless a typo fix is unavoidable.
  </action>
  <acceptance_criteria>
    - `grep -nF 'organizations' lib/mix/tasks/sigra.install.ex | head -5` exits **0** and at least one matching line is inside the **`@moduledoc`** block (before the closing `"""` of that moduledoc — verify by reading the file: the new bullet must appear in the **Options** list, not only in `@switches`).
    - `grep -F 'no-organizations' lib/mix/tasks/sigra.install.ex` exits **0**.
    - `mix run -e ':ok'` exits **0** (sanity compile).
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.install_golden` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Add guides/reference/generator-options.md</name>
  <files>guides/reference/generator-options.md</files>
  <read_first>
    - lib/mix/tasks/sigra.install.ex
    - guides/introduction/installation.md
    - .planning/phases/069-intermediate-path-optional-features/069-CONTEXT.md
  </read_first>
  <action>
    Create **`guides/reference/generator-options.md`** with:

    1. **H1** title: **`# Generator and install options`** (kebab file name already fixed).

    2. Opening paragraph: this page is the **canonical human index** for **`mix sigra.install`** switches; exhaustive machine truth is **`mix help sigra.install`** and **`Mix.Tasks.Sigra.Install`** on HexDocs.

    3. A **markdown table** with header row **exactly**: **`| Flag | Default | One-line effect | See also |`**. Data rows must cover **every** switch in **`@switches`** with defaults taken from **`@default_opts`** (use **true/false** or **string** as appropriate; `--table` is string override). **See also** cells must contain **relative** links to existing guides where applicable (e.g. **`../flows/api-authentication.html`**, **`../recipes/deployment.html`**, **`../flows/mfa.html`**, **`../recipes/passkeys.html`**, **`../recipes/multi-tenant.html`** — adjust relative paths from **`guides/reference/`**).

    4. Short prose **subsections** (level-2 headings) clustering: **LiveView**, **Binary IDs**, **API and JWT**, **Organizations**, **Passkeys**, **Admin**, **Non-interactive CI** — each **2–4** sentences + links to flow/recipe docs.

    5. Final **paragraph** (footer contract) containing **exact** sentence fragment: **`mix help sigra.install`** is authoritative (surround with normal prose so the phrase appears verbatim).

    Do **not** document flags that are not in **`@switches`**.
  </action>
  <acceptance_criteria>
    - `test -f guides/reference/generator-options.md` exits **0**.
    - `grep -F '| Flag |' guides/reference/generator-options.md` exits **0** AND `grep -F '| Default |' guides/reference/generator-options.md` exits **0** AND `grep -F '| One-line effect |' guides/reference/generator-options.md` exits **0** AND `grep -F '| See also |' guides/reference/generator-options.md` exits **0**.
    - `grep -F 'mix help sigra.install' guides/reference/generator-options.md` exits **0**.
    - `grep -E 'organizations|passkeys|binary-id|api|jwt|admin|no-live' guides/reference/generator-options.md` exits **0** (broad coverage smoke).
  </acceptance_criteria>
  <verify>
    `test -f guides/reference/generator-options.md` exits **0** (ExDoc not required until **`mix.exs`** lists the path).
  </verify>
</task>

<task type="execute">
  <name>Task 3: Add guides/introduction/intermediate-production-path.md</name>
  <files>guides/introduction/intermediate-production-path.md</files>
  <read_first>
    - guides/introduction/getting-started.md
    - guides/recipes/deployment.md
    - guides/flows/mfa.md
    - .planning/v1.10-ADOPTER-SCOPE.md
    - .planning/phases/069-intermediate-path-optional-features/069-CONTEXT.md
  </read_first>
  <action>
    Create **`guides/introduction/intermediate-production-path.md`** with:

    1. **H1:** **`# After the first hour: toward solo production`** (or equivalent; must not duplicate **`# First hour with Sigra`** title text).

    2. Intro stating audience: reader finished **first-hour** green loop / **getting-started** spine and wants **ordered** next steps without absorbing full **deployment** tables here.

    3. **Numbered list** (≥ **6** items) forming the narrative spine per **D-04**: local confidence → pointer to mail semantics (**`../recipes/deployment.html#mail-delivery-inline-vs-oban-tl-dr`** or equivalent stable anchor) → **production checklist** link (**`../recipes/deployment.html#production-checklist-read-first`**) → optional OAuth/MFA depth via **`../flows/oauth.html`** / **`../flows/mfa.html`** — **no** pasted env matrices.

    4. **Subsection** for **assumed install defaults** with hero command line **exactly** `mix sigra.install Accounts User users` as an indented code block (4 spaces), followed by prose **“v1.10 default bundle”** (exact phrase) summarizing LiveView, binary_id, organizations, admin, passkeys **on** — cite **`.planning/v1.10-ADOPTER-SCOPE.md`** as a **visible literal** substring in a sentence **and** include a markdown link whose URL starts with **`https://github.com/`** and ends with **`/blob/main/.planning/v1.10-ADOPTER-SCOPE.md`** (GitHub resolvable).

    5. **Subsection** “Sensitive flow: MFA (TOTP)” — prerequisites (authenticator app), enrollment + **backup codes** emphasis, pointer to **`../flows/mfa.html`**.

    6. **Short subsection** on password change + **session invalidation** semantics (2–4 sentences) + link to account lifecycle or password-reset flow doc.

    7. **Anti-pattern** callout (blockquote or short list): e.g. skipping backup codes, implying synchronous mail in prod without pointing at deployment mail section.

    Forbidden substrings in this file (new prose): **`SOC`**, **`certified`**, **`guarantee`** (case-insensitive).
  </action>
  <acceptance_criteria>
    - `test -f guides/introduction/intermediate-production-path.md` exits **0**.
    - `grep -F '.planning/v1.10-ADOPTER-SCOPE.md' guides/introduction/intermediate-production-path.md` exits **0**.
    - `grep -F 'https://github.com/' guides/introduction/intermediate-production-path.md` exits **0** AND `grep -F '/blob/main/.planning/v1.10-ADOPTER-SCOPE.md' guides/introduction/intermediate-production-path.md` exits **0**.
    - `grep -F 'v1.10 default bundle' guides/introduction/intermediate-production-path.md` exits **0**.
    - `grep -F 'mix sigra.install Accounts User users' guides/introduction/intermediate-production-path.md` exits **0**.
    - `grep -F '../recipes/deployment.html#production-checklist-read-first' guides/introduction/intermediate-production-path.md` exits **0** OR `grep -F 'deployment.html#production-checklist-read-first' guides/introduction/intermediate-production-path.md` exits **0**.
    - `grep -iE 'soc2|certified|guarantee' guides/introduction/intermediate-production-path.md` exits **1** (forbidden warranty words absent).
  </acceptance_criteria>
  <verify>
    `test -f guides/introduction/intermediate-production-path.md` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 4: Register extras and Reference group in mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - .planning/phases/069-intermediate-path-optional-features/069-CONTEXT.md
  </read_first>
  <action>
    In **`mix.exs`** function **`docs/0`**:

    1. Append to the **`extras:`** list (preserve existing order; add **after** `guides/introduction/first-hour.md` or in a sensible position) **two** new string entries:
       - **`"guides/introduction/intermediate-production-path.md"`**
       - **`"guides/reference/generator-options.md"`**

    2. In **`groups_for_extras`**, insert a **new** tuple **`{Reference, ~r{guides/reference/.?}}`** immediately **after** the **`Introduction:`** tuple and **before** the **`Flows:`** tuple. If Elixir version in repo disallows adjacent duplicate keys, keep Keyword list valid.

    Do **not** change **`main:`**, **`source_ref:`**, or **`source_url:`**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'guides/introduction/intermediate-production-path.md' mix.exs` exits **0**.
    - `grep -nF 'guides/reference/generator-options.md' mix.exs` exits **0**.
    - `grep -nF 'Reference:' mix.exs` exits **0** AND `grep -F 'guides/reference' mix.exs` exits **0**.
    - Line number of **`Reference:`** is **greater than** line number of **`Introduction:`** in `grep -n` output AND **less than** line number of **`Flows:`** (executor verifies with `grep -n`).
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 5: Thin installation.md bridge to generator-options</name>
  <files>guides/introduction/installation.md</files>
  <read_first>
    - guides/introduction/installation.md
    - guides/reference/generator-options.md
    - .planning/phases/069-intermediate-path-optional-features/069-CONTEXT.md
  </read_first>
  <action>
    In **`guides/introduction/installation.md`**:

    1. **Immediately before** the existing **`### mix sigra.install flags (reference)`** heading, insert one short paragraph + markdown link to **`generator-options.html`** (relative from **`guides/introduction/`** → **`../reference/generator-options.html`**) stating it is the **canonical full matrix**.

    2. In the existing flag table, add a row for **`--organizations` / `--no-organizations`** consistent with **`@default_opts`**.

    3. Add a single line in the table caption area or immediately below the table: label it as a **subset** / quick cheat pointing readers to **`../reference/generator-options.html`** for the complete list (exact relative link as above).
  </action>
  <acceptance_criteria>
    - `grep -F '../reference/generator-options.html' guides/introduction/installation.md` exits **0**.
    - `grep -iF 'organizations' guides/introduction/installation.md` exits **0**.
    - `grep -F '### mix sigra.install flags (reference)' guides/introduction/installation.md` exits **0** (heading preserved).
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 6: Extend first-hour.md reading map and handoffs</name>
  <files>guides/introduction/first-hour.md</files>
  <read_first>
    - guides/introduction/first-hour.md
    - .planning/phases/069-intermediate-path-optional-features/069-CONTEXT.md
  </read_first>
  <action>
    In **`guides/introduction/first-hour.md`**:

    1. Under **`## Reading map (shortest path)`**, append **two** new numbered items at the end of the ordered list (renumber if needed so the list stays ascending **1..N**): one link to **`intermediate-production-path.html`**; one link to **`../reference/generator-options.html`** (or correct relative path from intro).

    2. After **`## Checklist`** section (after the checklist bullets, before **`## Optional features`**) add a short **paragraph** (1–2 sentences) + link to **`intermediate-production-path.html`** as post-checklist handoff — must not duplicate the entire reading map.
  </action>
  <acceptance_criteria>
    - `grep -F 'intermediate-production-path.html' guides/introduction/first-hour.md` exits **0** AND `grep -c 'intermediate-production-path.html' guides/introduction/first-hour.md` returns count **≥ 2** (two distinct placements: reading map + handoff).
    - `grep -F 'generator-options.html' guides/introduction/first-hour.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 7: Extend getting-started.md What's next</name>
  <files>guides/introduction/getting-started.md</files>
  <read_first>
    - guides/introduction/getting-started.md
    - .planning/phases/069-intermediate-path-optional-features/069-CONTEXT.md
  </read_first>
  <action>
    In **`guides/introduction/getting-started.md`**:

    1. In **`## What's next`**, add **two** new bullet lines (markdown **`-`** list) with links: **`../reference/generator-options.html`** and **`intermediate-production-path.html`** (correct relative paths from **`guides/introduction/`**).

    2. Preserve existing bullets; do not remove **deployment** links in **`## Before you ship to production`**.
  </action>
  <acceptance_criteria>
    - `grep -F 'generator-options.html' guides/introduction/getting-started.md` exits **0**.
    - `grep -F 'intermediate-production-path.html' guides/introduction/getting-started.md` exits **0**.
    - `grep -F '## Before you ship to production' guides/introduction/getting-started.md` exits **0** AND `grep -F 'Production checklist' guides/introduction/getting-started.md` exits **0** (phase 68 closure preserved).
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.install_golden` exits **0**.
- `grep -RIF 'ACF-02' .planning/phases/069-intermediate-path-optional-features/069-01-PLAN.md` is unnecessary; instead confirm **`requirements:`** frontmatter lists **ACF-02** and **ACF-03** (spot-read plan frontmatter).
</verification>

<success_criteria>
- **ACF-02:** Intermediate dogfood narrative doc exists, links scope + deployment anchors, MFA exemplar + password-change sidebar.
- **ACF-03:** Canonical **`generator-options`** index + cross-links from **getting-started** and **first-hour** + **`@moduledoc`** organizations line.
</success_criteria>
