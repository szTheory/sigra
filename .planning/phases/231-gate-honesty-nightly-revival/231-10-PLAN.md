---
phase: 231-gate-honesty-nightly-revival
plan: 10
type: execute
wave: 9
depends_on: ["231-09"]
files_modified:
  - .github/workflows/playwright-github-pages.yml
  - .github/workflows/ci-observe.yml
  - MAINTAINING.md
  - scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs
  - scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs
  - .planning/todos/pending/2026-07-27-playwright-github-pages-publisher-red.md
  - .planning/todos/pending/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "D-17 (corrects the filed todo): the scheduled `Playwright reports (GitHub Pages)` red is NOT spec drift. `playwright-github-pages.yml` runs its dev-DB setup and then boots the example app with no demo-seeds step, while all four example-booting jobs in `ci.yml` run one (`:1288`, `:1950`, `:2258`, `:2506`), seeding roughly thirty loadtest users. Without them the admin users index never paginates and the checkpoint spec's next-page link never renders — which is exactly how scheduled run `30432494488` fails, in all three checkpoint projects at `admin-checkpoints.spec.ts:230`. The remedy is the seeds step, and the todo's spec-drift hypothesis is corrected on the record rather than quietly diverged from."
    - "Pattern-map finding 7 (a correction the plan must honor): the seeds step is copied from the `admin_eval_render` block at `ci.yml:2506-2513`, NOT from `:1288`, `:1950` or `:2258`. Those three carry a docs-only guard, and `playwright-github-pages.yml` has no `changes` job, so that guard would evaluate empty and silently skip the very step being added. Only the `admin_eval_render` copy is unguarded."
    - "D-19: the schedule-lane leniency in `ci-observe.yml` is DELETED, so the demotion receipt fails on the scheduled lane exactly as it does on the push lane. The code carried its own removal condition naming this phase and this requirement, and `MAINTAINING.md:255-262` records the same commitment. Leaving it would mean a demoted construct can silently stop executing on the nightly while the receipt merely warns — a new silent-rot channel opened by the phase meant to close them."
    - "Anchor drift corrected (RESEARCH `:184`): the deletion span is `ci-observe.yml:123-136`, not CONTEXT's `:130-136`. Deleting only `:130-136` orphans the rationale comment at `:123-129`, leaving prose that explains a branch which no longer exists. The unconditional failure line at `:137` stays and becomes the sole terminal branch."
    - "D-18 scope fence: GitHub Pages is currently building `main`'s repo root rather than the `gh-pages` branch the publisher targets, because `ensure-github-pages-legacy-branch.sh` runs only after a successful publish and the publisher has been red daily. This plan is in scope only insofar as D-17's fix lets that script finally run. If the source does not self-heal, it is FILED as a diagnosed defect with an owner under SC-1's fallback branch — the phase does NOT expand into a repo-admin Pages reconfiguration."
    - "Both structural fixes gain a prohibition guard, so neither can silently regress: one asserting the publisher seeds before it boots, one asserting no schedule-lane leniency exists in the observer. Each carries its own non-vacuity floor, so an extractor that matches nothing fails loudly instead of passing on an empty set."
    - "The publisher's `workflow_dispatch` trigger makes D-17's fix observable IN PHASE rather than only after the next 06:45 cron, and that same dispatch is what answers D-18's self-heal question."
  prohibitions:
    - statement: "MUST NOT answer a red publisher run by adding `continue-on-error`, a retry, or a narrowed assertion to the checkpoint spec. The filed todo floated the first of these explicitly; it is both unnecessary and forbidden."
      reason: "D-17 says so by name, 230's D-15 posture forbids it generally, and REQUIREMENTS.md \"Out of Scope\" lists masking flake with retries or continue-on-error. Descriptor-less per the spec-less probe fallback: no wired check, disposes flagged-unverified."
    - statement: "MUST NOT restore, re-add, or reintroduce by another route any schedule-lane exemption in the demotion receipt. A receipt that warns on one lane and fails on another is a gate whose consequence depends on the trigger, which is the rot channel D-19 exists to close."
      reason: "D-19 and the removal condition the code itself carries. Descriptor-less per the spec-less probe fallback."
    - statement: "MUST NOT expand into changing the repository's GitHub Pages source setting, tokens, or Settings-level configuration if the self-heal does not fire. File it; do not fix it."
      reason: "D-18's explicit scope fence and CONTEXT's Deferred Ideas list, which places a Pages source reconfiguration in the same human-gated class as the Hex retire. Descriptor-less per the spec-less probe fallback."
  artifacts:
    - path: ".github/workflows/playwright-github-pages.yml"
      provides: "a demo-seeds step between dev-DB setup and app boot, so the admin users index paginates and the checkpoint specs can pass"
      contains: "Run demo seeds"
    - path: ".github/workflows/ci-observe.yml"
      provides: "a demotion receipt whose consequence no longer depends on the trigger"
      contains: "demotion-observation.json"
    - path: "scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs"
      provides: "structural guard that the publisher seeds before it boots, with a non-vacuity floor"
      contains: "the parse broke"
    - path: "scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs"
      provides: "structural guard that the observer has no trigger-dependent early exit, with a non-vacuity floor"
      contains: "the parse broke"
    - path: "MAINTAINING.md"
      provides: "residual 4 reconciled — the leniency is recorded as removed, with the evidence that justified removing it"
      contains: "ci-observe.yml"
  key_links:
    - from: ".github/workflows/playwright-github-pages.yml"
      to: "test/example/priv/repo/seeds.exs"
      via: "a seeds step placed after the dev-DB setup and before the Playwright dependency install, copied from the unguarded ci.yml admin_eval_render block"
      pattern: "priv/repo/seeds\\.exs"
    - from: "scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs"
      to: ".github/workflows/playwright-github-pages.yml"
      via: "step-order assertion over the parsed publish job's step list"
      pattern: "playwright-github-pages\\.yml"
    - from: "scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs"
      to: ".github/workflows/ci-observe.yml"
      via: "comment-stripped assertion that no trigger-dependent early exit remains in the receipt's verdict shell"
      pattern: "ci-observe\\.yml"
---

<objective>
GATE-01's structural half — the fifth and last link in D-24's locked chain. It comes last because its
reds ARE the other requirements' defects: measured on nightly run `30425416933`, 23 of 25 jobs were
green and the only two failures were GATE-02's 320px assertion and GATE-04's two bugs, both fixed by
plans 231-02 through 231-07. GATE-01 is largely an observation of the others.

Two things are genuinely its own, and both are corrections to the written record.

**D-17.** The separate `Playwright reports (GitHub Pages)` workflow has been red daily, and the filed
todo guessed "real spec drift" and floated `continue-on-error` as a remedy. Both were wrong. The
workflow sets up the dev DB and boots the app with no demo-seeds step, while every example-booting job
in `ci.yml` runs one. Without ~30 seeded loadtest users the admin users index does not paginate, so
the checkpoint spec's next-page link never renders — which is precisely how scheduled run
`30432494488` fails, in all three checkpoint projects. The fix is one step. The pattern map adds a
correction the diagnosis alone would have missed: three of the four copyable seeds blocks carry a
docs-only guard, and this workflow has no `changes` job, so copying one of those would add a step that
silently never runs. Only the `admin_eval_render` copy is unguarded.

**D-19.** `ci-observe.yml` carries a schedule-lane leniency whose own comment names this phase and this
requirement as its removal condition, and `MAINTAINING.md` records the same commitment. It exists
because a tenth red on a 0-pass/9-fail nightly was unreadable. That premise is gone. Leaving it would
mean a demoted construct can stop executing on the nightly while the receipt merely warns — a fresh
silent-rot channel opened by the phase meant to close them. RESEARCH corrects the span: the removal is
`:123-136`, not `:130-136`, because the narrower cut orphans the rationale comment.

Both fixes get a prohibition guard, because a structural fix with no guard is a fix that regresses
the next time someone edits the file for an unrelated reason.

Purpose: the nightly's two non-CI reds are fixed, and the receipt that watches the nightly stops
grading it on a curve.
Output: one seeds step, one deleted branch, two prohibition guards, one reconciled runbook passage,
one corrected-and-resolved todo, and — only if the self-heal does not fire — one filed defect.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/231-gate-honesty-nightly-revival/231-CONTEXT.md
@.planning/phases/231-gate-honesty-nightly-revival/231-VALIDATION.md
@.planning/todos/pending/2026-07-27-playwright-github-pages-publisher-red.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed the Pages publisher before it boots, and guard it (D-17)</name>
  <behavior>
    - The publish job's step list contains a demo-seeds step.
    - That step appears strictly after the dev-DB setup step and strictly before the app-boot step.
    - The seeds step carries no condition at all — an added step that never runs is worse than no step, because it looks like coverage.
    - The guard's own extraction is non-vacuous: if the workflow parse yields zero steps, it fails loudly rather than passing on an empty list.
    - Negative control: a fixture workflow whose publish job omits the seeds step fails the guard, and one whose seeds step sits after the boot step fails it too.
  </behavior>
  <files>.github/workflows/playwright-github-pages.yml, scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs, .planning/todos/pending/2026-07-27-playwright-github-pages-publisher-red.md</files>
  <read_first>
    - `.github/workflows/playwright-github-pages.yml` lines 76-103 — the exact insertion neighbourhood: `Compile example` `:76-80`, `Setup example dev DB` `:81-88` ending in the ecto create-and-migrate line at `:88`, `Install Playwright deps` `:89-91`, `Install Playwright browsers` `:92-94`, and `Boot example app in background` `:95-103`. The insertion point is after `:88` and before `:89`.
    - `.github/workflows/playwright-github-pages.yml` lines 1-40 — the header comment describing the publish-then-ensure-Pages-source flow, the triggers at `:14-25` (note `workflow_dispatch` at `:15`, the `45 6 * * *` cron at `:18`, and the path-filtered push at `:19-25` which includes the Playwright directory), and the job permissions at `:36-38`.
    - `.github/workflows/ci.yml` lines 2498-2519 — the `admin_eval_render` boot prelude. Its seeds step at `:2506-2513` is the ONE copy in the repo that carries no docs-only guard, and it is the block to copy. Read its env keys and working directory exactly.
    - `.github/workflows/ci.yml` lines 1288-1296 — the docs-only-guarded variant, read purely so you can see the difference and NOT copy it. `:1950` and `:2258` are the same shape.
    - `231-PATTERNS.md` lines 678-735 — the analog section for this file: the current gap quoted verbatim, the correct block to copy with its surrounding steps for placement, the contrasting guarded variant, and the note that this workflow boots on the default port so the seeds step copies unchanged.
    - `test/example/priv/playwright/tests/admin-checkpoints.spec.ts` around line 230 — the next-page link assertion that fails without seeded users, so you can see what the seeds step is actually feeding.
    - `scripts/ci/prohibitions/p04-no-release-lane-cancellation.test.mjs` — a shipped example of a prohibition guard whose subject is a workflow file rather than a document, for the `readSubject` / `jobBlock` idiom and the failure-message voice.
    - `scripts/ci/prohibitions/_lib.mjs` — `readSubject`, `readRepoFile`, `jobBlocks`, `jobBlock`, `stripYamlComments`. Use these rather than hand-rolling a parse.
  </read_first>
  <action>
    Write the guard first, watch it fail, then make it pass.

    Create `scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs` following the house
    prohibition shape: a header block stating the prohibition in the imperative (the publisher must not
    boot the example app without first seeding it), the subject file it reads and how to substitute it,
    then the tests. Assert that the publish job's step list can be extracted at all and is non-empty,
    failing with the repo's literal broken-parse phrase when it is not. Assert a demo-seeds step exists.
    Assert its index sits strictly between the dev-DB setup step's index and the app-boot step's index.
    Assert the seeds step declares no condition key, and say in the failure message why a conditional
    seeds step in a workflow with no diff-classification job is worse than none — it evaluates empty,
    never runs, and looks like coverage. Include a negative control in the shape `p10` uses at `:102-110`
    so the assertions are falsifiable rather than merely present.

    Run the guard and confirm it FAILS against the current workflow. Record that observation — a guard
    that has never been seen red is a guard nobody has proven.

    Then edit `.github/workflows/playwright-github-pages.yml`. Insert a step named `Run demo seeds`
    immediately after the dev-DB setup step and before the Playwright dependency install, copying the
    `admin_eval_render` block's working directory, its four environment keys, and its run line exactly.
    Add no condition. Add a short comment above it recording three facts: that all four example-booting
    jobs in `ci.yml` run this step and this workflow did not; that its absence is what made the admin
    users index unable to paginate and the checkpoint specs fail; and that the block was copied from the
    unguarded `admin_eval_render` variant on purpose, because this workflow has no diff-classification
    job for the guarded variants' condition to read.

    Re-run the guard and confirm it now passes.

    Finally, correct the record. Move the publisher-red todo from the pending directory to the resolved
    directory, flip its status field, and append a resolution section stating plainly that its stated
    hypothesis was wrong: this was never spec drift, the specs were correct and the fixture data was
    absent. Name the seeds-prelude diagnosis, cite scheduled run `30432494488` and the three checkpoint
    projects that failed at the same assertion, and record that the `continue-on-error` option the todo
    floated was rejected as both unnecessary and forbidden. Cite the phase and this plan.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && actionlint -shellcheck= .github/workflows/playwright-github-pages.yml && python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/playwright-github-pages.yml'))
steps=d['jobs']['publish']['steps']
names=[s.get('name','') for s in steps]
seed=[i for i,s in enumerate(steps) if 'seeds.exs' in str(s.get('run',''))]
db=[i for i,n in enumerate(names) if n=='Setup example dev DB']
boot=[i for i,n in enumerate(names) if n.startswith('Boot example app')]
assert len(seed)==1, (seed, names)
assert db and boot, names
assert db[0] < seed[0] < boot[0], (db, seed, boot)
assert 'if' not in steps[seed[0]], steps[seed[0]]
assert steps[seed[0]]['name']=='Run demo seeds', steps[seed[0]]['name']
assert steps[seed[0]]['working-directory']=='test/example', steps[seed[0]]
print('OK', names[seed[0]-1], '->', names[seed[0]], '->', names[seed[0]+1])" && test ! -f .planning/todos/pending/2026-07-27-playwright-github-pages-publisher-red.md && test -f .planning/todos/resolved/2026-07-27-playwright-github-pages-publisher-red.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs` exists and is picked up by the shell glob the prohibition suite uses.
    - The guard was observed FAILING against the pre-fix workflow, and that fail-first observation — including the failure message — is recorded verbatim in the plan SUMMARY.
    - `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0 after the fix.
    - Parsing `playwright-github-pages.yml` as YAML: the publish job has exactly one step whose run invokes the seeds script; its name is `Run demo seeds`; its working directory is `test/example`; it declares no `if` key; and its index sits strictly between the `Setup example dev DB` step and the `Boot example app in background` step.
    - That step's `env` keys are identical to the `admin_eval_render` seeds step's env keys at `ci.yml:2506-2513` (compare both parsed step mappings, do not eyeball them).
    - The guard carries a non-vacuity branch that fails with the literal `the parse broke, this is not a pass` when its step extraction returns nothing, and a negative control proving the step-order assertion is falsifiable.
    - `actionlint -shellcheck= .github/workflows/playwright-github-pages.yml` exits 0.
    - `.planning/todos/pending/2026-07-27-playwright-github-pages-publisher-red.md` no longer exists, `.planning/todos/resolved/2026-07-27-playwright-github-pages-publisher-red.md` does, its frontmatter status is no longer pending, and its appended resolution names the seeds-prelude diagnosis, cites run `30432494488`, and states that the spec-drift hypothesis was wrong.
  </acceptance_criteria>
  <done>The Pages publisher seeds the app it boots, a guard prevents the regression, and the todo's wrong diagnosis is corrected on the record rather than quietly superseded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the schedule-lane leniency from the demotion receipt, and guard it (D-19)</name>
  <behavior>
    - The observer's verdict shell has exactly one terminal branch on a failing verdict, and it fails.
    - No trigger-dependent early exit remains anywhere in the receipt's verdict step, measured over comment-stripped content so an explanatory comment cannot satisfy or violate the guard.
    - The guard's extraction is non-vacuous: if the verdict step cannot be located, it fails loudly rather than passing on an empty string.
    - Negative control: a fixture whose verdict shell reintroduces a trigger-dependent early exit fails the guard.
    - The runbook passage that recorded the removal commitment now records the removal.
  </behavior>
  <files>.github/workflows/ci-observe.yml, scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs, MAINTAINING.md</files>
  <read_first>
    - `.github/workflows/ci-observe.yml` lines 110-140 — the receipt's verdict shell. Note the pass branch reading the verdict field and exiting zero, the error-annotation line that prints each failing construct, the rationale comment block at `:123-129`, the removal-condition comment at `:130-132`, the conditional early exit at `:133-136`, and the unconditional failure line at `:137` that stays.
    - `.github/workflows/ci-observe.yml` lines 16-25 — the boundary note naming GATE-03, which is NOT part of the deletion and must survive intact.
    - `231-RESEARCH.md` lines 179-184 — the anchor-drift correction: the removal target is `:123-136`, not CONTEXT's `:130-136`, because the narrower cut orphans the rationale comment.
    - `231-PATTERNS.md` lines 778-794 — the deletion quoted verbatim with the surviving line, and the note that the runbook passage is a same-commit companion edit.
    - `MAINTAINING.md` lines 253-262 — residual 4, whose closing sentence states the leniency is removed when this phase's GATE-01 lands. That sentence is now a statement about the past and must be rewritten as one.
    - `scripts/ci/prohibitions/_lib.mjs` — `readSubject`, `jobBlock`, `stripYamlComments`. The comment-stripping helper is load-bearing here: this workflow documents its own design in prose that necessarily contains the tokens the guard asserts on, exactly as `p06`'s header explains for `ci.yml`.
    - `scripts/ci/prohibitions/p06-never-docs-gate-asserting-lanes.test.mjs` lines 15-35 — the comment-stripping rationale and the parse-locates-its-subject test that precedes every assertion.
    - `scripts/ci/ci-demotion-observer.sh` lines 30-45 — the observer's own contract header, so the guard's failure message can speak in the same voice about what the receipt is for.
  </read_first>
  <action>
    Write the guard first, watch it fail, then make it pass.

    Create `scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs` on the house prohibition
    shape. State the prohibition in the imperative in its header: the demotion receipt must not grade
    one trigger on a curve, because a receipt whose consequence depends on the trigger is a gate that
    rots on the lane nobody reads. Read the observer workflow, strip YAML comments first — and say in a
    header comment why, following `p06`'s precedent: the file documents its own design in prose that
    necessarily contains the tokens being asserted on, so the guard must assert on effective content
    rather than on what the file says about itself.

    Locate the verdict step's shell body and fail with the repo's literal broken-parse phrase if the
    extraction yields nothing. Then assert that the comment-stripped body contains no conditional whose
    predicate is the run's trigger and whose consequence is a zero exit. Write the failure message so it
    names the removal condition the code itself carried, names this phase and requirement, and states
    the consequence of regression: a demoted construct silently stops executing on the nightly and the
    receipt warns instead of failing. Add a negative control against a fixture body that reintroduces
    such a branch, so the assertion is falsifiable.

    Run the guard and confirm it FAILS against the current workflow. Record the observation.

    Then delete the whole block from the start of the rationale comment through the end of the
    conditional, inclusive — the span RESEARCH corrects to `:123-136`. Do not delete only the
    conditional: that leaves prose explaining a branch that no longer exists, which is its own species
    of stale record. The unconditional failure line that follows stays and becomes the sole terminal
    branch, so the schedule lane now fails exactly as the push lane does.

    Re-run the guard and confirm it now passes.

    Then reconcile `MAINTAINING.md` residual 4. Rewrite its closing sentence from a forward commitment
    into a past-tense record: state that the leniency was removed, name the phase and requirement, and
    cite the evidence that justified removing it — the measured nightly whose only reds were this
    phase's own two defects, both now fixed. Keep the rest of the residual intact, including the
    statement that the observer is not in the gate's needs and never runs on pull requests, which is
    still true. Do not touch the five ruleset-required check-name strings at `:104-110`.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && actionlint -shellcheck= .github/workflows/ci-observe.yml && python3 -c "
import re
raw=open('.github/workflows/ci-observe.yml').read()
stripped='\n'.join(l for l in raw.splitlines() if not l.lstrip().startswith('#'))
hits=len(re.findall(r'RUN_EVENT', stripped))
assert hits==0 or 'exit 0' not in stripped.split('RUN_EVENT')[1][:400], 'a trigger-dependent early exit survives in effective content'
assert '::warning::Demotion receipt FAILED' not in stripped, 'the warn-instead-of-fail branch survives'
assert stripped.rstrip().count('exit 1') >= 1, 'the terminal failure branch was lost'
assert 'BOUNDARY WITH PHASE 231' in raw, 'the GATE-03 boundary note must survive'
print('OK')" && python3 -c "
m=open('MAINTAINING.md').read()
assert 'that leniency is removed when Phase 231' not in m, 'residual 4 still states the removal as a future commitment'
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs` exists and is picked up by the prohibition suite's shell glob.
    - The guard was observed FAILING against the pre-deletion workflow, and that fail-first observation including its message is recorded verbatim in the plan SUMMARY.
    - `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0 after the deletion.
    - Over comment-stripped content of `ci-observe.yml`, no conditional keyed on the run trigger exits zero, and the warn-instead-of-fail annotation string is absent.
    - The unconditional failure line survives as the verdict shell's sole terminal branch, and the GATE-03 boundary note at `:16-25` is byte-unchanged.
    - The guard strips YAML comments before asserting, carries a header sentence explaining why, and carries a non-vacuity branch failing with the literal `the parse broke, this is not a pass` when the verdict step cannot be located.
    - `MAINTAINING.md` residual 4 no longer states the removal as a future commitment; it records the removal in the past tense, names the phase and requirement, and cites the measured nightly as its justification.
    - `MAINTAINING.md:104-110`'s five ruleset-required check-name strings are byte-unchanged (`git diff MAINTAINING.md` shows no hunk in that range).
    - `actionlint -shellcheck= .github/workflows/ci-observe.yml` exits 0.
  </acceptance_criteria>
  <done>The demotion receipt fails on the scheduled lane exactly as it does on the push lane, a guard prevents the exemption from returning, and the runbook records the removal rather than promising it.</done>
</task>

<task type="auto">
  <name>Task 3: Observe the publisher on a real dispatched run, and answer D-18's self-heal question</name>
  <precondition>`gh` is on PATH and authenticated against `szTheory/sigra`, and Tasks 1 and 2 are pushed to the phase branch — `workflow_dispatch` resolves the workflow definition from the dispatched ref, so an unpushed seeds step cannot be dispatched.</precondition>
  <files>.planning/todos/pending/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md</files>
  <read_first>
    - `.github/workflows/playwright-github-pages.yml` lines 1-40 — the header describing the publish-then-ensure-Pages-source flow and the `workflow_dispatch` trigger at `:15`. Note the concurrency group at `:27-29`: a dispatched run cancels an in-flight one, so do not dispatch twice concurrently.
    - `scripts/ci/ensure-github-pages-legacy-branch.sh` — the script that sets the Pages source to legacy plus the publish branch, and the fact that it runs only after a successful publish. This is the chicken-and-egg D-18 describes.
    - `231-CONTEXT.md` D-18 — the finding (six consecutive `pages-build-deployment` failures whose log shows Jekyll rendering the repository root) and its scope fence: in scope only insofar as D-17's fix lets that script run; if it does not self-heal, file it, do not fix it.
    - `231-RESEARCH.md` lines 1176-1179 — Open Question 3 and its recommendation to observe the first post-fix run and stop there.
    - `.planning/todos/pending/2026-07-28-gate-ci-green-timeout-too-tight-for-push-to-main.md` — the house todo shape, for the file this task may need to create.
  </read_first>
  <action>
    Dispatch the Pages publisher on the phase branch and read what happened. Record the run id, the
    publish job's conclusion, and — whether it passes or fails — the Playwright summary lines for the
    three checkpoint projects, so D-17's fix is judged on the specs it was supposed to unblock rather
    than on the job's overall colour. Note in the SUMMARY that a dispatched publish force-pushes a
    report to the publish branch exactly as a nightly run does; that is the workflow's designed
    behaviour and not a side effect introduced here.

    If the publish job reds, the prohibition binds: diagnose and fix, or file with an owner. Do not add
    a tolerance. If it reds on something other than the pagination assertion, that is a second defect
    and it gets its own diagnosis rather than being folded into D-17's.

    Then answer D-18. After a successful publish, the ensure-Pages-source script runs for the first
    time in the repository's recent history. Read its step output, then read the repository's current
    Pages configuration and the most recent `pages-build-deployment` run. Record which branch and path
    Pages is now building.

    If the source self-healed, record that verbatim with the run id and stop — no todo is needed, and
    the SUMMARY says so explicitly so a later reader does not go looking for a file that correctly does
    not exist.

    If it did not self-heal, file the defect. Create the todo under the pending directory on the house
    frontmatter shape, with a title stating that Pages builds the repository root rather than the
    publish branch, area `ci`, the workflow and script as its files, a severity judged from the impact,
    and a source line attributing it to this phase. In the body, give the diagnosis rather than the
    symptom: state what the ensure-script attempted, quote the API response or the error it produced,
    and state the two candidate causes — an insufficient token scope, or a Settings-level change that
    no workflow token can make. Name an owner. Record explicitly that D-18 fences this out of the
    phase's scope and that it belongs to the same human-gated class as the deferred Hex retire, so the
    next reader does not mistake it for unowned work that was forgotten.

    Either way, this task changes no workflow, no script, and no runbook.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && actionlint -shellcheck= .github/workflows/playwright-github-pages.yml .github/workflows/ci-observe.yml && gh --version >/dev/null && python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/playwright-github-pages.yml'))
steps=d['jobs']['publish']['steps']
assert any('seeds.exs' in str(s.get('run','')) for s in steps), 'seeds step missing at receipt time'
print('publisher wiring intact; the deliverable is the dispatched-run receipt asserted below')"</automated>
  </verify>
  <acceptance_criteria>
    - A dispatched `Playwright reports (GitHub Pages)` run id is recorded in the plan SUMMARY together with the exact dispatch command and the publish job's conclusion.
    - The SUMMARY quotes that run's Playwright summary lines for all three checkpoint projects, and states whether the assertion that failed on scheduled run `30432494488` now passes.
    - The SUMMARY records the ensure-Pages-source step's output verbatim, plus the branch and path GitHub Pages is building after the run, read from the live configuration rather than inferred.
    - Exactly one of two outcomes is recorded and justified: either the Pages source self-healed, with the run id, and no todo file exists; or it did not, and `.planning/todos/pending/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md` exists carrying a diagnosis, an owner, and an explicit statement that D-18 fences the fix out of this phase.
    - If the todo was created, its frontmatter declares `created`, `status: pending`, `title`, `area`, `files`, `severity`, `source`, and its body distinguishes the two candidate causes rather than asserting one.
    - No workflow, script, manifest or runbook file is modified by this task: `git diff --stat HEAD -- .github scripts MAINTAINING.md` is empty.
    - The SUMMARY states explicitly that a dispatched publish is the workflow's designed behaviour and lists the publish branch it wrote to.
  </acceptance_criteria>
  <done>D-17's fix is judged on a real run rather than on the diff, and D-18 is answered one way or the other — self-healed and recorded, or filed with a diagnosis and an owner inside its scope fence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow trigger -> gate consequence | The demotion receipt currently decides its consequence from the run's trigger; that coupling is the surface being removed. |
| Publisher job -> `gh-pages` branch and Pages configuration | The publish job holds `contents: write` and `pages: write` and force-pushes a branch plus calls the Pages REST API. |
| Seeded demo data -> a published static site | The seeds step creates roughly thirty demo users whose rendered pages are published to a public site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-231-10-01 | Repudiation | `ci-observe.yml` schedule-lane leniency | high | mitigate | The trigger-dependent early exit is deleted so the receipt's consequence no longer depends on which lane observed it, and `p16` asserts over comment-stripped content that no equivalent branch returns. A receipt that warns on the lane nobody reads is a receipt that records nothing. |
| T-231-10-02 | Repudiation | a conditional seeds step | high | mitigate | The seeds step is copied from the one unguarded variant, and both an acceptance criterion and `p15` assert it declares no condition. A guarded copy in a workflow with no diff-classification job evaluates empty, never runs, and presents as coverage — a silent-rot channel created by the fix itself. |
| T-231-10-03 | Information Disclosure | published report site content | medium | accept | The publisher already boots the example app and publishes Playwright reports; seeding adds synthetic demo users to that same already-published surface. The seed data is fixture data committed in the repository, contains no secret, and is identical to what four `ci.yml` jobs already generate. |
| T-231-10-04 | Elevation of Privilege | publisher `contents: write` / `pages: write` | medium | accept | The job's declared permissions are unchanged by this plan; the seeds step needs neither. D-18's fence means no token, scope, or Settings change is attempted even if the self-heal fails. |
| T-231-10-05 | Tampering | `MAINTAINING.md` required-check strings | medium | mitigate | Residual 4 is rewritten while the five ruleset-required check-name strings at `:104-110` are asserted byte-unchanged by an acceptance criterion, so a documentation edit cannot strand merges under ruleset 14941512. |
| T-231-10-06 | Denial of Service | publisher concurrency group | low | accept | The workflow cancels an in-flight publish when a new one starts. Task 3's action states not to dispatch twice concurrently; a cancelled publish loses a report, never repository state, because the branch write is a single terminal force-push. |
| T-231-10-SC | Tampering | package-manager installs | low | accept | This plan installs nothing. `231-RESEARCH.md` § Package Legitimacy Audit records that no dependency manifest is edited anywhere in Phase 231. |
</threat_model>

## Artifacts this phase produces

Phase-wide inventory of every new symbol Phase 231 creates. Rows this plan owns are marked **(this plan)**.

| Kind | Symbol | Owning plan |
|---|---|---|
| Script | `scripts/ci/wait-for-ci-gate.sh` + `.test.sh` | 231-01 |
| Script | `scripts/ci/honest-skip-verdict.sh` + `.test.sh` | 231-08 |
| Prohibition test | `scripts/ci/prohibitions/p14-no-svg-classname-string-ops.test.mjs` | 231-04 |
| Prohibition test | `scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs` | **231-10 (this plan)** |
| Prohibition test | `scripts/ci/prohibitions/p16-no-schedule-lane-leniency.test.mjs` | **231-10 (this plan)** |
| New p10 assertions | `gate`-column parity, and no gate string may reference `head_ref` / a branch / a SHA | 231-07 |
| Inverted prohibition | `p05-admin-eval-red-not-abandoned.test.mjs` | 231-06 |
| `workflow_dispatch` input | `force_rot_probe` on `ci.yml` | 231-09 |
| `fast_checks` step names | `Wait-for-ci-gate self-test`, `Honest-skip verdict self-test` | 231-01, 231-08 |
| `ci-gate` step names | `Checkout (honest-skip manifest)`, `Honest-skip verdict (GATE-03)` | 231-09 |
| `playwright-github-pages.yml` step name | `Run demo seeds` | **231-10 (this plan)** |
| Cache key token | `${{ runner.os }}-playwright-chromium-webkit-1.59.1-v2` | 231-04 |
| Re-based ledgers | `guides/reference/admin-render-sha.json`, `guides/reference/fix-queue.json` | 231-05 |
| Todo file | `2026-07-29-example-unit-smoke-required-but-absent-from-ci-gate-needs.md` | 231-09 |
| Todo file (conditional) | `2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md` | **231-10 (this plan)** |
| Resolved todo | `2026-07-27-playwright-github-pages-publisher-red.md` moved to resolved with a corrected diagnosis | **231-10 (this plan)** |
| Diagnosis / evidence documents | `231-02-DIAGNOSIS.md`, `231-EVIDENCE.md` | 231-02, 231-11 |

## Flagged Planner Assumptions

Spec-less probe fallback is active (no SPEC exists for this phase). Unresolved probe rows are surfaced
here, never dropped.

| # | Probe row | Disposition |
|---|---|---|
| A1 | `GATE-01` / `adjacency` — "when two things are exactly equal or just touch, do they merge, collide, or separate?" | **Unresolved, flagged, and deliberately not forced.** The probe is algorithm-shaped and maps poorly onto a CI-honesty requirement: GATE-01 has no interval, no geometry, and no merge operation. The nearest real analogue would be step-ordering adjacency — whether the seeds step and the boot step can touch or must be separated — and that IS authored, as `p15`'s strict-between assertion in Task 1. Recorded here as the honest partial answer rather than as a manufactured predicate. |
| A2 | `GATE-01` / `ordering` — "when elements compare equal, is output order specified and stable?" | **Unresolved, flagged, and deliberately not forced.** GATE-01 produces no sorted output and has no comparator. The only ordering claim in its scope is workflow step order, which `p15` asserts positionally rather than by comparison. No stable-sort predicate is invented. |
| A3 | `GATE-01` / `empty` — "what is the result for empty, single-element, or null input?" | **Authored — see plan 231-11.** This row has a real analogue, per D-04's posture: an observation that reads zero jobs is a broken read, not a green nightly. It is authored as a truth in plan 231-11, which owns the nightly observation. Both prohibition guards in this plan carry the same posture at their own scale, with a non-vacuity floor failing on an empty extraction. |
| A4 | Adding seeds makes the checkpoint specs pass. | RESEARCH rates the mechanism HIGH — the assertion needs paginated data and the data is absent — but the outcome is unproven until Task 3's dispatched run. If the specs still fail, that is a second defect requiring its own diagnosis, and Task 3's action says so rather than allowing the seeds fix to be declared sufficient. |
| A5 | The Pages source self-heals once the ensure-script runs. | Explicitly unresolved. RESEARCH's Open Question 3 says the script's REST call may or may not succeed under the workflow's declared scope. Task 3 is written to record either outcome, and D-18's fence means the negative outcome is filed rather than fixed. |
| A6 | Deleting the leniency does not red the observer on the next nightly. | Conditional on plans 231-02 through 231-07 having fixed the two reds, which is exactly what D-16's measured inventory says. If the observer reds anyway, the prohibition binds: diagnose and fix or file with an owner. Restoring the exemption is not an available answer. |

<verification>
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0, with both new guards present and each proven fail-first.
- `actionlint -shellcheck= .github/workflows/playwright-github-pages.yml .github/workflows/ci-observe.yml` exits 0.
- YAML parse: the publisher's seeds step exists, is unconditional, and sits strictly between DB setup and boot.
- Comment-stripped read: no trigger-dependent early exit remains in the observer's verdict shell, and the terminal failure branch survives.
- `MAINTAINING.md` residual 4 records the removal in the past tense; the five required-check strings are byte-unchanged.
- A dispatched publisher run id is recorded, with the three checkpoint projects' summary lines and the Pages source outcome.
- The publisher-red todo is resolved with its hypothesis corrected.
</verification>

<success_criteria>
- The Pages publisher seeds the app before booting it, guarded against regression.
- The demotion receipt fails on the scheduled lane exactly as it does on the push lane, guarded against the exemption returning.
- The two wrong written records this phase owed — the todo's spec-drift hypothesis and the runbook's future-tense removal promise — are corrected rather than superseded in silence.
- D-18 is answered on a real run and either closed or filed inside its scope fence.
</success_criteria>

<output>
Create `.planning/phases/231-gate-honesty-nightly-revival/231-10-SUMMARY.md` when done, including both
guards' fail-first observations verbatim, the dispatched publisher run id with its three checkpoint
project summary lines, and the recorded Pages-source outcome.
</output>
