---
phase: 39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/audit/assertions.ex
  - test/sigra/audit/audit_assertions_test.exs
  - guides/recipes/testing.md
autonomous: true
requirements:
  - AUD-01
must_haves:
  truths:
    - "Library exposes plain-function audit test helpers with explicit `repo` and stable-field assertion surface (D-39-01..D-39-02)."
    - "Documentation covers Sandbox ordering, async allowances, and optional host DataCase snippet (D-39-04..D-39-05)."
  artifacts:
    - path: "lib/sigra/audit/assertions.ex"
      provides: "Reusable audit row assertions (library `lib/` so `test/example` subproject can import them)"
    - path: "guides/recipes/testing.md"
      provides: "Sandbox + audit assertion guidance for host apps"
nyquist_compliant: true
---

<objective>
Deliver **AUD-01**: audit-aware **assertion helpers** as **`Sigra.Audit.Assertions`** in **`lib/`** (plain functions, explicit `repo`) plus documentation for Sandbox / ordering / optional `DataCase` snippet — no new end-user auth features.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-39-01 | Information disclosure | Test helpers accidentally stringify secrets | high | Assert API accepts only whitelisted metadata keys; document never pass raw token/hash |
| T-39-02 | Repudiation | Flaky unordered `Repo.all` | medium | Require `order_by: [desc: inserted_at]` (or desc id) in helper query builder |
</threat_model>

<context>
@.planning/phases/39-audit-trail-completeness/39-CONTEXT.md
@.planning/phases/39-audit-trail-completeness/39-RESEARCH.md
@.planning/phases/39-audit-trail-completeness/39-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add Sigra.Audit.Assertions helper module</name>
  <files>lib/sigra/audit/assertions.ex</files>
  <read_first>
    - test/support/audit_test_event.ex
    - test/sigra/audit/log_safe_scope_test.exs
    - lib/sigra/audit/query.ex
    - .planning/phases/39-audit-trail-completeness/39-CONTEXT.md
  </read_first>
  <action>
    1. Create **`lib/sigra/audit/assertions.ex`** defining **`Sigra.Audit.Assertions`** (must live under **`lib/`**, not `test/support`, so the **`test/example`** Mix subproject can depend on it when running its own `mix test` from `test/example/` per root `mix.exs` `test_load_filters`).
    2. Implement at least:
       - **`latest_audit_event/3`** — `(repo, audit_schema_module, filters_keyword)` → build **`Sigra.Audit.query([audit_schema: audit_schema_module] ++ filters_keyword)`** (only keys allowed by **`Sigra.Audit.Query.allowed_filters/0`**), then **pipe** **`import Ecto.Query`** locally: **`|> order_by([desc: e.inserted_at]) |> limit(1)`** (add secondary **`order_by`** on **`e.id`** if schema has id tiebreak per D-39-05), then **`repo.one/1`**; returns `nil` or struct. **Do not** pass `:order_by` into **`Sigra.Audit.query/1`** — it raises **`ArgumentError`** on unknown keys.
       - **`assert_audit_fields/3`** — `(repo, audit_schema_module, required_map)` where `required_map` is a map of **atom field => expected value** for stable columns (`:action`, `:outcome`, `:actor_id`, `:effective_user_id`, `:target_id`, `:organization_id`); for `:metadata`, support **subset** match (expected keys must exist and equal in actual metadata).
    3. Do **not** add macros as the primary API (D-39-01).
    4. When `audit_schema` would be nil, helpers must **raise `ArgumentError`** with message containing **`audit_schema is required`** OR return documented `{:error, :no_audit_schema}` — pick one and document in `@moduledoc`; tests cover the branch.
  </action>
  <acceptance_criteria>
    - `grep -q "defmodule Sigra.Audit.Assertions" lib/sigra/audit/assertions.ex` exits 0.
    - `grep -q "def latest_audit_event" lib/sigra/audit/assertions.ex` exits 0.
    - `grep -q "Sigra.Audit.query" lib/sigra/audit/assertions.ex` exits 0.
    - `grep -q "order_by" lib/sigra/audit/assertions.ex` exits 0 (**`Ecto.Query`**, not passed into **`Sigra.Audit.query/1`** keywords).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile'</automated>
  </verify>
  <done>AUD-01 helper module compiles and is covered by dedicated tests.</done>
</task>

<task type="execute">
  <name>Task 2: Exercises + optional-audit tag pattern</name>
  <files>test/sigra/audit/audit_assertions_test.exs</files>
  <read_first>
    - lib/sigra/audit/assertions.ex
    - test/sigra/audit_test.exs
    - test/support/postgres_test_repo.ex
  </read_first>
  <action>
    1. Add `test/sigra/audit/audit_assertions_test.exs` with:
       - **Case A:** `Sigra.Test.AuditEvent` + `Sigra.AuditTest.StubRepo`-style or **`CaptureRepo`** from `log_safe_scope_test.exs` — prove `assert_audit_fields/3` fails when expected action mismatches.
       - **Case B (optional audit):** One test with `@tag :audit_optional` (or module attribute documented in moduledoc) that **skips** DB assertion when `audit_schema` absent — document pattern in moduledoc (D-39-03).
    2. If Postgres is required for `latest_audit_event/3` integration, use **`Sigra.Test.PostgresRepo`** minimal one-table DDL mirroring `Sigra.Test.AuditEvent` columns needed for query (see `test/sigra/admin/audit/query_test.exs`); use **`async: false`** for that describe block.
  </action>
  <acceptance_criteria>
    - File `test/sigra/audit/audit_assertions_test.exs` exists.
    - `grep -q "Sigra.Audit.Assertions" test/sigra/audit/audit_assertions_test.exs` exits 0.
    - `mix test test/sigra/audit/audit_assertions_test.exs` exits 0 under `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit/audit_assertions_test.exs'</automated>
  </verify>
  <done>Helpers are tested; optional-audit skip pattern is demonstrated once.</done>
</task>

<task type="execute">
  <name>Task 3: Document Sandbox + DataCase snippet</name>
  <files>guides/recipes/testing.md</files>
  <read_first>
    - guides/recipes/testing.md
    - CLAUDE.md
    - .planning/phases/39-audit-trail-completeness/39-CONTEXT.md
  </read_first>
  <action>
    1. Add a subsection **"Audit assertions and Ecto Sandbox"** (or equivalent H2/H3) to `guides/recipes/testing.md`.
    2. Must include verbatim guidance strings:
       - **`order_by` on audit queries** when multiple rows can exist.
       - **`async: true`** tests may need **`Ecto.Adapters.SQL.Sandbox.allow/3`** when audit runs in another process — keep as copy-paste-oriented prose.
       - A **copy-paste `DataCase` setup snippet** (not shipped as library `CaseTemplate`) showing `setup` sandbox allow for conn + DB audit (D-39-04).
    3. Link to **`Sigra.Audit.Assertions`** moduledoc as the library-side reference.
  </action>
  <acceptance_criteria>
    - `grep -q "order_by" guides/recipes/testing.md` exits 0.
    - `grep -q "Sandbox" guides/recipes/testing.md` exits 0.
    - `grep -q "Sigra.Audit.Assertions" guides/recipes/testing.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f guides/recipes/testing.md && grep -q order_by guides/recipes/testing.md && grep -q Sandbox guides/recipes/testing.md'</automated>
  </verify>
  <done>Host developers can follow Sandbox guidance for audit tests.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/audit/audit_assertions_test.exs` exits 0.
- No new dependencies in `mix.exs`.
</verification>

<success_criteria>
- **AUD-01** satisfied: reusable helpers + docs; decisions D-39-01..D-39-05 addressed in shipped artifacts.
</success_criteria>
