---
phase: 39
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - test/example/test/example_web/smoke/register_login_logout_test.exs
  - test/example/test/example_web/smoke/mfa_totp_test.exs
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
  - .planning/seeds/SEED-002-phase-9-log-safe-atomicity-followup.md
  - docs/audit-semantics.md
autonomous: true
requirements:
  - AUD-01
  - AUD-02
  - AUD-03
must_haves:
  truths:
    - "Example app smoke proves **`auth.login.success`** audit row on password login and **`auth.login.failure`** (known user / `invalid_password`) on bad password (D-39-10)."
    - "Example app MFA smoke proves at least one audited MFA success path (**`mfa.enroll.success`** or **`mfa.verify.success`** — pick one exercised by existing test flow) (D-39-11 MFA preference)."
    - "`REQUIREMENTS.md` AUD bullets reflect **tested** vs **deferred** honestly; if OAuth is not chosen, AUD-03 text states MFA path and does not claim OAuth coverage (D-39-12)."
    - "`CHANGELOG.md` and **`SEED-002`** reference the AUD-02 / AUD-03 closure or residual scope (D-39-13)."
  artifacts:
    - path: "test/example/test/example_web/smoke/register_login_logout_test.exs"
      provides: "Login audit assertions"
    - path: "test/example/test/example_web/smoke/mfa_totp_test.exs"
      provides: "MFA audit assertions"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Honest AUD checklist"
nyquist_compliant: true
---

<objective>
Deliver **AUD-03** sites **2–3** via **example app** smoke tests (strongest real `AuditEvent` wiring), update **canonical trio** docs (`REQUIREMENTS`, `CHANGELOG`, `SEED-002`), and add optional **`docs/audit-semantics.md`** (D-39-14).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-39-04 | Information disclosure | Tests log passwords or raw session tokens | high | Assertions use **`Sigra.Audit.Assertions`** on stable columns only; never print secrets |
| T-39-05 | Repudiation | REQUIREMENTS claims OAuth/MFA coverage without tests | medium | Grep gates on action strings present in test files referenced from REQ text |
</threat_model>

<context>
@.planning/phases/39-audit-trail-completeness/39-CONTEXT.md
@test/example/lib/example/accounts.ex
@lib/sigra/auth.ex
@lib/sigra/mfa.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Login success + failure audit rows (example app)</name>
  <files>test/example/test/example_web/smoke/register_login_logout_test.exs</files>
  <read_first>
    - test/example/test/example_web/smoke/register_login_logout_test.exs
    - test/example/lib/example/accounts.ex
    - lib/sigra/auth.ex
    - lib/sigra/audit/assertions.ex
  </read_first>
  <action>
    1. Import or alias **`Example.Repo`**, **`Example.Accounts.AuditEvent`**, and **`Sigra.Audit.Assertions`** (from Plan 01 — lives under **`lib/sigra/audit/assertions.ex`** so this subproject can load it) in the smoke test module.
    2. In **`get_user_by_email_and_password returns user with valid credentials`**, after successful login assertion, call **`Sigra.Audit.Assertions.latest_audit_event(Repo, AuditEvent, action: "auth.login.success")`** (or equivalent filters) and **`assert_audit_fields/3`** expecting at least **`action: "auth.login.success"`**, **`metadata`** subset **`%{method: "password"}`**, and **`target_id`** equal user id (binary_id aware).
    3. Add a new test: wrong password for **existing** user → expect latest relevant row with **`action: "auth.login.failure"`** and **`metadata`** containing **`reason: "invalid_password"`** (string keys if JSON column — match how **`AuditEvent`** stores metadata).
    4. Use **`Ecto.Adapters.SQL.Sandbox`**-safe patterns (tests already `use Example.DataCase`) — if async issues arise, set **`async: false`** for this module **only** as last resort and document in commit message.
  </action>
  <acceptance_criteria>
    - `grep -q "auth.login.success" test/example/test/example_web/smoke/register_login_logout_test.exs` exits 0.
    - `grep -q "auth.login.failure" test/example/test/example_web/smoke/register_login_logout_test.exs` exits 0.
    - `grep -q "Sigra.Audit.Assertions" test/example/test/example_web/smoke/register_login_logout_test.exs` exits 0.
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/smoke/register_login_logout_test.exs` exits 0 (example is a **separate** Mix project — never invoke its tests from the library root `mix test` without `cd`).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/smoke/register_login_logout_test.exs'</automated>
  </verify>
  <done>AUD-03 site 2 covered.</done>
</task>

<task type="execute">
  <name>Task 2: MFA audited path (example app)</name>
  <files>test/example/test/example_web/smoke/mfa_totp_test.exs</files>
  <read_first>
    - test/example/test/example_web/smoke/mfa_totp_test.exs
    - lib/sigra/mfa.ex
    - lib/sigra/audit/assertions.ex
  </read_first>
  <action>
    1. Identify the first test (or smallest sequence) that completes **TOTP enrollment** or **verify** with persisted user through **`Example.Accounts`** / **`Sigra.MFA`** such that **`mfa.enroll.success`** or **`mfa.verify.success`** is emitted per **`lib/sigra/mfa.ex`** comments.
    2. After that step, assert an **`Example.Accounts.AuditEvent`** row using **`Sigra.Audit.Assertions`** with the chosen **`action`** string exactly **`"mfa.enroll.success"`** or **`"mfa.verify.success"`** (must match **`Sigra.Audit`** call sites in **`mfa.ex`**).
    3. If the smoke file cannot reach a logged path without large fixture work, **stop** and instead add a new **`test/example/test/example/accounts/mfa_audit_test.exs`** (DataCase) — but prefer extending **`mfa_totp_test.exs`** first.
  </action>
  <acceptance_criteria>
    - At least one of: `grep -q "mfa.enroll.success" test/example/test/example_web/smoke/mfa_totp_test.exs` OR same for **`mfa.verify.success`** exits 0 **OR** new test file under **`test/example/test/`** contains that substring.
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/smoke/mfa_totp_test.exs` exits 0 (adjust path if new file used — run that file instead in verify).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/smoke/mfa_totp_test.exs'</automated>
  </verify>
  <done>AUD-03 site 3 (MFA) covered; OAuth explicitly not claimed unless implemented.</done>
</task>

<task type="execute">
  <name>Task 3: REQUIREMENTS + CHANGELOG + SEED-002</name>
  <files>.planning/REQUIREMENTS.md,CHANGELOG.md,.planning/seeds/SEED-002-phase-9-log-safe-atomicity-followup.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - CHANGELOG.md
    - .planning/seeds/SEED-002-phase-9-log-safe-atomicity-followup.md
    - .planning/phases/39-audit-trail-completeness/39-CONTEXT.md
  </read_first>
  <action>
    1. Update **AUD-01..AUD-03** checklist rows to distinguish **implemented / tested** language; add **deferral paragraph** only if executor could not land MFA assertion (D-39-12) — otherwise mark complete with pointers to test paths.
    2. **`CHANGELOG.md`**: Under unreleased (or next patch), add bullet summarizing **atomic `api.token_create` audit** + **audit-aware test helpers** + **smoke audit assertions** — no hype, cite modules.
    3. **`SEED-002`**: Add **"Phase 39 resolution"** subsection with links to **`39-02-SUMMARY.md`** / **`39-03-SUMMARY.md`** placeholders (executor fills after execution) and note residual **`log_safe`** sites if any remain intentionally.
  </action>
  <acceptance_criteria>
    - `grep -q "AUD-01" .planning/REQUIREMENTS.md` exits 0 (pre-existing) AND file mentions **`Sigra.Audit.Assertions`** OR **`audit assertions`** in AUD-01 bullet after edit.
    - `grep -q "api.token_create" CHANGELOG.md` exits 0 after edit.
    - `grep -q "Phase 39" .planning/seeds/SEED-002-phase-9-log-safe-atomicity-followup.md` exits 0 after edit.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "Phase 39" .planning/seeds/SEED-002-phase-9-log-safe-atomicity-followup.md && grep -q api.token_create CHANGELOG.md'</automated>
  </verify>
  <done>Canonical trio updated (D-39-13).</done>
</task>

<task type="execute">
  <name>Task 4 (optional): docs/audit-semantics.md</name>
  <files>docs/audit-semantics.md</files>
  <read_first>
    - lib/sigra/audit.ex
    - README.md
  </read_first>
  <action>
    1. If time permits in same PR as Phase 39 execution, create **`docs/audit-semantics.md`** summarizing:
       - **`log_safe/3`** — best-effort, never changes caller return shape; **`[:sigra, :audit, :log_safe_error]`**.
       - **`Ecto.Multi` + `log_multi_safe` / `__log_internal__`** — same-transaction; telemetry via **`emit_telemetry_from_changes/1`**.
       - **Non-goals:** full conversion of all `log_safe` sites.
    2. Add link from **`README.md`** **"Documentation"** or **guides** section pointing to this file **only** if README already lists doc links — keep diff minimal.
  </action>
  <acceptance_criteria>
    - Either **`test -f docs/audit-semantics.md`** AND **`grep -q "log_safe" docs/audit-semantics.md`** OR commit message states **`skip D-39-14`** — executor must not leave half-written file.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'if test -f docs/audit-semantics.md; then grep -q log_safe docs/audit-semantics.md; else true; fi'</automated>
  </verify>
  <done>Optional public semantics doc landed or explicitly waived in commit.</done>
</task>

</tasks>

<verification>
- `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/smoke/register_login_logout_test.exs test/example_web/smoke/mfa_totp_test.exs` exits 0.
- Spot-check: no raw password strings in new assertion code beyond existing fixture patterns.
</verification>

<success_criteria>
- **AUD-03** sites **2–3** evidenced in example smoke + docs trio updated.
- **D-39-14** optional doc is either present or explicitly waived.
</success_criteria>
