---
phase: 125-sso-only-enforcement-break-glass-truth
plan: 04
type: execute
wave: 4
depends_on:
  - 125-03
files_modified:
  - lib/sigra/install/features/organizations.ex
  - priv/templates/sigra.install/organizations/organization_auth_policy.ex
  - priv/templates/sigra.install/organizations/organization_auth_policy_exemption.ex
  - priv/templates/sigra.install/organizations/organization_auth_policies_migration.exs
  - priv/templates/sigra.install/organizations/organizations.ex
  - priv/templates/sigra.install/organizations/live/organization_settings_live.ex
  - priv/templates/sigra.install/core/session_controller.ex
  - test/sigra/install/features/organizations_test.exs
  - test/sigra/install/golden_diff_test.exs
autonomous: true
requirements:
  - ENF-01
must_haves:
  truths:
    - "The generated-host installer contract ships the same SSO-only and break-glass policy surfaces proven in the committed example app."
    - "Organizations feature templates and migrations remain coherent under installer output and golden fixtures."
    - "Installer regression coverage catches future drift between example-app proof and generated-host contract."
  artifacts:
    - path: "priv/templates/sigra.install/organizations/live/organization_settings_live.ex"
      provides: "Generated-host organization settings contract for SSO-only controls."
    - path: "priv/templates/sigra.install/core/session_controller.ex"
      provides: "Generated-host local login surface that preserves SSO-only denial truth."
    - path: "test/sigra/install/features/organizations_test.exs"
      provides: "Regression coverage for the new organizations feature files and template content."
  key_links:
    - from: "lib/sigra/install/features/organizations.ex"
      to: "priv/templates/sigra.install/organizations/organization_auth_policy.ex"
      via: "installer registers new auth-policy templates and migration"
      pattern: "organization_auth_policy"
    - from: "priv/templates/sigra.install/core/session_controller.ex"
      to: "test/example/lib/example_web/controllers/session_controller.ex"
      via: "generated-host login contract stays aligned with the committed example"
      pattern: "enterprise sign-in|sso_required"
---

<objective>
Lock the generated-host installer contract to the Phase 125 runtime and UX behavior so adopters get the same truthful SSO-only/break-glass surface that the example app proves.

Purpose: Prevent Phase 125 from being "example only" by syncing installer templates, organizations feature registration, and golden/install tests with the new policy and host-flow contract.
Output: Template additions and updates, installer feature registration, and regression coverage that protects the generated-host SSO-only contract from drift.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/125-sso-only-enforcement-break-glass-truth/125-CONTEXT.md
@.planning/phases/125-sso-only-enforcement-break-glass-truth/125-RESEARCH.md
@.planning/phases/125-sso-only-enforcement-break-glass-truth/125-03-PLAN.md
@lib/sigra/install/features/organizations.ex
@priv/templates/sigra.install/core/session_controller.ex
@priv/templates/sigra.install/organizations/live/organization_settings_live.ex
@test/sigra/install/features/organizations_test.exs

<interfaces>
From lib/sigra/install/features/organizations.ex:
```elixir
def files(binding)
def migrations(binding)
```

Existing template roots:
```text
priv/templates/sigra.install/core/
priv/templates/sigra.install/organizations/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SSO-only policy templates and installer registration</name>
  <read_first>.planning/phases/125-sso-only-enforcement-break-glass-truth/125-RESEARCH.md, lib/sigra/install/features/organizations.ex, priv/templates/sigra.install/organizations/organizations.ex, priv/templates/sigra.install/organizations/live/organization_settings_live.ex, test/sigra/install/features/organizations_test.exs</read_first>
  <files>lib/sigra/install/features/organizations.ex, priv/templates/sigra.install/organizations/organization_auth_policy.ex, priv/templates/sigra.install/organizations/organization_auth_policy_exemption.ex, priv/templates/sigra.install/organizations/organization_auth_policies_migration.exs, priv/templates/sigra.install/organizations/organizations.ex, priv/templates/sigra.install/organizations/live/organization_settings_live.ex, test/sigra/install/features/organizations_test.exs</files>
  <behavior>
    - Test 1: The organizations installer feature emits the new policy/exemption schemas and migration.
    - Test 2: Generated `organizations.ex` and `organization_settings_live.ex` templates expose the same SSO-only controls and break-glass safety rails as the example app.
    - Test 3: Feature tests assert these new files and content blocks are present.
  </behavior>
  <acceptance_criteria>
    - `test -f priv/templates/sigra.install/organizations/organization_auth_policy.ex` succeeds.
    - `test -f priv/templates/sigra.install/organizations/organization_auth_policy_exemption.ex` succeeds.
    - `test -f priv/templates/sigra.install/organizations/organization_auth_policies_migration.exs` succeeds.
    - `rg -n "organization_auth_policy|organization_auth_policy_exemption|organization_auth_policies_migration" lib/sigra/install/features/organizations.ex test/sigra/install/features/organizations_test.exs` finds installer registration and regression assertions.
    - `rg -n "SSO-only|break-glass" priv/templates/sigra.install/organizations/live/organization_settings_live.ex` finds the generated settings contract.
  </acceptance_criteria>
  <action>Add new organizations feature templates for the auth-policy schema, exemption schema, and migration. Register them in `Sigra.Install.Features.Organizations.files/1` and `migrations/1`, and update the generated `organizations.ex` and `organization_settings_live.ex` templates so installed apps receive the same wrapper APIs and operator controls as the committed example app. Extend `test/sigra/install/features/organizations_test.exs` to assert the new template sources, target paths, and content markers.</action>
  <verify>
    <automated>mix test test/sigra/install/features/organizations_test.exs</automated>
  </verify>
  <done>The installer organizations feature can generate the new Phase 125 policy substrate and operator surface, and tests prove those files are registered correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock generated-host login truth with template and golden regression coverage</name>
  <read_first>.planning/phases/125-sso-only-enforcement-break-glass-truth/125-03-PLAN.md, priv/templates/sigra.install/core/session_controller.ex, test/sigra/install/features/organizations_test.exs, test/sigra/install/golden_diff_test.exs, test/fixtures/install_golden/tree</read_first>
  <files>priv/templates/sigra.install/core/session_controller.ex, test/sigra/install/features/organizations_test.exs, test/sigra/install/golden_diff_test.exs, test/fixtures/install_golden/tree</files>
  <behavior>
    - Test 1: The generated session controller template preserves typed enterprise denial handling instead of only generic invalid-password fallback.
    - Test 2: Installer regression tests assert the new login and settings template markers.
    - Test 3: Golden output stays aligned after the new organizations files and session-controller contract land.
  </behavior>
  <acceptance_criteria>
    - `rg -n "sso_required|enterprise sign-in|break-glass" priv/templates/sigra.install/core/session_controller.ex` finds the generated login denial contract.
    - `rg -n "organization_auth_policy|SSO-only|break-glass" test/sigra/install/features/organizations_test.exs` finds template regression assertions.
    - `mix test test/sigra/install/features/organizations_test.exs test/sigra/install/golden_diff_test.exs` passes.
  </acceptance_criteria>
  <action>Update the generated core `session_controller.ex` template so installed apps get the same SSO-only denial translation as the example controller. Extend installer feature tests to assert those markers directly. Refresh the install golden fixture if needed so the golden diff contract covers the new organizations files and generated-host login/settings changes rather than letting Phase 125 behavior drift out of the installer path.</action>
  <verify>
    <automated>mix test test/sigra/install/features/organizations_test.exs test/sigra/install/golden_diff_test.exs</automated>
  </verify>
  <done>The installer path ships the same SSO-only/login truth as the example app, and golden regression coverage protects that generated-host contract from future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed example app -> installer templates | The generated-host contract must not diverge from the example proof surface. |
| installer templates -> golden regression fixture | Template drift must fail in tests instead of silently changing adopter output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-13 | T | organizations installer feature | mitigate | Register explicit policy/exemption files and migration so generated apps receive the same bounded substrate as the example app. |
| T-125-14 | S | generated session controller | mitigate | Sync the generated login flow with typed SSO-only denial handling so installer output does not reintroduce misleading local-auth fallback. |
| T-125-15 | R | installer regression coverage | mitigate | Add assertions and golden coverage for the new files/content so future drift in generated-host truth fails visibly. |
| T-125-16 | D | template/operator safety rails | mitigate | Carry the settings-page break-glass enablement checks into templates so installed apps do not ship a lockout-prone default surface. |
</threat_model>

<verification>
Run `mix test test/sigra/install/features/organizations_test.exs test/sigra/install/golden_diff_test.exs`.
</verification>

<success_criteria>
The installer path generates the same SSO-only policy, break-glass controls, and local-auth denial truth as the committed example app, with regression coverage that catches drift.
</success_criteria>

<output>
After completion, create `.planning/phases/125-sso-only-enforcement-break-glass-truth/125-04-SUMMARY.md`
</output>
