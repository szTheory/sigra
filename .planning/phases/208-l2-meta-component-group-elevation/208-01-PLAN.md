---
phase: "208"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02

must_haves:
  truths:
    - "D-02: All 11 board-mg-* isolated boards (mg-1…mg-11) and all 4 board-cfg-* composites (board-cfg-overview, board-cfg-users-list, board-cfg-user-detail, board-cfg-audit) are audited against the L2 Tier-2 proxies (intra-group single-tier sg-stack rhythm; no card-in-card; right-component-for-job; zero/loading/error states; byte-coherent reuse; MG-5/MG-6 desktop↔mobile content-equivalence) and the findings are recorded in the SUMMARY per group — citing the already-wired gates in admin-design.spec.ts, NOT rebuilding them"
    - "D-04: The audit confirms — and the SUMMARY records — that the L2 gate stack is already wired and green: per-board axe+screenshot ×3 projects (assertBoardScreenshot, GROUP_BOARDS loop ~:257), the .sg-card .sg-card card-in-card check (~:349-361, live on all 11 — no board carries data-sg-card-nesting-audit-only), the per-group catalog-state markers (GROUP_STATE_MARKERS ~:120-131 + the 'group boards expose catalog states' test ~:318-362 with exact component counts), and MG-5/MG-6 content-equivalence (~:364-383). The EXPECTED outcome is ZERO genuine CSS gaps and NO CSS edit"
    - "D-08: The SUMMARY records the ACTUAL state-marker shape per group as it exists in design_gallery_live.ex — only mg-3 uses the deliberate state-N/A note pattern (mg-3-zero-note ~:574, mg-3-loading-note ~:577); mg-9 and mg-11 render real mg-9-zero/mg-9-loading (~:1024,~:1029) and mg-11-zero/mg-11-loading (~:1108,~:1112) markers. Do NOT fabricate states and do NOT mislabel a real state as a deliberate N/A note — cite each group's true marker set verbatim from GROUP_STATE_MARKERS"
    - "D-06: The SUMMARY records the MG-7 (mg-7-organization-member-roster, board-mg-7) and MG-8 (mg-8-pending-invitations, board-mg-8) isolated-board-only ruling — there is no board-cfg-org composite and one MUST NOT be authored; isolated-board coverage is sufficient and the absence is intentional"
    - "D-03: IF (and only if) the audit surfaces a genuine sg-* gap, the narrow fix lands in priv/templates/sigra.install/admin/sigra_admin.css ONLY, and the two generated copies (test/example/priv/static/assets/sigra_admin.css and test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css) are re-synced byte-for-byte so all 3 copies share one identical md5"
  artifacts:
    - priv/templates/sigra.install/admin/sigra_admin.css
  prohibitions:
    - statement: "D-01: Phase 208 touches only the 11 board-mg-* isolated boards + 4 board-cfg-* composites in design_gallery_live.ex and the underlying sg-* CSS — it makes NO page-composition / cross-page-placement edits. Specifically NO Overview page-recompose (org-overview empty-state, index 'All clear' verbosity, org invite-CTA dead-end, total-users redundancy) — all of which are Phase 209 page-pass scope, not Phase 208"
      status: enforced
      verification: "git diff (changed-files scope-fence): the only files this plan may touch are priv/templates/sigra.install/admin/sigra_admin.css + its two generated copies, and (conditionally) the board markup of the 11 board-mg-*/4 board-cfg-* sections in design_gallery_live.ex; NO admin LiveView page file (index_live.ex, organization_live.ex, users_index_live.ex, user_show_live.ex, audit_index_live.ex, audit_user_live.ex, branding_live.ex) is modified"
    - statement: "D-06: NO net-new board-cfg-org composite is authored for MG-7/MG-8 — isolated-board coverage is sufficient and the milestone holds a 'no net-new surfaces' posture"
      status: enforced
      verification: "grep -c 'board-cfg-org' test/example/lib/example_web/live/admin/design_gallery_live.ex returns 0; CONFIG_BOARDS in admin-design.spec.ts (~:118) still lists exactly 4 ids and does not include board-cfg-org"
    - statement: "D-05: This plan does NOT capture or recapture any Playwright PNG baseline — baseline capture is Plan 02 (CI-native via admin_design_recapture). Capturing board PNGs on darwin is forbidden (reverted at 43f5a3e4)"
      status: enforced
      verification: "git status --porcelain test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ is empty after this plan"
  key_links:
    - "Per-group audit findings (single-tier rhythm, no card-in-card, right-component, state-marker set, content-equivalence for mg-5/mg-6) are recorded in the SUMMARY as the source material for Plan 03's 11 ledger evidence strings"
    - "Whether a CSS edit landed (expected: none) gates Plan 02's recapture scope: zero edit → Plan 02 still captures the 12 net-new cfg PNGs (no mg recapture); an edit → Plan 02 also recaptures the affected board-mg-* slugs"
    - "The MG-7/MG-8 isolated-only ruling (D-06) and the per-group true state-marker shape (D-08) recorded here are quoted verbatim into Plan 03's mg-7/mg-8/mg-3 evidence strings"
---

<objective>
Audit the 11 L2 meta-component group boards (board-mg-1…board-mg-11) on their isolated gallery
boards and the 4 real-page composites (board-cfg-overview, board-cfg-users-list,
board-cfg-user-detail, board-cfg-audit) against the L2 Tier-2 proxies. Cite the already-wired
gate stack in admin-design.spec.ts (per-board axe+screenshot ×3 projects, card-in-card check,
per-group catalog-state markers/counts, MG-5/MG-6 content-equivalence). Land a narrow sg-* CSS
fix ONLY if the audit surfaces a genuine gap; the research-verified expected outcome is ZERO
gaps and NO CSS edit.

Purpose: This is the audit-and-(maybe)-fix step of the proven 206/207 audit → cite →
narrow-gap-fix → recapture → ledger-flip method (D-02), reapplied one tier up at the L2 group
level. The 11 group boards are already structurally Tier-2 (right components, single-tier
sg-stack rhythm, no card-in-card, all four states rendered); the audit genuinely re-verifies
each group and documents what exists for Plan 03's evidence strings. The phase is cite-and-flip,
not refactor.

Output: A SUMMARY with a per-group audit section (11 mg groups + 4 cfg composites); an updated
sigra_admin.css ONLY if a real gap is found (with byte-coherent generated copies); a clear
"CSS edited: yes/no" signal + affected board-mg-* slug list for Plan 02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/208-l2-meta-component-group-elevation/208-CONTEXT.md
@.planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-PLAN.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-ui-principles.md
@test/example/lib/example_web/live/admin/design_gallery_live.ex
@test/example/priv/playwright/tests/admin-design.spec.ts
@priv/templates/sigra.install/admin/sigra_admin.css
</context>

## Artifacts this phase produces

- (conditional) a narrow `sg-*` CSS edit in `priv/templates/sigra.install/admin/sigra_admin.css` — ONLY if the audit surfaces a genuine gap (expected: none); re-synced byte-coherently to the 2 generated copies. Most of this plan is read-and-document, not create.
- a per-group audit section in the SUMMARY (11 `board-mg-*` groups + 4 `board-cfg-*` composites × the L2 proxies) — the source material for Plan 03 ledger evidence
- a "CSS edited: yes/no" signal + (if yes) the affected `board-mg-*` slug list in the SUMMARY that gates Plan 02's recapture scope
- the verbatim MG-7/MG-8 isolated-board-only ruling (D-06) and the per-group true state-marker shape (D-08) recorded in the SUMMARY for Plan 03's evidence strings

<tasks>

<task type="auto">
  <name>Task 1: Audit 11 board-mg-* groups + 4 board-cfg-* composites against the L2 Tier-2 proxies</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css</files>

  <read_first>
  - test/example/lib/example_web/live/admin/design_gallery_live.ex — read the 11 group board sections by id: board-mg-1 (~:457), board-mg-2 (~:491), board-mg-3 (~:557, a `<section>` not sg-card), board-mg-4 (~:589), board-mg-5 (~:613, a `<section>`), board-mg-6 (~:807), board-mg-7 (~:935), board-mg-8 (~:965), board-mg-9 (~:997), board-mg-10 (~:1042), board-mg-11 (~:1096); and the 4 cfg composites: board-cfg-overview (~:1192), board-cfg-users-list (~:1227), board-cfg-user-detail (~:1261), board-cfg-audit (~:1299). Note the actual state markers per group (e.g. mg-3-zero-note ~:574, mg-3-loading-note ~:577 are deliberate N/A notes; mg-9-zero ~:1024, mg-9-loading ~:1029, mg-11-zero ~:1108, mg-11-loading ~:1112 are real states).
  - test/example/priv/playwright/tests/admin-design.spec.ts — GROUP_BOARDS (~:104-116), CONFIG_BOARDS (~:118), GROUP_STATE_MARKERS (~:120-131, the authoritative per-group marker set), board screenshot loop (~:257-261), config-board archetype-section test (~:308-316), 'group boards expose catalog states and right components' test with exact component counts + the .sg-card .sg-card nesting check (~:318-362), MG-5/MG-6 content-equivalence test (~:364-383), assertUserResultEquivalence (~:162-173), assertAuditResultEquivalence (~:175-207)
  - guides/reference/admin-fractal-scorecard.md "L2 Meta-Component Group Add-ons" (~:61-79) — the proxy vocabulary: intra-group rhythm consistent, no card-in-card, right-component-for-job, zero/loading/error states defined, desktop-table↔mobile-card content-equivalence, byte-coherent reuse
  - guides/reference/admin-quality-ledger.md — the 11 mg-* L2 rows (~:74-84, currently tier 1); the Tier-2 evidence exemplar = the freshly-flipped L1 rows (~:61-73); the bare-2 / no-decorator parsing rule
  - guides/reference/admin-design-contract.md — archetype compositions + same-job→same-component (Overview / List / Detail / Audit archetypes the cfg composites mirror)
  - priv/templates/sigra.install/admin/sigra_admin.css — read the group sg-* blocks the boards depend on: sg-metric / sg-summary-facts, sg-filter-panel / sg-applied-chip, sg-card / sg-card-hover, sg-notice tones, sg-table-panel / sg-list / sg-list-row, sg-detail-grid, sg-confirm-overlay / sg-confirm-dialog; and the global @media (prefers-reduced-motion: reduce) block
  - .planning/phases/207-l1-component-elevation-wave-b-l0-token-layer/207-02-SUMMARY.md — the per-component audit shape to mirror at the group level (audit → cite gate → document; expected zero edits)
  </read_first>

  <action>
  Perform a code-level audit of all 11 board-mg-* groups and all 4 board-cfg-* composites. For each group, document in the SUMMARY (not inline in this PLAN) under a "Per-Group Audit" section, citing the already-wired gate that proves each proxy:

  1. INTRA-GROUP SINGLE-TIER RHYTHM — confirm each group uses one sg-stack--N tier between its components (sg-stack--4 on the mg board wrappers; no mixed stacking contexts within one group). Cite the gallery markup line.

  2. NO CARD-IN-CARD — confirm no .sg-card nested inside .sg-card within the group. Cite the live nesting check at admin-design.spec.ts ~:349-361 (no board carries data-sg-card-nesting-audit-only — the escape hatch is unused). Note the deliberate unframed wrappers: board-mg-3 (~:557) and board-mg-5 (~:613) are `<section>` not sg-card (mg-3 test asserts not.toHaveClass(sg-card) ~:334); board-mg-6's sg-card wrapper holds sg-table-panel/sg-list inner results, not nested sg-card.

  3. RIGHT-COMPONENT-FOR-JOB — confirm each group uses the canonical component per the design contract and the exact-count assertions: mg-1 has 7 .sg-metric (~:331); mg-2 has 6 .sg-applied-chip (~:332); mg-3 has 2 article.sg-card (~:333); mg-4 has 3 .sg-notice (~:335); mg-6 has 3 article.sg-list-row (~:340); mg-7/mg-8 have 3 .sg-list-row each (~:341-342); mg-9 has 1 .sg-summary-facts (~:343); mg-10 has 2 .sg-detail-grid (~:344); mg-11 has 2 .sg-confirm-overlay .sg-confirm-dialog (~:345).

  4. ZERO/LOADING/ERROR STATES — record the ACTUAL marker set per group from GROUP_STATE_MARKERS (~:120-131), verbatim. Specifically:
     - mg-3 uses deliberate state-N/A notes: mg-3-zero-note, mg-3-loading-note (per D-08 — cite as an intentional "N/A" ruling, NOT a missing state; mg-3 still has a real mg-3-error marker).
     - mg-9 and mg-11 render REAL states: mg-9-zero / mg-9-loading / mg-9-error and mg-11-zero / mg-11-loading / mg-11-error (do NOT mislabel these as notes).
     - mg-7/mg-8 zero-states use <.empty_state> (~:947-951, ~:977-983); mg-4 has a real alarm zero-state.
     - All other groups (mg-1,2,4,5,6,10) render the full populated/zero/loading/error set.
     Cite the 'group boards expose catalog states' test (~:318-329) that enforces each marker visible.

  5. MG-5 / MG-6 DESKTOP↔MOBILE CONTENT-EQUIVALENCE — confirm the table+mobile-card swap proxy applies to mg-5 and mg-6 ONLY (D-07): mg-5 via assertUserResultEquivalence, mg-6 via assertAuditResultEquivalence + the equivalence test (~:364-383). Record content-equivalence: N/A for all other groups.

  6. CFG COMPOSITES — for board-cfg-overview / board-cfg-users-list / board-cfg-user-detail / board-cfg-audit, confirm each exposes the archetype header (h1/h2) the config-board test asserts (~:308-316) and mirrors its page archetype per admin-design-contract.md. Record that there is NO board-cfg-org composite and per D-06 one MUST NOT be authored — MG-7/MG-8 are isolated-board-only by design; capture this ruling as an explicit sentence for the mg-7/mg-8 ledger evidence.

  7. BYTE-COHERENT REUSE — confirm groups that reuse a pattern (e.g. mg-2 applied-chips, mg-6 audit feed, mg-11 confirmation) render the same sg-* classes/data attributes across the isolated board and the cfg composite.

  VERDICT: record the expected "ZERO genuine sg-* CSS gaps — cite-and-flip, no CSS edit" verdict, OR — if the audit genuinely surfaces a real gap (e.g. a sg-* rule with a raw value, a missing single-tier rhythm) — describe the single narrow fix needed (Task 2 will apply it). Record "CSS edited: no" (expected) or "CSS edited: yes — <board-mg-* slug(s)>" in the SUMMARY for Plan 02.
  </action>

  <verify>
    <automated>bash -c 'grep -c "board-cfg-org" test/example/lib/example_web/live/admin/design_gallery_live.ex | grep -qx 0 && [ "$(grep -v "^#" priv/templates/sigra.install/admin/sigra_admin.css | grep -c "transition: all")" = "0" ] && echo OK'</automated>
  </verify>

  <acceptance_criteria>
  - SUMMARY contains a "Per-Group Audit" section covering all 11 board-mg-* groups + 4 board-cfg-* composites against the 6 L2 proxies (single-tier rhythm / no card-in-card / right-component / states / content-equivalence / byte-coherent reuse)
  - The SUMMARY records each group's ACTUAL state-marker set verbatim from GROUP_STATE_MARKERS; mg-3 is documented as the deliberate N/A-note group (mg-3-zero-note / mg-3-loading-note) while mg-9 and mg-11 are documented as real-state groups (D-08)
  - The SUMMARY records the MG-7/MG-8 isolated-board-only ruling (D-06) as an explicit sentence; no board-cfg-org composite exists: grep -c 'board-cfg-org' test/example/lib/example_web/live/admin/design_gallery_live.ex returns 0
  - The SUMMARY records content-equivalence applies to mg-5/mg-6 ONLY (D-07), N/A elsewhere
  - Audit confirms zero "transition: all" (filtered): grep -v '^#' priv/templates/sigra.install/admin/sigra_admin.css | grep -c 'transition: all' returns 0
  - The SUMMARY records an explicit verdict line: "CSS edited: no" (expected) or "CSS edited: yes — <board-mg-* slug(s)>" for Plan 02
  - No Playwright PNG changed: git status --porcelain test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ is empty
  </acceptance_criteria>

  <done>All 11 mg groups + 4 cfg composites audited against the 6 L2 proxies; per-group findings (true state markers, mg-7/mg-8 ruling, mg-5/mg-6 equivalence) documented in the SUMMARY; explicit "CSS edited: yes/no" verdict + slug list recorded for Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Apply narrow sg-* gap fix ONLY if found, and re-sync byte-coherence</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</files>

  <read_first>
  - .planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md (the in-progress SUMMARY from Task 1) — read the verdict; if "CSS edited: no", this task is a no-op verification only
  - priv/templates/sigra.install/admin/sigra_admin.css — the edit target IF a gap was found (Task 1 names the exact sg-* rule + line)
  - test/example/priv/static/assets/sigra_admin.css — generated copy 1; currently byte-identical to source
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css — generated copy 2; same md5
  - CLAUDE.md "Local development prerequisites" — the golden tests require phx_new 1.8.7 pinned (do NOT regenerate the fixture against a newer archive; install 1.8.7 if golden-diff shows a spurious config.exs byte-diff)
  </read_first>

  <action>
  IF Task 1's verdict is "CSS edited: no" (the research-verified expected outcome):
    No CSS edit. Verify the 3 copies remain byte-coherent (identical md5). Record "no-op — zero gaps confirmed" in the SUMMARY. STOP.

  IF Task 1 surfaced a genuine gap requiring a narrow sg-* fix:
    EDIT the source ONLY: priv/templates/sigra.install/admin/sigra_admin.css — apply the single narrow fix Task 1 described (e.g. a raw-value→token swap, a single-tier rhythm correction). Use Edit (scoped replacement), never Write the whole file. Do NOT introduce raw hex/px; do NOT add a per-component @media (prefers-reduced-motion) block (the global block is sufficient).

    PROPAGATE the identical character-for-character change to BOTH generated copies:
      - test/example/priv/static/assets/sigra_admin.css
      - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    Apply via Edit (scoped) to each — match the same surrounding context and apply the same replacement.

  VERIFY byte-coherence (always, both branches):
    - all 3 sigra_admin.css copies share one md5
    - bash scripts/ci/admin-css-conformance.sh exits 0 (no transition:all, no raw hex outside :root)
    - mix test test/sigra/admin/components_test.exs exits 0 (component golden suite — byte-match preserved)

  Record the final verdict and (if edited) the affected board-mg-* PNG slugs (hyphenated, e.g. board-mg-1) in the SUMMARY for Plan 02's recapture.
  </action>

  <verify>
    <automated>bash -c 'set -e; a=$(md5 -q priv/templates/sigra.install/admin/sigra_admin.css); b=$(md5 -q test/example/priv/static/assets/sigra_admin.css); c=$(md5 -q test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css); [ "$a" = "$b" ] && [ "$b" = "$c" ] && echo "byte-coherent: $a"'</automated>
  </verify>

  <acceptance_criteria>
  - All 3 sigra_admin.css copies share an identical md5 (byte-coherent): the verify command prints "byte-coherent: <hash>"
  - bash scripts/ci/admin-css-conformance.sh exits 0 (no transition:all, no raw hex outside :root)
  - mix test test/sigra/admin/components_test.exs exits 0 (golden byte-match preserved)
  - SUMMARY records the final "CSS edited: yes/no" verdict and, if yes, the exact affected board-mg-* slug list (hyphenated) for Plan 02
  - No board-cfg-org composite was introduced: grep -c 'board-cfg-org' test/example/lib/example_web/live/admin/design_gallery_live.ex returns 0
  </acceptance_criteria>

  <done>Either no CSS edit (3 copies byte-coherent) or the single narrow sg-* fix is applied to source + re-synced to both generated copies byte-coherently; component golden suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template source → generated copies | A sg-* edit to source must be mirrored to the 2 generated copies or template drift breaks golden/install/Playwright tests; the only host-shipped change in this phase is presentational sg-* CSS |
| design_gallery board markup → example-only grading surface | The 11 board-mg-*/4 board-cfg-* boards live example-only (not in installer templates); they are a grading surface, not a host-shipped attack surface |

This phase touches only example-only gallery board markup and presentational sg-* CSS. There is
NO auth / session / token / PII surface in scope and NO new attack surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208-01 | Tampering | Generated-copy drift | medium | mitigate | Any source sg-* edit is propagated to all 3 copies immediately; verified by md5 equality + components_test.exs golden byte-match (D-03) |
| T-208-02 | Tampering | Token/value swap on a group sg-* rule | low | accept | Audit-confirmed expected zero edits; any fix is a read-only semantic mapping re-verified by admin-css-conformance.sh before close |
</threat_model>

<verification>
After both tasks complete:
  md5 of all 3 sigra_admin.css copies are identical
  bash scripts/ci/admin-css-conformance.sh                                   (exits 0)
  mix test test/sigra/admin/components_test.exs                              (exits 0)
  grep -v '^#' priv/templates/sigra.install/admin/sigra_admin.css | grep -c 'transition: all'   (returns 0)
  grep -c 'board-cfg-org' test/example/lib/example_web/live/admin/design_gallery_live.ex   (returns 0)
  git status --porcelain test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/   (empty)
</verification>

<success_criteria>
- All 11 board-mg-* groups + 4 board-cfg-* composites audited against the 6 L2 proxies; per-group findings in SUMMARY
- Each group's TRUE state-marker set recorded (mg-3 = N/A notes; mg-9/mg-11 = real states); MG-7/MG-8 isolated-only ruling recorded (D-06); content-equivalence mg-5/mg-6 only (D-07)
- Expected outcome: zero sg-* CSS gaps, no edit (cite-and-flip) — OR a single narrow fix, byte-coherent across 3 copies
- admin-css-conformance.sh exits 0; component golden suite exits 0
- "CSS edited: yes/no" verdict + (if yes) affected board-mg-* slug list recorded for Plan 02; no board-cfg-org introduced
</success_criteria>

<output>
Create .planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md when done
</output>
