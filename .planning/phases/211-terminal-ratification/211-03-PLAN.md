---
phase: 211
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [GATE-01, GATE-02]
must_haves:
  truths:
    - "`mix test` is clean for the terminal gate: the only failures are the accepted known/env set — the 3 Sigra.UpgradeIntegrationTest env-DB failures (test/upgrade_test.exs) — with NO new real regression beyond it (D-05)."
    - "Sigra.Audit.Forwarders.NoopTest passes 3/3 in isolation, confirming its parallel-shard log-capture failure (if it appears in the full run) is a shard race, not a deterministic branch regression (D-05)."
    - "The 204 D-08 stale-contract tests (Phase192KnownFailureContractTest / Phase148 Vaultr→Tasklane) are re-confirmed green — NOT re-fixed (D-05)."
  artifacts: []
  key_links:
    - "The terminal mix test gate is supporting evidence for BOTH GATE-01 (ledger/idempotency lock rests on a green suite) and GATE-02 (generated-host parity + the milestone audit assert no new real regression)."
  prohibitions:
    - "MUST NOT 'fix' the 3 Sigra.UpgradeIntegrationTest env-DB failures — they are accepted known env failures (D-05), not milestone blockers."
    - "MUST NOT re-fix the 204 D-08 stale-contract tests — they are already resolved on this branch; re-confirm green only (D-05)."
    - "MUST NOT apply a NoopTest determinism fix unless the isolation run proves it is trivial and risk-free; lean documented-known (Claude's Discretion / D-05)."
    - "MUST NOT change any product behavior or add new tests to 'improve' the suite (D-10) — this plan RUNS the suite and classifies failures."
---

<objective>
Run the terminal `mix test` gate and prove the suite is clean modulo the accepted known/env
failure set — no NEW real regression. This is the supporting-evidence leg the milestone audit
(Plan 04) rolls up and the honest baseline GATE-01/GATE-02 rest on. Direct analog of Phase 204's
"clean mix test with accepted env failures" discipline (204 D-09).

Purpose: D-05 defines the accepted known/env failures — (a) the 3 Sigra.UpgradeIntegrationTest
env-DB failures (need a live per-fixture DB) and (b) the NoopTest parallel-shard log-capture
flake (passes 3/3 in isolation). The terminal gate must confirm the suite is otherwise green and
assert no new regression exists beyond this known set, so the milestone can close honestly.

Output: mix test classified against the known set; NoopTest isolation confirmed; stale-contract
tests re-confirmed green; a clean-suite verdict for the audit to cite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/211-terminal-ratification/211-CONTEXT.md
@.planning/phases/211-terminal-ratification/211-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the NoopTest shard-flake is a race, not a regression (D-05, isolation)</name>
  <files></files>
  <read_first>
    - test/sigra/audit/forwarders/noop_test.exs (the "does NOT emit any Logger output — D-23" test; passes 3/3 in isolation per research §3)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§3 known-failure set; Claude's Discretion — lean documented-known)
  </read_first>
  <action>
    Run `MIX_ENV=test mix test test/sigra/audit/forwarders/noop_test.exs` in isolation and confirm
    3 tests, 0 failures (research captured this live). This proves the NoopTest failure — if it
    appears in the full parallel run — is a shard log-capture race, not a deterministic branch
    regression. Per Claude's Discretion (D-05), lean documented-known: do NOT apply an async:false
    / scoped-capture determinism fix UNLESS the isolation run plus a quick read shows the fix is
    trivial and risk-free; documenting it as a known shard flake satisfies the gate.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && source tmp/db.env 2>/dev/null; MIX_ENV=test mix test test/sigra/audit/forwarders/noop_test.exs; echo "noop_isolation_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/sigra/audit/forwarders/noop_test.exs` in isolation reports 3 tests, 0 failures.
    - The failure is classified as a parallel-shard log-capture race (documented-known), not a regression.
    - No determinism fix applied unless proven trivial/risk-free (`git status` clean for noop_test.exs at the expected outcome).
  </acceptance_criteria>
  <done>NoopTest is confirmed a shard race (3/3 in isolation); the flake is documented-known, not a blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm the 204 D-08 stale-contract tests green (D-05, no re-fix)</name>
  <files></files>
  <read_first>
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§3: the Phase192KnownFailureContractTest + Phase148 evaluator-funnel/first-run-dx tests re-confirmed green 4/4 live; do NOT re-fix)
  </read_first>
  <action>
    Re-confirm the 204 D-08 stale-contract tests pass on this branch (research captured 4 tests,
    0 failures live) — the Phase192 known-failure contract test and the Phase148 evaluator-funnel /
    first-run DX test (the Vaultr→Tasklane rename lock). These are already resolved on the branch;
    re-confirm green only — do NOT re-fix (D-05). Locate the exact test paths from research/the
    tree; run them together.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && source tmp/db.env 2>/dev/null; MIX_ENV=test mix test $(git ls-files 'test/**/*phase_192_known_failure_contract_test.exs' 'test/**/*phase_148*_test.exs'); echo "stale_contract_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The Phase192 known-failure contract test and the Phase148 test both pass (green).
    - No source or test file was edited to make them pass (`git status` clean — re-confirmation only).
  </acceptance_criteria>
  <done>The 204 D-08 stale-contract tests are re-confirmed green without re-fixing.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full mix test gate; classify against the accepted known/env set (D-05)</name>
  <files></files>
  <read_first>
    - test/upgrade_test.exs (~lines 205-218: the 3 Sigra.UpgradeIntegrationTest env-DB failures — seed_users!/2 / run_data_migrations! need a live per-fixture DB; accepted env failures, do NOT fix)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§3 accepted known/env set; Validation Architecture full-suite command)
    - CLAUDE.md (local DB prereqs — source tmp/db.env or localhost:5432 fallback; mix test requires live Postgres)
  </read_first>
  <action>
    Run the full `mix test` suite (MIX_ENV=test; source tmp/db.env or rely on the localhost:5432
    fallback per CLAUDE.md). Classify the result against the accepted known/env set (D-05):
      - ACCEPTED, not blockers: the 3 Sigra.UpgradeIntegrationTest env-DB failures (test/upgrade_test.exs)
        and, if it appears, the single NoopTest shard-log-capture flake (proven a race in Task 1).
    Assert NO NEW real regression exists beyond this known set — enumerate every failure the run
    reports and confirm each maps to the accepted set. If ANY failure falls outside the accepted
    set, that is a real regression: STOP and escalate it as a milestone blocker (do NOT silently
    accept or fix it here). Do NOT attempt to fix the accepted env-DB failures (D-05).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && source tmp/db.env 2>/dev/null; MIX_ENV=test mix test 2>&1 | tail -40; echo "full_suite_exit=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `mix test` completes; every reported failure maps to the accepted known/env set (3 UpgradeIntegrationTest env-DB failures + at most the NoopTest shard flake).
    - No new real regression exists outside the accepted set (if one did, it is escalated as a blocker, not accepted/fixed).
    - No product-behavior change and no test edits were made to green the suite (`git status` clean).
  </acceptance_criteria>
  <done>The terminal mix test gate is clean modulo the accepted known/env failures; no new regression — an honest green baseline for the audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan runs the existing ExUnit suite and classifies
failures — no new code paths, inputs, endpoints, schemas, or crypto.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-211-03 | Tampering | mix test suite classification | low | accept | Verification-only phase; the terminal green-suite gate is itself a regression tripwire. No ASVS category newly engaged. Threat model unchanged from v1.41 close. |
</threat_model>

<verification>
- `mix test test/sigra/audit/forwarders/noop_test.exs` isolation = 3/0 (shard race confirmed).
- The 204 D-08 stale-contract tests are green (re-confirmed, not re-fixed).
- Full `mix test` failures map ONLY to the accepted known/env set (3 UpgradeIntegrationTest env-DB + at most NoopTest shard flake); no new regression.
- `git status` clean — no product/test edits to green the suite.
</verification>

<success_criteria>
The terminal `mix test` gate is proven clean modulo the D-05 accepted known/env failures, with
the NoopTest flake confirmed a shard race and the stale-contract tests re-confirmed green — a
truthful suite baseline the milestone audit (Plan 04) can roll up for GATE-01/GATE-02.
</success_criteria>

<output>
Create `.planning/phases/211-terminal-ratification/211-03-SUMMARY.md` when done.
</output>
