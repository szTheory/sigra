---
phase: 211
plan: 05
type: execute
wave: 3
depends_on: [211-01, 211-02, 211-03, 211-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [GATE-01, GATE-02]
must_haves:
  truths:
    - "Phase 208 is marked Complete (its 208-03/GROUP-02 work was folded into and executed as Phase 210 plan 02) — the ROADMAP `2/3 In Progress` row and the `[ ]` phase entry are closed so gsd-complete-milestone does not flag an incomplete phase (D-08.1)."
    - "GATE-01 and GATE-02 are flipped `[ ]→[x]` in REQUIREMENTS.md (lines ~44-45) and their traceability coverage rows (lines ~86-87) read Complete/Satisfied (D-08.2)."
    - "STATE.md milestone_name is corrected from the stale `CI-Gate Remediation` to `ADMIN-DS-ELEVATION` (authoritative per PROJECT.md / ROADMAP / REQUIREMENTS) (D-08.3)."
    - "The ROADMAP v1.42 milestone status/progress lines are flipped from in-progress → complete (D-08.4); no v1.42 git tag is created (D-09)."
  artifacts: []
  key_links:
    - "This close-out reflects the proven evidence chain: GATE-01 (Plan 01), GATE-02 parity (Plan 02), clean mix test (Plan 03), and the adversarial audit (Plan 04) — the checkboxes flip ONLY because those gates actually passed."
  prohibitions:
    - "MUST NOT flip GATE-01/GATE-02 to satisfied unless Plans 01-04 gates are actually green (honest close, not rubber-stamp)."
    - "MUST NOT create a `v1.42` milestone git tag — milestone tags were dropped after v1.35 (D-09)."
    - "MUST NOT use a whole-file Write on ROADMAP.md / REQUIREMENTS.md / STATE.md — scoped Edit only (a whole-file Write destroys entries outside the diff window)."
    - "MUST NOT edit any roadmap/requirements/state line other than the D-08 close-out targets (no scope creep, D-10)."
---

<objective>
Perform the milestone-close housekeeping (D-08): mark Phase 208 complete (208-03 folded into
210-02), flip GATE-01/GATE-02 to satisfied, correct the stale STATE.md milestone_name, and flip
the ROADMAP v1.42 status/progress to complete — with NO git tag (D-09). Direct analog of Phase
204-05 Task 3. This is the genuinely terminal plan; it runs AFTER all gates are green (Plans
01-04).

Purpose: D-08 closes the milestone's loose ticks so the roadmap/requirements/state reflect proven
reality. Left as-is, `gsd-complete-milestone` would flag Phase 208 as incomplete and the stale
`CI-Gate Remediation` milestone_name would misreport the milestone. The GATE checkboxes flip only
because Plans 01-04 proved them.

Output: Phase 208 Complete; GATE-01/GATE-02 satisfied + coverage rows Complete; STATE.md
milestone_name corrected; ROADMAP v1.42 marked complete; no tag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/211-terminal-ratification/211-CONTEXT.md
@.planning/phases/211-terminal-ratification/211-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip GATE-01/GATE-02 satisfied in REQUIREMENTS.md (D-08.2)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (GATE-01 line ~44, GATE-02 line ~45 both `- [ ]`; traceability rows ~86-87 `| GATE-01 | Phase 211 | Pending |` / `| GATE-02 | Phase 211 | Pending |`)
    - .planning/phases/211-terminal-ratification/211-01-SUMMARY.md, 211-02-SUMMARY.md, 211-04-SUMMARY.md (confirm the gates actually passed before flipping)
  </read_first>
  <action>
    First confirm Plans 01/02/03/04 gates are green (read their SUMMARYs). Then, using scoped
    `Edit` only (never whole-file Write):
    1. Flip `- [ ] **GATE-01**:` → `- [x] **GATE-01**:` and `- [ ] **GATE-02**:` →
       `- [x] **GATE-02**:` (lines ~44-45).
    2. Update the traceability coverage rows `| GATE-01 | Phase 211 | Pending |` → `| ... | Complete |`
       and `| GATE-02 | Phase 211 | Pending |` → `| ... | Complete |` (lines ~86-87).
    Do not edit any other requirement line. Only flip because Plans 01-04 proved the gates.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -nE '\*\*GATE-0[12]\*\*' .planning/REQUIREMENTS.md; grep -nE '\| GATE-0[12] \|' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - GATE-01 and GATE-02 checkbox lines both read `- [x]` in REQUIREMENTS.md.
    - The traceability rows for GATE-01 and GATE-02 read `Complete` (not `Pending`).
    - No other requirement line changed (`git diff` scoped to those entries).
  </acceptance_criteria>
  <done>GATE-01/GATE-02 are marked satisfied in REQUIREMENTS.md, reflecting the proven gate evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Mark Phase 208 complete + flip v1.42 ROADMAP status/progress (D-08.1 / D-08.4)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (line 4 `**Status:** v1.42 ... in progress`; line 8 `🚧 **v1.42 ADMIN-DS-ELEVATION** ... (in progress)`; line 24 Phase 208 `- [ ]`; line 27 Phase 211 `- [ ]`; line 230 progress row `| 208. ... | v1.42 | 2/3 | In Progress|`; line 233 `| 211. ... | 0/? | Not started |`)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§5: exact current ROADMAP literals; 208-03/GROUP-02 folded into 210-02)
  </read_first>
  <action>
    Using scoped `Edit` only (never whole-file Write on ROADMAP.md), apply the D-08 close-out edits
    using the exact current literals from RESEARCH §5:
    1. Line 24: Phase 208 `- [ ]` → `- [x]` (208-03/GROUP-02 folded into and executed as 210-02;
       GROUP-02 already reads Complete in the REQUIREMENTS traceability table).
    2. Line 230 progress row `| 208. L2 Meta-Component Group Elevation | v1.42 | 2/3 | In Progress|  |`
       → mark Complete (3/3 or note the fold, per gsd-complete-milestone expectation) with a
       completion date.
    3. Line 27: Phase 211 `- [ ]` → `- [x]` and line 233 progress row `| 211. Terminal Ratification |
       v1.42 | 0/? | Not started | - |` → Complete with plan count + date.
    4. Line 4 `**Status:** v1.42 ADMIN-DS-ELEVATION in progress (phases 205-211). Hex: v1.1.0.` →
       complete, and line 8 `- 🚧 **v1.42 ADMIN-DS-ELEVATION** — Phases 205-211 (in progress)` →
       `- ✅ **v1.42 ADMIN-DS-ELEVATION** — Phases 205-211 (shipped <date>)` matching the existing
       shipped-milestone format (line 9 v1.41 is the template).
    Do not edit any other roadmap line. Use `gsd roadmap` subcommands if available for the
    structural progress-row mutations; otherwise scoped Edit.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -nE 'Phase 208:|Phase 211:' .planning/ROADMAP.md | head; grep -nE 'v1\.42 ADMIN-DS-ELEVATION' .planning/ROADMAP.md; grep -nE '\| 208\.|\| 211\.' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase 208 and Phase 211 roadmap entries both read `- [x]`.
    - The 208 progress row reads Complete (3/3 or folded-note) and the 211 progress row reads Complete with plan count + date.
    - The v1.42 milestone status (line 4) and milestone list entry (line 8) read complete/✅ (shipped), matching the existing shipped-milestone format.
    - No other roadmap line changed.
  </acceptance_criteria>
  <done>Phase 208 is marked complete (folded into 210-02), Phase 211 ticked, and the v1.42 milestone reads shipped in ROADMAP.md.</done>
</task>

<task type="auto">
  <name>Task 3: Correct STATE.md milestone_name → ADMIN-DS-ELEVATION (D-08.3); no git tag (D-09)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (line 4 `milestone_name: CI-Gate Remediation` — stale, inherited from inserted Phase 208.1)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§5: STATE.md:4 → ADMIN-DS-ELEVATION; authoritative per PROJECT.md:35 / ROADMAP:4/8 / REQUIREMENTS:1)
  </read_first>
  <action>
    Using a scoped `Edit` (never whole-file Write on STATE.md), correct the frontmatter
    `milestone_name: CI-Gate Remediation` → `milestone_name: ADMIN-DS-ELEVATION` (the authoritative
    name per PROJECT.md / ROADMAP / REQUIREMENTS). Change only this line. Do NOT create any `v1.42`
    git tag (D-09) — milestone close is the ROADMAP/REQUIREMENTS/STATE edits + the audit doc, not a
    tag; verify no v1.42 tag was created.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -n 'milestone_name:' .planning/STATE.md; ! git tag --list 'v1.42' | grep -q . && echo "no_v1.42_tag_ok"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter reads `milestone_name: ADMIN-DS-ELEVATION` (no `CI-Gate Remediation` remaining).
    - Only that line changed in STATE.md (`git diff` scoped).
    - No `v1.42` git tag exists (D-09).
  </acceptance_criteria>
  <done>STATE.md milestone_name is corrected to ADMIN-DS-ELEVATION and no milestone git tag was created; the milestone close is honest and tag-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan edits planning docs (REQUIREMENTS / ROADMAP /
STATE) — no new code paths, inputs, endpoints, schemas, or crypto; no git tag.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-211-05 | Repudiation | milestone-close doc edits | low | accept | Close-out flips checkboxes ONLY on proven gate evidence (Plans 01-04 green), keeping the record honest; scoped Edit only, no other lines touched. No ASVS category newly engaged. Threat model unchanged from v1.41 close. |
</threat_model>

<verification>
- GATE-01/GATE-02 are `- [x]` + `Complete` in REQUIREMENTS.md.
- Phase 208 and Phase 211 are `- [x]` in ROADMAP.md; 208 progress row Complete; v1.42 milestone reads shipped/✅.
- STATE.md `milestone_name: ADMIN-DS-ELEVATION`.
- No `v1.42` git tag created (D-09).
- All edits scoped-Edit only; no unrelated roadmap/requirements/state lines changed.
</verification>

<success_criteria>
Milestone-close housekeeping is complete and honest: Phase 208 closed (folded into 210-02),
GATE-01/GATE-02 satisfied on proven evidence, STATE.md milestone_name corrected, and the v1.42
ROADMAP status flipped to shipped — with no git tag. The milestone is auditable and closed.
</success_criteria>

<output>
Create `.planning/phases/211-terminal-ratification/211-05-SUMMARY.md` when done.
</output>
