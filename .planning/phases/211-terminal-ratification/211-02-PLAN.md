---
phase: 211
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [GATE-02]
user_setup:
  - service: phx_new-archive
    why: "Install-golden byte-diff + admin-acceptance smoke must run against the CI-pinned generator (SEED-004); phx_new 1.8.8 is currently installed and injects a root_tag_attribute config block → spurious byte-diff."
    dashboard_config:
      - task: "Install the pinned generator: `mix archive.install --force hex phx_new 1.8.7`. Do NOT regenerate the golden fixture to absorb a 1.8.8 diff."
        location: "mix archive.install --force hex phx_new 1.8.7"
must_haves:
  truths:
    - "The local generator archive is phx_new 1.8.7 (the CI pin) before the golden/parity lane runs (D-04 / SEED-004)."
    - "The install-golden byte-diff suite (test/sigra/install/golden_diff_test.exs) is green against the phx_new 1.8.7 pin — installer↔example byte-parity holds; generated host matches the committed fixture byte-for-byte."
    - "scripts/ci/admin-acceptance-smoke.sh scaffolds a fresh phx_new 1.8.7 host, applies mix sigra.install, boots on PORT=4017, and its Playwright probes render the elevated, styled (sg-* applied) admin UI — generated-host runtime parity proven (D-04)."
  artifacts: []
  key_links:
    - "Generated-host parity connects the example proof to a freshly generated phx_new 1.8.7 host via the install-golden fixture + admin-acceptance-smoke.sh; the styled admin it renders must reflect the elevated v1.42 Tier-2 bar."
    - "If the smoke surfaces real installer-template-drift (an elevated sg-* selector shipped in test/example/ but not propagated to priv/templates/sigra.install/), that is a FIX commit in this plan (D-04), not just a verification tick — the milestone cannot close honestly otherwise."
  prohibitions:
    - "MUST NOT regenerate the install-golden fixture to silence a 1.8.8 root_tag_attribute byte-diff — install phx_new 1.8.7 instead (SEED-004 / CLAUDE.md)."
    - "MUST NOT modify installer templates or the golden fixture as a 'fix' for environmental archive drift; template edits are legitimate ONLY to propagate a real elevated sg-* selector that shipped to test/example but not to priv/templates/sigra.install."
    - "MUST NOT build any new parity harness or gate script (D-04, D-10) — run the existing admin-acceptance-smoke.sh and golden_diff_test.exs."
---

<objective>
Prove GATE-02 generated-host parity two ways: install-golden byte-diff green against the phx_new
1.8.7 pin, AND `admin-acceptance-smoke.sh` rendering the elevated, styled admin UI on a freshly
generated phx_new 1.8.7 host. This confirms the milestone's elevated v1.42 Tier-2 admin bar
survives the lib→generator boundary onto a real new Phoenix app. Direct analog of Phase 204-04.

Purpose: GATE-02 requires "installer↔example byte-parity + golden fixture stay green + a fresh
phx.new + mix sigra.install + admin-acceptance smoke renders the elevated styled admin" — the
elevated admin is library-owned but rendered through generated host scaffolding, so a fresh host
boot-and-render is the first-class runtime proof. Research flagged a BLOCKER: phx_new 1.8.8 is
installed, not the pinned 1.8.7 — the golden byte-diff will spuriously fail until 1.8.7 is
installed (SEED-004).

Output: phx_new 1.8.7 installed; byte-diff green; acceptance smoke exit 0 rendering the styled
elevated admin; any real installer-template drift fixed (not masked).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/211-terminal-ratification/211-CONTEXT.md
@.planning/phases/211-terminal-ratification/211-RESEARCH.md
@scripts/ci/admin-acceptance-smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight the phx_new 1.8.7 pin + install-golden byte-diff green (GATE-02 byte-parity, D-04 / SEED-004)</name>
  <files></files>
  <read_first>
    - CLAUDE.md (phx_new 1.8.7 pin: a newer archive injects a `config :phoenix_live_view, root_tag_attribute: …` block → spurious byte-diff; install 1.8.7, do NOT regenerate the fixture — SEED-004)
    - test/sigra/install/golden_diff_test.exs (the install golden byte-diff suite)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§4: 1.8.8 currently installed — the BLOCKER; §"Pitfall 2")
    - reference: installer template drift (priv/templates/sigra.install/ vs hand-maintained test/example/)
  </read_first>
  <action>
    PRE-FLIGHT the generator pin FIRST: run `mix archive.install --force hex phx_new 1.8.7` (research
    confirmed 1.8.8 is currently installed, which injects a root_tag_attribute config block and
    produces a spurious byte-diff). Confirm `mix archive | grep phx_new` reports phx_new-1.8.7.

    Then run the install golden byte-diff suite (`test/sigra/install/golden_diff_test.exs` plus the
    sibling `mix sigra.install` fixture tests) and confirm green — installer↔example byte-parity.
    If it fails, FIRST re-verify the archive is exactly 1.8.7 (the most common cause of a spurious
    diff) before treating any diff as a real regression. Do NOT regenerate the golden fixture to
    absorb archive drift (SEED-004).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix archive.install --force hex phx_new 1.8.7 >/tmp/211-archive.log 2>&1; mix archive | grep -i phx_new; source tmp/db.env 2>/dev/null; MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs; echo "golden_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `mix archive | grep phx_new` reports `phx_new-1.8.7` (the CI pin).
    - `MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs` exits 0 (byte-diff green).
    - No installer template or golden fixture was modified to absorb archive drift (`git status` clean for priv/templates and the fixture).
  </acceptance_criteria>
  <done>The generator is pinned to phx_new 1.8.7 and the install-golden byte-diff is green; the lib→generator boundary is byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: admin-acceptance-smoke renders the elevated styled admin on a fresh host (GATE-02 runtime parity, D-04)</name>
  <files></files>
  <read_first>
    - scripts/ci/admin-acceptance-smoke.sh (scaffolds phx.new + sigra.install + seed + boot + Playwright; PORT=4017; admin-generated.spec.ts; the runtime generated-host parity proof — RUN_PARITY / DIST-06)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§"GATE-02 legs" — run AFTER installing phx_new 1.8.7; expect "success")
    - reference: generated-host acceptance smoke; installer template drift (the fix-commit path if a real drift surfaces)
    - CLAUDE.md (local DB prereqs; the smoke runs its own scaffolded host on PORT=4017)
  </read_first>
  <action>
    Run the generated-host runtime parity proof: `GITHUB_WORKSPACE=$(pwd) bash
    scripts/ci/admin-acceptance-smoke.sh`. Confirm it scaffolds a fresh phx_new 1.8.7 host, applies
    `mix sigra.install`, migrates + seeds, boots on PORT=4017, and the `admin-generated.spec.ts`
    Playwright probes render the elevated, STYLED admin UI (sg-* design system applied — not
    unstyled). Treat a non-zero exit as a real parity failure.

    IF the smoke surfaces a real installer-template-drift regression — an elevated sg-* selector
    that shipped in test/example/ but was not propagated to priv/templates/sigra.install/ (the known
    drift pattern) — that is a FIX commit in this plan, not just a verification tick (D-04): diff the
    template against the example, propagate the missing selector to priv/templates/sigra.install/,
    re-run the smoke to green, and (if the golden fixture legitimately changes as a result of the
    template propagation) confirm golden_diff_test.exs still passes under phx_new 1.8.7. Do NOT edit
    templates to mask an ENV archive-drift diff (that is Task 1's phx_new pin, not a template fix).

    If a stale sigra_admin_smoke_* DB on port 5432 causes the first smoke ecto.migrate to fail
    (204-04 hit this), drop it and re-run (RESEARCH Pitfall 3 / 204-04 note).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && GITHUB_WORKSPACE=$(pwd) bash scripts/ci/admin-acceptance-smoke.sh; echo "smoke_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/admin-acceptance-smoke.sh` exits 0 (fresh host scaffolds, installs, migrates, seeds, boots, renders styled admin).
    - The admin-generated.spec.ts probes confirm the admin surface is STYLED (sg-* applied), not unstyled, on the generated host.
    - If real installer-template drift surfaced, the missing selector was propagated to priv/templates/sigra.install/ (a genuine fix commit) and the smoke re-ran green; golden_diff_test.exs still passes under phx_new 1.8.7. Otherwise `git status` is clean for priv/templates.
  </acceptance_criteria>
  <done>Generated-host runtime parity is proven: a freshly generated phx_new 1.8.7 host renders the elevated, styled v1.42 admin UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan pins an existing generator archive and runs the
existing install-golden byte-diff + admin-acceptance-smoke harness on a scaffolded host — no new
code paths, inputs, endpoints, schemas, or crypto. The one legitimate code edit (propagating a
real elevated sg-* selector from test/example to priv/templates/sigra.install if drift surfaces)
is a template-parity fix, not a new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-211-02 | Tampering | phx_new archive install / installer templates | low | accept | Verification/parity phase; the golden byte-diff itself is the tamper-detection guard (any installer-template change is caught byte-for-byte). No ASVS category newly engaged. Threat model unchanged from v1.41 close. |
</threat_model>

<verification>
- `mix archive | grep phx_new` reports phx_new-1.8.7; `mix test test/sigra/install/golden_diff_test.exs` exits 0.
- `scripts/ci/admin-acceptance-smoke.sh` exits 0 and renders the styled elevated admin on a fresh host.
- No installer template / golden fixture edits made to mask archive drift; any template edit is a genuine drift-propagation fix (re-verified green).
</verification>

<success_criteria>
GATE-02 generated-host parity is proven both byte-wise (install-golden vs phx_new 1.8.7) and at
runtime (admin-acceptance-smoke renders the elevated styled admin on a fresh host), with the
phx_new pin corrected up-front so the byte-diff is honest and any real installer-template drift
fixed rather than masked.
</success_criteria>

<output>
Create `.planning/phases/211-terminal-ratification/211-02-SUMMARY.md` when done.
</output>
