---
phase: 211
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "Every tier cell in guides/reference/admin-quality-ledger.md reads bare `2` (no decorators) — 36 cells; `scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 (forward-only lock holds). GATE-01 leg A."
    - "In-phase terminal idempotency is proven vs HEAD: `scripts/ci/snapshot-canary-guard.sh --base HEAD` PASSES with 0 changed slugs on BOTH the checkpoint lane (canary impersonation-banner) and the design lane (canary board-notice). GATE-01 leg B (D-02a mechanism ii)."
    - "Compare-mode re-render across the checkpoint Playwright projects shows zero PNG drift vs HEAD (idempotency proven); the design lane (board-*) is proven compare-mode only, never re-recorded on darwin. GATE-01 leg C."
    - "Both allowlists (snapshot-allowlist, snapshot-allowlist-design) are empty at steady state (comments only, zero non-comment lines)."
  artifacts: []
  key_links:
    - "The monotonic guard (quality-ledger-monotonic.sh) diffs the committed ledger table vs origin/main; the canary guard (snapshot-canary-guard.sh --base HEAD) diffs committed PNG bytes vs HEAD — both are the merge-blocking gates that lock the terminal state."
    - "The origin/main canary reconciliation (5 non-canary checkpoint slugs + the WCAG-fixed impersonation-banner canary drift vs the 368-commit-stale origin/main) is DEFERRED to the integration merge (PR #63) per D-02a mechanism (ii); this plan documents the hand-off, it does not push to origin/main."
  prohibitions:
    - "MUST NOT flip any ledger cell, author any new Tier-2 ratchet, or edit guides/reference/admin-quality-ledger.md tier digits (D-01, D-10) — this plan VERIFIES the terminal all-`2` state, it does not mutate it."
    - "MUST NOT re-record admin-design (board-*) PNG baselines on darwin — they are CI-native/ubuntu; a local --update-snapshots injects spurious 1px diffs. Prove the design lane compare-mode only."
    - "MUST NOT revert the impersonation-banner canary to origin/main's pre-fix bytes (re-opens the 204-03 WCAG AA `.vt-status-pill` contrast failure)."
    - "MUST NOT open a ci.yml:1852-style human-visual recapture PR, and MUST NOT push canary/checkpoint bytes to origin/main from this phase (D-02) — origin/main reconciliation is the integration merge's job."
    - "MUST NOT add any slug to either allowlist unless a real compare-mode CHECKPOINT diff surfaces; if one does, recapture that one slug in the same commit and reset the allowlist to empty (204 D-05 same-commit discipline)."
---

<objective>
Prove and lock GATE-01: the terminal all-`2` quality ledger with its forward-only monotonic
lock, plus in-phase snapshot/canary idempotency vs HEAD, with both allowlists empty and both
canaries byte-stable. This is the direct analog of Phase 204's ledger-lock + compare-mode
zero-drift legs, using D-02a mechanism (ii): prove vs HEAD in-phase, defer the origin/main
canary reconciliation to the integration merge (PR #63).

Purpose: GATE-01 is the "every ledger cell reads Tier-2 + monotonic guard exit 0 + baselines
recaptured with allowlists empty + canaries byte-stable + compare-mode zero PNG drift" gate.
Research confirmed the terminal state is already correct on-branch (36 cells bare `2`; ledger
guard exit 0; `--base HEAD` canary guard PASS 0-drift live). This plan re-proves that state as
the ratification lock — it is a verification pass, not a fresh capture.

Output: All GATE-01 legs green in-phase; allowlists empty; the origin/main hand-off documented
in the SUMMARY for the integration merge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/211-terminal-ratification/211-CONTEXT.md
@.planning/phases/211-terminal-ratification/211-RESEARCH.md
@scripts/ci/quality-ledger-monotonic.sh
@scripts/ci/snapshot-canary-guard.sh
@scripts/ci/snapshot-recapture-gate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify terminal all-`2` ledger + forward-only monotonic lock (GATE-01 leg A, D-01)</name>
  <files></files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (the terminal ledger; every L0/L1/L2/L3/L4 tier cell must read bare `2` — Phase 210 flipped the last 15; verify, do NOT flip)
    - scripts/ci/quality-ledger-monotonic.sh (the --base origin/main forward-only guard; parses the ledger table and fails on any tier decrease; ~36 cells)
  </read_first>
  <action>
    VERIFY the terminal ledger state — do NOT edit any tier digit (D-01, D-10). Run
    `scripts/ci/quality-ledger-monotonic.sh --base origin/main` and confirm exit 0 (research
    captured PASS, 36 cells, live this session). Independently confirm every tier cell in
    guides/reference/admin-quality-ledger.md reads bare `2` with no decorators — grep the tier
    column for any non-`2` tier value and confirm none remain, filtering out comment/legend lines
    (strip leading-`#` and prose rows) so the scale description is not miscounted. If the guard
    exits non-zero or any cell is not bare `2`, that is a terminal-state finding to escalate, NOT
    something to fix by flipping cells in this plan.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main; echo "monotonic_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0.
    - Every tier cell in guides/reference/admin-quality-ledger.md reads bare `2` (no decorators); no non-`2` tier cell remains.
    - `git status` shows guides/reference/admin-quality-ledger.md UNMODIFIED by this plan (verification only).
  </acceptance_criteria>
  <done>The terminal all-`2` ledger and its forward-only lock are verified; GATE-01 leg A holds with no cell flipped.</done>
</task>

<task type="auto">
  <name>Task 2: Prove snapshot/canary idempotency vs HEAD on both lanes; confirm allowlists empty (GATE-01 leg B, D-02a ii / D-03)</name>
  <files></files>
  <read_first>
    - scripts/ci/snapshot-canary-guard.sh (drift computation; CANARY=impersonation-banner (checkpoint lane, line ~20), board-notice (design lane); canary-modify hard-forbidden at line ~104 → exit 1; `added` tolerated as first-established; --base selects the diff ref)
    - test/example/priv/playwright/snapshot-allowlist (checkpoint allowlist — must be comments only)
    - test/example/priv/playwright/snapshot-allowlist-design (design allowlist — must be comments only)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§2 Ground-Truth: --base HEAD PASSES 0-drift on both lanes; the 5-slug origin/main drift incl. user-sessions `added` is deferred to the integration merge)
  </read_first>
  <action>
    Prove in-phase terminal idempotency vs HEAD on BOTH snapshot lanes (research captured both as
    PASS 0-drift live). Run the checkpoint lane `snapshot-canary-guard.sh --base HEAD` and confirm
    PASS with 0 changed slugs. Then run the design lane against the design snapshot dir with its
    board-notice canary + design allowlist (the SNAP_DIR / --allowlist / --canary invocation form
    shown in RESEARCH §"D-02a Command Sequence" step 2) and confirm PASS with 0 changed slugs.

    Then confirm both allowlists are at steady-state empty: strip comment and blank lines from each
    allowlist file and confirm zero non-comment lines in each.

    Do NOT run `--base origin/main` as a gate here — per D-02a mechanism (ii) the origin/main
    canary FAIL (5 stale non-canary checkpoint slugs incl. `user-sessions` `added`, plus the
    WCAG-fixed impersonation-banner canary vs the 368-commit-stale origin/main) is a publish-time
    reconciliation owned by the integration merge (PR #63), not this phase. Do NOT push to
    origin/main; do NOT revert the canary; do NOT allowlist the canary. Record the origin/main
    hand-off in the SUMMARY so the milestone close reads honestly (RESEARCH A1 / Open Question 1).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/snapshot-canary-guard.sh --base HEAD; echo "checkpoint_head_exit=$?"; SNAP_DIR="test/example/priv/playwright/tests/admin-design.spec.ts-snapshots" bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice; echo "design_head_exit=$?"; printf 'checkpoint_allowlist_nonComment='; grep -vcE '^[[:space:]]*#|^[[:space:]]*$' test/example/priv/playwright/snapshot-allowlist; printf 'design_allowlist_nonComment='; grep -vcE '^[[:space:]]*#|^[[:space:]]*$' test/example/priv/playwright/snapshot-allowlist-design</automated>
  </verify>
  <acceptance_criteria>
    - `snapshot-canary-guard.sh --base HEAD` (checkpoint lane) exits 0 with 0 changed slugs.
    - The design-lane `--base HEAD` guard (SNAP_DIR=admin-design snapshots, --canary board-notice) exits 0 with 0 changed slugs.
    - Both allowlists report 0 non-comment lines (empty at steady state).
    - No PNG baseline was modified and no allowlist slug was added by this task (`git status` clean for test/example/priv/playwright snapshots + allowlists).
  </acceptance_criteria>
  <done>In-phase snapshot/canary idempotency is proven vs HEAD on both lanes with both allowlists empty and both canaries byte-stable; the origin/main reconciliation is documented as the integration-merge hand-off.</done>
</task>

<task type="auto">
  <name>Task 3: Compare-mode zero PNG drift across the checkpoint Playwright projects (GATE-01 leg C, D-03)</name>
  <files></files>
  <read_first>
    - scripts/ci/snapshot-recapture-gate.sh (the automated recapture/approval path; requires ≥1 slug arg at :20-23; PORT default 4011; runs the Playwright projects in compare mode + the drift/canary guards; RUN_PARITY toggles the parity lane)
    - .planning/phases/211-terminal-ratification/211-RESEARCH.md (§"D-02a Command Sequence" steps 0/3 — boot on PORT=4011, pre-compile, SIGRA_EXAMPLE_URL; at steady state there is no intended slug so run the compare-mode Playwright projects directly; NEVER --update-snapshots board-* on darwin)
    - CLAUDE.md (local DB prereqs: source tmp/db.env or fall back to localhost:5432; example dev server on alt PORT to avoid the 4000 Rulestead-Docker collision)
  </read_first>
  <action>
    Boot the example dev server for compare-mode capture per RESEARCH §"D-02a Command Sequence"
    step 0: source tmp/db.env (or fall back to localhost:5432), `MIX_ENV=dev mix compile` FIRST to
    avoid the first-request code-reload crash, then start the server on PORT=4011 with
    SIGRA_EXAMPLE_URL=http://localhost:4011; wait until the URL answers before proceeding.

    Run the admin-checkpoint Playwright projects (chromium / mobile / dark) in COMPARE mode and
    confirm zero PNG drift — the branch is already terminal-idempotent, so this is a compare
    confirmation, not a fresh capture. Because `snapshot-recapture-gate.sh` requires ≥1 slug arg
    (:20-23) and at steady state there is NO intended slug, prove zero-drift by running the
    checkpoint Playwright compare projects directly (the invocations the gate uses internally) plus
    the two `--base HEAD` canary guards from Task 2 — do NOT pass a phantom slug.

    Prove the admin-design lane in COMPARE mode ONLY; NEVER run `--update-snapshots` on board-*
    PNGs on darwin (they are CI-native/ubuntu; local recapture injects spurious 1px diffs).

    If (and only if) a real compare-mode diff surfaces on the CHECKPOINT lane, that is an intended
    recapture finding: `--update-snapshots` that one project, restore any incidental non-intended
    PNGs, run `snapshot-recapture-gate.sh <slug>` with the slug in the SAME diff, then reset the
    allowlist to empty (204 D-05 same-commit discipline). At the expected steady state no such diff
    appears and no PNG changes.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && source tmp/db.env 2>/dev/null; MIX_ENV=dev mix compile >/tmp/211-compile.log 2>&1 && echo "compile_ok"; (PORT=4011 PHX_SERVER=true MIX_ENV=dev mix phx.server >/tmp/211-example-4011.log 2>&1 &) ; for i in $(seq 1 30); do curl -fsS http://localhost:4011/ >/dev/null 2>&1 && break; sleep 2; done; curl -fsS -o /dev/null -w "http_status=%{http_code}\n" http://localhost:4011/ ; SIGRA_EXAMPLE_URL=http://localhost:4011 npm --prefix test/example/priv/playwright test -- --project=admin-checkpoints --project=admin-checkpoints-mobile --project=admin-checkpoints-dark; echo "checkpoint_compare_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The example dev server boots on PORT=4011 (pre-compiled) and answers HTTP 200 at http://localhost:4011/.
    - The three admin-checkpoint Playwright projects (chromium/mobile/dark) pass in compare mode with zero PNG drift.
    - No board-* (admin-design) PNG was re-recorded on darwin; the design lane is proven compare-mode only.
    - `git status` shows zero modified PNG baselines and both allowlists still empty (no slug added) at the expected steady state.
  </acceptance_criteria>
  <done>Compare-mode re-render shows zero PNG drift across the checkpoint projects (idempotency proven); the design lane is compare-mode-verified; GATE-01 leg C holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan runs existing CI guard scripts against committed
artifacts (ledger markdown + PNG baselines) and reads git diffs — no new code paths, inputs,
endpoints, schemas, or crypto.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-211-01 | Tampering | admin-quality-ledger.md / PNG baselines | low | accept | Verification-only phase; no new attack surface. The one security-relevant invariant — preserving the 204-03 WCAG AA `.vt-status-pill` contrast fix — is protected by the canary hard-forbid (never revert the canary); no ASVS category newly engaged. Threat model unchanged from v1.41 close. |
</threat_model>

<verification>
- `scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 (36 cells, forward-only lock).
- `scripts/ci/snapshot-canary-guard.sh --base HEAD` exits 0 on BOTH the checkpoint (impersonation-banner) and design (board-notice) lanes — 0 changed slugs.
- The three admin-checkpoint Playwright projects pass compare-mode with zero PNG drift; board-* proven compare-mode only (never re-recorded on darwin).
- Both allowlists report 0 non-comment lines; no PNG baseline modified; no canary reverted.
- The origin/main canary reconciliation (5 stale checkpoint slugs incl. user-sessions + WCAG-fixed canary vs the 368-commit-stale origin/main) is documented in the SUMMARY as the integration-merge (PR #63) hand-off — NOT pushed from this phase.
</verification>

<success_criteria>
GATE-01 is proven and locked in-phase: terminal all-`2` ledger + monotonic guard exit 0, HEAD
idempotency 0-drift on both canary lanes, checkpoint compare-mode zero drift, both allowlists
empty, both canaries byte-stable — with the origin/main reconciliation cleanly deferred to the
integration merge per D-02a mechanism (ii). No cell flipped, no canary reverted, no push to
origin/main.
</success_criteria>

<output>
Create `.planning/phases/211-terminal-ratification/211-01-SUMMARY.md` when done.
</output>
