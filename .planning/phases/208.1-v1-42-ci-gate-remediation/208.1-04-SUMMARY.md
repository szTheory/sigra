---
phase: 208.1-v1-42-ci-gate-remediation
plan: "04"
subsystem: ci-native-baseline-capture
tags: [playwright, ci-native-baselines, ci-gate, integration-pr, cite-and-flip]
dependency_graph:
  requires: [208.1-01, 208.1-02, 208.1-03]
  provides: [208.1-S3-baseline-capture, 208.1-S5-ci-arbiter-ship]
  affects: [example-playwright-smoke-ci-gate]
tech_stack:
  added: []
  patterns: [ci-native-ubuntu-recapture, integration-pr-as-ci-arbiter]
key_files:
  created:
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-audit-admin-design-{chromium,dark,mobile}.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-overview-admin-design-{chromium,dark,mobile}.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-user-detail-admin-design-{chromium,dark,mobile}.png
    - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/board-cfg-users-list-admin-design-{chromium,dark,mobile}.png
  modified:
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts
    - test/example/test/example/personas_test.exs
    - test/example/test/example_web/controllers/page_controller_test.exs
    - test/example/test/example_web/controllers/impersonation_controller_test.exs
    - test/mix/tasks/sigra.install_test.exs
    - test/sigra/templates/session_templates_test.exs
    - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/controllers/auth/sudo_html.ex
decisions:
  - "Trigger branch was STALE (forked pre-208.1, missing all 17 Wave 1/2/3 commits incl. the 2 design-lane fixes); orchestrator rebuilt wip/recapture-trigger = current main + cherry-picked guard-relax before dispatch, averting the run-28401914980 zero-baseline failure mode the executor's cite-and-flip precondition check missed"
  - "Required check Example Playwright smoke only runs on push:[main] (PR-protected) or pull_request:[main] — workflow_dispatch is blocked by release_ref_guard. The phase exit criterion was therefore met by opening the v1.42 integration PR (#63, ship/v1.42-ci-gate-remediation -> main, draft), NOT a throwaway dispatch"
  - "Contaminated auto-PR #62 (carried the TEMP guard-relax + missing fixes) closed + branch deleted; 12 board-cfg PNGs cherry-picked onto main clean (no relax reaches main)"
  - "6 genuine ExUnit regressions hidden by the never-CI-validated debt surfaced by the first real CI run and fixed (personas 9->10 zoe, impersonation 302, stale admin_shell metrics, admin_user_filters 3-chip, MAINTAINING.md doc ref, admin-checkpoints mobile 32767px crash)"
  - "Plan 02 sudo-copy change was incomplete on the library side (3 assertions + golden fixture asserted old copy); completed here (cbe0b928), turning Library shard 1 + Install golden green"
  - "Two remaining PR reds are NOT 208.1: Fast checks snapshot-canary drift (cumulative Phases 200-204 backlog) and Library shard-2 NoopTest log-capture flake — both filed as v1.42 milestone-integration debt"
metrics:
  duration: "~3h (incl. CI wall-clock)"
  completed: "2026-06-30"
  tasks_completed: 4
  tasks_total: 4
  files_modified: 24
status: complete
---

# Phase 208.1 Plan 04: CI-Native Baseline Capture + CI-Arbiter Ship Summary

Captured the 12 net-new `board-cfg-*` baseline PNGs CI-native (ubuntu) and drove the
real required check `Example Playwright smoke (full lifecycle)` to **GREEN** on the v1.42
integration PR — the phase exit criterion, explicitly not local green. The first real CI
run on the backlog surfaced (and this plan fixed) regressions that the cite-and-flip debt
had hidden.

## Tasks Completed

| Task | Name | Key commits |
|------|------|-------------|
| CP | Human-verify: design lane behaviorally green | (gated — approved after stale-branch rebuild) |
| 1 | CI-native recapture of 12 board-cfg baselines | `e0f432d8` |
| 1b | Fix 6 never-CI-validated ExUnit regressions + admin-checkpoints mobile crash | `2ef6cfd6`, `24ee68c8` |
| 1c | Complete Plan 02 sudo-copy reconciliation (library tests + golden fixture) | `cbe0b928` |
| 2 | Drive `Example Playwright smoke (full lifecycle)` GREEN on CI | run `28451828047` ✅ |
| 3 | Cleanup: close contaminated PR #62, delete `wip/recapture-trigger` | (branch + relax never reach main) |

## Exit Criterion — VERIFIED

`Example Playwright smoke (full lifecycle)` = **success** on PR #63 run `28451828047`
(headSha `cbe0b928`), all 5 seams green (admin_behavior, admin_checkpoints, design_gallery,
non_admin, demo_showcase), admin_checkpoints not regressed. Verified directly against the
live CI run — not a code re-read.

## Deviations & Notes

- **Stale trigger branch (critical, caught pre-dispatch):** `origin/wip/recapture-trigger`
  was forked from `6cbcf6d5` (pre-208.1) and missing all 17 Wave 1/2/3 commits. Dispatching
  the recapture against it would have reproduced the run-28401914980 zero-baseline failure.
  Orchestrator rebuilt it (= current `main` + guard-relax) and force-pushed before dispatch.
- **PR as the CI arbiter:** workflow_dispatch can't run the required check (release_ref_guard
  blocks non-tag dispatch), so a draft integration PR (#63) was the only path — and is the
  correct one per S5 ("explicitly NOT local green").
- **Out-of-scope reds deferred (milestone debt):** Fast checks (snapshot-canary drift from
  Phases 200-204 + the impersonation-banner canary-policy tension) and Library shard-2
  (`NoopTest` log-capture flake) are pre-208.1 backlog issues, filed in
  `.planning/todos/pending/2026-06-30-v142-integration-snapshot-canary-drift.md` for a
  dedicated v1.42 integration gate.

## Self-Check: PASSED

Phase 208.1 goal achieved: the never-CI-validated admin Playwright gate is GREEN on CI.
