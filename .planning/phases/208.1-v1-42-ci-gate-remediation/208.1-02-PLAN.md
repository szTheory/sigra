---
phase: 208.1-v1-42-ci-gate-remediation
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/admin-user-operations.spec.ts
  - test/example/priv/playwright/tests/impersonation.spec.ts
  - test/example/priv/playwright/tests/admin-audit.spec.ts
  - test/example/priv/playwright/tests/admin-modal-interaction.spec.ts
autonomous: true
requirements:
  - 208.1-S2-behavioral-spec-reconciliation
user_setup: []

must_haves:
  truths:
    - "admin-user-operations.spec.ts:73, impersonation.spec.ts:89, admin-audit.spec.ts:77, admin-modal-interaction.spec.ts:68 pass on --project=chromium"
    - "Dual-DOM 'No active sessions' / 'Passkeys' strict-mode-violation assertions resolve exactly 1 visible element"
    - "Stale sudo heading assertion reconciled to the rendered 'Re-enter your password' copy"
    - "Audit 'Action prefix' textbox is targeted after opening the 'More filters' <details> disclosure"
    - "admin_checkpoints lane (the clean reference) is NOT touched and stays green"
  artifacts:
    - "Reconciled admin_behavior spec files using the adminUsersEmailLocator dual-DOM scoping idiom"
  key_links:
    - "[desktop],[mobile] locator → .filter({hasText}).filter({visible:true}).first() resolves the single visible variant (helpers/adminUsersIndex.ts idiom)"
    - "page.getByText('More filters').click() opens the collapsed audit disclosure before targeting the textbox"
---

<objective>
Reconcile the 4 admin_behavior Playwright specs broken by the v1.36+ admin redesign's desktop+mobile dual-DOM render and by Phase-202 / demo-branding copy drift. These are selector-scoping and stale-text fixes — NOT UI changes. The repo already contains the canonical fix idiom (`helpers/adminUsersIndex.ts`) and the clean `admin_checkpoints` lane proves it.

Purpose: The recurring `strict mode violation: ... resolved to 2 elements` signature ("No active sessions" ×2, "Passkeys" ×2) is the dual-DOM render the redesign introduced; older Phase-31 specs use unscoped `getByText`. Plus 2 stale-text fixes (sudo heading, audit disclosure).
Output: 4 reconciled spec files, each assertion scoped to the visible variant or reconciled to the rendered copy; the clean admin_checkpoints lane untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-VALIDATION.md

# The canonical idiom + clean reference + failing specs
@test/example/priv/playwright/helpers/adminUsersIndex.ts
@test/example/priv/playwright/tests/admin-checkpoints.spec.ts
@test/example/priv/playwright/tests/admin-user-operations.spec.ts
@test/example/priv/playwright/tests/impersonation.spec.ts
@test/example/priv/playwright/tests/admin-audit.spec.ts
@test/example/priv/playwright/tests/admin-modal-interaction.spec.ts
@lib/sigra/admin/live/user_show_live.ex
@lib/sigra/admin/live/user_sessions_live.ex
@lib/sigra/admin/live/audit_index_live.ex
@test/example/lib/example_web/controllers/auth/sudo_html.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope dual-DOM strict-mode assertions to the visible variant (S2)</name>
  <files>test/example/priv/playwright/tests/admin-user-operations.spec.ts, test/example/priv/playwright/tests/impersonation.spec.ts, test/example/priv/playwright/tests/admin-modal-interaction.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/helpers/adminUsersIndex.ts:10-21 — the canonical `adminUsersEmailLocator` idiom: `[desktop],[mobile]` locator → `.filter({hasText}).filter({visible:true}).first()`
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts:325-376 — the clean lane: targets `[data-testid="admin-audit-{user-}{desktop,mobile}-results"]` with `.toBeVisible()`/`.toBeHidden()` per viewport, never an unscoped getByText
    - priv/templates/sigra.install/admin/sigra_admin.css:293-317 — the .sg-show-desktop/.sg-show-mobile substrate that hides one variant per viewport
    - lib/sigra/admin/live/user_show_live.ex:115 — sessions preview "No active sessions"
    - lib/sigra/admin/live/user_sessions_live.ex:160 — dedicated sessions page "No active sessions"
    - admin-user-operations.spec.ts:73 + the "No active sessions"/"Passkeys" assertion sites; impersonation.spec.ts:89; admin-modal-interaction.spec.ts:68
  </read_first>
  <action>
    Reconcile the dual-DOM strict-mode-violation assertions (per S2 / RESEARCH Pitfall 5) so each resolves exactly one VISIBLE element. The strings "No active sessions" and "Passkeys" each appear in both the desktop and mobile DOM variants (and "No active sessions" appears on both the user-detail Sessions preview AND the dedicated sessions page). For each failing unscoped `getByText(...)`:
    1. Scope to the visible page region first (e.g. the sessions `<section>` filtered by its heading), then chain `.filter({ visible: true }).first()` — the exact shape of the existing `adminUsersEmailLocator` idiom. Do NOT use `.or()` (it matches both hidden+visible) and do NOT use a bare `.nth(0)`/`.first()` without the visibility filter (it can resolve the hidden desktop row on mobile/webkit — the helper docstring warns of exactly this).
    2. Where a `[data-testid]` region marker already exists (per the admin-checkpoints precedent), prefer scoping to it.
    Apply the same idiom to the failing assertions in admin-user-operations.spec.ts:73, impersonation.spec.ts:89, and admin-modal-interaction.spec.ts:68. This is selector-scoping only — never delete a behavioral guard (the impersonation-banner check and the 7 APG gates in admin-modal-interaction must stay real assertions). Run the local enumeration FIRST to confirm each is a scoping fix vs a real regression before editing.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-user-operations.spec.ts tests/impersonation.spec.ts tests/admin-modal-interaction.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - No `strict mode violation: ... resolved to 2 elements` for "No active sessions" or "Passkeys"
    - Each scoped assertion uses `.filter({ visible: true }).first()` (or a `[data-testid]` region), not `.or()` or a bare `.first()`
    - No behavioral guard removed (impersonation banner + APG gates intact)
    - admin-user-operations, impersonation, admin-modal-interaction pass on --project=chromium
  </acceptance_criteria>
  <done>The dual-DOM strict-mode assertions resolve exactly one visible element via the ratified scoping idiom, with no behavioral guard weakened.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile stale sudo heading + audit disclosure selectors (S2)</name>
  <files>test/example/priv/playwright/tests/impersonation.spec.ts, test/example/priv/playwright/tests/admin-audit.spec.ts</files>
  <read_first>
    - test/example/lib/example_web/controllers/auth/sudo_html.ex:29 — `<h1 class="vt-auth__title">Re-enter your password</h1>` (the ratified product copy); :51 button "Confirm password" (matches confirmSudo, do not change)
    - impersonation.spec.ts:62 + :89 — the stale `getByRole('heading', { name: 'Confirm your password' })` sudo assertion
    - lib/sigra/admin/live/audit_index_live.ex:82-104 — the "Action prefix" textbox lives inside `<details><summary>More filters</summary>…</details>` collapsed by default; name="action_prefix" also on the Impersonation checkbox (line 73) — role-based selector disambiguates
    - admin-audit.spec.ts:77 + :135 — the `getByRole('textbox', { name: 'Action prefix' }).fill(...)` assertion
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (Open Question 1 — diff the installer sudo template before flipping admin-generated.spec.ts)
  </read_first>
  <action>
    Two stale-text reconciliations (per S2 / RESEARCH Pitfalls 3 and 4):
    1. **Sudo heading:** Update the sudo heading assertion in impersonation.spec.ts (the `getByRole('heading', { name: ... })`) to match the rendered template copy "Re-enter your password" (sudo_html.ex:29 is the more-recent product reality; the template is authoritative). Leave the "Confirm password" button selector unchanged (it already matches). Before changing the parallel `admin-generated.spec.ts:48` assertion, diff the installer sudo template (`priv/templates/sigra.install/...` sudo equivalent) against the example to confirm they render the same heading (installer template drift is a known hazard); if the installer template still reads "Confirm your password", reconcile the template to match rather than diverging the specs — keep installer and example consistent.
    2. **Audit disclosure:** In admin-audit.spec.ts, open the collapsed disclosure before targeting the field — add `await page.getByText('More filters').click()` (or `page.locator('details').click()`) immediately before the `getByRole('textbox', { name: 'Action prefix' }).fill(...)` call so the input is visible/interactable. Keep the role-based textbox selector (it disambiguates from the same-named checkbox by role).
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/impersonation.spec.ts tests/admin-audit.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - impersonation.spec.ts sudo heading assertion matches the rendered "Re-enter your password"
    - admin-audit.spec.ts opens the "More filters" disclosure before targeting the "Action prefix" textbox
    - installer sudo template vs example reconciled (consistent heading; admin-generated.spec.ts not left diverged)
    - Both specs pass on --project=chromium
  </acceptance_criteria>
  <done>The sudo heading and audit-disclosure selectors are reconciled to the rendered UI, installer/example stay consistent, and both specs pass.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm admin_behavior lane green + admin_checkpoints unregressed (S2)</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <read_first>
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-VALIDATION.md (Per-Task Verification Map — admin_behavior + no-regression rows)
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts — the clean reference lane (must NOT be edited; runs chromium/mobile/dark)
  </read_first>
  <action>
    Run the full admin_behavior spec group on chromium AND confirm the clean admin_checkpoints lane is still green (no regression) across its 3 projects. Do NOT edit admin-checkpoints.spec.ts — it is the reference pattern; this task only re-runs it to prove no regression. Record both results in the SUMMARY.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-user-operations.spec.ts tests/impersonation.spec.ts tests/admin-audit.spec.ts tests/admin-modal-interaction.spec.ts --project=chromium && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-chromium --project=admin-checkpoints-mobile --project=admin-checkpoints-dark</automated>
  </verify>
  <acceptance_criteria>
    - All 4 admin_behavior specs pass on --project=chromium
    - admin-checkpoints.spec.ts passes on chromium, mobile, dark (no regression)
    - admin-checkpoints.spec.ts unchanged (git shows no diff)
  </acceptance_criteria>
  <done>The admin_behavior lane is green on chromium and the clean admin_checkpoints reference lane stays green with no edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Playwright spec selector/text reconciliation only. The sudo re-auth flow, session revoke, impersonation, and admin policy are unchanged markup/selectors — no auth logic edited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208.1-03 | Repudiation | impersonation/modal behavioral guards | high | mitigate | Never delete a behavioral guard to force green — the impersonation-banner check and the 7 APG gates in admin-modal-interaction must remain real assertions. Only scope selectors / reconcile copy. |
| T-208.1-04 | Tampering | sudo heading reconciliation | low | mitigate | Reconcile the spec to the ratified template copy; keep installer template and example consistent so admin-generated.spec.ts is not left diverged. |

No security-sensitive code paths touched: sudo/session/impersonation logic unchanged; admin routes already guarded; changes are selector-scoping + stale-copy reconciliation (ASVS L1 — V2–V6 N/A per RESEARCH Security Domain).
</threat_model>

<verification>
- All 4 admin_behavior specs green on --project=chromium
- No strict-mode violations remain on "No active sessions"/"Passkeys"
- Sudo heading + audit disclosure reconciled to rendered UI
- admin_checkpoints reference lane green and unedited
- No behavioral guard deleted
</verification>

<success_criteria>
The 4 admin_behavior specs pass on chromium via selector-scoping and stale-copy reconciliation (no UI redesign, no guard weakened), and the clean admin_checkpoints lane remains green and untouched.
</success_criteria>

<output>
Create `.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-02-SUMMARY.md` when done. Record any failure classified as a real regression (vs scoping) and how it was reconciled to the ratified contract.
</output>
