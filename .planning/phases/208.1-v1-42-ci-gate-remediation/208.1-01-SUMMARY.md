---
phase: 208.1-v1-42-ci-gate-remediation
plan: 01
subsystem: testing
tags: [playwright, admin-design, responsive, sg-show-desktop, sg-show-mobile, design-gallery]

requires:
  - phase: 208-code-review-and-audit
    provides: design gallery board catalog (board-mg-*, board-cfg-*) and admin design contract

provides:
  - board-mg-1 .sg-metric count assertion corrected to 6 (matches ratified slim 3-chip strip markup)
  - board-cfg-audit responsive desktop/mobile swap conforming to MG-5/MG-6 pattern
  - Wave 3 ordering gate: admin-design.spec.ts S1-targeted tests behaviorally green on chromium

affects:
  - 208.1-02 (behavioral spec reconciliation - non-blocking; S1 env-independent bugs are the hard gate)
  - 208.1-04 (CI-native baseline capture - depends_on this plan's behavioral green state)

tech-stack:
  added: []
  patterns:
    - "board-cfg-audit now follows the ratified MG-5/MG-6 responsive table pattern: sg-table-panel sg-show-desktop + sg-stack sg-show-mobile sibling with audit_row cards"

key-files:
  created: []
  modified:
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/lib/example_web/live/admin/design_gallery_live.ex

key-decisions:
  - "Fix the TEST count to match markup (6), not the markup to match the test's stale 7 — the cite-and-flip audit had never counted the actual DOM nodes"
  - "board-cfg-audit responsive fix mirrors MG-5/MG-6 exactly: sg-show-desktop table + sg-show-mobile audit_row cards with show_detail show_codes attrs; no overflow:auto scroll wrapper"
  - "Pre-existing content-equivalence and filter-form failures (admin-design.spec.ts:364, :434) are out of scope for Plan 01 — they are redesign-broken behavioral specs classified as bucket 2 in FINDINGS, addressed by Plan 02"

patterns-established:
  - "Responsive audit composite: wrap desktop table in sg-table-panel sg-show-desktop with data-testid; add sg-stack sg-show-mobile sibling using audit_row with show_detail show_codes"

requirements-completed:
  - 208.1-S1-env-independent-gallery-bugs

coverage:
  - id: D1
    description: "board-mg-1 .sg-metric count assertion corrected from 7 to 6 (admin-design.spec.ts:331)"
    requirement: 208.1-S1-env-independent-gallery-bugs
    verification:
      - kind: automated_ui
        ref: "admin-design.spec.ts:318 › group boards expose catalog states and right components (admin-design-chromium)"
        status: pass
    human_judgment: false
  - id: D2
    description: "board-cfg-audit responsive desktop/mobile swap added — no overflow at 320px"
    requirement: 208.1-S1-env-independent-gallery-bugs
    verification:
      - kind: automated_ui
        ref: "admin-design.spec.ts:269 › component and group boards do not overflow at required responsive widths (admin-design-chromium)"
        status: pass
    human_judgment: false

duration: 13min
completed: 2026-06-30
status: complete
---

# Phase 208.1 Plan 01: Env-Independent Gallery Bug Fixes Summary

**Corrected board-mg-1 .sg-metric count from 7 to 6 and added MG-5/MG-6-style responsive desktop/mobile swap to board-cfg-audit, fixing the two env-independent admin-design failures that block CI-native Wave 3 baseline capture**

## Performance

- **Duration:** ~13 min
- **Started:** 2026-06-30T02:57:54Z
- **Completed:** 2026-06-30T03:10:50Z
- **Tasks:** 3 (2 code tasks + 1 verification)
- **Files modified:** 2

## Accomplishments

- Fixed board-mg-1 `.sg-metric` count assertion — test asserted `toHaveCount(7)` but markup renders exactly 6 (3 in mg-1-populated + 3 in mg-1-zero; loading state uses `.sg-skeleton`, not `.sg-metric`). Changed test count to 6; markup unchanged (slim 3-chip strip contract preserved).
- Added responsive desktop/mobile swap to board-cfg-audit — wrapped the 4-column audit table in `sg-table-panel sg-show-desktop` (data-testid=cfg-audit-desktop-results) and added a `sg-stack sg-show-mobile` sibling (data-testid=cfg-audit-mobile-results) using `<.audit_row>` cards with `show_detail show_codes` attrs, mirroring MG-5/MG-6 pattern exactly. At 320px the @media(1024px) toggle hides the desktop table; mobile cards render within viewport.
- Confirmed admin-design.spec.ts S1-targeted behavioral tests green: "group boards expose catalog states" and "do not overflow at required responsive widths" both pass on admin-design-chromium. No board baseline PNGs committed from darwin (snapshot churn discarded after --update-snapshots run).

## Task Commits

1. **Task 1: Correct board-mg-1 .sg-metric count assertion to 6** - `341e32ae` (fix)
2. **Task 2: Add responsive desktop/mobile swap to board-cfg-audit** - `19859942` (fix)

(Task 3 was verification-only — no files changed; result documented in this SUMMARY.)

## Files Created/Modified

- `test/example/priv/playwright/tests/admin-design.spec.ts` — Line 331: `toHaveCount(7)` → `toHaveCount(6)` for `#board-mg-1 .sg-metric`
- `test/example/lib/example_web/live/admin/design_gallery_live.ex` — board-cfg-audit: added `sg-show-desktop` to table panel div + cfg-audit-desktop-results testid; new `sg-stack sg-show-mobile` sibling with cfg-audit-mobile-results testid + two `<.audit_row>` cards reusing same row data

## Decisions Made

- **Fix test, not markup:** The board-mg-1 count of 7 came from a cite-and-flip audit that read the test's own expected value, never counting actual DOM nodes. Markup renders 6. Changed the test assertion; the slim 3-chip strip contract (3 chips × 2 states = 6 metrics) is the correct design.
- **Mirror MG-5/MG-6 exactly:** board-cfg-audit responsive fix uses `audit_row` component with `show_detail show_codes` attrs — same as the MG-6 mobile block. No new card component invented; no `overflow-x: auto` scroll wrapper (not the ratified contract).
- **Pre-existing failures out of scope for Plan 01:** admin-design.spec.ts:364 (content-equivalence, navigates to live /admin/users) and :434 (filter form submission) fail with redesign-era strict-mode violations. These are classified as "bucket 2 — redesign-broken behavioral specs" in FINDINGS and are addressed by Plan 02.

## Deviations from Plan

None — plan executed exactly as written. Task 3 verified both S1-targeted tests pass; pre-existing failures outside Plan 01 scope documented above.

## Wave 3 Gate: admin-design behaviorally green

**GATE CONFIRMED:** The two S1-targeted behavioral tests (overflow + catalog states) pass on admin-design-chromium after the Plan 01 fixes. Wave 3 (CI-native baseline capture via `admin_design_recapture`) can proceed. The 2 remaining failures in admin-design.spec.ts (content-equivalence :364, filter-form :434) are pre-existing redesign-era failures addressed by Plan 02; they do NOT affect the board-level gallery behavioral tests that gate snapshot capture.

## Known Stubs

None — no stubs introduced. board-cfg-audit mobile cards use the same static audit row data as the desktop table.

## Threat Flags

None — changes are a test count literal + responsive presentational markup only. No auth/session/access-control logic touched. Admin routes remain guarded by the existing SigraAdminPolicy.

## Self-Check: PASSED

- `test/example/priv/playwright/tests/admin-design.spec.ts` exists: FOUND
- `test/example/lib/example_web/live/admin/design_gallery_live.ex` exists: FOUND
- Commit `341e32ae` exists: FOUND (git log confirms)
- Commit `19859942` exists: FOUND (git log confirms)

## Issues Encountered

None — straightforward fixes. Gallery app server started on :4011 against Docker Postgres for verification.

## Next Phase Readiness

- Plan 02 can proceed: the 2 S1 env-independent failures are fixed; Plan 02 targets the redesign-broken behavioral specs (admin_behavior: strict-mode dual-DOM violations, stale text selectors)
- Wave 3 unblocked: `admin_design_recapture` CI job can now capture the 12 board-cfg-* PNGs (4 boards × 3 projects) — dispatch via `wip/recapture-trigger` branch once all admin-design behavioral tests are green

---
*Phase: 208.1-v1-42-ci-gate-remediation*
*Completed: 2026-06-30*
