---
phase: 208.1-v1-42-ci-gate-remediation
verified: 2026-06-30T00:00:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: false
---

# Phase 208.1: v1.42 CI-Gate Remediation Verification Report

**Phase Goal:** The required "Example Playwright smoke (full lifecycle)" CI check passes green on the v1.42 backlog — the ~15 real, never-CI-validated admin Playwright behavioral failures are fixed — so the backlog can ship through branch protection and Phase 208 can complete.
**Verified:** 2026-06-30
**Status:** passed
**Re-verification:** No — initial verification

---

## Requirements Coverage Note

The phase requirement IDs (208.1-S1 through 208.1-S5) are defined inline in the ROADMAP.md Phase 208.1 entry and in PLAN frontmatter. They are **not** entries in REQUIREMENTS.md, which covers the parent v1.42 ADMIN-DS-ELEVATION milestone arc (INSTR/COMP/GROUP/PAGE/GATE). This is correct: Phase 208.1 was inserted as a CI-gate remediation step with its own sub-scope, not as a milestone requirement. All 5 sub-requirements are accounted for across the four plans.

---

## Goal Achievement

### Observable Truths

Derived from the phase goal and PLAN must_haves (no `success_criteria:` block in ROADMAP for this inserted phase — truths derived from the 5 scope items and Plan 04 exit criterion):

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | board-mg-1 `.sg-metric` count assertion corrected to 6 (S1) | VERIFIED | `admin-design.spec.ts:331` reads `toHaveCount(6)` — confirmed in codebase |
| 2 | board-cfg-audit has `.sg-table-panel.sg-show-desktop` + `.sg-stack.sg-show-mobile` sibling, no 320px overflow (S1) | VERIFIED | `design_gallery_live.ex` lines 1324 and 1364 confirmed: `data-testid="cfg-audit-desktop-results"` + `data-testid="cfg-audit-mobile-results"` present |
| 3 | 4 admin_behavior specs + non_admin + demo_showcase reconciled — dual-DOM strict-mode violations, sudo heading, audit disclosure, org/passkey/demo copy (S2) | VERIFIED | Codebase confirms: `{ exact: true }` on "No active sessions" (admin-user-operations:155), "Re-enter your password" in impersonation.spec.ts:64 + admin-generated.spec.ts:50, sudo_html.ex template:21, "More filters" click in admin-audit:140, vt-status-pill selectors in organizations.spec.ts, passkey-login `exact:true` at :94, demo-showcase `page_size=50` at :920 |
| 4 | 12 board-cfg-* CI-native baseline PNGs captured and committed (S3) | VERIFIED | Exactly 12 PNGs confirmed on disk: board-cfg-audit, board-cfg-overview, board-cfg-user-detail, board-cfg-users-list × {chromium, dark, mobile} |
| 5 | Cite-and-flip re-validation ran 16 specs live — real execution, not code re-read (S4) | VERIFIED | Plan 03 Summary records explicit per-spec table: 16 specs run, 16 pass, 0 fail; commit b9642f52 + 9a32dfad are the code artifacts |
| 6 | Required CI check "Example Playwright smoke (full lifecycle)" is GREEN on the v1.42 integration PR — all 5 seams (S5) | VERIFIED | `gh run view 28451828047` conclusion = `success`; all 5 seam steps confirmed green: "Run admin behavior browser truth (chromium)", "Run admin checkpoints (chromium, mobile, dark-chromium)", "Run design gallery boards (chromium, mobile, dark)", "Run non-admin example browser smoke", "Run demo-showcase spec (demo-showcase-chromium)" |
| 7 | wip/recapture-trigger guard-relax never reaches main (S5) | VERIFIED | `git ls-remote origin wip/recapture-trigger` returns nothing; `git log --all` finds no af75b127 commit; remote tracking branches show only `ci/recapture-admin-design-*` (legitimate recapture auto-PRs) |
| 8 | admin_checkpoints reference lane not regressed | VERIFIED | CI run step "Run admin checkpoints (chromium, mobile, dark-chromium)" = success; Plan 02 Summary confirms lane unedited |

**Score: 5/5 scope items (8 derived truths all VERIFIED)**

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `test/example/priv/playwright/tests/admin-design.spec.ts` | board-mg-1 count = 6 | VERIFIED | Line 331: `toHaveCount(6)` confirmed |
| `test/example/lib/example_web/live/admin/design_gallery_live.ex` | board-cfg-audit responsive swap (sg-show-desktop + sg-show-mobile) | VERIFIED | Lines 1324 + 1364 with correct testids and CSS classes |
| `test/example/priv/playwright/tests/admin-user-operations.spec.ts` | `{ exact: true }` on "No active sessions" | VERIFIED | Line 155 confirmed |
| `test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` | Navigate to sessions page before APG assertions | VERIFIED | Present per commit ae1f9479 |
| `test/example/priv/playwright/tests/impersonation.spec.ts` | "Re-enter your password" heading assertion | VERIFIED | Line 64 confirmed |
| `test/example/priv/playwright/tests/admin-audit.spec.ts` | "More filters" click before Action prefix targeting | VERIFIED | Lines 140-141 confirmed |
| `test/example/priv/playwright/tests/admin-generated.spec.ts` | "Re-enter your password" heading assertion | VERIFIED | Line 50 confirmed |
| `priv/templates/sigra.install/core/sudo_html.ex` | "Re-enter your password" heading | VERIFIED | Line 21 confirmed |
| `test/example/priv/playwright/tests/organizations.spec.ts` | vt-* design system selectors + Tasklane copy | VERIFIED | vt-status-pill, vt-menu__panel, org-danger-zone selectors confirmed |
| `test/example/priv/playwright/tests/passkey-login.spec.ts` | `exact: true` on "Passkeys" heading | VERIFIED | Line 94 confirmed |
| `test/example/priv/playwright/tests/demo-showcase.spec.ts` | page_size=50 + zoe in DEMO_LOCALS | VERIFIED | Lines 50 (zoe), 920 (page_size=50) confirmed |
| 12 board-cfg-* baseline PNGs in `admin-design.spec.ts-snapshots/` | Exactly 12 PNGs (4 boards × 3 projects) | VERIFIED | All 12 confirmed on disk: board-cfg-audit/overview/user-detail/users-list × chromium/dark/mobile |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `admin-design.spec.ts:331` | board-mg-1 markup (3 chips × 2 states = 6 `.sg-metric`) | test count literal `6` | VERIFIED | Count matches ratified slim-strip contract |
| `design_gallery_live.ex` cfg-audit block | `@media(1024px)` visibility CSS in `sigra_admin.css` | `sg-show-desktop`/`sg-show-mobile` classes | VERIFIED | CSS substrate pre-existed; classes correctly applied to new elements |
| Spec dual-DOM assertions | visible DOM variant | `.filter({ visible: true }).first()` or `{ exact: true }` | VERIFIED | Pattern confirmed in all reconciled specs |
| CI run 28451828047 (headSha cbe0b928) | "Example Playwright smoke (full lifecycle)" | GitHub Actions required check | VERIFIED | Machine-readable `gh run view` conclusion = `success` |

---

## Behavioral Spot-Checks

Step 7b behavioral checks ran via the CI arbiter (the sole arbiter of done per S5 — local green is explicitly NOT sufficient per Plan 04). CI run 28451828047 executed all five seams end-to-end against the ubuntu environment with a live Phoenix app + demo seeds + real Playwright browsers.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 5 Playwright smoke seams green | `gh run view 28451828047 ... conclusion` | `"success"` | PASS |
| admin_behavior chromium (admin-user-ops, impersonation, admin-audit, admin-modal) | CI step "Run admin behavior browser truth (chromium)" | success | PASS |
| admin_checkpoints (chromium, mobile, dark) | CI step "Run admin checkpoints ..." | success | PASS |
| design_gallery (chromium, mobile, dark) | CI step "Run design gallery boards ..." | success | PASS |
| non_admin (organizations, passkey-login) | CI step "Run non-admin example browser smoke" | success | PASS |
| demo_showcase (demo-showcase-chromium) | CI step "Run demo-showcase spec ..." | success | PASS |

---

## Requirements Coverage

Phase 208.1 defines its own sub-scope requirements inline. All 5 are satisfied:

| Requirement | Plan | Description | Status | Evidence |
|-------------|------|-------------|--------|----------|
| 208.1-S1-env-independent-gallery-bugs | 01 | Fix board-mg-1 count (7→6) + board-cfg-audit 320px responsive swap | SATISFIED | Codebase: spec:331 = 6; gallery_live:1324/1364 = sg-show-desktop/mobile |
| 208.1-S2-behavioral-spec-reconciliation | 02, 03 | Reconcile admin_behavior + non_admin + demo_showcase specs | SATISFIED | Codebase: all selector/copy fixes confirmed; CI all-seams green |
| 208.1-S3-baseline-capture | 04 | Capture 12 board-cfg-* PNGs CI-native (ubuntu) | SATISFIED | 12 PNGs on disk; captured via `ci/recapture-admin-design-28440585028` PR |
| 208.1-S4-cite-and-flip-revalidation | 03 | Re-run 205/206/207/208 audit-claimed specs; real execution not code re-read | SATISFIED | Plan 03 Summary: 16 specs run locally, 16 pass, 0 fail; per-spec table recorded |
| 208.1-S5-ci-arbiter-ship | 04 | Required check green; guard-relax branch deleted | SATISFIED | CI run 28451828047 = success; wip/recapture-trigger absent from origin |

**Orphaned requirements check:** The milestone REQUIREMENTS.md (INSTR/COMP/GROUP/PAGE/GATE) does not reference 208.1-S* IDs — those IDs live in ROADMAP + PLAN frontmatter only, which is the correct structure for this inserted remediation phase. No orphaned IDs.

---

## Anti-Patterns Found

No `TBD`, `FIXME`, or `XXX` markers found in any file modified by this phase. No stubs or placeholder implementations introduced — all changes are behavioral: test count literals, selector fixes, copy reconciliation, responsive markup, and baseline PNGs.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

---

## Out-of-Scope Reds (not counted against phase goal)

Two CI reds remain on PR #63 that are explicitly outside 208.1 scope, per the phase definition and Plan 04 Summary. Both are filed as tracked debt:

1. **Fast checks: snapshot-canary drift** — cumulative drift from Phases 200-204 backlog + impersonation-banner canary-policy tension. Filed: `.planning/todos/pending/2026-06-30-v142-integration-snapshot-canary-drift.md`
2. **Library shard-2: NoopTest log-capture flake** — passes 3/3 locally; untouched by 208.1. Filed in same todo.

These are not counted against the phase goal, which is the required check "Example Playwright smoke (full lifecycle)" — confirmed green.

---

## Human Verification Required

None. The phase exit criterion (CI required check green) is machine-verifiable and was confirmed directly via `gh run view 28451828047`. The PR #63 branch protection check is the definitive gate.

---

## Gaps Summary

No gaps. All phase must-haves are verified against the codebase and the live CI run.

---

_Verified: 2026-06-30_
_Verifier: Claude (gsd-verifier)_
