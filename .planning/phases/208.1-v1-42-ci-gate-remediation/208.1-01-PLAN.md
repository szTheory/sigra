---
phase: 208.1-v1-42-ci-gate-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
autonomous: true
requirements:
  - 208.1-S1-env-independent-gallery-bugs
user_setup: []

must_haves:
  truths:
    - "admin-design.spec.ts 'group boards expose catalog states' passes — #board-mg-1 .sg-metric count assertion matches the 6 metrics the markup renders"
    - "admin-design.spec.ts 'do not overflow at required responsive widths' passes — board-cfg-audit has no overflowingChild at 320px"
    - "admin-design.spec.ts is BEHAVIORALLY green on admin-design-chromium (gates Wave 3 baseline capture)"
  artifacts:
    - "test/example/priv/playwright/tests/admin-design.spec.ts (board-mg-1 count corrected to 6)"
    - "test/example/lib/example_web/live/admin/design_gallery_live.ex (board-cfg-audit responsive desktop/mobile swap)"
  key_links:
    - "board-cfg-audit desktop table wrapped in .sg-table-panel.sg-show-desktop + content-equivalent .sg-show-mobile card stack (MG-5/MG-6 precedent)"
    - "@media(1024px) .sg-show-desktop visibility toggle hides the table at 320px so it cannot overflow"
---

<objective>
Fix the 2 env-independent (pure DOM/geometry) admin_design gallery bugs that block both the design lane and the Wave 3 CI-native baseline capture: the stale board-mg-1 `.sg-metric` count (test asserts 7, markup renders 6) and the board-cfg-audit responsive overflow at 320px (bare desktop table with no mobile-card swap).

Purpose: These are the hard ordering gate. `admin_design_recapture` runs `--update-snapshots`, which does NOT make behavioral assertions pass — so if either bug is unfixed the recapture step aborts and produces zero baselines (the literal run 28401914980 failure mode). Both bugs are reproducible anywhere (no CI/env divergence).
Output: corrected admin-design.spec.ts count assertion; responsive board-cfg-audit markup conforming to the ratified MG-5/MG-6 table pattern.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-VALIDATION.md
@guides/reference/admin-design-contract.md

# Source files to reconcile
@test/example/priv/playwright/tests/admin-design.spec.ts
@test/example/lib/example_web/live/admin/design_gallery_live.ex
@lib/sigra/admin/components.ex
@priv/templates/sigra.install/admin/sigra_admin.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct board-mg-1 .sg-metric count assertion to 6 (S1)</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts:331 — the assertion `expect(page.locator('#board-mg-1 .sg-metric')).toHaveCount(7)`
    - test/example/lib/example_web/live/admin/design_gallery_live.ex:456-487 — board-mg-1 markup (mg-1-populated: 3 summary_chips + mg-1-zero: 3 summary_chips; loading state uses .sg-skeleton not .sg-metric)
    - lib/sigra/admin/components.ex:169-227 — summary_chip/1 emits one `<dl class="sg-metric">` per chip
    - guides/reference/admin-design-contract.md (List Archetype D-03) — the ratified slim 3-chip User-health strip
  </read_first>
  <action>
    Change the board-mg-1 metric-count assertion at admin-design.spec.ts:331 from `toHaveCount(7)` to `toHaveCount(6)` (per S1 / RESEARCH Pitfall 2 / FINDINGS triage item 1). The markup renders exactly 6 `.sg-metric` nodes: board-mg-1 has two populated states (mg-1-populated + mg-1-zero), each a 3-chip strip mirroring the ratified slim User-health strip, and each summary_chip emits one `<dl class="sg-metric">`. The loading state uses `.sg-skeleton`, not `.sg-metric`, so it does not contribute a 7th. The 7 was lifted from a cite-and-flip audit (the test's own expected value) and was never counted against markup — fix the TEST, not the markup. Do NOT add a 7th summary_chip (that would violate the slim-strip contract). Touch only the count literal on that one assertion line; leave every other `toHaveCount` assertion in the catalog-states test unchanged.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium -g "group boards expose catalog states"</automated>
  </verify>
  <acceptance_criteria>
    - admin-design.spec.ts:331 reads `toHaveCount(6)` for `#board-mg-1 .sg-metric`
    - The "group boards expose catalog states and right components" test passes on admin-design-chromium
    - No summary_chip added or removed in design_gallery_live.ex (markup unchanged for mg-1)
    - No other catalog-states count assertion changed
  </acceptance_criteria>
  <done>The board-mg-1 metric-count assertion matches the 6 metrics the ratified slim-strip markup renders, and the catalog-states test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add responsive desktop/mobile swap to board-cfg-audit (S1)</name>
  <files>test/example/lib/example_web/live/admin/design_gallery_live.ex</files>
  <read_first>
    - test/example/lib/example_web/live/admin/design_gallery_live.ex:1298-1364 — board-cfg-audit composite (renders a bare `<div class="sg-table-panel"><table class="sg-table">` with NO sg-show-desktop wrapper and NO mobile swap)
    - test/example/lib/example_web/live/admin/design_gallery_live.ex:617-841 — MG-5/MG-6 reference: `<div data-testid="mg-5-desktop-results" class="sg-table-panel sg-show-desktop"><table class="sg-table">…</table></div>` paired with `<div data-testid="mg-5-mobile-results" class="sg-stack sg-stack--3 sg-show-mobile">…cards…</div>`
    - priv/templates/sigra.install/admin/sigra_admin.css:293-317 — the .sg-show-desktop/.sg-show-mobile @media(1024px) visibility substrate
    - test/example/priv/playwright/tests/admin-design.spec.ts:269-306 — the overflow test (RESPONSIVE_WIDTHS includes 320; reports overflowingChild when a child's getBoundingClientRect().right exceeds viewport)
  </read_first>
  <action>
    Reconcile board-cfg-audit to the ratified responsive-table contract by mirroring the MG-5/MG-6 desktop+mobile swap (per S1 / RESEARCH Pattern 2). Wrap the existing 4-column audit `<table class="sg-table">` in `<div class="sg-table-panel sg-show-desktop">` (add `sg-show-desktop` to the existing panel wrapper; give it a `data-testid="cfg-audit-desktop-results"`). Then add a content-equivalent `<div data-testid="cfg-audit-mobile-results" class="sg-stack sg-stack--3 sg-show-mobile">` sibling that renders the same audit rows as mobile cards using the existing shared audit-row component already used by MG-5/MG-6 (do not invent a new card component or new copy). At 320px the `@media(1024px)` toggle sets the desktop table to `display:none`, so the table cannot overflow; the mobile cards stack within the viewport. Preserve the existing column data and ordering — this is presentational reconciliation, NOT a redesign. Do NOT use a custom `overflow-x: auto` scroll wrapper (that is not the ratified contract); use the desktop/mobile swap exactly as MG-5/MG-6 do.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium -g "do not overflow"</automated>
  </verify>
  <acceptance_criteria>
    - board-cfg-audit renders a `.sg-table-panel.sg-show-desktop` table AND a `.sg-stack.sg-show-mobile` card stack
    - The "component and group boards do not overflow at required responsive widths" test passes — no overflowingChild for board-cfg-audit at 320px
    - The mobile cards reuse the existing shared audit-row component (no new component, no new copy)
    - The 4-column desktop table data and ordering are unchanged
  </acceptance_criteria>
  <done>board-cfg-audit conforms to the ratified MG-5/MG-6 responsive swap and the overflow test is green at all RESPONSIVE_WIDTHS including 320px.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm admin-design.spec.ts is behaviorally green across all 3 projects (S1 gate)</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (Pitfall 1 — the hard ordering constraint: admin-design must be behaviorally green before recapture)
    - test/example/priv/playwright/tests/admin-design.spec.ts (full file — the three behavioral tests: overflow :269, catalog states :318, content-equivalence)
  </read_first>
  <action>
    Run the full admin-design.spec.ts across all three projects (admin-design-chromium, admin-design-mobile, admin-design-dark) with `--update-snapshots` (to suppress snapshot-PNG churn — board baselines are captured CI-native in Wave 3, never committed from darwin) and confirm ALL behavioral assertions pass. This is the hard ordering gate for Wave 3: the CI recapture step runs `--update-snapshots` and aborts on ANY behavioral failure. After the run, DISCARD all snapshot churn via `git checkout -- test/example/priv/playwright/tests && git clean -fdq test/example/priv/playwright/tests` (read-only intent locally; no PNGs committed from darwin). Record the green result in the plan SUMMARY — this is the behavioral-green confirmation that Wave 3 depends_on.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --update-snapshots</automated>
  </verify>
  <acceptance_criteria>
    - All behavioral assertions in admin-design.spec.ts pass on chromium, mobile, and dark projects
    - Snapshot churn from --update-snapshots is discarded (no board PNGs committed locally on darwin)
    - SUMMARY records "admin-design behaviorally green" as the Wave 3 gate
  </acceptance_criteria>
  <done>admin-design.spec.ts is behaviorally green on all 3 design projects, unblocking the Wave 3 CI-native baseline capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a Playwright spec count literal and design-gallery presentational markup only. No untrusted-input boundary is added or crossed. Admin routes remain guarded by the existing SigraAdminPolicy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208.1-01 | Tampering | admin-design.spec.ts behavioral assertions | low | mitigate | Fix the count to match ratified markup (6); never relax the overflow assertion (`overflowingChild: null` stays). No behavioral guard is deleted to force green. |
| T-208.1-02 | Repudiation | board-cfg-audit responsive markup | low | accept | Presentational-only swap; no auth/audit-logging path touched. Admin audit data displayed unchanged (same columns/ordering). |

No security-sensitive code paths touched: no auth/session/access-control logic edited; admin routes already guarded; changes are a test count literal + responsive presentational markup (ASVS L1 — V2–V6 all N/A per RESEARCH Security Domain).
</threat_model>

<verification>
- admin-design.spec.ts behaviorally green on admin-design-chromium, admin-design-mobile, admin-design-dark
- board-mg-1 .sg-metric count assertion reads 6 and passes
- board-cfg-audit no overflowingChild at 320px
- No new design components/copy introduced (reconciliation only)
- No board baseline PNGs committed from darwin
</verification>

<success_criteria>
admin-design.spec.ts is behaviorally green across all 3 design projects, with the board-mg-1 count corrected to 6 and board-cfg-audit conforming to the ratified MG-5/MG-6 responsive swap — satisfying the hard ordering gate that Wave 3 (CI-native baseline capture) depends_on.
</success_criteria>

<output>
Create `.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-01-SUMMARY.md` when done. Record the "admin-design behaviorally green" confirmation explicitly (Wave 3 gate).
</output>
