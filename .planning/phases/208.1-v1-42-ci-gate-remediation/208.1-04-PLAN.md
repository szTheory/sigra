---
phase: 208.1-v1-42-ci-gate-remediation
plan: 04
type: execute
wave: 3
depends_on:
  - 208.1-01
  - 208.1-02
  - 208.1-03
files_modified:
  - test/example/priv/playwright/tests/__snapshots__
autonomous: false
requirements:
  - 208.1-S3-baseline-capture
  - 208.1-S5-ci-arbiter-ship
user_setup: []

must_haves:
  truths:
    - "12 net-new board-cfg-* baseline PNGs (4 boards x 3 projects) are captured CI-native and committed to the backlog branch"
    - "The real required CI check 'Example Playwright smoke (full lifecycle)' is GREEN on the backlog branch"
    - "admin_checkpoints lane stays green (no regression) on CI"
    - "wip/recapture-trigger (guard-relax af75b127) is DELETED at phase close and NEVER merged to main"
  artifacts:
    - "12 board-cfg-* baseline PNGs committed via the recapture PR, merged into the backlog branch"
  key_links:
    - "admin_design_recapture job dispatched via gh workflow run 'CI' --ref wip/recapture-trigger (the only branch-ref dispatch path) — runs ONLY after the design lane is behaviorally green (Wave 1)"
    - "recapture PR PNGs merged into the backlog branch; the real CI required check is the sole arbiter of done"
---

<objective>
Capture the 12 net-new `board-cfg-*` baseline PNGs CI-native (ubuntu only) and drive the real required CI check `Example Playwright smoke (full lifecycle)` to GREEN on the backlog branch — the phase exit criterion. Then clean up the throwaway CI branch so the guard-relax never reaches main.

Purpose: 0 board-cfg baselines exist today; they must be captured on ubuntu (darwin font metrics diverge — admin baselines are CI-native only). The capture is HARD-GATED behind Wave 1 (the design lane behaviorally green): `admin_design_recapture` runs `--update-snapshots`, which aborts the commit/PR step on any behavioral failure (the run 28401914980 failure mode). Local green is necessary but NOT the arbiter — the real CI required check is.
Output: 12 board-cfg PNGs merged into the backlog branch; a green CI required check; wip/recapture-trigger deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-VALIDATION.md
@.github/workflows/ci.yml
@scripts/ci/snapshot-canary-guard.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm design lane behaviorally green before CI-native recapture</name>
  <action>Blocking human checkpoint — confirm the hard ordering precondition (design lane behaviorally green) before dispatching the CI-native recapture. See what-built / how-to-verify / resume-signal below; do not dispatch the recapture until the resume-signal is given.</action>
  <what-built>
    Wave 1 (Plan 01) made admin-design.spec.ts behaviorally green (board-mg-1 count = 6, board-cfg-audit no 320px overflow); Waves 2 (Plans 02/03) made admin_behavior, non_admin, and demo_showcase green locally. This checkpoint confirms the HARD ordering precondition before dispatching the CI-native recapture: the design lane MUST be behaviorally green or the recapture capture step is skipped (run 28401914980 failure mode) and produces zero baselines.
  </what-built>
  <how-to-verify>
    1. Confirm Plan 01 SUMMARY records "admin-design behaviorally green" across admin-design-chromium/mobile/dark.
    2. Confirm Plans 02 and 03 SUMMARYs record all admin_behavior/non_admin/demo_showcase specs green on chromium, with the cite-and-flip enumeration RUN and recorded.
    3. Verify the backlog branch HEAD contains the Wave 1+2 fixes and `wip/recapture-trigger` is rebased/updated to that HEAD plus ONLY the guard-relax `af75b127` (no other divergence).
    Expected: all three preconditions true. If the design lane is NOT behaviorally green, STOP — dispatching now wastes a CI run and produces no baselines.
  </how-to-verify>
  <resume-signal>Type "design-green confirmed" to proceed with the CI-native recapture dispatch, or describe what is still red.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Dispatch CI-native recapture + capture 12 board-cfg baselines (S3)</name>
  <files>test/example/priv/playwright/tests/__snapshots__</files>
  <read_first>
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (CI-native baseline capture block; Pitfall 1 — capture only after design lane behaviorally green)
    - .github/workflows/ci.yml (admin_design_recapture job — runs on push-to-main / schedule / workflow_dispatch from a v* tag or the wip/recapture-trigger branch-ref relax; never on PRs)
    - scripts/ci/snapshot-canary-guard.sh (forbids canary modify, tolerates canary add — runs in the recapture job)
    - .planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md (reusable assets — wip/recapture-trigger af75b127)
  </read_first>
  <action>
    Per S3: dispatch the CI-native `admin_design_recapture` job to capture the 12 net-new board-cfg-* PNGs (4 boards × 3 projects: admin-design-chromium/mobile/dark). Run `gh workflow run "CI" --ref wip/recapture-trigger` (the only branch-ref dispatch path; admin_design_recapture never runs on PRs). The job recaptures all admin-design PNGs (now 28 boards × 3 incl. the 12 new board-cfg), gates via `snapshot-canary-guard.sh` (the board-notice canary must stay un-modified — add-tolerant), and opens a `ci/recapture-admin-design-<run_id>` PR for review. NEVER capture these baselines locally on darwin (font metrics diverge from ubuntu CI — admin baselines are CI-native only). If the run fails at the capture step with behavioral failures, STOP and return to the relevant Wave 1/2 plan — do not retry until the design lane is behaviorally green on CI. Merge the recapture PR's 12 board-cfg PNGs into the backlog branch.
  </action>
  <verify>
    <human-check>
      The recapture run succeeded (not skipped at the capture step); the recapture PR contains exactly 12 net-new board-cfg-* PNGs (4 boards × 3 projects); the board-notice canary is un-modified per snapshot-canary-guard.sh; the PNGs are merged into the backlog branch.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The admin_design_recapture run reached the capture/commit/PR step (did NOT skip)
    - Exactly 12 board-cfg-* baseline PNGs are produced (4 boards × 3 projects)
    - snapshot-canary-guard.sh passes (board-notice canary not modified)
    - The 12 PNGs are merged into the backlog branch
    - No board baseline captured locally on darwin
  </acceptance_criteria>
  <done>12 board-cfg baseline PNGs are captured CI-native and merged into the backlog branch, with the canary intact.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the real CI required check to GREEN (phase exit) (S5)</name>
  <files>test/example/priv/playwright/tests/__snapshots__</files>
  <read_first>
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (the required CI check is the SOLE arbiter of done; local↔CI divergences — font metrics, JS bundle drift, compile-time PORT, disposable-PG)
    - .github/workflows/ci.yml:878 — the required check "Example Playwright smoke (full lifecycle)" (5 seams; aggregator re-fails on any seam failure)
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-VALIDATION.md (phase-gate sampling — real CI green is the arbiter, not local)
  </read_first>
  <action>
    Per S5: drive the real required CI check `Example Playwright smoke (full lifecycle)` (ci.yml:878, 5 seams) to GREEN on the backlog branch — this is the phase exit criterion, explicitly NOT local green. Trigger a real CI run on the backlog branch (with the 12 board-cfg PNGs merged in). Iterate against CI: if a seam fails on CI but passed locally, diagnose against the documented divergences (OS font metrics, JS bundle drift between served app.js and source admin_hooks.js, compile-time PORT baked into _build, disposable-PG state) and reconcile, then re-run. Do NOT treat local green as done. The aggregator re-fails the job on any single seam failure, so all 5 seams (admin_behavior, admin_checkpoints, design_gallery, non_admin, demo_showcase) must be green — including no regression in admin_checkpoints.
  </action>
  <verify>
    <human-check>
      The required check "Example Playwright smoke (full lifecycle)" reports green on the backlog branch's latest CI run; all 5 seams green; admin_checkpoints shows no regression.
    </human-check>
  </verify>
  <acceptance_criteria>
    - "Example Playwright smoke (full lifecycle)" is GREEN on the backlog branch (real CI run)
    - All 5 seams green; admin_checkpoints not regressed
    - Any CI-only divergence was diagnosed against the documented causes and reconciled (not papered over locally)
  </acceptance_criteria>
  <done>The real required CI check is green on the backlog branch — the phase exit criterion is met.</done>
</task>

<task type="auto">
  <name>Task 3: Delete wip/recapture-trigger + record ship-readiness (S5)</name>
  <files>test/example/priv/playwright/tests/__snapshots__</files>
  <read_first>
    - .planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md (the guard-relax af75b127 must NEVER reach main; delete the branch at phase close)
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (Security Domain — leaving the release_ref_guard relax on origin is a CI-gate-bypass tampering threat)
  </read_first>
  <action>
    Per S5 / RESEARCH Security Domain: delete the throwaway CI branch so the `release_ref_guard` relax (`af75b127`) never reaches main. Run `git push origin --delete wip/recapture-trigger` (and delete any local copy) ONLY after the recapture PNGs are merged and the required CI check is green — the guard-relax must never be merged to main. Verify via `git ls-remote origin wip/recapture-trigger` returns nothing. Confirm the backlog branch now carries: the Wave 1+2 spec/markup fixes, the 12 board-cfg PNGs, a green required check, and no guard-relax commit. Record in the SUMMARY that the phase exit criterion is met and the one-PR backlog ship (get-current) can resume per the prior decision. Do NOT perform the backlog ship itself here — only confirm ship-readiness.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && test -z "$(git ls-remote origin wip/recapture-trigger 2>/dev/null)" && echo "wip/recapture-trigger deleted" && ! git log --all --oneline 2>/dev/null | grep -q af75b127 || echo "WARNING: guard-relax still present"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote origin wip/recapture-trigger` returns nothing (branch deleted)
    - The guard-relax af75b127 is not on main / the backlog branch
    - SUMMARY records phase exit criterion met + ship-readiness (backlog ship NOT performed here)
  </acceptance_criteria>
  <done>wip/recapture-trigger is deleted, the guard-relax never reaches main, and the backlog is confirmed ready to ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate / branch ref | The throwaway `wip/recapture-trigger` branch carries a `release_ref_guard` relax (`af75b127`) allowing a branch-ref dispatch. This is a deliberate temporary CI-gate relaxation that MUST be torn down. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208.1-07 | Tampering | wip/recapture-trigger release_ref_guard relax | high | mitigate | Delete the branch at phase close (Task 3); the guard-relax af75b127 must NEVER reach main. Verified via git ls-remote returning nothing. |
| T-208.1-08 | Tampering | board-notice snapshot canary | medium | mitigate | snapshot-canary-guard.sh runs in the recapture job — forbids canary modify, tolerates canary add. The tripwire stays armed across the recapture. |
| T-208.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (no mix deps.get / npm install). RESEARCH Package Legitimacy Audit: Not applicable. |

No security-sensitive application code paths touched: this plan captures baseline PNGs, runs CI, and deletes a throwaway branch. The only security-relevant action is tearing down the temporary CI-gate relax (T-208.1-07), which this plan mandates (ASVS L1 — application V2–V6 N/A per RESEARCH Security Domain).
</threat_model>

<verification>
- 12 board-cfg-* baseline PNGs captured CI-native and merged into the backlog branch
- "Example Playwright smoke (full lifecycle)" GREEN on the backlog branch (all 5 seams)
- admin_checkpoints not regressed
- wip/recapture-trigger deleted; guard-relax af75b127 never on main
- No baseline captured locally on darwin
</verification>

<success_criteria>
The 12 board-cfg baselines are captured CI-native and merged, the real required CI check is green on the backlog branch (the phase exit criterion), and the throwaway guard-relax branch is deleted — leaving the backlog ready for the one-PR ship.
</success_criteria>

<output>
Create `.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-04-SUMMARY.md` when done. Record the green CI run URL/SHA, the 12-PNG merge, and confirmation that wip/recapture-trigger is deleted.
</output>
