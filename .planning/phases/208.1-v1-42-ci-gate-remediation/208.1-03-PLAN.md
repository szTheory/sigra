---
phase: 208.1-v1-42-ci-gate-remediation
plan: 03
type: execute
wave: 3
depends_on:
  - 208.1-02
files_modified:
  - test/example/priv/playwright/tests/organizations.spec.ts
  - test/example/priv/playwright/tests/passkey-login.spec.ts
  - test/example/priv/playwright/tests/demo-showcase.spec.ts
autonomous: true
requirements:
  - 208.1-S2-behavioral-spec-reconciliation
  - 208.1-S4-cite-and-flip-revalidation
user_setup: []

must_haves:
  truths:
    - "organizations.spec.ts:165 and passkey-login.spec.ts:182 pass on --project=chromium"
    - "demo-showcase.spec.ts:306, :866, :894 pass on --project=demo-showcase-chromium (9 personas visible + home stat 'personas'=9 + morgan featured)"
    - "Each non_admin/demo_showcase failure is classified scoping-only vs real regression BEFORE editing (enumerate locally first)"
    - "The 205/206/207/208 cite-and-flip audit claims are re-validated by RUNNING the specs, not re-reading code"
  artifacts:
    - "Reconciled organizations.spec.ts, passkey-login.spec.ts, demo-showcase.spec.ts"
    - "A recorded cite-and-flip re-validation result (real enumeration output) in the SUMMARY"
  key_links:
    - "demo-showcase 9-persona visibility loop uses adminUsersEmailLocator against /admin/users?q=demo.tasklane.test"
    - "home-stat-personas / home-featured-personas testids render '9' and contain morgan@demo.tasklane.test"
---

<objective>
Reconcile the non_admin (organizations, passkey-login) and demo_showcase Playwright specs, AND execute the cite-and-flip re-validation (scope item 4): actually RUN the specs the 205/206/207/208 audits claimed green rather than re-reading code. The 9 demo personas (incl. morgan@demo.tasklane.test) all exist in seed — these are visibility/selector/pagination failures, not missing data.

Purpose: This closes the remaining behavioral lanes and the root-cause discipline gap (cite-and-flip audits never executed the specs — the exact failure mode that produced this debt). The findings flag these as needing local enumeration FIRST to classify scoping-only vs real regression.
Output: 3 reconciled spec files; a recorded real-enumeration result that supersedes the unsound 205/206/207/208 audit claims.

Note: This is the highest-uncertainty plan in the phase (S2+S4, MEDIUM-confidence open questions on classification and cite-and-flip re-validation). The Task 3 escalation routing below is the mitigation — newly-discovered non-trivial in-scope failures STOP and surface to the orchestrator rather than being fixed inline under green-pressure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md
@.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-VALIDATION.md

# Failing specs + the dual-DOM idiom + seed
@test/example/priv/playwright/helpers/adminUsersIndex.ts
@test/example/priv/playwright/tests/organizations.spec.ts
@test/example/priv/playwright/tests/passkey-login.spec.ts
@test/example/priv/playwright/tests/demo-showcase.spec.ts
@test/example/priv/repo/seeds.exs
@lib/sigra/admin/live/users_index_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate + reconcile non_admin specs (organizations + passkey-login) (S2)</name>
  <files>test/example/priv/playwright/tests/organizations.spec.ts, test/example/priv/playwright/tests/passkey-login.spec.ts</files>
  <read_first>
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (Open Question 3 — classify each as selector-scoping vs real regression; Code Examples for the scoping idiom)
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (Local full-suite enumeration recipe)
    - test/example/priv/playwright/tests/organizations.spec.ts:165 — the failing empty-state copy assertion inside the register→…→multi-org flow
    - test/example/priv/playwright/tests/passkey-login.spec.ts:182 — inside the "real options path without leaking browser errors" test
    - test/example/priv/playwright/helpers/adminUsersIndex.ts — the dual-DOM scoping idiom (if the org failure is dual-DOM)
  </read_first>
  <action>
    Per S2 / RESEARCH Open Question 3: enumerate organizations.spec.ts:165 and passkey-login.spec.ts:182 locally FIRST (via the enumeration recipe) to classify each as (a) selector-scoping/empty-state-copy drift or (b) a real flow/regression. Then reconcile:
    - If org :165 is empty-state copy drift, reconcile the spec assertion to the rendered org empty-state copy (or, if the empty-state copy itself regressed against the ratified contract, fix the markup — confirm against the design contract which is authoritative). If it is dual-DOM, apply the `adminUsersEmailLocator` `.filter({visible:true}).first()` idiom.
    - For passkey-login :182, classify whether it is selector-scoping or flow-state timing; reconcile selectors to the rendered UI or stabilize the wait, without weakening the "without leaking browser errors" guard.
    Reconciliation only — no new design. Record the classification (scoping vs regression) for each in the SUMMARY.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/organizations.spec.ts tests/passkey-login.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - organizations.spec.ts and passkey-login.spec.ts pass on --project=chromium
    - Each failure classified scoping-only vs real regression (recorded in SUMMARY)
    - The passkey "without leaking browser errors" guard is preserved
    - Any markup fix conforms to the ratified contract (no new design)
  </acceptance_criteria>
  <done>Both non_admin specs pass on chromium with each failure classified, and no behavioral guard weakened.</done>
</task>

<task type="auto">
  <name>Task 2: Classify + reconcile demo-showcase persona visibility/selectors (S2)</name>
  <files>test/example/priv/playwright/tests/demo-showcase.spec.ts</files>
  <read_first>
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (Open Question 2 — the real failing assertions are INSIDE the cited declaration lines: home stat/morgan at :822-826 in the :306 test; the 9-persona visibility loop at :922 in the :894 test)
    - test/example/priv/playwright/tests/demo-showcase.spec.ts:306, :822-826, :866, :894, :922 — home-stat-personas "9", home-featured-personas contains morgan@demo.tasklane.test, and the per-persona visibility loop via adminUsersEmailLocator on /admin/users?q=demo.tasklane.test
    - test/example/priv/repo/seeds.exs — confirms all 9 demo personas exist (incl. morgan@demo.tasklane.test)
    - lib/sigra/admin/live/users_index_live.ex — default page_size / pagination (does the users list show all 9 in one page or paginate?)
  </read_first>
  <action>
    Per S2 / RESEARCH Open Question 2: classify the 3 demo-showcase failures (the data exists — all 9 personas are seeded). Run the spec locally to determine whether each is visibility, selector, or pagination:
    - **9-persona loop (:922):** if the default `page_size` paginates and hides some of the 9 on `/admin/users?q=demo.tasklane.test`, raise the query specificity or add an explicit `page_size`/page param so all 9 are on one page (the same deterministic approach used elsewhere in the suite), or assert against the page that shows them. Use the `adminUsersEmailLocator` idiom for each email so dual-DOM does not trip strict mode.
    - **home-stat / morgan markers (:822-826):** confirm the `home-stat-personas` testid renders "9" and `home-featured-personas` contains `morgan@demo.tasklane.test` (markup audit); if a marker selector drifted, reconcile the spec to the rendered testid. If the markup itself is wrong against the ratified demo home, fix it minimally.
    Reconciliation only. Record the classification (visibility/selector/pagination) per failure in the SUMMARY.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium</automated>
  </verify>
  <acceptance_criteria>
    - demo-showcase.spec.ts passes on --project=demo-showcase-chromium (all 3 cited tests)
    - The 9-persona visibility loop resolves all 9 seeded personas (no pagination hiding)
    - home-stat-personas renders "9" and home-featured-personas contains morgan@demo.tasklane.test
    - Each failure classified visibility/selector/pagination (recorded in SUMMARY)
  </acceptance_criteria>
  <done>demo-showcase.spec.ts is green with all 9 personas visible and the home stat/morgan markers asserted against the rendered testids.</done>
</task>

<task type="auto">
  <name>Task 3: Cite-and-flip re-validation — RUN the 205/206/207/208 specs (S4)</name>
  <files>test/example/priv/playwright/tests/demo-showcase.spec.ts</files>
  <read_first>
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-RESEARCH.md (Local full-suite enumeration recipe; "Don't Hand-Roll" — re-confirming an audit by RUNNING the spec, not re-reading source)
    - .planning/v1.42-CI-GATE-REMEDIATION-FINDINGS.md (the cite-and-flip root cause — 205/206/207/208 audits read source without executing specs)
    - .planning/phases/208.1-v1-42-ci-gate-remediation/208.1-VALIDATION.md (Re-validate cite-and-flip audits row)
  </read_first>
  <action>
    Per S4 (scope item 4): RE-RUN (not re-read) the specs that the 205/206/207/208 audits claimed green, using the full local enumeration recipe from RESEARCH §"Local full-suite enumeration recipe" — boot the example on :4011 against Docker test PG, run all 5 required-check spec groups with `--update-snapshots` (suppress snapshot noise; behavioral only), then DISCARD snapshot churn (`git checkout -- test/example/priv/playwright/tests && git clean -fdq test/example/priv/playwright/tests`). Record the REAL behavioral result of the enumeration in the SUMMARY — this is the executable evidence that supersedes the unsound cite-and-flip audit claims.

    **Fail-routing for newly-discovered failures (root-cause guard).** If the enumeration surfaces any failure NOT already covered by Plans 01/02/03 Tasks, classify it:
    - **Trivial reconciliation** (a never-before-seen selector-scoping drift or rendered-copy drift, fixable the same way as the rest of this phase — scope to the visible variant / reconcile to rendered copy): fix it inline here, scoping to the visible variant or rendered copy, and record the fix + classification in the SUMMARY.
    - **NON-trivial in-scope behavioral failure** (anything that is NOT a trivial selector/copy reconciliation — e.g. a real flow regression, a markup defect against the ratified contract, a data/seed gap, a timing/flow-state failure needing more than a wait stabilization): **STOP. Do NOT fix it inline (over-editing under green-pressure is the failure mode this phase exists to prevent) and do NOT defer it to Plan 04's CI run.** Surface it to the orchestrator as a plan-revision request (a new task or new plan to resolve it), with the enumeration output and your classification. This is the explicit escalation path — declaring this task done while such a failure is recorded-but-unresolved would re-enter the exact declare-done-on-incomplete-evidence root cause.

    This task RUNS specs; it edits a spec ONLY for the trivial-reconciliation case above. Recording a newly-discovered non-trivial failure in the SUMMARY is NOT sufficient to close this task — it must be resolved inline (trivial) or escalated to the orchestrator (non-trivial). The task cannot be marked done with an unresolved newly-discovered in-scope failure left only as a SUMMARY note.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-user-operations.spec.ts tests/impersonation.spec.ts tests/admin-audit.spec.ts tests/admin-modal-interaction.spec.ts tests/organizations.spec.ts tests/passkey-login.spec.ts --project=chromium && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium</automated>
  </verify>
  <acceptance_criteria>
    - The full local enumeration (all 5 required-check groups) was RUN, not re-read
    - The real behavioral result is recorded in the SUMMARY (supersedes the cite-and-flip claims)
    - Every newly-discovered in-scope failure is RESOLVED, not merely recorded: trivial selector/copy drift fixed inline (and classified in the SUMMARY); any NON-trivial in-scope failure ESCALATED to the orchestrator as a plan-revision request (new task/plan) — NOT fixed inline and NOT deferred to Plan 04's CI run
    - This task is NOT marked done if any newly-discovered in-scope failure remains unresolved (recorded-only in the SUMMARY is insufficient)
    - Snapshot churn discarded (no PNGs committed from darwin)
  </acceptance_criteria>
  <done>The 205/206/207/208 audit claims are re-validated by an actual local enumeration run, the real result is recorded, and every newly-discovered in-scope failure is either reconciled inline (trivial) or escalated to the orchestrator (non-trivial) — none left unresolved as a SUMMARY note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Playwright spec reconciliation + (at most) minimal presentational markup. No untrusted-input boundary added; org/passkey/admin-list routes already guarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208.1-05 | Repudiation | passkey "without leaking browser errors" guard | medium | mitigate | Reconcile selectors/waits only; never weaken the browser-error guard to force green. |
| T-208.1-06 | Tampering | cite-and-flip re-validation discipline | medium | mitigate | Re-validate by RUNNING the spec (executable evidence), never by re-reading source — the documented cause of this debt. Newly-discovered failures are RESOLVED (trivial drift fixed inline, non-trivial escalated to the orchestrator for a plan revision), never absorbed, never deferred to CI, and never left as a recorded-only SUMMARY note — the explicit guard against re-entering declare-done-on-incomplete-evidence. |

No security-sensitive code paths touched: org/passkey/demo flows unchanged in logic; admin routes already guarded; changes are selector-scoping + (minimal) presentational markup (ASVS L1 — V2–V6 N/A per RESEARCH Security Domain).
</threat_model>

<verification>
- organizations, passkey-login green on --project=chromium
- demo-showcase green on --project=demo-showcase-chromium (9 personas + stat/morgan)
- Each failure classified scoping vs regression
- Cite-and-flip re-validation RUN (not re-read); real result recorded; new gaps surfaced
- No behavioral guard weakened
</verification>

<success_criteria>
The non_admin and demo_showcase lanes are green on chromium with every failure classified, AND the 205/206/207/208 cite-and-flip audits are re-validated by an actual enumeration run whose real result is recorded — closing the root-cause discipline gap.
</success_criteria>

<output>
Create `.planning/phases/208.1-v1-42-ci-gate-remediation/208.1-03-SUMMARY.md` when done. Record the per-failure classification AND the cite-and-flip re-validation enumeration result (with any newly-discovered gap).
</output>
