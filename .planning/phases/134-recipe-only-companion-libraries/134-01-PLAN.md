---
phase: 134-recipe-only-companion-libraries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/companion-libs/accrue.md
  - guides/recipes/companion-libs/lockspire.md
  - guides/recipes/companion-libs/relyra.md
  - guides/recipes/companion-libs/rulestead.md
  - mix.exs
autonomous: true
requirements: [RC-03, RC-04, RC-05, RC-06]

must_haves:
  truths:
    - "An adopter reading accrue.md can wire seat-limit gating via before_add_member/4 and lifecycle cleanup via Sigra.Hooks against the Accrue.Auth behaviour, with Sigra owning no seat-limit logic"
    - "An adopter reading lockspire.md can stand up a Lockspire integration honoring ADR 001 (no sigra_lockspire glue package), implementing the 4 required AccountResolver callbacks, cross-linking companion-oauth-provider"
    - "An adopter reading relyra.md can wire SAML 2.0 SP coverage and mint a Sigra session via Sigra.Auth.create_session/4 after the ACS round-trip, with Sigra owning no SAML metadata storage"
    - "An adopter reading rulestead.md can gate a Sigra-protected controller on the real Rulestead.Runtime.enabled?/3 form and derive a RulesteadPolicy implementing can?/4 from current_scope, with Sigra shipping no opinionated authorization"
    - "All four recipes carry the Phase 132 LOCKED template: validated_against HTML-comment block, visible Validated against line, Sigra-works-fully-standalone banner, Failure modes, Non-goals, See also"
    - "mix docs --warnings-as-errors exits 0 and the D-20 banned-phrase grep returns zero matches after all four recipes land and the four skip-warnings entries are removed"
  artifacts:
    - path: "guides/recipes/companion-libs/accrue.md"
      provides: "RC-03 Accrue integration recipe"
      contains: "before_add_member"
    - path: "guides/recipes/companion-libs/lockspire.md"
      provides: "RC-04 Lockspire integration recipe"
      contains: "companion-oauth-provider"
    - path: "guides/recipes/companion-libs/relyra.md"
      provides: "RC-05 Relyra SAML SP integration recipe"
      contains: "Sigra.Auth.create_session/4"
    - path: "guides/recipes/companion-libs/rulestead.md"
      provides: "RC-06 Rulestead feature-flag integration recipe"
      contains: "RulesteadPolicy"
    - path: "mix.exs"
      provides: "ExDoc extras: registration for four recipes + removal of Phase 133 skip-warnings bridge"
      contains: "guides/recipes/companion-libs/accrue.md"
  key_links:
    - from: "guides/recipes/companion-libs/relyra.md"
      to: "lib/sigra/auth.ex:1284 Sigra.Auth.create_session/4"
      via: "session-mint pin (NOT Sigra.Session.create_session/3)"
      pattern: "Sigra\\.Auth\\.create_session/4"
    - from: "guides/recipes/companion-libs/lockspire.md"
      to: "guides/recipes/companion-oauth-provider.md"
      via: "cross-link to architectural recipe"
      pattern: "companion-oauth-provider"
    - from: "guides/recipes/companion-libs/rulestead.md"
      to: "rulestead/lib/rulestead/admin/authorizer.ex:149 can?/4"
      via: "RulesteadPolicy callback pin (NOT authorize/4; NOT enabled?(\"flag\", conn))"
      pattern: "can\\?/4"
    - from: "mix.exs extras:"
      to: "the four new companion-libs recipe files"
      via: "ExDoc registration append after mailglass.md (line 227)"
      pattern: "companion-libs/accrue\\.md"
---

<objective>
Publish the four recipe-only companion-library docs (Accrue, Lockspire, Relyra, Rulestead)
under `guides/recipes/companion-libs/` so the Phase 133 suite-narrative cross-links land on
real recipes, then atomically register them in `mix.exs` `extras:` and remove the four Phase
133 `skip_undefined_reference_warnings_on:` bridge entries.

Purpose: Close RC-03..RC-06. The four recipes are the link targets the v1.29 suite narrative
already points to; until they exist, `mix docs --warnings-as-errors` is held up only by the
four bridge suppressions. This phase lands the recipes AND removes the suppressions in one
atomic edit so ExDoc resolves the cross-links cleanly with no failure window.

Output: Four new `.md` recipe files matching the Phase 132 LOCKED template + one two-block
`mix.exs` edit. NO `lib/` runtime-code changes. This is a DOCS-ONLY phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-recipe-only-companion-libraries/134-CONTEXT.md
@.planning/phases/134-recipe-only-companion-libraries/134-RESEARCH.md
@.planning/phases/134-recipe-only-companion-libraries/134-VALIDATION.md

<critical_research_corrections>
The RESEARCH.md verified every load-bearing pin against the local sister-repo checkouts and
found THREE places where CONTEXT.md / STACK.md are WRONG. Where they conflict on a
callback/function surface, RESEARCH wins (it read the actual `~> 1.2` / `~> 0.1` source).
You MUST encode the corrected facts — never the stale CONTEXT/STACK claims:

1. **Lockspire AccountResolver = 4 required + 2 optional, NOT 5 + 1.**
   `@optional_callbacks [redirect_for_logout: 2, verify_backchannel_user_code: 3]` at
   `account_resolver.ex:36`. REQUIRED: `resolve_current_account/2`, `resolve_account/2`,
   `build_claims/2`, `redirect_for_login/2`. OPTIONAL: `verify_backchannel_user_code/3` (CIBA),
   `redirect_for_logout/2`. CONTEXT D-06 listed `verify_backchannel_user_code/3` as required —
   it is OPTIONAL.

2. **Rulestead `enabled?("flag", conn)` is a FABRICATION — it does not exist.** The REAL
   surfaces: low-level `Rulestead.enabled?/2` = `(flag_payload, context)` at
   `rulestead.ex:1189-1194`; Phoenix convenience form `Rulestead.Runtime.enabled?/3` =
   `(environment_key, flag_key, context)` at README:84-85. README line 111 is the admin MOUNT,
   not an enabled? example. Pin the real adopter-facing form (`Rulestead.Runtime.enabled?/3`),
   cite the low-level `Rulestead.enabled?/2` as the explicit contract it wraps.

3. **Rulestead host policy callback is `can?/4`, NOT `authorize/4`.** The host
   `RulesteadPolicy` implements `can?(actor, action, resource, environment_key) :: boolean()`,
   called at `authorizer.ex:149`, configured via `config :rulestead, :admin_policy`. The
   mounted admin UI also requires a SEPARATE `{:rulestead_admin, "~> 0.1"}` package
   (README:97-98).

CONFIRMED CORRECT (do NOT second-guess):
- **Relyra session-mint:** `Sigra.Auth.create_session/4` at `lib/sigra/auth.ex:1284`
  signature `(config, user, metadata, opts \\ [])`. NEVER `Sigra.Session.create_session/3` —
  that function does not exist (`lib/sigra/session.ex` has no `create_session`). A wrong
  reference ships an UndefinedFunctionError and fails `mix docs --warnings-as-errors`. This is
  the single most consequential pin in the phase.
- **mix.exs edit targets, zero line drift:** `extras:` `mailglass.md` at line 227 (NO trailing
  comma — last element before `]` on line 228). Skip-warnings: Phase 133 comment header
  lines 174-175 + four entries lines 176-179 → delete all six lines; line 173
  (`mailglass.md` skip-warnings entry) currently ends with `,` and becomes the last element
  before `]` after deletion, so its trailing comma must be removed.
</critical_research_corrections>

<interfaces>
<!-- Pinned surfaces the recipes reference BY LINE-RANGE (D-07: never copy sister-lib code). -->
<!-- All verified against local checkouts this session. Pin these exactly. -->

Sigra seams (this repo):
- `lib/sigra/auth.ex:1284` — `Sigra.Auth.create_session/4` = `(config, user, metadata, opts \\ [])` :: `{:ok, Sigra.Session.t()} | {:error, term()}` (Relyra session-mint)
- `lib/sigra/organizations/callbacks.ex:17-18, 38-48` — `before_add_member/4` ("Check seat limits", line 17) + `after_member_remove/2` (Accrue seat gating)
- `lib/sigra/hooks.ex:1-103` — user-lifecycle registry: `on_register / on_password_change / on_email_change / on_delete` (Accrue subscription cleanup)
- `lib/sigra/audit.ex` — `AuditEvent` source-of-truth (Accrue `log_audit/2` is a consumer, not a destination swap)
- `lib/sigra/scope.ex:16-25` — `Sigra.Scope.build/3`; constructs `user / active_organization / membership / impersonating_from`. Full `current_scope` field bus is on the host-generated `%Scope{}` struct (FEATURES.md:26 enumerates `:user, :active_organization, :active_organization_id, :membership, :role, :auth_method, :impersonating_from, :token_id, :id`)
- `lib/sigra/enterprise_connections.ex`, `lib/sigra/enterprise_routing.ex`, `lib/sigra/oauth/enterprise_reconciliation.ex` — v1.27 ENT-SSO OIDC-via-Assent surface (Relyra contrasts SAML against this)

Sister-repo behaviour contracts (local checkouts; pin BY LINE-RANGE, do NOT copy):
- `/Users/jon/projects/accrue/accrue/lib/accrue/auth.ex:41-49` — `Accrue.Auth`: 5 required (`current_user/1`, `require_admin_plug/0`, `user_schema/0`, `log_audit/2`, `actor_id/1`) + 2 optional (`step_up_challenge/2`, `verify_step_up/3`). Config: `config :accrue, :auth_adapter, MyApp.Accrue.Auth`
- `/Users/jon/projects/lockspire/lib/lockspire/host/account_resolver.ex:14-39` — AccountResolver: 4 required (`resolve_current_account/2`@14, `resolve_account/2`@17, `build_claims/2`@20, `redirect_for_login/2`@23) + 2 optional (`verify_backchannel_user_code/3`, `redirect_for_logout/2`; `@optional_callbacks` line 36)
- `/Users/jon/projects/relyra/lib/relyra.ex:6-12, 28` — moduledoc names `start_login/3` + `consume_response/3` returning `%Relyra.LoginResult{}`; `start_login/3` @ line 28-29, `consume_response/3` @ line 150-152
- `/Users/jon/projects/rulestead/rulestead/lib/rulestead.ex:1189-1194` — `enabled?/2` = `(flag_payload, context)` :: `{:ok, boolean()} | {:error, Error.t()}`
- `/Users/jon/projects/rulestead/README.md:84-85` — `Rulestead.Runtime.enabled?/3` = `(environment_key, flag_key, context)`; README:88-89 explicit-contract note; README:97-98 `{:rulestead_admin, "~> 0.1"}`; README:111 admin mount
- `/Users/jon/projects/rulestead/rulestead/lib/rulestead/admin/authorizer.ex:146-150` — host policy call site `policy.can?(actor, action, resource, environment_key)`; actor normalization `%{id, display, roles}` (roles `:viewer | :editor | :admin`) at lines 294-308; config `:rulestead, :admin_policy`

Planning / template references:
- `guides/recipes/companion-libs/threadline.md` (158 lines) — LOCKED template: HTML-comment validated_against (lines 1-2), visible line 5, banner line 7, role table, Prerequisites, mix.exs snippet, contract-by-line-range, Failure modes, Non-goals, See also
- `guides/recipes/companion-libs/mailglass.md` (130 lines) — "Host app only — Sigra does not add <Lib> as a dependency" framing (line 30)
- `guides/recipes/companion-oauth-provider.md` (52 lines, v1.7) — architectural Lockspire recipe; names `mix lockspire.install --sigra-host` (line 38); lockspire.md cross-links via `../companion-oauth-provider.html`, does NOT duplicate
- `.planning/decisions/001-defer-sigra-lockspire-glue-package.md` — ADR 001 deferral trigger (lockspire.md Non-goals quotes it)

Cross-link forms (verified): sibling recipes `./<name>.html`; flows `../flows/<name>.html`; suite narrative `../introduction/suite-integration.html`; architectural recipe `../companion-oauth-provider.html`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write accrue.md (RC-03) — the canary recipe</name>
  <files>guides/recipes/companion-libs/accrue.md</files>
  <read_first>
    - guides/recipes/companion-libs/threadline.md (LOCKED template: section order, banner line 7, validated_against form, role table, Failure modes / Non-goals / See also)
    - guides/recipes/companion-libs/mailglass.md ("Host app only — Sigra does not add <Lib> as a dependency" framing)
    - lib/sigra/organizations/callbacks.ex (lines 11-49 — confirm `before_add_member/4` "Check seat limits" row at line 17 and `after_member_remove/2`; pin BY line-range, do NOT copy)
    - lib/sigra/hooks.ex (lines 1-103 — confirm `on_register / on_password_change / on_email_change / on_delete` registry)
    - lib/sigra/audit.ex (moduledoc only — confirm `AuditEvent` is source-of-truth; Accrue `log_audit/2` is a consumer)
    - /Users/jon/projects/accrue/accrue/lib/accrue/auth.ex (lines 38-90 — confirm 5 required + 2 optional callbacks and `config :accrue, :auth_adapter, ...` wiring at line 7)
  </read_first>
  <action>
    Create `guides/recipes/companion-libs/accrue.md` matching the threadline.md/mailglass.md
    LOCKED template exactly:
    - Lines 1-2: HTML comments `<!-- validated_against: accrue ~> 1.2 -->` then
      `<!-- last_validated: 2026-05-28 -->`.
    - Line 3: H1 `# Recipe: Sigra + Accrue (seat-limit gating + subscription lifecycle)`.
    - Line 5: visible `Validated against: ` + backticked `accrue ~> 1.2` + ` as of 2026-05-28`.
    - Line 7: the banner blockquote generalized per D-08/specifics: `> **Sigra works fully
      standalone.** Accrue is an optional integration; Sigra ships without it, and removing the
      wiring below returns Sigra to standalone operation with no further changes.` (use "wiring"
      since this is multi-step, not a single config entry).
    - `## What this is` role table (default: role table for consistency).
    - `## Prerequisites` — "Sigra organizations + hooks must be green first" framing; Accrue
      host-app-only.
    - `## mix.exs snippet` — `{:accrue, "~> 1.2"}` host-only, state "Host app only — Sigra does
      not add Accrue as a dependency" (mailglass.md:30 precedent).
    - Body: per D-01, make the SPLIT explicit — (a) organizations callbacks = seat gating on
      membership add/remove, pinned by line-range to `lib/sigra/organizations/callbacks.ex:17-18,
      38-48` (`before_add_member/4` "Check seat limits", `after_member_remove/2`); (b)
      user-lifecycle hooks = subscription cleanup on user delete, pinned to `lib/sigra/hooks.ex:1-103`
      (`on_delete`). Per D-02, show the host implementing the `Accrue.Auth` behaviour once
      (pin `/Users/jon/projects/accrue/accrue/lib/accrue/auth.ex:41-49`: 5 required —
      `current_user/1`, `require_admin_plug/0`, `user_schema/0`, `log_audit/2`, `actor_id/1`;
      2 optional — `step_up_challenge/2`, `verify_step_up/3`), configured via
      `config :accrue, :auth_adapter, MyApp.Accrue.Auth`, with seat-gating wired through the
      `before_add_member/4` callback. Per D-03, the `log_audit/2` case cross-links
      `lib/sigra/audit.ex` and the threadline recipe — Sigra `AuditEvent` row stays
      source-of-truth; Accrue's `log_audit/2` consumes identity-event side-effects, NOT a
      destination swap. Per D-04, do NOT invent Accrue webhook payloads — link
      `../flows/audit-logging.html` and show event-type filtering on existing webhook
      subscriptions.
    - `## Failure modes` — numbered `### N.` subsections (e.g. Accrue dep absent at boot;
      seat-limit callback returns `{:error, _}` aborting membership add; `log_audit/2` raising).
    - `## Non-goals` — Sigra owns NO seat-limit logic (owns the seam); no `--with-accrue` install
      flag; Accrue owns billing/subscription state.
    - `## See also` — `../introduction/suite-integration.html`, `./threadline.html`,
      `../flows/audit-logging.html`, sibling recipes as relevant.
    Pin all contracts BY LINE-RANGE REFERENCE (D-07); do NOT copy sister-lib code. Target
    ~120-160 lines. Avoid the four D-20 banned phrases.
  </action>
  <acceptance_criteria>
    - `test -f guides/recipes/companion-libs/accrue.md` succeeds.
    - File line 1 matches `<!-- validated_against: accrue ~> 1.2 -->` and line 2 matches
      `<!-- last_validated: 2026-05-28 -->`.
    - `grep -q "validated_against: accrue ~> 1.2" guides/recipes/companion-libs/accrue.md`.
    - `grep -q "^> \*\*Sigra works fully standalone\.\*\*" guides/recipes/companion-libs/accrue.md`.
    - `grep -q "^## Failure modes" guides/recipes/companion-libs/accrue.md` AND
      `grep -q "^## Non-goals" guides/recipes/companion-libs/accrue.md` AND
      `grep -q "^## See also" guides/recipes/companion-libs/accrue.md`.
    - File contains `before_add_member` AND `Accrue.Auth` AND `on_delete` (or
      `lib/sigra/hooks.ex`) AND references `lib/sigra/audit.ex`.
    - File contains the string `Host app only`.
    - D-20 grep scoped to this file returns zero matches:
      `! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/accrue.md`.
    - File does NOT claim Sigra owns seat-limit logic (Non-goals states Sigra owns the seam, not the logic).
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/recipes/companion-libs/accrue.md && grep -q "^## Failure modes" guides/recipes/companion-libs/accrue.md && grep -q "^## Non-goals" guides/recipes/companion-libs/accrue.md && grep -q "before_add_member" guides/recipes/companion-libs/accrue.md && grep -q "Accrue.Auth" guides/recipes/companion-libs/accrue.md && ! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/accrue.md</automated>
  </verify>
  <done>accrue.md exists matching the LOCKED template, pins the two Sigra seams (organizations callbacks + hooks) and the Accrue.Auth behaviour by line-range, states Sigra owns no seat-limit logic, and carries zero banned phrases.</done>
</task>

<task type="auto">
  <name>Task 2: Write lockspire.md (RC-04)</name>
  <files>guides/recipes/companion-libs/lockspire.md</files>
  <read_first>
    - guides/recipes/companion-libs/threadline.md (LOCKED template)
    - guides/recipes/companion-oauth-provider.md (architectural recipe — lockspire.md COMPLEMENTS and cross-links it via `../companion-oauth-provider.html`; names `mix lockspire.install --sigra-host` at line 38; does NOT duplicate)
    - /Users/jon/projects/lockspire/lib/lockspire/host/account_resolver.ex (lines 1-39 — confirm `@optional_callbacks [redirect_for_logout: 2, verify_backchannel_user_code: 3]` at line 36; 4 required + 2 optional)
    - lib/sigra/scope.ex (lines 16-25 — `Sigra.Scope.build/3`; the stub reads `conn.assigns.current_scope.user`)
    - .planning/decisions/001-defer-sigra-lockspire-glue-package.md (ADR 001 deferral trigger — Non-goals quotes it)
  </read_first>
  <action>
    Create `guides/recipes/companion-libs/lockspire.md` matching the LOCKED template (HTML
    comments lines 1-2: `accrue`→`lockspire`, `validated_against: lockspire ~> 1.2`,
    `last_validated: 2026-05-28`; H1 `# Recipe: Sigra + Lockspire (embedded OAuth/OIDC provider)`;
    visible line 5; banner line 7 with "wiring"; role table; Prerequisites; mix.exs snippet;
    Failure modes; Non-goals; See also).
    - Per D-05: concrete paste-and-go recipe that COMPLEMENTS `companion-oauth-provider.md` —
      cross-link to it (`../companion-oauth-provider.html`), do NOT re-explain the
      architecture-level framing. Show `{:lockspire, "~> 1.2"}` host-only in mix.exs (NOT in
      Sigra deps), `mix lockspire.install --sigra-host` to generate the AccountResolver stub.
    - Per D-06 AS CORRECTED BY RESEARCH §1: pin the AccountResolver contract by line-range to
      `/Users/jon/projects/lockspire/lib/lockspire/host/account_resolver.ex:14-39` and describe
      it as **4 REQUIRED** (`resolve_current_account/2`, `resolve_account/2`, `build_claims/2`,
      `redirect_for_login/2`) **+ 2 OPTIONAL** (`verify_backchannel_user_code/3` for CIBA
      backchannel flows, `redirect_for_logout/2`). MUST NOT describe it as "5 required + 1
      optional" and MUST NOT mark `verify_backchannel_user_code/3` as required.
    - Per D-07: pin the Sigra scope read to `conn.assigns.current_scope.user` per
      `lib/sigra/scope.ex:18-25` — the field the AccountResolver stub reads to answer
      "who is logged in?".
    - Per D-08: Non-goals quotes the ADR 001 deferral trigger (no `sigra_lockspire` glue package
      until both APIs semver-stable AND a public reference app exercises both under CI); also
      "no `--with-lockspire` install flag". Cross-link `../companion-oauth-provider.html` for
      architecture-level framing.
    Pin BY LINE-RANGE (D-07), do NOT copy sister-lib code. Target ~110-150 lines. Avoid D-20
    banned phrases.
  </action>
  <acceptance_criteria>
    - `test -f guides/recipes/companion-libs/lockspire.md` succeeds.
    - Line 1 `<!-- validated_against: lockspire ~> 1.2 -->`; line 2 `<!-- last_validated: 2026-05-28 -->`.
    - `grep -q "companion-oauth-provider" guides/recipes/companion-libs/lockspire.md` (cross-link present).
    - File names `mix lockspire.install --sigra-host` and `current_scope.user`.
    - File describes the AccountResolver as 4 required + 2 optional: contains
      `resolve_current_account`, `resolve_account`, `build_claims`, `redirect_for_login`, and
      labels `verify_backchannel_user_code` AND `redirect_for_logout` as optional.
    - File does NOT contain the string `5 required` and does NOT mark
      `verify_backchannel_user_code` as required (no "required" within the same clause as that
      callback — reviewer confirms it is listed under optional).
    - `grep -q "^## Failure modes" ...` AND `grep -q "^## Non-goals" ...` AND
      `grep -q "^## See also" ...` AND `grep -q "^> \*\*Sigra works fully standalone\.\*\*" ...`.
    - Non-goals references ADR 001 / no `sigra_lockspire` glue package.
    - D-20 grep zero matches:
      `! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/lockspire.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/recipes/companion-libs/lockspire.md && grep -q "companion-oauth-provider" guides/recipes/companion-libs/lockspire.md && grep -q "resolve_current_account" guides/recipes/companion-libs/lockspire.md && grep -q "redirect_for_login" guides/recipes/companion-libs/lockspire.md && ! grep -q "5 required" guides/recipes/companion-libs/lockspire.md && grep -q "^## Non-goals" guides/recipes/companion-libs/lockspire.md && ! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/lockspire.md</automated>
  </verify>
  <done>lockspire.md exists, cross-links companion-oauth-provider, pins the AccountResolver contract as 4 required + 2 optional (RESEARCH §1 correction), pins the Sigra scope read, quotes ADR 001 in Non-goals, and carries zero banned phrases.</done>
</task>

<task type="auto">
  <name>Task 3: Write relyra.md (RC-05) — the load-bearing session-mint pin</name>
  <files>guides/recipes/companion-libs/relyra.md</files>
  <read_first>
    - guides/recipes/companion-libs/threadline.md (LOCKED template)
    - lib/sigra/auth.ex (lines 1270-1314 — confirm `Sigra.Auth.create_session/4` = `(config, user, metadata, opts \\ [])` at line 1284; this is the session-mint seam)
    - /Users/jon/projects/relyra/lib/relyra.ex (lines 1-160 — confirm moduledoc 6-12 names `start_login/3` + `consume_response/3` returning `%Relyra.LoginResult{}`; `start_login/3` @ 28-29, `consume_response/3` @ 150-152. Read the `Relyra.LoginResult` struct definition to pin the documented field for the authenticated subject — do NOT invent a field name; if no precise field is documented, describe the hand-off at the contract level per RESEARCH Open Question 1)
    - lib/sigra/enterprise_connections.ex, lib/sigra/enterprise_routing.ex, lib/sigra/oauth/enterprise_reconciliation.ex (confirm existence — Relyra contrasts SAML against this v1.27 ENT-SSO OIDC-via-Assent surface)
  </read_first>
  <action>
    Create `guides/recipes/companion-libs/relyra.md` matching the LOCKED template
    (`validated_against: relyra ~> 1.2`, `last_validated: 2026-05-28`; H1
    `# Recipe: Sigra + Relyra (SAML 2.0 SP federation)`; banner line 7 with "wiring"; role table;
    Prerequisites; mix.exs snippet `{:relyra, "~> 1.2"}` host-only; Failure modes; Non-goals;
    See also).
    - Per D-09 (THE LOAD-BEARING PIN): pin the Sigra session-mint to
      `Sigra.Auth.create_session/4` (`lib/sigra/auth.ex:1284`), signature
      `(config, user, metadata, opts \\ [])`. The recipe MUST NEVER write
      `Sigra.Session.create_session/3` — that function does not exist and would ship an
      UndefinedFunctionError + fail `mix docs --warnings-as-errors`.
    - Per D-10: pin the Relyra hand-off contract by reference — `Relyra.start_login/3`
      (`/Users/jon/projects/relyra/lib/relyra.ex:28-29`) + `Relyra.consume_response/3`
      (`relyra.ex:150-152`), with the moduledoc (`relyra.ex:6-12`) as the human-readable
      summary. Walkthrough: after the ACS callback, the host reads the authenticated subject
      from the `%Relyra.LoginResult{}` Relyra returns, looks up/creates the Sigra user, then
      mints the session via `Sigra.Auth.create_session/4`. Pin the documented LoginResult field
      read at write-time from the struct; if not precisely documented, describe at the contract
      level (RESEARCH Open Question 1) — do NOT fabricate a field path.
    - Per D-11: inline-rephrase the OIDC-vs-SAML decision matrix in ADOPTER voice (NOT planning
      vocabulary); default to a compact 2-row decision table (OIDC-via-Assent in Sigra /
      SAML-via-Relyra out-of-Sigra). Cross-link the v1.27 ENT-SSO surface
      (`lib/sigra/enterprise_connections.ex`, `lib/sigra/enterprise_routing.ex`,
      `lib/sigra/oauth/enterprise_reconciliation.ex`) and contrast it against Relyra's SAML path.
      Do NOT link `.planning/` paths (maintainer-private).
    - Per D-12: Non-goals enumerate what stays with Relyra — SAML metadata storage, signing keys,
      IdP-initiated SLO, certificate rotation. Sigra owns ONLY the org-scoped session it issues
      after the assertion. Also "no `--with-relyra` install flag".
    Pin BY LINE-RANGE (D-07), do NOT copy sister-lib code. Target ~120-160 lines. Avoid D-20
    banned phrases.
  </action>
  <acceptance_criteria>
    - `test -f guides/recipes/companion-libs/relyra.md` succeeds.
    - Line 1 `<!-- validated_against: relyra ~> 1.2 -->`; line 2 `<!-- last_validated: 2026-05-28 -->`.
    - File contains `Sigra.Auth.create_session/4`.
    - File does NOT contain the string `Sigra.Session.create_session` (any arity). Hard gate.
    - File names `Relyra.start_login/3` (or `start_login/3`) AND `consume_response/3` AND
      `Relyra.LoginResult`.
    - File contains an OIDC-vs-SAML contrast (references the ENT-SSO surface, e.g.
      `enterprise_connections` or `OIDC`, alongside `SAML`).
    - File does NOT contain any `.planning/` link.
    - `grep -q "^## Failure modes" ...` AND `grep -q "^## Non-goals" ...` AND
      `grep -q "^## See also" ...` AND `grep -q "^> \*\*Sigra works fully standalone\.\*\*" ...`.
    - Non-goals enumerates SAML metadata storage / signing keys / SLO / cert rotation as Relyra-owned.
    - D-20 grep zero matches:
      `! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/relyra.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/recipes/companion-libs/relyra.md && grep -q "Sigra.Auth.create_session/4" guides/recipes/companion-libs/relyra.md && ! grep -q "Sigra.Session.create_session" guides/recipes/companion-libs/relyra.md && grep -q "consume_response" guides/recipes/companion-libs/relyra.md && grep -q "^## Non-goals" guides/recipes/companion-libs/relyra.md && ! grep -q ".planning/" guides/recipes/companion-libs/relyra.md && ! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/relyra.md</automated>
  </verify>
  <done>relyra.md exists, pins the session-mint to Sigra.Auth.create_session/4 (and contains zero occurrences of Sigra.Session.create_session), pins the Relyra ACS hand-off by line-range, includes the OIDC-vs-SAML contrast, enumerates Relyra-owned non-goals, and carries zero banned phrases.</done>
</task>

<task type="auto">
  <name>Task 4: Write rulestead.md (RC-06) — three RESEARCH corrections</name>
  <files>guides/recipes/companion-libs/rulestead.md</files>
  <read_first>
    - guides/recipes/companion-libs/threadline.md (LOCKED template)
    - /Users/jon/projects/rulestead/rulestead/lib/rulestead.ex (lines 1185-1195 — confirm `enabled?/2` = `(flag_payload, context)` :: `{:ok, boolean()} | {:error, Error.t()}` at 1189-1194)
    - /Users/jon/projects/rulestead/README.md (lines 80-113 — confirm `Rulestead.Runtime.enabled?/3` = `(environment_key, flag_key, context)` at 84-85; explicit-contract note at 88-89; `{:rulestead_admin, "~> 0.1"}` at 97-98; admin mount `rulestead_admin "/admin/flags", policy: MyApp.RulesteadPolicy` at 111)
    - /Users/jon/projects/rulestead/rulestead/lib/rulestead/admin/authorizer.ex (lines 144-150 — host policy call site `policy.can?(actor, action, resource, environment_key)`; lines 294-308 — actor normalization `%{id, display, roles}`, roles `:viewer | :editor | :admin`)
    - lib/sigra/scope.ex (lines 16-25 — `Sigra.Scope.build/3`; the `current_scope` fields a RulesteadPolicy reads; full field bus per FEATURES.md:26 incl. `:role`)
  </read_first>
  <action>
    Create `guides/recipes/companion-libs/rulestead.md` matching the LOCKED template
    (`validated_against: rulestead ~> 0.1`, `last_validated: 2026-05-28`; H1
    `# Recipe: Sigra + Rulestead (feature-flag gating from current_scope)`; banner line 7 with
    "wiring"; role table; Prerequisites; mix.exs snippet; Failure modes; Non-goals; See also).
    - Per D-13: pin `validated_against: rulestead ~> 0.1`. Surface the `1.0.0 narrative GA vs
      ~> 0.1 Hex line` mismatch as ONE neutral Prerequisites sentence (e.g. "Rulestead's
      published Hex line is `~> 0.1` even though its narrative GA is 1.0.0; pin `~> 0.1`"). No drama.
    - Per D-14 AS CORRECTED BY RESEARCH §2: pin the REAL adopter-facing surfaces. The canonical
      entry point from a Sigra-protected controller is the Phoenix convenience form
      `Rulestead.Runtime.enabled?/3` = `(environment_key, flag_key, context)` (README:84-85),
      building `context` from `conn.assigns` (derived from `current_scope`). Cite the low-level
      `Rulestead.enabled?/2` = `(flag_payload, context)` (`rulestead.ex:1189-1194`) as the
      explicit payload-first contract the Runtime form wraps (README:88-89). The recipe MUST NOT
      contain the fabricated `Rulestead.enabled?("flag", conn)` form anywhere.
    - Per D-15 AS CORRECTED BY RESEARCH §3: show `MyApp.RulesteadPolicy` implementing **`can?/4`**
      = `(actor, action, resource, environment_key) :: boolean()` (pin
      `authorizer.ex:146-150` call site + `authorizer.ex:294-308` actor shape
      `%{id, display, roles}`), configured via `config :rulestead, :admin_policy,
      MyApp.RulesteadPolicy`. The policy derives `roles` from `current_scope.role`
      (FEATURES.md:26 + `lib/sigra/scope.ex:16-25`). The recipe MUST NOT pin `authorize/4` as the
      host contract.
    - Per RESEARCH §3 note: the admin UI requires a SEPARATE `{:rulestead_admin, "~> 0.1"}`
      package. Primary mix.exs snippet shows `{:rulestead, "~> 0.1"}` (core); an OPTIONAL
      admin-mount subsection shows `{:rulestead_admin, "~> 0.1"}` plus the
      `rulestead_admin "/admin/flags", policy: MyApp.RulesteadPolicy` mount (README:111).
    - Non-goals (D-15): Sigra owns NO flag storage, evaluator, or admin UI; no `--with-rulestead`
      install flag. Sigra `current_scope` only supplies the evaluation/authorization context.
    Pin BY LINE-RANGE (D-07), do NOT copy sister-lib code. Target ~120-160 lines. Avoid D-20
    banned phrases.
  </action>
  <acceptance_criteria>
    - `test -f guides/recipes/companion-libs/rulestead.md` succeeds.
    - Line 1 `<!-- validated_against: rulestead ~> 0.1 -->`; line 2 `<!-- last_validated: 2026-05-28 -->`.
    - File contains `Rulestead.Runtime.enabled?/3` AND `Rulestead.enabled?/2` (both layers cited).
    - File does NOT contain the fabricated string `enabled?("flag", conn)`. Hard gate.
    - File contains `RulesteadPolicy` AND `can?/4` (or `can?(`). File does NOT pin `authorize/4`
      as the host policy callback (reviewer confirms `can?` is the host contract).
    - File contains `{:rulestead_admin, "~> 0.1"}` (admin package noted as separate).
    - File references `current_scope` and `:role` (policy derives roles from scope).
    - `grep -q "^## Failure modes" ...` AND `grep -q "^## Non-goals" ...` AND
      `grep -q "^## See also" ...` AND `grep -q "^> \*\*Sigra works fully standalone\.\*\*" ...`.
    - Non-goals states Sigra owns no flag storage / evaluator / admin UI.
    - D-20 grep zero matches:
      `! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/rulestead.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/recipes/companion-libs/rulestead.md && grep -q "Rulestead.Runtime.enabled?/3" guides/recipes/companion-libs/rulestead.md && grep -q "Rulestead.enabled?/2" guides/recipes/companion-libs/rulestead.md && ! grep -qF 'enabled?("flag", conn)' guides/recipes/companion-libs/rulestead.md && grep -q "RulesteadPolicy" guides/recipes/companion-libs/rulestead.md && grep -q "can?" guides/recipes/companion-libs/rulestead.md && grep -q "rulestead_admin" guides/recipes/companion-libs/rulestead.md && grep -q "^## Non-goals" guides/recipes/companion-libs/rulestead.md && ! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/rulestead.md</automated>
  </verify>
  <done>rulestead.md exists, pins the real Rulestead.Runtime.enabled?/3 and Rulestead.enabled?/2 surfaces (never the fabricated enabled?("flag", conn)), pins the RulesteadPolicy can?/4 callback (never authorize/4), notes the separate rulestead_admin package, derives the policy from current_scope, and carries zero banned phrases.</done>
</task>

<task type="auto">
  <name>Task 5: Apply the mix.exs two-block atomic edit (D-16/D-17)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (lines 155-240 — the `docs/0` function: `skip_undefined_reference_warnings_on:` block at 160-180 incl. Phase 133 header 174-175 + entries 176-179, with line 173 `mailglass.md` entry ending in a comma; `extras:` list at 186-228 with `mailglass.md` at line 227 having NO trailing comma before `]` on line 228; `groups_for_extras:` `"Companion Libraries"` regex at line 233 — READ-ONLY)
    - .planning/phases/134-recipe-only-companion-libraries/134-RESEARCH.md (the `mix.exs edit targets` section + Pitfall 5 trailing-comma traps)
  </read_first>
  <action>
    Apply ONE atomic two-block edit to `mix.exs` (D-16). Do NOT touch `groups_for_extras:`
    (the `"Companion Libraries"` regex at line 233 already absorbs the four files — Phase 132
    D-11 LOCKED, read-only).

    BLOCK A — `extras:` append (after `"guides/recipes/companion-libs/mailglass.md"` at line 227):
    - Add a trailing comma to the existing `mailglass.md` line (it currently has NONE — it is the
      last element before the `]` on line 228).
    - Append four entries, in this order, the LAST one (`rulestead.md`) with NO trailing comma:
      `"guides/recipes/companion-libs/accrue.md"`,
      `"guides/recipes/companion-libs/lockspire.md"`,
      `"guides/recipes/companion-libs/relyra.md"`,
      `"guides/recipes/companion-libs/rulestead.md"`.

    BLOCK B — `skip_undefined_reference_warnings_on:` removal:
    - Delete the Phase 133 comment header (lines 174-175) AND the four entries (lines 176-179):
      the `accrue.md`, `lockspire.md`, `relyra.md`, `rulestead.md` skip-warnings entries.
    - After deletion, the `mailglass.md` skip-warnings entry (currently line 173) becomes the
      LAST element before the block's closing `]` (currently line 180). It currently ends with a
      trailing comma — REMOVE that trailing comma so the list does not have a dangling comma
      before `]`.

    Both comma traps (Pitfall 5) are the failure surface: a missed comma on the appended
    `mailglass.md` extras line, or a leftover dangling comma on the `mailglass.md` skip-warnings
    line, breaks `mix compile`. The edit is atomic — recipes (Tasks 1-4) already exist and the
    suppressions are removed in the same change, so no intermediate state ships (D-17).
  </action>
  <acceptance_criteria>
    - `mix compile` succeeds (the comma-trap guard — exit 0, no syntax error). Run with
      `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=dev mix compile` (or default
      env; compilation does not need the DB).
    - `extras:` now contains all four entries:
      `grep -c "guides/recipes/companion-libs/\(accrue\|lockspire\|relyra\|rulestead\).md" mix.exs`
      — the four recipe filenames each appear in the `extras:` list (the skip-warnings entries
      having been removed, the only remaining occurrences are in `extras:`).
    - The four skip-warnings entries are GONE: after the edit, each of the four recipe paths
      appears exactly ONCE in mix.exs (in `extras:`), confirming the duplicate skip-warnings
      occurrence was removed. Verify per file:
      `test "$(grep -c 'companion-libs/accrue.md' mix.exs)" = "1"` (and likewise lockspire,
      relyra, rulestead).
    - The Phase 133 comment header is gone:
      `! grep -q "companion-lib recipes pending Phase 134" mix.exs`.
    - `groups_for_extras:` is unchanged:
      `grep -q '"Companion Libraries": ~r{guides/recipes/companion-libs/.?}' mix.exs`.
  </acceptance_criteria>
  <verify>
    <automated>mix compile && test "$(grep -c 'companion-libs/accrue.md' mix.exs)" = "1" && test "$(grep -c 'companion-libs/rulestead.md' mix.exs)" = "1" && ! grep -q "companion-lib recipes pending Phase 134" mix.exs && grep -q '"Companion Libraries"' mix.exs</automated>
  </verify>
  <done>mix.exs has the four recipes registered in extras: (each filename appearing exactly once), the four Phase 133 skip-warnings entries + comment header removed, both trailing-comma traps handled, groups_for_extras: untouched, and mix compile succeeds.</done>
</task>

<task type="auto">
  <name>Task 6: Phase verification gate — mix docs --warnings-as-errors + D-20 grep + structural headings</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/134-recipe-only-companion-libraries/134-VALIDATION.md (the per-task verification map + the structural-heading loop + the D-20 command)
  </read_first>
  <action>
    Run the three Phase 134 acceptance gates (D-18 step 6, D-20). This task modifies no files; it
    proves the deliverable is validated per VALIDATION.md's "Validated" definition:
    1. `mix docs --warnings-as-errors` exits 0 — the gate Phase 136 PROOF-01 re-runs at close.
       This only resolves cleanly because all four recipes now exist AND the four skip-warnings
       suppressions were removed in Task 5 (D-17 atomic rationale: the Phase 133 suite narrative
       already cross-links the four `.html` targets).
    2. The D-20 banned-phrase grep returns ZERO matches across all four files:
       `rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/accrue.md guides/recipes/companion-libs/lockspire.md guides/recipes/companion-libs/relyra.md guides/recipes/companion-libs/rulestead.md`.
    3. The per-file structural-heading check (VALIDATION.md lines 64-72) passes for all four
       files: each carries `## Failure modes`, `## Non-goals`, the
       `> **Sigra works fully standalone.**` banner, and a `validated_against:` line.
    If any gate fails, FIX the offending recipe/edit (return to the relevant earlier task's
    output) and re-run — do NOT add `@tag :skip`, waivers, or new skip-warnings suppressions
    (Phase 136 PROOF-01 forbids waivers on v1.29 work).
  </action>
  <acceptance_criteria>
    - `mix docs --warnings-as-errors` exits 0 (no undefined-reference warnings, no broken
      autolinks). This is the blocking gate.
    - D-20 banned-phrase grep across ALL FOUR files returns zero matches (exit 1 from `rg`,
      i.e. "no matches"):
      `! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/accrue.md guides/recipes/companion-libs/lockspire.md guides/recipes/companion-libs/relyra.md guides/recipes/companion-libs/rulestead.md`.
    - The structural-heading loop prints NO `FAIL:` line for any of the four files:
      ```
      for f in accrue lockspire relyra rulestead; do
        p="guides/recipes/companion-libs/$f.md"
        grep -q "^## Failure modes" "$p" && grep -q "^## Non-goals" "$p" \
          && grep -q "^> \*\*Sigra works fully standalone\.\*\*" "$p" \
          && grep -q "validated_against:" "$p" || echo "FAIL: $f missing required section"
      done
      ```
    - Each file's `validated_against:` pin is correct: `accrue ~> 1.2`, `lockspire ~> 1.2`,
      `relyra ~> 1.2`, `rulestead ~> 0.1`.
    - No `@tag :skip`, no new `skip_undefined_reference_warnings_on:` entries were added to work
      around a warning.
  </acceptance_criteria>
  <verify>
    <automated>mix docs --warnings-as-errors && ! rg -i "seamlessly|just works|production-ready out of the box|the recommended way" guides/recipes/companion-libs/accrue.md guides/recipes/companion-libs/lockspire.md guides/recipes/companion-libs/relyra.md guides/recipes/companion-libs/rulestead.md && for f in accrue lockspire relyra rulestead; do p="guides/recipes/companion-libs/$f.md"; grep -q "^## Failure modes" "$p" && grep -q "^## Non-goals" "$p" && grep -q "^> \*\*Sigra works fully standalone\.\*\*" "$p" && grep -q "validated_against:" "$p" || { echo "FAIL: $f"; exit 1; }; done</automated>
  </verify>
  <done>mix docs --warnings-as-errors exits 0, the D-20 grep returns zero matches across all four recipes, every recipe carries the required structural headings + banner + correct validated_against pin, and no waivers or new suppressions were added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter ← recipe prose | An adopter copy-pastes wiring guidance from a Sigra recipe into their own auth-adjacent host code. The recipe is the only "input" crossing into the adopter's codebase. |

This is a DOCS-ONLY phase. It ships NO runtime code — four markdown files plus a `mix.exs`
ExDoc-config edit (`extras:` registration + removal of bridge `skip_undefined_reference_warnings_on:`
entries). The Sigra runtime attack surface is unchanged: no new endpoints, plugs, schemas,
queries, env vars, or dependencies are added to the library.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Tampering (guidance integrity) | relyra.md session-mint pin | mitigate | Pin `Sigra.Auth.create_session/4` (`lib/sigra/auth.ex:1284`), never `Sigra.Session.create_session/3` (does not exist). A wrong pin ships an `UndefinedFunctionError` to adopters and an insecure copy-paste. Caught by Task 3 acceptance criterion (`! grep -q "Sigra.Session.create_session"`) + Task 6 `mix docs --warnings-as-errors` autolink gate. |
| T-134-02 | Tampering (guidance integrity) | rulestead.md flag/policy pins | mitigate | Pin the REAL `Rulestead.Runtime.enabled?/3` + `Rulestead.enabled?/2` and the `can?/4` policy callback; forbid the fabricated `enabled?("flag", conn)` and the wrong `authorize/4`. An adopter wiring a misnamed authorization callback could ship a flag/authz gate that silently does not run. Caught by Task 4 acceptance criteria (string-presence + forbidden-string grep) + the `mix docs` autolink gate. |
| T-134-03 | Information disclosure | lockspire.md AccountResolver contract | mitigate | Describe the contract as 4 required + 2 optional (CIBA `verify_backchannel_user_code/3` is OPTIONAL). Mis-marking it required could lead a host to stub a backchannel-code verifier incorrectly; pinning the real `~> 1.2` surface by line-range keeps the OAuth-AS host seam honest. Caught by Task 2 acceptance criterion (`! grep -q "5 required"`). |
| T-134-04 | Tampering (build integrity) | mix.exs two-block edit | mitigate | The atomic add+remove avoids a `mix docs --warnings-as-errors` failure window; the two trailing-comma traps would break `mix compile`. Caught by Task 5 `mix compile` gate + Task 6 `mix docs` gate. |
| T-134-SC | Tampering (supply chain) | npm/pip/cargo/hex installs | accept | This phase installs NO packages. The recipes document host-app-only deps (`accrue`, `lockspire`, `relyra`, `rulestead`, `rulestead_admin`) that the ADOPTER adds to THEIR `mix.exs` — Sigra adds none. No package-legitimacy gate applies because no install task runs in this phase. The recipes pin sister-lib versions via `validated_against:` so the version contract is explicit. |

All four mitigatable threats reduce to a single class — **inaccurate integration guidance** —
and are mitigated by the same two mechanisms: the `validated_against:` line-range pins (verified
against local sister-repo checkouts this session) and the `mix docs --warnings-as-errors`
autolink gate that fails on any broken/undefined reference. No runtime threats are introduced
that this docs phase does not already carry.
</threat_model>

<verification>
Phase-level checks (Task 6 runs these as the gate; Phase 136 PROOF-01 re-runs item 1 at close):

1. `mix docs --warnings-as-errors` exits 0 — all four recipes exist, cross-links resolve, and
   the four Phase 133 skip-warnings suppressions are removed.
2. `mix compile` succeeds — the `mix.exs` two-block edit introduced no comma-trap syntax error.
3. D-20 banned-phrase grep returns zero matches across all four recipe files.
4. Per-file structural-heading check passes for all four: banner, `validated_against:`,
   `## Failure modes`, `## Non-goals`, `## See also`.
5. Each `validated_against:` pin is correct (`accrue ~> 1.2`, `lockspire ~> 1.2`,
   `relyra ~> 1.2`, `rulestead ~> 0.1`).
6. The three RESEARCH corrections are encoded: relyra.md contains `Sigra.Auth.create_session/4`
   and NOT `Sigra.Session.create_session`; lockspire.md describes 4 required + 2 optional (NOT
   "5 required"); rulestead.md pins `Rulestead.Runtime.enabled?/3` + `Rulestead.enabled?/2` +
   `can?/4` and NOT `enabled?("flag", conn)`.
</verification>

<success_criteria>
The phase is complete when ALL five ROADMAP Phase 134 success criteria are TRUE:

1. accrue.md lets an adopter wire seat-limit gating + lifecycle integration through
   `lib/sigra/hooks.ex` against the `Accrue.Auth` behaviour, with Sigra owning no seat-limit
   logic. (Task 1)
2. lockspire.md lets an adopter stand up a Lockspire integration respecting ADR 001 and
   cross-referencing `companion-oauth-provider`. (Task 2)
3. relyra.md lets an adopter wire SAML 2.0 SP coverage citing the v1.27 ENT-SSO OIDC-vs-SAML
   matrix, explicit that Sigra does not own SAML metadata storage. (Task 3)
4. rulestead.md lets an adopter gate a Sigra-protected controller on the real Rulestead
   `enabled?` surface plus derive a `RulesteadPolicy` (`can?/4`) from `current_scope`, with
   Sigra shipping no opinionated authorization. (Task 4)
5. All four recipes carry uniform `validated_against:` + `last_validated:` frontmatter,
   `## Failure modes`, `## Non-goals`, and the "Sigra works fully standalone" banner — matching
   the Phase 132 template. (Tasks 1-4 + Task 6 gate)

Plus the phase gate: `mix docs --warnings-as-errors` exits 0 and the D-20 grep returns zero
matches (Tasks 5-6).
</success_criteria>

<output>
Create `.planning/phases/134-recipe-only-companion-libraries/134-01-SUMMARY.md` when done.
</output>
