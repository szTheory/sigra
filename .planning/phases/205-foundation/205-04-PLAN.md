---
phase: 205-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 205-01
  - 205-02
files_modified:
  - .planning/v1.42-IA-DIAGNOSTIC.md
autonomous: true
requirements:
  - INSTR-03

must_haves:
  truths:
    - ".planning/v1.42-IA-DIAGNOSTIC.md is committed with 3 sections: (1) inventory of 8 admin pages × archetype × L0/L1/L2 blocks; (2) persona-panel pass — 9-cell matrix per page at diagnostic depth; (3) prioritized disposition list (D-12)"
    - "The diagnostic is advisory only — explicitly marked NOT a binding gate; Phase 209 is the binding gate (D-13)"
    - "Prioritization uses coarse Impact×Effort 3-bin each (High/Med/Low), NOT RICE; a kill from any lens auto-promotes to ≥P2 (D-14)"
    - "Disposition table columns match D-14: Surface | Worst verdict | Lenses flagging | Impact | Effort | Priority | Feeds phase"
    - "The 8 admin pages are covered: index_live (Global Overview), organization_live (Org Overview), users_index_live (Users List), user_show_live (User Detail), user_sessions_live (User Sessions), audit_index_live (Audit Index), audit_user_live (Per-User Audit), branding_live (Branding)"
    - "Each page's persona-panel matrix shows all 3 lenses × 3 verdict questions at diagnostic depth (headline findings, not full evidence docs)"
    - "Empty-state observations from zoe (zero panels on user_show_live) and ghost-org (empty org roster on organization_live) are incorporated in the relevant page diagnostics"
    - "Anti-collision (D-07): the diagnostic's disposition table never places bare 0/1/2 in column-4 (uses High/Med/Low and P1/P2/P3, not integer tiers)"
  artifacts:
    - ".planning/v1.42-IA-DIAGNOSTIC.md"
  key_links:
    - "diagnostic -> guides/reference/admin-persona-jtbd-rubric.md (the rubric this diagnostic applies)"
    - "diagnostic disposition list -> Phases 206-210 (Feeds phase column)"
    - "diagnostic page inventory -> admin-design-contract.md archetypes (4 archetype types)"
    - "diagnostic empty-state observations -> zoe@demo.tasklane.test + ghost-org (FIXT-01 entities)"
---

<objective>
Author `.planning/v1.42-IA-DIAGNOSTIC.md` — an advisory up-front persona-panel diagnostic across all 8 admin pages that sequences and prioritizes the downstream component/group/page elevation work in Phases 206-210.

This is a planning artifact (committed to `.planning/`), NOT a reference doc. It applies the `admin-persona-jtbd-rubric.md` instrument (from Plan 01) at diagnostic depth — one pass, headline findings — across all 8 admin pages to produce a prioritized disposition list. The diagnostic is explicitly marked advisory; Phase 209 per-surface scored docs are the binding gate.

Purpose: Without a diagnostic read, Phases 206-210 would sequence work by arbitrary convention rather than actual IA/UX signal. The diagnostic makes the "worst pages" / "kill candidates" / "quick wins" visible before committing to per-phase scopes.

Output: `.planning/v1.42-IA-DIAGNOSTIC.md` (new file in .planning/ root, not in a phases/ subdirectory).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-foundation/205-CONTEXT.md
@guides/reference/admin-persona-jtbd-rubric.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-fractal-scorecard.md
@guides/reference/admin-quality-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .planning/v1.42-IA-DIAGNOSTIC.md — inventory + persona-panel pass + disposition list (D-12, D-13, D-14)</name>
  <files>.planning/v1.42-IA-DIAGNOSTIC.md</files>
  <read_first>
    - guides/reference/admin-persona-jtbd-rubric.md — full file (written in Plan 01); the 3 lenses, 3 verdict questions, keep/tighten/kill scale, and output schema the diagnostic instantiates at diagnostic depth
    - guides/reference/admin-design-contract.md — Overview, List, Detail, Workbench archetypes; "same job → same component" principle; per-archetype composition specs; the D-04 "Confirmed pill kill" exemplar showing how a panel-level kill looks
    - guides/reference/admin-ui-principles.md — GOV.UK service-thinking framing for the IA verdict question; the 3 product-frame personas (platform admin / support investigator / org admin)
    - guides/reference/admin-quality-ledger.md — the current tier state of all 8 L3 pages + 3 L4 flows (all Tier 1 or Tier 2 at current state); the page names as they appear in the ledger (index-live, organization-live, users-index-live, user-show-live, user-sessions, audit-index-live, audit-user-live, branding-live)
    - lib/sigra/admin/live/index_live.ex — skim for Overview archetype block inventory (which L0/L1/L2 blocks it composes)
    - lib/sigra/admin/live/organization_live.ex — skim for Org Overview + member roster + invitations blocks
    - lib/sigra/admin/live/users_index_live.ex — skim for List archetype blocks (filter panel, metric strip, desktop table, mobile card)
    - lib/sigra/admin/live/user_show_live.ex — skim for Detail archetype blocks (identity header, sessions panel, passkey panel, identity panel, orgs panel, confirm overlay)
    - lib/sigra/admin/live/user_sessions_live.ex — skim for Sessions page block inventory
    - lib/sigra/admin/live/audit_index_live.ex — skim for Audit List blocks (filter panel, results table, pagination)
    - lib/sigra/admin/live/audit_user_live.ex — skim for Per-User Audit blocks
    - lib/sigra/admin/live/branding_live.ex — skim for Workbench archetype blocks (tab nav, disclosed panels, color fields, preview rails)
  </read_first>
  <action>
    Create `.planning/v1.42-IA-DIAGNOSTIC.md`. This file lives at the `.planning/` root (not inside a phases/ subdirectory). It is a planning artifact, not a reference doc.

    FRONTMATTER:
    ```
    ---
    phase: v1.42
    type: ia-diagnostic
    status: advisory
    binding_gate: Phase 209 (per-surface scored panel — PAGE-01/PAGE-02)
    authored: 2026-06-28
    instrument: guides/reference/admin-persona-jtbd-rubric.md
    ---
    ```

    ADVISORY NOTE (first section after frontmatter):
    State explicitly that this diagnostic is advisory (early-warning + sequencing input only). Findings at this depth are headline-only (one pass, not full Phase 209 evidence docs). The Phase 209 per-surface panel is the single binding gate. Double-gating is avoided by design (D-13). Late-discovered issues surface in Phase 209 without a roadmap edit.

    SECTION 1: ADMIN PAGE INVENTORY
    Table format: Page | Ledger cell | Archetype | Primary L0/L1/L2 blocks
    8 rows:
    1. Global Overview / index-live / Overview / [stat (L1), task_card (L1), notice (L1), metric-summary-strip (L2 mg-1), task-card-grid (L2 mg-3), alarm-notice-band (L2 mg-4)]
    2. Org Overview / organization-live / Overview + tail / [same Overview blocks as above, plus: organization-member-roster (L2 mg-7), pending-invitations (L2 mg-8)]
    3. Users List / users-index-live / List / [scope_ribbon (L1), applied_chip (L1), summary_chip (L1), filter-panel-applied-chips (L2 mg-2), metric-summary-strip (L2 mg-1), user-results-pagination (L2 mg-5)]
    4. User Detail / user-show-live / Detail / [scope_ribbon (L1), summary_chip (L1), page_back (L1), empty_state (L1), identity-header-summary-facts (L2 mg-9), detail-facts-membership-panels (L2 mg-10), destructive-action-confirmation (L2 mg-11)]
    5. User Sessions / user-sessions / Detail (sessions variant) / [scope_ribbon (L1), audit_row (L1), empty_state (L1), destructive-action-confirmation (L2 mg-11)]
    6. Audit Index / audit-index-live / List (audit variant) / [scope_ribbon (L1), applied_chip (L1), audit_row (L1), filter-panel-applied-chips (L2 mg-2), audit-feed-pagination (L2 mg-6)]
    7. Per-User Audit / audit-user-live / Detail + audit feed / [page_back (L1), scope_ribbon (L1), applied_chip (L1), audit_row (L1), filter-panel-applied-chips (L2 mg-2), audit-feed-pagination (L2 mg-6)]
    8. Branding / branding-live / Workbench / [notice (L1), field_help (L1), empty_state (L1), tab nav (bespoke), disclosed panels (bespoke), color_field (Sigra.Admin.Components), preview_pair (Sigra.Admin.Components)]

    SECTION 2: PERSONA-PANEL PASS (9-cell matrix per page)
    For each of the 8 pages, produce a compact matrix table:
    Columns: Lens | Earning its place? | IA muddy? | Redundant/coherent/least-surprising?
    Rows: platform-admin | support-investigator | org-admin
    Fill at DIAGNOSTIC DEPTH (headline verdict + 1 finding or NONE — searched for: <what>). Use keep/tighten/kill. This is NOT the full Phase 209 scored doc — it is a one-pass read.

    Base the diagnostic on:
    - Knowledge of the real LiveView source code (skimmed in read_first)
    - The admin-design-contract.md archetype specs and "same job → same component" principle
    - The admin-ui-principles.md GOV.UK service-thinking framing
    - Empty-state observations from zoe@demo.tasklane.test (user_show_live all 4 panels empty) and ghost-org (organization_live member roster + invitations empty)
    - Apply the adversarial framing from the rubric: seek the strongest case AGAINST each surface for each lens; if no finding, use NONE — searched for: <what>

    Guidelines for diagnostic depth:
    - Earning-its-place: flag any element that duplicates information already visible in shell chrome or in an adjacent section
    - IA muddy: flag any surface where the page posture fights the archetype (e.g. List page starting with metrics rather than the search affordance, or Detail page with no scope_ribbon)
    - Redundant/coherent: flag any label, copy, or section that diverges from a sibling surface doing the same job

    Key observations to incorporate from context:
    - Global Overview (index-live): task_card grid is the primary posture (triage-first, correct); notice alarm band renders even on "all clear" states — note for platform-admin lens
    - Users List (users-index-live): search-first composition after Phase 201 (correct); metric strip demoted (correct); quick-filter chips below panel vs. inline with search (note)
    - User Detail (user-show-live): zoe@demo.tasklane.test drives empty_state on all 4 panels simultaneously — the diagnostic should note that the empty-entity path is now testable; observe whether the empty_state copy is page-consistent
    - Org Overview (organization-live): ghost-org drives zero-member roster + zero-invitations on the scoped-detail tail — note whether empty_state renders or the section is absent
    - Branding (branding-live): Workbench archetype; not a List or Detail; org-admin lens gets a clean 403 (no overreach available)
    - User Sessions (user-sessions): owns the destructive-action confirmation (mg-11) — support-investigator lens has revocation flow; note clarity of consequence copy
    - Audit pages: filter disclosure (More filters) is a progressive-reveal pattern — support-investigator lens should note whether the power-user path is accessible without scan cost

    SECTION 3: PRIORITIZED DISPOSITION LIST
    Table columns: Surface | Worst verdict | Lenses flagging | Impact | Effort | Priority | Feeds phase

    Rows: One row per finding-cluster that rises to ≥P3. Apply D-14 rules:
    - Impact = number of lenses flagging + worst verdict weight (kill=3, tighten=2, keep=1)
    - Effort = surface area touched (L1 component = Low, whole-page recompose = High)
    - kill from any lens → auto-promotes to ≥P2
    - Priority bins: P1 (High impact, Low effort — quick wins + kills), P2 (High impact, High effort — kills + deep rewrites), P3 (Low impact, Low effort — easy polish)
    - Feeds phase: which of 206-210 this finding sequences into

    ANTI-COLLISION: Do NOT use bare integers (0, 1, 2) in table column-4. Use High/Med/Low, keep/tighten/kill, P1/P2/P3 as column values. The disposition table's column-4 (if it holds "Worst verdict") must contain verdict text, not tier integers.

    Target: 8-15 disposition rows (one per significant finding cluster), ordered P1 → P2 → P3. Every finding that is NOT keep across all lenses should appear.
  </action>
  <verify>
    <automated>
      test -f .planning/v1.42-IA-DIAGNOSTIC.md
      grep -c "advisory\|ADVISORY\|not a binding gate\|Phase 209" .planning/v1.42-IA-DIAGNOSTIC.md
      grep -c "index-live\|organization-live\|users-index-live\|user-show-live" .planning/v1.42-IA-DIAGNOSTIC.md
      grep -c "platform.admin\|support.investigator\|org.admin" .planning/v1.42-IA-DIAGNOSTIC.md
      grep -c "Feeds phase\|feeds.phase" .planning/v1.42-IA-DIAGNOSTIC.md
    </automated>
  </verify>
  <done>
    `.planning/v1.42-IA-DIAGNOSTIC.md` is committed; advisory notice is prominently placed; Section 1 covers all 8 admin pages with archetype + L0/L1/L2 block inventory; Section 2 contains 3-lens × 3-question matrices for all 8 pages with keep/tighten/kill verdicts at diagnostic depth; Section 3 contains a prioritized disposition list with the "Feeds phase" column populated for 206-210. No bare integer in table column-4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostic doc → downstream phases | Planning artifact only — advisory signal, no CI gate; trust boundary is the Phase 209 binding gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-11 | Tampering | IA-DIAGNOSTIC.md — column-4 integer collision | low | mitigate | D-07: diagnostic explicitly uses High/Med/Low + P1/P2/P3 + keep/tighten/kill in tables; no bare 0/1/2 integer in column-4 |
| T-205-12 | Repudiation | Advisory diagnostic mistaken for binding gate | low | mitigate | D-13: advisory notice is the first substantive content in the file; binding_gate field in frontmatter; Phase 209 is named explicitly |
| T-205-13 | Information Disclosure | Diagnostic reveals internal UX weaknesses | low | accept | Planning artifact in .planning/ — not shipped to HexDocs or public docs; accessible only to maintainers |
</threat_model>

<verification>
1. `test -f .planning/v1.42-IA-DIAGNOSTIC.md` exits 0
2. File contains "advisory" framing + explicit "Phase 209 is the binding gate" statement (D-13)
3. All 8 admin page ledger cell names appear: index-live, organization-live, users-index-live, user-show-live, user-sessions, audit-index-live, audit-user-live, branding-live
4. All 3 lens names appear: platform-admin, support-investigator, org-admin
5. All 3 verdict question tokens appear: "Earning its place", "IA muddy", "Redundant"
6. Disposition table has "Feeds phase" column
7. D-07 anti-collision: `grep -E '^\| [a-z]' .planning/v1.42-IA-DIAGNOSTIC.md | awk -F'|' '{t=$4; gsub(/ /,"",t); if (t ~ /^[012]$/) print}' | wc -l` returns 0
8. "zoe" or "zero-state" appears in the diagnostic (D-16/D-17 empty-entity observations incorporated)
9. "ghost-org" or "ghost org" or "empty org" appears in the diagnostic
</verification>

<success_criteria>
- `.planning/v1.42-IA-DIAGNOSTIC.md` committed with all 3 required sections (inventory, persona-panel matrices, disposition list)
- Advisory status is unambiguous (not a binding gate; Phase 209 is the gate)
- Disposition table column ordering matches D-14 specification exactly
- kill-from-any-lens auto-promotion to ≥P2 applied correctly throughout
- No bare integer in any table column-4 (D-07 anti-collision)
- Empty-entity state observations (zoe, ghost-org) incorporated in the relevant page diagnostic entries
- "Feeds phase" column populated for every row, pointing to 206-210
</success_criteria>

<output>
Create `.planning/phases/205-foundation/205-04-SUMMARY.md` when done
</output>
