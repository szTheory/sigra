---
phase: 205-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-persona-jtbd-rubric.md
  - guides/reference/admin-fractal-scorecard.md
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - INSTR-01

must_haves:
  truths:
    - "guides/reference/admin-persona-jtbd-rubric.md exists and defines 3 lenses (platform-admin / support-investigator / org-admin) bound by entry-point+intent to flow-platform-admin / flow-support-investigator / flow-org-admin ledger cells (D-02)"
    - "The rubric documents 3 fixed refutation-prompt verdict questions mapped to the milestone's named failure modes: earning-its-place, IA-muddy, redundant-coherent-least-surprising (D-03)"
    - "Adversarial anti-rubber-stamp framing and forced-finding floor (NONE — searched for: <what>) are present in the standing rubric instruction (D-04)"
    - "The keep/tighten/kill ordinal scale is defined with anchors and worst-verdict-across-3-lenses disposition rule (D-05)"
    - "Fixed output schema is specified: YAML frontmatter (surface, ledger_cell, rubric_version, disposition, 9-key verdicts map, findings list) + Markdown refutation-log body (D-06)"
    - "D-07 anti-collision: rubric explicitly states it never places bare 0/1/2 integers in table column-4; relationship-to-quality-ledger section present"
    - "Bidirectional cross-reference pointers exist: scorecard points near L4 Flow add-on; ledger points near flow-* rows (D-01)"
    - "IN-04: ledger Terminal-Ratification prose no longer hardcodes stale '200-204' — replaced with dated note / ROADMAP pointer"
  artifacts:
    - guides/reference/admin-persona-jtbd-rubric.md
  key_links:
    - "rubric <-> fractal scorecard (scorecard references rubric near L4 add-on block, ~line 104-121)"
    - "rubric <-> quality ledger (ledger references rubric near flow-* rows ~lines 93-95)"
    - "rubric output schema -> Phase 209 per-surface docs (template the 8 instantiations)"
---

<objective>
Author `guides/reference/admin-persona-jtbd-rubric.md` as the adversarial persona/JTBD judge instrument for v1.42. The rubric formalizes the 3 admin-UI lenses (platform admin / support investigator / org admin) already implicit in the fractal scorecard and quality ledger, making them explicit with entry-point bindings, refutation-prompt verdict questions, and a fixed machine-rollup-able output schema. Add bidirectional cross-reference pointers into the fractal scorecard and quality ledger. Fix the stale "200-204" Terminal-Ratification prose in the ledger (IN-04).

Purpose: Without a rubric, each downstream phase's persona-panel pass (206-210) would differ in structure, making verdicts non-comparable and rubber-stamping likely. The rubric is the measuring instrument.

Output: `guides/reference/admin-persona-jtbd-rubric.md` (new), targeted edits to `admin-fractal-scorecard.md` (cross-ref block near L4 add-on) and `admin-quality-ledger.md` (cross-ref block near flow-* rows + IN-04 prose fix).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-foundation/205-CONTEXT.md
@guides/reference/admin-fractal-scorecard.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author admin-persona-jtbd-rubric.md</name>
  <files>guides/reference/admin-persona-jtbd-rubric.md</files>
  <read_first>
    - guides/reference/admin-fractal-scorecard.md — full file; use the per-level add-on block structure as the authoring template; note the Tier Vocabulary / Shared Dimensions / Per-Level Add-ons / Tier-2 Add-on organization
    - guides/reference/admin-ui-principles.md — Product Frame section (the 3 personas the lenses formalize)
    - guides/reference/admin-design-contract.md — page archetypes (the IA-diagnostic inventory axis the rubric questions target)
    - test/example/lib/example/demo/personas.ex — lens binding data: admin (platform + investigator), morgan (org_admin: :acme), target personas dave/frank/grace/carol
    - guides/reference/admin-quality-ledger.md — flow-* rows (~lines 93-95) to understand the L4 ledger cell binding; Terminal-Ratification prose (~lines 97-113) for IN-04 fix context
    - prompts/Phoenix Auth Library — Jobs to Be Done, Personas & User Flows.md — the integrator personas (A-E); the rubric must explicitly distinguish admin lenses from integrator personas
  </read_first>
  <action>
    Create `guides/reference/admin-persona-jtbd-rubric.md` as a sibling reference doc mirroring the structure of `admin-fractal-scorecard.md`. Use this section order:

    1. Opening paragraph: state what the rubric is (adversarial persona/JTBD judge instrument for admin surfaces), what it is NOT (a competing tier system or new persona set), and its relationship to `admin-fractal-scorecard.md` (complementary — scorecard grades visual/technical quality; rubric grades UX fitness-for-purpose from 3 named admin-operator lenses).

    2. IMPORTANT DISAMBIGUATION section: explicitly state that the 3 admin lenses (platform admin / support investigator / org admin) are *admin UI operator lenses* bound to demo personas, DISTINCT from the integrator personas (A-E) in the JTBD prompt. Reviewers must not conflate them.

    3. LENS DEFINITIONS table (3 rows): per D-02, bind each lens 1:1 by entry-point + intent (not credentials):
       - Platform admin → `admin` persona, entry `/admin`, posture "triage" (what needs attention, where next?), ledger cell `flow-platform-admin`
       - Support investigator → `admin` acting on a target (dave/frank/grace/carol), entry `/admin/users/:id`, posture "investigate" (find→audit→impersonate→return-with-banner), ledger cell `flow-support-investigator`
       - Org admin → `morgan` (`org_admin: :acme`, non-platform), entry `/admin/organizations/:slug`, posture "bound" (tenant-only; clean 403 on overreach), ledger cell `flow-org-admin`

    4. VERDICT SCALE section: per D-05, define 3-point ordinal scale keep/tighten/kill with one-line anchors + worked examples mirroring how scorecard defines Tier 0/1/2. State that element disposition = worst verdict across the 3 lenses (any kill is a kill).

    5. THREE VERDICT QUESTIONS section: per D-03, document each question as a refutation prompt with its target failure mode:
       - Question 1 "Earning its place?": target = verbosity/info-dump; probe = "name one element NOT earning its place for this lens; if none, say so explicitly with what you searched for"
       - Question 2 "Is the IA muddy?": target = IA/hierarchy; probe = "where does general→specific hierarchy break, or the next action is not obvious? point to the exact element"
       - Question 3 "Redundant / coherent / least-surprising?": target = redundancy/coherence; probe = "name one place this says the same thing twice, diverges from a sibling surface doing the same job, or would surprise the operator"

    6. ADVERSARIAL FRAMING & FORCED-FINDING FLOOR section: per D-04, document the standing rubric instruction verbatim. Include: (a) "find the strongest case AGAINST this surface for this lens"; (b) a `keep` with zero findings is valid only after actively trying to find a fault and stating what was searched for; (c) every (lens × question) cell holds either a cited element OR the literal token `NONE — searched for: <what>`; (d) every finding must cite a concrete DOM/section anchor, never a vibe.

    7. OUTPUT SCHEMA section: per D-06, document the fixed machine-rollup-able output schema:
       - YAML frontmatter keys: `surface`, `ledger_cell`, `rubric_version`, `disposition` (clean/actionable/blocked = worst across cells), `verdicts` (9-key map: lens_name × question_key → keep/tighten/kill), `findings` (list of actionable rows: element, refutation, disposition_action)
       - Markdown body: per-lens refutation log (one section per lens, ordered platform-admin / support-investigator / org-admin)
       - Note: `disposition` rollup rule: any `kill` finding → `blocked`; any `tighten` and no kill → `actionable`; all `keep` → `clean`

    8. RELATIONSHIP TO QUALITY LEDGER section: per D-07, explicitly state:
       (a) The quality ledger's column-4 integer parse (`awk -F'|'`) matches bare 0/1/2 only; the rubric uses keep/tighten/kill + clean/actionable/blocked and MUST NEVER place a bare integer in table column-4.
       (b) The roll-up index (`v1.42-PERSONA-JTBD-PANEL.md`, a Phase 209 artifact) is a plain table (surface | disposition | kill-count | tighten-count | links), deliberately NOT the ledger's integer-parse format.
       (c) Cross-reference pointer: see `admin-fractal-scorecard.md → L4 Flow Add-ons` (the flows the lenses test) and `admin-quality-ledger.md → flow-* rows` (the L4 cells where findings land).

    Tone and formatting: match `admin-fractal-scorecard.md` — markdown headings, pipe-delimited tables, code-formatted identifiers, no emojis. The rubric is a reference doc, not a tutorial.

    ANTI-COLLISION: do NOT place any bare integer (0, 1, 2) in the 4th pipe-delimited column of any markdown table in this file (per D-07 guard parse collision constraint).
  </action>
  <verify>
    <automated>
      test -f guides/reference/admin-persona-jtbd-rubric.md
      grep -c "platform.admin\|support.investigator\|org.admin" guides/reference/admin-persona-jtbd-rubric.md
      grep -c "keep.*tighten.*kill\|keep / tighten / kill\|keep/tighten/kill" guides/reference/admin-persona-jtbd-rubric.md
      grep -c "Earning its place\|IA muddy\|Redundant.*coherent\|redundant.*coherent" guides/reference/admin-persona-jtbd-rubric.md
      grep -c "NONE.*searched for\|forced.finding" guides/reference/admin-persona-jtbd-rubric.md
      grep -c "clean.*actionable.*blocked\|clean/actionable/blocked" guides/reference/admin-persona-jtbd-rubric.md
    </automated>
  </verify>
  <done>
    File exists; all 6 grep counts above return ≥ 1; file contains distinct sections for lens definitions, verdict scale, verdict questions, adversarial framing, output schema, and quality-ledger relationship.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add bidirectional cross-reference pointers (scorecard + ledger) and fix IN-04 Terminal-Ratification prose</name>
  <files>guides/reference/admin-fractal-scorecard.md, guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-fractal-scorecard.md — full file; the L4 Flow Add-ons section (~lines 103-122) is the cross-ref insertion point; must read lines around line 121 to find the exact insertion boundary
    - guides/reference/admin-quality-ledger.md — full file; the L4 flow-* rows (~lines 93-95) are the second cross-ref insertion point; Terminal-Ratification section (~lines 97-113) contains the stale "200-204" prose (IN-04)
  </read_first>
  <action>
    Edit `admin-fractal-scorecard.md`: Insert a cross-reference block immediately AFTER the L4 Flow Add-ons section (after the last bullet in the "Light/Dark/System persists across flow" and "Deterministic fixture" bullets, before the `### Tier-2 Award-grade Add-on` heading). The block should read:

    "### Persona-JTBD Rubric (Cross-Reference)

    The 3 L4 flow cells (`flow-platform-admin`, `flow-support-investigator`, `flow-org-admin`) have corresponding admin-operator lenses in the Persona-JTBD Rubric. That rubric provides the adversarial refutation-prompt verdict questions (earning-its-place / IA-muddy / redundant-coherent-least-surprising), the keep/tighten/kill ordinal scale, and the fixed YAML output schema for Phase 209 per-surface evidence docs. See [`admin-persona-jtbd-rubric.md`](admin-persona-jtbd-rubric.md)."

    Edit `admin-quality-ledger.md`:

    (a) INSERT a cross-reference block immediately AFTER the 3 flow-* rows (after `flow-org-admin` row, before the `## Terminal Ratification` heading). Block:

    "## Persona-JTBD Rubric (Cross-Reference)

    The L4 `flow-*` cells are the ledger binding points for the Persona-JTBD Rubric. Each lens maps 1:1 to an L4 flow cell. Per-surface panel verdicts (Phase 209) instantiate the rubric output schema and roll up into `v1.42-PERSONA-JTBD-PANEL.md` (a Phase 209 artifact). See [`admin-persona-jtbd-rubric.md`](admin-persona-jtbd-rubric.md)."

    (b) IN-04 FIX: In the Terminal-Ratification section, locate the sentence that hardcodes the phase range "200-204" (approximately reads "Ratcheting individual surfaces to Tier 2 begins in Phases 200-204") and replace it with a dated note that does not hardcode future phase numbers. Replace with: "Ratcheting individual surfaces to Tier 2 began in Phases 200-204 (v1.41 ADMIN-DS-ELEVATION milestone, completed 2026-06-27). v1.42 ADMIN-DS-ELEVATION continues this work in Phases 205-211; see `.planning/ROADMAP.md` for the current phase map."

    Use Edit (scoped replacement) for both files — never Write the entire file. Two separate Edit calls or multiple targeted edits.
  </action>
  <verify>
    <automated>
      grep -c "admin-persona-jtbd-rubric.md" guides/reference/admin-fractal-scorecard.md
      grep -c "admin-persona-jtbd-rubric.md" guides/reference/admin-quality-ledger.md
      grep -v "200-204" guides/reference/admin-quality-ledger.md | grep -c "Tier 2 begins" || true
      grep -c "completed 2026-06-27\|2026-06-27.*v1.41\|see.*ROADMAP" guides/reference/admin-quality-ledger.md
    </automated>
  </verify>
  <done>
    Both reference files contain a pointer to `admin-persona-jtbd-rubric.md`; the Terminal-Ratification section no longer contains a bare "200-204" forward reference without a dated completion note; the ledger's column-4 integer values remain bare integers (no decorators introduced by the edit).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc authoring → CI | Markdown changes go through CI but are not machine-executed — no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-01 | Tampering | admin-quality-ledger.md column-4 | medium | mitigate | D-07: rubric explicitly prohibits bare integers in table column-4; enforced by existing monotonic guard parse (grep -E '^\| [a-z]' + awk tier ~ /^[012]$/) |
| T-205-02 | Information Disclosure | IN-04 stale forward-phase reference | low | mitigate | Replace hardcoded "200-204" with dated completion note + ROADMAP pointer per IN-04 fix |
| T-205-03 | Denial of Service | Large markdown doc breaking CI doc-lint | low | accept | No automated doc-lint gate currently; prose-only change has no executable surface |
</threat_model>

<verification>
1. `test -f guides/reference/admin-persona-jtbd-rubric.md` exits 0
2. `grep -c "admin-persona-jtbd-rubric.md" guides/reference/admin-fractal-scorecard.md` ≥ 1
3. `grep -c "admin-persona-jtbd-rubric.md" guides/reference/admin-quality-ledger.md` ≥ 1
4. D-07 anti-collision: `grep -E '^\| [a-z]' guides/reference/admin-persona-jtbd-rubric.md | awk -F'|' '{t=$4; gsub(/ /,"",t); if (t ~ /^[012]$/) print}' | wc -l` returns 0 (no bare integer column-4 rows in rubric)
5. IN-04: `grep -c "200-204" guides/reference/admin-quality-ledger.md` returns 0 OR the matching line also contains a completion-date mention (2026-06-27)
6. `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 (no tier regressions from the ledger prose edit)
</verification>

<success_criteria>
- `guides/reference/admin-persona-jtbd-rubric.md` is committed, ≥ 100 lines, contains all 6 required sections (lens definitions, verdict scale, verdict questions, adversarial framing, output schema, ledger relationship)
- Both `admin-fractal-scorecard.md` and `admin-quality-ledger.md` contain a link to the rubric file
- Ledger Terminal-Ratification prose updated (IN-04): the stale "200-204" hardcode is replaced with a dated completion note
- Monotonic guard exits 0 on the ledger edit (no column-4 integer decreased)
- No bare integer appears in column-4 of any table in `admin-persona-jtbd-rubric.md`
</success_criteria>

<output>
Create `.planning/phases/205-foundation/205-01-SUMMARY.md` when done
</output>
