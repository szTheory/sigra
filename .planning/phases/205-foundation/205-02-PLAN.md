---
phase: 205-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example/demo/personas.ex
  - test/example/lib/example/demo/seeds.ex
  - test/example/test/example/demo/seeds_test.exs
  - scripts/ci/quality-ledger-monotonic.test.sh
autonomous: true
requirements:
  - FIXT-01

must_haves:
  truths:
    - "Personas.all/0 returns 10 personas (9 existing + zoe); demo_users == length(Personas.all()) invariant holds in seeds_test.exs (D-17)"
    - "zoe@demo.tasklane.test is a confirmed user with zero MFA/passkey/identity/org/sessions/audit, seeded idempotently (D-16)"
    - "ghost-org (slug: ghost-org, zero members/invitations/enterprise/audit) is seeded via seed_organizations/0 using upsert_organization/2 pattern (D-16)"
    - "One i18n/RTL overflow user is in the loadtest- cohort with multi-byte CJK + RTL + diacritics + emoji in display name, OUT of Personas.all/0 (D-18)"
    - "Seeds.bulk_cohort_size/0 public function exists; seeds_test.exs references it instead of a hardcoded literal (IN-02)"
    - "@seconds_per_day 86_400 module attribute extracted in seeds.ex; 3 DateTime.add call-sites use it (IN-03)"
    - "IN-01 comment added to seed_bulk_users/0 confirm-branch explaining partial-cohort recovery"
    - "seeds_test.exs: zero-state invariant test for zoe (0 sessions, 0 identities, 0 orgs, 0 audit); zero-membership invariant test for ghost-org (IN-05 via D-17)"
    - "quality-ledger-monotonic.test.sh: Test C (1→2 increase exits 0) added; Test D (decorated column-4 e.g. '2*' false-pass guard) added (IN-05)"
    - "mix test test/example/test/example/demo/seeds_test.exs passes with 0 failures"
    - "D-19 determinism: no DateTime.utc_now / faker / randomness in new seeds; all values derived from @seed_reference_ts or deterministic hashes"
  artifacts:
    - "Seeds.bulk_cohort_size/0 — new public function in test/example/lib/example/demo/seeds.ex"
    - "@seconds_per_day — new module attribute in seeds.ex"
    - "zoe persona entry in Personas.all/0 and feature_map/0"
    - "ghost-org entry in seed_organizations/0"
    - "i18n/RTL loadtest user in seed_bulk_users/0 loop"
    - "Test C and Test D in quality-ledger-monotonic.test.sh"
  key_links:
    - "Personas.all/0 -> seeds.ex seed_users/0 -> seeds_test.exs demo_users == length(Personas.all())"
    - "Seeds.bulk_cohort_size/0 -> seeds_test.exs @bulk_cohort_size reference"
    - "ghost-org -> seeds_test.exs zero-membership invariant"
    - "zoe -> seeds_test.exs zero-state invariant + /admin/users row count (46 total)"
---

<objective>
Add the FIXT-01 edge/empty fixtures and fold in the IN-01…IN-05 hardening items from the Phase 199 code-review todo. Specifically: add the zoe@demo.tasklane.test zero-state persona to `Personas.all/0` (and all SSoT lockstep touchpoints), add ghost-org (zero-member org) to `seed_organizations/0`, add one i18n/RTL overflow user to the loadtest- cohort, expose `Seeds.bulk_cohort_size/0`, extract `@seconds_per_day`, add IN-01 confirm-branch comment, and extend `quality-ledger-monotonic.test.sh` with Test C (1→2 exits 0) and Test D (decorated column-4 false-pass guard).

Purpose: Live admin pages (`/admin/users`, `/admin/users/:id`, `/admin/audit`) currently have no navigable zero-state because every seeded entity is populated. Zoe + ghost-org give the downstream phases real empty-entity first-run surfaces to test — distinct from "filter to nothing" (D-15). The IN-* items harden the fixture/guard substrate the entire v1.42 milestone builds on.

Output: modified `personas.ex`, `seeds.ex`, `seeds_test.exs`; modified `quality-ledger-monotonic.test.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-foundation/205-CONTEXT.md
@test/example/lib/example/demo/personas.ex
@test/example/lib/example/demo/seeds.ex
@test/example/test/example/demo/seeds_test.exs
@scripts/ci/quality-ledger-monotonic.test.sh
@.planning/todos/pending/2026-06-25-phase199-code-review-info-hardening.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add zoe persona to Personas.all/0 and feature_map/0 (D-16, D-17)</name>
  <files>test/example/lib/example/demo/personas.ex</files>
  <read_first>
    - test/example/lib/example/demo/personas.ex — full file; understand the map shape: email/display_name/password/confirmed/totp/passkey/locked/scheduled_deletion/identity_github/org_owner/org_admin/org_member; the print_credentials loop references feature_map/0 by local part
    - test/example/test/example/demo/seeds_test.exs — the `demo_users == length(Personas.all())` assertion at ~line 125 and ~line 148 (BOTH queries must count zoe after she's added)
  </read_first>
  <behavior>
    - Personas.all/0 returns a list of length 10 after the change (was 9)
    - The new persona map for zoe has: email: email("zoe"), display_name: "Zoe", password: "ZoeDemoPass1!Empty", confirmed: true, totp: false, passkey: false, locked: false, scheduled_deletion: false, identity_github: false, org_owner: nil, org_admin: nil, org_member: nil
    - feature_map/0 includes key "zoe" with value describing her zero-state purpose
    - length(Personas.all()) == 10 is directly testable
  </behavior>
  <action>
    In `test/example/lib/example/demo/personas.ex`:

    1. Add a new persona map for zoe at the END of the list in `all/0` (after grace). The map must have ALL the same keys as the other personas:
       - `email: email("zoe")` — resolves to "zoe@demo.tasklane.test"
       - `display_name: "Zoe"`
       - `password: "ZoeDemoPass1!Empty"` — policy-passing, deterministic
       - `confirmed: true` — she is confirmed (to be navigable in /admin/users)
       - `totp: false, passkey: false, locked: false, scheduled_deletion: false, identity_github: false`
       - `org_owner: nil, org_admin: nil, org_member: nil`

    2. Add key "zoe" to `feature_map/0` with value: "Zero-state user — confirmed, no MFA/passkey/identity/org/sessions/audit; drives empty panels on user detail"

    3. Update the `@moduledoc` to reflect 10 personas (was nine) and mention zoe's role.

    Determinism contract (D-19): password is a static string literal, not time-derived. No faker, no randomness.
  </action>
  <verify>
    <automated>mix run --no-start -e "IO.puts(length(Example.Demo.Personas.all()))" 2>/dev/null | grep "^10$" || (cd test/example && mix run --no-start -e "IO.puts(length(Example.Demo.Personas.all()))")</automated>
  </verify>
  <done>
    `Personas.all/0` returns a list of exactly 10 maps; "zoe" is the 10th entry with confirmed: true and all other state fields false/nil; `feature_map/0` contains key "zoe" with zero-state description.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend seeds.ex — ghost-org, zoe wiring, i18n/RTL user, IN-01/02/03 hardening (D-16, D-18, D-19, IN-01, IN-02, IN-03)</name>
  <files>test/example/lib/example/demo/seeds.ex</files>
  <read_first>
    - test/example/lib/example/demo/seeds.ex — full file; focus on: @bulk_cohort_size (line 47), seed_bulk_users/0 confirm-branch (lines 139-148), seed_organizations/0 (lines 293-315), upsert_organization/2 (lines 299-315), seed_memberships/3 (line 319+), DateTime.add with 86_400 (lines 513, 800, 823), @seed_reference_ts (line 41)
    - test/example/lib/example/demo/personas.ex — read after Task 1 modifies it; zoe must be seeded through seed_users/0 via Personas.all/0 automatically (no special case)
    - .planning/todos/pending/2026-06-25-phase199-code-review-info-hardening.md — IN-01/02/03 exact file locations
  </read_first>
  <behavior>
    - Seeds.bulk_cohort_size/0 is callable from seeds_test.exs and returns the integer 36
    - @seconds_per_day 86_400 is defined; all 3 DateTime.add call-sites use it instead of the literal
    - seed_organizations/0 returns {acme, beta, ghost_org} (or seeds ghost-org as a side effect); ghost-org is seeded idempotently
    - seed_memberships/3 does NOT receive ghost-org as a seed target (no membership entry for ghost-org)
    - run/0 still returns :ok; idempotent on second call
  </behavior>
  <action>
    Apply these changes to `test/example/lib/example/demo/seeds.ex` in order:

    IN-01: In `seed_bulk_users/0`, in the `if user.confirmed_at do` block (the true-branch at ~line 141), add a comment: "# Already confirmed — only reachable on partial-cohort re-runs (idempotency recovery path)"

    IN-02: Convert `@bulk_cohort_size` from a private module attribute to a public function. Keep the `@bulk_cohort_size 36` module attribute as the backing value, but add a public function:
    ```
    @doc "Returns the target size of the loadtest- bulk user cohort."
    @spec bulk_cohort_size() :: non_neg_integer()
    def bulk_cohort_size, do: @bulk_cohort_size
    ```
    This exposes the constant as `Seeds.bulk_cohort_size/0` without changing the attribute name used internally.

    IN-03: Add `@seconds_per_day 86_400` as a module attribute immediately after (or alongside) `@seed_reference_ts`. Then replace the 3 literal `86_400` occurrences in `DateTime.add` calls with `@seconds_per_day` (lines ~513, ~800, ~823).

    D-16 ghost-org: In `seed_organizations/0`, after seeding acme and beta, add:
    `ghost = upsert_organization("Ghost Org", "ghost-org")` and return `{acme, beta, ghost}` from the function. Update the `{acme, beta} = seed_organizations()` call in `run/0` to `{acme, beta, _ghost} = seed_organizations()` — ghost-org is seeded but its return value is not passed to seed_memberships (so it gets zero memberships). The organization name is "Ghost Org" and the slug is "ghost-org". Idempotent via the existing `upsert_organization/2` check-then-insert pattern. No entry in seed_memberships, no seed_invitation for ghost-org.

    D-18 i18n/RTL overflow user: In `seed_bulk_users/0`, after the main Enum.each loop that seeds @bulk_cohort_size users, add a separate count-threshold guard for exactly one i18n/RTL overflow user with:
    - email: "loadtest-i18n-rtl@demo.tasklane.test" (loadtest- prefix keeps it out of Personas.all())
    - display_name containing: multi-byte CJK ("张三李四"), RTL Arabic ("مستخدم"), combining diacritics ("café résumé"), and emoji ("🌏") — e.g. "张三李四 مستخدم café résumé 🌏 Test"
    - password: "I18nRtl1!LoadTest2026" (deterministic, no randomness)
    - confirmed: true
    The count-threshold guard checks `Repo.aggregate(from(u in User, where: u.email == ^"loadtest-i18n-rtl@demo.tasklane.test"), :count) < 1` before inserting (idempotency). No @bulk_cohort_size change needed — this user is EXTRA to the cohort, not counted in the 36.

    Determinism contract (D-19): all new values are static string literals derived from deterministic hashes or fixed strings — no DateTime.utc_now, no Enum.random, no :rand. ghost-org timestamps are not set (organization schema uses inserted_at defaults). i18n display_name is a fixed literal.
  </action>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>
    `mix compile --warnings-as-errors` passes in test/example/; `Seeds.bulk_cohort_size/0` is exported (visible in mix docs or function_exported?); `@seconds_per_day` replaces the 3 literal 86_400 occurrences; ghost-org is seeded in `seed_organizations/0`; one i18n/RTL user is seeded in `seed_bulk_users/0`; `run/0` still returns `:ok`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend seeds_test.exs — SSoT lockstep assertions + zero-state invariants (D-17) and quality-ledger-monotonic.test.sh Test C + Test D (IN-05)</name>
  <files>test/example/test/example/demo/seeds_test.exs, scripts/ci/quality-ledger-monotonic.test.sh</files>
  <read_first>
    - test/example/test/example/demo/seeds_test.exs — full file; understand the existing describe blocks, snapshot_counts/0, @bulk_cohort_size reference at ~line 43, `demo_users == length(Personas.all())` assertions at ~lines 125 and 148-149
    - test/example/lib/example/demo/seeds.ex — after Task 2; Seeds.bulk_cohort_size/0 signature
    - scripts/ci/quality-ledger-monotonic.test.sh — full file; Test A and Test B structure; the LEDGER_PATH heredoc format; PASS/FAIL counter pattern; how git -C "$REPO" is used for the temp-repo setup
    - scripts/ci/quality-ledger-monotonic.sh — skim to understand the guard's awk -F'|' parse rule (column-4 matches /^[012]$/) to write accurate Test D fixture
  </read_first>
  <behavior>
    - seeds_test.exs: @bulk_cohort_size module attribute in the test is replaced by Seeds.bulk_cohort_size() call (IN-02 SSoT)
    - seeds_test.exs: new test "zoe is the zero-state persona with no sessions, identities, orgs, or audit events" in the SEED-02/03 describe block
    - seeds_test.exs: new test "ghost-org has zero memberships and zero invitations" in the SEED-02/03 describe block
    - quality-ledger-monotonic.test.sh: Test C (1→2 increase) exits 0
    - quality-ledger-monotonic.test.sh: Test D (decorated column-4 "2*") — guard exits non-zero OR 0 depending on whether the guard treats decorated values as unrecognized (per the ledger's own warning); whichever behavior matches, the test proves it empirically
    - bash scripts/ci/quality-ledger-monotonic.test.sh exits 0
  </behavior>
  <action>
    In `test/example/test/example/demo/seeds_test.exs`:

    1. Replace the module-level `@bulk_cohort_size 36` attribute with a reference to `Seeds.bulk_cohort_size()` in the test assertion body. Specifically: remove the `@bulk_cohort_size 36` module attribute, and in the test "bulk cohort contains exactly @bulk_cohort_size users after run/0" change the assertion to use `Seeds.bulk_cohort_size()` inline:
       - `assert count == Seeds.bulk_cohort_size(), "expected #{Seeds.bulk_cohort_size()} bulk cohort users; got #{count}"`
       - Similarly update the second bulk-cohort idempotency test.

    2. Add to the "persona catalog + states (SEED-02, SEED-03)" describe block (after setup):
       - Test "zoe is the zero-state persona with zero sessions, identities, orgs, and audit events":
         - `zoe = demo_user!("zoe@demo.tasklane.test")` then assert zoe is not nil
         - Assert `Repo.aggregate(from(s in UserSession, where: s.user_id == ^zoe.id), :count) == 0`
         - Assert `Repo.aggregate(from(ui in UserIdentity, where: ui.user_id == ^zoe.id), :count) == 0`
         - Assert `Repo.aggregate(from(m in OrganizationMembership, where: m.user_id == ^zoe.id), :count) == 0`
         - Assert `Repo.aggregate(from(a in AuditEvent, where: a.effective_user_id == ^zoe.id), :count) == 0`
         - Assert `zoe.confirmed_at != nil`

       - Test "ghost-org has zero memberships and zero invitations":
         - `ghost = Repo.get_by(Organization, slug: "ghost-org")`
         - Assert `ghost != nil`
         - Assert `Repo.aggregate(from(m in OrganizationMembership, where: m.organization_id == ^ghost.id), :count) == 0`
         - Assert `Repo.aggregate(from(i in OrganizationInvitation, where: i.organization_id == ^ghost.id), :count) == 0`

    3. Add "ghost-org" to the organizations count assertion in `snapshot_counts/0`. The current query checks `o.slug in ^["acme-corp", "beta-labs"]` and expects `== 2`. Update to include "ghost-org" in the slug list and expect `== 3`.

    In `scripts/ci/quality-ledger-monotonic.test.sh`:

    Add Test C BEFORE the `## Summary` section (after Test B cleanup):

    Test C: 1→2 increase MUST exit 0
    - Restore working tree to committed baseline state (same as Test B setup)
    - Mutate working tree: change the row from `| visual-baseline   | global | 1    |` to `| visual-baseline   | global | 2    |`
    - Run the guard; it should exit 0 (1→2 is allowed, monotonic increase)
    - Check exit code is 0: pass/fail accordingly
    - No "tier decreased" in stderr expected

    Add Test D: decorated column-4 ("2*") must NOT be treated as a valid tier integer by the guard
    - Restore working tree to committed baseline state
    - Mutate working tree: change `| accessibility     | page   | 2    |` to `| accessibility     | page   | 2*   |`
    - Run the guard against the BASE_COMMIT (where accessibility was bare "2"); the guard's awk parse reads column-4 `2*` and since `2* ~ /^[012]$/` is FALSE (awk regex), it is skipped entirely — the item is invisible to the guard (not compared, not protected)
    - Expected: guard exits 0 (no decrease detected, because the decorated row is invisible to the parse)
    - Add a pass/fail that documents this behavior: "Test D: decorated '2*' is invisible to the guard parse (exits 0, not protected)"
    - Add a comment explaining WHY this is the documented-but-dangerous behavior (the ledger prose says decorators are forbidden precisely because they make the cell unprotectable, not because the guard catches them)

    Update the PASS/FAIL summary to count 4 tests total. The script must still exit 0 only when FAIL==0.
  </action>
  <verify>
    <automated>
      cd test/example && mix test test/example/demo/seeds_test.exs --seed 0 2>&1 | tail -10
      bash scripts/ci/quality-ledger-monotonic.test.sh
    </automated>
  </verify>
  <done>
    `mix test test/example/test/example/demo/seeds_test.exs` passes (0 failures); `bash scripts/ci/quality-ledger-monotonic.test.sh` exits 0 with "4 passed, 0 failed"; the zoe zero-state and ghost-org zero-membership tests are in the passing output; `@bulk_cohort_size 36` literal is removed from the test module and `Seeds.bulk_cohort_size()` is used in its place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeds.ex → Repo | Seeds run behind MIX_ENV=:test raise guard in priv/repo/seeds.exs; Seeds module itself has no env guard (by design; tests call it directly in sandbox) |
| quality-ledger-monotonic.test.sh → temp git repo | Self-test operates in mktemp -d throwaway; no real-repo mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-04 | Tampering | seeds.ex — ghost-org seeded outside MIX_ENV guard | low | accept | seeds.ex module has no env guard by design; the SCRIPT priv/repo/seeds.exs carries the guard; all tests call Seeds.run() inside sandbox which rolls back |
| T-205-05 | Information Disclosure | i18n/RTL display_name in admin UI | low | accept | Demo data only; display names are publicly visible in /demo/credentials by design; no real PII |
| T-205-06 | Elevation of Privilege | zoe persona — confirmed, zero-state; could appear as exploitable "empty" user | low | accept | Demo domain (@demo.tasklane.test) is segregated from golden CI fixture domain; zoe has a real password (policy-passing) and is a standard confirmed user |
| T-205-07 | Denial of Service | snapshot drift from zoe row (46th user, 2nd page) | medium | mitigate | D-20 blast radius: recapture in plan 03 (INSTR-02) handles affected slugs; canaries byte-stable; allowlists empty at phase close |
</threat_model>

<verification>
1. `cd test/example && mix test test/example/test/example/demo/seeds_test.exs` exits 0, 0 failures
2. `bash scripts/ci/quality-ledger-monotonic.test.sh` exits 0, "4 passed, 0 failed"
3. `grep -c "bulk_cohort_size()" test/example/test/example/demo/seeds_test.exs` ≥ 2 (replaces attribute reference in 2 test bodies)
4. `grep -c "^  @bulk_cohort_size" test/example/test/example/demo/seeds_test.exs` returns 0 (removed from test module)
5. `grep -c "def bulk_cohort_size" test/example/lib/example/demo/seeds.ex` returns 1
6. `grep -c "@seconds_per_day" test/example/lib/example/demo/seeds.ex` ≥ 4 (1 definition + 3 use-sites)
7. `grep -c "86_400" test/example/lib/example/demo/seeds.ex` returns 0 (all literal occurrences replaced)
8. `grep -c "ghost-org" test/example/lib/example/demo/seeds.ex` ≥ 1
9. `grep -c "zoe" test/example/lib/example/demo/personas.ex` ≥ 2 (in all/0 and feature_map/0)
10. `grep -c "loadtest-i18n-rtl" test/example/lib/example/demo/seeds.ex` ≥ 1
11. D-19: `grep -c "DateTime.utc_now\|Faker\|:rand\." test/example/lib/example/demo/seeds.ex` returns 0 for any new additions (pre-existing references are OK if they were already there — check that new additions don't add these)
</verification>

<success_criteria>
- 10 personas in Personas.all/0 (zoe added); all seeds_test.exs persona-count assertions pass
- ghost-org (slug: ghost-org) exists after Seeds.run/0; zero-membership invariant test passes
- i18n/RTL loadtest user seeded with multi-byte/RTL/emoji display name
- Seeds.bulk_cohort_size/0 exported; seeds_test.exs uses it (IN-02 complete)
- @seconds_per_day 86_400 extracted; 3 literal occurrences replaced (IN-03 complete)
- IN-01 confirm-branch comment added (IN-01 complete)
- quality-ledger-monotonic.test.sh has Test C (1→2 exits 0) and Test D (decorated column-4 behavior documented); script exits 0 overall (IN-05 complete)
- D-19 determinism: all new seeds are static-literal + deterministic-hash only
</success_criteria>

<output>
Create `.planning/phases/205-foundation/205-02-SUMMARY.md` when done
</output>
