---
phase: 45
plan: 04
type: execute
wave: 4
depends_on:
  - "45-03"
files_modified:
  - lib/sigra/workers/account_deletion.ex
  - lib/sigra/account.ex
  - lib/sigra/deletion.ex
  - test/sigra/workers/account_deletion_test.exs
  - test/sigra/account/deletion_test.exs
autonomous: true
requirements:
  - AUD-08
must_haves:
  truths:
    - "`account.deletion_executed` audit row shares Repo.transaction with Deletion.execute domain effects when invoked from worker and from Sigra.Account.execute_deletion/3."
    - "Oban retry does not insert duplicate account.deletion_executed rows for the same logical deletion."
nyquist_compliant: true
---

<objective>
Implement **D-45-04**: **`Sigra.Workers.AccountDeletion.perform/2`** must not call **`Sigra.Audit.log_safe(\"account.deletion_executed\", ...)`** after a separate successful **`Deletion.execute`**. Extract or extend an internal API (prefer **`Sigra.Account`** or **`Sigra.Deletion`**) so **HTTP** and **Oban** paths share **one** **`Repo.transact`** composition: domain deletion effects + **`account.deletion_executed`** audit when `:audit_schema` is set. Document Oban bookkeeping orthogonality in **`09-VERIFICATION.md`** (plan **45-05** pointer).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-45-W1 | Repudiation | Deletion runs without canonical executed audit | high | T1 Multi/transact |
| T-45-W2 | Tampering | Retry duplicates forensic rows | medium | Idempotency guard + test |
</threat_model>

<context>
@.planning/phases/45-oauth-ops-c1-signoff/45-CONTEXT.md
@lib/sigra/workers/account_deletion.ex
@lib/sigra/account.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Shared deletion+audit internal API</name>
  <files>lib/sigra/account.ex,lib/sigra/workers/account_deletion.ex</files>
  <read_first>
    - lib/sigra/account.ex
    - lib/sigra/workers/account_deletion.ex
    - lib/sigra/deletion.ex
    - test/sigra/account/deletion_test.exs
    - test/sigra/workers/account_deletion_test.exs
  </read_first>
  <action>
    1. Introduce **`Sigra.Account.execute_deletion_with_audit/3`** (exact name at executor discretion) or extend **`execute_deletion/3`** so that when **`Deletion.execute/3`** returns **`{:ok, strategy}`**, the **`account.deletion_executed`** audit is written in the **same** database transaction as the domain mutations that constitute “executed” per product semantics (match **`AUD-04-042`** / **D-44-04** alignment already done in Account for pre-execute row).
    2. Update **`Sigra.Workers.AccountDeletion`** generated **`perform/2`** to call the shared API and **remove** the standalone **`log_safe`** block at **~line 140**.
    3. Ensure **`Code.ensure_loaded?(Oban.Worker)`** compile guards remain valid for optional Oban.
  </action>
  <acceptance_criteria>
    - `grep -n "account.deletion_executed" lib/sigra/workers/account_deletion.ex` returns **no** line containing **`log_safe`** after change (use `rg` / `grep` as appropriate).
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/workers/account_deletion_test.exs test/sigra/account/deletion_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/workers/account_deletion_test.exs test/sigra/account/deletion_test.exs'</automated>
  </verify>
  <done>Worker and Account share T1 deletion executed audit.</done>
</task>

<task type="tdd">
  <name>Task 2: Retry-safe deletion executed audit test</name>
  <files>test/sigra/workers/account_deletion_test.exs</files>
  <read_first>
    - test/sigra/workers/account_deletion_test.exs
    - test/sigra/account_audit_atomicity_test.exs
  </read_first>
  <action>
    1. Add test simulating **second `perform`** when user already deleted or deletion not scheduled: assert **at most one** **`account.deletion_executed`** row per logical user id (query **`order_by: [asc: :id]`** on audit schema).
  </action>
  <acceptance_criteria>
    - `grep -q "deletion_executed" test/sigra/workers/account_deletion_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/workers/account_deletion_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/workers/account_deletion_test.exs'</automated>
  </verify>
  <done>Idempotency covered by automated test.</done>
</task>

</tasks>

<verification>
- `mix format --check-formatted`
- Full account deletion test paths green.
</verification>

<success_criteria>
- **`account.deletion_executed`** is **T1** per **D-45-04**; worker uses shared API; retry semantics tested.
</success_criteria>
