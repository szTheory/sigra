---
phase: 45
plan: 06
type: execute
wave: 6
depends_on:
  - "45-02"
  - "45-03"
  - "45-04"
  - "45-05"
files_modified:
  - test/sigra/**/*audit*atomicity*.exs
  - .planning/phases/45-oauth-ops-c1-signoff/45-VALIDATION.md
autonomous: true
requirements:
  - AUD-08
must_haves:
  truths:
    - "PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test exits 0."
    - "45-VALIDATION.md frontmatter nyquist_compliant: true after sampling map updated."
nyquist_compliant: true
---

<objective>
Close **AUD-08** verification: align atomicity tests with all converted **T1** boundaries from plans **45-02**–**45-04**, refresh **`45-VALIDATION.md`** per-task status table, and run **full library CI** locally (**mix format --check-formatted` + full `mix test`**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-45-V1 | Repudiation | Missing atomicity test for new Multi | medium | Gap grep for `log_multi_safe` without matching `*_audit_atomicity_test.exs` |
</threat_model>

<context>
@.planning/phases/45-oauth-ops-c1-signoff/45-VALIDATION.md
@guides/recipes/testing.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Audit atomicity test sweep</name>
  <files>test/sigra/</files>
  <read_first>
    - test/sigra/auth/login_and_lockout_audit_atomicity_test.exs
    - test/sigra/mfa_audit_atomicity_test.exs
    - test/sigra/oauth/oauth_audit_atomicity_test.exs
    - test/sigra/account_audit_atomicity_test.exs
    - lib/sigra/audit.ex
  </read_first>
  <action>
    1. For each new **`log_multi_safe`/`__log_internal__`** surface in **OAuth**, **lockout path**, **suspicious login**, **impersonation**, and **account deletion worker**, ensure a matching **`mix test`** file proves rollback / ordering per **D-45-07**.
    2. Add or extend tests only where Phase **45** introduced **T1** conversions; do not weaken Phase **43–44** coverage.
  </action>
  <acceptance_criteria>
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/` exits 0.
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'mix format --check-formatted && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/'</automated>
  </verify>
  <done>Sigra test subtree green.</done>
</task>

<task type="execute">
  <name>Task 2: Full suite + VALIDATION.md sign-off</name>
  <files>.planning/phases/45-oauth-ops-c1-signoff/45-VALIDATION.md</files>
  <read_first>
    - .planning/phases/45-oauth-ops-c1-signoff/45-VALIDATION.md
  </read_first>
  <action>
    1. Run **`PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test`** (entire suite).
    2. Update **`45-VALIDATION.md`**: set **`nyquist_compliant: true`**, mark Per-Task table statuses **✅ green**, set **Approval** date.
  </action>
  <acceptance_criteria>
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test` exits 0.
    - `grep -q "nyquist_compliant: true" .planning/phases/45-oauth-ops-c1-signoff/45-VALIDATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test'</automated>
  </verify>
  <done>Phase validation contract signed.</done>
</task>

</tasks>

<verification>
- No compiler warnings-as-errors regressions (project default).
</verification>

<success_criteria>
- **Full library CI green** per ROADMAP phase **45** success criteria.
</success_criteria>
