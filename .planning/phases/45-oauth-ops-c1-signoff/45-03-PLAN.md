---
phase: 45
plan: 03
type: execute
wave: 3
depends_on:
  - "45-02"
files_modified:
  - lib/sigra/lockout.ex
  - lib/sigra/suspicious_login.ex
  - lib/sigra/impersonation.ex
  - lib/sigra/auth.ex
  - test/sigra/lockout_test.exs
  - test/sigra/suspicious_login_test.exs
  - test/sigra/impersonation_test.exs
  - test/sigra/auth/login_and_lockout_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-08
must_haves:
  truths:
    - "Lockout counter + lockout audit row share fate where D-45-03 mandates T1 (typically via Auth Multi, not orphan log_safe after increment)."
    - "Suspicious login and impersonation sites classified T1 vs T2 in 45-AUD-04-INVENTORY.md with EX rows for retained log_safe."
nyquist_compliant: true
---

<objective>
Apply **D-45-03** to **`Sigra.Lockout`**, **`Sigra.SuspiciousLogin`**, and **`Sigra.Impersonation`**: move **T1** DB-backed security state + audit into the same **`Repo.transaction`/`Multi`** as domain writes; keep **T2** read-heavy failure **`log_safe`** with **EX-45-*** rows; avoid duplicate audits on plug retries (**single writer** in context modules per CONTEXT).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-45-L1 | Tampering | Lockout increment commits without `security.lockout` audit | high | Fold audit into Auth failure Multi or extend Lockout API with repo+audit opts |
| T-45-S1 | Repudiation | Suspicious-login notify decision not aligned with DB row | medium | T1 when notification row persisted |
</threat_model>

<context>
@.planning/phases/45-oauth-ops-c1-signoff/45-CONTEXT.md
@.planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md
@lib/sigra/auth.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Lockout audit co-fate with increment path</name>
  <files>lib/sigra/auth.ex,lib/sigra/lockout.ex</files>
  <read_first>
    - lib/sigra/auth.ex
    - lib/sigra/lockout.ex
    - test/sigra/auth/login_and_lockout_audit_atomicity_test.exs
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
  </read_first>
  <action>
    1. Trace **`handle_failed_login_with_lockout/5`** and related paths: when **`Lockout.increment!`** persists counter/threshold and **`Lockout.audit_lockout/1`** emits **`security.lockout`**, ensure both occur in **one** **`repo.transact`** (extend existing Multi from Phase **43–44** if present) when `:audit_schema` is set.
    2. If **`Lockout.audit_lockout/1`** must remain standalone for API stability, add **`@doc`** deprecation pointer to Multi-based Auth path only after call sites migrated.
    3. Preserve enumeration-safe timing (**dummy hash** behavior unchanged).
  </action>
  <acceptance_criteria>
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/login_and_lockout_audit_atomicity_test.exs` exits 0.
    - `grep -q "security.lockout" lib/sigra/auth.ex` OR `grep -q "audit_lockout" lib/sigra/auth.ex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/login_and_lockout_audit_atomicity_test.exs'</automated>
  </verify>
  <done>Lockout threshold audit aligned with D-45-03 T1.</done>
</task>

<task type="execute">
  <name>Task 2: Suspicious login + impersonation audit tiers</name>
  <files>lib/sigra/suspicious_login.ex,lib/sigra/impersonation.ex</files>
  <read_first>
    - lib/sigra/suspicious_login.ex
    - lib/sigra/impersonation.ex
    - test/sigra/suspicious_login_test.exs
    - test/sigra/impersonation_test.exs
  </read_first>
  <action>
    1. For each **`Audit.log_safe`** in **`suspicious_login.ex`**, either compose into the same transaction as any **DB row** that records the notify decision (**T1**), or add **EX-45-** row in **`45-AUD-04-INVENTORY.md`** with compensating control (**rate limits**, telemetry) if SMTP/enqueue boundary forces **T2**.
    2. For **`impersonation.ex`**, apply the same tier rule: start/end impersonation that mutates persisted state must use **`log_multi_safe`/`__log_internal__`** with the domain **`repo.update`/`insert`/`delete`** in one transaction when `:audit_schema` set.
    3. Metadata: **no secrets**; stable reason codes only.
  </action>
  <acceptance_criteria>
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/suspicious_login_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/impersonation_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/suspicious_login_test.exs test/sigra/impersonation_test.exs'</automated>
  </verify>
  <done>Ops modules honor tier model.</done>
</task>

</tasks>

<verification>
- `mix format --check-formatted`
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/lockout_test.exs test/sigra/mfa/lockout_test.exs`
</verification>

<success_criteria>
- **D-45-03** satisfied for lockout, suspicious login, impersonation with inventory updates.
</success_criteria>
