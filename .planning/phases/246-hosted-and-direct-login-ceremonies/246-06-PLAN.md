---
phase: 246-hosted-and-direct-login-ceremonies
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/sigra.install.ex
  - lib/sigra/install/features/app_sessions.ex
  - test/mix/tasks/sigra.install_test.exs
  - test/sigra/install/app_sessions_generator_test.exs
autonomous: true
requirements: [APP-01]
must_haves:
  truths:
    - "`--app-sessions`, `--app-password-login`, `--api`, and `--jwt` are independently selected; password login requires app sessions (D-01)."
    - "No app-session artifact appears in an unselected install, and app selection emits no PAT/JWT artifact."
  artifacts:
    - path: "lib/sigra/install/features/app_sessions.ex"
      provides: "Isolated additive generator feature and migration slots"
    - path: "test/sigra/install/app_sessions_generator_test.exs"
      provides: "Independent option matrix and negative inventory contract"
  key_links:
    - from: "lib/mix/tasks/sigra.install.ex"
      to: "lib/sigra/install/features/app_sessions.ex"
      via: "independent parsed bindings consumed by the additive feature runner"
      pattern: "AppSessions"
---

<objective>
Introduce the independent generator selection contract before any generated ceremony artifacts are registered.

Purpose: Lock APP-01 and D-01 so later template work cannot accidentally couple app sessions to PAT or JWT generation.
Output: Installer switches, isolated feature module, migration slots, and complete option-matrix tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-PATTERNS.md
@lib/mix/tasks/sigra.install.ex
@lib/sigra/install/features/core.ex
@test/sigra/install/features/core_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Select one app-session generator feature without selecting API or JWT</name>
  <files>lib/mix/tasks/sigra.install.ex, lib/sigra/install/features/app_sessions.ex, test/mix/tasks/sigra.install_test.exs, test/sigra/install/app_sessions_generator_test.exs</files>
  <behavior>
    - Defaults select neither app flag; `--app-sessions` selects only app-session ownership; `--app-password-login` without app sessions fails with actionable CLI text.
    - The complete boolean matrix proves API, JWT, app sessions, and direct password login neither imply nor suppress one another, except the declared password→app prerequisite.
    - Migration slots are allocated deterministically and rerun paths retain the feature-runner contract.
  </behavior>
  <action>Start with a table-driven RED matrix over CLI parsing, binding, `enabled?/1`, files/injections/migrations, and negative artifact groups. Add false defaults and separate switches/binding keys for both app flags per D-01; register a dedicated additive `Sigra.Install.Features.AppSessions` module after Core. Reject `--app-password-login` unless `--app-sessions` is explicitly selected. Give the feature deterministic ceremony/family/token migration slots through the Runner allocator. Correct the stale task documentation that says JWT implies API. Do not register product templates yet beyond symbolic fixture paths needed for the feature contract, and do not change Core's API/JWT predicates.</action>
  <verify><automated>MIX_ENV=test mix test test/mix/tasks/sigra.install_test.exs test/sigra/install/app_sessions_generator_test.exs test/sigra/install/features/core_test.exs --trace</automated></verify>
  <done>Every allowed flag combination has an exact positive and negative file/injection/migration inventory, and the invalid dependency combination fails closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CLI flags → generated attack surface | Adopter selection controls which credential endpoints and tables exist. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-16 | Elevation of Privilege | feature implication | high | mitigate | Exhaustive option matrix and fail-closed password-without-session validation. |
| T-246-17 | Tampering | migration allocation | medium | mitigate | Runner-owned deterministic slots and rerun assertions. |
</threat_model>

<verification>Run installer matrix plus Core isolation tests, exact formatter, and negative inventories for app/API/JWT cross-residue.</verification>
<success_criteria>APP-01 is locked before generated implementation expands the feature.</success_criteria>
<output>Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-06-SUMMARY.md` when done.</output>
