---
phase: 246-hosted-and-direct-login-ceremonies
plan: 11
type: execute
wave: 9
depends_on: [246-10]
files_modified:
  - lib/sigra/app_login.ex
  - priv/templates/sigra.install/app_sessions/app_login_controller.ex
  - test/sigra/app_login_test.exs
  - test/sigra/install/app_sessions_routes_test.exs
  - test/sigra/install/app_sessions_auth_continuation_test.exs
  - scripts/ci/generated-app-login-runtime-proof.sh
  - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
autonomous: true
gap_closure: true
requirements: [APP-02]
must_haves:
  truths:
    - "A fresh generated host completes hosted start, explicit approval, and exchange because the persisted attempt carries kind `:hosted_code` (D-03, D-08, D-09)."
    - "Only a completed `:standard` or `:remember_me` browser session can render or submit approval; `:mfa_pending` retains the signed continuation and returns to normal MFA (D-03, D-04)."
    - "The hosted code remains 60-second, digest-only, single-use evidence and the generated success path returns the Phase 245 app-session contract (D-03, D-08, D-09)."
  artifacts:
    - path: "lib/sigra/app_login.ex"
      provides: "Hosted approval insert with the generated schema discriminator"
    - path: "priv/templates/sigra.install/app_sessions/app_login_controller.ex"
      provides: "Completed-browser-assurance gate for hosted approval"
    - path: "scripts/ci/generated-app-login-runtime-proof.sh"
      provides: "Fresh generated-host hosted start-to-exchange tracer"
  key_links:
    - from: "priv/templates/sigra.install/app_sessions/app_login_controller.ex"
      to: "conn.private[:sigra_session]"
      via: "explicit completed-assurance classification before rendering or submitting approval"
      pattern: "type.*standard|remember_me|mfa_pending"
    - from: "lib/sigra/app_login.ex"
      to: "priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex"
      via: "hosted approval constructs the Ecto.Enum discriminator required by generated storage"
      pattern: "kind: :hosted_code"
    - from: "scripts/ci/generated-app-login-runtime-proof.sh"
      to: "/api/app-login/exchange"
      via: "real generated routes and one cookie jar carry start, approval, callback code, and exchange"
      pattern: "app-login/approve|app-login/exchange"
  assumptions:
    - id: A-246-G11-01
      status: flagged
      statement: "APP-02 concurrency and transaction guarantees remain those already proven in Plans 246-01 and 246-03; this gap plan adds generated-route success and one-use evidence without replacing the existing barrier-controlled PostgreSQL proof."
  prohibitions:
    - id: P-246-01
      requirement_id: APP-02
      category: safety
      status: planned
      verification: "Task 2: MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_auth_continuation_test.exs --trace"
      statement: "The hosted ceremony must not let password-only or MFA-pending browser identity become app-session authority."
      reason: "Descriptor-less adversarial-probe item carried as flagged-unverified; deterministic bypass tests are planned, but prohibition lifecycle verification was not author-resolved in a plain SPEC."
---

<objective>
Repair and trace one complete generated hosted ceremony from browser start through explicit approval and code exchange.

Purpose: Close the generated-schema discriminator failure and the MFA-pending approval bypass while preserving the signed continuation and Phase 245 transaction contract per D-03, D-04, D-08, and D-09.
Output: Correct hosted attempt persistence, a completed-browser-assurance gate, deterministic bypass regressions, and a successful fresh-host route tracer.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-02-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-08-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-09-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-10-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete one fresh generated hosted start-to-exchange journey</name>
  <files>lib/sigra/app_login.ex, test/sigra/app_login_test.exs, priv/templates/sigra.install/app_sessions/app_login_controller.ex, scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs</files>
  <read_first>
    - lib/sigra/app_login.ex
    - priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex
    - priv/templates/sigra.install/app_sessions/app_sessions_migration.exs
    - priv/templates/sigra.install/app_sessions/app_login_controller.ex
    - scripts/ci/generated-app-login-runtime-proof.sh
    - test/sigra/app_login_test.exs
    - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
  </read_first>
  <behavior>
    - Hosted approval persists an attempt whose `kind` is `:hosted_code`, and the row satisfies the generated NOT NULL/Ecto.Enum contract.
    - A disposable generated host creates deterministic fixture identity/session state, drives strict S256/state/callback start, submits the explicit approval form through its stable hook, captures the callback code/state, and exchanges through the real generated JSON route.
    - The returned access/refresh/family response is created only after the existing consume-and-issue transaction commits.
  </behavior>
  <action>Start RED with the library regression and the existing fresh-host runtime contract. Add `kind: :hosted_code` to the attempt constructed by `Sigra.AppLogin.approve_hosted/5`, matching the generated Ecto.Enum and migration rather than weakening storage. Extend the hosted mode of the disposable-host script so it provisions deterministic fixture data, uses a real cookie jar and the existing stable approval hook to execute GET start → authenticated explicit POST approval → registered callback capture → POST exchange through generated routes. Retain literal callback matching, original state, PKCE S256, CSRF, no-referrer, 60-second expiry, digest-only storage, and post-commit credential release per D-03/D-08/D-09. The proof must fail before writing its receipt if any transition or persisted discriminator is absent. Do not add or modify migration/schema fields: the corrective write supplies the value the existing schema already requires.</action>
  <acceptance_criteria>
    - The focused library test observes `attempt.kind == :hosted_code` after approval.
    - `bash scripts/ci/generated-app-login-runtime-proof.sh --hosted` reaches a 2xx generated exchange with a real access/refresh/family result.
    - The hosted proof uses generated routes and browser cookie/CSRF transport, not a repository-owned handwritten attempt schema or a direct `Sigra.AppLogin` call.
  </acceptance_criteria>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login_test.exs test/sigra/planning/phase_246_generated_app_login_runtime_test.exs --trace &amp;&amp; bash scripts/ci/generated-app-login-runtime-proof.sh --hosted</automated>
  </verify>
  <done>A fresh generated host persists a typed hosted attempt and exchanges its explicitly approved code for the existing opaque app session through real routes.</done>
  <reversibility rating="reversible">The discriminator and proof changes restore an existing generated contract and can be reverted without data migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject MFA-pending browser approval while preserving continuation</name>
  <files>priv/templates/sigra.install/app_sessions/app_login_controller.ex, test/sigra/install/app_sessions_routes_test.exs, test/sigra/install/app_sessions_auth_continuation_test.exs</files>
  <read_first>
    - priv/templates/sigra.install/app_sessions/app_login_controller.ex
    - priv/templates/sigra.install/app_sessions/app_login_continuation.ex
    - priv/templates/sigra.install/core/session_controller.ex
    - priv/templates/sigra.install/core/mfa_challenge_controller.ex
    - lib/sigra/plug/fetch_session.ex
    - test/sigra/install/app_sessions_routes_test.exs
    - test/sigra/install/app_sessions_auth_continuation_test.exs
  </read_first>
  <behavior>
    - `:standard` and `:remember_me` Sigra sessions with a current user may render and submit approval.
    - `:mfa_pending` retains only `:sigra_app_login_continuation`, redirects into the normal generated MFA ceremony, and creates no hosted attempt or callback redirect.
    - Missing, malformed, or unrecognized private session assurance cannot approve even when a current user assign is present.
  </behavior>
  <action>Add one private completed-assurance classifier in the generated controller and use it consistently in hosted start, continue, approve, and cancel decisions per D-03/D-04. Permit only generated Sigra sessions with type `:standard` or `:remember_me`; route `:mfa_pending` to the existing `/users/mfa` ceremony without taking or expanding the signed continuation, and fail closed for absent/unknown private session state. Add rendered controller tests and deterministic Conn-level generated-host cases that set `current_scope.user` alongside each `conn.private[:sigra_session]` type, proving an MFA-pending request cannot call `AppSessions.approve_hosted/3`, persist an attempt, or redirect to a callback. Preserve only the already-generated signed continuation handle across MFA; do not copy its payload into session, flash, query parameters, or logs.</action>
  <acceptance_criteria>
    - Generated controller tests cover standard, remember-me, MFA-pending, missing-session, and malformed-session cases.
    - MFA-pending continue/approve requests enter normal MFA with the signed handle intact and no app-login attempt row.
    - Completed MFA resumes at `/app-login/continue` and still requires the explicit CSRF-protected approval POST.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_auth_continuation_test.exs --trace</automated>
  </verify>
  <done>Generated browser approval requires completed Sigra assurance, and MFA-pending sessions preserve only the signed continuation until ordinary MFA succeeds.</done>
  <reversibility rating="reversible">This narrows an existing generated controller guard without changing stored data or published route names.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- `Sigra.AppLogin.approve_hosted/5` persists the existing `kind` field as `:hosted_code`.
- Generated `AppLoginController` gains one private completed-browser-assurance classifier used by start/continue/approve/cancel.
- `scripts/ci/generated-app-login-runtime-proof.sh --hosted` executes a successful fresh generated hosted ceremony.
- Focused library, rendered-template, continuation, and runtime regression cases cover the discriminator and MFA-pending bypass.

<assumption_delta_decision>
no-change — the primary noun remains independently selected installer capabilities. This repair changes neither `--app-sessions` nor `--app-password-login` selection and does not generalize first-party profile identity; it restores the already-selected hosted capability.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser session → generated approval controller | A current user assign is insufficient; trusted completed Sigra assurance must cross with it. |
| signed continuation → hosted attempt row | Bounded browser state becomes one typed digest-only database attempt only after explicit approval. |
| generated host → evidence receipt | The receipt may represent hosted success only after the real route journey completes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-29 | Elevation of Privilege | generated hosted approval | high | mitigate | Permit only `:standard`/`:remember_me` private Sigra sessions and prove `:mfa_pending` cannot render or submit approval. |
| T-246-30 | Tampering | hosted attempt persistence | high | mitigate | Persist `kind: :hosted_code` and assert the generated NOT NULL/Ecto.Enum row through the successful route path. |
| T-246-31 | Tampering | continuation/callback | high | mitigate | Preserve only the signed handle through MFA and retain literal callback, state, S256, CSRF, and no-referrer checks. |
| T-246-32 | Repudiation | hosted runtime receipt | medium | mitigate | Receipt-last behavior and generated-route assertions make hosted evidence causal rather than declarative. |
</threat_model>

<verification>
Run both task commands, exact formatter checks for modified Elixir/tests, `bash -n scripts/ci/generated-app-login-runtime-proof.sh`, and `git diff --check`. There is no schema push task because no schema-relevant file changes: the existing non-null discriminator is populated correctly.
</verification>

<success_criteria>
APP-02 hosted success works in a fresh generated host, and no MFA-pending or assurance-less browser request can create or approve an app-login code.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-11-SUMMARY.md` when done.
</output>
