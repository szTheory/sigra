---
phase: 246-hosted-and-direct-login-ceremonies
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/sigra/app_session.ex
  - lib/sigra/app_login.ex
  - lib/sigra/app_login/attempt.ex
  - test/support/app_login_schemas.ex
  - test/sigra/app_login_test.exs
autonomous: true
requirements: [APP-02, APP-03]
must_haves:
  truths:
    - "A valid hosted code is consumed and produces the Phase 245 app-session contract in one committed PostgreSQL transaction (D-03, D-08, D-09)."
    - "A failed issuance leaves the code unconsumed and returns no raw credential."
  artifacts:
    - path: "lib/sigra/app_login.ex"
      provides: "Hosted exchange facade and normalized ceremony results"
    - path: "lib/sigra/app_login/attempt.ex"
      provides: "Digest-addressed locked attempt Multi builders"
    - path: "test/support/app_login_schemas.ex"
      provides: "Representative host-owned ceremony schema"
  key_links:
    - from: "lib/sigra/app_login.ex"
      to: "lib/sigra/app_session.ex"
      via: "append-only issuance Multi composed after locked code consumption"
      pattern: "AppSession.*issue"
---

<objective>
Trace one production hosted authorization code from digest lookup through atomic consumption and Phase 245 app-session issuance.

Purpose: Prove the phase's highest-risk transaction boundary before expanding either ceremony, per D-03, D-08, and D-09.
Output: A reusable app-session issuance Multi seam, hosted exchange facade, locked attempt builder, and PostgreSQL tracer tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RESEARCH.md
@.planning/phases/245-opaque-app-session-core/245-01-SUMMARY.md
@lib/sigra/app_session.ex
@test/support/app_session_schemas.ex

<interfaces>
`Sigra.AppSession.issue/4` is the only public credential issuer. Preserve its post-commit map (`access_token`, `refresh_token`, `family_id`, `expires_in`) and refactor its internal Multi construction into a composable seam; neither ceremony may create credential rows directly.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Exchange one locked hosted code into the existing app-session family</name>
  <files>lib/sigra/app_session.ex, lib/sigra/app_login.ex, lib/sigra/app_login/attempt.ex, test/support/app_login_schemas.ex, test/sigra/app_login_test.exs</files>
  <behavior>
    - Valid 60-second code digest plus matching verifier/profile/callback locks one attempt, marks it consumed, inserts one Phase 245 family and access/refresh pair, and returns raw credentials only after commit.
    - Invalid, expired, consumed, wrong-profile, wrong-callback, or wrong-verifier input returns one bounded invalid-code result and creates no family.
    - Injected attempt-consumption, app-session persistence, or optional audit failure rolls back every mutation and returns no credential.
  </behavior>
  <action>Start RED with the real PostgreSQL happy path and rollback cases. Refactor `Sigra.AppSession.issue/4` to delegate to a composable Multi builder that retains raw material only inside transaction changes and exposes it only after the outer commit. Implement `Sigra.AppLogin.exchange_hosted/5` and digest-addressed `FOR UPDATE` attempt classification, then compose consume, optional bounded audit, and the Phase 245 issue builder in one transaction per D-08/D-09. Store only code/challenge digests and server-selected bindings; do not add routes, generator artifacts, password handling, OAuth types, scopes, or another credential representation.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login_test.exs test/sigra/app_session_test.exs --trace</automated>
  </verify>
  <done>The tracer proves one committed hosted exchange produces credentials accepted by `Sigra.AppSession.authenticate/2`, while every terminal or rollback branch produces neither a usable family nor leaked raw credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public code/verifier → ceremony service | Untrusted bearer values enter digest lookup and PKCE validation. |
| ceremony transaction → credential response | Raw app credentials may cross only after durable commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-01 | Elevation of Privilege | hosted exchange | high | mitigate | Digest-addressed row lock, binding validation, terminal transition, and issue share one Multi. |
| T-246-02 | Information Disclosure | post-commit result | high | mitigate | Persist digests only and return raw credentials only from a successful outer transaction. |
| T-246-03 | Tampering | persistence/audit fault | medium | mitigate | Constraint-fault tests prove rollback restores the code and creates no family. |
</threat_model>

<verification>
Run the focused PostgreSQL tracer plus the existing Phase 245 issuance suite; run exact formatter checks for all five files and `git diff --check`.
</verification>

<success_criteria>
Hosted exchange has one authoritative transaction and reuses both Phase 245 issuance and authentication without changing their public credential contract.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-01-SUMMARY.md` when done.
</output>
