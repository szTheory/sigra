---
phase: 246-hosted-and-direct-login-ceremonies
plan: 13
type: execute
wave: 11
depends_on: [246-12]
files_modified:
  - scripts/ci/generated-app-login-runtime-proof.sh
  - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
  - .github/workflows/generated-app-login-runtime-proof.yml
autonomous: true
gap_closure: true
requirements: [APP-02, APP-03]
must_haves:
  truths:
    - "Fresh generated hosted and direct ceremonies each produce a real access credential that authenticates a protected generated route through `Sigra.Plug.FetchAppSession` (D-08)."
    - "Reusing the hosted authorization code or the completed direct-MFA challenge is rejected through the same real generated routes and creates no second family (D-03, D-06, D-09)."
    - "The machine-readable receipt is written last and records only transitions actually executed in the disposable hosts."
  artifacts:
    - path: "scripts/ci/generated-app-login-runtime-proof.sh"
      provides: "Successful generated hosted/direct ceremonies, replay checks, and FetchAppSession authentication"
    - path: "test/sigra/planning/phase_246_generated_app_login_runtime_test.exs"
      provides: "Executable source/evidence contract for the strengthened runtime lane"
    - path: ".github/workflows/generated-app-login-runtime-proof.yml"
      provides: "CI parsing and retention of the successful structured receipt"
  key_links:
    - from: "hosted exchange response"
      to: "Sigra.Plug.FetchAppSession"
      via: "Bearer access token against a generated protected route"
      pattern: "authorization.*Bearer|FetchAppSession"
    - from: "direct backup-code response"
      to: "Sigra.Plug.FetchAppSession"
      via: "the same generated protected route and bounded private auth facts"
      pattern: "kind.*app_session|auth_method.*app_session"
    - from: "scripts/ci/generated-app-login-runtime-proof.sh"
      to: "runtime-proof.json"
      via: "receipt-last structured assertions and exact source SHA values"
      pattern: "hosted.*replay|direct.*replay|fetch_app_session"
  assumptions:
    - id: A-246-G13-01
      status: flagged
      statement: "Generated-route replay is required here; same-attempt concurrent-caller scheduling and transaction rollback remain the deterministic barrier/fault evidence already supplied by Plans 246-03 and 246-05."
    - id: A-246-G13-02
      status: flagged
      statement: "APP-01 parallel installer guarantees are already satisfied by Plans 246-06, 246-07, and 246-10 and are checked only as a non-regression inventory, not replanned in this gap closure."
  prohibitions:
    - id: P-246-03
      requirement_id: APP-02
      category: transparency
      status: planned
      verification: "Task 2: MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs --trace && bash -n scripts/ci/generated-app-login-runtime-proof.sh"
      statement: "The runtime receipt must not claim successful ceremony, replay, or credential-authentication transitions that the harness did not execute."
      reason: "Descriptor-less adversarial-probe item carried as flagged-unverified; causal runtime assertions are planned, but prohibition lifecycle verification was not author-resolved in a plain SPEC."
    - id: P-246-04
      requirement_id: APP-03
      category: safety
      status: planned
      verification: "Task 1: source tmp/db.env && MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/plug/fetch_app_session_test.exs --trace && bash scripts/ci/generated-app-login-runtime-proof.sh --all"
      statement: "The proof host must not broaden first-party app login into OAuth/OIDC authority, dynamic client registration, request-selected scopes, or client-selected identity."
      reason: "Descriptor-less adversarial-probe item carried as flagged-unverified; existing scope contracts remain in force and no new authority surface is planned."
---

<objective>
Turn the generated-host harness into causal end-to-end evidence for successful hosted and direct credentials, replay rejection, and `FetchAppSession` authentication.

Purpose: Close the remaining verification gap so the phase goal is proven by disposable adopter hosts rather than malformed requests plus handwritten library tests.
Output: Real generated protected-route authentication for both ceremonies, replay assertions, a structured receipt, and CI validation/retention.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-10-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-11-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-12-SUMMARY.md
@lib/sigra/plug/fetch_app_session.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authenticate both generated credentials and reject both replays</name>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs</files>
  <read_first>
    - scripts/ci/generated-app-login-runtime-proof.sh
    - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
    - lib/sigra/plug/fetch_app_session.ex
    - lib/sigra/plug/credential_auth.ex
    - priv/templates/sigra.install/app_sessions/router_injection.ex
    - priv/templates/sigra.install/app_sessions/auth_app_sessions.ex
    - test/sigra/plug/fetch_app_session_test.exs
  </read_first>
  <behavior>
    - Hosted exchange access token authenticates a generated protected route, which observes only the normal current-user Scope and bounded `sigra_auth` app-session facts.
    - Direct backup-code access token authenticates the identical protected route and produces the same credential kind/method/assurance shape.
    - Repeating the hosted exchange and direct-MFA completion through HTTP is rejected, leaves the original credential valid, and leaves exactly one family per ceremony.
  </behavior>
  <action>Add a temporary-host-only protected proof route/pipeline that invokes `Sigra.Plug.FetchAppSession` with the generated `Auth.AppSessions.sigra_config/0` and generated Scope module, then returns a bounded assertion payload containing no raw credential, digest, callback, state, challenge, client reference, or account secret. After the Plan 11 hosted exchange and Plan 12 direct backup-code completion, call that route with each returned Bearer access credential and assert the normal user Scope plus `kind: :app_session`, `auth_method: :app_session`, empty scopes, empty assurance, and required credential/family identifiers. Replay the same hosted code/verifier exchange and the same direct challenge/code/factor completion through their real generated endpoints; assert rejection, exactly one family for each ceremony, and continued authentication of the originally issued access credentials. Keep the already-proven barrier concurrency and rollback suites as separate focused evidence; do not substitute repository tests for any fresh-host success, replay, or protected-route assertion.</action>
  <acceptance_criteria>
    - `bash scripts/ci/generated-app-login-runtime-proof.sh --all` completes two successful fresh generated ceremonies and two successful `FetchAppSession` protected-route requests.
    - The same command proves hosted-code and direct-challenge replay rejection through HTTP and exactly one persisted family per ceremony.
    - The proof route returns only bounded credential facts and no ceremony or credential material.
  </acceptance_criteria>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/plug/fetch_app_session_test.exs --trace &amp;&amp; bash scripts/ci/generated-app-login-runtime-proof.sh --all</automated>
  </verify>
  <done>Both real generated ceremony credentials authenticate through `FetchAppSession`, and both one-time inputs reject replay without duplicate families.</done>
  <reversibility rating="reversible">The protected proof route exists only inside disposable hosts and changes no shipped route or credential contract.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the structured receipt and CI claims causally exact</name>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs, .github/workflows/generated-app-login-runtime-proof.yml</files>
  <read_first>
    - scripts/ci/generated-app-login-runtime-proof.sh
    - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
    - .github/workflows/generated-app-login-runtime-proof.yml
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
  </read_first>
  <behavior>
    - `runtime-proof.json` is absent on any failed transition and is the final successful write.
    - The receipt has independently checkable hosted/direct success, replay rejection, and FetchAppSession fields plus exact SHA-256 bindings for relevant library/template/script sources.
    - CI parses and asserts the receipt schema/status/booleans before retaining it with bounded diagnostics.
  </behavior>
  <action>Replace the prose-only proof claim with a versioned structured receipt whose booleans are set only by completed assertion functions for hosted success, direct backup-code success, hosted replay rejection, direct replay rejection, hosted FetchAppSession authentication, and direct FetchAppSession authentication. Bind the receipt to exact SHA-256 values for `lib/sigra/app_login.ex`, `lib/sigra/plug/fetch_app_session.ex`, both changed generated templates, and the runtime script. Keep receipt creation as the last filesystem write after both hosts finish; trap cleanup may copy it but must never synthesize or edit it. Update the source/evidence test and workflow so CI parses the JSON and requires every field before artifact upload; retain server logs on failure without creating a passing receipt. Continue using bounded readiness probes and no fixed-delay waiting.</action>
  <acceptance_criteria>
    - The CI command rejects a missing, malformed, stale-schema, false-field, or SHA-mismatched receipt.
    - The receipt is emitted only after both hosts, both replays, and both protected-route checks pass.
    - Failed runs retain bounded diagnostics but cannot expose a `status: passed` artifact.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs --trace &amp;&amp; bash -n scripts/ci/generated-app-login-runtime-proof.sh</automated>
  </verify>
  <done>The generated-host CI lane can claim only the exact ceremony transitions and credential checks it has executed successfully.</done>
  <reversibility rating="reversible">Receipt schema and CI assertions are evidence contracts with no production data migration.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- Temporary generated-host protected route wired through `Sigra.Plug.FetchAppSession`.
- Shell assertion functions for hosted success, direct backup-code success, both one-use replays, and both protected-route authentications.
- `sigra.generated-app-login-runtime-proof/v2` receipt fields for each executed transition plus exact source SHA-256 bindings.
- CI receipt parser/assertion step and retained machine-readable evidence artifact.

<assumption_delta_decision>
no-change — the primary noun remains independently selected installer capabilities. The proof creates separate hosted and password-enabled hosts, preserves the two feature flags, and compares only their shared Phase 245 session output rather than generalizing client identity or authority.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| issued Bearer credential → generated protected route | Untrusted transport is accepted only by `FetchAppSession` and projected into bounded server facts. |
| replay request → one-time attempt row | Reused hosted code or direct challenge must never create another family. |
| runtime assertions → receipt/CI claim | Machine evidence must be causal, last-written, parseable, and source-bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-37 | Spoofing | protected proof route | high | mitigate | Invoke only generated `FetchAppSession`; assert trusted app-session private facts and normal Scope. |
| T-246-38 | Elevation of Privilege | hosted/direct replay | high | mitigate | Repeat exact real-route requests, assert rejection and one family, and retain the original credential's validity. |
| T-246-39 | Information Disclosure | proof response/logs | high | mitigate | Return/assert only bounded facts; exclude raw credentials, digests, callback, state, challenge, client ref, and account secrets. |
| T-246-40 | Tampering / Repudiation | receipt and CI | high | mitigate | Receipt-last per-transition booleans, exact source hashes, CI parsing, and no passing receipt on failure. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Gap Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Independently selected first-party app sessions obtainable through hosted or policy-gated direct login | 246-11–246-13 | COVERED | Hosted/direct generated success and shared authenticated session are proven. |
| REQ | APP-01 | Independent app-session/password/API/JWT selection | Executed 246-06/07/10 | COVERED | Verified satisfied; intentionally not replanned in gap closure. |
| REQ | APP-02 | Hosted S256/state/exact callback/approval/60s single-use exchange | 246-11, 246-13 | COVERED | Repairs typed persistence and assurance; proves success/replay/authentication. |
| REQ | APP-03 | Uniform policy-gated direct password/MFA and shared session | 246-12, 246-13 | COVERED | Adds trusted backup-code transport and proves success/replay/authentication. |
| RESEARCH | — | Static profiles, literal callbacks, S256, signed continuation, digest-only attempt | Executed 246-02/07/08 plus 246-11 | COVERED | Existing implementation retained; hosted repair restores generated success. |
| RESEARCH | — | Locked consume-and-issue, replay, concurrency, rollback, uniform direct failures | Executed 246-03/05 plus 246-12/13 | COVERED | Existing PostgreSQL barrier/fault tests remain authoritative; gap plans add route replay. |
| RESEARCH | — | Fresh-host real-route and FetchAppSession evidence | 246-11–246-13 | COVERED | Replaces malformed-request-only evidence. |
| CONTEXT | D-01 | Independent flags | Executed 246-06/07/10 | COVERED | No gap plan changes feature selection. |
| CONTEXT | D-02 | Static profiles and exact callbacks | Executed 246-02/07/08 | COVERED | Preserved by both runtime hosts. |
| CONTEXT | D-03 | Hosted system browser, state, S256, explicit approval, 60s single use | 246-11, 246-13 | COVERED | Typed persistence, assurance, success, and replay. |
| CONTEXT | D-04 | One bounded signed continuation across login/MFA | 246-11 | COVERED | MFA-pending bypass closed without expanding stored state. |
| CONTEXT | D-05 | Uniform direct failures | 246-12 | COVERED | Invalid factor shapes retain exact uniform response. |
| CONTEXT | D-06 | Opaque digest-only five-minute MFA challenge | 246-12, 246-13 | COVERED | Backup success and challenge replay proven. |
| CONTEXT | D-07 | `browser_required` before authentication | Executed 246-04/05/10 | COVERED | No gap changes policy order. |
| CONTEXT | D-08 | Hosted/direct use the same AppSession issuer | 246-11–246-13 | COVERED | Both credentials authenticate through the same plug contract. |
| CONTEXT | D-09 | Locked transaction, post-commit credentials, barrier concurrency | Executed 246-01/03/05 plus 246-13 | COVERED | Existing concurrency/fault evidence retained; generated replay added. |

Deferred native SDK, PWA/offline/media, Crosswake, Electron, OAuth/OIDC-server, dynamic-registration, and admin/operator ideas remain excluded exactly as the phase artifacts require.

<verification>
Run both task commands, the complete focused Phase 246 app-login/generator/continuation/FetchAppSession suite, exact formatter and shell syntax checks, workflow parsing, and `git diff --check`. No schema push is planned because this plan changes only disposable proof-host content, tests, script logic, workflow assertions, and emitted evidence.
</verification>

<success_criteria>
The generated-host lane provides causal machine evidence that hosted and direct success each issue one replay-resistant credential accepted by `FetchAppSession`, with no unsupported claim in the receipt.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-13-SUMMARY.md` when done.
</output>
