---
phase: 246-hosted-and-direct-login-ceremonies
plan: 18
type: execute
wave: 15
depends_on: [246-17]
files_modified:
  - priv/templates/sigra.install/core/mfa_challenge_controller.ex
  - test/sigra/install/app_sessions_mfa_session_upgrade_test.exs
autonomous: true
gap_closure: true
requirements: [APP-02]
must_haves:
  truths:
    - "A generated MFA controller proves a persisted `:mfa_pending` browser session exists before invoking either TOTP or backup-code verification (D-03, D-04)."
    - "An ordinary completed browser session cannot consume a backup code or advance TOTP replay/lockout state through the MFA completion endpoint (D-03, D-04)."
    - "A valid pending session still rotates through the established completion seam before hosted approval resumes (D-04)."
  artifacts:
    - path: "priv/templates/sigra.install/core/mfa_challenge_controller.ex"
      provides: "Pre-verifier persisted-session authority gate for both generated controller factor branches"
    - path: "test/sigra/install/app_sessions_mfa_session_upgrade_test.exs"
      provides: "Executable rendered-controller regressions for ordinary and MFA-pending sessions"
  key_links:
    - from: "priv/templates/sigra.install/core/mfa_challenge_controller.ex"
      to: "conn.private[:sigra_session]"
      via: "one pre-verifier `:mfa_pending` match shared by TOTP and backup-code submissions"
      pattern: "mfa_pending.*mfa_verify|mfa_pending.*mfa_verify_backup"
    - from: "priv/templates/sigra.install/core/mfa_challenge_controller.ex"
      to: "Auth.complete_mfa_verification/3"
      via: "the same trusted pending row is passed from the pre-verifier gate into persisted-session rotation"
      pattern: "complete_mfa_verification"
  assumptions:
    - id: A-246-APP-01-CONCURRENCY
      status: flagged
      statement: "APP-01 concurrency remains unresolved by the spec-less edge probe; this plan changes no installer selection or concurrent generator behavior and makes no new concurrency claim for APP-01."
    - id: A-246-APP-02-UNCLASSIFIED
      status: flagged
      statement: "APP-02 has an unresolved unclassified spec-less edge; this plan closes only the authoritative MFA-before-factor ordering named by 246-VERIFICATION.md and does not silently classify other APP-02 edges."
    - id: A-246-APP-03-UNCLASSIFIED
      status: flagged
      statement: "APP-03 has an unresolved unclassified spec-less edge; direct-login MFA uses the separate locked challenge path and is unchanged by this generated browser-controller repair."
---

<objective>
Gate generated browser MFA factor verification on an authoritative persisted pending session before either one-time factor can be consumed.

Purpose: Close the remaining generated-controller factor-consumption blocker without changing the completed session-rotation or direct-login MFA contracts, per D-03 and D-04.
Output: One pre-verifier session-authority seam and executable generated-controller regressions for TOTP, backup code, ordinary sessions, and valid pending sessions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RESEARCH.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-PATTERNS.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-UI-SPEC.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-14-SUMMARY.md

<interfaces>
`MFAChallengeController.create/2` currently chooses `Auth.mfa_verify/2` or `Auth.mfa_verify_backup/2` before `complete_mfa_session/4` matches `%{type: :mfa_pending}`. The repair must move the authoritative match ahead of both verifier callbacks and pass that exact row into the existing `Auth.complete_mfa_verification/3` plus `UserAuth.put_user_session_token/2` rotation path.

`test/sigra/install/app_sessions_mfa_session_upgrade_test.exs` currently asserts rendered strings. Its new adversarial cases must execute the rendered controller with deterministic fixture modules/counters so verifier non-invocation and one-time factor state are behavioral evidence, not source ordering inference.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute one generated controller request through the pending-session gate</name>
  <files>priv/templates/sigra.install/core/mfa_challenge_controller.ex, test/sigra/install/app_sessions_mfa_session_upgrade_test.exs</files>
  <read_first>
    - priv/templates/sigra.install/core/mfa_challenge_controller.ex
    - priv/templates/sigra.install/core/user_auth.ex
    - priv/templates/sigra.install/core/auth.ex
    - test/sigra/install/app_sessions_mfa_session_upgrade_test.exs
    - test/sigra/install/app_sessions_auth_continuation_test.exs
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-14-SUMMARY.md
  </read_first>
  <behavior>
    - A POST carrying a valid TOTP from an ordinary `:standard` session rejects before `Auth.mfa_verify/2`; the verifier counter remains zero and TOTP replay/lockout state remains unchanged.
    - A POST carrying a valid backup code from an ordinary `:remember_me` session rejects before `Auth.mfa_verify_backup/2`; the verifier counter remains zero and the backup code remains unused.
    - A valid `:mfa_pending` row permits exactly the selected verifier, then uses that same row in `complete_mfa_verification/3`, rotates the browser token, and resumes only the bounded hosted continuation.
    - Missing, malformed, foreign-user, or terminal private session state cannot invoke either factor verifier or session-upgrade callback.
  </behavior>
  <action>Start RED by replacing the rendered-string-only security evidence with executable tests over the rendered generated controller. Build the narrowest isolated generated-controller fixture needed to call `create/2` with Plug/Phoenix connection state, deterministic Auth/UserAuth test doubles, and explicit factor invocation counters; do not mock away the controller branch ordering being tested. Cover ordinary `:standard` TOTP and ordinary `:remember_me` backup-code requests separately, and assert both the callback count and factor-specific durable state remain unchanged. Then refactor `create/2` so it obtains the current user and calls one private gate that accepts only a map with `type: :mfa_pending` bound to that user/session context before reading the submitted method/code or invoking either host verifier. Pass the accepted row into the existing `finish_mfa_verification/5` and `complete_mfa_session/4` rotation seam rather than performing a second lookup after factor consumption. Preserve uniform browser recovery, remember-me/trust behavior, signed continuation retention, the controller-owned LiveView POST seam, and app-session-disabled output. Do not change `Sigra.MFA`, the direct app-login challenge verifier, routes, session schema, or approval UI.</action>
  <acceptance_criteria>
    - Executable rendered-controller tests prove ordinary `:standard` TOTP and ordinary `:remember_me` backup-code requests invoke neither verifier and mutate neither factor state.
    - Pending-session TOTP and backup-code cases still invoke exactly one selected verifier and rotate the exact trusted pending session before `/users/app-login/continue`.
    - Missing/malformed/foreign/terminal session cases fail closed before verifier and upgrade callbacks.
    - Existing controller/LiveView continuation tests remain green and contain no sleep or timing-based ordering.
  </acceptance_criteria>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/install/app_sessions_mfa_session_upgrade_test.exs test/sigra/install/app_sessions_auth_continuation_test.exs --trace</automated>
  </verify>
  <done>The generated controller establishes persisted `:mfa_pending` authority before TOTP or backup-code verification, and valid pending requests retain the previously proven rotation/continuation behavior.</done>
  <reversibility rating="reversible">The change only reorders and centralizes an internal generated-controller guard; it changes no stored format, route, or public callback contract.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- A private generated-controller pre-verifier gate for authoritative `:mfa_pending` session state.
- Executable generated-controller test fixtures/cases inside `test/sigra/install/app_sessions_mfa_session_upgrade_test.exs`; no new file is created.
- No UI, schema, migration, external API, package, or admin design artifact.

<ui_considerations>
No rendered UI contract changes. Preserve the existing `sigra-auth-*` Light/Dark/System shell and the UI-SPEC interaction rule exactly: “Native navigation is the loading treatment.” The selected LiveView forms continue to POST through the existing CSRF-protected controller seam; no spinner, confirmation, callback display, or new styling is introduced.
</ui_considerations>

<assumption_delta_decision>
no-change — evidence: the verified failure is ordering inside the existing browser MFA controller, not a new identity noun or lookup key. The generalized app-session contract remains primary; hosted browser MFA remains one ceremony step keyed by the existing persisted session and signed continuation.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser POST → generated MFA controller | Untrusted factor input must not reach a one-time verifier without authoritative pending-session state. |
| persisted browser session → factor verifier | Only the trusted `:mfa_pending` row for the current user may authorize TOTP replay advancement or backup-code consumption. |
| verified factor → completed browser session | The accepted pending row must rotate through the existing server-owned completion seam before hosted approval. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-56 | Elevation of Privilege | `MFAChallengeController.create/2` | high | mitigate | Match and bind a trusted `:mfa_pending` session before either verifier callback; executable ordinary-session tests prove non-invocation. |
| T-246-57 | Tampering | TOTP/backup one-time state | high | mitigate | Separate factor regressions assert replay/lockout and backup consumption state are unchanged when the session gate rejects. |
| T-246-58 | Spoofing | current user/session binding | high | mitigate | Reject missing, malformed, foreign-user, and terminal session context before verification or rotation. |
| T-246-59 | Information Disclosure | browser recovery | low | accept | Existing generic MFA recovery is retained; no factor/session reason or app-login protocol fact is added to the response. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Secure hosted or policy-gated direct acquisition of the same app session | 246-18 | COVERED | Closes the remaining hosted MFA authority ordering blocker; direct issuance is unchanged. |
| REQ | APP-01 | Independent installer selection | 246-06/07/10/16/17 | COVERED | Completed work remains authoritative; this plan changes no generator selection. |
| REQ | APP-02 | Hosted S256/state/exact callback/explicit continuation/single-use code | 246-18 | COVERED | Repairs the unsafe MFA continuation entry path named by verification. |
| REQ | APP-03 | Uniform policy-gated direct password/MFA | 246-04/05/12/13/17 | COVERED | Completed direct challenge path is deliberately not modified. |
| RESEARCH | — | Reuse existing MFA verifier and browser pending-session ordering | 246-18 | COVERED | Gate precedes both existing verifier callbacks and retains normal rotation. |
| RESEARCH | — | No sleeps; deterministic generated-host evidence | 246-18 | COVERED | Executable generated-controller tests use explicit state/counters. |
| CONTEXT | D-03 | Explicit hosted continuation after browser authentication | 246-18 | COVERED | Only valid pending state may perform the factor step. |
| CONTEXT | D-04 | One bounded continuation across controller/LiveView/MFA | 246-18 | COVERED | Existing signed handle and controller seam remain intact. |

Deferred native SDK, PWA/offline/media, Crosswake, Electron, OAuth/OIDC-server, dynamic-registration, and admin/operator ideas remain excluded. Spec-less edge rows A-246-APP-01-CONCURRENCY, A-246-APP-02-UNCLASSIFIED, and A-246-APP-03-UNCLASSIFIED are preserved as explicit flagged assumptions; none is silently classified or dismissed. Descriptor-less prohibition recall yields no bespoke values/safety/ethics item beyond the canon authentication threats already routed to this ASVS threat model, so no fabricated `must_haves.prohibitions` descriptor is emitted.

<verification>
Run the task command against PostgreSQL, render/parse selected and unselected controller/LiveView templates, run exact formatter checks for the test file, and finish with `git diff --check`. No schema push applies because this plan changes no Ecto schema or migration.
</verification>

<success_criteria>
The verifier can observe that both generated browser factor types fail before consumption outside a valid persisted `:mfa_pending` session, while legitimate controller and LiveView MFA still rotate the session before hosted approval.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-18-SUMMARY.md` when done.
</output>
