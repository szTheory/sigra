---
phase: 246-hosted-and-direct-login-ceremonies
plan: 08
type: execute
wave: 6
depends_on: [246-05, 246-07]
files_modified:
  - lib/sigra/install/features/app_sessions.ex
  - priv/templates/sigra.install/app_sessions/app_login_controller.ex
  - priv/templates/sigra.install/app_sessions/app_login_html.ex
  - priv/templates/sigra.install/app_sessions/app_login_approve.html.heex
  - priv/templates/sigra.install/app_sessions/app_login_continuation.ex
  - priv/templates/sigra.install/app_sessions/router_injection.ex
  - test/sigra/install/app_sessions_routes_test.exs
autonomous: true
requirements: [APP-02, APP-03]
must_haves:
  truths:
    - "Real generated routes create one signed continuation and always require explicit approve/cancel (D-03, D-04)."
    - "Hosted exchange and separately gated direct password/MFA endpoints expose strict, rate-limited, redacted protocol responses."
  artifacts:
    - path: "priv/templates/sigra.install/app_sessions/app_login_controller.ex"
      provides: "Hosted start/approve/cancel/exchange and optional direct endpoints"
    - path: "priv/templates/sigra.install/app_sessions/app_login_continuation.ex"
      provides: "One signed bounded continuation seam shared by all browser auth modes"
    - path: "priv/templates/sigra.install/app_sessions/router_injection.ex"
      provides: "Generated public/browser route and rate-limit injection"
  key_links:
    - from: "priv/templates/sigra.install/app_sessions/app_login_controller.ex"
      to: "priv/templates/sigra.install/app_sessions/app_login_continuation.ex"
      via: "hosted start and approval consume the same signed bounded handle"
      pattern: "AppLoginContinuation"
---

<objective>
Expose hosted and optional direct ceremonies through real generated Phoenix routes and an explicit approval surface.

Purpose: Carry the proven service contract through the adopter-facing transport without credential or redirect confusion.
Output: Generated controller, approval UI, continuation helper, router/rate-limit injections, and rendered route tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RESEARCH.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-05-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete one hosted system-browser route journey through explicit approval</name>
  <files>lib/sigra/install/features/app_sessions.ex, priv/templates/sigra.install/app_sessions/app_login_controller.ex, priv/templates/sigra.install/app_sessions/app_login_continuation.ex, priv/templates/sigra.install/app_sessions/router_injection.ex, test/sigra/install/app_sessions_routes_test.exs</files>
  <behavior>
    - Strict start parameters create one signed continuation and redirect unauthenticated users into normal browser login; authenticated users render approve/cancel rather than redirecting to the app.
    - Approval redirects only to the exact stored callback with code/state and `Referrer-Policy: no-referrer`; cancel returns a bounded denial without a code.
    - Exchange is JSON, scalar-strict, rate-limited, and returns the committed Phase 245 credentials; malformed/list/extra inputs are rejected uniformly.
  </behavior>
  <action>Render controller-based hosted start, approve, cancel, exchange, and separately gated direct JSON routes. Centralize put/fetch/take/expiry/tamper handling in `AppLoginContinuation` and keep only a bounded signed handle/session value. Register browser and public JSON pipelines with dedicated rate-limit keys and stable route names/hooks. Set no-referrer on approval/callback responses and pass only the library's exact registered callback. Do not place code/verifier/state/password or app credentials in flash, logs, telemetry, URL fragments, browser storage, or cookies.</action>
  <verify><automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_generator_test.exs --trace</automated></verify>
  <done>Rendered hosted routes complete the real start→login→approve→callback→exchange contract with exact routing, strict inputs, and no implicit approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render explicit approval and cancellation in the existing auth shell</name>
  <files>priv/templates/sigra.install/app_sessions/app_login_html.ex, priv/templates/sigra.install/app_sessions/app_login_approve.html.heex, test/sigra/install/app_sessions_routes_test.exs</files>
  <behavior>
    - Authenticated users see a semantic approve/cancel surface naming only the static app profile; neither action is implicit.
    - Approval/cancel output preserves Light, Dark, and System auth styling and stable role/test hooks.
  </behavior>
  <action>Render the approval page through the existing `sigra-auth-*` shell and tokens, with clear app identity, explicit approve/cancel POST controls, accessible headings/labels/focus, and stable hooks. Keep Rail Accent/auth brand behavior and all three theme modes. Do not add admin `sg-*` components or redesign the existing auth shell.</action>
  <verify><automated>MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs --trace</automated></verify>
  <done>The generated approval decision is explicit, accessible, deterministic, and visually native to the existing auth surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| native app → generated public routes | Untrusted profile/callback/state/PKCE/password/MFA JSON enters Phoenix. |
| browser cookie → native callback | Browser authentication may authorize only after explicit continuation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-21 | Information Disclosure | callback/headers/logs | high | mitigate | Exact stored redirect, no-referrer, redacted parameters, and no browser credential transport. |
| T-246-22 | CSRF / Tampering | approve/cancel | high | mitigate | Browser pipeline/CSRF, signed one-time continuation, strict method and scalar keys. |
| T-246-23 | Denial of Service | public ceremony routes | medium | mitigate | Dedicated generated rate-limit pipelines with source/runtime tests. |
</threat_model>

<verification>Run rendered route suites, formatter, generated-template compilation, and secret/browser-storage negative source contracts.</verification>
<success_criteria>Every real generated app-login route maps cleanly to the library state machine and requires explicit browser approval.</success_criteria>
<output>Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-08-SUMMARY.md` when done.</output>
