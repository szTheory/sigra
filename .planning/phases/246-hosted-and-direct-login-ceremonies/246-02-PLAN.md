---
phase: 246-hosted-and-direct-login-ceremonies
plan: 02
type: tdd
wave: 2
depends_on: [246-01]
files_modified:
  - lib/sigra/config.ex
  - lib/sigra/app_login.ex
  - lib/sigra/app_login/pkce.ex
  - test/sigra/config_test.exs
  - test/sigra/app_login_test.exs
autonomous: true
requirements: [APP-02]
must_haves:
  truths:
    - "Hosted start accepts only a static public profile, literal registered callback, nonempty state, and PKCE S256 (D-02, D-03)."
    - "An authenticated browser must explicitly approve before a 60-second code exists (D-03, D-04)."
  artifacts:
    - path: "lib/sigra/app_login/pkce.ex"
      provides: "Strict verifier/challenge syntax and RFC 7636 S256 transform"
    - path: "lib/sigra/config.ex"
      provides: "Validated host schemas and static first-party profile registry seam"
  key_links:
    - from: "lib/sigra/app_login.ex"
      to: "lib/sigra/config.ex"
      via: "server-owned profile lookup before password, persistence, or redirect"
      pattern: "first_party.*profile"
---

<objective>
Expand the hosted tracer into a complete library ceremony contract for static profiles, exact callbacks, state, PKCE S256, signed continuation, and explicit approval.

Purpose: Make every redirect and code binding server-authoritative per D-02 through D-04.
Output: Validated profile/config seams, PKCE primitives, hosted start/approve/cancel behavior, and exhaustive state-machine tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RESEARCH.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-01-SUMMARY.md
@lib/sigra/config.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Start, approve, and exchange one bound hosted browser ceremony</name>
  <files>lib/sigra/config.ex, lib/sigra/app_login.ex, lib/sigra/app_login/pkce.ex, test/sigra/config_test.exs, test/sigra/app_login_test.exs</files>
  <behavior>
    - Exact static profile and callback plus S256 challenge/state create a bounded signed continuation; an already authenticated browser still requires approval.
    - Approval persists one digest-only code expiring exactly 60 seconds after issue and returns only the registered callback plus code/state.
    - Cancel, expired/tampered continuation, malformed/list/extra values, `plain` PKCE, callback variants, and unregistered profiles create no code or redirect to caller-selected destinations.
  </behavior>
  <action>Write RED tests for strict scalar syntax, literal string callback equality (including literal loopback port only when registered), S256 verifier derivation, signed continuation expiry/tamper, explicit approve/cancel, and the full start→approve→exchange path. Extend app-session configuration with paired ceremony schema modules and a finite host-owned profile registry containing only `id`, `client_ref`, exact callback strings, and direct-login policy per D-02. Implement hosted start and approval using injected clock support; code TTL is exactly 60 seconds. Keep raw state only in the signed bounded continuation/redirect and keep code storage digest-only. Do not normalize URLs, support wildcard/variable ports, persist PKCE verifiers, silently approve existing browser sessions, or implement OAuth/OIDC surfaces.</action>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/config_test.exs test/sigra/app_login_test.exs --trace</automated>
  </verify>
  <done>The entire hosted library path is deterministic and rejects every non-exact profile/callback/state/PKCE/continuation variant before issuance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| app start parameters → static host policy | Request-selected identifiers and redirect values cross into trusted configuration. |
| browser session → explicit continuation | Existing cookie authentication must not silently authorize app credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-04 | Spoofing | profile/callback lookup | high | mitigate | Exact static lookup and literal callback equality before any state is created. |
| T-246-05 | Tampering | continuation/state/PKCE | high | mitigate | Signed expiry-bound continuation, state binding, and strict S256 validation. |
| T-246-06 | Elevation of Privilege | browser approval | high | mitigate | Explicit approve action is mandatory for authenticated and newly authenticated browsers alike. |
</threat_model>

<verification>
Run the focused config and app-login tests, exact formatter checks, and a negative grep confirming no discovery, JWKS, consent, dynamic-registration, client-secret, or wildcard-callback implementation entered the files.
</verification>

<success_criteria>
The hosted ceremony satisfies every APP-02 protocol condition before generated transport is added.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-02-SUMMARY.md` when done.
</output>
