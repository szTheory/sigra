---
phase: 246-hosted-and-direct-login-ceremonies
plan: 16
type: execute
wave: 13
depends_on: [246-15]
files_modified:
  - priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex
  - priv/templates/sigra.install/app_sessions/app_sessions_migration.exs
  - test/sigra/install/app_sessions_generator_test.exs
  - test/sigra/app_login/concurrency_test.exs
autonomous: true
gap_closure: true
requirements: [APP-01, APP-02]
must_haves:
  truths:
    - "A fresh `--app-sessions` host receives the unique digest field/index required by the single-use approval contract, while unrelated API/JWT/direct-login selections remain independent (D-01, D-03)."
    - "Two barrier-released callers approving the same signed continuation produce exactly one hosted code and one bounded replay rejection (D-03, D-09)."
    - "Generated storage contains only the approval digest and existing bounded ceremony facts; it adds no dynamic client, secret, scope, or identity surface (D-02)."
  artifacts:
    - path: "priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex"
      provides: "Generated host approval-digest field"
    - path: "priv/templates/sigra.install/app_sessions/app_sessions_migration.exs"
      provides: "Prefix-aware unique approval-digest index for PostgreSQL and supported adapters"
    - path: "test/sigra/app_login/concurrency_test.exs"
      provides: "Ready/go same-continuation approval race proof"
  key_links:
    - from: "lib/sigra/app_login.ex"
      to: "priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex"
      via: "configured schema accepts the `approval_digest` written by hosted approval"
      pattern: "approval_digest"
    - from: "priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex"
      to: "priv/templates/sigra.install/app_sessions/app_sessions_migration.exs"
      via: "schema field maps to a unique indexed binary column in prefixed and unprefixed hosts"
      pattern: "unique_index.*approval_digest"
---

<objective>
Propagate atomic continuation consumption into generated host storage and prove the concurrent approval race.

Purpose: Make Plan 246-15's library contract installable and deterministic for every independently selected app-session host per D-01/D-02/D-03/D-09.
Output: Generated schema/migration parity, installer non-regressions, and PostgreSQL barrier concurrency evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-06-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-07-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-15-SUMMARY.md
@lib/sigra/install/features/app_sessions.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate one migratable unique approval-digest contract</name>
  <files>priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex, priv/templates/sigra.install/app_sessions/app_sessions_migration.exs, test/sigra/install/app_sessions_generator_test.exs</files>
  <read_first>
    - priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex
    - priv/templates/sigra.install/app_sessions/app_sessions_migration.exs
    - lib/sigra/install/features/app_sessions.ex
    - test/sigra/install/app_sessions_generator_test.exs
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-15-SUMMARY.md
  </read_first>
  <behavior>
    - Prefix-aware and unprefixed migrations add a binary `approval_digest` column and a unique index whose name matches the library changeset constraint.
    - The rendered attempt schema exposes that field without any raw nonce, verifier, password, secret, dynamic registration, or scope field.
    - App-session-only, direct-enabled, API-only, JWT-only, and unselected inventories retain D-01 independence.
  </behavior>
  <action>Start RED in the existing generator suite, then add the exact `approval_digest` binary field and prefix-aware unique index required by Plan 246-15. Use one stable explicit index name in both adapter branches so `Ecto.Changeset.unique_constraint/3` can normalize replay without adapter-generated naming drift. Render and parse both auth-prefix and unprefixed templates, run the existing installer twice, and retain the four-feature inventory matrix. Add absence assertions for raw nonce and all prohibited authority fields rather than changing unrelated generated output. This is a host-owned ceremony-storage addition, not a dynamic profile or identity feature per D-01/D-02.</action>
  <acceptance_criteria>
    - Fresh rendered schemas/migrations compile and migrate with and without an auth prefix.
    - A duplicate approval digest is rejected by the named unique constraint.
    - Existing independent feature inventories and rerun idempotency remain green.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/app_sessions_generator_test.exs test/sigra/install/features/core_test.exs --trace</automated>
  </verify>
  <done>Every newly generated app-session host can persist and enforce the single-use approval digest without feature-selection drift.</done>
  <reversibility rating="costly">Generated adopters receive a new internal column/index; reversal requires a coordinated host migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize two approvals of the same signed continuation</name>
  <files>test/sigra/app_login/concurrency_test.exs</files>
  <read_first>
    - test/sigra/app_login/concurrency_test.exs
    - test/sigra/app_login_test.exs
    - lib/sigra/app_login.ex
    - lib/sigra/app_login/attempt.ex
  </read_first>
  <behavior>
    - Two callers released from explicit ready/go barriers approve the identical signed continuation concurrently.
    - The result multiset is one success and one invalid-continuation result, independent of winner identity.
    - PostgreSQL retains one approval digest and one hosted-code row; the winning code remains exchangeable exactly once into one family.
  </behavior>
  <action>Extend the PostgreSQL concurrency fixture with the generated approval-digest column and named unique index. Start a real hosted continuation, allow two Sandbox callers, synchronize them with the existing ready/go mailbox barrier, and invoke `approve_hosted/5` concurrently with the same user and continuation. Assert only the result multiset and persisted state. Exchange the one returned code, then prove one family/two tokens and ordinary hosted-code replay rejection. Do not use sleeps, retry loops, or a designated winner. Retain the existing exchange/direct-MFA concurrency cases unchanged.</action>
  <acceptance_criteria>
    - Repeated focused runs deterministically observe one approval success, one bounded rejection, and one digest/code row.
    - The winning code exchanges once into one normal app-session family.
    - All pre-existing hosted exchange and direct-MFA barrier tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login/concurrency_test.exs --trace</automated>
  </verify>
  <done>PostgreSQL uniqueness serializes concurrent signed-continuation approval to one hosted code and one app session.</done>
  <reversibility rating="reversible">This task adds test evidence for the storage contract and changes no additional production interface.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- Generated attempt schema and migration support for a unique digest-only approval binding.
- Installer matrix coverage showing APP-01 independence remains intact.
- Barrier-controlled PostgreSQL proof of atomic same-continuation approval.

<assumption_delta_decision>
no-change — the generalized app-session contract remains primary, hosted/direct remain ceremony variants, and the digest column records one approval event rather than introducing a new identity key.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| library write → generated host schema | The host schema/migration must enforce the same digest contract the library assumes. |
| concurrent approval callers → PostgreSQL | Only the unique database constraint may determine the winner; application preflight reads are insufficient. |
| installer flags → generated inventory | Storage support must follow app sessions without enabling direct/API/JWT features. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-48 | Tampering | generated migration/schema | high | mitigate | Render a binary digest field and stable unique index in prefix-aware and unprefixed branches. |
| T-246-49 | Elevation of Privilege | concurrent hosted approval | high | mitigate | Race identical continuations behind barriers and require exactly one persisted code/session outcome. |
| T-246-50 | Information Disclosure | generated attempt row | medium | mitigate | Store only the digest and retain source assertions excluding raw nonce, code, verifier, password, and secrets. |
| T-246-51 | Spoofing | installer feature selection | medium | mitigate | Re-run the APP-01 option inventory and assert unrelated features remain absent. |
</threat_model>

<verification>
Run both task commands, render/parse prefix variants, perform exact formatter checks, and run `git diff --check`. Because a generated schema/migration changes, also run the project's schema-drift contract if present and record its result in the summary.
</verification>

<success_criteria>
A fresh independently selected app-session host enforces atomic one-use hosted approval under replay and concurrency without adding identity or authority surface.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-16-SUMMARY.md` when done.
</output>
