# Deferred Items

- `state.advance-plan` cannot parse the pre-existing `STATE.md` plan-position fields (`Cannot parse Current Plan or Total Plans in Phase from STATE.md`). Other SDK state updates completed. Repair the state-format parser or normalize the state position before the next plan advances it.
