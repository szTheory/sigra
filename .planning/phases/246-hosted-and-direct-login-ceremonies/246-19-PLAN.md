---
phase: 246-hosted-and-direct-login-ceremonies
plan: 19
type: execute
wave: 15
depends_on: [246-17]
files_modified:
  - lib/sigra/app_login.ex
  - priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex
  - priv/templates/sigra.install/app_sessions/app_login_controller.ex
  - test/support/app_login_schemas.ex
  - test/sigra/app_login_test.exs
  - test/sigra/app_login/concurrency_test.exs
  - test/sigra/install/app_sessions_generator_test.exs
  - test/sigra/install/app_sessions_routes_test.exs
autonomous: true
gap_closure: true
requirements: [APP-02]
must_haves:
  truths:
    - "Cancelling a valid signed hosted continuation atomically claims its nonce as a terminal `:hosted_cancel` decision and produces no authorization code or app-session family (D-03, D-09)."
    - "A byte-for-byte continuation copied before cancellation cannot later approve, even from another browser session or concurrent caller (D-03, D-04, D-09)."
    - "Concurrent approve-versus-cancel callers resolve to exactly one terminal decision through PostgreSQL uniqueness, with no application-selected winner and no sleeps (D-09)."
  artifacts:
    - path: "lib/sigra/app_login.ex"
      provides: "Shared transactional hosted-decision claim for approve and cancel"
    - path: "priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex"
      provides: "Generated `:hosted_cancel` terminal decision discriminator"
    - path: "test/sigra/app_login_test.exs"
      provides: "Copied-pre-cancel continuation and rollback-safe terminality regressions"
    - path: "test/sigra/app_login/concurrency_test.exs"
      provides: "Barrier-controlled approve-versus-cancel PostgreSQL race proof"
  key_links:
    - from: "lib/sigra/app_login.ex"
      to: "configured app_login_code_schema"
      via: "approve and cancel both write the identical arbitration key `approval_digest: Token.hash_token(approval_nonce)` in a repository transaction governed by the existing named unique constraint; the cancel row's separate domain-separated `digest` is not the decision arbiter"
      pattern: "approval_digest|hosted_cancel|repo.transaction"
    - from: "priv/templates/sigra.install/app_sessions/app_login_controller.ex"
      to: "Sigra.AppLogin.approve_hosted/5"
      via: "the cancellation POST awaits durable terminal claim before clearing only its local Plug-session copy"
      pattern: "approve_hosted.*:cancel"
  assumptions:
    - id: A-246-APP-01-CONCURRENCY
      status: flagged
      statement: "APP-01 concurrency remains unresolved by the spec-less edge probe; this plan changes no installer selection race and makes no new APP-01 concurrency claim."
    - id: A-246-APP-02-UNCLASSIFIED
      status: flagged
      statement: "APP-02 has an unresolved unclassified spec-less edge; this plan resolves only copied-continuation cancellation terminality and approve-versus-cancel serialization."
    - id: A-246-APP-03-UNCLASSIFIED
      status: flagged
      statement: "APP-03 has an unresolved unclassified spec-less edge; direct password/MFA challenges do not use the hosted continuation nonce and remain unchanged."
---

<objective>
Make hosted cancellation a durable, atomically terminal decision for the same nonce that already serializes approval.

Purpose: Close the copied-continuation cancellation blocker while preserving the exact callback, state, S256, 60-second code, explicit UI decision, and Phase 245 issuance contracts in D-03, D-04, and D-09.
Output: A shared transactional decision claim, generated terminal discriminator support, copied-handle and concurrent race regressions, and unchanged cancellation UI transport.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RESEARCH.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-PATTERNS.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-UI-SPEC.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-15-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-16-SUMMARY.md

<interfaces>
`Sigra.AppLogin.start_hosted/3` signs an `approval_nonce`; `approve_hosted/5` hashes it into the generated schema's unique `approval_digest`. Approval currently inserts a `:hosted_code` row in one `Ecto.Multi`, while cancellation returns `{:ok, :cancelled}` without validating or claiming the nonce.

`UserAppLoginAttempt.kind` is an Ecto.Enum backed by the migration's unconstrained string column. Adding `:hosted_cancel` requires schema enum parity in the library test schema and generated template, but no database column/index migration: existing generated hosts already store arbitrary kind strings and already enforce the unique nonce digest.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Cancel one copied continuation into a durable terminal decision</name>
  <files>lib/sigra/app_login.ex, test/support/app_login_schemas.ex, test/sigra/app_login_test.exs</files>
  <read_first>
    - lib/sigra/app_login.ex
    - test/support/app_login_schemas.ex
    - test/sigra/app_login_test.exs
    - test/sigra/app_login_audit_cofate_test.exs
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-15-SUMMARY.md
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
  </read_first>
  <behavior>
    - A caller copies a valid signed continuation, cancels the original, then approval with the untouched copy returns `:invalid_continuation`; no hosted code, callback redirect, or family exists.
    - Cancellation validates signature, TTL, static profile/callback, user binding, and nonce exactly as approval does before inserting one terminal `:hosted_cancel` row whose `approval_digest` is exactly `Token.hash_token(approval_nonce)`, identical to the value approval would write for that continuation.
    - The cancel row's required non-null `digest` is exactly `Token.hash_token("hosted_cancel:" <> approval_nonce)`; this domain-separated row digest is never used to arbitrate approve versus cancel.
    - Cancellation persists no raw nonce or authorization code; its decision row is already terminal and cannot be exchanged.
    - A database failure rolls back cancellation completely so the same still-valid continuation may be retried; a committed cancellation cannot be retried or changed to approval.
  </behavior>
  <action>Start RED with the exact copied-pre-cancel sequence against real PostgreSQL: bind two variables to the byte-identical signed continuation, cancel through the public facade, then approve the retained copy and assert bounded rejection plus zero active hosted-code/family/token rows. Refactor `approve_hosted/5` so both `:approve` and `:cancel` first run the existing continuation/user/schema validation and then write the identical arbitration key `approval_digest: Token.hash_token(approval_nonce)` through one shared repository transaction governed by the existing named unique constraint per D-03/D-09. Approval retains the current `:hosted_code`, generated opaque code digest, 60-second expiry, callback/state result, and post-commit raw code release. Cancellation inserts a `:hosted_cancel` decision row with both `approval_digest: Token.hash_token(approval_nonce)` and the separate non-null row key `digest: Token.hash_token("hosted_cancel:" <> approval_nonce)`, sets terminal lifecycle state in the same insert, returns no code/callback/state, and exposes `{:ok, :cancelled}` only after commit. The domain-separated `digest` satisfies the existing row contract and must not participate in approve-versus-cancel arbitration; only the shared `approval_digest` and its existing unique constraint choose the committed decision. Add the enum value to the representative test schema. Normalize duplicate, stale, malformed, wrong-user, and constraint outcomes to `:invalid_continuation`; do not clear a browser session as a substitute for the database claim, add another nonce table, expose a raw decision token, or weaken the existing unique index.</action>
  <acceptance_criteria>
    - The copied-pre-cancel test proves the retained signed value cannot approve after committed cancellation and that no code/session artifacts exist.
    - One committed cancellation row contains `kind: :hosted_cancel`, `approval_digest == Token.hash_token(approval_nonce)`, `digest == Token.hash_token("hosted_cancel:" <> approval_nonce)`, terminal lifecycle state, and no reusable code material.
    - A deterministic assertion derives the approval key from the same continuation nonce and proves the cancel row's `approval_digest` equals the value approval would persist, while its domain-separated `digest` differs and is not the decision arbiter.
    - Injected transaction failure produces no terminal row and leaks no result; retry succeeds only because the first transaction did not commit.
    - Sequential approve→cancel, cancel→approve, repeated cancel, expired/tampered continuation, and wrong-user cases all reject the second/invalid decision uniformly; approve and cancel for one continuation can never both commit.
  </acceptance_criteria>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login_test.exs test/sigra/app_login_audit_cofate_test.exs --trace</automated>
  </verify>
  <done>A durable cancellation claims the signed continuation nonce exactly once, and a copied pre-cancel continuation cannot later mint a hosted code.</done>
  <reversibility rating="costly">The internal generated schema enum gains a persisted terminal discriminator; reversal requires coordinated handling of existing `hosted_cancel` rows, though the database column/index format is unchanged.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate terminal cancellation through generated storage and concurrent controller decisions</name>
  <files>priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex, priv/templates/sigra.install/app_sessions/app_login_controller.ex, test/sigra/install/app_sessions_generator_test.exs, test/sigra/install/app_sessions_routes_test.exs, test/sigra/app_login/concurrency_test.exs</files>
  <read_first>
    - priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex
    - priv/templates/sigra.install/app_sessions/app_login_controller.ex
    - priv/templates/sigra.install/app_sessions/app_login_continuation.ex
    - priv/templates/sigra.install/app_sessions/app_sessions_migration.exs
    - test/sigra/install/app_sessions_generator_test.exs
    - test/sigra/install/app_sessions_routes_test.exs
    - test/sigra/app_login/concurrency_test.exs
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-16-SUMMARY.md
  </read_first>
  <behavior>
    - A fresh generated attempt schema accepts `:hosted_cancel` while the existing migration and named unique `approval_digest` index remain byte-compatible and enforce one decision.
    - The generated cancellation controller clears its local `AppLoginContinuation` only after `AppSessions.approve_hosted(..., :cancel)` commits; a failed claim returns the generic HTTP 400 and leaves no success redirect.
    - Two ready/go callers using one signed continuation, one approving and one cancelling, produce a result multiset containing exactly one success and one invalid-continuation rejection.
    - If cancel wins there is one terminal cancel row and no code/family; if approve wins there is one active hosted-code row whose returned code exchanges exactly once. Tests assert invariants, not winner identity.
  </behavior>
  <action>Add `:hosted_cancel` to the generated attempt Ecto.Enum and pin that parity in the generator suite; do not change `app_sessions_migration.exs` because `kind` is already an unconstrained string and the existing named unique `approval_digest` index is the required database arbiter. Keep `AppLoginController.cancel/2` as the separate CSRF-protected POST from D-03/UI-SPEC, but add executable or rendered transport evidence that it awaits durable service success before `AppLoginContinuation.take/1` and uses only the generic invalid-request response on failure. Extend the real PostgreSQL ready/go suite with approve and cancel callers released together against the same continuation. Assert the allowed result/state alternatives as invariant branches, assert both attempted decisions derive the identical `approval_digest: Token.hash_token(approval_nonce)`, and prove exactly one can commit under that constraint. When cancel wins, additionally assert its `digest: Token.hash_token("hosted_cancel:" <> approval_nonce)` is distinct from `approval_digest` and is not treated as the arbitration key. Exchange only when approval wins, and prove cancellation never returns a callback/code or creates a family. Retain the existing two-approval race and use no sleeps, retries, application locks, preflight winner reads, or designated caller ordering.</action>
  <acceptance_criteria>
    - Generated schema rendering/parsing recognizes `:hosted_cancel`; prefix/unprefixed migration source and APP-01 inventory remain unchanged and green.
    - Generated controller coverage proves the local session handle is taken only after a committed terminal decision and error responses expose no profile/callback/state/code facts.
    - Repeated concurrency runs always produce one terminal decision and one rejection, with persistence matching whichever decision won; approve and cancel cannot both commit because both contend on the identical nonce-derived `approval_digest`.
    - Existing hosted exchange, two-approval, and direct-MFA concurrency tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/install/app_sessions_generator_test.exs test/sigra/install/app_sessions_routes_test.exs test/sigra/app_login/concurrency_test.exs --trace</automated>
  </verify>
  <done>Generated hosts persist terminal cancellation and PostgreSQL deterministically serializes approve-versus-cancel without changing installer independence or the direct ceremony.</done>
  <reversibility rating="costly">Generated schema consumers must retain awareness of the persisted `:hosted_cancel` enum value even though no migration or public route changes.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- `:hosted_cancel` as the terminal generated/test `UserAppLoginAttempt.kind` discriminator.
- A shared internal transactional hosted-decision claim inside `Sigra.AppLogin.approve_hosted/5`; no new public function is required.
- Copied-pre-cancel, rollback, sequential-decision, generated-controller ordering, and approve-versus-cancel concurrency regressions in existing test files.
- No new file, migration, API matrix, external package, admin UI, or credential representation.

<ui_considerations>
Lift the Phase 246 UI-SPEC cancellation contract unchanged: “Decline app sign-in immediately consumes the bounded continuation and returns to normal sign-in; no modal, typed confirmation, or extra confirmation page.” Keep two separate CSRF-protected POST forms and the existing stable hooks. Native navigation remains the only loading treatment. Cancellation produces no callback redirect, credential, account-specific explanation, new styling, or visible success screen; invalid/consumed state returns exactly `Invalid app login request.` with HTTP 400 and no sensitive context.
</ui_considerations>

<assumption_delta_decision>
no-change — evidence: cancellation uses the existing signed continuation nonce and generated attempt row, so it introduces neither a new identity noun nor a new primary lookup key. Promote no assumption; add `:hosted_cancel` alongside the existing internal decision discriminator only to make the already-required decline decision durable and observable.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser cancellation POST → ceremony service | Clearing one cookie cannot revoke copies; the signed nonce must cross into durable terminal state. |
| approve/cancel callers → PostgreSQL | Competing decisions for one nonce must serialize through the existing unique digest constraint. |
| committed decision → browser response | A cancel redirect or approval callback may occur only after its corresponding transaction commits. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-60 | Elevation of Privilege | copied signed continuation | high | mitigate | Persist a terminal cancel row keyed by the same unique nonce digest and prove later approval rejects. |
| T-246-61 | Tampering | approve-versus-cancel race | high | mitigate | One PostgreSQL unique constraint chooses the sole committed decision; barrier tests assert result/state invariants. |
| T-246-62 | Repudiation | hosted decision state | medium | mitigate | Store an explicit `:hosted_cancel` discriminator plus bounded server-selected bindings; no raw nonce/code is retained. |
| T-246-63 | Information Disclosure | cancellation/error response | high | mitigate | Return no callback/code/state/credential on cancel and preserve the generic HTTP 400 failure surface. |
| T-246-64 | Denial of Service | duplicate decision submissions | low | accept | Duplicate decisions incur one bounded unique-constraint rejection on an authenticated CSRF-protected route; no unbounded retry or work queue is added. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Secure hosted or policy-gated direct acquisition of the same app session | 246-19 | COVERED | Makes hosted decline terminal without changing direct acquisition. |
| REQ | APP-01 | Independent installer selection | 246-06/07/10/16/17 + 246-19 regression | COVERED | Generator inventory is rerun; no flag or emitted-file selection changes. |
| REQ | APP-02 | Hosted S256/state/exact callback/explicit continuation/single-use code | 246-19 | COVERED | The explicit cancel decision now consumes the same nonce authority as approval. |
| REQ | APP-03 | Uniform policy-gated direct password/MFA | 246-04/05/12/13/17 | COVERED | Direct challenge state is separate and untouched. |
| RESEARCH | — | Signed bounded continuation and explicit approve/cancel | 246-19 | COVERED | Existing signed nonce becomes durably terminal on either decision. |
| RESEARCH | — | One PostgreSQL transaction, uniqueness, barriers, no sleeps | 246-19 | COVERED | Cancel insert and approve/cancel race use the established transaction/index pattern. |
| CONTEXT | D-02 | Static public profiles and bounded server facts | 246-19 | COVERED | Cancel row carries only existing server-selected bindings and digests. |
| CONTEXT | D-03 | Explicit approve/cancel and atomic single use | 246-19 | COVERED | Both decisions claim one nonce; approval code behavior is unchanged. |
| CONTEXT | D-04 | One bounded signed continuation | 246-19 | COVERED | No second browser transport or raw payload is added. |
| CONTEXT | D-09 | Transactional terminal state and barrier concurrency | 246-19 | COVERED | PostgreSQL uniqueness serializes approve/cancel and failures roll back. |

Deferred native SDK, PWA/offline/media, Crosswake, Electron, OAuth/OIDC-server, dynamic-registration, and admin/operator ideas remain excluded. Spec-less edge rows A-246-APP-01-CONCURRENCY, A-246-APP-02-UNCLASSIFIED, and A-246-APP-03-UNCLASSIFIED are preserved as explicit flagged assumptions; none is silently classified or dismissed. Descriptor-less prohibition recall yields no bespoke values/safety/ethics item beyond the canon authentication threats already routed to this ASVS threat model, so no fabricated `must_haves.prohibitions` descriptor is emitted.

<verification>
Run both task commands with real PostgreSQL, repeat the approve-versus-cancel focused test enough to prove scheduling independence without sleeps, render/parse the generated schema/controller, run exact formatter checks, and finish with `git diff --check`. Do not run a schema push: this Elixir/Ecto change adds an enum value over the existing unconstrained string column and changes no migration artifact.
</verification>

<success_criteria>
A copied signed continuation is powerless after committed cancellation, approve-versus-cancel concurrency yields one terminal decision, and generated hosts retain the exact existing explicit-decision UI and installer contract.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-19-SUMMARY.md` when done.
</output>
