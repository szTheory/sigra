---
phase: 246-hosted-and-direct-login-ceremonies
plan: 07
type: execute
wave: 2
depends_on: [246-01, 246-06]
files_modified:
  - lib/sigra/install/features/app_sessions.ex
  - priv/templates/sigra.install/app_sessions/user_app_session_family.ex
  - priv/templates/sigra.install/app_sessions/user_app_session_token.ex
  - priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex
  - priv/templates/sigra.install/app_sessions/app_sessions_migration.exs
  - priv/templates/sigra.install/app_sessions/first_party_apps.ex
  - priv/templates/sigra.install/app_sessions/auth_app_sessions.ex
  - test/sigra/install/app_sessions_generator_test.exs
autonomous: true
requirements: [APP-01, APP-02, APP-03]
must_haves:
  truths:
    - "A selected host receives paired Phase 245 schemas, digest-only ceremony storage, static profiles, and an issuance delegate; unselected hosts receive none (D-01, D-02, D-08)."
    - "Generated storage has the unique digests, FKs, and lifecycle indexes required for locked code/challenge exchange."
  artifacts:
    - path: "priv/templates/sigra.install/app_sessions/app_sessions_migration.exs"
      provides: "Host-owned family/token/attempt tables and indexes"
    - path: "priv/templates/sigra.install/app_sessions/first_party_apps.ex"
      provides: "Static public-client profiles with exact callback policy"
    - path: "priv/templates/sigra.install/app_sessions/auth_app_sessions.ex"
      provides: "Thin host policy/delegate seam into Sigra.AppLogin/AppSession"
  key_links:
    - from: "priv/templates/sigra.install/app_sessions/auth_app_sessions.ex"
      to: "lib/sigra/app_login.ex"
      via: "validated config and host password/MFA callbacks"
      pattern: "Sigra.AppLogin"
---

<objective>
Generate the host-owned schema, migration, static profile, configuration, and delegate seams consumed by the ceremony library.

Purpose: Turn the library tracer into an installable host contract without moving lifecycle authority into templates.
Output: App-session/attempt templates registered behind the independent feature and rendered-source tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RESEARCH.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-01-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-06-SUMMARY.md
@priv/templates/sigra.install/core/user_api_token.ex
@priv/templates/sigra.install/core/api_token_migration.exs
@priv/templates/sigra.install/core/auth_jwt.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render one migratable host persistence contract</name>
  <files>priv/templates/sigra.install/app_sessions/user_app_session_family.ex, priv/templates/sigra.install/app_sessions/user_app_session_token.ex, priv/templates/sigra.install/app_sessions/app_sessions_migration.exs, test/sigra/install/app_sessions_generator_test.exs</files>
  <behavior>
    - Rendered family/token fields exactly satisfy Phase 245 config and queries; attempt rows distinguish hosted code and direct MFA with digest, bindings, expiry, consumed timestamp, and no raw secret.
    - Migration preserves adapter/auth-prefix conventions, unique digest constraints, FK deletion, and family/user/terminal lookup indexes.
  </behavior>
  <action>Render the Phase 245 family/token schemas and their allocated migration. Preserve Phase 245 field names/types and the repository's binary-id/adapter/auth-prefix branches. Keep all lifecycle queries in Sigra library modules. Add rendered-source and migration inventory tests, including rerun-safe timestamp allocation and negative unselected output.</action>
  <verify><automated>MIX_ENV=test mix test test/sigra/install/app_sessions_generator_test.exs --trace</automated></verify>
  <done>Generated schemas compile structurally against the Phase 245 and Phase 246 configuration contracts and the migration provides every locked-query index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render digest-only hosted-code and direct-MFA attempt storage</name>
  <files>lib/sigra/install/features/app_sessions.ex, priv/templates/sigra.install/app_sessions/user_app_login_attempt.ex, test/sigra/install/app_sessions_generator_test.exs</files>
  <behavior>
    - Attempt rows distinguish hosted code and direct MFA, are uniquely digest-addressed, and carry only bounded trusted bindings and terminal timestamps.
    - The allocated migration includes the attempt table and lock/query indexes without any plaintext ceremony material.
  </behavior>
  <action>Register the attempt schema and migration entry behind app sessions. Store kind, digest, user/profile/client/callback/PKCE binding, expiry, consumed timestamp, and bounded audit correlation; include no plaintext code/challenge/verifier/state/password/factor/client-secret fields. Test exact fields, unique digest and lifecycle indexes, feature absence, and deterministic allocated migration target.</action>
  <verify><automated>MIX_ENV=test mix test test/sigra/install/app_sessions_generator_test.exs --trace</automated></verify>
  <done>Generated attempt persistence supports both locked ceremonies while remaining digest-only and feature-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render static profiles and thin ceremony delegates</name>
  <files>lib/sigra/install/features/app_sessions.ex, priv/templates/sigra.install/app_sessions/first_party_apps.ex, priv/templates/sigra.install/app_sessions/auth_app_sessions.ex, test/sigra/install/app_sessions_generator_test.exs</files>
  <behavior>
    - Generated profile module demonstrates finite IDs, server-selected client refs, literal callbacks, and `:browser_required`/`:password_allowed` policy without secrets or runtime registration.
    - Generated delegate extends the host's existing config with all three schemas and calls only Sigra AppLogin/AppSession public facades.
    - Direct password/MFA delegates appear only under the separate password flag.
  </behavior>
  <action>Generate a static host-owned profile module and `Auth.AppSessions` facade per the recorded research resolution. Use exact callback strings and conservative `:browser_required` policy in emitted example configuration. Delegate hosted start/approval/exchange and refresh/revoke to library APIs. Under the separate direct flag only, wire existing host `Auth.authenticate_user/2`, `mfa_verify/2`, and backup verifier callbacks into the library facade; do not copy password, MFA, transaction, or token logic. Add precise positive/negative template assertions for both flags and API/JWT isolation.</action>
  <verify><automated>MIX_ENV=test mix test test/sigra/install/app_sessions_generator_test.exs test/sigra/install/features/core_test.exs --trace</automated></verify>
  <done>Selected hosts receive complete static config/delegates; password delegates are separately gated and no unselected/API/JWT surface drifts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| generated host schema/config → library | Host-owned modules become trusted lifecycle and redirect policy. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-18 | Tampering | generated attempt schema | high | mitigate | Exact field/index source tests and digest-only storage contract. |
| T-246-19 | Spoofing | static profiles | high | mitigate | Finite server-owned profile/client/callback records with no secrets or registration API. |
| T-246-20 | Elevation of Privilege | direct delegate emission | high | mitigate | Separate flag predicate and negative generated inventory assertions. |
</threat_model>

<verification>Run generator/Core isolation suites, render both binary-id/auth-prefix branches, format generated Elixir, and compile rendered modules in the generator fixture.</verification>
<success_criteria>The generator emits every required host seam with no raw-secret persistence or optional-feature coupling.</success_criteria>
<output>Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-07-SUMMARY.md` when done.</output>
