---
phase: 246-hosted-and-direct-login-ceremonies
plan: 17
type: execute
wave: 14
depends_on: [246-14, 246-16]
files_modified:
  - scripts/ci/generated-app-login-runtime-proof.sh
  - .github/workflows/generated-app-login-runtime-proof.yml
  - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
  - test/sigra/planning/phase_246_runtime_evidence_contract_test.exs
  - .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json
  - .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json
autonomous: true
gap_closure: true
requirements: [APP-01, APP-02, APP-03]
must_haves:
  truths:
    - "The PostgreSQL CI lane executes `generated-app-login-runtime-proof.sh --all` and proves controller/LiveView MFA session upgrade, atomic approval single use, direct backup-code behavior, both ceremony replays, FetchAppSession equivalence, and browser-required policy (D-03–D-09)."
    - "The passing receipt is last-written, source-SHA-bound, redacted, and committed with exact GitHub run/head/artifact provenance; a failed or mismatched run cannot produce durable passing evidence."
    - "The evidence preserves APP-01 feature independence and demonstrates hosted/direct only as ceremony variants over the same generalized app-session contract, with no new identity key or external authority (D-01, D-02, D-08)."
  artifacts:
    - path: "scripts/ci/generated-app-login-runtime-proof.sh"
      provides: "Complete PostgreSQL generated-host causal proof invoked with --all"
    - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json"
      provides: "Durable redacted runtime receipt downloaded from the successful CI artifact"
    - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json"
      provides: "Exact workflow run, head SHA, URL, conclusion, artifact identity, and receipt SHA provenance"
    - path: "test/sigra/planning/phase_246_runtime_evidence_contract_test.exs"
      provides: "Offline fail-closed contract for retained receipt and provenance"
  key_links:
    - from: ".github/workflows/generated-app-login-runtime-proof.yml"
      to: "scripts/ci/generated-app-login-runtime-proof.sh"
      via: "credential-free PostgreSQL job invokes --all and validates every causal field before upload"
      pattern: "generated-app-login-runtime-proof.sh --all"
    - from: "246-RUNTIME-PROOF-RUN.json"
      to: "246-RUNTIME-PROOF.json"
      via: "artifact SHA-256, workflow run ID/URL, and exact head SHA"
      pattern: "receipt_sha256|head_sha|run_id"
    - from: "hosted/direct access tokens"
      to: "Sigra.Plug.FetchAppSession"
      via: "both generated hosts call the identical protected proof route and compare bounded facts"
      pattern: "FetchAppSession|fetch_app_session_equivalent"
---

<objective>
Execute the repaired ceremonies in the PostgreSQL CI lane and retain durable, source-bound machine evidence.

Purpose: Convert the two behavior-unverified truths and both repaired blockers into causal evidence that can be independently checked after the CI artifact expires.
Output: Expanded `--all` proof, validated receipt schema, one successful GitHub run, and committed redacted receipt/provenance contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-13-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-14-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-16-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make `--all` causally prove every remaining Phase 246 behavior</name>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, .github/workflows/generated-app-login-runtime-proof.yml, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs</files>
  <read_first>
    - scripts/ci/generated-app-login-runtime-proof.sh
    - .github/workflows/generated-app-login-runtime-proof.yml
    - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
    - test/sigra/install/app_sessions_mfa_session_upgrade_test.exs
    - test/sigra/app_login/concurrency_test.exs
    - priv/templates/sigra.install/app_sessions/first_party_apps.ex
    - lib/sigra/plug/fetch_app_session.ex
  </read_first>
  <behavior>
    - `--all` executes the PostgreSQL controller/LiveView session-upgrade and same-continuation concurrency tests, then exercises real generated hosted and direct routes.
    - The direct generated host uses a real backup code, proves malformed factors remain uniform, and proves `ios-primary` returns `browser_required` before password authentication or family creation.
    - Hosted/direct one-time inputs reject replay, both original access credentials authenticate through the same FetchAppSession route with equivalent bounded facts, and the hosted approval continuation cannot mint a second code.
    - Receipt v3 is atomically published only after every transition succeeds and binds every changed implementation/template/test/script source by SHA-256.
  </behavior>
  <action>Start RED by expanding the source contract and workflow parser for a v3 receipt. Extend the existing disposable-host proof rather than adding a second harness. In the hosted journey, enroll the fixture for browser MFA and drive the generated completion path through to explicit approval; repeat the captured approval request and assert no second code, while the Plan 246-16 barrier test supplies the concurrent PostgreSQL proof. Run the Plan 246-14 controller/LiveView PostgreSQL suite inside `--all`. In the direct journey, keep the real backup-code completion and add a request against the static browser-required profile that proves the policy result precedes authentication and leaves the family count unchanged. Preserve both HTTP replay checks and compare the two FetchAppSession responses for the same credential kind/method/scopes/assurance shape. Set each receipt boolean only after its assertion function returns successfully, bind exact source hashes including both MFA templates and approval storage templates, and rename the receipt as the final successful filesystem write. Update CI parsing before artifact upload. Keep bounded readiness probes and one workflow job; add no sleeps, secrets, dynamic clients, or request-selected authority.</action>
  <acceptance_criteria>
    - The CI workflow's only proof command is `scripts/ci/generated-app-login-runtime-proof.sh --all` against its PostgreSQL service.
    - Receipt v3 independently records controller MFA upgrade, LiveView MFA upgrade, approval replay rejection, direct backup-code success, hosted/direct replay rejection, FetchAppSession equivalence, and browser-required-before-authentication.
    - Missing/false fields, stale schema, source SHA mismatch, raw secrets, or any failed transition prevent a passing receipt and artifact validation.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs --trace &amp;&amp; bash -n scripts/ci/generated-app-login-runtime-proof.sh &amp;&amp; ruby -e 'require "yaml"; YAML.load_file(".github/workflows/generated-app-login-runtime-proof.yml")'</automated>
  </verify>
  <done>The dedicated PostgreSQL lane has one fail-closed `--all` contract covering every remaining runtime claim.</done>
  <reversibility rating="reversible">The harness, receipt schema, and workflow assertions are evidence contracts with no production data migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch once and retain the exact successful CI evidence</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`; the current branch is non-detached and pushable; one `gh api rate_limit` read reports REST core remaining above 250; Plans 246-14 through 246-16 are committed; and no watcher is already polling this workflow run.</precondition>
  <files>.planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json, .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json, test/sigra/planning/phase_246_runtime_evidence_contract_test.exs</files>
  <read_first>
    - AGENTS.md
    - .github/workflows/generated-app-login-runtime-proof.yml
    - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
    - .planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
  </read_first>
  <behavior>
    - Exactly one workflow-dispatch run for the pushed implementation head is selected by workflow, event, branch, and head SHA.
    - One watcher uses the repository-required 60-second interval; after success, the structured summary is fetched once and the named artifact is downloaded once.
    - The retained receipt equals the downloaded artifact byte-for-byte and its provenance records the exact successful run/head/artifact plus receipt SHA-256.
  </behavior>
  <action>Commit Task 1, push the current evidence branch, record its exact head SHA, and dispatch `generated-app-login-runtime-proof.yml` for that ref. Identify the single new `workflow_dispatch` run bound to the exact head SHA without racing unrelated runs. Follow AGENTS.md: use one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; after completion fetch the structured summary once, and fetch failed logs only if the conclusion is failure. Retry one transient non-rate-limit failure once. Treat HTTP 403/429 or core remaining at/below 250 as a hard stop until the reported reset/retry time; never substitute local evidence. On success, download the named artifact once to a temporary directory, validate v3 fields and all source hashes against the dispatched head, redact-check it, and copy it byte-for-byte to `246-RUNTIME-PROOF.json`. Write `246-RUNTIME-PROOF-RUN.json` with repository, workflow path/name, event, run ID/URL, conclusion, created/updated timestamps, exact head SHA/ref, artifact name/ID/expiry if returned, receipt SHA-256, literal reproduction commands, and observed-at timestamp. Add an offline ExUnit contract that rejects run/head/workflow mismatch, non-success, missing behavior fields, receipt/provenance SHA mismatch, unknown fields that carry secrets, or source hashes that differ from the committed implementation. Commit only these redacted durable files; do not retain logs, cookies, tokens, temporary hosts, or raw credentials.</action>
  <acceptance_criteria>
    - Both committed JSON files parse, the provenance receipt hash matches byte-for-byte, and every v3 behavior/source binding validates offline.
    - Provenance names one successful workflow-dispatch run whose head SHA is the implementation commit tested by CI.
    - No local run, skipped job, expired artifact pointer, or manually edited receipt can satisfy the offline contract.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs test/sigra/planning/phase_246_runtime_evidence_contract_test.exs --trace &amp;&amp; jq -e '.status == "passed"' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF.json &amp;&amp; jq -e '.conclusion == "success" and (.run_id | type == "number") and (.head_sha | test("^[0-9a-f]{40}$"))' .planning/phases/246-hosted-and-direct-login-ceremonies/246-RUNTIME-PROOF-RUN.json</automated>
  </verify>
  <done>A successful PostgreSQL CI run is preserved as redacted, source-bound, independently verifiable Phase 246 evidence.</done>
  <reversibility rating="reversible">The retained evidence can be superseded by a later exact-SHA run without changing product data or public APIs.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- Receipt-v3 generated-host proof for MFA rotation, continuation single use, direct backup code, replay, FetchAppSession equivalence, and browser-required policy.
- One durable committed runtime receipt and one exact GitHub run/artifact provenance record.
- Offline contract tests that fail closed on stale, missing, edited, secret-bearing, or source-mismatched evidence.
- No `COVERAGE.md` change: this closure adds no external third-party API/SDK integration and the existing no-external-capability declaration remains accurate; no capability matrix is fabricated.

<assumption_delta_decision>
no-change — the generalized app-session contract remains primary, hosted/direct are ceremony variants that converge on FetchAppSession, and neither the runtime proof nor the nonce/session repairs introduce a new identity key.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated HTTP ceremonies → PostgreSQL | Real one-time inputs, MFA state, and app-session families must satisfy atomic database invariants. |
| issued credentials → FetchAppSession | Both ceremony outputs cross the same explicit credential pipeline and expose only bounded facts. |
| GitHub Actions → committed evidence | Mutable external CI output becomes durable project evidence only with exact run/head/artifact/hash binding. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-52 | Spoofing | FetchAppSession equivalence proof | high | mitigate | Authenticate both real generated credentials through the identical protected route and compare bounded trusted facts. |
| T-246-53 | Elevation of Privilege | MFA/replay/browser policy journeys | high | mitigate | Execute PostgreSQL-backed MFA rotation, approval/direct replay, and pre-auth browser-required assertions before receipt publication. |
| T-246-54 | Information Disclosure | CI artifact and retained JSON | high | mitigate | Redact-check, allowlist fields, retain no logs/cookies/tokens, and bind only SHA/provenance plus booleans. |
| T-246-55 | Tampering / Repudiation | durable CI evidence | high | mitigate | Select one exact-head run, watch once, validate artifact/source hashes, and commit byte-identical receipt plus run provenance. |
| T-246-SC | Tampering | pinned Actions/Phoenix setup | medium | mitigate | Preserve pinned action SHAs and the already-audited Phoenix archive version; add no package dependency in this closure. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Gap Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Independently selected first-party sessions obtainable through hosted or policy-gated direct login | 246-14–246-17 | COVERED | Repairs hosted MFA/single-use approval and retains causal evidence for both variants. |
| REQ | APP-01 | Independent app-session/direct/API/JWT selection | 246-16, 246-17 | COVERED | Existing 246-06/07/10 contract remains green and is rechecked by generated inventory. |
| REQ | APP-02 | Hosted S256/state/exact callback/approval/60s single-use exchange | 246-14–246-17 | COVERED | Session upgrade, atomic approval consumption, concurrency, and runtime evidence close both blockers. |
| REQ | APP-03 | Uniform policy-gated direct password/MFA and shared app session | 246-17 | COVERED | CI proves real backup code, uniform invalid factor, browser-required ordering, replay, and FetchAppSession parity. |
| RESEARCH | — | Static profiles, literal callbacks, S256, digest-only ceremony evidence | 246-15–246-17 | COVERED | Existing protocol bindings remain and approval gains only a digest-backed nonce. |
| RESEARCH | — | Locked transactions, barriers, rollback, generated-host proof | 246-14–246-17 | COVERED | PostgreSQL tests and CI artifact provide the required causal evidence. |
| CONTEXT | D-01 | Independent installer flags | 246-16, 246-17 | COVERED | Inventory remains isolated. |
| CONTEXT | D-02 | Static public-client profiles, no secrets | 246-15–246-17 | COVERED | No identity/registration surface added. |
| CONTEXT | D-03 | System browser, state, S256, explicit approval, 60s single use | 246-14–246-17 | COVERED | Both verified gaps are repaired and replay-proven. |
| CONTEXT | D-04 | One bounded continuation across controller/LiveView/MFA | 246-14, 246-15, 246-17 | COVERED | Session renewal preserves the signed handle and approval consumes its nonce once. |
| CONTEXT | D-05 | Uniform direct failures | 246-17 | COVERED | Existing behavior is executed through generated routes. |
| CONTEXT | D-06 | Digest-only five-minute direct MFA | 246-17 | COVERED | Real backup code and replay evidence retained. |
| CONTEXT | D-07 | Browser-required policy before authentication | 246-17 | COVERED | Generated route proves no verification/family creation occurs first. |
| CONTEXT | D-08 | Hosted/direct use the same AppSession issuer | 246-17 | COVERED | Both outputs authenticate through identical FetchAppSession facts. |
| CONTEXT | D-09 | Same transaction, post-commit secrets, barrier concurrency | 246-15–246-17 | COVERED | Approval digest/code co-fate and deterministic races are PostgreSQL-backed. |

Deferred native SDK, PWA/offline/media, Crosswake, Electron, OAuth/OIDC-server, dynamic-registration, and admin/operator ideas remain excluded.

<verification>
Run both task commands, the complete focused Phase 246 library/generator/continuation/concurrency suite, exact formatter/shell/YAML checks, the offline evidence mutation tests, and `git diff --check`. The summary must record the exact GitHub run URL/head SHA and retained receipt/provenance paths.
</verification>

<success_criteria>
APP-01/APP-02/APP-03 have durable PostgreSQL-backed CI evidence: both hosted repairs hold, direct backup and browser policy execute, both replays reject, and both credentials converge on the same FetchAppSession contract.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-17-SUMMARY.md` when done.
</output>
