---
phase: 246-hosted-and-direct-login-ceremonies
plan: 03
type: tdd
wave: 3
depends_on: [246-02]
files_modified:
  - lib/sigra/app_login.ex
  - lib/sigra/app_login/attempt.ex
  - test/sigra/app_login_audit_cofate_test.exs
  - test/sigra/app_login/concurrency_test.exs
autonomous: true
requirements: [APP-02]
must_haves:
  truths:
    - "Two simultaneous exchanges can never issue two app sessions from one hosted code (D-08, D-09)."
    - "Expiry, replay, PKCE mismatch, and persistence/audit faults leak no credential and preserve atomic state."
  artifacts:
    - path: "test/sigra/app_login/concurrency_test.exs"
      provides: "Barrier-controlled real PostgreSQL exchange proof"
    - path: "test/sigra/app_login_audit_cofate_test.exs"
      provides: "Audit-on/off and constraint rollback proof"
  key_links:
    - from: "lib/sigra/app_login/attempt.ex"
      to: "lib/sigra/app_session.ex"
      via: "one locked Multi under real concurrent callers"
      pattern: "lock.*FOR UPDATE|lock:.*update"
---

<objective>
Harden hosted exchange with deterministic replay, concurrency, audit co-fate, and fault evidence.

Purpose: Prove D-08/D-09 under adversarial scheduling and rollback, not only sequential behavior.
Output: Real PostgreSQL barrier tests, fault injection, and bounded audit behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-02-SUMMARY.md
@.planning/phases/245-opaque-app-session-core/245-04-SUMMARY.md
@test/sigra/app_session/concurrency_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Serialize two real hosted-code exchanges</name>
  <files>lib/sigra/app_login.ex, lib/sigra/app_login/attempt.ex, test/sigra/app_login/concurrency_test.exs</files>
  <behavior>
    - Two Sandbox-allowed callers signal ready, receive one go barrier, and race the same code; exactly one returns an app session and one returns the uniform terminal code result.
    - Persisted state contains one consumed attempt, one family, and exactly one access/refresh pair.
  </behavior>
  <action>Adapt the Phase 245 ready/go barrier and Sandbox ownership pattern to the hosted code row. Exercise the public exchange facade with a real PostgreSQL row lock; assert result multiset and exact row state rather than winner identity. Add no sleeps, retries, mocked locks, polling loops, or timing-based ordering.</action>
  <verify><automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login/concurrency_test.exs --trace</automated></verify>
  <done>Concurrent replay deterministically produces one committed session and one bounded rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove hosted audit and persistence faults roll back together</name>
  <files>lib/sigra/app_login.ex, test/sigra/app_login_audit_cofate_test.exs</files>
  <behavior>
    - Audit enabled and disabled modes have identical lifecycle results and bounded metadata.
    - Injected audit or credential-row constraint failures consume no code, create no family, and return no raw credential; the same code remains usable after cleanup.
  </behavior>
  <action>Compose optional hosted-exchange audit into the existing transaction with only action, attempt ID, profile ID, and family ID; exclude state, callback, verifier, code/challenge, digests, password, user email, and credentials. Emit telemetry only after commit. Use temporary deterministic PostgreSQL constraints/triggers with cleanup in `on_exit`; failures remain test failures and are never suppressed.</action>
  <verify><automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login_audit_cofate_test.exs test/sigra/app_login/concurrency_test.exs --trace</automated></verify>
  <done>Audit-on/off, rollback recovery, expiry boundary, replay, and two-caller behavior are proven without secret leakage or timing sleeps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| concurrent public callers → one attempt row | Adversarial callers race the same bearer code. |
| transaction → audit/telemetry | Security evidence must co-fate without exposing secrets. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-07 | Elevation of Privilege | concurrent exchange | high | mitigate | Exact digest row `FOR UPDATE` and one terminal transition in the issuance Multi. |
| T-246-08 | Repudiation | hosted audit | medium | mitigate | Bounded same-transaction audit plus post-commit telemetry. |
| T-246-09 | Information Disclosure | fault paths | high | mitigate | Fault tests assert no raw secret in result, row, audit, telemetry, or exception text. |
</threat_model>

<verification>Run both PostgreSQL suites together, exact formatter checks, `git diff --check`, and a source scan proving no sleep-based coordination.</verification>
<success_criteria>Hosted exchange remains single-use and atomic under replay, faults, and two concurrent callers.</success_criteria>
<output>Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-03-SUMMARY.md` when done.</output>
