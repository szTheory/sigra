---
phase: 246-hosted-and-direct-login-ceremonies
plan: 04
type: tdd
wave: 4
depends_on: [246-03]
files_modified:
  - lib/sigra/app_login.ex
  - lib/sigra/app_login/attempt.ex
  - test/sigra/app_login_direct_test.exs
autonomous: true
requirements: [APP-03]
must_haves:
  truths:
    - "A password-allowed profile returns either the same app session, an opaque five-minute MFA challenge, or one uniform failure (D-05, D-06, D-08)."
    - "A browser-required profile returns `browser_required` before password authentication or issuance (D-07)."
  artifacts:
    - path: "test/sigra/app_login_direct_test.exs"
      provides: "Direct password/MFA/policy state-machine evidence"
  key_links:
    - from: "lib/sigra/app_login.ex"
      to: "lib/sigra/app_session.ex"
      via: "direct password and MFA successes compose the same issuance Multi"
      pattern: "AppSession.*issue"
---

<objective>
Add the optional first-party direct password/MFA ceremony while preserving one issuance contract and uniform public failures.

Purpose: Deliver APP-03 without creating an OAuth password grant or a second MFA/session lifecycle.
Output: Direct policy/password/MFA service behavior and PostgreSQL tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-RESEARCH.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-03-SUMMARY.md
@lib/sigra/mfa.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace password success and browser-required policy to their terminal result</name>
  <files>lib/sigra/app_login.ex, test/sigra/app_login_direct_test.exs</files>
  <behavior>
    - `:password_allowed` plus host-authenticated non-MFA user issues the same response map and persisted family/token shape as hosted exchange.
    - `:browser_required` returns that literal bounded outcome before invoking the injected password verifier.
    - Unknown profile/user, bad password, disabled direct surface, locked/unconfirmed user, and verifier errors return one identical status/body-domain result.
  </behavior>
  <action>Define a narrow direct-login facade that receives generated-host password and MFA callback functions rather than implementing hashing/factors. Look up static profile policy first; short-circuit `:browser_required` without invoking authentication per D-07. For allowed profiles, normalize every credential/account/policy failure to the same public result per D-05. Successful non-MFA authentication composes the Phase 245 issuance seam. Do not accept scopes, authority, client secrets, grant types, or caller-selected client references.</action>
  <verify><automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login_direct_test.exs --trace</automated></verify>
  <done>Direct password success matches hosted issuance exactly, and browser-required/uniform-failure branches expose no user or credential fact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete one opaque five-minute MFA challenge</name>
  <files>lib/sigra/app_login.ex, lib/sigra/app_login/attempt.ex, test/sigra/app_login_direct_test.exs</files>
  <behavior>
    - MFA-required password success returns only a random opaque challenge; persistence stores its digest, user/profile/client binding, and exact 300-second expiry.
    - Valid TOTP or backup-code callback under the locked challenge consumes it and issues the same app session in one transaction.
    - Wrong factor, profile mismatch, expiry, replay, malformed input, and callback error all use the same direct failure and create no family.
  </behavior>
  <action>Generate a high-entropy challenge with existing token helpers, persist only its digest and bounded trusted facts, and set expiry to exactly 300 seconds per D-06. On completion lock by digest, validate profile/user/terminal state, call the existing host MFA delegate, then consume and compose app-session issue plus optional audit in the same transaction per D-08/D-09. A failed factor must not consume the challenge; all public failures remain indistinguishable.</action>
  <verify><automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login_direct_test.exs test/sigra/app_login_test.exs --trace</automated></verify>
  <done>TOTP and backup-code success yield the hosted credential contract; opaque challenge expiry/replay/mismatch/failure reveal nothing and cannot issue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| password/MFA JSON → host verifiers | Secrets cross into existing password and factor authority. |
| profile policy → direct endpoint | Trusted server policy decides whether credentials may be evaluated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-10 | Information Disclosure | direct failures | high | mitigate | One status/body domain across user/password/MFA/account/policy failures. |
| T-246-11 | Elevation of Privilege | MFA challenge | high | mitigate | Digest-only, user/profile bound, 300-second expiry, row lock, single use, same-transaction issuance. |
| T-246-12 | Spoofing | browser-required policy | high | mitigate | Static policy lookup precedes and suppresses password verification. |
</threat_model>

<verification>Run direct and hosted equivalence suites against PostgreSQL, exact formatter checks, and source contracts excluding OAuth grant/scopes/secret vocabulary from direct routes.</verification>
<success_criteria>Direct login satisfies APP-03 with uniform denial, opaque MFA, browser-required policy, and exact Phase 245 issuance parity.</success_criteria>
<output>Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-04-SUMMARY.md` when done.</output>
