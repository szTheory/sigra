---
phase: 246-hosted-and-direct-login-ceremonies
plan: 12
type: execute
wave: 10
depends_on: [246-11]
files_modified:
  - priv/templates/sigra.install/app_sessions/app_login_controller.ex
  - priv/templates/sigra.install/app_sessions/auth_app_sessions.ex
  - test/sigra/install/app_sessions_routes_test.exs
  - test/sigra/install/app_sessions_generator_test.exs
  - scripts/ci/generated-app-login-runtime-proof.sh
  - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
  - .planning/phases/246-hosted-and-direct-login-ceremonies/COVERAGE.md
autonomous: true
gap_closure: true
requirements: [APP-03]
must_haves:
  truths:
    - "The generated direct-MFA endpoint accepts exactly one external factor selector and maps only `totp` or `backup_code` to trusted atoms before calling the library (D-05, D-06)."
    - "A fresh password-enabled generated host completes direct MFA with an actual backup code and receives the same Phase 245 app-session response as other ceremony success (D-06, D-08, D-09)."
    - "Missing, malformed, list-valued, extra, or unknown factor input retains the exact uniform `invalid_credentials` response and cannot choose an atom or verifier callback (D-05)."
  artifacts:
    - path: "priv/templates/sigra.install/app_sessions/app_login_controller.ex"
      provides: "Strict external direct-MFA factor transport and fixed allowlist conversion"
    - path: "priv/templates/sigra.install/app_sessions/auth_app_sessions.ex"
      provides: "Trusted-factor forwarding to Sigra.AppLogin"
    - path: "scripts/ci/generated-app-login-runtime-proof.sh"
      provides: "Fresh generated-host backup-code success journey"
    - path: ".planning/phases/246-hosted-and-direct-login-ceremonies/COVERAGE.md"
      provides: "Generated direct-MFA HTTP surface coverage record"
  key_links:
    - from: "priv/templates/sigra.install/app_sessions/app_login_controller.ex"
      to: "priv/templates/sigra.install/app_sessions/auth_app_sessions.ex"
      via: "allowlisted factor atom forwarded as an explicit argument"
      pattern: "backup_code.*:backup_code|totp.*:totp"
    - from: "priv/templates/sigra.install/app_sessions/auth_app_sessions.ex"
      to: "Sigra.AppLogin.complete_direct_mfa/4"
      via: "factor keyword plus both existing host-owned verifier callbacks"
      pattern: "factor: factor"
    - from: "scripts/ci/generated-app-login-runtime-proof.sh"
      to: "/api/app-login/direct/mfa"
      via: "real generated backup-code request completes the persisted opaque challenge"
      pattern: "backup_code"
  assumptions:
    - id: A-246-G12-01
      status: flagged
      statement: "APP-03 transaction and concurrent one-use semantics remain the Plan 246-05 locked-row contract; this transport repair must preserve those library guarantees and add generated-route backup-code proof."
  prohibitions:
    - id: P-246-02
      requirement_id: APP-03
      category: safety
      status: planned
      verification: "Task 1: source tmp/db.env && MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_generator_test.exs test/sigra/app_login_direct_test.exs test/sigra/app_login_direct_fault_test.exs --trace"
      statement: "The factor selector must not become a caller-controlled atom, verifier name, module, or authority selector."
      reason: "Descriptor-less adversarial-probe item carried as flagged-unverified; fixed allowlist tests are planned, but prohibition lifecycle verification was not author-resolved in a plain SPEC."
---

<objective>
Carry the advertised backup-code factor through the generated direct-MFA HTTP transport without weakening uniform failure or trusted callback selection.

Purpose: Close APP-03's generated transport gap while retaining D-05/D-06/D-08/D-09 and the existing locked challenge transaction.
Output: One fixed external selector allowlist, trusted atom forwarding, generated-host backup-code success, uniform rejection regressions, and updated API coverage evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-VERIFICATION.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-04-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-05-SUMMARY.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-11-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transport one allowlisted direct-MFA factor without dynamic atoms</name>
  <files>priv/templates/sigra.install/app_sessions/app_login_controller.ex, priv/templates/sigra.install/app_sessions/auth_app_sessions.ex, test/sigra/install/app_sessions_routes_test.exs, test/sigra/install/app_sessions_generator_test.exs, .planning/phases/246-hosted-and-direct-login-ceremonies/COVERAGE.md</files>
  <read_first>
    - priv/templates/sigra.install/app_sessions/app_login_controller.ex
    - priv/templates/sigra.install/app_sessions/auth_app_sessions.ex
    - lib/sigra/app_login.ex
    - lib/sigra/app_login/attempt.ex
    - test/sigra/install/app_sessions_routes_test.exs
    - test/sigra/install/app_sessions_generator_test.exs
    - .planning/phases/246-hosted-and-direct-login-ceremonies/COVERAGE.md
  </read_first>
  <behavior>
    - The exact direct-MFA JSON keys are `challenge`, `code`, and `factor`.
    - External `totp` maps to `:totp`; external `backup_code` maps to `:backup_code`; no other value reaches the library or a callback.
    - Every missing, empty, list-valued, unknown, or extra-key selector returns the same status, body, and headers as all existing direct credential failures.
  </behavior>
  <action>Start RED in rendered controller/facade tests. Change the generated direct-MFA endpoint to require exactly three scalar binary fields: `challenge`, `code`, and `factor`. Add a private fixed two-entry factor decoder mapping the external strings `totp` and `backup_code` to trusted atoms; use pattern matching or a literal map and never `String.to_atom/1`, `String.to_existing_atom/1`, module lookup, or callback-name lookup. Change the generated `Auth.AppSessions.complete_direct_mfa` facade to accept the trusted factor argument and forward it as `factor: factor` alongside the existing `mfa_verify` and `mfa_verify_backup` callbacks. Preserve the library's uniform `:invalid_credentials` normalization and the locked digest-only five-minute challenge transaction per D-05/D-06/D-09. Update `COVERAGE.md` to record the accepted generated HTTP field/values, uniform invalid-selector response, and why no external-service integration coverage applies; this is an existing first-party endpoint surface change, not an OAuth or third-party API.</action>
  <acceptance_criteria>
    - Rendered direct controller and facade tests prove both accepted mappings and exact rejection of all other shapes.
    - No request value is converted by any dynamic atom function or used to choose an arbitrary callback.
    - `COVERAGE.md` explicitly accounts for the generated endpoint change and contains a reason for the external-integration opt-out.
  </acceptance_criteria>
  <verify>
    <automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/install/app_sessions_routes_test.exs test/sigra/install/app_sessions_generator_test.exs test/sigra/app_login_direct_test.exs test/sigra/app_login_direct_fault_test.exs --trace</automated>
  </verify>
  <done>The generated HTTP surface selects TOTP or backup-code verification only through a fixed trusted allowlist and retains observationally uniform denial.</done>
  <reversibility rating="reversible">The change adds a required field to an unreleased generated ceremony endpoint while retaining the library API's existing optional factor contract.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete generated direct MFA with a real backup code</name>
  <files>scripts/ci/generated-app-login-runtime-proof.sh, test/sigra/planning/phase_246_generated_app_login_runtime_test.exs</files>
  <read_first>
    - scripts/ci/generated-app-login-runtime-proof.sh
    - test/sigra/planning/phase_246_generated_app_login_runtime_test.exs
    - priv/templates/sigra.install/app_sessions/first_party_apps.ex
    - priv/templates/sigra.install/app_sessions/app_login_controller.ex
    - priv/templates/sigra.install/app_sessions/auth_app_sessions.ex
    - priv/templates/sigra.install/core/user_backup_code.ex
  </read_first>
  <behavior>
    - The disposable password-enabled host provisions one MFA user and one generated-host backup code, starts direct login through the real route, and receives an opaque five-minute challenge.
    - Completing `/api/app-login/direct/mfa` with `factor=backup_code` consumes that backup code and returns the standard app-session credential map.
    - The same route with an unknown selector has the exact existing uniform denial and leaves no credential family.
  </behavior>
  <action>Extend the direct mode of the fresh-host harness to seed an MFA-enabled user and one backup code using the generated host's schemas and existing Sigra MFA helpers. POST real generated direct-password credentials, assert the response exposes only the opaque challenge, then POST the challenge, backup code, and external `backup_code` selector to the generated MFA route. Assert successful credential issuance, consumed backup-code state, consumed challenge state, and no raw challenge/code in stored ceremony rows. Add a negative request with an unknown selector and compare its serialized response with the direct uniform-failure contract. Use bounded HTTP readiness and deterministic fixtures; use no fixed-delay waiting.</action>
  <acceptance_criteria>
    - `bash scripts/ci/generated-app-login-runtime-proof.sh --direct` produces a successful generated backup-code ceremony.
    - The proof verifies the backup-code row and direct-MFA challenge are consumed exactly once and the credential response has the Phase 245 keys.
    - Unknown selector, missing selector, and malformed selector requests are indistinguishable from `invalid_credentials` and issue no family.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_246_generated_app_login_runtime_test.exs --trace &amp;&amp; bash scripts/ci/generated-app-login-runtime-proof.sh --direct</automated>
  </verify>
  <done>A fresh generated direct-login host proves advertised backup-code MFA success and uniform rejection through real routes.</done>
  <reversibility rating="reversible">Harness and generated transport changes are additive to the existing separately selected password-login capability.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- Generated direct-MFA JSON field `factor` with accepted external values `totp` and `backup_code`.
- Generated controller private fixed factor decoder returning only `:totp` or `:backup_code`.
- Generated `Auth.AppSessions.complete_direct_mfa/3` forwarding a trusted `factor:` option.
- Fresh-host direct backup-code success and invalid-selector runtime assertions.
- Updated Phase 246 `COVERAGE.md` entry for the generated HTTP contract and external-integration opt-out reason.

<assumption_delta_decision>
no-change — the primary noun remains independently selected installer capabilities. The selector belongs only to the already separate `--app-password-login` endpoint and does not unify profiles, imply app sessions, or change API/JWT selection.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| direct-MFA JSON → generated controller | An untrusted string requests one of two host-supported factor paths. |
| generated controller → host MFA facade | Only a trusted allowlisted atom may select the prewired verifier callback. |
| backup code → locked challenge transaction | Factor proof may consume the challenge and issue credentials only in the existing atomic flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-33 | Tampering | factor decoder | high | mitigate | Exact scalar keys plus literal two-value mapping; no dynamic atom or callback conversion. |
| T-246-34 | Information Disclosure | invalid factor responses | high | mitigate | Compare exact public status/body/header shape with all other direct failures. |
| T-246-35 | Elevation of Privilege | backup-code completion | high | mitigate | Existing user/profile-bound digest challenge, locked consume-and-issue Multi, and real generated backup-code proof. |
| T-246-36 | Repudiation | direct runtime evidence | medium | mitigate | Assert persisted backup-code/challenge consumption before receipt generation. |
</threat_model>

<verification>
Run both task commands, formatter checks for rendered and repository Elixir, `bash -n scripts/ci/generated-app-login-runtime-proof.sh`, a scoped check that the generated factor path contains no dynamic atom conversion, and `git diff --check`. No schema push is needed because the selector is HTTP/facade transport only.
</verification>

<success_criteria>
APP-03's advertised backup-code path works in a fresh generated host, while every untrusted selector remains uniform and incapable of choosing arbitrary atoms or authority.
</success_criteria>

<output>
Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-12-SUMMARY.md` when done.
</output>
