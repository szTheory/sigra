---
phase: 246-hosted-and-direct-login-ceremonies
plan: 09
type: execute
wave: 7
depends_on: [246-08]
files_modified:
  - priv/templates/sigra.install/core/session_controller.ex
  - priv/templates/sigra.install/core/session_live.ex
  - priv/templates/sigra.install/core/mfa_challenge_controller.ex
  - priv/templates/sigra.install/core/mfa_challenge_live.ex
  - test/sigra/install/app_sessions_auth_continuation_test.exs
autonomous: true
requirements: [APP-02, APP-03]
must_haves:
  truths:
    - "Controller and LiveView login/MFA branches preserve the same signed bounded continuation and return only to explicit approval (D-03, D-04)."
    - "Hosts without app sessions retain byte-for-byte equivalent login/MFA behavior and generated route ownership."
  artifacts:
    - path: "test/sigra/install/app_sessions_auth_continuation_test.exs"
      provides: "Focused LiveView/controller login and MFA continuation contract"
  key_links:
    - from: "priv/templates/sigra.install/core/mfa_challenge_live.ex"
      to: "priv/templates/sigra.install/app_sessions/app_login_continuation.ex"
      via: "normal MFA success restores only the server-selected approval route"
      pattern: "AppLoginContinuation"
---

<objective>
Wire the generated app-login continuation through existing controller and LiveView login/MFA branches without changing unselected-host behavior.

Purpose: Complete D-04 behind the Plan 08 continuation contract while isolating shared auth-template ownership from app-route generation.
Output: Four narrowly updated auth templates and one focused rendered continuation suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-08-SUMMARY.md
@priv/templates/sigra.install/core/session_controller.ex
@priv/templates/sigra.install/core/session_live.ex
@priv/templates/sigra.install/core/mfa_challenge_controller.ex
@priv/templates/sigra.install/core/mfa_challenge_live.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resume one hosted continuation through controller and LiveView MFA</name>
  <files>priv/templates/sigra.install/core/session_controller.ex, priv/templates/sigra.install/core/session_live.ex, priv/templates/sigra.install/core/mfa_challenge_controller.ex, priv/templates/sigra.install/core/mfa_challenge_live.ex, test/sigra/install/app_sessions_auth_continuation_test.exs</files>
  <behavior>
    - Controller and LiveView password success, MFA pending, MFA success, cancel, expiry, tamper, and re-entry preserve or reject one identical continuation contract.
    - Successful browser authentication restores only the server-selected explicit approval route; it never redirects directly to the app callback or issues credentials.
    - With app sessions unselected, rendered controller/LiveView login and MFA behavior remains unchanged.
  </behavior>
  <action>Start RED with rendered live and `--no-live` paths. Add narrowly scoped hooks that call the Plan 08 generated continuation helper only when the app-session feature is selected; preserve the same bounded handle through normal login and MFA transitions. Restore only the fixed approval route after successful browser authentication, and let expiry/tamper/cancel clear the handle without changing normal auth results. Keep direct JSON handling in the Plan 08 controller and preserve all existing session/MFA policy, passkey behavior, redirects, and unselected output. Do not duplicate signing, callback, code, password, MFA verification, or app-session issuance logic in these four templates.</action>
  <verify><automated>MIX_ENV=test mix test test/sigra/install/app_sessions_auth_continuation_test.exs test/sigra/install/generator_mfa_test.exs test/sigra/install/generator_passkey_mfa_challenge_test.exs --trace</automated></verify>
  <done>Both generated auth modes preserve one continuation through password/MFA and reach explicit approval, while no-flag hosts retain their prior behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser login/MFA state → app approval | Existing authentication state may restore only a valid server-owned continuation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-24 | Elevation of Privilege | login/MFA resume | high | mitigate | Shared helper only restores fixed approval after normal browser login/MFA succeeds. |
| T-246-28 | Tampering | signed continuation | high | mitigate | Expiry/tamper clears the handle and resumes ordinary auth behavior without callback use. |
</threat_model>

<verification>Run the focused continuation/MFA generator suites for LiveView, controller, selected, and unselected modes; run exact formatter and `git diff --check`.</verification>
<success_criteria>The Plan 08 continuation is wired across every generated browser-auth branch without expanding route or credential authority.</success_criteria>
<output>Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-09-SUMMARY.md` when done.</output>
