---
phase: 246-hosted-and-direct-login-ceremonies
plan: 05
type: tdd
wave: 5
depends_on: [246-04]
files_modified:
  - lib/sigra/app_login.ex
  - lib/sigra/app_login/attempt.ex
  - test/sigra/app_login_direct_fault_test.exs
  - test/sigra/app_login/concurrency_test.exs
autonomous: true
requirements: [APP-03]
must_haves:
  truths:
    - "Concurrent direct-MFA completion issues at most one session and cannot burn or duplicate a challenge (D-06, D-09)."
    - "Public direct failure responses are observationally uniform and persistence/audit faults leak no credential."
  artifacts:
    - path: "test/sigra/app_login_direct_fault_test.exs"
      provides: "Uniform-response and rollback fault matrix"
  key_links:
    - from: "test/sigra/app_login/concurrency_test.exs"
      to: "lib/sigra/app_login/attempt.ex"
      via: "ready/go callers exercise actual challenge row lock"
      pattern: "ready|go"
---

<objective>
Prove the direct ceremony remains uniform and atomic under replay, concurrent MFA completion, and transaction faults.

Purpose: Close the adversarial and fault branches of APP-03 with machine evidence.
Output: Direct fault/co-fate suite and barrier-controlled challenge concurrency suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md
@.planning/phases/246-hosted-and-direct-login-ceremonies/246-04-SUMMARY.md
@test/sigra/app_session/concurrency_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Race one opaque MFA challenge with two PostgreSQL callers</name>
  <files>lib/sigra/app_login.ex, lib/sigra/app_login/attempt.ex, test/sigra/app_login/concurrency_test.exs</files>
  <behavior>
    - Two ready/go callers completing one challenge produce exactly one app session, one uniform rejection, one consumed challenge, one family, and one access/refresh pair.
    - Audit enabled and disabled modes have the same concurrency result.
  </behavior>
  <action>Extend the existing hosted ready/go suite with direct MFA completion against the public facade and real PostgreSQL locks. Assert result multisets, exact terminal rows, and next authentication; do not assert scheduler order or use sleeps/retries/mock locks.</action>
  <verify><automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login/concurrency_test.exs --trace</automated></verify>
  <done>Concurrent MFA completion has exactly one committed winner and no duplicate credential family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the uniform-failure and rollback matrix</name>
  <files>lib/sigra/app_login.ex, test/sigra/app_login_direct_fault_test.exs</files>
  <behavior>
    - Unknown user, bad password, locked/unconfirmed account, wrong/expired/replayed challenge, wrong profile, MFA callback exception, and disabled direct surface share exact public status/body/header shape.
    - `browser_required` is distinct only for a valid static policy and is independent of user/password values.
    - Audit, challenge-consumption, family, or token constraint faults roll back the challenge and all issuance while returning no secret.
  </behavior>
  <action>Compare serialized public result tuples and generated-controller response contracts rather than internal error atoms. Add deterministic clock boundaries at 300 seconds and fault injection with guaranteed cleanup. Keep `browser_required` limited to the pre-auth policy branch; no other failure may select it. Bound audit metadata and verify logs/telemetry/exceptions contain no email, password, factor, raw challenge, code, callback state, or app credentials.</action>
  <verify><automated>source tmp/db.env &amp;&amp; MIX_ENV=test mix test test/sigra/app_login_direct_fault_test.exs test/sigra/app_login/concurrency_test.exs --trace</automated></verify>
  <done>Every direct denial and fault is machine-proven uniform or explicitly `browser_required`, with no secret leakage and correct rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| concurrent MFA clients → challenge row | Replayed factor submissions race one single-use record. |
| internal errors → public response | Detailed account/factor/persistence causes must be erased. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-246-13 | Elevation of Privilege | MFA race | high | mitigate | Locked digest row and deterministic two-caller PostgreSQL proof. |
| T-246-14 | Information Disclosure | error normalization | high | mitigate | Exact serialized response equality matrix across all failure dimensions. |
| T-246-15 | Repudiation | rollback/audit | medium | mitigate | Same-Multi bounded audit and post-commit telemetry with constraint-fault proof. |
</threat_model>

<verification>Run both direct hardening suites, formatter, diff check, and a no-sleep source gate scoped to the new test regions.</verification>
<success_criteria>Direct login is single-use, rollback-safe, and observationally uniform under all specified fault and concurrency cases.</success_criteria>
<output>Create `.planning/phases/246-hosted-and-direct-login-ceremonies/246-05-SUMMARY.md` when done.</output>
