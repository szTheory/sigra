---
phase: 27-admin-access-foundation
plan: 3
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - lib/sigra/admin/live/index_live.ex
  - lib/sigra/admin/live/organization_live.ex
  - test/example/lib/example/sigra_admin_policy.ex
  - test/example/lib/example_web/router.ex
  - test/example/lib/example_web/components/admin_shell.ex
  - test/example/lib/example_web/components/layouts.ex
  - test/example/test/example_web/admin_shell_test.exs
  - test/example/test/example_web/integration/phase_27_integration_test.exs
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
must_haves:
  truths:
    - "The generated/example host mounts `/admin` and `/admin/organizations/:org` through the admin enforcement stack."
    - "Admin chrome always shows `Admin` and the active scope (`Global` or an organization name)."
    - "Example integration tests prove global vs organization admin routing and denial behavior end to end."
  artifacts:
    - path: "test/example/lib/example/sigra_admin_policy.ex"
      provides: "Concrete host policy implementation used by generated/example router wiring"
    - path: "test/example/lib/example_web/components/admin_shell.ex"
      provides: "Generated/example admin chrome with sticky scope bar and navigation"
    - path: "test/example/test/example_web/integration/phase_27_integration_test.exs"
      provides: "End-to-end proof for admin route, shell, and denial behavior"
  key_links:
    - from: "test/example/lib/example_web/router.ex"
      to: "lib/sigra/plug/require_admin_access.ex"
      via: "admin pipeline and `live_session`"
      pattern: "Sigra\\.Plug\\.RequireAdminAccess|Sigra\\.LiveView\\.AdminScope"
    - from: "lib/sigra/admin/live/index_live.ex"
      to: "test/example/lib/example_web/components/admin_shell.ex"
      via: "rendered admin shell slot or helper"
      pattern: "Admin|Global"
nyquist_compliant: true
---

<objective>
Wire the new admin foundation into the generated/example host app, render the visible scope chrome, and prove the behavior with example-app integration tests.

Purpose: Phase 27 is only complete when the default generated host path actually mounts the admin surface and shows the active scope clearly to operators.
Output: Foundation admin LiveViews, example host policy module, router wiring, admin shell component, layout hook, and integration coverage for the admin entry routes.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-admin-access-foundation/27-CONTEXT.md
@.planning/phases/27-admin-access-foundation/27-UI-SPEC.md
@.planning/phases/27-admin-access-foundation/27-VALIDATION.md
@.planning/phases/14-org-plugs-scope-hydration/14-02-library-plugs-SUMMARY.md
@.planning/phases/16-org-liveviews-switcher/16-06-SUMMARY.md
@/Users/jon/projects/sigra/test/example/lib/example_web/router.ex
@/Users/jon/projects/sigra/test/example/lib/example_web/components/layouts.ex
@/Users/jon/projects/sigra/test/example/lib/example_web/components/org_switcher.ex

<interfaces>
From `test/example/lib/example_web/router.ex`:

```elixir
live_session :organization_scoped,
  on_mount: [
    {ExampleWeb.UserAuth, :ensure_authenticated},
    {ExampleWeb.UserAuth, :assign_user_organizations},
    {Sigra.LiveView.OrganizationScope,
     [organizations: Example.Organizations, scope_module: Example.Accounts.Scope]}
  ] do
```

From `test/example/lib/example_web/components/layouts.ex`:

```elixir
def app(assigns) do
  ~H"""
  <header class="navbar ...">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the example host to the admin feature and render the admin shell</name>
  <read_first>
    - test/example/lib/example_web/router.ex
    - test/example/lib/example_web/components/layouts.ex
    - test/example/lib/example_web/components/org_switcher.ex
    - lib/sigra/admin/live/index_live.ex
    - lib/sigra/admin/live/organization_live.ex
    - priv/templates/sigra.install/admin/router_injection.ex
    - priv/templates/sigra.install/admin/policy.ex
    - priv/templates/sigra.install/admin/components/admin_shell.ex
    - .planning/phases/27-admin-access-foundation/27-UI-SPEC.md
  </read_first>
  <files>lib/sigra/admin/live/index_live.ex, lib/sigra/admin/live/organization_live.ex, test/example/lib/example/sigra_admin_policy.ex, test/example/lib/example_web/router.ex, test/example/lib/example_web/components/admin_shell.ex, test/example/lib/example_web/components/layouts.ex</files>
  <action>Instantiate the generated admin host seams in the example app and add the foundation LiveViews they mount. Create `Sigra.Admin.Live.IndexLive` and `Sigra.Admin.Live.OrganizationLive` as thin placeholder LiveViews that assign titles, consume the resolved admin scope, and render through the admin shell without introducing user-operations logic yet. Create `Example.SigraAdminPolicy` that implements the new `Sigra.Admin.Policy` behaviour with explicit test-fixture logic for platform-admin and org-admin users; do not infer admin from signup order. Update `test/example/lib/example_web/router.ex` to add an admin pipeline using `Sigra.Plug.RequireAdminAccess`, and mount two `live_session` blocks for `/admin` and `/admin/organizations/:org` that pair `ExampleWeb.UserAuth` with `Sigra.LiveView.AdminScope`. Create `ExampleWeb.Components.AdminShell` using the UI-SPEC shell contract: sticky top scope bar, `Admin` label, active scope chip, separate special-session chip slot, desktop navigation groups, and mobile bottom-nav affordances. Update `layouts.ex` only as needed so the admin routes render this shell without replacing the existing application shell or org switcher patterns.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Sigra\\.Admin\\.Live\\.IndexLive|defmodule Sigra\\.Admin\\.Live\\.OrganizationLive" lib/sigra/admin/live/index_live.ex lib/sigra/admin/live/organization_live.ex` returns both module names.
    - `rg -n "defmodule Example\\.SigraAdminPolicy|platform_admin\\?|admin_org_ids" test/example/lib/example/sigra_admin_policy.ex` returns all 3 patterns.
    - `rg -n "/admin\"|/admin/organizations/:org|Sigra\\.Plug\\.RequireAdminAccess|Sigra\\.LiveView\\.AdminScope" test/example/lib/example_web/router.ex` returns all 4 patterns.
    - `rg -n "Admin|Global|bottom nav|scope" test/example/lib/example_web/components/admin_shell.ex` returns matches for visible scope chrome and mobile navigation copy/contracts.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "defmodule Sigra\\.Admin\\.Live\\.IndexLive|defmodule Sigra\\.Admin\\.Live\\.OrganizationLive" lib/sigra/admin/live/index_live.ex lib/sigra/admin/live/organization_live.ex && rg -n "defmodule Example\\.SigraAdminPolicy|platform_admin\\?|admin_org_ids" test/example/lib/example/sigra_admin_policy.ex && rg -n "/admin\"|/admin/organizations/:org|Sigra\\.Plug\\.RequireAdminAccess|Sigra\\.LiveView\\.AdminScope" test/example/lib/example_web/router.ex && rg -n "Admin|Global|scope" test/example/lib/example_web/components/admin_shell.ex'</automated>
  </verify>
  <done>The example app can mount the admin foundation routes through explicit host policy and render the persistent admin shell with visible scope context.</done>
</task>

<task type="auto">
  <name>Task 2: Add example-app shell and integration coverage for global and org admin flows</name>
  <read_first>
    - test/example/lib/example/sigra_admin_policy.ex
    - test/example/lib/example_web/router.ex
    - test/example/lib/example_web/components/admin_shell.ex
    - test/example/lib/example_web/components/layouts.ex
    - test/example/test/example_web/integration/phase_27_integration_test.exs
    - .planning/phases/27-admin-access-foundation/27-VALIDATION.md
    - .planning/phases/27-admin-access-foundation/27-RESEARCH.md
  </read_first>
  <files>test/example/test/example_web/admin_shell_test.exs, test/example/test/example_web/integration/phase_27_integration_test.exs</files>
  <action>Add example-app tests that prove ADMIN-03 through ADMIN-05 end to end. `admin_shell_test.exs` should assert the rendered shell always includes `Admin` plus either `Global` or the active organization name, and that denied states are not blank pages. `phase_27_integration_test.exs` should exercise at least these flows with seeded users/orgs: platform admin reaches `/admin` and intentionally enters `/admin/organizations/:org`; org admin is denied at `/admin`; org admin reaches only their allowed `/admin/organizations/:org`; org admin is denied on another org slug; the rendered page keeps the active scope chip visible in both global and org modes. Keep the tests inside the example app and use direct route/liveview assertions rather than browser-only checks.</action>
  <acceptance_criteria>
    - `rg -n "Admin|Global|access denied|organization" test/example/test/example_web/admin_shell_test.exs test/example/test/example_web/integration/phase_27_integration_test.exs` returns all four terms.
    - `rg -n "/admin\"|/admin/organizations/" test/example/test/example_web/integration/phase_27_integration_test.exs` returns both route patterns.
    - `rg -n "platform admin|org admin|out-of-scope" test/example/test/example_web/integration/phase_27_integration_test.exs` returns scenario coverage for all three role/scope cases.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'mix test test/sigra/templates/installer_drift_test.exs --max-failures 1 && cd test/example && mix test test/example_web/admin_shell_test.exs test/example_web/integration/phase_27_integration_test.exs --max-failures 1'</automated>
  </verify>
  <done>The example app has direct automated proof that admin shell chrome is visible and that platform-admin versus org-admin routing is enforced through the generated host integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated/example router -> library admin guards | Host wiring must preserve the library-owned enforcement stack |
| library admin LiveViews -> host shell component | Visual chrome must reflect the resolved scope without altering auth semantics |
| example test fixtures -> route assertions | Fixtures must model platform admin and org admin explicitly to catch hidden inference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | E | example host policy | mitigate | `Example.SigraAdminPolicy` uses explicit fixture-backed admin grants; tests fail if a non-admin reaches `/admin`. |
| T-27-11 | I | admin shell chrome | mitigate | Shell tests assert `Admin` plus `Global` or organization name is always rendered, preventing ambiguous scope routing and hidden admin inference. |
| T-27-12 | I | org routing | mitigate | Integration tests cover org-admin denial on `/admin` and on other organizations before later user-operations pages are added. |
| T-27-13 | T | generated/example router wiring | mitigate | Router assertions and installer drift coverage keep the generated/example route shape aligned with the feature templates and library guard modules. |
</threat_model>

<verification>
Run the example-app tests for the admin shell and integration flows, then run the installer drift suite so the generated example files stay aligned with the templates introduced in 27-01.
</verification>

<success_criteria>
- The example app mounts the admin routes through the new enforcement stack.
- Scope chrome is visible in both global and organization modes.
- Example integration tests prove platform-admin and org-admin behavior end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/27-admin-access-foundation/27-admin-access-foundation-03-SUMMARY.md`
</output>
