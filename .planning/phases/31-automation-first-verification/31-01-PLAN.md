---
phase: 31-automation-first-verification
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/playwright.config.ts
  - test/example/priv/playwright/helpers/adminArtifacts.ts
  - test/example/priv/playwright/tests/admin-generated.spec.ts
autonomous: true
requirements:
  - VFY-01
  - VFY-02
  - VFY-04
must_haves:
  truths:
    - "Playwright runs one main admin behavior lane instead of replaying the same admin journeys across a broad viewport/theme matrix."
    - "Generated-host browser smoke stays shallow and deterministic, proving shipped admin seams without duplicating the example app's deep operator journeys."
    - "The harness can emit curated screenshot attachments for reviewer-visible checkpoints while keeping failure diagnostics aligned with the existing retry/trace policy."
  artifacts:
    - path: "test/example/priv/playwright/playwright.config.ts"
      provides: "Partitioned Playwright projects and test matching for admin behavior, generated-host parity, and checkpoint capture"
    - path: "test/example/priv/playwright/helpers/adminArtifacts.ts"
      provides: "Shared helper for predictable screenshot names and report attachments on passing admin checkpoints"
    - path: "test/example/priv/playwright/tests/admin-generated.spec.ts"
      provides: "Generated-host browser parity smoke limited to shell, scope, navigation, mobile, and denial semantics"
  key_links:
    - from: "test/example/priv/playwright/playwright.config.ts"
      to: "test/example/priv/playwright/tests/admin-generated.spec.ts"
      via: "project and testMatch partitioning that keeps generated-host admin smoke narrow"
      pattern: "testMatch|admin-generated|mobile|dark"
    - from: "test/example/priv/playwright/tests/admin-generated.spec.ts"
      to: "test/example/priv/playwright/helpers/adminArtifacts.ts"
      via: "curated screenshot attachment and stable artifact naming"
      pattern: "attach|screenshot|artifact"
nyquist_compliant: true
---

<objective>
Partition the admin Playwright harness so Phase 31 keeps one canonical browser-truth lane, one curated checkpoint lane, and one shallow generated-host parity lane.

Purpose: Per D-01 through D-06 and D-26 through D-30, the browser harness must prove operator UX where real browser semantics matter without turning Phase 31 into a giant viewport/theme matrix or duplicating negative-case coverage that belongs in ExUnit.
Output: Updated Playwright project partitioning, a shared admin artifact helper, and a generated-host browser smoke spec that stays parity-focused.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-automation-first-verification/31-CONTEXT.md
@.planning/phases/31-automation-first-verification/31-RESEARCH.md
@.planning/phases/31-automation-first-verification/31-PATTERNS.md
@.planning/phases/30-audit-exploration-and-export/30-VERIFICATION.md
@/Users/jon/projects/sigra/test/example/priv/playwright/playwright.config.ts
@/Users/jon/projects/sigra/test/example/priv/playwright/tests/admin-generated.spec.ts
@/Users/jon/projects/sigra/scripts/ci/admin-acceptance-smoke.sh

<interfaces>
From `test/example/priv/playwright/playwright.config.ts`:

```ts
export default defineConfig({
  reporter: [['list'], ['html', { open: 'never' }]],
  use: {
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'mobile', use: { ...devices['iPhone 13'] } },
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
});
```

From `scripts/ci/admin-acceptance-smoke.sh`:

```bash
PLAYWRIGHT_SPEC="tests/admin-generated.spec.ts"
TEST_TARGET="all"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Partition Playwright projects around admin behavior truth and checkpoint capture</name>
  <files>test/example/priv/playwright/playwright.config.ts</files>
  <action>Update the Playwright config per D-01 through D-05, D-19 through D-25, and D-26 through D-30 so the admin suite stops acting like one shared matrix. Keep the main behavior suite on one desktop `chromium` lane, add a compact checkpoint lane for `chromium`, `mobile`, and `dark-chromium`, and scope generated-host smoke so it proves parity without replaying the example app's journeys. Preserve `trace: 'on-first-retry'`, add global `screenshot: 'only-on-failure'`, and make selective retained video explicit with `video: 'retain-on-failure'` only for the admin checkpoint/generated-host failure-oriented projects where reviewer or debugging value is real. Keep retries/workers aligned with the current low-flake LiveView harness instead of widening them.</action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test --list tests/admin-user-operations.spec.ts tests/impersonation.spec.ts tests/admin-audit.spec.ts tests/admin-generated.spec.ts</automated>
  </verify>
  <done>The config clearly separates behavioral truth from checkpoint capture, keeps the generated-host spec shallow, and exposes a dedicated dark-mode project without broadening the main admin workflow matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared admin artifact helpers and keep generated-host smoke parity-focused</name>
  <files>test/example/priv/playwright/helpers/adminArtifacts.ts, test/example/priv/playwright/tests/admin-generated.spec.ts</files>
  <action>Create a small shared helper for explicit screenshot attachment and predictable output naming, then update `admin-generated.spec.ts` per D-03, D-05, D-17, D-20, D-21, D-24, and D-25 so the generated-host suite covers only shell render, visible scope labels, admin navigation, allowed organization access, denied global access, and not-found out-of-scope organization access. Use the helper for curated desktop/mobile reviewer artifacts, and make failure-oriented retained video explicit for this generated-host suite where useful by relying on the selective `retain-on-failure` project policy from Task 1. Do not move broader negative-case matrices or example-app workflows into this generated-host spec.</action>
  <verify>
    <automated>GITHUB_WORKSPACE=$(pwd) scripts/ci/admin-acceptance-smoke.sh --test all</automated>
  </verify>
  <done>The generated-host browser suite remains deterministic and parity-focused, and passing runs can attach curated screenshots without changing the suite into a second example-app workflow runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright project selection -> admin browser coverage | A mis-scoped project matrix can create false confidence by either skipping required checkpoints or hiding regressions inside redundant reruns |
| generated-host browser smoke -> shipped admin templates | The generated app proves installer/template/runtime parity for shipped seams and must not drift into an unbounded duplicate suite |
| curated attachments -> CI review artifacts | Reviewer-facing screenshots must be explicit and predictable so green runs remain inspectable without uploading raw output indiscriminately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | T | `playwright.config.ts` | mitigate | Per D-01 through D-05 and D-26 through D-29, partition projects with explicit `testMatch` and a dedicated dark-mode lane so only the checkpoint spec expands across viewport/theme combinations. |
| T-31-02 | D | generated-host Playwright smoke | mitigate | Per D-03, D-05, and D-17, keep `admin-generated.spec.ts` narrow and deterministic so runtime parity checks remain reliable instead of ballooning into a flaky second suite. |
| T-31-03 | I | reviewer artifact helper | mitigate | Per D-20, D-24, and D-25, attach only named screenshots for passing checkpoints while leaving richer traces/videos to failure paths. |
</threat_model>

<verification>
List the partitioned specs through Playwright to prove the config routes them into the intended projects, then run the generated-host acceptance smoke so the generated-admin spec still passes end to end with the new harness shape.
</verification>

<success_criteria>
- The admin Playwright harness has a clear boundary between behavior truth, checkpoint capture, and generated-host parity.
- Generated-host browser smoke stays narrow and deterministic.
- The harness supports curated admin screenshots without broadening traces or videos for every passing run.
</success_criteria>

<output>
After completion, create `.planning/phases/31-automation-first-verification/31-automation-first-verification-01-SUMMARY.md`
</output>
