---
phase: "151"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .tool-versions
  - mix.lock
autonomous: true
requirements: [ECO-01, ECO-02, ECO-03]
requirements_addressed: [ECO-01, ECO-02, ECO-03]
must_haves:
  truths:
    - "CI pipeline uses latest minor versions of Elixir and OTP (Erlang 28.5) natively via .tool-versions without deprecation warnings."
    - "Hex dependencies are updated to their latest secure and compatible versions using existing constraints in mix.exs."
    - "The test suite successfully passes after the updates, demonstrating framework alignment."
  tasks:
    - "Update .tool-versions Erlang to 28.5."
    - "Run mix local.hex --force and mix local.rebar --force."
    - "Run mix deps.update --all to batch update hex packages."
    - "Run mix compile --warnings-as-errors to ensure zero deprecation warnings."
    - "Run mix test to verify system integrity."
  artifacts:
    - path: ".tool-versions"
      provides: "Erlang/OTP toolchain versions"
      contains: "erlang 28.5"
    - path: "mix.lock"
      provides: "Updated Hex dependencies"
  key_links:
    - from: ".tool-versions"
      to: ".github/workflows/ci.yml"
      via: "erlef/setup-beam version-file strict parsing"
---

<objective>
Update the project's Erlang/OTP environment to the latest 28.5 patch release and batch update all Hex dependencies within existing constraints to ensure ecosystem alignment and supply-chain security.

Purpose: Maintain framework alignment and resolve potential vulnerabilities by keeping dependencies updated, validating against the latest toolchain via `.tool-versions`, and enforcing zero deprecation warnings.
Output: Updated `.tool-versions` and `mix.lock` reflecting the newly resolved ecosystem versions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-ecosystem-sync-hex-dependency-management/151-CONTEXT.md
@.planning/phases/151-ecosystem-sync-hex-dependency-management/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolchain Alignment (OTP/Elixir)</name>
  <files>.tool-versions</files>
  <action>
    Modify `.tool-versions` to enforce the latest target Erlang/OTP versions.
    Per D-02, CI relies entirely on `.tool-versions` rather than a build matrix.

    - Set Erlang version to `erlang 28.5`.
    - Ensure Elixir version is set to `elixir 1.19.5-otp-28`.
    - Do NOT edit `.github/workflows/ci.yml` as it already dynamically references `.tool-versions`.
  </action>
  <verify>
    <automated>grep -q "erlang 28.5" .tool-versions</automated>
  </verify>
  <done>`.tool-versions` reflects Erlang 28.5.</done>
</task>

<task type="auto">
  <name>Task 2: Hex Dependency Batch Update & Compilation</name>
  <files>mix.lock</files>
  <action>
    Update all hex dependencies and verify zero deprecation warnings during compilation.

    1. Ensure local toolchain is up-to-date: run `mix local.hex --force` and `mix local.rebar --force`.
    2. Per D-01 and D-04: Run `mix deps.update --all` to resolve dependencies within existing `mix.exs` constraints. Do NOT modify `mix.exs` constraints or introduce dependabot config for hex.
    3. Per D-03: Run `mix compile --warnings-as-errors`. If there are deprecation warnings, fix them via code changes (no compiler suppression).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>All Hex dependencies are updated in `mix.lock` and project compiles cleanly with zero warnings.</done>
</task>

<task type="auto">
  <name>Task 3: CI Framework Alignment Verification</name>
  <files>mix.lock</files>
  <action>
    Run the test suite to verify the updated dependencies and toolchain don't introduce regressions.

    Run `mix test` to confirm framework alignment and CI reliability before the CI pipeline takes over in GitHub Actions.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>The test suite passes 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build to Execution | Third-party hex package code executed in CI and production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Tampering | Hex Registry | mitigate | Use standard `mix deps.update --all` which leverages mix.lock hex signature verifications. |
| T-151-SC | Tampering | mix deps installs | mitigate | slopcheck omitted per assumptions log as only existing dependencies are being bumped within existing constraints. |
</threat_model>

<verification>
1. `.tool-versions` dictates Erlang 28.5.
2. `mix compile --warnings-as-errors` passes cleanly (zero deprecation warnings).
3. `mix.lock` contains the updated hashes from `mix deps.update --all`.
4. `mix test` is completely green.
</verification>

<success_criteria>
The project toolchain is cleanly updated to Erlang 28.5 and Hex dependencies are pushed to their maximum compatible bounds inside `mix.exs` constraints without causing CI regressions or deprecation warnings.
</success_criteria>

<output>
Create `.planning/phases/151-ecosystem-sync-hex-dependency-management/151-01-SUMMARY.md` when done
</output>
