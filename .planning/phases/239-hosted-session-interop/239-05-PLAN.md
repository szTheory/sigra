---
phase: 239-hosted-session-interop
plan: 05
type: execute
wave: 5
depends_on: [239-04]
files_modified:
  - test/example/lib/example/accounts/crosswake_session_adapter.ex
  - test/example/test/example/accounts/crosswake_session_adapter_test.exs
  - guides/recipes/b2c-alpha.md
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-05/D-06, approved hosted-return evidence without a fresh valid current session cannot construct authority or admit a Crosswake route/replay."
    - "Forbidden session/subject/org/authority/token fields are rejected by the released AuthReturn constructor and never forwarded by the host adapter."
    - "The B2C recipe names the exact released range and preserves org_id: nil, opaque-ref, fresh-resolution, and evidence-only semantics."
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; secret-field rejection and published personal-scope instructions are the explicit verification surface."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; evidence-only/no-session denial and authority-smuggling rejection are the explicit verification surface."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT represent a personal account by manufacturing organization authority or accepting a blank organization value."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-01; no fallback descriptor is invented."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT carry a raw session credential, stored token digest, provider payload, or OAuth credential across the companion boundary."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-02; no fallback descriptor is invented."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or hosted-return evidence select a session, replace fresh host resolution, or independently admit a route."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-05; no fallback descriptor is invented."
  artifacts:
    - path: test/example/lib/example/accounts/crosswake_session_adapter.ex
      provides: "reference-only return envelope composition with mandatory fresh host authority"
    - path: test/example/test/example/accounts/crosswake_session_adapter_test.exs
      provides: "return evidence and authority-smuggling matrix"
    - path: guides/recipes/b2c-alpha.md
      provides: "consumer-facing released interop contract"
  key_links:
    - from: test/example/lib/example/accounts/crosswake_session_adapter.ex
      to: Crosswake.Companions.Sigra.AuthReturn
      via: "public envelope constructor rejects forbidden authority/credential fields"
      pattern: "AuthReturn.*new_envelope"
    - from: guides/recipes/b2c-alpha.md
      to: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json
      via: "documented package requirement matches verified release receipt"
---

<objective>
Close the callback/hosted-return authority-smuggling boundary and publish exact consumer instructions.

Purpose: Evidence may explain or navigate a hosted attempt, but only the freshly validated current SIGRA session can supply Crosswake authority.
Output: Evidence-aware adapter path, deterministic rejection/no-session proof, and aligned B2C recipe.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-RESEARCH.md
@.planning/phases/239-hosted-session-interop/239-04-SUMMARY.md
@guides/recipes/b2c-alpha.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep hosted-return data evidence-only under replay</name>
  <files>test/example/lib/example/accounts/crosswake_session_adapter.ex, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>
    - Released `Crosswake.Companions.Sigra.AuthReturn` source/tests from the pinned package, especially `new_envelope/1` forbidden-field validation.
    - `test/example/lib/example/accounts/crosswake_session_adapter.ex` after Plan 04 for fresh resolution and binding order.
    - `.planning/phases/239-hosted-session-interop/239-CONTEXT.md` D-05/D-06.
  </read_first>
  <behavior>
    - Test 1: a valid evidence/navigation envelope plus no current session denies before evaluator invocation.
    - Test 2: the same valid envelope plus expired/revoked/switched state remains denied by the host gates.
    - Test 3: envelope input containing session_ref, subject_ref, org_id, authority/access-grant flags, raw token, stored digest, provider payload, or OAuth credential is rejected.
    - Test 4: valid evidence can be returned as evidence but cannot change the expected binding, current lane/context, evaluator options, or route result.
  </behavior>
  <action>Accept hosted-return input only through the released public AuthReturn envelope constructor and keep its approved evidence/navigation result separate from authority inputs. Preserve the evaluation order: parse/validate evidence, freshly resolve current session/user, validate currentness/time, compare server-owned binding, construct facts, then evaluate. Evidence-construction failure or missing host authority denies without evaluator invocation. Return approved evidence only in an evidence field; never merge it into lane/context/options or use it to choose a session. Add the full forbidden-field matrix using public constructors, not internal struct fabrication.</action>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_session_adapter_test.exs --only crosswake_return_evidence</automated>
  </verify>
  <acceptance_criteria>Valid evidence alone, invalid evidence, and valid evidence paired with every invalid host state all deny safely; forbidden authority/credential fields are rejected; only fresh valid bound host state can reach the evaluator.</acceptance_criteria>
  <done>Callback and hosted-return input is provably evidence/navigation only.</done>
</task>

<task type="auto">
  <name>Task 2: Align the B2C interop recipe with the released contract</name>
  <files>guides/recipes/b2c-alpha.md, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>
    - `guides/recipes/b2c-alpha.md:38-48` for the existing boundary wording.
    - `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json` for the exact compatible Mix requirement.
    - The complete adapter test for executable behavior names the recipe should reference.
  </read_first>
  <action>Update only the Crosswake hosted-session section to name the verified companion requirement and host adapter contract. State that personal scope is `org_id: nil`; opaque refs and version are server-owned; each projection/replay repeats canonical database session/user resolution; missing/revoked/expired/mismatched/switched state denies; return evidence cannot choose or grant a session. Link the focused ExUnit command. Preserve the wider Phase 240 provider/device launch boundary and do not add admin, organization setup, passkeys, MFA, native/deep-link authority, or manual UAT.</action>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_session_adapter_test.exs</automated>
  </verify>
  <acceptance_criteria>The recipe's dependency range matches the release receipt and every authority claim is backed by the complete deterministic adapter suite.</acceptance_criteria>
  <done>Consumers receive an exact, no-secrets, fail-closed personal-account integration contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth/callback return → host adapter | Attacker-controlled evidence can attempt to smuggle session or authority claims. |
| approved evidence → Crosswake evaluation | Even structurally valid evidence must remain non-authoritative. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-15 | Tampering | callback authority smuggling | high | mitigate | Public AuthReturn constructor rejects session/subject/org/authority/access/token fields before host evaluation. |
| T-239-16 | Elevation of Privilege | evidence-only replay | high | mitigate | Mandatory fresh session/user resolution and binding checks remain independent of envelope validity. |
| T-239-17 | Information Disclosure | return envelope/result | high | mitigate | Allowlisted evidence fields only; forbidden credential/provider material covered by constructor and integration tests. |
| T-239-18 | Repudiation | published integration contract | low | accept | Recipe is backed by named deterministic tests and immutable release receipt; no additional signed audit record is needed at ASVS L1. |
</threat_model>

<verification>
Run the focused evidence tag and then the complete adapter suite. Compare the recipe requirement with the JSON receipt mechanically during Plan 06.
</verification>

<success_criteria>
- Return input is never authority.
- Every named smuggling field is rejected.
- Consumer documentation matches the exact tested release and host behavior.
</success_criteria>

## Artifacts this phase produces

- Complete return-evidence boundary tests.
- Released-package instructions in the B2C alpha recipe.

<output>
Create `.planning/phases/239-hosted-session-interop/239-05-SUMMARY.md` when done.
</output>

