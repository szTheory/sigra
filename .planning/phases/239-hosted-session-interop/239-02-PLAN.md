---
phase: 239-hosted-session-interop
plan: 02
type: execute
wave: 2
depends_on: [239-01]
files_modified:
  - .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json
  - test/example/mix.exs
  - test/example/mix.lock
  - test/example/lib/example/accounts/crosswake_session_adapter.ex
  - test/example/test/example/accounts/crosswake_session_adapter_test.exs
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-01/D-02, a current personal SIGRA session reaches the released Crosswake evaluator with org_id: nil, opaque references, server-owned timestamps/version, and no credential material."
    - "Per D-03, the tracer resolves the current database session and user from the raw cookie for the evaluation call rather than trusting a cached scope or serialized lane."
    - "The exact Crosswake release provenance is durable and the dependency is confined to the example/proof host, not SIGRA's root runtime."
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the tracer's personal projection and negative secret assertions are the explicit verification surface."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; fresh lookup and evidence-free happy-path evaluation establish the backstop for later denial expansions."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT represent a personal account by manufacturing organization authority or accepting a blank organization value."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-01; no fallback check descriptor is fabricated."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT carry a raw session credential, stored token digest, provider payload, or OAuth credential across the companion boundary."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-02; no fallback check descriptor is fabricated."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or hosted-return evidence select a session, replace fresh host resolution, or independently admit a route."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-05; no fallback check descriptor is fabricated."
  artifacts:
    - path: test/example/lib/example/accounts/crosswake_session_adapter.ex
      provides: "host-owned fresh session projection/evaluation boundary"
    - path: test/example/test/example/accounts/crosswake_session_adapter_test.exs
      provides: "database-backed personal-session tracer proof"
    - path: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json
      provides: "immutable companion release provenance"
  key_links:
    - from: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
      to: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json
      via: "Plan 02 derives and validates the normalized receipt directly from the Wave 0 proof before dependency resolution"
    - from: test/example/lib/example/accounts/crosswake_session_adapter.ex
      to: test/example/lib/example/accounts.ex
      via: "Accounts.get_user_and_session_by_token/1 on every projection/evaluation"
      pattern: "get_user_and_session_by_token"
    - from: test/example/lib/example/accounts/crosswake_session_adapter.ex
      to: Crosswake.Companions.Sigra.Contracts
      via: "fact-only SessionAuthorityLane and AuthContext constructors"
      pattern: "new_session_authority_lane|new_auth_context"
---

<objective>
Prove one production-quality personal-session path end to end from current SIGRA storage through the released Crosswake evaluator.

Purpose: This tracer resolves the hardest architectural constraint early: personal `org_id: nil` must survive the real released contract without inventing authority or leaking the cookie/token hash.
Output: pinned example dependency, immutable release receipt, host adapter, and one database-backed happy-path test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-RESEARCH.md
@.planning/phases/239-hosted-session-interop/239-PATTERNS.md
@.planning/phases/239-hosted-session-interop/239-01-SUMMARY.md
@test/example/lib/example/accounts.ex
@lib/sigra/session.ex
@test/example/mix.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Project one freshly resolved personal session through Crosswake</name>
  <precondition>The Wave 0 proof JSON exists and contains the schema-valid published Crosswake coordinates plus the exact four passing command records required by Plan 01.</precondition>
  <reversibility rating="costly">Opaque reference derivation and the adapter call contract may be consumed by hosted continuations, so changing them later requires coordinated host/continuation updates.</reversibility>
  <files>.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json, test/example/mix.exs, test/example/mix.lock, test/example/lib/example/accounts/crosswake_session_adapter.ex, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>
    - `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json` as the sole authority for release coordinates and required Crosswake test outcomes; fail closed if its exact-array invariant or any receipt field is missing.
    - `.planning/phases/239-hosted-session-interop/239-01-SUMMARY.md` for execution context only; do not source or override release coordinates from the summary.
    - `test/example/mix.exs` for proof-host dependency scope; do not add Crosswake to root `mix.exs`.
    - `test/example/lib/example/accounts.ex:289-330` for canonical raw-token decode/hash/session/user lookup.
    - `lib/sigra/session.ex:23-86` for raw-versus-hashed token semantics.
    - `test/example/test/support/fixtures/auth_fixtures.ex:42-72` and `test/example/test/example/accounts/session_invalidation_test.exs:19-79` for real Ecto session fixtures.
  </read_first>
  <behavior>
    - RED 1: a real personal session and its raw cookie token produce an allowed Crosswake result with `org_id == nil` after fresh `user_sessions` and user resolution.
    - RED 2: `session_ref` and `subject_ref` are stable opaque keyed references and differ from session/user IDs, raw token, and stored digest.
    - RED 3: the lane/context/result contain no raw token, stored digest, provider payload, OAuth credential, or `active_organization_id` authority.
    - RED 4: a nonblank organization value remains accepted by the released contract and a blank value is rejected, without projecting an organization in the B2C adapter.
  </behavior>
  <action>
    First validate `239-CROSSWAKE-RELEASE-PROOF.json` using the same exact-array invariant as Plans 00/01: exactly four command records in the declared order, then numeric zero exit_status and exact `passed` outcome on every unfiltered record. Derive `239-CROSSWAKE-RELEASE.json` directly from that proof's repository, package, version, requirement, git_tag, full git_sha, hex_checksum, published_at, and verified_at fields; copy no coordinate or outcome from `239-01-SUMMARY.md`. Mechanically compare every normalized receipt coordinate back to the proof and confirm the exact four required passing outcomes before changing the dependency or invoking `mix deps.get`. Write the proof's exact compatible requirement into `test/example/mix.exs`, verify that dependency declaration against the proof, and only then resolve it and update `test/example/mix.lock` normally.

    Next write the focused integration test and prove it fails because the adapter does not exist. Add `Example.Accounts.CrosswakeSessionAdapter` as the host-owned boundary. Its public happy-path call accepts the raw cookie token, an injected/fixed `as_of`, the route requirement, and server-owned expected binding; it calls `Accounts.get_user_and_session_by_token/1` inside that call, validates the current session, then constructs the released lane/context and invokes the pure evaluator.

    Derive domain-separated opaque session/subject references with keyed host HMAC over the server row IDs; do not serialize or expose those IDs. Derive the non-negative `session_version` from the server-owned session-row `inserted_at` at microsecond precision, keeping the derivation private and stable for the row lifecycle. Project personal `org_id: nil`, active state, authentication/assurance facts, `authenticated_at`, `last_seen_at`, idle/absolute expiries, version, and `as_of`. Do not hydrate `active_organization_id`. Return a small allow/deny result whose details cannot reveal secrets. Run the RED test again to GREEN and retain the implementation as the phase skeleton.
  </action>
  <verify>
    <automated>jq -e --slurpfile proof .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json '. as $receipt | $proof[0] as $proof | ($proof.commands | type == "array") and ($proof.commands | length == 4) and ([$proof.commands[].command] == ["mix format --check-formatted packages/crosswake_sigra", "mix test packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs", "mix test packages/crosswake_sigra/test/crosswake/proof/phase57_auth_return_boundaries_test.exs", "cd packages/crosswake_sigra &amp;&amp; mix test"]) and all($proof.commands[]; (.exit_status | type) == "number" and .exit_status == 0 and .outcome == "passed") and ([.repository, .package, .version, .requirement, .git_tag, .git_sha, .hex_checksum, .published_at, .verified_at] == [$proof.repository, $proof.package, $proof.version, $proof.requirement, $proof.git_tag, $proof.git_sha, $proof.hex_checksum, $proof.published_at, $proof.verified_at])' .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json &amp;&amp; requirement=$(jq -er '.requirement' .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json) &amp;&amp; rg -F "{:crosswake_sigra, \"$requirement\"}" test/example/mix.exs &amp;&amp; cd test/example &amp;&amp; mix deps.get &amp;&amp; mix test test/example/accounts/crosswake_session_adapter_test.exs --only crosswake_tracer</automated>
  </verify>
  <acceptance_criteria>The focused tagged test proves the real database-backed personal path reaches Crosswake with `org_id: nil`; organization compatibility and blank rejection pass; assertions prove no raw token, digest, row ID, provider/OAuth field, or organization hydration appears in projection/result.</acceptance_criteria>
  <done>One current personal account can be evaluated end to end against the exact released companion while the host remains the sole storage authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cookie → host adapter | Untrusted raw credential enters server-owned resolution. |
| SIGRA database/user → Crosswake facts | Authoritative rows become a restricted fact projection. |
| host secret/IDs → opaque references | Identifiers must not be reversible or exposed. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-03 | Spoofing | raw cookie/session resolution | high | mitigate | Call canonical server-side lookup inside every public adapter operation and reject lookup/user failure before contract construction. |
| T-239-04 | Elevation of Privilege | personal organization scope | high | mitigate | Project literal `org_id: nil`, never hydrate active organization, and retain companion org/blank compatibility tests. |
| T-239-05 | Information Disclosure | lane/context/result | high | mitigate | Domain-separated keyed opaque refs plus negative assertions for raw token, digest, IDs, provider payloads, and OAuth material. |
| T-239-06 | Tampering | session version/as_of | medium | mitigate | Derive version/timestamps from the freshly fetched server row and injected server clock, never callback/client fields. |
</threat_model>

<verification>
Run the tagged tracer from the example Mix project in under 60 seconds. Dependency resolution must match the approved receipt and must not alter root SIGRA dependencies.
</verification>

<success_criteria>
- A real current personal session allows through the released in-process contract/evaluator.
- `org_id: nil` is semantic personal scope, not a sentinel.
- All Crosswake-facing data is fact-only and secret-free.
</success_criteria>

## Artifacts this phase produces

- Immutable local receipt for the released Crosswake successor.
- Example-host Crosswake adapter skeleton.
- Database-backed tracer test for XW-01 and the fresh-resolution part of XW-02.

<output>
Create `.planning/phases/239-hosted-session-interop/239-02-SUMMARY.md` when done.
</output>
