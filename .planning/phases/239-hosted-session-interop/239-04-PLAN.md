---
phase: 239-hosted-session-interop
plan: 04
type: execute
wave: 4
depends_on: [239-03]
files_modified:
  - test/example/lib/example/accounts/crosswake_session_adapter.ex
  - test/example/test/example/accounts/crosswake_session_adapter_test.exs
autonomous: true
requirements: [XW-02]
must_haves:
  truths:
    - "Per D-04/D-06, stale version, subject mismatch, session mismatch, and account switch all deny before evaluator invocation."
    - "Expected bindings come from a server-owned continuation record/input, never callback parameters or a replacement current account."
  assumptions:
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the exhaustive binding matrix and evaluator non-invocation assertions are the explicit verification surface."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT represent a personal account by manufacturing organization authority or accepting a blank organization value."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-01; no fallback descriptor is invented."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT carry a raw session credential, stored token digest, provider payload, or OAuth credential across the companion boundary."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-02; no fallback descriptor is invented."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or hosted-return evidence select a session, replace fresh host resolution, or independently admit a route."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-05; no fallback descriptor is invented."
  artifacts:
    - path: test/example/lib/example/accounts/crosswake_session_adapter.ex
      provides: "server-owned binding and account-switch gate"
    - path: test/example/test/example/accounts/crosswake_session_adapter_test.exs
      provides: "stale/switch mismatch matrix"
  key_links:
    - from: "server-owned expected binding"
      to: test/example/lib/example/accounts/crosswake_session_adapter.ex
      via: "constant-time opaque ref comparison plus integer version equality after fresh resolution"
      pattern: "expected_session_ref|expected_subject_ref|expected_session_version"
---

<objective>
Bind replay to the original server-owned session, subject, and version and reject account switching explicitly at the host.

Purpose: Released Crosswake cannot observe the browser's newly selected account; the host must prevent valid account B from replacing the continuation authority established for account A.
Output: Ordered binding gate and deterministic mismatch/switch proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-RESEARCH.md
@.planning/phases/239-hosted-session-interop/239-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce session, subject, and version equality before Crosswake</name>
  <reversibility rating="costly">Changing the persisted/continued binding tuple later requires coordinated updates wherever hosted continuations retain the expected values.</reversibility>
  <files>test/example/lib/example/accounts/crosswake_session_adapter.ex, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>
    - `test/example/lib/example/accounts/crosswake_session_adapter.ex` after Plan 03 for current lookup/order.
    - `.planning/phases/239-hosted-session-interop/239-RESEARCH.md` assumptions A2 and Pattern 3.
    - Released Crosswake evaluator types via the pinned dependency for exact expected-version option names.
  </read_first>
  <behavior>
    - Test 1: expected opaque session mismatch denies with zero evaluator calls.
    - Test 2: expected opaque subject mismatch denies with zero evaluator calls.
    - Test 3: expected server-owned version mismatch denies with zero evaluator calls; exact match proceeds.
    - Test 4: malformed or incomplete binding input denies without leaking which supplied field differed.
  </behavior>
  <action>Represent the trusted continuation binding as a host adapter struct/map containing only the expected opaque session ref, opaque subject ref, and non-negative session version. After fresh current-row validation, derive the current tuple and compare it before lane/context construction. Use constant-time comparison for opaque binary refs and exact integer equality for the version. Collapse all mismatches into one safe binding-denial class; never echo expected or actual values. Pass the already-validated current version to Crosswake rather than accepting a callback/client integer as authority.</action>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_session_adapter_test.exs --only crosswake_binding</automated>
  </verify>
  <acceptance_criteria>Session, subject, version, and malformed binding cases all deny before evaluator invocation; exact server-owned binding still reaches the tracer allow path.</acceptance_criteria>
  <done>Replay is cryptographically/semantically tied to the original server-owned authority tuple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove an account switch cannot substitute new authority</name>
  <files>test/example/lib/example/accounts/crosswake_session_adapter.ex, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>
    - `test/example/test/support/fixtures/auth_fixtures.ex` for independent user/session fixtures.
    - `test/example/lib/example/accounts/crosswake_session_adapter.ex` after Task 1.
    - `.planning/phases/239-hosted-session-interop/239-CONTEXT.md` D-04 account-switch lock.
  </read_first>
  <behavior>
    - Test 1: binding for user/session A plus current cookie for valid user/session B denies before evaluator invocation.
    - Test 2: switching only the session within the same user also denies.
    - Test 3: the adapter never rewrites expected bindings to B and never returns B's refs in denial details.
  </behavior>
  <action>Create two real users and multiple real session rows. Exercise replay with A's server-owned binding and B's current valid cookie, then with a second session belonging to A. Preserve the ordered lookup-then-binding-check boundary: confirming B is a valid current account does not authorize it for A's continuation. Add no organization, browser UI, or evaluator-only substitute test.</action>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_session_adapter_test.exs --only crosswake_account_switch</automated>
  </verify>
  <acceptance_criteria>Both cross-account and same-account/session-switch tests deny with evaluator calls zero and without revealing either account/session identifier.</acceptance_criteria>
  <done>Account/session switching is explicitly fail-closed at the only layer that can observe the current cookie and database owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server continuation binding → fresh current session | Original authority is compared with the current cookie-selected account. |
| callback/client values → binding API | Untrusted fields must not become expected authority. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-11 | Spoofing | account/session switch | high | mitigate | Compare A's server-owned opaque tuple against freshly resolved current tuple and deny before evaluator invocation. |
| T-239-12 | Tampering | expected binding/version | high | mitigate | Accept only trusted host binding input; derive actual refs/version from current server rows. |
| T-239-13 | Information Disclosure | mismatch diagnostics | medium | mitigate | One categorical denial and constant-time ref comparisons; never return expected/actual values. |
| T-239-14 | Elevation of Privilege | evaluator substitution | high | mitigate | Mox/injected seam proves evaluator receives no call for all switch and binding failures. |
</threat_model>

<verification>
Run both focused tags and the complete adapter test. The test must use real Ecto rows and fixed inputs, with no browser or time delay.
</verification>

<success_criteria>
- Expected session, subject, and version are host-owned and mandatory.
- A valid replacement account/session remains unauthorized for the original continuation.
- Crosswake is never asked to decide a host binding failure.
</success_criteria>

## Artifacts this phase produces

- Trusted continuation binding contract in the adapter.
- Deterministic stale-version, mismatch, and account-switch matrix.

<output>
Create `.planning/phases/239-hosted-session-interop/239-04-SUMMARY.md` when done.
</output>

