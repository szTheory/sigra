---
phase: 239-hosted-session-interop
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
autonomous: false
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "The public Crosswake successor is tied to one immutable tag, full commit SHA, Hex version/checksum, and compatible Mix requirement before SIGRA consumes it."
    - "The required contracts, AuthReturn boundary, and complete companion test commands execute successfully from a clean checkout of that exact SHA."
    - "Machine-readable command outcomes are generated from execution, not copied from a maintainer-supplied receipt."
  artifacts:
    - path: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
      provides: "verified Crosswake release coordinates and exact-command outcomes"
  key_links:
    - from: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
      to: https://github.com/szTheory/crosswake
      via: "git_tag and git_sha identify the clean checkout used for every recorded command"
    - from: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
      to: https://hex.pm/api/packages/crosswake_sigra
      via: "package, version, checksum, and publication metadata are checked against the public registry"
---

<objective>
Produce deterministic release proof for the external Crosswake successor before the existing Phase 239 release gate.

Purpose: Close the Nyquist gap by rerunning the D-01/D-02/D-05 companion contract and AuthReturn tests at the immutable published SHA and recording machine-derived outcomes.
Output: `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-RESEARCH.md
@.planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Authorize or complete the external Crosswake publication</name>
  <what-built>The Crosswake-owned D-01/D-02/D-05 changes and tests described by the handoff are ready for an independently versioned release.</what-built>
  <instructions>Complete any maintainer-only release authorization, signing, or Hex publication step that cannot be performed through the available authenticated CLI. Provide the public version and tag so the executor can locate the candidate; these values remain untrusted until Task 2 reproduces the proof.</instructions>
  <action>Complete any maintainer-only release authorization, signing, or Hex publication step that cannot be performed through the available authenticated CLI. Supply the public package version and immutable release tag only as discovery inputs for Task 2; they are not accepted as proof of test success or source identity.</action>
  <verify>
    <automated>curl --fail --silent --show-error https://hex.pm/api/packages/crosswake_sigra | jq -e '.name == "crosswake_sigra" and (.latest_version | type == "string")' &gt;/dev/null &amp;&amp; git ls-remote --tags https://github.com/szTheory/crosswake.git &gt;/dev/null</automated>
    <human-check>Confirm only that any maintainer-only authorization/publication action completed and the release is publicly visible; Task 2 independently verifies all coordinates and outcomes.</human-check>
  </verify>
  <verification>The candidate successor version and tag are publicly visible through Hex and GitHub; no claim about their source identity or test status is accepted yet.</verification>
  <done>The candidate successor is public and can be fetched without private maintainer state.</done>
  <resume-signal>Type `published` with the public version and tag, or describe the publication blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reproduce the Crosswake release proof at the immutable SHA</name>
  <files>.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json</files>
  <read_first>
    - `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md` for the exact contract, commands, and release fields.
    - `.planning/phases/239-hosted-session-interop/239-RESEARCH.md` for the pinned predecessor and Package Legitimacy Audit.
    - `AGENTS.md` for automation-first, retry-once, and durable-diagnostic rules.
  </read_first>
  <action>Use a temporary clean checkout of `https://github.com/szTheory/crosswake.git` at the public successor tag, resolve the peeled tag to a full commit SHA, and compare package/version/checksum/publication metadata against the Hex API. Require a published successor distinct from the researched predecessor `0.1.1`. From that checkout run exactly: `mix format --check-formatted packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs packages/crosswake_sigra/test/crosswake/proof/phase57_auth_return_boundaries_test.exs`; `mix test packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs`; `mix test packages/crosswake_sigra/test/crosswake/proof/phase57_auth_return_boundaries_test.exs`; and `cd packages/crosswake_sigra &amp;&amp; mix test`. The formatter command is intentionally scoped to the two released D-01/D-02/D-05 contract files: the published package contains pre-existing unrelated formatting drift, which must not obscure proof of this release-critical surface. Fail if the checkout is dirty, the tag/SHA/Hex metadata disagree, a required test file is absent, or any command is nonzero. After all checks pass, atomically write schema `sigra.phase239.crosswake-release-proof.v1` with repository, package, version, compatible requirement, git_tag, full git_sha, hex_checksum, published_at, verified_at, and a `commands` array containing exactly four entries in the documented order, with no missing, extra, duplicate, or reordered records. Every unfiltered entry must contain its exact command string, numeric exit_status equal to zero, and outcome exactly `passed`. Derive statuses from the executed processes; do not transcribe maintainer-reported outcomes. Preserve durable diagnostics and do not create a pass-shaped JSON file on failure.</action>
  <verify>
    <automated>jq -e '.schema == "sigra.phase239.crosswake-release-proof.v1" and .repository == "https://github.com/szTheory/crosswake.git" and .package == "crosswake_sigra" and (.version | type == "string" and length &gt; 0) and .version != "0.1.1" and (.requirement | type == "string" and length &gt; 0) and (.git_tag | type == "string" and length &gt; 0) and (.git_sha | test("^[0-9a-f]{40}$")) and (.hex_checksum | type == "string" and length &gt; 0) and (.published_at | type == "string" and length &gt; 0) and (.verified_at | type == "string" and length &gt; 0) and (.commands | type == "array") and (.commands | length == 4) and ([.commands[].command] == ["mix format --check-formatted packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs packages/crosswake_sigra/test/crosswake/proof/phase57_auth_return_boundaries_test.exs", "mix test packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs", "mix test packages/crosswake_sigra/test/crosswake/proof/phase57_auth_return_boundaries_test.exs", "cd packages/crosswake_sigra &amp;&amp; mix test"]) and all(.commands[]; (.exit_status | type) == "number" and .exit_status == 0 and .outcome == "passed")' .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json</automated>
  </verify>
  <acceptance_criteria>The proof JSON contains mutually consistent public release coordinates and four machine-derived passing command outcomes from the exact immutable SHA.</acceptance_criteria>
  <done>Phase 239 has deterministic external-release evidence that later plans can validate without trusting human-supplied receipt fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external release metadata → local proof | Human-supplied coordinates and public registry/tag metadata are untrusted until reconciled. |
| immutable source checkout → command outcomes | Tests must execute at the same SHA named by the release proof. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-00-01 | Spoofing | release coordinates | high | mitigate | Resolve the public tag, full SHA, and Hex release independently before writing proof. |
| T-239-00-02 | Tampering | command outcomes | high | mitigate | Derive numeric statuses from four exact commands in a clean immutable checkout. |
| T-239-00-03 | Repudiation | release proof | high | mitigate | Record schema, source coordinates, public package metadata, timestamps, commands, and statuses in JSON. |
| T-239-SC | Tampering | Hex package consumption | high | mitigate | Keep publication authorization human-gated while making package/source/test legitimacy mechanically reproducible. |
</threat_model>

<verification>
The JSON schema gate passes only after all four required Crosswake commands pass at the public release SHA and the Hex/Git metadata agree.
</verification>

<success_criteria>
- Public package coordinates resolve to one immutable Crosswake commit.
- Contract, AuthReturn, and full companion tests pass at that commit.
- Later plans can reject incomplete, reordered, nonzero, or human-only outcome claims mechanically.
</success_criteria>

<output>
Create `.planning/phases/239-hosted-session-interop/239-00-SUMMARY.md` when done.
</output>
