---
phase: 239-hosted-session-interop
plan: 06
type: execute
wave: 6
depends_on: [239-05]
files_modified:
  - scripts/ci/hosted-session-interop-proof.sh
  - test/sigra/planning/phase_239_hosted_session_interop_test.exs
  - .planning/phases/239-hosted-session-interop/239-INTEROP-EVIDENCE.json
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "One bounded automation command proves release provenance, the complete ExUnit/Mox matrix, no forbidden dependency placement, no sleep/manual UAT, and recipe alignment."
    - "A machine-readable exact-SHA receipt records commands and outcomes without claiming missing Crosswake or SIGRA evidence as passed."
    - "All D-01 through D-06 decisions and XW-01/XW-02 assertions are traceable to deterministic proof."
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the final source/runtime receipt preserves this unresolved fallback row rather than silently classifying it."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the final source/runtime receipt preserves this unresolved fallback row rather than silently classifying it."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT represent a personal account by manufacturing organization authority or accepting a blank organization value."
      reason: "Descriptor-less phase-specific safety prohibition retained at close; deterministic tests support it without fabricating a fallback descriptor."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT carry a raw session credential, stored token digest, provider payload, or OAuth credential across the companion boundary."
      reason: "Descriptor-less phase-specific safety prohibition retained at close; deterministic tests support it without fabricating a fallback descriptor."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or hosted-return evidence select a session, replace fresh host resolution, or independently admit a route."
      reason: "Descriptor-less phase-specific safety prohibition retained at close; deterministic tests support it without fabricating a fallback descriptor."
  artifacts:
    - path: scripts/ci/hosted-session-interop-proof.sh
      provides: "bounded automation-first phase proof command"
    - path: test/sigra/planning/phase_239_hosted_session_interop_test.exs
      provides: "fast source/release/coverage contract"
    - path: .planning/phases/239-hosted-session-interop/239-INTEROP-EVIDENCE.json
      provides: "exact-SHA machine-readable results and assumption/prohibition disposition"
  key_links:
    - from: scripts/ci/hosted-session-interop-proof.sh
      to: test/example/test/example/accounts/crosswake_session_adapter_test.exs
      via: "runs the complete database-backed adapter suite"
      pattern: "crosswake_session_adapter_test.exs"
    - from: test/sigra/planning/phase_239_hosted_session_interop_test.exs
      to: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json
      via: "checks required receipt fields, exact dependency requirement, guide alignment, and API coverage declaration"
---

<objective>
Seal Phase 239 with automation-first, exact-source evidence rather than manual UAT or source inspection alone.

Purpose: Make XW-01/XW-02 completion reproducible and fail closed when the release receipt, tests, dependency placement, or documented boundary drifts.
Output: bounded proof runner, fast contract test, and machine-readable evidence receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-VALIDATION.md
@.planning/phases/239-hosted-session-interop/COVERAGE.md
@.planning/phases/239-hosted-session-interop/239-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the source, release, and scope contract</name>
  <files>test/sigra/planning/phase_239_hosted_session_interop_test.exs, scripts/ci/hosted-session-interop-proof.sh</files>
  <read_first>
    - `test/sigra/planning/phase_238_generated_auth_runtime_proof_test.exs` for fail-on-missing planning/source contracts.
    - `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json`, `COVERAGE.md`, `test/example/mix.exs`, adapter/tests, and B2C recipe as the contract inputs.
    - `AGENTS.md` automation-first and no-waiver rules.
  </read_first>
  <behavior>
    - Test 1: receipt has all required fields and the example Mix requirement equals it; root `mix.exs` has no `crosswake_sigra` dependency.
    - Test 2: adapter/test contain fresh lookup, personal nil-org, opaque-ref/version, every denial family, evaluator non-invocation, and evidence-only markers.
    - Test 3: recipe requirement and semantics match the receipt/adapter; COVERAGE.md has an unambiguous `detected: false` reason and re-run trigger.
    - Test 4: no sleep/manual-UAT authority, organization sentinel, client-owned binding, or forbidden secret field is introduced in the adapter region.
  </behavior>
  <action>Write the fast ExUnit contract first and make missing files/JSON keys fail directly. Use structural predicates scoped to the adapter/test/recipe regions and rely on runtime assertions for semantic outcomes. Create a strict shell runner that checks formatting, runs the fast contract, then runs the complete example adapter test under a bounded timeout with the configured PostgreSQL service. The script must propagate all failures, print no receipt checksum/secret-like values unnecessarily, use no sleeps/watch loops, and write evidence only after every command succeeds.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_239_hosted_session_interop_test.exs &amp;&amp; bash -n scripts/ci/hosted-session-interop-proof.sh &amp;&amp; shellcheck -x scripts/ci/hosted-session-interop-proof.sh</automated>
  </verify>
  <acceptance_criteria>The fast contract fails on missing/mismatched release, dependency, guide, coverage, or security-boundary inputs; the runner is bounded, failure-propagating, and contains no delay/manual verification path.</acceptance_criteria>
  <done>Phase 239 has a deterministic entry point that cannot pass by merely reading source or skipping database-backed tests.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full proof and record exact-SHA evidence</name>
  <files>.planning/phases/239-hosted-session-interop/239-INTEROP-EVIDENCE.json</files>
  <read_first>
    - `scripts/ci/hosted-session-interop-proof.sh` and fast contract after Task 1.
    - `.planning/phases/239-hosted-session-interop/239-VALIDATION.md` for the complete denial/test map.
    - `AGENTS.md` for retry-once and durable-diagnostic behavior.
  </read_first>
  <action>Run the proof runner against the current commit and available PostgreSQL service. Retry once only for a diagnosed transient service startup failure; repair deterministic failures and rerun from clean evidence. On success, write JSON containing schema identifier, SIGRA full SHA, Crosswake package/version/tag/SHA/checksum identifier, UTC capture time, each exact command, exit status, test category results, XW-01/XW-02 mapping, D-01 through D-06 mapping, all unresolved fallback assumptions, all three descriptor-less flagged-unverified prohibitions, and the API detector declaration. On any missing/failed command, do not write a pass receipt; preserve durable diagnostics and stop.</action>
  <verify>
    <automated>scripts/ci/hosted-session-interop-proof.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_239_hosted_session_interop_test.exs</automated>
  </verify>
  <acceptance_criteria>The evidence JSON is generated only by a green full run, names the current full SHA and exact companion release, maps every requirement/decision, and explicitly retains the unresolved assumptions/prohibitions without marking them verified.</acceptance_criteria>
  <done>Phase 239 has machine-readable, exact-source proof for the full fail-closed interop boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/tests → evidence claim | Static presence must not be mistaken for executed behavior. |
| dependency/release metadata → receipt | Drift or substitution can invalidate the tested contract. |
| test environment → pass receipt | Missing PostgreSQL or failed tests must not become a pass-shaped artifact. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-19 | Repudiation | phase evidence | high | mitigate | Exact SIGRA/Crosswake SHAs, commands, UTC time, and statuses in machine-readable receipt. |
| T-239-20 | Tampering | release/dependency drift | high | mitigate | Fast contract compares receipt, example requirement, lock/source boundary, and guide. |
| T-239-21 | Elevation of Privilege | vacuous source-only proof | high | mitigate | Runner requires complete database-backed ExUnit/Mox suite before writing evidence. |
| T-239-22 | Denial of Service | proof runner | medium | mitigate | Bounded command timeout, no watch/sleep loop, one transient retry only. |
| T-239-23 | Information Disclosure | logs/evidence | medium | mitigate | Record identifiers/checksum provenance without raw credentials/tokens and avoid unnecessary secret-like output. |
</threat_model>

<verification>
The final gate is the proof runner followed by the fast contract over the generated receipt. Missing services/evidence remain failures with diagnostics, never waivers.
</verification>

<success_criteria>
- All focused and complete adapter tests execute successfully.
- Release/dependency/docs/coverage stay mechanically aligned.
- Exact-SHA evidence covers XW-01, XW-02, D-01 through D-06, and all declared threats.
</success_criteria>

## Artifacts this phase produces

- `scripts/ci/hosted-session-interop-proof.sh` as the reproducible proof entry point.
- Fast phase contract test.
- `239-INTEROP-EVIDENCE.json` exact-source receipt.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plans | Status | Notes |
|--------|----|-----------------------|-------|--------|-------|
| GOAL | — | Fail-closed SIGRA-to-Crosswake backend-session boundary for personal accounts | 02–06 | COVERED | Tracer plus currentness, binding, evidence, and seal expansions. |
| REQ | XW-01 | Backend-validated personal projection without invented org or exposed credentials/tokens | 01, 02, 05, 06 | COVERED | External contract gate, personal tracer, evidence/docs, final proof. |
| REQ | XW-02 | Missing/expired/revoked/switched replay denies; return alone never grants | 02–06 | COVERED | Fresh lookup, denial matrices, evidence boundary, final receipt. |
| CONTEXT | D-01 | Nullable personal org with nonblank organization compatibility | 01, 02, 06 | COVERED | Crosswake handoff/release and tracer contract tests. |
| CONTEXT | D-02 | Opaque refs and fact-only secret-free projection | 01, 02, 06 | COVERED | Keyed refs and negative assertions. |
| CONTEXT | D-03 | Fresh session/user resolution before every evaluation/replay | 02, 03, 05, 06 | COVERED | Adapter order and non-invocation proof. |
| CONTEXT | D-04 | Current session/subject/version binding and explicit account-switch denial | 03, 04, 06 | COVERED | Complete binding/switch matrix. |
| CONTEXT | D-05 | Return data is evidence/navigation only | 01, 05, 06 | COVERED | Released AuthReturn contract plus host no-session proof. |
| CONTEXT | D-06 | Deterministic contract/evaluator matrix | 01–06 | COVERED | Cross-repository focused tests plus ExUnit/Mox/Ecto receipt. |
| RESEARCH | — | Backward-compatible Crosswake successor and ordered release handoff | 01, 02 | COVERED | Explicit external dependency; no false local ownership. |
| RESEARCH | — | Server-owned session version source | 02, 04 | COVERED | Session-row insertion timestamp in microseconds, bound and mismatch-tested. |
| RESEARCH | — | No root runtime dependency; use example/proof host | 02, 06 | COVERED | Dependency placement and source contract. |
| RESEARCH | — | Exact expiry semantics, no sleeps, real Ecto + Mox | 03, 04, 06 | COVERED | Deterministic matrices and runner. |
| RESEARCH | — | AttemptRecord conditional only if exercised | 01, 05 | COVERED | Handoff applies nullable rule only if release path uses it; host proof stays envelope-only. |

Excluded without gaps: runtime auth-schema prefix override, admin/organizations/passkeys/MFA/native authority, provider/device/adopter proof, and Phase 240 launch operations.

<output>
Create `.planning/phases/239-hosted-session-interop/239-06-SUMMARY.md` when done.
</output>

