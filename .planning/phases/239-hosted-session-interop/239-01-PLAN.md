---
phase: 239-hosted-session-interop
plan: 01
type: execute
wave: 1
depends_on: [239-00]
files_modified: []
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "A deterministic proof confirms the independently published Crosswake successor is the release produced from the locked D-01/D-02/D-05 handoff before SIGRA resolves it."
    - "The release preserves organization-scoped compatibility while accepting personal org_id: nil and rejecting blank organization authority."
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the released Crosswake contract and provenance receipt provide the explicit review surface."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; return-boundary and fail-closed tests provide the explicit review surface."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT represent a personal account by manufacturing organization authority or accepting a blank organization value."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-01; flagged-unverified because the fallback supplied no check descriptor."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT carry a raw session credential, stored token digest, provider payload, or OAuth credential across the companion boundary."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-02; flagged-unverified because the fallback supplied no check descriptor."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or hosted-return evidence select a session, replace fresh host resolution, or independently admit a route."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-05; flagged-unverified because the fallback supplied no check descriptor."
  artifacts:
    - path: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md
      provides: "exact external repository patch, test, release, and receipt contract"
    - path: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
      provides: "immutable release coordinates and machine-derived Crosswake command outcomes"
  key_links:
    - from: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md
      to: szTheory/crosswake/packages/crosswake_sigra
      via: "maintainer executes the named contract/test/release steps in the companion repository"
    - from: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
      to: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE.json
      via: "validated proof fields are the only accepted release-receipt input for Plan 02"
---

<objective>
Fail closed on the cross-repository release boundary before SIGRA consumes the companion package.

Purpose: Crosswake is independently owned and published; an explicit legitimacy/provenance gate prevents a fabricated local implementation claim or an unverified package install.
Output: Mechanically validated successor version, tag/SHA, checksum, and passing companion contract evidence for Plan 02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-RESEARCH.md
@.planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md
@.planning/phases/239-hosted-session-interop/239-00-SUMMARY.md
@.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate SIGRA consumption on the reproducible Crosswake release proof</name>
  <files>.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json</files>
  <read_first>
    - `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md` for the exact D-01/D-02/D-05 external contract and receipt fields.
    - `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json` for the machine-derived source, package, and command evidence from Plan 00.
    - `.planning/phases/239-hosted-session-interop/239-RESEARCH.md` Package Legitimacy Audit and assumptions A1/A3.
  </read_first>
  <action>Reject the release unless the Plan 00 proof uses schema `sigra.phase239.crosswake-release-proof.v1`, supplies every package/version/requirement/tag/full-SHA/checksum/timestamp field, and records the four exact handoff commands in order with numeric zero statuses and `passed` outcomes. Independently query the public Hex API and resolve the public Git tag in a temporary clean checkout to confirm that the proof package/version/checksum and peeled tag SHA still match. Treat the verified proof fields as the sole source for Plan 02's `239-CROSSWAKE-RELEASE.json`; maintainer prose or checkpoint receipt fields cannot override them. This gate approves package provenance and Crosswake-owned test execution only; it does not substitute for SIGRA integration tests.</action>
  <verify>
    <automated>jq -e '.schema == "sigra.phase239.crosswake-release-proof.v1" and .repository == "https://github.com/szTheory/crosswake.git" and .package == "crosswake_sigra" and (.version | type == "string" and length &gt; 0) and .version != "0.1.1" and (.requirement | type == "string" and length &gt; 0) and (.git_tag | type == "string" and length &gt; 0) and (.git_sha | test("^[0-9a-f]{40}$")) and (.hex_checksum | type == "string" and length &gt; 0) and (.published_at | type == "string" and length &gt; 0) and (.verified_at | type == "string" and length &gt; 0) and ([.commands[] | select(.exit_status == 0 and .outcome == "passed") | .command] == ["mix format --check-formatted packages/crosswake_sigra", "mix test packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs", "mix test packages/crosswake_sigra/test/crosswake/proof/phase57_auth_return_boundaries_test.exs", "cd packages/crosswake_sigra &amp;&amp; mix test"])' .planning/phases/239-hosted-session-interop/239-CROSSWAKE-RELEASE-PROOF.json</automated>
  </verify>
  <acceptance_criteria>The proof supplies every release coordinate and exact command outcome required by the handoff, its public Hex/tag metadata revalidates, and no human-supplied receipt field is accepted as standalone proof.</acceptance_criteria>
  <done>The executor has trustworthy release coordinates to record and consume; no SIGRA dependency file has changed before this gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external maintainer/repository → SIGRA executor | Release coordinates and test claims cross repository ownership. |
| Hex/GitHub metadata → dependency resolution | A tag, checksum, or package can be mismatched or substituted. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-01 | Spoofing | Crosswake release identity | high | mitigate | Mechanically compare package, immutable tag/SHA, checksum, and public registry metadata before dependency resolution. |
| T-239-02 | Tampering | companion contract release | high | mitigate | Require machine-derived passing personal/org/blank, AuthReturn, and full companion test outcomes from the exact published SHA. |
| T-239-SC | Tampering | Hex package consumption | high | mitigate | Consume only Plan 00's public-metadata-verified proof after the non-auto-approvable publication checkpoint. |
</threat_model>

<verification>
No package-manager command or SIGRA source edit occurs in this plan. The gate is satisfied only by a schema-valid proof whose exact command outcomes and public release metadata are mechanically consistent.
</verification>

<success_criteria>
- Crosswake delivery remains explicitly external.
- The published successor is tied to immutable source and machine-derived focused/full companion test outcomes.
- Plans 02–06 receive exact release coordinates rather than guessing a version.
</success_criteria>

## Artifacts this phase produces

- External: one backward-compatible `crosswake_sigra` successor release implementing D-01/D-02/D-05.
- Local planning input: the validated `239-CROSSWAKE-RELEASE-PROOF.json` values consumed by `239-CROSSWAKE-RELEASE.json` in Plan 02.

<output>
Create `.planning/phases/239-hosted-session-interop/239-01-SUMMARY.md` when done.
</output>
