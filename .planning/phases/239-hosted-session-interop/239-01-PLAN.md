---
phase: 239-hosted-session-interop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "A human confirms the independently published Crosswake successor is the release produced from the locked D-01/D-02/D-05 handoff before SIGRA resolves it."
    - "The release preserves organization-scoped compatibility while accepting personal org_id: nil and rejecting blank organization authority."
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the released Crosswake contract and provenance receipt provide the explicit review surface."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; return-boundary and fail-closed tests provide the explicit review surface."
  prohibitions:
    - requirement_id: XW-01
      category: authority-integrity
      status: unverified
      flagged: true
      statement: "MUST NOT represent a personal account by manufacturing organization authority or accepting a blank organization value."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-01; flagged-unverified because the fallback supplied no check descriptor."
    - requirement_id: XW-01
      category: secret-boundary
      status: unverified
      flagged: true
      statement: "MUST NOT carry a raw session credential, stored token digest, provider payload, or OAuth credential across the companion boundary."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-02; flagged-unverified because the fallback supplied no check descriptor."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unverified
      flagged: true
      statement: "MUST NOT let callback or hosted-return evidence select a session, replace fresh host resolution, or independently admit a route."
      reason: "Descriptor-less phase-specific safety prohibition recalled from D-05; flagged-unverified because the fallback supplied no check descriptor."
  artifacts:
    - path: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md
      provides: "exact external repository patch, test, release, and receipt contract"
  key_links:
    - from: .planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md
      to: szTheory/crosswake/packages/crosswake_sigra
      via: "maintainer executes the named contract/test/release steps in the companion repository"
---

<objective>
Fail closed on the cross-repository release boundary before SIGRA consumes the companion package.

Purpose: Crosswake is independently owned and published; an explicit legitimacy/provenance gate prevents a fabricated local implementation claim or an unverified package install.
Output: Human-confirmed successor version, tag/SHA, checksum, and passing companion contract evidence for Plan 02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-hosted-session-interop/239-CONTEXT.md
@.planning/phases/239-hosted-session-interop/239-RESEARCH.md
@.planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the Crosswake successor release and package legitimacy</name>
  <what-built>The Crosswake maintainer has executed `239-CROSSWAKE-HANDOFF.md` in the external repository and published the independently versioned companion successor.</what-built>
  <how-to-verify>
    1. Open the public Hex package version and immutable GitHub release tag supplied by the maintainer.
    2. Confirm the tag resolves to the supplied full SHA and the package metadata/checksum identifies that release.
    3. Confirm the focused personal/org/blank contract and AuthReturn boundary commands passed at that SHA.
    4. Return every field listed under `Return Receipt`; do not approve a partial or inconsistent receipt.
  </how-to-verify>
  <read_first>
    - `.planning/phases/239-hosted-session-interop/239-CROSSWAKE-HANDOFF.md` for the exact D-01/D-02/D-05 external contract and receipt fields.
    - `.planning/phases/239-hosted-session-interop/239-RESEARCH.md` Package Legitimacy Audit and assumptions A1/A3.
  </read_first>
  <action>Using the completed Crosswake handoff, obtain the successor version, compatible Mix requirement, immutable tag and full SHA, Hex checksum, UTC publication time, and passing focused contract/AuthReturn commands. Verify the public Hex page and GitHub tag point to the same released source. This gate approves package provenance only; it does not substitute for SIGRA integration tests.</action>
  <verify>
    <automated>MISSING — the Hex legitimacy seam does not support a conclusive registry verdict; the executor must perform read-only Hex/GitHub checks and record their outputs after the blocking human confirmation.</automated>
    <human-check>Confirm the package page, immutable tag/SHA, checksum, and companion test results match the handoff and contain the D-01 personal-account change.</human-check>
  </verify>
  <acceptance_criteria>The response supplies every Return Receipt field from `239-CROSSWAKE-HANDOFF.md`, and the human types `approved` only after comparing the public Hex release with the immutable Git tag/SHA.</acceptance_criteria>
  <done>The executor has trustworthy release coordinates to record and consume; no SIGRA dependency file has changed before this gate.</done>
  <resume-signal>Type `approved` and provide the complete Return Receipt fields, or describe the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external maintainer/repository → SIGRA executor | Release coordinates and test claims cross repository ownership. |
| Hex/GitHub metadata → dependency resolution | A tag, checksum, or package can be mismatched or substituted. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-01 | Spoofing | Crosswake release identity | high | mitigate | Blocking comparison of package, immutable tag/SHA, checksum, and public registry metadata before dependency resolution. |
| T-239-02 | Tampering | companion contract release | high | mitigate | Require passing personal/org/blank and AuthReturn tests from the exact published SHA. |
| T-239-SC | Tampering | Hex package consumption | high | mitigate | Treat unsupported legitimacy automation as ASSUMED and require this non-auto-approvable human checkpoint. |
</threat_model>

<verification>
No package-manager command or SIGRA source edit occurs in this plan. The gate is satisfied only by complete, mutually consistent public release evidence.
</verification>

<success_criteria>
- Crosswake delivery remains explicitly external.
- The published successor is tied to immutable source and focused tests.
- Plans 02–06 receive exact release coordinates rather than guessing a version.
</success_criteria>

## Artifacts this phase produces

- External: one backward-compatible `crosswake_sigra` successor release implementing D-01/D-02/D-05.
- Local planning input: the complete receipt values consumed by `239-CROSSWAKE-RELEASE.json` in Plan 02.

<output>
Create `.planning/phases/239-hosted-session-interop/239-01-SUMMARY.md` when done.
</output>
