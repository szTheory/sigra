---
phase: 35
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/templates/installer_drift_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "installer_drift_test.exs gains at least one generalized must_not (or equivalent loop) that flags inert span-based faux nav items in admin shell templates."
    - "Existing fixtures #1–#18 remain unchanged in intent (still pass)."
  artifacts:
    - path: "test/sigra/templates/installer_drift_test.exs"
      provides: "INT-04 dead-text class guard beyond Users-only"
nyquist_compliant: true
---

<objective>
Implement **ROADMAP SC2**: extend `installer_drift_test.exs` so **dead-text navigation labels** (inert `<span>` inside nav `<li>`) regress for **admin shell** templates, not only the Phase 33 Users fixture.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation |
|----|--------|---------|----------|------------|
| T-35-02 | Tampering | Over-broad regex breaks legitimate markup | medium | Restrict regex to `admin/components/admin_shell.ex` paths only; run example `admin_shell_test.exs` |
</threat_model>

<context>
@.planning/phases/35-shift-left-verification-automation/35-CONTEXT.md
@priv/templates/sigra.install/admin/components/admin_shell.ex
@test/example/lib/example_web/components/admin_shell.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Generalized dead-span nav guard fixture</name>
  <files>test/sigra/templates/installer_drift_test.exs</files>
  <read_first>
    - test/sigra/templates/installer_drift_test.exs (full @fixtures structure and fix #18)
    - priv/templates/sigra.install/admin/components/admin_shell.ex
    - test/example/lib/example_web/components/admin_shell.ex
  </read_first>
  <action>
    1. Append a new `@fixtures` entry `id: "fix #19 — admin_shell no inert span nav labels (generalized INT-04)"` targeting:
       - `template:` `priv/templates/sigra.install/admin/components/admin_shell.ex`
       - `example:` `test/example/lib/example_web/components/admin_shell.ex`
    2. Add `must_not` tuples that forbid the pattern **`<li>`** containing **only** a **`<span`** navigation row **without** an `<a ` on the same line for lines that match `nav` / `sidebar` context OR use a single high-signal regex agreed in PR: e.g. forbid `&lt;li&gt;\s*&lt;span class=\"[^\"]*text-base-content` sequence that appears inside the desktop sidebar block comment region—**concrete**: copy the exact forbidden regex from Phase 33’s dead Users line generalized to any label: `~r/&lt;li&gt;\s*&lt;span[^&gt;]*&gt;[^&lt;]+&lt;\/span&gt;\s*&lt;\/li&gt;/` must **not** appear unless immediately preceded in file by `href=` on same logical nav item (if too broad, instead add **two** explicit `must_not` rows for `Organizations` and `Users` span-only rows matching current template structure—document choice in fixture comment).
    3. Ensure template and example columns use **identical** regex strings in each tuple per existing fixture rules.
  </action>
  <acceptance_criteria>
    - `grep -q "fix #19" test/sigra/templates/installer_drift_test.exs` exits 0.
    - `grep -c "id: \"fix #" test/sigra/templates/installer_drift_test.exs` prints a number **≥ 19**.
  </acceptance_criteria>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/templates/installer_drift_test.exs</automated>
  </verify>
  <done>SC2 drift guard extended; full drift file green.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/templates/installer_drift_test.exs` exits 0
</verification>

<success_criteria>
- SC2 satisfied: new fixture + generalized dead-text detection documented in fixture `id` comment.
</success_criteria>
