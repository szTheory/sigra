---
phase: 35
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - scripts/ci/milestone-verification-gate.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "scripts/ci/milestone-verification-gate.sh exits 0 on main when every completed v1.2 phase has NN-*-VERIFICATION.md."
    - "ci.yml invokes the gate in a job that runs on pull_request and push to main."
  artifacts:
    - path: "scripts/ci/milestone-verification-gate.sh"
      provides: "Mechanical VERIFICATION.md presence gate"
nyquist_compliant: true
---

<objective>
Implement **ROADMAP SC4**: add `scripts/ci/milestone-verification-gate.sh` and CI wiring so the repo fails if a **completed** v1.2 roadmap phase lacks `{N}-VERIFICATION.md` under `.planning/phases/`.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation |
|----|--------|---------|----------|------------|
| T-35-04 | Tampering | Script skipped in CI | medium | `bash -n` + required job in workflow |
</threat_model>

<context>
@scripts/ci/phase34-uat-contracts.sh
@.planning/ROADMAP.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: milestone-verification-gate.sh</name>
  <files>scripts/ci/milestone-verification-gate.sh</files>
  <read_first>
    - scripts/ci/phase34-uat-contracts.sh
    - .planning/ROADMAP.md (Progress table lines ~188–198)
  </read_first>
  <action>
    1. Create `scripts/ci/milestone-verification-gate.sh` with `set -euo pipefail` and `ROOT` resolution mirroring phase34 script.
    2. Define bash array `PHASES_NEEDING_VERIFICATION=(27 28 29 30 31 32 33 34)` — **comment** “Add 35 when Phase 35 execution completes and ROADMAP marks it Complete.”
    3. For each phase number `N`, run `find "${ROOT}/.planning/phases" -name "${N}-*-VERIFICATION.md" | head -1` — require non-empty output; else `fail "missing VERIFICATION for phase ${N}"`.
    4. Add optional `grep` contracts: each file must contain heading `##` and string `VERIFICATION` (case-sensitive) — keep minimal.
    5. `echo OK: milestone-verification-gate` on success.
  </action>
  <acceptance_criteria>
    - `test -x scripts/ci/milestone-verification-gate.sh || test -f scripts/ci/milestone-verification-gate.sh` — file exists; if not executable, CI invokes `bash scripts/ci/milestone-verification-gate.sh`.
    - `grep -q 'PHASES_NEEDING_VERIFICATION' scripts/ci/milestone-verification-gate.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/milestone-verification-gate.sh && bash scripts/ci/milestone-verification-gate.sh</automated>
  </verify>
  <done>Gate passes on current repo.</done>
</task>

<task type="execute">
  <name>Task 2: Wire CI job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (near other `scripts/ci/*` steps)
  </read_first>
  <action>
    1. Add job `milestone_verification_gate` (or step inside existing lint job) running on `pull_request` + `push` with `ubuntu-latest`, checkout, `bash scripts/ci/milestone-verification-gate.sh`.
    2. Place it early in workflow (no Postgres required).
  </action>
  <acceptance_criteria>
    - `grep -q "milestone-verification-gate.sh" .github/workflows/ci.yml` exits 0.
  </acceptance_criteria>
  <verify>
    - `grep -n "milestone-verification-gate" .github/workflows/ci.yml` shows valid YAML indentation (human + CI).
  </verify>
  <done>SC4 wired in CI.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/milestone-verification-gate.sh` exits 0
- `bash -n scripts/ci/milestone-verification-gate.sh` exits 0
</verification>

<success_criteria>
- SC4 satisfied: script + CI reference merged.
</success_criteria>
