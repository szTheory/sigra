---
phase: 35
plan: 06
type: execute
wave: 4
depends_on: ["35-03"]
files_modified:
  - scripts/ci/admin-artifact-bundle-contract.sh
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Script or CI step asserts ≥15 PNGs and min size bytes for curated admin checkpoints."
    - "CONTRIBUTING.md lists reviewer checklist for admin artifact bundles."
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Reviewer checklist for CI artifacts"
nyquist_compliant: true
---

<objective>
Implement **ROADMAP SC6**: artifact-bundle contract (15 PNGs × size floor) plus **CONTRIBUTING.md** documentation for reviewers.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation |
|----|--------|---------|----------|------------|
| T-35-06 | Info disclosure | CONTRIBUTING leaks secrets | low | Document artifact names only, no credentials |
</threat_model>

<context>
@.github/workflows/ci.yml
@test/example/priv/playwright/helpers/adminArtifacts.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: admin-artifact-bundle-contract.sh</name>
  <files>scripts/ci/admin-artifact-bundle-contract.sh</files>
  <read_first>
    - .github/workflows/ci.yml (artifact upload paths for admin checkpoints)
    - test/example/priv/playwright/helpers/adminArtifacts.ts
  </read_first>
  <action>
    1. Create `scripts/ci/admin-artifact-bundle-contract.sh` accepting one argument `ARTIFACT_ROOT` defaulting to `test/example/priv/playwright/artifacts/admin-checkpoints`.
    2. Count `*.png` under that root recursively; require count **≥ 15**.
    3. For each PNG, `stat -f%z` (mac) / `stat -c%s` (linux)—use portable `wc -c < file` in bash loop; require each **≥ 5000** bytes (constant `MIN_BYTES=5000` at top).
    4. On failure print which files failed count/size checks.
  </action>
  <acceptance_criteria>
    - `grep -q "MIN_BYTES" scripts/ci/admin-artifact-bundle-contract.sh` exits 0.
    - `grep -q "15" scripts/ci/admin-artifact-bundle-contract.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/admin-artifact-bundle-contract.sh</automated>
  </verify>
  <done>Script parses; optional dry-run documented if paths empty locally.</done>
</task>

<task type="execute">
  <name>Task 2: Run contract after Playwright in CI + CONTRIBUTING.md</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (`example_playwright_smoke` job tail)
  </read_first>
  <action>
    1. After Playwright admin checkpoint steps in `example_playwright_smoke`, add `bash scripts/ci/admin-artifact-bundle-contract.sh` with `working-directory` or `ARTIFACT_ROOT` env pointing to the directory CI populates (`test/example/priv/playwright/artifacts/admin-checkpoints`).
    2. Create repo-root `CONTRIBUTING.md` if missing with sections: **Developing**, **CI overview**, **Reviewing admin Playwright artifacts** (bullets: download `admin-example-report`, open HTML report, verify 15 PNGs in `artifacts/admin-checkpoints/`, cross-check job names `example_playwright_smoke` / `generated_admin_playwright_smoke`).
  </action>
  <acceptance_criteria>
    - `grep -q "admin-artifact-bundle-contract" .github/workflows/ci.yml` exits 0.
    - `test -f CONTRIBUTING.md` exits 0.
    - `grep -q "admin-example-report" CONTRIBUTING.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/admin-artifact-bundle-contract.sh</automated>
  </verify>
  <done>SC6 wired.</done>
</task>

</tasks>

<verification>
- CI YAML loads; `CONTRIBUTING.md` exists and references artifact checklist
</verification>

<success_criteria>
- SC6 satisfied.
</success_criteria>
