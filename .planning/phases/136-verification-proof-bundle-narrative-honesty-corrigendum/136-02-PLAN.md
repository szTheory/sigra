---
phase: 136-verification-proof-bundle-narrative-honesty-corrigendum
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - CHANGELOG.md
  - .planning/phases/136-verification-proof-bundle-narrative-honesty-corrigendum/136-02-SUMMARY.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "A reader of the v1.25 EMAIL-RAILS entry in MILESTONES.md sees an explicit corrigendum stating the library-resident Sigra.Mailers.Adapters.Mailglass module and --with-mailglass installer flag from Phase 111/114 did NOT land on the release branch and are not part of the supported surface."
    - "The same correction is applied to the 'Previously shipped: v1.25 EMAIL-RAILS' bullet in PROJECT.md so the two planning narratives agree (no internal contradiction)."
    - "CHANGELOG.md [Unreleased] notes the v1.29 Mailglass integration posture as host-owned wiring via the Sigra.Mailer behaviour, referencing guides/recipes/companion-libs/mailglass.md; and CHANGELOG.md has exactly one ## [Unreleased] header (the stray duplicate at line 222 is removed)."
    - "All corrigendum prose is truthful and carries no banned marketing phrases (seamlessly, just works, production-ready out of the box, the recommended way)."
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "v1.25 EMAIL-RAILS entry with Mailglass-overclaim corrigendum covering lines 79, 80, 84"
      contains: "did not land"
    - path: ".planning/PROJECT.md"
      provides: "v1.25 narrative bullet corrected to match MILESTONES.md corrigendum"
      contains: "--with-mailglass"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] host-owned-wiring note + duplicate Unreleased header removed"
      contains: "Sigra.Mailer"
  key_links:
    - from: "CHANGELOG.md"
      to: "guides/recipes/companion-libs/mailglass.md"
      via: "[Unreleased] note references the host-owned-wiring recipe as the supported posture"
      pattern: "companion-libs/mailglass"
    - from: ".planning/PROJECT.md"
      to: ".planning/MILESTONES.md"
      via: "both v1.25 entries carry the same Mailglass-did-not-land correction"
      pattern: "Mailglass"
---

<objective>
Land the DOC-01 narrative-honesty corrigendum: correct the three v1.25 EMAIL-RAILS Mailglass overclaims across `MILESTONES.md`, `PROJECT.md`, and `CHANGELOG.md`, and remove the stray duplicate `## [Unreleased]` header as a same-file honesty cleanup. The corrigendum states the truth: the library-resident `Sigra.Mailers.Adapters.Mailglass` module and `--with-mailglass` installer flag from Phase 111/114 did NOT land on the release branch and are not part of the supported surface; v1.29's Mailglass posture is recipe-only host-owned wiring via the `Sigra.Mailer` behaviour.

Purpose: v1.25's planning narrative overclaimed a Mailglass adapter + install flag that never shipped. v1.29 locks Mailglass-is-recipe-only, so this is the canonical milestone to correct the v1.25 narrative — keeping the planning record internally honest and consistent with the recipe-only decision.

Output:
- `.planning/MILESTONES.md` (v1.25 corrigendum)
- `.planning/PROJECT.md` (matching correction)
- `CHANGELOG.md` ([Unreleased] note + duplicate-header removal)
- `.planning/phases/136-verification-proof-bundle-narrative-honesty-corrigendum/136-02-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/136-verification-proof-bundle-narrative-honesty-corrigendum/136-CONTEXT.md

# Corrigendum targets (exact lines pinned below in interfaces)
@.planning/MILESTONES.md
@CHANGELOG.md

# CHANGELOG note referent — the supported recipe-only Mailglass posture
@guides/recipes/companion-libs/mailglass.md

<interfaces>
<!-- DOC-01 corrigendum targets, pinned to exact current lines (verified at planning time): -->
<!-- MILESTONES.md:73  "## v1.25 EMAIL-RAILS (Shipped: 2026-05-23)" -->
<!-- MILESTONES.md:79  overclaim: "Introduced an optional `Sigra.Mailers.Adapters.Mailglass` shim and `--with-mailglass` installer path ..." -->
<!-- MILESTONES.md:80  overclaim: "Shipped generated-host override rails and the Mailglass preview catalog for all 18 auth email surfaces." -->
<!-- MILESTONES.md:84  overclaim: "Mailglass adapter compilation is now optional-dependency-safe, and the nested example app can compile ..." -->
<!-- PROJECT.md:101 "### Previously shipped: v1.25 EMAIL-RAILS" -->
<!-- PROJECT.md:103 overclaim: "optional Mailglass adapter plus `--with-mailglass` installer path" -->
<!-- CHANGELOG.md:33  the correctly-placed "## [Unreleased]" header (add the note here) -->
<!-- CHANGELOG.md:222 the STRAY duplicate "## [Unreleased]" header (remove this one, D-09) -->
<!-- REQUIREMENTS.md Out-of-Scope: banned phrases = "seamlessly", "just works", "production-ready out of the box", "the recommended way" -->
</interfaces>

<source_audit>
GOAL: Phase 136 goal "corrigendum landed" is covered by Task 136-02-01 (MILESTONES.md + PROJECT.md corrigendum) and Task 136-02-02 (CHANGELOG.md note + duplicate-header removal).
REQ: DOC-01 is covered by both tasks in this plan and appears in the frontmatter `requirements`. PROOF-01 is covered by Plan 01; traceability reconciliation by Plan 03.
RESEARCH: No 136-RESEARCH.md (no-new-code phase). The overclaim line numbers are pinned from 136-CONTEXT.md canonical_refs and re-verified against current file state.
CONTEXT: D-06 (MILESTONES.md corrigendum covering lines 79/80/84) → Task 136-02-01. D-07 (PROJECT.md lines 103-104) → Task 136-02-01. D-08 (CHANGELOG [Unreleased] host-owned-wiring note at line 33) → Task 136-02-02. D-09 (remove stray duplicate ## [Unreleased] at line 222) → Task 136-02-02. Out-of-scope honored: DOC-01 is strictly the v1.25 Mailglass narrative; the v1.29 Threadline forwarder is NOT in scope; no re-landing of the orphaned Phase 111/114 adapter; banned marketing phrases excluded.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 136-02-01: Corrigendum in MILESTONES.md + PROJECT.md (D-06, D-07)</name>
  <files>
    .planning/MILESTONES.md
    .planning/PROJECT.md
  </files>
  <read_first>
    .planning/MILESTONES.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    guides/recipes/companion-libs/mailglass.md
    .planning/ROADMAP.md
  </read_first>
  <action>
    D-06 (MILESTONES.md, v1.25 EMAIL-RAILS entry at lines 73-84): append a corrigendum to the v1.25 entry whose scope covers all three overclaiming lines — line 79 (the `Sigra.Mailers.Adapters.Mailglass` shim + `--with-mailglass` installer path claim), line 80 (the "Shipped ... Mailglass preview catalog" claim), and line 84 (the "Mailglass adapter compilation is now optional-dependency-safe" claim). Add a clearly-labeled `**Corrigendum (v1.29 DOC-01, 2026-05-28):**` line (or short block) after the key-accomplishments bullets, before the `---` separator at line 86. State plainly: the library-resident `Sigra.Mailers.Adapters.Mailglass` module and the `--with-mailglass` installer flag described in Phase 111/114 did NOT land on the release branch and are NOT part of the supported surface; the supported Mailglass posture is recipe-only host-owned wiring via the `Sigra.Mailer` behaviour (see `guides/recipes/companion-libs/mailglass.md`). Do NOT delete the original bullets (preserve the historical record); the corrigendum corrects them in place. Do NOT reopen the recover-vs-delete decision for the orphaned wip branches — the corrigendum only corrects the narrative.

    D-07 (PROJECT.md, "Previously shipped: v1.25 EMAIL-RAILS" at lines 101-106): apply the same correction to the line-103 bullet "optional Mailglass adapter plus `--with-mailglass` installer path". Either replace that bullet with a truthful one (host-owned Mailglass wiring via `Sigra.Mailer`; no library-resident adapter, no `--with-mailglass` flag) or append a parallel `**Corrigendum:**` note under the v1.25 bullets, so PROJECT.md and MILESTONES.md tell the same story and are not internally contradictory.

    All prose must be truthful and must contain none of the REQUIREMENTS.md banned phrases: "seamlessly", "just works", "production-ready out of the box", "the recommended way".
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; M=.planning/MILESTONES.md; P=.planning/PROJECT.md; grep -qi "did not land\|did .not. land\|not part of the supported surface" "$M"; grep -q "Sigra.Mailers.Adapters.Mailglass" "$M"; grep -q -- "--with-mailglass" "$M"; grep -qi "Sigra.Mailer" "$M"; grep -qi "Mailglass" "$P"; grep -q -- "--with-mailglass" "$P"; grep -qi "Sigra.Mailer\|recipe-only\|host-owned\|did not land\|not part of the supported surface" "$P"; for f in "$M" "$P"; do ! grep -qiE "seamlessly|just works|production-ready out of the box|the recommended way" "$f"; done'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MILESTONES.md` v1.25 entry contains a corrigendum line/block stating the library-resident `Sigra.Mailers.Adapters.Mailglass` module and `--with-mailglass` flag did NOT land and are not part of the supported surface, and naming the recipe-only `Sigra.Mailer` host-owned posture (referencing `guides/recipes/companion-libs/mailglass.md`).
    - The corrigendum's coverage addresses all three overclaiming lines (79 shim/flag, 80 catalog, 84 adapter-compile) — not only line 79.
    - The original v1.25 bullets are preserved (not deleted); the corrigendum corrects them in place.
    - `.planning/PROJECT.md` "Previously shipped: v1.25 EMAIL-RAILS" no longer asserts an unqualified "optional Mailglass adapter plus `--with-mailglass` installer path" as shipped; it carries the same correction so it agrees with MILESTONES.md.
    - Neither file contains any of the banned phrases: `seamlessly`, `just works`, `production-ready out of the box`, `the recommended way`.
  </acceptance_criteria>
  <done>Both v1.25 planning narratives (MILESTONES.md and PROJECT.md) carry the truthful, consistent Mailglass corrigendum with no marketing voice.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 136-02-02: CHANGELOG [Unreleased] host-owned-wiring note + remove duplicate header (D-08, D-09)</name>
  <files>
    CHANGELOG.md
  </files>
  <read_first>
    CHANGELOG.md
    guides/recipes/companion-libs/mailglass.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    D-08 (add the note): under the correctly-placed `## [Unreleased]` header at CHANGELOG.md line 33, add a bullet clarifying the v1.29 Mailglass integration posture as host-owned wiring via the `Sigra.Mailer` behaviour (the host owns the mailer module; Sigra ships no library-resident Mailglass adapter and no `--with-mailglass` installer flag), with a referent to `guides/recipes/companion-libs/mailglass.md`. Place the bullet under the existing line-33 `## [Unreleased]` section (the one immediately above the `## [0.2.5]` release at line 35), NOT under the stray duplicate further down. Keep the Keep-a-Changelog idiom (e.g. a `### Documentation` or `### Changed` subsection bullet is fine if it matches surrounding style); the note must be truthful and free of banned marketing phrases.

    D-09 (remove the stray duplicate): the file currently has two `## [Unreleased]` headers — the correct one at line 33 and a stray duplicate at line 222 (just below a list of historical commit-style entries near line 220). Remove the duplicate `## [Unreleased]` header at line 222 so the file has exactly one `## [Unreleased]` section. Preserve the bullet content that currently sits under the line-222 header (the `.formatter.exs` chore, the Human GA pointer, the AUD-04 inventory entries, etc.) — that content is historical changelog material and must not be lost; only the redundant second `## [Unreleased]` header line is removed (the orphaned bullets fold under their preceding context / the single canonical Unreleased section as appropriate, with no header text duplicated). After the edit, exactly one `## [Unreleased]` header must remain in the file.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; C=CHANGELOG.md; test "$(grep -c "^## \[Unreleased\]" "$C")" -eq 1; grep -qi "Sigra.Mailer" "$C"; grep -q "companion-libs/mailglass" "$C"; grep -q -- "--with-mailglass" "$C"; ! grep -qiE "seamlessly|just works|production-ready out of the box|the recommended way" "$C"; grep -q "formatter.exs" "$C"; grep -q "Human GA" "$C"'</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains exactly one `## [Unreleased]` header (`grep -c "^## \[Unreleased\]"` returns 1) — the stray duplicate previously at line 222 is removed.
    - The remaining `## [Unreleased]` section contains a bullet naming the host-owned `Sigra.Mailer` Mailglass posture, stating no library-resident adapter and no `--with-mailglass` flag, and referencing `guides/recipes/companion-libs/mailglass.md`.
    - The historical bullet content that sat under the removed duplicate header is preserved (the `.formatter.exs` chore line and the `Human GA` pointer still grep-match).
    - `CHANGELOG.md` contains none of the banned phrases: `seamlessly`, `just works`, `production-ready out of the box`, `the recommended way`.
  </acceptance_criteria>
  <done>CHANGELOG.md has a truthful [Unreleased] Mailglass host-owned-wiring note pointing at the recipe, and exactly one [Unreleased] header with no historical content lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning narrative → adopter/maintainer belief | Overclaimed Mailglass adapter/flag in v1.25 narrative misleads readers about the supported surface; the corrigendum is the only material risk class here (docs honesty). |
| edit scope → historical record | Corrigendum edits must correct without erasing the historical bullets or losing CHANGELOG history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-04 | Repudiation | v1.25 narrative accuracy (MILESTONES.md / PROJECT.md) | mitigate | Corrigendum states plainly the adapter + `--with-mailglass` flag did not land and are unsupported; automated verify greps for the truthful phrasing and the recipe-only posture in both files. |
| T-136-05 | Tampering | CHANGELOG.md integrity (duplicate header) | mitigate | Remove the stray `## [Unreleased]` while preserving its bullet content; verify asserts exactly one header AND that the historical `.formatter.exs` / `Human GA` content still grep-matches. |
| T-136-06 | Spoofing | marketing voice creeping into honesty docs | mitigate | Negative grep for the four banned phrases across all three edited files. |
| T-136-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase; it edits `.md` files only. No new attack surface, so no install legitimacy checkpoint is warranted. |
</threat_model>

<verification>
Overall phase checks (Plan 02):

1. MILESTONES.md corrigendum present + truthful + no banned phrases: `grep -i "did not land\|not part of the supported surface" .planning/MILESTONES.md` and `! grep -iE "seamlessly|just works|production-ready out of the box|the recommended way" .planning/MILESTONES.md`
2. PROJECT.md correction agrees: `grep -i "Sigra.Mailer\|host-owned\|did not land" .planning/PROJECT.md`
3. CHANGELOG single Unreleased header: `grep -c "^## \[Unreleased\]" CHANGELOG.md` == 1
4. CHANGELOG note references recipe: `grep "companion-libs/mailglass" CHANGELOG.md`
5. CHANGELOG history preserved: `grep "formatter.exs" CHANGELOG.md && grep "Human GA" CHANGELOG.md`
</verification>

<success_criteria>
- MILESTONES.md and PROJECT.md both carry the truthful Mailglass corrigendum (adapter + `--with-mailglass` flag did not land, unsupported; recipe-only host-owned posture), covering all three v1.25 overclaim lines.
- CHANGELOG.md [Unreleased] notes the v1.29 host-owned-wiring posture referencing the mailglass recipe, and has exactly one `## [Unreleased]` header with no historical content lost.
- No banned marketing phrases appear in any edited file.
- Scope respected: only the v1.25 Mailglass narrative is corrected; the Threadline forwarder is not touched; the orphaned-adapter recover-vs-delete decision is not reopened.
</success_criteria>

<output>
After completion, create `.planning/phases/136-verification-proof-bundle-narrative-honesty-corrigendum/136-02-SUMMARY.md`.
</output>
