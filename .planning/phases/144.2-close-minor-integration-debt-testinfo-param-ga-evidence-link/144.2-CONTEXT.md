# Phase 144.2: Close Minor Integration Debt - Context

**Gathered:** 2026-05-30
**Status:** Ready for planning

<domain>
## Phase Boundary

Two discrete cleanup items deferred from the v1.31 DEMO-SHOWCASE phase group. Zero functional or behavioral changes — pure TypeScript convention fix and ExDoc link repair:

1. **testInfo rename** — Rename unused `testInfo: TestInfo` param to `_testInfo: TestInfo` in `assertDemoScreenshot()` in `demo-showcase.spec.ts:51`. The function body never references it; the `_` prefix is the TypeScript convention for intentionally unused parameters.

2. **ga-evidence.md ExDoc link** — Replace the relative `../.planning/` link at the bottom of `docs/ga-evidence.md` with an absolute GitHub URL. Remove `docs/ga-evidence.md` from `skip_undefined_reference_warnings_on` in `mix.exs` (the suppression was the workaround; the URL fix is the real fix).

**In scope:** `test/example/priv/playwright/tests/demo-showcase.spec.ts`, `docs/ga-evidence.md`, `mix.exs` (docs config only).

**Out of scope:** Any changes to `lib/sigra/`, LiveView code, seeds, Playwright logic, or other docs files.
</domain>

<decisions>
## Implementation Decisions

### testInfo param rename

- **D-01: Rename `testInfo` to `_testInfo`** in the `assertDemoScreenshot()` function signature at `demo-showcase.spec.ts:51`. This is a pure mechanical fix — the TypeScript convention for intentionally unused parameters. No ESLint suppression comment needed; the `_` prefix satisfies the `@typescript-eslint/no-unused-vars` rule natively.

### ga-evidence.md ExDoc link

- **D-02: Replace with absolute GitHub URL** — Change:
  ```
  - [v1.31 DEMO-SHOWCASE proof bundle (planning-internal)](../.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md)
  ```
  to:
  ```
  - [v1.31 DEMO-SHOWCASE proof bundle (planning-internal)](https://github.com/szTheory/sigra/blob/main/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md)
  ```
  This matches the established pattern in `ga-evidence.md` — the v1.4 GA matrix and requirements links above it already use `https://github.com/sztheory/sigra/blob/v0.2.1/.planning/...`. ExDoc treats absolute URLs as external links (no resolution attempted).

- **D-03: Remove suppression entry** — After fixing the link, remove `"docs/ga-evidence.md"` from `skip_undefined_reference_warnings_on` in `mix.exs` along with its `# Phase 144:` comment. The suppression was the workaround; once the link is an absolute URL, the warning no longer fires.

### Claude's Discretion

- URL casing: use `szTheory` (capital T) to match the existing absolute links already in `ga-evidence.md`.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Files to change

- `test/example/priv/playwright/tests/demo-showcase.spec.ts` — the `assertDemoScreenshot` function (lines ~49–60); `testInfo: TestInfo` param on line 51 is the target
- `docs/ga-evidence.md` — last bullet in "Where to read next" section is the broken link
- `mix.exs` — `skip_undefined_reference_warnings_on` list in the `defp docs do` block (around line 165); `"docs/ga-evidence.md"` entry + its comment above it must be removed

### Prior phase context

- `.planning/phases/144-readme-evaluator-lane-docs-proof/144-CONTEXT.md` — original Phase 144 decisions; DOC-03 added the VERIFICATION.md pointer to ga-evidence.md
- `.planning/phases/144.1-address-tech-debt-validation-md-finalization-dave-credential/144.1-01-SUMMARY.md` — Phase 144.1 touched both files but left these two items open

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `assertDemoScreenshot` function (`demo-showcase.spec.ts:49–60`) — the function receiving the rename; callers on lines 115, 132, 148, 160 pass `testInfo` which becomes irrelevant to the function signature after the rename (callers stay unchanged)

### Established Patterns

- **Absolute GitHub URL pattern** — `docs/ga-evidence.md` already links to `.planning/` artifacts via `https://github.com/sztheory/sigra/blob/v0.2.1/...`; the new link follows this same pattern with `blob/main/` instead of a version tag
- **`_` prefix convention** — TypeScript unused-param convention; no other suppressions or workarounds required

### Integration Points

- `mix docs --warnings-as-errors` is the verification gate — once `docs/ga-evidence.md` is removed from `skip_undefined_reference_warnings_on` and the link is absolute, the docs build must pass cleanly

</code_context>

<specifics>
## Specific Ideas

- GitHub URL to use: `https://github.com/szTheory/sigra/blob/main/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md`
- Verify `mix docs --warnings-as-errors` passes after both changes

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

### Reviewed Todos (not folded)

The following todos surfaced in cross-reference scan but are out of scope for this phase:

- `2026-05-29-phase-138-doctor-info-findings.md` — Phase 138 Info findings (IN-01/IN-02/IN-03); deferred from v1.30, low-priority maintainability; not related to this phase's scope
- `2026-05-29-deprecation-since-vs-removal-version-axis.md` — @doc since vs removal-version axis; deferred by design at v1.30 close
- `2026-05-28-phase-135-review-deferred-findings.md` — Phase 135 deferred findings (Threadline + upstream); out of scope

</deferred>

---

*Phase: 144.2-close-minor-integration-debt-testinfo-param-ga-evidence-link*
*Context gathered: 2026-05-30*
