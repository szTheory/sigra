# Phase 144.2: Close Minor Integration Debt - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-30
**Phase:** 144.2-close-minor-integration-debt-testinfo-param-ga-evidence-link
**Areas discussed:** ga-evidence link fix

---

## testInfo param rename

Treated as mechanical — no gray area presented. The `assertDemoScreenshot()` function in `demo-showcase.spec.ts:51` declares `testInfo: TestInfo` but never uses it in the function body. TypeScript convention: `_testInfo`. Auto-decided; not surfaced as a gray area.

---

## ga-evidence link fix

| Option | Description | Selected |
|--------|-------------|----------|
| Replace with GitHub absolute URL | Change `../.planning/...` to `https://github.com/szTheory/sigra/blob/main/.planning/...`; matches existing v1.4 planning-artifact pattern in same file; ExDoc treats absolute URLs as external | ✓ |
| Remove the bullet entirely | Drop the planning-internal link from the public docs page; planning-internal content arguably doesn't belong in shipped docs | |

**User's choice:** Replace with GitHub absolute URL
**Notes:** After the link is replaced, `docs/ga-evidence.md` should also be removed from `skip_undefined_reference_warnings_on` in `mix.exs` along with its `# Phase 144:` comment. The suppression was the workaround; the absolute URL fix eliminates the need for it.

---

## Claude's Discretion

- URL casing: `szTheory` (capital T) to match the existing absolute links already in `ga-evidence.md`

## Deferred Ideas

None — discussion stayed within phase scope.
