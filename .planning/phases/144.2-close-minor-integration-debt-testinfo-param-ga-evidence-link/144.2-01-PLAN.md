---
phase: "144.2"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/demo-showcase.spec.ts
  - docs/ga-evidence.md
  - mix.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "demo-showcase.spec.ts assertDemoScreenshot() carries no unused-parameter lint signal"
    - "docs/ga-evidence.md VERIFICATION.md link is an absolute GitHub URL resolvable by any browser"
    - "mix docs --warnings-as-errors builds cleanly without ga-evidence.md suppression"
  artifacts:
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts"
      provides: "convention-correct assertDemoScreenshot function signature"
      contains: "_testInfo: TestInfo"
    - path: "docs/ga-evidence.md"
      provides: "absolute GitHub URL to 144-VERIFICATION.md"
      contains: "https://github.com/szTheory/sigra/blob/main/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md"
    - path: "mix.exs"
      provides: "skip_undefined_reference_warnings_on without ga-evidence.md entry"
  key_links:
    - from: "docs/ga-evidence.md"
      to: ".planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md"
      via: "absolute GitHub URL (ExDoc external link — no resolution attempted)"
      pattern: 'github\.com/szTheory/sigra/blob/main/\.planning'
    - from: "mix.exs skip_undefined_reference_warnings_on"
      to: "docs/ga-evidence.md"
      via: "entry removed — no longer suppressed"
      pattern: 'ga-evidence\.md.*skip'
---

<objective>
Two mechanical cleanup items deferred from v1.31 DEMO-SHOWCASE:

1. Rename `testInfo` to `_testInfo` in `assertDemoScreenshot()` — TypeScript convention for intentionally unused parameters (D-01).
2. Replace the broken relative `../.planning/` link in `docs/ga-evidence.md` with an absolute GitHub URL, then remove the `skip_undefined_reference_warnings_on` workaround from `mix.exs` (D-02, D-03).

Purpose: Restore full ExDoc warning coverage and eliminate the unused-parameter style signal.
Output: Three files edited; `mix docs --warnings-as-errors` passes cleanly with no suppression for ga-evidence.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename testInfo to _testInfo in assertDemoScreenshot (D-01)</name>
  <files>test/example/priv/playwright/tests/demo-showcase.spec.ts</files>

  <read_first>
    - test/example/priv/playwright/tests/demo-showcase.spec.ts — read lines 49–60 to see the current assertDemoScreenshot function signature before editing; confirm line 51 reads `testInfo: TestInfo,`
  </read_first>

  <action>
    Edit line 51 of demo-showcase.spec.ts: change `testInfo: TestInfo,` to `_testInfo: TestInfo,`.

    This is a one-token change in the assertDemoScreenshot function signature. The function body (lines 54–59) does not reference testInfo at all — it only uses page and slug. Do NOT touch:
    - Line 50 (page: Page)
    - Line 52 (slug: string)
    - Lines 54–60 (function body)
    - Lines 102, 115, 132, 148, 160 (call sites — the `testInfo` variable there is the outer test() callback param, unrelated to the function signature)

    No ESLint suppression comment is needed — the `_` prefix satisfies @typescript-eslint/no-unused-vars natively.
  </action>

  <verify>
    <automated>grep -n '_testInfo: TestInfo' /Users/jon/projects/sigra/test/example/priv/playwright/tests/demo-showcase.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - demo-showcase.spec.ts line 51 contains `_testInfo: TestInfo,` (with leading underscore)
    - demo-showcase.spec.ts does NOT contain `  testInfo: TestInfo,` (without underscore) anywhere in the function signature block (lines 49–53)
    - Lines 102, 115, 132, 148, 160 are unchanged — they still pass `testInfo` from the outer test callback
  </acceptance_criteria>

  <done>assertDemoScreenshot signature reads `_testInfo: TestInfo` on line 51; no other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace broken ga-evidence.md relative link with absolute GitHub URL + remove mix.exs suppression (D-02, D-03)</name>
  <files>docs/ga-evidence.md, mix.exs</files>

  <read_first>
    - docs/ga-evidence.md — read the full file to confirm line 19 is the broken relative link: `[v1.31 DEMO-SHOWCASE proof bundle (planning-internal)](../.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md)`
    - mix.exs — read lines 165–182 to confirm the exact three-line block to remove: the `# Phase 144:` two-line comment (lines 179–180) and the `"docs/ga-evidence.md"` entry (line 181); also confirm line 220 (`"docs/ga-evidence.md"` in extras:) must NOT be touched
  </read_first>

  <action>
    Change 1 — docs/ga-evidence.md line 19:

    Replace the last bullet in the "Where to read next" section. The current text is:
      `- [v1.31 DEMO-SHOWCASE proof bundle (planning-internal)](../.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md)`

    Replace with:
      `- [v1.31 DEMO-SHOWCASE proof bundle (planning-internal)](https://github.com/szTheory/sigra/blob/main/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md)`

    URL is `https://github.com/szTheory/sigra/blob/main/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md` — note capital T in szTheory (per D-02 / Claude's Discretion), and `blob/main/` not a version tag because VERIFICATION.md is a planning artifact not versioned with releases.

    Change 2 — mix.exs skip_undefined_reference_warnings_on block:

    Remove the following three lines entirely from the skip_undefined_reference_warnings_on list (currently lines 179–181):
      `        # Phase 144: ga-evidence.md links to a planning-internal VERIFICATION.md at a`
      `        # ../.planning/ relative path that ExDoc cannot resolve as a docs page.`
      `        "docs/ga-evidence.md"`

    After removal the list closes at `"guides/recipes/companion-libs/mailglass.md"`. No trailing comma adjustment is needed — mailglass.md was already the second-to-last entry and its trailing comma stays.

    CRITICAL: Do NOT touch the `"docs/ga-evidence.md"` entry in the `extras:` list (around line 220). That entry renders the page in docs and must remain.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/sigra && mix docs --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - docs/ga-evidence.md line 19 contains `https://github.com/szTheory/sigra/blob/main/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md`
    - docs/ga-evidence.md does NOT contain `../.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md`
    - mix.exs skip_undefined_reference_warnings_on list does NOT contain `"docs/ga-evidence.md"`
    - mix.exs skip_undefined_reference_warnings_on list does NOT contain the `# Phase 144:` comment block
    - mix.exs extras: list still contains `"docs/ga-evidence.md"` at approximately line 220 (the extras entry is unchanged)
    - `mix docs --warnings-as-errors` exits 0 with no undefined reference warnings
  </acceptance_criteria>

  <done>ga-evidence.md last bullet is an absolute GitHub URL; mix.exs suppression entry and its comment are removed; `mix docs --warnings-as-errors` passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | All changes are TypeScript style and docs link repair — no auth logic, no user input, no external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.2-01 | Tampering | mix.exs extras: list | accept | Risk of accidentally removing the extras: entry instead of skip_undefined_reference_warnings_on entry; mitigated by explicit read_first + acceptance_criteria check that extras: entry remains |
</threat_model>

<verification>
Phase gate (run after both tasks):

```bash
cd /Users/jon/projects/sigra
mix docs --warnings-as-errors
mix test
```

Expected: Both exit 0. No undefined reference warnings. No regressions.

Spot checks:
- `grep '_testInfo: TestInfo' test/example/priv/playwright/tests/demo-showcase.spec.ts` — returns line 51
- `grep -c 'szTheory/sigra/blob/main' docs/ga-evidence.md` — returns 1
- `grep -c '"docs/ga-evidence.md"' mix.exs` — returns 1 (the extras: entry only; the suppression entry is gone)
</verification>

<success_criteria>
1. demo-showcase.spec.ts assertDemoScreenshot function signature reads `_testInfo: TestInfo` on line 51
2. docs/ga-evidence.md last bullet links to `https://github.com/szTheory/sigra/blob/main/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md`
3. mix.exs skip_undefined_reference_warnings_on no longer contains `"docs/ga-evidence.md"` or its `# Phase 144:` comment
4. mix.exs extras: list still contains `"docs/ga-evidence.md"` (rendered docs page intact)
5. `mix docs --warnings-as-errors` passes cleanly
6. `mix test` passes with no regressions
</success_criteria>

<output>
Create `/Users/jon/projects/sigra/.planning/phases/144.2-close-minor-integration-debt-testinfo-param-ga-evidence-link/144.2-01-SUMMARY.md` when done.
</output>
