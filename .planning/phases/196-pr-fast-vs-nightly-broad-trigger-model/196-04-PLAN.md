---
phase: 196-pr-fast-vs-nightly-broad-trigger-model
plan: 04
type: execute
wave: 3
depends_on: [196-03]
files_modified:
  - MAINTAINING.md
  - guides/recipes/local-development.md
  - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-VERIFICATION.md
autonomous: true
requirements: [CRIT-02, CRIT-03]
must_haves:
  truths:
    - "MAINTAINING.md documents the new nightly cadence: the schedule: cron, and which coverage runs nightly/main/dispatch vs every PR (the 5 moved jobs vs the PR-fast gate)"
    - "MAINTAINING.md records the two honest-truth residuals (D-07): upgrade_smoke whole-path is nightly/main-only on PRs, and generated-host template parity becomes nightly-only — explicitly backstopped by DIST-06 scripts/ci/admin-acceptance-smoke.sh — never silently moved"
    - "MAINTAINING.md documents the forced-failure probe runbook: `gh workflow run \"CI\" -f force_fail_probe=true` reds the nightly probe, default-false stays green (D-14)"
    - "The already-correct required-check section (5 lane names; ci-gate NOT required) is NOT rewritten — docs are ADD-only (D-13 / anti-pattern guard)"
    - "phase_51 test 51-02 stays green — the existing MAINTAINING.md strings it asserts (v1.4-GA-UAT.md, 50-VERIFICATION.md, install_golden_contract, mix ci.install_golden) are preserved"
    - "196-VERIFICATION.md records the D-13 correction: CRIT-03's 'single stable required check (ci-gate)' phrasing is stale; the enforced surface is the 5 lane names — recorded here, NOT by editing ROADMAP/REQUIREMENTS prose"
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Nightly cadence + two residuals + forced-failure probe runbook (ADD-only; required-check section untouched)"
      contains: "force_fail_probe"
  key_links:
    - from: "MAINTAINING.md nightly/probe docs"
      to: "ci.yml schedule cron + nightly_probe job + DIST-06 admin-acceptance-smoke.sh"
      via: "operator runbook + residual disclosure"
      pattern: "force_fail_probe"
---

<objective>
Document the new trigger model honestly in `MAINTAINING.md` (and a one-line note in `guides/recipes/local-development.md`): the nightly cadence, the two accepted nightly-only residuals (D-07), and the forced-failure probe runbook (D-14) — all as ADD-only subsections that do NOT touch the already-correct required-check section. Record the D-13 stale-CRIT-03-framing correction in `196-VERIFICATION.md` (the enforced surface is the 5 lane names; `ci-gate` is an internal aggregator) rather than editing ROADMAP/REQUIREMENTS prose.

Purpose: CRIT-02 (the never-strand honest-truth requirement — the two residuals MUST be written explicitly, never silently moved) and CRIT-03 (the reconciled required-check reality is documented; no doc churn to the already-correct section).

Output: MAINTAINING.md with a nightly-cadence subsection, two residual disclosures, and a probe runbook; a one-line nightly mention in the local-development guide; the D-13 correction recorded in 196-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-CONTEXT.md
@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-RESEARCH.md
@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-VALIDATION.md
@MAINTAINING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nightly cadence + two residuals + probe runbook to MAINTAINING.md (D-07/D-13/D-14)</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md (the "Branch protection — enforced required checks (live ruleset)" section ~95-122 — ALREADY CORRECT, do NOT rewrite; find a sensible adjacent placement for the new CI-cadence subsection; the install-golden waiver section holds the strings 51-02 asserts)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-RESEARCH.md §7 (MAINTAINING.md already correct — ADD only) + Validation Architecture (the two residuals)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-VALIDATION.md (Never-Strand Contract table — the residuals to disclose)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-03-SUMMARY.md (the exact probe job name + `gh workflow run` invocation from Plan 03)
  </read_first>
  <action>ADD (do NOT rewrite the already-correct required-check list) a new CI-cadence subsection to MAINTAINING.md covering three things. (1) Nightly cadence: the new `schedule:` cron and the PR-fast vs nightly/main-broad split — name which jobs run only on schedule/push:main/workflow_dispatch (the 5 moved: install_matrix ×4, upgrade_smoke, passkeys_manual_fallback_smoke, passkeys_opt_out_smoke, generated_admin_playwright_smoke) vs the fast representative PR gate (the 5 required lanes + install_golden_contract + library_tests_dep_off). (2) The two honest-truth residuals from the Never-Strand Contract (D-07): (a) upgrade_smoke has no per-PR behavioral proxy — its whole published->local upgrade path is nightly/main/release-dispatch-only and that is accepted as release-boundary coverage; (b) generated-host template parity (installer-emitted shell vs library admin) becomes nightly-only because generated_admin_playwright_smoke is fully moved — explicitly backstopped by the DIST-06 acceptance-smoke automation scripts/ci/admin-acceptance-smoke.sh (RUN_PARITY). Frame these as deliberate, disclosed tradeoffs, not omissions. (3) The forced-failure probe runbook (D-14): document that `gh workflow run "CI" -f force_fail_probe=true` reds the nightly probe job (proving the nightly lane reports failure), and that the default force_fail_probe=false keeps normal runs green. CRITICAL — preserve every existing string in MAINTAINING.md that the phase_51 51-02 test asserts: v1.4-GA-UAT.md, 50-VERIFICATION.md, install_golden_contract, mix ci.install_golden (these live in the install-golden waiver section; do not delete or relocate them out of existence). Do NOT "fix" the required-check list — it is already correct (the 5 names + ci-gate-not-required framing); only ADD the cadence/residual/probe content.</action>
  <verify>
    <automated>grep -qi 'force_fail_probe' MAINTAINING.md && grep -qi 'admin-acceptance-smoke' MAINTAINING.md && grep -qi 'upgrade_smoke' MAINTAINING.md && grep -q 'v1.4-GA-UAT.md' MAINTAINING.md && grep -q 'mix ci.install_golden' MAINTAINING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>MAINTAINING.md gains a CI-cadence subsection documenting the nightly schedule, the PR-fast vs nightly split, the two D-07 residuals (upgrade_smoke whole-path; generated-host template parity backstopped by admin-acceptance-smoke.sh), and the `gh workflow run "CI" -f force_fail_probe=true` probe runbook. The already-correct required-check section is unchanged. All four strings 51-02 asserts remain present.</acceptance_criteria>
  <done>The nightly cadence, two residuals, and probe runbook are documented ADD-only; the required-check section and the 51-02-asserted strings are intact.</done>
</task>

<task type="auto">
  <name>Task 2: One-line nightly mention in the local-development guide + record D-13 correction in VERIFICATION</name>
  <files>guides/recipes/local-development.md, .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-VERIFICATION.md</files>
  <read_first>
    - guides/recipes/local-development.md (find the CI/test-cadence area for a single contextual sentence; Claude's discretion on placement)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-RESEARCH.md §7 + Open Question 2 (record correction in VERIFICATION, do NOT edit ROADMAP/REQUIREMENTS prose)
  </read_first>
  <action>Two doc touches. (a) Add a single contextual sentence to guides/recipes/local-development.md noting that exhaustive/broad CI coverage (install matrix, upgrade smoke, generated-admin Playwright) now runs nightly/main, not on every PR — so a clean local `mix test` plus the PR-fast gate is the fast loop, and the broad set lands on schedule/main. Keep it to one line; this is a convenience pointer, not a rewrite. (b) Record the D-13 correction in 196-VERIFICATION.md: state explicitly that CRIT-03 / ROADMAP's phrasing "single stable required check (ci-gate aggregator)" is STALE — the live ruleset (14941512) enforces the 5 lane name: strings and ci-gate is an internal aggregator, NOT a required check (per 194-D01/D15; MAINTAINING.md already documents this correctly). Note that this correction is recorded here in VERIFICATION rather than by editing REQUIREMENTS.md/ROADMAP prose (RESEARCH §7 / Open Q2). If 196-VERIFICATION.md does not yet exist, create it with a short header and this reconciliation note; if it exists, append the note.</action>
  <verify>
    <automated>grep -qi 'nightly\|schedule' guides/recipes/local-development.md && grep -qi 'ci-gate' .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>local-development.md carries a one-line nightly/broad-cadence pointer; 196-VERIFICATION.md records the D-13 correction (5 lane names enforced; ci-gate not required; CRIT-03 phrasing stale) without any edit to REQUIREMENTS.md or ROADMAP prose.</acceptance_criteria>
  <done>The local-dev guide notes the nightly cadence in one line; the stale-CRIT-03 correction is captured in VERIFICATION, not perpetuated in source-of-truth prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator action | MAINTAINING.md is the operator runbook; an inaccurate or silently-incomplete nightly/residual disclosure would mislead a maintainer about what coverage actually runs on PRs. The honest-truth residual disclosure (D-07) is the integrity control here. |
| Required-check docs → enforcement reality | The already-correct required-check section maps the 5 lane names to ruleset 14941512; rewriting it risks introducing drift from the live ground truth — hence ADD-only discipline. |

## STRIDE Threat Register (ASVS L1, block-on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-04-01 | Repudiation | silent move of coverage to nightly | mitigate | The two residuals (upgrade_smoke whole-path; generated-host template parity) are written EXPLICITLY into MAINTAINING.md + 196-VERIFICATION (D-07 honest-truth requirement), with the DIST-06 backstop named — no coverage is silently dropped. |
| T-196-04-02 | Tampering | accidental rewrite of the correct required-check list | mitigate | ADD-only discipline (RESEARCH §7); the 5-lane / ci-gate-not-required section is left byte-stable; the 51-02-asserted strings are preserved (verified by grep). |
| T-196-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs introduced this plan (edits Markdown only); package legitimacy gate not applicable. |
</threat_model>

<verification>
- `grep -i force_fail_probe MAINTAINING.md` shows the probe runbook; `grep -i admin-acceptance-smoke MAINTAINING.md` shows the DIST-06 backstop; `grep -i upgrade_smoke MAINTAINING.md` shows the residual disclosure.
- The four 51-02-asserted strings (`v1.4-GA-UAT.md`, `50-VERIFICATION.md`, `install_golden_contract`, `mix ci.install_golden`) remain in MAINTAINING.md → `mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` stays green (test 51-02).
- guides/recipes/local-development.md has a one-line nightly/broad-cadence pointer.
- 196-VERIFICATION.md records the D-13 stale-CRIT-03 correction; REQUIREMENTS.md/ROADMAP prose unchanged.
- The required-check section in MAINTAINING.md is unchanged (ADD-only).
</verification>

<success_criteria>
- MAINTAINING.md: nightly cadence + two D-07 residuals (with DIST-06 backstop named) + force_fail_probe runbook, ADD-only; required-check section and 51-02 strings intact.
- local-development.md: one-line nightly/broad-cadence note.
- 196-VERIFICATION.md: D-13 correction recorded; no ROADMAP/REQUIREMENTS prose edits.
</success_criteria>

<output>
Create `.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-04-SUMMARY.md` when done. Confirm phase_51 (incl. 51-02) stays green after the MAINTAINING.md edits.
</output>
