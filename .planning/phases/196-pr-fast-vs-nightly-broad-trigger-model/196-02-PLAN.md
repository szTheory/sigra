---
phase: 196-pr-fast-vs-nightly-broad-trigger-model
plan: 02
type: execute
wave: 2
depends_on: [196-01]
files_modified:
  - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
autonomous: true
requirements: [CRIT-03]
must_haves:
  truths:
    - "phase_51_install_golden_ci_contract_test.exs passes via `mix test` — its previously-RED assertion (the removed installer_milestone_audit: job key) is re-anchored to the surviving fast_checks step (the 'Installer milestone audit' step name and/or scripts/ci/installer-milestone-audit.sh run command)"
    - "The path-detector ×2 assertion (test 51-01) and the install_golden_contract: assertion stay intact and green — only the stale job-key assertion is re-anchored"
    - "phase_58_oauth_oa01_ci_contract_test.exs stays green after the Plan-01 ci.yml edit (the library_tests_shard: -> library_tests: slicer is undisturbed by the job-level if: gates)"
    - "The test does not pass vacuously — the re-anchored assertion targets a string that genuinely exists in the live (Plan-01-edited) ci.yml fast_checks block"
  artifacts:
    - path: "test/sigra/planning/phase_51_install_golden_ci_contract_test.exs"
      provides: "Re-anchored installer-milestone CI contract test (green against the post-194 fast_checks fold)"
      contains: "installer-milestone-audit"
  key_links:
    - from: "phase_51 test assertion (line ~28)"
      to: "ci.yml fast_checks step 'Installer milestone audit' / scripts/ci/installer-milestone-audit.sh"
      via: "assert yml =~ surviving-step-anchor"
      pattern: "installer-milestone-audit"
---

<objective>
Fold the standing CI-contract todo (D-15): re-anchor `phase_51_install_golden_ci_contract_test.exs` from the removed `installer_milestone_audit:` **job** key (Phase 194 folded that job into the `fast_checks` job as a step) to a SURVIVING anchor in the `fast_checks` block — the step name `Installer milestone audit` and/or the run command `scripts/ci/installer-milestone-audit.sh`. Then re-verify (D-16) that `phase_58_oauth_oa01_ci_contract_test.exs` stays green after Plan 01's ci.yml edits.

Purpose: CRIT-03 — these two planning contract tests assert against `ci.yml` structure and are part of `mix test`; they gate this phase's own merge. `phase_51` is **already RED on main** (BLOCKING for merge per D-15) and MUST end green. `phase_58` must be confirmed (not assumed) green after the trigger-model restructure.

Output: an edited `phase_51` test that is green and honest (assertion + moduledoc/test-name wording reconciled to the fast_checks fold); a verified-green `phase_58`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-CONTEXT.md
@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-PATTERNS.md
@test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
@test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor phase_51 to the surviving fast_checks step (D-15)</name>
  <files>test/sigra/planning/phase_51_install_golden_ci_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs (moduledoc lines 2-6; test 51-01 lines 20-29; the RED assertion at line ~28; re-grep live)
    - .github/workflows/ci.yml (fast_checks block: the `Installer milestone audit (INT-01..03)` step name and its `bash scripts/ci/installer-milestone-audit.sh` run command — re-grep live; Phase 194 folded the standalone job here)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-PATTERNS.md §7 (exact re-anchor idiom and which lines to keep intact)
  </read_first>
  <action>Change ONLY the stale assertion in test 51-01 that asserts the removed `installer_milestone_audit:` job key (currently `assert yml =~ "installer_milestone_audit:"` at line ~28 — re-grep, prior commits shift it). Re-anchor it, using the existing `assert yml =~ "<string>"` idiom in the same file, to a SURVIVING string in the live fast_checks block: the step name `Installer milestone audit` and/or the run command `scripts/ci/installer-milestone-audit.sh`. Prefer anchoring on `scripts/ci/installer-milestone-audit.sh` (the most stable, least-likely-to-be-reworded anchor) — optionally also keep a step-name assertion for breadth. KEEP the path-detector ×2 assertion (the `Regex.scan … == 2` check) and the `assert yml =~ "install_golden_contract:"` assertion BYTE-IDENTICAL — only the removed-job-key assertion is RED (the other two stay green and must not be weakened or made vacuous). For honesty (D-15): also update the module `@moduledoc` (lines 2-6) and the test name on line ~20 — both still reference the `installer_milestone_audit` *job* — to describe the fast_checks *step* reality. Do NOT introduce a fenced literal of any string the test negative-greps for; this test only uses positive `=~`/`Regex.scan` asserts, so there is no negative-gate hazard here.</action>
  <verify>
    <automated>mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>`mix test` on `phase_51_install_golden_ci_contract_test.exs` is fully green (2 tests, 0 failures). The re-anchored assertion targets `scripts/ci/installer-milestone-audit.sh` and/or the `Installer milestone audit` step name — both present in the live (Plan-01-edited) ci.yml fast_checks block, so the test is NOT vacuous. The path-detector ×2 and `install_golden_contract:` assertions are unchanged and still pass. The moduledoc/test-name no longer claim a standalone `installer_milestone_audit` job exists.</acceptance_criteria>
  <done>phase_51 is green and its wording reflects the Phase-194 fold (job → fast_checks step); no other assertion in the file was weakened.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify phase_58 slicer stays green after the ci.yml edit (D-16)</name>
  <files>(read-only verify; no file written unless the slicer genuinely broke)</files>
  <read_first>
    - test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs (slicer `library_tests_shard:` -> `\n  library_tests:` lines ~27-38; assertions ~44-48)
    - .github/workflows/ci.yml (confirm no moved job landed between `library_tests_shard:` and `library_tests:`; the 5 moved jobs are at 502/554/604/733/1156, far from the 176→279 shard→aggregator boundary)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-PATTERNS.md §8
  </read_first>
  <action>Run `mix test test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` and confirm it is green. D-02's job-level `if:` gates (Plan 01) do NOT rename or reorder `library_tests_shard`/`library_tests`, and none of the 5 moved jobs sits between that boundary, so the workflow slicer should still isolate the shard-worker body and find `Run library tests` + `mix test --partitions 2` while still refuting any `--exclude.*oauth`. This is a VERIFY task, not an edit task. ONLY if the slicer is unexpectedly red (e.g. Plan 01 inadvertently inserted a moved job between the two job keys), surface the regression and reconcile by confirming the move jobs stayed in their original 502–1156 positions — do not patch the test to mask a real reordering.</action>
  <verify>
    <automated>mix test test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>`mix test` on `phase_58_oauth_oa01_ci_contract_test.exs` is green with no edit required. The shard→aggregator boundary (`library_tests_shard:` then `library_tests:`) is intact, with no moved job inserted between them.</acceptance_criteria>
  <done>phase_58 confirmed green against the Plan-01-edited ci.yml; the slicer boundary is undisturbed.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm both contract tests green together via the quick command</name>
  <files>(read-only verify)</files>
  <read_first>
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-VALIDATION.md (quick run command + sampling rate)
  </read_first>
  <action>Run the phase quick command — both planning contract tests in one invocation — to confirm they pass together after the re-anchor and the slicer re-verify. This is the per-commit sampling gate from 196-VALIDATION.md. Local Postgres is not required for these two tests (they read ci.yml/MAINTAINING.md text only); per the project DB prereqs, any Postgres-connection noise in the log is from OTHER async tests and does not affect these structural assertions — scope the run to just the two files so the signal is clean.</action>
  <verify>
    <automated>mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>Both planning contract tests pass in a single `mix test` invocation (0 failures). This is the executable per-commit gate for the contract layer of the phase.</acceptance_criteria>
  <done>The two contract tests are green together; the D-15 fold is complete and D-16 is confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow YAML → planning contract tests | The `phase_51`/`phase_58` ExUnit tests assert structural invariants of `ci.yml`; they are the self-verification boundary that catches silent drift between the workflow and its documented contract. A vacuously-passing test would erode this boundary. |

## STRIDE Threat Register (ASVS L1, block-on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-02-01 | Tampering | the re-anchored phase_51 assertion | mitigate | Anchor on a string that genuinely exists in the live ci.yml (`scripts/ci/installer-milestone-audit.sh`) so the contract test cannot pass vacuously; keep the two unrelated green assertions byte-identical. |
| T-196-02-02 | Repudiation | stale moduledoc/test-name wording | mitigate | Update the moduledoc + test name to the fast_checks-step reality (D-15 honesty requirement) so the contract's intent is not misrepresented. |
| T-196-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs introduced this plan (edits one `.exs` test); package legitimacy gate not applicable. |
</threat_model>

<verification>
- `mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` → 2 tests, 0 failures (the line-28 RED is fixed; the path-detector ×2 and install_golden_contract: assertions still pass).
- `mix test test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` → green, no edit required.
- The phase quick command (both files together) → 0 failures.
- The re-anchored assertion targets a string present in the live ci.yml fast_checks block (not vacuous); moduledoc/test-name reconciled to the fold.
</verification>

<success_criteria>
- phase_51 re-anchored to the surviving fast_checks step and green; path-detector ×2 + install_golden_contract: assertions intact.
- phase_58 confirmed green (slicer boundary undisturbed by the move).
- Both contract tests pass together via the quick command — the phase's contract layer gate is satisfied.
</success_criteria>

<output>
Create `.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-02-SUMMARY.md` when done. Record the before/after assertion (removed job key → surviving step anchor) and the green `mix test` results for both contract files.
</output>
