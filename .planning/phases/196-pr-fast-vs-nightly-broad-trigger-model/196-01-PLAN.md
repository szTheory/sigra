---
phase: 196-pr-fast-vs-nightly-broad-trigger-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CRIT-02, CRIT-03]
must_haves:
  truths:
    - "On a pull_request event, install_matrix / upgrade_smoke / passkeys_manual_fallback_smoke / passkeys_opt_out_smoke / generated_admin_playwright_smoke are skipped (gated job-level if), while the 5 ruleset-required lanes still run unconditionally"
    - "On schedule / push:main / workflow_dispatch events all 5 moved jobs run (gate is != 'pull_request', not == 'schedule')"
    - "A new schedule: cron trigger exists in ci.yml that does NOT collide with the 45 6 slot used by playwright-github-pages.yml"
    - "ci-gate tolerates result == 'skipped' on its needs (upgrade_smoke + generated_admin_playwright_smoke skip on PRs) and stays red on a real failure/cancelled"
    - "The 5 ruleset-required lane name: strings are byte-identical to the live ruleset (re-read at execution) and carry NO event_name gate"
    - "install_golden_contract and library_tests_dep_off remain on every PR (no job-level if: added)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "PR-fast vs nightly-broad trigger model (schedule cron + 5 moved-job gates + skip-tolerant ci-gate)"
      contains: "schedule:"
  key_links:
    - from: "ci.yml on: block"
      to: "5 moved job headers"
      via: "job-level if: github.event_name != 'pull_request'"
      pattern: "github.event_name != 'pull_request'"
    - from: "ci-gate result loop"
      to: "needs.upgrade_smoke / needs.generated_admin_playwright_smoke (skipped on PR)"
      via: "skip-tolerant condition"
      pattern: "result.*!=.*skipped"
---

<objective>
Restructure `.github/workflows/ci.yml` into a PR-fast vs nightly-broad trigger model in a single atomic edit: add a `schedule:` cron trigger, gate the 5 exhaustive/low-probability jobs off the PR path with one job-level `if:` line each, and make the `ci-gate` aggregator tolerate the `skipped` results that the move produces. The 5 ruleset-required lane names stay byte-identical and unconditional.

Purpose: CRIT-02 (move exhaustive coverage off every PR while never stranding correctness-critical coverage) and CRIT-03 (preserve the stable required-check surface; no path/skip pending-check trap). Bundling the move (D-02/D-05) and the ci-gate skip-tolerance (D-09) in one plan keeps the ci.yml edit causally coherent — D-09 exists only because D-02 produces `skipped` needs.

Output: a single edited ci.yml; the live ruleset re-read recorded as ground-truth evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-CONTEXT.md
@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-RESEARCH.md
@.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/playwright-github-pages.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-read live ruleset as ground truth (D-12, MANDATORY)</name>
  <files>(read-only; no file written — evidence captured in SUMMARY)</files>
  <read_first>
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-RESEARCH.md §6 (snapshot: 5 required contexts)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>Run `gh api repos/szTheory/sigra/rulesets/14941512` and extract the `required_status_checks` contexts. Confirm they are EXACTLY the 5 names: `Library tests`, `Example unit smoke (ExUnit + ConnTest)`, `Install smoke (fresh phx.new + sigra.install)`, `Example HTTP smoke (boot + curl critical routes)`, `Example Playwright smoke (full lifecycle)`, with `enforcement: active`. This is the standing 194-D03 mandate per D-12: the ruleset, not ROADMAP/MAINTAINING prose, is ground truth. If a 6th or renamed context has appeared, STOP and surface a reconcile checkpoint before touching any job name. If `gh` is unauthed, fall back to the 5 names in MAINTAINING.md and flag re-verification in the SUMMARY (per RESEARCH Environment Availability). Re-grep the live ci.yml job-header line numbers before editing — prior commits shift line anchors.</action>
  <verify>
    <automated>gh api repos/szTheory/sigra/rulesets/14941512 --jq '.rules[]?.parameters.required_status_checks[]?.context' 2>/dev/null | sort</automated>
  </verify>
  <acceptance_criteria>The live ruleset returns exactly the 5 required-check contexts above (or, if unauthed, the fallback path is taken and flagged). No 6th/renamed context. The 5 names are recorded in the SUMMARY as the byte-identical baseline that Task 2 must NOT touch.</acceptance_criteria>
  <done>Live required-check set captured and equals the 5 expected names (or fallback flagged); execution may proceed to the job edits.</done>
</task>

<task type="auto">
  <name>Task 2: Add schedule cron + gate the 5 moved jobs off PRs (D-01..D-08, D-11)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (on: block lines 3-10; moved job headers 502, 554, 604, 733, 1156 — re-grep live)
    - .github/workflows/playwright-github-pages.yml (lines 14-18, the cron precedent)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-PATTERNS.md §1 and §3 (exact analog shapes)
  </read_first>
  <action>Two edits to ci.yml. (a) D-01: insert a `schedule:` key into the existing `on:` block (alongside `workflow_dispatch`/`push:[main]`/`pull_request:[main]`) mirroring the `- cron: '…'` list-item form from playwright-github-pages.yml. Use `cron: '30 4 * * *'` (04:30 UTC) — any value clear of the existing `45 6` slot satisfies D-01; 30 4 is the RESEARCH recommendation. (b) D-02/D-05: insert exactly one line `if: github.event_name != 'pull_request'` at the job level (adjacent to `runs-on:`/`needs:`) on each of the 5 moved jobs: `upgrade_smoke`, `passkeys_manual_fallback_smoke`, `install_matrix`, `passkeys_opt_out_smoke`, `generated_admin_playwright_smoke`. The condition is `!= 'pull_request'` NOT `== 'schedule'` (D-03) so main-push + release-dispatch keep full coverage. Use JOB-level if: for whole-job removal, NOT step-level gating (D-04). Leave every job body byte-identical (services.postgres, caches, $GITHUB_STEP_SUMMARY, MATRIX_FLAGS env indirection all preserved). Do NOT add `if:` to the 5 required lanes, `install_golden_contract`, or `library_tests_dep_off` (D-06/D-11). Do NOT rename any job.</action>
  <verify>
    <automated>test "$(grep -c "github.event_name != 'pull_request'" .github/workflows/ci.yml)" -ge 5 && grep -q "schedule:" .github/workflows/ci.yml && ! grep -q "45 6 \* \* \*" <(grep cron .github/workflows/ci.yml | grep -v '^#') && echo OK</automated>
  </verify>
  <acceptance_criteria>ci.yml contains a new `schedule:` cron not equal to `45 6 * * *`; exactly the 5 moved job headers carry `if: github.event_name != 'pull_request'`; none of the 5 required lanes, install_golden_contract, or library_tests_dep_off carries an event_name gate; no job name string changed; the workflow still parses as valid YAML.</acceptance_criteria>
  <done>The 5 moved jobs are PR-skipped + schedule/main/dispatch-run; the schedule trigger is added; required lanes and kept jobs are untouched; YAML is valid.</done>
</task>

<task type="auto">
  <name>Task 3: Make ci-gate skip-tolerant (D-09, D-10)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (ci-gate job ~1276-1325; needs list 1279-1288; if: always() 1289; result loop condition ~1317 — re-grep live)
    - .planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-PATTERNS.md §4
  </read_first>
  <action>D-09: change the single ci-gate per-lane condition that currently fails on any non-success result so it ALSO tolerates `skipped` — i.e. fail only when the result is neither `success` nor `skipped`. This is the one-line change to the `if [[ "$result" != "success" ]]; then` test inside the result loop. Rationale: on PRs, `needs.upgrade_smoke.result` and `needs.generated_admin_playwright_smoke.result` become `skipped` (D-10 — only these two of the 5 moved jobs are in ci-gate.needs); `ci-gate` already runs `if: always()` (1289) so it still executes, and without skip-tolerance it would go red on every PR. A real `failure`/`cancelled` on a still-PR-running lane MUST stay red — do not weaken beyond tolerating `skipped`. Leave `if: always()` (1289) and the `needs:` list (1280-1288) UNTOUCHED. Do NOT add the other 3 moved jobs (install_matrix, passkeys_manual_fallback_smoke, passkeys_opt_out_smoke) to ci-gate.needs — they are not in it today and adding them creates needless skipped entries (D-10 anti-pattern).</action>
  <verify>
    <automated>grep -q 'skipped' .github/workflows/ci.yml && grep -Eq 'upgrade_smoke' .github/workflows/ci.yml && grep -Eq 'generated_admin_playwright_smoke' .github/workflows/ci.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>The ci-gate result loop tolerates `skipped` (fails only on results that are neither success nor skipped); `if: always()` and the 9-entry `needs:` list are unchanged; the 3 non-needed moved jobs were NOT added to ci-gate.needs; a real failure/cancelled still trips the gate red.</acceptance_criteria>
  <done>ci-gate stays green on a PR where upgrade_smoke + generated_admin_playwright_smoke are skipped, and red on a real lane failure; needs list and if: always() intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event → workflow trigger | The `on:` block + job-level `if:` decide which jobs run per event; an attacker-controlled PR must not be able to escalate into the broad/nightly-only jobs or relax required-check enforcement. |
| Workflow YAML → GitHub ruleset | The 5 job `name:` strings produce the required status contexts the ruleset (14941512) enforces; a name drift would strand a pending required context. |

## STRIDE Threat Register (ASVS L1, block-on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-01 | Tampering | matrix flag values in moved `install_matrix` / `passkeys_opt_out_smoke` | accept (unchanged) | `MATRIX_FLAGS` env-indirection (ci.yml ~681-692) preserved on move — bodies move byte-identical, no new injection surface. |
| T-196-02 | Tampering | `uses:` action pins in moved jobs | accept (unchanged) | All `uses:` already SHA-pinned (checkout, setup-beam); no actions added or repinned. |
| T-196-03 | Elevation of Privilege | required-check bypass via skip filtering | mitigate | The 5 required lanes carry NO event_name gate (D-11) so they never become perpetually-skipped pending contexts; only non-required jobs are gated (D-02 safe-because-non-required). `permissions: contents: read` (ci.yml:16-17) preserved. |
| T-196-04 | Denial of Service | ci-gate red on every PR after the move | mitigate | D-09 skip-tolerant loop tolerates `skipped` for the two moved-and-needed jobs while keeping real `failure`/`cancelled` red. |
| T-196-SC | Tampering | npm/pip/cargo installs | accept | No package installs introduced this phase (zero external packages); package legitimacy gate not applicable. |
</threat_model>

<verification>
- `gh api repos/szTheory/sigra/rulesets/14941512` returns the same 5 required contexts (Task 1) and they remain byte-identical in ci.yml job `name:` strings after Task 2.
- Workflow parses: the edited ci.yml is valid YAML (e.g. `python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/ci.yml"))'` or `gh workflow view CI`).
- `grep -c "github.event_name != 'pull_request'"` on ci.yml is ≥ 5 (the 5 moved jobs) and the gate appears on NONE of the 5 required lanes / install_golden_contract / library_tests_dep_off.
- The ci-gate loop condition tolerates `skipped`; `needs:` list and `if: always()` unchanged.
- Phase contract tests (phase_51, phase_58) are re-verified in Plan 02 against this edited ci.yml.
</verification>

<success_criteria>
- One ci.yml: `schedule:` cron added (not `45 6`); 5 moved jobs gated `if: github.event_name != 'pull_request'`; ci-gate skip-tolerant.
- 5 ruleset-required lane names byte-identical and unconditional; install_golden_contract + library_tests_dep_off kept on PR.
- ci-gate needs list + if: always() untouched; no job renamed; YAML valid.
</success_criteria>

<output>
Create `.planning/phases/196-pr-fast-vs-nightly-broad-trigger-model/196-01-SUMMARY.md` when done. Record the live ruleset re-read result (the 5 contexts) as ground-truth evidence.
</output>
