---
phase: 240-green-main-evidence-honest-pages-script
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/ensure-github-pages-legacy-branch.sh
  - scripts/ci/ensure-github-pages-legacy-branch.test.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [GREEN-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GET /repos/{repo}/pages returning 403 makes the script exit 1 and never issues POST /repos/{repo}/pages (the D-15 any-error-to-create regression is closed)."
    - "A 500 response whose body contains the digit sequence 403 exits 1 — the status line, not a substring of the body, decides (D-17)."
    - "PUT /repos/{repo}/pages returning 204 No Content with an empty body is read as success and exits 0 (D-18)."
    - "PUT /repos/{repo}/pages returning 403 exits 0 with the documented pages:write-is-not-repo-admin message on stderr, and 403 is the ONLY tolerated non-2xx status anywhere in the script (D-19)."
    - "PUT /repos/{repo}/pages returning 422 or 500 exits 1 with the response body echoed to stderr — the SC-3 loud-RED demonstration."
    - "With GH_TOKEN and GITHUB_TOKEN unset the script exits 0 having issued zero gh calls (D-22 no-token early exit preserved)."
    - "The three build-trigger POST /pages/builds calls at :30, :50 and :75 still end in || true and each carries a one-line reason comment (D-22)."
    - "bash scripts/ci/ensure-github-pages-legacy-branch.test.sh runs from a ci.yml fast_checks step, so the self-test is executed rather than merely present."
  artifacts:
    - scripts/ci/ensure-github-pages-legacy-branch.sh
    - scripts/ci/ensure-github-pages-legacy-branch.test.sh
    - .github/workflows/ci.yml
  key_links:
    - "playwright-github-pages.yml:206-211 is the sole caller; a loud exit 1 reddens only that publisher step, which is NOT in ci-gate.needs."
    - "The new fast_checks step sits alongside ci.yml:257-261 (Notify-failure-issue self-test) and must not touch ci-gate.needs (ci.yml:1547-1557)."
  prohibitions:
    - "MUST NOT deliberately misconfigure the live GitHub Pages settings of this public repository in order to manufacture an observable 403. The 403 arm is unreachable on the real repo (the :48 already-gh-pages arm fires); the fake-gh stub is the only honest proof surface (D-20)."
    - "MUST NOT ship a fake-gh stub that emits a response shape real gh never produces. A stub that proves a shape the real CLI cannot emit is a green gate verifying nothing — all four observable channels (exit 1, HTTP/2.0 status line as stdout line 1, JSON body on stdout after a blank line, gh: <msg> (HTTP <code>) on stderr) must match the live-probed gh 2.95.0 behaviour in 240-RESEARCH.md §4."
---

<objective>
Make `scripts/ci/ensure-github-pages-legacy-branch.sh` honest: every non-2xx status from the Pages
REST API is either a named, commented, tolerable case or a loud `exit 1` — never a silent
fall-through. Close the D-23 gap by giving the script its first sibling `.test.sh`, and wire that
self-test into `fast_checks` so it is actually executed (GREEN-05 / SC-3).

Purpose: the publisher job could report success while the site stayed broken. Two independent
lenient paths caused that — an `any-error -> create` GET split (D-15) and an unanchored `grep -qE
'403'` over a `2>&1`-merged blob (D-17).
Output: a rewritten script with two `case` arms on read status lines, a 9-mode hermetic stub
harness, and one new `ci.yml` `fast_checks` step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-CONTEXT.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-PATTERNS.md
</context>

## SC-3 ↔ D-19 reconciliation (state this in writing; a reviewer will otherwise read the
## implementation as contradicting its own success criterion)

ROADMAP SC-3 says the script "fails loudly on a 403". D-19 keeps the **PUT**-403 tolerable. Both
are right about different things. SC-3's real defect is that a 403 was **indistinguishable from
every other failure** — the same `grep` arm swallowed a 500, a 422, a rate limit and a network
blip. The fix makes the PUT-403 a single named, commented arm (justified: the caller declares
`pages: write`, not repo-admin, at `playwright-github-pages.yml:36-38`) and makes **every other
status** exit 1. The RED demonstration SC-3 asks for is therefore the `*)` arm firing on a
500/422, **plus** the GET-side 403 now exiting 1 instead of silently POST-creating — not the
PUT-403 path. This paragraph must be reproduced as a comment block in the script header.

## Flagged assumptions (probe-surfaced, UNRESOLVED — not silently dropped)

| Probe | Requirement | Status | Why it stays unresolved |
|---|---|---|---|
| `unclassified — review manually` | GREEN-05 | **unresolved** | The edge probe could not classify GREEN-05 into a known edge category (it is a bookkeeping + error-posture requirement, not a data-shape one). No defensible acceptance criterion and no backstop was minted; it is surfaced here for a human read rather than auto-resolved. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end honest GET arm — script, stub harness, and CI wiring in one slice</name>
  <files>scripts/ci/ensure-github-pages-legacy-branch.sh, scripts/ci/ensure-github-pages-legacy-branch.test.sh, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/ci/ensure-github-pages-legacy-branch.sh (whole file, 76 lines — current state; sites at `:18`, `:30`, `:43-52`, `:50`, `:66-73`, `:75`)
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md §4 (line-exact map, live `gh api -i` probe output, the proposed site-1 `case` block) and §5 (stub-mode table, the verbatim four-channel 403 payload)
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-PATTERNS.md — the `scripts/ci/ensure-github-pages-legacy-branch.sh` and `.test.sh` sections, plus the "No Analog Found" table
    - scripts/ci/notify-failure-issue.test.sh:1-70 (header case enumeration, PASS/FAIL tally, recording `gh` stub)
    - scripts/ci/capture-terminal-ratification-evidence.test.sh:13-23, :88-114 (`FAKE_MODE` / `FAKE_GH_LOG` idiom, expected-failure blocks that assert the reason)
    - .github/workflows/ci.yml:257-261 (`Notify-failure-issue self-test` — the exact shape of the new fast_checks step) and :383-393 (the prohibitions glob, for contrast — do NOT add a step there)
    - .github/workflows/playwright-github-pages.yml:36-38 and :196-211 (permissions + the sole caller)
  </read_first>
  <behavior>
    - `get_200_already_gh_pages` (default `ok`): GET returns `HTTP/2.0 200 OK`, headers, blank line, `{"build_type":"legacy","source":{"branch":"gh-pages","path":"/"}}`, rc 0 → script exits 0, prints `already gh-pages /`, and the call log contains **no** `--method PUT` and **no** `--method POST` against `/pages` (the builds POST is allowed).
    - `get_404`: GET returns `HTTP/2.0 404 Not Found` + body + `gh: Not Found (HTTP 404)` on stderr, rc 1 → the create `POST repos/<repo>/pages` fires exactly once, script exits 0.
    - `get_403`: all four channels of a real 403 → script exits **1**, the body is echoed to stderr, and the call log contains **zero** `--method POST` against `repos/<repo>/pages` (this is the D-15 regression guard).
    - `get_500`: `HTTP/2.0 500 Internal Server Error` plus a body containing the bare digits `403` → script exits **1** (the D-17 false-positive guard).
    - `build_type_workflow`: GET 200 with `"build_type":"workflow"` → exits 0, `not changing`, no PUT.
    - `no_token`: `GH_TOKEN` and `GITHUB_TOKEN` unset → exits 0 with `no GH_TOKEN/GITHUB_TOKEN; skip.` and **zero** lines in `FAKE_GH_LOG`.
  </behavior>
  <action>
Rewrite site 1 (`ensure-github-pages-legacy-branch.sh:18`) from `if ! pages_json=$(gh api "repos/${REPO}/pages" 2>/dev/null); then` into a status-line read plus a three-arm `case`. Capture with `gh api -i "repos/${REPO}/pages" 2>/dev/null || true` into `get_out`; derive `get_status` as field 2 of stdout line 1 (`head -n 1 | awk '{print $2}'`) — the wire version is literally `HTTP/2.0`, never `HTTP/2`, so never match on a fixed `HTTP/1.1` prefix. Arms: `200)` splits the body off after the blank separator line (tolerate a `\r`-terminated blank via `sed -n '/^\r\{0,1\}$/,$p' | tail -n +2`) and assigns it to `pages_json`; `404)` runs the existing create body from `:19-31` unchanged (echo, `POST repos/${REPO}/pages` with the `build_type: legacy` / `source.branch: gh-pages` / `source.path: /` JSON, echo, the `|| true` builds trigger, `exit 0`); `*)` echoes `GET /pages returned '<status>'; refusing to guess.` plus the raw `get_out` to stderr and `exit 1`. An empty `get_status` (gh itself did not run) must land in `*)` and exit 1 — fail closed. Do NOT use `$?` to discriminate: `gh` exits 1 for 403, 404 and 500 alike (live-verified, RESEARCH §4).

Add the D-16 rationale as a comment directly above the `case`: on a **public** repo `GET /pages` needs no admin, so 404 genuinely means "no site configured" (GitHub's 404-masking-403 policy covers private resources only; live-probed across 8 public repos, RESEARCH §4). Add the SC-3 ↔ D-19 reconciliation paragraph from this plan's body as a header comment block, and a comment citing `240-RESEARCH.md §4` as the source of truth for the `gh api -i` shape (there is no in-repo analog — `grep -rn 'gh api -i' scripts/` returns nothing).

Create `scripts/ci/ensure-github-pages-legacy-branch.test.sh` following `notify-failure-issue.test.sh:1-70`: `set -euo pipefail`, `ROOT` resolved from `BASH_SOURCE`, a header comment enumerating every mode as a lettered case list, a `mktemp -d` + `trap` cleanup, a recording stub written to `$TMP/bin/gh` that appends `"$*"` to `$FAKE_GH_LOG` on every invocation and then dispatches on `${FAKE_MODE:-ok}`, and a PASS/FAIL tally printed at the end with a non-zero exit on any FAIL. Invoke the script under test per case with `FAKE_MODE=<mode> FAKE_GH_LOG="$TMP/calls" PATH="$TMP/bin:$PATH" GITHUB_REPOSITORY=owner/name GH_TOKEN=stub bash "$SCRIPT"` — prepend `PATH` per invocation, never export it globally. Wrap every expected-failure invocation in `set +e` / capture `$?` / `set -e` and assert both the exit code **and** a distinguishing stderr token, never just non-zero.

The stub's 403 payload must reproduce all four channels verbatim per RESEARCH §5: `printf 'HTTP/2.0 403 Forbidden\r\n'`, a `Content-Type: application/json; charset=utf-8\r\n` header, a bare `\r\n`, then the JSON error object containing `"message":"Resource not accessible by integration"`, a `documentation_url`, and `"status":"403"` with **no trailing newline**; `gh: Resource not accessible by integration (HTTP 403)` to stderr; `exit 1`. The `get_500` mode's body must contain the bare digits `403` somewhere so the D-17 false positive is actually exercised.

Implement the six modes in this task's `<behavior>` block only; the PUT modes land in Task 2.

Wire the self-test into `ci.yml` as a new `fast_checks` step placed immediately after the existing `Notify-failure-issue self-test` step (`ci.yml:257-261`), using the identical three-line shape: `- name: Pages legacy-branch script self-test`, a comment naming Phase 240 / GREEN-05 / SC-3 and stating that it is hermetic with a `PATH`-shadowed `gh` and no network call, and `run: bash scripts/ci/ensure-github-pages-legacy-branch.test.sh`. Do NOT touch `ci-gate.needs` (`ci.yml:1547-1557`, exactly ten entries; `ci.yml:1543-1546` warns that `honest-skip-verdict.sh`'s extractor breaks on any comment or blank line inside that list). Do NOT add anything to `mix ci` (ROADMAP standing constraint 5).
  </action>
  <acceptance_criteria>
    - `bash scripts/ci/ensure-github-pages-legacy-branch.test.sh` exits 0 and its final line reads `ensure-github-pages-legacy-branch.test: PASS`.
    - `FAKE_MODE=get_403` run: exit code is `1`, stderr contains `refusing to guess`, and `grep -c -- '--method POST' "$FAKE_GH_LOG"` counting only lines matching `repos/owner/name/pages` (not `/pages/builds`) is `0`.
    - `FAKE_MODE=get_500` run: exit code is `1` even though the stubbed body contains the digits `403`.
    - `FAKE_MODE=get_404` run: exit code is `0` and the call log contains exactly one line matching `repos/owner/name/pages --method POST`.
    - `FAKE_MODE=no_token` run (both `GH_TOKEN` and `GITHUB_TOKEN` unset): exit `0`, stdout contains `no GH_TOKEN/GITHUB_TOKEN; skip.`, and `$FAKE_GH_LOG` is empty or absent.
    - `grep -n 'gh api -i' scripts/ci/ensure-github-pages-legacy-branch.sh` returns at least one hit, and the GET call site redirects stderr with `2>/dev/null` (read the line to confirm; stderr is not merged into the captured blob).
    - `.github/workflows/ci.yml` contains a step whose `run:` is exactly `bash scripts/ci/ensure-github-pages-legacy-branch.test.sh`, inside the `fast_checks` job.
    - `git diff .github/workflows/ci.yml` shows no change to any line between `ci.yml:1534` and `ci.yml:1560` (the `ci-gate` job header and its `needs:` list).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/ensure-github-pages-legacy-branch.test.sh</automated>
  </verify>
  <done>The GET arm is status-line-driven; 403 and 500 both exit 1 with no POST-create; 404 still creates; the self-test exists, passes, and runs from a `fast_checks` step.</done>
  <reversibility rating="reversible">Shell + workflow source edits, fully revertible by `git revert`; no external state is mutated.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest PUT arm — 204 success, the single tolerated 403, loud on everything else</name>
  <files>scripts/ci/ensure-github-pages-legacy-branch.sh, scripts/ci/ensure-github-pages-legacy-branch.test.sh</files>
  <read_first>
    - scripts/ci/ensure-github-pages-legacy-branch.sh:54-76 (the `mktemp`/`trap`/PUT-body heredoc, the `grep -qE '403|Resource not accessible by integration'` arm to be removed, and the final `|| true` builds trigger at `:75`)
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md §4 "Proposed replacement — site #2" and §5's `put_204` / `put_403` / `put_422` / `put_500` rows
    - .github/workflows/playwright-github-pages.yml:36-38 (the `contents: write` + `pages: write` permission block that makes the PUT-403 legitimate)
    - scripts/ci/ensure-github-pages-legacy-branch.test.sh (as written in Task 1 — extend it, do not rewrite it)
  </read_first>
  <behavior>
    - `put_204`: GET 200 with `source.branch: "main"`; PUT returns `HTTP/2.0 204 No Content` + headers + an **empty** body, rc 0 → script exits 0 and prints `updated.` (the case a body-parsing implementation gets wrong — there is no JSON to parse).
    - `put_403`: GET 200 branch=main; PUT emits all four channels of a real 403 → script exits **0**, the documented `pages:write is not repo-admin` message is on **stderr**, and the message names the manual Settings → Pages remedy.
    - `put_422`: GET 200 branch=main; PUT returns `HTTP/2.0 422 Unprocessable Entity` + a JSON body → script exits **1** with the body echoed to stderr.
    - `put_500`: GET 200 branch=main; PUT returns `HTTP/2.0 500 Internal Server Error` → script exits **1** with the body echoed to stderr. This is the SC-3 loud-RED demonstration.
  </behavior>
  <action>
Replace `ensure-github-pages-legacy-branch.sh:66-73` in full — the PUT invocation that merges stderr into the captured blob, together with the arm beneath it that tests that blob with an unanchored substring match — with a status-line `case`, mirroring Task 1's shape. Capture `gh api -i "repos/${REPO}/pages" --method PUT --input "${put_body}" 2>/dev/null || true` into `put_out`; derive `put_status` as field 2 of stdout line 1. Arms: `204|200)` echoes `updated.` and falls through; `403)` keeps the tolerable path — echo to **stderr** a message stating that `pages: write` permits requesting a build but is not repo-admin, that the gh-pages push already ran, and that Settings → Pages → branch `gh-pages` path `/` can be set manually — then `exit 0`; `*)` echoes `PUT /pages returned '<status>'.` plus the raw `put_out` to stderr and `exit 1`. An empty `put_status` lands in `*)`.

`-i` is required, not stylistic: a successful `PUT /pages` returns **204 No Content with an empty body**, so the status line is the only signal, and `--jq` is bypassed entirely on an error response. Record that reason as a comment above the call. Record the D-19 rationale as a comment inside the `403)` arm, citing `playwright-github-pages.yml:36-38`.

Leave the three build-trigger swallows at `:30`, `:50` and `:75` exactly as they are — `gh api ".../pages/builds" --method POST >/dev/null 2>&1 || true` — and add one reason line above each: reddening the publisher on a transient build-trigger failure is the opposite of this milestone's "no red for a reason unrelated to the diff" posture (D-22). Note in the comment that CONTEXT D-22's `:31`/`:52` citations are one line late; the actual swallows are `:30`, `:50`, `:75`. Likewise leave the no-token early `exit 0` at `:13-16` and the `jq`-presence `exit 1` at `:34-37` untouched.

Extend the Task 1 harness with the four PUT modes: the GET stub must return 200 with `source.branch: "main"` so control reaches the PUT, and the PUT stub must emit the mode-appropriate envelope. The `put_204` stub emits the status line, a header, a blank line, **nothing after it**, and exits 0.

Commit this separately from Task 1 (RESEARCH OQ2, accepted): the two sites have independent RED demonstrations — `get_403` versus `put_500` — and a bisect landing between them still leaves a coherent script.
  </action>
  <acceptance_criteria>
    - `FAKE_MODE=put_204` run: exit `0`, stdout contains `updated.`, and the script does not invoke `jq` against the PUT response (no `jq` parse error appears on stderr).
    - `FAKE_MODE=put_403` run: exit `0`, stderr contains both `403` and `not repo-admin`, and stdout does **not** contain `updated.`.
    - `FAKE_MODE=put_422` run: exit `1`, stderr contains `PUT /pages returned '422'`.
    - `FAKE_MODE=put_500` run: exit `1`, stderr contains `PUT /pages returned '500'` and the stubbed response body.
    - `grep -n "grep -qE '403" scripts/ci/ensure-github-pages-legacy-branch.sh` returns no hit.
    - Exactly three lines in `scripts/ci/ensure-github-pages-legacy-branch.sh` end in `|| true`, each preceded by a comment line naming D-22; verified by reading the three call sites, not by a count alone.
    - `bash scripts/ci/ensure-github-pages-legacy-branch.test.sh` exercises all ten modes and its header comment enumerates each by name.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/ensure-github-pages-legacy-branch.test.sh</automated>
  </verify>
  <done>403 is the only tolerated non-2xx status in the whole script; 204 is read as success without body parsing; 422/500 exit 1 loudly; the three build-trigger swallows are intact and documented.</done>
  <reversibility rating="reversible">Source-only; revertible.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Gate the whole change through the local suite and record the RED observations</name>
  <files>scripts/ci/ensure-github-pages-legacy-branch.sh, scripts/ci/ensure-github-pages-legacy-branch.test.sh</files>
  <read_first>
    - CLAUDE.md (local development prerequisites: `scripts/db/up.sh`, `direnv allow`, `mix archive.install --force hex phx_new 1.8.8`)
    - .planning/ROADMAP.md:32-40 (standing constraints 1, 3 and 6)
    - scripts/ci/capture-fast-01-remeasurement.test.sh:95-99 (the terminal PASS-line idiom)
  </read_first>
  <action>
Run `bash scripts/db/up.sh` and source `tmp/db.env` (or `direnv allow`), confirm the phx_new 1.8.8 archive is installed, then run `MIX_ENV=test mix ci` and require it green before any push (CLAUDE.md + ROADMAP standing constraint 3 — root `mix test` misses formatting and `test/example`). Run `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` and require it green, confirming this plan's `ci.yml` edit did not disturb any existing guard.

Capture, verbatim into the plan SUMMARY, the exit code and the distinguishing stderr line for each of the four RED modes — `get_403`, `get_500`, `put_422`, `put_500` — by running each stub mode individually through `bash -c` (never zsh: zsh does not word-split unquoted parameter expansions and has produced a confident false negative in this repo before, RESEARCH Pitfall 7). These four transcripts are the SC-3 "demonstrated red against a stubbed/denied API response" artifact and are the raw material Plan 04 folds into the `AFTER-PAGES-LOUD-RED` evidence slot.

Do not attempt any live observation of a 403 against the real repository: `gh api repos/szTheory/sigra/pages` currently returns `build_type: legacy` with `source.branch: gh-pages` and `source.path: /`, so the `:48` arm exits before the PUT ever runs and the 403 branch is unreachable (D-20). Deliberately misconfiguring Pages on a public repo to manufacture one is prohibited by this plan.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix ci` exits 0.
    - `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0.
    - The SUMMARY contains four transcript blocks, one per RED mode, each showing the invoked `FAKE_MODE`, the observed exit code, and the verbatim distinguishing stderr line.
    - No commit in this plan touches repository settings, and `gh api repos/szTheory/sigra/pages --jq '.source'` still returns `{"branch":"gh-pages","path":"/"}` after the plan completes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix ci</automated>
  </verify>
  <done>Full local gate green; the four RED transcripts are recorded for Plan 04's ledger.</done>
  <reversibility rating="reversible">Verification only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub REST API → `ensure-github-pages-legacy-branch.sh` | Untrusted HTTP response text (status line, headers, JSON body) crosses into shell |
| CI runner token → GitHub Pages settings | `pages: write` mutates outward-facing repository state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-240-01 | Tampering | `ensure-github-pages-legacy-branch.sh` status read | high | mitigate | Read the anchored status line via `head -n 1 \| awk '{print $2}'`; never `grep` a `2>&1`-merged blob, never `eval` the body, never interpolate the body into a further shell command. Empty status falls to `*)` and exits 1 (fail closed). |
| T-240-04 | Tampering | Live public-repo Pages settings | high | mitigate | D-20: the RED proof is a `PATH`-shadowed fake `gh`, never a deliberate live misconfiguration. Acceptance asserts `.source` is unchanged after the plan. |
| T-240-02 | Elevation of Privilege | Caller permissions | medium | accept | The script requests nothing beyond the caller's existing `contents: write` + `pages: write` (`playwright-github-pages.yml:36-38`). A settings-source PUT legitimately 403s and stays tolerable (D-19). |
| T-240-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager dependency. No new packages are installed; the `npm ci` in adjacent workflows resolves from the committed lockfile only. No `[ASSUMED]`/`[SUS]` packages exist to gate. |

Canon-referral breadcrumb (probe Stage 2, dropped as canon): shell command injection from untrusted
HTTP response text is OWASP-canon input validation; mitigated by T-240-01 above and not minted as a
separate project prohibition.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Symbol | Kind |
|---|---|
| `scripts/ci/ensure-github-pages-legacy-branch.test.sh` | new file |
| `get_out`, `get_status`, `put_out`, `put_status` | new shell variables in `ensure-github-pages-legacy-branch.sh` |
| `FAKE_MODE` | stub-mode env var read by the new `.test.sh` |
| `FAKE_GH_LOG` | stub call-log path env var read by the new `.test.sh` |
| `get_200_already_gh_pages`, `get_404`, `get_403`, `get_500`, `build_type_workflow`, `no_token`, `put_204`, `put_403`, `put_422`, `put_500` | `FAKE_MODE` values (10 stub modes) |
| `Pages legacy-branch script self-test` | new `fast_checks` step name in `.github/workflows/ci.yml` |
| `ensure-github-pages-legacy-branch.test: PASS` | terminal PASS line emitted by the new self-test |
</artifacts_this_phase_produces>

<verification>
- `bash scripts/ci/ensure-github-pages-legacy-branch.test.sh` exits 0 with the terminal PASS line.
- All ten stub modes behave exactly as the two `<behavior>` blocks specify.
- `MIX_ENV=test mix ci` green; `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` green.
- `ci.yml` diff is confined to the `fast_checks` job; `ci-gate.needs` untouched.
</verification>

<success_criteria>
GREEN-05's script half is satisfied: every non-2xx Pages API status is either the single named
tolerable PUT-403 or a loud `exit 1`, proven by a hermetic 10-mode self-test that runs in CI.
</success_criteria>

<output>
Create `.planning/phases/240-green-main-evidence-honest-pages-script/240-01-SUMMARY.md` when done.
</output>
