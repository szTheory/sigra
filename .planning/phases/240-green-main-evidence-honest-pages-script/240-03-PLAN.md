---
phase: 240-green-main-evidence-honest-pages-script
plan: 03
type: execute
wave: 2
depends_on: [240-02]
files_modified:
  - scripts/ci/capture-green-04-evidence.sh
  - scripts/ci/capture-green-04-evidence.test.sh
  - .planning/phases/240-green-main-evidence-honest-pages-script/COVERAGE.md
  - .planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md
autonomous: true
requirements: [GREEN-04]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Actions API read passes `per_page=100` and asserts that the summed item count is exactly equal to the envelope's `total_count`; when the two are exactly equal the collection is accepted, and when they differ by even one the collector exits non-zero with the token `total_count_disagreement` rather than silently returning a short list."
    - "An empty or single-element result fails closed: zero legs, fewer than 20 legs, a non-terminal empty page (`nonterminal_empty_page`), or an absent terminal empty page (`absent_terminal_empty_page`) each exit non-zero with that named token — the collector never emits a receipt from a truncated or empty collection."
    - "Output ordering is specified and stable: legs are emitted sorted by `matrix_repeat` ascending and `main`-window runs by `run_id` ascending, and the receipt is emitted through `jq -S` so two captures of the same window produce byte-identical JSON."
    - "An interrupted or still-running leg is never counted as evidence: any selected job whose `conclusion` is null (queued, in_progress, or cancelled) exits non-zero, and the receipt's `sc1.verdict` is `pass` only when all 20 legs concluded `success`."
    - "The collector asserts `git status --porcelain` is empty and that the dispatch run's `head_sha` equals `git rev-parse HEAD`, failing with `dirty_tree` or `evidence_run_head_sha_is_not_final_committed_head` (D-13, the Phase 216 SC-5 trap)."
    - "The receipt records `head_sha` and `clean_tree` in the JSON itself, so the claim is self-dating and a later ledger commit cannot invalidate it."
    - "SC-1 and SC-2 are both measured at the JOB conclusion level via `GET /repos/{repo}/actions/runs/{id}/jobs`, selected by job `name` — never at the run conclusion (D-08, the Phase 238 lesson inverted)."
    - "`sc2.caveat` names in writing that `example_unit_smoke` is absent from `ci-gate.needs` (`ci.yml:1547-1557` lists exactly ten entries) while being independently required by ruleset 14941512, so a `ci-gate: success` is a nine-of-ten claim (D-10)."
    - "`jobs_filter` is set explicitly to `latest` in the receipt and in the request URL, never left to the endpoint default (D-11)."
  artifacts:
    - scripts/ci/capture-green-04-evidence.sh
    - scripts/ci/capture-green-04-evidence.test.sh
    - .planning/phases/240-green-main-evidence-honest-pages-script/COVERAGE.md
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md
  key_links:
    - "`JOB_NAME` in the collector must equal `green-04-evidence.yml`'s job `name:` exactly (`Generated admin Playwright smoke (GREEN-04 repeat)`); a mismatch silently harvests zero legs."
    - "`240-EVIDENCE.md` is parsed by `scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` only when `GSD_PROHIB_SUBJECT` redirects it — p12 is hardcoded to the Phase 230 ledger at `p12:26`."
  prohibitions:
    - "MUST NOT accept a caller-configurable repository, workflow, job name or evidence window. A collector whose window the caller steers lets whoever runs it choose the window that flatters the verdict, which is evidence forgery wearing a green receipt. The repo, both workflow ids, both job-name selectors and the `filter` value are fixed constants in the script (the `capture-terminal-ratification-evidence.sh:5-8` in-repo pattern); only the dispatch run id and the output path are arguments."
    - "MUST NOT write a ledger slot that claims a green outcome without naming the GitHub Actions run ids it was read from. A `captured` slot with no run id is an assertion in prose wearing the costume of evidence — the exact defect p12 exists to catch."
---

<objective>
Ship `scripts/ci/capture-green-04-evidence.sh` — the fail-closed, non-steerable collector that
reads SC-1 (the n≥20 dispatch legs) and SC-2 (the `main` window) at the **job** conclusion level
and emits a canonical JSON receipt — plus its hermetic stub harness, the external-API coverage
matrix, and the `240-EVIDENCE.md` ledger skeleton with every slot still `pending`.

Purpose: "main is green" must be readable from the API run list, not asserted in prose.
Output: one collector, one self-test, `COVERAGE.md`, and a p12-grammar ledger skeleton.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-CONTEXT.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-PATTERNS.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-02-SUMMARY.md
</context>

## Probe-surfaced edge coverage (no silent drops)

Five edge probes were surfaced for this phase. Four are GREEN-04 shaped and are authored above as
explicit `must_haves.truths` (adjacency → `total_count` exactly equal to `(.jobs|length)`; empty →
zero-leg / truncated `/jobs` page; ordering → run-id list stability in the receipt and ledger;
concurrency → `max-parallel: 5` and interrupted matrix legs). The fifth, an **unclassified**
GREEN-05 row, remains **unresolved** and is surfaced as a flagged assumption in `240-01-PLAN.md`.
Equality holds: 5 surfaced == 4 authored + 1 flagged.

<tasks>

<task type="auto">
  <name>Task 1: `scripts/ci/capture-green-04-evidence.sh` — fixed-parameter, fail-closed collector</name>
  <files>scripts/ci/capture-green-04-evidence.sh</files>
  <read_first>
    - scripts/ci/capture-terminal-ratification-evidence.sh (whole file — `:1-16` header + fixed-parameter pattern, `:18-46` arg surface / fail-closed preflight / temp-output discipline, `:47-92` THE pagination core to copy verbatim including `request_page` / `validate_manifest` / `collect_pages`, `:66` the `total_count_disagreement` assertion, `:126-136` the real `/jobs` call site with its trailing `?`, `:138-153` canonical-output emission)
    - scripts/ci/capture-fast-01-remeasurement.sh:1-30, :84-92 (`schema_version` constant, rate-limit preflight, the `jq -S -n` + `--slurpfile` + temp-then-`mv -f` idiom, and `:25-29` the git-side protected-constant assertion style to mirror for the D-13 checks)
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md §2 (the skeleton, the "What must CHANGE relative to the runs-endpoint collectors" table, and the canonical JSON shape)
    - .github/workflows/green-04-evidence.yml (for the exact job `name:` string the collector selects on)
    - .github/workflows/ci.yml:1547-1557 (the ten `ci-gate.needs` entries the SC-2 caveat cites) and :2087-2110 (`admin_eval_render`, the job outside `ci-gate.needs` that makes run-level `main` red)
  </read_first>
  <action>
Create `scripts/ci/capture-green-04-evidence.sh` with `set -euo pipefail` and a header comment
recording its dual provenance: the pagination core is copied from
`capture-terminal-ratification-evidence.sh:47-92` and the canonical-output idiom from
`capture-fast-01-remeasurement.sh:84-92`. State explicitly that
`capture-fast-01-remeasurement.sh` is **not** extended — its cutoff SHA and timestamp at `:5-11`
are protected constants pinned by
`test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs` (D-12).

Declare these as fixed, non-configurable constants: `REPO="szTheory/sigra"`,
`EVIDENCE_WORKFLOW="green-04-evidence.yml"`, `CI_WORKFLOW="ci.yml"`,
`JOB_NAME="Generated admin Playwright smoke (GREEN-04 repeat)"`,
`CI_JOB_NAME="Generated admin Playwright smoke"`, `CI_GATE_JOB_NAME="ci-gate"`,
`JOBS_FILTER="latest"`, `MIN_LEGS=20`, `MAX_PAGES=10000`,
`SCHEMA_VERSION="sigra.green-04-evidence/v1"`. Comment that the caller cannot steer the repository,
the workflow, the job selector or the filter — the in-repo pattern at
`capture-terminal-ratification-evidence.sh:5-8`, and the mitigation for evidence forgery.

Argument surface, and nothing more: `--output PATH`, `--run-id <dispatch run id>`,
`--main-window-start <UTC ISO8601>`, `--main-window-end <UTC ISO8601>`. Unknown flags exit non-zero.
Define `fail() { echo "capture-green-04-evidence: FAIL: $*" >&2; exit 1; }` and route every
assertion through it with a stable snake_case token so tests can grep the reason rather than just
the exit code.

Preflight, in order, each fail-closed: `command -v gh` and `command -v jq`
(`gh_not_found` / `jq_not_found`); exactly one rate-limit check —
`gh api rate_limit --jq '.resources.core.remaining'` must exceed 250 (`rate_limit_too_low`);
then the D-13 pair — `[[ -z "$(git status --porcelain)" ]]` or `fail "dirty_tree"`, and the
dispatch run's `head_sha` (`gh api "repos/$REPO/actions/runs/$RUN_ID" --jq .head_sha`) must equal
`git rev-parse HEAD` or `fail "evidence_run_head_sha_is_not_final_committed_head"`. Mirror the
git-assertion style of `capture-fast-01-remeasurement.sh:25-29`.

Copy `request_page`, `validate_manifest` and `collect_pages` verbatim from
`capture-terminal-ratification-evidence.sh:47-92`. They already encode every D-11 requirement:
`per_page=100` on every call, `total_count` stability across pages (`total_count_changed`), summed
item length equal to `total_count` (`total_count_disagreement`), a proven terminal empty page
(`absent_terminal_empty_page`), contiguity (`non_contiguous_or_duplicate_page`), no empty
non-terminal page (`nonterminal_empty_page`), duplicate item ids (`duplicate_item_id`), and the
`minimum_pages` bound. Do not re-derive this loop.

SC-1 collection: `collect_pages "repos/$REPO/actions/runs/$RUN_ID/jobs?filter=$JOBS_FILTER" jobs …`
— note the endpoint string carries its own `?` so `request_page` can append `&per_page=…` (the
`capture-terminal-ratification-evidence.sh:126-136` call-site shape). Select jobs by
`.name == $JOB_NAME`. Assert: at least `MIN_LEGS` selected (`insufficient_legs`); every selected
job has a non-null `conclusion` (`leg_without_conclusion` — an in-progress or cancelled leg means
the capture ran too early and must never be counted); the set of `matrix_repeat` values, derived by
parsing the trailing parenthesised integer from each job's `name` or from the job's `.name` suffix,
is exactly the integers 1 through 20 with no duplicates (`matrix_repeat_set_mismatch`); and every
selected job's parent `run_id` equals the single dispatch `RUN_ID` (`foreign_run_id`).
`sc1.verdict` is `pass` only when all legs concluded `success`, otherwise `fail`.

SC-2 collection: `collect_pages "repos/$REPO/actions/workflows/$CI_WORKFLOW/runs?branch=main&created=$START..$END"`
over `workflow_runs`, then for each run id
`collect_pages "repos/$REPO/actions/runs/$id/jobs?filter=$JOBS_FILTER" jobs …`. Record per run the
`run_id`, `url`, the run-level `run_conclusion` **and** the per-job conclusions, but compute the
verdict from the jobs. Count `ci_gate_conclusions` by the `ci-gate` job's conclusion
(`success`/`failure`/`skipped`) and set `flake_attributable_red_count` to the number of `main` runs
whose `Generated admin Playwright smoke` job concluded `failure`. Set `sc2.caveat` to the literal
string naming that `example_unit_smoke` is absent from `ci-gate.needs` (`ci.yml:1547-1557`) while
being independently required by ruleset 14941512, so a `ci-gate: success` is a nine-of-ten claim.

Emission: build the receipt with `jq -S -n` plus `--slurpfile` from the validated manifests, matching
the canonical shape in RESEARCH §2 — top-level `schema_version`, `repository`, `head_sha`,
`clean_tree`, then `sc1` (`workflow`, `dispatch_run_id`, `job_name`, `jobs_filter`, `legs[]` with
`run_id`/`job_id`/`matrix_repeat`/`url`/`conclusion`, `leg_count`, `verdict`) and `sc2` (`workflow`,
`branch`, `window.start`, `window.end`, `runs[]`, `ci_gate_conclusions`,
`flake_attributable_red_count`, `caveat`). Sort `sc1.legs` by `matrix_repeat` ascending and
`sc2.runs` by `run_id` ascending before emission so repeated captures of the same window are
byte-identical. Write to a temp file inside the output directory and `mv -f` into place only after
every assertion has passed — never take ownership of the caller's receipt path until a complete,
validated replacement exists.

The script must NOT write `240-EVIDENCE.md`. Neither existing collector writes a ledger; the JSON
is the receipt and the markdown is the claim, authored by the executor from the JSON.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/ci/capture-green-04-evidence.sh` exits 0 and the file is executable.
    - `grep -n 'JOB_NAME=' scripts/ci/capture-green-04-evidence.sh` shows the value byte-identical to the `name:` of the `green_04_evidence_repeat` job in `.github/workflows/green-04-evidence.yml`.
    - The script defines `request_page`, `validate_manifest`, `collect_pages` and `fail`, and every `collect_pages` endpoint string ends with a `?` or contains a `&`-joinable query.
    - `filter=latest` appears in both `/jobs` endpoint strings and `jobs_filter` appears in the emitted receipt.
    - Running the script with any flag other than `--output`, `--run-id`, `--main-window-start`, `--main-window-end` exits non-zero.
    - The script contains no variable assignment that lets an environment variable or argument override `REPO`, `EVIDENCE_WORKFLOW`, `CI_WORKFLOW`, `JOB_NAME`, `CI_JOB_NAME` or `JOBS_FILTER` — verified by reading each constant's assignment line.
    - The failure tokens `dirty_tree`, `evidence_run_head_sha_is_not_final_committed_head`, `total_count_disagreement`, `insufficient_legs`, `leg_without_conclusion` and `foreign_run_id` each appear in the script.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/capture-green-04-evidence.sh</automated>
  </verify>
  <done>The collector exists, reads only job-level conclusions, is non-steerable, and fails closed on every pagination, population and D-13 hazard.</done>
  <reversibility rating="reversible">New script; no external mutation (all reads).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `scripts/ci/capture-green-04-evidence.test.sh` — hermetic stub harness</name>
  <files>scripts/ci/capture-green-04-evidence.test.sh, scripts/ci/capture-green-04-evidence.sh</files>
  <read_first>
    - scripts/ci/capture-terminal-ratification-evidence.test.sh (whole file — `:1-18` stub construction, `:69-71` happy path, `:83-85` and `:105-114` negative assertions from the call log, `:88-103` the expected-failure idiom that asserts the reason)
    - scripts/ci/capture-fast-01-remeasurement.test.sh:49-95, :99 (`FAKE_HTTP` / `FAKE_RUNS` mode selectors and the terminal PASS line)
    - scripts/ci/capture-green-04-evidence.sh (as authored in Task 1)
  </read_first>
  <behavior>
    - `ok`: a stubbed dispatch run with 20 legs, all `success`, `total_count` equal to the item count, a terminal empty page, and a `head_sha` matching `git rev-parse HEAD` in a stub git repo → exit 0, receipt written, `jq -e '.sc1.leg_count == 20 and .sc1.verdict == "pass"'` true.
    - `truncated`: `total_count` reports 45 while the pages return 30 items → exit non-zero, stderr contains `total_count_disagreement`, and **no** receipt file is created at the `--output` path.
    - `empty_jobs`: the `/jobs` envelope returns zero items → exit non-zero with `insufficient_legs`, no receipt.
    - `leg_in_progress`: one of the 20 legs has `conclusion: null` → exit non-zero with `leg_without_conclusion`.
    - `dirty_tree`: the working tree has an uncommitted change → exit non-zero with `dirty_tree`, and the call log shows **zero** `gh api` calls after the assertion point.
    - `head_mismatch`: the dispatch run's `head_sha` differs from `git rev-parse HEAD` → exit non-zero with `evidence_run_head_sha_is_not_final_committed_head`.
    - `rate_limited`: `rate_limit` reports 10 remaining → exit non-zero with `rate_limit_too_low` before any collection call is issued.
    - Determinism: running `ok` twice against identical stub data produces byte-identical receipts (`cmp` reports no difference).
  </behavior>
  <action>
Create `scripts/ci/capture-green-04-evidence.test.sh` following
`capture-terminal-ratification-evidence.test.sh`. Resolve `ROOT` from `BASH_SOURCE`, `mktemp -d`
with a `trap` cleanup, and write a recording `gh` stub to `$TMP/bin/gh` that appends `"$*"` to
`$FAKE_GH_LOG` on every invocation and dispatches on `${FAKE_MODE:-ok}`. Prepend
`PATH="$TMP/bin:$PATH"` per invocation only. Run the script under test inside a throwaway git
repository created in `$TMP` so the D-13 `git status` / `git rev-parse HEAD` assertions have real
git state to read; expose the stub's reported `head_sha` through `FAKE_HEAD_SHA` and the reported
envelope count through `FAKE_TOTAL_COUNT` so the `truncated` and `head_mismatch` modes are data
driven rather than hand-forked.

Wrap every expected-failure invocation in `set +e` / capture `$?` / `set -e`, then assert both the
non-zero exit **and** the specific failure token on stderr — asserting the reason, not merely
non-zero. Use the call log for the negative facts: after a `dirty_tree` or `rate_limited` failure,
assert that no line in `$FAKE_GH_LOG` matches the `/jobs?` endpoint. After a failed run, assert the
`--output` path does not exist, proving the temp-then-`mv -f` discipline held.

Add the determinism case last: run `ok` twice to two different output paths with identical stub
data and `cmp` the two receipts.

End with a PASS/FAIL tally and the terminal line `capture-green-04-evidence.test: PASS`, exiting
non-zero if any case failed.

Do **not** wire this self-test into `ci.yml`. Neither `capture-fast-01-remeasurement.test.sh` nor
`capture-terminal-ratification-evidence.test.sh` has a CI caller; this one follows that precedent
deliberately, and the SUMMARY must say so explicitly rather than leaving it unstated. (The Pages
self-test in Plan 01 *is* wired, because that script runs on the publisher path in CI; this
collector is operator-invoked once.)
  </action>
  <acceptance_criteria>
    - `bash scripts/ci/capture-green-04-evidence.test.sh` exits 0 and its final line reads `capture-green-04-evidence.test: PASS`.
    - The `truncated` case exits non-zero, stderr contains `total_count_disagreement`, and the `--output` path does not exist afterwards.
    - The `empty_jobs` case exits non-zero with `insufficient_legs`.
    - The `leg_in_progress` case exits non-zero with `leg_without_conclusion`.
    - The `dirty_tree` case exits non-zero with `dirty_tree` and `$FAKE_GH_LOG` contains no line matching `/jobs?`.
    - The `head_mismatch` case exits non-zero with `evidence_run_head_sha_is_not_final_committed_head`.
    - The determinism case: `cmp` of the two receipts reports no difference.
    - The SUMMARY states in one sentence that this self-test is intentionally unwired from CI, naming the two precedent collectors.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/capture-green-04-evidence.test.sh</automated>
  </verify>
  <done>Every fail-closed path of the collector is exercised hermetically and asserts its reason, and the receipt is proven byte-stable.</done>
  <reversibility rating="reversible">New test file.</reversibility>
</task>

<task type="auto">
  <name>Task 3: `COVERAGE.md` external-API matrix and the `240-EVIDENCE.md` ledger skeleton</name>
  <files>.planning/phases/240-green-main-evidence-honest-pages-script/COVERAGE.md, .planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md (whole file — the summary table with anchored slot links at `:1-9`, the captured-slot body shape at `:11-34`, and the `## AFTER-P17-GUARD-OBSERVED` slot)
    - scripts/ci/prohibitions/_lib.mjs:254-294 (`parseEvidenceSlots` — the authoritative slot grammar)
    - scripts/ci/prohibitions/p12-run-id-provenance.test.mjs:26, :44-60 (the hardcoded Phase 230 ledger path, the `Status:` regex, the corroboration rule, the fenced-block rule, the non-vacuity floors)
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md §3 (the grammar derived from the parser rather than from the example)
  </read_first>
  <action>
Write `.planning/phases/240-green-main-evidence-honest-pages-script/COVERAGE.md` as a markdown
table with the columns `| capability | decision | reason |` over the GitHub REST surface this phase
touches through `gh`. `INTEGRATE` is the default; the matrix is the subtraction record, so every
`OPT-OUT` row carries a one-line reason. Mark INTEGRATE for: `GET /repos/{repo}/actions/runs/{id}`
(head_sha), `GET /repos/{repo}/actions/runs/{id}/jobs`,
`GET /repos/{repo}/actions/workflows/{file}/runs`,
`POST /repos/{repo}/actions/workflows/{file}/dispatches` (operator, via `gh workflow run`),
`GET /rate_limit`, `GET /repos/{repo}/pages`, `POST /repos/{repo}/pages`,
`PUT /repos/{repo}/pages`, `POST /repos/{repo}/pages/builds`, `GET /repos/{repo}/issues/{n}`,
`POST /repos/{repo}/issues/{n}/comments`, and `PATCH /repos/{repo}/issues/{n}` (close). Mark
OPT-OUT, each with its reason, for at minimum:
`GET /repos/{repo}/actions/runs/{id}/artifacts` (the receipt cites run and job ids, not artifact
blobs), `GET /repos/{repo}/actions/runs/{id}/logs` (log text is not the evidence surface; job
conclusions are), `POST /repos/{repo}/actions/runs/{id}/rerun` (a rerun would falsify the n≥20
window), `POST /repos/{repo}/actions/runs/{id}/cancel` (cancelling a leg manufactures a null
conclusion the collector is built to reject), `DELETE /repos/{repo}/pages` (destructive, outward
facing, and out of scope), `GET /repos/{repo}/pages/builds` and `GET /repos/{repo}/pages/health`
(build status is deliberately not gated — D-22 keeps the build triggers lenient).

Write `.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md` modelled on
`236-EVIDENCE.md`: a title, a summary table of slots with anchored links, then one `##` slot
section per row. Author these six slots, in this order, with these exact headings:
`## BEFORE-MAIN-RED-MISATTRIBUTION`, `## AFTER-PAGES-LOUD-RED`, `## AFTER-P20-GUARD-OBSERVED`,
`## AFTER-GREEN-04-N20`, `## AFTER-CI-GATE-MAIN-WINDOW`, `## AFTER-ISSUE-231-CLOSED`. Slot headings
must be level-2, prefixed `BEFORE-` or `AFTER-`, and contain only uppercase letters, digits and
hyphens — lowercase or underscores do not parse as a slot.

Every slot's `Status:` line in this plan is `pending (<reason> obligation)` — the literal word
**obligation** is required by the parser — and every slot must already carry at least one fenced
block holding the exact command that will later produce its evidence. Six slots clears p12's
non-vacuity floor of four.

One mandatory deviation from the `236-EVIDENCE.md` analog: that ledger's
`## AFTER-P17-GUARD-OBSERVED` slot uses a **bare** `Status: captured` with no parenthetical, which
**fails** p12's grammar regex. It passes today only because p12 never reads the 236 ledger. Do not
copy that form. The only legal captured forms are `captured (run <id>)` and
`captured (runs <id>, <id>, …)`. Record this deviation as an HTML comment in the ledger header so a
later editor does not reintroduce the bare form.

Record in the ledger header, as prose, the two claims Plan 04 will later evidence and that must be
stated in writing regardless: (a) D-09 — `ci.yml`'s **run-level** conclusion on `main` is currently
`failure` for a reason outside `ci-gate`; on run `35365693716` (HEAD `bca3ad72`) the only
non-success job was `Admin eval render + probe`, which `ci.yml:2087-2106` documents as not being in
`ci-gate.needs`, and `Notify on red ci-gate` was `skipped`, which per `ci.yml:1651-1652` positively
proves `ci-gate` itself was not red; a naive `gh run list --branch main` would report red and
misattribute it to the flake. (b) D-10 — the SC-2 verdict is a per-lane job table over the `main`
window, and `example_unit_smoke` is absent from the ten `ci-gate.needs` entries at
`ci.yml:1547-1557` while being independently required by ruleset 14941512, so a `ci-gate: success`
is a nine-of-ten claim. This phase **discloses** that caveat; it does not fix it (the fix is owned
by `.planning/todos/pending/2026-07-29-example-unit-smoke-required-but-absent-from-ci-gate-needs.md`).

Finally, run p12 redirected at the new ledger to prove the skeleton parses. Because every slot is
still `pending`, p12's `captured.length >= 3` floor will not yet be satisfiable — record that
expected failure verbatim in the SUMMARY together with the slot count p12 reports, and state that
Plan 04 flips the slots and re-runs it to green. A parse yielding **zero** slots would be a
different and real failure (`evidence ledger parsed to zero BEFORE-*/AFTER-* slots`) and must not
be accepted.
  </action>
  <acceptance_criteria>
    - `COVERAGE.md` exists, its table header is exactly `| capability | decision | reason |`, every row's decision is `INTEGRATE` or `OPT-OUT`, and every `OPT-OUT` row has a non-empty reason cell.
    - `240-EVIDENCE.md` contains exactly six level-2 headings matching `^## (BEFORE|AFTER)-[A-Z0-9-]+$`, with the six names listed in the action.
    - Every slot body contains a `Status:` line matching `pending (` and containing the word `obligation`, and at least one fenced block.
    - At least one fenced block in the ledger contains a `gh api`, `gh run` or `gh pr` invocation.
    - `GSD_PROHIB_SUBJECT=.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` reports a failure on the captured-count floor and **not** the `parsed to zero BEFORE-*/AFTER-* slots` error; the exact reported message is recorded in the SUMMARY.
    - No slot uses a bare `Status: captured` with no parenthetical, and the header HTML comment names that trap.
    - The ledger header states the D-09 misattribution finding naming run `35365693716` and the D-10 nine-of-ten caveat naming `example_unit_smoke`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md','utf8');const s=[...t.matchAll(/^## ((?:BEFORE|AFTER)-[A-Z0-9-]+)\s*$/gm)];if(s.length!==6)throw new Error('expected 6 slots, got '+s.length);if(/^Status:\s*captured\s*$/m.test(t))throw new Error('bare captured Status present')"</automated>
  </verify>
  <done>`COVERAGE.md` records every external-API capability decision with reasons for each subtraction; `240-EVIDENCE.md` exists with six parseable pending slots and the D-09/D-10 prose on record.</done>
  <reversibility rating="reversible">Planning documents only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions REST API → collector | Untrusted JSON crosses into `jq` and into a committed receipt |
| Local caller → collector arguments | An argument surface that admits repo/workflow/window would let the caller choose the evidence |
| Receipt JSON → `240-EVIDENCE.md` → reviewers | The receipt is the basis of a public claim that `main` is green |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-240-03 | Repudiation | `capture-green-04-evidence.sh` window selection | high | mitigate | Repo, both workflow ids, both job-name selectors and `filter` are fixed constants; only `--run-id` and `--output` (plus the window bounds bounded by the dispatch run) are arguments. `head_sha` + `clean_tree` are recorded in the receipt so the claim is self-dating (D-13). |
| T-240-03b | Information Disclosure | Silent pagination truncation | high | mitigate | `per_page=100` on every call plus `total_count_disagreement`, `nonterminal_empty_page` and `absent_terminal_empty_page` assertions copied from `capture-terminal-ratification-evidence.sh:47-92`. A silently short list becomes a hard failure. |
| T-240-03c | Spoofing | Run-level vs job-level conclusion | high | mitigate | D-08: select by job `name` from `/runs/{id}/jobs`; never read the run conclusion as the verdict. `admin_eval_render` makes run-level `main` red for a reason outside `ci-gate`. |
| T-240-03d | Tampering | `jq` over untrusted response text | medium | mitigate | Response bodies are only ever piped to `jq` with `-e` schema assertions; never `eval`ed and never interpolated into a further shell command. |
| T-240-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager dependency; the collector uses only `gh`, `jq`, `git` and `bash`, all already required repo-wide. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Symbol | Kind |
|---|---|
| `scripts/ci/capture-green-04-evidence.sh` | new collector |
| `scripts/ci/capture-green-04-evidence.test.sh` | new hermetic self-test |
| `fail`, `request_page`, `validate_manifest`, `collect_pages` | shell functions in the collector |
| `--output`, `--run-id`, `--main-window-start`, `--main-window-end` | CLI flags on the collector |
| `REPO`, `EVIDENCE_WORKFLOW`, `CI_WORKFLOW`, `JOB_NAME`, `CI_JOB_NAME`, `CI_GATE_JOB_NAME`, `JOBS_FILTER`, `MIN_LEGS`, `MAX_PAGES`, `SCHEMA_VERSION` | fixed collector constants |
| `dirty_tree`, `evidence_run_head_sha_is_not_final_committed_head`, `insufficient_legs`, `leg_without_conclusion`, `foreign_run_id`, `matrix_repeat_set_mismatch`, `rate_limit_too_low`, `gh_not_found`, `jq_not_found` | new failure tokens |
| `FAKE_MODE`, `FAKE_GH_LOG`, `FAKE_HEAD_SHA`, `FAKE_TOTAL_COUNT` | stub env vars in the collector self-test |
| `ok`, `truncated`, `empty_jobs`, `leg_in_progress`, `dirty_tree`, `head_mismatch`, `rate_limited` | `FAKE_MODE` values |
| `capture-green-04-evidence.test: PASS` | terminal PASS line |
| `sigra.green-04-evidence/v1` | receipt `schema_version` |
| `schema_version`, `repository`, `head_sha`, `clean_tree`, `sc1.workflow`, `sc1.dispatch_run_id`, `sc1.job_name`, `sc1.jobs_filter`, `sc1.legs[].run_id`, `sc1.legs[].job_id`, `sc1.legs[].matrix_repeat`, `sc1.legs[].url`, `sc1.legs[].conclusion`, `sc1.leg_count`, `sc1.verdict`, `sc2.workflow`, `sc2.branch`, `sc2.window.start`, `sc2.window.end`, `sc2.runs[]`, `sc2.ci_gate_conclusions`, `sc2.flake_attributable_red_count`, `sc2.caveat` | receipt JSON keys |
| `.planning/phases/240-green-main-evidence-honest-pages-script/COVERAGE.md` | new API-coverage matrix |
| `.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md` | new ledger |
| `BEFORE-MAIN-RED-MISATTRIBUTION`, `AFTER-PAGES-LOUD-RED`, `AFTER-P20-GUARD-OBSERVED`, `AFTER-GREEN-04-N20`, `AFTER-CI-GATE-MAIN-WINDOW`, `AFTER-ISSUE-231-CLOSED` | ledger slot ids |
</artifacts_this_phase_produces>

<verification>
- `bash scripts/ci/capture-green-04-evidence.test.sh` exits 0 with its PASS line.
- `bash -n scripts/ci/capture-green-04-evidence.sh` exits 0.
- `240-EVIDENCE.md` parses to exactly six slots; p12's only complaint is the captured-count floor.
- `COVERAGE.md` has a reason on every `OPT-OUT` row.
- `MIX_ENV=test mix ci` green before push.
</verification>

<success_criteria>
The machinery that will read SC-1 and SC-2 exists, cannot be steered by its caller, fails closed on
every truncation and timing hazard, and its output shape is fixed before a single runner-minute is
spent.
</success_criteria>

<output>
Create `.planning/phases/240-green-main-evidence-honest-pages-script/240-03-SUMMARY.md` when done.
</output>
