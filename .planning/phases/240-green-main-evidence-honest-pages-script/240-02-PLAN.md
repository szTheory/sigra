---
phase: 240-green-main-evidence-honest-pages-script
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/green-04-evidence.yml
  - scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs
  - test/fixtures/prohibitions/p20-green-04-step-drift.yml
autonomous: true
requirements: [GREEN-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.github/workflows/green-04-evidence.yml` exists, is `on: workflow_dispatch` only, and its single job carries `if: github.ref == 'refs/heads/main'` at JOB level plus `strategy: {fail-fast: false, max-parallel: 5, matrix: {repeat: [1..20]}}`."
    - "Each of the 20 matrix legs uploads artifacts under a name unique to that leg — the four artifact names carry a `-${{ matrix.repeat }}` suffix — so two legs finishing at the same moment cannot collide on a duplicate artifact name within one run (the mandatory D-02 deviation)."
    - "`max-parallel: 5` is present verbatim, so an interrupted or queued leg never consumes more than 5 of the account-wide concurrent-job ceiling; wall clock is 4 sequential waves."
    - "`node --test --test-reporter=tap scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs` passes against the real workflow pair."
    - "The same guard, pointed at `test/fixtures/prohibitions/p20-green-04-step-drift.yml` via `GSD_PROHIB_SUBJECT`, fails — observed RED in the commit the guard is born (ROADMAP standing constraint 6)."
    - "The guard carries a non-vacuity floor: a parse yielding zero steps from either side is reported as a failure, never as a pass."
  artifacts:
    - .github/workflows/green-04-evidence.yml
    - scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs
    - test/fixtures/prohibitions/p20-green-04-step-drift.yml
  key_links:
    - "The guard is picked up with ZERO workflow edits by the prohibitions glob at `ci.yml:383-393`."
    - "**THE JOB-NAME CONTRACT (stated once here; plans 03 and 04 restate it verbatim):** a matrix job's Actions-API `name` is the workflow's `name:` value **plus a ` (N)` matrix suffix** — `ci.yml:556-560` records the rule (*\"A bare matrix on a named job emits `Library tests (1)`/`(2)` — never a bare `Library tests`\"*). Because `green_04_evidence_repeat` carries both `name: Generated admin Playwright smoke (GREEN-04 repeat)` and `strategy.matrix.repeat`, `GET /runs/{id}/jobs` returns twenty jobs named `Generated admin Playwright smoke (GREEN-04 repeat) (1)` … `(20)`. A collector selecting on `.name == \"…(GREEN-04 repeat)\"` therefore harvests **zero** legs. Every selector in this phase must be **prefix/anchored-regex** (`^<name> \\((\\d+)\\)$`), never byte equality."
  prohibitions:
    - "MUST NOT let the evidence workflow become a sibling harness. Twenty green runs of a job whose step list has drifted from `ci.yml`'s `generated_admin_playwright_smoke` say nothing about the lane that reds `ci-gate` — the proof's validity rests entirely on the copy staying a copy, and a comment alone is the 'green gate that verified nothing' pattern this milestone exists to remove."
---

<objective>
Ship the dispatch-only n≥20 evidence workflow as a byte-faithful copy of `ci.yml`'s
`generated_admin_playwright_smoke` job, and mechanically defend that faithfulness with a new
offline prohibition guard (`p20`) demonstrated RED against a committed known-bad fixture.

Purpose: D-02's premise — *"any drift makes the proof worthless"* — is unenforceable by comment.
Output: one new workflow, one new guard, one new RED fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-CONTEXT.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-PATTERNS.md
</context>

## Documented exceptions to D-02 (record these inline in the workflow file as comments)

D-02 asks for a **byte-faithful** copy. Three deviations are mandatory, not optional, and each
must carry its reason as a comment in `green-04-evidence.yml`:

1. **Artifact names must be suffixed `-${{ matrix.repeat }}`** (4 steps: `ci.yml:1497`, `:1506`,
   `:1518`, `:1527`). `actions/upload-artifact` at the pinned v7 major **errors** on a duplicate
   artifact name within one run, so a literal copy under a 20-leg matrix fails on leg 2.
2. **`needs: release_ref_guard` (`ci.yml:1421`) cannot be copied** — that job does not exist in the
   new workflow and an unresolvable `needs:` is a workflow-validation error. Its role (restricting
   dispatch) is replaced by D-01's **job-level** `if: github.ref == 'refs/heads/main'`, per the
   precedent at `fast-01-remeasurement-evidence.yml:11`.
3. **The workflow-level `concurrency` block (`ci.yml:53-55`) is omitted entirely.** Its group key
   references `github.event.pull_request.number`, meaningless on a dispatch-only workflow, and
   `cancel-in-progress: true` would let a second dispatch kill an in-flight 20-leg matrix. Both
   existing dispatch-only evidence workflows omit it.

Resolved discretion (RESEARCH OQ3, accepted): the four `github.ref != 'refs/heads/main'` upload
legs are **kept verbatim** even though the job-level `if` makes them dead. Byte-faithfulness is
D-02's point, the dead legs cost nothing, and keeping them means `p20` needs no allowance beyond
the artifact-name normalisation.

## ROADMAP scope-discipline reconciliation (state this in writing)

ROADMAP's scope-discipline line reads *"Reuse the FAST-01 n=52 machinery; build no new harness."*
This plan authors a new workflow and Plan 03 authors a new collector, so a reviewer reading only the
ROADMAP will read both as violating a binding constraint. They do not, and the reconciliation must
be on the record:

- **D-01** (locked, dated after the ROADMAP line) explicitly selects a **new dispatch-only evidence
  workflow** rather than a matrix on the live `ci.yml` job — and **D-06 (rejected alternative,
  recorded; nothing to implement)** records why the live-job matrix was refused: `ci.yml:556-560`
  documents that a bare matrix suffixes the job name, which churns the PR lane and moves a job id
  Phase 241 SC-3/SC-5 is about to pin. **D-05 (rejected alternative, recorded; nothing to
  implement)** likewise refuses dispatching `ci.yml` itself 20x (≈20x full-DAG cost, and it re-runs
  `admin_eval_render` — currently red on `main` — polluting the very run list SC-2 reads).
- **D-12** (locked) directs the collector to be **copied from** `capture-fast-01-remeasurement.sh` /
  `capture-terminal-ratification-evidence.sh` rather than extending them, because
  `capture-fast-01-remeasurement.sh`'s cutoff SHA and timestamp at `:5-11` are protected constants
  pinned by `test/sigra/planning/phase_235_fast_01_*_contract_test.exs`; editing it reopens v1.47
  debt Phase 241 owns.

So the FAST-01 machinery **is** reused — as a copied pagination/emission core with identical
fail-closed semantics — while the two artifacts that must be new are new for reasons the locked
decisions name. The later-dated locked decisions govern.

## Structural parity guard (D-03)

D-03 puts a structural step-list parity guard "in scope if cheap" — new guards drop into
`scripts/ci/prohibitions/*.test.mjs` with zero workflow edits (the glob at `ci.yml:383-393`,
ROADMAP standing constraint 5). It is cheap, so Task 2 builds it: `p20`.

<tasks>

<task type="auto">
  <name>Task 1: Author `.github/workflows/green-04-evidence.yml`</name>
  <files>.github/workflows/green-04-evidence.yml</files>
  <read_first>
    - .github/workflows/ci.yml:1401-1533 (the entire `generated_admin_playwright_smoke` job — the source of truth being copied), plus `:36-37` (workflow-level `permissions: contents: read`) and `:53-55` (the `concurrency` block that must NOT be copied)
    - .github/workflows/fast-01-remeasurement-evidence.yml:1-19 (dispatch-only evidence-workflow skeleton; `:11` is the job-level `if: github.ref == 'refs/heads/main'` shape to copy)
    - .github/workflows/generated-app-login-runtime-proof.yml:9-33 (accepted precedent for duplicating generated-host `ci.yml` steps into a dispatch-only workflow, including the same `services.postgres` block and pinned action SHAs)
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-RESEARCH.md §1 (the 11-step table, the job-level key table, and the "What CANNOT be copied verbatim" flag list)
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-PATTERNS.md — the `green-04-evidence.yml` section (matrix keys, artifact-upload pattern, pinned-SHA list)
  </read_first>
  <action>
Create `.github/workflows/green-04-evidence.yml` with `name: GREEN-04 evidence (n>=20 repeat)`,
`on: workflow_dispatch` and nothing else, workflow-level `permissions: contents: read` only (no
`id-token`, no `attestations`, no `actions: write` — least privilege, ASVS V4), and **no**
`concurrency` block.

Declare exactly one job with id `green_04_evidence_repeat` and
`name: Generated admin Playwright smoke (GREEN-04 repeat)`. The distinct name is load-bearing: it
is the **prefix** Plan 03's collector selects on, and it guarantees the harvest can never
accidentally ingest a real `ci.yml` job. Record the job-name contract from this plan's `key_links`
as a comment beside the `name:` key: because this job also carries `strategy.matrix`, the Actions
API reports each leg as `<this name> (N)` for N = 1..20 (`ci.yml:556-560`), so downstream selectors
must anchor on the prefix and read `N` from the suffix — never compare for byte equality.
Job-level keys: `if: github.ref == 'refs/heads/main'`,
`runs-on: ubuntu-latest`, `timeout-minutes: 15`, and the `services.postgres` block copied verbatim
from `ci.yml:1422-1430` (`image: postgres:15`, `env.POSTGRES_PASSWORD: postgres`,
`ports: ['5432:5432']`, and the `--health-cmd pg_isready --health-interval 10s --health-timeout 5s
--health-retries 5` options). Add
`strategy: { fail-fast: false, max-parallel: 5, matrix: { repeat: [1, 2, ..., 20] } }` with the 20
integers listed explicitly. Comment `max-parallel: 5` as load-bearing: GitHub's concurrent-job
ceiling is per-**account** (Free = 20, Pro = 40) with no public-repo exemption, so an unset
`max-parallel` would consume 100% of a Free budget and queue every job of any concurrently
triggered PR behind this matrix (D-04). Also comment the bounded cost per **D-07**: `ci.yml:1401-1407`
records this job's measured duration as **3.73m** (230-EVIDENCE.md), so 20 repeats is roughly
75-100 runner-minutes in one dispatch — a known, one-off, operator-triggered spend, not a recurring
PR-lane cost.

Copy all **thirteen** steps from `ci.yml:1401-1533` in order — three unnamed `uses:` steps, six
named steps, and the four upload steps — preserving every `uses:` pin at the
identical full commit SHA and its trailing version comment: `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1`,
`erlef/setup-beam@54075bcc5e249e4758d363f27d099f55d843f124` (with `version-file: .tool-versions`,
`version-type: strict`), `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020` (with
`node-version: '20'`, `cache: 'npm'`,
`cache-dependency-path: 'test/example/priv/playwright/package-lock.json'`), and
`actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`. Never substitute a tag for a
SHA. The named steps are, in order: `Install Hex + Rebar`, `Install phx_new archive`
(`mix archive.install --force hex phx_new 1.8.8`), `Install Playwright deps` (`npm ci` in
`test/example/priv/playwright`), `Install Playwright browsers`
(`npx playwright install --with-deps chromium webkit`), `Run generated admin acceptance smoke`
(`scripts/ci/admin-acceptance-smoke.sh --test all`, with `PGUSER`/`PGPASSWORD`/`PGHOST` and
`GITHUB_WORKSPACE: ${{ github.workspace }}`), `Collect curated generated-host admin screenshots`
(`if: always()`), then the four upload steps.

Apply the three documented D-02 exceptions above. Concretely: suffix all four artifact names so
they read `generated-admin-report-${{ matrix.repeat }}` and
`generated-admin-failure-diagnostics-${{ matrix.repeat }}`; omit `needs: release_ref_guard`;
omit `concurrency`. Keep the `retention-days: 14` / `retention-days: 7` split and all four `if:`
guards verbatim. Put a header comment at the top of the file citing `ci.yml:1401-1533` as the
source of truth, naming `scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs` as
the guard that enforces it, and listing the three exceptions with their reasons.
  </action>
  <acceptance_criteria>
    - `.github/workflows/green-04-evidence.yml` exists and contains the literal `max-parallel: 5`.
    - The file's `on:` block declares `workflow_dispatch` and no other trigger.
    - The job block contains `if: github.ref == 'refs/heads/main'` at job level and the matrix lists all 20 integers `1` through `20`.
    - The four artifact `name:` values each end with `-${{ matrix.repeat }}`; no two upload steps share an artifact name after matrix expansion.
    - Every `uses:` in the file is a 40-character commit SHA that also appears in `.github/workflows/ci.yml` — verified by reading each pin against its `ci.yml` counterpart, not by counting.
    - The file contains no `needs:` key and no `concurrency:` key **outside comment lines** — the header comment deliberately names both omissions with their reasons, so the check must strip `#`-prefixed lines before asserting absence (a bare whole-file grep is self-invalidating here).
    - Workflow-level `permissions:` is exactly `contents: read`.
    - The step names, read top to bottom, are identical to `ci.yml:1401-1533`'s step names top to bottom, and there are thirteen steps in total (three unnamed `uses:`, six named, four uploads).
    - The job's `name:` value is followed by a comment recording the job-name contract — that the Actions API reports each matrix leg as this name plus a ` (N)` suffix, citing `ci.yml:556-560` — so a later editor cannot reintroduce an equality selector downstream.
    - **Each of the three mandatory D-02 exceptions is named in the file's header comment together with its reason** — read the header and confirm three distinct entries: (1) the `-${{ matrix.repeat }}` artifact-name suffix, reason = `upload-artifact` v7 errors on a duplicate artifact name within one run; (2) the omitted `needs: release_ref_guard`, reason = unresolvable `needs:` is a workflow-validation error, role replaced by the job-level `if`; (3) the omitted workflow-level `concurrency` block, reason = its group key references a pull-request number meaningless on a dispatch-only workflow and `cancel-in-progress` would kill an in-flight 20-leg matrix. An exception present in the YAML but absent from the header is a failure of this criterion.
    - The header comment cites `ci.yml:1401-1533` as the source of truth and names `scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs` as the guard that enforces it.
    - The `max-parallel: 5` line carries its own reason comment naming the per-account concurrent-job ceiling (D-04).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const raw=fs.readFileSync('.github/workflows/green-04-evidence.yml','utf8');for(const n of ['max-parallel: 5','workflow_dispatch','ci.yml:1401-1533','p20-green-04-evidence-step-parity.test.mjs'])if(!raw.includes(n))throw new Error('missing: '+n);const y=raw.split('\n').filter(l=>!/^\s*#/.test(l)).join('\n');if(!/if:\s*github\.ref == 'refs\/heads\/main'/.test(y))throw new Error('job-level if missing');if(/^\s*needs:/m.test(y))throw new Error('needs: key present outside comments');if(/^\s*concurrency:/m.test(y))throw new Error('concurrency key present outside comments');const sh=[...y.matchAll(/uses:\s*\S+@[0-9a-f]{40}/g)];if(sh.length!==4)throw new Error('expected 4 SHA-pinned uses, got '+sh.length);const art=[...y.matchAll(/name:\s*generated-admin-[a-z-]+/g)];const suf=[...y.matchAll(/name:\s*generated-admin-[a-z-]+-\\\$\{\{\s*matrix\.repeat\s*\}\}/g)];if(art.length!==4||suf.length!==4)throw new Error('artifact names: '+art.length+' found, '+suf.length+' matrix-suffixed');const steps=[...y.matchAll(/^      - (?:name:|uses:)/gm)];if(steps.length!==13)throw new Error('expected 13 steps, got '+steps.length)"</automated>
  </verify>
  <done>The dispatch-only evidence workflow exists, is a faithful copy modulo the three documented exceptions, and is pinned to `ci.yml`'s exact action SHAs.</done>
  <reversibility rating="reversible">A new workflow file; deleting it restores the prior state. It cannot fire until merged to `main` and dispatched.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The p20 step-parity guard plus its committed known-bad fixture</name>
  <files>scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs, test/fixtures/prohibitions/p20-green-04-step-drift.yml</files>
  <read_first>
    - scripts/ci/prohibitions/p15-pages-publisher-seeds-before-boot.test.mjs (whole file — `:1-31` header-comment contract, `:40-56` the `stepList()` function to copy verbatim, `:64-72` the pure-checker parse-broke guard clause, `:99-118` subject read + non-vacuity floor, `:120-139` the negative-control test shape)
    - scripts/ci/prohibitions/_lib.mjs:33-38, :70-84, :93-113, :128-146, :179-182 (`subjectPath`/`readSubject`, `readRepoFile`, `jobBlocks`, `stripYamlComments`, `jobBlock`)
    - scripts/ci/prohibitions/p19-tag-namespace-ruleset.test.mjs:14-21 (house header shape for a newly born guard)
    - test/fixtures/prohibitions/p06-fast-checks-docs-gated.yml:1-11 (fixture prose-header pattern: structurally valid so the guard's non-vacuity floor passes and the red comes from the clause under test)
    - .github/workflows/green-04-evidence.yml (as authored in Task 1)
    - .github/workflows/ci.yml:1401-1533 (the reference side of the comparison)
  </read_first>
  <behavior>
    - Against the real pair (`ci.yml#generated_admin_playwright_smoke` vs `green-04-evidence.yml#green_04_evidence_repeat`) the checker returns `null` — no parity issue.
    - Against `test/fixtures/prohibitions/p20-green-04-step-drift.yml` (the `Install Playwright browsers` step deleted) the checker returns a non-null message naming the drift.
    - If either side parses to zero steps, the checker returns a message stating the parse broke — a broken parse is never a pass.
    - Artifact `name:` values inside upload-step **bodies** differing only by the `-${{ matrix.repeat }}` suffix are normalised away and do NOT count as drift; any other body difference still reports as drift.
  </behavior>
  <action>
Create `scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs`. `p20` is the correct
id: `ls scripts/ci/prohibitions/` returns `p01`–`p17` and `p19` — there is no `p18`, and reusing a
skipped id invites "was this deleted or never born?" ambiguity.

Follow the house header contract from `p15:1-31`: restate the prohibition as a `MUST NOT` sentence
(must not let `green-04-evidence.yml`'s job body drift from `ci.yml`'s
`generated_admin_playwright_smoke` step list, because the GREEN-04 n≥20 proof is only evidence
about `ci-gate`'s lane while the two step lists agree), name the subject
(`.github/workflows/green-04-evidence.yml`, redirectable via `GSD_PROHIB_SUBJECT`), and state
`STRUCTURAL AND OFFLINE BY DESIGN — no gh, no token, no network` (standing constraint: guards never
hit a live API on the PR critical path).

Import only `node:test`, `node:assert/strict`, and `./_lib.mjs`'s `readSubject`, `readRepoFile`,
`jobBlock` and `stripYamlComments`. Add no dependency — guards are dependency-free `node --test` by
house rule, and the glob at `ci.yml:383-393` runs them with zero install. Copy `stepList()`
verbatim from `p15:40-56`; it yields ordered `{name, hasCondition, text}` records. Strip YAML
comments on **both** sides before any content assertion — comment prose containing a step's tokens
is a documented false-match vector.

Write a pure `parityIssue(refSteps, evSteps)` checker so the real subject and the fixture run
through identical code (the p15/p19 house idiom). It must: return a parse-broke message when either
list is empty; report a step-count mismatch naming both counts; report the first index where the
**step names** differ, naming both; and finally assert that the four load-bearing command
substrings — `npm ci`, `npx playwright install --with-deps chromium webkit`,
`mix archive.install --force hex phx_new 1.8.8`, and
`scripts/ci/admin-acceptance-smoke.sh --test all` — are present on both sides or absent on both
sides, reporting any asymmetry as command drift. Return `null` only when every check passes.

**Scope the artifact-name normalisation correctly.** The `-${{ matrix.repeat }}` suffix does **not**
live on any *step name* — the copied step names (`Upload generated admin review bundle (main, 14d
retention)` and its three siblings) are byte-identical on both sides. The suffix lives inside each
upload step's **body**, on its `with.name:` value. So normalising step names for that suffix would
be a no-op and must not be written as if it did work. Instead, apply the normalisation where the
divergence actually is: when comparing the four upload steps' `text`, strip
`-${{ matrix.repeat }}` from any `name:` value inside the step body before comparing, so the
mandatory D-02 artifact-name exception is the one and only tolerated body difference and every other
body edit still reports as drift.

Emit three `test()` blocks: the behavior test (real pair → `parityIssue` is `null`), a non-vacuity
floor test (both parses find the acceptance-smoke invocation, so a pass can never come from an
empty parse), and a negative-control test that reads
`test/fixtures/prohibitions/p20-green-04-step-drift.yml` through `readRepoFile` and asserts
`parityIssue` is NOT null.

Create `test/fixtures/prohibitions/p20-green-04-step-drift.yml` as a copy of
`green-04-evidence.yml` with the `Install Playwright browsers` step deleted — the most consequential
possible drift, because the smoke would still "run", against no browsers. Give it the
`p06-fast-checks-docs-gated.yml:1-11` prose header: state that it is a KNOWN-BAD fixture for P20,
that it is structurally valid so the guard's non-vacuity floor PASSES and the red comes from the
clause under test, and name the injected defect explicitly. Keep the job id
`green_04_evidence_repeat` so `jobBlock` resolves it.

Add nothing to `ci.yml` and nothing to `mix ci` — the prohibitions glob at `ci.yml:383-393` already
picks up `scripts/ci/prohibitions/*.test.mjs` (ROADMAP standing constraint 5).
  </action>
  <acceptance_criteria>
    - `node --test --test-reporter=tap scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs` exits 0 with all three tests passing.
    - `GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p20-green-04-step-drift.yml node --test --test-reporter=tap scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs` exits **non-zero**, and the failure message names the missing `Install Playwright browsers` step (RED observed in the commit the guard is born — ROADMAP standing constraint 6).
    - `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0 (p20 does not disturb p01–p19).
    - `git diff --name-only` for this task lists no file under `.github/workflows/` and no change to `mix.exs`.
    - The guard file imports nothing outside `node:test`, `node:assert/strict` and `./_lib.mjs`.
    - `test/fixtures/prohibitions/p20-green-04-step-drift.yml` parses to a non-empty step list through `stepList(jobBlock(...))` — confirmed by the non-vacuity floor test passing against it in the negative-control path.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs && ! GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p20-green-04-step-drift.yml node --test --test-reporter=tap scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs</automated>
  </verify>
  <done>p20 exists, passes green against the real pair, and has been observed RED against a committed known-bad fixture in the same commit.</done>
  <reversibility rating="reversible">New guard + fixture; deletable.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Record the p20 RED transcript and gate locally</name>
  <files>scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs, test/fixtures/prohibitions/p20-green-04-step-drift.yml</files>
  <read_first>
    - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-EVIDENCE.md (the `## AFTER-P17-GUARD-OBSERVED` slot — the exact RED-half / shared-glob-unaffected shape Plan 04 will reuse for p20)
    - .planning/ROADMAP.md:32-40 (standing constraints 3 and 6)
    - CLAUDE.md (local development prerequisites)
  </read_first>
  <action>
Run the RED invocation and the green invocation through `bash -c` (never zsh — zsh does not
word-split unquoted parameter expansions and has produced a confident false negative in this repo
before). Record verbatim into the plan SUMMARY: the exact command line, the observed exit code, and
the assertion message for the RED run; then the exact command line and exit code for the green run
against the real pair; then the exit code of the full glob
`node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` to prove the shared glob is
unaffected. These three transcripts are the raw material for Plan 04's `AFTER-P20-GUARD-OBSERVED`
evidence slot.

Then boot the test database (`bash scripts/db/up.sh`, then `direnv allow` or
`source tmp/db.env`), confirm the phx_new 1.8.8 archive is installed, and run
`MIX_ENV=test mix ci` to green before any push.

Do not dispatch `green-04-evidence.yml` from this plan. `workflow_dispatch` is only offered once
the workflow file is on the default branch, and D-13 forces the capture to happen at the final
committed HEAD on a clean tree — both are Plan 04's responsibility.
  </action>
  <acceptance_criteria>
    - The SUMMARY contains the RED transcript with a non-zero exit code and the verbatim assertion message naming the dropped step.
    - The SUMMARY contains the green transcript with exit code 0 against the real workflow pair.
    - The SUMMARY records `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exiting 0.
    - `MIX_ENV=test mix ci` exits 0.
    - No GitHub Actions workflow was dispatched during this plan (`gh run list --workflow green-04-evidence.yml` returns no runs).
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs</automated>
    <note>`MIX_ENV=test mix ci` is the **wave gate**, asserted in `&lt;verification&gt;` and in the acceptance criteria above, not the per-task `&lt;automated&gt;` loop: it runs for minutes and blows the 30s per-task feedback-latency target (nyquist 8b). The fast, per-task sample is the prohibitions glob.</note>
  </verify>
  <done>The RED and green transcripts are recorded; the full local gate is green; no dispatch has happened.</done>
  <reversibility rating="reversible">Verification only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party GitHub Action → CI runner | Action code executes with the workflow's token and full filesystem access |
| Workflow token → repository | `GITHUB_TOKEN` scope determines what a compromised step can mutate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-240-02 | Tampering | `green-04-evidence.yml` `uses:` pins | high | mitigate | Every `uses:` is the same full 40-char commit SHA already used in `ci.yml` (`:1432`, `:1433`, `:1437`, `:1498`); never a tag. Acceptance reads each pin against its `ci.yml` counterpart. |
| T-240-02b | Elevation of Privilege | Workflow token scope | high | mitigate | Workflow-level `permissions: contents: read` only. No `id-token`, no `attestations`, no `actions: write` — the job reads and runs, it never writes repository state. |
| T-240-05 | Repudiation | Evidence-harness drift | high | mitigate | `p20` asserts step-list and command parity against `ci.yml:1401-1533` on every PR; demonstrated RED against a committed known-bad fixture before acceptance. |
| T-240-SC | Tampering | npm/pip/cargo installs | medium | accept | The copied `npm ci` step installs strictly from the committed `test/example/priv/playwright/package-lock.json`; no new package is added by this plan, so there is no `[ASSUMED]`/`[SUS]` entry to gate. `npx playwright install` fetches browser binaries from the already-pinned `@playwright/test` version. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Symbol | Kind |
|---|---|
| `.github/workflows/green-04-evidence.yml` | new file |
| `GREEN-04 evidence (n>=20 repeat)` | workflow `name:` |
| `green_04_evidence_repeat` | job id |
| `Generated admin Playwright smoke (GREEN-04 repeat)` | job `name:` — the **prefix** the collector anchors on; the Actions API reports each leg as this value plus a ` (N)` matrix suffix |
| `Generated admin Playwright smoke (GREEN-04 repeat) (1)` … ` (20)` | the twenty job names the Actions API actually emits |
| `repeat` | matrix key, values `1`–`20` |
| `generated-admin-report-${{ matrix.repeat }}`, `generated-admin-failure-diagnostics-${{ matrix.repeat }}` | artifact names |
| `scripts/ci/prohibitions/p20-green-04-evidence-step-parity.test.mjs` | new guard (`p20`) |
| `parityIssue`, `stepList`, `normalize` | functions inside the guard |
| `SUBJECT`, `REFERENCE`, `REF_JOB`, `EVIDENCE_JOB` | guard constants |
| `test/fixtures/prohibitions/p20-green-04-step-drift.yml` | new known-bad fixture |
</artifacts_this_phase_produces>

<verification>
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` green.
- p20 observed RED under `GSD_PROHIB_SUBJECT` pointed at the drift fixture.
- `green-04-evidence.yml` step names match `ci.yml:1401-1533` top to bottom; only the three documented exceptions differ.
- `MIX_ENV=test mix ci` green.
</verification>

<success_criteria>
The n≥20 harness exists and its faithfulness to the lane it claims to prove is mechanically
enforced, not asserted in a comment.
</success_criteria>

<output>
Create `.planning/phases/240-green-main-evidence-honest-pages-script/240-02-SUMMARY.md` when done.
</output>
