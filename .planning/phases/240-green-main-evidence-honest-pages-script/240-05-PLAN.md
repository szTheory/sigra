---
phase: 240-green-main-evidence-honest-pages-script
plan: 05
type: execute
wave: 4
depends_on: [240-04]
files_modified:
  - .planning/phases/240-green-main-evidence-honest-pages-script/240-ISSUE-231-CLOSURE.md
  - .planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md
  - .planning/todos/completed/2026-07-30-admin-generated-audit-presets-actor-filter-race.md
  - .planning/todos/completed/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md
  - .planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md
autonomous: false
requirements: [GREEN-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Issue #231 is CLOSED, verified by re-reading `gh issue view 231 --json state --jq '.state'` after the close, not by asserting it."
    - "The closure comment cites the n≥20 dispatch run ids from `240-GREEN-04-EVIDENCE.json`, the live Pages payload re-read at closure time, and an explicit evidence window `<start>..<end>` rather than implying permanence."
    - "`.planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md` is closed with a dated evidence section citing the CORRECTED coordinates `lib/sigra/admin/live/audit_index_live.ex` and `admin-generated.spec.ts:459`, and moved to `.planning/todos/completed/`."
    - "`.planning/todos/completed/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md` keeps its location, gains a dated evidence-append section, and its frontmatter `status:` field at line 3 is flipped from `pending` to `completed` (Phase 237's closure moved the file without updating the field). Per **D-25** this is an evidence-append, **not** a move — no `git mv` is budgeted for this file."
    - "`## AFTER-ISSUE-231-CLOSED` in `240-EVIDENCE.md` is `captured (runs …)` and p12, redirected at that ledger, exits 0 with all six slots captured."
  artifacts:
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-ISSUE-231-CLOSURE.md
    - .planning/todos/completed/2026-07-30-admin-generated-audit-presets-actor-filter-race.md
  key_links:
    - "`notify_release_lane_rot` (`ci.yml:1647-1684`) re-files an issue titled exactly `ci-gate red on main (release-lane-rot)` — the title of #231 — on any `failure` of `ci-gate` on a non-PR event. The evidence window in the comment is what makes a future re-file correct behaviour rather than a falsification of this phase's claim (D-27)."
  prohibitions:
    - "MUST NOT close a todo against a description that was never true. The pending todo's `files:` list names `lib/sigra/admin/live/audit_live.ex`, a path that does not exist, and cites `admin-generated.spec.ts:454-458` and an 'Apply filters' button HEAD's spec does not have. Closing it while repeating those coordinates records a fiction as a resolution; the closure prose must carry the corrected coordinates from **Phase 236's** `236-CONTEXT.md` D-06/D-07 — not this phase's `240-CONTEXT.md` D-06, which is an unrelated recorded rejected alternative. The phase-240 decision governing this closure is **D-26**."
    - "MUST NOT state or imply that #231 can never re-open. `notify_release_lane_rot` is a machine that re-files that exact title on any future `ci-gate` failure; a closure comment claiming permanence would be falsified by correct behaviour."
---

<objective>
Close the loop in public: comment on issue #231 with the measured run ids, the live Pages state and
an explicit evidence window; close it; verify the close by re-reading it; then close both owning
todos with the same evidence and flip the last ledger slot (GREEN-05 / SC-4).

Purpose: SC-4 is the outward-facing half of "main is green is a measured claim".
Output: a closed #231, two closed todos, and a fully captured `240-EVIDENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-CONTEXT.md
@.planning/phases/240-green-main-evidence-honest-pages-script/240-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the #231 closure comment and close both owning todos</name>
  <files>.planning/phases/240-green-main-evidence-honest-pages-script/240-ISSUE-231-CLOSURE.md, .planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md, .planning/todos/completed/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md</files>
  <read_first>
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-GREEN-04-EVIDENCE.json (the run ids, window bounds, leg table and SC-2 tally to quote)
    - .planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md (whole file — frontmatter `:1-22` including the wrong `files:` entry, and the body at `:26` and `:65` citing `admin-generated.spec.ts:454-458` and an "Apply filters" button)
    - .planning/todos/completed/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md (whole file — `:3` still reads `status: pending`; its tail is the evidence-append + `## Required owner action` pattern to follow)
    - .planning/phases/236-flake-root-cause-reproduce-name-fix/236-CONTEXT.md — **Phase 236's** D-06/D-07 (the corrected coordinates; distinct from this phase's 240-CONTEXT D-06)
    - .github/workflows/ci.yml:1647-1684 (`notify_release_lane_rot` — the machine that produced #231 and can re-file it)
  </read_first>
  <action>
Re-read the live Pages payload now, at closure time — `gh api repos/szTheory/sigra/pages` — and use
that response, not any value quoted in the phase research. Live claims in this repo are explicitly
point-in-time and expire.

Write `.planning/phases/240-green-main-evidence-honest-pages-script/240-ISSUE-231-CLOSURE.md` as
the comment body for `gh issue comment 231 --body-file`. It must contain: the dispatch run id and
the full list of the n≥20 leg run/job ids from `240-GREEN-04-EVIDENCE.json` with their
conclusions; the SC-2 per-lane job table over the `main` window with the `ci_gate_conclusions`
tally and `flake_attributable_red_count`; the D-10 caveat that `example_unit_smoke` is absent from
the ten `ci-gate.needs` entries at `ci.yml:1547-1557` while being independently required by ruleset
14941512, so `ci-gate: success` is a nine-of-ten claim disclosed here and fixed by its own todo; the
freshly re-read live Pages payload verbatim; a one-paragraph note that
`scripts/ci/ensure-github-pages-legacy-branch.sh` now exits 1 on every non-2xx Pages status except
the single documented `pages: write`-is-not-repo-admin PUT-403, with the stubbed RED transcripts
named; and an explicit evidence window written as `runs <ids>, <start>..<end>`.

State plainly in the comment that `notify_release_lane_rot` (`ci.yml:1647-1684`) will re-file an
issue with this exact title on any future `failure` of `ci-gate` on a non-PR event, and that such a
re-file is correct behaviour rather than a falsification of this window's claim. Do not write
anything that implies permanence. This repository is public — no adopter PII, and no values beyond
the CI facts being cited.

Close `.planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md`:
append a dated evidence section in the file's own voice (there is no repo-wide `## Resolution`
convention; completed todos append evidence in their existing voice), citing the corrected
coordinates `lib/sigra/admin/live/audit_index_live.ex` and `admin-generated.spec.ts:459` —
**not** the file's own `lib/sigra/admin/live/audit_live.ex`, which does not exist, and **not**
`:454-458` or the "Apply filters" button, which HEAD's spec does not have. Note inside the appended
section that the original `files:` entry and line citations were stale and were corrected per
**Phase 236's** CONTEXT decisions D-06/D-07 (i.e.
`.planning/phases/236-flake-root-cause-reproduce-name-fix/236-CONTEXT.md`, **not** this phase's
`240-CONTEXT.md`, whose D-06 is an unrelated recorded rejected alternative). This phase's own
governing decision here is **D-26**. Cite the phase-240 evidence: the n≥20 green legs and the `main` window
with zero flake-attributable reds. Flip its frontmatter `status:` to `completed` and move the file
to `.planning/todos/completed/` with `git mv`.

For `.planning/todos/completed/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md`:
per **D-25** this is a **verification plus evidence-append, not a move** — the file already lives
under `completed/` (Phase 237 closed it), and D-25 says explicitly that a plan budgeting a move for
it is burning a step. D-25 is also why only the *other* todo
(`2026-07-30-admin-generated-audit-presets-actor-filter-race.md`) is treated as still pending. Append a dated evidence section following its existing tail
pattern (a fenced verbatim log excerpt, then the prose), recording the freshly re-read live Pages
payload and the fact that the publisher script can no longer report success while the site stays
broken. Additionally flip its frontmatter `status:` field at line 3 from `pending` to `completed`,
and note in the appended section that the Phase 237 closure moved the file without updating that
field.

Do not create the missing `release-lane-rot` GitHub label. It is a named, not-fixed adjacent defect
owned by `.planning/todos/pending/2026-07-28-release-lane-rot-label-missing-breaks-hard-02-signal.md`
(D-28; ROADMAP standing constraint 4).
  </action>
  <acceptance_criteria>
    - `240-ISSUE-231-CLOSURE.md` exists and contains the dispatch run id, at least 20 leg identifiers, the string `example_unit_smoke`, the freshly re-read Pages payload, and an evidence window in the form `runs …, <start>..<end>`.
    - `240-ISSUE-231-CLOSURE.md` names `notify_release_lane_rot` and states that a future re-file is correct behaviour.
    - `grep -n 'audit_index_live.ex' .planning/todos/completed/2026-07-30-admin-generated-audit-presets-actor-filter-race.md` returns a hit inside the appended evidence section, and the appended section contains `admin-generated.spec.ts:459`.
    - The appended section explicitly states that the file's original `files:` entry and `:454-458` citation were stale.
    - `.planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md` no longer exists; `.planning/todos/completed/2026-07-30-admin-generated-audit-presets-actor-filter-race.md` does, and its frontmatter `status:` reads `completed`.
    - `sed -n '3p' .planning/todos/completed/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md` reads `status: completed`, and the file remains under `completed/` (no move occurred).
    - `.planning/todos/pending/2026-07-28-release-lane-rot-label-missing-breaks-hard-02-signal.md` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/todos/completed/2026-07-30-admin-generated-audit-presets-actor-filter-race.md && ! test -f .planning/todos/pending/2026-07-30-admin-generated-audit-presets-actor-filter-race.md && grep -q 'admin-generated.spec.ts:459' .planning/todos/completed/2026-07-30-admin-generated-audit-presets-actor-filter-race.md && sed -n '3p' .planning/todos/completed/2026-07-29-github-pages-source-builds-main-root-not-gh-pages.md | grep -q 'status: completed'</automated>
  </verify>
  <done>The comment body is drafted against live data, and both owning todos are closed with the corrected coordinates.</done>
  <reversibility rating="reversible">Local files only; nothing has been posted publicly yet.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: One-way gate — authorize the public #231 comment</name>
  <decision>Post the drafted comment to public issue #231 and close it now.</decision>
  <context>
    This is the phase's only one-way action. A comment on a public GitHub issue notifies watchers
    and is archived by third parties the moment it is posted; deleting it afterwards does not
    unsend it. Closing the issue is itself reversible (it can be reopened), but the comment is not.
    The comment body is at
    `.planning/phases/240-green-main-evidence-honest-pages-script/240-ISSUE-231-CLOSURE.md` — read
    it before deciding, and check that the run ids, the Pages payload and the evidence window are
    the ones you expect, and that it contains no value you would not want permanently public.
  </context>
  <options>
    <option id="post-now">
      <name>Post the comment and close #231</name>
      <pros>Completes SC-4; the run ids and the window are on the public record where the issue's readers are.</pros>
      <cons>Irreversible publication; any error in the cited ids or window is permanent.</cons>
    </option>
    <option id="revise-first">
      <name>Revise the comment body first</name>
      <pros>Catches a wrong run id or an unwanted disclosure before it is permanent.</pros>
      <cons>Delays closure; the live Pages payload will need re-reading if the delay is long.</cons>
    </option>
    <option id="defer">
      <name>Defer the public closure to a follow-up</name>
      <pros>Keeps the phase's code and evidence work landed without an irreversible step.</pros>
      <cons>SC-4 is unmet and GREEN-05 stays open; the phase cannot be verified complete.</cons>
    </option>
  </options>
  <resume-signal>Select: post-now, revise-first, or defer.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator comments on, closes, and re-reads issue #231</name>
  <what-built>`240-ISSUE-231-CLOSURE.md` holds the closure comment body: the n≥20 run ids, the SC-2 `main`-window job table, the disclosed `example_unit_smoke` caveat, the freshly re-read live Pages payload, and an explicit evidence window. Both owning todos are already closed locally with the corrected coordinates.</what-built>
  <how-to-verify>
    Run these three commands in this order (D-24 — comment, then close, then re-read):
    1. `gh issue comment 231 --body-file .planning/phases/240-green-main-evidence-honest-pages-script/240-ISSUE-231-CLOSURE.md`
    2. `gh issue close 231`
    3. `gh issue view 231 --json state --jq '.state'` — this must print `CLOSED`. The re-read is the
       verification artifact; do not substitute the exit code of step 2.
    Also re-read `gh api repos/szTheory/sigra/pages` one final time and paste the payload into the
    resume message, so the ledger's closing slot quotes a payload observed after the close rather
    than before it.
  </how-to-verify>
  <resume-signal>Reply with the output of step 3, the comment URL, and the final Pages payload — or `blocked: <reason>`.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Flip the final ledger slot and prove the whole ledger parses</name>
  <precondition>`gh issue view 231 --json state --jq '.state'` returns `CLOSED` and the closure comment URL is known.</precondition>
  <files>.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md (five captured slots plus the pending `## AFTER-ISSUE-231-CLOSED`)
    - scripts/ci/prohibitions/p12-run-id-provenance.test.mjs:44-60 (the `Status:` grammar, the corroboration rule, the fenced-block rule, the non-vacuity floors)
  </read_first>
  <action>
Flip `## AFTER-ISSUE-231-CLOSED` from `pending (…obligation…)` to
`Status: captured (runs <the same dispatch and main-window run ids cited in the comment>)`. The body
must contain: the closure comment URL, the verbatim output of `gh issue view 231 --json state`
showing `CLOSED`, the final live Pages payload observed after the close, and a fenced block holding
the three `gh issue` commands that were run. Every run id named in the `Status:` line must appear at
least once more inside this slot's body — the corroboration rule counts the `Status:` occurrence
plus at least one other.

Re-run p12 redirected at the ledger and require it green with all six slots now `captured`:
`GSD_PROHIB_SUBJECT=.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md
node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs`. Then run
`node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` to confirm the shared glob is
unaffected, and `MIX_ENV=test mix ci` before the final push.

Record in the SUMMARY that p12 remains hardcoded to the Phase 230 ledger at `p12:26` and was run
here only via `GSD_PROHIB_SUBJECT`, so 240's ledger is enforced by an explicit evidence step and not
by the PR lane. Generalising p12 to a ledger glob is Phase 241's call and is already owned by
`.planning/todos/pending/2026-09-15-p12-evidence-guard-is-pinned-to-phase-230.md`.
  </action>
  <acceptance_criteria>
    - All six ledger slots have a `Status:` line beginning `captured (run ` or `captured (runs `; none is bare and none is still `pending`.
    - `GSD_PROHIB_SUBJECT=.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` exits 0.
    - `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` exits 0.
    - `MIX_ENV=test mix ci` exits 0.
    - `## AFTER-ISSUE-231-CLOSED` contains the closure comment URL, the literal `CLOSED`, and the post-close Pages payload.
    - The SUMMARY states that p12 was run via `GSD_PROHIB_SUBJECT` and names the todo that owns generalising it.
  </acceptance_criteria>
  <verify>
    <automated>GSD_PROHIB_SUBJECT=.planning/phases/240-green-main-evidence-honest-pages-script/240-EVIDENCE.md node --test --test-reporter=tap scripts/ci/prohibitions/p12-run-id-provenance.test.mjs && node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs</automated>
  </verify>
  <done>Every ledger slot is captured with its run ids, and the ledger parses green under the guard that enforces run-id provenance.</done>
  <reversibility rating="reversible">Documentation edit; the irreversible step already happened under its own decision checkpoint.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator credentials → public issue #231 | A comment and a close on a public repository, visible and archived externally |
| Local evidence receipt → public claim | Values quoted from `240-GREEN-04-EVIDENCE.json` become permanently public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-240-04 | Repudiation | The #231 closure claim | high | mitigate | The comment cites run ids and an explicit `runs …, <start>..<end>` window rather than implying permanence, and names `notify_release_lane_rot` (`ci.yml:1647-1684`) as the machine that may correctly re-file the same title (D-27). The close is verified by re-reading `gh issue view 231 --json state`, never by the close command's exit code. |
| T-240-04b | Information Disclosure | Public comment content | high | mitigate | This repository is public. The comment carries only CI facts — run ids, job conclusions, and the Pages API payload. A blocking `checkpoint:decision` precedes publication so the body is read before it is permanent. |
| T-240-04c | Repudiation | Todo closed against a false description | medium | mitigate | The pending todo's `files:` entry and line citations are stale; the closure prose carries the corrected `audit_index_live.ex` / `admin-generated.spec.ts:459` coordinates and says so explicitly (D-26). |
| T-240-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; it edits markdown and calls `gh`. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Symbol | Kind |
|---|---|
| `.planning/phases/240-green-main-evidence-honest-pages-script/240-ISSUE-231-CLOSURE.md` | new comment-body file |
| `.planning/todos/completed/2026-07-30-admin-generated-audit-presets-actor-filter-race.md` | moved + evidence-appended todo |
| The `## AFTER-ISSUE-231-CLOSED` slot flipped to `captured (runs …)` | ledger state transition |
| The public closure comment on issue #231 | outward-facing artifact (URL recorded in the ledger) |
</artifacts_this_phase_produces>

<verification>
- `gh issue view 231 --json state --jq '.state'` prints `CLOSED`.
- Both owning todos are closed; neither remains under `.planning/todos/pending/`.
- p12, redirected at `240-EVIDENCE.md`, exits 0 with six captured slots.
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` and `MIX_ENV=test mix ci` green.
</verification>

<success_criteria>
GREEN-05 is complete: issue #231 is closed against measured evidence with an explicit window, both
owning todos are closed with corrected coordinates, and the ledger's every slot names the run ids
it was read from.
</success_criteria>

<output>
Create `.planning/phases/240-green-main-evidence-honest-pages-script/240-05-SUMMARY.md` when done.
</output>