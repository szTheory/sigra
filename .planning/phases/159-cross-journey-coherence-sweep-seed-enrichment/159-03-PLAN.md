---
phase: 159-cross-journey-coherence-sweep-seed-enrichment
plan: 03
type: execute
wave: 2
depends_on:
  - 159-01
  - 159-02
files_modified:
  - test/example/lib/example/demo/seeds.ex
  - test/example/test/example/demo/seeds_test.exs
autonomous: true
requirements:
  - FIXT-01
  - FIXT-02
  - FIXT-03
  - FIXT-04
  - FIXT-05

must_haves:
  truths:
    - "Seeds.run/0 seeds an expired Acme invitation for expired-invite@demo.sigra.dev with expires_at in the real past"
    - "Seeds.run/0 seeds grace as an Acme member with deleted_at/scheduled_deletion_at set"
    - "Seeds.run/0 seeds a passkey row for pat (passkey-only persona)"
    - "Seeds.run/0 appends 4 new persona audit events using reserved-prefix action strings and @seed_reference_ts offsets 30-33"
    - "Seeds.run/0 called twice on a fresh DB yields identical snapshot_counts"
    - "seeds_test.exs asserts all four new seed states and the idempotency test covers them"
  artifacts:
    - path: "test/example/lib/example/demo/seeds.ex"
      provides: "enriched seed orchestrator"
      contains: "expired-invite@demo.sigra.dev"
    - path: "test/example/test/example/demo/seeds_test.exs"
      provides: "new assertions for all four FIXT states + updated snapshot_counts"
      contains: "expired_invitations"
  key_links:
    - from: "seeds.ex:@persona_audit_events"
      to: "seeds.ex:seed_audit_events/2 count-threshold guard (line 548)"
      via: "length(@audit_actions) + length(@persona_audit_events) auto-covers new rows"
      pattern: "length\\(@persona_audit_events\\)"
    - from: "seeds.ex:seed_passkey/1"
      to: "personas.ex:all/0 — pat with passkey: true"
      via: "extended seed_passkey/1 looks up pat@demo.sigra.dev in users map"
      pattern: "pat@demo.sigra.dev"
    - from: "seeds_test.exs:snapshot_counts/0"
      to: "seeds_test.exs idempotency test (first == second)"
      via: "expired_invitations and passkeys keys cover new seed rows"
      pattern: "expired_invitations"
---

<objective>
Wire the two new personas (from Plan 02) into the seed orchestrator and add all enriched
seed data: expired invitation (FIXT-01), grace Acme membership (FIXT-02 data), pat passkey
(FIXT-03), richer audit events (FIXT-04). Update seeds_test.exs with new assertions and
snapshot_counts coverage (FIXT-05 lockstep).

Purpose: This plan delivers ALL the demo database enrichment that makes previously-empty
UI states visible. Plan 01 delivered the code path; this plan delivers the data.

Output: Enriched seeds.ex + updated seeds_test.exs. Running mix test exits 0 with
seeds_test passing including idempotency.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-CONTEXT.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-RESEARCH.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-PATTERNS.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-01-SUMMARY.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From test/example/lib/example/demo/seeds.ex (current state):
- @seed_reference_ts = ~U[2026-05-15 12:00:00Z] (line 39) — all occurred_at MUST use DateTime.add from this
- @audit_actions list: 18 entries, offsets 0–17, all admin-tied events (lines 414–433)
- @persona_audit_events list: 12 entries, offsets 18–29 (lines 435–532)
- Count-threshold guard at line 548: length(@audit_actions) + length(@persona_audit_events) = 30 currently
- seed_invitation/1 at lines 240–268: check-then-insert pattern on (org_id, email, is_nil/is_nil)
- seed_passkey/1 at lines 295–312: single admin passkey — needs pat added (extract upsert_passkey/2 helper)
- seed_memberships/3 at lines 212–230: add grace lookup + upsert call
- @persona_audit_events already has allow_reserved: true path in insert_audit_batch (lines 577, 601)
- maybe_schedule_deletion/2 clause at lines 166–181: activated by scheduled_deletion: true on any persona

From test/example/test/example/demo/seeds_test.exs (current state):
- snapshot_counts/0 at lines 38–85: has passkeys key (counts all demo-user passkeys) — will auto-increment by 1 for pat
- invitations key at line 54–57: scoped to "invited@demo.sigra.dev" ONLY — does NOT cover expired-invite row
- idempotency test at lines 88–102: first == second asserts all snapshot_counts keys identical
- persona catalog test at lines 112–123: dynamic length(Personas.all()) — auto-covers 9 personas
- membership shape test at lines 199–223: explicitly lists alice, carol, dave — must add grace assertion
- admin passkey test at lines 170–180: asserts admin has >= 1 passkey — still correct, pat adds another
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enrich seeds.ex — invitation, passkey, membership, audit rows</name>
  <files>test/example/lib/example/demo/seeds.ex</files>

  <read_first>
    Read seeds.ex lines 210–312 (seed_memberships/3, seed_invitation/1, seed_passkey/1)
    and lines 414–551 (@audit_actions, @persona_audit_events, seed_audit_events/2).
    The exact patterns for check-then-insert (invitation), upsert_passkey, and
    @persona_audit_events map shape are in PATTERNS.md — follow them exactly.
  </read_first>

  <action>
    Four changes to seeds.ex:

    CHANGE A — Extend seed_invitation/1 with a second expired invitation block (FIXT-01, per D-01):
    Inside defp seed_invitation/1, after the existing `unless existing do ... end` block,
    add a SECOND check-then-insert block for the expired invitation:
      expired_email = "expired-invite@demo.sigra.dev"
      expired_ts    = ~U[2026-01-01 00:00:00Z]
    Use the identical Repo.one/from query pattern keyed on (organization_id, expired_email,
    is_nil accepted_at, is_nil revoked_at). Insert via %OrganizationInvitation{} changeset
    with role: :member, expires_at: expired_ts. This is a PAST date — expired? will be true
    because DateTime.compare(~U[2026-01-01...], DateTime.utc_now()) == :lt.
    Do NOT alter the existing invited@demo.sigra.dev block.

    CHANGE B — Extend seed_memberships/3 with grace's Acme membership (FIXT-02, per D-03):
    In defp seed_memberships/3, add after the existing Acme members block:
      grace = users["grace@demo.sigra.dev"]
      upsert_membership(grace.id, acme.id, :member)
    Grace's org_member: :acme in personas.ex tells Plan 02's persona what her role is; this
    call actually creates the DB row.

    CHANGE C — Extend seed_passkey/1 with pat's passkey (FIXT-03, per D-02):
    Replace the current monolithic defp seed_passkey/1 with a version that:
      1. Extracts a private defp upsert_passkey(user, seed_string) helper that computes
         credential_id = :crypto.hash(:sha256, seed_string) and
         public_key = :crypto.hash(:sha256, seed_string <> "-pubkey") and inserts with
         on_conflict: :nothing, conflict_target: [:credential_id].
      2. The outer seed_passkey/1 calls upsert_passkey for both:
           admin = users["admin@demo.sigra.dev"]
           pat   = users["pat@demo.sigra.dev"]
           upsert_passkey(admin, "sigra-demo-admin-passkey-credential-id-v1")
           upsert_passkey(pat,   "sigra-demo-pat-passkey-credential-id-v1")
    The admin seed string must stay IDENTICAL to the current one (do not change it — the
    existing on_conflict: :nothing guarantees idempotency for admin's row).
    Use the same %UserPasskey{} + UserPasskey.create_changeset/2 pattern as before.

    CHANGE D — Append 4 entries to @persona_audit_events (FIXT-04, per D-04):
    After the last existing entry (offset: 29), append EXACTLY these four maps to the
    @persona_audit_events list. Use offsets 30–33 (sequential, no gaps or overlaps):
      %{email: "pat@demo.sigra.dev",   actor: "pat@demo.sigra.dev",   action: "auth.password.change",  outcome: "success", offset: 30, org: nil}
      %{email: "pat@demo.sigra.dev",   actor: "pat@demo.sigra.dev",   action: "auth.magic_link.sent",   outcome: "success", offset: 31, org: nil}
      %{email: "grace@demo.sigra.dev", actor: "grace@demo.sigra.dev", action: "api.token.create",       outcome: "success", offset: 32, org: :acme}
      %{email: "carol@demo.sigra.dev", actor: "carol@demo.sigra.dev", action: "auth.oauth.link",        outcome: "success", offset: 33, org: :acme}

    All four use reserved prefixes (auth.*, api.*) — allow_reserved: true is already present
    in insert_audit_batch. Do NOT add inline Repo.insert! calls — ALL new audit rows must live
    in the @persona_audit_events list to be covered by the count-threshold guard (FIXT-05).
    The carol row at offset 33 demonstrates a SECOND auth.oauth.link event (google provider);
    no metadata field is needed — the action string is sufficient for the presenter fallback.
  </action>

  <verify>
    <automated>grep -n "expired-invite\|grace@demo\|pat@demo\|auth.password.change\|upsert_passkey" test/example/lib/example/demo/seeds.ex</automated>
  </verify>

  <acceptance_criteria>
    - grep shows "expired-invite@demo.sigra.dev" in a seed_invitation block
    - grep shows "grace@demo.sigra.dev" in seed_memberships
    - grep shows "pat@demo.sigra.dev" in seed_passkey AND in @persona_audit_events
    - grep shows "auth.password.change" and "api.token.create" in @persona_audit_events
    - grep shows upsert_passkey (the extracted helper function)
    - mix test test/example/test/example/demo/seeds_test.exs exits 0
  </acceptance_criteria>

  <done>
    seeds.ex has expired invitation block, grace Acme membership, pat passkey via upsert_passkey/2
    helper, and 4 new @persona_audit_events entries at offsets 30-33; seeds_test.exs idempotency
    test passes (first == second on double run).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update seeds_test.exs — snapshot_counts + new state assertions</name>
  <files>test/example/test/example/demo/seeds_test.exs</files>

  <read_first>
    Read seeds_test.exs lines 38–85 (snapshot_counts/0) and lines 88–102 (idempotency test)
    and lines 199–223 (membership shape test) and lines 232–284 (audit liveness tests).
    The PATTERNS.md test block shapes for grace, pat, and expired-invite assertions are
    the canonical templates to follow.
  </read_first>

  <action>
    Three changes to seeds_test.exs:

    CHANGE A — Add expired_invitations key to snapshot_counts/0 (FIXT-05 lockstep):
    Inside defp snapshot_counts/0, after the existing `invitations:` key block, add:
      expired_invitations:
        Repo.aggregate(
          from(i in OrganizationInvitation, where: i.email == ^"expired-invite@demo.sigra.dev"),
          :count
        ),
    This key is scoped to the expired-invite email only, mirroring the invitations key pattern.
    The idempotency test (first == second) will now enforce that the expired-invite row is
    seeded exactly once and not duplicated on a second run.

    CHANGE B — Add three new persona-state test blocks to the "persona catalog + states" describe:
    Add after the existing frank test (line 139) or in a logical grouping near it:

    Test block 1 — grace (FIXT-02 + D-03):
      test "grace is a deletion-scheduled Acme member" do
        acme  = Repo.get_by!(Organization, slug: "acme-corp")
        grace = demo_user!("grace@demo.sigra.dev")
        refute is_nil(grace.deleted_at)
        refute is_nil(grace.scheduled_deletion_at)
        assert membership_role(grace.id, acme.id) == :member,
               "grace should be an Acme Corp member so in-roster deletion pill renders"
      end

    Test block 2 — pat (FIXT-03 + D-02):
      test "pat has no MFA credential but has a passkey row" do
        pat = demo_user!("pat@demo.sigra.dev")
        mfa_count = Repo.aggregate(from(c in UserMFACredential, where: c.user_id == ^pat.id), :count)
        assert mfa_count == 0
        passkey_count = Repo.aggregate(from(p in UserPasskey, where: p.user_id == ^pat.id), :count)
        assert passkey_count >= 1
      end

    Test block 3 — expired invitation (FIXT-01 + D-01):
      test "exactly one expired invitation for expired-invite@demo.sigra.dev" do
        expired = Repo.all(
          from i in OrganizationInvitation,
            where: i.email == ^"expired-invite@demo.sigra.dev" and is_nil(i.accepted_at) and is_nil(i.revoked_at)
        )
        assert length(expired) == 1
        expired_row = hd(expired)
        assert DateTime.compare(expired_row.expires_at, DateTime.utc_now()) == :lt,
               "expired invitation expires_at should be in the past"
      end

    CHANGE C — Add grace to the membership shape test (lines 199–223):
    In the existing test "membership shape: admin in 2 orgs, alice+carol+dave in Acme, bob owns Beta",
    add after the existing Acme assertions:
      grace = demo_user!("grace@demo.sigra.dev")
      assert membership_role(grace.id, acme.id) == :member,
             "grace should be an Acme member for the roster deletion pill to render"

    Do NOT modify the `>=15` admin_tied assertion (line 241) or `alice_tied >= 3` assertion
    (line 270) — both are already >= bounds and auto-cover new audit rows. Do NOT change the
    persona count assertion `first.demo_users == length(Personas.all())` — it auto-derives.
  </action>

  <verify>
    <automated>mix test test/example/test/example/demo/seeds_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - mix test test/example/test/example/demo/seeds_test.exs exits 0
    - All existing tests pass (idempotency, persona catalog, audit liveness, security posture)
    - New tests pass: grace deletion-scheduled membership, pat no-MFA + passkey, expired invitation in past
    - Idempotency test: first == second includes expired_invitations key (count stays at 1 on double run)
    - seeds_test.exs:100 still asserts first.demo_users == length(Personas.all()) — passes with 9 personas
  </acceptance_criteria>

  <done>
    seeds_test.exs passes all tests; snapshot_counts has expired_invitations key; three new
    persona-state tests pass for grace, pat, and expired-invite; idempotency test covers all
    new seed states; full suite mix test exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeds.ex → DB | Seed writes are wrapped in transactions; MIX_ENV=test guard in priv/repo/seeds.exs prevents seeds from running in CI test suite |
| seeds_test.exs → seeds.ex | Tests call Seeds.run/0 directly inside SQL Sandbox — writes roll back at test end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-04 | Spoofing / Elevation | Demo credentials in seed data (pat, grace new passwords) | accept | All demo credentials are intentionally public-by-design; module docstring says "Never use in production"; no new pattern introduced |
| T-159-05 | Information Disclosure | MIX_ENV=test guard in priv/repo/seeds.exs | accept | RESEARCH.md confirms guard is at line ~5 of seeds.exs script (Mix.env() == :test raise); Seeds module itself has no guard — tests call it inside sandbox. Guard intact, not touched by this plan. |
| T-159-06 | Tampering | audit rows with allow_reserved: true | accept | All reserved-prefix action strings are legitimate Sigra auth events; allow_reserved: true is already present on every audit changeset call in seeds.ex; no new bypass |
| T-159-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Quick run after each task commit:
  mix test test/example/test/example/demo/seeds_test.exs

Full suite after plan completion:
  mix test

Expected: exits 0 with all tests passing. Key assertions to confirm:
  - Idempotency: first == second on double run (covers expired_invitations, passkeys +1 for pat)
  - grace is an Acme member with deleted_at non-nil
  - pat has 0 MFA credentials and >= 1 passkey
  - expired-invite invitation has expires_at in the past
  - audit_events count >= 15 + 4 new events (total: 34 persona+admin events)
</verification>

<success_criteria>
- seeds.ex seed_invitation/1 seeds expired-invite@demo.sigra.dev with ~U[2026-01-01 00:00:00Z] expires_at
- seeds.ex seed_memberships/3 upserts grace.id into acme with :member role
- seeds.ex seed_passkey/1 extracts upsert_passkey/2 and calls it for both admin and pat
- seeds.ex @persona_audit_events has 16 entries (was 12; +4 at offsets 30-33)
- seeds_test.exs snapshot_counts has expired_invitations key scoped to expired-invite email
- seeds_test.exs has test blocks for grace, pat, and expired-invite persona states
- mix test test/example/test/example/demo/seeds_test.exs exits 0
- mix test exits 0
</success_criteria>

<output>
Create `.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-03-SUMMARY.md` when done.
</output>
