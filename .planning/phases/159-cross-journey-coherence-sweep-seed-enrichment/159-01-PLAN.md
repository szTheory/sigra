---
phase: 159-cross-journey-coherence-sweep-seed-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/organizations/detail.ex
  - lib/sigra/admin/live/organization_live.ex
  - lib/sigra/admin/components.ex
autonomous: true
requirements:
  - FIXT-02

must_haves:
  truths:
    - "The org-roster renders a 'Deletion scheduled' pill for members whose user has a non-nil deleted_at"
    - "The notice/1 component wraps slot content in a <div> instead of <p>, eliminating the nested-block-in-p hazard"
    - "organization_live.ex format_date/1 handles NaiveDateTime without silently rendering '—'"
  artifacts:
    - path: "lib/sigra/admin/organizations/detail.ex"
      provides: "deletion_scheduled?: boolean in member_row type + shape_member_row/1"
      contains: "deletion_scheduled?"
    - path: "lib/sigra/admin/live/organization_live.ex"
      provides: "roster template deletion pill + expanded format_date/1"
      contains: "member.deletion_scheduled?"
    - path: "lib/sigra/admin/components.ex"
      provides: "notice/1 with <div> wrapper instead of <p>"
      contains: "sg-text-sm"
  key_links:
    - from: "lib/sigra/admin/organizations/detail.ex:shape_member_row/1"
      to: "lib/sigra/admin/live/organization_live.ex roster template"
      via: "member_row map — deletion_scheduled? key"
      pattern: "deletion_scheduled\\?"
    - from: "lib/sigra/admin/components.ex:notice/1"
      to: "organization_live.ex, index_live.ex, user_show_live.ex (call sites)"
      via: "<.notice> component slot"
      pattern: "sg-text-sm"
---

<objective>
Three targeted lib-owned code fixes that unlock the FIXT-02 roster pill, harden the notice
component's HTML correctness, and make format_date/1 robust to NaiveDateTime inputs.

Purpose: FIXT-02 requires THREE coordinated changes — data shape, template, and seed (seed in Plan 03).
This plan delivers the code side. No behavior changes to any existing passing pill states.

Output: Patched detail.ex (deletion_scheduled? field), patched organization_live.ex (roster pill
+ expanded format_date/1), patched components.ex (notice <p>→<div>).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-CONTEXT.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-RESEARCH.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-PATTERNS.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/sigra/admin/organizations/detail.ex (current state):
- @type member_row at lines 22–28: has user, role, confirmed?, locked?, display_name — MISSING deletion_scheduled?
- defp shape_member_row/1 at lines 103–113: uses Map.get/2 for all field access (host-struct pattern)
- @type invitation_row at lines 30–35: shows the derived-boolean pattern (expired? from expires_at)

From lib/sigra/admin/live/organization_live.ex (current state):
- Roster template at lines 132–143: sg-cluster with role/locked?/confirmed?/unconfirmed pills
- defp format_date/1 at lines 190–191: single-clause DateTime only, catch-all "—"
- Invitation template at lines 160–163: shows the same pill pattern for reference

From lib/sigra/admin/components.ex (current state):
- notice/1 at lines 302–308: wraps render_slot(@inner_block) in <p class="sg-text-sm">
- format_date/1 (private, shared) at lines 407–413: multi-clause DateTime/NaiveDateTime/nil/raise
- All 3 notice call sites pass only inline text — no block <p> children currently
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add deletion_scheduled? to detail.ex shape_member_row/1</name>
  <files>lib/sigra/admin/organizations/detail.ex</files>

  <read_first>
    Read lib/sigra/admin/organizations/detail.ex lines 22–28 (@type member_row) and
    lines 103–113 (shape_member_row/1) before editing.
    Analog: shape_invitation_row/2 at lines 115–127 — same derived-boolean-from-field pattern.
  </read_first>

  <action>
    Two targeted edits to detail.ex:

    1. In @type member_row (lines 22–28), add the field:
       deletion_scheduled?: boolean()
       after locked?: boolean() and before display_name.

    2. In defp shape_member_row/1 (lines 103–113), add the key:
       deletion_scheduled?: not is_nil(Map.get(user, :deleted_at))
       after locked?: not is_nil(Map.get(user, :locked_at))
       and before display_name.

    Use Map.get/2 (not direct struct field access) — the user is a host-app struct and the
    field may not exist in older installs; Map.get falls back to nil safely (per D-03 and
    PATTERNS.md host-struct pattern).

    Do NOT remove or reorder any existing fields. Do NOT change the function head or clause.
  </action>

  <verify>
    <automated>grep -n "deletion_scheduled?" lib/sigra/admin/organizations/detail.ex</automated>
  </verify>

  <acceptance_criteria>
    - grep outputs at least two lines: one in the @type member_row block, one in shape_member_row/1
    - The value expression uses Map.get(user, :deleted_at) — NOT user.deleted_at (direct access)
    - mix test exits 0 (no compile errors)
  </acceptance_criteria>

  <done>
    detail.ex @type member_row has deletion_scheduled?: boolean() and shape_member_row/1
    derives it as not is_nil(Map.get(user, :deleted_at)); mix test exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add roster deletion pill to organization_live.ex + expand format_date/1</name>
  <files>lib/sigra/admin/live/organization_live.ex</files>

  <read_first>
    Read lib/sigra/admin/live/organization_live.ex lines 130–145 (roster template sg-cluster)
    and lines 188–193 (local format_date/1 definition).
    Analog for the pill: users_index_live.ex:355 pattern — same tone ("warn") and label
    ("Deletion scheduled"). Analog for format_date: components.ex:407–413 multi-clause version.
  </read_first>

  <action>
    Two edits to organization_live.ex:

    EDIT A — Roster template deletion pill (per D-03):
    After the existing <span :if={member.locked?} ...>Locked</span> line (line 136) and
    BEFORE the <span :if={member.confirmed?} ...> line, insert:
      <span :if={member.deletion_scheduled?} class="sg-status-pill" data-tone="warn">Deletion scheduled</span>

    This mirrors users_index_live.ex:355 exactly: data-tone="warn", label "Deletion scheduled".
    The member map now has deletion_scheduled? from Task 1.

    EDIT B — Expand local format_date/1 (folded todo admin-format-date-naivedatetime):
    Replace the existing two-clause defp format_date/1 at lines 190–191 with four clauses:
      defp format_date(%DateTime{} = dt),       do: Calendar.strftime(dt, "%Y-%m-%d")
      defp format_date(%NaiveDateTime{} = ndt), do: Calendar.strftime(ndt, "%Y-%m-%d")
      defp format_date(nil),                    do: "—"
      defp format_date(_),                      do: "—"

    Keep the date-only format "%Y-%m-%d" — do NOT add HH:MM (the local helper is date-only;
    the shared components.ex version includes time, which would be a visual regression here).
    Keep the silent fallback "—" for unknown types (do NOT introduce an ArgumentError raise —
    that matches components.ex behavior but not this file's existing contract).
  </action>

  <verify>
    <automated>grep -n "deletion_scheduled?\|NaiveDateTime\|format_date" lib/sigra/admin/live/organization_live.ex</automated>
  </verify>

  <acceptance_criteria>
    - grep shows member.deletion_scheduled? in the roster template
    - grep shows NaiveDateTime clause in format_date/1
    - The pill has data-tone="warn" and text "Deletion scheduled"
    - format_date/1 has exactly 4 clauses (DateTime, NaiveDateTime, nil, catch-all)
    - mix test exits 0
  </acceptance_criteria>

  <done>
    organization_live.ex roster renders .sg-status-pill[data-tone="warn"] with text
    "Deletion scheduled" when member.deletion_scheduled? is true; format_date/1 handles
    NaiveDateTime without rendering "—"; mix test exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix notice/1 nested-p hazard in components.ex (org-notice-nested-p)</name>
  <files>lib/sigra/admin/components.ex</files>

  <read_first>
    Read lib/sigra/admin/components.ex lines 298–310 (notice/1 def and ~H template).
    Verify all 3 call sites pass only inline content (not block <p>) before changing:
      - organization_live.ex lines 62–68
      - index_live.ex lines 52–59
      - user_show_live.ex line 132
    RESEARCH.md confirms none currently pass block children — this change is safe.
  </read_first>

  <action>
    In components.ex notice/1 (lines 302–308), change the inner wrapper from <p> to <div>:

    Change:
      <p class="sg-text-sm">{render_slot(@inner_block)}</p>
    To:
      <div class="sg-text-sm">{render_slot(@inner_block)}</div>

    This is a single-tag swap. No other lines in the notice/1 function change. The CSS
    selector .sg-text-sm applies to both <p> and <div> — no visual change. The change
    eliminates the LiveView patch-desync hazard where block-level slot content would
    produce invalid nested-<p> HTML.

    Do NOT change the outer <div class=["sg-notice", ...] wrapper. Do NOT change any attr
    or slot declarations. Do NOT change the format_date/1 or any other function in this file.
  </action>

  <verify>
    <automated>grep -n "sg-text-sm" lib/sigra/admin/components.ex</automated>
  </verify>

  <acceptance_criteria>
    - The notice/1 ~H template contains <div class="sg-text-sm"> (not <p class="sg-text-sm">)
    - grep for '<p class="sg-text-sm">' in components.ex returns no results inside notice/1
    - mix test exits 0 (no compile errors, no existing tests broken)
  </acceptance_criteria>

  <done>
    notice/1 wraps render_slot(@inner_block) in <div class="sg-text-sm">; <p> is gone from
    the notice slot wrapper; mix test exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib → test/example | Lib-owned LiveView/components render data from host app schemas; deletion_scheduled? is derived via Map.get (safe for missing fields in older installs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Information Disclosure | organization_live.ex roster — deletion_scheduled? flag | accept | Flag derives from host user struct; admin authorization gate already requires platform-admin scope; no new data exposure beyond what locked?/confirmed? already reveal |
| T-159-02 | Tampering | components.ex notice/1 <p>→<div> | accept | Pure HTML structure change; no behavior or auth logic; no new attack surface; call sites already validated to pass inline content only |
| T-159-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After all three tasks:
  mix test
Expected: exits 0, no compile errors, no existing tests broken.

Spot-check roster pill presence (requires a running dev server with seeded DB — deferred to Plan 03 which adds the seed data and Plan 04 which adds the Playwright assertion):
  grep -n "deletion_scheduled?" lib/sigra/admin/organizations/detail.ex lib/sigra/admin/live/organization_live.ex
</verification>

<success_criteria>
- detail.ex @type member_row includes deletion_scheduled?: boolean()
- detail.ex shape_member_row/1 sets deletion_scheduled?: not is_nil(Map.get(user, :deleted_at))
- organization_live.ex roster template has <span :if={member.deletion_scheduled?} class="sg-status-pill" data-tone="warn">Deletion scheduled</span> after the Locked pill
- organization_live.ex format_date/1 has 4 clauses including NaiveDateTime
- components.ex notice/1 uses <div class="sg-text-sm"> not <p class="sg-text-sm">
- mix test exits 0
</success_criteria>

<output>
Create `.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-01-SUMMARY.md` when done.
</output>
