---
phase: 159-cross-journey-coherence-sweep-seed-enrichment
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/components.ex
  - lib/sigra/admin/live/organization_live.ex
  - lib/sigra/admin/organizations/detail.ex
  - test/example/lib/example/demo/seeds.ex
  - test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts
autonomous: true
requirements: [GATE-03]

must_haves:
  truths:
    - "scope_ribbon/1 in components.ex emits the class sg-scope-ribbon on every render so Playwright can locate it via .sg-scope-ribbon"
    - "OrganizationLive renders <.scope_ribbon> immediately after </header> so Screen 2 satisfies the coherence contract"
    - "admin-coherence-sweep.spec.ts GATE-03 motion check is discriminating: it fails if the @media pointer:fine guard is removed from app.css and passes when the guard is present"
    - "shape_invitation_row/2 in detail.ex does not raise ArgumentError when expires_at is a NaiveDateTime"
    - "insert_audit_batch/3 in seeds.ex propagates transaction failure instead of silently discarding it"
    - "npx playwright test admin-coherence-sweep.spec.ts --project=chromium passes against a seeded dev server (verified via autonomous Playwright run)"
  artifacts:
    - path: "lib/sigra/admin/components.ex"
      provides: "scope_ribbon/1 emitting sg-scope-ribbon stable hook class"
      contains: "sg-scope-ribbon sg-muted sg-text-sm"
    - path: "lib/sigra/admin/live/organization_live.ex"
      provides: "scope_ribbon call after </header> in render/1 and scope_copy/1 private function"
      contains: "<.scope_ribbon copy={scope_copy(@admin_scope)}"
    - path: "lib/sigra/admin/organizations/detail.ex"
      provides: "NaiveDateTime guard in shape_invitation_row/2"
    - path: "test/example/lib/example/demo/seeds.ex"
      provides: "transaction result matched in insert_audit_batch/3"
    - path: "test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts"
      provides: "discriminating GATE-03 motion assertion"
  key_links:
    - from: "lib/sigra/admin/components.ex:scope_ribbon/1"
      to: "test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts"
      via: "page.locator('.sg-scope-ribbon')"
      pattern: "sg-scope-ribbon"
    - from: "lib/sigra/admin/live/organization_live.ex:render/1"
      to: "admin-coherence-sweep.spec.ts:89"
      via: "<.scope_ribbon> call after </header> using @admin_scope"
      pattern: "scope_ribbon"
    - from: "test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts:GATE-03"
      to: "test/example/priv/static/assets/css/app.css"
      via: "app.css source assertion — transition inside @media (hover:hover) and (pointer:fine)"
      pattern: "@media (hover: hover) and (pointer: fine)"
---

<objective>
Close the GATE-03 coherence spec blocker left by Phase 159 execution. Five scope-ribbon
Playwright assertions target `.sg-scope-ribbon` but `scope_ribbon/1` never emits that class;
Screen 2 (`OrganizationLive`) additionally has no `<.scope_ribbon>` call at all. Also
tighten the GATE-03 motion check so it actually fails if the `@media` pointer:fine CSS guard
is removed, and fix two robustness issues (NaiveDateTime crash in detail.ex, silent
transaction discard in seeds.ex).

Purpose: Satisfy GATE-03 so the Phase 159 verification passes 6/6 and the milestone's
automated coherence contract is real rather than tautological.

Output:
- `lib/sigra/admin/components.ex` — `sg-scope-ribbon` class prepended to scope_ribbon/1
- `lib/sigra/admin/live/organization_live.ex` — `<.scope_ribbon>` call after `</header>` + `scope_copy/1` helper
- `lib/sigra/admin/organizations/detail.ex` — NaiveDateTime guard in shape_invitation_row/2
- `test/example/lib/example/demo/seeds.ex` — transaction result matched, not discarded
- `test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts` — discriminating GATE-03 motion check
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-CONTEXT.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-VERIFICATION.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From lib/sigra/admin/components.ex:259-263 (current — BROKEN):
```elixir
def scope_ribbon(assigns) do
  ~H"""
  <span class={["sg-muted sg-text-sm", @class]} {@rest}>{@copy}</span>
  """
end
```

From lib/sigra/admin/live/users_index_live.ex (PATTERN — scope_ribbon placement):
Line 86: `</header>`
Line 88: `<.scope_ribbon copy={scope_copy(@admin_scope)} />`   ← placed AFTER </header>, NOT inside it
Lines 384-389 (scope_copy/1 private function):
```elixir
defp scope_copy(%Scope{mode: :global}), do: "Global user operations"
defp scope_copy(%Scope{organization: %{name: name}}),
  do: "#{name} org admin"
defp scope_copy(_), do: "User operations"
```

From lib/sigra/admin/live/audit_index_live.ex (PATTERN — scope_copy/1):
Lines 267-270:
```elixir
defp scope_copy(%Scope{mode: :organization, organization: %{name: name}}),
  do: "#{name} audit"
defp scope_copy(_admin_scope), do: "Global audit explorer"
```

From lib/sigra/admin/live/organization_live.ex:44-58 (render/1 header region — current structure):
```heex
<section class="sg-stack sg-stack--6">
  <%!-- [1] open header — unchanged --%>
  <header class="sg-page-header">
    <p class="sg-page-kicker">Organization overview</p>
    <h1 class="sg-page-title">{@organization_name}</h1>
    <p class="sg-page-copy">
      Work inside this organization scope: ...
    </p>
  </header>
  <%!-- [2] LOUD ALARM — first child after header --%>
  <.notice ...>
```
The `<.scope_ribbon>` call goes between `</header>` and `<.notice>`, mirroring the users_index_live.ex pattern.

From lib/sigra/admin/live/organization_live.ex:196-198 (organization_name/1 — reference for pattern):
```elixir
defp organization_name(%Scope{organization: %{name: name}}) when is_binary(name), do: name
defp organization_name(%Scope{organization_slug: slug}) when is_binary(slug), do: slug
defp organization_name(_), do: "Organization"
```

From lib/sigra/admin/organizations/detail.ex:117-129 (shape_invitation_row/2 — BROKEN):
```elixir
defp shape_invitation_row(invitation, now) do
  expires_at = Map.get(invitation, :expires_at)

  expired? =
    not is_nil(expires_at) and DateTime.compare(expires_at, now) == :lt

  %{email: ..., role: ..., expires_at: expires_at, expired?: expired?}
end
```

From lib/sigra/admin/live/organization_live.ex:191-194 (format_date/1 — NaiveDateTime pattern):
```elixir
defp format_date(%DateTime{} = dt), do: Calendar.strftime(dt, "%Y-%m-%d")
defp format_date(%NaiveDateTime{} = ndt), do: Calendar.strftime(ndt, "%Y-%m-%d")
defp format_date(nil), do: "—"
defp format_date(_), do: "—"
```

From test/example/lib/example/demo/seeds.ex:625-678 (insert_audit_batch/3 — BROKEN):
```elixir
defp insert_audit_batch(admin, users, organizations) do
  Repo.transaction(fn ->  # result discarded — Repo.transaction returns {:ok, _} | {:error, _}
    Enum.each(@audit_actions, fn ... -> ... |> Repo.insert!() end)
    Enum.each(@persona_audit_events, fn ... -> ... |> Repo.insert!() end)
  end)
  # <-- nothing returned; caller gets :ok regardless of transaction outcome
end
```

From lib/sigra/admin/scope.ex (Scope struct modes):
```elixir
@type mode :: :global | :organization
# mode: :global | :organization
# organization: map() | nil   (has :name key when mode: :organization)
# organization_slug: String.t() | nil
```

From test/example/priv/static/assets/css/app.css:858-879 (actual CSS structure for GATE-03):
```css
/* unconditional block — NO transition here */
.sg-filter-chip {
  display: inline-flex;
  align-items: center;
  gap: var(--sg-space-2);
  min-height: var(--sg-control-md);
  padding: var(--sg-space-2) var(--sg-space-3);
  border-radius: var(--sg-radius-sm);
  background: var(--sg-color-panel);
  box-shadow: var(--sg-elev-inset);
  cursor: pointer;
  font-size: var(--sg-text-sm);
}
/* transition lives INSIDE this guard — this is what GATE-03 must protect */
@media (hover: hover) and (pointer: fine) {
  .sg-filter-chip {
    transition: var(--sg-transition-tone), var(--sg-transition-press);
  }
  .sg-filter-chip:hover { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scope_ribbon class + add OrganizationLive ribbon call</name>
  <files>lib/sigra/admin/components.ex, lib/sigra/admin/live/organization_live.ex</files>
  <read_first>
    Read lib/sigra/admin/components.ex lines 240-270 (scope_ribbon/1 doc + implementation).
    Read lib/sigra/admin/live/organization_live.ex lines 1-60 (imports, mount, render header region).
    Read lib/sigra/admin/live/users_index_live.ex lines 80-95 and 380-395 (scope_ribbon call and scope_copy/1 pattern to mirror).
  </read_first>
  <action>
    Part A — components.ex:

    In `scope_ribbon/1` (currently line 261), prepend `sg-scope-ribbon` to the class list so the
    component emits `sg-scope-ribbon sg-muted sg-text-sm` (plus any `@class` additions). The fix
    is changing the class list from `["sg-muted sg-text-sm", @class]` to
    `["sg-scope-ribbon sg-muted sg-text-sm", @class]`. Keep `@rest` and `{@copy}` intact. The
    `sg-scope-ribbon` class is a stable hook class (per D-07) consumed by Playwright; it carries
    no visual styling, so no CSS changes are required.

    Part B — organization_live.ex:

    1. In the `render/1` function, add `<.scope_ribbon copy={scope_copy(@admin_scope)} />`
       immediately AFTER the closing `</header>` tag and BEFORE the `<.notice>` block — per
       D-07 (cross-cutting constraint from Phase 156) and matching the pattern in
       `users_index_live.ex` where line 86 is `</header>` and line 88 is the ribbon call,
       NOT nested inside the header. Do NOT place it inside `<header class="sg-page-header">`.
       The ribbon sits between `</header>` and the `<.notice ...>` LOUD ALARM block.
       The `@admin_scope` assign is already present (assigned in `mount/3`; used throughout render).

    2. Add a private `scope_copy/1` function below the existing `organization_name/1` helper
       (around line 196). Model the pattern on `users_index_live.ex:384-389`:
       - Clause 1: `%Scope{organization: %{name: name}}` — produce `"#{name} org overview"` (the
         overview archetype; a distinct label from the users/audit sub-screens).
       - Clause 2: `%Scope{organization_slug: slug}` when `is_binary(slug)` — produce
         `"#{slug} org overview"` as a fallback when organization struct not loaded.
       - Clause 3: catch-all — produce `"Organization overview"`.

       `Sigra.Admin.Scope` is already aliased. No new imports needed — `scope_ribbon/1` is already
       imported via `import Sigra.Admin.Components` at line 8.

    Deferred — do NOT fix in this task: `users_path/1` and `audit_path/1` nil-slug catch-all
    (WARNING at lines 200-204). The verifier noted it as a misconfigured-routing edge case.
    Deferring to a targeted fix in a later phase to keep this plan focused on GATE-03 closure.
    Record the deferral in the SUMMARY.

    Also deferred: `format_date/1` catch-all at line 194. The current 4-clause pattern already
    has `defp format_date(_), do: "—"` — changing it to raise would be a behavioral break for
    host apps that pass unexpected types, and the verifier marked it INFO (not WARNING). Defer
    to CLAUDE.md convention enforcement pass.
  </action>
  <verify>
    <automated>grep -n "sg-scope-ribbon" /Users/jon/projects/sigra/lib/sigra/admin/components.ex</automated>
    <automated>grep -n "scope_ribbon\|scope_copy" /Users/jon/projects/sigra/lib/sigra/admin/live/organization_live.ex</automated>
    Compile check: `cd /Users/jon/projects/sigra && mix compile --no-deps-check 2>&1 | grep -i error` should return zero lines.
    Placement check: confirm the scope_ribbon call appears on a line AFTER the `</header>` line in organization_live.ex — `grep -n "</header>\|scope_ribbon" lib/sigra/admin/live/organization_live.ex` must show `</header>` at a lower line number than the `scope_ribbon` call.
  </verify>
  <done>
    `scope_ribbon/1` emits `class={["sg-scope-ribbon sg-muted sg-text-sm", @class]}`.
    `OrganizationLive.render/1` has `<.scope_ribbon copy={scope_copy(@admin_scope)} />`
    immediately after `</header>`, not nested inside the header block. `scope_copy/1` is
    defined with 3 clauses. `mix compile` has zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make GATE-03 motion check discriminating (WR-02)</name>
  <files>test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts</files>
  <read_first>
    Read test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts lines 134-147 (current
    GATE-03 check — tautological: `expect(transition).not.toContain('transform')` passes in any
    headless environment whether or not the @media guard is present).
    Read test/example/priv/static/assets/css/app.css lines 858-884 (the unconditional
    .sg-filter-chip block with NO transition, and the @media (hover: hover) and (pointer: fine)
    guard block that contains the transition — to confirm what the static assertions must target).
  </read_first>
  <action>
    Replace the tautological GATE-03 assertion block (lines ~134-145) with a two-phase check:
    one static (CSS source read) and one runtime (computed style in headless). Drop any
    emulateMedia or probe-injection steps entirely.

    Phase 1 — Static CSS source assertion:
    At the top of the GATE-03 block, read `test/example/priv/static/assets/css/app.css` as
    raw text using `fs.readFileSync` (Node.js built-in; add `import fs from 'fs'` or
    `const fs = require('fs')` at the top of the spec file if not already imported, or use
    `import { readFileSync } from 'fs'`). The path to read is the absolute path to
    `test/example/priv/static/assets/css/app.css` relative to the spec file location — use
    `path.resolve(__dirname, '../../../priv/static/assets/css/app.css')` (adjust relative
    depth to match the actual directory structure of the spec file).

    Assert two things about the CSS text:
    a) The unconditional `.sg-filter-chip { ... }` rule block does NOT contain `transition`.
       Mechanism: extract the text between the first `.sg-filter-chip {` and its matching `}`
       (before the `@media` guard) and assert it does not match `/transition/`.
    b) The `@media (hover: hover) and (pointer: fine)` block DOES contain a `transition`
       declaration scoped to `.sg-filter-chip`. Mechanism: find the substring starting at
       `@media (hover: hover) and (pointer: fine)` and assert it contains both
       `.sg-filter-chip` and `transition` within that block.

    These two assertions together mean: if the `@media` guard is deleted and the transition
    rule is moved into the unconditional `.sg-filter-chip` block, assertion (a) fails — the
    check is genuinely discriminating.

    Phase 2 — Runtime computed-style assertion (keep as complement):
    Navigate to `/admin/users`, wait for LiveView ready, locate `label.sg-filter-chip` as
    `chip`. Evaluate `getComputedStyle(chip).transition` in the headless browser (which has
    no pointer:fine capability). Assert that `transition` does NOT contain `'transform'`.
    This confirms the @media guard is suppressing the animation at runtime in a
    non-pointer:fine environment.

    Keep the navigation and LiveView-ready wait that precede the existing check; only replace
    the assertion body. No emulateMedia calls. No probe style injection. No `gate-03-probe`
    style element.

    Update the comment block above the GATE-03 section to describe the two-phase mechanism
    (static CSS read + runtime computed style) so the next reader understands the intent.
    Remove any mention of "tautological", "emulateMedia", or "probe injection" from comments.
  </action>
  <verify>
    <automated>grep -n "readFileSync\|@media (hover: hover)\|transition\|getComputedStyle" /Users/jon/projects/sigra/test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts</automated>
    <automated>grep -c "toHaveScreenshot" /Users/jon/projects/sigra/test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts</automated>
    The static assertion is present if `readFileSync` and `@media (hover: hover)` appear in the
    spec. The runtime assertion is present if `getComputedStyle` appears. The `toHaveScreenshot`
    count must be 0. No `emulateMedia` or `gate-03-probe` should remain:
    `grep -c "emulateMedia\|gate-03-probe" test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts` must return 0.
  </verify>
  <done>
    The GATE-03 block contains: (a) static read of app.css asserting unconditional
    .sg-filter-chip block has no `transition`, (b) static assertion that the @media guard
    block contains `transition` for .sg-filter-chip, (c) runtime assertion that
    `getComputedStyle(chip).transition` does not contain `transform` in headless. No
    `toHaveScreenshot` calls. No `emulateMedia` or probe injection. If the `@media` guard
    is deleted from app.css and the transition moved to the unconditional block, assertion
    (a) catches `transition` in the unconditional rule and fails.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix WR-01 (NaiveDateTime guard) and WR-04 (transaction result discard)</name>
  <files>lib/sigra/admin/organizations/detail.ex, test/example/lib/example/demo/seeds.ex</files>
  <read_first>
    Read lib/sigra/admin/organizations/detail.ex lines 115-130 (shape_invitation_row/2 — the
    `DateTime.compare` call that raises on NaiveDateTime inputs).
    Read test/example/lib/example/demo/seeds.ex lines 615-680 (seed_audit_events/3 and
    insert_audit_batch/3 — the discarded Repo.transaction result).
  </read_first>
  <action>
    WR-01 — detail.ex shape_invitation_row/2:

    The current `expired?` computation calls `DateTime.compare(expires_at, now)` where `now`
    is a `DateTime` (UTC). If `expires_at` is a `NaiveDateTime` (host schema uses naive
    timestamps), `DateTime.compare/2` raises `ArgumentError: not a DateTime`.

    Guard for this by adding a `normalize_to_datetime/1` private helper that:
    - Returns `%DateTime{}` unchanged.
    - Converts `%NaiveDateTime{}` to `DateTime` via `DateTime.from_naive!(ndt, "Etc/UTC")`.
    - Returns `nil` for `nil` (pass-through).

    Replace the bare `DateTime.compare(expires_at, now)` call with:
    `DateTime.compare(normalize_to_datetime(expires_at), now)` — but only when
    `expires_at` is non-nil (preserve the outer `not is_nil(expires_at) and ...` guard).
    The nil guard must remain first so `normalize_to_datetime` is never called with nil.

    Do NOT add a silent catch-all clause that returns false for unexpected types — raise on
    truly unexpected types (the library robustness policy from components.ex). The three
    legitimate input types for `expires_at` are `DateTime`, `NaiveDateTime`, and `nil`.

    WR-04 — seeds.ex insert_audit_batch/3:

    `Repo.transaction/1` returns `{:ok, result}` on success or `{:error, reason}` on rollback.
    The current implementation discards this return value — a failed batch is invisible.

    Pattern-match the result and raise on failure, consistent with the seeds file's existing
    `Repo.insert!()` style (seeds use bang variants throughout; a seed failure should be loud).
    Change the function body to bind the result and handle it:
    - On `{:ok, _}` — return `:ok` (or let the function return naturally).
    - On `{:error, reason}` — call `raise` with a descriptive message including the reason,
      so `Seeds.run/0` fails loudly instead of proceeding past a rolled-back batch.

    Do NOT change the internal `Repo.insert!()` calls inside the transaction — they already
    raise on failure (triggering the transaction rollback), which is correct. The fix is only
    on the outer `Repo.transaction(fn -> ... end)` result.
  </action>
  <verify>
    <automated>grep -n "normalize_to_datetime\|NaiveDateTime\|from_naive" /Users/jon/projects/sigra/lib/sigra/admin/organizations/detail.ex</automated>
    <automated>grep -n "transaction\|:ok\|:error\|raise" /Users/jon/projects/sigra/test/example/lib/example/demo/seeds.ex | grep -A2 -B2 "transaction"</automated>
    Compile check: `cd /Users/jon/projects/sigra && mix compile --no-deps-check 2>&1 | grep -i error` returns zero lines.
    Unit check (detail.ex): the module compiles cleanly and no existing tests fail:
    `mix test test/sigra/admin/organizations/ --no-deps-check 2>&1 | tail -5`
    Seeds test: `cd /Users/jon/projects/sigra/test/example && mix test test/example/demo/seeds_test.exs --no-deps-check 2>&1 | tail -5`
  </verify>
  <done>
    `detail.ex` has `normalize_to_datetime/1` covering DateTime, NaiveDateTime, and nil inputs;
    `shape_invitation_row/2` calls it before `DateTime.compare`. `seeds.ex`
    `insert_audit_batch/3` pattern-matches `{:ok, _}` / `{:error, reason}` and raises on error.
    Both modules compile without errors. Existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run coherence sweep Playwright spec autonomously</name>
  <files>test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts</files>
  <read_first>
    Confirm the spec file exists and review the test summary structure at the top of
    test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts (lines 1-30).
  </read_first>
  <action>
    Execute the full Playwright coherence filmstrip against a seeded dev server. All steps run
    autonomously — no human input required.

    Step 1 — Pre-compile the example app to avoid code-reload crash during server boot
    (mirrors the admin Playwright harness pattern). Run in the test/example directory:
    `PORT=4011 mix phx.compile` (4000 is reserved for Rulestead Docker locally).

    Step 2 — Start the server in the background on port 4011:
    `PORT=4011 mix phx.server` as a background process. Poll until port 4011 is listening
    before proceeding (use `until nc -z localhost 4011; do sleep 1; done` or equivalent;
    timeout after 60 seconds and fail if the server does not come up).

    Step 3 — Seed the database:
    `cd test/example && PORT=4011 mix run priv/repo/seeds.exs`

    Step 4 — Run the Playwright spec:
    `cd test/example && BASE_URL=http://localhost:4011 npx playwright test admin-coherence-sweep.spec.ts --project=chromium`
    Capture the exit code.

    Step 5 — Tear down: send SIGTERM to the background server process in all cases (whether
    the spec passed or failed). Assert the captured exit code from Step 4 is 0. If the exit
    code is non-zero, surface the Playwright output and fail.

    Expected result: Playwright reports "1 passed, 0 failed". Specifically:
    - All `.sg-scope-ribbon` toBeVisible() calls on Screens 1-6 pass (not timeout)
    - Screen 2 / OrganizationLive ribbon (added in Task 1) is found
    - GATE-03 static CSS assertions pass (unconditional block has no transition; @media
      guard block contains transition)
    - GATE-03 runtime assertion passes (getComputedStyle headless transition not contain transform)
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && BASE_URL=http://localhost:4011 npx playwright test admin-coherence-sweep.spec.ts --project=chromium; echo "EXIT:$?"</automated>
    Playwright exit code must be 0. Output must contain "1 passed" and "0 failed".
  </verify>
  <done>
    All 6 scope_ribbon assertions pass. GATE-03 block (static CSS + runtime computed style)
    passes. Playwright exits with code 0 ("1 passed, 0 failed"). Background server torn down.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin_scope → scope_copy/1 | The ribbon copy is derived from `@admin_scope` assigned during LiveView mount; the scope is already authorization-checked by the admin plug before `mount/3` runs |
| seeds.ex → Repo | Seeds run under MIX_ENV=dev or test only; the insert_audit_batch/3 fix adds a raise on rollback — this does not introduce a new trust boundary |
| Playwright CSS source read | The `fs.readFileSync` in GATE-03 reads a static asset file in the test directory; no production runtime involvement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-05-01 | Information Disclosure | scope_ribbon/1 copy text | accept | `scope_copy(@admin_scope)` derives copy solely from the already-authorized `admin_scope` struct (name/slug already visible on the page); no cross-org data exposed; the helper is a private function scoped to the calling LiveView |
| T-159-05-02 | Tampering | GATE-03 static CSS assertion | accept | CSS read is in a Playwright test file, not production code; test environment only; no attack surface in library or host app |
| T-159-05-03 | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages; no package manager changes; slopcheck not required |
| T-159-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Full phase re-verification after gap closure:

1. `grep -n "sg-scope-ribbon" lib/sigra/admin/components.ex` — must return the class list line
2. `grep -n "scope_ribbon\|scope_copy" lib/sigra/admin/live/organization_live.ex` — must show call site and helper definition
3. `grep -c "sg-scope-ribbon" lib/sigra/admin/components.ex` — must return 1
4. `grep -c "toHaveScreenshot" test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts` — must return 0
5. `grep -n "readFileSync\|@media (hover: hover)" test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts` — must return the static CSS assertion lines
6. `grep -c "emulateMedia\|gate-03-probe" test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts` — must return 0
7. Placement check: `grep -n "</header>\|scope_ribbon" lib/sigra/admin/live/organization_live.ex` — `</header>` line number must be lower than `scope_ribbon` line number (ribbon is after header, not inside it)
8. `grep -n "normalize_to_datetime" lib/sigra/admin/organizations/detail.ex` — must return the helper and its call site
9. `grep -A3 "Repo.transaction" test/example/lib/example/demo/seeds.ex` — must show pattern-match on result
10. `cd /Users/jon/projects/sigra && mix compile --no-deps-check 2>&1 | grep -i error` — must return zero lines
11. `cd /Users/jon/projects/sigra && mix test test/sigra/admin/ --no-deps-check 2>&1 | tail -5` — must pass
12. `cd /Users/jon/projects/sigra/test/example && mix test test/example/demo/seeds_test.exs --no-deps-check 2>&1 | tail -5` — must pass
13. Playwright: `BASE_URL=http://localhost:4011 npx playwright test admin-coherence-sweep.spec.ts --project=chromium` against seeded dev server — 1 passed, 0 failed
</verification>

<success_criteria>
- `lib/sigra/admin/components.ex`: `scope_ribbon/1` emits `class={["sg-scope-ribbon sg-muted sg-text-sm", @class]}`
- `lib/sigra/admin/live/organization_live.ex`: `<.scope_ribbon copy={scope_copy(@admin_scope)} />` placed immediately AFTER `</header>` (not nested inside the header block), between `</header>` and the `<.notice>` block; `scope_copy/1` private function has 3 clauses covering org name, org slug fallback, and catch-all
- `test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts`: GATE-03 block uses static CSS source read to assert transition lives inside the @media guard (not the unconditional block); removing the `@media` guard from app.css would cause the static assertion to fail; runtime computed-style assertion confirms headless suppression; no emulateMedia, no probe injection
- `lib/sigra/admin/organizations/detail.ex`: `shape_invitation_row/2` does not raise on `NaiveDateTime` inputs; `normalize_to_datetime/1` helper converts NaiveDateTime to UTC DateTime
- `test/example/lib/example/demo/seeds.ex`: `insert_audit_batch/3` matches `{:ok, _}` / `{:error, reason}` and raises on failure
- `mix compile --no-deps-check` clean; existing admin/ tests and seeds_test.exs pass
- Playwright coherence filmstrip: all 6 screens pass, verified via autonomous Playwright run (exit code 0, "1 passed, 0 failed")
- Phase 159 re-verification scores 6/6 (all must-haves satisfied)
</success_criteria>

<output>
Create `.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-05-SUMMARY.md` when done.

Include in SUMMARY:
- Which files were changed and what was changed in each
- Deferred items: (1) `users_path/1`/`audit_path/1` nil-slug catch-all in organization_live.ex lines 200-204 — deferred; only crashes on misconfigured nil-slug routing; tracked for future phase. (2) `format_date/1` silent catch-all at line 194 — deferred; behavioral break risk; INFO severity; tracked for CLAUDE.md convention pass.
- Playwright run result (passed / failed counts)
- Confirmation that GATE-03 check is now discriminating (static CSS source assertion)
</output>
