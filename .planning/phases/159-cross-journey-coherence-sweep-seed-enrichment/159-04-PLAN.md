---
phase: 159-cross-journey-coherence-sweep-seed-enrichment
plan: 04
type: execute
wave: 3
depends_on:
  - 159-03
files_modified:
  - test/example/priv/static/assets/css/app.css
  - test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts
autonomous: true
requirements:
  - GATE-03
  - FIXT-01
  - FIXT-02
  - FIXT-03

must_haves:
  truths:
    - "sg-filter-chip transition is scoped to pointer:fine devices — keyboard chip-toggle produces no animation"
    - "admin-coherence-sweep.spec.ts passes: scope_ribbon on all 6 screens, page_back on detail screens, notice visible, empty-state structure"
    - "GATE-03 keyboard-only Playwright pass: filter chip keyboard toggle produces no transition"
    - "Expired pill, Deletion scheduled pill, and Passkeys pill are visible in the seeded demo DB via DOM text assertions"
  artifacts:
    - path: "test/example/priv/static/assets/css/app.css"
      provides: "sg-filter-chip transition scoped to pointer:fine guard"
      contains: "hover: hover"
    - path: "test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts"
      provides: "sibling coherence filmstrip spec — behavior-only, no toHaveScreenshot"
      contains: "admin-coherence-sweep"
  key_links:
    - from: "app.css sg-filter-chip block (line 858)"
      to: "app.css @media (hover: hover) and (pointer: fine) block (line 871)"
      via: "move transition: var(--sg-transition-tone), var(--sg-transition-press) inside media guard"
      pattern: "sg-filter-chip"
    - from: "admin-coherence-sweep.spec.ts"
      to: "seeded demo DB (pat@demo.sigra.dev, grace@demo.sigra.dev, expired-invite row)"
      via: "navigate to /admin/users?q=pat%40... and /admin/organizations/acme-corp"
      pattern: "pat%40demo"
---

<objective>
Two artifacts: (1) CSS motion fix — scope sg-filter-chip transition to pointer:fine devices
so keyboard chip-toggle is animation-free (D-06, GATE-03). (2) New Playwright sibling spec
admin-coherence-sweep.spec.ts — behavior-only DOM assertions across all 6 admin screens plus
GATE-03 keyboard motion check (D-07, criterion 4).

Purpose: GATE-03 is verify-not-build. The CSS change is the ONLY code written; everything
else asserts existing contracts. The spec must NOT call toHaveScreenshot() — pixel baselines
are Phase 160's job.

Output: app.css with scoped filter-chip transition; new admin-coherence-sweep.spec.ts that
passes in the default chromium project without any new playwright.config.ts changes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-CONTEXT.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-RESEARCH.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-PATTERNS.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-UI-SPEC.md
@.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-03-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From test/example/priv/static/assets/css/app.css (current state):
- .sg-filter-chip block starts at line 858
- line 869: transition: var(--sg-transition-tone), var(--sg-transition-press); [UNCONDITIONAL]
- line 871: @media (hover: hover) and (pointer: fine) { [existing guard]
- line 872:   .sg-filter-chip:hover { translateY(-1px), brand-soft bg, box-shadow }
- line 878: .sg-filter-chip:has(input:checked) { ... }
- line 889: nested @media inside the checked block (dark mode)
- D-06 fix: MOVE the line 869 transition INTO the existing media block at line 871
  alongside the :hover rule. Remove it from the unconditional block. 3-line net change.

From test/example/priv/playwright/ (current state):
- playwright.config.ts: ADMIN_CHECKPOINTS_SPEC = /admin-checkpoints\.spec\.ts/ (line 26)
- Default chromium project (line 84): testIgnore includes ADMIN_CHECKPOINTS_SPEC,
  ADMIN_GENERATED_SPEC, DEMO_SHOWCASE_SPEC — does NOT exclude admin-coherence-sweep.spec.ts
- helpers/fixtures.ts: exports TEST_PASSWORD = 'CorrectHorseBatteryStaple123!'
- helpers/adminArtifacts.ts, helpers/adminUsersIndex.ts: NOT imported by the sibling spec
- admin-checkpoints.spec.ts: local helpers registerUser, waitForLiveViewReady,
  clearBrowserSession, createOrganization — copy verbatim into sibling spec

From admin-checkpoints.spec.ts (existing journey — for assertion reference):
- scope_ribbon assertions use: locator('.sg-scope-ribbon') or text "Global" in header
- page_back assertions use: getByRole('link', { name: 'Back to user' })
- notice assertions: locator('.sg-notice').first().toBeVisible()
- Pill assertions (new, from PATTERNS.md): locator('.sg-status-pill[data-tone="risk"]').filter({ hasText: 'Expired' })

Seeded demo DB dependencies (Playwright runs against dev server — seeds must have been run):
- "Expired" pill: navigate to /admin/organizations/acme-corp, verify Expired pill
- "Deletion scheduled" pill: navigate to /admin/organizations/acme-corp, verify member pill
- "Passkeys" pill: navigate to /admin/users?q=pat%40demo.sigra.dev, verify Passkeys pill
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scope sg-filter-chip transition to pointer:fine (D-06 GATE-03 fix)</name>
  <files>test/example/priv/static/assets/css/app.css</files>

  <read_first>
    Read app.css lines 855–895 (the complete .sg-filter-chip block and its media guards).
    The exact structure: unconditional block → @media (hover:hover) and (pointer:fine) → :checked block.
    The fix is: remove line 869 from the unconditional block, add it inside the media block.
  </read_first>

  <action>
    One surgical edit to app.css inside @layer sg-components:

    REMOVE the unconditional transition line from .sg-filter-chip:
    Line 869 currently reads:
      transition: var(--sg-transition-tone), var(--sg-transition-press);
    Delete this line from the .sg-filter-chip { } block.

    ADD the same transition inside the existing @media (hover: hover) and (pointer: fine)
    block (currently at line 871), as a new nested .sg-filter-chip { } rule BEFORE the
    .sg-filter-chip:hover rule:
      @media (hover: hover) and (pointer: fine) {
        .sg-filter-chip {
          transition: var(--sg-transition-tone), var(--sg-transition-press);
        }
        .sg-filter-chip:hover {
          background: var(--sg-color-brand-soft);
          ...
        }
      }

    Do NOT alter any other lines in the .sg-filter-chip block, the :checked block, the dark
    mode overrides, or any other CSS rules. This is a 3-line net change (remove 1, add 3).
    The transition stays for pointer:fine devices (mouse users); keyboard/touch users see
    no animation, satisfying GATE-03's keyboard-frequent contract.
  </action>

  <verify>
    <automated>grep -n "sg-filter-chip\|hover: hover" test/example/priv/static/assets/css/app.css | grep -v "^#"</automated>
  </verify>

  <acceptance_criteria>
    - grep shows NO standalone `transition: var(--sg-transition-tone), var(--sg-transition-press);`
      inside the unconditional .sg-filter-chip { } block
    - grep shows the transition inside a @media (hover: hover) and (pointer: fine) block
      that also contains .sg-filter-chip:hover
    - All other .sg-filter-chip rules (color, padding, min-height, border-radius, :checked,
      dark mode) remain unchanged
    - The CSS file still compiles without errors (no broken @layer or missing braces)
  </acceptance_criteria>

  <done>
    app.css sg-filter-chip transition is inside @media (hover: hover) and (pointer: fine) only;
    keyboard users toggling a filter chip see no transform animation; mouse users retain
    press/tone transitions; CSS compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin-coherence-sweep.spec.ts — behavior filmstrip + GATE-03 check</name>
  <files>test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts</files>

  <read_first>
    Read test/example/priv/playwright/tests/admin-checkpoints.spec.ts lines 1–88
    (imports, local helpers: waitForLiveViewReady, registerUser, clearBrowserSession, createOrganization).
    These helper functions are copied verbatim into the sibling spec.
    Read test/example/priv/playwright/helpers/fixtures.ts for TEST_PASSWORD.
    PATTERNS.md provides all DOM assertion patterns for pills, scope_ribbon, page_back, notice.
  </read_first>

  <action>
    Create a NEW file at test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts.

    The file structure and content:

    IMPORTS: import { test, expect, type Page } from '@playwright/test'; and
             import { TEST_PASSWORD } from '../helpers/fixtures';
    (Omit captureAdminCheckpoint, statSync, AxeBuilder — not needed in behavior-only spec.)

    LOCAL HELPERS: Copy the four helper functions verbatim from admin-checkpoints.spec.ts:
      waitForLiveViewReady, registerUser, clearBrowserSession, createOrganization.
    Also add openUserDetail (copy from admin-checkpoints.spec.ts:74-81) for navigating to
    user detail/audit pages.

    SINGLE TEST BLOCK: test.describe('Phase 159 coherence sweep', ...) containing one test:
    test('all 6 admin screens satisfy the coherence contract', async ({ page }) => { ... })

    The test performs a single authenticated platform-admin journey. Structure:

    SETUP — Register a platform admin:
      const adminEmail = `platform-admin+coherence-${Date.now()}@example.test`;
      await registerUser(page, adminEmail, TEST_PASSWORD);
      await page.goto('/admin'); await waitForLiveViewReady(page);

    SCREEN 1 — Global overview (/admin):
      Assert: await expect(page.locator('.sg-notice').first()).toBeVisible();
      (Notice is the alarm; scope_ribbon is NOT required on overview archetype)

    SCREEN 2 — Org overview (/admin/organizations/acme-corp):
      Navigate: await page.goto('/admin/organizations/acme-corp'); await waitForLiveViewReady(page);
      Assert scope_ribbon: await expect(page.locator('.sg-scope-ribbon')).toBeVisible();
      Assert expired pill (FIXT-01, seeded demo DB): await expect(page.locator('.sg-status-pill[data-tone="risk"]').filter({ hasText: 'Expired' })).toBeVisible();
      Assert deletion scheduled pill (FIXT-02, seeded demo DB): await expect(page.locator('.sg-status-pill[data-tone="warn"]').filter({ hasText: 'Deletion scheduled' })).toBeVisible();
      Add a comment: // Requires mix run priv/repo/seeds.exs to have been run in dev env

    SCREEN 3 — Users index (/admin/users):
      Navigate: await page.goto('/admin/users'); await waitForLiveViewReady(page);
      Assert scope_ribbon: await expect(page.locator('.sg-scope-ribbon')).toBeVisible();
      Assert Passkeys pill (FIXT-03, seeded demo DB):
        await page.goto('/admin/users?q=pat%40demo.sigra.dev'); await waitForLiveViewReady(page);
        await expect(page.locator('.sg-status-pill[data-tone="ok"]').filter({ hasText: 'Passkeys' })).toBeVisible();
      Add a comment: // Requires mix run priv/repo/seeds.exs — pat@demo.sigra.dev must exist

    SCREEN 4 — User audit (/admin/users/:id/audit, via openUserDetail):
      Navigate to /admin/users, then open any user's detail, then click audit tab.
      Simpler: navigate via /admin/users?q=<adminEmail>, open user, navigate to audit.
      Assert page_back: await expect(page.getByRole('link', { name: /back/i })).toBeVisible();
      Assert scope_ribbon: await expect(page.locator('.sg-scope-ribbon')).toBeVisible();

    SCREEN 5 — Audit explorer (/admin/audit):
      Navigate: await page.goto('/admin/audit'); await waitForLiveViewReady(page);
      Assert scope_ribbon: await expect(page.locator('.sg-scope-ribbon')).toBeVisible();

    SCREEN 6 — Org roster (/admin/organizations/acme-corp/users):
      Navigate: await page.goto('/admin/organizations/acme-corp/users'); await waitForLiveViewReady(page);
      Assert scope_ribbon: await expect(page.locator('.sg-scope-ribbon')).toBeVisible();

    GATE-03 KEYBOARD MOTION CHECK:
    After the chip scoping fix (Task 1), assert that on a non-pointer-fine environment
    the filter chip has no transition. Navigate to /admin/users, find a filter chip,
    and check computed style:
      await page.goto('/admin/users'); await waitForLiveViewReady(page);
      const chip = page.locator('label.sg-filter-chip').first();
      await chip.focus();
      const transition = await chip.evaluate((el) => getComputedStyle(el).transition);
      // Playwright's chromium test environment is not pointer:fine by default, so
      // the transition should be empty or 'none 0s ease 0s' after the D-06 fix.
      // Add a comment documenting this environmental dependency.
      // Use a loose assertion — just confirm it doesn't contain 'transform':
      expect(transition).not.toContain('transform');

    IMPORTANT CONSTRAINTS:
    - NO toHaveScreenshot() calls in this spec — zero pixel baselines (Phase 160 owns those)
    - The test.describe and test name must start with 'Phase 159 coherence sweep' for traceability
    - Document the seeded-DB dependency with a comment near each pill assertion
    - The spec file name admin-coherence-sweep.spec.ts does NOT match the ADMIN_CHECKPOINTS_SPEC
      regex (/admin-checkpoints\.spec\.ts/) in playwright.config.ts, so it runs automatically
      in the default chromium project — NO changes to playwright.config.ts are needed
  </action>

  <verify>
    <automated>grep -n "toHaveScreenshot\|admin-coherence-sweep\|Phase 159" test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - File exists at test/example/priv/playwright/tests/admin-coherence-sweep.spec.ts
    - grep confirms no toHaveScreenshot() calls in the file
    - grep shows "Phase 159 coherence sweep" in test.describe
    - All 6 screen navigation URLs are present (/admin, /admin/organizations/acme-corp,
      /admin/users, /admin/audit, /admin/organizations/acme-corp/users, and a user audit URL)
    - .sg-scope-ribbon assertions present for screens 2, 3, 5, 6
    - .sg-status-pill[data-tone="risk"] Expired assertion present (FIXT-01)
    - .sg-status-pill[data-tone="warn"] Deletion scheduled assertion present (FIXT-02)
    - .sg-status-pill[data-tone="ok"] Passkeys assertion present (FIXT-03)
    - GATE-03 keyboard chip transition assertion present (expect(transition).not.toContain('transform'))
    - Seeded-DB dependency documented in comments near pill assertions
    - npx playwright test admin-coherence-sweep.spec.ts --project=chromium exits 0
      (run against a running dev server with seeded demo DB)
  </acceptance_criteria>

  <done>
    admin-coherence-sweep.spec.ts exists and passes in the chromium project; all 6 screens
    verified for scope_ribbon; page_back present on user-audit screen; Expired/Deletion-scheduled/
    Passkeys pills confirmed via DOM text; GATE-03 keyboard chip motion check passes; zero
    toHaveScreenshot() calls; no playwright.config.ts changes required.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → dev server | Coherence spec runs against the running dev server (same pattern as admin-checkpoints.spec.ts); no new auth surface exposed |
| CSS → browser | Motion scoping to pointer:fine is purely presentational; no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-07 | Information Disclosure | Playwright spec navigates to /admin/* as a platform-admin registered user | accept | admin-checkpoints.spec.ts already uses the same pattern; no new access pattern; test user is ephemeral ad-hoc registration |
| T-159-08 | Tampering | CSS @media guard prevents touch/keyboard animations | accept | Purely presentational; reduces animation scope for keyboard users; does not alter any auth or access control behavior |
| T-159-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; Playwright and all deps already in package.json |
</threat_model>

<verification>
CSS change verification (no running server needed):
  grep -n "sg-filter-chip" test/example/priv/static/assets/css/app.css
  Expected: no unconditional transition on .sg-filter-chip; transition appears inside @media (hover: hover) and (pointer: fine)

Playwright spec verification (requires running dev server with seeded DB):
  cd test/example/priv/playwright
  npx playwright test admin-coherence-sweep.spec.ts --project=chromium
  Expected: 1 test passing, no failures

Full verification gate (before /gsd:verify-work):
  mix test
  npx playwright test admin-coherence-sweep.spec.ts
</verification>

<success_criteria>
- app.css sg-filter-chip transition is inside @media (hover: hover) and (pointer: fine) only
- admin-coherence-sweep.spec.ts has no toHaveScreenshot() calls
- Test asserts scope_ribbon on screens 2 (org overview), 3 (users index), 5 (audit explorer), 6 (org roster)
- Test asserts page_back on user audit screen (screen 4)
- Test asserts Expired pill (data-tone="risk") on org overview
- Test asserts Deletion scheduled pill (data-tone="warn") on org overview roster section
- Test asserts Passkeys pill (data-tone="ok") on users index for pat@demo.sigra.dev
- GATE-03: computed transition on filter chip does not contain 'transform' in Playwright's non-pointer-fine environment
- npx playwright test admin-coherence-sweep.spec.ts passes (requires seeded dev server)
</success_criteria>

<output>
Create `.planning/phases/159-cross-journey-coherence-sweep-seed-enrichment/159-04-SUMMARY.md` when done.
</output>
