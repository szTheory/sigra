---
phase: 220-terminal-ratification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md
autonomous: true
requirements: [RATIFY-01]

must_haves:
  truths:
    - "quality-ledger-monotonic.sh exits 0 against the resolved merge-base (origin/main), substantively comparing the ~36 quality-ledger cells (the SC-1 forward-lock proof)"
    - "quality-ledger-monotonic.test.sh and award-guard.test.mjs (14/14) both run green in the same evidence capture"
    - "award-guard.mjs exits 0 against the merge-base via its SKIP path — the record states honestly that main predates the net-new award ledger, so its substantive forward-lock is intra-branch + self-test, NOT the merge-base diff (PI-1)"
    - "The record distinguishes the on-branch local PREVIEW from the authoritative on-PR CI proof (fast_checks running the guards at committed HEAD against the real merge-base), closing the 216 SC-5 trap"
  artifacts:
    - ".planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md"
  key_links:
    - "The guards resolve base via git merge-base origin/<base> HEAD (ci.yml:79-85); the record's merge-base must be the same origin/main resolution the PR will use"
  prohibitions:
    - "No guard code weakened, no cells re-rendered or re-climbed — verify-then-confirm, not fresh-climb (D-01); a fresh re-render is explicitly out of scope"
    - "The record does NOT substitute a documentation assertion for live-guard evidence (D-02 / 216 SC-5 trap); green self-tests alone are NOT accepted as the committed-HEAD proof"
---

<objective>
Produce the SC-1 live-guard confirmation record: run the two monotonic ratchet guards and their
self-tests against the real terminal-PR merge-base (origin/main), capture the outputs, and record them
honestly per PI-1 and the 216 SC-5 trap.

D-01: no net-new guard code — the forward-only lock already exists and is wired into `fast_checks`
(`quality-ledger-monotonic.sh` at ci.yml:120, `award-guard.mjs` at ci.yml:133, both diffing the
resolved merge-base at ci.yml:79-85). D-02: the lock is proven by LIVE guard execution against the real
merge-base, not a doc assertion. PI-1: award-guard exits 0 via its SKIP path (main predates the net-new
award ledger), so the SUBSTANTIVE SC-1 proof is quality-ledger-monotonic's ~36-cell compare.

Purpose: create the honest, reproducible evidence record the close-readiness record (Plan 04) and the
terminal PR reference. Output: 220-LIVE-GUARD-CONFIRMATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/220-terminal-ratification/220-CONTEXT.md
@.planning/phases/220-terminal-ratification/220-RESEARCH.md
@.planning/phases/220-terminal-ratification/220-VALIDATION.md
</context>

## Artifacts this phase produces (this plan)

- **New:** `.planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md` — the SC-1
  evidence record: the resolved merge-base sha, the four guard/self-test invocations with their
  captured outputs, the PI-1 honesty note (award-guard skip vs quality-ledger substantive compare),
  and the pre-PR-preview vs on-PR-authoritative distinction (216 SC-5 trap).

<tasks>

<task type="auto">
  <name>Task 1: Run the guards + self-tests against the real merge-base and record the evidence</name>
  <read_first>
    .planning/phases/220-terminal-ratification/220-RESEARCH.md — the Live guard execution preview
    table (lines 51-58), PI-1 (lines 64-84), and the SC->signal map (lines 129-137).
    .planning/phases/220-terminal-ratification/220-VALIDATION.md — the SC-1 row and Validation Sign-Off.
    scripts/ci/quality-ledger-monotonic.sh and scripts/ci/award-guard.mjs — to confirm the base-diff
    invocation matches ci.yml:120 / ci.yml:133.
  </read_first>
  <files>.planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md</files>
  <action>
    First `git fetch origin main` so the merge-base is current. Resolve the merge-base exactly as CI
    does: `MB=$(git merge-base origin/main HEAD)` (ci.yml:79-85 uses git merge-base origin/<base> HEAD).
    Run and capture verbatim the output + exit code of all four invocations against that MB:
    (1) `bash scripts/ci/quality-ledger-monotonic.sh --base "$MB"` — expect PASS with an explicit
    cells-compared count (research saw "PASS (36 cells checked ...)"), exit 0;
    (2) `bash scripts/ci/quality-ledger-monotonic.test.sh` — expect green, exit 0;
    (3) `node scripts/ci/award-guard.mjs --base "$MB"` — expect exit 0 via the SKIP path
    ("no base ledger ... skipping comparison (initial commit)") because origin/main predates the
    net-new award ledger;
    (4) `node scripts/ci/award-guard.test.mjs` — expect "14 passed, 0 failed", exit 0.
    Write 220-LIVE-GUARD-CONFIRMATION.md capturing: the resolved MB sha; each command, its captured
    output, and exit code; a PI-1 HONESTY section stating that quality-ledger-monotonic's ~36-cell
    compare is the SUBSTANTIVE SC-1 forward-lock, while award-guard's merge-base exit-0 is a
    skip-path (main has no award ledger) whose substantive lock is proven intra-branch (216-218) + by
    the 14/14 self-test; and a 216-SC-5 section stating clearly that this on-branch run is a PREVIEW
    and the AUTHORITATIVE committed-HEAD proof is the same guards running in the terminal PR's
    `fast_checks` job against the real merge-base (to be confirmed via `gh pr checks` on the PR).
    Do NOT re-render or re-climb any cell (D-01 verify-then-confirm). Do NOT weaken any guard.
  </action>
  <verify>
    <automated>git fetch origin main --quiet; MB=$(git merge-base origin/main HEAD); bash scripts/ci/quality-ledger-monotonic.sh --base "$MB" && bash scripts/ci/quality-ledger-monotonic.test.sh && node scripts/ci/award-guard.mjs --base "$MB" && node scripts/ci/award-guard.test.mjs</automated>
    <automated>test -f .planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md && grep -q 'quality-ledger-monotonic' .planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md && grep -q 'award-guard' .planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md && grep -qiE 'skip|initial commit' .planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md</automated>
    <automated>grep -qiE 'preview|authoritative|SC-5|committed[- ]HEAD' .planning/phases/220-terminal-ratification/220-LIVE-GUARD-CONFIRMATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All four invocations exit 0 against the freshly-resolved merge-base; quality-ledger-monotonic
      prints an explicit cells-checked count and PASS.
    - The record captures the MB sha, all four commands with outputs + exit codes.
    - The record contains the PI-1 honesty note (award-guard skip vs quality-ledger substantive compare)
      and the 216-SC-5 preview-vs-authoritative distinction (on-PR fast_checks is the definitive proof).
    - No cell re-rendered/re-climbed; no guard weakened.
  </acceptance_criteria>
  <done>220-LIVE-GUARD-CONFIRMATION.md exists with all four guard/self-test outputs, the PI-1 honesty note, and the SC-5 preview-vs-authoritative framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forward-only ratchet guard → merge decision | The monotonic guard is the mechanism that prevents a silent quality/award regression from merging; a weakened or unrun guard defeats SC-1 |
| self-test green → live-guard green | Trusting a green self-test in place of a live committed-HEAD run is the 216 SC-5 trap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-220-01 | Tampering | quality-ledger-monotonic.sh / award-guard.mjs forward-lock | high | mitigate | Guards are run LIVE against the real merge-base and recorded (D-02); no guard code is weakened or allowlisted; the record is the audit trail. The guard itself is the mitigation — the plan only confirms it holds. |
| T-220-07 | Repudiation | SC-1 lock evidence honesty | medium | mitigate | Record explicitly separates award-guard's skip-path exit-0 from quality-ledger's substantive 36-cell compare (PI-1), and preview from authoritative on-PR proof (SC-5) — no overclaim. |
| T-220-SC | Tampering | npm/pip/cargo installs | low | accept | No package install; guards + self-tests run against already-present scripts. |
</threat_model>

<verification>
- Live: all four guard/self-test invocations exit 0 against the freshly-resolved origin/main merge-base.
- Record: 220-LIVE-GUARD-CONFIRMATION.md captures MB sha, four outputs, PI-1 honesty note, SC-5 framing.
- Authoritative (on terminal PR): the same guards green in fast_checks at committed HEAD (`gh pr checks`).
</verification>

<success_criteria>
SC-1 proven live: quality-ledger-monotonic locks the ~36 award sub-score cells forward against the real
merge-base (exit 0, would exit non-zero on any decrease), self-tests green, award-guard exit-0 recorded
honestly per PI-1 — with the definitive committed-HEAD proof deferred to the terminal PR's fast_checks.
</success_criteria>

<output>
Create `.planning/phases/220-terminal-ratification/220-03-SUMMARY.md` when done.
</output>
