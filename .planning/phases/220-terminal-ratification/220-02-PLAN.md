---
phase: 220-terminal-ratification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-eval-runbook.md
autonomous: true
requirements: [RATIFY-01]

must_haves:
  truths:
    - "admin-eval-runbook.md carries a you-are-here preamble distinguishing the local eval-harness loop (gitignored eval/ bundles) from committed-baseline PNG recapture (CI-native ubuntu/amd64 ONLY — never darwin)"
    - "The runbook notes that the merge-boundary impersonation-banner canary-red is EXPECTED and reconciled post-merge, and is NEVER allowlisted"
    - "The runbook cross-references the branch-scoped recapture dispatch (recapture_branch input / release_ref_guard relaxation)"
    - "A zero-context future agent can read the runbook and orient without prior phase context (SC-2)"
  artifacts:
    - "guides/reference/admin-eval-runbook.md (three additive freshness notes, single commit)"
  key_links:
    - "The runbook is the SC-2 artifact of record (D-03) — the UAT-demo scripts/uat/RUNBOOK.md is a different file and is NOT touched"
  prohibitions:
    - "Runbook is NOT rewritten — D-04 is ADDITIVE freshness notes only (D-03 keeps the existing SRE-hallmark structure)"
    - "No note instructs allowlisting/weakening the canary guard, and no note instructs recapturing committed baseline PNGs on darwin/local (CI-native only)"
---

<objective>
Add the three LOCKED D-04 additive freshness notes to the SC-2 harness runbook
`guides/reference/admin-eval-runbook.md` so a zero-context future agent is oriented post-219.

D-03 keeps the existing runbook (no rewrite — it already satisfies SRE-runbook hallmarks). D-04 adds
exactly three light, additive notes in a single commit: (a) a you-are-here preamble separating the
local eval loop from committed-baseline recapture; (b) an "expected canary-red at the merge boundary"
note; (c) a one-line cross-ref to the branch-scoped recapture dispatch.

Purpose: satisfy SC-2's explicit "re-runnable by a zero-context agent" test without disturbing the
locked runbook content. Output: three additive notes in admin-eval-runbook.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/220-terminal-ratification/220-CONTEXT.md
@guides/reference/admin-eval-runbook.md
</context>

## Artifacts this phase produces (this plan)

- **Modified:** `guides/reference/admin-eval-runbook.md` — three additive orientation notes (D-04),
  landed in one commit. No existing section is rewritten or removed.

<tasks>

<task type="auto">
  <name>Task 1: Add the three D-04 freshness notes to admin-eval-runbook.md</name>
  <read_first>
    guides/reference/admin-eval-runbook.md (whole file, 379 lines) — to place the notes coherently
    without duplicating existing content.
    .planning/phases/220-terminal-ratification/220-CONTEXT.md — D-03/D-04 (lines 55-66) for the exact
    intent of each of the three notes.
    .planning/phases/219-baseline-recapture-canary-reconciliation/219-CONTEXT.md — for the
    branch-scoped recapture dispatch (recapture_branch input / release_ref_guard relaxation) cross-ref.
  </read_first>
  <files>guides/reference/admin-eval-runbook.md</files>
  <action>
    Add exactly three additive notes (do NOT rewrite existing sections; D-03 keeps the runbook):
    (a) A "you-are-here" preamble near the top of the document that distinguishes the two loops a
    future agent will confuse: the LOCAL eval-harness iteration loop (produces gitignored `eval/`
    evidence bundles under test/example/priv/playwright/eval/, never committed) versus committed
    baseline PNG recapture (the admin-checkpoints/*.png snapshots), which is CI-native
    ubuntu/amd64-only and must NEVER be recaptured on darwin/local. Use a stable, greppable heading
    or lead phrase containing the words "you are here".
    (b) A note (placed near the guard/merge-gate discussion) that the merge-boundary
    `impersonation-banner` canary snapshot appearing as red/modified at the ship boundary is EXPECTED
    behavior — it is reconciled post-merge via a quarantine baselines-only PR and is NEVER allowlisted
    (snapshot-canary-guard.sh is never-allowlistable by design). Reference the slug `impersonation-banner`.
    (c) A one-line cross-reference to the branch-scoped recapture dispatch: the `recapture_branch`
    workflow_dispatch input and the `release_ref_guard` relaxation that let a baselines-only recapture
    PR target a working branch. Name both `recapture_branch` and `release_ref_guard` so it is greppable.
    Keep each note short and additive. Land all three in this one commit. Do NOT touch
    scripts/uat/RUNBOOK.md (that is the unrelated UAT-demo runbook, not the SC-2 artifact).
  </action>
  <verify>
    <automated>grep -qi 'you are here' guides/reference/admin-eval-runbook.md && grep -q 'impersonation-banner' guides/reference/admin-eval-runbook.md && grep -q 'recapture_branch' guides/reference/admin-eval-runbook.md && grep -q 'release_ref_guard' guides/reference/admin-eval-runbook.md</automated>
    <automated>grep -qi 'never' guides/reference/admin-eval-runbook.md && grep -qiE 'ubuntu|amd64|ci-native|CI-native' guides/reference/admin-eval-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - All three notes are present and greppable: "you are here" preamble; `impersonation-banner`
      expected-canary-red note; `recapture_branch` + `release_ref_guard` cross-ref.
    - The you-are-here note explicitly states committed-baseline recapture is CI-native ubuntu/amd64
      only (never darwin), distinct from the gitignored local eval/ loop.
    - No existing runbook section was rewritten or deleted (additive-only, D-03).
    - scripts/uat/RUNBOOK.md is untouched.
  </acceptance_criteria>
  <done>Three additive D-04 notes present and greppable; existing runbook structure intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook guidance → future operator action | A bad instruction could lead an operator to allowlist the canary or recapture baselines on darwin, corrupting the merge gate or the baseline bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-220-05 | Tampering | runbook guidance on the canary guard | medium | mitigate | The note explicitly states the canary red is EXPECTED and NEVER allowlisted (mirrors the never-allowlistable guard); it does not instruct any bypass. |
| T-220-06 | Tampering | runbook guidance on baseline recapture | medium | mitigate | The you-are-here note states committed baselines are recaptured CI-native ubuntu/amd64 only, never darwin — preventing the classic drift-inducing local recapture. |
| T-220-SC | Tampering | npm/pip/cargo installs | low | accept | Docs-only change; no dependency or package install introduced. |
</threat_model>

<verification>
- Greppable presence of all three D-04 notes.
- CI-native/never-darwin phrasing present in the you-are-here note.
- Existing runbook sections unmodified (additive-only).
</verification>

<success_criteria>
SC-2 satisfied: the committed harness runbook orients a zero-context future agent — one-iteration loop,
where human sign-off sits, how to read the dossier (existing) plus the three post-219 freshness notes
(new), with no rewrite of the locked content.
</success_criteria>

<output>
Create `.planning/phases/220-terminal-ratification/220-02-SUMMARY.md` when done.
</output>
