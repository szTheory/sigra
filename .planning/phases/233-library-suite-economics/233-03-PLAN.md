---
phase: 233-library-suite-economics
plan: 03
type: execute
wave: 3
depends_on: ["233-02"]
files_modified:
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
  - test/upgrade_test.exs
  - test/sigra/install/golden_diff_test.exs
  - test/sigra/install/idempotency_test.exs
  - test/sigra/install/generator_passkeys_opt_out_test.exs
  - test/sigra/install/features/passkeys_js_test.exs
  - test/sigra/install/vault_promotion_test.exs
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "Per D-05, exactly the six canonical InstallFixture-heavy modules carry `:scaffold`; the async template-render module remains ordinary."
    - "Per D-06, a dedicated scaffold receiver executes on every pull request, including docs-only and unrelated changes."
    - "Per D-03 and D-07, the byte-identical `Library tests` aggregator succeeds only when both ordinary shards and the scaffold receiver succeed."
    - "Upgrade, golden, and idempotency coverage remain on the PR path throughout extraction; no intermediate commit strands them behind a path or event gate."
    - "FLAGGED/RESOLVED TEST-03 fallback probe: missing, skipped, cancelled, or failed receiver results are all non-success and make the required aggregator fail."
  assumptions:
    - "The canonical six-module set is exact because each module invokes the expensive scaffold fixture and `template_render_test.exs` explicitly does not."
  prohibitions: []
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "unconditional scaffold receiver and two-input required aggregator"
    - path: test/sigra/planning/phase_233_library_economics_contract_test.exs
      provides: "exact tag-set, receiver, and fail-closed aggregator contract"
    - path: test/upgrade_test.exs
      provides: "scaffold-classified upgrade coverage"
  key_links:
    - from: "six scaffold-tagged modules"
      to: "library_tests_scaffold"
      via: "mix test --only scaffold on every PR"
      pattern: "only scaffold"
    - from: "library_tests_scaffold result"
      to: "library_tests / Library tests"
      via: "needs plus explicit exact-success check under if: always()"
      pattern: "needs.library_tests_scaffold.result"
---

<objective>
Extract the canonical scaffold-heavy test class into an unconditional PR receiver without weakening the required merge verdict, per D-05 through D-07.

Purpose: remove the subprocess tail from ordinary shards while keeping the audit's sharpest upgrade/golden/idempotency hazard hard-failing.
Output: exact module tags, a PR-always scaffold lane, and a two-input byte-identical required aggregator.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/233-library-suite-economics/233-02-SUMMARY.md
@.planning/research/SEED-005-CICD-AUDIT-2026-06-20.md
@.github/workflows/ci.yml
@test/support/install_fixture.ex
@MAINTAINING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the upgrade/golden/idempotency hazard into a required PR receiver</name>
  <reversibility rating="costly">The new lane is coupled to the repository's required context and every PR, so rollback requires a coordinated workflow and tag change.</reversibility>
  <files>test/upgrade_test.exs, test/sigra/install/golden_diff_test.exs, test/sigra/install/idempotency_test.exs, .github/workflows/ci.yml, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` jobs `install_golden_contract`, `library_tests_shard`, and `library_tests`.
    - `test/upgrade_test.exs`, `golden_diff_test.exs`, and `idempotency_test.exs` module headers and setup lifecycles.
    - `MAINTAINING.md` required-check and installer-golden contracts.
    - `233-CONTEXT.md` D-05, D-06, D-07, and D-08.
    - `233-PATTERNS.md` fail-closed aggregator and shell/context patterns.
    - Every file listed in `<files>` before editing it.
  </read_first>
  <behavior>
    - "The three sharp-hazard modules carry module-level `:scaffold` without assertion/setup/timeout changes."
    - "Ordinary shards exclude scaffold; a dedicated Postgres-backed job selects scaffold and runs on pull_request without path/docs gating."
    - "The exact display name `Library tests` remains unchanged and rejects every receiver result other than success."
  </behavior>
  <action>
    Start with failing contract assertions for the three module tags, ordinary exclusion, unconditional receiver, and two-result aggregator. Add one module-level `@moduletag :scaffold` to upgrade, golden diff, and idempotency without changing their existing tags, async mode, setup, timeout, or assertions.

    Add a dedicated `library_tests_scaffold` job using the existing library worker's runner, strict Beam setup, shared cache dimensions, Postgres service, phx_new archive, and test environment. It must run on every PR and must not depend on `changes`, docs classification, or a non-PR event predicate. Invoke the selected class once, add the same timing formatter with `/tmp/sigra-library-scaffold-timings.json`, and retain its receipt. Add `--exclude scaffold` to ordinary shard execution only after the receiver exists in the same commit.

    Extend job id `library_tests` without changing display name `Library tests`: `needs` must include both jobs, `if: always()` remains, GitHub results are mapped through `env:`, and strict shell logic exits nonzero unless both equal `success`. Keep `install_golden_contract` and all drift-detector assertions hard-failing; duplication is acceptable until all six tags land.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The receiver command selects the three named modules through `--only scaffold`, and no test body/assertion changed; Plan 05 supplies observed execution.
    - The receiver is structurally unconditional for PRs and retains Postgres/phx_new prerequisites.
    - Ordinary shards exclude the class only in the commit that adds the receiver and required aggregation.
    - `Library tests` is byte-identical and fails closed for any non-success dependency result.
  </acceptance_criteria>
  <done>The upgrade/golden/idempotency coverage hazard is extracted but remains universal and merge-blocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the exact six-module scaffold classification</name>
  <files>test/sigra/install/generator_passkeys_opt_out_test.exs, test/sigra/install/features/passkeys_js_test.exs, test/sigra/install/vault_promotion_test.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - The three remaining module files and their existing InstallFixture call sites.
    - `test/sigra/install/template_render_test.exs` — explicit fast ordinary-suite exclusion from this edit.
    - Task 1's committed contract test and workflow receiver.
    - `233-RESEARCH.md` Pitfall 5 and Open Question 2.
    - Every file listed in `<files>` before editing it.
  </read_first>
  <behavior>
    - "Generator passkeys opt-out, passkeys JS, and vault promotion join the same scaffold receiver."
    - "The contract enumerates exactly six scaffold modules and rejects a seventh or missing member."
    - "Template render remains async and untagged as scaffold, so ordinary shards continue to execute it."
  </behavior>
  <action>
    Add module-level `@moduletag :scaffold` to the remaining three InstallFixture-heavy modules without altering their tests or fixture lifecycle. Expand the contract to parse module headers and enforce the exact six-path set across the repository, explicitly assert template-render lacks the scaffold classification and remains in the ordinary manifest universe, and verify the receiver selects the entire class. Preserve existing `:upgrade`, `:install`, timeout, and async semantics.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Exactly six canonical modules are tagged and selected by the receiver.
    - Template render remains ordinary, async, and unchanged.
    - No assertions are deleted, skipped, softened, or moved to a non-PR-only path.
    - The focused exact-set and receiver-command contract passes; observed scaffold execution remains an explicit Plan 05 gate.
  </acceptance_criteria>
  <done>The unified scaffold class is complete, exact, and continuously PR-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test tags → CI selection | A classification controls whether expensive correctness coverage executes. |
| Worker results → protected status context | Two untrusted dependency result strings determine merge eligibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-233-07 | Tampering | scaffold selection | high | mitigate | Exact six-file contract, explicit template-render exclusion, and direct `--only scaffold` execution. |
| T-233-08 | Elevation of Privilege | `Library tests` aggregator | high | mitigate | Preserve name/id, `if: always()`, and exact-success checks for both dependencies; reject skipped/cancelled/failure. |
| T-233-09 | Tampering | workflow shell | high | mitigate | Map contexts via `env:`, quote values, and retain `set -euo pipefail`. |
</threat_model>

## Artifacts this phase produces

- New ExUnit classification: module-level `@moduletag :scaffold` on exactly six existing test modules.
- New CI job id: `library_tests_scaffold` (display name chosen by executor and recorded in summary).
- New CI timing artifact: `library-test-timings-scaffold` at `/tmp/sigra-library-scaffold-timings.json`.
- Modified aggregator inputs: `library_tests_shard` and `library_tests_scaffold`; protected display name remains exactly `Library tests` per D-03.
- Modified files: `.github/workflows/ci.yml`, the Phase 233 contract test, and the six named test modules.
- No new dependency, public API, ORM schema, or generated-host output.

<verification>
The focused contract proves exact classification, receiver selection, and fail-closed topology; Plan 05 is the mandatory observed execution gate.
</verification>

<success_criteria>
TEST-03 is structurally complete: scaffold work leaves ordinary shards but never leaves the universal PR path or the required `Library tests` verdict.
</success_criteria>

<output>
Create `.planning/phases/233-library-suite-economics/233-03-SUMMARY.md` when done.
</output>
