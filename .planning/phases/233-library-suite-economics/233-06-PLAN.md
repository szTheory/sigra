---
phase: 233-library-suite-economics
plan: 06
type: execute
wave: 6
depends_on: ["233-05"]
files_modified:
  - test/support/ci/library_test_partitions.exs
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "Before either ordinary shard starts `mix test`, manifest construction discovers the current eligible `*_test.exs` universe through the project's actual `test_load_filters` and fails unless the two partitions equal that universe minus the exact six scaffold paths."
    - "The union of partition 1 and partition 2 contains every current ordinary test exactly once, contains no scaffold test, and contains no path excluded by `test_load_filters`."
    - "A newly created on-disk, untagged ordinary test that is absent from historical `per_file_costs` and both partitions deterministically makes partition construction fail."
    - "The existing measured assignment remains authoritative: live discovery validates coverage but never silently appends an unmeasured file, changes the two-shard topology, or alters verified TEST-01/02/03 economics and PR routing."
    - "FLAGGED/RESOLVED TEST-02 fallback probes remain explicit: empty partitions, duplicate paths, missing paths, stale paths, and deterministic equal-cost/tie ordering are rejected or resolved by named tests."
  assumptions:
    - "The project-root `test_load_filters` value in `Mix.Project.config/0` is the authoritative eligibility rule because it is the same configuration consumed by root `mix test`; the guard must read it rather than duplicating its current regex."
    - "The six D-05 scaffold paths remain the exact subtraction set; the existing tag-discovery contract continues to prove that the named paths and `:scaffold` module tags agree."
  prohibitions:
    - "FLAGGED/UNVERIFIED — TEST-01 descriptor absent; no bespoke prohibition is inferred beyond preserving its already verified same-run parallel timing contract."
    - "FLAGGED/UNVERIFIED — TEST-03 descriptor absent; no bespoke prohibition is inferred beyond preserving its already verified exact scaffold receiver and required-aggregate routing contract."
  artifacts:
    - path: test/support/ci/library_test_partitions.exs
      provides: "current-universe discovery and exact-equality validation executed by both ordinary shard argv builders"
      exports: ["Sigra.CI.LibraryTestPartitions.partition/1", "Sigra.CI.LibraryTestPartitions.build_partitions!/1", "Sigra.CI.LibraryTestPartitions.current_ordinary_paths!/1", "Sigra.CI.LibraryTestPartitions.validate_current_universe!/2"]
    - path: test/sigra/planning/phase_233_library_economics_contract_test.exs
      provides: "on-disk omission regression plus filter, scaffold, equality, duplicate, stale, and ordering contracts"
  key_links:
    - from: mix.exs
      to: test/support/ci/library_test_partitions.exs
      via: "current_ordinary_paths!/1 reads Mix.Project.config()[:test_load_filters] and applies those filters to discovered test paths"
      pattern: "test_load_filters"
    - from: test/support/ci/library_test_partitions.exs
      to: .github/workflows/ci.yml
      via: "partition/1 builds and validates the complete manifest before the existing process-substitution command can launch mix test"
      pattern: "LibraryTestPartitions.partition"
    - from: test/sigra/planning/phase_233_library_economics_contract_test.exs
      to: test/support/ci/library_test_partitions.exs
      via: "a temporary repo-shaped on-disk test universe exercises the same build/validation path used by CI"
      pattern: "validate_current_universe!"
---

<objective>
Close the verified manifest-drift gap without changing the measured economics or routing delivered by Plans 01–05.

Purpose: make explicit shard argv selection self-invalidating whenever the current eligible ordinary test universe diverges from the historical receipt-derived manifest.
Output: production fail-closed universe reconciliation and a deterministic on-disk missing-test regression.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-library-suite-economics/233-CONTEXT.md
@.planning/phases/233-library-suite-economics/233-VERIFICATION.md
@.planning/phases/233-library-suite-economics/233-05-SUMMARY.md
@mix.exs
@test/support/ci/library_test_partitions.exs
@test/sigra/planning/phase_233_library_economics_contract_test.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one live unmeasured ordinary test before shard execution</name>
  <reversibility rating="costly">The validation runs on the merge-blocking shard argv path, so changing its eligibility contract requires coordinated manifest, test, and CI evidence updates.</reversibility>
  <files>test/support/ci/library_test_partitions.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `233-VERIFICATION.md` gap truth, reason, and both missing bullets — implement the live-universe safety property, not the inaccurate narrower CR-01 claim about the two already-listed Phase 233 tests.
    - `mix.exs` `project/0` keys `test_load_filters` and `test_ignore_filters` — reuse the former as the root library eligibility authority; do not copy its regex into the manifest module.
    - `test/support/ci/library_test_partitions.exs` — preserve source run `30666977944`, measured greedy assignment, exact scaffold list, and the partition selector consumed by CI.
    - `test/sigra/planning/phase_233_library_economics_contract_test.exs` — preserve all 13 verified contracts and its exact six-module tag discovery.
    - `.github/workflows/ci.yml` `library_tests_shard` run step — confirm `partition/1` is evaluated under `set -euo pipefail` before the single `mix test` invocation; no workflow edit is needed when the production selector itself raises.
  </read_first>
  <behavior>
    - "With the current repository and receipt, partition 1 and partition 2 build successfully and their union equals the current eligible ordinary universe exactly once."
    - "Given a temporary repo-shaped root containing an untagged `test/new_ordinary_test.exs` that is not present in injected historical costs, the same build path raises an ArgumentError naming the missing path."
    - "Files rejected by the actual project `test_load_filters` are outside the eligible universe; the six exact scaffold paths are eligible test files but are subtracted from ordinary ownership."
    - "Discovery never assigns a default cost or appends an unknown path; refreshing measured evidence/manifest is required after rejection."
  </behavior>
  <action>
    Start RED by adding a `:partition_universe`-tagged regression that creates an isolated temporary repository tree on disk, writes at least one measured ordinary `*_test.exs`, the exact scaffold-path fixtures needed by the validator, and one untagged ordinary `*_test.exs` absent from the injected `per_file_costs`. Invoke the production partition-building path with the temporary root and injected costs; assert a deterministic `ArgumentError` identifies the missing ordinary path, and remove the temporary tree with `on_exit`.

    In `Sigra.CI.LibraryTestPartitions`, make `build_partitions!/1` a public, option-injectable seam while keeping `partition/1` on production defaults. Add `current_ordinary_paths!/1` to enumerate `test/**/*_test.exs` beneath the selected root, normalize each result to a repository-relative slash-separated path, apply the live `Mix.Project.config()[:test_load_filters]` collection using the same match semantics as Mix, require the exact named scaffold paths to remain the subtraction set, and sort the result lexically. Add `validate_current_universe!/2` to flatten both assigned partition path lists, reject duplicates and scaffold leakage, and require exact set equality with current ordinary paths; diagnostics must separately list missing current paths and stale manifest paths. Call this validator inside `build_partitions!/1` after measured assignment and before returning either partition. The injected root/cost seam exists for deterministic tests only; production defaults still read `233-EVIDENCE.json` and never accept an unmeasured fallback.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_233_library_economics_contract_test.exs --only partition_universe</automated>
  </verify>
  <acceptance_criteria>
    - The regression first fails because current-universe discovery/equality is absent, then passes after production validation is wired into `partition/1` through `build_partitions!/1`.
    - The temporary untagged ordinary file exists on disk during the assertion and is absent from both injected historical costs and assigned partitions.
    - The raised diagnostic names the missing path and does not silently append it with a guessed cost.
    - Running the production selector against the current repository succeeds only when its two partition lists equal the eligible ordinary universe exactly once.
  </acceptance_criteria>
  <done>A real on-disk ordinary test omitted from receipt-derived ownership makes shard argv construction fail before `mix test` can start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal exact exclusions, duplicate/stale rejection, and preserved economics</name>
  <files>test/support/ci/library_test_partitions.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - Task 1's committed discovery, build, and equality-validation functions plus regression fixture.
    - `mix.exs` comments and configured `test_load_filters` examples for root library inclusion and `test/example/**` / `test/fixtures/**` exclusion.
    - `233-CONTEXT.md` D-01 through D-08 — keep all verified topology, formatter, measured-balance, scaffold, and required-check decisions unchanged.
    - `233-05-SUMMARY.md` observed 115s/114s ordinary durations, 909s receiver, retry-free PR run, and named upgrade/golden/idempotency proof.
  </read_first>
  <behavior>
    - "A temp test below an included root-library path is eligible; temp tests below `test/example/` and `test/fixtures/` are excluded only because the live load filters reject them."
    - "Each of the exact six scaffold paths is excluded from ordinary ownership, while `test/sigra/install/template_render_test.exs` remains eligible and appears exactly once."
    - "Duplicate ownership, stale receipt paths, missing ordinary paths, scaffold leakage, and either empty partition are independently rejected with stable diagnostics."
    - "Equal measured costs retain lexical input ordering, the lower cumulative-cost partition receives the next file, and partition 1 wins an exact cumulative tie."
    - "The workflow contract still proves two ordinary shards, one parallel test invocation per shard, the same-run timing formatters, the unconditional scaffold receiver, and exact-success `Library tests` aggregation."
  </behavior>
  <action>
    Expand the temporary-filesystem contract matrix around the production discovery/validation seam. Prove live-filter inclusion and both configured excluded subtrees, exact scaffold subtraction, template-render ordinary ownership, and equality in both directions. Add separate mutation cases for a path present in current discovery but absent from costs, a stale cost/partition path absent from disk, cross-partition duplication, scaffold leakage, and an empty partition. Keep or refine the existing TEST-02 empty/duplicate/invalid-cost and equal-cost/tie-order assertions so the specless adjacency, empty, and ordering probes remain executable rather than assumed.

    Add a bounded assertion that the existing `library_tests_shard` shell evaluates `LibraryTestPartitions.partition(...)` before its sole `mix test` command under strict shell mode, proving a raised equality error blocks the job rather than being masked. Retain all prior Phase 233 workflow, tag, timing, receiver, aggregate, and measured-manifest assertions. Do not edit `.github/workflows/ci.yml`, the receipt, scaffold tags, evidence ledgers, or partition cost algorithm: Plans 01–05 already proved TEST-01/02/03 economics and named PR coverage, and this task adds only the missing fail-closed guard.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_233_library_economics_contract_test.exs &amp;&amp; mix test test/support/ci/ex_unit_timing_formatter_test.exs &amp;&amp; mix format --check-formatted test/support/ci/library_test_partitions.exs test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Current eligible files equal the disjoint partition union; only live load-filter exclusions and the exact six scaffold paths are absent.
    - Named tests independently reject missing, stale, duplicate, empty, and scaffold-leaking manifests and preserve deterministic adjacency/order behavior.
    - The ordinary template-render test is selected exactly once and all six scaffold modules are selected zero times.
    - The existing workflow topology, same-run timing, measured two-shard assignment, receiver, exact required name, and recorded economics remain unchanged.
  </acceptance_criteria>
  <done>The manifest is self-invalidating against current disk state while every previously verified Phase 233 economic and coverage contract remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical timing receipt → merge-blocking test selection | Stale measured data controls the explicit test paths passed to ordinary CI shards. |
| Repository filesystem/config → manifest validation | Current test files and Mix load filters define which correctness tests must be owned. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-233-13 | Tampering | `LibraryTestPartitions.build_partitions!/1` | high | mitigate | Discover the live eligible universe, subtract only the exact scaffold set, require disjoint exact equality, and raise before returning shard argv. |
| T-233-14 | Repudiation | manifest drift diagnostics | medium | mitigate | Report sorted missing and stale path lists and cover both directions with deterministic tests. |
| T-233-15 | Denial of Service | newly added unmeasured ordinary test | low | accept | Intentional fail-closed behavior blocks CI until measured evidence is refreshed; it prevents the higher-impact silent coverage loss. |
| T-233-16 | Elevation of Privilege | test-root injection seam | medium | mitigate | Keep production callers on fixed defaults; expose root/cost injection only as an explicit test seam and normalize discovered paths before comparison. |
</threat_model>

## Artifacts this phase produces

- Modified module: `Sigra.CI.LibraryTestPartitions` with public `build_partitions!/1`, `current_ordinary_paths!/1`, and `validate_current_universe!/2` seams; existing `partition/1`, `total_us/1`, measured assignment, source run, and exact scaffold set remain compatible.
- Modified contract suite: an on-disk untagged ordinary omission regression plus live-filter, exact-scaffold, stale, duplicate, empty, ordering, and workflow fail-propagation assertions.
- No workflow, evidence-ledger, scaffold-tag, package, schema, external API, or UI artifact changes.

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Library shards remain off the post-Playwright pole without dishonest coverage | 233-01..06 | COVERED | Plans 01–05 preserve measured economics; Plan 06 closes silent omission.
REQ | TEST-01 | Parallel execution and same-run slow-test visibility | 233-01,02,05,06 | COVERED | Plan 06 reruns and preserves the verified formatter/workflow contract.
REQ | TEST-02 | Comparable deterministic two-shard ownership | 233-02,04,05,06 | COVERED | Plan 06 makes the measured partition union exact against current disk state and preserves adjacency/empty/ordering probes.
REQ | TEST-03 | Scaffold work removed from ordinary shards with retained coverage | 233-03,05,06 | COVERED | Plan 06 subtracts only the exact six receiver-owned paths and preserves the required aggregate contract.
CONTEXT | D-01,D-02 | Non-serial ordinary run with same-run timing | 233-01,02,05,06 | COVERED | Preserved and rerun; no workflow change.
CONTEXT | D-03,D-04 | Two measured, balanced ordinary shards | 233-02,04,05,06 | COVERED | Live discovery validates ownership only; it does not rebalance or add a shard.
CONTEXT | D-05,D-06,D-07 | Exact scaffold class, universal receiver, fail-closed aggregate | 233-03,05,06 | COVERED | Exact subtraction and existing routing assertions remain executable.
CONTEXT | D-08 | Retry-free PR evidence with named coverage | 233-02,05,06 | COVERED | Existing observed evidence is preserved; closure is deterministic local contract work.
RESEARCH | — | Measured explicit manifest remains the owner; no round-robin fallback | 233-04,06 | COVERED | Discovery is equality validation only and never assigns unmeasured files.
RESEARCH | — | Root suite respects project load filters and exact scaffold separation | 233-03,04,06 | COVERED | Production reads live Mix config and contract covers both exclusion classes.
RESEARCH | — | Deterministic fail-closed validation without a new dependency | 233-01,04,06 | COVERED | Existing Elixir/Mix/ExUnit stack only; no package install.
</source_coverage>

<verification>
Run the full Phase 233 contract and formatter tests, format both modified files, and execute both production partition selectors. Each selector must return a non-empty disjoint list; their union must equal live eligible ordinary discovery, and the tagged temporary omission regression must reject an unmeasured on-disk file.
</verification>

<success_criteria>
- Both missing bullets from `233-VERIFICATION.md` are closed by production behavior and deterministic regression evidence.
- Required CI cannot construct ordinary shard argv when any eligible ordinary on-disk test is missing, duplicated, stale, or scaffold-leaking.
- Only the project's live load-filter exclusions and the exact six D-05 scaffold paths are outside ordinary ownership.
- All TEST-01/02/03 focused contracts remain green with no human UAT and no changes to verified economics or named PR coverage.
</success_criteria>

<output>
Create `.planning/phases/233-library-suite-economics/233-06-SUMMARY.md` when done.
</output>
