---
phase: 233-library-suite-economics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/ci/ex_unit_timing_formatter.ex
  - test/support/ci/ex_unit_timing_formatter_test.exs
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
  - .github/workflows/ci.yml
  - .planning/phases/233-library-suite-economics/233-VALIDATION.md
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Per D-01, each ordinary library shard runs without a Mix option that implicitly or explicitly serializes ExUnit."
    - "Per D-02, the same parallel test process emits a deterministic slow-test timing receipt; no second test invocation supplies timing data."
    - "The timing formatter preserves ExUnit.CLIFormatter output and writes only to a fixed CI-owned path selected by the shard identity."
    - "FLAGGED/RESOLVED TEST-01 fallback probe: zero completed tests yields a valid empty receipt, one test yields one entry, and malformed completed-test data fails rather than being represented as measured timing."
  assumptions:
    - "TEST-01's previously unclassified probe is resolved by treating same-run provenance, deterministic ordering, and explicit empty/error behavior as the relevant edges."
  prohibitions: []
  artifacts:
    - path: test/support/ci/ex_unit_timing_formatter.ex
      provides: "Sigra.CI.ExUnitTimingFormatter same-run deterministic timing receipt"
      exports: ["Sigra.CI.ExUnitTimingFormatter"]
    - path: test/support/ci/ex_unit_timing_formatter_test.exs
      provides: "formatter lifecycle, ordering, empty, and path-safety tests"
    - path: test/sigra/planning/phase_233_library_economics_contract_test.exs
      provides: "Phase 233 workflow contract"
    - path: .github/workflows/ci.yml
      provides: "parallel library shard command and retained timing artifact"
  key_links:
    - from: .github/workflows/ci.yml
      to: test/support/ci/ex_unit_timing_formatter.ex
      via: "ordinary shard mix test command configures the additive formatter and fixed timing path"
      pattern: "Sigra.CI.ExUnitTimingFormatter"
    - from: .github/workflows/ci.yml
      to: "uploaded library timing artifact"
      via: "the path written by the formatter is summarized and uploaded from the same job"
      pattern: "SIGRA_EXUNIT_TIMING_PATH"
---

<objective>
Restore within-shard ExUnit parallelism while retaining deterministic slow-test visibility from the exact same run, per D-01 and D-02.

Purpose: prove the phase's hardest architectural constraint before moving any test coverage between lanes.
Output: a tested project-local timing formatter, a non-serial ordinary shard invocation, and retained per-shard timing receipts with all existing PR coverage still in place.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-library-suite-economics/233-CONTEXT.md
@.planning/phases/233-library-suite-economics/233-RESEARCH.md
@.planning/phases/233-library-suite-economics/233-PATTERNS.md
@.planning/phases/233-library-suite-economics/233-VALIDATION.md
@.github/workflows/ci.yml
@test/test_helper.exs
@test/support/install_fixture.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one parallel shard can publish a same-run timing receipt</name>
  <reversibility rating="costly">The receipt schema and environment key are consumed by later balancing and evidence plans, so changing them requires coordinated workflow and evidence updates.</reversibility>
  <files>test/support/ci/ex_unit_timing_formatter.ex, test/support/ci/ex_unit_timing_formatter_test.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs, .github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` job `library_tests_shard` and its timing-summary steps — preserve the two-leg Postgres topology, cache, and all current test coverage.
    - `test/test_helper.exs` — current ExUnit startup and conditional `:upgrade` exclusion.
    - `test/support/install_fixture.ex` — local support-module naming, specs, and explicit error style.
    - `233-RESEARCH.md` Pattern 1 and Security Domain — formatter lifecycle and fixed-path threat.
    - `233-PATTERNS.md` formatter and workflow analogs.
    - Every file listed in `<files>` that already exists, before editing it.
  </read_first>
  <behavior>
    - "A completed synthetic ExUnit test becomes one JSON entry containing file, module, name, time_us, and outcome."
    - "Entries sort by descending time_us, then file/module/name ascending so equal durations are stable."
    - "The ordinary shard command configures both ExUnit.CLIFormatter and Sigra.CI.ExUnitTimingFormatter without starting a second mix test process."
    - "The workflow publishes the formatter output from the shard job while the six scaffold candidates still run in their original PR coverage path."
  </behavior>
  <action>
    Start RED with focused formatter tests and a bounded workflow-contract test. Implement `Sigra.CI.ExUnitTimingFormatter` as an additive ExUnit formatter with `init/1`, `handle_cast/2`, `build_receipt/2`, `validate_output_path!/1`, and `write_receipt!/2`. It collects `{:test_finished, %ExUnit.Test{}}` events and, at suite completion, writes one JSON object with `schema_version`, `partition`, `tests`, and aggregate counts. Use `test.time` microseconds, normalize the outcome to a small stable string set, omit wall-clock timestamps, and sort by the behavior contract before one atomic write.

    In `library_tests_shard`, map matrix partition and the fixed `/tmp/sigra-library-{1|2}-timings.json` output through step `env:` values, invoke one `mix test` process with both formatters, and retain the ordinary two-partition topology. Remove the old log-derived timing-summary mechanism and publish the JSON as both a job-summary table and a uniquely named artifact using the repository's already-pinned upload action. Do not extract or exclude any tests in this tracer: preserving PR coverage while the new observation path proves itself is part of D-08.

    The contract test must extract only the bounded `library_tests_shard` job body and prove one test invocation, both formatters, a CI-owned timing path, artifact retention, and the unchanged two-leg matrix. Map GitHub expressions through `env:` and quote shell variables; do not place event/context expressions directly in shell bodies.
  </action>
  <verify>
    <automated>mix test test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests first fail for missing formatter/workflow behavior, then pass after implementation.
    - The ordinary job has exactly one `mix test` execution and retains partitions 1 and 2 plus current test coverage.
    - The same execution produces deterministic JSON, CLI output, a job-summary slowest table, and an uploaded artifact.
    - No new dependency or second timing-only suite invocation is introduced.
  </acceptance_criteria>
  <done>One production shard path runs tests in parallel and retains deterministic same-run timing evidence without moving coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden deterministic receipt and fixed-path failure behavior</name>
  <files>test/support/ci/ex_unit_timing_formatter.ex, test/support/ci/ex_unit_timing_formatter_test.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs, .github/workflows/ci.yml, .planning/phases/233-library-suite-economics/233-VALIDATION.md</files>
  <read_first>
    - Task 1's committed formatter and focused tests.
    - Task 1's committed `library_tests_shard` block.
    - `233-VALIDATION.md` Wave 0 expectations.
    - Every file listed in `<files>` before editing it.
  </read_first>
  <behavior>
    - "An empty event list serializes to `tests: []` with zero aggregates; a single event remains a one-entry list."
    - "Equal durations retain lexical file/module/name order."
    - "A missing, relative, traversal-like, or non-approved output path raises a named error before writing."
    - "If the test process reaches suite completion, missing timing output makes the upload/summary path fail closed rather than printing a success-looking fallback."
  </behavior>
  <action>
    Expand the RED/GREEN coverage for empty, singleton, equal-duration, malformed-event, missing-path, and rejected-path cases. Restrict production writes to the three CI-owned receipt names for shard 1, shard 2, and the later scaffold receiver under `/tmp`; keep serialization as a pure function so tests do not race on fixed files. Ensure the workflow's summary and upload steps do not convert a missing/invalid receipt into a passing measurement. Update `233-VALIDATION.md` to set Wave 0 formatter and workflow-contract rows present/green and `nyquist_compliant: true` only after the named focused command passes.
  </action>
  <verify>
    <automated>mix test test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs &amp;&amp; mix format --check-formatted test/support/ci/ex_unit_timing_formatter.ex test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Empty, singleton, equal-duration, malformed-event, and output-path cases have deterministic focused tests.
    - Arbitrary filesystem targets cannot be selected by workflow or formatter input.
    - Missing receipt output cannot be represented as successful timing publication.
    - Validation metadata changes only after the executable Wave 0 tests exist and pass.
  </acceptance_criteria>
  <done>The timing artifact is deterministic, safe, and fail-closed across the fallback probe edges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub matrix/context → shell | Workflow-controlled values select a shard and timing file. |
| ExUnit events → retained artifact | Runtime test data becomes durable CI evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-233-01 | Tampering | `library_tests_shard` shell | high | mitigate | Map expressions through `env:`, quote variables, retain strict shell failure, and contract-test one test invocation. |
| T-233-02 | Tampering | timing output path | high | mitigate | Accept only fixed CI-owned `/tmp` receipt names and unit-test rejected paths before any write. |
| T-233-03 | Repudiation | timing receipt | medium | mitigate | Stable schema, deterministic ordering, artifact upload, and same-command provenance. |
</threat_model>

## Artifacts this phase produces

- New module: `Sigra.CI.ExUnitTimingFormatter` in `test/support/ci/ex_unit_timing_formatter.ex`.
- New formatter functions: `init/1`, `handle_cast/2`, `build_receipt/2`, `validate_output_path!/1`, and `write_receipt!/2`.
- New environment/config key: `SIGRA_EXUNIT_TIMING_PATH`.
- New receipt fields: `schema_version`, `partition`, `tests`, aggregate counts; per-test fields `file`, `module`, `name`, `time_us`, `outcome`.
- New artifact names: `library-test-timings-1` and `library-test-timings-2`.
- New files: `test/support/ci/ex_unit_timing_formatter.ex`, `test/support/ci/ex_unit_timing_formatter_test.exs`, and `test/sigra/planning/phase_233_library_economics_contract_test.exs`.
- Modified files: `.github/workflows/ci.yml` and `.planning/phases/233-library-suite-economics/233-VALIDATION.md`.

<verification>
Focused formatter/workflow tests pass, formatting is clean, and the ordinary job contract proves parallel same-run timing with unchanged coverage.
</verification>

<success_criteria>
TEST-01 is implementation-complete locally: slow-test visibility and parallel execution coexist in one shard command, with fixed-path deterministic receipts ready for observed PR proof.
</success_criteria>

<output>
Create `.planning/phases/233-library-suite-economics/233-01-SUMMARY.md` when done.
</output>
