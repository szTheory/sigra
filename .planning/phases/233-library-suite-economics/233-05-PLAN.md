---
phase: 233-library-suite-economics
plan: 05
type: execute
wave: 5
depends_on: ["233-04"]
files_modified:
  - .planning/phases/233-library-suite-economics/233-EVIDENCE.json
  - .planning/phases/233-library-suite-economics/233-EVIDENCE.md
  - .planning/phases/233-library-suite-economics/233-VALIDATION.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "Per D-08, a retry-free real pull_request run observes both ordinary shards, the scaffold receiver, and the byte-identical `Library tests` required context green."
    - "The after ordinary-shard duration gap is strictly smaller than the inherited 192-second before gap, and both after durations plus receiver duration are recorded."
    - "The PR timing artifacts prove ordinary shards contain no scaffold files and the receiver contains upgrade, golden diff, and idempotency coverage."
    - "Slowest-test visibility and restored parallelism are observed in the same ordinary jobs, not inferred from YAML or a second run."
    - "The required aggregator remains fail-closed: only success/success reaches green; missing, skipped, cancelled, or failed dependencies are rejected by the executable contract."
  assumptions:
    - "Resolved TEST-01 unclassified row: completion requires same-run artifact provenance plus a successful PR observation."
    - "Resolved TEST-02 adjacency row: equal costs remain separate files, with lexical path and partition-1 cumulative-tie rules."
    - "Resolved TEST-02 empty row: null/missing costs, empty partitions, duplicates, omissions, and scaffold leakage are rejected."
    - "Resolved TEST-02 ordering row: equal-cost output order is stable and specified in the manifest contract."
    - "Resolved TEST-03 unclassified row: named PR execution and exact-success aggregation are the coverage edge."
  prohibitions: []
  artifacts:
    - path: .planning/phases/233-library-suite-economics/233-EVIDENCE.json
      provides: "final machine-readable before/after durations, run identity, coverage, and requirement disposition"
    - path: .planning/phases/233-library-suite-economics/233-EVIDENCE.md
      provides: "re-fetchable final evidence ledger with verbatim commands/log excerpts"
  key_links:
    - from: "final pull_request run"
      to: .planning/phases/233-library-suite-economics/233-EVIDENCE.json
      via: "exact SHA/event/retry-free run, jobs, artifacts, logs, and required-check capture"
    - from: "after ordinary shard durations"
      to: "TEST-02 disposition"
      via: "absolute gap compared mechanically with inherited 192-second baseline"
---

<objective>
Close the phase with a real pull-request observation and a committed, machine-readable before/after evidence ledger, per D-08.

Purpose: prove that faster means measured duration, preserved coverage means named execution, and merge blocking means the exact required context.
Output: final JSON/Markdown evidence with TEST-01/02/03 dispositions and no human UAT.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/233-library-suite-economics/233-02-SUMMARY.md
@.planning/phases/233-library-suite-economics/233-04-SUMMARY.md
@.planning/phases/233-library-suite-economics/233-EVIDENCE.json
@.planning/phases/233-library-suite-economics/233-EVIDENCE.md
@scripts/ci/ci-run-metrics.sh
@MAINTAINING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and inspect the final retry-free pull-request topology</name>
  <precondition>`gh auth status` succeeds, the scoped phase PR is open, and all functional commits through Plan 04 can be pushed to its head branch.</precondition>
  <files>.planning/phases/233-library-suite-economics/233-EVIDENCE.json, .planning/phases/233-library-suite-economics/233-EVIDENCE.md</files>
  <read_first>
    - `233-04-SUMMARY.md` — final functional head, manifest interface, and focused verification.
    - Existing `233-EVIDENCE.json`/`.md` — baseline and timing-probe slots to preserve.
    - `.github/workflows/ci.yml` final job ids and artifact names.
    - `scripts/ci/ci-run-metrics.sh` canonical duration output.
    - `MAINTAINING.md` exact required status name.
    - Every file listed in `<files>` before editing it.
  </read_first>
  <action>
    Push the functional head through Plan 04 to the existing scoped PR. Identify the resulting CI run by exact PR head SHA, `pull_request` event, workflow name, and creation time. Poll with `gh run watch &lt;run-id&gt; --exit-status` or bounded status reads with a hard timeout and no sleeps; retry one transient GitHub API failure once and diagnose deterministic failures instead of accepting a rerun. Require one attempt and success for both ordinary shard legs, the scaffold receiver, and aggregator.

    Capture `gh run view --json databaseId,event,headSha,conclusion,jobs,url`, `gh pr checks`, `ci-run-metrics.sh --jobs`, selected job logs, and all three timing artifacts. Inspect artifacts, not YAML, to require that upgrade, golden diff, and idempotency files appear in the scaffold receiver and no canonical scaffold path appears in ordinary receipts. Inspect commands/logs for the same-run formatter, absence of a second timing pass, and parallel ordinary execution. Write a provisional `after` JSON slot and verbatim Markdown slot; stop if any job is skipped, any artifact is absent, any expected file is missing, or the exact `Library tests` context is not successful.
  </action>
  <verify>
    <automated>RUN_ID=$(jq -r '.after.run_id' .planning/phases/233-library-suite-economics/233-EVIDENCE.json) &amp;&amp; gh run view "$RUN_ID" --json event,headSha,conclusion,jobs --jq 'select(.event == "pull_request" and .conclusion == "success")' &amp;&amp; gh pr checks "$(jq -r '.after.pr_number' .planning/phases/233-library-suite-economics/233-EVIDENCE.json)" --required</automated>
  </verify>
  <acceptance_criteria>
    - The observed run matches the functional head SHA and has no rerun attempt.
    - Both ordinary shards, scaffold receiver, and exact required context execute and succeed.
    - Three artifacts exist; receiver artifact names upgrade/golden/idempotency, ordinary artifacts name none of the six scaffold paths.
    - Missing/skipped/cancelled/failed evidence blocks rather than being recorded as green.
  </acceptance_criteria>
  <done>The final topology has direct, retry-free PR execution evidence for timing, coverage routing, and required status.</done>
</task>

<task type="auto">
  <name>Task 2: Seal before/after economics and requirement dispositions</name>
  <files>.planning/phases/233-library-suite-economics/233-EVIDENCE.json, .planning/phases/233-library-suite-economics/233-EVIDENCE.md, .planning/phases/233-library-suite-economics/233-VALIDATION.md</files>
  <read_first>
    - Task 1's provisional after slot and downloaded evidence.
    - `233-CONTEXT.md` D-01 through D-08.
    - `233-VALIDATION.md` phase sign-off.
    - `test/sigra/planning/phase_233_library_economics_contract_test.exs` final structural contract.
    - Every file listed in `<files>` before editing it.
  </read_first>
  <action>
    Compute `before_gap_seconds = abs(470 - 278)` and `after_gap_seconds = abs(after shard 1 - after shard 2)` from recorded job durations; require the latter to be strictly smaller. Record both after shard durations and scaffold receiver duration, their job ids, artifact digests, test-file membership, exact required-check result, and commands. Separate observed claims from structural contract claims in both ledgers.

    Add final TEST-01/02/03 dispositions keyed to D-01 through D-08 and the five resolved fallback probe rows. State that the prohibition probe retained zero bespoke values/safety/ethics prohibitions after precision filtering; record a breadcrumb that generic shell injection is canonical security work covered by this phase's STRIDE controls, not a minted descriptor. Treat the successful PR run as the full-suite execution and run the focused contract and formatter tests locally. Set `233-VALIDATION.md` sign-off rows green only after commands and evidence checks pass. Commit the evidence after the functional-head run, and state its observed SHA explicitly so a documentation-only evidence commit is never confused with the measured code SHA.
  </action>
  <verify>
    <automated>jq -e '(.before_gap_seconds == 192) and (.after_gap_seconds &lt; .before_gap_seconds) and (.after.retry_free == true) and (.after.event == "pull_request") and (.after.required_check.name == "Library tests") and (.after.required_check.conclusion == "success") and ([.requirements[] | select(.status == "complete") | .id] | sort == ["TEST-01","TEST-02","TEST-03"])' .planning/phases/233-library-suite-economics/233-EVIDENCE.json &amp;&amp; mix test test/support/ci/ex_unit_timing_formatter_test.exs test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The after gap is mechanically and strictly below 192 seconds; no qualitative balancing claim substitutes for it.
    - Both after ordinary durations and receiver duration are recorded with run/job provenance.
    - TEST-01/02/03 each map to observed and structural evidence plus their locked D-01..D-08 decisions.
    - All five fallback rows have explicit final resolutions and zero retained bespoke prohibitions is recorded with the security breadcrumb.
    - Focused and full suites pass; validation metadata reflects actual evidence only.
  </acceptance_criteria>
  <done>The phase goal and all three requirements are proven by measured economics, named PR coverage, and fail-closed required aggregation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub run/log/artifact data → completion claim | External observations decide whether requirements can close. |
| Evidence JSON → human-readable ledger | Machine facts are summarized without changing their meaning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-233-13 | Spoofing | final run selection | high | mitigate | Match exact PR event/head SHA/workflow and reject reruns or skipped jobs. |
| T-233-14 | Repudiation | before/after claim | high | mitigate | Preserve run ids, commands, raw excerpts, artifact digests, and machine-computed gaps. |
| T-233-15 | Tampering | coverage disposition | high | mitigate | Verify named files in receipts and exact required check through `gh`, plus executable static contracts. |
</threat_model>

## Artifacts this phase produces

- Updated files: `.planning/phases/233-library-suite-economics/233-EVIDENCE.json`, `233-EVIDENCE.md`, and validation status in `233-VALIDATION.md`.
- New JSON slot/key set: `after`, `before_gap_seconds`, `after_gap_seconds`, ordinary shard durations, scaffold receiver duration, `required_check`, artifact digests, observed file membership, `requirements`, and edge-probe dispositions.
- New evidence records: final run id, PR number, functional head SHA, event, retry-free status, job ids/conclusions/durations, exact `Library tests` result, and upgrade/golden/idempotency receipt membership.
- No new production symbols, runtime flags, dependencies, public APIs, or schema files.

<source_coverage>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Library shards do not become the post-Playwright pole | 01-05 | COVERED | Parallelism, extraction, balance, and real PR durations. |
| REQ | TEST-01 | Slow visibility without serial execution | 01,02,05 | COVERED | Same-run formatter plus observed PR artifacts. |
| REQ | TEST-02 | Comparable two-shard duration | 02,04,05 | COVERED | Measured manifest and strict smaller after-gap. |
| REQ | TEST-03 | Scaffold tests stop dominating ordinary shards without coverage loss | 03,05 | COVERED | Exact tags, unconditional receiver, named execution. |
| CONTEXT | D-01 | Remove serializing slowest mechanism | 01,02,05 | COVERED | Contract plus same-run PR proof. |
| CONTEXT | D-02 | Preserve same-run timing artifact | 01,02,05 | COVERED | Formatter, artifacts, evidence. |
| CONTEXT | D-03 | Keep two shards and byte-identical aggregator | 01,03,04,05 | COVERED | Matrix and required-name contracts. |
| CONTEXT | D-04 | Rebalance by measured cost and compare before/after | 02,04,05 | COVERED | Per-file costs, manifest, strict gap comparison. |
| CONTEXT | D-05 | Exact unified scaffold class; template render ordinary | 03,04,05 | COVERED | Exact-set and manifest guards. |
| CONTEXT | D-06 | Dedicated every-PR receiver | 03,05 | COVERED | Structural and observed proof. |
| CONTEXT | D-07 | Receiver feeds fail-closed `Library tests` | 03,05 | COVERED | Two-result exact-success aggregator. |
| CONTEXT | D-08 | Retry-free real PR coverage/duration evidence | 02,05 | COVERED | Timing probe and final observed run. |
| RESEARCH | — | Fixed timing output paths and safe shell interpolation | 01,03 | COVERED | Unit/contract tests and STRIDE mitigations. |
| RESEARCH | — | Exact six heavy modules, no template-render extraction | 03,04 | COVERED | Exact set plus exhaustive manifest. |
| RESEARCH | — | Deterministic artifact retention and evidence ledger | 01,02,05 | COVERED | Uploaded receipts and committed JSON/Markdown. |
| RESEARCH | — | No new packages | 01-05 | COVERED | All work uses ExUnit, Actions, gh, jq, and repo scripts. |

Deferred ideas remain excluded: the 29 unrelated todo matches and expanded generated-policy golden coverage do not appear in any task.
</source_coverage>

<verification>
The final evidence JSON passes strict comparisons, the referenced PR run and required check resolve through GitHub, and focused plus full suites pass.
</verification>

<success_criteria>
All locked decisions and TEST-01/02/03 are complete with retry-free real-PR evidence; the ordinary after-gap is smaller, scaffold coverage is named and merge-blocking, and `Library tests` remains byte-identical.
</success_criteria>

<output>
Create `.planning/phases/233-library-suite-economics/233-05-SUMMARY.md` when done.
</output>
