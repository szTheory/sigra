---
phase: 233-library-suite-economics
plan: 02
type: execute
wave: 2
depends_on: ["233-01"]
files_modified:
  - .planning/phases/233-library-suite-economics/233-EVIDENCE.json
  - .planning/phases/233-library-suite-economics/233-EVIDENCE.md
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "A real pull_request run proves both ordinary shards execute without serialization and publish same-run timing artifacts per D-01/D-02."
    - "The evidence records the inherited 470s/278s before-job baseline and a PR-observed per-file cost input suitable for D-04 rebalancing."
    - "The observed run is retry-free; missing, skipped, cancelled, or failed shard/artifact data blocks the evidence slot."
    - "Machine-readable evidence names its run id, PR number, head SHA, event, job durations, artifact identities, and per-file aggregate costs."
  assumptions:
    - "A single retry-free timing-probe PR run is an admissible measured-cost input for the first committed partition manifest; the final plan must separately prove the after-gap on the final topology."
  prohibitions: []
  artifacts:
    - path: .planning/phases/233-library-suite-economics/233-EVIDENCE.json
      provides: "machine-readable before and timing-probe observations"
    - path: .planning/phases/233-library-suite-economics/233-EVIDENCE.md
      provides: "human-readable commands, raw excerpts, and evidence disposition"
  key_links:
    - from: "GitHub pull_request run timing artifacts"
      to: .planning/phases/233-library-suite-economics/233-EVIDENCE.json
      via: "gh run/pr/artifact reads and deterministic per-file aggregation"
    - from: .planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md
      to: .planning/phases/233-library-suite-economics/233-EVIDENCE.json
      via: "verbatim inherited 470s/278s baseline provenance"
---

<objective>
Observe the tracer on a real PR and commit the measured per-file cost input required for later extraction and balancing.

Purpose: prevent a static workflow read from being mistaken for proof and give D-04 an empirical input rather than file-count guesses.
Output: retry-free PR receipts in machine-readable JSON plus a re-fetchable Markdown ledger.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/233-library-suite-economics/233-01-SUMMARY.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md
@scripts/ci/ci-run-metrics.sh
@.planning/phases/233-library-suite-economics/233-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create or update the scoped evidence PR and collect the timing-probe run</name>
  <precondition>`gh auth status` succeeds for the repository and the Plan 01 commit is on a non-main branch that can be pushed.</precondition>
  <files>.planning/phases/233-library-suite-economics/233-EVIDENCE.json, .planning/phases/233-library-suite-economics/233-EVIDENCE.md</files>
  <read_first>
    - `233-01-SUMMARY.md` — actual formatter schema, artifact names, and verification results.
    - `230-EVIDENCE.md` — baseline run id and the observed 470s/278s library job durations.
    - `scripts/ci/ci-run-metrics.sh` — canonical job-duration output.
    - `232-07-SUMMARY.md` — current evidence-ledger standard for retry-free real PR receipts.
    - Every file listed in `<files>` that already exists, before editing it.
  </read_first>
  <action>
    Push the current scoped phase branch, create a pull request against `main` if one does not exist, or reuse the open PR for the same head branch. Discover the run by matching workflow name, `pull_request` event, PR head SHA, and creation time; never select merely the latest repository run. Poll with `gh run watch &lt;run-id&gt; --exit-status` (or bounded `gh run view` status polling with a hard timeout and no sleeps), retry one transient GitHub API failure once, and stop on a deterministic CI failure.

    Fetch `gh run view --json databaseId,event,headSha,conclusion,jobs,url`, `gh pr checks`, `bash scripts/ci/ci-run-metrics.sh --jobs &lt;run-id&gt;`, both library-shard logs, and both timing artifacts. Require event `pull_request`, matching head SHA, conclusion success, both non-skipped shard jobs, and no rerun attempts. Initialize the JSON ledger with schema version 1 and `before` plus `timing_probe` slots; initialize Markdown with the exact producing commands and verbatim outputs. Do not claim TEST-02 complete yet.
  </action>
  <verify>
    <automated>jq -e '.schema_version == 1 and .before.shards["1"].duration_seconds == 470 and .before.shards["2"].duration_seconds == 278 and .timing_probe.event == "pull_request" and .timing_probe.retry_free == true and (.timing_probe.shards | length == 2)' .planning/phases/233-library-suite-economics/233-EVIDENCE.json &amp;&amp; gh run view "$(jq -r '.timing_probe.run_id' .planning/phases/233-library-suite-economics/233-EVIDENCE.json)" --json event,headSha,conclusion,jobs --jq 'select(.event == "pull_request" and .conclusion == "success")'</automated>
  </verify>
  <acceptance_criteria>
    - The selected run is tied to the PR's exact head SHA and is retry-free.
    - Both ordinary shard jobs execute and succeed; their timing artifacts download and parse.
    - JSON and Markdown both record producing commands and immutable run identity.
    - Missing or skipped evidence stops the task rather than producing a partial green slot.
  </acceptance_criteria>
  <done>A real PR run proves the tracer and supplies complete raw inputs for deterministic cost aggregation.</done>
</task>

<task type="auto">
  <name>Task 2: Aggregate measured per-file costs and seal the pre-extraction evidence slot</name>
  <files>.planning/phases/233-library-suite-economics/233-EVIDENCE.json, .planning/phases/233-library-suite-economics/233-EVIDENCE.md</files>
  <read_first>
    - The downloaded Plan 01 timing artifacts and job logs from Task 1.
    - `233-EVIDENCE.json` and `233-EVIDENCE.md` as committed by Task 1.
    - `233-CONTEXT.md` D-04/D-08 proof requirements.
    - Every file listed in `<files>` before editing it.
  </read_first>
  <action>
    Validate both receipts against the Plan 01 schema, then aggregate `time_us` by repository-relative test file. Reject null/non-integer/negative timing values, duplicate test identities across receipts, a missing artifact, or an empty shard. Sort the committed `per_file_costs` by descending total microseconds and then path ascending for equal totals. Preserve the raw artifact names and SHA-256 digests in JSON, but commit analyzed aggregates rather than volatile raw receipts. In Markdown, record the exact deterministic aggregation command, the two observed job durations, confirmation that the shard command lacked serialization, and the artifact/log lines proving same-run receipt creation. Mark TEST-01 observed; mark TEST-02 as measured-input-ready, not complete.
  </action>
  <verify>
    <automated>jq -e '(.timing_probe.per_file_costs | length) &gt; 0 and ([.timing_probe.per_file_costs[].time_us] | all(type == "number" and . &gt;= 0)) and ([.timing_probe.per_file_costs[].path] | length) == ([.timing_probe.per_file_costs[].path] | unique | length) and (.timing_probe.artifacts | length == 2)' .planning/phases/233-library-suite-economics/233-EVIDENCE.json &amp;&amp; rg -n 'pull_request|retry-free|same run|per-file|470|278' .planning/phases/233-library-suite-economics/233-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Every committed per-file cost is non-negative, uniquely keyed, and derived from the two validated receipts.
    - Equal totals have stable lexical path ordering; no null/empty input is silently accepted.
    - Raw receipt digests preserve provenance without committing volatile raw output.
    - TEST-01 is observed on PR; TEST-02 remains open pending extraction, manifest, and final after-run comparison.
  </acceptance_criteria>
  <done>The phase has a re-derivable, machine-readable empirical input for scaffold extraction and cost balancing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub API/artifacts → repository evidence | External CI observations determine whether performance claims are admissible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-233-04 | Spoofing | run selection | high | mitigate | Match PR number, event, exact head SHA, workflow, and run identity before capture. |
| T-233-05 | Repudiation | evidence ledger | high | mitigate | Commit run ids, commands, raw excerpts, artifact names/digests, and machine-readable dispositions. |
| T-233-06 | Tampering | timing aggregation | medium | mitigate | Reject malformed values/duplicates/empty inputs and use stable sort rules recorded in JSON. |
</threat_model>

## Artifacts this phase produces

- New files: `.planning/phases/233-library-suite-economics/233-EVIDENCE.json` and `233-EVIDENCE.md`.
- New JSON keys: `schema_version`, `before`, `timing_probe`, `run_id`, `pr_number`, `head_sha`, `event`, `retry_free`, `shards`, `artifacts`, `sha256`, `per_file_costs`, `path`, `time_us`, and requirement dispositions.
- New evidence slots: `before` (inherited 470s/278s) and `timing_probe` (real PR).
- No production symbols, runtime flags, dependencies, or schema files.

<verification>
JSON structural assertions pass, the referenced GitHub run still resolves as successful `pull_request`, and the Markdown ledger contains exact provenance.
</verification>

<success_criteria>
The tracer is observed in CI and the phase owns measured per-file costs suitable for deterministic post-extraction balancing.
</success_criteria>

<output>
Create `.planning/phases/233-library-suite-economics/233-02-SUMMARY.md` when done.
</output>
