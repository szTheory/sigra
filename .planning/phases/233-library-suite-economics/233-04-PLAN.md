---
phase: 233-library-suite-economics
plan: 04
type: execute
wave: 4
depends_on: ["233-03"]
files_modified:
  - test/support/ci/library_test_partitions.exs
  - .github/workflows/ci.yml
  - test/sigra/planning/phase_233_library_economics_contract_test.exs
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "Per D-03, ordinary library coverage remains exactly two shards behind the byte-identical `Library tests` aggregator."
    - "Per D-04, the post-extraction file assignment is derived from the Plan 02 measured per-file costs, not file count."
    - "Every ordinary `*_test.exs` file is assigned exactly once; all six scaffold files are assigned zero times; template render is assigned once."
    - "FLAGGED/RESOLVED TEST-02 adjacency/ordering: equal measured costs use lexical path order and the next file goes to the lower cumulative-cost shard, with partition 1 winning an exact cumulative tie."
    - "FLAGGED/RESOLVED TEST-02 empty/single/null: missing or null costs, an empty partition, duplicate paths, uncovered files, and scaffold leakage all fail the contract."
  assumptions:
    - "Measured file totals from the retry-free Plan 02 run are representative enough for the first two-list assignment; D-04 still requires the final PR duration comparison before completion."
  prohibitions: []
  artifacts:
    - path: test/support/ci/library_test_partitions.exs
      provides: "committed reviewable two-list ordinary test manifest with measured cost provenance"
    - path: .github/workflows/ci.yml
      provides: "matrix selection of explicit manifest files"
    - path: test/sigra/planning/phase_233_library_economics_contract_test.exs
      provides: "one-to-one coverage and deterministic balance contract"
  key_links:
    - from: .planning/phases/233-library-suite-economics/233-EVIDENCE.json
      to: test/support/ci/library_test_partitions.exs
      via: "measured per-file costs and stable greedy assignment"
    - from: test/support/ci/library_test_partitions.exs
      to: .github/workflows/ci.yml
      via: "matrix partition selects one explicit list passed to mix test"
      pattern: "library_test_partitions"
---

<objective>
Replace file-count round robin with a deterministic, measured two-list manifest after scaffold extraction, per D-03 and D-04.

Purpose: reduce shard idle time without adding workers or losing/duplicating ordinary coverage.
Output: a reviewable cost-based partition manifest, workflow selection, and exhaustive coverage guards.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/233-library-suite-economics/233-02-SUMMARY.md
@.planning/phases/233-library-suite-economics/233-03-SUMMARY.md
@.planning/phases/233-library-suite-economics/233-EVIDENCE.json
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate and wire the measured two-list ordinary manifest</name>
  <reversibility rating="costly">The committed assignment is cheap to regenerate but coordinated with CI invocation and exhaustive coverage guards.</reversibility>
  <files>test/support/ci/library_test_partitions.exs, .github/workflows/ci.yml, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - `233-EVIDENCE.json` timing-probe per-file costs and run provenance.
    - `233-03-SUMMARY.md` exact scaffold set and receiver topology.
    - `.github/workflows/ci.yml` current ordinary shard command after extraction.
    - `233-PATTERNS.md` manifest analog and bounded workflow parser.
    - Every file listed in `<files>` that already exists, before editing it.
  </read_first>
  <behavior>
    - "The manifest has explicit partition 1 and partition 2 file lists with measured total cost metadata and source run id."
    - "Assignment sorts candidates by descending measured cost/path and greedily assigns to the currently lighter shard."
    - "The workflow selects only the list matching matrix.partition and invokes one ordinary test process with scaffold excluded."
  </behavior>
  <action>
    Start RED with contract assertions for manifest provenance, exactly two explicit lists, and workflow consumption. Derive the candidate universe after removing the six scaffold paths from the Plan 02 measured costs. Sort by descending microseconds and path ascending; assign each file to the lower cumulative-cost partition, with partition 1 winning an exact cumulative tie. Commit the resulting explicit lists and source timing-probe run id in `test/support/ci/library_test_partitions.exs`; do not add a runtime glob, round-robin fallback, or implicit discovery path.

    Update `library_tests_shard` to resolve `matrix.partition` to the corresponding explicit file list and pass those repository-relative paths to its single `mix test` invocation while retaining `--exclude scaffold`, both formatters, fixed receipt path, services, and two-leg matrix. Keep `MIX_TEST_PARTITION` only if another proven contract consumes it; the explicit list, not Mix round robin, must determine file ownership. The contract must compare the manifest against the repository's ordinary test universe and the evidence source run.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The manifest cites the real timing-probe run and represents exactly two non-empty explicit lists.
    - Each ordinary test file is selected exactly once; scaffold files are selected zero times.
    - The contract expands both manifest-driven shard commands to non-empty, disjoint explicit path sets; Plan 05 supplies observed execution.
    - The workflow remains two-leg and starts no additional ordinary test process.
  </acceptance_criteria>
  <done>Measured cost, rather than sorted file position, now owns ordinary shard assignment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make manifest drift and edge behavior fail closed</name>
  <files>test/support/ci/library_test_partitions.exs, test/sigra/planning/phase_233_library_economics_contract_test.exs</files>
  <read_first>
    - Task 1's committed manifest format and contract test.
    - The exact six scaffold module paths from Plan 03.
    - `test/sigra/install/template_render_test.exs` ordinary-suite invariant.
    - Every file listed in `<files>` before editing it.
  </read_first>
  <behavior>
    - "Adding an unlisted ordinary test, duplicating a path, leaking a scaffold path, or emptying a partition makes the contract red."
    - "A missing/null/negative measured cost or source run id makes provenance validation red."
    - "Equal file costs and equal cumulative partition totals resolve by the documented stable tie rules."
  </behavior>
  <action>
    Expand the contract around pure parsed manifest data so the listed mutation cases are mechanically rejected without editing production files during the test. Assert repository discovery includes all `test/**/*_test.exs` files that the ordinary suite owns, subtracts only the exact scaffold set and intentional non-suite fixtures, and requires template render exactly once. Assert paths are repository-relative, sorted within each partition for review stability, and the stored measured totals equal the sum of cited evidence costs. Keep the contract fail-closed when evidence keys or paths are missing.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_233_library_economics_contract_test.exs &amp;&amp; mix format --check-formatted test/support/ci/library_test_partitions.exs test/sigra/planning/phase_233_library_economics_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All adjacency, empty, singleton, null, duplicate, missing, and tie-order cases have named deterministic assertions.
    - Coverage cannot silently drift when a test file is added or reclassified.
    - Stored totals and source run provenance agree with machine-readable evidence.
    - No scaffold or template-render classification regression passes the contract.
  </acceptance_criteria>
  <done>The measured manifest is exhaustive, deterministic, and self-invalidating on coverage drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Measured evidence → committed selection | External runtime data determines which correctness tests run in each worker. |
| Manifest → shell argv | Committed paths become test-runner arguments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-233-10 | Tampering | partition manifest | high | mitigate | Repository-relative allowlisted test paths, exhaustive exact-once guard, and evidence-total reconciliation. |
| T-233-11 | Denial of Service | shard imbalance | medium | mitigate | Measured greedy assignment plus final observed duration comparison in Plan 05. |
| T-233-12 | Tampering | manifest shell expansion | high | mitigate | Emit only validated repository-relative test paths and keep workflow context outside shell bodies. |
</threat_model>

## Artifacts this phase produces

- New file: `test/support/ci/library_test_partitions.exs`.
- New manifest interface: partition selector argument `--partition 1|2` returning explicit repository-relative paths.
- New manifest data: source timing run id, partition 1 paths/estimated microseconds, partition 2 paths/estimated microseconds.
- New deterministic rules: descending measured cost/path input order; lower-total assignment; partition 1 exact-total tie winner; lexical presentation order.
- Modified workflow/config: ordinary shard consumes explicit manifest paths while retaining matrix `[1, 2]`.
- Expanded Phase 233 contract symbols/helpers for manifest parsing, repository universe calculation, exact-once coverage, provenance, and mutation cases (exact private names recorded in summary).

<verification>
The exhaustive contract validates both manifest-driven shard commands; no file is duplicated, omitted, or misclassified, with observed execution required in Plan 05.
</verification>

<success_criteria>
TEST-02 is structurally ready for observed proof: two measured partitions cover the ordinary suite exactly once and retain scaffold separation.
</success_criteria>

<output>
Create `.planning/phases/233-library-suite-economics/233-04-SUMMARY.md` when done.
</output>
