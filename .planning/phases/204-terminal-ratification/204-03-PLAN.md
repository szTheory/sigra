---
phase: 204-terminal-ratification
plan: 03
type: execute
wave: 2
depends_on: [204-01, 204-02]
files_modified:
  - test/example/priv/static/assets/css/app.css
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots
autonomous: true
requirements: [RATIFY-01]
user_setup:
  - service: local-example-server
    why: "Playwright recapture/compare runs against a booted example app"
    dashboard_config:
      - task: "Boot the example dev server on an alt PORT (4000 collides with Rulestead Docker); pre-compile before launch to avoid the code-reload crash; export SIGRA_EXAMPLE_URL to that base URL"
        location: "test/example (see CLAUDE.md local-development + reference_example_playwright_boot)"
must_haves:
  truths:
    - "The .vt-status-pill base rule and .vt-status-pill--ok variant in app.css raise the --vt-color-ink proportion so text contrast is ≥4.5:1 on BOTH light and dark themes, verified by the axe gate reporting pass (D-03)."
    - "The recaptured impersonation-banner canary PNG, the recaptured audit-mobile PNGs (user-audit, audit-explorer + any pill-rendering mobile checkpoint), and the app.css contrast fix all land in the SAME commit (D-05)."
    - "After the commit, snapshot-recapture-gate.sh (--base HEAD lanes) sees zero post-commit drift: compare-mode across all 6 projects shows 0 diffs and the canary/drift guard passes."
    - "Both allowlists (snapshot-allowlist, snapshot-allowlist-design) are empty (comments only) in the committed tree."
    - "quality-ledger-monotonic.sh --base origin/main exits 0."
  artifacts:
    - test/example/priv/static/assets/css/app.css
    - "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/user-audit-admin-checkpoints-mobile.png (and sibling recaptured mobile + canary PNGs)"
  key_links:
    - "admin-checkpoints.spec.ts runs all 6 checkpoints as ONE linear test per project; the .vt-status-pill axe failure on the impersonation-banner page aborts before the downstream audit captures — the pill fix unblocks the stale audit-mobile baselines."
    - "The impersonation-banner canary is non-allowlistable; its recaptured pixels must ride the same commit as the fix so the --base HEAD guard sees the new PNG as the established baseline."
  prohibitions:
    - "MUST NOT add the impersonation-banner (or board-notice) canary slug to any allowlist — canaries are NEVER allowlistable (D-05)."
    - "MUST NOT leave any slug in snapshot-allowlist or snapshot-allowlist-design after the recapture commit — both reset to empty/comments-only (D-02)."
    - "MUST NOT force-recapture chromium/dark checkpoints, design-gallery lanes, or component byte-goldens — those are proven by compare-mode zero-drift, not recapture (D-01)."
    - "MUST NOT corrupt app.css: the file has stray-comment corruption that silently drops the next CSS rule. After editing, the surrounding .vt-status-pill--ok and .vt-table-panel rules MUST survive intact (D-04)."
    - "MUST NOT introduce any new Tier-2 ledger ratchet (D-11)."
---

<objective>
The one sequencing trap of the phase: fix the `.vt-status-pill` WCAG AA contrast failure (D-03),
recapture ONLY the pill-blocked audit-mobile + canary baselines, and land the CSS fix +
recaptured PNGs in the SAME commit (D-05) so that vs `origin/main` the new PNGs are the
established baseline and the in-phase `--base HEAD` guard sees zero post-commit drift. Prove
everything else via compare-mode zero-drift idempotency (D-01/D-02), then confirm the monotonic
ledger guard is green vs `origin/main`.

Purpose: `admin-checkpoints.spec.ts` runs 6 checkpoints linearly per project; the pill's axe
failure on the impersonation-banner page aborts before the audit captures — which is exactly why
the audit-mobile baselines are stale. Fixing the pill unblocks them; recapturing in one commit
keeps the non-allowlistable canary honest.

Output: Contrast-fixed app.css + recaptured mobile/canary PNGs in one commit; empty allowlists;
green recapture gate and monotonic guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204-terminal-ratification/204-CONTEXT.md
@scripts/ci/snapshot-recapture-gate.sh
@scripts/ci/snapshot-canary-guard.sh
@scripts/ci/quality-ledger-monotonic.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise .vt-status-pill contrast to ≥4.5:1 on both themes, axe-verified (D-03/D-04)</name>
  <files>test/example/priv/static/assets/css/app.css</files>
  <read_first>
    - test/example/priv/static/assets/css/app.css (lines ~1089-1112: base `.vt-status-pill` uses `color-mix(in oklab, var(--vt-color-caution) 62%, var(--vt-color-ink))` ≈3.33:1; the `.vt-status-pill--ok` variant uses `var(--vt-color-primary) 64%, var(--vt-color-ink)`; the surrounding `.vt-table-panel` rule must survive)
    - CLAUDE.md (app.css stray-comment corruption hazard — edit precisely)
    - scripts/ci/snapshot-canary-guard.sh (impersonation-banner canary semantics)
  </read_first>
  <action>
    Edit ONLY the `color:` color-mix ratios of the base `.vt-status-pill` and the
    `.vt-status-pill--ok` variant in app.css (~lines 1089-1112) to raise the `--vt-color-ink`
    proportion (lower the caution/primary proportion) until the text contrast is ≥4.5:1 on BOTH
    light and dark. The axe gate is the oracle — pick the ratio it confirms (CONTEXT.md suggests
    caution/primary in the ~45-50% range). Do not change the `background` color-mix, padding,
    radius, or any other property; do not touch the `/* Positive variant … */` comment in a way
    that could drop the next rule. After editing, re-read the surrounding region and confirm the
    `.vt-status-pill--ok` block and the `.vt-table-panel` rule (line ~1114) are intact and
    well-formed (no orphaned/merged braces from comment corruption).

    Verify contrast with the axe gate, not by eye: boot the example app (alt PORT, pre-compiled,
    SIGRA_EXAMPLE_URL set), then run the admin-checkpoints axe pass which fails on the pill — it
    must now report PASS on the impersonation-banner page across chromium + mobile + dark. (The
    full recapture/zero-drift proof is Task 2.)
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -n 'vt-status-pill\|vt-table-panel\|color-mix' test/example/priv/static/assets/css/app.css | sed -n '1,12p'</automated>
  </verify>
  <acceptance_criteria>
    - The base `.vt-status-pill` and `.vt-status-pill--ok` `color:` color-mix ratios are raised toward `--vt-color-ink`; no other property changed.
    - Re-reading app.css around lines 1089-1116 shows `.vt-status-pill`, `.vt-status-pill--ok`, and `.vt-table-panel` rules all intact (balanced braces, no dropped rule) — D-04 guard.
    - The admin-checkpoints axe pass on the impersonation-banner page reports PASS on chromium, mobile, and dark (the contrast failure is resolved) — axe gate is the oracle, confirmed during Task 2's gate run.
  </acceptance_criteria>
  <done>The pill contrast failure is fixed at ≥4.5:1 both themes, app.css uncorrupted.</done>
</task>

<task type="auto">
  <name>Task 2: Same-commit recapture + zero-drift idempotency + monotonic guard (D-01/D-02/D-05)</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots, test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/snapshot-allowlist-design</files>
  <read_first>
    - scripts/ci/snapshot-recapture-gate.sh (drives compare across 6 projects; routes slugs to lanes; `--base HEAD` guard; RUN_PARITY for the parity lane)
    - scripts/ci/snapshot-canary-guard.sh (--base, --allow, --require-all, canary handling — canary fires on modify/delete of an established baseline)
    - scripts/ci/quality-ledger-monotonic.sh (--base origin/main forward-only)
    - test/example/priv/playwright/snapshot-allowlist and snapshot-allowlist-design (must end empty/comments-only)
  </read_first>
  <action>
    Follow the D-02 recapture sequence precisely:
    (1) With the Task-1 pill fix in the working tree, recapture `admin-checkpoints-mobile` ONLY —
        `--update-snapshots` scoped to the `admin-checkpoints-mobile` project (the lane that
        renders the pill on the impersonation-banner page and was aborting before the audit
        captures). This regenerates the stale `user-audit` + `audit-explorer` mobile PNGs and the
        `impersonation-banner` mobile canary PNG, plus any other mobile checkpoint that renders the
        pill.
    (2) Restore any incidentally-drifted NON-pill, NON-audit mobile baseline PNGs (git checkout
        those specific PNGs) so only the intended slugs differ.
    (3) Re-run ALL 6 projects (admin-checkpoints + admin-design × chromium/mobile/dark) in
        compare mode and confirm 0 diffs — chromium/dark/design/byte-goldens are proven by
        compare-mode zero-drift, NOT recapture (D-01).
    (4) STAGE the app.css fix (Task 1) AND the recaptured mobile/canary PNGs together and commit
        them in ONE commit (D-05 hard constraint), with both allowlists empty (comments only).
        The recapture gate's `--base HEAD` canary/drift guard then sees the new canary PNG as the
        established baseline → zero post-commit drift.
    (5) Run `scripts/ci/snapshot-recapture-gate.sh user-audit audit-explorer` (plus any other
        recaptured slug) to drive the full compare + canary guard end to end (all-green ==
        approval, no human review). Then run
        `scripts/ci/quality-ledger-monotonic.sh --base origin/main` and confirm exit 0.

    Never add the `impersonation-banner` (or `board-notice`) canary to any allowlist. Leave both
    allowlists empty after the commit.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && echo "checkpoints:"; grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -c '[^[:space:]]' || true; echo "design:"; grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -c '[^[:space:]]' || true; bash scripts/ci/quality-ledger-monotonic.sh --base origin/main</automated>
  </verify>
  <acceptance_criteria>
    - Both allowlists contain ZERO non-comment, non-blank lines after the commit (the two `grep -v '^#' … grep -c` checks each report 0).
    - `git show --stat HEAD` confirms the app.css contrast fix AND the recaptured `impersonation-banner` / `user-audit` / `audit-explorer` (and any other pill-rendering mobile) PNGs are in the SAME commit (D-05).
    - `scripts/ci/snapshot-recapture-gate.sh user-audit audit-explorer` (plus any other recaptured slug) exits 0 — compare-mode 0 diffs across all 6 projects and the `--base HEAD` canary/drift guard passes (zero post-commit drift).
    - `scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0.
    - The `impersonation-banner` / `board-notice` canary slugs appear in NO allowlist.
  </acceptance_criteria>
  <done>The pill fix + recaptured canary/audit baselines are one commit, allowlists empty, recapture gate and monotonic guard green vs origin/main; everything else proven by zero-drift compare.</done>
</task>

</tasks>

<verification>
- `scripts/ci/snapshot-recapture-gate.sh user-audit audit-explorer` exits 0 (compare 0 diffs across 6 projects + canary/drift guard green at `--base HEAD`).
- `git show --stat HEAD` shows app.css + the recaptured mobile/canary PNGs in one commit (D-05).
- Both allowlists are empty (comments only).
- `scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0.
- The admin-checkpoints axe pass on the impersonation-banner page is green on chromium/mobile/dark.
</verification>

<success_criteria>
The milestone's stale audit-mobile baselines are unblocked and recaptured in a single,
canary-honest commit; all other baselines are proven steady via compare-mode zero-drift; the
monotonic ledger guard is green vs `origin/main`; both allowlists are empty.
</success_criteria>

<output>
Create `.planning/phases/204-terminal-ratification/204-03-SUMMARY.md` when done.
</output>