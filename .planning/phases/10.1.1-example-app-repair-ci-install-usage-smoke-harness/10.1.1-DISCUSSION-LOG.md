# Phase 10.1.1: example-app repair + CI install/usage smoke harness — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10
**Phase:** 10.1.1-example-app-repair-ci-install-usage-smoke-harness
**Areas discussed:** Bug fix scope, Session store unification (B6), CI smoke harness approach, Audit schema fix (B8)

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Bug fix scope | Which of B5-B9 + doc drift + llms.txt are in-scope vs deferred | ✓ |
| Session store unification (B6) | user_tokens vs user_sessions — architectural | ✓ |
| CI smoke harness approach | Install / browser / curl / combination | ✓ |
| Audit schema fix (B8) | actor_id uuid vs users.id bigint mismatch | ✓ |

---

## Bug fix scope

### Which bugs should this phase fix? (multi-select)

| Option | Description | Selected |
|--------|-------------|----------|
| All 5 UAT bugs (B5-B9) | B5 confirmation email, B6 dual session stores, B7 sudo template, B8 audit type mismatch, B9 LiveView form submit | ✓ |
| Doc drift in getting-started.md | log-in/log_in URLs, UserRegistrationLive module name, fetch_current_scope_for_user, on_mount names | ✓ |
| Missing doc/llms.txt | ex_doc 0.40.1 auto-generation broken or missing config | ✓ |
| Brew Postgres collision doc in runbook | Cosmetic runbook update for the collision case | ✓ |

**User's choice:** All four in scope.

### B9 — password login form swallowed inside LiveView. How to fix?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep LiveView, fix form submit (Recommended) | phx-submit with handle_event, matches phx.gen.auth 1.8 | |
| Convert login to dead view controller | Plain controller + HTML form, breaks consistency | |
| Hybrid: LiveView wrapper, form POSTs to controller | LiveView renders, form action → controller. Matches what Phoenix 1.8 phx.gen.auth actually generates | ✓ |

**User's choice:** Hybrid pattern. Rationale: matches real Phoenix 1.8 phx.gen.auth output.

---

## B6 — Session store unification

### Dual session stores — what's the fix?

| Option | Description | Selected |
|--------|-------------|----------|
| Rip out user_tokens, use Sigra.Auth end-to-end (Recommended) | UserAuth.log_in_user calls Sigra.Auth.create_session/4. Drop phx.gen.auth legacy. Surfaces generator gaps | ✓ |
| Keep user_tokens, make SessionLive read from it | Minimal change but contradicts library architecture | |
| Keep both, add adapter layer | Worst of both worlds | |

**User's choice:** Rip out and unify on Sigra.

---

## B8 — Audit schema fix

### actor_id uuid vs users.id bigint — canonical fix?

| Option | Description | Selected |
|--------|-------------|----------|
| Regenerate example migration with bigint actor_id (Recommended) | Match phx.gen defaults, document both PK types via install option | |
| Switch example users.id to uuid | Match audit_events.actor_id. Bigger blast radius, affects every FK | ✓ |
| Make actor_id polymorphic | Library-level change, probably overkill | |
| Installer detects user PK type, emits matching migration | Correct long-term, scope creeps into installer | |

**User's choice:** Switch example users.id to uuid (diverged from recommended).

### How far does the uuid switch propagate?

| Option | Description | Selected |
|--------|-------------|----------|
| Example app only, library stays PK-agnostic (Recommended) | Change example migrations/schemas to uuid, library flexible | |
| Library default flips to uuid too | Update generator templates, bigger docs/migration story, probably own phase | ✓ |
| Example uuid + installer --primary-key flag | Medium scope, installer work bleeds in | |

**User's choice:** Library default flips to uuid. Scope expansion flagged in CONTEXT D-10 for planner to split cleanly.

---

## B5 — Confirmation email wiring

### What wiring does this phase add?

| Option | Description | Selected |
|--------|-------------|----------|
| Call Sigra.Auth.request_confirmation in RegistrationLive (Recommended) | Wire library function into success branch, remove stub | ✓ |
| Move email trigger into Sigra.Auth.register_user itself | Library-level change, more opinionated, probably wrong scope | |

**User's choice:** Wire at the example app level; keep library API explicit.

---

## CI smoke harness approach

### What should the harness do? (multi-select)

| Option | Description | Selected |
|--------|-------------|----------|
| Fresh mix phx.new + mix sigra.install | Catches generator output bugs (would have caught B8) | ✓ |
| HTTP curl smoke of critical routes | Fast, would have caught B7 and B3/B4 | ✓ |
| Playwright browser smoke (full flows) | Catches JS/LiveView bugs (B1, B2, B9) | ✓ |
| mix test --include example_app (current, keep) | Existing ConnTest layer | ✓ |

**User's choice:** All four layers.

### How should the 4 layers be structured as CI jobs?

| Option | Description | Selected |
|--------|-------------|----------|
| Separate jobs, all required on PR (Recommended) | 4 parallel GitHub Actions, clearest signal | ✓ |
| Separate jobs, playwright informational only | continue-on-error for browser, risk signal ignored | |
| One serialized job running all 4 | Simpler file, one failure masks later steps | |
| install_smoke nightly, rest on PR | Faster PRs, slower regression signal | |

**User's choice:** Four parallel jobs, all required.

### Playwright golden path — minimum flow?

| Option | Description | Selected |
|--------|-------------|----------|
| Register → confirm → login → logout (Recommended) | Core UAT 4.1 flow, catches B1/B2/B5/B9 | |
| Above + sudo re-auth + session list | Adds B6/B7 coverage | |
| Above + MFA enrollment | Full lifecycle, catches phase 6 UAT items too | ✓ |

**User's choice:** Full lifecycle including MFA enrollment.

---

## Claude's Discretion

- Exact `doc/llms.txt` fix approach (config change vs mix docs.llms step)
- Sudo template fix (rename assign vs add field)
- Playwright test framework choice (Playwright Test default)
- install_smoke caching strategy
- Playwright TOTP library choice

## Deferred Ideas

- Installer `--primary-key bigint` flag — own phase
- Phase 6 MFA backup code regeneration assertion
- Phase 5 OAuth human UAT
- Nyquist VALIDATION.md for 10.1 and 10.1.1
- REQUIREMENTS.md / ROADMAP.md traceability cleanup
- Polymorphic `actor_id` (rejected as overkill)
- Library-level auto-send-confirmation in `Sigra.Auth.register_user` (rejected)
