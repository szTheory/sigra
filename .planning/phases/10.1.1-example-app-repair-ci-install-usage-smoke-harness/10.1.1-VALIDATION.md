---
phase: 10.1.1
slug: example-app-repair-ci-install-usage-smoke-harness
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-10
---

# Phase 10.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Note: see RESEARCH.md "Validation Architecture" section for the full mapping of D-13/D-14/D-15 CI jobs to B1–B9 failure modes. This file is the execution-time sampling contract; the per-task rows below are the planner-filled execution sampling map.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (library + example), bash+curl (http smoke), Playwright Test (browser smoke), mix phx.new + mix sigra.install (install smoke) |
| **Config file** | `test/example/mix.exs`, `.github/workflows/ci.yml`, `test/example/priv/playwright/playwright.config.ts` (to be created) |
| **Quick run command** | `mix test --include example_app` |
| **Full suite command** | `mix test --include example_app && (cd test/example/priv/playwright && npx playwright test)` |
| **Estimated runtime** | ~60s quick / ~180s full (local) |

---

## Sampling Rate

- **After every task commit:** Run `mix compile --warnings-as-errors` and targeted `mix test` for touched modules
- **After every plan wave:** Run `mix test --include example_app`
- **Before `/gsd-verify-work`:** Full suite must be green locally; five CI jobs green on PR
- **Max feedback latency:** 60 seconds for unit/compile feedback

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 10.1.1-01-01 | 01 | 1 | D-02 | — | Docs no longer reference dead routes/modules | docs-grep | `rg -n "users/log-in\|users/log-out\|UserRegistrationLive\|fetch_current_scope_for_user\|on_mount :require_authenticated" guides/introduction/getting-started.md; test $? -eq 1` | ✅ | ⬜ pending |
| 10.1.1-01-02 | 01 | 1 | D-03 | — | `mix docs` produces `doc/llms.txt` without warnings | docs-build | `rm -rf doc && mix docs --warnings-as-errors && test -f doc/llms.txt && echo "llms.txt present"` | ✅ | ⬜ pending |
| 10.1.1-01-03 | 01 | 1 | D-04 | — | Runbook documents port-5432 collision workaround | docs-grep | `rg -n "brew services stop postgresql@14" scripts/uat/RUNBOOK.md && rg -n "port-5432 collision\|5432 collision" scripts/uat/RUNBOOK.md` | ✅ | ⬜ pending |
| 10.1.1-02-01 | 02 | 1 | D-05 | T-10.1.1-02-01 (Information Disclosure: confirmation email) | Registration triggers confirmation email; stub marker deleted | compile + grep | `cd test/example && mix compile --warnings-as-errors 2>&1 \| tee /tmp/b5.log && rg -n "deliver_user_confirmation_instructions" lib/example_web/live/registration_live.ex && rg -n "Phase 3 will send confirmation email here" lib/example_web/live/registration_live.ex; test $? -eq 1` | ✅ | ⬜ pending |
| 10.1.1-02-02 | 02 | 1 | D-08 | T-10.1.1-02-02 (Tampering: sudo password field) | Sudo form uses `<.input field={f[:password]}>` — no naked input KeyError | compile + conntest + grep | `cd test/example && mix compile --warnings-as-errors && MIX_ENV=test mix test test/example_web/controllers/auth/sudo_controller_test.exs 2>/dev/null; rg -n "field={f\\[:password\\]}" lib/example_web/controllers/auth/sudo_html.ex` | ✅ | ⬜ pending |
| 10.1.1-03-01 | 03 | 1 | D-06 | T-10.1.1-03-01 (Spoofing: session store unification) | Wave 0 RED: ConnTest pinning canonical `user_sessions` store must fail before impl lands | conntest (RED gate) | `cd test/example && mix test --include example_app test/example_web/user_auth_test.exs 2>&1 \| tee /tmp/b6-red.log; grep -E "failures\|0 failures" /tmp/b6-red.log` | ✅ | ⬜ pending |
| 10.1.1-03-02 | 03 | 1 | D-06, D-07 | T-10.1.1-03-02 (Tampering: session fixation via renew_session preserved), T-10.1.1-03-04 (Repudiation: session.create audit row) | GREEN: log_in_user writes to canonical `user_sessions` with IP/user_agent; templates synced | compile + conntest + grep | `cd test/example && mix compile --warnings-as-errors && mix test --include example_app test/example_web/user_auth_test.exs && rg -n "Sigra\\.Auth\\.create_session" lib/example/accounts.ex priv/templates/sigra.install/auth.ex 2>/dev/null \|\| rg -n "Sigra\\.Auth\\.create_session" ../../lib/mix/tasks/../../priv/templates/sigra.install/auth.ex` | ✅ | ⬜ pending |
| 10.1.1-04-01 | 04 | 1 | D-09 | T-10.1.1-04-01 (Spoofing: session controller CSRF) | Wave 0 RED: SessionController GET+POST /users/log_in ConnTest must fail before LoginLive removal | conntest (RED gate) | `cd test/example && mix test --include example_app test/example_web/controllers/session_controller_test.exs 2>&1 \| tee /tmp/b9-red.log; grep -E "failures\|0 failures" /tmp/b9-red.log` | ✅ | ⬜ pending |
| 10.1.1-04-02 | 04 | 1 | D-09 | T-10.1.1-04-02 (Tampering: CSRF token on login form) | GREEN: LoginLive deleted, SessionController+HTML in place, router updated, templates synced | compile + conntest + file-absence + router-grep | `cd test/example && mix compile --warnings-as-errors && mix test --include example_app test/example_web/controllers/session_controller_test.exs && test ! -f lib/example_web/live/login_live.ex && rg -n "live \"/log_in\"" lib/example_web/router.ex; test $? -eq 1` | ✅ | ⬜ pending |
| 10.1.1-05-01 | 05 | 2 | D-09 (audit) | T-10.1.1-05-01 (Repudiation: audit row on login) | Wave 0 RED: audit_integration_test.exs must fail before uuid flip + audit wiring land | conntest (RED gate) | `cd test/example && mix test --include example_app test/example_web/audit_integration_test.exs 2>&1 \| tee /tmp/b8-red.log; grep -E "failures\|0 failures" /tmp/b8-red.log` | ✅ | ⬜ pending |
| 10.1.1-05-02 | 05 | 2 | D-09 | T-10.1.1-05-02 (Tampering: uuid PK eliminates actor_id type mismatch) | GREEN: migrations/schemas use uuid; DB drop+recreate clean; full example suite green | ecto-setup + compile + test | `cd test/example && MIX_ENV=test mix ecto.drop && MIX_ENV=test mix ecto.create && MIX_ENV=test mix ecto.migrate && mix compile --warnings-as-errors && mix test --include example_app` | ✅ | ⬜ pending |
| 10.1.1-05-03 | 05 | 2 | D-10, D-11 | T-10.1.1-05-03 (Tampering: installer default propagates uuid PKs) | Installer flips default to `binary_id`; templates + guides synced | compile + installer-test + grep | `mix compile --warnings-as-errors && mix test test/mix/tasks/sigra.install_test.exs && rg "binary_id: true" lib/mix/tasks/sigra.install.ex && rg "Keyword.get\\(opts, :binary_id, true\\)" lib/mix/tasks/sigra.install.ex` | ✅ | ⬜ pending |
| 10.1.1-06-01 | 06 | 3 | D-13, D-15 | T-10.1.1-06-01 (DoS: non-interactive installer prevents CI hang) | `mix sigra.install --yes` flag lands with test coverage | installer-test + grep | `rg -n "yes: :boolean" lib/mix/tasks/sigra.install.ex && mix test test/mix/tasks/sigra.install_test.exs` | ✅ | ⬜ pending |
| 10.1.1-06-02 | 06 | 3 | D-13, D-15 | T-10.1.1-06-02 (Tampering: locally-debuggable smoke drivers) | install-smoke + http-smoke shell drivers syntactically valid and executable | bash -n + executable-bit + grep | `test -x scripts/ci/install-smoke.sh && test -x scripts/ci/http-smoke.sh && bash -n scripts/ci/install-smoke.sh && bash -n scripts/ci/http-smoke.sh && rg -n "mix sigra.install --yes" scripts/ci/install-smoke.sh && rg -n "curl.*%\\{http_code\\}" scripts/ci/http-smoke.sh` | ✅ | ⬜ pending |
| 10.1.1-06-03 | 06 | 3 | D-13, D-15 | T-10.1.1-06-03 (Tampering: SHA-pinned CI actions, IN-03) | install_smoke + example_http_smoke jobs added with SHA-pinned actions | yaml-grep + count | `rg -n "install_smoke:" .github/workflows/ci.yml && rg -n "example_http_smoke:" .github/workflows/ci.yml && rg -c "actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5" .github/workflows/ci.yml \| grep -E "^4$"` | ✅ | ⬜ pending |
| 10.1.1-07-01 | 07 | 3 | D-13, D-14 | T-10.1.1-07-01 (Information Disclosure: TOTP secret DOM hook for deterministic test) | `data-testid="mfa-totp-secret"` added to example app + installer template; compile clean | grep + compile | `rg -n 'data-testid="mfa-totp-secret"' test/example/lib/example_web/live/mfa_settings_live.ex && rg -n 'data-testid="mfa-totp-secret"' priv/templates/sigra.install/mfa_settings_live.ex && cd test/example && mix compile --warnings-as-errors` | ✅ | ⬜ pending |
| 10.1.1-07-02 | 07 | 3 | D-13, D-14 | T-10.1.1-07-02 (Tampering: Playwright golden-path harness) | Playwright scaffolding + golden-path spec + mailbox fixture exist; selectors use frameLocator + testid | file-exists + grep | `test -f test/example/priv/playwright/package.json && test -f test/example/priv/playwright/playwright.config.ts && test -f test/example/priv/playwright/tests/golden-path.spec.ts && test -f test/example/priv/playwright/fixtures/mailbox.ts && rg -n "frameLocator\|data-testid=\"mfa-totp-secret\"" test/example/priv/playwright/` | ✅ | ⬜ pending |
| 10.1.1-07-03 | 07 | 3 | D-13, D-14, D-15 | T-10.1.1-07-03 (Tampering: SHA-pinned Playwright CI job, no continue-on-error) | example_playwright_smoke job added; no floating refs; no continue-on-error | yaml-grep + negative-assertions | `rg -n "example_playwright_smoke:" .github/workflows/ci.yml && rg -n "npx playwright test" .github/workflows/ci.yml && rg -n "continue-on-error" .github/workflows/ci.yml; test $? -eq 1` | ✅ | ⬜ pending |
| 10.1.1-08-01 | 08 | 4 | D-13 | T-10.1.1-08-01 (Repudiation: CI job rename preserves required-check audit trail) | Workflow job renamed `example_app_smoke` → `example_unit_smoke` | yaml-grep | `rg -n "example_app_smoke" .github/workflows/ci.yml; test $? -eq 1 && rg -n "example_unit_smoke:" .github/workflows/ci.yml` | ✅ | ⬜ pending |
| 10.1.1-08-02 | 08 | 4 | D-13 | T-10.1.1-08-02 (Information Disclosure: runbook documents required checks) | RUNBOOK.md documents all five required checks + branch protection update | docs-grep | `rg -n "example_unit_smoke" scripts/uat/RUNBOOK.md && rg -n "example_playwright_smoke" scripts/uat/RUNBOOK.md && rg -n "install_smoke" scripts/uat/RUNBOOK.md && rg -in "branch protection" scripts/uat/RUNBOOK.md` | ✅ | ⬜ pending |
| 10.1.1-08-03 | 08 | 4 | D-13 | T-10.1.1-08-03 (Elevation of Privilege: human checkpoint prevents silent required-check drop) | Blocking human-verify checkpoint — maintainer confirms GitHub branch protection updated | manual checkpoint (no automated verify) | `echo "MANUAL_CHECKPOINT: awaits human confirmation via resume signal; no automated verify"` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `test/example/priv/playwright/package.json` — Playwright Test + TOTP lib dep (scheduled in Plan 07 Task 2)
- [x] `test/example/priv/playwright/playwright.config.ts` — config pointing at local example app boot (scheduled in Plan 07 Task 2)
- [x] `scripts/ci/install_smoke.sh` — driver for fresh mix phx.new + mix sigra.install run (scheduled in Plan 06 Task 2, path: `scripts/ci/install-smoke.sh`)
- [x] `scripts/ci/http_smoke.sh` — driver for curl-based route assertions (scheduled in Plan 06 Task 2, path: `scripts/ci/http-smoke.sh`)
- [x] Plan 03 Task 1 Wave 0 RED test — `test/example/test/example_web/user_auth_test.exs` (B6 ConnTest pinning)
- [x] Plan 04 Task 1 Wave 0 RED test — `test/example/test/example_web/controllers/session_controller_test.exs` (B9 structural lock)
- [x] Plan 05 Task 1 Wave 0 RED test — `test/example/test/example_web/audit_integration_test.exs` (B8 audit row assertion)

*Wave 0 items are all scheduled within the plan set; no additional scaffolding required outside the plans.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GitHub branch protection update | D-13 | GitHub repository settings cannot be set via workflow YAML; maintainer must update the required-checks list in repo settings | See Plan 08 Task 3 — blocking human-verify checkpoint; maintainer confirms all five job names (`library_tests`, `example_unit_smoke`, `install_smoke`, `example_http_smoke`, `example_playwright_smoke`) are listed as required status checks on `main` |

*All phase behaviors except the branch-protection GUI update have automated verification through the five CI jobs.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
