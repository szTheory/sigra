---
phase: 10.1.1
slug: example-app-repair-ci-install-usage-smoke-harness
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-10
---

# Phase 10.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Note: see RESEARCH.md "Validation Architecture" section for the full mapping of D-13/D-14/D-15 CI jobs to B1–B9 failure modes. This file is the execution-time sampling contract; the planner will fill the per-task rows.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (library + example), bash+curl (http smoke), Playwright Test (browser smoke), mix phx.new + mix sigra.install (install smoke) |
| **Config file** | `test/example/mix.exs`, `.github/workflows/ci.yml`, `test/example/priv/playwright/playwright.config.ts` (to be created) |
| **Quick run command** | `mix test --include example_app` |
| **Full suite command** | `mix test --include example_app && (cd test/example/priv/playwright && npx playwright test)` |
| **Estimated runtime** | ~60s quick / ~180s full (local) |

---

## Sampling Rate

- **After every task commit:** Run `mix compile --warnings-as-errors` and targeted `mix test` for touched modules
- **After every plan wave:** Run `mix test --include example_app`
- **Before `/gsd-verify-work`:** Full suite must be green locally; four CI jobs green on PR
- **Max feedback latency:** 60 seconds for unit/compile feedback

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {filled by planner} | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/example/priv/playwright/package.json` — Playwright Test + TOTP lib dep
- [ ] `test/example/priv/playwright/playwright.config.ts` — config pointing at local example app boot
- [ ] `scripts/ci/install_smoke.sh` — driver for fresh mix phx.new + mix sigra.install run (if extracted from workflow)
- [ ] `scripts/ci/http_smoke.sh` — driver for curl-based route assertions (if extracted from workflow)

*These may collapse into inline workflow steps at planner discretion; Wave 0 only creates what's needed to unblock later tasks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| None expected | — | The whole point of this phase is to eliminate manual UAT regressions for B1–B9 | Playwright golden path covers full lifecycle incl. MFA enrollment |

*All phase behaviors have automated verification through the four CI jobs.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
