---
phase: 235-terminal-ratification-measured-not-read
plan: 17
type: execute
wave: 11
depends_on: [235-16]
files_modified:
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.attestation.jsonl
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT-TRUSTED-ROOT.jsonl
  - scripts/ci/verify-fast-01-source-complete-attestation-offline.sh
  - test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs
autonomous: false
gap_closure: true
requirements: [FAST-01, GATE-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A reversible automated preflight retains and validates protected-main identity/blob equality, readiness, REST budget/reset facts, and the bounded pre-dispatch projection before authorization is requested."
    - "Exactly one newly authorized protected-main measurement is dispatched immediately after the decision checkpoint; no intervening external read or mutation can stale the presented facts, and an undesirable p50 never causes a rerun."
    - "Per D-01 through D-03, the authenticated scripts/ci/ci-run-metrics.sh wall-mode receipt is the terminal population/statistic/verdict authority, and offline source-first replay independently agrees with its membership, durations, order, median, and strict verdict."
    - "The evidence handoff qualifies for the Plan 18 FAST-01 pass branch only when the authenticated metrics-script result has at least 10 rows and p50 is below 720 seconds and the independent source oracle agrees; equality or a higher value remains an exact measured gap."
    - "Per D-04 through D-06, GATE-05 stays Complete and byte-stable while Plan 17 produces the authenticated evidence handoff consumed by Plan 18; no requirement or closeout surface changes in this plan."
  artifacts:
    - path: ".planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json"
      provides: "Durable preflight receipt presented at authorization, then finalized with the bounded post-dispatch singleton correlation before the sole watcher"
    - path: ".planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.json"
      provides: "New protected source-complete FAST-01 subject with authoritative metrics-script receipt and signed median/maximum job-step pages on a miss"
    - path: ".planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.attestation.jsonl"
      provides: "Exact-subject attestation bundle"
    - path: ".planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT-TRUSTED-ROOT.jsonl"
      provides: "Contemporaneous trusted root for offline verification"
    - path: "scripts/ci/verify-fast-01-source-complete-attestation-offline.sh"
      provides: "Pinned offline provenance and source replay verifier"
  key_links:
    - from: "Plan 16 protected-main merge"
      to: "new workflow dispatch"
      via: "retained preflight-stage ancestry/blob, readiness, rate-budget, workflow-ID, protected-SHA, UTC-boundary, and bounded-projection facts presented by the immediately following decision checkpoint"
    - from: "bounded pre/post workflow-run projections"
      to: "sole gh run watcher"
      via: "validated durable correlation receipt binds UTC boundary, workflow ID, protected SHA, selected singleton run ID/URL, and cardinality"
    - from: "new attestation bundle"
      to: "new subject source_collection"
      via: "network-denied repository/signer/ref/digest verification"
    - from: "source_collection.pages"
      to: "235-17-SUMMARY.md evidence handoff"
      via: "independent membership/duration/order/p50 recomputation records the authenticated pass-or-miss result for dependent Plan 18"
  prohibitions:
    - "MUST NOT dispatch before readiness, rerun for a favorable p50, omit non-success conclusions, move the remediation cutoff, weaken the strict threshold, combine populations, or overwrite either historical miss."
    - "MUST NOT couple FAST-01 reconciliation to any downgrade, replacement, or reopening of the protected ownership proof."
---

<objective>
Capture one new source-complete protected population and verify it offline from raw signed facts without disturbing GATE-05.

Purpose: Produce the independently replayable evidence handoff that a dependent closeout plan can reconcile without coupling mutable records to the irreversible dispatch.
Output: New retained subject/bundle/root, durable dispatch correlation, pinned offline verifier, adverse contract, and an exact authenticated pass-or-miss handoff.

Decision coverage: D-01: fixed bounded protected population; D-02: queue-inclusive source-first median semantics; D-03: exact honest pass-or-miss evidence; D-04: protected ownership-artifact preservation; D-05: inventory/receipt non-regression; D-06: contributor-topology preservation; D-07: evidence-limited handoff to closeout; D-08: fixed scope with no unrelated work.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-15-EXECUTION-DIAGNOSTICS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-16-SUMMARY.md
@scripts/ci/capture-fast-01-gap-closure.sh
@.github/workflows/fast-01-gap-closure-evidence.yml
@scripts/ci/verify-fast-01-source-complete-attestation-offline.sh
@test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Retain a reversible dispatch preflight for immediate authorization (D-01, D-02, D-03)</name>
  <precondition>Plan 16 completed, `235-16-SUMMARY.md` records the protected-main merge SHA, and no source-complete evidence dispatch has occurred for this plan.</precondition>
  <reversibility rating="reversible">This task performs read-only GitHub operations and writes only a replaceable local preflight receipt and its deterministic contract; it creates no workflow run or attestation.</reversibility>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json, test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</files>
  <read_first>
    - `235-16-SUMMARY.md` — exact protected-main merge SHA and tested blob identities required before dispatch.
    - `AGENTS.md` GitHub API rules — one watcher, 60-second interval, one summary, failure-only logs, rate-limit hard stop.
    - `capture-fast-01-gap-closure.sh`, `fast-01-gap-closure-evidence.yml`, and source-complete contracts — exact readiness, workflow identity, selection, schema, and offline replay path.
    - `235-VERIFICATION.md` and `235-15-EXECUTION-DIAGNOSTICS.md` — CR-02 gap and the immutable rejected candidate that cannot be resumed.
    - Existing historical FAST subjects and the GATE-05 protected receipt/verifier — overlap rejection and non-regression inputs.
  </read_first>
  <behavior>
    - "Protected-main ancestry and all seven Plan 16 blob identities match the merge recorded in 235-16-SUMMARY.md."
    - "Readiness is retained from the authoritative scripts/ci/ci-run-metrics.sh wall-mode output and contains at least 10 eligible terminal rows without becoming verdict authority."
    - "A single REST budget response retains observed_at, core remaining, reset time, and any retry time; remaining must be above 250 and HTTP 403/429 blocks authorization until the reported time."
    - "The retained bounded pre-dispatch projection contains the numeric workflow ID, protected main SHA, UTC dispatch_not_before boundary, exact query/limit, and every projected run identity needed for the later set difference."
    - "The preflight receipt has status ready_for_authorization and no selected run, post-dispatch projection, watcher, subject, or attestation fields."
    - "Missing, malformed, stale, contradictory, undersized, low-budget, or rate-limited preflight facts fail with named diagnostics and cannot reach the checkpoint."
  </behavior>
  <action>
    Perform the complete reversible preflight before asking for authorization. Fetch `origin/main`, prove the Plan 16 merge SHA is its ancestor, and compare the authoritative metrics script/test, collector, workflow, verifier, and source-contract blobs on protected main to that merge. Run one fresh readiness capture and retain the authoritative `scripts/ci/ci-run-metrics.sh` wall-mode receipt with at least 10 eligible terminal PR rows; treat readiness as a dispatch predicate only, never as the terminal verdict. Query `gh api rate_limit` exactly once and retain the observation time, core remaining, reset time, and reported retry time. If remaining is 250 or less or GitHub returns HTTP 403/429, stop before the checkpoint until the reported reset/retry time; retry one other transient read-only failure at most once.

    Resolve the numeric workflow ID and protected-main SHA, choose the bounded projection query and limit, retain exactly one pre-dispatch projection with every returned run's ID, URL, event, ref, head SHA, status, conclusion, and created time, then record a UTC `dispatch_not_before` boundary after that projection. Atomically write the correlation path as schema `sigra.fast-01-dispatch-correlation/1` with `status: ready_for_authorization`, protected-main ancestry/blob receipts, readiness receipt/count, rate-limit receipt, numeric workflow ID, protected SHA, projection query/limit, complete `pre_dispatch` array, and UTC boundary. Do not include a post-dispatch array, selected run, or candidate count at this stage.

    Extend the ExUnit contract first with failing preflight fixtures, then validate exact keys, canonical public URLs, protected SHA/blob equality, metrics-script authority, minimum count, rate-budget threshold, reset/retry handling, workflow identity, bounded query/limit, projection row uniqueness and shape, UTC ordering, and the absence of post-dispatch evidence. Add named mutations for every behavior above. Perform no workflow dispatch, watcher, evidence download, attestation request, requirement edit, or GATE-05 mutation in this task.
  </action>
  <acceptance_criteria>
    - The preflight receipt deterministically proves protected-main identity/blob equality, authoritative readiness of at least 10, acceptable REST budget, numeric workflow identity, protected SHA, complete bounded pre-dispatch projection, and UTC boundary.
    - The focused contract rejects stale/malformed identity, blob mismatch, alternate readiness authority, fewer than 10 rows, core remaining at or below 250, 403/429 before reset, malformed projection rows, duplicate IDs, and any premature post-dispatch field.
    - The receipt is ready to present verbatim at the immediately following checkpoint and no immutable GitHub workflow run or attestation has been created.
    - Historical FAST evidence and completed GATE-05 artifacts remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.schema_version == "sigra.fast-01-dispatch-correlation/1" and .status == "ready_for_authorization" and .readiness.eligible_pr_run_count &gt;= 10 and .rate_limit.core_remaining &gt; 250 and (.workflow_id | type == "number") and (.protected_sha | test("^[0-9a-f]{40}$")) and (.pre_dispatch | type == "array") and (.dispatch_not_before | type == "string") and (has("post_dispatch") | not) and (has("selected") | not) and (has("candidate_count") | not)' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</automated>
    <fails_when>non-zero exit; jq emits false/null; ExUnit reports 0 tests or failures; any protected-main/blob/readiness/rate/projection predicate is absent or contradictory; or the receipt contains post-dispatch selection fields</fails_when>
  </verify>
  <done>A complete, locally validated, reversible preflight is retained for the decision maker, and no dispatch has occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize exactly one immutable protected-main measurement dispatch</name>
  <decision>Review the exact Task 1 preflight receipt and authorize exactly one dispatch of `.github/workflows/fast-01-gap-closure-evidence.yml`, or stop with FAST-01 open.</decision>
  <context>Present the retained protected-main merge/blob identities, readiness count and metrics receipt, REST remaining/reset/retry facts, workflow ID, protected SHA, bounded pre-dispatch projection, and UTC boundary from `235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json`. The workflow run and attestation are permanent external records. No external query, refreshed readiness check, projection, fetch, or other mutation may occur between this decision and Task 3's dispatch; the prior run 34272746647 cannot be resumed, replaced, or treated as authority.</context>
  <options>
    <option id="authorize-once">
      <name>Authorize one dispatch</name>
      <pros>Allows the source-complete measurement to proceed from the presented protected producer and readiness facts.</pros>
      <cons>Creates an immutable GitHub workflow run and attestation even if the measured result misses.</cons>
    </option>
    <option id="stop-with-gap">
      <name>Stop with FAST-01 open</name>
      <pros>Creates no new immutable external record.</pros>
      <cons>FAST-01 remains Gaps Found because the existing signed candidate lacks source-complete proof.</cons>
    </option>
  </options>
  <resume-signal>Select `authorize-once` to permit exactly one immediate dispatch from the retained preflight, or `stop-with-gap` to halt without dispatch.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dispatch first, finalize correlation, retain signed evidence, and replay offline (D-01, D-02, D-03)</name>
  <precondition>Task 2 returned `authorize-once`, and Task 1's retained preflight receipt passes its local deterministic validation unchanged.</precondition>
  <reversibility rating="one-way">The first external mutation is the authorized workflow dispatch, and its run and attestation are immutable historical events; a second run cannot undo or replace it.</reversibility>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.json, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.attestation.jsonl, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT-TRUSTED-ROOT.jsonl, scripts/ci/verify-fast-01-source-complete-attestation-offline.sh, test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</files>
  <read_first>
    - The unchanged Task 1 preflight receipt and Task 2 `authorize-once` signal — sole dispatch inputs; validate locally without refreshing any external fact.
    - `AGENTS.md` GitHub API rules — one watcher, 60-second interval, one summary, failure-only logs, rate-limit hard stop.
    - `capture-fast-01-gap-closure.sh`, `fast-01-gap-closure-evidence.yml`, and source-complete contracts — exact dispatch, selection, capture, schema, and offline replay path.
    - `235-VERIFICATION.md` and `235-15-EXECUTION-DIAGNOSTICS.md` — CR-02 gap and immutable rejected candidate.
    - Existing historical FAST subjects and GATE-05 protected receipt/verifier — overlap rejection and non-regression inputs.
  </read_first>
  <behavior>
    - "The single workflow dispatch is the first external mutation after authorization; no intervening external query refreshes or stales the retained preflight."
    - "Exactly one new workflow_dispatch run is selected by workflow ID, main ref, protected head SHA, UTC boundary, and bounded pre/post identity set difference."
    - "Before the watcher starts, the durable receipt is finalized with the retained preflight, bounded post projection, selected run ID/URL, and candidate_count=1; malformed, missing, zero, or multiple selection evidence blocks watching."
    - "The retained signed source records the exact scripts/ci/ci-run-metrics.sh wall-mode command/output that authoritatively supplies membership, wall_seconds, canonical order, n, p50, and verdict, plus all raw pages needed for an independent comparison replay."
    - "If the authoritative verdict misses, the signed subject includes exhaustively paginated raw job/step receipts for the instrument-selected median and maximum runs and proves exact run/job/step linkage offline."
    - "Mutating any timestamp, event, conclusion, page identity/count, exhaustion flag, run ID, ordering, p50, signer, ref, digest, endpoint, or cutoff makes offline verification fail."
    - "The prior 772-second, 724-second, and rejected derived-466 candidate remain immutable and are never combined with the new population."
  </behavior>
  <action>
    After Task 2 returns `authorize-once`, validate the unchanged preflight receipt locally without calling GitHub. With no intervening external fetch, ancestry check, blob comparison, readiness capture, rate-limit query, workflow lookup, or run projection, make the single dispatch of the retained numeric workflow ID on main as this task's first external mutation. Then obtain exactly one bounded post-dispatch projection using the same retained query and limit and select exactly one new run matching the retained workflow ID, `workflow_dispatch`, main ref, protected SHA, UTC boundary, and pre/post identity set difference. Do not reuse or resume run 34272746647, and never dispatch again because of a failed run, undesirable p50, selection error, or insufficient evidence.

    Before starting any watcher, atomically finalize the existing correlation receipt with `status: dispatched`, the exact bounded `post_dispatch` array, selected singleton run ID/URL/created_at, and `candidate_count: 1`, preserving every presented preflight fact byte-for-value. Validate exact keys, canonical URLs, set difference, field equality, time boundary, singleton cardinality, and preflight preservation with the ExUnit contract; do not proceed if the receipt is absent or invalid. Read the watcher ID only from this validated durable receipt, then use `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`. Fetch the structured summary exactly once; fetch failed logs exactly once only for failure. Retry one non-rate-limit transient post-dispatch operation once; on HTTP 403/429 stop until the reported reset/retry time without starting another watcher or dispatch.

    Download the exact source-complete subject, attestation bundle, and contemporaneous trusted root to the declared paths. Replace the verifier's fail-closed capture pins with the actual subject digest, trusted-root digest, workflow SHA, cutoff, and endpoint. Under the same network denial, first verify repository/signer/ref/digest provenance and the exact `instrument_receipt` from `scripts/ci/ci-run-metrics.sh`. Independently replay source-page contiguity/exhaustion, closed-window membership, chronology, queue-inclusive durations, all conclusions, stable order, n, p50, and strict verdict only as a comparison oracle and require exact agreement with the instrument output. If the result is a miss, validate both retained median/maximum job-page sequences through explicit empty-page exhaustion, bind their run IDs to the instrument-selected poles, and verify each job and ordered step belongs to the same run with terminal status and chronological timestamps. Extend the ExUnit contract with in-memory provenance, dispatch-correlation, source/instrument disagreement, and job/step linkage mutations for each behavior above. Per D-04 and D-05, run the original GATE verifier and terminal contract without editing their files.
  </action>
  <acceptance_criteria>
    - One and only one newly authorized protected-main evidence run is selected and retained under every repository API rule.
    - The dispatch is the first external mutation after authorization, and no external refresh or projection occurs between the checkpoint and dispatch.
    - The durable correlation receipt preserves the presented preflight, validates before the watcher starts, and proves the bounded pre/post sets, UTC boundary, workflow ID, protected SHA, selected singleton run ID/URL, and cardinality.
    - The subject, bundle, and trusted root verify offline and source-first recomputation exactly matches every derived row/statistic/verdict.
    - Closed-boundary equality, zero-duration rows, stable equal-duration ordering, empty/undersized populations, all page faults, all terminal conclusions, and 719/720/721 comparisons have adverse coverage.
    - On a miss, raw median and maximum job/step pages are signed, exhaustively paginated, linked to instrument-selected run IDs, and rejected for any run/job/step mismatch.
    - Every historical FAST subject remains intact and disjoint; GATE-05 evidence, status, and 93 rows remain byte-exact.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; bash scripts/ci/capture-fast-01-gap-closure.test.sh &amp;&amp; jq -e '.schema_version == "sigra.fast-01-dispatch-correlation/1" and .candidate_count == 1 and (.pre_dispatch | type == "array") and (.post_dispatch | type == "array") and (.workflow_id | type == "number") and (.protected_sha | test("^[0-9a-f]{40}$")) and (.selected.id | type == "number") and (.selected.html_url | test("^https://github.com/szTheory/sigra/actions/runs/[0-9]+$"))' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json &amp;&amp; bash scripts/ci/verify-fast-01-source-complete-attestation-offline.sh &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; jq -e '.schema_version == "sigra.fast-01-source-complete-remeasurement/1" and .authority == "protected_main_attestation" and .source_collection.exhausted == true and .instrument_receipt.mode == "wall" and (.source_collection.requested_pages | length) &gt;= 1 and .source_collection.terminal_page == .source_collection.requested_pages[-1] and .source_collection.pages[-1].returned_count == 0 and .eligible_pr_run_count &gt;= 10 and .status == "measured" and (.verdict == "pass" or (.verdict == "miss" and .binding_poles.median.exhausted == true and .binding_poles.maximum.exhausted == true))' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.json &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</automated>
    <fails_when>non-zero exit; either offline verifier omits its success banner; jq emits false/null; or ExUnit reports 0 tests, failures, a source/derived mismatch, or any GATE-05 non-regression diagnostic</fails_when>
  </verify>
  <done>One new protected subject supplies independently authenticated raw source pages and an offline-recomputed strict FAST-01 verdict, with all prior evidence preserved.</done>
</task>

</tasks>

## Flagged assumptions

- **FAST-01 adjacency:** The signed window is closed on `created_at`; equality with cutoff or endpoint is included, equality of timestamps yields zero seconds, equal durations remain separate and sort by numeric `run_id`, and duplicate identities are rejected.
- **FAST-01 empty/degenerate:** Zero through nine eligible rows cannot reach measured status or update requirements; a population of at least 10 may include non-negative zero-second rows.
- **FAST-01 ordering/stability:** The only accepted order is ascending `{wall_seconds, run_id}`, with p50 at `floor(n/2)`; the numeric run ID makes equal-duration selection deterministic.
- **GATE-05 unclassified:** Manual maintainer review remains recommended and is not auto-dismissed. The existing network-denied verifier, digest pins, exact 93-row contract, and byte-stable requirement records are the deterministic backstop.

## Artifacts this phase produces

- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.json` — retained `sigra.fast-01-source-complete-remeasurement/1` subject with `authority`, `repository`, `workflow`, `event`, `cutoff`, `window`, `source_collection`, `runs`, `eligible_pr_run_count`, `statistics`, `verdict`, and `status`; page/run fields are those declared by Plan 16.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-DISPATCH-CORRELATION.json` — reversible preflight receipt containing protected-main identity/blob, readiness, REST-budget/reset, workflow-ID, protected-SHA, UTC-boundary, and bounded pre-projection facts presented at authorization, then finalized pre-watcher with the bounded post projection, singleton selection, and cardinality.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT.attestation.jsonl` — exact-subject GitHub/Sigstore provenance bundle.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-SOURCE-COMPLETE-REMEASUREMENT-TRUSTED-ROOT.jsonl` — contemporaneous official trust material.
- `scripts/ci/verify-fast-01-source-complete-attestation-offline.sh` — actual `SUBJECT_DIGEST`, `TRUSTED_ROOT_DIGEST`, `EXPECTED_WORKFLOW_SHA`, `EXPECTED_CUTOFF_SHA`, `EXPECTED_CUTOFF`, and `EXPECTED_ENDPOINT` pins plus source-first verification.
- `test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs` — actual-evidence provenance, population, recomputation, status, historical-evidence, and GATE-05 non-regression cases.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data | 235-17 | COVERED | New protected source pages are authenticated and replayed offline. |
| GOAL | — | Maintainer sees what moved and where it landed | 235-17 | COVERED | GATE-05's existing protected 93-row proof remains exact. |
| REQ | FAST-01 | At least 10 post-change PR runs, p50 strictly below 720 | 235-17→18 | COVERED | Plan 17 authenticates the strict result; Plan 18 alone reconciles the requirement and closeout surfaces. |
| REQ | GATE-05 | Single before/after no-drop artifact | 235-17 | COVERED | Original receipt/ledger/verifier/status are non-regression inputs. |
| RESEARCH | — | Wall/all-conclusion/floor-median semantics and exact IDs | 235-16–17 | COVERED | The mandated metrics instrument is authoritative; signed source pages support an independent comparison oracle. |
| RESEARCH | — | Diagnose a miss with job data | 235-16–17 | COVERED | Producer signs exhaustive median/maximum job/step pages; verifier checks linkage offline. |
| CONTEXT | D-01–D-03 | Fixed method, exact provenance, honest result | 235-16–17 | COVERED | One protected run, authoritative metrics-script output, independent comparison replay, complete miss poles, strict branch. |
| CONTEXT | D-04–D-05 | Complete before/after ownership proof | 235-17 | COVERED | GATE-05 artifacts remain byte-exact and verified. |
| CONTEXT | D-06 | Truthful contributor topology | 235-17 | COVERED | CONTRIBUTING remains unchanged and its terminal contract stays green. |
| CONTEXT | D-07 | Close only to supported extent | 235-18 | COVERED | Dependent reconciliation closes only on an exact pass; an exact miss remains owned and linked. |
| CONTEXT | D-08 | No unrelated work | 235-17→18 | COVERED | Plan 17 is evidence-only and Plan 18 is limited to the five named closeout files. |
| VERIFICATION | CR-02 | Authenticated timestamps and exhaustive pages before p50 | 235-17 | COVERED | Subject/bundle/root plus source-first offline verifier close the gap. |
| PROBE | FAST adjacency/empty/order | Explicit equality, undersized, stable-order semantics | 235-17 | COVERED | Signed semantics and adverse tests are required. |
| PROBE | GATE-05 unclassified | Review manually | 235-17 | COVERED | Flag retained; automated backstop protects the completed proof. |

No authoritative source item is missing. Deferred ideas and unrelated remediation remain excluded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Protected main → dispatch | Only the corrected protected producer may authorize the immutable external run. |
| GitHub run/API/attestation → repository evidence | External mutable state and cryptographic provenance become durable local facts. |
| Signed source → Plan 18 handoff | Only independently replayed source data may become reconciliation authority. |
| Evidence handoff → GATE-05 | The open performance requirement remains isolated from the completed ownership proof before closeout begins. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-17-01 | Spoofing | dispatch/run identity | high | mitigate | Validate and retain protected-main identity, readiness, rate budget, workflow ID, protected SHA, not-before boundary, and pre-projection before authorization; make dispatch the next external mutation; then bind the exact post difference before one watcher. |
| T-235-17-02 | Tampering | subject/bundle/root | high | mitigate | Pin and verify repository, signer, ref, workflow SHA, subject digest, trusted-root digest, cutoff, and endpoint under network denial. |
| T-235-17-03 | Tampering | source population | high | mitigate | Rebuild contiguous pages through the empty terminal page and reject count, identity, chronology, event, conclusion, bound, overlap, and source/derived mutations. |
| T-235-17-04 | Repudiation | FAST evidence handoff | high | mitigate | Record independently recomputed n/p50/verdict and exact producer evidence in the Plan 17 summary consumed by dependent Plan 18. |
| T-235-17-05 | Tampering | GATE-05 proof | high | mitigate | Run the original offline verifier and exact 93-row contract and reject any requirement, digest, ledger, or contributor-topology change. |
| T-235-17-06 | Denial of service | GitHub operations | medium | mitigate | One rate preflight, bounded projections/pages, one 60-second watcher, one summary/failure-log read, one transient retry, and hard stop on 403/429 or core at/below 250. |
| T-235-17-07 | Information disclosure | retained files | low | accept | Commit public run metadata and cryptographic proof only; exclude tokens, headers, cookies, proxies, authenticated state, and raw logs. |
</threat_model>

<verification>
Prove protected-main producer ancestry, capture exactly one new source-complete population, verify and recompute it offline, exercise all adverse semantics, run all FAST/GATE planning contracts, and emit the authenticated evidence handoff without editing closeout records.
</verification>

<success_criteria>
- One new protected subject contains at least 10 eligible rows, authenticates the mandated metrics-script wall result, and independently confirms complete pagination, membership, chronology, durations, ordering, p50, and verdict as a comparison.
- Authorization follows a deterministic retained preflight, and the single dispatch is the first external mutation after that checkpoint.
- The handoff records whether the recomputed p50 is below 720 seconds; equality or higher is an exact measured miss with current poles and no rerun, and no status changes occur until Plan 18.
- Every historical FAST result remains intact.
- GATE-05 remains checked, Complete, byte-stable, and independently verified.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-17-SUMMARY.md` when done.
</output>
