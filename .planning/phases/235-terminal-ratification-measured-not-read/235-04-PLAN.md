---
phase: 235-terminal-ratification-measured-not-read
plan: 04
type: execute
wave: 4
depends_on: [235-03]
files_modified:
  - .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
  - CONTRIBUTING.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "D-01/D-02/D-03: Every terminal event's retained run population deterministically reproduces its count, outcomes, queue-inclusive wall durations, mean, sorted floor(n/2) p50, maximum, canonical metrics bytes, and SHA-256 receipt before the strict FAST-01 verdict is accepted."
    - "FAST-01 remains a strict miss at exactly 720 seconds and whenever fewer than 10 eligible PR runs exist; the committed 19-run population continues to produce the honest 772-second miss."
    - "D-04/D-05: The actual ownership {family, spec, event} keys equal the declared Phase 234 inventory plus terminal non-Playwright universe exactly."
    - "D-06: Contributor topology statements are mechanically related to the live workflow's direct-owner, aggregate-needs, command, and non-PR condition blocks, including resistance to contradictory prose."
    - "D-07/D-08: The reconciled miss closeout and its single residual remain ledger-backed while unrelated todos and remediation stay outside this corrective plan."
  artifacts:
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      provides: "retained canonical wall-mode output bytes bound to each event population"
    - path: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      provides: "adversarial evidence-integrity, exact-universe, and semantic-topology contract"
    - path: CONTRIBUTING.md
      provides: "unique whole CI-topology statements consumed by the semantic contract"
  key_links:
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      to: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      via: "retained runs -> recomputed statistics -> canonical output receipt bytes -> SHA-256 -> verdict and closeout"
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      via: "exact cross-product of inventory specs/non-Playwright families with pull_request, push, and schedule"
    - from: .github/workflows/ci.yml
      to: CONTRIBUTING.md
      via: "job-block extraction proves shard execution, aggregate dependencies, and diagnostic event guards before accepting unique role statements"
---

<objective>
Close the three Phase 235 verification gaps by making the terminal ratification contract derive its claims from retained evidence and reject semantic contradictions.

Purpose: FAST-01 and GATE-05 must be ratifiable from immutable run and ownership data rather than trusted ledger fields or unrelated text tokens, per D-01 through D-08.
Output: receipt-bound metric recomputation, exact ownership-universe equality, and contributor-topology contradiction resistance.
</objective>

<execution_context>
@/Users/developer/.codex/gsd-core/workflows/execute-plan.md
@/Users/developer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-REVIEW.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-03-SUMMARY.md
</context>

<flagged_assumptions>
- **A-01 — FAST-01 adjacency:** D-02 and the requirement text resolve the boundary as strict `p50_seconds < 720`; 720 seconds is a miss.
- **A-02 — FAST-01 empty/small input:** nil and empty run populations are invalid evidence; a single valid run can be recomputed but remains a miss because at least 10 eligible PR runs are mandatory.
- **A-03 — FAST-01 ordering:** p50 is the duration at zero-based `floor(n/2)` after ascending numeric sort; equal durations remain deterministic because only their equal numeric value is selected.
- **A-04 — GATE-05 unclassified universe:** the reviewed terminal universe is exactly 20 hash-pinned Phase 234 Playwright specs plus `required_non_playwright_families/0`, crossed with `pull_request`, `push`, and `schedule`. A new family, spec, or event requires an explicit contract/inventory change rather than silent admission.
- **Descriptor-less prohibition recall:** not applicable; this evidence schema uses explicit family/spec/event keys and introduces no descriptor-derived allowlist or prohibition.
</flagged_assumptions>

## Artifacts this phase produces

- `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — adds `measurements.{pull_request,push,schedule}.output_receipt`, the verbatim canonical JSON stdout bytes already represented by each stored SHA-256.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — adds `recompute_statistics!/2`, `sha256_hex/1`, `expected_ownership_keys!/0`, `workflow_job_block!/2`, `ci_overview!/1`, and `require_unique_statement!/3` (or equivalently named private helpers with the same contracts), plus the adversarial mutations named below.
- `CONTRIBUTING.md` — preserves one unique whole statement for each direct-owner/aggregate relation and each non-PR signal condition so the contract validates propositions rather than token presence.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace retained PR runs through canonical receipt bytes to the strict FAST-01 verdict</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` and `235-REVIEW.md` — CR-01 and the authoritative forged p50/count/hash/outcome gap.
    - Both files listed in `<files>` — current 19/2/2 retained populations, stored statistics/hashes, strict verdict helpers, and closeout links.
    - `scripts/ci/ci-run-metrics.sh` — D-01/D-02 wall duration, conclusion retention, JSON key shape, and sorted floor(n/2) semantics.
    - `scripts/ci/ci-run-metrics.test.sh` — hermetic instrument contract that must remain green.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-01, D-02, D-03, D-07, and D-08.
  </read_first>
  <behavior>
    - "For each event, ISO-8601 updated_at minus created_at is clamped at zero; all conclusions contribute to n/durations and only success contributes to pass."
    - "The computed statistics map exactly equals the stored statistics and the single-object canonical output receipt; SHA-256 is lowercase 64-hex and equals the digest of the receipt bytes."
    - "eligible_pr_run_count, observed_p50_seconds, same_window_measurements, verdict status, and closeout performance boolean derive from recomputed populations."
    - "Forged p50, eligible count, output digest, receipt bytes, and outcome totals each fail while unchanged evidence passes."
    - "Nil and empty populations fail closed; a single-element population recomputes deterministically but cannot pass the minimum-count gate; equal-duration median selection is stable."
    - "719 seconds with 10 eligible runs passes, while 720 and 721 seconds miss."
  </behavior>
  <action>
    Start RED by adding focused adversarial tests for the unchanged PR happy path and every mutation in `<behavior>`; prove the current validator accepts the forged cases before implementing the fix. Then, per D-01/D-02/D-03, add `output_receipt` to each captured event as the exact pretty-printed JSON stdout (including its trailing newline) whose bytes already produce the committed `output_sha256`; reconstruct these bytes from the committed statistics without querying GitHub or changing the immutable window.

    Implement `recompute_statistics!/2` from the retained `runs`: parse timestamps with `DateTime.from_iso8601/1`, calculate queue-inclusive seconds with `DateTime.diff/3`, clamp negative values to zero, sort numeric durations, select index `div(n, 2)`, and compute `n`, `mean_seconds`, `p50_seconds`, `max_seconds`, `pass`, and `fail` with the exact script key shape. Reject nil, empty, malformed timestamps, missing terminal conclusions, event mismatches, and non-list inputs with specific diagnostics. Compare the computed map to both `measurement["statistics"]` and the decoded one-element `output_receipt`; require a lowercase 64-hex digest and compare it to `:crypto.hash(:sha256, receipt_bytes)`.

    Feed the recomputed PR map into strict verdict validation: `eligible_pr_run_count == n`, observed p50 and same-window fields equal recomputed fields, at least 10 PR runs are required for pass, and the comparator remains strict at 720. Keep the measured 19-run/772-second miss, D-07 closeout, and single residual unchanged. Per D-08, do not capture new runs, adjust CI timing, or touch unrelated todos.
  </action>
  <acceptance_criteria>
    - The unchanged ledger recomputes 19 PR runs, p50 772, mean 821.1052631578947, max 1447, 13 pass, and 6 fail, and retains status `miss`.
    - Each event receipt decodes to exactly one recomputed statistics object and its SHA-256 matches the literal retained bytes.
    - Separate mutations of stored p50, eligible count, output hash, output receipt, conclusion/outcome totals, nil/empty runs, and the 720-second boundary are rejected deterministically.
    - No GitHub API call, workflow edit, new measurement window, or closeout reinterpretation occurs.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh</automated>
  </verify>
  <done>The committed run population is the sole source of metric, receipt, verdict, and closeout truth, and forged FAST-01 evidence fails the focused contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the GATE-05 ownership universe under exact key equality</name>
  <files>test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — current `validate_rows!/1`, `row_key/1`, and `required_non_playwright_families/0`.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — current 93 sorted ownership rows.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — D-05 hash-pinned set of 20 current Playwright specs.
    - `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — fail-closed current-spec reconciliation precedent.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-04/D-05 exact before/after scope.
  </read_first>
  <behavior>
    - "Expected ownership keys are the MapSet cross-product of every inventory Playwright spec and every required non-Playwright family with exactly pull_request, push, and schedule."
    - "The current 93 unique sorted rows equal that expected key set."
    - "An otherwise valid extra family row fails exact equality."
    - "An otherwise valid workflow_dispatch event row fails exact equality and event validation."
    - "Missing, duplicate, stale-spec, receiverless, receiptless, and aggregate-only mutations continue to fail."
  </behavior>
  <action>
    Start RED with two mutations that preserve all existing required rows and valid-looking owner/receiver/receipt data: append a fourth event copied from an existing row, and append a row for an unclassified family. Confirm the current presence-only loop accepts them. Then implement `expected_ownership_keys!/0` from the hash-validated Phase 234 inventory and `required_non_playwright_families/0`, crossing each identifier with `@events` per D-04/D-05 and flagged assumption A-04.

    In `validate_rows!/1`, compare `MapSet.new(Enum.map(rows, &amp;row_key/1))` to the expected set and report missing and unexpected keys separately. Retain the existing sorted-order, duplicate, direct-owner, receiver, receipt, inventory SHA, and executable-owner checks. Keep event acceptance explicit to `@events` so a novel event cannot enter the complete terminal claim merely by carrying plausible metadata.
  </action>
  <acceptance_criteria>
    - The exact expected and actual ownership key sets contain 93 keys and are equal for the committed ledger.
    - Extra-family and extra-event mutations fail with diagnostics that identify the unexpected key.
    - Existing negative mutations for omissions, duplicates, stale specs, and invalid ownership remain green as tests.
    - The Phase 234 inventory remains the single Playwright source per D-05; no competing scan or copied spec list is introduced.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs</automated>
  </verify>
  <done>GATE-05 accepts exactly the reviewed family/spec/event universe and rejects silent extension as well as omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind contributor topology propositions to workflow job semantics</name>
  <files>CONTRIBUTING.md, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - Both files listed in `<files>` — current CI overview bullets and substring-based `validate_contributor_topology!/5`.
    - `.github/workflows/ci.yml` — `library_tests_shard`, `library_tests`, `example_playwright_shard`, `example_playwright_smoke`, `admin_eval_render`, and `admin_design_recapture` job blocks.
    - `mix.exs`, `test/example/priv/playwright/playwright.config.ts`, and `test/example/priv/playwright/package.json` — existing local and browser command seams.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` and `235-REVIEW.md` — semantic contradiction failure and required mutations.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-06 contributor contract and D-07/D-08 boundaries.
  </read_first>
  <behavior>
    - "The library direct-owner job contains MIX_ENV=test mix ci; its terminal aggregate needs that job and contains no library test execution command."
    - "The Playwright shard contains browser test commands; its terminal aggregate needs the shard and contains no browser execution command."
    - "admin_eval_render and admin_design_recapture each carry the exact non-pull-request job condition inside their own workflow blocks."
    - "The CI overview contains one unique whole role statement for each direct-owner/aggregate pair and one unique whole statement for each diagnostic event boundary."
    - "Appending an aggregate-as-executor claim or a diagnostic-as-PR-executor claim fails even when every formerly required token and sentence remains present."
  </behavior>
  <action>
    Start RED with contradiction mutations that preserve every existing required token: append within the CI overview that a terminal aggregate executes its underlying suites, and separately that a diagnostic job executes on pull requests. Confirm the current independent substring checks accept both.

    Implement indentation-scoped `workflow_job_block!/2` extraction for the six named workflow jobs and section-scoped `ci_overview!/1` extraction from `CONTRIBUTING.md`. Relate the live blocks before accepting D-06 prose: each direct owner contains its real execution command, each terminal aggregate names its direct owner in `needs`, aggregate blocks do not contain the corresponding execution command, and both diagnostic blocks contain the exact `github.event_name != 'pull_request'` condition. Avoid a whole-file token search for these relations because identical terms in unrelated job blocks do not prove ownership.

    Normalize the four CI overview bullets into unique whole statements and validate each exactly once with `require_unique_statement!/3`. Add contradiction detection within the same section for executor-role and PR-event assertions, using sentence/paragraph semantics rather than isolated job-name presence. Preserve the supported reproduction path and the existing D-07 measured-miss closeout contract; per D-08, do not change workflow topology or fold other documentation work into this task.
  </action>
  <acceptance_criteria>
    - The contract proves direct-owner commands, aggregate `needs` relationships, aggregate non-execution, and exact diagnostic event guards from their own workflow job blocks.
    - CONTRIBUTING contains one mechanically checked whole statement for each role/event proposition.
    - Aggregate-as-executor and non-PR-signal-as-PR-executor mutations fail while retaining all legacy tokens, job names, and canonical statements.
    - The focused contract, Phase 198 contributor contract, and planning-contract directory remain green without manual UAT or sleeps.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/</automated>
  </verify>
  <done>Contributor guidance is accepted only when its complete propositions agree with the live direct-owner, aggregate, and event topology and contain no semantic contradiction.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data; maintainer sees what moved and where it landed | 235-04 | COVERED | Tasks 1–3 bind metrics, ownership, and contributor topology to retained/live sources. |
| REQ | FAST-01 | Strict under-12-minute p50 over at least 10 post-change PR runs | 235-04 | COVERED | Task 1 recomputes the honest 19-run miss and locks adjacency/empty/order semantics. |
| REQ | GATE-05 | Single exact before/after PR/main/nightly ownership artifact | 235-04 | COVERED | Task 2 enforces set equality; Task 3 binds contributor interpretation. |
| RESEARCH | — | Baseline-compatible wall-mode measurement and all-conclusion retention | 235-04 | COVERED | Task 1 derives the script's exact statistics from timestamps. |
| RESEARCH | — | Phase 234 inventory is input, not replacement | 235-04 | COVERED | Task 2 builds the exact universe from its pinned contents. |
| RESEARCH | — | Direct executable owner differs from terminal aggregate | 235-04 | COVERED | Task 3 proves commands and `needs` per job block. |
| CONTEXT | D-01 | Immutable bounded wall-mode window | 235-04 | COVERED | Task 1 retains the existing window and binds its output bytes. |
| CONTEXT | D-02 | Queue-inclusive, all-outcome, sorted floor(n/2) semantics | 235-04 | COVERED | Task 1 recomputation implements each semantic explicitly. |
| CONTEXT | D-03 | Exact commands/run IDs and honest miss | 235-04 | COVERED | Task 1 preserves receipt provenance and strict 772-second miss. |
| CONTEXT | D-04 | Complete before/after ownership artifact | 235-04 | COVERED | Task 2 enforces the full cross-product. |
| CONTEXT | D-05 | Consume pinned Phase 234 inventory and reject silent drift | 235-04 | COVERED | Task 2 derives expected Playwright keys only from that input. |
| CONTEXT | D-06 | Contributor direct-owner/aggregate/reproduction/non-PR truth | 235-04 | COVERED | Task 3 validates whole propositions against job blocks. |
| CONTEXT | D-07 | Honest ledger-backed closeout/residual | 235-04 | COVERED | Task 1 keeps verdict/closeout derived; Task 3 reruns closeout contract. |
| CONTEXT | D-08 | No pending todos folded in | 235-04 | COVERED | All tasks are restricted to the three gap artifacts and current guidance. |

Deferred ideas remain excluded. API detection is negative: the plan changes a JSON evidence artifact, ExUnit contract, and contributor documentation only; it defines no HTTP route, controller, socket, GraphQL resolver, or client API. No package install, schema push, UI, snapshot, CI polling, or external-service setup is introduced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Retained GitHub run metadata → terminal metric verdict | Checked-in timestamps and conclusions are untrusted evidence until recomputed and receipt-bound. |
| Phase 234 inventory / terminal rows → GATE-05 completeness claim | A plausible extra or omitted key can distort the declared ownership universe. |
| GitHub Actions workflow → contributor prose | Names may be present while prose assigns contradictory executor, aggregate, or event roles. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-04-01 | Tampering | FAST-01 statistics, receipt, count, verdict | high | mitigate | Recompute from retained timestamps/outcomes, decode canonical output bytes, verify lowercase SHA-256, and mutation-test p50/count/hash/output/outcomes. |
| T-235-04-02 | Tampering | GATE-05 ownership rows | high | mitigate | Compare exact expected/actual MapSets and mutation-test extra family/event plus existing missing/duplicate cases. |
| T-235-04-03 | Repudiation | contributor topology claim | high | mitigate | Extract scoped workflow job blocks and unique documentation statements; reject aggregate/executor and event contradictions that preserve tokens. |
| T-235-04-04 | Information disclosure | retained metric output | low | accept | Receipt contains only the already-committed aggregate statistics; no tokens, raw logs, headers, cookies, or authenticated payloads are retained. |
| T-235-04-05 | Denial of service | deterministic contract suite | low | mitigate | Tests use checked-in files and bounded local parsing; no network calls, watchers, sleeps, or unbounded inputs. |

## ASVS L1 Automated Controls

- V5 validation: explicit JSON shapes, ISO-8601 parsing, closed event/key sets, digest format/content checks, and fail-closed malformed-input tests.
- V7 logging/error handling: mutation failures identify the mismatched evidence dimension without printing secrets or authenticated data.
- V12 files/resources: every parser reads fixed repository paths and bounded checked-in content only.
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs`, `bash scripts/ci/ci-run-metrics.test.sh`, `MIX_ENV=test mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs`, and `MIX_ENV=test mix test test/sigra/planning/`. All commands must exit 0; the focused suite must include explicit mutations for forged p50/count/hash/outcomes, receipt bytes, extra family/event, and contradictory contributor topology.
</verification>

<success_criteria>
- The retained 19-run PR data independently recomputes the committed 772-second strict miss, and all three event hashes bind exact canonical output bytes.
- FAST-01 edge behavior is explicit and tested: nil/empty fail closed, fewer than 10 cannot pass, sorted floor(n/2) is deterministic, and exactly 720 seconds misses.
- GATE-05 ownership keys are exactly equal to the reviewed 93-key universe.
- Contributor topology contradictions fail even when all names and legacy substrings remain present.
- D-07 closeout and D-08 scope remain unchanged, and all deterministic verification gates pass.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-04-SUMMARY.md` when done.
</output>
