---
phase: 235-terminal-ratification-measured-not-read
plan: 14
type: execute
wave: 14
depends_on: [235-13]
files_modified:
  - scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh
  - scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh
  - test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs
  - scripts/ci/verify-terminal-ratification-attestation-offline.sh
  - scripts/ci/verify-terminal-ratification-attestation-offline.test.sh
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
  - .planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "The executor snapshots the current bytes and content hashes of the user-owned dirty config, ROADMAP, and FAST readiness JSON before any task edit, and proves them unchanged and unstaged after both tasks."
    - "FAST-01's retained n=15, p50=486, strict-pass receipt is accepted only after a verifier establishes trusted staging without inherited PATH, TMPDIR, TMP, or TEMP control."
    - "GATE-05's retained 93-row ledger is accepted only after its verifier establishes the same trusted staging boundary before copying or parsing retained inputs."
    - "A hostile-environment regression for each verifier reaches successful staging and full verification; an earlier unrelated failure cannot satisfy the regression."
    - "The strict FAST-01 comparator remains p50 < 720: equality at 720 is a miss, and no temporary path equal to or component-contained by an attacker-selected root is accepted."
    - "Missing trusted utilities, missing/unset temporary overrides, invalid fixed-parent state, or staging failure stops closed; a valid invocation still verifies the fixed retained inputs."
    - "Trusted staging precedes retained-input copying/parsing and the existing network-denied provenance verification in deterministic order, while network-denied repository/signer/main-ref/subject-digest semantics remain unchanged."
    - "Both verifier entrypoints are executable files pinned to `#!/bin/bash`; each hostile-PATH self-test invokes its verifier directly, reaches the final positive marker, and records zero executions of every sentinel including `bash`."
    - "The executor snapshots the repository-wide pre-execution path/status baseline, then proves the execution-created changed-path delta is exactly the six declared implementation files plus `235-14-SUMMARY.md`, while the three baseline dirty planning paths retain their saved bytes, hashes, and statuses."
  assumptions:
    - "[RESOLVED] The supported Darwin and Ubuntu runners expose absolute env, uname, mkdir, cp, rm, and platform-appropriate mktemp/stat utilities; the verifier must validate each required executable before staging and fail closed when one is unavailable."
    - "[RESOLVED] The platform fixed staging parent is selected by verifier-owned policy, validated as an absolute real directory with trusted ownership and no symlink traversal, and never derived from TMPDIR, TMP, TEMP, PATH, or receipt data."
    - "[RESOLVED] Hostile temp overrides that are unset, empty, equal to the fixed parent, adjacent by string prefix, or point above/below it are ignored; component-aware canonical containment and ownership checks decide trust."
    - "[RESOLVED] The GATE-05 regression proves the staging checkpoint by requiring the hostile-environment invocation to complete the positive retained-subject verification, not merely by observing a shadow binary was unused."
    - "[RESOLVED] `/bin/bash` is the supported pinned interpreter on the Darwin and Ubuntu runners; verifier execution must not consult `/usr/bin/env` or inherited PATH to select Bash."
  prohibitions:
    - "[UNVERIFIED] `.planning/config.json`, `.planning/ROADMAP.md`, and `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json` are read-only user-owned dirty files: do not edit, format, stage, revert, restore, or include them in a task commit."
    - "[UNVERIFIED] No receipt, attestation bundle, trusted root, requirement claim, terminal ledger, CI topology, contributor documentation, schema, package, timeout, retry policy, or UI artifact may change."
    - "[UNVERIFIED] No new external measurement, protected workflow dispatch, network-enabled fallback, sleep, or human UAT may be introduced."
  artifacts:
    - path: "scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh"
      provides: "Fail-closed FAST-01 trusted-staging and network-denied provenance verifier"
    - path: "scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh"
      provides: "Hostile PATH and temp-root regression that must reach FAST staging and verification"
    - path: "test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs"
      provides: "Static and behavioral contract for staging order, threshold equality, and fail-closed FAST inputs"
    - path: "scripts/ci/verify-terminal-ratification-attestation-offline.sh"
      provides: "Fail-closed GATE-05 trusted-staging and network-denied provenance verifier"
    - path: "scripts/ci/verify-terminal-ratification-attestation-offline.test.sh"
      provides: "Checkpoint-reaching GATE-05 hostile-environment regression with no error suppression"
    - path: "test/sigra/planning/phase_235_terminal_ratification_contract_test.exs"
      provides: "Contract for fixed-parent ownership, non-symlink staging, and preserved provenance policy"
    - path: ".planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md"
      provides: "Required execution summary included in the deterministic final changed-path delta"
  key_links:
    - from: "Task 1 preservation manifest and byte snapshots"
      to: ".planning/config.json, .planning/ROADMAP.md, and 235-FAST-01-GAP-CLOSURE-READINESS.json"
      via: "Pre-edit git hashes plus byte-for-byte copies must match after Task 2, and the three paths must remain absent from the staged diff"
      pattern: "git hash-object --no-filters"
    - from: "scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh"
      to: "scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh"
      via: "Hostile PATH/TMPDIR/TMP/TEMP invocation must complete the fixed retained-subject check"
      pattern: "PATH=.*TMPDIR=.*TMP=.*TEMP=.*verify-fast-01-gap-closure-attestation-offline"
    - from: "scripts/ci/verify-terminal-ratification-attestation-offline.test.sh"
      to: "scripts/ci/verify-terminal-ratification-attestation-offline.sh"
      via: "Success-required staging checkpoint regression replaces error-suppressed early exits"
      pattern: "offline_attestation_verified"
    - from: "both offline verifiers"
      to: "retained receipt/bundle/root triples"
      via: "Trusted staging then network-denied exact repository/signer/ref/digest verification"
      pattern: "attestation verify"
    - from: "both verifier shebangs and hostile-PATH self-tests"
      to: "the supported `/bin/bash` interpreter"
      via: "Direct executable invocation selects `/bin/bash` before verifier code runs; an executable hostile-PATH `bash` sentinel remains at zero while the final positive marker is observed"
      pattern: "#!/bin/bash"
    - from: "pre-execution repository path/status baseline"
      to: "the final changed-path allowlist"
      via: "A normalized baseline path set is subtracted from the normalized post-execution path set only after the three baseline dirty files pass byte/hash/status checks"
      pattern: "comm -13"
---

<objective>
Close the two Phase 235 verification blockers by pinning both executable verifier entrypoints to `/bin/bash` and making their post-entry staging safe against hostile pre-isolation PATH and temporary-directory control, with deterministic tests that prove staging and final verification were reached.

Purpose: FAST-01 and GATE-05 already have real retained evidence, but neither requirement can be ratified until its verifier's staging trust boundary is fail-closed.
Output: Two hardened verifiers, two hostile-environment verifier self-tests, focused ExUnit contracts, and a summary covered by an exact changed-path delta; no evidence is recaptured or remeasured.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-13-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove FAST-01 trusted staging end to end under hostile caller state</name>
  <reversibility rating="reversible">The repair is confined to one verifier and its tests; the immutable retained evidence and public interfaces do not change.</reversibility>
  <files>scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh, scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh, test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs</files>
  <read_first>
    - `.planning/config.json`, `.planning/ROADMAP.md`, and `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json` — user-owned dirty files; before any edit or test, snapshot their current bytes, content hashes, and status into the scoped preservation directory described below, then treat all three as read-only. At the same pre-edit point, save repository-wide porcelain-v1 status and a normalized path set; require that baseline path set to equal exactly these three known dirty paths so the final set subtraction cannot hide an execution edit behind a pre-existing status record.
    - `scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh` — modify its pre-isolation utility resolution, staging creation, validation, and cleanup ordering.
    - `scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh` — new file; confirm it is absent, then create the FAST hostile-environment runtime regression.
    - `test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs` — modify the existing strict comparator and verifier contract coverage.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — authoritative FAST blocker and missing proof.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-REMEASUREMENT.json` — fixed n=15/p50=486 subject; read only.
  </read_first>
  <behavior>
    - "RED/preflight: before any production or test edit, a private task-specific preservation directory contains byte-for-byte copies and `git hash-object --no-filters` values for the dirty config, ROADMAP, and FAST readiness JSON; none of the three paths becomes staged, reverted, restored, formatted, or modified."
    - "RED: the executable FAST verifier starts with the exact supported `#!/bin/bash` entrypoint, and its hostile-PATH self-test executes the verifier directly rather than selecting an interpreter through PATH."
    - "RED: script-root derivation executes no inherited-PATH command after the pinned interpreter starts: Bash parameter expansion plus builtin `cd`/`pwd -P` derives ROOT, so a hostile `dirname` cannot redirect PHASE or retained inputs."
    - "RED: a full hostile PATH shadows every verifier utility name reachable before trusted staging or network isolation — `bash`, `dirname`, `uname`, `mktemp`, `realpath`, `readlink`, `stat`, `env`, `gh`, `jq`, `mkdir`, `cp`, `rm`, `sandbox-exec`, `unshare`, `sudo`, and `true`, plus FAST's `dd`, `shasum`, and `awk` — yet no shadow runs and the final positive marker is still required."
    - "RED: PATH shadow binaries for realpath and mktemp plus hostile TMPDIR/TMP/TEMP are never executed or selected, while the invocation reaches trusted staging and completes the positive retained-subject verification."
    - "RED: an unset or empty temp override behaves identically to a hostile one because staging policy does not consume it."
    - "RED: fixed-parent absence, symlink substitution, wrong ownership, non-directory state, utility absence, or mktemp failure exits nonzero before any retained input is copied or parsed."
    - "RED: canonical work paths must be strict children of the fixed parent and must not equal, prefix-alias, or component-overlap an attacker-selected temp root."
    - "RED: p50 values 719, 720, and 721 yield pass, miss, and miss respectively; the existing n=15/p50=486 subject still passes only after provenance verification."
  </behavior>
  <action>
    Before any edit or test, preserve the three known user-owned dirty files named in `read_first`. Select an existing absolute `/usr/bin/mktemp` or `/bin/mktemp`, create a mode-0700 directory from `/tmp/sigra-235-14-preservation.XXXXXX`, and record its absolute path in the task-specific pointer returned by `git rev-parse --git-path gsd-235-14-preservation.path`; fail rather than overwrite a pre-existing pointer. Copy the current bytes of each file into that directory, write a manifest containing each path and its `git hash-object --no-filters` value, and record their current scoped porcelain status. Also save `git status --porcelain=v1 -z --untracked-files=all` for the entire repository as `status.repo.baseline.z`; normalize its paths into `paths.repo.baseline` with a Bash `read -r -d ''` loop, reject rename/copy records and pathnames containing newline or tab, and compare the sorted result to an explicit `paths.user-dirty.expected` containing only the three known dirty paths. Write `paths.execution.expected` containing exactly the six implementation paths from frontmatter plus `.planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md`. Immediately compare the byte snapshots and hashes once before continuing. The snapshots are observation-only safeguards: do not use them to restore anything, and do not write the real Git index. Throughout both tasks, do not edit, format, stage, revert, restore, or commit `.planning/config.json`, `.planning/ROADMAP.md`, or `235-FAST-01-GAP-CLOSURE-READINESS.json`.

    Start RED by creating the FAST shell self-test and extending the ExUnit contract with assertions for every behavior above. Require the production verifier to be executable and begin with exactly `#!/bin/bash`. The runtime test must execute the real fixed-input verifier directly under a hostile PATH directory containing executable sentinels for the complete command inventory: `bash`, `dirname`, `uname`, `mktemp`, `realpath`, `readlink`, `stat`, `env`, `gh`, `jq`, `mkdir`, `cp`, `rm`, `sandbox-exec`, `unshare`, `sudo`, `true`, `dd`, `shasum`, and `awk`. It must also set adversarial TMPDIR/TMP/TEMP, require `offline_fast_01_gap_closure_attestation_verified`, and prove the aggregate sentinel log is empty and every attacker-selected directory remained untouched. Add explicit failing cases for unavailable trusted utility/staging creation and component-boundary adjacency; do not use error suppression to turn verifier failure into test success.

    Then harden the FAST verifier by replacing its `/usr/bin/env` entrypoint with the supported pinned absolute `#!/bin/bash` before any other line can consult caller state. Derive the script directory and ROOT without `dirname` or any other external command, using only Bash parameter expansion and builtin `cd`/`pwd -P`; reject an unusable source path before constructing PHASE or retained-input paths. Build an explicit utility inventory before staging/network isolation. The only allowed commands in this interval are Bash builtins or verifier-selected absolute executables whose path and executable state satisfy a hard-coded platform allowlist/canonical validation. This inventory must cover the commands already reachable in the verifier (`gh`, `jq`, `uname`, `mktemp`, `mkdir`, `cp`, `rm`, `env`, `sandbox-exec` or `unshare`/`sudo` with absolute `true`, `dd`, `shasum`, and `awk`) and any `realpath`, `readlink`, or `stat` introduced for canonicalization/ownership checks. Never fall back from an unavailable absolute/canonically validated binary to inherited PATH. Clear TMPDIR, TMP, and TEMP through trusted absolute env before invoking trusted mktemp with a template under a fixed platform staging parent. Before copying any receipt/bundle/root, validate the fixed parent and created work directory using those trusted absolute utilities and shell builtins: absolute canonical containment by path component, parent/work ownership, real-directory/non-symlink state, and private work permissions. Install cleanup only after the work path passes validation, and keep cleanup confined to that validated path. Make missing tools and every staging failure explicit nonzero exits. Ensure namespace capability probes execute an absolute trusted `true`, never a PATH-resolved child command.

    Preserve every locked decision explicitly. D-01: use the already retained immutable wall-mode window and do not capture another. D-02: retain queue-inclusive timing, all conclusions, floor-index p50, and the canonical ordering. D-03: retain the exact measured n=15/p50=486/run-ID claim and strict `<720` verdict; equality is still a miss. D-04: leave the single before/after ownership artifact unchanged. D-05: preserve its mechanically reconciled 93-row inventory and receivers. D-06: leave the `MIX_ENV=test mix ci` contributor topology and documentation unchanged. D-07: preserve the evidence-backed SEED-005/CI-PERF closeout. D-08: fold no pending or unrelated work into this repair. Keep the existing network isolation and repository/signer/main-ref/digest/adverse-case policy after staging; staging/trust establishment must execute before copying, parsing, or isolation assertions in a testable deterministic order.
  </action>
  <verify>
    <automated>test -x scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh &amp;&amp; test "$(head -n 1 scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh)" = '#!/bin/bash' &amp;&amp; scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh &amp;&amp; scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs &amp;&amp; preserve_pointer="$(git rev-parse --git-path gsd-235-14-preservation.path)" &amp;&amp; test -s "$preserve_pointer" &amp;&amp; IFS= read -r preserve_dir &lt; "$preserve_pointer" &amp;&amp; test -d "$preserve_dir" &amp;&amp; cmp -s "$preserve_dir/config.json" .planning/config.json &amp;&amp; cmp -s "$preserve_dir/ROADMAP.md" .planning/ROADMAP.md &amp;&amp; cmp -s "$preserve_dir/readiness.json" .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json &amp;&amp; while IFS=' ' read -r expected path; do actual="$(git hash-object --no-filters -- "$path")"; test "$actual" = "$expected" || exit 1; done &lt; "$preserve_dir/manifest.git-hash" &amp;&amp; git status --porcelain=v1 -- .planning/config.json .planning/ROADMAP.md .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json &gt; "$preserve_dir/status.current" &amp;&amp; cmp -s "$preserve_dir/status.porcelain" "$preserve_dir/status.current" &amp;&amp; git diff --cached --quiet -- .planning/config.json .planning/ROADMAP.md .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json</automated>
  </verify>
  <acceptance_criteria>
    - The preflight manifest and byte snapshots exist before the first edit, match all three user-owned dirty files after Task 1, and none of those paths is staged or restored.
    - Bash-only script-root derivation and the complete hostile command inventory prove that no PATH-shadowed `dirname` or other pre-staging/pre-isolation utility can redirect the phase/input paths or execute before trust establishment.
    - The hostile-environment self-test reaches the verifier's final success marker and proves PATH/TMPDIR/TMP/TEMP inputs never control a command or staging path.
    - Trusted fixed-parent and work-directory checks happen before retained inputs are copied or parsed, and all invalid/missing/staging-failure cases stop closed.
    - Component-aware containment rejects equality and adjacency tricks; 720 remains a strict miss.
    - The live verifier accepts the unchanged retained FAST subject under the existing network-denied provenance policy.
    - The FAST verifier is executable with exact entrypoint `#!/bin/bash`; its hostile test invokes it directly, includes an executable `bash` sentinel, observes the final positive marker, and proves zero sentinel executions.
    - Before Task 1 edits, repository-wide status and its normalized path set are saved; the normalized baseline equals exactly the three known user-owned dirty paths, and the explicit seven-path execution allowlist is saved for Task 2's final set-difference comparison.
  </acceptance_criteria>
  <done>FAST-01's unchanged n=15/p50=486 receipt has an end-to-end verifier whose pre-isolation staging is independent of hostile caller PATH and temp state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand trusted staging to GATE-05 and run scoped terminal ratification</name>
  <reversibility rating="reversible">The GATE-05 repair changes only local verifier internals and regression coverage; the protected receipt and 93-row ledger remain immutable.</reversibility>
  <files>scripts/ci/verify-terminal-ratification-attestation-offline.sh, scripts/ci/verify-terminal-ratification-attestation-offline.test.sh, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs, .planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md</files>
  <read_first>
    - The preservation pointer from Task 1 and its private snapshot directory — compare `.planning/config.json`, `.planning/ROADMAP.md`, and `235-FAST-01-GAP-CLOSURE-READINESS.json` against both saved bytes and hashes before editing Task 2 and again after all Task 2 verification; retain `status.repo.baseline.z`, `paths.repo.baseline`, `paths.user-dirty.expected`, and `paths.execution.expected` through the final changed-path delta assertion. These user-owned dirty files remain read-only and outside staging/commits.
    - `scripts/ci/verify-terminal-ratification-attestation-offline.sh` — modify its fixed-parent staging, ownership/non-symlink validation, and cleanup ordering.
    - `scripts/ci/verify-terminal-ratification-attestation-offline.test.sh` — replace early-exit-permitting cases with checkpoint-reaching hostile-environment coverage.
    - `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — modify its verifier trust-boundary contract.
    - `scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh` and `scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh` — reuse the production-quality Task 1 staging pattern without creating a second policy.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.json` and `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — fixed protected receipt and 93-row ledger; read only.
  </read_first>
  <behavior>
    - "RED: the executable terminal verifier starts with the exact supported `#!/bin/bash` entrypoint, and its hostile-PATH self-test executes the verifier directly rather than selecting an interpreter through PATH."
    - "RED: the terminal hostile PATH shadows the complete applicable inventory — `bash`, `dirname`, `uname`, `mktemp`, `realpath`, `readlink`, `stat`, `env`, `gh`, `jq`, `mkdir`, `cp`, `rm`, `sandbox-exec`, `unshare`, `sudo`, `true`, and `dd` — and the test requires both an empty aggregate sentinel log and the final positive marker."
    - "RED: the Task 1 saved bytes and content hashes for config, ROADMAP, and readiness still match after all Task 2 commands, and none of the three user-owned dirty paths is staged."
    - "RED: the terminal self-test fails unless the verifier reaches and passes trusted staging plus the final positive protected-subject verification."
    - "RED: hostile PATH/TMPDIR/TMP/TEMP, including empty/unset/equal/adjacent-root variants, cannot select utilities or staging storage."
    - "RED: fixed-parent ownership, canonical strict-child containment, non-symlink state, private work permissions, and cleanup confinement are contractually required before copying inputs."
    - "RED: the protected run identity, subject digest, repository, signer workflow, main ref, trusted root, adverse cases, and 93 ownership rows retain their existing semantics."
    - "RED/final scope: after the required summary is written, the normalized post-execution status path set minus the saved baseline path set equals exactly the six implementation files plus `235-14-SUMMARY.md`; the three baseline dirty paths separately retain their exact bytes, hashes, and status records."
  </behavior>
  <action>
    Before editing Task 2, load the Task 1 preservation pointer and compare all three saved byte copies and manifest hashes with the current config, ROADMAP, and readiness files; halt on any mismatch or staged state rather than restoring or otherwise touching those paths.

    Start RED by rewriting the terminal shell self-test so the PATH and temp-root adversarial cases execute the real verifier directly, complete the real positive verification, and observe `offline_attestation_verified`. Require the production verifier to be executable and begin with exactly `#!/bin/bash`. Replace the partial shadows with executable sentinels for the complete applicable inventory: `bash`, `dirname`, `uname`, `mktemp`, `realpath`, `readlink`, `stat`, `env`, `gh`, `jq`, `mkdir`, `cp`, `rm`, `sandbox-exec`, `unshare`, `sudo`, `true`, and `dd`. Require an empty aggregate sentinel log, and remove the existing error-suppressed invocations that can pass before staging. Extend the ExUnit contract using `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` as the implementation analog identified by `235-PATTERNS.md`: follow its fail-closed reconciliation/error-handling pattern so the contract recomputes or validates authoritative evidence and rejects mismatch or incomplete proof instead of accepting stored claims or early exits. Bind the pinned interpreter, Bash-only ROOT derivation, the full checkpoint-reaching hostile test, trusted fixed-parent policy, ownership/non-symlink/private-mode checks, absolute namespace probe children, and the required ordering before retained-input copies.

    Apply the Task 1 trusted-staging pattern to the terminal verifier, beginning by replacing its `/usr/bin/env` entrypoint with the supported pinned absolute `#!/bin/bash`: Bash-only script-root derivation; an enumerated, allowlisted/canonically validated absolute utility inventory with no inherited-PATH fallback; trusted absolute environment; cleared TMPDIR/TMP/TEMP; fixed platform parent; component-aware canonical strict-child validation; parent/work ownership and non-symlink checks; private permissions; and cleanup confined to the validated work directory. The inventory must cover every command reachable before staging/network isolation, including `uname`, `mktemp`, `mkdir`, `cp`, `rm`, `env`, `sandbox-exec` or `unshare`/`sudo` plus absolute `true`, `gh`, `jq`, and `dd`, as well as any introduced `realpath`, `readlink`, or `stat`. Preserve the existing trusted absolute sudo fallback and network-denied verification path. Do not change protected receipt bytes, the 93-row ledger, workflow wiring, status claims, retries, timeouts, packages, schema, admin UI, or any external evidence.

    Finish with the scoped Phase 235 commands only. Run both verifier self-tests directly, then both live offline verifiers directly, then both focused ExUnit contracts. Write `.planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md` from the required summary template before the final scope gate. After those commands, compare all three current files with the Task 1 byte snapshots and content-hash manifest, compare their scoped porcelain status with the saved status, and assert `git diff --cached --quiet --` for the three paths. Capture repository-wide post-execution porcelain-v1 `-z` status, normalize it with the same fail-closed Bash parser used at baseline, and use `comm -13 paths.repo.baseline paths.repo.post` to produce `paths.execution.actual`; compare it byte-for-byte with the sorted explicit seven-path `paths.execution.expected`. Because preflight required the baseline to contain exactly the three known user-owned dirty paths, this subtraction cannot confuse those paths with execution-created changes; any eighth path or missing required path fails. Do not use reset, checkout, restore, the real Git index, or staging to derive the delta. Only on success remove the exact snapshot, manifest, baseline/post/delta path-list files, their now-empty private directory, and the pointer using individually named validated paths (no glob, recursive deletion, or broad operation). This is verification of retained evidence, not a recapture or a new measurement, per D-01 through D-08.
  </action>
  <verify>
    <automated>test -x scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh &amp;&amp; test -x scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; test "$(head -n 1 scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh)" = '#!/bin/bash' &amp;&amp; test "$(head -n 1 scripts/ci/verify-terminal-ratification-attestation-offline.sh)" = '#!/bin/bash' &amp;&amp; scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh &amp;&amp; scripts/ci/verify-terminal-ratification-attestation-offline.test.sh &amp;&amp; scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh &amp;&amp; scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; test -s .planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md &amp;&amp; preserve_pointer="$(git rev-parse --git-path gsd-235-14-preservation.path)" &amp;&amp; test -s "$preserve_pointer" &amp;&amp; IFS= read -r preserve_dir &lt; "$preserve_pointer" &amp;&amp; test -d "$preserve_dir" &amp;&amp; cmp -s "$preserve_dir/config.json" .planning/config.json &amp;&amp; cmp -s "$preserve_dir/ROADMAP.md" .planning/ROADMAP.md &amp;&amp; cmp -s "$preserve_dir/readiness.json" .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json &amp;&amp; while IFS=' ' read -r expected path; do actual="$(git hash-object --no-filters -- "$path")"; test "$actual" = "$expected" || exit 1; done &lt; "$preserve_dir/manifest.git-hash" &amp;&amp; git status --porcelain=v1 -- .planning/config.json .planning/ROADMAP.md .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json &gt; "$preserve_dir/status.current" &amp;&amp; cmp -s "$preserve_dir/status.porcelain" "$preserve_dir/status.current" &amp;&amp; git diff --cached --quiet -- .planning/config.json .planning/ROADMAP.md .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-GAP-CLOSURE-READINESS.json &amp;&amp; git status --porcelain=v1 -z --untracked-files=all &gt; "$preserve_dir/status.repo.post.z" &amp;&amp; while IFS= read -r -d '' record; do case "${record:0:2}" in R*|C*) exit 1;; esac; path="${record:3}"; case "$path" in *$'\n'*|*$'\t'*) exit 1;; esac; printf '%s\n' "$path"; done &lt; "$preserve_dir/status.repo.post.z" | LC_ALL=C sort -u &gt; "$preserve_dir/paths.repo.post" &amp;&amp; comm -13 "$preserve_dir/paths.repo.baseline" "$preserve_dir/paths.repo.post" &gt; "$preserve_dir/paths.execution.actual" &amp;&amp; cmp -s "$preserve_dir/paths.execution.expected" "$preserve_dir/paths.execution.actual" &amp;&amp; rm -f -- "$preserve_dir/config.json" "$preserve_dir/ROADMAP.md" "$preserve_dir/readiness.json" "$preserve_dir/manifest.git-hash" "$preserve_dir/status.porcelain" "$preserve_dir/status.current" "$preserve_dir/status.repo.baseline.z" "$preserve_dir/status.repo.post.z" "$preserve_dir/paths.repo.baseline" "$preserve_dir/paths.repo.post" "$preserve_dir/paths.user-dirty.expected" "$preserve_dir/paths.execution.expected" "$preserve_dir/paths.execution.actual" &amp;&amp; rmdir -- "$preserve_dir" &amp;&amp; rm -f -- "$preserve_pointer"</automated>
  </verify>
  <acceptance_criteria>
    - Both complete hostile command inventories reach their respective final positive markers without executing any PATH shadow, including `dirname`, platform/staging tools, isolation helpers, and commands used by FAST's digest checks.
    - Both hostile-environment tests require final successful verification, so no earlier unrelated failure can produce a pass.
    - Both live verifiers pass using fixed trusted staging and preserve network-denied provenance semantics.
    - Focused ExUnit contracts prove strict threshold equality behavior, fail-closed staging, exact protected identities, and the unchanged 93-row ownership proof.
    - The six declared verifier/test files are the only implementation files changed by this plan. The preflight byte snapshots and hashes prove `.planning/config.json`, `.planning/ROADMAP.md`, and `235-FAST-01-GAP-CLOSURE-READINESS.json` are unchanged after both tasks; the three paths remain unstaged and are never reverted, restored, or included in a task commit.
    - Both verifier files are executable with exact entrypoint `#!/bin/bash`; both hostile tests invoke their verifier directly, include an executable `bash` sentinel, require the respective final positive verification marker, and prove the aggregate sentinel log stayed empty.
    - The pre-execution repository path/status baseline contains exactly the three preserved user-owned dirty paths, and the final normalized set difference contains exactly the six implementation files plus `235-14-SUMMARY.md`; no baseline dirty path is counted as execution-created and no additional changed path is permitted.
  </acceptance_criteria>
  <done>FAST-01 and GATE-05 both have deterministic, checkpoint-reaching proof that hostile pre-isolation environment state cannot compromise their retained evidence verification.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/ci/verify-fast-01-gap-closure-attestation-offline.test.sh` — new internal shell self-test; no public CLI, environment variable, field, or external API is introduced.
- `scripts/ci/verify-fast-01-gap-closure-attestation-offline.sh` — internal trusted-utility and trusted-staging helpers/policy; fixed-input CLI remains unchanged.
- `scripts/ci/verify-terminal-ratification-attestation-offline.sh` — internal trusted-staging helpers/policy aligned with FAST; fixed-input CLI remains unchanged.
- `scripts/ci/verify-terminal-ratification-attestation-offline.test.sh` — strengthened checkpoint-reaching hostile-environment cases; no public symbols.
- `test/sigra/planning/phase_235_fast_01_gap_closure_contract_test.exs` — new internal staging-order/fail-closed assertions; no public symbols.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — new internal fixed-parent/ownership/non-symlink assertions; no public symbols.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md` — execution summary required before the final deterministic changed-path assertion.
- No new receipt fields, ledger rows, requirement statuses, workflow jobs, packages, schemas, UI symbols, or external API integrations.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data and ownership evidence | 14 | COVERED | Existing retained evidence becomes safely verifiable; no remeasurement. |
| REQ | FAST-01 | n>=10 and p50 strictly below 720 | 14 | COVERED | Retained n=15/p50=486; strict equality miss remains tested. |
| REQ | GATE-05 | Single before/after PR/main/nightly ownership proof | 14 | COVERED | Retained protected 93-row ledger is verified without mutation. |
| RESEARCH | — | Offline provenance is independent, fail-closed, and network denied | 14 | COVERED | Trusted staging precedes existing exact-subject policy. |
| CONTEXT | D-01–D-03 | Immutable wall-mode method, canonical p50, honest exact result | 14 | COVERED | Receipt and comparator are unchanged and regression-tested. |
| CONTEXT | D-04–D-05 | One complete mechanically checked ownership artifact | 14 | COVERED | The 93-row artifact/receipt stay immutable; verifier trust is repaired. |
| CONTEXT | D-06 | Contributor topology remains accurate | 14 | COVERED | No topology or contributor-doc change. |
| CONTEXT | D-07 | Closeout follows measured evidence | 14 | COVERED | No claim edit; valid offline proof restores its prerequisite. |
| CONTEXT | D-08 | No unrelated todos or remediation | 14 | COVERED | Six verifier/test files only. |
| VERIFICATION | FAST blocker | PATH and temp-root control before isolation | 14 | COVERED | Task 1 hardens and proves the full staging path. |
| VERIFICATION | GATE blocker | Inherited temp root and early-exit false-positive test | 14 | COVERED | Task 2 establishes fixed staging and success-required regression. |
| EDGE | FAST adjacency/empty/ordering | Equality miss, root containment, missing inputs/tools, deterministic staging order | 14 | COVERED | Explicit must-haves, behaviors, and automated contracts. |
| EDGE | GATE unclassified | Regression must reach trusted staging | 14 | COVERED | Final verifier success marker is mandatory. |

No source item is missing. Deferred ideas and work assigned to other phases remain excluded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller environment → verifier staging | PATH and TMPDIR/TMP/TEMP are untrusted before network isolation. |
| Existing dirty worktree → plan execution | User-owned config, ROADMAP, and readiness bytes/status must survive both tasks without staging or restoration. |
| Shared fixed parent → private work directory | Symlink, ownership, containment, permission, and creation races must fail closed. |
| Staged inputs → network-denied attestation verifier | Exact retained bytes must reach unchanged repository/signer/ref/digest verification. |
| Regression harness → security claim | A test must reach the staging checkpoint and final verification, not pass on an earlier error. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-14-01 | Spoofing | Interpreter, PATH utility, and script-root resolution | high | mitigate | Pin both executable entrypoints to `#!/bin/bash`; invoke verifiers directly; shadow `bash` plus dirname, platform/staging/isolation tools, gh/jq, and FAST digest utilities; require empty sentinel logs and both final positive markers; derive ROOT with Bash builtins only. |
| T-235-14-02 | Tampering | TMPDIR/TMP/TEMP staging selection | high | mitigate | Clear all overrides with trusted env; fixed platform parent; hostile empty/equal/adjacent/descendant-root cases. |
| T-235-14-03 | Tampering | staging parent/work path | high | mitigate | Canonical component containment, ownership, directory/non-symlink/private-mode checks, validated cleanup target, fail-closed creation. |
| T-235-14-04 | Tampering | staging race/symlink swap | high | mitigate | Atomic trusted mktemp under validated sticky/root-owned parent, immediate post-create validation before copies, confined cleanup. |
| T-235-14-05 | Repudiation | verifier regression result | high | mitigate | Both self-tests require final positive marker; no error suppression or shadow-only early-exit assertion. |
| T-235-14-06 | Information disclosure | staged retained evidence | low | mitigate | Private work permissions, empty isolated HOME, credentials/proxies cleared, deterministic cleanup. |
| T-235-14-07 | Denial of service | missing utilities/staging failure | medium | mitigate | Explicit prerequisite checks and nonzero diagnostics; no fallback into untrusted storage. |
| T-235-14-08 | Elevation of privilege | Linux sudo isolation fallback | high | mitigate | Preserve absolute `/usr/bin/sudo` invocation and cleanup; never resolve sudo through PATH. |
| T-235-14-09 | Tampering | network-denied provenance semantics | high | mitigate | Preserve exact repository, signer workflow, main ref, subject digest, trusted root, and adverse verification cases. |
| T-235-14-10 | Tampering | user-owned dirty planning files | high | mitigate | Pre-edit byte copies plus git content-hash/status manifest; compare after each task and before exact-path cleanup; prohibit staging, restore, revert, formatting, or task commits for all three paths. |
| T-235-14-11 | Tampering | execution changed-path scope | high | mitigate | Save and validate the repository-wide pre-execution status path baseline, subtract it from the normalized post-execution path set, and require exact equality with the six implementation paths plus `235-14-SUMMARY.md`; do not mutate the real index. |
</threat_model>

<verification>
Before any edit, snapshot current bytes, git content hashes, and porcelain status for `.planning/config.json`, `.planning/ROADMAP.md`, and `235-FAST-01-GAP-CLOSURE-READINESS.json` into the private task-specific preservation directory. At the same point, save repository-wide NUL-delimited porcelain status, normalize and sort its paths, require that baseline to equal exactly those three known dirty paths, and save the explicit seven-path execution allowlist. Require both verifier files to be executable with exact `#!/bin/bash` headers. Run both hostile-environment verifier self-tests directly, both live offline verifiers directly, and the two focused ExUnit contracts in that order. Require each hostile inventory to include `bash`, reach its verifier's final positive marker, and produce an empty sentinel log. Compare the three user-owned files byte-for-byte and by hash after Task 1 and again after Task 2, prove they remain unstaged, write `235-14-SUMMARY.md`, normalize post-execution status with the same parser, and require `comm -13` of baseline versus post paths to equal exactly the six implementation files plus the summary before exact-path cleanup. Confirm the implementation diff excludes all receipts, ledgers, requirement claims, workflow topology, admin UI, schema, package, timeout, and retry-policy files.
</verification>

<success_criteria>
- FAST-01's unchanged n=15/p50=486 receipt passes only through trusted fixed staging and existing network-denied provenance verification.
- GATE-05's unchanged protected receipt and 93-row ledger pass through the same trust boundary.
- PATH and TMPDIR/TMP/TEMP manipulation cannot influence utilities, paths, copies, parsing, isolation, or cleanup.
- Both regressions prove the staging checkpoint was reached; missing tools/inputs or staging faults fail closed.
- Bash-only root derivation and trusted absolute/canonically validated utilities prevent every pre-staging/pre-isolation inherited-PATH command, including `dirname`, from redirecting or executing the verification path.
- `/bin/bash` is selected by both verifier shebangs before hostile PATH can act; direct hostile-PATH execution reaches each final marker with zero `bash` or other sentinel executions.
- The executor proves the existing dirty config, ROADMAP, and readiness files retain their exact pre-task bytes/hashes and unstaged state after both tasks.
- The normalized post-execution changed-path delta relative to the exact three-path baseline is exactly the six implementation files plus `235-14-SUMMARY.md`, with no extra or missing path.
- No external data is recaptured, remeasured, or dispatched, and all excluded files remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-14-SUMMARY.md` when done.
</output>
