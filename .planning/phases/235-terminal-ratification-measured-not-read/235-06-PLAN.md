---
phase: 235-terminal-ratification-measured-not-read
plan: 06
type: execute
wave: 6
depends_on: [235-05]
files_modified:
  - .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "D-01/D-02/D-03: the retained 19 PR, 2 push, and 2 schedule runs are the exact bounded subset of a byte-retained, digest-pinned canonical GitHub run-list response, and the honest FAST-01 verdict remains a 772-second miss."
    - "D-03: exactly two binding-pole receipts are selected from the source-bound PR population—the sorted floor(n/2) run and the maximum-duration run—and each receipt's wall duration, command, output bytes, digest, and diagnosed job are replay-verified."
    - "D-04/D-05: every one of the 93 ownership rows has the exact family/event owner, seam, aggregate, state, receiver, phase provenance, and execution receipt required by the Phase 234 inventory, parsed ci.yml topology, and retained evidence bytes."
    - "Wrong-but-plausible substitutions using another live workflow job or existing receipt fail closed; nonempty strings alone never satisfy ownership proof."
    - "D-07/D-08: the existing measured residual remains the only Phase 235 residual, no pending work is folded in, and no CI topology or performance behavior changes."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: When two things are exactly equal or just touch, do they merge, collide, or separate?"
    - requirement_id: FAST-01
      category: empty
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: What is the result for empty, single-element, or null input?"
    - requirement_id: FAST-01
      category: ordering
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: When elements compare equal, is output order specified and stable?"
    - requirement_id: GATE-05
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT represent FAST-01 as achieved from fewer than ten eligible post-cutoff PR runs, a p50 at or above 720 seconds, filtered conclusions, or substituted timing semantics."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present coverage as complete while omitting an affected spec or suite, naming only a terminal aggregate, or lacking a receiver and execution receipt for moved work."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
  artifacts:
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      provides: "byte-retained run-list, pole-metrics, and ownership-job receipts for the existing terminal ledger"
    - path: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      provides: "source-population, pole-selection/replay, and exhaustive ownership-semantics contract"
  key_links:
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      to: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      via: "literal receipt bytes -> pinned SHA-256 -> decoded authoritative population and jobs -> ledger comparisons"
    - from: scripts/ci/ci-run-metrics.sh
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      via: "offline replay of byte-retained jobs through the mandated wall/job metrics instrument"
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      via: "hash-pinned spec lane fields expanded across PR, push, and schedule with workflow/event proof"
    - from: .github/workflows/ci.yml
      to: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      via: "parsed job IDs, steps, conditions, and aggregate needs validate every after-state destination"
---

<objective>
Close the three unresolved Phase 235 verification gaps by making the existing terminal ledger independently replayable and semantically exhaustive.

Purpose: Plans 04–05 proved the key universe but left its source population, pole selection, and most ownership values self-asserted. This plan binds those claims to retained bytes and authoritative repository inputs without changing the measured miss per D-01 through D-08.
Output: the existing ledger gains canonical source/replay receipts, and the focused ExUnit contract rejects coherent population forgery, forged pole metadata, and plausible-but-false ownership mappings.
</objective>

<execution_context>
@/Users/developer/.codex/gsd-core/workflows/execute-plan.md
@/Users/developer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-05-SUMMARY.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
@test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
@scripts/ci/ci-run-metrics.sh
@scripts/ci/ci-run-metrics.test.sh
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
@test/sigra/planning/phase_234_playwright_inventory_contract_test.exs
@.github/workflows/ci.yml
</context>

<flagged_planner_assumptions>
- Probe-source coverage: 4 source items = 4 surfaced assumptions; no silent drops.
- **FAST-01 / adjacency — FLAGGED UNRESOLVED:** When two things are exactly equal or just touch, do they merge, collide, or separate? Existing D-02 logic remains in force, but this spec-less fallback row is not marked resolved.
- **FAST-01 / empty — FLAGGED UNRESOLVED:** What is the result for empty, single-element, or null input? Existing minimum-population validation remains in force, but this spec-less fallback row is not marked resolved.
- **FAST-01 / ordering — FLAGGED UNRESOLVED:** When elements compare equal, is output order specified and stable? Existing sorted floor(n/2) behavior remains in force, but this spec-less fallback row is not marked resolved.
- **GATE-05 / unclassified — FLAGGED UNRESOLVED:** unclassified — review manually. The exact ownership mapping and mutations provide a review surface without inventing a classification.
- Assumption-delta detector: `detected:false`; no additional delta is introduced.
</flagged_planner_assumptions>

## Artifacts this phase produces

- New paths: none.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` adds `capture_endpoint.source_receipt` (`command`, literal `output`, `sha256`), byte-complete `metrics_receipt` objects for both existing binding-pole entries, and literal output bytes/digests for each `receipts.ownership` event receipt. Existing fields may be normalized into these closed receipt objects; no second evidence artifact is created.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` adds private validation/replay helpers for canonical population reconciliation, deterministic pole selection, closed receipt schemas, parsed workflow ownership, and exhaustive row semantics. No public runtime symbol, CLI flag, schema field, or application interface is introduced.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace canonical run-list bytes through the bounded FAST-01 verdict</name>
  <precondition>`gh auth status --hostname github.com` succeeds and all 23 retained run IDs remain readable without a 403/429 response.</precondition>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — first unresolved gap and the exact source-population failure mode.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — current capture endpoint, self-supplied event populations, and unused population digest.
    - `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — current `validate_capture!/1` and `validate_captured_ledger!/1`, which pin strings but never decode a source receipt.
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — exact `gh run list` fields, wall-mode bounds, all-conclusion retention, and floor(n/2) semantics required by D-01/D-02.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-01, D-02, D-03, D-07, and D-08.
  </read_first>
  <behavior>
    - "The exact stdout bytes from the ledger's canonical `gh run list` command are retained under `capture_endpoint.source_receipt.output`, and their lowercase 64-hex SHA-256 equals both the receipt digest and the independently pinned contract constant."
    - "After decoding those bytes, filtering by the immutable cutoff and capture endpoint, and partitioning by event, the result has exactly 19 pull_request, 2 push, and 2 schedule rows."
    - "Every bounded source row occurs exactly once in `measurements.EVENT.runs` and matches databaseId/id, event, URL, createdAt/created_at, updatedAt/updated_at, conclusion, and headSha/head_sha field-for-field."
    - "Nil/string/duplicate IDs, source omissions/additions, provenance changes, digest changes, and a coherent replacement of source bytes plus all ledger measurements/statistics are rejected."
    - "The replayed PR result remains n=19, p50=772, status=miss; push and schedule remain in the same source-bound window."
  </behavior>
  <action>
    Start RED by adding focused mutations for absent source bytes, changed bytes with stale digest, changed bytes with an updated ledger digest, omitted and invented source rows, each provenance field, invalid ID types, and a coherent alternative population that rewrites runs, run_ids, output receipts, statistics, and verdict together. Prove the current `validate_capture!/1` plus `validate_captured_ledger!/1` accepts at least one coherent replacement before implementing the new boundary.

    Per D-01/D-02/D-03, execute the ledger's literal `capture_endpoint.source_command` once and retain its exact stdout including the trailing newline in a closed `source_receipt` object with exactly `command`, `output`, and `sha256`. Do not dispatch or watch any run, select a new cutoff, filter conclusions, or change the 2026-08-02T18:07:04Z capture endpoint. Pin the resulting byte digest as a module attribute independent of editable ledger values. Decode the receipt, require the documented GitHub field set and unique positive integer IDs, then derive the bounded population by the locked cutoff/capture timestamps and events. If the canonical list contains post-endpoint runs, retain them in the raw receipt but exclude them only through the pinned upper-bound comparison; fail if any of the existing 23 rows is absent from the bounded result.

    Replace self-consistency with exact source-to-ledger equality for all identity and provenance fields, exact event partitions, and exact set cardinality. Recompute statistics through the existing baseline-compatible helper and keep the strict 772-second miss plus current push/schedule outcomes unchanged per D-03/D-07. Per D-08, do not modify the workflow, contributor docs, requirements status, residual, or any unrelated planning record.
  </action>
  <acceptance_criteria>
    - The canonical run-list output is present as literal bytes, its closed receipt schema and pinned digest validate, and its bounded event partitions equal all 23 retained rows field-for-field.
    - Independent mutations for every source field, row membership, digest, ID type, and coherent replacement fail with specific diagnostics.
    - The source-bound recomputation retains all conclusions and produces PR n=19, p50=772, strict status `miss`, plus the existing same-window push and schedule outcomes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs --trace &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh</automated>
  </verify>
  <done>The terminal verdict is derived from a byte-retained canonical GitHub population rather than editable ledger rows alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive and replay the exact median and maximum pole receipts</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - Both files in `<files>` — current two binding-pole entries and permissive `validate_binding_pole_receipts!/2` implementation.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — second unresolved gap and CR-01.
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — mandated `--jobs RUN_ID --format json` output and duration clamping.
    - `.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md` — D-07 residual references that must remain true.
  </read_first>
  <behavior>
    - "The receipt list has exactly the selections `median_neighbor` and `maximum_duration`, with no duplicates or extras."
    - "Sorting source-bound PR runs by `{wall_seconds, id}` and selecting floor(n/2) yields the recorded median receipt; selecting the maximum wall duration with the same deterministic tie rule yields the recorded maximum receipt."
    - "Each receipt's `wall_seconds` equals its selected run's queue-inclusive run duration, and its run ID/URL equal the source-bound population row."
    - "Each `source_receipt` and `metrics_receipt` has exactly command/output/sha256; both digests cover literal bytes and both commands contain the selected run ID."
    - "Offline replay of the bound source receipt's `.jobs` through `ci-run-metrics.sh --jobs` is byte-equal to `metrics_receipt.output`, and the diagnosed job's name, conclusion, and duration occur exactly once in that output."
    - "Swapped selection labels, wrong wall seconds, altered commands/bytes/digests, extra receipts, and plausible job substitutions are rejected."
  </behavior>
  <action>
    Start RED with independent mutations for the two selection labels, selected run IDs, `wall_seconds`, source command/bytes/digest, metrics command/bytes/digest, binding job name/conclusion/duration, duplicate receipt, and extra receipt. Include CR-01's concrete forgery: label a different retained PR run as the median while preserving a valid job source receipt.

    Derive expected selections only from Task 1's source-bound PR rows. Compute queue-inclusive wall seconds from run timestamps, sort deterministically by `{duration, id}`, require the floor(n/2) and maximum selections exactly once, and compare both IDs, URLs, and wall seconds. Convert each existing loose metrics command/hash pair into a closed `metrics_receipt` containing the literal command, stdout (including trailing newline), and digest. Validate both source and metrics receipt key sets exactly; pin neither selection by unverified labels alone.

    Build a temporary deterministic `gh` executable inside the test that accepts only the exact `gh run view RUN_ID --repo szTheory/sigra --json jobs --jq .jobs` invocation made by `scripts/ci/ci-run-metrics.sh`, returns the decoded `.jobs` array from the digest-checked `source_receipt`, and rejects every other call. Run the mandated script with this executable first on `PATH`, require exit 0 and byte equality with `metrics_receipt.output`, then confirm the named binding job appears exactly once with matching conclusion and duration. Keep the residual's current run IDs and literal metrics commands checked against these validated receipts per D-03/D-07; never change the measured 772-second miss into a pass.
  </action>
  <acceptance_criteria>
    - Exactly two selection receipts are derived from the source-bound PR population and match the deterministic median/max IDs, URLs, and wall durations.
    - Both metrics outputs replay byte-for-byte offline from their digest-checked source jobs through `ci-run-metrics.sh`.
    - Every selection, command, output, digest, wall duration, job field, duplicate, and extra-receipt mutation fails; CR-01's valid-job/wrong-pole forgery is rejected.
    - The existing FAST-01 residual cites the two validated run IDs and metrics commands, and the verdict remains 772 seconds / `miss`.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs --trace &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh</automated>
  </verify>
  <done>The median and maximum diagnoses are selected from the measured population and reproducible from byte-pinned jobs evidence through the mandated instrument.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate all 93 ownership destinations against workflow and receipt evidence</name>
  <precondition>The three retained ownership run IDs remain readable through `gh run view` without a 403/429 response.</precondition>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - Both files in `<files>` — current 93 rows, `receipts.ownership`, and `validate_rows!/1`, which semantically constrains only `ci_gate_aggregate`.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — third unresolved gap and hollow-mapping diagnosis.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` and `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — D-05 spec-to-lane source and executable invocation rules.
    - `.github/workflows/ci.yml` — authoritative job IDs, event guards, direct commands, seams, and aggregate `needs` edges.
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` and `.planning/phases/233-library-suite-economics/233-EVIDENCE.json` — phase provenance named by ownership rows.
    - `CONTRIBUTING.md` — already-satisfied direct-owner versus aggregate vocabulary; read for consistency but do not modify.
  </read_first>
  <behavior>
    - "Each `receipts.ownership.EVENT` entry retains the exact `gh run view` stdout bytes, hashes them, and decodes to the declared run ID, event, URL, conclusion, and jobs array."
    - "For every after-state row whose state is executed, the declared direct owner resolves to exactly one job in that event receipt and its invocation/seam resolves in the parsed workflow job block or the pinned Phase 234 lane/harness mapping."
    - "For intentionally absent rows, the direct job exists in ci.yml but the parsed event condition excludes that event, and the event receipt does not falsely claim execution."
    - "Every terminal aggregate resolves to a parsed workflow job whose `needs` graph includes the direct owner or its declared matrix/diagnostic receiver path; aggregate rows use their own exact topology contract."
    - "Playwright spec semantics derive from the hash-pinned inventory's lane fields; the 11 non-Playwright families derive from an explicit contract map tied to workflow blocks and Phase 232/233 evidence paths."
    - "Every receiver, phase provenance, and receipt key is exact; substituting another live job, phase, or existing receipt fails even when it is syntactically valid."
  </behavior>
  <action>
    Start RED with table-driven mutations across representative Playwright, library, diagnostic, generated-host, design-snapshot, and aggregate rows. For each class substitute another real direct job, real aggregate, real receiver, valid phase label, and existing receipt key; also mutate state/event agreement and aggregate `needs`. Require these currently accepted nonempty substitutions to fail after hardening.

    Re-fetch each fixed `receipts.ownership` run ID once using its already-recorded `jobs_command`; add exact stdout bytes including the trailing newline and validate the existing digest against those bytes. Do not pick replacement runs or alter the window. Parse ci.yml into the complete job-ID/block map and decode each event receipt into a job-name/result map. Resolve workflow job IDs to receipt job display names through the job block's `name:` contract, including matrix names, instead of accepting substring coincidence.

    Per D-04/D-05, construct the expected 93-row semantic map from authoritative inputs: derive each Playwright spec's lane, seam, events, command marker, project, and approved harness indirection from the hash-pinned Phase 234 inventory; define all 11 non-Playwright family contracts in one exhaustive private map whose job IDs, commands/conditions, aggregate edges, Phase 232/233 evidence paths, and event receipt keys are verified before use. Compare each row's complete `after`, `receiver`, `phase`, and `receipt` values to that expected map. Correct ledger rows only where this derivation proves the current value wrong. For executed rows, require receipt execution; for intentionally absent rows, require a parsed excluding condition and absence from the event receipt. Require every receipt reference to resolve to a validated registry entry with matching event/phase semantics.

    Preserve D-06's already-verified contributor content and D-07 closeout wording without editing them. Per D-08, do not add families, alter workflow behavior, recapture tests, or fold pending todos. This task changes only the single ledger and its focused contract.
  </action>
  <acceptance_criteria>
    - All three ownership event receipts contain byte-retained, digest-validated job output bound to their existing run IDs/events.
    - Every one of the exact 93 rows equals the authoritative family/spec/event semantic map and is proven executed or intentionally absent from workflow plus receipt evidence.
    - Live-but-wrong direct-owner, aggregate, receiver, phase, receipt, state, event, and dependency substitutions fail deterministically across every ownership class.
    - The Phase 234 inventory remains the sole Playwright ownership input; no second inventory, workflow change, contributor-doc edit, or new residual is introduced.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs test/sigra/planning/phase_234_playwright_inventory_contract_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs --trace &amp;&amp; MIX_ENV=test mix test test/sigra/planning/ &amp;&amp; jq -e '.verdict.fast_01.observed_p50_seconds == 772 and .verdict.fast_01.status == "miss" and (.ownership.rows | length) == 93' .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json</automated>
  </verify>
  <done>The single ownership artifact proves every before/after destination with complete workflow and retained-receipt semantics, not merely exact keys or nonempty labels.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data; maintainer sees what moved and where it landed | 235-06 | COVERED | Tasks 1–3 bind population, pole diagnosis, and every ownership destination. |
| REQ | FAST-01 | Strict under-12-minute p50 over at least 10 post-change PR runs | 235-06 | COVERED | Tasks 1–2 preserve the honest 19-run/772-second miss while making its source and diagnosis independently replayable. |
| REQ | GATE-05 | One before/after PR/main/nightly artifact proves nothing silently dropped | 235-06 | COVERED | Task 3 proves complete semantics for all 93 rows from workflow, inventory, and event receipts. |
| RESEARCH | — | Baseline-compatible wall mode, bounded population, retained conclusions | 235-06 | COVERED | Task 1 binds raw source bytes to the existing calculations. |
| RESEARCH | — | Per-run job metadata diagnoses an honest miss | 235-06 | COVERED | Task 2 derives selections and replays metrics offline. |
| RESEARCH | — | Phase 234 inventory is the Playwright input | 235-06 | COVERED | Task 3 derives spec lanes without duplicating the inventory. |
| RESEARCH | — | Direct owner, aggregate, event state, and live receipt are distinct proof dimensions | 235-06 | COVERED | Task 3 validates all dimensions and plausible substitutions. |
| CONTEXT | D-01 | Immutable bounded wall-mode after-window | 235-06 | COVERED | Task 1 retains and filters canonical source bytes against fixed bounds. |
| CONTEXT | D-02 | Queue-inclusive duration, all conclusions, sorted floor(n/2) | 235-06 | COVERED | Tasks 1–2 reuse and replay the existing instrument semantics. |
| CONTEXT | D-03 | Exact commands/IDs and honest miss with binding pole | 235-06 | COVERED | Tasks 1–2 bind every measurement and both diagnoses to literal receipts. |
| CONTEXT | D-04 | Complete before/after ownership artifact | 235-06 | COVERED | Task 3 validates all 93 complete row semantics. |
| CONTEXT | D-05 | Consume Phase 234 inventory and reject silent ownership drift | 235-06 | COVERED | Task 3 uses the pinned inventory plus workflow/event receipts. |
| CONTEXT | D-06 | Contributor topology remains accurate | 235-06 | COVERED | Task 3 preserves and runs the existing contributor contract; no new documentation work is claimed. |
| CONTEXT | D-07 | Honest ledger-backed closeout and residual | 235-06 | COVERED | Tasks 1–2 preserve 772-second miss and validate existing residual references. |
| CONTEXT | D-08 | No pending todos folded in | 235-06 | COVERED | Only the existing ledger and focused contract are modified. |

Deferred ideas and unrelated Phase 230–234/product/release/UI work remain excluded. The authoritative verification report lists exactly three unresolved gaps, and Tasks 1–3 map one-to-one to them. The deterministic assumption-delta scan returned `detected:false`; no checkpoint is added. No package install, admin UI, schema file, schema-push action, new API, or manual UAT is present.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checked-in GitHub run-list bytes → terminal measurements | Untrusted evidence determines the FAST-01 population and verdict. |
| Checked-in run/job bytes → metrics replay → binding-pole diagnosis | Forged selection or metadata can misstate why the target missed. |
| Inventory/workflow/event receipts → ownership ledger | Valid-looking but false destinations can conceal silently dropped work. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-06-01 | Tampering | canonical source population | high | mitigate | Retain exact command output, pin its digest independently, decode a closed field set, and compare the complete bounded population field-for-field. |
| T-235-06-02 | Spoofing | retained run identity | high | mitigate | Require unique positive IDs, canonical repository URLs, fixed events, timestamps, conclusions, and head SHAs from digest-checked bytes. |
| T-235-06-03 | Tampering | median/max pole receipts | high | mitigate | Derive selections from source-bound durations and replay exact job bytes through the mandated metrics script with byte-equal output. |
| T-235-06-04 | Tampering | ownership destinations | high | mitigate | Compare all 93 complete semantic rows against pinned inventory, parsed workflow jobs/needs/guards, phase evidence, and retained event jobs. |
| T-235-06-05 | Repudiation | evidence provenance | medium | mitigate | Closed receipt schemas preserve exact commands, outputs, digests, run IDs, events, and phase paths with adversarial mutation tests. |
| T-235-06-06 | Information disclosure | GitHub receipts | low | accept | Store only public Actions metadata required for proof; exclude tokens, headers, logs, environment data, and artifact contents. |
| T-235-06-07 | Denial of service | GitHub evidence retrieval | low | mitigate | Perform one bounded read per fixed command, no watchers or sleeps, stop on 403/429, and execute all later checks offline. |

No application authentication, authorization, session, database, schema, network endpoint, or UI trust boundary changes. ASVS L1 V4 is satisfied by existing least-privilege read-only `gh` access; V5/V7/V12 are covered by closed schemas, exact paths, bounded inputs, digest validation, and fail-closed diagnostics.
</threat_model>

<verification>
Run the focused Phase 235 contract, the metrics shell contract, the Phase 234 inventory contract, the contributor-DX contract, and the complete planning-contract directory. All commands must exit 0 without network access after receipt capture. Confirm the ledger still contains exactly 19/2/2 source-bound runs, 93 ownership rows, and FAST-01 p50 772/status `miss`. Inspect the focused test names to confirm independent mutations exist for coherent population replacement, CR-01 wrong-pole selection, and live-but-wrong ownership values.
</verification>

<success_criteria>
- Canonical run-list bytes independently bind every retained run and the existing 772-second FAST-01 miss.
- The exact median and maximum pole receipts are deterministically selected and reproducible offline through `ci-run-metrics.sh`.
- All 93 ownership rows are fully proven against the Phase 234 inventory, live workflow semantics, Phase 232/233 provenance, and byte-retained event job receipts.
- No source code, workflow behavior, schema, UI, contributor documentation, requirement status, or residual scope changes.
- Every automated verification command passes and each of the three authoritative verification gaps has an adversarial regression test.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-06-SUMMARY.md` when done.
</output>
