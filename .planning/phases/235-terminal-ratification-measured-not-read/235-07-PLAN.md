---
phase: 235-terminal-ratification-measured-not-read
plan: 07
type: execute
wave: 7
depends_on: [235-06]
files_modified:
  - .github/workflows/terminal-ratification-evidence.yml
  - scripts/ci/capture-terminal-ratification-evidence.sh
  - scripts/ci/capture-terminal-ratification-evidence.test.sh
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
  - .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "D-01/D-02/D-03: a default-branch-only workflow captures the already-closed 2026-08-01T02:06:30Z through 2026-08-02T18:07:04Z CI window through an explicit bounded page=N workflow-runs REST loop, retains contiguous requested-page identities including a terminal empty page, and never changes the measured 19-run/772-second FAST-01 miss."
    - "The protected workflow signs the complete source-and-job receipt artifact with GitHub artifact attestation provenance; a checksum stored beside editable evidence is not accepted as provenance."
    - "Every captured run and job rejects an updated timestamp earlier than its created/started timestamp; inverted intervals are never converted to zero."
    - "GitHub collection is automation-only and obeys AGENTS.md: one rate-limit read before collection and a hard stop on 403/429 or core remaining at 250 or below; capture itself is finite REST data collection, not CI polling, and adds no watcher. Plan 08 alone owns its one 60-second workflow-run watcher, one structured summary, and failure-only log fetch."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED FROM D-02 AND REQUIREMENTS: strict p50_seconds < 720 applies; exactly 720 seconds is a miss."
    - requirement_id: FAST-01
      category: empty
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED FOR THIS CONTRACT: null and empty populations fail closed; one through nine eligible PR runs recompute deterministically but cannot pass the minimum-count gate."
    - requirement_id: FAST-01
      category: ordering
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED FOR THIS CONTRACT: duration ordering is stable by {wall_seconds, run_id}; equal durations therefore select the same floor(n/2) row and binding pole offline."
    - requirement_id: GATE-05
      category: unclassified
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED BY PLAN-TIME REVIEW: deterministic acceptance requires the exact 93 ownership keys plus ci.yml event eligibility and a protected retained job receipt for every executed owner; labels alone are insufficient."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT represent FAST-01 as achieved from fewer than ten eligible post-cutoff PR runs, a p50 at or above 720 seconds, filtered conclusions, or substituted timing semantics."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present coverage as complete while omitting an affected spec or suite, naming only a terminal aggregate, or lacking a receiver and execution receipt for moved work."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
  artifacts:
    - path: .github/workflows/terminal-ratification-evidence.yml
      provides: "protected main-ref capture, attestation, and upload path that does not join the CI required-check DAG"
    - path: scripts/ci/capture-terminal-ratification-evidence.sh
      provides: "bounded paginated GitHub workflow-run/job receipt collector with fail-closed chronology and exhaustion checks"
    - path: scripts/ci/capture-terminal-ratification-evidence.test.sh
      provides: "offline fake-gh fixtures for one-page, multi-page, exact-100, terminal-empty, duplicate/missing-page, bound-hit, 403/429, event-job, and inverted-time collection paths"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md
      provides: "GitHub REST/CLI capability and deterministic verification coverage declaration"
  key_links:
    - from: .github/workflows/terminal-ratification-evidence.yml
      to: scripts/ci/capture-terminal-ratification-evidence.sh
      via: "main-only workflow_dispatch job with actions:read invokes the bounded capture and passes no caller-controlled window"
    - from: scripts/ci/capture-terminal-ratification-evidence.sh
      to: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373
      via: "complete canonical receipt JSON becomes the attestation subject before upload"
    - from: scripts/ci/capture-terminal-ratification-evidence.test.sh
      to: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      via: "shell fixtures prove capture behavior; ExUnit locks workflow identity, permissions, immutable action pins, and no CI-DAG coupling"
---

<objective>
Create the protected provenance path that can authenticate Phase 235's already-closed run and job evidence outside the editable ledger contract.

Purpose: close the capped-population, self-authentication, and inverted-interval root causes before any terminal status can change.
Output: a default-branch-only attested capture workflow, its hermetic collector/tests, and GitHub API coverage evidence.
</objective>

<execution_context>
@/Users/developer/.codex/gsd-core/workflows/execute-plan.md
@/Users/developer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-REVIEW.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-06-SUMMARY.md

<interfaces>
GitHub workflow-runs API: `GET /repos/szTheory/sigra/actions/workflows/ci.yml/runs?created=2026-08-01T02:06:30Z..2026-08-02T18:07:04Z&per_page=100&page=N`; response carries `total_count` and `workflow_runs`, with REST pagination proving exhaustion.
GitHub job API: `GET /repos/szTheory/sigra/actions/runs/{run_id}/jobs?per_page=100&page=N`; response carries `total_count` and `jobs`.
Offline provenance verification contract: `gh attestation verify ARTIFACT -R szTheory/sigra --bundle BUNDLE --custom-trusted-root TRUSTED_ROOT --signer-workflow github.com/szTheory/sigra/.github/workflows/terminal-ratification-evidence.yml --source-ref refs/heads/main --format json`.
Pinned actions: `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1`, `actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373`, `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one protected bounded receipt from GitHub API through attestation</name>
  <files>.github/workflows/terminal-ratification-evidence.yml, scripts/ci/capture-terminal-ratification-evidence.sh, scripts/ci/capture-terminal-ratification-evidence.test.sh, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md` — mapped repository analogs for the Phase 235 contract test and evidence artifact.
    - `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — fail-closed exact-universe and executable-row validation analog for extending the Phase 235 contract test.
    - `.github/workflows/ci-observe.yml` — default-branch `workflow_run` precedent, least-privilege actions reads, and explanation of why terminal receipts cannot be produced inside ci.yml.
    - `.github/workflows/ci.yml` — live workflow name/events and job IDs; this plan must not add a required job or alter this DAG.
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — D-01/D-02 wall-mode semantics and repository shell-test style.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — immutable cutoff/endpoint, retained run IDs, binding-pole IDs, and ownership event receipt IDs; use these as the fixed historical window, not as an authenticity anchor.
    - `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — current source/job validators, `event_job_executed?/2`, and timestamp clamping defects named by verification.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` and `235-REVIEW.md` — all four blocker behaviors.
    - `AGENTS.md` — rate-limit, watcher, retry, and structured-summary constraints.
  </read_first>
  <behavior>
    - "A fixture with two non-empty workflow-run pages followed by an empty response produces one canonical artifact with data_page_count 2, requested_pages [1,2,3], exact invariant API total, explicit exhausted true, and the later run retained."
    - "One through 99 records require a following empty page; exactly 100 records require page 2 to be requested and empty; every success manifest retains each requested page number once, contiguously from 1 through the terminal empty page."
    - "A missing or duplicate manifest page, non-contiguous request sequence, total_count change or disagreement, duplicate run ID, non-empty safety-bound page, absent terminal empty page, or run outside the fixed closed interval fails with a named diagnostic and leaves no canonical success artifact."
    - "The hermetic fake-gh suite executes the same production collector and manifest validator for one-page, multi-page, exact-100, terminal-empty, duplicate/missing-page, safety-bound, and HTTP 403/429 cases; it does not reproduce pagination logic in a test-only collector."
    - "An inverted run createdAt/updatedAt or job startedAt/completedAt pair fails before any wall duration is computed."
    - "The workflow runs only by workflow_dispatch on refs/heads/main, grants contents:read/actions:read/id-token:write/attestations:write, invokes the collector, attests its exact JSON subject, and uploads it; it is absent from ci.yml and every required aggregate needs list."
    - "The collector retains the complete bounded workflow-run population and paginated job responses for the fixed pull_request, push, and schedule receipt runs without filtering conclusions."
  </behavior>
  <action>
    Start RED with a hermetic fake-`gh` PATH fixture that records the exact `page=N` arguments and serves workflow-run and job responses to the production executable. Cover zero-to-99 records plus the required following empty page, multiple non-empty pages plus the required following empty page, exactly 100 records plus an empty page 2, a malformed retained manifest with a missing page number, one with a duplicate page number, total-count disagreement, duplicate item IDs, the safety bound reached while still non-empty, HTTP 403, HTTP 429, and inverted run/job intervals. The duplicate/missing-page fixtures must enter the same production manifest validator used immediately before canonical emission; do not copy that logic into the test. Add an ExUnit workflow-contract test that initially fails because the protected workflow does not exist.

    Implement `capture-terminal-ratification-evidence.sh` per D-01/D-02/D-03 with fixed constants `szTheory/sigra`, workflow `ci.yml`, cutoff `2026-08-01T02:06:30Z`, and endpoint `2026-08-02T18:07:04Z`; accept an output path only, not caller-selected bounds. Do not use `gh api --paginate` or infer exhaustion from a short/full data page. Implement one reusable production `page=N` collector for workflow runs and jobs: request `per_page=100&page=1`, increment exactly by one, and append an envelope containing the collector-supplied requested page number and sanitized JSON body to a private temporary manifest. Continue until a requested page returns an empty item array, including when the preceding page contains fewer than 100 or exactly 100 records. The empty response is the terminal manifest envelope and the only successful exhaustion proof.

    Set an internal, non-configurable `MAX_PAGES=10000` safety circuit for each resource. After the first response, require a nonnegative integer `total_count`, compute whether `ceil(total_count / 100) + 1` can fit within that bound, and fail immediately if it cannot. If page 10000 is non-empty, fail loudly as `pagination_bound_reached`; never interpret the bound as exhaustion or truncate to a successful population. On every API failure, delete the temporary manifest and output candidate; on HTTP 403 or 429, stop immediately without retry in accordance with AGENTS.md. Perform the existing rate-limit preflight once; core remaining at 250 or below is also a blocking stop. This capture is bounded data collection rather than CI-run polling, so it creates no watcher and performs no summary/log polling.

    Before emitting the canonical output, validate the retained manifest itself: page identities occur exactly once and equal the contiguous sequence 1..terminal_page; only the final envelope has an empty item array; every response reports the same `total_count`; the sum of all non-terminal item counts equals that total; and item IDs are unique. A missing/duplicate page, changed total, count mismatch, duplicate ID, malformed envelope, absent empty terminal page, or any command failure removes the output candidate and emits a named nonzero diagnostic. Retain `requested_pages`, `data_page_count`, `terminal_page`, and `exhausted: true` in canonical sorted output so the proof survives beyond process logs. Apply this same collector and validation path to jobs for the three ledger ownership receipt run IDs and two binding-pole run IDs, deduplicating repeated run IDs before collection. Preserve all conclusions and public metadata; reject missing terminal timestamps, empty conclusion, malformed identity, and every negative interval before deriving seconds. Never echo `GH_TOKEN` or retain response headers containing authentication data.

    Create `.github/workflows/terminal-ratification-evidence.yml` as a separate `workflow_dispatch` evidence workflow, not a ci.yml job. Gate the only job to `refs/heads/main`, use GitHub-hosted Ubuntu with a bounded timeout, least-privilege `contents: read`, `actions: read`, `id-token: write`, and `attestations: write`, and pin checkout, attestation, and upload actions to the SHAs in `<interfaces>`. Invoke the collector with `GH_TOKEN: ${{ github.token }}`, attest the exact canonical JSON with `actions/attest-build-provenance`, then upload that same file. Do not add inputs, pull_request execution, secrets, write permissions beyond attestation, CI required contexts, retries, or changes to `.github/workflows/ci.yml`.

    Extend the focused ExUnit contract using `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` as the fail-closed exact-universe and executable-row validation analog: require the exact workflow trigger/ref guard/permissions/action SHAs/collector command and prove the workflow is not named by ci.yml aggregates. Keep the ledger's 19/772 miss unchanged in this task; this tracer proves the protected route and one complete fixture path, while the real default-branch receipt is captured only after this workflow lands on main.
  </action>
  <reversibility rating="costly">Removing the evidence workflow after it signs receipts is code-reversible, but would strand the provenance replay path and requires coordinated evidence replacement.</reversibility>
  <verify>
    <automated>bash scripts/ci/capture-terminal-ratification-evidence.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The shell test drives the production executable and its production manifest validator through fake gh for one non-empty page, multiple non-empty pages, exactly 100 records, a required terminal empty page, duplicate and missing manifest pages, invariant/accurate total_count, duplicate IDs, safety-bound failure, HTTP 403/429 hard stops, and run/job chronology rejection without network access.
    - Every successful resource manifest records contiguous requested page identities including exactly one final empty page; reaching MAX_PAGES without that page fails nonzero and retains no canonical success artifact.
    - The workflow is main-only, least-privilege, immutable-action-pinned, produces and attests one canonical receipt artifact, and has no edge to ci.yml or a protected aggregate.
    - The collector's bounds are the existing closed Phase 235 window and cannot be overridden by dispatch input.
    - The existing ledger still reports 19 eligible PR runs, p50 772, and status miss.
  </acceptance_criteria>
  <done>One production protected path can capture a complete closed-window receipt and cryptographically bind its bytes to the main-only GitHub workflow identity.</done>
</task>

<task type="auto">
  <name>Task 2: Record GitHub API and provenance verification coverage</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md</files>
  <read_first>
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-COVERAGE.md` — project coverage-artifact shape and capability declarations.
    - `.github/workflows/terminal-ratification-evidence.yml` and `scripts/ci/capture-terminal-ratification-evidence.sh` — actual API calls, auth boundary, outputs, and attestation identity created by Task 1.
    - `AGENTS.md` — the mandatory GitHub API operational controls.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-01 through D-08 and deferred exclusions.
  </read_first>
  <action>
    Create `235-COVERAGE.md` because this closure adds direct GitHub REST collection and attestation verification. Declare `frontend=false`, `schema=false`, `product_api=false`, and `external_evidence_api=true`. Inventory the workflow-runs pagination call, per-run jobs pagination call, rate-limit preflight, workflow dispatch/run-summary/log calls used by Plan 08, artifact download, attestation download, trusted-root acquisition, and offline attestation verification. For each capability name the authenticated actor, request shape, retained sanitized fields, hermetic fixture or contract test, failure behavior, and AGENTS.md rate-limit/watcher rule. State that 403/429, core remaining at or below 250, incomplete pagination, failed attestation, signer/ref mismatch, or chronology inversion is a blocking failure and never a passed evidence row.

    Include the multi-source audit below, the four resolved-but-flagged spec-less probes from `must_haves.assumptions`, and the two descriptor-less flagged-unverified prohibitions without inventing new prohibition descriptors. Record D-08 exclusions: no re-audit, new gate, product/UI/release work, test deletion, timeout change, retry masking, schema change, or unrelated todo. Cite the official GitHub workflow-runs pagination and offline attestation commands as implementation references, while keeping repository sources authoritative for project semantics.
  </action>
  <verify>
    <automated>test -s .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md &amp;&amp; grep -q 'external_evidence_api=true' .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md &amp;&amp; grep -q '403/429' .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md &amp;&amp; grep -q 'offline attestation' .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md accounts for every GitHub REST/CLI/attestation operation used by Plans 07–08 and maps each to deterministic evidence.
    - Rate-limit, watcher, retry, sanitization, provenance, and hard-stop rules agree exactly with AGENTS.md and the workflow/script.
    - All four source types and all gap-contract defects are mapped; deferred items remain explicitly excluded.
  </acceptance_criteria>
  <done>The phase has a machine-oriented coverage declaration for every new external evidence capability and its fail-closed test seam.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/terminal-ratification-evidence.yml` — new default-branch-only, manually dispatched provenance workflow; no public product API and no CI required-check relationship.
- `scripts/ci/capture-terminal-ratification-evidence.sh` — new internal command `capture-terminal-ratification-evidence.sh OUTPUT_PATH`; its time bounds, repository, and workflow are fixed constants.
- `scripts/ci/capture-terminal-ratification-evidence.test.sh` — new hermetic shell contract with fake GitHub API pages and job payloads.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — extends the existing internal planning contract with workflow/provenance structure checks; no runtime API.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md` — new external-evidence API coverage and source-audit artifact.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from run data | 235-07→08 | COVERED | This plan establishes protected provenance; Plan 08 consumes it offline. |
| REQ | FAST-01 | Under-12m p50 over at least 10 runs | 235-07 | COVERED | Locks complete population/chronology while preserving the measured miss. |
| REQ | GATE-05 | One artifact proves no silent drop | 235-07 | COVERED | Defines protected job-receipt source for real per-event proof. |
| RESEARCH | — | Immutable-window wall-mode measurement | 235-07 | COVERED | Fixed closed bounds and no conclusion filtering. |
| RESEARCH | — | Fail-closed receipt provenance | 235-07 | COVERED | Protected attestation replaces same-file digest trust. |
| CONTEXT | D-01–D-03 | Measurement window, semantics, exact claims | 235-07 | COVERED | Collector and fixtures implement all three. |
| CONTEXT | D-04–D-05 | Complete ownership with real receipts | 235-07→08 | COVERED | Captures protected event jobs; Plan 08 reconciles all rows. |
| CONTEXT | D-06–D-07 | Contributor/closeout honesty | 235-08 | COVERED | Existing correct docs remain; final status update follows proof. |
| CONTEXT | D-08 | No unrelated todo or remediation | 235-07 | COVERED | Workflow is evidence-only and outside ci.yml DAG. |
| VERIFICATION | CR-02 | Complete pagination/exhaustion | 235-07 | COVERED | Task 1 shell fixtures and collector. |
| VERIFICATION | CR-03 | External/protected provenance | 235-07 | COVERED | Main-only GitHub artifact attestation. |
| VERIFICATION | CR-04 | Reject inverted timestamps | 235-07 | COVERED | Run and job chronology fail before computation. |

Deferred ideas and out-of-phase work are excluded. The four spec-less probes are resolved deterministically but remain visibly flagged; both descriptor-less prohibitions remain flagged-unverified. No package-manager install or schema operation occurs. Admin UI is out of scope.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub REST API → protected capture workflow | Remote run/job metadata enters the provenance builder through authenticated read-only requests. |
| Protected main workflow → attested artifact | The signer identity, source ref, and subject digest must be outside the editable ledger contract. |
| Attested artifact → local Phase 235 verifier | Later offline verification must reject altered bytes, signer/ref changes, incomplete pages, and invalid chronology. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-07-01 | Tampering | paginated run/job population | high | mitigate | Preserve every API page, total count, page count, explicit exhaustion, unique IDs, and hermetic omitted-page/cap-hit mutations. |
| T-235-07-02 | Spoofing | receipt provenance | high | mitigate | GitHub attestation must bind subject bytes to the exact signer workflow, `szTheory/sigra`, and `refs/heads/main`; local hashes are secondary only. |
| T-235-07-03 | Tampering | run/job timestamps | high | mitigate | Reject updated/completed before created/started in collector fixtures and downstream contract before durations are derived. |
| T-235-07-04 | Information disclosure | API and workflow output | high | mitigate | Retain public IDs/timestamps/conclusions/job names only; never echo tokens, authorization headers, cookies, or raw authenticated state. |
| T-235-07-05 | Denial of service | GitHub API collection | medium | mitigate | Use bounded REST collection only, with one rate-limit preflight and a hard stop on 403/429 or low core budget per AGENTS.md; Plan 08 alone owns the single 60-second workflow-run watcher, structured summary, and failure-only log retrieval. |
| T-235-07-SC | Tampering | pinned GitHub actions | high | mitigate | Use verified first-party actions pinned to immutable commit SHAs; no package-manager install is introduced. |

## ASVS L1 Automated Controls

- V4 access control: the workflow runs only on main and grants read scopes plus the two attestation-write scopes.
- V5 validation: page shape/count/exhaustion, identity, event, conclusion, and chronology fail closed before output or attestation.
</threat_model>

<verification>
Run the hermetic capture test and focused ExUnit contract. Confirm the ledger remains the honest 19-run/772-second miss and `.github/workflows/ci.yml` is byte-unchanged.
</verification>

<success_criteria>
The repository contains a tested main-only workflow capable of producing a complete, externally signed receipt for the fixed Phase 235 window, with no CI gate or product behavior change. The 235-07 summary records the committed workflow blob ID for the Plan 08 protected-main handoff; Plan 08 remains blocked until a maintainer confirms that commit was merged and its executor independently matches the exact blob on `origin/main`.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-07-SUMMARY.md` when done, recording `git hash-object .github/workflows/terminal-ratification-evidence.yml` and the commit containing that blob for the protected-main merge checkpoint. Do not claim or perform the protected merge as part of this plan.
</output>
