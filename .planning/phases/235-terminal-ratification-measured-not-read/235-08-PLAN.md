---
phase: 235-terminal-ratification-measured-not-read
plan: 08
type: execute
wave: 8
depends_on: [235-07]
files_modified:
  - scripts/ci/correlate-terminal-ratification-dispatch.sh
  - scripts/ci/verify-terminal-ratification-attestation-offline.sh
  - .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.attestation.jsonl
  - .planning/phases/235-terminal-ratification-measured-not-read/235-TRUSTED-ROOT.jsonl
  - .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "Plan 08 dispatches nothing until an invocation-authorized scoped PR has passed every protected-main rule, auto-merged without bypass, and the executor has fetched origin/main and matched its exact Git blob to the Plan 07 handoff blob."
    - "The dispatched run is selected by a bounded pre/post set difference for the exact evidence workflow, event workflow_dispatch, protected-main SHA, and UTC dispatch-time boundary; zero, multiple, stale, wrong-workflow, or wrong-SHA candidates fail before the sole watcher starts."
    - "The protected workflow's exact receipt artifact, attestation bundle, and contemporaneous Sigstore/GitHub trusted root are retained and pass deterministic offline verification for repository szTheory/sigra, signer workflow terminal-ratification-evidence.yml, source ref refs/heads/main, and the attested source digest."
    - "D-01/D-02/D-03: the attested complete population reproduces exactly 19 pull_request runs and p50 772 seconds under queue-inclusive wall semantics, so FAST-01 remains an owned miss and REQUIREMENTS.md remains Gaps Found for FAST-01."
    - "D-04/D-05: every one of the 93 ownership rows proves ci.yml event eligibility and an attested retained job that is present and terminal/non-skipped when state is executed; push and schedule unavailable-owner mutations fail."
    - "GATE-05 changes to Complete only after the exact ownership universe, real event eligibility, protected receipts, and offline attestation all validate together; no claim is advanced from labels or local digests."
    - "No human UAT, sleep, duplicate watcher, rate-limit retry, synthetic CI result, or unrelated product/CI remediation is used."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: exactly 720 seconds remains a miss under the strict comparator."
    - requirement_id: FAST-01
      category: empty
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: null/empty evidence is invalid and fewer than ten PR runs cannot pass."
    - requirement_id: FAST-01
      category: ordering
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: equal durations are ordered by run ID before floor(n/2) selection."
    - requirement_id: GATE-05
      category: unclassified
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED BY PLAN-TIME REVIEW: exact keys plus workflow eligibility plus protected terminal job receipt is the deterministic acceptance rule."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT represent FAST-01 as achieved from fewer than ten eligible post-cutoff PR runs, a p50 at or above 720 seconds, filtered conclusions, or substituted timing semantics."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present coverage as complete while omitting an affected spec or suite, naming only a terminal aggregate, or lacking a receiver and execution receipt for moved work."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
  artifacts:
    - path: scripts/ci/correlate-terminal-ratification-dispatch.sh
      provides: "production dispatch-run selector with hermetic zero/one/multiple and identity-boundary contract cases"
    - path: scripts/ci/verify-terminal-ratification-attestation-offline.sh
      provides: "single fail-closed Darwin/Linux network-isolation harness for positive and adversarial offline attestation verification"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.json
      provides: "complete protected workflow-run/job population for the fixed Phase 235 window"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.attestation.jsonl
      provides: "GitHub/Sigstore provenance bundle for the exact receipt subject"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-TRUSTED-ROOT.jsonl
      provides: "contemporaneous trusted material for deterministic offline attestation verification"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      provides: "single terminal ledger linked to attested complete population and real event-owner proof"
    - path: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      provides: "offline provenance, complete-source, chronology, event-eligibility, receipt, and status contract"
  key_links:
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-07-SUMMARY.md
      to: origin/main:.github/workflows/terminal-ratification-evidence.yml
      via: "invocation-authorized scoped PR, protected auto-merge, and exact Git blob-ID equality before dispatch"
    - from: gh workflow dispatch
      to: gh run watch
      via: "bounded workflow-specific pre/post run projections and an exact one-ID set difference constrained by workflow_dispatch, protected-main SHA, and UTC dispatch boundary"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.attestation.jsonl
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.json
      via: "gh attestation verify offline subject digest plus signer/repository/source-ref policy"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.json
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      via: "exact complete bounded run map and per-event job responses replace editable embedded receipt trust"
    - from: .github/workflows/ci.yml
      to: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      via: "parsed workflow trigger/job guards determine eligibility; attested jobs prove actual execution"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      to: .planning/REQUIREMENTS.md
      via: "FAST-01 stays Gaps Found at 772 seconds while GATE-05 becomes Complete only after the unified contract passes"
---

<objective>
Capture the protected evidence on main, verify it offline, and close the ownership proof without changing the honest FAST-01 miss.

Purpose: replace self-authenticated/capped evidence and no-op event checks with complete protected provenance and real workflow/job execution proof.
Output: retained attestation materials, a provenance-linked terminal ledger, adversarial contract tests, and accurate requirement statuses.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-REVIEW.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-07-SUMMARY.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md

<interfaces>
Protected workflow dispatch: `gh workflow run terminal-ratification-evidence.yml --repo szTheory/sigra --ref main`.
Dispatch correlation: resolve the workflow numeric ID once, snapshot `GET /repos/szTheory/sigra/actions/workflows/{workflow_id}/runs?event=workflow_dispatch&amp;branch=main&amp;head_sha={protected_main_sha}&amp;per_page=100&amp;page=1` immediately before dispatch, record the UTC not-before boundary, query that same bounded endpoint once after dispatch, then pass the sanitized projections to `scripts/ci/correlate-terminal-ratification-dispatch.sh`; only one new matching run ID is valid.
Single watcher: `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`.
Offline verification: `gh attestation verify 235-PROTECTED-RECEIPTS.json -R szTheory/sigra --bundle 235-PROTECTED-RECEIPTS.attestation.jsonl --custom-trusted-root 235-TRUSTED-ROOT.jsonl --signer-workflow github.com/szTheory/sigra/.github/workflows/terminal-ratification-evidence.yml --source-ref refs/heads/main --format json`.
Offline harness: `bash scripts/ci/verify-terminal-ratification-attestation-offline.sh`; it stages only the retained receipt, bundle, and trusted-root files into a temporary directory, clears GitHub credentials, and runs every positive/negative `gh attestation verify` case behind Darwin `sandbox-exec` network denial or a Linux `unshare --user --map-root-user --net` network namespace. It fails closed when the platform primitive cannot establish isolation.
Current ledger verdict: `verdict.fast_01 = {status: miss, eligible_pr_run_count: 19, observed_p50_seconds: 772, threshold_seconds: 720, comparator: lt}`; these values are immutable outcomes unless protected replay disproves the current artifact, in which case execution blocks with diagnostics rather than inventing a new closeout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the protected evidence workflow through the main ruleset</name>
  <action>Create or reuse the scoped `ci/phase-235-protected-evidence` branch from current `origin/main`, containing only `.github/workflows/terminal-ratification-evidence.yml`, `scripts/ci/capture-terminal-ratification-evidence.sh`, and `scripts/ci/capture-terminal-ratification-evidence.test.sh`. The user's explicit invocation authorizes branch push, PR creation, and squash auto-merge after every active protected-main requirement succeeds. Never approve the PR, use `--admin`, bypass a required check, or dispatch from the evidence branch.</action>
  <instructions>Before any push, run `actionlint .github/workflows/terminal-ratification-evidence.yml` and `bash scripts/ci/capture-terminal-ratification-evidence.test.sh` in a clean worktree. If `origin/main` already contains the exact handoff blob, skip PR creation idempotently. Otherwise fail closed if the named remote branch or open PR exists with a different patch, push without force, create the scoped PR, enable squash auto-merge with `--match-head-commit`, and identify its CI run by exact pull-request head SHA and event. Before the sole watcher, require REST core remaining above 250; watch once with the repository-mandated 60-second interval, fetch one structured summary after completion, and fetch failed logs only on failure. Retry one non-rate-limit transient failure once; never retry 403/429 before reset. After auto-merge, fetch `origin main`, discover the merge SHA from the merged PR, verify it is reachable from `origin/main`, and prove `git rev-parse origin/main:.github/workflows/terminal-ratification-evidence.yml` equals the workflow blob ID recorded in `235-07-SUMMARY.md`.</instructions>
  <verification>The PR is merged by the active main ruleset with all required checks successful, its merge SHA is reachable from `origin/main`, and the protected-main workflow blob is exactly `dcb7a8d051e14f58a8e49ec7385dad291aa315b3` before Task 2 dispatches.</verification>
</task>

<task type="auto">
  <name>Task 2: Capture and retain the protected main attestation bundle</name>
  <files>scripts/ci/correlate-terminal-ratification-dispatch.sh, scripts/ci/verify-terminal-ratification-attestation-offline.sh, .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.json, .planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.attestation.jsonl, .planning/phases/235-terminal-ratification-measured-not-read/235-TRUSTED-ROOT.jsonl</files>
  <precondition>The blocking maintainer checkpoint supplied a merge commit SHA; `git fetch origin main` succeeds; that SHA is an ancestor of `origin/main`; `git rev-parse origin/main:.github/workflows/terminal-ratification-evidence.yml` exactly equals the workflow blob ID recorded in `235-07-SUMMARY.md`; `gh auth status` succeeds for `szTheory/sigra`; and one `gh api rate_limit` read reports `.resources.core.remaining > 250`. If any predicate fails, halt with durable diagnostics and do not dispatch or substitute local evidence.</precondition>
  <read_first>
    - `AGENTS.md` — exact watcher, structured-summary, failed-log, retry, and hard-stop rules.
    - `.github/workflows/terminal-ratification-evidence.yml` — dispatched protected workflow identity, output artifact name, and signer permissions.
    - `scripts/ci/capture-terminal-ratification-evidence.sh` — canonical receipt schema and fixed historical bounds.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md` — complete API call/test inventory and blocking outcomes.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — expected immutable 19/2/2 partition, 772-second p50, ownership run IDs, and binding-pole run IDs.
  </read_first>
  <action>
    Assert the precondition before any external write. Resolve the numeric workflow ID from `GET /repos/szTheory/sigra/actions/workflows/terminal-ratification-evidence.yml` and the expected protected-main SHA from `git rev-parse origin/main`. Immediately before dispatch, make one bounded REST query to `GET /repos/szTheory/sigra/actions/workflows/{workflow_id}/runs?event=workflow_dispatch&amp;branch=main&amp;head_sha={protected_main_sha}&amp;per_page=100&amp;page=1`; require `total_count &lt;= 100`, reject any returned row whose `workflow_id`, `event`, or `head_sha` differs, and persist only the pre-dispatch `{id,html_url,workflow_id,event,head_sha,created_at}` projection in temporary state. Record `dispatch_not_before` in UTC immediately after that snapshot and before invoking exactly one `gh workflow run terminal-ratification-evidence.yml --repo szTheory/sigra --ref main`.

    After dispatch, make one same-endpoint, same-filter, same-bound REST query, with the single permitted non-rate-limit transient retry only if that one-shot query fails; this bounded discovery is not a watcher. Implement `scripts/ci/correlate-terminal-ratification-dispatch.sh` as the production selector used here: validate both projections, compute `post_ids - pre_ids`, then retain candidates only when `workflow_id` equals the resolved workflow ID, `event` is `workflow_dispatch`, `head_sha` equals the exact protected-main SHA, `created_at` is at or after `dispatch_not_before`, and both ID and `html_url` are present. Require exactly one candidate. Fail closed with a machine-readable diagnostic on zero or multiple candidates, `total_count &gt; 100`, an older candidate, or any wrong-workflow/wrong-event/wrong-SHA row; never choose newest-by-time as a fallback. Atomically write the selected `{id,html_url,workflow_id,event,head_sha,created_at,dispatch_not_before}` to a private `mktemp` JSON work record before starting the sole `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`, and read the watcher ID from that record rather than recomputing it. Retain its ID and URL in the terminal ledger during Task 3, then remove the private work record. The two bounded discovery queries are not watchers; do not start another watcher or poll in parallel.

    Deterministic recovery is bounded to three total dispatch attempts. If the first dispatch succeeds externally but a selector defect prevents the work record from being written before any watcher starts, retain that run ID as a `superseded_dispatch` with the named tooling failure, fix the selector under RED→GREEN self-tests, and take a fresh pre-snapshot that includes the superseded run before dispatching one replacement. If that replacement is correlated and watched but the protected producer fails before creating any receipt or attestation, retain its run ID and failure diagnostic as a second `superseded_dispatch`; fix the producer under a hermetic RED→GREEN regression, merge that fix through the same protected-main PR process, and permit one final dispatch against the new protected-main SHA. Every fresh post-minus-pre difference must contain exactly one candidate. Never reconstruct a lost boundary, accept evidence from a superseded run, dispatch more than three times total, or retry an attested/artifact-producing run merely to obtain a preferred result.

    Give the production selector a hermetic self-test mode using fixture projections that exercises zero, exactly one, and multiple set-difference candidates plus stale, wrong-workflow, wrong-event, and wrong-SHA rows. The exactly-one case must emit the expected ID and URL; every other case must exit nonzero with its named diagnostic. After the sole watcher completes, fetch the structured summary once; fetch failed logs once only if conclusion is failure. Retry one non-rate-limit transient operation at most once. On HTTP 403/429, core remaining at or below 250, workflow failure, missing artifact, or signer permission failure, stop until the reported reset/retry condition with a durable machine-readable diagnostic; do not treat a partial artifact as evidence.

    Download the workflow's canonical receipt artifact to `235-PROTECTED-RECEIPTS.json`. Download its attestation bundle with `gh attestation download`, naming the retained result `235-PROTECTED-RECEIPTS.attestation.jsonl`, and capture a contemporaneous official trusted root with `gh attestation trusted-root` as `235-TRUSTED-ROOT.jsonl`. Do not retain `GH_TOKEN`, headers, cookies, raw log archives, or unrelated workflow artifacts.

    Create `scripts/ci/verify-terminal-ratification-attestation-offline.sh` as the sole attestation acceptance command. Require `gh`, `jq`, `mktemp`, and exactly one supported network-denial primitive: `/usr/bin/sandbox-exec` on Darwin (present in the planning workspace) with `(deny network*)`, or Linux `unshare --user --map-root-user --net` (the repository's workflows run on Ubuntu) after a probe proves that namespace creation succeeds. Fail closed rather than running `gh` normally when neither isolation path works. Copy only the three retained inputs into a fresh `mktemp -d`, use a fresh empty `HOME`, unset `GH_TOKEN`, `GITHUB_TOKEN`, and proxy/config variables, and run the exact repository/signer/main-ref verify command inside the network-denied process. Assert the positive JSON binds the subject digest and certificate identity. In that same isolated harness independently require failure after (1) changing one receipt byte, (2) changing one bundle byte, (3) changing one trusted-root byte, (4) substituting a different signer workflow, and (5) substituting `refs/heads/not-main`. Restore or recreate pristine temporary inputs between cases; never mutate the retained files.

    Validate the artifact schema locally: complete contiguous pages, exact totals, explicit exhaustion, unique identities, closed bounds, all conclusions, all required run/job receipts, and chronological run/job intervals. Compare its recomputed event partitions against the existing ledger; require 19 PR, 2 push, 2 schedule and p50 772. If protected evidence disagrees, halt and report the exact mismatch without editing the ledger, requirements, residual, SEED-005, or CI-PERF.
  </action>
  <reversibility rating="costly">The external attestation is append-only provenance; local retained copies are replaceable only by another independently signed main-workflow capture.</reversibility>
  <verify>
    <automated>test "$(git rev-parse origin/main:.github/workflows/terminal-ratification-evidence.yml)" = "$(git hash-object .github/workflows/terminal-ratification-evidence.yml)" &amp;&amp; bash scripts/ci/correlate-terminal-ratification-dispatch.sh --self-test &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; bash scripts/ci/capture-terminal-ratification-evidence.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Bounded pre/post projections for the exact workflow ID, workflow_dispatch event, protected-main SHA, and UTC dispatch boundary yield exactly one new run; the selected ID and URL are persisted before exactly one 60-second watcher observes it.
    - The production selector's hermetic fixtures pass the exactly-one outcome and reject zero, multiple, stale, wrong-workflow, wrong-event, and wrong-SHA outcomes; discovery queries remain bounded one-shot calls, not watchers.
    - The maintainer-confirmed merge SHA is on origin/main and the dispatched workflow's exact Git blob matches the Plan 07 handoff; branch-local presence is insufficient.
    - The network-denied harness binds the exact receipt bytes to `szTheory/sigra`, the exact signer workflow, and `refs/heads/main`; altered receipt, bundle, trusted root, signer, and source ref each fail under the same isolation boundary.
    - The protected replay proves explicit pagination exhaustion and the exact preserved 19/2/2, 772-second miss with no inverted intervals.
    - No token, authenticated state, or unrelated artifact is committed.
  </acceptance_criteria>
  <done>The complete historical population and job receipts have protected external provenance that is reproducible offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile real event eligibility and requirements to the protected receipts</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs, .planning/REQUIREMENTS.md</files>
  <read_first>
    - All three files in `<files>` — current embedded receipt/digest contract, no-op event predicate, honest residual/status wording, and authoritative requirement checkboxes/traceability.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md` — authoritative mapping from each Phase 235 edit target to its repository analog.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — single-document evidence ledger analog for `235-TERMINAL-RATIFICATION.json`.
    - `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — fail-closed exact-universe, executable-row, and in-memory mutation analog for the Phase 235 contract test.
    - The three protected evidence files from Task 1 — sole authenticity source for population and job receipts.
    - `.github/workflows/ci.yml` — workflow events, every direct-owner job block, job-level `if` guards, aggregate `needs`, and display names.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — exact current Playwright ownership input required by D-05.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — expected gap closure and FAST-01 status ruling.
    - `.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md`, `.planning/seeds/SEED-005-ci-cd-pipeline-performance-audit.md`, and `.planning/MILESTONE-ARC.md` — already-correct 772-second miss/residual wording that must remain unchanged.
  </read_first>
  <behavior>
    - "The ledger no longer embeds an authoritative source/job digest beside editable bytes; it points to the protected artifact, attestation bundle, trusted root, signer workflow, source ref, and subject digest validated offline."
    - "For every executed ownership row, ci.yml proves the direct owner is eligible for that row's event and the corresponding protected event receipt contains exactly one terminal, non-skipped matching job; aggregate identity/needs are also checked."
    - "For every intentionally_absent row, ci.yml proves the owner is ineligible and the protected event receipt contains no executing matching job."
    - "Removing or skipping an expected owner from push or schedule, changing a job guard, swapping a receipt run, changing signer/ref/digest, omitting a later page, or inverting either ledger or source timestamps fails with a named diagnostic."
    - "FAST-01 remains unchecked and Gaps Found at 19 runs/772 seconds; GATE-05 becomes checked and Complete only when the unified protected proof passes."
  </behavior>
  <action>
    Start RED by replacing the unconditional `event_job_executed?/2` cases with table-driven failure expectations before implementation. Per `235-PATTERNS.md`, use `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` as the single-document terminal-ledger analog and `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` as the fail-closed exact-universe, executable-row, and in-memory mutation analog for the contract test. Mutate one push owner and one schedule owner to absent/skipped in the protected receipt, make `admin_eval_render` eligible on pull_request, disable a normally executing owner in ci.yml, swap an event receipt run ID, omit a second source page, replace both local bytes and their editable digest, alter signer/source ref, and invert timestamps separately in the ledger and protected source. Require each mutation to fail for the stated reason. Keep exact 719/720/721, null/empty/single/fewer-than-ten, and equal-duration/run-ID ordering cases visible.

    Per D-01/D-02/D-03, replace `@population_sha` and same-contract digest authority with protected-provenance metadata in `235-TERMINAL-RATIFICATION.json`: artifact path, attestation path, trusted-root path, signer workflow, source ref, attested subject digest, workflow run ID/URL, and exact capture command. The ledger may retain SHA-256 values for corruption detection, but the contract must establish authenticity through successful offline attestation verification before parsing measurements. Recompute the source population from all protected pages, require explicit exhaustion and exact bounds, reject inverted intervals before `DateTime.diff/3`, and reproduce the existing 19/2/2 statistics and 772-second miss. Do not alter the residual, p50, run count, SEED-005, MILESTONE-ARC, or contributor text.

    Per D-04/D-05 and the plan-time GATE-05 acceptance resolution, parse ci.yml workflow triggers and each referenced job's actual job-level `if` expression into an explicit event-eligibility map for the finite owner/event universe. Avoid a general YAML expression interpreter: validate the exact supported guard forms present in ci.yml and fail on an unknown condition. Match job IDs to protected receipt job names using the workflow display name and matrix-prefix rules already represented by the Phase 234 inventory/Phase 235 rows. An `executed` row requires eligible=true plus exactly one terminal/non-skipped job in that event receipt; `intentionally_absent` requires eligible=false plus no executing match. Validate terminal aggregates and `needs` separately. Delete the unconditional predicate.

    After the complete focused/planning suite and offline attestation command pass together, edit only the FAST-01 and GATE-05 representations in `.planning/REQUIREMENTS.md`: keep FAST-01 unchecked and traceability `Gaps Found` with the 19-run/772-second residual; mark GATE-05 checked and traceability `Complete` with the protected artifact/workflow run and exact 93-row execution-proof reference. Never mark FAST-01 achieved, never change the requirement text/threshold, and do not touch TEST-01..03, DX-01..06, deferred requirements, other phases, or the existing residual.
  </action>
  <reversibility rating="reversible">Ledger/test/status edits can be reverted locally; the attested source remains available to repeat or audit the reconciliation.</reversibility>
  <verify>
    <automated>bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; bash scripts/ci/capture-terminal-ratification-evidence.test.sh &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs test/sigra/planning/phase_234_playwright_inventory_contract_test.exs test/sigra/planning/phase_198_contributor_dx_contract_test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - Offline attestation verification is a prerequisite to parsing or accepting protected receipts; coherent local replacement of bytes/digests fails.
    - Complete pagination, exact population, chronological intervals, and the 19-run/772-second miss are recomputed from the attested artifact.
    - Every executed/absent ownership row agrees with both ci.yml event eligibility and the protected event job response; push/schedule unavailable-owner mutations fail.
    - REQUIREMENTS.md ends with FAST-01 unchecked/Gaps Found and GATE-05 checked/Complete; all unrelated requirement rows are byte-unchanged.
    - The existing FAST-01 residual, SEED-005, MILESTONE-ARC, CONTRIBUTING.md, ci.yml, runtime tests, UI, snapshots, schema, and package files are unchanged.
  </acceptance_criteria>
  <done>The single ledger proves its complete source and all 93 event destinations from protected run data, closes GATE-05, and preserves FAST-01 as the honest 772-second miss.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/ci/correlate-terminal-ratification-dispatch.sh` — production pre/post run-set selector with hermetic zero/one/multiple and identity-boundary fixtures.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.json` — new canonical fixed-window run/job receipt subject; internal evidence schema only.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-PROTECTED-RECEIPTS.attestation.jsonl` — new GitHub/Sigstore attestation bundle for that exact subject.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-TRUSTED-ROOT.jsonl` — new contemporaneous trusted-root material used for offline verification.
- `scripts/ci/verify-terminal-ratification-attestation-offline.sh` — one fail-closed cross-platform harness that network-isolates the positive verification and receipt/bundle/root/signer/ref mutations.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — replaces embedded-self-authentication authority with protected provenance pointers and exact execution evidence; preserves schema/version and 772-second outcome.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — adds offline provenance, pagination, chronology, eligibility, job-receipt, and requirement-status mutations; no public runtime API.
- `.planning/REQUIREMENTS.md` — status-only reconciliation: FAST-01 stays open, GATE-05 closes after proof.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven; maintainer sees landing | 235-08 | COVERED | Protected ledger plus exact requirement status. |
| REQ | FAST-01 | Under-12m p50 over ≥10 runs | 235-08 | COVERED | Proven honest miss: 19 runs, 772 seconds, residual retained. |
| REQ | GATE-05 | Single before/after artifact proves no silent drop | 235-08 | COVERED | 93 rows require event eligibility plus protected execution receipt. |
| RESEARCH | — | Same immutable window for PR/push/schedule | 235-08 | COVERED | Attested fixed-window population is the sole source. |
| RESEARCH | — | Direct owner distinct from aggregate | 235-08 | COVERED | Both job execution and aggregate needs are validated. |
| CONTEXT | D-01–D-03 | Exact measurement/proof/honest verdict | 235-08 | COVERED | Offline provenance and replay preserve 772 miss. |
| CONTEXT | D-04–D-05 | Complete before/after ownership with receipts | 235-08 | COVERED | Exact inventory universe plus real event jobs. |
| CONTEXT | D-06 | Contributor topology | 235-08 | COVERED | Existing verified CONTRIBUTING remains unchanged. |
| CONTEXT | D-07 | Honest closeout/residual | 235-08 | COVERED | FAST remains open; GATE closes only on proof. |
| CONTEXT | D-08 | No unrelated todos | 235-08 | COVERED | Explicit byte-unchanged exclusions in Task 2. |
| VERIFICATION | CR-01 | Replace no-op event predicate | 235-08 | COVERED | Workflow eligibility + protected job receipt and mutations. |
| VERIFICATION | CR-02 | Prove page exhaustion | 235-08 | COVERED | Attested complete pages/totals/exhaustion. |
| VERIFICATION | CR-03 | Independent provenance | 235-08 | COVERED | Main signer identity verified offline. |
| VERIFICATION | CR-04 | Reject inverted intervals | 235-08 | COVERED | Both ledger and protected-source mutations. |
| VERIFICATION | status | Preserve FAST-01 miss/status | 235-08 | COVERED | Requirement text/status acceptance is explicit. |

All authoritative gaps are covered across Plans 07–08. Deferred ideas, prior-phase gaps, product/UI/release work, test deletion, timeout changes, retry masking, schema changes, and unrelated todos remain excluded. The four spec-less probes have deterministic Phase 235 acceptance rules and remain visibly flagged; the two descriptor-less prohibitions remain flagged-unverified without new descriptors.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Protected GitHub workflow → retained local evidence | Artifact bytes and signer identity cross from protected external state into editable repository files. |
| ci.yml policy → ownership eligibility map | Workflow trigger/job conditions determine whether each direct owner could run on an event. |
| Protected job receipts → ownership ledger/status | Actual job presence/conclusion determines whether GATE-05 can close. |
| Terminal ledger → REQUIREMENTS.md | Evidence must not turn a measured FAST-01 miss into an achieved checkbox. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-08-01 | Spoofing | attestation signer/source | high | mitigate | The checked-in harness denies network and verifies exact repository, signer workflow, main source ref, subject digest, bundle, and trusted root before parsing evidence; all five altered-input cases fail inside the same isolation boundary. |
| T-235-08-02 | Tampering | complete run/job population | high | mitigate | Reconcile attested pages/totals/exhaustion and exact identities; reject local bytes+digest replacement and all interval inversions. |
| T-235-08-03 | Tampering | event eligibility/execution | high | mitigate | Parse finite supported ci.yml guard forms and require matching terminal/non-skipped job receipt for each executed row; push/schedule negative mutations. |
| T-235-08-04 | Repudiation | FAST-01/GATE-05 status | high | mitigate | Contract ties requirement checkbox/traceability status to exact protected verdict and ownership proof; FAST miss/residual remain unchanged. |
| T-235-08-05 | Information disclosure | retained API/attestation files | high | mitigate | Commit public metadata and cryptographic bundles only; exclude tokens, headers, cookies, and raw authenticated logs. |
| T-235-08-06 | Denial of service | remote capture monitoring | medium | mitigate | Two bounded one-shot run-list projections feed an exact set-difference selector before one watcher at a 60-second interval; use one summary/log fetch, one transient retry, rate-budget preflight, and hard stop on 403/429. |
| T-235-08-07 | Spoofing | dispatched workflow run correlation | high | mitigate | Bind pre/post projections to numeric workflow ID, workflow_dispatch, exact protected-main SHA, and UTC not-before boundary; require exactly one new ID, persist its ID/URL before watching, and fail zero/multiple/stale/wrong-identity fixture cases. |

## ASVS L1 Automated Controls

- V4 access control: only the protected main workflow can satisfy signer/ref policy; local PR-authored receipts cannot.
- V5 validation: exact schemas, pages, totals, chronology, event guards, job conclusions, ownership keys, and status congruence all fail closed.
</threat_model>

<verification>
After the blocking protected-main merge checkpoint and exact origin/main blob match, run the production dispatch selector's hermetic zero/one/multiple and identity-boundary cases, verify the attestation through the fail-closed network-isolation harness, run the hermetic capture and metrics tests, run focused Phase 235/234/198 contracts, then run the complete planning test directory. Assert the terminal ledger remains 19/772/miss, FAST-01 remains Gaps Found, and GATE-05 alone becomes Complete.
</verification>

<success_criteria>
Complete protected run data authenticates the preserved FAST-01 miss and every GATE-05 ownership destination; all blocker mutations fail and requirement statuses state exactly what was proven.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-08-SUMMARY.md` when done.
</output>
