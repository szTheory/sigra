---
phase: 235-terminal-ratification-measured-not-read
plan: 02
type: execute
wave: 2
depends_on: [235-01]
files_modified:
  - .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-INSUFFICIENT-RUNS.json
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
autonomous: false
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "D-01: at least ten terminal pull_request runs created at or after 2026-08-01T02:06:30Z and at or before one recorded capture endpoint form the terminal after-window."
    - "D-02: all three event measurements use scripts/ci/ci-run-metrics.sh --mode wall with the same cutoff and retain every conclusion; no successful-only, job-span, rolling, or hand-calculated substitute is accepted."
    - "D-03: FAST-01 is pass only when the measured pull_request p50 is strictly less than 720 seconds; a miss records the observed value and binding-pole job receipt without changing the threshold."
    - "The GATE-05 ownership rows receive concrete live receipts from the same bounded window, and a missing or nonterminal receipt leaves the ledger pending rather than complete."
  artifacts:
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      provides: "captured same-window run population, terminal statistics, ownership receipts, honest verdict, and binding-pole diagnosis when required"
    - path: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      provides: "window, command, cutoff, run-count, terminal-outcome, comparator, and receipt validation"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-INSUFFICIENT-RUNS.json
      provides: "conditional machine-readable blocking checkpoint with exact run-count and rate-limit diagnostics"
  key_links:
    - from: scripts/ci/ci-run-metrics.sh
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      via: "literal wall-mode PR/push/schedule commands and sanitized JSON output"
    - from: GitHub Actions run/job metadata
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      via: "real run IDs, terminal conclusions, URLs, timestamps, and optional --jobs binding-pole receipt"
---

<objective>
Capture the legitimate post-change run population once, seal its same-window receipts, and record the honest FAST-01 pass or miss.

Purpose: turn the ledger's pending measurement contract into the milestone's terminal run-data result without manufacturing traffic or filtering inconvenient outcomes.
Output: captured PR/push/schedule statistics, selected run IDs, live ownership receipts, and a strict measured verdict.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VALIDATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-01-SUMMARY.md
@.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md
@.planning/phases/231-gate-honesty-nightly-revival/231-EVIDENCE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
@.planning/phases/233-library-suite-economics/233-EVIDENCE.json
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture one immutable PR, push, and schedule window with real run IDs</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`; a fresh `gh api rate_limit` read reports `.resources.core.remaining &gt; 250`; and a fresh read-only run-list proves at least ten terminal `pull_request` runs created at or after `2026-08-01T02:06:30Z` exist. An unmet count or rate-limit predicate triggers the blocking automation checkpoint below; it cannot be approved by human UAT.</precondition>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, .planning/phases/235-terminal-ratification-measured-not-read/235-INSUFFICIENT-RUNS.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <checkpoint_contract>
    On fewer than ten eligible runs, a REST core budget at or below 250, or HTTP 403/429, write `235-INSUFFICIENT-RUNS.json` with `status: blocked`, immutable cutoff, observed UTC timestamp, exact eligible terminal PR count and public run IDs, source command, rate-limit remaining/reset or retry time, and a reason code; keep the terminal ledger `pending`, return a blocking precondition checkpoint/escalation, and stop before any producing command or verdict work. This is machine evidence, not human verification. Resume this same Plan 02 from Task 1 only after a fresh read-only check proves both at least ten eligible post-cutoff terminal PR runs and `.resources.core.remaining &gt; 250`; for 403/429, do not re-enter before GitHub's reported reset/retry time. On successful re-entry, retain the prior diagnostic as historical evidence and mark it `resolved` with the fresh observed count/time rather than deleting it.
  </checkpoint_contract>
  <read_first>
    - Both files listed in `<files>` — exact schema, pending state, complete ownership universe, and existing mutations from Plan 01.
    - `AGENTS.md` — one watcher per run, 60-second interval, rate-limit threshold, one transient retry, and 403/429 hard-stop rules.
    - `scripts/ci/ci-run-metrics.sh` — exact `--since`, `--event`, `--mode wall`, `--format json`, and `--jobs` behavior; do not reproduce its jq formula.
    - `scripts/ci/ci-run-metrics.test.sh` — hermetic semantics contract that must remain green.
    - `.planning/REQUIREMENTS.md` — authoritative baseline and strict FAST-01 threshold/count.
    - `.github/workflows/ci.yml` — workflow name/event vocabulary and direct-owner job IDs needed for receipt attachment.
    - Phase 230–234 evidence files referenced in `<context>` — known execution receipts to reconcile with the selected after-window.
  </read_first>
  <behavior>
    - "The capture endpoint is one UTC instant later than or equal to every selected run's createdAt/updatedAt and is reused for pull_request, push, and schedule."
    - "The PR population contains at least ten distinct terminal run IDs, every createdAt is on/after the cutoff and on/before the endpoint, and failed/cancelled conclusions are retained."
    - "The three stored producing commands differ only by event and all explicitly use --mode wall, --since 2026-08-01T02:06:30Z, --format json, and one sufficient fixed --limit."
    - "A pre-cutoff ID, duplicate ID, nonterminal conclusion, wrong event, mismatched endpoint, fewer than ten PR runs, or command using jobspan is rejected."
  </behavior>
  <action>
    Assert the precondition with read-only calls before editing the terminal ledger. Run `gh api rate_limit` once and parse `.resources.core.remaining`, then fetch one sanitized run population using `gh run list --repo szTheory/sigra --workflow ci.yml --limit 100 --json databaseId,event,createdAt,updatedAt,conclusion,url,headSha`. If fewer than ten eligible terminal PR runs exist, the core budget is 250 or lower, or GitHub returns 403/429, execute `<checkpoint_contract>` exactly: persist the machine-readable blocked diagnostic, leave the ledger pending, and halt without human UAT, synthetic dispatch, producing metrics commands, or partial verdict work. Retry any other transient collection failure at most once. Re-enter this same plan only after the checkpoint's count, time, and rate-limit predicates pass. Once eligible, immediately record the UTC capture endpoint and preserve only public metadata. Select every terminal run in the closed interval from `2026-08-01T02:06:30Z` through that endpoint for `pull_request`, `push`, and `schedule`; do not filter by success.

    Run exactly these three producing commands once each, substituting no alternate formula: `bash scripts/ci/ci-run-metrics.sh --mode wall --since '2026-08-01T02:06:30Z' --event pull_request --limit 100 --format json`, then the same command with `--event push`, then `--event schedule`. Store each literal command, sanitized JSON result, exact selected run IDs/outcomes/timestamps/URLs/SHAs, output SHA-256, capture endpoint, and exit status under its measurement slot. If no push or schedule run exists inside the legitimate window, record that event as an explicit zero-observation pending fact with the source population command; do not invent a success or widen the cutoff. Attach concrete same-window run/job receipts to the ownership rows where selected runs execute the direct owners, preserving intentional event absences.

    Extend the contract to require the exact common cutoff/endpoint/limit/mode, selected-ID uniqueness and containment, terminal conclusions, `pull_request` count of at least ten, and correspondence between stored summary numbers and the unmodified script output. Add mutations for fewer than ten, a pre-cutoff ID, a post-endpoint timestamp, duplicate/wrong-event/nonterminal IDs, mismatched event windows, and altered command semantics. If an already-active selected workflow run must be awaited before it becomes eligible, use exactly one watcher for that run: `gh run watch <run-id> --repo szTheory/sigra --compact --interval 60 --exit-status`; never poll the same run elsewhere, and fetch its structured summary once after completion. Normal completed-run collection needs no watcher.
  </action>
  <verify>
    <automated>bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; jq -e '(.measurements.pull_request.run_ids | length) &gt;= 10 and (.capture_endpoint.status == "captured")' .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json</automated>
    <blocked>When the precondition checkpoint fires: jq -e '.status == "blocked" and (.eligible_pr_run_count | type == "number") and (.eligible_pr_run_ids | type == "array") and (.immutable_cutoff == "2026-08-01T02:06:30Z") and (.observed_at | type == "string") and (.reason_code | IN("insufficient_eligible_runs", "rate_limit_budget", "rate_limited")) and (.resume_when.minimum_eligible_pr_runs == 10) and (.resume_when.minimum_core_remaining == 251)' .planning/phases/235-terminal-ratification-measured-not-read/235-INSUFFICIENT-RUNS.json</blocked>
  </verify>
  <acceptance_criteria>
    - One run-list receipt and exactly three wall-mode metrics commands are persisted with sanitized output hashes and a shared immutable cutoff/endpoint.
    - At least ten distinct terminal post-cutoff PR IDs exist; every conclusion, including failures/cancellations, remains in the population.
    - Push and schedule use the identical bounded interval and are never replaced by a different rolling window.
    - The eight named window/ID/command mutation classes fail with deterministic diagnostics.
    - No synthetic run is dispatched, no second watcher polls a run, no raw token/authenticated state is retained, and 403/429 causes a hard stop.
    - An insufficient-run or rate-limit branch writes the declared blocking JSON diagnostic and stops; re-entry occurs only when a fresh check proves at least ten eligible runs, core remaining above 250, and any reported 403/429 reset/retry time has passed.
  </acceptance_criteria>
  <done>The ledger contains one re-fetchable, baseline-compatible after-window with enough legitimate PR observations and same-window main/nightly counterparts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute the strict FAST-01 verdict and diagnose any measured miss</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - Both files listed in `<files>` after Task 1 — captured metrics output, run population, receipt links, and current verdict validator.
    - `scripts/ci/ci-run-metrics.sh` — `--jobs <run_id> --format json` is the only allowed binding-pole measurement path.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-02/D-03 strict comparator and honest-result branch.
    - `.planning/REQUIREMENTS.md` — FAST-01 wording and baseline table.
    - `.planning/phases/198-acceptance-evidence/198-ACCEPTANCE.md` if present, otherwise `.planning/milestones/v1.40-phases/198-acceptance-evidence/198-ACCEPTANCE.md` — precedent for disclosing a measured miss and binding pole without rewriting the target.
  </read_first>
  <behavior>
    - "verdict.fast_01 is pass only when eligible_pr_run_count >= 10 and observed_p50_seconds < 720; equality and larger values are miss."
    - "The observed p50 is copied from the stored metrics output and is never recomputed, rounded below threshold, or filtered to successful runs."
    - "A miss contains at least one real --jobs receipt identifying the longest direct job(s), command, run ID, job names, conclusions, durations, and sanitized output hash."
    - "A pass cannot carry contradictory miss language; a miss cannot set closeout performance_target_achieved true."
  </behavior>
  <action>
    Read the pull-request `p50_seconds` directly from Task 1's stored `ci-run-metrics.sh` JSON. Set `verdict.fast_01.threshold_seconds` to `720`, `comparator` to `lt`, `eligible_pr_run_count` to the stored population count, `observed_p50_seconds` to that unmodified integer/number, and `status` to `pass` only if the observed value is strictly below 720. Record baseline delta fields against 1638 seconds without using them to override the pass/miss status. Copy same-window push and schedule result tables into the terminal verdict summary and preserve all observed outcomes.

    If status is `miss`, choose the median-neighbor run identified by the script's sorted population plus the maximum-duration representative, deduplicating if they are the same. For each selected run invoke `bash scripts/ci/ci-run-metrics.sh --jobs <run_id> --format json` exactly once, retain the literal command/run ID/public job names/conclusions/durations/output hash, and name the binding pole from the longest non-skipped job data. This diagnostic uses job spans only to explain the already-measured miss; it must not replace the queue-inclusive wall verdict. If status is `pass`, set `binding_pole_receipts` to an empty list and do not make unnecessary per-job API calls.

    Set `closeout.measurement_ready` true only after all window and verdict validations pass; set `closeout.performance_target_achieved` to the exact pass boolean and leave `closeout.records_reconciled` false for Plan 03. Extend tests with exact boundary cases for 719/720/721 seconds, a p50 altered away from the stored script output, fewer-than-ten pass-shaped input, filtered outcomes, a miss missing `--jobs` provenance, a pass carrying miss-only receipts, and a closeout boolean contradicting the verdict. Keep both descriptor-less transparency prohibitions flagged-unverified; the deterministic contract supports them but does not fabricate a prohibition descriptor.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; jq -e '(.verdict.fast_01.status == "pass" and .verdict.fast_01.observed_p50_seconds &lt; 720) or (.verdict.fast_01.status == "miss" and .verdict.fast_01.observed_p50_seconds &gt;= 720 and (.receipts.binding_pole | length) &gt;= 1)' .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json</automated>
  </verify>
  <acceptance_criteria>
    - The artifact reports the exact observed p50 and a strict `lt 720` pass/miss; 720 seconds is demonstrably a miss.
    - The result uses at least ten retained-outcome PR runs and matches the stored metrics output byte-for-byte at its statistical fields.
    - A miss has real per-job receipt(s) and a named binding pole; a pass has no unnecessary binding-pole API sweep.
    - Same-window push and schedule outcomes remain visible beside the PR result.
    - Closeout readiness mirrors the verdict exactly and records remain unreconciled until Plan 03.
  </acceptance_criteria>
  <done>FAST-01 has an honest run-data verdict, including a binding-pole diagnosis when the target is missed, without any methodology substitution.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — populates existing fields `capture_endpoint`, `measurements.pull_request`, `measurements.push`, `measurements.schedule`, `receipts.ownership`, `receipts.binding_pole`, `verdict.fast_01`, and `closeout.measurement_ready`; no new path, command, or flag.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-INSUFFICIENT-RUNS.json` — conditional automation checkpoint evidence for an insufficient eligible-run population or blocked GitHub rate-limit state, with deterministic same-plan re-entry predicates; it never authorizes a verdict.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — adds same-window/run-population validation and exact 719/720/721 comparator, provenance, miss-diagnosis, and contradictory-closeout cases; no new public production symbol.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Headline claims proven from real run data | 02 | COVERED | This plan performs the sole authorized live capture and verdict. |
| REQ | FAST-01 | Under-12m p50 over ≥10 post-change PR runs | 02 | COVERED | Strict count/window/method/comparator and honest miss path. |
| REQ | GATE-05 | Before/after artifact has execution proof | 01–02 | COVERED | Plan 01 maps ownership; this plan attaches live same-window receipts. |
| RESEARCH | — | Existing wall-mode script and immutable cutoff | 02 | COVERED | Exact three commands; no alternate calculator. |
| RESEARCH | — | Binding-pole `--jobs` evidence on miss | 02 | COVERED | Conditional per-run receipts explain, never replace, wall result. |
| RESEARCH | — | Rate-limit/credential/terminal-result controls | 02 | COVERED | One population fetch, bounded retry, one watcher if needed, hard stop on 403/429. |
| CONTEXT | D-01–D-03 | Measurement methodology and honest verdict | 02 | COVERED | Both tasks cite and implement each decision. |
| CONTEXT | D-04–D-05 | One complete ownership artifact | 01–02 | COVERED | Plan 01 structure plus this plan's runtime receipts. |
| CONTEXT | D-06–D-07 | Docs/closeout from measured result | 03 | COVERED | Runs only after this plan seals the verdict. |
| CONTEXT | D-08 | No pending todos folded in | 02 | COVERED | No remediation or unrelated todo is executed. |

The four fallback edge items remain explicitly flagged in Plan 01 and are exercised, not silently disposed, by this plan's boundary mutations. The descriptor-less fallback prohibitions remain flagged-unverified. No deferred idea, API integration, package install, schema change, UI work, topology rewrite, or synthetic-run generation is planned.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub run/job API → committed ledger | External mutable responses and credentials cross into durable project evidence. |
| Metrics output → FAST-01 verdict | A method, count, cutoff, event, or comparator change can manufacture a pass. |
| Rate-limit state → collection loop | Repeated polling can exhaust quota or amplify a service throttle. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-02-01 | Tampering | measurement population/verdict | high | mitigate | Exact cutoff/endpoint/commands/IDs, retained outcomes, ≥10 contract, script-output equality, and 719/720/721 mutations block manufactured success. |
| T-235-02-02 | Repudiation | live run/job receipts | high | mitigate | Persist real run IDs, URLs, SHAs, timestamps, literal commands, hashes, and terminal conclusions; miss diagnosis cites exact jobs. |
| T-235-02-03 | Information disclosure | GitHub credentials/output | high | mitigate | Never print/store GH_TOKEN, cookies, authenticated HTML, or environment dumps; retain only public metadata and sanitized hashes. |
| T-235-02-04 | Denial of service | GitHub API collection | high | mitigate | Check core budget, use one fetch, one retry for non-rate transient failure, at most one 60-second watcher per run, and hard-stop on 403/429 until reset. |

## ASVS L1 Automated Controls

- V4 least privilege: read-only `gh run list/view` and rate-limit endpoints only; no dispatch, issue, PR, branch, or workflow mutation.
- V5 input validation: exact event/time/ID/outcome/command shapes and contradiction mutations gate every terminal status.
</threat_model>

<verification>
The metrics self-test and focused ledger contract pass against the captured artifact; jq confirms either a strict pass or an evidence-backed miss, never a third success-shaped state.
</verification>

<success_criteria>
At least ten legitimate post-cutoff PR runs and the same-window push/nightly population are recorded, FAST-01 is judged strictly and honestly, and every live claim remains re-fetchable without exposing credentials or storming GitHub.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-02-SUMMARY.md` when done.
</output>
