---
phase: 235-terminal-ratification-measured-not-read
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
  - test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
autonomous: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "D-01/D-02: one sigra.terminal-ratification/v1 ledger records an immutable topology cutoff, one capture endpoint, baseline-compatible wall measurements, and all terminal outcomes without recomputation."
    - "D-04/D-05: the same ledger consumes the Phase 234 Playwright inventory and contains sorted before/after ownership for every affected Playwright and non-Playwright family across pull_request, push, and schedule."
    - "Every moved ownership row names a receiver plus an immutable phase or live-run receipt; an aggregate without a direct executable owner is rejected."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: When two things are exactly equal or just touch, do they merge, collide, or separate? The literal under-12-minute comparator implies 720 seconds is not a pass, but the fallback edge remains flagged for independent verification."
    - requirement_id: FAST-01
      category: empty
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: What is the result for empty, single-element, or null input? The contract rejects a terminal verdict below ten eligible runs, but this fallback edge remains flagged rather than auto-dismissed."
    - requirement_id: FAST-01
      category: ordering
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: When elements compare equal, is output order specified and stable? The mandated instrument owns sorting and floor(n/2), but this fallback edge remains flagged rather than silently backstopped."
    - requirement_id: GATE-05
      category: unclassified
      status: unresolved
      flagged: true
      statement: "FLAGGED/UNRESOLVED: unclassified — review manually; the exact ownership universe and receipt contract must provide the review surface."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT represent FAST-01 as achieved from fewer than ten eligible post-cutoff PR runs, a p50 at or above 720 seconds, filtered conclusions, or substituted timing semantics."
      reason: "Descriptor-less flagged-unverified fallback prohibition; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present coverage as complete while omitting an affected spec or suite, naming only a terminal aggregate, or lacking a receiver and execution receipt for moved work."
      reason: "Descriptor-less flagged-unverified fallback prohibition; no check descriptor is fabricated."
  artifacts:
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      provides: "single terminal measurement and before/after ownership source of truth"
      contains: "sigra.terminal-ratification/v1"
    - path: test/sigra/planning/phase_235_terminal_ratification_contract_test.exs
      provides: "fail-closed ledger, live-topology, inventory, receipt, and closeout contract"
  key_links:
    - from: .planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      via: "source_inventory path/schema/hash and exact current Playwright row reconciliation"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json
      to: .github/workflows/ci.yml
      via: "each after owner, seam, invocation marker, event state, and terminal aggregate is checked against the live workflow"
---

<objective>
Create the production terminal ledger and prove one ownership path end to end before expanding it to the complete milestone universe.

Purpose: establish the single fail-closed source that both FAST-01 measurement and GATE-05 coverage ratification will consume.
Output: `235-TERMINAL-RATIFICATION.json` and its focused ExUnit contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VALIDATION.md
@.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-21-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one library-suite ownership row through the terminal ledger and live workflow</name>
  <reversibility rating="costly">The versioned ledger shape is consumed by the phase contract and later closeout edits, so changing it requires coordinated updates across the remaining plans, but no external migration or published API break.</reversibility>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-01 through D-05 and the single-artifact boundary.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md` — recommended ledger fields, exact mutation set, and validation architecture.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` — evidence schema/version, immutable identifier, command, digest, and diagnostics precedent.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — required after-state input and `sigra.playwright-ownership/v1` marker.
    - `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — async ExUnit, decode-once helpers, exact universe, workflow seam, and mutation patterns.
    - `test/sigra/planning/phase_234_evidence_contract_test.exs` — exact-key-set and pending-versus-terminal transition patterns.
    - `.github/workflows/ci.yml` — live `library_tests_shard` direct owner, `library_tests` aggregate, events, and command seam.
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — D-01/D-02 measurement semantics that the schema must carry without reimplementation.
    - Every file listed in `<files>` before editing; the JSON path is expected not to exist on first execution.
  </read_first>
  <behavior>
    - "RED first: the focused test fails because the versioned terminal ledger is absent."
    - "The initial ledger has exact top-level keys schema_version, topology_cutoff, capture_endpoint, baseline, measurements, ownership, receipts, verdict, and closeout."
    - "schema_version equals sigra.terminal-ratification/v1; baseline preserves pull_request n=21, mean=1770, p50=1638, max=2502, pass=17, fail=4 plus the committed push and schedule baselines."
    - "The tracer ownership row covers library/scaffold/golden execution for pull_request, push, and schedule with before/after direct owner, invocation seam, terminal aggregate, state, receiver, and provenance."
    - "The ledger may be evidence-pending, but pending can never satisfy FAST-01 or closeout completion."
  </behavior>
  <action>
    Create `Sigra.Planning.Phase235TerminalRatificationContractTest` as `async: true`. Start with a failing happy-path test for a decoded `sigra.terminal-ratification/v1` ledger, then add the JSON artifact with exact top-level keys `schema_version`, `topology_cutoff`, `capture_endpoint`, `baseline`, `measurements`, `ownership`, `receipts`, `verdict`, and `closeout`. Set `topology_cutoff.source_commit_sha` to `6c57d7b4a22aa87a757a6f508f2cf4fdb414e40a`, `topology_cutoff.committed_at` to `2026-08-01T02:06:30Z`, and explain that this is the last milestone commit in the live topology-path history; the contract must verify the full SHA exists and its timestamp matches Git. Set `capture_endpoint.status` and all three measurement slots to `pending`, with no invented run IDs or statistics.

    Copy only the authoritative baseline numbers from REQUIREMENTS.md into seconds: pull_request 21 / mean 1770 / p50 1638 / max 2502 / 17 pass / 4 fail; push 7 / mean 1830 / p50 1656 / max 2538 / 6 pass / 1 fail; schedule 9 / mean 1638 / p50 1626 / max 1764 / 0 pass / 9 fail. Add one production-quality `library_scaffold_golden` ownership tracer spanning `pull_request`, `push`, and `schedule`; distinguish direct execution owner `library_tests_shard` from the byte-stable `library_tests` / `Library tests` aggregate, cite Phase 233 receipts for the before/after move, and model explicit state rather than treating absent/skipped as success.

    Implement helpers that require the exact schema and known enum values, reject extra top-level keys, validate full SHA/UTC/command/run-ID shapes, and reconcile the tracer's after owner and command marker to `.github/workflows/ci.yml`. Add in-memory mutations for missing/extra keys, malformed cutoff, absent direct owner, aggregate-only ownership, and pending data incorrectly marked complete. Do not call GitHub, recalculate metrics, copy the Phase 234 inventory, edit workflow topology, or declare either requirement complete in this task. This is the leading D-01/D-02/D-04/D-05 tracer and remains production code for the later expansion.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; jq -e '.schema_version == "sigra.terminal-ratification/v1"' .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json</automated>
  </verify>
  <acceptance_criteria>
    - The focused test demonstrates RED before the artifact exists and GREEN after the tracer ledger is added.
    - The JSON has exactly the nine named top-level keys and the immutable cutoff SHA/timestamp specified above.
    - Baseline values equal REQUIREMENTS.md after exact minutes-to-seconds conversion; no new arithmetic path computes terminal metrics.
    - The library/scaffold/golden tracer has exactly three event rows and each row distinguishes direct owner, invocation, aggregate, state, receiver, and receipt.
    - Pending measurement/closeout state is accepted only as pending and cannot produce a pass or completed claim.
  </acceptance_criteria>
  <done>A maintainer can inspect one real before/after family end to end, and the same production contract rejects an aggregate-only or success-shaped counterfeit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the ledger to every affected spec, suite, event, and receiver</name>
  <files>.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json, test/sigra/planning/phase_235_terminal_ratification_contract_test.exs</files>
  <read_first>
    - Both files listed in `<files>` after Task 1 — preserve the tracer schema and helpers rather than creating a parallel model.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-PLAYWRIGHT-INVENTORY.json` — exact sorted current Playwright spec universe and lane fields; consume by path/schema/SHA rather than copying it as a second inventory.
    - `test/sigra/planning/phase_234_playwright_inventory_contract_test.exs` — live-spec and lane reconciliation helpers, including the two sanctioned harness indirections.
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md` and `230-VERIFICATION.md` — design snapshot/axe, `admin_eval_render`, event demotion, and pre-milestone ownership receipts.
    - `.planning/phases/231-gate-honesty-nightly-revival/231-EVIDENCE.md` and `231-VERIFICATION.md` — generated-host, nightly, gate-honesty, and non-PR lane receipts.
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` and `232-VERIFICATION.md` — five Playwright seam receivers and terminal aggregate receipts.
    - `.planning/phases/233-library-suite-economics/233-EVIDENCE.json` and `233-VERIFICATION.md` — library/scaffold/golden direct-owner and aggregate receipts.
    - `.planning/phases/234-hygiene-supply-chain-and-contributor-dx/234-EVIDENCE.json` and `234-VERIFICATION.md` — final current-state receipts and inventory completion.
    - `.github/workflows/ci.yml` plus `test/example/priv/playwright/playwright.config.ts` — current jobs, events, command markers, projects, and aggregates.
  </read_first>
  <behavior>
    - "Every one of the 20 Phase 234 Playwright spec rows is represented through the source_inventory link and has complete pull_request/push/schedule before/after ownership."
    - "Non-Playwright rows cover design snapshots, per-board axe, admin_eval_render, library ordinary shards, scaffold/golden/dep-off suites, generated-host acceptance, and terminal aggregates."
    - "Rows are sorted and unique by family/spec/event; every move has a non-empty receiver and immutable receipt; intentionally absent work has an explicit event state."
    - "Mutations for missing, stale, duplicate, unowned, non-executable, receiverless, receiptless, or aggregate-only rows fail with the affected identifier named."
  </behavior>
  <action>
    Extend the tracer into the complete GATE-05 ownership map per D-04 and D-05. Record `ownership.source_inventory` with the literal Phase 234 path, its `sigra.playwright-ownership/v1` schema, `phase_235_gate_input: true`, and its actual SHA-256. Reference every one of its 20 sorted specs without duplicating the inventory's lane model. For each affected family/spec, materialize three event comparisons (`pull_request`, `push`, `schedule`) containing `before` and `after` objects with `direct_owner`, `seam`, `invocation`, `terminal_aggregate`, and `state`; moved rows additionally require `receiver`, `phase`, and a concrete run/evidence reference.

    Include the full non-Playwright surface named by D-04: design-gallery snapshots separately from per-board axe coverage, `admin_eval_render` plus downstream harness guards, generated-host acceptance, library ordinary shards, scaffold/install-golden/idempotency/dep-off ownership, and the `Library tests`, `Example Playwright smoke (full lifecycle)`, and `ci-gate` terminal aggregates. Use Phase 230–234 evidence/verification records for historical before receivers and execution receipts; do not infer execution solely from YAML. Preserve intentional non-PR state for `admin_eval_render` and snapshot/recapture signals and require the direct executable lane even when a terminal aggregate is present.

    Expand the test with exact sorted-universe comparison, Phase 234 inventory hash/schema/path validation, current live-spec reconciliation, current job/seam/invocation/event checks, receipt shape/provenance checks, and in-memory negative fixtures for missing, stale, duplicate, unowned, non-executable, receiverless, receiptless, and aggregate-only rows. Require exact event coverage for each identifier and reject extra evidence slots. Preserve all four flagged edge assumptions and both descriptor-less prohibitions in the plan/summary handoff; do not auto-resolve them in the ledger. Per D-08, do not modify pending todos, CI topology, tests under ownership, or unrelated UI/release/product records.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_234_playwright_inventory_contract_test.exs &amp;&amp; jq -e '.ownership.rows == (.ownership.rows | sort_by(.family, (.spec // ""), .event))' .planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json</automated>
  </verify>
  <acceptance_criteria>
    - The terminal artifact consumes the Phase 234 inventory by exact path/schema/SHA and reconciles all 20 current specs without creating a competing inventory.
    - Every affected Playwright and non-Playwright identifier has exactly pull_request, push, and schedule before/after rows.
    - Design snapshots and axe coverage, admin_eval_render and its harness guards, generated-host acceptance, library/scaffold/golden/dep-off suites, and all named terminal aggregates are present.
    - Every moved row names its receiver and concrete phase/run provenance; aggregate-only or receiptless claims are rejected.
    - All eight required negative mutation classes fail closed with deterministic named diagnostics.
  </acceptance_criteria>
  <done>The single artifact provides a complete, mechanically reconciled answer to where every affected test ran before and after the milestone.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — new path and `sigra.terminal-ratification/v1` schema; new top-level fields `topology_cutoff`, `capture_endpoint`, `baseline`, `measurements`, `ownership`, `receipts`, `verdict`, and `closeout`; no new CLI command or flag.
- `test/sigra/planning/phase_235_terminal_ratification_contract_test.exs` — new `Sigra.Planning.Phase235TerminalRatificationContractTest` module plus private exact-schema, inventory, ownership, live-seam, receipt, verdict, and closeout validators.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Prove claims from run data and show exactly what moved | 01–03 | COVERED | This plan creates the one source and complete ownership layer. |
| REQ | FAST-01 | Under-12m p50 over at least ten post-change PR runs | 01–02 | COVERED | This plan locks schema/baseline/method; Plan 02 captures and judges the window. |
| REQ | GATE-05 | Single before/after PR/main/nightly artifact | 01 | COVERED | Full ownership universe and receipts are implemented here. |
| RESEARCH | — | Existing wall-mode instrument; no hand calculation | 01–02 | COVERED | Schema stores commands/output; Plan 02 invokes only the existing script. |
| RESEARCH | — | Fail-closed inventory/live-seam/receipt mutations | 01 | COVERED | Focused ExUnit contract rejects every named malformed class. |
| RESEARCH | — | Docs and closeout derive from ledger | 03 | COVERED | Reserved for dependent Plan 03 after live verdict. |
| CONTEXT | D-01–D-03 | Immutable window and honest measured verdict | 01–02 | COVERED | Schema here; capture and branch-on-result in Plan 02. |
| CONTEXT | D-04–D-05 | Complete single ownership artifact consuming Phase 234 | 01 | COVERED | Both tasks implement this directly. |
| CONTEXT | D-06–D-07 | Contributor and planning closeout | 03 | COVERED | Dependent documentation/closeout plan. |
| CONTEXT | D-08 | No pending todos folded in | 01–03 | COVERED | No unrelated todo is implemented. |

Deferred ideas and out-of-phase work are excluded. The four fallback edge items are preserved under `must_haves.assumptions`; the two kept transparency prohibitions are descriptor-less and flagged-unverified under `must_haves.prohibitions`. API coverage detector result: `detected:false`. Package installs, schema changes, admin UI, snapshots, CI topology changes, and new retries are absent.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 230–234 artifacts → terminal ledger | Historical evidence can be stale, partial, duplicated, or detached from the current topology. |
| Phase 234 inventory / live files → ownership rows | A plausible row can name a job or aggregate that no longer executes the spec. |
| Ledger status → requirement claim | Success-shaped or pending data could be promoted to a completed claim. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-01-01 | Tampering | terminal ledger rows | high | mitigate | Exact schema/universe, inventory hash, live seam/invocation checks, immutable receipts, and negative mutations block malformed or stale ownership. |
| T-235-01-02 | Repudiation | moved-family provenance | high | mitigate | Every move names receiver, phase, command/run receipt, and direct execution owner; aggregate-only evidence is rejected. |
| T-235-01-03 | Information disclosure | persisted evidence | high | mitigate | Persist only public run/job identifiers, commands, timestamps, hashes, and sanitized diagnostics; never tokens, cookies, authenticated HTML, or raw environment state. |
| T-235-01-04 | Denial of service | focused contract | medium | mitigate | Local JSON/workflow/spec checks are deterministic and bounded; no polling, sleeps, or network calls occur in this plan. |

## ASVS L1 Automated Controls

- V4 least privilege: this plan performs no GitHub write and stores no credential material.
- V5 input validation: exact keys, enums, paths, IDs, hashes, event sets, and executable seams are mutation-tested before a terminal claim can exist.
</threat_model>

<verification>
The focused Phase 235 test, Phase 234 inventory contract, metrics self-test, and JSON structural checks all pass; every required malformed-ledger mutation remains red.
</verification>

<success_criteria>
One versioned ledger exists, one complete before/after ownership universe is mechanically reconciled, and pending measurement state cannot be mistaken for FAST-01 completion.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-01-SUMMARY.md` when done.
</output>
