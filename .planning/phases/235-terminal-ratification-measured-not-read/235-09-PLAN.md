---
phase: 235-terminal-ratification-measured-not-read
plan: 09
type: execute
wave: 9
depends_on: [235-08]
files_modified:
  - scripts/ci/capture-fast-01-remeasurement.sh
  - scripts/ci/capture-fast-01-remeasurement.test.sh
  - .github/workflows/fast-01-remeasurement-evidence.yml
  - test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs
  - .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json
  - .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]

must_haves:
  truths:
    - "D-01/D-02/D-03: a new measurement window is fixed to protected-main commit a282b3deed009e62707b1a01d16da053a53e37d8 at 2026-08-03T15:36:12Z, retains all conclusions, uses queue-inclusive wall time and sorted floor(n/2), and cannot emit a FAST-01 verdict before at least ten terminal pull_request runs exist."
    - "The non-authoritative readiness artifact reports the real eligible count and exact public run identities from an endpoint derived internally at invocation; `insufficient_population` or `ready` can gate dispatch but can never become a FAST-01 verdict or attested measurement window."
    - "The protected producer is main-only, has no caller-controlled cutoff or endpoint, paginates to an explicit terminal empty page, and attests the exact fresh-window receipt without joining ci.yml or its required-check DAG."
    - "GATE-05 remains independently Complete: the existing protected terminal receipt, 93 ownership rows, requirement status, and offline verification stay valid and are not replaced by FAST-01 remeasurement."
    - "D-08: no binding-pole optimization, test deletion, timeout/retry change, unrelated todo, or new product work is folded into this measurement plan."
  assumptions:
    - requirement_id: FAST-01
      category: adjacency
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: the comparator remains strict observed_p50_seconds < 720; exactly 720 is a miss."
    - requirement_id: FAST-01
      category: empty
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: null and empty populations fail closed; one through nine terminal PR runs produce insufficient_population and no verdict."
    - requirement_id: FAST-01
      category: ordering
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: equal durations are ordered by {wall_seconds, run_id} before floor(n/2) selection."
    - requirement_id: GATE-05
      category: unclassified
      status: resolved
      flagged: true
      statement: "FLAGGED/RESOLVED: GATE-05 remains governed by the existing 93-row protected ownership contract and is outside the fresh FAST-01 receipt."
  prohibitions:
    - requirement_id: FAST-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT represent FAST-01 as achieved from fewer than ten eligible post-cutoff PR runs, a p50 at or above 720 seconds, filtered conclusions, or substituted timing semantics."
      reason: "Descriptor-less flagged-unverified fallback prohibition carried from 235-01; no check descriptor is fabricated."
    - requirement_id: GATE-05
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT regress or reopen GATE-05 while collecting or judging a separate FAST-01 population."
      reason: "The verified ownership proof is independent of the remaining performance gap."
  artifacts:
    - path: scripts/ci/capture-fast-01-remeasurement.sh
      provides: "fixed-cutoff, bounded, paginated fresh-window collector with honest insufficient/measured states"
    - path: .github/workflows/fast-01-remeasurement-evidence.yml
      provides: "protected-main attestation producer for one independent FAST-01 window"
    - path: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json
      provides: "machine-readable current population count, rate-limit state, exact run identities, and blocking/ready status"
    - path: test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs
      provides: "fresh-window, strict-verdict, provenance, and GATE-05 non-regression contract"
  key_links:
    - from: a282b3deed009e62707b1a01d16da053a53e37d8
      to: scripts/ci/capture-fast-01-remeasurement.sh
      via: "immutable full-SHA and 2026-08-03T15:36:12Z cutoff constants checked against protected origin/main history"
    - from: scripts/ci/capture-fast-01-remeasurement.sh
      to: .github/workflows/fast-01-remeasurement-evidence.yml
      via: "separate interfaces: local `--readiness OUTPUT` derives current UTC internally and is non-authoritative, while the input-free protected workflow independently generates the sole verdict endpoint and attests its population"
    - from: .planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
      to: .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json
      via: "the sole remaining gap becomes a durable insufficient_population/ready state without changing the 772-second historical verdict"
---

<objective>
Create the fail-closed path for one fresh, independently protected FAST-01 population and record whether enough real PR runs exist to use it.

Purpose: preserve the verified 772-second miss until a fixed post-topology window contains at least ten terminal PR runs, while making the eventual remeasurement deterministic and provenance-backed.
Output: a protected fresh-window collector/workflow, hermetic contracts, API coverage update, and a machine-readable readiness artifact.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-PATTERNS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-08-SUMMARY.md
@.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md

<interfaces>
Existing calculator: `bash scripts/ci/ci-run-metrics.sh --repo szTheory/sigra --workflow ci.yml --limit 100 --since ISO8601 --event pull_request --mode wall --format json`.
Fresh cutoff: commit `a282b3deed009e62707b1a01d16da053a53e37d8`, committed `2026-08-03T15:36:12Z`, currently `origin/main`; this commit integrates the Phase 235 protected proof into required `fast_checks` and is therefore the latest topology-affecting main commit before the independent window.
GitHub source API: `GET /repos/szTheory/sigra/actions/workflows/ci.yml/runs?created=2026-08-03T15:36:12Z..ENDPOINT&amp;per_page=100&amp;page=N`.
Local readiness invocation: `bash scripts/ci/capture-fast-01-remeasurement.sh --readiness .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json`. This mode accepts no endpoint/cutoff argument, defines no endpoint environment override, derives `window.endpoint` internally from `/usr/bin/date -u +%Y-%m-%dT%H:%M:%SZ` immediately before page 1, and always records `authority=readiness_only`, `endpoint_source=collector_current_utc`, and `verdict=null`.
Protected measurement invocation: the input-free main-only workflow independently records its own UTC start endpoint and supplies it to the collector's protected-output path. Only the resulting protected-main attested subject may carry `status=measured` and a pass/miss verdict; local readiness bytes and endpoint never become or select that verdict window.
Protected attestation pattern: `.github/workflows/terminal-ratification-evidence.yml` → `scripts/ci/capture-terminal-ratification-evidence.sh` → `actions/attest-build-provenance`.
Historical invariant: `235-TERMINAL-RATIFICATION.json` remains `eligible_pr_run_count=19`, `observed_p50_seconds=772`, `status=miss`; `GATE-05` remains Complete from protected run `30782184713` and 93 ownership rows.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one fixed-cutoff PR page into a non-authoritative readiness artifact</name>
  <reversibility rating="costly">The fixed cutoff determines every future accepted row; changing it after protected capture would require discarding that receipt and producing a new independently attested population.</reversibility>
  <files>scripts/ci/capture-fast-01-remeasurement.sh, scripts/ci/capture-fast-01-remeasurement.test.sh, test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs, .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json</files>
  <read_first>
    - `AGENTS.md` — one-watcher, rate-budget, retry, 403/429, and automation-only evidence rules.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md` — D-01 through D-03 measurement semantics and D-08 scope fence.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md` — authoritative sole gap and strict 772-versus-720 result.
    - `.planning/todos/pending/2026-08-02-fast-01-terminal-p50-miss.md` — separate-remediation ownership and the two recorded binding poles.
    - `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — queue-inclusive all-conclusion floor(n/2) contract; call or exactly reconcile this instrument rather than introducing different arithmetic.
    - `scripts/ci/capture-terminal-ratification-evidence.sh` and `scripts/ci/capture-terminal-ratification-evidence.test.sh` — explicit page loop, terminal-empty-page proof, chronology checks, fake-gh fixtures, and sanitized canonical output pattern.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-TERMINAL-RATIFICATION.json` — historical verdict and protected GATE-05 invariants that stay unchanged.
    - Every path in `<files>` before editing; the four paths are expected not to exist on first execution.
  </read_first>
  <behavior>
    - "RED first: the focused tests fail because no fresh-window collector/readiness artifact exists."
    - "The exact local invocation is `--readiness OUTPUT`; it accepts no endpoint/cutoff option, derives current UTC internally immediately before collection, and records that derivation and its non-authoritative role."
    - "Every requested source page is retained contiguously through one terminal empty page; IDs are unique, timestamps are chronological, and all conclusions remain present."
    - "Zero through nine terminal PR rows yield status insufficient_population; ten or more yield status ready. Both retain observed count, exact IDs/URLs, internally derived endpoint, command, and rate-limit remaining/reset with statistics/verdict null."
    - "Only the protected-output path may emit status measured and statistics computed with wall seconds, stable {wall_seconds, run_id} ordering, floor(n/2), and strict less-than 720, and only its protected attestation may influence FAST-01."
    - "Moving the cutoff, omitting a conclusion, selecting only successful rows, accepting exactly 720, or changing the historical 19/772 miss fails."
  </behavior>
  <action>
    Start RED with hermetic fake-gh fixtures for 0, 1, 9, 10, and 11 terminal pull-request runs; equal-duration ties; 719/720/721-second protected medians; mixed success/failure/cancelled conclusions; duplicate IDs; missing/duplicate pages; a non-empty safety-bound page; inverted timestamps; post-endpoint rows; HTTP 403/429; and core remaining at 250. Add interface-adversary cases that pass an endpoint as a third argument, try `--endpoint`, set plausible endpoint environment variables, and preseed a favorable endpoint in the output file; every local attempt must either be rejected or overwritten by an endpoint derived inside the current invocation. Bound the recorded endpoint between test timestamps taken immediately before and after the call, and prove local output remains non-authoritative with a null verdict even for 10/11 rows. Drive the production script rather than a test-only calculator.

    Implement `capture-fast-01-remeasurement.sh` with immutable repository `szTheory/sigra`, workflow `ci.yml`, event `pull_request`, cutoff SHA `a282b3deed009e62707b1a01d16da053a53e37d8`, and cutoff timestamp `2026-08-03T15:36:12Z`. Before collection, prove the SHA is on `origin/main`, its commit time matches the constant, and the committed diff is topology-affecting because it integrates the retained offline proof into required `fast_checks`; never select a cutoff from observed timing results. Define exactly two non-interchangeable interfaces: local `--readiness OUTPUT`, which rejects further arguments and derives its endpoint internally from the absolute `/usr/bin/date` UTC command immediately before page 1; and a protected-output path used only by the input-free protected workflow with that workflow's independently generated UTC start endpoint. Do not read an endpoint/cutoff from environment variables or existing output bytes. Reuse the explicit bounded page=N/terminal-empty manifest rules from the terminal collector, retain sanitized public run fields, reject chronology inversions before duration calculation, and preserve every conclusion.

    Produce a closed JSON schema `sigra.fast-01-remeasurement-readiness/v1` containing `authority=readiness_only`, `endpoint_source=collector_current_utc`, cutoff identity, internally derived endpoint, exact invocation, source API path, requested pages/exhaustion, rate-limit remaining/reset, exact run rows, eligible count, `statistics=null`, `verdict=null`, status, and diagnostics. For fewer than ten terminal rows set `status=insufficient_population`; at ten or more set `status=ready`. Atomically retain the artifact in either branch. The local mode never calculates or serializes a pass/miss and its endpoint can never be copied into the protected subject. Separately retain the protected-output schema behavior: it computes/reconciles queue-inclusive statistics using existing metric semantics and sets verdict to `pass` only when p50 is strictly below 720; a measured miss remains `miss` and cannot be recollected with another cutoff or filtered population.

    Add the ExUnit contract that pins the two interface schemas, exact cutoff SHA/time, source identities, readiness authority/endpoint derivation/minimum count, protected stable median/strict comparator, and historical Phase 235/GATE-05 invariants. Run exactly `bash scripts/ci/capture-fast-01-remeasurement.sh --readiness .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json` after `gh api rate_limit` reports core remaining above 250; at planning time the authoritative query found four post-cutoff terminal PR runs, so an unchanged external population must produce `insufficient_population`, not a fabricated verdict. Do not dispatch CI, change workflow topology, edit the old ledger/requirements/residual, or optimize either recorded binding pole in this task, per D-08.
  </action>
  <verify>
    <automated>bash scripts/ci/capture-fast-01-remeasurement.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; jq -e '.schema_version == "sigra.fast-01-remeasurement-readiness/v1" and .authority == "readiness_only" and .endpoint_source == "collector_current_utc" and .statistics == null and .verdict == null and ((.eligible_pr_run_count &lt; 10 and .status == "insufficient_population") or (.eligible_pr_run_count &gt;= 10 and .status == "ready"))' .planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json</automated>
  </verify>
  <acceptance_criteria>
    - The production collector and its fake-gh suite prove complete pagination, all-conclusion retention, chronology, 0/1/9/10/11 boundaries, stable ties, and strict 719/720/721 behavior.
    - The cutoff is exactly the protected-main SHA/time above, while local `--readiness OUTPUT` derives its current UTC endpoint internally and rejects/ignores every command-line, environment, or preexisting-output attempt to choose an endpoint or favorable upper bound.
    - The live readiness artifact contains the real current count/IDs, `authority=readiness_only`, null statistics/verdict, and either insufficient_population or ready; it cannot become the protected measured subject.
    - The historical terminal ledger remains 19/772/miss and the existing offline GATE-05 proof still passes.
    - No CI dispatch, topology optimization, test deletion, timeout/retry change, or unrelated todo occurs.
  </acceptance_criteria>
  <done>A maintainer has one deterministic artifact saying whether the independent population is usable; an undersized population is durably blocked and cannot masquerade as FAST-01 evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put the fresh-window collector behind protected-main attestation</name>
  <files>.github/workflows/fast-01-remeasurement-evidence.yml, test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs, .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md</files>
  <read_first>
    - `.github/workflows/terminal-ratification-evidence.yml` — proven main-only permissions, pinned checkout/attestation/upload actions, bounded timeout, and no ci.yml edge.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md` — existing external-evidence API inventory; extend it rather than creating another coverage matrix.
    - The Task 1 collector/test/contract — preserve its fixed cutoff and readiness semantics.
    - `.github/workflows/ci.yml` — prove the new evidence workflow is not a required job, aggregate dependency, or source of the measured PR population.
    - `.planning/phases/235-terminal-ratification-measured-not-read/235-08-SUMMARY.md` — protected provenance and offline verification precedent.
  </read_first>
  <behavior>
    - "The workflow has workflow_dispatch only, no inputs, and its sole job requires refs/heads/main."
    - "The job grants only contents:read, actions:read, id-token:write, and attestations:write; all external actions are immutable-SHA pinned."
    - "The workflow records its UTC start as the endpoint, invokes the fixed-cutoff collector, requires at least ten rows, attests the exact output, and uploads that same subject."
    - "The evidence workflow is absent from ci.yml and every aggregate needs list, so it neither generates qualifying PR rows nor changes GATE-05 ownership."
    - "Coverage records rate-limit preflight, paginated run reads, dispatch/run correlation, one 60-second watcher, one summary, failure-only logs, artifact/bundle/root retrieval, and offline verification."
  </behavior>
  <action>
    Create `fast-01-remeasurement-evidence.yml` using the protected terminal workflow as the repository analog. Use `workflow_dispatch` without inputs, gate the only job to `refs/heads/main`, set a bounded timeout, and grant only the four proven permissions. Pin checkout, `actions/attest-build-provenance`, and upload-artifact to the same immutable SHAs already accepted by the Phase 235 contract. Independently derive the protected UTC endpoint from workflow execution; do not read or reuse the local readiness endpoint. Call the collector's protected-output path with `GH_TOKEN: github.token`, require `eligible_pr_run_count &gt;= 10`, require `status=measured`, attest the exact JSON subject, and upload that exact subject. An undersized population must fail before attestation with its diagnostic retained in logs; it must not dispatch synthetic CI runs to increase n.

    Extend the focused contract with RED then GREEN workflow mutations for caller-controlled cutoff/endpoint, reuse of the local readiness endpoint/artifact as the protected subject, non-main ref, broader permissions, mutable action references, missing minimum-count gate, changed subject between attestation/upload, and accidental ci.yml/aggregate linkage. Prove the local artifact can gate only on count and that only the independently endpointed protected subject can carry the verdict. Extend the existing `235-COVERAGE.md` external-evidence section with the precise new workflow-runs request, authentication actor, pagination/readiness tests, later dispatch correlation, single-watcher rule, artifact/attestation/trusted-root retrieval, and 403/429/core-budget hard stops. Do not create a second coverage document or API matrix.
  </action>
  <verify>
    <automated>actionlint .github/workflows/fast-01-remeasurement-evidence.yml &amp;&amp; bash scripts/ci/capture-fast-01-remeasurement.test.sh &amp;&amp; MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs &amp;&amp; bash scripts/ci/verify-terminal-ratification-attestation-offline.sh &amp;&amp; grep -q 'fast-01-remeasurement-evidence.yml' .planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - The protected workflow is main-only, input-free, least-privilege, action-SHA-pinned, and attests/uploads one byte-identical fresh-window subject.
    - The workflow fails before attestation when fewer than ten terminal PR runs exist and never creates qualifying runs itself.
    - Contract mutations reject every authority, cutoff, count, subject, and CI-DAG regression named in `<behavior>`.
    - The existing coverage document—not a new matrix—describes every added external evidence call and AGENTS.md control.
    - Existing GATE-05 offline verification and focused Phase 235 contracts remain green.
  </acceptance_criteria>
  <done>The independent FAST-01 population can be captured once by protected main when ready, without moving its cutoff or perturbing the CI gate being measured.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/ci/capture-fast-01-remeasurement.sh` — fixed-cutoff collector with non-authoritative `--readiness OUTPUT` self-endpointing and a separate protected-workflow output path.
- `scripts/ci/capture-fast-01-remeasurement.test.sh` — hermetic pagination, count-boundary, chronology, rate-limit, and strict-median fixtures.
- `.github/workflows/fast-01-remeasurement-evidence.yml` — new main-only attestation producer with no inputs and no ci.yml dependency.
- `test/sigra/planning/phase_235_fast_01_remeasurement_contract_test.exs` — new fresh-window/provenance/non-regression contract.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-FAST-01-REMEASUREMENT-READINESS.json` — new machine-readable readiness state; no requirement status change.
- `.planning/phases/235-terminal-ratification-measured-not-read/235-COVERAGE.md` — extends the existing external-evidence inventory; no second coverage matrix.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Prove the remaining headline claim from run data | 235-09..10 | COVERED | This plan creates honest readiness/protected capture; Plan 10 judges only a qualifying receipt. |
| REQ | FAST-01 | Strict under-12-minute p50 over at least ten post-change PR runs | 235-09..10 | COVERED | Fixed fresh cutoff, minimum count, exact semantics, and protected verdict path. |
| REQ | GATE-05 | Existing single ownership artifact remains complete | 235-09..10 | COVERED | Explicit non-regression invariant and existing offline contract; no ownership re-audit. |
| RESEARCH | — | Wall mode, all conclusions, floor(n/2), exact IDs/commands | 235-09 | COVERED | Production collector and contract use the existing semantics. |
| RESEARCH | — | GitHub rate-limit/watcher controls and protected provenance | 235-09..10 | COVERED | Coverage, workflow, and later dispatch task carry exact controls. |
| CONTEXT | D-01 | Immutable cutoff and at least ten PR runs | 235-09 | COVERED | Cutoff is the latest protected topology-affecting main commit; under-ten is blocking. |
| CONTEXT | D-02 | Queue-inclusive, all-conclusion, stable p50 | 235-09 | COVERED | Hermetic boundaries and live readiness use the same contract. |
| CONTEXT | D-03 | Exact commands/IDs and honest pass/miss | 235-09..10 | COVERED | Readiness has exact rows; protected receipt alone can advance status. |
| CONTEXT | D-04–D-05 | Ownership artifact and inventory remain authoritative | 235-09..10 | COVERED | Preserved and verified, not rebuilt. |
| CONTEXT | D-06–D-07 | Contributor/closeout truth remains ledger-backed | 235-10 | COVERED | Plan 10 changes only FAST-01 status/residual after protected proof. |
| CONTEXT | D-08 | No pending todos or unrelated remediation | 235-09..10 | COVERED | Binding-pole optimization remains separately owned and absent. |
| VERIFICATION | FAST-01 gap | Future independent population below 720 | 235-09..10 | COVERED | Readiness blocks until n≥10; Plan 10 captures and judges exactly once. |

Deferred ideas, prior-phase gaps, product/UI/release work, test deletion, topology optimization, timeout/retry changes, schema changes, package installs, and unrelated todos are excluded. Spec-less fallback probes remain explicit in `must_haves`; no requirement or prohibition is silently dropped. External API detection is true only for GitHub evidence collection, so the existing `235-COVERAGE.md` is extended rather than creating another matrix.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Protected origin/main history → cutoff contract | A movable or outcome-selected cutoff could manufacture a favorable population. |
| GitHub workflow-run pages → readiness/verdict | Untrusted remote rows determine minimum count and performance result. |
| Protected evidence workflow → future receipt | Signer/ref/subject identity determines whether local evidence is independently attributable. |
| Fresh FAST-01 path → existing GATE-05 proof | New measurement work must not replace or weaken the completed ownership proof. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-09-01 | Tampering | fresh cutoff/window | high | mitigate | Pin full SHA/time before outcome, require protected-main ancestry, expose no cutoff/endpoint inputs, and test mutations. |
| T-235-09-02 | Tampering | run population/statistics | high | mitigate | Prove terminal-empty pagination, exact unique rows, chronology, all conclusions, n≥10, stable ordering, and strict comparator. |
| T-235-09-03 | Spoofing | protected producer identity | high | mitigate | Main-only workflow, least privilege, immutable action pins, exact attested/uploaded subject, and no branch/local authority. |
| T-235-09-04 | Repudiation | insufficient population | high | mitigate | Retain exact count, IDs, endpoint, command, rate state, and null verdict in a machine-readable blocking artifact. |
| T-235-09-05 | Denial of service | GitHub collection | medium | mitigate | One rate-limit preflight, finite page loop, no watcher in this plan, one transient retry only where allowed, and hard stop on 403/429 or core≤250. |
| T-235-09-06 | Information disclosure | evidence artifacts | low | accept | Only public run metadata, commands, digests, and diagnostics are retained; tokens, headers, cookies, logs, and environment state are excluded. |
| T-235-09-07 | Tampering | GATE-05 proof | high | mitigate | Run the existing offline verifier and contract; forbid edits to the protected ownership artifacts and GATE-05 status. |

ASVS L1 V4/V5 controls are deterministic and block all high-severity threats. No application auth, session, database, schema, product API, UI, or package trust boundary changes.
</threat_model>

<verification>
Run the hermetic collector suite, focused fresh-window ExUnit contract, existing metrics contract, actionlint, and existing offline GATE-05 verifier. Confirm the live readiness artifact has exact run identities, an internally derived endpoint, null statistics/verdict, and either insufficient_population or ready status. Confirm only the protected schema can be measured, the old ledger remains 19/772/miss, and GATE-05 remains Complete.
</verification>

<success_criteria>
- A fixed, non-outcome-selected window has a deterministic readiness artifact and protected capture producer.
- Fewer than ten runs is durably blocked with no FAST-01 verdict; ten or more uses exact D-01/D-02/D-03 semantics.
- The producer cannot alter the cutoff, manufacture qualifying runs, or join the gate it measures.
- GATE-05 remains independently verified and unchanged.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-09-SUMMARY.md` when done.
</output>
