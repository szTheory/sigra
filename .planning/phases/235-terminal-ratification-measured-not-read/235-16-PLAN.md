---
phase: 235-terminal-ratification-measured-not-read
plan: 16
type: execute
wave: 10
depends_on: [235-15]
files_modified:
  - scripts/ci/ci-run-metrics.sh
  - scripts/ci/ci-run-metrics.test.sh
  - scripts/ci/capture-fast-01-gap-closure.sh
  - scripts/ci/capture-fast-01-gap-closure.test.sh
  - .github/workflows/fast-01-gap-closure-evidence.yml
  - scripts/ci/verify-fast-01-source-complete-attestation-offline.sh
  - test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs
autonomous: true
gap_closure: true
requirements: [FAST-01, GATE-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-02, scripts/ci/ci-run-metrics.sh in wall mode is the sole authoritative implementation of bounded membership, queue-inclusive duration, stable ordering, floor-median, and terminal statistics; the protected subject retains every raw input needed for independent comparison replay."
    - "The evidence workflow cannot be dispatched until the source-complete collector, workflow assertion, offline verifier policy, and adverse contracts are merged on protected main."
    - "Per D-03 and D-07, a passing-looking derived value never changes FAST-01 until the signed source population is independently verified."
    - "Per D-04 through D-06, the completed GATE-05 receipt, 93-row ownership ledger, contributor topology, and requirement status remain unchanged."
  artifacts:
    - path: "scripts/ci/ci-run-metrics.sh"
      provides: "Sole authoritative terminal membership/statistics implementation with a source-page input seam and median/maximum run selection"
    - path: "scripts/ci/capture-fast-01-gap-closure.sh"
      provides: "Source-complete protected subject writer"
    - path: ".github/workflows/fast-01-gap-closure-evidence.yml"
      provides: "Main-only source-complete capture, assertion, attestation, and upload path"
    - path: "scripts/ci/verify-fast-01-source-complete-attestation-offline.sh"
      provides: "Fixed-path network-denied provenance and source-population verifier"
    - path: "test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs"
      provides: "Repository contract for the new evidence schema and GATE-05 isolation"
  key_links:
    - from: "GitHub workflow-run pages"
      to: "source_collection.pages[*].runs[*]"
      via: "capture-fast-01-gap-closure.sh retains every returned row before deriving eligible runs"
    - from: "source_collection"
      to: "runs/statistics/verdict"
      via: "scripts/ci/ci-run-metrics.sh wall-mode source-page input produces the authoritative population, canonical {wall_seconds, run_id} order, p50, and verdict"
    - from: "scripts/ci/ci-run-metrics.sh output"
      to: "source_collection and binding_poles"
      via: "the collector records the instrument command/output and fetches bounded job/step pages for the instrument-selected median and maximum only on a miss"
    - from: ".github/workflows/fast-01-gap-closure-evidence.yml"
      to: "protected main"
      via: "scoped PR, required CI, merge, and origin/main blob confirmation before any dispatch"
  prohibitions:
    - "MUST NOT dispatch before readiness, rerun for a favorable p50, omit non-success conclusions, move the remediation cutoff, weaken the strict threshold, combine populations, or overwrite either historical miss."
    - "MUST NOT couple FAST-01 reconciliation to any downgrade, replacement, or reopening of the protected ownership proof."
---

<objective>
Replace the evidentially hollow protected FAST-01 subject contract with a source-complete contract and land that exact collection path on protected main before any new measurement.

Purpose: Repair the cause of CR-02 rather than reinterpret run 34272746647: its signature authenticates derived values, but the subject discarded timestamps and pagination/exhaustion facts.
Output: A hermetically tested collector/workflow/verifier contract whose signed subject can be replayed independently, plus proof that the exact path is present on protected main.

Decision coverage: D-01: fixed bounded window and minimum population; D-02: queue-inclusive source-first median semantics; D-03: exact honest evidence; D-04: protected ownership-artifact preservation; D-05: inventory/receipt non-regression; D-06: contributor-topology preservation; D-07: evidence-limited closeout; D-08: fixed scope with no unrelated work.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-CONTEXT.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-RESEARCH.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-VERIFICATION.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-15-EXECUTION-DIAGNOSTICS.md
@.planning/phases/235-terminal-ratification-measured-not-read/235-15-SUMMARY.md
@scripts/ci/capture-fast-01-gap-closure.sh
@scripts/ci/capture-fast-01-gap-closure.test.sh
@.github/workflows/fast-01-gap-closure-evidence.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one raw workflow page through capture, derivation, workflow assertion, and offline policy (D-01, D-02, D-03)</name>
  <reversibility rating="costly">The signed evidence wire format coordinates collector, protected workflow, retained artifact, verifier, and tests, but no published product API or stored application data depends on it.</reversibility>
  <files>scripts/ci/ci-run-metrics.sh, scripts/ci/ci-run-metrics.test.sh, scripts/ci/capture-fast-01-gap-closure.sh, scripts/ci/capture-fast-01-gap-closure.test.sh, .github/workflows/fast-01-gap-closure-evidence.yml, scripts/ci/verify-fast-01-source-complete-attestation-offline.sh, test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</files>
  <read_first>
    - `235-VERIFICATION.md` gap and CR-02 rationale — exact missing source facts and the prohibition on treating run 34272746647 as closure.
    - `capture-fast-01-gap-closure.sh` and its shell test — current `MANIFEST` validation, readiness output, and protected branch that discards raw facts.
    - `fast-01-gap-closure-evidence.yml` — current main-only capture/assert/attest/upload sequence.
    - `verify-fast-01-gap-closure-attestation-offline.sh` and `phase_235_fast_01_gap_closure_contract_test.exs` — fixed-path isolation, provenance binding, stable ordering, strict comparator, and GATE-05 digest-pin patterns.
    - `235-15-EXECUTION-DIAGNOSTICS.md` — immutable rejected run, subject digest, and no-reroll record.
  </read_first>
  <behavior>
    - "A signed source page retains its one-based page identity, returned row count, and every returned run's run_id, url, event, conclusion, created_at, and updated_at; the final requested page is retained with count zero and exhausted=true."
    - "Eligible rows are derived only from retained source rows with event pull_request, a non-empty terminal conclusion, and created_at inside the inclusive [2026-08-03T21:37:08Z, endpoint] interval."
    - "updated_at equal to created_at is a valid zero-second interval; updated_at before created_at is rejected. Equal-duration runs remain separate and sort by ascending numeric run_id before floor(n/2)."
    - "Zero through nine eligible rows produce insufficient_population in readiness and cannot produce an attestable measured subject; ten or more can produce pass only when independently recomputed p50 is strictly below 720."
    - "The subject records the exact scripts/ci/ci-run-metrics.sh wall-mode command and output as the authoritative membership/statistics result; an independent source-first oracle must match it but never replaces it."
    - "On a miss, the subject retains contiguous, explicitly exhausted raw job pages including steps for the instrument-selected median and maximum runs, with exact run/job/step linkage and no aggregate-only pole claim."
    - "Missing, repeated, non-contiguous, non-terminal, or post-terminal pages; duplicate run IDs; source/derived disagreement; omitted failure or cancelled conclusions; and 720-as-pass fail with named diagnostics."
  </behavior>
  <action>
    Start RED in the shell and ExUnit contracts with one two-page fake GitHub response plus the empty terminal page. Extend `scripts/ci/ci-run-metrics.sh` and its existing hermetic test with a file/stdin source-page input seam that preserves the current default GitHub path and default wall mode. The instrument must own closed-window membership, all-conclusion retention, queue-inclusive `updatedAt - createdAt`, canonical `{wall_seconds, run_id}` ordering, floor(n/2), statistics, strict threshold comparison, and selection of the median and maximum run IDs. Its machine output must expose the authoritative eligible rows and selected pole IDs in addition to the existing statistics without changing legacy invocations. Do not implement those decisions in the collector.

    Change the protected writer to emit schema `sigra.fast-01-source-complete-remeasurement/1` at `fast-01-source-complete-remeasurement.json`. Add `source_collection` with the literal REST resource, `created` query bounds, `per_page: 100`, contiguous `requested_pages`, `terminal_page`, `exhausted: true`, and `pages`; each page records `page`, `returned_count`, and all source run fields. Pass those retained pages to `scripts/ci/ci-run-metrics.sh` in wall mode and store its exact command/output as `instrument_receipt`; copy the instrument-emitted `runs`, `eligible_pr_run_count`, statistics, selected pole IDs, strict verdict, and status into the subject without independently calculating them in the producer.

    When the instrument reports a miss, collect the median and maximum runs' job endpoints with bounded `per_page: 100` pagination through an empty terminal page. Retain each page's identity/count and each job's database ID, run ID, name, conclusion, started/completed timestamps, and complete ordered step array (step number/name/status/conclusion/started/completed timestamps). Store these under `binding_poles.median` and `binding_poles.maximum`, keyed to the instrument-selected run IDs. Add hermetic multi-page, missing-page, wrong-run, wrong-job, wrong-step, duplicate, nonterminal, and chronology mutations. A pass requires no pole pages; a miss without both complete pole receipts is invalid per D-03.

    Make the workflow's assertion require the source-complete schema, a non-empty page sequence ending in an empty terminal page, an exact `instrument_receipt`, at least 10 eligible rows, pass-or-miss, and complete median/maximum job-and-step receipts on a miss without changing its main-only trigger, permissions, immutable action pins, or separation from `ci.yml`. Add the fixed-path offline verifier now, using the existing network-denied `gh attestation verify` pattern and explicit placeholders that fail closed until Plan 17 installs the new subject digest, trusted-root digest, workflow SHA, and endpoint. Its semantic validator first validates the signed raw pages and instrument receipt, then independently rebuilds membership, queue-inclusive durations, all-conclusion counts, `{wall_seconds, run_id}` order, p50, and verdict solely as a comparison oracle. Require exact equality with the recorded output from `scripts/ci/ci-run-metrics.sh`; never treat the verifier's implementation as terminal authority. On a miss, also verify median/maximum run selection, page exhaustion, and run→job→step identity/chronology linkage offline.

    Per D-04, D-05, and D-06, leave `235-PROTECTED-RECEIPTS.json`, `235-TERMINAL-RATIFICATION.json`, the original terminal verifier/test, `CONTRIBUTING.md`, and GATE-05 requirement text untouched. Per D-08, make no `ci.yml`, UI, schema, package, retry, timeout, test-universe, or unrelated todo change.
  </action>
  <acceptance_criteria>
    - One hermetic raw page flows through the exact production writer into source and derived sections, and independent recomputation returns the same n/p50/verdict.
    - Closed-boundary equality, zero duration, stable equal-duration ordering, empty/undersized populations, every page-shape fault, omitted terminal conclusions, chronology inversion, and 719/720/721 are deterministic test cases.
    - The workflow attests/uploads only the source-complete artifact and remains main-only and independent of `ci.yml`.
    - The recorded terminal result demonstrably originates from scripts/ci/ci-run-metrics.sh in wall mode, and the independent replay agrees byte-for-value without becoming an alternate authority.
    - A miss includes signed, exhaustively paginated median and maximum job/step receipts linked to the instrument-selected runs; malformed linkage or incomplete pages fail.
    - The offline verifier has fixed retained paths, network denial, repository/signer/ref/digest policy, and source-first semantic validation; unset capture-specific pins fail closed.
    - Existing GATE-05 evidence and all historical FAST subjects remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/ci-run-metrics.test.sh &amp;&amp; bash scripts/ci/capture-fast-01-gap-closure.test.sh &amp;&amp; ASDF_ERLANG_VERSION=28.4.1 MIX_ENV=test mix test test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs test/sigra/planning/phase_235_terminal_ratification_contract_test.exs &amp;&amp; git diff --check -- scripts/ci/ci-run-metrics.sh scripts/ci/ci-run-metrics.test.sh scripts/ci/capture-fast-01-gap-closure.sh scripts/ci/capture-fast-01-gap-closure.test.sh .github/workflows/fast-01-gap-closure-evidence.yml scripts/ci/verify-fast-01-source-complete-attestation-offline.sh test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</automated>
    <fails_when>non-zero exit; the shell suite omits its PASS banner; ExUnit reports 0 tests or failures; or git reports whitespace errors</fails_when>
  </verify>
  <done>The protected path can sign a source-complete bounded population whose membership, durations, order, p50, and verdict are independently derivable, while the completed ownership proof is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Land the exact source-complete evidence path on protected main before dispatch</name>
  <precondition>The Task 1 focused shell and ExUnit contracts pass and the seven declared files are committed on a scoped evidence branch.</precondition>
  <reversibility rating="reversible">The isolated evidence workflow and scripts can be reverted without changing required CI topology or product behavior.</reversibility>
  <files>scripts/ci/ci-run-metrics.sh, scripts/ci/ci-run-metrics.test.sh, scripts/ci/capture-fast-01-gap-closure.sh, scripts/ci/capture-fast-01-gap-closure.test.sh, .github/workflows/fast-01-gap-closure-evidence.yml, scripts/ci/verify-fast-01-source-complete-attestation-offline.sh, test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</files>
  <read_first>
    - `AGENTS.md` Automation-first verification and GitHub API usage sections — scoped PR authority, one-watcher rule, rate-limit preflight, and 403/429 stop behavior.
    - Task 1 commit and the seven files in `<files>` — exact bytes that must reach protected main.
    - Live repository ruleset/required checks — merge prerequisites; do not infer protection from workflow YAML.
  </read_first>
  <action>
    Push the scoped evidence branch and create or update exactly one PR containing the Task 1 commit. Before any long watch, query `gh api rate_limit`; if core remaining is 250 or less, or GitHub returns 403/429, stop until the reported reset/retry time. Use at most one watcher for each workflow run and, when needed, the exact command `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`; fetch one structured summary after completion and failed logs only for a failure. Diagnose and repair deterministic failures on the same scoped branch, retry one non-rate-limit transient failure once, satisfy the live required checks, enable auto-merge when allowed, and confirm the PR merge commit is on `origin/main`.

    After merge, fetch `origin/main` and prove its blobs for the authoritative metrics script/test, collector, workflow, verifier, and source-complete contract match the tested commit. Record the protected-main merge SHA in the plan summary for Plan 17's read-only precondition. Do not dispatch `fast-01-gap-closure-evidence.yml` in this task; landing the corrected producer before collection is the invariant this task establishes.
  </action>
  <acceptance_criteria>
    - Exactly one scoped PR carries the tested evidence-path change and reaches protected main through live required checks.
    - GitHub polling follows the repository's 60-second watcher, single-summary, failure-logs-only, rate-limit, and transient-retry rules.
    - The protected-main merge SHA and blob equality for all seven files are retained in the summary.
    - No evidence workflow dispatch occurs during this plan.
  </acceptance_criteria>
  <verify>
    <automated>git fetch origin main &amp;&amp; git merge-base --is-ancestor HEAD origin/main &amp;&amp; git diff --exit-code HEAD origin/main -- scripts/ci/ci-run-metrics.sh scripts/ci/ci-run-metrics.test.sh scripts/ci/capture-fast-01-gap-closure.sh scripts/ci/capture-fast-01-gap-closure.test.sh .github/workflows/fast-01-gap-closure-evidence.yml scripts/ci/verify-fast-01-source-complete-attestation-offline.sh test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs</automated>
    <fails_when>fetch fails; HEAD is not an ancestor of origin/main; or any declared protected-main blob differs from the tested commit</fails_when>
  </verify>
  <done>The exact source-complete producer and verification contract are present on protected main, making one new protected measurement safe to authorize.</done>
</task>

</tasks>

## Flagged assumptions

- **FAST-01 adjacency:** Resolved by an explicit closed `created_at` interval: a run exactly equal to either bound is included; equal-duration runs do not merge or collide and remain separate rows ordered by numeric `run_id`; duplicate identities collide and fail.
- **FAST-01 empty/degenerate:** Resolved by explicit behavior: empty through nine-row populations cannot be attested as measured, while a zero-second run interval is valid only when `updated_at == created_at` and the total eligible population is at least 10.
- **FAST-01 ordering/stability:** Resolved by deterministic ascending `{wall_seconds, run_id}` ordering before selecting index `floor(n/2)`.
- **GATE-05 unclassified:** Remains flagged for maintainer judgment; this plan neither reopens nor re-ratifies it. Byte/digest pins plus the existing offline verifier and 93-row contract are the automated non-regression backstop.

## Artifacts this phase produces

- `scripts/ci/capture-fast-01-gap-closure.sh` — protected output schema `sigra.fast-01-source-complete-remeasurement/1`; new `source_collection`, `query`, `requested_pages`, `terminal_page`, `exhausted`, `pages`, `returned_count`, and raw `created_at`/`updated_at` fields; derived rows retain timestamps and `wall_seconds`.
- `scripts/ci/ci-run-metrics.sh` and `scripts/ci/ci-run-metrics.test.sh` — source-page input seam plus authoritative emitted membership/statistics and selected median/maximum IDs, preserving legacy default wall-mode behavior.
- `scripts/ci/capture-fast-01-gap-closure.test.sh` — source-retention, closed-boundary, zero-duration, undersized-population, page-integrity, chronology, stable-order, and strict-comparator cases.
- `.github/workflows/fast-01-gap-closure-evidence.yml` — source-complete output filename, assertion, attestation subject, and upload artifact; trigger/topology unchanged.
- `scripts/ci/verify-fast-01-source-complete-attestation-offline.sh` — fixed-path offline verifier with fail-closed capture pins and source-first recomputation.
- `test/sigra/planning/phase_235_fast_01_source_complete_contract_test.exs` — new schema/workflow/verifier/GATE-isolation contract.
- Protected-main merge SHA recorded in `235-16-SUMMARY.md` — prerequisite evidence for Plan 17; no runtime field or flag.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Prove headline claims from run data | 235-16 | COVERED | Builds the source-complete protected producer; does not claim the target yet. |
| GOAL | — | Preserve maintainer visibility into moved tests | 235-16 | COVERED | Existing GATE-05 artifact is immutable input with automated non-regression. |
| REQ | FAST-01 | At least 10 post-change PR runs, strict p50 below 720 | 235-16 | COVERED | Source schema and derivation contract precede capture. |
| REQ | GATE-05 | Single before/after no-drop artifact | 235-16 | COVERED | Existing 93-row proof and status remain unchanged and tested. |
| RESEARCH | — | Queue-inclusive wall time, all conclusions, floor median, exact provenance | 235-16 | COVERED | Raw timestamps/pages are signed; the mandated instrument emits the authoritative result and offline replay is comparison-only. |
| RESEARCH | — | Protected workflow must precede protected evidence | 235-16 | COVERED | Task 2 lands exact tested bytes on main with blob proof. |
| CONTEXT | D-01–D-03 | Fixed window, baseline semantics, exact honest evidence | 235-16 | COVERED | Closed bounds feed the mandated wall-mode instrument; miss poles retain linked raw job/step pages; no status edit. |
| CONTEXT | D-04–D-05 | Preserve complete ownership proof | 235-16 | COVERED | Protected GATE files and 93-row contract remain byte-exact. |
| CONTEXT | D-06 | Preserve truthful contributor topology | 235-16 | COVERED | CONTRIBUTING is not edited and its existing contract remains an input. |
| CONTEXT | D-07 | Close only to extent supported | 235-16 | COVERED | This plan changes evidence capability only; it makes no FAST closure claim. |
| CONTEXT | D-08 | No unrelated work | 235-16 | COVERED | No required CI, UI, schema, package, retry, timeout, or unrelated todo change. |
| VERIFICATION | CR-02 | Signed source timestamps and exhaustive pagination | 235-16 | COVERED | Direct root-cause target. |
| PROBE | FAST adjacency/empty/order | Define equality, undersized, and stable-order semantics | 235-16 | COVERED | Explicit behaviors and adverse tests above. |
| PROBE | GATE-05 unclassified | Review manually | 235-16 | COVERED | Explicit flagged assumption; automated checks are backstop only. |

No authoritative source item is missing. Deferred ideas remain excluded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub REST pages → subject | Mutable authenticated API responses cross into a durable signed artifact. |
| Subject source → derived verdict | Producer calculations must not substitute for independently replayable source facts. |
| Evidence branch → protected main | The producer must land through repository protection before it can create authoritative evidence. |
| FAST repair → GATE-05 proof | A performance-evidence change must not mutate the completed ownership proof. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-235-16-01 | Tampering | source_collection | high | mitigate | Sign raw page identities, counts, every source row, and terminal exhaustion; reject all page and source/derived mutations. |
| T-235-16-02 | Repudiation | FAST verdict | high | mitigate | Recompute window membership, duration, ordering, n, p50, and strict verdict from authenticated source fields. |
| T-235-16-03 | Spoofing | protected producer | high | mitigate | Require exact repository, signer workflow, source ref, workflow SHA, subject digest, and trusted-root digest under network denial. |
| T-235-16-04 | Tampering | protected-main landing | high | mitigate | Merge through live required checks, then compare all five tested blobs against origin/main before dispatch. |
| T-235-16-05 | Denial of service | GitHub operations | medium | mitigate | Preflight REST budget, use one 60-second watcher, one summary, failure-only logs, one transient retry, and hard-stop on 403/429 or core at/below 250. |
| T-235-16-06 | Information disclosure | committed evidence | low | accept | The subject retains public workflow metadata and cryptographic material only; credentials, headers, cookies, proxies, and raw authenticated state are excluded. |
| T-235-16-07 | Tampering | GATE-05 artifacts | high | mitigate | Keep original protected files immutable and run the existing terminal contract alongside the new source contract. |
</threat_model>

<verification>
Run the hermetic collector suite and focused ExUnit contracts, then prove the exact tested evidence-path blobs reached protected main without dispatching the workflow.
</verification>

<success_criteria>
- The new signed-subject contract retains enough raw data to derive population membership, chronology, queue-inclusive duration, completeness, stable ordering, p50, and verdict independently.
- Every adjacency, empty/undersized, ordering, pagination, chronology, and strict-threshold edge has a deterministic adverse test.
- The exact collector/workflow/verifier/tests are on protected main before measurement.
- GATE-05 remains Complete and byte-stable.
</success_criteria>

<output>
Create `.planning/phases/235-terminal-ratification-measured-not-read/235-16-SUMMARY.md` when done.
</output>
