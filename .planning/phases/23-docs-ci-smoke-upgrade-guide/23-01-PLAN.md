---
phase: 23-docs-ci-smoke-upgrade-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/introduction/getting-started.md
  - guides/introduction/upgrading-to-v1.1.md
  - guides/recipes/multi-tenant.md
  - guides/recipes/passkeys.md
  - mix.exs
  - test/sigra/guides_dx02_test.exs
  - test/upgrade_test.exs
autonomous: true
requirements:
  - DX-03
  - DX-04
  - DX-05
  - DX-06
  - DX-08
must_haves:
  truths:
    - "`getting-started.md` extends the existing happy-path walkthrough with one organizations-and-passkeys continuation instead of becoming a reference dump."
    - "HexDocs publishes an operational v1.0 to v1.1 upgrade guide and two recipe guides that reflect the shipped logical-org and passkey behavior."
    - "Organizations and passkeys are documented as the default install posture, with opt-out flags called out as exceptions."
    - "`mix docs --warnings-as-errors` remains green after the new docs are wired into `mix.exs`."
    - "The upgrade guide's command sequence matches an exercised upgrade test path instead of speculative prose."
  artifacts:
    - "guides/introduction/getting-started.md contains an Organizations & Passkeys continuation per D-01, D-02, and D-03."
    - "guides/introduction/upgrading-to-v1.1.md exists and documents both backfill modes, passkey surface, and the exact upgrade test invocation per D-04 and D-05."
    - "guides/recipes/multi-tenant.md teaches Sigra's shipped logical multi-tenancy and `for_org/2` discipline instead of the pre-v1.1 placeholder posture."
    - "guides/recipes/passkeys.md exists and covers enrollment, passkey-primary config, RP ID/origin rename playbook, and recovery posture."
    - "mix.exs adds the new guides to `extras` under the existing Introduction and Recipes groups per D-01 and D-12."
    - "test/sigra/guides_dx02_test.exs and test/upgrade_test.exs lock the new docs contract."
  key_links:
    - "`mix.exs` HexDocs extras/groups must include every new guide file so docs generation and sidebar output stay aligned."
    - "`guides/introduction/upgrading-to-v1.1.md` must mirror the command path exercised by `test/upgrade_test.exs` and `test/support/install_fixture.ex`."
    - "`guides/recipes/multi-tenant.md` must point readers at `Sigra.Organizations.Query.for_org/2` and the logical-org model already shipped in the library."
    - "`guides/recipes/passkeys.md` must describe the existing generated routes/config/runtime behavior rather than inventing a new passkey surface."
---

<objective>
Ship the Phase 23 docs surface on the existing HexDocs rails: update the fast getting-started guide, add an operational upgrade guide, replace the stale multi-tenant recipe posture, and publish a passkeys recipe without breaking the docs build.

Purpose: DX docs are part of the release gate for v1.1. They need to describe the shipped organizations and passkeys behavior accurately, with the same command path the repo already proves in automation.
Output: Four aligned guides, updated HexDocs extras/groups, and doc/upgrader regression tests that fail if the prose drifts from the code.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-docs-ci-smoke-upgrade-guide/23-CONTEXT.md
@.planning/phases/23-docs-ci-smoke-upgrade-guide/23-RESEARCH.md
@.planning/phases/23-docs-ci-smoke-upgrade-guide/23-PATTERNS.md
@.planning/phases/23-docs-ci-smoke-upgrade-guide/23-VALIDATION.md
@.planning/phases/18-backfill-organizations-generator-wiring/18-CONTEXT.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-CONTEXT.md
@.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md
@guides/introduction/getting-started.md
@guides/recipes/testing.md
@guides/recipes/multi-tenant.md
@mix.exs
@test/upgrade_test.exs
@test/support/install_fixture.ex
@test/sigra/guides_dx02_test.exs
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 23 | Docs close the shipped org/passkey DX surface without changing product behavior | 23-01 | COVERED | This plan owns guides, docs wiring, and docs/build regression.
REQ | DX-03 | Getting started org + passkey continuation | 23-01 | COVERED | Implement per D-01, D-02, D-03.
REQ | DX-04 | Upgrade guide | 23-01 | COVERED | Implement as an operational runbook per D-04 and D-05.
REQ | DX-05 | Multi-tenancy guide | 23-01 | COVERED | Replace stale pre-v1.1 posture with shipped logical MT model and `for_org/2`.
REQ | DX-06 | Passkeys guide | 23-01 | COVERED | Document the shipped routes/config/recovery behavior.
REQ | DX-08 | Docs build clean | 23-01 | COVERED | `mix.exs` + `mix docs --warnings-as-errors`.
CONTEXT | D-01 | Keep existing HexDocs taxonomy | 23-01 | COVERED | New files stay under `guides/introduction/` and `guides/recipes/`.
CONTEXT | D-02 | Keep getting-started fast and happy-path | 23-01 | COVERED | Extend instead of rewriting into a reference dump.
CONTEXT | D-03 | Default-on posture explicit | 23-01 | COVERED | Mainline commands use `mix sigra.install`; opt-outs are secondary.
CONTEXT | D-04 | Upgrade guide operational | 23-01 | COVERED | Terminal-first runbook.
CONTEXT | D-05 | Upgrade docs aligned with automation | 23-01 | COVERED | Test path mirrored in prose and regression checks.
CONTEXT | D-12 | Docs gate stays hard | 23-01 | COVERED | `mix docs --warnings-as-errors` remains task verify.
Deferred | docs taxonomy redesign, broader docs UX review | N/A | EXCLUDED | Explicitly deferred in CONTEXT.md.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Update the shipped guide set on the existing HexDocs rails</name>
  <files>guides/introduction/getting-started.md, guides/introduction/upgrading-to-v1.1.md, guides/recipes/multi-tenant.md, guides/recipes/passkeys.md, mix.exs</files>
  <read_first>.planning/phases/23-docs-ci-smoke-upgrade-guide/23-CONTEXT.md, .planning/phases/23-docs-ci-smoke-upgrade-guide/23-RESEARCH.md, .planning/phases/23-docs-ci-smoke-upgrade-guide/23-PATTERNS.md, guides/introduction/getting-started.md, guides/recipes/multi-tenant.md, guides/recipes/testing.md, mix.exs, test/upgrade_test.exs, test/support/install_fixture.ex, lib/sigra/organizations/query.ex, test/example/priv/playwright/tests/passkey-login.spec.ts</read_first>
  <action>Update `guides/introduction/getting-started.md` with one coherent `Organizations & Passkeys` continuation after the existing baseline flow per D-01, D-02, and D-03. Keep the under-30-minute walkthrough structure, numbered steps, and runnable generated-app posture; do not rewrite the entire guide into a reference manual. Create `guides/introduction/upgrading-to-v1.1.md` as an operational runbook per D-04 and D-05: require branch/backup hygiene, document both `mix sigra.upgrade --yes` and `mix sigra.upgrade --backfill-personal-orgs --yes`, mention passkeys as newly generated surface, and include the exact upgrade-test invocation already exercised in `test/upgrade_test.exs`. Rewrite `guides/recipes/multi-tenant.md` so it teaches the shipped logical multi-tenancy model, `Sigra.Organizations.Query.for_org/2`, and the explicit rejection of PG-schema-per-tenant for Sigra itself. Add `guides/recipes/passkeys.md` covering enrollment, passkey-primary config gate, RP ID/origin rename playbook, and recovery posture from phases 20-22. In `mix.exs`, wire the new files into `docs().extras` and keep them inside the existing `Introduction` / `Recipes` groups; do not create a new docs taxonomy.</action>
  <acceptance_criteria>
    - `rg -n "Organizations & Passkeys" guides/introduction/getting-started.md` finds the new continuation section.
    - `rg -n "mix sigra.install( |$)" guides/introduction/getting-started.md` finds the default-on install path, and `rg -n -- "--no-organizations|--no-passkeys" guides/introduction/getting-started.md` only finds opt-out mentions, not the primary flow.
    - `test -f guides/introduction/upgrading-to-v1.1.md` succeeds.
    - `rg -n "mix sigra.upgrade --yes|mix sigra.upgrade --backfill-personal-orgs --yes|mix test test/upgrade_test\\.exs" guides/introduction/upgrading-to-v1.1.md` finds the operational command path.
    - `rg -n "for_org/2|logical multi-tenancy|schema-per-tenant|PG schema-per-tenant" guides/recipes/multi-tenant.md` finds the shipped tenancy posture and rejection rationale.
    - `rg -n "RP ID|origin|rename|magic link|passkey_primary_enabled" guides/recipes/passkeys.md` finds the operational passkey topics.
    - `rg -n "guides/introduction/upgrading-to-v1.1.md|guides/recipes/passkeys.md" mix.exs` shows both files in `extras`.
  </acceptance_criteria>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <done>The docs tree publishes the new guide set under the existing taxonomy, the prose reflects the shipped org/passkey behavior, and the docs build remains clean.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the docs and upgrade-runbook contract in regression tests</name>
  <files>test/sigra/guides_dx02_test.exs, test/upgrade_test.exs</files>
  <read_first>.planning/phases/23-docs-ci-smoke-upgrade-guide/23-VALIDATION.md, test/sigra/guides_dx02_test.exs, test/upgrade_test.exs, guides/introduction/getting-started.md, guides/introduction/upgrading-to-v1.1.md, guides/recipes/multi-tenant.md, guides/recipes/passkeys.md, test/support/install_fixture.ex</read_first>
  <action>Extend `test/sigra/guides_dx02_test.exs` so the docs regression suite knows about `guides/introduction/upgrading-to-v1.1.md` and `guides/recipes/passkeys.md`, asserts the updated multi-tenant recipe language, and keeps the getting-started reading-budget/tested-signature posture intact per D-02 and D-12. Update `test/upgrade_test.exs` so the same command sequence documented in `upgrading-to-v1.1.md` is actually exercised: preserve the existing `--yes` / `--backfill-personal-orgs` coverage, and add any narrow assertions needed to keep the guide's exact commands and expectations truthful. Do not document or test speculative no-breaking-schema claims that the current harness cannot prove.</action>
  <acceptance_criteria>
    - `rg -n "upgrading-to-v1\\.1\\.md|passkeys\\.md" test/sigra/guides_dx02_test.exs` shows both new guide files under regression coverage.
    - `rg -n "for_org/2|logical multi-tenancy|schema-per-tenant" test/sigra/guides_dx02_test.exs` finds assertions locking the rewritten tenancy posture.
    - `rg -n "run_sigra_upgrade\\(app_dir, \\[\\]\\)|run_sigra_upgrade\\(app_dir, \\[\"--backfill-personal-orgs\"\\]\\)" test/upgrade_test.exs` finds both documented upgrade command paths.
    - `rg -n "mix sigra.upgrade --yes|backfill-personal-orgs" guides/introduction/upgrading-to-v1.1.md test/upgrade_test.exs` shows the guide and test talking about the same commands.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/guides_dx02_test.exs test/upgrade_test.exs --max-failures 1</automated>
  </verify>
  <done>Guide drift and upgrade-runbook drift both fail fast in automation, and the docs only promise flows the repo actually exercises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs prose -> developer action | Engineers will execute the documented install/upgrade commands in their own apps. |
| HexDocs config -> published docs output | New pages only ship if `mix.exs` extras/groups are correct. |
| Guide claims -> automated proof | Phase 23 must not publish guidance the repo never executes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | T | upgrade guide commands | mitigate | Mirror the exact `test/upgrade_test.exs` command path in the guide and keep regression checks on both files per D-05. |
| T-23-01-02 | I | multi-tenant and passkey docs | mitigate | Rewrite the stale tenancy recipe and add a passkeys guide anchored to shipped runtime/config behavior, avoiding speculative architecture or recovery claims. |
| T-23-01-03 | R | HexDocs publishing | mitigate | Update `mix.exs` extras/groups in the same change and require `mix docs --warnings-as-errors` to pass per D-12. |
| T-23-01-04 | E | docs drift toward opt-out posture | mitigate | Keep `mix sigra.install` as the mainline and mention `--no-organizations` / `--no-passkeys` only as opt-outs per D-03. |
</threat_model>

<verification>
Run:

```bash
mix test test/sigra/guides_dx02_test.exs test/upgrade_test.exs --max-failures 1
mix docs --warnings-as-errors
```
</verification>

<success_criteria>
Phase 23's docs are shippable: the getting-started guide stays concise, the upgrade guide is executable, the tenancy/passkey recipes match the shipped product, and the docs build plus targeted regressions are green.
</success_criteria>

<output>
After completion, create `.planning/phases/23-docs-ci-smoke-upgrade-guide/23-01-SUMMARY.md`
</output>
