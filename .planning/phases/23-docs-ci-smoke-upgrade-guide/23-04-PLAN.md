---
phase: 23-docs-ci-smoke-upgrade-guide
plan: 04
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - CONVENTIONS.md
  - .credo.exs
  - lib/sigra/credo/no_unscoped_org_query_in_lib.ex
  - test/sigra/credo/no_unscoped_org_query_in_lib_test.exs
autonomous: true
requirements:
  - DX-09
must_haves:
  truths:
    - "Phase 23 closes DX-09 explicitly instead of leaving tenant-scope discipline implied."
    - "If a scoped Credo check fits under the requirement budget, it ships and is wired into the existing custom Credo infrastructure."
    - "If the Credo spike exceeds the requirement budget, the fallback to conventions plus integration enforcement is documented explicitly in `CONVENTIONS.md`."
    - "Either outcome has automated proof, not just a prose note."
  artifacts:
    - "`CONVENTIONS.md` records the DX-09 outcome explicitly."
    - "Either `.credo.exs`, `lib/sigra/credo/no_unscoped_org_query_in_lib.ex`, and `test/sigra/credo/no_unscoped_org_query_in_lib_test.exs` exist, or the fallback path is captured with an automated regression check."
  key_links:
    - "Any new Credo check must reuse the existing `Code.ensure_loaded?(Credo.Check)` pattern and `.credo.exs` `requires` wiring already used by `Sigra.Credo.NoLogSafe2InLib`."
    - "The decision must stay anchored to `for_org/2` discipline in `CONVENTIONS.md` so the docs and static-analysis story cannot drift apart."
---

<objective>
Resolve DX-09 explicitly by running the tenant-scope enforcement spike on the existing custom-Credo rails, shipping the check if it stays within the locked budget and otherwise documenting the fallback with automated proof.

Purpose: The repo already documents `for_org/2` discipline, but the requirement says the outcome must be explicit. This plan turns that ambiguity into a concrete shipped decision.
Output: Either a budget-compliant custom Credo check wired into the existing lint setup, or a documented fallback to conventions plus integration enforcement, with automation proving whichever outcome lands.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-docs-ci-smoke-upgrade-guide/23-CONTEXT.md
@.planning/phases/23-docs-ci-smoke-upgrade-guide/23-RESEARCH.md
@.planning/phases/23-docs-ci-smoke-upgrade-guide/23-VALIDATION.md
@CONVENTIONS.md
@.credo.exs
@lib/sigra/credo/no_log_safe2_in_lib.ex
@test/sigra/credo/no_log_safe2_in_lib_test.exs
@lib/sigra/organizations/query.ex
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 23 | Make tenant-scope enforcement outcome explicit | 23-04 | COVERED | This plan owns the DX-09 closeout.
REQ | DX-09 | Time-boxed Credo check spike, ship if <=300 lines else explicit fallback | 23-04 | COVERED | Plan contains the branch condition and explicit fallback.
CONTEXT | D-07/D-08 adjacent helper discipline | 23-04 | PARTIAL | This plan does not change helper APIs; it preserves the same "explicit and narrow" style in lint/fallback wording.
RESEARCH | No tenant-scope Credo check currently exists | 23-04 | COVERED | Plan resolves the gap explicitly.
VALIDATION | Wave 0 explicit DX-09 decision needed | 23-04 | COVERED | Automated proof is required for whichever outcome lands.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the DX-09 scoped-Credo spike against the existing custom-check infrastructure</name>
  <files>.credo.exs, lib/sigra/credo/no_unscoped_org_query_in_lib.ex, test/sigra/credo/no_unscoped_org_query_in_lib_test.exs</files>
  <read_first>.planning/phases/23-docs-ci-smoke-upgrade-guide/23-RESEARCH.md, .planning/phases/23-docs-ci-smoke-upgrade-guide/23-VALIDATION.md, .credo.exs, lib/sigra/credo/no_log_safe2_in_lib.ex, test/sigra/credo/no_log_safe2_in_lib_test.exs, CONVENTIONS.md, lib/sigra/organizations/query.ex</read_first>
  <behavior>
    - Test 1: A new custom Credo check, if shipped, fires on clear `Repo.*(SchemaWithOrganizationId)` calls inside `lib/sigra/**` that omit `Sigra.Organizations.Query.for_org/2`.
    - Test 2: The check stays silent on valid `for_org/2` call sites, files outside `lib/sigra/**`, and documented exceptions comparable to the existing custom-check pattern.
    - Test 3: The combined new source for the check plus its focused test stays at or under 300 added lines, matching DX-09's ship threshold.
  </behavior>
  <action>Use the existing custom Credo pattern as the spike rail. Prototype `lib/sigra/credo/no_unscoped_org_query_in_lib.ex` guarded by `Code.ensure_loaded?(Credo.Check)` and wire it into `.credo.exs` only if the check plus its focused test file stay within 300 added lines total for the new files. Scope the checker narrowly: it should target obvious library-side `Repo.all/one/get/get_by` calls on org-scoped schemas that omit `for_org/2`, not try to solve general query proving. Mirror the current `NoLogSafe2InLib` structure for `requires`, issue formatting, and test setup in `test/sigra/credo/no_unscoped_org_query_in_lib_test.exs`. If the spike cannot stay within 300 lines without becoming misleadingly noisy or vague, do not ship a partial checker in this task; leave `.credo.exs` untouched and let Task 2 close DX-09 explicitly via the documented fallback path instead.</action>
  <acceptance_criteria>
    - If the Credo path ships: `test -f lib/sigra/credo/no_unscoped_org_query_in_lib.ex` and `test -f test/sigra/credo/no_unscoped_org_query_in_lib_test.exs` both succeed.
    - If the Credo path ships: `rg -n "no_unscoped_org_query_in_lib\\.ex" .credo.exs` finds the new require entry and `rg -n "Sigra\\.Credo\\.NoUnscopedOrgQueryInLib" .credo.exs` finds the enabled check.
    - If the Credo path ships: `bash -lc 'test $(wc -l < lib/sigra/credo/no_unscoped_org_query_in_lib.ex) -le 220 && test $(wc -l < test/sigra/credo/no_unscoped_org_query_in_lib_test.exs) -le 80'` succeeds, keeping the combined new files within 300 lines.
    - If the Credo path does not ship: `test ! -f lib/sigra/credo/no_unscoped_org_query_in_lib.ex` succeeds and Task 2 must carry the explicit fallback decision.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; if test -f lib/sigra/credo/no_unscoped_org_query_in_lib.ex; then mix test test/sigra/credo/no_unscoped_org_query_in_lib_test.exs --max-failures 1; else echo "DX-09 Credo spike fell back to conventions path"; fi'</automated>
  </verify>
  <done>The DX-09 spike outcome is real: either a budget-compliant Credo check exists and is wired in, or the codebase is still untouched and Task 2 must document the fallback explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Record the DX-09 outcome explicitly in conventions and prove it automatically</name>
  <files>CONVENTIONS.md</files>
  <read_first>.planning/phases/23-docs-ci-smoke-upgrade-guide/23-CONTEXT.md, .planning/phases/23-docs-ci-smoke-upgrade-guide/23-VALIDATION.md, CONVENTIONS.md, .credo.exs, lib/sigra/credo/no_unscoped_org_query_in_lib.ex, test/sigra/credo/no_unscoped_org_query_in_lib_test.exs</read_first>
  <action>Update `CONVENTIONS.md` so DX-09 closes explicitly. Add a short outcome section under `Organization Scoping (DX-09)` with one of two exact paths:

1. If Task 1 shipped the checker: state that Sigra now has a narrow custom Credo check for obvious unscoped org queries in `lib/sigra/**`, name the module `Sigra.Credo.NoUnscopedOrgQueryInLib`, and note that `for_org/2` remains the primary discipline while the lint rule is defense-in-depth.
2. If Task 1 did not ship the checker: state exactly that the DX-09 spike exceeded the 300-line budget and Sigra is intentionally closing on conventions plus integration enforcement for now, with `for_org/2` and existing org query tests as the enforced path.

Do not leave this as an implicit omission. The exact DX-09 outcome text must be grep-visible in `CONVENTIONS.md`, and the verify command must branch cleanly based on whether the Credo file exists.</action>
  <acceptance_criteria>
    - `rg -n "DX-09 outcome" CONVENTIONS.md` finds an explicit outcome section.
    - If the Credo path shipped: `rg -n "Sigra\\.Credo\\.NoUnscopedOrgQueryInLib|defense-in-depth" CONVENTIONS.md` finds the shipped-check wording.
    - If the fallback path shipped: `rg -n "exceeded the 300-line budget|conventions plus integration enforcement" CONVENTIONS.md` finds the explicit fallback wording.
    - `rg -n "for_org/2" CONVENTIONS.md` still anchors the decision to the primary scoping discipline.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; if test -f lib/sigra/credo/no_unscoped_org_query_in_lib.ex; then rg -n "DX-09 outcome|Sigra\\.Credo\\.NoUnscopedOrgQueryInLib|for_org/2" CONVENTIONS.md && mix test test/sigra/credo/no_unscoped_org_query_in_lib_test.exs --max-failures 1; else rg -n "DX-09 outcome|exceeded the 300-line budget|conventions plus integration enforcement|for_org/2" CONVENTIONS.md; fi'</automated>
  </verify>
  <done>DX-09 is no longer ambiguous: the repo records whether a narrow Credo check shipped or whether the phase intentionally closed on conventions plus integration enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library query code -> tenant data isolation | Missing `for_org/2` discipline can leak cross-tenant data. |
| Static analysis -> contributor behavior | An overbroad or misleading lint rule can create false confidence or noisy false positives. |
| Conventions docs -> future planning/execution | DX-09 must be explicit so later work does not assume a checker exists when it does not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | I | org query discipline | mitigate | Ship a narrow Credo check only if it stays within the DX-09 budget and catches obvious unscoped cases; otherwise document the fallback explicitly and keep `for_org/2` as the enforced path. |
| T-23-04-02 | T | custom Credo rule | mitigate | Reuse the existing `NoLogSafe2InLib` guard/require/test pattern so the new rule, if shipped, is reviewable and bounded. |
| T-23-04-03 | R | DX-09 outcome | mitigate | Add explicit outcome text to `CONVENTIONS.md` so future contributors cannot infer the wrong enforcement posture. |
| T-23-04-04 | D | lint false-positive cost | accept | The spike is allowed to fall back if a useful checker cannot fit under the 300-line limit without harmful noise. |
</threat_model>

<verification>
Run:

```bash
bash -lc 'set -euo pipefail; if test -f lib/sigra/credo/no_unscoped_org_query_in_lib.ex; then mix test test/sigra/credo/no_unscoped_org_query_in_lib_test.exs --max-failures 1 && rg -n "DX-09 outcome|Sigra\\.Credo\\.NoUnscopedOrgQueryInLib|for_org/2" CONVENTIONS.md; else rg -n "DX-09 outcome|exceeded the 300-line budget|conventions plus integration enforcement|for_org/2" CONVENTIONS.md; fi'
```
</verification>

<success_criteria>
Phase 23 closes DX-09 explicitly with automated proof: either a budget-compliant narrow Credo check ships on the existing custom-check rails, or the repo records the intentional fallback to conventions plus integration enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/23-docs-ci-smoke-upgrade-guide/23-04-SUMMARY.md`
</output>
