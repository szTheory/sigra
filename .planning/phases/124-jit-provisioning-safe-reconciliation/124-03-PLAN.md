---
phase: 124-jit-provisioning-safe-reconciliation
plan: 03
type: execute
wave: 3
depends_on:
  - 124-02
files_modified:
  - test/example/lib/example_web/controllers/enterprise_sso_controller.ex
  - test/example/lib/example_web/user_auth.ex
  - test/example/test/example_web/controllers/enterprise_sso_controller_test.exs
  - test/example/test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs
autonomous: true
requirements:
  - SSO-04
must_haves:
  truths:
    - "A safe enterprise sign-in lands the user in the resolved organization, honoring only a compatible local return path."
    - "If there is no compatible return path, the example host falls back to `/organizations`."
    - "Ambiguous or unsafe enterprise outcomes do not create a normal signed-in session and return the user to an enterprise-mode recovery surface."
  artifacts:
    - path: "test/example/lib/example_web/controllers/enterprise_sso_controller.ex"
      provides: "Example-app enterprise callback redirect and flash contract."
    - path: "test/example/lib/example_web/user_auth.ex"
      provides: "Return-to sanitization helper for post-enterprise sign-in redirects."
    - path: "test/example/test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs"
      provides: "Example proof for return_to compatibility, fallback, and unsafe-outcome handling."
  key_links:
    - from: "test/example/lib/example_web/controllers/enterprise_sso_controller.ex"
      to: "test/example/lib/example_web/user_auth.ex"
      via: "sanitized enterprise post-sign-in redirect path"
      pattern: "return_to"
    - from: "test/example/lib/example_web/controllers/enterprise_sso_controller.ex"
      to: "Sigra.Auth.create_session/4"
      via: "session is created only on safe callback success"
      pattern: "create_session"
---

<objective>
Prove the generated-host/example enterprise callback experience lands safely in the right org and fails honestly when reconciliation is unsafe.

Purpose: Close Phase 124 at the host boundary by aligning the example controller and tests with the new library contract for redirects, flash truth, and no-session denial behavior.
Output: Updated example controller/user-auth helpers plus controller and integration coverage for compatible return paths, `/organizations` fallback, and bounded unsafe recovery.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-jit-provisioning-safe-reconciliation/124-CONTEXT.md
@.planning/phases/124-jit-provisioning-safe-reconciliation/124-VALIDATION.md
@.planning/phases/123-org-aware-enterprise-routing/123-03-SUMMARY.md
@test/example/lib/example_web/controllers/enterprise_sso_controller.ex
@test/example/lib/example_web/user_auth.ex

<interfaces>
From test/example/lib/example_web/controllers/enterprise_sso_controller.ex:
```elixir
def callback(conn, %{"org" => org_slug} = params) do
  ...
  case Sigra.Auth.create_session(Auth.sigra_config(), user, metadata, []) do
```

From test/example/lib/example_web/user_auth.ex:
```elixir
def put_user_session_token(conn, token) when is_binary(token) do
  conn
  |> renew_session()
  |> put_token_in_session(token)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Honor only org-compatible return paths after enterprise sign-in</name>
  <read_first>.planning/phases/124-jit-provisioning-safe-reconciliation/124-CONTEXT.md, test/example/lib/example_web/controllers/enterprise_sso_controller.ex, test/example/lib/example_web/user_auth.ex, .planning/phases/123-org-aware-enterprise-routing/123-03-SUMMARY.md</read_first>
  <files>test/example/lib/example_web/controllers/enterprise_sso_controller.ex, test/example/lib/example_web/user_auth.ex, test/example/test/example_web/controllers/enterprise_sso_controller_test.exs</files>
  <behavior>
    - Test 1: A safe enterprise callback honors a sanitized local `user_return_to` when it stays inside the resolved organization context.
    - Test 2: Absolute URLs, wrong-organization paths, and non-local redirects are rejected and fall back to `/organizations`.
    - Test 3: Ordinary success does not redirect to organization settings by default, and success copy stays lightweight unless the reconciliation outcome materially changed membership or active-org context.
  </behavior>
  <acceptance_criteria>
    - `rg -n "/organizations\"|user_return_to|return_to" test/example/lib/example_web/controllers/enterprise_sso_controller.ex test/example/lib/example_web/user_auth.ex` shows the compatible return path and `/organizations` fallback contract.
    - `rg -n "settings" test/example/test/example_web/controllers/enterprise_sso_controller_test.exs` no longer asserts the old settings redirect as the default success path.
    - `cd test/example && mix test --include example_app test/example_web/controllers/enterprise_sso_controller_test.exs` passes.
  </acceptance_criteria>
  <action>Add a host-owned redirect helper in `test/example/lib/example_web/user_auth.ex` or a closely related helper module that captures the pre-renewal `:user_return_to`, accepts only local paths compatible with the resolved organization per D-17, and rejects absolute URLs, wrong-org paths, and any open-redirect shape. Update `ExampleWeb.EnterpriseSSOController.callback/2` to use that sanitized path first, otherwise fall back to `/organizations` per D-18, and remove the old hardcoded redirect to `/organizations/:org/settings`. Keep success low-friction per D-19 by using lightweight flash confirmation only for materially changed outcomes such as `:jit_created` or an active-org switch.</action>
  <verify>
    <automated>cd test/example && mix test --include example_app test/example_web/controllers/enterprise_sso_controller_test.exs</automated>
  </verify>
  <done>The example controller honors only org-compatible local return paths, defaults to `/organizations` when none is safe, and no longer hardcodes organization settings as the enterprise success destination.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep unsafe enterprise outcomes in bounded recovery without a normal session</name>
  <read_first>.planning/phases/124-jit-provisioning-safe-reconciliation/124-CONTEXT.md, test/example/lib/example_web/controllers/enterprise_sso_controller.ex, test/example/test/example_web/controllers/enterprise_sso_controller_test.exs, test/example/test/example_web/integration/enterprise_sso_routing_flow_test.exs</read_first>
  <files>test/example/lib/example_web/controllers/enterprise_sso_controller.ex, test/example/test/example_web/controllers/enterprise_sso_controller_test.exs, test/example/test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs</files>
  <behavior>
    - Test 1: Unsafe callback outcomes return to the org-scoped enterprise entry or bounded recovery surface and do not create `:user_token`.
    - Test 2: The example flow proves `/organizations` fallback when no valid return path exists after safe reconciliation.
    - Test 3: The integration flow covers one happy-path JIT or existing-membership landing and one unsafe ambiguity/refusal branch.
  </behavior>
  <acceptance_criteria>
    - `rg -n "user_token" test/example/test/example_web/controllers/enterprise_sso_controller_test.exs test/example/test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs` asserts no normal session on unsafe outcomes.
    - `test -f test/example/test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs` succeeds and the file contains the new reconciliation flow proof.
    - `cd test/example && mix test --include example_app test/example_web/controllers/enterprise_sso_controller_test.exs test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs` passes.
  </acceptance_criteria>
  <action>Update the example controller’s error/recovery handling so typed enterprise refusal codes from Plan 124-02 stay inside the enterprise flow per D-20 and never mint a normal signed-in session per D-12. Extend the existing controller test and add `test/example/test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs` to prove the safe landing contract, `/organizations` fallback, and no-session recovery behavior for unsafe or ambiguous enterprise outcomes.</action>
  <verify>
    <automated>cd test/example && mix test --include example_app test/example_web/controllers/enterprise_sso_controller_test.exs test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs</automated>
  </verify>
  <done>The example app proves the new enterprise redirect and recovery contract end to end, and unsafe outcomes stay in enterprise mode without setting a normal user session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session return_to -> example controller | User-controlled redirect input must be sanitized before post-sign-in navigation. |
| library callback result -> example controller | The host must surface safe success or bounded recovery without changing the security decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-09 | T | `ExampleWeb.EnterpriseSSOController` redirect logic | mitigate | Reject open redirects and org-incompatible local paths; only honor sanitized local return paths for the resolved organization, otherwise use `/organizations`. |
| T-124-10 | E | example callback failure handling | mitigate | Keep typed enterprise refusal outcomes inside the enterprise flow and avoid creating `:user_token` for unsafe outcomes, preventing fallback into another auth mode. |
| T-124-11 | I | example flashes and confirmation copy | mitigate | Use bounded, truthful copy that distinguishes success from recovery without leaking account-linking internals on generic surfaces. |
| T-124-12 | R | example proof surface | mitigate | Add controller and integration assertions for safe landing, fallback, and no-session denial so regressions in redirect or recovery behavior fail visibly. |
</threat_model>

<verification>
Run `cd test/example && mix test --include example_app test/example_web/controllers/enterprise_sso_controller_test.exs test/example_web/integration/enterprise_sso_reconciliation_flow_test.exs`.
</verification>

<success_criteria>
The example enterprise callback lands users in a safe org-compatible destination, falls back to `/organizations` when needed, and keeps unsafe outcomes in enterprise-mode recovery without issuing a normal session.
</success_criteria>

<output>
After completion, create `.planning/phases/124-jit-provisioning-safe-reconciliation/124-03-SUMMARY.md`
</output>
