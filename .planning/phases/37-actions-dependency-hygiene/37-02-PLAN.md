---
phase: 37
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/37-actions-dependency-hygiene/37-VALIDATION.md
autonomous: true
requirements:
  - CI-02
  - CI-03
must_haves:
  truths:
    - "`37-CI-PIN-POLICY.md` records CI-02 evidence (URL to a green GitHub Actions run on the merge commit or PR head) and CI-03 pin policy."
    - "`REQUIREMENTS.md` marks CI-01, CI-02, and CI-03 checkboxes as done (`[x]`) only after that evidence exists."
  artifacts:
    - path: ".planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md"
      provides: "CI-02 + CI-03 durable record"
nyquist_compliant: true
---

<objective>
Satisfy **CI-02** (prove full **CI** workflow green for the landed bumps) and **CI-03** (document intentional pins and revisit triggers), then close traceability in `REQUIREMENTS.md`.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-37-04 | Repudiation | CLAIM CI green without URL | medium | Require literal `CI-02 evidence:` line with `https://github.com/` run or commit URL |
| T-37-05 | Information disclosure | Leak secrets in policy doc | low | Document only public workflow pins — no tokens |
</threat_model>

<context>
@.planning/phases/37-actions-dependency-hygiene/37-RESEARCH.md
@.planning/REQUIREMENTS.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="execute">
  <name>Task 1: Write 37-CI-PIN-POLICY.md (CI-02 + CI-03)</name>
  <files>.planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md</files>
  <read_first>
    - .planning/phases/37-actions-dependency-hygiene/37-RESEARCH.md
    - .github/workflows/ci.yml
    - .github/workflows/playwright-github-pages.yml
  </read_first>
  <action>
    1. Create `.planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md` with markdown heading `# Phase 37 — CI pin policy`.
    2. Under a `## CI-02 evidence` section, add a single line exactly matching pattern `CI-02 evidence: https://github.com/` (complete URL to the **CI** workflow run that validated the merged pins — conclusion `success`; use merge commit run on `main` if that is what shipped).
    3. Under `## CI-03 intentional pins`, list each of **`erlef/setup-beam`**, **`actions/cache`**, and **`peaceiris/actions-gh-pages`** with: current `uses:` SHA from the workflow file, inline version comment, one-line **reason** it was not part of the 999.2 triad bump in Plan 01, and a **revisit trigger** (e.g. "next Dependabot PR" or "quarterly supply-chain review").
    4. Under `## Adopted pins (reference)`, paste one example line each for the three bumped first-party actions (`checkout`, `setup-node`, `upload-artifact`) after Plan 01 (SHAs must match what is on `main`).
  </action>
  <acceptance_criteria>
    - `grep -q "^# Phase 37 — CI pin policy" .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md` exits 0.
    - `grep -q "^CI-02 evidence: https://github.com/" .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md` exits 0.
    - `grep -qi "erlef/setup-beam" .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md` exits 0.
    - `grep -qi "actions/cache" .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md` exits 0.
    - `grep -qi "peaceiris/actions-gh-pages" .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -s .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md'</automated>
  </verify>
  <done>CI-02 and CI-03 are auditable without opening GitHub blindly.</done>
</task>

<task type="execute">
  <name>Task 2: Check off CI-01..CI-03 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md
  </read_first>
  <action>
    1. In `.planning/REQUIREMENTS.md` under **CI & supply-chain hygiene**, change the three lines for **CI-01**, **CI-02**, and **CI-03** from `[ ]` to `[x]` **only if** `37-CI-PIN-POLICY.md` already contains the `CI-02 evidence:` line from Task 1 (do not check early).
    2. Do not edit REQ rows for other phases (VAL, UAT, AUD, TOOL, REL).
  </action>
  <acceptance_criteria>
    - `grep -E '^- \[x\] \*\*CI-01\*\*:' .planning/REQUIREMENTS.md` exits 0.
    - `grep -E '^- \[x\] \*\*CI-02\*\*:' .planning/REQUIREMENTS.md` exits 0.
    - `grep -E '^- \[x\] \*\*CI-03\*\*:' .planning/REQUIREMENTS.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "CI-02 evidence: https://github.com/" .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md && grep -Eq "^- \[x\] \*\*CI-01\*\*:" .planning/REQUIREMENTS.md'</automated>
  </verify>
  <done>Milestone traceability shows CI hygiene closed.</done>
</task>

<task type="execute">
  <name>Task 3: Flip 37-VALIDATION.md sign-off after execution</name>
  <files>.planning/phases/37-actions-dependency-hygiene/37-VALIDATION.md</files>
  <read_first>
    - .planning/phases/37-actions-dependency-hygiene/37-VALIDATION.md
  </read_first>
  <action>
    1. Set YAML frontmatter `nyquist_compliant: true` and `status: approved` once Tasks 1–2 of this plan and all Plan 01 tasks are done in git.
    2. Check all boxes under **Validation sign-off** in the markdown body.
    3. Set **Approval:** line to `approved 2026-04-17` or the actual completion date.
  </action>
  <acceptance_criteria>
    - `grep -q "^nyquist_compliant: true" .planning/phases/37-actions-dependency-hygiene/37-VALIDATION.md` exits 0.
    - `grep -q "^status: approved" .planning/phases/37-actions-dependency-hygiene/37-VALIDATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "nyquist_compliant: true" .planning/phases/37-actions-dependency-hygiene/37-VALIDATION.md'</automated>
  </verify>
  <done>Validation artifact matches executed reality.</done>
</task>

</tasks>

<verification>
- `grep -q "CI-02 evidence: https://github.com/" .planning/phases/37-actions-dependency-hygiene/37-CI-PIN-POLICY.md`
- All three CI requirement lines in `REQUIREMENTS.md` show `[x]`.
</verification>

<success_criteria>
- CI-02 and CI-03 satisfied with grep-auditable docs; REQUIREMENTS traceability updated.
</success_criteria>
