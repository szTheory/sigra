---
phase: 37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/playwright-github-pages.yml
autonomous: true
requirements:
  - CI-01
must_haves:
  truths:
    - "Every `actions/checkout`, `actions/setup-node`, and `actions/upload-artifact` step uses the v6 / v6 / v6 SHAs from Phase 37 research (or newer patch on same major re-resolved from GitHub API)."
    - "No remaining references to the old checkout SHA `34e114876b0b11c390a56381ad16ebd13914f8d5` or old setup-node SHA `0a44ba7841725637a19e28fa30b79a866c81b0a6` or old upload-artifact SHA `b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882` in `.github/workflows/`."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Bumped first-party Actions pins"
    - path: ".github/workflows/playwright-github-pages.yml"
      provides: "Bumped first-party Actions pins"
nyquist_compliant: true
---

<objective>
Satisfy **CI-01**: triage and land **999.2**-scoped first-party `actions/*` major upgrades (`checkout`, `setup-node`, `upload-artifact`) with SHA + `# vX.Y.Z` comments, preserving existing `permissions:` and step `if:` logic.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-37-01 | Tampering | Impostor-commit SHA not on official tag | high | Pin only SHAs from `actions/*` release tags; verify reachability |
| T-37-02 | Denial of service | upload-artifact v6 behavior breaks artifact upload | medium | Read v5/v6 release notes; keep mutually exclusive `if:` for duplicate names |
| T-37-03 | Misconfiguration | setup-node v6 drops Node 20 support | low | Keep `node-version: '20'` in `with:`; confirm in setup-node README |
</threat_model>

<context>
@.planning/phases/37-actions-dependency-hygiene/37-CONTEXT.md
@.planning/phases/37-actions-dependency-hygiene/37-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Triage open Dependabot PRs for github-actions</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml
    - .planning/phases/37-actions-dependency-hygiene/37-RESEARCH.md
  </read_first>
  <action>
    1. On GitHub: list open pull requests authored by `dependabot` that touch `.github/workflows/` (UI or `gh pr list --search "dependabot" --state open`).
    2. For each open PR: note which of `actions/checkout`, `actions/setup-node`, `actions/upload-artifact` it targets and whether it is **superseded** by the manual pins in Task 2 (same major line). If superseded, add a one-line comment on the PR or close it with reference to the manual bump PR (developer discretion).
    3. If no open Dependabot PRs exist, record in the Task 2 PR description: "CI-01 triage: no open Dependabot github-actions PRs on {date}."
  </action>
  <acceptance_criteria>
    - File `.planning/phases/37-actions-dependency-hygiene/37-TRIAGE-NOTES.md` exists and contains the literal substring `CI-01 triage` (body lists each open Dependabot workflow PR found, or states none on the recorded date).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .planning/phases/37-actions-dependency-hygiene/37-TRIAGE-NOTES.md && grep -q "CI-01 triage" .planning/phases/37-actions-dependency-hygiene/37-TRIAGE-NOTES.md'</automated>
  </verify>
  <done>CI-01 triage is not tacit — outcome is written down.</done>
</task>

<task type="execute">
  <name>Task 2: Bump actions/checkout, setup-node, upload-artifact in both workflows</name>
  <files>.github/workflows/ci.yml,.github/workflows/playwright-github-pages.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/playwright-github-pages.yml
    - https://github.com/actions/upload-artifact/releases (read v5.0.0 and v6.0.0 notes for breaking `with:` changes)
  </read_first>
  <action>
    1. Replace **every** `uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1` with `uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2` in both workflow files (12 + 1 occurrences expected in repo before edit — verify count after).
    2. Replace **every** `uses: actions/setup-node@0a44ba7841725637a19e28fa30b79a866c81b0a6  # v4.0.4` with `uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903  # v6.0.0` (2 + 1 occurrences).
    3. Replace **every** `uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882  # v4.4.3` with `uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0` in `ci.yml` only (8 occurrences).
    4. If upload-artifact release notes require `with:` key renames or new defaults, apply the minimal diff; otherwise leave `with:` blocks unchanged.
    5. Do **not** change `erlef/setup-beam`, `actions/cache`, or `peaceiris/actions-gh-pages` pins in this task unless a release-note conflict forces a coordinated bump (then document in PR body).
  </action>
  <acceptance_criteria>
    - `grep -r "34e114876b0b11c390a56381ad16ebd13914f8d5" .github/workflows` exits 1 (no matches).
    - `grep -r "0a44ba7841725637a19e28fa30b79a866c81b0a6" .github/workflows` exits 1.
    - `grep -r "b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882" .github/workflows` exits 1.
    - `grep -c "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" .github/workflows/ci.yml` prints `12`.
    - `grep -c "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" .github/workflows/playwright-github-pages.yml` prints `1`.
    - `grep -c "actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903" .github/workflows/ci.yml` prints `2`.
    - `grep -c "actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903" .github/workflows/playwright-github-pages.yml` prints `1`.
    - `grep -c "actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f" .github/workflows/ci.yml` prints `8`.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/milestone-verification-gate.sh</automated>
  </verify>
  <done>Workflow YAML matches Phase 37 target pins; milestone gate still passes locally.</done>
</task>

</tasks>

<verification>
- All Task 2 `grep` acceptance criteria pass.
- `bash scripts/ci/milestone-verification-gate.sh` exits 0.
- GitHub **CI** workflow green on the PR that contains these commits (record URL in Plan 02 artifact).
</verification>

<success_criteria>
- CI-01 satisfied: Dependabot triage documented; first-party `actions/*` majors bumped with SHA pins.
</success_criteria>
