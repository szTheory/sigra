---
phase: 70
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - ACF-06
must_haves:
  truths:
    - "**`.planning/REQUIREMENTS.md`** Out of Scope table rows for **Lockspire** / **`sigra_lockspire`** and **full SEED-002** each include an inline **markdown link** to **`.planning/decisions/001-defer-sigra-lockspire-glue-package.md`** and **`.planning/seeds/SEED-002-phase-9-log-safe-atomicity-followup.md`** respectively (paths relative to REQUIREMENTS: **`decisions/001-defer-sigra-lockspire-glue-package.md`** and **`seeds/SEED-002-phase-9-log-safe-atomicity-followup.md`**)."
    - "**`.planning/PROJECT.md`** Current Milestone section contains **clickable-style** references: the substring **`decisions/001-defer-sigra-lockspire-glue-package.md`** and the substring **`seeds/SEED-002-phase-9-log-safe-atomicity-followup.md`** (markdown or backtick path acceptable if grep-verifiable)."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: ACF-06 Out of Scope attestation with pointers
nyquist_compliant: true
---

<objective>
Ship **ACF-06**: make **Lockspire** / **`sigra_lockspire`** and **full SEED-002** deferrals explicit in **`.planning/REQUIREMENTS.md`** and **`.planning/PROJECT.md`** with accurate pointers to **ADR 001** and **SEED-002** seed files.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-70-03 | Misrepresentation | Reader thinks Lockspire or full SEED-002 ships in v1.10 | medium | Table + PROJECT copy restates **deferral**; links go to authoritative ADR/seed |
| T-70-04 | Repudiation | Broken relative links | low | Grep for exact path strings committed in REQUIREMENTS/PROJECT |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add ADR and SEED links to REQUIREMENTS Out of Scope table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/decisions/001-defer-sigra-lockspire-glue-package.md
    - .planning/seeds/SEED-002-phase-9-log-safe-atomicity-followup.md
  </read_first>
  <action>
    In **`.planning/REQUIREMENTS.md`**, under **`## Out of scope (v1.10)`**, edit the **markdown table** so that:

    1. The row whose first column mentions **`sigra_lockspire`** / **Lockspire** includes an inline markdown link **`[ADR 001](decisions/001-defer-sigra-lockspire-glue-package.md)`** (exact URL segment in parentheses) in the **Reason** column, placed **before** or **within** the existing ADR **001** prose.

    2. The row whose first column mentions **SEED-002** / **`Multi`** includes an inline markdown link **`[SEED-002](seeds/SEED-002-phase-9-log-safe-atomicity-followup.md)`** (exact URL segment in parentheses) in the **Reason** column.

    Preserve table structure (leading `|` pipes). Do not remove existing accurate rationale text — **augment** with links.
  </action>
  <acceptance_criteria>
    - `grep -F '[ADR 001](decisions/001-defer-sigra-lockspire-glue-package.md)' .planning/REQUIREMENTS.md` exits **0**.
    - `grep -F '[SEED-002](seeds/SEED-002-phase-9-log-safe-atomicity-followup.md)' .planning/REQUIREMENTS.md` exits **0**.
    - `grep -F '## Out of scope (v1.10)' .planning/REQUIREMENTS.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix compile --warnings-as-errors` exits **0** (sanity — no Elixir change expected but confirms repo health).
  </verify>
</task>

<task type="execute">
  <name>Task 2: Ensure PROJECT.md cites ADR and SEED paths verbatim</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    In **`.planning/PROJECT.md`**, **`## Current Milestone: v1.10 Adopter confidence for solo production`** (or equivalent **Current Milestone** heading if renamed):

    1. Ensure the **Target features** or adjacent bullet that mentions **`sigra_lockspire`** / **ADR 001** includes the **verbatim path substring** **`decisions/001-defer-sigra-lockspire-glue-package.md`** at least once (markdown link preferred: **`[ADR 001](decisions/001-defer-sigra-lockspire-glue-package.md)`**).

    2. Ensure the subsection that discusses **SEED-002** / full conversion deferral includes the **verbatim path substring** **`seeds/SEED-002-phase-9-log-safe-atomicity-followup.md`** at least once (may already exist in **Reference (continuing work)** — if present, only add the ADR path if missing; do not duplicate the same sentence twice).

    If both path substrings already exist verbatim, this task is **no-op** but executor must document in commit message: “ACF-06 PROJECT paths already satisfied.”
  </action>
  <acceptance_criteria>
    - `grep -F 'decisions/001-defer-sigra-lockspire-glue-package.md' .planning/PROJECT.md` exits **0**.
    - `grep -F 'seeds/SEED-002-phase-9-log-safe-atomicity-followup.md' .planning/PROJECT.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix compile --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>

## PLANNING COMPLETE
