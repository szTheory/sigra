---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - lib/sigra/testing.ex
  - test/sigra/testing_audit_test.exs
autonomous: true
requirements: [DX-01]
tags: [dx, testing, audit, docs]
must_haves:
  truths:
    - "REQUIREMENTS.md DX-01 wording names the canonical shipped signatures (log_in_user/3, register_user/2, setup_totp/2, create_api_token/3)"
    - "Sigra.Testing source file has nine section comment headers organizing its existing ~60 functions"
    - "audit_event_fixture/1 and assert_audit_event/2 are defined in Sigra.Testing and importable in ExUnit"
    - "test/support/audit_fixtures.ex callers are migrated to the Sigra.Testing functions (one canonical location)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DX-01 text matching shipped signatures"
      contains: "create_api_token/3"
    - path: "lib/sigra/testing.ex"
      provides: "Section headers + audit helpers"
      contains: "# --- Audit (Phase 9) ---"
    - path: "test/sigra/testing_audit_test.exs"
      provides: "Unit coverage for audit_event_fixture/1 and assert_audit_event/2"
  key_links:
    - from: "Sigra.Testing.audit_event_fixture/1"
      to: "configured repo + generated audit_events schema"
      via: "opts[:repo] and opts[:audit_schema] keyword args"
      pattern: "repo\\.insert!"
    - from: "Sigra.Testing.assert_audit_event/2"
      to: "ExUnit.AssertionError"
      via: "raise on mismatch with diff-style message"
      pattern: "raise ExUnit.AssertionError"
---

<objective>
Close the Phase 9 audit-helper carryover, update REQUIREMENTS.md DX-01 wording to the shipped signatures (D-01), and add section comment headers (D-19) to the monolithic Sigra.Testing module.

Purpose: Establish a canonical Sigra.Testing organization and DX-01 contract BEFORE downstream plans build on it. No code moves; only additions and comments.

Output: Updated REQUIREMENTS.md DX-01 row, Sigra.Testing with 9 section headers and 2 new audit helpers, test/sigra/testing_audit_test.exs proving both helpers work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-developer-experience/10-CONTEXT.md
@.planning/phases/10-developer-experience/10-RESEARCH.md
@.planning/phases/09-audit-logging/09-CONTEXT.md
@lib/sigra/testing.ex
@test/support/audit_fixtures.ex
@lib/sigra/audit.ex
@priv/templates/sigra.install/audit_event.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md DX-01 to shipped signatures</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 129 DX-01 entry)
    - .planning/phases/10-developer-experience/10-CONTEXT.md §decisions D-01
    - .planning/phases/10-developer-experience/10-RESEARCH.md §"DX-01 Signature Reconciliation" / §User Constraints
  </read_first>
  <action>
    Replace the DX-01 bullet in .planning/REQUIREMENTS.md §"Developer Experience" with the exact text below (single bullet, matching existing formatting):

    ```
    - [ ] **DX-01**: Testing helpers importable in ExUnit that set up auth state in one call: `YourAppWeb.ConnCase.log_in_user/3` (generated), `YourApp.Accounts.register_user/2` (generated), `Sigra.Testing.setup_totp/2`, and `Sigra.Testing.create_api_token/3`. Arities reflect real option needs (`:mfa`, `:config`); names follow phx.gen.auth convention (Phase 1 D-43) and Phase 7 D-63 (`create_api_token`, not `create_api_key`).
    ```

    Do NOT add or rename shipped helpers. Do NOT introduce `create_api_key` alias. Do NOT add a v1.x note. Per D-01 this is a text edit only.

    Add a dated footer line at the bottom of REQUIREMENTS.md before the final `---`:

    ```
    *Last updated: 2026-04-09 — DX-01 wording reconciled with shipped helper signatures (Phase 10 D-01).*
    ```
  </action>
  <verify>
    <automated>rg -n 'create_api_token/3' .planning/REQUIREMENTS.md && rg -n 'log_in_user/3' .planning/REQUIREMENTS.md && rg -n 'setup_totp/2' .planning/REQUIREMENTS.md && rg -n 'register_user/2' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'DX-01' .planning/REQUIREMENTS.md` returns a line containing all four canonical signatures
    - `rg -n 'create_api_key' .planning/REQUIREMENTS.md` returns zero matches (no alias introduced)
    - File still parses as valid Markdown (no header / bullet breakage)
  </acceptance_criteria>
  <done>REQUIREMENTS.md DX-01 row names the four shipped helpers with exact arities; dated footer appended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add audit helpers and section headers to Sigra.Testing</name>
  <files>lib/sigra/testing.ex, test/sigra/testing_audit_test.exs, test/support/audit_fixtures.ex</files>
  <read_first>
    - lib/sigra/testing.ex (full file, ~1007 LOC — confirm existing function order and imports)
    - test/support/audit_fixtures.ex (existing 48-line scaffold — source for migration)
    - lib/sigra/audit.ex (log/2, log_multi/3 public API, line ~46-62)
    - priv/templates/sigra.install/audit_event.ex (schema field list for assert signature)
    - .planning/phases/10-developer-experience/10-CONTEXT.md §decisions D-18, D-19
    - .planning/phases/10-developer-experience/10-RESEARCH.md §"Audit Test Helpers (D-18)" §"Section comment headers for Sigra.Testing (D-19)" — note the 9 cluster names
    - .planning/phases/09-audit-logging/09-CONTEXT.md (canonical schema: action, outcome, actor_id, actor_type, target_id, target_type, metadata, occurred_at/inserted_at)
  </read_first>
  <behavior>
    - Test 1: `audit_event_fixture(repo: Repo, audit_schema: AuditEvent)` inserts a row with default action "test.event", outcome "success"; returned struct has an id
    - Test 2: `audit_event_fixture/1` with overrides (action:, outcome:, actor_id:, metadata:) persists those exact values
    - Test 3: `assert_audit_event(%{action: "test.event", outcome: "success"}, repo: Repo, audit_schema: AuditEvent)` returns true when the most recent event matches
    - Test 4: `assert_audit_event/2` raises ExUnit.AssertionError with a diff-style message when action mismatches
    - Test 5: `assert_audit_event/2` deep-matches a metadata subset and ignores extra keys present on the event
    - Test 6: `assert_audit_event/2` with `position: 1` checks the second-most-recent event
  </behavior>
  <action>
    Step A — Add nine section comment headers to lib/sigra/testing.ex in the order below, placed immediately above the first `def` in each cluster. Do NOT move functions; only insert comment lines. The clusters (from RESEARCH.md §"Section comment headers"):

    ```
    # --- Core Assertions ---
    # --- Email ---
    # --- Lockout ---
    # --- MFA ---
    # --- API Tokens ---
    # --- Account Lifecycle ---
    # --- Hooks ---
    # --- OAuth ---
    # --- Audit (Phase 9) ---
    ```

    Use grep to locate the first def for each cluster; use the function name lists in RESEARCH.md §"Section comment headers for Sigra.Testing (D-19)" (e.g. `assert_password_hashed` begins Core Assertions, `assert_email_sent` begins Email, `simulate_lockout` begins Lockout, etc.).

    Step B — At the bottom of Sigra.Testing, under the `# --- Audit (Phase 9) ---` header, add the two public functions. Use the exact implementation from 10-RESEARCH.md §"Audit Test Helpers in Sigra.Testing (D-18)" (lines ~422-501). Both take `opts` keyword list with required `:repo` and `:audit_schema` keys. `audit_event_fixture/1` inserts directly via `Ecto.Changeset.change` + `repo.insert!`. `assert_audit_event/2` queries most recent (or `position:` offset) row, compares top-level fields, deep-matches `:metadata` subset (string OR atom keys), raises `ExUnit.AssertionError` with formatted diff on mismatch.

    Function signatures (exact):
    ```elixir
    @spec audit_event_fixture(keyword()) :: struct()
    def audit_event_fixture(opts) when is_list(opts)

    @spec assert_audit_event(map(), keyword()) :: true
    def assert_audit_event(expected, opts) when is_map(expected) and is_list(opts)
    ```

    Step C — Write test/sigra/testing_audit_test.exs covering behaviors 1-6 above. Use the existing test repo (Sigra.Test.Repo) and a minimal audit_events schema. If no test schema exists yet, reuse test/support/audit_fixtures.ex's schema reference (it currently uses `Sigra.Test.AuditEvent` or similar — grep to confirm).

    Step D — Migrate callers of test/support/audit_fixtures.ex (Open Q3 resolution: one canonical location). Grep for `audit_fixtures` / `AuditFixtures` imports and replace with `import Sigra.Testing` + call `audit_event_fixture/1` directly passing `repo: X, audit_schema: Y`. Delete test/support/audit_fixtures.ex once all callers migrated. If any caller still needs it, keep the file as a thin re-export wrapper calling Sigra.Testing.

    Do NOT rename any existing function in Sigra.Testing. Do NOT reorder functions. Per D-19 keep the module monolithic.
  </action>
  <verify>
    <automated>mix test test/sigra/testing_audit_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c '^\s*# --- .* ---\s*$' lib/sigra/testing.ex` returns 9
    - `rg -n 'def audit_event_fixture\(' lib/sigra/testing.ex` returns exactly 1 match
    - `rg -n 'def assert_audit_event\(' lib/sigra/testing.ex` returns exactly 1 match
    - `mix test test/sigra/testing_audit_test.exs` exits 0
    - `mix compile --warnings-as-errors` succeeds
    - `rg -n 'Sigra.Test.AuditFixtures|test/support/audit_fixtures' test/` shows no remaining imports outside of Sigra.Testing (or file deleted)
  </acceptance_criteria>
  <done>Sigra.Testing has 9 section headers, 2 new audit helpers with full behavior coverage; callers of old audit_fixtures.ex migrated; module remains monolithic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExUnit test process → repo | Test helpers insert rows; must use configured repo, never global state |
| Documentation surface → user apps | REQUIREMENTS.md wording drives user expectations; drift = support burden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | audit_event_fixture/1 bypasses Ecto.Multi wrapping; test rows could be mistaken for real events | accept | Fixtures inserted via test repo only; production Sigra.Audit.log/2 path unchanged; documented in @doc as "test-only" |
| T-10-07 | Information Disclosure | assert_audit_event/2 diff output could include PII from real audit rows | mitigate | Diff message uses `inspect/1` on expected map only; never dumps full event record |
| T-10-08 | Repudiation | REQUIREMENTS.md text drift from shipped signatures leads users to write insecure glue | mitigate | Task 1 aligns text with shipped code; future checker enforces `rg` on signatures |
</threat_model>

<verification>
- `mix test test/sigra/testing_audit_test.exs` passes
- `mix compile --warnings-as-errors` succeeds
- `mix credo --strict` passes on lib/sigra/testing.ex
- REQUIREMENTS.md DX-01 row contains all four canonical signatures
- Sigra.Testing contains exactly 9 section comment headers
</verification>

<success_criteria>
- DX-01 requirement text reconciled with shipped code (D-01)
- Sigra.Testing has section headers (D-19) without function movement
- Audit helpers (D-18) shipped, tested, and migrated from support scaffold
- No new public API surface beyond the two audit helpers
</success_criteria>

<output>
After completion, create `.planning/phases/10-developer-experience/10-01-SUMMARY.md`.
</output>
