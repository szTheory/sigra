---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-03]
files_modified:
  - mix.exs
  - guides/introduction/installation.md
  - guides/introduction/getting-started.md
  - guides/flows/registration.md
  - guides/flows/login-and-logout.md
  - guides/flows/password-reset.md
  - guides/flows/mfa.md
  - guides/flows/oauth.md
  - guides/flows/api-authentication.md
  - guides/flows/account-lifecycle.md
  - guides/flows/audit-logging.md
  - guides/recipes/testing.md
  - guides/recipes/subdomain-auth.md
  - guides/recipes/custom-user-fields.md
  - guides/recipes/multi-tenant.md
  - guides/recipes/deployment.md
  - guides/upgrading/.keep
autonomous: true
requirements: [DX-02]
tags: [dx, docs, ex_doc]
must_haves:
  truths:
    - "mix.exs docs/0 includes all 15 guide files in :extras and three :groups_for_extras (Introduction, Flows, Recipes)"
    - "mix docs --warnings-as-errors builds cleanly with grouped sidebar"
    - "All 15 guide markdown files exist as stubs with H1 title and one-paragraph intro"
    - "subdomain-auth.md documents the cookie_domain config from plan 03 — leading-dot rule and env-var pattern"
  artifacts:
    - path: "mix.exs"
      provides: ":extras and :groups_for_extras docs config"
    - path: "guides/introduction/"
      provides: "installation.md + getting-started.md stubs"
    - path: "guides/flows/"
      provides: "8 flow guide stubs"
    - path: "guides/recipes/"
      provides: "5 recipe guide stubs including subdomain-auth.md with cookie_domain content"
  key_links:
    - from: "mix.exs docs/0 :extras"
      to: "guides/**/*.md"
      via: "relative paths"
      pattern: "guides/"
    - from: "guides/recipes/subdomain-auth.md"
      to: "Sigra.Config :cookie_domain (plan 03)"
      via: "prose reference + code block"
      pattern: "cookie_domain"
---

<objective>
Scaffold the ex_doc guide directory with all 15 guide files (stubs), wire them into `mix.exs` via `:extras` and `:groups_for_extras`, and verify the build. The subdomain-auth.md recipe is the one stub that gets full content in this plan because it documents cookie_domain from plan 03 — all other stubs get content in plan 05.

Purpose: Establish the DX-02 documentation skeleton so plan 05 can fill in content incrementally without touching mix.exs or the directory structure.

Output: mix.exs docs config, guides/ directory tree, stubs compile clean under `mix docs --warnings-as-errors`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-developer-experience/10-CONTEXT.md
@.planning/phases/10-developer-experience/10-RESEARCH.md
@mix.exs
@lib/sigra/config.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guides/ directory tree with 15 stub files + subdomain-auth content</name>
  <files>
    guides/introduction/installation.md,
    guides/introduction/getting-started.md,
    guides/flows/registration.md,
    guides/flows/login-and-logout.md,
    guides/flows/password-reset.md,
    guides/flows/mfa.md,
    guides/flows/oauth.md,
    guides/flows/api-authentication.md,
    guides/flows/account-lifecycle.md,
    guides/flows/audit-logging.md,
    guides/recipes/testing.md,
    guides/recipes/subdomain-auth.md,
    guides/recipes/custom-user-fields.md,
    guides/recipes/multi-tenant.md,
    guides/recipes/deployment.md,
    guides/upgrading/.keep
  </files>
  <read_first>
    - .planning/phases/10-developer-experience/10-CONTEXT.md §decisions D-11, D-12
    - .planning/phases/10-developer-experience/10-RESEARCH.md §"Recommended Project Structure" (~117-150), §Security Domain (~850-876)
    - lib/sigra/config.ex (after plan 03 merged — for cookie_domain doc reference)
  </read_first>
  <action>
    Step A — Create the guides/ directory tree matching this exact structure (from RESEARCH §"Recommended Project Structure"):

    ```
    guides/
    ├── introduction/
    │   ├── installation.md
    │   └── getting-started.md
    ├── flows/
    │   ├── registration.md
    │   ├── login-and-logout.md
    │   ├── password-reset.md
    │   ├── mfa.md
    │   ├── oauth.md
    │   ├── api-authentication.md
    │   ├── account-lifecycle.md
    │   └── audit-logging.md
    ├── recipes/
    │   ├── testing.md
    │   ├── subdomain-auth.md
    │   ├── custom-user-fields.md
    │   ├── multi-tenant.md
    │   └── deployment.md
    └── upgrading/
        └── .keep
    ```

    For EACH stub file (all except subdomain-auth.md), write EXACTLY this content (replacing `{Title}` with a human-readable title derived from the filename):

    ```markdown
    # {Title}

    > Stub — full content lands in Phase 10 plan 05.

    This guide will cover {one-sentence description}. See the [getting-started guide](getting-started.html) for the end-to-end walkthrough and `Sigra` module docs for the library API reference.
    ```

    Human-readable titles:
    - installation.md → "Installation"
    - getting-started.md → "Getting Started"
    - registration.md → "User Registration"
    - login-and-logout.md → "Login and Logout"
    - password-reset.md → "Password Reset"
    - mfa.md → "Multi-Factor Authentication"
    - oauth.md → "OAuth and Social Login"
    - api-authentication.md → "API Authentication"
    - account-lifecycle.md → "Account Lifecycle"
    - audit-logging.md → "Audit Logging"
    - testing.md → "Testing Auth Flows"
    - custom-user-fields.md → "Custom User Fields"
    - multi-tenant.md → "Multi-Tenant Apps"
    - deployment.md → "Deployment"

    Step B — Write guides/recipes/subdomain-auth.md with FULL content (not a stub). This is the one guide that depends on plan 03's cookie_domain work. Content:

    ```markdown
    # Subdomain Authentication

    Sigra supports cookie-based auth across subdomains (`app.example.com`, `api.example.com`, `www.example.com`) via the `:cookie_domain` config option. This recipe covers the config, pitfalls, and the recommended deployment pattern.

    ## The config key

    `:cookie_domain` is a top-level key on `Sigra.Config`. Defaults to `nil` (host-only cookies — safe for dev, test, and single-domain prod). Set a string to enable subdomain auth:

        config :my_app, MyApp.Auth.Config,
          cookie_domain: System.get_env("COOKIE_DOMAIN")

    The value applies to all Sigra-managed cookies: remember-me, MFA trust, and any future cookie added to the library. It does NOT automatically configure the Phoenix session cookie itself — that lives in your `endpoint.ex` under `Plug.Session`. See below.

    ## Writing the domain value

    **Always use a leading dot** so the cookie is recognized by every subdomain:

        # Good
        cookie_domain: ".example.com"

        # Bad — host-only, subdomains won't see the cookie
        cookie_domain: "example.com"

        # BAD — overly broad, leaks cookies to every `.com` site
        cookie_domain: ".com"

    Scope the domain to your own registrable domain. Never use a public suffix like `.com`, `.co.uk`, or `.dev` — browsers will reject or you will leak session cookies to unrelated sites.

    ## Why there is no auto-detection

    Sigra intentionally does NOT offer `:parent` or `:auto` atom values for `:cookie_domain`. Auto-detection requires knowledge of public-suffix boundaries that cannot be statically inferred from a request host (e.g., `foo.github.io` is a public suffix, `foo.example.com` is not). Sigra keeps the surface explicit — you pass a string, you own the choice.

    ## Configuring the Phoenix session cookie

    The Phoenix session cookie (managed by `Plug.Session` in your `endpoint.ex`) is NOT controlled by Sigra. To share sessions across subdomains, edit your `lib/my_app_web/endpoint.ex`:

        plug Plug.Session,
          store: :cookie,
          key: "_my_app_key",
          signing_salt: "...",
          same_site: "Lax",
          domain: System.get_env("COOKIE_DOMAIN")

    Keep this domain value in sync with `Sigra.Config.cookie_domain` — typically via the same env var.

    ## Prod boot warning

    If `:cookie_domain` is `nil` when your app boots in `:prod`, Sigra emits a `Logger.warning` pointing to this guide. The warning is safe to ignore for single-domain deployments; silence it by setting `cookie_domain: nil` explicitly in your prod config (the warning only fires on unset, not on explicit nil — see `Sigra.Application`).

    ## Testing subdomain auth locally

    `/etc/hosts` trick:

        127.0.0.1 app.localtest.me api.localtest.me

    Then set `cookie_domain: ".localtest.me"` in `config/dev.exs`. `localtest.me` resolves to `127.0.0.1` for every subdomain out of the box.

    ## Common pitfalls

    - **Remember-me cookies broken after browser restart** — you set `cookie_domain` after cookies were already issued. Clear cookies and log in again.
    - **MFA trust cookie not recognized on sibling subdomain** — `Sigra.MFA.Trust.cookie_opts/1` honors `cookie_domain` only if you pass the config struct. Generated `mfa_challenge_controller` does this automatically; if you copy-paste into a custom controller, make sure you also pass the config.
    - **Mixed-domain deployments** (`app.example.com` + `admin.anotherco.com`) — you need TWO cookie scopes. Sigra does not support per-request cookie domains today; file an issue if you need this.

    ## Related

    - `Sigra.Config` — full option list
    - `Sigra.MFA.Trust.cookie_opts/1` — MFA trust cookie
    - [Deployment guide](deployment.html)
    ```

    Step C — Create `guides/upgrading/.keep` as an empty file so the directory is committed (reserved for future version-migration guides per D-12).

    Do NOT write getting-started.md content in this plan — that is plan 05's job. Do NOT fill in any flow/ or recipe/ content beyond subdomain-auth.md. Stubs only.
  </action>
  <verify>
    <automated>ls guides/introduction/installation.md guides/introduction/getting-started.md guides/flows/registration.md guides/flows/login-and-logout.md guides/flows/password-reset.md guides/flows/mfa.md guides/flows/oauth.md guides/flows/api-authentication.md guides/flows/account-lifecycle.md guides/flows/audit-logging.md guides/recipes/testing.md guides/recipes/subdomain-auth.md guides/recipes/custom-user-fields.md guides/recipes/multi-tenant.md guides/recipes/deployment.md guides/upgrading/.keep</automated>
  </verify>
  <acceptance_criteria>
    - All 15 .md files + 1 .keep file exist
    - `rg -c '^# ' guides/introduction/installation.md` returns 1 (single H1)
    - `rg -n 'cookie_domain' guides/recipes/subdomain-auth.md` returns at least 5 matches (fully written)
    - `rg -c '^# ' guides/recipes/subdomain-auth.md` returns 1 (single H1)
    - `wc -l guides/recipes/subdomain-auth.md` returns >= 60 lines (fully written)
    - Other stubs are <= 10 lines each
  </acceptance_criteria>
  <done>Guide tree created with 15 files; 14 stubs + 1 fully-written subdomain-auth recipe; upgrading/.keep placeholder committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire guides into mix.exs docs config and verify build</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (current docs/0 function or absence thereof; find where :docs and dev deps live)
    - .planning/phases/10-developer-experience/10-RESEARCH.md §"ex_doc :extras + :groups_for_extras config" (~504-545)
    - .planning/phases/10-developer-experience/10-CONTEXT.md §decisions D-12, D-15
  </read_first>
  <behavior>
    - Test 1: `mix docs --warnings-as-errors` exits 0
    - Test 2: Generated doc/index.html references guides under each of the three groups (Introduction, Flows, Recipes)
    - Test 3: `mix.exs` contains `:extras` with all 15 guide paths
    - Test 4: `:groups_for_extras` includes Introduction, Flows, Recipes regex entries
  </behavior>
  <action>
    In `mix.exs`, add or replace the `docs/0` private function with EXACTLY this shape (from 10-RESEARCH.md §"ex_doc :extras + :groups_for_extras config" lines ~508-545), ensuring it is referenced from the `project/0` function via `docs: docs()`:

    ```elixir
    defp docs do
      [
        main: "getting-started",
        source_ref: "v#{@version}",
        source_url: @source_url,
        formatters: ["html"],
        extras: [
          "CHANGELOG.md",
          "guides/introduction/installation.md",
          "guides/introduction/getting-started.md",
          "guides/flows/registration.md",
          "guides/flows/login-and-logout.md",
          "guides/flows/password-reset.md",
          "guides/flows/mfa.md",
          "guides/flows/oauth.md",
          "guides/flows/api-authentication.md",
          "guides/flows/account-lifecycle.md",
          "guides/flows/audit-logging.md",
          "guides/recipes/testing.md",
          "guides/recipes/subdomain-auth.md",
          "guides/recipes/custom-user-fields.md",
          "guides/recipes/multi-tenant.md",
          "guides/recipes/deployment.md"
        ],
        groups_for_extras: [
          Introduction: ~r{guides/introduction/.?},
          Flows: ~r{guides/flows/.?},
          Recipes: ~r{guides/recipes/.?}
        ],
        groups_for_modules: [
          Core: [Sigra, Sigra.Auth, Sigra.Config, Sigra.Crypto],
          Plugs: ~r{Sigra.Plug.*},
          MFA: ~r{Sigra.MFA.*},
          Audit: ~r{Sigra.Audit.*},
          Testing: [Sigra.Testing]
        ]
      ]
    end
    ```

    Ensure `@version` and `@source_url` module attributes exist (they likely already do — grep before adding). Adjust `main:` value to `"getting-started"` so the guide lands at /doc/index.html.

    If `CHANGELOG.md` does not exist at repo root, either create an empty stub or remove it from `:extras`. Grep first.

    Do NOT add `before_closing_head_tag` or custom CSS. Do NOT add `llms.txt` manually — ex_doc ≥ 0.40 auto-generates it (CLAUDE.md confirmed).

    Run `mix docs --warnings-as-errors` locally and fix any reported issues (typically missing files, broken internal links, or duplicate anchors). Because stubs are minimal, the only expected issue is the CHANGELOG.md reference — handle it.
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'groups_for_extras' mix.exs` returns exactly 1 match
    - `rg -c 'guides/' mix.exs` returns >= 15 (15 guide paths in :extras)
    - `mix docs --warnings-as-errors` exits 0
    - `doc/index.html` exists after build
    - `rg -l 'Introduction|Flows|Recipes' doc/api-reference.html doc/index.html 2>/dev/null` shows the sidebar groups rendered
  </acceptance_criteria>
  <done>mix.exs docs config wires all 15 guides into grouped sidebar; `mix docs --warnings-as-errors` builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library docs → user apps | Copy-paste examples in guides become user code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Guide doctest examples hard-code secrets or unsafe patterns that get copy-pasted | mitigate | This plan only writes one full guide (subdomain-auth.md); that guide recommends `System.get_env("COOKIE_DOMAIN")` explicitly — no hardcoded secrets. Plan 05 carries the same rule for content-filling. |
| T-10-14 | Information Disclosure | subdomain-auth.md example showing overly-broad `.com` could mislead users | mitigate | The guide explicitly flags `.com` as BAD and documents the leading-dot + registrable-domain rule |
| T-10-15 | Repudiation | ex_doc `:extras` references a missing guide file, docs build succeeds but sidebar omits a guide | mitigate | `mix docs --warnings-as-errors` fails loudly on any missing reference; Task 2 verify enforces this |
</threat_model>

<verification>
- `mix docs --warnings-as-errors` exits 0
- 15 guide files exist under guides/ plus upgrading/.keep
- mix.exs :extras lists all 15 paths
- subdomain-auth.md contains cookie_domain guidance (leading dot, env var, no auto-detection)
</verification>

<success_criteria>
- Guide scaffold complete and ex_doc builds cleanly
- subdomain-auth recipe documents DX-04 work from plan 03
- All other guides are ready for content in plan 05
- No changes to library modules
</success_criteria>

<output>
After completion, create `.planning/phases/10-developer-experience/10-04-SUMMARY.md`.
</output>
