---
phase: 10
plan: 05
type: execute
wave: 3
depends_on: [10-04]
files_modified:
  - guides/introduction/installation.md
  - guides/introduction/getting-started.md
  - guides/flows/registration.md
  - guides/flows/login-and-logout.md
  - guides/flows/password-reset.md
  - guides/flows/mfa.md
  - guides/flows/oauth.md
  - guides/flows/api-authentication.md
  - guides/flows/account-lifecycle.md
  - guides/flows/audit-logging.md
  - guides/recipes/testing.md
  - guides/recipes/custom-user-fields.md
  - guides/recipes/multi-tenant.md
  - guides/recipes/deployment.md
  - lib/sigra/testing.ex
  - lib/sigra/auth.ex
  - lib/sigra/config.ex
  - mix.exs
autonomous: false
requirements: [DX-02]
tags: [dx, docs, doctests]
must_haves:
  truths:
    - "guides/introduction/getting-started.md is a complete end-to-end walkthrough (install → register → login → logout → password reset email) readable in under 30 minutes"
    - "All 14 remaining guide stubs are replaced with full content using phx.gen.auth naming conventions only (no create_user, login, sign_in)"
    - "Sigra.Testing has full doctest coverage on its pure helper functions"
    - "Sigra.Auth has doctests on pure helpers (no repo setup required)"
    - "Sigra.Config has doctests showing new/1 with common option combinations including cookie_domain"
    - "mix docs --warnings-as-errors still builds cleanly after content filled in"
    - "No doctest hard-codes secrets; env-var patterns used throughout"
  artifacts:
    - path: "guides/introduction/getting-started.md"
      provides: "<30min readthrough end-to-end guide"
      min_lines: 200
    - path: "guides/flows/*.md"
      provides: "8 complete flow guides"
    - path: "guides/recipes/*.md"
      provides: "4 complete recipe guides (subdomain-auth already done in plan 04)"
    - path: "lib/sigra/testing.ex"
      provides: "Full doctest coverage on pure helpers"
    - path: "lib/sigra/auth.ex"
      provides: "Doctests on pure helpers"
    - path: "lib/sigra/config.ex"
      provides: "Doctests on Sigra.Config.new/1"
  key_links:
    - from: "getting-started.md"
      to: "test/example/ app (plan 06)"
      via: "code blocks exercised by plan 06's getting_started_flow_test.exs"
      pattern: "register_user|log_in_user"
    - from: "guides/*/*.md doctests"
      to: "mix test --only doctest"
      via: "doctest Sigra.Testing etc."
      pattern: "doctest "
---

<objective>
Fill in content for the 14 remaining guide files and add doctests to `Sigra.Testing`, `Sigra.Auth`, and `Sigra.Config` (per Open Q4 density resolution: pure-function coverage only on Auth/Config; full on Testing). Include a user checkpoint to manually verify the getting-started walkthrough takes < 30 min on a fresh install (DX-02 acceptance bar).

Purpose: Deliver the DX-02 content layer. The skeleton exists; plan 06 exercises it via smoke tests. This plan fills the prose and doctests.

Output: Full-content guide files, doctests, clean `mix docs --warnings-as-errors` build, user-verified getting-started experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-developer-experience/10-CONTEXT.md
@.planning/phases/10-developer-experience/10-RESEARCH.md
@lib/sigra/testing.ex
@lib/sigra/auth.ex
@lib/sigra/config.ex
@guides/introduction/installation.md
@guides/introduction/getting-started.md
@guides/recipes/subdomain-auth.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write getting-started + installation + 8 flow guides + 4 recipe guides</name>
  <files>
    guides/introduction/installation.md,
    guides/introduction/getting-started.md,
    guides/flows/registration.md,
    guides/flows/login-and-logout.md,
    guides/flows/password-reset.md,
    guides/flows/mfa.md,
    guides/flows/oauth.md,
    guides/flows/api-authentication.md,
    guides/flows/account-lifecycle.md,
    guides/flows/audit-logging.md,
    guides/recipes/testing.md,
    guides/recipes/custom-user-fields.md,
    guides/recipes/multi-tenant.md,
    guides/recipes/deployment.md,
    mix.exs
  </files>
  <read_first>
    - guides/recipes/subdomain-auth.md (exemplar for tone, length, structure)
    - lib/sigra/auth.ex (public function names + arities — guides must match exactly)
    - lib/sigra/testing.ex (public helpers — used in testing.md recipe)
    - priv/templates/sigra.install/auth.ex.eex, user_auth.ex, accounts.ex (generated code shapes referenced in guides)
    - .planning/phases/10-developer-experience/10-CONTEXT.md §decisions D-12, D-13, D-14
    - .planning/phases/10-developer-experience/10-RESEARCH.md §"Pitfall 6: Guide examples drifting" (~379-383), §"User Constraints from CLAUDE.md" (~106-113)
    - CLAUDE.md §"Conventions" — phx.gen.auth naming (register_user, log_in_user, deliver_*, verify_*)
  </read_first>
  <action>
    Step A — Write `guides/introduction/installation.md`. Cover:
    - Adding `{:sigra, "~> 0.1"}` to mix.exs (version pinned to current)
    - `mix deps.get`
    - `mix sigra.install` (what it generates — schemas, migrations, context, routes, LiveViews)
    - `mix ecto.migrate`
    - Verifying the install by visiting `/users/register`
    - Link to getting-started.md for the next step

    Length: 40-80 lines. Use code blocks for shell commands and generated file snippets.

    Step B — Write `guides/introduction/getting-started.md`. This is THE critical guide (D-13). Must hit register → login → logout + password reset email in <30 min readthrough. Structure:

    1. **Prerequisites** (Phoenix 1.8 app, Postgres, mix.exs dep already added)
    2. **Install** (`mix sigra.install` + migrate)
    3. **Register your first user** — code block showing `/users/register` form submission + what the generated LiveView does
    4. **Log in** — form submission + session verification
    5. **Protect a route** — add the `:require_authenticated_user` plug to a live_session in the router
    6. **Log out** — link in layout, verify session is gone
    7. **Request a password reset** — `/users/reset_password` form, show email printed to the Swoosh dev adapter
    8. **Click the reset link** — navigate to `/users/reset_password/:token`, set new password
    9. **Log in again with new password**
    10. **What's next** — links to flow guides (MFA, OAuth, API auth) and recipes

    Every code block must use phx.gen.auth names EXACTLY: `register_user`, `log_in_user`, `deliver_user_reset_password_instructions`, `YourApp.Accounts`, `YourAppWeb.UserAuth`. NEVER use `create_user`, `login`, `sign_in`, `signup`, `userAuth`.

    Do NOT embed actual screenshots. Do NOT reference `Accounts.create_user/1`. Do NOT hardcode any secrets — use `System.get_env(...)` or placeholder strings like `"<your-secret>"`.

    Length: 200-400 lines. Code blocks should be directly runnable against a phoenix app with Sigra installed (plan 06 test/example/ verifies this).

    Step C — Write the 8 flow guides. Each guide: 80-200 lines. Structure template:

    ```markdown
    # {Title}

    {One-paragraph summary of what this flow does}

    ## What Sigra gives you

    - {Generated file 1}: {what it does}
    - {Generated file 2}: {what it does}
    - {Library function}: {what it does}

    ## Happy path

    {Code block: minimal working example}

    ## Customization

    {Common customization: changing template, adding fields, extending validations}

    ## Error handling

    {What errors look like, how to display them}

    ## Testing

    {Code block showing Sigra.Testing helper usage}

    ## Related

    - {Link to related guides}
    - {Link to library module docs}
    ```

    Guide-specific content highlights:
    - **registration.md** — Accounts.register_user/2, UserRegistrationLive, confirmation email, `:allow_unconfirmed_login` config
    - **login-and-logout.md** — UserAuth.log_in_user/3, log_out_user/1, remember-me toggle, session cookie
    - **password-reset.md** — deliver_user_reset_password_instructions/2, ResetPasswordLive, reset_user_password/2, 60-minute TTL
    - **mfa.md** — Sigra.MFA enrollment, TOTP QR code, backup codes, :mfa_pending session state, trust-this-browser
    - **oauth.md** — Assent strategies, Google/GitHub tier 1 config, account linking confirmation, unlinking
    - **api-authentication.md** — create_api_token/3, Bearer header, scopes, revocation, JWT opt-in
    - **account-lifecycle.md** — email change with re-verification, password change, account deletion, sudo mode
    - **audit-logging.md** — Sigra.Audit.log/2, query API, custom events, audit_event_fixture for tests

    Step D — Write the 4 remaining recipes (subdomain-auth is already done in plan 04). Structure similar to flow guides but task-focused. Highlights:

    - **testing.md** — `import Sigra.Testing`, scenario fixtures (from plan 02), setup_totp, create_api_token, assert_email_sent, bypass_mfa. Reference the 7 scenario names and show a parametric test using `for state <- ~w(anonymous authenticated locked)a`.
    - **custom-user-fields.md** — Editing the generated User schema, re-running migrations, extending user_fixture, propagating through scenario fixtures
    - **multi-tenant.md** — Adding a :tenant_id column, scoping queries, using Sigra.Scope, guidance on org membership (linking to v2 roadmap)
    - **deployment.md** — env vars (DATABASE_URL, SECRET_KEY_BASE, COOKIE_DOMAIN), Fly.io / Gigalixir notes, cookie_domain in prod (cross-link subdomain-auth.md), health check endpoint

    Step E — Rule for EVERY guide: no hardcoded secrets, no `create_user`/`login`/`sign_in`, no fictional functions. Every code block's function calls must grep-match an actual shipped symbol. Verify by running:

        rg 'register_user\|log_in_user\|log_out_user\|deliver_\|verify_\|create_api_token\|setup_totp' guides/ --count

    Any function referenced in a guide that does NOT exist in `lib/sigra/` or `priv/templates/sigra.install/` is a bug and must be removed or corrected.

    After writing, run `mix docs --warnings-as-errors` and fix any broken internal links (e.g., `[text](other-guide.html)` where `other-guide.html` does not exist).

    Step F — Flip `mix.exs` docs landing page from `main: "readme"` (set in plan 04) to `main: "getting-started"`. Plan 04 left the landing page on README.md because getting-started.md was a stub; now that this plan has filled in the content, the guide becomes the canonical landing page. Edit `mix.exs` `docs/0`:

    ```elixir
    # BEFORE (from plan 04)
    main: "readme",

    # AFTER
    main: "getting-started",
    ```

    Re-run `mix docs --warnings-as-errors` to confirm the flip is clean. README.md remains in `:extras` (unchanged) so it is still reachable from the sidebar.
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l guides/introduction/getting-started.md` returns >= 200 lines
    - `wc -l guides/introduction/installation.md` returns >= 40 lines
    - `wc -l guides/flows/*.md | tail -1` total >= 800 lines (8 guides × 100 avg)
    - `rg -c 'create_user|[^_]login\b|sign_in|signup' guides/` returns 0 (no banned vocabulary — exclude compound words like `user_login_live`)
    - `rg -n 'STRIPE_KEY\|SECRET_KEY_BASE="' guides/` returns zero hardcoded secret values (env-var pattern is fine, literal values are not)
    - `rg -c 'register_user' guides/` returns >= 3 (cross-referenced in multiple guides)
    - `mix docs --warnings-as-errors` exits 0
    - `rg -n 'main: "getting-started"' mix.exs` returns exactly 1 match (flipped from "readme" per Step F)
  </acceptance_criteria>
  <done>14 guides filled with content; getting-started.md is the flagship end-to-end walkthrough; mix.exs main: flipped to "getting-started"; mix docs still builds cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add doctests to Sigra.Testing, Sigra.Auth, Sigra.Config</name>
  <files>lib/sigra/testing.ex, lib/sigra/auth.ex, lib/sigra/config.ex, test/sigra/doctest_test.exs</files>
  <read_first>
    - lib/sigra/testing.ex (all public functions — find pure ones suitable for doctests)
    - lib/sigra/auth.ex (grep public functions; identify those that do NOT require repo setup)
    - lib/sigra/config.ex (new/1 function — pure transformation)
    - .planning/phases/10-developer-experience/10-RESEARCH.md §"Open Questions §4" (~772)
    - .planning/phases/10-developer-experience/10-CONTEXT.md §decisions D-14, §Claude's Discretion
  </read_first>
  <behavior>
    - Test 1: `doctest Sigra.Config` runs and all doctests pass (Sigra.Config.new/1 examples)
    - Test 2: `doctest Sigra.Auth` runs and all doctests pass (pure helpers only — see scope below)
    - Test 3: `doctest Sigra.Testing` runs and all doctests pass (fixture/assertion pure helpers)
  </behavior>
  <action>
    Step A — In `lib/sigra/config.ex`, add doctests to `new/1` covering:
    - Default construction (no options)
    - `cookie_domain: nil` (explicit)
    - `cookie_domain: ".example.com"` (binary)
    - At least 2 other common options (whatever exists — session_max_age, lockout_threshold)

    Example doctest block:

    ```elixir
    @doc """
    Creates a new Sigra.Config struct from options.

    ## Examples

        iex> config = Sigra.Config.new(cookie_domain: ".example.com")
        iex> config.cookie_domain
        ".example.com"

        iex> config = Sigra.Config.new([])
        iex> config.cookie_domain
        nil
    """
    def new(opts \\ [])
    ```

    Step B — In `lib/sigra/auth.ex`, identify PURE helper functions (no repo access, no external services). Candidates: `normalize_email/1`, `valid_email?/1`, any token-format helpers, password-policy helpers. Add doctests only for these. Skip any function that calls Repo.

    Target: 5-10 doctests total across the module. Do NOT force doctests on repo-using functions (Open Q4 resolution — integration weight carried by example-app smoke tests).

    Step C — In `lib/sigra/testing.ex`, add doctests to every pure helper. Full coverage per Open Q4. Candidates: any function that does not require repo state (hash helpers, format helpers, pure assertion helpers). Functions like `setup_totp/2`, `create_api_token/3`, `audit_event_fixture/1` require repo and should NOT get doctests — cover them via the existing unit tests in test/sigra/testing_test.exs and test/sigra/testing_audit_test.exs.

    Target: 10-20 doctests depending on pure surface.

    Step D — Create `test/sigra/doctest_test.exs`:

    ```elixir
    defmodule Sigra.DoctestTest do
      use ExUnit.Case, async: true

      doctest Sigra.Config
      doctest Sigra.Auth
      doctest Sigra.Testing
    end
    ```

    This file exists solely to exercise the doctests. If a module already has `doctest Module` in an existing test file, do not duplicate — grep before adding. The test_helper.exs should include doctests by default (they usually do); no config changes needed.

    Step E — Run `mix test --only doctest` if the test file uses tags, or `mix test test/sigra/doctest_test.exs`. Fix any failing doctest (usually copy-paste errors in iex> prompts or output formatting).
  </action>
  <verify>
    <automated>mix test test/sigra/doctest_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'iex>' lib/sigra/config.ex` returns >= 4 (at least 2 doctest examples with at least 2 prompts each)
    - `rg -c 'iex>' lib/sigra/auth.ex` returns >= 5
    - `rg -c 'iex>' lib/sigra/testing.ex` returns >= 10
    - `mix test test/sigra/doctest_test.exs` exits 0
    - `mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <done>Doctests added and passing across Sigra.Config, Sigra.Auth, Sigra.Testing per Open Q4 density resolution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of getting-started walkthrough (DX-02 30-min bar)</name>
  <files>N/A — human verification only</files>
  <what-built>
    - guides/introduction/getting-started.md — full end-to-end walkthrough
    - 13 other guides with complete content
    - Doctests on Sigra.Config / Sigra.Auth / Sigra.Testing
  </what-built>
  <how-to-verify>
    1. Open a fresh terminal and start a timer.
    2. Read guides/introduction/getting-started.md end-to-end as if you were a developer new to Sigra.
    3. Follow the code blocks mentally (or literally, against test/example/ once plan 06 lands) — register a user, log in, log out, request a password reset.
    4. Stop the timer.
    5. Confirm the readthrough took < 30 minutes.
    6. Also spot-check guides/flows/mfa.md and guides/recipes/testing.md for accuracy — the code blocks should match actual shipped signatures.
    7. Open `doc/index.html` (generated by `mix docs`) in a browser; confirm the sidebar has three groups (Introduction, Flows, Recipes) with the expected entries.
    8. Verify `mix docs --warnings-as-errors` still builds cleanly.

    Expected outcome: < 30 minute readthrough, no broken signatures, no missing sidebar entries.

    If any guide is too long, too vague, or references a nonexistent function, request revision with specific pointers.
  </how-to-verify>
  <action>See <how-to-verify> above. Human-only checkpoint; no automation.</action>
  <verify>Manual sign-off via resume-signal</verify>
  <done>User types "approved" in resume-signal</done>
  <resume-signal>Type "approved" or describe required revisions (e.g., "getting-started lines 80-120 too detailed, trim auth internals").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library docs → user apps | Guide code blocks become user code |
| Doctest examples → CI gate | Doctest output format mismatches break CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Guides hard-code secrets / unsafe patterns → copy-paste regressions | mitigate | Acceptance criteria grep for hardcoded secrets; all env-driven values use `System.get_env` / placeholders |
| T-10-16 | Repudiation | Guide drift from shipped signatures (Pitfall 6) | mitigate | Acceptance criteria grep for banned vocabulary; plan 06 smoke-tests exercise code blocks against test/example/ |
| T-10-17 | Information Disclosure | Doctest examples leak test fixture data in failure messages | accept | Low severity; ExUnit doctest failures run in test env only |
</threat_model>

<verification>
- `mix docs --warnings-as-errors` exits 0
- `mix test test/sigra/doctest_test.exs` exits 0
- `mix test` (full suite) stays green
- All 14 guides have substantive content (>= 40 lines each, getting-started >= 200)
- No banned vocabulary (`create_user`, `login`, `sign_in`)
- Human-verified < 30 minute readthrough
</verification>

<success_criteria>
- DX-02 content complete: all guides full, doctests covering pure helpers
- Getting-started delivers "zero to working auth in an afternoon" bar
- All code blocks reference shipped (grep-verified) symbols
- User-approved via the checkpoint task
</success_criteria>

<output>
After completion, create `.planning/phases/10-developer-experience/10-05-SUMMARY.md`.
</output>
