---
phase: 28-user-operations-surface
plan: 4
type: execute
wave: 4
depends_on:
  - 28-03
files_modified:
  - lib/sigra/admin/live/users_index_live.ex
  - lib/sigra/admin/live/user_show_live.ex
  - test/example/lib/example_web/components/admin_shell.ex
  - test/example/priv/playwright/tests/admin-user-operations.spec.ts
  - .planning/phases/28-user-operations-surface/28-VALIDATION.md
autonomous: true
requirements:
  - USER-05
must_haves:
  truths:
    - "The search -> open user -> revoke session flow is usable on a mobile viewport and a desktop viewport."
    - "Scope remains visible in shell chrome while operating on mobile and desktop."
    - "Phase 28 ends with browser evidence and a finalized validation contract, not only server-side tests."
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-user-operations.spec.ts"
      provides: "End-to-end operator journey for Phase 28 on real browser viewports"
    - path: "lib/sigra/admin/live/users_index_live.ex"
      provides: "Final responsive index rendering and mobile-safe controls"
    - path: ".planning/phases/28-user-operations-surface/28-VALIDATION.md"
      provides: "Finalized Nyquist validation state for Phase 28"
  key_links:
    - from: "test/example/priv/playwright/tests/admin-user-operations.spec.ts"
      to: "lib/sigra/admin/live/users_index_live.ex"
      via: "search and open-user browser assertions"
      pattern: "Open user"
    - from: "test/example/priv/playwright/tests/admin-user-operations.spec.ts"
      to: "lib/sigra/admin/live/user_show_live.ex"
      via: "revoke-session browser assertions"
      pattern: "Revoke session"
---

<objective>
Harden the Phase 28 user-operations surface for real browser use, with mobile-safe layouts and finalized validation artifacts.

Purpose: USER-05 is not complete until the UI is proven on real mobile and desktop viewports and the phase validation file reflects that completed coverage.
Output: responsive polish, browser journey coverage, and finalized `28-VALIDATION.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-user-operations-surface/28-UI-SPEC.md
@.planning/phases/28-user-operations-surface/28-VALIDATION.md
@.planning/phases/28-user-operations-surface/28-03-PLAN.md
@test/example/priv/playwright/tests/organizations.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish responsive polish for the index, detail page, and shell chrome</name>
  <read_first>
    lib/sigra/admin/live/users_index_live.ex
    lib/sigra/admin/live/user_show_live.ex
    test/example/lib/example_web/components/admin_shell.ex
    .planning/phases/28-user-operations-surface/28-UI-SPEC.md
    .planning/phases/28-user-operations-surface/28-CONTEXT.md
  </read_first>
  <files>lib/sigra/admin/live/users_index_live.ex, lib/sigra/admin/live/user_show_live.ex, test/example/lib/example_web/components/admin_shell.ex</files>
  <action>Adjust the Phase 28 UI so the main jobs-to-be-done stay usable on phone and desktop. Keep one data model with two list presentations per D-11/D-14. Ensure the mobile index uses stacked result cards, the detail page keeps session actions visible without tabbing, icon-only controls carry exact `aria-label` values from the UI-SPEC examples, and high-frequency actions preserve a 44px minimum tap target. Do not add a horizontally scrolling desktop table fallback, a dashboard-first detour, or dense row-level destructive menus. Keep the persistent admin scope bar visible during the mobile flow and leave `Users` as the primary destination in shell navigation.</action>
  <acceptance_criteria>
    - `lib/sigra/admin/live/users_index_live.ex` contains mobile card markup and does not rely on horizontal scrolling for the primary list.
    - `lib/sigra/admin/live/user_show_live.ex` keeps the session actions above low-priority sections.
    - `test/example/lib/example_web/components/admin_shell.ex` still renders the visible `Admin` scope bar and a `Users` navigation item.
    - Icon-only controls in the touched files contain concrete `aria-label` values such as `Open user`, `Copy user id`, `Previous page`, or `Next page`.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example_web/live/admin_user_index_live_test.exs test/example_web/live/admin_user_filters_live_test.exs test/example_web/live/admin_user_show_live_test.exs --max-failures 1</automated>
  </verify>
  <done>The rendered Phase 28 UI stays usable at mobile and desktop breakpoints without losing scope visibility or burying the main actions.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the browser operator journey and finalize the validation artifact</name>
  <read_first>
    test/example/priv/playwright/tests/admin-user-operations.spec.ts
    test/example/priv/playwright/tests/organizations.spec.ts
    .planning/phases/28-user-operations-surface/28-VALIDATION.md
    .planning/phases/28-user-operations-surface/28-UI-SPEC.md
  </read_first>
  <files>test/example/priv/playwright/tests/admin-user-operations.spec.ts, .planning/phases/28-user-operations-surface/28-VALIDATION.md</files>
  <action>Implement the Playwright journey declared in the validation spec so it proves one end-to-end operator flow on a real browser: navigate to the admin user index, search for a named user, apply at least one quick filter, open the user detail page, trigger a guarded `Revoke session` confirmation, complete the revoke, and verify the post-action UI state. Tag the primary journey with `@smoke` so per-task verification stays fast. Use that smoke path for the task-level browser check while building the flow, then use the exact full-suite wave-gate command from `28-VALIDATION.md` to prove the final mobile and desktop browser state before marking the phase validation artifact complete. After the browser flow is green, update `28-VALIDATION.md` so the per-task status table reflects the completed commands, every Phase 28 task row is green instead of pending, `wave_0_complete: true` is set, and `nyquist_compliant: true` is set in frontmatter. Finish the phase closeout by running `mix precommit` from the repo root after the browser and validation updates pass.</action>
  <acceptance_criteria>
    - `test/example/priv/playwright/tests/admin-user-operations.spec.ts` contains a complete `@smoke` search -> filter -> open user -> revoke session flow.
    - The Playwright spec passes on both a mobile project and a desktop project.
    - `.planning/phases/28-user-operations-surface/28-VALIDATION.md` frontmatter contains `wave_0_complete: true` and `nyquist_compliant: true`.
    - `.planning/phases/28-user-operations-surface/28-VALIDATION.md` marks the full Phase 28 task status table as green instead of leaving any `⬜ pending` rows.
    - `mix precommit` exits 0 from the repo root after the final browser verification and validation updates.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright &amp;&amp; npx playwright test tests/admin-user-operations.spec.ts --project=mobile --grep @smoke &amp;&amp; cd /Users/jon/projects/sigra &amp;&amp; rg -n "@smoke" test/example/priv/playwright/tests/admin-user-operations.spec.ts</automated>
  </verify>
  <done>Phase 28 ends with browser proof for USER-05 and a finalized validation artifact instead of a draft strategy file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered mobile UI -> operator action | Smaller viewports can hide scope or destructive action context if the UI compresses poorly |
| Playwright evidence -> release confidence | Browser artifacts are used as release evidence for the mobile/desktop UX contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | Spoofing | mobile admin UI | mitigate | Keep visible scope chrome and exact confirmation copy on small screens; do not hide scope in a secondary picker or off-screen drawer. |
| T-28-11 | Tampering | browser action flow | mitigate | Exercise the real `Revoke session` UI path in Playwright instead of mocking the destructive action. |
| T-28-12 | Repudiation | validation artifact | mitigate | Update `28-VALIDATION.md` with the actual green commands and statuses after the browser and focused test runs pass. |
</threat_model>

<verification>
Run the focused example LiveView tests, then the Playwright smoke command from Task 2 for fast feedback. Use the full example + Playwright wave-gate command from `28-VALIDATION.md` before marking the phase validation artifact complete, and finish with `mix precommit` from the repo root as the final phase closeout.
</verification>

<success_criteria>
- Mobile and desktop both support the main operator flow without layout collapse or hidden scope context.
- The real browser journey passes for search, open-user, and session revoke.
- `28-VALIDATION.md` ends the phase in a finalized Nyquist-compliant state.
- `mix precommit` passes after the final browser and validation checks.
</success_criteria>

<output>
After completion, create `.planning/phases/28-user-operations-surface/28-user-operations-surface-04-SUMMARY.md`
</output>
