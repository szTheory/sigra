---
phase: 28-user-operations-surface
plan: 2
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - lib/sigra/admin/users/query.ex
  - lib/sigra/admin/live/users_index_live.ex
  - lib/sigra/admin/live/index_live.ex
  - lib/sigra/admin/live/organization_live.ex
  - test/example/lib/example_web/router.ex
  - test/example/lib/example_web/components/admin_shell.ex
  - test/sigra/admin/users_query_test.exs
  - test/example/test/example_web/live/admin_user_index_live_test.exs
  - test/example/test/example_web/live/admin_user_filters_live_test.exs
autonomous: true
requirements:
  - USER-01
  - USER-02
  - USER-05
must_haves:
  truths:
    - "Admins can open `/admin/users` or `/admin/organizations/:org/users` and see a scope-safe, URL-addressable user index."
    - "Search, quick filters, more-filters params, sorting, and pagination all survive patch navigation and back-navigation."
    - "Mobile uses stacked result cards while desktop uses a sortable table over the same query contract."
  artifacts:
    - path: "lib/sigra/admin/users/query.ex"
      provides: "Validated search/filter/pagination contract for admin users"
      exports: ["list_users", "summary_counts", "normalize_params"]
    - path: "lib/sigra/admin/live/users_index_live.ex"
      provides: "Global and org-scoped user list LiveView"
      exports: ["mount", "handle_params", "handle_event", "render"]
    - path: "test/example/lib/example_web/router.ex"
      provides: "Global and org user index routes under existing admin live_session blocks"
  key_links:
    - from: "lib/sigra/admin/live/users_index_live.ex"
      to: "lib/sigra/admin/users/query.ex"
      via: "handle_params/3 list reload"
      pattern: "Sigra.Admin.Users.Query"
    - from: "lib/sigra/admin/users/query.ex"
      to: "lib/sigra/admin/authorizer.ex"
      via: "scope_query/2"
      pattern: "scope_query"
    - from: "test/example/lib/example_web/router.ex"
      to: "lib/sigra/admin/live/users_index_live.ex"
      via: "live /admin/users and /admin/organizations/:org/users"
      pattern: "UsersIndexLive"
---

<objective>
Build the searchable, filterable Phase 28 user index on top of the Phase 27 admin shell and scope enforcement foundation.

Purpose: USER-01, USER-02, and the list portion of USER-05 all depend on one canonical, validated list contract instead of custom per-route query logic.
Output: admin users query module, index LiveView, index routes, shell navigation updates, and passing list/filter tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-user-operations-surface/28-CONTEXT.md
@.planning/phases/28-user-operations-surface/28-RESEARCH.md
@.planning/phases/28-user-operations-surface/28-PATTERNS.md
@.planning/phases/28-user-operations-surface/28-UI-SPEC.md
@.planning/phases/28-user-operations-surface/28-01-PLAN.md
@.planning/phases/27-admin-access-foundation/27-admin-access-foundation-03-SUMMARY.md

<interfaces>
From [lib/sigra/admin/authorizer.ex](/Users/jon/projects/sigra/lib/sigra/admin/authorizer.ex):
```elixir
authorize_organization!(admin_scope, organization_or_id)
scope_query(queryable, admin_scope)
```

From [test/example/lib/example/sigra_admin_users.ex](/Users/jon/projects/sigra/test/example/lib/example/sigra_admin_users.ex):
```elixir
display_name_field/0
display_name(user)
extra_detail_sections(user)
copy_overrides/0
```

From [test/example/lib/example_web/components/admin_shell.ex](/Users/jon/projects/sigra/test/example/lib/example_web/components/admin_shell.ex):
```elixir
<nav aria-label="Admin navigation">
<nav aria-label="Admin bottom nav">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the canonical scope-safe admin user query</name>
  <read_first>
    lib/sigra/admin/authorizer.ex
    lib/sigra/audit/query.ex
    lib/sigra/auth.ex
    test/example/lib/example/accounts/user.ex
    test/example/lib/example/sigra_admin_users.ex
    test/sigra/admin/users_query_test.exs
    .planning/phases/28-user-operations-surface/28-CONTEXT.md
  </read_first>
  <files>lib/sigra/admin/users/query.ex, test/sigra/admin/users_query_test.exs</files>
  <action>Create `lib/sigra/admin/users/query.ex` as the single source of truth for USER-01 and USER-02. The module must expose `normalize_params/1`, `list_users/3`, and `summary_counts/2`. Follow the validation-first whitelist/query-composition pattern already used in `lib/sigra/audit/query.ex` instead of inventing a second query-shaping style. Use Flop validation instead of a custom parser. Accept these concrete URL params: `q`, `organization`, `page`, `page_size`, `order_by`, `order_direction`, `confirmed`, `mfa`, `passkeys`, `locked`, `deleted`, `provider`, `registered_from`, and `registered_to`. Resolve the host hook module explicitly through `Sigra.Admin.Users.Hooks.resolve(config)` before building search conditions, then use `display_name_field/0` and `extra_search_fields/0` from that resolved module when they return non-empty values. Search must cover `users.email`, `users.id`, the resolved host display-name field when present, and any additional host-provided search fields returned by `extra_search_fields/0`. For org-scoped admin access, do not call `Sigra.Admin.Authorizer.scope_query/2` on the `users` schema. Instead, validate the selected organization context with `Sigra.Admin.Authorizer.authorize_organization!/2`, then scope users through an explicit organization-membership join or subquery owned by `Sigra.Admin.Users.Query` so org admins only see users connected to their allowed organization memberships. The explicit `organization` param is the Phase 28 organization-membership lookup contract for USER-01: it must join that same membership path and match at least organization slug and organization name so admins can narrow users by membership without leaving the list. `summary_counts/2` must derive counts from the same scoped membership-aware base query rather than separate unscoped counts.</action>
  <acceptance_criteria>
    - `lib/sigra/admin/users/query.ex` exports `normalize_params`, `list_users`, and `summary_counts`.
    - `lib/sigra/admin/users/query.ex` contains `Sigra.Admin.Users.Hooks.resolve`.
    - `lib/sigra/admin/users/query.ex` contains the exact param keys `q`, `organization`, `confirmed`, `mfa`, `passkeys`, `locked`, `deleted`, `provider`, `registered_from`, and `registered_to`.
    - `lib/sigra/admin/users/query.ex` defines concrete behavior for `provider` when no linked-identity schema/config exists by returning a stable local-only result set or an empty match instead of raising.
    - `lib/sigra/admin/users/query.ex` contains `Sigra.Admin.Authorizer.authorize_organization!`.
    - `lib/sigra/admin/users/query.ex` scopes org-admin user access through a membership join or subquery instead of direct `scope_query/2` on `users`.
    - `test/sigra/admin/users_query_test.exs` passes and contains coverage for `confirmed`, `mfa`, `passkeys`, `locked`, `deleted`, `provider`, `registered_from`, and `registered_to` filter behavior.
    - `test/sigra/admin/users_query_test.exs` passes and contains coverage for global scope, org scope, display-name search, and organization-membership lookup.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/admin/users_query_test.exs --max-failures 1</automated>
  </verify>
  <done>The list contract is centralized, validated, scope-safe, and proven by focused library tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build the users index LiveView and route it as the admin landing</name>
  <read_first>
    lib/sigra/admin/live/index_live.ex
    lib/sigra/admin/live/organization_live.ex
    test/example/lib/example_web/live/organization_members_live.ex
    test/example/lib/example_web/router.ex
    test/example/lib/example_web/components/admin_shell.ex
    test/example/test/example_web/live/admin_user_index_live_test.exs
    test/example/test/example_web/live/admin_user_filters_live_test.exs
    .planning/phases/28-user-operations-surface/28-UI-SPEC.md
  </read_first>
  <files>lib/sigra/admin/live/users_index_live.ex, lib/sigra/admin/live/index_live.ex, lib/sigra/admin/live/organization_live.ex, test/example/lib/example_web/router.ex, test/example/lib/example_web/components/admin_shell.ex, test/example/test/example_web/live/admin_user_index_live_test.exs, test/example/test/example_web/live/admin_user_filters_live_test.exs</files>
  <action>Create `Sigra.Admin.Live.UsersIndexLive` with `handle_params/3` as the source of truth per D-04. Model the list and mobile interaction patterns after `test/example/lib/example_web/live/organization_members_live.ex` where it already demonstrates the intended LiveView list, modal, and mobile-safe interaction shape for this repo. Resolve the host hook module through `Sigra.Admin.Users.Hooks.resolve(config)` and render any host-provided `extra_list_badges/1` and `extra_list_columns/0` output alongside the core Phase 28 fields without allowing host hooks to replace the locked base identity/status metadata. The new user index must load rows and summary chips from `Sigra.Admin.Users.Query`, assign the current params for back-navigation, and render the same results in two presentations per D-11/D-14: a sortable desktop table and stacked mobile cards with no horizontal scrolling. In both row and card presentations, implement the locked operator-identification payload from D-12 and D-13: lead with name, primary email, and a copyable stable id, then show confirmation state, MFA/passkey state, lockout state, deletion state, org membership summary, last sign-in or last activity when available, and created-at or registration timing. Keep secondary metadata support-oriented rather than dumping raw schema fields. The default filter surface must satisfy D-18: expose the highest-frequency quick filters inline on the main page, and render a `More filters` drawer or sheet that holds the lower-frequency and multi-input controls, including provider mix and registration date range. Update the existing placeholder `IndexLive` and `OrganizationLive` to redirect or hand off into `/admin/users` and `/admin/organizations/:org/users` so D-06 makes user operations the landing surface. Update `test/example/lib/example_web/router.ex` to add the two concrete index routes inside the existing admin `live_session`s: `/admin/users` and `/admin/organizations/:org/users`. Do not add detail routes in this plan; those route mounts stay with Plan `28-03`, where `Sigra.Admin.Live.UserShowLive` is created. Update `admin_shell.ex` so `Users` is a real navigation link in desktop and mobile admin nav. Every `Open user` action must carry the current query string forward as a `return_to` param so Plan `28-03` can restore the operator's exact list state after navigating back from detail. Use `test/sigra/admin/users_query_test.exs`, `test/example/test/example_web/live/admin_user_index_live_test.exs`, and `test/example/test/example_web/live/admin_user_filters_live_test.exs` as the Wave-0 contract files for search, organization-membership lookup, quick filters, more-filters UI, and pagination.</action>
  <acceptance_criteria>
    - `test/example/lib/example_web/router.ex` contains the exact paths `/admin/users` and `/admin/organizations/:org/users`.
    - `test/example/lib/example_web/router.ex` does not add `:id` detail routes in this plan.
    - `test/example/lib/example_web/components/admin_shell.ex` contains a real `Users` link instead of placeholder text.
    - `lib/sigra/admin/live/users_index_live.ex` defines `handle_params/3`.
    - `lib/sigra/admin/live/users_index_live.ex` contains the exact text `Open user`.
    - `lib/sigra/admin/live/users_index_live.ex` renders name, primary email, and a copyable stable id in both the desktop row and mobile card presentations.
    - `lib/sigra/admin/live/users_index_live.ex` renders confirmation, MFA/passkey, lockout, deletion, organization summary, activity, and registration metadata in the initial visible list content.
    - `lib/sigra/admin/live/users_index_live.ex` contains the exact text `More filters` and includes provider plus registration date controls inside that drawer or sheet.
    - `lib/sigra/admin/live/users_index_live.ex` renders both desktop and mobile result containers.
    - `test/example/test/example_web/live/admin_user_filters_live_test.exs` contains behavioral coverage for the `organization` membership lookup/filter, `confirmed`, `mfa`, `passkeys`, `locked`, `deleted`, `provider`, `registered_from`, and `registered_to` controls.
    - `test/example/test/example_web/live/admin_user_index_live_test.exs` and `test/example/test/example_web/live/admin_user_filters_live_test.exs` pass.
  </acceptance_criteria>
  <verify>
    <automated>cd test/example &amp;&amp; mix test test/example_web/live/admin_user_index_live_test.exs test/example_web/live/admin_user_filters_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Admins can navigate into a real Phase 28 user index from both global and org scopes, with URL-preserved search, the locked row/card identity and auth-state metadata, and a compact quick-filter plus More-filters UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> user query | Untrusted search/filter/sort params influence database reads |
| admin shell nav -> list routes | Operators switch between global and org user surfaces through scope-sensitive links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Information Disclosure | `lib/sigra/admin/users/query.ex` | mitigate | Scope the base query through `Sigra.Admin.Authorizer.scope_query/2` before Flop filters or counts run. |
| T-28-05 | Tampering | URL filter params | mitigate | Whitelist only the concrete params in `normalize_params/1` and validate through Flop instead of applying arbitrary keys. |
| T-28-06 | Spoofing | `test/example/lib/example_web/components/admin_shell.ex` | mitigate | Keep scope visibility in persistent shell chrome and make `Users` links route-specific (`/admin/users` vs `/admin/organizations/:org/users`) so operators do not infer scope from cards alone. |
</threat_model>

<verification>
Run the focused library query tests, then the focused example LiveView index/filter tests. Do not proceed until both commands are green.
</verification>

<success_criteria>
- The admin landing routes now point at the user-operations surface without creating detail-route/liveview ordering conflicts.
- Search, filters, sort, pagination, and summary chips are all URL-addressable and scope-safe.
- Desktop and mobile render the same result set without a horizontal-scroll desktop table fallback.
- Focused list/filter tests are green.
</success_criteria>

<output>
After completion, create `.planning/phases/28-user-operations-surface/28-user-operations-surface-02-SUMMARY.md`
</output>
