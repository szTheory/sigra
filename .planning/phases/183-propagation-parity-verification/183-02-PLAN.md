---
phase: 183-propagation-parity-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 183-01
files_modified:
  - test/example/priv/playwright/snapshot-allowlist
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png
autonomous: true
requirements:
  - BRAND2-13
  - BRAND2-14

must_haves:
  truths:
    - "All 7 non-canary admin-checkpoints slugs have recaptured Playwright baselines reflecting the D4 lockup"
    - "The impersonation-banner canary PNGs are byte-identical to their pre-recapture state (unchanged)"
    - "snapshot-recapture-gate.sh exits 0 with all 7 slugs declared"
    - "snapshot-allowlist is empty (comments only) after the gate passes"
    - "All JSON/SVG/HTML files parse without errors; no stray binary files outside the baseline dir; mix test green; git status clean"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/"
      provides: "21 recaptured baseline PNGs (7 slugs × 3 projects) + 3 restored canary PNGs"
      contains: "D4 lockup rendered in topbar across all admin screens"
    - path: "test/example/priv/playwright/snapshot-allowlist"
      provides: "Empty steady state (comments only)"
      contains: "# STEADY STATE: this file is empty"
  key_links:
    - from: "scripts/ci/snapshot-recapture-gate.sh"
      to: "test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/"
      via: "npx playwright test --update-snapshots=all (pre-run) then compare-mode gate (formal run)"
      pattern: "snapshot-recapture-gate\\.sh"
    - from: "test/example/priv/playwright/snapshot-allowlist"
      to: "scripts/ci/snapshot-canary-guard.sh"
      via: "--require-all --allow <slug>... check at gate step (b)"
      pattern: "audit-explorer global-overview global-user-index org-overview org-scoped-admin user-audit user-detail"
---

<objective>
Recapture the 7 non-canary admin Playwright baselines exactly once (the logo is in every admin topbar, so all baselines legitimately change); restore the impersonation-banner canary PNGs; run the approval gate; reset the snapshot-allowlist to empty steady state; then sweep for hygiene (parseability, no stray binaries, mix test green, git clean).

Purpose: Final milestone step. Plan 01 must be complete and both test suites green before this plan runs. The recapture is the riskiest step — isolated here to make it auditable and to ensure baselines only reflect known-good code.

Output: 21 new baseline PNGs committed; 3 canary PNGs restored to pre-recapture state; snapshot-allowlist empty; hygiene sweep green; git status clean.
</objective>

<execution_context>
@/Users/jon/projects/sigra/.planning/phases/183-propagation-parity-verification/183-RESEARCH.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
@/Users/jon/projects/sigra/.planning/phases/183-propagation-parity-verification/183-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/183-propagation-parity-verification/183-PATTERNS.md

<interfaces>
<!-- Snapshot allowlist: current state has 4 slugs (global-overview, org-overview, user-audit, user-detail).
     Phase 183 must add 3 more (audit-explorer, global-user-index, org-scoped-admin) for 7 total,
     then reset to empty (comments only) after the gate passes.

From 183-PATTERNS.md §snapshot-allowlist:
  Pre-recapture state (add 3 slugs to existing 4):
    global-overview
    org-overview
    user-audit
    user-detail
    audit-explorer
    global-user-index
    org-scoped-admin

  Post-gate steady state: all 7 slug lines removed; header comment block (lines 1–13) kept verbatim.
  NEVER add impersonation-banner.

Gate invocation (from 183-RESEARCH.md §Unknown 3):
  scripts/ci/snapshot-recapture-gate.sh \
    audit-explorer global-overview global-user-index \
    org-overview org-scoped-admin user-audit user-detail

Canary PNGs to restore after --update-snapshots=all:
  test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-chromium.png
  test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-mobile.png
  test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-dark.png
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update snapshot-allowlist, pre-compile, recapture baselines, restore canary, run gate, reset allowlist</name>
  <files>
    test/example/priv/playwright/snapshot-allowlist,
    test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png
  </files>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist — read full file to see current 4 slugs and the header comment block (lines 1–13 must be preserved verbatim)
    - scripts/ci/snapshot-recapture-gate.sh — read to understand the 3-step gate process and confirm the --allow flag syntax
    - scripts/ci/snapshot-canary-guard.sh — read lines around the impersonation-banner hard-fail check to confirm canary slug name
    - test/example/priv/playwright/playwright.config.ts — confirm baseURL / SIGRA_EXAMPLE_URL environment variable name
    - 183-RESEARCH.md §Unknown 3 — full step-by-step recapture procedure, pitfalls, slug list, canary logic
    - 183-PATTERNS.md §snapshot-allowlist — exact pre- and post-recapture file content
    - memory/reference_admin_checkpoint_playwright.md — pre-compile + port 4011 pattern, notice/1 constraint
  </read_first>
  <action>
    PREREQUISITE CHECK: Confirm Plan 01 is committed and both test suites are still green before proceeding. Confirm no dev server is running on port 4011 (and mix test is not running — see pitfall 5 in RESEARCH.md).

    Step 1 — Update snapshot-allowlist to add 3 missing slugs.
    The file already has 4 slug lines (global-overview, org-overview, user-audit, user-detail) after its 13-line comment block. Add exactly 3 lines after the existing 4:
      audit-explorer
      global-user-index
      org-scoped-admin
    Total: 7 active slug lines. NEVER add impersonation-banner.

    Step 2 — Pre-compile the example app to prevent code-reload crash on first request.
      (cd test/example && mix compile)

    Step 3 — Boot the example dev server on port 4011 (NOT 4000 — Rulestead Docker holds 4000).
      cd test/example
      PORT=4011 PHX_SERVER=true MIX_ENV=dev mix phx.server &
      SERVER_PID=$!
    Wait for the server to be ready (allow ~5–10 seconds), then warm the admin routes:
      sleep 8
      curl -sf http://localhost:4011/admin > /dev/null || true

    Step 4 — Run --update-snapshots=all to force-rewrite ALL 24 baseline PNGs.
    Run from test/example/priv/playwright/:
      cd /path/to/sigra/test/example/priv/playwright
      SIGRA_EXAMPLE_URL=http://localhost:4011 \
        npx playwright test tests/admin-checkpoints.spec.ts \
          --project=admin-checkpoints-chromium \
          --project=admin-checkpoints-mobile \
          --project=admin-checkpoints-dark \
          --update-snapshots=all
    This will PASS (update mode always exits 0 when all tests can run).

    Step 5 — CRITICAL: Immediately restore the 3 impersonation-banner canary PNGs before running the gate.
      git checkout -- \
        test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-chromium.png \
        test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-mobile.png \
        test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/impersonation-banner-admin-checkpoints-dark.png
    Confirm they are restored: git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -q 'impersonation-banner' && echo "FAIL: canary changed" || echo "OK: canary restored"

    Step 6 — Kill the dev server.
      kill $SERVER_PID 2>/dev/null || true

    Step 7 — Run the approval gate from the repo root (the gate runs on committed+unstaged changes, no running server needed for the compare step — the baselines were just recaptured with =all so the compare pass will see them as matching).
    IMPORTANT: Run gate from repo root (not from test/example/priv/playwright/).
      scripts/ci/snapshot-recapture-gate.sh \
        audit-explorer global-overview global-user-index \
        org-overview org-scoped-admin user-audit user-detail
    If the gate fails: diagnose before retrying. Common causes: canary not restored (Step 5), allowlist has wrong slugs, example dev server not responding during =all pass.

    Step 8 — If the gate passes: reset snapshot-allowlist to empty steady state.
    Remove all 7 slug lines; keep ONLY the 13-line header comment block verbatim. The file after reset must contain zero non-comment, non-empty lines.
  </action>
  <verify>
    <automated>
      # Canary PNGs are unchanged (git diff shows no impersonation-banner PNG changes)
      git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep 'impersonation-banner' | wc -l | grep -q '^0$' && echo "OK: canary unchanged" || echo "FAIL: canary modified"
      # Exactly 21 non-canary baseline PNGs changed (7 slugs × 3 projects)
      CHANGED=$(git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -v 'impersonation-banner' | wc -l)
      echo "Changed baseline PNGs (expect 21): $CHANGED"
      # Allowlist is empty (comments only)
      grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l | grep -q '^0$' && echo "OK: allowlist empty" || echo "FAIL: allowlist has active slugs"
    </automated>
  </verify>
  <done>
    snapshot-recapture-gate.sh exits 0 with all 7 slugs declared; 21 non-canary baseline PNGs changed (D4 lockup visible in topbar); 3 impersonation-banner PNGs unchanged; snapshot-allowlist reset to comments-only empty steady state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final hygiene sweep — parseability, no stray binaries, mix test green, git clean</name>
  <files>
    (no new files — verification and cleanup only)
  </files>
  <read_first>
    - 183-RESEARCH.md §Unknown 5 "Full Gate Sequence" — all hygiene checks
    - 183-RESEARCH.md §Validation Architecture — scoped binary check command (exclude playwright baseline dir)
    - .planning/phases/183-propagation-parity-verification/183-01-SUMMARY.md — confirm Plan 01 completed successfully
  </read_first>
  <action>
    Run the complete hygiene sweep. All checks must pass before committing.

    1. SVG parseability — all 6 new/updated SVGs must parse as valid XML:
       xmllint --noout \
         priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg \
         test/example/priv/static/images/sigra-logo-primary{,-dark}.svg \
         test/example/priv/static/images/rail-accent-mark{,-dark}.svg
       Expected: no output (exit 0). If any file fails, fix the invalid XML.

    2. No stray binary files outside the Playwright baseline directory:
       STRAY=$(git diff --name-only --diff-filter=A HEAD \
         -- ':!test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png' \
         | xargs -I{} file {} 2>/dev/null | grep -i 'binary\|executable\|image\|data' | grep -v 'SVG' | wc -l)
       [ "$STRAY" -eq 0 ] && echo "OK: no stray binaries" || echo "FAIL: $STRAY stray binary files"
       If STRAY > 0, list the files and remove any that are not the legitimate Playwright baselines.

    3. Snapshot allowlist is empty:
       grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l
       Expected: 0.

    4. Brandbook size delta check — confirm brandbook/ did not grow unexpectedly:
       du -sh brandbook/
       Expected: approximately the same as before (~604KB reported in RESEARCH.md). No new binaries, no raster images should have been added to brandbook/ in this phase.

    5. Final mix test (both suites):
       mix test                          # from repo root
       (cd test/example && mix test)    # example suite
       Both must exit 0. If either fails, diagnose — it likely means an SVG was written with content that differs from what the test expects (re-check viewBox and provenance strings in the four lockup SVGs).

    6. Git status clean:
       git status --short | grep -v '\.png'
       Expected: empty output (all non-PNG files are either committed or untracked and expected). The Playwright baseline PNGs should be the only modified files if all SVG and test file changes were committed as part of Plan 01 work. If any unexpected files appear, resolve before declaring the phase complete.

    If all checks pass, commit the hygiene sweep with an appropriate message. The Phase 183 execution is complete.
  </action>
  <verify>
    <automated>
      # XML parseability
      xmllint --noout priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg test/example/priv/static/images/sigra-logo-primary{,-dark}.svg test/example/priv/static/images/rail-accent-mark{,-dark}.svg 2>&1 | grep -c 'error' | grep -q '^0$' && echo "OK: all SVGs parse" || echo "FAIL: SVG parse errors"
      # No stray binaries outside Playwright baseline dir
      STRAY=$(git diff --name-only --diff-filter=A HEAD -- ':!test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png' | xargs -I{} file {} 2>/dev/null | grep -i 'binary\|executable' | grep -v 'SVG\|ASCII\|text' | wc -l); [ "$STRAY" -eq 0 ] && echo "OK: no stray binaries" || echo "FAIL: $STRAY"
      # Allowlist empty
      grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l | grep -q '^0$' && echo "OK: allowlist empty" || echo "FAIL: active slugs remain"
      # mix test green (root)
      mix test
      # example test green
      (cd test/example && mix test)
      # git status clean (ignoring PNGs which are legitimate recapture artifacts)
      DIRTY=$(git status --short | grep -v '\.png' | wc -l); [ "$DIRTY" -eq 0 ] && echo "OK: git clean" || echo "FAIL: $DIRTY unexpected files"
    </automated>
  </verify>
  <done>
    All 6 new SVGs parse as valid XML; no stray binary files outside the Playwright baseline dir; snapshot-allowlist is empty (comments only); mix test (root) exits 0; cd test/example && mix test exits 0; git status shows only the intended Playwright PNG changes and is otherwise clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example dev server (port 4011) → Playwright | Playwright makes HTTP requests to the local dev server for screenshot capture; no external network |
| git checkout -- canary PNGs | File-system restore from git object store; no network or untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-03 | Tampering | Playwright baseline PNGs | accept | Baselines are QA artifacts; recapture is gated by snapshot-recapture-gate.sh + canary guard; stray binary check scoped to exclude the legitimate baseline dir |
| T-183-04 | Tampering | snapshot-allowlist | mitigate | After gate passes, allowlist is reset to empty so future CI catches any unauthorized baseline changes; canary is hard-coded in the guard script and cannot be added to allowlist |
| T-183-SC | Tampering | npm/pip/cargo installs | accept | npx playwright invocation reuses existing Playwright install in test/example/priv/playwright/; no new packages installed this phase |
</threat_model>

<verification>
Run after both tasks complete:

```bash
# 1. 21 non-canary baseline PNGs changed, 3 canary PNGs unchanged
git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep 'impersonation-banner' | wc -l  # expect 0
git diff --name-only test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ | grep -v 'impersonation-banner' | wc -l  # expect 21

# 2. Snapshot allowlist empty
grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l  # expect 0

# 3. All 6 SVGs parse
xmllint --noout \
  priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg \
  test/example/priv/static/images/sigra-logo-primary{,-dark}.svg \
  test/example/priv/static/images/rail-accent-mark{,-dark}.svg

# 4. No stray binaries
git diff --name-only --diff-filter=A HEAD \
  -- ':!test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/*.png' \
  | xargs -I{} file {} 2>/dev/null | grep -i 'binary\|executable' | grep -v 'SVG\|text'
# Expected: empty output

# 5. Both test suites green
mix test
(cd test/example && mix test)

# 6. Git clean (non-PNG files)
git status --short | grep -v '\.png'
# Expected: empty
```
</verification>

<success_criteria>
- snapshot-recapture-gate.sh exits 0 with 7 slugs declared (audit-explorer global-overview global-user-index org-overview org-scoped-admin user-audit user-detail)
- Exactly 21 non-canary baseline PNGs updated; 3 impersonation-banner PNGs are byte-identical to pre-recapture HEAD
- snapshot-allowlist contains zero non-comment non-empty lines
- All 6 new/updated SVGs (4 lockups + 2 companion marks) parse as valid XML via xmllint
- No stray binary files added outside test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/
- mix test (root) exits 0
- cd test/example && mix test exits 0
- git status --short filtered for non-PNG files returns empty output
</success_criteria>

<output>
Create .planning/phases/183-propagation-parity-verification/183-02-SUMMARY.md when done.
</output>
