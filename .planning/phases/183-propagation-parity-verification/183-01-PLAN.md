---
phase: 183-propagation-parity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/sigra-logo-primary.svg
  - priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg
  - test/example/priv/static/images/sigra-logo-primary.svg
  - test/example/priv/static/images/sigra-logo-primary-dark.svg
  - test/example/priv/static/images/rail-accent-mark.svg
  - test/example/priv/static/images/rail-accent-mark-dark.svg
  - test/example/test/example_web/admin_shell_test.exs
  - test/sigra/install/features/admin_test.exs
autonomous: true
requirements:
  - BRAND2-11
  - BRAND2-12

must_haves:
  truths:
    - "Both admin lockup SVGs (light + dark) carry the D4 viewBox '20 220 2361 1000' and 'Space Grotesk v2.0' provenance substring"
    - "The installer templates and example app logo copies are byte-identical (cmp exits 0 for both light and dark)"
    - "Both companion marks (rail-accent-mark{,-dark}.svg) contain D4 abstract mark geometry with explicit fills and no CSS media query"
    - "Both guard tests assert the D4 viewBox + Space Grotesk provenance and pass with mix test"
    - "Token parity greps confirm ember-700=#c2410c and rail-accent=#fdba74 across all three surfaces — no value edits needed"
  artifacts:
    - path: "priv/templates/sigra.install/admin/sigra-logo-primary.svg"
      provides: "D4 admin lockup, light variant, installer source of truth"
      contains: "viewBox=\"20 220 2361 1000\""
    - path: "priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg"
      provides: "D4 admin lockup, dark variant, installer source of truth"
      contains: "viewBox=\"20 220 2361 1000\""
    - path: "test/example/priv/static/images/sigra-logo-primary.svg"
      provides: "Example app lockup, light — byte-identical to installer template"
      contains: "viewBox=\"20 220 2361 1000\""
    - path: "test/example/priv/static/images/sigra-logo-primary-dark.svg"
      provides: "Example app lockup, dark — byte-identical to installer template"
      contains: "viewBox=\"20 220 2361 1000\""
    - path: "test/example/priv/static/images/rail-accent-mark.svg"
      provides: "D4 abstract mark, light fills"
      contains: "fill=\"#c2410c\""
    - path: "test/example/priv/static/images/rail-accent-mark-dark.svg"
      provides: "D4 abstract mark, dark fills"
      contains: "fill=\"#fdba74\""
  key_links:
    - from: "priv/templates/sigra.install/admin/sigra-logo-primary.svg"
      to: "test/example/priv/static/images/sigra-logo-primary.svg"
      via: "cp + cmp byte-identical verification"
      pattern: "cmp.*sigra-logo-primary\\.svg"
    - from: "brandbook/logo-primary.svg"
      to: "priv/templates/sigra.install/admin/sigra-logo-primary.svg"
      via: "viewBox reframe (paths verbatim, viewBox changed to 20 220 2361 1000)"
      pattern: "viewBox=\"20 220 2361 1000\""
    - from: "test/sigra/install/features/admin_test.exs"
      to: "priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg"
      via: "File.read! + assert source =~"
      pattern: "assert source =~ ~s\\(viewBox=\"20 220 2361 1000\"\\)"
---

<objective>
Propagate the ratified D4 Linked Rail logo into all four shipping SVG locations under the same filenames; produce the two D4 companion marks; update the two guard test files (documented deviation: tests pin v1 content strings — updating 2 assertions per file is the minimum required churn); verify token parity is unchanged across three surfaces; confirm both test suites are green.

Purpose: This is the first shipping code change of the v1.38 milestone. Every step before Playwright recapture must be complete and green before baselines are touched.

Output: 4 admin lockup SVGs (installer + example, both light/dark, byte-identical pairs), 2 companion marks (D4 abstract mark geometry, explicit light/dark fills, no CSS), 2 test files updated (4 assertion lines total), token parity confirmed.
</objective>

<execution_context>
@/Users/jon/projects/sigra/.planning/phases/183-propagation-parity-verification/183-RESEARCH.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
@/Users/jon/projects/sigra/.planning/phases/183-propagation-parity-verification/183-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/183-propagation-parity-verification/183-PATTERNS.md

<interfaces>
<!-- Source SVG geometry — read brandbook/logo-primary.svg and brandbook/logo-primary-dark.svg in full before writing admin lockups. The paths are long; read the full file to get all five g-0 through g-4 path d= values and the rail-tittle rect exactly. -->

From brandbook/logo-primary.svg (light — already in PATTERNS.md):
- viewBox="0 220 2410 1026" → reframe to "20 220 2361 1000"
- <g id="glyphs" fill="#151515"> with paths g-0 through g-4 (copy verbatim)
- <rect id="rail-tittle" x="557" y="246" width="200" height="200" fill="#c2410c" />

From brandbook/logo-primary-dark.svg (dark):
- Same paths, <g id="glyphs" fill="#f4f1eb">, <rect id="rail-tittle" fill="#fdba74" />

From brandbook/logo-mark.svg (companion mark geometry source):
- viewBox="-70 -60 1040 1040"
- <g id="glyphs"> containing two rects: x="540" y="360" w="180" h="580" and x="180" y="760" w="540" h="180"
- <rect id="rail-block" x="400" y="-20" width="320" height="320" />
- The <style> block must be REMOVED in the companion mark derivation

Current guard test assertions (lines to UPDATE):
  test/example/test/example_web/admin_shell_test.exs lines 79-80:
    BEFORE: assert source =~ ~s(viewBox="20 12 188 54")
            assert source =~ "Inter Display Black v4.1."
    AFTER:  assert source =~ ~s(viewBox="20 220 2361 1000")
            assert source =~ "Space Grotesk v2.0"

  test/sigra/install/features/admin_test.exs lines 252-253:
    BEFORE: assert source =~ ~s(viewBox="20 12 188 54")
            assert source =~ "Inter Display Black v4.1."
    AFTER:  assert source =~ ~s(viewBox="20 220 2361 1000")
            assert source =~ "Space Grotesk v2.0"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author D4 admin lockup SVGs (4 files) and verify byte-identity</name>
  <files>
    priv/templates/sigra.install/admin/sigra-logo-primary.svg,
    priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg,
    test/example/priv/static/images/sigra-logo-primary.svg,
    test/example/priv/static/images/sigra-logo-primary-dark.svg
  </files>
  <read_first>
    - brandbook/logo-primary.svg — full file, verbatim path data source for light admin lockup
    - brandbook/logo-primary-dark.svg — full file, verbatim path data source for dark admin lockup
    - priv/templates/sigra.install/admin/sigra-logo-primary.svg — current v1 installer file (structure/title/desc convention; will be replaced)
    - priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg — current v1 dark installer file (same reason)
    - 183-PATTERNS.md §"priv/templates/sigra.install/admin/sigra-logo-primary.svg" — exact desc text for light and dark admin lockups
    - 183-RESEARCH.md §Unknown 1 — viewBox derivation, title/desc, light/dark fills table
  </read_first>
  <action>
    Produce the four admin lockup SVGs as pure viewBox reframes of the D4 brandbook files. Do NOT modify path data.

    DOCUMENTED DEVIATION NOTE: The ROADMAP SC1 phrase "without modification to test expectations" reflected the incorrect assumption that tests only pin filenames. These SVG changes are intentional; the guard tests MUST be updated in Task 2 to accept D4 content.

    Installer light lockup (priv/templates/sigra.install/admin/sigra-logo-primary.svg):
    - xmlns="http://www.w3.org/2000/svg", viewBox="20 220 2361 1000", role="img", aria-labelledby="title desc"
    - <title id="title">Sigra primary logo</title>
    - <desc id="desc"> must contain the exact substring "Space Grotesk v2.0" (required by the updated test assertion). Use: "The Sigra D4 Linked Rail tight lockup for light surfaces. Font: Space Grotesk v2.0 (OFL) wght=700, outlined with opentype.js 2.0.0."
    - Copy the <g id="glyphs" fill="#151515"> block with paths g-0 through g-4 VERBATIM from brandbook/logo-primary.svg
    - Copy the <rect id="rail-tittle" x="557" y="246" width="200" height="200" fill="#c2410c" /> VERBATIM

    Installer dark lockup (priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg):
    - Same outer structure, same viewBox="20 220 2361 1000", role="img", aria-labelledby="title desc"
    - <title id="title">Sigra primary logo for dark surfaces</title>
    - <desc id="desc">The Sigra D4 Linked Rail tight lockup with a light wordmark for dark surfaces. Font: Space Grotesk v2.0 (OFL) wght=700, outlined with opentype.js 2.0.0.</desc>
    - Copy the <g id="glyphs" fill="#f4f1eb"> block with paths g-0 through g-4 VERBATIM from brandbook/logo-primary-dark.svg
    - Copy the <rect id="rail-tittle" x="557" y="246" width="200" height="200" fill="#fdba74" /> VERBATIM

    After writing the two installer files, copy them byte-for-byte to the example locations:
      cp priv/templates/sigra.install/admin/sigra-logo-primary.svg test/example/priv/static/images/sigra-logo-primary.svg
      cp priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg test/example/priv/static/images/sigra-logo-primary-dark.svg

    Verify byte-identity:
      cmp priv/templates/sigra.install/admin/sigra-logo-primary.svg test/example/priv/static/images/sigra-logo-primary.svg
      cmp priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg test/example/priv/static/images/sigra-logo-primary-dark.svg
    Both cmp commands must exit 0. If they differ, fix the example copies.

    Validate all four SVGs parse as valid XML:
      for f in priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg test/example/priv/static/images/sigra-logo-primary{,-dark}.svg; do
        xmllint --noout "$f" 2>/dev/null && echo "OK: $f" || echo "FAIL: $f"
      done
  </action>
  <verify>
    <automated>
      # viewBox and provenance present in all 4 files
      for f in priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg test/example/priv/static/images/sigra-logo-primary{,-dark}.svg; do
        grep -q 'viewBox="20 220 2361 1000"' "$f" && echo "OK viewBox: $f" || echo "FAIL viewBox: $f"
        grep -q 'Space Grotesk v2.0' "$f" && echo "OK provenance: $f" || echo "FAIL provenance: $f"
        grep -q '<path' "$f" && echo "OK has-path: $f" || echo "FAIL has-path: $f"
        grep -q '<text' "$f" && echo "FAIL has-text: $f" || echo "OK no-text: $f"
        grep -q 'font-family' "$f" && echo "FAIL font-family: $f" || echo "OK no-font-family: $f"
      done
      # byte-identity
      cmp priv/templates/sigra.install/admin/sigra-logo-primary.svg test/example/priv/static/images/sigra-logo-primary.svg && echo "OK: light byte-identical" || echo "FAIL: light differ"
      cmp priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg test/example/priv/static/images/sigra-logo-primary-dark.svg && echo "OK: dark byte-identical" || echo "FAIL: dark differ"
      # XML parseability
      xmllint --noout priv/templates/sigra.install/admin/sigra-logo-primary.svg priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg test/example/priv/static/images/sigra-logo-primary.svg test/example/priv/static/images/sigra-logo-primary-dark.svg 2>&1 | grep -c 'error' | grep -q '^0$' && echo "OK: all SVGs parse" || echo "FAIL: SVG parse errors"
    </automated>
  </verify>
  <done>
    All four admin lockup SVGs exist with viewBox="20 220 2361 1000" and "Space Grotesk v2.0" in desc; paths are path-only (no text, no font-family); installer templates and example copies are byte-identical; all four parse as valid XML.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author D4 companion marks (rail-accent-mark{,-dark}.svg)</name>
  <files>
    test/example/priv/static/images/rail-accent-mark.svg,
    test/example/priv/static/images/rail-accent-mark-dark.svg
  </files>
  <read_first>
    - brandbook/logo-mark.svg — full file; geometry source (viewBox, two rects in <g id="glyphs">, <rect id="rail-block">); the <style> block MUST be removed in derivation
    - test/example/priv/static/images/rail-accent-mark.svg — current v1 file; note the scoped ID naming pattern: rail-accent-mark-title / rail-accent-mark-desc
    - test/example/priv/static/images/rail-accent-mark-dark.svg — current v1 dark file; note rail-accent-mark-dark-title / rail-accent-mark-dark-desc IDs
    - 183-PATTERNS.md §"test/example/priv/static/images/rail-accent-mark.svg" — full derivation recipe
    - 183-RESEARCH.md §"Companion marks: rail-accent-mark{,-dark}.svg" — reference check and Playwright assertion scope
  </read_first>
  <action>
    Produce two companion marks derived from brandbook/logo-mark.svg geometry with the following transformations:

    BOTH variants:
    - Preserve: xmlns, viewBox="-70 -60 1040 1040", role="img", aria-labelledby (use scoped IDs per v1 convention)
    - REMOVE: the <style> block entirely (no CSS media query; companion marks use explicit hardcoded fills)
    - ADD: fill attribute directly on <g id="glyphs"> and on <rect id="rail-block">
    - KEEP: geometry verbatim — the two rects in <g id="glyphs"> (x="540" y="360" w="180" h="580" and x="180" y="760" w="540" h="180") and <rect id="rail-block" x="400" y="-20" width="320" height="320" />

    Light variant (rail-accent-mark.svg):
    - aria-labelledby="rail-accent-mark-title rail-accent-mark-desc"
    - <title id="rail-accent-mark-title">Sigra mark</title>
    - <desc id="rail-accent-mark-desc">Sigra D4 abstract rail glyph for light surfaces. Ink stem, leftward foot, ember-700 (#c2410c) rail block.</desc>
    - <g id="glyphs" fill="#151515">
    - <rect id="rail-block" x="400" y="-20" width="320" height="320" fill="#c2410c" />

    Dark variant (rail-accent-mark-dark.svg):
    - aria-labelledby="rail-accent-mark-dark-title rail-accent-mark-dark-desc"
    - <title id="rail-accent-mark-dark-title">Sigra mark for dark surfaces</title>
    - <desc id="rail-accent-mark-dark-desc">Sigra D4 abstract rail glyph for dark surfaces. Ember-300 (#fdba74) rail block on dark.</desc>
    - <g id="glyphs" fill="#f4f1eb">
    - <rect id="rail-block" x="400" y="-20" width="320" height="320" fill="#fdba74" />

    After writing, validate both parse as valid XML:
      xmllint --noout test/example/priv/static/images/rail-accent-mark.svg test/example/priv/static/images/rail-accent-mark-dark.svg
  </action>
  <verify>
    <automated>
      # Light mark: correct fill and geometry, no CSS
      grep -q 'fill="#151515"' test/example/priv/static/images/rail-accent-mark.svg && echo "OK: light glyphs fill" || echo "FAIL"
      grep -q 'fill="#c2410c"' test/example/priv/static/images/rail-accent-mark.svg && echo "OK: light rail-block fill" || echo "FAIL"
      grep -q 'viewBox="-70 -60 1040 1040"' test/example/priv/static/images/rail-accent-mark.svg && echo "OK: light viewBox" || echo "FAIL"
      grep -q '@media\|prefers-color-scheme' test/example/priv/static/images/rail-accent-mark.svg && echo "FAIL: CSS present" || echo "OK: no CSS"
      # Dark mark: correct fill
      grep -q 'fill="#f4f1eb"' test/example/priv/static/images/rail-accent-mark-dark.svg && echo "OK: dark glyphs fill" || echo "FAIL"
      grep -q 'fill="#fdba74"' test/example/priv/static/images/rail-accent-mark-dark.svg && echo "OK: dark rail-block fill" || echo "FAIL"
      grep -q '@media\|prefers-color-scheme' test/example/priv/static/images/rail-accent-mark-dark.svg && echo "FAIL: CSS present" || echo "OK: no CSS"
      # XML parseability
      xmllint --noout test/example/priv/static/images/rail-accent-mark.svg test/example/priv/static/images/rail-accent-mark-dark.svg 2>&1 | grep -c 'error' | grep -q '^0$' && echo "OK: companion marks parse" || echo "FAIL: parse error"
    </automated>
  </verify>
  <done>
    Both companion marks have D4 abstract mark geometry (two-rect glyph + rail-block rect), explicit fills (light: #151515/#c2410c; dark: #f4f1eb/#fdba74), no CSS style block, and viewBox="-70 -60 1040 1040"; both parse as valid XML.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update 2 guard test files (4 assertion lines) + verify token parity + run both test suites green</name>
  <files>
    test/example/test/example_web/admin_shell_test.exs,
    test/sigra/install/features/admin_test.exs
  </files>
  <read_first>
    - test/example/test/example_web/admin_shell_test.exs — read the full "ships cropped path-only Sigra lockup assets" test (~lines 72–85); confirm the exact current strings on lines 79–80 before editing
    - test/sigra/install/features/admin_test.exs — read the full "admin logo templates are cropped path-only lockups" test (~lines 245–258); confirm the exact current strings on lines 252–253 before editing
    - 183-RESEARCH.md §Unknown 2 — complete assertion change table and full test context for both files
    - 183-PATTERNS.md §test files — full before/after assertion blocks
  </read_first>
  <action>
    DOCUMENTED DEVIATION: This edit is the intended minimum test churn accompanying a deliberate logo change. The ROADMAP SC1 phrase "without modification to test expectations" was based on a false premise (that tests only pin filenames). The structural invariant being tested is IDENTICAL — cropped, path-only, no live text, no font-family — only the two v1-pinned value strings per file change to D4 equivalents.

    File 1: test/example/test/example_web/admin_shell_test.exs
    Change ONLY lines 79 and 80 (inside "ships cropped path-only Sigra lockup assets"):
      Line 79:  assert source =~ ~s(viewBox="20 12 188 54")
             →  assert source =~ ~s(viewBox="20 220 2361 1000")
      Line 80:  assert source =~ "Inter Display Black v4.1."
             →  assert source =~ "Space Grotesk v2.0"
    Lines 81–84 (assert <path, refute <text, refute font-family) MUST NOT change.

    File 2: test/sigra/install/features/admin_test.exs
    Change ONLY lines 252 and 253 (inside "admin logo templates are cropped path-only lockups"):
      Line 252: assert source =~ ~s(viewBox="20 12 188 54")
              → assert source =~ ~s(viewBox="20 220 2361 1000")
      Line 253: assert source =~ "Inter Display Black v4.1."
              → assert source =~ "Space Grotesk v2.0"
    Lines 254–257 MUST NOT change.

    After updating both test files, run the token parity greps (verify-unchanged — no value edits expected):
      grep '"ember-700"' brandbook/tokens.json | grep -q '#c2410c' && echo "OK: brandbook ember-700=#c2410c" || echo "FAIL: ember-700"
      grep 'sg-color-brand:' test/example/priv/static/assets/css/app.css | grep -q '#c2410c' && echo "OK: sg-color-brand=#c2410c" || echo "FAIL: sg-color-brand"
      grep 'sg-logo-rail-accent:' test/example/priv/static/assets/css/app.css | grep -q '#fdba74' && echo "OK: sg-logo-rail-accent=#fdba74" || echo "FAIL: sg-logo-rail-accent"
      grep 'sigra-auth-light-accent' priv/templates/sigra.install/core/sigra_auth.css | grep -q '#c2410c' && echo "OK: auth accent=#c2410c" || echo "FAIL: auth accent"
    All four must print "OK". If any print "FAIL", stop and report the divergence — do NOT silently edit values.

    Then run both test suites:
      mix test                               (from repo root — covers test/sigra/install/features/admin_test.exs)
      (cd test/example && mix test)         (covers test/example/test/example_web/admin_shell_test.exs)
    Both must exit 0. Postgres must be available at localhost:5432. If test database is missing run mix ecto.create in test/example first.

    Do NOT run mix test while any dev server is running (code-reload crash risk — see RESEARCH pitfall 5).
  </action>
  <verify>
    <automated>
      # Test assertion strings are updated (not old v1 strings)
      grep -q 'viewBox="20 220 2361 1000"' test/example/test/example_web/admin_shell_test.exs && echo "OK: example test viewBox" || echo "FAIL"
      grep -q 'Space Grotesk v2.0' test/example/test/example_web/admin_shell_test.exs && echo "OK: example test provenance" || echo "FAIL"
      grep -q 'viewBox="20 12 188 54"' test/example/test/example_web/admin_shell_test.exs && echo "FAIL: stale v1 viewBox still present" || echo "OK: v1 viewBox gone"
      grep -q 'viewBox="20 220 2361 1000"' test/sigra/install/features/admin_test.exs && echo "OK: installer test viewBox" || echo "FAIL"
      grep -q 'Space Grotesk v2.0' test/sigra/install/features/admin_test.exs && echo "OK: installer test provenance" || echo "FAIL"
      grep -q 'viewBox="20 12 188 54"' test/sigra/install/features/admin_test.exs && echo "FAIL: stale v1 viewBox still present" || echo "OK: v1 viewBox gone"
      # Structural assertions unchanged (still present)
      grep -c '"&lt;path"\|"<path"' test/example/test/example_web/admin_shell_test.exs
      # Token parity (all 4 greps)
      grep '"ember-700"' brandbook/tokens.json | grep -q '#c2410c' && echo "OK: brandbook ember-700" || echo "FAIL"
      grep 'sg-color-brand:' test/example/priv/static/assets/css/app.css | grep -q '#c2410c' && echo "OK: sg-color-brand" || echo "FAIL"
      grep 'sg-logo-rail-accent:' test/example/priv/static/assets/css/app.css | grep -q '#fdba74' && echo "OK: sg-logo-rail-accent" || echo "FAIL"
      grep 'sigra-auth-light-accent' priv/templates/sigra.install/core/sigra_auth.css | grep -q '#c2410c' && echo "OK: auth accent" || echo "FAIL"
      # Both test suites green
      mix test
      (cd test/example && mix test)
    </automated>
  </verify>
  <done>
    Both guard test files updated with D4 viewBox and Space Grotesk provenance (4 lines changed, structural assertions unchanged); all 4 token parity greps print "OK" (no value edits made); mix test (root) exits 0; cd test/example && mix test exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brandbook → installer templates | SVG content authored from D4 sources; no external fetch, no script execution |
| installer templates → example app | cp byte copy; verified with cmp; no transformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01 | Tampering | Admin lockup SVG path data | accept | SVGs are purely static decorative assets served from priv/static; no auth/trust boundary crossed; ExUnit guards structural invariants |
| T-183-02 | Information Disclosure | SVG desc provenance string | accept | Font provenance in <desc> is intentional public information; no secrets |
| T-183-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Plan 01; pure file authoring + mix test |
</threat_model>

<verification>
Run after all three tasks complete:

```bash
# 1. All 4 admin lockup SVGs: D4 viewBox + provenance
for f in priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg test/example/priv/static/images/sigra-logo-primary{,-dark}.svg; do
  grep -q 'viewBox="20 220 2361 1000"' "$f" && echo "OK viewBox: $f" || echo "FAIL: $f"
  grep -q 'Space Grotesk v2.0' "$f" && echo "OK prov: $f" || echo "FAIL: $f"
done

# 2. Byte-identity: installer == example
cmp priv/templates/sigra.install/admin/sigra-logo-primary.svg test/example/priv/static/images/sigra-logo-primary.svg && echo "OK: light byte-identical"
cmp priv/templates/sigra.install/admin/sigra-logo-primary-dark.svg test/example/priv/static/images/sigra-logo-primary-dark.svg && echo "OK: dark byte-identical"

# 3. Companion marks: correct fills, no CSS, correct viewBox
grep -q 'fill="#151515"' test/example/priv/static/images/rail-accent-mark.svg && echo "OK: light glyphs"
grep -q 'fill="#c2410c"' test/example/priv/static/images/rail-accent-mark.svg && echo "OK: light block"
grep -q 'fill="#f4f1eb"' test/example/priv/static/images/rail-accent-mark-dark.svg && echo "OK: dark glyphs"
grep -q 'fill="#fdba74"' test/example/priv/static/images/rail-accent-mark-dark.svg && echo "OK: dark block"
grep -q '@media' test/example/priv/static/images/rail-accent-mark.svg && echo "FAIL: CSS in light" || echo "OK: no CSS in light"
grep -q '@media' test/example/priv/static/images/rail-accent-mark-dark.svg && echo "FAIL: CSS in dark" || echo "OK: no CSS in dark"

# 4. XML parseability for all 6 new SVGs
xmllint --noout \
  priv/templates/sigra.install/admin/sigra-logo-primary{,-dark}.svg \
  test/example/priv/static/images/sigra-logo-primary{,-dark}.svg \
  test/example/priv/static/images/rail-accent-mark{,-dark}.svg

# 5. Token parity (4 greps, all must be OK)
grep '"ember-700"' brandbook/tokens.json | grep -q '#c2410c' && echo "OK: brandbook ember-700"
grep 'sg-color-brand:' test/example/priv/static/assets/css/app.css | grep -q '#c2410c' && echo "OK: sg-color-brand"
grep 'sg-logo-rail-accent:' test/example/priv/static/assets/css/app.css | grep -q '#fdba74' && echo "OK: sg-logo-rail-accent"
grep 'sigra-auth-light-accent' priv/templates/sigra.install/core/sigra_auth.css | grep -q '#c2410c' && echo "OK: auth accent"

# 6. Both test suites green
mix test
(cd test/example && mix test)
```
</verification>

<success_criteria>
- All four admin lockup SVGs contain viewBox="20 220 2361 1000" and the substring "Space Grotesk v2.0" in desc
- All four SVGs are path-only: grep -q '<path' passes; grep -q '<text' and grep -q 'font-family' both fail (no matches)
- cmp exits 0 for both light and dark installer-vs-example pairs
- Companion marks have D4 geometry, correct explicit fills (light: #151515/#c2410c; dark: #f4f1eb/#fdba74), no CSS media query
- Both guard test files no longer contain the v1 strings "20 12 188 54" or "Inter Display Black v4.1." and now contain "20 220 2361 1000" and "Space Grotesk v2.0"
- All 4 token parity greps print "OK" — no CSS values were edited
- mix test (root) exits 0
- cd test/example && mix test exits 0
</success_criteria>

<output>
Create .planning/phases/183-propagation-parity-verification/183-01-SUMMARY.md when done.
</output>
