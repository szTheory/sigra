---
phase: 10.1-installer-and-library-fixes
plan: 06
type: execute
wave: 2
depends_on:
  - "10.1-02"
files_modified:
  - test/mix/tasks/sigra.install_test.exs
  - test/sigra/audit/cursor_portability_test.exs
  - lib/mix/tasks/sigra.install.ex
  - lib/sigra/audit/cursor.ex
  - .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md
autonomous: true
requirements:
  - remediation
  - D-10.1-15
  - D-10.1-16
tags: [test-fixes, pre-existing, investigation]

must_haves:
  truths:
    - "Root cause of each of the 3 pre-existing failing tests is documented before a fix is written"
    - "test/mix/tasks/sigra.install_test.exs passes (2 failures → 0)"
    - "test/sigra/audit/cursor_portability_test.exs passes (1 failure → 0)"
    - "If the root cause is a real library/installer bug (not stale assertions), the fix is in library/installer code, not in the test"
  artifacts:
    - path: ".planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md"
      provides: "Investigation notes for each of the 3 failures — root cause, fix decision (test vs library/installer), rationale"
    - path: "test/mix/tasks/sigra.install_test.exs"
      provides: "2 previously-failing tests now passing"
    - path: "test/sigra/audit/cursor_portability_test.exs"
      provides: "1 previously-failing test now passing"
  key_links:
    - from: "investigation findings"
      to: "fix in library OR test OR installer template"
      via: "root cause dictates the fix location"
      pattern: "investigation"
---

<objective>
Fix the 3 pre-existing failing tests that predate phase 10 and have been deferred across multiple plans:

1. **D-10.1-15:** `test/mix/tasks/sigra.install_test.exs` — 2 failures
2. **D-10.1-16:** `test/sigra/audit/cursor_portability_test.exs` — 1 failure

Critical requirement from phase context: **investigate root cause BEFORE fixing**. These tests have been failing since before phase 10 and may indicate genuine installer or cursor-pagination bugs worth surfacing. Do not paper over them by updating test assertions until the underlying cause is understood.

Purpose: Remove the "3 pre-existing failures" footer from every future phase summary and unblock the `mix test` all-green gate.

Output: Investigation document explaining each root cause, plus the minimum-scope fix (test, installer template, or library — whichever the investigation says is correct), plus green CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-installer-and-library-fixes/10.1-CONTEXT.md
@.planning/phases/10-developer-experience/deferred-items.md
@.planning/phases/10-developer-experience/10-06-SUMMARY.md

<interfaces>
<!-- The failing tests are known from phase 10 summaries.  -->
<!-- Deferred-items.md 10-03 entry confirms cursor_portability predates phase 10. -->
<!-- 10-06-SUMMARY.md confirms sigra.install_test.exs failures are fixture-template-compile checks that fail on main even without 10-06. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate root cause of all 3 pre-existing failures</name>
  <files>
    .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md
  </files>
  <read_first>
    - `test/mix/tasks/sigra.install_test.exs` — the full file (find the 2 failing tests)
    - `test/sigra/audit/cursor_portability_test.exs` — the full file (find the 1 failing test)
    - `lib/sigra/audit/cursor.ex` (or wherever cursor pagination lives) — read for context
    - `lib/mix/tasks/sigra.install.ex` — the install task (for sigra.install_test failures)
    - `.planning/phases/10-developer-experience/deferred-items.md` line "2026-04-09 (10-03)" — prior investigation notes
  </read_first>
  <action>
    **Step 1: Run the failing tests in isolation and capture exact output:**

    ```
    mix test test/mix/tasks/sigra.install_test.exs 2>&1 | tee /tmp/10.1-06-install.log
    mix test test/sigra/audit/cursor_portability_test.exs 2>&1 | tee /tmp/10.1-06-cursor.log
    ```

    For each failure, capture:
    - Test name + line number
    - Full assertion error (left vs right for `assert ==`, or the specific raise for `assert_raise`)
    - Stack trace (top 5 frames)

    **Step 2: For each failure, classify the root cause into ONE of these buckets:**

    a. **Stale test assertion** — the test hardcodes a value (e.g., template substring) that the code has moved past. Fix = update the test. Low risk.

    b. **Genuine library/installer bug** — the test is correct, the code is broken. Fix = fix the library or installer template. Medium-high risk, may surface other issues.

    c. **Environmental flake** — the test depends on DB state, file-system, or process state that isn't deterministic. Fix = add setup/teardown or mark test flaky with a plan to fix properly. Rare but possible.

    d. **Test depends on unimplemented feature** — the test was written aspirationally. Fix = either ship the feature or delete the test with a justification.

    **Step 3: Document in `.planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md`:**

    ```markdown
    # Phase 10.1-06 Pre-existing Failing Test Investigation

    ## Failure 1: test/mix/tasks/sigra.install_test.exs — <test name>

    **Line:** <file:line>
    **Captured output:**
    ```
    <paste the exact failure output>
    ```

    **Root cause classification:** [stale-assertion | library-bug | installer-bug | flake | aspirational]

    **Analysis:**
    <3-5 sentences explaining what the test is checking, what the code actually does,
    and why they disagree. Cite specific file:line references.>

    **Fix location:** [test file | library file | installer template | delete test]

    **Fix description:**
    <1-2 sentences describing what will change and why.>

    ## Failure 2: test/mix/tasks/sigra.install_test.exs — <test name>
    <same structure>

    ## Failure 3: test/sigra/audit/cursor_portability_test.exs — <test name>
    <same structure>

    ## Fix Order
    <If one fix enables the others, explicit ordering. Otherwise "independent".>
    ```

    **Step 4: git blame the failing lines** to see when the test was added/last modified and what the original intent was:
    ```
    git blame test/mix/tasks/sigra.install_test.exs | grep -A1 -B1 <line>
    git blame test/sigra/audit/cursor_portability_test.exs
    ```
    Include the commit hash and date in the investigation doc.

    **Step 5: Stop after writing the doc.** Do NOT apply fixes yet — Task 2 applies them based on the decisions recorded here.

    Commit: `docs(10.1-06): investigate root cause of 3 pre-existing failing tests`
  </action>
  <acceptance_criteria>
    - [ ] `test -f .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md`
    - [ ] `grep -c "## Failure" .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md` == 3
    - [ ] `grep -c "Root cause classification" .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md` == 3
    - [ ] `grep -c "Fix location" .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md` == 3
    - [ ] Each Failure section cites a file:line reference
    - [ ] Commit `docs(10.1-06): investigate` exists in git log
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md && test $(grep -c "^## Failure" .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md) -eq 3</automated>
  </verify>
  <done>Investigation document complete with root cause, classification, and proposed fix location for each of the 3 failures. No code changed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Apply fixes per investigation findings</name>
  <files>
    test/mix/tasks/sigra.install_test.exs,
    test/sigra/audit/cursor_portability_test.exs,
    lib/mix/tasks/sigra.install.ex,
    lib/sigra/audit/cursor.ex
  </files>
  <read_first>
    - `.planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md` (the product of Task 1)
    - Whichever file(s) the investigation said to fix (different per failure)
  </read_first>
  <action>
    **Follow the investigation document.** Each failure's fix depends on its root cause classification:

    - **stale-assertion:** Update the test assertion to match current code behavior. Document in commit message why the old assertion was wrong.

    - **library-bug / installer-bug:** Fix the library or template file. This is the higher-risk path — the fix may surface related issues. Before committing, run `mix test` on the broader area (e.g., all installer tests, all audit tests) to catch regressions.

    - **flake:** Add deterministic setup (e.g., `async: false`, explicit Sandbox checkout, `Process.sleep` is NOT acceptable — find the real cause). If truly flaky, use `@tag :skip` with a comment pointing at a tracking issue.

    - **aspirational:** Delete the test with a commit message explaining which phase the aspirational feature will actually ship in. If the phase isn't scheduled, file a roadmap note instead of deleting.

    Apply fixes one at a time, committing each separately so blame is preserved:
    - `fix(10.1-06): sigra.install_test #1 — <root cause shorthand>`
    - `fix(10.1-06): sigra.install_test #2 — <root cause shorthand>`
    - `fix(10.1-06): cursor_portability_test — <root cause shorthand>`

    **Step 2: After each fix, verify isolated green:**
    ```
    mix test test/mix/tasks/sigra.install_test.exs
    mix test test/sigra/audit/cursor_portability_test.exs
    ```

    **Step 3: After all 3 fixes, verify full suite:**
    ```
    mix test 2>&1 | tail -5
    ```
    Expected: `N tests, 0 failures` where N is the new total (increased by 3 compared to the old baseline where 3 failed).

    **Step 4: If the investigation concluded that one failure is an aspirational test that should be deleted,** the full-suite count will DECREASE instead of the failure count; that's fine as long as failures == 0.

    **Caveat:** If while applying a library-bug fix you surface a NEW test failure elsewhere, STOP and add it to `deferred-items.md` with a note explaining the chain. Do NOT expand scope of this plan to chase cascading failures — that's a new plan.
  </action>
  <acceptance_criteria>
    - [ ] `mix test test/mix/tasks/sigra.install_test.exs 2>&1 | grep -E "0 failures"` returns non-empty (test file is clean)
    - [ ] `mix test test/sigra/audit/cursor_portability_test.exs 2>&1 | grep -E "0 failures"` returns non-empty
    - [ ] `mix test 2>&1 | tail -5 | grep -E "0 failures"` returns non-empty (full suite clean OR only failures are in test/example subproject which is expected per test_load_filters)
    - [ ] At least 1 commit per failure with a descriptive message in git log
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/sigra.install_test.exs test/sigra/audit/cursor_portability_test.exs</automated>
  </verify>
  <done>All 3 previously-failing tests now pass. Fix location (test vs library/installer) matches the investigation document. Full mix test suite has 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → CI | Failing tests erode trust in the suite; "always 3 failures" numbs the team to real regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-06-01 | Repudiation | Fixing a test by updating its assertion papers over a real library bug | mitigate | Task 1 MANDATES investigation before fix. Fix location is documented per-failure. Reviewer can spot-check the decision. |
| T-10.1-06-02 | Elevation of Privilege | cursor_portability_test failure hides a real pagination bug that could leak data across users | mitigate | Investigation will surface this. If root cause is a real bug, Task 2 fixes the library, not the test. |
| T-10.1-06-03 | Denial of Service | sigra.install_test failure hides a real installer bug that breaks fresh installs | mitigate | Same pattern — investigate first. Plan 10.1-02 already backported 16 known installer bugs; these 2 test failures may overlap with that list, which is fine if so. |
| T-10.1-06-04 | Scope creep | Applying a library-bug fix surfaces cascading failures | mitigate | Task 2 Step 4 caveat: stop and log in deferred-items.md, do not expand plan scope |
</threat_model>

<verification>
- `mix test test/mix/tasks/sigra.install_test.exs` exits 0
- `mix test test/sigra/audit/cursor_portability_test.exs` exits 0
- `mix test` (full suite, no subproject) exits 0 with 0 failures
- Investigation document cites specific root cause for each failure
</verification>

<success_criteria>
1. Investigation doc identifies root cause + fix location for each of 3 failures
2. All 3 previously-failing tests now pass
3. `mix test` full suite has 0 failures
4. Fixes are in the correct location (test vs library/installer) per the investigation
5. No cascading failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-installer-and-library-fixes/10.1-06-SUMMARY.md`
</output>
