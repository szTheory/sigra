---
phase: 10.1-installer-and-library-fixes
plan: 06
subsystem: test-suite-hygiene
tags: [test-fixes, pre-existing, investigation, installer, cursor]
requires:
  - priv/templates/sigra.install/auth_fixtures.ex (plan 10.1-02 fixes #13,#14)
  - priv/templates/sigra.install/conn_case_helpers.ex (plan 10.1-02 fix #15)
  - priv/templates/sigra.install/reset_password_controller.ex (plan 10.1-02 fix #7)
  - test/sigra/audit/cursor_test.exs (actual cursor pagination coverage)
provides:
  - .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md (root cause analysis for all pre-existing failures)
  - Full `mix test` suite: 1244 tests, 0 failures (was 1245 tests, 4 failures)
  - Removal of "3 pre-existing failures" footer from future phase summaries
affects:
  - test/mix/tasks/sigra.install_test.exs (3 test bindings extended)
  - test/sigra/install/generator_reset_test.exs (1 stale assertion replaced)
  - test/sigra/audit/cursor_portability_test.exs (deleted — aspirational stub)
tech-stack:
  added: []
  patterns:
    - "Investigate before fix: classify each failure as stale-assertion / library-bug / flake / aspirational and pick fix location accordingly (test vs library vs delete)"
    - "When templates acquire new EEx bindings, every caller — installer real path AND test fixtures — must be updated in lockstep. The drift test (10.1-02 Task 3) catches template/example drift; these failures show that test-only bindings need the same discipline."
key-files:
  created:
    - .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md
  modified:
    - test/mix/tasks/sigra.install_test.exs
    - test/sigra/install/generator_reset_test.exs
  deleted:
    - test/sigra/audit/cursor_portability_test.exs
decisions:
  - "The cursor_portability_test was deleted, not repaired. Its StubRepo.all/1 returned [] unconditionally — the test never worked and cannot work without a full in-memory ordered StubRepo implementation that would duplicate the Ecto integration coverage already provided by test/sigra/audit/cursor_test.exs."
  - "The generator_reset_test 'alias MyApp.Auth' assertion was replaced with a positive + refute pair (assert fully-qualified call present, refute bare alias line present) so future regressions are caught from both directions, not just re-added aliases."
  - "Scope decision: Failure 5 (generator_reset_test) was surfaced by Task 2 Step 3 full-suite run and resolved in scope because it shares the same root-cause class as Failures 1-3 (stale assertions after plan 10.1-02 template refactor) and blocks the plan's own 0-failures success criterion. Not scope creep because it does not leave the 'stale 10.1-02 template assertion' subtree."
metrics:
  duration: ~35 minutes
  tasks: 2
  files_created: 1
  files_modified: 2
  files_deleted: 1
  completed: 2026-04-10
requirements: [remediation, D-10.1-15, D-10.1-16]
---

# Phase 10.1 Plan 06: Pre-existing Failing Tests — Root Cause + Fix Summary

One-liner: Investigated all pre-existing failing tests in the sigra test suite, classified each by root cause, and applied minimum-scope fixes — 3 `sigra.install_test.exs` cases + 1 `generator_reset_test.exs` case were stale assertions after the plan 10.1-02 template refactor (test bindings missing `web_module` / `app_module` / `context_alias`, and one bare `alias` assertion after fix #7 dropped the alias), and `cursor_portability_test.exs` was deleted as an aspirational Wave-0 scaffold whose inline `StubRepo.all/1` returned `[]` unconditionally.

## What Shipped

### Task 1: Investigation document

`.planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md` — per-failure breakdown (captured output, root cause classification, git blame, fix location, fix description) for all 4 initially-known failures. Addendum added after Task 2 Step 3 for Failure 5 (generator_reset_test) which was surfaced by the full-suite run.

Classification summary:

| # | File                                    | Test                                                           | Class           | Fix                                                      |
| - | --------------------------------------- | -------------------------------------------------------------- | --------------- | -------------------------------------------------------- |
| 1 | sigra.install_test.exs:179              | renders fixtures template                                      | stale-assertion | Add web_module + context_alias to binding                |
| 2 | sigra.install_test.exs:194              | renders fixtures template with Phase 4 session fixtures        | stale-assertion | Add web_module + context_alias to binding                |
| 3 | sigra.install_test.exs:278              | renders conn_case_helpers with session type options            | stale-assertion | Add app_module + context_alias to binding                |
| 4 | cursor_portability_test.exs:31          | paginates deterministically across cursor boundary             | aspirational    | Delete (StubRepo.all/1 returns [] — never worked)        |
| 5 | generator_reset_test.exs:28 (addendum)  | reset_password_controller.ex renders module name from assigns  | stale-assertion | Replace `alias MyApp.Auth` assertion with FQ-call check  |

Note: The plan expected 2 install-test failures but baseline showed 3 — all three are the same class, fixed in one commit.

### Task 2: Apply fixes

- **Commit 32dbae1** — `fix(10.1-06): sigra.install_test bindings — stale after plan 10.1-02`
  - test/mix/tasks/sigra.install_test.exs — three `binding` lists extended with `web_module: "MyAppWeb"`, `app_module: "MyApp"`, `context_alias: "Accounts"` to match the post-10.1-02 templates.
- **Commit 182edbf** — `fix(10.1-06): delete aspirational cursor_portability_test`
  - test/sigra/audit/cursor_portability_test.exs deleted. Moduledoc confirmed the test was Wave-0 placeholder scaffolding; real portability coverage lives in test/sigra/audit/cursor_test.exs (Ecto integration) and test/sigra/audit/query_test.exs (query construction).
- **Commit 81d66fd** — `fix(10.1-06): generator_reset_test stale alias assertion`
  - test/sigra/install/generator_reset_test.exs:28 — replaced `assert result =~ "alias MyApp.Auth"` with:
    ```elixir
    assert result =~ "MyApp.Auth.get_user_by_email"
    refute result =~ ~r/^\s*alias MyApp\.Auth\s*$/m
    ```
    The positive assertion confirms the fully-qualified call is preserved; the refute confirms the bare alias line is absent (catching both accidental re-additions and future drift).

## Deviations from Plan

### Rule 1 — Auto-fixed bug: scope expansion to Failure 5

- **Found during:** Task 2 Step 3 (full-suite run)
- **Issue:** `test/sigra/install/generator_reset_test.exs:28` failed with the same stale-assertion pattern as Failures 1-3 (expected `alias MyApp.Auth` in rendered template, but plan 10.1-02 fix #7 removed that alias).
- **Decision:** Fixed in scope rather than deferring, because (a) same root-cause class as Failures 1-3, (b) blocks plan success criterion #3 (`mix test` full suite 0 failures), (c) no scope creep into unrelated subsystems.
- **Files modified:** test/sigra/install/generator_reset_test.exs
- **Commit:** 81d66fd

### Count discrepancy: "2 failures" → 3 failures in sigra.install_test.exs

The plan described 2 failures in `sigra.install_test.exs` but the baseline showed 3. All three share identical root cause (missing post-10.1-02 bindings), so this is a count error in the plan description, not a functional divergence. Investigation doc documents all three.

## Acceptance Criteria — Verification

### Task 1

- [x] `test -f .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md` — present
- [x] `grep -c "^## Failure" .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md` — 4 (plan said 3, but baseline had 4 because the addendum documents Failure 5)
- [x] Each Failure section cites specific file:line references
- [x] Commit `docs(10.1-06): investigate` exists: 73f5065
- [x] Addendum commit: eb6303e

### Task 2

- [x] `mix test test/mix/tasks/sigra.install_test.exs` → 19 tests, 0 failures
- [x] `test/sigra/audit/cursor_portability_test.exs` — deleted (verify: `test ! -f test/sigra/audit/cursor_portability_test.exs`)
- [x] `mix test test/sigra/install/generator_reset_test.exs` → 23 tests, 0 failures
- [x] `mix test` (full suite) → 1244 tests, 0 failures (+ 30 doctests, 3 properties)
- [x] 3 commits: 32dbae1 (install_test bindings), 182edbf (cursor delete), 81d66fd (generator_reset)

## Success Criteria — Verification

1. ✓ Investigation doc identifies root cause + fix location for each failure (5 total, not 3 as originally scoped — delta documented)
2. ✓ All previously-failing tests now pass (or are deleted with justification)
3. ✓ `mix test` full suite has 0 failures (1244 tests, 30 doctests, 3 properties, 0 failures)
4. ✓ Fixes are in the correct location per investigation:
   - Failures 1-3, 5 → test files (stale-assertion → update test)
   - Failure 4 → delete test (aspirational → never worked)
5. ✓ No cascading failures — full suite is fully green

## Commits

- `73f5065` — docs(10.1-06): investigate root cause of 3 pre-existing failing tests
- `32dbae1` — fix(10.1-06): sigra.install_test bindings — stale after plan 10.1-02
- `182edbf` — fix(10.1-06): delete aspirational cursor_portability_test
- `81d66fd` — fix(10.1-06): generator_reset_test stale alias assertion
- `eb6303e` — docs(10.1-06): investigation addendum — Failure 5 same root cause class

## Self-Check: PASSED

- [x] .planning/phases/10.1-installer-and-library-fixes/10.1-06-investigation.md — FOUND
- [x] test/mix/tasks/sigra.install_test.exs modified — FOUND (3 bindings extended)
- [x] test/sigra/install/generator_reset_test.exs modified — FOUND
- [x] test/sigra/audit/cursor_portability_test.exs — DELETED (verified absent)
- [x] Commit 73f5065 — FOUND
- [x] Commit 32dbae1 — FOUND
- [x] Commit 182edbf — FOUND
- [x] Commit 81d66fd — FOUND
- [x] Commit eb6303e — FOUND
- [x] `mix test` full suite — 1244 tests, 0 failures
