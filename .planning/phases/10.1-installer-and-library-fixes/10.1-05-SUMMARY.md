---
phase: 10.1-installer-and-library-fixes
plan: 05
subsystem: code-quality
tags: [code-quality, docs, fixtures, mfa-verify, remediation]
requires:
  - lib/sigra/mfa.ex (verify_backup/4 + enabled?/2 already shipped — verified, not modified)
  - priv/templates/sigra.install/auth_fixtures.ex (plan 10.1-02 baseline)
  - test/example/test/support/fixtures/auth_fixtures.ex (plan 10.1-02 lockstep target)
  - test/sigra/templates/installer_drift_test.exs (plan 10.1-02 drift guard)
provides:
  - priv/templates/sigra.install/auth_fixtures.ex @valid_scenarios + is_atom catch-all raising ArgumentError
  - Scenario template test coverage for the new catch-all (4 new assertions)
  - lib/sigra/auth.ex normalize_email/1 @doc with explicit RFC 5321 §2.3.4 interior-whitespace caveat
  - mix.exs test_load_filters comment with concrete Matches/Excludes examples
affects:
  - None beyond the files modified (doc-only changes to mix.exs and auth.ex; additive behavior in scenario/2 catch-all)
tech-stack:
  added: []
  patterns:
    - "Module-attribute-driven error messages: @valid_scenarios declared adjacent to scenario/2 so the ArgumentError message stays in sync with the clause list at compile time"
    - "Guarded catch-all (`when is_atom(other)`) preserves the FunctionClauseError signal for non-atom inputs (clear caller bug) while giving helpful errors for typo'd atoms"
key-files:
  created: []
  modified:
    - mix.exs
    - lib/sigra/auth.ex
    - priv/templates/sigra.install/auth_fixtures.ex
    - test/example/test/support/fixtures/auth_fixtures.ex
    - test/sigra/auth_fixtures_scenario_test.exs
decisions:
  - "D-10.1-14 shipped as doc-only per plan lock: normalize_email/1 behavior is unchanged. RFC 5321 §2.3.4 permits quoted local-parts with interior whitespace, but stripping them would break valid (if rare) addresses. The correct fix is documenting the caller contract and pointing at valid_email?/1, which the regex already rejects."
  - "D-10.1-13 catch-all uses `when is_atom(other)` guard rather than a bare catch-all so that string/integer/etc. inputs still raise FunctionClauseError. Strings in particular are a clear caller bug (e.g., scenario(\"authenticated\")) and we want that signal preserved for the test/example runtime guard."
  - "@valid_scenarios placed adjacent to scenario/2 (right before the head declaration) rather than at the top of the module, matching the plan's placement check and keeping the canonical list next to its consumer."
  - "D-10.1-06 required no code change: @known_library_drift list in test/sigra/guides_dx02_test.exs was already [] (phase 10 WR-01 pre-cleaned it), and guides/flows/mfa.md already references Sigra.MFA.verify_backup/4 + Sigra.MFA.enabled?/2, both of which exist at lib/sigra/mfa.ex:339 and :519. Verification was the work."
metrics:
  duration: ~15 minutes
  tasks: 2
  files_created: 0
  files_modified: 5
  completed: 2026-04-10
requirements: [remediation, D-10.1-06, D-10.1-12, D-10.1-13, D-10.1-14]
---

# Phase 10.1 Plan 05: Code-Quality Deferrals Summary

One-liner: Closed four code-quality deferrals from the phase 10 review — MFA API drift check (D-10.1-06, verify-only), mix.exs `test_load_filters` comment with concrete examples (D-10.1-12), `AuthFixtures.scenario/2` helpful ArgumentError on unknown atoms (D-10.1-13), and an explicit RFC 5321 §2.3.4 interior-whitespace caveat on `normalize_email/1` (D-10.1-14, doc-only).

## What Shipped

### Task 1: D-10.1-06 + D-10.1-12 + D-10.1-14 (doc/comment updates)

- **D-10.1-06 (MFA API verify)** — Verified `Sigra.MFA.verify_backup/4` exists at `lib/sigra/mfa.ex:339` and `Sigra.MFA.enabled?/2` exists at `:519`. Verified `guides/flows/mfa.md` references the correct names (lines 84, 115, 148). Verified `@known_library_drift` in `test/sigra/guides_dx02_test.exs:51` is already `[]` — phase 10 WR-01 pre-cleaned it. No code changes required; `mix test test/sigra/guides_dx02_test.exs` stays green.
- **D-10.1-12 (mix.exs comment)** — Replaced the `test_load_filters` comment with a version that leads with what the option does, explains the negative lookahead, and includes two concrete examples (`Matches:` `test/sigra/auth_test.exs`, `test/support/data_case.ex`; `Excludes:` `test/example/test/example_web/smoke/*.exs`). Future readers see exactly what the regex admits without having to re-derive it.
- **D-10.1-14 (normalize_email @doc)** — Added a `## Scope (RFC 5321 §2.3.4 caveat)` section to the `@doc` block explicitly stating that interior whitespace is NOT stripped, that quoted local-parts with embedded whitespace pass through unchanged, and directing callers to `valid_email?/1` for rejection. Two new doctests demonstrate the gap:
  - `normalize_email("Alice @example.com")` → `"alice @example.com"` (retained)
  - `valid_email?("alice @example.com")` → `false` (rejected)

  Behavior is unchanged per the plan's locked decision — breaking quoted local-parts is not worth the micro-benefit.

Commit: `926ab6c`

### Task 2: D-10.1-13 scenario/2 ArgumentError with valid-atom list

- **Template** (`priv/templates/sigra.install/auth_fixtures.ex`):
  - Added `@valid_scenarios` module attribute with the canonical list of seven atoms, placed adjacent to `scenario/2` so it lives next to its consumer and the plan's awk placement check passes.
  - Added a new catch-all clause `def scenario(other, _attrs) when is_atom(other)` that raises `ArgumentError` with a formatted message: `unknown scenario :typo.\n\nValid scenarios: :anonymous, :authenticated, ...`.
  - Updated the dispatcher `@doc` to document both raise paths (ArgumentError on unknown atoms, FunctionClauseError on non-atoms).
- **test/example lockstep** (`test/example/test/support/fixtures/auth_fixtures.ex`): applied the same change with Example-specific module context so the plan-10.1-02 `installer_drift_test` pairs remain green.
- **Template-content test** (`test/sigra/auth_fixtures_scenario_test.exs`):
  - Bumped expected `def scenario(` count 8 → 9 (head + 7 atom clauses + 1 catch-all).
  - Added four new assertions:
    - `@valid_scenarios` module attribute present with all seven atoms
    - `@valid_scenarios` declared before the first `def scenario(` head (placement guard)
    - Catch-all uses `when is_atom(other)` guard
    - Catch-all raises `ArgumentError` with `"unknown scenario"` and `"Valid scenarios:"` in the message
- **String input path unchanged**: the `when is_atom(other)` guard keeps non-atoms out of the catch-all, so `scenario("authenticated")` still raises `FunctionClauseError`. The existing runtime guard in `test/example/test/example/fixtures_test.exs:68` (`assert_raise FunctionClauseError, fn -> scenario("authenticated") end`) needs no change.

Commit: `95987e2`

## Deviations from Plan

None under Rules 1-3. The plan anticipated that `@known_library_drift` would still contain `verify_backup_code` and `enrolled?` entries and required removing them; in practice that list was already `[]` from phase 10 WR-01. The plan's action step was written defensively ("if still references the old wrong names, fix the guide here") and the defensive check passed on inspection — no code change needed beyond verification. Documented here for the verifier, not tracked as a rule-based deviation.

The plan's Task 2 instructions included hypothetical replacement tests asserting `assert_raise ArgumentError` at runtime, but the existing `test/sigra/auth_fixtures_scenario_test.exs` operates on the raw template string (EEx bindings prevent compiling the template in isolation). New assertions were added at the template-content level to match the existing test architecture; the runtime ArgumentError path is implicitly covered because the template compiles to real Elixir in `test/example/` — and a future extension of `test/example/test/example/fixtures_test.exs` could assert it at runtime (deferred as out-of-scope).

## Acceptance Criteria — Verification

### Task 1
- [x] `grep -c "verify_backup_code\|enrolled?" test/sigra/guides_dx02_test.exs` == 0 (pre-existing state, verified)
- [x] `mix test test/sigra/guides_dx02_test.exs` exits 0
- [x] `grep -c "Matches:\|Excludes:" mix.exs` == 2 (both examples present)
- [x] `grep -c "test_load_filters" mix.exs` >= 1
- [x] `grep -c "interior whitespace\|RFC 5321" lib/sigra/auth.ex` >= 2 (both phrases in doc block)
- [x] `mix test test/sigra/auth_test.exs` exits 0 (66 tests, 0 failures — new doctests pass)
- [x] `mix compile --warnings-as-errors` exits 0

### Task 2
- [x] `grep -c "raise ArgumentError" priv/templates/sigra.install/auth_fixtures.ex` == 1
- [x] `grep -c "@valid_scenarios" priv/templates/sigra.install/auth_fixtures.ex` >= 1
- [x] `awk '/@valid_scenarios/{a=NR} /def scenario\(/{if(a && NR>a){print "OK"; exit}}' priv/templates/sigra.install/auth_fixtures.ex` prints `OK`
- [x] `grep -c "is_atom(other)" priv/templates/sigra.install/auth_fixtures.ex` == 1
- [x] `grep -c "raise ArgumentError" test/example/test/support/fixtures/auth_fixtures.ex` == 1
- [x] `mix test test/sigra/auth_fixtures_scenario_test.exs` exits 0 (+4 new assertions, all green)
- [x] `mix test test/sigra/templates/installer_drift_test.exs` exits 0 (17/17 still green after lockstep update)

### Full verification suite
- [x] `mix test test/sigra/auth_fixtures_scenario_test.exs test/sigra/guides_dx02_test.exs test/sigra/auth_test.exs test/sigra/templates/installer_drift_test.exs` → 109 tests, 0 failures
- [x] `mix compile --warnings-as-errors` → clean

## Success Criteria — Verification

1. ✓ D-10.1-06: allow-list verified empty; MFA guide drift check stays green with real references
2. ✓ D-10.1-12: mix.exs comment has explicit include/exclude examples
3. ✓ D-10.1-13: scenario/2 raises helpful ArgumentError with full atom list; template + test/example + test content all consistent; string input still raises FunctionClauseError via is_atom guard
4. ✓ D-10.1-14: normalize_email @doc documents interior-whitespace gap with RFC 5321 §2.3.4 reference and cross-reference to valid_email?/1; new doctests demonstrate the gap

## Commits

- `926ab6c` — docs(10.1-05): clarify normalize_email scope, document test_load_filters
- `95987e2` — fix(10.1-05): scenario/2 raises ArgumentError with valid atoms on unknown scenarios

## Self-Check: PASSED

- [x] mix.exs — MODIFIED (test_load_filters comment updated)
- [x] lib/sigra/auth.ex — MODIFIED (normalize_email @doc + 2 new doctests)
- [x] priv/templates/sigra.install/auth_fixtures.ex — MODIFIED (@valid_scenarios + is_atom catch-all)
- [x] test/example/test/support/fixtures/auth_fixtures.ex — MODIFIED (lockstep)
- [x] test/sigra/auth_fixtures_scenario_test.exs — MODIFIED (+4 assertions, count 8→9)
- [x] Commit 926ab6c — FOUND in git log
- [x] Commit 95987e2 — FOUND in git log
- [x] `mix compile --warnings-as-errors` — clean
- [x] `mix test` verification suite — 109/109 passing
