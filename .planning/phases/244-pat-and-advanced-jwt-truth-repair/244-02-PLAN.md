---
phase: 244-pat-and-advanced-jwt-truth-repair
plan: 02
type: execute
wave: 2
depends_on: [244-01]
files_modified:
  - lib/sigra/api_token.ex
  - lib/sigra/auth.ex
  - test/sigra/api_token_test.exs
autonomous: true
requirements: [PAT-02]
must_haves:
  truths:
    - "Per D-04, PAT revocation constrains both token ID and the Scope-derived owner and does not disclose a foreign owner."
    - "Per D-05, PAT creation accepts empty scopes and a single configured scope, rejects duplicates/malformed/unregistered scopes, and validates at the library boundary."
    - "Repeated revoke of an absent, foreign, or already-revoked token returns the same bounded not-found result without additional mutation."
  artifacts:
    - path: "lib/sigra/api_token.ex"
      provides: "Owner-constrained revoke and server-side scope allowlist enforcement"
    - path: "lib/sigra/auth.ex"
      provides: "Public owner-aware PAT facade"
    - path: "test/sigra/api_token_test.exs"
      provides: "Owner, empty/single scope, and idempotent denial contracts"
  key_links:
    - from: "lib/sigra/auth.ex"
      to: "Sigra.APIToken.revoke_for_user/3"
      via: "owner-required public facade"
      pattern: "revoke.*user"
    - from: "lib/sigra/api_token.ex"
      to: "Sigra.APIToken.ScopeRegistry.validate_scopes/2"
      via: "library-enforced configured allowlist per D-05"
      pattern: "validate_scopes"
  prohibitions:
    - "No request parameter selects the PAT owner."
    - "No controller-only scope validation."
    - "No raw PAT persistence, logging, or telemetry."
    - "No app-session, native, OAuth-server, Lockspire, or Crosswake change."
---

<objective>
Trace PAT self-management through the library boundary so ownership and configured scopes remain server authoritative (PAT-02; D-04, D-05).

Purpose: Browser gates cannot prevent cross-account mutation if the persistence call remains ID-only, and controllers cannot be the only scope validator.
Output: Owner-aware PAT revoke APIs and deterministic scope/ownership tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-CONTEXT.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-PATTERNS.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-01-SUMMARY.md

<interfaces>
- `Sigra.APIToken.create(config, user, attrs)` already validates scopes through `ScopeRegistry.validate_scopes/2` before persistence.
- `Sigra.APIToken.revoke(config, token_id)` currently loads by ID; the new self-management API must require an owner and constrain `id` plus `user_id`.
- `Sigra.Auth` is the public facade consumed by generated host delegates.
</interfaces>
</context>

## Artifacts this phase produces

- `lib/sigra/install/features/core.ex` — independent API/JWT file, injection, config, route, and instruction selection.
- `priv/templates/sigra.install/core/auth_api_token.ex` and `api_token_controller.ex` — PAT-only owner-bound host delegates and browser management surface.
- `priv/templates/sigra.install/core/auth_jwt.ex` — JWT-only host-policy issuance/refresh delegate seam.
- `lib/sigra/api_token.ex` and `lib/sigra/auth.ex` — server-allowlisted PAT creation and owner-constrained revocation.
- `lib/sigra/config.ex`, `lib/sigra/jwt.ex`, `lib/sigra/jwt/validator.ex`, and `lib/sigra/jwt/refresh_token.ex` — strict JWT configuration, claims, verification, and locked refresh lifecycle.
- `test/sigra/install/features/core_test.exs`, `api_token_generator_test.exs`, and `test/sigra/planning/phase_244_generated_auth_runtime_proof_test.exs` — independent source and fresh-host proof.
- `test/sigra/api_token_test.exs`, `test/sigra/jwt_test.exs`, `test/sigra/jwt/signer_test.exs`, and `test/sigra/jwt_refresh_audit_cofate_test.exs` — PAT/JWT security and transaction contracts.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Revoke one PAT through an owner-constrained public API (PAT-02)</name>
  <precondition>A PostgreSQL service matching `test/test_helper.exs` is reachable so owner and audit co-fate are proven against the real query/transaction boundary.</precondition>
  <reversibility rating="costly">The owner-required public function signature changes generated-host call sites, but rollback requires coordinated code changes rather than a migration.</reversibility>
  <files>test/sigra/api_token_test.exs, lib/sigra/api_token.ex, lib/sigra/auth.ex</files>
  <read_first>
Read the existing create/revoke/revoke-all sections in `lib/sigra/api_token.ex`, their `Sigra.Auth` delegates, and the PAT test helpers. Preserve current Ecto.Multi audit co-fate, telemetry-after-commit, raw-token-once, expiry, and generic administrative capabilities outside the new self-management call.
  </read_first>
  <behavior>
    - RED first: the owner can revoke their token; another owner receives the same result as a missing ID and no row changes.
    - A second revoke of the same token is deterministic and does not create an extra audit mutation.
    - Creating with no scopes or one configured scope succeeds; malformed, duplicate, or unregistered scopes fail before persistence per D-05.
    - The public self-management facade cannot be called without an owner argument.
  </behavior>
  <action>
Add failing database-backed contracts first. Introduce `APIToken.revoke_for_user/3` (or an equivalently explicit owner-required name) whose query matches both token ID and `owner.id`, normalizes absent/foreign/already-revoked outcomes without ownership disclosure, and reuses the existing transactional update/audit/telemetry path. Expose the same owner-required call through `Sigra.Auth`. Keep `ScopeRegistry.validate_scopes/2` authoritative in `create/3`, and extend tests for empty and single configured scopes plus malformed, duplicate, and unregistered values. Do not introduce list ordering semantics: the generic PAT-02 ordering probe is treated as a flagged assumption because the requirement defines authorization membership, not presentation order.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/api_token_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
The test is red before implementation and green afterward; owner, foreign, absent, repeated revoke, empty scope, single scope, and invalid scope behavior are deterministic and persistence-safe.
  </acceptance_criteria>
  <done>PAT library operations enforce owner and server allowlist boundaries independently of presentation code.</done>
</task>

</tasks>

<threat_model>
## ASVS posture

Level 1; blocking threshold high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Host delegate → PAT library | Host-supplied owner/token identifiers enter the persistence boundary. |
| Requested scopes → configured registry | Client-selected subset is checked against server policy before write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-05 | Elevation of Privilege | PAT revoke query | high | mitigate | Query by token ID and owner ID per D-04 and normalize foreign/missing outcomes. |
| T-244-06 | Tampering | PAT create scopes | high | mitigate | Retain library-level ScopeRegistry validation and test empty/single/invalid boundaries per D-05. |
| T-244-07 | Information Disclosure | revoke result | medium | mitigate | Return the same bounded outcome for foreign and absent identifiers. |
| T-244-08 | Repudiation | repeated revoke | medium | mitigate | Reuse transactional audit co-fate and prove no duplicate mutation after terminal state. |
</threat_model>

<verification>
Run the focused Postgres suite with the project database available. Do not substitute a mock for the owner-constrained query and audit transaction.
</verification>

<success_criteria>
- PAT-02 ownership and allowlist rules are enforced in the library, not only the generated controller.
- Relevant empty/single/idempotency edge probes are executable; adjacency and list ordering remain explicitly non-normative assumptions.
</success_criteria>

<output>
Create `.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-02-SUMMARY.md` when done.
</output>
