---
phase: 244-pat-and-advanced-jwt-truth-repair
plan: 07
type: execute
wave: 7
depends_on: [244-06]
files_modified:
  - test/sigra/install/isolation_test.exs
  - test/sigra/install/templates_layout_test.exs
  - lib/sigra/install/features/core.ex
  - test/sigra/install/features/core_post_instructions_test.exs
autonomous: true
gap_closure: true
requirements: [PAT-01, PAT-02, JWT-01, JWT-02]
must_haves:
  truths:
    - "The CI-facing core-template inventory recognizes exactly the 56 intentional templates shipped after Phase 244, including the independent JWT host-policy template per D-01 and D-06."
    - "The explicit sorted template manifest equals the directory contents and includes auth_jwt.ex, rate_limit.ex, registration_controller.ex, and settings_controller.ex."
    - "PAT post-install guidance points adopters to the generated browser-session management route /users/api-tokens per D-03, not a nonexistent bearer-management path."
    - "Focused installer integration tests pass, and the repository-wide mix ci result is reported as live evidence with direct Phase 244 regressions separated from unrelated historical failures rather than waived."
  artifacts:
    - path: "test/sigra/install/isolation_test.exs"
      provides: "Exact 56-template isolation inventory contract"
      contains: "contains exactly 56 templates"
    - path: "test/sigra/install/templates_layout_test.exs"
      provides: "Complete sorted manifest and exact physical-layout contract"
      contains: "auth_jwt.ex"
    - path: "lib/sigra/install/features/core.ex"
      provides: "Truthful generated PAT management route guidance"
      contains: "/users/api-tokens"
    - path: "test/sigra/install/features/core_post_instructions_test.exs"
      provides: "Regression proof for the generated PAT route guidance"
      contains: "/users/api-tokens"
  key_links:
    - from: "priv/templates/sigra.install/core"
      to: "test/sigra/install/templates_layout_test.exs"
      via: "sorted directory listing equals the explicit 56-entry manifest"
      pattern: "assert core_files == Enum.sort\\(@manifest_post_move\\)"
    - from: "lib/sigra/install/features/core.ex"
      to: "test/sigra/install/features/core_post_instructions_test.exs"
      via: "--api post-install output names the same browser management route generated by the router"
      pattern: "/users/api-tokens"
---

<objective>
Close the Phase 244 repository-integration gap by reconciling the exact core-template inventory and making PAT post-install route guidance match the generated browser route (PAT-01, PAT-02, JWT-01, JWT-02; D-01, D-03, D-06).

Purpose: The Phase 244 behaviors are proven, but repository CI and adopter-facing guidance must describe the artifacts and routes that actually ship.
Output: Exact 56-template layout contracts, truthful PAT route instructions, focused regression coverage, and an honest repository-wide CI diagnostic.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-CONTEXT.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-RESEARCH.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-VERIFICATION.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-01-SUMMARY.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-03-SUMMARY.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-05-SUMMARY.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-06-SUMMARY.md

<interfaces>
- `test/sigra/install/templates_layout_test.exs` owns the only explicit core-template manifest, `@manifest_post_move`; the directory currently contains 56 sorted entries.
- The four entries absent from that manifest are `auth_jwt.ex`, `rate_limit.ex`, `registration_controller.ex`, and `settings_controller.ex`.
- `Core.post_instructions/2` renders `base_instruction_block/1`; its `--api` line must describe the browser/sudo PAT management routes generated under `/users/api-tokens` per D-03.
- `test/sigra/install/features/core_post_instructions_test.exs` renders the iodata through its existing `render/1` helper and already isolates `--api` and `--jwt` bindings.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reconcile the shipped template inventory and PAT guidance through CI-facing contracts</name>
  <precondition>`tmp/db.env` exists and the configured PostgreSQL service is reachable before the repository-wide `mix ci` diagnostic; the focused installer tests themselves remain database-independent.</precondition>
  <files>test/sigra/install/isolation_test.exs, test/sigra/install/templates_layout_test.exs, lib/sigra/install/features/core.ex, test/sigra/install/features/core_post_instructions_test.exs</files>
  <read_first>
Read each listed file once. Preserve the isolation symbol checks, flat-directory prohibition, sorted-manifest equality, existing output rendering helper, and independent `--api`/`--jwt` instruction branches. Use `244-VERIFICATION.md` as the attribution boundary: only inventory/layout failures and stale PAT route guidance are direct Phase 244 closure work.
  </read_first>
  <behavior>
    - RED first: the layout contract expects exactly 56 core templates and exact equality with a manifest containing all directory entries.
    - The manifest retains its existing 52 entries and adds exactly `auth_jwt.ex`, `rate_limit.ex`, `registration_controller.ex`, and `settings_controller.ex` in the established readable ordering.
    - The isolation contract expects exactly 56 templates while continuing to scan every template for forbidden cross-feature symbols.
    - `--api` post-install output names `/users/api-tokens`, matching the generated browser/authenticated/sudo route required by D-03; the isolated `--jwt` output remains host-policy-only per D-06.
    - Focused inventory and post-instruction tests pass before the repository-wide diagnostic runs.
    - The final `mix ci` result records its exit status and exact failing test modules/files; no remaining failure is labeled historical merely because it is outside the focused suite.
  </behavior>
  <action>
Update the two exact inventory assertions from 52 to 56. Extend `@manifest_post_move` with the four currently shipped entries that the manifest lacks; keep exact sorted directory equality as the contract and do not replace it with a count-only or subset assertion. Change the `--api` line in `base_instruction_block/1` to point to `/users/api-tokens`, which is the browser-session PAT management surface generated under D-03, while retaining the Auth-context follow-up guidance and leaving JWT instructions independent per D-01 and D-06. Change the focused post-instruction test to require the generated browser route and to reject drift back to the stale path. Run the focused four-file test command, then run the repository-wide `mix ci` diagnostic once. If CI remains nonzero, identify every failure by test/module and file, explicitly mark whether it is one of this plan's four modified contracts or unrelated pre-existing repository debt, and preserve that exact distinction in the plan SUMMARY. Do not repair unrelated Phase 234/236/240 evidence, architecture-guide, or legacy generator assertions in this gap plan; do not waive them or describe the repository gate as passing.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/isolation_test.exs test/sigra/install/templates_layout_test.exs test/sigra/install/features/core_post_instructions_test.exs --trace</automated>
    <diagnostic>source tmp/db.env &amp;&amp; MIX_ENV=test mix ci; record the real exit status and classify every remaining failure in 244-07-SUMMARY.md without converting a nonzero result into a passing gate.</diagnostic>
  </verify>
  <acceptance_criteria>
The three focused installer test modules pass; the explicit manifest exactly matches all 56 core templates; `--api` output names `/users/api-tokens`; and repository CI either passes or has a precise, non-waived list of remaining failures that contains no failure from the four modified contracts.
  </acceptance_criteria>
  <done>The direct Phase 244 inventory/layout and PAT-guidance integration gaps are closed, with current repository CI truth preserved.</done>
</task>

</tasks>

<threat_model>
## ASVS posture

Level 1; blocking threshold high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Template directory → CI manifest | Files shipped to generated hosts cross into an exact repository contract that must detect additions and omissions. |
| Installer output → adopter action | Generated guidance directs an adopter to the PAT management trust boundary. |
| Repository CI output → completion claim | Mixed current and historical failures must remain attributable and cannot be silently waived. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-26 | Tampering | core-template inventory | medium | mitigate | Retain exact sorted manifest equality for all 56 entries, including the independent JWT template required by D-01/D-06. |
| T-244-27 | Elevation of Privilege | PAT post-install route guidance | medium | mitigate | Direct adopters to the browser/authenticated/sudo management path required by D-03 and lock it with focused output coverage. |
| T-244-28 | Repudiation | repository CI diagnostics | medium | mitigate | Record the real exit status and exact remaining failures; distinguish direct Phase 244 regressions from unrelated historical debt without waiver language. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Independently generate and safely use PAT and advanced JWT capabilities | 01-07 | COVERED | Plan 07 closes repository inventory and guidance integration after behavior proof. |
| REQ | PAT-01 | Complete independent API host | 01, 03, 07 | COVERED | Plan 07 makes the shipped-template contract and instructions match the generated host. |
| REQ | PAT-02 | Browser/CSRF/sudo owner-bound PAT management | 02, 03, 07 | COVERED | Plan 07 corrects adopter guidance to the browser management route. |
| REQ | JWT-01 | Independent strict advanced JWT host | 01, 04, 05, 07 | COVERED | Plan 07 registers `auth_jwt.ex` in the exact shipped-template manifest. |
| REQ | JWT-02 | Host-policy issuance and atomic refresh families | 01, 04-07 | COVERED | Host-policy template inventory and lifecycle proof remain covered. |
| RESEARCH | — | Independent generator artifact groups and truthful post-install instructions | 01, 03, 05, 07 | COVERED | Exact inventory and route output now follow the generated contracts. |
| RESEARCH | — | Owner-bound browser PAT management | 02, 03, 07 | COVERED | Guidance uses the browser/sudo route rather than bearer management. |
| RESEARCH | — | Strict JWT validation and locked refresh transaction | 04-06 | COVERED | No new closure work required; focused behavior remains proven. |
| CONTEXT | D-01 | Independent API/JWT contracts | 01, 03, 05, 07 | COVERED | Inventory includes the distinct JWT template without coupling PAT output. |
| CONTEXT | D-02 | Explicit FetchAPIToken pipeline | 01, 03 | COVERED | No gap in verification. |
| CONTEXT | D-03 | Browser/CSRF/auth/sudo PAT path | 03, 07 | COVERED | Generated instruction path matches the browser route. |
| CONTEXT | D-04 | Scope-derived owner | 02, 03 | COVERED | No gap in verification. |
| CONTEXT | D-05 | Server PAT scope allowlist | 02, 03 | COVERED | No gap in verification. |
| CONTEXT | D-06 | Host-policy JWT issuance | 01, 04, 05, 07 | COVERED | `auth_jwt.ex` is registered; password exchange remains absent. |
| CONTEXT | D-07 | Mandatory JWT claims/header contract | 04, 05 | COVERED | No gap in verification. |
| CONTEXT | D-08 | Configured signer and post-signature typ | 04 | COVERED | No gap in verification. |
| CONTEXT | D-09 | Scalar/array exact audience | 04, 05 | COVERED | No gap in verification. |
| CONTEXT | D-10 | One transaction audit-on/off | 06 | COVERED | No gap in verification. |
| CONTEXT | D-11 | Digest-only reuse family revoke | 06 | COVERED | No gap in verification. |

Deferred app-session, OAuth/OIDC authorization-server, native SDK, and generalized offline work remains excluded.

<verification>
Run `MIX_ENV=test mix test test/sigra/install/isolation_test.exs test/sigra/install/templates_layout_test.exs test/sigra/install/features/core_post_instructions_test.exs --trace`. Then run `source tmp/db.env &amp;&amp; MIX_ENV=test mix ci` once as a diagnostic. A nonzero repository result is not success: the SUMMARY must report the exit status and every remaining failing test/file, while proving the direct inventory/layout and route-guidance failures are absent.
</verification>

<success_criteria>
- Both exact template contracts accept the intentional 56-file inventory and still fail on any unregistered addition or omission.
- PAT post-install guidance and its focused regression test use `/users/api-tokens` per D-03.
- Focused tests pass with no implementation drift to independent PAT/JWT behavior.
- Repository-wide CI evidence is current and accurately separates direct Phase 244 integration status from unrelated historical failures without waiver.
</success_criteria>

<output>
Create `.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-07-SUMMARY.md` when done.
</output>
