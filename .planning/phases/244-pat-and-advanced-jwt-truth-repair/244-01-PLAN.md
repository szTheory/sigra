---
phase: 244-pat-and-advanced-jwt-truth-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/install/features/core.ex
  - priv/templates/sigra.install/core/auth_jwt.ex
  - test/sigra/install/features/core_test.exs
  - test/sigra/install/api_token_generator_test.exs
autonomous: true
requirements: [PAT-01, JWT-01, JWT-02]
must_haves:
  truths:
    - "Per D-01, `--api` and `--jwt` select disjoint PAT and advanced-JWT files, injections, configuration, routes, and instructions."
    - "Per D-02, a generated PAT pipeline names FetchAPIToken; generated output never recommends FetchBearer."
    - "Per D-06, JWT generation exposes a host-policy issuance delegate and emits neither a password exchange route nor request-selected scopes."
    - "Running either feature installation twice is deterministic and does not duplicate an injection, route, configuration block, or instruction."
  artifacts:
    - path: "lib/sigra/install/features/core.ex"
      provides: "Independent API and JWT generator predicates and emission branches"
    - path: "priv/templates/sigra.install/core/auth_jwt.ex"
      provides: "JWT-only host-policy issuance and refresh delegates"
    - path: "test/sigra/install/features/core_test.exs"
      provides: "Four-combination feature and repeat-install contracts"
    - path: "test/sigra/install/api_token_generator_test.exs"
      provides: "Negative route, configuration, and authority-surface contracts"
  key_links:
    - from: "lib/sigra/install/features/core.ex"
      to: "priv/templates/sigra.install/core/auth_jwt.ex"
      via: "JWT-only file group and injection"
      pattern: "auth_jwt"
    - from: "generated API router"
      to: "Sigra.Plug.FetchAPIToken"
      via: "explicit API authentication pipeline per D-02"
      pattern: "FetchAPIToken"
  prohibitions:
    - "No app-session storage, routes, verifier, or ceremony."
    - "No native login or direct password-to-token exchange."
    - "No OAuth/OIDC authorization-server behavior."
    - "No Lockspire or Crosswake source change."
    - "No request-selected PAT or JWT authority scopes."
    - "No fresh-host FetchBearer recommendation."
---

<objective>
Trace one install invocation through the generator and establish independent PAT/JWT emission contracts (PAT-01, JWT-01, JWT-02; D-01, D-02, D-06).

Purpose: Every later behavior depends on the installer selecting the right capability without silently emitting the other credential kind.
Output: Split generator predicates, a JWT-only host delegate template, and executable four-combination plus repeat-install source contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-CONTEXT.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-RESEARCH.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-PATTERNS.md
@.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-VALIDATION.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-01-SUMMARY.md
@.planning/phases/243-credential-boundary-and-pipeline-foundation/243-02-SUMMARY.md

<interfaces>
- `Sigra.Install.Features.Core.files/1` and `injections/1` consume installer binding options and return artifact/injection declarations.
- Phase 243 provides `Sigra.Plug.FetchAPIToken` and `Sigra.Plug.FetchJWT`; `FetchBearer` is deprecated compatibility dispatch.
- `Sigra.Auth.create_jwt_tokens(config, user, scopes)` is host-called; the generated delegate must choose scopes from host policy rather than request parameters.
</interfaces>
</context>

## Artifacts this phase produces

- `lib/sigra/install/features/core.ex` — independent API/JWT file, injection, config, route, and instruction selection.
- `priv/templates/sigra.install/core/auth_api_token.ex` and `api_token_controller.ex` — PAT-only owner-bound host delegates and browser management surface.
- `priv/templates/sigra.install/core/auth_jwt.ex` — JWT-only host-policy issuance/refresh delegate seam.
- `lib/sigra/api_token.ex` and `lib/sigra/auth.ex` — server-allowlisted PAT creation and owner-constrained revocation.
- `lib/sigra/config.ex`, `lib/sigra/jwt.ex`, `lib/sigra/jwt/validator.ex`, and `lib/sigra/jwt/refresh_token.ex` — strict JWT configuration, claims, verification, and locked refresh lifecycle.
- `test/sigra/install/features/core_test.exs`, `api_token_generator_test.exs`, and `test/sigra/planning/phase_244_generated_auth_runtime_proof_test.exs` — independent source and fresh-host proof.
- `test/sigra/api_token_test.exs`, `test/sigra/jwt_test.exs`, `test/sigra/jwt/signer_test.exs`, and `test/sigra/jwt_refresh_audit_cofate_test.exs` — PAT/JWT security and transaction contracts.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace independent feature selection through every generator branch (PAT-01, JWT-01, JWT-02)</name>
  <reversibility rating="costly">Generated host contracts are public adopter output; changing them later requires coordinated templates, fixtures, documentation, and upgrade guidance, but no persisted-data migration.</reversibility>
  <files>test/sigra/install/features/core_test.exs, test/sigra/install/api_token_generator_test.exs, lib/sigra/install/features/core.ex, priv/templates/sigra.install/core/auth_jwt.ex</files>
  <read_first>
Read each target file once. In `core.ex`, inventory every predicate and branch affecting files, injections, router pipelines/routes, config, and post-install instructions. In the tests, retain the established binding/render helpers and injection-marker conventions.
  </read_first>
  <behavior>
    - RED first: the option matrix proves no flags, API only, JWT only, and both flags produce exact disjoint unions per D-01.
    - API only includes the PAT schema/migration/config/Auth/controller, browser management routes, and FetchAPIToken; it contains no JWT config, delegate, or route.
    - JWT only includes JWT config and the host-policy delegate; it contains no PAT schema/migration/controller/delegate/config and no password/MFA exchange route per D-06.
    - Reapplying API only and JWT only preserves exactly one copy of each marker, injection, route, config block, and instruction.
  </behavior>
  <action>
Write the failing matrix and negative contracts first. Split `api?` and `jwt?` at their source and carry both independently through every generator branch, including helper predicates. Keep PAT artifacts under API selection only. Add `auth_jwt.ex` as the JWT-only host delegate and remove the generated token controller and password/MFA exchange routes from selected output. Emit explicit `FetchAPIToken` and `FetchJWT` pipelines for their respective credential kinds; never route primary output through the compatibility dispatcher. Make every injection marker stable so a second installer run is byte-stable and non-duplicating. Preserve unrelated installer flags and template groups.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/install/features/core_test.exs test/sigra/install/api_token_generator_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
The tests were red against the coupled contract and are green after the split; all four flag combinations, positive and negative outputs, and independent repeat installation are proven.
  </acceptance_criteria>
  <done>The production generator has a tested independent API/JWT spine, with no password exchange or compatibility-dispatch regression.</done>
</task>

</tasks>

<threat_model>
## ASVS posture

Level 1; blocking threshold high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Installer flags → generated host | Untrusted operator options select security-sensitive files and routes. |
| Generated router/config → runtime verifier | Emitted pipelines determine which verifier and policy receive bearer credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-244-01 | Elevation of Privilege | `Core` feature predicates | high | mitigate | Four-combination negative tests enforce D-01 and prevent one flag from silently enabling the other credential kind. |
| T-244-02 | Spoofing | generated API/JWT pipelines | high | mitigate | Emit only the explicit Phase 243 verifier for each selected credential kind per D-02. |
| T-244-03 | Tampering | generated JWT issuance surface | high | mitigate | Remove the password exchange/request-scope route and expose only a host-policy delegate per D-06. |
| T-244-04 | Denial of Service | repeat installer execution | medium | mitigate | Stable markers and repeat-install tests prove injections remain singular and deterministic. |
</threat_model>

<verification>
Run the focused command. Confirm the new tests demonstrate RED before production edits and GREEN after, with no package installation and no external integration.
</verification>

<success_criteria>
- D-01, D-02, and D-06 have executable generator contracts.
- PAT-01, JWT-01, and JWT-02 have an independent emission spine for later vertical expansion.
- Repeat installation is deterministic; generic generator concurrency does not define shared-state behavior and remains an explicitly irrelevant probe assumption.
</success_criteria>

<output>
Create `.planning/phases/244-pat-and-advanced-jwt-truth-repair/244-01-SUMMARY.md` when done.
</output>
