---
phase: 50
plan: 02
type: execute
wave: 2
depends_on:
  - "50-01"
files_modified:
  - .planning/phases/41-backup-codes-ga-product-closure/41-VALIDATION.md
  - .planning/phases/42-human-ga-matrix-evidence/42-VALIDATION.md
  - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md
  - .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md
  - MAINTAINING.md
  - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "**Nyquist policy table** for phases **41–44** lives in **`MAINTAINING.md`** (or a single linked **`50-NYQUIST-POLICY.md`** in the phase dir **and** **`MAINTAINING.md` links it** — pick one; table must have columns **Phase**, **Mode** (`Full Nyquist` | `Waiver + superseding evidence`), **Primary evidence path**, **Reopen trigger**)."
    - "Each **`41-VALIDATION.md` … `44-VALIDATION.md`** updates **## Nyquist deferral** / sign-off text so it no longer implies 'batch deferred to phase 50' **without** naming what phase **50** delivered (**`mix ci.install_golden`**, **`install_golden_contract`**, validation refresh) — per **D-50-01** / **D-50-04**."
    - "**`50-VERIFICATION.md`** exists with YAML frontmatter (`phase: \"50\"`, `verified` date, `status: passed` only after merge gate green), **Merge gate** citing **`mix ci.install_golden`** and **`PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test`**, plus **`git rev-parse HEAD`** in **Notes**."
  artifacts:
    - path: ".planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md"
      provides: "Dated receipts for phase 50 close"
nyquist_compliant: false
---

<objective>
Execute the **documentation + Nyquist honesty** slice: align **41–44** `*-VALIDATION.md` with the hybrid policy (**D-50-01**), publish the **policy table**, and freeze **`50-VERIFICATION.md`** receipts after **`50-01`** lands (**D-50-04**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-50-10 | Tampering | `nyquist_compliant: true` while deferral text still says batch open | high | Tasks grep for contradictory pair before flip; default keeps **`false`** unless explicit row-level closure text added |
| T-50-11 | Repudiation | `50-VERIFICATION.md` omits merge gate commands | medium | Task 5 template requires literal **`mix ci.install_golden`** block |
| T-50-12 | Elevation | ROADMAP criterion checked without evidence | low | Task 6 only adds ✅ when **`50-VERIFICATION.md` `status: passed`** |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Nyquist policy table</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-CONTEXT.md
    - .planning/phases/47-phase-43-verification-aud0405/47-CONTEXT.md
  </read_first>
  <action>
    Insert **`## Nyquist policy (phases 41–44)`** into **`MAINTAINING.md`** (after the **`50-01`** subsection if present, else after intro) containing a **Markdown table** with **four** rows (**41**–**44**) and columns:

    | Phase | Mode | Primary evidence | Reopen trigger |

    - **Mode** values must be exactly **`Full Nyquist`** or **`Waiver + superseding evidence`** (no other strings).
    - **Primary evidence** cells must reference concrete paths: **`.planning/phases/41-backup-codes-ga-product-closure/41-VERIFICATION.md`**, **`42-VERIFICATION.md`** (if exists; if missing, cite **`42-VALIDATION.md`** + ROADMAP), **`43-VERIFICATION.md`**, **`44-VERIFICATION.md`** — use paths that **exist** in repo at execution time.
    - **Reopen trigger** for installer-heavy rows: cite **`priv/templates/sigra.install/`** or **`lib/sigra/install/`** changes → re-run **`mix ci.install_golden`**.
  </action>
  <acceptance_criteria>
    - `grep -q "## Nyquist policy (phases 41–44)" MAINTAINING.md` exits 0.
    - `grep -q "41-backup-codes" MAINTAINING.md` exits 0.
    - `grep -q "44-mfa-account-api" MAINTAINING.md` exits 0.
    - `grep -q "Waiver + superseding evidence\\|Full Nyquist" MAINTAINING.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep -c "^|" MAINTAINING.md` is ≥ **8** (table has header + separator + 4 rows minimum).
  </verify>
</task>

<task type="execute">
  <name>Task 2: Refresh 41-VALIDATION.md Nyquist text</name>
  <files>.planning/phases/41-backup-codes-ga-product-closure/41-VALIDATION.md</files>
  <read_first>
    - .planning/phases/41-backup-codes-ga-product-closure/41-VALIDATION.md
    - .planning/phases/41-backup-codes-ga-product-closure/41-VERIFICATION.md
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-RESEARCH.md
  </read_first>
  <action>
    In **`## Validation Sign-Off`** / deferral areas: add a bullet that **`mix ci.install_golden`** (root **`mix.exs`**) is the cited contract for **`golden_diff_test.exs` / `idempotency_test.exs`** post phase **50**. Keep **`nyquist_compliant: false`** unless every per-row checkbox is honestly satisfied — if flipping to **`true`**, add a one-line **rationale** referencing **`41-VERIFICATION.md`** merge gate.
  </action>
  <acceptance_criteria>
    - `grep -q "ci.install_golden" .planning/phases/41-backup-codes-ga-product-closure/41-VALIDATION.md` exits 0.
    - `grep -q "nyquist_compliant" .planning/phases/41-backup-codes-ga-product-closure/41-VALIDATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    Frontmatter YAML still parses (leading `---` block intact).
  </verify>
</task>

<task type="execute">
  <name>Task 3: Refresh 42-VALIDATION.md Nyquist text</name>
  <files>.planning/phases/42-human-ga-matrix-evidence/42-VALIDATION.md</files>
  <read_first>
    - .planning/phases/42-human-ga-matrix-evidence/42-VALIDATION.md
    - .planning/v1.4-GA-UAT.md
  </read_first>
  <action>
    Extend **## Nyquist deferral** (or add if missing) with a **waiver-style** paragraph: owner **`maintainers`**, date **`2026-04-21`**, pointer to **`.planning/v1.4-GA-UAT.md`** for human GA rows, reopen trigger when **`v1.4-GA-UAT.md`** GA-02..GA-05 flip from Pending. Explicitly state **full automated Nyquist** is not claimed for phase **42** scaffolding.
  </action>
  <acceptance_criteria>
    - `grep -qi "v1.4-GA-UAT" .planning/phases/42-human-ga-matrix-evidence/42-VALIDATION.md` exits 0.
    - `grep -qi "GA-02\\|human" .planning/phases/42-human-ga-matrix-evidence/42-VALIDATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep -q "^nyquist_compliant: false" .planning/phases/42-human-ga-matrix-evidence/42-VALIDATION.md` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 4: Refresh 43- and 44-VALIDATION.md batch pointers</name>
  <files>.planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md,.planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md</files>
  <read_first>
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
  </read_first>
  <action>
    Replace generic **“deferred to phase 50”** sentences with **past-tense closure**: phase **50** updates batch policy (**link `MAINTAINING.md` section anchor**); scoped evidence remains **`43-VERIFICATION.md` / `44-VERIFICATION.md`**. Do **not** set **`nyquist_compliant: true`** unless **D-50-01** hybrid rules are met — default leave **`false`** with honest explanation referencing **`install_golden_contract`** for installer drift class.
  </action>
  <acceptance_criteria>
    - `grep -qi "MAINTAINING" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` exits 0.
    - `grep -qi "MAINTAINING" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md` exits 0.
    - `grep -q "43-VERIFICATION" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` exits 0.
    - `grep -q "44-VERIFICATION" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    Neither file removes the YAML closing `---` delimiter after frontmatter.
  </verify>
</task>

<task type="execute">
  <name>Task 5: Create 50-VERIFICATION.md</name>
  <files>.planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-CONTEXT.md
  </read_first>
  <action>
    Create **`.planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md`** modeled on **`43-VERIFICATION.md`**:

    - YAML keys: **`phase: "50"`**, **`status:`** `draft` until merge gate passes then `passed`, **`verified:`** ISO date.
    - **Must-haves** list: (1) **`mix ci.install_golden`** green, (2) policy table present in **`MAINTAINING.md`**, (3) **41–44** VALIDATION files reference phase **50** closure.
    - **Merge gate** code block:

      ```bash
      PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.install_golden
      ```

    - **Automated checks run** table with PASS + exit code + approximate timing.
    - **Notes**: output line from **`git rev-parse HEAD`**.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0.
    - `grep -q "mix ci.install_golden" .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0.
    - `grep -q "git rev-parse HEAD" .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep -q "^---$" .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0 (YAML fence present).
  </verify>
</task>

<task type="execute">
  <name>Task 6: ROADMAP phase 50 row (bookkeeping only)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
  </read_first>
  <action>
    When **`50-VERIFICATION.md`** records **`status: passed`**: add ✅ + date on ROADMAP row **50** (same style as rows **47–49**). If verification is still draft, **skip** this task and leave ROADMAP unchanged — document skip in SUMMARY.
  </action>
  <acceptance_criteria>
    - If edited: `grep -E "\\*\\*50\\*\\*.*✅" .planning/ROADMAP.md` exits 0.
    - If skipped: `grep -E "^\\|\\s*\\*\\*50\\*\\*" .planning/ROADMAP.md` unchanged from pre-task git diff.
  </acceptance_criteria>
  <verify>
    Markdown table pipes remain aligned (no broken row).
  </verify>
</task>

</tasks>

<verification>
- Grep bundle: `grep -q "ci.install_golden" MAINTAINING.md .planning/phases/41-backup-codes-ga-product-closure/41-VALIDATION.md` — all exit 0.
- **`50-VERIFICATION.md`** merge gate command matches **`mix.exs`** alias byte-for-byte on the test path list.
</verification>

<success_criteria>
- **D-50-01..04** reflected in **VALIDATION** prose + **`50-VERIFICATION.md`**.
- No contradictory **`nyquist_compliant: true`** + “deferred to phase 50” phrasing in the same file without waiver paragraph.
</success_criteria>
