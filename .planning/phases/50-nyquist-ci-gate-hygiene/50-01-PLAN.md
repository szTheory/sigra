---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - MAINTAINING.md
  - docs/uat-ci-coverage.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "`mix ci.install_golden` is a flat string-key alias in `mix.exs` whose body is a **single** `mix test …` invocation listing **`test/sigra/install/golden_diff_test.exs`** and **`test/sigra/install/idempotency_test.exs`** (installer subprocess harness — `test/fixtures/install_golden/tree/` has **no** `mix.exs`; see **`50-RESEARCH.md` §4**)."
    - "`.github/workflows/ci.yml` gains a **`install_golden_contract`** (or similarly named) job that runs the same tests with Postgres + `phx_new` archive, uses path filters aligned with **`installer_milestone_audit`**, and **always** runs on **`push` to `main`**."
    - "`MAINTAINING.md` and **`docs/uat-ci-coverage.md`** link the alias/job and record **expected wall-clock** class for golden tests (≥ **`300_000`** ms module timeout)."
  artifacts:
    - path: "mix.exs"
      provides: "Named `ci.install_golden` merge-gate command"
    - path: ".github/workflows/ci.yml"
      provides: "CI visibility for install-golden contract beyond default path-filter skips"
nyquist_compliant: false
---

<objective>
Ship the **named Mix alias** and **CI job** for the installer golden / subprocess contract (**D-50-03**), and add **maintainer-facing docs** (**D-50-02**, **D-50-04**) so expensive suites are not “green locally only.”
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-50-01 | Tampering | Job claims green without running golden tests | high | Workflow step must invoke **`mix test`** with the two explicit paths; **`50-VERIFICATION.md`** (plan 02) records output |
| T-50-02 | Elevation | Alias expands to zero test files | medium | Acceptance greps list both **`golden_diff_test.exs`** and **`idempotency_test.exs`** substrings inside **`mix.exs`** |
| T-50-03 | Denial of service | Duplicate `phx_new` installs across jobs | low | Reuse same **`Install phx_new archive`** step pattern as **`library_tests`**. **D-50-03** fixture-tree cache keys apply only if CI adds a nested **`mix`** step under **`test/fixtures/install_golden/`**; per **50-RESEARCH §4** this job runs **root** `mix test` on explicit paths — document root **`mix.lock`** / BEAM pinning only (no false claim of fixture-scoped caches). |
| T-50-04 | Information disclosure | Postgres credentials in logs | low | Use same **`PGUSER`/`PGPASSWORD`** pattern as existing jobs — no new secrets |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add mix ci.install_golden alias</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-CONTEXT.md
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-RESEARCH.md
  </read_first>
  <action>
    In **`defp aliases/0`**, append a sibling entry next to **`"ci.audit_45"`**:

    ```elixir
    "ci.install_golden": [
      "test test/sigra/install/golden_diff_test.exs test/sigra/install/idempotency_test.exs"
    ]
    ```

    Keep **`"ci.audit_45"`** unchanged. Do **not** nest under **`ci:`** — match the existing **flat string-key** style.
  </action>
  <acceptance_criteria>
    - `grep -q "ci.install_golden" mix.exs` exits 0.
    - `grep -q "golden_diff_test.exs" mix.exs` exits 0.
    - `grep -q "idempotency_test.exs" mix.exs` exits 0.
    - `mix format --check-formatted mix.exs` exits 0.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=test mix compile` exits 0 from repo root.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Add CI job install_golden_contract</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - mix.exs
  </read_first>
  <action>
    Add a new job **`install_golden_contract`** after **`installer_milestone_audit`** (or adjacent to **`library_tests`**):

    - **`runs-on: ubuntu-latest`**
    - **Postgres service** — copy the **`postgres:15`** block from **`library_tests`** (ports **`5432:5432`**, **`POSTGRES_PASSWORD: postgres`**, healthcheck).
    - Steps: **`actions/checkout`** (pinned SHA consistent with siblings), **`erlef/setup-beam`** with **`version-file: .tool-versions`**, **`version-type: strict`**, **`mix local.hex`**, **`mix local.rebar`**, **`mix archive.install --force hex phx_new`**, **`mix deps.get`**, then run:

      `env PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs test/sigra/install/idempotency_test.exs`

    - **PR path filter:** replicate the **`git diff`** pattern from **`installer_milestone_audit`** (same `grep -qE '^priv/templates/sigra\.install/|^lib/sigra/install/'` rule) so PRs only run when installer paths change; for **`github.event_name != pull_request`**, set **`run=true`** so **`main` pushes always run** this job.
    - Optional: add **`schedule:`** cron weekly on **`main`** (document in commit message if added).

    Name the job distinctly from **`library_tests`** so merge-gate docs can cite **`install_golden_contract`**.
  </action>
  <acceptance_criteria>
    - `grep -q "install_golden_contract" .github/workflows/ci.yml` exits 0.
    - `grep -q "golden_diff_test.exs" .github/workflows/ci.yml` exits 0.
    - `grep -q "idempotency_test.exs" .github/workflows/ci.yml` exits 0.
    - `python3 -c "import yaml; j=yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']['install_golden_contract']; assert j['services']['postgres']['image']=='postgres:15'"` exits 0 (exact Postgres image on **`install_golden_contract`** only).
  </acceptance_criteria>
  <verify>
    `python3 -c "import yaml; assert 'install_golden_contract' in yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']"` exits 0 (job key present).
  </verify>
</task>

<task type="execute">
  <name>Task 3: Maintainer + UAT doc links</name>
  <files>MAINTAINING.md,docs/uat-ci-coverage.md</files>
  <read_first>
    - MAINTAINING.md
    - docs/uat-ci-coverage.md
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-CONTEXT.md
  </read_first>
  <action>
    1. **`MAINTAINING.md`** — Add subsection **## Installer golden CI contract (phase 50)** after the opening maintainer intro (before **GitHub Actions repository settings** is fine) containing:
       - Verbatim shell one-liner: **`PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.install_golden`**
       - Sentence that **`golden_diff_test.exs`** uses **`@moduletag timeout: 300_000`** (cite the file path).
       - Link to **`.github/workflows/ci.yml`** job id **`install_golden_contract`** by name.
    2. **`docs/uat-ci-coverage.md`** — Add one bullet under the CI overview listing **`mix ci.install_golden`** + the **`install_golden_contract`** job as the nested golden/idempotency contract (do not duplicate full YAML — link **`ci.yml`**).
  </action>
  <acceptance_criteria>
    - `grep -q "mix ci.install_golden" MAINTAINING.md` exits 0.
    - `grep -q "300_000" MAINTAINING.md` exits 0.
    - `grep -q "install_golden_contract" MAINTAINING.md` exits 0.
    - `grep -q "ci.install_golden" docs/uat-ci-coverage.md` exits 0.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.install_golden` exits 0 when Postgres is up (executor environment); if CI-only, note **SKIP** with reason in **`50-02` summary** — prefer local run when available.
  </verify>
</task>

</tasks>

<verification>
- `mix ci.install_golden` exits 0 with Postgres (same env as **`library_tests`**).
- New workflow job key **`install_golden_contract`** parses under **`jobs:`**.
</verification>

<success_criteria>
- Alias + CI + docs reference the **same** two test paths.
- **`50-RESEARCH.md`** constraint (no fictional **`cd tree`** Mix project) respected.
</success_criteria>
