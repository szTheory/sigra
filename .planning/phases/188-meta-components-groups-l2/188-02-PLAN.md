---
phase: 188-meta-components-groups-l2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - test/example/priv/static/assets/css/app.css
autonomous: true
requirements:
  - GROUP-01
  - GROUP-03
  - GROUP-04
user_setup: []
must_haves:
  truths:
    - "Required MG-1..MG-11 group CSS ships from canonical sigra_admin.css, not example-only app.css."
    - "Canonical, example, and install-golden sigra_admin.css files are byte-identical after migration."
    - "Migrated L2 selectors depend on --sg-* tokens and do not change Phase 186 token values."
  artifacts:
    - path: "priv/templates/sigra.install/admin/sigra_admin.css"
      provides: "Canonical shipped L2 group CSS"
      contains: ".sg-confirm-overlay"
    - path: "test/example/priv/static/assets/css/app.css"
      provides: "Example-only host CSS after required L2 sg-* duplicate removal"
      contains: "--vt-"
    - path: "test/example/priv/static/assets/sigra_admin.css"
      provides: "Example mirror of canonical admin CSS"
      min_lines: 1
    - path: "test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css"
      provides: "Install-golden mirror of canonical admin CSS"
      min_lines: 1
  key_links:
    - from: "priv/templates/sigra.install/admin/sigra_admin.css"
      to: "test/example/priv/static/assets/sigra_admin.css"
      via: "byte copy"
      pattern: "cmp -s"
    - from: "priv/templates/sigra.install/admin/sigra_admin.css"
      to: "test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css"
      via: "byte copy"
      pattern: "cmp -s"
---

<objective>
Migrate the L2 group CSS required by MG-1..MG-11 into the shipped admin stylesheet and keep all parity surfaces byte-coherent.

Purpose: Per D-03, D-04, and D-05, generated hosts must receive the same group styling that `/admin/_design` and the example app use for L2 evidence.
Output: Canonical L2 CSS in `sigra_admin.css`, duplicate required group selectors removed from example-only `app.css`, and byte-identical mirrors.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
@.planning/phases/188-meta-components-groups-l2/188-RESEARCH.md
@.planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
@.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
@.planning/phases/187-individual-components-l1/187-VALIDATION.md
@AGENTS.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md

<interfaces>
Known example-only L2 selectors from current app.css:
- .sg-detail-grid, .sg-form-grid, .sg-form-grid--cols, .sg-action-row
- .sg-confirm-overlay, .sg-confirm-dialog, .sg-confirm-dialog__actions
- .sg-search-row, .sg-filter-chip and its checked/input/hover variants
- .sg-truncate, .sg-tabular, .sg-list, .sg-kv, .sg-kv__term, .sg-kv__value
- .sg-table, .sg-table-panel table/cell/row/tone rules
- .sg-summary-facts, .sg-summary-facts__num, .sg-danger-panel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move required L2 group selectors into canonical sigra_admin.css</name>
  <read_first>
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
    - .planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
    - .planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
    - guides/reference/admin-design-contract.md
  </read_first>
  <files>priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/css/app.css</files>
  <action>Per D-03 through D-05, move required L2 group/layout selectors from `test/example/priv/static/assets/css/app.css` into the existing `@layer sg-components` block in `priv/templates/sigra.install/admin/sigra_admin.css`. Migrate these selector families: `.sg-detail-grid`, `.sg-form-grid`, `.sg-form-grid--cols`, `.sg-action-row`, `.sg-confirm-overlay`, `.sg-confirm-dialog`, `.sg-confirm-dialog__actions`, `.sg-search-row`, `.sg-filter-chip`, `.sg-filter-chip input[type="checkbox"]`, `.sg-filter-chip input[type="radio"]`, `.sg-filter-chip:has(input:checked)`, `.sg-truncate`, `.sg-tabular`, `.sg-list`, `.sg-kv`, `.sg-kv__term`, `.sg-kv__value`, `.sg-table`, `.sg-table-panel`, table/cell/row/tone rules below `.sg-table-panel` and `.sg-table`, `.sg-summary-facts`, `.sg-summary-facts__num`, and `.sg-danger-panel`. Remove the migrated duplicate selector blocks from `app.css`, including responsive media-query copies, pseudo-class variants, descendant selectors, and modifier/element selectors under those families. Keep `app.css` host/demo material such as `--vt-*` and `.vt-*`. Use only `var(--sg-*)`, `color-mix(...)`, existing media queries, and established CSS primitives. Do not introduce raw colors when an existing `--sg-*` token exists, do not add Tailwind/DaisyUI classes, and do not change any `--sg-*` token value.</action>
  <verify>
    <automated>rg -n "^\\s*\\.(sg-detail-grid|sg-form-grid|sg-action-row|sg-confirm-overlay|sg-confirm-dialog|sg-search-row|sg-filter-chip|sg-truncate|sg-tabular|sg-list|sg-kv|sg-table|sg-table-panel|sg-summary-facts|sg-danger-panel)(\\b|__|--|:|\\s|,|\\.|\\[|>|\\+|~)" priv/templates/sigra.install/admin/sigra_admin.css</automated>
    <automated>! rg -n "^\\s*\\.(sg-detail-grid|sg-form-grid|sg-action-row|sg-confirm-overlay|sg-confirm-dialog|sg-search-row|sg-filter-chip|sg-truncate|sg-tabular|sg-list|sg-kv|sg-table|sg-table-panel|sg-summary-facts|sg-danger-panel)(\\b|__|--|:|\\s|,|\\.|\\[|>|\\+|~)" test/example/priv/static/assets/css/app.css</automated>
    <automated>rg -n "transition:\\s*all" priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/css/app.css && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: canonical `sigra_admin.css` contains every selector family named in the action.
    - Source assertion: example-only `app.css` no longer defines any required L2 `sg-*` group selector family named in the action, including indented rules inside media queries, pseudo-class variants, descendant selectors, and modifier/element selectors.
    - Source assertion: no migrated selector uses `transition: all`.
    - Source assertion: no `--sg-*` token value changes outside the migrated selector blocks.
  </acceptance_criteria>
  <done>L2 group styling needed for generated hosts is canonical, token-driven, and no longer masked by example-only duplicate selectors.</done>
</task>

<task type="auto">
  <name>Task 2: Sync CSS mirrors and prove byte parity</name>
  <read_first>
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    - test/sigra/install/features/admin_test.exs
    - test/sigra/install/golden_diff_test.exs
    - .planning/phases/184-distribution-parity/184-02-SUMMARY.md
    - .planning/phases/187-individual-components-l1/187-02-SUMMARY.md
  </read_first>
  <files>test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</files>
  <action>Copy `priv/templates/sigra.install/admin/sigra_admin.css` byte-for-byte to `test/example/priv/static/assets/sigra_admin.css` and `test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css`. Preserve the Phase 184 DIST-05 parity contract and the Phase 187 shipped-CSS mirror pattern. Do not edit the mirrors by hand after copying.</action>
  <verify>
    <automated>cmp -s priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css</automated>
    <automated>cmp -s priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</automated>
    <automated>mix test test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs</automated>
    <automated>rg -n "var\\(--vt-|\\.vt-|VAULTR" priv/templates/sigra.install/admin/sigra_admin.css && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: both `cmp -s` commands exit 0.
    - Behavior assertion: install feature and golden diff tests exit 0.
    - Source assertion: canonical `sigra_admin.css` contains no `var(--vt-`, `.vt-`, or `VAULTR` text.
  </acceptance_criteria>
  <done>All shipped CSS parity surfaces are byte-identical and generated-host golden tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example CSS -> generated host CSS | Required L2 styling crosses from local evidence to installer-distributed asset. |
| canonical CSS -> test mirrors | Byte copies become merge-blocking evidence for generated-host styling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-03 | Repudiation | test/example/priv/static/assets/css/app.css | mitigate | Remove required L2 selectors from example-only app.css so gallery evidence cannot pass on unshipped styling. |
| T-188-04 | Tampering | sigra_admin.css mirrors | mitigate | Use byte parity with `cmp -s` and existing ExUnit golden tests after every canonical CSS edit. |
| T-188-05 | Denial of Service | responsive table/card CSS | mitigate | Preserve scroll containment, wrapping, and truncation selectors for MG-5/MG-6 so narrow viewports do not create unusable layouts. |
| T-188-SC | Tampering | package installs | mitigate | No new packages; stop if an executor attempts package-manager installation. |
</threat_model>

<verification>
The plan is complete only after both CSS mirrors match canonical bytes, `mix test test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs` passes, and app.css no longer owns the migrated L2 selector families.
</verification>

<success_criteria>
- D-03, D-04, and D-05 are implemented.
- Required L2 group CSS lives in canonical `sigra_admin.css`.
- Example and install-golden CSS mirrors are byte-identical to the canonical template.
- No token values, schema files, packages, Tailwind, or DaisyUI modal styles are introduced.
</success_criteria>

<output>
Create `.planning/phases/188-meta-components-groups-l2/188-02-SUMMARY.md` when done.
</output>
