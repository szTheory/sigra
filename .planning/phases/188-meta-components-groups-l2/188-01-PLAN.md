---
phase: 188-meta-components-groups-l2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/install/features/admin_test.exs
  - test/example/priv/playwright/tests/admin-theme.spec.ts
  - guides/reference/admin-token-reference.md
autonomous: true
requirements:
  - GROUP-01
  - GROUP-03
  - GROUP-04
user_setup: []
must_haves:
  truths:
    - "D-11 dark-token parity evidence is structural and survives CSS line movement."
    - "The admin-theme Playwright helper for notice contrast is defined once and reused."
    - "The token reference completeness guard proves documented --sg-* tokens match canonical sigra_admin.css."
  artifacts:
    - path: "test/sigra/install/features/admin_test.exs"
      provides: "Structural dark-block and token-reference completeness assertions"
      contains: "D-11 System↔explicit-toggle dark-block parity"
    - path: "test/example/priv/playwright/tests/admin-theme.spec.ts"
      provides: "Single shared readNoticeStyles helper for contrast assertions"
      contains: "readNoticeStyles"
    - path: "guides/reference/admin-token-reference.md"
      provides: "Documented --sg-* token rows consumed by completeness guard"
      contains: "`--sg-color-brand`"
  key_links:
    - from: "test/sigra/install/features/admin_test.exs"
      to: "priv/templates/sigra.install/admin/sigra_admin.css"
      via: "File.read! structural token extraction"
      pattern: "File\\.read!\\(\"priv/templates/sigra.install/admin/sigra_admin\\.css\"\\)"
    - from: "test/example/priv/playwright/tests/admin-theme.spec.ts"
      to: "/admin/_design"
      via: "computed-style contrast checks"
      pattern: "readNoticeStyles"
---

<objective>
Harden the folded Phase 186 D-11 validation work that the user explicitly moved into Phase 188.

Purpose: Per D-15 and D-16, make token/theme evidence robust without changing token values or expanding this phase into a new L0 audit.
Output: Structural ExUnit extractors, a single Playwright notice-style helper, and a token-reference completeness guard.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
@.planning/phases/188-meta-components-groups-l2/188-RESEARCH.md
@.planning/phases/188-meta-components-groups-l2/188-VALIDATION.md
@.planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
@.planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
@AGENTS.md
@test/example/AGENTS.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md

<interfaces>
From test/sigra/install/features/admin_test.exs:
- Existing describe: "DIST-05 example≡template byte-parity (sigra_admin.css)"
- Existing describe: "D-11 System↔explicit-toggle dark-block parity"
- Existing helper names: extract_dark_media_props/1, extract_explicit_dark_props/1, extract_token_value/2, source_fragment/3
- Current brittle comments reference fixed line windows; remove the line-window dependency.

From test/example/priv/playwright/tests/admin-theme.spec.ts:
- Existing duplicated local helper text: const readNoticeStyles = async () => ...
- Existing contrast tests poll readNoticeStyles().color and readNoticeStyles().background.
- Existing imports include Page and TestInfo; add Locator to the type import if the shared helper accepts a Locator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace D-11 line-window extractors with structural CSS extraction</name>
  <read_first>
    - test/sigra/install/features/admin_test.exs
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
    - priv/templates/sigra.install/core/sigra_auth.css
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
    - .planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
  </read_first>
  <files>test/sigra/install/features/admin_test.exs</files>
  <action>Per D-15 and D-16, replace the fixed-line `Enum.slice(...)` logic inside `extract_dark_media_props/1` and `extract_explicit_dark_props/1` with structural extraction. The admin extractor must find the `@media (prefers-color-scheme: dark)` block in `priv/templates/sigra.install/admin/sigra_admin.css`, then collect only complete `--sg-*` declarations. The example extractor must find the explicit dark theme selector block in `test/example/priv/static/assets/css/app.css` that contains `data-sg-admin-theme="dark"`, then collect only complete `--sg-*` declarations. Sort both declaration lists before comparing. Keep `extract_token_value/2` for light-token lookups. Remove comments that claim the dark block lives on specific line numbers. Do not change token values in any CSS file.</action>
  <verify>
    <automated>mix test test/sigra/install/features/admin_test.exs</automated>
    <automated>rg -n "Enum\\.slice|zero-indexed|lines [0-9]+-[0-9]+" test/sigra/install/features/admin_test.exs && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/sigra/install/features/admin_test.exs` contains no `Enum.slice` inside the D-11 dark-block helpers.
    - Source assertion: the D-11 parity describe still asserts `--sg-color-brand-strong: #fdba74;`.
    - Behavior assertion: `mix test test/sigra/install/features/admin_test.exs` exits 0.
  </acceptance_criteria>
  <done>D-11 parity tests no longer depend on CSS line numbers and still prove System dark tokens equal explicit-toggle dark tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Hoist duplicated Playwright notice contrast helper</name>
  <read_first>
    - test/example/priv/playwright/tests/admin-theme.spec.ts
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
    - .planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
    - test/example/AGENTS.md
  </read_first>
  <files>test/example/priv/playwright/tests/admin-theme.spec.ts</files>
  <action>Per D-15, hoist the two duplicated local `const readNoticeStyles = async () => ...` helpers into one shared top-level helper named `readNoticeStyles(notice: Locator)`. It must read the `.sg-text-sm` child color and the `.sg-notice` background color exactly as the current local helpers do. Update both contrast polling blocks to call the shared helper. If the file imports Playwright types as `type Page, type TestInfo`, add `type Locator` to that import. Preserve role selectors, LiveView readiness gates, and existing theme-switch behavior. Do not add sleeps. Before the Playwright verify command, ensure `SIGRA_EXAMPLE_URL=http://localhost:4011` points at the `test/example` app; if not, start `cd test/example && PORT=4011 mix phx.server` in a managed background session and stop it after verification.</action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-theme.spec.ts --project=chromium --grep "tone notice"</automated>
    <automated>test "$(rg -n "function readNoticeStyles" test/example/priv/playwright/tests/admin-theme.spec.ts | wc -l | tr -d ' ')" = "1"</automated>
    <automated>test "$(rg -n "const readNoticeStyles" test/example/priv/playwright/tests/admin-theme.spec.ts | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: exactly one top-level `function readNoticeStyles` exists.
    - Source assertion: no `const readNoticeStyles` local helper remains.
    - Behavior assertion: the `tone notice` Playwright test passes against the running example app.
  </acceptance_criteria>
  <done>Notice contrast checks share one deterministic helper and remain Playwright-green.</done>
</task>

<task type="auto">
  <name>Task 3: Add lightweight admin-token-reference completeness guard</name>
  <read_first>
    - test/sigra/install/features/admin_test.exs
    - guides/reference/admin-token-reference.md
    - priv/templates/sigra.install/admin/sigra_admin.css
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
  </read_first>
  <files>test/sigra/install/features/admin_test.exs, guides/reference/admin-token-reference.md</files>
  <action>Per D-15 and D-16, add a focused ExUnit guard to `test/sigra/install/features/admin_test.exs` that extracts unique `--sg-*` token names from the canonical `:root` declaration blocks in `priv/templates/sigra.install/admin/sigra_admin.css` and asserts that each appears as a backticked token name in `guides/reference/admin-token-reference.md`. If the guard finds a missing token, add the missing row to the existing token-reference table with the current canonical value and a concise rationale. This is a completeness guard only: do not change `sigra_admin.css`, do not add token categories, and do not alter Phase 186 token values.</action>
  <verify>
    <automated>mix test test/sigra/install/features/admin_test.exs</automated>
    <automated>rg -n "admin-token-reference|token reference" test/sigra/install/features/admin_test.exs</automated>
    <automated>git diff -- priv/templates/sigra.install/admin/sigra_admin.css --exit-code</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `admin_test.exs` contains an automated guard that reads both `sigra_admin.css` and `admin-token-reference.md`.
    - CLI assertion: `git diff -- priv/templates/sigra.install/admin/sigra_admin.css --exit-code` exits 0 after this task.
    - Behavior assertion: `mix test test/sigra/install/features/admin_test.exs` exits 0.
  </acceptance_criteria>
  <done>The folded token-reference cleanup is enforced by tests without any token-value mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/tests -> UI evidence | Documentation and test helpers create claims that later plans rely on for L2 evidence. |
| canonical CSS -> generated-host proof | The test harness reads canonical CSS and can either prove or misrepresent parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-01 | Repudiation | test/sigra/install/features/admin_test.exs | mitigate | Replace line-window extractors with structural block extraction so D-11 evidence can be reproduced after CSS migration. |
| T-188-02 | Information Disclosure | admin-token-reference.md | accept | The guard reads public design tokens only; no secrets or user data are present. |
| T-188-SC | Tampering | package installs | mitigate | No package manager install is in scope; if an executor attempts npm/pip/cargo install, stop and re-plan with Package Legitimacy Gate. |
</threat_model>

<verification>
Run the task-level automated commands. Before closing the plan, run `mix test test/sigra/install/features/admin_test.exs` and the focused `tone notice` Playwright command if the example app is available at `SIGRA_EXAMPLE_URL`.
</verification>

<success_criteria>
- D-15 and D-16 are implemented exactly: harness robustness only, no token-value changes, no L0 audit expansion.
- The D-11 parity test no longer depends on CSS line numbers.
- `readNoticeStyles` exists once and is reused.
- The token reference completeness guard is green.
</success_criteria>

<output>
Create `.planning/phases/188-meta-components-groups-l2/188-01-SUMMARY.md` when done.
</output>
