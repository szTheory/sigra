---
phase: 188-meta-components-groups-l2
plan: 03
type: execute
wave: 2
depends_on:
  - 188-02
files_modified:
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - GROUP-03
  - GROUP-04
user_setup: []
must_haves:
  truths:
    - "/admin/_design renders board-mg-1 through board-mg-11 with stable ids."
    - "Each group board exposes populated, zero, loading, and error evidence or an explicit not-applicable note."
    - "Group boards compose ratified L1 components and avoid scored .sg-card .sg-card nesting."
    - "The gallery contains deterministic equivalent-data examples for reused groups."
  artifacts:
    - path: "test/example/lib/example_web/live/admin/design_gallery_live.ex"
      provides: "MG-1..MG-11 L2 state catalog"
      contains: "id=\"board-mg-11\""
  key_links:
    - from: "test/example/lib/example_web/live/admin/design_gallery_live.ex"
      to: "Sigra.Admin.Components"
      via: "import Sigra.Admin.Components"
      pattern: "summary_chip|applied_chip|task_card|notice|empty_state|skeleton|audit_row"
---

<objective>
Expand the example-only design gallery from the stale MG-1..MG-5 starter catalog to the approved MG-1..MG-11 Phase 188 group catalog.

Purpose: Per D-01, D-02, D-06, D-10, D-11, and D-12, the gallery must be a deterministic L2 evidence surface for every group and state before Playwright can enforce the scorecard.
Output: `design_gallery_live.ex` renders stable MG-1..MG-11 boards with state matrices and equivalent-data reuse examples.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
@.planning/phases/188-meta-components-groups-l2/188-RESEARCH.md
@.planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
@.planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
@AGENTS.md
@test/example/AGENTS.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md

<interfaces>
From test/example/lib/example_web/live/admin/design_gallery_live.ex:
- Module: ExampleWeb.Admin.DesignGalleryLive
- Uses: `use ExampleWeb, :live_view`
- Imports: `Sigra.Admin.Components`
- Existing board convention: id="board-{name}" for L1, id="board-mg-{n}" for L2.
- Gallery data must remain static literal assigns; do not add DB queries or Query module imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace MG-1..MG-5 starter group section with MG-1..MG-4 state boards</name>
  <read_first>
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - .planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
    - .planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
    - lib/sigra/admin/components.ex
    - guides/reference/admin-design-contract.md
  </read_first>
  <files>test/example/lib/example_web/live/admin/design_gallery_live.ex</files>
  <action>Per D-01, D-02, D-06, D-10, and D-11, rewrite the Component Groups section so MG-1 through MG-4 use the approved names and stable board ids: `board-mg-1` Metric / Summary Strip, `board-mg-2` Filter Panel + Applied-chip Row, `board-mg-3` Task-card Grid, and `board-mg-4` Alarm Notice Band. Each board must include deterministic child markers `data-testid="mg-N-populated"`, `data-testid="mg-N-zero"`, `data-testid="mg-N-loading"`, and `data-testid="mg-N-error"` when the state is reachable. For MG-3 zero/loading, render explicit note markers `data-testid="mg-3-zero-note"` and `data-testid="mg-3-loading-note"` with text that zero/loading are not applicable because overview tasks are static capability launchers. Use `summary_chip` for MG-1 metrics, `applied_chip` for MG-2 removable filter state, `task_card` for MG-3 action prompts, `notice` and `notice_link` for MG-4 contextual alerts, `empty_state` for zero data, and `skeleton` for loading. Board wrappers for groups containing `.sg-card` children, especially MG-3, must not themselves include `sg-card`; use an unframed `section` or `div` with `sg-stack sg-stack--4` and stable id.</action>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors</automated>
    <automated>test "$(rg -n "id=\"board-mg-[1-4]\"" test/example/lib/example_web/live/admin/design_gallery_live.ex | wc -l | tr -d ' ')" = "4"</automated>
    <automated>rg -n "mg-[1-4]-(populated|zero|loading|error)|mg-3-zero-note|mg-3-loading-note" test/example/lib/example_web/live/admin/design_gallery_live.ex</automated>
    <automated>rg -n "id=\"board-mg-3\"[^\\n]*sg-card" test/example/lib/example_web/live/admin/design_gallery_live.ex && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: exactly four MG-1..MG-4 board ids exist.
    - Source assertion: MG-1 uses `summary_chip`; MG-2 uses `applied_chip`; MG-3 uses `task_card`; MG-4 uses `notice`.
    - Source assertion: MG-3 board wrapper is not a `.sg-card`.
    - Behavior assertion: `cd test/example && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>MG-1..MG-4 render the approved catalog names, state evidence, and right L1 components.</done>
</task>

<task type="auto">
  <name>Task 2: Add MG-5..MG-11 list, detail, roster, and confirmation state boards</name>
  <read_first>
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - .planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
    - .planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
    - lib/sigra/admin/components.ex
    - lib/sigra/admin/live/users_index_live.ex
    - lib/sigra/admin/live/audit_index_live.ex
    - lib/sigra/admin/live/audit_user_live.ex
    - lib/sigra/admin/live/organization_live.ex
    - lib/sigra/admin/live/user_show_live.ex
    - lib/sigra/admin/live/branding_live.ex
  </read_first>
  <files>test/example/lib/example_web/live/admin/design_gallery_live.ex</files>
  <action>Per D-01, D-02, D-06, D-08, D-10, D-11, D-12, D-13, and D-14, add stable boards `board-mg-5` through `board-mg-11` using approved names: MG-5 User Results + Pagination, MG-6 Audit Feed + Pagination, MG-7 Organization Member Roster, MG-8 Pending Invitations, MG-9 Identity Header + Summary Facts, MG-10 Detail Facts + Membership Panels, and MG-11 Destructive Action + Confirmation. For every board, render `data-testid="mg-N-populated"`, `data-testid="mg-N-zero"`, `data-testid="mg-N-loading"`, and `data-testid="mg-N-error"` where reachable; use note markers with the exact `mg-N-STATE-note` pattern only when the UI-SPEC marks a state as not applicable. MG-5 must include desktop `data-testid="mg-5-desktop-results"` and mobile `data-testid="mg-5-mobile-results"` examples containing the same user name/email/id, status, secondary facts, and `Open user` action. MG-6 must include desktop `data-testid="mg-6-desktop-results"` and mobile `data-testid="mg-6-mobile-results"` examples containing the same event id, action label/code, outcome, actor/effective user, and pagination/export affordance. MG-7 and MG-8 must use `sg-list` and `sg-list-row` plus `skeleton` loading rows. MG-9 must use `sg-page-header`, status pills, and `sg-summary-facts`. MG-10 must use `sg-detail-grid`, `sg-detail-panel`, `sg-kv`, `empty_state`, and `audit_row`. MG-11 must use `sg-danger-panel`, `sg-confirm-overlay`, and `sg-confirm-dialog`; do not use `dialog.modal` or `.modal-box`. Add equivalent-data reuse examples with stable pairs `data-testid="mg-2-coherence-a/b"`, `mg-6-coherence-a/b`, and `mg-11-coherence-a/b` where the two rendered snippets use the same component structure for the same data.</action>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors</automated>
    <automated>test "$(rg -n "id=\"board-mg-[0-9]+\"" test/example/lib/example_web/live/admin/design_gallery_live.ex | wc -l | tr -d ' ')" = "11"</automated>
    <automated>rg -n "mg-(5|6)-(desktop|mobile)-results|mg-(2|6|11)-coherence-a|mg-(2|6|11)-coherence-b|sg-confirm-dialog" test/example/lib/example_web/live/admin/design_gallery_live.ex</automated>
    <automated>rg -n "dialog\\.modal|class=\"modal|modal-box" test/example/lib/example_web/live/admin/design_gallery_live.ex && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: exactly 11 `board-mg-N` ids exist.
    - Source assertion: MG-5 and MG-6 include both desktop and mobile result test ids.
    - Source assertion: MG-11 uses `sg-confirm-overlay` and `sg-confirm-dialog` and contains no `.modal` or `.modal-box`.
    - Source assertion: equivalent-data coherence pairs exist for MG-2, MG-6, and MG-11.
    - Behavior assertion: `cd test/example && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>MG-5..MG-11 render the approved catalog, state evidence, table/card examples, confirmation pattern, and reuse evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic gallery -> quality ledger | Static board content becomes evidence for L2 quality claims. |
| L1 components -> L2 groups | Group boards compose lower-level components and can drift into bespoke markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-06 | Repudiation | design_gallery_live.ex | mitigate | Add stable board/state ids and explicit not-applicable notes so omitted states are reproducible. |
| T-188-07 | Tampering | MG board markup | mitigate | Use ratified L1 components named in D-11 and source assertions for no `dialog.modal`. |
| T-188-08 | Information Disclosure | synthetic board data | accept | Gallery uses static fake data only; do not add DB queries or real user data. |
| T-188-SC | Tampering | package installs | mitigate | No package installs; use existing LiveView and Playwright stack only. |
</threat_model>

<verification>
Run `cd test/example && mix compile --warnings-as-errors` and source assertions for all eleven board ids, state markers, no modal classes, and the MG-5/MG-6 desktop/mobile test ids.
</verification>

<success_criteria>
- D-01, D-02, D-06, D-10, D-11, D-12, D-13, and D-14 are represented in gallery markup.
- GROUP-01 and GROUP-02 have visible board evidence for all approved groups.
- GROUP-03 has MG-5/MG-6 desktop/mobile examples with equivalent content.
- GROUP-04 has stable equivalent-data reuse examples for Playwright assertions.
</success_criteria>

<output>
Create `.planning/phases/188-meta-components-groups-l2/188-03-SUMMARY.md` when done.
</output>
