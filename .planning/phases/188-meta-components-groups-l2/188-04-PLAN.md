---
phase: 188-meta-components-groups-l2
plan: 04
type: execute
wave: 2
depends_on:
  - 188-02
files_modified:
  - lib/sigra/admin/live/user_show_live.ex
autonomous: true
requirements:
  - GROUP-01
  - GROUP-04
user_setup: []
must_haves:
  truths:
    - "User detail MG-11 uses Sigra-owned confirmation overlay/dialog markup, not DaisyUI modal chrome."
    - "Session revocation events and payloads remain unchanged while confirmation copy names the action and consequence."
    - "Destructive confirmation can be cancelled without revoking sessions."
  artifacts:
    - path: "lib/sigra/admin/live/user_show_live.ex"
      provides: "MG-11 destructive action + confirmation production implementation"
      contains: "sg-confirm-dialog"
  key_links:
    - from: "lib/sigra/admin/live/user_show_live.ex"
      to: "lib/sigra/admin/live/branding_live.ex"
      via: "sg-confirm-overlay / sg-confirm-dialog pattern"
      pattern: "role=\"dialog\""
---

<objective>
Standardize the production user-detail destructive confirmation group on the Sigra-owned MG-11 contract.

Purpose: Per D-13 and D-14, eliminate the DaisyUI modal outlier so production MG-11 evidence matches the gallery and design contract without changing session-revocation logic.
Output: `UserShowLive` renders `sg-confirm-overlay` / `sg-confirm-dialog` confirmation markup with action-specific copy and preserved LiveView events.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
@.planning/phases/188-meta-components-groups-l2/188-RESEARCH.md
@.planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
@.planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
@AGENTS.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md

<interfaces>
From lib/sigra/admin/live/user_show_live.ex:
- Existing events: open_revoke_session, open_revoke_all_sessions, cancel_confirm, confirm_action.
- Existing assign: confirm_action.
- Existing drift: `<dialog :if={@confirm_action} open class="modal">` and `.modal-box`.

From lib/sigra/admin/live/branding_live.ex:
- Confirmation precedent uses `.sg-confirm-overlay`, `.sg-confirm-dialog`, `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, and `.sg-confirm-dialog__actions`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add action-specific MG-11 confirmation assign data</name>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex
    - lib/sigra/admin/live/branding_live.ex
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
    - .planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
    - guides/reference/admin-design-contract.md
  </read_first>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <action>Per D-13 and D-14, keep the existing LiveView event names and revocation event flow, but enrich the `confirm_action` assign maps created by `open_revoke_session` and `open_revoke_all_sessions`. Each map must include `title`, `copy`, `confirm_label`, and `cancel_label` in addition to the existing type/token data. Use action-specific copy: session revoke title `Revoke this session?`, all-sessions title `Revoke all sessions?`, session copy that names the current user's email and says it signs them out of that browser or device, and all-sessions copy `Revoke every active session for {email}? This signs them out everywhere.` Use cancel label `Keep sessions`. Use confirm labels `Revoke session` and `Revoke all sessions`. Preserve `cancel_confirm` and `confirm_action` event behavior and payload handling; do not alter `Actions.revoke_session` or `Actions.revoke_all_sessions` calls.</action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs</automated>
    <automated>rg -n "Revoke this session\\?|Revoke all sessions\\?|Keep sessions|confirm_label|cancel_label" lib/sigra/admin/live/user_show_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `confirm_action` maps include `title`, `copy`, `confirm_label`, and `cancel_label`.
    - Source assertion: copy contains `Revoke this session?`, `Revoke all sessions?`, and `Keep sessions`.
    - Behavior assertion: existing component/install tests still pass.
  </acceptance_criteria>
  <done>Confirmation state carries concrete action/consequence copy while session revocation logic stays intact.</done>
</task>

<task type="auto">
  <name>Task 2: Replace DaisyUI dialog markup with sg-confirm overlay/dialog</name>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex
    - lib/sigra/admin/live/branding_live.ex
    - priv/templates/sigra.install/admin/sigra_admin.css
    - .planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
    - .planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
  </read_first>
  <files>lib/sigra/admin/live/user_show_live.ex</files>
  <action>Replace the `dialog.modal` block in `UserShowLive` with the Sigra-owned pattern: outer `<div :if={@confirm_action} class="sg-confirm-overlay" role="presentation">`, inner `<section class="sg-confirm-dialog" role="dialog" aria-modal="true" aria-labelledby="user-session-confirm-title">`, title `<p id="user-session-confirm-title" class="sg-section-heading">{@confirm_action.title}</p>`, copy paragraph using `@confirm_action.copy`, and action row `<div class="sg-confirm-dialog__actions">`. The cancel button must call `cancel_confirm` and render `@confirm_action.cancel_label` with `sg-btn sg-btn--ghost sg-btn--sm`. The confirm button must call `confirm_action` and render `@confirm_action.confirm_label` with `sg-btn sg-btn--danger sg-btn--sm`. Remove `.modal`, `.modal-box`, `.modal-action`, and generic `Confirm` copy from this file. Keep focus-visible behavior provided by existing button classes and leave Escape handling to the Phase 189 page-level overlay pass; do not add new JavaScript.</action>
  <verify>
    <automated>cd test/example && mix compile --warnings-as-errors</automated>
    <automated>rg -n "sg-confirm-overlay|sg-confirm-dialog|aria-modal=\"true\"|user-session-confirm-title" lib/sigra/admin/live/user_show_live.ex</automated>
    <automated>rg -n "dialog\\.modal|class=\"modal|modal-box|modal-action|>Confirm<" lib/sigra/admin/live/user_show_live.ex && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `UserShowLive` contains `sg-confirm-overlay`, `sg-confirm-dialog`, `role="dialog"`, `aria-modal="true"`, and `aria-labelledby="user-session-confirm-title"`.
    - Source assertion: `UserShowLive` contains no `dialog.modal`, `class="modal`, `modal-box`, `modal-action`, or generic `>Confirm<`.
    - Behavior assertion: `cd test/example && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Production MG-11 uses the Sigra confirmation contract and no DaisyUI modal markup remains in `UserShowLive`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operator -> LiveView event | Destructive session-revocation events are triggered from admin UI controls. |
| confirmation copy -> operator intent | Copy determines whether the operator understands action and consequence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-09 | Tampering | UserShowLive session revoke controls | mitigate | Preserve existing event names and `Actions.revoke_*` calls while changing only confirmation presentation. |
| T-188-10 | Repudiation | confirmation copy | mitigate | Use action-specific title, consequence copy, and labels instead of generic `Confirm`. |
| T-188-11 | Denial of Service | accidental session revocation | mitigate | Keep cancel path explicit as `Keep sessions`; destructive confirm action appears only inside the armed confirmation dialog. |
| T-188-SC | Tampering | package installs | mitigate | No package installs; use existing LiveView and sg-* CSS only. |
</threat_model>

<verification>
Run `cd test/example && mix compile --warnings-as-errors`, source assertions for `sg-confirm-*`, and source assertions that DaisyUI modal tokens are absent from `UserShowLive`.
</verification>

<success_criteria>
- D-13 and D-14 are implemented in production `UserShowLive`.
- MG-11 no longer has DaisyUI modal drift.
- Session revocation behavior is presentation-only changed: existing events and action calls remain.
</success_criteria>

<output>
Create `.planning/phases/188-meta-components-groups-l2/188-04-SUMMARY.md` when done.
</output>
