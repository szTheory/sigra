---
phase: 188-meta-components-groups-l2
plan: 05
type: execute
wave: 3
depends_on:
  - 188-01
  - 188-02
  - 188-03
  - 188-04
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/
  - test/example/priv/playwright/snapshot-allowlist-design
autonomous: true
requirements:
  - GROUP-01
  - GROUP-02
  - GROUP-03
  - GROUP-04
user_setup: []
must_haves:
  truths:
    - "admin-design Playwright captures board-mg-1 through board-mg-11 in chromium, mobile, and dark projects with axe."
    - "Group boards have no overflow/clip/squish at 320, 375, 768, 1024, and 1440px."
    - "MG-5 and MG-6 desktop-table and mobile-card representations expose equivalent content."
    - "Repeated group examples have byte-coherent normalized DOM for equivalent data."
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-design.spec.ts"
      provides: "L2 Playwright assertions for catalog, states, responsiveness, equivalence, no nesting, and coherence"
      contains: "GROUP_BOARDS"
    - path: "test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/"
      provides: "Board screenshot baselines for MG-1..MG-11"
      min_lines: 0
    - path: "test/example/priv/playwright/snapshot-allowlist-design"
      provides: "Design-lane allowlist steady-state"
      contains: "snapshot"
  key_links:
    - from: "test/example/priv/playwright/tests/admin-design.spec.ts"
      to: "test/example/lib/example_web/live/admin/design_gallery_live.ex"
      via: "board ids and data-testid selectors"
      pattern: "board-mg-11"
    - from: "test/example/priv/playwright/tests/admin-design.spec.ts"
      to: "lib/sigra/admin/live/users_index_live.ex"
      via: "data-testid admin-users desktop/mobile result hooks"
      pattern: "admin-users-desktop-results"
    - from: "test/example/priv/playwright/tests/admin-design.spec.ts"
      to: "lib/sigra/admin/live/audit_index_live.ex"
      via: "data-testid admin-audit desktop/mobile result hooks"
      pattern: "admin-audit-desktop-results"
---

<objective>
Extend the admin-design Playwright lane so it enforces the Phase 188 L2 scorecard over MG-1..MG-11.

Purpose: Per D-07, D-08, D-09, D-10, D-11, and D-12, screenshots alone are insufficient; tests must assert states, right components, no card-in-card nesting, responsive fit, table/card equivalence, and byte-coherent reuse.
Output: Expanded `admin-design.spec.ts` plus deliberate board baseline recapture for all intended L2 visual deltas.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
@.planning/phases/188-meta-components-groups-l2/188-RESEARCH.md
@.planning/phases/188-meta-components-groups-l2/188-VALIDATION.md
@.planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
@.planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
@test/example/AGENTS.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md
@.planning/phases/187-individual-components-l1/187-07-SUMMARY.md

<interfaces>
From test/example/priv/playwright/tests/admin-design.spec.ts:
- Existing helpers: waitForLiveViewReady(page), loginAsAdmin(page), assertNoAxeViolations(page, label), assertBoardScreenshot(page, testInfo, boardId)
- Existing constants: RESPONSIVE_WIDTHS = [320, 375, 768, 1024, 1440] as const; COMPONENT_BOARDS; GROUP_BOARDS
- Existing pattern: board screenshots are element-scoped and paired with axe.

Production stable hooks:
- MG-5 users: `admin-users-desktop-results`, `admin-users-mobile-results`
- MG-6 audit explorer: `admin-audit-desktop-results`, `admin-audit-mobile-results`
- MG-6 user audit: `admin-audit-user-desktop-results`, `admin-audit-user-mobile-results`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand GROUP_BOARDS and add catalog/state/component/no-nesting assertions</name>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - .planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
    - .planning/phases/188-meta-components-groups-l2/188-PATTERNS.md
    - test/example/AGENTS.md
  </read_first>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <action>Per D-01, D-02, D-06, D-09, D-10, and D-11, replace `GROUP_BOARDS` with the exact array `['board-mg-1','board-mg-2','board-mg-3','board-mg-4','board-mg-5','board-mg-6','board-mg-7','board-mg-8','board-mg-9','board-mg-10','board-mg-11']`. Add a Playwright test named `group boards expose catalog states and right components` that navigates to `/admin/_design`, waits for LiveView readiness, and asserts every board id is visible. For each board, assert populated plus zero/loading/error state markers or the explicit `mg-N-STATE-note` marker created by Plan 03. Add right-component assertions: MG-1 contains `.sg-metric`, MG-2 contains `.sg-applied-chip`, MG-3 contains `article.sg-card` task cards but the `#board-mg-3` wrapper itself is not `.sg-card`, MG-4 contains `.sg-notice`, MG-5 contains `mg-5-desktop-results` and `mg-5-mobile-results`, MG-6 contains `mg-6-desktop-results`, `mg-6-mobile-results`, and `article.sg-list-row`, MG-7/MG-8 contain `.sg-list-row`, MG-9 contains `.sg-summary-facts`, MG-10 contains `.sg-detail-grid`, and MG-11 contains `.sg-confirm-overlay .sg-confirm-dialog`. Add a no-nesting assertion that scans `#board-mg-1` through `#board-mg-11` and fails if any scored board has `.sg-card .sg-card` without the board carrying an explicit audit-only exclusion attribute. Use stable ids/test ids and role selectors; do not add sleeps. Before any Playwright command, ensure `SIGRA_EXAMPLE_URL=http://localhost:4011` points at the `test/example` app; if not, start `cd test/example && PORT=4011 mix phx.server` in a managed background session and stop it after verification.</action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --grep "catalog states|right components"</automated>
    <automated>test "$(rg -n "board-mg-" test/example/priv/playwright/tests/admin-design.spec.ts | wc -l | tr -d ' ')" -ge "11"</automated>
    <automated>rg -n "group boards expose catalog states and right components|sg-confirm-dialog|sg-card \\.sg-card" test/example/priv/playwright/tests/admin-design.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `GROUP_BOARDS` contains all eleven approved ids and no stale five-item array remains.
    - Behavior assertion: focused catalog/state/component Playwright test exits 0.
    - Source assertion: the test includes a no `.sg-card .sg-card` scan over group boards.
  </acceptance_criteria>
  <done>Playwright enforces L2 catalog coverage, state markers, right components, and no card-in-card nesting.</done>
</task>

<task type="auto">
  <name>Task 2: Add group responsive, MG-5/MG-6 equivalence, and reuse-coherence assertions</name>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - lib/sigra/admin/live/users_index_live.ex
    - lib/sigra/admin/live/audit_index_live.ex
    - lib/sigra/admin/live/audit_user_live.ex
    - lib/sigra/admin/live/organization_live.ex
    - .planning/phases/188-meta-components-groups-l2/188-UI-SPEC.md
    - .planning/phases/188-meta-components-groups-l2/188-CONTEXT.md
  </read_first>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <action>Per D-07, D-08, and D-12, extend the existing `RESPONSIVE_WIDTHS` overflow loop so it checks both `COMPONENT_BOARDS` and `GROUP_BOARDS` at 320, 375, 768, 1024, and 1440px. Add a test named `MG-5 and MG-6 desktop and mobile representations are content-equivalent` that asserts users desktop/mobile containers expose the same primary identity/email/id, status text, organization/activity facts, and `Open user` action; audit desktop/mobile containers expose the same event id, action label/code, outcome/tone text, actor/effective-user text, and pagination/export affordance. Cover both `admin-audit-*` and `admin-audit-user-*` hooks if both are present on their pages. Add a test named `reused group examples render byte-coherently for equivalent data` that compares normalized `innerHTML` for gallery pairs `mg-2-coherence-a/b`, `mg-6-coherence-a/b`, and `mg-11-coherence-a/b`; normalization may remove generated Phoenix attributes such as `data-phx-*`, but must not remove class names, roles, labels, hrefs, `data-tone`, or visible text. Use `expect(...).toEqual(...)` for the normalized byte strings. Before any Playwright command, ensure `SIGRA_EXAMPLE_URL=http://localhost:4011` points at the `test/example` app; if not, start `cd test/example && PORT=4011 mix phx.server` in a managed background session and stop it after verification.</action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --grep "content-equivalent|byte-coherently|overflow"</automated>
    <automated>rg -n "MG-5 and MG-6 desktop and mobile representations are content-equivalent|reused group examples render byte-coherently|GROUP_BOARDS" test/example/priv/playwright/tests/admin-design.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: group responsive checks run against all five widths for `GROUP_BOARDS`.
    - Behavior assertion: focused content-equivalence and byte-coherence Playwright tests exit 0.
    - Source assertion: the test names include `content-equivalent` and `byte-coherently`.
    - Source assertion: no `page.waitForTimeout` or test sleep is introduced.
  </acceptance_criteria>
  <done>GROUP-03 and GROUP-04 are enforced by browser assertions, not only screenshots.</done>
</task>

<task type="auto">
  <name>Task 3: Recapture intended MG board baselines and prove canary discipline</name>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/priv/playwright/snapshot-allowlist-design
    - scripts/ci/snapshot-canary-guard.sh
    - scripts/ci/snapshot-recapture-gate.sh
    - .planning/phases/185-audit-infrastructure/185-03-SUMMARY.md
    - .planning/phases/187-individual-components-l1/187-07-SUMMARY.md
  </read_first>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/, test/example/priv/playwright/snapshot-allowlist-design</files>
  <action>After Tasks 1 and 2 pass without `--update-snapshots`, deliberately recapture the intended L2 board visual deltas for `board-mg-1` through `board-mg-11` across `admin-design-chromium`, `admin-design-mobile`, and `admin-design-dark`. This is the only full admin-design trio task in this plan; keep Tasks 1 and 2 on focused smoke/grep/project subsets for fast feedback. Use the existing admin-design snapshot lane and the project recapture/canary workflow from Phase 185; do not leave broad allowlist entries behind. Keep `board-notice` as the canary and preserve the steady-state comments-only `snapshot-allowlist-design` unless the recapture gate requires a temporary entry that is removed before completion. Before any Playwright command, ensure `SIGRA_EXAMPLE_URL=http://localhost:4011` points at the `test/example` app; if not, start `cd test/example && PORT=4011 mix phx.server` in a managed background session and stop it after verification.</action>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --update-snapshots=all</automated>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark</automated>
    <automated>SNAP_DIR=test/example/priv/playwright/tests/admin-design.spec.ts-snapshots bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice</automated>
    <automated>! rg -v '^#|^$' test/example/priv/playwright/snapshot-allowlist-design</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: full admin-design trio exits 0 after recapture.
    - CLI assertion: design canary guard exits 0 for `board-notice`.
    - Source assertion: `snapshot-allowlist-design` contains only comments/blank lines at completion.
    - Artifact assertion: snapshots exist for all eleven MG board ids across chromium, mobile, and dark projects.
  </acceptance_criteria>
  <done>Visual evidence for MG-1..MG-11 is deliberately captured and steady-state canary discipline is restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser evidence -> ledger claims | Playwright assertions and screenshots become the proof for L2 rows. |
| desktop DOM -> mobile DOM | Two separate representations can accidentally omit different content. |
| recapture workflow -> repository baseline | Snapshot updates can hide regressions if not canary-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-188-12 | Repudiation | admin-design.spec.ts | mitigate | Add deterministic assertions for states, components, equivalence, and coherence before ledger updates. |
| T-188-13 | Information Disclosure | MG-5/MG-6 mobile/desktop swaps | mitigate | Assert identifiers, status/outcome, secondary facts, and actions appear in both representations. |
| T-188-14 | Tampering | board PNG baselines | mitigate | Use deliberate recapture followed by no-update rerun, canary guard, and empty allowlist proof. |
| T-188-15 | Denial of Service | responsive group boards | mitigate | Check all group boards at 320, 375, 768, 1024, and 1440px for overflow/clip/squish. |
| T-188-SC | Tampering | package installs | mitigate | No new packages; use existing Playwright and axe stack. |
</threat_model>

<verification>
Before Playwright, ensure the `test/example` Phoenix app is serving on port 4011; start it in a managed background session if needed. Run focused tests after Tasks 1 and 2, then the full admin-design trio only for Task 3 recapture/final validation, followed by the snapshot canary guard and empty allowlist check.
</verification>

<success_criteria>
- D-07, D-08, D-09, D-10, D-11, and D-12 are enforced by Playwright.
- GROUP-01..GROUP-04 each have automated evidence.
- `GROUP_BOARDS` is the approved 11-item catalog.
- Full admin-design trio and canary guard are green.
</success_criteria>

<output>
Create `.planning/phases/188-meta-components-groups-l2/188-05-SUMMARY.md` when done.
</output>
