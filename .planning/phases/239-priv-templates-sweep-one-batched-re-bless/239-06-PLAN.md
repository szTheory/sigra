---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 06
type: execute
wave: 6
depends_on: ["239-05"]
files_modified:
  - priv/templates/sigra.install/core/sigra_auth.css
  - priv/templates/sigra.install/core/mfa_settings_live.ex
  - priv/templates/sigra.install/organizations/live/organization_members_live.ex
  - priv/templates/sigra.install/organizations/live/organizations_live/index.ex
  - priv/templates/sigra.install/organizations/organization_invitation.ex
  - priv/templates/sigra.install/organizations/organization_invitation_email.ex
  - priv/templates/sigra.install/organizations/live/invitation_accept_live.ex
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`priv/templates/sigra.install/core/sigra_auth.css` carries no GitHub Actions run id, no `round N` iteration history, no bare `NNN-NN` plan id, and no `do not re-litigate this` — while the CSS mechanism it documents (min-width: 0 on grid items; `overflow-wrap: anywhere` rather than `break-word` because only `anywhere` participates in automatic-minimum-size) survives in full."
    - "`grep -cE '<V2>' $(git ls-files priv/templates)` returns 0, paired with the positive control `grep -lc defmodule $(git ls-files priv/templates) | wc -l` returning 97 on the same surface (D-01)."
    - "Every `FIX-239-06` row in the `## WIDENED-UNION-LEDGER` triage table is closed, and the closure is recorded row-by-row rather than as an aggregate count."
    - "The seven prose repairs enumerated in `239-REVIEW.md` as WR-03 … WR-07 and IN-01 … IN-03 are applied in `priv/templates/` using the review's suggested wordings as the source of truth (D-18: rewrite, never strip)."
    - "No sentence in the rewritten prose attributes library behaviour to a generated schema module: `organization_invitation.ex`'s moduledoc names the library as the owner of invitation generation, delivery, and accept/reject."
    - "`organization_members_live.ex` contains no pointer to a HEEx comment marker that does not exist — `grep -c 'this section' HEEx` style pointers are gone and the seam is described in the present tense."
    - "The security rationale in `organization_invitation_email.ex` and `invitation_accept_live.ex` is strengthened, not thinned: the phishing-defense parenthetical names what is spoofed, and the `:mismatch` zero-accept-controls invariant carries a tool-agnostic note that its absence is asserted, not merely conventional (SC-5, IN-03)."
    - "`MIX_ENV=test mix compile --warnings-as-errors` exits 0 with no warning after the edits."
    - "`git show --name-only --format= HEAD` for this plan's commit lists only paths under `priv/templates/` plus the phase evidence file — no `test/example/`, no `test/fixtures/`, no `.github/` (D-19, D-23)."
  artifacts:
    - priv/templates/sigra.install/core/sigra_auth.css
    - priv/templates/sigra.install/organizations/live/organization_members_live.ex
    - priv/templates/sigra.install/organizations/organization_invitation.ex
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  key_links:
    - "`core/sigra_auth.css` is copied verbatim into every generated app's `priv/static/assets/sigra_auth.css` (D-13), so its comment text is literally adopter-shipped bytes — this is why CR-01 is a blocker and not a style note."
    - "Every file edited here renders into `test/fixtures/install_golden/tree/`, so all of these edits must land BEFORE plan 239-08's single re-bless or they will require a second one, breaking SC-3's one-batched-re-bless topology."
  prohibitions:
    - statement: "Must NOT modify executed plans 239-01…239-04 or their SUMMARYs."
    - statement: "Must NOT delete a `# SECURITY:`-class rationale sentence, and must NOT weaken one further — IN-03 already records two thinned rationales; this plan restores them (SC-5, D-16)."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23)."
    - statement: "Must NOT touch `test/example/` in this commit — the mirror is plan 239-07's (D-19 commit topology)."
    - statement: "Must NOT hand-edit `test/fixtures/install_golden/tree/` — it is a generated snapshot; these template edits reach it only via plan 239-08's re-bless (D-09)."
    - statement: "Must NOT re-sweep the five D-03 deliberate exclusions (the `policy.ex` TODO, the `mfa_challenge_live.ex` XXXX-XXXX placeholder, the `sigra_auth.css` v1.46 compat prose, the `scope.ex` UPGRADE-v1.2.md pointer, arity/version strings)."
    - statement: "Must NOT run `mix sigra.fixture.rebless_golden` without `--check`; exactly one re-bless commit lands and it is plan 239-08's."
    - statement: "Must NOT introduce a forward-looking or subjectless sentence in adopter-facing prose — the prose gap exists precisely because the previous sweep did."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`/`unresolved`): assumed edge is the dead-grep case; every zero here is paired with a positive control. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `unclassified`/`unresolved`): assumed edge is commit topology; this plan's commit is asserted path-scoped to `priv/templates/`. Surfaced, not resolved."
---

<objective>
Close the remaining adopter-shipped bookkeeping (GAP 1) and repair the prose this phase's own sweep
damaged (GAP 4), in one path-scoped `priv/templates/` commit, so that plan 239-08's single re-bless
carries every template change at once.

Purpose: `sigra_auth.css` is copied verbatim into every generated app, and it still carries two live
GitHub Actions run ids, a bare plan id, Sigra's review-round history, and "do not re-litigate this".
Separately, five rewrites from the first sweep left sentences that are ungrammatical, unfollowable,
or factually wrong in text adopters read. Both are the same class of defect: adopter-facing comment
text that serves Sigra rather than the adopter.

Output: one commit under `priv/templates/`, a V2-clean template corpus with a paired positive
control, and a row-by-row closure record for every `FIX-239-06` disposition.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-05-SUMMARY.md
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **`239-REVIEW.md` is the source of truth for replacement wording.** Read the suggested wording in
   CR-01, WR-03…WR-07 and IN-01…IN-03 and use it; do not re-derive a wording from scratch.
2. **Rewrite, never strip** (D-17/D-18). A token that sits on a line with a rationale word means the
   sentence is rewritten so the meaning survives and the token goes.
3. **Every zero is paired with a positive control on the same surface.**
4. **A zero-match grep must be a measurement, not a fatal status** — brace group with `|| true`
   inside command substitutions, never a bare `grep | wc -l` under `set -eo pipefail`.
5. **`mix ci`, never root `mix test`** (Standing Constraint 3). The full gate runs in plan 239-08;
   here `MIX_ENV=test mix compile --warnings-as-errors` is the local check.
6. **These are EEx templates.** `<%= web_module %>` and friends are bindings — a comment rewrite must
   never touch an EEx tag, a HEEx sigil, or a CSS declaration. Only comment/doc prose changes.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the two `sigra_auth.css` comment blocks to state the mechanism without Sigra's CI/iteration history</name>
  <files>priv/templates/sigra.install/core/sigra_auth.css</files>
  <read_first>
    - `priv/templates/sigra.install/core/sigra_auth.css` lines 500-540 and 685-700 — the two comment blocks as they stand, including the CSS declarations they annotate (which must not move).
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md` § CR-01 — the authoritative suggested replacement text for both blocks, including the `:696` substitution.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## WIDENED-UNION-LEDGER` — the V2 regex and the `FIX-239-06` rows for this file.
  </read_first>
  <action>
Rewrite the comment block that currently begins at `:514` (`/* Live multi-run CI evidence showed
H2/P sharing one identical right edge …`) so that it states only the mechanism: H2 and P share one
identical right edge with the surrounding stack; neither has `min-width: 0`, so as grid items of a
single-implicit-column `.sigra-auth-stack` both stretch to the shared column track, which is floored
by whichever sibling's min-content is largest (the `<p>` copy's longest unbreakable word);
`min-width: 0` lets each item shrink. Keep the second paragraph's specificity argument intact:
`.sigra-auth p` (0,1,1) outranks `.sigra-auth__product` (0,1,0) and appears later, and per the CSS
Text spec only `overflow-wrap: anywhere` — not `break-word` — participates in a flex/grid item's
automatic-minimum-size calculation, so `break-word` here would silently override the wordmark's
`anywhere` and reintroduce an un-contained min-content floor.

Remove from that block, specifically: `Live multi-run CI evidence`, `after rounds 1-2`, `an initial
round-3 draft`, `observed on live CI runs 30518012012 and 30518015684`, `immediately after the
round-3 commit, never before it`, and the pixel-forensics payload (`321.4375px`, `scrollWidth
445-473px vs innerWidth 320px`) — those numbers are the record of a Sigra debugging session, not
information an adopter can act on.

In the second block (currently ending near `:698`), replace `even though 231-02's min-width: 0
already reached the leaf input/button` with a plan-id-free statement that the earlier leaf-level
`min-width: 0` reached only the input/button, and delete `do not re-litigate this` together with the
reflow-failure payload sentence (`DIV.fieldset/LABEL/SPAN.label/INPUT/BUTTON sharing one identical
right edge`). Keep the mechanism: `.sigra-auth form` is `display: grid`, Phoenix 1.8's `<.input>`
wrapper `<div class="fieldset mb-2">` is itself a grid item, DaisyUI's compiled `.fieldset` is also
`display: grid`, so this un-contained grid-item ancestor still carries its automatic minimum width
(min-content) — which is why the leaf-only fix was insufficient.

Do not move, reformat, or edit any CSS declaration, selector, or value. Do not touch the D-03
deliberate exclusion elsewhere in this file (the v1.46 compat prose).
  </action>
  <verify>
    <automated>bash -lc 'f=priv/templates/sigra.install/core/sigra_auth.css; n=$(grep -cE "30518012012|30518015684|round-3|rounds 1-2|231-02|re-litigate" "$f" || true); p=$(grep -c "min-width: 0" "$f" || true); test "$n" -eq 0 && test "$p" -gt 0 && echo CSS_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '30518012012|30518015684|actions/runs/' priv/templates/sigra.install/core/sigra_auth.css` → `0`.
    - `grep -cE '[Rr]ound[s]?[ -][0-9]' priv/templates/sigra.install/core/sigra_auth.css` → `0`.
    - `grep -cE '\b[0-9]{3}-[0-9]{2}\b' priv/templates/sigra.install/core/sigra_auth.css` → `0` (the bare `231-02` plan id is gone).
    - `grep -c 'do not re-litigate this' priv/templates/sigra.install/core/sigra_auth.css` → `0`.
    - Paired positive control on the same file: `grep -c 'min-width: 0' …` → `>= 1` and `grep -c 'overflow-wrap' …` → `>= 1`; the mechanism survived the deletion.
    - `grep -c 'automatic-minimum-size\|automatic minimum' priv/templates/sigra.install/core/sigra_auth.css` → `>= 1` — the reason `anywhere` is required is still stated.
    - `git diff -- priv/templates/sigra.install/core/sigra_auth.css | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-]\s*(/\*|\*|[A-Za-z0-9`.,;:()<>|&@#%$-]|\*/)' ` produces no line that is a CSS declaration — inspected and recorded; every changed line lies inside a `/* … */` comment range.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The file every adopter receives as `priv/static/assets/sigra_auth.css` explains its own CSS and says nothing about Sigra's CI runs, review rounds, or plan ids.</done>
  <reversibility rating="reversible">Comment-only edits in one template file; `git revert` restores the prior text exactly and nothing is published from this commit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Apply the seven prose repairs (WR-03 … WR-07, IN-01 … IN-03) in `priv/templates/`</name>
  <files>priv/templates/sigra.install/organizations/live/organization_members_live.ex, priv/templates/sigra.install/organizations/live/organizations_live/index.ex, priv/templates/sigra.install/organizations/organization_invitation.ex, priv/templates/sigra.install/core/mfa_settings_live.ex, priv/templates/sigra.install/organizations/organization_invitation_email.ex, priv/templates/sigra.install/organizations/live/invitation_accept_live.ex</files>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md` § WR-03, WR-04, WR-05, WR-06, WR-07, IN-01, IN-02, IN-03 — each finding carries its suggested replacement wording; this is the source of truth and must not be re-derived.
    - `priv/templates/sigra.install/organizations/live/organization_members_live.ex` lines 1-35.
    - `priv/templates/sigra.install/organizations/live/organizations_live/index.ex` lines 1-16.
    - `priv/templates/sigra.install/organizations/organization_invitation.ex` lines 8-20.
    - `priv/templates/sigra.install/core/mfa_settings_live.ex` lines 600-612, and `test/example/lib/example_web/live/mfa_settings_live.ex` lines 627-636 — the example holds the clean wording the template must adopt (read only; do not edit the example here).
    - `priv/templates/sigra.install/organizations/organization_invitation_email.ex` lines 10-18 and `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` lines 12-24 — the two IN-03 security-rationale sites.
  </read_first>
  <action>
Apply, in `priv/templates/` only:

**WR-03 + WR-04 + IN-01 + IN-02 — `organization_members_live.ex`.** Delete the unfollowable pointer
sentence (`Look for the \`this section\` HEEx comment to find the fill-in point.`) and replace the
`## Pending invitations seam` paragraph with a present-tense description of what the code actually
does: the `pending-invitations-section` `<section>` below the members table renders a populated
`@streams.pending_invitations` table, or an empty-state card when `@pending_count == 0`. Correct the
Architecture bullet that still calls the seam "an empty-state card with a HEEx comment marker" to
match. Join the `Generated by` / `` `mix sigra.install --live` `` wrap onto one line so it matches the
example (IN-01). Delete the stray `only.` adverb in the pagination bullet (IN-02), keeping the
following "Flop / sortable columns are a v1.2 concern" sentence which already carries the meaning.

**WR-05 — `organizations_live/index.ex`.** Replace the Branch B bullet with the review's wording:
`Branch B: pending invitations list with an Accept action for each invitation`. No actor-less verb,
no "until then".

**WR-06 — `organization_invitation.ex`.** Restore the missing subject: the `hashed_token` field
stores the HMAC-signed invitation token; the **library** implements the full invitation flow
(generation, email delivery, accept/reject); this schema only persists the state. The generated
schema must not be described as owning behaviour that lives in `Sigra.Organizations` /
`Sigra.Token.generate_invite_envelope/2`.

**WR-07 — `mfa_settings_live.ex:606`.** Adopt the example's already-clean wording:
`# Auto-submit when 6 digits entered. Calls the confirm path` followed by `# directly instead of
dispatching via`. The trailing bare colon is removed and both trees converge on one wording.

**IN-03(a) — `organization_invitation_email.ex`.** Change the phishing parenthetical so the threat
is explicit rather than reading as though spoofing is the defense: `(phishing defense — prevents
inviter/org spoofing)`.

**IN-03(b) — `invitation_accept_live.ex`.** Append a tool-agnostic sentence to the
`## Structural Jetstream #907 defense` paragraph stating that the absence of accept controls in the
`:mismatch` branch is asserted by a test and must not be treated as stylistic — do not add accept
controls there. Do not name a plan-checker, a plan id, or a grep command; the point is that the
invariant is machine-checked, not which tool checks it.

Every edit is comment/doc prose only. Do not touch a HEEx sigil, an EEx tag, a function head, or an
attribute list. Do not touch `test/example/` — plan 239-07 converges the mirrors onto these exact
wordings.
  </action>
  <verify>
    <automated>bash -lc 'set -o pipefail; a=$(grep -c "this section. HEEx" priv/templates/sigra.install/organizations/live/organization_members_live.ex || true); b=$(grep -c "until then" priv/templates/sigra.install/organizations/live/organizations_live/index.ex || true); c=$(grep -c "6 digits entered:" priv/templates/sigra.install/core/mfa_settings_live.ex || true); test "$a" -eq 0 && test "$b" -eq 0 && test "$c" -eq 0 && echo PROSE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Look for the' priv/templates/sigra.install/organizations/live/organization_members_live.ex` → no output; and `grep -c 'pending-invitations-section' …` → `>= 1` (the seam is still named, only the false pointer is gone).
    - `grep -n 'will replace the card body\|will later gain' priv/templates/sigra.install/organizations/live/organization_members_live.ex` → no output; the paragraph is present tense.
    - `grep -cE '^\s+only\.' priv/templates/sigra.install/organizations/live/organization_members_live.ex` → `0`; `grep -c 'v1.2 concern' …` → `1`.
    - `grep -c 'until then' priv/templates/sigra.install/organizations/live/organizations_live/index.ex` → `0`; `grep -c 'Branch B' …` → `1`.
    - `grep -c 'library implements the full invitation flow' priv/templates/sigra.install/organizations/organization_invitation.ex` → `1`, and the line `Implements the full invitation flow` (subjectless form) returns `0`.
    - `grep -c '6 digits entered\. Calls the confirm path' priv/templates/sigra.install/core/mfa_settings_live.ex` → `1`; `grep -c '6 digits entered:' …` → `0`.
    - `grep -c 'prevents inviter/org spoofing' priv/templates/sigra.install/organizations/organization_invitation_email.ex` → `1`; the pre-existing `XSS defense` and `HMAC-signed by the library` bullets are untouched (diff shows no change on those lines).
    - `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` contains a sentence asserting the `:mismatch` zero-accept-controls invariant is test-asserted; `grep -c 'ZERO .phx-click' …` still returns `1` — the original invariant statement is intact, not replaced.
    - `git diff --stat -- priv/templates` lists exactly the six files named in this task plus `sigra_auth.css` from Task 1; no other template changed.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0 with no warning.
  </acceptance_criteria>
  <done>Adopter-facing prose in `priv/templates/` is grammatical, followable, factually true about who owns which behaviour, and its security rationale is stronger than before the sweep.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure `priv/templates/` under V2, close every `FIX-239-06` row, and commit path-scoped</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## WIDENED-UNION-LEDGER` — the triage table whose `FIX-239-06` rows this task closes, and the V2 string to re-run.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md` § D-19 — the commit topology this commit extends, and § D-23 — the path restriction.
  </read_first>
  <action>
Re-run V2 over `$(git ls-files priv/templates)` and record the count in a new
`## CLOSURE-TEMPLATE-COMMIT` slot in `239-EVIDENCE.md` (slot-heading convention as before: bare
`^## [A-Z0-9-]+$`, `Status:` line beneath). The expected count is 0; whatever it is, record it with
its paired positive control (`grep -lc defmodule` across the same file list → 97 files).

If V2 is non-zero, every remaining hit must be dispositioned in the ledger before the commit —
either closed here (if it is `priv/templates/` bookkeeping) or moved to `OUT-OF-SCOPE` with a named
requirement. A non-zero V2 count with undispositioned rows is a stop-the-line event: halt and report.

Extend the ledger slot with a **row-by-row closure record**: one line per `FIX-239-06` row from
plan 239-05's triage, stating the file, the line as it read before, and the literal grep that now
proves it closed. Aggregate counts alone are not acceptable here — the whole gap exists because an
aggregate green was trusted.

Re-prove SC-5 for this commit's diff, at this commit: run
`.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-security-comment-diff-check.sh`
against the diff of this commit (D-16 pins the script — run it, never edit it) and record its
`examined_removed_lines=<N>` with N > 0 alongside its exit code. Record
`git diff --name-only origin/main -- .github/` as empty.

Commit path-scoped to `priv/templates` plus the evidence file:
`refactor(239): strip residual CI/plan bookkeeping and repair swept prose in priv/templates (SURF-01, SURF-03)`.
Then assert `git show --name-only --format= HEAD` lists nothing outside those paths.
  </action>
  <verify>
    <automated>bash -lc 'git show --name-only --format= HEAD | grep -vE "^(priv/templates/|\.planning/phases/239-)" | wc -l | grep -qx 0 && echo SCOPE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '<V2>' $(git ls-files priv/templates)` → `0`, recorded in the ledger with the exact command.
    - Paired positive control recorded: `grep -lc defmodule $(git ls-files priv/templates) | wc -l` → `97`. A zero here would mean the grep itself is broken and the V2 zero is worthless.
    - The `## CLOSURE-TEMPLATE-COMMIT` slot contains one closure line per `FIX-239-06` row from plan 239-05, each with a literal proving grep; the number of closure lines equals the number of `FIX-239-06` rows.
    - `237-security-comment-diff-check.sh` against this commit's diff exits 0 and prints `examined_removed_lines=` with a value `> 0` (a vacuous pass is impossible); output recorded verbatim in the ledger.
    - `git diff --name-only origin/main -- .github/` → empty, recorded.
    - `git show --name-only --format= HEAD` lists only paths under `priv/templates/` and `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/`.
    - `git status --porcelain` is clean after the commit.
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` now exits **2** with `DRIFT DETECTED:` — the expected, correct state: the templates moved and the golden fixture has not been re-blessed yet. A 0 here would mean the template edits never reach generated output, which is a stop-the-line event.
  </acceptance_criteria>
  <done>`priv/templates/` is clean under the widened definition with a live positive control, every triage row is individually closed, and the golden fixture is provably stale — which is what plan 239-08 exists to resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `priv/templates/` → adopter application source | `mix sigra.install` copies these bytes into a third party's repo; `sigra_auth.css` is copied verbatim (D-13). |
| Comment rationale → future maintainer | The `:mismatch` zero-accept-controls invariant and the phishing-defense note are the only in-code record of why those controls are shaped as they are. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-06-01 | Information disclosure | `sigra_auth.css` shipped comments (CI run ids, plan ids) | medium | mitigate | Rewrite both blocks to mechanism-only; assert by grep on the exact literals with a mechanism-survived positive control. |
| T-239-06-02 | Tampering | Security rationale weakened while editing adjacent prose | high | mitigate | IN-03(a)/(b) strengthen rather than delete; `237-security-comment-diff-check.sh` run over this commit's diff with `examined_removed_lines > 0`; D-16 forbids editing the checker to pass. |
| T-239-06-03 | Elevation of privilege | An "accept control" added to the `:mismatch` branch by a future editor who no longer sees the invariant is machine-checked | high | mitigate | IN-03(b) restores a tool-agnostic "asserted by a test — do not add accept controls here" note in the moduledoc. |
| T-239-06-04 | Tampering | A CSS declaration silently altered under cover of a comment rewrite | high | mitigate | Diff inspected for non-comment changed lines; `mix compile --warnings-as-errors` clean; plan 239-08's classifier re-checks the generated result. |
| T-239-06-05 | Denial of service | `.github/` `name:` drift breaking required status contexts | high | mitigate | Path-scoped commit plus asserted-empty `.github/` diff (D-23). |
</threat_model>

## Artifacts this phase produces (plan 239-06)

| Artifact | New or modified | What it is |
|---|---|---|
| `priv/templates/sigra.install/core/sigra_auth.css` | modified | Two comment blocks rewritten to mechanism-only; CI run ids, round history, plan id, and "do not re-litigate this" removed. |
| `priv/templates/sigra.install/organizations/live/organization_members_live.ex` | modified | WR-03/WR-04/IN-01/IN-02 prose repairs. |
| `priv/templates/sigra.install/organizations/live/organizations_live/index.ex` | modified | WR-05 Branch B rewrite. |
| `priv/templates/sigra.install/organizations/organization_invitation.ex` | modified | WR-06 subject restored; library named as flow owner. |
| `priv/templates/sigra.install/core/mfa_settings_live.ex` | modified | WR-07; converges on the example's clean wording. |
| `priv/templates/sigra.install/organizations/organization_invitation_email.ex` | modified | IN-03(a) phishing-defense parenthetical strengthened. |
| `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` | modified | IN-03(b) tool-agnostic "asserted by a test" note restored. |
| `239-EVIDENCE.md` § `## CLOSURE-TEMPLATE-COMMIT` | new slot | V2 re-measure with positive control, row-by-row `FIX-239-06` closure record, SC-5 re-proof, `.github/` empty-diff proof. |

<verification>
- V2 over `priv/templates/` → 0, with the 97-file `defmodule` positive control.
- Every `FIX-239-06` row closed individually, each with a proving grep.
- `237-security-comment-diff-check.sh` exits 0 with `examined_removed_lines > 0`.
- Commit lists nothing outside `priv/templates/` and the phase directory.
- `rebless_golden --check` exits 2 — the templates provably moved.
</verification>

<success_criteria>
Nothing in `priv/templates/` tells an adopter about Sigra's CI runs, review rounds, or plan ids, and
no sentence the previous sweep damaged is still shipping.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-06-SUMMARY.md` when done.
Record: the V2 count and its positive control, the closure record row count, the SC-5 checker output
verbatim, and the `--check` exit-2 observation.
</output>
