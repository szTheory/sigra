---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 07
type: execute
wave: 7
depends_on: ["239-06"]
files_modified:
  - test/example/lib/example_web/router.ex
  - test/example/lib/example_web/components/layouts.ex
  - test/example/lib/example_web/controllers/session_html.ex
  - test/example/lib/example_web/live/organization_members_live.ex
  - test/example/lib/example_web/live/organizations_live/index.ex
  - test/example/lib/example_web/live/invitation_accept_live.ex
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
autonomous: true
requirements: [SURF-03]
gap_closure: true

estimate:
  tokens: 100000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`test/example/lib/example_web/router.ex` carries none of the five swept tokens at its former lines 36, 77, 103, 209 and 226, and each rewritten comment matches the wording already committed in `test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex` at lines 86, 97, 137, 151 and 169 — the generated tree is the wording authority, not a fresh invention."
    - "`test/example/lib/example_web/components/layouts.ex` line 9's comment and line 52's `attr … doc:` string both carry no bookkeeping token; the `doc:` string is treated as user-visible text because ExDoc renders it and LSP hover surfaces it (it is not a comment)."
    - "`test/example/` and `priv/templates/` state ONE wording each for every sentence WR-03…WR-07 and IN-01…IN-04 touched — compared sentence by sentence after normalizing the module namespace, with the comparison result recorded per sentence."
    - "`239-MIRROR-CHECKLIST.md` disposition for `organizations/router_injection.ex` names `test/example/lib/example_web/router.ex` as its de-facto counterpart with the five mirrored sites, replacing the stale `no counterpart (delivered by injection)` row."
    - "`239-MIRROR-CHECKLIST.md` carries a row for `lib/sigra/install/features/core.ex`, which this phase edited and which the frozen checklist omitted entirely."
    - "`239-MIRROR-CHECKLIST.md` carries a row for `test/example/lib/example_web/components/layouts.ex` recording both the line-9 comment and the line-52 `attr doc:` string."
    - "`239-MIRROR-CHECKLIST.md` carries a `## Closure batch (plans 239-05 / 239-06)` table with one per-file disposition for each of the eight templates the closure edited — so SC-4's per-edited-file rule holds for the closure batch and not only for the first sweep. `core/sigra_auth.css` and `organizations/organization_invitation_email.ex` each carry an explicit no-mirror-needed disposition asserted by a recorded grep, not by assertion."
    - "The checklist states the sha it is now accurate against, and states that it was originally frozen against `cafd9a53` before the router sweep landed in `6fa3ead2` / `e4e03980` — the staleness is recorded, not silently overwritten."
    - "`grep -cE '<V2>' test/example/lib/example_web/router.ex test/example/lib/example_web/components/layouts.ex` returns 0, paired with a recorded non-zero V2 count on the IN-05 example-only files, proving the grep is live on this tree (D-01 positive-control discipline)."
    - "`git show --name-only --format= HEAD` for this plan's commit lists only paths under `test/example/` and the phase directory — SC-4's `only`-clause holds for the closure mirror exactly as it did for `e69f4d9b`."
  artifacts:
    - test/example/lib/example_web/router.ex
    - test/example/lib/example_web/components/layouts.ex
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  key_links:
    - "`priv/templates/sigra.install/organizations/router_injection.ex` and the router heredoc in `lib/sigra/install/features/core.ex` are spliced into a generated app's `router.ex`, which is why `test/example/lib/example_web/router.ex` is their de-facto counterpart despite having no `{:eex, …}` emission target — the exact reason the frozen checklist mis-dispositioned it."
    - "`test/example/` never reaches an adopter — this commit closes a SURF-03 mirror-clause divergence, not an adopter leak; claiming otherwise would overclaim."
  prohibitions:
    - statement: "Must NOT modify executed plans 239-01…239-04 or their SUMMARYs."
    - statement: "Must NOT touch any file outside `test/example/` and the phase directory in this commit — SC-4's `only`-clause is the thing being preserved (D-19, D-23)."
    - statement: "Must NOT sweep the IN-05 example-only files (`test/example/lib/example/demo/seeds.ex`, `…/sigra_admin_policy.ex`, `…/live/admin/design_gallery_live.ex`, `test/example/test/**`) — they have no `priv/templates/` counterpart, are explicitly outside this phase's declared scope, and serve as the positive control that V2 is still live on this tree."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23)."
    - statement: "Must NOT hand-edit `test/fixtures/install_golden/tree/` (D-09) and must NOT run `mix sigra.fixture.rebless_golden` without `--check` — the single re-bless is plan 239-08's."
    - statement: "Must NOT build the FUT-01 template↔example parity guard — it stays a filed todo (D-25, Standing Constraint 5)."
    - statement: "Must NOT change behaviour: every edit is comment or `doc:` string text; no route, plug, pipeline, attr name, default, import, or function head moves."
    - statement: "Must NOT make a phantom edit to a file that is already converged — record the confirmed disposition instead, as the first mirror's `already absent` rows did."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `unclassified`/`unresolved`): assumed edge is that an `attr … doc:` string is user-visible text rather than a comment, so SURF-03's mirror clause covers it. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`/`unresolved`): assumed edge is the dead-grep case; the V2 zero on the two edited example files is paired with a non-zero V2 count elsewhere under `test/example/`. Surfaced, not resolved."
---

<objective>
Close the SC-4 mirror gap: bring `test/example/` into line with the swept generator on the two files
the first mirror never reached, converge every prose repair onto one wording per sentence, and make
`239-MIRROR-CHECKLIST.md` tell the truth about what was edited and what its counterpart is.

Purpose: SURF-03's mirror clause exists to prevent exactly the divergence that now exists —
`test/example/lib/example_web/router.ex` still carries all five tokens the generator no longer
emits, and `layouts.ex` carries two more, one of them an ExDoc-rendered `attr doc:` string. The
checklist was frozen against commit `cafd9a53` and never updated after the router sweep landed, so
it dispositions `router_injection.ex` as having no counterpart and omits
`lib/sigra/install/features/core.ex` altogether.

Output: one commit confined to `test/example/`, a checklist accurate at the final HEAD that records
its own prior staleness, and a V2 measurement with a live positive control.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-06-SUMMARY.md
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **The golden tree is the wording authority for router comments.** Copy the swept wording from
   `test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex`; do not invent a
   third wording — a third wording is exactly how the WR-04 and WR-07 divergences happened.
2. **Namespace substitution is expected and is not divergence.** The golden tree says
   `SigraInstallGoldenTmpWeb`, the example says `ExampleWeb`; the prose must match, the module names
   must not be copied across.
3. **Every zero is paired with a positive control on the same surface.**
4. **A zero-match grep must be a measurement, not a fatal status** — brace group with `|| true`
   inside command substitutions, never a bare `grep | wc -l` under `set -eo pipefail`.
5. **`mix ci`, never root `mix test`** (Standing Constraint 3) — the full gate runs in plan 239-08;
   here `MIX_ENV=test mix compile --warnings-as-errors` is the local check.
6. **Found-while-cleaning becomes a todo, never an in-phase fix** (Standing Constraint 4).

<tasks>

<task type="auto">
  <name>Task 1: Mirror the five `router.ex` sites and the two `layouts.ex` sites into `test/example/`</name>
  <files>test/example/lib/example_web/router.ex, test/example/lib/example_web/components/layouts.ex</files>
  <read_first>
    - `test/example/lib/example_web/router.ex` lines 30-40, 70-82, 98-110, 203-215, 220-230 — the five token-bearing comments in their current form.
    - `test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex` lines 84-100, 133-155, 165-172 — the already-swept wording that is the authority.
    - `test/example/lib/example_web/components/layouts.ex` lines 5-12 and 48-56 — the line-9 comment and the line-52 `attr :user_organizations` `doc:` string.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md` § WR-01 and § WR-02 — the side-by-side table of every site and the exact suggested replacements.
  </read_first>
  <action>
In `test/example/lib/example_web/router.ex`, replace the five token-bearing comment lines with the
golden tree's swept wording, leaving each comment's remaining continuation lines and all code
beneath unchanged:

  * `# MFA challenge (accessible with mfa_pending sessions, D-24)` → `# MFA challenge (accessible with mfa_pending sessions)`
  * `# Phase 17 D-06: single unscoped InvitationAcceptLive at` → `# Single unscoped InvitationAcceptLive at` (the following lines about the route staying outside `:require_authenticated_user` stay verbatim)
  * `# Sigra organizations (Phase 16)` → `# Sigra organizations`
  * `# "switch" as a slug (D-06).` → `# "switch" as a slug.`
  * the three-line `# Phase 10.1.1 B9: login page is a plain controller + HEEx render, / # NOT a LiveView. Keeping it outside the live_session ensures / # \`Phoenix.Component.form/1\` renders a plain \`<form action=… method="post">\`` block → whatever shape the golden router actually took at its line 97 (`# Login page is a plain controller, not a LiveView.`). Read the golden file first: if it collapsed the block to one line, collapse the example's too; if it kept explanatory continuation lines, keep them and strip only the `Phase 10.1.1 B9:` prefix. Do not guess.

In `test/example/lib/example_web/components/layouts.ex`:

  * line 9 → `# Organization switcher function component.`
  * line 52 → `doc: "list of {organization, role} tuples for the current user"` — this is an
    `attr/3` `:doc` option, not a comment: it renders into ExDoc and LSP hover, so the token is
    user-visible text. It must remain a valid double-quoted string with the same meaning minus the
    token.

Touch nothing else in either file. Read the diff before staging and confirm no route, pipeline,
plug, `attr` name, `default:`, import, or function head moved.
  </action>
  <verify>
    <automated>bash -lc 'n=$( { grep -chE "D-[0-9]{2}|[Pp]hase[ -][0-9]+|\bB[0-9]\b" test/example/lib/example_web/router.ex test/example/lib/example_web/components/layouts.ex || true; } | paste -sd+ - | bc ); test "$n" -eq 0 && echo MIRROR_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'D-[0-9]{2}|[Pp]hase[ -][0-9]+|\bB[0-9]\b' test/example/lib/example_web/router.ex` → `0`; paired positive control `grep -c 'pipe_through' test/example/lib/example_web/router.ex` → `>= 5`.
    - `grep -cE 'D-[0-9]{2}|[Pp]hase[ -][0-9]+' test/example/lib/example_web/components/layouts.ex` → `0`; paired positive control `grep -c 'attr :' …` → `>= 2`.
    - `grep -c 'MFA challenge (accessible with mfa_pending sessions)' test/example/lib/example_web/router.ex` → `1` — byte-identical prose to the golden tree's line 86.
    - `grep -c 'Single unscoped InvitationAcceptLive at' test/example/lib/example_web/router.ex` → `1`; `grep -cE '^\s*# Sigra organizations$' …` → `1`; `grep -c '"switch" as a slug\.' …` → `1`.
    - `grep -c 'doc: "list of {organization, role} tuples for the current user"' test/example/lib/example_web/components/layouts.ex` → `1`, with no trailing parenthetical.
    - `git diff -- test/example/lib/example_web/router.ex test/example/lib/example_web/components/layouts.ex` contains no changed line outside a `#` comment or the `doc:` string — every `+`/`-` pair is comment or doc text, inspected and recorded.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0 with no warning.
  </acceptance_criteria>
  <done>The example app's router and layouts say what a freshly generated app's router and layouts say, and the checklist's highest-signal divergence is closed.</done>
  <reversibility rating="reversible">Comment and doc-string edits in a demo app that never ships; `git revert` restores them exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Converge the example counterparts onto the single wording chosen in plan 239-06</name>
  <files>test/example/lib/example_web/live/organization_members_live.ex, test/example/lib/example_web/live/organizations_live/index.ex, test/example/lib/example_web/controllers/session_html.ex, test/example/lib/example_web/live/invitation_accept_live.ex</files>
  <read_first>
    - `priv/templates/sigra.install/organizations/live/organization_members_live.ex` — the post-239-06 wording the example must adopt.
    - `test/example/lib/example_web/live/organization_members_live.ex` lines 1-32 — currently a *different* rewrite of the same swept sentence (`will later gain` vs the template's `will replace the card body`), plus the same unfollowable `this section` pointer.
    - `priv/templates/sigra.install/organizations/live/organizations_live/index.ex` and `test/example/lib/example_web/live/organizations_live/index.ex` lines 8-16 — both carry the identical WR-05 Branch B defect.
    - `priv/templates/sigra.install/core/login_html.ex` (post-239-05) and `test/example/lib/example_web/controllers/session_html.ex` lines 1-14 — the `during UAT` + `LiveView's / LiveView` moduledoc; D-21 maps this renamed counterpart.
    - `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` (post-239-06) and `test/example/lib/example_web/live/invitation_accept_live.ex` lines 12-24 — the IN-03(b) `:mismatch` invariant note.
    - `test/example/lib/example_web/live/mfa_settings_live.ex` lines 627-636 — already carries the clean WR-07 wording; confirm rather than assume.
    - `test/example/lib/example/accounts/organization_invitation.ex` — `@moduledoc false`; confirm there is no WR-06 counterpart sentence rather than assuming.
  </read_first>
  <action>
Apply to `test/example/` the same wording plan 239-06 applied to the templates, with only the
module/namespace substitution differing:

  * `live/organization_members_live.ex` — adopt the template's present-tense `## Pending
    invitations seam` paragraph and its Architecture bullet, delete the `Look for the \`this
    section\` HEEx comment` pointer, delete the stray `only.` adverb.
  * `live/organizations_live/index.ex` — adopt the template's Branch B line (an Accept action per
    invitation; no actor-less verb, no dangling "until then").
  * `controllers/session_html.ex` — adopt the template's rewritten moduledoc: no `during UAT`, one
    `LiveView's form-submission attributes`, causal claim intact.
  * `live/invitation_accept_live.ex` — adopt the template's IN-03(b) sentence that the absence of
    accept controls in the `:mismatch` branch is asserted by a test and must not be treated as
    stylistic.

For `live/mfa_settings_live.ex` and `accounts/organization_invitation.ex`, record the confirmed
disposition (`already converged` / `@moduledoc false — no counterpart sentence`) instead of making a
phantom edit; a phantom edit would misrepresent an already-correct file as freshly mirrored.

Then prove convergence mechanically: for each of the four converged sentences, extract it from both
trees, normalize the module namespace (`ExampleWeb` ↔ `<%= web_module %>` / `Example.` ↔
`<%= app_module %>`), and diff. Record the per-sentence comparison result in the evidence ledger. A
prose difference beyond the namespace substitution is a stop-the-line event for that sentence — go
back and pick one wording rather than shipping two.
  </action>
  <verify>
    <automated>bash -lc 'a=$(grep -c "during UAT" test/example/lib/example_web/controllers/session_html.ex || true); b=$(grep -c "until then" test/example/lib/example_web/live/organizations_live/index.ex || true); c=$(grep -c "Look for the" test/example/lib/example_web/live/organization_members_live.ex || true); test "$a" -eq 0 -a "$b" -eq 0 -a "$c" -eq 0 && echo CONVERGED'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'during UAT' test/example/lib/example_web/controllers/session_html.ex` → `0`; `grep -c "LiveView's" …` → `1` (duplication collapsed, reference not deleted); `grep -c 'SessionController.create/2' …` → `1` (causal claim survived).
    - `grep -c 'until then' test/example/lib/example_web/live/organizations_live/index.ex` → `0`; `grep -c 'Branch B' …` → `1`.
    - `grep -c 'Look for the' test/example/lib/example_web/live/organization_members_live.ex` → `0`; `grep -c 'pending-invitations-section' …` → `>= 1`; `grep -cE '^\s+only\.' …` → `0`.
    - `test/example/lib/example_web/live/invitation_accept_live.ex` contains the same "asserted by a test — do not add accept controls here" sentence as the template, and `grep -c 'ZERO .phx-click' …` still returns `1`.
    - A per-sentence convergence table exists in `239-EVIDENCE.md` with one row per converged sentence and a `MATCH` / `DIVERGES` verdict; every row reads `MATCH` after namespace normalization.
    - Two rows record the confirmed no-edit dispositions for `mfa_settings_live.ex` and `accounts/organization_invitation.ex`, each with the grep that confirmed it; neither file appears in `git diff --name-only`.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0 with no warning.
  </acceptance_criteria>
  <done>Every sentence WR-03…WR-07 and IN-01…IN-04 touched reads identically in both trees, and the two already-correct files are recorded as such rather than edited for appearance.</done>
</task>

<task type="auto">
  <name>Task 3: Correct `239-MIRROR-CHECKLIST.md`, record the V2 measurement with its positive control, and commit path-scoped</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md` — the whole file: the header paragraph defining the three dispositions, the 30-row mirrored table, the already-absent table, and row 79's `router_injection.ex` disposition.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the SC-4 gap block naming the wrong row and the two missing rows.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## WIDENED-UNION-LEDGER` — the V2 string to re-run.
    - `lib/sigra/install/features/core.ex` — the file this phase edited that the checklist never recorded; read enough to state what was edited and what its `test/example/` counterpart is.
    - `test/example/priv/static/assets/sigra_auth.css` — confirm by reading that it does not contain the two comment blocks plan 239-06 rewrote, so the closure row's no-mirror-needed disposition is a measured fact rather than an inference from file size.
  </read_first>
  <action>
Correct the checklist in place:

  * Replace row 79's `no counterpart (delivered by injection)` disposition for
    `organizations/router_injection.ex` with a disposition naming
    `test/example/lib/example_web/router.ex` as its de-facto counterpart, listing the five mirrored
    sites and the commit that mirrored them. Add a one-sentence note explaining *why* the original
    disposition was defensible-but-wrong: the template has no `{:eex, …}` emission target, so a
    filename-based mapping finds nothing, but the router heredoc is spliced into the generated
    `router.ex` all the same.
  * Add a row for `lib/sigra/install/features/core.ex` — edited by this phase, previously absent
    from the checklist entirely — with its counterpart disposition.
  * Add a row for `test/example/lib/example_web/components/layouts.ex` recording both sites: the
    line-9 comment and the line-52 `attr … doc:` string, flagged as ExDoc/LSP-rendered rather than
    a comment.
  * Add a `## Closure batch (plans 239-05 / 239-06)` table carrying **one row per template edited by
    the closure**, so SC-4's per-file-disposition rule holds for this batch exactly as it held for
    the first sweep. Eight rows, one each for: `core/login_html.ex`, `core/sigra_auth.css`,
    `core/mfa_settings_live.ex`, `organizations/live/organization_members_live.ex`,
    `organizations/live/organizations_live/index.ex`, `organizations/organization_invitation.ex`,
    `organizations/organization_invitation_email.ex`, and
    `organizations/live/invitation_accept_live.ex`. Each row takes one disposition: a
    `test/example/` counterpart path with the sentences converged into it by this plan, or an
    explicit no-mirror-needed disposition with its reason. Two of the eight take the latter and each
    must be asserted by a grep recorded in the row, never by assertion alone:
      - `core/sigra_auth.css` → **no mirror needed**: `test/example/priv/static/assets/sigra_auth.css`
        is a different, shorter file that does not contain the two swept comment blocks. Assert with
        `grep -c 'min-width: 0' test/example/priv/static/assets/sigra_auth.css` and a zero count for
        the closure's removed literals (`30518012012|30518015684|re-litigate`) in that file, both
        recorded — the example copy has nothing to converge, so a "mirrored" claim here would be a
        phantom edit.
      - `organizations/organization_invitation_email.ex` → **no counterpart** per D-22, restated for
        the closure batch. Assert with a recorded search showing no `test/example/` file carries the
        IN-03(a) phishing parenthetical to converge.
    The remaining six rows name the counterpart this plan actually edited or confirmed, reusing the
    same "record the confirmed disposition, never a phantom edit" rule already applied to
    `mfa_settings_live.ex` and `accounts/organization_invitation.ex` in Task 2.
  * Add a dated header note stating the sha the checklist is now accurate against, and stating that
    it was originally frozen against `cafd9a53` and went stale when the router sweep landed in
    `6fa3ead2` / `e4e03980`. Do not delete the original framing — record the correction.

Add a `## CLOSURE-MIRROR-COMMIT` slot to `239-EVIDENCE.md` (bare `^## [A-Z0-9-]+$` heading,
`Status:` line beneath) holding: the V2 count on the two edited example files (expected 0); the
paired positive control — the V2 count on the IN-05 example-only files, which must be non-zero and
which this plan deliberately does not sweep; the per-sentence convergence table from Task 2; and
`git diff --name-only origin/main -- .github/` recorded as empty.

Commit path-scoped to `test/example` plus the phase directory:
`refactor(239): mirror the router and layouts sweep into test/example and correct the SC-4 checklist (SURF-03)`.
Assert afterwards that `git show --name-only --format= HEAD` lists nothing outside those two paths.
  </action>
  <verify>
    <automated>bash -lc 'git show --name-only --format= HEAD | grep -vE "^(test/example/|\.planning/phases/239-)" | wc -l | grep -qx 0 && grep -q "features/core.ex" .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md && echo CHECKLIST_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `239-MIRROR-CHECKLIST.md` contains no remaining `no counterpart (delivered by injection)` disposition for `organizations/router_injection.ex`; its row names `test/example/lib/example_web/router.ex` and lists five mirrored sites.
    - `grep -c 'lib/sigra/install/features/core.ex' 239-MIRROR-CHECKLIST.md` → `>= 1`.
    - `grep -c 'components/layouts.ex' 239-MIRROR-CHECKLIST.md` → `>= 1`, and that row records both the line-9 comment and the line-52 `attr doc:` string with the ExDoc/LSP note.
    - The checklist contains a `## Closure batch (plans 239-05 / 239-06)` table with **exactly eight** rows, one per closure-edited template, each carrying exactly one disposition; `grep -c 'login_html.ex' 239-MIRROR-CHECKLIST.md` → `>= 1` and the same for the other seven template basenames.
    - The `core/sigra_auth.css` closure row reads as a no-mirror-needed disposition and records the two proving greps: a non-zero `grep -c 'min-width: 0' test/example/priv/static/assets/sigra_auth.css` and a zero `grep -cE '30518012012|30518015684|re-litigate'` on the same file. The row does not claim a mirrored edit, and `test/example/priv/static/assets/sigra_auth.css` does not appear in `git diff --name-only` for this commit.
    - The `organizations/organization_invitation_email.ex` closure row reads as a no-counterpart disposition citing D-22 and records the search that confirms no `test/example/` file carries the IN-03(a) phishing parenthetical.
    - The checklist header note names the sha it is accurate against and names `cafd9a53`, `6fa3ead2` and `e4e03980` — the staleness is recorded, not erased.
    - `239-EVIDENCE.md` contains a `^## CLOSURE-MIRROR-COMMIT$` heading with a `Status:` line beneath it.
    - Recorded in that slot: V2 over the two edited example files → `0`, AND V2 over the IN-05 example-only files → non-zero. A zero on both would mean the grep is dead and the first number proves nothing.
    - `git diff --name-only origin/main -- .github/` → empty, recorded verbatim.
    - `git show --name-only --format= HEAD` lists only paths under `test/example/` and `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/`.
    - `git status --porcelain` is clean after the commit.
  </acceptance_criteria>
  <done>The SC-4 checklist is accurate at the final HEAD, records why it went stale, and the mirror commit is provably confined to `test/example/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/example/` → browser/integration suites | The demo app is what the Playwright and example suites exercise; a behaviour change disguised as a comment edit would silently move what those suites assert. |
| `attr … doc:` string → ExDoc / LSP hover | Unlike a comment, this string is rendered to readers of the generated docs, so it is user-visible text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-07-01 | Tampering | A route, pipeline, or plug altered under cover of a comment mirror in `router.ex` | high | mitigate | Diff inspected for non-comment changed lines; `mix compile --warnings-as-errors` clean; the full `MIX_ENV=test mix ci` gate runs in plan 239-08 over this commit. |
| T-239-07-02 | Tampering | `attr` name or `default:` changed while editing the `doc:` string | high | mitigate | Acceptance criterion asserts the exact `doc:` literal and a non-zero `attr :` positive control; diff limited to the string body. |
| T-239-07-03 | Repudiation | A corrected checklist that quietly overwrites its own stale history | medium | mitigate | Header note records the original freeze sha and the two commits that made it stale, rather than silently restating. |
| T-239-07-04 | Information disclosure | Bookkeeping remaining in an ExDoc-rendered `doc:` string | medium | mitigate | Line 52 swept and asserted by exact-literal grep. |
| T-239-07-05 | Denial of service | `.github/` `name:` drift breaking required status contexts | high | mitigate | Path-scoped commit plus asserted-empty `.github/` diff (D-23). |
| T-239-07-06 | Tampering | The IN-03(b) `:mismatch` security-rationale sentence deleted or weakened while being converged into `test/example/lib/example_web/live/invitation_accept_live.ex` | high | mitigate | Mirrors `T-239-06-02`: the sentence is converged from the template, never re-derived; acceptance asserts the example carries the same "asserted by a test — do not add accept controls here" sentence as the template AND that `grep -c 'ZERO .phx-click'` still returns `1`, so a thinning edit fails the criterion. `239-REVIEW.md` IN-03 records that this exact weakening already happened once in this phase. Phase-level backstop: plan 239-08 runs `237-security-comment-diff-check.sh` over the full closure diff (D-16 pins that script — run, never edit). |
</threat_model>

## Artifacts this phase produces (plan 239-07)

| Artifact | New or modified | What it is |
|---|---|---|
| `test/example/lib/example_web/router.ex` | modified | Five swept comment sites brought to the golden tree's wording. |
| `test/example/lib/example_web/components/layouts.ex` | modified | Line-9 comment and line-52 ExDoc-rendered `attr doc:` string swept. |
| `test/example/lib/example_web/controllers/session_html.ex` | modified | Converged onto the template's `during UAT`-free moduledoc. |
| `test/example/lib/example_web/live/organization_members_live.ex` | modified | Converged onto the template's present-tense seam paragraph; pointer and stray adverb removed. |
| `test/example/lib/example_web/live/organizations_live/index.ex` | modified | Converged onto the template's Branch B line. |
| `test/example/lib/example_web/live/invitation_accept_live.ex` | modified | Converged onto the template's IN-03(b) invariant note. |
| `239-MIRROR-CHECKLIST.md` | modified | Row 79 disposition corrected; rows added for `lib/sigra/install/features/core.ex` and `layouts.ex`; a new eight-row `## Closure batch (plans 239-05 / 239-06)` table giving every closure-edited template a per-file disposition, two of them grep-asserted no-mirror-needed; staleness recorded with shas. |
| `239-EVIDENCE.md` § `## CLOSURE-MIRROR-COMMIT` | new slot | V2 measurement with live positive control, per-sentence convergence table, `.github/` empty-diff proof. |

<verification>
- V2 → 0 on the two edited example files, non-zero on the deliberately unswept IN-05 files.
- Every converged sentence reads `MATCH` after namespace normalization.
- The checklist names a counterpart for `router_injection.ex`, carries rows for
  `features/core.ex` and `layouts.ex`, and records its own staleness.
- The commit lists nothing outside `test/example/` and the phase directory.
</verification>

<success_criteria>
The demo app and the generator agree on every sentence this phase touched, and the SC-4 checklist is
a record a reader can trust rather than a snapshot of a tree that has since moved.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-07-SUMMARY.md` when done.
Record: the seven mirrored sites, the per-sentence convergence verdicts, the two confirmed no-edit
dispositions, the V2 numbers with the positive control, and the three checklist corrections.
</output>
