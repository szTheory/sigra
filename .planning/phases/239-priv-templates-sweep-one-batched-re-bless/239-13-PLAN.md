---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 13
type: execute
wave: 13
depends_on: ["239-12"]
files_modified:
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
  # Split out of plan 239-12 at planning time rather than mid-run. Every live external observation in
  # the closure lands here: Postgres + a scaffolded app, a built tarball, the full `mix ci` gate,
  # SC-5 at two diff bases, and the SURF-03 re-check. None of it is inheritable from a SUMMARY.

must_haves:
  truths:
    - "All three tiers are clean under V3 at final HEAD: `239-v3-vocabulary-check.sh` exits 0 with `hits_outside_allowlist=0` and a non-zero `control_defmodule=` on `priv-templates`, the scoped `example` counterpart set, AND `golden`. Each was demonstrated RED in plan 239-09 with the same definition, the same tier file lists and the same allowlist (D-30), so the RED/GREEN pair is complete per tier."
    - "The raw `hits=` total is recorded per tier alongside the outside-allowlist zero, and the `example` tier's result is recorded WITH its scoping — it is a claim about SC-4's mirrored-counterpart surface, never about all of `test/example/`, whose measured remainder plan 239-09 routed to Phase 241 SURF-04."
    - "The SC-1 goal is falsifiable independently of every regex in this phase: fixed-string `grep -rnF` for each of the two removed sentences returns 0 over a FRESHLY GENERATED app from `scripts/ci/install-smoke.sh`, paired with a non-zero `defmodule` control on that same generated tree."
    - "An extracted `mix hex.build` tarball returns 0 `.planning/` hits under `lib/` and `priv/` — SC-2 as amended by D-27 — with the deliberate out-of-scope hits in `docs/`, `README.md` and `CHANGELOG.md` enumerated by per-file count rather than claimed absent, and their Phase 241 SURF-04 owner named."
    - "`MIX_ENV=test mix ci` reports 0 failures at the final committed HEAD on a clean tree (Standing Constraints 2 and 3)."
    - "SC-5 is re-proven after the closure, not assumed to have survived it: `git diff --name-only <chosen-base> -- .github/` is empty and no `name:` line changed. The choice of base is stated with its reason, given that `origin/main` is 37 commits behind local `main`."
    - "The `install_golden_contract` GitHub Actions clause remains a recorded ship-time deferral in `239-EVIDENCE.md` § `## HONEST-CLAIMS`, not upgraded from a local proxy — nothing is pushed and no Actions verdict exists to read."
    - "SURF-03 is re-checked to `[x]` with its roll-up row returned to Complete ONLY after every truth above is observed — and the re-check is the last commit of the plan and of the closure, so no intermediate commit anywhere in plans 239-09 … 239-13 carries a checkbox that does not hold."
    - "The instrument and the allowlist are byte-unchanged by this plan: neither `239-v3-vocabulary-check.sh` nor `239-v3-allowlist.tsv` appears in any commit here. The closure's final green comes from the surface, never from the measurement."
    - { statement: "Every V3 hit that disappeared between plan 239-09's RED and this plan's GREEN disappeared because the text was removed, not because an alternation stopped matching — checked by re-running the per-alternation positive controls at final HEAD and confirming each still fires on its named surface", verification: backstop }
  artifacts:
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "`scripts/ci/install-smoke.sh` is the SC-1 vehicle (D-15) — a real `mix phx.new` + `mix sigra.install` left on disk. Grep it in place; the golden fixture is a committed snapshot and grepping it instead would be the source tree wearing a disguise."
    - "The golden tree is admissible as generated-app evidence ONLY because plan 239-12's `--check` exited 0. That exit code is re-confirmed here at final HEAD rather than cited, because a commit landed after it."
    - "V3 is an instrument this phase put on trial. The closure's central claim therefore cannot rest on V3 alone — the fixed-string grep on a freshly generated app is the one criterion that is falsifiable without it, and it is the criterion that matters most."
    - "SURF-03's checkbox and its roll-up row near `.planning/REQUIREMENTS.md:158` must move together. A checked box with a Pending roll-up row (or the reverse) is the same false-complete defect the SURF-02 todo already records."
  prohibitions:
    - statement: "Must NOT inherit any observation from a prior SUMMARY. Every number in this plan is produced by a command run at the final committed HEAD on a clean tree, and the command is recorded next to its output."
    - statement: "Must NOT re-check SURF-03 before every observation it asserts has been made. A checkbox flipped on expectation rather than observation is the SURF-02 false-complete defect this phase already filed a todo about."
    - statement: "Must NOT claim `install_golden_contract` is green from GitHub Actions. This phase pushes nothing; the Actions clause stays a recorded ship-time deferral."
    - statement: "Must NOT fix source to chase a build artifact. A red `mix ci` from a stale `_build` is diagnosed as a build artifact and recovered by forcing recompilation, with zero source changes; a source edit made to turn such a red green is prohibited."
    - statement: "Must NOT leave `sigra-*.tar` or an unpacked `sigra-*/` behind after the tarball check (REPO-01)."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23) — a renamed required status context never reports and every PR hangs forever."
    - statement: "Must NOT edit `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv`, `239-comment-only-diff-check.sh`, or `237-security-comment-diff-check.sh` (D-16, D-30). If an instrument reports red at final HEAD, that is a finding to report, not a file to adjust."
    - statement: "Must NOT report the `example` tier's zero as a statement about all of `test/example/`. The tier is scoped to SC-4's counterpart set by D-30, and the record must say so wherever the number appears."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): the dead-instrument case. Every zero in this plan is paired with a positive control on the same surface, the per-alternation controls are re-run at final HEAD so a zero cannot come from an alternation that quietly stopped matching, and the instrument distinguishes exit 1 (surface dirty) from exit 3 (instrument cannot answer). The probe row itself is not auto-resolved by those mitigations."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `empty`, UNRESOLVED): an empty file list, an empty generated app, or an empty unpacked tarball would each make a check trivially pass. Every zero here is paired with a same-surface positive control and the instrument fails closed on an empty tier. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `ordering`, UNRESOLVED): 'when elements compare equal, is output order specified and stable?' Read here as the evidence-then-checkbox ordering: the record is committed before the flip, and the flip is last and alone. Asserted by commit-scope checks, not by tooling; the probe row stays unresolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `adjacency`, UNRESOLVED): the three-tier equality question. `--check` exit 0 is the only mechanism that proves template and golden are equal-by-generation; template↔example equality has no mechanism at all (FUT-01, filed and unbuilt). Surfaced, not resolved."
---

<assumption_delta_decision>
**Outcome: `no-change`.** The `pluralization`/`second` signal is the D-26 batch-count amendment prose;
no identity-model consequence. Its residue — an unbounded per-batch re-bless count — is bounded by
**D-29**, discharged for batch 3 in plan 239-12. This plan records the resulting count in the closure
outcome so the number stays visible.
</assumption_delta_decision>

<objective>
Re-prove every criterion this closure touched, live, at the final committed HEAD — then and only then
re-check SURF-03.

Purpose: plans 239-09 … 239-12 built an instrument, amended two criteria, edited two templates,
mirrored them and re-blessed the fixture. Every one of those steps produced a claim. This plan is
where the claims are re-observed rather than inherited: three tiers clean under the instrument that
was demonstrated RED on each, the goal falsified independently of every regex under examination on a
freshly generated app, an extracted tarball measured under the amended D-27 scope with its
out-of-scope hits enumerated and owned, a green `MIX_ENV=test mix ci`, and SC-5 re-proven at two
deliberately chosen diff bases.

This is the only plan in the closure that needs live Postgres, a scaffolded Phoenix app, and a built
Hex tarball. It was split out of plan 239-12 at planning time because those observations are the
expensive, fragile half of the closure and discovering the split mid-run is strictly worse than
declaring it — the observation most likely to be cited instead of made is the last one in a long
context.

Output: a `## BATCH-3-CLOSURE-OUTCOME` record carrying every command and its output; an extended
`## HONEST-CLAIMS` block; and an honest SURF-03 checkbox, flipped last and alone.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-12-SUMMARY.md
@scripts/ci/install-smoke.sh
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **Every observation is live, at the final committed HEAD, on a clean tree** (Standing Constraint
   2). Nothing is inherited from a SUMMARY.
2. **A zero-match grep must be a measurement, not a fatal status.** Under `set -e`, a `grep` that
   matches nothing aborts the shell and yields a confident false negative. Brace-group every counting
   grep with `|| true` inside the command substitution. This is the exact shape of every "returns
   zero hits" assertion in this plan.
3. **Every zero is paired with a positive control on the same surface** (Standing Constraint 1).
4. **`MIX_ENV=test mix ci`, never root `mix test`** (Standing Constraint 3) — root `mix test` misses
   formatting and `test/example`.
5. **`.github/` is read-only** (D-23), and so is every measurement instrument (D-16, D-30).
6. **The repo is public.** No home-directory paths, adopter PII, or brand hex values in any artifact.
   The generated app and unpacked tarball live under tmp paths — record repo-relative paths and
   basenames, not absolute home paths.
7. **Do not push.** Commit only, ending messages with
   `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

### Environment preconditions — the last waves hit every one of these

- **Postgres must be live.** `scripts/db/up.sh` then `source tmp/db.env`. A missing database fails
  fast rather than skipping. `scripts/ci/install-smoke.sh` requires Postgres at the configured host
  with user/password `postgres`.
- **The phx_new 1.8.8 archive must be installed locally** or golden diffs are spurious:
  `mix archive.install --force hex phx_new 1.8.8`.
- **A stale `_build` masquerades as a real failure.** Plan 239-08's first `MIX_ENV=test mix ci` was
  RED with six `Sigra.Audit.Forwarders.ThreadlineTest` failures reporting `Threadline.attach/1 is
  undefined`, caused by a local `_build` holding a sigra beam compiled before the `:threadline` dep
  existed. The fix was `mix deps.compile threadline --force && mix compile --force`, with **zero
  source changes**. Diagnose before editing anything.
- **`mix phx.new` rejects hyphens in app names.** Use underscores in any scaffolded tmp app path.
- **asdf finds no `.tool-versions` under `/tmp`**, so a script run there fails with
  `No version is set for command mix`. Prefix with
  `ASDF_ERLANG_VERSION=28.5 ASDF_ELIXIR_VERSION=1.19.5-otp-28`.
- **`gsd-tools` is not on PATH** — invoke `node ~/.claude/gsd-core/bin/gsd-tools.cjs`.

<tasks>

<task type="auto">
  <name>Task 1: Three-tier V3 at final HEAD with live per-alternation controls, then the fixed-string proof on a freshly generated app</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>Plan 239-12's re-bless commit is HEAD, `git status --porcelain` is empty, Postgres is live (`scripts/db/up.sh` then `source tmp/db.env`), and the phx_new 1.8.8 archive is installed. `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 0 here; an exit 2 means a commit landed after the re-bless that changed generated output, and this plan must not build on it.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## VOCABULARY-LEDGER` — V3 verbatim, the three tier file lists as fixed by plan 239-09, the allowlist and its reason, and the per-alternation positive-control table whose every row must be re-run here. Read § `## CLOSURE-OUTCOME` for the record format this task's section matches.
    - `scripts/ci/install-smoke.sh` lines 1-30 — how it scaffolds `mix phx.new` + local-dep `mix sigra.install` into `${TMP_APP_DIR:-/tmp/tmp_app}`, that it compiles `--warnings-as-errors`, and its Postgres requirement. This is the SC-1 vehicle (D-15); it leaves the generated app on disk, which is what makes an in-place grep possible.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the SC-1 `gaps:` block, which names both removed sentences verbatim. Take the fixed strings from there, not from memory.
    - `.planning/ROADMAP.md` — the Phase 239 SC-1 bullet **as amended by plan 239-10**. Measure against the amended text, including its stated measured form (zero hits outside the committed allowlist over the frozen tier file lists), not the text quoted in older artifacts.
  </read_first>
  <action>
**Three-tier V3 at final HEAD.** Run `239-v3-vocabulary-check.sh` against `priv-templates`, `example`
and `golden`. Each must exit 0 with `hits_outside_allowlist=0` and a non-zero `control_defmodule=`.
Record each tier's raw `hits=` and `allowlisted=` alongside — `priv-templates` is expected to keep a
raw total of `1`, the allowlisted SVG-coordinate false positive dispositioned by name in plan
239-09's ledger. An exit 3 on any tier means the instrument cannot answer: halt and report, never
read it as a result. Do not edit the instrument or the allowlist under any circumstance (D-30).

Record the `example` tier's result **with its scope stated in the same sentence**: it is a claim
about SC-4's two mirrored-counterpart files, not about all of `test/example/`. Restate the measured
remainder figure from `## VOCABULARY-LEDGER` and name its owner (Phase 241 SURF-04, via the todo plan
239-09 filed). A number recorded without its scope is how a narrow measurement becomes a broad claim.

**Per-alternation controls, re-run live.** Re-run every per-alternation positive control recorded in
`## VOCABULARY-LEDGER` at this HEAD and confirm each still fires on its named surface with a non-zero
count. This is the check that tells a real clean tier apart from an alternation that quietly stopped
matching — without it, the three zeros above are unfalsifiable. Any alternation now returning zero is
reported as an instrument regression in the record, not silently accepted.

**Fixed-string check on a freshly generated app.** Boot Postgres, run `scripts/ci/install-smoke.sh`,
and grep the generated app **in place** — not the golden fixture, which is a committed snapshot, i.e.
the source tree wearing a disguise (D-15). For each of the two sentences removed in plan 239-11, run a
fixed-string `grep -rnF` over the generated app's `lib/` and `priv/` and require 0, paired with a
non-zero `defmodule` control on that same generated tree. This is the criterion that is falsifiable
independently of V1, V2 and V3 — all three are instruments under examination in this phase, and a goal
that can only be falsified by the instrument validating it is not falsifiable. Also run V3 over the
generated app's `lib/` and `priv/` and record the result with its control. Use underscores in the tmp
app name (`mix phx.new` rejects hyphens) and prefix with
`ASDF_ERLANG_VERSION=28.5 ASDF_ELIXIR_VERSION=1.19.5-otp-28` if the script runs under `/tmp`, where
asdf finds no `.tool-versions`.

Hold all of this for Task 3's record; write nothing to `239-EVIDENCE.md` until then, so the evidence
section lands as one coherent commit rather than in fragments. Keep every command and its full output
verbatim for that write-up — a paraphrased number is an inherited number.

No commit in this task.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; s="$d/239-v3-vocabulary-check.sh"; test -x "$s" || { echo "MISSING_INSTRUMENT"; exit 1; }; test -s "$d/239-v3-allowlist.tsv" || { echo "MISSING_ALLOWLIST"; exit 1; }; git diff --quiet || { echo "DIRTY_TREE"; exit 1; }; for t in priv-templates example golden; do out=$("$s" "$t" 2>&1) || { echo "TIER_DIRTY: $t"; printf "%s\n" "$out"; exit 1; }; printf "%s\n" "$out"; printf "%s\n" "$out" | grep -q "hits_outside_allowlist=0" || { echo "NO_ZERO: $t"; exit 1; }; printf "%s\n" "$out" | grep -qE "control_defmodule=[1-9]" || { echo "CONTROL_DEAD: $t"; exit 1; }; done; echo THREE_TIERS_CLEAN_WITH_CONTROLS'</automated>
  </verify>
  <acceptance_criteria>
    - `239-v3-vocabulary-check.sh` exits `0` with `hits_outside_allowlist=0` and `control_defmodule` `>= 1` on each of `priv-templates`, `example`, and `golden`. All three were demonstrated RED in plan 239-09 under the same definition, tier file lists and allowlist; the RED/GREEN pair is recorded per tier with its raw `hits=` and `allowlisted=` counts.
    - The `example` tier's zero is recorded together with its D-30 scoping and the measured `test/example/` remainder figure, with Phase 241 SURF-04 named as the remainder's owner. A record of that zero without its scope fails this criterion.
    - Every per-alternation positive control from `## VOCABULARY-LEDGER` is re-run at final HEAD and returns a non-zero count on its named surface. Any alternation that now returns zero is reported as an instrument regression, not silently accepted.
    - **Generated-app fixed-string proof.** `scripts/ci/install-smoke.sh` produced a generated app on disk; for each of the two removed sentences, `grep -rnF` over that app's `lib/` and `priv/` returns `0`, paired with a `defmodule` control returning `>= 1` on the same generated tree. The generated app path (as a relative or tmp basename, not an absolute home path), all four numbers, and the exact commands are captured. This criterion does not reference V1, V2 or V3.
    - V3 over the generated app's `lib/` and `priv/` returns `hits_outside_allowlist=0` with a non-zero control, captured.
    - No instrument was modified: `git status --porcelain` is empty at the end of this task and `239-v3-vocabulary-check.sh` and `239-v3-allowlist.tsv` are byte-unchanged.
  </acceptance_criteria>
  <done>The closure's central claim is proven twice over at final HEAD — once by the widened instrument on all three tiers with its per-alternation controls re-fired live, and once by a fixed-string grep on a freshly generated app that depends on no regex this phase put on trial.</done>
  <reversibility rating="reversible">Measurement only; no file is written and no commit is made. Rated reversible, so no `checkpoint:decision` is emitted.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The tarball under the amended D-27 scope, the full gate, and SC-5 re-proven at two diff bases</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>Task 1's observations are complete and held, `git status --porcelain` is empty, and no commit has landed since Task 1's measurements — otherwise those measurements are no longer at final HEAD and must be re-run.</precondition>
  <read_first>
    - `mix.exs` around `:161-163` (the `ci.install_golden` alias members) and `:184` (the Hex `files:` list) — needed for the tarball measurement's scope and for knowing which alias members need a database.
    - `.planning/ROADMAP.md` — the Phase 239 SC-2 and SC-5 bullets **as amended by plan 239-10**. SC-2's scope is `lib/` and `priv/` under D-27; measure against that text, not the older unqualified wording.
    - `.planning/todos/pending/2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md` — filed by plan 239-10, carrying the 58-reference per-file breakdown. This task re-measures rather than citing it, and reconciles any difference explicitly.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the SC-5 row, which states `git diff origin/main --stat -- .github/` returned 0 lines at the prior HEAD; and the note that `origin/main` is 37 commits behind local `main`, which is why this task must choose and justify its diff base.
  </read_first>
  <action>
**Tarball under the D-27 scope.** Run `mix hex.build --unpack`, then grep the unpacked tree for
`.planning/` under `lib/` and `priv/` and require 0 with a positive control on that same unpacked
tree. Separately measure and **enumerate** the out-of-scope hits by file — `CHANGELOG.md`,
`README.md`, and each `docs/` file — with per-file counts and the total, and name their owner (Phase
241 SURF-04, plus the todo filed in plan 239-10). SC-2 as amended by D-27 is satisfied by the
`lib/`+`priv/` zero; the enumeration is what keeps it from over-claiming. If the re-measured total
differs from the 58 recorded in plan 239-10's todo, say so and reconcile it rather than quietly
adopting the new number. Delete `sigra-*.tar` and any unpacked `sigra-*/` immediately afterwards
(REPO-01) and confirm `git status --porcelain` is empty.

**The gate.** Run `MIX_ENV=test mix ci` — never root `mix test`, which misses formatting and
`test/example`. If it comes back RED, **diagnose before editing anything**. A stale `_build` holding a
sigra beam compiled before a dep existed produces `undefined function` failures that look exactly like
a real regression; plan 239-08 hit precisely this and the fix was
`mix deps.compile threadline --force && mix compile --force` with zero source changes. Recover the
build and re-run before considering any source change, and record both runs. A source edit made to
turn such a red green is prohibited.

**SC-5 re-proof, with the base chosen deliberately.** SC-5's text names `origin/main`, but `origin/main`
is 37 commits behind local `main`, so that diff spans far more than this closure. Run **both**: the
literal `git diff --name-only origin/main -- .github/` the criterion names, and a closure-scoped
`git diff --name-only <plan-239-08-head> -- .github/`. Require both empty, and require
`git diff origin/main -- .github/ | grep '^[+-].*name:'` empty. State in the record which base is the
criterion's and which is the closure's, and why both were run — a renamed required status context
never reports and every PR hangs forever, so this is checked at both widths rather than at whichever
is convenient. Also re-run
`.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-security-comment-diff-check.sh`
over the full closure diff and require exit 0 with `examined_removed_lines` > 0, or re-state its single
documented structural survivor as the known instrument limitation — never by editing the script (D-16).

Hold these observations for Task 3's record along with Task 1's. No commit in this task.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; git diff --quiet || { echo "DIRTY_TREE"; exit 1; }; ! ls -d sigra-*.tar sigra-*/ >/dev/null 2>&1 || { echo "TARBALL_RESIDUE"; exit 1; }; ! git diff --name-only origin/main -- .github/ | grep -q . || { echo "GITHUB_TOUCHED"; exit 1; }; ! git diff origin/main -- .github/ | grep -q "^[+-].*name:" || { echo "GITHUB_NAME_LINE_MOVED"; exit 1; }; MIX_ENV=test mix ci && echo GATE_GREEN_GITHUB_UNTOUCHED'</automated>
  </verify>
  <acceptance_criteria>
    - **Tarball.** `mix hex.build --unpack` produced an unpacked tree; `.planning/` under its `lib/` and `priv/` counts `0` with a positive control on that same tree; the out-of-scope hits are enumerated per file with counts and a total, and Phase 241 SURF-04 plus the plan-239-10 todo are named as owners. Any difference from the recorded 58 is stated and reconciled. Afterwards `ls sigra-*.tar sigra-*/ 2>/dev/null` finds nothing and `git status --porcelain` is empty.
    - `MIX_ENV=test mix ci` exits `0` at the final committed HEAD on a clean tree. If a first run was RED, both runs are captured along with the diagnosis, and `git diff --name-only` across the recovery shows zero source changes.
    - **SC-5 at both widths.** `git diff --name-only origin/main -- .github/` is empty, `git diff --name-only <plan-239-08-head> -- .github/` is empty, and `git diff origin/main -- .github/ | grep '^[+-].*name:'` is empty. The record states which base belongs to the criterion and which to the closure, and why both were run.
    - `237-security-comment-diff-check.sh` over the full closure diff exits `0` with `examined_removed_lines` > 0, or its documented survivor is re-stated verbatim; the script file is byte-unchanged (`git diff --name-only` does not list it).
    - No file was modified by this task: `git status --porcelain` is empty and `git log --oneline -1` is unchanged from Task 1's HEAD.
  </acceptance_criteria>
  <done>SC-2 holds under its amended scope with the surface it deliberately excludes enumerated and owned rather than claimed absent; the full gate is green at the final committed HEAD; and `.github/` is proven untouched at both the criterion's base and the closure's, with no `name:` line moved.</done>
  <reversibility rating="reversible">Measurement only; nothing is written and nothing is committed. The one side effect — a built tarball — is deleted in the same task.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Record the closure outcome, extend HONEST-CLAIMS, then re-check SURF-03 last and alone</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md, .planning/REQUIREMENTS.md</files>
  <precondition>Every observation from Tasks 1 and 2 is complete, captured with its command and full output, and was made at the current HEAD on a clean tree. If any observation is missing or was taken at an earlier HEAD, it is re-made before this task writes anything — a record is only worth the freshness of the weakest number in it.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## CLOSURE-OUTCOME` — the format this task's closing record matches — and § `## HONEST-CLAIMS` with its plan-239-08 extension block, the existing deferral records this task extends rather than restates.
    - `.planning/REQUIREMENTS.md` — the SURF-03 item and its roll-up row as left by plan 239-09 Task 1 (`[ ]` and `Pending`), and the exact `Complete` cell format used by other complete rows in that table.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` — the batch count and shas plan 239-12 recorded, to carry into the closure outcome.
  </read_first>
  <action>
Write a `## BATCH-3-CLOSURE-OUTCOME` section in `239-EVIDENCE.md` in the style of
`## CLOSURE-OUTCOME`, carrying every command from Tasks 1 and 2, its full output, and its paired
control — verbatim, not summarized. Include: the three-tier V3 table with raw/allowlisted/outside
counts and the `example` tier's scoping stated inline; the re-fired per-alternation control table; the
generated-app fixed-string proof with all four numbers and the generating commands; the tarball
measurement with its per-file out-of-scope enumeration and owner; the `mix ci` result; the SC-5
two-base re-proof with the reason each base was used; and the security-classifier result. Record the
re-bless commit count for the phase and their shas, carried from `## BATCH-JUSTIFICATION`.

Extend `## HONEST-CLAIMS` — do not restate it — with: the `install_golden_contract` Actions clause
remaining a ship-time deferral because nothing is pushed and no Actions verdict exists to read
(`MIX_ENV=test mix ci` exit 0 and `--check` exit 0 are the honest local substitutes, and they are
real); the tarball `lib/` bookkeeping baseline still deferred to Phase 241's SURF-04 ratchet; the
packaged-docs surface routed by D-27; the `test/example/` remainder outside SC-4's counterpart scope
routed by D-30 with its measured figure; and FUT-01 still filed and unbuilt.

Commit the evidence record first, path-scoped to `239-EVIDENCE.md`, message
`docs(239): batch-3 closure evidence — three-tier V3 clean, generated-app fixed-string proof, tarball, gate, SC-5 (SURF-01, SURF-03)`.

Only after that record is committed, flip SURF-03 back to `- [x]` in `.planning/REQUIREMENTS.md` and
return its roll-up row to `Complete`. Commit that flip **last and alone**, path-scoped to
`.planning/REQUIREMENTS.md`, message `docs(239): re-check SURF-03 — it now holds at HEAD (SURF-03)`,
so no intermediate commit anywhere in this closure carries a checkbox asserting a state that had not
yet been observed. Leave SURF-01 `[x]` and SURF-04 `[ ]` / `Phase 241` / `Pending` untouched.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; e=.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md; r=.planning/REQUIREMENTS.md; test -s "$e" && test -s "$r" && grep -q "^## BATCH-3-CLOSURE-OUTCOME" "$e" && grep -q "^## HONEST-CLAIMS" "$e" && grep -q "install_golden_contract" "$e" && grep -q "^- \[x\] \*\*SURF-03\*\*" "$r" && grep -q "^- \[x\] \*\*SURF-01\*\*" "$r" && grep -q "^- \[ \] \*\*SURF-04\*\*" "$r" && ! grep -qE "^\| SURF-03 \| Phase 239 \| Pending" "$r" && test "$(git show --name-only --format= HEAD)" = ".planning/REQUIREMENTS.md" && git diff --quiet && echo CLOSURE_PROVEN'</automated>
  </verify>
  <acceptance_criteria>
    - `239-EVIDENCE.md` contains `## BATCH-3-CLOSURE-OUTCOME` with every command from Tasks 1 and 2 and its full output, each zero paired with its control, and the `example` tier's scoping stated wherever its number appears.
    - `## HONEST-CLAIMS` gained an extension block and lost nothing — `git diff` shows no `-` line inside the existing `## HONEST-CLAIMS` ranges. The extension names all five deferrals: the Actions clause, the tarball `lib/` baseline, the packaged-docs surface (D-27), the `test/example/` remainder (D-30) with its measured figure, and FUT-01.
    - The Actions deferral is still recorded as deferred: the evidence contains a statement that no `install_golden_contract` Actions verdict was read, and no text anywhere in this plan's commits claims that job is green.
    - `grep -c '^- \[x\] \*\*SURF-03\*\*' .planning/REQUIREMENTS.md` returns `1` and its roll-up row reads `Complete` with no remaining `Pending` cell for SURF-03.
    - SURF-01 is still `[x]` and SURF-04 is still `[ ]` with a `Phase 241` / `Pending` roll-up row — neither is collaterally moved.
    - The SURF-03 flip is the last commit and lists exactly `.planning/REQUIREMENTS.md`: `git show --name-only --format= HEAD` returns that one path. The evidence commit is its parent and lists exactly `239-EVIDENCE.md`.
    - `git status --porcelain` is empty at the end, and nothing was pushed: `git log origin/main..HEAD` is non-empty and no push occurred in this plan.
  </acceptance_criteria>
  <done>Every claim this phase makes is written down with the command that produced it and the control that makes it falsifiable; every deferral the closure leaves behind is named with an owner; and SURF-03 reads `[x]` only because all of it was observed first, in a commit that carries nothing else.</done>
  <reversibility rating="reversible">Evidence records and a checkbox flip. The re-check is revertable in one line if a later reading falsifies an observation, which is exactly why it is committed last and alone.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local proxy -> CI claim | `mix ci` exit 0 is not an Actions verdict. Treating it as one manufactures a green that no one ran. |
| Sigra repo -> adopter download (`mix hex.build`) | The tarball is the artifact an adopter actually receives; what it contains is what they get. |
| Generated app -> phase verdict | `scripts/ci/install-smoke.sh` output is the only local artifact that is a real adopter project rather than a committed snapshot of one. |
| Evidence record -> future reader | A recorded number outlives the run that produced it and is reasoned from as fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-13-01 | Spoofing | zero-result greps | high | mitigate | A zero from an empty file list, a dead alternation, or a `set -e` abort reads identically to a clean surface. Every zero is paired with a same-surface positive control, the per-alternation controls are re-fired at final HEAD, exit 3 (instrument cannot answer) is distinct from exit 1 (surface dirty), and the SC-1 proof is a fixed-string grep that depends on no regex in this phase. |
| T-239-13-02 | Repudiation | `install_golden_contract` Actions clause | high | mitigate | Claiming a CI job green from a local proxy would be a fabricated verdict. Recorded as a ship-time deferral in `## HONEST-CLAIMS`, with `mix ci` exit 0 and `--check` exit 0 named as the honest local substitutes. |
| T-239-13-03 | Spoofing | the `example` tier's zero | high | mitigate | A zero measured over two files could be read as a statement about all of `test/example/`. Every record of it carries its D-30 scoping and the measured remainder figure with its Phase 241 SURF-04 owner; a prohibition forbids the unscoped form. |
| T-239-13-04 | Tampering | `.github/` workflow `name:` lines | critical | mitigate | A renamed required status context never reports and every PR hangs forever (D-23). `.github/` is read-only here and the absence of change is proven at two deliberately chosen diff bases, including a `name:`-line grep. |
| T-239-13-05 | Repudiation | SURF-03 checkbox | high | mitigate | A `[x]` flipped on expectation lets a later reader believe a verification that never happened — the SURF-02 defect this phase already filed. The flip is the last commit, alone, after the evidence commit that carries its justification. |
| T-239-13-06 | Denial of service | stale `_build` misdiagnosed as a regression | medium | mitigate | A red gate from a stale beam invites a source "fix" that breaks working code. The known signature and its zero-source-change recovery are written into this plan, and a source edit made to turn such a red green is prohibited by name. |
| T-239-13-07 | Information disclosure | unpacked tarball, tmp generated app, evidence record | medium | mitigate | Build artifacts left in the tree could be committed (REPO-01). `sigra-*.tar` and `sigra-*/` are deleted immediately and `git status --porcelain` is asserted empty. The repo is public: no home-directory path, adopter PII, or brand hex value is written into any record. |
| T-239-13-08 | Tampering | measurement instruments | high | mitigate | This is the last plan in the closure, where a red instrument creates maximum pressure to adjust it. `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv`, `239-comment-only-diff-check.sh` and `237-security-comment-diff-check.sh` are all read-only here by prohibition (D-16, D-30), with byte-unchanged assertions in the acceptance criteria. |
| T-239-13-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists. `mix archive.install --force hex phx_new 1.8.8` is a version-pinned Hex archive already required by CI (`ci.yml:478,546,627,774`) and installed as an environment precondition rather than a new dependency; RESEARCH.md's Package Legitimacy Gate covers no new package here. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification):

- `239-EVIDENCE.md` § `## BATCH-3-CLOSURE-OUTCOME` (new section)
- The plan-239-13 extension block in `239-EVIDENCE.md` § `## HONEST-CLAIMS` (new block)
- The restored `Complete` roll-up status cell for SURF-03 in `.planning/REQUIREMENTS.md` (changed cell value)

No new module, function, route, script, or configuration key is created by this plan. The generated
app and the unpacked tarball are transient build artifacts, deleted or left outside the repo.
</artifacts_this_phase_produces>

<verification>
- `239-v3-vocabulary-check.sh` exits 0 with `hits_outside_allowlist=0` and a live `control_defmodule` on all three tiers, and every per-alternation control still fires at final HEAD.
- Fixed-string greps for both removed sentences return 0 on a freshly generated app from `scripts/ci/install-smoke.sh` with a live control on that same tree.
- An extracted `mix hex.build` tarball has 0 `.planning/` under `lib/` and `priv/`, with out-of-scope hits enumerated per file and owned by Phase 241 SURF-04.
- `MIX_ENV=test mix ci` exits 0 at the final committed HEAD on a clean tree.
- `.github/` is unchanged at both diff bases and no `name:` line moved.
- `239-EVIDENCE.md` carries `## BATCH-3-CLOSURE-OUTCOME` and an appended `## HONEST-CLAIMS` block naming all five deferrals.
- SURF-03 reads `[x]` with a `Complete` roll-up row, flipped in the last commit of the plan; SURF-01 and SURF-04 are unmoved.
- No measurement instrument is listed in any commit of this plan.
</verification>

<success_criteria>
The phase goal is proven on the bytes an adopter generates by a check that does not depend on any
regex this phase put on trial; all three tiers are clean under the widened instrument with its
per-alternation controls re-fired live; SC-2 holds under its amended scope with its out-of-scope
surface enumerated and owned; the gate is green; SC-5 survives at two diff bases; every deferral the
closure leaves behind is named with an owner; and SURF-03 reads `[x]` only because every one of those
was observed first.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-13-SUMMARY.md` when done.
</output>
