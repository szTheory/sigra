---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 15
type: execute
wave: 15
depends_on: ["239-14"]
files_modified:
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-4.txt
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless4-code-change.diff
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  - test/fixtures/install_golden/tree
  - test/fixtures/install_golden/STDOUT.txt
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 2
  confidence: low
  # Calibration factor 2.0 applied (no local sample set; `confidence: low` is derived from a sample
  # count of zero, not self-rated). Mechanically identical in shape to plan 239-12, which came in at
  # ~7.3k actual on a two-file batch; this batch renders into one golden file. The estimate is held
  # high because the re-bless task scaffolds a fresh app under `--check` and a red `--check` from a
  # stale `_build` or a mismatched `phx_new` archive costs a diagnosis cycle.

must_haves:
  truths:
    - "Batch 4 lands exactly ONE new re-bless commit — `git log --format=%H <batch-4-freeze-sha>..HEAD -- test/fixtures/install_golden | wc -l` returns `1`, and `git show --name-only --format=` for it lists nothing outside `test/fixtures/install_golden/`. This is SC-3 as amended by D-26."
    - "The batch-4 justification required by D-29 is written into `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` BEFORE the re-bless runs, naming which template edit composes batch 4 and why it could not have been folded into batch 3 — provable from the commit graph: `git merge-base --is-ancestor <freeze-sha> <rebless-sha>` exits 0 and the two shas are distinct."
    - "The `BATCH-4-JUSTIFICATION-PENDING` marker plan 239-14 wrote is gone at this plan's final HEAD, so the discharge is greppable rather than asserted: `/usr/bin/grep -c 'BATCH-4-JUSTIFICATION-PENDING' 239-EVIDENCE.md` returns `0`, paired with a non-zero count of a batch-4 justification heading in the same file as its positive control."
    - "The running count of re-bless commits is recorded with every sha, before batch 4 runs: 3 prior (`38c9bd9a`, `265f7195`, `87581665`), 4 after. D-29's auditability obligation is a number in the record, not an inference from `git log`."
    - "A fourth frozen expected-removed set `239-golden-expected-4.txt` exists, generated from the golden tree as committed BEFORE the re-bless, never from the diff it validates, with its generating command in its header line and its `BASE` naming plan 239-14's decision commit — the last commit before the batch-4 template edit."
    - "`239-comment-only-diff-check.sh` run against the re-bless-4 diff with `239-golden-expected-4.txt` prints `nonconforming=0` alongside `changed_lines`, `removed_lines` and `files`, and exits 0 — with `GOLDEN_MIN_FILES` set to the computed distinct-path count of the expected set, so the non-vacuity floor stays real rather than disabled."
    - "The same classifier, run against a committed known-bad fixture derived from the re-bless-4 diff with exactly one code line altered, exits 1 with `nonconforming >= 1` and names the offending line — RED before GREEN is believed (Standing Constraint 6). The classifier is also demonstrated fail-closed live on empty input."
    - "`MIX_ENV=test mix sigra.fixture.rebless_golden --check` prints `OK: fixture is up-to-date (check mode.)` and exits 0 after the re-bless commit on a clean tree — the only local proof that the golden tree equals real generated output, and the exit code every SC-1 claim resting on the golden tree depends on."
    - "The `golden` tier is clean under V3 at this plan's final HEAD: `239-v3-vocabulary-check.sh golden` exits 0 with `hits_outside_allowlist=0` and a non-zero `control_defmodule=`. An exit 3 is an instrument failure and a halt, never a result."
    - "The false installer-test claim is gone from the golden tier specifically — a fixed-string `/usr/bin/grep -rcF` for the retracted clause over `test/fixtures/install_golden/tree` returns 0, paired with a live positive control on the same tree. This is the tier an adopter's bytes are modelled by, and the tier where plan 239-12 blessed the false sentence in."
    - "`239-v3-vocabulary-check.sh` and `239-v3-allowlist.tsv` are byte-unchanged by this plan: `git diff --name-only <plan-base>..HEAD --` those two paths is empty. D-32 chose option 5(b) precisely so this holds without exception."
    - "SURF-03 is NOT re-checked in this plan. It stays `[ ]` with a `Pending` roll-up row through every commit here; plan 239-13, re-run unchanged after this plan, re-checks it last and alone."
    - { statement: "The classifier's green on the re-bless diff came from a diff that genuinely contains only comment and docstring lines, not from a floor set low enough to wave anything through — checked by recording `GOLDEN_MIN_FILES`, the reported `files` count, and the expected set's distinct-path count side by side, with the derivation of the computed value written out rather than the number asserted", verification: backstop }
  artifacts:
    - test/fixtures/install_golden/tree
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-4.txt
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless4-code-change.diff
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  key_links:
    - "`mix sigra.fixture.rebless_golden` calls `File.rm_rf!` and rewrites the tree wholesale (`lib/mix/tasks/sigra.fixture.rebless_golden.ex:92-103`) — it makes drift disappear whether or not the drift was intended. That is why the diff is captured and classified BEFORE anything is staged, and why the expected set is frozen in an earlier commit."
    - "`--check` compares `STDOUT.txt` by byte equality on its own line (`rebless_golden.ex:115-140`), so a `--check` exit 0 independently covers D-13's independent-drift hazard. `STDOUT.txt` is still checked under V3 separately rather than assumed covered by the tree diff."
    - "The golden tree is admissible as generated-app evidence ONLY because `--check` proves it equals a freshly scaffolded install. Without that exit 0, every SC-1 claim resting on the golden tree collapses to a claim about the source tree."
    - "This plan ends at the fixture. `scripts/ci/install-smoke.sh` — the SC-1 vehicle (D-15), a real `mix phx.new` + `mix sigra.install` left on disk — belongs to plan 239-13, along with the tarball, the full gate, SC-5 and the SURF-03 re-check. Do not reach for it here: a committed fixture greped in place of a generated app is exactly the substitution that produced this closure's reopen, and it is likeliest to happen in the plan that already has the fixture open."
    - "Plan 239-13 is re-run **unchanged** after this plan. Its two amendments arrive through D-31 and D-32 in `239-CONTEXT.md`, which its `<context>` block already loads. Nothing in this plan may edit `239-13-PLAN.md` or pre-empt its observations."
  prohibitions:
    - statement: "Must NOT land more than one new re-bless commit for batch 4. Re-runs before commit are fine; commits are what count. If the first captured diff fails the classifier, fix the template in a new commit and re-run — never amend the classification to fit."
    - statement: "Must NOT wave through a non-conforming line in the re-bless diff. A nonconforming line is a stop-the-line event: halt, report, and do not commit. The re-bless task makes drift disappear regardless of whether the drift was intended, so an unexplained line is never a judgment call."
    - statement: "Must NOT use the re-bless as a way to make unexplained drift disappear. If `--check` or the classifier reveals drift this closure did not author, the drift is diagnosed and named in the evidence record before any blessing, even when blessing it would be green."
    - statement: "Must NOT hand-edit `test/fixtures/install_golden/tree/` or `STDOUT.txt` — only `MIX_ENV=test mix sigra.fixture.rebless_golden` writes them (D-09)."
    - statement: "Must NOT derive `239-golden-expected-4.txt` from the re-bless diff — that is circular and makes the classifier unfalsifiable."
    - statement: "Must NOT weaken `239-comment-only-diff-check.sh`'s fail-closed behaviour. The empty-input guard, the empty-expected-set guard, the add-only-hunk class, and the `removed_lines >= T-record-count` floor all stay; only the already-parameterized `GOLDEN_MIN_FILES` value is set, and it is set to the computed count, never to zero."
    - statement: "Must NOT set `GOLDEN_MIN_FILES` to a number carried over from round 3. It is computed from round 4's expected set, and the derivation is written out beside the value. A reused number is indistinguishable from a disabled floor."
    - statement: "Must NOT edit `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv`, `239-comment-only-diff-check.sh`, or `237-security-comment-diff-check.sh` (D-16, D-30). If an instrument reports red, that is a finding to report, not a file to adjust. The golden tier's green must come from the re-bless carrying real edits, never from the measurement moving."
    - statement: "Must NOT run the full `MIX_ENV=test mix ci` gate here. It belongs to plan 239-13 along with every other live external observation; running it here duplicates the closure's most expensive step and consumes the context 239-13 needs."
    - statement: "Must NOT re-check SURF-03 in this plan under any circumstance, including if the re-bless and `--check` both come back green. A checkbox flipped on expectation rather than observation is the SURF-02 false-complete defect this phase already filed a todo about."
    - statement: "Must NOT edit `239-13-PLAN.md`, and must NOT fold any of its live observations into this plan."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23) — a renamed required status context never reports and every PR hangs forever."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `ordering`, UNRESOLVED): 'when elements compare equal, is output order specified and stable?' This plan owns it for batch 4. The D-19 ordering — edit, then mirror, then justify-and-freeze, then bless, each alone in its commit — is asserted by commit-graph checks (`git merge-base --is-ancestor` on the freeze commit versus the re-bless commit, plus per-commit path scoping), not by tooling. The probe row stays unresolved: nothing mechanically enforces the ordering, and a future phase could violate it without a gate firing."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `adjacency`, UNRESOLVED): the three-tier equality question. `--check` exit 0 is the only mechanism that proves template and golden are equal-by-generation; template↔example equality has no mechanism at all (FUT-01, filed and unbuilt). Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): the dead-instrument case. Every zero in this plan is paired with a positive control on the same surface, uses `/usr/bin/grep` explicitly, and the instrument distinguishes 'surface dirty' (exit 1) from 'instrument cannot answer' (exit 3). The per-alternation controls are re-run at final HEAD in plan 239-13, which is where a zero that came from an alternation quietly ceasing to match would be caught. The probe row itself is not auto-resolved by either mitigation."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `empty`, UNRESOLVED): an empty file list, an empty diff, or an empty expected set would each make a check trivially pass. The classifier fails closed on all three and that behaviour is demonstrated live rather than cited. Surfaced, not resolved."
---

<objective>
Carry plan 239-14's moduledoc retraction into the golden fixture with one batched re-bless, justified
under D-29 before it runs and proven comment-only against a set frozen in an earlier commit.

Purpose: the golden fixture is the only local artifact provably byte-identical to a freshly generated
app, and it is the tier where plan 239-12 blessed the false installer-test claim in. Until batch 4
reaches it, the false sentence still ships in the bytes this phase models an adopter's project with.
The re-bless task rewrites the tree wholesale and makes drift disappear whether or not the drift was
intended — so the expected-removed set is frozen in an earlier commit, the classifier is demonstrated
RED on a known-bad fixture before its green is believed, and the diff is classified before anything is
staged.

This plan is deliberately shaped as plan 239-12 was: a justification-and-freeze commit that is a
strict ancestor of a single, isolated re-bless commit. It stops at the fixture. Every live external
observation — Postgres, a scaffolded app, the tarball, `MIX_ENV=test mix ci`, SC-5 and the SURF-03
re-check — belongs to plan **239-13**, which is re-run **unchanged** after this plan. The two
amendments 239-13 needs reach it through D-31 and D-32 in `239-CONTEXT.md`, not through an edit to its
plan file.

Output: one re-bless commit; a discharged `BATCH-4-JUSTIFICATION-PENDING` marker; a RED-then-GREEN
classifier demonstration; `--check` exit 0; a golden tier green under V3; and SURF-03 still honestly
`[ ]`, handed back to plan 239-13.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-14-SUMMARY.md
@lib/mix/tasks/sigra.fixture.rebless_golden.ex
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **Exactly one new re-bless commit** for batch 4 (SC-3 as amended by D-26). Re-runs before commit
   are fine; commits are what count.
2. **`test/fixtures/install_golden/tree/` is never hand-edited** (D-09).
3. **The re-bless task does not stage or commit** (`rebless_golden.ex:19-20`). Stage by path.
4. **A nonconforming classifier line halts the plan.** Do not commit and then explain.
5. **Measurement hygiene is non-negotiable.** Three separate confident false negatives have already
   been caught in this phase. Every counting command must:
   - invoke **`/usr/bin/grep`** explicitly — this repo's interactive shell resolves bare `grep` to a
     `ugrep` wrapper that can silently return zero;
   - iterate file lists as **`git ls-files -z … | xargs -0 …`** — this shell does **not** word-split
     an unquoted `$(git ls-files …)` expansion, so the whole list arrives as one argument and every
     count comes back `0`;
   - carry **no inline `#`** inside a generator command — a round-2 generator commented itself out
     and emitted zero records, which is the specific hazard in this plan's expected-set generator;
   - be brace-grouped with `|| true` inside the command substitution, because under `set -e` a
     zero-match grep aborts the shell and yields a confident false negative.
6. **Every zero is paired with a live positive control on the same surface** (Standing Constraint 1).
   A bare zero is not evidence.
7. **Instrument exit 3 is a fail-closed HALT, never a result.** Exit 1 means the surface is dirty;
   exit 3 means the instrument cannot answer.
8. **Evidence at the final committed HEAD on a clean tree** (Standing Constraint 2).
9. **This plan does NOT run the full gate.** `MIX_ENV=test mix ci` (never root `mix test`, Standing
   Constraint 3) belongs to plan 239-13.
10. **`.github/` is read-only** (D-23), and so is every measurement instrument (D-16, D-30).
11. **`.planning/REQUIREMENTS.md` is untouched.** SURF-03 stays `[ ]`.
12. **The repo is public.** No home-directory paths, adopter PII, or brand hex values in any artifact.
13. **Do not push.** Commit only, ending messages with
    `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

### Environment preconditions — the last waves hit every one of these

- **Postgres must be live.** `scripts/db/up.sh` then `source tmp/db.env`. A missing database fails
  fast rather than skipping; the re-bless task scaffolds a real app under `--check`.
- **The phx_new 1.8.8 archive must be installed locally** or golden diffs are spurious:
  `mix archive.install --force hex phx_new 1.8.8`. A different local archive version produces
  byte-diffs against the committed fixture that are artifacts, not findings.
- **A stale `_build` masquerades as a real failure.** Plan 239-08's first `MIX_ENV=test mix ci` was
  RED with six `Sigra.Audit.Forwarders.ThreadlineTest` failures reporting `Threadline.attach/1 is
  undefined`, caused by a local `_build` holding a sigra beam compiled before the `:threadline` dep
  existed. The fix was `mix deps.compile threadline --force && mix compile --force`, with **zero
  source changes**. The re-bless task compiles too, so the same hazard applies to a red `--check`
  here. Diagnose before editing anything.
- **`mix phx.new` rejects hyphens in app names.** Use underscores in any scaffolded tmp app path.
- **asdf finds no `.tool-versions` under `/tmp`**, so a script run there fails with
  `No version is set for command mix`. Prefix with
  `ASDF_ERLANG_VERSION=28.5 ASDF_ELIXIR_VERSION=1.19.5-otp-28`.
- **`gsd-tools` is not on PATH** — invoke `node ~/.claude/gsd-core/bin/gsd-tools.cjs`.

<tasks>

<task type="auto">
  <name>Task 1: Justify batch 4, freeze the round-4 expected set, and demonstrate the classifier RED — all before any re-bless runs</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-4.txt, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless4-code-change.diff, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>The working tree is clean at plan 239-14's mirror commit, and `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 2 printing `DRIFT DETECTED:` on exactly the one golden file batch 4 renders into — proving batch 4's template edit has not yet reached the golden fixture. An exit 0 here means the template edit never reached generated output, or the fixture was hand-edited: halt and report.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-comment-only-diff-check.sh` — the whole script: the `T:`/`N:` record format, the containment lookup keyed on (path, trimmed text) and never on line number, the three violation classes, the usage and exit-code contract in its header, the four `refusing to report success` fail-closed guards, and the two non-vacuity floors `floor_removed="$expected_t_count"` and `floor_files="${GOLDEN_MIN_FILES:-30}"`.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-3.txt` — its header line records round 3's exact generating command, including its `BASE=` variable, its `T:`/`N:` two-pass structure, and its `awk` de-duplication. Round 4's generator is the same command with `BASE` substituted. Copy it; do not retype it, and do not introduce an inline `#` into it.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless3-code-change.diff` — round 3's known-bad fixture, as the shape template for round 4's: a real diff hunk with exactly one code line altered. Round 3's altered line was a `defp` head.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## REFREEZE-LEDGER-3` (round 3's freeze record and its `git merge-base --is-ancestor` proof format), § `## BATCH-JUSTIFICATION` (the D-29 obligation, the batch-1/2/3 rows, and plan 239-14's `BATCH-4-JUSTIFICATION-PENDING` marker), and § `## VOCABULARY-LEDGER` (V3 verbatim, to be copied into the generator, never retyped).
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-14-SUMMARY.md` — which template edit composes batch 4 and the sha of the decision commit that precedes it, needed for the D-29 justification and for the generator's `BASE`.
    - `lib/mix/tasks/sigra.fixture.rebless_golden.ex` lines 40-140 — what the task writes, that it never stages or commits, and the exact `--check` exit semantics.
  </read_first>
  <!-- planner-discipline-allow: BATCH-4-JUSTIFICATION-PENDING
       The acceptance criteria negative-grep this literal, but the grep is scoped to
       `239-EVIDENCE.md`; naming the marker in the action cannot invalidate a check that never
       reads this plan file, and the executor must know the exact token it is replacing. -->
  <action>
Discharge D-29 first. Fill the batch-4 slot in `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION`, replacing
plan 239-14's `BATCH-4-JUSTIFICATION-PENDING` marker so the discharge is greppable:

  * **Composition.** Name the single template edit that composes batch 4 — the `@moduledoc`
    retraction in `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` — with
    its commit sha from `239-14-SUMMARY.md`, and its `test/example/` mirror commit.
  * **Why not foldable into batch 3.** Batch 3 *is* what made batch 4 necessary: batch 3's own repair
    introduced the claim batch 4 retracts, and the falsifying evidence — the installer's `_test.exs`
    target map — was not consulted until threat row `T-239-12-03` forced the observation to be re-made
    on a real generated app, which happened at plan 239-13's halt, after batch 3 had been blessed. An
    edit that corrects a prior batch cannot, by construction, have been folded into that batch. State
    that plainly rather than reaching for a chronology argument alone.
  * **Running count with shas.** Before batch 4: **3** (`38c9bd9a` batch 1 / 239-04, `265f7195`
    batch 2 / 239-07, `87581665` batch 3 / 239-12). After batch 4: **4**, one per batch, which is SC-3
    as amended by D-26 and bounded by D-29. Record batch 4's own sha as landing in
    § `## REBLESS-COMMIT-4`, since a commit cannot carry its own sha while staying path-scoped.

Then generate `239-golden-expected-4.txt` from the **currently committed** golden tree, before running
any re-bless. Two record classes, as in rounds 1 through 3:

  * `T:` records — every line in the golden tree file list matched by V3 (copied out of
    § `## VOCABULARY-LEDGER`, never retyped).
  * `N:` records — the golden-tree lines corresponding to batch 4's prose retraction that V3 cannot
    match because they are ordinary English. Each `N:` record is located by a **literal anchor** taken
    from `239-14-SUMMARY.md` — never by a blanket radius around a match. Enumerate the anchors in the
    ledger; a record with no listed anchor is not admissible.

Set the generator's `BASE` to plan 239-14's decision commit — the last commit before the batch-4
template edit — and record the sha and the reason for that choice in the header line alongside the
command, as rounds 1 through 3 did. Inspect the generated file: it must be non-empty and its records
must resolve to real lines. An empty or record-less file is a broken generator (the inline-`#` hazard
and the word-splitting hazard both produce exactly that), not a finding that the tree is clean — halt
and repair the command.

Then build the round-4 known-bad fixture. Take the real re-bless-4 diff shape and alter exactly one
**code** line in it — a `def`/`defp` head or a pipeline line, not a comment and not a docstring line —
and commit it as `fixtures/239-golden-rebless4-code-change.diff`. Run the classifier against it with
`239-golden-expected-4.txt` and require exit 1, `nonconforming >= 1`, and the offending line named in
the output. This is the RED half: rounds 1 through 3 all did this, and a green from an unfalsified
instrument is what produced this phase's original gap.

Also demonstrate the empty-input guard live: pipe an empty diff on stdin and require exit 1 with
`refusing to report success` in the output.

Compute the value `GOLDEN_MIN_FILES` will take for round 4: the exact count of distinct paths in
`239-golden-expected-4.txt`. Batch 4 renders into a single golden file, so the computed value will be
small and the default floor of 30 would fail spuriously. **Write out the derivation, not just the
number** — the distinct-path count, the command that produced it, and the statement that it is a real
non-vacuity floor equal to the expected path count rather than a disabled one. If the computed value
coincides with the `GOLDEN_MIN_FILES=1` used for the RED demonstration, say so explicitly and say why
the coincidence is not a reused number: one is the expected set's measured distinct-path count, the
other is a deliberate floor-bypass for a single-hunk fixture, and they are equal here by arithmetic
rather than by carry-over. Record both invocations side by side with their two different jobs, as
plan 239-12 did.

Do not change any line of the classifier — it was already parameterized in plan 239-08.

Commit path-scoped to the three files, message
`chore(239): justify batch 4, freeze the round-4 expected set, and demonstrate the classifier RED`.
This commit must land **before** Task 2's re-bless commit, so that "frozen first" is a property of the
commit graph rather than of recorded prose. Record its sha in a new § `## REFREEZE-LEDGER-4` alongside
the round-3 ledger's format.
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; exp="$d/239-golden-expected-4.txt"; bad="$d/fixtures/239-golden-rebless4-code-change.diff"; cls="$d/239-comment-only-diff-check.sh"; test -s "$exp" || { echo "MISSING_EXPECTED_SET"; exit 1; }; test -s "$bad" || { echo "MISSING_KNOWN_BAD_FIXTURE"; exit 1; }; test -x "$cls" || { echo "MISSING_CLASSIFIER"; exit 1; }; rec=$( { /usr/bin/grep -cE "^(T|N):" "$exp" || true; } ); test "$rec" -ge 1 || { echo "EXPECTED_SET_HAS_NO_RECORDS (a broken generator, not a clean tree)"; exit 1; }; pend=$( { /usr/bin/grep -c "BATCH-4-JUSTIFICATION-PENDING" "$d/239-EVIDENCE.md" || true; } ); test "$pend" = 0 || { echo "MARKER_NOT_DISCHARGED n=$pend"; exit 1; }; ctl=$( { /usr/bin/grep -c "^## BATCH-JUSTIFICATION" "$d/239-EVIDENCE.md" || true; } ); test "$ctl" -ge 1 || { echo "CONTROL_DEAD: the marker grep is not reading a file that has the section"; exit 1; }; guards=$( { /usr/bin/grep -c "refusing to report success" "$cls" || true; } ); test "$guards" -ge 4 || { echo "FAIL_CLOSED_GUARDS_WEAKENED n=$guards"; exit 1; }; /usr/bin/grep -q "floor_removed=\"\$expected_t_count\"" "$cls" || { echo "REMOVED_FLOOR_MISSING"; exit 1; }; chg=$( { git diff --name-only HEAD~1..HEAD -- "$cls" "$d/239-v3-vocabulary-check.sh" "$d/239-v3-allowlist.tsv" || true; } ); test -z "$chg" || { echo "INSTRUMENT_MODIFIED: $chg"; exit 1; }; gold=$( { git show --name-only --format= HEAD | /usr/bin/grep -c "test/fixtures/install_golden/" || true; } ); test "$gold" = 0 || { echo "FREEZE_COMMIT_TOUCHED_FIXTURE n=$gold"; exit 1; }; out=$(GOLDEN_MIN_FILES=1 "$cls" "$bad" "$exp" 2>&1); rc=$?; printf "%s\n" "$out"; test "$rc" = 1 || { echo "CLASSIFIER_NOT_RED rc=$rc"; exit 1; }; printf "%s\n" "$out" | /usr/bin/grep -qE "nonconforming=[1-9]" || { echo "NO_NONCONFORMING_COUNT"; exit 1; }; echo CLASSIFIER_RED_CONFIRMED'</automated>
    <!-- GOLDEN_MIN_FILES=1 is correct HERE and only here. This invocation is the RED demonstration
         against a single-hunk known-bad fixture, whose whole purpose is to prove the classifier can
         fail on a code line; the file-count floor is a non-vacuity guard against a thin REAL diff
         and is irrelevant to a fixture that is deliberately one hunk. Task 2's real run uses the
         value computed from round 4's expected set, and that is the run the acceptance criteria
         gate on. If the two values coincide arithmetically, Task 1's record says so and says why
         that is not a carried-over number. -->
  </verify>
  <acceptance_criteria>
    - `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION`'s batch-4 slot is filled: the `BATCH-4-JUSTIFICATION-PENDING` marker written by plan 239-14 is gone (`/usr/bin/grep -c` returns `0`, paired with a non-zero count of the section heading in the same file as its positive control), the batch-4 template edit is named with its sha, the "could not have been folded into batch 3" reason is stated, and the running count of re-bless commits with their shas is recorded.
    - `239-golden-expected-4.txt` exists, is non-empty, has a `#`-prefixed header line recording its generating command and its `BASE=` sha with the reason for that base, and contains at least one `T:` or `N:` record. A record-less file is a broken generator and a halt, never a finding that the tree is clean.
    - Every `N:` record corresponds to a literal anchor enumerated in § `## REFREEZE-LEDGER-4`; the count of `N:` records equals the count of listed anchors. No blanket-radius records.
    - **The freeze is git-provable.** After Task 2 lands, `git merge-base --is-ancestor <this-commit-sha> <rebless-sha>` exits `0` and the two shas are distinct. Both are recorded in § `## REFREEZE-LEDGER-4`.
    - This commit lists exactly three paths and nothing under `test/fixtures/install_golden/` — `git show --name-only --format= HEAD | /usr/bin/grep -c 'test/fixtures/install_golden/'` returns `0`.
    - **Classifier RED demonstrated:** run against `fixtures/239-golden-rebless4-code-change.diff` with the round-4 expected set, it exits `1`, prints `nonconforming` with a value `>= 1`, and its output names the altered code line. The command and full output are recorded in the ledger.
    - **Fail-closed demonstrated live:** an empty diff piped on stdin makes the classifier exit `1` with `refusing to report success` in its output. Command and output recorded.
    - The classifier's four fail-closed guards survive: `/usr/bin/grep -c 'refusing to report success' 239-comment-only-diff-check.sh` returns `>= 4`, and `floor_removed="$expected_t_count"` is still present verbatim.
    - `git diff --name-only HEAD~1..HEAD --` the classifier, `239-v3-vocabulary-check.sh` and `239-v3-allowlist.tsv` is empty — no instrument is modified in this round.
    - The computed `GOLDEN_MIN_FILES` value is recorded **with its derivation** — the distinct-path count, the command that produced it, and the statement that it is a real floor equal to the expected path count, not `0` and not a number carried from round 3. If it coincides with the RED demonstration's `1`, the record states that the coincidence is arithmetic and explains the two different jobs the two invocations do.
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exited `2` with `DRIFT DETECTED:` at the start of this task, recorded with its output and with the golden path(s) it named.
  </acceptance_criteria>
  <done>Batch 4 is justified in writing before it runs and its pending marker is discharged greppably, a non-circular round-4 expected set is committed ahead of the re-bless so the ordering is readable from `git log`, and the classifier is proven able to fail on a real code change and on empty input before any of its greens are believed.</done>
  <reversibility rating="costly">The round-4 expected set re-bases what "comment-only" means for this batch, and the classification recorded against it is what a later reader compares to. Git-revertable, but the anchor list must be enumerated in the ledger rather than implied, so the derivation survives the revert. Reusing round 3's set is not viable — it contains lines that no longer exist in the tree.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the single batched re-bless for batch 4, classify the diff GREEN, and commit it alone</name>
  <files>test/fixtures/install_golden/tree, test/fixtures/install_golden/STDOUT.txt</files>
  <precondition>Task 1's freeze commit is HEAD, the working tree is clean, `239-golden-expected-4.txt` is committed, Postgres is live (`scripts/db/up.sh` then `source tmp/db.env`), and the phx_new 1.8.8 archive is installed locally (`mix archive.install --force hex phx_new 1.8.8`). A different archive version produces byte-diffs that are artifacts, not findings.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-4.txt` — written in Task 1, including its computed `GOLDEN_MIN_FILES` derivation recorded in the ledger.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## REBLESS-COMMIT-3` — how round 3 captured, classified and committed its diff, and the record format § `## REBLESS-COMMIT-4` follows. Round 4 follows the same sequence.
    - `lib/mix/tasks/sigra.fixture.rebless_golden.ex` lines 40-140 — that the task calls `File.rm_rf!` and rewrites the tree wholesale, that it never stages or commits, and the exact `--check` output strings and exit codes.
  </read_first>
  <action>
Run `MIX_ENV=test mix sigra.fixture.rebless_golden` exactly once, batched — never per-file. Do not
stage anything yet.

Capture the resulting working-tree diff to a file before touching the index:
`git diff -- test/fixtures/install_golden`. Run
`239-comment-only-diff-check.sh <captured-diff> 239-golden-expected-4.txt` with `GOLDEN_MIN_FILES`
set to the value computed in Task 1. Require exit 0 and `nonconforming=0`, and capture the printed
`changed_lines`, `removed_lines`, `files` and the three sub-counters verbatim.

If the classifier reports any nonconforming line, **halt**. Do not commit, do not reclassify, do not
relax the floor. A non-conforming line in a re-bless diff means something changed that this closure
did not author, and the re-bless task will have already erased the evidence of what it was in the
tree — so the captured diff is the only record. Diagnose it from the captured diff, name it in the
evidence ledger, and stop for the operator. Blessing it through is prohibited even when blessing it
would be green.

If the classifier is green, stage by path — `git add test/fixtures/install_golden` and nothing else —
and commit alone, message
`chore(239): re-bless install golden fixture after the batch-4 moduledoc retraction (SURF-03)`. The
commit must contain no other path: not the phase directory, not `.planning/`, not a template. That
isolation is what lets a reviewer tell generated drift from hand edits (D-19).

After the commit, run `MIX_ENV=test mix sigra.fixture.rebless_golden --check` on the clean tree and
require exit 0 with `OK: fixture is up-to-date (check mode.)`. This scaffolds a fresh tmp app and is
the only local proof that the committed golden tree equals real generated output — every SC-1 claim
resting on the golden tree depends on this exit code. Confirm `git diff --quiet` succeeds afterwards:
check mode must write nothing into the working tree.

Separately check `STDOUT.txt` under V3, rather than assuming the tree diff covers it: D-13 records
that installer summary output can drift independently of the tree. Record the count as an explicit
number.

**Then prove the retraction reached the tier that models an adopter's bytes.** A fixed-string
`/usr/bin/grep -rcF` for the retracted clause over `test/fixtures/install_golden/tree` returns `0`,
paired with a live positive control on the same tree — a fixed-string count of a phrase that IS
present in the corrected paragraph, returning non-zero. This is the tier where plan 239-12 blessed the
false sentence in, so a zero here without a control beside it is precisely the shape of evidence this
closure has already been burned by twice.

Finally close the golden tier under V3: run `239-v3-vocabulary-check.sh golden` and require exit 0
with `hits_outside_allowlist=0` and a non-zero `control_defmodule=`. Record the raw `hits=` total
alongside. Do not edit the instrument or the allowlist (D-30); if the tier will not go green after a
`--check`-clean re-bless, something the closure did not author is in the fixture — halt and report. An
exit 3 is an instrument failure, never a result.

Record everything in a new `239-EVIDENCE.md` § `## REBLESS-COMMIT-4`, following round 3's format, and
commit that record separately from the re-bless so the re-bless commit stays path-isolated.
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; s="$d/239-v3-vocabulary-check.sh"; g=test/fixtures/install_golden/tree; test -x "$s" || { echo "MISSING_INSTRUMENT"; exit 1; }; MIX_ENV=test mix sigra.fixture.rebless_golden --check || { echo "CHECK_RED"; exit 1; }; git diff --quiet || { echo "CHECK_MODE_WROTE_TO_TREE"; exit 1; }; ctl=$( { /usr/bin/grep -rcF "by construction, not by convention" "$g" | /usr/bin/grep -v ":0$" | /usr/bin/grep -c "." || true; } ); test "$ctl" -ge 1 || { echo "CONTROL_DEAD: anchor phrase not found in the golden tree, the greps are not reading it"; exit 1; }; new=$( { /usr/bin/grep -rcF "does not inherit that assertion" "$g" | /usr/bin/grep -v ":0$" | /usr/bin/grep -c "." || true; } ); test "$new" -ge 1 || { echo "RETRACTION_NOT_IN_GOLDEN"; exit 1; }; out=$( "$s" golden 2>&1 ); rc=$?; printf "%s\n" "$out"; test "$rc" = 0 || { echo "GOLDEN_TIER_DIRTY rc=$rc (rc=3 means the instrument could not answer, not a dirty surface)"; exit 1; }; printf "%s\n" "$out" | /usr/bin/grep -q "hits_outside_allowlist=0" || { echo "NO_ZERO"; exit 1; }; printf "%s\n" "$out" | /usr/bin/grep -qE "control_defmodule=[1-9]" || { echo "CONTROL_DEAD: golden tier control is zero"; exit 1; }; sr=$( { /usr/bin/grep -c "^- \[ \] \*\*SURF-03\*\*" .planning/REQUIREMENTS.md || true; } ); test "$sr" = 1 || { echo "SURF03_MOVED n=$sr"; exit 1; }; echo REBLESS4_CHECK_CLEAN'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit touches `test/fixtures/install_golden`: `git log --format=%H <freeze-sha>..HEAD -- test/fixtures/install_golden | wc -l` returns `1`.
    - That commit contains nothing else: `git show --name-only --format= <rebless-sha> | /usr/bin/grep -cv '^test/fixtures/install_golden/'` returns `0`.
    - The captured diff was classified BEFORE the commit and the classifier exited `0` with `nonconforming=0`. The full classifier output — `changed_lines`, `removed_lines`, `files`, `nonconforming` and the three sub-counters — is recorded verbatim in the evidence ledger, not summarized.
    - `GOLDEN_MIN_FILES` was set to the Task-1 computed value, and `files` in the classifier output is `>=` that value — the floor was cleared, not disabled. Both numbers and the derivation are recorded.
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits `0` and prints `OK: fixture is up-to-date (check mode.)` on the clean tree after the commit. Output recorded verbatim.
    - `git diff --quiet` succeeds after the `--check` run — check mode wrote nothing into the working tree.
    - **The retraction reached the golden tier:** a fixed-string `/usr/bin/grep -rcF` for the retracted clause over `test/fixtures/install_golden/tree` returns `0`, recorded beside a live non-zero positive control on the same tree. A zero with no adjacent control is inadmissible.
    - The `DO NOT add an accept button here even "for convenience"` imperative and the `Jetstream #907 / CVE-2026-1529` attribution are each present exactly once in the golden counterpart file — the retraction did not collaterally damage the lines that carry the defense.
    - A V3 count over `test/fixtures/install_golden/STDOUT.txt` is recorded as an explicit number (D-13's independent-drift hazard re-checked, not assumed still nil).
    - No file under `test/fixtures/install_golden/` was written by anything other than the re-bless task: no `Edit`/`Write`/`sed` touched that path, confirmed by the commit's provenance being a single task invocation and by the diff classifying clean against a set frozen in an earlier commit.
    - `git merge-base --is-ancestor <freeze-sha> <rebless-sha>` exits `0` and the two shas differ.
    - `239-v3-vocabulary-check.sh golden` exits `0` with `hits_outside_allowlist=0` and a non-zero `control_defmodule=`. The raw `hits=` total is recorded alongside. An exit `3` is an instrument failure, never a result: halt.
    - `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv` and `239-comment-only-diff-check.sh` are byte-unchanged by this plan: `git diff --name-only <freeze-sha>~1..HEAD --` those three paths is empty.
    - SURF-03 is still `[ ]` at this plan's final HEAD: `/usr/bin/grep -c '^- \[ \] \*\*SURF-03\*\*' .planning/REQUIREMENTS.md` returns `1`, and its roll-up row still reads `Pending`. The re-check belongs to plan 239-13.
    - `239-13-PLAN.md` is byte-unchanged: `git diff --name-only <plan-base>..HEAD -- <that path>` is empty. The plan must remain re-runnable exactly as written.
  </acceptance_criteria>
  <done>One batched re-bless commit carries batch 4 into the golden fixture, its diff was proven comment-only against a set frozen in an earlier commit before anything was staged, the commit contains that directory and nothing else, `--check` exit 0 re-establishes the golden tree as admissible generated-app evidence, and the false installer-test claim is provably gone from the tier that models an adopter's bytes.</done>
  <reversibility rating="costly">The re-bless rewrites the fixture tree wholesale and erases whatever drift existed. Git-revertable, but the pre-run state survives only in the captured diff and the frozen expected set — which is why both are committed. Not one-way, so no `checkpoint:decision` is emitted.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template tier -> golden fixture (`mix sigra.fixture.rebless_golden`) | A task that rewrites the tree wholesale and makes drift disappear whether or not it was intended. Anything crossing here becomes the record of "what an adopter generates". |
| Golden fixture -> phase verdict | The fixture is admissible as generated-app evidence only while `--check` exits 0. Without that, every SC-1 claim degrades to a claim about the source tree. |
| Frozen expected set -> classifier verdict | The expected set is what "comment-only" means for this batch. Derived from the diff it validates, it would certify anything. |
| This plan -> plan 239-13's re-run | 239-13 is re-run unchanged. Anything this plan pre-empts, edits, or folds in silently weakens the closure's only live-observation plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-15-01 | Tampering | `mix sigra.fixture.rebless_golden` | critical | mitigate | The task erases drift as a side effect of its purpose. The diff is captured and classified before staging, against an expected set frozen in an earlier commit proven ancestral by `git merge-base --is-ancestor`; any nonconforming line halts the plan with the captured diff as the only surviving record. |
| T-239-15-02 | Spoofing | the comment-only classifier | high | mitigate | A classifier that cannot fail produces a meaningless green. Demonstrated RED on a committed known-bad fixture with one code line altered, and fail-closed on empty input, both live, before its green is believed. |
| T-239-15-03 | Spoofing | a record-less expected set read as a clean tree | high | mitigate | Round 2's generator carried an inline `#` that commented itself out and emitted zero records; the word-splitting hazard produces the same shape. Task 1 requires the generated file to be non-empty with resolvable records and treats an empty result as a broken generator and a halt, never as a finding. |
| T-239-15-04 | Repudiation | the D-29 batch-4 justification | medium | mitigate | A justification written after the fact is indistinguishable from a rationalization. The marker discharge is greppable and the freeze commit is proven a strict ancestor of the re-bless commit, so "justified first" is a property of the commit graph rather than of prose. |
| T-239-15-05 | Spoofing | the golden-tier fixed-string zero | high | mitigate | This is the exact evidence shape that produced the reopen: a zero measured on a surface that could not have contained the thing being counted. The golden-tier retraction check pairs its zero with a live positive control on the same tree, uses `/usr/bin/grep` explicitly, and the plan treats an unpaired zero as inadmissible. |
| T-239-15-06 | Repudiation | `install_golden_contract` Actions clause | high | transfer | Claiming a CI job green from a local proxy would be a fabricated verdict. This plan makes no CI claim at all; the deferral record stays in plan 239-13's `## HONEST-CLAIMS` extension. |
| T-239-15-07 | Tampering | `.github/` workflow `name:` lines | critical | mitigate | A renamed required status context never reports and every PR hangs forever (D-23). `.github/` is read-only here; the re-bless task writes only under `test/fixtures/install_golden/` and the commit-scope criteria prove it. SC-5's re-proof stays plan 239-13's. |
| T-239-15-08 | Denial of service | stale `_build` / wrong `phx_new` archive | medium | mitigate | A `--check` red from a stale beam or a mismatched `phx_new` archive looks exactly like real drift and invites a source "fix" that breaks working code. Both signatures and their zero-source-change recoveries are written into this plan's environment preconditions and must be ruled out before any source change. |
| T-239-15-09 | Information disclosure | `239-EVIDENCE.md`, committed fixtures | medium | mitigate | Public repo. The captured diff, the expected set and the known-bad fixture carry repo-relative paths and repo content only — no home-directory paths, adopter PII, or brand hex values. |
| T-239-15-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists. `mix archive.install --force hex phx_new 1.8.8` is a version-pinned Hex archive already required by CI (`ci.yml:478,546,627,774`) and installed as an environment precondition rather than a new dependency; RESEARCH.md's Package Legitimacy Gate covers no new package here. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification):

- `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-4.txt` (new file)
- `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless4-code-change.diff` (new file)
- The filled batch-4 slot in `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION`, replacing plan 239-14's `BATCH-4-JUSTIFICATION-PENDING` marker
- `239-EVIDENCE.md` § `## REFREEZE-LEDGER-4` (new section)
- `239-EVIDENCE.md` § `## REBLESS-COMMIT-4` (new section)
- The regenerated contents of `test/fixtures/install_golden/tree/` and `STDOUT.txt` (rewritten wholesale by the re-bless task, not hand-authored)

No new module, function, route, script, or configuration key is created.
</artifacts_this_phase_produces>

<verification>
- The `BATCH-4-JUSTIFICATION-PENDING` marker count is `0` at final HEAD, paired with a non-zero section-heading control in the same file.
- Exactly one new commit touches `test/fixtures/install_golden`, contains nothing else, and is a descendant of the freeze commit.
- The classifier exits 1 on the known-bad fixture and on empty input, and exits 0 with `nonconforming=0` on the real diff with the round-4 computed `GOLDEN_MIN_FILES` floor applied and its derivation recorded.
- `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 0 with `OK: fixture is up-to-date (check mode.)` on a clean tree after the re-bless commit, and `git diff --quiet` succeeds afterwards.
- A fixed-string search for the retracted clause over the golden tree returns `0` with a live positive control beside it; the imperative and the attribution each count exactly `1` in the golden counterpart.
- `239-v3-vocabulary-check.sh golden` exits 0 with `hits_outside_allowlist=0` and a live `control_defmodule`; the instrument, the allowlist and the comment-only classifier are byte-unchanged across this plan.
- `git diff --name-only <plan-base>..HEAD -- .github/ .planning/REQUIREMENTS.md .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-13-PLAN.md` is empty.
- SURF-03 still reads `[ ]` with a `Pending` roll-up row — the re-check is plan 239-13's last commit, and 239-13 is re-run unchanged after this plan.
</verification>

<success_criteria>
Batch 4 reaches the golden fixture through one batched, comment-only-proven, isolated re-bless whose
justification and expected set were both frozen in an earlier commit and whose classifier was shown
able to fail first; `--check` exit 0 re-establishes the fixture as admissible generated-app evidence;
the false installer-test claim is provably gone from the tier that models an adopter's bytes, proven
by a fixed-string zero with a live control beside it; the instrument set is byte-unchanged; and
SURF-03 is left honestly unchecked for plan 239-13, which is re-run unchanged with its two amendments
supplied by D-31 and D-32.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-15-SUMMARY.md` when done.
</output>
