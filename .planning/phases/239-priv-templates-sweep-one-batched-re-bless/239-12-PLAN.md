---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 12
type: execute
wave: 12
depends_on: ["239-11"]
files_modified:
  - test/fixtures/install_golden/tree
  - test/fixtures/install_golden/STDOUT.txt
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-3.txt
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless3-code-change.diff
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low
  # Pre-split at the Task 3 boundary rather than discovering the split mid-run. This plan is the
  # freeze and the re-bless — short, mechanical, and bounded by the classifier. Every live external
  # observation, the full `mix ci` gate, SC-5's re-proof and the SURF-03 re-check moved to plan
  # 239-13, which is the only plan that needs Postgres, a scaffolded app and a tarball.

must_haves:
  truths:
    - "Batch 3 lands exactly ONE new re-bless commit — `git log <batch-3-base>..HEAD -- test/fixtures/install_golden` returns exactly one sha, and `git show --name-only` for it lists nothing outside `test/fixtures/install_golden/`. This is SC-3 as amended by D-26."
    - "The batch-3 justification required by D-29 is written into `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` BEFORE the re-bless runs, naming which template edits compose batch 3 and why they could not have been folded into batch 2 — provable from the commit graph, since the justification commit is an ancestor of the re-bless commit and distinct from it."
    - "A third frozen expected-removed set `239-golden-expected-3.txt` exists, generated from the golden tree as committed BEFORE the re-bless, never from the diff it validates, with its generating command in its header line."
    - "`239-comment-only-diff-check.sh` run against the re-bless-3 diff with `239-golden-expected-3.txt` prints `nonconforming=0` alongside `changed_lines`, `removed_lines` and `files`, and exits 0 — with `GOLDEN_MIN_FILES` set to the exact count of distinct paths in the expected set, so the non-vacuity floor stays real rather than disabled."
    - "The same classifier, run against a committed known-bad fixture derived from the re-bless-3 diff with one code line altered, exits 1 with `nonconforming >= 1` and names the offending line — RED before GREEN is believed (Standing Constraint 6)."
    - "`MIX_ENV=test mix sigra.fixture.rebless_golden --check` prints `OK: fixture is up-to-date (check mode.)` and exits 0 after the re-bless commit — the only local proof that the golden tree equals real generated output."
    - "The `golden` tier is clean under V3 at this plan's final HEAD: `239-v3-vocabulary-check.sh golden` exits 0 with `hits_outside_allowlist=0` and a non-zero `control_defmodule=`, completing the RED/GREEN pair plan 239-09 opened on this tier. The remaining two tiers were closed in plan 239-11; the three-tier re-confirmation at final HEAD belongs to plan 239-13."
    - "SURF-03 is NOT re-checked in this plan. It stays `[ ]` with a `Pending` roll-up row through every commit here; plan 239-13 re-checks it, last and alone, after the live observations."
    - { statement: "The classifier's green on the re-bless diff came from a diff that genuinely contains only comment lines, not from a floor set low enough to wave anything through — checked by recording `GOLDEN_MIN_FILES`, the reported `files` count, and the expected set's distinct-path count side by side", verification: backstop }
  artifacts:
    - test/fixtures/install_golden/tree
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-3.txt
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless3-code-change.diff
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  key_links:
    - "`mix sigra.fixture.rebless_golden` calls `File.rm_rf!` and rewrites the tree wholesale (`lib/mix/tasks/sigra.fixture.rebless_golden.ex:92-103`) — it makes drift disappear whether or not the drift was intended. That is exactly why the diff is captured and classified BEFORE anything is committed, and why the expected set is frozen in an earlier commit."
    - "`--check` compares `STDOUT.txt` by byte equality on its own line (`rebless_golden.ex:115-140`), so a `--check` exit 0 independently covers D-13's independent-drift hazard."
    - "The golden tree is admissible as generated-app evidence ONLY because `--check` proves it equals a freshly scaffolded install. Without that exit 0, every SC-1 claim resting on the golden tree collapses to a claim about the source tree."
    - "This plan ends at the fixture. `scripts/ci/install-smoke.sh` — the SC-1 vehicle (D-15), a real `mix phx.new` + `mix sigra.install` left on disk — belongs to plan 239-13, along with the tarball, the full gate and the SURF-03 re-check. Do not reach for it here: a committed fixture greped in place of a generated app is the source tree wearing a disguise, and that substitution is likeliest to happen in the plan that already has the fixture open."
  prohibitions:
    - statement: "Must NOT land more than one new re-bless commit for batch 3. Re-runs before commit are fine; commits are what count. If the first captured diff fails the classifier, fix the template in a new commit and re-run — never amend the classification to fit."
    - statement: "Must NOT wave through a non-comment line in the re-bless diff. A nonconforming line is a stop-the-line event: halt, report, and do not commit. The re-bless task makes drift disappear regardless of whether the drift was intended, so an unexplained line is never a judgment call."
    - statement: "Must NOT use the re-bless as a way to make unexplained drift disappear. If `--check` or the classifier reveals drift this closure did not author, the drift is diagnosed and named in the evidence record before any blessing, even when blessing it would be green."
    - statement: "Must NOT hand-edit `test/fixtures/install_golden/tree/` or `STDOUT.txt` — only `MIX_ENV=test mix sigra.fixture.rebless_golden` writes them (D-09)."
    - statement: "Must NOT derive `239-golden-expected-3.txt` from the re-bless diff — that is circular and makes the classifier unfalsifiable."
    - statement: "Must NOT weaken `239-comment-only-diff-check.sh`'s fail-closed behaviour. The empty-input guard, the empty-expected-set guard, the add-only-hunk class, and the `removed_lines >= T-record-count` floor all stay; only the already-parameterized `GOLDEN_MIN_FILES` value is set, and it is set to a real count, never to zero."
    - statement: "Must NOT claim `install_golden_contract` is green from GitHub Actions. This phase pushes nothing; the Actions clause stays a recorded ship-time deferral."
    - statement: "Must NOT re-check SURF-03 in this plan under any circumstance, including if the re-bless and `--check` both come back green. The re-check belongs to plan 239-13, after the live external observations. A checkbox flipped on expectation rather than observation is the SURF-02 false-complete defect this phase already filed a todo about."
    - statement: "Must NOT edit `239-v3-vocabulary-check.sh` or `239-v3-allowlist.tsv`. Both are frozen by plan 239-09 (D-30); the golden tier's green must come from the re-bless carrying real edits, never from the measurement moving."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23) — a renamed required status context never reports and every PR hangs forever."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `ordering`, UNRESOLVED): 'when elements compare equal, is output order specified and stable?' This plan owns it. The D-19 ordering — edit, then mirror, then bless, each alone in its commit — is asserted here by commit-graph checks (`git merge-base --is-ancestor` on the freeze commit versus the re-bless commit, and per-commit path scoping), not by tooling. The probe row stays unresolved: nothing mechanically enforces the ordering, and a future phase could violate it without a gate firing."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `adjacency`, UNRESOLVED): the three-tier equality question. `--check` exit 0 is the only mechanism that proves template and golden are equal-by-generation; template↔example equality has no mechanism at all (FUT-01, filed and unbuilt). Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): the dead-instrument case. Every zero in this plan is paired with a positive control on the same surface, and the instrument distinguishes 'surface dirty' (exit 1) from 'instrument cannot answer' (exit 3). The per-alternation controls are re-run at final HEAD in plan 239-13, which is where a zero that came from an alternation quietly ceasing to match would be caught. The probe row itself is not auto-resolved by either mitigation."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `empty`, UNRESOLVED): an empty file list, an empty diff, or an empty expected set would each make a check trivially pass. The classifier fails closed on all three and that behaviour is demonstrated live rather than cited. Surfaced, not resolved."
---

<assumption_delta_decision>
**Outcome: `no-change`.** The `pluralization`/`second` signal is the D-26 batch-count amendment prose;
no identity-model consequence. Its residue — an unbounded per-batch re-bless count — is bounded by
**D-29**, and this plan is where D-29 is first discharged: batch 3's justification is written into
`239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` before batch 3's re-bless runs.
</assumption_delta_decision>

<objective>
Carry the batch-3 template edits into the golden fixture with one batched re-bless, proven
comment-only against a set frozen in an earlier commit.

Purpose: the golden fixture is the only local artifact that is provably byte-identical to a freshly
generated app, so until batch 3 reaches it the phase goal is unproven on the bytes an adopter
receives. The re-bless task rewrites the tree wholesale and makes drift disappear whether or not the
drift was intended — so the expected-removed set is frozen in an earlier commit, the classifier is
demonstrated RED on a known-bad fixture before its green is believed, and the diff is classified
before anything is committed.

This plan stops at the fixture. Re-observing the phase's claims at final HEAD — three-tier V3, the
fixed-string proof on a freshly generated app, the tarball under the D-27 scope, `MIX_ENV=test mix
ci`, SC-5, and the SURF-03 re-check — is plan **239-13**. That boundary is declared here rather than
discovered mid-run: this plan's work is mechanical and bounded by the classifier, while 239-13's is
every live external observation in the closure (Postgres, a scaffolded app, a built tarball, the full
gate). Running both in one context is how the last observation ends up cited instead of made.

Output: one re-bless commit; a RED-then-GREEN classifier demonstration; `--check` exit 0; a golden
tier green under V3; and SURF-03 still honestly `[ ]`, handed to plan 239-13.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-11-SUMMARY.md
@lib/mix/tasks/sigra.fixture.rebless_golden.ex
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **Exactly one new re-bless commit** for batch 3 (SC-3 as amended by D-26). Re-runs before commit
   are fine; commits are what count.
2. **`test/fixtures/install_golden/tree/` is never hand-edited** (D-09).
3. **The re-bless task does not stage or commit** (`rebless_golden.ex:19-20`). Stage by path.
4. **A nonconforming classifier line halts the plan.** Do not commit and then explain.
5. **A zero-match grep must be a measurement, not a fatal status.** Under `set -e`, a `grep` that
   matches nothing aborts the shell and yields a confident false negative. Brace-group every counting
   grep with `|| true` inside the command substitution. This is the exact shape of every "returns
   zero hits" assertion in this plan.
6. **Every zero is paired with a positive control on the same surface** (Standing Constraint 1).
7. **Evidence at the final committed HEAD on a clean tree** (Standing Constraint 2).
8. **This plan does NOT run the full gate.** `MIX_ENV=test mix ci` (never root `mix test`, Standing
   Constraint 3) belongs to plan 239-13, along with every other live external observation. Running it
   here would duplicate the closure's most expensive step and consume the context 239-13 needs.
9. **`.github/` is read-only** (D-23).
10. **The repo is public.** No home-directory paths, adopter PII, or brand hex values in any artifact.
11. **Do not push.** Commit only, ending messages with
    `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

### Environment preconditions — the last waves hit every one of these

- **Postgres must be live.** `scripts/db/up.sh` then `source tmp/db.env`. A missing database fails
  fast rather than skipping.
- **The phx_new 1.8.8 archive must be installed locally** or golden diffs are spurious:
  `mix archive.install --force hex phx_new 1.8.8`. A different local archive version produces
  byte-diffs against the committed fixture that are artifacts, not findings.
- **A stale `_build` masquerades as a real failure.** Plan 239-08's first `MIX_ENV=test mix ci` was
  RED with six `Sigra.Audit.Forwarders.ThreadlineTest` failures reporting `Threadline.attach/1 is
  undefined`, caused by a local `_build` holding a sigra beam compiled before the `:threadline` dep
  existed. The fix was `mix deps.compile threadline --force && mix compile --force`, with **zero
  source changes**. The re-bless task compiles too, so the same hazard applies to a red `--check`
  here. Diagnose before editing anything.
- **`mix phx.new` rejects hyphens in app names.** Use underscores in any scaffolded tmp app path.
- **asdf finds no `.tool-versions` under `/tmp`**, so a script run there fails with
  `No version is set for command mix`. Prefix with
  `ASDF_ERLANG_VERSION=28.5 ASDF_ELIXIR_VERSION=1.19.5-otp-28`.
- **`gsd-tools` is not on PATH** — invoke `node ~/.claude/gsd-core/bin/gsd-tools.cjs`.

<tasks>

<task type="auto">
  <name>Task 1: Justify batch 3, freeze the round-3 expected set, and demonstrate the classifier RED — all before any re-bless runs</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-3.txt, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless3-code-change.diff, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>The working tree is clean at plan 239-11's mirror commit, and `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 2 printing `DRIFT DETECTED:` — proving batch 3's template edits have not yet reached the golden fixture. An exit 0 here means the template edits never reached generated output, or the fixture was hand-edited: halt and report.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-comment-only-diff-check.sh` — the whole script: the `T:`/`N:` record format, the containment lookup keyed on (path, trimmed text) and never on line number, the three violation classes, the usage and exit-code contract in its header, the four `refusing to report success` fail-closed guards, and the two non-vacuity floors `floor_removed="$expected_t_count"` and `floor_files="${GOLDEN_MIN_FILES:-30}"`.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-2.txt` — its header line records round 2's exact generating command. Round 3's generator is the same shape with V3 substituted for V2.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless2-code-change.diff` — round 2's known-bad fixture, as the shape template for round 3's: a real diff hunk with exactly one code line altered.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## VOCABULARY-LEDGER` (V3 verbatim), § `## REFREEZE-LEDGER` (round 2's freeze record and its `git merge-base --is-ancestor` proof format), and § `## BATCH-JUSTIFICATION` (the D-29 obligation and its empty batch-3 slot).
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-11-SUMMARY.md` — which template edits composed batch 3, needed for the D-29 justification.
    - `lib/mix/tasks/sigra.fixture.rebless_golden.ex` lines 40-140 — what the task writes, that it never stages or commits, and the exact `--check` exit semantics.
  </read_first>
  <action>
Discharge D-29 first. Fill the batch-3 slot in `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION`: name the
two template edits that compose batch 3, and state why they could not have been folded into batch 2 —
they were surfaced by `239-VERIFICATION.md` and `239-REVIEW.md`, both of which were produced *after*
batch 2's re-bless commit, so no earlier batch could have contained them. Write the count of re-bless
commits the phase now carries and the list of their shas, so the per-batch count stays auditable even
though D-26 no longer fixes it.

Then generate `239-golden-expected-3.txt` from the **currently committed** golden tree, before running
any re-bless. Two record classes, as in rounds 1 and 2:

  * `T:` records — every line in `$(git ls-files test/fixtures/install_golden/tree)` matched by V3
    (copied out of `## VOCABULARY-LEDGER`, never retyped).
  * `N:` records — the golden-tree lines corresponding to batch 3's prose repairs that V3 cannot match
    because they are ordinary English: the WR-01 sentences replaced in the invitation moduledoc, and
    the pagination-bullet line whose terminator is restored. Each `N:` record is located by a
    **literal anchor** taken from `239-11-SUMMARY.md` and `239-REVIEW.md` — never by a blanket radius
    around a match. Enumerate the anchors in the ledger; a record with no listed anchor is not
    admissible.

Write the generating command into the file's header line, as rounds 1 and 2 did.

Then build the round-3 known-bad fixture. Take the real re-bless-3 diff shape and alter exactly one
**code** line in it — a `def`/`defp` head or a pipeline line, not a comment — and commit it as
`fixtures/239-golden-rebless3-code-change.diff`. Run the classifier against it with
`239-golden-expected-3.txt` and require exit 1, `nonconforming >= 1`, and the offending line named in
the output. This is the RED half: round 1 and round 2 both did this, and a green from an unfalsified
instrument is what produced this phase's gap in the first place. Also demonstrate the empty-input
guard live: pipe an empty diff on stdin and require exit 1 with `refusing to report success` in the
output.

Record explicitly, next to both numbers, why the RED demonstration above runs with
`GOLDEN_MIN_FILES=1` while Task 2's real run must use the computed value: the known-bad fixture is a
single hunk and must fail on the altered code line, not on a file-count floor it was never built to
clear; the floor is a non-vacuity guard for a thin *real* diff, and it is applied — not disabled —
where it matters. Two different runs, two different jobs, both written down.

Compute the value `GOLDEN_MIN_FILES` will take for round 3: the exact count of distinct paths in
`239-golden-expected-3.txt`. Batch 3 renders into a handful of files, far fewer than round 1's 35, so
the default floor of 30 would fail spuriously. Record the computed value and state explicitly that it
is a real non-vacuity floor equal to the expected path count, not a disabled one. Do not change any
other line of the classifier — it was already parameterized in plan 239-08.

Commit path-scoped to the three files, message
`chore(239): justify batch 3, freeze the round-3 expected set, and demonstrate the classifier RED`.
This commit must land **before** Task 2's re-bless commit, so that "frozen first" is a property of the
commit graph rather than of recorded prose. Record its sha in `## REFREEZE-LEDGER` alongside rounds 1
and 2.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; exp="$d/239-golden-expected-3.txt"; bad="$d/fixtures/239-golden-rebless3-code-change.diff"; cls="$d/239-comment-only-diff-check.sh"; test -s "$exp" || { echo "MISSING_EXPECTED_SET"; exit 1; }; test -s "$bad" || { echo "MISSING_KNOWN_BAD_FIXTURE"; exit 1; }; test -x "$cls" || { echo "MISSING_CLASSIFIER"; exit 1; }; grep -qE "^(T|N):" "$exp" || { echo "EXPECTED_SET_HAS_NO_RECORDS"; exit 1; }; set +e; out=$(GOLDEN_MIN_FILES=1 "$cls" "$bad" "$exp" 2>&1); rc=$?; set -e; printf "%s\n" "$out"; test "$rc" = 1 || { echo "CLASSIFIER_NOT_RED rc=$rc"; exit 1; }; printf "%s\n" "$out" | grep -qE "nonconforming=[1-9]" || { echo "NO_NONCONFORMING_COUNT"; exit 1; }; echo CLASSIFIER_RED_CONFIRMED'</automated>
    <!-- GOLDEN_MIN_FILES=1 is correct HERE and only here. This invocation is the RED demonstration
         against a single-hunk known-bad fixture, whose whole purpose is to prove the classifier can
         fail on a code line; the file-count floor is a non-vacuity guard against a thin REAL diff
         and is irrelevant to a fixture that is deliberately one hunk. Setting it to the round-3
         computed value would make this run fail on the floor instead of on the code line, which
         would prove nothing about the classifier's ability to detect the code line. The floor is
         NOT disabled: Task 2's real run uses the computed value from this task, and that is the run
         the acceptance criteria gate on. -->

  </verify>
  <acceptance_criteria>
    - `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION`'s batch-3 slot is filled: the unfilled marker written by plan 239-10 is gone, the two batch-3 template edits are named, the "could not have been folded into batch 2" reason is stated, and the running count of re-bless commits with their shas is recorded.
    - `239-golden-expected-3.txt` exists, is non-empty, has a `#`-prefixed header line recording its generating command, and contains at least one `T:` or `N:` record.
    - Every `N:` record corresponds to a literal anchor enumerated in `## REFREEZE-LEDGER`; the count of `N:` records equals the count of listed anchors. No blanket-radius records.
    - **The freeze is git-provable.** After Task 2 lands, `git merge-base --is-ancestor <this-commit-sha> <rebless-sha>` exits 0 and the two shas are distinct. Both are recorded in `## REFREEZE-LEDGER`.
    - This commit lists exactly three paths and nothing under `test/fixtures/install_golden/` — `git show --name-only --format= HEAD | grep -c 'test/fixtures/install_golden/'` returns `0`.
    - **Classifier RED demonstrated:** run against `fixtures/239-golden-rebless3-code-change.diff` with the round-3 expected set, it exits `1`, prints `nonconforming` with a value `>= 1`, and its output names the altered code line. The command and full output are recorded in the ledger.
    - **Fail-closed demonstrated live:** an empty diff piped on stdin makes the classifier exit `1` with `refusing to report success` in its output. Command and output recorded.
    - The classifier's four fail-closed guards survive: `grep -c 'refusing to report success' 239-comment-only-diff-check.sh` returns `>= 4`, and `floor_removed="$expected_t_count"` is still present verbatim.
    - `git diff --name-only HEAD~1 -- .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-comment-only-diff-check.sh` is empty — the classifier itself is not modified in this round.
    - The computed `GOLDEN_MIN_FILES` value is recorded and equals the distinct-path count of the expected set; it is not `0` and not `1`-by-default. Stated for the record so the two numbers in this task do not read as a contradiction: the RED demonstration above deliberately runs with `GOLDEN_MIN_FILES=1` because it is measuring a **single-hunk known-bad fixture** and must fail on the altered code line rather than on a file-count floor that the fixture was never built to clear; the computed value is mandatory for Task 2's run against the **real** re-bless diff, which is the run the floor exists to protect and the run the acceptance criteria gate on. Record both numbers and this reason together.
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exited `2` with `DRIFT DETECTED:` at the start of this task, recorded with its output.
  </acceptance_criteria>
  <done>Batch 3 is justified in writing before it runs, a non-circular round-3 expected set is committed ahead of the re-bless so the ordering is readable from `git log`, and the classifier is proven able to fail on a real code change and on empty input before any of its greens are believed.</done>
  <reversibility rating="costly">The round-3 expected set re-bases what "comment-only" means for this batch, and the classification recorded against it is what a later reader compares to. Git-revertable, but the anchor list must be enumerated in the ledger rather than implied, so the derivation survives the revert. Reusing round 2's set is not viable — it contains lines that no longer exist in the tree.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the single batched re-bless for batch 3, classify the diff GREEN, and commit it alone</name>
  <files>test/fixtures/install_golden/tree, test/fixtures/install_golden/STDOUT.txt</files>
  <precondition>Task 1's freeze commit is HEAD, the working tree is clean, `239-golden-expected-3.txt` is committed, and the phx_new 1.8.8 archive is installed locally (`mix archive.install --force hex phx_new 1.8.8`). A different archive version produces byte-diffs that are artifacts, not findings.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-3.txt` — written in Task 1.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## REBLESS-COMMIT` and the round-2 re-bless record — how rounds 1 and 2 captured, classified and committed their diffs. Round 3 follows the same sequence.
    - `lib/mix/tasks/sigra.fixture.rebless_golden.ex` lines 40-140 — that the task calls `File.rm_rf!` and rewrites the tree wholesale, that it never stages or commits, and the exact `--check` output strings and exit codes.
  </read_first>
  <action>
Run `MIX_ENV=test mix sigra.fixture.rebless_golden` exactly once, batched — never per-file. Do not
stage anything yet.

Capture the resulting working-tree diff to a file before touching the index:
`git diff -- test/fixtures/install_golden`. Run
`239-comment-only-diff-check.sh <captured-diff> 239-golden-expected-3.txt` with `GOLDEN_MIN_FILES`
set to the value computed in Task 1. Require exit 0 and `nonconforming=0`, and capture the printed
`changed_lines`, `removed_lines`, `files` and the three sub-counters.

If the classifier reports any nonconforming line, **halt**. Do not commit, do not reclassify, do not
relax the floor. A non-comment line in a re-bless diff means something changed that this closure did
not author, and the re-bless task will have already erased the evidence of what it was in the tree —
so the captured diff is the only record. Diagnose it from the captured diff, name it in the evidence
ledger, and stop for the operator. Blessing it through is prohibited even when blessing it would be
green.

If the classifier is green, stage by path — `git add test/fixtures/install_golden` and nothing else —
and commit alone, message
`chore(239): re-bless install golden fixture after the batch-3 template edits (SURF-03)`. The commit
must contain no other path: not the phase directory, not `.planning/`, not a template. That isolation
is what lets a reviewer tell generated drift from hand edits (D-19).

After the commit, run `MIX_ENV=test mix sigra.fixture.rebless_golden --check` on the clean tree and
require exit 0 with `OK: fixture is up-to-date (check mode.)`. This scaffolds a fresh tmp app and is
the only local proof that the committed golden tree equals real generated output — every SC-1 claim
resting on the golden tree depends on this exit code.

Separately check `STDOUT.txt` under V3, rather than assuming the tree diff covers it: D-13 records
that installer summary output can drift independently of the tree. Record the count.

Finally close the golden tier's RED/GREEN pair: run `239-v3-vocabulary-check.sh golden` and require
exit 0 with `hits_outside_allowlist=0` and a non-zero `control_defmodule=`. Record the raw `hits=`
total alongside. Do not edit the instrument or the allowlist (D-30); if the tier will not go green
after a `--check`-clean re-bless, something the closure did not author is in the fixture — halt and
report. Record the tier result in `239-EVIDENCE.md` § `## REBLESS-COMMIT`'s round-3 entry.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; s="$d/239-v3-vocabulary-check.sh"; test -x "$s" || { echo "MISSING_INSTRUMENT"; exit 1; }; MIX_ENV=test mix sigra.fixture.rebless_golden --check && git diff --quiet && out=$("$s" golden 2>&1) && printf "%s\n" "$out" && printf "%s\n" "$out" | grep -q "hits_outside_allowlist=0" && echo REBLESS3_CHECK_CLEAN'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit touches `test/fixtures/install_golden`: `git log --format=%H <freeze-sha>..HEAD -- test/fixtures/install_golden | wc -l` returns `1`.
    - That commit contains nothing else: `git show --name-only --format= <rebless-sha> | grep -cv '^test/fixtures/install_golden/'` returns `0`.
    - The captured diff was classified BEFORE the commit and the classifier exited `0` with `nonconforming=0`. The full classifier output — `changed_lines`, `removed_lines`, `files`, `nonconforming` and the three sub-counters — is recorded verbatim in the evidence ledger, not summarized.
    - `GOLDEN_MIN_FILES` was set to the Task-1 computed value, and `files` in the classifier output is `>=` that value — the floor was cleared, not disabled. Both numbers recorded.
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits `0` and prints `OK: fixture is up-to-date (check mode.)` on the clean tree after the commit. Output recorded verbatim.
    - `git diff --quiet` succeeds after the `--check` run — the check mode wrote nothing into the working tree.
    - `grep -cE '<V3>' test/fixtures/install_golden/STDOUT.txt` is recorded as an explicit number (D-13's independent-drift hazard re-checked under the widened definition, not assumed still nil).
    - No file under `test/fixtures/install_golden/` was written by anything other than the re-bless task: no `Edit`/`Write`/`sed` touched that path, confirmed by the commit's provenance being a single task invocation and by the diff classifying clean against a set frozen in an earlier commit.
    - `git merge-base --is-ancestor <freeze-sha> <rebless-sha>` exits `0` and the two shas differ.
    - `239-v3-vocabulary-check.sh golden` exits `0` with `hits_outside_allowlist=0` and a non-zero `control_defmodule=`, closing the RED/GREEN pair plan 239-09 opened on this tier. The raw `hits=` total is recorded alongside. An exit `3` is an instrument failure, never a result: halt.
    - `239-v3-vocabulary-check.sh` and `239-v3-allowlist.tsv` are byte-unchanged by this plan: `git diff --name-only <freeze-sha>~1..HEAD -- .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-allowlist.tsv` is empty.
    - SURF-03 is still `[ ]` at this plan's final HEAD: `grep -c '^- \[ \] \*\*SURF-03\*\*' .planning/REQUIREMENTS.md` returns `1`. The re-check belongs to plan 239-13.
  </acceptance_criteria>
  <done>One batched re-bless commit carries batch 3 into the golden fixture, its diff was proven comment-only against a set frozen in an earlier commit before anything was staged, the commit contains that directory and nothing else, and `--check` exit 0 re-establishes the golden tree as admissible generated-app evidence.</done>
  <reversibility rating="costly">The re-bless rewrites the fixture tree wholesale and erases whatever drift existed. Git-revertable, but the pre-run state survives only in the captured diff and the frozen expected set — which is why both are committed. Not one-way, so no `checkpoint:decision` is emitted.</reversibility>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template tier -> golden fixture (`mix sigra.fixture.rebless_golden`) | A task that rewrites the tree wholesale and makes drift disappear whether or not it was intended. Anything crossing here becomes the record of "what an adopter generates". |
| Golden fixture -> phase verdict | The fixture is admissible as generated-app evidence only while `--check` exits 0. Without that, every SC-1 claim degrades to a claim about the source tree. |
| Frozen expected set -> classifier verdict | The expected set is what "comment-only" means for this batch. Derived from the diff it validates, it would certify anything. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-12-01 | Tampering | `mix sigra.fixture.rebless_golden` | critical | mitigate | The task erases drift as a side effect of its purpose. The diff is captured and classified before staging, against an expected set frozen in an earlier commit proven ancestral by `git merge-base --is-ancestor`; any nonconforming line halts the plan with the captured diff as the only surviving record. |
| T-239-12-02 | Spoofing | the comment-only classifier | high | mitigate | A classifier that cannot fail produces a meaningless green. Demonstrated RED on a committed known-bad fixture with one code line altered, and fail-closed on empty input, both live, before its green is believed. |
| T-239-12-03 | Spoofing | WR-01's replacement prose, now blessed into the fixture | high | transfer | A re-bless silently blesses whatever prose exists into the artifact that reads as verified. The replacement's two supporting observations — that T19's subject is the template path, and that the generated `test/` tree contains 0 `_test.exs` files — are re-made against a freshly generated app in plan **239-13** Task 1 (with a `*.exs` positive control on the same tree), not cited from plan 239-11's SUMMARY, which measured them against the committed golden snapshot. Plan 239-13's acceptance criteria carry that landing place by name and record this row as discharged. Named here so the hazard is not lost at the plan boundary. |
| T-239-12-04 | Repudiation | `install_golden_contract` Actions clause | high | transfer | Claiming a CI job green from a local proxy would be a fabricated verdict. This plan makes no CI claim at all; the deferral record is written in plan 239-13's `## HONEST-CLAIMS` extension. |
| T-239-12-05 | Tampering | `.github/` workflow `name:` lines | critical | mitigate | A renamed required status context never reports and every PR hangs forever (D-23). `.github/` is read-only here; the re-bless task writes only under `test/fixtures/install_golden/` and the commit-scope criteria prove it. SC-5's two-base re-proof runs in plan 239-13. |
| T-239-12-06 | Spoofing | zero-result greps | high | mitigate | A zero from an empty file list, a dead alternation, or a `set -e` abort reads identically to a clean surface. Every zero here is paired with a same-surface positive control, and `239-v3-vocabulary-check.sh` fails closed on exit 3 — distinct from the exit 1 that means the surface is dirty — so a broken instrument cannot read as a result. |
| T-239-12-07 | Information disclosure | `239-EVIDENCE.md`, committed fixtures | medium | mitigate | Public repo. The captured diff, the expected set and the known-bad fixture carry repo-relative paths and repo content only — no home-directory paths, adopter PII, or brand hex values. |
| T-239-12-08 | Denial of service | stale `_build` / wrong `phx_new` archive | medium | mitigate | A `--check` red from a stale beam or a mismatched `phx_new` archive looks exactly like real drift and invites a source "fix" that breaks working code. Both signatures and their zero-source-change recoveries are written into this plan's environment preconditions and must be ruled out before any source change. |
| T-239-12-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists. `mix archive.install --force hex phx_new 1.8.8` is a version-pinned Hex archive already required by CI (`ci.yml:478,546,627,774`) and installed as an environment precondition rather than a new dependency; RESEARCH.md's Package Legitimacy Gate covers no new package here. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification):

- `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-3.txt` (new file)
- `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless3-code-change.diff` (new file)
- The filled batch-3 slot in `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` (new content in a section created by plan 239-10)
- The round-3 entry in `239-EVIDENCE.md` § `## REFREEZE-LEDGER` (new entry)
- The round-3 entry in `239-EVIDENCE.md` § `## REBLESS-COMMIT` (new entry)
- The regenerated contents of `test/fixtures/install_golden/tree/` and `STDOUT.txt` (rewritten wholesale by the re-bless task, not hand-authored)
</artifacts_this_phase_produces>

<verification>
- Exactly one new commit touches `test/fixtures/install_golden`, contains nothing else, and is a descendant of the freeze commit.
- The classifier exits 1 on the known-bad fixture and on empty input, and exits 0 with `nonconforming=0` on the real diff with the round-3 computed `GOLDEN_MIN_FILES` floor applied.
- `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 0 with `OK: fixture is up-to-date (check mode.)` on a clean tree after the re-bless commit.
- `239-v3-vocabulary-check.sh golden` exits 0 with `hits_outside_allowlist=0` and a live `control_defmodule`, and the instrument and allowlist files are byte-unchanged across this plan.
- `git diff --name-only <plan-base>..HEAD -- .github/` is empty; nothing under `.github/`, `lib/`, `priv/` or `test/example/` moved.
- SURF-03 still reads `[ ]` with a `Pending` roll-up row — the re-check is plan 239-13's last commit.
</verification>

<success_criteria>
Batch 3 reaches the golden fixture through one batched, comment-only-proven, isolated re-bless whose
expected set was frozen in an earlier commit and whose classifier was shown able to fail first;
`--check` exit 0 re-establishes the fixture as admissible generated-app evidence; the golden tier
closes its RED/GREEN pair under the frozen instrument; and SURF-03 is left honestly unchecked for
plan 239-13.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-12-SUMMARY.md` when done.
</output>
