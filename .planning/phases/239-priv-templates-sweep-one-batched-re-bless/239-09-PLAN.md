---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 09
type: execute
wave: 9
depends_on: ["239-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  - .planning/todos/pending/2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` marks SURF-03 as `[ ]` (unchecked) and its roll-up row reads a non-Complete status, at every commit from this plan's first commit until plan 239-12 re-checks it — the checkbox never asserts a state that does not hold at that commit."
    - "A V3 vocabulary definition exists as a committed, runnable phase artifact `239-v3-vocabulary-check.sh`, strictly wider than V2 (every V2 alternation retained, plus a vocabulary class), recorded verbatim in `239-EVIDENCE.md` § `## VOCABULARY-LEDGER`."
    - "V3 is demonstrated RED at this plan's HEAD on ALL THREE tiers simultaneously — `priv/templates/`, `test/example/`, and `test/fixtures/install_golden/tree/` — with a non-zero hit count in each, and the two sentences named in `239-VERIFICATION.md`'s SC-1 gap are among the reported hits in each tier."
    - "Every alternation V3 adds over V2 carries a live positive control returning a non-zero count on a named surface, recorded per-alternation in the ledger — a dead alternation cannot masquerade as a clean result."
    - "Every zero V3 reports anywhere is paired with a positive control on the same file list (a non-zero `defmodule` count on the same `git ls-files` output), so an empty file list can never be read as a clean tree."
    - "Every V3 hit that is NOT bookkeeping is triaged in the ledger with a named reason (false-positive class), not silently dropped from the count."
    - "The existing todo `2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md` carries V3 verbatim and states that Phase 241's `p18` guard must be built on V3, superseding the V2 text it currently names as authoritative."
    - "This plan wires V3 into nothing — `grep -rn '239-v3-vocabulary-check' mix.exs .github scripts/ 2>/dev/null` returns no matches (Standing Constraint 5 / D-04: Phase 239 builds no guard)."
    - { statement: "The V3 recall pass over `priv/templates/` reviewed every hit rather than only the two known sentences, and the ledger records the reviewed hit count alongside the two known ones", verification: backstop }
  artifacts:
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
    - .planning/REQUIREMENTS.md
    - .planning/todos/pending/2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md
  key_links:
    - "V3 is the instrument every acceptance criterion in plans 239-11 and 239-12 is measured against. If V3 is not demonstrated RED here, a green in 239-12 proves nothing — that is the exact V1 and V2 failure this closure exists to stop repeating."
    - "`239-EVIDENCE.md` § `## WIDENED-UNION-LEDGER` holds V2 verbatim; V3 must be generated by extension from that string, not retyped, so 'strictly wider' is mechanically checkable."
    - "The SURF-03 checkbox in `.planning/REQUIREMENTS.md:84` and its roll-up row near `:158` must move together — a checked box with a Pending roll-up row (or the reverse) is the same false-complete defect the SURF-02 todo already records."
  prohibitions:
    - statement: "Must NOT narrow, tune, or hand-fit V3 so that the current tree happens to pass it. V3 is fixed before any template edit lands (plan 239-11), and its RED demonstration at this plan's HEAD is what makes that ordering provable from the commit graph."
    - statement: "Must NOT accept a regex zero as proof that the goal is met. Every zero in this closure is a measurement under a named, versioned definition, and must be reported as such rather than as 'clean'."
    - statement: "Must NOT drop a V3 hit from the reported count because it looks like a false positive. Triage it in the ledger by name and reason, leaving the raw count visible alongside the triaged count."
    - statement: "Must NOT re-check SURF-03 in this plan under any circumstance, including if the work 'obviously will' complete. The re-check belongs to plan 239-12, after the sentences are gone from all three tiers."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): the probe returned `unclassified — review manually` and is NOT auto-resolved by a backstop. Manual read: the assumed edge is the dead-instrument case — a grep that returns zero because it can match nothing, rather than because the surface is clean. Handled here by the per-alternation positive controls and the paired `defmodule` control, but the probe row itself remains unresolved and is surfaced, not closed."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `adjacency`, UNRESOLVED): 'when two things are exactly equal or just touch, do they merge, collide, or separate?' Read against this phase: the three-tier template/example/golden relationship. Template and golden are required to be byte-equal-by-generation; template and example are hand-mirrored and are permitted to differ only in module naming. The probe is surfaced unresolved because nothing in this closure mechanically enforces that distinction — FUT-01 is the filed todo for exactly this and is explicitly not built here."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `empty`, UNRESOLVED): 'what is the result for empty, single-element, or null input?' Read against this phase: an empty `git ls-files` argument list makes every grep return zero. Mitigated by pairing every zero with a same-file-list positive control, but the probe row stays unresolved because the mitigation is per-command convention, not an enforced property."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `ordering`, UNRESOLVED): 'when elements compare equal, is output order specified and stable?' Read against this phase: the D-19 commit ordering (sweep -> mirror -> re-bless, each alone in its commit). Surfaced here and owned by plans 239-11 and 239-12; the probe row itself remains unresolved."
---

<assumption_delta_decision>
**Outcome: `no-change`.**

The detector fired `kind: pluralization`, term `second`, on the D-26 amendment prose ("exactly **one**
for the phase" -> "one per batch"). That is a batch-count amendment, not an identity-model change: no
noun lost its monopoly, no primary key moved, no anchor was redefined. No plan in this closure changes
what a re-bless *is*, only how many are permitted.

**Recorded residue (the real signal, kept rather than dismissed):** "exactly one re-bless for the
phase" was *countable*; "one per batch" is *unbounded* — nothing now caps how many re-bless commits a
future phase can justify by declaring another batch. This closure does not accept that as silent debt:
plan 239-10 lands **D-29**, which bounds it by requiring every batch to be justified by name in
`239-EVIDENCE.md` before its re-bless runs, so the count stays auditable even though it is no longer
fixed.
</assumption_delta_decision>

<objective>
Make the phase's bookkeeping instrument able to see the class of bookkeeping that is still shipping,
and stop the SURF-03 checkbox from asserting a state that does not hold.

Purpose: `239-VERIFICATION.md` failed SC-1 because two Sigra-internal sentences ship into every
generated app and the V2 regex is structurally blind to them — V2 matches plan *identifiers*
(`D-12`, `239-05`, run ids, `UAT`), and these are plan *vocabulary*. That is the same shape of blind
spot V1 had, one class up. Editing the two sentences without widening the instrument would close the
symptom and leave the mechanism that hid them intact, so the next vocabulary-class leak would ship
under a green measurement exactly as these two did. The instrument is built and demonstrated RED
**before** any template edit lands, so its later green is a real negative.

This plan leads with the SURF-03 uncheck rather than with the tracer, deliberately: the tracer task
lands a commit, and the brief requires the checkbox to be honest at every intermediate commit.

Output: a runnable V3 vocabulary definition committed as a phase artifact, demonstrated RED across
all three tiers with per-alternation live positive controls; a `## VOCABULARY-LEDGER` record; an
honest SURF-03 checkbox; and the Phase 241 inheritance todo pointed at V3 instead of V2.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **This plan builds no guard** (Standing Constraint 5, D-04). V3 is a phase artifact under the
   phase directory, wired into nothing. Do not touch `mix.exs`, `.github/`, or
   `scripts/ci/prohibitions/`.
2. **No template, `test/example/`, or `test/fixtures/install_golden/` file is edited here.** This
   plan measures; plan 239-11 edits. A change under any of those paths in this plan's commits is a
   stop-the-line event.
3. **A zero-match grep must be a measurement, not a fatal status.** `set -e` plus a `grep` that
   matches nothing aborts the shell mid-script and produces a confident false negative. Wrap every
   counting grep so a zero is a value: `n=$( { grep -cE "$RE" file || true; } )`.
4. **Every zero is paired with a positive control on the same surface** (Standing Constraint 1 —
   count-only acceptance is rejected at review).
5. **`mix ci`, never root `mix test`** (Standing Constraint 3). This plan runs neither; plan 239-12
   owns the gate.
6. **Do not push.** Commit only. Commit messages end with
   `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
7. **The repo is public.** No home-directory paths, adopter PII, or brand hex values in any artifact
   written here.

<tasks>

<task type="auto">
  <name>Task 1: Uncheck SURF-03 and correct its roll-up row, so the requirement stops asserting a state that does not hold</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read the whole `### Clean shipped surface (SURF)` block (around `:82-85`) for the exact SURF-01 / SURF-03 / SURF-04 wording, and the roll-up table rows near `:156-159` for the exact `| SURF-03 | Phase 239 | … |` cell format and the vocabulary used by other non-complete rows (`Pending`).
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the `gaps:` block and the Requirements Coverage table row that rates SURF-03 `⚠ SATISFIED AS MEASURED`; this is the evidence the checkbox contradicts.
    - `.planning/todos/pending/2026-09-17-surf-02-marked-complete-but-does-not-hold-at-head.md` — the identical false-complete defect this phase already filed for SURF-02; match its framing so a reader sees one pattern, not two unrelated notes.
  </read_first>
  <action>
Flip the SURF-03 list item from `- [x] **SURF-03**:` to `- [ ] **SURF-03**:` in
`.planning/REQUIREMENTS.md`. Leave the requirement's prose body byte-identical — this task changes
the completion marker only, not what the requirement says.

Change the roll-up table row for SURF-03 from `Complete` to `Pending (gap closure 239-09 … 239-12)`,
matching the cell style already used by the `Pending` rows in that table.

Leave SURF-01 checked, and add a one-sentence audit note immediately after the SURF-01 list item
recording the judgement explicitly rather than leaving it inferable: SURF-01's own wording is
narrowly scoped to `.planning/` path references in `lib/` and `priv/templates/`, verified on a
freshly generated app and the built tarball; both measure zero at HEAD, which is why it stays `[x]`
while SURF-03 does not. Cite `239-VERIFICATION.md`'s Requirements Coverage row as the source. Do not
alter SURF-01's requirement text itself.

Do not touch `.planning/ROADMAP.md` in this task — SC-1 and SC-2 amendments belong to plan 239-10
(D-27 / D-28), and mixing them here would put a criterion amendment in a commit whose stated subject
is an honesty correction.

Commit path-scoped to `.planning/REQUIREMENTS.md` with message
`docs(239): uncheck SURF-03 — it does not hold at HEAD (SURF-03)`.
  </action>
  <verify>
    <automated>bash -lc 'set -o pipefail; grep -q "^- \[ \] \*\*SURF-03\*\*" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*SURF-01\*\*" .planning/REQUIREMENTS.md && ! grep -qE "^\| SURF-03 \| Phase 239 \| Complete" .planning/REQUIREMENTS.md && echo SURF03_HONEST'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*SURF-03\*\*' .planning/REQUIREMENTS.md` returns `1`, and `grep -c '^- \[x\] \*\*SURF-03\*\*' .planning/REQUIREMENTS.md` returns `0`.
    - `grep -E '^\| SURF-03 \| Phase 239 \|' .planning/REQUIREMENTS.md` prints a row whose status cell does not contain the word `Complete`, and does contain `Pending`.
    - `grep -c '^- \[x\] \*\*SURF-01\*\*' .planning/REQUIREMENTS.md` still returns `1` — SURF-01 is not collaterally unchecked.
    - The SURF-01 audit note is present and cites the verification report: `grep -c '239-VERIFICATION.md' .planning/REQUIREMENTS.md` returns `>= 1`.
    - The SURF-03 requirement prose is byte-unchanged apart from the checkbox character: `git diff HEAD~1 -- .planning/REQUIREMENTS.md | grep -c '^[+-]' ` counts only the checkbox line pair, the roll-up row pair, and the added SURF-01 note line — no other `+`/`-` line touches the SURF-03 sentence body.
    - `git show --name-only --format= HEAD` lists exactly `.planning/REQUIREMENTS.md` and nothing else.
    - `git status --porcelain` is empty after the commit.
  </acceptance_criteria>
  <done>SURF-03 reads `[ ]` with a `Pending` roll-up row; SURF-01 stays `[x]` with its narrow-scope judgement written down rather than inferred; the commit contains that one file.</done>
  <reversibility rating="reversible">A checkbox flip in a planning document, revertable with a one-line edit. Rated reversible, so no `checkpoint:decision` is emitted.</reversibility>
</task>

<!-- planner-discipline-allow: plan-checker -->
<!-- planner-discipline-allow: v1.2 concern -->
<task type="tracer">
  <name>Task 2: End-to-end V3 vocabulary instrument — one definition, demonstrated RED across all three tiers, before any edit lands</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md, .planning/todos/pending/2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md</files>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## WIDENED-UNION-LEDGER` (around `:400`) — the V2 string verbatim, its six-alternation delta over V1, and the per-alternation positive-control format this ledger already established. V3 must be produced by *extending* that string, not by retyping it.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-comment-only-diff-check.sh` — the header block and the fail-closed guard style (`refusing to report success on no input`), the `set -euo pipefail` preamble, the usage/exit-code contract, and the "phase artifact, not repository tooling (D-04)" disclaimer. Reuse this shape; do not invent a new one.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the `gaps:` block, which names both residual sentences with their file paths and line numbers verbatim, and the "Instrument credibility (V2)" section, which states exactly why V2 cannot match them.
    - `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` — read lines 315-335 (the structural-invariant comment block) to see the first sentence in situ.
    - `priv/templates/sigra.install/organizations/live/organization_members_live.ex` — read lines 1-35 (the `@moduledoc`) to see the second sentence in situ.
    - `.planning/todos/pending/2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md` — the whole file; it currently names V2 as the definition Phase 241 must inherit, and that is the statement this task supersedes.
  </read_first>
  <action>
Build `239-v3-vocabulary-check.sh` in the phase directory as the thinnest runnable path that proves
a vocabulary-class leak is detectable end-to-end across every tier this phase ships into. It takes a
tier name or an explicit file list, runs the V3 definition over it, and prints `tier=`, `hits=`,
`control_defmodule=`, and one `path:line:` record per hit on stdout. Exit 0 on a clean tier, exit 1
on hits, exit 1 fail-closed when the file list is empty or the `defmodule` control is zero, exit 2
on usage error. Carry the same "phase artifact, not repository tooling (D-04)" header disclaimer and
the same `refusing to report success` fail-closed wording as `239-comment-only-diff-check.sh`.

Define V3 as V2 (copied mechanically out of `## WIDENED-UNION-LEDGER`, never retyped) plus a
vocabulary alternation class. Seed the class with, at minimum: the hyphenated `plan-checker`
compound and its spaced variant; `\bthis phase\b`; `\bthe plan\b`; a `v[0-9]+\.[0-9]+ concern`
pattern; `\bgap[- ]closure\b`; `\bre-?bless\b`; `\bROADMAP\b`; `SUMMARY\.md`. Then run a **recall
pass**: grep each candidate over `.planning/ROADMAP.md` and `.planning/phases/` to confirm it is
live vocabulary, and over `priv/templates/` to see what it actually catches. Keep, drop, or add
alternations based on that pass and record every keep/drop with its reason — a dropped candidate
with no recorded reason is a silent narrowing of the instrument.

Two non-negotiable properties of the definition:

  * **Strictly wider than V2.** Every V2 alternation survives verbatim. Prove it mechanically: the
    V2 string must be a substring of the V3 string, or each V2 alternation must be greppable
    individually out of V3. State which check was used.
  * **Per-alternation live positive control.** Each alternation V3 adds over V2 gets its own control
    line in the ledger: the alternation, a named surface it fires on (`.planning/ROADMAP.md`, a
    `239-*-PLAN.md`, this CONTEXT file), and the non-zero count it returns there. An alternation
    that fires nowhere is dead and is removed with that fact recorded — this is the pattern plan
    239-05 established for V2's six, and it is what stops a dead alternation from producing a
    reassuring zero later.

Then run the RED demonstration at this plan's HEAD, over all three tiers in one pass:
`priv/templates/` (via `git ls-files priv/templates`), `test/example/` (via `git ls-files
test/example`), and `test/fixtures/install_golden/tree/` (via `git ls-files
test/fixtures/install_golden/tree`). Each tier must report a non-zero `hits=` and a non-zero
`control_defmodule=`, and the two sentences named in `239-VERIFICATION.md`'s SC-1 gap must appear by
`path:line:` in each tier's hit list. This is the falsification the whole closure rests on: if V3
cannot see them today, its zero tomorrow means nothing.

Triage every other hit V3 reports over the three tiers. Each is either bookkeeping the closure must
remove (add it to the plan-239-11 work list and say so), or a false positive with a named class
(ordinary English caught by a broad alternation, an Elixir version string, a real adopter-facing
guide pointer per D-03). Record the raw hit count and the triaged count side by side. Never reduce
the reported count by deleting a row.

Record all of this in a new `239-EVIDENCE.md` section `## VOCABULARY-LEDGER`, placed after
`## WIDENED-UNION-LEDGER`: V3 verbatim; the V2 -> V3 delta; the per-alternation positive-control
table; the keep/drop record from the recall pass; the three-tier RED table with paired controls; and
the full triage. Add a `## VOCABULARY-LEDGER` entry stating **D-28** explicitly so it is a decision
and not an implicit drift: *this closure implements the widening as a measurement instrument only —
V3 is defined, demonstrated and recorded here, wired into nothing, and Phase 241's SURF-04 `p18`
guard inherits it as its spec* (Standing Constraint 5 and D-04 both forbid building the guard here).
Note under D-28 that plan 239-10 is where SC-1's wording is amended to name the definition it is
measured under, so the criterion stops over-claiming.

Finally, update the existing todo `2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md`
rather than filing a new one: retitle it to name V3, add V3 verbatim, add the vocabulary-class delta
and its positive controls, and add a short "why V2 was not enough" paragraph citing this closure's
SC-1 gap. Keep the existing V2 text in place as history and mark it superseded — do not delete it;
the point of the file is the audit trail of an instrument being widened twice.

Commit path-scoped to the three files with message
`chore(239): define V3 vocabulary bookkeeping definition, demonstrated RED on all three tiers (SURF-01, SURF-03)`.
  </action>
  <verify>
    <automated>bash -lc 'd=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; test -x "$d/239-v3-vocabulary-check.sh" || chmod +x "$d/239-v3-vocabulary-check.sh"; "$d/239-v3-vocabulary-check.sh" priv-templates; rc=$?; "$d/239-v3-vocabulary-check.sh" example; rc2=$?; "$d/239-v3-vocabulary-check.sh" golden; rc3=$?; test "$rc" = 1 -a "$rc2" = 1 -a "$rc3" = 1 && echo V3_RED_ALL_THREE_TIERS'</automated>
  </verify>
  <acceptance_criteria>
    - `239-v3-vocabulary-check.sh` exists, is executable, and its usage error path exits `2` when invoked with no arguments.
    - Run over each of the three tiers at this plan's HEAD it exits `1` and prints `hits=` with a value `>= 1` and `control_defmodule=` with a value `>= 1`. All three tiers, all three non-zero. A tier that reports `hits=0` here means V3 is still blind and the plan halts.
    - Each tier's hit list contains a `path:line:` record for the structural-invariant comment line in `invitation_accept_live.ex` and for the pagination-bullet second sentence in `organization_members_live.ex` — the two sentences enumerated in `239-VERIFICATION.md`'s SC-1 `gaps:` block. Matched by path and line number against that block, not by eye.
    - Fail-closed on empty input is demonstrated live: invoking the script with an explicitly empty file list exits `1` and prints a message containing `refusing to report success`. Record the command and its output.
    - V3 is provably wider than V2: a check is run and recorded showing every V2 alternation is present in V3. State in the ledger which check was used (substring containment, or per-alternation grep) and show its output.
    - `## VOCABULARY-LEDGER` exists in `239-EVIDENCE.md` and contains: V3 verbatim; a per-alternation positive-control row for every alternation V3 adds, each with a named surface and a non-zero count; a keep/drop record from the recall pass with a reason on every row; the three-tier RED table; and a triage row for every hit with a raw-vs-triaged count pair.
    - D-28 is stated by that ID in the ledger and names both halves of the decision (V3 implemented here as measurement only; mechanization handed to Phase 241 SURF-04).
    - The todo file contains the V3 string and the literal `superseded`, and still contains its original V2 string — `grep -c 'V2' <todo>` returns `>= 1` after the edit.
    - Nothing is wired: `grep -rn '239-v3-vocabulary-check' mix.exs .github scripts/ 2>/dev/null | grep -v '^$' | wc -l` returns `0`.
    - `git show --name-only --format= HEAD` lists exactly the three files named in `<files>` and nothing under `priv/`, `test/`, or `lib/`.
  </acceptance_criteria>
  <done>A single runnable V3 definition sees the leak end-to-end in every tier that ships, proven by a live RED with paired controls and a demonstrated fail-closed empty case, recorded with per-alternation positive controls, and handed to Phase 241 through the existing todo — all before a single template byte is edited.</done>
  <reversibility rating="costly">V3 becomes the definition every later acceptance criterion in this closure is measured against, and Phase 241's `p18` guard inherits it. Reverting it later means re-deriving the measurements in plans 239-11 and 239-12. Not one-way — it is a phase artifact wired into nothing and deletable — so no `checkpoint:decision` is emitted. The cost is mitigated by requiring every keep/drop from the recall pass to carry a recorded reason, so a later reader can reconstruct the definition's derivation rather than inheriting it on trust.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra repo -> adopter project (`mix sigra.install`) | Template bytes cross into a project Sigra does not control and cannot patch. Prose that crosses this boundary is read as authoritative by someone who cannot check it. |
| Measurement instrument -> phase verdict | A regex output is converted into a pass/fail claim about the adopter-facing surface. A blind or dead instrument produces a false clean. |
| Phase 239 -> Phase 241 (`p18` guard) | A definition handed forward becomes permanent CI policy. A narrow definition freezes its own blind spot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-09-01 | Spoofing | V3 instrument output | high | mitigate | A dead alternation spoofs a clean result. Every added alternation carries a live positive control with a named surface and non-zero count, recorded per-alternation in `## VOCABULARY-LEDGER`. |
| T-239-09-02 | Tampering | V3 definition | high | mitigate | V3 tuned to what the current tree passes would move the goalpost. V3 is frozen and demonstrated RED at this plan's HEAD, before plan 239-11 edits anything, so the ordering is provable from the commit graph. |
| T-239-09-03 | Information disclosure | `239-EVIDENCE.md`, phase artifacts | medium | mitigate | The repo is public. No home-directory paths, adopter PII, or brand hex values are written into the ledger; only repo-relative paths. |
| T-239-09-04 | Repudiation | SURF-03 checkbox | medium | mitigate | A `[x]` that does not hold lets a later reader believe a verification that never happened. Task 1 unchecks it; plan 239-12 re-checks it only after live proof. |
| T-239-09-05 | Denial of service | `set -e` + zero-match grep | low | accept | A false negative from an aborted shell is a known local hazard, mitigated by the `|| true` brace-group convention in the safety ruleset. Accepted rather than engineered around. |
| T-239-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan — it edits Markdown and adds one bash script. RESEARCH.md's Package Legitimacy Gate is not triggered. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification — these do not exist at this plan's base commit):

- `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh` (new file)
- `239-EVIDENCE.md` § `## VOCABULARY-LEDGER` (new section)
- Decision ID **D-28** (new; stated in the ledger, recorded into `239-CONTEXT.md` by plan 239-10)
- The V3 definition string itself (new; a strict superset of the existing V2 string)
- The `Pending (gap closure 239-09 … 239-12)` roll-up status cell for SURF-03 in `.planning/REQUIREMENTS.md` (new cell value)
- The SURF-01 audit note in `.planning/REQUIREMENTS.md` (new sentence)
</artifacts_this_phase_produces>

<verification>
- `239-v3-vocabulary-check.sh` exits 1 with non-zero `hits=` and non-zero `control_defmodule=` on each of `priv/templates/`, `test/example/`, and `test/fixtures/install_golden/tree/` at this plan's HEAD.
- The empty-input invocation exits 1 with `refusing to report success` in its output.
- `.planning/REQUIREMENTS.md` shows SURF-03 `[ ]` with a non-Complete roll-up row and SURF-01 still `[x]`.
- `git log --oneline -2` shows exactly two new commits, each path-scoped to the files its task names, with nothing under `priv/`, `test/`, `lib/`, or `.github/`.
- `git diff --name-only HEAD~2 -- .github/` is empty.
</verification>

<success_criteria>
The closure's measuring instrument can see the class of leak that is still shipping, proven by a live
RED on all three tiers with paired positive controls, before any edit lands; and the SURF-03 checkbox
no longer asserts an outcome that `239-VERIFICATION.md` has already falsified.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-09-SUMMARY.md` when done.
</output>
