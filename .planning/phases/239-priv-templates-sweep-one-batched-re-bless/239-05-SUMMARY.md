---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 05
subsystem: install-templates
tags: [gap-closure, evidence, bookkeeping-sweep, surf-01, surf-03, instrument]
requires:
  - "239-04 (wave 4 gap-closure predecessor)"
  - "239-EVIDENCE.md § PREFLIGHT-UNION-LEDGER (the frozen V1 union regex V2 extends)"
provides:
  - "239-EVIDENCE.md § WIDENED-UNION-LEDGER — the V2 bookkeeping definition, per-alternation positive controls, V1↔V2 delta, per-hit triage table, overflow rule"
  - "The measured, dispositioned FIX-239-06 list plan 239-06 executes against"
  - "priv/templates/.../login_html.ex with the `during UAT` leak closed and IN-04 collapsed"
  - "Two pending todos carrying V2 into Phase 241 SURF-04 and flagging SURF-02's false Complete mark"
affects:
  - "239-06 (its fix list is now measured, and 1 row wider than the verifier's enumeration)"
  - "239-07 (the login_html.ex mirror into test/example is still that plan's commit — untouched here)"
  - "239-08 (four golden-tree rows clear at the single re-bless; one of them is this plan's fix)"
  - "Phase 241 SURF-04 (p18 guard must inherit V2, not V1)"
tech-stack:
  added: []
  patterns:
    - "Every zero-assertion grep is paired with a positive control on the same surface"
    - "Brace group + `|| true` inside command substitution so a zero-match grep is a measurement, not a fatal status under `set -eo pipefail`"
    - "Adopter-surface claims are made on generated bytes (install-smoke app), never on priv/templates/ (D-15)"
key-files:
  created:
    - .planning/todos/pending/2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md
    - .planning/todos/pending/2026-09-17-surf-02-marked-complete-but-does-not-hold-at-head.md
  modified:
    - priv/templates/sigra.install/core/login_html.ex
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
decisions:
  - "V2 adds six alternations, not the verifier's five: `\\b[Ww]ave [0-9]` is included because GSD wave numbering is the same class of internal bookkeeping and the verifier's own wider-net command already used it."
  - "The `actions/runs/[0-9]+` positive control uses `SEED-006-...md` rather than `239-VERIFICATION.md`; the verification report cites run IDs as bare numbers, not URLs, so it returns 0 there. Recording the surface that actually fires is the point of a control."
  - "All of this plan's work lands as ONE path-scoped commit per the plan's Task 3 instruction (D-19 commit topology), overriding the executor default of one commit per task."
  - "The install-smoke scratch app is `/tmp/sigra_239_05_app`, not the plan's `/tmp/sigra-239-05-app`: `mix phx.new` rejects hyphens in an inferred app name."
metrics:
  duration: "~35m"
  completed: 2026-09-17
actuals:
  tokens: 12953
  tasks: 3
  commits: 1
status: complete
---

# Phase 239 Plan 05: Widen the Bookkeeping Definition, Close the UAT Leak, Carry It Forward — Summary

Fixed the instrument before the leak: the frozen V1 union regex certified `priv/templates/` and the
golden tree clean at **0 hits** while **8 lines of Sigra-internal bookkeeping were still shipping**;
V2 sees all of them, every one is now triaged to exactly one disposition, and the one leak this plan
owned is gone from a freshly generated app.

## What Was Built

### 1. `239-EVIDENCE.md` § `## WIDENED-UNION-LEDGER` (new slot)

**V2, verbatim** — V1 extended (never retyped) with six appended alternations:

```
\.planning/|[Pp]hase[ -][0-9]+|\bD-[0-9]{2}\b|\bPlan [0-9]{2}\b|[0-9]{3}-[A-Z0-9-]+\.md|[0-9]{2}-CONTEXT\.md|SC-[0-9]|ORG-UX-[0-9]{2}|GATE-0[0-9]|UI-SPEC|DX-[0-9]{2}|IN-[0-9]{2}|T-[0-9]+-[0-9]+|\bB[0-9]\b|\b[0-9]{3}-[0-9]{2}\b|\b[Rr]ound[s]?[ -][0-9]|\b[Rr]uns? [0-9]{9,}|actions/runs/[0-9]+|\bUAT\b|\b[Ww]ave [0-9]
```

**Per-alternation positive controls — all six live, none dead:**

| Alternation | Control surface | Count |
|---|---|---|
| `\b[0-9]{3}-[0-9]{2}\b` | `239-VERIFICATION.md` | 10 |
| `\b[Rr]ound[s]?[ -][0-9]` | `239-VERIFICATION.md` | 4 |
| `\b[Rr]uns? [0-9]{9,}` | `239-VERIFICATION.md` | 2 |
| `actions/runs/[0-9]+` | `SEED-006-...md` | 3 |
| `\bUAT\b` | `239-VERIFICATION.md` | 9 |
| `\b[Ww]ave [0-9]` | `239-EVIDENCE.md` | 2 |

**V1 ↔ V2 delta — the instrument gap, measured:**

| Surface | V1 | V2 (pre-fix) | V2 (final HEAD) |
|---|---|---|---|
| `priv/templates/` | **0** | **5** | **4** |
| `test/fixtures/install_golden/tree/` | **0** | **4** | **4** |
| `test/fixtures/install_golden/STDOUT.txt` | **0** | **0** | **0** |

Paired positive control on the `priv/templates/` measurement: 2 files with hits against **97**
greppable files (`grep -lc defmodule $(git ls-files priv/templates) | wc -l`) — a clean tree, not an
empty file list.

**Per-hit triage — 8 rows (4 + 4 + 0), one disposition each:**

| Disposition | Count | Rows |
|---|---|---|
| `FIX-239-06` | **3** | `sigra_auth.css:524` (`round-3`), `:531` (`runs 30518012012` + `round-3`), `:696` (`231-02`) |
| `FALSE-POSITIVE` | **1** | `sigra.gen.oauth/oauth_html.ex:54` — `5.373-12` is SVG `path d=` geometry in the Facebook button, two float coordinates, not a plan ID |
| `OUT-OF-SCOPE` | **0** | — |
| `MIRRORS-<template>` | **4** | golden `session_html.ex:8` → `login_html.ex`; golden `sigra_auth.css:524/:531/:696` → template `sigra_auth.css` |

Every golden hit resolves to a source template — **no orphan golden hit**, so the stop-the-line
condition did not fire.

### 2. The leak closed in `priv/templates/sigra.install/core/login_html.ex`

`during UAT` removed; the pre-existing IN-04 `LiveView's / LiveView` duplication collapsed to a
single `LiveView's form-submission attributes`; the causal claim preserved verbatim in substance
(`SessionController.create/2` still cited). `MIX_ENV=test mix compile --warnings-as-errors` exits 0
with no output.

### 3. Observed gone on generated bytes (not the source tree)

`scripts/ci/install-smoke.sh` scaffolded a fresh Phoenix 1.8.8 app, installed Sigra, ran
`mix sigra.gen.oauth`, and compiled `--warnings-as-errors` (`SMOKE_EXIT=0`).

- `grep -rhE '\bUAT\b' <app>/lib <app>/priv | wc -l` → **0**
- paired positive control `grep -rhc defmodule <app>/lib <app>/priv` → **94** across **93 files**

The generated `session_html.ex` moduledoc carries the rewritten sentence. Scratch app removed
(REPO-01).

### 4. Two durable todos, neither fixed here

- `2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md` — V2 verbatim, the diagnosis (V1's
  `[0-9]{3}-[A-Z0-9-]+\.md` requires a trailing `.md`, so a bare `231-02` is unmatchable), the
  consequence (Phase 241 depends on 239, so a `p18` built on V1 freezes the blind spot permanently),
  and a pointer to the ledger's positive controls. Explicit "not built here" (Standing Constraint 5).
- `2026-09-17-surf-02-marked-complete-but-does-not-hold-at-head.md` — SURF-02 is `[x]` in
  `REQUIREMENTS.md` but `lib/sigra/account.ex:16`, `lib/sigra/audit.ex:7-14`, and
  `lib/sigra/plug/put_active_organization.ex:9` still ship bookkeeping onto HexDocs. Addressed to the
  milestone owner with an explicit uncheck-or-narrow decision; the fix belongs to Phase 241's ratchet.

## Overflow — the widened net found 1 beyond the verifier's enumeration

`sigra_auth.css:696` (`231-02`) is the row V1 was structurally blind to and the verifier's GAP-1
enumeration listed only as part of the `:689-698` block. It lives inside `priv/templates/`, so plan
239-06 absorbs it with **no scope change**. No triage row required work outside
`priv/templates/`/`test/example/`, so the "record as OUT-OF-SCOPE and file a todo, never widen the
phase" branch of the overflow rule was not triggered.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `install-smoke.sh` died on asdf toolchain resolution**

- **Found during:** Task 1
- **Issue:** The script `cd`s to `TMP_APP_DIR`'s parent (`/tmp`), where asdf finds no
  `.tool-versions` in scope: `No version is set for command mix` (exit 126).
- **Fix:** Exported `ASDF_ERLANG_VERSION=28.5 ASDF_ELIXIR_VERSION=1.19.5-otp-28` for the invocation.
  No script edit — this exact defect is **already filed** in
  `.planning/todos/pending/2026-09-17-bookkeeping-leak-guard-on-distribution-boundary.md` under
  "Adjacent gap found while verifying (same script)", so no duplicate todo was created.
- **Files modified:** none

**2. [Rule 3 - Blocking] `mix phx.new` rejected the plan's scratch-app path**

- **Found during:** Task 1
- **Issue:** `TMP_APP_DIR=/tmp/sigra-239-05-app` → *"Application name must start with a letter and
  have only lowercase letters, numbers and underscore, got: `sigra-239-05-app`"*.
- **Fix:** Used `/tmp/sigra_239_05_app`. Cosmetic path substitution; the generated app and every
  recorded count are unaffected.
- **Files modified:** none

### Deliberate departure from executor default

**Commit topology:** the executor default is one commit per task; the plan's Task 3 instructs all
three tasks to land as **one path-scoped commit**, and its acceptance criterion asserts
`git show --name-only --format= HEAD` lists exactly four paths. The plan wins — D-19 commit topology
is a phase-level contract that 239-06/07/08 depend on. One commit: `f3c7f700`.

**Tracer feedback gate:** Task 1 is `type="tracer"`. Auto mode is active for this run, so the gate
was discharged by re-running the tracer's `<verify>` end-to-end (`TRACER_OK`) before any expansion
task, rather than by emitting an interactive checkpoint.

## Known Stubs

None. Nothing in this plan is a placeholder — the ledger is measured, the fix is applied, and both
todos are deliberately unbuilt work with their diagnoses attached.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or schema change. The only source
change is a comment rewrite in an adopter-facing `@moduledoc`; no `# SECURITY:`-class rationale
sentence was touched, and `git diff --name-only origin/main -- .github/` is empty (0 lines) — T-239-05-04
mitigated.

## Verification Results

| Check | Result |
|---|---|
| Task 1 `<automated>` | `TRACER_OK` |
| Task 2 `<automated>` | `TRIAGE_PRESENT` |
| Task 3 `<automated>` | `TODOS_OK` |
| `grep -n 'during UAT' login_html.ex` | no output, exit 1 |
| `grep -c "LiveView's" login_html.ex` | 1 |
| `grep -n 'SessionController.create/2' login_html.ex` | matches (line 9) |
| `grep -cE '<V2>' login_html.ex` | 0, paired with `grep -c defmodule` → 1 |
| `MIX_ENV=test mix compile --warnings-as-errors` | exit 0, no output |
| `scripts/ci/install-smoke.sh` | `SMOKE_EXIT=0` |
| generated app `\bUAT\b` / `defmodule` | 0 / 94 across 93 files |
| `git show --name-only --format= HEAD` | exactly 4 paths |
| `git diff --name-only origin/main -- .github/` | empty |
| `git diff --name-only -- test/example test/fixtures .github` | empty |

**Working-tree note:** `git status --porcelain` after the commit shows only ` M .planning/STATE.md`,
which was already modified before this plan started (orchestrator bookkeeping) and is deliberately
not staged into the four-path commit.

## Flagged Assumptions (unresolved, per the plan's frontmatter)

- **SURF-01 edge:** the assumption that SURF-01's edge is the *empty-grep* case is honoured — every
  zero in this plan carries a paired positive control. The assumption itself remains unresolved.
- **SURF-03 edge:** commit topology. This plan's single path-scoped commit preserves the D-19 shape;
  the re-interpretation of a closure re-bless vs. a second unbatched re-bless is plan 239-08's to
  record.

## Residual Instrument Gap (recorded, not fixed)

V2 is line-based. In `sigra_auth.css`, `after rounds\n     1-2.` is split across lines 514-515 and
matches nothing; so do `Live multi-run CI evidence` (line 519, no digit after `multi-run`) and `do
not re-litigate this` (line 698). All three sit **inside** comment blocks already dispositioned
`FIX-239-06`, which 239-06 rewrites whole, so Phase 239 loses nothing — but a line-based `p18`
inherits the gap. Carried into the Phase 241 todo.

## Self-Check: PASSED

- `priv/templates/sigra.install/core/login_html.ex` — FOUND
- `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` — FOUND
- `.planning/todos/pending/2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md` — FOUND
- `.planning/todos/pending/2026-09-17-surf-02-marked-complete-but-does-not-hold-at-head.md` — FOUND
- commit `f3c7f700` — FOUND in `git log`
