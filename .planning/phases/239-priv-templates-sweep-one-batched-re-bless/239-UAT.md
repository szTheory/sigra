---
status: complete
phase: 239-priv-templates-sweep-one-batched-re-bless
source:
  - 239-01-SUMMARY.md
  - 239-02-SUMMARY.md
  - 239-03-SUMMARY.md
  - 239-04-SUMMARY.md
started: 2026-09-17
updated: 2026-09-17
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: From a cold build, `mix compile --force` then `MIX_ENV=test mix ci` exits 0 (2606 tests, 0 failures), and `mix sigra.fixture.rebless_golden --check` exits 0 with `OK: fixture is up-to-date (check mode).`
result: pass
source: automated
evidence: Cold build: `mix compile --force` exit 0; `MIX_ENV=test mix ci` exit 0 — 33 doctests, 3 properties, 2606 tests, 0 failures. `rebless_golden --check` exit 0, printed `OK: fixture is up-to-date (check mode).`

### 2. SC-1 — A freshly generated app is clean
expected: `mix phx.new` + `mix sigra.install` produces a tree whose `lib/` and `priv/` contain 0 `.planning/` hits and 0 planning-bookkeeping union-token hits — including the formerly dead `organizations.ex:59` `.planning/` path — with the positive control proving the grep was live.
result: pass
source: automated
evidence: install-smoke.sh exit 0 end-to-end (phx.new 1.8.8 scaffold + `mix sigra.install --yes Accounts User users` + compile --warnings-as-errors + ecto.migrate + 3 tests/0 failures). On the GENERATED tree, never the source tree: SC1_planning_hits=0, SC1_union_token_hits=0 against the frozen union regex. Positive controls live: 84 defmodule lines, 108 files under lib/+priv/. The historical leak site is gone — organizations.ex IS generated into the app and priv/templates/.../organizations.ex:59 now reads `# configured @sigra_org_config.` with no .planning/ path. Stronger than the SUMMARY claim: the ecto.migrate leg passed here too (Postgres was on localhost:5432), so the grep does not rest on a partially-failed run.

### 3. SC-2 — The Hex tarball is clean
expected: `mix hex.build`, unpacked, greps clean for `.planning/` under `lib/` and `priv/`; the 32 out-of-scope hits (docs/ 12, README 1, CHANGELOG 19) are enumerated rather than claimed absent, and no `sigra-*.tar` / `sigra-*/` is left behind in the repo.
result: pass
source: automated
evidence: `mix hex.build` exit 0; unpacked tarball lib/+priv/: 0 `.planning/` hits, positive control 282 files present. Out-of-scope enumerated exactly as predicted: docs 12, README 1, CHANGELOG 19, mix.exs 0 = 32. No stray sigra-*.tar left in repo (Safety Rule 8).

### 4. SC-3 — Exactly one batched re-bless, comment-only
expected: `38c9bd9a` is the single commit touching `test/fixtures/install_golden/` (35 paths, 0 outside), the comment-only classifier reports `nonconforming=0` with both non-vacuity floors cleared (removed_lines=140 ≥ floor 139), and `STDOUT.txt` is byte-unchanged.
result: pass
source: automated
evidence: 1 commit touches `test/fixtures/install_golden/` (38c9bd9a); 35 paths, 0 outside; STDOUT.txt unchanged vs origin/main.

### 5. SC-4 — Only the real counterparts were mirrored
expected: `239-MIRROR-CHECKLIST.md` disposes all 46 edited templates explicitly (30 mirrored / 4 already-absent / 12 no-counterpart), the mirror commit `e69f4d9b` is entirely `test/example/`-scoped, and the missing parity guard is filed as FUT-01 rather than built in-phase.
result: pass
source: automated
evidence: Repaired during UAT: checklist listed a non-existent path `priv/templates/sigra.install/sigra.upgrade/data_migration.exs`; real path is `priv/templates/sigra.upgrade/data_migration.exs`. After fix: 46 rows = 30 mirrored + 4 already-absent + 12 no-counterpart, bijective with the 46 paths in sweep commit cafd9a53 (set difference 0 both directions). Mirror commit e69f4d9b: 30 paths, 0 outside test/example/, parent df04e43a. FUT-01 todo exists.

### 6. SC-5 — Load-bearing infrastructure provably untouched
expected: `git diff origin/main -- .github/` shows zero paths and zero changed `name:` lines (no renamed required context), and no `# SECURITY:`-class rationale sentence was deleted.
result: pass
source: automated
evidence: `git diff origin/main -- .github/`: 0 paths, 0 changed `name:` lines.

### 7. Repaired prose reads correctly
expected: The four repaired comment sites read as complete, correct sentences — `sigra_auth.css:698`, `organization_members_live.ex:24`, and `features/core.ex:479` and `:482` — and `core.ex` no longer contains a literal capitalised `Organization` (Pitfall X-1 invariant; core_test.exs 29 tests, 0 failures).
result: pass
source: automated
evidence: All four repaired sites read as complete sentences (sigra_auth.css:698, organization_members_live.ex:24, features/core.ex:479 and :482). `grep -cE '\bOrganization\b' lib/sigra/install/features/core.ex` = 0 — Pitfall X-1 invariant holds.

### 8. Commit topology deviation is acceptable
expected: Two repair commits (`061c1758`, `e4e03980`) landing ahead of the re-bless, and the withdrawal of `d40bec7e` / `a757d517` via `git reset --mixed` (unpushed, patches saved), are an acceptable, honest departure from the strict D-19 three-commit topology.
result: pass
source: automated
evidence: d40bec7e and a757d517 both NOT-IN-HISTORY (withdrawal real, not claimed). Repair commits 061c1758/e4e03980 touch no golden path — the single-re-bless-commit constraint the rule protects is intact.

### 9. SC-5b RED accepted as an instrument gap (239-02 D1)
expected: The SC-5b false positive on `core/auth.ex:530` — where the sweep removed the citation `(10.1 IN-03)` and `IN-NN` is outside the script's narrower tolerated set — is a classifier scope mismatch, not lost security rationale. The accepted rationale in `239-EVIDENCE.md ## SWEEP-COMMIT` is sound.
result: pass
source: automated
evidence: Rationale verifiably intact in priv/templates/sigra.install/core/auth.ex: the comment still states test-only helper, HMAC-rewind/audit/telemetry bypass, and `production flows must use the signed token clause so security signals are preserved`. Only the citation `(10.1 IN-03)` was removed. Instrument scope mismatch confirmed, not lost rationale.

### 10. SC-5b RED on the full sweep-commit diff (239-02 D3)
expected: The same `auth.ex:530` family trips SC-5b on the full sweep commit diff; D-16 pins the script as unmodifiable, so recording the diagnosis + positive control (409 removed lines examined, exit 0 with the mirrors excluded) and filing a todo — rather than widening the instrument — is the right disposition.
result: pass
source: automated
evidence: SC-5b over the full phase diff exits 1 on exactly one line family in 3 mirrored locations. Positive control (same script, auth.ex/accounts.ex mirrors excluded) examined 440 removed lines and exited 0 — the check was live and this family is its sole trip. Todo filed; D-16 pins the script.

### 11. Pre-existing working-tree drift, not scope creep (239-02 D2)
expected: The uncommitted `.planning/state.json` change flagged by the Task 2 path-scope check was present before the plan started and was never staged into the sweep commit `cafd9a53` — pre-existing drift, not sweep scope creep.
result: pass
source: automated
evidence: Sweep commit cafd9a53: 46 paths, 0 outside priv/templates/, 0 `.planning/` paths. The flagged state.json drift was never staged. Working tree now carries only this UAT run's own new artifacts.

### 12. Pre-existing example compile warning filed as a todo (239-03 D1)
expected: `MIX_ENV=test mix compile --warnings-as-errors` inside `test/example` fails only on the pre-existing SettingsLive dev-only `~p"/dev/mailbox"` route (confirmed via `git stash` against unmodified HEAD); filing it as a todo (`df04e43a`) rather than fixing in-phase is correct under the v1.48 standing constraint.
result: pass
source: automated
evidence: test/example compile --force --warnings-as-errors exits 1 on exactly ONE warning: `no route path for ExampleWeb.Router matches "/dev/mailbox"` at settings_live.ex:133. That line is byte-identical at origin/main and the file is untouched by phase 239 (positive control: the same query returns e69f4d9b for user_auth.ex, a file the phase did touch). Todo df04e43a is the right disposition.

### 13. Trailing evidence commit resolution (239-03 D3)
expected: Recording the mirror commit's sha and `mix ci` run #1 in a trailing commit (`0f24144c`) — because a commit cannot contain the sha of a commit that must follow it — with the mirror commit's own path-scope and `.github/` assertions verified against `e69f4d9b` directly, is faithful to the plan's intent (an honestly-scoped, unpolluted mirror commit).
result: pass
source: automated
evidence: Topology confirmed: df04e43a (checklist) -> e69f4d9b (mirror, 30 paths all test/example/) -> 0f24144c (evidence, sole path 239-EVIDENCE.md). A commit cannot contain the sha of a commit that must follow it, so the trailing-evidence form is the only faithful one; it matches the 239-01 (aa37775d) and 239-02 (2b362456) precedent. Both ledger shas resolve correctly once de-chunked (`cafd9a-...-7537` -> cafd9a53, `e69f4d-...-d884` -> e69f4d9b) — the hyphen-chunking is the deliberate 4512c462 SIGPIPE false-multi-match fix, not a broken reference.

### 14. Deferred CI-job clause is honestly recorded
expected: SC-3's `install_golden_contract` Actions-job clause is unobservable locally (nothing pushed) and is recorded as deferred in `239-EVIDENCE.md ## HONEST-CLAIMS` rather than asserted from a local proxy — the correct disposition, to be closed at ship time.
result: pass
source: automated
evidence: `## HONEST-CLAIMS` records the install_golden_contract ship-time deferral explicitly. All three claimed todos exist in .planning/todos/pending/.

## Summary

total: 14
passed: 14
issues: 0
pending: 0
skipped: 0
human_questions_asked: 0
verification_mode: automated (all 14 machine-verified with positive controls)
repairs_applied: 1 (Test 5 — SC-4 checklist path typo)

## Gaps

[none — all 14 checkpoints machine-verified pass]

## Repairs Applied During UAT

- test: 5
  defect: "239-MIRROR-CHECKLIST.md row listed `priv/templates/sigra.install/sigra.upgrade/data_migration.exs`, a path that does not exist; the real file is `priv/templates/sigra.upgrade/data_migration.exs`."
  severity: cosmetic
  disposition: fixed in place — disposition was already correct, only the path string was wrong. Post-fix the checklist is bijective with the 46 paths of sweep commit cafd9a53 (set difference 0 both directions).

## Follow-Ups Filed

- todo: 2026-09-17-bookkeeping-leak-guard-on-distribution-boundary.md
  why: "SC-1 and SC-2 were proven true once, at one sha, by hand. `install-smoke.sh` contains no planning grep and no test touches the hex.build tarball — the leak can silently return. This is the one checkpoint in this phase with recurring CI value."
  blocked_by: "Phase 239 SC-5 asserts `git diff origin/main -- .github/` is empty; adding the gate in-phase would invalidate the phase's own success criterion. Lands next."

## Verification Notes (automated run)

- `install-smoke.sh` is not locally runnable as written: it operates in `TMP_APP_DIR`'s parent, where asdf finds no `.tool-versions`, so `mix` resolves to nothing and it dies before scaffolding. CI has a single global toolchain so this never fires there. Worked around by placing `.tool-versions` in the run directory. Folded into the guard todo above.
- One grep in this run produced a FALSE NEGATIVE: checking whether the 239-02 evidence commit recorded the sweep sha returned 0. The ledger deliberately hyphen-chunks shas (`cafd9a-5328b5-...-7537`) as the 4512c462 SIGPIPE false-multi-match fix. De-chunked, both shas resolve correctly. Do not re-investigate.
- CI-value triage (recurring vs one-shot): SC-3's classifier is frozen to a pre-sweep sha and is dead after this phase — `rebless_golden --check` is already its durable form in CI. SC-4/SC-5/commit-topology are phase-scoped artifacts. Prose is durably covered by core_test.exs Pitfall X-1 plus --warnings-as-errors. The cold-start footgun (`mix ci` ends in sigra.dep_off and poisons the next cold run) is local-only — CI is always cold, so a gate there would never fire.
