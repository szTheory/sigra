diff --git a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts/organization_invitation.ex b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts/organization_invitation.ex
index 11d4ff19..98c84fc8 100644
--- a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts/organization_invitation.ex
+++ b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp/accounts/organization_invitation.ex
@@ -10,9 +10,9 @@ defmodule SigraInstallGoldenTmp.Accounts.OrganizationInvitation do
   - **Accepted:** `accepted_at` is set
   - **Revoked:** `revoked_at` is set
 
-  The `hashed_token` field stores the HMAC-signed invitation token.
-  Implements the full invitation flow (generation, email
-  delivery, accept/reject).
+  The `hashed_token` field stores the HMAC-signed invitation token. The
+  library implements the full invitation flow (generation, email delivery,
+  accept/reject); this schema only persists the state.
 
   This schema is generated by `mix sigra.install` and is owned by your
   application.
diff --git a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/controllers/session_html.ex b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/controllers/session_html.ex
index b9f47d29..d6214c8b 100644
--- a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/controllers/session_html.ex
+++ b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/controllers/session_html.ex
@@ -4,9 +4,9 @@ defmodule SigraInstallGoldenTmpWeb.SessionHTML do
 
   The login page is a plain controller +
   HEEx template in BOTH `--live` and `--no-live` installs. LiveView's
-  LiveView form submission attributes were swallowing the browser form
-  submit during UAT. With no LiveView process on the page, the browser
-  performs a real HTTP POST to `SessionController.create/2`.
+  form-submission attributes were swallowing the browser form submit.
+  With no LiveView process on the page, the browser performs a real
+  HTTP POST to `SessionController.create/2`.
 
   Two separate form assigns (`@form` and `@magic_link_form`) isolate
   validation/flash state so an error on one form does not corrupt the
diff --git a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/invitation_accept_live.ex b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/invitation_accept_live.ex
index f9325ce4..b3cf9dc7 100644
--- a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/invitation_accept_live.ex
+++ b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/invitation_accept_live.ex
@@ -16,7 +16,8 @@ defmodule SigraInstallGoldenTmpWeb.InvitationAcceptLive do
   The `:mismatch` render branch contains ZERO `phx-click`/`phx-submit`
   accept controls. Even if every server-side guard regressed, there is
   no accept form in the rendered DOM to submit. This is the
-  def handle_event("open_remove_modal", %{"id" => id}, socket) do
+  "by construction, not by convention" defense. That absence is asserted by a
+  test, not merely conventional — do not add accept controls to this branch.
 
   The `:invalid`, `:expired`, `:revoked`, and `:already_accepted`
   branches also render zero accept controls (defense in depth).
diff --git a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
index dbaac936..19e58dec 100644
--- a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
+++ b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
@@ -600,8 +600,8 @@ defmodule SigraInstallGoldenTmpWeb.MFASettingsLive do
     form = to_form(%{"code" => code}, as: "enroll")
     socket = assign(socket, enroll_form: form)
 
-    # Auto-submit when 6 digits entered:
-    # call the confirm path directly instead of dispatching via
+    # Auto-submit when 6 digits entered. Calls the confirm path
+    # directly instead of dispatching via
     # `send(self(), …)`. The mailbox round-trip allowed a stale 6-digit
     # prefix to fire after the user typed a 7th character, wasting an
     # attempt against the MFA lockout counter.
diff --git a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organization_members_live.ex b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organization_members_live.ex
index 9a5626ec..d372cb1a 100644
--- a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organization_members_live.ex
+++ b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organization_members_live.ex
@@ -5,14 +5,14 @@ defmodule SigraInstallGoldenTmpWeb.OrganizationMembersLive do
   Route: `GET /organizations/:org/members` (owner/admin only — enforced by the
   `:org_scoped` pipeline + `Sigra.Plug.RequireMembership role: [:owner, :admin]`).
 
-  Generated by
-  `mix sigra.install --live`; this file is host-owned, customize freely.
+  Generated by `mix sigra.install --live`; this file is host-owned, customize freely.
 
   ## Architecture
 
-    * Two LiveView streams: `@streams.members` (populated on mount) and the
-      `pending-invitations-section` seam (currently an empty-state
-      card with a HEEx comment marker).
+    * Two LiveView streams: `@streams.members` (populated on mount) and
+      `@streams.pending_invitations`, rendered in the
+      `pending-invitations-section` `<section>`, which falls back to an
+      empty-state card when `@pending_count == 0`.
     * Row mutations (role change, remove) funnel through native `<dialog
       class="modal">` confirmation modals. The modal stays open when the
       server returns `{:error, :last_owner}` and renders an inline error above
@@ -21,15 +21,14 @@ defmodule SigraInstallGoldenTmpWeb.OrganizationMembersLive do
       purges `user_sessions` rows scoped to the removed user + this org inside
       the same transaction (force-logout).
     * Pagination is `LIMIT 100` + "Load more" via `stream_insert(..., at: -1)`
-      only. Flop / sortable columns are a v1.2 concern.
+      Flop / sortable columns are a v1.2 concern.
 
   ## Pending invitations seam
 
   The `pending-invitations-section` `<section>` below the members table
-  currently renders an empty-state card. This section will replace the card body
-  with a populated `@streams.pending_invitations` and add an "Invite member"
-  action above the table. Look for the `this section` HEEx
-  comment to find the fill-in point.
+  renders a populated `@streams.pending_invitations` table, or an empty-state
+  card when `@pending_count == 0`. The "Invite member" action sits above the
+  table.
   """
   use SigraInstallGoldenTmpWeb, :live_view
 
diff --git a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organizations_live/index.ex b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organizations_live/index.ex
index 1e559968..8700e8ee 100644
--- a/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organizations_live/index.ex
+++ b/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organizations_live/index.ex
@@ -10,8 +10,8 @@ defmodule SigraInstallGoldenTmpWeb.OrganizationsLive.Index do
     * `([], [])` — Branch A: zero-state hero + inline create form
       (also the post-signup destination via the zero-line
       registration path)
-    * `([], [_|_])` — Branch B: pending invitations list (wires Accept;
-      renders Accept disabled until then)
+    * `([], [_|_])` — Branch B: pending invitations list with an Accept
+      action for each invitation
     * `([_|_], _)` — Branch C: picker with per-row switch forms
 
   Edit freely — this file is your code.
diff --git a/test/fixtures/install_golden/tree/priv/static/assets/sigra_auth.css b/test/fixtures/install_golden/tree/priv/static/assets/sigra_auth.css
index c795ebc5..997e50e2 100644
--- a/test/fixtures/install_golden/tree/priv/static/assets/sigra_auth.css
+++ b/test/fixtures/install_golden/tree/priv/static/assets/sigra_auth.css
@@ -511,26 +511,23 @@
     text-decoration: underline;
   }
 
-  /* Live multi-run CI evidence showed H2/P sharing one
-     identical right edge (321.4375px) as the last remaining offenders after rounds
-     1-2. Neither element had min-width: 0, so as grid items of a
-     single-implicit-column .sigra-auth-stack, both stretch to the shared column
-     track's width, which is itself floored by whichever sibling's own min-content is
-     largest (here, the <p> copy's longest unbreakable word). min-width: 0 lets each
-     item shrink.
-     This rule's selector,
+  /* H2 and P shared one identical right edge with the surrounding stack.
+     Neither had min-width: 0, so as grid items of a single-implicit-column
+     .sigra-auth-stack both stretch to the shared column track's width, which is
+     itself floored by whichever sibling's own min-content is largest (here, the
+     <p> copy's longest unbreakable word). min-width: 0 lets each item shrink.
+
+     Use `overflow-wrap: anywhere` here, NOT `break-word`: this rule's selector,
      `.sigra-auth p`, has higher specificity (0,1,1) than the pre-existing
-     `.sigra-auth__product` rule (0,1,0) below and appears later in the file, so an
-     initial round-3 draft that used `overflow-wrap: break-word` here silently
-     overrode that rule's `overflow-wrap: anywhere` for the wordmark <p>. Per the CSS
-     Text spec, `anywhere` (not `break-word`) is what participates in a flex/grid
-     item's automatic-minimum-size calculation -- `break-word` only affects actual
-     render-time wrapping, not intrinsic sizing -- so that override reintroduced an
-     un-contained min-content floor on the wordmark and produced a much larger,
-     bursty overflow (scrollWidth 445-473px vs innerWidth 320px, observed on live CI
-     runs 30518012012 and 30518015684 immediately after the round-3 commit, never
-     before it). Using `anywhere` here matches .sigra-auth__product's already-proven
-     value, so the cascade agrees everywhere instead of silently overriding it. */
+     `.sigra-auth__product` rule (0,1,0) below and appears later in the file, so
+     `break-word` here would silently override that rule's `overflow-wrap:
+     anywhere` for the wordmark <p>. Per the CSS Text spec, `anywhere` (not
+     `break-word`) is what participates in a flex/grid item's
+     automatic-minimum-size calculation -- `break-word` only affects actual
+     render-time wrapping, not intrinsic sizing -- so that override would
+     reintroduce an un-contained min-content floor on the wordmark. Using
+     `anywhere` here matches .sigra-auth__product's value, so the cascade agrees
+     everywhere instead of silently overriding it. */
   .sigra-auth h1,
   .sigra-auth h2,
   .sigra-auth h3,
@@ -693,17 +690,14 @@
      <.input> core component's <div class="fieldset mb-2"> wrapper (and the <label>
      inside it) is itself a grid item of the form. DaisyUI's compiled .fieldset is
      ALSO display:grid, so this un-contained grid-item ancestor still carries its
-     automatic minimum width (min-content) even though 231-02's min-width: 0 already
-     reached the leaf input/button. That leaf-only fix was insufficient on its own --
-     do not re-litigate this; this comment records the verified mechanism
-     and the reflow failure payload (DIV.fieldset/LABEL/SPAN.label/INPUT/BUTTON sharing
-     one identical right edge) that proves it. */
+     automatic minimum width (min-content); the earlier leaf-level min-width: 0
+     reached only the input/button, which is why that leaf-only fix was
+     insufficient on its own. */
   .sigra-auth form :where(.fieldset, label) {
     min-width: 0;
   }
 
-  /* Live multi-run CI evidence
-     showed a residual, distinct overflow AFTER the grid-item containment fix above --
+  /* A residual, distinct overflow survives the grid-item containment fix above --
      SPAN.label (the field-label wrapper Phoenix's default <.input> core component
      renders) inherits DaisyUI's compiled .label utility class, which sets
      white-space: nowrap. At 320px / 32px root font (WCAG 1.4.4 2x zoom simulation), a
