---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 11
type: execute
wave: 11
depends_on: ["239-10"]
files_modified:
  - priv/templates/sigra.install/organizations/live/invitation_accept_live.ex
  - priv/templates/sigra.install/organizations/live/organization_members_live.ex
  - test/example/lib/example_web/live/invitation_accept_live.ex
  - test/example/lib/example_web/live/organization_members_live.ex
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The two residual bookkeeping sentences named in `239-VERIFICATION.md`'s SC-1 `gaps:` block are gone from `priv/templates/` — proven by a named-string grep on the bytes, independently of any regex: a literal `grep -rn` for each sentence over `git ls-files priv/templates` returns 0, paired with a non-zero `defmodule` control on the same file list."
    - "`239-v3-vocabulary-check.sh priv-templates` exits 0 printing `hits_outside_allowlist=0` alongside its raw `hits=` total and a non-zero `control_defmodule=` — the instrument that was demonstrated RED on this tier in plan 239-09 now reports clean on the same tier, with the same definition, the same tier file list, and the same allowlist (D-30). The RED-then-GREEN pair is what makes the zero admissible. The raw total is expected to remain `1` on this tier — the allowlisted SVG-coordinate false positive in `sigra.gen.oauth/oauth_html.ex` — and that number is recorded, not hidden."
    - "`239-v3-vocabulary-check.sh example` exits 0 with `hits_outside_allowlist=0` over the SCOPED SC-4 counterpart file set fixed in plan 239-09 — the two files this plan edits. It is not run over `git ls-files test/example`, and this plan does not touch, claim, or measure-as-clean the unswept remainder of `test/example/`, which plan 239-09 measured and routed to Phase 241 SURF-04 (D-30)."
    - "Neither plan modifies the instrument or the allowlist: `git diff --name-only <base>..HEAD -- .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-allowlist.tsv` is empty across both of this plan's commits. The green is produced by editing the surface, never by editing the measurement."
    - "The WR-01 false safety claim is gone: the adopter-shipped moduledoc no longer asserts that the adopter's own copy is covered by a test. The replacement prose states what is true for an adopter — that Sigra's suite asserts the absence in the shipped template, that `mix sigra.install` generates no tests, and that a customizer must add their own assertion."
    - "The WR-01 replacement is checked against reality, not against itself: `mix sigra.install`'s generated `test/` tree is re-observed to contain zero `_test.exs` files, and that observation is recorded next to the new prose."
    - "WR-03 is repaired: the pagination bullet in `organization_members_live.ex` ends in a sentence terminator, in all three tiers that carry it."
    - "The commit topology holds (D-19): the `priv/templates/` sweep and the `test/example/` mirror are two separate commits, the sweep commit lists only paths under `priv/templates/`, and the mirror commit lists only paths under `test/example/` plus phase-directory artifacts."
    - "`test/example/` is byte-consistent with `priv/templates/` for both edited files, modulo the module-name substitution the templates' EEx performs — asserted by a per-file diff of the changed regions, recorded in `239-MIRROR-CHECKLIST.md` rather than claimed."
    - "The mirror enumeration is re-derived from the files, not inherited: `test/example/lib/example_web/router.ex:103` and `:175` are both re-read and their disposition recorded, because a prior plan's enumeration missed `:175`."
    - "No executable behavior changed: every `+`/`-` line in the sweep and mirror diffs lies inside a `@moduledoc`/`@doc` heredoc or a `#` comment — no function head, guard, pattern, pipeline, route, plug, `attr` name, or `default:` is touched."
    - "`mix format --check-formatted` passes on all four edited Elixir files."
    - "`237-security-comment-diff-check.sh` run over this plan's combined diff exits 0 with `examined_removed_lines` > 0, or its single documented structural survivor is re-stated as the known instrument limitation — never by editing the script (D-16)."
    - { statement: "Removing the two sentences removed bookkeeping and not the reason a constraint exists — each removal is checked against the surrounding block to confirm the constraint it guarded is still stated by a surviving line, and that surviving line is quoted in the evidence record", verification: backstop }
  artifacts:
    - priv/templates/sigra.install/organizations/live/invitation_accept_live.ex
    - priv/templates/sigra.install/organizations/live/organization_members_live.ex
    - test/example/lib/example_web/live/invitation_accept_live.ex
    - test/example/lib/example_web/live/organization_members_live.ex
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  key_links:
    - "The WR-02 line and the WR-01 sentence sit in the same file and contradict each other: line 19 claims a test enforces the invariant, line 326 claims internal tooling does. Fixing one without the other leaves an adopter unable to act on either. Both are in this plan's single sweep commit by design."
    - "The `DO NOT add an accept button here` lines already in the structural-invariant block carry the full warning, which is why the WR-02 line can be deleted outright with zero information loss (239-REVIEW.md states this). That surviving warning is the constraint the removal must not take with it."
    - "`test/example/` is hand-mirrored and drifts behind `priv/templates/` — the standing installer-template-drift lesson. The per-file checklist exists because a blanket mirror would silently miss a renamed counterpart (D-21)."
    - "Deleting a whole line shifts every line number below it in all three tiers. The golden fixture's line numbers are regenerated by plan 239-12's re-bless; no line number recorded in this plan's evidence should be quoted as still valid after that."
  prohibitions:
    - statement: "Must NOT remove the reason a constraint exists while removing its bookkeeping. This is WR-04's exact failure mode, one plan earlier in the same phase. Before each deletion, identify the surviving line that still states the constraint and quote it in the evidence record; if no such line survives, rewrite instead of deleting."
    - statement: "Must NOT replace bookkeeping with a claim stronger than what is true for an adopter. This is WR-01's exact failure mode. Every replacement sentence is checked against the generated app, not against the Sigra repo — the adopter's project is the subject, and Sigra's own test suite is not part of it."
    - statement: "Must NOT weaken a security-rationale sentence while leaving it technically present. SC-5's control only catches *deleted* SECURITY-class sentences; a sentence rewritten into vagueness passes that control and is prohibited here by name."
    - statement: "Must NOT hand-edit `test/fixtures/install_golden/`. The golden tier is reached only by `mix sigra.fixture.rebless_golden` in plan 239-12 (D-09). A golden path in this plan's commits is a stop-the-line event."
    - statement: "Must NOT run a repo-wide substitution. Every edit is applied by path to one of the four named files (D-23) — a repo-wide sweep could rewrite a `.github/` `name:` line and hang every PR forever."
    - statement: "Must NOT fold the mirror into the sweep commit, or either into a re-bless commit. D-19's three-commit separation is what lets a reviewer tell hand edits from generated drift."
    - statement: "Must NOT edit `239-v3-vocabulary-check.sh` or `239-v3-allowlist.tsv` to turn a red verify green. Both are frozen by plan 239-09 (D-30). If a tier will not go green, the surface is wrong or the plan is wrong — the instrument is not the thing that moves. If a genuinely new false positive appears, halt and report it to the operator rather than adding an allowlist row mid-plan."
    - statement: "Must NOT widen the `example` tier beyond the SC-4 counterpart file set, and must NOT sweep the unswept remainder of `test/example/`. That remainder is measured and routed by plan 239-09 (D-30); sweeping it here is 4.3× over the SC-4 scope (`239-RESEARCH.md` § 1.6) and would blow this plan's two-file commit scope and D-19's commit topology."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `adjacency`, UNRESOLVED): 'when two things are exactly equal or just touch, do they merge, collide, or separate?' This plan owns the template/example adjacency: the two tiers must end up equal modulo module naming, and this plan asserts that per-file by diffing the changed regions. The probe row itself stays unresolved — nothing mechanically enforces the relationship, which is FUT-01's filed and unbuilt todo."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `ordering`, UNRESOLVED): the D-19 sweep-then-mirror ordering is asserted here by commit-scope checks, not by tooling. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): the dead-grep case. Every zero in this plan is paired with a `defmodule` control on the same file list. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `empty`, UNRESOLVED): an empty `git ls-files` argument list would make every grep here return zero. Mitigated by the paired controls; surfaced, not resolved."
---

<assumption_delta_decision>
**Outcome: `no-change`.** The `pluralization`/`second` signal is the D-26 batch-count amendment prose
and carries no identity-model consequence: no noun lost its monopoly, no key or anchor moved. The
residue it names (an unbounded per-batch count) is answered by D-29, recorded in plan 239-10.
</assumption_delta_decision>

<objective>
Remove the two Sigra-internal bookkeeping sentences that still ship into every generated app, and
repair the two prose defects this phase introduced into the same two files, in one sweep commit
followed by one mirror commit.

Purpose: `239-VERIFICATION.md` failed SC-1 on these two sentences. They are in all three tiers. Two
of the code review's four warnings — WR-01's false safety claim on an authorization-bypass defense,
and WR-03's fused unterminated sentence — are in the same two files, so opening them for the SC-1 fix
and not repairing them would ship a known defect for the cost of nothing. WR-02 *is* one of the two
SC-1 sentences.

WR-01 deserves its own sentence of purpose. Plan 239-06 replaced bookkeeping with prose asserting the
adopter has a regression guard on an authorization-bypass defense (Jetstream #907 / CVE-2026-1529)
that they do not have. `mix sigra.install` generates zero `_test.exs` files; the test that exists is
a Sigra-repo test that reads the *template*, never the adopter's copy. That is strictly worse than
the bookkeeping it replaced, and it is already blessed into the golden fixture where it reads as
verified. The replacement written here is checked against the generated app, not against the Sigra
repo.

Output: two template files clean under the V3 definition demonstrated RED in plan 239-09, their
`test/example/` counterparts mirrored in a separate commit, a per-file checklist entry for each, and
an evidence record naming the surviving line that still states each constraint whose bookkeeping was
removed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **Two commits, in order** (D-19): sweep (`priv/templates/` only), then mirror (`test/example/`
   only, plus phase-directory artifacts). Never one commit. Never a golden path in either.
2. **`test/fixtures/install_golden/` is untouched here** (D-09). Plan 239-12 owns it.
3. **Path-restricted edits only** (D-23). No repo-wide substitution, no `.github/` edit.
4. **Every zero is paired with a positive control on the same surface** (Standing Constraint 1).
5. **A zero-match grep must be a measurement, not a fatal status.** Under `set -e`, wrap counting
   greps: `n=$( { grep -c … || true; } )`.
6. **Evidence at the final committed HEAD on a clean tree** (Standing Constraint 2).
7. **`mix ci`, never root `mix test`** (Standing Constraint 3). This plan runs `mix format
   --check-formatted` only; plan 239-13 owns the full gate.
8. **The repo is public.** No home-directory paths, adopter PII, or brand hex values in any artifact.
9. **Do not push.** Commit only, ending messages with
   `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

<tasks>

<!-- planner-discipline-allow: asserted by a test, not merely conventional -->
<!-- planner-discipline-allow: asserted by a test -->
<!-- planner-discipline-allow: merely conventional -->
<task type="auto" tdd="false">
  <name>Task 1: Batch-3 sweep — remove the two residual bookkeeping sentences and repair WR-01 and WR-03 in priv/templates, in one commit</name>
  <files>priv/templates/sigra.install/organizations/live/invitation_accept_live.ex, priv/templates/sigra.install/organizations/live/organization_members_live.ex, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>`.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh` exists, is executable, `239-v3-allowlist.tsv` is committed alongside it, and the script exits 1 with `hits_outside_allowlist >= 1` when run against the `priv/templates/` tier — the RED demonstration from plan 239-09 still holds at this plan's base. An exit 0 means the instrument no longer sees the leak it was built to see; an exit 3 means the instrument cannot answer at all. Either way: halt and report, do not proceed.</precondition>
  <read_first>
    - `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` — read lines 1-40 (the `@moduledoc`, including the `## Structural Jetstream #907 defense` section and the two sentences at `:19-20`) and lines 315-335 (the `# STRUCTURAL INVARIANT (Jetstream #907 / CVE-2026-1529):` comment block above `defp render_mismatch/1`, including the line at `:326` and the `DO NOT add an accept button here` lines below it). Read the current bytes; do not assume the line numbers from any prior artifact still hold.
    - `priv/templates/sigra.install/organizations/live/organization_members_live.ex` — read lines 1-35 (the whole `@moduledoc`), specifically the `## Architecture` pagination bullet at `:23-24` and the `## Pending invitations seam` heading at `:26`.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md` — WR-01, WR-02 and WR-03 in full. Each carries the offending text verbatim and a suggested fix. WR-02's entry states that the surviving `DO NOT add an accept button here` lines already carry the full warning, which is the justification for deleting rather than rewriting.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the SC-1 `gaps:` block, which names both sentences with their paths and line numbers, and the "Defects the phase itself introduced into adopter-shipped files" table.
    - `test/example/test/example_web/live/invitation_accept_live_test.exs` around `:582` — the `T19` test the WR-01 prose currently mis-describes. Confirm for yourself that its subject is the template file path, not a generated app. This is the observation the replacement prose depends on.
    - `test/fixtures/install_golden/tree/test/` — list it. It is the generated app's whole `test/` tree and it contains zero `_test.exs` files. This is the second observation the replacement prose depends on. Read-only; do not edit anything under `test/fixtures/`.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## CLOSURE-TEMPLATE-COMMIT` — the format plan 239-06's template commit used to record its edits. Match it.
  </read_first>
  <action>
Edit exactly two files under `priv/templates/`, by path, with no repo-wide substitution.

**In `organizations/live/invitation_accept_live.ex`:**

*WR-01, in the `@moduledoc` at `:19-20`.* The current text asserts the absence of accept controls is
"asserted by a test, not merely conventional". That is false in an adopter's project. Replace those
two lines so the paragraph reads, after the existing `"by construction, not by convention" defense`
clause: `— do not add accept controls to this branch. Sigra's own suite asserts this absence in the
shipped template; \`mix sigra.install\` generates no tests, so if you customize this file, add an
equivalent assertion to your own test suite.` Keep the surrounding paragraph and the
`## Structural Jetstream #907 defense` heading intact — the heading has exactly ONE space after the
`##`, as it appears in the file today; verify that against the bytes before building any `sed` range
from it, because a range anchored on a heading that does not exist extracts nothing and every
region-scoped grep below then passes vacuously against an empty region. The replacement swaps the
false claim for a true one and keeps the imperative, it does not shorten the security section. The
em-dash join to the existing
clause is deliberate: the imperative must not become a free-floating sentence that a later reader can
delete as redundant.

*WR-02 and SC-1 gap item 1, in the comment block above `defp render_mismatch/1`.* Delete, in full,
the single `#` comment line at `:326` — the one naming GSD's internal plan-checking tool, quoted
verbatim in `239-REVIEW.md` § WR-02 and in `239-VERIFICATION.md`'s SC-1 `gaps:` block. Delete the
whole line; do not rewrite it. Before deleting, confirm from the read that the `DO NOT add an accept
button here even "for convenience"` lines immediately below it survive and still carry the full
warning, and quote that surviving text into the evidence record as the proof that the constraint's
reason was not removed with its bookkeeping. If those lines are not present, halt — the deletion
would then be lossy and the line must be rewritten instead.

**In `organizations/live/organization_members_live.ex`:**

*WR-03 and SC-1 gap item 2, in the `## Architecture` pagination bullet at `:23-24`.* Restore the
sentence terminator on line 23, so the pagination bullet ends with a period immediately after the
closing backtick of the `stream_insert` call. Then delete line 24 in full — the second sentence,
which names an internal Sigra release-sequencing concern and is quoted verbatim in `239-REVIEW.md`
§ WR-03 and in `239-VERIFICATION.md`'s SC-1 `gaps:` block. This one edit closes both findings: the
run-on disappears because the second sentence is gone, and the terminator makes the first sentence
whole. Leave the rest of the `## Architecture` list, and the `## Pending invitations seam` heading at
`:26`, byte-unchanged — IN-01 through IN-04 remain open in `239-REVIEW.md` and are deliberately not
closed by this closure, to keep the batch-3 diff minimal and line-by-line auditable.

**Then verify, before committing.** Run the named-string check: for each of the two removed sentences,
a literal `grep -rn` over `git ls-files priv/templates` returns 0, paired with a non-zero `defmodule`
count on the same file list. This check is deliberately independent of every regex in this phase —
V1, V2 and V3 are all instruments under examination, and a criterion that can only be falsified by
the instrument it validates is not falsifiable. Then run `239-v3-vocabulary-check.sh` against the
`priv/templates/` tier and require exit 0, `hits_outside_allowlist=0`, and a non-zero
`control_defmodule=`. Record the raw `hits=` total alongside — it is expected to stay `1`, the
allowlisted SVG-coordinate false positive in `sigra.gen.oauth/oauth_html.ex` dispositioned by name
in plan 239-09's ledger — because the criterion passing is not a licence to stop reporting the real
number. Do not edit the instrument or the allowlist under any circumstance (D-30); if the tier will
not go green, halt and report.

Run `mix format --check-formatted` on both edited files. Run
`.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-security-comment-diff-check.sh`
over this task's staged diff and require exit 0 with `examined_removed_lines` > 0; if its single
documented structural survivor fires, re-state it as the known instrument limitation and do not edit
the script (D-16).

Record all of it in a new `239-EVIDENCE.md` section `## BATCH-3-SWEEP-COMMIT`, matching
`## CLOSURE-TEMPLATE-COMMIT`'s format: the two files, each edit with before/after text, the surviving
constraint line quoted for each deletion, the named-string grep results with their controls, the V3
tier result with its control, the `mix format` result, the security-classifier output, and the commit
sha. Also record, explicitly, the two observations the WR-01 replacement rests on: that `T19`'s
subject is the template path, and that the generated app's `test/` tree contains zero `_test.exs`
files — with the commands that produced both.

Commit path-scoped to the two template files ONLY, message
`refactor(239): remove residual plan vocabulary and repair the false safety claim in priv/templates (SURF-01, SURF-03)`.
The `239-EVIDENCE.md` section is committed with the mirror commit in Task 2, so the sweep commit's
diff is exactly the two template files a reviewer reads line by line (D-19).
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; s="$d/239-v3-vocabulary-check.sh"; test -x "$s" || { echo "MISSING_INSTRUMENT"; exit 1; }; test -s "$d/239-v3-allowlist.tsv" || { echo "MISSING_ALLOWLIST"; exit 1; }; files=$(git ls-files priv/templates); ctl=$( { grep -l defmodule $files 2>/dev/null || true; } | wc -l ); test "$ctl" -ge 90 || { echo "CONTROL_DEAD ctl=$ctl"; exit 1; }; out=$("$s" priv-templates 2>&1); rc=$?; test "$rc" = 0 || { echo "TIER_NOT_CLEAN rc=$rc (rc=3 means the instrument could not answer, not a dirty surface)"; printf "%s\n" "$out"; exit 1; }; printf "%s\n" "$out"; printf "%s\n" "$out" | grep -q "hits_outside_allowlist=0" && mix format --check-formatted priv/templates/sigra.install/organizations/live/invitation_accept_live.ex priv/templates/sigra.install/organizations/live/organization_members_live.ex && echo SWEEP_CLEAN_WITH_CONTROL'</automated>
  </verify>
  <acceptance_criteria>
    - **Falsifiable independently of every regex in this phase.** For each of the two removed sentences, a literal fixed-string `grep -rnF` over `git ls-files priv/templates` returns 0 matches, and the paired control `grep -l defmodule` over the same file list returns a count `>= 90`. Both numbers recorded. A zero with a zero control fails this criterion.
    - `239-v3-vocabulary-check.sh priv-templates` exits `0`, prints `hits_outside_allowlist=0`, prints its raw `hits=` total (expected `1`, the allowlisted SVG-coordinate false positive) and `allowlisted=1`, and prints `control_defmodule=` with a value `>= 90`. This is the GREEN half of the RED/GREEN pair established in plan 239-09 on the same tier, with the same definition, tier file list, and allowlist. An exit `3` is an instrument failure, never a result: halt.
    - The instrument and the allowlist are byte-unchanged by this plan: `git diff --name-only HEAD~1 -- .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-vocabulary-check.sh .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-allowlist.tsv` is empty.
    - **Region extraction is proven non-empty before any region-scoped grep is trusted.** The `sed` range is anchored on the heading as it exists in the file (one space after `##`), and the extracted region is asserted to be at least 5 lines long. A region-scoped grep over an empty extraction passes vacuously and proves nothing; the length assertion is what makes the two criteria below real.
    - The WR-01 replacement is present: the extracted region contains `generates no tests` and contains `your own test suite`.
    - **The false claim is gone, checked across the line break it spans.** The claim occupies two source lines in the file today, so a single-line grep cannot express its absence. Flatten the extracted region first and then check: `sed -n '<range>p' <file> | tr '\n' ' ' | tr -s ' ' | grep -cF 'asserted by a test'` returns `0`, and the same pipeline with `grep -cF 'merely conventional'` returns `0`. Both run against the same flattened, length-asserted extraction.
    - The security imperative survives the WR-01 rewrite: the flattened extraction still contains `do not add accept controls to this branch`.
    - The WR-02 deletion is lossless: the comment block above `defp render_mismatch` still contains `DO NOT add an accept button here` and still contains `Jetstream #907 / CVE-2026-1529`. Both quoted into the evidence record.
    - The pagination bullet in `organization_members_live.ex` ends in a sentence terminator: the line containing `stream_insert` inside the `@moduledoc` ends with a period. Checked with a region-scoped grep over lines 1-35 only.
    - No executable line moved. `git diff HEAD~1 -- priv/templates` produces only `+`/`-` lines that lie inside a `@moduledoc` heredoc or begin (after leading whitespace) with `#`. Verified by listing every `+`/`-` line and classifying each; the classification is recorded, not asserted.
    - `mix format --check-formatted` exits 0 on both edited files.
    - `237-security-comment-diff-check.sh` over the sweep diff exits 0 with `examined_removed_lines` > 0, or its documented survivor is re-stated verbatim; the script file itself is byte-unchanged (`git diff --name-only` does not list it).
    - `git show --name-only --format= HEAD` lists exactly the two `priv/templates/` files — nothing under `test/`, `lib/`, `.github/`, or `.planning/`.
    - `git diff --name-only HEAD~1 -- test/fixtures/install_golden/` is empty.
  </acceptance_criteria>
  <done>Both residual bookkeeping sentences are gone from `priv/templates/`, proven by a fixed-string grep with a live control and by the V3 instrument flipping RED to GREEN on the tier it was demonstrated RED on; WR-01's false safety claim is replaced with prose true in an adopter's project and backed by two recorded observations; WR-03's run-on is repaired; and the sweep commit contains those two files and nothing else.</done>
  <reversibility rating="reversible">Two comment/moduledoc edits in template files, revertable by `git revert` of a single commit. No executable behavior depends on them. Rated reversible, so no `checkpoint:decision` is emitted.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror the batch-3 sweep into test/example and re-derive the mirror enumeration from the files</name>
  <files>test/example/lib/example_web/live/invitation_accept_live.ex, test/example/lib/example_web/live/organization_members_live.ex, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>Task 1's sweep commit is HEAD and the working tree is clean. `git show --name-only --format= HEAD` lists exactly the two `priv/templates/` files; if it lists anything else, the D-19 separation already failed and this task must not build on it.</precondition>
  <read_first>
    - `test/example/lib/example_web/live/invitation_accept_live.ex` — read lines 10-30 (the moduledoc region carrying the WR-01 sentences) and lines 320-340 (the structural-invariant comment block; the counterpart line sits at `:332` in this tier, not `:326` — the example file has drifted six lines from the template and a line number copied across tiers will edit the wrong line).
    - `test/example/lib/example_web/live/organization_members_live.ex` — read lines 1-35; the pagination bullet sits at `:23-24` in this tier.
    - `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` and `.../organization_members_live.ex` at the sweep commit — the post-edit bytes, which are the source of truth for what the mirror must say.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md` — the whole file: the row format, the three dispositions (`Mirrored →`, `n/a — already absent`, `no counterpart (delivered by injection)`), and the dated staleness-correction block from plan 239-07 which is appended rather than written over. Append in that same style; do not overwrite the existing framing.
    - `test/example/lib/example_web/router.ex` lines 95-115 and 165-185 — both router comment sites the code review flagged. `:175` was a sixth site a prior plan's enumeration missed, which is why this task re-derives the enumeration from the files instead of inheriting it.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md` — D-20 and D-21, the template→counterpart mapping and the four renamed counterparts that must be mapped by hand rather than by basename.
  </read_first>
  <action>
Apply the same two edits from Task 1 to the two `test/example/` counterparts, located by reading the
files rather than by copying line numbers across tiers — the example copies have drifted and the
counterpart of the deleted comment line sits several lines lower there.

The mirrored text must be identical to the template text modulo the module-name substitution the
templates' EEx performs (`<%= web_module %>` renders as the example's web module, `<%= app_module %>`
as the example's app module). Assert that per file: extract the changed region from both tiers, apply
the module-name substitution to the template side, and diff. A non-empty diff after substitution is a
stop-the-line event — record it and halt rather than reconciling by eye.

Re-derive the mirror enumeration from the files rather than inheriting a prior plan's list. Walk both
edited templates' counterpart mappings against D-20 and D-21, and independently re-read
`test/example/lib/example_web/router.ex` at both flagged sites. Record each site's disposition:
whether it carries any text this batch changes, and if not, why not. The router is not expected to be
edited by this batch — the point of re-reading it is that a prior plan's enumeration missed a site,
so the enumeration is confirmed complete against the bytes before the re-bless in plan 239-12 makes a
missed site expensive to discover.

Append a dated correction block to `239-MIRROR-CHECKLIST.md` in the same style plan 239-07 used:
state the date and the plan (239-11), the two rows added for this batch with their `Mirrored →`
dispositions and mirrored line counts, the router re-derivation result for both sites with their
dispositions, and the parent commit this checklist is now accurate as of. Do not rewrite the existing
rows or the existing correction block — append.

Record the mirror in `239-EVIDENCE.md`: extend the `## BATCH-3-SWEEP-COMMIT` section written in
Task 1 with a mirror subsection carrying the per-file post-substitution diff results, the router
re-derivation, and both commit shas (sweep and mirror). Confirm in the record that the three-tier
state is now template == example, with golden still stale by exactly this batch — which is the
expected state, and is precisely what plan 239-12's re-bless closes.

Run `mix format --check-formatted` on both edited example files.

Commit in two steps is NOT permitted here: one mirror commit, path-scoped to the two `test/example/`
files plus the two phase-directory artifacts, message
`refactor(239): mirror the batch-3 sweep into test/example and re-derive the mirror enumeration (SURF-03)`.
Nothing under `priv/templates/` or `test/fixtures/install_golden/` may appear in it (D-19).
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; s="$d/239-v3-vocabulary-check.sh"; test -x "$s" || { echo "MISSING_INSTRUMENT"; exit 1; }; mix format --check-formatted test/example/lib/example_web/live/invitation_accept_live.ex test/example/lib/example_web/live/organization_members_live.ex && out=$("$s" example 2>&1) && printf "%s\n" "$out" && printf "%s\n" "$out" | grep -q "hits_outside_allowlist=0" && echo EXAMPLE_TIER_CLEAN'</automated>
  </verify>
  <acceptance_criteria>
    - For each of the two removed sentences, a fixed-string `grep -rnF` over `git ls-files test/example` returns 0, paired with a `grep -l defmodule` control over the same file list returning `>= 50`. Both numbers recorded. This fixed-string check is deliberately run over the WHOLE of `test/example/` — the two sentences are literal strings and their absence repo-wide is cheap to assert — while the V3 criterion below stays scoped to the SC-4 counterpart set. The two are different claims and the record must not conflate them.
    - `239-v3-vocabulary-check.sh example` exits `0` with `hits_outside_allowlist=0` and a non-zero `control_defmodule=` over the SCOPED counterpart file set fixed in plan 239-09 — the second of the three tiers demonstrated RED now reports clean. The scoping is restated in the evidence record alongside the measured remainder figure and its Phase 241 SURF-04 owner, so a later reader cannot mistake this zero for a claim about all of `test/example/`.
    - Post-substitution tier equality is asserted per file, not claimed: for each edited file, the changed region extracted from the template with `<%= web_module %>` and `<%= app_module %>` substituted diffs empty against the same region in the example copy. The diff command and its empty output are recorded.
    - The example moduledoc carries the WR-01 replacement: `grep -c 'generates no tests' test/example/lib/example_web/live/invitation_accept_live.ex` returns `1`.
    - The example's structural-invariant comment block still contains `DO NOT add an accept button here` and `Jetstream #907 / CVE-2026-1529`.
    - `239-MIRROR-CHECKLIST.md` gained an appended dated block naming plan `239-11`, containing two `Mirrored →` rows and an explicit disposition for each of the two router sites at `test/example/lib/example_web/router.ex`. The pre-existing rows and the plan-239-07 correction block are byte-unchanged — `git diff HEAD~1 -- 239-MIRROR-CHECKLIST.md` shows `+` lines only, zero `-` lines.
    - `mix format --check-formatted` exits 0 on both edited example files.
    - `git show --name-only --format= HEAD` lists exactly four paths: the two `test/example/` files and the two phase-directory artifacts. Nothing under `priv/templates/` or `test/fixtures/`.
    - The golden tier is still stale by exactly this batch — `239-v3-vocabulary-check.sh golden` still exits `1` with `hits_outside_allowlist >= 1`, recorded as the expected state that plan 239-12 closes. A golden `hits_outside_allowlist=0` here would mean the fixture was hand-edited: halt.
  </acceptance_criteria>
  <done>`test/example/` matches `priv/templates/` for both edited files after module-name substitution, proven by an empty per-file diff rather than by inspection; the mirror enumeration is re-derived from the bytes including the router site a prior enumeration missed; the checklist is appended to, not overwritten; and the mirror is a separate commit from the sweep.</done>
  <reversibility rating="reversible">Comment/moduledoc edits in hand-maintained example files, revertable by a single `git revert`.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra repo -> adopter project (`mix sigra.install`) | Template bytes cross into a project Sigra cannot patch. An adopter reads shipped prose as authoritative about their own project's guarantees. |
| Template tier -> golden fixture | Whatever is in the template at re-bless time becomes the byte-exact record of "what an adopter generates", where a false claim then reads as verified. |
| Prose about a security invariant -> reader behavior | A claim that a guard exists changes what a reader bothers to check. This boundary is where WR-01 did its damage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-11-01 | Spoofing | `invitation_accept_live.ex` moduledoc (WR-01) | high | mitigate | Prose asserting a regression guard on an authorization-bypass defense that the adopter does not have. Replacement states what is true for the adopter and is backed by two recorded live observations: `T19`'s subject is the template path, and the generated `test/` tree contains zero `_test.exs` files. |
| T-239-11-02 | Spoofing | the replacement prose itself | high | mitigate | The replacement could also be wrong, and a re-bless would bless it into the fixture where it reads as verified. Mitigated by requiring the two observations to be re-run against the generated app in this plan rather than cited from a prior SUMMARY, and by recording the commands that produced them. |
| T-239-11-03 | Tampering | comment-block deletions | high | mitigate | Deleting bookkeeping could take the constraint's reason with it (WR-04's shape). Each deletion requires the surviving constraint line to be identified and quoted into the evidence record before the deletion lands; absence of such a line forces a rewrite instead. |
| T-239-11-04 | Information disclosure | the surviving SECURITY-class prose | high | mitigate | SC-5's control only catches *deleted* SECURITY-class sentences; a sentence weakened into vagueness passes it. Named as a prohibition and checked by region-scoped greps asserting the imperative and the CVE reference both survive verbatim. |
| T-239-11-05 | Tampering | repo-wide substitution | high | mitigate | A repo-wide `sed` could rewrite a `.github/` `name:` line and hang every PR forever (D-23). Edits are path-restricted to four named files; commit-scope assertions prove it. |
| T-239-11-06 | Elevation of privilege | `render_mismatch/1` structural invariant | critical | mitigate | The invariant is that the `:mismatch` branch renders no accept control. No executable line is touched by this plan, asserted by classifying every `+`/`-` line in both diffs as moduledoc or `#` comment, and by `mix format --check-formatted` passing. |
| T-239-11-07 | Tampering | `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv` | high | mitigate | This is the plan where a red verify creates pressure to edit the measurement instead of the surface. Both files are frozen by plan 239-09 (D-30), a prohibition names the temptation explicitly, and an acceptance criterion asserts both files are byte-unchanged across both commits. Exit 3 (instrument cannot answer) is distinct from exit 1 (surface dirty), so a broken script cannot be mistaken for a finding. |
| T-239-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. RESEARCH.md's Package Legitimacy Gate is not triggered. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification):

- `239-EVIDENCE.md` § `## BATCH-3-SWEEP-COMMIT` (new section, including its mirror subsection)
- The appended dated correction block naming plan `239-11` in `239-MIRROR-CHECKLIST.md` (new block)
- The WR-01 replacement sentence in `invitation_accept_live.ex`'s `@moduledoc` (new prose, in template and example tiers)

No new module, function, route, or configuration key is created by this plan. Every edit is to
`@moduledoc` prose or a `#` comment.
</artifacts_this_phase_produces>

<verification>
- Fixed-string greps for both removed sentences return 0 over `git ls-files priv/templates` and over `git ls-files test/example`, each paired with a non-zero `defmodule` control.
- `239-v3-vocabulary-check.sh` exits 0 with `hits_outside_allowlist=0` on the `priv/templates/` tier and on the scoped `example` counterpart tier, and still exits 1 on the `golden` tier (expected until plan 239-12). The instrument and allowlist files are byte-unchanged across both commits.
- `mix format --check-formatted` exits 0 on all four edited Elixir files.
- `237-security-comment-diff-check.sh` over the combined diff exits 0 with `examined_removed_lines` > 0, with the script byte-unchanged.
- `git log --oneline -2` shows exactly two commits; the first lists only `priv/templates/` paths, the second only `test/example/` paths plus two phase-directory artifacts.
- `git diff --name-only HEAD~2 -- test/fixtures/install_golden/ .github/` is empty.
</verification>

<success_criteria>
The two sentences that falsified the phase goal are gone from the two tiers this plan owns, proven by
a check independent of every regex under examination; the false safety claim on an authorization-bypass
defense is replaced with prose that is true in an adopter's project; and the D-19 commit topology is
intact for plan 239-12's re-bless.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-11-SUMMARY.md` when done.
</output>
