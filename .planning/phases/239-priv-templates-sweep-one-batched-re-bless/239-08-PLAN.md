---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 08
type: execute
wave: 8
depends_on: ["239-07"]
files_modified:
  - test/fixtures/install_golden/tree
  - test/fixtures/install_golden/STDOUT.txt
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-2.txt
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-comment-only-diff-check.sh
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless2-code-change.diff
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 140000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The closure work lands exactly ONE new re-bless commit — `git log <closure-base>..HEAD -- test/fixtures/install_golden` returns exactly one sha, and `git show --name-only` for it lists nothing outside `test/fixtures/install_golden/` (SC-3's one-batched-re-bless rule, applied to the re-run)."
    - "A second frozen expected-removed set `239-golden-expected-2.txt` exists, generated BEFORE the re-bless runs from the widened definition plus an enumerated literal-anchor list of the prose-fix sites — never from the diff it validates."
    - "`239-comment-only-diff-check.sh` run against the re-bless-2 diff with `239-golden-expected-2.txt` prints `nonconforming=0` alongside `changed_lines`, `removed_lines` and `files`, and exits 0."
    - "The same classifier, run against a committed known-bad fixture derived from the re-bless-2 diff with one code line altered, exits 1 with `nonconforming` >= 1 and names the offending line — the instrument is demonstrated RED before its green is believed (Standing Constraint 6)."
    - "`MIX_ENV=test mix sigra.fixture.rebless_golden --check` prints the literal `OK: fixture is up-to-date (check mode.)` and exits 0 after the commit."
    - "`grep -cE '<V2>' $(git ls-files test/fixtures/install_golden/tree)` returns 0 with the paired positive control `grep -lc defmodule` non-zero on the same tree — the widened definition is clean on the literal bytes an adopter generates, not on the source tree."
    - "`grep -cE '<V2>' test/fixtures/install_golden/STDOUT.txt` is recorded (D-13's independent-drift hazard is re-checked under the widened net, not assumed still nil)."
    - "A freshly generated app from `scripts/ci/install-smoke.sh` returns 0 V2 hits across its `lib/` and `priv/`, with a non-zero `defmodule` control on the same generated tree."
    - "An extracted `mix hex.build` tarball returns 0 `.planning/` hits under `lib/` and `priv/`, and 0 V2 hits under `priv/`; the deliberate out-of-scope hits (README.md, CHANGELOG.md, docs/) and the tarball `lib/` ratchet deferral are enumerated rather than claimed absent."
    - "`MIX_ENV=test mix ci` reports 0 failures at the final committed HEAD on a clean tree (Standing Constraints 2 and 3)."
    - "`git diff --name-only origin/main -- .github/` is empty and `git diff origin/main -- .github/ | grep '^[+-].*name:'` is empty — SC-5 is re-proven after the closure, not assumed to have survived it."
    - "`237-security-comment-diff-check.sh` run over the full closure diff (plans 239-05 … 239-08) exits 0 with `examined_removed_lines` > 0, or its single documented structural survivor is re-stated as the same known instrument limitation, never by editing the script (D-16)."
  artifacts:
    - test/fixtures/install_golden/tree
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-2.txt
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless2-code-change.diff
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  key_links:
    - "`mix sigra.fixture.rebless_golden` calls `File.rm_rf!` and rewrites the tree wholesale (`lib/mix/tasks/sigra.fixture.rebless_golden.ex:92-103`) — it makes drift disappear whether or not the drift was intended, which is exactly why the classifier runs on the captured diff BEFORE the commit."
    - "`--check` compares `STDOUT.txt` by byte equality on its own line (`rebless_golden.ex:115-140`), so a `--check` exit 0 independently covers the D-13 independent-drift hazard."
    - "The re-bless carries the template edits from plans 239-05 and 239-06 into the golden tree; the plan-239-07 mirror commit touches `test/example/` only and must leave this diff untouched, or the D-19 separation collapses."
  prohibitions:
    - statement: "Must NOT land more than one new re-bless commit for the entire closure — if the first captured diff fails the classifier, fix the template and re-run the task, but only one re-bless commit exists at the end (SC-3)."
    - statement: "Must NOT hand-edit `test/fixtures/install_golden/tree/` or `STDOUT.txt` — only `MIX_ENV=test mix sigra.fixture.rebless_golden` writes them (D-09)."
    - statement: "Must NOT wave through a non-comment line in the re-bless diff. A nonconforming line is a stop-the-line event: halt, report, and do not commit — never a judgment call."
    - statement: "Must NOT weaken `239-comment-only-diff-check.sh`'s fail-closed behaviour — the empty-input guard, the expected-set-empty guard, the add-only-hunk class, and the `removed_lines >= T-record-count` floor all stay; only the hardcoded `files >= 30` floor may become an explicit, recorded parameter."
    - statement: "Must NOT derive `239-golden-expected-2.txt` from the re-bless diff — that is circular and would make the classifier unfalsifiable."
    - statement: "Must NOT modify `237-security-comment-diff-check.sh` to make it pass (D-16) — its `IN-NN` tolerance gap is a documented structural limitation."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23)."
    - statement: "Must NOT wire any phase script into `mix ci`, `.github/workflows/`, or `scripts/ci/prohibitions/*.test.mjs` — Phase 239 builds no guard (Standing Constraint 5, D-04)."
    - statement: "Must NOT fix the tarball `lib/` bookkeeping (472 lines / 83 files) or SURF-02's HexDocs leak — both are Phase 241 SURF-04 ratchet work and are recorded as enumerated deferrals, not closed here."
    - statement: "Must NOT claim `install_golden_contract` is green from GitHub Actions — this phase pushes nothing; the Actions clause stays a recorded ship-time deferral in `## HONEST-CLAIMS`."
    - statement: "Must NOT leave `sigra-*.tar` or an unpacked `sigra-*/` behind after the tarball check (REPO-01)."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`/`unresolved`): assumed edge is the dead-grep case; every zero here is paired with a positive control, and the adopter-facing claims are made on generated and built bytes. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `unclassified`/`unresolved`): assumed edge is commit topology — SURF-03 says 'one sweep plus one batched re-bless, in separate commits', and this closure adds a second re-bless commit to the phase. The plan records that re-interpretation explicitly in the evidence ledger rather than letting a reader infer it. Surfaced, not resolved."
---

<objective>
The single batched re-bless of the closure, and the phase's live external observations re-run under
the widened definition.

Purpose: every template edit from plans 239-05 and 239-06 has to reach the golden fixture, and the
golden fixture is the artifact that proves what an adopter actually generates. The re-bless task
makes drift disappear regardless of whether the drift was intended, so the diff is captured and
classified before anything is committed — and the classifier itself is demonstrated RED first,
because a green from an unfalsified instrument is what produced this gap in the first place.

Output: one re-bless commit; a re-frozen expected-removed set generated before the run; a RED then
GREEN classifier demonstration; `--check` exit 0; V2 zero on the golden tree, on a freshly generated
app, and on the built tarball, each with a positive control; a green `MIX_ENV=test mix ci`; SC-5
re-proven; and an honest-claims record of what this closure did and did not establish.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-04-SUMMARY.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-07-SUMMARY.md
@lib/mix/tasks/sigra.fixture.rebless_golden.ex
@scripts/ci/install-smoke.sh
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **Exactly one new re-bless commit** for the whole closure (SC-3). Re-runs before commit are fine;
   commits are what count.
2. **`test/fixtures/install_golden/tree/` is never hand-edited** (D-09).
3. **The re-bless task does not stage or commit** (`rebless_golden.ex:19-20`). Stage by path.
4. **A nonconforming classifier line halts the plan.** Do not commit and then explain.
5. **A zero-match grep must be a measurement, not a fatal status** — brace group with `|| true`
   inside command substitutions. `bash -c 'set -eo pipefail; n=$(printf "" | grep -E zzz | wc -l); echo reached'`
   prints nothing and exits 1; that is the exact shape of every "returns zero hits" assertion here.
6. **Every zero is paired with a positive control on the same surface.**
7. **Evidence is captured at the final committed HEAD on a clean tree** (Standing Constraint 2).
8. **`mix ci`, never root `mix test`** (Standing Constraint 3). The golden tests need the phx_new
   1.8.8 archive locally (`mix archive.install --force hex phx_new 1.8.8`) or the fixture comparison
   produces spurious byte-diffs; `mix test` needs a live Postgres (`scripts/db/up.sh`, then
   `source tmp/db.env`).
9. **Clean up tarball artifacts immediately** (REPO-01).

<tasks>

<task type="auto">
  <name>Task 1: Re-freeze the expected-removed set and parameterize the file floor, BEFORE any re-bless runs</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-2.txt, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-comment-only-diff-check.sh, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>The working tree is clean at plan 239-07's commit, and `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 2 with `DRIFT DETECTED:` — proving the template edits from plans 239-05/239-06 have not yet reached the golden fixture. A `--check` exit 0 here means the template edits never reached generated output: halt.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-comment-only-diff-check.sh` — the whole script: the `T:`/`N:` record format, the containment lookup keyed on (path, trimmed text) and never on line number, the three violation classes, and the two non-vacuity floors at `floor_removed="$expected_t_count"` and `floor_files=30`.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected.txt` — its header line records the exact generating command for round 1; round 2's generator is the same shape with V2 substituted for V1.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## WIDENED-UNION-LEDGER` — the V2 string, and § `## CLOSURE-TEMPLATE-COMMIT` — the list of prose-fix sites whose golden-tree lines will be removed but which V2 does not match (they are prose repairs, not tokens).
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-code-change.diff` — the round-1 known-bad fixture, as the shape template for round 2's.
  </read_first>
  <action>
Generate `239-golden-expected-2.txt` from the **currently committed** golden tree, before running any
re-bless. Two record classes, exactly as round 1:

  * `T:` records — every line in `$(git ls-files test/fixtures/install_golden/tree)` matched by V2.
  * `N:` records — the golden-tree lines corresponding to the prose repairs from plans 239-05/239-06
    that V2 cannot match because they are ordinary English (the `Look for the \`this section\` HEEx`
    pointer, the `will replace the card body` sentence, the `until then` Branch B line, the
    subjectless `Implements the full invitation flow` line, the `6 digits entered:` line, the
    `during UAT` line, the `LiveView's / LiveView` duplication, the `only.` adverb, the
    `Generated by` wrap, the phishing parenthetical, the `:mismatch` invariant sentence, and the
    `sigra_auth.css` comment-block lines). Each `N:` record is located by a **literal anchor** taken
    from this plan and from `239-REVIEW.md` — never by a blanket radius around a match.

Write the generating command into the file's header line, as round 1 did, so the set is reproducible
and its provenance is visible. Record in `239-EVIDENCE.md` a new `## REFREEZE-LEDGER` slot stating:
why a second expected set is required (round 1's set contains the 139 V1 token lines that no longer
exist in the tree, so it cannot contain round 2's removals); the `T:` and `N:` counts; and the exact
list of literal anchors used for the `N:` records.

Then parameterize the one floor that cannot survive round 2. `floor_files=30` is hardcoded and
calibrated to round 1's 35-file diff; round 2 touches only the handful of files the closure edits
render into. Change that single line to read the floor from an environment variable with the
existing value as its default (`floor_files="${GOLDEN_MIN_FILES:-30}"`) and change nothing else in
the script. Record in the ledger: the diff of that one line, the value `GOLDEN_MIN_FILES` will be
set to for round 2 (the exact count of distinct paths in `239-golden-expected-2.txt`, so the floor
stays a real non-vacuity floor rather than a disabled one), and an explicit statement that the
empty-input guard, the empty-expected-set guard, the add-only-hunk class, and the
`removed_lines >= T-record-count` floor are all unchanged.
  </action>
  <verify>
    <automated>bash -lc 'd=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; test -s "$d/239-golden-expected-2.txt" && grep -q "GOLDEN_MIN_FILES" "$d/239-comment-only-diff-check.sh" && grep -qE "^(T|N):" "$d/239-golden-expected-2.txt" && echo REFREEZE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `239-golden-expected-2.txt` exists, is non-empty, has a `#`-prefixed header line recording its generating command, and contains at least one `T:` or `N:` record.
    - Every `N:` record in the file corresponds to a literal anchor enumerated in the `## REFREEZE-LEDGER` slot; the count of `N:` records equals the count of anchors listed. No blanket-radius records.
    - The file is generated at a sha where `git status --porcelain` shows no change under `test/fixtures/install_golden/` — proven by recording `git log -1 --format=%H -- test/fixtures/install_golden` and showing it predates this plan's re-bless commit.
    - `grep -c 'GOLDEN_MIN_FILES' 239-comment-only-diff-check.sh` → `1`, and `git diff -- 239-comment-only-diff-check.sh` shows exactly one changed line.
    - The script still contains its four fail-closed guards: `grep -c 'refusing to report success' 239-comment-only-diff-check.sh` → `>= 4`.
    - `floor_removed="$expected_t_count"` is unchanged in the script (grep confirms the literal).
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 2 at the start of this task, recorded — the precondition that the fixture is genuinely stale.
  </acceptance_criteria>
  <done>A non-circular expected-removed set for round 2 exists, frozen before the run, and the classifier's only round-1-specific constant is an explicit recorded parameter rather than a silent mismatch.</done>
  <reversibility rating="costly">Re-freezing the expected set re-bases what "comment-only" means for round 2. It is git-revertable, but once round 2's classification is recorded in the evidence ledger, a later reader compares against the new set, not the old one — so the anchor list must be enumerated in the ledger rather than implied. The alternative (reusing round 1's set) is not viable: it contains 139 lines that no longer exist in the tree.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Demonstrate the classifier RED on a committed known-bad fixture, then run the single re-bless and classify it GREEN</name>
  <files>test/fixtures/install_golden/tree, test/fixtures/install_golden/STDOUT.txt, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless2-code-change.diff</files>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-golden-expected-2.txt` — written in Task 1.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-code-change.diff` — the round-1 known-bad fixture's shape (a real diff hunk with one code line altered, which the round-1 run proved returns `nonconforming=1`, exit 1, naming `def rename_organization`).
    - `lib/mix/tasks/sigra.fixture.rebless_golden.ex` lines 40-140 — what the task writes, that it never stages or commits, and the exact `--check` exit semantics (`OK: fixture is up-to-date (check mode.)` / `DRIFT DETECTED:` + `exit({:shutdown, 2})`).
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-04-SUMMARY.md` — how round 1 captured, classified, and committed its diff, so round 2 follows the same sequence.
  </read_first>
  <action>
Run `MIX_ENV=test mix sigra.fixture.rebless_golden` (no `--check`) exactly once, then **capture the
working-tree diff to a file before staging anything**:
`git diff -- test/fixtures/install_golden > <scratch>/rebless2.diff`.

Build the round-2 known-bad fixture from that captured diff: copy it to
`.planning/phases/239-priv-templates-sweep-one-batched-re-bless/fixtures/239-golden-rebless2-code-change.diff`
and alter exactly one line inside it so it represents a code change rather than a comment change —
pick a real Elixir function head or a CSS declaration from the golden tree and show it as a
removed/added pair. Commit that fixture. Run the classifier against it with
`239-golden-expected-2.txt` and `GOLDEN_MIN_FILES` set to the round-2 value, and record: exit code 1,
`nonconforming` >= 1, and the named offending line. **Do this before believing the green** — a guard
that has never been observed RED does not count (Standing Constraint 6).

Then run the classifier against the real `rebless2.diff` with the same expected set and the same
`GOLDEN_MIN_FILES`. Record `changed_lines`, `removed_lines`, `files`, `nonconforming`,
`nonconforming_removed`, `nonconforming_files`, `nonconforming_addonly_hunks`, and
`removed_lines_floor`, and the exit code.

If `nonconforming > 0`: HALT. Do not commit. The only permitted remediation is (a) fixing the
template if the nonconforming line reveals a real code change, or (b) adding an `N:` record for a
line that is provably a reflow neighbour of an enumerated fix site AND lies inside a `@moduledoc`,
`@doc`, `#` comment, or `/* … */` range AND lives in a file this closure edited — with the literal
anchor recorded in the `## REFREEZE-LEDGER` slot before the re-run. Any other nonconforming line —
any executable line, any string literal, any EEx binding, any CSS property or value, any file this
closure did not edit — is a stop-the-line event: halt and report, never widen the expected set to
admit it.

Once `nonconforming=0` and the exit code is 0, commit path-scoped to `test/fixtures/install_golden`
alone: `chore(239): re-bless install golden fixture after the closure template edits (SURF-03)`.
Then run `MIX_ENV=test mix sigra.fixture.rebless_golden --check` and require the literal
`OK: fixture is up-to-date (check mode.)` and exit 0. Assert the commit's
`git show --name-only --format=` lists nothing outside `test/fixtures/install_golden/`, and that
`git log <closure-base>..HEAD -- test/fixtures/install_golden` returns exactly one sha.

Record the SURF-03 topology re-interpretation explicitly in the evidence ledger: the phase now
carries two re-bless commits — the original `38c9bd9a` and this one — because gap closure required a
second batch of template edits; each is individually one batched run, confined to the fixture
directory, and comment-only under a classifier demonstrated falsifiable. State it plainly rather
than letting a reader discover it from `git log`.
  </action>
  <verify>
    <automated>bash -lc 'MIX_ENV=test mix sigra.fixture.rebless_golden --check 2>&1 | grep -q "OK: fixture is up-to-date" && echo CHECK_ZERO'</automated>
  </verify>
  <acceptance_criteria>
    - The classifier against `fixtures/239-golden-rebless2-code-change.diff` exits `1`, prints `nonconforming` >= 1, and names the offending line on stderr; the full output is recorded verbatim in the ledger.
    - The classifier against the real captured `rebless2.diff` exits `0` and prints `nonconforming=0` together with non-zero `changed_lines`, `removed_lines` and `files`; all seven counters are recorded verbatim.
    - `removed_lines` is `>=` the `T:` record count of `239-golden-expected-2.txt` (the floor held, not was bypassed), and `files` is `>=` the `GOLDEN_MIN_FILES` value used.
    - Any `N:` record added mid-task appears in the `## REFREEZE-LEDGER` anchor list with its literal anchor and a stated reason; if none were added, the ledger says so explicitly.
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` prints the literal `OK: fixture is up-to-date (check mode.)` and exits `0`.
    - `git show --name-only --format= HEAD` for the re-bless commit lists only paths under `test/fixtures/install_golden/`.
    - `git log --format=%H <closure-base>..HEAD -- test/fixtures/install_golden | wc -l` → `1`.
    - The evidence ledger states, in one sentence a reader cannot miss, that the phase now carries two re-bless commits and why.
  </acceptance_criteria>
  <done>The golden fixture matches a freshly generated app byte-for-byte again, and the diff that got it there was proven comment-only by an instrument that was proven able to fail first.</done>
  <reversibility rating="costly">The re-bless commit is git-revertable, but it re-states SC-3's "exactly one batched re-bless" as "one per batch of template edits" for this phase's record. That re-interpretation, once written into the evidence ledger and read by the verifier, is what a later reader reasons from — hence the explicit statement rather than a silent second commit.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Re-run every live external observation under V2, re-prove SC-5, and record the honest claims</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>PostgreSQL is reachable with user `postgres` / password `postgres` (`scripts/db/up.sh` then `source tmp/db.env`, or a local 5432), and `mix phx.new --version` reports `1.8.8` — a different archive produces spurious byte-diffs against the committed fixture and `install-smoke.sh` hard-fails its `PHX_NEW_PIN` check.</precondition>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## HONEST-CLAIMS` — the existing honesty posture, including the `install_golden_contract` Actions clause recorded as a ship-time deferral; the closure must extend it, not restate it.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-04-SUMMARY.md` — the exact commands round 1 used for the install-smoke and `mix hex.build` observations, and the 32 enumerated out-of-scope tarball hits.
    - `scripts/ci/install-smoke.sh` — `TMP_APP_DIR`, the `PHX_NEW_PIN` assertion, and the fact that it never runs `mix sigra.upgrade` (so 3 templates are structurally unreachable by SC-1 and are covered by the tarball check instead).
    - `.planning/phases/237-clean-working-tree-green-pages-clean-lib-docs-surface/237-security-comment-diff-check.sh` — the SC-5 instrument; D-16 pins it, so it is run, never edited.
  </read_first>
  <action>
At the final committed HEAD on a clean tree, run and record, each with its paired positive control:

  1. **Golden tree under V2** — `grep -cE '<V2>' $(git ls-files test/fixtures/install_golden/tree)`
     expecting 0, with `grep -lc defmodule` on the same list non-zero. Plus
     `grep -cE '<V2>' test/fixtures/install_golden/STDOUT.txt`, recorded whatever its value (D-13).
  2. **Freshly generated app** — `TMP_APP_DIR=/tmp/sigra-239-08-app GITHUB_WORKSPACE=$(pwd)
     scripts/ci/install-smoke.sh`, then V2 and `\.planning/` greps across its `lib/` and `priv/`
     expecting 0, with a non-zero `defmodule` control on the same generated tree. Remove the app dir
     afterwards. State the SC-1 coverage boundary explicitly: the three `sigra.upgrade/` templates
     are not reachable by this vehicle.
  3. **Built tarball** — `mix hex.build`, extract to scratch, grep `\.planning/` across `lib` and
     `priv` expecting 0 and V2 across `priv` expecting 0, with a non-zero control. Enumerate the
     deliberate out-of-scope hits (README.md, CHANGELOG.md, docs/) rather than claiming absence, and
     record the tarball `lib/` V2 count as the Phase 241 SURF-04 ratchet baseline — deferred here,
     not fixed. Delete `sigra-*.tar` and any unpacked `sigra-*/` immediately (REPO-01).
  4. **Full gate** — `MIX_ENV=test mix ci`, requiring 0 failures, with the test count recorded
     (Standing Constraint 1 rejects count-free green).
  5. **SC-5, re-proven** — `git diff --name-only origin/main -- .github/` empty;
     `git diff origin/main -- .github/ | grep '^[+-].*name:'` empty; and
     `237-security-comment-diff-check.sh` over the full closure diff (plans 239-05 … 239-08) with its
     `examined_removed_lines=<N>`, N > 0, recorded verbatim. If it reports the same single structural
     survivor round 1 documented (`core/auth.ex:530`'s `IN-03` tolerance gap), restate it as the same
     known instrument limitation with the same reasoning — do not edit the script to clear it (D-16).

Write all of this into a `## CLOSURE-OUTCOME` slot in `239-EVIDENCE.md`, and extend `## HONEST-CLAIMS`
with, at minimum: the `install_golden_contract` Actions clause still being a ship-time deferral
(this closure pushes nothing); the tarball `lib/` ratchet deferral with its measured number; the
SURF-02-does-not-hold-at-HEAD flag pointing at its todo; and the two unresolved edge-probe rows for
SURF-01 and SURF-03 carried forward as flagged assumptions rather than resolved.

Commit the evidence as its own commit, path-scoped to the phase directory:
`docs(239): closure evidence — widened re-measure, second re-bless, SC-5 re-proof (SURF-01, SURF-03)`.
  </action>
  <verify>
    <automated>MIX_ENV=test mix ci</automated>
  </verify>
  <acceptance_criteria>
    - `MIX_ENV=test mix ci` exits 0 with a recorded test count and `0 failures`; a bare "green" with no count is not acceptable.
    - Golden tree V2 count → `0`, recorded with a non-zero `defmodule` file count on the same list.
    - `test/fixtures/install_golden/STDOUT.txt` V2 count recorded (any value); if non-zero, a disposition accompanies it.
    - Generated-app V2 and `\.planning/` counts → `0` each, recorded with a non-zero `defmodule` control on `/tmp/sigra-239-08-app`; the `sigra.upgrade/` coverage boundary is stated.
    - Tarball `lib`+`priv` `\.planning/` count → `0`; `priv` V2 count → `0`; the out-of-scope hits are enumerated by file; the tarball `lib/` V2 count is recorded as the Phase 241 baseline and explicitly not fixed.
    - `git diff --name-only origin/main -- .github/` → empty; `git diff origin/main -- .github/ | grep '^[+-].*name:'` → empty. Both recorded.
    - `237-security-comment-diff-check.sh` output recorded verbatim with `examined_removed_lines` > 0; the script file is unmodified (`git diff --name-only origin/main -- .planning/phases/237-*/237-security-comment-diff-check.sh` is empty).
    - `239-EVIDENCE.md` contains `^## CLOSURE-OUTCOME$` with a `Status:` line, and `## HONEST-CLAIMS` contains the four extensions named in the action.
    - No `sigra-*.tar` or unpacked `sigra-*/` remains: `ls sigra-*.tar` returns no match and `git status --porcelain` is clean.
  </acceptance_criteria>
  <done>Every adopter-facing claim this phase makes is re-established under a definition that can see the bookkeeping, on generated and built bytes, with a live positive control — and everything still unproven is written down as unproven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `priv/templates/` → generated app → published tarball | The re-bless is the point at which template bytes become the committed record of what an adopter receives; a code change absorbed here is invisible downstream. |
| Classifier verdict → phase acceptance | `nonconforming=0` is the only thing standing between a comment sweep and an unreviewed code change riding a generated-snapshot commit. |
| Repo → GitHub required status contexts | A `.github/` `name:` change silently renames a required context and hangs every PR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-08-01 | Tampering | A code change absorbed into the re-bless commit under cover of a comment sweep | high | mitigate | Diff captured before staging and classified; RED demonstrated on a committed known-bad fixture first; `nonconforming > 0` halts the plan without a commit. |
| T-239-08-02 | Repudiation | An unfalsifiable classifier green (expected set derived from the diff it validates) | high | mitigate | `239-golden-expected-2.txt` frozen before the re-bless runs, generated from V2 plus enumerated literal anchors; circular derivation is a named prohibition. |
| T-239-08-03 | Repudiation | A non-vacuity floor silently disabled to make round 2 pass | high | mitigate | `GOLDEN_MIN_FILES` is set to the exact expected-path count and recorded; the three other fail-closed guards and the `removed_lines` floor are asserted unchanged by grep. |
| T-239-08-04 | Tampering | Security rationale deleted across the full closure diff | high | mitigate | `237-security-comment-diff-check.sh` run over the whole closure diff with `examined_removed_lines > 0`; D-16 forbids editing the checker to pass. |
| T-239-08-05 | Denial of service | `.github/` `name:` drift renaming a required status context | high | mitigate | Two asserted-empty `.github/` diffs at the final HEAD (D-23). |
| T-239-08-06 | Information disclosure | Claiming adopters are clean when the published 1.5.0 tarball still carries the leak | medium | mitigate | `## HONEST-CLAIMS` states the fix reaches adopters only at the next publish (Phase 242), not at this commit. |
| T-239-08-07 | Tampering | Stray `sigra-*.tar` artifacts left in the tree after the tarball check | low | accept | REPO-01 already covers stray artifacts; the cleanup step is asserted by `git status --porcelain` clean, and a stray tar is detectable and harmless if it occurs. |
</threat_model>

## Artifacts this phase produces (plan 239-08)

| Artifact | New or modified | What it is |
|---|---|---|
| `test/fixtures/install_golden/tree/**` | modified (generated) | Re-blessed golden fixture carrying the closure's template edits. |
| `test/fixtures/install_golden/STDOUT.txt` | modified (generated, possibly unchanged) | Re-written by the same run; compared by byte equality in `--check`. |
| `239-golden-expected-2.txt` | new | Round-2 frozen expected-removed set: V2 `T:` records plus literal-anchored `N:` records. |
| `239-comment-only-diff-check.sh` | modified (one line) | `floor_files` becomes `${GOLDEN_MIN_FILES:-30}`; all fail-closed guards unchanged. |
| `fixtures/239-golden-rebless2-code-change.diff` | new | The round-2 committed known-bad fixture that demonstrates the classifier RED. |
| `239-EVIDENCE.md` § `## REFREEZE-LEDGER` | new slot | Why a second expected set exists, its counts, its literal anchors, and the floor parameterization. |
| `239-EVIDENCE.md` § `## CLOSURE-OUTCOME` | new slot | V2 on the golden tree / generated app / tarball with positive controls, `mix ci` result, SC-5 re-proof. |
| `239-EVIDENCE.md` § `## HONEST-CLAIMS` | extended | Actions-clause deferral, tarball `lib/` ratchet baseline, SURF-02 flag, two unresolved edge-probe assumptions. |

<verification>
- Exactly one new re-bless commit, confined to `test/fixtures/install_golden/`.
- Classifier RED on a committed known-bad fixture, then GREEN on the real diff, all seven counters
  recorded.
- `--check` exit 0 with the literal OK line.
- V2 → 0 on the golden tree, a freshly generated app, and the tarball's `priv/`, each with a
  positive control.
- `MIX_ENV=test mix ci` green with a recorded test count.
- SC-5 re-proven: `.github/` untouched, no `name:` change, no security rationale deleted.
</verification>

<success_criteria>
Nothing an adopter generates contains Sigra's internal planning bookkeeping — established under a
definition that can see it, on generated and built bytes, by an instrument that was shown able to
fail.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-08-SUMMARY.md` when done.
Record: the re-bless sha and its file list, the RED and GREEN classifier outputs verbatim, the
`--check` line, the three V2 measurements with their positive controls, the `mix ci` test count, the
SC-5 outputs, and every item added to `## HONEST-CLAIMS`.
</output>
