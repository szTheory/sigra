---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 10
type: execute
wave: 10
depends_on: ["239-09"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  - .planning/todos/pending/2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md
  - .planning/todos/pending/2026-09-18-wr-04-login-route-rationale-deleted-at-the-core-ex-source.md
  - .planning/todos/pending/2026-09-18-in-05-mfa-challenge-auto-submit-contradicts-the-strengthened-settings-comment.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ROADMAP Success Criterion 2 for Phase 239 names `lib/` and `priv/` explicitly and carries the D-27 amendment note stating plainly that it narrows what is *claimed* — greppable as the literal `under \\`lib/\\` and \\`priv/\\`` inside the SC-2 bullet."
    - "ROADMAP Success Criterion 1 names the definition its 'planning bookkeeping' half is measured under (the V3 vocabulary definition in `239-EVIDENCE.md` § `## VOCABULARY-LEDGER`), so the criterion stops over-claiming — and the named definition is strictly wider than the V2 regex the failed pass used, so this is a widening, not a narrowing."
    - "SURF-01's requirement text in `.planning/REQUIREMENTS.md` is confirmed consistent with the amended SC-2 — its wording already scopes to `lib/` and `priv/templates/`, and that consistency is asserted in writing with both texts quoted side by side rather than left to a reader's inference."
    - "The packaged `docs/` + `README.md` + `CHANGELOG.md` surface is TRACKED, not dropped: SURF-04's requirement text names it, AND a pending todo exists carrying the measured 58-reference breakdown and its per-file counts."
    - "`D-27`, `D-28` and `D-29` are recorded by ID in `239-CONTEXT.md`'s `## Implementation Decisions` block, each with a rationale and a reversibility rating, continuing the D-01 … D-26 sequence."
    - "D-29 bounds the D-26 residue: every re-bless batch must be justified by name in `239-EVIDENCE.md` before its run, so the now-unbounded per-batch count stays auditable. The bound is recorded as a decision and an evidence obligation — it does NOT re-amend SC-3 or SURF-03 prose."
    - "WR-04 and IN-05 each have a pending todo file with the review's diagnosis attached, including the out-of-range root location, and neither is fixed in this closure."
    - "No file under `priv/`, `test/`, `lib/`, or `.github/` is modified by this plan — `git diff --name-only HEAD~3 | grep -cE '^(priv|test|lib|\\.github)/'` returns 0."
    - { statement: "The amended SC-2 wording does not silently relax any property SC-2 was protecting beyond the lib/+priv/ scoping — checked by diffing the old and new bullet clause-by-clause and recording the delta", verification: backstop }
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
    - .planning/todos/pending/2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md
    - .planning/todos/pending/2026-09-18-wr-04-login-route-rationale-deleted-at-the-core-ex-source.md
    - .planning/todos/pending/2026-09-18-in-05-mfa-challenge-auto-submit-contradicts-the-strengthened-settings-comment.md
  key_links:
    - "SC-2 (ROADMAP) and SURF-01 (REQUIREMENTS) describe the same surface at two different widths. If only one is amended they disagree, and a re-verifier picks whichever one fails. Both must be read together and their consistency asserted in writing."
    - "SURF-04 (Phase 241) is the only downstream owner of the packaged-docs surface. If its text is not extended AND a todo is not filed, narrowing SC-2 silently deletes a real finding — which is the precise failure mode this closure exists to stop."
    - "`239-CONTEXT.md`'s decision block is what plan 239-11 and 239-12 executors read for authority. A decision recorded only in EVIDENCE is not readable by them."
  prohibitions:
    - statement: "Must NOT strip `.planning/` path references from `CHANGELOG.md`, `README.md`, or `docs/`, and must NOT remove `docs` from the Hex `files:` list in `mix.exs`. The user's decision is to narrow the claim and route the surface, not to clean it here; `CHANGELOG.md` is additionally Phase 242's file (D-06)."
    - statement: "Must NOT move a goalpost. An amendment that makes a criterion match whatever the tree currently passes is prohibited. Each amendment must state in its own text what it gives up, and SC-1's amendment must name a definition that is strictly *wider* than the one the failed pass used."
    - statement: "Must NOT drop a review finding by narrowing the criterion that surfaced it. Every finding this plan removes from Phase 239's scope leaves behind a named owner (a requirement clause, a todo file, or both) that a later reader can follow."
    - statement: "Must NOT re-amend SC-3's or SURF-03's comment-only, commit-isolation, `--check`-exit-0, or mirror clauses. D-29 adds an evidence obligation recorded as a decision; it does not touch those criteria's prose."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): surfaced in full in plan 239-09. Relevant here because SC-2's narrowing is the boundary case of the same question — what exactly is inside the asserted scope. Not auto-resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `adjacency`, UNRESOLVED): 'when two things are exactly equal or just touch, do they merge, collide, or separate?' Read here as SC-2 and SURF-01 describing the same surface at two widths — they must not be allowed to merge silently, and this plan asserts their consistency explicitly rather than assuming it. The probe row remains unresolved."
---

<assumption_delta_decision>
**Outcome: `no-change`.** Same rationale as plan 239-09: the `pluralization`/`second` signal is the
D-26 batch-count amendment, which carries no identity-model consequence. The residue it does carry —
an unbounded per-batch re-bless count — is answered in this plan by **D-29** rather than accepted as
silent debt.
</assumption_delta_decision>

<objective>
Make the phase's stated criteria match what is actually being asserted, and give every finding this
narrowing removes from Phase 239 a named downstream owner.

Purpose: `239-VERIFICATION.md` failed SC-2 because the criterion says "the tarball" while what holds
is "the tarball's `lib/` and `priv/`" — `mix.exs:184` also packages `docs/`, `README.md` and
`CHANGELOG.md`, which carry 58 `.planning/` references by design (D-05/D-06 recorded this
deliberately during discussion). The developer's decision is to narrow the claim and route the
surface, not to clean it here. A narrowing that quietly deletes a finding is the same defect as a
green gate that verified nothing, so the narrowing is only legitimate if the finding lands somewhere
with an owner — hence the SURF-04 text extension *and* a todo carrying the measured breakdown.

The same plan records D-28 (V3 as measurement-only, mechanization to Phase 241) into the decision
block where plan 239-11 and 239-12 executors will read it, amends SC-1 to name that definition, and
closes the D-26 residue with D-29.

Output: an amended SC-1 and SC-2, a SURF-04 clause and a todo owning the packaged-docs surface, three
new decision records, and pending todos for WR-04 and IN-05.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **This plan touches planning documents and todos only.** No file under `priv/`, `test/`, `lib/`,
   or `.github/`. A change there is a stop-the-line event (D-23).
2. **No `CHANGELOG.md`, `README.md`, `docs/`, or `mix.exs` edit** — the user's decision is to narrow
   and route, not to clean (D-05/D-06; `CHANGELOG.md` is Phase 242's file).
3. **Do not re-open a decided question.** D-27's shape was chosen by the developer. No
   `checkpoint:decision` is emitted, following the D-26 precedent recorded in `239-CONTEXT.md`:
   re-asking an answered question at execution time stops the run for nothing.
4. **Read before filing.** `.planning/todos/pending/` already holds four todos from this phase. Check
   for an existing owner before creating a new file; extend rather than duplicate.
5. **The repo is public.** No home-directory paths, adopter PII, or brand hex values.
6. **Do not push.** Commit only, ending messages with
   `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

<tasks>

<task type="auto">
  <name>Task 1: D-27 — narrow SC-2 to lib/+priv/, assert SURF-01 consistency, and route the packaged docs surface to Phase 241 SURF-04</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/todos/pending/2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — the `### Phase 239` block and its five `**Success Criteria**` bullets. Read SC-3 in full first: it already carries an inline amendment note in exactly the style this task must copy (`amended from "exactly **one** for the phase"` with the reason stated in the criterion itself). Match that style.
    - `.planning/REQUIREMENTS.md` — SURF-01 (around `:82`), SURF-03 (around `:84`) and SURF-04 (around `:85`) verbatim, plus the roll-up table row for SURF-04. SURF-03's text also carries the D-26 inline amendment; it is the second style reference.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the SC-2 gap entry: its `reason:` block, the two `artifacts:` rows, and the `missing:` option list. The per-file counts (CHANGELOG.md 19, docs/uat-ci-coverage.md 6, docs/ga-evidence.md 3, docs/nyquist-posture-matrix.md 2, docs/audit-semantics.md 1, README.md 1, total 58) come from there.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md` — D-05, D-06, D-07 and D-08: the measurement taken during discussion, the scoping rationale, and the `files:` list at `mix.exs:184`. D-06 already argues this scoping; the amendment makes it a criterion instead of a rationale.
    - `.planning/todos/pending/2026-09-17-bookkeeping-leak-guard-on-distribution-boundary.md` and `.planning/todos/pending/2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md` — both are Phase 241 SURF-04 inheritance todos. Confirm neither already owns the packaged-docs surface before filing a third; if one does, extend it and do not create the new file.
  </read_first>
  <action>
Amend ROADMAP Success Criterion 2 for Phase 239 so the asserted scope is explicit. The criterion must
state that the extracted `mix hex.build` tarball greps clean for `.planning/` paths **under `lib/`
and `priv/`**, keep its existing "the source tree is never the thing that is asserted" clause
unchanged, and carry an inline amendment note in the same style SC-3 already uses. That note states,
plainly and without euphemism: the criterion previously said "the tarball" unqualified, which
over-claimed, because `mix.exs:184` also packages `docs/`, `README.md` and `CHANGELOG.md`, which
together carry 58 `.planning/` references by design (D-05/D-06/D-08); the amendment narrows what is
**claimed**, not what is cleaned; the packaged docs surface is routed to Phase 241 SURF-04 and is
tracked, not dropped. Give the amendment the decision ID **D-27** in the note text, continuing this
phase's sequence.

Then read SURF-01's requirement text in `.planning/REQUIREMENTS.md` and assert its consistency with
the amended SC-2 in writing. SURF-01 is already scoped to `lib/` or `priv/templates/` with the
tarball named as the *verification vehicle* rather than the scope — which is the narrower and correct
wording, and is why `239-VERIFICATION.md` rates SURF-01 satisfied while SC-2 fails. Add a short
consistency note after the SURF-01 item quoting the amended SC-2's scope clause and SURF-01's own
scope clause side by side, and stating that they now describe the same surface. Do not change
SURF-01's requirement text itself — this is the note that makes an inference auditable, not a rewrite.

Then extend SURF-04's requirement text so Phase 241 owns the packaged-docs surface explicitly. Add a
clause naming `docs/`, `README.md` and `CHANGELOG.md` as packaged by `mix.exs`'s `files:` list and
therefore inside the artifact an adopter downloads, and stating that the SURF-04 ratchet covers that
surface alongside the `lib/` inline-comment ratchet it already covers. Keep every existing SURF-04
clause intact, including the hard-fail scope, the monotonic-decrease framing, "Zero is explicitly not
the v1.48 target", and "Never added to `mix ci`". SURF-04 stays `[ ]` and its roll-up row stays
`Phase 241` / `Pending` — this task widens the requirement, it does not schedule it.

Finally file the todo `2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md`
under `.planning/todos/pending/` with the same YAML front-matter shape the existing phase todos use
(`created`, `status: pending`, `title`, `area`, `files`, `source`, `audit_acknowledged` with
`milestone: v1.48`). Its body carries: the per-file measured breakdown totalling 58 with each file's
count; the `mix.exs:184` `files:` list verbatim; the statement that Phase 239 deliberately did not
clean this surface and why (D-05/D-06 — the references are real provenance links and `CHANGELOG.md`
belongs to Phase 242/REL-05); the consequence if ignored (adopters keep downloading a tarball whose
packaged documentation cites a planning directory that does not exist in their project); and its
owner, Phase 241 SURF-04. Cross-reference the two existing SURF-04 inheritance todos by filename so a
triager sees one cluster rather than three unrelated notes.

Commit path-scoped to the three files with message
`docs(239): D-27 — narrow SC-2 to lib/+priv/ and route the packaged docs surface to Phase 241 (SURF-01)`.
  </action>
  <verify>
    <automated>bash -lc 'set -o pipefail; grep -q "D-27" .planning/ROADMAP.md && grep -q "SURF-04" .planning/REQUIREMENTS.md && test -s .planning/todos/pending/2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md && grep -q "58" .planning/todos/pending/2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md && echo D27_LANDED'</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 239 SC-2 bullet in `.planning/ROADMAP.md` contains both `lib/` and `priv/` as its named scope and contains the literal `D-27`. Confirmed by extracting the SC-2 bullet with `sed` and grepping within it, not by grepping the whole file.
    - The SC-2 bullet still contains its original clause `the source tree is never the thing that is asserted` — the amendment adds scope and a note, it does not rewrite the criterion.
    - The SC-2 amendment note states what is given up. `grep` the extracted bullet for the word `claimed` and for `Phase 241`: both present.
    - SURF-04's requirement text in `.planning/REQUIREMENTS.md` names all three packaged surfaces. Extract the SURF-04 bullet and confirm it contains `docs/`, `README.md` and `CHANGELOG.md`, AND still contains its four pre-existing clauses: the `.planning/` hard-fail, `monotonic-decrease`, `Zero is explicitly not the v1.48 target`, and `Never added to \`mix ci\``.
    - SURF-04 is still unchecked: `grep -c '^- \[ \] \*\*SURF-04\*\*' .planning/REQUIREMENTS.md` returns `1`, and its roll-up row still reads `Phase 241` and `Pending`.
    - SURF-01's requirement sentence is byte-unchanged: the `git diff` for `.planning/REQUIREMENTS.md` shows no `-` line containing `**SURF-01**`.
    - The SURF-01 consistency note exists and quotes both scope clauses — it contains the string `SC-2` and the string `priv/templates/`.
    - The new todo file exists, is non-empty, parses as YAML front matter followed by Markdown (front matter delimited by two `---` lines), and contains: the total `58`, at least five per-file count rows, the literal `mix.exs`, the literal `Phase 241`, and the filenames of the two existing SURF-04 inheritance todos.
    - `git show --name-only --format= HEAD` lists exactly the three files in `<files>`; `grep -cE '^(priv|test|lib|\.github)/'` over that list returns `0`.
    - No packaged-docs content changed: `git diff --name-only HEAD~1 -- CHANGELOG.md README.md docs/ mix.exs` is empty.
  </acceptance_criteria>
  <done>SC-2 claims only what holds and says so in its own text; SURF-01 and SC-2 are asserted consistent with both clauses quoted; and the 58-reference packaged-docs surface has two owners a later reader can follow — a requirement clause and a todo — rather than being deleted by the narrowing.</done>
  <reversibility rating="costly">Amending a locked criterion changes the phase's contract, and a later reader reasons from the amended text. Not one-way: the note names what changed and why, so a reverter has the original claim reconstructable from the criterion itself. Following the D-26 precedent recorded in `239-CONTEXT.md`, no `checkpoint:decision` is emitted — the developer made this call during gap-closure planning and re-asking it at execution time would stop the run for nothing.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Record D-27, D-28 and D-29 into the decision block, and amend SC-1 to name the definition it is measured under</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md, .planning/ROADMAP.md, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md` — the whole `<decisions>` block, and D-26 in particular: it is the format template for a gap-closure-planning decision, carrying the developer's choice, the rationale, the options considered, a `Reversibility:` line, and an `Implemented by plan …` line. Every new record copies that shape.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md` § `## VOCABULARY-LEDGER` — written by plan 239-09; D-28's canonical statement lives there and this task copies it into the decision block rather than restating it differently.
    - `.planning/ROADMAP.md` — the Phase 239 SC-1 bullet verbatim, and SC-3's inline-amendment style.
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-VERIFICATION.md` — the section "Was the D-26 amendment legitimate, or a moved goalpost?", whose closing sentence names the exact weakening D-29 answers: "one per batch is unbounded where exactly one was countable — nothing now caps the number of re-bless commits a future phase can justify."
  </read_first>
  <action>
Append three decision records to `239-CONTEXT.md`'s `## Implementation Decisions` block, after D-26,
each following D-26's format exactly (statement, rationale, options where they existed, a
`Reversibility:` line, an `Implemented by plan …` line):

  * **D-27** — the SC-2 narrowing landed in Task 1. State the developer's choice (narrow the claim,
    route the surface), the measured 58 references and why they are deliberate, and that the
    alternatives — stripping the docs, or dropping `docs` from the Hex `files:` list — were
    explicitly not taken because they delete real provenance links and collide with Phase 242's
    `CHANGELOG.md` (REL-05). Reversibility: costly.

  * **D-28** — V3 is implemented in this closure as a **measurement instrument only**: defined,
    demonstrated RED on all three tiers, and recorded in `## VOCABULARY-LEDGER`, wired into nothing.
    Phase 241's SURF-04 `p18` guard inherits it as its spec through the updated
    `2026-09-17-widened-bookkeeping-definition-for-surf-04-p18.md` todo. The alternative — building
    the guard here — is forbidden by Standing Constraint 5 and D-04. The alternative of *not*
    widening and instead only restating SC-1 as "clean under the identifier regex" was rejected: it
    would leave the mechanism that hid the two sentences intact, so the next vocabulary-class leak
    ships under a green measurement exactly as these did. Reversibility: costly.

  * **D-29** — bound the D-26 residue. "One batched re-bless per batch of template edits" is
    unbounded where "exactly one for the phase" was countable. D-29 does not restore a count and does
    **not** re-amend SC-3 or SURF-03 prose; it adds an evidence obligation: every re-bless batch must
    be justified by name in `239-EVIDENCE.md` — what template edits compose the batch, and why they
    could not have been folded into the previous batch — recorded **before** that batch's re-bless
    runs. The count stays auditable even though it is no longer fixed. Plan 239-12 discharges this
    for batch 3 and is named as the implementing plan. Reversibility: reversible.

Then amend ROADMAP Success Criterion 1 for Phase 239 so its "planning bookkeeping" half names the
definition it is measured under, in SC-3's inline-amendment style. The criterion keeps its existing
text and its `organizations.ex:59` citation, and gains a clause stating that "planning bookkeeping"
is measured under the V3 vocabulary definition recorded in `239-EVIDENCE.md` §
`## VOCABULARY-LEDGER`, which is **strictly wider** than the V2 identifier regex the failed
verification pass used (D-28) — so naming the instrument widens the bar rather than lowering it. Say
that explicitly in the note: a criterion that names a narrower instrument would be a moved goalpost,
and this one names a wider one, which is checkable by the containment proof recorded in the ledger.

Finally add a short `## BATCH-JUSTIFICATION` section to `239-EVIDENCE.md` (placed after
`## VOCABULARY-LEDGER`) as the durable home for the D-29 obligation: a header explaining the
obligation, the retrospective one-line justifications for batch 1 and batch 2 derived from
`## SWEEP-COMMIT` and `## CLOSURE-TEMPLATE-COMMIT`, and an explicitly empty, labelled slot for
batch 3 that plan 239-12 fills before its re-bless runs.

Commit path-scoped to the three files with message
`docs(239): record D-27/D-28/D-29 and amend SC-1 to name its measuring definition (SURF-01, SURF-03)`.
  </action>
  <verify>
    <automated>bash -lc 'set -o pipefail; c=.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md; for d in D-27 D-28 D-29; do grep -q "\*\*$d" "$c" || { echo "MISSING $d"; exit 1; }; done; grep -q "VOCABULARY-LEDGER" .planning/ROADMAP.md && grep -q "## BATCH-JUSTIFICATION" .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md && echo DECISIONS_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - `239-CONTEXT.md` contains one record each for `D-27`, `D-28` and `D-29`, each inside the `<decisions>` block and each containing the literal `Reversibility:` and an `Implemented by plan` line naming a plan in the range 239-09 … 239-12.
    - Each of the three records states at least one alternative that was considered and not taken, with a reason — `grep` each record's body for `not taken` or `rejected` or `alternative`, at least one present per record.
    - The Phase 239 SC-1 bullet in `.planning/ROADMAP.md` contains `VOCABULARY-LEDGER` and the literal `strictly wider`, and still contains its original `organizations.ex:59` citation. Confirmed by extracting the SC-1 bullet and grepping within it.
    - The SC-1 amendment explicitly rules out the moved-goalpost reading: the extracted bullet contains a sentence naming that a narrower instrument would be a moved goalpost and this one is wider.
    - `239-EVIDENCE.md` contains a `## BATCH-JUSTIFICATION` section with three labelled slots — batch 1, batch 2, and an explicitly empty batch 3 — and the batch-3 slot contains a marker string a later grep can detect as unfilled.
    - D-29's record explicitly states that it does not re-amend SC-3 or SURF-03: its body contains both `SC-3` and a negation of amendment. Verified by reading the record, and mechanically by confirming the SC-3 bullet and the SURF-03 requirement text are byte-unchanged in this commit — `git diff HEAD~1 -- .planning/REQUIREMENTS.md` is empty, and the ROADMAP diff shows no `-` line inside the SC-3 bullet range.
    - `git show --name-only --format= HEAD` lists exactly the three files in `<files>`.
  </acceptance_criteria>
  <done>The three new decisions are readable by every downstream executor from the same block that carries D-01 … D-26; SC-1 names a strictly wider measuring definition and says why that is a widening; and the D-29 evidence obligation has a place to be discharged.</done>
  <reversibility rating="costly">SC-1's amendment changes the phase's contract, as D-27's does for SC-2. Mitigated by naming a strictly wider instrument with a recorded containment proof, so the amendment is mechanically checkable as a widening rather than accepted on the author's word. No `checkpoint:decision` — D-28's shape was delegated to the planner by the brief and is recorded here rather than left implicit.</reversibility>
</task>

<task type="auto">
  <name>Task 3: File WR-04 and IN-05 as todos with their out-of-range diagnoses attached, and fix neither</name>
  <files>.planning/todos/pending/2026-09-18-wr-04-login-route-rationale-deleted-at-the-core-ex-source.md, .planning/todos/pending/2026-09-18-in-05-mfa-challenge-auto-submit-contradicts-the-strengthened-settings-comment.md</files>
  <read_first>
    - `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-REVIEW.md` — the WR-04 finding in full, including its `Scope note` paragraph naming `lib/sigra/install/features/core.ex:503` as the authoritative wording and stating that plan 239-07 correctly mirrored the collapse; and the IN-05 finding in full, including both quoted comment bodies and the observation that the *challenge* screen is the one guarded by the lockout counter.
    - `lib/sigra/install/features/core.ex` around `:503` — read the router heredoc there to confirm the current collapsed wording and capture it verbatim for the todo. Read only; do not edit.
    - `priv/templates/sigra.install/core/mfa_challenge_live.ex` around `:355-365` and `priv/templates/sigra.install/core/mfa_settings_live.ex` around `:600-615` — the two comment bodies IN-05 contrasts. Read only.
    - `.planning/todos/pending/2026-09-17-ci-change-detector-omits-sigra-upgrade-and-gen-oauth.md` — an existing Phase 239 "found while cleaning" todo (D-24). Copy its front-matter shape and its diagnosis-attached body style.
    - `.planning/todos/pending/` — the full listing, to confirm neither finding already has an owner. If one does, extend that file instead of creating a duplicate and record that choice in the SUMMARY.
  </read_first>
  <action>
File two pending todos, each with the same YAML front matter shape the existing Phase 239 todos use
(`created`, `status: pending`, `title`, `area`, `files`, `source`, `audit_acknowledged` with
`milestone: v1.48`).

The WR-04 todo records that the login-route rationale was collapsed to a single fact, leaving the
constraint unguarded. Its body carries: the removed four-line comment verbatim as quoted in the
review; the single line that replaced it; the review's suggested restored wording; the reason the
rationale is load-bearing (a maintainer who moves `get "/log_in"` into the adjacent
`live_session :redirect_if_user_is_authenticated` block three lines below silently reintroduces
`phx-submit` interception on the login form, which is the exact defect `login_html.ex`'s own
moduledoc still documents, and which `CLAUDE.md` states as a project constraint); and the scope note
explaining why it is not fixed here — the authoritative wording lives at
`lib/sigra/install/features/core.ex:503`, outside the reviewed `aa1372cb..HEAD` range, and fixing it
requires editing the installer source plus re-mirroring the example and re-blessing the golden, which
is a fourth batch this closure is not chartered to open. Name the fix location explicitly so whoever
picks it up does not re-derive it: the `core.ex` source first, then example and golden.

The IN-05 todo records the sibling contradiction: the strengthened settings-screen comment names the
mailbox round-trip as a defect, and the challenge screen still does exactly that — and the challenge
screen is the one guarded by the lockout counter the comment cites. Its body carries both comment
bodies verbatim, the file and approximate line for each, the reason this is a correctness question
and not only a prose question (the same stale-prefix race the comment describes would burn a real
lockout attempt on the challenge screen), and the scope note that the review classified it
pre-existing and outside the `aa1372cb..HEAD` range.

Both todos state plainly, in their own text, that Phase 239 deliberately did not fix them, so a
triager does not read the filing as an oversight.

Do not edit `lib/sigra/install/features/core.ex`, `priv/templates/sigra.install/core/login_html.ex`,
`priv/templates/sigra.install/core/mfa_challenge_live.ex`, or
`priv/templates/sigra.install/core/mfa_settings_live.ex`. Any change to those files in this plan is a
stop-the-line event.

Commit path-scoped to the two new todo files with message
`docs(239): file WR-04 and IN-05 as todos — both roots sit outside this closure's range`.
  </action>
  <verify>
    <automated>bash -lc 'set -o pipefail; t=.planning/todos/pending; test -s "$t/2026-09-18-wr-04-login-route-rationale-deleted-at-the-core-ex-source.md" && test -s "$t/2026-09-18-in-05-mfa-challenge-auto-submit-contradicts-the-strengthened-settings-comment.md" && grep -q "core.ex:503" "$t/2026-09-18-wr-04-login-route-rationale-deleted-at-the-core-ex-source.md" && git diff --name-only HEAD~1 -- lib/ priv/ | grep -q . && echo UNEXPECTED_SOURCE_EDIT || echo TODOS_FILED_NO_SOURCE_EDIT'</automated>
  </verify>
  <acceptance_criteria>
    - Both todo files exist, are non-empty, and each begins with YAML front matter delimited by two `---` lines containing `status: pending` and an `audit_acknowledged:` block with `milestone: v1.48`.
    - The WR-04 todo contains the literal `lib/sigra/install/features/core.ex:503`, the literal `live_session :redirect_if_user_is_authenticated`, and the literal `phx-submit`.
    - The IN-05 todo names both files it contrasts — `mfa_settings_live.ex` and `mfa_challenge_live.ex` — and contains the word `lockout`.
    - Both todos contain an explicit statement that Phase 239 did not fix the finding. `grep -ci 'not fixed\|deliberately did not' <each file>` returns `>= 1`.
    - No source file was touched: `git diff --name-only HEAD~1 -- lib/ priv/ test/ .github/` is empty.
    - `git show --name-only --format= HEAD` lists exactly the two todo files.
    - `ls .planning/todos/pending/ | wc -l` increased by exactly 2 relative to before this task (or by fewer with a recorded extend-instead-of-duplicate decision in the SUMMARY).
  </acceptance_criteria>
  <done>WR-04 and IN-05 are tracked with enough diagnosis that the next owner does not re-derive them, the out-of-range reason is written down, and no source file moved.</done>
  <reversibility rating="reversible">Two new Markdown files. Deletable with no downstream effect.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra repo -> adopter project | `docs/`, `README.md` and `CHANGELOG.md` cross into the Hex tarball an adopter downloads. What they say is read as authoritative. |
| Phase criterion -> verifier verdict | An amended criterion is what a re-verifier applies at final HEAD. A criterion amended to match the tree produces a pass that means nothing. |
| Phase 239 -> Phase 241 | Findings removed from 239's scope survive only if 241's requirement text or a todo names them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-10-01 | Tampering | ROADMAP SC-1 / SC-2 | high | mitigate | A criterion amended to whatever currently passes is a moved goalpost. Each amendment carries an inline note stating what is given up; SC-1's names a definition proven *wider* than the failed pass's, with a recorded containment check. |
| T-239-10-02 | Repudiation | Review findings removed from scope | high | mitigate | Narrowing SC-2 could silently delete the 58-reference finding. Two owners are created before the narrowing lands: a SURF-04 clause and a todo carrying the measured per-file breakdown. |
| T-239-10-03 | Information disclosure | new todo files, CONTEXT, EVIDENCE | medium | mitigate | Public repo. Todos carry repo-relative paths and quoted repo content only — no home-directory paths, adopter PII, or brand hex values. |
| T-239-10-04 | Elevation of privilege | `lib/sigra/install/features/core.ex` | medium | accept | WR-04 leaves an auth-adjacent routing constraint unguarded by prose. Accepted for this closure with a filed owner: the root is outside the reviewed range and fixing it opens a fourth edit batch. Severity is below the `block_on: high` threshold because the routing behavior itself is unchanged and correct — only its documented rationale is missing. |
| T-239-10-05 | Information disclosure | WR-01 residue | high | transfer | Handled in plan 239-11, which replaces the false safety claim. Named here so it is not lost between plans. |
| T-239-10-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan — Markdown edits only. RESEARCH.md's Package Legitimacy Gate is not triggered. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification):

- `.planning/todos/pending/2026-09-18-packaged-docs-surface-carries-planning-paths-into-the-hex-tarball.md` (new file)
- `.planning/todos/pending/2026-09-18-wr-04-login-route-rationale-deleted-at-the-core-ex-source.md` (new file)
- `.planning/todos/pending/2026-09-18-in-05-mfa-challenge-auto-submit-contradicts-the-strengthened-settings-comment.md` (new file)
- Decision IDs **D-27**, **D-28**, **D-29** in `239-CONTEXT.md` (new records)
- `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` (new section, with an intentionally unfilled batch-3 slot)
- The SC-1 and SC-2 amendment notes in `.planning/ROADMAP.md` (new clauses)
- The SURF-04 packaged-docs clause and the SURF-01 consistency note in `.planning/REQUIREMENTS.md` (new text)
</artifacts_this_phase_produces>

<verification>
- The Phase 239 SC-2 bullet names `lib/` and `priv/` and carries `D-27`; the SC-1 bullet names `VOCABULARY-LEDGER` and `strictly wider`.
- SURF-04's text names `docs/`, `README.md` and `CHANGELOG.md` while retaining all four of its pre-existing clauses; SURF-04 remains `[ ]` / `Phase 241` / `Pending`.
- `D-27`, `D-28`, `D-29` each appear once in `239-CONTEXT.md`'s decision block with a `Reversibility:` line and an implementing plan.
- Three new todo files exist under `.planning/todos/pending/`.
- `git diff --name-only HEAD~3 -- priv/ test/ lib/ .github/ CHANGELOG.md README.md docs/ mix.exs` is empty.
</verification>

<success_criteria>
Phase 239's criteria assert only what holds; every finding the narrowing removes from its scope has a
named downstream owner; and D-27, D-28 and D-29 are recorded where the remaining executors read them.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-10-SUMMARY.md` when done.
</output>
