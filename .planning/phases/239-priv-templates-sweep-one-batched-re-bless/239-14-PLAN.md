---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 14
type: execute
wave: 14
depends_on: ["239-12"]
files_modified:
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
  - priv/templates/sigra.install/organizations/live/invitation_accept_live.ex
  - test/example/lib/example_web/live/invitation_accept_live.ex
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-13-PLAN.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 84000
  raw_tokens: 42000
  tasks: 4
  confidence: low
  # Calibration factor 2.0 applied (no local sample set; `confidence: low` is derived from a sample
  # count of zero, not self-rated). Four short commits, all in-repo: two decision records, a
  # four-line prose substitution in one template, its mirror, and a four-site surgical amendment to
  # 239-13-PLAN.md. No Postgres, no scaffolded app, no tarball, no re-bless — the fixture is plan
  # 239-15's and every live external OBSERVATION stays plan 239-13's; this plan only corrects the
  # criteria those observations are graded against.

must_haves:
  truths:
    - "The adopter-shipped `@moduledoc` in `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` makes NO claim about what `mix sigra.install` does or does not generate. Falsifiable: a fixed-string `/usr/bin/grep -cF` for the retracted clause returns 0 on that file, paired with a live positive control on the same file that returns non-zero."
    - "Every sentence that survives in that `@moduledoc` block is individually true and individually checked. Falsifiable per clause: (i) Sigra's own suite asserts the absence in the shipped template — the asserting test file and its assertion are named by path and line; (ii) the adopter's generated project does not inherit that assertion — proven by enumerating every `*_test.exs` target in `lib/sigra/install/features/*.ex` and showing the asserting test is not among them, with the enumeration itself non-empty as its own positive control."
    - "The `DO NOT add an accept button here even \"for convenience\"` imperative and the `Jetstream #907 / CVE-2026-1529` attribution are byte-intact in both edited tiers after this plan: `git diff <plan-base>..HEAD` shows neither line among the `+`/`-` content lines, and a fixed-string count of each returns exactly 1 per file."
    - "`test/example/lib/example_web/live/invitation_accept_live.ex` carries the identical corrected prose, located by re-reading the file rather than by citing a line number from plans 239-11 or 239-12 — every line number in their evidence was invalidated by the batch-3 re-bless `87581665`. Falsifiable: the changed region diffed between the two tiers after substitution is empty, and the re-derived counterpart line numbers are recorded next to the command that produced them."
    - "The template edit and its `test/example/` mirror land in two separate commits, each scoped to its own tier and to nothing else (D-19). Falsifiable: `git show --name-only` for the template commit lists exactly one path under `priv/templates/` and nothing else; the mirror commit lists the `test/example/` counterpart plus `.planning/` records only. Neither commit touches `test/fixtures/install_golden/` or `.github/`."
    - "**D-31** is recorded in `239-CONTEXT.md`: WR-01's 239-11 replacement prose is retracted as false on the bytes, the true replacement is stated, and `T-239-12-03`'s first supporting observation is re-based from `0 _test.exs files` to `>= 1 with the installer-emitted path named`. Falsifiable: the decision block exists with that ID and both consequences stated, and it cites `lib/sigra/install/features/admin.ex` by path and line as its evidence."
    - "**D-32** is recorded in `239-CONTEXT.md`: the generated-app V3 claim is scoped to the files `mix sigra.install` **created or modified**, the excluded class is named, and the reason option (a) — a new allowlist entry — was NOT taken is stated as a mechanical fact about the instrument, not a preference. Falsifiable: the instrument's union guard is quoted by line, and the claim that a non-tier allowlist path raises exit 3 on every tier is demonstrated live against a `V3_ALLOWLIST=` copy that is never committed, with its TSV parse controlled before its exit code is interpreted."
    - "D-32's not-a-goalpost-move argument rests on two **checkable facts**, not on two assertions: (a) `git ls-files test/fixtures/install_golden/tree | wc -l` is 84, excludes `page_html/home.html.heex` and `priv/static/images/logo.svg`, and includes `router.ex` — so the phase's primary generated-output surface has scoped by created-or-modified since before the halting probe; (b) SC-1's measured form as amended by D-28 is already the three frozen tier file lists, making plan 239-13's generated-app V3 run a supplement rather than SC-1 itself. Falsifiable: all three commands and SC-1's quoted amended text are recorded in the decision."
    - "The file set D-32 scopes to is frozen as a RULE in `239-EVIDENCE.md` § `## GENERATED-APP-SCOPE` in this plan — before any measurement it governs runs, which is plan 239-13's. Falsifiable: the section exists, cites `test/support/install_fixture.ex`'s `snapshot_paths/1` + `normalize_tree/2` created-or-modified rule by path and line as its derivation source, and is a strict ancestor of any commit carrying a generated-app V3 number."
    - "The frozen scope counts **injected** files as in-scope, not only created ones. `mix sigra.install` injects into `phx.new`-authored files — `lib/sigra/install/injection.ex` names `lib/my_app_web/router.ex` as the canonical `:target` and `lib/sigra/install/features/core.ex` builds `%Injection{}` records for router, config and `runtime.exs`, none of which appear in any `{:eex, …}` target map. Falsifiable: a scope derived from the target maps alone would exclude `router.ex`; the recorded rule must include it, and the recorded set must be either the created-or-modified diff or the explicit union of `{:eex, …}`/copy targets AND `%Injection{}` targets, with which one was used stated. The excluded remainder is enumerated by class and per-file count, never silently dropped (the D-27 two-owner precedent)."
    - "`239-v3-vocabulary-check.sh` and `239-v3-allowlist.tsv` are byte-unchanged by this plan — option 5(b) was chosen precisely so that they stay frozen (D-30). Falsifiable: `git diff --name-only <plan-base>..HEAD --` those two paths is empty."
    - "`.planning/REQUIREMENTS.md` is untouched and SURF-03 stays `[ ]` with a `Pending` roll-up row through every commit here. Only plan 239-13 re-checks it, last and alone."
    - "Plan 239-13's four `T-239-12-03` conflict sites are amended in this plan's own path-scoped commit, so no executor is ever asked to resolve a plan-versus-decision conflict at runtime. Falsifiable: at this plan's final HEAD, `/usr/bin/grep -c \"0 \\`_test.exs\\` files\" 239-13-PLAN.md` returns 0, `/usr/bin/grep -c 'sigra_admin_policy_test.exs' 239-13-PLAN.md` returns `>= 1`, and `git show --name-only` for that commit lists `239-13-PLAN.md` and nothing else."
    - "Batch 4's justification slot is opened but NOT filled here: § `## BATCH-JUSTIFICATION` carries a `BATCH-4-JUSTIFICATION-PENDING` marker following the convention plan 239-10 established, so the discharge in plan 239-15 is greppable rather than asserted."
    - { statement: "The corrected prose is not merely different from the false prose but actually true — checked by treating each surviving clause as a claim and producing a command that could falsify it, rather than by reading the sentence and agreeing with it. Re-writing a false claim into a differently-false claim is exactly what this plan exists to repair, and it is the failure mode a prose edit is most likely to repeat", verification: backstop }
  artifacts:
    - priv/templates/sigra.install/organizations/live/invitation_accept_live.ex
    - test/example/lib/example_web/live/invitation_accept_live.ex
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md
  key_links:
    - "`lib/sigra/install/features/admin.ex` maps `admin/policy_test.exs` to `Path.join([\"test\", otp_app, \"sigra_admin_policy_test.exs\"])`. That single map entry is what falsifies the prose plan 239-12 blessed. The executor re-derives it live rather than citing this line, because a cited map entry is the same substitution defect in a different costume."
    - "Plan 239-11 made its observation against `test/fixtures/install_golden/tree/test/`, which holds three `support/*.ex` modules and no `_test.exs` — because the golden fixture does not snapshot generated `test/*_test.exs` paths at all. A committed snapshot substituted for a generated app is the source tree wearing a disguise. `T-239-12-03` was written to catch exactly that, and it did."
    - "`239-v3-vocabulary-check.sh`'s union guard walks every allowlist entry against the union of the three named tier file lists and raises exit 3 — `is exercised by none of the three named tiers` — for any entry whose path is absent. The `phx.new`-authored paths that produce the five generated-app hits are in none of the three tiers, so option 5(a) cannot be implemented without editing the instrument, which D-30 forbids. That is why D-32 takes option 5(b)."
    - "This plan ends at the two source tiers plus the surgical 239-13 revision. The golden fixture is plan 239-15's single batched re-bless, and every live external observation — Postgres, a scaffolded app, the tarball, `MIX_ENV=test mix ci`, SC-5, the SURF-03 re-check — stays plan 239-13's, re-run after 239-15 with only its `T-239-12-03` lines amended."
    - "Plan 239-13 halted with 0/3 tasks and no commits, but its frontmatter still said `wave: 13` / `depends_on: [\"239-12\"]`. That was re-sequenced to `wave: 16` / `depends_on: [\"239-15\"]` at batch-4 planning time, because a wave-ordered dispatch would otherwise prefer running it BEFORE the correction and re-bless it now depends on. Task 4 asserts that re-sequencing still holds rather than re-applying it."
  prohibitions:
    - statement: "Must NOT re-assert anything about what `mix sigra.install` generates, in either tier, unless that assertion is re-verified live against a generated app — which this plan deliberately does not have. The safe move is to make no such claim at all, which is what D-31 records."
    - statement: "Must NOT alter, reflow, or re-indent the `DO NOT add an accept button here even \"for convenience\"` imperative or the `Jetstream #907 / CVE-2026-1529` attribution in either tier. Both survive byte-intact; they are the lines that actually carry the security constraint."
    - statement: "Must NOT edit `239-v3-vocabulary-check.sh` or `239-v3-allowlist.tsv`. Both are frozen by D-30; downstream plans assert byte-equality against them. Option 5(b) was chosen so this prohibition holds without exception. The fail-closed demonstration in Task 1 runs against a temporary `V3_ALLOWLIST=` copy outside the repo working tree, which is never committed and never replaces the real file."
    - statement: "Must NOT touch `test/fixtures/install_golden/` in any commit of this plan. The fixture is plan 239-15's, reached only through `MIX_ENV=test mix sigra.fixture.rebless_golden` (D-09), never by hand."
    - statement: "Must NOT edit `.planning/REQUIREMENTS.md`, and must NOT re-check SURF-03 under any circumstance. A checkbox flipped on expectation rather than observation is the SURF-02 false-complete defect this phase already filed a todo about."
    - statement: "Must NOT fold any of plan 239-13's live observations into this plan, and must NOT edit `239-13-PLAN.md` outside Task 4's four named lines. Task 4's revision is surgical and path-scoped: it changes the `T-239-12-03` supporting-observation lines and nothing else. Every other clause of 239-13 — its three-tier V3, its fixed-string proof, its tarball, its gate, SC-5 and the SURF-03 re-check — stays byte-identical, because the closure's live observations are exactly what must not be pre-empted."
    - statement: "Must NOT deliver D-31 to plan 239-13 by recording it in `239-CONTEXT.md` alone. Loading CONTEXT.md has never been the delivery vehicle in this phase: `239-13-PLAN.md` cites D-27 and D-30 because the plan text was REVISED to cite them (`c994139a`, then `1e132e74`). A plan whose acceptance criteria contradict a decision does not silently defer to the decision — an executor that resolved the conflict that way would be overriding its own acceptance criteria, which is the discipline this phase exists to defend. D-31 must therefore reach 239-13 as an edit to its text."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23) — a renamed required status context never reports and every PR hangs forever."
    - statement: "Must NOT fill the batch-4 justification here. D-29 requires it in the commit immediately preceding plan 239-15's re-bless; writing it two plans early breaks the ancestry argument's readability even though the ancestor relation would still hold."
    - statement: "Must NOT cite any line number from plans 239-11 or 239-12 as current. Every line number in their evidence was invalidated by the batch-3 re-bless `87581665` and by the 239-11 substitution itself (the counterpart sat at `:332` before and `:334` after). Re-derive every anchor live."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): the dead-instrument case. This repo's interactive shell resolves `grep` to a `ugrep` wrapper that can silently return zero, and this shell does not word-split an unquoted `$(git ls-files …)` expansion — plan 239-11 produced a real dead grep that only its paired control exposed. Every measurement in this plan uses `/usr/bin/grep` explicitly and `git ls-files -z | xargs -0`, and every zero is paired with a live positive control on the same surface. The probe row itself is not auto-resolved by those mitigations."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `adjacency`, UNRESOLVED): the three-tier equality question. Template↔example equality has no mechanism at all (FUT-01, filed and unbuilt); this plan asserts it for one changed region by diffing the substituted regions, which is a per-edit check, not a guard. Template↔golden equality is re-established by plan 239-15's `--check` exit 0. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `ordering`, UNRESOLVED): 'when elements compare equal, is output order specified and stable?' Read here as the D-19 topology — decisions, then template, then mirror, each alone in its commit. Asserted by per-commit path scoping and `git merge-base --is-ancestor`, not by tooling. A future phase could violate the ordering without a gate firing."
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `empty`, UNRESOLVED): an empty file list or an empty feature-map enumeration would make the `*_test.exs` enumeration in Task 2 trivially support the claim. The enumeration is required to be non-empty and to contain the admin policy test as its own positive control, so an empty result reads as a broken command rather than as evidence. Surfaced, not resolved."
---

<objective>
Retract the false adopter-facing claim that plan 239-11 wrote and plan 239-12 blessed into the golden
fixture, replace it with prose whose every surviving clause is separately falsifiable, mirror it into
`test/example/`, and record the two decisions that let plan 239-13 be re-run unchanged.

Purpose: plan 239-11's WR-01 repair replaced a false safety claim with a different false claim. The
replacement asserts that `mix sigra.install` emits no tests; `lib/sigra/install/features/admin.ex`
maps a `_test.exs` template into the adopter's `test/` tree, and a freshly generated app has that file
on disk carrying a real assertion. The observation that backed the replacement was made against
`test/fixtures/install_golden/tree/test/`, which holds only `support/*.ex` modules — because the
golden fixture does not snapshot generated `_test.exs` paths at all. Threat row `T-239-12-03` exists
to catch precisely that substitution, and plan 239-13 halted on it. The sentence now ships, verbatim,
in all three tiers.

The second blocker is dispositioned here rather than inside plan 239-13: running V3 over a freshly
generated app's `{lib,priv}` returns hits that are entirely `phx.new`-authored SVG path geometry —
the same false-positive class the allowlist already carries for `oauth_html.ex`. D-30 forbids
retrofitting the allowlist inside 239-13 to make a measurement pass, and the instrument's own union
guard makes such an entry mechanically impossible anyway. **D-32** scopes the generated-app claim to
Sigra-authored files instead, with the scoping rule frozen here — before the measurement it governs.

Output: two decision records, a frozen generated-app scope rule, one template commit, one mirror
commit, and an opened-but-unfilled batch-4 justification slot for plan 239-15.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-11-SUMMARY.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-12-SUMMARY.md
@priv/templates/sigra.install/organizations/live/invitation_accept_live.ex
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **Measurement hygiene is non-negotiable.** Three separate confident false negatives have already
   been caught in this phase. Every counting command in this plan must:
   - invoke **`/usr/bin/grep`** explicitly — this repo's interactive shell resolves bare `grep` to a
     `ugrep` wrapper that can silently return zero;
   - iterate file lists as **`git ls-files -z … | xargs -0 …`** — this shell does **not** word-split
     an unquoted `$(git ls-files …)` expansion, so the whole list arrives as one argument and every
     count comes back `0` (plan 239-11 hit this for real);
   - carry **no inline `#`** inside a generator command — a round-2 generator commented itself out
     and emitted zero records;
   - be brace-grouped with `|| true` inside the command substitution, because under `set -e` a
     zero-match grep aborts the shell and yields a confident false negative.
2. **Every zero is paired with a live positive control on the same surface** (Standing Constraint 1).
   A bare zero is not evidence. A negative result with no positive control beside it is ambiguous
   between "absent" and "did not run", and this plan treats that ambiguity as a halt.
3. **Instrument exit 3 is a fail-closed HALT, never a result.** Exit 1 means the surface is dirty;
   exit 3 means the instrument cannot answer. Never report an exit 3 as a measurement.
4. **`test/fixtures/install_golden/` is untouched by this plan.** Plan 239-15 owns it.
5. **`.planning/REQUIREMENTS.md` is untouched.** SURF-03 stays `[ ]`; plan 239-13 re-checks it.
6. **`.github/` is read-only** (D-23), and so is every measurement instrument (D-16, D-30).
7. **Evidence at the final committed HEAD on a clean tree** (Standing Constraint 2).
8. **The repo is public.** No home-directory paths, adopter PII, or brand hex values in any artifact.
9. **Do not push.** Commit only, ending messages with
   `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

### Environment preconditions

- **No Postgres, no scaffolded app, no tarball is needed here.** If a task in this plan appears to
  need one, the task has drifted into plan 239-13's scope — stop and report rather than scaffolding.
- **A stale `_build` masquerades as a real failure.** Plan 239-08's first `MIX_ENV=test mix ci` was
  RED with six `Sigra.Audit.Forwarders.ThreadlineTest` failures reporting `Threadline.attach/1 is
  undefined`, caused by a local `_build` holding a sigra beam compiled before the `:threadline` dep
  existed. The fix was `mix deps.compile threadline --force && mix compile --force`, with **zero
  source changes**. Diagnose before editing anything.
- **`mix format --check-formatted` on `priv/templates/` is inapplicable by construction** — templates
  carry EEx placeholders in Elixir syntax positions and `.formatter.exs` deliberately excludes the
  directory. Run the repo-configured `mix format --check-formatted` instead and record the
  substitution, as plan 239-11 did. The `test/example/` counterpart **is** in `inputs:` and is
  checked individually.
- **`gsd-tools` is not on PATH** — invoke `node ~/.claude/gsd-core/bin/gsd-tools.cjs`.

<tasks>

<task type="auto">
  <name>Task 1: Record D-31 and D-32, freeze the generated-app scope rule, and open the batch-4 justification slot</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>The working tree is clean at plan 239-12's final commit, `git status --porcelain` is empty, and `/usr/bin/grep -n '^- \*\*D-3' .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md` shows D-30 as the highest recorded decision. If a D-31 already exists, the numbering assumption is wrong: halt and report rather than overwriting someone else's decision.</precondition>
  <read_first>
    - `239-CONTEXT.md` § `### Gap-Closure Round 2 Decisions (plans 239-09 … 239-13)` — D-27 through D-30 in full. D-27 is the format template: statement, the measurement behind it, alternatives considered and **not** taken with the reason each was rejected, a reversibility rating, and the plan that implements it. D-27 is also the precedent for narrowing a claim while keeping the finding owned by two places rather than deleting it.
    - `239-v3-vocabulary-check.sh` — specifically the union-check block (`allowlist entry '…' is exercised by none of the three named tiers`), the `tier_file_list` function that fixes the three tier file lists, the per-entry non-vacuity control, and the `V3_ALLOWLIST` env override and what its header says it is for.
    - `239-v3-allowlist.tsv` — the single existing entry, its `FALSE-POSITIVE — SVG path coordinates` reason, and the TSV `path <TAB> literal <TAB> reason` shape.
    - `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` — the batch-1/2/3 row format and the `BATCH-3-JUSTIFICATION-PENDING` marker convention plan 239-10 established and plan 239-12 discharged.
    - `lib/sigra/install/features/admin.ex` around the `{:eex, "admin/policy_test.exs", …}` map entry, and every other file under `lib/sigra/install/features/` — the authoritative source for what the installer writes into an adopter's project.
  </read_first>
  <action>
**Re-derive the blocking fact before writing it down.** Enumerate every `test.exs` target across
`lib/sigra/install/features/*.ex` with `/usr/bin/grep -rn 'test\.exs' lib/sigra/install/features/`,
and record the full result. Require the enumeration to be non-empty — an empty result here means the
command is broken, not that the installer writes no tests, and that distinction is the entire subject
of this plan. Classify each hit rather than counting them: the `core.ex` hit is a `config/test.exs`
**injection** target and is not a test file, so only the `admin.ex` pair is a `_test.exs` **creation**
target. Record the exact file:line of the admin policy-test map entry and the `Path.join` target it
computes.

**Write D-31 into `239-CONTEXT.md`**, appended to the round-2 decisions block in D-27's format.
Its statement, in substance:

  * The `@moduledoc` sentence plan 239-11 wrote and plan 239-12 blessed asserts an absence of
    installer-emitted tests. That assertion is **false on the bytes**, disproven by the map entry
    enumerated above and by the corresponding file existing in a freshly generated app.
  * The repair is a **retraction, not a re-verification**: the corrected prose makes no claim about
    installer output at all. What survives are two claims, each separately checkable — Sigra's own
    suite asserts the absence in the shipped template, and the adopter's generated project does not
    inherit that assertion.
  * **Consequence for `T-239-12-03`:** plan 239-13's supporting observation is re-based. Where it
    required a count of zero `_test.exs` files in the generated app's `test/` tree, it now requires
    `>= 1` with `test/<otp_app>/sigra_admin_policy_test.exs` named — the live falsification of the
    retracted claim, paired as before with a `*.exs` positive control on the same tree so an empty
    result cannot pass as a measurement. The threat row is still discharged in 239-13, by the
    opposite observation. **This consequence is not left to be inferred from CONTEXT.md: Task 4 of
    this plan applies it to `239-13-PLAN.md`'s text directly**, because that plan's acceptance
    criteria currently require the `0` and an executor must never be put in the position of choosing
    between its criteria and a decision file.
  * Record why plan 239-11's observation looked right: it was made against
    `test/fixtures/install_golden/tree/test/`, which the golden fixture does not populate with
    `_test.exs` paths. Name the substitution so a later reader sees the mechanism, not just the error.
  * Alternatives considered and **not** taken: (a) *keep the sentence and add a caveat* — a caveat on
    a false sentence still ships a false sentence to every adopter; (b) *delete the whole paragraph* —
    removes a real, checkable safety note from the file the Jetstream #907 defense lives in, trading
    a false claim for a missing one; (c) *re-verify the claim against a generated app and keep it if
    true* — it is not true, and re-verifying inside this plan would require the scaffolded app that
    belongs to 239-13. Reversibility: **reversible** (prose in two source files, git-revertable).
  * Implemented by: Tasks 2 and 3 of this plan; consumed by plans 239-15 and 239-13.

**Then demonstrate, live, why the allowlist cannot carry the generated-app exclusion.** Copy
`239-v3-allowlist.tsv` to a scratch path **outside the repo working tree**, append one entry whose
path is a `phx.new`-authored generated-app path, and run
`V3_ALLOWLIST=<scratch-copy> 239-v3-vocabulary-check.sh priv-templates`. Require exit **3** with
`is exercised by none of the three named tiers` in the output.

**Control the scratch copy's parse before interpreting its exit code.** The allowlist reader splits
on `IFS=$'\t'` and `continue`s any row whose second field is empty, so a row written with **spaces**
instead of a literal tab silently parses as a comment — the run then exits **0** and would read as
a live falsification of a decision that is in fact true. Assert `awk -F'\t' 'NF>=2' <scratch> | wc -l`
equals **2** (the original entry plus the appended one) *before* the exit code means anything. A
count of 1 is a broken fixture, not a result: repair the tab and re-run.

Delete the scratch copy. Confirm `git status --porcelain` shows the real allowlist unchanged. This is
the measurement that makes D-32's central claim a fact rather than a reading of the source.

**Write D-32 into `239-CONTEXT.md`**, same format. Its statement, in substance:

  * The generated-app V3 claim (plan 239-13's criterion over a freshly generated app's `lib/` and
    `priv/`) is **scoped to Sigra-authored files** — the files `mix sigra.install` writes, and no
    other. The `phx.new`-authored remainder is out of scope and enumerated rather than cleaned.
  * **Why option (a), a new allowlist entry, was not taken:** it is mechanically impossible, not
    merely less attractive. Quote the union-check line and its exit-3 message, cite the live
    demonstration above with its output, and state the consequence — a non-tier allowlist path makes
    **every** tier run exit 3, so an entry added to pass a generated-app measurement would break the
    three-tier measurement that plans 239-11, 239-12 and 239-13 all depend on. Repairing that would
    require editing the instrument, which D-30 forbids outright.
  * **The rule is borrowed, not invented.** The scope is *"the files `mix sigra.install` created or
    modified"* — and that rule is **already implemented in this repo, by the golden fixture itself**:
    `test/support/install_fixture.ex` snapshots the app with `snapshot_paths/1` before the install,
    then `normalize_tree/2` drops every path byte-identical to that baseline, under the comment
    *"Only files sigra.install created or modified contribute to the golden snapshot."* Cite it by
    path and line. D-32 does not author a scope; it names the one the phase has been measuring with
    since before this phase existed. Do not re-derive the rule from the
    `lib/sigra/install/features/*.ex` target maps alone — see the under-inclusiveness trap below.
  * **Why this is not a goalpost move — two facts, not two arguments.**
    (a) **The phase has already been scoping this way, under a rule frozen long before the halting
    probe.** `git ls-files test/fixtures/install_golden/tree | wc -l` is **84**; neither
    `page_html/home.html.heex` nor `priv/static/images/logo.svg` is among those 84, while
    `router.ex` **is**. The golden tier — this phase's primary generated-output surface, clean under
    V3 since plan 239-09 — therefore already excludes the `phx.new` remainder and already includes
    the injected files, by the created-or-modified rule above. Scoping the generated-app run by
    authorship **aligns** it with the scope the phase has used throughout; leaving it unscoped is
    what would be the anomaly. Record the three commands and their outputs.
    (b) **The generated-app V3 run is a supplement to SC-1, not SC-1.** SC-1's measured form as
    amended by D-28 is already *zero hits outside the committed allowlist over the tier file lists
    frozen in* § `## VOCABULARY-LEDGER` — that is, over the three tiers. Plan 239-13's generated-app
    V3 run is an additional check layered on top of that named form, so scoping a supplement is a
    materially smaller act than narrowing a criterion. Quote SC-1's amended text to show this.
    Contrast both facts explicitly with the `T-239-09-02` shape the phase already refused: a
    measurement retrofitted **after** seeing its result. State plainly that the excluded class was
    surfaced by the live probe that halted plan 239-13 — the honesty of the record does not depend on
    pretending otherwise; it depends on the rule being content-independent, borrowed from a commit
    far older than this phase, and frozen here before the measurement it governs.
  * **The finding keeps an owner.** Following D-27, the excluded hits are enumerated by path and
    per-file count in § `## GENERATED-APP-SCOPE` with their class named, never silently dropped. They
    are upstream Phoenix content, so they are no adopter-facing Sigra surface and get no Sigra
    remediation owner — state that disposition explicitly rather than leaving the absence of an owner
    to be inferred.
  * Alternatives considered and **not** taken: (a) *retrofit the allowlist inside plan 239-13* —
    forbidden by D-30 and mechanically impossible as demonstrated; (b) *edit the instrument's union
    guard to permit non-tier entries* — the guard exists to stop an entry escaping its own control,
    and D-30 freezes the file; (c) *drop the generated-app V3 measurement entirely* — it is a real
    signal, and dropping a measurement because it is inconvenient is the defect this closure exists
    to repair; the scoped measurement is kept and the remainder is reported.
    Reversibility: **costly** — it amends a criterion plan 239-13 measures against, and a later reader
    reasons from the amended text. Not one-way: the pre-amendment claim is stated inside the decision,
    so it is reconstructable from the decision itself. No `checkpoint:decision` is emitted, following
    the D-26 and D-27 precedent for gap-closure criterion amendments.
  * Implemented by: this task's § `## GENERATED-APP-SCOPE` freeze; consumed by plan 239-13.

**Freeze the scope rule in `239-EVIDENCE.md` § `## GENERATED-APP-SCOPE`** (a new section). It must
carry:

  * The rule text verbatim — *"the files `mix sigra.install` created or modified"* — with its
    derivation source cited by path and line: `test/support/install_fixture.ex`'s
    `snapshot_paths/1` baseline plus `normalize_tree/2`'s byte-identity drop, and the in-source
    comment that states the rule. Record that this rule pre-dates the phase and was not authored by
    this closure.
  * **The under-inclusiveness trap, named so it cannot be walked into again.** `mix sigra.install`
    does not only *create* files; it also **injects into `phx.new`-authored ones**.
    `lib/sigra/install/injection.ex` documents `"lib/my_app_web/router.ex"` as the canonical
    `:target`, and `lib/sigra/install/features/core.ex` builds `%Injection{}` records for the
    router, `config/*.exs` and `runtime.exs` — none of which appear in any `{:eex, …}` target map.
    A scope derived from the target maps alone would put a Sigra-authored bookkeeping line injected
    into `router.ex` **outside** the measurement *and* outside the halt clause, because `router.ex`
    would not count as Sigra-authored. That is the `T-239-09-02` shape arriving through the back
    door, and the created-or-modified rule is what closes it. Record both the `injection.ex` line
    and the `core.ex` `%Injection{}` line numbers, re-derived live.
  * The operative file set, computed as the created-or-modified diff. If the executor cannot compute
    it that way at freeze time, the **minimum admissible fallback** is the explicit union of every
    `{:eex, …}`/copy target AND every `%Injection{}` `:target` across `lib/sigra/install/features/`
    — never the target maps alone. Record which of the two was used and why.
  * The resulting path set as path **patterns**, since a generated app's paths are parameterised by
    `otp_app` and the web module.
  * The excluded class named: `phx.new`-authored files that the installer neither created nor
    modified, and specifically the V2 alternation `\b[0-9]{3}-[0-9]{2}\b` firing on SVG coordinate
    pairs — the identical class already dispositioned FALSE-POSITIVE for `oauth_html.ex` in
    `239-v3-allowlist.tsv`.
  * An explicit instruction that plan 239-13 must enumerate the actual excluded hits by path and
    count at measurement time and **halt** if any excluded path turns out to be one the installer
    created or modified. That halt clause is what keeps the scoping from being able to hide a real
    finding — and it only works because the rule counts injected files as in-scope.

**Open the batch-4 slot** in § `## BATCH-JUSTIFICATION` with a `BATCH-4-JUSTIFICATION-PENDING`
marker, following plan 239-10's convention exactly so plan 239-15's discharge is greppable. Do not
fill it — D-29 requires the justification in the commit immediately before the re-bless.

Commit path-scoped to the two `.planning/` files, message
`docs(239): record D-31 and D-32 and freeze the generated-app measurement scope`.
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; c="$d/239-CONTEXT.md"; e="$d/239-EVIDENCE.md"; s="$d/239-v3-vocabulary-check.sh"; for id in D-31 D-32; do n=$( { /usr/bin/grep -cE -- "\*\*$id[ :]" "$c" || true; } ); test "$n" -ge 1 || { echo "MISSING_DECISION $id"; exit 1; }; done; n=$( { /usr/bin/grep -c "^## GENERATED-APP-SCOPE" "$e" || true; } ); test "$n" = 1 || { echo "MISSING_SCOPE_SECTION n=$n"; exit 1; }; n=$( { /usr/bin/grep -c "BATCH-4-JUSTIFICATION-PENDING" "$e" || true; } ); test "$n" -ge 1 || { echo "MISSING_BATCH4_MARKER"; exit 1; }; ctl=$( { /usr/bin/grep -cE -- "\*\*D-30[ :]" "$c" || true; } ); test "$ctl" -ge 1 || { echo "CONTROL_DEAD: D-30 not found, the decision grep is not measuring what it claims"; exit 1; }; chg=$( { git diff --name-only HEAD~1..HEAD -- "$s" "$d/239-v3-allowlist.tsv" || true; } ); test -z "$chg" || { echo "INSTRUMENT_MODIFIED: $chg"; exit 1; }; out=$( { git show --name-only --format= HEAD || true; } ); printf "%s\n" "$out"; stray=$( { printf "%s\n" "$out" | /usr/bin/grep -v "^$d/" | /usr/bin/grep -c "." || true; } ); test "$stray" = 0 || { echo "COMMIT_NOT_SCOPED stray=$stray"; exit 1; }; echo DECISIONS_RECORDED_AND_SCOPED'</automated>
  </verify>
  <acceptance_criteria>
    - `239-CONTEXT.md` carries **D-31** and **D-32** appended to the round-2 decisions block, each with a statement, the measurement or source behind it, alternatives considered and not taken with the reason for each rejection, a reversibility rating, and the plans that implement and consume it — D-27's format.
    - D-31 cites `lib/sigra/install/features/admin.ex` by path and line for the `_test.exs` map entry, records the full re-derived enumeration of `_test.exs` targets across `lib/sigra/install/features/`, and that enumeration is non-empty. An empty enumeration is a broken command and a halt, not a finding.
    - D-31 states the `T-239-12-03` re-basing explicitly: plan 239-13's supporting observation changes from a required count of zero to a required count `>= 1` with the installer-emitted path named, still paired with a `*.exs` positive control on the same generated tree.
    - D-32 states that option 5(a) is mechanically unavailable and backs it with a **live** demonstration: a scratch allowlist copy carrying one non-tier path makes `239-v3-vocabulary-check.sh priv-templates` exit `3` with `is exercised by none of the three named tiers`. The command and its output are recorded.
    - **The scratch allowlist's parse was controlled before its exit code was interpreted:** `awk -F'\t' 'NF>=2' <scratch> | wc -l` returned `2`, recorded. Only with that control in hand does an exit other than `3` falsify D-32 — and then the task halts and re-plans rather than writing the decision anyway. Without the control, an exit `0` means a space-instead-of-tab row was skipped as a comment, which is a broken fixture and not a finding.
    - D-32 records both goalpost-move facts with their commands: the golden tier's `84`-file list excluding `page_html/home.html.heex` and `priv/static/images/logo.svg` while including `router.ex`, and SC-1's D-28-amended text quoted to show the generated-app V3 run is a supplement to it.
    - § `## GENERATED-APP-SCOPE` cites `test/support/install_fixture.ex`'s created-or-modified rule by path and line, names the injection under-inclusiveness trap with `injection.ex`'s `router.ex` target and `core.ex`'s `%Injection{}` line numbers re-derived live, and states which of the two admissible derivations (created-or-modified diff, or the union of `{:eex, …}`/copy and `%Injection{}` targets) was used. A scope derived from the `{:eex, …}` target maps alone is inadmissible and the task is not done.
    - The scratch allowlist copy lived outside the repo working tree and is deleted: `git status --porcelain` is empty and `git diff --name-only HEAD~1..HEAD -- <instrument> <allowlist>` is empty.
    - D-32 contains a paragraph headed with the goalpost-move question and answers it on the two stated grounds — the rule is content-independent (it names authorship, not matching text) and it is committed before the measurement it governs — and explicitly contrasts itself with the `T-239-09-02` retrofit the phase already refused.
    - `239-EVIDENCE.md` § `## GENERATED-APP-SCOPE` exists and carries: the rule verbatim, the derivation command next to its output, the Sigra-authored path-pattern set, the named excluded class, and the halt clause requiring plan 239-13 to enumerate the actual excluded hits and stop if any is Sigra-authored.
    - § `## BATCH-JUSTIFICATION` carries a `BATCH-4-JUSTIFICATION-PENDING` marker and the batch-4 slot is **not** filled. `/usr/bin/grep -c 'BATCH-4-JUSTIFICATION-PENDING'` returns `>= 1`.
    - This commit lists only paths under the phase directory: `git show --name-only --format= HEAD` contains no path under `priv/`, `lib/`, `test/`, `.github/`, or `.planning/REQUIREMENTS.md`.
    - Every counting command recorded in this task used `/usr/bin/grep` explicitly, and every zero is recorded beside a live positive control on the same surface. A recorded zero with no adjacent control is inadmissible and the task is not done.
  </acceptance_criteria>
  <done>The two amendments plan 239-13 needs are recorded where 239-13 already reads them, the generated-app measurement scope is frozen by a content-independent rule in a commit that precedes every measurement it governs, the impossibility of the allowlist alternative is a demonstrated fact rather than a reading, and batch 4's justification slot is open and greppable for plan 239-15.</done>
  <reversibility rating="costly">D-32 amends a criterion plan 239-13 measures against, and a later reader reasons from the amended text — the same basis as D-27. Not one-way: both decisions state the pre-amendment claim inside themselves, so the original is reconstructable from the record. No `checkpoint:decision` is emitted, following the D-26/D-27 precedent for gap-closure criterion amendments made during planning.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Replace the retracted clause in the template with prose whose every surviving claim is separately checked</name>
  <files>priv/templates/sigra.install/organizations/live/invitation_accept_live.ex</files>
  <precondition>Task 1's decision commit is HEAD and the working tree is clean. `/usr/bin/grep -n 'Structural Jetstream #907 defense' priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` returns exactly one line, anchoring the `@moduledoc` block. If it returns zero or more than one, the file has moved out from under the plan: halt and re-derive rather than editing by remembered line number.</precondition>
  <read_first>
    - `priv/templates/sigra.install/organizations/live/invitation_accept_live.ex` — the whole `@moduledoc`, top of file through the closing `"""`. Re-derive the current line numbers of the `## Structural Jetstream #907 defense` heading and of every line in the paragraph beneath it. Plans 239-11 and 239-12 quote line numbers that the batch-3 re-bless and the 239-11 substitution both invalidated.
    - The same file's `# STRUCTURAL INVARIANT (Jetstream #907 / CVE-2026-1529):` comment block and the `DO NOT add an accept button here even "for convenience"` line beneath it — read them so the edit can be checked against leaving them byte-intact, not because they are edited.
    - The test that asserts the `:mismatch` branch renders zero accept controls — **`T19`**, expected at `test/example/test/example_web/live/invitation_accept_live_test.exs`, in the `describe "structural invariant (Jetstream #907 static check)"` block, named `test "T19: mismatch_branch source has zero phx-click=\"accept...\" and zero phx-submit=\"accept...\""`. It lives inside the **nested example app**, not the root `test/` tree, so a root-scoped search can miss it. Plan 239-13 already names T19, so it is a known-present anchor: its absence means the search is broken, not that the test does not exist. Its path and line are what claim (i) of the corrected prose refers to, and D-31 requires them named.
    - `lib/sigra/install/features/*.ex` — the `_test.exs` target enumeration recorded in Task 1, which is what claim (ii) rests on.
  </read_first>
  <action>
Substitute the closing clause at the end of the `## Structural Jetstream #907 defense` paragraph —
the clause that opens with the inline-code installer invocation and asserts an absence of
installer-emitted tests — so the paragraph's closing sentence instead reads:

```
  this branch. Sigra's own suite asserts this absence in the shipped template;
  your generated project does not inherit that assertion, so if you customize
  this file, add an equivalent assertion to your own test suite.
```

The paragraph is four lines before and four lines after: the first line
(`"by construction, not by convention" defense — do not add accept controls to`) is unchanged and
the three lines quoted above replace the three that follow it. The paragraph's line count is
unchanged. Nothing
outside this paragraph moves.

Then **check each surviving claim as a claim**, and record the check next to it. This is the step
plan 239-11 skipped, and skipping it is what produced the defect being repaired:

  * *Sigra's own suite asserts this absence in the shipped template.* Re-confirm **T19** live at the
    anchor named in `<read_first>` — record its path, its re-derived line number, and the `path =
    Path.join([… "priv", "templates", …])` lines that make the template path its subject, which is
    also what makes the second clause true. Search the **nested example app**, not the root `test/`
    tree: `/usr/bin/grep -rn 'T19:' test/example/test/`. T19 is a known-present anchor that plan
    239-13 already names, so a not-found here is `CONTROL_DEAD` — a broken search — and the executor
    widens the search rather than concluding the clause is false. Only a search that demonstrably
    reaches the file and still finds no such assertion falsifies the clause; in that case halt and
    report rather than writing a third false sentence.
  * *Your generated project does not inherit that assertion.* Show that the asserting test is not
    among the `_test.exs` targets enumerated in Task 1 — a fixed-string search of the enumeration for
    the asserting test's basename returns zero, with the enumeration's non-empty line count recorded
    beside it as the positive control. The zero means "this test is not shipped"; the non-empty count
    means "the enumeration is real".
  * *The paragraph now claims nothing about installer test generation.* A fixed-string
    `/usr/bin/grep -cF` for the retracted clause over this file returns `0`, paired with a
    fixed-string count of a phrase that IS present in the paragraph returning non-zero on the same
    file.

Also confirm the two must-survive lines are untouched: fixed-string counts of the
`DO NOT add an accept button here` imperative and of the `Jetstream #907 / CVE-2026-1529` attribution
each return exactly `1` in this file, and neither appears among the `+`/`-` content lines of
`git diff`.

Run V3 over the `priv-templates` tier and require exit 0 with `hits_outside_allowlist=0` and a
non-zero `control_defmodule=`. Record the raw `hits=` total alongside. Do not edit the instrument or
the allowlist (D-30). An exit 3 is an instrument failure, never a result: halt.

Run the repo-configured `mix format --check-formatted` and record exit 0, with the standing note that
`mix format` on `priv/templates/` is inapplicable by construction (EEx placeholders in Elixir syntax
positions; `.formatter.exs` deliberately excludes the directory) — the same substitution plan 239-11
recorded as a deviation.

Commit path-scoped to this one template file, message
`refactor(239): retract the false installer-test claim from the invitation moduledoc (SURF-01, SURF-03)`.
The commit must contain no other path — not the mirror, not `.planning/`. That isolation is what lets
a reviewer tell a template edit from its mirror (D-19).
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; f=priv/templates/sigra.install/organizations/live/invitation_accept_live.ex; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; s="$d/239-v3-vocabulary-check.sh"; pos=$( { /usr/bin/grep -cF "by construction, not by convention" "$f" || true; } ); test "$pos" -ge 1 || { echo "CONTROL_DEAD: anchor phrase absent, the greps are not reading this file"; exit 1; }; new=$( { /usr/bin/grep -cF "does not inherit that assertion" "$f" || true; } ); test "$new" = 1 || { echo "REPLACEMENT_MISSING n=$new"; exit 1; }; imp=$( { /usr/bin/grep -cF "DO NOT add an accept button here" "$f" || true; } ); test "$imp" = 1 || { echo "IMPERATIVE_DAMAGED n=$imp"; exit 1; }; att=$( { /usr/bin/grep -cF "Jetstream #907 / CVE-2026-1529" "$f" || true; } ); test "$att" = 1 || { echo "ATTRIBUTION_DAMAGED n=$att"; exit 1; }; out=$( "$s" priv-templates 2>&1 ); rc=$?; printf "%s\n" "$out"; test "$rc" = 0 || { echo "TIER_DIRTY rc=$rc (rc=3 means the instrument could not answer, not a dirty surface)"; exit 1; }; printf "%s\n" "$out" | /usr/bin/grep -q "hits_outside_allowlist=0" || { echo "NO_ZERO"; exit 1; }; printf "%s\n" "$out" | /usr/bin/grep -qE "control_defmodule=[1-9]" || { echo "CONTROL_DEAD: tier control is zero"; exit 1; }; names=$( { git show --name-only --format= HEAD || true; } ); printf "%s\n" "$names"; test "$names" = "$f" || { echo "COMMIT_NOT_SCOPED"; exit 1; }; echo TEMPLATE_CORRECTED'</automated>
  </verify>
  <acceptance_criteria>
    - The retracted clause is gone from the template: a fixed-string `/usr/bin/grep -cF` for it returns `0`, paired with a non-zero fixed-string count of an anchor phrase still present in the same paragraph of the same file. Both numbers recorded.
    - The corrected closing sentence is present exactly once, and the paragraph's line count is unchanged from before the edit. Both the before and after line counts are recorded.
    - **Claim (i) checked:** T19 is re-confirmed live in the nested example app at `test/example/test/example_web/live/invitation_accept_live_test.exs`, with its re-derived line number recorded and its template-path subject quoted. A not-found is treated as `CONTROL_DEAD` — a root-scoped search that missed the nested app — and the search is widened; it is never recorded as the clause being false. Only a search proven to reach the file and still find nothing falsifies the clause, and then the task halts with a report.
    - **Claim (ii) checked:** a fixed-string search of the Task-1 `test.exs` target enumeration for T19's basename returns `0`, recorded beside the enumeration's non-empty line count as its positive control, and beside the classification showing the `core.ex` hit is a `config/test.exs` injection target rather than a test file.
    - **Claim (iii) checked:** the paragraph makes no assertion about installer test generation, verified by the fixed-string zero in the first criterion above.
    - `DO NOT add an accept button here even "for convenience"` and `Jetstream #907 / CVE-2026-1529` each count exactly `1` in this file and appear in neither the `+` nor the `-` content lines of the commit's diff.
    - `239-v3-vocabulary-check.sh priv-templates` exits `0` with `hits_outside_allowlist=0` and `control_defmodule >= 1`; the raw `hits=` total is recorded. Exit `3` is an instrument failure and a halt, never a result.
    - The instrument and allowlist are byte-unchanged: `git diff --name-only HEAD~1..HEAD --` those two paths is empty.
    - The commit contains exactly one path, the template file. `git show --name-only --format= HEAD` equals that path and nothing else — no `test/example/`, no `test/fixtures/install_golden/`, no `.planning/`, no `.github/`.
    - The repo-configured `mix format --check-formatted` exits `0`, recorded with the standing note that `mix format` over `priv/templates/` is inapplicable by construction.
  </acceptance_criteria>
  <done>The template no longer ships a claim about installer output, every clause that does ship has a recorded command that could have falsified it, the two security-carrying lines are byte-intact, and the edit is alone in its commit.</done>
  <reversibility rating="reversible">A four-line prose substitution inside one `@moduledoc`, in one file, in one commit. Git-revertable with no downstream state to unwind — the golden fixture does not carry it until plan 239-15 re-blesses.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Mirror the correction into test/example and record the batch-4 sweep evidence</name>
  <files>test/example/lib/example_web/live/invitation_accept_live.ex, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-MIRROR-CHECKLIST.md</files>
  <precondition>Task 2's template commit is HEAD and the working tree is clean. The counterpart file `test/example/lib/example_web/live/invitation_accept_live.ex` exists and still carries the retracted clause — a fixed-string count returning `0` here would mean the mirror is already ahead of the template, which contradicts the phase's mirror model: halt and report.</precondition>
  <read_first>
    - `test/example/lib/example_web/live/invitation_accept_live.ex` — the whole `@moduledoc`. Re-derive its current line numbers. The counterpart's numbering differs from the template's: plan 239-11 recorded the invariant-comment counterpart at `:334` against the template's `:326`, and the `use`/`import` block differs between the tiers. Never copy a template line number.
    - `239-MIRROR-CHECKLIST.md` § the batch-3 block (plan 239-11) — the append-only convention, the per-row disposition format, and the "counterpart line located by reading the file, not by copying the template's number" note. Batch 4 appends a block in the same shape; prior rows stay byte-unchanged.
    - `239-EVIDENCE.md` § `## BATCH-3-SWEEP-COMMIT` — the subsection lettering and record format that § `## BATCH-4-SWEEP-COMMIT` follows.
  </read_first>
  <action>
Apply the identical substitution to the `test/example/` counterpart, located by reading the file.
The three replacement lines are byte-identical to the template's; the surrounding lines are the
counterpart's own.

Then prove tier equality for the changed region rather than asserting it: extract the substituted
paragraph from both files and diff the two extractions — the diff must be **empty**. Record the
extraction commands and both re-derived line ranges. If a difference outside the changed region shows
up, record it and prove it pre-existing by reproducing the identical diff at this plan's base commit,
exactly as plan 239-11 did for the `class="modal"` vs `class="vt-modal"` drift. Recorded, not
reconciled — reconciling pre-existing drift inside a mirror commit makes the commit unauditable.

Re-run the same three claim checks from Task 2 against the counterpart file: the retracted clause
counts `0` with a live control, the corrected sentence counts `1`, and the imperative and attribution
each count `1`.

Run V3 over the `example` tier and require exit 0 with `hits_outside_allowlist=0` and a non-zero
`control_defmodule=`. Record the result **with its scoping** — the `example` tier is
`239-v3-vocabulary-check.sh`'s two-file SC-4 counterpart list, never all of `test/example/`, whose
measured 482-line / 157-file remainder is routed to Phase 241 SURF-04 (D-30). A record that omits the
scoping overstates the claim.

Run `mix format --check-formatted` on this counterpart individually — unlike the template, it **is**
in `.formatter.exs` `inputs:` — and record exit 0.

Write § `## BATCH-4-SWEEP-COMMIT` into `239-EVIDENCE.md` carrying every command and its output
verbatim: the template edit and its commit sha, the mirror edit and its re-derived line ranges, the
three claim checks per tier with their controls, the two V3 tier results with raw/allowlisted/outside
counts, the format results, and the changed-line classification (how many `+`/`-` content lines, and
that every one of them is `@moduledoc` prose with no function head, guard, route, plug, `attr`, or
`default:` among them).

Append the batch-4 block to `239-MIRROR-CHECKLIST.md` in plan 239-11's append-only style: prior rows
and blocks byte-unchanged, one new row for the mirrored file with its disposition and its re-derived
counterpart line numbers.

Commit path-scoped to the counterpart plus the two `.planning/` records, message
`docs(239-14): mirror the moduledoc retraction into test/example and record batch-4 sweep evidence`.
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; t=priv/templates/sigra.install/organizations/live/invitation_accept_live.ex; x=test/example/lib/example_web/live/invitation_accept_live.ex; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; s="$d/239-v3-vocabulary-check.sh"; for f in "$t" "$x"; do pos=$( { /usr/bin/grep -cF "by construction, not by convention" "$f" || true; } ); test "$pos" -ge 1 || { echo "CONTROL_DEAD on $f"; exit 1; }; n=$( { /usr/bin/grep -cF "does not inherit that assertion" "$f" || true; } ); test "$n" = 1 || { echo "REPLACEMENT_MISSING on $f n=$n"; exit 1; }; i=$( { /usr/bin/grep -cF "DO NOT add an accept button here" "$f" || true; } ); test "$i" = 1 || { echo "IMPERATIVE_DAMAGED on $f"; exit 1; }; a=$( { /usr/bin/grep -cF "Jetstream #907 / CVE-2026-1529" "$f" || true; } ); test "$a" = 1 || { echo "ATTRIBUTION_DAMAGED on $f"; exit 1; }; done; out=$( "$s" example 2>&1 ); rc=$?; printf "%s\n" "$out"; test "$rc" = 0 || { echo "EXAMPLE_TIER_DIRTY rc=$rc (rc=3 means the instrument could not answer)"; exit 1; }; printf "%s\n" "$out" | /usr/bin/grep -q "hits_outside_allowlist=0" || { echo "NO_ZERO"; exit 1; }; printf "%s\n" "$out" | /usr/bin/grep -qE "control_defmodule=[1-9]" || { echo "CONTROL_DEAD: example tier control is zero"; exit 1; }; sec=$( { /usr/bin/grep -c "^## BATCH-4-SWEEP-COMMIT" "$d/239-EVIDENCE.md" || true; } ); test "$sec" = 1 || { echo "MISSING_EVIDENCE_SECTION n=$sec"; exit 1; }; g=$( { git diff --name-only HEAD~2..HEAD -- test/fixtures/install_golden .github .planning/REQUIREMENTS.md || true; } ); test -z "$g" || { echo "FORBIDDEN_PATHS_TOUCHED: $g"; exit 1; }; mix format --check-formatted || { echo "FORMAT_RED"; exit 1; }; echo MIRROR_COMPLETE'</automated>
  </verify>
  <acceptance_criteria>
    - The counterpart carries the identical corrected sentence exactly once, and its counterpart line numbers were **re-derived by reading the file** — the command that produced them is recorded, and no line number from plans 239-11 or 239-12 is cited as current.
    - The substituted paragraph extracted from both tiers diffs **empty**, with both extraction commands and both line ranges recorded. Any difference outside the changed region is recorded and proven pre-existing by reproducing the identical diff at this plan's base commit; it is not reconciled here.
    - The three claim checks pass on the counterpart with the same controls as Task 2: retracted clause `0` with a live positive control, corrected sentence `1`, imperative `1`, attribution `1`.
    - `239-v3-vocabulary-check.sh example` exits `0` with `hits_outside_allowlist=0` and `control_defmodule >= 1`, and the result is recorded **with its scoping** — two SC-4 counterpart files, never all of `test/example/`, whose 482-line / 157-file remainder stays routed to Phase 241 SURF-04 (D-30). A record of this number without its scoping is inadmissible.
    - `mix format --check-formatted` exits `0`, and the counterpart was additionally checked individually since it is in `.formatter.exs` `inputs:`.
    - `239-EVIDENCE.md` § `## BATCH-4-SWEEP-COMMIT` exists and carries every command with its verbatim output, including the changed-line classification showing every `+`/`-` content line is `@moduledoc` prose — no function head, guard, route, plug, `attr`, or `default:`.
    - `239-MIRROR-CHECKLIST.md` gained a batch-4 block append-only: `git diff --numstat HEAD~1..HEAD -- <checklist>` shows `0` deletions.
    - The mirror commit contains the counterpart plus the two `.planning/` records and nothing else. `git diff --name-only HEAD~2..HEAD -- test/fixtures/install_golden .github .planning/REQUIREMENTS.md` is empty across both of this plan's source commits.
    - SURF-03 is still `[ ]` at this plan's final HEAD: `/usr/bin/grep -c '^- \[ \] \*\*SURF-03\*\*' .planning/REQUIREMENTS.md` returns `1`.
    - `git merge-base --is-ancestor <task-2-template-sha> <this-mirror-sha>` exits `0` and the two shas differ — the D-19 template-then-mirror ordering is a property of the commit graph.
  </acceptance_criteria>
  <done>Both source tiers carry the corrected prose, their changed regions are proven equal rather than assumed equal, the evidence record carries every command with its output and its controls, and the golden fixture is left stale by exactly this batch — which is the state plan 239-15 requires.</done>
  <reversibility rating="reversible">A mirror of a prose substitution plus two append-only records. Git-revertable; nothing downstream depends on it until plan 239-15's re-bless, which is a separate commit in a separate plan.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Apply D-31 to plan 239-13's text so its acceptance criteria stop contradicting it</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-13-PLAN.md</files>
  <precondition>Task 3's mirror commit is HEAD, the working tree is clean, and D-31 is recorded in `239-CONTEXT.md` from Task 1 — the citation this task writes must resolve to a decision that already exists. Also assert the re-sequencing done at batch-4 planning time still holds: `239-13-PLAN.md`'s frontmatter reads `wave: 16` and `depends_on: ["239-15"]`. If it still reads `wave: 13` / `depends_on: ["239-12"]`, a wave-ordered dispatch would run 239-13 before this plan's own prerequisites: halt and report rather than silently re-applying it.</precondition>
  <read_first>
    - `239-13-PLAN.md` — the four `T-239-12-03` conflict sites, located by re-reading rather than by the line numbers quoted below, which are a starting anchor only: (1) the `must_haves.truths` entry asserting the generated `test/` tree contains zero `_test.exs` files (~L34, shifted by this plan's own frontmatter comment); (2) the Task 1 action's rationale paragraph leaning on the now-retracted prose (~L187); (3) the Task 1 action's first bullet requiring a count of `0` (~L189); (4) the Task 1 acceptance criterion requiring `0` (~L214).
    - `239-13-PLAN.md`'s existing citation style for D-27 and D-30 — the phrasing to match when writing `as amended by D-31`. The plan cites D-27 eight times and D-30 ten times because its text was revised to cite them (`c994139a`, then `1e132e74`); this task is the same operation for D-31, and `1e132e74`'s commit message is the shape precedent.
    - `239-CONTEXT.md`'s D-31, as written in Task 1 — the amendment's authoritative wording, which this task transcribes into 239-13 rather than paraphrasing.
  </read_first>
  <action>
Make a **surgical, path-scoped** revision to `239-13-PLAN.md`. Four sites, nothing else.

At each of the three sites that require a count of `0` `_test.exs` files in the generated app's
`test/` tree, invert the requirement to the D-31 form: **`>= 1`**, with
`test/<otp_app>/sigra_admin_policy_test.exs` named as the file that must be found, and the `*.exs`
positive control on that same tree **retained unchanged** — the control is what stops an empty result
passing as a measurement, and it is as necessary for a `>= 1` as it was for a `0`. Cite the authority
inline in the phase's established style, `as amended by D-31`, so a later reader following the number
lands on the decision rather than on a bare reversal.

At the fourth site, re-base the rationale paragraph. It currently justifies the observation by
appealing to the prose being blessed into the fixture — prose that plan 239-15's re-bless removes, so
after batch 4 the paragraph argues from a sentence that no longer exists in any tier. Replace the
appeal with the retraction's own logic: the claim was **retracted** because it was false on the bytes,
and the observation is now made to **falsify** it live on the artifact the adopter actually gets,
which is what discharges `T-239-12-03` rather than merely transferring it.

Do not touch anything else in the file. Its three-tier V3 run, its fixed-string proof, its tarball
check, `MIX_ENV=test mix ci`, SC-5 and the SURF-03 re-check stay byte-identical — those are the
closure's live observations and pre-empting or softening any of them is precisely what this plan must
not do. Record the before/after of each of the four sites in `239-EVIDENCE.md` § `## BATCH-4-SWEEP-COMMIT`
as a sub-entry, so the amendment is auditable without a diff archaeology pass.

Verify the amendment is self-consistent before committing: the file must no longer require a zero
anywhere in the `T-239-12-03` row, must name the installer-emitted path, must retain the `*.exs`
control, and must still pass `verify.plan-structure`.

Commit path-scoped to `239-13-PLAN.md` alone, message
`docs(239-13): re-base the T-239-12-03 supporting observation per D-31`.
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; p="$d/239-13-PLAN.md"; test -s "$p" || { echo "MISSING_239_13"; exit 1; }; ctl=$( { /usr/bin/grep -c "T-239-12-03" "$p" || true; } ); test "$ctl" -ge 2 || { echo "CONTROL_DEAD: T-239-12-03 not found in 239-13, the greps are not reading the right file"; exit 1; }; z=$( { /usr/bin/grep -cE "0 .?_test\.exs.? files|_test\.exs.{0,40}wc -l. returns .?0" "$p" || true; } ); test "$z" = 0 || { echo "STALE_ZERO_REQUIREMENT n=$z"; exit 1; }; n=$( { /usr/bin/grep -c "sigra_admin_policy_test.exs" "$p" || true; } ); test "$n" -ge 1 || { echo "INSTALLER_PATH_NOT_NAMED"; exit 1; }; c=$( { /usr/bin/grep -c "as amended by D-31" "$p" || true; } ); test "$c" -ge 1 || { echo "D31_CITATION_MISSING"; exit 1; }; ctrl2=$( { /usr/bin/grep -c "\*\.exs" "$p" || true; } ); test "$ctrl2" -ge 1 || { echo "POSITIVE_CONTROL_DROPPED"; exit 1; }; w=$( { /usr/bin/grep -c "^depends_on: \[\"239-15\"\]" "$p" || true; } ); test "$w" = 1 || { echo "RESEQUENCING_LOST"; exit 1; }; names=$( { git show --name-only --format= HEAD || true; } ); printf "%s\n" "$names"; test "$names" = "$p" || { echo "COMMIT_NOT_SCOPED"; exit 1; }; node ~/.claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure "$p" | /usr/bin/grep -q "\"valid\": true" || { echo "STRUCTURE_INVALID"; exit 1; }; echo PLAN_239_13_AMENDED'</automated>
  </verify>
  <acceptance_criteria>
    - All four `T-239-12-03` conflict sites in `239-13-PLAN.md` are amended and located by re-reading, not by the anchor line numbers in `<read_first>`. The before/after of each is recorded in § `## BATCH-4-SWEEP-COMMIT`.
    - No site in that file still requires a count of `0` `_test.exs` files. The negative grep is paired with a live positive control (`T-239-12-03` itself appears `>= 2` times) so a zero cannot mean the grep read the wrong file.
    - `test/<otp_app>/sigra_admin_policy_test.exs` is named at least once, and the `*.exs` positive control on the generated `test/` tree is retained — dropping the control while inverting the count would trade one unfalsifiable claim for another.
    - `as amended by D-31` appears at least once, in the phase's existing D-27/D-30 citation style, and D-31 exists in `239-CONTEXT.md` from Task 1 so the citation resolves.
    - The rationale paragraph no longer argues from the retracted prose — which plan 239-15 removes from every tier — and instead argues from the retraction itself.
    - Nothing else in `239-13-PLAN.md` changed: its three-tier V3, fixed-string proof, tarball check, `MIX_ENV=test mix ci`, SC-5 and SURF-03 re-check clauses are byte-identical. The diff is confined to the four sites plus the evidence sub-entry's counterpart lines.
    - The frontmatter re-sequencing survives: `depends_on: ["239-15"]` and `wave: 16` are intact. This task asserts them; it does not re-apply them.
    - `verify.plan-structure` reports `valid: true` on the amended file.
    - The commit contains exactly one path, `239-13-PLAN.md`. `git show --name-only --format= HEAD` equals that path and nothing else.
  </acceptance_criteria>
  <done>Plan 239-13's text agrees with D-31, so re-running it produces a green on the observation that actually holds instead of halting on a criterion that cannot be met — and no executor is ever asked to choose between its own acceptance criteria and a decision file.</done>
  <reversibility rating="costly">This edits another plan's acceptance criteria. Git-revertable, but a revert restores a criterion that is false on the bytes and would halt the closure a third time on the same row — so the record of what changed and why lives in § `## BATCH-4-SWEEP-COMMIT` and in D-31, not only in the diff. Not one-way; no `checkpoint:decision` is emitted.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra repository -> adopter's generated project (`mix sigra.install`) | Prose in `priv/templates/` becomes prose in every adopter's source tree. A false claim here is shipped, not merely recorded. |
| Committed fixture -> "generated app" evidence | A snapshot substituted for a generated app reads as the same evidence while measuring a different thing. This is the exact substitution that produced the defect under repair. |
| Decision record -> downstream plan authority | Plans 239-13 and 239-15 read `239-CONTEXT.md` for authority. A decision recorded only in EVIDENCE is not readable from there (D-30's own preamble states this). |
| Measurement instrument -> phase verdict | `239-v3-vocabulary-check.sh` and its allowlist are frozen. Any edit to either turns a surface result into a measurement result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-14-01 | Spoofing | the replacement `@moduledoc` prose | critical | mitigate | Replacing a false claim with a differently-false claim is the failure this plan repairs, and prose edits repeat it easily. Every surviving clause is treated as a claim with a recorded command that could falsify it; a clause that cannot be checked is removed rather than softened, and a failed check halts the task instead of producing a third revision. |
| T-239-14-02 | Spoofing | committed fixture substituted for a generated app | critical | mitigate | Plan 239-11's observation was made against `test/fixtures/install_golden/tree/test/`, which the fixture never populates with `_test.exs` paths. This plan makes no generated-app claim at all: claim (ii) rests on the installer's own `lib/sigra/install/features/*.ex` target maps, which are the authoritative source rather than a snapshot, and the generated-app re-observation stays plan 239-13's. |
| T-239-14-03 | Tampering | `239-v3-allowlist.tsv` | high | mitigate | Retrofitting an allowlist entry to make a measurement pass is the `T-239-09-02` goalpost move. D-32 takes option 5(b) instead; the impossibility of option 5(a) is demonstrated live against a scratch copy outside the working tree that is deleted immediately, and byte-equality of the real file is asserted in every task's criteria. |
| T-239-14-04 | Repudiation | the D-32 scope narrowing | high | mitigate | A narrowing that silently deletes a finding is the same defect as a green gate that verified nothing (D-27's own words). The excluded class is named, the excluded hits are enumerated at measurement time by a halt clause frozen in § `## GENERATED-APP-SCOPE`, and the rule is content-independent and committed before the measurement it governs. |
| T-239-14-05 | Spoofing | zero-result greps | high | mitigate | This repo's interactive shell resolves `grep` to a `ugrep` wrapper that can silently return zero; this shell does not word-split unquoted `$(git ls-files …)`; an inline `#` once commented out a generator. Three confident false negatives have already been caught in this phase. Every command uses `/usr/bin/grep` explicitly, iterates via `git ls-files -z | xargs -0`, carries no inline `#`, and pairs every zero with a live positive control on the same surface. |
| T-239-14-06 | Tampering | `test/fixtures/install_golden/` | high | mitigate | A hand-edited fixture would make the golden tier pass while diverging from generated output. This plan never writes to that path; the commit-scope criteria in all three tasks prove it, and plan 239-15 reaches it only through `mix sigra.fixture.rebless_golden` (D-09). |
| T-239-14-07 | Elevation of privilege | the `:mismatch` structural defense | critical | mitigate | The `DO NOT add an accept button here even "for convenience"` imperative and the `Jetstream #907 / CVE-2026-1529` attribution are the lines that actually carry the authorization-bypass defense. Both are asserted byte-intact by fixed-string counts in both tiers and by their absence from the diff's content lines. A prose edit that damaged them would weaken a real security control. |
| T-239-14-08 | Tampering | `.github/` workflow `name:` lines | critical | mitigate | A renamed required status context never reports and every PR hangs forever (D-23). `.github/` is read-only here and its absence from every commit is asserted across both source commits. |
| T-239-14-09 | Information disclosure | `239-CONTEXT.md`, `239-EVIDENCE.md` | medium | mitigate | Public repo. Every recorded path is repo-relative; the scratch allowlist copy lives outside the working tree and its path is recorded as a basename, never as an absolute home path. No adopter PII and no brand hex values. |
| T-239-14-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. No new dependency is added; RESEARCH.md's Package Legitimacy Gate covers no new package here. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification):

- `D-31` and `D-32` in `239-CONTEXT.md` § `### Gap-Closure Round 2 Decisions` (new decision records)
- `239-EVIDENCE.md` § `## GENERATED-APP-SCOPE` (new section)
- `239-EVIDENCE.md` § `## BATCH-4-SWEEP-COMMIT` (new section)
- The `BATCH-4-JUSTIFICATION-PENDING` marker in `239-EVIDENCE.md` § `## BATCH-JUSTIFICATION` (new content in a section created by plan 239-10)
- The batch-4 block in `239-MIRROR-CHECKLIST.md` (new append-only block)

No new module, function, route, script, or configuration key is created. Every source change is
`@moduledoc` prose.
</artifacts_this_phase_produces>

<verification>
- D-31 and D-32 exist in `239-CONTEXT.md` in D-27's format, each with its measurement, its rejected alternatives, and its reversibility rating.
- D-32's central claim is backed by a live exit-3 demonstration against a scratch allowlist copy that is deleted and never committed.
- `239-EVIDENCE.md` carries § `## GENERATED-APP-SCOPE` with the content-independent rule, the excluded class, and the halt clause for plan 239-13.
- The retracted clause returns `0` on fixed-string count in both source tiers, each paired with a live positive control on the same file.
- The `DO NOT add an accept button here` imperative and the `Jetstream #907 / CVE-2026-1529` attribution each count exactly `1` per tier and appear in neither the `+` nor the `-` content lines.
- `239-v3-vocabulary-check.sh` exits `0` with `hits_outside_allowlist=0` and a live `control_defmodule` on `priv-templates` and on the scoped `example` tier; the instrument and allowlist are byte-unchanged across this plan.
- Three commits, D-19 topology: decisions, then template, then mirror — each path-scoped, ancestry provable by `git merge-base --is-ancestor`.
- `git diff --name-only <plan-base>..HEAD -- test/fixtures/install_golden .github .planning/REQUIREMENTS.md` is empty. SURF-03 still reads `[ ]` with a `Pending` roll-up row.
</verification>

<success_criteria>
The adopter-shipped moduledoc carries no claim about installer output and every claim it does carry
has a recorded command that could have falsified it; the correction is mirrored with its changed
region proven equal across tiers; D-31 and D-32 give plan 239-13 both amendments it needs without
editing `239-13-PLAN.md`; the generated-app measurement scope is frozen by a content-independent rule
committed before the measurement it governs; the instrument and allowlist are byte-unchanged; and the
golden fixture is left stale by exactly this batch for plan 239-15.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-14-SUMMARY.md` when done.
</output>
