---
phase: 239-priv-templates-sweep-one-batched-re-bless
plan: 16
type: execute
wave: 17
depends_on: ["239-15"]
files_modified:
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
  - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-13-PLAN.md
autonomous: true
requirements: [SURF-01, SURF-03]
gap_closure: true

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: low
  # Calibration factor 2.0 applied (no local sample set; `confidence: low` is derived from a sample
  # count of zero, not self-rated). Two in-repo commits: one decision record with a live RED
  # demonstration on a scratch surface, and a two-site surgical amendment to 239-13-PLAN.md. No
  # Postgres, no scaffolded app, no tarball, no re-bless, no source edit. Plan 239-14's comparable
  # decision-record task came in well under this; the margin covers the scratch-surface probe.

must_haves:
  truths:
    - "**D-33** is recorded in `239-CONTEXT.md` in the D-26…D-32 style, with a Statement, the measurement behind it, alternatives considered and NOT taken with the reason each was rejected, a Reversibility line, and the implementing plan named. Falsifiable: `/usr/bin/grep -cE -- '\\*\\*D-33[ :]' 239-CONTEXT.md` returns `>= 1`, paired with the same grep for `D-32` as a live positive control on the same file."
    - "D-33's amended criterion is stated precisely enough to execute without interpretation: `hits_outside_allowlist = 0` **after excluding hits whose (basename, literal) matches a committed allowlist record**, with the exception enumerated by name and count, and a halt clause for any surviving hit. Falsifiable: the derivation is written as a runnable procedure over the instrument's printed `path:line: text` records — not as a description of one — and it uses no instrument edit."
    - "**The goalpost question is answered from the commit graph, not argued.** The disposition D-33 re-matches already existed long before the measurement that falsified the old criterion: plan 239-08 dispositioned the `373-12` SVG-coordinate hit FALSE-POSITIVE (`1a85508e`) and plan 239-09 committed it to `239-v3-allowlist.tsv` (`23f3c711`). Falsifiable: `git merge-base --is-ancestor 1a85508e 23f3c711` exits 0, `git merge-base --is-ancestor 23f3c711 HEAD` exits 0, and both shas are recorded. D-33 changes only how an existing disposition is matched across a rendering boundary — never what counts as bookkeeping."
    - "**The exception is bounded and the bound is stated as a property, not a promise.** It admits a hit only when the hit's literal is byte-identical to a committed allowlist record's literal AND the hit's basename equals that record's basename. Falsifiable: a hit carrying the allowlisted literal under a DIFFERENT basename is NOT excluded, and a hit under the allowlisted basename carrying a DIFFERENT literal is NOT excluded — both demonstrated live on a scratch surface. The exception therefore cannot widen to cover anything not already triaged."
    - "**The amended criterion is demonstrated non-vacuous — it can still fail.** On a scratch surface outside the repo working tree, a V3 hit that is not an allowlisted literal survives the (basename, literal) exclusion and leaves the criterion RED. Falsifiable: the run is recorded with its command, its `files_measured`, its non-zero `control_defmodule`, and the surviving hit named. A criterion demonstrated only on a surface where it passes is an unfalsified instrument, which is the defect this phase has spent four plans repairing."
    - "The scratch surface is parse-controlled before its result is interpreted, lives outside the repo working tree, is deleted after, and is never committed. Falsifiable: `files_measured` equals the scratch file count and `control_defmodule` is non-zero (a zero raises the instrument's own exit 3), and `git status --porcelain` is empty at the end of the task."
    - "Plan 239-13's generated-app V3 criterion is amended **in its own text**, path-scoped to that one file, so no executor is ever asked to choose between its acceptance criteria and a decision file. Falsifiable: at this plan's final HEAD, `/usr/bin/grep -c 'no allowlist entry.s path is expected in it' 239-13-PLAN.md` returns `0` paired with a live `D-33` count `>= 1` on the same file, and `git show --name-only --format=` for that commit lists `239-13-PLAN.md` and nothing else."
    - "Every other clause of `239-13-PLAN.md` is byte-identical: its three-tier V3, its per-alternation controls, its SC-1 fixed-string proof, its tarball check, `MIX_ENV=test mix ci`, SC-5 and the SURF-03 re-check. Falsifiable: the commit's diff is confined to the two amended sites; no other `+`/`-` content line exists."
    - "`239-13-PLAN.md`'s frontmatter re-sequencing to `wave: 18` / `depends_on: [\"239-16\"]` is **asserted, not re-applied** — it was set at this plan's planning time so that \"runs last\" is machine-readable before any dispatch. Falsifiable: `/usr/bin/grep -c '^depends_on: \\[\"239-16\"\\]' 239-13-PLAN.md` returns `1`."
    - "`239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv` and `239-comment-only-diff-check.sh` are byte-unchanged by this plan. D-33 amends the criterion precisely so the instrument does not have to move. Falsifiable: `git diff --name-only <plan-base>..HEAD --` those three paths is empty."
    - "`.planning/REQUIREMENTS.md` is untouched and SURF-03 stays `[ ]` with a `Pending` roll-up row through every commit here. Only plan 239-13 re-checks it, last and alone."
    - { statement: "The amendment narrows nothing. Checked by confirming the excluded hit's literal is byte-identical on both surfaces and fired by the same alternation, so the same line that was already triaged on the template surface is the line being excluded on the rendered one — if the two literals differed even by whitespace, this would be a new disposition wearing an old one's name", verification: backstop }
  artifacts:
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md
    - .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-13-PLAN.md
  key_links:
    - "`239-v3-vocabulary-check.sh` keys allowlist matching on `(path, literal)` — `h_path = AL_PATHS[i]` plus a substring test on the hit text. The template path `priv/templates/sigra.gen.oauth/oauth_html.ex` and the rendered path `lib/<web>/controllers/oauth_html.ex` are different strings carrying identical bytes, so the committed record cannot match its own rendered counterpart. That is the whole defect; it is a matching-key artifact, not a finding about the surface."
    - "The criterion's embedded assumption — *no allowlist entry's path is expected in it* — held at every tier ever measured, because the golden fixture contains **zero** `oauth_html` files while the install-smoke app installs the OAuth generator's output. The assumption was true of every prior stand-in for generated output and false of the real thing. Amending it is correcting an assumption that was never tested, not lowering a bar that was."
    - "The generated `oauth_html.ex` IS inside D-32's created-or-modified scope, so D-32 neither can nor may exclude it. Reaching for D-32 here would be scope-creep on a decision written for a different class of file, and would put a Sigra-authored file outside the measurement — exactly what D-32's injection clause exists to prevent."
    - "This phase's delivery vehicle for a decision that changes 239-13's criteria is **revising 239-13's text** (`c994139a`, `1e132e74`, `cc2f5c61`), never `239-CONTEXT.md` alone. A plan whose acceptance criteria contradict a decision does not silently defer to the decision."
  prohibitions:
    - statement: "Must NOT edit `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv` or `239-comment-only-diff-check.sh` (D-16, D-30). Amending the allowlist's matching key would be an instrument edit, and every plan since 239-09 asserts byte-equality against these files. The criterion moves; the instrument does not."
    - statement: "Must NOT add, edit or reorder any record in `239-v3-allowlist.tsv`, including a rendered-path duplicate of the existing record. Beyond the byte-equality prohibition above, a rendered-path entry is not in any of the three named tiers and would raise the instrument's union guard to exit 3 on every tier run — the mechanical impossibility D-32 already demonstrated live."
    - statement: "Must NOT widen D-32 to cover the generated `oauth_html.ex`. It is a file `mix sigra.install` created, squarely inside D-32's scope; excluding it there would put a Sigra-authored file outside the measurement and hollow out D-32's own halt clause."
    - statement: "Must NOT drop, soften or make optional the generated-app V3 measurement. The operator's decision is to amend the criterion and keep the measurement; dropping it is the goalpost move this phase has twice refused."
    - statement: "Must NOT state the exception as a path allowlist, a directory exclusion, or a count threshold. It is a (basename, literal) match against a committed record, and the literal half is what bounds it. An exception keyed on basename alone would admit any future hit in a file with that name."
    - statement: "Must NOT deliver D-33 to plan 239-13 by recording it in `239-CONTEXT.md` alone. An executor that resolved a criteria-versus-decision conflict in favour of the decision would be overriding its own acceptance criteria, which is the discipline this phase exists to defend."
    - statement: "Must NOT edit `239-13-PLAN.md` outside the two amended criterion sites. Its three-tier V3, per-alternation controls, SC-1 fixed-string proof, tarball check, `MIX_ENV=test mix ci`, SC-5 and SURF-03 re-check stay byte-identical — those are the closure's live observations and pre-empting or softening any of them is what this plan must not do."
    - statement: "Must NOT re-apply `239-13-PLAN.md`'s frontmatter re-sequencing. `wave: 18` / `depends_on: [\"239-16\"]` were set at this plan's planning time; Task 2 asserts them and halts if they have drifted."
    - statement: "Must NOT edit `.planning/REQUIREMENTS.md` or re-check SURF-03. A checkbox flipped on expectation rather than observation is the SURF-02 false-complete defect this phase already filed a todo about."
    - statement: "Must NOT commit the scratch surface or any scratch allowlist copy, and must NOT place either inside the repo working tree. Both live outside it and are deleted before the task ends."
    - statement: "Must NOT run `scripts/ci/install-smoke.sh`, scaffold an app, build a tarball, or run `MIX_ENV=test mix ci`. Every live external observation stays plan 239-13's; re-making one here would pre-empt the plan this amendment exists to unblock."
    - statement: "Must NOT edit any file under `.github/`, and in particular must NOT change a `name:` line (D-23) — a renamed required status context never reports and every PR hangs forever."
  edge_coverage_assumptions:
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `unclassified`, UNRESOLVED): the dead-instrument case. **Four** confident false negatives have now been caught in this phase — the `ugrep` wrapper returning zero silently, zsh not word-splitting an unquoted `$(git ls-files …)`, a generator command with an inline `#` that commented itself out, and a scratch allowlist row written with spaces instead of a tab that parsed as a comment. Every measurement here uses `/usr/bin/grep` explicitly, pairs every zero with a live positive control on the same surface, and treats a missing known-present anchor as `CONTROL_DEAD`. The probe row itself is not auto-resolved by those mitigations."
    - "FLAGGED ASSUMPTION (SURF-01, edge-probe `adjacency`, UNRESOLVED): the template↔rendered equality question, which is the exact boundary this plan's defect lives on. D-33 asserts byte-identity of one literal across that boundary and checks it; nothing mechanically enforces it for any other line, and template↔example equality still has no mechanism at all (FUT-01, filed and unbuilt). Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `empty`, UNRESOLVED): an empty scratch surface, an unreadable scratch path, or an empty allowlist would each make the non-vacuity demonstration trivially pass or trivially fail. `files_measured` and `control_defmodule` are asserted on every scratch run and the instrument fails closed at exit 3 on a dead control. Surfaced, not resolved."
    - "FLAGGED ASSUMPTION (SURF-03, edge-probe `ordering`, UNRESOLVED): 'when elements compare equal, is output order specified and stable?' Read here as decision-then-amendment-then-measurement. The decision commit precedes the amendment commit which precedes plan 239-13's re-run, asserted by per-commit path scoping and by the `wave: 18` / `depends_on` re-sequencing. Nothing mechanically enforces it; a future phase could violate the ordering without a gate firing."
---

<objective>
Amend the generated-app V3 criterion so an already-triaged false positive is still recognised after it
crosses the template-to-rendered boundary — and prove the amended criterion can still fail.

Purpose: plan 239-13 halted a second time, 0/3 with no commits, with everything substantive green.
Three V3 tiers exit 0 with live controls, all eight per-alternation controls re-fired, `install-smoke`
exit 0, SC-1's fixed-string proof 0/0/0 against a 94-line control, `T-239-12-03` dischargeable on the
bytes, and D-32's halt clause evaluated against a real bare-`phx.new` baseline without tripping. The
single blocker is `lib/<web>/controllers/oauth_html.ex:54` — the **rendered counterpart** of the one
committed allowlist record. The instrument keys allowlist matching on `(path, literal)`; the template
path and the rendered path are different strings carrying identical bytes, so the record cannot match
its own counterpart. Nobody caught it earlier because the golden fixture — the only prior stand-in for
generated output — contains zero `oauth_html` files, so the criterion's embedded assumption that no
allowlist path appears in a generated-app run held at every tier ever measured and was never tested
against a real generated app.

This plan amends a criterion **after** seeing the result that falsifies it, which is the exact shape
this phase has twice refused. The distinguishing property is made checkable rather than asserted: the
disposition being re-matched was made by plan 239-08 and committed by plan 239-09, both strict
ancestors of this measurement, so D-33 changes only *how an existing disposition is matched across a
rendering boundary* — never *what counts as bookkeeping*. The exception is bounded by literal
byte-identity, so it cannot widen to anything not already triaged, and it is demonstrated able to
fail before its pass is believed.

Output: D-33 recorded with its commit-graph proof and its bound; a live RED demonstration of the
amended criterion on a scratch surface; and a two-site surgical amendment to `239-13-PLAN.md` so its
acceptance criteria and the decision agree.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md
@.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-v3-allowlist.tsv
</context>

## SAFETY RULESET (CRITICAL — do not bypass)

1. **Measurement hygiene is non-negotiable.** **Four** confident false negatives have now been caught
   in this phase. Every counting command must:
   - invoke **`/usr/bin/grep`** explicitly — this repo's interactive shell resolves bare `grep` to a
     `ugrep` wrapper that can silently return zero;
   - iterate file lists as **`git ls-files -z … | xargs -0 …`** — this shell does **not** word-split
     an unquoted `$(git ls-files …)` expansion, so the whole list arrives as one argument and every
     count comes back `0`;
   - carry **no inline `#`** inside a generator command — a round-2 generator commented itself out
     and emitted zero records;
   - be brace-grouped with `|| true` inside the command substitution, because under `set -e` a
     zero-match grep aborts the shell and yields a confident false negative;
   - and, for any TSV written by this plan, be **parse-controlled before its result is interpreted** —
     a row written with spaces instead of a literal tab parses as a comment and the run reports the
     opposite of the truth. That one has already bitten this phase once.
2. **Every zero is paired with a live positive control on the same surface** (Standing Constraint 1).
   A bare zero is inadmissible. Where a known-present anchor is expected, its absence is `CONTROL_DEAD`
   — a broken search that FAILS the gate — never a finding.
3. **Instrument exit 3 is a fail-closed HALT, never a result.** Exit 1 means the surface is dirty;
   exit 3 means the instrument cannot answer.
4. **The instruments are frozen** (D-16, D-30): `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv`,
   `239-comment-only-diff-check.sh`. The criterion moves; the instrument does not.
5. **No live external observation here.** No `install-smoke.sh`, no scaffolded app, no tarball, no
   `MIX_ENV=test mix ci`. All of it stays plan 239-13's.
6. **`.planning/REQUIREMENTS.md` is untouched.** SURF-03 stays `[ ]`.
7. **`.github/` is read-only** (D-23).
8. **Evidence at the final committed HEAD on a clean tree** (Standing Constraint 2).
9. **The repo is public.** No home-directory paths, adopter PII, or brand hex values in any artifact —
   record the scratch surface by basename, never by absolute path.
10. **Do not push.** Commit only, ending messages with
    `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

### Environment preconditions

- **Nothing external is required.** No Postgres, no `phx_new` archive, no scaffolded app. If a task
  here appears to need one, it has drifted into plan 239-13's scope — stop and report.
- **The scratch surface must contain a `defmodule` line.** `239-v3-vocabulary-check.sh` raises exit 3
  when `control_defmodule=0`, so a scratch file without one fails closed and produces no result at
  all. That is the instrument working correctly; supply the control rather than working around it.
- **`gsd-tools` is not on PATH** — invoke `node ~/.claude/gsd-core/bin/gsd-tools.cjs`.

<tasks>

<task type="auto">
  <name>Task 1: Record D-33 with its commit-graph proof and its bound, and demonstrate the amended criterion RED on a scratch surface</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-CONTEXT.md, .planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-EVIDENCE.md</files>
  <precondition>The working tree is clean at plan 239-15's final commit plus the D-33 STATE record, `git status --porcelain` is empty, and `/usr/bin/grep -cE -- '\*\*D-3[123][ :]' 239-CONTEXT.md` shows D-31 and D-32 present with D-33 absent. If a D-33 already exists in `239-CONTEXT.md`, the numbering assumption is wrong: halt and report rather than overwriting someone else's decision.</precondition>
  <read_first>
    - `239-CONTEXT.md` D-32 in full — the format to match: **Statement**, the measurement behind it with commands and outputs inline, alternatives considered and **not** taken with the reason each was rejected, a **Reversibility** line, and the implementing/consuming plans named. D-32 is also the precedent for a decision whose central claim is backed by a live demonstration rather than by a reading of the source, and for the parse-control discipline on a scratch TSV.
    - `239-v3-vocabulary-check.sh` — the allowlist reader (`IFS=$'\t'`, the empty-second-field `continue`), the union guard over the three named tier lists, the per-entry non-vacuity control scoped to exercised entries, the `--files` mode, the `[ALLOWLISTED]` marking that tests `h_path = AL_PATHS[i]` plus a substring match on the hit text, and the printed record format `path:line:[ ALLOWLISTED] text`. Note precisely where the `(path, literal)` key is applied — that location is the defect, and it is the line D-33 quotes.
    - `239-v3-allowlist.tsv` — the single committed record: its path, its literal, and its `FALSE-POSITIVE — SVG path coordinates` reason naming the `\b[0-9]{3}-[0-9]{2}\b` alternation and the `373-12` coordinate pair.
    - `239-EVIDENCE.md` § `## VOCABULARY-LEDGER` — V3 verbatim and the allowlist's triage record, which is where the FALSE-POSITIVE disposition lives.
    - `.planning/STATE.md`'s D-33 line — the operator's recorded decision, which this task transcribes into the phase's decision block rather than paraphrasing.
  </read_first>
  <action>
**Re-derive the defect before writing it down.** Establish, live and recorded:

  * the committed record's path and literal, read from `239-v3-allowlist.tsv`;
  * the instrument line where allowlist matching is keyed on `(path, literal)`, quoted with its line
    number;
  * that the template path and the rendered path differ as strings while the **literal is
    byte-identical** on both surfaces. Check the literal, not a paraphrase of it: a difference of even
    one space would make this a new disposition wearing an old one's name, and the whole legitimacy of
    D-33 rests on it being the same already-triaged line. Compare with `/usr/bin/grep -cF` on the
    template surface, paired with a live positive control on that same file;
  * that the golden fixture contains **zero** `oauth_html` files, paired with a live non-zero control
    on the same list (for instance the count of `.ex` paths in the golden tree), so the zero cannot
    mean the list was empty or the grep was dead. This is the fact that explains why the criterion's
    embedded assumption survived every prior measurement.

**Write D-33 into `239-CONTEXT.md`**, appended to the round-2 decisions block in D-32's format.

  * **Statement.** The generated-app V3 criterion asserts `hits_outside_allowlist = 0` **after
    excluding hits whose (basename, literal) matches a committed allowlist record**, with the
    exception enumerated by name and count in the record, and a **halt clause**: any hit that is not
    such a match is a finding and stops the plan. The criterion is amended; the instrument is not.
  * **The derivation, written as a runnable procedure rather than described.** State exactly how an
    executor applies the exclusion to the instrument's printed hit records without editing it: for
    each printed record, take the path's basename and the hit text; the record is excluded only when
    some committed allowlist row has that same basename AND that row's literal is a substring of the
    hit text. Specify the commands, including the `-F` fixed-string handling of the literal, and
    require the excluded and surviving records to be listed separately with counts. An executor must
    not have to invent this.
  * **Why the criterion and not the instrument.** Amending the allowlist's matching key is an
    instrument edit; every plan since 239-09 asserts byte-equality against these files, and D-30
    freezes them. Record that as the rejection reason for alternative (a).
  * **Why not drop the measurement.** Rejected as the goalpost move this phase has twice refused. The
    measurement is a real signal and it is kept; what changes is the matching of a disposition that
    already existed.
  * **Why not reach for D-32.** The generated `oauth_html.ex` is a file the installer **created**, so
    it is squarely inside D-32's created-or-modified scope. Excluding it there would put a
    Sigra-authored file outside the measurement and hollow out D-32's own halt clause. Record this as
    a third rejected alternative so a later reader does not re-propose it.
  * **The goalpost paragraph, answered from the commit graph.** Record the two shas — plan 239-08's
    FALSE-POSITIVE disposition and plan 239-09's allowlist commit — and the two
    `git merge-base --is-ancestor` results proving the disposition precedes both the allowlist record
    and this measurement. State the consequence in one line: D-33 changes only how an
    already-existing disposition is matched across a rendering boundary, never what counts as
    bookkeeping. Then state why the assumption it replaces was never a bar: it held at every tier ever
    measured because the golden fixture carries zero `oauth_html` files while the install-smoke app
    installs the OAuth generator's output — so it was an untested assumption, not a standard being
    lowered. Both facts get their commands and outputs inline.
  * **The bound.** The exception admits a hit only when its literal is byte-identical to a committed
    record's literal AND its basename equals that record's basename. It therefore cannot widen to
    cover anything not already triaged. State that the bound is demonstrated, not promised, and point
    at the demonstration below.
  * **Reversibility: costly.** It amends a criterion plan 239-13 measures against and a later reader
    reasons from the amended text. Not one-way: the pre-amendment criterion, including its embedded
    assumption, is quoted inside the decision, so the original is reconstructable from the decision
    itself. No `checkpoint:decision` is emitted, following the D-26/D-27/D-32 precedent for
    gap-closure criterion amendments.
  * **Implemented by** Task 2 of this plan; **consumed by** plan 239-13.

**Then demonstrate the amended criterion is non-vacuous — three probes on a scratch surface**, built
outside the repo working tree, each parse-controlled, all deleted afterwards. The instrument is run in
`--files` mode against scratch paths; the committed allowlist is used unmodified via its normal
lookup, so nothing about the real files changes. Every scratch file carries a `defmodule` line,
because `control_defmodule=0` raises exit 3 and yields no result at all.

  * **Probe A — the criterion can still fail.** A scratch file named `oauth_html.ex` carrying both the
    allowlisted literal AND a genuine bookkeeping line that V3 matches for a different reason. The
    instrument reports both as hits; the (basename, literal) exclusion removes the first and **not**
    the second, so the amended criterion stays **RED** with the bookkeeping line named. This is the
    demonstration that matters: an exclusion rule that cannot leave anything behind is not a criterion.
  * **Probe B — the bound holds on basename.** A scratch file under a **different** basename carrying
    the allowlisted literal. The exclusion does **not** apply; the hit survives. A literal alone
    cannot buy an exemption.
  * **Probe C — the bound holds on literal.** A scratch file named `oauth_html.ex` carrying a V3 hit
    that is **not** the allowlisted literal. The exclusion does **not** apply; the hit survives. A
    basename alone cannot buy an exemption.

Record every probe with its exact command, the instrument's full output (`hits`, `allowlisted`,
`hits_outside_allowlist`, `control_defmodule`, `files_measured` and the hit records), the exclusion
derivation applied to those records, and the surviving count. Assert `files_measured` equals the
scratch file count and `control_defmodule` is non-zero on every probe — a zero on either means the
scratch path was not read, which is a broken probe and not a result.

Delete the scratch surface. Confirm `git status --porcelain` is empty and that
`git diff --name-only` shows no instrument or allowlist change.

Write the whole record into a new `239-EVIDENCE.md` § `## D-33-CRITERION-AMENDMENT`: the re-derived
defect, the commit-graph proof with both `is-ancestor` results, the golden-fixture zero with its
control, the three probes, and the derivation procedure verbatim.

Commit path-scoped to the two `.planning/` files, message
`docs(239): record D-33 and demonstrate the amended generated-app criterion RED`.
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; c="$d/239-CONTEXT.md"; e="$d/239-EVIDENCE.md"; s="$d/239-v3-vocabulary-check.sh"; a="$d/239-v3-allowlist.tsv"; n=$( { /usr/bin/grep -cE -- "\*\*D-33[ :]" "$c" || true; } ); test "$n" -ge 1 || { echo "MISSING_DECISION D-33"; exit 1; }; ctl=$( { /usr/bin/grep -cE -- "\*\*D-32[ :]" "$c" || true; } ); test "$ctl" -ge 1 || { echo "CONTROL_DEAD: D-32 absent, the decision grep is not reading what it claims"; exit 1; }; sec=$( { /usr/bin/grep -c "^## D-33-CRITERION-AMENDMENT" "$e" || true; } ); test "$sec" = 1 || { echo "MISSING_EVIDENCE_SECTION n=$sec"; exit 1; }; for sha in 1a85508e 23f3c711; do git merge-base --is-ancestor "$sha" HEAD || { echo "NOT_ANCESTOR $sha"; exit 1; }; n=$( { /usr/bin/grep -c "$sha" "$c" || true; } ); test "$n" -ge 1 || { echo "SHA_NOT_RECORDED $sha"; exit 1; }; done; git merge-base --is-ancestor 1a85508e 23f3c711 || { echo "DISPOSITION_NOT_BEFORE_ALLOWLIST"; exit 1; }; for k in basename literal; do n=$( { /usr/bin/grep -ci "$k" "$c" || true; } ); test "$n" -ge 1 || { echo "BOUND_HALF_MISSING $k"; exit 1; }; done; chg=$( { git diff --name-only HEAD~1..HEAD -- "$s" "$a" "$d/239-comment-only-diff-check.sh" || true; } ); test -z "$chg" || { echo "INSTRUMENT_MODIFIED: $chg"; exit 1; }; test -z "$(git status --porcelain)" || { echo "SCRATCH_LEAKED_OR_TREE_DIRTY"; exit 1; }; out=$( { git show --name-only --format= HEAD || true; } ); printf "%s\n" "$out"; stray=$( { printf "%s\n" "$out" | /usr/bin/grep -v "^$d/" | /usr/bin/grep -c "." || true; } ); test "$stray" = 0 || { echo "COMMIT_NOT_SCOPED stray=$stray"; exit 1; }; echo D33_RECORDED_AND_DEMONSTRATED'</automated>
  </verify>
  <acceptance_criteria>
    - `239-CONTEXT.md` carries **D-33** appended to the round-2 decisions block in D-32's format: Statement, the measurement behind it with commands and outputs inline, alternatives considered and not taken with each rejection reason, a Reversibility line, and the implementing and consuming plans named. The D-33 grep is paired with a live D-32 control on the same file.
    - The amended criterion is written as a **runnable derivation** over the instrument's printed `path:line: text` records — basename extraction, the fixed-string literal containment test against committed allowlist rows, and the requirement to list excluded and surviving records separately with counts. A prose description without the procedure does not satisfy this.
    - **Three alternatives are recorded as rejected, with reasons:** amend the allowlist's matching key (an instrument edit, forbidden by D-30 and asserted byte-unchanged by every plan since 239-09); drop the measurement (the goalpost move this phase has twice refused); widen D-32 to cover the file (it is installer-created, squarely in D-32's scope, and excluding it there would hollow out D-32's halt clause).
    - **The goalpost paragraph is answered from the commit graph.** Both shas are recorded and both ancestor relations verified: `git merge-base --is-ancestor 1a85508e 23f3c711` exits `0` (plan 239-08's disposition precedes plan 239-09's allowlist record) and `git merge-base --is-ancestor 23f3c711 HEAD` exits `0` (the record precedes this measurement). The one-line consequence — that D-33 changes only how an existing disposition is matched across a rendering boundary — is stated explicitly.
    - **The untested-assumption fact is recorded with its control:** the golden fixture carries `0` `oauth_html` files, paired with a live non-zero control on the same file list. A bare zero here is inadmissible.
    - **The literal is proven byte-identical across the rendering boundary**, checked with `/usr/bin/grep -cF` and a live positive control on the same file — not paraphrased, not eyeballed. A difference of even one character makes D-33 a new disposition and the task halts.
    - **Probe A recorded and RED:** on a scratch `oauth_html.ex` carrying both the allowlisted literal and a genuine V3-matching bookkeeping line, the exclusion removes the first and not the second, and the amended criterion leaves `>= 1` surviving hit, named. This is the non-vacuity demonstration; without it the amended criterion is an unfalsified instrument.
    - **Probes B and C recorded and surviving:** the allowlisted literal under a different basename is NOT excluded, and a different literal under the allowlisted basename is NOT excluded. Together they demonstrate the bound rather than asserting it.
    - Every probe records `files_measured` equal to its scratch file count and `control_defmodule >= 1`. A zero on either is a broken probe and a halt, never a result. An exit `3` is an instrument failure, never a result.
    - The scratch surface lived outside the repo working tree and is deleted: `git status --porcelain` is empty and `git diff --name-only HEAD~1..HEAD --` the three instrument paths is empty.
    - `239-EVIDENCE.md` § `## D-33-CRITERION-AMENDMENT` exists and carries the re-derived defect, both ancestor results, the golden-fixture zero with its control, all three probes verbatim, and the derivation procedure.
    - This commit lists only paths under the phase directory: `git show --name-only --format= HEAD` contains no path under `priv/`, `lib/`, `test/`, `.github/`, or `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <done>D-33 exists in the phase's decision block with its derivation runnable, its rejected alternatives reasoned, its legitimacy provable from the commit graph rather than argued, and its bound demonstrated on both halves — and the amended criterion has been shown able to fail before any of its passes are believed.</done>
  <reversibility rating="costly">D-33 amends a criterion plan 239-13 measures against, and a later reader reasons from the amended text — the same basis as D-27 and D-32. Not one-way: the pre-amendment criterion including its embedded assumption is quoted inside the decision, so the original is reconstructable from the record itself. No `checkpoint:decision` is emitted, following the established gap-closure precedent.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Apply D-33 to plan 239-13's text so its acceptance criteria stop contradicting it</name>
  <files>.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-13-PLAN.md</files>
  <precondition>Task 1's decision commit is HEAD, the working tree is clean, and D-33 is recorded in `239-CONTEXT.md` — the citation this task writes must resolve to a decision that already exists. Also assert the re-sequencing done at this plan's planning time still holds: `239-13-PLAN.md`'s frontmatter reads `wave: 18` and `depends_on: ["239-16"]`. If it reads `wave: 16` / `depends_on: ["239-15"]`, a wave-ordered dispatch would run 239-13 before this plan: halt and report rather than silently re-applying it.</precondition>
  <read_first>
    - `239-13-PLAN.md` — the generated-app V3 sites, located by re-reading rather than by the line numbers below, which are starting anchors only and have already shifted once from plan 239-14's frontmatter comment: (1) the Task 1 action sentence directing a V3 run over the generated app's `lib/` and `priv/` (~L184); (2) the Task 1 acceptance criterion carrying the embedded assumption *"no allowlist entry's path is expected in it"* (~L229). **Search exhaustively for a third**: `/usr/bin/grep -n 'allowlist\|generated app' 239-13-PLAN.md` and classify every hit, so the amendment cannot leave a stale sibling behind. The `<verification>` section does not currently mention this run — confirm that rather than assuming it.
    - `239-13-PLAN.md`'s existing D-31 citation, applied by plan 239-14 Task 4 in `cc2f5c61` — the phrasing and placement to match when writing `as amended by D-33`. Earlier precedents are `c994139a` and `1e132e74`; `cc2f5c61` is the closest in shape and its commit message is the template for this task's.
    - `239-CONTEXT.md`'s D-33, as written in Task 1 — the amendment's authoritative wording and its derivation procedure, which this task transcribes into 239-13 rather than paraphrasing.
  </read_first>
  <action>
Make a **surgical, path-scoped** revision to `239-13-PLAN.md`. The sites identified above, and nothing
else.

At the acceptance-criterion site, replace the embedded assumption — that no allowlist entry's path is
expected in a generated-app run — with the D-33 form. The replacement must carry, in the criterion
itself so an executor never has to leave the plan to grade its own work:

  * the assertion as `hits_outside_allowlist = 0` **after** the (basename, literal) exclusion;
  * the derivation procedure, transcribed from D-33, precise enough to run;
  * the requirement that the exception be **enumerated by name and count** — the excluded records
    listed, not summarised as a number;
  * the **halt clause**: any hit that is not a (basename, literal) match against a committed record is
    a finding, and the plan stops rather than reclassifying it;
  * the retained exit-3 rule — an instrument failure is never a result;
  * and the retained requirement for a live non-zero `control_defmodule` on the same file list, since
    the exclusion makes it easier, not harder, for a zero to mean nothing.

At the action site, direct the same procedure rather than the loose "record the result with its
control", and cite the authority inline in the phase's established style, `as amended by D-33`, so a
later reader following the number lands on the decision.

State in the amended criterion **why the assumption it replaces was wrong** — one sentence, so the
next reader does not restore it: the golden fixture carries zero `oauth_html` files while the
install-smoke app installs the OAuth generator's output, so the assumption held at every tier ever
measured and was never tested against a real generated app.

Do not touch anything else in the file. Its three-tier V3 run, its per-alternation controls, its SC-1
fixed-string proof, its tarball check, `MIX_ENV=test mix ci`, SC-5 and the SURF-03 re-check stay
byte-identical — those are the closure's live observations and pre-empting or softening any of them is
precisely what this plan must not do. Do not touch the frontmatter.

Record the before/after of each amended site in `239-EVIDENCE.md` § `## D-33-CRITERION-AMENDMENT` as a
sub-entry — write it in this task but commit it with nothing else if it would break the path scoping;
prefer folding it into Task 1's section via a follow-up docs commit rather than polluting the
amendment commit.

Verify the amendment is self-consistent before committing: the file must no longer carry the replaced
assumption anywhere, must name D-33, must retain the exit-3 rule and the `control_defmodule`
requirement, and must still pass `verify.plan-structure`.

Commit path-scoped to `239-13-PLAN.md` alone, message
`docs(239-13): amend the generated-app V3 criterion per D-33`.
  </action>
  <verify>
    <automated>bash -lc 'set -uo pipefail; d=.planning/phases/239-priv-templates-sweep-one-batched-re-bless; p="$d/239-13-PLAN.md"; test -s "$p" || { echo "MISSING_239_13"; exit 1; }; ctl=$( { /usr/bin/grep -c "hits_outside_allowlist" "$p" || true; } ); test "$ctl" -ge 2 || { echo "CONTROL_DEAD: hits_outside_allowlist absent, the greps are not reading the right file"; exit 1; }; stale=$( { /usr/bin/grep -c "no allowlist entry.s path is expected in it" "$p" || true; } ); test "$stale" = 0 || { echo "STALE_ASSUMPTION_REMAINS n=$stale"; exit 1; }; cit=$( { /usr/bin/grep -c "as amended by D-33" "$p" || true; } ); test "$cit" -ge 1 || { echo "D33_CITATION_MISSING"; exit 1; }; bn=$( { /usr/bin/grep -ci "basename" "$p" || true; } ); test "$bn" -ge 1 || { echo "EXCLUSION_KEY_NOT_STATED"; exit 1; }; e3=$( { /usr/bin/grep -c "instrument failure, never a result" "$p" || true; } ); test "$e3" -ge 1 || { echo "EXIT3_RULE_DROPPED"; exit 1; }; cd_=$( { /usr/bin/grep -c "control_defmodule" "$p" || true; } ); test "$cd_" -ge 1 || { echo "CONTROL_REQUIREMENT_DROPPED"; exit 1; }; w=$( { /usr/bin/grep -c "^depends_on: \[\"239-16\"\]" "$p" || true; } ); test "$w" = 1 || { echo "RESEQUENCING_LOST"; exit 1; }; d31=$( { /usr/bin/grep -c "as amended by D-31" "$p" || true; } ); test "$d31" -ge 1 || { echo "D31_AMENDMENT_CLOBBERED"; exit 1; }; names=$( { git show --name-only --format= HEAD || true; } ); printf "%s\n" "$names"; test "$names" = "$p" || { echo "COMMIT_NOT_SCOPED"; exit 1; }; node ~/.claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure "$p" | /usr/bin/grep -q "\"valid\": true" || { echo "STRUCTURE_INVALID"; exit 1; }; echo PLAN_239_13_AMENDED_PER_D33'</automated>
  </verify>
  <acceptance_criteria>
    - Every generated-app V3 site in `239-13-PLAN.md` is amended, and the sites were found by an **exhaustive** re-grep for `allowlist` and `generated app` with each hit classified — not by the anchor line numbers in `<read_first>`. The classification is recorded, including the confirmation that the `<verification>` section carries no fourth site.
    - The replaced assumption is gone: `/usr/bin/grep -c 'no allowlist entry.s path is expected in it'` returns `0`, paired with a live positive control on the same file (`hits_outside_allowlist` appears `>= 2` times) so the zero cannot mean the grep read the wrong file.
    - The amended criterion carries all six required parts: the post-exclusion assertion; the runnable derivation transcribed from D-33; the enumerate-by-name-and-count requirement; the halt clause for any non-matching hit; the retained exit-3 rule; and the retained live `control_defmodule` requirement. Each is independently greppable and each is checked.
    - One sentence in the amended criterion states why the replaced assumption was wrong — the golden fixture's zero `oauth_html` files versus the install-smoke app installing the OAuth generator's output — so a later reader does not restore it.
    - `as amended by D-33` appears at least once in the phase's D-31/D-32 citation style, and D-33 exists in `239-CONTEXT.md` from Task 1 so the citation resolves.
    - **Plan 239-14's D-31 amendment is not clobbered:** `as amended by D-31` still appears, and the `T-239-12-03` row still requires `>= 1` with `sigra_admin_policy_test.exs` named and the `*.exs` control retained. An amendment that silently reverted an earlier one would be the worst possible outcome of this task.
    - Nothing else in `239-13-PLAN.md` changed: its three-tier V3, per-alternation controls, SC-1 fixed-string proof, tarball check, `MIX_ENV=test mix ci`, SC-5 and SURF-03 re-check clauses are byte-identical. The diff is confined to the identified sites.
    - The frontmatter is untouched and the re-sequencing survives: `depends_on: ["239-16"]` and `wave: 18` are intact. This task asserts them; it does not re-apply them.
    - `verify.plan-structure` reports `valid: true` on the amended file.
    - The commit contains exactly one path, `239-13-PLAN.md`. `git show --name-only --format= HEAD` equals that path and nothing else.
    - SURF-03 is still `[ ]` at this plan's final HEAD: `/usr/bin/grep -c '^- \[ \] \*\*SURF-03\*\*' .planning/REQUIREMENTS.md` returns `1`.
  </acceptance_criteria>
  <done>Plan 239-13's text agrees with D-33, so its third run grades the generated-app surface against a criterion that is both satisfiable and still capable of failing — and no executor is asked to choose between its own acceptance criteria and a decision file.</done>
  <reversibility rating="costly">This edits another plan's acceptance criteria for the second time in this closure. Git-revertable, but a revert restores an assumption that is false against a real generated app and would halt the closure a third time on the same row — so the record of what changed and why lives in D-33 and in § `## D-33-CRITERION-AMENDMENT`, not only in the diff. Not one-way; no `checkpoint:decision` is emitted.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template surface -> rendered generated-app surface | Identical bytes arrive under a different path. Every path-keyed judgement made on one side silently fails to apply on the other, and this is the boundary the halting defect lives on. |
| Criterion -> phase verdict | A criterion amended after seeing its failing result is indistinguishable from a moved goalpost unless the distinguishing property is checkable. |
| Decision record -> downstream plan authority | Plan 239-13 grades itself against its own acceptance criteria. A decision that lives only in `239-CONTEXT.md` cannot override them. |
| Measurement instrument -> phase verdict | `239-v3-vocabulary-check.sh`, its allowlist, and the comment-only classifier are frozen. Any edit turns a surface result into a measurement result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-239-16-01 | Repudiation | the D-33 criterion amendment | critical | mitigate | Amending a criterion after seeing the result that falsifies it is the shape this phase has twice refused, and an argument for why this instance differs is worth nothing on its own. The distinguishing property is made checkable from the commit graph: the disposition being re-matched was made in `1a85508e` and committed in `23f3c711`, both strict ancestors of the measurement, so only the matching changes and never the definition of bookkeeping. Both ancestor relations are asserted in the verify gate. |
| T-239-16-02 | Spoofing | the (basename, literal) exception | critical | mitigate | An exclusion rule that cannot leave anything behind certifies everything. Demonstrated non-vacuous live: Probe A leaves a real bookkeeping hit surviving and the criterion RED; Probes B and C show the bound holds on each half independently, so neither a matching basename nor a matching literal alone buys an exemption. |
| T-239-16-03 | Tampering | `239-v3-allowlist.tsv` | high | mitigate | The obvious "fix" is a second allowlist record at the rendered path. Beyond the D-30 freeze, such an entry is in none of the three named tiers and raises the instrument's union guard to exit 3 on every tier run — the mechanical impossibility D-32 already demonstrated live. Prohibited explicitly and byte-equality asserted in both tasks. |
| T-239-16-04 | Elevation of privilege | D-32's created-or-modified scope | high | mitigate | Reaching for D-32 to exclude the generated `oauth_html.ex` would be scope-creep onto a decision written for `phx.new`-authored files, would put an installer-created file outside the measurement, and would hollow out D-32's own halt clause. Recorded as an explicitly rejected alternative so it is not re-proposed, and prohibited. |
| T-239-16-05 | Spoofing | zero-result greps and dead probes | high | mitigate | Four confident false negatives have been caught in this phase, including a scratch TSV row written with spaces that parsed as a comment and reported the opposite of the truth. Every command uses `/usr/bin/grep` explicitly, every zero is paired with a live same-surface control, every probe asserts `files_measured` and a non-zero `control_defmodule`, and the verify gates carry `CONTROL_DEAD` branches that fail. |
| T-239-16-06 | Tampering | plan 239-14's D-31 amendment | high | mitigate | Task 2 is the second surgical edit to the same file. A careless rewrite could silently revert the `T-239-12-03` inversion, restoring a criterion already proven false on the bytes. An acceptance criterion and a verify branch both assert `as amended by D-31` survives alongside the new D-33 citation. |
| T-239-16-07 | Repudiation | scope of the 239-13 edit | high | mitigate | Editing another plan's acceptance criteria is how a closure could quietly soften its own remaining obligations. The edit is path-scoped to one file, confined to the identified sites, and every other clause — three-tier V3, per-alternation controls, SC-1 fixed-string proof, tarball, gate, SC-5, SURF-03 — is asserted byte-identical. |
| T-239-16-08 | Information disclosure | `239-CONTEXT.md`, `239-EVIDENCE.md`, the scratch surface | medium | mitigate | Public repo. The scratch surface lives outside the working tree, is recorded by basename rather than absolute path, is deleted, and `git status --porcelain` is asserted empty. No home-directory paths, adopter PII, or brand hex values. |
| T-239-16-09 | Denial of service | scratch file without a `defmodule` control | low | accept | The instrument raises exit 3 on `control_defmodule=0`, so a scratch file lacking the control yields no result at all. That is the guard working correctly; the plan supplies the control rather than routing around it, and the environment preconditions name the symptom so it is not misdiagnosed as a probe failure. |
| T-239-16-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. No new dependency is added and no archive is installed; RESEARCH.md's Package Legitimacy Gate covers no new package here. |
</threat_model>

<artifacts_this_phase_produces>
Newly created symbols and files (exclude from drift verification):

- `D-33` in `239-CONTEXT.md` § `### Gap-Closure Round 2 Decisions` (new decision record)
- `239-EVIDENCE.md` § `## D-33-CRITERION-AMENDMENT` (new section)
- The amended generated-app V3 sites in `239-13-PLAN.md` (revised text, not new content)

No new module, function, route, script, or configuration key is created. No source file is touched.
</artifacts_this_phase_produces>

<verification>
- `D-33` exists in `239-CONTEXT.md` in D-32's format, with its runnable derivation, three rejected alternatives, a Reversibility line, and the implementing and consuming plans named.
- The goalpost question is answered from the commit graph: `git merge-base --is-ancestor 1a85508e 23f3c711` and `git merge-base --is-ancestor 23f3c711 HEAD` both exit `0`, both shas recorded.
- The exception's bound is demonstrated on both halves (Probes B and C) and its non-vacuity on Probe A, each with `files_measured` and a live non-zero `control_defmodule`.
- The golden fixture's zero `oauth_html` files is recorded with a live non-zero control on the same list.
- `239-13-PLAN.md` no longer carries the replaced assumption, names D-33, retains the exit-3 rule and the `control_defmodule` requirement, and still carries plan 239-14's D-31 amendment intact.
- Two commits, each path-scoped: the decision record, then the amendment. Neither touches `priv/`, `lib/`, `test/`, `.github/` or `.planning/REQUIREMENTS.md`.
- `239-v3-vocabulary-check.sh`, `239-v3-allowlist.tsv` and `239-comment-only-diff-check.sh` are byte-unchanged across this plan; `git status --porcelain` is empty at final HEAD.
- SURF-03 still reads `[ ]` with a `Pending` roll-up row — the re-check is plan 239-13's last commit, and 239-13 is sequenced behind this plan at `wave: 18` / `depends_on: ["239-16"]`.
</verification>

<success_criteria>
The generated-app V3 criterion recognises an already-triaged false positive after it crosses the
template-to-rendered boundary; the amendment's legitimacy is provable from the commit graph rather
than argued, and its bound and its non-vacuity are demonstrated live rather than promised; plan
239-13's own text carries the amendment so no executor is asked to choose between its criteria and a
decision file, with plan 239-14's earlier D-31 amendment intact beside it; the instrument set is
byte-unchanged; and SURF-03 is left honestly unchecked for plan 239-13, which is sequenced last and
machine-readably so.
</success_criteria>

<output>
Create `.planning/phases/239-priv-templates-sweep-one-batched-re-bless/239-16-SUMMARY.md` when done.
</output>
