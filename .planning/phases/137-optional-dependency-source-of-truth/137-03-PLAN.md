---
phase: 137-optional-dependency-source-of-truth
plan: 03
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - lib/sigra/delivery.ex
  - lib/sigra/audit/forwarders.ex
  - lib/sigra/enterprise_connections/validation.ex
  - lib/sigra/account/deletion.ex
autonomous: true
requirements: [OD-02]
must_haves:
  truths:
    - "The compound runtime guards (Oban load + liveness; Req load + arity) delegate ONLY the load half to Sigra.OptionalDeps; the liveness/arity half stays literal at the call site (D-06, SC#2)"
    - "Oban :auto sync↔async routing is unchanged: an app that adds :oban to mix.exs but never supervises it still routes :sync (Process.whereis liveness check preserved) — no runtime behavior change (OD-02)"
    - "The forwarders.ex test-override branch (line 94) and the deletion.ex internal-worker leg (line 308) are NOT changed (Pitfalls 2 and 3)"
  artifacts:
    - path: "lib/sigra/delivery.ex"
      provides: "oban_running?/0 delegating the Oban load-half, keeping the Process.whereis liveness half"
      contains: "Sigra.OptionalDeps.oban_available?() and Process.whereis(Oban)"
    - path: "lib/sigra/enterprise_connections/validation.ex"
      provides: "default_http_get/1 delegating the Req load-half, keeping function_exported? arity half"
      contains: "Sigra.OptionalDeps.req_available?() and function_exported?(Req"
  key_links:
    - from: "lib/sigra/delivery.ex"
      to: "Sigra.OptionalDeps.oban_available?/0"
      via: "compound guard: load-half delegated, liveness half (Process.whereis) literal"
      pattern: "Sigra\\.OptionalDeps\\.oban_available\\?\\(\\) and Process\\.whereis"
    - from: "lib/sigra/account/deletion.ex"
      to: "Sigra.OptionalDeps.oban_available?/0"
      via: "with-clause: Oban leg (307) delegated, worker leg (308) left literal"
      pattern: "with true <- Sigra\\.OptionalDeps\\.oban_available\\?\\(\\)"
---

<objective>
Delegate the LOAD HALF of the compound runtime optional-dep guards to `Sigra.OptionalDeps` (OD-02, D-06), keeping the liveness/arity half literal at each call site. These guards deliberately split load-check from liveness-check; folding the whole expression into the SOT would be a runtime behavior change (the exact regression the inline comments exist to prevent), so ONLY the `Code.ensure_loaded?(NamedMod)` token is swapped.

Purpose: Completes OD-02 for the Oban (delivery, forwarders, deletion) and Req (validation) compound sites, so every in-scope runtime guard delegates (SC#2) while sync↔async routing semantics are provably unchanged (SC#3).

Output: 4 files edited — `delivery.ex` (1), `audit/forwarders.ex` (1 + comment update), `enterprise_connections/validation.ex` (1), `account/deletion.ex` (1 of 2 with-legs). No new file. No file overlap with plan 02, so plans 02 and 03 run in parallel in Wave 2.

Critical fences (DO NOT TOUCH):
- `audit/forwarders.ex:94` — the `{:ok, oban_override}` test-override branch has NO `Code.ensure_loaded?` and must NOT change (Pitfall 2).
- `account/deletion.ex:308` — `Code.ensure_loaded?(Sigra.Workers.AccountDeletion)` is an internal conditionally-compiled worker (Bucket C), NOT a named optional dep; leave it literal (Pitfall 3 / Open Question 2). Only the `Oban` leg at line 307 delegates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-optional-dependency-source-of-truth/137-CONTEXT.md
@.planning/phases/137-optional-dependency-source-of-truth/137-PATTERNS.md
@.planning/phases/137-optional-dependency-source-of-truth/137-VALIDATION.md
@lib/sigra/optional_deps.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate Oban load-half in delivery.ex and audit/forwarders.ex (:error branch only)</name>
  <files>lib/sigra/delivery.ex, lib/sigra/audit/forwarders.ex</files>
  <read_first>
    - lib/sigra/optional_deps.ex — confirm `oban_available?/0` exists (this plan depends on 137-01).
    - 137-PATTERNS.md § "Compound-guard split (D-06)" — the delivery.ex canonical compound pattern (keep the explanatory comment), and the forwarders.ex `:error`-branch-only rule with the comment-update note.
    - lib/sigra/delivery.ex:108-116 — `oban_running?/0`; the `Code.ensure_loaded?(Oban) and Process.whereis(Oban) != nil` compound and its 3-line explanatory comment about why liveness is a separate concern.
    - lib/sigra/audit/forwarders.ex:88-101 — `oban_running?/1` is a `case`. The `{:ok, oban_override}` branch (line 94) skips `Code.ensure_loaded?` deliberately (override is a named process, not a module) — DO NOT TOUCH. Only the `:error` (production) branch at line 99 has the load check. Lines 97-98 carry a "mirrors delivery.ex byte-for-byte" comment that should be updated to reference the delegated form.
  </read_first>
  <action>
- `lib/sigra/delivery.ex:114` — in `oban_running?/0`, swap `Code.ensure_loaded?(Oban) and Process.whereis(Oban) != nil` → `Sigra.OptionalDeps.oban_available?() and Process.whereis(Oban) != nil`. KEEP the `and Process.whereis(Oban) != nil` liveness half (D-06 / Pitfall 4) and KEEP the 3-line explanatory comment above the function unchanged.
- `lib/sigra/audit/forwarders.ex:99` — in the `:error ->` (production) branch ONLY, swap `Code.ensure_loaded?(Oban) and Process.whereis(Oban) != nil` → `Sigra.OptionalDeps.oban_available?() and Process.whereis(Oban) != nil`. KEEP the liveness half. The `{:ok, oban_override}` branch at line 94 (`Process.whereis(oban_override) != nil`) MUST NOT change — it has no load check (Pitfall 2). Update the byte-for-byte mirror comment at lines 97-98 so it references the delegated form (e.g. "mirrors lib/sigra/delivery.ex oban_running?/0: Sigra.OptionalDeps.oban_available?() and Process.whereis(Oban) != nil") to keep the comment honest.
Reiterate: liveness halves byte-preserved; the test-override branch untouched. An app that adds :oban without supervising it must still route :sync — that is the regression the liveness half prevents.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/application_forwarders_test.exs && git grep -n "Code.ensure_loaded?(Oban)" lib/sigra/delivery.ex lib/sigra/audit/forwarders.ex</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "Code.ensure_loaded?(Oban)" lib/sigra/delivery.ex lib/sigra/audit/forwarders.ex` returns NOTHING.
    - `lib/sigra/delivery.ex` contains `Sigra.OptionalDeps.oban_available?() and Process.whereis(Oban) != nil` (liveness half intact).
    - `lib/sigra/audit/forwarders.ex` contains `Sigra.OptionalDeps.oban_available?() and Process.whereis(Oban) != nil` in the `:error` branch.
    - `git diff lib/sigra/audit/forwarders.ex` shows the `{:ok, oban_override}` branch (`Process.whereis(oban_override) != nil`) UNCHANGED (Pitfall 2).
    - `mix test test/sigra/application_forwarders_test.exs` exits 0 (sync↔async routing + test-override path unchanged).
  </acceptance_criteria>
  <done>Oban load-half delegates in delivery.ex and forwarders.ex `:error` branch; liveness halves and the forwarders test-override branch byte-preserved; forwarders tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate Req load-half (validation.ex) and Oban with-leg (deletion.ex:307 only)</name>
  <files>lib/sigra/enterprise_connections/validation.ex, lib/sigra/account/deletion.ex</files>
  <read_first>
    - lib/sigra/optional_deps.ex — confirm `req_available?/0` and `oban_available?/0` exist.
    - 137-PATTERNS.md § "Compound-guard split (D-06)" — the validation.ex and deletion.ex swaps, and the explicit "leave line 308 literal" rule.
    - lib/sigra/enterprise_connections/validation.ex:88-93 — `default_http_get/1`; `Code.ensure_loaded?(Req) and function_exported?(Req, :get, 1)` compound. KEEP the `function_exported?` arity half.
    - lib/sigra/account/deletion.ex:305-310 — `maybe_enqueue_deletion_job/4` `with` chain: line 307 `with true <- Code.ensure_loaded?(Oban),` (delegate), line 308 `true <- Code.ensure_loaded?(Sigra.Workers.AccountDeletion),` (LEAVE LITERAL — internal worker, Bucket C, Pitfall 3).
  </read_first>
  <action>
- `lib/sigra/enterprise_connections/validation.ex:91` — in `default_http_get/1`, swap `Code.ensure_loaded?(Req) and function_exported?(Req, :get, 1)` → `Sigra.OptionalDeps.req_available?() and function_exported?(Req, :get, 1)`. KEEP the `and function_exported?(Req, :get, 1)` arity half (D-06).
- `lib/sigra/account/deletion.ex:307` — swap `with true <- Code.ensure_loaded?(Oban),` → `with true <- Sigra.OptionalDeps.oban_available?(),`. The `with` structure stays.
- `lib/sigra/account/deletion.ex:308` — `true <- Code.ensure_loaded?(Sigra.Workers.AccountDeletion),` MUST stay LITERAL. `Sigra.Workers.AccountDeletion` is a Sigra-internal conditionally-compiled worker, NOT a named optional dep — it is Bucket C and has no SOT predicate (Pitfall 3 / Open Question 2). Do NOT add a predicate for it; do NOT delegate it.
Reiterate: arity half byte-preserved; the internal-worker leg untouched.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/account/deletion_test.exs test/sigra/enterprise_connections/validation_test.exs 2>&1 | tail -20; git grep -n "Code.ensure_loaded?" lib/sigra/enterprise_connections/validation.ex lib/sigra/account/deletion.ex</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "Code.ensure_loaded?(Req)" lib/sigra/enterprise_connections/validation.ex` returns NOTHING; file contains `Sigra.OptionalDeps.req_available?() and function_exported?(Req, :get, 1)`.
    - `git grep -n "Code.ensure_loaded?(Oban)" lib/sigra/account/deletion.ex` returns NOTHING; file contains `with true <- Sigra.OptionalDeps.oban_available?(),`.
    - `git grep -n "Code.ensure_loaded?(Sigra.Workers.AccountDeletion)" lib/sigra/account/deletion.ex` STILL returns line 308 (left literal — Pitfall 3).
    - `mix compile --warnings-as-errors` exits 0.
    - Existing deletion / enterprise-connections tests stay green unchanged (Oban-absent enqueue path + Req path).
  </acceptance_criteria>
  <done>Req load-half delegates in validation.ex (arity half preserved); the Oban with-leg in deletion.ex:307 delegates while the internal-worker leg 308 stays literal; tests green; compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure delegation refactor. The compound guards' liveness/arity halves (the actual runtime-safety control) are preserved byte-for-byte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-06 | Denial of Service / availability | `delivery.ex` / `forwarders.ex` Oban `:auto` routing | mitigate | Folding `Process.whereis(Oban) != nil` into the SOT would flip `:sync`→`:async` for an app that adds :oban but never supervises it, crashing on insert. Mitigation (D-06): delegate ONLY the load half; keep the liveness half literal. `application_forwarders_test.exs` stays green. |
| T-137-07 | Tampering | `forwarders.ex:94` test-override branch | mitigate | The override branch (named process, no load check) must not change, or `oban_running?/1` test-override behavior drifts (Pitfall 2). Mitigation: branch left byte-identical; covered by acceptance criterion `git diff` check. |
| T-137-08 | Tampering | `deletion.ex:308` internal-worker leg | mitigate | Inventing a `Sigra.Workers.AccountDeletion`-availability predicate (Pitfall 3) would conflate an internal conditionally-compiled worker with a named optional dep. Mitigation: leave line 308 literal; only the Oban leg (307) delegates; no predicate added for the worker. |
| T-137-SC | Tampering | npm/pip/cargo/mix installs | accept | No package installs in this plan (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `git grep -n "Code.ensure_loaded?(Oban)" lib/sigra/delivery.ex lib/sigra/audit/forwarders.ex lib/sigra/account/deletion.ex` and `Code.ensure_loaded?(Req)` in validation.ex return NOTHING (all in-scope compound load-halves delegated).
- `git grep -n "Code.ensure_loaded?(Sigra.Workers.AccountDeletion)" lib/sigra/account/deletion.ex` STILL returns line 308 (correctly fenced literal).
- `mix compile --warnings-as-errors` exits 0 (no new `no_warn_undefined` entry — D-10; `git diff mix.exs` empty).
- `mix test test/sigra/application_forwarders_test.exs` + deletion/validation tests exit 0; routing semantics unchanged.
</verification>

<success_criteria>
- OD-02 (compound subset) satisfied: 4 compound load-halves delegate to `Sigra.OptionalDeps`; liveness/arity halves literal (SC#2, D-06).
- No runtime behavior change: sync↔async routing, the forwarders test-override branch, and the deletion internal-worker leg are all preserved; existing tests pass unchanged (SC#3).
- After plans 02 + 03: every Bucket A runtime guard delegates; Buckets B/C/D (incl. deletion.ex:308, forwarders.ex:94) remain literal as designed.
</success_criteria>

<output>
Create `.planning/phases/137-optional-dependency-source-of-truth/137-03-SUMMARY.md` when done.
</output>
