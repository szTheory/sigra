---
status: complete
phase: 137-optional-dependency-source-of-truth
source: [137-01-SUMMARY.md, 137-02-SUMMARY.md, 137-03-SUMMARY.md]
started: 2026-05-29T18:55:35Z
updated: 2026-05-29T19:10:00Z
---

## Current Test

[testing complete]

<!--
Phase 137 is a pure internal library refactor (establish Sigra.OptionalDeps SOT;
delegate Code.ensure_loaded?/1 guards to it). No UI, no user-facing behavior change —
the "no behavior change" invariant IS the deliverable. Per zero-human-UAT preference,
all checks were run automatically by Claude rather than manually by the user.
-->

## Tests

### 1. OptionalDeps SOT module exists with full predicate surface
expected: lib/sigra/optional_deps.ex defines 9 zero-arity availability predicates (oban, bcrypt, eqrcode, threadline, assent, swoosh, joken, hammer, req) plus config-driven encryption_active?/1
result: pass
evidence: "grep confirms all 9 *_available?/0 defs + encryption_active?/1 present (optional_deps.ex:79-198)"

### 2. SOT drift-catching unit tests pass
expected: test/sigra/optional_deps_test.exs asserts predicate() == Code.ensure_loaded?(Mod) for all 9 deps + 3 encryption fixture cases; suite green
result: pass
evidence: "mix test test/sigra/optional_deps_test.exs — 12 tests, 0 failures"

### 3. Single-leaf guards delegated to SOT (no stray Code.ensure_loaded?)
expected: All 11 single-leaf runtime guards in crypto, hashers/bcrypt, mfa, jwt/signer, plug/rate_limit, and 5 oauth strategies now call Sigra.OptionalDeps.*_available?(); no literal Code.ensure_loaded? remains at those sites
result: pass
evidence: "git grep Code.ensure_loaded? across all 10 single-leaf files returns NONE"

### 4. Compound-guard load-halves delegated; fenced literals preserved
expected: Oban/Req load-halves in delivery, forwarders, validation, deletion delegated to SOT; liveness/arity halves preserved; deletion.ex:308 Code.ensure_loaded?(Sigra.Workers.AccountDeletion) deliberately left literal (Bucket C fence)
result: pass
evidence: "git grep Code.ensure_loaded?(Oban)/(Req) in the 4 compound files returns NONE; deletion.ex:308 AccountDeletion literal still present as intended"

### 5. Library compiles warnings-as-errors clean (no new no_warn_undefined)
expected: mix compile --warnings-as-errors exits 0 after delegation; no mix.exs changes needed
result: pass
evidence: "mix compile --warnings-as-errors exit 0; mix.exs unchanged (D-10)"

### 6. No behavior change across affected-file suites
expected: Test suites for every module touched by the refactor (crypto, mfa, rate_limit, forwarders, deletion, validation, …) pass identically — the delegation is a no-op token swap
result: pass
evidence: "mix test crypto+mfa+rate_limit+application_forwarders+deletion+validation — 88 tests, 0 failures. Affected modules in full run also pass."

### 7. Full test suite (no-regression sweep)
expected: Full `mix test` shows no failures attributable to the optional-deps refactor
result: pass
evidence: "After `sudo xcodebuild -license accept` + `mix deps.compile argon2_elixir --force`, full suite is GREEN: 33 doctests, 3 properties, 2296 tests, 0 failures, 0 invalid (493.8s). The prior 11 failures + 2 invalid were 100% the Xcode-license NIF-compile env gate; all cleared with zero code changes."

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all phase-137 deliverables verified; the single blocked item is a host-machine Xcode-license env gate, not a code gap]
