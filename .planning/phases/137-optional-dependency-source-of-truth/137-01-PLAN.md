---
phase: 137-optional-dependency-source-of-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/optional_deps.ex
  - test/sigra/optional_deps_test.exs
autonomous: true
requirements: [OD-01]
must_haves:
  truths:
    - "A maintainer can answer 'is this optional dep available?' for every guarded dep (Oban, Bcrypt, EQRCode, Threadline, Assent, Swoosh, Joken, Req) by reading one module (SC#1, SC#4, D-01)"
    - "Encryption posture (stub vs real vault) is answerable from the SOT via a config-driven mirror, NOT a Cloak load check (D-07)"
    - "Each predicate returns the same truth value as the inline Code.ensure_loaded?(Mod) it replaces — un-memoized, one-to-one (D-03)"
  artifacts:
    - path: "lib/sigra/optional_deps.ex"
      provides: "Sigra.OptionalDeps SOT: 9 zero-arity availability predicates + encryption_active?/1"
      contains: "def oban_available?"
      min_lines: 40
    - path: "test/sigra/optional_deps_test.exs"
      provides: "Unit tests asserting each predicate == Code.ensure_loaded?(Mod), plus encryption stub-vs-vault fixtures"
      contains: "encryption_active?"
  key_links:
    - from: "lib/sigra/optional_deps.ex"
      to: "Code.ensure_loaded?/1"
      via: "thin un-memoized wrapper, one predicate per module atom"
      pattern: "Code\\.ensure_loaded\\?\\("
    - from: "lib/sigra/optional_deps.ex encryption_active?/1"
      to: "module.__sigra_encryption_mode__/0"
      via: "config-driven derivation of *.Encrypted.Binary, mirrors application.ex:184-230"
      pattern: "__sigra_encryption_mode__"
---

<objective>
Create `Sigra.OptionalDeps` (OD-01): the single canonical module answering "is this optional dependency available?" for every optional dep Sigra guards today, plus a config-driven encryption-posture predicate. Create its unit test file (Wave 0 gap from VALIDATION.md).

Purpose: Establishes the source of truth that Wave 2 delegation (OD-02, plans 02/03) and Phase 138 (`mix sigra.doctor`) both consume. Replaces the scattered-grep maintenance model with one-module audit (SC#4).

Output:
- `lib/sigra/optional_deps.ex` — 9 flat zero-arity predicates (`oban_available?/0`, `bcrypt_available?/0`, `eqrcode_available?/0`, `threadline_available?/0`, `assent_available?/0`, `swoosh_available?/0`, `joken_available?/0`, `hammer_available?/0`, `req_available?/0`) each a thin `Code.ensure_loaded?(Mod)` wrapper (D-01, D-03), plus `encryption_active?/1` mirroring the stub-vs-real config check (D-07). Carries the D-05 `@moduledoc` scope note.
- `test/sigra/optional_deps_test.exs` — drift-catching equality assertions + encryption fixtures.

Scope note: `swoosh_available?/0` and `req_available?/0` are created for SOT completeness (OD-01) and Phase-138 consumption. They have NO in-scope runtime delegation target in `lib/` (Swoosh's only `lib/` guard is the out-of-scope test-helper `testing.ex:98`; Req is a transitive dep guarded only at the compound `validation.ex:91` whose load-half plan 03 delegates). Do NOT treat the absence of a Swoosh delegation site as a missing-delegation gap (RESEARCH drift item #2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-optional-dependency-source-of-truth/137-CONTEXT.md
@.planning/phases/137-optional-dependency-source-of-truth/137-PATTERNS.md
@.planning/phases/137-optional-dependency-source-of-truth/137-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sigra.OptionalDeps module (9 predicates + encryption_active?/1)</name>
  <files>lib/sigra/optional_deps.ex</files>
  <read_first>
    - lib/sigra/rate_limiter.ex:1-26 — house-style `@moduledoc` voice, `@doc since: "0.1.0"`, explicit `@spec` discipline; location/style precedent for a top-level `Sigra.` module (it is NOT a behaviour triad — do not generate a behaviour or Noop sibling).
    - 137-PATTERNS.md § "lib/sigra/optional_deps.ex (NEW)" and § "Encryption predicate (D-07)" — the predicate body shape, the nine module atoms, and the encryption mirror logic with exact source excerpts.
    - lib/sigra/application.ex:184-230 — `verify_vault!/1` (the operative `__sigra_encryption_mode__() == :stub` check) and `encrypted_binary_module/1` (the `user_schema` → drop-last → `++ ["Encrypted","Binary"]` → `Module.concat` derivation) that `encryption_active?/1` must replicate.
    - mix.exs:65-91 — the `no_warn_undefined` whitelist (already lists Oban, Bcrypt, EQRCode, Threadline, Assent, Swoosh.Email, Joken, Hammer); confirm Req presence is unnecessary because `Code.ensure_loaded?/1` takes a module atom and emits no compile warning (D-10).
  </read_first>
  <action>
Create `lib/sigra/optional_deps.ex` defining `defmodule Sigra.OptionalDeps`.

Define nine flat zero-arity predicates, each with `@spec name() :: boolean()` and a single-clause body `def name, do: Code.ensure_loaded?(TheModule)` (D-01, D-03 — thin, pure, un-memoized; NO caching, NO ETS, NO persistent_term, NO dispatcher):
- `oban_available?` → `Code.ensure_loaded?(Oban)`
- `bcrypt_available?` → `Code.ensure_loaded?(Bcrypt)`
- `eqrcode_available?` → `Code.ensure_loaded?(EQRCode)`
- `threadline_available?` → `Code.ensure_loaded?(Threadline)`
- `assent_available?` → `Code.ensure_loaded?(Assent)`
- `swoosh_available?` → `Code.ensure_loaded?(Swoosh)`
- `joken_available?` → `Code.ensure_loaded?(Joken)`
- `hammer_available?` → `Code.ensure_loaded?(Hammer)`
- `req_available?` → `Code.ensure_loaded?(Req)`

Add the encryption predicate `encryption_active?/1` (name is planner discretion per D-07; use `encryption_active?`), `@spec encryption_active?(keyword()) :: boolean()`, guarded `when is_list(host_sigra)`. It MUST mirror `application.ex` logic and MUST NOT use `Code.ensure_loaded?(Cloak)` (D-07 — a load check would report "available" while the app is still on the plaintext stub, a silent security regression). Replicate `encrypted_binary_module/1` as a private helper (same `Keyword.get(host_sigra, :user_schema)` → `Module.split |> Enum.drop(-1) |> Kernel.++(["Encrypted","Binary"]) |> Module.concat` derivation, returning `nil` when `:user_schema` is absent/non-atom). Then: `nil -> false`; otherwise return `function_exported?(module, :__sigra_encryption_mode__, 0) and module.__sigra_encryption_mode__() != :stub`. Use `!= :stub` (NOT `== :vault`/`== :real`) — the live non-stub value is `:vault` and the mirror semantics are stub-vs-everything-else, matching `application.ex:194` which only checks `== :stub`.

The `@moduledoc` MUST carry the D-05 scope note (these strings are load-bearing for the Phase 140 verifier): (a) scope is RUNTIME call-site guards only; (b) compile-time `defmodule` wrappers (`workers/*.ex`, `audit/forwarders/threadline.ex`) stay literal and do NOT delegate here (D-04), because they run before this module may be compiled; (c) encryption is config-driven not load-driven (D-07); (d) note that `application.ex:77`'s boot-warning `Code.ensure_loaded?(Oban)` is intentionally left literal as a known non-delegated runtime check (Open Question 1 default). Follow the prose `@moduledoc` voice of `rate_limiter.ex`.

Do NOT add any new entry to the `mix.exs:65-91` `no_warn_undefined` whitelist (D-10).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; test -f lib/sigra/optional_deps.ex && grep -c "def .*available?\|def encryption_active?" lib/sigra/optional_deps.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/sigra/optional_deps.ex` defines `defmodule Sigra.OptionalDeps`.
    - File contains each of: `def oban_available?`, `def bcrypt_available?`, `def eqrcode_available?`, `def threadline_available?`, `def assent_available?`, `def swoosh_available?`, `def joken_available?`, `def hammer_available?`, `def req_available?`, `def encryption_active?` (10 public functions).
    - `git grep -n "Code.ensure_loaded?(Cloak)" lib/sigra/optional_deps.ex` returns NOTHING (D-07).
    - `grep -n "__sigra_encryption_mode__" lib/sigra/optional_deps.ex` shows the mirror uses `!= :stub`.
    - `@moduledoc` contains the words "runtime" and "compile-time" and references the `defmodule` wrapper out-of-scope note (D-04/D-05).
    - `mix compile --warnings-as-errors` exits 0.
    - `git diff mix.exs` is empty — no new `no_warn_undefined` entry (D-10).
  </acceptance_criteria>
  <done>`Sigra.OptionalDeps` exists at `lib/sigra/optional_deps.ex` with all 9 availability predicates + `encryption_active?/1`, the D-05 `@moduledoc` scope note, no Cloak load check, no mix.exs change, and compiles warnings-as-errors clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create SOT unit test file (Wave 0 gap)</name>
  <files>test/sigra/optional_deps_test.exs</files>
  <read_first>
    - lib/sigra/optional_deps.ex — the module just created (predicate names + `encryption_active?/1` signature to assert against).
    - test/sigra/audit/forwarders/noop_test.exs:1-50 — thin-module unit-test voice: `use ExUnit.Case, async: true`, `describe` per public function, AAA, decision-ID references in test names.
    - test/sigra/upgrade_test.exs:229-260 — the in-test encryption-mode fixture-module pattern (define `*.Encrypted.Binary` with `def __sigra_encryption_mode__, do: :stub` / `:vault`); copy this pattern.
    - 137-VALIDATION.md § "Wave 0 Requirements" — the drift-catching equality strategy: assert each predicate EQUALS a freshly-evaluated `Code.ensure_loaded?(Mod)`, NOT a hardcoded `true`, so the same test stays valid in a dep-off env.
  </read_first>
  <action>
Create `test/sigra/optional_deps_test.exs` with `use ExUnit.Case, async: true` (no DB needed — these are pure).

For each of the 9 predicates write a test asserting equality to a freshly-evaluated `Code.ensure_loaded?(Mod)` (e.g. `assert Sigra.OptionalDeps.oban_available?() == Code.ensure_loaded?(Oban)`). This is a drift-catching tautology that stays valid in both the all-deps `library_tests` lane (asserts `== true`) and the Threadline dep-off lane (`threadline_available?/0 == false`). Do NOT hardcode `true`.

For `encryption_active?/1`, follow the `upgrade_test.exs` fixture pattern inside a `describe "encryption_active?/1"` block: define module fixtures `StubVault.Encrypted.Binary` (with `def __sigra_encryption_mode__, do: :stub`) and `RealVault.Encrypted.Binary` (with `def __sigra_encryption_mode__, do: :vault`), plus throwaway `StubVault.User`/`RealVault.User` schemas (the derivation drops the last segment of `:user_schema` and appends `Encrypted.Binary`). Assert: `refute Sigra.OptionalDeps.encryption_active?(user_schema: StubVault.User)` (stub → false); `assert Sigra.OptionalDeps.encryption_active?(user_schema: RealVault.User)` (vault → true); and `refute Sigra.OptionalDeps.encryption_active?([])` (no `:user_schema` → nil module → false).

Use decision-ID references in test names (e.g. "mirrors stub-vs-real config check — D-07"). AAA style, flat, self-contained per CLAUDE.md.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/optional_deps_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/sigra/optional_deps_test.exs` exists and uses `ExUnit.Case, async: true`.
    - `grep -c "Code.ensure_loaded?" test/sigra/optional_deps_test.exs` returns >= 9 (one equality assertion per predicate, drift-catching not hardcoded).
    - File contains a `describe "encryption_active?/1"` block with both `:stub` and `:vault` fixtures and a no-`:user_schema` (false) case.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/optional_deps_test.exs` exits 0.
  </acceptance_criteria>
  <done>The SOT unit file passes: all 9 predicate equality assertions green and the 3 encryption cases (stub→false, vault→true, missing→false) green. Wave 0 gap from VALIDATION.md is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal refactor. No new external input crosses any boundary — predicates take only module atoms; `encryption_active?/1` takes read-only host config. Attack surface is essentially nil. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Information Disclosure | `Sigra.OptionalDeps.encryption_active?/1` | mitigate | A naive `cloak_available?/0` load check (rejected by D-07) would report "encryption available" while the app is still on the plaintext stub — a SILENT at-rest-encryption-disabled regression (ASVS V6). Mitigation: the predicate mirrors `application.ex` `__sigra_encryption_mode__() != :stub` config check exactly; no `Code.ensure_loaded?(Cloak)`. Unit test asserts stub→false. **Severity: this is the one real security-posture item in the phase; mitigated, not left open.** |
| T-137-02 | Tampering | predicate truth-value drift vs inline check | mitigate | Each predicate is a one-to-one un-memoized `Code.ensure_loaded?(Mod)` wrapper (D-03); the unit test asserts `predicate() == Code.ensure_loaded?(Mod)`, catching any future drift. No caching that could stale a truth value. |
| T-137-SC | Tampering | npm/pip/cargo/mix installs | accept | No new packages installed (RESEARCH § Package Legitimacy Audit: N/A — all 9 referenced deps already present, 8 declared `optional: true` in mix.exs, `req` transitive). No install task; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 (no new `no_warn_undefined` whitelist entry — D-10; `git diff mix.exs` empty).
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/optional_deps_test.exs` exits 0.
- `git grep -n "Code.ensure_loaded?(Cloak)" lib/` returns nothing (encryption never load-checked — D-07).
</verification>

<success_criteria>
- OD-01 satisfied: `Sigra.OptionalDeps` exposes a per-dep availability predicate for Oban, Bcrypt, EQRCode, Threadline, Assent, Swoosh, Joken, Hammer, Req, plus `encryption_active?/1` for the cloak/encryption posture (SC#1).
- A maintainer can audit/add an optional dep by editing this one module (SC#4).
- All predicates are un-memoized one-to-one wrappers (preserves OD-02 provability for Wave 2 — D-03).
- mix.exs `no_warn_undefined` unchanged (D-10).
</success_criteria>

<output>
Create `.planning/phases/137-optional-dependency-source-of-truth/137-01-SUMMARY.md` when done.
</output>
