---
phase: 137-optional-dependency-source-of-truth
plan: 02
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - lib/sigra/crypto.ex
  - lib/sigra/hashers/bcrypt.ex
  - lib/sigra/mfa.ex
  - lib/sigra/jwt/signer.ex
  - lib/sigra/plug/rate_limit.ex
  - lib/sigra/oauth/strategies/apple.ex
  - lib/sigra/oauth/strategies/facebook.ex
  - lib/sigra/oauth/strategies/github.ex
  - lib/sigra/oauth/strategies/generic.ex
  - lib/sigra/oauth/strategies/google.ex
autonomous: true
requirements: [OD-02]
must_haves:
  truths:
    - "The single-leaf runtime optional-dep guards (Bcrypt, EQRCode, Joken, Hammer, Assent) delegate to Sigra.OptionalDeps instead of inlining Code.ensure_loaded? (SC#2)"
    - "bcrypt-verify timing-protection fallback, MFA QR nil path, Joken/Assent raise-guards, and Hammer-vs-Noop resolution behave identically — no runtime behavior change (OD-02, proven by existing tests staying green unchanged)"
  artifacts:
    - path: "lib/sigra/crypto.ex"
      provides: "bcrypt_verify/2 delegating the Bcrypt load-check to the SOT"
      contains: "Sigra.OptionalDeps.bcrypt_available?()"
    - path: "lib/sigra/oauth/strategies/apple.ex"
      provides: "ensure_assent! raise-guard delegating to the SOT"
      contains: "Sigra.OptionalDeps.assent_available?()"
  key_links:
    - from: "lib/sigra/crypto.ex"
      to: "Sigra.OptionalDeps.bcrypt_available?/0"
      via: "if-guard delegation (else timing-protection branch byte-preserved)"
      pattern: "Sigra\\.OptionalDeps\\.bcrypt_available\\?\\(\\)"
    - from: "lib/sigra/jwt/signer.ex"
      to: "Sigra.OptionalDeps.joken_available?/0"
      via: "unless-guard delegation (raise block byte-preserved)"
      pattern: "Sigra\\.OptionalDeps\\.joken_available\\?\\(\\)"
---

<objective>
Delegate the single-leaf (non-compound) runtime optional-dep guards to `Sigra.OptionalDeps` (OD-02). Each is a mechanical one-to-one token swap — `Code.ensure_loaded?(NamedMod)` → `Sigra.OptionalDeps.<dep>_available?()` — with all branch bodies, raise blocks, and else-clauses byte-preserved. That invariance IS the OD-02 no-behavior-change proof.

Purpose: Routes the Bcrypt, EQRCode, Joken, Hammer, and Assent runtime guards through the SOT created in plan 01, so a maintainer audits one module rather than grepping call sites (SC#4) and the semantics are provably unchanged (SC#3).

Output: 10 files edited (crypto, hashers/bcrypt ×2 sites, mfa, jwt/signer, plug/rate_limit, 5 oauth strategies) — 11 guard expressions total. No new file. No file overlap with plan 03 (compound guards), so plans 02 and 03 run in parallel in Wave 2.

Bucket fence (DO NOT TOUCH): no compile-time `defmodule` wrappers in scope here; no dynamic-module guards; no test-helpers. This plan touches ONLY the Bucket A single-leaf sites enumerated in the tasks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-optional-dependency-source-of-truth/137-CONTEXT.md
@.planning/phases/137-optional-dependency-source-of-truth/137-PATTERNS.md
@.planning/phases/137-optional-dependency-source-of-truth/137-VALIDATION.md
@lib/sigra/optional_deps.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate Bcrypt + EQRCode guards (crypto, hashers/bcrypt, mfa)</name>
  <files>lib/sigra/crypto.ex, lib/sigra/hashers/bcrypt.ex, lib/sigra/mfa.ex</files>
  <read_first>
    - lib/sigra/optional_deps.ex — confirm `bcrypt_available?/0` and `eqrcode_available?/0` exist (this plan depends on 137-01).
    - 137-PATTERNS.md § "lib/sigra/crypto.ex:244", § "lib/sigra/hashers/bcrypt.ex:39,48", § "lib/sigra/mfa.ex:1059" — the exact before→after token swaps and the byte-preserve rules.
    - lib/sigra/crypto.ex:243-252 — `bcrypt_verify/2`; the else-branch `no_user_verify(); false` is a timing-protection security control (ASVS V2/timing) that MUST stay byte-equivalent.
    - lib/sigra/hashers/bcrypt.ex:35-52 — `no_user_verify/0` (line 39 `if`, else falls back to Argon2 timing) and `ensure_loaded!/0` (line 48 `unless` raise guard).
    - lib/sigra/mfa.ex:1057-1063 — `generate_qr_svg/1` (line 1059 `if`, else returns `nil`).
  </read_first>
  <action>
Swap exactly these guard expressions; change NOTHING else on the lines and leave every branch body / else-clause byte-identical:
- `lib/sigra/crypto.ex:244` — `if Code.ensure_loaded?(Bcrypt) do` → `if Sigra.OptionalDeps.bcrypt_available?() do`. The else timing-protection branch (`no_user_verify(); false`) stays byte-equivalent (drift item: this is the canonical site).
- `lib/sigra/hashers/bcrypt.ex:39` — `if Code.ensure_loaded?(Bcrypt) do` → `if Sigra.OptionalDeps.bcrypt_available?() do` (in `no_user_verify/0`; the Argon2-timing else stays). DRIFT site not in CONTEXT.md — in scope per RESEARCH Bucket A #2.
- `lib/sigra/hashers/bcrypt.ex:48` — `unless Code.ensure_loaded?(Bcrypt) do` → `unless Sigra.OptionalDeps.bcrypt_available?() do` (in `ensure_loaded!/0`; raise block preserved). DRIFT site not in CONTEXT.md — in scope per RESEARCH Bucket A #3.
- `lib/sigra/mfa.ex:1059` — `if Code.ensure_loaded?(EQRCode) do` → `if Sigra.OptionalDeps.eqrcode_available?() do` (else `nil` branch stays).
Reiterate: branch bodies, raise blocks, else-clauses are byte-preserved — that invariance is the OD-02 proof.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/crypto_test.exs && git grep -n "Code.ensure_loaded?" lib/sigra/crypto.ex lib/sigra/hashers/bcrypt.ex lib/sigra/mfa.ex</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "Code.ensure_loaded?(Bcrypt)" lib/sigra/crypto.ex lib/sigra/hashers/bcrypt.ex` returns NOTHING.
    - `git grep -n "Code.ensure_loaded?(EQRCode)" lib/sigra/mfa.ex` returns NOTHING.
    - `lib/sigra/crypto.ex` contains `Sigra.OptionalDeps.bcrypt_available?()`; `lib/sigra/hashers/bcrypt.ex` contains it twice (lines ~39, ~48); `lib/sigra/mfa.ex` contains `Sigra.OptionalDeps.eqrcode_available?()`.
    - `mix test test/sigra/crypto_test.exs` exits 0 (bcrypt-verify fallback unchanged).
    - `git diff lib/sigra/crypto.ex` shows ONLY the guard line changed — the `no_user_verify(); false` else-branch is byte-identical.
  </acceptance_criteria>
  <done>Bcrypt (3 sites) and EQRCode (1 site) runtime guards delegate to the SOT; crypto tests green; else/timing branches byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate Joken, Hammer, and 5 Assent raise/resolve guards</name>
  <files>lib/sigra/jwt/signer.ex, lib/sigra/plug/rate_limit.ex, lib/sigra/oauth/strategies/apple.ex, lib/sigra/oauth/strategies/facebook.ex, lib/sigra/oauth/strategies/github.ex, lib/sigra/oauth/strategies/generic.ex, lib/sigra/oauth/strategies/google.ex</files>
  <read_first>
    - lib/sigra/optional_deps.ex — confirm `joken_available?/0`, `hammer_available?/0`, `assent_available?/0` exist.
    - 137-PATTERNS.md § "Raise-guard pattern (jwt/signer + 5 oauth strategies)" and § "lib/sigra/plug/rate_limit.ex:85" — canonical raise-guard shape (apple.ex:74-81) and the Hammer resolve site, with exact swaps.
    - lib/sigra/jwt/signer.ex:14-24 — `ensure_joken!/0`; the raise block (Security V/Tampering: must still raise on absence) stays byte-preserved.
    - lib/sigra/plug/rate_limit.ex:82-92 — `resolve_limiter(nil)`; the else Noop-fallback + `Logger.warning` branch stays.
    - lib/sigra/oauth/strategies/apple.ex:76 (and facebook:80, github:77, generic:83, google:74) — 5 identical `unless Code.ensure_loaded?(Assent) do` raise guards.
  </read_first>
  <action>
Swap exactly these guard expressions; raise blocks / else-branches byte-preserved:
- `lib/sigra/jwt/signer.ex:18` — `unless Code.ensure_loaded?(Joken) do` → `unless Sigra.OptionalDeps.joken_available?() do`. The `raise RuntimeError, """..."""` block must still fire on absence (Tampering/DoS mitigation).
- `lib/sigra/plug/rate_limit.ex:85` — `if Code.ensure_loaded?(Hammer) do` → `if Sigra.OptionalDeps.hammer_available?() do`. The else branch (`Sigra.RateLimiters.Noop` + `Logger.warning` fail-open fallback) stays byte-identical.
- `lib/sigra/oauth/strategies/apple.ex:76`, `facebook.ex:80`, `github.ex:77`, `generic.ex:83`, `google.ex:74` — all five identical `unless Code.ensure_loaded?(Assent) do` → `unless Sigra.OptionalDeps.assent_available?() do`. Each raise block preserved (must still raise on Assent absence).
Reiterate: only the load-check token changes; raise blocks and else-clauses are byte-preserved.
  </action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/plug/rate_limit_test.exs && git grep -cn "Code.ensure_loaded?(Assent)" lib/sigra/oauth/strategies/ ; git grep -n "Code.ensure_loaded?(Joken)\|Code.ensure_loaded?(Hammer)" lib/sigra/jwt/signer.ex lib/sigra/plug/rate_limit.ex</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "Code.ensure_loaded?(Joken)" lib/sigra/jwt/signer.ex` returns NOTHING; file contains `Sigra.OptionalDeps.joken_available?()`.
    - `git grep -n "Code.ensure_loaded?(Hammer)" lib/sigra/plug/rate_limit.ex` returns NOTHING; file contains `Sigra.OptionalDeps.hammer_available?()`.
    - `git grep -n "Code.ensure_loaded?(Assent)" lib/sigra/oauth/strategies/` returns NOTHING; all 5 strategy files contain `Sigra.OptionalDeps.assent_available?()`.
    - `mix test test/sigra/plug/rate_limit_test.exs` exits 0 (Hammer-vs-Noop resolution unchanged).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Joken (1), Hammer (1), and Assent (5) runtime guards delegate to the SOT; raise/fallback branches byte-preserved; rate-limit tests green; compiles warnings-as-errors clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure delegation refactor. No new external input; the guarded control flow (raise/fallback) is preserved byte-for-byte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-03 | Information Disclosure (timing) | `crypto.ex` / `hashers/bcrypt.ex` bcrypt-verify fallback | mitigate | The else-branch timing protection (`no_user_verify()`; Argon2 timing fallback) is a side-channel control. Only the load-check token is swapped; the timing branches stay byte-equivalent. `crypto_test.exs` stays green unchanged. |
| T-137-04 | Tampering / DoS | `jwt/signer.ex`, 5 `oauth/strategies/*.ex` raise-guards | mitigate | These guards must still RAISE on dep absence. `unless predicate(), do: raise` preserves the raise exactly; existing oauth/jwt tests stay green unchanged. |
| T-137-05 | Tampering | accidental delegation of an out-of-scope guard | mitigate | This plan touches ONLY the enumerated Bucket A single-leaf sites; `files_modified` excludes all `workers/*.ex`, `credo/*.ex`, `testing.ex`, and dynamic-module guards (Bucket B/C/D fence). No compound-guard liveness half is in scope here (those are plan 03). |
| T-137-SC | Tampering | npm/pip/cargo/mix installs | accept | No package installs in this plan (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `git grep -n "Code.ensure_loaded?" lib/sigra/crypto.ex lib/sigra/hashers/bcrypt.ex lib/sigra/mfa.ex lib/sigra/jwt/signer.ex lib/sigra/plug/rate_limit.ex lib/sigra/oauth/strategies/*.ex` returns NOTHING (all 11 single-leaf guards delegated).
- `mix compile --warnings-as-errors` exits 0 (no new `no_warn_undefined` entry — D-10; `git diff mix.exs` empty).
- `mix test test/sigra/crypto_test.exs test/sigra/plug/rate_limit_test.exs` exits 0; existing oauth/jwt/mfa tests stay green unchanged.
</verification>

<success_criteria>
- OD-02 (single-leaf subset) satisfied: 11 runtime guards across 10 files delegate to `Sigra.OptionalDeps` (SC#2).
- No runtime behavior change: raise blocks, timing-protection else-branches, and Noop fallbacks are byte-preserved; existing tests pass unchanged (SC#3).
- No out-of-scope guard touched (Buckets B/C/D fenced via `files_modified`).
</success_criteria>

<output>
Create `.planning/phases/137-optional-dependency-source-of-truth/137-02-SUMMARY.md` when done.
</output>
