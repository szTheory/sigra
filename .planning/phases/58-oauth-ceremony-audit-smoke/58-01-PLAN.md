---
phase: 58
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/oauth/oauth_ceremony_audit_test.exs
  - test/sigra/oauth/oauth_audit_atomicity_test.exs
autonomous: true
requirements:
  - OA-01
must_haves:
  truths:
    - "**`Sigra.OAuthCeremonyAuditTest`** (`test/sigra/oauth/oauth_ceremony_audit_test.exs`) exists with **`@moduledoc`** citing **OA-01** and **`.planning/REQUIREMENTS.md`**, and **`mix test`** for that file exits **0** under Postgres env (**58-CONTEXT D-58-07**)."
    - "**Registration ceremony:** **`Callback.process_callback/4`** on a **new user** persists **`oauth.register_via_oauth`** audit row verifiable via **`Sigra.Audit.Assertions`** (**D-58-01**, moved from atomicity file)."
    - "**Authorize ceremony:** **`Sigra.OAuth.authorize_url/3`** with **`audit: [audit_schema: Sigra.Test.AuditEvent]`** (or equivalent test schema module bound in config) persists **`oauth.authorize`** with metadata containing **only** provider string (**D-58-02**, **D-58-06**)."
    - "**`oauth_audit_atomicity_test.exs`** retains **rollback** and **constraint rejection** tests and **does not** retain the moved happy-path test (**D-58-08**)."
    - "**No** live-provider secrets or raw tokens in new fixtures/metadata (**roadmap success criterion 3**)."
  artifacts:
    - path: "test/sigra/oauth/oauth_ceremony_audit_test.exs"
      provides: "Merge-blocking OA-01 ceremony + audit assertions"
    - path: "test/sigra/oauth/oauth_audit_atomicity_test.exs"
      provides: "Atomicity / rollback proofs only (phase 45 narrative)"
nyquist_compliant: false
---

<objective>
Implement **OA-01** merge-blocking OAuth ceremony tests: new **`Sigra.OAuthCeremonyAuditTest`** module with **registration** + **`authorize_url`** audit row proofs using **real `PostgresRepo` + Sandbox**, move the existing happy-path registration audit test out of **`oauth_audit_atomicity_test.exs`**, and keep atomicity file focused on rollback/constraint behavior (**58-CONTEXT**, **58-RESEARCH**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-58-01 | Information disclosure | Audit **`metadata`** or test logs capture tokens, refresh payloads, or PII | high | Assertions allow-list **`:provider`** only; use **`MockStrategy`**-style static tokens; mirror **`oauth.ex`** comment **D-23** |
| T-58-02 | Repudiation | Tests use **`MockRepo`** and claim DB audit truth | medium | **Forbidden** for these proofs — use **`PostgresRepo`** + DDL like **`OAuthAuditAtomicityTest`** (**58-PATTERNS**) |
| T-58-03 | Tampering | Moved test duplicated instead of moved → drift | low | **Delete** **`persists oauth.register_via_oauth after successful registration`** from atomicity file in same change set |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add Sigra.OAuthCeremonyAuditTest module</name>
  <files>test/sigra/oauth/oauth_ceremony_audit_test.exs</files>
  <read_first>
    - test/sigra/oauth/oauth_audit_atomicity_test.exs
    - test/sigra/oauth/oauth_test.exs
    - lib/sigra/oauth.ex
    - lib/sigra/oauth/callback.ex
    - lib/sigra/audit/assertions.ex
    - .planning/phases/58-oauth-ceremony-audit-smoke/58-CONTEXT.md
  </read_first>
  <action>
    Create **`test/sigra/oauth/oauth_ceremony_audit_test.exs`** defining **`Sigra.OAuthCeremonyAuditTest`** with:

    1. **`use ExUnit.Case, async: false`** (matches repo-holding OAuth integration tests).
    2. **`@moduledoc`** — first paragraph must include literal substring **`OA-01`** and a pointer to **`.planning/REQUIREMENTS.md`** (path as written here).
    3. **Nested schemas** — copy **`OAuthUser`** / **`OAuthIdentity`** module definitions from **`oauth_audit_atomicity_test.exs`** (same table names **`oauth_atomic_users`** / **`oauth_atomic_identities`**) or extract to shared test support only if you complete **Task 3** in the same PR; default is inline copy to avoid scope creep.
    4. **`setup` block** — same sequence as atomicity file: `start_supervised!({PostgresRepo, PostgresRepo.default_config()})`; `CREATE EXTENSION IF NOT EXISTS "uuid-ossp"`; `DROP TABLE IF EXISTS` for **`oauth_atomic_identities`**, **`oauth_atomic_users`**, **`audit_events`**; **`CREATE TABLE`** DDL for all three (copy verbatim from **`oauth_audit_atomicity_test.exs`** lines **50–108** region); **`TRUNCATE … CASCADE`** before each test.
    5. **`oauth_config/1`** helper — return the same **`%{}`** shape as **`oauth_config/1`** in **`oauth_audit_atomicity_test.exs`** ( **`audit: [audit_schema: Sigra.Test.AuditEvent]`** aliased as **`AuditTestEvent`** if you keep the same alias pattern).

    6. **`describe "registration"`** — move the test named **`persists oauth.register_via_oauth after successful registration`** from **`oauth_audit_atomicity_test.exs`** here verbatim in behavior (same **`Callback.process_callback`**, **`Assertions.assert_audit_fields`**, **`user_info("persist@example.com")`**, **`token()`**), keeping test name string identical for grep stability.

    7. **`defmodule MockStrategy`** inside this file (or nested under test module) implementing **`authorize_url/1`** returning **`{:ok, %{url: "https://provider.example.com/auth?state=original&scope=email", session_params: %{code_verifier: "pkce_verifier"}}}`** — same shape as **`Sigra.OAuthTest.MockStrategy`** in **`oauth_test.exs`** lines **13–20**.

    8. **`describe "authorize"`** — one test **`persists oauth.authorize after successful authorize_url`** that builds config:

       - **`repo`**: setup repo
       - **`secret_key_base`**: **`String.duplicate("a", 64)`**
       - **`user_schema`**, **`identity_schema`**: same modules as registration block
       - **`oauth`**: **`[enabled: true, providers: [mock: [client_id: "test_id", client_secret: "test_secret", strategy: <YourMockStrategy>]], session_type: :remember_me, link_confirmation: :required, trust_provider_email: true]`**
       - **`session`**: **`[session_schema: OAuthUser, store: Sigra.Test.MockSessionStore]`**
       - **`audit: [audit_schema: AuditTestEvent]`**

       Call **`assert {:ok, _url, _} = Sigra.OAuth.authorize_url(config, :mock, [])`**, then **`Sigra.Audit.Assertions.assert_audit_fields(repo, AuditTestEvent, %{action: "oauth.authorize", metadata: %{"provider" => "mock"}})`** (use **`%{}`** key shape that matches stored JSON — if tests expect atom keys inside metadata map from Ecto, match existing **`assert_audit_fields`** usage in repo).

    9. **Grouped `describe`** names must be **`"registration"`** and **`"authorize"`** (lowercase) per **D-58-07**.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F 'defmodule Sigra.OAuthCeremonyAuditTest' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F 'OA-01' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F '.planning/REQUIREMENTS.md' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F 'describe "registration"' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F 'describe "authorize"' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F 'persists oauth.register_via_oauth after successful registration' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F 'persists oauth.authorize after successful authorize_url' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `grep -F 'oauth.authorize' test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/oauth/oauth_ceremony_audit_test.exs` exits **0**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 2: Trim oauth_audit_atomicity_test.exs</name>
  <files>test/sigra/oauth/oauth_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/oauth/oauth_audit_atomicity_test.exs
    - test/sigra/oauth/oauth_ceremony_audit_test.exs
  </read_first>
  <action>
    Remove the **`test "persists oauth.register_via_oauth after successful registration"`** block entirely from **`oauth_audit_atomicity_test.exs`** (it now lives in **`Sigra.OAuthCeremonyAuditTest`**).

    Keep **`test "rolls back registration when oauth.register_via_oauth audit insert is rejected"`** unchanged in behavior.

    If **`@moduledoc`** is **`false`** on the outer module, optionally add a one-line **`@moduledoc`** clarifying this file is **atomicity / rollback** proofs, not the **OA-01** primary entry point — do **not** remove imports or setup required by the remaining test.
  </action>
  <acceptance_criteria>
    - `grep -F 'persists oauth.register_via_oauth after successful registration' test/sigra/oauth/oauth_audit_atomicity_test.exs` exits **1** (no matches)
    - `grep -F 'rolls back registration when oauth.register_via_oauth audit insert is rejected' test/sigra/oauth/oauth_audit_atomicity_test.exs` exits **0**
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/oauth/oauth_audit_atomicity_test.exs` exits **0**
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/oauth/oauth_ceremony_audit_test.exs test/sigra/oauth/oauth_audit_atomicity_test.exs` exits **0**
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/oauth/oauth_ceremony_audit_test.exs test/sigra/oauth/oauth_audit_atomicity_test.exs`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- **`Sigra.OAuthCeremonyAuditTest`** runs green locally with Postgres **postgres/postgres@localhost**.
- **OA-01** satisfied: **two** distinct ceremony paths (registration + authorize) assert persisted audit actions where **`log_safe`** already writes rows.
- Atomicity file no longer carries the moved happy-path test.
</success_criteria>
