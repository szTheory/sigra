---
phase: 58
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs
autonomous: true
requirements:
  - OA-01
must_haves:
  truths:
    - "ExUnit module (recommended name **`Sigra.Planning.Phase58OauthOa01CiContractTest`**) reads **`.github/workflows/ci.yml`** and asserts the **`library_tests`** job still runs **`mix test`** in the **`Run library tests`** step without OAuth-related **`--exclude`** flags in that step’s **`run:`** line (**D-58-11**)."
    - "Assertions use **stable substring anchors** documented in the test **`@moduledoc`** so intentional workflow edits update one file deliberately."
  artifacts:
    - path: "test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs"
      provides: "Structural CI honesty for OA-01 merge gate"
nyquist_compliant: false
---

<objective>
Add optional **phase 50–52 style** planning contract test (**D-58-11**) that locks **`library_tests` → Run library tests → `mix test`** as the behavioral merge gate for OA-01, without duplicating a second full test run in CI.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-58-04 | Denial of service (maintainer) | Brittle YAML assertions break on unrelated workflow edits | low | Anchor on **`library_tests`**, **`Run library tests`**, and literal **`run: mix test`** (or multiline block containing **`mix test`** only) — document in **`@moduledoc`** |
| T-58-05 | Elevation of privilege | Contract test claims OAuth coverage while **`--exclude`** hides tests | medium | Assert **`Run library tests`** step’s **`run:`** does **not** contain **`--exclude`** substring (whole-job read is acceptable) |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: phase_58_oauth_oa01_ci_contract_test.exs</name>
  <files>test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
    - test/sigra/planning/phase_52_milestone_honesty_contract_test.exs
    - .github/workflows/ci.yml
    - .planning/phases/58-oauth-ceremony-audit-smoke/58-CONTEXT.md
  </read_first>
  <action>
    Create **`test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs`** with:

    1. **`use ExUnit.Case, async: true`**
    2. **`@moduledoc`** explaining this file is the **D-58-11** structural lock for **OA-01** / phase **58** and that **`library_tests`** must run plain **`mix test`**.
    3. Private **`read_ci!/0`** — `Path.expand("../../..", __DIR__)` joined with **`.github/workflows/ci.yml`** then **`File.read!/1`** (same pattern as **`phase_51`**).
    4. **One test** (name must include **`58-01`** for planning grep conventions) that:

       - Reads CI YAML into **`yml`**
       - Asserts **`yml =~ "library_tests:"`**
       - Asserts **`yml =~ "Run library tests"`**
       - Extracts the **`library_tests`** job section with a simple line-based heuristic **or** regex — acceptable approaches: (a) split on **`library_tests:`** and assert subsequent content until next top-level **`^[a-z_]+:`** job key contains **`Run library tests`** and a **`run:`** line with **`mix test`**; **or** (b) assert ordered substrings **`library_tests:`**, **`Run library tests`**, **`run: mix test`** appear in **`yml`** in that order (simpler, slightly looser).

       - Assert the **`library_tests`** region (or full **`yml`** if simpler) does **not** match case-insensitive **`--exclude`** near OAuth — minimal honest check: **`refute yml =~ ~r/--exclude.*oauth/i`** **or** assert the **`Run library tests`** step’s **`run:`** is exactly **`mix test`** on a single line if your read of **`ci.yml`** confirms that — **match current repo truth** (as of **2026-04-22** the step is single-line **`run: mix test`** at line **166**).

    5. **Async: true** — file must not start **`PostgresRepo`**.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` exits **0**
    - `grep -F 'Phase58OauthOa01CiContractTest' test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` exits **0**
    - `grep -F '58-01' test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` exits **0**
    - `grep -F 'library_tests' test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` exits **0**
    - `grep -F 'Run library tests' test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` exits **0**
    - `MIX_ENV=test mix test test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs` exits **0**
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0**
  </verify>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/sigra/planning/phase_58_oauth_oa01_ci_contract_test.exs`
</verification>

<success_criteria>
- Contract test green without Postgres (**async** planning test).
- CI workflow still documents **`library_tests`** behavioral gate for **`mix test`**.
</success_criteria>
