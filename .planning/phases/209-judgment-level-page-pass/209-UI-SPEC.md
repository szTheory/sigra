---
phase: 209
slug: judgment-level-page-pass
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-30
---

# Phase 209 — UI Design Contract

> Visual and interaction contract for Phase 209: Judgment-Level Page Pass.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Context Note

Phase 209 is a **judgment + in-place remediation pass** over the 8 existing admin
LiveView pages — not a greenfield surface. No new pages, components, or routes are
introduced (D-06). The design contract below governs **what to enforce, fix, and
measure** when remediating copy, IA, and component usage across those pages. Every
value here is pre-populated from the existing `sg-*` design system; the executor
must not introduce deviations.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (hand-authored `sg-*` BEM + cascade layers) |
| Preset | not applicable — no shadcn, no Tailwind pipeline |
| Component library | `Sigra.Admin.Components` (`lib/sigra/admin/components.ex`) |
| Icon library | SVG inline (no external icon library) |
| Font | Space Grotesk v2.0 (brand wordmark); system-ui / ui-sans-serif (admin UI body) |

Source: `priv/templates/sigra.install/admin/sigra_admin.css` `:root` block, `brandbook/tokens.css`, `guides/reference/admin-ui-principles.md`.

---

## Spacing Scale

Declared values from `--sg-space-*` tokens (4px base, rem units):

| Token | Rem | Approx px | Usage |
|-------|-----|-----------|-------|
| `--sg-space-1` | 0.25rem | 4px | Icon gaps, inline micro-padding |
| `--sg-space-2` | 0.5rem | 8px | Chip internal padding, tight element gaps |
| `--sg-space-3` | 0.75rem | 12px | Compact list rows, form field gaps |
| `--sg-space-4` | 1rem | 16px | Default element spacing, card padding |
| `--sg-space-5` | 1.25rem | 20px | Medium section gaps |
| `--sg-space-6` | 1.5rem | 24px | Section padding, `sg-stack--3` rhythm |
| `--sg-space-8` | 2rem | 32px | Major section breaks, `sg-stack--4` rhythm |
| `--sg-space-10` | 2.5rem | 40px | Page-level section gaps, `sg-stack--6` rhythm |
| `--sg-space-12` | 3rem | 48px | Top-level page stack gaps |

**Standard values** (multiples of 4, within the {4, 8, 16, 24, 32, 48, 64} canonical set): `--sg-space-1` (4px), `--sg-space-2` (8px), `--sg-space-4` (16px), `--sg-space-6` (24px), `--sg-space-8` (32px), `--sg-space-12` (48px).

**Documented exceptions** (multiples of 4; fall outside the standard {4, 8, 16, 24, 32, 48, 64} set — each justified below):

| Token | Value | Justification |
|-------|-------|---------------|
| `--sg-space-3` | 12px | Compact list rows and form field gaps where 8px is too tight (elements visually merge) and 16px wastes too much vertical rhythm. 12px is the midpoint that preserves scannable row density in the admin table/list archetype without crowding. |
| `--sg-space-5` | 20px | Medium section gaps between related content groups (e.g. filter panel to table body) where 16px under-separates sibling sections and 24px introduces too much visual weight. 20px maintains the section hierarchy without inflating the layout. |
| `--sg-space-10` | 40px | Page-level section gaps and `sg-stack--6` rhythm where 32px is too tight for major page sections and 48px is reserved for top-level page stack gaps (`--sg-space-12`). 40px distinguishes major structural separations from minor ones within the page frame. |

**Other exceptions:**
- Touch target minimum 44px (APG requirement) — enforced via explicit `min-height`/`min-width` on interactive controls, not via spacing tokens.
- `--sg-space-7` (1.75rem / 28px) exists in the token set; use only when `--sg-space-6` and `--sg-space-8` both misfit a specific component (rare).

Source: `sigra_admin.css:22–31`.

---

## Typography

Declared type scale from `--sg-text-*` and `--sg-weight-*` / `--sg-leading-*` tokens:

| Role | Token | Approx Size | Weight Token | Approx Weight | Line Height Token | Ratio |
|------|-------|-------------|--------------|---------------|-------------------|-------|
| Small / muted copy | `--sg-text-xs` | 12px | `--sg-weight-regular` | 450 | `--sg-leading-normal` | 1.5 |
| Body / table cells | `--sg-text-sm` | 14px | `--sg-weight-regular` | 450 | `--sg-leading-normal` | 1.5 |
| Default body | `--sg-text-base` | 16px | `--sg-weight-regular` | 450 | `--sg-leading-normal` | 1.5 |
| Section headings | `--sg-text-md` | 18px | `--sg-weight-semibold` | 700 | `--sg-leading-snug` | 1.3 |

**Reserved display size (outside the 4-size body scale):** H1 page titles use `--sg-text-lg` (~23px, weight 800, `--sg-leading-tight` 1.1) — a display size reserved exclusively for H1 page headings. Do not use `lg` in page body content. `xl`/`2xl` are kicker-label sizes; same restriction applies.

**Font weights (body scale — exactly 2):** regular (450) for body and table copy; semibold (700) for section headings.

**Reserved display weight (outside the 2-weight body scale):** H1 page titles use weight 800 (`--sg-weight-bold`) — a display weight paired with `--sg-text-lg` for H1 headings only. Do not apply weight 800 to any other element.

Source: `sigra_admin.css:34–51`.

---

## Color

| Role | Light Token | Light Value | Dark Override | Usage |
|------|-------------|-------------|---------------|-------|
| Dominant (60%) | `--sg-color-subtle` | `#f6f5f2` | `#171614` | Page background |
| Secondary (30%) | `--sg-color-panel` | `#ffffff` | `#1f1d1a` | Cards (`sg-card`), filter panels, table panels |
| Accent (10%) | `--sg-color-brand` | `#c2410c` | `#c2410c` | See reserved list below |
| Destructive | `--sg-color-risk` | `#b42318` | unchanged | `sg-btn--danger`, `data-tone="risk"` notices only |

**Accent (`--sg-color-brand` / ember) is reserved exclusively for:**
- Primary action buttons (`sg-btn--primary` — `background: --sg-color-brand`)
- Focused/selected state rings (`--sg-focus`)
- Rail Accent logo mark in the admin shell topbar
- Active tab indicator in `sg-tabs` nav (e.g. branding workbench)
- Ownership-boundary emphasis when a scope changes (e.g. `sg-scope-pill` in topbar)

Accent must **not** be applied to headings, icons, status pills, body copy, or decorative elements.

**Semantic tone colors** (non-accent — each paired with `*-soft` fill):
- `--sg-color-risk` / `--sg-color-risk-soft` — locked accounts, expired invitations, destructive confirms
- `--sg-color-warn` / `--sg-color-warn-soft` — unconfirmed accounts, no-MFA, deletion scheduled, pending invitations
- `--sg-color-ok` / `--sg-color-ok-soft` — success states (use sparingly; absence of warn/risk implies ok)
- `--sg-color-info` / `--sg-color-info-soft` — neutral informational notices

**Status signal rule (from `admin-ui-principles.md` Phase 203 D-02/D-03):** emit **only decision-bearing** status pills. Do not add always-present positive-confirmation pills ("Confirmed", "MFA active") — their absence implies the confirmed state.

Source: `sigra_admin.css:57–188`, `brandbook/tokens.css`, `guides/reference/admin-design-contract.md`.

---

## Visuals

**Primary visual anchor:** H1 page title (entity name, email, or resource type) — draws the eye first on every admin page. On leaf screens the H1 is the subject entity's display name or email; on list screens it is the resource type noun (e.g. "Users", "Audit"). This is the single dominant focal point per screen; no competing visual hierarchy element should match its weight.

---

## Copywriting Contract

This contract governs both the **panel scoring documents** and the **in-place remediations** that Phase 209 produces.

### Phase 209 Specific Copy

| Element | Copy |
|---------|------|
| Panel verdict disposition (clean surface) | `clean` |
| Panel verdict disposition (needs work) | `actionable` |
| Panel verdict disposition (blocked by external dep) | `blocked` |
| Forced-finding floor token (when no issue found) | `NONE — searched for: <what>` |
| Waiver rationale prefix | `Waiver: <rationale>` |

### Remediation Targets — Required Copy Changes

The following in-place copy changes are the known remediation floor from D-04. Exact wording is determined by the fresh adversarial panel output; the constraints below are the **floor contract**:

| Surface | Location | Current Copy (broken) | Remediation Constraint |
|---------|----------|-----------------------|------------------------|
| `index_live.ex` | Alarm notice (`:52`) | `All clear` | Replace with a posture-descriptive phrase that states the system state ("No flagged accounts" or similar); do not leave bare "All clear" — it reads as a dismissive placeholder, not a real status message |
| `user_sessions_live.ex` | H1 heading (`:108`) | `Sessions` | Change to entity-name H1 matching the sibling detail page pattern (display name or email as H1, with "Sessions" as a page kicker `<p class="sg-page-kicker">`) |
| `user_sessions_live.ex` | Revoke confirm copy (`:206`, `:209`) | `They can sign in again` | Rephrase to preserve security-remediation posture without undermining urgency — the copy must convey consequence + reversibility, not minimize the action |
| `organization_live.ex` | Members empty-state (`:96`) | Bare `<p class="sg-section-copy">` with invite prompt | Replace bare `<p>` with `<.empty_state>` component; the action link to the invite flow must be present (not a dead-end) |
| `organization_live.ex` | Pending invitations empty-state (`:117`) | Bare `<p class="sg-section-copy">` | Replace bare `<p>` with `<.empty_state>` component |
| `branding_live.ex` | Scope ribbon (`:106`) | Hardcoded literal `"Global auth/email profile"` | Replace with computed `scope_copy/1` helper, consistent with every other page |

### Cross-Page Composition Fixes

| Surface | Asymmetry | Required Alignment |
|---------|-----------|-------------------|
| `audit_index_live.ex` | `scope_ribbon` renders **below** header (`:56`) | Move `scope_ribbon` to render **above** the header, matching sibling pages |
| `users_index_live.ex` | Applied chips inside `<form>` (`:107–114`) | Canonical: chips inside `<form>` per `admin-design-contract.md` List Archetype D-01 — this is already correct; confirm no regression |
| `audit_index_live.ex` / `audit_user_live.ex` | Applied chips outside `<form>` (`:142–149`, `:163–172`) | Canonical: post-form position is the Audit Explorer Archetype's defined pattern (D-03 in contract) — verify the asymmetry is intentional per D-03, or align to one canonical position with a documented rationale |

### Standard Admin Copy Patterns

| Element | Pattern |
|---------|---------|
| Empty state heading | Noun phrase stating the absent thing: "No active sessions", "No organizations", "No audit activity" |
| Empty state body | One sentence max. State what the absence means + optional next step: "This user has no active sessions in the current scope." |
| Destructive confirm heading | Question form stating the action: "Restore defaults?", "Revoke session?" |
| Destructive confirm body | One sentence consequence + one sentence reversibility: "This removes all configured branding overrides. This action cannot be undone." |
| Destructive confirm CTA | Exact action verb matching the button that opened the dialog: "Restore defaults", "Revoke" |
| Destructive cancel | Always: "Cancel" (exact, no variation) |
| Scope ribbon | Use `scope_copy/1` helper output — never hardcode literal strings |
| Kicker label | Noun identifying the resource type in title case: "User", "User operations", "Branding", "Audit" |
| H1 page title | Entity name or page noun. On leaf screens: display name or email. On list screens: resource type (e.g. "Users") |

### Glossary Enforcement

All new and changed copy in Phase 209 remediations must pass `glossary_test.exs` (auto-enforced on CI via `mix test`). Do not introduce banned terms. Do not use "account" as a synonym for "user" in admin UI copy.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project uses `sg-*` hand-authored CSS, not shadcn |
| Third-party | none | not applicable |

No new component registries are introduced in Phase 209. All component usage routes through `Sigra.Admin.Components` (`components.ex`) and the existing `sg-*` CSS layer.

---

## Interaction Contracts

### Confirmed Interaction Patterns (do not change in Phase 209)

| Pattern | Contract |
|---------|----------|
| Applied chips | Navigation-only `<a>` tags; no `phx-click`; GET form contract preserved |
| Filter panel | `<form method="get">` only; `handle_params/3` is the sole state path |
| Advanced filters | CSS-only `<details><summary>More filters</summary>` — no LiveView round-trip |
| Tab navigation (branding) | LiveView `patch` navigation via `?panel=` URL param; NOT JS toggle |
| ConfirmDialog | `phx-hook="ConfirmDialog"` + `data-sg-confirm-cancel` initial focus; 7 APG gates enforced |
| Session revoke confirm | Lives on `UserSessionsLive`, not `user_show_live.ex` — do not re-add confirm overlay to detail page |
| Scope ribbon position | Above the page `<header>` on every list/leaf page; NOT below (fixing `audit_index_live.ex`) |
| Empty state component | Always `<.empty_state>` — never bare `<p class="sg-section-copy">` as empty state |

### Motion Contract (unchanged from prior phases)

| Motion type | Spec |
|-------------|------|
| Button hover | `translateY(-1px)` + `box-shadow` at 140ms `var(--sg-ease)` — pointer devices only |
| Chip hover | `transition: var(--sg-transition-tone)` at 140ms — color change only |
| No-animate path | Filter-apply, chip-remove, table/list state changes, keyboard navigation |
| Reduced motion | `prefers-reduced-motion: reduce` strips all movement via universal rule at `app.css:1463–1473` |
| Forbidden | `transition: all` — never use |

### Baseline Recapture Discipline

| Rule | Contract |
|------|----------|
| Platform | CI-native ubuntu ONLY — never darwin (platform-pinned baselines, no OS token in `pathTemplate`) |
| Allowlist discipline | Add slug(s) before recapture → clear both allowlists before phase close |
| Canary | `impersonation-banner` canary re-designated (D-10): re-baseline with documented WCAG contrast-fix rationale; do NOT revert the WCAG fix |
| Checkpoint slugs | 9 slugs: `audit-explorer`, `global-overview`, `global-user-index`, `impersonation-banner`, `org-overview`, `org-scoped-admin`, `user-audit`, `user-detail`, `user-sessions` |
| Branding | No checkpoint slug for branding — evidence is modal-interaction spec, not screenshot |
| Monotonic guard | `scripts/ci/quality-ledger-monotonic.sh --base origin/main` must exit 0 after all remediations |
| User-sessions tier | Do NOT ratchet `user-sessions` to Tier-2 in Phase 209 — Tier-2 elevation is Phase 210 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
