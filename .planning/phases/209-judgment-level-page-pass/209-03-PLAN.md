---
phase: 209-judgment-level-page-pass
plan: 03
type: execute
wave: 2
depends_on: [209-02]
files_modified:
  - lib/sigra/admin/live/index_live.ex
  - lib/sigra/admin/live/organization_live.ex
autonomous: true
requirements: [PAGE-02]
must_haves:
  truths:
    - "Neither Overview page renders a bare 'All clear' placeholder; both state a real posture (e.g. 'No flagged accounts')"
    - "The Global Overview no longer duplicates the 'Total users' metric that the Users-List strip owns"
    - "The Org Overview members + pending-invitations empty-states use <.empty_state>, not bare <p class=\"sg-section-copy\">"
    - "The members empty-state is not a dead-end (has an action link) OR carries a documented Waiver rationale"
  artifacts:
    - "lib/sigra/admin/live/index_live.ex (remediated)"
    - "lib/sigra/admin/live/organization_live.ex (remediated)"
  key_links:
    - "organization_live empty-states -> <.empty_state> component (components.ex:410)"
    - "index_live / organization_live 'All clear' -> posture-descriptive copy (glossary-clean)"
---

<objective>
Remediate the two Overview LiveViews' `actionable` verdicts from the Wave-1 panel: kill the bare "All clear" placeholder on both (index_live:52 + organization_live:69 NEW-1), de-duplicate the "Total users" metric (Overview owns it; Users-List strip is the partner — remove/reframe the Overview copy per the panel verdict), and swap the org members/pending empty-states from bare `<p>` to `<.empty_state>` (resolving the members dead-end per D-04/OQ-1).

Purpose: PAGE-02 / SC-2 for the Overview cluster. All edits are in-place in library-owned single-source LiveViews (RESEARCH Canonical Source: NO generated copy, NO install_golden regen for these files). Per D-06.
Output: 2 remediated LiveViews. Touches checkpoint slugs `global-overview` + `org-overview` (recaptured in Plan 06, not here).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-judgment-level-page-pass/209-CONTEXT.md
@.planning/phases/209-judgment-level-page-pass/209-RESEARCH.md
@.planning/phases/209-judgment-level-page-pass/209-UI-SPEC.md
@.planning/uat-evidence/v1.42-persona-jtbd/index-live.md
@.planning/uat-evidence/v1.42-persona-jtbd/organization-live.md
@lib/sigra/admin/components.ex
@guides/reference/admin-glossary.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: index_live — kill bare "All clear" + de-duplicate Total-users</name>
  <files>lib/sigra/admin/live/index_live.ex, .planning/uat-evidence/v1.42-persona-jtbd/index-live.md</files>
  <read_first>
    - `.planning/uat-evidence/v1.42-persona-jtbd/index-live.md` — the authored panel verdicts for this surface (the authoritative worklist for THIS remediation; the diff must resolve each non-keep verdict).
    - `lib/sigra/admin/live/index_live.ex:52` — the bare "All clear" in the `else` of the zero-risk notice; `:88`/`:90` — `label="Total users"` + `value_suffix="total users"`.
    - `lib/sigra/admin/live/users_index_live.ex:188` — the duplication partner `label="Total users"` (do NOT edit users_index in this plan; it belongs to Plan 05 confirmation — just understand which side the panel says to keep).
    - UI-SPEC Copywriting Contract row (index_live `:52`): replace bare "All clear" with a posture-descriptive phrase stating system state ("No flagged accounts" or similar); UI-SPEC "Status signal rule" — emit only decision-bearing signals.
    - `guides/reference/admin-glossary.md` — banned terms; do not use "account" as a synonym for "user".
  </read_first>
  <action>
    In `lib/sigra/admin/live/index_live.ex`: replace the bare `All clear` alarm copy (:52) with a posture-descriptive phrase that states the real system state per the panel verdict + UI-SPEC (e.g. "No flagged accounts" / "No accounts need attention" — pick the exact wording from the panel doc's finding, keep it glossary-clean, do not use "account" as a synonym for "user"). Resolve the "Total users" duplication per the panel verdict: the panel identifies Overview:88 and Users-List:188 as duplicate metrics — apply the panel's chosen resolution on the Overview side (remove or reframe the Overview "Total users" stat so it does not restate what the Users-List strip owns). Do NOT introduce new banned synonyms. Do NOT touch users_index_live.ex here.

    Append a resolution note to the index-live panel doc (`.planning/uat-evidence/v1.42-persona-jtbd/index-live.md`) for each resolved verdict: the commit ref / diff summary OR `Waiver: <rationale>` (D-07). No unresolved actionable verdict may remain.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && ! grep -n 'All clear' lib/sigra/admin/live/index_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `index_live.ex` no longer contains the bare literal "All clear".
    - The Overview "Total users" duplication is resolved per the panel verdict (removed/reframed).
    - `glossary_test.exs` passes; monotonic guard exits 0.
    - The index-live panel doc carries a resolution note (diff ref or Waiver) for each actionable verdict.
  </acceptance_criteria>
  <done>Global Overview alarm states a real posture and no longer duplicates the Total-users metric; verdicts resolved in the panel doc.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: organization_live — kill bare "All clear" (NEW-1) + swap empty-states to <.empty_state></name>
  <files>lib/sigra/admin/live/organization_live.ex, .planning/uat-evidence/v1.42-persona-jtbd/organization-live.md</files>
  <read_first>
    - `.planning/uat-evidence/v1.42-persona-jtbd/organization-live.md` — authored panel verdicts for this surface (authoritative worklist).
    - `lib/sigra/admin/live/organization_live.ex:69` — bare "All clear" (NEW-1, same bug as index:52); `:95` bare `<p class="sg-section-copy">` members empty-state; `:96` members copy dead-end (no invite link); `:117` bare `<p>` pending-invitations empty-state; `:60` comment documenting scope_ribbon intentional omission (waiver anchor — do NOT add a scope_ribbon).
    - `lib/sigra/admin/components.ex:410` — `<.empty_state>` signature: required `title` attr + `inner_block` slot for body/CTA. Swap is to an EXISTING component (no component build).
    - RESEARCH OQ-1 — there is NO admin-pipeline invite route (only host-owned `/members` under RequireMembership; public `/invitations/:token/accept`). Recommendation: resolve the members-empty-state "action link" clause as a documented `Waiver: admin overview is read-only; invitation creation lives on the member self-service surface` UNLESS a clean in-pipeline target exists. The `<.empty_state>` component swap still lands regardless.
    - UI-SPEC Standard Admin Copy Patterns — empty-state heading = noun phrase of the absent thing; body one sentence.
  </read_first>
  <action>
    In `lib/sigra/admin/live/organization_live.ex`: (1) replace the bare "All clear" (:69) with the same posture-descriptive treatment used on index_live (NEW-1 — fix both Overviews consistently so the panel's coherence lens is satisfied). (2) Replace the members empty-state bare `<p class="sg-section-copy">` (:95-96) with `<.empty_state>` (title = noun phrase of the absent thing, body one sentence per UI-SPEC). For the members dead-end: if a clean in-pipeline action target exists, add it as the `<.empty_state>` action; otherwise resolve the action-link clause as a documented `Waiver:` in the panel doc with the read-only-overview rationale (OQ-1) — the component swap lands either way. (3) Replace the pending-invitations bare `<p>` (:117) with `<.empty_state>`. Do NOT add a scope_ribbon (:60 documents its intentional omission — that stays a waiver). Keep all copy glossary-clean.

    Append resolution notes to `.planning/uat-evidence/v1.42-persona-jtbd/organization-live.md` for each resolved verdict (diff ref or `Waiver: <rationale>`).
  </action>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && ! grep -n 'All clear' lib/sigra/admin/live/organization_live.ex && ! grep -nE '<p class="sg-section-copy">[^<]*(No members yet|No pending invitations)' lib/sigra/admin/live/organization_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `organization_live.ex` no longer contains bare "All clear" and no longer uses bare `<p class="sg-section-copy">` for the members/pending empty-states.
    - Both empty-states render via `<.empty_state>`; the members dead-end is resolved (action link OR documented Waiver in the panel doc).
    - scope_ribbon remains intentionally omitted (waiver at :60 preserved — not added).
    - `glossary_test.exs` passes; monotonic guard exits 0; panel doc carries resolution notes.
  </acceptance_criteria>
  <done>Org Overview alarm states a real posture, both empty-states use the correct component, and the members dead-end is resolved; verdicts resolved in the panel doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operator -> admin Overview pages | Read-only overview surfaces; copy/IA edits must not alter auth-scoping or add a write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-03-01 | Elevation of Privilege | org members empty-state action link | low | mitigate | Do NOT add a net-new admin invite route/page (D-06). If an action link is added it targets an EXISTING route only; otherwise the clause is waived (OQ-1). No new write path or permission is introduced on the read-only overview. |
| T-209-03-02 | Information Disclosure | posture copy replacing "All clear" | low | accept | New copy states aggregate posture ("No flagged accounts") without leaking individual account identifiers; no PII surfaced that the page did not already show. |
| T-209-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — Elixir/HEEx source edits only, reusing the existing `<.empty_state>` component. |
</threat_model>

<verification>
- `glossary_test.exs` green (copy remains banned-term-free).
- `quality-ledger-monotonic.sh --base origin/main` exits 0 (no Tier-2 regression).
- Grep gates confirm removal of bare "All clear" and bare `<p>` empty-states.
- Each resolved actionable verdict has a resolution note (diff or Waiver) in its panel doc.
</verification>

<success_criteria>
- Both Overview `actionable` verdicts resolved in-place (or documented waiver); no Tier-2 regression; copy glossary-clean.
</success_criteria>

<output>
Create `.planning/phases/209-judgment-level-page-pass/209-03-SUMMARY.md` when done.
</output>
