---
phase: 209-judgment-level-page-pass
plan: 04
type: execute
wave: 2
depends_on: [209-02]
files_modified:
  - lib/sigra/admin/live/user_show_live.ex
  - lib/sigra/admin/live/user_sessions_live.ex
  - lib/sigra/admin/live/branding_live.ex
autonomous: true
requirements: [PAGE-02]
must_haves:
  truths:
    - "User Detail no longer duplicates the sessions-count between the header facts and the Sessions panel sub-heading"
    - "'Manage sessions' is no longer an easily-missed secondary button; its prominence matches its importance per the panel verdict"
    - "User Sessions uses an entity-name H1 with 'Sessions' as a kicker, matching sibling detail pages"
    - "Revoke confirm copy conveys consequence + reversibility without minimizing the action (security-remediation posture preserved)"
    - "Branding scope ribbon is computed via a scope_copy/1 helper, not a hardcoded literal"
  artifacts:
    - "lib/sigra/admin/live/user_show_live.ex (remediated)"
    - "lib/sigra/admin/live/user_sessions_live.ex (remediated — copy/IA ONLY, no tier ratchet)"
    - "lib/sigra/admin/live/branding_live.ex (remediated — adds context-appropriate scope_copy/1)"
  key_links:
    - "branding_live scope_ribbon -> new defp scope_copy/1 (NEW-2: branding had none)"
    - "user_sessions H1 -> entity-name pattern shared with sibling detail pages"
---

<objective>
Remediate the User Detail, User Sessions, and Branding LiveViews' `actionable` verdicts from the Wave-1 panel: de-duplicate the User-Detail sessions count, raise "Manage sessions" prominence, unify the 4 empty_state copies + terse kicker; fix the User-Sessions H1 heading hierarchy and revoke-confirm copy (copy/IA ONLY — NO tier ratchet, D-08); replace the Branding hardcoded scope_ribbon literal with a new context-appropriate `scope_copy/1` helper (NEW-2 — branding has none).

Purpose: PAGE-02 / SC-2 for the detail/sessions/branding cluster. In-place library-owned single-source edits (RESEARCH: no generated copy). Per D-06. The user_sessions security-remediation posture must be PRESERVED (threat model below).
Output: 3 remediated LiveViews. Touches slugs `user-detail` + `user-sessions` (recaptured in Plan 06). Branding has NO checkpoint slug (D-09).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-judgment-level-page-pass/209-CONTEXT.md
@.planning/phases/209-judgment-level-page-pass/209-RESEARCH.md
@.planning/phases/209-judgment-level-page-pass/209-UI-SPEC.md
@.planning/uat-evidence/v1.42-persona-jtbd/user-show-live.md
@.planning/uat-evidence/v1.42-persona-jtbd/user-sessions.md
@.planning/uat-evidence/v1.42-persona-jtbd/branding-live.md
@lib/sigra/admin/components.ex
@guides/reference/admin-glossary.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: user_show_live — de-dup sessions count, raise "Manage sessions", unify empty-states + kicker</name>
  <files>lib/sigra/admin/live/user_show_live.ex, .planning/uat-evidence/v1.42-persona-jtbd/user-show-live.md</files>
  <read_first>
    - `.planning/uat-evidence/v1.42-persona-jtbd/user-show-live.md` — authored panel verdicts (authoritative worklist).
    - `lib/sigra/admin/live/user_show_live.ex:57` sessions count in `<dl class="sg-summary-facts">`; `:84` Sessions-panel sub-heading count (duplicate of :57); `:86-88` "Manage sessions" secondary button; `:115/:146/:182/:202` four `<.empty_state>` copies (sessions/identities/orgs/audit); `:47` kicker "User".
    - UI-SPEC Copywriting Contract + Standard Admin Copy Patterns: empty-state heading = noun phrase; kicker = title-case resource noun; H1 = entity name.
    - UI-SPEC Interaction Contract: "Session revoke confirm lives on UserSessionsLive, not user_show_live.ex — do not re-add confirm overlay to detail page." Do NOT add a confirm overlay here.
  </read_first>
  <action>
    In `lib/sigra/admin/live/user_show_live.ex`, apply the panel's resolutions: (1) remove the duplicate sessions-count — keep it in ONE place per the panel verdict (the panel identifies :57 header-fact vs :84 panel sub-heading as the duplicate pair; drop the redundant instance). (2) Raise "Manage sessions" (:86) prominence per the panel verdict so it is not an easily-missed secondary button (e.g. promote placement/affordance — follow the panel's chosen remediation; do NOT add a confirm overlay, that lives on UserSessionsLive). (3) Unify the 4 empty_state copies (:115/:146/:182/:202) to the consistent noun-phrase pattern from UI-SPEC (each states the absent thing; one-sentence body). (4) Sharpen the terse kicker "User" (:47) per the panel/UI-SPEC kicker pattern. Keep all copy glossary-clean. Do NOT ratchet any tier.

    Append resolution notes (diff ref or Waiver) to the user-show-live panel doc for each actionable verdict (D-07).
  </action>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main</automated>
  </verify>
  <acceptance_criteria>
    - Sessions count appears once (duplicate removed); "Manage sessions" prominence raised per panel verdict; 4 empty-states use the consistent pattern; kicker sharpened.
    - No confirm overlay added to the detail page (stays on UserSessionsLive).
    - `glossary_test.exs` passes; monotonic guard exits 0; panel doc carries resolution notes.
  </acceptance_criteria>
  <done>User Detail is de-duplicated, empty-states are coherent, "Manage sessions" is discoverable, and the kicker is sharpened; verdicts resolved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: user_sessions_live — entity-name H1 + revoke-confirm copy (copy/IA ONLY, no tier ratchet)</name>
  <files>lib/sigra/admin/live/user_sessions_live.ex, .planning/uat-evidence/v1.42-persona-jtbd/user-sessions.md</files>
  <read_first>
    - `.planning/uat-evidence/v1.42-persona-jtbd/user-sessions.md` — authored panel verdicts (authoritative worklist).
    - `lib/sigra/admin/live/user_sessions_live.ex:107` kicker "User"; `:108` `<h1 class="sg-page-title">Sessions</h1>` (diverges from sibling entity-name H1); `:205` `revoke_session_copy` ("They can sign in again"); `:209` `revoke_all_sessions_copy`. NOTE: revoke copy anchor is `:205` (not :206 per RESEARCH row 14).
    - UI-SPEC Copywriting Contract: H1 -> entity-name (display name/email) with "Sessions" as kicker; revoke copy MUST "preserve security-remediation posture without undermining urgency — convey consequence + reversibility, not minimize the action."
    - D-08 + UI-SPEC Baseline Recapture Discipline: do NOT ratchet user-sessions to Tier-2 (Phase 210 owns that). This task is copy/IA only.
    - UI-SPEC Interaction Contract: ConfirmDialog 7 APG gates + `data-sg-confirm-cancel` initial focus + confirm-dialog ownership stay UNCHANGED — edit the copy string, not the dialog mechanics.
  </read_first>
  <action>
    In `lib/sigra/admin/live/user_sessions_live.ex`: (1) change the `<h1>Sessions` (:108) to the entity-name H1 pattern used by sibling detail pages (display name or email as H1), moving "Sessions" into the `<p class="sg-page-kicker">` (:107) — match the sibling hierarchy exactly. (2) Rephrase the revoke-confirm copy at `:205` and `:209` so it conveys BOTH the consequence AND reversibility WITHOUT minimizing the action (per UI-SPEC + the security threat model below) — do not weaken the revoke UX; the goal is a clearer security-remediation posture, not softer language. Keep glossary-clean.

    Do NOT change the ConfirmDialog hook, focus target, APG gates, or confirm-dialog ownership. Do NOT ratchet the ledger tier for this page (D-08). Append resolution notes to the user-sessions panel doc.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && ! grep -nE '<h1 class="sg-page-title">Sessions</h1>' lib/sigra/admin/live/user_sessions_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - The literal `<h1 class="sg-page-title">Sessions</h1>` is gone; H1 is the entity name with "Sessions" as kicker.
    - Revoke-confirm copy at :205/:209 conveys consequence + reversibility without minimizing the action; ConfirmDialog mechanics/APG gates unchanged.
    - `user-sessions` ledger tier is NOT ratcheted (still Tier-1); monotonic guard exits 0.
    - `glossary_test.exs` passes; panel doc carries resolution notes.
  </acceptance_criteria>
  <done>User Sessions has a sibling-consistent entity-name H1 and a security-preserving revoke-confirm copy — copy/IA only, no tier change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: branding_live — replace hardcoded scope_ribbon literal with a context-appropriate scope_copy/1</name>
  <files>lib/sigra/admin/live/branding_live.ex, .planning/uat-evidence/v1.42-persona-jtbd/branding-live.md</files>
  <read_first>
    - `.planning/uat-evidence/v1.42-persona-jtbd/branding-live.md` — authored panel verdicts (authoritative worklist).
    - `lib/sigra/admin/live/branding_live.ex:106` `<.scope_ribbon copy="Global auth/email profile" />` hardcoded literal; RESEARCH NEW-2 + OQ-2: branding has NO `defp scope_copy/1`; the 5 sibling pages that do return DIVERGENT strings ("Global user operations" vs "Global audit explorer"), so this is NOT a drop-in reuse — the fix must add a branding-context helper.
    - A sibling page's `defp scope_copy/1` (e.g. in `lib/sigra/admin/live/audit_index_live.ex` or `user_show_live.ex`) to mirror the shape (arity, scope-argument pattern, return-copy convention) — read one to match the pattern.
    - UI-SPEC Copywriting Contract: "Scope ribbon — use `scope_copy/1` helper output — never hardcode literal strings." Confirm the desired branding-context string(s) with the branding panel doc / UI-SPEC.
    - `lib/sigra/admin/components.ex:461,465` — `<.scope_ribbon>` requires the `:copy` attr.
  </read_first>
  <action>
    In `lib/sigra/admin/live/branding_live.ex`: add a `defp scope_copy/1` mirroring the sibling pattern but returning a branding-appropriate computed copy per scope (global auth/email profile for the global scope; the org-scoped variant if branding renders under an org scope — confirm the exact string from the panel doc / UI-SPEC copy contract). Replace the hardcoded `<.scope_ribbon copy="Global auth/email profile" />` (:106) with `<.scope_ribbon copy={scope_copy(@scope_or_assign)} />` (use whatever scope assign the page already carries — match the sibling call site's argument). Do NOT introduce a shared components.ex helper (per RESEARCH, `scope_copy/1` is intentionally a per-page `defp` with divergent copy). Keep glossary-clean.

    Note: branding has NO checkpoint slug (D-09) — this change is covered by the modal-interaction + glossary evidence, not a screenshot; it does not require a Plan-06 recapture. Append a resolution note to the branding panel doc.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && grep -qn 'defp scope_copy' lib/sigra/admin/live/branding_live.ex && ! grep -nE 'scope_ribbon copy="Global auth/email profile"' lib/sigra/admin/live/branding_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `branding_live.ex` defines a `defp scope_copy/1` and the scope_ribbon uses its computed output — the hardcoded literal is gone.
    - The helper returns branding-context copy (not blindly reused sibling copy — NEW-2).
    - No shared components.ex helper added; `glossary_test.exs` passes; monotonic guard exits 0; panel doc carries a resolution note.
  </acceptance_criteria>
  <done>Branding routes its scope ribbon through a context-appropriate computed helper, consistent with the design contract; verdict resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operator -> session revocation | The revoke-confirm copy is the last human checkpoint before an irreversible-in-the-moment security action (killing a session). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-04-01 | Repudiation / Tampering | user_sessions revoke-confirm copy (:205/:209) | medium | mitigate | Rephrase MUST convey consequence + reversibility WITHOUT minimizing the action (UI-SPEC L156). Do not weaken the revoke UX or soften urgency. ConfirmDialog hook, `data-sg-confirm-cancel` focus, and the 7 APG gates are left byte-unchanged — copy string only. Verified by glossary + the existing admin-modal-interaction spec (unchanged). |
| T-209-04-02 | Elevation of Privilege | detail-page "Manage sessions" prominence | low | accept | Raising discoverability changes placement/affordance only; the target route + its existing auth-scoping are unchanged. No confirm overlay is re-added to the detail page (stays on UserSessionsLive per UI-SPEC). |
| T-209-04-03 | Tampering | branding scope_copy/1 | low | accept | New `defp` computes display copy only; it does not read secrets or alter the branding write path / auth-scoping. |
| T-209-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — Elixir/HEEx source edits only. |
</threat_model>

<verification>
- `glossary_test.exs` green across all 3 files.
- `quality-ledger-monotonic.sh --base origin/main` exits 0 (user-sessions NOT ratcheted; no Tier-2 regression).
- Grep gates confirm: H1 literal removed, branding literal removed + `defp scope_copy` present.
- Revoke-confirm copy reviewed against the security threat model (consequence + reversibility, not minimized) — panel doc records the rationale.
- Each actionable verdict has a resolution note (diff or Waiver) in its panel doc.
</verification>

<success_criteria>
- Detail/sessions/branding `actionable` verdicts resolved in-place; user-sessions stays Tier-1; revoke security posture preserved; copy glossary-clean.
</success_criteria>

<output>
Create `.planning/phases/209-judgment-level-page-pass/209-04-SUMMARY.md` when done.
</output>
