---
phase: 209-judgment-level-page-pass
plan: 06
type: execute
wave: 3
depends_on: [209-01, 209-03, 209-04, 209-05]
files_modified:
  - test/example/priv/playwright/snapshot-allowlist
  - test/example/priv/playwright/snapshot-allowlist-design
  - test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots
  - .planning/phases/209-judgment-level-page-pass/209-CANARY-REDESIGNATION.md
autonomous: true
requirements: [PAGE-02]
user_setup: []
must_haves:
  truths:
    - "Every checkpoint slug touched by the Wave-2 remediations is recaptured CI-native on ubuntu (never darwin)"
    - "The impersonation-banner canary is re-baselined (D-10 part 2) preserving the shipped WCAG contrast fix — byte-stable vs phase HEAD, with a documented rationale"
    - "The 200-204 legitimately-drifted slugs are allowlisted so PR #63 fast_checks snapshot-canary lane reads them as intended vs stale origin/main"
    - "Both allowlists are EMPTY at phase close; the canary is byte-stable; the monotonic guard exits 0"
  artifacts:
    - "recaptured checkpoint PNG baselines (touched slugs)"
    - ".planning/phases/209-judgment-level-page-pass/209-CANARY-REDESIGNATION.md (rationale artifact)"
  key_links:
    - "admin_checkpoint_recapture (Plan 01) -> recaptured baselines -> snapshot-canary-guard.sh green"
    - "snapshot-allowlist + snapshot-allowlist-design -> EMPTY at close"
---

<objective>
Close the phase's visual-baseline + integration-reconciliation obligations (SC-4 + D-10). Two DISTINCT base comparisons in one phase, kept separate: (A) the phase's OWN recaptures reconcile vs phase HEAD — recapture the checkpoint slugs touched by Wave-2 remediations CI-native on ubuntu (Plan 01's job), allowlist->recapture->clear; (B) the D-10 integration reconciliation vs the stale origin/main — allowlist the 4 legitimately-drifted 200-204 slugs so PR #63's fast_checks snapshot-canary lane reads them as intended, and re-designate/re-baseline the impersonation-banner canary preserving the shipped WCAG contrast fix (never revert it).

Purpose: PAGE-02 / SC-4. Per D-09 (ubuntu-native, platform-pinned baselines) + D-10 (user-ratified integration binding gate).
Output: recaptured touched checkpoint baselines; a canary re-designation rationale artifact; both allowlists empty at close; canary byte-stable; PR #63 fast_checks lane green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-judgment-level-page-pass/209-CONTEXT.md
@.planning/phases/209-judgment-level-page-pass/209-RESEARCH.md
@.planning/phases/209-judgment-level-page-pass/209-UI-SPEC.md
@.planning/todos/pending/2026-06-30-v142-integration-snapshot-canary-drift.md
@scripts/ci/snapshot-canary-guard.sh
@test/example/priv/playwright/snapshot-allowlist
@test/example/priv/playwright/snapshot-allowlist-design
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Recapture the touched checkpoint slugs CI-native on ubuntu (allowlist -> recapture -> clear)</name>
  <files>test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots</files>
  <read_first>
    - RESEARCH Sequencing & Slug-Impact Map — which remediation touches which checkpoint slug: index_live->`global-overview`; organization_live->`org-overview`; user_show->`user-detail`; user_sessions->`user-sessions`; audit_index->`audit-explorer`; audit_user->`user-audit` (only if audit_user DOM changed in Plan 05); users_index->`global-user-index` (only if it changed — expected NO); branding->NONE (no slug, D-09).
    - The Plan-01 `admin_checkpoint_recapture` job in `.github/workflows/ci.yml` — the CI-native ubuntu recapture mechanism (runs on push-to-main/dispatch; commits recaptured PNGs to `ci/recapture-admin-checkpoints-<run_id>` + opens a PR; re-establishes the impersonation-banner canary as 'added').
    - `scripts/ci/snapshot-canary-guard.sh` — the allowlist mechanics + `--require-all` (proves the intended slugs actually changed) + the canary-not-allowlistable rule.
    - `test/example/priv/playwright/snapshot-allowlist` — the checkpoint allowlist (canary banned). D-09 discipline: add intended slugs -> recapture on CI -> CLEAR before phase close.
    - RESEARCH Pitfall 3 (never `--update-snapshots` on darwin — byte-fails ubuntu CI) + Pitfall 5 (only DOM-changing remediations force a recapture; do not recapture D-05 untouched slugs).
  </read_first>
  <action>
    Determine the exact set of touched slugs from the Wave-2 SUMMARY files (only slugs whose page DOM/visible-copy actually changed). This task has TWO `<done>`-checkable steps that must not collapse into a no-op:

    **Step 1 — bring recaptured PNGs into the branch (intent-proven):** Add those intended checkpoint slugs to `test/example/priv/playwright/snapshot-allowlist` (one per line, hyphenated filename form; NEVER the `impersonation-banner` canary). Trigger the Plan-01 `admin_checkpoint_recapture` job on CI (push to a recapture-trigger branch or use the job's `github.event_name != 'pull_request'` dispatch path) so recapture runs ubuntu-native; the job opens a `ci/recapture-admin-checkpoints-<run_id>` PR with regenerated PNGs. Bring the recaptured baselines into the phase branch (merge/cherry-pick the recapture PR's PNG commit). BEFORE clearing the allowlist, run `snapshot-canary-guard.sh --base origin/main --require-all --allowlist test/example/priv/playwright/snapshot-allowlist --canary impersonation-banner` with the intended `--allow <slug>` args — `--require-all` FAILS unless every allowlisted slug actually changed, which proves the recapture landed (guards against a green-on-no-op). Additionally, `git diff --name-only origin/main...HEAD` MUST list a changed `*-admin-checkpoints-*.png` for each intended slug (the positive assertion the verify grep enforces).

    **Step 2 — clear allowlist + verify byte-stable vs HEAD:** Only after Step 1 proves the PNGs changed, CLEAR the checkpoint allowlist back to comments-only, then confirm `snapshot-canary-guard.sh --base HEAD` is byte-stable.

    Do NOT recapture branding (no slug). Do NOT recapture any D-05 untouched slug. Do NOT recapture on darwin.
  </action>
  <verify>
    <automated>git diff --name-only origin/main...HEAD | grep -qE '\-admin-checkpoints-(chromium|mobile|dark)\.png$' && bash scripts/ci/snapshot-canary-guard.sh --base HEAD && ! grep -qvE '^[[:space:]]*(#|$)' test/example/priv/playwright/snapshot-allowlist</automated>
  </verify>
  <acceptance_criteria>
    - **Step 1 (recapture landed, not a no-op):** at least one `*-admin-checkpoints-*.png` baseline for the intended touched slugs is changed in the phase branch vs `origin/main` (positive `git diff` assertion); the pre-clear `--require-all --allow <slug>` guard run proved every intended slug actually changed. A no-op tree (no recapture) FAILS this criterion.
    - Every touched checkpoint slug is recaptured ubuntu-native (via the Plan-01 job), baselines committed to the phase branch.
    - Branding + D-05 untouched slugs are NOT recaptured.
    - **Step 2 (clear + verify):** `snapshot-canary-guard.sh --base HEAD` passes (no un-allowlisted drift, canary byte-stable) with the checkpoint allowlist CLEARED (comments-only) at task close.
  </acceptance_criteria>
  <done>Step 1: the phase's touched checkpoint baselines are provably regenerated in the branch (git-diff positive + `--require-all` intent proof). Step 2: the allowlist is cleared and the tree is byte-stable vs phase HEAD — the task cannot report done on an untouched tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-designate the impersonation-banner canary + write the rationale artifact (D-10 part 2)</name>
  <files>.planning/phases/209-judgment-level-page-pass/209-CANARY-REDESIGNATION.md, test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots</files>
  <read_first>
    - `.planning/todos/pending/2026-06-30-v142-integration-snapshot-canary-drift.md` — the folded D-10 scope: Phase 204-03 intentionally modified `impersonation-banner-mobile` as part of a WCAG >=4.5:1 contrast fix (STATE:180), which collides with the canary "must stay byte-green" rule.
    - `scripts/ci/snapshot-canary-guard.sh` — canary rule: `added` (first-establishment) OK; `modified`/`deleted`/`mixed` FAIL; canary is NOT allowlistable (Pitfall 4).
    - The Plan-01 recapture job's canary re-establish step (delete `impersonation-banner-admin-checkpoints-*.png` before `--update-snapshots` so it recaptures as `added`, not `modified`).
    - RESEARCH Pitfall 4 — do NOT allowlist the canary and do NOT revert the WCAG fix; re-baseline is the only resolution.
  </read_first>
  <action>
    Confirm the impersonation-banner canary was re-established as `added` by the Plan-01 recapture job (delete-before-recapture), preserving the shipped Phase 204-03 WCAG contrast fix — NEVER revert the accessibility fix, NEVER add the canary to any allowlist. Write `.planning/phases/209-judgment-level-page-pass/209-CANARY-REDESIGNATION.md` documenting: (1) the original canary purpose (byte-green tripwire), (2) why it was legitimately modified (204-03 WCAG >=4.5:1 contrast fix — cite STATE:180), (3) the resolution (re-baseline as `added`, tripwire re-armed for future PRs), (4) explicit statement that the WCAG fix was preserved and the canary was NOT allowlisted. After re-baselining, the canary is byte-stable vs phase HEAD.
  </action>
  <verify>
    <automated>test -f .planning/phases/209-judgment-level-page-pass/209-CANARY-REDESIGNATION.md && grep -qi 'wcag' .planning/phases/209-judgment-level-page-pass/209-CANARY-REDESIGNATION.md && bash scripts/ci/snapshot-canary-guard.sh --base HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Canary re-established as `added` (not `modified`), WCAG contrast fix preserved, canary NOT allowlisted.
    - `209-CANARY-REDESIGNATION.md` documents purpose, the legitimate 204-03 modification, the re-baseline resolution, and the preservation statement.
    - `snapshot-canary-guard.sh --base HEAD` passes (canary byte-stable vs phase HEAD).
  </acceptance_criteria>
  <done>The canary is re-designated with a documented rationale, byte-stable vs phase HEAD, WCAG fix intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: D-10 integration reconciliation vs stale origin/main + final allowlist-clear close</name>
  <files>test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/snapshot-allowlist-design</files>
  <read_first>
    - `.planning/todos/pending/2026-06-30-v142-integration-snapshot-canary-drift.md` — the 4 legitimately-drifted slugs (`audit-explorer`, `user-audit`, `global-user-index`, `org-scoped-admin`) accumulated across Phases 200-204 vs the stale origin/main integration base; plus the design board-* baselines if the design-lane guard needs them.
    - RESEARCH Sequencing "Two distinct base comparisons in ONE phase" — the phase-own recaptures reconcile vs phase HEAD (done in Task 1); the D-10 reconciliation is vs origin/main; a slug touched by BOTH (e.g. audit-explorer, user-audit, global-user-index) must have the phase-own recapture LAST so the final committed baseline is the phase's, and the origin/main allowlisting is layered on top.
    - `ci.yml` fast_checks snapshot-canary lane (the PR #63 red lane) + the design-lane invocation (`--allowlist ...-design`, `--canary board-notice`).
    - Both allowlist files — must end EMPTY (comments-only) at phase close (D-09).
  </read_first>
  <action>
    Verify the D-10 integration reconciliation against the stale `origin/main`: run `snapshot-canary-guard.sh --base origin/main` (checkpoint lane) and confirm the 4 drifted 200-204 slugs plus any Phase-209 touched slugs are accounted for — the phase's recaptured baselines supersede the stale origin/main versions, so once the phase's PNG commits land the drift resolves without needing a permanent allowlist entry. If the guard still reports a drifted slug vs origin/main that is legitimate and NOT superseded by a phase recapture, allowlist it transiently to confirm intent, then confirm the phase's committed baselines make it byte-stable. Do the SAME check for the design lane (`--base origin/main --allowlist ...-design --canary board-notice`) if the design board-* baselines drifted.

    Perform the phase-own recaptures LAST (already done in Task 1) so the final committed baseline wins for any slug in both sets. Then CLEAR BOTH allowlists to comments-only. Confirm PR #63's `fast_checks` snapshot-canary lane goes green (the D-10 net effect). Record the integration-reconciliation outcome in the phase SUMMARY.
  </action>
  <verify>
    <automated>bash scripts/ci/snapshot-canary-guard.sh --base origin/main && ! grep -qvE '^[[:space:]]*(#|$)' test/example/priv/playwright/snapshot-allowlist && ! grep -qvE '^[[:space:]]*(#|$)' test/example/priv/playwright/snapshot-allowlist-design && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main</automated>
  </verify>
  <acceptance_criteria>
    - `snapshot-canary-guard.sh --base origin/main` passes for both checkpoint + design lanes (the 200-204 drift + Phase-209 recaptures reconcile; canary byte-stable).
    - BOTH allowlists are EMPTY (comments-only) at phase close.
    - `quality-ledger-monotonic.sh --base origin/main` exits 0 (no Tier-2 regression; user-sessions still Tier-1).
    - PR #63 `fast_checks` snapshot-canary lane is green (D-10 net effect) — recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The 200-204 integration drift + the impersonation-banner canary are reconciled vs stale origin/main, both allowlists are empty, the canary is byte-stable, and PR #63's fast_checks lane is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline PNGs -> snapshot-canary guard | Recaptured baselines + allowlist entries feed the guard that protects against unintended visual regressions and canary tampering. |
| CI recapture PR -> phase branch | Regenerated baselines cross from an automated recapture branch into the phase branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-06-01 | Tampering | impersonation-banner canary re-baseline | medium | mitigate | Canary re-established ONLY via delete-before-recapture ('added' path), never allowlisted, never reverting the WCAG fix; rationale documented in `209-CANARY-REDESIGNATION.md`; guard re-arms the tripwire for future PRs. |
| T-209-06-02 | Tampering | allowlist entries left populated | low | mitigate | Both allowlists MUST end comments-only at close; the verify greps fail if any non-comment line remains, and the guard runs vs both HEAD and origin/main. |
| T-209-06-03 | Repudiation | recaptured baselines merged without review | low | accept | The Plan-01 job opens a reviewable PR (branch-protected) rather than pushing to main; baseline changes are auditable. |
| T-209-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs — the recapture reuses the existing pinned Playwright toolchain in the Plan-01 job. |
</threat_model>

<verification>
- `snapshot-canary-guard.sh --base HEAD` (phase-own) AND `--base origin/main` (integration) both pass, checkpoint + design lanes.
- Both allowlists empty (comments-only) — verify greps enforce this.
- `quality-ledger-monotonic.sh --base origin/main` exits 0.
- `209-CANARY-REDESIGNATION.md` exists with the WCAG-preservation rationale.
- PR #63 `fast_checks` snapshot-canary lane green (recorded in SUMMARY).
</verification>

<success_criteria>
- SC-4: touched checkpoint baselines recaptured under allowlist->clear; both allowlists empty; canary byte-stable; D-10 integration drift + canary reconciled vs origin/main; PR #63 fast_checks green.
</success_criteria>

<output>
Create `.planning/phases/209-judgment-level-page-pass/209-06-SUMMARY.md` when done.
</output>