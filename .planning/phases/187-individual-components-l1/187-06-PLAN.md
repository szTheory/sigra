---
phase: 187-individual-components-l1
plan: 06
type: execute
wave: 6
depends_on:
  - 187-05
files_modified:
  - lib/sigra/admin/components.ex
  - test/sigra/admin/components_test.exs
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - test/example/priv/static/assets/css/app.css
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
user_setup: []
must_haves:
  truths:
    - "skeleton is decorative, shape-matched, and reduced motion leaves a static block."
    - "skeleton loading evidence places `aria-busy=\"true\"` on the containing region, not on the skeleton itself."
    - "audit_row uses sg-list-row, sg-status-pill, optional sg-code, and tone cues beyond color."
    - "audit_row tones for success/info/risk are visible and responsive without clipping at required widths."
  artifacts:
    - "design_gallery_live.ex has complete matrices for board-skeleton and board-audit_row."
    - "sigra_admin.css contains shipped sg-skeleton, sg-list-row, sg-status-pill, and sg-code styling."
    - "admin-quality-ledger.md rows for skeleton and audit_row carry board evidence."
  key_links:
    - "skeleton reduced-motion CSS -> Playwright reduced-motion assertion."
    - "audit_row tone mapping in components.ex -> status pill/code CSS -> board-audit_row evidence."
---

<objective>
Audit and improve loading and audit-event components.

Purpose: Bring `skeleton` and `audit_row` through L1 with reduced-motion proof, ARIA-busy container evidence, status cues beyond color, shipped CSS parity, axe, responsive checks, and ledger evidence.
Output: Updated loading/audit state matrices, CSS parity, focused assertions, and quality ledger rows for the last two components.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-individual-components-l1/187-CONTEXT.md
@.planning/phases/187-individual-components-l1/187-UI-SPEC.md
@.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-quality-ledger.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete skeleton and audit_row states</name>
  <files>lib/sigra/admin/components.ex, test/sigra/admin/components_test.exs, priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css, test/example/priv/static/assets/css/app.css, test/example/lib/example_web/live/admin/design_gallery_live.ex, test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - lib/sigra/admin/components.ex
    - test/sigra/admin/components_test.exs
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - guides/reference/admin-design-contract.md
    - .planning/phases/187-individual-components-l1/187-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `skeleton` board shows line, block, and card-shape examples, with a containing region carrying `aria-busy="true"`.
    - Test 2: reduced-motion mode computes no active skeleton shimmer movement.
    - Test 3: `audit_row` board shows success, info, and risk/failure rows, compact/full variants, optional code, and status pill cues beyond color.
    - Test 4: `audit_row` board does not horizontally overflow at the required responsive widths.
  </behavior>
  <action>Audit `skeleton` and `audit_row`. Keep skeleton markup decorative; the loading board must place `aria-busy="true"` on a containing region and not on each `.sg-skeleton`. Ensure `.sg-skeleton::after` shimmer uses transform only and reduced-motion leaves a static block by stripping animation. For `audit_row`, keep tone mapping in `components.ex`, preserve `sg-list-row`, `sg-status-pill`, and optional `code.sg-code`; ensure tone variants include non-color cues such as glyph, border, or text. Expand `board-skeleton` with line/block/card-shape examples and reduced-motion evidence labels. Expand `board-audit_row` with success, info, risk/failure, compact, full, and code-visible examples. Add Playwright assertions for `aria-busy`, reduced-motion skeleton animation, audit-row tone variants, status pill text, optional code visibility, and responsive fit. Sync CSS parity surfaces after any shipped CSS edit.</action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs</automated>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --grep "skeleton|audit_row|overflow|reduced motion"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `board-skeleton` contains an ancestor example with `aria-busy="true"` and `.sg-skeleton` itself does not receive `aria-busy`.
    - Behavior assertion: Playwright reduced-motion assertion verifies the skeleton shimmer is static or stripped.
    - Source assertion: `board-audit_row` includes success, info, and risk/failure examples.
    - Source assertion: `sg-status-pill` tone CSS includes a cue beyond color, such as `::before` content or equivalent text/glyph evidence.
    - Source assertion: no `transition: all` appears in `sigra_admin.css` or `app.css`.
  </acceptance_criteria>
  <done>`skeleton` and `audit_row` have complete shipped state matrices and reduced-motion/status evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Record loading/audit L1 evidence in the ledger</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md
    - guides/reference/admin-fractal-scorecard.md
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
  </read_first>
  <action>Update only the `skeleton` and `audit_row` L1 rows. Keep the tier column as a bare integer `1` or `2`; do not lower any tier. Evidence cells must name `board-skeleton`, `board-audit_row`, and `admin-design.spec.ts`; include `components_test.exs` only if markup/ARIA assertions changed.</action>
  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base HEAD</automated>
    <automated>for c in skeleton audit_row; do rg "^\\| ${c} \\| L1 \\| [12] \\|" guides/reference/admin-quality-ledger.md >/dev/null || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `skeleton` and `audit_row` ledger rows have bare integer tiers.
    - Source assertion: each evidence cell names its matching board id.
    - CLI assertion: the monotonic guard exits 0.
  </acceptance_criteria>
  <done>Loading/audit L1 scorecard evidence is recorded and monotonic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Loading state -> assistive technology | Skeleton visual loading blocks must not misrepresent busy state semantics. |
| Audit row data -> rendered component | Audit action labels, ids, and timestamps render through HEEx. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-16 | Denial of Service | `skeleton` animation | mitigate | Reduced-motion assertion strips shimmer movement; animation uses transform only. |
| T-187-17 | Information Disclosure / XSS | `audit_row` data fields | mitigate | Preserve HEEx escaping for action labels and code ids; do not add raw HTML. |
| T-187-18 | Spoofing | `audit_row` tone cues | mitigate | Tone must be conveyed beyond color through text/glyph/border evidence. |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run component goldens, install parity, golden diff, focused Playwright board tests, reduced-motion assertions, and monotonic guard.
</verification>

<success_criteria>
- `skeleton` and `audit_row` pass L1 state, reduced-motion, a11y, responsive, and axe checks.
- `skeleton` busy semantics are on the containing region.
- Ledger rows for the two components are monotonic and evidence-linked.
</success_criteria>

<output>
Create `.planning/phases/187-individual-components-l1/187-06-SUMMARY.md` when done.
</output>
