---
phase: 187-individual-components-l1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
  - .planning/phases/187-individual-components-l1/187-VALIDATION.md
  - lib/sigra/admin/components.ex
  - test/sigra/admin/components_test.exs
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - test/example/priv/playwright/tests/admin-design.spec.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
user_setup: []
must_haves:
  truths:
    - "D-08/D-10: The gallery and Playwright lane expose all 13 canonical component boards, including standalone board-notice_link."
    - "D-10: Component board overflow/clip/squish checks run at 320, 375, 768, 1024, and 1440 pixels before component tiers are raised."
    - "D-05/D-08: field_help and summary_chip help states have deterministic open-state evidence and Escape-close assertions."
    - "D-01: An exact CSS-block inventory exists before any component CSS migration starts."
  artifacts:
    - ".planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md records every migrated sg-* component block source range."
    - "test/example/priv/playwright/tests/admin-design.spec.ts contains board-notice_link, RESPONSIVE_WIDTHS, and help-state assertions."
    - "lib/sigra/admin/components.ex exposes default-false open attrs only for deterministic help evidence."
  key_links:
    - "design_gallery_live.ex board ids -> admin-design.spec.ts COMPONENT_BOARDS must include the same 13 L1 board ids."
    - "components.ex open attrs -> components_test.exs proves default closed HTML remains unchanged."
---

<objective>
Establish Phase 187 Wave 0 validation scaffolding before component-quality claims are made.

Purpose: Establish exact CSS migration evidence, per-component board attribution, deterministic help states, five-width responsive assertions, and runtime measurements required by 187-VALIDATION.md.
Output: CSS inventory, updated validation strategy, expanded gallery/test scaffolding, focused component tests for deterministic help state exposure, and Wave 0 rows 187-W0-01 through 187-W0-03 marked green when their evidence passes. Row 187-W0-04 remains pending for Plan 02's shipped-CSS migration proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/187-individual-components-l1/187-CONTEXT.md
@.planning/phases/187-individual-components-l1/187-RESEARCH.md
@.planning/phases/187-individual-components-l1/187-VALIDATION.md
@.planning/phases/187-individual-components-l1/187-PATTERNS.md
@.planning/phases/187-individual-components-l1/187-UI-SPEC.md
@AGENTS.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-fractal-scorecard.md
@guides/reference/admin-quality-ledger.md
@.planning/phases/185-audit-infrastructure/185-01-SUMMARY.md
@.planning/phases/185-audit-infrastructure/185-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exact component CSS inventory</name>
  <files>.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md</files>
  <read_first>
    - .planning/phases/187-individual-components-l1/187-CONTEXT.md
    - .planning/phases/187-individual-components-l1/187-RESEARCH.md
    - .planning/phases/187-individual-components-l1/187-PATTERNS.md
    - test/example/priv/static/assets/css/app.css
    - priv/templates/sigra.install/admin/sigra_admin.css
    - lib/sigra/admin/components.ex
  </read_first>
  <action>Create `.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md` with one `## component_name` heading for each canonical component: `stat`, `stat_link`, `task_card`, `summary_chip`, `applied_chip`, `empty_state`, `page_back`, `scope_ribbon`, `notice`, `notice_link`, `field_help`, `skeleton`, `audit_row`. Under each heading record the exact `test/example/priv/static/assets/css/app.css` line ranges and selectors that supply visual/state styling for that component, using the known selector families `sg-metric`, `sg-metric-link`, `sg-btn`, `sg-card-hover`, `sg-applied-chip`, `sg-empty-state`, `sg-scope-ribbon`, `sg-notice`, `sg-notice__action`, `sg-field-help`, `sg-skeleton`, `sg-list-row`, `sg-status-pill`, and `sg-code`. Include a `## Shared Blocks` section for blocks used by more than one component and a `## Migration Checklist` table with columns `Component`, `Selectors`, `Source Range`, `Target`, `Status`; initialize every status as `pending`.</action>
  <verify>
    <automated>test -f .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md && for c in stat stat_link task_card summary_chip applied_chip empty_state page_back scope_ribbon notice notice_link field_help skeleton audit_row; do rg "^## ${c}$" .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md >/dev/null || exit 1; done</automated>
    <automated>rg "sg-(metric|metric-link|btn|card-hover|applied-chip|empty-state|notice|notice__action|field-help|skeleton|list-row|status-pill|code)" .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `187-CSS-INVENTORY.md` has exactly 13 component headings and one `## Shared Blocks` heading.
    - Source assertion: every inventory row has a non-empty source range and target `priv/templates/sigra.install/admin/sigra_admin.css @layer sg-components`.
    - Source assertion: no inventory status is anything except `pending` at the end of this task.
  </acceptance_criteria>
  <done>The executor has a deterministic checklist for D-01/D-02 migration and no component CSS block can be silently skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add board attribution, open help states, and five-width assertions</name>
  <files>lib/sigra/admin/components.ex, test/sigra/admin/components_test.exs, test/example/lib/example_web/live/admin/design_gallery_live.ex, test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - lib/sigra/admin/components.ex
    - test/sigra/admin/components_test.exs
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - priv/templates/sigra.install/admin/admin_hooks.js
    - guides/reference/admin-design-contract.md
    - .planning/phases/187-individual-components-l1/187-UI-SPEC.md
    - .planning/phases/187-individual-components-l1/187-VALIDATION.md
    - .planning/phases/185-audit-infrastructure/185-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: default `field_help/1` still renders `aria-expanded="false"` and a hidden tooltip panel.
    - Test 2: `field_help/1` with `open: true` renders `aria-expanded="true"`, `data-help-open="true"`, and an unhidden tooltip panel.
    - Test 3: `summary_chip/1` with `help` and `open: true` renders `data-help-open="true"` and an unhidden `sg-metric__help` panel.
    - Test 4: Playwright sees exactly 13 entries in `COMPONENT_BOARDS`, including `board-notice_link`.
    - Test 5: Playwright fails if any component board horizontally overflows at 320, 375, 768, 1024, or 1440 pixels.
  </behavior>
  <action>Add a default-false `open` boolean attr to `summary_chip/1` and `field_help/1` only. For `summary_chip/1`, when `help` is present and `open` is true, render root `data-help-open="true"` and render the `sg-metric__help` panel without `hidden`; default rendering remains closed. For `field_help/1`, render the trigger `aria-expanded={to_string(@open)}`, root `data-help-open="true"` only when open, and panel `hidden={!@open}`. Update `test/sigra/admin/components_test.exs` with focused structural assertions for these open states while preserving existing default closed assertions. In `design_gallery_live.ex`, add a standalone `board-notice_link` adjacent to but separate from the `board-notice` canary, and add closed/open examples for `summary_chip` help and `field_help`. In `admin-design.spec.ts`, add `board-notice_link` to `COMPONENT_BOARDS`, add `const RESPONSIVE_WIDTHS = [320, 375, 768, 1024, 1440] as const`, add a component-board overflow/clip assertion that checks `scrollWidth <= clientWidth` and each board bounding box stays inside the viewport at every required width, and add a focused help-state test that opens field-help and summary-chip help, verifies `aria-expanded`/`data-help-open`, sends Escape, and verifies the panels close without trapping focus. Use stable ids/hooks and LiveView readiness; do not add sleeps.</action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs</automated>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --grep "notice_link|overflow|help"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `COMPONENT_BOARDS` includes `board-notice_link` and contains 13 `board-` component ids before `GROUP_BOARDS`.
    - Source assertion: `admin-design.spec.ts` contains the literal `RESPONSIVE_WIDTHS = [320, 375, 768, 1024, 1440] as const`.
    - Behavior assertion: Escape closes both `field_help` and `summary_chip` help panels in the Playwright test without `page.waitForTimeout`.
    - Source assertion: `board-notice` remains in `COMPONENT_BOARDS` and is still commented as the designated canary.
    - Golden-scope assertion: any `components_test.exs` strict golden text change must be preceded by a comment containing `Phase 187 intended markup delta:`; if no strict golden text changes are needed, no such comment is required.
  </acceptance_criteria>
  <done>D-08/D-10 Wave 0 evidence shape is implemented and deterministic for all component-board validation.</done>
</task>

<task type="auto">
  <name>Task 3: Measure validation runtime and stage Wave 0 status</name>
  <files>.planning/phases/187-individual-components-l1/187-VALIDATION.md</files>
  <read_first>
    - .planning/phases/187-individual-components-l1/187-VALIDATION.md
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/sigra/admin/components_test.exs
    - test/sigra/install/features/admin_test.exs
    - test/sigra/install/golden_diff_test.exs
  </read_first>
  <action>Run the validation quick command `mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs` and the full command `mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs && (cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark)`. Record measured wall-clock runtimes in `187-VALIDATION.md` by replacing `Estimated runtime | TBD after Wave 0 confirms affected-board lane timing` and `Max feedback latency: TBD after Wave 0 measures the admin design trio` with concrete values in seconds or minutes. After the matching Wave 0 evidence passes, update the Per-Task Verification Map so `187-W0-01`, `187-W0-02`, and `187-W0-03` have `Status` set to `green`: `187-W0-01` after 13-board inventory/`board-notice_link` evidence passes, `187-W0-02` after the 320/375/768/1024/1440 overflow assertions pass, and `187-W0-03` after deterministic `field_help` and summary-chip help open/focus/Escape evidence passes. Do not mark `187-W0-04` green in this plan; leave it `pending` and add a note below the Per-Task Verification Map: `187-W0-04 remains pending until Plan 02 completes shipped-CSS migration, duplicate removal from app.css, and install/golden parity checks.` Keep `wave_0_complete: false` until Plan 02 has made `187-W0-04` green and confirmed all four Wave 0 rows are green. Keep `nyquist_compliant: false` until the final ratification plan has run all phase gates.</action>
  <verify>
    <automated>! rg "TBD after Wave 0" .planning/phases/187-individual-components-l1/187-VALIDATION.md</automated>
    <automated>for id in 187-W0-01 187-W0-02 187-W0-03; do rg "^\\| ${id} \\|.*\\| green \\|$" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null || exit 1; done && rg "^\\| 187-W0-04 \\|.*\\| pending \\|$" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null && rg "187-W0-04 remains pending until Plan 02 completes shipped-CSS migration" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null && rg "wave_0_complete: false" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null</automated>
    <automated>rg "nyquist_compliant: false" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: quick and full validation commands were run once and their measured runtimes are written in `187-VALIDATION.md`.
    - Source assertion: `187-W0-01`, `187-W0-02`, and `187-W0-03` each have `Status` set to `green` in the Per-Task Verification Map.
    - Source assertion: `187-W0-04` remains `pending` and names Plan 02 as the point where shipped-CSS migration/removal checks will green it.
    - Source assertion: `wave_0_complete: false` remains in frontmatter because `187-W0-04` is not green yet.
    - Source assertion: `nyquist_compliant: false` remains until Plan 07 final gates pass.
  </acceptance_criteria>
  <done>Wave 0 validation rows 187-W0-01 through 187-W0-03 are backed by concrete tests, while 187-W0-04 is explicitly deferred to Plan 02's shipped-CSS migration proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> example `/admin/_design` route | Authenticated admin browser reaches an example-only gallery route. |
| Component assigns -> HEEx | Component-local copy and slot content enter Phoenix components and must remain escaped. |
| Keyboard/focus -> tooltip helpers | User input opens/closes help panels through delegated JS and ARIA state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-01 | Information Disclosure | `test/example/lib/example_web/live/admin/design_gallery_live.ex` | mitigate | Keep `board-notice_link` and all Wave 0 evidence only in `test/example`; do not add any `design` file or route under `priv/templates/sigra.install/`; rely on `test/sigra/install/design_gallery_isolation_test.exs`. |
| T-187-02 | Denial of Service | `field_help` and `summary_chip` help | mitigate | Use native button/focus hooks, Escape close, outside close, non-interactive `role="tooltip"` content, and Playwright no-focus-trap assertions. |
| T-187-03 | Information Disclosure / XSS | Component microcopy and slots | mitigate | Preserve HEEx escaping; do not introduce `raw`, `Phoenix.HTML.raw`, or unsafe HTML for board copy. |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; existing Playwright and axe dependencies are reused. |
</threat_model>

<verification>
Overall plan verification requires `mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs` and the focused `admin-design.spec.ts` grep lane to pass with no sleeps added.
</verification>

<success_criteria>
- `187-CSS-INVENTORY.md` exists with all 13 components and exact selector/range checklist rows.
- `admin-design.spec.ts` registers 13 component boards, includes `board-notice_link`, and asserts 320/375/768/1024/1440 board fit.
- `field_help` and `summary_chip` deterministic open-state evidence exists without changing default closed behavior.
- `187-VALIDATION.md` has concrete runtime values, `187-W0-01` through `187-W0-03` green, `187-W0-04` pending for Plan 02, and `wave_0_complete: false`.
</success_criteria>

<output>
Create `.planning/phases/187-individual-components-l1/187-01-SUMMARY.md` when done.
</output>
