---
phase: 187-individual-components-l1
plan: 07
type: execute
wave: 7
depends_on:
  - 187-03
  - 187-04
  - 187-05
  - 187-06
files_modified:
  - .planning/phases/187-individual-components-l1/187-VALIDATION.md
  - guides/reference/admin-quality-ledger.md
  - test/example/priv/playwright/snapshot-allowlist
  - test/example/priv/playwright/snapshot-allowlist-design
  - test/example/priv/playwright/tests/admin-design.spec.ts-snapshots
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
user_setup: []
must_haves:
  truths:
    - "All 13 L1 rows in admin-quality-ledger.md are at least tier 1 and never decreased."
    - "All 13 component boards pass axe in light and dark and responsive checks at 320/375/768/1024/1440."
    - "D-04/D-07: CSS parity and component goldens are green; strict goldens changed only for intended markup/ARIA deltas."
    - "D-10: Snapshot canary and both allowlist disciplines are green after any intended board recaptures."
  artifacts:
    - "Updated admin-design snapshots only for intended component-board deltas."
    - "snapshot-allowlist and snapshot-allowlist-design return to steady-state empty after recapture."
    - "187-VALIDATION.md marks nyquist_compliant true only after all final gates pass."
  key_links:
    - "component family plans -> final full suite -> validation sign-off."
    - "ledger rows -> quality-ledger-monotonic.sh guard."
---

<objective>
Ratify the completed L1 component pass.

Purpose: Run the full Phase 187 gate, recapture intended design-board deltas through the sanctioned scripts, reset allowlists, verify canaries, finalize validation fields, and prove every COMP requirement is covered by executable evidence.
Output: Final snapshots if needed, steady-state allowlists, monotonic ledger, and `187-VALIDATION.md` marked compliant.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-individual-components-l1/187-CONTEXT.md
@.planning/phases/187-individual-components-l1/187-VALIDATION.md
@.planning/phases/187-individual-components-l1/187-UI-SPEC.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
@scripts/ci/snapshot-recapture-gate.sh
@scripts/ci/snapshot-canary-guard.sh
@scripts/ci/quality-ledger-monotonic.sh
@.planning/phases/185-audit-infrastructure/185-03-SUMMARY.md
@.planning/phases/186-token-foundation-l0/186-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recapture intended component-board visual deltas through guarded scripts</name>
  <files>test/example/priv/playwright/snapshot-allowlist, test/example/priv/playwright/snapshot-allowlist-design, test/example/priv/playwright/tests/admin-design.spec.ts-snapshots</files>
  <read_first>
    - test/example/priv/playwright/snapshot-allowlist
    - test/example/priv/playwright/snapshot-allowlist-design
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - scripts/ci/snapshot-recapture-gate.sh
    - scripts/ci/snapshot-canary-guard.sh
    - .planning/phases/185-audit-infrastructure/185-03-SUMMARY.md
    - .planning/phases/187-individual-components-l1/187-VALIDATION.md
  </read_first>
  <action>If component-board screenshots changed because of intentional L1 visual/state improvements, declare only the affected component board slugs in both `test/example/priv/playwright/snapshot-allowlist` and `test/example/priv/playwright/snapshot-allowlist-design`, then run `scripts/ci/snapshot-recapture-gate.sh` for the admin-design lane. Do not include `board-notice` in either allowlist unless the canary itself was deliberately changed with a written rationale in the plan summary. After successful recapture, reset both allowlists to comments-only steady state. Commit or leave staged only the intended `admin-design.spec.ts-snapshots` PNG deltas created by the sanctioned recapture path.</action>
  <verify>
    <automated>bash scripts/ci/snapshot-canary-guard.sh --kind design --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice</automated>
    <automated>! rg -v '^#|^$' test/example/priv/playwright/snapshot-allowlist test/example/priv/playwright/snapshot-allowlist-design</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: snapshot canary guard exits 0 with `board-notice` as canary.
    - Source assertion: both allowlists contain no non-comment, non-empty lines after recapture.
    - Diff assertion: only intended admin-design board PNG baselines changed; no admin-checkpoints baselines are changed in this phase.
  </acceptance_criteria>
  <done>Visual baseline changes, if any, are deliberate, guarded, and returned to empty-allowlist steady state.</done>
</task>

<task type="auto">
  <name>Task 2: Run full L1 validation and final ledger guard</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md
    - guides/reference/admin-fractal-scorecard.md
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/sigra/admin/components_test.exs
    - test/sigra/install/features/admin_test.exs
    - test/sigra/install/golden_diff_test.exs
    - scripts/ci/quality-ledger-monotonic.sh
  </read_first>
  <action>Run the full Phase 187 validation command: `mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs && (cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark)`. Then run `bash scripts/ci/quality-ledger-monotonic.sh --base HEAD`. Inspect `guides/reference/admin-quality-ledger.md` and ensure all 13 L1 component rows `stat`, `stat_link`, `task_card`, `summary_chip`, `applied_chip`, `empty_state`, `page_back`, `scope_ribbon`, `notice`, `notice_link`, `field_help`, `skeleton`, and `audit_row` have bare integer tier `1` or `2` and evidence naming their board id. Do not lower any tier.</action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs && (cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark)</automated>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base HEAD</automated>
    <automated>for c in stat stat_link task_card summary_chip applied_chip empty_state page_back scope_ribbon notice notice_link field_help skeleton audit_row; do rg "^\\| ${c} \\| L1 \\| [12] \\|.*board-${c}" guides/reference/admin-quality-ledger.md >/dev/null || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: ExUnit component, installer parity, and golden diff tests exit 0.
    - CLI assertion: admin-design chromium, mobile, and dark projects exit 0 with axe `wcag2a`/`wcag2aa` violations count 0.
    - CLI assertion: monotonic guard exits 0.
    - Source assertion: all 13 L1 component rows have tier `1` or `2` and evidence naming their board id.
  </acceptance_criteria>
  <done>COMP-01 through COMP-06 are proven by full-suite automation and monotonic ledger evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Mark validation strategy compliant after gates pass</name>
  <files>.planning/phases/187-individual-components-l1/187-VALIDATION.md</files>
  <read_first>
    - .planning/phases/187-individual-components-l1/187-VALIDATION.md
    - guides/reference/admin-quality-ledger.md
    - test/example/priv/playwright/tests/admin-design.spec.ts
  </read_first>
  <action>After Task 1 and Task 2 pass, first confirm `187-W0-01`, `187-W0-02`, `187-W0-03`, and `187-W0-04` are already `green` and `wave_0_complete: true` is already set in `187-VALIDATION.md`; do not finalize Nyquist compliance if any Wave 0 row still has `Status` `pending`, `red`, `flaky`, or `TBD`. Then update `187-VALIDATION.md`: set `nyquist_compliant: true`, keep `wave_0_complete: true`, set the `187-PHASE` row `Status` to `green`, fill the Validation Sign-Off checkboxes as complete only for items proven by commands in this plan, and replace `**Approval:** pending` with a finalized approval value. Do not write a separate VERIFICATION.md.</action>
  <verify>
    <automated>rg "nyquist_compliant: true|wave_0_complete: true" .planning/phases/187-individual-components-l1/187-VALIDATION.md</automated>
    <automated>for id in 187-W0-01 187-W0-02 187-W0-03 187-W0-04 187-PHASE; do rg "^\\| ${id} \\|.*\\| green \\|$" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null || exit 1; done</automated>
    <automated>! rg "TBD|^\\| 187-(W0-0[1-4]|PHASE) \\|.*\\| pending \\|$|^\\*\\*Approval:\\*\\* pending$" .planning/phases/187-individual-components-l1/187-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `nyquist_compliant: true` and `wave_0_complete: true` appear in frontmatter.
    - Source assertion: `187-W0-01`, `187-W0-02`, `187-W0-03`, `187-W0-04`, and `187-PHASE` all have `Status` set to `green`.
    - Source assertion: no `TBD` remains in `187-VALIDATION.md`.
    - Source assertion: no Wave 0 or phase validation row remains `pending`, and `**Approval:** pending` is replaced with a finalized approval value.
    - Source assertion: no `VERIFICATION.md` is created by this plan.
  </acceptance_criteria>
  <done>Nyquist validation is explicitly complete only after full Phase 187 gates have passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot baseline files -> CI visual guard | PNG baselines and allowlists determine what visual deltas CI accepts. |
| Ledger file -> CI monotonic guard | Machine-parseable tier rows gate forward-only quality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-19 | Tampering | Snapshot allowlists and PNG baselines | mitigate | Use `snapshot-recapture-gate.sh`; reset both allowlists to comments-only; run canary guard with `board-notice`. |
| T-187-20 | Repudiation | Quality ledger evidence | mitigate | Require every L1 evidence cell to name its board id and keep tier as bare integer for monotonic parsing. |
| T-187-21 | Information Disclosure | Gallery/dev route | mitigate | Final validation relies on existing design-gallery isolation guard; no installer route/files are added. |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run full ExUnit and admin-design Playwright trio, snapshot canary guard, empty-allowlist assertions, and monotonic guard.
</verification>

<success_criteria>
- All six COMP requirements have passing automated evidence.
- All 13 component boards pass light/dark/mobile axe and responsive checks.
- CSS parity, golden diff, and component goldens pass.
- Allowlists are empty, canary is green, and `187-VALIDATION.md` is Nyquist compliant.
</success_criteria>

<output>
Create `.planning/phases/187-individual-components-l1/187-07-SUMMARY.md` when done.
</output>
