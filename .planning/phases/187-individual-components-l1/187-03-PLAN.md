---
phase: 187-individual-components-l1
plan: 03
type: execute
wave: 3
depends_on:
  - 187-02
files_modified:
  - lib/sigra/admin/components.ex
  - test/sigra/admin/components_test.exs
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - test/example/priv/static/assets/css/app.css
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
user_setup: []
must_haves:
  truths:
    - "stat is static read-only with no hover, focus, or navigation affordance."
    - "stat_link is a real link with hover, focus-visible, and active states, and it uses shipped sg-metric-link CSS."
    - "summary_chip and field_help expose closed/open/focus/Escape evidence without focus traps."
    - "D-09: summary_chip and field_help copy serves component-local JTBD and does not introduce system-wide glossary work."
  artifacts:
    - "design_gallery_live.ex has complete matrices for board-stat, board-stat_link, board-summary_chip, and board-field_help."
    - "admin-quality-ledger.md L1 rows for stat, stat_link, summary_chip, and field_help are raised or retained with board evidence."
  key_links:
    - "components.ex help attrs -> admin_hooks.js delegated behavior -> admin-design.spec.ts behavior assertions."
    - "sigra_admin.css metric/help CSS -> generated host parity surfaces."
---

<objective>
Audit and improve the metrics/help component family.

Purpose: Bring `stat`, `stat_link`, `summary_chip`, and `field_help` through the L1 scorecard with shipped CSS, complete state evidence, per-component axe, responsive checks, and component-local microcopy.
Output: Updated metric/help CSS and gallery matrices, focused behavior assertions, and ledger evidence for the four components.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-individual-components-l1/187-CONTEXT.md
@.planning/phases/187-individual-components-l1/187-UI-SPEC.md
@.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-quality-ledger.md
@priv/templates/sigra.install/admin/admin_hooks.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete metrics/help states and component semantics</name>
  <files>lib/sigra/admin/components.ex, test/sigra/admin/components_test.exs, priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css, test/example/priv/static/assets/css/app.css, test/example/lib/example_web/live/admin/design_gallery_live.ex, test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - lib/sigra/admin/components.ex
    - test/sigra/admin/components_test.exs
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - priv/templates/sigra.install/admin/admin_hooks.js
    - guides/reference/admin-design-contract.md
    - .planning/phases/187-individual-components-l1/187-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `stat` board renders read-only KPI states with no `href`, no `tabindex`, and no hover-only class on the component root.
    - Test 2: `stat_link` board renders default, hover, focus-visible, and active evidence for `.sg-metric-link`.
    - Test 3: `summary_chip` board renders neutral, ok, warn, risk, info, help-closed, help-open, and focus-visible evidence.
    - Test 4: `field_help` board renders closed, open, focus-visible, click/tap, and Escape-close evidence with `role="tooltip"` and no links/buttons inside the panel.
  </behavior>
  <action>Audit `stat`, `stat_link`, `summary_chip`, and `field_help` against the UI contract. Keep `stat` non-interactive: no `href`, no `tabindex`, no hover lift, no focus styling on the read-only root. Ensure `.sg-metric-link` in shipped CSS has pointer-gated hover lift, visible focus-visible ring, and active state using exact-property transitions. Ensure `summary_chip` tone styling pairs color with icon/text treatment, help state uses `data-sg-metric-help-root`, `data-help-open`, `role="tooltip"`, and `--sg-transition-tooltip`; do not add a visible question-mark trigger. Ensure `field_help` uses native button trigger, `aria-controls`, `aria-describedby`, `aria-expanded`, `role="tooltip"`, `--sg-transition-tooltip`, active press scale, and no interactive descendants inside the panel. Expand the four gallery boards with labeled state examples using stable board-local ids. Update Playwright assertions to verify those state labels, ARIA attributes, and Escape behavior. Sync any CSS changes across the three `sigra_admin.css` parity surfaces and keep migrated duplicates out of `app.css`.</action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs</automated>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --grep "stat|summary_chip|field_help|overflow|help"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `board-stat`, `board-stat_link`, `board-summary_chip`, and `board-field_help` each contain state labels in `design_gallery_live.ex`.
    - Source assertion: `.sg-metric-link:hover` appears only inside `@media (hover: hover) and (pointer: fine)`.
    - Source assertion: `field_help` panel content in the gallery contains no `<a`, `<button`, `phx-click`, or `role="button"`.
    - Source assertion: no `transition: all` appears in `sigra_admin.css` or `app.css`.
    - Golden-scope assertion: strict golden edits happen only with a `Phase 187 intended markup delta:` comment and a structural assertion proving the new state.
  </acceptance_criteria>
  <done>The four metrics/help components have complete shipped states and deterministic behavior evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Record metrics/help L1 evidence in the ledger</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md
    - guides/reference/admin-fractal-scorecard.md
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - .planning/phases/187-individual-components-l1/187-VALIDATION.md
  </read_first>
  <action>Update only the `stat`, `stat_link`, `summary_chip`, and `field_help` L1 rows in `guides/reference/admin-quality-ledger.md`. Keep the tier column as a bare integer `1` or `2`. Evidence links must name the component board ids `board-stat`, `board-stat_link`, `board-summary_chip`, and `board-field_help`, and include `admin-design.spec.ts` plus `components_test.exs` where markup/ARIA assertions exist. Do not lower any existing tier.</action>
  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base HEAD</automated>
    <automated>for c in stat stat_link summary_chip field_help; do rg "^\\| ${c} \\| L1 \\| [12] \\|" guides/reference/admin-quality-ledger.md >/dev/null || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the four target ledger rows have tier `1` or `2` as a bare integer.
    - Source assertion: each target row evidence cell names its matching board id.
    - CLI assertion: the monotonic guard exits 0.
  </acceptance_criteria>
  <done>Metrics/help L1 scorecard evidence is machine-parseable and monotonic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component assigns -> HEEx | Metric/help labels, values, captions, and help text render through Phoenix components. |
| Keyboard/pointer -> tooltip helpers | Help panels open through browser input and delegated JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-07 | Information Disclosure / XSS | `summary_chip`, `field_help` slots | mitigate | Preserve HEEx escaping and forbid raw HTML helpers in component-local copy. |
| T-187-08 | Denial of Service | Help tooltip focus behavior | mitigate | Require Escape close, outside close, non-interactive tooltip body, and no focus trap in Playwright. |
| T-187-09 | Spoofing | `stat` vs `stat_link` affordance | mitigate | Keep `stat` non-interactive and `stat_link` native `<a>` so visual affordance matches behavior. |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run component goldens, install parity, golden diff, focused admin-design tests, and monotonic guard.
</verification>

<success_criteria>
- `stat`, `stat_link`, `summary_chip`, and `field_help` pass L1 state, motion, a11y, responsive, and microcopy checks.
- No help panel traps focus or contains interactive content.
- Ledger rows for the four components have monotonic evidence.
</success_criteria>

<output>
Create `.planning/phases/187-individual-components-l1/187-03-SUMMARY.md` when done.
</output>
