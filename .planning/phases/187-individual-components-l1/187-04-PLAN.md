---
phase: 187-individual-components-l1
plan: 04
type: execute
wave: 4
depends_on:
  - 187-03
files_modified:
  - lib/sigra/admin/components.ex
  - test/sigra/admin/components_test.exs
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - test/example/priv/static/assets/css/app.css
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
user_setup: []
must_haves:
  truths:
    - "task_card hover lift is pointer-gated and CTA focus remains on the native link."
    - "applied_chip remove affordance has default, hover, focus-visible, and active evidence with `aria-label=\"Remove filter {label}\"`."
    - "page_back remains a leaf-return ghost link with an assistive-tech-hidden arrow glyph and descriptive text."
    - "D-05: Disabled visual states included for sg-btn examples are inert via disabled, aria-disabled, or is-disabled plus pointer-events none."
  artifacts:
    - "design_gallery_live.ex has complete matrices for board-task_card, board-applied_chip, and board-page_back."
    - "sigra_admin.css contains shipped sg-btn, sg-card-hover, sg-applied-chip, and page_back state styling."
    - "admin-quality-ledger.md rows for task_card, applied_chip, and page_back carry board evidence."
  key_links:
    - "task_card/page_back/applied_chip component markup -> shipped sg-btn and chip CSS -> Playwright state assertions."
---

<objective>
Audit and improve action, filter-chip, and leaf-return components.

Purpose: Bring `task_card`, `applied_chip`, and `page_back` through L1 with shipped interaction states, inert disabled examples where applicable, deterministic board evidence, and stable ledger rows.
Output: Updated state styling, gallery matrices, focused Playwright assertions, and quality ledger evidence for the three action-family components.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-individual-components-l1/187-CONTEXT.md
@.planning/phases/187-individual-components-l1/187-UI-SPEC.md
@.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-quality-ledger.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete task_card, applied_chip, and page_back states</name>
  <files>lib/sigra/admin/components.ex, test/sigra/admin/components_test.exs, priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css, test/example/priv/static/assets/css/app.css, test/example/lib/example_web/live/admin/design_gallery_live.ex, test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - lib/sigra/admin/components.ex
    - test/sigra/admin/components_test.exs
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - guides/reference/admin-design-contract.md
    - .planning/phases/187-individual-components-l1/187-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `task_card` board shows default, pointer-hover, CTA focus-visible, CTA active, and an inert disabled `sg-btn` example.
    - Test 2: `applied_chip` board shows filter label, remove link, `aria-label="Remove filter Role: Admin"`, hover, focus-visible, and active evidence.
    - Test 3: `page_back` board shows default, hover, focus-visible, active, arrow `aria-hidden="true"`, and descriptive link text.
  </behavior>
  <action>Audit `task_card`, `applied_chip`, and `page_back`. Keep `task_card` as an `<article>` with one native CTA link; pointer hover lift belongs to `.sg-card-hover` and must be wrapped in `@media (hover: hover) and (pointer: fine)`. Keep keyboard focus on the CTA link and do not make the whole card focusable. For disabled visual evidence, use `sg-btn[disabled]`, `.sg-btn[aria-disabled="true"]`, or `.sg-btn.is-disabled` examples with `pointer-events: none`; do not create a clickable disabled link. Ensure `applied_chip` remove links use `aria-label="Remove filter {label}"`, the visible times glyph stays `aria-hidden="true"`, and the remove affordance uses exact-property tone transitions only. Ensure `page_back` remains a small ghost native link with visible arrow glyph `aria-hidden="true"` and descriptive text such as `Back to users`. Expand `board-task_card`, `board-applied_chip`, and `board-page_back` with labeled state examples, add focused Playwright assertions for the state labels and inert disabled examples, and sync CSS parity surfaces after any shipped CSS edit.</action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs</automated>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --grep "task_card|applied_chip|page_back|overflow"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `design_gallery_live.ex` contains labeled state examples for `task_card`, `applied_chip`, and `page_back`.
    - Source assertion: `.sg-card-hover:hover` appears only inside `@media (hover: hover) and (pointer: fine)`.
    - Behavior assertion: disabled `sg-btn` examples have `pointer-events: none` in computed style and are not keyboard focusable.
    - Source assertion: applied-chip remove examples contain `aria-label="Remove filter Role: Admin"`.
    - Source assertion: no `transition: all` appears in `sigra_admin.css` or `app.css`.
  </acceptance_criteria>
  <done>The three action-family components have complete shipped state matrices and deterministic inert-state proof.</done>
</task>

<task type="auto">
  <name>Task 2: Record action-family L1 evidence in the ledger</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md
    - guides/reference/admin-fractal-scorecard.md
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
  </read_first>
  <action>Update only the `task_card`, `applied_chip`, and `page_back` L1 rows. Keep the tier column as a bare integer `1` or `2`; do not lower any tier. Evidence cells must name `board-task_card`, `board-applied_chip`, and `board-page_back`, plus `admin-design.spec.ts`; include `components_test.exs` in evidence only when markup/ARIA assertions were added or changed.</action>
  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base HEAD</automated>
    <automated>for c in task_card applied_chip page_back; do rg "^\\| ${c} \\| L1 \\| [12] \\|" guides/reference/admin-quality-ledger.md >/dev/null || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the three target ledger rows have bare integer tiers.
    - Source assertion: each evidence cell names its matching board id.
    - CLI assertion: the monotonic guard exits 0.
  </acceptance_criteria>
  <done>Action-family L1 scorecard evidence is recorded without breaking the ledger parser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> action links | Native links and buttons trigger navigation or filter removal. |
| CSS disabled state -> actual interactivity | Visual disabled examples must not remain clickable or focusable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-10 | Elevation of Privilege | Disabled action examples | mitigate | Require disabled, aria-disabled, or is-disabled plus inert pointer and keyboard behavior; Playwright verifies inertness. |
| T-187-11 | Spoofing | `task_card`/`page_back` affordance | mitigate | Preserve native link semantics and visible focus so affordance matches behavior. |
| T-187-12 | Tampering | Applied filter removal | mitigate | Keep remove control as explicit link with `aria-label="Remove filter {label}"`; no hidden clickable region. |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run component goldens, install parity, golden diff, focused Playwright board tests, and monotonic guard.
</verification>

<success_criteria>
- `task_card`, `applied_chip`, and `page_back` pass L1 state, motion, inertness, responsive, axe, and microcopy checks.
- No disabled-looking example remains interactive.
- Ledger rows for the three components are monotonic and evidence-linked.
</success_criteria>

<output>
Create `.planning/phases/187-individual-components-l1/187-04-SUMMARY.md` when done.
</output>
