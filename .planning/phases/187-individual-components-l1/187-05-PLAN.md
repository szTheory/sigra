---
phase: 187-individual-components-l1
plan: 05
type: execute
wave: 5
depends_on:
  - 187-04
files_modified:
  - lib/sigra/admin/components.ex
  - test/sigra/admin/components_test.exs
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - test/example/priv/static/assets/css/app.css
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
user_setup: []
must_haves:
  truths:
    - "empty_state uses exact component-local copy `No users found` and `Try adjusting your filters.`"
    - "scope_ribbon remains static body scope copy with no navigation role or animation."
    - "notice preserves load-present no-live-region default and tone is conveyed beyond color alone."
    - "notice_link has standalone board evidence while board-notice remains the canary."
  artifacts:
    - "design_gallery_live.ex has complete matrices for board-empty_state, board-scope_ribbon, board-notice, and board-notice_link."
    - "sigra_admin.css contains shipped notice, notice_link, empty_state, and scope_ribbon CSS."
    - "admin-quality-ledger.md rows for empty_state, scope_ribbon, notice, and notice_link carry evidence."
  key_links:
    - "admin-design-contract.md copy contract -> gallery board copy -> ledger evidence."
    - "board-notice canary -> snapshot-canary-guard.sh must remain stable unless deliberately recaptured through Plan 07."
---

<objective>
Audit and improve content/status components.

Purpose: Bring `empty_state`, `scope_ribbon`, `notice`, and `notice_link` through L1 with on-brand local copy, tone cues beyond color, standalone notice-link evidence, per-component axe, and no canary drift.
Output: Updated content/status state matrices, shipped CSS, focused assertions, and ledger evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-individual-components-l1/187-CONTEXT.md
@.planning/phases/187-individual-components-l1/187-UI-SPEC.md
@.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-quality-ledger.md
@.planning/phases/185-audit-infrastructure/185-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete empty, scope, notice, and notice_link states</name>
  <files>lib/sigra/admin/components.ex, test/sigra/admin/components_test.exs, priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css, test/example/priv/static/assets/css/app.css, test/example/lib/example_web/live/admin/design_gallery_live.ex, test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - lib/sigra/admin/components.ex
    - test/sigra/admin/components_test.exs
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - guides/reference/admin-design-contract.md
    - .planning/phases/187-individual-components-l1/187-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `empty_state` board contains `No users found` and `Try adjusting your filters.` exactly.
    - Test 2: `scope_ribbon` board shows global and organization scope variants and has no `href`, `role="link"`, or `tabindex`.
    - Test 3: `notice` board retains ok, warn, risk, info, and neutral tones and no default `role="alert"` or `role="status"` unless passed through `:rest`.
    - Test 4: `notice_link` board shows default, hover, focus-visible, and active inline-link evidence and remains a native `<a>`.
  </behavior>
  <action>Audit `empty_state`, `scope_ribbon`, `notice`, and `notice_link`. Set the gallery empty-state example copy to exactly `No users found` and `Try adjusting your filters.`. Keep `scope_ribbon` static: no link role, no href, no tabindex, no animation. Preserve `notice/1` load-present default with no live-region role; only call sites using `:rest` may opt in. Ensure notice tones use `data-tone` plus border/glyph/text cues beyond color. Keep `notice_link` as `.sg-notice__action`, a native underlined inline link whose hover/focus/active states affect text color, underline color, and focus ring only; do not convert it to filled button styling. Add or refine state labels in `board-empty_state`, `board-scope_ribbon`, `board-notice`, and `board-notice_link`, keeping `board-notice` as the canary board and using `board-notice_link` for per-component evidence. Add focused Playwright assertions for exact copy, no live-region default, native link semantics, and responsive board fit. Sync CSS parity surfaces after any shipped CSS edit.</action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs</automated>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --grep "empty_state|scope_ribbon|notice|notice_link|overflow"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `design_gallery_live.ex` contains `No users found` and `Try adjusting your filters.` exactly.
    - Source assertion: `board-notice` remains present and still includes all five notice tones.
    - Source assertion: `board-notice_link` is present and separate from `board-notice`.
    - Behavior assertion: Playwright verifies `notice` has no default live-region role and `notice_link` is a native link.
    - Source assertion: no `transition: all` appears in `sigra_admin.css` or `app.css`.
  </acceptance_criteria>
  <done>The four content/status components have complete shipped state evidence and component-local copy proof.</done>
</task>

<task type="auto">
  <name>Task 2: Record content/status L1 evidence in the ledger</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md
    - guides/reference/admin-fractal-scorecard.md
    - test/example/priv/playwright/tests/admin-design.spec.ts
    - test/example/lib/example_web/live/admin/design_gallery_live.ex
  </read_first>
  <action>Update only the `empty_state`, `scope_ribbon`, `notice`, and `notice_link` L1 rows. Keep the tier column as a bare integer `1` or `2`; do not lower any tier. Evidence cells must name `board-empty_state`, `board-scope_ribbon`, `board-notice`, and `board-notice_link`, plus `admin-design.spec.ts`; include `components_test.exs` only where markup/ARIA assertions changed.</action>
  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base HEAD</automated>
    <automated>for c in empty_state scope_ribbon notice notice_link; do rg "^\\| ${c} \\| L1 \\| [12] \\|" guides/reference/admin-quality-ledger.md >/dev/null || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the four target ledger rows have bare integer tiers.
    - Source assertion: `notice_link` evidence names `board-notice_link`, not only `board-notice`.
    - CLI assertion: the monotonic guard exits 0.
  </acceptance_criteria>
  <done>Content/status L1 scorecard evidence is recorded and notice_link has standalone attribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component slots -> notice rendering | Notice and notice_link copy enters HEEx slots. |
| Gallery canary -> snapshot guards | `board-notice` is a stability canary for design-lane snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-13 | Information Disclosure / XSS | `notice` and `notice_link` slots | mitigate | Preserve HEEx escaping; do not use raw HTML for inline notice copy. |
| T-187-14 | Repudiation | Notice tone semantics | mitigate | Pair tone with explicit text and non-color cues so screenshots and DOM evidence explain the condition. |
| T-187-15 | Tampering | `board-notice` canary | mitigate | Keep canary board stable; any intended visual delta must go through Plan 07 recapture and allowlist gates. |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run component goldens, install parity, golden diff, focused Playwright board tests, and monotonic guard.
</verification>

<success_criteria>
- `empty_state`, `scope_ribbon`, `notice`, and `notice_link` pass L1 state, copy, a11y, responsive, and axe checks.
- `notice_link` has standalone per-component evidence.
- `board-notice` remains the design-lane canary.
</success_criteria>

<output>
Create `.planning/phases/187-individual-components-l1/187-05-SUMMARY.md` when done.
</output>
