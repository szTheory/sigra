---
phase: 187-individual-components-l1
plan: 02
type: execute
wave: 2
depends_on:
  - 187-01
files_modified:
  - .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
  - priv/templates/sigra.install/admin/sigra_admin.css
  - test/example/priv/static/assets/sigra_admin.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - test/example/priv/static/assets/css/app.css
  - guides/reference/admin-token-reference.md
  - .planning/phases/187-individual-components-l1/187-VALIDATION.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
user_setup: []
must_haves:
  truths:
    - "D-01/D-04: Component visual/state CSS ships from canonical sigra_admin.css and remains byte-identical across installer, example mirror, and install golden fixture."
    - "D-02: app.css no longer defines migrated sg-* component rules that would mask missing shipped CSS."
    - "D-03: Migrated component rules live in @layer sg-components and use --sg-* tokens, not --vt-* or daisyUI dependencies."
    - "D-06: Existing Phase 186 motion token values stay byte-identical; only net-new motion tokens are added."
  artifacts:
    - "priv/templates/sigra.install/admin/sigra_admin.css contains migrated component CSS and net-new L1 motion tokens."
    - "test/example/priv/static/assets/css/app.css no longer contains duplicate sg-* component selector definitions."
    - "guides/reference/admin-token-reference.md documents the net-new L1 motion tokens."
    - ".planning/phases/187-individual-components-l1/187-VALIDATION.md marks 187-W0-04 green only after shipped-CSS migration/removal checks pass."
  key_links:
    - "sigra_admin.css canonical template -> example mirror -> install golden fixture must be byte-identical."
    - "187-CSS-INVENTORY.md checklist statuses move from pending to migrated for every component selector family."
    - "Plan 02 migration checks -> 187-VALIDATION.md W0-04 and wave_0_complete status."
---

<objective>
Make the component CSS migration real before component-family polish begins.

Purpose: Close the example-green/generated-host-bare failure mode by moving existing component visual/state rules into shipped CSS, removing duplicates from app.css, and adding only net-new L1 motion tokens without changing ratified Phase 186 values.
Output: Shipped component CSS parity across all three surfaces, cleaned example app.css, documented additive motion tokens, and an updated migration checklist.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-individual-components-l1/187-CONTEXT.md
@.planning/phases/187-individual-components-l1/187-RESEARCH.md
@.planning/phases/187-individual-components-l1/187-PATTERNS.md
@.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
@.planning/phases/187-individual-components-l1/187-VALIDATION.md
@.planning/phases/186-token-foundation-l0/186-01-SUMMARY.md
@.planning/phases/186-token-foundation-l0/186-04-SUMMARY.md
@guides/reference/admin-token-reference.md
@guides/reference/admin-ui-principles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add L1-only motion tokens without retuning locked values</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css, guides/reference/admin-token-reference.md</files>
  <read_first>
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    - guides/reference/admin-token-reference.md
    - .planning/phases/187-individual-components-l1/187-CONTEXT.md
    - .planning/phases/186-token-foundation-l0/186-04-SUMMARY.md
  </read_first>
  <action>Add exactly four net-new tokens to the `:root` motion section of canonical `sigra_admin.css`: `--sg-motion-exit: 160ms;`, `--sg-motion-tooltip: 180ms;`, `--sg-transition-exit: opacity var(--sg-motion-exit) var(--sg-ease-in), transform var(--sg-motion-exit) var(--sg-ease-in);`, and `--sg-transition-tooltip: opacity var(--sg-motion-tooltip) var(--sg-ease-out), transform var(--sg-motion-tooltip) var(--sg-ease-out);`. Do not change the existing values for `--sg-motion-press: 120ms;`, `--sg-motion-pop: 180ms;`, `--sg-motion-fast: 140ms;`, `--sg-motion-medium: 220ms;`, or `--sg-motion-slow: 300ms;`. Mirror the canonical CSS byte-for-byte to the example and install golden fixture CSS files. Add rows for the four new tokens to `guides/reference/admin-token-reference.md` in the Motion section, with rationale that D-06 adds exit asymmetry and tooltip-specific timing while preserving the Phase 186 value lock.</action>
  <verify>
    <automated>rg -- '--sg-motion-exit: 160ms;|--sg-motion-tooltip: 180ms;|--sg-transition-exit:|--sg-transition-tooltip:' priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</automated>
    <automated>rg -- '--sg-motion-press: 120ms;|--sg-motion-pop: 180ms;|--sg-motion-fast: 140ms;|--sg-motion-medium: 220ms;|--sg-motion-slow: 300ms;' priv/templates/sigra.install/admin/sigra_admin.css</automated>
    <automated>mix test test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the four new token names appear in all three `sigra_admin.css` parity surfaces.
    - Source assertion: the five existing Phase 186 motion duration declarations still have their exact locked values.
    - Source assertion: `admin-token-reference.md` contains rows for `--sg-motion-exit`, `--sg-motion-tooltip`, `--sg-transition-exit`, and `--sg-transition-tooltip`.
    - CLI assertion: DIST-05 and install golden diff tests pass after syncing parity surfaces.
  </acceptance_criteria>
  <done>D-06 is implemented with additive tokens only and no locked token value is retuned.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all component CSS blocks into shipped sg-components layer</name>
  <files>.planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md, priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css, test/example/priv/static/assets/css/app.css</files>
  <read_first>
    - .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
    - .planning/phases/187-individual-components-l1/187-PATTERNS.md
    - .planning/phases/184-distribution-parity/184-CONTEXT.md
  </read_first>
  <action>Using `187-CSS-INVENTORY.md` as the checklist, move the current visual/state selector blocks for `sg-metric`, `sg-metric-link`, `sg-btn`, `sg-card-hover`, `sg-applied-chip`, `sg-empty-state`, `sg-scope-ribbon`, `sg-notice`, `sg-notice__action`, `sg-field-help`, `sg-skeleton`, `sg-list-row`, `sg-status-pill`, and `sg-code` from `test/example/priv/static/assets/css/app.css` into `priv/templates/sigra.install/admin/sigra_admin.css` inside the existing `@layer sg-components` block. Preserve pointer hover wrappers as `@media (hover: hover) and (pointer: fine)`, focus rings as `box-shadow: var(--sg-focus-ring)`, disabled/inert selectors with `pointer-events: none`, and exact-property transitions only. Remove the migrated selector blocks from `app.css` so the example can no longer mask missing shipped component CSS. The duplicate-removal proof must reject migrated component selectors anywhere in `app.css`, including indented selectors, selectors inside media blocks, pseudo selectors, attribute selectors, BEM element/modifier selectors, descendant selectors, and compound selectors. Sync canonical CSS to `test/example/priv/static/assets/sigra_admin.css` and `test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css`. Mark every checklist row in `187-CSS-INVENTORY.md` as `migrated` with no `pending` rows remaining.</action>
  <verify>
    <automated>! rg -n '^\\s*[^{}]*\\.(sg-metric|sg-btn|sg-notice|sg-status-pill|sg-field-help|sg-list-row|sg-metric-link|sg-applied-chip|sg-empty-state|sg-skeleton|sg-card-hover|sg-code|sg-scope-ribbon)(?:__[-a-z0-9]+|--[-a-z0-9]+)?([\\s:{.#\\[]|$)' test/example/priv/static/assets/css/app.css</automated>
    <automated>node -e 'const fs=require("fs"); const css=fs.readFileSync("priv/templates/sigra.install/admin/sigra_admin.css","utf8"); const start=css.indexOf("@layer sg-components"); if (start === -1) { console.error("missing @layer sg-components"); process.exit(1); } const end=css.indexOf("@layer sg-overrides", start); const block=css.slice(start, end === -1 ? undefined : end); const families=["sg-metric","sg-btn","sg-notice","sg-status-pill","sg-field-help","sg-list-row","sg-metric-link","sg-applied-chip","sg-empty-state","sg-skeleton","sg-card-hover","sg-code","sg-scope-ribbon"]; const missing=families.filter((f)=>!(new RegExp("\\\\."+f+"(?:__[-a-z0-9]+|--[-a-z0-9]+)?(?=[\\\\s:{.#\\\\[]|$)")).test(block)); if (missing.length) { console.error("missing migrated selectors under sg-components: "+missing.join(", ")); process.exit(1); }'</automated>
    <automated>cmp -s priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css && cmp -s priv/templates/sigra.install/admin/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</automated>
    <automated>! rg -- '--vt-' priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css</automated>
    <automated>! rg 'transition:\\s*all' priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/css/app.css</automated>
    <automated>! rg '\\| pending \\|' .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md</automated>
    <automated>mix test test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: migrated component selector blocks exist under canonical `@layer sg-components`, not before the layer declaration and not in `@layer sg-overrides`.
    - Source assertion: `app.css` has no duplicate definitions anywhere for the migrated `sg-*` component selectors listed in this task, including indented, media-block, pseudo, attribute, BEM element/modifier, descendant, and compound selector forms.
    - CLI assertion: canonical `priv/templates/sigra.install/admin/sigra_admin.css` contains each migrated selector family under `@layer sg-components`.
    - CLI assertion: the example and install-golden parity copies are byte-identical to canonical `sigra_admin.css`.
    - Source assertion: canonical shipped CSS contains no `--vt-*` references.
    - Source assertion: `187-CSS-INVENTORY.md` has no `pending` migration statuses.
    - CLI assertion: the example copy and install golden fixture remain byte-identical to the canonical template.
  </acceptance_criteria>
  <done>D-01/D-02/D-03/D-04 are implemented for the full L1 component selector inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Green W0-04 after shipped CSS proof</name>
  <files>.planning/phases/187-individual-components-l1/187-VALIDATION.md</files>
  <read_first>
    - .planning/phases/187-individual-components-l1/187-VALIDATION.md
    - .planning/phases/187-individual-components-l1/187-CSS-INVENTORY.md
    - priv/templates/sigra.install/admin/sigra_admin.css
    - test/example/priv/static/assets/css/app.css
  </read_first>
  <action>After Task 2's shipped-CSS migration, duplicate-removal grep/script, canonical-layer selector check, parity `cmp`, and install/golden tests pass, update `187-VALIDATION.md` so `187-W0-04` has `Status` set to `green`. Before changing `wave_0_complete`, confirm `187-W0-01`, `187-W0-02`, `187-W0-03`, and `187-W0-04` are all `green`; set `wave_0_complete: true` only if every one of those four rows is green. If any Wave 0 row is not green, leave `wave_0_complete: false` and record the blocker under the Per-Task Verification Map. Keep `nyquist_compliant: false` until Plan 07 completes final phase gates.</action>
  <verify>
    <automated>for id in 187-W0-01 187-W0-02 187-W0-03 187-W0-04; do rg "^\\| ${id} \\|.*\\| green \\|$" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null || exit 1; done && rg "wave_0_complete: true" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null</automated>
    <automated>rg "nyquist_compliant: false" .planning/phases/187-individual-components-l1/187-VALIDATION.md >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `187-W0-04` is green only after the migrated CSS selector families exist in shipped `sigra_admin.css`, are absent from `app.css`, and parity checks pass.
    - Source assertion: `wave_0_complete: true` appears only when `187-W0-01`, `187-W0-02`, `187-W0-03`, and `187-W0-04` are all green.
    - Source assertion: `nyquist_compliant: false` remains until Plan 07 final gates pass.
  </acceptance_criteria>
  <done>W0-04 and Wave 0 completion are greened at the correct post-migration point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installer template -> generated host | Shipped CSS crosses from library template into adopter applications. |
| Example CSS -> visual evidence | Example app rendering must not hide missing shipped CSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-04 | Tampering | `sigra_admin.css` parity surfaces | mitigate | Run DIST-05 and golden_diff tests after every CSS edit; keep the three CSS files byte-identical. |
| T-187-05 | Information Disclosure | `app.css` duplicate sg-* rules | mitigate | Remove migrated component rules from `app.css` so generated-host evidence cannot be masked by example-only styles. |
| T-187-06 | Denial of Service | Motion/reduced-motion rules | mitigate | Use exact-property transitions and preserve reduced-motion clamp; no `transition: all`. |
| T-187-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; existing dependencies are reused. |
</threat_model>

<verification>
Run `mix test test/sigra/install/features/admin_test.exs test/sigra/install/golden_diff_test.exs`, byte-compare canonical/example/fixture CSS, verify migrated selector families exist under canonical `@layer sg-components`, and run whitespace-tolerant duplicate-selector checks proving no migrated component selectors remain anywhere in `app.css`.
</verification>

<success_criteria>
- All component CSS selector families from the inventory ship from `sigra_admin.css`.
- `app.css` no longer defines the migrated component visual/state blocks.
- Existing motion tokens remain locked and four new L1 motion tokens are documented.
- CSS parity tests pass.
- `187-VALIDATION.md` marks `187-W0-04` green and `wave_0_complete: true` only after all four Wave 0 rows are green.
</success_criteria>

<output>
Create `.planning/phases/187-individual-components-l1/187-02-SUMMARY.md` when done.
</output>
