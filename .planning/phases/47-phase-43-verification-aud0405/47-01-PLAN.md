---
phase: 47
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md
  - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
  - mix.exs
autonomous: true
requirements:
  - AUD-04
  - AUD-05
must_haves:
  truths:
    - "`43-VALIDATION.md` per-plan rows reference real test paths (not `test/sigra/*register*audit*` glob placeholders) and honest Status markers."
    - "`43-VERIFICATION.md` exists beside other phase **43** artifacts, mirrors `46-VERIFICATION.md` structure, and lists verbatim merge-gate + optional attestation commands with receipt language."
    - "Nyquist ownership for **41–44** remains explicitly phase **50** — no false `nyquist_compliant: true` on `43-VALIDATION.md` without maintainer decision."
  artifacts:
    - path: ".planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md"
      provides: "Dated verification snapshot for phase 43 AUD-04/05 closure"
nyquist_compliant: true
---

<objective>
Refresh **`43-VALIDATION.md`** to match merged phase **43** work (inventory + atomicity tests), publish **`43-VERIFICATION.md`** (outcome snapshot + command receipts per **D-47-02** / **D-47-03**), and optionally add **`mix ci.audit_43`** as a stable merge-gate entry point (**D-47-03** discretion).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-47-01 | Tampering | Validation map claims ✅ without matching tests | high | Acceptance greps bind rows to literal file paths from repo |
| T-47-02 | Repudiation | `43-VERIFICATION.md` omits commands or SHA context | medium | Task requires verbatim command block + `git rev-parse HEAD` in Notes |
| T-47-03 | Elevation | Flipping `nyquist_compliant` early | high | Explicit grep: frontmatter must stay `nyquist_compliant: false` unless `STATE.md` records escalation |
| T-47-04 | Information disclosure | Verification pastes secrets from test logs | low | `read_first` cites no-secret policy; acceptance forbids `client_secret` in new md |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Align 43-VALIDATION.md with executed plans</name>
  <files>.planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md</files>
  <read_first>
    - .planning/phases/47-phase-43-verification-aud0405/47-CONTEXT.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-02-PLAN.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-03-PLAN.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-04-PLAN.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md
  </read_first>
  <action>
    Update the **Per-Task Verification Map** table in `43-VALIDATION.md`:

    - **43-01-01** row: set **Automated Command** to exactly `rg "Sigra\\.Audit\\.log_safe" lib/sigra` (or keep if already exact); set **File Exists** ✅; set **Status** to ✅ green if inventory task completed per `43-01-SUMMARY.md`, else document ⚠️ with one-line note.
    - **43-02** row(s): bind to `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/register_audit_atomicity_test.exs`.
    - **43-03** row(s): bind to `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs`.
    - **43-04** row(s): bind to two commands on separate lines in the cell or split rows: (1) `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/login_and_lockout_audit_atomicity_test.exs` (2) `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth_test.exs`.

    Add a short **## Nyquist deferral** subsection (before **Validation Sign-Off**): full Nyquist batch **41–44** is owned by **phase 50**; phase **47** does not assert `nyquist_compliant: true`.

    Keep frontmatter `nyquist_compliant: false` unless `STATE.md` contains an explicit maintainer line authorizing `true`.
  </action>
  <acceptance_criteria>
    - `grep -F "test/sigra/auth/register_audit_atomicity_test.exs" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` exits 0.
    - `grep -F "test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` exits 0.
    - `grep -F "test/sigra/auth/login_and_lockout_audit_atomicity_test.exs" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` exits 0.
    - `grep -F "test/sigra/auth_test.exs" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` exits 0.
    - First YAML frontmatter line for the key must remain false: `awk 'NR>=1&&NR<=12&&/^nyquist_compliant:/{print; exit}' .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md | grep -q "nyquist_compliant: false"` exits 0, unless `.planning/STATE.md` contains literal `nyquist_escalation_authorized` (then `true` allowed).
    - `grep -i "phase 50" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep -c "register_audit_atomicity" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VALIDATION.md` returns integer ≥ 1.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Create 43-VERIFICATION.md snapshot</name>
  <files>.planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/46-human-ga-matrix-gap-closure/46-VERIFICATION.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-01-SUMMARY.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-02-SUMMARY.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-03-SUMMARY.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-04-SUMMARY.md
  </read_first>
  <action>
    Create **`.planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md`** modeled on `46-VERIFICATION.md`:

    YAML frontmatter keys at minimum: `status:` (`draft` until Task 3 passes, then `passed`), `phase: "43"`, `verified:` (ISO date when merge gate run).

    Sections:
    1. **## Must-haves** — table columns **Item | Evidence**; rows must cite `43-AUD-04-INVENTORY.md` (by section or row label), the three `*_audit_atomicity_test.exs` files, `43-0x-SUMMARY.md` references, and AUD-05 Auth core scope.
    2. **## Merge gate** (labeled block per D-47-03) — list verbatim: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile` (if project treats compile as merge gate; else omit with one-line justification) and the **compound** `mix test` covering all four paths: `test/sigra/auth/register_audit_atomicity_test.exs`, `test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs`, `test/sigra/auth/login_and_lockout_audit_atomicity_test.exs`, `test/sigra/auth_test.exs`.
    3. **## Release attestation (optional)** — document full root `mix test` command; state **Executed** with date or **Not run in this closure** honestly.
    4. **## Notes** — `git rev-parse HEAD` output line for SHA recorded at verification time; paragraph deferring full Nyquist to **phase 50**.

    Optional: if Task 4 adds Mix alias, add a Must-have row pointing to `mix ci.audit_43`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0.
    - `grep -E "^---$" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md | wc -l | grep -qE "^[[:space:]]*2"` exits 0 (YAML frontmatter opens and closes).
    - `grep -F "register_audit_atomicity_test.exs" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0.
    - `grep -F "43-AUD-04-INVENTORY.md" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0.
    - `grep -i "phase 50" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0.
    - `grep -iE "client_secret|Bearer [A-Za-z0-9_-]{20,}" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits **1**.
  </acceptance_criteria>
  <verify>
    `head -n 8 .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: Run merge gate and finalize verification status</name>
  <files>.planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
    - mix.exs
  </read_first>
  <action>
    With local Postgres at `localhost:5432` (`postgres`/`postgres` per `CLAUDE.md`), run and capture logs:

    1. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile`
    2. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/register_audit_atomicity_test.exs test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs test/sigra/auth/login_and_lockout_audit_atomicity_test.exs test/sigra/auth_test.exs`

    If both exit 0: set frontmatter `status: passed` and `verified:` to today’s ISO date; in **Automated checks run** / **Merge gate**, state **PASS (exit 0)** with the literal command lines.

    If either fails: keep `status: draft`, document **FAIL** with first error line — **do not** proceed to plan **47-02**.
  </action>
  <acceptance_criteria>
    - `grep -E "^status: passed" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0 OR file contains literal substring `status: draft` AND substring `FAIL` in body (honest failure path).
    - If `status: passed`: `grep -F "PASS" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0.
    - `grep -E "^verified: 20" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0 when status is passed (date prefix `20` for 2026-xx-xx).
  </acceptance_criteria>
  <verify>
    Re-run the same compound `mix test` line; must exit 0 when verification claims passed.
  </verify>
</task>

<task type="execute">
  <name>Task 4 (optional): Add mix ci.audit_43 alias</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
  </read_first>
  <action>
    If maintainers want a stable entry point: add `aliases/0` to `Mix.Project` and include in `project/0` key `aliases: aliases()` where `"ci.audit_43"` expands to the **single** `mix test` invocation listing the four test paths (same order as Task 3). Update `43-VERIFICATION.md` **Merge gate** to cite `mix ci.audit_43` **in addition to** the raw command (or replace if team prefers one line).

    If skipping: add one sentence to `43-VERIFICATION.md` **Notes**: “No Mix alias in this closure — raw compound `mix test` is canonical.”
  </action>
  <acceptance_criteria>
    - Either `grep -q "ci.audit_43" mix.exs` exits 0 **or** `grep -q "No Mix alias" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0.
    - If alias added: `grep -q "register_audit_atomicity_test.exs" mix.exs` exits 0.
  </acceptance_criteria>
  <verify>
    If alias added: `mix help ci.audit_43` or `mix run -e ":ok"` after `mix compile` — alias must not break `mix compile`.
  </verify>
</task>

</tasks>

<verification>
1. All Task 1–3 acceptance greps pass.
2. Compound `mix test` from Task 3 exits 0 when `status: passed`.
3. `43-VERIFICATION.md` contains no secret patterns per Task 2 acceptance.
</verification>

<success_criteria>
- Living validation map and frozen verification snapshot are consistent with merged phase **43** code.
- Merge gate commands are verbatim and tied to AUD-05 atomicity boundaries.
</success_criteria>
