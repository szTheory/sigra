---
phase: 47
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - AUD-04
  - AUD-05
must_haves:
  truths:
    - "**AUD-04** and **AUD-05** show `[x]` in `REQUIREMENTS.md` only when `43-VERIFICATION.md` frontmatter reads `status: passed`."
    - "Traceability table rows **AUD-04** and **AUD-05** show **Complete** with ISO date **2026-04-21** or later (execution date)."
    - "Traceability narrative under **Audit trail atomicity** does not claim Nyquist completion for phase **43** beyond what `43-VERIFICATION.md` states."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Flipped AUD-04/05 checkboxes + aligned traceability table"
nyquist_compliant: true
---

<objective>
Reconcile **`.planning/REQUIREMENTS.md`** (AUD-04, AUD-05, traceability table, prose consistency) and **`.planning/ROADMAP.md`** (phase **47** narrative only if drift) after **`43-VERIFICATION.md`** proves merge gate green — same PR / change-set as **D-47-04**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-47-10 | Tampering | REQ checkboxes `[x]` without passing verification | high | Grep gate: `status: passed` in `43-VERIFICATION.md` before edits |
| T-47-11 | Repudiation | Traceability table date/phase out of sync with body | medium | Single edit session updates table + bullet mapping |
| T-47-12 | Elevation | ROADMAP claims Nyquist done for **43** | medium | ROADMAP edits optional; if touched, must not contradict phase **50** ownership |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Pre-flight — verification gate</name>
  <files>.planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
    - .planning/phases/47-phase-43-verification-aud0405/47-CONTEXT.md
  </read_first>
  <action>
    Abort plan **02** (do not edit REQUIREMENTS) unless `43-VERIFICATION.md` frontmatter contains `status: passed`. If `status: draft`, stop and return to **47-01** Task 3.
  </action>
  <acceptance_criteria>
    - `grep -E "^status: passed" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0 before any `REQUIREMENTS.md` edit in this plan.
  </acceptance_criteria>
  <verify>
    `grep "^status:" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 2: Flip AUD-04 / AUD-05 and refresh traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    In **## Audit trail atomicity**:

    - Change **AUD-04** bullet from `- [ ]` to `- [x]`; append `*(Closed in Phase 47 — see `43-VERIFICATION.md`.)*` or equivalent one-line parenthetical with backtick path to `.planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md`.
    - Change **AUD-05** bullet similarly.

    In **## Traceability** table:

    - Set **AUD-04** row: **Status** = `Complete (YYYY-MM-DD)` using verification `verified:` date or execution date.
    - Set **AUD-05** row: same pattern; **Phase** column remains **47** (or `43+47` if you document dual — prefer **47** per existing table).

    Ensure **Primary phase mapping** prose under audit section still lists **47–49** for gap closure — adjust only if now inconsistent (checkboxes must match table).
  </action>
  <acceptance_criteria>
    - `grep -E "^- \\[x\\] \\*\\*AUD-04" .planning/REQUIREMENTS.md` exits 0.
    - `grep -E "^- \\[x\\] \\*\\*AUD-05" .planning/REQUIREMENTS.md` exits 0.
    - `grep "AUD-04 | 47 | Complete" .planning/REQUIREMENTS.md` exits 0.
    - `grep "AUD-05 | 47 | Complete" .planning/REQUIREMENTS.md` exits 0.
    - `grep "43-VERIFICATION.md" .planning/REQUIREMENTS.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep "^| AUD-0" .planning/REQUIREMENTS.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: ROADMAP micro-sync (optional)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
  </read_first>
  <action>
    Locate the **47** row. If the **Success looks like** column still matches delivered artifacts, make **no** edit.

    If drift exists (e.g. still says “Nyquist run” implies done in **47**): rewrite so criterion (3) explicitly defers full Nyquist to **phase 50** while **47** publishes `43-VERIFICATION.md` + REQ reconciliation — aligned with `47-CONTEXT.md` D-47-01.
  </action>
  <acceptance_criteria>
    - `grep "| **47** |" .planning/ROADMAP.md` exits 0 (table row still present).
    - If ROADMAP edited: `git diff --stat .planning/ROADMAP.md` shows ≤ 15 lines changed.
  </acceptance_criteria>
  <verify>
    `grep "47" .planning/ROADMAP.md | head -n 3`
  </verify>
</task>

</tasks>

<verification>
1. `grep -E "^status: passed" .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md` exits 0.
2. AUD-04/05 lines in `REQUIREMENTS.md` match Task 2 acceptance greps.
3. No `[ ] **AUD-04**` or `[ ] **AUD-05**` remains: `grep "\\[ \\] \\*\\*AUD-0[45]" .planning/REQUIREMENTS.md` exits **1**.
</verification>

<success_criteria>
- Requirements truth matches verification artifact (no checkbox-before-evidence ordering violation).
- Traceability table matches body for AUD-04/05.
</success_criteria>
