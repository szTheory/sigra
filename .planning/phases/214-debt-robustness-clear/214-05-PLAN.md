---
phase: "214"
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/panel-schema-check.sh
  - guides/introduction/contract.md
  - .planning/todos/pending/2026-07-01-phase209-code-review-deferred.md
autonomous: true
requirements:
  - DEBT-02
  - DEBT-04
must_haves:
  truths:
    - "scripts/ci/panel-schema-check.sh is retired with committed rationale (in-file comment explaining why it is not wired into CI)"
    - "guides/introduction/contract.md line 9 reads 1.1.0 (not 1.20.0)"
    - "git tag v1.20.0 is deleted from local and remote"
    - "A runbook for mix hex.retire sigra 1.20.0 is documented in the phase summary for Jon to run manually"
    - "DEBT-02 phase-209 todo closed; DEBT-04 stray-tag wart closed"
  artifacts:
    - scripts/ci/panel-schema-check.sh (file updated with retire rationale comment; kept in place per D-05)
    - guides/introduction/contract.md (line 9 corrected to 1.1.0)
  key_links:
    - "git tag v1.20.0 deleted: git tag -d v1.20.0 && git push origin :refs/tags/v1.20.0"
  prohibitions:
    - "Do not wire panel-schema-check.sh into ci.yml — it is retired, not wired (D-05)"
    - "Do not run mix hex.retire automatically — it is a manual runbook step for Jon (D-14)"
    - "Do not delete panel-schema-check.sh from disk — only add the retire rationale comment (D-05 says retired, not deleted)"
---

<objective>
Resolve two doc/script/tag debt items in a single lightweight plan:

**DEBT-02 (D-05..D-07):** Retire `scripts/ci/panel-schema-check.sh` by adding a prominent in-file
comment explaining why it is not wired into CI. The script validates frozen Phase-209 persona-JTBD
planning artifacts — closed milestone deliverables that never change. Wiring it would guard against
a corruption that cannot occur. WR-01/IN nits (D-06, D-07) become won't-fix on the retired script.

**DEBT-04 (D-12..D-14):** Correct the stray version reference: delete git tag `v1.20.0` (a three-segment
milestone tag that Hex misinterprets as a package version, causing `{:sigra, "~> 1.0"}` to resolve
to `1.20.0` instead of `1.1.0`). Correct `guides/introduction/contract.md:9` from `1.20.0` to `1.1.0`.
Document the `mix hex.retire sigra 1.20.0` runbook in the phase summary (manual, not automated).

Purpose: Both items are low-risk, high-signal paperwork that clear the DEBT backlog. The DEBT-04 git tag
deletion is the only action that has external impact — it prevents Hex dependency resolution from picking
up the wrong version.

Output: Retired script with rationale; corrected contract.md; deleted git tag; hex retire runbook in
summary. D-05, D-06, D-07, D-12, D-13, D-14 resolved.

## Artifacts This Plan Produces
- scripts/ci/panel-schema-check.sh: in-file RETIRED comment added at top
- guides/introduction/contract.md: line 9 corrected from 1.20.0 to 1.1.0
- git tag v1.20.0 deleted (local + remote)
- 214-05-SUMMARY.md: hex retire runbook for Jon
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-debt-robustness-clear/214-CONTEXT.md
@.planning/phases/214-debt-robustness-clear/214-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire panel-schema-check.sh with rationale (per D-05, D-06, D-07)</name>
  <files>scripts/ci/panel-schema-check.sh, .planning/todos/pending/2026-07-01-phase209-code-review-deferred.md</files>
  <read_first>
    - scripts/ci/panel-schema-check.sh (full file — understand what it validates and current header)
    - .github/workflows/ci.yml (grep for panel-schema-check to confirm zero references — this is already known from RESEARCH.md but verify once more)
    - .planning/todos/pending/2026-07-01-phase209-code-review-deferred.md (understand the full scope of the todo being closed)
    - 214-CONTEXT.md D-05, D-06, D-07
    - 214-RESEARCH.md section "DEBT-02 Ground Truth — panel-schema-check.sh"
  </read_first>
  <action>
**Step 1 — Add RETIRED comment to `scripts/ci/panel-schema-check.sh`.**

Insert a RETIRED banner comment at the top of the file (after the shebang line). The comment should
explain:
- What the script validates (Phase-209 persona-JTBD panel schema, `.planning/uat-evidence/v1.42-persona-jtbd/*.md`)
- Why it is NOT wired into CI: the inputs are frozen v1.42 milestone deliverables (closed, archived
  milestone); they never change; a guard over frozen historical docs enforces nothing forward-looking
- When this retirement was decided: Phase 214 DEBT-02 (D-05), 2026-07-02
- That this is intentionally not deleted (kept as a reference for the retire decision)

Example banner structure:
```
# RETIRED: Phase 214 DEBT-02 (D-05) — 2026-07-02
#
# This script validates the Phase-209 persona-JTBD panel schema files at
# .planning/uat-evidence/v1.42-persona-jtbd/*.md.
#
# It is NOT wired into CI and will NOT be wired into CI because:
# - The inputs are frozen v1.42 milestone deliverables (closed, archived)
# - Frozen historical planning docs cannot be corrupted by future development
# - A guard over immutable files enforces nothing forward-looking
#
# WR-01/IN nits from the phase-209 code review (D-06/D-07) are won't-fix
# on this retired script.
#
# The script is kept as documentation of the retire decision. Do not wire.
```

Leave the rest of the file unchanged.

**Step 2 — Close the todo.**

Move `.planning/todos/pending/2026-07-01-phase209-code-review-deferred.md` to
`.planning/todos/resolved/` (or mark it as resolved per the project's todo-close convention —
read an existing resolved todo to understand the file format/location).
  </action>
  <verify>
    <automated>grep -c "RETIRED" scripts/ci/panel-schema-check.sh</automated>
  </verify>
  <done>panel-schema-check.sh contains the RETIRED comment at the top; DEBT-02 todo moved to resolved/</done>
</task>

<task type="auto">
  <name>Task 2: Fix stray v1.20.0 version wart — delete git tag and correct contract.md (per D-12, D-13)</name>
  <files>guides/introduction/contract.md</files>
  <read_first>
    - guides/introduction/contract.md lines 1-20 (find line 9 with stale 1.20.0 citation)
    - mix.exs line 4 (confirm @version "1.1.0" — the correct current version)
    - 214-RESEARCH.md section "DEBT-04 Ground Truth — Stray 1.20.0 Version"
    - 214-CONTEXT.md D-12, D-13, D-14
  </read_first>
  <action>
**Step 1 — Delete git tag `v1.20.0` (per D-13).**

Run the following git commands:
1. `git tag -d v1.20.0` — delete the local tag
2. `git push origin :refs/tags/v1.20.0` — delete the remote tag

Verify: `git tag -l "v1.20.0"` returns empty (local tag gone); `git ls-remote origin "refs/tags/v1.20.0"` returns empty (remote tag gone).

**Step 2 — Correct `guides/introduction/contract.md:9` (per D-13).**

In `guides/introduction/contract.md`, on line 9 (RESEARCH.md confirms it reads "The current published
package truth before the release PR is `1.20.0`"), replace the version reference from `1.20.0` to
`1.1.0`. Only change the version number; preserve surrounding sentence structure.

Verify: `grep "1.20.0" guides/introduction/contract.md` returns zero matches.

**Step 3 — Document hex retire runbook in SUMMARY.md (per D-14).**

When writing the 214-05-SUMMARY.md, include a RUNBOOK section with exact commands for Jon to run
to retire the published `1.20.0` Hex release:

```
## Hex Retire Runbook (Manual — Jon to run)

The git tag v1.20.0 has been deleted from local and remote. The Hex registry still lists
sigra 1.20.0 as a published version. To prevent Hex from resolving {:sigra, "~> 1.0"} to
1.20.0 (which outranks 1.1.0 in Hex version ordering), retire it:

1. Ensure you have Hex publishing credentials:
   mix hex.user auth

2. Retire the version (retire, NOT delete — past the grace window for deletion):
   mix hex.retire sigra 1.20.0 invalid --message "Accidental milestone-version tag; correct version is 1.1.0"

3. Verify retirement:
   mix hex.info sigra 1.20.0
   (Should show "This package has been retired")

Note: Hex retire marks the version as retired/invalid without removing it. Future dependency
resolution will warn users not to use it and prefer other versions.
```

DEBT-04 is marked resolved once the git tag is deleted and contract.md is corrected. The Hex retire
is tracked as a manual runbook step — it cannot be automated in-phase (requires interactive Hex credentials).
  </action>
  <verify>
    <automated>git tag -l "v1.20.0" | wc -l | tr -d ' '; grep -c "1\.20\.0" guides/introduction/contract.md</automated>
  </verify>
  <done>git tag -l returns empty (0 lines); grep on contract.md returns 0 — stray 1.20.0 reference eliminated; hex retire runbook written in SUMMARY.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry → host app dependency resolution | v1.20.0 git tag deletion prevents Hex from resolving sigra 1.20.0 as the "latest" for ~> 1.0 ranges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214-05-01 | Tampering | git tag v1.20.0 | low | mitigate | Delete local + remote tag; document Hex retire runbook for Jon. Mitigation is the subject of this plan. |
| T-214-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |

No new attack surface. Script retirement and doc correction are purely additive (comment + version number fix).
</threat_model>

<verification>
After both tasks:
- `grep -c "RETIRED" scripts/ci/panel-schema-check.sh` returns 1
- `grep -c "panel-schema-check" .github/workflows/ci.yml` returns 0 (unchanged — confirmed)
- `git tag -l "v1.20.0"` returns empty
- `grep "1.20.0" guides/introduction/contract.md` returns empty
- `grep "1.1.0" guides/introduction/contract.md` shows the corrected line
- DEBT-02 todo moved to resolved/
- 214-05-SUMMARY.md contains the hex retire runbook
</verification>

<success_criteria>
- panel-schema-check.sh has RETIRED banner with rationale (D-05); not wired into CI
- WR-01/IN nits from phase-209 review are closed as won't-fix (D-06, D-07)
- git tag v1.20.0 deleted local and remote (D-13)
- contract.md:9 reads 1.1.0 (D-13)
- Hex retire runbook documented in SUMMARY.md for Jon (D-14)
- DEBT-02 and DEBT-04 todos closed
</success_criteria>

<output>
Create `.planning/phases/214-debt-robustness-clear/214-05-SUMMARY.md` when done.
Close todo `.planning/todos/pending/2026-07-01-phase209-code-review-deferred.md` (move to resolved/).

Include a RUNBOOK section in the SUMMARY.md with the mix hex.retire commands for Jon to run manually.
</output>
