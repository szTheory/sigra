---
phase: 193-baseline-observability-one-line-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md
autonomous: true
requirements: [BASE-01, BASE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "A committed baseline artifact records per-job CI durations, critical path, cache hit/miss, required-vs-not, quality signal, and likely bottleneck for the before-state."
    - "The artifact records Elixir-side diagnostics (slowest tests, schedulers_online, slow compile modules) as the named optimization target every later phase measures against."
  artifacts:
    - path: ".planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md"
      provides: "Before-state CI performance baseline + Elixir diagnostics (the reference for phases 194-198)"
      contains: "critical path"
      min_lines: 60
  key_links:
    - from: ".planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md"
      to: ".github/workflows/ci.yml"
      via: "per-job table derived from ci-gate.needs (required lanes) + gh run view job timings"
      pattern: "ci-gate|library_tests|example_playwright_smoke"
---

<objective>
Capture the before-state CI performance baseline and Elixir-side diagnostics as a committed planning artifact (BASE-01, BASE-02). This is the measure-before-optimize reference that every later phase (194-198) diffs against. Read-only data gathering only — no `ci.yml`, spec, or runtime-code changes in this plan.

Purpose: Without a committed baseline, later "we made CI faster" claims are unfalsifiable. This artifact is the falsifiable reference.
Output: `.planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md` with the SEED-005 §3 per-job column set, critical-path prose, and the BASE-02 Elixir diagnostics.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/seeds/SEED-005-ci-cd-pipeline-performance-audit.md
@.planning/phases/193-baseline-observability-one-line-wins/193-RESEARCH.md
@.planning/phases/193-baseline-observability-one-line-wins/193-PATTERNS.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather CI run data and write the per-job baseline table (BASE-01)</name>
  <files>.planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md</files>
  <read_first>
    - .planning/seeds/SEED-005-ci-cd-pipeline-performance-audit.md (§3 verbatim audit-playbook column set + critical-path prose requirements)
    - .planning/phases/193-baseline-observability-one-line-wins/193-RESEARCH.md ("Baseline Artifact (BASE-01)", "CI Job Graph", "CRIT-01: the serialization, proven" sections — has the exact gh commands and the verified run 27846034918 timings)
    - .planning/phases/193-baseline-observability-one-line-wins/193-PATTERNS.md ("193-BASELINE.md — BASE-01/02" section — column list + data-gathering commands)
    - .github/workflows/ci.yml (confirm the 23 jobs and the ci-gate.needs 10-lane required list at the ci-gate job, currently near line 1177; do NOT modify)
  </read_first>
  <action>
    Create `.planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md`. Gather data read-only with the authenticated `gh` CLI v2.94.0: use `gh run view <RUN_ID> --json jobs` with a jq expression computing `(.completedAt|fromdateiso8601)-(.startedAt|fromdateiso8601)` per job for per-job durations, and `gh run view <RUN_ID> --json createdAt,updatedAt` for run-level wall-clock. Use the run REQUIREMENTS.md cites (27846034918) as the primary sample; pull `gh run list --workflow CI --limit 20 --json databaseId,event,conclusion,createdAt,updatedAt` for additional recent runs and report p95 with an explicit sample-size note (n<5 means point estimate, per RESEARCH Pitfall 2 — never present p95 as if many runs exist). Read cache hit/miss for the snapshot from step logs via `gh run view --job <JOB_ID> --log | grep -iE "cache (restored|not found|hit|miss)"` (no cache step has an `id:` yet, so there is no machine-readable cache-hit today). Write a per-job table with the SEED-005 §3 verbatim columns: workflow name, trigger, job name, runner, matrix dimensions, services/containers, command(s), average duration, p95 duration, failure/rerun rate, cache usage, required-for-merge, quality signal, likely bottleneck, notes. Derive the required-for-merge column from the ci-gate.needs 10-lane list (install_golden_contract, library_tests, library_tests_dep_off, install_smoke, upgrade_smoke, example_http_smoke, example_playwright_smoke, generated_admin_playwright_smoke, snapshot_drift_guard, quality_ledger_monotonic). Add a "Critical path" prose section naming the long poles (example_playwright_smoke ~22m, library_tests ~16m, library_tests_dep_off ~14m), which jobs gate merge, which run in parallel, which determines wall-clock, and that example_playwright_smoke currently starts ~2s after library_tests ends (the gratuitous serialization CRIT-01 removes in plan 03). Do NOT modify ci.yml, the spec, or any runtime file — this plan is read-only data capture into one new markdown file.
  </action>
  <verify>
    <automated>test -f .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md && grep -qi "critical path" .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md && grep -q "example_playwright_smoke" .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md && grep -qi "required" .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `193-BASELINE.md` exists and contains a "critical path" heading/section, names `example_playwright_smoke`, and includes a required-for-merge column derived from the ci-gate 10-lane list.
    - Behavior assertion: the per-job table includes the SEED-005 §3 columns (duration, p95, cache usage, required-for-merge, quality signal, likely bottleneck) for the required lanes.
    - p95 figures carry an explicit sample-size note; no p95 is presented without one.
    - No file other than `193-BASELINE.md` is modified by this task.
  </acceptance_criteria>
  <done>The baseline artifact exists with the SEED-005 §3 per-job columns + critical-path prose; required-vs-not is correct against ci-gate.needs; p95 is honestly sample-sized.</done>
</task>

<task type="auto">
  <name>Task 2: Capture Elixir-side diagnostics into the baseline as the optimization target (BASE-02)</name>
  <files>.planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md</files>
  <read_first>
    - .planning/phases/193-baseline-observability-one-line-wins/193-RESEARCH.md ("Elixir Diagnostics (BASE-02)" section — exact invocations + the 2-core/schedulers_online=2 fact)
    - .planning/phases/193-baseline-observability-one-line-wins/193-PATTERNS.md ("BASE-02 diagnostics to record" command list)
    - CLAUDE.md ("Local development prerequisites" — `scripts/db/up.sh` then `source tmp/db.env` to bring up the test Postgres before `mix test`)
    - .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md (the artifact created in Task 1 — append the diagnostics section)
  </read_first>
  <action>
    Bring up the local test Postgres per CLAUDE.md (`scripts/db/up.sh` then `source tmp/db.env`; if the shared local Postgres is saturated, fall back to a disposable PG per the project's documented pattern). Capture and record into a "Elixir diagnostics (optimization target)" section of `193-BASELINE.md`: (1) `mix test --slowest 20` output (the N slowest tests and slowest modules) — this is the primary BASE-02 deliverable targeting the library_tests / library_tests_dep_off long poles; (2) `System.schedulers_online()` and `System.schedulers()` via `elixir -e 'IO.inspect({System.schedulers_online(), System.schedulers()}, label: :schedulers)'`, and record that the CI runner is `ubuntu-latest` = 2 vCPU so the CI value is 2 (ExUnit default max_cases = 2 * schedulers_online = 4) — this is the quantified motivation for partitioning in phase 195; (3) slow compile modules via `MIX_ENV=test mix compile --force --profile time` (record the top slow `Compiled <module> in Nms` lines) and optionally `mix xref graph --label compile-connected` for the compile-connected chains. Label local-vs-CI values clearly (local core count may differ from the CI 2-core figure; both must appear). Do NOT change any source/test/workflow file — diagnostics are recorded into the baseline markdown only.
  </action>
  <verify>
    <automated>grep -Eq "slowest|schedulers_online" .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md && grep -q "compile" .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `193-BASELINE.md` contains a diagnostics section matching `slowest` and `schedulers_online` and references compile timing.
    - Behavior assertion: the recorded `schedulers_online` for CI is stated as 2 (ubuntu-latest 2 vCPU), with the ExUnit max_cases implication noted as the partitioning motivation.
    - The `mix test --slowest 20` slowest-tests output is captured verbatim or summarized with concrete timings.
    - No runtime/test/workflow file is modified.
  </acceptance_criteria>
  <done>The baseline records the slowest tests, schedulers_online (CI=2), and slow compile modules as the named optimization target for phases 194-198.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → `gh` CLI → GitHub API | Read-only run-data fetch; no write scope exercised |
| local `mix`/`elixir` → test Postgres | Diagnostics run against an ephemeral local DB; no production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-01-I | Information Disclosure | committed `193-BASELINE.md` | accept | Only public CI timing + test-name data is recorded; no secrets, tokens, or PG creds are pasted into the artifact (review before commit). |
| T-193-01-T | Tampering | `gh`/`mix` invocations | accept | All commands are read-only data gathering; no workflow, action pin, or runtime file is touched, so no supply-chain surface is altered. |
</threat_model>

<verification>
- `test -f .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md` and `grep -qi "critical path"` on it.
- `grep -Eq "slowest|schedulers_online"` on the baseline artifact.
- `git diff --name-only` shows only `193-BASELINE.md` added/modified (no ci.yml/spec/runtime drift).
</verification>

<success_criteria>
- BASE-01: committed baseline table with the SEED-005 §3 columns + critical-path prose; required-vs-not correct against ci-gate.needs.
- BASE-02: slowest tests, schedulers_online (CI=2), and slow compile modules recorded as the optimization target.
- Zero changes outside the one new markdown artifact.
</success_criteria>

<output>
Create `.planning/phases/193-baseline-observability-one-line-wins/193-01-SUMMARY.md` when done.
</output>
