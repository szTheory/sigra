---
phase: 193-baseline-observability-one-line-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/demo-showcase.spec.ts
  - .planning/todos/pending/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md
  - .planning/todos/completed/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md
autonomous: true
requirements: [FLAKE-01]
nyquist_compliant: true

must_haves:
  truths:
    - "The demo-showcase remember-checkbox accent-color assertion passes deterministically across repeated runs with retries OFF (no reliance on retries: 1 to mask off-by-one rgb)."
    - "The FLAKE-01 todo is closed (moved from pending/ to completed/)."
  artifacts:
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts"
      provides: "De-flaked per-channel-tolerance color assertion using the in-file rgbChannels() parser"
      contains: "toBeLessThanOrEqual"
    - path: ".planning/todos/completed/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md"
      provides: "Closed FLAKE-01 todo (state hygiene)"
  key_links:
    - from: "test/example/priv/playwright/tests/demo-showcase.spec.ts"
      to: "test/example/priv/playwright/tests/demo-showcase.spec.ts"
      via: "the de-flaked assertion reuses the in-file rgbChannels() parser (line 52)"
      pattern: "rgbChannels\\("
---

<objective>
De-flake the demo-showcase "remember me" checkbox accent-color assertion (FLAKE-01) by replacing the exact rgb-string equality with a ±2 per-channel tolerance using the file's existing `rgbChannels()` parser, then close the tracking todo. No blanket retries — `retries: 1` already exists and must NOT be the fix.

Purpose: An exact `toBe` on a rendered `:checked` paint flakes by 1-2 rgb units per channel (sub-pixel/color-mix rounding) and only passes because `retries: 1` masks it — which the project's guardrails forbid as a resting state.
Output: a deterministic assertion (passes with `--retries=0` across repeated runs) plus the todo moved pending → completed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/193-baseline-observability-one-line-wins/193-RESEARCH.md
@.planning/phases/193-baseline-observability-one-line-wins/193-PATTERNS.md
@test/example/priv/playwright/tests/demo-showcase.spec.ts
@.planning/todos/pending/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the exact color toBe with ±2 per-channel tolerance and close the todo (FLAKE-01)</name>
  <files>test/example/priv/playwright/tests/demo-showcase.spec.ts, .planning/todos/pending/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md</files>
  <read_first>
    - test/example/priv/playwright/tests/demo-showcase.spec.ts (the file being modified — read the `rgbChannels()` parser at line 52, the test block `home page orients evaluators before login` at line 403, the styles-read evaluate block around lines 843-882, and the exact flaky assertion at lines 884-887; confirm current line numbers by grep rather than trusting these)
    - .planning/phases/193-baseline-observability-one-line-wins/193-RESEARCH.md ("FLAKE-01: Demo-Showcase Remember-Checkbox Color" section — failure signature, the ±2 tolerance fix, and the forbidden-retries guardrail)
    - .planning/phases/193-baseline-observability-one-line-wins/193-PATTERNS.md ("demo-showcase.spec.ts — FLAKE-01" section — the exact before/after and scope discipline)
    - .planning/todos/pending/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md (the todo to close; acceptance criteria)
  </read_first>
  <action>
    In `test/example/priv/playwright/tests/demo-showcase.spec.ts`, replace ONLY the exact `expect(rememberCheckedStyles.backgroundColor).toBe(rememberCheckedStyles.expectedAccent)` assertion (currently lines 885-887, inside the `home page orients evaluators before login` test at line 403) with a per-channel tolerance check that reuses the in-file `rgbChannels()` parser (line 52): parse both `rememberCheckedStyles.backgroundColor` and `rememberCheckedStyles.expectedAccent` into `[r,g,b]` channels and assert `Math.abs(channel - expected) <= 2` for each of the three channels using `toBeLessThanOrEqual(2)`. Keep every surrounding assertion unchanged: the `appearance` `toBe("none")` (line 884), `beforeContent`/`beforeDisplay` (888-889), `afterOpacity` `toBe("1")` (893), and the existing `expect.poll(...).toBe("1")` settle-wait on `::after` opacity (lines 836-842) all stay as-is. Do NOT touch the same-family `afterBackgroundColor` vs `expectedOnAccent` exact check at lines 890-892 — the todo does not evidence it as flaky (RESEARCH Open Question 2); leave it untouched. FORBIDDEN: do not add, raise, or rely on `retries` anywhere; do not edit `playwright.config.ts` `retries: 1` (line 50). Then close the todo by moving `.planning/todos/pending/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md` to `.planning/todos/completed/` (use `git mv` to preserve history; the completed/ directory already exists).
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium --retries=0` passes (deterministic with retries OFF); run it repeatedly to confirm no flake.
    - Source assertion: the spec contains a per-channel `toBeLessThanOrEqual(2)` comparison built from `rgbChannels(...)` for the remember-checkbox background color, and no longer contains an exact `toBe(rememberCheckedStyles.expectedAccent)` on `backgroundColor`.
    - Source assertion: `playwright.config.ts` `retries` is unchanged (no diff to line 50); no `retries` value was added or raised anywhere in this change.
    - Source assertion: the `afterBackgroundColor` exact check (lines 890-892) is unchanged.
    - State assertion: `.planning/todos/pending/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md` no longer exists and `.planning/todos/completed/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md` exists.
  </acceptance_criteria>
  <done>The accent-color assertion is deterministic with retries=0, only the evidenced assertion changed, no retry config touched, and the todo is moved to completed/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright spec → rendered DOM | The assertion reads a computed color from the browser; the only change is comparison tolerance, no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-02-T | Tampering | flake-masking via retries | mitigate | The fix is a deterministic per-channel tolerance, NOT a retry bump; `playwright.config.ts` `retries: 1` is left untouched and is not relied on to pass. Acceptance requires `--retries=0` green. |
| T-193-02-R | Repudiation | lost test coverage | accept | Tolerance (±2) preserves the assertion's value (the checked accent still must render within 2 units of the brand token); coverage is not deleted, only de-flaked. |
</threat_model>

<verification>
- `cd test/example/priv/playwright && npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium --retries=0` passes on repeated runs.
- `grep -n "toBeLessThanOrEqual" test/example/priv/playwright/tests/demo-showcase.spec.ts` shows the new per-channel checks.
- `git diff playwright.config.ts` is empty (retries untouched).
- `test -f .planning/todos/completed/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md` and `! test -f .planning/todos/pending/2026-06-19-demo-showcase-remember-checkbox-color-flaky.md`.
</verification>

<success_criteria>
- FLAKE-01: the remember-checkbox accent-color assertion passes deterministically with retries OFF; only the evidenced assertion changed; no retry config altered; todo closed.
</success_criteria>

<output>
Create `.planning/phases/193-baseline-observability-one-line-wins/193-02-SUMMARY.md` when done.
</output>
