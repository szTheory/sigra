---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/playwright.config.ts
  - test/example/priv/playwright/tests/admin-design.setup.ts
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/sigra/planning/phase_232_playwright_economics_test.exs
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
autonomous: true
requirements: [PW-01]
assumption_delta_decision:
  noun: "Playwright seam execution order"
  decision: no-change
  rationale: "The detector fired only on the temporal phrase 'one after another'; it does not describe a singular-to-plural identity-model transition, so no identity migration is warranted."

must_haves:
  truths:
    - "D-01: each design project has its own explicit setup dependency, policy-valid platform-admin+ identity, and distinct ephemeral storageState path."
    - "D-02: every design test navigates to /admin/_design and waits for LiveView attachment plus Space Grotesk readiness without registering in beforeEach, sleeping, or adding retries."
    - "The existing gallery loading state remains gated by .phx-connected, document.fonts.ready, and document.fonts.check('16px \"Space Grotesk\"')."
    - "The populated gallery retains 13 component boards, 11 group boards, 4 configuration boards, existing IDs, assertions, axe scans, and snapshots."
    - "Desktop and iPhone overflow behavior, responsive navigation overflow, and desktop-light/mobile/dark render contexts remain unchanged."
    - "No LiveView, route, markup, CSS, design token, Rail Accent asset, copy, interaction, or Light/Dark/System behavior changes in this phase."
  assumptions:
    - "FLAGGED/UNRESOLVED PW-01 adjacency probe: the source artifacts do not define special semantics for setup definitions that are adjacent or textually equal; the plan assumes project names and state paths, not declaration adjacency, determine separation."
    - "FLAGGED/UNRESOLVED PW-01 empty probe: the source artifacts do not specify an empty, missing, or null storageState fallback; the plan assumes a missing state is a hard setup/test failure and does not invent fallback authentication."
    - "FLAGGED/UNRESOLVED PW-01 ordering probe: the source artifacts do not specify stable ordering when setup projects compare equal; the plan assumes project names are unique, so equality is invalid rather than order-bearing."
  prohibitions:
    - statement: "MUST NOT change rendered admin behavior, markup, styling, brand assets, theme behavior, board inventory, or application copy to make authentication reuse pass."
      reason: "Bespoke product-integrity constraint surfaced by the spec-less two-stage recall/precision probe; descriptor-less and flagged-unverified."
    - statement: "MUST NOT make the gallery appear covered by silently deleting assertions, snapshots, axe scans, projects, or stable readiness checks."
      reason: "Bespoke coverage-honesty constraint surfaced by the spec-less two-stage recall/precision probe; descriptor-less and flagged-unverified."
  artifacts:
    - path: test/example/priv/playwright/tests/admin-design.setup.ts
      provides: "three project-specific authentication setup paths"
    - path: test/sigra/planning/phase_232_playwright_economics_test.exs
      provides: "non-vacuous structural contracts for Phase 232"
    - path: .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
      provides: "ordered PW-01 and shard-run receipt ledger"
  key_links:
    - from: test/example/priv/playwright/playwright.config.ts
      to: test/example/priv/playwright/tests/admin-design.setup.ts
      via: "three named setup dependencies and matching storageState files under project output directories"
    - from: test/example/priv/playwright/tests/admin-design.spec.ts
      to: "/admin/_design"
      via: "navigation followed by LiveView and font readiness"
---

<objective>
Replace per-test design-board registration with one reusable authenticated session per design project, while proving one chromium path end-to-end before expanding the same production-quality pattern to mobile and dark (D-01, D-02).

Purpose: remove the dominant repeated authentication cost without changing coverage or UI behavior.
Output: setup projects, distinct ephemeral auth states, a readiness-only design beforeEach, Phase 232 structural contracts, and the initial evidence ledger.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one chromium design session end-to-end (D-01, D-02)</name>
  <files>test/example/priv/playwright/playwright.config.ts, test/example/priv/playwright/tests/admin-design.setup.ts, test/example/priv/playwright/tests/admin-design.spec.ts, test/sigra/planning/phase_232_playwright_economics_test.exs, .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md</files>
  <read_first>
    - `test/example/priv/playwright/playwright.config.ts` — existing project names, viewport/theme use values, output settings, retry/worker posture, and ADMIN_DESIGN_SPEC matching.
    - `test/example/priv/playwright/tests/admin-design.spec.ts` — registerUser, beforeEach, waitForLiveViewReady, project-derived identity helper, role/stable-hook selectors, axe and snapshot inventory.
    - `test/example/priv/playwright/helpers/fixtures.ts` — canonical TEST_PASSWORD test-only value.
    - `test/example/lib/example/sigra_admin_policy.ex` — exact platform-admin+ policy prefix.
    - `test/sigra/planning/phase_230_design_gallery_split_test.exs` — File.read!/scoped extraction/non-vacuous contract style.
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md` and `scripts/ci/ci-run-metrics.sh` — evidence slot format and measurement command.
    - `guides/reference/admin-ui-principles.md` and `guides/reference/admin-design-contract.md` — deterministic selector/readiness/no-sleeps and UI preservation contract.
    - Every file listed in `<files>` — establish current content or absence before editing.
  </read_first>
  <behavior>
    - "admin-design-chromium depends on exactly one chromium setup project and consumes its unique storageState path."
    - "The setup registers a unique platform-admin+ identity through the UI, observes the success redirect/alert, and writes state below Playwright output."
    - "The chromium design spec reaches /admin/_design authenticated and retains LiveView/font readiness with no registerUser call in beforeEach."
  </behavior>
  <action>
    First add Phase 232 ExUnit assertions for the chromium tracer: explicit setup project, exact dependency, state path under an output directory, policy prefix, and readiness-only beforeEach. Confirm RED against the current files.

    Create `admin-design.setup.ts` using Playwright's setup-project pattern, TEST_PASSWORD, role-based submit/alert assertions, and an identity beginning `platform-admin+dg-chromium-` with a collision-resistant run suffix. Persist only the chromium context to a chromium-specific `.auth/*.json` path under Playwright output; create the parent directory before storageState writes and never print or upload its contents.

    In config, add the chromium setup project and make `admin-design-chromium` depend on it and consume the exact matching path while retaining its Desktop Chrome, video, viewport, and light-theme behavior. Move registration-only code out of `admin-design.spec.ts`; its beforeEach must only navigate to `/admin/_design` and execute the existing LiveView/font checks.

    Initialize `232-EVIDENCE.md` with the exact Phase 230 slot grammar and a `BEFORE-PW-01` section captured from the research-verified pre-change run `30390832059`, including the exact `gh run view`/metrics command, design-gallery job and step duration, assertion/snapshot/list counts available from that run, and an explicit statement that topology is still the pre-shard topology. Do not hand-calculate missing values; retain verbatim command output.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_economics_test.exs &amp;&amp; cd test/example/priv/playwright &amp;&amp; npx playwright test --list --project=admin-design-chromium --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 232 contract test was observed failing before production edits and passes after them.
    - `admin-design-chromium` names exactly one setup dependency and one chromium-only storageState path under Playwright output.
    - `admin-design.setup.ts` registers through UI roles with a `platform-admin+` email and writes state after the success assertion.
    - `admin-design.spec.ts` beforeEach contains navigation, `.phx-connected`, `document.fonts.ready`, and the Space Grotesk check, but no registration call, timeout sleep, retry, or broad text selector.
    - The chromium list command exits 0 with the same design-spec inventory as before.
    - `232-EVIDENCE.md` contains a captured `BEFORE-PW-01` slot citing run `30390832059` and its producing command.
  </acceptance_criteria>
  <done>One real design-project path authenticates once and reaches the unchanged gallery with deterministic readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand distinct authentication to mobile and dark without coverage drift (D-01, D-02)</name>
  <files>test/example/priv/playwright/playwright.config.ts, test/example/priv/playwright/tests/admin-design.setup.ts, test/sigra/planning/phase_232_playwright_economics_test.exs</files>
  <read_first>
    - `test/example/priv/playwright/playwright.config.ts` after Task 1 — chromium tracer mapping and unchanged mobile/dark viewport/colorScheme values.
    - `test/example/priv/playwright/tests/admin-design.setup.ts` after Task 1 — tracer registration/storage pattern to expand without sharing state.
    - `test/example/priv/playwright/tests/admin-design.spec.ts` — retained project inventory and readiness/axe/snapshot behavior.
    - `232-UI-SPEC.md` Interaction and Test Preservation Contract — all five preservation obligations.
    - Every file listed in `<files>` after Task 1.
  </read_first>
  <behavior>
    - "chromium, mobile, and dark each have one unique setup project, email suffix, and storage path."
    - "mobile retains iPhone 13 configuration and dark retains desktop Chromium plus dark colorScheme."
    - "all three project lists retain identical assertion/snapshot counts to the pre-change inventory."
  </behavior>
  <action>
    Extend the contract test first so it rejects missing, duplicate, or cross-wired setup names/state paths and verifies all three existing viewport/theme configurations. Then add mobile and dark setup projects and explicit setup tests, each using its own policy-valid identity and matching ephemeral state file. Wire each design project only to its namesake setup and state.

    Keep `fullyParallel`, `workers`, global retry configuration, spec selection, snapshot routing, role selectors, stable hooks, board IDs, axe scans, and all viewport/theme `use` values unchanged in this PW-01 plan. The observed proof later forces `--retries=0`; do not use retry configuration to hide a setup defect.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_economics_test.exs &amp;&amp; cd test/example/priv/playwright &amp;&amp; npx playwright test --list --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - The config exposes three and only three design setup projects and three distinct dependency/state mappings.
    - The setup file creates three unique `platform-admin+` identities and three ephemeral state files; no state path is shared or artifact-uploaded.
    - Chromium/light, iPhone 13/mobile, and Chromium/dark configuration values remain byte-equivalent to their pre-change values.
    - The combined list command exits 0 and records the same per-project design-test inventory used in the BEFORE receipt.
    - The Phase 232 contract test exits 0 and explicitly rejects a reintroduced design-spec registration call.
  </acceptance_criteria>
  <done>All three design projects authenticate once independently while preserving the complete gallery test surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| setup project → browser state file | An authenticated test session crosses into ephemeral filesystem state. |
| test identity → admin policy | The generated email determines platform-admin authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-232-01 | Spoofing / Information Disclosure | storageState files | high | mitigate | Keep three distinct files below cleaned Playwright output, exclude them from artifacts/logs, and contract-test uniqueness. |
| T-232-02 | Elevation of Privilege | setup email | medium | mitigate | Require the test-only `platform-admin+` prefix and unique suffix; do not alter production policy. |
| T-232-03 | Repudiation | coverage counts | medium | mitigate | Capture before/after list and observed-run counts under `--retries=0`. |
</threat_model>

## Artifacts this phase produces

- New path: `test/example/priv/playwright/tests/admin-design.setup.ts`.
- New Playwright projects: three explicit design authentication setup projects.
- New ephemeral paths: three project-specific `.auth/*.json` storageState files below Playwright output.
- New test identities: three unique `platform-admin+dg-*` identities.
- New path: `test/sigra/planning/phase_232_playwright_economics_test.exs`.
- New path and sections: `232-EVIDENCE.md`, including `BEFORE-PW-01`.
- No application symbols, routes, fields, CSS, flags, or production schema changes.

<verification>
Run the focused ExUnit contract and Playwright list command for all three projects; compare counts against the BEFORE ledger slot.
</verification>

<success_criteria>
PW-01 is implemented structurally with three distinct setup states, no per-test registration, preserved readiness, and a captured pre-change receipt ready for the ordered observed run.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-01-SUMMARY.md` when done.
</output>
