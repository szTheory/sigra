---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 03
type: execute
wave: 3
depends_on: ["232-02"]
files_modified:
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
autonomous: true
requirements: [PW-01]
must_haves:
  truths:
    - "D-03: BEFORE-PW-01 and AFTER-PW-01 are captured with verbatim run ids/commands before PW-02 changes begin."
    - "Identical assertion, snapshot, and project counts make the duration delta attributable to authentication reuse."
    - "The five UI-SPEC covered loading/populated/overflow states remain supported by the unchanged count receipt."
  assumptions:
    - "FLAGGED/UNRESOLVED PW-01 probe rows remain explicit in 232-01; observed equality of counts does not resolve adjacency/empty/ordering semantics absent from the SPEC."
  prohibitions:
    - statement: "MUST NOT report a PW-01 speedup without the verbatim run ids, producing commands, equal counts, and pre-shard topology proof."
      reason: "Descriptor-less, flagged-unverified evidence-integrity prohibition from the two-stage fallback probe."
  artifacts:
    - path: .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
      provides: "captured AFTER-PW-01 slot and attributable comparison"
  key_links:
    - from: 232-02-SUMMARY.md
      to: 232-EVIDENCE.md
      via: "validated run ids transcribed with commands and verbatim output"
---

<objective>
Commit the attributable PW-01 before/after receipt and close the ordering gate (D-03).

Purpose: establish the authentication-reuse win before any sharding or boot extraction can confound it.
Output: captured `AFTER-PW-01` evidence and a count/duration comparison.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture and compare the PW-01-only observed run (D-03)</name>
  <files>.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md</files>
  <read_first>
    - `232-EVIDENCE.md` — BEFORE slot grammar, run id, command, counts, and current topology statement.
    - `232-02-SUMMARY.md` — user-approved AFTER run id and count confirmation.
    - `scripts/ci/ci-run-metrics.sh` — canonical job-duration capture output.
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md` — captured/pending status and verbatim-output conventions.
    - The file listed in `<files>` before editing.
  </read_first>
  <action>
    Run `scripts/ci/ci-run-metrics.sh --jobs` for both approved run ids and use `gh run view ... --json jobs` plus the design-gallery log to capture the exact step duration, command, Playwright totals, assertion total, snapshot routing/count, event, head SHA, and conclusion.

    Add `AFTER-PW-01` in the inherited evidence-slot grammar, repeat its run id in status/body/command, and include verbatim output. Add a comparison table that shows both durations and count columns without rounding or recomputation. State explicitly that the AFTER head contains PW-01 only and the pre-shard topology. If any count differs or the AFTER command used retries, mark the slot failed and stop; do not characterize a speedup.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_economics_test.exs &amp;&amp; bash scripts/ci/ci-run-metrics.sh --jobs &lt;before-run-id&gt; &amp;&amp; bash scripts/ci/ci-run-metrics.sh --jobs &lt;after-pw01-run-id&gt;</automated>
  </verify>
  <acceptance_criteria>
    - `232-EVIDENCE.md` contains captured BEFORE-PW-01 and AFTER-PW-01 slots, each with a real run id and exact producing command.
    - Both slots show successful same-topology design-gallery execution; AFTER's head predates Phase 232 workflow topology edits.
    - Assertion, snapshot, project, and list counts are identical and `--retries=0` is visible in AFTER proof.
    - The duration comparison is verbatim-derived and does not claim a result if any count or topology differs.
    - The focused structural test exits 0 after the ledger edit.
  </acceptance_criteria>
  <done>PW-01 has a durable, re-fetchable performance receipt independent of sharding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub CLI output → committed evidence | External run data becomes a claimed performance result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-232-05 | Repudiation | evidence comparison | high | mitigate | Preserve commands/run ids/verbatim outputs and hard-stop on unequal counts or topology. |
</threat_model>

## Artifacts this phase produces

- New evidence section: `AFTER-PW-01`.
- New evidence table: exact BEFORE/AFTER PW-01 duration and coverage-count comparison.
- No production code, CI topology, UI, schema, fields, or flags.

<verification>
Focused contracts pass and both cited runs remain re-fetchable with the committed measurement command.
</verification>

<success_criteria>
D-03 is closed: PW-01 is landed, observed, and recorded before PW-02/PW-03 restructuring.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-03-SUMMARY.md` when done.
</output>
