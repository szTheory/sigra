---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 07
type: execute
wave: 6
depends_on: ["232-06"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [PW-03]
must_haves:
  truths:
    - "All six `ci.yml` prelude call sites — `example_unit_smoke`, `example_http_smoke`, `example_playwright_smoke`, `admin_design_recapture`, `admin_checkpoint_recapture`, `admin_eval_render` — invoke `./.github/actions/boot-example-app` instead of repeating the prelude steps (D-26)."
    - "Every per-site difference the call-site matrix recorded is expressed as a `with:` value, not absorbed into the action as a hidden special case: `MIX_ENV=test` and no boot for `example_unit_smoke`, boot without seeds for `example_http_smoke`, `PORT: 4011` and a one-path warm list for `admin_eval_render`, and the branched browser install only where a caller-owned browser cache exists."
    - "The docs-only `if:` conditions stay in `ci.yml` on the callers' `uses:` steps, so `p10`'s `stepIf()` and `honest-skip-verdict.sh` can still resolve them (Pitfall 7)."
    - "The Playwright browser cache step and its key literal remain in `ci.yml` inside `example_playwright_smoke`, so `scripts/ci/playwright-cache-key-guard.sh:56-62` still finds the key with no `--workflow` argument and needs no rewrite."
    - "Each caller's `Cache hit summary` step now reads the composite's exported `deps-cache-hit` output rather than an action-scoped internal step id, which the caller cannot see (D-29)."
    - "The two stale 'verbatim clone of ci.yml:NNN–NNN' comments at `:2008-2009` and `:2316-2317` are gone, because the duplication they describe is gone."
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "Six `- uses: ./.github/actions/boot-example-app` steps -> the single definition authored in plan 232-06; each carries `id: boot_example_app` so `p10` can resolve its `if:` near its `id:`."
    - "`example_playwright_smoke`'s caller-owned `actions/cache` for `~/.cache/ms-playwright` (id `playwright_browsers_cache`, key `ci.yml:1359`) -> the composite's `browser-cache-hit` input."
  prohibitions:
    - "MUST NOT change any job's `name:` string in this plan; five of them are hard-coded in `scripts/ci/docs-only-receipt.sh:41-49` and `scripts/ci/prohibitions/_lib.mjs:193-200`, and `MAINTAINING.md:102-113` says not to rename or remove them (D-24)."
    - "MUST NOT change any job's `timeout-minutes`; `p09:74-88` pins `example_playwright_smoke` at 45 against a measured value and forbids changing it without a fresh measurement."
    - "MUST NOT add or remove a step `id:` that `p02`, `p10`, or the seam aggregator loop at `ci.yml:1584-1589` reads — the six seam step ids and `design_gallery`'s three `--project` flags plus its grep flag must survive verbatim."
    - "MUST NOT move a docs-only `if:` inside the composite."
    - "MUST NOT change the deps-cache key shape; a changed shape orphans every warm entry and makes the first run after merge a cold compile on six jobs."
    - "MUST NOT restructure `example_playwright_smoke` into shards in this plan — that is PW-02, downstream by D-31."
---

<objective>
Collapse the six `ci.yml` copies of the boot prelude onto the single composite definition, one job at a
time, running the full guard sweep after each so a breakage is attributable to the job that caused it.

Purpose: SC-4's "exactly one definition referenced by the jobs that boot the app". The duplication is
already producing wrong documentation — `ci.yml:2008-2009` calls `admin_design_recapture` a "verbatim
clone of ci.yml:886–968", a range that has not contained the source for several phases, and
`:2316-2317` repeats the mistake.
Output: six converted call sites in `.github/workflows/ci.yml`, each with its variance in a `with:`
block, and the stale clone comments removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the two non-browser call sites — example_unit_smoke and example_http_smoke</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml:718-791` — `example_unit_smoke` in full: `MIX_ENV: test`, cache id `example_unit_deps_cache` at `:750`, per-step `if: needs.changes.outputs.docs_only != 'true'` on every step, the `Cache hit summary` step at `:785-790`, and the absence of node, browsers, seeds and boot
    - `.github/workflows/ci.yml:1159-1246` — `example_http_smoke` in full: cache id `http_smoke_deps_cache` at `:1188`, boot at `:1218`, the `Cache hit summary` at `:1240-1245`, and the fact that it is the only app-booting job with no seeds step
    - `.github/actions/boot-example-app/action.yml` — the input names, defaults and outputs as plan 232-06 authored them
    - `232-RESEARCH.md` § Composite Action Shape ▸ Call-Site Difference Matrix — the two columns for these jobs
    - `scripts/ci/prohibitions/p06-never-docs-gate-asserting-lanes.test.mjs` — which jobs may not carry `docs_only` tokens, so the conversion does not accidentally add one where it is forbidden
  </read_first>
  <action>
Replace the prelude steps in `example_unit_smoke` and `example_http_smoke` with one
`- uses: ./.github/actions/boot-example-app` step each, carrying `id: boot_example_app`. Convert these
two first: they are the edge cases of the input surface, so if the composite's defaults are wrong they
fail here rather than three jobs later.

`example_unit_smoke` — `with:` `mix-env: 'test'`, and leave `node`, `seeds`, `boot` at their `'false'`
defaults and `browsers` at `'none'` by stating them explicitly anyway. Stating a falsey input
explicitly rather than relying on a default is the C-5 lesson in practice: a future default change must
not silently alter a lane. Preserve the job's `if:` semantics by putting
`if: needs.changes.outputs.docs_only != 'true'` on the `uses:` step, since every prelude step it
replaces carried one.

`example_http_smoke` — `with:` `mix-env: 'dev'`, `boot: 'true'`, `seeds: 'false'`, `node: 'false'`,
`browsers: 'none'`, `server-log: /tmp/example-http-server.log`, `port: '4000'`, and `warm-paths` left
empty because this job does not warm routes. Add a comment on the `seeds: 'false'` line recording that
this is the only app-booting job in the repo that does not seed, so the value is deliberate and not an
oversight — RESEARCH C-5 shows `p15`'s own header comment currently gets this wrong. Put the docs-only
`if:` on the `uses:` step.

In both jobs, repoint the `Cache hit summary` step to read
`steps.boot_example_app.outputs.deps-cache-hit` instead of the now-removed caller-side cache step id.
The internal id is action-scoped and unreadable from the caller (D-29); the composite exports the value
precisely so these summary steps survive.

Leave everything after the prelude untouched: the ExUnit invocation in `example_unit_smoke`, the
`http-smoke.sh` invocation and log tail in `example_http_smoke`, both jobs' `name:` strings, and both
`timeout-minutes` values.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && mix test test/sigra/planning/ && bash scripts/ci/honest-skip-verdict.test.sh && bash scripts/ci/playwright-cache-key-guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - [L1] Each of `example_unit_smoke` and `example_http_smoke` contains exactly one `- uses: ./.github/actions/boot-example-app` step and no `actions/checkout`, `erlef/setup-beam`, `actions/setup-node`, or `actions/cache` step of its own.
    - [L1] Each converted `uses:` step carries both `id: boot_example_app` and an `if:` referencing `docs_only`.
    - [L1] Both jobs' `Cache hit summary` steps reference `steps.boot_example_app.outputs.deps-cache-hit` and no longer reference `example_unit_deps_cache` or `http_smoke_deps_cache`.
    - [L1] `example_http_smoke`'s `with:` block sets `seeds: 'false'` with an adjacent comment stating it is the only app-booting job that does not seed.
    - [L1] `git diff .github/workflows/ci.yml` shows no change to either job's `name:` or `timeout-minutes`.
    - [L1] `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` passes — `p09` (timeouts), `p10` (manifest resolution), `p06` (never-docs-gated lanes) all green.
    - [L1] `bash scripts/ci/honest-skip-verdict.test.sh` and `mix test test/sigra/planning/` pass.
  </acceptance_criteria>
  <done>The two edge-case call sites run through the composite with their variance stated explicitly, their cache-hit summaries repointed at the exported output, and every guard still green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert example_playwright_smoke, keeping the browser cache caller-owned</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml:1248-1420` — the fullest call site: `name:` at `:1249` (byte-identical to a required context — do not touch), `timeout-minutes: 45` at `:1251`, the prelude `:1270-1408`, the deps cache id `example_deps_cache` at `:1284`, the browser cache `:1327-1359` with its key at `:1359` and its rationale comment at `:1333-1355`, the branched install at `:1361-1375`, the boot at `:1376`, the warm loop at `:1389-1408`
    - `.github/workflows/ci.yml:1414-1620` — the six seam steps and their ids, the `Aggregate Playwright step outcomes` step at `:1563` with its six-id loop at `:1584-1589`, and the `Cache hit summary` at `:1610-1616` which reads both cache ids
    - `.github/actions/boot-example-app/action.yml` — the `browser-cache-hit` input and the `deps-cache-hit` output
    - `scripts/ci/playwright-cache-key-guard.sh:35-62` — the `--workflow` flag and the key-extraction grep that fails closed if the key literal leaves `ci.yml`
    - `232-RESEARCH.md` § Corrections C-4 and § Runtime State Inventory category 5 (the browser cache's `restore-keys` safety argument and its sole-consumer premise)
  </read_first>
  <action>
Convert `example_playwright_smoke`'s prelude to the composite while keeping the Playwright browser
cache step physically in `ci.yml`.

Keep, in the caller and unchanged: the `actions/cache` step for `~/.cache/ms-playwright` with
`id: playwright_browsers_cache`, its key literal at `:1359`, its `restore-keys` prefix, and the whole
rationale comment block at `:1333-1355`. Two things depend on that literal staying put —
`scripts/ci/playwright-cache-key-guard.sh` greps `ci.yml` for it and fails closed if it is absent, and
D-27's intent that no shared definition can ever restore browsers into a job that must not have them.
Order the steps so the browser cache step runs before the composite call, because the composite
consumes its result.

Then replace the remaining prelude steps with one `- uses: ./.github/actions/boot-example-app` step,
`id: boot_example_app`, carrying the docs-only `if:` the replaced steps carried, and `with:`
`mix-env: 'dev'`, `node: 'true'`, `seeds: 'true'`, `browsers: 'chromium webkit'`,
`browser-cache-hit: ${{ steps.playwright_browsers_cache.outputs.cache-hit }}`, `boot: 'true'`,
`port: '4000'`, `server-log: /tmp/example-playwright-server.log`, and `warm-paths` set to the same
eight-path list the warm loop iterates today, in the same order.

Repoint the `Cache hit summary` step at `:1610-1616`: its deps line reads
`steps.boot_example_app.outputs.deps-cache-hit`; its browser line keeps reading
`steps.playwright_browsers_cache.outputs.cache-hit`, which is still a caller-side step.

Change nothing else in this job. In particular: the `name:` at `:1249` stays byte-identical, the
`timeout-minutes: 45` stays, all six seam step ids stay, the `design_gallery` step keeps its three
`--project` flags and its grep-inversion flag verbatim (`p02` asserts all of them), the
`Stage admin checkpoint PNGs` gating on `steps.admin_checkpoints.outcome` stays, the six-id aggregator
loop stays, and the artifact bundle contract and upload steps stay. Sharding this job is PW-02's work
in plan 232-10, not this plan's.

Add one comment above the `uses:` step recording why the browser cache is above it rather than inside
the action, naming C-4's correction and the cache-key guard as the two reasons.
  </action>
  <verify>
    <automated>bash scripts/ci/playwright-cache-key-guard.sh && bash scripts/ci/playwright-cache-key-guard.test.sh && node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `bash scripts/ci/playwright-cache-key-guard.sh` passes with no arguments — the browser-cache key literal is still discoverable in `ci.yml`.
    - [L1] `example_playwright_smoke` contains exactly one `actions/cache` step (the browser cache) and one `- uses: ./.github/actions/boot-example-app` step, and the cache step precedes the composite call.
    - [L1] The composite call's `browser-cache-hit` input is wired to `steps.playwright_browsers_cache.outputs.cache-hit`.
    - [L1] `git diff .github/workflows/ci.yml` shows no change to the job's `name:`, its `timeout-minutes: 45`, any of the six seam step ids, the `design_gallery` step's project or grep flags, or the aggregator loop.
    - [L1] `node --test scripts/ci/prohibitions/p02-axe-signal-not-reduced.test.mjs` passes — the axe signal and the design step's invocation shape survived.
    - [L1] `mix test test/sigra/planning/phase_230_design_gallery_split_test.exs` passes — all nine tests, including the three job-scoped ones, are still green (they break in PW-02, not here).
    - [L1] `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` passes in full.
  </acceptance_criteria>
  <done>The largest duplicated block is gone from the critical-path job, the browser cache is demonstrably still caller-owned and still findable by its guard, and every contract that pins this job is green.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the three non-PR jobs and delete the stale clone comments</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml:1989-2089` — `admin_design_recapture`: the stale "verbatim clone of ci.yml:886–968" comment at `:2008-2009`, cache id `recapture_example_deps_cache` at `:2022`, seeds at `:2048`, boot at `:2062`, a plain (unbranched) browser install, and no per-step docs-only `if:`
    - `.github/workflows/ci.yml:2297-2390` — `admin_checkpoint_recapture`: the second stale clone comment at `:2316-2317`, cache id at `:2330`, seeds at `:2356`, boot at `:2370`
    - `.github/workflows/ci.yml:2547-2690` — `admin_eval_render`: job-level `PORT: "4011"` at `:2558-2559`, the deps cache id `admin_eval_render_deps_cache` at `:2582`, the absence of any browser cache, seeds at `:2608`, boot at `:2622`, the single-route warm at `:2632`, and the `Cache hit summary` at `:2673-2678`
    - `.github/workflows/ci.yml:2553-2557` — the recorded lesson that `config.exs:39` bakes `PORT` as a `compile_env` key, so a non-4000 port must agree at compile and run time (D-16)
    - `232-RESEARCH.md` § Common Pitfalls ▸ Pitfall 10 (the stale-comment cascade) and § Composite Action Shape ▸ Call-Site Difference Matrix
  </read_first>
  <action>
Convert the three remaining `ci.yml` call sites and clean up the comments the duplication left behind.

`admin_design_recapture` and `admin_checkpoint_recapture` — one `- uses: ./.github/actions/boot-example-app`
step each, `id: boot_example_app`, with `mix-env: 'dev'`, `node: 'true'`, `seeds: 'true'`,
`browsers: 'chromium webkit'`, `boot: 'true'`, `port: '4000'`, the job's existing distinct
`server-log` path (`/tmp/example-recapture-server.log` and
`/tmp/example-checkpoint-recapture-server.log` respectively), and the same eight-path `warm-paths`
list. Leave `browser-cache-hit` unset: neither job has a browser cache, so the empty default selects
the full install branch — which is exactly the plain `install --with-deps` these jobs run today. Neither
job carries per-step docs-only conditions, so add no `if:` to the `uses:` step in these two.

`admin_eval_render` — same shape, but `port: '4011'`, `server-log:
/tmp/example-admin-eval-render-server.log`, and `warm-paths` containing only `/admin/_design`. Keep the
job-level `env: PORT: "4011"` where it is, so the value agrees at compile time and run time, and keep
the `:2553-2557` comment explaining why. Leave `browser-cache-hit` unset — this job's absence of a
browser cache is the structural guarantee C-4 clarified and D-27's intent protects. Repoint its
`Cache hit summary` at `:2673-2678` to `steps.boot_example_app.outputs.deps-cache-hit`.

Then delete both stale clone comments. `ci.yml:2008-2009` claims `admin_design_recapture` is a
"verbatim clone of ci.yml:886–968" — a range that has not held the source for several phases (it is now
`:1270-1408`), and after this plan there is no clone at all. `:2316-2317` makes the same claim. Replace
each with one sentence naming the composite action as the shared definition and stating what this job
passes that differs. Prefer naming the action and the differing inputs over citing a line range; line
citations in comments are not machine-checked and this is the third time they have rotted.

Finally, sweep `ci.yml` for any other comment that describes the prelude as duplicated, and correct it
in this same commit.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && mix test test/sigra/planning/ && bash scripts/ci/honest-skip-verdict.test.sh && bash scripts/ci/docs-only-receipt.test.sh && bash scripts/ci/playwright-cache-key-guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `grep -c 'uses: ./.github/actions/boot-example-app' .github/workflows/ci.yml` returns 6.
    - [L1] `grep -n 'verbatim clone of' .github/workflows/ci.yml` returns nothing.
    - [L1] `admin_eval_render` contains no `actions/cache` step whose path mentions the Playwright browser cache directory, and its composite call sets no `browser-cache-hit` input.
    - [L1] `admin_eval_render`'s composite call sets `port: '4011'` and its job-level `env: PORT: "4011"` is unchanged.
    - [L1] All three jobs' `name:` strings and `timeout-minutes` values are unchanged in `git diff`.
    - [L1] `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` passes, including `p09`, `p10` and `p05`.
    - [L1] `bash scripts/ci/docs-only-receipt.test.sh` and `bash scripts/ci/honest-skip-verdict.test.sh` pass — the five required context names and the nine-lane list are undisturbed.
    - [L1] `mix test test/sigra/planning/` passes in full.
  </acceptance_criteria>
  <done>All six `ci.yml` call sites route through the one definition, `admin_eval_render` still has no browser cache by construction, and the two comments that described a duplication that no longer exists are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ci.yml` -> guard scripts | `p02`, `p09`, `p10`, `honest-skip-verdict.sh` and `playwright-cache-key-guard.sh` all read this file literally; a structural edit can blind them |
| Actions cache namespace -> six jobs | A key-shape change orphans warm entries across every converted job at once |
| Composite -> caller job env | `$GITHUB_ENV` writes inside the action leak outward into the caller's later steps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-29 | Repudiation | docs-only `if:` becoming invisible to `p10` / `honest-skip-verdict.sh` | high | mitigate | Conditions stay on the caller's `uses:` step with an `id:` beside them; every task runs `p10` and `honest-skip-verdict.test.sh` as an acceptance gate. |
| T-232-30 | Tampering (ASVS V14) | browser-cache key literal leaving `ci.yml`, blinding its fail-closed guard | high | mitigate | The browser cache step stays physically in `example_playwright_smoke`; `playwright-cache-key-guard.sh` is run with no `--workflow` argument as an acceptance criterion, exactly as `fast_checks:337` invokes it. |
| T-232-31 | Tampering | a renamed required-status-check context | high | mitigate | Prohibition against touching any `name:`; `docs-only-receipt.test.sh` (which hard-codes the five contexts) is an acceptance gate on the last task. |
| T-232-32 | Denial of Service | cache-key shape change causing six simultaneous cold compiles | medium | mitigate | Composite reuses the existing key shape with `mix-env` substituted for the hard-coded token; prohibition forbids changing the shape. |
| T-232-33 | Elevation | a dropped SHA pin during the move | high | mitigate | The pins live in the composite and were asserted in plan 232-06; this plan removes pinned steps from callers rather than rewriting them, and the full prohibition sweep runs after each task. |
| T-232-SC | Tampering | npm/pip/cargo installs | low | accept | Vacuous: no dependency manifest changes; `npm ci` and `mix deps.get` resolve against existing committed lockfiles. |
</threat_model>

## Artifacts this phase produces

This plan creates no new symbols; it converts call sites to symbols created in plan 232-06.

- **Scripts:** `scripts/ci/ci-step-metrics.sh`, `scripts/ci/ci-step-metrics.test.sh`,
  `scripts/ci/playwright-shard-nonempty.sh`, `scripts/ci/playwright-shard-nonempty.test.sh`
- **CI step names (new):** `CI step metrics self-test`, `Playwright shard non-emptiness self-test`
- **CI step id (new, this plan):** `boot_example_app` — the composite call step id, present once per
  converted job
- **CI job ids (new):** `example_playwright_shard`, `example_playwright_design`
- **CI job `name:` strings (new):** `Example Playwright smoke shard ${{ matrix.seam }}`,
  `Example Playwright design gallery`
- **Workflow inputs (new):** `force_shard_fail_probe` (`workflow_dispatch`)
- **Composite action:** `.github/actions/boot-example-app/action.yml` — inputs `mix-env`, `seeds`,
  `node`, `browsers`, `browser-cache-hit`, `boot`, `port`, `pg-database`, `server-log`, `warm-paths`; outputs
  `deps-cache-hit`, `otp-version`, `elixir-version`; internal step id `example_deps_cache`
- **Playwright:** `test/example/priv/playwright/tests/auth.setup.ts`,
  `test/example/priv/playwright/helpers/storageState.ts`; project name `design-auth-setup`; config
  consts `AUTH_SETUP_FILE`, `DESIGN_STORAGE_STATE`; storageState path
  `test/example/priv/playwright/.playwright/design-admin.json`; `json` reporter outputFile
  `.playwright/last-run-report.json`
- **Prohibition guards (new):** `p17-pw-evidence-run-id-provenance.test.mjs`,
  `p18-pw-no-green-on-empty-count.test.mjs`, `p19-pw-sc-restatement-recorded.test.mjs`,
  `p20-pw-no-lane-green-because-skipped.test.mjs`, `p21-pw-committed-method-provenance.test.mjs`
- **ExUnit contracts (new):** `test/sigra/planning/phase_232_playwright_auth_setup_test.exs`,
  `test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`,
  `test/sigra/planning/phase_232_playwright_shard_topology_test.exs`
- **Planning artifacts:** `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md`

<verification>
- `grep -c 'uses: ./.github/actions/boot-example-app' .github/workflows/ci.yml` — 6.
- `grep -n 'verbatim clone of' .github/workflows/ci.yml` — no output.
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` — green.
- `bash scripts/ci/playwright-cache-key-guard.sh`, `bash scripts/ci/playwright-cache-key-guard.test.sh`, `bash scripts/ci/honest-skip-verdict.test.sh`, `bash scripts/ci/docs-only-receipt.test.sh` — all green.
- `mix test test/sigra/planning/` — green.
</verification>

<success_criteria>
Six `ci.yml` jobs boot the example app through one composite call each, every site's difference is
visible in its `with:` block, the browser cache is still caller-owned and still findable by its
fail-closed guard, no required-check name or timeout moved, and the two comments describing a
duplication that no longer exists are gone.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-07-SUMMARY.md` when done.
</output>
