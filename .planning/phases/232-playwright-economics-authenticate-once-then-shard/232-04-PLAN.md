---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 04
type: execute
wave: 3
depends_on: ["232-03"]
files_modified:
  - test/example/priv/playwright/helpers/storageState.ts
  - test/example/priv/playwright/tests/auth.setup.ts
  - test/example/priv/playwright/playwright.config.ts
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/sigra/planning/phase_232_playwright_auth_setup_test.exs
autonomous: true
requirements: [PW-01]
must_haves:
  truths:
    - "The design gallery's `test.beforeEach` performs no user registration; the authenticated identity comes from a `storageState` written once by a Playwright setup project (D-01), logging in as the already-seeded `admin@demo.tasklane.test` persona through the existing `loginDemoAdmin` helper (D-02)."
    - "`auth.setup.ts` is matched by exactly two projects' `testIgnore` arrays — `chromium` (`playwright.config.ts:94`) and `mobile` (`:103-112`) — and by no other project's; and the setup project declares no `use.baseURL`, so its origin is byte-identical to the design projects' inherited `playwright.config.ts:76` value (D-04, D-06). storageState cookie origin matching is exact: `localhost:4000` and `127.0.0.1:4000` are different origins and the session silently does not apply."
    - "The setup ends with an explicit authenticated assertion before it writes state (D-05), because storageState auth failure is silent — an unauthenticated run just redirects to the login page with no exception and every downstream test then fails confusingly."
    - "One shared storageState file serves all three design projects across Chromium and WebKit (D-03); Sigra records `user_agent` at `lib/sigra/session_stores/ecto.ex:123` but never rejects on mismatch."
    - "The four `admin-design.spec.ts` text contracts in `test/sigra/planning/phase_230_design_gallery_split_test.exs:65-147` and `p02-axe-signal-not-reduced.test.mjs` stay green through this edit (D-10)."
    - "The design projects still execute the same number of tests: 41 per project, 28 of them tagged `@snapshot` — 39 on a PR step, 84 on the non-PR snapshot step, 123 ungrepped."
    - statement: "Under per-shard databases, rows in `/admin/users` that compare equal on `inserted_at` do not change the design specs' observed ordering — the specs filter by an explicit `q=` query rather than relying on position (`admin-design.spec.ts:448-450`), and after this change the design lane creates zero users at all."
      verification: backstop
  artifacts:
    - test/example/priv/playwright/tests/auth.setup.ts
    - test/example/priv/playwright/helpers/storageState.ts
    - test/sigra/planning/phase_232_playwright_auth_setup_test.exs
  key_links:
    - "`design-auth-setup` project <- `dependencies:` on each of `admin-design-chromium` (`:176-183`), `admin-design-mobile` (`:185-192`), `admin-design-dark` (`:195-203`), each with `use.storageState` pointing at the same absolute path."
    - "`auth.setup.ts` -> `helpers/adminFlows.ts` `loginDemoAdmin` (`:87-89`) -> `loginDemoUser` (`:65-81`); login mechanics are reused, never re-implemented."
    - "`helpers/storageState.ts` exports one absolute path consumed by both `playwright.config.ts` and `auth.setup.ts`, so a differing working directory cannot silently produce two paths."
  prohibitions:
    - "MUST NOT re-implement the login form fill in the setup file; reuse `loginDemoAdmin` (D-02), whose `adminFlows.ts:57-63` comment records why no MFA challenge fires despite the persona having TOTP enrolled."
    - "MUST NOT give the setup project its own `use.baseURL` — it must inherit the top-level value at `playwright.config.ts:76` (D-04)."
    - "MUST NOT name the setup file with a `.spec.ts` suffix (D-07)."
    - "MUST NOT add `testIgnore` entries to the five `testMatch`-scoped projects (`admin-checkpoints-*`, `admin-eval*`, `demo-showcase-*`, `admin-generated`) — they are structurally immune and touching them is scope the phase did not ask for."
    - "MUST NOT delete the `admin-design.spec.ts:267-272` comment explaining per-test context isolation; rewrite it, because left as-is it reads as contradicting the new code."
    - "MUST NOT change `test.describe('Design gallery board snapshots', () => {`, the `@snapshot` tag placement, the axe test's title, or the 13/11/4 board list — four contracts pin them (D-10)."
    - "MUST NOT change `.github/workflows/ci.yml` in this plan; per D-01 the two design invocations at `:1497-1503` and `:1525-1530` require no change, because `--grep`, `--grep-invert`, `--shard` and positional file args apply only to primary tests, never to dependency-project tests."
---

<objective>
Land PW-01: the design projects authenticate through a setup project and a shared `storageState`
instead of registering a fresh user before every one of 41 tests per project.

Purpose: `admin-design.spec.ts:273-278` calls `registerUser(...)` at `:275` inside `test.beforeEach`,
paying a full LiveView registration over dev-mode longpoll plus an Argon2id hash for every test in all
three design projects. D-13 prices the honest expectation up front: roughly 175s off the PR path (39
tests), with larger savings on the non-PR gallery and recapture lanes — PW-01 alone lands the critical
path near 13.6m, still above the 12m FAST-01 target, which is why PW-02 exists.
Output: `auth.setup.ts`, a shared absolute storageState path module, the config topology, the
`beforeEach` edit, and an ExUnit contract that pins the wiring.

Ordering (D-31): this is the "PW-01 land" step. Plan 232-05 is the "PW-01 measured" step, and no PW-02
or PW-03 work may start before it completes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup project, shared storageState path, and the three design projects' dependency wiring</name>
  <files>test/example/priv/playwright/helpers/storageState.ts, test/example/priv/playwright/tests/auth.setup.ts, test/example/priv/playwright/playwright.config.ts</files>
  <read_first>
    - `test/example/priv/playwright/helpers/adminFlows.ts:20-95` — `DEMO_ADMIN_EMAIL` (`:22`), `DEMO_ADMIN_PASSWORD` (`:25`), the no-MFA rationale comment (`:57-63`), `loginDemoUser` (`:65-81`), `loginDemoAdmin` (`:87-89`)
    - `test/example/priv/playwright/playwright.config.ts` (whole file) — the const idiom at `:24-44`, `use.baseURL` at `:76`, the `chromium` single-line `testIgnore` at `:94`, the `mobile` multi-line `testIgnore` at `:103-112`, and the three design project blocks at `:176-183`, `:185-192`, `:195-203`
    - `test/example/priv/playwright/.gitignore` — `.playwright/` is already ignored (line 4)
    - `test/example/lib/example/demo/personas.ex:50-80` — the seeded admin persona, and `test/example/lib/example/sigra_admin_policy.ex:1-35` — the email-match `platform_admin?` grant at `:19-24`
    - `232-PATTERNS.md` § `tests/auth.setup.ts` and § `playwright.config.ts` — the verbatim login helper body, the project block shape to copy, and both `testIgnore` arrays reproduced in full
    - `232-RESEARCH.md` § Architecture Patterns ▸ Pattern 1, and § Common Pitfalls ▸ Pitfall 6
  </read_first>
  <action>
Create `test/example/priv/playwright/helpers/storageState.ts` exporting a single const
`DESIGN_STORAGE_STATE`, computed as an absolute path with `path.join(__dirname, '..', '.playwright',
'design-admin.json')`. The subproject is CommonJS (`package.json` declares no `"type": "module"`), so
`__dirname` resolves under Playwright's TypeScript transform. An absolute path is deliberate: Pitfall 6
records that a relative `storageState` path resolved against a different working directory is one of
the three ways storageState silently fails, and both the config and the setup file must agree on one
string. Add a comment stating that `.playwright/` is gitignored, that this file must never be uploaded
as an artifact because it holds a real Sigra session cookie, and that the phase's `json` reporter also
writes into the same directory for the same reason.

Create `test/example/priv/playwright/tests/auth.setup.ts` — non-`.spec.ts` by name (D-07), placed under
`testDir` (`tests/`) so it is discoverable at all. It imports `test as setup` and `expect` from
`@playwright/test`, `loginDemoAdmin` from `../helpers/adminFlows`, and `DESIGN_STORAGE_STATE` from
`../helpers/storageState`. One `setup(...)` block named for what it does — authenticate as the seeded
demo platform admin. Its body: call `loginDemoAdmin(page)` (do not re-implement the form fill; D-02,
and `adminFlows.ts:57-63` records why no MFA challenge fires despite the persona having TOTP enrolled);
then perform an explicit authenticated assertion before writing state (D-05) — navigate to
`/admin/users`, assert the URL is not the login page, and assert a platform-admin-only element is
visible; then `await page.context().storageState({ path: DESIGN_STORAGE_STATE })`. Add a file header
comment recording: which persona and why it is authorised (`sigra_admin_policy.ex:19-24` grants
`platform_admin?` by email match); that the credential is a seeded, public-by-design demo credential
already committed at `adminFlows.ts:25` and not a secret; that seeds already run in every app-booting
job; and that the explicit assertion exists because storageState auth failure is otherwise silent.

Edit `playwright.config.ts`:
- Add a const beside the existing matchers at `:24-44`: `const AUTH_SETUP_FILE = /auth\.setup\.ts/;`
  with a comment naming its lane binding, so Phase 234's DX-04 spec inventory does not flag it as an
  unnamed lane (D-07).
- Import `DESIGN_STORAGE_STATE` from `./helpers/storageState`.
- Add a project `{ name: 'design-auth-setup', testMatch: AUTH_SETUP_FILE }` immediately before the
  three design project blocks. Give it no `use` block at all — it must inherit `use.baseURL` from
  `:76` byte-identically (D-04).
- On each of `admin-design-chromium`, `admin-design-mobile` and `admin-design-dark`, add
  `dependencies: ['design-auth-setup']` and, inside the existing `use:` block, `storageState:
  DESIGN_STORAGE_STATE`. Leave `testMatch`, `devices`, `colorScheme` and `video` exactly as they are.
- Add `AUTH_SETUP_FILE` to exactly two `testIgnore` arrays: `chromium` at `:94` and `mobile` at
  `:103-112` (D-06). Both are directory-wide ignore lists, so a new file in `tests/` is otherwise
  picked up as a normal spec by lanes that must not run it. Do not touch the five `testMatch`-scoped
  projects — they are structurally immune.
- Add a comment above the setup project recording that the setup runs once per `npx playwright test`
  invocation rather than once per dependent project (verified as RESEARCH finding E-4), and once per
  shard leg (E-1), which is the reason SC-1 is restated in `232-EVIDENCE.md`.

Do not change `workers`, `fullyParallel`, `retries`, or the reporter array in this plan.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test --list --project=admin-design-chromium --reporter=list | head -5 && npx playwright test --list --project=chromium --reporter=list | grep -c 'auth.setup' ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `npx playwright test --list --project=admin-design-chromium` from `test/example/priv/playwright` exits 0 and its listing includes a `design-auth-setup` entry, proving `--project=X` auto-pulls X's dependencies (D-01).
    - [L1] `npx playwright test --list --project=chromium` and `--project=mobile` both exit 0 and neither listing contains an `auth.setup` entry.
    - [L1] `npx playwright test --list --project=admin-checkpoints-chromium` exits 0 and its listing is unchanged from before this plan — the `testMatch`-scoped projects were not touched.
    - [L1] `helpers/storageState.ts` exports exactly one const and `git grep -n 'design-admin.json' -- test/example .github scripts` shows the literal only inside `helpers/storageState.ts`.
    - [L1] The `design-auth-setup` project block contains no `use` key at all, so `baseURL` is inherited from `playwright.config.ts:76`.
    - [L1] `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` and `mix test test/sigra/planning/` both pass.
  </acceptance_criteria>
  <done>A setup project exists, the three design projects depend on it and consume one shared absolute storageState path, and the setup file is invisible to exactly the two directory-wide-ignore projects and to nothing else.</done>
</task>

<!-- planner-discipline-allow: registerUser -->
<task type="auto">
  <name>Task 2: Remove the per-test registration from the design gallery and prove it live</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <precondition>The example app is running locally at exactly `http://localhost:4000` with seeds applied, and port 4000 is not held by another project's container. Establish with: `scripts/db/up.sh` then `source tmp/db.env`; from `test/example`, `MIX_ENV=dev mix ecto.create && MIX_ENV=dev mix ecto.migrate && MIX_ENV=dev mix run priv/repo/seeds.exs`; then background `MIX_ENV=dev PHX_SERVER=true mix phx.server`. Port 4000 is not negotiable — `config.exs:39` bakes `System.get_env("PORT","4000")` as a `compile_env` key and a different port trips `validate_compile_env` (D-16). Assert with `curl -sf http://localhost:4000/users/log_in`. If the port cannot be freed, halt and surface it rather than running the design projects against a different origin — D-04 makes origin mismatch a silent failure.</precondition>
  <read_first>
    - `test/example/priv/playwright/tests/admin-design.spec.ts:1-70` — the imports at `:1-3`, `waitForLiveViewReady` at `:27`, the registration helper at `:40-50`, the module-level `registrationSequence` at `:52`, and `adminDesignEmail` at `:54-64`
    - `test/example/priv/playwright/tests/admin-design.spec.ts:266-302` — the describe title at `:266` (pinned verbatim by ExUnit), the context-isolation comment at `:267-272` that must be rewritten rather than deleted, the `test.beforeEach` at `:273-278`, the tagged board loop at `:286-290`, and the untagged axe test at `:300-302`. Per **D-09**, do not anchor on the ROADMAP's citation of `admin-design.spec.ts:250-255` — it is stale; grep for the `test.beforeEach(` symbol and confirm the range before editing, because `ci.yml` and this spec have both moved since the ROADMAP was written
    - `test/example/priv/playwright/tests/admin-design.spec.ts:440-460` — the `/admin/users` `inserted_at DESC` comment at `:448-450` and the explicit `q=` filter, which is why ordering stays deterministic once the harness stops creating users
    - `test/sigra/planning/phase_230_design_gallery_split_test.exs:53-147` — the four assertions that pin this file's text (D-10)
    - `scripts/ci/prohibitions/p02-axe-signal-not-reduced.test.mjs` — the axe test title, tag-absence, and helper-shape assertions
    - `232-RESEARCH.md` § Runtime State Inventory (the "Stored data" row: after this change the design lane creates zero users, so the `q=` filter becomes redundant-but-correct and the pagination fixture must still be satisfied by the seeded admin)
  </read_first>
  <action>
Edit `test/example/priv/playwright/tests/admin-design.spec.ts` so the design gallery inherits its
authenticated session from the setup project rather than creating one per test.

In the `test.beforeEach` at `:273-278`: drop the per-test email derivation and the registration call at
`:275`, keeping `page.goto('/admin/_design')` and `await waitForLiveViewReady(page)`. Change the block
signature from `async ({ page }, testInfo)` to `async ({ page })` since `testInfo` is no longer used
there.

Then remove the symbols that become dead, and only those, after confirming with `git grep -n` inside
this file that nothing else references them: the registration helper at `:40-50`, the module-level
counter at `:52`, the per-test email builder at `:54-64`, and the `TEST_PASSWORD` import at `:3`.
Removing the module-level counter also closes D-18 item 4's per-worker-process hazard for this file.
Do not remove `waitForLiveViewReady` — it is used at `:427`, `:435`, `:452`, `:459`, `:461`, `:484`,
`:507` and elsewhere. Do not remove the `Page` or `TestInfo` type imports without checking their
remaining uses; `assertBoardScreenshot` still takes a `testInfo`.

Rewrite — do not delete — the comment at `:267-272`. Its current text explains that each Playwright
test runs in an isolated browser context so a `beforeAll` login on a separate page does not
authenticate the test pages. That fact is still true and is precisely why `storageState` (not
`beforeAll`) is the mechanism: storageState is applied to every new context at creation. The rewritten
comment must say that, name the `design-auth-setup` project as the source of the session and
`admin@demo.tasklane.test` as the identity, and keep the existing note that the gallery renders static
literal assigns so no email ever appears in a board screenshot. Left as-is the comment reads as
contradicting the new code.

Leave untouched: `test.describe('Design gallery board snapshots', () => {` at `:266`, the `@snapshot`
tag placement on the per-board `test(...)` at `:287`, the axe test's title and its untagged status, the
13/11/4 board lists, and `assertBoardScreenshot`'s element-scoped locator at `:108`. Four mechanical
contracts pin these (D-10) and the element scoping is what makes it safe to change the authenticated
identity without touching a single PNG baseline.

Finally, prove it live against the locally booted app. Run each of the three design projects at
`--retries=0` (the config sets `retries: process.env.CI ? 1 : 0`, so a local run is already 0, but pass
it explicitly so the command is the same one CI will use), confirm every project reports 41 passed with
zero snapshot diffs, and confirm the `design-auth-setup` test appears exactly once per invocation even
when two design projects are selected in one command (RESEARCH E-4). Record the observed counts in the
task's commit message. If any board PNG diffs, that is a signal the change reached further than
intended — investigate, do not recapture (CLAUDE.md's admin-UI directive and RESEARCH § Project
Constraints both say so).
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --retries=0 --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - [L3] The three-project invocation above passes with `123 passed` reported by the `list` reporter and zero snapshot diffs, against the locally booted app on `http://localhost:4000`.
    - [L3] The same invocation's output shows the `design-auth-setup` test executing exactly once, not three times — one login per invocation (RESEARCH E-4).
    - [L3] `jq '.stats' .playwright/last-run-report.json` after that invocation reports `expected: 123`, `unexpected: 0`, `flaky: 0`.
    - [L3] A single-project run at `--grep-invert '@snapshot'` reports 13 passed, and at `--grep '@snapshot'` reports 28 passed — the PR/non-PR split arithmetic is unchanged.
    - [L1] `mix test test/sigra/planning/phase_230_design_gallery_split_test.exs` passes — all four spec-text contracts still hold (D-10).
    - [L1] `node --test scripts/ci/prohibitions/p02-axe-signal-not-reduced.test.mjs` passes.
    - [L1] `git diff test/example/priv/playwright/tests/admin-design.spec.ts` shows the `:267-272` comment rewritten (not removed) and shows no change to the describe title, the tag placement, the axe test title, or the board lists.
    - [L1] `git status --porcelain test/example/priv/playwright/tests/admin-design.spec.ts-snapshots` is empty — no PNG baseline changed.
  </acceptance_criteria>
  <done>The design gallery runs 41 tests per project as the seeded platform admin with zero registrations, proven on a live local run at `--retries=0` with unchanged counts and unchanged baselines.</done>
</task>

<!-- planner-discipline-allow: --no-deps -->
<task type="auto">
  <name>Task 3: ExUnit contract pinning the setup-project topology</name>
  <files>test/sigra/planning/phase_232_playwright_auth_setup_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_230_design_gallery_split_test.exs:1-80` — the module preamble, the path-constant idiom, the "no YAML or TypeScript parser exists in this suite and none is added here" rationale, `extract_job/2` at `:31-51`, and the assertion + failure-message style at `:53-80`
    - `test/example/priv/playwright/playwright.config.ts` — the topology this contract asserts over, as Task 1 left it
    - `.github/workflows/ci.yml:1477-1531` — the two design invocations whose flags this contract asserts about
    - `232-VALIDATION.md` § Per-Requirement Verification Map — the PW-01 rows this test closes at layer L1
  </read_first>
  <action>
Create `test/sigra/planning/phase_232_playwright_auth_setup_test.exs`, module
`Sigra.Planning.Phase232PlaywrightAuthSetupTest`, `use ExUnit.Case, async: true`, following
`phase_230_design_gallery_split_test.exs` exactly: module-attribute path constants, string/regex
assertions over raw file contents rather than a parser, and a multi-sentence failure message on every
assertion explaining what silently breaks.

Path constants: `@config_path "test/example/priv/playwright/playwright.config.ts"`,
`@setup_path "test/example/priv/playwright/tests/auth.setup.ts"`,
`@spec_path "test/example/priv/playwright/tests/admin-design.spec.ts"`,
`@ci_path ".github/workflows/ci.yml"`,
`@design_projects ["admin-design-chromium", "admin-design-mobile", "admin-design-dark"]`.

Tests to write, each with a failure message naming the consequence:
1. The setup file exists, is not named with a `.spec.ts` suffix, and lives under the `testDir` value
   declared at `playwright.config.ts:52` (D-07).
2. The config declares a project named `design-auth-setup` whose `testMatch` is the `AUTH_SETUP_FILE`
   const, and that project block contains no `use:` key — a project-level `use` would let a future edit
   diverge the origin from `playwright.config.ts:76` and storageState would silently not apply (D-04).
3. Each of the three design projects declares `dependencies: ['design-auth-setup']` and a
   `storageState:` referencing the shared const. Assert per project so a partial wiring names the
   project that is missing.
4. The setup-file matcher appears in exactly two `testIgnore` arrays. Assert the count over the whole
   config, and assert specifically that the `chromium` and `mobile` project blocks each contain it.
   A count other than two means either a lane that must not run it will pick it up, or a design lane
   lost its dependency (D-06).
5. No Playwright invocation in `ci.yml` carries Playwright's dependency-suppression flag — the flag
   that disables project `dependencies:`. Assert its absence over the whole workflow. Passing it would
   suppress the setup project and yield an unauthenticated run whose failures look like application
   bugs (RESEARCH Pitfall 9).
6. The design gallery's `test.beforeEach` block performs no registration: extract the block bounded by
   `test.beforeEach(` and its closing `});` from the spec, and assert the extracted region contains
   neither a registration call nor a per-test email builder call. Scope the assertion to the extracted
   region rather than the whole file so the test states what it means.
7. A non-vacuity floor as the first test in the file, matching the repo convention: assert each of the
   four subject files is readable and non-trivial in size, with a message ending "the parse broke, this
   is not a pass". Without it, a moved or emptied config makes every assertion above vacuously true.

Do not add this file's assertions to `phase_230_design_gallery_split_test.exs` — that file is Phase
230's contract and stays scoped to Phase 230's claims.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_auth_setup_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `mix test test/sigra/planning/phase_232_playwright_auth_setup_test.exs` passes with at least seven tests.
    - [L2] Each assertion is falsifiable: temporarily removing `dependencies: ['design-auth-setup']` from one design project makes test 3 fail naming that project; removing the setup matcher from the `mobile` ignore array makes test 4 fail with the observed count. Restore both afterwards and confirm green.
    - [L1] The file's first test is the non-vacuity floor and its message ends with the repo's "the parse broke, this is not a pass" convention.
    - [L1] `mix test test/sigra/planning/` passes in full — no prior-phase contract regressed.
    - [L1] `git diff --stat test/sigra/planning/phase_230_design_gallery_split_test.exs` is empty.
  </acceptance_criteria>
  <done>The setup-project topology, both `testIgnore` memberships, the dependency-suppression-flag absence, and the registration-free `beforeEach` are all mechanically pinned, and each pin is proven falsifiable.</done>
</task>

</tasks>

## Edge-probe coverage carried by this plan

- **`PW-01 / adjacency`** ("When two things are exactly equal or just touch, do they merge, collide, or
  separate?") — resolved **covered**. Its domain instances are storageState's exact origin matching
  (`localhost:4000` and `127.0.0.1:4000` are different origins, D-04) and D-06's "exactly two
  `testIgnore` arrays" boundary claim. Both are authored into `must_haves.truths` above and pinned by
  contract tests 2 and 4.
- **`PW-01 / ordering`** ("When elements compare equal, is output order specified and stable?") —
  resolved **backstop**, authored as a structured `verification: backstop` truth above. There is no
  in-phase test that observes two rows with an identical `inserted_at`; the design specs sidestep the
  question with an explicit `q=` filter and, after this plan, create no rows at all. A verifier that
  cannot confirm it with explicit evidence must abstain to `human_needed`, not pass silently.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser -> example app `/users/log_in` | A real credentialed login crosses here; a bypass would invalidate every downstream authenticated-rendering assertion |
| Browser context -> `.playwright/design-admin.json` | A real Sigra session cookie is serialised to disk |
| `.playwright/` -> CI artifact uploads | `ci.yml:1673-1700` uploads `test-results/`; the session file must remain unreachable from any upload path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-14 | Spoofing (ASVS V2) | `auth.setup.ts` authentication | high | mitigate | The setup performs a real form login through the existing `loginDemoAdmin` helper — never a forged cookie, an injected session row, or a test-only bypass. Contract test 5 additionally forbids the CI flag that would suppress the setup project entirely and silently run unauthenticated. |
| T-232-15 | Information Disclosure (ASVS V3) | serialised session cookie at `.playwright/design-admin.json` | high | mitigate | Path is inside the already-gitignored `.playwright/`, outside `test-results/` and `playwright-report/`. Acceptance criterion greps that the literal appears only in `helpers/storageState.ts` and in no upload path; plan 232-09 re-verifies after the composite lands. |
| T-232-16 | Elevation (ASVS V4) | platform-admin grant | low | accept | No policy change. `sigra_admin_policy.ex:19-24`'s existing email-match grant for the seeded persona is reused as-is. |
| T-232-17 | Repudiation | a silently unauthenticated design run | high | mitigate | D-05's explicit authenticated assertion before `storageState()` is written, so an unauthenticated setup fails in the setup test rather than as 41 confusing downstream failures. |
| T-232-18 | Tampering | weakening a pinned spec contract to make the edit pass | high | mitigate | Prohibitions forbid touching the describe title, tag placement, axe title and board lists; acceptance criteria run both `phase_230_design_gallery_split_test.exs` and `p02` and assert an empty diff over the snapshot directory. |
| T-232-SC | Tampering | npm/pip/cargo installs | low | accept | Vacuous: no dependency manifest is touched. |
</threat_model>

## Artifacts this phase produces

Created **by this plan** is marked ✱.

- **Scripts:** `scripts/ci/ci-step-metrics.sh`, `scripts/ci/ci-step-metrics.test.sh`,
  `scripts/ci/playwright-shard-nonempty.sh`, `scripts/ci/playwright-shard-nonempty.test.sh`
- **CI step names (new):** `CI step metrics self-test`, `Playwright shard non-emptiness self-test`
- **CI job ids (new):** `example_playwright_shard`, `example_playwright_design`
- **CI job `name:` strings (new):** `Example Playwright smoke shard ${{ matrix.seam }}`,
  `Example Playwright design gallery`
- **Workflow inputs (new):** `force_shard_fail_probe` (`workflow_dispatch`)
- **Composite action:** `.github/actions/boot-example-app/action.yml` — inputs `mix-env`, `seeds`,
  `node`, `browsers`, `browser-cache-hit`, `boot`, `port`, `pg-database`, `server-log`, `warm-paths`; outputs
  `deps-cache-hit`, `otp-version`, `elixir-version`
- **Playwright:** ✱`test/example/priv/playwright/tests/auth.setup.ts`,
  ✱`test/example/priv/playwright/helpers/storageState.ts`; ✱project name `design-auth-setup`;
  ✱config consts `AUTH_SETUP_FILE`, ✱exported const `DESIGN_STORAGE_STATE`; ✱storageState path
  `test/example/priv/playwright/.playwright/design-admin.json`; `json` reporter outputFile
  `.playwright/last-run-report.json`
- **Prohibition guards (new):** `p17-pw-evidence-run-id-provenance.test.mjs`,
  `p18-pw-no-green-on-empty-count.test.mjs`, `p19-pw-sc-restatement-recorded.test.mjs`,
  `p20-pw-no-lane-green-because-skipped.test.mjs`, `p21-pw-committed-method-provenance.test.mjs`
- **ExUnit contracts (new):** ✱`test/sigra/planning/phase_232_playwright_auth_setup_test.exs`,
  `test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`,
  `test/sigra/planning/phase_232_playwright_shard_topology_test.exs`
- **Planning artifacts:** `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md`

<verification>
- `cd test/example/priv/playwright && npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --retries=0` — 123 passed, one setup execution.
- `mix test test/sigra/planning/` — green, including the new Phase 232 contract and the untouched Phase 230 one.
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` — green.
- `git status --porcelain test/example/priv/playwright/tests/admin-design.spec.ts-snapshots` — empty.
</verification>

<success_criteria>
The design gallery authenticates through a setup project and a shared storageState, `beforeEach`
registers nobody, all 41 tests per project still pass at `--retries=0` on a live app with zero baseline
drift, and the topology is pinned by a falsifiable ExUnit contract. No CI workflow file changed — per
D-01 the two design invocations need none.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-04-SUMMARY.md` when done.
</output>
