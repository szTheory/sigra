---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 06
type: execute
wave: 6
depends_on: ["232-05"]
files_modified: []
autonomous: false
requirements: [PW-02, PW-03]
must_haves:
  truths:
    - "D-05: a real run shows more than one overlapping, non-zero-duration shard succeeding at --retries=0."
    - "D-07: a real PR resolves `Example Playwright smoke (full lifecycle)` and every shard outcome feeds it."
    - "D-08: a qualifying non-PR run shows every event-gated shared-boot consumer starts successfully."
    - "All five UI-SPEC covered states and all viewport/theme/coverage counts remain unchanged in observed output."
  assumptions:
    - "FLAGGED/UNRESOLVED PW-02 and PW-03 unclassified probe rows remain explicit; this gate requires observed exhaustive seam/consumer evidence rather than inventing unclassified backstops."
  prohibitions:
    - statement: "MUST NOT accept skipped, serial, retried, partial, or YAML-only evidence as proof of concurrent isolation or shared-boot coverage."
      reason: "Descriptor-less, flagged-unverified evidence-honesty prohibition from the fallback probe."
  artifacts: []
  key_links:
    - from: "real shard job timestamps/results"
      to: "terminal required check"
      via: "overlap plus exhaustive result aggregation and gh pr checks"
---

<objective>
Pause for the real PR and non-PR observations that structural tests cannot manufacture (D-05, D-07, D-08).

Purpose: prove concurrency, isolation, merge protection, and every shared-boot consumer on GitHub-hosted runners.
Output: approved AFTER-SHARD-PR and AFTER-SHARD-NONPR run ids plus PR number.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-05-SUMMARY.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm live concurrent-shard and shared-boot receipts (D-05, D-07, D-08)</name>
  <read_first>
    - `232-05-SUMMARY.md` — final matrix seam set, action inputs, aggregator job id, and commit.
    - `.github/workflows/ci.yml` — exact event gates needed to choose PR/non-PR observations.
    - `232-VALIDATION.md` Manual-Only Verifications — required overlap, retry-zero, check-resolution, and consumer-boot evidence.
    - `232-EVIDENCE.md` — existing ordered PW-01 slots that must remain untouched.
  </read_first>
  <what-built>Runner-isolated Playwright matrix shards, one shared boot action, and the byte-identical terminal required check.</what-built>
  <action>Inspect the supplied PR and non-PR runs with the commands in `<verify>`, calculate overlap directly from job start/completion timestamps, and require the resolved five matrix seams (`admin_behavior`, `admin_checkpoints`, `design_gallery`, `non_admin_smoke`, `demo_showcase`) plus the four shared-boot consumers (`example_playwright_smoke`, `admin_design_recapture`, `admin_checkpoint_recapture`, `admin_eval_render`). Approve only when all results are successful, non-skipped where their event gates require execution, and retry-free. This task records no file change and does not create, rename, or switch a branch.</action>
  <how-to-verify>
    Without creating, renaming, or switching a branch in this task, provide:
    1. A completed pull-request run id whose head contains 232-05, with at least two shard jobs overlapping in time, all non-zero duration, all successful, and every command showing `--retries=0`.
    2. The pull request number and `gh pr checks` output showing `Example Playwright smoke (full lifecycle)` successful.
    3. A qualifying non-PR run id covering event-gated snapshot, recapture, and eval/shared-boot consumers; every expected consumer must execute and boot successfully rather than skip.
    4. Any failure/interference/log discrepancy; do not approve until repaired and rerun.
  </how-to-verify>
  <verify>
    <automated>gh run view &lt;after-shard-pr-run-id&gt; --json databaseId,event,headSha,conclusion,jobs &amp;&amp; gh pr checks &lt;pr-number&gt; &amp;&amp; gh run view &lt;after-shard-nonpr-run-id&gt; --json databaseId,event,headSha,conclusion,jobs</automated>
  </verify>
  <acceptance_criteria>
    - PR run succeeds with at least two overlapping non-zero-duration shard intervals and explicit retry-zero commands.
    - Every expected matrix seam appears and succeeds; no result is omitted, retried, advisory, or continue-on-error.
    - `gh pr checks` shows the exact byte-identical required name successful.
    - Non-PR run executes every event-gated app-booting consumer required by D-08 and each reports successful readiness/startup.
    - Assertion/snapshot/project counts and UI preservation observations match prior receipts.
  </acceptance_criteria>
  <resume-signal>Reply with PR run id, PR number, non-PR run id, and “all shards/consumers green”, or describe discrepancies.</resume-signal>
</task>

</tasks>

<threat_model>
## Security Enforcement Contract

**ASVS baseline: L1; block threshold: high.** Any high-or-critical observation failure blocks approval; medium/low findings follow the dispositions below.

| ASVS control area | Gate mitigation | Verification mapping |
|---|---|---|
| V2 Authentication | Require every shard/consumer to complete its real authentication-dependent browser work without retries. | PR/non-PR job logs and explicit retry-zero commands. |
| V3 Session Management | Require concurrent isolated shard ownership and non-zero successful overlap. | Timestamp, DB/app/port/log inspection for all five seams. |
| V4 Access Control | Require every shard result to reach the exact protected terminal context and every shared-boot consumer to start. | `gh pr checks` plus both `gh run view` receipts. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub-hosted concurrency → project evidence | Scheduler timestamps and job outputs substantiate isolation and required-check claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-232-11 | Repudiation | live shard proof | high | mitigate | Require run ids, overlapping timestamps, retry-zero commands, exhaustive results, PR checks, and non-PR consumer run. |
</threat_model>

## Artifacts this phase produces

- No files or symbols; this checkpoint produces validated PR run id, non-PR run id, PR number, overlap evidence, and exact required-check observation.

<verification>
All three GitHub CLI commands resolve and their outputs meet every acceptance assertion.
</verification>

<success_criteria>
The real environment proves retry-free concurrency, exhaustive required aggregation, and every shared-boot consumer.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-06-SUMMARY.md` when approved.
</output>
