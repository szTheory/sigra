---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 09
type: execute
wave: 7
depends_on: ["232-07", "232-08"]
files_modified:
  - test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
autonomous: true
requirements: [PW-03]
must_haves:
  truths:
    - "SC-4's 'exactly one definition' is mechanically pinned: the composite exists, and no job in either workflow re-declares the prelude steps it replaced."
    - "SC-4's second half — 'a full run afterwards shows every one of those jobs still booting successfully' — is closed by a `workflow_dispatch` run read back by ID, not by reading YAML (D-33)."
    - "All seven call sites are observed reaching a terminal state on that run: six `ci.yml` jobs plus the Pages publisher, each either green or explicitly and legitimately gated off with its gate named."
    - "`.playwright/` — which holds both the storageState session cookie and the `json` run report — is reachable from no artifact upload path in either workflow after the composite landed."
  artifacts:
    - test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs
    - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
  key_links:
    - "The SC-4 contract -> `extract_job/2` from `phase_230_design_gallery_split_test.exs:31-51`, reused verbatim so job-body extraction has one implementation rather than two."
    - "`AFTER-PW03-NONPR` slot -> `gh run view <id> --json jobs`, the capture command the slot declared in plan 232-02."
  prohibitions:
    - "MUST NOT close SC-4 on a run where any of the seven call sites was `skipped` without naming the gate and the event type that makes the skip legitimate — a skipped job proves nothing (D-33)."
    - "MUST NOT assert 'exactly one definition' by counting `uses:` lines alone; the contract must also prove no job re-declares the replaced steps, or a partial revert would pass."
    - "MUST NOT restructure any job in this plan — PW-02 is the next wave and D-31 requires PW-03 to be observed before it starts."
    - "MUST NOT add the new contract's assertions to `phase_230_design_gallery_split_test.exs`; that file is Phase 230's contract."
---

<objective>
Close PW-03: pin "exactly one definition" mechanically, then prove on a real run that every job which
now calls that definition still boots.

Purpose: SC-4 has two halves and only one of them is a file property. The other — "a full run
afterwards shows every one of those jobs still booting successfully" — is a claim about what a run did,
and this phase's verification philosophy (D-33) makes an observed run the only thing that closes it.
This plan is also the wave boundary that keeps D-31's ordering structural: PW-02 does not begin until
PW-03 is observed.
Output: `test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`, and the
`AFTER-PW03-NONPR` slot captured against a run ID.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-07-SUMMARY.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExUnit contract for SC-4 — one definition, no re-declared prelude</name>
  <files>test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs</files>
  <read_first>
    - `test/sigra/planning/phase_230_design_gallery_split_test.exs:1-51` — the module preamble, the path constants, the "no YAML parser is added here" rationale, and `extract_job/2` at `:31-51` including its `^  ([a-z_]+):$` job-id regex (which does not match hyphenated ids such as `ci-gate`)
    - `.github/actions/boot-example-app/action.yml` — the step names the contract asserts are defined exactly once
    - `.github/workflows/ci.yml` — the six converted jobs as plan 232-07 left them
    - `.github/workflows/playwright-github-pages.yml` — the seventh, as plan 232-08 left it
    - `232-VALIDATION.md` § Per-Requirement Verification Map — the two PW-03 / SC-4 rows this test closes at layer L1
  </read_first>
  <action>
Create `test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`, module
`Sigra.Planning.Phase232BootPreludeSingleDefinitionTest`, `use ExUnit.Case, async: true`, following the
Phase 230 contract file's idiom exactly: path constants as module attributes, raw-content string and
regex assertions rather than a parser, and a multi-sentence failure message on every assertion naming
what silently breaks.

Constants: `@action_path ".github/actions/boot-example-app/action.yml"`,
`@ci_path ".github/workflows/ci.yml"`,
`@pages_path ".github/workflows/playwright-github-pages.yml"`,
`@call_sites ["example_unit_smoke", "example_http_smoke", "example_playwright_smoke",
"admin_design_recapture", "admin_checkpoint_recapture", "admin_eval_render"]`, and the composite's
literal step names as a list.

Copy `extract_job/2` verbatim from `phase_230_design_gallery_split_test.exs:31-51` rather than writing a
second job-body extractor, and add a comment saying it is a deliberate copy of that helper so the two
contracts cannot diverge in how they read a job body. Note in the comment that its job-id regex does not
match hyphenated ids, so an extracted block runs to the next underscore-only id.

Tests:
1. Non-vacuity floor, first in the file: all three subject files are readable and non-trivial, message
   ending "the parse broke, this is not a pass".
2. The composite exists and declares `runs.using: composite`.
3. Each of the six `ci.yml` call sites, asserted one job at a time so a failure names the job: its body
   contains exactly one `uses: ./.github/actions/boot-example-app` line.
4. The Pages publish job's body contains exactly one such line.
5. No calling job re-declares the prelude. For each of the seven job bodies assert the absence of a
   `mix deps.get` invocation, a `mix ecto.create` invocation, a `seeds.exs` invocation, an
   `erlef/setup-beam` usage, and an `npm ci` invocation — these are the steps the composite now owns.
   This is the half that a `uses:`-line count alone would miss: a partial revert that adds the prelude
   back beside the composite call would still count one `uses:` line.
6. Exactly one definition: each of the composite's literal step names appears exactly once across the
   union of the two workflow files and the action file — i.e. only inside the action.
7. The browser cache remains caller-owned and singular: `example_playwright_smoke`'s body declares an
   `actions/cache` step whose path names the Playwright browser cache directory, and no other job body
   in either workflow does, and the action file declares none. This is D-27's intent as corrected by
   RESEARCH C-4, expressed as a contract rather than a comment.
8. `example_http_smoke`'s composite call passes a seeds input whose value is the literal false, with a
   message recording that it is the only app-booting job that deliberately does not seed (RESEARCH C-5)
   — so a future "consistency" edit that flips it has to argue with a test.

Do not add any of these assertions to `phase_230_design_gallery_split_test.exs`.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs && mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `mix test test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs` passes with at least eight tests.
    - [L2] Each assertion is falsifiable: temporarily re-adding a `mix deps.get` step to one converted job makes test 5 fail naming that job; temporarily adding a browser-cache step to `admin_eval_render` makes test 7 fail. Restore both and confirm green.
    - [L1] The file's first test is the non-vacuity floor and its message ends with "the parse broke, this is not a pass".
    - [L1] `extract_job/2` is a verbatim copy of the Phase 230 helper with a comment recording that fact.
    - [L1] `git diff --stat test/sigra/planning/phase_230_design_gallery_split_test.exs` is empty.
    - [L1] `mix test test/sigra/planning/` passes in full.
  </acceptance_criteria>
  <done>SC-4's file-property half is pinned by a falsifiable contract that catches both a missing call site and a re-declared prelude, and that encodes the browser-cache ownership split as a test rather than a comment.</done>
</task>

<task type="auto">
  <name>Task 2: Observe a full non-PR run and capture AFTER-PW03-NONPR</name>
  <files>.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md</files>
  <precondition>The commits from plans 232-06 through 232-09 Task 1 are pushed to the phase branch on `origin`, and both `gh workflow run ci.yml --ref <branch>` and a dispatch or scheduled path for `playwright-github-pages.yml` on that branch are available. `gh auth status` succeeds.</precondition>
  <read_first>
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — the `AFTER-PW03-NONPR` slot's declared obligation and capture command
    - `.github/workflows/ci.yml` — the six converted jobs' `name:` strings, needed because the Actions API returns display names and never ids
    - `.github/workflows/ci.yml:1653-1700` — the artifact bundle contract and the failure-diagnostics uploads, the surface that must not be able to reach `.playwright/`
    - `.github/workflows/playwright-github-pages.yml` — the publisher's trigger surface
    - `232-RESEARCH.md` § Security Domain (the V3 row: the storageState file must never be swept into an uploaded artifact)
  </read_first>
  <action>
Dispatch one `workflow_dispatch` run of `ci.yml` on the phase branch, wait for it to reach a terminal
state, and flip `AFTER-PW03-NONPR` to `captured (run <id>)`.

A dispatch event is the right vehicle because it is the one event on which every app-booting job runs:
`admin_design_recapture`, `admin_checkpoint_recapture` and `admin_eval_render` are all gated off
`pull_request`, so a PR run would leave three of the six unobserved.

Capture into the slot:
1. Run id, head SHA, event type, and the run's conclusion.
2. `gh run view <id> --repo szTheory/sigra --json jobs --jq '.jobs[] | {name, conclusion, startedAt, completedAt}'` output pasted verbatim in a fenced block — this is the slot's declared capture command and what `p21` looks for.
3. A table with one row per call site — the six `ci.yml` jobs plus the Pages publisher — giving the job's
   display name, its conclusion, and the log line proving the app booted (the composite's wait step
   prints an "App responding after Ns" line). A conclusion alone is not proof of booting; the boot line
   is.
4. For the Pages publisher: either a run of `playwright-github-pages.yml` on this branch with the same
   treatment, or, if that workflow cannot be dispatched on a non-default ref, an explicit statement of
   that constraint plus a `pending (<obligation>)` sub-note naming when it will be observed and the
   command that will observe it. Do not silently omit the seventh site.
5. For any of the seven that reported `skipped`, the literal word `skipped` plus the gate expression and
   the event type the skip is legitimate for (`p20` requires both; D-33 makes a skip prove nothing).
6. A one-line statement of the deps-cache outcome per job, read from each job's `Cache hit summary`
   step, so the cache-key-shape question RESEARCH raised is answered with observation: the six `ci.yml`
   jobs should show hits (their key shape is unchanged) and the Pages publisher should show a miss on
   this first run (its key shape was normalised in plan 232-08).

Then re-verify the V3 boundary now that the composite has landed, and record the result in the slot:
confirm that no `actions/upload-artifact` step in either workflow has a path that can reach
`test/example/priv/playwright/.playwright/`, which holds both the storageState session cookie and the
`json` run report. Record the command used and its output.

Change nothing outside `232-EVIDENCE.md` in this task.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && node --input-type=module -e "import fs from 'node:fs'; const files=['.github/workflows/ci.yml','.github/workflows/playwright-github-pages.yml']; const bad=[]; for(const f of files){ const t=fs.readFileSync(f,'utf8'); for(const m of t.matchAll(/upload-artifact[\s\S]{0,600}?path:\s*\|?\s*([^\n]*(?:\n\s{10,}[^\n]*)*)/g)){ if(/\.playwright/.test(m[1])) bad.push(f+': '+m[1].trim()); } } if(bad.length) throw new Error('artifact upload path can reach .playwright/: '+bad.join(' | ')); console.log('no upload path reaches .playwright/');"</automated>
  </verify>
  <acceptance_criteria>
    - [L3] `gh run view <AFTER_PW03_RUN_ID> --json event --jq .event` returns `workflow_dispatch` and the run's conclusion is `success`.
    - [L3] The `AFTER-PW03-NONPR` slot contains a row for each of the seven call sites with a conclusion and, for every non-skipped one, the composite's boot line quoted verbatim.
    - [L3] Every `skipped` entry in the slot carries its gate expression and the event type the skip is legitimate for.
    - [L3] The slot records the per-job deps-cache outcome, including the expected single miss on the Pages publisher after its key normalisation.
    - [L1] The V3 re-verification command in this task's `<verify>` prints `no upload path reaches .playwright/`, and its output is recorded in the slot.
    - [L1] `p17`, `p20` and `p21` all pass over the updated ledger.
    - [L1] `git status --porcelain` shows `232-EVIDENCE.md` as the only modified file in this task.
  </acceptance_criteria>
  <done>SC-4 is closed on a terminal, green `workflow_dispatch` run: every call site is observed booting or explicitly and legitimately gated off, the cache-key normalisation's cost is observed rather than predicted, and the session-cookie directory is re-confirmed unreachable from any artifact upload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions run -> committed ledger | Job conclusions become the phase's proof of SC-4 |
| `.playwright/` -> artifact uploads | The directory now holds both a real session cookie and the run report |
| Contract file -> merge gate | A contract that counts the wrong thing lets a partial revert pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-39 | Information Disclosure (ASVS V3) | storageState session cookie in `.playwright/` | high | mitigate | Re-verified after the composite landed: an automated scan of every `upload-artifact` path in both workflows must find no path reaching `.playwright/`; the result is recorded in the ledger slot. |
| T-232-40 | Repudiation | SC-4 closed on a run where three of six jobs were gated off | high | mitigate | A `workflow_dispatch` event is required precisely because it is the one event on which every app-booting job runs; every skip must name its gate and event type. |
| T-232-41 | Tampering | a partial revert passing a `uses:`-count-only contract | high | mitigate | Contract test 5 asserts the absence of the replaced prelude steps in every calling job body, independently of the `uses:` count. |
| T-232-42 | Tampering | the browser cache silently migrating into a shared definition later | high | mitigate | Contract test 7 pins the ownership split — one browser cache, in `example_playwright_smoke`, none in the action, none elsewhere. |
| T-232-43 | Denial of Service | an unnoticed cache-key regression cold-compiling six jobs | medium | mitigate | Per-job deps-cache outcome is read off the run and recorded, so a mass miss is visible rather than inferred. |
| T-232-SC | Tampering | npm/pip/cargo installs | low | accept | Vacuous: no dependency manifest changes. |
</threat_model>

## Artifacts this phase produces

Created **by this plan** is marked ✱.

- **Scripts:** `scripts/ci/ci-step-metrics.sh`, `scripts/ci/ci-step-metrics.test.sh`,
  `scripts/ci/playwright-shard-nonempty.sh`, `scripts/ci/playwright-shard-nonempty.test.sh`
- **CI step names (new):** `CI step metrics self-test`, `Playwright shard non-emptiness self-test`
- **CI step id (new):** `boot_example_app`
- **CI job ids (new):** `example_playwright_shard`, `example_playwright_design`
- **CI job `name:` strings (new):** `Example Playwright smoke shard ${{ matrix.seam }}`,
  `Example Playwright design gallery`
- **Workflow inputs (new):** `force_shard_fail_probe` (`workflow_dispatch`)
- **Composite action:** `.github/actions/boot-example-app/action.yml` — inputs `mix-env`, `seeds`,
  `node`, `browsers`, `browser-cache-hit`, `boot`, `port`, `pg-database`, `server-log`, `warm-paths`; outputs
  `deps-cache-hit`, `otp-version`, `elixir-version`; internal step id `example_deps_cache`
- **Playwright:** `test/example/priv/playwright/tests/auth.setup.ts`,
  `test/example/priv/playwright/helpers/storageState.ts`; project name `design-auth-setup`; config
  consts `AUTH_SETUP_FILE`, `DESIGN_STORAGE_STATE`; storageState path
  `test/example/priv/playwright/.playwright/design-admin.json`; `json` reporter outputFile
  `.playwright/last-run-report.json`
- **Prohibition guards (new):** `p17-pw-evidence-run-id-provenance.test.mjs`,
  `p18-pw-no-green-on-empty-count.test.mjs`, `p19-pw-sc-restatement-recorded.test.mjs`,
  `p20-pw-no-lane-green-because-skipped.test.mjs`, `p21-pw-committed-method-provenance.test.mjs`
- **ExUnit contracts (new):** `test/sigra/planning/phase_232_playwright_auth_setup_test.exs`,
  ✱`test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`,
  `test/sigra/planning/phase_232_playwright_shard_topology_test.exs`
- **Planning artifacts:** `232-EVIDENCE.md` (✱ slot `AFTER-PW03-NONPR` flipped to captured)

<verification>
- `mix test test/sigra/planning/` — green, including the new SC-4 contract.
- `gh run view <AFTER_PW03_RUN_ID> --json event,conclusion` — `workflow_dispatch` / `success`.
- The upload-path scan in Task 2's `<verify>` — prints `no upload path reaches .playwright/`.
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` — green.
</verification>

<success_criteria>
PW-03 is done and proven: one definition, seven call sites, a falsifiable contract that catches both a
missing call and a re-declared prelude, and a terminal green dispatch run showing every one of those
jobs still booting — with the session-cookie directory re-confirmed unreachable from any artifact.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-09-SUMMARY.md` when done.
</output>
