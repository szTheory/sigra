---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 02
type: execute
wave: 2
depends_on: ["232-01"]
files_modified:
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
  - .planning/ROADMAP.md
  - scripts/ci/prohibitions/p17-pw-evidence-run-id-provenance.test.mjs
  - scripts/ci/prohibitions/p18-pw-no-green-on-empty-count.test.mjs
  - scripts/ci/prohibitions/p19-pw-sc-restatement-recorded.test.mjs
  - scripts/ci/prohibitions/p20-pw-no-lane-green-because-skipped.test.mjs
  - scripts/ci/prohibitions/p21-pw-committed-method-provenance.test.mjs
autonomous: true
requirements: [PW-01, PW-02, PW-03]
must_haves:
  truths:
    - "`232-EVIDENCE.md` declares all eight observed-run slots up front — `BEFORE-STEPS`, `AFTER-PW01-PR`, `AFTER-PW01-NONPR`, `AFTER-PW03-NONPR`, `AFTER-PW02-PR`, `AFTER-PW02-CONTEXT`, `AFTER-PW02-REDPROBE`, `AFTER-PUSH` — each either captured with a run id or `pending (<obligation>)` carrying its future capture command."
    - "SC-1's 'once per project' wording is restated in the ledger under a `## Restated Success Criterion (SC-1)` heading, because the chosen shape authenticates once per invocation, not once per project (D-08, empirically E-4)."
    - "A Phase-232-scoped prohibition guard reds when the 232 ledger loses a run id, an executed count, a restatement, or a skipped-lane disclosure — the existing `p01`/`p03`/`p11`/`p12`/`p13` cannot, because every one of them hard-pins `LEDGER` to the 230 ledger (RESEARCH C-3)."
    - "Every new guard fails closed on its own vacuity: an emptied or restructured ledger makes the guard red, never silently pass."
  artifacts:
    - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
    - scripts/ci/prohibitions/p17-pw-evidence-run-id-provenance.test.mjs
    - scripts/ci/prohibitions/p18-pw-no-green-on-empty-count.test.mjs
    - scripts/ci/prohibitions/p19-pw-sc-restatement-recorded.test.mjs
    - scripts/ci/prohibitions/p20-pw-no-lane-green-because-skipped.test.mjs
    - scripts/ci/prohibitions/p21-pw-committed-method-provenance.test.mjs
  key_links:
    - "New guards -> `fast_checks` via the existing glob `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` (`ci.yml:380`); the glob is load-bearing and no wiring change is needed."
    - "`## Restated Success Criterion (SC-1)` in the ledger <- a prose pointer in `ROADMAP.md`'s Phase 232 block; the machine-readable half must live in the ledger, because a ROADMAP-only restatement satisfies no guard (RESEARCH C-2)."
  prohibitions:
    - "MUST NOT edit `p01`, `p03`, `p08`, `p11`, `p12` or `p13` to point at the 232 ledger — they are 230's receipts and repointing them would silently retire 230's coverage."
    - "MUST NOT record a slot as `captured` without a run id that appears at least twice in its own body."
    - "MUST NOT write a `pending` slot without a fenced block carrying the command that will capture it, and without the literal word `obligation` in the pending parenthetical."
    - "MUST NOT weaken a new guard's non-vacuity floor to accommodate a ledger that is not yet filled in — a pending slot is a legitimate ledger state and the guard must be written to accept it explicitly, not by lowering the floor."
---

<objective>
Stand up the phase's evidence contract and the guards that make it un-fakeable, so every later plan
has a declared slot to write into and a mechanical check that reds when it does not.

Purpose: `232-VALIDATION.md` names eight observed-run slots, and D-31's ordering only holds if the
slots exist before the work does — otherwise "measured" degrades into "measured afterwards, from
memory". The existing ledger prohibitions cannot help: `p01`, `p03`, `p08`, `p11`, `p12` and `p13` all
hard-code `LEDGER` to `230-EVIDENCE.md` (`p11:29`, `p12:26`), so a 232 omission never reds anything
(RESEARCH C-3). D-08's SC-1 restatement obligation is real but likewise unenforced until a 232-scoped
guard exists.
Output: a fully declared `232-EVIDENCE.md`, a ROADMAP prose pointer, and five 232-pinned prohibition
guards picked up automatically by the `fast_checks` glob.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare all eight evidence slots and record the SC-1 restatement (D-08)</name>
  <files>.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — the file as plan 232-01 left it (thesis, Slot Index, `BEFORE-STEPS`)
    - `.planning/phases/230-tier-1-critical-path-reclamation/230-EVIDENCE.md:13-45` (Slot Index + first slot body) and `:655-680` (the `## Restated Success Criterion (SC-2)` section, including the blockquoted "Operative restatement — verify against this, not the ROADMAP wording:" form)
    - `scripts/ci/prohibitions/_lib.mjs:205-230` — `SLOT_HEADING_RE`, `parseEvidenceSlots()` and the fields they expose (`statusRaw`, slot body)
    - `scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` — the exact status grammar regex and the `pending \(.*obligation.*\)/i` test
    - `scripts/ci/prohibitions/p11-sc-restatement-recorded.test.mjs` — the restatement heading grammar `^##\s+Restated Success Criterion\s*\(SC-\d+\)\s*$` and what it requires in the body
    - `.planning/ROADMAP.md:73-78` — the Phase 230 `**Success criterion 2 restatement**:` prose-pointer form to mirror, and `:173-186` — the Phase 232 block being edited
    - `232-VALIDATION.md` § 'The Observed-Run Evidence Contract' — the eight slots and what each is for
  </read_first>
  <action>
Add the seven remaining slots to `232-EVIDENCE.md` and register every one in the `## Slot Index` table.
Each new slot is `Status: pending (<obligation>)` and the parenthetical must contain the literal word
`obligation`, because `p12`'s pending test matches `/pending \(.*obligation.*\)/i` and the new `p17`
copies it. Every pending slot also needs at least one fenced block holding the exact command that will
capture it later — a pending slot with no capture command is an intention, not an obligation.

The seven slots, with the obligation text and capture command each must carry:

`## AFTER-PW01-PR` — obligation: capture on the first `pull_request` run after plan 232-04 lands;
command `bash scripts/ci/ci-step-metrics.sh --run <id> --job 'Example Playwright smoke (full lifecycle)'`.
`## AFTER-PW01-NONPR` — obligation: capture on a `workflow_dispatch` run of `ci.yml` on the phase
branch after 232-04 lands, where `design_gallery_snapshots` and `admin_design_recapture` actually run;
same command plus the recapture job name.
`## AFTER-PW03-NONPR` — obligation: capture on a `workflow_dispatch` run after the composite lands
(plans 232-06..08), proving all seven prelude call sites still boot; command
`gh run view <id> --repo szTheory/sigra --json jobs`.
`## AFTER-PW02-PR` — obligation: capture on a `pull_request` run after plan 232-10 lands, recording
per-leg executed counts and the lane's wall clock at `--retries=0`; commands
`bash scripts/ci/ci-step-metrics.sh --run <id>` and `bash scripts/ci/ci-run-metrics.sh --jobs <id>`.
`## AFTER-PW02-CONTEXT` — obligation: capture on a real PR that branch protection evaluates; commands
`gh pr checks <n>` and `gh api repos/szTheory/sigra/rulesets/14941512 --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'` (the read recorded at `MAINTAINING.md:116`).
`## AFTER-PW02-REDPROBE` — obligation: capture on a deliberately-failed shard leg, showing the
`example_playwright_smoke` aggregator concluding `failure`; command
`gh run view <id> --repo szTheory/sigra --json jobs --jq '.jobs[] | select(.name=="Example Playwright smoke (full lifecycle)") | {name, conclusion}'`.
`## AFTER-PUSH` — obligation: capture post-merge from `ci-observe.yml`'s demotion receipt; command
`bash scripts/ci/ci-demotion-observer.sh --run <id> --format table`. State in this slot's body that it
is post-merge by construction and therefore closes as an explicit standing obligation, not as a gap.

Then add a `## Restated Success Criterion (SC-1)` section, heading matching
`^##\s+Restated Success Criterion\s*\(SC-\d+\)\s*$` exactly. Its body states the unsatisfiability
finding — ROADMAP SC-1 says the design projects authenticate "once **per project**", but the chosen
shape (one `design-auth-setup` project feeding all three design projects through `dependencies:` and a
shared `storageState`) authenticates once per `npx playwright test` invocation, verified empirically as
RESEARCH finding E-4, i.e. **once total** per CI step and not once per project (D-08). Follow it with a
blockquoted line "**Operative restatement — verify against this, not the ROADMAP wording:**" and the
restated criterion: on an observed run, `admin-design.spec.ts`'s `beforeEach` performs no user
registration; exactly one `design-auth-setup` login executes per `npx playwright test` invocation (and
therefore once per shard leg after PW-02, per E-1); and the design-gallery step's executed test count
is unchanged at 39 on a PR / 84 on the non-PR snapshot step / 123 ungrepped, while its duration falls
against D-12's recorded baseline `39 passed (3.9m)`, run `30412458437`, job `90451525539`. Include the
run id in the body so the section carries a provenance anchor.

Finally add the human-facing half in `.planning/ROADMAP.md`, inside the Phase 232 block, immediately
after the `**Proof discipline**:` line, as a bolded prose pointer in exactly the Phase 230 form used at
`ROADMAP.md:73-78`: `**Success criterion 1 restatement**: …` naming the once-per-invocation finding and
pointing at `232-EVIDENCE.md`'s `## Restated Success Criterion (SC-1)` as the operative wording. Do not
put the machine-readable restatement only in the ROADMAP — RESEARCH C-2 establishes that a ROADMAP-only
restatement satisfies no guard.
  </action>
  <verify>
    <automated>node -e "const {parseEvidenceSlots,readSubject}=require('node:module').createRequire(process.cwd()+'/');" 2>/dev/null; node --input-type=module -e "import {parseEvidenceSlots} from './scripts/ci/prohibitions/_lib.mjs'; import fs from 'node:fs'; const s=parseEvidenceSlots(fs.readFileSync('.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md','utf8')); if(s.length!==8){throw new Error('expected 8 slots, got '+s.length);} console.log(s.map(x=>x.name).join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `parseEvidenceSlots()` over `232-EVIDENCE.md` returns exactly 8 slots, named `BEFORE-STEPS`, `AFTER-PW01-PR`, `AFTER-PW01-NONPR`, `AFTER-PW03-NONPR`, `AFTER-PW02-PR`, `AFTER-PW02-CONTEXT`, `AFTER-PW02-REDPROBE`, `AFTER-PUSH`.
    - [L1] Every slot's `Status:` line matches `^(captured \((run|runs) [\s\S]+\)|pending \(.+\))$`, and every `pending` parenthetical contains the word `obligation`.
    - [L1] Every slot body contains at least one fenced block; each `captured` slot's fenced blocks include one naming `ci-step-metrics.sh`, `ci-run-metrics.sh`, or matching `gh (run|pr|api)`.
    - [L1] `grep -cE '^##[[:space:]]+Restated Success Criterion[[:space:]]*\(SC-1\)[[:space:]]*$' 232-EVIDENCE.md` returns 1.
    - [L1] `.planning/ROADMAP.md`'s Phase 232 block contains a line beginning `**Success criterion 1 restatement**:` that names `232-EVIDENCE.md`.
    - [L1] `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` still passes (the 230-pinned guards are untouched).
  </acceptance_criteria>
  <done>All eight slots are declared with parseable status lines and capture commands, and SC-1's restatement exists in the ledger with a ROADMAP prose pointer — so D-08's obligation has a machine-readable home before any guard checks for it.</done>
</task>

<task type="auto">
  <name>Task 2: Five Phase-232-pinned prohibition guards over the 232 ledger</name>
  <files>scripts/ci/prohibitions/p17-pw-evidence-run-id-provenance.test.mjs, scripts/ci/prohibitions/p18-pw-no-green-on-empty-count.test.mjs, scripts/ci/prohibitions/p19-pw-sc-restatement-recorded.test.mjs, scripts/ci/prohibitions/p20-pw-no-lane-green-because-skipped.test.mjs, scripts/ci/prohibitions/p21-pw-committed-method-provenance.test.mjs</files>
  <read_first>
    - `scripts/ci/prohibitions/p12-run-id-provenance.test.mjs` (whole file) — the model for `p17`: header contract in four parts, `LEDGER` const, `parseEvidenceSlots`, the non-vacuity floor test, the status-grammar regex
    - `scripts/ci/prohibitions/p03-no-green-on-empty-grep.test.mjs` (whole file) — the model for `p18`, including its executed-count regex `/\b(\d+)\s+(?:passed|tests)\b/`
    - `scripts/ci/prohibitions/p11-sc-restatement-recorded.test.mjs` (whole file) — the model for `p19` and the restatement heading grammar
    - `scripts/ci/prohibitions/p13-no-lane-green-because-skipped.test.mjs` (whole file) — the model for `p20`
    - `scripts/ci/prohibitions/p01-committed-method-provenance.test.mjs` (whole file) — the model for `p21`
    - `scripts/ci/prohibitions/_lib.mjs:32-53` (`subjectPath()`, `readSubject()`, `readRepoFile()`) and `:205-230` (`SLOT_HEADING_RE`, `parseEvidenceSlots()`)
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — the subject these guards read, as Task 1 left it
    - `232-RESEARCH.md` § Corrections C-3 — why new guards are required rather than edits to the existing ones
  </read_first>
  <action>
Create five node:test guards under `scripts/ci/prohibitions/`, each structurally copied from its
230-pinned twin with `LEDGER` re-pointed at
`.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` and its
subject overridable through `GSD_PROHIB_SUBJECT` via `readSubject()`. Leave `p01`, `p03`, `p08`, `p11`,
`p12`, `p13` byte-unchanged — they are Phase 230's receipts and repointing them retires 230's coverage.

Every guard reproduces the four-part header contract from `p12:1-20`: the prohibition sentence in
`MUST NOT` form, a `Subject:` line noting `GSD_PROHIB_SUBJECT` substitutability, a "STRUCTURAL AND
OFFLINE BY DESIGN" rationale, and a "What silently breaks if this guard is deleted" paragraph. Every
guard's FIRST test is a non-vacuity floor asserting `slots.length >= 8` with the message form "an
emptied or restructured ledger would make every assertion below vacuously true — the parse broke, this
is not a pass".

`p17-pw-evidence-run-id-provenance.test.mjs` — MUST NOT record a phase-232 observation without a
traceable run id. Asserts, per slot: `statusRaw` matches
`^(captured \((run|runs) [\s\S]+\)|pending \(.+\))$`; every `pending` parenthetical matches
`/obligation/i`; every slot has at least one fenced block; every `captured` slot has a fenced block
containing `ci-step-metrics.sh` or `ci-run-metrics.sh` or matching `/\bgh (run|pr|api)\b/`; and every
run id named in a `captured` status appears at least twice inside that slot's own body.

`p18-pw-no-green-on-empty-count.test.mjs` — MUST NOT claim a Playwright lane got faster without
recording what it executed (D-14). Asserts that every slot whose name begins `AFTER-PW01` or
`AFTER-PW02` and whose status is `captured` contains at least one match of
`/\b(\d+)\s+(?:passed|tests)\b/` in its body, and that no such match is the number zero. State in the
header that this is the D-19/E-3 hazard in ledger form: `--shard` suppresses Playwright's own
`No tests found` exit-1, so a duration with no count is indistinguishable from a green on nothing.

`p19-pw-sc-restatement-recorded.test.mjs` — MUST NOT quietly reinterpret a success criterion. Asserts
the ledger contains at least one heading matching
`^##\s+Restated Success Criterion\s*\(SC-\d+\)\s*$`, that its section body is non-trivial (a floor on
character count, matching `p11`'s idiom), and that the body carries either a run id or a command /
artifact reference. Additionally assert the ROADMAP carries a prose pointer: read
`.planning/ROADMAP.md` through `readRepoFile()` and require a line matching
`/^\*\*Success criterion \d+ restatement\*\*:/m` that mentions `232-EVIDENCE.md`.

`p20-pw-no-lane-green-because-skipped.test.mjs` — MUST NOT let a `skipped` job or step stand as
evidence that work ran (D-33). Asserts that any `captured` slot body mentioning the literal `skipped`
also, within that same slot, states a reason and names the event type the skip was legitimate for; and
that no `captured` slot claims a criterion closed while its own body's only outcome token is `skipped`.
Encode the check as: for each captured slot, if the body contains `skipped`, it must also match
`/(pull_request|workflow_dispatch|push|schedule)/` and contain a line explaining the gate.

`p21-pw-committed-method-provenance.test.mjs` — MUST NOT produce a phase-232 timing claim by a method
that is not itself committed. Asserts that every fenced block in a `captured` slot which contains a
duration-like token references one of the committed instruments by path
(`scripts/ci/ci-step-metrics.sh`, `scripts/ci/ci-run-metrics.sh`, `scripts/ci/ci-demotion-observer.sh`)
or a bare `gh run`/`gh pr`/`gh api` invocation, and that each referenced `scripts/ci/*.sh` path exists
on disk via `readRepoFile()`.

No `fast_checks` wiring change is needed: `ci.yml:380` already runs the glob
`node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs`, and its adjacent comment records
that a bare directory argument is not valid under node 22 — leave the glob exactly as it is.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - [L2] `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` passes with all five new files reporting tests (the tap output names `p17`..`p21`).
    - [L2] Each new guard fails closed when its subject is emptied: `GSD_PROHIB_SUBJECT=/dev/null node --test scripts/ci/prohibitions/p17-pw-evidence-run-id-provenance.test.mjs` exits non-zero, and likewise for `p18`..`p21`.
    - [L2] `p19` reds if the ROADMAP prose pointer is removed — demonstrate by pointing `GSD_PROHIB_SUBJECT` at a fixture lacking the restatement heading and observing a non-zero exit.
    - [L1] `git diff --stat scripts/ci/prohibitions/p01-*.test.mjs scripts/ci/prohibitions/p03-*.test.mjs scripts/ci/prohibitions/p08-*.test.mjs scripts/ci/prohibitions/p11-*.test.mjs scripts/ci/prohibitions/p12-*.test.mjs scripts/ci/prohibitions/p13-*.test.mjs` reports no changes — the 230-pinned guards were not repointed.
    - [L1] Each new file's header contains all four contract parts, including a paragraph beginning "What silently breaks if this guard is deleted".
  </acceptance_criteria>
  <done>Five 232-scoped guards exist, run under the existing `fast_checks` glob, and each reds on an emptied or restructured 232 ledger — so the phase's evidence obligations are mechanically enforced rather than remembered.</done>
</task>

</tasks>

## Flagged assumptions (spec-less edge probe)

This phase has no SPEC.md, so the deterministic edge probe ran in fallback mode. Two of its five rows
arrived `unclassified` and are surfaced here rather than silently dropped or auto-backstopped:

- **`PW-02 / unclassified`** — the probe could not classify PW-02's edge. Proposed classification:
  *the aggregator-reports-success-when-skipped edge* (D-22). A job whose `needs` dependency failed is
  skipped, and a skipped required check reports success. This is carried as an L3 obligation by plan
  232-12's red-probe task and by the `AFTER-PW02-REDPROBE` slot declared above; it is **not** authored
  into `must_haves.truths` here, so it remains an explicit flagged assumption until that run exists.
- **`PW-03 / unclassified`** — proposed classification: *the composite `seeds` default edge* (RESEARCH
  C-5). `example_http_smoke` is the only app-booting job in the repo that does not seed, so a composite
  defaulting `seeds` to true silently changes that lane. Carried by plan 232-06's input-default
  decision and plan 232-07's per-call-site explicitness; flagged, not asserted, here.

The three `PW-01` probe rows (adjacency, empty, ordering) are authored into `must_haves` in plans
232-03 and 232-04. Five probe items, five dispositions, no silent drops.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed ledger -> phase verdict | Text in `232-EVIDENCE.md` is what `/gsd-verify-work` and the milestone audit will treat as proof |
| Guard file -> merge gate | A guard that passes vacuously converts a missing proof into a green |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-05 | Repudiation | `232-EVIDENCE.md` slots | high | mitigate | `p17` requires a parseable status, a capture command, and the run id twice per captured slot; `p21` requires the producing method to be a committed script that exists on disk. |
| T-232-06 | Tampering | a vacuously-passing new guard | high | mitigate | Every new guard's first test is a non-vacuity floor (`slots.length >= 8`), and each is proven to exit non-zero under `GSD_PROHIB_SUBJECT=/dev/null`. |
| T-232-07 | Repudiation | silent reinterpretation of SC-1 | medium | mitigate | `p19` requires the `## Restated Success Criterion (SC-N)` heading in the ledger AND a ROADMAP prose pointer naming the ledger, so a reinterpretation cannot happen in only one of the two places. |
| T-232-08 | Tampering | retiring Phase 230 coverage by repointing its guards | high | mitigate | New files rather than edits; acceptance criterion asserts `git diff --stat` over the six 230-pinned guards is empty. |
| T-232-SC | Tampering | npm/pip/cargo installs | low | accept | Vacuous: no dependency manifest is touched (RESEARCH § Package Legitimacy Audit). |
</threat_model>

## Artifacts this phase produces

Created **by this plan** is marked ✱.

- **Scripts:** `scripts/ci/ci-step-metrics.sh`, `scripts/ci/ci-step-metrics.test.sh`,
  `scripts/ci/playwright-shard-nonempty.sh`, `scripts/ci/playwright-shard-nonempty.test.sh`
- **CI step names (new):** `CI step metrics self-test`, `Playwright shard non-emptiness self-test`
- **CI job ids (new):** `example_playwright_shard`, `example_playwright_design`
- **CI job `name:` strings (new):** `Example Playwright smoke shard ${{ matrix.seam }}`,
  `Example Playwright design gallery`
- **Workflow inputs (new):** `force_shard_fail_probe` (`workflow_dispatch`)
- **Composite action:** `.github/actions/boot-example-app/action.yml` — inputs `mix-env`, `seeds`,
  `node`, `browsers`, `browser-cache-hit`, `boot`, `port`, `pg-database`, `server-log`, `warm-paths`; outputs
  `deps-cache-hit`, `otp-version`, `elixir-version`
- **Playwright:** `test/example/priv/playwright/tests/auth.setup.ts`; project name `design-auth-setup`;
  config consts `AUTH_SETUP_FILE`, `DESIGN_STORAGE_STATE`; storageState path
  `test/example/priv/playwright/.playwright/design-admin.json`; `json` reporter outputFile
  `.playwright/last-run-report.json`
- **Prohibition guards (new):** ✱`p17-pw-evidence-run-id-provenance.test.mjs`,
  ✱`p18-pw-no-green-on-empty-count.test.mjs`, ✱`p19-pw-sc-restatement-recorded.test.mjs`,
  ✱`p20-pw-no-lane-green-because-skipped.test.mjs`, ✱`p21-pw-committed-method-provenance.test.mjs`
- **ExUnit contracts (new):** `test/sigra/planning/phase_232_playwright_auth_setup_test.exs`,
  `test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`,
  `test/sigra/planning/phase_232_playwright_shard_topology_test.exs`
- **Planning artifacts:** `232-EVIDENCE.md` (✱ slots `AFTER-PW01-PR`, `AFTER-PW01-NONPR`,
  `AFTER-PW03-NONPR`, `AFTER-PW02-PR`, `AFTER-PW02-CONTEXT`, `AFTER-PW02-REDPROBE`, `AFTER-PUSH`, and
  the `## Restated Success Criterion (SC-1)` section)

<verification>
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` — green, 21 guards.
- `GSD_PROHIB_SUBJECT=/dev/null node --test scripts/ci/prohibitions/p17-*.test.mjs` — non-zero (and the same for p18..p21).
- `mix test test/sigra/planning/` — green.
- `git diff --stat scripts/ci/prohibitions/p0*.test.mjs scripts/ci/prohibitions/p1[123]-*.test.mjs` — empty.
</verification>

<success_criteria>
Every observed-run obligation this phase owes is declared with a run id or a named future capture
command, SC-1's restatement is recorded where a guard can read it, and five Phase-232-scoped guards
red when any of that goes missing — none of which the 230-pinned guards could ever have caught.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-02-SUMMARY.md` when done.
</output>
