---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 05
type: execute
wave: 5
depends_on: ["232-04"]
files_modified:
  - .github/workflows/ci.yml
  - test/example/priv/playwright/playwright.config.ts
  - test/sigra/planning/phase_232_playwright_economics_test.exs
autonomous: true
requirements: [PW-02, PW-03]
must_haves:
  truths:
    - "D-04/D-06: independent matrix seams execute concurrently on separate runners, each owning its PostgreSQL service/database, app process, port/base URL, and log path; no runtime auth-schema prefix override exists."
    - "D-05: every shard command uses --retries=0; retries and continue-on-error do not mask interference."
    - "D-07: one fail-closed always-running terminal result has the byte-identical display name `Example Playwright smoke (full lifecycle)` and receives every shard outcome."
    - "D-08: every shard boots through the sole shared action from 232-04."
    - "All five covered UI-SPEC states, gallery inventory, role/stable selectors, viewport/theme projects, axe scans, and snapshot routing remain unchanged."
  assumptions:
    - "FLAGGED/UNRESOLVED PW-02 unclassified probe: the absent plain SPEC defines no additional shard-edge taxonomy; the plan assumes current seam/event routing is exhaustive and proves that set structurally and by live runs."
    - "FLAGGED/UNRESOLVED PW-03 unclassified probe remains: shared-boot equivalence is judged against every current consumer and its parameters, not an invented fallback."
  prohibitions:
    - statement: "MUST NOT claim parallel safety by only increasing workers/fullyParallel against a shared app or database, or by adding a runtime auth-schema prefix override."
      reason: "Bespoke isolation-integrity prohibition from D-04/D-06 and the two-stage fallback probe; descriptor-less and flagged-unverified."
    - statement: "MUST NOT let a shard become advisory, omitted from the terminal verdict, retried, or continue-on-error."
      reason: "Bespoke gate-honesty prohibition; descriptor-less and flagged-unverified."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "isolated Playwright matrix and exact-name terminal aggregator"
  key_links:
    - from: .github/workflows/ci.yml
      to: .github/actions/example-playwright-boot/action.yml
      via: "each matrix leg passes isolated DB/port/base-url/log inputs"
    - from: "matrix result"
      to: "Example Playwright smoke (full lifecycle)"
      via: "always-running fail-closed needs/result aggregator"
---

<objective>
Shard the residual Playwright seams across runner-isolated matrix jobs and preserve the required terminal verdict (D-04 through D-07).

Purpose: collapse wall-clock without creating cross-spec database/app interference or weakening merge protection.
Output: matrix seam topology, per-shard isolation, retry-zero commands, and the exact-name aggregator.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-04-SUMMARY.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock isolated shard inventory and retry-zero routing (D-04, D-05, D-06)</name>
  <files>test/sigra/planning/phase_232_playwright_economics_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` current seam ids/commands/event conditions and library matrix/result analog.
    - `test/example/config/dev.exs` existing PGDATABASE/PGPORT/PORT seams.
    - `test/example/priv/playwright/playwright.config.ts` worker/retry comments and project inventory.
    - `phase_230_design_gallery_split_test.exs` snapshot/recapture guards that must survive.
    - `232-RESEARCH.md` recommended five-seam mapping and Pattern 2/3.
    - The file listed in `<files>` before editing.
  </read_first>
  <behavior>
    - "matrix enumerates every existing independent seam and fail-fast is false."
    - "each row supplies a database, port/base URL, log path, exact command, and --retries=0."
    - "design PR/non-PR snapshot routing and recapture ungrepped behavior remain preserved."
  </behavior>
  <action>
    Add RED structural assertions for the resolved exhaustive five-row mapping: `admin_behavior`, `admin_checkpoints`, `design_gallery` (with its event-specific `design_gallery_snapshots` branch), `non_admin_smoke`, and `demo_showcase`. Assert `strategy.fail-fast: false`, one PostgreSQL service per matrix job, explicit PGDATABASE/PORT/SIGRA_EXAMPLE_URL/log inputs, use of the shared boot action, and `--retries=0` on every command. Assert no runtime auth-prefix variable/config and preserve Phase 230 snapshot/axe/recapture routing. Do not coalesce rows based on timing: the resolved research inventory preserves the five current failure-ownership seams one-to-one.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_economics_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before the topology edit for missing matrix/isolation/retry-zero assertions.
    - Expected seam set is explicit and non-empty; no generic substring count can pass with a missing seam.
    - Every row contract includes DB, app port/base URL, log, command, shared boot, and retry-zero data.
    - Tests explicitly reject auth-schema override and preserve snapshot/recapture routing.
  </acceptance_criteria>
  <done>A failing contract describes the complete isolated shard topology.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the matrix and exact-name fail-closed aggregator (D-04 through D-07)</name>
  <files>.github/workflows/ci.yml, test/example/priv/playwright/playwright.config.ts</files>
  <read_first>
    - `.github/workflows/ci.yml` `library_tests_shard`/`library_tests`, current `example_playwright_smoke`, `ci-gate.needs`, honest-skip manifest/verdict wiring, and shared-boot consumers after 232-04.
    - `.github/actions/example-playwright-boot/action.yml` — exact input contract each matrix row must satisfy.
    - `test/example/priv/playwright/playwright.config.ts` — update only the stale shared-global-state rationale; retain per-invocation serial defaults unless independently proven.
    - `MAINTAINING.md` required checks section — exact required context compatibility contract.
    - Every file listed in `<files>` before editing.
  </read_first>
  <action>
    Replace the serial seam execution job with a five-row matrix over `admin_behavior`, `admin_checkpoints`, `design_gallery`, `non_admin_smoke`, and `demo_showcase`; keep the non-PR design snapshot invocation in the `design_gallery` row's event branch. Give every leg its own runner-local `postgres:15` service and explicit database/app/log inputs into the shared boot action. Keep event/docs-only gates, browser/cache behavior, artifact diagnostics, snapshot PR/non-PR split, and each existing Playwright spec/project selection. Append `--retries=0` to every shard invocation and use `fail-fast: false` so all outcomes reach aggregation.

    Create a thin terminal job whose displayed name is byte-for-byte `Example Playwright smoke (full lifecycle)`, `needs` the matrix job (and any explicitly separate seam result), runs under `if: always()`, preserves legitimate docs-only behavior, and exits nonzero for any required non-success result. Keep its job id compatible with `ci-gate.needs` and honest-skip wiring or update all consumers atomically so no required edge disappears.

    Update the Playwright config comment to distinguish per-invocation serialization from global cross-seam correctness. Do not introduce a runtime auth-schema prefix, schema file, package, retries, or `continue-on-error`; do not change application/UI files.
  </action>
  <verify>
    <automated>mix test test/sigra/planning/phase_232_playwright_economics_test.exs &amp;&amp; mix test test/sigra/planning/ &amp;&amp; cd test/example/priv/playwright &amp;&amp; npx playwright test --list --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - The matrix contains exactly the five resolved rows `admin_behavior`, `admin_checkpoints`, `design_gallery`, `non_admin_smoke`, and `demo_showcase`, uses `fail-fast: false`, and each leg owns PostgreSQL/app/port/log inputs and the shared boot action.
    - Every Playwright command includes `--retries=0`; affected job regions contain no `continue-on-error`.
    - The sole terminal display name is exactly `Example Playwright smoke (full lifecycle)`, runs with `always()`, and fails closed on any shard result.
    - `ci-gate` and honest-skip consumers still depend on the terminal job id; docs-only required-context behavior remains explicit.
    - No auth-prefix runtime override, schema file, UI file, package manifest, or lockfile is changed.
    - Focused/planning tests and Playwright list exit 0 with preserved project/spec inventory.
  </acceptance_criteria>
  <done>Residual Playwright seams are structurally independent and all converge on the unchanged required verdict.</done>
</task>

</tasks>

<threat_model>
## Security Enforcement Contract

**ASVS baseline: L1; block threshold: high.** Any high-or-critical shard or gate finding blocks completion; medium/low findings follow the dispositions below.

| ASVS control area | Task mitigation | Verification mapping |
|---|---|---|
| V2 Authentication | Each seam uses the shared, existing UI-authentication-capable boot path; no cookie shortcut or retry mask. | Five-row structural contract and retry-zero Playwright inventory. |
| V3 Session Management | Isolate every shard's database/app process and preserve project-specific design state. | T-232-08/09 assertions for DB/port/log/state ownership. |
| V4 Access Control | Reject runtime auth-prefix overrides and preserve the terminal verdict over every protected-admin seam. | Focused negative contract plus exact-name aggregator assertions. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| matrix row → runner-local services | Row inputs select the database, app process, port, and command. |
| shard outcomes → terminal required check | Multiple job results collapse into one branch-protection verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-232-08 | Tampering | shared database/app | high | mitigate | Separate runner-local service/app per matrix job with explicit inputs and structural contracts. |
| T-232-09 | Repudiation | retry/continue masks | high | mitigate | `--retries=0`, no continue-on-error, live concurrent proof in plans 06-07. |
| T-232-10 | Tampering / Repudiation | terminal aggregator | high | mitigate | Exact stable name, `always()`, exhaustive needs, fail closed on non-success, real PR check proof. |
</threat_model>

## Artifacts this phase produces

- New workflow job: isolated Playwright matrix execution job.
- New matrix fields: seam id, database, port/base URL, log path, and exact retry-zero command (names follow the implemented exhaustive mapping).
- New/reshaped terminal result job with display name `Example Playwright smoke (full lifecycle)`.
- Updated config rationale for per-invocation serialization versus cross-seam correctness.
- No production schema, runtime auth-prefix flag, UI symbol, dependency, or application path.

<verification>
Structural suites and Playwright listing prove topology/inventory; live concurrency and required-context proof follow in plans 06-07.
</verification>

<success_criteria>
PW-02/PW-03 are structurally implemented with isolated retry-free shards, one boot definition, and an exhaustive exact-name aggregator.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-05-SUMMARY.md` when done.
</output>
