---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 13
type: execute
wave: 11
depends_on: ["232-12"]
files_modified:
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PW-01, PW-02, PW-03]

must_haves:
  truths:
    - "SC-2 is closed by a real observed run in which the sharded lane passed with `--retries=0` on the CLI and every leg recorded a non-zero executed test count — not by reasoning about isolation (D-17, D-33)."
    - "SC-3 is closed in both directions: the bare context `Example Playwright smoke (full lifecycle)` is observed resolving on a real pull request against ruleset 14941512, and a deliberately-failed leg is observed turning that same context red."
    - "Every number recorded in `232-EVIDENCE.md` was produced by a committed instrument invoked against a real run and cited by run id; none was estimated, rounded, or averaged before recording."
    - "The phase's residual gap to the 12m FAST-01 target is recorded as a measured number, whether or not the target was met — an honest shortfall is the deliverable, not a rounding opportunity."
    - "`AFTER-PUSH` closes as an explicit standing post-merge obligation carrying its capture command and its reason, not as a gap and not as a silent pass — `ci.yml` triggers on `pull_request: branches: [main]`, so the post-merge demotion receipt cannot exist before this branch merges."
    - statement: "No cross-spec database interference occurred between concurrently-running legs."
      verification: backstop
  artifacts:
    - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
    - .planning/ROADMAP.md
  key_links:
    - "`gh pr checks <n>` -> the bare required context string -> `gh api repos/szTheory/sigra/rulesets/14941512` -> the five contexts recorded at `MAINTAINING.md:102-113`, which is the durable BEFORE record this task compares against."
    - "`force_shard_fail_probe=true` dispatch -> one leg `failure` -> `needs.example_playwright_shard.result` -> the aggregator's `if: always()` body -> the required context's `failure` conclusion."
    - "`232-EVIDENCE.md` slots -> `p17`/`p18`/`p20`/`p21` -> `fast_checks`, so a ledger claim without a run id, without an executed count, or with an unexplained `skipped` reds on every PR."
  prohibitions:
    - "MUST NOT record a duration, an executed count, or a wall clock produced by any method other than a committed instrument (`ci-step-metrics.sh`, `ci-run-metrics.sh`, `ci-demotion-observer.sh`) or a bare `gh run`/`gh pr`/`gh api` invocation quoted verbatim."
    - "MUST NOT read a run before it reaches a terminal state — unfinished steps serialize `completedAt: 0001-01-01T00:00:00Z`, which every instrument here clamps to 0, producing a table of zeros that reads as a spectacular improvement."
    - "MUST NOT record a `skipped` job or step as evidence that work ran; a skipped required check reports success and a skipped lane proves nothing (D-33)."
    - "MUST NOT close `AFTER-PUSH` as captured pre-merge, and MUST NOT delete it or mark it not-applicable — it is a standing obligation with a named capture command."
    - "MUST NOT re-run a red observation until it is green and record only the green one without saying so; if a run is red for a reason this phase caused, fix the cause in the owning plan and record that the AFTER measurement came from the corrected run."
    - "MUST NOT smooth, round, or average a measured number before recording it."
---

<objective>
Close PW-02 by observation: run the sharded lane for real, read the numbers back by run id, and prove
the required check both resolves and reddens.

Purpose: D-33 binds this phase — success criteria are proven by running CI and reading measured
numbers, never by reading YAML, and a `skipped` job proves nothing. Everything before this plan is
structure. SC-2's "passes at `--retries=0` with no cross-spec interference", SC-3's "branch protection
still resolves it on a real PR", and the milestone's FAST-01 wall-clock claim are all statements about
what a run did.
Output: four `232-EVIDENCE.md` slots flipped from `pending` to `captured`, the SC-2 and SC-3 verdicts
recorded against their criteria, the measured residual gap to FAST-01, and `AFTER-PUSH` left as an
explicit standing post-merge obligation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Observe the sharded lane on a real pull_request run and capture AFTER-PW02-PR</name>
  <files>.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md</files>
  <precondition>The commits from plans 232-10 through 232-12 are pushed to the phase branch on `origin`, and the pull request opened for plan 232-05's `AFTER-PW01-PR` capture is still open on that branch. `gh auth status` succeeds.</precondition>
  <read_first>
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — the `AFTER-PW02-PR` slot's declared obligation and capture commands, the `BEFORE-STEPS` per-seam table, and the `AFTER-PW01-PR` slot whose wall clock this run is compared against
    - `scripts/ci/ci-step-metrics.sh` and `scripts/ci/ci-run-metrics.sh` — the flags each accepts, and `ci-run-metrics.sh`'s header contract naming it the sole authority for job-level and wall-clock claims
    - `.github/workflows/ci.yml` — the three Playwright job ids and their `name:` strings, so the display names passed to the instruments are read from the file rather than guessed
    - `232-VALIDATION.md` § The Observed-Run Evidence Contract — the `AFTER-PW02-PR` row and the eight-slot format rules
    - `232-RESEARCH.md` § Assumptions Log A2 (runner core count, readable from a `library_tests_shard` step summary) and A4 (the unmeasured seam balance this run finally settles)
  </read_first>
  <action>
Trigger and observe one real `pull_request` run of `ci.yml` on the phase branch, wait for it to reach a
terminal state, then flip `AFTER-PW02-PR` from `pending` to `captured (run <id>)`.

Wait for terminal state before reading anything. An unfinished step serializes
`completedAt: 0001-01-01T00:00:00Z`, which every instrument in this repo clamps to 0 — reading early
produces a table of zeros that looks like an enormous win.

Capture into the slot body:

1. Run id, head SHA, event type, and the run's conclusion.
2. The job list, from `gh run view <id> --repo szTheory/sigra --json jobs --jq '.jobs[] | {name, conclusion}'`,
   quoted verbatim — it must show every shard leg, `Example Playwright design gallery`, and
   `Example Playwright smoke (full lifecycle)`, with each leg's conclusion.
3. Verbatim `bash scripts/ci/ci-step-metrics.sh --run <id> --format table` output for each shard leg's
   display name and for the design job. Read those display names off the run's own job list rather than
   assuming the interpolated spelling.
4. Per-leg executed counts. Two sources, both recorded: the `list` reporter's `N passed (Xs)` tail
   quoted verbatim from each leg's log, and the `$GITHUB_STEP_SUMMARY` line
   `scripts/ci/playwright-shard-nonempty.sh` emits with its four `.stats` numbers. State the label each
   count belongs to. No leg may show zero — that is the E-3 hazard the wrapper exists to catch, and a
   zero here is a finding, not a formatting problem.
5. The `--retries=0` proof: quote the invocation line from at least one leg's log showing the flag was
   passed on the CLI, and quote Playwright's own header line for that invocation. D-17 requires the flag
   on the command line because `playwright.config.ts:55` sets `retries: process.env.CI ? 1 : 0`; a run
   that passed *because* it retried is not evidence for SC-2.
6. Any flaky or retried test on any leg, quoted. If the reporter shows a retry despite `--retries=0`, or
   a flake, stop and record it as a finding — that is precisely the cross-spec interference SC-2 claims
   does not exist, and recording a green summary over it would be the failure class this milestone
   exists to remove.
7. Wall clock: verbatim `bash scripts/ci/ci-run-metrics.sh --jobs <id>` output, plus the per-leg
   durations, plus the new critical path (the longest job on the run) named explicitly. Compare against
   the `AFTER-PW01-PR` wall clock and against the milestone's 29.5m mean / 27.3m p50 baseline, and state
   the delta in seconds and percent for each.
8. The leg-balance verdict: the longest and shortest leg durations and their ratio, answering RESEARCH
   assumption A4 with numbers, and stating whether the chosen composition idled a leg (the TEST-02
   failure mode). If it did, say so and quantify it.
9. For every job or step that reported `skipped` on this event, the literal word `skipped`, the gate
   that made the skip legitimate, and the event type it is legitimate for — `p20` requires both, and a
   skipped construct proves nothing.

Keep the slot grammar intact: one `## AFTER-PW02-PR` heading, one `Status: captured (run <id>)` line, at
least one fenced block naming a committed instrument, the run id repeated at least twice in the body.
Update the `## Slot Index` row. Change nothing outside `232-EVIDENCE.md` in this task.
  </action>
  <verify>
    <automated>node --test scripts/ci/prohibitions/p17-pw-evidence-run-id-provenance.test.mjs scripts/ci/prohibitions/p18-pw-no-green-on-empty-count.test.mjs scripts/ci/prohibitions/p20-pw-no-lane-green-because-skipped.test.mjs scripts/ci/prohibitions/p21-pw-committed-method-provenance.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - [L3] `gh run view <AFTER_PW02_PR_RUN_ID> --json event --jq .event` returns `pull_request` and `--json conclusion --jq .conclusion` returns `success`.
    - [L3] The slot records a non-zero executed test count for every shard leg and for the design job, each from both the reporter tail and the wrapper's summary line.
    - [L3] The slot quotes an invocation line showing `--retries=0` on the CLI, and records zero flaky and zero retried tests across all legs (or records them explicitly as a finding).
    - [L3] The slot contains verbatim `ci-step-metrics.sh` output for every Playwright job on the run and verbatim `ci-run-metrics.sh --jobs` output for the run.
    - [L3] The slot names the run's new critical-path job and its duration, with the delta against the `AFTER-PW01-PR` wall clock in seconds and percent.
    - [L1] The slot states the longest/shortest leg ratio and answers RESEARCH assumption A4 with a number.
    - [L1] `p17`, `p18`, `p20` and `p21` pass over the updated ledger.
    - [L1] `git status --porcelain` shows `232-EVIDENCE.md` as the only modified file.
  </acceptance_criteria>
  <done>SC-2's measurable half is backed by a terminal, green, real `pull_request` run in which every leg executed a recorded non-zero number of tests with retries disabled on the command line.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the required context in both directions — AFTER-PW02-CONTEXT and AFTER-PW02-REDPROBE</name>
  <files>.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md</files>
  <precondition>The same pull request is open and has at least one completed run of `ci.yml`, and `gh` can read the repository ruleset (`gh api repos/szTheory/sigra/rulesets/14941512` succeeds).</precondition>
  <read_first>
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — the `AFTER-PW02-CONTEXT` and `AFTER-PW02-REDPROBE` slots and the commands each declares
    - `MAINTAINING.md` — the `### Branch protection — enforced required checks (live ruleset)` section: the five context strings and the documented `gh api` verification command. This section is the durable BEFORE record for SC-3
    - `.github/workflows/ci.yml` — the `force_shard_fail_probe` input and the probe step plan 232-12 added, and the aggregator job whose conclusion the probe run is read for
    - `232-RESEARCH.md` § Common Pitfalls 2 and 3, and § Assumptions Log A7 (the ruleset's state was not re-queried during research and must be read now)
    - `.planning/phases/231-gate-honesty-nightly-revival/231-CONTEXT.md` — GATE-03's precedent: the fail direction was made credible by a deliberate red, not by argument
  </read_first>
  <action>
Close SC-3 with two observations that cannot be made from the workflow file.

**AFTER-PW02-CONTEXT — the context resolves.** Read the live ruleset with the command recorded at
`MAINTAINING.md:116` and quote its output verbatim: the list of required status-check contexts, and the
ruleset's `enforcement` value. Compare it item by item against the five strings recorded in
`MAINTAINING.md`'s branch-protection section, which is this phase's durable BEFORE record, and state
explicitly that `Example Playwright smoke (full lifecycle)` is present byte-identically and that the
list did not grow or shrink. Then read the PR's own checks with `gh pr checks <n>` and quote the output
verbatim, showing the bare context name reporting a terminal state — not `Example Playwright smoke
(full lifecycle) (behavior)` or any `Name (value)` variant, which is what an orphaned required context
looks like. Record the PR number, the head SHA the checks were read at, and the run id they came from.
State plainly in the slot that this is the observation SC-3 asks for and that no static read of `ci.yml`
substitutes for it. If any `Name (value)` variant appears, stop: that is Pitfall 2, a merge outage, and
it is fixed in plan 232-11, not recorded here.

**AFTER-PW02-REDPROBE — the context reddens.** Dispatch one run with the probe on:
`gh workflow run ci.yml --ref <branch> -f force_shard_fail_probe=true`. Wait for terminal state. Capture
verbatim the job list with each job's conclusion, and record four facts as separate lines: the probe leg
concluded `failure`; at least one sibling leg concluded `success`, which is `fail-fast: false` working;
`Example Playwright smoke (full lifecycle)` concluded `failure` rather than `skipped`, which is
`if: always()` working; and the aggregator's step log names the failing `needs` result. Quote the
aggregator's own output line. State in the slot why this observation is not optional: a job whose
`needs` dependency failed is skipped by default, GitHub reports a skipped required check as *success*,
and without `always()` a failing shard would silently let a PR merge — an argument that is only ever
settled by watching it happen.

Then dispatch or identify one ordinary run **without** the probe and record, in the same slot, that the
probe leg did not run on it and the aggregator concluded `success` — the probe must be shown inert, or
it is a permanent red rather than a self-test.

Flip both slots to `captured (run <id>)`, each with its own run id repeated at least twice in its body,
each with at least one fenced block containing the `gh` invocation that produced it, and update the
`## Slot Index` rows. Change nothing outside `232-EVIDENCE.md`.
  </action>
  <verify>
    <automated>node --test scripts/ci/prohibitions/p17-pw-evidence-run-id-provenance.test.mjs scripts/ci/prohibitions/p20-pw-no-lane-green-because-skipped.test.mjs scripts/ci/prohibitions/p21-pw-committed-method-provenance.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - [L3] The `AFTER-PW02-CONTEXT` slot quotes the live ruleset's required-context list verbatim, shows `enforcement: active`, and shows the five strings matching `MAINTAINING.md`'s record item for item.
    - [L3] The slot quotes `gh pr checks <n>` output in which the bare `Example Playwright smoke (full lifecycle)` context appears with a terminal state, and no `Example Playwright smoke (full lifecycle) (` variant appears anywhere in the output.
    - [L3] The `AFTER-PW02-REDPROBE` slot records a `workflow_dispatch` run with `force_shard_fail_probe=true` in which the probe leg is `failure`, a sibling leg is `success`, and the aggregator is `failure` — three conclusions read from `gh run view --json jobs`, not inferred.
    - [L3] The same slot records a run without the probe in which the aggregator is `success` and the probe leg did not execute, proving the probe inert.
    - [L1] Both slots carry `Status: captured (run <id>)` with the id repeated at least twice in the body and at least one fenced `gh` command block each.
    - [L1] `p17`, `p20` and `p21` pass over the updated ledger.
    - [L1] `git status --porcelain` shows `232-EVIDENCE.md` as the only modified file.
  </acceptance_criteria>
  <done>SC-3 is proven in both directions on real runs: the bare required context resolves on a live PR under an active ruleset, and a single failing leg turns that same context red while its siblings finish.</done>
</task>

<task type="auto">
  <name>Task 3: Record the SC-2 and SC-3 verdicts, the FAST-01 reconciliation, and the standing post-merge obligation</name>
  <files>.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — all eight slots as Tasks 1 and 2 and the earlier plans left them, and the `## Restated Success Criterion (SC-1)` section plan 232-02 wrote
    - `.planning/ROADMAP.md` Phase 232 — the four success criteria verbatim, the `**Proof discipline**` line, and the plan list
    - `.planning/REQUIREMENTS.md` — PW-01/PW-02/PW-03 and the FAST-01 baseline the milestone is judged against
    - `.planning/STATE.md` — the milestone baseline (PR mean 29.5m / p50 27.3m, n=21) and the verification philosophy
    - `232-CONTEXT.md` D-13 — the predicted ~175s PR-side PW-01 saving and the ~13.6m projection, and D-31's five-step ordering
    - `232-VALIDATION.md` § Validation Sign-Off — the checklist this record must satisfy
  </read_first>
  <action>
Write the phase's measured verdict, then leave the one obligation that cannot close pre-merge stated as
an obligation rather than as a gap.

**Verdicts.** Beneath the existing SC-1 verdict, add one short subsection per remaining criterion, each
headed with the verdict word (met / not met / partially met) followed by its evidence, every line
carrying the run id it came from:

- *SC-2* — that the lane ran sharded with per-leg database and app and passed at `--retries=0`, citing
  the per-leg executed counts and the absence of retries and flakes. State the interference question
  honestly: the isolation is structural (one runner VM, one Postgres service, one database name per
  leg) and the evidence for "no interference" is a green run at zero retries, which is what SC-2 asks
  for and is also the limit of what a single run can show. If any leg was retried or flaked, the verdict
  is *not met* regardless of the run's colour.
- *SC-3* — both directions, citing the ruleset read, the `gh pr checks` output, and the red-probe run.
- *SC-4* — the verdict already recorded by plan 232-09, cited by reference so the ledger reads as one
  document rather than repeated.

**FAST-01 reconciliation.** In one short section, put the four numbers side by side: the milestone
baseline (29.5m mean / 27.3m p50), the post-230 measured critical path, the `AFTER-PW01-PR` wall clock,
and the `AFTER-PW02-PR` wall clock — each with its run id and each produced by `ci-run-metrics.sh`.
State the total measured reduction in seconds and percent, name the run's new critical-path job, and
state the residual gap to the 12m target as a number. Reconcile against D-13's prediction explicitly:
it priced PW-01's PR-side saving at roughly 175s and projected ~13.6m. If the measured result is worse,
say so plainly and name the consequence for the milestone — Phases 233 and 234 must carry the
remainder, and Phase 235 ratifies whatever the real number is. Do not round, average, or smooth any
number before recording it, and do not present a projection as a measurement.

**The standing obligation.** Leave `AFTER-PUSH` as `pending`, with its parenthetical containing the
literal word `obligation` (`p17` matches on it), and expand its body to state four things: that
`ci.yml` triggers on `pull_request: branches: [main]` so a post-merge demotion receipt cannot exist
before this branch merges — it is post-merge by construction, not an oversight; the exact capture
command (`bash scripts/ci/ci-demotion-observer.sh --run <id> --format table`); what a PASS must show
(the tier-B `design_gallery_snapshots` step observed executing on its new parent job
`Example Playwright design gallery`, and `admin_eval_render` observed executing, both on a
non-`pull_request` run); and who closes it — the first `ci-observe.yml` run after this phase's merge,
which runs automatically. Add a matching one-line note in the Slot Index. This is the same treatment
Phase 230 gave its two post-merge slots, and it is what keeps an unclosable-in-phase obligation from
being read either as a gap or as a silent pass.

**ROADMAP.** The Phase 232 block already carries its plan count and per-wave plan list (written at
plan time). Reconcile it with what actually shipped — tick each plan's checkbox, and if execution added
or renumbered a plan, correct the list and its wave grouping rather than leaving a stale one. Then add
one `**Measured**:` line after `**Proof discipline**` naming the before and after critical-path numbers
with their run ids and pointing at `232-EVIDENCE.md` for the full record. Do not restate the success
criteria there and do not edit any other phase's block.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && mix test test/sigra/planning/</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `232-EVIDENCE.md` contains a verdict subsection for SC-1, SC-2, SC-3 and SC-4, each with an explicit verdict word and at least one run id per evidentiary claim.
    - [L1] The FAST-01 reconciliation section contains four wall-clock numbers, each attributed to a run id and to `ci-run-metrics.sh`, plus the residual gap to 12m as an explicit number.
    - [L1] The reconciliation states whether D-13's ~175s / ~13.6m prediction held, in plain language, without smoothing the measured values.
    - [L1] `AFTER-PUSH` is still `Status: pending (...obligation...)`, its body names the capture command, what a PASS must show, and why it cannot close pre-merge; it is neither deleted nor marked captured.
    - [L1] All eight slots are present; seven are `captured (run <id>)` and exactly one (`AFTER-PUSH`) is `pending`.
    - [L1] `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` passes, including `p17`–`p21` over the completed ledger.
    - [L1] `.planning/ROADMAP.md`'s Phase 232 block lists every plan with its wave and no longer says `TBD`, and `git diff .planning/ROADMAP.md` touches no other phase block.
    - [L1] `mix test test/sigra/planning/` passes.
  </acceptance_criteria>
  <done>The phase's four criteria carry measured verdicts with run ids, the milestone's remaining gap to FAST-01 is a recorded number rather than a hope, and the one obligation that cannot close before merge is stated as an obligation with its command and its reason.</done>
</task>

</tasks>

## Edge-probe coverage carried by this plan

This plan closes the `PW-02 / unclassified` probe row that plans 232-02, 232-11 and 232-12 carried
forward as a flagged assumption: the `AFTER-PW02-REDPROBE` slot is the observation that turns *the
aggregator-reports-success-when-skipped edge* from an argument into a recorded run id. With this plan
all five probe-surfaced rows are accounted for — `PW-01 / empty` in plan 232-03, `PW-01 / adjacency` and
`PW-01 / ordering` in plan 232-04, `PW-03 / unclassified` as a flagged assumption resolved by plan
232-07's explicit per-call-site `seeds` values, and `PW-02 / unclassified` here. None was dropped and
none was auto-dismissed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a real CI run -> the evidence ledger | The ledger is the only durable record of what was observed; an unsourced number in it is indistinguishable from a guess |
| the live branch ruleset -> the repository | Required-context configuration lives in a GitHub API surface, outside git |
| a probe dispatch -> the merge gate | A deliberate red is being produced against a merge-blocking lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-60 | Repudiation | a measured claim with no run id | high | mitigate | `p17` requires `captured (run <id>)` with the id twice in the slot body; `p21` requires a committed producing method in a fenced block; both run in `fast_checks`. |
| T-232-61 | Spoofing | a duration read from a non-terminal run | high | mitigate | Prohibition plus an explicit wait-for-terminal instruction; instruments clamp zero-dated steps to 0, so an early read is silently plausible without it. |
| T-232-62 | Tampering | a green summary written over an observed flake or retry | critical | mitigate | Task 1 requires flakes and retries to be quoted and turns SC-2's verdict to *not met* if any occurred; `p18` requires an executed count and `p20` forbids a `skipped` construct standing as evidence. |
| T-232-63 | Elevation | the probe leaving the lane permanently red | medium | mitigate | Task 2 requires a second, probe-free run showing the aggregator `success` and the probe leg absent, recorded in the same slot. |
| T-232-64 | Information Disclosure | run logs or ruleset output quoted into the ledger carrying a credential | low | mitigate | The only credential in scope is the seeded demo password, which is public by design and already committed in `helpers/adminFlows.ts`; `gh` output quoted is job/step metadata and check names. Quote command output, never environment dumps. |
| T-232-65 | Repudiation | an unclosable obligation read as a gap or as a pass | medium | mitigate | `AFTER-PUSH` stays `pending` with the literal word `obligation`, its capture command, its PASS definition and its reason — the treatment Phase 230 established for its two post-merge slots. |
| T-232-SC | Tampering | npm/pip/cargo installs | low | accept | Vacuous: no dependency manifest is touched by this plan. |
</threat_model>

## Artifacts this phase produces

Created **by this plan** is marked ✱.

- **Scripts:** `scripts/ci/ci-step-metrics.sh`, `scripts/ci/ci-step-metrics.test.sh`,
  `scripts/ci/playwright-shard-nonempty.sh`, `scripts/ci/playwright-shard-nonempty.test.sh`
- **CI step names (new):** `CI step metrics self-test`, `Playwright shard non-emptiness self-test`,
  `Forced-failure probe (shard aggregator self-test)`
- **CI step id (new):** `boot_example_app`
- **CI job ids (new):** `example_playwright_design`, `example_playwright_shard`
- **CI job `name:` strings (new):** `Example Playwright design gallery`,
  `Example Playwright smoke shard ${{ matrix.seam }}`
- **Artifact names (new):** `admin-example-failure-diagnostics-${{ matrix.seam }}`,
  `admin-example-failure-diagnostics-design`
- **Workflow inputs (new):** `force_shard_fail_probe` (`workflow_dispatch`, default `false`)
- **Cache key token (changed):** `playwright-chromium-webkit-1.59.1-v3`
- **Composite action:** `.github/actions/boot-example-app/action.yml` — inputs `mix-env`, `seeds`,
  `node`, `browsers`, `browser-cache-hit`, `boot`, `port`, `pg-database`, `server-log`, `warm-paths`;
  outputs `deps-cache-hit`, `otp-version`, `elixir-version`
- **Playwright:** `test/example/priv/playwright/tests/auth.setup.ts`,
  `test/example/priv/playwright/helpers/storageState.ts`; project name `design-auth-setup`; config
  consts `AUTH_SETUP_FILE`, `DESIGN_STORAGE_STATE`; storageState path
  `test/example/priv/playwright/.playwright/design-admin.json`; `json` reporter outputFile
  `.playwright/last-run-report.json`
- **Prohibition guards (new):** `p17`–`p21` as named by plan 232-02
- **ExUnit contracts (new):** `test/sigra/planning/phase_232_playwright_auth_setup_test.exs`,
  `test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`,
  `test/sigra/planning/phase_232_playwright_shard_topology_test.exs`
- **Manifest rows (new):** tier-C `example_playwright_design`, tier-C `example_playwright_shard`
- **Planning artifacts:** ✱`.planning/phases/232-…/232-EVIDENCE.md` (completed: seven captured slots,
  one standing obligation, four SC verdicts, the FAST-01 reconciliation),
  `.planning/phases/232-…/COVERAGE.md`, ✱the Phase 232 plan list and `**Measured**:` line in
  `.planning/ROADMAP.md`

<verification>
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` — green over the completed ledger.
- `mix test test/sigra/planning/` — green.
- Seven of eight evidence slots `captured (run <id>)`; `AFTER-PUSH` `pending (<obligation>)` with its
  capture command and its reason.
- `gh run view` / `gh pr checks` / `gh api …/rulesets/14941512` outputs quoted verbatim in the slots
  they belong to.
</verification>

<success_criteria>
PW-02 is closed by observation rather than by argument: the sharded lane passed at `--retries=0` with a
recorded non-zero executed count on every leg, the bare required context resolved on a live PR under an
active ruleset and reddened when one leg was deliberately failed, the milestone's remaining gap to
FAST-01 is a measured number, and the single obligation that cannot close before merge is recorded as
an obligation with its command, its PASS definition and its reason.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-13-SUMMARY.md` when done.
</output>
