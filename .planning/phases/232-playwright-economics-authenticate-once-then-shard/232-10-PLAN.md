---
phase: 232-playwright-economics-authenticate-once-then-shard
plan: 10
type: execute
wave: 8
depends_on: ["232-05", "232-09"]
files_modified:
  - .github/workflows/ci.yml
  - .github/ci-skip-manifest.tsv
  - MAINTAINING.md
  - test/sigra/planning/phase_230_design_gallery_split_test.exs
  - .planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md
autonomous: false
requirements: [PW-02]

must_haves:
  truths:
    - "The PW-02 shard axis is chosen from the measured per-seam durations captured by plans 232-01 and 232-05, not from a guess — RESEARCH assumption A4 is closed with numbers before any job is restructured."
    - "The two design seams (`design_gallery`, `design_gallery_snapshots`) run on their own non-matrix job whose `name:` is a static literal, so `p10`'s literal ci.yml match and `ci-demotion-observer.sh`'s API-interpolated lookup resolve to the same string — the manifest/observer conflict is resolved by construction rather than by weakening either consumer."
    - "The required context `Example Playwright smoke (full lifecycle)` is still produced by a job of that exact name after this plan — the extraction is a strict subtraction from that job, not a rename of it."
    - "Every seam id that existed before this plan still reaches an aggregator: four remain in `example_playwright_smoke`'s loop and two move into the new job's loop, and their union is still the same six."
    - "`.github/ci-skip-manifest.tsv`, `MAINTAINING.md`, `.github/workflows/ci.yml` and `phase_230_design_gallery_split_test.exs` change in ONE commit, because `p10` and the job-scoped ExUnit contracts red the moment ci.yml and the manifest disagree (D-24, D-32)."
    - statement: "The Playwright browser cache's bare-prefix `restore-keys` stays safe once more than one job consumes it, because every consumer installs the same `chromium webkit` set on both cache branches."
      verification: backstop
  artifacts:
    - .github/workflows/ci.yml
    - .github/ci-skip-manifest.tsv
    - MAINTAINING.md
    - test/sigra/planning/phase_230_design_gallery_split_test.exs
  key_links:
    - "`.github/ci-skip-manifest.tsv` tier-B step row `design_gallery_snapshots` -> its new `parent_job_id` `example_playwright_design` -> that job's static `name: Example Playwright design gallery`, which is what `ci-demotion-observer.sh:151-153` looks up against the Actions API on every post-merge non-PR run."
    - "`example_playwright_design`'s `Aggregate Playwright step outcomes` step -> `steps.design_gallery.outcome` and `steps.design_gallery_snapshots.outcome` -> the job's own conclusion -> plan 232-11's thin aggregator, which is the required context."
    - "`232-EVIDENCE.md`'s per-seam AFTER table -> Task 1's decision record -> the `matrix.seam` list plan 232-11 writes."
  prohibitions:
    - "MUST NOT put the required `name:` string `Example Playwright smoke (full lifecycle)` on a matrix job, now or later — Actions emits `Name (value)` contexts and the ruleset's bare context never resolves, which is a merge outage rather than a test failure."
    - "MUST NOT resolve the manifest/observer conflict by teaching `p10` to normalize `${{ }}` out of a display name, nor by loosening `ci-demotion-observer.sh` to prefix matching — both weaken a live fail-closed instrument to accommodate a layout choice that has a lossless alternative."
    - "MUST NOT add `example_playwright_design` (or any later shard job) to `ci-gate.needs` — `honest-skip-verdict.sh` holds a fixed nine-lane list cross-checked bidirectionally and reds immediately (D-23)."
    - "MUST NOT delete or job-scope-weaken any assertion in `phase_230_design_gallery_split_test.exs`; re-anchor it to the job that now owns the construct and record the re-anchoring in-file."
    - "MUST NOT remove the `example_playwright_smoke` row from `.github/ci-skip-manifest.tsv` — `honest-skip-verdict.sh` fails closed below five manifest rows intersecting the nine `ci-gate.needs` lanes, and this is one of the five."
    - "MUST NOT prove any part of this plan by reading `.github/workflows/ci.yml` alone; static reads are necessary-but-never-sufficient pre-checks (D-33)."
---

<objective>
Take the design-gallery seams off the job that carries the required check, onto a job of their own,
and choose the shard axis from the numbers plans 232-01 and 232-05 measured.

Purpose: PW-02's restructuring has one unresolved constraint that fails closed in two directions at
once. `p10` requires `.github/ci-skip-manifest.tsv` to record a `display_name` that matches a literal
`^    name:` line in `ci.yml`; `ci-demotion-observer.sh:151-153` requires that same cell to match the
*interpolated* name the Actions API returns. For a matrix job those two demands are incompatible.
Keeping the demoted `design_gallery_snapshots` step on a non-matrix job with a static name satisfies
both at full strength and changes no live instrument — RESEARCH's recommended resolution (a). Doing it
first also shrinks the job that plan 232-11 turns into a matrix, so the matrix carries only seams whose
couplings tolerate it.
Output: a new `example_playwright_design` job, a four-seam `example_playwright_smoke`, a manifest and
`MAINTAINING.md` that describe the new shape in the same commit, re-anchored Phase 230 contracts, and a
recorded shard-axis decision in `232-EVIDENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-CONTEXT.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-RESEARCH.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-PATTERNS.md
@.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Choose the shard axis and leg composition from the measured seam table</name>
  <decision>
How many matrix legs `example_playwright_shard` will have in plan 232-11, and which of the four
residual seams (`admin_behavior`, `admin_checkpoints`, `non_admin_smoke`, `demo_showcase`) each leg
runs.
  </decision>
  <context>
Present the measured evidence before the options, read out of
`.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md`:

- the `BEFORE-STEPS` per-seam duration table (plan 232-01),
- the AFTER column added by plan 232-05 Task 2, with the run id each cell came from,
- the boot-prelude cost per job, read from the same tables (the `Wait for app and warm up LiveView
  routes` step plus everything before it) — this is the fixed toll every additional leg pays,
- the measured PR wall clock from `bash scripts/ci/ci-run-metrics.sh --jobs <AFTER_PR_RUN_ID>`, and
  the residual gap to the 12m FAST-01 target recorded in plan 232-05 Task 3.

Two couplings are not negotiable and constrain every option: `admin_checkpoints` must stay in the same
leg as its `Stage admin checkpoint PNGs` step and the `Admin artifact bundle contract` step (the
contract enforces a 15-PNG floor over `test-results/` produced by that seam alone); and the design
seams are not on this axis at all — Task 2 moves them to their own job.

The decision rule to apply, stated before looking at the options: choose the composition that minimises
`max(leg wall clock)` where a leg's wall clock is the measured boot-prelude cost plus the sum of its
seams' measured durations. Where two compositions are within ~60s of each other on that metric, prefer
the one with fewer legs, because each leg pays the full prelude in runner minutes.
  </context>
  <options>
    <option id="option-a">
      <name>Three legs: behavior | checkpoints | smoke (non_admin_smoke + demo_showcase)</name>
      <pros>Isolates the two heaviest seams; `demo_showcase` is a single spec and folds into the smoke leg for free; keeps the checkpoints coupling intact in one leg.</pros>
      <cons>Pays the boot prelude three times. If `admin_behavior` alone dominates, the extra leg buys little.</cons>
    </option>
    <option id="option-b">
      <name>Two legs: checkpoints | everything else (behavior + non_admin_smoke + demo_showcase)</name>
      <pros>Cheapest in runner minutes; the checkpoints coupling is isolated; likely the best ratio if the checkpoints seam is the pole.</pros>
      <cons>The combined leg may still dominate the wall clock, leaving FAST-01 unmet by PW-02 alone.</cons>
    </option>
    <option id="option-c">
      <name>Four legs: one per seam</name>
      <pros>Maximum theoretical parallelism; simplest seam-to-leg mapping.</pros>
      <cons>`demo_showcase` is one spec — a whole runner and a full prelude for a few seconds of work is the TEST-02 idling failure mode in a new place; four preludes is the highest runner-minute cost of the three.</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md` — the `BEFORE-STEPS` and `AFTER-PW01-PR` / `AFTER-PW01-NONPR` slots and the per-seam table with its AFTER column
    - `.github/workflows/ci.yml` — the `example_playwright_smoke` job as plan 232-07 left it: the composite call step, the six seam step ids, the `Stage admin checkpoint PNGs` step gated on `steps.admin_checkpoints.outcome`, and the `Admin artifact bundle contract` step
    - `scripts/ci/admin-artifact-bundle-contract.sh` — the `MIN_COUNT` default of 15 and what it counts
    - `232-RESEARCH.md` § Sharding Mechanics ▸ "The three couplings that constrain the shard axis" and ▸ "Two candidate axes"; § Assumptions Log A2 (runner core count) and A4 (unmeasured seam balance)
    - `.planning/ROADMAP.md` Phase 233 — TEST-02's "one shard no longer idles while the other works", the failure mode this decision exists to avoid repeating
  </read_first>
  <resume-signal>Select: option-a, option-b, or option-c (or describe a different composition with the numbers that justify it)</resume-signal>
  <acceptance_criteria>
    - [L3] The evidence presented at the checkpoint quotes a measured duration for every one of the six seams, each labelled with the run id it came from — no seam is presented as an estimate.
    - [L1] The chosen composition, the `max(leg wall clock)` arithmetic for all three options, and the decision rationale are appended to `232-EVIDENCE.md` under the `AFTER-PW01-NONPR` slot's handoff paragraph, naming the run ids used.
    - [L1] The record states explicitly whether the chosen composition is projected to reach the 12m FAST-01 target, and if not, by how many seconds it falls short — an honest shortfall recorded now is worth more than a number discovered at ratification.
    - [L1] `node --test scripts/ci/prohibitions/p17-pw-evidence-run-id-provenance.test.mjs` passes over the amended ledger.
  </acceptance_criteria>
  <done>The shard axis is a recorded decision backed by measured per-seam durations, and plan 232-11 has a concrete `matrix.seam` list to write.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the design seams onto example_playwright_design — ci.yml, manifest, docs and contracts in one commit</name>
  <files>.github/workflows/ci.yml, .github/ci-skip-manifest.tsv, MAINTAINING.md, test/sigra/planning/phase_230_design_gallery_split_test.exs</files>
  <precondition>Plans 232-06 through 232-09 have landed, so `.github/actions/boot-example-app/action.yml` exists and `example_playwright_smoke` already calls it with `id: boot_example_app`.</precondition>
  <reversibility rating="costly">Moving a demoted step to a new parent job changes the cell `ci-demotion-observer.sh` resolves post-merge; a mistake is revertible by one commit but is only observed on a non-PR run after merge, so the cost is a delayed, noisy detection rather than an immediate red.</reversibility>
  <read_first>
    - `.github/workflows/ci.yml` — the whole `example_playwright_smoke` job as plan 232-07 left it: job header (`name:`, `timeout-minutes: 45`, `needs: [release_ref_guard, changes]`, the `if:` expression, the `services.postgres` block), the browser-cache step `id: playwright_browsers_cache` with its key literal and its rationale comment, the composite call step `id: boot_example_app` with its full `with:` block, the six seam steps, the `Aggregate Playwright step outcomes` step and its six-id loop
    - `.github/workflows/ci.yml` — `library_tests` (`ci.yml:604-622`) for the `if: always()` + `needs.<job>.result != 'success'` idiom, referenced here only so the new job's relationship to the future aggregator is understood
    - `.github/ci-skip-manifest.tsv` — the full header (column semantics, tier definitions) and the tier-B row `design_gallery_snapshots` plus the tier-C row `example_playwright_smoke`
    - `scripts/ci/prohibitions/p10-no-undocumented-demotion.test.mjs` — every assertion, especially `stepIf()` (which resolves a step's `if:` by `^ {8}id: <id>` inside its recorded parent's block), the `display_name` literal match `^ {4}name: …`, and the "every step row names a parent that is itself a manifest job row" test
    - `scripts/ci/ci-demotion-observer.sh:151-153` and `:184-187` — the parent-display-name lookup and the ≥2 assert-row floor
    - `scripts/ci/honest-skip-verdict.sh:101-111` and `:172-185` — the fixed nine-lane list and the ≥5 manifest-intersection floor that forbids deleting the `example_playwright_smoke` row
    - `scripts/ci/prohibitions/p02-axe-signal-not-reduced.test.mjs:68-86` — the `id: design_gallery` regex, which is file-wide and job-agnostic, and the three `--project` flags plus `--grep-invert` it requires to survive verbatim
    - `test/sigra/planning/phase_230_design_gallery_split_test.exs` — `extract_job/2` (job-id regex `^  ([a-z_]+):$`), the `@aggregated_seam_ids` list, the test "the PR lane is filtered and the snapshot lane is event-gated" (its `id: design_gallery_snapshots\n(.*?)- name: Run non-admin` adjacency regex), and the test "the aggregator enumerates every seam id"
    - `MAINTAINING.md` — the `### Honest-skip set after Phase 230` section, its tier-B bullet describing `design_gallery_snapshots` "inside `example_playwright_smoke`", and § residual 4 near the end of the honest-skip discussion
    - `232-RESEARCH.md` § Sharding Mechanics ▸ "The manifest/observer conflict"; § Runtime State Inventory category 5 (the browser cache's sole-consumer premise); § Guard and Prohibition Surface rows for `p02`, `p10`, `honest-skip-verdict.sh`, `ci-demotion-observer.sh`, `phase_230_design_gallery_split_test.exs`
  </read_first>
  <action>
Create a new job `example_playwright_design` in `.github/workflows/ci.yml`, immediately after
`example_playwright_smoke`, and move exactly two steps into it. Then make every consumer that names
those constructs agree, in the same commit.

**The new job.** `name: Example Playwright design gallery` — a static literal with no `${{ }}` in it,
because `p10` matches the manifest's `display_name` against `^    name: …` in ci.yml while
`ci-demotion-observer.sh` matches the same cell against the Actions API's returned name; a static name
is the only spelling both resolve. `runs-on: ubuntu-latest`. `timeout-minutes: 45` — the same ceiling
`example_playwright_smoke` carries, with a comment stating that the value is inherited rather than
measured and that re-pinning belongs to Phase 235, which is what `p09`'s own message instructs.
`needs: [release_ref_guard, changes]` and the same `if: ${{ !cancelled() && needs.release_ref_guard.result == 'success' }}`
expression the source job uses — copy it, do not re-derive it, and copy the comment above it explaining
why it is `!cancelled()` and not `always()`. Copy the `services.postgres` block verbatim: this job boots
its own app against its own database, which is the isolation shape D-15 requires and the reason a
concurrent design lane cannot interfere with the shard legs.

**Its steps, in order.** (1) The Playwright browser cache step, copied from `example_playwright_smoke`
with `id: playwright_browsers_cache` and the identical key and `restore-keys` values — see the
re-tokening instruction below. (2) The composite call `- uses: ./.github/actions/boot-example-app` with
`id: boot_example_app`, carrying the same docs-only `if:` and the same `with:` block the source job
uses (`mix-env: 'dev'`, `node: 'true'`, `seeds: 'true'`, `browsers: 'chromium webkit'`,
`browser-cache-hit: ${{ steps.playwright_browsers_cache.outputs.cache-hit }}`, `boot: 'true'`,
`port: '4000'`, the eight-path `warm-paths` list), except `server-log`, which becomes
`/tmp/example-design-server.log` so the two jobs' logs are distinguishable. (3) The `design_gallery`
step, moved verbatim — its `id:`, its `if:`, its `working-directory`, its `env:`, its whole comment
block and its exact `npx playwright test` invocation with the three `--project` flags and
`--grep-invert '@snapshot'`. (4) The `design_gallery_snapshots` step, moved verbatim, including its
non-PR `if:` and its full comment block, except the one sentence in that comment claiming the step
"lives inside this job rather than a new job because the job name … is a ruleset-required context" —
that premise is now false and must be replaced with the current one: the step lives on this job, whose
result flows into the required aggregator through `needs`, and it stays adjacent to its `design_gallery`
sibling so both halves of the same spec file share one boot. (5) A `Aggregate Playwright step outcomes`
step for this job, `if: always()`, structurally identical to the source job's — same `set -euo pipefail`,
same `fail` / `all_skipped` two-variable loop, same messages — iterating exactly
`steps.design_gallery.outcome` and `steps.design_gallery_snapshots.outcome`. Carry both halves of the
loop: the `all_skipped` half is Phase 231 GATE-03's signal for telling a correctly-gated skip from a
rotted one. (6) A `Dump example app log (on failure)` step reading the new log path, copied from the
source job's equivalent.

**What leaves `example_playwright_smoke`.** Delete the two moved steps and remove
`steps.design_gallery.outcome` and `steps.design_gallery_snapshots.outcome` from its aggregator loop,
leaving exactly four: `admin_behavior`, `admin_checkpoints`, `non_admin_smoke`, `demo_showcase`. Add a
comment above that loop recording that the two design ids moved to `example_playwright_design` in Phase
232 and that the union of the two loops is still the original six — the invariant is "every seam id
reaches an aggregator", not "every seam id is in this loop". Change nothing else in that job: its
`name:` stays byte-identical, its `timeout-minutes: 45` stays, its remaining four seam ids stay, the
checkpoint staging and artifact-bundle steps stay.

**Browser-cache re-tokening.** The comment on the browser cache step argues its bare-prefix
`restore-keys` is safe because "its ONLY consumer is this job". That premise dies here. In both copies
of the step, change the trailing cache-key token from `-v2` to `-v3` and rewrite the comment to state
the true consumer set (`example_playwright_smoke` and `example_playwright_design` after this plan,
plus `example_playwright_shard` after 232-11), and to state why the prefix restore is still safe: every
consumer installs the same `chromium webkit` pair on both the hit and miss branches, so no consumer can
restore a browser set it did not earn. Keep the two key literals byte-identical to each other —
`scripts/ci/playwright-cache-key-guard.sh` greps ci.yml and takes the first match, so a divergence
between copies would make the guard assert against one job while another drifts.

**`.github/ci-skip-manifest.tsv`, same commit.** Change the tier-B `design_gallery_snapshots` row's
`parent_job_id` from `example_playwright_smoke` to `example_playwright_design` and its parent's
resolvable name accordingly; the row's own `display_name` (`Run design gallery board snapshots (non-PR)`),
`gate` and `observer: assert` are unchanged because the step moved verbatim. Add a tier-C `kind=job` row
for `example_playwright_design` with `display_name` `Example Playwright design gallery`,
`gate_level: step`, `gate` `needs.changes.outputs.docs_only != 'true'`, `observer: ignore` — a step row's
parent must itself be a manifest job row or `p10` reds, and the job is diff-gated at step level exactly
like the four required lanes. Keep the `example_playwright_smoke` row: `honest-skip-verdict.sh` fails
closed below five manifest rows intersecting the nine `ci-gate.needs` lanes and this is one of the five.

**`MAINTAINING.md`, same commit (D-32).** In the tier-B bullet, change "inside `example_playwright_smoke`"
to name `example_playwright_design`, and update the sentence about the seam-outcome aggregator to say
which job's loop now carries the id and that the job's result reaches the required context through
`needs`. In the tier-C paragraph, add `example_playwright_design` to the enumerated diff-gated
constructs. Add one dated sentence recording that Phase 232 split the design seams onto their own job
so the demoted step's parent has a static, non-interpolated name that both `p10` and
`ci-demotion-observer.sh` can resolve. Do not restructure any other section — this file is a renderer of
the manifest, and an unrelated rewrite here is churn a reviewer cannot attribute.

**`test/sigra/planning/phase_230_design_gallery_split_test.exs`, same commit.** Re-anchor the two
job-scoped tests that this move breaks, and record the re-anchoring in-file. In "the PR lane is filtered
and the snapshot lane is event-gated", change `extract_job(ci, "example_playwright_smoke")` to
`extract_job(ci, "example_playwright_design")` and change the adjacency regex's terminator from
`- name: Run non-admin` to the step name that now follows `design_gallery_snapshots` in the new job
(`- name: Aggregate Playwright step outcomes`) — the assertion's meaning ("the snapshot step's body
carries the non-PR gate") is preserved; only the anchor moves. In "the aggregator enumerates every seam
id", split the single job read into two: assert the four residual ids appear in
`example_playwright_smoke`'s aggregator region and the two design ids appear in
`example_playwright_design`'s, and add an explicit assertion that the union of the two id sets equals
`@aggregated_seam_ids` — so the contract still fails if a seam id stops reaching any aggregator. Add a
module-level comment recording that Phase 232 re-anchored these tests, which job now owns which
construct, and that the invariant is unchanged. Leave the four spec-pinning tests (the describe title,
the axe-helper separation, the untagged axe test, the 13+11+4 board counts) untouched — D-10 requires
them green through this phase.

Commit all four files together. `p10` resolves a step row's `if:` inside the block of the job the
manifest names as its parent, and the job-scoped ExUnit tests read ci.yml directly: any commit that
lands ci.yml without the manifest, or the manifest without the contracts, is red between commits.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs && mix test test/sigra/planning/ && bash scripts/ci/honest-skip-verdict.test.sh && bash scripts/ci/ci-demotion-observer.test.sh && bash scripts/ci/playwright-cache-key-guard.sh && bash scripts/ci/playwright-cache-key-guard.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - [L1] `.github/workflows/ci.yml` declares a job `example_playwright_design` whose `name:` line is exactly `    name: Example Playwright design gallery` with no `${{` on that line.
    - [L1] The steps `id: design_gallery` and `id: design_gallery_snapshots` appear exactly once each in ci.yml, both inside `example_playwright_design`, and neither appears inside `example_playwright_smoke`.
    - [L1] `example_playwright_smoke`'s aggregator loop references exactly four `steps.<id>.outcome` entries; `example_playwright_design`'s references exactly two; the union is the original six.
    - [L1] `node --test scripts/ci/prohibitions/p02-axe-signal-not-reduced.test.mjs` passes — the moved `design_gallery` step still carries all three `--project` flags and `--grep-invert`.
    - [L1] `node --test scripts/ci/prohibitions/p10-no-undocumented-demotion.test.mjs` passes — the tier-B row's new parent resolves, the new tier-C job row exists, and every `display_name` matches a literal ci.yml `name:`.
    - [L1] `node --test scripts/ci/prohibitions/p09-timeouts-not-truncating.test.mjs` passes — the new job declares exactly one `timeout-minutes` inside [5, 60], and `example_playwright_smoke` still declares exactly 45.
    - [L2] `bash scripts/ci/honest-skip-verdict.test.sh` passes — `ci-gate.needs` still declares exactly the nine lanes and the manifest still intersects at least five of them.
    - [L2] `bash scripts/ci/ci-demotion-observer.test.sh` passes, and a hand-built `--from-json` payload in which a job named `Example Playwright design gallery` contains a step named `Run design gallery board snapshots (non-PR)` yields a PASS verdict for that assert row, while the same payload with the old parent name yields a FAIL — proving the lookup follows the move rather than silently missing.
    - [L1] `bash scripts/ci/playwright-cache-key-guard.sh` passes, and over non-comment lines only (`grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'playwright-chromium-webkit-1.59.1-v3'`) the count equals the number of jobs declaring the browser cache, while the same comment-filtered pipeline finds zero `-v2` keys. The comment-filtering is required, not cosmetic: the rewritten rationale comment names the old token while explaining the change, and an unfiltered count would red on its own explanation.
    - [L1] `mix test test/sigra/planning/` passes in full, including all nine tests in `phase_230_design_gallery_split_test.exs`.
    - [L1] `git show --stat HEAD` for this task lists `.github/workflows/ci.yml`, `.github/ci-skip-manifest.tsv`, `MAINTAINING.md` and `test/sigra/planning/phase_230_design_gallery_split_test.exs` in one commit.
    - [L1] `git grep -c 'Example Playwright smoke (full lifecycle)' .github/workflows/ci.yml scripts/ci/docs-only-receipt.sh scripts/ci/prohibitions/_lib.mjs` is unchanged from before this task — the required-context string was not touched.
  </acceptance_criteria>
  <done>The demoted snapshot step lives on a job whose name both the manifest guard and the post-merge observer resolve identically, the required context's job lost two seams and kept its name, and every guard, contract and document that names those constructs was updated in the same commit.</done>
</task>

</tasks>

## Edge-probe coverage carried by this plan

The spec-less fallback probe's `PW-02 / unclassified` row was surfaced by plan 232-02 as a flagged
assumption with the proposed classification *the aggregator-reports-success-when-skipped edge* (D-22).
This plan does not close it — it removes one obstacle to closing it, by giving the design seams a job
whose result can be a `needs` input to the aggregator plan 232-11 builds. The assumption stays flagged
until plan 232-12's red probe and the `AFTER-PW02-REDPROBE` slot exist. No probe row is dropped.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ci.yml job layout -> `.github/ci-skip-manifest.tsv` | A step's owning job is data in one file and structure in another; a disagreement makes the post-merge receipt blind |
| manifest cell -> Actions API job name | The observer resolves by display name; an interpolated name resolves to something the manifest cannot literally hold |
| `example_playwright_smoke` job name -> ruleset 14941512 | The required status context is collected from the job `name:` string |
| shared browser-cache prefix -> N consumer jobs | A restore-keys prefix consumed by more jobs than its safety argument assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-232-44 | Spoofing | the demoted step's parent job name | high | mitigate | The new job's `name:` is a static literal with no `${{ }}`, so the manifest cell that `p10` matches literally is the same string the Actions API returns; acceptance criteria assert both directions, including a synthesized observer payload. |
| T-232-45 | Repudiation | a seam failure no aggregator observes | critical | mitigate | Both aggregator loops are asserted by `phase_230_design_gallery_split_test.exs`, re-anchored in the same commit, with an explicit union assertion so a seam id cannot fall out of both loops. |
| T-232-46 | Tampering | weakening `p10` or `ci-demotion-observer.sh` to fit a matrix name | high | mitigate | Prohibited outright; the non-matrix layout is the lossless resolution and is what this plan implements. |
| T-232-47 | Elevation | required context orphaned by a layout change | critical | mitigate | `example_playwright_smoke` keeps its id and its byte-identical `name:` in this plan; the acceptance criteria grep the five-context strings for an unchanged count. |
| T-232-48 | Tampering | cache poisoning across jobs via a bare restore-keys prefix | medium | mitigate | Key re-tokened `-v2` -> `-v3` and the comment rewritten to state the real consumer set and why every consumer installs the same browser pair; carried as a backstop truth because the property is argued structurally, not asserted directly. |
| T-232-SC | Tampering | npm/pip/cargo installs | low | accept | Vacuous: no dependency manifest is touched by this plan. |
</threat_model>

## Artifacts this phase produces

Created **by this plan** is marked ✱.

- **Scripts:** `scripts/ci/ci-step-metrics.sh`, `scripts/ci/ci-step-metrics.test.sh`,
  `scripts/ci/playwright-shard-nonempty.sh`, `scripts/ci/playwright-shard-nonempty.test.sh`
- **CI step names (new):** `CI step metrics self-test`, `Playwright shard non-emptiness self-test`
- **CI step id (new):** `boot_example_app` — the composite call step id, once per converted job
- **CI job ids (new):** ✱`example_playwright_design`, `example_playwright_shard`
- **CI job `name:` strings (new):** ✱`Example Playwright design gallery`,
  `Example Playwright smoke shard ${{ matrix.seam }}`
- **Workflow inputs (new):** `force_shard_fail_probe` (`workflow_dispatch`)
- **Cache key token (changed):** ✱`playwright-chromium-webkit-1.59.1-v3` (was `-v2`)
- **Composite action:** `.github/actions/boot-example-app/action.yml` — inputs `mix-env`, `seeds`,
  `node`, `browsers`, `browser-cache-hit`, `boot`, `port`, `pg-database`, `server-log`, `warm-paths`;
  outputs `deps-cache-hit`, `otp-version`, `elixir-version`
- **Playwright:** `test/example/priv/playwright/tests/auth.setup.ts`,
  `test/example/priv/playwright/helpers/storageState.ts`; project name `design-auth-setup`; config
  consts `AUTH_SETUP_FILE`, `DESIGN_STORAGE_STATE`; storageState path
  `test/example/priv/playwright/.playwright/design-admin.json`; `json` reporter outputFile
  `.playwright/last-run-report.json`
- **Prohibition guards (new):** `p17-pw-evidence-run-id-provenance.test.mjs`,
  `p18-pw-no-green-on-empty-count.test.mjs`, `p19-pw-sc-restatement-recorded.test.mjs`,
  `p20-pw-no-lane-green-because-skipped.test.mjs`, `p21-pw-committed-method-provenance.test.mjs`
- **ExUnit contracts (new):** `test/sigra/planning/phase_232_playwright_auth_setup_test.exs`,
  `test/sigra/planning/phase_232_boot_prelude_single_definition_test.exs`,
  `test/sigra/planning/phase_232_playwright_shard_topology_test.exs`
- **Manifest rows (new):** ✱tier-C `example_playwright_design`, tier-C `example_playwright_shard`
- **Planning artifacts:** `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-EVIDENCE.md`,
  `.planning/phases/232-playwright-economics-authenticate-once-then-shard/COVERAGE.md`

<verification>
- `node --test --test-reporter=tap scripts/ci/prohibitions/*.test.mjs` — green.
- `mix test test/sigra/planning/` — green, all nine `phase_230_design_gallery_split_test.exs` tests.
- `bash scripts/ci/honest-skip-verdict.test.sh` — green.
- `bash scripts/ci/ci-demotion-observer.test.sh` — green, plus the hand-built payload probe in the
  acceptance criteria.
- `bash scripts/ci/playwright-cache-key-guard.sh` and its self-test — green.
- The shard-axis decision recorded in `232-EVIDENCE.md` with its arithmetic and its run ids.
</verification>

<success_criteria>
The design seams run on a job with a static, doubly-resolvable name; the required context's job kept
its name and lost only what moved; every consumer of the old layout was corrected in the same commit;
and the shard axis plan 232-11 will implement is a recorded decision backed by measured seconds.
</success_criteria>

<output>
Create `.planning/phases/232-playwright-economics-authenticate-once-then-shard/232-10-SUMMARY.md` when done.
</output>
