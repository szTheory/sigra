---
phase: 218-elevation-wave-nit-cleanup
plan: 06
type: execute
wave: 4
depends_on: [218-01, 218-02, 218-03, 218-04, 218-05]
files_modified:
  - guides/reference/admin-award-ledger.json
  - guides/reference/admin-panel-verdicts.json
autonomous: false
requirements: [ELEVATE-01, ELEVATE-02, ELEVATE-03]
user_setup:
  - service: anthropic
    why: "The operator-driven LLM panel (admin-panel.sh --all --changed-only) needs a real ANTHROPIC_API_KEY; a missing key hard-degrades to exit 0 (JUDGE-CI-01) and the climb-decision step cannot run"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (operator-supplied, local only)"
must_haves:
  truths:
    - "The LLM panel has been run locally, operator-driven, via admin-panel.sh --all --changed-only over the full expanded matrix; verdicts are cached in admin-panel-verdicts.json keyed on render_sha256 so re-runs are ~free"
    - "Panel output lands in the parallel per-cell panel-findings.json only (gitignored, written under the render bundle dir by admin-panel.sh — NOT a committed guides/reference/ path) — never merged into findings.json, never entering the deterministic open_findings count, never gating"
    - "The operator has reviewed the deferred award-raise proposals from Plans 02/03 + the panel findings, and applied the earned gradient raises they approve (each protected by the monotonic guard)"
    - "The elevation result is assembled as ONE reviewable PR (via gsd-pr-branch, filtering .planning/ commits) with a before/after render strip and narrowed options per residual judgment call"
    - "UI-01 and UI-02 are confirmed resolved (both todos in resolved/) and ride the same PR"
  artifacts:
    - guides/reference/admin-award-ledger.json
    - guides/reference/admin-panel-verdicts.json
  key_links:
    - "admin-panel.sh degrades to exit 0 on missing ANTHROPIC_API_KEY (JUDGE-CI-01 structural guarantee)"
    - "the content-hash skip cache (admin-panel-verdicts.json) is keyed on render_sha256 — re-runs skip unchanged cells"
    - "gsd-pr-branch filters .planning/ commits out of the review branch"
    - "every operator-approved raise passes award-guard.mjs (band=min(axes), rendered:true, resolvable evidence) + quality-findings-monotonic.sh"
  prohibitions:
    - "MUST NOT wire the LLM panel into any CI gate or fast_checks — advisory/off-CI only (JUDGE-CI-01)"
    - "MUST NOT merge panel-findings.json into findings.json or the deterministic open_findings count"
    - "MUST NOT auto-apply CSS fixes (217 D-13 locked); the 13 CSS-anchored token findings are eligible for MANUAL operator fix in the PR only (a direct sigra_admin.css token correction is allowed; it just cannot ride the auto-apply rail)"
    - "MUST NOT recapture any baseline PNG in this PR (D-07) — the before/after strip READS existing screenshot.png only; new-board baselines coordinate with Phase 219"
    - "MUST NOT climb any cell the operator has not explicitly approved (honesty-first D-05); over-claiming weakens the operator-truth guarantee"
    - "MUST NOT include .planning/ commits in the reviewable PR branch"
---

<objective>
Close the wave: run the operator-driven LLM panel over the full expanded matrix (D-04), let the operator apply the earned gradient raises they approve, and assemble the whole elevation result + the two nit fixes into a single reviewable PR (D-12). Auto-fix applies ≈0 fixes this wave (D-06) — the yield is deterministic-probe + operator-panel driven, and the 13 CSS-anchored token findings are manual-operator-fix-only.

This plan is autonomous: false — it requires the operator's ANTHROPIC_API_KEY and human judgment. The executor sets up and verifies the harness state and prepares the PR mechanics; the operator runs the keyed panel and makes the climb/sign-off decisions.

Purpose: SC-1 (loop run + verify-then-climb with monotonic-guarded raises), SC-2 (UI-01 + UI-02 confirmed resolved on the same PR), SC-3 (reviewable PR with before/after strip + narrowed options — operator review bounded to approval decisions, not a fresh issue hunt).
Output: panel verdicts cached, operator-approved raises applied, one reviewable PR.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-CONTEXT.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@scripts/ci/admin-panel.sh
@scripts/ci/award-guard.mjs
@scripts/ci/quality-findings-monotonic.sh
@guides/reference/admin-award-ledger.json
@guides/reference/admin-render-sha.json
@guides/reference/admin-panel-verdicts.json
@guides/reference/fix-queue.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (OPERATOR): Run the LLM panel over the full matrix + make climb decisions</name>
  <files>guides/reference/admin-award-ledger.json, guides/reference/admin-panel-verdicts.json (+ gitignored panel-findings.json under the render bundle dir — NOT a committed guides/reference/ path)</files>
  <what-built>
    Plans 01-05 landed: the full L1/L2/L3 matrix is in the committed ledgers, every deterministic-probe cell is verify-held, Plans 02/03 recorded a set of proposed-but-deferred award raises, and UI-01/UI-02 are resolved. The panel is ready to run.
  </what-built>
  <action>
    OPERATOR-DRIVEN (autonomous:false — needs the operator's ANTHROPIC_API_KEY + judgment; the executor pauses here). The operator performs:
    1. Export ANTHROPIC_API_KEY locally (never commit it). A missing key hard-degrades admin-panel.sh to exit 0 (JUDGE-CI-01) — climb decisions cannot be made without it.
    2. Run scripts/ci/admin-panel.sh --all --changed-only. The content-hash skip cache (admin-panel-verdicts.json, keyed on render_sha256) makes re-runs ~free; first-run token spend is bounded to changed cells. Then confirm the panel ACTUALLY EVALUATED cells: admin-panel-verdicts.json must show new/updated verdict entries for the expanded matrix (a missing key would have hard-degraded to exit 0 with zero evaluation — verify that did NOT happen).
    3. Confirm panel output landed in the gitignored PARALLEL per-cell panel-findings.json under the render bundle dir (NOT a committed guides/reference/ path) — NOT merged into findings.json and did NOT change the deterministic open_findings count in admin-render-sha.json.
    4. Review panel findings alongside the deferred award-raise proposals recorded in the 218-02 and 218-03 SUMMARYs.
    5. For each raise approved as earned: apply it to admin-award-ledger.json (raise only the specific axis, set rendered:true, verified_at_sha = clean-tree HEAD, add a resolvable evidence_ref). Leave un-approved raises un-applied (honesty-first D-05).
    6. Optionally hand-fix the 13 CSS-anchored off-scale token findings directly in sigra_admin.css (allowed manual operator fix; must NOT ride the auto-apply rail — 217 D-13 stays locked).
    7. Run node scripts/ci/award-guard.mjs and bash scripts/ci/quality-findings-monotonic.sh — both must exit 0.
  </action>
  <how-to-verify>
    The panel must have ACTUALLY EVALUATED cells — not silently hard-degraded to exit 0 on a missing key (JUDGE-CI-01). Green guards alone do NOT prove the panel ran. Confirm ALL of:
    1. ANTHROPIC_API_KEY was exported before the run (a missing key makes admin-panel.sh exit 0 with zero evaluation — this is the trap this checkpoint exists to catch).
    2. admin-panel-verdicts.json shows verdicts newly written or updated for the expanded matrix cells this run (new render_sha256 keys present, and/or updated verdict timestamps/entries vs the pre-run state) — this is the positive evidence the panel evaluated cells, not just that guards are green. Diff admin-panel-verdicts.json against its pre-run state and confirm non-empty, matrix-covering verdict changes.
    3. Panel output stayed in the gitignored per-cell panel-findings.json under the bundle dir — findings.json and the deterministic open_findings in admin-render-sha.json are unchanged.
    4. Approved raises applied; both guards (award-guard.mjs, quality-findings-monotonic.sh) exit 0.
  </how-to-verify>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node scripts/ci/award-guard.mjs && bash scripts/ci/quality-findings-monotonic.sh && echo PASS</automated>
  </verify>
  <done>The panel has run AND demonstrably evaluated cells (admin-panel-verdicts.json shows new/updated verdicts for the expanded matrix, not a no-op exit-0 degrade), verdicts are cached, operator-approved raises are applied under the monotonic guard, panel output stayed in the gitignored per-cell panel-findings.json (findings.json / open_findings unchanged), and both guards exit 0.</done>
  <resume-signal>Type "approved" once you have confirmed the panel actually evaluated cells — i.e. admin-panel-verdicts.json shows new/updated verdicts for the expanded matrix (not a zero-eval exit-0 degrade from a missing key) — verdicts are cached, your approved raises are applied, and both guards exit 0. Otherwise describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Assemble the single reviewable ELEVATE-03 PR with before/after strip + narrowed options</name>
  <read_first>
    - 218-CONTEXT.md D-12 (single reviewable PR; before/after render strip; narrowed options per judgment call; gsd-pr-branch filters .planning/)
    - the 218-02 / 218-03 SUMMARYs (applied vs deferred raises — the deferred set becomes the narrowed options)
    - the per-cell panel-findings.json under the render bundle dir (gitignored, written by admin-panel.sh — advisory panel findings feeding the judgment-call options; NOT a committed guides/reference/ path)
    - each cell's screenshot.png under the render bundle dir (READ-only, for the before/after strip)
  </read_first>
  <action>
    Assemble the whole wave into ONE reviewable PR. Use gsd-pr-branch to produce a review branch with .planning/ commits filtered out. Build a before/after render strip that READS each cell's existing screenshot.png (do NOT recapture — Phase 219 owns baselines; new-board baselines that don't yet exist are flagged for Phase 219 coordination). For each residual judgment call (deferred raises + operator-relevant panel findings + the 13 CSS-token findings), present a narrowed set of options so the operator's PR review is bounded to approval decisions, not a fresh issue hunt. Confirm the two nit todos (UI-01, UI-02) are in .planning/todos/resolved/ and their fixes ride this PR. Write the PR body summarizing: surfaces verify-held, raises applied (with evidence), residual judgment calls (as narrowed options), and the UI-01/UI-02 resolutions.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node scripts/ci/award-guard.mjs && bash scripts/ci/quality-findings-monotonic.sh && test -f .planning/todos/resolved/2026-06-19-uat-demo-dx-polish-nits.md && test -f .planning/todos/resolved/2026-06-22-vaultr-authed-rebrand-residuals.md && echo PASS</automated>
  </verify>
  <done>A single reviewable PR branch exists (via gsd-pr-branch, .planning/ filtered) with a before/after render strip (reading existing PNGs, no recapture) and narrowed options per residual judgment call; UI-01 + UI-02 todos are in resolved/ and their fixes ride the PR; award-guard.mjs + quality-findings-monotonic.sh exit 0. Any new-board baseline gap is flagged for Phase 219.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Anthropic API | The operator supplies ANTHROPIC_API_KEY locally to run the advisory panel. The key is never committed; a missing key hard-degrades to exit 0 (JUDGE-CI-01). Render bundles (DOM + facts) are sent to the panel — no secrets are in the static-gallery render bundles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-06-01 | Information Disclosure | ANTHROPIC_API_KEY leakage | medium | mitigate | key is operator-supplied env var, local only, never committed; admin-panel-verdicts.json + panel-findings.json contain verdicts/findings, not the key |
| T-218-06-02 | Tampering | panel output contaminating the deterministic gate | high | mitigate | JUDGE-CI-01 structural split: panel writes only panel-findings.json (parallel), never findings.json / open_findings; panel never wired into CI or fast_checks; verify step confirms open_findings unchanged |
| T-218-06-03 | Repudiation | operator over-claims an unearned raise | low | mitigate | award-guard.mjs + quality-findings-monotonic.sh gate every raise (band=min(axes), rendered:true, resolvable evidence); honesty-first D-05 — un-approved raises stay un-applied |
| T-218-06-SC | Tampering | supply-chain via npm/pip/cargo installs | n/a | accept | No package installs in this plan; the panel + guards are existing committed scripts. Package Legitimacy Gate does not apply. |

Security surface for this plan: the panel is advisory/off-CI (JUDGE-CI-01), the one place it touches trust is the operator's own API key (local, never committed). The deterministic merge gate is structurally isolated from panel output.
</threat_model>

<verification>
- Panel run via admin-panel.sh --all --changed-only AND demonstrably evaluated cells (admin-panel-verdicts.json shows new/updated verdicts for the expanded matrix — not a missing-key exit-0 no-op); verdicts cached (keyed on render_sha256).
- Panel output in the gitignored per-cell panel-findings.json (bundle dir) only; findings.json / open_findings unchanged (JUDGE-CI-01).
- Operator-approved raises applied under award-guard.mjs; un-approved raises un-applied.
- Optional manual CSS-token fixes did NOT ride the auto-apply rail (217 D-13 intact).
- Single reviewable PR via gsd-pr-branch (.planning/ filtered) with before/after strip (no recapture) + narrowed options.
- UI-01 + UI-02 todos in resolved/; fixes ride the PR.
- award-guard.mjs + quality-findings-monotonic.sh exit 0.
</verification>

<success_criteria>
The wave is closed: the operator-driven panel has run over the full matrix, earned raises are applied under the monotonic guard, and the whole elevation + UI-01/UI-02 nits land as one reviewable PR bounded to operator approval decisions (SC-1, SC-2, SC-3).
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-06-SUMMARY.md` when done. Record: the panel run result (cells evaluated / cached), the raises the operator approved vs declined, whether the 13 CSS-token findings were hand-fixed or left as PR options, the PR branch name/URL, and any new-board baseline gap flagged for Phase 219.
</output>
