---
phase: 218-elevation-wave-nit-cleanup
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/uat/up.sh
autonomous: true
gap_closure: true
requirements: [ELEVATE-02]
must_haves:
  truths:
    - "The up.sh stale-stack reaper covers BOTH proxy-host labels its comment claims (vendor-neutral dev.local.proxy-host AND legacy dev.sigra.proxy-host), so a stack labeled only with the vendor-neutral label is no longer leaked"
  artifacts:
    - "scripts/uat/up.sh reap_stale_uat_stacks querying both labels with sort -u"
  key_links:
    - "reap_stale_uat_stacks docker ps -a filter <-> its own comment: must match the proxy_host_claimants dual-label etiquette"
---

<objective>
Close WR-05: the `scripts/uat/up.sh` stale-stack reaper (`reap_stale_uat_stacks`, around :631-643) filters
`docker ps -a` on only `label=dev.sigra.proxy-host`, though its comment claims it also covers the
vendor-neutral `dev.local.proxy-host`. A leaked stack labeled ONLY with the vendor-neutral label is never
reaped and leaks until a manual `down.sh`. This is example/demo DX only — no installer template mirror.

Purpose: Make the reaper actually cover both labels, honoring the etiquette the rest of the script migrates
toward (see the existing `proxy_host_claimants` dual-label union).
Output: A reaper that unions both labels and dedupes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-REVIEW.md
@scripts/uat/up.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Union both proxy-host labels in reap_stale_uat_stacks (WR-05)</name>
  <read_first>
    - scripts/uat/up.sh:631-660 (reap_stale_uat_stacks — the single-label `docker ps -a` filter that contradicts its comment)
    - scripts/uat/up.sh:251-266 (proxy_host_claimants — the dual-label union precedent: filters both dev.local.proxy-host and dev.sigra.proxy-host)
  </read_first>
  <files>scripts/uat/up.sh</files>
  <action>
    Per 218-REVIEW.md WR-05: change the `stale_projects` assignment in `reap_stale_uat_stacks` so it queries
    BOTH labels and dedupes, mirroring `proxy_host_claimants`. Run two `docker ps -a` invocations — one
    filtered on `label=dev.sigra.proxy-host` and one on `label=dev.local.proxy-host`, each also filtered on
    `label=com.docker.compose.project` and emitting `--format '{{.Label "com.docker.compose.project"}}'` —
    and pipe the combined output through `sort -u`, wrapped so an error in either leg does not abort the
    function (keep the existing `|| true` tolerance). Preserve every existing safety invariant: the
    `SIGRA_UAT_REAP` opt-out, the `command -v docker` / `docker ps` availability guards, the
    "never reap SIGRA_UAT_PROJECT" check, and the "only reap projects with no running containers" gate. The
    comment already describes both labels — it becomes accurate; adjust only if wording needs it.
  </action>
  <verify>
    <automated>bash -n scripts/uat/up.sh && awk '/reap_stale_uat_stacks\(\)/{f=1} f&&/^}/{print; exit} f' scripts/uat/up.sh | grep -c 'dev.local.proxy-host\|dev.sigra.proxy-host'</automated>
  </verify>
  <done>reap_stale_uat_stacks filters on both dev.sigra.proxy-host and dev.local.proxy-host with sort -u; `bash -n scripts/uat/up.sh` passes; the reaper block references both labels (grep count >= 2); all existing safety guards preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local docker daemon -> reaper | Leaked demo stacks labeled only with the vendor-neutral label evade cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-10-01 | Denial (resource leak) | reap_stale_uat_stacks label filter | low | mitigate | Union both proxy-host labels + sort -u so vendor-neutral-only stacks are reaped |
| T-218-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
- `bash -n scripts/uat/up.sh` — syntax clean.
- Reaper covers both labels: the `reap_stale_uat_stacks` body references both `dev.sigra.proxy-host` and `dev.local.proxy-host`.
- Safety invariants (SIGRA_UAT_REAP opt-out, current-project skip, running-container skip) preserved.
</verification>

<success_criteria>
The stale-stack reaper covers both proxy-host labels its comment claims, so no vendor-neutral-only stack
leaks until manual teardown.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-10-SUMMARY.md` when done
</output>
