---
phase: 218-elevation-wave-nit-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/uat/up.sh
autonomous: true
requirements: [ELEVATE-02]
must_haves:
  truths:
    - "A flag passed in a mode where it is a no-op prints an explicit 'ignored in <mode> mode' warning instead of being silently inert"
    - "--status re-probes liveness with curl -fsS --max-time 2 inside print_status, so a frozen SIGRA_UAT_READY no longer prints STARTING forever for a server that came up after the boot probe timed out"
    - "The host-run wait_for_http call allows ~120s (or detects a Compiling log line and extends) so a cold --dev boot is not falsely reported STARTING"
    - "A stale leaked UAT stack can be reaped (mirroring the existing stale-build wipe precedent)"
  artifacts:
    - scripts/uat/up.sh
  key_links:
    - "print_status re-probe reuses the exact curl -fsS --max-time 2 guard already present in wait_for_http"
    - "flag-inert warning uses the existing yellow/red helpers and mirrors the --proxy + --private-traefik mutual-exclusion precedent"
  prohibitions:
    - "MUST NOT touch any scripts/ci, scripts/panel, or admin-eval harness file — UI-01 is self-contained shell DX with zero harness overlap"
    - "MUST NOT change the default proxy boot path's already-live-verified behavior — these are additive warnings/re-probes/timeout bumps, not a rewrite"
    - "MUST NOT reintroduce the correctness bugs already fixed in PR #56 commit b93f8e2f (orphaned BEAM, leaking watch volumes, PID reuse, attach-ignored) — those are done; this is only the deferred low-severity nits"
---

<objective>
Fold the UI-01 carry-forward todo (2026-06-19-uat-demo-dx-polish-nits) per D-10 — the low-severity demo-DX polish nits deferred from the PR #56 code review. Self-contained bash in scripts/uat/up.sh; zero overlap with the admin harness loop, so this plan runs in parallel with the harness plans.

Four nits:
1. Flag-inert warnings — a one-line "ignored in <mode> mode" warning when a flag can't apply in the active mode.
2. --status re-probe — a live curl -fsS --max-time 2 inside print_status so a frozen readiness flag doesn't lie.
3. Host-run wait_for_http timeout — bump to ~120s (or detect Compiling and extend) so a cold --dev boot isn't falsely reported STARTING.
4. Stale leaked-UAT-stack reap — mirror the existing stale-build wipe precedent.

Purpose: SC-2 requires UI-01 fully resolved with no outstanding items in the carry-forward todo.
Output: a polished scripts/uat/up.sh; the todo moved to resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-CONTEXT.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@scripts/uat/up.sh
@.planning/todos/pending/2026-06-19-uat-demo-dx-polish-nits.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag-inert warnings + --status live re-probe</name>
  <read_first>
    - scripts/uat/up.sh lines 629-680 (the arg case that sets booleans with no cross-mode validity check) and line 698 (the --proxy + --private-traefik mutual-exclusion check — the precedent shape for a post-parse validity pass)
    - up.sh line 161 (print_status) and lines 169-171 (the STARTING — not yet responding branch to flip live)
    - up.sh line 541 (the curl -fsS --max-time 2 guard inside wait_for_http to copy) and line 551 (yellow helper usage)
    - .planning/todos/pending/2026-06-19-uat-demo-dx-polish-nits.md nits #5 and #7
  </read_first>
  <action>
    Add a post-parse validity pass (mirroring the existing --proxy/--private-traefik mutual-exclusion check at up.sh:698) that emits a one-line yellow "ignored in <mode> mode" warning for any flag that is a no-op in the resolved mode (e.g. --no-watch passed in --dev mode). Use the existing yellow/red helpers. Separately, inside print_status when invoked via --status, add a live re-probe using the same curl -fsS --max-time 2 "${url}" guard already present in wait_for_http (up.sh:541) so the STARTING/live branch (up.sh:169-171) reflects the server's actual current liveness instead of the frozen SIGRA_UAT_READY flag written at boot. Do not change the default proxy boot path's verified behavior.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash -n scripts/uat/up.sh && grep -q 'curl -fsS --max-time 2' scripts/uat/up.sh && grep -qi 'ignored in' scripts/uat/up.sh && echo PASS</automated>
  </verify>
  <done>up.sh passes bash -n; a no-op flag in the wrong mode prints an "ignored in <mode> mode" warning; print_status re-probes liveness with curl -fsS --max-time 2 when invoked via --status.</done>
</task>

<task type="auto">
  <name>Task 2: Host-run wait_for_http timeout bump + stale leaked-stack reap</name>
  <read_first>
    - scripts/uat/up.sh lines 534-553 (wait_for_http, default timeout="${2:-60}") and the host-run call site(s) around up.sh:526 / up.sh:865 (wait_for_http "${SIGRA_UAT_RAW_URL}")
    - up.sh line 450 (the "Wipe a stale example build whose frozen endpoint port differs" precedent to mirror for a leaked-stack reap)
    - .planning/todos/pending/2026-06-19-uat-demo-dx-polish-nits.md nits #8 + the "reap stale leaked UAT stacks" item
  </read_first>
  <action>
    Bump the host-run wait_for_http call to ~120s (pass an explicit second arg at the host-run call site so the default 60s stays for other callers), or detect a "Compiling" line in the boot log and extend the wait — a first-run mix phx.server compiles example + sigra (~70-90s) and would otherwise falsely print STARTING and skip the browser open. Add a stale leaked-UAT-stack reap mirroring the existing stale-build wipe precedent (up.sh:450): detect an orphaned prior UAT compose project/volumes and reap it (docker compose -p <project> --profile '*' down -v --remove-orphans, or targeted docker rm -f / volume rm). Keep it opt-in-safe — do not reap the stack the current invocation is about to use.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash -n scripts/uat/up.sh && echo PASS</automated>
  </verify>
  <done>The host-run wait_for_http call allows ~120s (or Compiling-detect extend); a stale leaked UAT stack can be reaped without touching the current stack; up.sh passes bash -n. Where docker + a Postgres are available, the executor SHOULD live-exercise the --dev host-run boot end-to-end and record the result in the SUMMARY; otherwise document that live exercise is deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan edits a local demo/UAT shell orchestrator (scripts/uat/up.sh). It runs on the operator's machine against the demo stack; it is not part of the shipped library, CI gates, or any runtime auth path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-04-01 | Denial of Service | stale-stack reap could tear down an in-use stack | low | mitigate | reap logic explicitly excludes the compose project the current invocation is about to use; mirrors the guarded stale-build wipe precedent |

No packages installed; Package Legitimacy Gate N/A. Security surface LOW (local demo shell DX; not shipped, not a CI gate).
</threat_model>

<verification>
- up.sh passes bash -n.
- No-op flag in wrong mode prints "ignored in <mode> mode".
- --status re-probes with curl -fsS --max-time 2.
- Host-run wait_for_http allows ~120s (or Compiling-detect extend).
- Stale leaked-stack reap present and excludes the current stack.
- No scripts/ci, scripts/panel, or admin-eval file touched.
</verification>

<success_criteria>
UI-01 fully resolved: all four nits landed in scripts/uat/up.sh, no outstanding items in the carry-forward todo, and the todo moved to .planning/todos/resolved/.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-04-SUMMARY.md` when done. Move `.planning/todos/pending/2026-06-19-uat-demo-dx-polish-nits.md` to `.planning/todos/resolved/`. Record whether the --dev host-run path was live-exercised or deferred.
</output>
