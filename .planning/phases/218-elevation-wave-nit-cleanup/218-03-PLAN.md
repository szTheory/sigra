---
phase: 218-elevation-wave-nit-cleanup
plan: 03
type: execute
wave: 2
depends_on: [218-01]
files_modified:
  - guides/reference/admin-award-ledger.json
autonomous: true
requirements: [ELEVATE-01]
must_haves:
  truths:
    - "All 8 L3 admin/operator surfaces (index-live, organization-live, users-index-live, user-show-live, user-sessions, audit-index-live, audit-user-live, branding-live) are re-verified to HOLD their claimed Tier-2 against the rendered output of their representative gallery board (via the proxy sha)"
    - "The 6 net-new L3 award cells (index, organization, user-sessions, audit-index, audit-user, branding) added by 218-01 are verified at an honest band that reflects their proxy board's rendered evidence — not an aspirational band"
    - "Any L3 cell that fails to hold Tier-2 against its proxy's rendered output is corrected DOWN to its honest band and documented"
    - "award sub-scores are raised ONLY where the proxy board's fresh rendered evidence clearly earns it; verified_at_sha = clean-tree HEAD"
  artifacts:
    - guides/reference/admin-award-ledger.json
  key_links:
    - "L3 proxy render_sha256 is byte-identical to its representative board — the panel/probe evidence for the L3 cell IS that board's evidence"
    - "award-guard.mjs recomputes band=min(axes) and FAILs on climb-without-render / band!=min / unresolved-evidence / axis-decrease"
    - "evidence_ref entries resolve via resolveEvidenceRef (probe:<canonical-id> / test: / conformance: only)"
  prohibitions:
    - "MUST NOT boot live admin routes to capture L3 surfaces — L3 is re-verified through representative gallery boards only (216 D-02 static-gallery invariant); no per-test DB seeding"
    - "MUST NOT recapture any baseline PNG (Phase 219/RECAP-01; darwin-local prohibited)"
    - "MUST NOT author any NEW admin surface — elevation/verification of existing cells only (D-01)"
    - "MUST NOT climb an L3 cell above what its proxy board's rendered evidence earns (honesty-first D-05); ambiguous raises defer to operator PR sign-off (Plan 06)"
    - "MUST NOT lower an L3 sub-score EXCEPT where the cell genuinely fails to hold Tier-2 against its proxy's rendered output"
---

<objective>
Verify-then-climb the 8 L3 admin/operator quality-ledger surfaces through their representative gallery-board proxies (the mg-2/5=users-index and mg-9/10/11=user-show pilots proved the mechanism; 218-01 added the 6 net-new proxy mappings). Because each L3 proxy's render_sha256 is byte-identical to its representative board, the L3 cell's rendered evidence IS that board's evidence. The primary deliverable is verify-hold: confirm each of the 8 L3 surfaces still HOLDS its claimed Tier-2 against the proxy board's rendered output, catching any optimistically cite-and-flipped surface. Earned raises are the exception, deferred mostly to the operator PR sign-off.

Purpose: SC-1 requires all 8 admin surfaces to be run through the loop and verify-then-climbed. This plan covers the L3 surface layer.
Output: an award ledger where all 8 L3 surfaces are re-verified against clean-tree HEAD through their board proxies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-CONTEXT.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@scripts/ci/award-guard.mjs
@scripts/ci/quality-findings-monotonic.sh
@scripts/ci/lib/eval-probe-ids.mjs
@guides/reference/admin-award-ledger.json
@guides/reference/admin-render-sha.json
@guides/reference/admin-quality-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify-hold all 8 L3 surfaces through their board proxies</name>
  <read_first>
    - guides/reference/admin-quality-ledger.md L85-92 (the 8 L3 surfaces + their Tier claims to re-verify)
    - guides/reference/admin-render-sha.json (the 8 L3 proxy entries added by 218-01; each proxy sha is byte-identical to its representative board)
    - guides/reference/admin-award-ledger.json (users-index-live / user-show-live pilots + the 6 new L3 cells from 218-01)
    - 218-PATTERNS.md section 3 (proxy-sha mechanism; verify-then-climb rule)
    - 218-CONTEXT.md D-01 (8 L3 surfaces re-verified through representative gallery boards, NOT live-route captures)
  </read_first>
  <action>
    For each of the 8 L3 surfaces, re-verify it HOLDS its claimed Tier-2 against the rendered output of its representative gallery board (the proxy sha in admin-render-sha.json). Because the proxy sha is byte-identical to the board's sha, use the board's probe findings / rendered bundle as the L3 cell's evidence — do NOT boot live admin routes and do NOT seed per-test DB fixtures (216 D-02 static-gallery invariant). For the 2 pilots (users-index-live, user-show-live) this is a re-confirmation; for the 6 new cells (index, organization, user-sessions, audit-index, audit-user, branding) confirm the honest band 218-01 assigned actually matches the proxy board's rendered evidence, correcting DOWN any cell that fails to hold. Update verified_at_sha for every L3 cell to the clean-tree HEAD at re-verification. Do NOT climb in this task — verification only.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node -e "const a=require('./guides/reference/admin-award-ledger.json').cells; const l3=['index','organization','users-index-live','user-show-live','user-sessions','audit-index','audit-user','branding']; const missing=l3.filter(s=>!a[s]); if(missing.length){console.error('MISSING L3 award cells:',missing);process.exit(1)} console.log('PASS: 8 L3 award cells present')" && node scripts/ci/award-guard.mjs && bash scripts/ci/quality-findings-monotonic.sh</automated>
  </verify>
  <done>All 8 L3 award cells present and re-verified against their proxy board's rendered output with verified_at_sha = clean-tree HEAD; any cite-and-flipped L3 cell corrected to its honest band and documented; award-guard.mjs and quality-findings-monotonic.sh exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Apply earned L3 raises (selective) and record deferred raises for operator sign-off</name>
  <read_first>
    - guides/reference/admin-award-ledger.json (post-Task-1 verified L3 cells)
    - scripts/ci/award-guard.mjs (each raise needs rendered:true + updated verified_at_sha + resolvable evidence_ref; band=min(axes))
    - 218-CONTEXT.md D-05 (honesty-first; most raises land as operator PR sign-off decisions, not automated bumps)
  </read_first>
  <action>
    For each L3 surface where the proxy board's fresh rendered evidence CLEARLY earns a higher award sub-score on a specific axis, raise ONLY that axis, set rendered:true, update verified_at_sha to clean-tree HEAD, and add a resolvable evidence_ref. Apply only unambiguous, probe-evidenced raises. Every ambiguous or taste-dependent raise is DEFERRED to the operator PR sign-off in Plan 06 — do not bump it here (D-05 honesty-first). Record in the SUMMARY both the raises applied and the raises proposed-but-deferred (these become the narrowed options for the ELEVATE-03 PR).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node scripts/ci/award-guard.mjs && bash scripts/ci/quality-findings-monotonic.sh</automated>
  </verify>
  <done>All applied L3 raises pass award-guard.mjs; ambiguous raises are recorded as deferred proposals for Plan 06; no sub-score lowered except honest cite-and-flip corrections; monotonic gates exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan re-verifies L3 award cells against already-rendered board proxies and edits one committed JSON ledger. No runtime auth/session/authz code is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-03-01 | Tampering | L3 award-cell integrity | low | mitigate | award-guard.mjs recomputes band=min(axes), rejects climb-without-render / unresolved-evidence; proxy sha must equal the representative board sha (218-01 invariant) |
| T-218-03-02 | Repudiation | over-claimed L3 band | low | mitigate | verified_at_sha pinned to clean-tree HEAD; ambiguous raises deferred to operator PR sign-off (D-05) |

No packages installed; Package Legitimacy Gate N/A. Security surface LOW (JSON ledger re-verification via static board proxies).
</threat_model>

<verification>
- 8 L3 award cells present and re-verified through their board proxies; verified_at_sha = clean-tree HEAD.
- Any cite-and-flipped L3 cell corrected DOWN to honest band and documented.
- Applied raises pass award-guard.mjs; ambiguous raises recorded as deferred proposals for Plan 06.
- No live-route capture; no baseline recapture; no new surface authored.
- award-guard.mjs + quality-findings-monotonic.sh exit 0.
</verification>

<success_criteria>
All 8 L3 admin surfaces verify-then-climbed through their static-gallery board proxies: each holds its Tier-2 against rendered evidence (or is corrected), earned raises applied under the monotonic guard, ambiguous raises staged for operator sign-off.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-03-SUMMARY.md` when done. Record: each of the 8 L3 surfaces verified, its representative board proxy, every cite-and-flip correction, every applied raise (axis + evidence_ref), and the deferred raise proposals for the ELEVATE-03 PR.
</output>
