---
phase: 218-elevation-wave-nit-cleanup
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/admin-eval.spec.ts
  - test/example/priv/playwright/lib/eval/probes.ts
autonomous: true
gap_closure: true
requirements: [ELEVATE-01]
must_haves:
  truths:
    - "probeIdsDriftCheck() is invoked from the eval suite so drift between probes.ts PROBE_IDS and the canonical eval-probe-ids.mjs actually fails the suite (no longer orphaned dead code)"
    - "adminEvalEmail() is unique across parallel Playwright workers (worker-unique entropy prevents duplicate-registration flake)"
    - "probes.ts control-height fallback and probe-#2 docstring match the actual heuristics (no misleading dead fallback / doc drift)"
  artifacts:
    - "test/example/priv/playwright/tests/admin-eval.spec.ts (wired drift guard + worker-unique email)"
    - "test/example/priv/playwright/lib/eval/probes.ts (numeric-guarded control-height, aligned probe-#2 comment)"
  key_links:
    - "probes.ts probeIdsDriftCheck() -> admin-eval.spec.ts test.beforeAll: the guard must run when the eval suite runs"
---

<objective>
Close the harness/test-determinism gaps in the Phase 218 eval spec and probe library:

1. Gap #1 (218-01 truth #4): `probeIdsDriftCheck()` (probes.ts:69) is defined and correct but has ZERO
   callers repo-wide, so drift between the local `PROBE_IDS` and the canonical
   `scripts/ci/lib/eval-probe-ids.mjs` would NOT actually be caught. Wire it into the eval suite.
2. WR-06: `adminEvalEmail()` (admin-eval.spec.ts:171-179) builds the local-part from
   `ms + project + registrationSequence + retry`; `registrationSequence` resets per worker, so two
   parallel workers can emit the same email -> duplicate-registration assertion flake. Add worker-unique
   entropy.
3. IN-01 / IN-02 (info, cheap): numeric-guard the dead control-height fallback (probes.ts:493) and align
   the probe-#2 docstring/comment (probes.ts:196-198, :225) with the actual fractional-offset heuristic.

Purpose: Make the eval harness deterministic under parallelism and self-guarding against probe-id drift.
Output: A wired drift guard + collision-free worker emails + honest probe helper docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-REVIEW.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@test/example/priv/playwright/lib/eval/probes.ts
@test/example/priv/playwright/tests/admin-eval.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire probeIdsDriftCheck() into the eval suite</name>
  <read_first>
    - test/example/priv/playwright/lib/eval/probes.ts:69-90 (probeIdsDriftCheck — reads canonical PROBE_IDS from eval-probe-ids.mjs, throws on drift; throws, does not return a value)
    - test/example/priv/playwright/tests/admin-eval.spec.ts:31-39 (the named import block from '../lib/eval/probes.ts' — probeIdsDriftCheck is NOT currently imported)
    - test/example/priv/playwright/tests/admin-eval.spec.ts:169-180 (module-scope helpers) and the file-level test.beforeEach (~:335) which registers+navigates and needs a live server
  </read_first>
  <files>test/example/priv/playwright/tests/admin-eval.spec.ts</files>
  <action>
    Per 218-VERIFICATION.md gap #1: add `probeIdsDriftCheck` to the existing named import from
    `'../lib/eval/probes.ts'` (the import list at :31-39), then invoke it from a top-level
    `test.beforeAll(() => { probeIdsDriftCheck(); })` placed BEFORE the describe/beforeEach that registers
    a user and navigates. `probeIdsDriftCheck()` only reads files and deep-compares arrays (no `page`), so
    the beforeAll needs no server; if the two arrays drift it throws and fails the whole suite — which is
    the protection the plan promised. Do not remove or weaken the existing self-test in probes.ts; you are
    only giving it a caller.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsc --noEmit -p tsconfig.json 2>/dev/null; grep -c 'probeIdsDriftCheck()' tests/admin-eval.spec.ts</automated>
  </verify>
  <done>admin-eval.spec.ts imports probeIdsDriftCheck and calls it in a test.beforeAll that runs without a server; the grep gate finds the invocation (>=1); the playwright subproject typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Worker-unique email entropy (WR-06) + probes.ts IN-01/IN-02 nits</name>
  <read_first>
    - test/example/priv/playwright/tests/admin-eval.spec.ts:169-180 (adminEvalEmail; registrationSequence resets per worker; local-part = ms-project-sequence-retry)
    - test/example/priv/playwright/lib/eval/probes.ts:493 (IN-01: `parseFloat(cs.minHeight || cs.height)` — minHeight resolves to "0px" truthy, so the height fallback is dead)
    - test/example/priv/playwright/lib/eval/probes.ts:196-198 and :225-227 (IN-02: docstring/comment say "1-6px" but code flags any fractional offset in (0.05,0.95))
  </read_first>
  <files>test/example/priv/playwright/tests/admin-eval.spec.ts, test/example/priv/playwright/lib/eval/probes.ts</files>
  <action>
    WR-06 (admin-eval.spec.ts): make `adminEvalEmail(testInfo)` worker-unique. Add worker entropy to the
    local-part — include `testInfo.workerIndex` (Playwright's per-worker index) AND a short random suffix
    (e.g. a few base36 chars from `Math.random()`), keeping the required `platform-admin+ev-` prefix and a
    valid `@example.test` address. The goal is that two workers registering in the same millisecond at
    sequence 1 / retry 0 can no longer collide. Keep the existing prefix + project slug so
    Example.SigraAdminPolicy still grants global admin.

    IN-01 (probes.ts:493): replace `parseFloat(cs.minHeight || cs.height)` with a numeric guard — compute
    `mh = parseFloat(cs.minHeight)` and use `mh > 0 ? mh : parseFloat(cs.height)` so controls sized purely
    via `height` (no explicit min-height) are no longer silently skipped by the off-scale-control gate.

    IN-02 (probes.ts:196-198 docstring and the :225 inline comment): reword the "1-6px" phrasing to
    describe the actual heuristic — any sub-pixel fractional offset in the (0.05, 0.95) band — so the
    comment no longer overstates a bounded pixel range the code does not enforce. Do not change the
    detection logic at :222-227; only the prose.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsc --noEmit -p tsconfig.json 2>/dev/null && grep -Eq 'workerIndex' tests/admin-eval.spec.ts && echo OK</automated>
  </verify>
  <done>adminEvalEmail includes worker-unique entropy (workerIndex + random suffix) and still carries the platform-admin prefix; probes.ts:493 numeric-guards the min-height fallback; the probe-#2 docstring/comment describe the fractional-offset band; the playwright subproject typechecks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parallel test workers -> shared DB | Two workers registering the same email collide on the unique-email constraint (flake, not a security breach) |
| probes.ts PROBE_IDS vs canonical eval-probe-ids.mjs | Two hand-maintained arrays can silently drift if the guard is never invoked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-08-01 | Tampering | probe-id drift guard | low | mitigate | Wire probeIdsDriftCheck() into test.beforeAll so drift fails the suite |
| T-218-08-02 | Denial (flake) | adminEvalEmail worker collision | low | mitigate | Worker-unique entropy (workerIndex + random suffix) in the local-part |
| T-218-08-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
- Playwright subproject typechecks: `cd test/example/priv/playwright && npx tsc --noEmit -p tsconfig.json`.
- Drift guard is wired: `grep -c 'probeIdsDriftCheck()' test/example/priv/playwright/tests/admin-eval.spec.ts` >= 1.
- Email carries worker entropy: `grep -Eq 'workerIndex' test/example/priv/playwright/tests/admin-eval.spec.ts`.
</verification>

<success_criteria>
The probe-id drift guard actually runs in the eval suite, worker emails cannot collide under parallelism,
and the probe helper docs/fallbacks match their real behavior.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-08-SUMMARY.md` when done
</output>
