---
phase: 218-elevation-wave-nit-cleanup
plan: 02
type: execute
wave: 2
depends_on: [218-01]
files_modified:
  - guides/reference/admin-award-ledger.json
  - guides/reference/fix-queue.json
  - guides/reference/admin-render-sha.json
autonomous: true
requirements: [ELEVATE-01]
must_haves:
  truths:
    - "Every L2 cell (board-mg-1..11, all rendered states) is re-verified to HOLD its claimed Tier-2 against fresh rendered output — any cite-and-flipped cell is caught and its award band corrected DOWN only if it fails to hold"
    - "Every L1 cell (13 component boards) is re-verified to HOLD its claimed tier against fresh rendered output"
    - "award sub-scores are raised ONLY where fresh rendered evidence clearly earns it; each raise's verified_at_sha is the clean-tree HEAD at re-verification"
    - "The full-matrix fix-queue is regenerated (built minus settled) so open_findings reflects the expanded L1/L2 surface set; running fix-queue-build.mjs recomputes open_findings for the L1/L2 board surfaces in admin-render-sha.json while structurally skipping the 8 proxy-flagged L3 cells (the pins from Plan 01 Task 4 survive)"
  artifacts:
    - guides/reference/admin-award-ledger.json
    - guides/reference/fix-queue.json
    - guides/reference/admin-render-sha.json
  key_links:
    - "verify-then-climb: award-guard.mjs recomputes band=min(axes) and FAILs on climb-without-render, band!=min, unresolved evidence, or axis-decrease"
    - "quality-findings-monotonic.sh gates any findings-count regression against the base ledger"
    - "evidence_ref entries resolve via resolveEvidenceRef (probe:<canonical-id> / test: / conformance: only)"
  prohibitions:
    - "MUST NOT recapture any baseline PNG (Phase 219/RECAP-01 owns in-CI ubuntu recapture; darwin-local prohibited)"
    - "MUST NOT climb broadly to look like progress — honesty-first (D-05); over-claiming on cells that merely hold weakens the operator-truth guarantee"
    - "MUST NOT lower an award sub-score EXCEPT where a cell genuinely fails to hold Tier-2 against rendered output (correcting an optimistic cite-and-flip is the primary yield, not a monotonic-guard violation to route through the base-ledger update)"
    - "MUST NOT auto-apply CSS fixes (217 D-13 locked); the 13 CSS-anchored token findings stay operator-queue only"
    - "MUST NOT merge panel output into findings.json or the deterministic open_findings count (JUDGE-CI-01)"
---

<objective>
Run the L1 (13 component boards) and L2 (mg-1..11) fractal through the deterministic-probe half of the harness loop and verify-then-climb every cell. The primary deliverable is verify-hold: re-confirm every L1/L2 cell still HOLDS its claimed tier against FRESH rendered output (catching any optimistically cite-and-flipped cell from earlier phases). Earned award sub-score raises are the exception, applied only where fresh rendered evidence clearly earns them and each protected by the monotonic guard.

Purpose: SC-1 requires the L1/L2 component fractal to be run through the loop and verify-then-climbed. This plan is the deterministic (non-operator, non-LLM) portion; the operator panel + final climb decisions land in Plan 06.
Output: an award ledger where every L1/L2 cell is re-verified against clean-tree HEAD, and a regenerated full-matrix fix-queue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-CONTEXT.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@scripts/ci/admin-eval-harness.sh
@scripts/ci/award-guard.mjs
@scripts/ci/quality-findings-monotonic.sh
@scripts/ci/fix-queue-build.mjs
@scripts/ci/lib/eval-probe-ids.mjs
@guides/reference/admin-award-ledger.json
@guides/reference/admin-render-sha.json
@guides/reference/fix-queue.json
@guides/reference/admin-quality-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render + deterministic-probe the L1/L2 matrix, verify-hold every cell</name>
  <read_first>
    - scripts/ci/admin-eval-harness.sh (the --all render orchestrator; produces bundles + findings per cell)
    - scripts/ci/award-guard.mjs (recomputes band=min(axes); FAILs on climb-without-render / band!=min / unresolved-evidence / axis-decrease)
    - guides/reference/admin-award-ledger.json (the 11 L2 + 13 L1 cells added by 218-01 at honest-floor bands; the users-index-live cell shows the verified_at_sha / evidence_ref shape)
    - guides/reference/admin-quality-ledger.md (per-cell Tier claims to re-verify against rendered output)
    - 218-PATTERNS.md section 3 (verify-then-climb rule: verified_at_sha must be clean-tree HEAD; evidence_ref must resolve)
  </read_first>
  <action>
    Run the deterministic render + probe half of the loop across the expanded L1/L2 matrix (scripts/ci/admin-eval-harness.sh, where a local Postgres + example server are available; if the environment cannot boot the server, record that and drive the verification from the committed bundles/ledgers instead). For EACH L1 and L2 cell, re-verify it HOLDS its claimed tier against the fresh rendered output. This verify-hold pass is the primary yield: if a cell was optimistically cite-and-flipped in an earlier phase and does NOT hold against rendered evidence, correct its award band DOWN to the honest level and record the discrepancy in the SUMMARY. Update verified_at_sha for every re-verified cell to the clean-tree HEAD at re-verification. Do NOT climb any cell in this task — verification only. Keep board-scoped probes; do not introduce cross-board anchors.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node scripts/ci/award-guard.mjs && bash scripts/ci/quality-findings-monotonic.sh</automated>
  </verify>
  <done>Every L1/L2 cell in admin-award-ledger.json has verified_at_sha set to the clean-tree HEAD; any cell that fails to hold Tier-2 against rendered output is corrected to its honest band and documented; award-guard.mjs and quality-findings-monotonic.sh exit 0. The SUMMARY lists every cell verified and every correction made.</done>
</task>

<task type="auto">
  <name>Task 2: Apply earned award sub-score raises (selective, honesty-first) + regenerate fix-queue</name>
  <read_first>
    - guides/reference/admin-award-ledger.json (post-Task-1 verified cells)
    - scripts/ci/award-guard.mjs (each raise must have rendered:true + updated verified_at_sha + resolvable evidence_ref; band=min(axes))
    - scripts/ci/fix-queue-build.mjs (open = built minus settled; the sole open_findings writer for BOTH fix-queue.json and admin-render-sha.json — step (j), taught in Plan 01 Task 4 to skip "proxy": true surfaces so the 8 L3 proxy pins are never clobbered)
    - guides/reference/fix-queue.json (current 117-finding shape; ~103 judgment / 1 component / 13 CSS-token-refused)
    - guides/reference/admin-render-sha.json (step (j) rewrites open_findings on the L1/L2 board surfaces here; proxy-flagged L3 cells are skipped — this is why admin-render-sha.json is in this plan's files_modified)
    - guides/reference/settled-findings.tsv (the settled/waiver OPEN-set sibling)
  </read_first>
  <action>
    For each L1/L2 cell where the fresh rendered evidence from Task 1 CLEARLY earns a higher award sub-score on a specific axis, raise ONLY that axis, set rendered:true, update verified_at_sha to clean-tree HEAD, and add a resolvable evidence_ref (probe:<canonical-id> / test: / conformance:). Do NOT raise broadly to show progress (D-05) — most raises are deferred to the operator PR sign-off in Plan 06; this task applies only the unambiguous, probe-evidenced raises. Then regenerate fix-queue.json via scripts/ci/fix-queue-build.mjs so open_findings reflects the expanded L1/L2 surface set (built minus settled). The same builder run also rewrites open_findings for the L1/L2 board surfaces in admin-render-sha.json (step j) — this is expected and why admin-render-sha.json is in this plan's files_modified. It structurally SKIPS the 8 proxy-flagged L3 cells (Plan 01 Task 4), so their pinned values (197 light / 181 dark) are preserved; confirm they are unchanged after the run. Do NOT hand-edit fix-queue.json — it is derived. Do NOT auto-apply CSS fixes; the 13 CSS-anchored token findings stay in the operator queue (217 D-13). Record each applied raise and its evidence in the SUMMARY, and list the raises deferred to Plan 06.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node scripts/ci/award-guard.mjs && bash scripts/ci/quality-findings-monotonic.sh && node -e "const q=require('./guides/reference/fix-queue.json'); if(!Array.isArray(q)&&!q.findings){console.error('fix-queue shape unexpected');process.exit(1)} const r=require('./guides/reference/admin-render-sha.json').cells; const proxies=['index','organization','users-index-live','user-show-live','user-sessions','audit-index','audit-user','branding']; const bad=proxies.filter(s=>r[s]&&Object.values(r[s]).some(c=>c&&typeof c==='object'&&'open_findings'in c&&c.open_findings!==197&&c.open_findings!==181)); if(bad.length){console.error('proxy open_findings clobbered on:',bad);process.exit(1)} console.log('PASS fix-queue regenerated; proxy pins intact')"</automated>
  </verify>
  <done>All applied raises pass award-guard.mjs (band=min(axes), rendered:true, resolvable evidence, no axis decrease); fix-queue.json is regenerated (not hand-edited) reflecting the full L1/L2 matrix; the builder's rewrite of admin-render-sha.json recomputed L1/L2 open_findings while leaving the 8 proxy-flagged L3 cells pinned (197 light / 181 dark); no CSS auto-apply occurred; quality-findings-monotonic.sh exits 0. SUMMARY lists applied raises vs raises deferred to Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan runs the render/probe harness and edits two committed JSON ledgers (award + derived fix-queue). No runtime auth/session/authz code is touched; the LLM panel is not invoked here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-02-01 | Tampering | award-ledger integrity | low | mitigate | award-guard.mjs recomputes band=min(axes) and rejects climb-without-render / unresolved-evidence; verify-hold is the honesty check |
| T-218-02-03 | Tampering | proxy open_findings clobbered when the builder rewrites admin-render-sha.json | medium | mitigate | fix-queue-build.mjs skips "proxy": true surfaces (Plan 01 Task 4); Task 2 verify asserts the 8 proxy pins are unchanged after the builder run |
| T-218-02-02 | Repudiation | over-claimed award raise passed off as verified | low | mitigate | verified_at_sha pinned to clean-tree HEAD; evidence_ref must resolve to a canonical probe id; D-05 honesty-first posture defers ambiguous raises to operator PR sign-off |

No packages installed; Package Legitimacy Gate N/A. Security surface LOW (harness run + JSON ledgers).
</threat_model>

<verification>
- Every L1/L2 cell re-verified to hold its tier against rendered output; verified_at_sha = clean-tree HEAD.
- Any cite-and-flipped cell corrected DOWN to honest band and documented.
- Applied raises pass award-guard.mjs; ambiguous raises deferred to Plan 06.
- fix-queue.json regenerated (derived, not hand-edited); the builder's rewrite of admin-render-sha.json recomputed L1/L2 open_findings and left the 8 proxy-flagged L3 pins intact (197 light / 181 dark).
- award-guard.mjs + quality-findings-monotonic.sh exit 0.
</verification>

<success_criteria>
The L1/L2 fractal is verify-then-climbed: every cell holds its tier against fresh rendered output (or is corrected), earned raises are applied under the monotonic guard, and the fix-queue reflects the full matrix. All deterministic gates green.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-02-SUMMARY.md` when done. Record: every L1/L2 cell verified, every cite-and-flip correction made, every award raise applied (with axis + evidence_ref), and the list of raises deferred to the operator PR sign-off (Plan 06).
</output>
