---
phase: 218-elevation-wave-nit-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/live/mfa_settings_live.ex
  - test/example/lib/example_web/live/organization_members_live.ex
autonomous: true
requirements: [ELEVATE-02]
must_haves:
  truths:
    - "mfa_settings_live.ex enrollment sub-flows (QR step, backup-codes grid, passkey rows) use vt-panel row treatment and .vt-form .input instead of residual daisyUI/Tailwind utilities"
    - "organization_members_live.ex confirm modals (invite/revoke/change-role/remove) and the invite form use a vt-modal restyle of the native <dialog> (preserving phx-hook=DialogModal) and .vt-form, instead of daisyUI modal/modal-box/modal-action/modal-backdrop and input input-bordered"
    - "A new vt-modal class (or a vt-* restyle of the native <dialog>) exists — the one net-new class in the phase — authored against the sg-*/vt-* design contract"
    - "The reachability of mfa_challenge_html.ex + mfa_challenge_controller.ex is confirmed: removed if dead, else wrapped in the vt-auth surface"
  artifacts:
    - test/example/lib/example_web/live/mfa_settings_live.ex
    - test/example/lib/example_web/live/organization_members_live.ex
  key_links:
    - "settings_live.ex is the vt-* analog — copy its vt-panel/vt-form/.input shapes verbatim"
    - "phx-hook=DialogModal behavior is preserved through the restyle (keep the hook, swap the classes)"
    - "the demo is --no-tailwind so residual daisy classes render as plain HTML, not broken — this is cosmetic coherence, not a functional bug"
  prohibitions:
    - "MUST NOT touch any scripts/ci, scripts/panel, or admin-eval file — UI-02 is a cosmetic --no-tailwind demo restyle with zero harness overlap"
    - "MUST NOT alter any authz/session/MFA-enforcement behavior — this is a class/markup restyle ONLY; enrollment logic, phx-submit/phx-click events, and the DialogModal hook stay identical"
    - "MUST NOT recapture any baseline PNG"
    - "MUST NOT introduce Tailwind utilities — the example is --no-tailwind; use vt-*/sg-* tokens only"
---

<objective>
Fold the UI-02 carry-forward todo (2026-06-22-vaultr-authed-rebrand-residuals) per D-11 — the residual daisyUI/Tailwind spots the WS3 Tasklane rebrand left in two authed demo LiveViews. Cosmetic --no-tailwind demo restyle; touches no harness files, so it runs in parallel with the harness plans.

Scope:
1. mfa_settings_live.ex — migrate the enrollment sub-flows (QR step, backup-codes grid, passkey rows) from raw Tailwind to vt-panel / .vt-form .input; preserve the already-vt-* panel shell.
2. organization_members_live.ex — restyle the four native <dialog class="modal"> confirm modals + the invite form from daisyUI to a new vt-modal (vt-* restyle of <dialog>) + .vt-form, preserving phx-hook=DialogModal.
3. Confirm whether mfa_challenge_html.ex + mfa_challenge_controller.ex are reachable (unwired vs live /users/mfa) — remove if dead, else wrap in vt-auth.

Purpose: SC-2 requires UI-02 fully resolved with no outstanding items in the carry-forward todo.
Output: two restyled LiveViews, a new vt-modal class, a reachability decision on the mfa_challenge pair, and the todo moved to resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-CONTEXT.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@test/example/lib/example_web/live/settings_live.ex
@test/example/lib/example_web/live/mfa_settings_live.ex
@test/example/lib/example_web/live/organization_members_live.ex
@guides/reference/admin-design-contract.md
@.planning/todos/pending/2026-06-22-vaultr-authed-rebrand-residuals.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate mfa_settings_live.ex enrollment sub-flows to vt-*</name>
  <read_first>
    - test/example/lib/example_web/live/settings_live.ex lines 88-103 (the vt-panel + vt-form + .input reference shape to copy)
    - mfa_settings_live.ex lines 311-330 (passkey rows — raw Tailwind, migrate to vt-panel row treatment)
    - mfa_settings_live.ex lines 238-241 (dispatch of render_enrollment_qr / render_backup_codes) and lines 159, 210 (raw code inputs to migrate to .vt-form .input)
    - mfa_settings_live.ex lines 71, 261 (panel shell already vt-panel — migrate inner sub-flows only, preserve the shell)
    - .planning/todos/pending/2026-06-22-vaultr-authed-rebrand-residuals.md item 1
  </read_first>
  <action>
    Migrate ONLY the inner enrollment sub-flows to vt-*, preserving the already-vt-* panel shell. Passkey rows (mfa_settings_live.ex:311-330) get vt-panel row treatment; the backup-codes grid + QR-step code inputs (render_enrollment_qr / render_backup_codes, and the raw inputs at :159 and :210) become .vt-form .input. Copy the vt-panel/vt-form/.input shapes verbatim from settings_live.ex:88-103. This is a class/markup restyle ONLY — do not change any phx-submit/phx-click event, enrollment state, or MFA logic. Use vt-*/sg-* tokens; do not introduce Tailwind (the example is --no-tailwind).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c 'vt-panel\|vt-form' test/example/lib/example_web/live/mfa_settings_live.ex | grep -qv '^0$' && grep -c 'space-y-\|text-gray-\|border-gray-200\|border-red-300' test/example/lib/example_web/live/mfa_settings_live.ex | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>The enrollment sub-flows (QR step, backup-codes grid, passkey rows) use vt-panel / .vt-form .input; no residual space-y-*/text-gray-*/border-gray-200/border-red-300 Tailwind utilities remain; all phx-* events and MFA enrollment logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Build vt-modal + restyle organization_members_live.ex dialogs; resolve mfa_challenge reachability</name>
  <read_first>
    - organization_members_live.ex lines 455-505 (representative invite dialog — native <dialog class="modal"> with modal-box/modal-action/modal-backdrop + input input-bordered; phx-hook=DialogModal) and the other three dialogs at :507 (revoke), :537 (role), :569 (remove)
    - organization_members_live.ex lines 491, 496, 520, 525 (buttons already carry vt-* classes — only the modal/input daisy anchors remain)
    - settings_live.ex lines 88-103 (vt-form/.input reference)
    - guides/reference/admin-design-contract.md (the sg-*/vt-* design system to author vt-modal against)
    - 218-PATTERNS.md section 6 + "No Analog Found" (vt-modal is net-new; grep vt-modal returns zero repo-wide; preserve phx-hook=DialogModal through the restyle)
    - test/example/lib/example_web/... mfa_challenge_html.ex + mfa_challenge_controller.ex (confirm router wiring vs live /users/mfa)
    - .planning/todos/pending/2026-06-22-vaultr-authed-rebrand-residuals.md items 2 and 3
  </read_first>
  <action>
    Build a vt-modal class (or a vt-* restyle of the native <dialog>) authored against the sg-*/vt-* design contract — this is the one net-new class in the phase. Apply it to all four confirm dialogs (invite/revoke/change-role/remove) in organization_members_live.ex, replacing the daisyUI modal / modal-box / modal-action / modal-backdrop anchors and the invite-form input input-bordered / select select-bordered / label-text with vt-modal + .vt-form + .input. KEEP phx-hook=DialogModal and every phx-* event exactly as-is — this is a class swap, not a behavior change. Then resolve the mfa_challenge pair: grep the router to confirm whether mfa_challenge_html.ex + mfa_challenge_controller.ex are reachable (the live /users/mfa is the real path). If they are dead/unwired, remove both; if reachable, wrap the challenge markup in the vt-auth surface (mirroring mfa_challenge_live). Record the reachability decision in the SUMMARY. Do not introduce Tailwind.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c 'vt-modal' test/example/lib/example_web/live/organization_members_live.ex | grep -qv '^0$' && grep -c 'class="modal"\|modal-box\|modal-action\|modal-backdrop\|input input-bordered\|select select-bordered' test/example/lib/example_web/live/organization_members_live.ex | grep -qx 0 && grep -c 'DialogModal' test/example/lib/example_web/live/organization_members_live.ex | grep -qv '^0$' && echo PASS</automated>
  </verify>
  <done>A vt-modal class exists and is applied to all four dialogs; no daisyUI modal/modal-box/modal-action/modal-backdrop or input input-bordered/select select-bordered/label-text anchors remain; phx-hook=DialogModal preserved on every dialog; the mfa_challenge pair reachability is resolved (removed if dead, else vt-auth-wrapped) and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to LiveView (mfa_settings_live / organization_members_live) | These are auth-adjacent authed surfaces (MFA enrollment, org membership). The trust boundary is real, but this plan makes ONLY cosmetic class/markup changes — no authz, session, MFA-enforcement, or phx-* event logic is altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-05-01 | Elevation of Privilege | accidental authz/MFA-enforcement change while restyling auth-adjacent LiveViews | medium | mitigate | restyle is class-swap ONLY; every phx-submit/phx-click event, enrollment state machine, and the DialogModal hook are preserved verbatim; verify greps confirm no logic anchors removed, only presentation classes; reviewer confirms diff is markup/class-only at PR sign-off |
| T-218-05-02 | Information Disclosure | dead mfa_challenge_controller.ex left reachable | low | mitigate | Task 2 explicitly resolves reachability — removed if dead, else wrapped in vt-auth; decision recorded in SUMMARY |

No packages installed; Package Legitimacy Gate N/A. Security note (per planning guidance): UI-02 touches auth-adjacent LiveViews — restyle changes are confirmed cosmetic-only and do not alter authz/session behavior.
</threat_model>

<verification>
- mfa_settings_live.ex enrollment sub-flows use vt-panel / .vt-form .input; no residual Tailwind utilities.
- vt-modal class exists; organization_members_live.ex dialogs + invite form use vt-modal + .vt-form; no daisyUI modal/input anchors remain; phx-hook=DialogModal preserved.
- mfa_challenge pair reachability resolved and documented.
- No authz/session/MFA-enforcement logic changed (class/markup restyle only).
- No scripts/ci, scripts/panel, admin-eval file touched; no Tailwind introduced; no PNG recapture.
</verification>

<success_criteria>
UI-02 fully resolved: both LiveViews restyled to vt-*, vt-modal built, mfa_challenge reachability decided, no outstanding items in the carry-forward todo, and the todo moved to .planning/todos/resolved/.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-05-SUMMARY.md` when done. Move `.planning/todos/pending/2026-06-22-vaultr-authed-rebrand-residuals.md` to `.planning/todos/resolved/`. Record the vt-modal class design, the mfa_challenge reachability decision, and confirmation the diff is markup/class-only.
</output>
