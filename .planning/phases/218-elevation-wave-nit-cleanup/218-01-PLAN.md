---
phase: 218-elevation-wave-nit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/lib/eval/probes.ts
  - test/example/priv/playwright/tests/admin-eval.spec.ts
  - guides/reference/admin-render-sha.json
  - guides/reference/admin-award-ledger.json
autonomous: true
requirements: [ELEVATE-01]
must_haves:
  truths:
    - "probes.ts no longer hand-maintains a second PROBE_IDS array; drift between it and eval-probe-ids.mjs is caught at test time (import or deep-equal self-test)"
    - "admin-eval.spec.ts renders the 13 L1 COMPONENT_BOARDS into the eval matrix (single-state per board, not a fabricated 4-state matrix)"
    - "First-navigation goto uses waitUntil:'domcontentloaded' + waitForLiveViewReady; re-running admin-eval-harness.sh shows near-zero flaky count"
    - "admin-render-sha.json and admin-award-ledger.json hold the full matrix: all 11 L2 boards, all 13 L1 boards, and all 8 L3 surface-to-board proxies (the 2 pilots plus 6 net-new)"
    - "admin-panel.sh --all and award-guard.mjs enumerate the full expanded surface set from admin-render-sha.json"
  artifacts:
    - test/example/priv/playwright/lib/eval/probes.ts
    - test/example/priv/playwright/tests/admin-eval.spec.ts
    - guides/reference/admin-render-sha.json
    - guides/reference/admin-award-ledger.json
  key_links:
    - "L1/L3 surface keys produce finding_id via sha256(surface NUL class NUL anchor) so they collide with settled-findings waivers"
    - "L3 proxy render_sha256 values are byte-identical to their representative board's sha (the proxy mechanism)"
    - "New evidence_ref entries resolve via resolveEvidenceRef (probe:<canonical-id> / test: / conformance: only)"
  prohibitions:
    - "MUST NOT recapture any baseline PNG (D-07 — darwin-local recapture prohibited; Phase 219/RECAP-01 owns in-CI ubuntu recapture) — this plan only reads screenshot.png if at all"
    - "MUST NOT fabricate 4 states (populated/zero/loading/error) for L1 component boards that expose only one fixture — capture ONE cell per L1 board"
    - "MUST NOT run fix-queue-build.mjs against the L3 proxy cells — open_findings on proxy cells stays pinned per the render-sha notes field (panel reads only render_sha256)"
    - "MUST NOT reopen 217 D-13 — no CSS auto-apply is introduced here"
    - "MUST NOT lower any existing award sub-score (award-guard.mjs is monotonic)"
---

<objective>
Harden the 216/217 evaluation harness and expand the committed render/award ledgers from the current 2-pilot / 4-cell slice to the FULL fractal matrix, so the rest of the wave can run every surface through the loop. This is the wave barrier: Plans 02 and 03 depend on the expanded matrix being present in admin-render-sha.json.

Three units of work, ordered for drift-safety (D-08 before D-02):
1. D-08 — fold probes.ts PROBE_IDS to a single source (import or deep-equal self-test); remove the FOLLOW-UP(216) markers.
2. D-09 — fix the first-nav goto flake (16 flakes observed in 216-09) so the larger matrix runs fast and deterministically.
3. D-02 — extend admin-eval.spec.ts to render the 13 L1 boards, and promote the full matrix (11 L2 + 13 L1 + 8 L3 proxies) into both committed ledgers.

Purpose: SC-1 requires all 8 admin surfaces + the L1/L2 fractal to run through the loop; admin-panel.sh --all and award-guard.mjs only see surfaces present in admin-render-sha.json, so this expansion is the precondition.
Output: single-source probe ids, deflaked render spec, and full-matrix render/award ledgers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-CONTEXT.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@scripts/ci/lib/eval-probe-ids.mjs
@test/example/priv/playwright/lib/eval/probes.ts
@test/example/priv/playwright/tests/admin-eval.spec.ts
@test/example/priv/playwright/tests/admin-design.spec.ts
@guides/reference/admin-render-sha.json
@guides/reference/admin-award-ledger.json
@guides/reference/admin-quality-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold probes.ts PROBE_IDS to a single source (D-08)</name>
  <read_first>
    - test/example/priv/playwright/lib/eval/probes.ts (lines 27, 35 carry FOLLOW-UP(216) markers; lines 39-49 duplicate PROBE_IDS)
    - scripts/ci/lib/eval-probe-ids.mjs (lines 19-29 — canonical frozen PROBE_IDS, the single source the guards import)
    - test/example/priv/playwright/tests/admin-eval.spec.ts lines 14-20 (the documented import.meta.url CJS/ESM workaround — same interop class that got D-08 deferred)
  </read_first>
  <action>
    Make probes.ts stop hand-maintaining its own PROBE_IDS. First attempt the direct import: import PROBE_IDS from scripts/ci/lib/eval-probe-ids.mjs and delete the local array. If Playwright's CJS/ESM transform cannot resolve the cross-tree .mjs import cleanly (the documented reason D-08 was deferred in 216-08 Task 2), fall back to keeping a local array PLUS a deep-equal self-test that reads the canonical list and fails at test time if the two drift. Either way, remove both marker lines that reference the deferred follow-up. Do this task FIRST — a larger surface set (Task 3) raises the drift risk this closes. Do not change any probe id string; the nine ids are frozen.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsc --noEmit && grep -c 'FOLLOW-UP(216)' lib/eval/probes.ts | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>probes.ts has no FOLLOW-UP(216) marker; either imports PROBE_IDS from the canonical .mjs, or retains a deep-equal self-test that would fail on drift; TypeScript compiles clean; the nine probe id strings are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Deflake first-navigation goto (D-09)</name>
  <read_first>
    - test/example/priv/playwright/tests/admin-eval.spec.ts lines 141-148 (waitForLiveViewReady helper — already exists, keep pairing it with every goto)
    - admin-eval.spec.ts line 153 (registerUser goto /users/register) and line 158 (waitForURL timeout 30_000)
    - admin-eval.spec.ts lines 318-323 (beforeEach goto /admin/_design — the primary flake site)
    - .planning/todos/pending/2026-07-04-admin-eval-first-nav-flake.md (16 first-nav flakes, ~16 min hangs on the fixed per-test timeout)
  </read_first>
  <action>
    At both goto sites (registerUser's /users/register and the beforeEach /admin/_design), pass { waitUntil: 'domcontentloaded' } instead of the default 'load', then keep the existing explicit waitForLiveViewReady gate immediately after each goto. Consider lowering the registerUser waitForURL timeout from 30_000 so a stuck first-nav fails fast into its Playwright retry instead of hanging ~16 min on a fixed per-test timeout. Do not change what waitForLiveViewReady asserts (it is lifted verbatim from admin-design.spec.ts per D-03 and gates on phx-connected + Space Grotesk font-ready). Do this early — 16 first-nav flakes at 4 cells scale badly across a 36+-cell matrix.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsc --noEmit && grep -c "waitUntil: 'domcontentloaded'" tests/admin-eval.spec.ts | grep -qv '^0$' && echo PASS</automated>
  </verify>
  <done>Both first-nav goto calls use waitUntil:'domcontentloaded' followed by waitForLiveViewReady; TypeScript compiles. The executor SHOULD, where a local Postgres + example server are available, re-run scripts/ci/admin-eval-harness.sh and confirm a near-zero flaky count; record the flaky count in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Extend eval matrix to L1 boards + promote full matrix into both ledgers (D-02)</name>
  <read_first>
    - admin-eval.spec.ts line 187 (GROUP_BOARDS mg-1..11) and lines 335-364 (the L2 4-state capture loop to mirror)
    - admin-design.spec.ts lines 98-103 (the 13-entry COMPONENT_BOARDS const to copy verbatim) and line 257 (single-state L1 iteration — one screenshot per board)
    - guides/reference/admin-render-sha.json (cells: currently board-mg-5-* / board-mg-9-* + 2 L3 proxies users-index-live / user-show-live; note the L3-proxy shape reuses the board's render_sha256 byte-identically; read the notes field about proxy open_findings)
    - guides/reference/admin-award-ledger.json (cells: users-index-live / user-show-live — the award-cell template with axes/band/verified_at_sha/rendered/evidence_ref)
    - guides/reference/admin-quality-ledger.md L85-92 (the 8 L3 surfaces + the 36-cell fractal inventory — the surface names to key on)
    - 218-PATTERNS.md sections 1 and 3 (L1 single-state caveat; proxy-sha mechanism; do-not-run-fix-queue-on-proxy note)
  </read_first>
  <action>
    Two moves. (a) In admin-eval.spec.ts add a COMPONENT_BOARDS const copied verbatim from admin-design.spec.ts lines 98-103 and add an L1 render loop that captures ONE cell per L1 board (a synthetic -default state), mirroring the existing L2 captureSurface/writeBundleLocal machinery. L1 boards are single-fixture — do NOT fabricate populated/zero/loading/error states for them. Keep board-scoped probes (root: '#' + boardId); design-token reads stay global. (b) Promote the full matrix into both committed ledgers by copying the existing entry shapes: into admin-render-sha.json add the remaining 10 L2 boards (mg-1..4, mg-6..8, mg-10, mg-11 — mg-5/mg-9 already present) each with their 4 states, the 13 L1 boards each single-state, and the 6 not-yet-mapped L3 surface-to-board proxies (index, organization, user-sessions, audit-index, audit-user, branding) each reusing a representative board's render_sha256 byte-identically (mirroring the mg-2/5=users-index and mg-9/10/11=user-show pilots). Author the concrete L3-to-board proxy mapping against design_gallery_live.ex fixtures + admin-quality-ledger.md evidence. Into admin-award-ledger.json add award cells for the 6 new L3 surfaces using the users-index-live cell as the template (axes/band/rendered/verified_at_sha/evidence_ref), with evidence_ref entries that resolve via resolveEvidenceRef (probe:<canonical-id> / test: / conformance: only). Set award cells for newly-added surfaces at the honest floor — do NOT climb here; climb happens in Plans 02/03/06 against fresh render. Do NOT run fix-queue-build.mjs against the L3 proxy cells; open_findings on proxy cells stays pinned per the render-sha notes field.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && node -e "const r=require('./guides/reference/admin-render-sha.json').cells; const l2=['board-mg-1','board-mg-2','board-mg-3','board-mg-4','board-mg-5','board-mg-6','board-mg-7','board-mg-8','board-mg-9','board-mg-10','board-mg-11']; const mL2=l2.filter(b=>!r[b+'-populated']); const l3=['index','organization','users-index-live','user-show-live','user-sessions','audit-index','audit-user','branding']; const mL3=l3.filter(s=>!r[s]); if(mL2.length||mL3.length){console.error('MISSING L2:',mL2,'L3:',mL3);process.exit(1)} console.log('PASS: 11 L2 + 8 L3 present')" && bash scripts/ci/quality-findings-monotonic.sh</automated>
  </verify>
  <done>admin-eval.spec.ts renders the 13 L1 boards single-state; admin-render-sha.json contains all 11 L2 boards (4 states each), 13 L1 boards, and all 8 L3 surfaces (2 pilots + 6 new proxies with byte-identical proxy shas); admin-award-ledger.json has award cells for all 8 L3 surfaces at honest floor bands; award-guard.mjs and quality-findings-monotonic.sh both exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan edits test-harness Playwright specs, a probe library, and two committed JSON ledgers. No untrusted input crosses a boundary; no runtime auth/session/authz code is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-01-01 | Tampering | ledger integrity (admin-render-sha.json / admin-award-ledger.json) | low | mitigate | award-guard.mjs (monotonic) + quality-findings-monotonic.sh gate any award-sub-score decrease or findings-count regression; L3 proxy shas must be byte-identical to their board (proxy invariant) |

No packages are installed in this plan; the Package Legitimacy Gate does not apply. Security surface is LOW (test harness + JSON data only).
</threat_model>

<verification>
- probes.ts: no FOLLOW-UP(216) markers; single-source PROBE_IDS (import or deep-equal self-test); tsc clean.
- admin-eval.spec.ts: L1 COMPONENT_BOARDS loop present (single-state); both first-nav gotos use waitUntil:'domcontentloaded'; tsc clean.
- admin-render-sha.json: 11 L2 boards (4 states) + 13 L1 boards + 8 L3 surfaces; L3 proxy shas byte-identical to their representative board.
- admin-award-ledger.json: award cells for all 8 L3 surfaces; no sub-score lowered.
- award-guard.mjs and quality-findings-monotonic.sh exit 0.
</verification>

<success_criteria>
Full fractal matrix (11 L2 + 13 L1 + 8 L3) present in both committed ledgers; probe ids single-sourced; first-nav flake fixed; all monotonic guards green. Plans 02 and 03 can now enumerate every surface via admin-render-sha.json.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-01-SUMMARY.md` when done. Record: which D-08 path was taken (import vs deep-equal self-test), the measured flaky count from any harness re-run, the concrete 6 L3-to-board proxy mappings authored, and the honest-floor bands assigned to the 6 new L3 award cells.
</output>
