---
phase: 218-elevation-wave-nit-cleanup
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/live/mfa_settings_live.ex
  - priv/templates/sigra.install/core/mfa_settings_live.ex
  - test/example/lib/example_web/live/organization_members_live.ex
  - priv/templates/sigra.install/organizations/live/organization_members_live.ex
  - test/example/priv/static/assets/css/app.css
  - test/example/test/example_web/live/organization_members_live_test.exs
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organization_members_live.ex
autonomous: true
gap_closure: true
requirements: [ELEVATE-02]
must_haves:
  truths:
    - "do_confirm_enrollment/2 survives DB/transaction error tuples ({:error, changeset} / {:error, _reason}) without a CaseClauseError crash — in BOTH the example LiveView and the installer template"
    - "change_role / remove_member cannot be crashed by a client sending the event while pending_action is nil (MatchError guarded) — in BOTH example and template"
    - "A role/remove lookup miss flashes an error instead of silently no-op'ing — in BOTH example and template"
    - "The MFA enabled success icon renders in the positive tone via a defined CSS token (example only)"
    - "The dead vt-modal backdrop form is removed and the app.css comment corrected (example only)"
    - "The installer golden fixture reflects the mirrored template fixes (golden_diff stays green)"
  artifacts:
    - "example + template mfa_settings_live.ex with a {:error, _reason} fallthrough clause"
    - "example + template organization_members_live.ex with guarded change_role/remove_member heads and flash-on-miss lookup"
    - "test/example/priv/static/assets/css/app.css with corrected backdrop comment"
    - "organization_members_live_test.exs regression tests for the nil-pending_action event and the lookup-miss flash"
    - "regenerated test/fixtures/install_golden/ tree for the two mirrored templates"
  key_links:
    - "example LiveView <-> installer template: WR-01/WR-02/WR-03 fixes must land in BOTH so generated hosts do not ship the bug (installer_drift_test parity)"
    - "template edits -> golden fixture: mix sigra.fixture.rebless_golden regenerates the committed tree"
---

<objective>
Close the demo-LiveView correctness gaps from 218-REVIEW.md (WR-01, WR-02, WR-03, WR-04, WR-07) and mirror
the security-relevant crash fixes into the installer templates so generated host apps do not ship the same
bugs. Sigra is a hybrid lib+generator: `test/example/` is the reference host app and
`priv/templates/sigra.install/` generates host apps — templates historically drift behind the example, so
WR-01/WR-02/WR-03 (which exist verbatim in the templates, verified) MUST be fixed in lockstep.

Verified template-reach (do not re-investigate; confirmed against the actual files):
- WR-01: template core/mfa_settings_live.ex has the identical non-exhaustive `do_confirm_enrollment/2`
  case (only `{:ok,...}` / `{:error, :invalid_code}`) -> MIRROR the fallthrough.
- WR-02: template organizations/live/organization_members_live.ex hard-destructures pending_action in
  change_role (:134) and remove_member (:304) -> MIRROR the guards.
- WR-03: template find_streamed_member (:617) refetches list_members_with_activity(limit: 1_000) ->
  MIRROR the flash-on-miss. No `Organizations.get_member`-by-id fn exists in either the example context
  or the template membership schema, so the robust by-id fetch would widen scope beyond a nit fix — use
  the flash-on-miss fallback and record the by-id upgrade as a follow-up (do NOT add new context surface).
- WR-04: EXAMPLE-ONLY. Template mfa uses Tailwind `text-green-500`, not `--vt-color-ok`. NO template mirror.
- WR-07: EXAMPLE-ONLY. Template org_members uses daisyUI `modal-backdrop`, not the vt-modal restyle. NO
  template mirror.

Note (installer-template-drift): editing the two templates changes the committed install golden fixture at
`test/fixtures/install_golden/tree/.../live/{mfa_settings_live,organization_members_live}.ex`. Task 3
re-blesses that fixture. The golden tests require the phx_new 1.8.8 archive pinned in CLAUDE.md.

Purpose: Eliminate client-crashable / silent-no-op paths in the demo AND in every generated host.
Output: Hardened example + template LiveViews, regression tests, and a re-blessed golden fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-REVIEW.md
@.planning/phases/218-elevation-wave-nit-cleanup/218-PATTERNS.md
@CLAUDE.md
@test/example/lib/example_web/live/mfa_settings_live.ex
@priv/templates/sigra.install/core/mfa_settings_live.ex
@test/example/lib/example_web/live/organization_members_live.ex
@priv/templates/sigra.install/organizations/live/organization_members_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: MFA fixes — WR-01 fallthrough (example + template) + WR-04 token (example only)</name>
  <read_first>
    - test/example/lib/example_web/live/mfa_settings_live.ex:955-979 (do_confirm_enrollment/2 case — only {:ok,...} and {:error,:invalid_code})
    - priv/templates/sigra.install/core/mfa_settings_live.ex:896-920 (identical case — same non-exhaustive shape)
    - lib/sigra/mfa.ex:277-300 (confirm_enrollment also returns {:error, changeset} / {:error, _reason} on write failure)
    - test/example/lib/example_web/live/mfa_settings_live.ex:250 (WR-04: style="color:var(--vt-color-ok)" — undefined token)
  </read_first>
  <files>test/example/lib/example_web/live/mfa_settings_live.ex, priv/templates/sigra.install/core/mfa_settings_live.ex</files>
  <action>
    WR-01 (BOTH files): add a `{:error, _reason}` fallthrough clause to `do_confirm_enrollment/2`, AFTER
    the existing `{:error, :invalid_code}` clause, so a transaction/insert failure from
    Auth.mfa_confirm_enrollment no longer raises CaseClauseError and crashes the LiveView on a keystroke.
    The fallthrough flashes a generic retry message and resets the enroll form, mirroring the shape of the
    existing `{:error, :invalid_code}` clause (put_flash :error + assign enroll_form to an empty
    to_form(..., as: "enroll")). Use the wording from 218-REVIEW.md WR-01 (a "Could not verify your code.
    Please try again." style message). Apply the SAME clause to both the example and the template file
    (the template body is byte-equivalent for this defp; keep them in lockstep for installer_drift_test).
    Do NOT introduce a fenced/verbatim copy of any acceptance literal a gate negative-greps.

    WR-04 (EXAMPLE ONLY): at mfa_settings_live.ex:250 change `style="color:var(--vt-color-ok)"` to
    `style="color:var(--vt-color-primary)"` (the defined positive-tone token used by .vt-status-pill--ok).
    Do NOT touch the template mfa file for WR-04 — it uses Tailwind `text-green-500` (no vt-* rebrand).
  </action>
  <verify>
    <automated>cd test/example && MIX_ENV=test mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>Both example and template do_confirm_enrollment/2 have a {:error, _reason} fallthrough; example mfa uses var(--vt-color-primary) at the enabled icon; example compiles clean under --warnings-as-errors; template mfa WR-04 untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Org-members fixes — WR-02 guards + WR-03 flash-on-miss (example + template) + WR-07 dead form (example)</name>
  <read_first>
    - test/example/lib/example_web/live/organization_members_live.ex:95-121 (open_role_modal/open_remove_modal — the nil branches currently return {:noreply, socket} silently)
    - test/example/lib/example_web/live/organization_members_live.ex:133-135, :300-302 (change_role/remove_member hard-destructure pending_action)
    - test/example/lib/example_web/live/organization_members_live.ex:625-636 (find_streamed_member capped at 1_000)
    - test/example/lib/example_web/live/organization_members_live.ex:506,536,567,595 (the four dead `<form method="dialog" class="vt-modal__backdrop">` sites)
    - priv/templates/sigra.install/organizations/live/organization_members_live.ex:96-120, :133-134, :303-304, :617-625 (identical structures — mirror WR-02 + WR-03 here; template uses <%%= %> EEx escaping — leave existing EEx untouched)
    - test/example/priv/static/assets/css/app.css:2842 (.vt-modal__backdrop { display:none } and its misleading comment)
    - test/example/test/example_web/live/organization_members_live_test.exs (existing LiveView test file — add regressions here)
  </read_first>
  <behavior>
    - change_role event with pending_action == nil: LiveView does NOT crash (no MatchError); returns noreply, no mutation.
    - remove_member event with pending_action == nil: LiveView does NOT crash; returns noreply, no mutation.
    - open_role_modal with an id that find_streamed_member cannot resolve: an error flash is set (not a silent no-op).
  </behavior>
  <files>test/example/lib/example_web/live/organization_members_live.ex, priv/templates/sigra.install/organizations/live/organization_members_live.ex, test/example/priv/static/assets/css/app.css, test/example/test/example_web/live/organization_members_live_test.exs</files>
  <action>
    WR-02 (BOTH example + template): guard `change_role` and `remove_member` against a nil pending_action.
    Add a matching function head that pattern-matches the pending_action in the socket assigns
    (e.g. `%{assigns: %{pending_action: {:role, member}}} = socket` for change_role, and the `{:remove, member}`
    analog for remove_member), and add a catch-all no-op head
    (`def handle_event("change_role", _params, socket), do: {:noreply, socket}` and the remove_member analog)
    AFTER the guarded head, so a raced/crafted websocket event while pending_action is nil returns noreply
    instead of raising MatchError. Move the existing body under the guarded head; remove the now-redundant
    inline `{:role, member} = socket.assigns.pending_action` / `{:remove, member} = ...` destructure lines.
    Preserve all existing scope-checked library calls and flashes. In the template, keep the `<%%= %>` EEx
    escaping intact; only the Elixir handle_event heads change.

    WR-03 (BOTH example + template): make the lookup miss non-silent. In the `nil ->` branches of
    open_role_modal and open_remove_modal, replace the bare `{:noreply, socket}` with a flash telling the
    admin the member could not be found (e.g. put_flash :error "That member could not be found. Refresh and
    try again."). Do NOT add a new Organizations context function or widen library surface — the robust
    by-id scoped fetch is recorded as a follow-up (see below). Leave find_streamed_member's capped refetch
    as-is; the flash converts the silent >1000-member no-op into a visible failure.

    WR-07 (EXAMPLE ONLY): remove the four vestigial `<form method="dialog" class="vt-modal__backdrop">...`
    lines (:506, :536, :567, :595) that claim to cover the backdrop but are `display:none`. Then correct
    the misleading comment at app.css:2842 so it no longer claims a click-outside-close affordance that does
    not exist (the modal closes via Cancel/Escape; keep `display:none`). Do NOT touch the template
    org_members for WR-07 (it uses daisyUI `modal-backdrop`, not the vt-modal restyle). When editing the
    app.css comment, do not reintroduce any literal that a CSS/comment gate negative-greps.

    Tests (organization_members_live_test.exs): add three regressions per the <behavior> block —
    (1) render the members LiveView, send a `change_role` event with pending_action still nil, assert the
    process does not crash (the render call returns normally); (2) same for `remove_member`; (3) trigger
    `open_role_modal` with an id absent from the streamed/scoped set and assert an error flash is present.
    Follow the AAA, flat, self-contained style; reuse the file's existing setup/login helpers.
  </action>
  <verify>
    <automated>cd test/example && mix test test/example_web/live/organization_members_live_test.exs 2>&1 | tail -8</automated>
  </verify>
  <done>change_role/remove_member are guarded (no MatchError on nil pending_action) in example AND template; lookup miss flashes in both; the four dead backdrop forms are gone and the app.css comment is corrected (example only); the three new regressions pass; template WR-07 untouched. Record the WR-03 robust-by-id scoped fetch (Organizations.get_member-style) as a follow-up note in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Re-bless the install golden fixture for the mirrored template edits</name>
  <read_first>
    - CLAUDE.md ("install golden tests ... require the phx_new 1.8.8 archive installed locally to match the CI pin")
    - lib/mix/tasks/sigra.fixture.rebless_golden.ex:1-95 (MIX_ENV=test mix sigra.fixture.rebless_golden; --check diffs without writing)
    - test/sigra/install/golden_diff_test.exs:1-40 (fixture shape; byte-identical gate)
    - test/sigra/templates/installer_drift_test.exs (example<->template parity guard — must stay green after Tasks 1-2)
  </read_first>
  <files>test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/organization_members_live.ex</files>
  <action>
    Because Tasks 1-2 edited two installer templates (core/mfa_settings_live.ex and
    organizations/live/organization_members_live.ex), the committed golden fixture now drifts. Regenerate it
    from the repo root with `MIX_ENV=test mix sigra.fixture.rebless_golden` (requires the phx_new 1.8.8
    archive per CLAUDE.md; install with `mix archive.install --force hex phx_new 1.8.8` if missing). Review
    the resulting diff and confirm it ONLY shows the intended fallthrough clause (mfa) and the guarded
    handle_event heads + flash-on-miss (org_members) in the regenerated fixture tree — no unrelated churn.
    Commit the regenerated fixture files. If the diff shows anything beyond the intended lines, stop and
    surface it rather than committing.
  </action>
  <verify>
    <automated>MIX_ENV=test mix sigra.fixture.rebless_golden --check 2>&1 | tail -5</automated>
  </verify>
  <done>`mix sigra.fixture.rebless_golden --check` reports no drift (committed fixture matches a fresh generation); golden_diff_test and installer_drift_test are green; the fixture diff is limited to the WR-01/WR-02/WR-03 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client websocket -> LiveView handle_event | A client can send change_role/remove_member directly (or race cancel_action) with pending_action == nil |
| DB/transaction -> MFA confirm handler | mfa_confirm_enrollment can return error tuples the case does not handle |
| installer template -> generated host app | A template bug ships to every host generated by mix sigra.install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-09-01 | Denial of Service | change_role/remove_member (example + template) | medium | mitigate | Guarded handle_event heads + catch-all no-op; crafted/nil-pending_action event returns noreply, cannot crash the LiveView |
| T-218-09-02 | Denial of Service | do_confirm_enrollment (example + template) | medium | mitigate | {:error, _reason} fallthrough flashes retry instead of CaseClauseError crash on a keystroke |
| T-218-09-03 | Tampering (silent failure) | find_streamed_member lookup miss | low | mitigate | Flash-on-miss makes the >1000-member no-op visible; scope-checked mutations already prevent cross-tenant writes |
| T-218-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; `mix archive.install phx_new 1.8.8` is the CLAUDE.md-pinned CI archive, not a new dependency |
</threat_model>

<verification>
- Example compiles clean: `cd test/example && MIX_ENV=test mix compile --warnings-as-errors`.
- Org-members regressions pass: `cd test/example && mix test test/example_web/live/organization_members_live_test.exs`.
- Golden fixture in sync: `MIX_ENV=test mix sigra.fixture.rebless_golden --check` reports no drift.
- Example<->template parity holds: `MIX_ENV=test mix test test/sigra/templates/installer_drift_test.exs`.
- Grep parity: both example and template mfa have a `{:error, _reason} ->` clause in do_confirm_enrollment; both example and template org_members have a catch-all `handle_event("change_role", _params, socket)` head.
</verification>

<success_criteria>
Client-crashable and silent-no-op paths are closed in the demo AND in every generated host; the golden
fixture reflects the mirrored template fixes; the WR-04/WR-07 example-only cosmetics are corrected.
</success_criteria>

<output>
Create `.planning/phases/218-elevation-wave-nit-cleanup/218-09-SUMMARY.md` when done
</output>
