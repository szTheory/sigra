---
phase: 66
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/mfa.ex
  - test/sigra/mfa_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-09
must_haves:
  truths:
    - "**`confirm_enrollment/5`** success path remains **one** `repo.transaction/1` over **`Ecto.Multi`** including **`Sigra.Audit.log_multi_safe(\"mfa.enroll.success\", ...)`** (**AUD-04-020** T1)."
    - "**`mfa.enroll.failure`** with **`metadata.reason == \"insert_failed\"`** is emitted via **`Ecto.Multi`** + **`Sigra.Audit.log_multi_safe/3`** inside its **own** `repo.transaction/1`, with **`Sigra.Audit.emit_telemetry_from_changes/1`** on success (**AUD-04-021**)."
    - "**`invalid_code`** branch keeps **`Sigra.Audit.log_safe/3`** unless **EX-44-02** is explicitly superseded in the same change set (**AUD-04-022** / **066-CONTEXT D-02** default)."
    - "New or extended tests in **`mfa_audit_atomicity_test.exs`** prove **021** failure audit path and preserve **020** rollback invariant."
  artifacts:
    - path: "lib/sigra/mfa.ex"
      provides: "AUD-04-021 production mechanism"
    - path: "test/sigra/mfa_audit_atomicity_test.exs"
      provides: "AUD-09 audit-aware tests"
nyquist_compliant: false
---

<objective>
Ship **AUD-09** production + test work for **`Sigra.MFA.confirm_enrollment/5`** **AUD-04-020..022**: align **021** with **`Multi` + `log_multi_safe`** in a dedicated transaction; leave **022** as documented **T2** unless discuss unlocks otherwise; extend **`mfa_audit_atomicity_test.exs`** with deterministic **`insert_failed`** coverage (**066-CONTEXT**, **066-RESEARCH**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-66-01 | Repudiation | **`insert_failed`** emits no durable **`mfa.enroll.failure`** when audit insert fails silently | high | Replace **`log_safe`** with transactional **`log_multi_safe`** + explicit **`{:ok, changes}`** telemetry path; on audit txn error, mirror **`log_safe`** observability (telemetry) without changing **`{:error, changeset}`** return |
| T-66-02 | Tampering | Partial credential without matching audit row | high | Keep success path inside single **`Multi`**; tests keep **`mfa.enroll.success`** CHECK rollback |
| T-66-03 | Denial of service | Extra transaction per enrollment failure | low | Single-row audit **`Multi`** — acceptable vs security goals |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: MFA enroll failure (insert_failed) uses Multi + log_multi_safe transaction</name>
  <files>lib/sigra/mfa.ex</files>
  <read_first>
    - lib/sigra/mfa.ex (function `confirm_enrollment/5`, ~lines 115–218)
    - lib/sigra/audit.ex (`log_safe/3`, `log_multi_safe/3`, `emit_telemetry_from_changes/1`–`2`)
    - lib/sigra/scope.ex (`from_config/2`, scope struct fields)
    - docs/audit-semantics.md
    - .planning/phases/066-seed-002-bounded-batch/066-CONTEXT.md
    - .planning/phases/066-seed-002-bounded-batch/066-RESEARCH.md
  </read_first>
  <action>
    In **`confirm_enrollment/5`**, in the **`case repo.transaction(multi)`** clause **`{:error, _step, changeset, _changes}`**:

    1. **Remove** the **`Sigra.Audit.log_safe(\"mfa.enroll.failure\", Sigra.Scope.from_config(config, user), Keyword.merge(mfa_audit_opts(config), actor_id: user.id, target_id: user.id, outcome: \"failure\", metadata: %{method: \"totp\", reason: \"insert_failed\"}))`** call.

    2. **Rebuild opts with the same merge order as `log_safe/3`:** `scope = Sigra.Scope.from_config(config, user)`; **`scope_opts`** = if `scope` matches a scope struct with **`:user`**, set **`organization_id`** from **`scope.active_organization && scope.active_organization.id`**, **`effective_user_id`** from **`scope.user.id`**, **`actor_id`** from **`(Map.get(scope, :impersonating_from) || scope.user).id`**; else **`[organization_id: nil, effective_user_id: nil, actor_id: nil]`** (mirror **`Sigra.Audit`** `scope_fields/1` behavior). Then **`caller_opts = Keyword.merge(mfa_audit_opts(config), actor_id: user.id, target_id: user.id, outcome: \"failure\", metadata: %{method: \"totp\", reason: \"insert_failed\"})`**, then **`failure_opts = Keyword.merge(scope_opts, caller_opts)`** (caller wins per **D-06**).

    3. **`failure_multi = Ecto.Multi.new() |> Sigra.Audit.log_multi_safe(\"mfa.enroll.failure\", Keyword.merge(failure_opts, audit_multi_step: :audit_mfa_enroll_insert_failed))`** — pick a **unique** **`audit_multi_step`** atom if this collides elsewhere in **`mfa.ex`**.

    4. **`case repo.transaction(failure_multi)`**: on **`{:ok, changes}`**, call **`Sigra.Audit.emit_telemetry_from_changes(changes, [:audit_mfa_enroll_insert_failed])`**.

    5. On **`{:error, _, _, _}`** for the failure-audit transaction: invoke the same telemetry path **`Sigra.Audit`** uses when **`repo.insert`** fails inside **`do_log_safe`** (grep **`emit_log_safe_error`** / **`[:sigra, :audit, :log_safe_error]`** in **`audit.ex`**) so operators keep visibility; **always** return **`{:error, changeset}`** from the **original** enrollment **`Multi`** failure.

    6. **Do not** change the **`verify_totp`** **`{:error, _reason}`** branch (**022**) in this task.

    7. Update the **`lib/sigra/mfa.ex`** header dispatch comment (~lines 35–43): **`confirm_enrollment`** **`insert_failed`** uses **follow-up** **`repo.transaction(Multi + log_multi_safe)`** after the rolled-back enrollment **`Multi`**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'insert_failed' lib/sigra/mfa.ex` shows **`log_multi_safe`** on the same branch or adjacent lines (not **`log_safe`** for that reason string).
    - `grep -nF 'mfa.enroll.failure' lib/sigra/mfa.ex` contains **`log_multi_safe`** at least once.
    - `grep -nF 'confirm_enrollment' lib/sigra/mfa.ex` — the **`def confirm_enrollment`** function contains **`repo.transaction`** at least **twice** (enrollment multi + failure-audit multi) OR one extracted private function that makes the two boundaries obvious to grep (**≥ 2** `repo.transaction` occurrences in the function body is acceptable).
    - `mix format lib/sigra/mfa.ex` exits **0**.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits **0**.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0** after Task 2 lands (re-run after Task 2).
  </verify>
</task>

<task type="execute">
  <name>Task 2: Tests for insert_failed audit path + invariants</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/mfa_audit_atomicity_test.exs
    - lib/sigra/mfa.ex
    - .planning/phases/061-seed-002-bounded-batch/061-01-PLAN.md (test tone / CHECK patterns)
  </read_first>
  <action>
    Add **at least one** integration-style test that forces **`confirm_enrollment`** into the **`insert_failed`** path and asserts a durable **`mfa.enroll.failure`** audit row with **`metadata`** containing **`insert_failed`** (substring match on **`metadata::text`** is fine):

    - **Technique:** in **`try/after`**, add **`ALTER TABLE user_mfa_backup_codes ADD CONSTRAINT mfa_enroll_backup_guard CHECK (false)`** (or equivalent **CHECK** that rejects all inserts), call **`MFA.confirm_enrollment(...)`**, expect **`{:error, %Ecto.Changeset{}}`**, assert **`count(repo, \"user_mfa_credentials\") == 0`**, **`count(repo, \"user_mfa_backup_codes\") == 0`**, and **`count_where(repo, \"audit_events\", \"action = 'mfa.enroll.failure'\") >= 1`** with metadata containing **`insert_failed`**.

    Add **one** **`try/after`** test that adds a **`CHECK`** constraint on **`audit_events`** rejecting **`mfa.enroll.failure`**, invokes **`confirm_enrollment`** through the **`insert_failed`** path (same backup guard or equivalent), and asserts **no** **`mfa.enroll.failure`** row exists and **no** credential rows — proving the **audit-only** transaction rolls back (mirrors existing **`mfa.enroll.success`** guard test spirit for the new transaction).

    Optional: assert **`invalid_code`** path does **not** insert **`user_mfa_credentials`** when **`MFA.confirm_enrollment`** returns **`{:error, :invalid_code}`** — only if stable with current schemas.
  </action>
  <acceptance_criteria>
    - `grep -nF 'insert_failed' test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
    - `grep -nF 'mfa.enroll.failure' test/sigra/mfa_audit_atomicity_test.exs` exits **0** with **≥ 1** line in a **`test \"`** block added by this phase.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs --trace` exits **0** when debugging ordering issues.
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
- `MIX_ENV=test mix compile --warnings-as-errors` exits **0**.
</verification>

<success_criteria>
- **AUD-04-021** implemented with **`Multi` + `log_multi_safe`** + tests; **020** success invariant unchanged; **022** default unchanged (**066-CONTEXT**).
</success_criteria>

## PLANNING COMPLETE
