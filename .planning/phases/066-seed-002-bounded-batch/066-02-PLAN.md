---
phase: 66
plan: 02
type: execute
wave: 2
depends_on:
  - "066-01-PLAN.md"
files_modified:
  - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
  - .planning/phases/09-audit-logging/09-VERIFICATION.md
autonomous: true
requirements:
  - AUD-09
must_haves:
  truths:
    - "**`44-AUD-04-INVENTORY.md`** rows **AUD-04-020..022** mechanism + notes match **`lib/sigra/mfa.ex`** after **066-01**."
    - "**`09-VERIFICATION.md`** C-1 table rows **AUD-04-020..022** match merged mechanisms (**T1** vs honest **T2** / **EX-44-02** for **022**)."
  artifacts:
    - path: ".planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md"
      provides: "AUD-04 traceability"
    - path: ".planning/phases/09-audit-logging/09-VERIFICATION.md"
      provides: "C-1 honesty (**066-CONTEXT D-07**)"
nyquist_compliant: false
---

<objective>
Close **066-CONTEXT D-07**: refresh **AUD-04-020..022** in **`44-AUD-04-INVENTORY.md`** and **`09-VERIFICATION.md`** so C-1 truth matches **`confirm_enrollment/5`** post–**066-01** (**AUD-09** docs gate).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-66-04 | Repudiation | Matrix claims **`log_safe`** post-txn for **020** while code uses **`log_multi_safe`** inside enrollment **`Multi`** | high | Grep **`lib/sigra/mfa.ex`** before editing rows |
| T-66-05 | Information disclosure | Accidental secrets in planning docs | low | Text-only inventory edits |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Update 44-AUD-04-INVENTORY for confirm_enrollment 020..022</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - lib/sigra/mfa.ex (`**confirm_enrollment/5**`)
    - .planning/phases/066-seed-002-bounded-batch/066-CONTEXT.md
    - .planning/phases/066-seed-002-bounded-batch/066-RESEARCH.md
  </read_first>
  <action>
    For inventory rows **AUD-04-020**, **AUD-04-021**, **AUD-04-022**:

    1. Set **mechanism** column to match **`grep -nE 'log_multi_safe|log_safe' lib/sigra/mfa.ex`** inside **`confirm_enrollment/5`** for each action (**`mfa.enroll.success`**, **`mfa.enroll.failure`** / **`insert_failed`**, **`mfa.enroll.failure`** / **`invalid_code`**).

    2. Add a short **Phase 66** note (footnote or **Note** row) citing **AUD-09** and date **2026-04-23**, mirroring the **061** inventory note style.

    3. For **022**, if code remains **`log_safe`**, inventory must **not** claim full **T1** — reference **`EX-44-02`** / **tier 9** honestly per **`066-CONTEXT D-02`**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'Phase 66' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` exits **0**.
    - `grep -nE 'AUD-04-020|AUD-04-021|AUD-04-022' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` shows **three** lines whose mechanism text includes **`log_multi_safe`** or explicit **`log_safe`** consistent with **`mfa.ex`** (post–**066-01**).
  </acceptance_criteria>
  <verify>
    `test -f .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Update 09-VERIFICATION C-1 rows AUD-04-020..022</name>
  <files>.planning/phases/09-audit-logging/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-VERIFICATION.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - lib/sigra/mfa.ex
  </read_first>
  <action>
    In the **C-1** subsection listing **AUD-04-020..022**:

    1. Update **mechanism** and **verdict** (and **tier** if present) so they match **`066-01`** merged code and honest **T2** labeling for **022**.

    2. Ensure the **evidence** / test pointer column still references **`test/sigra/mfa_audit_atomicity_test.exs`** (or add **`mfa.ex`** line pointer if project convention allows).

    3. Do **not** edit **`09-03-SUMMARY.md`** (**066-CONTEXT D-08** — **phase 67**).
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-020' .planning/phases/09-audit-logging/09-VERIFICATION.md` shows a table row whose mechanism substring matches **`lib/sigra/mfa.ex`** for **`mfa.enroll.success`** (**`log_multi_safe`** inside enrollment **`Multi`**).
    - `grep -nF 'AUD-04-021' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0** with mechanism reflecting **`log_multi_safe`** follow-up transaction after **066-01** (not bare **`log_safe`** for **`insert_failed`**).
    - `grep -nF 'AUD-04-022' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `grep -cF 'AUD-04-02' .planning/phases/09-audit-logging/09-VERIFICATION.md` returns **≥ 3**.
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0** (regression guard after doc wave).
- `MIX_ENV=test mix compile --warnings-as-errors` exits **0**.
</verification>

<success_criteria>
- **AUD-09** documentation gate satisfied: **C-1** and inventory honest for **020..022** after code merge.
</success_criteria>

## PLANNING COMPLETE
