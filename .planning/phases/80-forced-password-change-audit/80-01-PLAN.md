---
phase: 80
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/account.ex
  - test/sigra/account_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-17-01
  - AUD-17-02
  - AUD-17-03
must_haves:
  truths:
    - "**`Sigra.Account.clear_password_change_requirement/3`** co-fates **`must_change_password: false`** with **`account.password_change`** (**`metadata: %{forced: true}`**) when **`:audit_schema`** is set, using the same **`Multi` + `log_multi_safe` + `finish_audit_multi`** pattern as **`change_password/5`** / **`set_password/4`**."
    - "**`audit_forced_password_change/2`** carries **`@deprecated`** with a message naming **`clear_password_change_requirement/3`**; **CHANGELOG** warns hosts not to call both for the same completion (**AUD-17-02**)."
    - "**`test/sigra/account_audit_atomicity_test.exs`** proves happy path + **`CHECK`** rollback for the forced-clear atomic path (**AUD-17-03**)."
  artifacts:
    - path: "lib/sigra/account.ex"
      provides: "Atomic forced-clear API + deprecation"
    - path: "test/sigra/account_audit_atomicity_test.exs"
      provides: "Postgres atomicity receipts"
nyquist_compliant: false
---

<objective>
Implement **AUD-17-01** / **AUD-17-02** / **AUD-17-03**: add **`Sigra.Account.clear_password_change_requirement(repo, user, opts)`**, deprecate standalone **`audit_forced_password_change/2`** for the completion path now covered by **`Multi`**, refresh the **D-26** dispatch comment block, and extend **`account_audit_atomicity_test.exs`** with happy + fault-injection tests mirroring existing **`change_password`** **`CHECK`** patterns.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-80-01 | Tampering | User flag cleared while audit insert fails — durable state diverges from audit trail | high | **`Ecto.Multi`** + **`log_multi_safe`** in one **`finish_audit_multi`**; **`CHECK`** test proves rollback |
| T-80-02 | Repudiation | Hosts call deprecated **`log_safe`** helper after the atomic API — duplicate **`account.password_change`** rows | medium | **`@deprecated`** message + **CHANGELOG** explicit “do not pair” |
| T-80-03 | Information disclosure | N/A for this slice | low | — |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add clear_password_change_requirement/3 and deprecate audit_forced_password_change/2</name>
  <files>lib/sigra/account.ex</files>
  <read_first>
    - lib/sigra/account.ex (entire file — especially **`change_password/5`** ~246–279, **`set_password/4`** ~284–318, private helpers **`audit_enabled?`**, **`password_change_scope_kw`**, **`finish_audit_multi`**, **`audit_forced_password_change/2`** ~485–505, dispatch comment ~37–49)
    - lib/sigra/account/password_change.ex (`clear_force_change/2` ~117–128)
    - .planning/phases/80-forced-password-change-audit/80-CONTEXT.md (D-80-01..D-80-03)
  </read_first>
  <action>
    1. Add **`@doc`** + **`@spec`** public **`clear_password_change_requirement(repo, user, opts)`** **`@doc since:`** appropriate next patch per semver:
       - If **`audit_enabled?(opts)`** is **false**: delegate to **`PasswordChange.clear_force_change(repo, user)`** (return shape unchanged: **`{:ok, user}`** / **`{:error, changeset}`**).
       - If **true**: build **`Multi.new()`** → **`Multi.run(:domain, fn r, _ -> PasswordChange.clear_force_change(r, user) end)`** → **`Sigra.Audit.log_multi_safe("account.password_change", Keyword.merge(audit_repo_opts(repo, opts), Keyword.merge(scope_kw, actor_id: user.id, target_id: user.id, metadata: %{forced: true})))`** where **`scope_kw = password_change_scope_kw(opts, user)`** (same as **`change_password`** branch). Pipe through **`finish_audit_multi(repo, multi)`** and map success to **`{:ok, u}`** / domain errors like **`change_password`** (**`{:error, :domain, reason, _}`** → **`{:error, reason}`** or **`%Ecto.Changeset{}`** as appropriate — match **`PasswordChange.clear_force_change`** error shapes exactly).

    2. Replace the dispatch comment lines ~37–49 so they list **`clear_password_change_requirement`** alongside other **`Multi` + `log_multi_safe`** entrypoints when audit is on, and state **`audit_forced_password_change/2`** is **deprecated** (legacy **`log_safe`** only — do not pair with the new API).

    3. Immediately **above** **`def audit_forced_password_change`**, add **`@deprecated`** attribute with message: **`"Use clear_password_change_requirement/3 when :audit_schema is configured; do not call this function for the same forced-clear completion or you may duplicate audit rows."`** Keep the existing **`log_safe`** implementation body for backward compatibility during the deprecation window (**80-CONTEXT D-80-02**).
  </action>
  <acceptance_criteria>
    - `grep -nF 'def clear_password_change_requirement' lib/sigra/account.ex` exits **0**.
    - `grep -nF 'metadata: %{forced: true}' lib/sigra/account.ex` matches **at least one** line inside the new **`log_multi_safe`** call path (forced-clear branch).
    - `grep -nF '@deprecated' lib/sigra/account.ex` exits **0** and the next function definition line contains **`audit_forced_password_change`**.
    - `grep -nF 'clear_password_change_requirement' lib/sigra/account.ex` matches the dispatch comment block (**≥ 2** occurrences: comment + **`def`**).
  </acceptance_criteria>
  <verify>
    `mix compile` exits **0** from project root.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Postgres atomicity tests for clear_password_change_requirement/3</name>
  <files>test/sigra/account_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/account_audit_atomicity_test.exs (full file — copy structure from **`rolls back change_password when audit insert is rejected`** ~236–283)
    - lib/sigra/account.ex (new function signatures)
  </read_first>
  <action>
    1. Add **`test "clear_password_change_requirement persists must_change_password false and account.password_change audit together"`**: insert user with **`must_change_password: true`**, call **`Account.clear_password_change_requirement(repo, user, base_opts(repo))`**, assert reloaded user has **`must_change_password == false`**, assert **`count_where(..., "action = 'account.password_change'") == 1`**, and optionally assert metadata contains **`forced`** truth (if test schema exposes JSON query — otherwise assert action count only).

    2. Add **`test "rolls back clear_password_change_requirement when audit insert is rejected by database guard"`**: inside **`try`/`after`**, **`ALTER TABLE audit_events ADD CONSTRAINT ... CHECK (action <> 'account.password_change')`** (use a **unique** constraint name e.g. **`account_audit_forced_clear_guard`**), insert **`must_change_password: true`** user, **`assert_raise Ecto.ConstraintError`**, assert user still **`must_change_password == true`** and **`count(audit_events)==0`**, **`after`** **`DROP CONSTRAINT IF EXISTS`**.

    3. When audit is **disabled** (**`base_opts` without `:audit_schema`** or pass **`audit_schema: nil`** per existing patterns if any — else **`Keyword.drop(base_opts(repo), [:audit_schema])`**), assert **`clear_password_change_requirement`** still returns **`{:ok, _}`** and **no** audit row (if repo still has empty **`audit_events`** table — count **0**).
  </action>
  <acceptance_criteria>
    - `grep -nF 'clear_password_change_requirement' test/sigra/account_audit_atomicity_test.exs` exits **0** with **≥ 3** matches (test names / calls).
    - `grep -nF 'account_audit_forced_clear_guard' test/sigra/account_audit_atomicity_test.exs` exits **0** OR the chosen unique constraint name appears in both **`ADD`** and **`DROP`** clauses.
    - File contains substring **`must_change_password: true`** in the new tests’ insert attrs.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/account_audit_atomicity_test.exs` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/account_audit_atomicity_test.exs test/sigra/account/password_change_test.exs`
</verification>

<success_criteria>
- **AUD-17-01** / **AUD-17-02** / **AUD-17-03** satisfied in **`lib/`** + **`test/sigra/account_audit_atomicity_test.exs`** before planning-truth plan **02**.
</success_criteria>

## PLANNING COMPLETE
