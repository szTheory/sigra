---
phase: 80
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
  - .planning/phases/09-audit-logging/09-VERIFICATION.md
  - .planning/phases/09-audit-logging/09-03-SUMMARY.md
  - CHANGELOG.md
autonomous: true
requirements:
  - AUD-17-04
must_haves:
  truths:
    - "**44-AUD-04-INVENTORY** row **AUD-04-043** documents **`Multi` + `log_multi_safe`** via **`clear_password_change_requirement/3`** (phase **80**) and **EX-44-05** is **closed** / reconciled per inventory rules."
    - "**09-VERIFICATION.md** C-1 row **043** reads **T1** with mechanism **`Multi` + `log_multi_safe`** and evidence **`test/sigra/account_audit_atomicity_test.exs`**."
    - "**09-03-SUMMARY.md** notes phase **80** / **AUD-17** bounded closure for **043**."
    - "**`CHANGELOG.md` `[Unreleased]`** documents **v1.17** / **AUD-04-043** atomic forced-clear + deprecation guidance."
  artifacts:
    - path: ".planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md"
      provides: "AUD-04-043 mechanism + EX-44-05 honesty"
    - path: ".planning/phases/09-audit-logging/09-VERIFICATION.md"
      provides: "C-1 row 043 T1 receipt"
nyquist_compliant: false
---

<objective>
Close **AUD-17-04**: align **44-AUD-04-INVENTORY**, **09-VERIFICATION** C-1 **043**, **09-03-SUMMARY**, and **`CHANGELOG` [Unreleased]** with shipped **`clear_password_change_requirement/3`** + tests from **80-01-PLAN**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-80-04 | Repudiation | Planning matrices still claim **`log_safe` T2** for **043** while **`lib/`** is **T1** | high | Update mechanism + verdict cells + grep log block if required by inventory conventions |
| T-80-05 | Information disclosure | Stale **EX-44-05** language implies unresolved exception | medium | Mark exception **closed** with pointer to **phase 80** + **`lib/sigra/account.ex`** |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Refresh 44-AUD-04-INVENTORY row AUD-04-043 + EX-44-05 appendix</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md (row **043**, **EX-44-05**, **`## Grep log`**)
    - lib/sigra/account.ex (post-**80-01** implementations)
    - .planning/phases/80-forced-password-change-audit/80-CONTEXT.md
  </read_first>
  <action>
    1. Update table row **AUD-04-043**: **`mechanism today`** → **`Multi` + `log_multi_safe`** (via **`Sigra.Account.clear_password_change_requirement/3`**); adjust **`notes`** / **`Phase`** columns to **phase 80**; ensure **`log_safe`** is not claimed as the live mechanism for the paired write path.

    2. Update **EX-44-05** appendix row: status text must reflect **closure** — paired **`Ecto`** write + atomic audit shipped in-library (**phase 80**), not a future trigger.

    3. If the file’s **`## Grep log`** fenced block is stale vs **`lib/sigra/account.ex`**, replace its body with fresh output of:

       `rg -n "Sigra\\.Audit\\.(log_safe|log_multi_safe)" lib/sigra/account.ex`

       (paste full **`text`**-fenced output from project root).
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-043' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` matches a line containing **`Multi` + `log_multi_safe`**.
    - `grep -nF 'EX-44-05' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` matches a line containing **`80`** or **`phase 80`** or **`closed`** (case-insensitive ok).
  </acceptance_criteria>
  <verify>
    `wc -l .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` returns line count **> 30**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Update 09-VERIFICATION C-1 row 043</name>
  <files>.planning/phases/09-audit-logging/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-VERIFICATION.md (search **`AUD-04-043`**)
    - test/sigra/account_audit_atomicity_test.exs (test names for evidence string)
  </read_first>
  <action>
    1. Replace the **markdown table line** for **AUD-04-043** (~line 82) so **mechanism** reads **`Multi` + `log_multi_safe`** (**`clear_password_change_requirement/3`**), **verdict** **T1** referencing **phase 80** / **AUD-17**, **evidence** **`test/sigra/account_audit_atomicity_test.exs`; `lib/sigra/account.ex`**, **tier** stays **7** unless **REQUIREMENTS** mandate otherwise.

    2. Edit the **footnote paragraphs** under the C-1 table that still say **043** is **`log_safe`** / **EX-44-05** open — e.g. lines starting **`**Phase 79`**, **`**Phase 78`** — replace with text stating **043** closed in **phase 80** (**AUD-17**), **`EX-44-05`** satisfied, while keeping historical **phase 78**/**79** facts for other rows intact.
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-043' .planning/phases/09-audit-logging/09-VERIFICATION.md` matches **exactly one** pipe-table line containing **`Multi` + `log_multi_safe`**.
    - That same line contains **`80`** (phase pointer).
    - `grep -nF '043 unchanged' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **1** (substring removed or rewritten).
    - `grep -nF '043 stays' .planning/phases/09-audit-logging/09-VERIFICATION.md` exits **1**.
  </acceptance_criteria>
  <verify>
    `grep -cE 'phase[[:space:]]+80|Phase[[:space:]]+80' .planning/phases/09-audit-logging/09-VERIFICATION.md` returns **≥ 1**.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Update 09-03-SUMMARY + CHANGELOG [Unreleased]</name>
  <files>.planning/phases/09-audit-logging/09-03-SUMMARY.md,CHANGELOG.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-03-SUMMARY.md (C-1 verification note paragraph)
    - CHANGELOG.md (`[Unreleased]` section head)
    - .planning/REQUIREMENTS.md (**AUD-17** wording)
  </read_first>
  <action>
    1. In **09-03-SUMMARY.md**, extend the **C-1 verification note** (or add one sentence) stating **043** is **T1** / **`Multi` + `log_multi_safe`** via **`clear_password_change_requirement/3`**, **phase 80**, evidence **`account_audit_atomicity_test.exs`**; note **`audit_forced_password_change/2`** deprecated for that path.

    2. In **`CHANGELOG.md` `[Unreleased]`**, add a bullet under an appropriate subsection (e.g. **Security** or **Changed**) describing: atomic forced password-change clear + audit; new **`Sigra.Account.clear_password_change_requirement/3`**; **`@deprecated`** on **`audit_forced_password_change/2`**; “do not call both.”
  </action>
  <acceptance_criteria>
    - `grep -nF '043' .planning/phases/09-audit-logging/09-03-SUMMARY.md` matches a line containing **`80`** or **`phase 80`**.
    - `grep -nF 'clear_password_change_requirement' CHANGELOG.md` exits **0** **or** `grep -nF 'AUD-04-043' CHANGELOG.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `grep -nF '[Unreleased]' CHANGELOG.md | head -1` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- Grep spot-checks in **acceptance_criteria** for each task.
- Optional: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/account_audit_atomicity_test.exs` if **`lib/`** changed in same PR as docs (should already be green after plan **01**).
</verification>

<success_criteria>
- **AUD-17-04** complete — planning truth matches **`lib/`** for **043** / **EX-44-05**.
</success_criteria>

## PLANNING COMPLETE
