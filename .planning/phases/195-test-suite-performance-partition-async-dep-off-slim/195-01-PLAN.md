---
phase: 195-test-suite-performance-partition-async-dep-off-slim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - test/sigra/optional_deps_test.exs
  - test/sigra/application_forwarders_test.exs
  - test/sigra/audit/forwarders/noop_test.exs
  - test/sigra/workers/audit_forward_test.exs
  - test/sigra/doctor_test.exs
  - test/sigra/mix/tasks/doctor_task_test.exs
  - test/sigra/config_forwarders_test.exs
  - test/sigra/auth_plain_map_regression_test.exs
  - test/sigra/passkeys/rate_limit_test.exs
  - guides/recipes/testing.md
  - guides/recipes/local-development.md
autonomous: true
requirements: [TEST-02, TEST-03]
tags: [ci, testing, elixir, async, threadline-guard]

must_haves:
  truths:
    - "mix test --only threadline_guard selects exactly the guard modules and runs (nonzero count); zero matches would fail RED"
    - "MIX_ENV=test mix sigra.dep_off reproduces the CI dep-off lane locally with one command (D-14)"
    - "The two named async candidates run async: true and stay green with no new flake"
    - "An async-safety checklist exists in the testing guide so contributors can decide async: correctly (D-17)"
  artifacts:
    - path: "mix.exs"
      provides: "sigra.dep_off alias (D-14)"
      contains: "sigra.dep_off"
    - path: "test/sigra/optional_deps_test.exs"
      provides: "D-13(b) threadline_available?/0 == false canary, tagged"
      contains: "threadline_guard"
    - path: "guides/recipes/testing.md"
      provides: "Async-safety checklist (D-17 deliverable)"
      contains: "async-safe"
  key_links:
    - from: "mix.exs sigra.dep_off alias"
      to: "test --only threadline_guard"
      via: "alias final step selects the tagged subset"
      pattern: "only threadline_guard"
    - from: "guard test modules"
      to: "threadline_guard tag"
      via: "moduletag picked up by mix test --only"
      pattern: "@moduletag :threadline_guard"
---

<objective>
Lay the test-tree, build-alias, and docs foundations that the ci.yml dep-off lane rewrite (Plan 03) depends on, plus the safe async conversions and the lasting async-safety deliverable.

This plan does NOT touch `.github/workflows/ci.yml`. It produces the `:threadline_guard` tag set, the `mix sigra.dep_off` alias, the two async flips, and the async-safety checklist. The dep-off lane (Plan 03) calls the alias, so the alias and the tags MUST exist first (D-14 dependency).

Purpose: TEST-02 coverage selection (tag-based, fail-red-on-zero-match per D-10/D-11) and TEST-03 async cleanup plus the checklist (D-15/D-17) are independent of the CI topology change and land early.
Output: `mix.exs` alias, `:threadline_guard` on 7 guard modules, 2 `async: true` flips, async-safety checklist plus a dep-off repro note in docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-CONTEXT.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-RESEARCH.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-VALIDATION.md
</context>

<artifacts_produced>
New symbols / files this plan introduces (downstream plans plus verification depend on these exact names):
- `:threadline_guard` ExUnit moduletag — added to 7 guard modules.
- `mix sigra.dep_off` alias — in `mix.exs` `aliases/0`. Steps in order: `deps.unlock threadline`, `deps.clean threadline --build`, `compile --warnings-as-errors --no-deps-check`, `test --only threadline_guard --no-deps-check`.
- Async-safety checklist section — in `guides/recipes/testing.md`.
- Dep-off local-repro note (`MIX_ENV=test mix sigra.dep_off`) — in `guides/recipes/local-development.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Tag the 7 threadline-absent guard modules with the :threadline_guard moduletag (TEST-02 / D-10, D-11, D-13)</name>
  <read_first>
    - test/sigra/audit/forwarders/threadline_test.exs (lines 12-13 — the requires_threadline moduletag idiom this mirrors; this file MUST KEEP requires_threadline and MUST NOT gain threadline_guard, per D-12)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md (lines 188-219 — the exact module set plus D-13 clause map)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-RESEARCH.md (lines 260-273 — D-13 coverage map; A1 inspection note for phase_148)
  </read_first>
  <files>test/sigra/optional_deps_test.exs, test/sigra/application_forwarders_test.exs, test/sigra/audit/forwarders/noop_test.exs, test/sigra/workers/audit_forward_test.exs, test/sigra/doctor_test.exs, test/sigra/mix/tasks/doctor_task_test.exs, test/sigra/config_forwarders_test.exs</files>
  <action>
Add a single moduletag line for `:threadline_guard` directly under the `use ExUnit.Case` line in each of these 7 modules, mirroring the existing requires_threadline idiom at threadline_test.exs:13. The 7 modules and their D-13 coverage role:
- test/sigra/optional_deps_test.exs (under `use ExUnit.Case, async: true` at line 2) — D-13(b) threadline_available?/0 returns false canary.
- test/sigra/application_forwarders_test.exs (under `use ExUnit.Case, async: false` at line 2) — D-13(c) boot degrade path, exactly one Logger.warning. Keep this module async: false (it mutates Application env, per D-16).
- test/sigra/audit/forwarders/noop_test.exs (under `use ExUnit.Case, async: true` at line 2) — D-13(d) Noop fallback dispatch, no apply/3 crash.
- test/sigra/workers/audit_forward_test.exs (under `use ExUnit.Case, async: true` at line 6) — D-13(d) worker dispatch path.
- test/sigra/doctor_test.exs (under `use ExUnit.Case, async: true` at line 2) — D-13(e) doctor reports threadline false.
- test/sigra/mix/tasks/doctor_task_test.exs (under `use ExUnit.Case` at line 2) — D-13(e) doctor task in-process.
- test/sigra/config_forwarders_test.exs (under `use ExUnit.Case, async: true` at line 2) — supporting forwarder config cascade.

Do NOT tag test/sigra/audit/forwarders/threadline_test.exs (keeps requires_threadline; needs the dep PRESENT, per D-12). Do NOT tag test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs: A1 inspection confirms it only passes a threadline-false value as an injected dep_off_opts keyword to Mix.Tasks.Sigra.Doctor.run_with_opts (a DX/doctor-render assertion), not a genuine dep-absent guard path — the 7 modules above cover all of D-13(a–e) without it.

Do not alter any other line in these files; this is a pure one-line-per-module tag add.
  </action>
  <verify>
    <automated>test "$(grep -rl 'threadline_guard' test/ | sort | tr '\n' ',')" = "test/sigra/application_forwarders_test.exs,test/sigra/audit/forwarders/noop_test.exs,test/sigra/config_forwarders_test.exs,test/sigra/doctor_test.exs,test/sigra/mix/tasks/doctor_task_test.exs,test/sigra/optional_deps_test.exs,test/sigra/workers/audit_forward_test.exs," && echo TAGSET_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl threadline_guard test/` lists exactly the 7 modules above and nothing else (NOT threadline_test.exs, NOT phase_148_*).
    - `grep -c requires_threadline test/sigra/audit/forwarders/threadline_test.exs` is still nonzero (D-12 preserved) and that file contains no threadline_guard.
    - `grep -c 'threadline_guard' test/sigra/planning/phase_148_evaluator_funnel_and_first_run_dx_test.exs` returns 0.
  </acceptance_criteria>
  <done>The 7 guard modules carry the :threadline_guard moduletag; threadline_test.exs is untouched (D-12); phase_148 is excluded (A1).</done>
</task>

<task type="auto">
  <name>Task 2: Add the mix sigra.dep_off alias and flip the 2 verified-safe async modules (TEST-02 / D-14, TEST-03 / D-15)</name>
  <read_first>
    - mix.exs (lines 137-151 — the aliases/0 keyword-list idiom; add the new entry inside this list)
    - test/sigra/auth_plain_map_regression_test.exs (line 28 async: false; StubRepo at ~80-90 stores rows in the process dictionary — process-local, SAFE to flip)
    - test/sigra/passkeys/rate_limit_test.exs (line 2 async: false; RecordingLimiter at ~17-35 uses the process dictionary — process-local, SAFE to flip)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md (lines 164-184 alias; 223-247 the two flips plus must-stay-serial set)
  </read_first>
  <files>mix.exs, test/sigra/auth_plain_map_regression_test.exs, test/sigra/passkeys/rate_limit_test.exs</files>
  <action>
Two edits:

(1) D-14 — add a `sigra.dep_off` alias to the `aliases/0` keyword list in mix.exs (alongside ci.install_golden / test.db). The alias is a list of four mix task strings, in this exact order: `deps.unlock threadline`, then `deps.clean threadline --build`, then `compile --warnings-as-errors --no-deps-check` (the D-09 load-bearing compile-without-dep proof, kept), then `test --only threadline_guard --no-deps-check` (the D-10 slimmed run). Add a short comment noting the local repro is `MIX_ENV=test mix sigra.dep_off` and that the alias assumes deps are already compiled (the CI lane compiles deps before calling it). Match the existing keyword-list formatting.

(2) D-15 — flip both named async candidates from async: false to async: true: change the `use ExUnit.Case, async: false` header to `use ExUnit.Case, async: true` at test/sigra/auth_plain_map_regression_test.exs:28 and at test/sigra/passkeys/rate_limit_test.exs:2. Both are verified process-local-only (process-dictionary stubs, no Application/System env, no named ETS, no global telemetry — they pass the D-17 checklist). Do NOT flip any must-stay-serial module (D-16): Sigra.Install.*, Sigra.UpgradeIntegrationTest, durable-DDL checkout_repo!/unboxed_run tests, global telemetry-handler tests, or Application.put_env / System.put_env mutators (including application_forwarders_test.exs).

Leave the sandbox/pool config UNCHANGED (D-18): `Sigra.Test.PostgresCase` already uses `shared: not tags[:async]` (async-correct under partitioning) and `pool_size: 4` stays — do not touch postgres_case.ex or postgres_test_repo.ex. Do NOT proactively split any serial module (D-19): the serial modules are serial for DDL/telemetry/subprocess reasons, not size, so splitting yields two still-serial modules. Per D-20 this async audit is a small, bounded cleanup (the checklist is the real deliverable) — flip only inspection-verified-safe modules and stop; do not over-invest.
  </action>
  <verify>
    <automated>grep -q 'sigra.dep_off' mix.exs && grep -Eq 'use ExUnit.Case, async: true' test/sigra/auth_plain_map_regression_test.exs && grep -Eq 'use ExUnit.Case, async: true' test/sigra/passkeys/rate_limit_test.exs && MIX_ENV=test mix test test/sigra/auth_plain_map_regression_test.exs test/sigra/passkeys/rate_limit_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sigra.dep_off' mix.exs` is nonzero and the alias lists the four tasks in order, ending with `test --only threadline_guard --no-deps-check`.
    - Both async candidates show `use ExUnit.Case, async: true` (no remaining `async: false` on their header lines).
    - `MIX_ENV=test mix test` on the two flipped files passes (0 failures); rerun under `--repeat-until-failure 20` locally to confirm zero new flake.
    - application_forwarders_test.exs is still `async: false` (D-16 serial set untouched).
  </acceptance_criteria>
  <done>mix.exs carries the sigra.dep_off alias; the two named modules are async: true and green; the serial set is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Document the async-safety checklist and the dep-off one-command repro (TEST-03 / D-17, TEST-02 / D-14)</name>
  <read_first>
    - guides/recipes/testing.md (lines 220-231 — the Pitfalls section; add the checklist as a new section near it)
    - guides/recipes/local-development.md (lines 32-51 — the Postgres for mix test section; add the dep-off repro note here)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-CONTEXT.md (D-17 checklist criteria; D-14 repro command)
    - test/sigra/audit/forwarders/threadline_test.exs (lines 6-13 — the `# async: false because <reason>` comment convention to standardize)
  </read_first>
  <files>guides/recipes/testing.md, guides/recipes/local-development.md</files>
  <action>
Two doc additions (no code):

(1) D-17 — add an async-safety checklist section to guides/recipes/testing.md (a new H2, e.g. "Is this test allowed to be async: true?", placed near the Pitfalls section). State the rule: a module is async-safe ONLY if it touches NO shared mutable global state — no Application/System env, no :persistent_term, no named ETS, no named/singleton processes, no global telemetry handlers, no filesystem/cwd/temp-dir/archive mutation; does DB access sandboxed-only (no durable un-sandboxed DDL via checkout_repo!/unboxed_run); uses private-mode Mox only (no set_mox_global); and keeps all state process-local (process dictionary, message passing). Standardize the convention that any `async: false` module carries a short `# async: false because <reason>` comment (cite the existing threadline_test.exs example). Note partitioning does not change this rule (each shard is an independent VM/connection against its own PG container).

(2) D-14 — add a dep-off one-command repro note to guides/recipes/local-development.md (in or just after the "Postgres for mix test" section): document `MIX_ENV=test mix sigra.dep_off` as the exact local reproduction of the CI dep-off lane (it unlocks/cleans :threadline, compiles with --warnings-as-errors --no-deps-check, and runs `mix test --only threadline_guard`), and that it requires a live test Postgres like the rest of the suite.

Keep both additions consistent with each guide's existing heading style and voice.
  </action>
  <verify>
    <automated>grep -qi 'async-safe' guides/recipes/testing.md && grep -q 'set_mox_global' guides/recipes/testing.md && grep -q 'sigra.dep_off' guides/recipes/local-development.md</automated>
  </verify>
  <acceptance_criteria>
    - testing.md contains a checklist section naming the async-safety rule and the global-state items (Application/System env, named ETS, global telemetry, set_mox_global) and the `# async: false because <reason>` convention.
    - local-development.md documents `MIX_ENV=test mix sigra.dep_off` as the dep-off local repro.
    - No source or ci.yml file is touched by this task.
  </acceptance_criteria>
  <done>The async-safety checklist (D-17 deliverable) lives in the testing guide and the dep-off repro is documented in the local-dev guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI required-check signal -> merge gate | The dep-off lane's coverage selection must not silently lose guard coverage (integrity of the test signal). No app-layer input boundary exists in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-01 | Repudiation | `:threadline_guard` tag selection (TEST-02) | mitigate | Tag-based `mix test --only` fails RED on zero matches (verified `mix help test`); a dropped/renamed tag turns CI red, never silently green. Greppable invariant enforced in Task 1 verify. |
| T-195-02 | Tampering | async flips introducing flake (TEST-03) | mitigate | Only process-local-verified modules flipped (D-15); must-stay-serial set (D-16) untouched; zero-new-flake checked via `--repeat-until-failure` and green CI. |
| T-195-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new external dependency introduced. No legitimacy gate required. |
</threat_model>

<verification>
- `grep -rl threadline_guard test/` lists exactly the 7 guard modules (not threadline_test.exs, not phase_148).
- `MIX_ENV=test mix test --only threadline_guard` runs a nonzero count and is green (covers D-13 b–e in-process; the compile proof D-13a is a CI step exercised in Plan 03).
- `MIX_ENV=test mix sigra.dep_off` runs end-to-end locally (requires test Postgres).
- The two flipped modules are `async: true` and green; `application_forwarders_test.exs` remains `async: false`.
- testing.md has the async-safety checklist; local-development.md has the dep-off repro command.
</verification>

<success_criteria>
- TEST-02 foundation: `:threadline_guard` tag set + `mix sigra.dep_off` alias exist and select the right modules with the fail-red property.
- TEST-03: 2 safe async flips green with no new flake; serial set unchanged; async-safety checklist documented (the D-17 deliverable).
- Zero ci.yml changes in this plan (topology change is Plans 02/03).
</success_criteria>

<output>
Create `.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-01-SUMMARY.md` when done.
</output>
