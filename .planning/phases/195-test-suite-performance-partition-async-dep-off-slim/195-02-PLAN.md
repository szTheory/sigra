---
phase: 195-test-suite-performance-partition-async-dep-off-slim
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements: [TEST-01]
tags: [ci, github-actions, matrix, partition, required-check]

must_haves:
  truths:
    - "library_tests runs as a 2-leg matrix (library_tests_shard) with per-leg isolated Postgres; both legs run the same partitioned suite"
    - "A single bare required check named 'Library tests' still resolves green (not 'Library tests (1)'/'(2)') and ci-gate reads needs.library_tests.result unchanged (D-02/D-03)"
    - "mix docs --warnings-as-errors runs once on a non-shard compiled job, not per shard (D-07)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "library_tests_shard matrix worker + library_tests thin aggregator + relocated mix docs"
      contains: "library_tests_shard"
  key_links:
    - from: "library_tests aggregator job"
      to: "library_tests_shard matrix job"
      via: "needs.library_tests_shard.result == 'success' gate"
      pattern: "needs.library_tests_shard.result"
    - from: "ci-gate"
      to: "library_tests aggregator"
      via: "needs.library_tests (unchanged id, D-03)"
      pattern: "needs.library_tests.result"
---

<objective>
Partition the `library_tests` lane into a 2-leg matrix worker (`library_tests_shard`) and add a thin name-preserving aggregator job that keeps the protected required check `Library tests` byte-identical, so the merge gate and `ci-gate` wiring are untouched. Relocate `mix docs --warnings-as-errors` out of the shard so it runs once.

Purpose: TEST-01 — halve the serial library-test walltime at N=2 with identical quality signal, without orphaning the required check (D-01..D-08). This is the load-bearing ci.yml topology change; it carries the mandatory execution-time ruleset re-read (D-02) and the per-shard measurement gate (RESEARCH Pitfall 1).
Output: rewritten `library_tests` region of ci.yml — worker matrix + aggregator + `mix docs` relocated to a compiled non-shard job.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-CONTEXT.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-RESEARCH.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-VALIDATION.md
@.planning/phases/194-caching-correctness-micro-job-consolidation/194-CONTEXT.md
</context>

<artifacts_produced>
New / changed CI symbols this plan introduces (the aggregator name and job ids are load-bearing — verification and ci-gate depend on them exactly):
- `library_tests_shard` job (id) with `name: Library tests shard ${{ matrix.partition }}`, `strategy.matrix.partition: [1, 2]`, `fail-fast: false`. Per-leg `services.postgres`. Runs `MIX_TEST_PARTITION=${{ matrix.partition }} mix test --partitions 2` plus per-shard `$GITHUB_STEP_SUMMARY` timing on a partition-suffixed log.
- `library_tests` aggregator job (id reused), `name: Library tests` — byte-identical protected required-check name (ruleset 14941512). `needs: [library_tests_shard]`, `if: always()`, single step failing unless `needs.library_tests_shard.result == 'success'`.
- `mix docs --warnings-as-errors` relocated to a non-shard compiled job (fast_checks with a setup-beam+deps prelude, or another already-compiled non-shard job) per D-07.
- `MIX_TEST_PARTITION` env usage in the shard step.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Convert library_tests into the library_tests_shard matrix worker + add the library_tests thin aggregator (TEST-01 / D-01, D-02, D-03, D-04, D-05, D-06)</name>
  <read_first>
    - .github/workflows/ci.yml (lines 172-247 — the current library_tests job to transform in place; lines 1218-1269 — the ci-gate aggregator pattern the new aggregator mirrors)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md (lines 25-127 — the worker matrix header, byte-for-byte cache step, partition-suffixed log, and aggregator skeleton)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-RESEARCH.md (lines 70-96 aggregator mechanism; 149-161 Pitfalls 1-2; 180-235 worker skeleton)
    - .planning/phases/194-caching-correctness-micro-job-consolidation/194-CONTEXT.md (protected required-check names; cache-key shape — keep unchanged, D-06)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
MANDATORY FIRST STEP (D-02 / A2 — execution-time guardrail): run `gh api repos/szTheory/sigra/rulesets/14941512` and confirm the required status check string is still byte-identical `Library tests` before editing. If the ruleset no longer requires exactly `Library tests`, STOP and surface to the user — do not proceed with the rename. Record the confirmation in the SUMMARY.

Then transform the current `library_tests` job (ci.yml:172-247) into TWO jobs:

(1) WORKER — rename the job id `library_tests` -> `library_tests_shard`. Set `name: Library tests shard ${{ matrix.partition }}`. Add `strategy.fail-fast: false` (D-01: one shard failing must not cancel the sibling) and `strategy.matrix.partition: [1, 2]` (D-04: N=2). Keep `needs: release_ref_guard` and the per-leg `services.postgres` block UNCHANGED (D-05 — each matrix leg gets its own container = free DB isolation; static `sigra_test` DB name is fine). Keep the checkout, setup-beam, Install Hex+Rebar, phx_new 1.8.7 archive, and `mix deps.get` steps unchanged. Keep the cache step BYTE-FOR-BYTE (D-06: the `-library-` key and restore-keys are unchanged; the partition is deliberately NOT in the key so both legs share one warm entry). In the test run step, add `MIX_TEST_PARTITION: ${{ matrix.partition }}` to env and change the command to run `mix test --partitions 2` (see Task 2 for the --slowest measurement gate), tee-ing to a partition-suffixed log `/tmp/library_tests_${{ matrix.partition }}.log` so the two legs do not collide. Point the existing `if: always()` `$GITHUB_STEP_SUMMARY` timing/summary steps at that partition-suffixed log. REMOVE the `Check docs build cleanly` step from this job (it relocates in Task 3, D-07).

(2) AGGREGATOR — add a NEW job with id `library_tests`, `name: Library tests` (BYTE-IDENTICAL to ruleset 14941512 — DO NOT EDIT this string), `runs-on: ubuntu-latest`, `needs: [library_tests_shard]`, `if: always()`. Its single step reads `needs.library_tests_shard.result` (which aggregates the whole matrix: success only if every leg succeeded) and exits 1 unless it equals `success`. Mirror the existing ci-gate result-gate shell shape (set -euo pipefail; if not success, echo + exit 1). This restores both the bare `Library tests` required-check context AND the job id `library_tests` that ci-gate.needs references.

Leave `ci-gate` UNCHANGED (D-03): it still lists `library_tests` in needs and resolves `LIBRARY_TESTS: ${{ needs.library_tests.result }}` against the aggregator id. Do NOT add `library_tests_shard` to ci-gate.needs (the aggregator already gates the matrix — adding both double-counts).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']; assert 'library_tests_shard' in j, 'worker missing'; assert j['library_tests_shard']['strategy']['matrix']['partition']==[1,2], 'matrix wrong'; assert j['library_tests_shard']['strategy']['fail-fast'] is False, 'fail-fast must be false'; assert j['library_tests']['name']=='Library tests', 'aggregator name must be byte-identical'; assert j['library_tests']['needs']==['library_tests_shard'], 'aggregator needs'; assert 'library_tests' in j['ci-gate']['needs'] and 'library_tests_shard' not in j['ci-gate']['needs'], 'ci-gate wiring (D-03)'; print('TOPOLOGY_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/szTheory/sigra/rulesets/14941512` confirmed `Library tests` byte-identical BEFORE the edit (recorded in SUMMARY).
    - ci.yml parses as valid YAML; `library_tests_shard` has matrix `partition: [1, 2]` and `fail-fast: false` with its own services.postgres.
    - aggregator job id `library_tests` has `name: Library tests` (exact), `needs: [library_tests_shard]`, `if: always()`, and fails unless `needs.library_tests_shard.result == 'success'`.
    - ci-gate.needs still contains `library_tests` and does NOT contain `library_tests_shard` (D-03).
    - The shard test step sets `MIX_TEST_PARTITION: ${{ matrix.partition }}` and runs `mix test --partitions 2`; the log path is partition-suffixed.
    - The `Check docs build cleanly` step is no longer in the shard job.
  </acceptance_criteria>
  <done>library_tests is a matrix worker + name-preserving aggregator; the protected required check stays bare and green; ci-gate is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the per-shard test command via the --slowest/--trace measurement gate (TEST-01 / RESEARCH Pitfall 1, CACHE-03 discipline applied to TEST-01)</name>
  <read_first>
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-RESEARCH.md (lines 11-13 premise correction; 149-156 Pitfall 1; 318-323 Open Question 1 — the A/B gate)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-CONTEXT.md (D-01 "keep --slowest 10" vs the authoritative-decisions note #1 — treat the per-shard command as a MEASUREMENT gate, not a verbatim copy)
    - .github/workflows/ci.yml (the library_tests_shard test step from Task 1)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Apply the measurement-gate discipline (the same CACHE-03/D-23 ethos) to the per-shard test command. The premise correction (HIGH, verified via `mix help test`): `--slowest` forces `--trace` forces `--max-cases 1`, so keeping `--slowest 10` per shard makes each shard run fully serial within the leg — parallelism across legs only, not within. D-01 says "keep --slowest 10"; the authoritative-decisions note overrides this to a measured choice.

Decision rule for the executor:
- Option (a): keep `mix test --partitions 2 --slowest 10` per shard — simplest, preserves identical per-test timing observability, accepts serial-within-shard. Defensible and the D-01 default.
- Option (b): run `mix test --partitions 2` WITHOUT `--slowest` (within-shard async at max_cases=4) and keep timing via the existing tee + the `$GITHUB_STEP_SUMMARY` log-scrape (which captures the "Finished in / N tests" footer even without `--trace`).

Encode the chosen command in the shard step AND record the A/B evidence in the SUMMARY: run the shard both ways on a throwaway CI run (or local `MIX_TEST_PARTITION=1 mix test --partitions 2` with and without `--slowest`), compare per-shard walltime x the observability survives. Default to Option (a) (keep `--slowest 10`, D-01) UNLESS Option (b) shows a material within-shard speedup with the timing observability still present — in which case adopt (b) and note that per-test timing is dropped on the hot path (kept as a separate diagnostic if needed). Either outcome is acceptable; the requirement is that the choice is MEASURED, not assumed. Add an inline comment in the shard step recording which option was chosen and why.

Warning sign to record: if post-change shard walltime is approximately baseline/N with no extra speedup, the shard is still --trace-serial (expected under Option a); if it drops more than N x, within-shard async was unlocked (Option b paid off).
  </action>
  <verify>
    <automated>grep -q 'mix test --partitions 2' .github/workflows/ci.yml && grep -Eq 'Pitfall 1|measurement|slowest' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - The shard step runs `mix test --partitions 2` (with or without `--slowest 10` per the measured decision).
    - An inline comment in the shard step records which --slowest option was chosen and the measurement rationale.
    - The SUMMARY records the A/B per-shard walltime comparison (the measurement gate was actually run, not skipped).
    - The `$GITHUB_STEP_SUMMARY` timing step still emits a non-empty timing/footer block for the chosen command.
  </acceptance_criteria>
  <done>The per-shard test command is a recorded measured decision (not a silent copy of the baseline); observability survives either way.</done>
</task>

<task type="auto">
  <name>Task 3: Relocate mix docs --warnings-as-errors to a compiled non-shard job (TEST-01 / D-07)</name>
  <read_first>
    - .github/workflows/ci.yml (lines 48-100 — the fast_checks job, which runs pure-shell guards with NO setup-beam/deps/compile today; lines 246-247 — the docs step being relocated)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md (lines 131-141 — the relocation analog + the planner caveat that fast_checks needs a compiled-toolchain prelude or another host)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-CONTEXT.md (D-07 — run-once, never per-shard)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Move `mix docs --warnings-as-errors` so it runs exactly ONCE per CI run, never per shard (D-07). `mix docs` needs a compiled toolchain; `fast_checks` today runs pure-shell guards with no setup-beam, no deps, no compile. Choose ONE host and wire it correctly:

- Preferred: add `mix docs --warnings-as-errors` to `fast_checks` AND add the minimal prelude it needs there (erlef/setup-beam pinned to the same SHA as the rest of ci.yml with version-file `.tool-versions`, the `-library-` deps cache step copied from the library lane for a warm restore, `mix local.hex --force`/`mix local.rebar --force`, `mix deps.get`, and a compile sufficient for ex_doc). Keep the existing pure-shell guard steps intact.
- Alternative (if adding a toolchain to fast_checks is judged too heavy): place the single `mix docs --warnings-as-errors` step on another already-compiled NON-shard job (NOT library_tests_shard, NOT the aggregator) — e.g. an existing example/install job that already has setup-beam + compiled deps — so it runs once. Do NOT leave it in the shard (that is N x cost for one artifact).

Whichever host is chosen, ensure: the docs step is removed from `library_tests_shard` (done in Task 1) and present exactly once elsewhere. Record the chosen host and rationale in the SUMMARY.
  </action>
  <verify>
    <automated>test "$(grep -c 'mix docs --warnings-as-errors' .github/workflows/ci.yml)" -eq 1 && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']; import json; steps=[s for jid,job in j.items() if jid not in ('library_tests_shard',) for s in (job.get('steps') or []) if 'mix docs --warnings-as-errors' in (s.get('run') or '')]; assert steps, 'mix docs must live on a non-shard job'; print('DOCS_RELOCATED_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mix docs --warnings-as-errors' .github/workflows/ci.yml` equals 1 (run-once, D-07).
    - That single docs step lives on a NON-shard job that has a compiled toolchain (setup-beam + deps available in that job).
    - `library_tests_shard` contains no `mix docs` step.
    - The chosen host + rationale recorded in SUMMARY.
  </acceptance_criteria>
  <done>mix docs runs once on a compiled non-shard job; the shard never runs it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Branch-protection ruleset (GitHub-side state) -> merge gate | The required check string `Library tests` is the integrity boundary; a matrix rename that drops the bare context would orphan the gate. No app-layer input boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-03 | Tampering / Elevation | required check silently dropped by matrix rename -> unreviewed merges | mitigate | D-02 aggregator restores the bare `Library tests` context; MANDATORY execution-time re-read of ruleset 14941512 (Task 1 first step) confirms the string before merge. |
| T-195-04 | Tampering | cache poisoning via partition-split keys | mitigate | D-06 keeps one warm `-library-` key shape; the partition is NOT in the key (no split, no doubled cold-compile). |
| T-195-05 | Supply chain | unpinned action introduced for sharding | mitigate | No new action — the matrix is native YAML; all existing actions stay pinned to full SHA. |
| T-195-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no new external dependency. No legitimacy gate required. |
</threat_model>

<verification>
- `gh api repos/szTheory/sigra/rulesets/14941512` shows `Library tests` (re-read at execution; D-02/A2).
- ci.yml parses; `library_tests_shard` is a `[1,2]` matrix with `fail-fast: false`; aggregator `library_tests` (`name: Library tests`) gates on `needs.library_tests_shard.result`.
- ci-gate.needs contains `library_tests` and not `library_tests_shard` (D-03).
- `mix docs --warnings-as-errors` appears exactly once and on a compiled non-shard job (D-07).
- On a PR branch (phase gate): a single bare `Library tests` check resolves (not `(1)`/`(2)`), both shard durations are recorded in `$GITHUB_STEP_SUMMARY`, and `ci-gate` is green.
</verification>

<success_criteria>
- TEST-01: library_tests runs as 2 DB-isolated partitions; the same test set passes across legs; the bare required check is preserved and green; ci-gate unchanged.
- The per-shard command is a recorded measured decision; mix docs runs once.
</success_criteria>

<output>
Create `.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-02-SUMMARY.md` when done.
</output>
