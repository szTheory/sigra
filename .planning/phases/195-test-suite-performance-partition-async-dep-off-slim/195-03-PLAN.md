---
phase: 195-test-suite-performance-partition-async-dep-off-slim
plan: 03
type: execute
wave: 2
depends_on: [195-01, 195-02]
files_modified:
  - .github/workflows/ci.yml
  - guides/recipes/local-development.md
autonomous: true
requirements: [TEST-02, CACHE-03]
tags: [ci, github-actions, dep-off, threadline-guard, runners, runbook]

must_haves:
  truths:
    - "library_tests_dep_off keeps the compile-without-Threadline proof (--warnings-as-errors --no-deps-check) and runs only the tagged guard subset, not the full suite (D-09/D-10)"
    - "The dep-off lane and local repro cannot drift: both go through mix sigra.dep_off (D-14)"
    - "Every CI job stays runs-on: ubuntu-latest; larger runners are NOT adopted (CACHE-03 / D-21)"
    - "A measurement-gate runbook with a before/after table exists (D-23)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "slimmed library_tests_dep_off lane calling the guard-subset run"
      contains: "only threadline_guard"
    - path: "guides/recipes/local-development.md"
      provides: "CACHE-03 measurement-gate runbook (D-23)"
      contains: "measurement"
  key_links:
    - from: "library_tests_dep_off final step"
      to: "threadline_guard tagged subset"
      via: "mix test --only threadline_guard --no-deps-check"
      pattern: "only threadline_guard"
---

<objective>
Slim the `library_tests_dep_off` lane to run only the `:threadline_guard` tagged subset (keeping the compile-without-dep proof), routed so it cannot drift from the local `mix sigra.dep_off` repro, and ship the CACHE-03 measurement-gate runbook that documents why larger runners are not adopted.

This plan touches `.github/workflows/ci.yml` after Plan 02 has rewritten the `library_tests` region (file-ownership dependency), and uses the `:threadline_guard` tag + `mix sigra.dep_off` alias created in Plan 01.

Purpose: TEST-02 — turn the ~14m full-suite dep-off rerun into a <3m targeted guard subset with the fail-red-on-zero-match trust property (D-09..D-14); CACHE-03 — record the larger-runner decision rule and keep every job on free standard runners (D-21..D-23).
Output: rewritten `library_tests_dep_off` final step + the measurement-gate runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-CONTEXT.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-RESEARCH.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-VALIDATION.md
@.planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-01-SUMMARY.md
@.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-02-SUMMARY.md
</context>

<artifacts_produced>
New / changed symbols this plan introduces:
- Slimmed `library_tests_dep_off` final test step — `mix test --only threadline_guard --no-deps-check` (replacing `--exclude requires_threadline`), routed through `mix sigra.dep_off` so CI and local repro share one source of truth (D-14).
- CACHE-03 measurement-gate runbook section — in `guides/recipes/local-development.md`, with the larger-runner decision rule (D-22) and a before/after table template (D-23).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Slim library_tests_dep_off to the guard subset via the sigra.dep_off alias (TEST-02 / D-09, D-10, D-11, D-14)</name>
  <read_first>
    - .github/workflows/ci.yml (lines 249-323 — the current dep-off lane; KEEP the compile-without-Threadline proof step at 305-308, change only the final test step at 309-315)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md (lines 144-184 — the in-place transform + the alias-vs-discrete-steps decision)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-RESEARCH.md (lines 237-248 alias note: the alias assumes deps are compiled; the lane's preceding deps.compile stays a separate step)
    - mix.exs (the sigra.dep_off alias added in Plan 01 — its four steps: deps.unlock/deps.clean/compile-proof/test --only)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Transform the `library_tests_dep_off` lane (ci.yml:249-323) in place. KEEP UNCHANGED: checkout, setup-beam, the dep-off cache step, Install Hex+Rebar, phx_new 1.8.7 archive, `mix deps.get`, the `Compile deps in test env` step (ci.yml:295-298, needed so `--no-deps-check` has deps built on a cache miss), the `Remove :threadline from dep graph` step (deps.unlock/deps.clean), and the `Compile without Threadline (--warnings-as-errors --no-deps-check)` proof step at ci.yml:305-308 — this is the D-09 load-bearing assertion and MUST stay.

Change ONLY the final test run step (ci.yml:309-315): replace `mix test --exclude requires_threadline --no-deps-check` with `mix test --only threadline_guard --no-deps-check` (D-10). This makes the lane run only the tagged guard subset; `--only` fails RED on zero matches (the D-11 trust property) so a dropped/renamed tag turns CI red, never silently green. Rename the step to reflect it runs the guard subset.

Routing-vs-drift (D-14): the lane's discrete unlock/clean/compile-proof/test steps overlap with the four steps inside the `mix sigra.dep_off` alias. Choose ONE of:
- (preferred) Keep the discrete CI steps as-is and just change the final test command to `mix test --only threadline_guard --no-deps-check` — the alias remains the SINGLE SOURCE OF TRUTH for LOCAL repro (`MIX_ENV=test mix sigra.dep_off`), and CI's final command matches the alias's final command verbatim. Add a comment in the lane pointing to the alias as the local-repro equivalent.
- (alternative) Collapse the lane's final unlock+clean+compile+test steps into one `run: mix sigra.dep_off` step (after the separate `mix deps.get` + `mix deps.compile` prelude the alias assumes), dropping the now-redundant discrete steps.
Either keeps CI and local from drifting; pick whichever keeps the lane legible. Do NOT lose the compile-without-dep proof either way.
  </action>
  <verify>
    <automated>grep -q 'only threadline_guard' .github/workflows/ci.yml && ! grep -q 'exclude requires_threadline' .github/workflows/ci.yml && grep -q 'warnings-as-errors --no-deps-check' .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The dep-off final step runs `mix test --only threadline_guard --no-deps-check` (or `mix sigra.dep_off`); `--exclude requires_threadline` no longer appears in ci.yml.
    - The `mix compile --warnings-as-errors --no-deps-check` proof step is still present in the dep-off lane (D-09).
    - ci.yml parses as valid YAML.
    - A comment links the lane to `MIX_ENV=test mix sigra.dep_off` as the local repro (D-14).
  </acceptance_criteria>
  <done>The dep-off lane proves the compile-without-dep path AND runs only the tagged guard subset, sharing the alias as the no-drift source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the CACHE-03 measurement-gate runbook and assert no larger runners (CACHE-03 / D-21, D-22, D-23)</name>
  <read_first>
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-CONTEXT.md (D-21 stay on ubuntu-latest; D-22 decision rule; D-23 runbook shape)
    - .planning/phases/193-baseline-observability-one-line-wins/193-BASELINE.md (the baseline durations the runbook's before/after table diffs against)
    - .planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-PATTERNS.md (lines 263-265 — runbook doc analog: the guides/recipes numbered-runbook format)
    - guides/recipes/local-development.md (existing structure + the dep-off note added in Plan 01)
  </read_first>
  <files>guides/recipes/local-development.md, .github/workflows/ci.yml</files>
  <action>
Two deliverables (D-23 runbook + D-21 assertion):

(1) Add a short "Larger-runner measurement gate" runbook section to guides/recipes/local-development.md (or a sibling guides/recipes doc if the planner/executor judges a dedicated CI-perf doc cleaner — keep it discoverable). The runbook documents the decision rule (D-22): adopt a larger runner ONLY if the job is genuinely un-shardable AND on the critical path, AND Δwall-clock is material (≥30% on that job), AND the run-level wall-clock actually drops (not masked by another pole, e.g. Playwright ~22m), AND the recurring $/min is accepted — otherwise reject. Include the billing fact (D-21): standard runners are free/unlimited on public repos; larger runners are billed per-minute even on public repos and do not consume included minutes, so 2 free standard shards (4 cores, $0/min) strictly dominate one paid 4-core runner for parallelizable work. Provide the procedure: take the baseline from 193-BASELINE.md → A/B one job on a larger label ×3 runs → measure wall-clock + billed-minutes + cache-hit → apply the decision rule → record a before/after table. Include an empty before/after table template (columns: job, runner label, wall-clock, billed-minutes, cache-hit, verdict).

(2) Assert D-21 in CI reality: confirm every job in ci.yml stays `runs-on: ubuntu-latest` (no larger-runner labels adopted this phase). This is a verification assertion, not an edit — do not add any larger-runner label.
  </action>
  <verify>
    <automated>grep -qi 'measurement' guides/recipes/local-development.md && grep -qi 'billed' guides/recipes/local-development.md && test "$(grep -cE '^\s*runs-on:' .github/workflows/ci.yml)" -eq "$(grep -cE '^\s*runs-on:\s*ubuntu-latest' .github/workflows/ci.yml)"</automated>
  </verify>
  <acceptance_criteria>
    - guides/recipes/local-development.md (or a sibling CI-perf recipe) contains the measurement-gate runbook: the D-22 decision rule, the D-21 billing fact, the A/B procedure, and an empty before/after table template.
    - Every `runs-on:` in ci.yml is `ubuntu-latest` (no larger-runner label) — counts of `runs-on:` and `runs-on: ubuntu-latest` are equal.
    - No larger runner was adopted; the runbook records the reject-by-default posture.
  </acceptance_criteria>
  <done>The CACHE-03 runbook exists with the decision rule + before/after template, and every job stays on free standard runners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dep-off coverage selection -> false-green guard signal | The dep-off lane must fail red if guard coverage is dropped/renamed (integrity of the optional-dependency signal). No app-layer input boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-06 | Repudiation | dep-off coverage silently lost on tag rename -> false-green | mitigate | `mix test --only threadline_guard` fails RED on zero matches (verified `mix help test`); the compile-without-dep proof (D-09) is kept as a separate belt-and-suspenders step. |
| T-195-07 | Tampering | drift between CI dep-off lane and local repro | mitigate | Both route through `mix sigra.dep_off` (D-14); CI's final command matches the alias's final command verbatim. |
| T-195-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no new external dependency. No legitimacy gate required. |
</threat_model>

<verification>
- `mix test --only threadline_guard --no-deps-check` is the dep-off final command; `--exclude requires_threadline` is gone.
- The `mix compile --warnings-as-errors --no-deps-check` proof step remains in the dep-off lane (D-09).
- A comment links the lane to `MIX_ENV=test mix sigra.dep_off` (D-14).
- Every `runs-on:` in ci.yml is `ubuntu-latest` (D-21).
- The measurement-gate runbook exists with the decision rule + before/after template (D-23).
- Phase gate (PR branch): the dep-off lane is materially faster than the 13.8m baseline (target <3m) and the fail-red property is demonstrable by a throwaway tag drop.
</verification>

<success_criteria>
- TEST-02: dep-off lane proves the Threadline-absent compile/guard paths via the tagged subset and is materially faster than the prior full-suite rerun; fail-red-on-zero-match preserved.
- CACHE-03: larger runners not adopted; the measurement-gate runbook is shipped.
</success_criteria>

<output>
Create `.planning/phases/195-test-suite-performance-partition-async-dep-off-slim/195-03-SUMMARY.md` when done.
</output>
