---
phase: 185-audit-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/live/admin/design_gallery_live.ex
  - test/example/lib/example_web/router.ex
  - test/sigra/install/design_gallery_isolation_test.exs
  - guides/reference/admin-quality-ledger.md
  - guides/reference/admin-fractal-scorecard.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04
  - INFRA-06

must_haves:
  truths:
    - "Gallery LiveView renders all 13 component boards + MG-1..MG-5 group boards inside the real admin shell at /admin/_design"
    - "Gallery route compiles out in MIX_ENV=test (dev_routes gate)"
    - "D-04 contract guard (ExUnit) fails fast if any *design* artifact appears in priv/templates/sigra.install/"
    - "Quality ledger exists with machine-parseable tier=1 rows for all L1/L2/L3 items"
    - "Fractal scorecard rubric committed with D1-D11 + per-level add-ons"
  artifacts:
    - path: "test/example/lib/example_web/live/admin/design_gallery_live.ex"
      provides: "ExampleWeb.Admin.DesignGalleryLive module; all 13 component boards + MG-1..MG-5 boards; static literal assigns only"
      contains: "import Sigra.Admin.Components"
    - path: "test/example/lib/example_web/router.ex"
      provides: "dev-gated /admin/_design route inside if compile_env(:example, :dev_routes) block"
      contains: "live \"/_design\""
    - path: "test/sigra/install/design_gallery_isolation_test.exs"
      provides: "D-04 ExUnit contract guard; fails if priv/templates/sigra.install/ contains any 'design' path"
    - path: "guides/reference/admin-quality-ledger.md"
      provides: "Machine-parseable tier ledger; 13 L1 + 5 L2 + 6 L3 rows, all tier=1"
    - path: "guides/reference/admin-fractal-scorecard.md"
      provides: "Fractal scorecard rubric; D1-D11 shared + Component/Group/Page/Flow add-ons; Tier 0/1/2 vocabulary"
  key_links:
    - from: "test/example/lib/example_web/router.ex"
      to: "test/example/lib/example_web/live/admin/design_gallery_live.ex"
      via: "live \"/_design\", DesignGalleryLive, :index inside live_session :admin_design_gallery"
      pattern: "DesignGalleryLive"
    - from: "test/example/lib/example_web/live/admin/design_gallery_live.ex"
      to: "lib/sigra/admin/components.ex"
      via: "import Sigra.Admin.Components"
      pattern: "import Sigra.Admin.Components"
    - from: "guides/reference/admin-quality-ledger.md"
      to: "scripts/ci/quality-ledger-monotonic.sh"
      via: "tier column parsed by grep -E '^\\| [a-z]' + awk -F'|' gensub extraction"
      pattern: "^\\| [a-z]"
---

<objective>
Build the gallery LiveView scaffold, D-04 contract guard, quality ledger, and fractal
scorecard rubric — the three reference documents and two new code artifacts that ground
the rest of Phase 185's instruments.

Purpose: These are the foundation instruments that phases 186-192 will use to grade and
record quality. The gallery is the audit surface; the ledger is the grade book; the rubric
is the grading key. Nothing can regress below what gets recorded here.

Output:
- ExampleWeb.Admin.DesignGalleryLive (dev-only, all 13 component boards + MG-1..MG-5 group boards)
- dev-gated /admin/_design route inside the compile_env gate
- D-04 ExUnit contract guard (pure filesystem glob, no Postgres)
- guides/reference/admin-quality-ledger.md (machine-parseable, all tiers=1)
- guides/reference/admin-fractal-scorecard.md (D1-D11 + per-level add-ons)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/phases/185-audit-infrastructure/185-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/185-audit-infrastructure/185-RESEARCH.md
@/Users/jon/projects/sigra/.planning/phases/185-audit-infrastructure/185-PATTERNS.md
@/Users/jon/projects/sigra/.planning/phases/185-audit-infrastructure/185-UI-SPEC.md

<interfaces>
From lib/sigra/admin/components.ex (verified, component signatures for gallery assigns):

  stat_link(href:, label:, value: integer, class: nil, rest: [])
  stat(label:, value: integer, class: nil, rest: [])
  task_card(title:, body:, href:, action:, class: nil, rest: [])
  summary_chip(label:, value: integer, id: nil, icon: nil, value_unit: nil,
    value_suffix: nil, subvalue: nil, help: nil, tone: nil, class: nil, rest: [])
    -- enhanced branch triggered by any optional field present
  applied_chip(label:, remove_href:, class: nil, rest: [])
  empty_state(title:, class: nil, rest: [], inner_block: required slot)
  page_back(return_to:, label:, class: nil, rest: [])
  scope_ribbon(copy:, class: nil, rest: [])
  notice(tone: nil|:ok|:warn|:risk|:info, class: nil, rest: [], inner_block: required slot)
  notice_link(href:, class: nil, rest: [], inner_block: required slot)
    -- must be used INSIDE a notice slot, not standalone
  field_help(id:, label:, class: nil, rest: [], inner_block: required slot)
  skeleton(class: nil, rest: [])
  audit_row(row: %{id, inserted_at, action, action_label, action_badge, actor_label,
    effective_user_label, actor_summary, outcome},
    show_detail: false, show_codes: false, class: nil, rest: [])
    -- outcome != "success" triggers risk tone
    -- action_badge present triggers info badge

From test/sigra/admin/components_test.exs (audit_row row map shapes to mirror):
  @compact_row: id "uuid-1234", action "auth.login.success", action_badge nil, outcome "success"
  @impersonation_row: action_badge "Impersonation", actor_summary "admin@ acting as alice@"
  @failure_row: action "auth.login.failure", outcome "failure"

From test/example/lib/example_web/router.ex lines 172-182 (dev-route gate):
  if Application.compile_env(:example, :dev_routes) do
    scope "/dev" do ... end
    scope "/demo", ExampleWeb do ... end
    # ADD: scope "/admin", ExampleWeb.Admin do ... end
  end

From test/example/lib/example_web/router.ex lines 249-263 (admin live_session shape to clone):
  live_session :admin_global,
    layout: {ExampleWeb.Layouts, :admin},
    on_mount: [
      {ExampleWeb.UserAuth, :ensure_authenticated},
      {Sigra.LiveView.AdminScope,
       [mode: :global, policy: Example.SigraAdminPolicy, login_path: "/users/log_in"]}
    ] do
    ...
  end

From test/sigra/install/template_syntax_test.exs (D-04 guard pattern):
  defmodule Sigra.Install.TemplateSyntaxTest do
    use ExUnit.Case, async: true
    @template_root "priv/templates/sigra.install"
    for path <- Path.wildcard(Path.join([@template_root, "**", "*.ex"])) do ...

From guides/reference/admin-design-contract.md (sibling — Markdown heading/table format to follow)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gallery LiveView + dev-gated router entry (INFRA-01, D-01..D-03)</name>
  <files>
    test/example/lib/example_web/live/admin/design_gallery_live.ex
    test/example/lib/example_web/router.ex
  </files>
  <read_first>
    - test/example/lib/example_web/live/demo/credentials_live.ex — dev-gated LiveView shape; mount/3 static-assign pattern; render/1 bare ~H heredoc (no Layouts.app wrapper); DEV ONLY badge with data-testid
    - test/example/lib/example_web/router.ex lines 172-182 — the if Application.compile_env(:example, :dev_routes) block to extend
    - test/example/lib/example_web/router.ex lines 249-263 — live_session :admin_global shape (layout, on_mount) to clone exactly
    - lib/sigra/admin/components.ex — all 13 function signatures for board assigns (lines 44-724)
    - test/sigra/admin/components_test.exs lines 97-131 — @compact_row/@impersonation_row/@failure_row maps for audit_row board
    - .planning/phases/185-audit-infrastructure/185-PATTERNS.md — board wrapper contract, board-notice canary content, audit_row board content
    - .planning/phases/185-audit-infrastructure/185-UI-SPEC.md — page structure contract, state matrix coverage table
  </read_first>
  <action>
    Create the directory test/example/lib/example_web/live/admin/ (does not exist yet) and write design_gallery_live.ex.

    Module: ExampleWeb.Admin.DesignGalleryLive (NOT Sigra.Admin.Live.* — must stay in example namespace per D-04).
    Preamble: @moduledoc explaining dev-only, no DB queries, all assigns static literals.
    Use macro: use ExampleWeb, :live_view
    Import: import Sigra.Admin.Components (ONLY this import — no alias or import of any Sigra.Admin.* query module).

    mount/3: return {:ok, assign(socket, page_title: "Design Gallery")} — no DB calls.

    render/1: bare ~H""" (no <Layouts.app> wrapper — the admin shell is applied by the router live_session). Top-level element is:
      <section class="sg-stack sg-stack--6">
        <header class="sg-page-header">
          <p class="sg-page-kicker">Design System</p>
          <h1 class="sg-page-title">Design Gallery</h1>
          <p class="sg-page-copy">Component and group state matrix for audit and regression checking. Available in development only.</p>
          <span data-testid="design-gallery-dev-only-badge">DEV ONLY</span>
        </header>
        <section class="sg-stack sg-stack--4"> [L1: Components] ... </section>
        <section class="sg-stack sg-stack--4"> [L2: Component Groups] ... </section>
      </section>

    Board wrappers follow the contract: <div id="board-{name}" class="sg-card sg-stack sg-stack--4">.
    Inside each board: <p class="sg-muted sg-text-sm">{name}</p> then <div class="sg-stack sg-stack--3"> with state rows.
    State row pattern: <span class="sg-muted sg-text-xs">{state label}</span> then the component call.

    Per UI-SPEC state coverage for each of the 13 component boards:
    - board-stat: one <.stat label="Active Users" value={1_247} />
    - board-stat_link: one <.stat_link href="/admin/users" label="Total Users" value={3_842} />
    - board-task_card: one instance with title="Review flagged accounts", body="3 accounts require review.", href="/admin/users?flagged=true", action="Review"
    - board-summary_chip: basic form (label="Sessions", value={12}) + enhanced form (label="Failed Logins", value={7}, icon=:shield, value_unit="today", subvalue="Spike detected", help="Logins that failed authentication.", tone={:risk}) + tone :warn + tone :ok + tone :info
    - board-applied_chip: one <.applied_chip label="Status: Active" remove_href="?status=" />
    - board-empty_state: one <.empty_state title="No users found"><p>Adjust your filters.</p></.empty_state>
    - board-page_back: one <.page_back return_to="/admin" label="Dashboard" />
    - board-scope_ribbon: global scope <.scope_ribbon copy="Viewing all organizations" /> + org scope <.scope_ribbon copy="Viewing Acme Corp" />
    - board-notice (CANARY, D-10): all 5 tones — nil, :ok, :warn, :risk (with embedded <.notice_link>), :info — per PATTERNS.md canary board content
    - board-notice_link: combined into board-notice as a fifth notice containing notice_link (do NOT create a separate board-notice_link to keep COMPONENT_BOARDS clean; notice_link is validated within board-notice)
    - board-field_help: <.field_help id="fh-example" label="API Token">Token generated at account creation.</.field_help>
    - board-skeleton: <.skeleton class="sg-w-48 sg-h-4" /> (representative width)
    - board-audit_row: three instances — compact (show_detail: false, outcome success), explorer (show_detail, show_codes, action_badge present), failure (non-success outcome risk tone) — row maps mirror @compact_row/@impersonation_row/@failure_row shapes from components_test.exs

    L2 section boards for MG-1..MG-5 (id="board-mg-{n}", class="sg-card sg-stack sg-stack--4"):
    - board-mg-1 (Metric / Summary Strip): board label "MG-1 Metric / Summary Strip"; a <dl class="sg-metric-grid"> containing 3-4 summary_chip calls with static data mirroring IndexLive's region
    - board-mg-2 (Filter Panel + Applied-chip Row): board label "MG-2 Filter Panel + Applied-chip Row"; a filter form section + applied chip row with 2 chips + "Clear all" link using sg-cluster--start wrapper
    - board-mg-3 (Task-card Grid): board label "MG-3 Task-card Grid"; a <div class="sg-grid sg-grid--2"> with 2 task_card calls
    - board-mg-4 (Alarm Notice Band): board label "MG-4 Alarm Notice Band"; a risk-tone notice at top of section (mirrors IndexLive overview alert region) + an ok-tone notice below
    - board-mg-5 (Audit Feed + Pagination): board label "MG-5 Audit Feed + Pagination"; 2 audit_row rows + desktop table header + mobile card pattern + a pagination <nav> with prev/next links (static static href="#")

    All data in all boards must be STATIC LITERAL values — no assigns computed from DB calls, no import/alias of Query modules.

    For the router: add a new scope INSIDE the existing if Application.compile_env(:example, :dev_routes) do block, after the /demo scope:
      scope "/admin", ExampleWeb.Admin do
        pipe_through [:browser, :require_authenticated, :admin_global]
        live_session :admin_design_gallery,
          layout: {ExampleWeb.Layouts, :admin},
          on_mount: [
            {ExampleWeb.UserAuth, :ensure_authenticated},
            {Sigra.LiveView.AdminScope, [mode: :global, policy: Example.SigraAdminPolicy, login_path: "/users/log_in"]}
          ] do
          live "/_design", DesignGalleryLive, :index
        end
      end

    The live_session name is :admin_design_gallery (distinct from :admin_global). The scope alias is ExampleWeb.Admin so DesignGalleryLive resolves to ExampleWeb.Admin.DesignGalleryLive.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && MIX_ENV=dev mix compile --no-deps-check 2>&1 | grep -E "error|warning" | grep -v "^$" | head -20; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - MIX_ENV=dev mix compile --no-deps-check exits 0 with no errors (warnings about unused vars are acceptable but zero errors)
    - file exists at test/example/lib/example_web/live/admin/design_gallery_live.ex
    - grep -c "import Sigra.Admin.Components" test/example/lib/example_web/live/admin/design_gallery_live.ex outputs 1
    - grep -c "id=\"board-notice\"" test/example/lib/example_web/live/admin/design_gallery_live.ex outputs 1
    - grep -c "board-mg-" test/example/lib/example_web/live/admin/design_gallery_live.ex | awk '$1 >= 5 {exit 0} {exit 1}'
    - grep "live \"/_design\"" test/example/lib/example_web/router.ex outputs the route line
    - grep "compile_env.*dev_routes" test/example/lib/example_web/router.ex and the /_design route is INSIDE that block (not outside it)
    - MIX_ENV=test mix compile --no-deps-check exits 0 and the router does NOT define /admin/_design (compile gate works)
  </acceptance_criteria>
  <done>
    design_gallery_live.ex compiled clean in MIX_ENV=dev; all 13 component boards + MG-1..MG-5 boards present; route inside dev_routes compile gate; MIX_ENV=test compile excludes the route.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-04 contract guard + reference docs (INFRA-01 D-04, INFRA-04, INFRA-06)</name>
  <files>
    test/sigra/install/design_gallery_isolation_test.exs
    guides/reference/admin-quality-ledger.md
    guides/reference/admin-fractal-scorecard.md
  </files>
  <read_first>
    - test/sigra/install/template_syntax_test.exs — Path.wildcard + ExUnit assert pattern (the D-04 guard analog); note no @moduletag :install so it runs without tag filtering
    - .planning/phases/185-audit-infrastructure/185-PATTERNS.md — Pattern 12 (D-04 guard test) + guides/reference/admin-quality-ledger.md machine-parseable table format (Pattern 9) + guides/reference/admin-fractal-scorecard.md structure (D-17/D-18 content)
    - .planning/phases/185-audit-infrastructure/185-RESEARCH.md Pattern 9 (ledger machine-parseable format; tier column = column 4 in | delimited rows; tier extraction one-liner) + Pattern 10 (monotonic guard reads it)
    - guides/reference/admin-design-contract.md — sibling file; Markdown heading/table format conventions to follow
    - .planning/REQUIREMENTS.md INFRA-04, INFRA-06 definitions
  </read_first>
  <action>
    PATH DECISION (resolving pattern-mapper note): Place the D-04 guard at test/sigra/install/design_gallery_isolation_test.exs (NOT test/sigra/installer/) to match the existing test/sigra/install/ convention. The CONTEXT.md mentions "installer/" but the codebase uses "install/" — this plan honors the actual directory.

    Write test/sigra/install/design_gallery_isolation_test.exs:
      Module: Sigra.Install.DesignGalleryIsolationTest
      use ExUnit.Case, async: true
      @installer_template_root "priv/templates/sigra.install"
      Single test "no design gallery artifact exists in installer template tree (D-04)":
        offenders = Path.wildcard("#{@installer_template_root}/**/*") |> Enum.filter(&String.contains?(&1, "design"))
        assert offenders == [], "Design gallery artifacts found in installer template tree (D-04 violation):\n" <> Enum.join(offenders, "\n")
      No @moduletag — must run in default mix test suite (no Postgres, pure filesystem glob).

    Write guides/reference/admin-quality-ledger.md:
      Title: "# Admin Quality Ledger"
      Preamble explaining: tier 0 Drift / 1 Ratified / 2 Award-grade; the tier column is machine-parseable (integer only, no whitespace decoration); the monotonic guard reads column 4 (1-indexed in | table).
      Table with EXACTLY FOUR columns separated by | pipes:
        | item | level | tier | evidence |
        |------|-------|------|----------|
      13 L1 component rows: stat, stat_link, task_card, summary_chip, applied_chip, empty_state, page_back, scope_ribbon, notice, notice_link, field_help, skeleton, audit_row — all tier 1, evidence linking to test/sigra/admin/components_test.exs
      5 L2 group rows: mg-1-metric-strip, mg-2-filter-panel, mg-3-task-grid, mg-4-alarm-notice, mg-5-audit-feed — all tier 1, evidence linking to admin-design.spec.ts board placeholders (#)
      6 L3 page rows: index-live, organization-live, users-index-live, user-show-live, audit-index-live, audit-user-live — all tier 1, evidence linking to admin-checkpoints slugs
      CRITICAL: The tier cell MUST be exactly "1" (single digit, leading/trailing space allowed by Markdown table padding, but no other characters). The monotonic guard extracts column 4 and checks /^[012]$/.

    Write guides/reference/admin-fractal-scorecard.md:
      Title: "# Admin Fractal Scorecard Rubric"
      Preamble: This file is the fixed grading anchor for phases 186-192. Re-runs use this rubric to fill the quality ledger identically.
      Section: "## Tier Vocabulary"
        - 0 Drift: fails one or more scorecard axes; visible regressions vs v1.34 contract
        - 1 Ratified: meets the v1.34 contract bar; passes all required axes; no obvious gaps
        - 2 Award-grade: emilkowal.ski-level micro-interaction quality; coherent on-brand copy; pixel-considered spacing; delightful in detail
      Section: "## Shared Dimensions (D1-D11)"
        Markdown table with columns: | dimension | description | pass criteria | score (Pass/Fail/N-A) | evidence |
        11 rows: D1 Brand color, D2 Brand type, D3 Spacing/radius/shadow, D4 Light/Dark/System, D5 Contrast WCAG AA, D6 Motion quality, D7 Interaction states, D8 Mobile-first responsive, D9 IA/least-surprise, D10 Microcopy, D11 A11y semantics
        Each row has a one-line pass criterion and a blank score/evidence for phases 186-192 to fill.
        Cross-reference: See admin-design-contract.md for per-component ARIA and motion specs (D11, D6).
      Section: "## Per-Level Add-ons"
        Subsection "### L1 Individual Component Add-ons": complete/visually-distinct interaction states; reduced-motion strips movement; per-component axe clean; reflows at 320-1440 without overflow
        Subsection "### L2 Meta-Component Group Add-ons": intra-group rhythm consistent; no card-in-card nesting; right-component-for-job; zero/loading/error states defined; desktop-table to mobile-card swap content-equivalent; reused groups render byte-coherently
        Subsection "### L3 Page Composition Add-ons": archetype conformance (Overview/List/Detail); GOV.UK IA (general to specific, tasks-first/posture-second/capabilities-last); principle-of-least-surprise; page vertical rhythm no flush sections or double gaps; landmark/heading order correct; focus management on navigate/patch
        Subsection "### L4 Flow Add-ons": persona JTBD happy + main-error + boundary; scope/return-context preserved; full keyboard operability; calm reduced-motion; Light/Dark/System persists across flow; deterministic fixture reproduces flow
      Footer: "Cross-reference: admin-design-contract.md, admin-ui-principles.md"
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix test test/sigra/install/design_gallery_isolation_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/sigra/install/design_gallery_isolation_test.exs exits 0 (no design artifacts in installer template tree)
    - grep -c "D[0-9]" guides/reference/admin-fractal-scorecard.md | awk '$1 >= 11 {exit 0} {exit 1}' (at least 11 D-N references)
    - grep -E "^\| [a-z]" guides/reference/admin-quality-ledger.md | awk -F'|' '{tier=substr($4,2,length($4)-2); gsub(/ /,"",tier); if (tier !~ /^[012]$/) {print "BAD TIER: " tier; exit 1}}' exits 0 (all tiers machine-parseable)
    - grep -E "^\| [a-z]" guides/reference/admin-quality-ledger.md | wc -l outputs 24 (13 L1 + 5 L2 + 6 L3)
    - grep -c "tier.*1" guides/reference/admin-quality-ledger.md | awk '$1 >= 24 {exit 0} {exit 1}' (all 24 rows start at tier 1)
    - file test/sigra/install/design_gallery_isolation_test.exs exists
    - grep "Sigra.Install.DesignGalleryIsolationTest" test/sigra/install/design_gallery_isolation_test.exs outputs the module line
  </acceptance_criteria>
  <done>
    D-04 ExUnit guard passes; ledger has 24 machine-parseable tier=1 rows; scorecard rubric has D1-D11 + 4 per-level add-on sections; all three files committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev browser → /admin/_design | Request crosses into admin-authed gallery; gallery sits behind admin_global pipeline |
| priv/templates/sigra.install/ → generated host | Installer templates propagate to end-user hosts; gallery must never cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-01 | Elevation of Privilege | /admin/_design route | mitigate | Route is gated by Application.compile_env(:example, :dev_routes) which evaluates false in MIX_ENV=prod/test — compiles out entirely. Existing admin_global pipeline (AdminScope on_mount) enforces auth even in dev. |
| T-185-02 | Information Disclosure | Gallery assigns | accept | All gallery assigns are static literal strings (no DB queries, no user data). No PII risk. The no-query constraint is enforced by code review (no import of Sigra.Admin.* query modules) and pitfall documentation in RESEARCH.md. |
| T-185-03 | Tampering | priv/templates/sigra.install/ (D-04 violation) | mitigate | D-04 ExUnit contract guard (test/sigra/install/design_gallery_isolation_test.exs) fails mix test if any path containing "design" exists in the installer template tree. Runs on every mix test execution — local and CI. |
| T-185-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. @axe-core/playwright already present. No slopcheck needed. |
</threat_model>

<verification>
After both tasks complete:
1. MIX_ENV=dev mix compile --no-deps-check exits 0
2. MIX_ENV=test mix compile --no-deps-check exits 0 (gallery route compiled out)
3. mix test test/sigra/install/design_gallery_isolation_test.exs exits 0
4. grep -E "^\| [a-z]" guides/reference/admin-quality-ledger.md | wc -l outputs 24
5. grep -c "D[0-9]" guides/reference/admin-fractal-scorecard.md (>= 11)
6. grep "live \"/_design\"" test/example/lib/example_web/router.ex outputs one match
7. grep "import Sigra.Admin.Components" test/example/lib/example_web/live/admin/design_gallery_live.ex outputs one match
</verification>

<success_criteria>
- Gallery LiveView compiles in dev, route exists behind compile_env gate, all 13 component boards + MG-1..MG-5 boards present with stable board-{name} ids
- D-04 ExUnit guard passes and would fail if any *design* path appeared under priv/templates/sigra.install/
- Quality ledger has 24 machine-parseable tier=1 rows (13 L1 + 5 L2 + 6 L3)
- Fractal scorecard rubric has D1-D11 shared dimensions + L1/L2/L3/L4 add-on sections committed
</success_criteria>

<output>
Create .planning/phases/185-audit-infrastructure/185-01-SUMMARY.md when done.
</output>
