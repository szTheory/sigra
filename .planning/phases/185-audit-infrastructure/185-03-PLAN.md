---
phase: 185-audit-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 185-01
  - 185-02
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/example/priv/playwright/playwright.config.ts
  - test/example/priv/playwright/snapshot-allowlist-design
  - scripts/ci/snapshot-canary-guard.sh
  - scripts/ci/snapshot-recapture-gate.sh
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "admin-design.spec.ts runs board-snapshot + axe against all 13 component boards + MG-1..MG-5 group boards across 3 Playwright projects"
    - "Each board is element-scoped (page.locator('#board-{name}')), not full-page"
    - "Each board gets an assertNoAxeViolations call (wcag2a+wcag2aa, 0 violations)"
    - "snapshot-allowlist-design file exists and is steady-state empty (comments only)"
    - "snapshot-canary-guard.sh recognizes -admin-design-* slug suffix"
    - "snapshot-recapture-gate.sh runs the design lane (a2/b2 steps)"
    - "gallery boards are captured for the first time (initial baselines committed)"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-design.spec.ts"
      provides: "Board-snapshot spec; assertBoardScreenshot helper (element-scoped); assertNoAxeViolations copy; COMPONENT_BOARDS + GROUP_BOARDS loop"
    - path: "test/example/priv/playwright/playwright.config.ts"
      provides: "admin-design-{chromium,mobile,dark} project trio; ADMIN_DESIGN_SPEC regex; testIgnore extensions"
    - path: "test/example/priv/playwright/snapshot-allowlist-design"
      provides: "Second empty allowlist (comments-only, board-notice canary note)"
    - path: "scripts/ci/snapshot-canary-guard.sh"
      provides: "Extended slug_of() or second-invocation wiring for -admin-design-* suffix"
    - path: "scripts/ci/snapshot-recapture-gate.sh"
      provides: "Design-lane steps a2/b2 added after existing a/b checkpoints steps"
    - path: ".github/workflows/ci.yml"
      provides: "Design board Playwright run step in example_playwright_smoke; design-lane snapshot drift guard step"
    - path: "test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/"
      provides: "Initial board PNG baselines (auto-created on first --update-snapshots run)"
  key_links:
    - from: "test/example/priv/playwright/tests/admin-design.spec.ts"
      to: "test/example/lib/example_web/live/admin/design_gallery_live.ex"
      via: "page.goto('/admin/_design') + page.locator('#board-{name}')"
      pattern: "/admin/_design"
    - from: "test/example/priv/playwright/playwright.config.ts"
      to: "test/example/priv/playwright/tests/admin-design.spec.ts"
      via: "testMatch: ADMIN_DESIGN_SPEC regex"
      pattern: "admin-design"
    - from: ".github/workflows/ci.yml"
      to: "test/example/priv/playwright/tests/admin-design.spec.ts"
      via: "npx playwright test tests/admin-design.spec.ts --project=admin-design-{chromium,mobile,dark}"
      pattern: "admin-design.spec.ts"
---

<objective>
Wire the Playwright board-snapshot lane: create the admin-design spec, add 3 projects to
playwright.config.ts, create the empty allowlist, extend the canary guard and recapture
gate for the design lane, extend CI for the board run and design-lane drift guard, then
capture the initial baselines.

Purpose: This closes the loop on INFRA-02 and INFRA-03. After this plan, every board
has a committed PNG baseline and axe is asserted green. Future PRs that regress any board
will fail the snapshot drift guard; the board-notice canary is immutable.

Output:
- admin-design.spec.ts (board spec)
- playwright.config.ts (3 new projects + testIgnore extensions)
- snapshot-allowlist-design (empty)
- snapshot-canary-guard.sh (design-lane extension)
- snapshot-recapture-gate.sh (design-lane a2/b2 steps)
- .github/workflows/ci.yml (design board run step + design drift guard step)
- initial PNG baselines committed under tests/admin-design.spec.ts-snapshots/
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/phases/185-audit-infrastructure/185-CONTEXT.md
@/Users/jon/projects/sigra/.planning/phases/185-audit-infrastructure/185-RESEARCH.md
@/Users/jon/projects/sigra/.planning/phases/185-audit-infrastructure/185-PATTERNS.md

<interfaces>
From test/example/priv/playwright/tests/admin-checkpoints.spec.ts (copy verbatim):
  Line 1-3: import AxeBuilder + import { test, expect, type Page, type TestInfo } from '@playwright/test'
  Line 6:   import { TEST_PASSWORD } from '../helpers/fixtures';
  Lines 43-47: waitForLiveViewReady(page) — page.waitForSelector('[data-phx-session].phx-connected', state:'attached')
  Lines 49-58: registerUser(page, email, password) — goto /users/register, fill email+password, requestSubmit
  Lines 114-126: assertNoAxeViolations(page, label) — AxeBuilder({page}).withTags(['wcag2a','wcag2aa']).analyze()
  Lines 144-146: maxDiffPixels thresholds — ci ? 200_000 : dark ? 75_000 : mobile ? 45_000 : 30_000
                  maxDiffPixelRatio — ci ? 0.22 : dark ? 0.1 : mobile ? 0.08 : 0.06
  Lines 157-160: admin registration pattern — const adminEmail = `platform-admin+checkpoint-${suffix}@example.test`
  (no storageState — spec registers fresh user per run; checkpoints use registerUser inline)

From test/example/priv/playwright/playwright.config.ts (exact patterns to clone):
  Lines 26-27: const ADMIN_CHECKPOINTS_SPEC = /admin-checkpoints\.spec\.ts/;
  Line 84: chromium project testIgnore: [ADMIN_CHECKPOINTS_SPEC, ADMIN_GENERATED_SPEC, DEMO_SHOWCASE_SPEC]
  Lines 93-100: mobile project testIgnore: [ADMIN_BEHAVIOR_SPECS, ADMIN_CHECKPOINTS_SPEC, ADMIN_GENERATED_SPEC, WEBAUTHN_CDP_SPECS, DEMO_SHOWCASE_SPEC]
  Lines 107-138: admin-checkpoints-{chromium,mobile,dark} project defs with checkpointVideo
  Lines 60-61: pathTemplate: '{testDir}/{testFilePath}-snapshots/{arg}{-projectName}{ext}'

From test/example/priv/playwright/snapshot-allowlist (format reference — comments-only):
  # Intended-delta snapshot slugs for the admin-checkpoints Playwright baselines.
  # [format explanation lines]
  # The `impersonation-banner` canary must NEVER appear here.

From scripts/ci/snapshot-canary-guard.sh lines 17-20 (env defaults + flags accepted):
  SNAP_DIR="${SNAP_DIR:-test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots}"
  ALLOWLIST="${ROOT}/test/example/priv/playwright/snapshot-allowlist"
  BASE="HEAD"
  CANARY="impersonation-banner"
  -- accepts --base, --allowlist, --allow, --canary, --require-all; SNAP_DIR is env-overridable

From scripts/ci/snapshot-recapture-gate.sh lines 30-38 (two-step pattern to extend):
  echo "snapshot-recapture-gate: (a) compare-mode admin checkpoints across 3 projects"
  ( cd "$PW" && CI=true ... npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-{chromium,mobile,dark} )
  echo "snapshot-recapture-gate: (b) drift/canary guard ..."
  bash "${ROOT}/scripts/ci/snapshot-canary-guard.sh" --base HEAD --require-all "${ALLOW_ARGS[@]}"

From .github/workflows/ci.yml lines 806-822 (checkpoints run step to clone for design run):
  - name: Run admin checkpoints (chromium, mobile, dark-chromium)
    working-directory: test/example/priv/playwright
    env: CI: "true" + SIGRA_EXAMPLE_URL: "http://localhost:4000"
    run: npx playwright test tests/admin-checkpoints.spec.ts --project=admin-checkpoints-{chromium,mobile,dark}

From .github/workflows/ci.yml lines 1095-1114 (snapshot_drift_guard job — add design step to it):
  - name: Run snapshot canary / drift allowlist guard
    run: bash scripts/ci/snapshot-canary-guard.sh --base "${{ steps.base.outputs.ref }}"
  -- ADD after this step: design-lane guard invocation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin-design spec + playwright.config.ts + empty allowlist (INFRA-02, INFRA-03, D-05..D-09)</name>
  <files>
    test/example/priv/playwright/tests/admin-design.spec.ts
    test/example/priv/playwright/playwright.config.ts
    test/example/priv/playwright/snapshot-allowlist-design
  </files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts — FULL FILE; copy verbatim: imports block (lines 1-6), waitForLiveViewReady (lines 43-47), registerUser (lines 49-58), assertNoAxeViolations (lines 114-126), threshold values (lines 144-146), platform-admin+ registration pattern (lines 157-160)
    - test/example/priv/playwright/playwright.config.ts — FULL FILE; locate: ADMIN_CHECKPOINTS_SPEC const; chromium testIgnore list; mobile testIgnore list; admin-checkpoints-{chromium,mobile,dark} project defs (lines 107-138); checkpointVideo const; pathTemplate
    - test/example/priv/playwright/snapshot-allowlist — format reference (comment header style)
    - .planning/phases/185-audit-infrastructure/185-PATTERNS.md — Pattern 3 (COMPONENT_BOARDS list without board-notice_link; notice_link absorbed into board-notice), Pattern 4 (GROUP_BOARDS MG-1..MG-5), Pattern 5 (playwright.config.ts diff), Pattern 6 (assertBoardScreenshot helper with element-scoped locator)
  </read_first>
  <action>
    Write test/example/priv/playwright/tests/admin-design.spec.ts:

    Imports (copy verbatim from admin-checkpoints.spec.ts lines 1-6):
      import AxeBuilder from '@axe-core/playwright';
      import { test, expect, type Page, type TestInfo } from '@playwright/test';
      import { TEST_PASSWORD } from '../helpers/fixtures';

    Helper functions to copy VERBATIM from admin-checkpoints.spec.ts:
      waitForLiveViewReady (lines 43-47) — copy unchanged
      registerUser (lines 49-58) — copy unchanged
      assertNoAxeViolations (lines 114-126) — copy unchanged

    New helper assertBoardScreenshot (adapted from assertCheckpointScreenshot but element-scoped):
      async function assertBoardScreenshot(page: Page, testInfo: TestInfo, boardId: string) {
        await assertNoAxeViolations(page, `axe:${boardId}`);
        const dark = testInfo.project.name.includes('dark');
        const mobile = testInfo.project.name.includes('mobile');
        const ci = process.env.CI === 'true';
        const locator = page.locator(`#${boardId}`);
        await expect(locator).toHaveScreenshot(`${boardId}.png`, {
          maxDiffPixels: ci ? 200_000 : dark ? 75_000 : mobile ? 45_000 : 30_000,
          maxDiffPixelRatio: ci ? 0.22 : dark ? 0.1 : mobile ? 0.08 : 0.06,
        });
      }
    CRITICAL: toHaveScreenshot is called on page.locator(`#${boardId}`) NOT on page — this is the defining difference from checkpoints (element-scoped, not full-page).
    Threshold values MUST match the checkpoints spec exactly (copy from lines 144-146).

    Board ID constants:
      const COMPONENT_BOARDS = [
        'board-stat', 'board-stat_link', 'board-task_card', 'board-summary_chip',
        'board-applied_chip', 'board-empty_state', 'board-page_back', 'board-scope_ribbon',
        'board-notice',      // designated canary (D-10)
        'board-field_help', 'board-skeleton', 'board-audit_row',
      ];
      const GROUP_BOARDS = ['board-mg-1', 'board-mg-2', 'board-mg-3', 'board-mg-4', 'board-mg-5'];
    NOTE: board-notice_link is NOT a separate board ID — notice_link is shown inside board-notice (combined canary board per PATTERNS.md). COMPONENT_BOARDS has 12 entries (notice covers notice_link).

    test.describe block:
      test.describe('Design gallery board snapshots', () => {
        test.beforeAll(async ({ browser }) => {
          const suffix = Date.now();
          const adminEmail = `platform-admin+design-${suffix}@example.test`;
          const page = await browser.newPage();
          await registerUser(page, adminEmail, TEST_PASSWORD);
          await page.close();
        });
        test.beforeEach(async ({ page }) => {
          await page.goto('/admin/_design');
          await waitForLiveViewReady(page);
        });
        for (const boardId of [...COMPONENT_BOARDS, ...GROUP_BOARDS]) {
          test(`board: ${boardId}`, async ({ page }, testInfo) => {
            await assertBoardScreenshot(page, testInfo, boardId);
          });
        }
      });
    NOTE: session is established via registerUser with platform-admin+ prefix (Example.SigraAdminPolicy grants admin to platform-admin+ users). No storageState needed — fresh registration per beforeAll.

    Edit test/example/priv/playwright/playwright.config.ts:
    1. Add ADMIN_DESIGN_SPEC const alongside ADMIN_CHECKPOINTS_SPEC:
       const ADMIN_DESIGN_SPEC = /admin-design\.spec\.ts/;
    2. Add ADMIN_DESIGN_SPEC to chromium project testIgnore array (so design spec only runs in admin-design-* projects)
    3. Add ADMIN_DESIGN_SPEC to mobile project testIgnore array (same reason)
    4. Add three new projects at the END of the projects array (after admin-checkpoints-dark):
       { name: 'admin-design-chromium', testMatch: ADMIN_DESIGN_SPEC, use: { ...devices['Desktop Chrome'], video: checkpointVideo } }
       { name: 'admin-design-mobile', testMatch: ADMIN_DESIGN_SPEC, use: { ...devices['iPhone 13'], video: checkpointVideo } }
       { name: 'admin-design-dark', testMatch: ADMIN_DESIGN_SPEC, use: { ...devices['Desktop Chrome'], colorScheme: 'dark', video: checkpointVideo } }
    Do NOT change the pathTemplate — the existing template already produces the correct snapshot paths.
    Do NOT modify any other projects.

    Write test/example/priv/playwright/snapshot-allowlist-design:
    Copy the comment header format from snapshot-allowlist but adapt for the design lane:
      # Intended-delta snapshot slugs for the admin-design Playwright baselines.
      #
      # One slug per line. A slug covers all three projects at once
      # (chromium / mobile / dark) — e.g. `board-stat` allows
      # board-stat-admin-design-{chromium,mobile,dark}.png to change.
      #
      # STEADY STATE: this file is empty (comments only). scripts/ci/snapshot-canary-guard.sh
      # fails CI if any baseline PNG changes whose slug is not listed here, so a PR that
      # deliberately re-records baselines MUST add the slug(s) in the same diff.
      # Reset to empty once the re-recording PR merges.
      #
      # The `board-notice` canary must NEVER appear here.
      #
    File must end with a trailing newline. No non-comment lines.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/sigra && grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -q . && echo "FAIL: non-comment content" || echo "PASS: empty allowlist"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -q . exits 1 (no non-comment content → allowlist is empty)
    - grep "ADMIN_DESIGN_SPEC" test/example/priv/playwright/playwright.config.ts outputs at least 5 lines (const + 2 testIgnore + 3 testMatch)
    - grep "admin-design-chromium\|admin-design-mobile\|admin-design-dark" test/example/priv/playwright/playwright.config.ts | wc -l outputs 3
    - grep "board-notice" test/example/priv/playwright/tests/admin-design.spec.ts outputs the canary entry in COMPONENT_BOARDS
    - grep "assertBoardScreenshot" test/example/priv/playwright/tests/admin-design.spec.ts | wc -l >= 2 (helper definition + call inside loop)
    - grep "page.locator" test/example/priv/playwright/tests/admin-design.spec.ts | grep "boardId" | wc -l >= 1 (element-scoped capture)
    - grep -c "assertNoAxeViolations" test/example/priv/playwright/tests/admin-design.spec.ts >= 2 (function def + call inside assertBoardScreenshot)
    - node -e "require('./test/example/priv/playwright/playwright.config.ts')" or: cd test/example/priv/playwright && npx playwright --version exits 0 (config parseable)
  </acceptance_criteria>
  <done>
    admin-design.spec.ts written with element-scoped assertBoardScreenshot + assertNoAxeViolations; 12 component + 5 group board IDs; playwright.config.ts has 3 admin-design-* projects + testIgnore extensions; snapshot-allowlist-design is empty (comments-only).
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard extensions + CI wiring + initial baseline capture (INFRA-02, INFRA-03, D-09..D-10)</name>
  <files>
    scripts/ci/snapshot-canary-guard.sh
    scripts/ci/snapshot-recapture-gate.sh
    .github/workflows/ci.yml
  </files>
  <read_first>
    - scripts/ci/snapshot-canary-guard.sh — FULL FILE; locate: slug_of() lines 53-55 (current sed with -admin-checkpoints-); SNAP_DIR env default lines 17-20; --base/--allowlist/--canary flags; CANARY default
    - scripts/ci/snapshot-recapture-gate.sh — FULL FILE; locate: step (a) playwright run lines 30-35; step (b) guard invocation lines 36-38; overall script structure; PW variable; ALLOW_ARGS
    - .github/workflows/ci.yml lines 806-838 — existing checkpoints run step (clone for design run step) + stage step after it
    - .github/workflows/ci.yml lines 1095-1114 — snapshot_drift_guard job (add design-lane step after existing guard step)
    - .planning/phases/185-audit-infrastructure/185-PATTERNS.md — Pattern 7 (Option A second invocation vs Option B slug_of generalization; recommendation is Option A), Pattern 8 (recapture gate extension), pattern for SNAP_DIR override
  </read_first>
  <action>
    GUARD EXTENSION — OPTION A (second invocation, per RESEARCH.md recommendation to avoid sed alternation ordering risk):
    In scripts/ci/snapshot-canary-guard.sh, leave slug_of() UNCHANGED. The design lane is handled entirely by a second CI invocation with SNAP_DIR/--allowlist/--canary overrides. No change to slug_of().
    However, the guard script must be able to strip the -admin-design-{chromium,mobile,dark}.png suffix when called with SNAP_DIR pointing at the design snapshots directory — to do this cleanly, apply Option B (generalize slug_of()) instead of a raw second invocation: add the design suffix pattern to slug_of() as a second sed alternation:
      slug_of() {
        basename "$1" | sed -E \
          's/-admin-checkpoints-(chromium|mobile|dark)\.png$//;
           s/-admin-design-(chromium|mobile|dark)\.png$//'
      }
    This makes slug_of() handle both lane suffixes. The second invocation in CI uses SNAP_DIR + --allowlist + --canary overrides, which is safe because the slug_of() now correctly strips design suffixes. No other change to the script.

    RECAPTURE GATE EXTENSION:
    In scripts/ci/snapshot-recapture-gate.sh, add design-lane steps (a2) and (b2) after the existing (a)+(b) checkpoints steps:
      echo "snapshot-recapture-gate: (a2) compare-mode admin design gallery across 3 projects"
      ( cd "$PW" && CI=true SIGRA_EXAMPLE_URL="$SIGRA_EXAMPLE_URL" \
          npx playwright test tests/admin-design.spec.ts \
            --project=admin-design-chromium \
            --project=admin-design-mobile \
            --project=admin-design-dark )
      echo "snapshot-recapture-gate: (b2) drift/canary guard — design lane"
      SNAP_DIR="${PW}/tests/admin-design.spec.ts-snapshots" \
      bash "${ROOT}/scripts/ci/snapshot-canary-guard.sh" \
        --base HEAD \
        --allowlist "${PW}/snapshot-allowlist-design" \
        --canary board-notice \
        --require-all "${ALLOW_ARGS[@]}"
    Insert these lines after the existing step (b) guard invocation and before any subsequent steps in the script.

    CI WIRING in .github/workflows/ci.yml:
    1. In the example_playwright_smoke job, add a new step after the existing "Run admin checkpoints" step (around line 822):
         - name: Run design gallery boards (chromium, mobile, dark)
           working-directory: test/example/priv/playwright
           env:
             CI: "true"
             SIGRA_EXAMPLE_URL: "http://localhost:4000"
           run: |
             npx playwright test \
               tests/admin-design.spec.ts \
               --project=admin-design-chromium \
               --project=admin-design-mobile \
               --project=admin-design-dark
    2. In the snapshot_drift_guard job, add a second step after the existing "Run snapshot canary / drift allowlist guard" step:
         - name: Run snapshot canary / drift guard — design lane
           run: |
             SNAP_DIR="test/example/priv/playwright/tests/admin-design.spec.ts-snapshots" \
             bash scripts/ci/snapshot-canary-guard.sh \
               --base "${{ steps.base.outputs.ref }}" \
               --allowlist test/example/priv/playwright/snapshot-allowlist-design \
               --canary board-notice

    INITIAL BASELINE CAPTURE:
    After all file edits are committed, start the example app (mix phx.server in test/example) and capture the initial baselines:
      cd test/example/priv/playwright
      npx playwright test tests/admin-design.spec.ts \
        --project=admin-design-chromium \
        --project=admin-design-mobile \
        --project=admin-design-dark \
        --update-snapshots
    This creates the PNG files in tests/admin-design.spec.ts-snapshots/. Commit them.
    After capture, run WITHOUT --update-snapshots to confirm all tests pass (axe + visual match).
    Then run the design-lane canary guard to confirm it exits 0 (no changes, canary intact):
      SNAP_DIR="test/example/priv/playwright/tests/admin-design.spec.ts-snapshots" \
      bash scripts/ci/snapshot-canary-guard.sh \
        --base HEAD \
        --allowlist test/example/priv/playwright/snapshot-allowlist-design \
        --canary board-notice

    NOTE on stage step: After the design gallery run in CI, there is an existing "Stage admin checkpoint PNGs" step. The design lane PNGs have a board-* prefix (not admin-*), so they are NOT staged by the existing step (which filters on 'admin-*.png'). This is acceptable for this phase — the design PNGs are in the snapshots directory and are not part of the artifact bundle contract. If artifact bundling of design PNGs is desired later, a separate stage step can be added.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/sigra && bash -n scripts/ci/snapshot-canary-guard.sh && echo "canary-guard syntax OK" && bash -n scripts/ci/snapshot-recapture-gate.sh && echo "recapture-gate syntax OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -A3 "slug_of()" scripts/ci/snapshot-canary-guard.sh | grep "admin-design" (design suffix strip is present in slug_of)
    - grep "admin-design" scripts/ci/snapshot-recapture-gate.sh | wc -l >= 2 (a2 step present)
    - grep "admin-design.spec.ts" .github/workflows/ci.yml | wc -l >= 2 (design run step + drift guard step)
    - grep "canary board-notice" .github/workflows/ci.yml (design-lane drift guard uses board-notice canary)
    - bash -n scripts/ci/snapshot-canary-guard.sh exits 0 (syntax clean after edit)
    - bash -n scripts/ci/snapshot-recapture-gate.sh exits 0 (syntax clean after edit)
    - python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" exits 0
    - After --update-snapshots run: ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | wc -l outputs >= 51 (17 boards x 3 projects)
    - After non-update run: npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium exits 0 (all boards pass + axe clean)
    - SNAP_DIR=test/example/priv/playwright/tests/admin-design.spec.ts-snapshots bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice exits 0 (canary intact, no allowlist violations)
  </acceptance_criteria>
  <done>
    snapshot-canary-guard.sh slug_of() handles design suffix; recapture-gate has a2/b2 steps; CI has design run + design drift guard steps; initial PNG baselines committed; chromium project run exits 0 with axe clean; design-lane canary guard exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Initial admin-design board PNG baselines captured across chromium/mobile/dark Playwright projects. All 17 boards (12 component + 5 group) x 3 projects = 51 PNGs committed. Axe reported 0 violations on all boards in the chromium run. board-notice canary guard exits 0.
  </what-built>
  <how-to-verify>
    1. Start the example app: cd test/example && mix phx.server (or use port 4011 if already running via scripts/uat/up.sh)
    2. Open a browser at http://localhost:4000/admin/_design — log in as a platform-admin+ user (any user whose email starts with platform-admin+)
    3. Confirm you see: "Design Gallery" heading, "Components" section with 13 boards (stat, stat_link, task_card, summary_chip, applied_chip, empty_state, page_back, scope_ribbon, notice, field_help, skeleton, audit_row), "Component Groups" section with 5 boards (MG-1..MG-5)
    4. Confirm board-notice shows all 5 tones (nil, ok, warn, risk with embedded notice_link, info)
    5. Confirm all boards render inside the real admin shell (Rail Accent brand, topbar, theme toggle present)
    6. Inspect a handful of captured PNGs at test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ — confirm they are human-meaningful (each board shows the component states as a composite matrix, not a blank or broken render)
    7. Confirm MIX_ENV=test mix compile --no-deps-check exits 0 (gallery compiles out)
    8. Confirm mix test test/sigra/install/design_gallery_isolation_test.exs exits 0 (D-04 guard passes)
  </how-to-verify>
  <resume-signal>Type "approved" if boards render correctly and baselines look human-meaningful, or describe any rendering issues for fixing before approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test run → /admin/_design | Browser automation authenticates as a fresh platform-admin+ user; gallery is read-only |
| snapshot-allowlist-design → ci-gate | Empty allowlist enforces that no board baseline changes silently; canary board-notice is immutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-06 | Information Disclosure | Gallery PNGs in snapshot dir | accept | Board PNGs contain only static synthetic data (no real user PII). The gallery uses static literal assigns — no DB queries. PNGs are committed to the repo for regression tracking, same as existing admin-checkpoints PNGs. |
| T-185-07 | Elevation of Privilege | /admin/_design route in Playwright spec | accept | The spec registers a fresh platform-admin+ user (no pre-existing credentials stored). The route is dev-only (compile_env gate). The registered user has no special permissions beyond admin scope granted by the email prefix convention. |
| T-185-08 | Denial of Service | CI design board run adding latency | accept | 3 projects × ~17 boards = ~51 tests. Runtime is bounded (~3-5 min per VALIDATION.md). Does not block other CI jobs (parallel). Acceptable tradeoff for the visual regression value. |
| T-185-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. @axe-core/playwright and @playwright/test already installed. |
</threat_model>

<verification>
After all tasks and human checkpoint complete:
1. npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium exits 0 (all boards pass + axe 0 violations)
2. ls test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ | wc -l >= 51
3. grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -q . exits 1 (allowlist empty)
4. SNAP_DIR=test/example/priv/playwright/tests/admin-design.spec.ts-snapshots bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice exits 0
5. bash -n scripts/ci/snapshot-canary-guard.sh && bash -n scripts/ci/snapshot-recapture-gate.sh (both syntax-clean)
6. python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" exits 0
7. grep "admin-design" .github/workflows/ci.yml | wc -l >= 2
8. mix test test/sigra/install/design_gallery_isolation_test.exs exits 0 (D-04 still clean)
</verification>

<success_criteria>
- admin-design.spec.ts runs all 17 board tests (12 component + 5 group) with element-scoped capture and axe assertion
- 51 initial PNG baselines committed (17 boards × 3 projects)
- snapshot-allowlist-design is empty (comments-only); board-notice is the canary
- snapshot-canary-guard.sh recognizes -admin-design-* slugs
- snapshot-recapture-gate.sh has a2/b2 design-lane steps
- CI has design board run step and design-lane drift guard step
- Human checkpoint confirms boards render correctly in real admin shell with human-meaningful composites
</success_criteria>

<output>
Create .planning/phases/185-audit-infrastructure/185-03-SUMMARY.md when done.
</output>
