---
phase: 22-passkeys-generator-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/sigra.install.ex
  - lib/sigra/install/features/core.ex
  - test/sigra/install/features/passkeys_test.exs
  - test/sigra/install/features/coverage_test.exs
autonomous: true
requirements:
  - PK-02
must_haves:
  truths:
    - "Default installs treat passkeys as enabled without requiring an explicit flag."
    - "`mix sigra.install --no-passkeys` flips the generator binding into structural omission mode."
    - "Installer summary text makes the passkey default and opt-out state explicit."
  artifacts:
    - "lib/mix/tasks/sigra.install.ex defines passkeys as a default-on boolean and threads `passkeys?` into binding."
    - "lib/sigra/install/features/core.ex prints `Passkeys: enabled (default)` or `Passkeys: disabled via --no-passkeys` in the install summary."
    - "test/sigra/install/features/passkeys_test.exs and test/sigra/install/features/coverage_test.exs lock the default-on contract."
  key_links:
    - "Mix.Tasks.Sigra.Install build_binding/4 passes `passkeys?` into feature templates without runner special cases."
    - "Core summary output reflects the same passkey flag state parsed by Mix.Tasks.Sigra.Install."
---

<objective>
Make the installer treat passkeys as a default-on feature and lock that contract in manifest-level tests before the broader generator rewiring starts.

Purpose: Establish the Phase 22 control plane per D-11 and D-12 so later plans can gate generated output off a trustworthy `passkeys?` binding.
Output: Updated CLI default/help/binding posture plus manifest-level tests that fail if passkeys drift back to opt-in semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md
@.planning/phases/22-passkeys-generator-wiring/22-RESEARCH.md
@.planning/phases/22-passkeys-generator-wiring/22-PATTERNS.md
@.planning/phases/22-passkeys-generator-wiring/22-VALIDATION.md
@test/example/AGENTS.md
@.planning/phases/11-generator-feature-system/11-05-SUMMARY.md
@.planning/phases/18-backfill-organizations-generator-wiring/18-03-upgrade-test-fixture-and-ci-matrix-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip passkeys to default-on installer semantics</name>
  <files>lib/mix/tasks/sigra.install.ex, lib/sigra/install/features/core.ex</files>
  <read_first>.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md, .planning/phases/22-passkeys-generator-wiring/22-RESEARCH.md, .planning/phases/22-passkeys-generator-wiring/22-PATTERNS.md, lib/mix/tasks/sigra.install.ex, lib/sigra/install/features/core.ex</read_first>
  <action>Update `Mix.Tasks.Sigra.Install` so `@default_opts` sets `passkeys: true`, the module docs and option help explicitly document `--passkeys` / `--no-passkeys` as `(default: true)`, and `build_binding/4` threads `passkeys?: Keyword.get(opts, :passkeys, true)` alongside `organizations?` per D-11. In `Sigra.Install.Features.Core`, update the install-summary output so it prints exactly `Passkeys: enabled (default)` when `opts[:passkeys]` is true and `Passkeys: disabled via --no-passkeys` when false per D-12. Keep `Sigra.Install.Runner` feature-agnostic; do not add passkey-specific branches there per D-06.</action>
  <acceptance_criteria>
    - `rg -n "passkeys: true" lib/mix/tasks/sigra.install.ex` finds the default-on flag.
    - `rg -n "passkeys\\?: Keyword.get\\(opts, :passkeys, true\\)" lib/mix/tasks/sigra.install.ex` finds the binding handoff.
    - `rg -n -- "--passkeys.*default: true|--no-passkeys.*default: true" lib/mix/tasks/sigra.install.ex` finds explicit help text for both flags.
    - `rg -n "Passkeys: enabled \\(default\\)|Passkeys: disabled via --no-passkeys" lib/sigra/install/features/core.ex` finds both summary strings.
    - `rg -n "passkeys" lib/sigra/install/runner.ex` does not add a new passkey-specific code path beyond pre-existing generic manual-action handling.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/install/features/passkeys_test.exs --max-failures 1</automated>
  </verify>
  <done>The CLI, binding, and install summary all describe passkeys as default-on and the runner remains feature-agnostic.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the default-on contract in manifest-level tests</name>
  <files>test/sigra/install/features/passkeys_test.exs, test/sigra/install/features/coverage_test.exs</files>
  <read_first>.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md, .planning/phases/22-passkeys-generator-wiring/22-PATTERNS.md, test/sigra/install/features/passkeys_test.exs, test/sigra/install/features/coverage_test.exs, lib/sigra/install/features/passkeys.ex</read_first>
  <action>Update `Sigra.Install.Features.PasskeysTest` so `enabled?/1` asserts default true, explicit `--passkeys` true, and `--no-passkeys` false per D-11. Extend the same module to assert the feature manifest remains the ownership boundary for passkey-only artifacts and manual-action reporting. Update `CoverageTest` so any passkey injection-content templates introduced in Phase 22 are whitelisted under `Sigra.Install.Features.Passkeys` instead of being treated as orphan drift. Do not relax the coverage lint globally; only register concrete passkey-owned files per D-04.</action>
  <acceptance_criteria>
    - `rg -n "returns true by default|default-on" test/sigra/install/features/passkeys_test.exs` finds the default-enabled assertion.
    - `rg -n "assert Passkeys.enabled\\(\\[\\]\\) == true|assert Passkeys.enabled\\(\\[\\]\\)" test/sigra/install/features/passkeys_test.exs` proves the no-arg contract.
    - `rg -n "Sigra.Install.Features.Passkeys => \\[" test/sigra/install/features/coverage_test.exs` shows explicit passkey ownership entries when needed.
    - `rg -n "@known_drift.*Passkeys" test/sigra/install/features/coverage_test.exs` still shows an empty or tightly scoped passkeys drift list, not a blanket exception.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/install/features/passkeys_test.exs test/sigra/install/features/coverage_test.exs --max-failures 1</automated>
  </verify>
  <done>Feature-level tests fail on any regression that turns passkeys back into opt-in behavior or leaves new passkey-owned templates orphaned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags -> generator binding | Untrusted install options determine whether passkey surfaces are emitted or omitted. |
| Feature manifest -> shared summary output | Installer UX must report the actual feature state, not a stale or inferred value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | T | `lib/mix/tasks/sigra.install.ex` | mitigate | Flip the passkeys default in one source of truth and thread `passkeys?` from parsed opts into binding so `--no-passkeys` cannot be ignored or partially applied. |
| T-22-01-02 | R | `lib/sigra/install/features/core.ex` summary output | mitigate | Emit exact enabled/disabled strings from the same parsed flag state so install logs can prove whether passkeys were intentionally omitted. |
| T-22-01-03 | I | feature tests / coverage lint | mitigate | Add manifest-level assertions so dead passkey drift or a reverted default is caught before broader generator rewiring lands. |
</threat_model>

<verification>
Run `mix test test/sigra/install/features/passkeys_test.exs test/sigra/install/features/coverage_test.exs --max-failures 1`.
</verification>

<success_criteria>
Phase 22 has a stable default-on passkeys control path: CLI docs, parsed options, template binding, and install summary all agree on the feature state, and feature-level tests pin that behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/22-passkeys-generator-wiring/22-01-SUMMARY.md`
</output>
