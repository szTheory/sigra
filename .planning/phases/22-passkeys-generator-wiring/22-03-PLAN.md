---
phase: 22-passkeys-generator-wiring
plan: 03
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - priv/templates/sigra.install/core/auth.ex
  - priv/templates/sigra.install/core/session_controller.ex
  - priv/templates/sigra.install/core/login_html.ex
  - priv/templates/sigra.install/core/mfa_settings_live.ex
  - priv/templates/sigra.install/core/mfa_challenge_live.ex
  - priv/templates/sigra.install/core/registration_html.ex
  - priv/templates/sigra.install/core/confirmation_controller.ex
  - test/sigra/install/generator_passkeys_opt_out_test.exs
autonomous: true
requirements:
  - PK-02
must_haves:
  truths:
    - "`mix sigra.install --no-passkeys` generates no shared-template passkey residue in auth controllers, LiveViews, or login/signup flows."
    - "`mix sigra.install --no-organizations --no-passkeys` also generates no passkey residue, so omission holds across both disabled combinations."
    - "Default installs still compile and expose passkey branches where expected because shared templates are locally guarded, not replaced with stubs."
    - "A generated-app omission suite fails if passkey strings, routes, helpers, or dependency references survive the disabled path."
  artifacts:
    - "Shared Core auth templates gate passkey-only regions with `passkeys?` instead of unconditional aliases, helpers, actions, or UI branches."
    - "test/sigra/install/generator_passkeys_opt_out_test.exs proves omission directly on generated output for both `--no-passkeys` legs."
    - "No passkey-only noop helpers or compatibility wrappers are introduced into shared templates."
  key_links:
    - "Core templates receive `passkeys?` and omit `Sigra.Passkeys`, `Sigra.Plug.PasskeyChallenge`, `UserPasskey`, and passkey route helpers on disabled installs."
    - "The opt-out generator suite inspects real generated files for forbidden residue strings and file/dependency absence under both `--no-passkeys` and `--no-organizations --no-passkeys`."
    - "This plan consumes the feature-owned wiring from 22-02 rather than reintroducing passkey logic into Core ownership."
---

<objective>
Guard the canonical shared auth templates and add the generated-app omission suite so `--no-passkeys` produces a structurally clean host app.

Purpose: Deliver the shared-template half of PK-02 per D-03, D-05, D-06, and D-10 by removing disabled-path residue without inventing a new variant system.
Output: Shared templates guarded with `passkeys?` plus an authoritative omission regression suite over the generated app.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md
@.planning/phases/22-passkeys-generator-wiring/22-RESEARCH.md
@.planning/phases/22-passkeys-generator-wiring/22-PATTERNS.md
@.planning/phases/22-passkeys-generator-wiring/22-VALIDATION.md
@test/example/AGENTS.md
@.planning/phases/22-passkeys-generator-wiring/22-02-PLAN.md
@.planning/phases/21-passkey-liveviews-post-auth-controller/21-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate shared auth templates with `passkeys?` and remove disabled-path residue</name>
  <files>priv/templates/sigra.install/core/auth.ex, priv/templates/sigra.install/core/session_controller.ex, priv/templates/sigra.install/core/login_html.ex, priv/templates/sigra.install/core/mfa_settings_live.ex, priv/templates/sigra.install/core/mfa_challenge_live.ex, priv/templates/sigra.install/core/registration_html.ex, priv/templates/sigra.install/core/confirmation_controller.ex</files>
  <read_first>.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md, .planning/phases/22-passkeys-generator-wiring/22-PATTERNS.md, priv/templates/sigra.install/core/auth.ex, priv/templates/sigra.install/core/session_controller.ex, priv/templates/sigra.install/core/login_html.ex, priv/templates/sigra.install/core/mfa_settings_live.ex, priv/templates/sigra.install/core/mfa_challenge_live.ex, priv/templates/sigra.install/core/registration_html.ex, priv/templates/sigra.install/core/confirmation_controller.ex</read_first>
  <action>Per D-03, D-05, D-06, and D-10, keep the canonical shared templates but wrap passkey-only aliases, helpers, assigns, controller actions, and UI branches in `passkeys?` guards. Under `--no-passkeys`, do not emit `UserPasskey`, `Sigra.Passkeys`, `Sigra.Plug.PasskeyChallenge`, passkey-primary login branches, passkey settings cards, passkey MFA CTAs, signup passkey enrollment prompts, or confirmation follow-through redirects. Do not generate fake no-op functions or dead helper wrappers. If a shared file becomes unreadable, extract only a small local helper or move that one whole target; do not create a repo-wide variant system.</action>
  <acceptance_criteria>
    - `rg -n "if passkeys\\?" priv/templates/sigra.install/core/auth.ex priv/templates/sigra.install/core/session_controller.ex priv/templates/sigra.install/core/login_html.ex priv/templates/sigra.install/core/mfa_settings_live.ex priv/templates/sigra.install/core/mfa_challenge_live.ex priv/templates/sigra.install/core/registration_html.ex priv/templates/sigra.install/core/confirmation_controller.ex` finds explicit guards in each shared file touched.
    - `rg -n "UserPasskey|Sigra\\.Passkeys|Sigra\\.Plug\\.PasskeyChallenge|passkey_primary_enabled|complete_passkey|passkey_registration_options|passkey_authentication_options" priv/templates/sigra.install/core` shows those references only inside `passkeys?`-guarded regions.
    - `rg -n "def .*passkey" priv/templates/sigra.install/core/auth.ex priv/templates/sigra.install/core/session_controller.ex` does not leave unconditional passkey helper or controller functions behind.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/install/generator_passkeys_foundation_test.exs test/sigra/install/generator_passkey_management_test.exs --max-failures 1</automated>
  </verify>
  <done>Shared auth templates compile cleanly with passkeys both on and off, and the disabled path contains no residual passkey code surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add generated-app omission regression coverage for both disabled passkey legs</name>
  <files>test/sigra/install/generator_passkeys_opt_out_test.exs</files>
  <read_first>.planning/phases/22-passkeys-generator-wiring/22-VALIDATION.md, .planning/phases/22-passkeys-generator-wiring/22-PATTERNS.md, test/sigra/install/generator_passkeys_opt_out_test.exs, test/support/install_fixture.ex</read_first>
  <action>Create or finish `test/sigra/install/generator_passkeys_opt_out_test.exs` as the authoritative omission suite called out in 22-VALIDATION.md. Parameterize it so it generates real apps for both disabled combinations: `--no-passkeys` and `--no-organizations --no-passkeys`. For each leg, use `Sigra.Test.InstallFixture.setup_tmp_app_without_install/1` plus `run_sigra_install/2`, then assert absence of passkey routes, `user_passkeys` migration, `UserPasskey` schema, `passkey_hooks.js`, `passkey_browser.js`, `{:wax_, "~> 0.7"}`, `\"@simplewebauthn/browser\"`, passkey config blocks, and shared-template residue strings such as `Sigra.Passkeys`, `Sigra.Plug.PasskeyChallenge`, and passkey route literals. Keep this suite authoritative for omission risk; do not spread the same disabled-path assertions across many files without need.</action>
  <acceptance_criteria>
    - `test/sigra/install/generator_passkeys_opt_out_test.exs` exists and contains assertions for route absence, file absence, dependency absence, and config absence for both disabled flag combinations.
    - `rg -n "Sigra\\.Passkeys|Sigra\\.Plug\\.PasskeyChallenge|@simplewebauthn/browser|\\{:wax_, \"~> 0\\.7\"\\}" test/sigra/install/generator_passkeys_opt_out_test.exs` finds direct omission checks for all four critical residue classes.
    - `rg -n "--no-passkeys|--no-organizations --no-passkeys|passkeys disabled|opt out" test/sigra/install/generator_passkeys_opt_out_test.exs` shows both disabled legs are explicitly named in the suite.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/install/generator_passkeys_opt_out_test.exs test/sigra/install/generator_passkeys_foundation_test.exs test/sigra/install/generator_passkey_management_test.exs --max-failures 1</automated>
  </verify>
  <done>The repo has a generated-app omission test that fails on any partial-apply leak across both disabled passkey combinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared template guards -> generated controller/UI surface | Conditional template regions must fully remove passkey mutation endpoints and helper references on opt-out. |
| Omission regression tests -> generated host files | The disabled-path suite must inspect the real generated app shape, not only template source fragments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | E | `session_controller.ex` and shared auth templates | mitigate | Guard every passkey session mutation and passkey-primary branch with `passkeys?`, and omit them entirely instead of emitting stubs, closing the session-mutation surface when opted out. |
| T-22-03-02 | I | Shared login/signup/settings templates | mitigate | Remove unconditional passkey aliases, helpers, and UI branches from shared templates so disabled installs do not reference missing passkey modules or routes. |
| T-22-03-03 | R | `generator_passkeys_opt_out_test.exs` | mitigate | Add generated-app omission assertions that directly grep for forbidden residue strings, files, routes, and dependency lines on the disabled path. |
</threat_model>

<verification>
Run `mix test test/sigra/install/generator_passkeys_opt_out_test.exs test/sigra/install/generator_passkeys_foundation_test.exs test/sigra/install/generator_passkey_management_test.exs --max-failures 1`.
</verification>

<success_criteria>
The generated app has honest shared-template shapes across both disabled combinations: passkeys-enabled installs keep the intended auth UX, and both `--no-passkeys` legs omit every passkey-specific branch without dead compatibility shims.
</success_criteria>

<output>
After completion, create `.planning/phases/22-passkeys-generator-wiring/22-03-SUMMARY.md`
</output>
