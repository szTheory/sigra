---
phase: 22-passkeys-generator-wiring
plan: 04
type: execute
wave: 4
depends_on:
  - 22-03
files_modified:
  - test/support/install_fixture.ex
  - scripts/ci/passkeys-opt-out-smoke.sh
  - scripts/ci/install-matrix-local.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PK-02
must_haves:
  truths:
    - "All four organizations x passkeys install combinations compile and boot."
    - "The no-passkeys path is proven in an assets-enabled harness, not only in a `--no-assets` matrix leg."
    - "CI keeps browser/passkey ceremony coverage single-path while omission coverage stays focused on opt-out risk."
  artifacts:
    - "scripts/ci/passkeys-opt-out-smoke.sh proves asset, dependency, route, and boot omission on both `--no-passkeys` combinations."
    - ".github/workflows/ci.yml runs the four install matrix combinations and the dedicated no-passkeys smoke."
    - "scripts/ci/install-matrix-local.sh can reproduce the expanded matrix locally."
  key_links:
    - "CI matrix flags mirror the phase context's required four combinations."
    - "The opt-out smoke harness scaffolds real Phoenix apps for both disabled combinations so package.json and JS omissions are observable."
    - "Matrix and smoke jobs stay separated from passkey-enabled Playwright coverage per D-17."
---

<objective>
Expand verification and CI so the passkey opt-out is proven across the four-way install matrix and with an assets-enabled omission smoke harness.

Purpose: Close the Phase 22 regression loop from D-14 through D-17 by making `--no-passkeys` failures visible in local and CI automation, then finish with the required repository-wide precommit gate.
Output: Dedicated opt-out smoke script, updated local reproduction helper, a four-leg CI matrix plus opt-out smoke job, and the final `mix precommit` gate required by the example app rules.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md
@.planning/phases/22-passkeys-generator-wiring/22-RESEARCH.md
@.planning/phases/22-passkeys-generator-wiring/22-VALIDATION.md
@test/example/AGENTS.md
@.planning/phases/18-backfill-organizations-generator-wiring/18-03-upgrade-test-fixture-and-ci-matrix-SUMMARY.md
@scripts/ci/install-smoke.sh
@scripts/ci/passkeys-manual-fallback-smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an assets-enabled `--no-passkeys` smoke harness</name>
  <files>test/support/install_fixture.ex, scripts/ci/passkeys-opt-out-smoke.sh</files>
  <read_first>.planning/phases/22-passkeys-generator-wiring/22-VALIDATION.md, .planning/phases/18-backfill-organizations-generator-wiring/18-03-upgrade-test-fixture-and-ci-matrix-SUMMARY.md, test/support/install_fixture.ex, scripts/ci/install-smoke.sh, scripts/ci/passkeys-manual-fallback-smoke.sh</read_first>
  <action>Create `scripts/ci/passkeys-opt-out-smoke.sh` as the assets-enabled omission harness described in 22-VALIDATION.md. Parameterize it to run both disabled combinations: `--no-passkeys` and `--no-organizations --no-passkeys`. For each leg, scaffold a fresh Phoenix app without `--no-assets`, patch in the local Sigra path dep, run `mix sigra.install Accounts User users ... --yes`, and then assert: no `assets/js/passkey_hooks.js`, no `assets/js/passkey_browser.js`, no `\"@simplewebauthn/browser\"` in `assets/package.json`, no `{:wax_, "~> 0.7"}` in `mix.exs`, no `passkeys:` config block, no passkey route literals, compile clean, assets build clean, DB migrate clean, and the app boots/responds. Update `InstallFixture` only if a narrow reusable helper materially reduces duplication; keep additions additive like Phase 18's fixture extensions.</action>
  <acceptance_criteria>
    - `scripts/ci/passkeys-opt-out-smoke.sh` exists and contains checks for `passkey_hooks.js`, `passkey_browser.js`, `@simplewebauthn/browser`, `{:wax_, "~> 0.7"}`, and passkey route/config absence for both disabled legs.
    - `rg -n "mix sigra\\.install .*--no-passkeys --yes|--no-organizations --no-passkeys" scripts/ci/passkeys-opt-out-smoke.sh` finds both exact opt-out invocations.
    - `rg -n "assets\\.deploy|mix compile --warnings-as-errors|mix ecto\\.migrate|curl -sf" scripts/ci/passkeys-opt-out-smoke.sh` shows compile, asset, migrate, and boot checks all run in the harness.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/passkeys-opt-out-smoke.sh</automated>
  </verify>
  <done>The repo has a reproducible assets-enabled smoke harness that proves both disabled passkey paths from install through boot.</done>
</task>

<task type="auto">
  <name>Task 2: Expand CI and local matrix reproduction to all four flag combinations</name>
  <files>scripts/ci/install-matrix-local.sh, .github/workflows/ci.yml</files>
  <read_first>.planning/phases/22-passkeys-generator-wiring/22-CONTEXT.md, .planning/phases/22-passkeys-generator-wiring/22-VALIDATION.md, scripts/ci/install-matrix-local.sh, .github/workflows/ci.yml</read_first>
  <action>Update `.github/workflows/ci.yml` so `install_matrix.strategy.matrix.flags` contains exactly `\"\"`, `\"--no-passkeys\"`, `\"--no-organizations\"`, and `\"--no-organizations --no-passkeys\"` per D-14/D-15. Add a dedicated job or step that runs `scripts/ci/passkeys-opt-out-smoke.sh` on CI, keeping passkey-enabled Playwright/browser smoke single-path per D-17. Update `scripts/ci/install-matrix-local.sh` so the default local legs match the expanded four-way matrix and its header comments no longer describe only two legs. Treat this as the closing plan and include the repo-wide `mix precommit` phase gate required by `test/example/AGENTS.md`.</action>
  <acceptance_criteria>
    - `rg -n "\"--no-passkeys\"|\"--no-organizations --no-passkeys\"" .github/workflows/ci.yml` finds both new matrix legs.
    - `rg -n "passkeys-opt-out-smoke\\.sh" .github/workflows/ci.yml` finds a CI invocation of the new harness.
    - `rg -n "LEGS=.*--no-passkeys|--no-organizations --no-passkeys" scripts/ci/install-matrix-local.sh` shows the local helper reproduces all four combinations.
    - `rg -n "Playwright|browser" .github/workflows/ci.yml` does not add the no-passkeys combinations to passkey-enabled browser ceremony jobs.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/ci/install-matrix-local.sh</automated>
  </verify>
  <done>CI and the local matrix helper both exercise the full four-way flag matrix, while the heavier omission smoke and `mix precommit` gates remain in the final plan-level verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI matrix flags -> generated tmp apps | Each flag combination must produce a distinct but valid generated host app. |
| Assets-enabled smoke harness -> host dependency files | Omission of package and JS assets is only observable when the generated app actually has an asset tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | R | `.github/workflows/ci.yml` install matrix | mitigate | Expand the matrix to all four required flag combinations so partial-apply regressions cannot hide in an untested quadrant. |
| T-22-04-02 | I | `scripts/ci/passkeys-opt-out-smoke.sh` | mitigate | Run an assets-enabled harness that directly proves omission of JS files and package dependencies, not just compile success under `--no-assets`. |
| T-22-04-03 | D | CI/browser coverage split | mitigate | Keep no-passkeys verification focused on compile/boot/omission smoke and avoid multiplying expensive Playwright ceremony jobs across disabled combinations. |
</threat_model>

<verification>
Run `mix test test/sigra/install/generator_passkeys_opt_out_test.exs --max-failures 1`, `bash scripts/ci/install-matrix-local.sh`, `bash scripts/ci/passkeys-opt-out-smoke.sh`, and `mix precommit`.
</verification>

<success_criteria>
The disabled passkey path is continuously verified: local helpers, CI matrix legs, an assets-enabled smoke harness, and the repo-wide precommit gate all agree on the four required feature-flag combinations and catch omission regressions early.
</success_criteria>

<output>
After completion, create `.planning/phases/22-passkeys-generator-wiring/22-04-SUMMARY.md`
</output>
