---
phase: 202-audit-surfaces-elevation
plan: 04
type: execute
wave: 3
depends_on: [202-02, 202-03]
files_modified:
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/example/test/example_web/live/admin_audit_index_live_test.exs
autonomous: true
requirements: [AUDIT-02, AUDIT-03]
must_haves:
  truths:
    - "`assertAuditResultEquivalence` still extracts EXACTLY 2 `code.sg-code` tokens from the desktop container after the codes moved into the in-row `<details>` — and FAILS LOUDLY (not vacuously) if it extracts fewer than 2."
    - "All three call sites of `assertAuditResultEquivalence` (gallery MG-6, live /admin/audit, live per-user audit) pass against the elevated desktop markup."
    - "A deterministic ExUnit LiveView test proves the pagination `<nav>` is present at ≥26 events and absent at ≤25 — independent of MIX_ENV=dev seeds."
  artifacts:
    - "test/example/priv/playwright/tests/admin-design.spec.ts updated assertAuditResultEquivalence + strict length guard"
    - "test/example/test/example_web/live/admin_audit_index_live_test.exs new pagination test"
  key_links:
    - "the desktop code.sg-code selector is updated in lockstep with the Wave-2 DOM move (D-06); content-equivalence stays a real proof, not a rubber stamp"
    - "one helper, three call sites — a single selector edit ripples to MG-6 + both live audit checks"
    - "the ExUnit pagination test reuses the existing insert_audit_event/1 seam (no dev seeds)"
---

<objective>
Close the two highest-value test seams the Wave-2 DOM changes require:

1. **D-06 lockstep (HIGHEST RISK):** Update the shared Playwright helper `assertAuditResultEquivalence`
   so it still extracts exactly 2 `code.sg-code` tokens from the desktop results container now that the
   codes live inside the in-row `<details>`, and add a strict `length === 2` guard so under-extraction
   FAILS LOUDLY instead of silently degrading the assertion to a vacuous rubber stamp (the exact 201
   D-06 failure mode). One helper drives THREE call sites — re-run all three.

2. **D-10 pagination proof:** Add a deterministic ExUnit LiveView test asserting the pagination `<nav>`
   renders at ≥26 events and is absent at ≤25, reusing the existing direct-insert seam — so the
   pagination proof does not depend on the `MIX_ENV=dev`-only seeded screenshot.

Purpose: AUDIT-02 (content-equivalence stays a real proof + honest pagination proven) and AUDIT-03
(the content-equivalence and pagination Tier-2 proxy evidence Plan 05 cites in the ledger).

Output: a strengthened equivalence helper (all 3 call sites green) + a new deterministic ExUnit
pagination test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-audit-surfaces-elevation/202-CONTEXT.md
@.planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md
@.planning/phases/202-audit-surfaces-elevation/202-RESEARCH.md
@.planning/phases/202-audit-surfaces-elevation/202-02-SUMMARY.md
@.planning/phases/202-audit-surfaces-elevation/202-03-SUMMARY.md

@test/example/priv/playwright/tests/admin-design.spec.ts
@test/example/test/example_web/live/admin_audit_index_live_test.exs
@lib/sigra/admin/components.ex
</context>

<artifacts_this_phase_produces>
- Updated `assertAuditResultEquivalence` (admin-design.spec.ts:166-178) — desktop `code.sg-code` selector revised to reach inside the `<details>`, PLUS a new strict assertion that the extracted code-token list length is exactly 2 (loud-fail on under-extraction).
- New ExUnit test in `admin_audit_index_live_test.exs`: "pagination nav renders at ≥26 events and is absent at ≤25" using the existing `insert_audit_event/1` helper (:145-161).
No new application code, no new CSS.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lockstep-update assertAuditResultEquivalence + add strict 2-code guard; re-run all 3 call sites</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts:142-178 — `firstTexts/3` and the `assertAuditResultEquivalence` helper (reads `code.sg-code` ×2, `.sg-status-pill` ×2, `td:nth-child(3) span` ×3, and `data-tone` parity).
    - test/example/priv/playwright/tests/admin-design.spec.ts:325-392 — the three call sites: gallery MG-6 (:334), live /admin/audit (:354-361), live per-user audit (:380-388).
    - test/example/priv/playwright/tests/admin-design.spec.ts:642-654 — the SEPARATE `#board-audit_row` count assertion (`code.sg-code` toHaveCount 2) that targets the gallery `audit_row` component (mobile card with show_codes) — verify the Wave-2 changes did NOT disturb it; do not break it.
    - lib/sigra/admin/components.ex — the Wave-1 `audit_table_row/1` Event-cell `<details>` structure (where the two `code.sg-code` nodes now live).
    - .planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md:230-251 and 202-RESEARCH.md Pitfall 1/2 — the lockstep + strict-guard requirement.
  </read_first>
  <action>
    Update `assertAuditResultEquivalence` so its desktop `code.sg-code` extraction reaches the two code nodes now nested inside the Event-cell `<details>`. Because the codes are still real `<code class="sg-code">` text nodes INSIDE `[data-testid="admin-audit-desktop-results"]` (Wave-1 guaranteed this), the existing `firstTexts(desktop, 'code.sg-code', 2)` SHOULD still return 2 — confirm it does against the live elevated markup; if the `<details>` collapsed state hides the nodes from text extraction, adjust the selector to read the `<code>` text regardless of open/closed state (text content of a closed `<details>` is still in the DOM and readable via `textContent`/Playwright `allInnerTexts`), NOT by forcing the disclosure open in a way that changes the screenshot baseline.

    Add a STRICT guard (per Pitfall 1): after extracting the desktop code tokens, assert the extracted code-token count is exactly 2 — e.g. capture the `firstTexts(desktop, 'code.sg-code', 2)` result into a variable and `expect(codeTokens.length, \`${label}: desktop must expose exactly 2 audit codes\`).toBe(2)` BEFORE merging into the token list. This makes under-extraction (codes leaving the desktop container / becoming data-attrs) FAIL LOUDLY instead of silently weakening the equivalence to a vacuous pass. Keep the rest of the helper (status-pill, Actor `td:nth-child(3) span`, `data-tone` parity) unchanged — the 4-column `td:nth-child` order is frozen by Wave 1.

    This helper drives THREE call sites — treat the edit as one lockstep change and run all three (gallery MG-6, live /admin/audit, live per-user audit). Do NOT change the `#board-audit_row` count assertion (:651) — it targets the mobile-card gallery board, which Wave 2 left intact.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example/priv/playwright && npx playwright test admin-design.spec.ts --grep "content-equivalent" 2>&1 | tail -20</automated>
  </verify>
  <done>`assertAuditResultEquivalence` extracts exactly 2 desktop `code.sg-code` tokens and asserts that count strictly; all three call sites (MG-6 + both live audit checks) pass; the `#board-audit_row` count assertion is untouched and still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add deterministic ExUnit pagination test (≥26 → nav present, ≤25 → absent)</name>
  <files>test/example/test/example_web/live/admin_audit_index_live_test.exs</files>
  <read_first>
    - test/example/test/example_web/live/admin_audit_index_live_test.exs:1-60 — the test module setup, the platform-admin fixture, and the existing "preserves URL-driven filters across sort and pagination links" test (it drives `page_size=1`).
    - test/example/test/example_web/live/admin_audit_index_live_test.exs:145-161 — the `insert_audit_event/1` seam (direct Repo insert, no dev seeds).
    - lib/sigra/admin/live/audit_index_live.ex — the hidden `page_size=25` default and the `aria-label="Next page"` nav rendered by the shared `audit_pagination_nav/1` (now in components.ex).
    - .planning/phases/202-audit-surfaces-elevation/202-RESEARCH.md "Recommended NEW deterministic ExUnit pagination test (D-10)" — the test sketch.
  </read_first>
  <action>
    Add a deterministic test "pagination nav renders at >=26 events and is absent at <=25" to `admin_audit_index_live_test.exs`. Using the existing `insert_audit_event/1` helper and the platform-admin fixture, insert 26 self-tied audit events (actor + effective_user = the admin), request `/admin/audit` as that admin, and assert the response HTML contains `aria-label="Next page"` (the `<nav>` rendered because `multi_page?/1` is true at the default `page_size=25`). Then prove the absent case deterministically — either in a fresh sandbox with ≤25 events, or by requesting with a filter/`page_size` that yields a single page — and assert the HTML does NOT contain `aria-label="Next page"`. Keep the test `async: false` (it inserts rows, matching the module's existing ConnCase usage). Do NOT depend on `MIX_ENV=dev` seeds (hard-blocked in test) — the direct-insert seam makes this test-environment-independent.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix test test/example_web/live/admin_audit_index_live_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>The new test passes: with 26 inserted events the page renders the `aria-label="Next page"` nav; with ≤25 (or a single-page filter) the nav is absent. The test uses the existing insert seam and runs without dev seeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → rendered DOM | Playwright/ExUnit read rendered markup; no production trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-202T-01 | Repudiation | content-equivalence proof integrity | high | mitigate | The vacuous-pass risk (codes silently leaving the desktop container → equivalence becomes a rubber stamp) is the central phase risk. Mitigation: the strict `length === 2` guard in Task 1 converts under-extraction into a loud test failure, so the equivalence proxy cannot silently degrade (closes the 201 D-06 failure mode). This is a TEST-integrity mitigation, not a runtime security one. |
| T-202T-02 | Tampering | pagination proof depends on dev seeds | low | mitigate | The ExUnit test inserts its own rows via the existing Repo seam, removing the dependency on the `MIX_ENV=dev`-only seeded screenshot — the pagination proof is deterministic and CI-runnable. |
| T-202-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs — edits existing Playwright spec + ExUnit test files only. No package legitimacy gate applies. |
</threat_model>

<verification>
- `npx playwright test admin-design.spec.ts --grep "content-equivalent"` green (MG-6 + both live audit checks).
- The strict 2-code guard is present in `assertAuditResultEquivalence` (grep for the length assertion).
- `mix test test/example_web/live/admin_audit_index_live_test.exs` green including the new pagination test.
- Manual sanity: temporarily moving a code node out of the desktop container makes the equivalence helper FAIL (loud), not pass — confirm the guard bites (optional, document in SUMMARY).
</verification>

<success_criteria>
- Content-equivalence is a real proof: exactly 2 desktop codes extracted, strictly asserted, all 3 call sites green.
- Pagination is proven deterministically in ExUnit at the ≥26/≤25 boundary, independent of dev seeds.
</success_criteria>

<output>
Create `.planning/phases/202-audit-surfaces-elevation/202-04-SUMMARY.md` when done.
</output>
