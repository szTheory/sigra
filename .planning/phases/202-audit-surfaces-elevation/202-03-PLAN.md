---
phase: 202-audit-surfaces-elevation
plan: 03
type: execute
wave: 2
depends_on: [202-01]
files_modified:
  - lib/sigra/admin/live/audit_index_live.ex
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
must_haves:
  truths:
    - "The global audit page wraps its text/date advanced fields in a native CSS-only `<details>` disclosure, byte-coherent with the per-user page's disclosure."
    - "The page renders its desktop table via the shared `audit_table_row/1`, pagination via `audit_pagination_nav/1`, and empty-state via `audit_empty_state/1`; the private `audit_tone/1` copy is deleted."
    - "Export CSV stays in the consolidated filter action row; the single `sg-filter-panel` form and 6-key `@chip_keys` (incl. actor/effective_user) are preserved."
  artifacts:
    - "lib/sigra/admin/live/audit_index_live.ex with <details> disclosure + shared-component calls, dup helpers deleted"
  key_links:
    - "desktop results container keeps data-testid=admin-audit-desktop-results wrapping the shared rows so code.sg-code (×2) stays extractable for the equivalence helper"
    - "GET-form / URL-driven contract preserved; quick chips stay GET checkboxes, applied chips stay navigation-only <a>"
    - "shared <details> + <table>/thead choice matches the per-user page (Plan 02) so shared markup is byte-coherent"
---

<objective>
Rewire the global audit page (`audit_index_live.ex` — already a single `sg-filter-panel` form) to add
the same native `<details>` advanced-disclosure as the per-user page and to render its desktop table /
pagination / empty-state through the Wave-1 shared components, deleting the now-duplicate private
helpers. The index page is the lighter half of the AUDIT-01/02 surgery (it is already single-form with
Export in the action row); the work here is disclosure + shared-component adoption + byte-coherence.

Purpose: AUDIT-01 (advanced-disclosure folded in) and AUDIT-02 (byte-coherent shared markup + density
reduction) for the global audit surface.

Output: `audit_index_live.ex` with a `<details>` disclosure and shared-component calls, byte-coherent
with `audit_user_live.ex` in shared markup, with duplicate helpers removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-audit-surfaces-elevation/202-CONTEXT.md
@.planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md
@.planning/phases/202-audit-surfaces-elevation/202-UI-SPEC.md
@.planning/phases/202-audit-surfaces-elevation/202-01-SUMMARY.md

@lib/sigra/admin/live/audit_index_live.ex
@lib/sigra/admin/live/audit_user_live.ex
@lib/sigra/admin/components.ex
</context>

<artifacts_this_phase_produces>
No new public symbols. This plan REMOVES duplication and ADDS a disclosure:
- Adds a `<details>` advanced-disclosure wrapping the existing `sg-form-grid` advanced fields.
- Deletes the private `audit_tone/1` (audit_index_live.ex:244-246) and any private `multi_page?/1` (:309-313) / `format_timestamp/1` (:315-318) now owned by Wave-1 shared components.
- Replaces the hand-written desktop `<tbody>` `<tr>` loop, `<nav>`, and empty-state with shared-component calls.
Helpers that STAY (legitimate per-page divergence, D-09): scope `<header class="sg-page-header">`, 6-key `@chip_keys` (incl. actor/effective_user), the Effective-user filter field, and all index routing helpers (`index_path/1`, `sort_path/3`, `page_path/3`, `remove_chip_path/3`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add &lt;details&gt; advanced-disclosure to the global audit filter form</name>
  <files>lib/sigra/admin/live/audit_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_index_live.ex:58-137 — the single `sg-filter-panel` form: quick chips :59-80, advanced `sg-form-grid` :82-126, Export action row :128-132.
    - lib/sigra/admin/live/audit_index_live.ex:88-91,119,124 — the Effective-user field and the `from`/`to` `type="date"` inputs (the per-user page converges to these).
    - lib/sigra/admin/live/audit_user_live.ex — Plan 02's `<details>` markup (match its summary wording and structure for byte-coherence).
    - .planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md:189-208 and 202-UI-SPEC.md:102 — the disclosure contract and "More filters" summary copy.
  </read_first>
  <action>
    Wrap the advanced text/date fields (the `sg-form-grid` block at :82-126) in a native CSS-only `<details><summary>More filters</summary>...</details>` (or the exact glossary-clean summary wording chosen in Plan 02 — the two MUST match byte-for-byte). Keep the quick-chip cluster (:59-80) OUTSIDE the disclosure as the always-visible summary controls (D-01/D-02). Keep the Effective-user field inside the disclosure (index-only field — legitimate per-page divergence, D-09, but still byte-coherent in the shební disclosure wrapper). No `phx-hook`/`phx-click`; browser-default `<details>`; no chevron / new `sg-*` class (D-13; target zero new CSS). Keep Export CSV in the action row (:128-132 — already there, D-04). The single `<form method="get">` and every named input inside it stay intact (D-03).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>The global audit advanced fields are inside a `<details>` whose `<summary>` matches the per-user page byte-for-byte; quick chips stay outside; Export stays in the action row; single GET form intact; no new `sg-*` class.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire desktop table / pagination / empty-state to shared components and delete dup helpers</name>
  <files>lib/sigra/admin/live/audit_index_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_index_live.ex:151-236 — desktop results container (data-testid=admin-audit-desktop-results :151, sg-show-desktop), `<thead>` :155-160, hand-written `<tbody>` `<tr>` loop :163-191, `<nav>` :216-236.
    - lib/sigra/admin/live/audit_index_live.ex:199-214 — mobile results container (data-testid=admin-audit-mobile-results) + empty-state.
    - lib/sigra/admin/live/audit_index_live.ex:244-246 (private audit_tone/1), :309-313 (multi_page?/1), :315-318 (format_timestamp/1) — duplicates to delete.
    - lib/sigra/admin/components.ex — Wave-1 audit_table_row/1, audit_pagination_nav/1, audit_empty_state/1 signatures (from 202-01-SUMMARY.md), and whether Plan 01 shared the whole `<table>` or just the `<tr>`.
  </read_first>
  <action>
    Replace the hand-written `<tbody>` `<tr>` loop (:163-191) inside the `data-testid="admin-audit-desktop-results"` container with `<.audit_table_row :for={row <- @rows} row={row} />`. Keep the `<thead>` here (or share the whole `<table>` if Plan 01 chose that — match the per-user page exactly for byte-coherence). The desktop container's `data-testid` and `sg-table-panel sg-show-desktop` wrapper stay as-is so the two `code.sg-code` nodes (now inside the shared row's `<details>`) remain inside the desktop results container for `assertAuditResultEquivalence`.

    Replace the `<nav>` (:216-236) with `<.audit_pagination_nav meta={@meta} prev_href={...} next_href={...} />`, building hrefs from the existing index `page_path/3` (no user_id — index routing divergence stays here, D-09).

    Replace the inline empty-state (:205-214) with `<.audit_empty_state title="No audit events match this view">...</.audit_empty_state>` carrying the filter-aware index body + "Clear all filters" link (index copy from 202-UI-SPEC.md:105-106).

    Keep the mobile card list (`data-testid="admin-audit-mobile-results"`) rendering `<.audit_row show_detail show_codes>` (D-07; already canonical).

    DELETE the now-dead private helpers: `audit_tone/1` (:244-246), `multi_page?/1` (:309-313 — owned by the shared nav), and `format_timestamp/1` (:315-318) if the shared row owns timestamp formatting. Keep the index-specific helpers (scope header, 6-key `@chip_keys`, Effective-user field, `index_path/1`, `sort_path/3`, `page_path/3`, `remove_chip_path/3`) — D-09.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors 2>&1 | tail -5 && cd /Users/jon/projects/sigra && mix test test/sigra/admin/glossary_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>Global audit desktop table renders via `audit_table_row/1`, pagination via `audit_pagination_nav/1`, empty-state via `audit_empty_state/1`; private `audit_tone/1`, `multi_page?/1`, `format_timestamp/1` deleted; mobile card unchanged; glossary_test passes; the shared markup emitted matches the per-user page byte-for-byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → global audit LiveView | Admin authorization plug gates `/admin/audit` (existing, unchanged). |
| GET query string → `handle_params/3` | URL filter params are the only state input; parsed/scoped by `Explorer`/`query_params.ex` (unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-202I-01 | Spoofing | filter-param injection into Ecto query | low | accept | Filter params (`actor`, `effective_user`, `action_prefix`, `outcome`, `from`, `to`) flow through the existing parameterized `Explorer`/`query.ex` layer, untouched by this UI phase. No raw SQL interpolation introduced. |
| T-202I-02 | Information Disclosure | inline-code disclosure on a global surface | low | accept | Event id + action code were already primary columns on this admin-gated page; deferring them behind a CSS-only `<details>` changes presentation, not audience or authorization. CSV `event_id` contract unaffected (separate controller path). |
| T-202I-03 | Tampering | HEEx output | low | mitigate | All row/filter values render through HEEx auto-escaping; no `raw/1`. |
| T-202-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs nothing — pure in-repo Elixir/HEEx changes. No package legitimacy gate applies. |
</threat_model>

<verification>
- `cd test/example && mix compile --warnings-as-errors` clean.
- `mix test test/sigra/admin/glossary_test.exs` passes.
- The `<details>` summary text and the shared table/nav/empty-state markup match `audit_user_live.ex` byte-for-byte (diff the shared regions).
- The two `code.sg-code` nodes still live inside `[data-testid="admin-audit-desktop-results"]`.
- No new `sg-*` class (grep changed HEEx for `sg-chevron` → 0).
</verification>

<success_criteria>
- The global audit page has a `<details>` advanced-disclosure and renders desktop table / pagination / empty-state via the Wave-1 shared components.
- Export stays in the action row; the single GET form, 6-key `@chip_keys`, and Effective-user field are preserved.
- Duplicate private helpers removed; shared markup byte-coherent with the per-user page.
</success_criteria>

<output>
Create `.planning/phases/202-audit-surfaces-elevation/202-03-SUMMARY.md` when done.
</output>
