---
phase: 202-audit-surfaces-elevation
plan: 02
type: execute
wave: 2
depends_on: [202-01]
files_modified:
  - lib/sigra/admin/live/audit_user_live.ex
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
must_haves:
  truths:
    - "The per-user audit page emits exactly ONE `<form>` (the `sg-filter-panel`), not three; the Failures and Impersonation quick toggles are GET checkboxes inside that single form."
    - "Text/date filter fields fold into a native CSS-only `<details>` advanced-disclosure; quick toggles stay visible outside it."
    - "Export CSV stays in the consolidated filter action row (not relocated)."
    - "The `return_to` hidden input survives exactly once and the return_to round-trip still works across filter/sort/page/chip/clear/export links."
    - "The page renders its desktop table via the shared `audit_table_row/1`, pagination via `audit_pagination_nav/1`, and empty-state via `audit_empty_state/1`; the private `audit_tone/1` copy is deleted."
  artifacts:
    - "lib/sigra/admin/live/audit_user_live.ex with 1 form, <details> disclosure, shared-component calls, return_to plumbing intact"
  key_links:
    - "GET-form / URL-driven contract: handle_params/3 stays the only state path; toggles are GET checkboxes, chips are navigation-only <a>, no phx-click"
    - "The ?action_prefix=admin.impersonation checkpoint entry path still sets the Impersonation chip :checked"
    - "desktop results container keeps data-testid=admin-audit-user-desktop-results wrapping the shared rows so code.sg-code stays extractable"
---

<objective>
Primary surgery of the phase: collapse `audit_user_live.ex` from THREE forms (two standalone
quick-toggle forms above the main filter form) into ONE coherent `sg-filter-panel` GET form matching
the index page, add a native `<details>` advanced-disclosure for the text/date fields, and rewire the
desktop table / pagination / empty-state to the Wave-1 shared components — while preserving the
`return_to` round-trip and the GET-form/URL-driven contract exactly.

Purpose: AUDIT-01 (single form + advanced-disclosure + Export in action row) and AUDIT-02
(byte-coherent shared markup + density reduction) for the per-user audit surface.

Output: `audit_user_live.ex` with one filter form, a `<details>` disclosure, shared-component calls,
and deleted duplicate helpers — byte-coherent with `audit_index_live.ex` in shared markup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-audit-surfaces-elevation/202-CONTEXT.md
@.planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md
@.planning/phases/202-audit-surfaces-elevation/202-UI-SPEC.md
@.planning/phases/202-audit-surfaces-elevation/202-01-SUMMARY.md

@lib/sigra/admin/live/audit_user_live.ex
@lib/sigra/admin/live/audit_index_live.ex
@lib/sigra/admin/components.ex
</context>

<artifacts_this_phase_produces>
No new public symbols. This plan REMOVES duplication:
- Deletes the two standalone quick-toggle `<form>` elements (audit_user_live.ex:81-108).
- Deletes the private `audit_tone/1` (audit_user_live.ex:273-275) and any private `multi_page?/1` / `format_timestamp/1` now owned by the Wave-1 shared components.
- Replaces the hand-written desktop `<tbody>` `<tr>` loop, `<nav>`, and empty-state with shared-component calls.
Helpers that STAY (legitimate per-page divergence, D-09): breadcrumbs, page header, `return_to` plumbing, `clear_path`/`export_params`, 5-key `@chip_keys`, and all per-user routing helpers (`index_path/2`, `sort_path`, `page_path/4`, `remove_chip_path/5`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Collapse 3 forms into 1 with folded-in toggles + &lt;details&gt; disclosure</name>
  <files>lib/sigra/admin/live/audit_user_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_user_live.ex:81-164 — the three forms to collapse (two quick-toggle forms :82-94 and :95-107; main filter form :110-164) and the return_to hidden inputs (:83, :96, :160).
    - lib/sigra/admin/live/audit_index_live.ex:58-137 — the canonical single-form target shape: quick-chip cluster :59-80, advanced sg-form-grid :82-126, Export action row :128-132.
    - .planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md:137-186 — the form-collapse analog with the exact folded-in checkbox markup and the return_to/GET-form constraints.
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts:349-368 — the checkpoint that enters via ?action_prefix=admin.impersonation, asserts the Impersonation chip :checked, and asserts Export CSV visible.
  </read_first>
  <action>
    Rewrite the filter region of `audit_user_live.ex` so it is a SINGLE `<form method="get" action={index_path(@admin_scope, @detail.user.id)} class="sg-filter-panel sg-stack">`:

    1. DELETE both standalone quick-toggle forms (:82-94, :95-107). Move the Failures and Impersonation toggles INTO the single form as GET checkboxes, copying the quick-chip cluster pattern from audit_index_live.ex:59-80 — a `<div class="sg-cluster">` holding two `<label class="sg-filter-chip">` checkboxes: `name="outcome" value="failure"` checked when `param_value(@current_params, "outcome") == "failure"`, and `name="action_prefix" value="admin.impersonation"` checked when `param_value(@current_params, "action_prefix") == "admin.impersonation"`. Keep them visible OUTSIDE the disclosure as the always-visible summary controls (per D-01/D-02). Preserve the exact `label.sg-filter-chip:has(input[name="action_prefix"][value="admin.impersonation"])` structure the checkpoint selector depends on, and the visible text "Impersonation" / "Failures".

    2. Wrap the text/date advanced fields in a native CSS-only `<details>` with a `<summary>` of "More filters" (or the glossary-clean wording in 202-UI-SPEC Copywriting Contract). No `phx-hook`, no `phx-click`, no client JS — browser-default `<details>`. Use the existing `sg-form-grid` chrome for the fields inside. Do NOT add a chevron / new `sg-*` class (D-13 triple-copy gate; target zero new CSS).

    3. Keep Export CSV (the existing `export_path` `<a>` at :155-157) in the action row alongside `Apply filters` (`sg-btn--primary`) and `Clear` (`sg-btn--ghost`) — do NOT relocate Export (D-04).

    4. `return_to` hidden input (`<input :if={@return_to} type="hidden" name="return_to" value={@return_to} />`) MUST survive EXACTLY ONCE inside the single form (the :160 conditional one is canonical; the two copies at :83/:96 die with their forms). Every named filter input stays inside the form. Toggles remain checkboxes (never phx-click) so deep-linking, the ?action_prefix=admin.impersonation checkpoint entry, the :checked-state assertion, and the return_to round-trip all keep working (D-03, Pitfall 4).

    Per RESEARCH Open Question 1 (Claude's Discretion): the per-user `from`/`to` inputs are currently `type="text"` while index uses `type="date"`. Converge the per-user fields to `type="date"` to match the index advanced-disclosure fieldset (coherence win, removes one divergence) — unless that breaks an existing test, in which case keep `type="text"` and note it in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>Per-user page emits exactly one `<form>`; both toggles are GET checkboxes inside it with the impersonation-chip selector structure intact; text/date fields are inside a `<details>`; Export stays in the action row; `return_to` hidden input appears exactly once; no `phx-click`/`phx-hook` on toggles; no new `sg-*` class.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire desktop table / pagination / empty-state to shared components and delete dup helpers</name>
  <files>lib/sigra/admin/live/audit_user_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_user_live.ex:180-266 — the desktop results container (data-testid=admin-audit-user-desktop-results :180), the hand-written `<tbody>` `<tr>` loop (:193-220), and the `<nav>` (:246-266) to replace.
    - lib/sigra/admin/live/audit_user_live.ex:273-275 (private audit_tone/1) and :475-479 (private multi_page?/1) — duplicates to delete.
    - lib/sigra/admin/live/audit_user_live.ex:403-409 — the per-user `page_path/4` (user_id + return_to) used to build the pagination hrefs to pass into the shared nav.
    - lib/sigra/admin/components.ex — the Wave-1 audit_table_row/1, audit_pagination_nav/1, audit_empty_state/1 signatures (from 202-01-SUMMARY.md).
  </read_first>
  <action>
    Replace the hand-written desktop table body inside the `data-testid="admin-audit-user-desktop-results"` container with `<.audit_table_row :for={row <- @rows} row={row} />`. Per the CANONICAL Wave-1 decision (202-01): the shared component is `audit_table_row/1` (the `<tr>` only) — keep this page's own `<thead>` per-page; the `<thead>` is NOT shared. Key off the Wave-1 output, NOT off what audit_index_live.ex/Plan 03 does. The desktop results container's `data-testid` and the surrounding `sg-table-panel sg-show-desktop` wrapper stay exactly as-is so the codes (now inside the shared row's `<details>`) remain inside the desktop container for the equivalence helper.

    Replace the `<nav>` (:246-266) with `<.audit_pagination_nav meta={@meta} prev_href={...} next_href={...} />`, building the prev/next hrefs from the existing per-user `page_path/4` (passing `@detail.user.id` and `@return_to`) so the per-user routing divergence stays in this LiveView (D-09).

    Replace the inline empty-state (:234-244) with `<.audit_empty_state title="No audit events for this user">...</.audit_empty_state>` (per-user copy from 202-UI-SPEC.md:107), keeping any per-user body wording.

    Keep the mobile card list (`data-testid="admin-audit-user-mobile-results"`) rendering `<.audit_row show_detail show_codes>` — D-07 requires the disclosed codes render in the mobile expansion too; this is already the canonical mobile presentation, leave it.

    DELETE the now-dead private helpers: this LiveView's local `audit_tone/1` (:273-275 — the shared `audit_table_row/1` calls the existing private `audit_tone/1` co-located in `components.ex`, NOT a public `Components.audit_tone/1`; do NOT call `Components.audit_tone(...)` from this LiveView, it is private and would fail to compile) and `multi_page?/1` (:475-479 — now owned by `audit_pagination_nav/1`). Also delete any private `format_timestamp/1` if the shared row owns timestamp formatting. Keep all per-page routing/breadcrumb/return_to/chip helpers (D-09).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors 2>&1 | tail -5 && cd /Users/jon/projects/sigra && mix test test/sigra/admin/glossary_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>Per-user desktop table renders via `audit_table_row/1`, pagination via `audit_pagination_nav/1`, empty-state via `audit_empty_state/1`; private `audit_tone/1` and `multi_page?/1` deleted; mobile card still uses `<.audit_row show_detail show_codes>`; glossary_test passes; compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → per-user audit LiveView | Admin authorization plug + per-user scope (`subject_user_id`) gate the route (existing, unchanged). |
| GET query string → `handle_params/3` | URL filter params (`outcome`, `action_prefix`, `actor`, `from`, `to`, `cursor`, `page_size`, `return_to`) are the only state input; validated/scoped by `Explorer`/`query_params.ex` (unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-202U-01 | Tampering | `return_to` GET param | medium | mitigate | `return_to` is already sanitized via `sanitize_return_to/3` (audit_user_live.ex:293-302, falls back to a default scoped path). This plan does NOT change sanitization; it only ensures the hidden input survives exactly once. Preserving the existing sanitizer is the mitigation — do not bypass it. |
| T-202U-02 | Spoofing | filter-param injection into Ecto query | low | accept | Filter params flow through the existing `Explorer`/`query.ex`/`query_params.ex` parameterized query layer, untouched by this UI phase (D-10: data/query layer stays functionally intact). No raw interpolation introduced. |
| T-202U-03 | Information Disclosure | scope correctness (org-scoped admin) | low | accept | Per-user scope and org fail-closed behavior live in `Explorer.list_subject_events/4`, unchanged; the form-collapse does not alter which events are loaded (existing test: "organization explorer fails closed outside the resolved organization scope"). |
| T-202-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs nothing — pure in-repo Elixir/HEEx changes. No package legitimacy gate applies. |
</threat_model>

<verification>
- `cd test/example && mix compile --warnings-as-errors` clean.
- `mix test test/sigra/admin/glossary_test.exs` passes (per-user file stays glossary-clean).
- Exactly one `<form` in the render (grep the render block).
- `return_to` hidden input appears exactly once.
- No `phx-click` / `phx-hook` on the quick toggles.
- No new `sg-*` class (grep changed HEEx for `sg-chevron` → 0).
</verification>

<success_criteria>
- The per-user audit page is a single coherent `sg-filter-panel` form with folded-in GET-checkbox toggles + a `<details>` advanced-disclosure.
- Export stays in the action row; `return_to` round-trip preserved.
- Desktop table / pagination / empty-state render via the Wave-1 shared components; duplicate private helpers deleted.
- Byte-coherent with `audit_index_live.ex` in the shared markup it now emits.
</success_criteria>

<output>
Create `.planning/phases/202-audit-surfaces-elevation/202-02-SUMMARY.md` when done.
</output>
