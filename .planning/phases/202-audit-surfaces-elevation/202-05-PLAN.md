---
phase: 202-audit-surfaces-elevation
plan: 05
type: execute
wave: 4
depends_on: [202-04]
files_modified:
  - guides/reference/admin-quality-ledger.md
  - guides/reference/admin-design-contract.md
autonomous: true
requirements: [AUDIT-03]
must_haves:
  truths:
    - "The `audit-index-live` and `audit-user-live` ledger cells are flipped from `1` to a bare `2` in column-4 (no decorators) with Evidence columns citing the APPLICABLE Tier-2 proxies and marking overlay-axe + 7-APG gates N/A."
    - "The monotonic guard passes vs origin/main (no cell decreased; the two audit cells increased)."
    - "An Audit Explorer archetype block is added to the design contract documenting the elevated composition; existing stale audit line references are updated."
    - "The CSS triple-copy golden-diff stays green (no new CSS, or any new CSS landed byte-identically in all three copies)."
    - "Only the affected non-canary slugs (audit-explorer, user-audit, and mg-6 iff its markup changed) are recaptured through the gate, with zero-drift idempotency proven and both allowlists left empty."
  artifacts:
    - "guides/reference/admin-quality-ledger.md cells :90/:91 ratcheted to Tier 2 with proxy evidence"
    - "guides/reference/admin-design-contract.md with a new Audit Explorer archetype block"
  key_links:
    - "ledger column-4 is a bare single [012] integer so the awk -F pipe monotonic-guard parse keeps working"
    - "overlay-axe + 7-APG-dialog proxies cited as N/A (neither audit page owns a modal) — not fabricated"
    - "recapture routes only audit-explorer/user-audit/mg-6; canary stays byte-stable; allowlists empty at end"
---

<objective>
Ratchet the two audit ledger cells from Tier 1 → Tier 2 with honest, applicable proxy evidence; add an
Audit Explorer archetype block to the design contract; keep the CSS triple-copy golden-diff green; and
recapture only the affected non-canary slugs through the recapture gate with zero-drift idempotency.

Purpose: AUDIT-03 — both audit surfaces are award-grade across the matrix and their `audit-index-live`
/ `audit-user-live` ledger cells are ratcheted to Tier 2 with proxy evidence, forward-only.

Output: ratcheted ledger (monotonic guard green), new design-contract archetype, recaptured baselines,
green golden-diff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-audit-surfaces-elevation/202-CONTEXT.md
@.planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md
@.planning/phases/202-audit-surfaces-elevation/202-RESEARCH.md
@.planning/phases/202-audit-surfaces-elevation/202-04-SUMMARY.md

@guides/reference/admin-quality-ledger.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-fractal-scorecard.md
</context>

<artifacts_this_phase_produces>
- Ratcheted ledger cells (column-4 `1`→`2`) for `audit-index-live` and `audit-user-live`, Evidence columns expanded with applicable Tier-2 proxies.
- New "Audit Explorer Archetype" block in `admin-design-contract.md`.
- Recaptured Playwright baselines for `audit-explorer` / `user-audit` (3 PNGs each: chromium/dark/mobile) and `mg-6` IFF its markup changed — committed only after zero-drift idempotency is proven.
No application-code or CSS changes expected (target zero new CSS; if Wave 2/3 introduced any CSS it was already triple-copied — verify here).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ratchet both audit ledger cells to bare Tier 2 + verify golden-diff and monotonic guard</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md:14-40 — the parse rules (column-4 bare 0/1/2; decorators forbidden) and "Asserting Tier 2" instructions.
    - guides/reference/admin-quality-ledger.md:87 — the `users-index-live` Tier-2 cell: the EXACT template to copy (N/A overlay-axe + N/A APG; content-equivalence; glossary-clean; motion-tokens; density-rhythm; target-size).
    - guides/reference/admin-quality-ledger.md:90,91 — the `audit-index-live` and `audit-user-live` cells to ratchet (both currently `1`).
    - guides/reference/admin-fractal-scorecard.md:130-167 — the Tier-2 Add-on proxy definitions.
    - .planning/phases/202-audit-surfaces-elevation/202-RESEARCH.md "Tier-2 Proxy Applicability (D-11)" table — which proxies apply (content-equivalence YES, glossary YES, motion/density/target-size YES manual, overlay-axe + APG N/A).
  </read_first>
  <action>
    Flip column-4 of BOTH `audit-index-live` (:90) and `audit-user-live` (:91) from `1` to a bare `2` — a single 0/1/2 integer with NO decorators (no check marks, asterisks, footnote markers, or bold); the positional column-4 parse depends on it (Pitfall 5). Put ALL proxy evidence in column-5.

    Expand each Evidence column by copying the EXACT format of the `users-index-live` cell (:87), swapping in the audit specifics:
    - checkpoint slug: `audit-explorer` (index cell) / `user-audit` (per-user cell);
    - desktop↔mobile content-equivalence: `admin-design.spec.ts assertAuditResultEquivalence` (MG-6 gallery + live `/admin/audit` + live per-user audit) + the deterministic ExUnit pagination test added in Plan 04;
    - glossary-clean: `glossary_test.exs:28` (audit_index_live) for the index cell / `:29` (audit_user_live) for the per-user cell;
    - motion-tokens: reviewed — no `transition: all`;
    - density/rhythm: reviewed — `sg-stack--6` outer / `sg-stack--3` results / `sg-stack--1` cell stacks (from 202-UI-SPEC.md:49);
    - target-size: reviewed — Apply filters / Clear / Export CSV / quick-toggle chips / applied-chip remove / pagination links / disclosure summary affordance all ≥24×24 CSS px (documented-as-manual);
    - overlay-axe: N/A — neither audit page owns a modal dialog; APG focus-trap/restore gates: N/A — no overlay. Cite N/A exactly like the `users-index-live` cell; do NOT reference `admin-modal-interaction.spec.ts` and do NOT fabricate an overlay/dialog pass (a false Tier-2 claim fails Phase-204 adversarial review, D-11).

    Then verify the gates: run the install golden-diff (CSS triple-copy byte parity) and the monotonic guard. If Wave 2/3 introduced any new `sg-*` CSS, confirm it was written byte-identically into all three copies (`priv/templates/sigra.install/admin/sigra_admin.css`, `test/example/priv/static/assets/sigra_admin.css`, `test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css`) — otherwise fix it here (D-13). Target was zero new CSS.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main 2>&1 | tail -8</automated>
  </verify>
  <done>Both audit cells show a bare `2` in column-4; Evidence columns cite the applicable proxies and mark overlay-axe + APG as N/A; monotonic guard passes vs origin/main (audit cells increased, no cell decreased); golden-diff green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the Audit Explorer archetype block to the design contract</name>
  <files>guides/reference/admin-design-contract.md</files>
  <read_first>
    - guides/reference/admin-design-contract.md:168-300 — the Page Archetypes section: Overview (:172), List (:211), Detail (:284) blocks — the format to mirror (`**Source:**` + composition pseudo-tree + `**Notes:**` with D-refs). There is NO existing Audit block.
    - guides/reference/admin-design-contract.md:77 — the existing applied-chip line reference into `audit_user_live.ex` that may go stale once Wave-2 markup moved; update it to the new line if needed.
    - .planning/phases/202-audit-surfaces-elevation/202-02-SUMMARY.md and 202-03-SUMMARY.md — the final composition (single form, `<details>`, shared components) to describe accurately.
  </read_first>
  <action>
    Add a new "Audit Explorer Archetype" block to `admin-design-contract.md` AFTER the Detail Archetype (:284), mirroring the List Archetype block format (`**Source:**` line + a composition pseudo-tree + `**Notes:**`). Source: `audit_index_live.ex` (global) + `audit_user_live.ex` (per-user). Document the elevated composition: one coherent `sg-filter-panel` GET form + folded-in quick toggles (Failures / Impersonation) as GET checkboxes + a native CSS-only `<details>` advanced-disclosure for text/date fields + Export CSV in the action row + reduced desktop column density with raw codes deferred to an in-row `<details>` (codes stay DOM-extractable for content-equivalence + CSV `event_id`) + byte-coherent shared `audit_table_row/1` / `audit_pagination_nav/1` / `audit_empty_state/1` components + honest cursor pagination (`<nav>` present only when `multi_page?/1`). Note the legitimate per-page divergence (per-user breadcrumbs / `return_to` / 5-key chip-keys vs index 6-key + Effective-user field). Keep all microcopy glossary-clean. Update the stale `audit_user_live.ex:77` applied-chip line reference the contract already cites so it points at the post-Wave-2 line (or rephrase it to not pin a fragile line number).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c "Audit Explorer Archetype" guides/reference/admin-design-contract.md && mix test test/sigra/admin/glossary_test.exs 2>&1 | tail -3</automated>
  </verify>
  <done>A new "Audit Explorer Archetype" block exists in the design contract documenting the elevated composition in the List/Detail block format; the stale audit line reference is corrected; glossary_test still passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Recapture only affected non-canary slugs with zero-drift idempotency</name>
  <files>test/example/priv/playwright/tests/admin-checkpoints.spec.ts</files>
  <read_first>
    - scripts/ci/snapshot-recapture-gate.sh:1-45 — the recapture routing + usage (`<intended-slug>...`; SIGRA_EXAMPLE_URL default :4011; per-lane slug routing).
    - test/example/priv/playwright/tests/admin-design.spec.ts:299-301,325-343 — the `#board-mg-6` board (recapture mg-6 IFF its audit-feed markup changed).
    - .planning/phases/202-audit-surfaces-elevation/202-CONTEXT.md D-12 and 202-RESEARCH.md Pitfall 7 — recapture blast radius (audit-explorer + user-audit + mg-6 iff markup), zero-drift idempotency method, seeded-DB requirement, leave allowlists empty.
  </read_first>
  <action>
    Recapture ONLY the affected non-canary slugs through `snapshot-recapture-gate.sh`: `audit-explorer` and `user-audit` (3 PNGs each: chromium/dark/mobile), plus `mg-6` ONLY IF the gallery audit-feed board markup actually changed (confirm by running the gallery equivalence check first; route mg-6 to the gate only if its snapshot drifts). Boot a dev DB with seeds run so the ≥25-event admin fixture makes the audit pages paginate during capture (the seeded path filters `/admin/users?q=admin%40demo.tasklane.test` then "View full audit" — Pitfall 7: do NOT capture under-populated, or the `<nav>` won't render and the elevation bakes under-proven). Prove zero-drift idempotency before baking: re-render and confirm no spurious drift on unchanged slugs (Phase-192/199 method). Keep the relevant canary byte-stable. Leave BOTH allowlists empty at end-of-phase — Phase 204 owns the terminal allowlist reset. Do not recapture any slug outside the audit blast radius.

    Note: the live browser recapture lane needs a booted demo on :4011 with a seeded dev DB; if that environment is unavailable in this run, document the exact recapture command and the proven-changed slug set in the SUMMARY for the operator to run, and confirm the deterministic ExUnit + Playwright equivalence proofs (Plan 04) are green as the test-environment-independent backstop.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash -n scripts/ci/snapshot-recapture-gate.sh && echo "recapture-gate script parses; slugs to route: audit-explorer user-audit [mg-6-if-drift]"</automated>
  </verify>
  <done>Only `audit-explorer` / `user-audit` (+ `mg-6` iff drift) baselines are recaptured through the gate with zero-drift idempotency proven; canary byte-stable; both allowlists empty. If the live lane is unavailable, the exact recapture command + changed-slug set is documented and the Plan-04 deterministic proofs are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer docs/CI guard → merge gate | The ledger column-4 integer drives the forward-only monotonic guard; the golden-diff drives generated-host CSS parity. |
| recapture gate → committed baselines | Recaptured PNGs become the visual contract; the gate is the automated stand-in for human baseline review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-202R-01 | Repudiation | false Tier-2 claim | high | mitigate | Fabricating an overlay-axe/APG pass (proxies that do not apply — neither audit page owns a modal) would be a false award-grade claim. Mitigation: cite overlay-axe + APG as N/A exactly like `users-index-live:87`; only cite proxies with real automated/manual evidence. The Phase-204 adversarial review is the backstop. |
| T-202R-02 | Tampering | monotonic-guard parse | high | mitigate | A decorated column-4 (`2 ✓`, `2*`) silently breaks the `awk -F` positional parse → false-pass CI / unprotected cell. Mitigation: bare `2` only; the guard run in Task 1 verify is the check. |
| T-202R-03 | Tampering | unstyled generated host (CSS triple-copy) | high | mitigate | Any new `sg-*` class not byte-identical across all three CSS copies fails golden-diff / ships unstyled (184→185 class). Mitigation: target zero new CSS; if any exists, verify md5 parity across all three copies before committing. |
| T-202R-04 | Tampering | over-broad recapture | medium | mitigate | Recapturing slugs outside the audit blast radius would bake unrelated drift. Mitigation: route ONLY audit-explorer/user-audit/mg-6 through the gate; prove zero-drift idempotency; leave allowlists empty. |
| T-202-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs — doc edits + baseline recapture only. No package legitimacy gate applies. |
</threat_model>

<verification>
- `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` passes (audit cells at `2`, nothing decreased).
- Both audit ledger cells show a bare `2` in column-4 (no decorators).
- Install golden-diff (CSS triple-copy) green.
- `grep -c "Audit Explorer Archetype" guides/reference/admin-design-contract.md` ≥ 1.
- `mix test test/sigra/admin/glossary_test.exs` passes.
- Recapture routed only to audit-explorer / user-audit / mg-6 (iff drift); allowlists empty; canary byte-stable.
</verification>

<success_criteria>
- `audit-index-live` and `audit-user-live` ratcheted to Tier 2 with honest applicable proxy evidence (overlay/APG cited N/A, not fabricated); monotonic guard green.
- Audit Explorer archetype documented in the design contract; stale audit line ref corrected.
- CSS triple-copy golden-diff green; only audit-scoped non-canary baselines recaptured with zero-drift idempotency; allowlists empty.
</success_criteria>

<output>
Create `.planning/phases/202-audit-surfaces-elevation/202-05-SUMMARY.md` when done.
</output>
