---
phase: 202-audit-surfaces-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/components.ex
autonomous: true
requirements: [AUDIT-02]
must_haves:
  truths:
    - "A single public `audit_table_row/1` component renders the 4-column desktop `<tr>` (Occurred / Event / Actor / Outcome) for both audit pages identically."
    - "The two raw codes (event id + action code) render as real `<code class=\"sg-code\">` text nodes inside the Event cell's native `<details>` disclosure — not data-attrs, not JS-only — so a desktop-scoped `code.sg-code` selector still extracts exactly 2."
    - "A single public `audit_pagination_nav/1` component renders the honest-cursor `<nav>` (present iff multi_page?), taking pre-built prev/next hrefs."
    - "A single public `audit_empty_state/1` component renders the audit zero-state, parametrized for per-page copy."
    - "`audit_tone/1` has exactly one source of truth (the existing public `Components.audit_tone/1`); no private re-declarations are introduced."
  artifacts:
    - "lib/sigra/admin/components.ex with audit_table_row/1, audit_pagination_nav/1, audit_empty_state/1 public function components"
  key_links:
    - "audit_table_row/1 Event cell `<details>` keeps both `code.sg-code` nodes in the desktop DOM (the D-06 equivalence contract)"
    - "td:nth-child column order stays frozen: Occurred=1, Event=2, Actor=3, Outcome=4 (no sibling detail <tr>)"
    - "audit_pagination_nav/1 owns multi_page?/1 so both pages render identical pagination markup"
---

<objective>
Extract the hand-duplicated audit desktop-table row, pagination `<nav>`, and empty-state markup —
currently byte-copied between `audit_index_live.ex` and `audit_user_live.ex` — into THREE new PUBLIC
function components in `lib/sigra/admin/components.ex`, and fold the two raw audit codes (event id +
action code) out of the primary desktop columns into a native CSS-only `<details>` disclosure inside
the Event cell. This is the keystone Wave-1 deliverable both LiveViews consume in Wave 2.

Purpose: AUDIT-02 byte-coherence (shared markup) + density reduction (codes deferred to drill-down)
without losing the codes' DOM-extractability (the D-06 content-equivalence contract) or the CSV
`event_id` contract (untouched — codes still render as DOM text).

Output: `components.ex` gains `audit_table_row/1`, `audit_pagination_nav/1`, `audit_empty_state/1`
(all public, so two separate LiveViews can share them — private-to-one will not satisfy cross-page
byte-coherence). No new CSS. No new route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-audit-surfaces-elevation/202-CONTEXT.md
@.planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md
@.planning/phases/202-audit-surfaces-elevation/202-UI-SPEC.md

@lib/sigra/admin/components.ex
@lib/sigra/admin/live/audit_index_live.ex
@lib/sigra/admin/live/audit_user_live.ex
</context>

<artifacts_this_phase_produces>
New public symbols introduced by THIS plan (Wave 1):
- `Sigra.Admin.Components.audit_table_row/1` — desktop `<tr>` body, 4 cells, with the inline-code `<details>` disclosure in the Event cell.
- `Sigra.Admin.Components.audit_pagination_nav/1` — honest-cursor `<nav>` (consumes `meta`, `prev_href`, `next_href`).
- `Sigra.Admin.Components.audit_empty_state/1` — audit zero-state wrapper (parametrized title + body slot).
- (Possibly) a private `format_timestamp/1` co-located helper if the row needs with-seconds timestamps distinct from the existing `format_date/1` — see Task 1.
No new test files in this plan. No new CSS classes (target zero new CSS; browser-default `<details>`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add audit_table_row/1 with inline-code &lt;details&gt; disclosure</name>
  <files>lib/sigra/admin/components.ex</files>
  <read_first>
    - lib/sigra/admin/components.ex:685-735 — existing public `audit_row/1` (the attr+def shape to mirror) and the private `audit_tone/1` (:723-725) + `format_date/1` (:732+) helpers already in this module.
    - lib/sigra/admin/live/audit_index_live.ex:163-191 — the hand-written desktop `<tbody>` `<tr>` body to lift verbatim (raw id at :167-168, raw action code at :177, Actor column at :180-186, Outcome at :187-189).
    - lib/sigra/admin/live/audit_index_live.ex:315-318 — the private `format_timestamp/1` (with-seconds) the row uses; decide whether to co-locate it here or reuse `format_date/1`.
    - .planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md:28-80 — the exact cell-body analog and the D-05/D-06 disclosure rules.
  </read_first>
  <action>
    Add a public `audit_table_row/1` function component to `lib/sigra/admin/components.ex` (co-located with the existing `audit_row/1`). Declare `attr :row, :map, required: true`. Render `<tr data-tone={audit_tone(@row)}>` with exactly FOUR `<td>`s in this frozen order so the positional `td:nth-child` selector contract holds — Occurred (1), Event (2), Actor (3), Outcome (4):

    - td 1 (Occurred): the timestamp `<span>` only. Per D-05, MOVE the raw event-id `<code class="sg-code">{@row.id}</code>` OUT of this cell.
    - td 2 (Event): the `action_label` `sg-status-pill` (toned by `audit_tone/1`) + optional `action_badge` pill (`data-tone="info"`), THEN a native `<details>` whose `<summary>` is a glossary-clean affordance label (e.g. the wording from 202-UI-SPEC Copywriting Contract row "Inline-code disclosure summary") and whose body holds BOTH `<code class="sg-code">{@row.id}</code>` and `<code class="sg-code">{@row.action}</code>`. The `<details>` MUST live inside this Event `<td>` (NOT a sibling `<tr>`) so the 4-column `td:nth-child` contract stays frozen and both code nodes stay inside the desktop results container that Wave-2 wraps with `data-testid="admin-audit-desktop-results"`.
    - td 3 (Actor): keep the existing Actor `<div class="sg-stack sg-stack--1 sg-text-sm">` with `actor_summary` `<span>` first (the equivalence helper reads `td:nth-child(3) span`) plus the conditional Actor/Effective-user muted lines.
    - td 4 (Outcome): keep the existing `sg-show-desktop` outcome cell (risk pill vs muted span).

    Use the existing public `Components.audit_tone/1` (already in this module at :723-725) as the single source of truth — do NOT add a private `audit_tone/1`. For the Occurred timestamp, reuse the existing `format_date/1` if no-seconds is acceptable, OR co-locate a private `format_timestamp/1` (with-seconds, matching audit_index_live.ex:315-318) — pick one and use it consistently; this is Claude's Discretion (D-08) but the two pages MUST end up calling the SAME timestamp formatter for byte-coherence.

    Use only existing `sg-*` classes (`sg-stack`, `sg-stack--1`, `sg-cluster`, `sg-cluster--2`, `sg-status-pill`, `sg-code`, `sg-nowrap`, `sg-text-sm`, `sg-muted`, `sg-show-desktop`). Rely on browser-default `<details>` rendering — do NOT add a chevron class and never reintroduce `sg-chevron` (0 rules in all three CSS copies; adding any new class triggers the D-13 triple-copy golden-diff gate). Target: zero new CSS.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>`audit_table_row/1` compiles clean; renders a 4-`<td>` `<tr>` with the two `code.sg-code` nodes inside a `<details>` in the Event cell; Actor `actor_summary` span remains the first span in td:nth-child(3); no private `audit_tone/1` added; no new `sg-*` class introduced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add audit_pagination_nav/1 (honest-cursor nav, hrefs passed in)</name>
  <files>lib/sigra/admin/components.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_index_live.ex:216-236 — the `<nav>` body to lift verbatim.
    - lib/sigra/admin/live/audit_index_live.ex:309-313 — the `multi_page?/1` honest-cursor guard (byte-identical to audit_user_live.ex:475-479).
    - .planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md:84-121 — the audit_pagination_nav/1 analog and the "pass hrefs in" rationale.
  </read_first>
  <action>
    Add a public `audit_pagination_nav/1` to `components.ex`. Declare `attr :meta, :map, required: true`, `attr :prev_href, :string, required: true`, `attr :next_href, :string, required: true`. Render `<nav :if={@meta && multi_page?(@meta)} class="sg-cluster sg-cluster--between">` containing the Previous-page `<a>` (uses `@prev_href`, `is-disabled` class + `aria-disabled` from `@meta.previous_page`, `aria-label="Previous page"`, sr-only label), the `Page {@meta.current_page || 1}` span, and the Next-page `<a>` (`@next_href`, `aria-label="Next page"`) — lifting the markup verbatim from audit_index_live.ex:216-236.

    Move the `multi_page?/1` honest-cursor guard into this module as a private helper (copy audit_index_live.ex:309-313 verbatim — `multi_page?(nil) -> false`; otherwise true iff `meta.previous_page` or `meta.next_page` is non-nil). The per-page divergence is ONLY the href builder (index `page_path/3` vs per-user `page_path/4` with `user_id`/`return_to`); the nav markup is shared — that is why prev/next hrefs are passed in (D-09 legitimate divergence stays in the LiveViews).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>`audit_pagination_nav/1` compiles clean; renders the `<nav>` only when `multi_page?/1` is true; consumes pre-built `prev_href`/`next_href`; private `multi_page?/1` lives in `components.ex`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add audit_empty_state/1 (parametrized audit zero-state)</name>
  <files>lib/sigra/admin/components.ex</files>
  <read_first>
    - lib/sigra/admin/components.ex:407 — the existing generic `empty_state/1` this wraps.
    - lib/sigra/admin/live/audit_index_live.ex:205-214 — index empty-state copy (filter-aware + "Clear all filters" link).
    - lib/sigra/admin/live/audit_user_live.ex:234-244 — per-user empty-state copy ("No audit events for this user").
    - .planning/phases/202-audit-surfaces-elevation/202-PATTERNS.md:125-133 and 202-UI-SPEC.md:105-108 — empty-state copy contract.
  </read_first>
  <action>
    Add a public `audit_empty_state/1` to `components.ex` that wraps the existing `empty_state/1` with audit-specific copy. Declare a `title` attr and an inner-block slot (default body) so the per-page copy divergence (index "No audit events match this view" + filter-aware body with a "Clear all filters" link vs per-user "No audit events for this user") is parametrized rather than duplicated. Keep the copy strings glossary-clean (both audit files are already scoped by glossary_test.exs:28-29). Use the exact heading/body strings from 202-UI-SPEC.md:105-108. This is the lowest-value DRY (D-09 allows legitimate copy differences to stand) — parametrize for coherence; do not invent new copy.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>`audit_empty_state/1` compiles clean; accepts a `title` attr + inner-block slot; wraps `empty_state/1`; copy matches 202-UI-SPEC.md heading/body strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → LiveView render | Authenticated admin/operator personas only; admin authorization plug gates every audit route (existing). |
| presenter row map → HEEx | Audit row data flows from `Explorer`/`presenter.ex` into the new components; values are rendered as text nodes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-202-01 | Information Disclosure | `audit_table_row/1` inline-code `<details>` | low | accept | Codes (event id, action code) were already visible in the primary columns; this plan only defers them behind a CSS-only disclosure on the same admin-gated page. No new audience, no new export path. Authorization is unchanged (existing admin plug). |
| T-202-02 | Tampering | HEEx output | low | mitigate | All row values render through HEEx auto-escaping as text; no `raw/1`, no attr injection. New components add no user-controlled attributes. |
| T-202-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs nothing — pure in-repo Elixir component extraction (RESEARCH: "Phase 202 installs nothing"). No package legitimacy gate applies. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- The three new components are public (callable as `<.audit_table_row>` / `<.audit_pagination_nav>` / `<.audit_empty_state>` from a LiveView that imports `Sigra.Admin.Components`).
- No new `sg-*` CSS class introduced (grep the changed HEEx for `sg-chevron` → 0).
- `audit_tone/1` has exactly one definition in `components.ex` (no private re-declaration added by this plan).
</verification>

<success_criteria>
- Three public audit function components exist in `components.ex` and compile clean.
- The desktop row's two `code.sg-code` nodes live inside the Event-cell `<details>` (still real DOM text nodes), preserving the D-06 equivalence contract for Wave 2/3.
- The 4-column `td:nth-child` order (Occurred=1, Event=2, Actor=3, Outcome=4) is frozen.
- Zero new CSS; honest-cursor `multi_page?/1` owned by the shared nav component.
</success_criteria>

<output>
Create `.planning/phases/202-audit-surfaces-elevation/202-01-SUMMARY.md` when done.
</output>
