---
phase: 83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/mfa.ex
autonomous: true
requirements:
  - AUD-20-01
must_haves:
  truths:
    - "When **`:audit_schema`** is set on config, **`confirm_enrollment/5`** invalid-TOTP path records **`mfa.enroll.failure`** via **`Repo.transaction/1`** on **`Ecto.Multi`** + **`Sigra.Audit.log_multi_safe/3`**, reusing the **`commit_ad_hoc_mfa_audit/5`** shell (telemetry + rescue + **`:constraint_violation`** telemetry)."
    - "When audit schema is **absent**, invalid-TOTP path performs **no** `Repo.transaction` solely for audit (parity with prior **`log_safe`** no-op)."
    - "Public return on invalid TOTP remains **`{:error, :invalid_code}`** regardless of audit subsystem outcome (**D-83-02**)."
  artifacts:
    - path: lib/sigra/mfa.ex
      provides: "AUD-04-022 promoted mechanism for pre-DB invalid TOTP"
nyquist_compliant: false
---

<objective>
Close **AUD-20-01** for **022**: replace standalone **`log_safe/3`** on the **`confirm_enrollment/5`** invalid-code branch with **`commit_ad_hoc_mfa_audit/5`** when audit is enabled; update module dispatch comment and **`@doc`** for **`confirm_enrollment/5`** per **83-CONTEXT.md** D-83-01 and D-83-02.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-83-01 | Repudiation | Invalid enrollment attempt leaves no durable **`mfa.enroll.failure`** when audit is configured | medium | Route through **`commit_ad_hoc_mfa_audit`** with same action/metadata as today |
| T-83-02 | Tampering | Empty **`Repo.transaction`** on audit-off hot path adds latency / surprises | low | Guard **`commit_ad_hoc_mfa_audit`** behind non-**`nil`** **`:audit_schema`** from **`mfa_audit_opts(config)`** |
| T-83-03 | Elevation of privilege | Audit failure changes caller-visible result | high | Never map audit errors to **`{:ok, _}`**; keep **`{:error, :invalid_code}`**; subsystem failures → **`[:sigra, :audit, :log_safe_error]`** only (**D-83-02**) |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Promote invalid-TOTP audit to `commit_ad_hoc_mfa_audit/5`</name>
  <files>lib/sigra/mfa.ex</files>
  <read_first>
    - lib/sigra/mfa.ex (`confirm_enrollment/5` ~L180–301; `commit_ad_hoc_mfa_audit/5` ~L72–115; module comment ~L33–44)
    - lib/sigra/audit.ex (`log_multi_safe/3` nil-schema behavior ~L255–261)
    - .planning/phases/83-mfa-confirm-enrollment-022/83-CONTEXT.md (D-83-01, D-83-02)
  </read_first>
  <action>
    1. In **`confirm_enrollment/5`**, replace the **`{:error, _reason}`** branch that calls **`Sigra.Audit.log_safe("mfa.enroll.failure", …)`** with:
       - If **`Keyword.get(mfa_audit_opts(config), :audit_schema)`** is **`nil`**: do **nothing** for audit (no transaction).
       - Else: call **`commit_ad_hoc_mfa_audit(config, "mfa.enroll.failure", user, extra_opts, :audit_mfa_enroll_invalid_code)`** where **`extra_opts`** merges caller fields equivalent to today’s **`log_safe`** invocation: **`actor_id: user.id`**, **`target_id: user.id`**, **`outcome: "failure"`**, **`metadata: %{method: "totp", reason: "invalid_code"}`** — and rely on **`commit_ad_hoc_mfa_audit`** to merge **`mfa_audit_opts(config)`** + **`mfa_ad_hoc_audit_scope_fields(Sigra.Scope.from_config(config, user))`** via its existing **`opts`** pipeline (pass only the deltas needed so the composed opts match current **`log_safe`** audit rows).

    2. Update the **`# --- Audit integration helpers`** dispatch comment (~L37): change the **`invalid_code still log_safe/3 (AUD-04-022`** line to state **`Repo.transaction` + `Multi` + `log_multi_safe`** via **`commit_ad_hoc_mfa_audit`** (Phase **83**), not standalone **`log_safe`**.

    3. Extend **`@doc` for `confirm_enrollment/5`**: add **Returns** / audit note per **D-83-02** — invalid code ⇒ **`{:error, :invalid_code}`**; with **`:audit_schema`**, Sigra **may** attempt durable **`mfa.enroll.failure`** in an audit-only transaction; audit persistence failure does **not** change the return; operators watch **`[:sigra, :audit, :log_safe_error]`**.
  </action>
  <acceptance_criteria>
    - `awk '/def confirm_enrollment/,/^  @doc /' lib/sigra/mfa.ex | grep -c "Audit.log_safe"` exits **0** with output **`0`** (no **`log_safe`** in **`confirm_enrollment`** body).
    - `grep -nF "commit_ad_hoc_mfa_audit" lib/sigra/mfa.ex` shows at least one call site inside **`confirm_enrollment`** body OR a **`defp`** helper exclusively used from that branch — evidence the promoted path uses the shared shell.
    - `grep -nF "audit_mfa_enroll_invalid_code" lib/sigra/mfa.ex` exits **0** (distinct **`audit_multi_step`**).
    - `MIX_ENV=test mix compile --warnings-as-errors` exits **0** from repo root.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=test mix compile --warnings-as-errors`
  </verify>
</task>

</tasks>

<verification>
- After **83-02-PLAN** tests land: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
</verification>

<success_criteria>
- **AUD-20-01** satisfied in **`lib/sigra/mfa.ex`**: **022** uses **D-AUD-05** audit-only transaction when **`:audit_schema`** set; audit-off path avoids audit-only transactions.
</success_criteria>
