---
phase: 83
plan: 02
type: execute
wave: 2
depends_on:
  - "83-01-PLAN.md"
files_modified:
  - test/sigra/mfa_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-20-02
must_haves:
  truths:
    - "Three named tests cover invalid TOTP + audit on, invalid TOTP + audit off, and CHECK fault injection with **`[:sigra, :audit, :log_safe_error]`** assertion — per **83-CONTEXT** D-83-03."
    - "Tests use **`async: false`**, unique **`telemetry.attach`** handler IDs, and **`cfg/2`** audit toggle."
  artifacts:
    - path: test/sigra/mfa_audit_atomicity_test.exs
      provides: "Executable proof for AUD-04-022 promotion"
nyquist_compliant: false
---

<objective>
Implement **AUD-20-02**: extend **`mfa_audit_atomicity_test.exs`** with the **022** invalid-code matrix (audit on row + metadata, audit off no rows, fault injection telemetry) aligned with **`commit_ad_hoc_mfa_audit`** semantics.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-83-04 | Repudiation | Tests pass without asserting durable audit content | medium | Assert **`mfa.enroll.failure`** count and **`metadata`** contains **`invalid_code`** / **`totp`** |
| T-83-05 | Tampering | Fault injection raises **`ConstraintError`** to test process | high | Expect **no** audit row + **`{:error, :invalid_code}`** + **`assert_receive`** telemetry — not **`assert_raise`** |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Invalid-code audit matrix tests</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/mfa_audit_atomicity_test.exs (`cfg/1`, setup, **`confirm_enrollment insert_failed`** tests ~L747–841, telemetry patterns elsewhere in file)
    - lib/sigra/mfa.ex (`confirm_enrollment/5` after plan **01** merge)
    - .planning/phases/83-mfa-confirm-enrollment-022/83-CONTEXT.md (D-83-03 matrix)
  </read_first>
  <action>
    1. Add **`test "confirm_enrollment invalid TOTP writes mfa.enroll.failure when audit enabled"`** (name may vary): **`MFA.enroll`** → wrong code (e.g. **`"000000"`** or time-skewed code guaranteed invalid) → assert **`{:error, :invalid_code}`**; **`count(repo, "user_mfa_credentials") == 0`**; **`count_where(..., \"action = 'mfa.enroll.failure'\") == 1`**; read **`metadata`** JSON contains **`invalid_code`** and evidence of **`totp`** method (string match on **`metadata::text`** or decoded map).

    2. Add **`test "confirm_enrollment invalid TOTP skips audit when audit disabled"`**: **`cfg(repo, false)`** → same invalid path → **`{:error, :invalid_code}`**; **`count_where(..., \"action = 'mfa.enroll.failure'\") == 0`**.

    3. Add **`test "confirm_enrollment invalid TOTP emits log_safe_error when failure audit blocked"`** (mirrors insert_failed guard style): **`ALTER TABLE audit_events ADD CONSTRAINT ... CHECK (action <> 'mfa.enroll.failure')`** in **`try/after`**; invalid code; assert **`{:error, :invalid_code}`**; **`count_where(..., 'mfa.enroll.failure') == 0`**; attach handler and **`assert_receive`** event **`[:sigra, :audit, :log_safe_error]`** with **`%{reason: :constraint_violation}`** or equivalent pattern matching **`commit_ad_hoc_mfa_audit`** rescue path (follow existing telemetry tests in this file for handler id uniqueness).

    4. Keep **`async: false`** module attribute; do not weaken existing tests.
  </action>
  <acceptance_criteria>
    - `grep -nF "invalid TOTP" test/sigra/mfa_audit_atomicity_test.exs` exits **0** with **≥ 3** lines OR three distinct new test strings **`invalid_code`** / **`audit disabled`** / **`failure audit blocked`** appear in new test names (verifiable: `grep -c "confirm_enrollment invalid" test/sigra/mfa_audit_atomicity_test.exs` returns **`3`**).
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs`
  </verify>
</task>

</tasks>

<verification>
- Full file green: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs`
</verification>

<success_criteria>
- **AUD-20-02** covered: **022** mechanism observable in tests with audit on/off and fault injection.
</success_criteria>
