---
phase: 155-shared-component-foundation-keystone
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/reference/admin-design-contract.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "The design contract notice entry no longer mandates role=alert/role=status in Phase 155 HEEx markup; load-present notices carry NO live-region role"
    - "The contract's notice markup-cell target is the sg-notice div (not sg-list-row)"
    - "The admin-checkpoint Playwright job physically cannot start until the library_tests byte-equality gate passes"
  artifacts:
    - path: "guides/reference/admin-design-contract.md"
      provides: "D-09 corrected notice ARIA spec aligned with the shipped component"
      contains: "sg-notice"
    - path: ".github/workflows/ci.yml"
      provides: "D-14 hard CI job dependency wiring the byte-equality gate before Playwright"
      contains: "needs: [release_ref_guard, library_tests]"
  key_links:
    - from: ".github/workflows/ci.yml example_playwright_smoke job"
      to: "library_tests job (DB-free mix test lane running components_test.exs)"
      via: "needs: [release_ref_guard, library_tests]"
      pattern: "needs: \\[release_ref_guard, library_tests\\]"
    - from: "guides/reference/admin-design-contract.md notice entry"
      to: "lib/sigra/admin/components.ex notice component (D-08 no-role form)"
      via: "corrected ARIA cell describing the shipped behavior"
      pattern: "no live-region role|sg-notice"
---

<objective>
Make two required edits that align the canonical spec and the CI gate with the keystone decisions:
1. Amend `guides/reference/admin-design-contract.md` (notice entry) per D-09 — a correction-of-fact: the committed contract currently MANDATES `role="alert"`/`role="status"` "in Phase 155 HEEx markup", which directly CONTRADICTS the shipped no-role component (D-08). The goldens' drift messages cite this contract, so it must agree with the code.
2. Edit `.github/workflows/ci.yml` per D-14 — add `library_tests` to the `example_playwright_smoke` job's `needs:` so the admin-checkpoint Playwright job physically cannot start until the DB-free byte-equality gate passes.

Purpose: Without the D-09 amendment the canonical spec instructs the exact ARIA behavior the component deliberately omits (Pitfall 3). Without the D-14 gate wiring the byte-equality proof is advisory rather than blocking — the keystone "re-record is a bug" rule needs a hard job dependency, not in-script ordering.

Output: Two edited files — the design contract notice entry and the CI workflow `needs:` line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-shared-component-foundation-keystone/155-CONTEXT.md
@.planning/phases/155-shared-component-foundation-keystone/155-PATTERNS.md

<interfaces>
<!-- The exact current text being edited (read directly before editing). -->

admin-design-contract.md notice entry (~lines 111-115) currently states:
  "Winning markup / CSS" cell: <div class="sg-list-row" data-tone={tone}><p class="sg-text-sm">...</p></div>
       + "ARIA attributes (role=\"alert\" for risk; role=\"status\" aria-live=\"polite\" for warn/ok/info)
          are applied in Phase 155 HEEx markup..."
  "ARIA role(s)" cell: 'risk tone: role="alert"; warn/ok/info: role="status" aria-live="polite"; No-tone: role="region".
          ARIA attributes are applied in Phase 155 HEEx markup, not via CSS.'

ci.yml example_playwright_smoke job (~line 697) currently reads:
    needs: release_ref_guard
target (D-14):
    needs: [release_ref_guard, library_tests]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend the design contract notice ARIA (D-09 correction-of-fact)</name>
  <files>guides/reference/admin-design-contract.md</files>
  <read_first>
    - guides/reference/admin-design-contract.md (lines 107-115) — the notice entry being edited (Winning markup / CSS cell + ARIA role(s) cell)
    - 155-CONTEXT.md (lines 29-38) — D-07/D-08/D-09 the notice decision + exact required amendment wording
    - 155-PATTERNS.md (lines 241-257) — the exact target markup-cell value and the one-line ARIA correction text + rationale to fold in
    - 155-RESEARCH.md (lines 122-130) — the CONFLICT writeup (the contract contradicts D-08; the amendment is required, not optional)
  </read_first>
  <action>
    Edit the `notice` entry in `guides/reference/admin-design-contract.md`. In the "Winning markup / CSS" cell change the markup target to `<div class="sg-notice" data-tone={tone}><p class="sg-text-sm">…</p></div>` and REMOVE the sentence asserting that `role="alert"`/`role="status"` ARIA attributes "are applied in Phase 155 HEEx markup". In the "ARIA role(s)" cell REPLACE the `role="alert"`/`role="status" aria-live="polite"`/`role="region"` prescription with the D-09 one-line correction-of-fact: load-present notices carry NO live-region role; tone is conveyed visually via `data-tone` and textually via copy; a live-region role is added per-call-site (via `:rest`) only for genuinely post-load dynamic notices (future Phase 157 / LAND-01). Fold in the rationale (D-08): `role="alert"` is inert on load-present content (WAI-ARIA APG); `role="status"` is for post-load updates (MDN) and risks duplicate announcements on LiveView re-render; Phoenix `flash/1` uses `role="alert"` only because it is dynamically injected with focus-management JS — not Sigra's render model. Do NOT change any other component entry.
  </action>
  <verify>
    <automated>grep -n 'sg-notice' guides/reference/admin-design-contract.md | grep -q 'data-tone' && ! sed -n '107,116p' guides/reference/admin-design-contract.md | grep -q 'applied in Phase 155 HEEx markup' && sed -n '107,116p' guides/reference/admin-design-contract.md | grep -Eqi 'no .*live-region role|carry no'</automated>
  </verify>
  <acceptance_criteria>
    - The notice entry's markup cell now targets `<div class="sg-notice" data-tone={tone}>` (no longer `sg-list-row` as the migration target line)
    - The notice entry no longer contains the phrase `role="alert"` / `role="status"` as a mandated Phase 155 application
    - The notice entry no longer contains the sentence "are applied in Phase 155 HEEx markup"
    - The ARIA cell states load-present notices carry no live-region role (tone visual via `data-tone` + textual via copy; live-region role opt-in per-call-site for post-load dynamic notices)
    - No other component entry in the contract is modified
  </acceptance_criteria>
  <done>The canonical design contract's notice entry agrees with the shipped no-role component: markup-cell target is the sg-notice div, and the ARIA prescription is the D-09 correction-of-fact.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the CI gate so Playwright needs library_tests (D-14)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (lines 694-705) — the `example_playwright_smoke` job head + its current `needs: release_ref_guard` line
    - .github/workflows/ci.yml (lines 146-191) — the `library_tests` job (DB-free `mix test` lane the byte-equality test runs in)
    - 155-CONTEXT.md (line 45) — D-14 gate wiring decision
    - 155-PATTERNS.md (lines 225-237) — exact current vs target `needs:` value
    - 155-RESEARCH.md (lines 173-178) — verified one-line edit; library_tests is DB-free, the gate edit is the `needs:` line
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, on the `example_playwright_smoke` job, change the `needs: release_ref_guard` line to `needs: [release_ref_guard, library_tests]`. This adds a hard CI job dependency so the admin-checkpoint Playwright job (which hosts the 5×3 baselines) physically cannot start until the `library_tests` lane — where `mix test` runs `components_test.exs` byte-equality — passes (D-14). This is a single-line edit. Do NOT modify any other job, step, or `needs:` declaration in the file.
  </action>
  <verify>
    <automated>grep -q 'needs: \[release_ref_guard, library_tests\]' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` `example_playwright_smoke` job `needs:` includes `library_tests` (exact value `needs: [release_ref_guard, library_tests]`)
    - The file remains valid YAML (`yaml.safe_load` succeeds)
    - No other job's `needs:` line is changed
  </acceptance_criteria>
  <done>The admin-checkpoint Playwright job declares `needs: [release_ref_guard, library_tests]`, making byte-equality a hard, non-bypassable precondition for the baseline guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job DAG | The `needs:` edit changes the job dependency graph so the baseline guard cannot run before the byte-equality gate. No runtime/user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-G01 | Tampering / bypass | CI gate ordering | mitigate | A hard `needs:` job dependency (not in-script step ordering) makes the Playwright baseline guard physically unable to start until the DB-free byte-equality test passes (D-14) — stronger and less bypassable. |
| T-155-G02 | Repudiation | spec/code disagreement | mitigate | The D-09 contract amendment removes the role=alert/role=status mandate so the canonical spec the goldens cite agrees with the shipped component (Pitfall 3). |
| T-155-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — doc + YAML edits only. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `guides/reference/admin-design-contract.md` notice entry targets `sg-notice` and no longer mandates `role="alert"`/`role="status"` in Phase 155 markup.
- `.github/workflows/ci.yml` is valid YAML and the `example_playwright_smoke` job `needs:` includes `library_tests`.
- No call-site, component, or other CI job changed.
</verification>

<success_criteria>
- D-09 satisfied: the canonical contract agrees with the shipped no-role notice (correction-of-fact applied).
- D-14 satisfied: the byte-equality gate is a hard precondition for the Playwright baseline guard.
- Keystone invariant protected: the gate ordering makes a silent re-record structurally impossible to reach without passing byte-equality first.
</success_criteria>

<output>
Create `.planning/phases/155-shared-component-foundation-keystone/155-03-SUMMARY.md` when done
</output>
