---
phase: 213-latest-phoenix-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["213-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release-please.yml
  - .github/workflows/hex-publish.yml
  - CLAUDE.md
  - CONTRIBUTING.md
  - mix.exs
  - guides/recipes/local-development.md
  - test/sigra/planning/phase_198_contributor_dx_contract_test.exs
  - scripts/ci/install-smoke.sh
  - scripts/ci/admin-acceptance-smoke.sh
autonomous: true
requirements: [COMPAT-03]

must_haves:
  truths:
    - "No `phx_new 1.8.7` archive-install string remains in any CI workflow file, CLAUDE.md, CONTRIBUTING.md, mix.exs, or guides/recipes/local-development.md (D-07/D-08, COMPAT-03)"
    - "All 11 archive-install pin sites read the concrete target `phx_new 1.8.8` (not floating/unpinned) (D-07)"
    - "`mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs` passes after the CONTRIBUTING.md rewrite (the literal-string assertion was updated to the new version) (COMPAT-03)"
    - "A CI drift-detector runs `mix sigra.fixture.rebless_golden --check` and fails on fixture drift (D-06, exits 2 on drift)"
    - "Both smoke scripts assert the resolved phx.new version in their preamble and fail fast if it is not the pin target (D-11)"
    - "The generated-host acceptance smoke runs green against the current pinned phx.new (COMPAT-03)"
  artifacts:
    - ".github/workflows/ci.yml (9 pin sites flipped + new `--check` drift-detector step in install_golden_contract)"
    - ".github/workflows/release-please.yml (1 pin site flipped)"
    - ".github/workflows/hex-publish.yml (1 pin site flipped)"
    - "CLAUDE.md (dev-prereq note inverted to install the new version; the 'don't rebless' warning removed)"
    - "CONTRIBUTING.md (1.8.7 references rewritten to the new version)"
    - "mix.exs + guides/recipes/local-development.md (prose refs refreshed to the new version)"
    - "test/sigra/planning/phase_198_contributor_dx_contract_test.exs (assertion + title updated off 1.8.7)"
    - "scripts/ci/install-smoke.sh + scripts/ci/admin-acceptance-smoke.sh (D-11 resolved-version assert preamble)"
  key_links:
    - "The phase-198 DX-contract test asserts the literal version string in CONTRIBUTING.md — it MUST be updated in the SAME commit as the CONTRIBUTING.md rewrite or the pin-flip plan goes red (Pitfall 2)"
    - "This plan depends on 213-01: the golden fixture must already carry the root_tag_attribute block before pins flip to 1.8.8, else every install/golden job goes red on the byte-diff (D-09 load-bearing ordering)"
  prohibitions:
    - "Do NOT fully unpin / float the phx.new archive install — the pin MUST be a concrete version string (D-07 — floating re-exposes the SEED-004 silent-upgrade failure)"
    - "Do NOT edit any golden fixture bytes in this plan — the fixture was reblessed and committed in Plan 01"
    - "Do NOT leave any stale `1.8.7` breadcrumb in the touched files — a passing `grep -rn 'phx_new 1.8.7'` over CI + docs must return nothing"
---

<objective>
Move CI and docs off the frozen `phx_new 1.8.7` archive atomically: flip all 11 archive-install
pins to the concrete `phx_new 1.8.8` target, rewrite the three coupled doc/test touch-sites
(CLAUDE.md, CONTRIBUTING.md, and the phase-198 DX-contract test that hard-asserts the version
string), refresh two low-priority prose refs (mix.exs, local-development.md), add a
`rebless_golden --check` CI drift-detector (D-06), and add the D-11 resolved-version assert to both
smoke scripts so a cached 1.8.7 archive cannot false-green. This is the SECOND half of the
load-bearing D-09 ordering and depends on Plan 01 having already committed the reblessed fixture.

Purpose: COMPAT-03 — the `1.8.7` pin is absent from all CI + docs, CI tracks current Phoenix via a
concrete pin plus a drift-detector (not a floating archive), and the generated-host acceptance smoke
runs green against current `phx.new`.
Output: 3 workflow files + 4 doc/test files + 2 smoke scripts updated; the pin is a concrete
`1.8.8` everywhere; the drift-detector and version-asserts are wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/213-latest-phoenix-compatibility/213-CONTEXT.md
@.planning/phases/213-latest-phoenix-compatibility/213-RESEARCH.md
@.planning/phases/213-latest-phoenix-compatibility/213-VALIDATION.md

# The rebless task whose --check mode powers the new drift-detector job (--check at 120-145)
@lib/mix/tasks/sigra.fixture.rebless_golden.ex
# The smoke scripts to add the D-11 version-assert to
@scripts/ci/install-smoke.sh
@scripts/ci/admin-acceptance-smoke.sh
# The phase-198 DX-contract test that hard-asserts the CONTRIBUTING.md version string
@test/sigra/planning/phase_198_contributor_dx_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip all 11 archive pins + refresh docs (CLAUDE.md, CONTRIBUTING.md, mix.exs, local-development.md) + phase-198 test</name>
  <files>.github/workflows/ci.yml, .github/workflows/release-please.yml, .github/workflows/hex-publish.yml, CLAUDE.md, CONTRIBUTING.md, mix.exs, guides/recipes/local-development.md, test/sigra/planning/phase_198_contributor_dx_contract_test.exs</files>
  <read_first>
    - RESEARCH.md "Complete Touch-Site Inventory" (the 11 pin sites + 3 coupled + 2 low-priority refs, line-anchored — match on the literal string, not the line number, since edits reflow)
    - test/sigra/planning/phase_198_contributor_dx_contract_test.exs (lines 9, 72, 78-79 carry the literal `1.8.7` string this task must update)
  </read_first>
  <action>
    Confirm the concrete pin target with `mix hex.info phx_new` / the version Plan 01 reblessed
    against (per A3 it is `1.8.8` unless a newer concrete patch shipped; use the exact version Plan 01
    used — the fixture and the pin MUST agree). Treat that string as TARGET below.

    (a) PIN SITES (D-07/D-08) — flip all 11 archive-install lines from
    `mix archive.install --force hex phx_new 1.8.7` to `mix archive.install --force hex phx_new TARGET`.
    They are the identical line; match the literal string. Nine live in `.github/workflows/ci.yml`,
    one in `.github/workflows/release-please.yml`, one in `.github/workflows/hex-publish.yml`. Keep a
    CONCRETE version (D-07) — do NOT drop the version to a floating install.

    (b) CLAUDE.md dev-prereq note (~lines 210-222) — INVERT it: instruct installing TARGET locally
    (`mix archive.install --force hex phx_new TARGET`) and DELETE the stale "do not rebless the
    fixture to fix 1.8.8 drift / install 1.8.7 instead" warning, whose premise is inverted once the
    fixture is reblessed. Exact wording is Claude's discretion (per CONTEXT.md), but it must no longer
    tell contributors to keep 1.8.7.

    (c) CONTRIBUTING.md (lines ~33-39, 62) — rewrite every `1.8.7` reference to TARGET, including the
    "install the pinned version" command and the "install 1.8.7 to fix" guidance.

    (d) phase-198 test — update `test/sigra/planning/phase_198_contributor_dx_contract_test.exs`:
    the moduledoc line (~9), the test title (~72), and the assertion + message (~78-79) all carry the
    literal `1.8.7` string. Change them to TARGET. This MUST land in the same task/commit as the
    CONTRIBUTING.md rewrite (Pitfall 2 — the test hard-asserts `contributing =~ "1.8.7"` and turns
    red the instant CONTRIBUTING.md drops the string).

    (e) Low-priority prose refs — refresh `mix.exs` (~lines 141-142 comment) and
    `guides/recipes/local-development.md` (~line 30) from `1.8.7` to TARGET so no stale breadcrumb
    remains.

    After all edits, `grep -rn 'phx_new 1.8.7' .github/workflows CLAUDE.md CONTRIBUTING.md mix.exs guides/recipes/local-development.md`
    must return nothing.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'phx_new 1.8.7' .github/workflows CLAUDE.md CONTRIBUTING.md mix.exs guides/recipes/local-development.md 2>/dev/null)" && MIX_ENV=test mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs</automated>
  </verify>
  <done>
    Zero `phx_new 1.8.7` strings remain across the 3 workflow files + CLAUDE.md + CONTRIBUTING.md +
    mix.exs + local-development.md. All 11 archive-install lines read the concrete `phx_new TARGET`
    (no floating install). The phase-198 DX-contract test passes with its assertion updated to TARGET.
    The CLAUDE.md "don't rebless to fix 1.8.8 drift" warning is deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the rebless_golden --check CI drift-detector (D-06) and the D-11 smoke version-asserts</name>
  <files>.github/workflows/ci.yml, scripts/ci/install-smoke.sh, scripts/ci/admin-acceptance-smoke.sh</files>
  <read_first>
    - lib/mix/tasks/sigra.fixture.rebless_golden.ex (--check mode at lines 120-145: writes to a tmp dir, never touches the repo, prints "OK: fixture is up-to-date" on match and exits 2 on drift)
    - scripts/ci/install-smoke.sh (the `mix phx.new` invocation is at ~line 36; add the version-assert BEFORE it)
    - scripts/ci/admin-acceptance-smoke.sh (the `mix phx.new` invocation is at ~line 84; add the version-assert BEFORE it)
    - .github/workflows/ci.yml install_golden_contract job (the archive + deps + golden-test steps already exist there ~lines 168-183 — fold the --check step in after the golden test run)
  </read_first>
  <action>
    (a) DRIFT-DETECTOR (D-06) — add a CI step that runs `MIX_ENV=test mix sigra.fixture.rebless_golden --check`
    as a HARD GATE (per RESEARCH Open Question 1 recommendation: a single hard-gate step against the
    pinned archive satisfies the requirement; a floating "latest" advisory job is optional and out of
    scope). Fold it into the existing `install_golden_contract` job in `.github/workflows/ci.yml` (per
    D-06 discretion — the archive + deps are already set up there), as a new step after the "Run
    install golden + idempotency tests" step, gated by the same `if: steps.detect.outputs.run == 'true'`
    condition and the same MIX_ENV/PG env. The `--check` mode writes only to a tmp dir and exits 2 on
    drift, so a non-zero exit fails the job — no `continue-on-error`.

    (b) D-11 VERSION-ASSERT — add a preamble to BOTH `scripts/ci/install-smoke.sh` and
    `scripts/ci/admin-acceptance-smoke.sh`, placed BEFORE their respective `mix phx.new` invocations,
    that captures the resolved installer version via `mix phx.new --version` and fails fast (non-zero
    exit with a clear message) unless it matches the expected TARGET (the pin from Task 1). This
    defends against a cached/stale 1.8.7 archive on a reused runner producing a false green (Pitfall
    4). Note: `mix phx.new --version` prints a line of the form `Phoenix installer vX.Y.Z`; grep for
    the concrete TARGET version substring, and abort with a descriptive error naming the expected vs
    resolved version if absent. Keep the assert self-contained and `set -euo pipefail`-safe (both
    scripts already set that).
  </action>
  <verify>
    <automated>MIX_ENV=test mix sigra.fixture.rebless_golden --check && bash -n scripts/ci/install-smoke.sh && bash -n scripts/ci/admin-acceptance-smoke.sh && grep -q 'phx.new --version' scripts/ci/install-smoke.sh && grep -q 'phx.new --version' scripts/ci/admin-acceptance-smoke.sh && grep -q 'rebless_golden --check' .github/workflows/ci.yml</automated>
  </verify>
  <done>
    `mix sigra.fixture.rebless_golden --check` exits 0 (fixture up-to-date) against the pinned
    archive. A `--check` step exists in `install_golden_contract` in `ci.yml` as a hard gate. Both
    smoke scripts contain a version-assert preamble (using `mix phx.new --version`) before their
    `mix phx.new` scaffold call, and both pass `bash -n` syntax check.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove COMPAT-03 end-to-end — generated-host acceptance smoke green against current phx.new</name>
  <files>scripts/ci/admin-acceptance-smoke.sh</files>
  <read_first>
    - scripts/ci/admin-acceptance-smoke.sh (scaffolds phx.new + sigra.install + boots the generated host + runs the Playwright acceptance smoke; inherits whatever archive the pin resolves to)
    - CLAUDE.md dev-prereq / reference note on port + Postgres for the generated-host smoke (default PORT=4017; the generated host uses system Homebrew Postgres on 5432, not the Docker test PG — set PGPORT accordingly when a Docker test PG is exporting a dynamic port)
  </read_first>
  <action>
    Verification-only — no source edits expected in this task. Run the existing COMPAT-03 gate
    against the pinned (>= 1.8.8) archive to prove the generated host boots and passes the admin
    acceptance smoke: `GITHUB_WORKSPACE=$(pwd) scripts/ci/admin-acceptance-smoke.sh --test chrome`
    (the focused chrome slice is sufficient to prove generated-host parity; the full suite is the CI
    lane's job). The D-11 version-assert added in Task 2 will fail fast if the resolved archive is not
    the pin target — that is the intended guard, confirming the smoke ran against the correct version.

    Ensure live Postgres is available for the generated host. If a Dockerized test PG is exporting a
    dynamic PGPORT via `tmp/db.env`, override `PGPORT=5432` for this smoke (the generated host uses
    the system Postgres on 5432, per the Phase 211 lesson), or point it at whichever live Postgres is
    serving 5432.

    If the smoke fails for a real product reason (not an environment/DB/port issue), halt and surface
    it — this is the COMPAT-03 ground-truth gate.
  </action>
  <verify>
    <automated>GITHUB_WORKSPACE=$(pwd) PGPORT=5432 scripts/ci/admin-acceptance-smoke.sh --test chrome</automated>
  </verify>
  <done>
    `admin-acceptance-smoke.sh --test chrome` exits 0 against the pinned >= 1.8.8 archive: the
    generated host scaffolds, installs Sigra, boots, and passes the focused admin acceptance
    Playwright smoke (COMPAT-03). The D-11 version-assert confirmed the smoke ran against the pin
    target, not a stale archive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phx.new Hex archive pin → CI build inputs | The archive version pinned in workflow YAML is a build input across 11 jobs; a floating/unpinned install lets a silent upstream upgrade change generated bytes undetected |
| cached toolchain on a reused runner → smoke gate result | A stale cached 1.8.7 archive could make a smoke gate pass against the wrong generator version, masking the compat gap (false green) |
| committed fixture vs pinned-archive output | Future phx.new config additions can silently drift the fixture from what the pinned archive generates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-213-03 | Tampering / supply-chain | phx.new archive pin in `ci.yml`/`release-please.yml`/`hex-publish.yml` | high | mitigate | Keep a CONCRETE `phx_new 1.8.8` pin (D-07) rather than a floating archive install — a concrete pin keeps generated bytes deterministic and eliminates the SEED-004 silent-upgrade class across all 11 sites |
| T-213-04 | Repudiation (false-green) | `install-smoke.sh` / `admin-acceptance-smoke.sh` inheriting a cached/stale archive | high | mitigate | D-11 resolved-version assert in both smoke preambles — capture `mix phx.new --version` and fail fast unless it equals the pin target, so a cached 1.8.7 archive cannot produce a passing gate |
| T-213-05 | Tampering (undetected drift) | committed golden fixture vs future phx.new config additions | medium | mitigate | `rebless_golden --check` CI drift-detector (D-06) as a hard gate — a future silent phx.new config addition surfaces as an actionable red signal (exit 2), not an opaque byte-diff failure |
| T-213-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs no npm/pip/cargo packages; the only "package" is the already-vetted `phx_new` Hex archive, pinned to a concrete version (D-07 — no floating install) |
</threat_model>

<verification>
- `grep -rn 'phx_new 1.8.7' .github/workflows CLAUDE.md CONTRIBUTING.md mix.exs guides/recipes/local-development.md` — returns nothing (COMPAT-03).
- `MIX_ENV=test mix test test/sigra/planning/phase_198_contributor_dx_contract_test.exs` — green (assertion updated to pin target).
- `MIX_ENV=test mix sigra.fixture.rebless_golden --check` — exit 0 (fixture up-to-date; D-06 drift-detector).
- `grep -q 'rebless_golden --check' .github/workflows/ci.yml` — the drift-detector step is wired.
- Both smoke scripts contain a `mix phx.new --version` assert (D-11) and pass `bash -n`.
- `GITHUB_WORKSPACE=$(pwd) PGPORT=5432 scripts/ci/admin-acceptance-smoke.sh --test chrome` — exit 0 (COMPAT-03).
</verification>

<success_criteria>
- The `phx_new 1.8.7` pin is absent from all CI workflow files, CLAUDE.md, CONTRIBUTING.md, mix.exs,
  and local-development.md; all 11 pin sites read the concrete `phx_new 1.8.8` target (D-07/D-08,
  COMPAT-03).
- The phase-198 DX-contract test passes with its literal-string assertion updated in the same commit
  as the CONTRIBUTING.md rewrite (Pitfall 2).
- CI tracks current Phoenix via a concrete pin plus the `rebless_golden --check` hard-gate
  drift-detector (D-06), not a floating archive (D-07).
- Both smoke scripts assert the resolved phx.new version (D-11), and the generated-host acceptance
  smoke runs green against the current pinned phx.new (COMPAT-03).
</success_criteria>

<output>
Create `.planning/phases/213-latest-phoenix-compatibility/213-02-SUMMARY.md` when done. Record the
concrete pin version used, the full list of touched files, confirmation that zero `1.8.7`
breadcrumbs remain, and the smoke/`--check`/phase-198 gate results.
</output>
