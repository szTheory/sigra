---
phase: 20
plan: 05
type: execute
wave: 3
depends_on:
  - "20-03"
files_modified:
  - test/sigra/install/features/passkeys_js_test.exs
  - test/support/install_fixture.ex
autonomous: true
gap_closure: true
requirements:
  - GEN-06
must_haves:
  truths:
    - "GEN-06 installer integration coverage completes reliably under normal repo verification settings."
    - "The passkeys JS rerun/idempotency spec has an explicit timeout posture that matches the real integration cost or a measurably faster helper path."
  artifacts:
    - path: "test/sigra/install/features/passkeys_js_test.exs"
      provides: "Reliable GEN-06 integration coverage for marker injection, rerun idempotency, and manual fallback"
      contains: "rerunning install keeps a single passkey marker block"
    - path: "test/support/install_fixture.ex"
      provides: "Installer fixture helpers whose latency profile is suitable for repeated integration tests"
      contains: "run_sigra_install"
  key_links:
    - from: "test/sigra/install/features/passkeys_js_test.exs"
      to: "test/support/install_fixture.ex"
      via: "tmp-app installer integration runs through the shared fixture helpers"
      pattern: "InstallFixture.run_sigra_install"
    - from: "test/sigra/install/features/passkeys_js_test.exs"
      to: "lib/sigra/install/features/passkeys.ex"
      via: "GEN-06 verification still exercises the real passkeys feature install path"
      pattern: "--passkeys"
---

<objective>
Close the GEN-06 verification gap by making the passkeys installer integration test pass reliably under default repo settings, either through a cheaper rerun path or by declaring the integration timeout explicitly in the committed spec.

Purpose: The feature works, but the default verification path is red because the rerun/idempotency integration test exceeds ExUnit’s default timeout. This plan fixes the verification contract without reducing GEN-06 coverage.

Output: Faster or explicitly budgeted installer integration coverage for `test/sigra/install/features/passkeys_js_test.exs`, plus any minimal fixture support needed to keep the real install path under control.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-CONTEXT.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-VERIFICATION.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-03-SUMMARY.md

@test/sigra/install/features/passkeys_js_test.exs
@test/sigra/install/features/passkeys_test.exs
@test/support/install_fixture.ex
@test/sigra/install/golden_diff_test.exs
</context>

<interfaces>
Current failing path from verification:
- `mix test test/sigra/install/features/passkeys_js_test.exs:46 --timeout 180000` passed in 40.9s.
- The default focused phase suite timed out because ExUnit module/test timeout remained at the default 60000ms.

Existing fixture seam:

```elixir
@spec setup_tmp_app_without_install(keyword()) :: {:ok, %{app_dir: Path.t()}}
@spec run_sigra_install(Path.t(), [String.t()]) :: {:ok, String.t()}
```

This plan must keep the real installer path. It may:
- reduce fixture/setup latency for the rerun case, or
- declare explicit timeout expectations in `test/sigra/install/features/passkeys_js_test.exs`, or
- do both.

It must not replace GEN-06 with a mocked injector-only test.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Set an explicit timeout posture for the slow installer integration path</name>
  <files>test/sigra/install/features/passkeys_js_test.exs</files>
  <read_first>
    - test/sigra/install/features/passkeys_js_test.exs
    - test/sigra/install/golden_diff_test.exs
    - .planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-VERIFICATION.md
    - .planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-03-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - `test/sigra/install/features/passkeys_js_test.exs` declares an explicit module or test timeout greater than the observed 40.9s rerun duration.
    - The timeout declaration is narrow to this slow integration module, not a repo-wide ExUnit default change.
    - The file still exercises standard injection, rerun idempotency, manual fallback, and hook-template export coverage.
  </acceptance_criteria>
  <action>Update `test/sigra/install/features/passkeys_js_test.exs` to carry an explicit timeout budget that matches the real integration cost reported in `20-VERIFICATION.md`. Follow the repo pattern used by other expensive integration modules such as `golden_diff_test.exs`: scope the timeout to this module or the single slow rerun test, not global ExUnit config. Keep the test file otherwise focused on the real installer flow; do not downgrade it to injector-unit coverage.</action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/install/features/passkeys_js_test.exs --max-failures 1</automated>
  </verify>
  <done>The committed GEN-06 integration module has an explicit timeout posture that allows the real rerun/idempotency path to pass under normal repo settings.</done>
</task>

<task type="auto">
  <name>Task 2: Trim fixture overhead only if it materially improves the default pass path</name>
  <files>test/support/install_fixture.ex, test/sigra/install/features/passkeys_js_test.exs</files>
  <read_first>
    - test/support/install_fixture.ex
    - test/sigra/install/features/passkeys_js_test.exs
    - test/sigra/install/features/passkeys_test.exs
    - .planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - Any fixture change preserves the real `mix sigra.install` integration path and the existing golden-diff behavior.
    - No repo-wide timeout defaults are changed.
    - The focused Phase 20 verification subset passes with the passkeys JS module included and without requiring a CLI `--timeout` override.
  </acceptance_criteria>
  <action>Inspect `test/support/install_fixture.ex` for narrow opportunities to reduce repeated setup cost in the passkeys JS rerun scenario, but only keep changes that materially improve the default verification path without changing the meaning of the test. Acceptable examples are sharing already-prepared tmp-app state inside the module or avoiding redundant reads/writes in the helper path. Do not remove the second real installer run from the rerun/idempotency test, do not convert the test to a unit-level injector call, and do not touch unrelated install fixtures. If timeout scoping alone is sufficient and fixture trimming is unnecessary, keep this task as a no-op code path and only confirm the focused subset passes under default invocation.</action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/plug/passkey_challenge_test.exs test/sigra/passkeys/config_test.exs test/sigra/passkeys/rate_limit_test.exs test/sigra/install/features/passkeys_js_test.exs test/sigra/install/features/passkeys_test.exs --max-failures 1</automated>
  </verify>
  <done>The verifier’s GEN-06 gap is closed: the default focused Phase 20 suite completes with the real passkeys installer integration coverage still intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → generated tmp app | The fixture shells out into a real Phoenix app install flow and must preserve that trust boundary |
| verification budget → regression confidence | Timeout handling can hide or expose real failures depending on how narrowly it is scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-GC-04 | D | `test/sigra/install/features/passkeys_js_test.exs` | mitigate | Add an explicit timeout budget for this slow integration module so normal verification does not fail on ExUnit defaults alone |
| T-20-GC-05 | T | `test/support/install_fixture.ex` | mitigate | Preserve the real `mix sigra.install` path; any optimization must not replace integration coverage with a cheaper but less faithful path |
| T-20-GC-06 | R | GEN-06 verification contract | mitigate | Keep the rerun/idempotency assertion and manual-fallback assertion intact so timing fixes do not silently narrow coverage |
</threat_model>

<verification>
Run `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/plug/passkey_challenge_test.exs test/sigra/passkeys/config_test.exs test/sigra/passkeys/rate_limit_test.exs test/sigra/install/features/passkeys_js_test.exs test/sigra/install/features/passkeys_test.exs --max-failures 1`.
</verification>

<success_criteria>
GEN-06 is re-verified when the focused Phase 20 suite passes under default invocation and the passkeys JS installer coverage still uses the real install/rerun flow.
</success_criteria>

<output>
After completion, create `.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-05-SUMMARY.md`.
</output>
