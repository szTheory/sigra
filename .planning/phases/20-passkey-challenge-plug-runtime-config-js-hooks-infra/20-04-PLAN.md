---
phase: 20
plan: 04
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - lib/sigra/plug/passkey_challenge.ex
  - test/sigra/plug/passkey_challenge_test.exs
autonomous: true
gap_closure: true
requirements:
  - PK-06
must_haves:
  truths:
    - "A tampered passkey challenge token is deterministically rejected before `verify/5` reaches the callback."
    - "PK-06 regression coverage proves callback non-execution on tamper in the normal focused phase suite."
  artifacts:
    - path: "test/sigra/plug/passkey_challenge_test.exs"
      provides: "Deterministic tamper regression coverage for passkey challenge verification"
      contains: "callback should not be invoked for tampered tokens"
    - path: "lib/sigra/plug/passkey_challenge.ex"
      provides: "Stable token verification path that returns before callback execution on invalid tokens"
      exports: ["verify/5"]
  key_links:
    - from: "test/sigra/plug/passkey_challenge_test.exs"
      to: "lib/sigra/plug/passkey_challenge.ex"
      via: "tampered token regression exercises `verify/5` before the callback boundary"
      pattern: "PasskeyChallenge.verify"
    - from: "lib/sigra/plug/passkey_challenge.ex"
      to: "lib/sigra/token.ex"
      via: "signed token verification must fail before challenge reconstruction"
      pattern: "Sigra.Token.verify"
---

<objective>
Close the PK-06 verification gap by making tampered-token regression coverage deterministic and by explicitly proving that `Sigra.Plug.PasskeyChallenge.verify/5` never reaches the callback when the session token has been altered.

Purpose: The Phase 20 implementation already appears correct, but the verifier caught a regression test that is not trustworthy enough to defend the replay/tamper boundary. This plan hardens the proof, not the product scope.

Output: A stable tamper regression in `test/sigra/plug/passkey_challenge_test.exs` and any narrowly-scoped implementation adjustment needed in `lib/sigra/plug/passkey_challenge.ex` to make the failure mode unambiguous.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-CONTEXT.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-VERIFICATION.md
@.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-01-SUMMARY.md

@lib/sigra/plug/passkey_challenge.ex
@lib/sigra/token.ex
@test/sigra/plug/passkey_challenge_test.exs
@test/sigra/token_test.exs
</context>

<interfaces>
```elixir
@spec verify(Plug.Conn.t(), :registration | :authentication, Sigra.Config.t(), keyword(), verifier()) ::
        {:ok, Plug.Conn.t(), term()} | {:error, Plug.Conn.t(), term()}
```

Current gap shape from verification:
- `test/sigra/plug/passkey_challenge_test.exs:162` used a string-tail replacement tamper strategy that was not reliable in the full focused suite.
- Direct token probing already showed the implementation rejects a genuinely altered signed token with `{:error, :invalid}`.

Use the existing token tamper pattern already proven elsewhere in the repo:

```elixir
tampered_signed = <<prefix::binary, Bitwise.bxor(byte, 0x01)::8, suffix::binary>>
tampered = Base.url_encode64(tampered_signed, padding: false)
```

The regression must assert a side effect proving non-execution, not just rely on `flunk/1`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the flaky tamper regression with a deterministic callback-boundary proof</name>
  <files>test/sigra/plug/passkey_challenge_test.exs</files>
  <read_first>
    - test/sigra/plug/passkey_challenge_test.exs
    - test/sigra/token_test.exs
    - .planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-VERIFICATION.md
    - .planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-01-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - The tamper test mutates the signed token bytes rather than replacing a trailing character with a hardcoded string.
    - The tamper test includes an explicit callback side-effect assertion showing the callback was not reached, not only a `flunk/1` branch.
    - The tamper test asserts the returned tuple is `{:error, conn, :invalid}` unless the implementation intentionally maps invalid and expired together; if so, the task must tighten the implementation to return `:invalid` for tamper.
    - The test still asserts the tampered session slot is preserved after the failed verification attempt.
  </acceptance_criteria>
  <action>Update `test/sigra/plug/passkey_challenge_test.exs` per D-02 and the verifier gap so tampering is deterministic. Reuse the repo’s proven byte-flip token mutation pattern from `test/sigra/token_test.exs` instead of `binary_part(... ) <> "x"`, because the current tail replacement can accidentally produce a still-acceptable token representation. Add a callback boundary proof that uses a local message send or `refute_received` pattern after calling `PasskeyChallenge.verify/5`, so the regression demonstrates that verify returns before any callback execution on tamper. Keep the test in the same AAA style and preserve the existing session-slot retention assertion.</action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/plug/passkey_challenge_test.exs --max-failures 1</automated>
  </verify>
  <done>The PK-06 tamper regression fails whenever the callback is reached, passes reliably when the token is altered, and proves slot preservation after the rejected verify attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the verify path only if needed to keep tamper rejection unambiguous</name>
  <files>lib/sigra/plug/passkey_challenge.ex, test/sigra/plug/passkey_challenge_test.exs</files>
  <read_first>
    - lib/sigra/plug/passkey_challenge.ex
    - lib/sigra/token.ex
    - test/sigra/plug/passkey_challenge_test.exs
    - .planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - `Sigra.Plug.PasskeyChallenge.verify/5` returns before challenge reconstruction when token verification fails.
    - If any implementation change is made, it is limited to clarifying invalid-token handling and does not change the success-path contract, slot names, or delete-on-success semantics from the original Phase 20 plan.
    - The focused default verifier subset covering Phase 20 passkey tests passes without the tamper case invoking the callback.
  </acceptance_criteria>
  <action>Run the updated PK-06 test mentally against `lib/sigra/plug/passkey_challenge.ex`. If the test reveals ambiguous invalid/expired handling or any callback reachability on tamper, make the smallest possible adjustment inside `verify/5` so `Sigra.Token.verify/4` failure exits immediately before challenge reconstruction or callback invocation. Do not broaden this into a refactor, do not touch Phase 19 challenge builders, and do not alter the existing delete-on-success / preserve-on-error semantics from D-03.</action>
  <verify>
    <automated>PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/plug/passkey_challenge_test.exs test/sigra/passkeys/config_test.exs test/sigra/passkeys/rate_limit_test.exs test/sigra/install/features/passkeys_js_test.exs test/sigra/install/features/passkeys_test.exs --max-failures 1</automated>
  </verify>
  <done>The verifier’s PK-06 gap is closed: tampered tokens are rejected deterministically before callback execution, and the focused Phase 20 suite no longer fails on the tamper regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/session state → `verify/5` | Untrusted or altered challenge tokens cross into the passkey verification edge |
| token verification → callback | Only a verified server-authored token may allow the callback boundary to execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-GC-01 | T | `test/sigra/plug/passkey_challenge_test.exs` | mitigate | Replace flaky tamper mutation with deterministic signed-byte alteration so the regression really tests token integrity failure |
| T-20-GC-02 | E | `Sigra.Plug.PasskeyChallenge.verify/5` | mitigate | Prove and preserve the early return on invalid signed tokens before any callback execution |
| T-20-GC-03 | R | tamper failure path | mitigate | Keep the session slot intact on invalid-token errors so the failed state remains inspectable and retry semantics do not silently mutate |
</threat_model>

<verification>
Run `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost mix test test/sigra/plug/passkey_challenge_test.exs test/sigra/passkeys/config_test.exs test/sigra/passkeys/rate_limit_test.exs test/sigra/install/features/passkeys_js_test.exs test/sigra/install/features/passkeys_test.exs --max-failures 1`.
</verification>

<success_criteria>
PK-06 is re-verified when the focused phase suite proves that a tampered signed challenge token is rejected as invalid and `verify/5` never invokes the callback on that path.
</success_criteria>

<output>
After completion, create `.planning/phases/20-passkey-challenge-plug-runtime-config-js-hooks-infra/20-04-SUMMARY.md`.
</output>
