---
phase: 131
plan: 05
type: execute
wave: 4
depends_on:
  - 131-03
  - 131-04
files_modified:
  - lib/sigra/application.ex
  - test/sigra/application_forwarders_test.exs
autonomous: true
requirements:
  - TL-02
  - TL-04
user_setup: []

must_haves:
  truths:
    - "Sigra.Application.start/2 calls maybe_warn_missing_forwarder_deps/0 BEFORE attach_forwarders/0 (D-25 ordering)"
    - "maybe_warn_missing_forwarder_deps/0 emits ONE Logger.warning per configured forwarder whose module is not loaded (D-23, D-25, TL-04)"
    - "Logger.warning text names the forwarder module, links guides/recipes/companion-libs/threadline.md, and offers actionable fallback (add dep / remove entry) (RESEARCH.md §2.4)"
    - "Logger.warning text does NOT log :api_key, :endpoint, or any impl-specific opts (T-131-14)"
    - "attach_forwarders/0 iterates each configured forwarder; calls module.attach(opts) only when Code.ensure_loaded?(module) is true (D-23 — Noop is implicit via no-attach)"
    - "attach_forwarders/0 RAISES at boot when ANY entry has dispatch: :async AND Oban is not supervised (D-26 — boot-time fail is correct; silent degrade is forbidden)"
    - "Raise message names the offending forwarder, missing dep (:oban), and recommends :auto as fallback (D-26)"
    - "Config lookup uses Application.get_env(:sigra, :otp_app) then Application.get_env(otp_app, :sigra_config) cascade — SINGLE pattern (D-27)"
    - "Absent / empty :forwarders list = no warnings, no attach calls, zero overhead (D-09, TL-04)"
    - "Boot-wiring satisfies Success Criterion #2 (Threadline absent → clean compile + one warning + Noop substitutes)"
    - "Boot-wiring satisfies Success Criterion #4 (:async + no Oban raises at boot, not at first dispatch)"
  artifacts:
    - path: "lib/sigra/application.ex"
      provides: "Two new boot helpers + 2 new call sites in start/2"
      contains: "maybe_warn_missing_forwarder_deps"
    - path: "test/sigra/application_forwarders_test.exs"
      provides: "Boot-time wiring tests — missing-dep warning, :async raise, valid attach"
  key_links:
    - from: "Sigra.Application.start/2"
      to: "configured forwarder modules"
      via: "attach_forwarders/0 → module.attach(opts) per D-23 + D-25"
      pattern: "attach_forwarders"
    - from: "attach_forwarders/0"
      to: "host audit[:forwarders] config"
      via: "Application.get_env cascade per D-27"
      pattern: "Application\\.get_env\\(otp_app, :sigra_config\\)"
---

<objective>
Wire the boot-time activation of audit forwarders into `Sigra.Application.start/2`: emit a one-shot `Logger.warning` per configured-but-missing forwarder dep (TL-04) and call `attach/1` on every configured-and-loaded forwarder. Raise at boot when any entry has `dispatch: :async` and Oban is not supervised (D-26 — fail-fast over silent degrade).

Purpose: TL-02 + TL-04. Plan 05 is the glue that turns a config block into a running telemetry handler. Without it, the host's `forwarders: [...]` block does nothing at boot.

Output: 1 modified `lib/sigra/application.ex` + 1 new boot-wiring test file. Depends on Plan 03 (config schema + dispatcher's oban_running? helper) and Plan 04 (the actual modules `attach/1` is called on).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md

<interfaces>
<!-- Current Sigra.Application.start/2 (lib/sigra/application.ex:21-27 — verbatim in RESEARCH.md §2.4): -->
@impl Application
def start(_type, _args) do
  maybe_warn_audit_cleanup_fallback()
  maybe_warn_missing_cookie_domain()
  verify_vault!()
  Supervisor.start_link([], strategy: :one_for_one, name: Sigra.Supervisor)
end

<!-- Target after Plan 05 edit (D-25 ordering — RESEARCH.md §2.4): -->
@impl Application
def start(_type, _args) do
  maybe_warn_audit_cleanup_fallback()
  maybe_warn_missing_cookie_domain()
  maybe_warn_missing_forwarder_deps()   # NEW (D-25)
  attach_forwarders()                    # NEW (D-25; raises if :async + no Oban per D-26)
  verify_vault!()
  Supervisor.start_link([], strategy: :one_for_one, name: Sigra.Supervisor)
end

<!-- Existing precedent (D-25 voice mirror) — maybe_warn_audit_cleanup_fallback/0 at lib/sigra/application.ex:68-88, verbatim in RESEARCH.md §2.4. -->

<!-- Draft for maybe_warn_missing_forwarder_deps/0 — RESEARCH.md §2.4 provides the full body. -->

<!-- Config cascade pattern (D-27, mirror lib/sigra/application.ex:30-38 + 92-101 — single idiom): -->
otp_app = Application.get_env(:sigra, :otp_app)
forwarders =
  case otp_app && Application.get_env(otp_app, :sigra_config) do
    opts when is_list(opts) -> opts |> Keyword.get(:audit, []) |> Keyword.get(:forwarders, [])
    _ -> []
  end

<!-- Oban-running check (must call Plan 03's dispatcher's helper, NOT duplicate the logic — D-12 single source). -->
<!-- If Sigra.Audit.Forwarders.oban_running?/0 is private, expose a public Sigra.Audit.Forwarders.oban_running?/1 that takes opts (per D-32 :oban override). -->

<!-- D-26 raise message shape (no exact precedent — closest analog verify_vault!/1 at application.ex:113-124): -->
raise ArgumentError, """
[Sigra.Audit] Forwarder #{inspect(module)} is configured with dispatch: :async
but Oban is not supervised in this app.

Boot-time fail is intentional: silent degradation to :sync would mask the
misconfiguration. Fix one of:
  - add {:oban, "~> 2.17"} to mix.exs deps and supervise it, or
  - change dispatch to :auto (falls back to :sync if Oban is absent), or
  - remove the forwarder entry.

See guides/recipes/companion-libs/threadline.md for full wiring.
"""
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing boot-wiring tests</name>
  <files>test/sigra/application_forwarders_test.exs</files>
  <read_first>
    - lib/sigra/application.ex (full file — boot helpers + start/2; especially lines 21-27, 30-38, 68-88, 92-101, 113-124)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.4 — full body draft for maybe_warn_missing_forwarder_deps/0)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-25, D-26, D-27)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md (§7 application.ex modifications)
    - test/sigra/audit/changeset_test.exs (lines 1-30 — AAA voice mirror)
    - Confirm whether test/sigra/application_test.exs already exists: `ls test/sigra/application*`. If it exists, append to it; if not, create a sibling file as named in <files>.
  </read_first>
  <behavior>
    - Test 1 (D-25 — missing-dep warning): set `Application.put_env(:test_app, :sigra_config, audit: [forwarders: [[module: NotALoadedModule, dispatch: :sync, id: :test]]])` with `Application.put_env(:sigra, :otp_app, :test_app)`. Wrap `Sigra.Application.maybe_warn_missing_forwarder_deps()` in `ExUnit.CaptureLog.capture_log/1`. Assert the captured log contains "NotALoadedModule" AND "guides/recipes/companion-libs/threadline.md" (RESEARCH.md §2.4 voice). Assert it contains exactly ONE warning line (one-shot per D-23, not repeated).

    - Test 2 (D-25 — present dep emits no warning): configure a real forwarder module that IS loaded (use Sigra.Audit.Forwarders.Noop). Assert `capture_log(fn -> maybe_warn_missing_forwarder_deps() end) == ""` (no warning when module loaded).

    - Test 3 (D-9 — empty forwarders = silent): set audit: [forwarders: []]. Assert capture_log returns "" — empty list is zero overhead.

    - Test 4 (D-9 — absent audit key = silent): set sigra_config without :audit. Assert capture_log returns "" — no audit means no forwarders means no warnings.

    - Test 5 (D-23 — Logger.warning does NOT leak :api_key): configure forwarder with `[module: SomeMissingModule, dispatch: :sync, endpoint: "https://example.com", api_key: "TOPSECRET-DO-NOT-LEAK"]`. Assert the captured log does NOT contain "TOPSECRET-DO-NOT-LEAK" and does NOT contain "api_key" (the warning is module-name-only, not opts-blob).

    - Test 6 (D-26 — :async + no Oban raises): configure forwarder with `[module: Sigra.Audit.Forwarders.Noop, dispatch: :async]`. Mock Process.whereis(Oban) to return nil (or use :oban override per D-32 to point at a non-existent module). Assert `assert_raise ArgumentError, ~r/:async.+Oban.+:auto/, fn -> Sigra.Application.attach_forwarders() end`. The regex MUST match "async", "Oban", and ":auto" (the recommended fallback) per D-26.

    - Test 7 (D-26 — raise message names the offending forwarder): in the same scenario as Test 6, capture the raised exception's message; assert it contains "Sigra.Audit.Forwarders.Noop" (the module name).

    - Test 8 (D-25 — valid :auto config does NOT raise, attaches): with Sigra.Audit.Forwarders.Noop + dispatch: :auto, Oban absent → no raise, attach succeeds. Assert `Sigra.Audit.Forwarders.Noop.attach/1` was called (since Noop.attach returns :ok with no side effect, instrumenting this is tricky — alternative: assert attach_forwarders/0 returns :ok and that :telemetry.list_handlers([:sigra, :audit, :log]) reflects the absence of subscription — Noop does NOT attach to :telemetry per D-22).

    - Test 9 (D-27 — config cascade works): integration test that puts a real sigra_config and calls maybe_warn_missing_forwarder_deps; assert the cascade reads via Application.get_env(:sigra, :otp_app) → Application.get_env(otp_app, :sigra_config) → Keyword.get(:audit) → Keyword.get(:forwarders).

    In all tests, restore Application env in on_exit to prevent cross-test bleed.
  </behavior>
  <action>
    Create test/sigra/application_forwarders_test.exs (verify name with `ls test/sigra/application*` — if a sibling `application_test.exs` exists, prefer extending it; if not, this name follows the `<feature>_test.exs` convention). Use `use ExUnit.Case, async: false` (Application env is global, must be sequential). `import ExUnit.CaptureLog`. Tests 1-9 as enumerated.

    For Test 6/7 raise-on-async: the cleanest way to "mock" Oban-not-supervised in tests is the :oban-override option from D-32 + Plan 03's dispatcher — wire the option down so attach_forwarders/0 accepts an optional `oban: SomeNonExistentModule` opts override, and assert. If the production attach_forwarders/0 doesn't accept opts (it reads from Application env), build a test that pokes Process.whereis temporarily — but the simplest path is making attach_forwarders/0 accept opts for test injection per the lib/sigra/delivery.ex:47 precedent.

    Setup/teardown: `setup do; Application.put_env(:sigra, :otp_app, :test_app); on_exit(fn -> Application.delete_env(:test_app, :sigra_config); Application.delete_env(:sigra, :otp_app) end); end`.

    All tests MUST initially fail (UndefinedFunctionError on Sigra.Application.maybe_warn_missing_forwarder_deps/0 and attach_forwarders/0). Wave 0 RED.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/application_forwarders_test.exs 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: test/sigra/application_forwarders_test.exs exists with >= 7 test cases.
    - Source: `grep -c 'assert_raise ArgumentError' test/sigra/application_forwarders_test.exs` returns >= 1 (D-26).
    - Source: `grep -c 'capture_log' test/sigra/application_forwarders_test.exs` returns >= 4 (Tests 1-5 use it).
    - Source: `grep -c 'TOPSECRET-DO-NOT-LEAK\|api_key.*leak\|api_key.*MUST NOT' test/sigra/application_forwarders_test.exs` returns >= 1 (Test 5 redaction).
    - Source: `grep -c 'guides/recipes/companion-libs/threadline.md' test/sigra/application_forwarders_test.exs` returns >= 1 (Test 1 recipe link).
    - Source: `grep -c 'attach_forwarders\|maybe_warn_missing_forwarder_deps' test/sigra/application_forwarders_test.exs` returns >= 2 (both new helpers exercised).
    - Behavior (pre-impl): `MIX_ENV=test mix test test/sigra/application_forwarders_test.exs` exits non-zero (RED — helpers don't exist).
  </acceptance_criteria>
  <done>Test file committed and RED. Task 2 lands the helpers and turns it GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Implement maybe_warn_missing_forwarder_deps/0 + attach_forwarders/0 + wire into start/2</name>
  <files>lib/sigra/application.ex</files>
  <read_first>
    - lib/sigra/application.ex (FULL FILE — read entirely so you understand the current call order, the maybe_warn_audit_cleanup_fallback/0 precedent at 68-88, the config cascade at 30-38 + 92-101, and the verify_vault!/1 raise voice at 113-124)
    - lib/sigra/audit/forwarders.ex (Plan 03 — exposes oban_running?/1 or equivalent; verify the public surface and use it; do NOT duplicate Code.ensure_loaded?(Oban) and Process.whereis(Oban) logic in application.ex per D-12 "single source")
    - lib/sigra/plug/rate_limit.ex (lines 84-95 — resolve_limiter/1 D-23 split precedent: "if dep loaded → use it; else log warning + Noop")
    - test/sigra/application_forwarders_test.exs (Task 1 — acceptance contract)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.4 — full draft for maybe_warn_missing_forwarder_deps/0)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-02, D-23, D-25, D-26, D-27)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md (§7 application.ex modifications — explicit "voice mirror verify_vault!/1 for D-26 raise heredoc")
  </read_first>
  <action>
    Edit lib/sigra/application.ex. Use the planner-draft body in RESEARCH.md §2.4 as your literal voice mirror for `maybe_warn_missing_forwarder_deps/0`.

    Step 1 (D-25 — wire two new calls into start/2): locate `def start(_type, _args)` (lines 21-27 — verify with `grep -n 'def start' lib/sigra/application.ex`). Insert TWO new calls in the order specified by D-25 / RESEARCH.md §2.4:

    ```
    maybe_warn_audit_cleanup_fallback()
    maybe_warn_missing_cookie_domain()
    maybe_warn_missing_forwarder_deps()   # NEW (D-25)
    attach_forwarders()                    # NEW (D-25 + D-26 raise on :async + no Oban)
    verify_vault!()
    ```

    The order matters: warnings BEFORE attach (so the operator sees missing-dep messages before any attach error), attach BEFORE verify_vault (the vault check is a hard-fail; forwarder attach should run first since it can also raise — but only on misconfig, not on missing crypto).

    Step 2 — Implement `@doc false def maybe_warn_missing_forwarder_deps/0`. Use the literal body from RESEARCH.md §2.4 (the "Logger.warning(\"\"\"...\"\"\")" multi-line heredoc). Key requirements:

    - Use the D-27 config cascade EXACTLY: `otp_app = Application.get_env(:sigra, :otp_app); forwarders = case otp_app && Application.get_env(otp_app, :sigra_config) do; opts when is_list(opts) -> opts |> Keyword.get(:audit, []) |> Keyword.get(:forwarders, []); _ -> [] end` — byte-for-byte the same cascade pattern as the existing helpers at lines 30-38 and 92-101 (D-27 explicit: single config-resolution idiom).

    - Iterate `forwarders` with `Enum.each`. For each entry, extract `module = Keyword.fetch!(forwarder_opts, :module)`. If `Code.ensure_loaded?(module)` is false, emit ONE `Logger.warning("""...""")` per entry. The warning heredoc names the module (via `#{inspect(module)}`), names the missing dep (for Threadline, the message says `{:threadline, "~> 0.5", optional: true}`), and links `guides/recipes/companion-libs/threadline.md`. Voice mirror: RESEARCH.md §2.4 trailing draft.

    - T-131-14 (redaction): the Logger.warning text MUST NOT interpolate `forwarder_opts` itself — only the `:module` name. Do NOT `inspect(forwarder_opts)`. Test 5 in Task 1 grep-asserts no api_key leak.

    - Returns `:ok` at the end.

    Step 3 — Implement `@doc false def attach_forwarders/0`. Body:

    - Same D-27 config cascade as above (extract `forwarders` list).

    - Iterate `forwarders` with `Enum.each`. For each entry:
      * Extract `module = Keyword.fetch!(forwarder_opts, :module)` and `dispatch = Keyword.get(forwarder_opts, :dispatch, :auto)`.
      * D-26 raise: `if dispatch == :async and not Sigra.Audit.Forwarders.oban_running?(forwarder_opts)` (calls Plan 03's PUBLIC helper — BLOCKER 2 fix ensured this is `def oban_running?(opts)`, not `defp`; arity /1 with :oban override per D-32) THEN raise `ArgumentError` with the message in the <interfaces> block above (or equivalent — mirroring verify_vault!/1 voice at application.ex:113-124).
      * D-23 split: `if Code.ensure_loaded?(module), do: module.attach(forwarder_opts), else: :ok` (skip when missing — Plan 04's optional-dep wrap means the Threadline module doesn't exist when :threadline is absent; calling attach would raise UndefinedFunctionError. Skip silently — the warning from Step 2 already informed the operator).

    - Returns `:ok` at the end (when no entry raised).

    Step 4 — verify the new helpers are reachable from tests. Both are `@doc false` (private-ish) but `def` (not `defp`) so test/sigra/application_forwarders_test.exs can call them directly. This matches the `@doc false def maybe_warn_audit_cleanup_fallback` precedent at line 70.

    Do NOT modify maybe_warn_audit_cleanup_fallback/0, maybe_warn_missing_cookie_domain/0, or verify_vault!/1. Do NOT add a new Supervisor child. Do NOT introduce a second config-resolution pattern (D-27 explicit — Plan-validation will grep for this).

    NOTE (WARNING 3 — raise voice divergence): The D-26 raise uses `raise ArgumentError, """..."""` rather than the bare `raise """..."""` (RuntimeError default) used at `lib/sigra/application.ex:115` for `verify_vault!`. This is INTENTIONAL — `:async + no Oban` IS semantically an invalid argument (the host's `dispatch: :async` config is incompatible with their lack of a supervised Oban), so `ArgumentError` is the more idiomatic Elixir choice. Task 1's Test 6 already asserts `assert_raise ArgumentError, ~r/...async.+Oban.+:auto/`, so the test contract LOCKS this choice — do NOT change the test to `RuntimeError` or `raise` to align with verify_vault precedent. PATTERNS.md §7 says "voice mirror verify_vault!/1 for D-26 raise heredoc" — interpret that as the MESSAGE STRUCTURE (multi-line heredoc with action items and recipe link) mirroring verify_vault, not the exception class. The class is `ArgumentError` because the situation IS "bad argument".
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors; MIX_ENV=test mix test test/sigra/application_forwarders_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source (D-25 wiring): `grep -c 'maybe_warn_missing_forwarder_deps()' lib/sigra/application.ex` returns >= 2 (one definition + one call in start/2).
    - Source (D-25 wiring): `grep -c 'attach_forwarders()' lib/sigra/application.ex` returns >= 2 (one definition + one call in start/2).
    - Source (D-25 ordering): the line containing `maybe_warn_missing_forwarder_deps()` appears BEFORE the line containing `attach_forwarders()` (verify with `grep -n` and inspect line numbers).
    - Source (D-26 raise): `grep -c 'raise ArgumentError' lib/sigra/application.ex` returns >= 1 (was 0 before this plan or only in verify_vault!; if verify_vault uses ArgumentError too, expect >= 2).
    - Source (D-26 message): `grep -c ':async' lib/sigra/application.ex` returns >= 1 AND `grep -c ':auto' lib/sigra/application.ex` returns >= 1 (the raise message recommends :auto).
    - Source (D-27 — single cascade): `grep -c 'Application.get_env(otp_app, :sigra_config)' lib/sigra/application.ex` returns >= 3 (existing + 2 new helpers all use the same cascade).
    - Source (D-12 — no duplicated oban_running?): `grep -v '^#' lib/sigra/application.ex | grep -c 'Process\\.whereis(Oban)'` returns 0 (application.ex MUST NOT duplicate Plan 03's oban check; it calls Sigra.Audit.Forwarders.oban_running? or equivalent).
    - Source (T-131-14 — no opts leak): `grep -v '^#' lib/sigra/application.ex | grep -c 'inspect(forwarder_opts)\|api_key' | head -1` returns 0 (the warning text only inspects module, not opts).
    - Source (recipe link): `grep -c 'guides/recipes/companion-libs/threadline.md' lib/sigra/application.ex` returns >= 1.
    - Behavior: `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
    - Test: `MIX_ENV=test mix test test/sigra/application_forwarders_test.exs` exits 0 (all 9 tests GREEN).
    - Regression: `MIX_ENV=test mix test test/sigra/` exits 0.
  </acceptance_criteria>
  <done>Both new helpers exist; start/2 calls them in the D-25 order; D-26 raise fires on :async + no Oban with a message naming the forwarder, the dep, and :auto fallback; no PII leak in Logger.warning; all 9 boot-wiring tests GREEN.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Source assertions — wiring and ordering
grep -c 'maybe_warn_missing_forwarder_deps()' lib/sigra/application.ex          # >= 2 (def + call)
grep -c 'attach_forwarders()' lib/sigra/application.ex                          # >= 2 (def + call)
grep -n 'maybe_warn_missing_forwarder_deps()\|attach_forwarders()' lib/sigra/application.ex
# Inspect output: the maybe_warn_missing_forwarder_deps() CALL must appear before attach_forwarders() CALL (D-25)

# Source assertions — D-26 raise
grep -c 'raise ArgumentError' lib/sigra/application.ex                           # >= 1
grep -c ':async' lib/sigra/application.ex                                        # >= 1 (in raise message)
grep -c ':auto' lib/sigra/application.ex                                         # >= 1 (recommended fallback)
grep -c 'Oban' lib/sigra/application.ex                                          # >= 1 (in raise message)

# Source assertions — D-27 single cascade + D-12 single oban check
grep -c 'Application.get_env(otp_app, :sigra_config)' lib/sigra/application.ex   # >= 3
grep -v '^#' lib/sigra/application.ex | grep -c 'Process\\.whereis(Oban)'        # == 0 (must call Plan 03 helper)

# Source assertions — T-131-14 redaction
grep -v '^#' lib/sigra/application.ex | grep -c 'inspect(forwarder_opts)'        # == 0
grep -v '^#' lib/sigra/application.ex | grep -c 'api_key'                        # == 0 (Logger text doesn't leak)

# Source assertions — recipe link
grep -c 'guides/recipes/companion-libs/threadline.md' lib/sigra/application.ex   # >= 1

# Behavior assertions
MIX_ENV=test mix compile --warnings-as-errors                                     # exit 0
MIX_ENV=test mix test test/sigra/application_forwarders_test.exs                 # exit 0
MIX_ENV=test mix test test/sigra/                                                 # exit 0 (no regressions)

# Integration assertion — the full Phase 131 lib code compiles together
MIX_ENV=test mix test test/sigra/audit/ test/sigra/workers/audit_forward_test.exs test/sigra/application_forwarders_test.exs  # exit 0
```
</verification>

<must_haves>
truths:
  - "start/2 calls maybe_warn_missing_forwarder_deps/0 BEFORE attach_forwarders/0 (D-25 ordering)"
  - "Logger.warning text names module + links recipe + offers fallback (RESEARCH.md §2.4)"
  - "Logger.warning does NOT log :api_key / :endpoint (T-131-14 redaction)"
  - "attach_forwarders/0 raises ArgumentError on :async + Oban absent (D-26)"
  - "Raise message names the forwarder, names :oban dep, recommends :auto (D-26)"
  - "Empty / absent :forwarders list = zero overhead, no warnings (D-09)"
  - "D-27 single config-resolution cascade — no second pattern introduced"
  - "D-12 single source of truth for oban_running? — application.ex delegates to Sigra.Audit.Forwarders helper"
  - "SC-2 satisfied: Threadline absent → clean compile + warning + Noop substitutes"
  - "SC-4 satisfied: :async + no Oban raises at boot, not at first dispatch"
artifacts:
  - path: "lib/sigra/application.ex"
    provides: "Boot-wired forwarder activation"
  - path: "test/sigra/application_forwarders_test.exs"
    provides: "Boot wiring tests"
key_links:
  - from: "start/2"
    to: "host audit[:forwarders] config"
    via: "D-27 cascade"
    pattern: "Application\\.get_env\\(otp_app, :sigra_config\\)"
  - from: "attach_forwarders/0"
    to: "Sigra.Audit.Forwarders.oban_running?"
    via: "single source of truth (D-12)"
    pattern: "Sigra\\.Audit\\.Forwarders\\.oban_running"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host sigra_config → Sigra.Application.start/2 | Untrusted host keyword list. Schema (Plan 03) already validates shape; Plan 05 is the activation point. |
| Plan 05 Logger.warning → operator log stream | Warning text may be parsed by log aggregation; MUST NOT contain credentials. |
| attach_forwarders/0 → forwarder.attach(opts) | Plan 05 calls into Plan 04's impl with full opts (including :api_key). Trust the impl validates its own keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-18 | Information disclosure | Logger.warning leaks :api_key / :endpoint | mitigate | Warning text interpolates ONLY `#{inspect(module)}` — never `#{inspect(forwarder_opts)}` or any opts key. Test 5 grep-asserts no "TOPSECRET-DO-NOT-LEAK" or "api_key" appears in captured log. Source acceptance criteria grep `application.ex` for `inspect(forwarder_opts)` and `api_key` — both must return 0. |
| T-131-19 | Denial of service | :async + no Oban → silent degrade to :sync masks misconfig | mitigate | D-26 raise at boot. The raise NAMES the forwarder, the missing dep (:oban), and recommends :auto as the fallback. Test 6/7 assert the raise and message contents. Boot-time fail is the correct posture per D-26 explicit. |
| T-131-20 | Tampering | duplicated oban_running? logic in application.ex would drift from dispatcher (D-12) | mitigate | application.ex delegates to Sigra.Audit.Forwarders.oban_running?/N (Plan 03). Acceptance criterion `grep -v '^#' lib/sigra/application.ex | grep -c 'Process\\.whereis(Oban)' == 0` enforces single source. |
| T-131-21 | Repudiation | maybe_warn_missing_forwarder_deps fires zero warnings, operator unaware of misconfig | accept | Empty forwarders list IS valid config (D-09). Operators who want diagnostics enable telemetry monitoring on [:sigra, :audit, :forward, :*]. Future `mix sigra.doctor` task (deferred per STATE.md) will expose this; not Phase 131 scope. |
| T-131-22 | Elevation of privilege | host config evaluation order — attach happens BEFORE verify_vault! | accept | D-25 explicit order: forwarder attach precedes vault verify. Attach can raise on :async + no Oban (D-26) but does NOT touch crypto. If vault verify fails, attached forwarders are detached when supervisor dies — telemetry handlers don't persist across BEAM restart. Boot order is sound. |
| T-131-SC | Tampering | No new package installs in Plan 05 | n/a | Pure code. Plan 03 owned the dep introduction with the legitimacy gate. |
</threat_model>

<success_criteria>
- start/2 calls maybe_warn_missing_forwarder_deps/0 BEFORE attach_forwarders/0 BEFORE verify_vault! (D-25).
- maybe_warn_missing_forwarder_deps/0 emits exactly one Logger.warning per configured-but-missing forwarder; zero when all loaded; zero when forwarders list is empty (D-09).
- Logger.warning text contains module name + recipe path; does NOT contain api_key / endpoint values (T-131-18).
- attach_forwarders/0 raises ArgumentError with message naming the forwarder + :oban dep + :auto fallback when ANY entry has dispatch: :async and Oban is not supervised (D-26).
- attach_forwarders/0 calls module.attach(opts) only when Code.ensure_loaded?(module) is true; otherwise skips silently (D-23 split — warning from Step 2 already informed operator).
- Configuration cascade uses Application.get_env(:sigra, :otp_app) → Application.get_env(otp_app, :sigra_config) → Keyword.get(:audit) → Keyword.get(:forwarders) — single pattern across application.ex (D-27).
- application.ex does NOT duplicate Code.ensure_loaded?(Oban) and Process.whereis(Oban) (D-12 — single source in Plan 03's Sigra.Audit.Forwarders).
- mix compile --warnings-as-errors exits 0.
- All 9 boot-wiring tests GREEN.
- No regressions: test/sigra/ full slice GREEN.
- Plan 06 (optional dep-off CI lane) can now exercise the full boot path against an absent :threadline.
</success_criteria>

<output>
Create `.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-05-SUMMARY.md` when done. Include: line-range of edited start/2, line ranges of both new helpers, full text of the D-26 raise message (verbatim), full text of the D-25 Logger.warning (verbatim), test output for application_forwarders_test.exs, and confirmation of no Process.whereis(Oban) duplication via grep.
</output>
