---
phase: 131
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/audit.ex
  - test/sigra/audit_telemetry_test.exs
autonomous: true
requirements:
  - TL-05
user_setup: []

must_haves:
  truths:
    - "`[:sigra, :audit, :log]` telemetry metadata contains `:id` (UUID string from `event.id`) and `:occurred_at` (DateTime from `event.occurred_at`) (D-31)"
    - "Existing keys `:action`, `:actor_id`, `:outcome` remain present and unchanged (D-31 — additive only)"
    - "Existing subscribers (`Sigra.Telemetry.attach_default_logger` at `lib/sigra/telemetry.ex:341-348`) continue to receive their expected keys with zero behavioral change"
    - "Pitfall 4 (cross-system idempotency) is unblocked: Plan 04 Threadline impl can read `metadata.id` directly without re-querying the database"
  artifacts:
    - path: "lib/sigra/audit.ex"
      provides: "Extended `emit_telemetry/1` private fn — metadata superset"
      contains: "id: event.id"
    - path: "test/sigra/audit_telemetry_test.exs"
      provides: "Telemetry metadata contract test — asserts presence of all 5 keys"
  key_links:
    - from: "lib/sigra/audit.ex emit_telemetry/1"
      to: ":telemetry.execute([:sigra, :audit, :log], measurements, metadata)"
      via: "metadata map literal"
      pattern: "id: event\\.id"
---

<objective>
Extend `Sigra.Audit.emit_telemetry/1` metadata additively per D-31 so the `[:sigra, :audit, :log]` event carries `id` (audit row UUID) and `occurred_at` (DateTime) alongside the existing `action`, `actor_id`, `outcome`. Strict superset — no existing subscriber breaks. The new fields are the canonical idempotency-key pair the Threadline impl (Plan 04) ships to `Threadline.record_action/2` via `:correlation_id`.

Purpose: TL-05 enabler. Without these two metadata keys, Pitfall 4 (cross-system dedupe) cannot be solved without an extra DB query inside the handler. With them, the handler reads UUID + occurred_at off metadata directly.

Output: 1 modified lib file + 1 new test file. Zero file overlap with Plan 01 — runs in Wave 1 in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md

<interfaces>
<!-- The audit_event struct guarantees both new fields. Verified in RESEARCH.md §2.1. -->

From priv/templates/sigra.install/core/audit_event.ex:20-33:
- @primary_key {:id, :binary_id, autogenerate: true} — UUID, always present.

From lib/sigra/audit/changeset.ex:30-45:
- :occurred_at is in @cast_fields (line 40).
- validate_required([:action, :outcome, :occurred_at]) (line 71) — required, never nil.

From lib/sigra/audit.ex:522:
- occurred_at: Keyword.get(opts, :occurred_at, DateTime.utc_now()) — default supplied if caller omits.

Current emit_telemetry/1 body (lib/sigra/audit.ex:286-310, verbatim in RESEARCH.md §2.1):
- emits %{action: event.action, actor_id: event.actor_id, outcome: event.outcome}.

Target after Plan 02 edit (D-31 — additive only):
- emits %{action: event.action, actor_id: event.actor_id, outcome: event.outcome, id: event.id, occurred_at: event.occurred_at}.

Backwards-compat audit (RESEARCH.md §2.1): every existing subscriber pattern-matches on a subset; superset breaks nothing. Sigra.Telemetry.attach_default_logger at lib/sigra/telemetry.ex:341-348 is the canonical subscriber and tolerates extra keys.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing telemetry metadata contract test</name>
  <files>test/sigra/audit_telemetry_test.exs</files>
  <read_first>
    - lib/sigra/audit.ex (lines 286-310 — current emit_telemetry/1; verbatim in 131-RESEARCH.md §2.1)
    - lib/sigra/audit.ex (lines 1-30 — module attrs incl. @telemetry_event)
    - lib/sigra/telemetry.ex (lines 341-348 — attach_default_logger precedent)
    - test/sigra/audit/changeset_test.exs (lines 1-30 — AAA voice mirror)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-31)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.1, §8 Pitfall 4)
  </read_first>
  <behavior>
    - Test 1 (D-31 / TL-05 enabler): :telemetry.attach/4 a handler against [:sigra, :audit, :log]; call Sigra.Audit.log_safe/2 (or equivalent commit path) with a valid audit row insert; assert the handler received metadata that includes :id (binary UUID string), :occurred_at (%DateTime{}), :action, :actor_id, :outcome — all 5 keys present.
    - Test 2 (backwards-compat — RESEARCH.md §2.1): the existing keys (:action, :actor_id, :outcome) appear with exactly the values the test fixture set — adding keys does NOT mutate existing keys.
    - Test 3 (defensive): :telemetry.detach/1 the handler in on_exit to keep BEAM clean across the suite.
  </behavior>
  <action>
    Create test/sigra/audit_telemetry_test.exs (top-level under test/sigra/ — confirm with `ls test/sigra/audit*` before placing). Use `use Sigra.DataCase, async: false` (telemetry handlers are global; async: false prevents cross-test interference). In setup, attach a test-process telemetry handler against [:sigra, :audit, :log] that sends {:telemetry_event, metadata} to self(). Use a unique handler id like {__MODULE__, make_ref()}. In on_exit, detach.

    Test 1: build an audit event via the existing log_safe/2 fixture pattern (read one sibling test/sigra/audit/*_test.exs to confirm call shape) with known action/actor_id/outcome values. Assert `assert_receive {:telemetry_event, metadata}` and that metadata has keys [:action, :actor_id, :outcome, :id, :occurred_at] (all five). Assert is_binary(metadata.id) (UUID string) and match?(%DateTime{}, metadata.occurred_at).

    Test 2: assert metadata.action == fixture_action, metadata.actor_id == fixture_actor_id, metadata.outcome == fixture_outcome — proves additive (existing values unchanged).

    Test MUST initially fail because emit_telemetry/1 only emits 3 keys today. Task 2 fixes that.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/audit_telemetry_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: test/sigra/audit_telemetry_test.exs exists.
    - Source: `grep -c ':telemetry.attach' test/sigra/audit_telemetry_test.exs` returns >= 1.
    - Source: `grep -c 'metadata.id\|metadata\[:id\]' test/sigra/audit_telemetry_test.exs` returns >= 1.
    - Source: `grep -c 'metadata.occurred_at\|metadata\[:occurred_at\]' test/sigra/audit_telemetry_test.exs` returns >= 1.
    - Behavior (pre-impl): `MIX_ENV=test mix test test/sigra/audit_telemetry_test.exs` exits non-zero (RED — KeyError on :id / :occurred_at).
  </acceptance_criteria>
  <done>Test file committed; pre-impl run is RED. Task 2 turns it GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Sigra.Audit.emit_telemetry/1 metadata with :id + :occurred_at</name>
  <files>lib/sigra/audit.ex</files>
  <read_first>
    - lib/sigra/audit.ex (lines 280-320 — full emit_telemetry_from_changes/2 + emit_telemetry/1 block; verbatim in 131-RESEARCH.md §2.1)
    - lib/sigra/audit.ex (lines 1-30 — moduledoc that documents [:sigra, :audit, :log])
    - lib/sigra/audit/changeset.ex (lines 30-45 — confirms :occurred_at is cast + required)
    - priv/templates/sigra.install/core/audit_event.ex (lines 20-33 — confirms PK is binary_id UUID)
    - test/sigra/audit_telemetry_test.exs (just written in Task 1 — acceptance contract)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-31)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.1, §8 Pitfall 4)
  </read_first>
  <action>
    Edit lib/sigra/audit.ex emit_telemetry/1 private fn. Verify line number first with `grep -n 'defp emit_telemetry' lib/sigra/audit.ex`. The body is `:telemetry.execute(@telemetry_event, %{count: 1}, %{action: ..., actor_id: ..., outcome: ...})`. Extend the metadata map ONLY by adding two keys: `id: event.id` and `occurred_at: event.occurred_at`. Do NOT remove any existing key. Do NOT change `%{count: 1}` measurements. Do NOT change `@telemetry_event`. Final map keys MUST be exactly [:action, :actor_id, :outcome, :id, :occurred_at] per D-31.

    Update lib/sigra/audit.ex moduledoc (around line 13 per RESEARCH.md §2.1 "Header rule for the planner") to document the extended metadata shape. Find the existing section listing [:sigra, :audit, :log] metadata keys and append :id and :occurred_at to it with a one-line note "Added in v0.4.0 for cross-system idempotency (Pitfall 4)." Do NOT introduce a new top-level moduledoc section.

    Both fields are safe to read raw off event (per RESEARCH.md §2.1 backwards-compat audit): event.id from @primary_key {:id, :binary_id, autogenerate: true}; event.occurred_at from validate_required([:action, :outcome, :occurred_at]) + DateTime.utc_now() default. Do NOT introduce defensive `event.id || raise` — schema guarantees presence.
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors; MIX_ENV=test mix test test/sigra/audit_telemetry_test.exs test/sigra/audit/</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'id: event\.id' lib/sigra/audit.ex` returns >= 1 (D-31).
    - Source: `grep -c 'occurred_at: event\.occurred_at' lib/sigra/audit.ex` returns >= 1 (D-31).
    - Source: `grep -c 'action: event\.action' lib/sigra/audit.ex` returns >= 1 (existing key preserved).
    - Source: `grep -c 'actor_id: event\.actor_id' lib/sigra/audit.ex` returns >= 1 (existing key preserved).
    - Source: `grep -c 'outcome: event\.outcome' lib/sigra/audit.ex` returns >= 1 (existing key preserved).
    - Behavior: `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
    - Test: `MIX_ENV=test mix test test/sigra/audit_telemetry_test.exs` exits 0.
    - Backwards-compat: `MIX_ENV=test mix test test/sigra/audit/` exits 0 (no existing audit test breaks on the additive metadata).
  </acceptance_criteria>
  <done>emit_telemetry/1 emits all 5 metadata keys; new test green; all existing audit tests green; mix compile clean with --warnings-as-errors.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Source assertions
grep -c 'id: event\.id' lib/sigra/audit.ex                         # >= 1 (D-31)
grep -c 'occurred_at: event\.occurred_at' lib/sigra/audit.ex       # >= 1 (D-31)
grep -c 'action: event\.action' lib/sigra/audit.ex                 # >= 1 (preserved)
grep -c 'actor_id: event\.actor_id' lib/sigra/audit.ex             # >= 1 (preserved)
grep -c 'outcome: event\.outcome' lib/sigra/audit.ex               # >= 1 (preserved)

# Behavior assertions
MIX_ENV=test mix compile --warnings-as-errors                       # exit 0
MIX_ENV=test mix test test/sigra/audit_telemetry_test.exs          # exit 0
MIX_ENV=test mix test test/sigra/audit/                             # exit 0 (no regressions)
```
</verification>

<must_haves>
truths:
  - "[:sigra, :audit, :log] metadata carries :id (UUID) and :occurred_at (DateTime) (D-31)"
  - "Existing keys :action, :actor_id, :outcome are preserved with identical values (D-31 additive only)"
  - "Sigra.Telemetry.attach_default_logger continues to function (backwards-compat audit, RESEARCH.md §2.1)"
  - "Plan 04 Threadline impl can read metadata.id and metadata.occurred_at directly (Pitfall 4 unblocked)"
artifacts:
  - path: "lib/sigra/audit.ex"
    provides: "emit_telemetry/1 with 5-key metadata"
    contains: "id: event.id"
  - path: "test/sigra/audit_telemetry_test.exs"
    provides: "Metadata contract test"
key_links:
  - from: "lib/sigra/audit.ex"
    to: ":telemetry.execute([:sigra, :audit, :log], ...)"
    via: "metadata map"
    pattern: "id: event\\.id"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra.Audit commit path → :telemetry handlers | Metadata flows to any attached handler. Including audit row UUID is appropriate; including actor email/IP would not be. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-04 | Information disclosure | emit_telemetry/1 metadata | mitigate | Only :id (UUID), :occurred_at, :action, :actor_id (already present), :outcome are emitted. NO PII (no email, no IP, no metadata blob). Acceptance criteria grep-asserts exactly 5 keys. |
| T-131-05 | Tampering | Backwards-compat with existing subscribers | mitigate | Edit is additive only; T-131-04 grep checks the 3 existing keys remain present. Sigra.Telemetry.attach_default_logger (lib/sigra/telemetry.ex:341-348) pattern-matches on subset — superset breaks nothing (RESEARCH.md §2.1). |
| T-131-06 | Denial of service | event.id / event.occurred_at presence | accept | Schema validates :occurred_at as required (lib/sigra/audit/changeset.ex:71); PK is autogenerated. Reading event.id directly cannot raise. |
| T-131-SC | Tampering | mix.exs deps | n/a | Plan 02 adds no new package-manager installs. |
</threat_model>

<success_criteria>
- emit_telemetry/1 metadata map contains exactly 5 keys: :action, :actor_id, :outcome, :id, :occurred_at.
- All existing test/sigra/audit/ tests pass unchanged.
- The new test/sigra/audit_telemetry_test.exs passes.
- mix compile --warnings-as-errors exits 0.
- No new dependencies introduced.
- No file overlap with Plan 01 (Wave 1 parallel-safe).
</success_criteria>

<output>
Create `.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-02-SUMMARY.md` when done. Include: line-range of edited emit_telemetry/1, before/after metadata map snippets, test run output for audit_telemetry_test.exs + test/sigra/audit/ suite, and confirmation that no existing test required modification.
</output>
