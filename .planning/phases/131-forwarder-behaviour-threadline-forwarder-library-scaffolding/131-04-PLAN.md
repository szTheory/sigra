---
phase: 131
plan: 04
type: execute
wave: 3
depends_on:
  - 131-01
  - 131-02
  - 131-03
files_modified:
  - lib/sigra/audit/forwarders/threadline.ex
  - lib/sigra/workers/audit_forward.ex
  - test/sigra/audit/forwarders/threadline_test.exs
  - test/sigra/workers/audit_forward_test.exs
autonomous: true
requirements:
  - TL-01
  - TL-03
  - TL-05
user_setup: []

must_haves:
  truths:
    - "Sigra.Audit.Forwarders.Threadline entire defmodule wrapped in `if Code.ensure_loaded?(Threadline) do ... end` (D-18, TL-04 dep-off safety)"
    - "Threadline impl `@behaviour Sigra.Audit.Forwarder`; implements ONLY `attach/1` + an internal `handle_event/4` (D-01, D-33 — handle_event/4 is NOT a behaviour callback)"
    - "Threadline handle_event/4 wraps body in `try / rescue / catch :exit / catch :throw` — catches ALL kinds (D-19, D-20)"
    - "Threadline handle_event/4 NEVER raises to :telemetry; every code path returns `:ok` (D-20 — auto-detach landmine)"
    - "On caught failure, Threadline impl emits [:sigra, :audit, :forward, :error] with metadata.kind ∈ {:error, :exit, :throw} (D-29)"
    - "On success, Threadline impl emits [:sigra, :audit, :forward, :ok] with measurements.duration_ms and metadata.forwarder == :threadline (D-28, D-30)"
    - "Audit row UUID + occurred_at ride to Threadline via :correlation_id (RESEARCH.md §4 path 1, §7.2 — recommended)"
    - "Forwarder name in metadata is atom :threadline (D-30 — NOT a module name)"
    - "Sigra.Workers.AuditForward entire defmodule wrapped in `if Code.ensure_loaded?(Oban.Worker) do ... end` (D-18)"
    - "Worker uses `use Oban.Worker, queue: :sigra_audit_forward, max_attempts: 5` (D-14)"
    - "Worker backoff/1 mirrors lib/sigra/workers/email_delivery.ex:73-76 BYTE-FOR-BYTE (D-15)"
    - "Worker cancel taxonomy: {:cancel, :audit_event_not_found}, {:cancel, :unknown_forwarder}, {:cancel, :schema_mismatch}, {:error, reason} for retryable (D-16)"
    - "Worker job args: %{\"forwarder\" => module_string, \"audit_event_id\" => uuid, \"occurred_at\" => iso8601} — thin reference, never full payload (D-13)"
    - "Worker perform/1 NEVER raises; non-:ok exits fire [:sigra, :audit, :forward, :error] (D-17, Pitfall 2)"
    - "Threadline impl + worker pair satisfies Success Criterion #1 (committed audit → Threadline row + :ok telemetry) and #3 (forced failure → :error telemetry + audit row uncommitted)"
  artifacts:
    - path: "lib/sigra/audit/forwarders/threadline.ex"
      provides: "Threadline telemetry-tap impl"
      contains: "if Code.ensure_loaded?(Threadline) do"
    - path: "lib/sigra/workers/audit_forward.ex"
      provides: "Optional Oban worker"
      contains: "if Code.ensure_loaded?(Oban.Worker) do"
    - path: "test/sigra/audit/forwarders/threadline_test.exs"
      provides: "Happy path + failure isolation + idempotency"
    - path: "test/sigra/workers/audit_forward_test.exs"
      provides: "Worker shape + cancel taxonomy + backoff curve"
  key_links:
    - from: "lib/sigra/audit/forwarders/threadline.ex handle_event/4"
      to: "Threadline.record_action/2"
      via: ":correlation_id carrying metadata.id (UUID)"
      pattern: "correlation_id:"
    - from: "lib/sigra/audit/forwarders/threadline.ex handle_event/4"
      to: "Sigra.Audit.Forwarders.dispatch/3"
      via: "shared :auto/:async/:sync routing"
      pattern: "Sigra\\.Audit\\.Forwarders\\.dispatch"
    - from: "lib/sigra/workers/audit_forward.ex"
      to: "repo.get(audit_schema, audit_event_id)"
      via: "reload audit row from thin job args (D-13)"
      pattern: "repo\\.get\\(audit_schema\\|repo\\.get!\\(audit_schema"
---

<objective>
Ship the actual Threadline forwarder impl (`Sigra.Audit.Forwarders.Threadline`) and the optional Oban worker (`Sigra.Workers.AuditForward`) — the two pieces of NEW library code that make Success Criterion #1 and #3 pass. The Threadline impl is a telemetry-tap that catches ALL failure kinds and never raises to :telemetry (the auto-detach landmine, D-20). The worker is the `:async` execution path Plan 03's dispatcher enqueues to.

Purpose: TL-01 (Threadline forwards to Threadline) + TL-03 (optional Oban worker) + TL-05 (forward.ok / forward.error events fire). Plan 04 is the heart of Phase 131.

Output: 2 new lib files + 2 new test files. Both lib files are entirely wrapped in `Code.ensure_loaded?` guards — when their respective optional deps are absent, neither module exists, and Plan 05's boot wiring falls through to Noop (TL-04 dep-off safety).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md

<interfaces>
<!-- Behaviour from Plan 01: -->
defmodule Sigra.Audit.Forwarder do
  @callback attach(opts :: keyword()) :: :ok | {:error, term()}
end

<!-- Dispatcher from Plan 03: -->
Sigra.Audit.Forwarders.dispatch(forwarder_module, metadata, opts) :: :ok | {:error, term}

<!-- Extended telemetry metadata from Plan 02: -->
[:sigra, :audit, :log] metadata = %{
  action: string,
  actor_id: string | nil,
  outcome: atom,
  id: uuid_binary,           # NEW in Plan 02
  occurred_at: %DateTime{}   # NEW in Plan 02
}

<!-- Threadline 0.5.0 API (verified RESEARCH.md §4): -->
Threadline.record_action(name, opts)
  :: {:ok, %Threadline.AuditAction{}} | {:error, %Ecto.Changeset{}} | {:error, :missing_actor} | {:error, :invalid_actor_ref} | {:error, :missing_repo}
- name :: String.t() | atom
- opts MUST include: :actor or :actor_ref (Threadline.ActorRef), :repo
- opts MAY include: :status, :verb, :category, :reason, :comment, :correlation_id, :request_id, :job_id
- NO native idempotency-key option — RESEARCH.md §4 recommends sending audit UUID via :correlation_id (path 1)

<!-- New Phase 131 telemetry events (D-28, D-29, D-30): -->
[:sigra, :audit, :forward, :ok]
  measurements: %{count: 1, duration_ms: integer}
  metadata: %{forwarder: :threadline, audit_event_id: uuid, action: string, dispatch: :sync | :async}

[:sigra, :audit, :forward, :error]
  measurements: %{count: 1}
  metadata: %{forwarder: atom, audit_event_id: uuid | nil, action: string | nil, reason: term, kind: :error | :exit | :throw, attempt: pos_integer | nil}

<!-- Composite analogs (PATTERNS.md §3 + §5): -->
- Outer Code.ensure_loaded? wrap: lib/sigra/workers/audit_cleanup.ex:1 (RESEARCH.md §2.5)
- try/rescue voice: lib/sigra/rate_limiters/hammer.ex:27-40 (PATTERNS.md §3 verbatim) — extended to catch :exit and :throw per D-19
- Worker shape: lib/sigra/workers/email_delivery.ex:32-103 (RESEARCH.md §2.3 backoff/1; cancel taxonomy at lines 52, 64-65, 99)
- backoff/1 verbatim: trunc(:math.pow(attempt, 4) + 15 + :rand.uniform(10) * attempt)

<!-- Job args shape (D-13 — thin reference): -->
%{"forwarder" => "Elixir.Sigra.Audit.Forwarders.Threadline", "audit_event_id" => "<uuid>", "occurred_at" => "<iso8601>"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing Threadline + Worker tests (with the auto-detach landmine test in red ink)</name>
  <files>test/sigra/audit/forwarders/threadline_test.exs, test/sigra/workers/audit_forward_test.exs</files>
  <read_first>
    - test/sigra/rate_limiters/hammer_test.exs (lines 1-30 — impl-with-stub-dep idiom; MockHammer pattern is the literal analog per PATTERNS.md Test File Analogs)
    - test/sigra/workers/audit_cleanup_test.exs (lines 1-40 — Wave 0 scaffold + StubRepo + cancel-taxonomy assertion style per PATTERNS.md Test File Analogs)
    - lib/sigra/rate_limiters/hammer.ex (lines 27-40 — try/rescue voice; PATTERNS.md §3 verbatim)
    - lib/sigra/workers/email_delivery.ex (lines 32-103 — full worker shape per RESEARCH.md §2.3)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-13..D-21, D-28..D-30)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.3 worker shape, §2.5 wrap, §4 Threadline API, §8 Pitfall 2 + handler auto-detach)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md (§3 Threadline impl composed pattern, §5 worker composed pattern, §"try/rescue/catch handler robustness")
  </read_first>
  <behavior>
    threadline_test.exs (SC-1, SC-3, TL-01, TL-05):
    - Test 1 (D-19 happy path + :correlation_id idempotency): inject a MockThreadline (Mox or hand-stub) that captures the :correlation_id and other opts passed to record_action/2. Build an audit_event with a known UUID, attach the Threadline impl with dispatch: :sync, fire :telemetry.execute([:sigra, :audit, :log], %{count: 1}, %{id: uuid, occurred_at: dt, action: "auth.login.success", actor_id: "u1", outcome: :success}). Assert MockThreadline received correlation_id == uuid and that [:sigra, :audit, :forward, :ok] fired with metadata.forwarder == :threadline (D-30) and measurements.duration_ms is_integer.

    - Test 2 (D-19, D-20 — AUTO-DETACH LANDMINE — RESEARCH.md §8 "red ink"): make MockThreadline raise RuntimeError on the FIRST event. Assert [:sigra, :audit, :forward, :error] fired with metadata.kind == :error. Fire a SECOND telemetry event; assert the handler is STILL ATTACHED (the impl returned :ok to :telemetry, not raised) — verify by asserting MockThreadline receives the second call OR by asserting :telemetry.list_handlers([:sigra, :audit, :log]) still contains the Threadline handler id after the first failure.

    - Test 3 (D-19 — catch :exit): MockThreadline calls `exit(:boom)`. Assert [:sigra, :audit, :forward, :error] fired with metadata.kind == :exit.

    - Test 4 (D-19 — catch :throw): MockThreadline calls `throw(:nope)`. Assert [:sigra, :audit, :forward, :error] fired with metadata.kind == :throw.

    - Test 5 (D-21, SC-3 — Pitfall 2 boundary doctrine): wire a transactional audit insert in the SAME test process; force MockThreadline to fail. After the test, assert the audit_events Postgres row count is unchanged from before the failure (== 1, not 0) — the forwarder failure did NOT roll back the originating Repo.transaction. Use the live Postgres at localhost:5432 (CLAUDE.md mandates this).

    - Test 6 (D-30): metadata.forwarder is an ATOM (:threadline), not a module name — assert via `assert metadata.forwarder == :threadline` AND `refute metadata.forwarder == Sigra.Audit.Forwarders.Threadline`.

    audit_forward_test.exs (TL-03):
    - Test 1 (D-14): `use Oban.Worker, queue: :sigra_audit_forward, max_attempts: 5` — assert via worker module attributes (Sigra.Workers.AuditForward.__opts__() if using Oban.Worker, OR grep the source — both acceptable).

    - Test 2 (D-15 byte-for-byte): for attempt N ∈ {1, 2, 3}, AuditForward.backoff(%Oban.Job{attempt: N}) returns the same value as EmailDelivery.backoff(%Oban.Job{attempt: N}). (Note: backoff uses :rand.uniform so we can't compare exact ints across modules — instead assert that both functions return a positive integer within the same order of magnitude AND that the source code line for the backoff body is identical to email_delivery.ex:73-76. Use `File.read!/1` + `String.contains?/2` to assert source identity.)

    - Test 3 (D-16 cancel taxonomy): stub a repo where `get(_audit_schema, "missing-uuid")` returns nil. Call `AuditForward.perform(%Oban.Job{args: %{"forwarder" => "Elixir.Sigra.Audit.Forwarders.Threadline", "audit_event_id" => "missing-uuid", "occurred_at" => "2026-05-27T12:00:00Z"}})`. Assert returns `{:cancel, :audit_event_not_found}` (D-16).

    - Test 4 (D-16 unknown_forwarder): pass `"forwarder" => "Elixir.DoesNotExist"`. Assert returns `{:cancel, :unknown_forwarder}`.

    - Test 5 (D-17, Pitfall 2): when the forwarder module raises, AuditForward.perform returns `{:error, reason}` and fires [:sigra, :audit, :forward, :error]. The worker MUST NOT itself raise (Telemetry.span catches; perform/1 returns a tagged tuple).

    - Test 6 (D-13 thin reference): assert AuditForward.perform's args shape — only the three keys "forwarder", "audit_event_id", "occurred_at" are read from args. Full payload (action, actor_id, outcome) comes from the reloaded audit row, NOT from args.
  </behavior>
  <action>
    Create test/sigra/audit/forwarders/threadline_test.exs modeled on test/sigra/rate_limiters/hammer_test.exs:1-30. Use `use Sigra.DataCase, async: false` (live Postgres + global telemetry handlers require sequential execution). `import Mox`; `setup :verify_on_exit!`. Define a MockThreadline module at the top of the test file with `@behaviour ...` — OR use Mox.defmock(MockThreadline, for: SomeProtocol) if Threadline ships a behaviour (verify with `iex -S mix` → `Threadline.module_info(:attributes)`). If no behaviour, hand-stub MockThreadline with an Agent or :ets pattern that captures the calls and lets tests inject failures.

    Tests 1-6 as enumerated in <behavior>. For Test 2 (auto-detach landmine), the assertion sequence is critical: (a) attach handler, (b) fire event 1 (handler raises mid-body), (c) immediately fire event 2 BEFORE any sleep, (d) assert handler list still contains the id (`:telemetry.list_handlers([:sigra, :audit, :log]) |> Enum.any?(fn h -> h.id == handler_id end)`), (e) assert MockThreadline received both calls. This is the RESEARCH.md §8 "red ink" test.

    Create test/sigra/workers/audit_forward_test.exs modeled on test/sigra/workers/audit_cleanup_test.exs:1-40. Use `use ExUnit.Case, async: true` for stub-based tests, switch to `use Sigra.DataCase, async: false` only if any test touches the live DB. Define a StubRepo module per the audit_cleanup_test analog. Tests 1-6 as enumerated.

    For Test 2 (backoff byte-for-byte): use `File.read!("lib/sigra/workers/email_delivery.ex")` and `File.read!("lib/sigra/workers/audit_forward.ex")`, extract the `def backoff(%Oban.Job{attempt:` lines, and assert the function body strings match (modulo the function header which can differ in whitespace). This catches D-15 regressions cheaply.

    Both files MUST initially fail (UndefinedFunctionError or compile error) because the lib modules don't exist yet. Wave 0 RED.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/audit/forwarders/threadline_test.exs test/sigra/workers/audit_forward_test.exs 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source: test/sigra/audit/forwarders/threadline_test.exs exists with >= 6 test cases.
    - Source: `grep -c 'list_handlers\|telemetry.list_handlers' test/sigra/audit/forwarders/threadline_test.exs` returns >= 1 (Test 2 auto-detach landmine).
    - Source: `grep -c 'kind: :error\|kind: :exit\|kind: :throw' test/sigra/audit/forwarders/threadline_test.exs` returns >= 3 (D-19 — all three kinds tested).
    - Source: `grep -c ':telemetry\|telemetry.execute' test/sigra/audit/forwarders/threadline_test.exs` returns >= 1.
    - Source: `grep -c 'metadata.forwarder == :threadline\|forwarder: :threadline' test/sigra/audit/forwarders/threadline_test.exs` returns >= 1 (D-30).
    - Source: test/sigra/workers/audit_forward_test.exs exists with >= 5 test cases.
    - Source: `grep -c '{:cancel, :audit_event_not_found}\|{:cancel, :unknown_forwarder}' test/sigra/workers/audit_forward_test.exs` returns >= 2 (D-16).
    - Source: `grep -c 'queue: :sigra_audit_forward\|max_attempts: 5' test/sigra/workers/audit_forward_test.exs` returns >= 1 (D-14).
    - Behavior (pre-impl): both test files exit non-zero (RED — modules don't exist).
  </acceptance_criteria>
  <done>Both test files committed and RED.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sigra.Audit.Forwarders.Threadline (the impl)</name>
  <files>lib/sigra/audit/forwarders/threadline.ex</files>
  <read_first>
    - lib/sigra/audit/forwarder.ex (Plan 01 behaviour — your contract)
    - lib/sigra/audit/forwarders.ex (Plan 03 dispatcher — what handle_event/4 delegates to)
    - lib/sigra/audit/forwarders/noop.ex (Plan 01 Noop — module-shape sibling)
    - lib/sigra/rate_limiters/hammer.ex (lines 1-47 — outer impl shape; PATTERNS.md §3 verbatim try/rescue voice at lines 27-40)
    - lib/sigra/workers/audit_cleanup.ex (line 1 — outer `if Code.ensure_loaded?(Oban.Worker)` wrap; RESEARCH.md §2.5)
    - lib/sigra/telemetry.ex (lines 341-348 — attach_default_logger handler-id idiom per D-03)
    - lib/sigra/audit.ex (the extended emit_telemetry/1 from Plan 02 — confirms metadata keys :id, :occurred_at, :action, :actor_id, :outcome)
    - test/sigra/audit/forwarders/threadline_test.exs (Task 1 — your acceptance contract)
    - **deps/threadline/lib/threadline.ex** — VERBATIM Threadline.record_action/2 spec + @doc + return shape. Plan 03 Task 3 runs `mix deps.get` before Plan 04 starts, so this file is present. Read the actual `@spec record_action(name :: ..., opts :: keyword()) :: ...` line and the function head to confirm: (a) the parameter order (name first, opts second), (b) the documented opts keys (:actor / :actor_ref, :repo, :status, :verb, :category, :reason, :comment, :correlation_id, :request_id, :job_id), and (c) the full return-tuple taxonomy (`{:ok, %Threadline.AuditAction{}}`, `{:error, %Ecto.Changeset{}}`, `{:error, :missing_actor}`, `{:error, :invalid_actor_ref}`, `{:error, :missing_repo}`). If the file diverges from RESEARCH.md §4 (e.g. Threadline 0.5.x patch released a new error variant), follow the actual source — and surface the drift in the Plan 04 SUMMARY so RESEARCH.md gets updated. WARNING 5 fix — replaces "verify Threadline status taxonomy via hexdocs" write-time scramble.
    - **deps/threadline/lib/threadline/actor_ref.ex** — VERBATIM Threadline.ActorRef.new/1 spec. Confirm signature (likely `new(id) :: %Threadline.ActorRef{}` or `new(id_or_struct) :: ...`), the input shape (string UUID, integer ID, or `%User{}`-like struct), and any documented error/exception path. Mapping `metadata.actor_id` (string) to ActorRef.new/1 in the Threadline impl handle_event/4 body relies on this contract.
    - **deps/threadline/lib/threadline/audit_action.ex** — if present, the struct definition that record_action returns on `{:ok, _}`. Useful for confirming the `:correlation_id` field exists on the persisted struct (RESEARCH.md §4 idempotency path 1).
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-03, D-10, D-11, D-18, D-19, D-20, D-21, D-28, D-29, D-30, D-32)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.6 mirror, §4 Threadline API, §8 pitfalls — but TREAT deps/threadline/ SOURCE AS GROUND TRUTH if §4 and source disagree)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md (§3 composed pattern, shared try/rescue invariant)
  </read_first>
  <action>
    Create lib/sigra/audit/forwarders/threadline.ex. The ENTIRE defmodule is wrapped in `if Code.ensure_loaded?(Threadline) do ... end` (D-18) — when :threadline is absent from mix.lock, this file compiles to a no-op and the module simply does not exist (TL-04 dep-off safety; PATTERNS.md §3).

    Module name: `Sigra.Audit.Forwarders.Threadline`. Moduledoc: explain it subscribes to [:sigra, :audit, :log] and ships rows to Threadline via Threadline.record_action/2. Cite D-21 boundary doctrine. Document the per-event idempotency strategy: UUID rides as :correlation_id (RESEARCH.md §4 path 1 / §7.2 recommendation). Document the impl-specific :endpoint and :api_key opts validated inside attach/1 via its own NimbleOptions schema (RESEARCH.md §3 — keep cross-impl seam minimal per D-08).

    `@behaviour Sigra.Audit.Forwarder` and `@impl Sigra.Audit.Forwarder def attach(opts)`:
    - Validate Threadline-specific opts via NimbleOptions.validate!/2 with schema covering :id, :dispatch, :audit_schema, :repo, :oban, :endpoint, :api_key (per D-32 + Threadline.record_action requirements).
    - Compute handler_id = {__MODULE__, opts[:id] || :default} per D-03 (mirrors lib/sigra/telemetry.ex:341-348).
    - Call `:telemetry.attach(handler_id, [:sigra, :audit, :log], &__MODULE__.handle_event/4, opts)`.
    - Return :ok (or {:error, term} on attach failure).

    `def handle_event(_event, _measurements, metadata, opts)` (NOT an @impl — D-33 explicit: handle_event/4 is NOT a behaviour callback):

    Wrap the ENTIRE body in `try / rescue kind, reason -> ... catch :exit, reason -> ... catch :throw, value -> ... end`. MUST catch ALL three kinds (D-19). The PATTERNS.md §"try/rescue/catch handler robustness" block shows the exact structure.

    try body:
      - started_at = System.monotonic_time(:millisecond)
      - Build Threadline.record_action/2 args from metadata. Map:
        * name = metadata.action  (e.g. "auth.login.success")
        * :actor or :actor_ref = derive from metadata.actor_id (use Threadline.ActorRef.new(metadata.actor_id) — see `deps/threadline/lib/threadline/actor_ref.ex` for the verbatim signature read in <read_first>; if no actor_id, handle the `{:error, :missing_actor}` return from record_action/2 — see `deps/threadline/lib/threadline.ex` for the exact error taxonomy)
        * :repo = opts[:repo]
        * :correlation_id = metadata.id  (THE idempotency key — RESEARCH.md §4 path 1; D-31 ensures it's present in metadata)
        * :status = derive from metadata.outcome (:success → "ok", :failure → "error", etc. — verify Threadline status taxonomy by grepping `deps/threadline/lib/threadline.ex` for the documented `:status` values in the `@doc` block on record_action/2; this is the ground-truth source, not hexdocs)
      - Delegate to Sigra.Audit.Forwarders.dispatch(__MODULE__, metadata, opts) (D-10/D-11 — shared dispatcher).
        - The :sync path calls Threadline.record_action(name, opts) inline.
        - The :async path enqueues Sigra.Workers.AuditForward via Plan 03's dispatcher.
      - On {:ok, _result}: duration_ms = System.monotonic_time(:millisecond) - started_at; `:telemetry.execute([:sigra, :audit, :forward, :ok], %{count: 1, duration_ms: duration_ms}, %{forwarder: :threadline, audit_event_id: metadata.id, action: metadata.action, dispatch: dispatch_mode})` per D-28, D-30. Return :ok.
      - On {:error, reason}: `:telemetry.execute([:sigra, :audit, :forward, :error], %{count: 1}, %{forwarder: :threadline, audit_event_id: metadata.id, action: metadata.action, reason: reason, kind: :error, attempt: nil})` per D-29. Return :ok.

    rescue branch: emit [:sigra, :audit, :forward, :error] with kind: :error and reason: the caught exception; return :ok.
    catch :exit branch: emit with kind: :exit, reason: the exit reason; return :ok.
    catch :throw branch: emit with kind: :throw, reason: the thrown value; return :ok.

    INVARIANT (D-20): EVERY code path returns :ok to :telemetry. NEVER :stop. NEVER raise. This is the auto-detach landmine — Test 2 in Task 1's threadline_test.exs asserts this.

    Forwarder name in all telemetry metadata: literal atom `:threadline` per D-30 (NOT __MODULE__ — D-30 explicit "atom, not module").
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors; MIX_ENV=test mix test test/sigra/audit/forwarders/threadline_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: lib/sigra/audit/forwarders/threadline.ex exists.
    - Source: `head -3 lib/sigra/audit/forwarders/threadline.ex | grep -c 'if Code.ensure_loaded?(Threadline)'` returns >= 1 (D-18 outer wrap).
    - Source: `tail -3 lib/sigra/audit/forwarders/threadline.ex | grep -c '^end$\|^  end$'` returns >= 1 (closing end of the if-do).
    - Source: `grep -c '@behaviour Sigra.Audit.Forwarder' lib/sigra/audit/forwarders/threadline.ex` returns exactly 1.
    - Source: `grep -c '@impl Sigra.Audit.Forwarder' lib/sigra/audit/forwarders/threadline.ex` returns exactly 1 (only attach/1 is @impl; handle_event/4 is NOT per D-33).
    - Source: `grep -c 'def attach(' lib/sigra/audit/forwarders/threadline.ex` returns >= 1.
    - Source: `grep -c 'def handle_event(' lib/sigra/audit/forwarders/threadline.ex` returns >= 1.
    - Source (D-19 — ALL three kinds): `grep -c 'rescue' lib/sigra/audit/forwarders/threadline.ex` >= 1 AND `grep -c 'catch :exit\|catch :throw\|catch.*:exit\|catch.*:throw' lib/sigra/audit/forwarders/threadline.ex` >= 2 (one for :exit, one for :throw).
    - Source (D-30): `grep -c 'forwarder: :threadline' lib/sigra/audit/forwarders/threadline.ex` returns >= 2 (used in both :ok and :error metadata).
    - Source (D-28): `grep -c '\\[:sigra, :audit, :forward, :ok\\]' lib/sigra/audit/forwarders/threadline.ex` returns >= 1.
    - Source (D-29): `grep -c '\\[:sigra, :audit, :forward, :error\\]' lib/sigra/audit/forwarders/threadline.ex` returns >= 1.
    - Source (idempotency path 1): `grep -c 'correlation_id:' lib/sigra/audit/forwarders/threadline.ex` returns >= 1.
    - Source (D-03 handler id): `grep -c '{__MODULE__,' lib/sigra/audit/forwarders/threadline.ex` returns >= 1.
    - Source (D-10/D-11 shared dispatch): `grep -c 'Sigra.Audit.Forwarders.dispatch\|Forwarders.dispatch' lib/sigra/audit/forwarders/threadline.ex` returns >= 1.
    - Behavior: `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
    - Test: `MIX_ENV=test mix test test/sigra/audit/forwarders/threadline_test.exs` exits 0 (all 6 tests GREEN, including the auto-detach landmine).
  </acceptance_criteria>
  <done>Threadline impl compiles inside the optional-dep wrap; all 6 tests GREEN; auto-detach landmine test (Test 2) is GREEN — handler stays attached after a raised event.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Sigra.Workers.AuditForward Oban worker</name>
  <files>lib/sigra/workers/audit_forward.ex</files>
  <read_first>
    - lib/sigra/workers/email_delivery.ex (entire file — RESEARCH.md §2.3 covers lines 32-103 in detail; this is the literal precedent the worker mirrors)
    - lib/sigra/workers/audit_cleanup.ex (line 1, lines 31-33 — outer Code.ensure_loaded?(Oban.Worker) wrap; RESEARCH.md §2.5)
    - lib/sigra/audit/forwarders/threadline.ex (Task 2 — confirms handle_event/4 signature for the worker's perform call)
    - lib/sigra/audit/forwarders.ex (Plan 03 dispatcher)
    - lib/sigra/audit.ex (the audit_schema lookup pattern — `Application.get_env(otp_app, :sigra_config) |> Keyword.get(:audit) |> Keyword.fetch!(:audit_schema)`)
    - test/sigra/workers/audit_forward_test.exs (Task 1 — acceptance contract)
    - test/sigra/workers/audit_cleanup_test.exs (lines 1-40 — Wave 0 scaffold style)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-13, D-14, D-15, D-16, D-17, D-18, D-29)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.3 worker shape, §2.5 wrap)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md (§5 worker composed pattern)
  </read_first>
  <action>
    Create lib/sigra/workers/audit_forward.ex. The ENTIRE defmodule is wrapped in `if Code.ensure_loaded?(Oban.Worker) do ... end` (D-18) — mirrors lib/sigra/workers/audit_cleanup.ex:1 precedent. When Oban is absent, this file compiles to a no-op.

    Module name: `Sigra.Workers.AuditForward`. Use `use Oban.Worker, queue: :sigra_audit_forward, max_attempts: 5` per D-14 (queue name is new — bumped from email_delivery's :sigra_mailer; max_attempts: 5 is the deliberate bump from EmailDelivery's 3 because audit retries don't user-spam).

    Implement `@impl Oban.Worker def perform(%Oban.Job{args: args, attempt: attempt})`:

    1. Parse thin job args (D-13): `forwarder_string = args["forwarder"]`, `audit_event_id = args["audit_event_id"]`, `occurred_at_iso = args["occurred_at"]`. No other keys.

    2. Resolve forwarder module: try `Module.safe_concat([forwarder_string])` or `String.to_existing_atom/1`. If the module doesn't exist (forwarder removed from app), return `{:cancel, :unknown_forwarder}` (D-16).

    3. Resolve config: read `otp_app = Application.get_env(:sigra, :otp_app)`, then `sigra_config = Application.get_env(otp_app, :sigra_config)`, then extract `audit_opts = Keyword.get(sigra_config, :audit, [])`, then `audit_schema = Keyword.fetch!(audit_opts, :audit_schema)`, `repo = Keyword.fetch!(audit_opts, :repo)`. This is the same Application.get_env cascade as lib/sigra/application.ex:30-38 (D-27 — single config-resolution pattern).

    4. Reload audit row (D-13 — thin reference, full payload from DB): `audit_row = repo.get(audit_schema, audit_event_id)`. If nil, return `{:cancel, :audit_event_not_found}` (D-16).

    5. Build metadata map equivalent to what handle_event/4 receives: `%{id: audit_row.id, action: audit_row.action, actor_id: audit_row.actor_id, outcome: audit_row.outcome, occurred_at: audit_row.occurred_at}` — same shape as Plan 02's extended emit_telemetry/1.

    6. Call into the forwarder's payload mapping. Since the forwarder behaviour (per D-33) has only attach/1, the worker calls the impl's `handle_event/4` directly with synthetic args: `forwarder_module.handle_event([:sigra, :audit, :log], %{count: 1}, metadata, opts ++ [dispatch: :sync])` — forcing :sync so the worker itself does the inline call (it's already async by being in an Oban job). Alternatively, Sigra.Audit.Forwarders can expose a `perform_sync/3` seam — choose whichever is cleaner. Either way, the perform function MUST NOT re-enqueue.

    7. Catch schema mismatch: if Threadline.record_action returns `{:error, %Ecto.Changeset{}}` (verbatim error shape — confirm by reading `deps/threadline/lib/threadline.ex` record_action/2 spec read in Task 2), return `{:cancel, :schema_mismatch}` (D-16 — Threadline shipped a breaking schema change, not retryable). Also handle `{:error, :missing_actor}`, `{:error, :invalid_actor_ref}`, `{:error, :missing_repo}` as `{:cancel, :schema_mismatch}` (these are configuration errors, also not retryable). Network/timeout errors fall through to step 8.

    8. For network/timeout/transient errors: return `{:error, reason}` (D-16 — Oban retries with backoff).

    9. perform/1 NEVER raises (D-17). Wrap the whole body in Telemetry.span([:sigra, :audit, :forward], %{audit_event_id: audit_event_id, attempt: attempt}, fn -> ... end) — but the span body returns the tagged tuple; perform's outer try/rescue catches any leaked raise and converts to {:error, _}.

    10. Implement `@impl Oban.Worker def backoff(%Oban.Job{attempt: attempt})` mirroring lib/sigra/workers/email_delivery.ex:73-76 BYTE-FOR-BYTE per D-15:
        ```
        trunc(:math.pow(attempt, 4) + 15 + :rand.uniform(10) * attempt)
        ```
        Include the same `# Exponential backoff with jitter` comment.

    Moduledoc: short — "Optional Oban worker for async audit forwarder dispatch. Wrapped in `if Code.ensure_loaded?(Oban.Worker)` per D-18. Receives thin job args (forwarder + audit_event_id + occurred_at); reloads the audit row from configured repo + audit_schema (D-13). Cancel taxonomy: :audit_event_not_found / :unknown_forwarder / :schema_mismatch are non-retryable; {:error, _} is retryable."
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors; MIX_ENV=test mix test test/sigra/workers/audit_forward_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: lib/sigra/workers/audit_forward.ex exists.
    - Source (D-18): `head -3 lib/sigra/workers/audit_forward.ex | grep -c 'if Code.ensure_loaded?(Oban.Worker)'` returns >= 1.
    - Source (D-14): `grep -c 'queue: :sigra_audit_forward' lib/sigra/workers/audit_forward.ex` returns exactly 1.
    - Source (D-14): `grep -c 'max_attempts: 5' lib/sigra/workers/audit_forward.ex` returns exactly 1.
    - Source (D-15 byte-for-byte): `grep -c 'trunc(:math.pow(attempt, 4) + 15 + :rand.uniform(10) \\* attempt)' lib/sigra/workers/audit_forward.ex` returns exactly 1.
    - Source (D-16 cancel taxonomy — all four tuples):
      * `grep -c '{:cancel, :audit_event_not_found}' lib/sigra/workers/audit_forward.ex` >= 1
      * `grep -c '{:cancel, :unknown_forwarder}' lib/sigra/workers/audit_forward.ex` >= 1
      * `grep -c '{:cancel, :schema_mismatch}' lib/sigra/workers/audit_forward.ex` >= 1
    - Source (D-13 thin reference): `grep -c '"forwarder"\|"audit_event_id"\|"occurred_at"' lib/sigra/workers/audit_forward.ex` returns >= 3 (all three keys read from args; no others).
    - Source (D-13 reload): `grep -c 'repo\.get(\|repo\.get!(' lib/sigra/workers/audit_forward.ex` returns >= 1.
    - Source (D-27 config cascade): `grep -c 'Application.get_env(:sigra, :otp_app)\|Application.get_env(otp_app' lib/sigra/workers/audit_forward.ex` returns >= 1.
    - Source (D-17 — no raise): `grep -v '^#' lib/sigra/workers/audit_forward.ex | grep -c 'raise ' | head -1` returns 0 (the worker MUST NOT contain a bare `raise` call in production paths; rescue branches converting to {:error, _} are fine and don't match this grep).
    - Behavior: `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
    - Test: `MIX_ENV=test mix test test/sigra/workers/audit_forward_test.exs` exits 0.
    - Test (full audit slice): `MIX_ENV=test mix test test/sigra/audit/ test/sigra/workers/audit_forward_test.exs` exits 0.
  </acceptance_criteria>
  <done>AuditForward worker compiles inside Code.ensure_loaded? wrap; cancel taxonomy covers all four cases per D-16; backoff matches EmailDelivery byte-for-byte per D-15; all worker tests GREEN.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
# Source assertions — Threadline impl
grep -c 'if Code.ensure_loaded?(Threadline)' lib/sigra/audit/forwarders/threadline.ex             # >= 1 (D-18)
grep -c '@behaviour Sigra.Audit.Forwarder' lib/sigra/audit/forwarders/threadline.ex               # == 1
grep -c '@impl Sigra.Audit.Forwarder' lib/sigra/audit/forwarders/threadline.ex                    # == 1 (D-33 — only attach/1)
grep -c 'rescue' lib/sigra/audit/forwarders/threadline.ex                                          # >= 1 (D-19)
grep -c 'catch :exit\|catch.*:exit' lib/sigra/audit/forwarders/threadline.ex                       # >= 1 (D-19)
grep -c 'catch :throw\|catch.*:throw' lib/sigra/audit/forwarders/threadline.ex                     # >= 1 (D-19)
grep -c 'forwarder: :threadline' lib/sigra/audit/forwarders/threadline.ex                          # >= 2 (D-30)
grep -c '\\[:sigra, :audit, :forward, :ok\\]' lib/sigra/audit/forwarders/threadline.ex            # >= 1 (D-28)
grep -c '\\[:sigra, :audit, :forward, :error\\]' lib/sigra/audit/forwarders/threadline.ex         # >= 1 (D-29)
grep -c 'correlation_id:' lib/sigra/audit/forwarders/threadline.ex                                # >= 1 (RESEARCH.md §4 path 1)

# Source assertions — Worker
grep -c 'if Code.ensure_loaded?(Oban.Worker)' lib/sigra/workers/audit_forward.ex                  # >= 1 (D-18)
grep -c 'queue: :sigra_audit_forward' lib/sigra/workers/audit_forward.ex                          # == 1 (D-14)
grep -c 'max_attempts: 5' lib/sigra/workers/audit_forward.ex                                       # == 1 (D-14)
grep -c 'trunc(:math.pow(attempt, 4) + 15 + :rand.uniform(10) \\* attempt)' lib/sigra/workers/audit_forward.ex  # == 1 (D-15)
grep -c '{:cancel, :audit_event_not_found}' lib/sigra/workers/audit_forward.ex                    # >= 1 (D-16)
grep -c '{:cancel, :unknown_forwarder}' lib/sigra/workers/audit_forward.ex                        # >= 1 (D-16)
grep -c '{:cancel, :schema_mismatch}' lib/sigra/workers/audit_forward.ex                          # >= 1 (D-16)

# Behavior assertions
MIX_ENV=test mix compile --warnings-as-errors                                                       # exit 0
MIX_ENV=test mix test test/sigra/audit/forwarders/threadline_test.exs                              # exit 0
MIX_ENV=test mix test test/sigra/workers/audit_forward_test.exs                                    # exit 0
MIX_ENV=test mix test test/sigra/audit/                                                            # exit 0 (no regressions)
```
</verification>

<must_haves>
truths:
  - "Threadline impl wrapped in if Code.ensure_loaded?(Threadline) (D-18)"
  - "Threadline impl handle_event/4 catches :error, :exit, :throw (D-19 — all three kinds)"
  - "Handler returns :ok to :telemetry on ALL paths — NEVER raises (D-20 — auto-detach landmine)"
  - "Audit UUID rides to Threadline via :correlation_id (RESEARCH.md §4 path 1)"
  - "Metadata.forwarder is atom :threadline (D-30 — NOT module)"
  - "Worker wrapped in if Code.ensure_loaded?(Oban.Worker) (D-18)"
  - "Worker uses queue :sigra_audit_forward + max_attempts: 5 (D-14)"
  - "Worker backoff/1 mirrors EmailDelivery byte-for-byte (D-15)"
  - "Worker cancel taxonomy: audit_event_not_found / unknown_forwarder / schema_mismatch / {:error, _} (D-16)"
  - "Worker job args are thin reference only (D-13 — three keys)"
  - "SC-3 (Pitfall 2): forwarder failure NEVER rolls back the originating audit transaction"
artifacts:
  - path: "lib/sigra/audit/forwarders/threadline.ex"
    provides: "Threadline telemetry-tap impl"
  - path: "lib/sigra/workers/audit_forward.ex"
    provides: "Optional Oban worker"
key_links:
  - from: "Threadline.handle_event/4"
    to: "Threadline.record_action/2"
    via: "correlation_id"
    pattern: "correlation_id:"
  - from: "Threadline.handle_event/4"
    to: "Sigra.Audit.Forwarders.dispatch/3"
    via: "shared :sync/:async routing"
    pattern: "Sigra\\.Audit\\.Forwarders\\.dispatch"
  - from: "AuditForward.perform/1"
    to: "repo.get(audit_schema, audit_event_id)"
    via: "thin-args reload"
    pattern: "repo\\.get"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| :telemetry → Sigra.Audit.Forwarders.Threadline.handle_event/4 | If handler raises, :telemetry auto-detaches it permanently (BEAM lifetime). This is the worst failure mode in the phase. |
| Threadline.record_action/2 → external Threadline DB | Untrusted external store may fail / time out / return changeset errors; forwarder MUST NOT propagate failure back into Sigra audit transaction. |
| Oban.Worker.perform/1 → forwarder.handle_event/4 | Worker is already in an async context; must catch all forwarder failures and return tagged tuples, never re-raise to Oban (which would mark the job failed but is acceptable; raises are not — perform/1 explicit invariant D-17). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-11 | Denial of service | Handler raises → :telemetry auto-detach (CRITICAL — D-20) | mitigate | handle_event/4 wraps body in try/rescue/catch with ALL three kinds (:error, :exit, :throw); every path returns :ok. Threadline_test.exs Test 2 asserts the handler stays attached on a SECOND event after the first raised — this is the RESEARCH.md §8 "red ink" test. Grep acceptance criterion confirms `catch :exit` AND `catch :throw` both present. |
| T-131-12 | Tampering | Forwarder failure rolls back originating Sigra audit transaction (Pitfall 2 — D-21) | mitigate | Telemetry fires only on {:ok, _} commit branch (verified RESEARCH.md §2.1 — audit.ex emit_telemetry runs after Multi commit). handle_event/4 is in a SEPARATE process from the auth Repo.transaction. Threadline_test.exs Test 5 asserts audit_events Postgres row count is unchanged after forced Threadline failure. |
| T-131-13 | Information disclosure | actor email / IP leaked in [:sigra, :audit, :forward, :*] metadata | mitigate | Metadata only carries: :forwarder (atom), :audit_event_id (UUID), :action (string), :dispatch (atom), :reason (term — may include exception message but Threadline impl uses safe inspect), :kind, :attempt. NO actor email, NO actor IP, NO request payload. Grep acceptance criterion enumerates exact metadata keys. |
| T-131-14 | Information disclosure | api_key in Logger output | mitigate | The Threadline impl's attach/1 receives :api_key in opts. The impl MUST NOT log opts via Logger.warning/Logger.info (verify via `grep -v '^#' lib/sigra/audit/forwarders/threadline.ex | grep -c 'inspect(opts)\|Logger.*opts'` returns 0). Plan 05's boot wiring also redacts (its own threat model). |
| T-131-15 | Repudiation | Worker fails silently — operator has no signal | mitigate | TL-05: separate :ok and :error telemetry events. D-29 :error metadata includes :reason and :kind so operators trace failures. Oban dashboard shows {:error, _} retries; {:cancel, _} dispositions visible in Oban.Job state. |
| T-131-16 | Tampering | thin args spoofing — attacker manipulates "audit_event_id" to forward wrong row | accept | Oban jobs are inserted by Sigra (server-side) from the dispatcher's :async path. The args are not user-controllable. The worker uses repo.get/2 (parameterized query) — no SQL injection risk. If the row is gone (deleted by retention cleanup), {:cancel, :audit_event_not_found} per D-16. |
| T-131-17 | Denial of service | Threadline returns slow / hung HTTP | mitigate | :async path puts the call in Oban with max_attempts: 5 + exponential backoff — failures don't block auth. :sync path is the host's choice; recipe (Phase 132) documents the tradeoff in "Failure Modes" section. |
| T-131-SC | Tampering | No new package installs in Plan 04 | n/a | mix.exs Threadline pin landed in Plan 03 with the legitimacy gate. Plan 04 is pure code. |
</threat_model>

<success_criteria>
- All 6 threadline_test.exs tests GREEN — including Test 2 (auto-detach landmine — handler stays attached after raised event) and Test 5 (Pitfall 2 — audit row uncommitted by forwarder failure).
- All 6 audit_forward_test.exs tests GREEN — cancel taxonomy covers all 4 cases; backoff matches EmailDelivery byte-for-byte.
- Threadline impl source contains `catch :exit` AND `catch :throw` AND `rescue` (D-19 all three kinds).
- Threadline impl source contains `forwarder: :threadline` >= 2 times (D-30 — atom, used in both :ok and :error metadata).
- Threadline impl source contains `correlation_id:` (RESEARCH.md §4 path 1 idempotency).
- Worker source contains all four `{:cancel, _}` tuples (D-16).
- Worker source contains `max_attempts: 5` and `queue: :sigra_audit_forward` (D-14).
- Worker source contains the exact backoff body `trunc(:math.pow(attempt, 4) + 15 + :rand.uniform(10) * attempt)` (D-15).
- mix compile --warnings-as-errors exits 0.
- mix test for test/sigra/audit/ and test/sigra/workers/audit_forward_test.exs exits 0.
- Plan 05 (boot wiring) can now reference both modules safely.
</success_criteria>

<output>
Create `.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-04-SUMMARY.md` when done. Include: full file paths for both lib modules, line ranges, the auto-detach landmine test result (Test 2 verbatim output), confirmation of D-15 backoff byte-identity (file-read assertion result), and the exact telemetry metadata key list emitted by handle_event/4.
</output>
