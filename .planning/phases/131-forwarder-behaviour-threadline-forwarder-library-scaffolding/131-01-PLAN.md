---
phase: 131
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/audit/forwarder.ex
  - lib/sigra/audit/forwarders/noop.ex
  - test/sigra/audit/forwarder_test.exs
  - test/sigra/audit/forwarders/noop_test.exs
autonomous: true
requirements:
  - FB-01
  - TL-04
user_setup: []

must_haves:
  truths:
    - "`Sigra.Audit.Forwarder` behaviour defines a single `@callback attach(keyword) :: :ok | {:error, term}` (D-01, D-33)"
    - "Behaviour moduledoc documents the `Mox.defmock(MyForwarder, for: Sigra.Audit.Forwarder)` test path (D-04)"
    - "`Sigra.Audit.Forwarders.Noop.attach/1` returns `:ok` immediately, does NOT call `:telemetry.attach/4`, does NOT call `Logger` (D-22, D-23)"
    - "Noop moduledoc carries the `> #### Warning {: .warning}` ExDoc admonition (D-24)"
    - "A host module implementing `@behaviour Sigra.Audit.Forwarder` with only `attach/1` compiles without warnings (Success Criterion #5, FB-01)"
  artifacts:
    - path: "lib/sigra/audit/forwarder.ex"
      provides: "Forwarder behaviour module with single `attach/1` callback + Mox moduledoc"
      contains: "@callback attach(opts :: keyword()) :: :ok | {:error, term()}"
    - path: "lib/sigra/audit/forwarders/noop.ex"
      provides: "Fail-open Noop fallback impl"
      contains: "@behaviour Sigra.Audit.Forwarder"
    - path: "test/sigra/audit/forwarder_test.exs"
      provides: "Behaviour contract test — verifies a host stub module attaching against the behaviour"
    - path: "test/sigra/audit/forwarders/noop_test.exs"
      provides: "Noop contract test — `attach/1` returns `:ok`, no telemetry side effects"
  key_links:
    - from: "lib/sigra/audit/forwarders/noop.ex"
      to: "lib/sigra/audit/forwarder.ex"
      via: "`@behaviour Sigra.Audit.Forwarder`"
      pattern: "@behaviour Sigra\\.Audit\\.Forwarder"
    - from: "test/sigra/audit/forwarder_test.exs"
      to: "lib/sigra/audit/forwarder.ex"
      via: "stub module + Mox.defmock against the behaviour"
      pattern: "Sigra\\.Audit\\.Forwarder"
---

<objective>
Ship the `Sigra.Audit.Forwarder` behaviour (the contract every audit forwarder — Threadline, future Datadog/Honeycomb/OTel/in-house impls — pins against) and the `Sigra.Audit.Forwarders.Noop` fail-open fallback. Lock the behaviour shape in Wave 1 so Plan 03 (config schema), Plan 04 (Threadline impl), and Plan 05 (boot wiring) can depend on a stable contract.

Purpose: FB-01 (behaviour contract) + TL-04 (Noop fallback). The behaviour is the seam custom forwarders implement; the Noop is what `attach_forwarders/0` falls through to when a configured forwarder's dep is absent.

Output: 2 lib files + 2 test files. No production-code references to either module yet (those wire in Plans 03–05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md
@.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-PATTERNS.md

<interfaces>
<!-- Behaviour contract that Plan 04 (Threadline) and host custom forwarders will implement.
     Hand-extracted from RESEARCH.md §2.6 (Sigra.RateLimiter precedent) and D-01/D-04/D-32/D-33. -->

Phase 131 NEW behaviour module: `lib/sigra/audit/forwarder.ex`

```elixir
defmodule Sigra.Audit.Forwarder do
  @callback attach(opts :: keyword()) :: :ok | {:error, term()}
end
```

Documented (but NOT validated by the behaviour) keys in `opts` per D-32:
- `:id` — atom, default `:default`
- `:dispatch` — `:auto | :async | :sync`, default `:auto`
- `:audit_schema` — module (used by worker on `:async` path)
- `:repo` — module (used by worker)
- `:oban` — module override for tests, default `Oban`
- plus arbitrary impl-specific keys (Threadline: `:endpoint`, `:api_key`)

D-33: behaviour does NOT define `handle_event/4` or any payload-shape callback.

Existing analog (Plan 01 mirrors structure byte-for-byte except for callback name + arity):
- `lib/sigra/rate_limiter.ex:1-26` — single `@callback check_rate/3` + Mox moduledoc
- `lib/sigra/rate_limiters/noop.ex:1-21` — `@behaviour Sigra.RateLimiter` + body returning `{:allow, 1}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing behaviour-contract test + Noop test stubs</name>
  <files>test/sigra/audit/forwarder_test.exs, test/sigra/audit/forwarders/noop_test.exs</files>
  <read_first>
    - test/sigra/workers/behaviour_test.exs (lines 1-29) — analog for behaviour-contract test with `defmodule Stub... do @behaviour ... end` inside the test body (per PATTERNS.md §"Test File Analogs")
    - test/sigra/audit/changeset_test.exs (lines 1-30) — AAA + `use ExUnit.Case, async: true` voice mirror for `noop_test.exs`
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-01, D-04, D-22, D-23, D-24, D-32, D-33)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-VALIDATION.md (Wave 0 list + SC-5 mapping)
  </read_first>
  <behavior>
    - forwarder_test.exs Test 1 (FB-01 / SC-5): a `defmodule StubForwarder do @behaviour Sigra.Audit.Forwarder; def attach(_opts), do: :ok end` compiles without `@impl` warnings — proves the behaviour exposes only `attach/1` and nothing else.
    - forwarder_test.exs Test 2 (FB-01 / SC-5): the behaviour exports exactly one callback (`attach/1`) — assertable via `Sigra.Audit.Forwarder.behaviour_info(:callbacks) == [attach: 1]` (D-01, D-33).
    - forwarder_test.exs Test 3 (D-04): the module's `@moduledoc` contains the literal string `Mox.defmock(MyForwarder, for: Sigra.Audit.Forwarder)` (assertable via `Code.fetch_docs(Sigra.Audit.Forwarder)`).
    - noop_test.exs Test 1 (D-22): `Sigra.Audit.Forwarders.Noop.attach([]) == :ok`.
    - noop_test.exs Test 2 (D-22, D-23): calling `Sigra.Audit.Forwarders.Noop.attach([])` does NOT register a telemetry handler — assert via `:telemetry.list_handlers([:sigra, :audit, :log])` count before vs after is unchanged.
    - noop_test.exs Test 3 (D-23): calling `attach/1` produces no `Logger` output — wrap in `ExUnit.CaptureLog.capture_log/1` and assert the captured string is `""` (warning lives upstream in `Sigra.Application`, not in Noop).
  </behavior>
  <action>
    Create `test/sigra/audit/forwarder_test.exs` modeled on `test/sigra/workers/behaviour_test.exs:1-29` — `use ExUnit.Case, async: true`, define an inline `StubForwarder` module inside the test that has `@behaviour Sigra.Audit.Forwarder` + `def attach(_opts), do: :ok`, and assert (a) compilation succeeded (the test file loaded), (b) `Sigra.Audit.Forwarder.behaviour_info(:callbacks) == [attach: 1]` per D-01/D-33, (c) the moduledoc string from `Code.fetch_docs(Sigra.Audit.Forwarder)` contains `"Mox.defmock"` per D-04.

    Create `test/sigra/audit/forwarders/noop_test.exs` modeled on `test/sigra/audit/changeset_test.exs:1-30` — `use ExUnit.Case, async: true`. Three tests: (1) `assert Sigra.Audit.Forwarders.Noop.attach([]) == :ok`; (2) capture `:telemetry.list_handlers([:sigra, :audit, :log])` length before and after `attach/1` and assert unchanged (D-22, D-23 — Noop does NOT subscribe); (3) wrap `Noop.attach([])` in `ExUnit.CaptureLog.capture_log/1` and assert the result equals `""` (D-23 — Noop does NOT log).

    These tests MUST initially fail with `UndefinedFunctionError`/compile error because Plan 01 Task 2 has not created the lib files yet. That is correct Wave 0 RED state.
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors 2>&amp;1 | grep -E "(forwarder_test|noop_test)" || true; MIX_ENV=test mix test test/sigra/audit/forwarder_test.exs test/sigra/audit/forwarders/noop_test.exs 2>&amp;1 | grep -E "(failures|UndefinedFunctionError|CompileError)"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test/sigra/audit/forwarder_test.exs` exists; `grep -c 'behaviour_info(:callbacks)' test/sigra/audit/forwarder_test.exs` returns `>= 1`.
    - Source: `test/sigra/audit/forwarder_test.exs` defines an inline `StubForwarder` module with `@behaviour Sigra.Audit.Forwarder`; `grep -c '@behaviour Sigra.Audit.Forwarder' test/sigra/audit/forwarder_test.exs` returns `>= 1`.
    - Source: `test/sigra/audit/forwarders/noop_test.exs` exists; `grep -c 'list_handlers' test/sigra/audit/forwarders/noop_test.exs` returns `>= 1` and `grep -c 'capture_log' test/sigra/audit/forwarders/noop_test.exs` returns `>= 1`.
    - Behavior (pre-impl): `MIX_ENV=test mix test test/sigra/audit/forwarder_test.exs test/sigra/audit/forwarders/noop_test.exs` exits non-zero (RED). After Task 2 lands the lib modules these same tests must exit 0 (verified by Task 2's `<automated>`).
  </acceptance_criteria>
  <done>Both test files committed; running them today produces compile/RuntimeError because the lib modules do not exist yet — Wave 0 RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `Sigra.Audit.Forwarder` behaviour + `Sigra.Audit.Forwarders.Noop`</name>
  <files>lib/sigra/audit/forwarder.ex, lib/sigra/audit/forwarders/noop.ex</files>
  <read_first>
    - lib/sigra/rate_limiter.ex (entire file, 26 lines) — EXACT structural mirror per PATTERNS.md §1 (the Phase 131 mirror draft is in 131-RESEARCH.md §2.6 second code block)
    - lib/sigra/rate_limiters/noop.ex (entire file, 21 lines) — EXACT structural mirror per PATTERNS.md §2 (the Phase 131 mirror draft is in 131-RESEARCH.md §2.7 second code block)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-RESEARCH.md (§2.6 — behaviour mirror; §2.7 — Noop mirror)
    - .planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-CONTEXT.md (D-01, D-04, D-21 boundary doctrine, D-22, D-23, D-24, D-32, D-33)
    - test/sigra/audit/forwarder_test.exs (just written in Task 1 — your acceptance contract)
    - test/sigra/audit/forwarders/noop_test.exs (just written in Task 1)
  </read_first>
  <action>
    Create `lib/sigra/audit/forwarder.ex` mirroring `lib/sigra/rate_limiter.ex` line-for-line. Module name `Sigra.Audit.Forwarder`. Moduledoc body (paraphrased — see RESEARCH.md §2.6 second code block for the literal draft): explain that a forwarder subscribes in `attach/1` to `[:sigra, :audit, :log]` and ships committed audit rows to a downstream sink (Threadline, Datadog, Honeycomb, OTel, in-house); cite D-21 boundary doctrine (Sigra audit_events table remains source of truth — forwarders are post-commit projections, never destination swaps); list the in-tree impls (`Sigra.Audit.Forwarders.Threadline` when `:threadline` is loaded, `Sigra.Audit.Forwarders.Noop` fallback); include the `## Mox Usage` H2 with the one-line `Mox.defmock(MyForwarder, for: Sigra.Audit.Forwarder)` example per D-04. Document (in prose, NOT as additional callbacks) the keyword keys per D-32: `:id` (atom, default `:default`), `:dispatch` (`:auto | :async | :sync`, default `:auto`), `:audit_schema`, `:repo`, `:oban` (default `Oban`), plus arbitrary impl-specific keys. Single `@callback attach(opts :: keyword()) :: :ok | {:error, term()}` with `@doc since: "0.4.0"`. NO other callbacks per D-33 (no `handle_event/4`, no `detach/1`).

    Create `lib/sigra/audit/forwarders/noop.ex` mirroring `lib/sigra/rate_limiters/noop.ex` line-for-line. Module name `Sigra.Audit.Forwarders.Noop`. Moduledoc must include the `> #### Warning {: .warning}` ExDoc admonition block per D-24 (mirror the precedent at `rate_limiters/noop.ex:5-9`), adapted to the forwarder domain — "silently drops events when a forwarder is configured (e.g. Threadline) but its dep is not loaded; the upstream `Logger.warning` is emitted from `Sigra.Application.maybe_warn_missing_forwarder_deps/0` per D-23, NOT from here." Body: `@behaviour Sigra.Audit.Forwarder` + `@impl Sigra.Audit.Forwarder` + `def attach(_opts), do: :ok` per D-22. Do NOT call `:telemetry.attach/4`. Do NOT call `Logger`.

    Do not embed code fences here — use the literal drafts in RESEARCH.md §2.6 and §2.7 as your templates.
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors 2>&amp;1 | grep -E "Sigra\.Audit\.Forwarder" || true; MIX_ENV=test mix test test/sigra/audit/forwarder_test.exs test/sigra/audit/forwarders/noop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `lib/sigra/audit/forwarder.ex` exists; `grep -c '@callback attach(opts :: keyword()) :: :ok | {:error, term()}' lib/sigra/audit/forwarder.ex` returns exactly `1`.
    - Source: `lib/sigra/audit/forwarder.ex` has zero `@callback` lines other than `attach/1` — `grep -v '^#' lib/sigra/audit/forwarder.ex | grep -c '@callback'` returns exactly `1` (D-33).
    - Source: `lib/sigra/audit/forwarder.ex` moduledoc contains `Mox.defmock(MyForwarder, for: Sigra.Audit.Forwarder)` — `grep -c 'Mox.defmock(MyForwarder' lib/sigra/audit/forwarder.ex` returns `>= 1` (D-04).
    - Source: `lib/sigra/audit/forwarders/noop.ex` contains `@behaviour Sigra.Audit.Forwarder` AND `def attach(_opts), do: :ok` — both `grep -c` return `>= 1` (D-22).
    - Source: `lib/sigra/audit/forwarders/noop.ex` contains the warning admonition — `grep -c '#### Warning' lib/sigra/audit/forwarders/noop.ex` returns `>= 1` (D-24).
    - Source: `lib/sigra/audit/forwarders/noop.ex` does NOT call `:telemetry` or `Logger` — both `grep -v '^#' lib/sigra/audit/forwarders/noop.ex | grep -c ':telemetry'` and `grep -v '^#' lib/sigra/audit/forwarders/noop.ex | grep -c 'Logger\.'` return exactly `0` (D-22, D-23).
    - Behavior: `MIX_ENV=test mix compile --warnings-as-errors` exits 0 (no unused `@impl`, no missing `@callback`).
    - Test: `MIX_ENV=test mix test test/sigra/audit/forwarder_test.exs test/sigra/audit/forwarders/noop_test.exs` exits 0.
  </acceptance_criteria>
  <done>
    Both lib modules exist; behaviour contract test + Noop test pass; mix compile is clean with `--warnings-as-errors`.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Source assertions
grep -c '@callback attach(opts :: keyword()) :: :ok | {:error, term()}' lib/sigra/audit/forwarder.ex   # == 1
grep -v '^#' lib/sigra/audit/forwarder.ex | grep -c '@callback'                                          # == 1 (D-33: ONLY attach/1)
grep -c '#### Warning' lib/sigra/audit/forwarders/noop.ex                                                # >= 1 (D-24)
grep -v '^#' lib/sigra/audit/forwarders/noop.ex | grep -c ':telemetry'                                    # == 0 (D-22)
grep -v '^#' lib/sigra/audit/forwarders/noop.ex | grep -c 'Logger\.'                                      # == 0 (D-23)

# Behavior assertions
MIX_ENV=test mix compile --warnings-as-errors                                                              # exit 0
MIX_ENV=test mix test test/sigra/audit/forwarder_test.exs test/sigra/audit/forwarders/noop_test.exs       # exit 0
```
</verification>

<must_haves>
truths:
  - "`Sigra.Audit.Forwarder` behaviour defines a single `@callback attach(keyword) :: :ok | {:error, term}` (D-01, D-33)"
  - "Behaviour moduledoc documents `Mox.defmock(MyForwarder, for: Sigra.Audit.Forwarder)` (D-04)"
  - "`Sigra.Audit.Forwarders.Noop.attach/1` returns `:ok`, does NOT subscribe to telemetry, does NOT log (D-22, D-23)"
  - "Noop moduledoc carries the `> #### Warning {: .warning}` ExDoc admonition (D-24)"
  - "A host stub `@behaviour Sigra.Audit.Forwarder` module compiles with zero `@impl` warnings (FB-01 / SC-5)"
artifacts:
  - path: "lib/sigra/audit/forwarder.ex"
    provides: "Behaviour contract"
    contains: "@callback attach(opts :: keyword()) :: :ok | {:error, term()}"
  - path: "lib/sigra/audit/forwarders/noop.ex"
    provides: "Fail-open Noop fallback"
    contains: "@behaviour Sigra.Audit.Forwarder"
key_links:
  - from: "lib/sigra/audit/forwarders/noop.ex"
    to: "lib/sigra/audit/forwarder.ex"
    via: "@behaviour"
    pattern: "@behaviour Sigra\\.Audit\\.Forwarder"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app → Sigra behaviour | Host implements `attach/1`; we trust nothing about what they do inside it. Plan 04/05 will isolate failures. |
| ExUnit test process → behaviour | Mox-defined mocks invoke `attach/1`; we trust Mox's stub framework. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Information disclosure | `Sigra.Audit.Forwarder` moduledoc | accept | Moduledoc is public on hex docs; documenting Mox usage and keyword keys is intentional. No secrets in module surface. |
| T-131-02 | Tampering | `Sigra.Audit.Forwarders.Noop` | mitigate | Noop returns `:ok` and does NOT subscribe to telemetry (D-22) — eliminates the auto-detach landmine (D-20) for the fallback path. Acceptance criterion grep-asserts no `:telemetry` or `Logger` calls in the file. |
| T-131-03 | Repudiation | Behaviour contract | accept | Single `attach/1` callback means host impls cannot conceal `handle_event/4` semantics in the behaviour itself (D-33) — each impl owns its own emission shape; Plan 04 enforces the Threadline impl's invariants. |
| T-131-SC | Tampering | mix.exs deps (no new ones in this plan) | n/a | Plan 01 adds no new package-manager installs. Threadline pin lands in Plan 03 with legitimacy check there. |
</threat_model>

<success_criteria>
- `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
- `MIX_ENV=test mix test test/sigra/audit/forwarder_test.exs test/sigra/audit/forwarders/noop_test.exs` exits 0.
- All grep-based source assertions in `<verification>` pass.
- No `Code.ensure_loaded?` / `Application.get_env` calls in either lib file (the behaviour and Noop are pure surfaces; runtime wiring lives in Plan 05).
- Wave 1 sibling Plan 02 (`emit_telemetry/1` extension) can land in parallel — no file overlap.
</success_criteria>

<output>
Create `.planning/phases/131-forwarder-behaviour-threadline-forwarder-library-scaffolding/131-01-SUMMARY.md` when done. Include: file paths created, `mix test` output for both new test files, grep-assertion results for D-01/D-04/D-22/D-23/D-24/D-33, and a one-line statement of the behaviour callback signature.
</output>
