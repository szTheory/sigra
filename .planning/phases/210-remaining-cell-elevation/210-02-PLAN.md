---
phase: 210-remaining-cell-elevation
plan: 02
type: execute
wave: 2
depends_on:
  - 210-01
files_modified:
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - PAGE-03
  - FLOW-01

must_haves:
  truths:
    - "D-05/GROUP-02: Exactly the 11 mg-* L2 rows in guides/reference/admin-quality-ledger.md (~:74-84) are flipped from 1 to bare 2: mg-1-metric-summary-strip, mg-2-filter-panel-applied-chips, mg-3-task-card-grid, mg-4-alarm-notice-band, mg-5-user-results-pagination, mg-6-audit-feed-pagination, mg-7-organization-member-roster, mg-8-pending-invitations, mg-9-identity-header-summary-facts, mg-10-detail-facts-membership-panels, mg-11-destructive-action-confirmation. No other rows are modified"
    - "D-06: Every flipped tier value is a bare integer 2 in column 4 — never 2* or any decorated value (a decorated integer breaks the awk -F'|' /^[012]$/ monotonic parse and silently drops the row from the guard)"
    - "D-05: Each mg-* row's Evidence column carries the rich semicolon-delimited Tier-2 shape mirroring the freshly-flipped L1 rows (~:61-73): board-mg-N axe+screenshot ×3 projects (admin-design.spec.ts assertBoardScreenshot, GROUP_BOARDS loop); no-card-in-card (.sg-card .sg-card check ~:349-361); intra-group single-tier sg-stack rhythm; right-component-for-job (exact-count assertion); zero/loading/error state evidence with explicit N/A notes per 208 D-08; content-equivalence for mg-5/mg-6 ONLY (N/A elsewhere per 208 D-07); the board-cfg-* composite snapshot-clean ×3 projects citation per 208 D-05; the MG-7/MG-8 isolated-board-only ruling per 208 D-06"
    - "GROUP-02: The mg-* evidence cites the board-cfg-* composite proof (board-cfg-overview / board-cfg-users-list / board-cfg-user-detail / board-cfg-audit snapshot-clean ×3 projects) for the groups composited there, and records MG-7/MG-8 as isolated-board-only (no board-cfg-org, 208 D-06)"
    - "SC-4 (D-07): After the flip, scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 and — combined with Plan 01 — EVERY L0/L1/L2/L3/L4 cell reads bare 2 (the whole fractal is complete); the guard self-test quality-ledger-monotonic.test.sh stays green"
  artifacts:
    - guides/reference/admin-quality-ledger.md
  prohibitions:
    - statement: "D-06: The tier value is bare 2 — never 2* or any decorated integer; a decorator makes the row invisible to the awk -F'|' monotonic parse"
      status: enforced
      verification: "grep -E '^\\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$' returns empty output"
    - statement: "208 D-06: The mg-7 and mg-8 evidence strings contain the explicit isolated-board-only ruling so the absence of a board-cfg-org composite reads as intentional, not a coverage hole"
      status: enforced
      verification: "grep -E '^\\| mg-7-' guides/reference/admin-quality-ledger.md cites isolated-board-only / no board-cfg-org; grep -E '^\\| mg-8-' likewise"
    - statement: "208 D-08: The mg-3 evidence records the deliberate state-N/A note pattern (mg-3-zero-note / mg-3-loading-note); mg-9 and mg-11 evidence records real zero/loading/error states — no fabricated states, no mislabeling a real state as a note"
      status: enforced
      verification: "grep -E '^\\| mg-3-' guides/reference/admin-quality-ledger.md mentions the state-N/A note (zero-note/loading-note); grep -E '^\\| mg-9-' and '^\\| mg-11-' cite real zero/loading/error states"
    - statement: "208 D-07: content-equivalence is cited (non-N/A) for mg-5 and mg-6 ONLY (assertUserResultEquivalence / assertAuditResultEquivalence); N/A for all other mg-* groups"
      status: enforced
      verification: "grep -E '^\\| mg-(5|6)-' cites content-equivalence with assertUser/assertAuditResultEquivalence; the other 9 mg-* rows cite content-equivalence: N/A"
    - statement: "D-02: No LiveView/source/spec/gallery edits — only the 11 L2 group rows are flipped; no L3/L4 row is altered by this plan (Plan 01 owns those); no code, no test/example/ copy, no golden fixture, no Playwright baseline, no allowlist, no canary"
      status: enforced
      verification: "git diff --stat shows only guides/reference/admin-quality-ledger.md; the L3 page rows and L4 flow rows are byte-unchanged BY THIS PLAN (they were set by Plan 01)"
  key_links:
    - "The monotonic guard reads column 4 with awk -F'|' and /^[012]$/ — bare 2 only; a decorated value loses regression protection"
    - "208-01-SUMMARY.md per-group audit findings (single-tier rhythm, no card-in-card, right-component exact counts, true state-marker set, mg-5/mg-6 content-equivalence, mg-7/mg-8 isolated-only ruling) become the 11 mg-* evidence strings"
    - "The 12 committed board-cfg-* baselines (208-02) are the composite proof each composited mg-* evidence string cites (board-cfg-* snapshot-clean ×3 projects)"
---

<objective>
Execute the folded Phase 208-03 scope verbatim: flip exactly the 11 mg-* L2 rows in
guides/reference/admin-quality-ledger.md (~:74-84) from tier 1 to bare 2 — each with a rich
semicolon-delimited evidence string mirroring the freshly-flipped L1 rows (~:61-73). Cite, per
group: the board-mg-N axe+screenshot ×3-project gate, the no-card-in-card check, intra-group
single-tier rhythm, right-component-for-job, the true zero/loading/error state evidence (with
explicit N/A notes for mg-3 per 208 D-08), content-equivalence for mg-5/mg-6 only (208 D-07),
the board-cfg-* composite snapshot-clean proof (208 D-05), and the MG-7/MG-8 isolated-board-only
ruling (208 D-06). Verify the monotonic guard exits 0 vs origin/main and — with Plan 01 —
the entire L0/L1/L2/L3/L4 column now reads bare 2.

Purpose: This is the folded GROUP-02 flip (D-01/D-05) — the 11 mg-* L2 rows are still Tier-1
because Phase 208-03 was authored but never executed (208.1 was inserted instead). Folding it
here makes SC-4 ("every ledger cell is now Tier-2 — the whole fractal is complete") legitimately
true at close and keeps Phase 211 as pure terminal verification. Every evidence string cites the
already-wired automated gates (admin-design.spec.ts axe+screenshot harness, catalog-state +
card-in-card + content-equivalence tests) and the documented audit/baseline proofs from
208-01/208-02.

Output: guides/reference/admin-quality-ledger.md with the 11 mg-* rows at bare tier 2; monotonic
guard green vs origin/main; the whole L0–L4 fractal at Tier-2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/210-remaining-cell-elevation/210-CONTEXT.md
@.planning/phases/208-l2-meta-component-group-elevation/208-03-PLAN.md
@.planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md
@.planning/phases/208-l2-meta-component-group-elevation/208-02-SUMMARY.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
@scripts/ci/quality-ledger-monotonic.sh
</context>

## Artifacts this phase produces

No new symbols or files — this phase edits existing rows in guides/reference/admin-quality-ledger.md only. This plan flips the 11 existing mg-* L2 rows (mg-1…mg-11) from tier 1 to bare tier 2 and expands their Evidence columns with the rich semicolon-delimited template; it creates no net-new symbol or file.

<tasks>

<task type="auto">
  <name>Task 1: Flip the 11 mg-* L2 ledger rows to bare tier 2 with rich evidence (folded 208-03 Task 1)</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — read the full Quality Ledger; the 11 target rows are the mg-* L2 rows at ~:74-84 (currently tier 1 with sparse link-style evidence). The Tier-2 exemplar evidence shape = the freshly-flipped L1 rows at ~:61-73 (stat, stat_link, task_card, … audit_row). Also read the "Asserting Tier 2" / parsing-rules section (~:14-54) explaining the bare-2 requirement and forbidden decorators.
  - .planning/phases/208-l2-meta-component-group-elevation/208-03-PLAN.md — the folded plan (D-01/D-05). Its Task 1 action block enumerates the L2 evidence template + per-row specifics; execute those verbatim.
  - .planning/phases/208-l2-meta-component-group-elevation/208-01-SUMMARY.md "Per-Group Audit" — per-group findings: single-tier rhythm, no card-in-card (and the mg-3/mg-5 <section> + mg-6 sg-card-with-inner-results notes), right-component exact counts, the TRUE state-marker set per group (mg-3 = N/A notes; mg-9/mg-11 = real states), content-equivalence mg-5/mg-6 only, the MG-7/MG-8 isolated-board-only ruling, and the "CSS edited: no" verdict
  - .planning/phases/208-l2-meta-component-group-elevation/208-02-SUMMARY.md — confirmation that the 12 board-cfg-* baselines are captured CI-native and snapshot-clean ×3 projects; the cfg-board mapping (board-cfg-overview / board-cfg-users-list / board-cfg-user-detail / board-cfg-audit) for the per-group cfg citation
  - guides/reference/admin-fractal-scorecard.md "L2 Meta-Component Group Add-ons" — the proxy vocabulary the evidence strings use
  </read_first>

  <action>
  Use Edit (scoped replacement) to update each of the 11 mg-* rows — one Edit call per row, using the row's existing sparse cell text as the unique old_string. Never Write the full ledger file.

  Each new row format: | {name} | L2 | 2 | {evidence-string} |
  The tier column MUST be the bare integer 2 — no asterisks, no decorators. The awk -F'|' parser reads column 4 and requires /^[012]$/ after trimming whitespace.

  L2 evidence template (semicolon-delimited; use N/A for inapplicable proxies), mirroring the L1 Wave rows:
  "admin-design.spec.ts assertBoardScreenshot board-mg-N — 3 projects (admin-design-chromium / -mobile / -dark) × toHaveScreenshot + assertNoAxeViolations (wcag2a/2aa/21a/21aa/22aa) — 0 violations; no card-in-card: .sg-card .sg-card check (admin-design.spec.ts ~:349-361) — 0 nested, no data-sg-card-nesting-audit-only escape hatch; intra-group rhythm: single-tier sg-stack--4; right-component: {exact-count assertion}; zero/loading/error: {state evidence per 208 D-08}; content-equivalence: {mg-5/mg-6 only, else N/A per 208 D-07}; real-page composite: {board-cfg-* snapshot-clean ×3 projects per 208 D-05, or isolated-board-only per 208 D-06}; byte-coherent reuse: {note}; overlay-axe: N/A; APG: N/A"

  Per-row specifics (cite the exact board-mg-N id and the audit findings from 208-01-SUMMARY.md):

  - mg-1-metric-summary-strip: right-component: 7 .sg-metric (admin-design.spec.ts ~:331); states: mg-1-populated/zero/loading/error all rendered; content-equivalence: N/A; real-page composite: board-cfg-overview snapshot-clean ×3.
  - mg-2-filter-panel-applied-chips: right-component: 6 .sg-applied-chip (~:332); states: full set; byte-coherent reuse: applied-chips reused on board-cfg-users-list; content-equivalence: N/A; real-page composite: board-cfg-users-list snapshot-clean ×3.
  - mg-3-task-card-grid: right-component: 2 article.sg-card, board-mg-3 is a <section> (asserts not.toHaveClass(sg-card) ~:334); states: mg-3-zero-note / mg-3-loading-note — DELIBERATE state-N/A notes per 208 D-08 (not fabricated states) + real mg-3-error; content-equivalence: N/A; real-page composite: N/A (task-card grid not on a cfg composite).
  - mg-4-alarm-notice-band: right-component: 3 .sg-notice (~:335) incl notice tone={:risk} error; states: full set incl real alarm zero-state; content-equivalence: N/A; real-page composite: board-cfg-overview snapshot-clean ×3.
  - mg-5-user-results-pagination: right-component: results table + mobile cards (mg-5-desktop-results/mg-5-mobile-results ~:336-337); states: full set; content-equivalence: assertUserResultEquivalence green (~:364-371) — table+mobile-card swap proven (208 D-07); real-page composite: board-cfg-users-list snapshot-clean ×3.
  - mg-6-audit-feed-pagination: right-component: 3 article.sg-list-row (~:340), mg-6-desktop-results/mg-6-mobile-results; states: full set; content-equivalence: assertAuditResultEquivalence green (~:373-377) + Previous/Next/Export CSV controls (~:378-383) — 208 D-07; byte-coherent reuse: audit feed reused on board-cfg-audit; real-page composite: board-cfg-audit snapshot-clean ×3.
  - mg-7-organization-member-roster: right-component: 3 .sg-list-row (~:341), zero uses <.empty_state>; states: mg-7-populated/zero/loading/error; content-equivalence: N/A; real-page composite: ISOLATED-BOARD-ONLY — there is no board-cfg-org composite and per 208 D-06 (user-ratified) one is intentionally NOT authored; isolated board-mg-7 coverage is sufficient.
  - mg-8-pending-invitations: right-component: 3 .sg-list-row (~:342), zero uses <.empty_state>; states: mg-8-populated/zero/loading/error; content-equivalence: N/A; real-page composite: ISOLATED-BOARD-ONLY — no board-cfg-org composite, intentionally not authored per 208 D-06; isolated board-mg-8 coverage is sufficient.
  - mg-9-identity-header-summary-facts: right-component: 1 .sg-summary-facts (~:343); states: REAL mg-9-zero / mg-9-loading / mg-9-error (not notes — per 208 D-08); content-equivalence: N/A; real-page composite: board-cfg-user-detail snapshot-clean ×3.
  - mg-10-detail-facts-membership-panels: right-component: 2 .sg-detail-grid (~:344); states: full set; content-equivalence: N/A; real-page composite: board-cfg-user-detail snapshot-clean ×3.
  - mg-11-destructive-action-confirmation: right-component: 2 .sg-confirm-overlay .sg-confirm-dialog (~:345); states: REAL mg-11-zero / mg-11-loading / mg-11-error (not notes — per 208 D-08); byte-coherent reuse: confirmation reused on board-cfg-user-detail; content-equivalence: N/A; real-page composite: board-cfg-user-detail snapshot-clean ×3.

  Pull the exact counts, state-marker shapes, and the "CSS edited: no" context from 208-01/208-02-SUMMARY. Preserve table column spacing. Do NOT decorate the tier value. Do NOT place any literal a conformance guard negative-greps for inside an evidence string.
  </action>

  <verify>
    <automated>bash -c 'grep -E "^\| mg-[0-9]+-" guides/reference/admin-quality-ledger.md | awk -F"|" "{t=\$4; gsub(/^ +| +\$/,\"\",t); print t}" | grep -xc 2'</automated>
  </verify>

  <acceptance_criteria>
  - All 11 mg-* rows show bare 2 in column 4: the verify command returns 11
  - No decorated tier values anywhere in the ledger: `grep -E '^\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$'` returns empty output (D-06)
  - Each of the 11 mg-* rows cites the axe/screenshot harness: `grep -E '^\| mg-[0-9]+-' guides/reference/admin-quality-ledger.md | grep -c 'assertBoardScreenshot'` returns 11
  - mg-7 and mg-8 evidence record the isolated-board-only ruling: `grep -E '^\| mg-(7|8)-' guides/reference/admin-quality-ledger.md | grep -ci 'isolated-board-only\|no board-cfg-org'` returns 2 (208 D-06)
  - mg-3 evidence cites the deliberate state-N/A note: `grep -E '^\| mg-3-' guides/reference/admin-quality-ledger.md | grep -c 'note'` returns >= 1 (208 D-08); mg-5 and mg-6 evidence cite content-equivalence with the assert*ResultEquivalence assertions: `grep -E '^\| mg-(5|6)-' guides/reference/admin-quality-ledger.md | grep -c 'ResultEquivalence'` returns 2 (208 D-07)
  - Non-target rows untouched by this plan: the L0 token-layer cell + 13 L1 cells stay 2; the L3 page rows + L4 flow rows (set by Plan 01) are byte-unchanged by this plan (only the 11 mg-* L2 rows changed in this plan's git diff)
  </acceptance_criteria>

  <done>All 11 mg-* L2 rows show bare 2 with rich evidence (right-component counts, true state markers, content-equivalence mg-5/mg-6, cfg-composite citation, mg-7/mg-8 isolated-only ruling); no decorators; non-target rows untouched by this plan.</done>
</task>

<task type="auto">
  <name>Task 2: Run the monotonic guard to confirm no regressions and the full L0–L4 Tier-2 (folded 208-03 Task 2)</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — re-read after edits to confirm all 11 mg-* rows are bare 2 with rich evidence and no row was accidentally decorated before running the guard; confirm the Plan-01 rows (user-sessions + 3 flow-*) are already at 2, so the entire column should now read 2
  - scripts/ci/quality-ledger-monotonic.sh — confirm --base origin/main usage and that it parses column 4 with awk -F'|' and /^[012]$/; the PASS line reports a cell count
  </read_first>

  <action>
  Run the monotonic guard against origin/main to confirm:
  1. No tier values decreased vs main (the 11 mg-* rows moved 1 → 2, an allowed increase).
  2. Combined with Plan 01, the ENTIRE L0/L1/L2/L3/L4 column now reads bare 2 — the whole fractal is complete (SC-4).

  Command: bash scripts/ci/quality-ledger-monotonic.sh --base origin/main
  Expected: "quality-ledger-monotonic: PASS (N cells checked vs origin/main)", exit 0.

  If the guard exits non-zero or the cell count is lower than expected, diagnose: a decorated tier value (2*, 2 ✓) makes a row invisible to the awk parse — find and replace with bare 2; or a row was accidentally decreased. Fix and re-run.

  Also run the guard self-test to confirm the guard itself is still green:
  bash scripts/ci/quality-ledger-monotonic.test.sh

  Confirm the full-fractal Tier-2 claim: grep the ledger for every L0/L1/L2/L3/L4 row and assert every tier cell is 2.
  </action>

  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && bash scripts/ci/quality-ledger-monotonic.test.sh</automated>
  </verify>

  <acceptance_criteria>
  - `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0 and emits a PASS line (D-07)
  - `bash scripts/ci/quality-ledger-monotonic.test.sh` exits 0 (guard self-test still green) (D-07)
  - Every ledger row reads bare 2 (whole fractal complete, SC-4): `grep -E '^\| [a-z].* \| L[0-4] \|' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -vxc 2` returns 0 (no L0–L4 row at a tier other than 2)
  - `git diff --stat` for this plan shows only guides/reference/admin-quality-ledger.md modified — no source, no PNG, no allowlist, no canary (D-02, D-08)
  </acceptance_criteria>

  <done>Monotonic guard exits 0 vs origin/main; every L0–L4 cell reads bare 2 (whole fractal complete); guard self-test green; no other files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger edit → awk parse | A malformed tier column (e.g. "2*") silently removes the row from monotonic-guard forward-only regression protection |

No security-relevant surface: documentation-only change to guides/reference/admin-quality-ledger.md. No code, no data flow, no auth/authz/input-handling change, no trust-boundary change. This plan changes ZERO executable code (D-02).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-210-03 | Tampering | Ledger tier column format | low | mitigate | Acceptance criteria + Task 2 verify that no decorated value exists (grep -vE '^[012]$' returns empty) and the monotonic guard exits 0; any invisible row is caught before merge |
| T-210-04 | Repudiation | Evidence string accuracy | low | accept | Evidence strings are human-curated, bounded by 208-01 audit findings + 208-02 baseline proofs; the cited gates (admin-design.spec.ts assertBoardScreenshot / assertUser/AuditResultEquivalence) are independently runnable |
</threat_model>

<verification>
After both tasks complete:
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main   (exits 0)
  bash scripts/ci/quality-ledger-monotonic.test.sh                 (exits 0)
  grep -E '^\| mg-[0-9]+-' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -xc 2   (returns 11)
  grep -E '^\| mg-(7|8)-' guides/reference/admin-quality-ledger.md | grep -ci 'isolated-board-only\|no board-cfg-org'   (returns 2)
  grep -E '^\| [a-z].* \| L[0-4] \|' guides/reference/admin-quality-ledger.md | awk -F'|' '{t=$4; gsub(/^ +| +$/,"",t); print t}' | grep -vxc 2   (returns 0 — whole fractal at Tier-2)
</verification>

<success_criteria>
- 11 mg-* L2 rows at bare tier 2 with rich evidence (right-component counts, true state markers, content-equivalence mg-5/mg-6, cfg-composite citation, mg-7/mg-8 isolated-only ruling)
- No decorated tier values anywhere in the ledger (D-06)
- Monotonic guard exits 0 vs origin/main; guard self-test green (D-07)
- Combined with Plan 01, the entire L0/L1/L2/L3/L4 column reads bare 2 — the whole fractal is complete (SC-4)
- No source/spec/gallery/baseline/allowlist/canary changes (D-02, D-08)
</success_criteria>

<output>
Create .planning/phases/210-remaining-cell-elevation/210-02-SUMMARY.md when done
</output>
