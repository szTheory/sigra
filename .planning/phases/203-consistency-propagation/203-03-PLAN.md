---
phase: 203-consistency-propagation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/admin-modal-interaction.spec.ts
autonomous: true
requirements: [PROP-01]
must_haves:
  truths:
    - "admin-modal-interaction.spec.ts now opens the branding #restore-defaults-overlay and proves the 7 APG hard gates + axe-while-open (D-06)"
    - "The new branding case asserts aria-labelledby='restore-defaults-title' (NOT the user-sessions value) (Pitfall 4)"
    - "The branding dialog passes initial-focus, tab containment, ARIA, axe-while-open, Escape close, and focus return — the same canonical 7-gate APG set the user-sessions case proves"
    - "This test is the hard prerequisite for the D-08 branding-live overlay-axe/APG ledger evidence (Plan 05)"
  artifacts:
    - test/example/priv/playwright/tests/admin-modal-interaction.spec.ts
  key_links:
    - "new case triggers phx-click=open_restore_defaults (branding_live.ex:340), asserts #restore-defaults-overlay + restore-defaults-title, reuses the [data-sg-confirm-cancel] / .sg-confirm-dialog button:first/last-of-type contract"
  prohibitions:
    - "Do NOT copy the user-sessions selectors verbatim — the branding dialog uses #restore-defaults-overlay and aria-labelledby='restore-defaults-title', NOT #user-session-confirm-overlay / user-session-confirm-title (Pitfall 4)"
    - "Do NOT add toHaveScreenshot — this is interaction-only evidence, no baselines (D-10: no branding board/slug exists)"
    - "Do NOT delete or weaken the existing user-sessions 7-gate test — ADD a sibling branding case"
---

<objective>
Add a branding-specific ConfirmDialog modal-interaction test to `admin-modal-interaction.spec.ts` that opens `branding_live.ex`'s `#restore-defaults-overlay` (`:349-378`) and asserts the same 7 APG hard gates + axe-while-open that the existing user-sessions case proves. Today the spec ONLY opens `#user-session-confirm-overlay` — the branding dialog is wired with the identical `phx-hook="ConfirmDialog"` contract but is NOT exercised (Pitfall 6), so the `branding-live` ledger cell's existing `admin-modal-interaction` link is aspirational/misleading at Tier 1.

Purpose: This is the SINGLE net-new test in the phase and the HARD PREREQUISITE for the D-08 `branding-live` overlay-axe + 7-APG Tier-2 evidence (Plan 05). Land it green FIRST so the branding ledger ratchet cites a real, passing assertion (not a false Tier-2 claim that Phase 204's adversarial review fails).
Output: A new branding `#restore-defaults-overlay` case in `admin-modal-interaction.spec.ts`, passing on the chromium behavior lane; no screenshots.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-consistency-propagation/203-CONTEXT.md
@.planning/phases/203-consistency-propagation/203-UI-SPEC.md
@test/example/priv/playwright/tests/admin-modal-interaction.spec.ts
@lib/sigra/admin/live/branding_live.ex
</context>

<tasks>

<!-- planner-discipline-allow: toHaveScreenshot -->
<task type="auto">
  <name>Task 1: Add the branding #restore-defaults-overlay 7-APG + axe case (D-06)</name>
  <files>test/example/priv/playwright/tests/admin-modal-interaction.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-modal-interaction.spec.ts (the entire existing user-sessions case :67-188 — copy its 7-gate structure: open, initial focus on [data-sg-confirm-cancel], tab containment 3a/b/c/d via .sg-confirm-dialog button:first/last-of-type, ARIA, assertNoAxeViolations while open, Escape close, focus return; plus the registerUser/clearBrowserSession/waitForLiveViewReady helpers and the platform-admin+ email convention :77)
    - lib/sigra/admin/live/branding_live.ex (:340 open_restore_defaults trigger button "Restore config defaults"; :349 #restore-defaults-overlay phx-hook="ConfirmDialog"; :352-354 role="dialog" aria-modal="true" aria-labelledby="restore-defaults-title"; :363-364 cancel_restore_defaults + data-sg-confirm-cancel; :371 restore_config_defaults confirm)
    - .planning/phases/203-consistency-propagation/203-UI-SPEC.md (Interaction Contracts -> ConfirmDialog Branding Restore defaults — the 7 APG gates list + axe tag set)
    - .planning/phases/203-consistency-propagation/203-RESEARCH.md (Pitfall 4 — branding-specific selectors NOT user-sessions selectors; Pattern 4 D-06 adaptation)
  </read_first>
  <action>
    Add a NEW test (a sibling test inside the existing describe block, or a new describe) that proves the branding ConfirmDialog. Mirror the existing user-sessions case structure exactly, but parameterized on the branding dialog's identifiers. Steps: (a) register and sign in as a platform-admin (use the existing `platform-admin+...@example.test` convention that grants platform-admin via Example.SigraAdminPolicy, and the existing registerUser/clearBrowserSession helpers); (b) navigate to /admin/branding and waitForLiveViewReady; (c) capture the "Restore config defaults" trigger button (phx-click="open_restore_defaults", branding_live.ex:340) and click it; (d) assert overlay `#restore-defaults-overlay` visible and getByRole('dialog') visible; (e) Gate 2: initial focus on the Cancel button via the `[data-sg-confirm-cancel]` / `.sg-confirm-dialog button:first-of-type` contract; (f) Gate 3a/b/c/d: Tab/Shift+Tab containment using `.sg-confirm-dialog button:first-of-type`/`:last-of-type` and focus-stays-inside-dialog; (g) Gate 6 ARIA: assert `.sg-confirm-dialog[role="dialog"]` has `aria-modal="true"` AND `aria-labelledby` equal to `"restore-defaults-title"` (NOT the user-sessions title — Pitfall 4), and `#restore-defaults-title` is visible; (h) Gate 7: run assertNoAxeViolations while open (reuse the existing helper / withTags(['wcag2a','wcag2aa'])); (i) Gate 4: Escape closes the overlay (toBeHidden); (j) Gate 5: focus returns to the trigger. NO toHaveScreenshot (interaction-only). Do NOT remove or weaken the existing user-sessions case. The confirm button label match should be branding-appropriate (e.g. /restore|confirm/i) since the confirm copy is "Restore defaults".
  </action>
  <acceptance_criteria>
    - `grep -c 'restore-defaults-overlay' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` returns >= 1 (branding overlay now exercised)
    - `grep -c 'restore-defaults-title' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` returns >= 1 (branding label asserted, not the user-sessions label)
    - `grep -c 'user-session-confirm-overlay' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` returns >= 1 (existing case retained, not deleted)
    - `grep -c 'toHaveScreenshot' test/example/priv/playwright/tests/admin-modal-interaction.spec.ts` returns 0 (interaction-only, no baselines)
    - The new branding case passes against a booted example: 7 APG gates + axe-while-open all green
  </acceptance_criteria>
  <verify>
    <automated>cd test/example/priv/playwright && SIGRA_EXAMPLE_URL=http://localhost:4011 npx playwright test admin-modal-interaction.spec.ts --project=chromium</automated>
  </verify>
  <done>The branding #restore-defaults-overlay case passes all 7 APG gates + axe-while-open against the booted example; the existing user-sessions case is untouched; no screenshots; aria-labelledby asserts restore-defaults-title.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin operator -> branding LiveView modal | Admin-gated; the new test asserts the platform-admin email reaches /admin/branding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-03-a11y | (a11y-as-security adjacency) | #restore-defaults-overlay focus trap | low | mitigate | The new D-06 test proves focus-trap/restore + correct ARIA labelling + axe-clean while open — a focus-trap escape or scrim-hidden modal would fail the 7 gates |
| T-203-03-E | Elevation of privilege | /admin/branding reachability | low | accept | Existing SigraAdminPolicy gate unchanged; the test uses the platform-admin+ email convention to legitimately reach the surface |
</threat_model>

<verification>
- The new branding case passes: 7 APG gates + axe-while-open green on the chromium lane
- `grep -c 'restore-defaults-overlay' ...spec.ts` >= 1
- `grep -c 'restore-defaults-title' ...spec.ts` >= 1
- `grep -c 'user-session-confirm-overlay' ...spec.ts` >= 1 (existing case retained)
- `grep -c 'toHaveScreenshot' ...spec.ts` == 0
</verification>

<success_criteria>
admin-modal-interaction.spec.ts proves the branding #restore-defaults-overlay against the same 7 APG hard gates + axe-while-open as the user-sessions dialog, using branding-specific selectors (restore-defaults-overlay / restore-defaults-title). The existing user-sessions case is intact. This test is green and is the verified prerequisite for the Plan 05 branding-live Tier-2 evidence.
</success_criteria>

<artifacts_this_phase_produces>
- NEW test case in `test/example/priv/playwright/tests/admin-modal-interaction.spec.ts`: branding `#restore-defaults-overlay` ConfirmDialog — 7 APG hard gates + axe-while-open (interaction-only, no screenshots). This is the single net-new test of the phase and the prerequisite for the D-08 `branding-live` overlay-axe/APG ledger evidence.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/203-consistency-propagation/203-03-SUMMARY.md` when done
</output>
