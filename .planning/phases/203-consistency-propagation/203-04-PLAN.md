---
phase: 203-consistency-propagation
plan: 04
type: execute
wave: 2
depends_on: ["203-02"]
files_modified:
  - guides/reference/admin-design-contract.md
  - guides/reference/admin-ui-principles.md
autonomous: true
requirements: [PROP-02]
must_haves:
  truths:
    - "admin-design-contract.md gains a fifth archetype block: Branding/Workbench, documenting tab nav + disclosed panels + per-panel preview rail + ConfirmDialog restore-defaults (D-07)"
    - "The contract's Overview archetype block gets a light touch-up reflecting the org-roster pill reduction iff the pills changed (D-11)"
    - "admin-ui-principles.md is updated for the evolved interaction patterns (branding component routing D-05 / pill alignment D-02/D-03) (D-11)"
    - "The glossary drift-guard stays green (microcopy in docs introduces no banned synonyms) (D-11)"
    - "No archetype change is silent — the Workbench archetype is documented forward (PROP-02)"
  artifacts:
    - guides/reference/admin-design-contract.md
    - guides/reference/admin-ui-principles.md
  key_links:
    - "Branding/Workbench archetype block added after the Audit Explorer Archetype (:331) in admin-design-contract.md"
    - "Workbench archetype references the promoted color_field/preview_pair/detail_input components from Plan 02 and the #restore-defaults-overlay ConfirmDialog contract"
  prohibitions:
    - "Do NOT add a fifth archetype that contradicts the no-net-new-surfaces guardrail — the Workbench is a single-instance surface, not a repeatable list-driven one"
    - "Do NOT introduce microcopy that trips the glossary drift-guard (one-term-per-concept)"
    - "Do NOT touch the ledger cells here — the ratchet is owned by Plan 05 (D-08)"
---

<objective>
Document the evolved/new archetypes "forward, never silently" (PROP-02). The design contract currently has exactly four archetypes (Overview `:172`, List `:211`, Detail `:284`, Audit Explorer `:331`) and NO Workbench block. The branding workbench is a real, now-elevated composition with no archetype — that is a PROP-02 gap.

Two doc edits:
- D-07: add a Branding/Workbench archetype block to `admin-design-contract.md` (tab nav + disclosed panels + per-panel preview rail + ConfirmDialog restore-defaults), referencing the components Plan 02 promoted.
- D-11: light touch-up of `admin-ui-principles.md` for the evolved patterns (branding routing D-05, pill alignment D-02/D-03), and a light touch-up of the contract's Overview archetype block iff the roster pills changed (they did, in Plan 01).

Purpose: Close the PROP-02 documentation gap so the elevated bar is recorded, not implicit.
Output: A fifth archetype block in the contract; an updated UI-principles doc; glossary-clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-consistency-propagation/203-CONTEXT.md
@.planning/phases/203-consistency-propagation/203-UI-SPEC.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-ui-principles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Branding/Workbench archetype to the design contract (D-07)</name>
  <files>guides/reference/admin-design-contract.md</files>
  <read_first>
    - guides/reference/admin-design-contract.md (the four existing archetype blocks: Overview `### Overview Archetype` :172, List :211, Detail :284, Audit Explorer :331 — mirror the heading + composition-description format of the Audit Explorer block, and INSERT the new block after it; also the Overview Archetype :172-197 for the D-11 touch-up)
    - .planning/phases/203-consistency-propagation/203-UI-SPEC.md (Design Archetype — Branding/Workbench D-07: the required composition + documentation bullets)
    - lib/sigra/admin/live/branding_live.ex (the real composition the archetype documents: tab nav sg-tabs, three disclosed panels, per-panel preview rail with promoted color_field/preview_pair/detail_input, #restore-defaults-overlay ConfirmDialog)
  </read_first>
  <action>
    Add a new `### Branding/Workbench Archetype` block to admin-design-contract.md, inserted after the Audit Explorer Archetype (:331) so the contract documents five archetypes. Mirror the prose/structure style of the existing archetype blocks. Document the verified elevated composition: (1) tab navigation (sg-tabs) with three disclosed panels (Identity, Colors, Email), tab state managed via LiveView assigns not JS; (2) each panel = form section + per-panel preview rail; (3) the preview rail renders the promoted `color_field` / `preview_pair` / `detail_input` components (now in Sigra.Admin.Components per Plan 02); (4) restore-defaults destructive action in the form footer triggering the ConfirmDialog APG pattern (#restore-defaults-overlay, same hook contract as the user-sessions dialog); (5) no results table (content-equivalence proxy N/A); (6) no pagination; (7) note it is a single-instance workbench (one per admin install), NOT a repeatable list-driven surface. Use the exact heading text "Branding/Workbench Archetype" so the PROP-02 grep gate matches. Keep microcopy glossary-clean (no banned synonyms).
  </action>
  <acceptance_criteria>
    - `grep -c 'Branding/Workbench Archetype' guides/reference/admin-design-contract.md` returns >= 1
    - The block appears after the Audit Explorer Archetype heading (verify ordering)
    - The block names the promoted components (color_field/preview_pair/detail_input) and the #restore-defaults-overlay ConfirmDialog
    - The block states content-equivalence is N/A (no results table) and that it is a single-instance workbench
    - `mix test test/sigra/admin/glossary_test.exs` passes (the doc edit introduces no banned synonyms in the scoped source files; docs are not scoped but the change must not require renaming source microcopy)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Branding/Workbench Archetype' guides/reference/admin-design-contract.md && mix test test/sigra/admin/glossary_test.exs</automated>
  </verify>
  <done>The contract has a fifth Branding/Workbench archetype block (after Audit Explorer) documenting the elevated composition and the promoted components; glossary guard green.</done>
</task>

<task type="auto">
  <name>Task 2: UI-principles touch-up + Overview archetype touch-up (D-11)</name>
  <files>guides/reference/admin-ui-principles.md, guides/reference/admin-design-contract.md</files>
  <read_first>
    - guides/reference/admin-ui-principles.md (:29 "Same job means same component. Route reusable markup through Sigra.Admin.Components or the admin shell seam." — the principle the branding promotion now exemplifies; :47 no `transition: all`)
    - guides/reference/admin-design-contract.md (Overview Archetype :172-197 — the org-roster pill reduction from Plan 01 may warrant a light touch-up to the "Org variant" demoted-tail description if it lists the Confirmed pill)
    - .planning/phases/203-consistency-propagation/203-CONTEXT.md (D-11 — UI-principles touch-up for evolved interaction patterns; glossary stays drift-guarded)
  </read_first>
  <action>
    Apply a LIGHT, additive touch-up to admin-ui-principles.md reflecting the evolved patterns introduced this phase: (a) the branding workbench now routes its preview components through Sigra.Admin.Components (concrete exemplar of the :29 same-job->same-component principle — strengthen/annotate that principle with the branding routing as evidence, do not rewrite it); (b) the reduced-pill vocabulary (decision-bearing pills only; drop always-on Confirmed/coverage KPIs) as the consistent admin status-signal convention across Overviews and the Users Index. Then, in admin-design-contract.md, IFF the Overview archetype's "Org variant" description (:197) enumerates the Confirmed pill or the coverage chip as part of the documented composition, lightly update it to reflect the Plan 01 reduction (decision-bearing pills only). Keep both edits additive and glossary-clean; do NOT restructure either doc. If the Overview block does not name the dropped pill/chip, no contract change is needed there — record that in the SUMMARY.
  </action>
  <acceptance_criteria>
    - admin-ui-principles.md references the branding component routing and/or the reduced-pill convention as evolved patterns (git diff shows an additive touch-up to the :29 / status-signal area)
    - The contract's Overview archetype block does not document a dropped affordance (Confirmed pill / coverage chip) as still-present after Plan 01
    - `mix test test/sigra/admin/glossary_test.exs` passes
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/admin/glossary_test.exs && git diff --stat guides/reference/admin-ui-principles.md guides/reference/admin-design-contract.md</automated>
  </verify>
  <done>admin-ui-principles.md is updated forward for the branding routing + reduced-pill convention; the Overview archetype block no longer documents a dropped affordance as present; glossary guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> downstream agents/adopters | Design contract + principles are read by future planners and adopters; drift here propagates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-04-doc | Repudiation (silent archetype change) | admin-design-contract.md | low | mitigate | PROP-02 "forward, never silently" — the Workbench archetype is documented explicitly; glossary drift-guard keeps microcopy consistent |
</threat_model>

<verification>
- `grep -c 'Branding/Workbench Archetype' guides/reference/admin-design-contract.md` >= 1
- `mix test test/sigra/admin/glossary_test.exs` passes
- The contract documents five archetypes; the Workbench block follows Audit Explorer
- admin-ui-principles.md has an additive touch-up for the evolved patterns
</verification>

<success_criteria>
The design contract documents a fifth Branding/Workbench archetype (forward, never silently); admin-ui-principles.md reflects the branding component routing and reduced-pill convention; the Overview archetype block no longer documents a dropped affordance as present; the glossary drift-guard stays green (PROP-02 satisfied).
</success_criteria>

<artifacts_this_phase_produces>
- NEW archetype block: `### Branding/Workbench Archetype` in `guides/reference/admin-design-contract.md` (fifth archetype; documents tab nav + disclosed panels + per-panel preview rail + ConfirmDialog restore-defaults; references the promoted color_field/preview_pair/detail_input components).
- Additive touch-up to `guides/reference/admin-ui-principles.md` (branding component routing as a :29 exemplar; reduced-pill status-signal convention).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/203-consistency-propagation/203-04-SUMMARY.md` when done
</output>
