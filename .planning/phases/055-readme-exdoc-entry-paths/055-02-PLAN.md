---
phase: 55
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - docs/ga-evidence.md
  - mix.exs
  - guides/introduction/getting-started.md
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "**`mix.exs` `docs/0`** keeps **`main: \"getting-started\"`** unchanged and registers **`docs/ga-evidence.md`** in **`extras`**."
    - "**`docs/ga-evidence.md`** is a **thin hub**: purpose statement, **Executed vs Waived** glossary in plain language, **navigational bullets only** — **no** full GA matrix paste."
    - "**`guides/introduction/getting-started.md`** gains a short **Reading map** callout near the **top** (before or immediately after the first paragraph) linking to **`ga-evidence`**, maintainer docs, audit semantics, contributing, and security reporting — per **055-CONTEXT D-10**."
  artifacts:
    - path: "docs/ga-evidence.md"
      provides: "DOC-02 HexDocs-stable GA evidence index"
    - path: "guides/introduction/getting-started.md"
      provides: "≤2-hop path from default ExDoc landing"
nyquist_compliant: false
---

<objective>
Implement **DOC-02**: add packaged hub **`docs/ga-evidence.md`**, wire it into **`mix.exs`** `docs/0` **extras**, and add a **Reading map** callout to **`guides/introduction/getting-started.md`** so the default **`mix docs`** landing reaches maintainer-facing GA / audit narrative within **two hops**, per **`055-CONTEXT.md` D-08–D-11, D-17`**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-55-01 | Misrepresentation | Hub page reads like a **certification** or duplicates waived matrix | high | Thin router + forbidden grep tokens; link out to **`v1.4-GA-UAT.md`** blob |
| T-55-02 | Broken links | **`mix docs --warnings-as-errors`** fails on bad refs | medium | Use tag-scoped GitHub URLs for `.planning/` targets; verify command in tasks |
| T-55-03 | Information disclosure | Over-exposing maintainer-only workflow in integrator landing | low | Keep **Getting started** callout **short**; defer depth to hub + planning URLs |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Create docs/ga-evidence.md hub</name>
  <files>docs/ga-evidence.md</files>
  <read_first>
    - docs/uat-ci-coverage.md
    - docs/audit-semantics.md
    - mix.exs
    - .planning/phases/055-readme-exdoc-entry-paths/055-CONTEXT.md
  </read_first>
  <action>
    Create **`docs/ga-evidence.md`** with:

    1. First line: `# GA evidence & audit posture` (exact H1 string).
    2. A short introductory paragraph explaining this page is a **router** for **v1.4 GA** narrative (must contain substring **`router`**).
    3. A `## Executed vs Waived` subsection defining both terms in plain language (≤ 6 sentences total in that subsection).
    4. A `## Where to read next` subsection with **only** bullet links — **minimum 5 bullets**, including **all** of:
       - Tag-scoped **`https://github.com/sztheory/sigra/blob/v0.2.0/.planning/v1.4-GA-UAT.md`**
       - Tag-scoped **`https://github.com/sztheory/sigra/blob/v0.2.0/.planning/milestones/v1.4-REQUIREMENTS.md`**
       - `[UAT ↔ CI coverage](uat-ci-coverage.html)`
       - `[Audit semantics](audit-semantics.html)`
       - `[Maintaining & releasing](maintaining.html)`
       - `[Contributing](contributing.html)`
       - `[Security policy](security.html)`

    **Do not** include an HTML `<table>` or markdown pipe-table copying GA matrix rows.
  </action>
  <acceptance_criteria>
    - `test -f docs/ga-evidence.md` exits **0**.
    - `grep -nF '# GA evidence & audit posture' docs/ga-evidence.md` exits **0** and match is **line 1**.
    - `grep -F '## Executed vs Waived' docs/ga-evidence.md` exits **0**.
    - `grep -F '## Where to read next' docs/ga-evidence.md` exits **0**.
    - `grep -F 'https://github.com/sztheory/sigra/blob/v0.2.0/.planning/v1.4-GA-UAT.md' docs/ga-evidence.md` exits **0**.
    - `grep -F 'uat-ci-coverage.html' docs/ga-evidence.md` exits **0**.
    - `grep -F 'router' docs/ga-evidence.md` exits **0**.
    - `grep -E '\]\(\.planning/' docs/ga-evidence.md` exits **1**.
  </acceptance_criteria>
  <verify>
    `grep -Ei 'soc2|audit-certified' docs/ga-evidence.md` exits **1**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Register extras in mix.exs (uat-ci, ga-evidence, SECURITY)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - docs/ga-evidence.md
    - .planning/phases/055-readme-exdoc-entry-paths/055-CONTEXT.md
  </read_first>
  <action>
    In **`defp docs do`** → **`extras:`** list:

    1. If **`\"docs/uat-ci-coverage.md\"`** is **absent**, insert it immediately **after** **`\"docs/audit-semantics.md\"`** (**055-CONTEXT D-06**).

    2. If **`\"docs/ga-evidence.md\"`** is **absent**, insert it immediately **after** **`\"docs/uat-ci-coverage.md\"`** (or after **`audit-semantics`** if uat-ci was already present). **`ga-evidence`** line must be **after** **`audit-semantics`**.

    3. Add **`\"SECURITY.md\"`** to **`extras`** if not already present (after **`CONTRIBUTING.md`**). Extend **`Docs`** group regex:

    ```elixir
    Docs: ~r{^docs/|^SECURITY\.md$}
    ```

    **Do not** change **`main:`**, **`source_ref:`**, or **`source_url:`** values.
  </action>
  <acceptance_criteria>
    - `grep -nF '"docs/ga-evidence.md"' mix.exs` exits **0**.
    - `grep -nF '"docs/uat-ci-coverage.md"' mix.exs` exits **0**.
    - `grep -nF '"docs/audit-semantics.md"' mix.exs` exits **0** and the line number for **`audit-semantics`** is **less** than the line number for **`ga-evidence`**.
    - `grep -F 'main: "getting-started"' mix.exs` exits **0**.
    - `grep -F 'source_ref: "v#{@version}"' mix.exs` exits **0** OR the file still contains **`source_ref:`** with **`@version`** interpolation unchanged in meaning.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Getting started — Reading map callout</name>
  <files>guides/introduction/getting-started.md</files>
  <read_first>
    - guides/introduction/getting-started.md
    - docs/ga-evidence.md
    - SECURITY.md
    - mix.exs
  </read_first>
  <action>
    At the **top** of **`guides/introduction/getting-started.md`**, **before** the `# Getting Started` heading, insert a blockquote callout using **exact** first line:

    `> **Reading map:**`

    The callout (blockquote continues on following lines) **must** include **markdown links** using the **same flat `*.html` sibling pattern** as **`guides/introduction/installation.md`** (all guides + listed extras render under the project **`doc/`** root as **`*.html`**). Include **all** of:

    1. `[GA evidence & audit posture](ga-evidence.html)`
    2. `[Maintaining & releasing](maintaining.html)`
    3. `[Audit semantics](audit-semantics.html)`
    4. `[Contributing](contributing.html)`
    5. `[Security policy](security.html)` (**Plan 01** creates **`SECURITY.md`**; **Task 2** above registers it as an extra — this link must work in generated HTML).

    Keep the callout **≤ 8 lines** total including the `> **Reading map:**` line.
  </action>
  <acceptance_criteria>
    - `grep -nF '> **Reading map:**' guides/introduction/getting-started.md` exits **0** and the line number is **less than** `grep -nF '# Getting Started' guides/introduction/getting-started.md | head -1 | cut -d: -f1` (callout **before** the H1).
    - `grep -F 'ga-evidence.html' guides/introduction/getting-started.md` exits **0**.
    - `grep -F 'maintaining.html' guides/introduction/getting-started.md` exits **0**.
    - `grep -F 'audit-semantics.html' guides/introduction/getting-started.md` exits **0**.
    - `grep -F 'contributing.html' guides/introduction/getting-started.md` exits **0**.
    - `grep -F 'security.html' guides/introduction/getting-started.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `MIX_ENV=dev mix docs --warnings-as-errors` exits **0**.
- `mix compile --warnings-as-errors` exits **0**.
- Manual: from **`doc/getting-started.html`** (output of **`MIX_ENV=dev mix docs`**), click **GA evidence** → lands on **`ga-evidence.html`** without 404.
</verification>

<success_criteria>
- **DOC-02** satisfied: default **`mix docs`** landing has a **short** path to maintainer-facing GA / audit narrative via **hub + reading map**; **`main`** remains **getting-started**.
</success_criteria>
