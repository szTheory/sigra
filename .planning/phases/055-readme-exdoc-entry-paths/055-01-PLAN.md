---
phase: 55
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - SECURITY.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "**`README.md`** gains a **new** section **after** the **Security posture (headlines)** block (after the *map, not a spec* line) that explains **Executed vs Waived** in plain language and **does not** merge that narrative into the security posture table."
    - "**GA / milestone evidence** links intended to survive **HexDocs README rendering** use **tag-scoped GitHub URLs** (`https://github.com/sztheory/sigra/blob/v0.2.0/…`) for paths under **`.planning/`** — **no** bare `](.planning/` markdown links for those targets."
    - "A root **`SECURITY.md`** exists and describes **how to report security vulnerabilities** (coordinated disclosure), consistent with **055-CONTEXT D-14**."
  artifacts:
    - path: "README.md"
      provides: "DOC-01 integrator-facing GA posture map + HexDocs pointer"
    - path: "SECURITY.md"
      provides: "Coordinated disclosure policy for security issues"
nyquist_compliant: false
---

<objective>
Implement **DOC-01**: add **`SECURITY.md`** and a **README** **Production readiness & GA evidence** section (exact title may vary slightly) with **Executed vs Waived** framing and **tag-scoped** evidence links, per **`055-CONTEXT.md` D-01–D-07, D-11–D-17`** — without duplicating the GA matrix or violating Hex link rules.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-55-01 | Spoofing / misrepresentation | README implies **certified** GA or universal enablement of optional features | high | Forbid warranty-adjacent phrases; require *integration risk stays with host* sentence; grep list |
| T-55-02 | Tampering (documentation integrity) | **`.planning/`** relative links **404** from **hexdocs.pm** README | high | Grep rejects bare `](.planning/`; use **`v0.2.0`** blob URLs aligned with **`mix.exs`** `@version` |
| T-55-03 | Repudiation | No clear **coordinated disclosure** path for vulns | medium | Add **`SECURITY.md`**; link from new README section |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add SECURITY.md</name>
  <files>SECURITY.md</files>
  <read_first>
    - mix.exs
    - README.md
    - .planning/phases/055-readme-exdoc-entry-paths/055-CONTEXT.md
  </read_first>
  <action>
    Create **`SECURITY.md`** at the repository root (new file). It **must** include all of the following **case-sensitive substrings** (executor composes coherent prose around them):

    1. A first-line title: `# Security Policy`
    2. A `## Reporting a Vulnerability` heading
    3. The sentence (verbatim): `Please report security vulnerabilities via GitHub's private vulnerability reporting for this repository, not the public issue tracker.`
    4. A line containing **`https://github.com/sztheory/sigra/security/advisories`** as the primary reporting pointer (GitHub **Security** advisories area).
    5. A short expectation that maintainers will acknowledge **within a reasonable window** (executor picks wording; avoid SLAs that read like legal contracts).

    Do **not** paste the GA matrix, UAT tables, or installer flags into **`SECURITY.md`**.
  </action>
  <acceptance_criteria>
    - `test -f SECURITY.md` exits **0**.
    - `grep -nF '# Security Policy' SECURITY.md` exits **0** and the match is on **line 1**.
    - `grep -F '## Reporting a Vulnerability' SECURITY.md` exits **0**.
    - `grep -F 'Please report security vulnerabilities via GitHub' SECURITY.md` exits **0**.
    - `grep -F 'https://github.com/sztheory/sigra/security/advisories' SECURITY.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `mix compile --warnings-as-errors` exits **0** (sanity — no Elixir change expected).
  </verify>
</task>

<task type="execute">
  <name>Task 2: README — Production readiness & GA evidence block</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - mix.exs
    - SECURITY.md
    - .planning/phases/055-readme-exdoc-entry-paths/055-CONTEXT.md
  </read_first>
  <action>
    Immediately **after** the README paragraph that ends with **the map, not a spec.** (the closing line of the **Security posture (headlines)** section — currently *For threat-model detail… the README stays a map, not a spec.*), insert **two** newlines then a new section.

    **Section title:** Use a level-2 heading **`## Production readiness & GA evidence`** (exact string).

    **Body structure (mandatory elements — executor fills coherent paragraphs):**

    1. **Opening paragraph** must contain **all** of these substrings:
       - `Executed`
       - `Waived`
       - `host application`
       - `not a compliance certification`

    2. **Bullet list (≥ 4 bullets):** Each bullet is **either** a single navigational line **or** a factual scope line. Include **tag-scoped** links using **`https://github.com/sztheory/sigra/blob/v0.2.0/`** prefix for **each** of these paths (as its own bullet or inline in a bullet):
       - `.planning/milestones/v1.4-REQUIREMENTS.md`
       - `.planning/v1.4-GA-UAT.md`
       - `.planning/MILESTONES.md`

    3. **One bullet** (or sentence in a bullet) must link **`docs/uat-ci-coverage.md`** using a **repo-relative** markdown link acceptable on GitHub **and** ExDoc when README is an extra: `[docs/uat-ci-coverage.md](docs/uat-ci-coverage.md)` or path-only form that resolves in both contexts.

    4. **HexDocs bridge line:** Include a line containing **both** substrings: **`hexdocs.pm/sigra`** and **`ga-evidence`** (points readers to the packaged hub page path — e.g. `https://hexdocs.pm/sigra/0.2.0/ga-evidence.html`).

    5. **SECURITY pointer:** Include a markdown link to **`SECURITY.md`** using `[SECURITY.md](SECURITY.md)`.

    **Do not** add a `## [v1.4]` style semver heading. **Do not** paste the GA matrix table.
  </action>
  <acceptance_criteria>
    - `grep -nF '## Production readiness & GA evidence' README.md` exits **0** and the heading line number is **greater than** the line number for `grep -nF '## Security posture (headlines)' README.md | head -1`.
    - `grep -F 'https://github.com/sztheory/sigra/blob/v0.2.0/.planning/milestones/v1.4-REQUIREMENTS.md' README.md` exits **0**.
    - `grep -F 'https://github.com/sztheory/sigra/blob/v0.2.0/.planning/v1.4-GA-UAT.md' README.md` exits **0**.
    - `grep -F 'https://github.com/sztheory/sigra/blob/v0.2.0/.planning/MILESTONES.md' README.md` exits **0**.
    - `grep -iF 'docs/uat-ci-coverage.md' README.md` exits **0**.
    - `grep -iF 'hexdocs.pm/sigra' README.md` exits **0** AND `grep -iF 'ga-evidence' README.md` exits **0**.
    - `grep -F '[SECURITY.md](SECURITY.md)' README.md` exits **0**.
    - `grep -F 'not a compliance certification' README.md` exits **0**.
    - `grep -E '\]\(\.planning/' README.md` exits **1** (no bare `](.planning/` markdown links anywhere in README).
  </acceptance_criteria>
  <verify>
    `grep -Ei 'soc2|audit-certified|ga passed' README.md` exits **1** (forbidden warranty-adjacent tokens for this phase).
  </verify>
</task>

</tasks>

<verification>
- `grep -E '\]\(\.planning/' README.md` exits **1**.
- `MIX_ENV=dev mix docs --warnings-as-errors` exits **0** after **`README.md`** is listed as an extra (already true today — confirms no broken intra-doc links introduced by README edits).
- Spot-check: open **`README.md`** on GitHub preview — new section renders below security posture table, links are clickable.
</verification>

<success_criteria>
- **DOC-01** checklist in **`.planning/REQUIREMENTS.md`** is satisfiable: README carries honest **Executed / Waived** posture with **stable** evidence pointers; **`SECURITY.md`** exists for coordinated disclosure.
</success_criteria>
