---
phase: 75
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - guides/introduction/getting-started.md
  - MAINTAINING.md
  - CHANGELOG.md
autonomous: true
requirements:
  - TRN-02
must_haves:
  truths:
    - "**`getting-started.md`** **Faster path** includes **`[Upgrading notes — v1.12](upgrading-to-v1.12.html)`** adjacent to the existing **v1.11** upgrade link (**D-75-07**)."
    - "**`MAINTAINING.md`** contains a **v1.12 trust bundle** maintainer block with pointers to **`docs/uat-ci-coverage.md`** and **blob** URL for **`.planning/v1.12-UAT-EVIDENCE.md`** (**D-75-08**)."
    - "**`CHANGELOG.md`** **`[Unreleased]`** documents a **v1.12** trust-bundle bullet linking the **same three surfaces** as **75-01** (upgrade page, **`docs/uat-ci-coverage.md`**, evidence blob) — **no** eight-row table (**D-75-09**)."
    - "**`README.md`** is **unchanged** by this plan (**D-75-10**)."
  artifacts:
    - path: "guides/introduction/getting-started.md"
      provides: "Faster path v1.12 discovery (TRN-02)"
    - path: "MAINTAINING.md"
      provides: "Maintainer trust-bundle front door (TRN-02)"
    - path: "CHANGELOG.md"
      provides: "Unreleased trust-bundle bullet (TRN-02)"
nyquist_compliant: false
---

<objective>
Ship **TRN-02**: wire **v1.12** trust-bundle discovery on **`guides/introduction/getting-started.md`** (**Faster path**), add a **short maintainer block** to **`MAINTAINING.md`**, and add a **single** **`[Unreleased]`** documentation bullet to **`CHANGELOG.md`**, without touching **`README.md`** (**75-CONTEXT** **D-75-07**..**11**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-75-03 | Tampering | Stale or duplicate trust URLs drift from **75-01** / phase **74** | medium | Reuse the **exact** canonical **`https://github.com/sztheory/sigra/blob/main/.planning/v1.12-UAT-EVIDENCE.md`** string and **markdown** path **`docs/uat-ci-coverage.md`** (not renamed) |
| T-75-04 | Information disclosure | Pasting the eight-row matrix into **CHANGELOG** or **MAINTAINING** | low | **One sentence + links** per surface; forbid table rows in new prose |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Faster path v1.12 link in getting-started</name>
  <files>guides/introduction/getting-started.md</files>
  <read_first>
    - guides/introduction/getting-started.md
    - guides/introduction/upgrading-to-v1.12.md
    - .planning/phases/75-upgrade-continuity-triage-polish/75-CONTEXT.md
  </read_first>
  <action>
    In **`guides/introduction/getting-started.md`**, locate the **`**Faster path:**`** line (currently ends with **`[Upgrading notes — v1.11](upgrading-to-v1.11.html)`**).

    Append **` · [Upgrading notes — v1.12](upgrading-to-v1.12.html)`** **immediately after** the **v1.11** markdown link (same line, Interpunct **` · `** separator to match existing style).

    Do **not** add extra **Reading map** or **first-hour** prose unless you are executing an explicit follow-up outside this task (**D-75-07** default **skip**).
  </action>
  <acceptance_criteria>
    - `grep -nF '[Upgrading notes — v1.12](upgrading-to-v1.12.html)' guides/introduction/getting-started.md` exits **0**
    - `grep -nF 'upgrading-to-v1.11.html' guides/introduction/getting-started.md` exits **0**
  </acceptance_criteria>
  <verify>
    `mix docs --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 2: MAINTAINING v1.12 trust-bundle block</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - .planning/phases/75-upgrade-continuity-triage-polish/75-CONTEXT.md
  </read_first>
  <action>
    After the **`## Milestone cadence and pause (v1.11+)`** section (after its body — insert **before** **`## Installer golden CI contract (phase 50)`**), insert **one blank line** and this **exact heading + body**:

    ```markdown
    ## v1.12 trust bundle (audit + UAT evidence)

    **v1.12** packages the **bounded SEED-002 audit closure** narrative, the **eight-row GA·UAT outcome index**, and the **machine vs residual** CI catalog into a small set of stable links. Do **not** fork the eight-row matrix into **Hex-facing** guides — treat **[`docs/uat-ci-coverage.md`](docs/uat-ci-coverage.md)** as the catalog and **[`v1.12-UAT-EVIDENCE.md` on `main`](https://github.com/sztheory/sigra/blob/main/.planning/v1.12-UAT-EVIDENCE.md)** as the milestone outcome index.

    **Release ritual:** when cutting a **minor** or **major**, confirm **`CHANGELOG.md`**, **`guides/introduction/upgrading-to-v1.12.md`**, and the two URLs above still agree (no renamed paths, no duplicated tables).
    ```

    Adjust **only** if a merge conflict forces it — preserve the **two** URLs verbatim.
  </action>
  <acceptance_criteria>
    - `grep -nF '## v1.12 trust bundle (audit + UAT evidence)' MAINTAINING.md` exits **0**
    - `grep -nF 'https://github.com/sztheory/sigra/blob/main/.planning/v1.12-UAT-EVIDENCE.md' MAINTAINING.md` exits **0**
    - `grep -nF 'docs/uat-ci-coverage.md' MAINTAINING.md` exits **0**
  </acceptance_criteria>
  <verify>
    `mix docs --warnings-as-errors` exits **0**
  </verify>
</task>

<task type="execute">
  <name>Task 3: CHANGELOG Unreleased v1.12 bullet</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - guides/introduction/upgrading-to-v1.12.md
    - .planning/phases/75-upgrade-continuity-triage-polish/75-CONTEXT.md
  </read_first>
  <action>
    Under **`## [Unreleased]`** → **`### Documentation`**, add **one** new bullet **after** the existing **v1.11** bullet (keep the **v1.11** bullet intact). Use this **exact** bullet text:

    `* **planning:** **v1.12** trust bundle — [upgrading-to-v1.12.md](guides/introduction/upgrading-to-v1.12.md) (ExDoc extra), [docs/uat-ci-coverage.md](docs/uat-ci-coverage.md) (**v1.12 launch evidence** subsection), and [milestone UAT index](https://github.com/sztheory/sigra/blob/main/.planning/v1.12-UAT-EVIDENCE.md) on GitHub (**TRN-01**..**TRN-03** doc polish).`

    Do **not** add a markdown table or row-wise SEED enumeration.
  </action>
  <acceptance_criteria>
    - `grep -nF '**v1.12** trust bundle' CHANGELOG.md` exits **0**
    - `grep -nF 'v1.12-UAT-EVIDENCE.md' CHANGELOG.md` exits **0**
    - `grep -nF 'docs/uat-ci-coverage.md' CHANGELOG.md` exits **0**
  </acceptance_criteria>
  <verify>
    `mix docs --warnings-as-errors` exits **0**
  </verify>
</task>

</tasks>
