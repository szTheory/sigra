---
phase: 40
plan: 01
type: execute
wave: 1
depends_on:
  - 2
files_modified:
  - scripts/maintainers/planning-audit-hygiene.sh
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/phases/26-retroactive-v1-1-verification-closeout/26-01-PLAN.md
autonomous: true
requirements:
  - TOOL-01
must_haves:
  truths:
    - "`gsd-tools audit-open --json` formally deprecated in live planning narrative; supported path is MAINTAINING.md + optional scripts/maintainers helper (D-40-01..D-40-05)."
    - "MILESTONES / PROJECT / phase-26 plan template carry supersession pointers, not contributor-breaking instructions."
  artifacts:
    - path: "scripts/maintainers/planning-audit-hygiene.sh"
      provides: "Optional repo-owned hygiene helper (bash only)"
nyquist_compliant: true
---

<objective>
Deliver **TOOL-01**: formally **deprecate** **`gsd-tools audit-open --json`** in **live** `.planning` surfaces (**`MILESTONES.md`**, **`PROJECT.md`**), add **supersession** note to **`26-01-PLAN.md`** (active template), and add optional **`scripts/maintainers/planning-audit-hygiene.sh`** — all pointers reference **`MAINTAINING.md`** § **Planning hygiene** (created in Plan 02).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-40-03 | Denial of service / confusion | Contributors directed to broken Node tool | medium | No changes to `CONTRIBUTING.md` in this plan; script lives under **`scripts/maintainers/`** only |
| T-40-04 | Repudiation | Planning audits skipped with no replacement | low | Explicit **`MAINTAINING.md`** cross-links + executable helper echoing `rg` steps |
</threat_model>

<context>
@.planning/phases/40-tooling-release-ergonomics/40-CONTEXT.md
@.planning/phases/40-tooling-release-ergonomics/40-RESEARCH.md
@.planning/phases/40-tooling-release-ergonomics/40-PATTERNS.md
@MAINTAINING.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add scripts/maintainers/planning-audit-hygiene.sh</name>
  <files>scripts/maintainers/planning-audit-hygiene.sh</files>
  <read_first>
    - scripts/ci/milestone-verification-gate.sh
    - MAINTAINING.md
  </read_first>
  <action>
    1. Create directory **`scripts/maintainers/`** if missing.
    2. Add **`planning-audit-hygiene.sh`** with **`#!/usr/bin/env bash`**, **`set -euo pipefail`**, and behavior:
       - Print a short banner stating **`gsd-tools audit-open --json` is not a supported path** for Sigra; point readers to **`MAINTAINING.md`** section **`## Planning hygiene (without gsd-tools JSON)`**.
       - Echo at least **two** concrete **`rg` or `find`** commands useful from repo root (e.g. phases missing `VERIFICATION.md`, or listing `*-CONTEXT.md` without matching `*-VERIFICATION.md` — align commands with what **`MAINTAINING.md`** promises).
    3. **`chmod +x`** the script (git mode `100755`).
    4. Do **not** invoke **`node`** or **`gsd-tools`** inside the script.
  </action>
  <acceptance_criteria>
    - `test -f scripts/maintainers/planning-audit-hygiene.sh` exits 0.
    - `head -1 scripts/maintainers/planning-audit-hygiene.sh | grep -q "#!/usr/bin/env bash"` exits 0.
    - `grep -q "MAINTAINING.md" scripts/maintainers/planning-audit-hygiene.sh` exits 0.
    - `grep -q "rg " scripts/maintainers/planning-audit-hygiene.sh` exits 0 OR `grep -q "find " scripts/maintainers/planning-audit-hygiene.sh` exits 0 (at least one search command echoed).
    - `! grep -qE "gsd-tools|node " scripts/maintainers/planning-audit-hygiene.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -x scripts/maintainers/planning-audit-hygiene.sh 2>/dev/null || test -f scripts/maintainers/planning-audit-hygiene.sh'</automated>
  </verify>
  <done>Optional bash helper documents supported planning hygiene path.</done>
</task>

<task type="execute">
  <name>Task 2: Supersede audit-open narrative in MILESTONES and PROJECT</name>
  <files>.planning/MILESTONES.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/MILESTONES.md
    - .planning/PROJECT.md
    - MAINTAINING.md
  </read_first>
  <action>
    1. In **`.planning/MILESTONES.md`**, in **Tech Debt Carried Forward** (v1.2 section, bullet referencing **`gsd-tools audit-open --json`** ~line 83), **replace** that bullet with text that states the JSON helper is **deprecated for Sigra maintainers** and the **supported path** is **`MAINTAINING.md`** section **`## Planning hygiene (without gsd-tools JSON)`** plus optional **`scripts/maintainers/planning-audit-hygiene.sh`**.
    2. In **`.planning/PROJECT.md`**, update the **TOOL-01** bullet to match: deprecated reliance; pointer to **`MAINTAINING.md`** (same section anchor); optional script path.
  </action>
  <acceptance_criteria>
    - `grep -q "MAINTAINING.md" .planning/MILESTONES.md` exits 0.
    - `grep -q "Planning hygiene" .planning/MILESTONES.md` exits 0.
    - `grep -q "MAINTAINING.md" .planning/PROJECT.md` exits 0.
    - `grep -qi "deprecated" .planning/MILESTONES.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "MAINTAINING.md" .planning/MILESTONES.md && grep -qi "deprecated" .planning/MILESTONES.md'</automated>
  </verify>
  <done>Live milestone and project state describe TOOL-01 closure posture with MAINTAINING pointer.</done>
</task>

<task type="execute">
  <name>Task 3: Supersession note in 26-01-PLAN.md</name>
  <files>.planning/phases/26-retroactive-v1-1-verification-closeout/26-01-PLAN.md</files>
  <read_first>
    - .planning/phases/26-retroactive-v1-1-verification-closeout/26-01-PLAN.md
    - MAINTAINING.md
  </read_first>
  <action>
    1. Near the **existing HTML comment** that mentions **`gsd-tools.cjs` audit-open** (approximately line 131), add an adjacent **HTML comment** (or extend the existing one) containing **exact substring** **`Phase 40 supersession:`** and a pointer to **`MAINTAINING.md`** **`## Planning hygiene (without gsd-tools JSON)`** — historical steps may remain for archive honesty, but the new comment must state **`audit-open --json` is deprecated** for current maintainers.
    2. Do **not** delete the full historical procedure; **append** supersession guidance.
  </action>
  <acceptance_criteria>
    - `grep -q "Phase 40 supersession:" .planning/phases/26-retroactive-v1-1-verification-closeout/26-01-PLAN.md` exits 0.
    - `grep -q "MAINTAINING.md" .planning/phases/26-retroactive-v1-1-verification-closeout/26-01-PLAN.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "Phase 40 supersession:" .planning/phases/26-retroactive-v1-1-verification-closeout/26-01-PLAN.md'</automated>
  </verify>
  <done>Phase 26 template carries explicit Phase 40 supersession pointer.</done>
</task>

</tasks>

<verification>
1. `bash scripts/maintainers/planning-audit-hygiene.sh` exits 0 (script runs without error).
2. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test` exits 0.
3. `grep -R "audit-open --json" .planning --include='*.md' | wc -l` — acceptable if count is **only** historical VERIFICATION archives + updated supersession text; executor confirms **no new** “run this command” instructions without deprecation context in **MILESTONES/PROJECT/26-01**.
</verification>

<success_criteria>
- TOOL-01 satisfied: documented supported path; broken tool not presented as gate; optional bash helper only.
- Plan 02 merged first so **`MAINTAINING.md`** exists before narrative updates.
</success_criteria>

## PLANNING COMPLETE
