---
phase: 40
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
  - README.md
  - CONTRIBUTING.md
  - mix.exs
  - .github/workflows/hex-publish.yml
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "Root MAINTAINING.md documents Hex + GitHub release with ordered checklist, semver rules, and planning hygiene without gsd-tools JSON (D-40-06..D-40-20)."
    - "Optional hex-publish workflow is workflow_dispatch-only with HEX_API_KEY scoped to publish; actions SHA-pinned like ci.yml (D-40-11..D-40-13)."
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Maintainer shipping runbook"
    - path: ".github/workflows/hex-publish.yml"
      provides: "Optional CI-attested publish path"
nyquist_compliant: true
---

<objective>
Deliver **REL-01**: root **`MAINTAINING.md`** (Hex + GitHub release checklist, semver policy, optional GitHub Environment note), cross-links from **`README.md`** and **`CONTRIBUTING.md`**, **`mix.exs`** ExDoc extra, and **optional** **`.github/workflows/hex-publish.yml`** (`workflow_dispatch` only, tests before publish, secrets isolated).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-40-01 | Information disclosure | `HEX_API_KEY` leaked via overly broad job `env` or wrong workflow | high | Single workflow file; `secrets.HEX_API_KEY` only on step/job that runs `mix hex.publish`; never add to `ci.yml` |
| T-40-02 | Tampering | Unpinned third-party Actions in publish path | high | Pin `actions/checkout` and `erlef/setup-beam` to same SHAs as `.github/workflows/ci.yml` (lines 25 and 71) |
</threat_model>

<context>
@.planning/phases/40-tooling-release-ergonomics/40-CONTEXT.md
@.planning/phases/40-tooling-release-ergonomics/40-RESEARCH.md
@.planning/phases/40-tooling-release-ergonomics/40-PATTERNS.md
@.planning/REQUIREMENTS.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="execute">
  <name>Task 1: Author MAINTAINING.md</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - .planning/phases/40-tooling-release-ergonomics/40-CONTEXT.md
    - mix.exs
    - CHANGELOG.md
    - .planning/MILESTONES.md
  </read_first>
  <action>
    1. Create **`MAINTAINING.md`** at repo root.
    2. First sections (order flexible but must include the following **verbatim subsection title** for downstream Plan 40-01 links: **`## Planning hygiene (without gsd-tools JSON)`**):
       - Short intro: who this doc is for (maintainers), link to **`CONTRIBUTING.md`** for PR quality gates.
       - **Ordered checklist (minimum 12 numbered steps)** including copy-paste commands, covering at least: confirm **`mix.exs`** `@version`; update **`CHANGELOG.md`**; green **`PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test`**; **`git tag v&lt;version&gt;`** after version bump; **`mix hex.publish`** (document **`--yes`** for CI); create **GitHub Release** from tag; verify Hex badge / docs **`source_ref`** alignment.
       - **Semver subsection** stating explicitly: next Hex publish that includes **`Sigra.Audit.Assertions`** (or any new supported public **`lib/`** API since last Hex publish) must bump **at least `0.2.0`** if prior publish was **`0.1.0`** (D-40-17..D-40-18); do **not** jump to **`1.0.0`** without explicit stability messaging (D-40-19).
       - Under **`## Planning hygiene (without gsd-tools JSON)`**: state **`gsd-tools audit-open --json` is deprecated** for this repo; instruct maintainers to use **manual `rg`/`find`** over `.planning/phases/` (give 1–2 concrete example commands, e.g. list phase dirs missing `*-VERIFICATION.md`, or phases with `nyquist_compliant: false` in frontmatter — exact commands are your choice but must be runnable from repo root).
       - Optional GitHub **Environment** named **`hex`** with required reviewers — one short paragraph (D-40-14).
       - Link Hex publish docs: **`https://hex.pm/docs/publish`** once.
    3. Do **not** place **`HEX_API_KEY`** setup instructions inside **`CONTRIBUTING.md`** — only here or in workflow comment.
  </action>
  <acceptance_criteria>
    - `test -f MAINTAINING.md` exits 0.
    - `grep -q "## Planning hygiene (without gsd-tools JSON)" MAINTAINING.md` exits 0.
    - `grep -q "0.2.0" MAINTAINING.md` exits 0.
    - `grep -q "mix hex.publish" MAINTAINING.md` exits 0.
    - `grep -q "https://hex.pm/docs/publish" MAINTAINING.md` exits 0.
    - `grep -q "CONTRIBUTING.md" MAINTAINING.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f MAINTAINING.md && grep -q "Planning hygiene" MAINTAINING.md && grep -q "0.2.0" MAINTAINING.md'</automated>
  </verify>
  <done>MAINTAINING.md exists with checklist, semver guidance, and planning hygiene section.</done>
</task>

<task type="execute">
  <name>Task 2: Cross-link README and CONTRIBUTING</name>
  <files>README.md, CONTRIBUTING.md</files>
  <read_first>
    - README.md
    - CONTRIBUTING.md
    - MAINTAINING.md
  </read_first>
  <action>
    1. In **`README.md`**, add **one sentence** in the meta/contributing area (near existing **`CONTRIBUTING.md`** link in **Pick your lane** table or immediately below) pointing maintainers to **`MAINTAINING.md`** for Hex/GitHub releases.
    2. In **`CONTRIBUTING.md`**, after the opening **Developing** section (or end of file if cleaner), add **≤3 sentences** pointing release maintainers to **`MAINTAINING.md`**; explicitly state **`mix test` / CI does not require Node or `gsd-tools`** (D-40-04).
  </action>
  <acceptance_criteria>
    - `grep -q "MAINTAINING.md" README.md` exits 0.
    - `grep -q "MAINTAINING.md" CONTRIBUTING.md` exits 0.
    - `! grep -q "gsd-tools" CONTRIBUTING.md` exits 0 (contributor doc must not mention `gsd-tools`).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "MAINTAINING.md" README.md && grep -q "MAINTAINING.md" CONTRIBUTING.md && ! grep -q "gsd-tools" CONTRIBUTING.md'</automated>
  </verify>
  <done>README and CONTRIBUTING reference MAINTAINING without introducing gsd-tools to contributor path.</done>
</task>

<task type="execute">
  <name>Task 3: ExDoc extra for MAINTAINING.md</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - MAINTAINING.md
  </read_first>
  <action>
    1. In **`defp docs do` → `extras:`**, insert **`"MAINTAINING.md"`** immediately after **`"CONTRIBUTING.md"`** in the list.
  </action>
  <acceptance_criteria>
    - `grep -A30 'extras:' mix.exs | grep -q "MAINTAINING.md"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -A40 "extras:" mix.exs | grep -q "\"MAINTAINING.md\""'</automated>
  </verify>
  <done>MAINTAINING.md is included in ExDoc extras.</done>
</task>

<task type="execute">
  <name>Task 4: Optional hex-publish workflow</name>
  <files>.github/workflows/hex-publish.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/40-tooling-release-ergonomics/40-CONTEXT.md
  </read_first>
  <action>
    1. Create **`.github/workflows/hex-publish.yml`** with **only** trigger:
       ```yaml
       on:
         workflow_dispatch:
       ```
       (no `push` / `pull_request`.)
    2. Set **`permissions: contents: read`** at workflow level; add job-level overrides only if required for publish (default read is OK for checkout + publish).
    3. Single job (e.g. **`publish`**): steps must use **`actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd`** and **`erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93`** with **`version-file: .tool-versions`**, **`version-type: strict`** — same pins/params as **`ci.yml`** library job.
    4. Run **`mix deps.get`** then **`mix compile`** then full test with Postgres service **matching `ci.yml` `library_tests` job** (postgres:15, `POSTGRES_DB: sigra_test`, `POSTGRES_PASSWORD: postgres`, port 5432, healthcheck) OR invoke a documented subset — **preferred:** mirror **`library_tests`** service block so **`mix test`** is honest.
    5. Final step: **`mix hex.publish --yes`** with **`env: HEX_API_KEY: ${{ secrets.HEX_API_KEY }}`** scoped **only** to that step (or job `env` if single step job — job-level OK **only** if job does nothing else sensitive).
    6. File header comment: maintainers must configure repo secret **`HEX_API_KEY`**; workflow is optional and manual-dispatch only.
  </action>
  <acceptance_criteria>
    - `grep -q "workflow_dispatch" .github/workflows/hex-publish.yml` exits 0.
    - `! grep -q "pull_request" .github/workflows/hex-publish.yml` exits 0.
    - `grep -q "de0fac2e4500dabe0009e67214ff5f5447ce83dd" .github/workflows/hex-publish.yml` exits 0.
    - `grep -q "fc68ffb90438ef2936bbb3251622353b3dcb2f93" .github/workflows/hex-publish.yml` exits 0.
    - `grep -q "HEX_API_KEY" .github/workflows/hex-publish.yml` exits 0.
    - `grep -q "mix hex.publish" .github/workflows/hex-publish.yml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'f=.github/workflows/hex-publish.yml; test -f "$f" && grep -q workflow_dispatch "$f" && ! grep -q pull_request "$f" && grep -q de0fac2e4500dabe0009e67214ff5f5447ce83dd "$f"'</automated>
  </verify>
  <done>Optional dispatch-only Hex publish workflow added with pinned actions and isolated secret.</done>
</task>

</tasks>

<verification>
1. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test` exits 0 (docs-only change should not break tests).
2. `bash -lc 'grep -q "MAINTAINING.md" README.md CONTRIBUTING.md mix.exs'`
3. YAML: `workflow_dispatch` present; `pull_request` absent in `hex-publish.yml`.
</verification>

<success_criteria>
- REL-01 satisfied: maintainer checklist + optional publish workflow pattern; contributor docs unchanged in privilege posture.
- `mix.exs` docs list includes `MAINTAINING.md`.
</success_criteria>

## PLANNING COMPLETE
