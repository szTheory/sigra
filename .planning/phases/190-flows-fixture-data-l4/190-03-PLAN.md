---
phase: 190-flows-fixture-data-l4
plan: 03
type: execute
wave: 2
depends_on: ["190-01", "190-02"]
files_modified:
  - test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, DATA-01]
must_haves:
  truths:
    - "Platform admin JTBD journey: overview → users search → user detail → audit → return with scope/breadcrumb reconstructed"
    - "Happy case: alice (confirmed Acme member) — user detail renders, audit events visible"
    - "Main-error case: dave (locked + unconfirmed) — user detail shows locked/unconfirmed state, auth.login.failure + auth.lockout.start audit events visible"
    - "Boundary case: frank (scheduled-deletion) visible; empty search filter renders empty/no-data state without sg-empty-state rendering as error"
    - "FLOW-02: Tab through interactive elements produces visible focus; confirm dialog focus-containment invariant holds; Escape closes dialog and returns focus to trigger ([data-sg-confirm-cancel] used internally by WR-01)"
    - "FLOW-02: test.use({ reducedMotion: 'reduce' }) set at describe-block level; assertReducedMotionEffect() confirms CSS collapsed effect"
    - "FLOW-03: theme persists across LiveView navigation + page.reload() + system flip; seedThemeAndAssertNoFlash confirms no-flash on first paint"
    - "No sleeps; all assertions use web-first auto-retrying expect()"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts"
      provides: "Platform admin JTBD flow spec: happy/error/boundary + keyboard + reduced-motion + theme"
      min_lines: 120
  key_links:
    - from: "admin-flow-platform-admin.spec.ts"
      to: "helpers/adminFlows.ts"
      via: "loginDemoAdmin, waitForLiveViewReady, assertScopeChrome, assertThemeAttributes, assertReducedMotionEffect, seedThemeAndAssertNoFlash"
      pattern: "adminFlows"
    - from: "admin-flow-platform-admin.spec.ts"
      to: "/admin → /admin/users → /admin/users/:id → /admin/users/:id/audit"
      via: "page.goto + LiveView navigation"
      pattern: "waitForLiveViewReady"
---

<objective>
Author the platform admin JTBD flow spec covering happy + main-error + boundary cases with full keyboard, reduced-motion, and theme-persistence assertions — the first of three L4 flow specs proving FLOW-01/02/03/DATA-01 for the platform admin operator persona.

Purpose: Platform admin is the foundational operator journey (global posture, overview → users → detail → audit → return). This spec establishes the template structure and import conventions the investigator and org-admin specs follow.

Output: admin-flow-platform-admin.spec.ts with 3 describe blocks (happy/error/boundary) + a theme-persistence describe block.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-flows-fixture-data-l4/190-CONTEXT.md
@.planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md
@.planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md
@.planning/phases/190-flows-fixture-data-l4/190-UI-SPEC.md
@guides/reference/admin-ui-principles.md
@guides/reference/admin-design-contract.md

<interfaces>
<!-- Import block (from impersonation.spec.ts:1-3 pattern): -->
<!--   import { test, expect } from '@playwright/test'; -->
<!--   import { adminUsersEmailLocator } from '../helpers/adminUsersIndex'; -->
<!--   import { waitForLiveViewReady, loginDemoAdmin, assertScopeChrome, -->
<!--            seedThemeAndAssertNoFlash, assertThemeAttributes, assertReducedMotionEffect -->
<!--          } from '../helpers/adminFlows'; -->
<!--  -->
<!-- Demo persona emails + passwords (from 190-PATTERNS.md): -->
<!--   DEMO_ADMIN_EMAIL = 'admin@demo.vaultr.test' -->
<!--   DEMO_ALICE_EMAIL = 'alice@demo.vaultr.test' -->
<!--   DEMO_DAVE_EMAIL = 'dave@demo.vaultr.test' -->
<!--   DEMO_FRANK_EMAIL = 'frank@demo.vaultr.test' -->
<!--  -->
<!-- Seeded audit states (from 190-RESEARCH.md): -->
<!--   dave: 2 audit events — auth.login.failure + auth.lockout.start -->
<!--   frank: 1 audit event — account.deletion.schedule -->
<!--   alice: 3 audit events (login + 2 impersonation rows) -->
<!--  -->
<!-- URL patterns: -->
<!--   /admin (IndexLive: global overview) -->
<!--   /admin/users (UsersIndexLive: global list with ?q= scope) -->
<!--   /admin/users/:id (UserShowLive: user detail + confirm dialog) -->
<!--   /admin/users/:id/audit (AuditUserLive: per-user audit) -->
<!--  -->
<!-- Selector patterns (from 190-PATTERNS.md): -->
<!--   adminUsersEmailLocator(page, email) — desktop/mobile row locator -->
<!--   page.fill('input[name="q"]', email) — search input -->
<!--   page.click('button:has-text("Search")') — search submit -->
<!--   page.getByRole('navigation', { name: 'Breadcrumb' }) — breadcrumb nav -->
<!--   page.getByRole('link', { name: 'Users' }).toHaveAttribute('href', /return_to|\/admin\/users\?/) -->
<!--   page.locator('.sg-admin-loading-bar') for assertReducedMotionEffect -->
<!--  -->
<!-- Copy to assert (RESEARCH.md: session revoke short form is actual): -->
<!--   "Session revoked." — actual string from user_show_live.ex:81 -->
<!--   NOT the full brand-book string (gap routed to Phase 191) -->
<!--  -->
<!-- Keyboard pattern (from 190-PATTERNS.md): -->
<!--   trigger.focus() → keyboard.press('Enter') → assert dialog visible -->
<!--   assert containment: dialog.contains(document.activeElement) == true -->
<!--   keyboard.press('Escape') → assert dialog hidden → assert trigger.toBeFocused() -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author admin-flow-platform-admin.spec.ts — full platform admin JTBD flow spec</name>
  <files>test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts</files>
  <behavior>
    - test.use({ reducedMotion: 'reduce' }) set at top-level describe block (not per-page)
    - Happy path: loginDemoAdmin → /admin → assertScopeChrome('Global') → /admin/users → search alice → assertScopeChrome('Global') → open user row → waitForLiveViewReady → breadcrumb back-link href matches /admin/users?.*q= → navigate to /admin/users/alice/audit → assertScopeChrome persists → return via breadcrumb → URL has ?q= param
    - Main-error path: search dave → open user detail → assertScopeChrome → see locked/unconfirmed state indicators → navigate to dave audit → audit list shows auth.login.failure or auth.lockout.start event text
    - Boundary path: search frank → open user detail → see scheduled-deletion indicator; also search for a term with no results → page shows empty state without error styling
    - Keyboard: navigate to a user detail → Tab to the "Revoke all sessions" or session revoke button → keyboard.press('Enter') to open confirm dialog → assert focus inside .sg-confirm-dialog (containment invariant) → keyboard.press('Escape') → assert dialog is hidden → assert trigger element toBeFocused()
    - Reduced-motion: assertReducedMotionEffect() called after first page load (checks loading bar animation is 'none')
    - Theme: seedThemeAndAssertNoFlash(page, 'dark') → navigate to /admin/users → assertThemeAttributes(page, 'dark') → page.reload() → waitForLiveViewReady → assertThemeAttributes(page, 'dark') → emulateMedia({ colorScheme: 'light' }) → assertThemeAttributes still shows 'dark' (explicit choice overrides system) → page.evaluate to localStorage.setItem('sigra.admin.theme', 'system') → page.reload → html should NOT have data-sg-admin-theme (system mode removes override)
  </behavior>
  <read_first>
    - test/example/priv/playwright/tests/admin-user-operations.spec.ts (full file — analog for platform admin journey: search pattern, breadcrumb pattern, scope chrome pattern)
    - test/example/priv/playwright/tests/admin-theme.spec.ts (lines 360-485 — addInitScript no-flash pattern; assertThemeAttributes patterns; reload gap noted in RESEARCH)
    - test/example/priv/playwright/tests/admin-modal-interaction.spec.ts (lines 93-182 — ConfirmDialog keyboard gates: focus containment, Escape, focus return to trigger)
    - test/example/priv/playwright/helpers/adminFlows.ts (the helper module just created — use its exports)
    - test/example/priv/playwright/helpers/adminUsersIndex.ts (adminUsersEmailLocator — use for user row selection)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (admin-flow-platform-admin.spec.ts section with full code patterns)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (copy mismatch pitfall: assert "Session revoked." not the full brand-book string; reduced-motion guard notes)
    - lib/sigra/admin/live/user_show_live.ex (lines 315-335 — confirm dialog markup; line 81 — "Session revoked." flash string)
  </read_first>
  <action>
    Create test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts.

    File structure: a single `test.describe('Phase 190 platform admin flow (FLOW-01..03, DATA-01)', ...)` block containing:
    - `test.use({ reducedMotion: 'reduce' })` — at describe-block level, before any test
    - `test.beforeEach(...)` — loginDemoAdmin + waitForLiveViewReady after navigation to /admin
    - `test.describe('happy path — alice', ...)` with one test covering the full overview→users→detail→audit→return journey
    - `test.describe('main-error — dave locked/unconfirmed', ...)` with one test
    - `test.describe('boundary — frank scheduled-deletion + empty filter', ...)` with one test
    - `test.describe('keyboard operability (FLOW-02)', ...)` with one test
    - `test.describe('theme persistence (FLOW-03)', ...)` — uses addInitScript + seedThemeAndAssertNoFlash; this describe block does NOT use beforeEach (seeds theme before goto, not after)

    Concrete identifiers:
    - Search input selector: `input[name="q"]`
    - Search submit: `button:has-text("Search")` or `button[type="submit"]`
    - User row locator: `adminUsersEmailLocator(page, email)` from adminUsersIndex helper
    - "Open user" link: `page.getByRole('link', { name: 'Open user' }).first()` or the user-row link
    - Breadcrumb: `page.getByRole('navigation', { name: 'Breadcrumb' })` → link with href matching `/admin/users`
    - Scope chrome: `assertScopeChrome(page, 'Global')` from adminFlows helper
    - Confirm dialog: `.sg-confirm-overlay` (overlay) / `.sg-confirm-dialog` (dialog)
    - Trigger for keyboard test: the "Revoke session" or "Revoke all sessions" button (check user_show_live.ex for exact label)
    - Flash/notice: `page.getByRole('alert')` or `.sg-notice` containing 'Session revoked.'
    - Loading bar: `.sg-admin-loading-bar` for assertReducedMotionEffect
    - Empty state: `page.locator('.sg-empty-state')` or `page.getByRole('heading', { name: /no/i })`

    Theme test: Use `seedThemeAndAssertNoFlash(page, 'dark')` which calls addInitScript before goto('/admin'). After assert, call assertThemeAttributes(page, 'dark'). Navigate to /admin/users, assertThemeAttributes(page, 'dark'). Reload, waitForLiveViewReady, assertThemeAttributes(page, 'dark'). Assert system flip: page.evaluate to set localStorage to 'system', page.reload, waitForLiveViewReady, then html should NOT have data-sg-admin-theme.

    Assertions: web-first auto-retrying `expect(locator).toHaveAttribute(...)`, `expect(locator).toBeVisible()`, `expect(locator).toBeFocused()`. No sleeps. No toHaveCSS for theme color assertions (at most one toHaveCSS smoke to confirm tokens wire through).

    Copy assertion for "Session revoked.": assert the actual short-form string `"Session revoked."` — NOT the full brand-book string (RESEARCH pitfall #3; copy gap routed to Phase 191 per D-13).
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test admin-flow-platform-admin --project=chromium 2>&1 | tail -15</automated>
    <automated>cd test/example/priv/playwright && npx playwright test admin-flow-platform-admin --project=mobile --list 2>&1 | grep -c "admin-flow-platform-admin" | grep "^0$" && echo "MOBILE EXCLUSION OK"</automated>
  </verify>
  <done>
    - admin-flow-platform-admin.spec.ts exists with test.use({ reducedMotion: 'reduce' })
    - `npx playwright test admin-flow-platform-admin --project=chromium` exits 0 (all tests pass)
    - `npx playwright test admin-flow-platform-admin --project=mobile --list` shows 0 tests (mobile exclusion working)
    - Spec covers happy (alice) + main-error (dave) + boundary (frank + empty filter) + keyboard + theme-persistence describe blocks
    - No sleeps, no page.emulateMedia after goto, no computed color assertions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright spec → dev server | Flow spec sends requests as demo personas; tests admin-scoped routes |
| Admin session → admin routes | Platform admin persona has global scope; spec confirms scope ribbon shows 'Global' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-07 | Information Disclosure | Session revoke copy assertion | accept | Spec asserts "Session revoked." (short form, actual) not the full string; copy gap logged for Phase 191; no PII leak risk in test |
| T-190-08 | Elevation of Privilege | Platform admin scope assertions | mitigate | Spec asserts scope ribbon shows 'Global' — verifies global scope is actually present; prevents scope confusion regressions |
| T-190-09 | Denial of Service | Empty state not rendering as error | accept | Boundary test asserts .sg-empty-state renders without error styling; confirms empty is not broken (D-13 brand-voice rule: empty ≠ broken) |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No new packages; existing @playwright/test + helpers only |
</threat_model>

<verification>
- `cd test/example/priv/playwright && npx playwright test admin-flow-platform-admin --project=chromium` exits 0
- Test count ≥ 5 (happy + error + boundary + keyboard + theme each have ≥ 1 test)
- `npx playwright test --project=mobile --list 2>&1 | grep admin-flow-platform-admin | wc -l` → 0
- No `page.waitForTimeout` calls in the spec file
- `grep -c 'test.use.*reducedMotion' test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts` → 1
</verification>

<success_criteria>
1. admin-flow-platform-admin.spec.ts passes all tests on the chromium project with no mobile leakage.
2. Covers: overview → users → detail → audit → return (FLOW-01); Tab/Enter/Escape confirm dialog keyboard (FLOW-02); reducedMotion at context level + CSS effect collapsed (FLOW-02); dark theme persists across nav + reload + system mode (FLOW-03); deterministic fixture (alice/dave/frank) drives all cases (DATA-01).
3. No sleeps, computed-color assertions, or per-page emulateMedia after goto.
4. Copy assertion uses "Session revoked." (actual, not brand-book full string).
</success_criteria>

<output>
Create `.planning/phases/190-flows-fixture-data-l4/190-03-SUMMARY.md` when done
</output>
