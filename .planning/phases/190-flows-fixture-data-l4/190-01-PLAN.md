---
phase: 190-flows-fixture-data-l4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/admin_hooks.js
  - test/example/assets/js/admin_hooks.js
  - lib/sigra/admin/live/user_show_live.ex
  - lib/sigra/admin/live/branding_live.ex
autonomous: true
requirements: [FLOW-01, FLOW-02]
must_haves:
  truths:
    - "The ConfirmDialog Cancel button is targeted by an explicit [data-sg-confirm-cancel] selector, not the positional focusables[0] (WR-01)"
    - "The ConfirmDialog focus-return in destroyed() falls back to document.body when the trigger is detached from the DOM (WR-02)"
    - "The Escape keydown handler calls stopImmediatePropagation() before dispatching _cancel() (WR-03)"
    - "branding_live.ex error_message/1 handles %Ecto.Changeset{} with a human-readable traversal, not inspect/1 (WR-04)"
    - "priv/templates/sigra.install/admin/admin_hooks.js and test/example/assets/js/admin_hooks.js are byte-identical after all WR edits"
    - "user_show_live.ex and branding_live.ex Cancel buttons carry data-sg-confirm-cancel attribute"
    - "D-11: If any WR hardening reveals a real keyboard trap, missed reduced-motion transition, or theme-flash, the fix ships as a CSS/JS slice with three-surface byte-parity — flag as escalation, do not absorb as test-only"
  artifacts:
    - path: "priv/templates/sigra.install/admin/admin_hooks.js"
      provides: "Hardened ConfirmDialog with WR-01/02/03"
      contains: "data-sg-confirm-cancel"
    - path: "test/example/assets/js/admin_hooks.js"
      provides: "Byte-identical mirror of hardened ConfirmDialog"
      contains: "data-sg-confirm-cancel"
    - path: "lib/sigra/admin/live/user_show_live.ex"
      provides: "Cancel button with data-sg-confirm-cancel attribute"
      contains: "data-sg-confirm-cancel"
    - path: "lib/sigra/admin/live/branding_live.ex"
      provides: "Cancel button with data-sg-confirm-cancel + WR-04 changeset error mapping"
      contains: "data-sg-confirm-cancel"
  key_links:
    - from: "admin_hooks.js ConfirmDialog._cancel()"
      to: "user_show_live.ex cancel button"
      via: "[data-sg-confirm-cancel] querySelector"
      pattern: "data-sg-confirm-cancel"
    - from: "admin_hooks.js ConfirmDialog.destroyed()"
      to: "document.body"
      via: "sentinel fallback when trigger is detached"
      pattern: "document.body.focus"
    - from: "admin_hooks.js ConfirmDialog._onKeydown"
      to: "Escape handler"
      via: "stopImmediatePropagation"
      pattern: "stopImmediatePropagation"
    - from: "WR-01/02/03 hardening (this plan)"
      to: "escalation path (D-11)"
      via: "if a real keyboard trap / reduced-motion miss / theme-flash surfaces, ship a CSS/JS slice tripping three-surface byte-parity — flag under METHODOLOGY"
      pattern: "D-11"
---

<objective>
Harden the shipped ConfirmDialog JS hook and branding error-message function (WR-01..04 folded from Phase 189 review) so the Phase 190 flow specs can exercise APG-conformant keyboard behavior deterministically.

Purpose: Flow spec FLOW-02 keyboard traversal asserts focus on [data-sg-confirm-cancel], focus containment invariant, Escape stopImmediatePropagation, and body-sentinel focus fallback — these behaviors require the WR-01/02/03 hardening to exist in the shipped JS before the specs are authored. WR-04 is coherent org-admin flow work (FLOW-01 org-admin branding error case per D-13 brand-voice error rule).

Output: Four hardened files with three-surface byte-parity for admin_hooks.js.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-flows-fixture-data-l4/190-CONTEXT.md
@.planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md
@.planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md
@guides/reference/admin-design-contract.md
@guides/reference/admin-ui-principles.md

<interfaces>
<!-- Existing ConfirmDialog in admin_hooks.js (lines 369-480). Key targets: -->
<!-- mounted(): this._trigger = document.activeElement; focusables[0].focus() → WR-01 replaces with [data-sg-confirm-cancel] focus -->
<!-- _cancel(): focusables[0].click() → WR-01 replaces with dialog.querySelector('[data-sg-confirm-cancel]').click() -->
<!-- _onKeydown: if key === "Escape" → WR-03 adds event.stopImmediatePropagation() before self._cancel() -->
<!-- destroyed(): this._trigger.focus() → WR-02 adds document.contains(this._trigger) guard + document.body.focus() fallback -->
<!--  -->
<!-- user_show_live.ex:325 — cancel button: -->
<!--   <button type="button" phx-click="cancel_confirm" class="sg-btn sg-btn--ghost sg-btn--sm"> -->
<!-- WR-01: add data-sg-confirm-cancel attribute to this button -->
<!--  -->
<!-- branding_live.ex:366 — cancel button: -->
<!--   <button type="button" phx-click="cancel_restore_defaults" class="sg-btn sg-btn--ghost sg-btn--sm"> -->
<!-- WR-01: add data-sg-confirm-cancel attribute to this button -->
<!--  -->
<!-- branding_live.ex:710-719 — error_message/1: -->
<!--   defp error_message(%{message: message}) when is_binary(message), do: message -->
<!--   defp error_message(%ArgumentError{} = error), do: Exception.message(error) -->
<!--   defp error_message(%{__struct__: _module} = exception) do ... rescue _ -> inspect(exception) end -->
<!--   defp error_message(reason), do: "Could not save auth branding: #{inspect(reason)}" -->
<!-- WR-04: insert %Ecto.Changeset{} clause before the struct catch-all using Ecto.Changeset.traverse_errors/2 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01/02/03 — Harden ConfirmDialog in both admin_hooks.js mirrors + add data-sg-confirm-cancel to LiveView templates</name>
  <files>priv/templates/sigra.install/admin/admin_hooks.js, test/example/assets/js/admin_hooks.js, lib/sigra/admin/live/user_show_live.ex, lib/sigra/admin/live/branding_live.ex</files>
  <read_first>
    - priv/templates/sigra.install/admin/admin_hooks.js (full ConfirmDialog section, lines 369-480) — the canonical template; understand mounted(), _cancel(), _onKeydown(), destroyed(), _trapFocus()
    - test/example/assets/js/admin_hooks.js (verify it is byte-identical to template before editing)
    - lib/sigra/admin/live/user_show_live.ex (lines 315-335 — the sg-confirm-overlay markup; identify the cancel button at line 325)
    - lib/sigra/admin/live/branding_live.ex (lines 349-375 — the restore-defaults overlay; identify the cancel button)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (WR-01/02/03 section with exact before/after patterns)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (ConfirmDialog section for confirmed current state)
  </read_first>
  <action>
    Apply three targeted edits to the ConfirmDialog hook in the canonical admin_hooks.js template, then make the same edits to the example mirror. Apply companion HEEx attribute changes.

    WR-01 — Replace positional focusables[0] with explicit [data-sg-confirm-cancel] selector in two places:
    In mounted(), replace `focusables[0].focus()` with a call to `dialog.querySelector('[data-sg-confirm-cancel]')` and focus it if found, fall through to `focusables[0].focus()` otherwise (defensive).
    In _cancel(), replace `focusables[0].click()` with `var cancelEl = dialog.querySelector('[data-sg-confirm-cancel]'); if (cancelEl) { cancelEl.click(); return; }` then keep the existing `focusables[0].click()` as fallback.

    WR-02 — In destroyed(), replace the bare `if (this._trigger && this._trigger.focus) { this._trigger.focus(); }` with a guarded version: `if (this._trigger && document.contains(this._trigger) && this._trigger.focus) { this._trigger.focus(); } else { document.body.focus(); }`.

    WR-03 — In _onKeydown(), at the `if (key === "Escape")` branch, add `event.stopImmediatePropagation();` immediately after `event.preventDefault();` and before `self._cancel();`. Per D-14: this prevents co-resident document listeners from also firing on the same Escape keydown.

    After editing priv/templates/sigra.install/admin/admin_hooks.js, copy the identical content to test/example/assets/js/admin_hooks.js. Do not diff-then-patch — reproduce the same three edits to the example copy to ensure byte-identical content. The three-surface byte-parity rule (D-14 / 187 D-01..04) requires this.

    Companion HEEx changes (WR-01 requires the attribute to exist in templates):
    In lib/sigra/admin/live/user_show_live.ex at the cancel button inside `#user-session-confirm-overlay` (line ~325): add `data-sg-confirm-cancel` as a standalone attribute to the `<button type="button" phx-click="cancel_confirm"` element.
    In lib/sigra/admin/live/branding_live.ex at the cancel button inside `#restore-defaults-overlay` (line ~366): add `data-sg-confirm-cancel` as a standalone attribute to the `<button type="button" phx-click="cancel_restore_defaults"` element.

    Do NOT change any logic, events, CSS classes, or copy on these buttons — only add the data attribute.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/sigra && md5sum priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js | awk '{print $1}' | sort -u | wc -l | grep -q "^1$" && echo "BYTE-IDENTICAL OK" || echo "BYTE-PARITY FAIL"
    </automated>
    <automated>
      cd /Users/jon/projects/sigra && grep -c "data-sg-confirm-cancel" priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js lib/sigra/admin/live/user_show_live.ex lib/sigra/admin/live/branding_live.ex
    </automated>
    <automated>
      cd /Users/jon/projects/sigra && grep -c "stopImmediatePropagation" priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js
    </automated>
    <automated>
      cd /Users/jon/projects/sigra && grep -c "document.body.focus" priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js
    </automated>
  </verify>
  <done>
    - admin_hooks.js template and example mirror are byte-identical (md5 match)
    - [data-sg-confirm-cancel] appears in admin_hooks.js (template + mirror) and in both LiveView cancel buttons
    - stopImmediatePropagation appears in both admin_hooks.js files in the Escape branch
    - document.body.focus appears in both admin_hooks.js files in destroyed()
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-04 — Add %Ecto.Changeset{} clause to branding_live.ex error_message/1</name>
  <files>lib/sigra/admin/live/branding_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/branding_live.ex (lines 705-725 — the full error_message/1 function)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (WR-04 section with Ecto.Changeset.traverse_errors/2 pattern)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (WR-04 analysis confirming current behavior: Exception.message returns "changeset is invalid")
  </read_first>
  <action>
    Insert a new `defp error_message(%Ecto.Changeset{} = changeset)` clause in branding_live.ex between the existing `%ArgumentError{}` clause and the `%{__struct__: _module}` catch-all struct clause.

    The new clause uses `Ecto.Changeset.traverse_errors/2` to build a human-readable string. Pattern:
    Traverse errors to produce a map of `{field => [error_strings]}`, then map-join into "field: error1, error2" pairs separated by "; ". The interpolation Regex replacement follows Phoenix's `translate_error` convention: `~r"%{(\w+)}"` matched against the message template with `opts` keyword values substituted.

    Clause ordering matters — insert BEFORE the `%{__struct__: _module}` clause so the specific Ecto.Changeset pattern takes precedence. Do not alter any other clause. Do not change the final `defp error_message(reason)` catch-all.

    This satisfies D-13 brand-voice error rule (errors = what failed + why + next action; status-not-blame) and D-14 WR-04 for the org-admin branding error case in FLOW-01.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix test test/sigra/admin/live/branding_live_test.exs 2>&1 | tail -5</automated>
    <automated>cd /Users/jon/projects/sigra && grep -n "Ecto.Changeset.*traverse_errors\|traverse_errors" lib/sigra/admin/live/branding_live.ex | head -5</automated>
  </verify>
  <done>
    - branding_live.ex contains a %Ecto.Changeset{} clause that calls traverse_errors/2
    - mix test exits 0 for the branding_live test file (no regressions)
    - The new clause appears BEFORE the %{__struct__: _module} catch-all
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS hook → DOM | ConfirmDialog hook reads/writes DOM attributes and listens to document-level keydown events |
| LiveView template → browser | HEEx-rendered data attributes consumed by the ConfirmDialog JS hook |
| branding_live.ex → user | Error messages rendered into flash; must not leak raw Ecto.Changeset struct detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-01 | Information Disclosure | branding_live.ex error_message/1 | mitigate | WR-04: traverse_errors/2 produces field-level human copy; inspect/1 fallback on struct is removed for Ecto.Changeset; struct internals never reach the flash |
| T-190-02 | Tampering | Escape keydown handler co-resident listeners | mitigate | WR-03 stopImmediatePropagation prevents co-resident listeners from acting on the same Escape event that closes the dialog |
| T-190-03 | Denial of Service | Focus trap with body-sentinel fallback | accept | WR-02 fallback is document.body.focus() — no credentials or PII involved; low-severity UX edge case |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan; existing tooling only |
</threat_model>

<verification>
After both tasks:
- `md5sum priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js` — both hashes identical
- `grep -c "data-sg-confirm-cancel" lib/sigra/admin/live/user_show_live.ex` returns 1
- `grep -c "data-sg-confirm-cancel" lib/sigra/admin/live/branding_live.ex` returns at least 2 (cancel button + hook references)
- `mix test` exits 0 (no regressions in Elixir suite)
- `cd test/example/priv/playwright && npx playwright test admin-modal-interaction --project=chromium` exits 0 (existing modal interaction spec passes against hardened hook)
</verification>

<success_criteria>
1. admin_hooks.js ConfirmDialog: [data-sg-confirm-cancel] selector used in _cancel() and mounted(); Escape handler has stopImmediatePropagation(); destroyed() falls back to document.body.focus() when trigger is detached.
2. Both admin_hooks.js copies (template + example mirror) are byte-identical (md5 match confirms no drift).
3. user_show_live.ex and branding_live.ex Cancel buttons carry data-sg-confirm-cancel attribute.
4. branding_live.ex error_message/1 has an explicit %Ecto.Changeset{} clause using traverse_errors/2 — no raw inspect/1 fallback for changeset errors.
5. mix test exits 0; admin-modal-interaction spec exits 0.
</success_criteria>

<output>
Create `.planning/phases/190-flows-fixture-data-l4/190-01-SUMMARY.md` when done
</output>
