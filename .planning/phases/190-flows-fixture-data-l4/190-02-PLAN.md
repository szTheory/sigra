---
phase: 190-flows-fixture-data-l4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/playwright.config.ts
  - test/example/priv/playwright/helpers/adminFlows.ts
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, DATA-01]
must_haves:
  truths:
    - "The ADMIN_BEHAVIOR_SPECS regex in playwright.config.ts includes admin-flow so admin-flow-*.spec.ts runs on chromium only and is excluded from the mobile project"
    - "helpers/adminFlows.ts exports: waitForLiveViewReady, loginDemoUser, loginDemoAdmin, loginDemoMorgan, assertScopeChrome, seedThemeAndAssertNoFlash, assertThemeAttributes, assertReducedMotionEffect"
    - "loginDemoUser uses pre-seeded demo personas (not registerUser); no waitForLiveViewReady needed on the /users/log_in controller page"
    - "reducedMotion is handled at test.use() context level in helpers, not per-page after goto"
    - "No new Playwright projects or storageState infra added — inline login helper only"
    - "D-10: reducedMotion set at test.use() context/config level (not per-page after goto) so assertReducedMotionEffect() checks the collapsed CSS animation-duration effect; seedThemeAndAssertNoFlash asserts the inline applyTheme script in admin_shell.ex:24-42 (not admin_hooks.js) fires before paint"
  artifacts:
    - path: "test/example/priv/playwright/helpers/adminFlows.ts"
      provides: "Shared flow utilities: login, readiness, scope, theme, reduced-motion"
      exports: ["waitForLiveViewReady", "loginDemoUser", "loginDemoAdmin", "loginDemoMorgan", "assertScopeChrome", "seedThemeAndAssertNoFlash", "assertThemeAttributes", "assertReducedMotionEffect"]
    - path: "test/example/priv/playwright/playwright.config.ts"
      provides: "Updated ADMIN_BEHAVIOR_SPECS regex including admin-flow"
      contains: "admin-flow"
  key_links:
    - from: "playwright.config.ts mobile project testIgnore"
      to: "ADMIN_BEHAVIOR_SPECS regex"
      via: "admin-flow alternation added"
      pattern: "admin-flow"
    - from: "helpers/adminFlows.ts loginDemoUser"
      to: "/users/log_in controller page"
      via: "page.goto + page.fill + page.click + expect(page).not.toHaveURL"
      pattern: "loginDemoUser"
    - from: "helpers/adminFlows.ts assertReducedMotionEffect + seedThemeAndAssertNoFlash (D-10)"
      to: "sigra_admin.css:1467 @media guard + admin_shell.ex:24-42 inline applyTheme script"
      via: "reducedMotion at context level; addInitScript before goto; never computed colors"
      pattern: "D-10"
---

<objective>
Fix the blocking playwright.config.ts ADMIN_BEHAVIOR_SPECS regex gap (RESEARCH confirmed: flow specs will run on mobile project without this fix, violating D-09) and create the shared adminFlows.ts helper module that all three flow specs import.

Purpose: Plan 03/04/05 (Wave 2) cannot run correctly without the config fix, and all three specs depend on the shared helpers for login, readiness, scope, theme, and reduced-motion assertions. Both files are wave-1 prerequisites.

Output: One config edit + one new helper file; no new Playwright projects.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-flows-fixture-data-l4/190-CONTEXT.md
@.planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md
@.planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md

<interfaces>
<!-- playwright.config.ts:24-25 — current ADMIN_BEHAVIOR_SPECS: -->
<!--   const ADMIN_BEHAVIOR_SPECS = -->
<!--     /(admin-user-operations|admin-audit|admin-theme|impersonation)\.spec\.ts/; -->
<!-- Required: add |admin-flow to the alternation -->
<!-- mobile project testIgnore at lines 98-106 uses [ADMIN_BEHAVIOR_SPECS, ...] -->
<!--  -->
<!-- helpers/adminUsersIndex.ts (analog — module shape): -->
<!--   import type { Page } from '@playwright/test'; -->
<!--   export function adminUsersEmailLocator(page: Page, email: string) { ... } -->
<!--  -->
<!-- demo-showcase.spec.ts:183-198 — loginDemoUser + loginDemoAdmin source patterns -->
<!-- impersonation.spec.ts:20-24 — waitForLiveViewReady canonical form -->
<!-- admin-user-operations.spec.ts:62-69 — scope chrome assertion pattern -->
<!-- admin-theme.spec.ts:364-394, 415-479 — theme addInitScript + assertThemeAttributes patterns -->
<!-- sigra_admin.css:1467 — @media (prefers-reduced-motion: reduce) guard; -->
<!--   .sg-admin-loading-bar::before { animation: none !important; transition: none !important; } -->
<!-- Demo persona passwords (from personas.ex): -->
<!--   admin@demo.vaultr.test → "DemoAdmin1!SecurePass" -->
<!--   morgan@demo.vaultr.test → "MorganDemo1!OrgAdmin" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ADMIN_BEHAVIOR_SPECS regex in playwright.config.ts to include admin-flow</name>
  <files>test/example/priv/playwright/playwright.config.ts</files>
  <read_first>
    - test/example/priv/playwright/playwright.config.ts (lines 24-25 for the regex; lines 97-106 for the mobile project testIgnore that consumes it)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (Behavior lane section confirming the gap and the exact fix)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (playwright.config.ts section showing current vs required regex)
  </read_first>
  <action>
    Edit playwright.config.ts lines 24-25 to add `admin-flow` to the ADMIN_BEHAVIOR_SPECS alternation:

    Change from:
      const ADMIN_BEHAVIOR_SPECS =
        /(admin-user-operations|admin-audit|admin-theme|impersonation)\.spec\.ts/;

    Change to:
      const ADMIN_BEHAVIOR_SPECS =
        /(admin-user-operations|admin-audit|admin-theme|impersonation|admin-flow)\.spec\.ts/;

    This single-alternation addition ensures the mobile project's `testIgnore: [ADMIN_BEHAVIOR_SPECS, ...]` array excludes `admin-flow-*.spec.ts` files — keeping behavior-truth specs on the chromium lane only, per D-09.

    Do not change any other lines in playwright.config.ts. Do not add new Playwright projects or alter any other testIgnore arrays.
  </action>
  <verify>
    <automated>grep "admin-flow" test/example/priv/playwright/playwright.config.ts</automated>
    <automated>cd test/example/priv/playwright && npx playwright test --list --project=mobile 2>&1 | grep -c "admin-flow" | grep "^0$" && echo "MOBILE EXCLUSION OK" || echo "MOBILE EXCLUSION FAIL"</automated>
  </verify>
  <done>
    - playwright.config.ts ADMIN_BEHAVIOR_SPECS regex contains admin-flow in the alternation
    - `npx playwright test --list --project=mobile` shows zero admin-flow-*.spec.ts entries
  </done>
</task>

<task type="auto">
  <name>Task 2: Create helpers/adminFlows.ts — shared login, readiness, scope, theme, and reduced-motion utilities</name>
  <files>test/example/priv/playwright/helpers/adminFlows.ts</files>
  <read_first>
    - test/example/priv/playwright/helpers/adminUsersIndex.ts (module shape: named exports, Page type import)
    - test/example/priv/playwright/helpers/fixtures.ts (TEST_PASSWORD constant — check if demo passwords should live there or in adminFlows.ts)
    - test/example/priv/playwright/tests/demo-showcase.spec.ts (lines 183-198 — loginDemoUser + loginDemoAdmin source patterns)
    - test/example/priv/playwright/tests/impersonation.spec.ts (lines 20-24 — waitForLiveViewReady canonical form)
    - test/example/priv/playwright/tests/admin-user-operations.spec.ts (lines 62-69 — scope chrome assertion pattern)
    - test/example/priv/playwright/tests/admin-theme.spec.ts (lines 364-394 — addInitScript no-flash; lines 415-479 — assertThemeAttributes)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (helpers/adminFlows.ts section with all function signatures and source citations)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (Reduced-motion section noting sigra_admin.css:1467 guard; Architecture Patterns section)
  </read_first>
  <action>
    Create test/example/priv/playwright/helpers/adminFlows.ts as a TypeScript module exporting the following utilities. The file must import from `@playwright/test` only (no additional packages).

    DEMO_ADMIN_EMAIL: `'admin@demo.vaultr.test'` — constant, exported.
    DEMO_ADMIN_PASSWORD: `'DemoAdmin1!SecurePass'` — constant, exported.
    DEMO_MORGAN_EMAIL: `'morgan@demo.vaultr.test'` — constant, exported.
    DEMO_MORGAN_PASSWORD: `'MorganDemo1!OrgAdmin'` — constant, exported.

    waitForLiveViewReady(page: Page): extract from impersonation.spec.ts:20-24. Awaits `[data-phx-session].phx-connected` in 'attached' state. Note in JSDoc: do NOT call this on the /users/log_in controller page (not a LiveView).

    loginDemoUser(page: Page, email: string, password: string): from demo-showcase.spec.ts:183-194. Navigates to /users/log_in, fills #login_form email/password, clicks 'Log in' button, asserts page URL is NOT /users/log_in. Document in JSDoc: no MFA challenge — example app has no mfa.check_fn; creates :standard session directly. Login is safe for all 9 demo personas including admin (TOTP enrolled but no mfa.check_fn configured).

    loginDemoAdmin(page: Page): convenience wrapper calling loginDemoUser with DEMO_ADMIN_EMAIL + DEMO_ADMIN_PASSWORD.

    loginDemoMorgan(page: Page): convenience wrapper calling loginDemoUser with DEMO_MORGAN_EMAIL + DEMO_MORGAN_PASSWORD.

    assertScopeChrome(page: Page, scopeLabel: string): from admin-user-operations.spec.ts:62-69. Locates the first header element, asserts it contains 'Admin' text (exact) and the scopeLabel text (non-exact). Usage: 'Global' for platform admin posture; org name (e.g. 'Acme Corp') for org-scoped.

    seedThemeAndAssertNoFlash(page: Page, theme: 'dark' | 'light'): from admin-theme.spec.ts:364-394 pattern. Uses page.addInitScript to set localStorage["sigra.admin.theme"] to theme BEFORE page.goto("/admin"). After goto, asserts html has data-sg-admin-theme attribute equal to theme. This confirms the synchronous inline script in admin_shell.ex:24-42 fires before paint (no-flash). Note in JSDoc: the script being asserted is the inline <script> block in admin_shell.ex, NOT a <script src> element in admin_hooks.js.

    assertThemeAttributes(page: Page, theme: 'dark' | 'light' | 'system'): from admin-theme.spec.ts:415-479 pattern. For 'dark' or 'light': asserts .sg-admin-shell has data-theme attribute equal to theme; asserts html has data-sg-admin-theme attribute equal to theme; evaluates localStorage.getItem("sigra.admin.theme") and asserts it equals theme. For 'system': asserts html does NOT have data-sg-admin-theme attribute; asserts localStorage is null for the key. Use web-first auto-retrying `expect(locator).toHaveAttribute(...)` — never toHaveCSS for theme assertions.

    assertReducedMotionEffect(page: Page): asserts that the @media (prefers-reduced-motion: reduce) guard in sigra_admin.css:1467 has collapsed the loading bar animation. Evaluates window.getComputedStyle on .sg-admin-loading-bar for the ::before pseudo-element and asserts animationName equals 'none'. This is the CSS-effect assertion (not just matchMedia().matches). Note in JSDoc: this must be called AFTER test.use({ reducedMotion: 'reduce' }) is set at the describe-block level — never call page.emulateMedia after goto (playwright#31328 Firefox drops it).

    All functions must be exported named exports (not default export). Include a file-level JSDoc noting: "Phase 190 shared flow utilities. Import into admin-flow-*.spec.ts files."
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx tsc --noEmit helpers/adminFlows.ts 2>&1 | tail -5</automated>
    <automated>grep -c "export" test/example/priv/playwright/helpers/adminFlows.ts</automated>
  </verify>
  <done>
    - helpers/adminFlows.ts exists with all 8 exported items (4 constants + 6 functions)
    - TypeScript compilation of the helper file exits 0 (no type errors)
    - File contains waitForLiveViewReady, loginDemoUser, loginDemoAdmin, loginDemoMorgan, assertScopeChrome, seedThemeAndAssertNoFlash, assertThemeAttributes, assertReducedMotionEffect
    - File contains no sleeps, no per-page emulateMedia calls, no hardcoded URLs other than /users/log_in and /admin
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test helpers → demo server | loginDemoUser sends credentials to the dev server; credentials are public-by-design demo values |
| playwright.config.ts → CI | The ADMIN_BEHAVIOR_SPECS regex gates which projects run which specs; incorrect exclusion causes behavior specs to run on the wrong project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-04 | Information Disclosure | helpers/adminFlows.ts demo passwords | accept | All 9 demo passwords are public-by-design per personas.ex:12 ("Never use in production"); no real credentials; dev server only |
| T-190-05 | Tampering | ADMIN_BEHAVIOR_SPECS regex | mitigate | The alternation addition is minimal and tested by `--list --project=mobile` showing zero admin-flow entries; monotonic guard is unaffected (no ledger changes here) |
| T-190-06 | Spoofing | loginDemoUser anti-enumeration | accept | The flow specs test admin operators against the demo dev server only; the 403 assertion (plan 05) asserts the GENERIC "Access denied..." copy — no org-existence disclosure |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No new packages; existing @playwright/test only |
</threat_model>

<verification>
After both tasks:
- `grep 'admin-flow' test/example/priv/playwright/playwright.config.ts` returns a match
- `cd test/example/priv/playwright && npx playwright test --list --project=mobile 2>&1 | grep admin-flow | wc -l` returns 0
- `ls test/example/priv/playwright/helpers/adminFlows.ts` exists
- `grep -c 'export' test/example/priv/playwright/helpers/adminFlows.ts` returns ≥ 8
- `cd test/example/priv/playwright && npx tsc --noEmit 2>&1 | grep adminFlows` returns empty (no TS errors)
</verification>

<success_criteria>
1. playwright.config.ts ADMIN_BEHAVIOR_SPECS regex includes admin-flow; mobile project --list shows zero admin-flow entries.
2. helpers/adminFlows.ts exports: DEMO_ADMIN_EMAIL, DEMO_ADMIN_PASSWORD, DEMO_MORGAN_EMAIL, DEMO_MORGAN_PASSWORD, waitForLiveViewReady, loginDemoUser, loginDemoAdmin, loginDemoMorgan, assertScopeChrome, seedThemeAndAssertNoFlash, assertThemeAttributes, assertReducedMotionEffect.
3. TypeScript compilation of the helper module exits 0.
4. No sleeps, no emulateMedia after goto, no new Playwright projects in the helper file.
</success_criteria>

<output>
Create `.planning/phases/190-flows-fixture-data-l4/190-02-SUMMARY.md` when done
</output>
