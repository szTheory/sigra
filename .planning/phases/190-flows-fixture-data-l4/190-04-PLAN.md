---
phase: 190-flows-fixture-data-l4
plan: 04
type: execute
wave: 2
depends_on: ["190-01", "190-02"]
files_modified:
  - test/example/priv/playwright/tests/admin-flow-support-investigator.spec.ts
  - test/example/priv/playwright/tests/admin-flow-org-admin.spec.ts
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, DATA-01]
must_haves:
  truths:
    - "Support investigator journey: find alice → per-user audit evidence (read) → impersonate alice → impersonation banner visible + persistent across navigation → stop impersonation → return to /admin/users?.*q= with 'Global' scope chrome"
    - "Support investigator keyboard: Tab through investigator journey interactive elements; ConfirmDialog APG gates pass (per WR-01/02/03 hardened hook); focus returns to trigger after Escape"
    - "Support investigator theme: dark theme persists across the full impersonation journey and after page.reload()"
    - "Org admin journey: loginDemoMorgan → /admin → 403 response with generic 'Access denied...' body (anti-enumeration) → /admin/organizations/acme-corp → 200 with 'Acme Corp' scope"
    - "Org admin main-error: morgan hitting /admin returns HTTP 403; error body does not disclose org existence"
    - "Org admin boundary: navigate to morgan's per-user audit page → .sg-empty-state renders (zero audit events seeded for morgan)"
    - "Both specs: test.use({ reducedMotion: 'reduce' }) at describe-block level; assertReducedMotionEffect() confirms CSS collapsed effect"
    - "Both specs: no sleeps; web-first auto-retrying assertions; no computed color assertions"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-flow-support-investigator.spec.ts"
      provides: "Support investigator JTBD flow spec: happy/error/boundary + impersonation banner continuity + keyboard + theme"
      min_lines: 100
    - path: "test/example/priv/playwright/tests/admin-flow-org-admin.spec.ts"
      provides: "Org admin JTBD flow spec: tenant-bounded access + 403 permission-denied + empty audit boundary + theme"
      min_lines: 80
  key_links:
    - from: "admin-flow-support-investigator.spec.ts"
      to: "Sigra.Impersonation (lib/sigra/impersonation.ex)"
      via: "journey-level banner continuity assertions (not internal mechanics)"
      pattern: "Impersonating"
    - from: "admin-flow-org-admin.spec.ts"
      to: ":admin_global plug + AuthErrorHandler"
      via: "page.goto('/admin') → response.status() === 403"
      pattern: "Access denied"
    - from: "admin-flow-org-admin.spec.ts"
      to: "AuditUserLive (morgan's audit page)"
      via: "sg-empty-state renders for zero-audit-event user"
      pattern: "sg-empty-state"
---

<objective>
Author the support investigator and org admin JTBD flow specs — completing the three-persona L4 coverage required by FLOW-01/02/03/DATA-01. The investigator spec focuses on the impersonation journey continuity (banner persistence, scope/return context); the org admin spec focuses on tenant-bounded access, permission-denied, and the empty audit boundary case.

Purpose: The investigator and org admin are parallel to platform admin (no file overlap), running in Wave 2 alongside Plan 03. Both reuse the adminFlows.ts helpers from Plan 02.

Output: Two new spec files; no changes to shared infrastructure.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-flows-fixture-data-l4/190-CONTEXT.md
@.planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md
@.planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md
@.planning/phases/190-flows-fixture-data-l4/190-UI-SPEC.md
@guides/reference/admin-ui-principles.md

<interfaces>
<!-- Support investigator imports + key patterns: -->
<!--   import from helpers/adminFlows: loginDemoAdmin, waitForLiveViewReady, assertScopeChrome, -->
<!--     assertThemeAttributes, seedThemeAndAssertNoFlash, assertReducedMotionEffect -->
<!--   import adminUsersEmailLocator from helpers/adminUsersIndex -->
<!--  -->
<!-- Impersonation journey (from 190-PATTERNS.md + impersonation.spec.ts:89-144): -->
<!--   D-02: find account → per-user audit evidence (read) → impersonate → resolve → return -->
<!--   D-09: assert journey-level properties ONLY; does NOT re-test impersonation internals -->
<!--   sudo path: /users/sudo?return_to=<detail-url> → fill password → click 'Confirm password' -->
<!--   start impersonation: getByRole('button', { name: 'Start impersonation' }) -->
<!--   impersonation banner: section containing 'Impersonating alice@demo.vaultr.test' -->
<!--                          and 'Signed in as admin@demo.vaultr.test' -->
<!--   stop: getByRole('button', { name: 'End impersonation' }) -->
<!--   return URL: /admin/users?.*q= with scope chrome 'Global' -->
<!--  -->
<!-- Org admin imports + key patterns: -->
<!--   import from helpers/adminFlows: loginDemoMorgan, waitForLiveViewReady, assertScopeChrome, -->
<!--     assertThemeAttributes, seedThemeAndAssertNoFlash, assertReducedMotionEffect -->
<!--  -->
<!-- 403 response pattern (from 190-PATTERNS.md + admin-generated.spec.ts:170-186): -->
<!--   const response = await page.goto('/admin'); -->
<!--   expect(response?.status()).toBe(403); -->
<!--   await expect(page.locator('body')).toContainText('Access denied. You do not have access to this admin scope.'); -->
<!--   ANTI-ENUMERATION: body does not contain org name or user details -->
<!--  -->
<!-- Org-scoped happy path (from admin-generated.spec.ts:172-177): -->
<!--   const ok = await page.goto('/admin/organizations/acme-corp'); -->
<!--   expect(ok?.status()).toBe(200); -->
<!--   header contains 'Acme Corp' -->
<!--  -->
<!-- Morgan empty audit boundary: -->
<!--   Morgan has 0 audit events (RESEARCH confirmed) -->
<!--   Navigate to: /admin/organizations/acme-corp/users — list view shows members (alice, carol, dave, grace + morgan) -->
<!--   Navigate to morgan's own user detail (find morgan in the list) → /audit tab -->
<!--   Assert .sg-empty-state visible or heading containing /no audit/i -->
<!--   OR: navigate to /admin/users/:morgan_id/audit directly after finding morgan's ID from the list -->
<!--  -->
<!-- Demo persona credentials: -->
<!--   morgan: 'morgan@demo.vaultr.test' / 'MorganDemo1!OrgAdmin' -->
<!--   admin: 'admin@demo.vaultr.test' / 'DemoAdmin1!SecurePass' -->
<!--   alice: 'alice@demo.vaultr.test' / 'AliceDemoPass1!' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author admin-flow-support-investigator.spec.ts — investigator posture: find→audit→impersonate→return + keyboard + theme</name>
  <files>test/example/priv/playwright/tests/admin-flow-support-investigator.spec.ts</files>
  <behavior>
    - test.use({ reducedMotion: 'reduce' }) at describe-block level
    - Happy path: loginDemoAdmin → /admin/users → search alice → open alice user detail → waitForLiveViewReady → navigate to /admin/users/alice/audit → assert alice has audit events (at least the impersonation rows: admin.impersonation.start) → navigate back to user detail → perform sudo flow (/users/sudo?return_to=) → Start impersonation → banner shows "Impersonating alice@demo.vaultr.test" and "Signed in as admin@demo.vaultr.test" → navigate to another admin page while impersonating → banner still visible → End impersonation → assert URL is /admin/users?.*q= → assertScopeChrome('Global')
    - Main-error (investigator posture on dave): search dave → user detail → dave shows locked state indicators → dave audit shows auth.login.failure or auth.lockout.start events → do NOT attempt to impersonate dave (boundary of the spec; impersonation mechanics owned by impersonation.spec.ts)
    - Boundary: frank (scheduled-deletion) — search frank → user detail → frank shows deletion-scheduled indicator in detail view
    - Keyboard: Tab to the "Start impersonation" button → keyboard.press('Enter') or focus check; after return from impersonation, assertScopeChrome('Global') via keyboard navigation; assert focus-visible after Tab (not after click)
    - Reduced-motion: assertReducedMotionEffect() after first page load in the happy path
    - Theme: seedThemeAndAssertNoFlash(page, 'dark') → full impersonation journey → assertThemeAttributes persists during impersonation → page.reload() after stop → assertThemeAttributes(page, 'dark')
    - No re-testing of impersonation mechanics (stale-sudo redirect, session isolation, etc.) — those are owned by impersonation.spec.ts per D-02
  </behavior>
  <read_first>
    - test/example/priv/playwright/tests/impersonation.spec.ts (lines 89-144 — the canonical impersonation journey; D-02 says assert journey-level over this, not internals)
    - test/example/priv/playwright/helpers/adminFlows.ts (the helper module — use loginDemoAdmin, assertScopeChrome, assertThemeAttributes, seedThemeAndAssertNoFlash, assertReducedMotionEffect, waitForLiveViewReady)
    - test/example/priv/playwright/helpers/adminUsersIndex.ts (adminUsersEmailLocator)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (admin-flow-support-investigator.spec.ts section — full journey structure + ConfirmDialog keyboard patterns)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (Impersonation section confirming banner text; Theme hook section confirming reload gap; Seed table confirming alice has impersonation audit events)
    - lib/sigra/admin/live/user_show_live.ex (button labels: "Start impersonation", "End impersonation", "Revoke session")
  </read_first>
  <action>
    Create test/example/priv/playwright/tests/admin-flow-support-investigator.spec.ts.

    File structure: single `test.describe('Phase 190 support investigator flow (FLOW-01..03, DATA-01)', ...)` with:
    - `test.use({ reducedMotion: 'reduce' })` at describe-block level
    - `test.beforeEach(...)` — loginDemoAdmin + navigate to /admin + waitForLiveViewReady (beforeEach is skipped for the theme describe block which seeds before goto)
    - `test.describe('happy path — alice (find → audit → impersonate → return)', ...)` — one test
    - `test.describe('main-error — dave locked/unconfirmed', ...)` — one test (no impersonation attempt)
    - `test.describe('boundary — frank scheduled-deletion', ...)` — one test
    - `test.describe('keyboard operability (FLOW-02)', ...)` — one test
    - `test.describe('theme persistence across impersonation journey (FLOW-03)', ...)` — one test (no beforeEach, seeds theme before goto)

    Concrete identifiers for the happy path impersonation journey:
    - adminUsersEmailLocator(page, 'alice@demo.vaultr.test') to find alice in list
    - getByRole('link', { name: 'Open user' }).first() or the alice row link to navigate to user detail
    - URL encode the detail path for sudo: `encodeURIComponent(detailPath)` → `/users/sudo?return_to=...`
    - sudo form: `input[name="sudo[password]"]` → fill with DEMO_ADMIN_PASSWORD (from adminFlows import or local constant)
    - getByRole('button', { name: 'Confirm password' }) to submit sudo
    - getByRole('button', { name: 'Start impersonation' }) to start
    - Impersonation banner: `page.locator('section').filter({ hasText: 'Impersonating' }).first()`
    - Banner assertions: `toContainText('Impersonating alice@demo.vaultr.test')` and `toContainText('Signed in as admin@demo.vaultr.test')`
    - Stop: getByRole('button', { name: 'End impersonation' })
    - Return URL: `expect(page).toHaveURL(/\/admin\/users\?.*q=/)`

    Journey-level only (D-02): Do NOT assert stale-sudo redirect timing, session isolation details, or sub-second timing. Do assert: banner text, banner persistence across a navigate, stop returns to /admin/users?.*q=, scope chrome is 'Global' after stop.

    Theme test: seedThemeAndAssertNoFlash(page, 'dark') → goto /admin → loginDemoAdmin flow → navigate to alice user detail → assertThemeAttributes(page, 'dark') → start impersonation → assertThemeAttributes(page, 'dark') → End impersonation → page.reload() → waitForLiveViewReady → assertThemeAttributes(page, 'dark').

    Assertions: web-first auto-retrying expect(). No sleeps. No toHaveCSS for theme.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test admin-flow-support-investigator --project=chromium 2>&1 | tail -15</automated>
    <automated>cd test/example/priv/playwright && npx playwright test admin-flow-support-investigator --project=mobile --list 2>&1 | grep -c "admin-flow-support" | grep "^0$" && echo "MOBILE EXCLUSION OK"</automated>
  </verify>
  <done>
    - admin-flow-support-investigator.spec.ts passes all tests on chromium project
    - Mobile project shows 0 entries for this spec
    - Spec covers: investigator happy journey with impersonation banner persistence + stop→return context; main-error (dave locked); boundary (frank deletion); keyboard; theme across impersonation + reload
    - Does not re-test impersonation internals (owned by impersonation.spec.ts)
    - No sleeps, no emulateMedia after goto
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author admin-flow-org-admin.spec.ts — tenant-bounded: 403 permission-denied + org-scoped access + empty audit boundary + theme</name>
  <files>test/example/priv/playwright/tests/admin-flow-org-admin.spec.ts</files>
  <behavior>
    - test.use({ reducedMotion: 'reduce' }) at describe-block level
    - Happy path: loginDemoMorgan → /admin/organizations/acme-corp → response 200 → assertScopeChrome('Acme Corp') → /admin/organizations/acme-corp/users → member list shows alice, carol (Acme members) → navigate to alice user detail in org-scoped context
    - Main-error (permission-denied): loginDemoMorgan → page.goto('/admin') → response.status() === 403 → body contains 'Access denied. You do not have access to this admin scope.' → body does NOT contain 'Acme Corp' or morgan's email (anti-enumeration check)
    - Boundary (empty audit): loginDemoMorgan → navigate to morgan's own user detail within org scope → navigate to morgan's /audit tab → .sg-empty-state is visible (zero audit events for morgan) → heading or text matches /no audit/i or the ratified brand-book string "No audit rows yet..."
    - Reduced-motion: assertReducedMotionEffect() after first page load
    - Theme: seedThemeAndAssertNoFlash(page, 'dark') → full org-scoped journey → assertThemeAttributes persists → page.reload() → assertThemeAttributes(page, 'dark')
    - Anti-enumeration assertion: the 403 body copy is a GENERIC string — does not confirm org or user existence; assert body does NOT contain 'acme' or morgan's email
  </behavior>
  <read_first>
    - test/example/priv/playwright/tests/admin-generated.spec.ts (lines 159-201 — the 403 permission-denied analog and org-scoped access pattern used as the closest existing spec)
    - test/example/priv/playwright/helpers/adminFlows.ts (loginDemoMorgan, assertScopeChrome, assertThemeAttributes, seedThemeAndAssertNoFlash, assertReducedMotionEffect, waitForLiveViewReady)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (admin-flow-org-admin.spec.ts section — 403 pattern, org-scoped allowed access, empty state boundary)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (Open Questions #2 — morgan empty audit; Seed table confirming morgan has 0 audit events; 403 response is plain HTTP not LiveView redirect per Assumption A2)
    - test/example/lib/example/demo/seeds.ex (lines 229-250 — morgan's membership in Acme Corp; confirm morgan is :admin member of acme)
    - .planning/phases/190-flows-fixture-data-l4/190-UI-SPEC.md (Copywriting Contract — anti-enumeration and scope indicator sections)
  </read_first>
  <action>
    Create test/example/priv/playwright/tests/admin-flow-org-admin.spec.ts.

    File structure: single `test.describe('Phase 190 org admin flow (FLOW-01..03, DATA-01)', ...)` with:
    - `test.use({ reducedMotion: 'reduce' })` at describe-block level
    - `test.beforeEach(...)` — loginDemoMorgan + waitForLiveViewReady after goto('/admin/organizations/acme-corp') (NOT /admin — that's the 403 test)
    - `test.describe('happy path — morgan org-scoped admin access', ...)` — one test covering /admin/organizations/acme-corp access, member list, user detail
    - `test.describe('main-error — permission denied (global /admin blocked)', ...)` — one test; does NOT use beforeEach login; logs in and hits /admin directly; asserts 403 + generic body copy + anti-enumeration
    - `test.describe('boundary — morgan empty audit feed (zero events)', ...)` — one test; navigate to morgan's own audit page from org scope
    - `test.describe('keyboard operability (FLOW-02)', ...)` — one test; Tab navigation in org-scoped view
    - `test.describe('theme persistence across org-scoped journey (FLOW-03)', ...)` — one test; no beforeEach; seeds theme before goto

    Concrete identifiers:
    - Morgan login: `loginDemoMorgan(page)` from adminFlows helper
    - Org scope route: `/admin/organizations/acme-corp`
    - Org scope users: `/admin/organizations/acme-corp/users`
    - Scope chrome: `assertScopeChrome(page, 'Acme Corp')`
    - 403 test: `const response = await page.goto('/admin'); expect(response?.status()).toBe(403);`
    - 403 body: `await expect(page.locator('body')).toContainText('Access denied. You do not have access to this admin scope.');`
    - Anti-enumeration: `await expect(page.locator('body')).not.toContainText('Acme Corp');` and `await expect(page.locator('body')).not.toContainText('morgan@demo.vaultr.test');`
    - Empty state: navigate to morgan's user detail (find morgan in the org users list → open → navigate to /audit or the audit tab link) → `await expect(page.locator('.sg-empty-state')).toBeVisible();` or `await expect(page.getByRole('heading', { name: /no audit/i })).toBeVisible();`
    - adminUsersEmailLocator(page, 'morgan@demo.vaultr.test') — use to find morgan in the org users list if the org-scoped users view shows morgan as a member

    Open question resolution (RESEARCH.md Q2): Morgan is an :admin member of Acme Corp (seeds.ex:236) so morgan can navigate /admin/organizations/acme-corp/users. The org-scoped users list should show all Acme members. Morgan's own user detail should be reachable from this list. If morgan's user detail is not accessible in the org-scoped view (the authorizer may restrict self-view), use an alternative boundary: apply a date-range filter that returns no results in the audit explorer — assert the empty_state renders. Document the fallback in a spec comment if used.

    Assertions: web-first auto-retrying. No sleeps. No computed color assertions for theme.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && npx playwright test admin-flow-org-admin --project=chromium 2>&1 | tail -15</automated>
    <automated>cd test/example/priv/playwright && npx playwright test admin-flow-org-admin --project=mobile --list 2>&1 | grep -c "admin-flow-org-admin" | grep "^0$" && echo "MOBILE EXCLUSION OK"</automated>
  </verify>
  <done>
    - admin-flow-org-admin.spec.ts passes all tests on chromium project
    - Mobile project shows 0 entries for this spec
    - Spec covers: org-scoped happy access to /admin/organizations/acme-corp (200); permission-denied at /admin (403) with anti-enumeration body assertions; morgan's empty audit boundary (.sg-empty-state or equivalent); keyboard; theme persistence + reload
    - Anti-enumeration: 403 body asserted NOT to contain 'Acme Corp' or morgan's email
    - No sleeps, no emulateMedia after goto
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Org admin → global admin routes | morgan is org-scoped only; /admin must return 403 |
| Anti-enumeration boundary | 403 body must be generic — must not disclose org or user existence |
| Morgan → org-scoped audit data | Morgan can only see Acme Corp data; no cross-org data visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-10 | Information Disclosure | 403 response body anti-enumeration | mitigate | Spec asserts body NOT toContainText with org name or morgan email; generic copy confirmed by admin-generated.spec.ts:179-185 existing assertion |
| T-190-11 | Elevation of Privilege | Org admin accessing global /admin | mitigate | Flow spec asserts response.status() === 403 deterministically; the :admin_global plug is the actual control (already audited); spec provides regression evidence |
| T-190-12 | Information Disclosure | Morgan empty audit boundary | accept | Empty state reveals "no audit events for this user" — this is appropriate disclosure for an org admin viewing their own audit trail; not a PII or enumeration risk |
| T-190-13 | Spoofing | loginDemoMorgan demo credential | accept | Public-by-design demo credential; dev server only; not a production secret |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No new packages; existing @playwright/test + helpers only |
</threat_model>

<verification>
After both tasks:
- `cd test/example/priv/playwright && npx playwright test admin-flow --project=chromium` exits 0 (all 3 flow specs pass)
- `npx playwright test admin-flow-support-investigator admin-flow-org-admin --project=mobile --list 2>&1 | grep admin-flow | wc -l` → 0
- admin-flow-org-admin.spec.ts contains `response?.status()` === 403 assertion
- admin-flow-org-admin.spec.ts contains `.not.toContainText('Acme Corp')` anti-enumeration assertion
- admin-flow-support-investigator.spec.ts contains `Impersonating alice@demo.vaultr.test` banner assertion
- `grep -c 'waitForTimeout\|sleep' test/example/priv/playwright/tests/admin-flow-support-investigator.spec.ts test/example/priv/playwright/tests/admin-flow-org-admin.spec.ts` → 0
</verification>

<success_criteria>
1. admin-flow-support-investigator.spec.ts passes all chromium tests. Covers: find→audit→impersonate→return journey (FLOW-01); banner persistence (D-12); keyboard (FLOW-02); reduced-motion (FLOW-02); theme across impersonation + reload (FLOW-03); deterministic alice/dave/frank fixture (DATA-01).
2. admin-flow-org-admin.spec.ts passes all chromium tests. Covers: org-scoped 200 access (FLOW-01); 403 permission-denied with anti-enumeration body (D-13); morgan empty audit boundary (.sg-empty-state) (FLOW-01/DATA-01); keyboard (FLOW-02); reduced-motion (FLOW-02); theme persistence + reload (FLOW-03).
3. Both specs excluded from mobile project (ADMIN_BEHAVIOR_SPECS fix from Plan 02).
4. No sleeps, no computed color assertions, no impersonation internals in investigator spec.
</success_criteria>

<output>
Create `.planning/phases/190-flows-fixture-data-l4/190-04-SUMMARY.md` when done
</output>
