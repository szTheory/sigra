---
phase: 190-flows-fixture-data-l4
plan: 05
type: execute
wave: 3
depends_on: ["190-03", "190-04"]
files_modified:
  - guides/reference/admin-quality-ledger.md
  - .planning/phases/190-flows-fixture-data-l4/190-VALIDATION.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, DATA-01]
must_haves:
  truths:
    - "guides/reference/admin-quality-ledger.md has exactly 3 new L4 rows appended below the 6 L3 rows"
    - "L4 rows: flow-platform-admin, flow-support-investigator, flow-org-admin — lowercase-item, Level=L4, bare-integer tier (1), link to spec file"
    - "Ledger rows parse correctly under quality-ledger-monotonic.sh (no parse error on new rows)"
    - "190-VALIDATION.md Per-Task Verification Map is fully populated with plan/wave/task IDs and status"
    - "ROADMAP.md Phase 190 Plans section is updated to list the 5 plan files"
    - "mix run priv/repo/seeds.exs exits 0 (seed is idempotent and reproducible)"
    - "D-05: DATA-01 satisfied by existing Example.Demo.Seeds.run/0; each flow case verified reproducible; enrichment only for genuine gaps; any enrichment seeds terminal state directly (no browser-driven setup), relative timestamps to @seed_reference_ts, idempotent on_conflict upserts"
    - "D-06: any seed enrichment lands only in test/example/lib/example/demo/{personas,seeds}.ex; no priv/templates/sigra.install/ touch; no byte-parity duty for seed data (187/189 three-surface parity governs shipped CSS/JS only)"
    - "Full chromium behavior lane exits 0 after all flow specs pass"
  artifacts:
    - path: "guides/reference/admin-quality-ledger.md"
      provides: "3 new L4 rows below the 6 L3 rows"
      contains: "flow-platform-admin"
    - path: ".planning/phases/190-flows-fixture-data-l4/190-VALIDATION.md"
      provides: "Populated Per-Task Verification Map with all FLOW/DATA requirement rows"
  key_links:
    - from: "quality-ledger-monotonic.sh"
      to: "admin-quality-ledger.md L4 rows"
      via: "^\| [a-z] parsing rule; bare integer tier"
      pattern: "flow-platform-admin.*L4.*1"
    - from: "190-VALIDATION.md"
      to: "190-01..05-PLAN.md tasks"
      via: "task ID mapping to requirements FLOW-01/02/03/DATA-01"
      pattern: "FLOW-01"
    - from: "mix run priv/repo/seeds.exs (D-05)"
      to: "Example.Demo.Seeds.run/0"
      via: "verifies each flow's three cases are reproducible; enriches only genuine gaps with terminal-state direct seeding"
      pattern: "D-05"
    - from: "any seed enrichment (D-06)"
      to: "test/example/lib/example/demo/{personas,seeds}.ex only"
      via: "no priv/templates/sigra.install/ touch; no byte-parity obligation for seed data"
      pattern: "D-06"
---

<objective>
Ratify the 3 L4 quality ledger rows, populate the VALIDATION.md Per-Task Verification Map, update the ROADMAP.md plan list for Phase 190, and confirm seed reproducibility — completing the phase ledger and evidence artifacts.

Purpose: The ledger rows are the machine-parseable, monotonic-guard-protected record that proves each operator flow achieved Tier 1. The VALIDATION.md map closes the Nyquist audit gate. The seed verification confirms DATA-01 is satisfied without any enrichment needed.

Output: Ledger with 3 appended L4 rows; populated VALIDATION.md; updated ROADMAP.md plan list.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190-flows-fixture-data-l4/190-CONTEXT.md
@.planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md
@.planning/phases/190-flows-fixture-data-l4/190-VALIDATION.md

<interfaces>
<!-- Existing ledger tail (last 6 rows are L3, from RESEARCH.md and live read): -->
<!-- | index-live | L3 | 1 | [admin-checkpoints global-overview — 3 projects × toHaveScreenshot + axe](...) | -->
<!-- | organization-live | L3 | 1 | [...] | -->
<!-- | users-index-live | L3 | 1 | [...] | -->
<!-- | user-show-live | L3 | 1 | [...]; [...] | -->
<!-- | audit-index-live | L3 | 1 | [...] | -->
<!-- | audit-user-live | L3 | 1 | [...] | -->
<!-- After these 6, append the 3 new L4 rows. -->
<!--  -->
<!-- Ledger parsing rules (quality-ledger-monotonic.sh): -->
<!--   Item must match ^\| [a-z] (lowercase-item) -->
<!--   Tier column (column 3 by pipe count) must be a bare integer: 0, 1, or 2 -->
<!--   NO decorators, spaces within the integer, or prefixed text like "Tier 1" -->
<!--  -->
<!-- L4 row format (from 190-PATTERNS.md): -->
<!-- | flow-platform-admin | L4 | 1 | [admin-flow-platform-admin.spec.ts — ...](../../test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts) | -->
<!-- | flow-support-investigator | L4 | 1 | [admin-flow-support-investigator.spec.ts — ...](../../test/example/priv/playwright/tests/admin-flow-support-investigator.spec.ts) | -->
<!-- | flow-org-admin | L4 | 1 | [admin-flow-org-admin.spec.ts — ...](../../test/example/priv/playwright/tests/admin-flow-org-admin.spec.ts) | -->
<!--  -->
<!-- Tier rationale (D-08 weakest-link): -->
<!--   All L3 pages are Tier 1. Flow-only criteria (happy/error/boundary coverage, -->
<!--   keyboard, reduced-motion, theme persistence, deterministic fixture) are exercised -->
<!--   and passing per Plans 03/04. Tier 1 = Ratified. No Tier 2 award-grade claim -->
<!--   without a separate adversarial polish review. -->
<!--  -->
<!-- ROADMAP.md Phase 190 Plans section (currently "Not yet planned"): -->
<!--   Update to list 5 plan files with wave structure -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 3 L4 rows to admin-quality-ledger.md and verify monotonic guard parses them</name>
  <files>guides/reference/admin-quality-ledger.md</files>
  <read_first>
    - guides/reference/admin-quality-ledger.md (full file — confirm the 6 L3 rows are the last rows; identify the exact append point)
    - scripts/ci/quality-ledger-monotonic.sh (understand the awk parsing rule: ^\| [a-z] + tier column extraction)
    - .planning/phases/190-flows-fixture-data-l4/190-PATTERNS.md (guides/reference/admin-quality-ledger.md section — exact row format with link text)
    - .planning/phases/190-flows-fixture-data-l4/190-CONTEXT.md (D-07: lowercase-item, single-integer-tier, ^\| [a-z] parseable, weakest-link bounded; D-08: tier explanation)
  </read_first>
  <action>
    Append exactly 3 rows to guides/reference/admin-quality-ledger.md immediately after the last L3 row (audit-user-live). Do NOT modify any existing rows.

    The 3 rows to append (use exact format — pipe-aligned markdown table row, lowercase item, bare integer tier):

    | flow-platform-admin | L4 | 1 | [admin-flow-platform-admin.spec.ts — platform admin JTBD: happy/error/boundary, scope/return-context, keyboard, reduced-motion, theme-persistence + reload](../../test/example/priv/playwright/tests/admin-flow-platform-admin.spec.ts) |
    | flow-support-investigator | L4 | 1 | [admin-flow-support-investigator.spec.ts — investigator posture: find→audit→impersonate→return, banner continuity, ConfirmDialog APG gates, theme](../../test/example/priv/playwright/tests/admin-flow-support-investigator.spec.ts) |
    | flow-org-admin | L4 | 1 | [admin-flow-org-admin.spec.ts — org admin JTBD: tenant-bounded access, 403 permission-denied, empty audit boundary, theme](../../test/example/priv/playwright/tests/admin-flow-org-admin.spec.ts) |

    Tier rationale per D-08 (document as a comment or footnote if the ledger has a rationale section): The L4 tier is weakest-link bounded by the constituent L3 pages (all Tier 1) and the flow-only criteria (happy/error/boundary coverage, keyboard, reduced-motion, theme persistence, deterministic fixture). All flow-only criteria are exercised and passing per Plans 03/04. Tier 1 = Ratified. No Tier 2 claim is made without a dedicated adversarial polish review.

    After appending, verify the monotonic guard script parses the new rows without error.
  </action>
  <verify>
    <automated>grep -c "flow-platform-admin\|flow-support-investigator\|flow-org-admin" guides/reference/admin-quality-ledger.md | grep "^3$" && echo "ROWS OK"</automated>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh 2>&1 | tail -5</automated>
    <automated>grep "^| flow-" guides/reference/admin-quality-ledger.md | awk -F'|' '{gsub(/ /,"",$3); print $3}' | grep -v "^[012]$" | wc -l | grep "^0$" && echo "TIER FORMAT OK"</automated>
  </verify>
  <done>
    - guides/reference/admin-quality-ledger.md contains exactly 3 new rows matching ^\| flow-
    - quality-ledger-monotonic.sh exits 0 (no parse error, no tier regression)
    - Tier column for all 3 new rows is a bare integer (1) passing the awk parser
    - All 3 rows appear after the 6 L3 rows (audit-user-live is the last L3 row)
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate 190-VALIDATION.md + update ROADMAP.md + verify seed reproducibility</name>
  <files>.planning/phases/190-flows-fixture-data-l4/190-VALIDATION.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/190-flows-fixture-data-l4/190-VALIDATION.md (current draft — the Per-Task Verification Map has TBD placeholders)
    - .planning/ROADMAP.md (Phase 190 section — "Plans: Not yet planned" to be updated)
    - .planning/phases/189-page-compositions-l3/189-VALIDATION.md (analog for a completed validation document — see how Per-Task Verification Map is filled)
    - .planning/phases/190-flows-fixture-data-l4/190-RESEARCH.md (Validation Architecture section — Phase Requirements → Test Map)
  </read_first>
  <action>
    Update 190-VALIDATION.md:
    1. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true` and `status: ratified`.
    2. Replace the Per-Task Verification Map TBD rows with concrete entries. One row per task per requirement. Map:
       - Plan 01 Task 1 + Task 2 (WR hardening + WR-04): FLOW-02 (keyboard APG conformance pre-conditions) — automated: `md5sum priv/templates/sigra.install/admin/admin_hooks.js test/example/assets/js/admin_hooks.js`
       - Plan 02 Task 1 (config fix): FLOW-01/02/03/DATA-01 (chromium-only lane) — automated: `cd test/example/priv/playwright && npx playwright test --list --project=mobile 2>&1 | grep admin-flow`
       - Plan 02 Task 2 (adminFlows.ts): FLOW-01/02/03/DATA-01 (shared helper foundation) — automated: `cd test/example/priv/playwright && npx tsc --noEmit helpers/adminFlows.ts`
       - Plan 03 Task 1 (platform admin spec): FLOW-01/02/03/DATA-01 — automated: `cd test/example/priv/playwright && npx playwright test admin-flow-platform-admin --project=chromium`
       - Plan 04 Task 1 (investigator spec): FLOW-01/02/03/DATA-01 — automated: `cd test/example/priv/playwright && npx playwright test admin-flow-support-investigator --project=chromium`
       - Plan 04 Task 2 (org admin spec): FLOW-01/02/03/DATA-01 — automated: `cd test/example/priv/playwright && npx playwright test admin-flow-org-admin --project=chromium`
       - Plan 05 Task 1 (ledger rows): FLOW-01/02/03/DATA-01 — automated: `bash scripts/ci/quality-ledger-monotonic.sh`
    3. Update Wave 0 Requirements section — mark all items as complete (checked).
    4. Update Validation Sign-Off — check all items.

    Update ROADMAP.md Phase 190 section:
    - Change "Plans: Not yet planned" to "Plans: 5 plans"
    - Add the Wave structure and plan list:
      Wave 1 (parallel):
      - [ ] 190-01-PLAN.md — WR-01/02/03 admin_hooks.js hardening + WR-04 branding_live changeset error mapping + data-sg-confirm-cancel attribute
      - [ ] 190-02-PLAN.md — playwright.config.ts ADMIN_BEHAVIOR_SPECS regex fix + helpers/adminFlows.ts shared flow utilities
      Wave 2 (parallel, blocked on Wave 1):
      - [ ] 190-03-PLAN.md — Platform admin JTBD flow spec (happy/error/boundary + keyboard + reduced-motion + theme)
      - [ ] 190-04-PLAN.md — Support investigator + org admin JTBD flow specs (impersonation journey + 403 permission-denied + empty audit boundary)
      Wave 3 (blocked on Wave 2):
      - [ ] 190-05-PLAN.md — Quality ledger 3 L4 rows + VALIDATION.md ratification + seed verification

    Seed verification: Run `mix run priv/repo/seeds.exs` in the project root to confirm the seed exits 0 and is idempotent. Document the result in the VALIDATION.md. If the seed already ran (detected by count threshold guard in seeds.ex), re-running should be a no-op. Do not add any new seed data — RESEARCH confirmed no enrichment is needed (morgan's zero audit events is the natural boundary case, already present).
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" .planning/phases/190-flows-fixture-data-l4/190-VALIDATION.md</automated>
    <automated>grep "190-01-PLAN.md\|190-02-PLAN.md\|190-03-PLAN.md\|190-04-PLAN.md\|190-05-PLAN.md" .planning/ROADMAP.md | wc -l | grep "^5$" && echo "ROADMAP OK"</automated>
    <automated>cd /Users/jon/projects/sigra && mix run priv/repo/seeds.exs 2>&1 | tail -3</automated>
    <automated>cd test/example/priv/playwright && npx playwright test --project=chromium admin-flow 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 190-VALIDATION.md has nyquist_compliant: true; Per-Task Verification Map is fully populated (no TBD cells); Wave 0 requirements all checked; Validation Sign-Off all checked
    - ROADMAP.md Phase 190 Plans section lists all 5 plan files with wave structure
    - mix run priv/repo/seeds.exs exits 0 (idempotent)
    - All 3 flow specs pass on chromium project
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quality-ledger-monotonic.sh → ledger | Guard script parses ledger rows and compares tier integers against base ref; incorrect row format causes guard to fail |
| mix run seeds → dev Postgres | Seed runs against dev database; MIX_ENV != test guard prevents accidental production execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-14 | Tampering | quality-ledger-monotonic.sh tier parsing | mitigate | Tier column is a bare integer (1) — no decorators; verified by awk parser test in task 1 verify block |
| T-190-15 | Tampering | Ledger monotonic guard (merge-blocking) | mitigate | quality-ledger-monotonic.sh exits 0 after appending L4 rows — new rows only add entries, never demote existing Tier 1 rows (append-only invariant) |
| T-190-16 | Denial of Service | mix run seeds.exs re-run | accept | Seed has MIX_ENV == :test raise-guard (priv/repo/seeds.exs line confirmed) and idempotent on_conflict: :nothing upserts; re-run is safe |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced |
</threat_model>

<verification>
Phase gate (run after this plan completes):
1. `bash scripts/ci/quality-ledger-monotonic.sh` exits 0
2. `grep -c "^| flow-" guides/reference/admin-quality-ledger.md` returns 3
3. `cd test/example/priv/playwright && npx playwright test --project=chromium 2>&1 | grep "admin-flow"` shows all passing
4. `mix test` exits 0 (no Elixir regressions from WR-01..04 changes)
5. `mix run priv/repo/seeds.exs` exits 0
6. `grep "nyquist_compliant: true" .planning/phases/190-flows-fixture-data-l4/190-VALIDATION.md` returns a match
</verification>

<success_criteria>
1. Quality ledger has exactly 3 new L4 rows (flow-platform-admin, flow-support-investigator, flow-org-admin) at Tier 1 with executable links, parseable by quality-ledger-monotonic.sh (exits 0).
2. 190-VALIDATION.md Per-Task Verification Map is fully populated with no TBD cells; nyquist_compliant: true.
3. ROADMAP.md Phase 190 Plans section lists all 5 plan files with correct wave structure.
4. mix run priv/repo/seeds.exs exits 0 (DATA-01 confirmed idempotent; no enrichment added — morgan's zero audit events is the natural boundary).
5. Full chromium behavior lane exits 0 for all 3 flow specs.
</success_criteria>

<output>
Create `.planning/phases/190-flows-fixture-data-l4/190-05-SUMMARY.md` when done
</output>
