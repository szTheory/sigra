---
phase: 81
plan: 03
type: execute
wave: 3
depends_on:
  - "81-02"
files_modified:
  - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
  - .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md
  - .planning/phases/09-audit-logging/09-VERIFICATION.md
  - .planning/phases/09-audit-logging/09-03-SUMMARY.md
  - CHANGELOG.md
  - .planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md
autonomous: true
requirements:
  - AUD-18-04
must_haves:
  truths:
    - "**AUD-04-048** / **049** rows describe **`Repo.transaction/1` + `Multi` + `log_multi_safe`** for **`api.jwt_refresh*`** with **phase 81** closure and **AUD-08** footnote for persistence-only deferral."
    - "**`81-VERIFICATION.md`** exists with merge-gate checklist rows for this phase."
nyquist_compliant: false
---

<objective>
Implement **AUD-18-04**: surgical planning-truth alignment of **44** / **45** / **09** inventories and **`CHANGELOG.md` [Unreleased]** with **`lib/sigra/api_token.ex`** after plans **81-01**/**81-02** land; create **`.planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md`** recording the merge gate per **81-CONTEXT.md** D-81-04.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-81-03 | Repudiation | Inventories still claim **`log_safe`** for 048/049 after **Multi** migration | high | Replace mechanism cells and add **T1 (phase 81)** scoped footnote — **not** **AUD-08** persistence |
| T-81-04 | Information disclosure | CHANGELOG implies JWT persistence shipped | medium | One **\[Unreleased\]** bullet: **audit-row atomicity** for refresh/reuse only |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Update 44-AUD-04-INVENTORY rows AUD-04-048 / 049</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md (rows **AUD-04-048**, **AUD-04-049**)
    - lib/sigra/api_token.ex (`commit_api_token_jwt_audit` / wrappers — final line refs)
    - .planning/phases/81-jwt-refresh-audit-atomicity/81-CONTEXT.md (D-81-04)
  </read_first>
  <action>
    For table rows **AUD-04-048** and **AUD-04-049**, set **`mechanism today`** to **`Repo.transaction/1` + `Ecto.Multi` + `Audit.log_multi_safe`** (or equivalent concise string used elsewhere in the file for **Multi**-bound rows). Update **`notes`** / **`Phase`** to cite **phase 81** closure. Add a **short footnote** in the **notes** cell: **T1** here means **audit-row durability** for **`api.jwt_refresh*`**; **AUD-08** (JWT refresh-token **persistence** co-fate with audit) remains **deferred** — do **not** mark persistence work done.

    If **`## Grep log`** exists, refresh the fenced block with fresh **`rg -n "Sigra\\.Audit\\.(log_safe|log_multi_safe)" lib/sigra/api_token.ex`** output from repo root inside a **` ```text `** fence.
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-048' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` shows **`log_multi_safe`** on the same line.
    - `grep -nF 'AUD-04-048' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` does **not** match **`log_safe`** as the sole mechanism for that row (pipe cell may mention **`log_safe`** only in historical context — prefer **no** bare **`log_safe` |** mechanism for 048).
    - `grep -nF 'AUD-04-049' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` shows **`log_multi_safe`** on the same line.
  </acceptance_criteria>
  <verify>
    `wc -l .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` is a number **≥ 30**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Update 45-AUD-04-INVENTORY + EX-45-JWT appendix if needed</name>
  <files>.planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md (search **`048`**, **`049`**, **`EX-45-JWT`**)
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md (post task 1 — stay consistent)
  </read_first>
  <action>
    Align **048**/**049** (and any **EX-45-JWT-*** subsection that references **`log_safe`** for these actions) with **phase 81** **Multi** implementation. Prefer **additive dated clarification** over deleting history. Ensure cross-links do not imply **AUD-08** is closed by phase 81.
  </action>
  <acceptance_criteria>
    - `grep -nE "AUD-04-048|AUD-04-049" .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md | head -5` exits **0**.
    - At least one matched line for **048** or **049** contains **`81`** or **`log_multi_safe`** (phase pointer or mechanism).
  </acceptance_criteria>
  <verify>
    `grep -c "phase 81" .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md` returns **≥ 1** OR **`grep -c "log_multi_safe" .planning/phases/45-oauth-ops-c1-signoff/45-AUD-04-INVENTORY.md`** returns **≥ 1**.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Update 09-VERIFICATION C-1 rows AUD-04-048 / 049 + 09-03-SUMMARY note</name>
  <files>.planning/phases/09-audit-logging/09-VERIFICATION.md, .planning/phases/09-audit-logging/09-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-VERIFICATION.md (lines around **AUD-04-048** / **049**)
    - .planning/phases/09-audit-logging/09-03-SUMMARY.md (search **`048`**, **`049`**, **`jwt`**, **`81`**)
    - test/sigra/api_token_audit_atomic_test.exs (evidence pointer)
  </read_first>
  <action>
    Replace **C-1** table lines **AUD-04-048** and **AUD-04-049** so **mechanism** reads **`Repo.transaction/1` + `Multi` + `log_multi_safe`** (or file-consistent phrasing), **verdict** **T1 (phase 81; audit-only Multi)** with evidence **`test/sigra/api_token_audit_atomic_test.exs`**, and **implementation pointer** **`lib/sigra/api_token.ex`** (function names **`audit_jwt_refresh`**, **`audit_jwt_refresh_reuse`**). Remove **“Deferred to phase 45 / AUD-08”** as the **primary** verdict for these rows; if **AUD-08** must be mentioned, keep it as a **footnote** that persistence co-fate is **still out of scope**.

    In **`09-03-SUMMARY.md`**, add or update **one** short bounded-batch / **phase 81** note tying **048–049** closure to **AUD-18** without claiming **AUD-08**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-048' .planning/phases/09-audit-logging/09-VERIFICATION.md` matches a line containing **`log_multi_safe`**.
    - `grep -nF 'AUD-04-049' .planning/phases/09-audit-logging/09-VERIFICATION.md` matches a line containing **`log_multi_safe`**.
    - `grep -nE "048|049|81|AUD-18" .planning/phases/09-audit-logging/09-03-SUMMARY.md | head -3` exits **0**.
  </acceptance_criteria>
  <verify>
    `grep -c "AUD-04-048" .planning/phases/09-audit-logging/09-VERIFICATION.md` equals **1** and same for **`AUD-04-049`**.
  </verify>
</task>

<task type="execute">
  <name>Task 4: CHANGELOG + 81-VERIFICATION.md</name>
  <files>CHANGELOG.md, .planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md</files>
  <read_first>
    - CHANGELOG.md (`[Unreleased]` section heading)
    - .planning/phases/73-bounded-audit-atomicity-batch/73-VERIFICATION.md (format reference if present) OR **49**/**48** phase `*-VERIFICATION.md` for table style
  </read_first>
  <action>
    1. Under **`[Unreleased]`**, add **one** bullet describing **audit atomicity** for **`api.jwt_refresh`** / **`api.jwt_refresh_reuse`** via **`Repo.transaction` + `Multi` + `log_multi_safe`** when audit is enabled — **no** claim about refresh-token storage **AUD-08**.

    2. Create **`.planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md`** with: title **Phase 81 verification**, table columns **Check | Evidence | Status**, rows including **`mix test test/sigra/api_token_audit_atomic_test.exs`**, **`AUD-04-048/049` planning truth**, **`CHANGELOG` bullet**, and a **Maintainer sign-off** row left **pending** until human merge.
  </action>
  <acceptance_criteria>
    - `grep -nF "api.jwt_refresh" CHANGELOG.md` exits **0** in the **`[Unreleased]`** section (use **`grep -n "\\[Unreleased\\]" CHANGELOG.md`** first to confirm line number context if needed).
    - `test -f .planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md` is true (shell test).
    - `grep -nF "AUD-04-048" .planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md` exits **0**.
  </acceptance_criteria>
  <verify>
    `wc -l .planning/phases/81-jwt-refresh-audit-atomicity/81-VERIFICATION.md` returns a number **≥ 15**.
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/api_token_audit_atomic_test.exs` exits **0** (regression guard after doc edits should not break code — run once).
</verification>

<success_criteria>
- **AUD-18-04** satisfied; planning artifacts match **`lib/sigra/api_token.ex`** for **048–049**.
</success_criteria>
