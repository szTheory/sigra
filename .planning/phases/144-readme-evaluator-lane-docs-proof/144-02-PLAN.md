---
phase: 144-readme-evaluator-lane-docs-proof
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/assets/demo-credentials-demo-showcase-chromium.png
  - guides/assets/admin-user-detail-demo-showcase-chromium.png
  - guides/assets/admin-user-list-demo-showcase-chromium.png
  - guides/assets/audit-explorer-demo-showcase-chromium.png
  - guides/introduction/demo-showcase.md
  - mix.exs
  - docs/ga-evidence.md
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "guides/assets/ directory exists with 4 PNG files committed in git (D-08, D-12)"
    - "guides/introduction/demo-showcase.md exists with the 7-section structure from D-11"
    - "Guide uses feature-organized sections (not step-by-step numbered tutorial) — evaluators can skip to any section non-linearly (D-09)"
    - "All 6 personas featured but not equally: Admin full section, Alice briefly as baseline, Bob implicit via TOTP, Carol OAuth callout, Dave+Frank grouped Rough Edges (D-15)"
    - "All 4 screenshots embedded in the guide using assets/filename.png format (no leading slash, no ../guides/assets/ prefix) (D-12)"
    - "mix.exs docs/0 has assets: %{\"guides/assets\" => \"assets\"} immediately after formatters line (D-12)"
    - "mix.exs extras list contains guides/introduction/demo-showcase.md after suite-integration.md (D-13)"
    - "Carol's OAuth section is honest — seeded identity row only, live OAuth requires real GitHub credentials (D-14)"
    - "docs/ga-evidence.md has one new bullet under Where to read next pointing to 144-VERIFICATION.md (D-18)"
    - "mix docs --warnings-as-errors exits 0 after all changes are committed (Gates 5 and 6 prereq)"
  artifacts:
    - path: "guides/introduction/demo-showcase.md"
      provides: "Evaluator guide with embedded screenshots, feature-organized sections"
      contains: "Demo Showcase — Vaultr Example App"
    - path: "guides/assets/demo-credentials-demo-showcase-chromium.png"
      provides: "Credentials cheat-sheet screenshot"
    - path: "guides/assets/admin-user-detail-demo-showcase-chromium.png"
      provides: "Admin user detail screenshot"
    - path: "guides/assets/admin-user-list-demo-showcase-chromium.png"
      provides: "Admin user list screenshot"
    - path: "guides/assets/audit-explorer-demo-showcase-chromium.png"
      provides: "Audit log explorer screenshot"
    - path: "mix.exs"
      provides: "ExDoc assets config + extras registration"
      contains: "assets: %{\"guides/assets\" => \"assets\"}"
  key_links:
    - from: "guides/introduction/demo-showcase.md image references"
      to: "guides/assets/*.png"
      via: "ExDoc :assets copy (guides/assets → assets/ in output)"
      pattern: "assets/.*-demo-showcase-chromium.png"
    - from: "mix.exs extras list"
      to: "guides/introduction/demo-showcase.md"
      via: "ExDoc extras registration"
      pattern: "demo-showcase.md"
    - from: "docs/ga-evidence.md Where to read next"
      to: ".planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md"
      via: "markdown bullet link"
      pattern: "DEMO-SHOWCASE proof bundle"
---

<objective>
Create `guides/assets/` with the 4 Phase-143 screenshots copied in, write `guides/introduction/demo-showcase.md` with the locked 7-section structure and all 4 embedded screenshots, update `mix.exs` docs/0 with the `:assets` key and extras entry, and add one pointer bullet to `docs/ga-evidence.md`.

Purpose: DOC-02 requires a discoverable guide that walks an evaluator through the Vaultr demo with visual proof. The guide must be wired into ExDoc extras so it appears in Hexdocs navigation, and images must be served via the `:assets` config so they render in generated docs.

Output: `guides/assets/` (4 PNGs), `guides/introduction/demo-showcase.md`, updated `mix.exs`, updated `docs/ga-evidence.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-CONTEXT.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-RESEARCH.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy screenshots to guides/assets/ and update mix.exs ExDoc config</name>
  <files>
    guides/assets/demo-credentials-demo-showcase-chromium.png
    guides/assets/admin-user-detail-demo-showcase-chromium.png
    guides/assets/admin-user-list-demo-showcase-chromium.png
    guides/assets/audit-explorer-demo-showcase-chromium.png
    mix.exs
  </files>

  <read_first>
    Read `mix.exs` lines 161–248 (the `docs/0` function) to confirm the exact current state.
    Specifically confirm:
    - Line 184: `formatters: ["html", "markdown"],` — the `:assets` key goes IMMEDIATELY after this line
    - Lines 204–205: `"guides/introduction/suite-integration.md",` followed by `"guides/flows/registration.md"` — the new extras entry goes between these two lines
    - No `:assets` key currently exists in the list (confirmed in RESEARCH.md — verify anyway)

    Verify the 4 source PNG files exist before copying:
    ls -la test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/*.png
    Expected: 4 files with these names:
      demo-credentials-demo-showcase-chromium.png (78,521 bytes)
      admin-user-detail-demo-showcase-chromium.png (102,157 bytes)
      admin-user-list-demo-showcase-chromium.png (120,406 bytes)
      audit-explorer-demo-showcase-chromium.png (84,582 bytes)
    If any PNG is missing, stop and report — do not proceed with missing source files.
  </read_first>

  <action>
    Step 1 — Create `guides/assets/` directory and copy the 4 PNGs using shell cp commands.
    Source directory: `test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/`
    Destination directory: `guides/assets/`

    Copy each file individually (preserving exact filenames):
      demo-credentials-demo-showcase-chromium.png
      admin-user-detail-demo-showcase-chromium.png
      admin-user-list-demo-showcase-chromium.png
      audit-explorer-demo-showcase-chromium.png

    The copies must be committed in git (not gitignored). Verify with:
      ls -la guides/assets/*.png
    Expected: 4 PNG files listed.

    Step 2 — Edit `mix.exs` with two targeted changes to the `docs/0` function:

    Change 1: Add `:assets` key immediately after the `formatters` line (line 184).
    Insert this exact line after `formatters: ["html", "markdown"],`:
      assets: %{"guides/assets" => "assets"},
    Position: between `formatters: ["html", "markdown"],` and `extras: [`

    Change 2: Add the extras list entry after `"guides/introduction/suite-integration.md"` (line 204).
    Insert this exact string after that line:
      "guides/introduction/demo-showcase.md",
    Position: the new line goes between `"guides/introduction/suite-integration.md",` and
    `"guides/flows/registration.md",`

    Do NOT change `groups_for_extras` — the existing `Introduction: ~r{guides/introduction/.?}` regex
    already covers `demo-showcase.md` without any modification.

    Do NOT add any other changes to mix.exs.
  </action>

  <verify>
    <automated>ls -la guides/assets/*.png && grep -c "guides/assets" mix.exs</automated>
  </verify>

  <acceptance_criteria>
    - `ls guides/assets/demo-credentials-demo-showcase-chromium.png` exits 0
    - `ls guides/assets/admin-user-detail-demo-showcase-chromium.png` exits 0
    - `ls guides/assets/admin-user-list-demo-showcase-chromium.png` exits 0
    - `ls guides/assets/audit-explorer-demo-showcase-chromium.png` exits 0
    - `grep -q 'assets: %{"guides/assets" => "assets"}' mix.exs` exits 0
    - `grep -q '"guides/introduction/demo-showcase.md"' mix.exs` exits 0
    - `grep -A1 '"guides/introduction/suite-integration.md"' mix.exs` shows `demo-showcase.md` as the immediately following line
    - `grep -A1 'formatters: \["html", "markdown"\]' mix.exs` shows the `assets:` key as the next non-blank line
    - `grep -q 'groups_for_extras' mix.exs` exits 0 and the groups_for_extras section is unchanged (no new group added)
  </acceptance_criteria>

  <done>
    `guides/assets/` contains all 4 PNGs copied from the Playwright snapshot directory. `mix.exs`
    has the `:assets` key immediately after the `formatters` line and `demo-showcase.md` in the
    extras list after `suite-integration.md`. No other changes to mix.exs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write guides/introduction/demo-showcase.md and add ga-evidence.md pointer</name>
  <files>
    guides/introduction/demo-showcase.md
    docs/ga-evidence.md
  </files>

  <read_first>
    Read `guides/introduction/suite-integration.md` in full — it is the exact format analog.
    Note: no `<!-- validated_against: -->` HTML comment, no YAML frontmatter, H1 title at top,
    prose-first orientation, cross-links use `.html` extension for ExDoc-rendered pages, and
    "What's Next" section uses bolded link list.

    Read `docs/ga-evidence.md` in full to see the exact current "Where to read next" bullet list
    format. The new bullet must match the existing bullet style exactly (markdown list item,
    link label, relative repo path in parentheses).

    Read `test/example/lib/example/demo/personas.ex` lines 126–135 (`feature_map/0`) for the
    exact feature description text used in the guide's persona coverage sections. Reference these
    descriptions in the guide prose, not invented text.

    Verify the 4 PNGs now exist in `guides/assets/` (Task 1 must be complete before this task
    writes the guide — the guide references assets/filename.png which must be present).
  </read_first>

  <action>
    Write `guides/introduction/demo-showcase.md` with the locked 7-section structure from D-11.
    The file must have NO YAML frontmatter, NO `<!-- validated_against: -->` HTML comment.

    The exact section skeleton (headings are locked per D-11; prose within sections is Claude's
    discretion):

    H1: "# Demo Showcase — Vaultr Example App"

    Orientation paragraph (2 sentences): What Vaultr is (Sigra's seeded showcase Phoenix app) and
    what the reader will see in this guide.

    H2: "## Running the Demo"
    Content: `cd test/example` then `mix setup && mix phx.server`, then visit
    http://localhost:4000. Link to `test/example/README.md` for prerequisites. Keep brief — this
    section is a pointer to the README, not a duplicate.

    H2: "## Credentials Cheat-Sheet"
    Lead with screenshot (payoff before explanation per D-10):
      ![Credentials cheat-sheet showing all six demo persona emails and passwords](assets/demo-credentials-demo-showcase-chromium.png)
    Prose: the `/demo/credentials` page lists all six personas while the server is running.

    H2: "## Admin: Platform-Admin View"
    Lead with admin-user-detail screenshot (payoff first per D-10):
      ![Admin user detail showing TOTP MFA enrollment and passkey display row](assets/admin-user-detail-demo-showcase-chromium.png)
    Then admin-user-list screenshot:
      ![Admin user list showing all six demo personas](assets/admin-user-list-demo-showcase-chromium.png)
    Prose: what the admin persona demonstrates (TOTP MFA, passkey display row, multi-org membership,
    rich audit trail). Alice briefly mentioned as the standard confirmed user baseline.
    Bob covered as a second TOTP user / org owner.

    H2: "## Audit Log"
    Lead with screenshot (payoff first per D-10):
      ![Audit log explorer showing six or more distinct event types](assets/audit-explorer-demo-showcase-chromium.png)
    Prose: the /admin/audit route shows seeded events covering distinct event types, so the log
    reads as a live system rather than an empty scaffold.

    H2: "## Rough Edges: Locked and Scheduled-Deletion Accounts"
    No screenshot for this section. Prose covers:
    - Dave: account locked AND unconfirmed — try the wrong password to see the enumeration-resistant
      response; unlock via /admin/users as admin.
    - Frank: scheduled_deletion_at is set — account is still active; inspect via /admin/users as admin.
    These are the same trigger instructions as the README rough-edge callouts (D-05 applies here too).

    H2: "## OAuth Identity"
    Prose about Carol: the seeded GitHub identity row is visible in the admin user-detail page.
    HONEST framing per D-14: the live OAuth flow requires real GitHub credentials and does not work
    in the demo out of the box. Say exactly that — do not imply end-to-end OAuth works.

    H2: "## What's Next"
    Bullet list with bolded link labels and `.html` extension for ExDoc cross-links:
    - [Installation](installation.html) guide
    - [Getting started](getting-started.html) guide
    - [MFA guide](../flows/mfa.html) — TOTP enrollment and backup codes
    - [Full Sigra documentation](https://hexdocs.pm/sigra) on Hexdocs

    CRITICAL image path rule (D-12, RESEARCH.md Common Pitfalls):
    ALL image references MUST use exactly `assets/filename.png` format.
    NEVER use `../guides/assets/`, `/assets/`, `guides/assets/`, or any other prefix.
    NEVER use a leading slash.

    After writing demo-showcase.md, edit `docs/ga-evidence.md` to append one bullet line at the
    end of the existing "Where to read next" bullet list (after the `[Security policy](security.html)`
    line). The new line must be:
      - [v1.31 DEMO-SHOWCASE proof bundle (planning-internal)](.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md)

    This is the ONLY change to ga-evidence.md. Do NOT add a new section header, do NOT add
    verbatim gate output, do NOT add any other content.
  </action>

  <verify>
    <automated>grep -c "demo-showcase-chromium" guides/introduction/demo-showcase.md</automated>
  </verify>

  <acceptance_criteria>
    - `ls guides/introduction/demo-showcase.md` exits 0
    - `grep -q "Demo Showcase — Vaultr Example App" guides/introduction/demo-showcase.md` exits 0
    - `grep -c "assets/.*demo-showcase-chromium.png" guides/introduction/demo-showcase.md` outputs 4 (all 4 screenshots referenced)
    - `grep -q "assets/demo-credentials-demo-showcase-chromium.png" guides/introduction/demo-showcase.md` exits 0
    - `grep -q "assets/admin-user-detail-demo-showcase-chromium.png" guides/introduction/demo-showcase.md` exits 0
    - `grep -q "assets/admin-user-list-demo-showcase-chromium.png" guides/introduction/demo-showcase.md` exits 0
    - `grep -q "assets/audit-explorer-demo-showcase-chromium.png" guides/introduction/demo-showcase.md` exits 0
    - `grep -qv "guides/assets/" guides/introduction/demo-showcase.md` (no `guides/assets/` prefix in image refs)
    - Guide contains all 7 section headings from D-11: "Running the Demo", "Credentials Cheat-Sheet", "Admin: Platform-Admin View", "Audit Log", "Rough Edges: Locked and Scheduled-Deletion Accounts", "OAuth Identity", "What's Next"
    - Carol's section explicitly states the live OAuth flow requires real GitHub credentials (honest framing per D-14)
    - `grep -q "DEMO-SHOWCASE proof bundle" docs/ga-evidence.md` exits 0
    - `grep -q "144-VERIFICATION.md" docs/ga-evidence.md` exits 0
    - `grep -c "## " docs/ga-evidence.md` count is unchanged (no new section header added to ga-evidence.md)
    - `mix docs --warnings-as-errors` exits 0 (guide wired, assets present, no broken refs)
  </acceptance_criteria>

  <done>
    `guides/introduction/demo-showcase.md` exists with the 7-section structure, 4 embedded
    screenshots using `assets/filename.png` format, honest Carol OAuth section, and Dave/Frank
    rough-edge callouts with trigger instructions. `docs/ga-evidence.md` has one new bullet under
    "Where to read next" pointing to `144-VERIFICATION.md`. `mix docs --warnings-as-errors` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guides/assets/ PNG files | Static binary files — no executable code; copied from git-committed source |
| mix.exs docs/0 config | Build configuration — affects ExDoc output only; no runtime security surface |
| demo-showcase.md guide | Static documentation — no input handling, no auth paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-02-01 | Tampering | guides/assets/ PNG source integrity | accept | Source PNGs already committed in git at Phase 143 HEAD; cp is a git-tracked operation; no external download |
| T-144-02-02 | Information Disclosure | demo-showcase.md Carol OAuth section | mitigate | D-14 mandates honest framing — live OAuth requires real GitHub credentials; guide must not imply end-to-end OAuth works; executor must include this disclaimer explicitly |
| T-144-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; file copy + documentation authoring only |

**No new attack surface.** This plan copies static image files (already in git) and writes documentation. The only ExDoc config change (`:assets` key) affects doc build output only and introduces no runtime behavior change in the library.
</threat_model>

<verification>
After both tasks complete:

1. All 4 PNGs in guides/assets/:
   ls -la guides/assets/*.png

2. All 4 screenshots referenced in guide:
   grep -c "demo-showcase-chromium" guides/introduction/demo-showcase.md

3. mix.exs has both additions:
   grep "guides/assets" mix.exs
   grep "demo-showcase.md" mix.exs

4. ga-evidence.md pointer present:
   grep "DEMO-SHOWCASE proof bundle" docs/ga-evidence.md

5. ExDoc builds clean (guide wired + assets present + no broken refs):
   mix docs --warnings-as-errors
</verification>

<success_criteria>
- DOC-02 SC#2 satisfied: `guides/introduction/demo-showcase.md` exists with 7-section structure, all 4 screenshots embedded, wired into ExDoc extras, and `mix docs --warnings-as-errors` exits 0.
- DOC-03 partial (pointer line): `docs/ga-evidence.md` has exactly one new bullet under "Where to read next" pointing to `144-VERIFICATION.md`.
- Image references use exactly `assets/filename.png` (no wrong-prefix variants).
- Carol's OAuth section is honest — does not imply live OAuth flow works end-to-end.
</success_criteria>

<output>
Create `.planning/phases/144-readme-evaluator-lane-docs-proof/144-02-SUMMARY.md` when done.
</output>
