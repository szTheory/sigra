---
phase: 144-readme-evaluator-lane-docs-proof
plan: "03"
type: execute
wave: 2
depends_on:
  - "144-01"
  - "144-02"
files_modified:
  - .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "144-VERIFICATION.md filed with canonical YAML frontmatter (phase / verified / status / score / overrides_applied) following Phase 140 format (D-16)"
    - "Gate 1 (mix test) run and result recorded verbatim — test count, failure count, exit code (D-17)"
    - "Gate 2 (dep-off lane: unlock/clean/compile/test/restore) all sub-steps run and recorded; mix.lock restored after (D-17)"
    - "Gate 3 (cd test/example && mix ecto.drop && mix ecto.create && mix ecto.migrate && mix run priv/repo/seeds.exs) run from test/example/ with ecto.drop first; each sub-step recorded separately (D-17)"
    - "Gate 4 (ls -la test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/*.png) run and file listing recorded verbatim — must show 4 PNGs (D-17)"
    - "Gate 5 (grep -r 'demo-showcase-chromium' guides/introduction/demo-showcase.md) run and output recorded verbatim — must show 4 matches (D-17)"
    - "Gate 6 (mix docs --warnings-as-errors) run and exit code recorded verbatim — must exit 0 (D-17)"
    - "No assumed-green results — every gate records actual verbatim output (anti-overclaim per Phase 140 pattern)"
  artifacts:
    - path: ".planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md"
      provides: "Six-gate proof bundle with verbatim results"
      contains: "phase: 144-readme-evaluator-lane-docs-proof"
  key_links:
    - from: "144-VERIFICATION.md status field"
      to: "DOC-03 requirement"
      via: "all 6 gates run and results recorded"
      pattern: "status: passed"
    - from: "Gate 6 mix docs --warnings-as-errors"
      to: "guides/introduction/demo-showcase.md + guides/assets/ + mix.exs"
      via: "ExDoc build validates wiring"
      pattern: "exit code 0"
---

<objective>
Run all 6 proof gates against the repo HEAD (with Plans 01 and 02 committed), record actual output verbatim, and file `144-VERIFICATION.md` using the canonical Phase 140 proof-bundle format.

Purpose: DOC-03 requires a proof bundle confirming: full test suite green, dep-off CI lane green, `mix setup` works from a clean state, screenshots are committed, screenshots are referenced in the guide, and `mix docs --warnings-as-errors` passes. This plan closes DOC-03 and completes the v1.31 DEMO-SHOWCASE milestone.

This plan MUST run in Wave 2 — after Plans 01 and 02 are committed — because Gate 5 (screenshot guide references) depends on the demo-showcase.md from Plan 02 and Gate 6 (`mix docs --warnings-as-errors`) depends on the mix.exs `:assets` config and extras entry from Plan 02.

Output: `.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md` with six-gate results recorded verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-CONTEXT.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-RESEARCH.md
@.planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Gates 1–3 (full suite, dep-off lane, clean-state setup)</name>
  <files>.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md</files>

  <read_first>
    Read `.planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md`
    in full to internalize the EXACT format: YAML frontmatter, Result section, Goal Achievement
    table, Behavioral Spot-Checks table, Requirements Coverage, Anti-Overclaim Scan, Gaps Summary.
    Every section heading, table column, and row format must be reproduced exactly — 140-VERIFICATION.md
    is the canonical template.

    Verify Plans 01 and 02 are committed before running any gate (git log --oneline -5 to confirm
    recent commits include the README replacement and guide/mix.exs changes).

    Confirm `test/example/README.md` is NOT the Phoenix scaffold:
      grep -q "Try it locally" test/example/README.md

    Confirm `guides/introduction/demo-showcase.md` exists:
      ls guides/introduction/demo-showcase.md

    Confirm `guides/assets/` has all 4 PNGs:
      ls guides/assets/*.png
  </read_first>

  <action>
    Run Gates 1, 2, and 3 sequentially. Record actual verbatim output for each.
    Do NOT assume any gate is green — run every command and record what it returns.

    Gate 1: Full library suite
      Command: mix test
      (CLAUDE.md says `mix test` is sufficient — MIX_ENV=test and postgres credentials
      default to postgres/postgres/localhost from SIGRA_TEST_PG_* vars which default correctly)
      Record: test count, doctest count, property count, failure count, elapsed time, exit code.
      Expected: 0 failures, exit 0. If pre-existing failures exist (e.g., Xcode license install
      tests), record verbatim as FINDING (non-blocking) exactly as Phase 140 did — never suppress.

    Gate 2: Dep-off lane (5 sub-steps; MUST restore mix.lock after)
      Step 2a: mix deps.unlock threadline
        Record: output line ("Unlocked deps: threadline"), exit code.
      Step 2b: mix deps.clean threadline --build
        Record: output line ("Cleaning threadline"), exit code.
      Step 2c: MIX_ENV=test mix compile --warnings-as-errors --no-deps-check
        Record: exit code, any warnings (expect 0 warnings, exit 0).
      Step 2d: mix test --exclude requires_threadline --no-deps-check
        Record: test count, excluded count (expect 6 excluded), failure count, elapsed, exit code.
        Expected: 0 non-excluded failures, exit 0. Same Xcode FINDING protocol as Gate 1 applies.
      Step 2e: mix deps.get    (MANDATORY RESTORE — do not skip)
        Record: output line (threadline version restored), exit code.
        Verify: grep "threadline" mix.lock exits 0 (threadline is back in lock file).

    Gate 3: Clean-state mix setup from test/example/ (D-17 — MUST include ecto.drop)
      Working directory for ALL sub-steps: test/example/ (NOT the repo root)
      Step 3a: mix ecto.drop
        Record: output, exit code. If DB did not exist, note "Database does not exist" as acceptable.
      Step 3b: mix ecto.create
        Record: output ("The database example_dev has been created" or similar), exit code.
      Step 3c: mix ecto.migrate
        Record: migration count and any output, exit code.
      Step 3d: mix run priv/repo/seeds.exs
        Record: seed output (persona rows inserted, credentials summary block printed), exit code.
      After all sub-steps, cd back to repo root.
      Note: the `mix setup` alias in test/example/ runs ecto.create + ecto.migrate + seeds;
      for proof purposes Gate 3 runs each step explicitly with ecto.drop first (Gate 3 sub-steps
      are the explicit proof, not the alias — D-17 requires this).

    After running Gates 1–3, begin drafting 144-VERIFICATION.md using 140-VERIFICATION.md as the
    verbatim format template. Fill in YAML frontmatter and populate Gate 1–3 rows in the Behavioral
    Spot-Checks table with actual measured results. Leave Gates 4–6 rows as placeholders until
    Task 2 completes them.

    CRITICAL anti-overclaim rule: Never record expected or assumed output in place of actual output.
    Record what each gate actually returned, including test counts, elapsed times, and exit codes.
    Pre-existing failures (e.g., Xcode license issue) are labeled FINDING with explanation, not suppressed.
  </action>

  <verify>
    <automated>grep -q "threadline" mix.lock</automated>
  </verify>

  <acceptance_criteria>
    - Gate 1 run and result recorded verbatim (test count, failure count, exit code — not placeholders)
    - Gate 2 all 5 sub-steps run and recorded; mix.lock restored (`grep "threadline" mix.lock` exits 0)
    - Gate 3 all 4 sub-steps run from test/example/ with ecto.drop first; each sub-step result recorded separately
    - `mix.lock` contains threadline after Gate 2e restore
    - Draft 144-VERIFICATION.md exists with YAML frontmatter and Gate 1–3 rows filled with actual results
    - No Gate row in the draft shows placeholder text ("expected output", "TBD", "PENDING") in the Result column
  </acceptance_criteria>

  <done>
    Gates 1, 2, and 3 all run with actual output recorded. mix.lock is restored after the dep-off
    lane. Draft 144-VERIFICATION.md has YAML frontmatter and Gate 1–3 Behavioral Spot-Checks rows
    populated with verbatim results.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Gates 4–6, complete 144-VERIFICATION.md, file it</name>
  <files>.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md</files>

  <read_first>
    Read the draft 144-VERIFICATION.md created in Task 1 to see current state and which rows
    need Gate 4–6 results added.

    Read `.planning/phases/140-deprecation-hygiene-verification-docs-close/140-VERIFICATION.md`
    sections "Requirements Coverage", "Anti-Overclaim Scan", and "Gaps Summary" — reproduce the
    same section structure and level of detail in 144-VERIFICATION.md.
  </read_first>

  <action>
    Run Gates 4, 5, and 6. Record actual verbatim output. Then finalize 144-VERIFICATION.md.

    Gate 4: Screenshots committed
      Command: ls -la test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/*.png
      Record: complete file listing verbatim (all 4 PNG filenames, sizes, dates).
      Expected: 4 PNGs listed. If any PNG is missing → FINDING (non-blocking; note which PNG absent).

    Gate 5: Screenshots referenced in guide
      Command: grep -r "demo-showcase-chromium" guides/introduction/demo-showcase.md
      Record: grep output verbatim (all matched lines, or "no matches found" if none).
      Expected: exactly 4 matches (one per PNG). If fewer than 4 → FINDING.

    Gate 6: ExDoc clean build
      Command: mix docs --warnings-as-errors
      Record: exit code verbatim and key output lines (e.g., "Generating docs...", "View html docs",
      "View markdown docs").
      Expected: exit 0.
      If exit non-zero: record the warning/error output verbatim. Do NOT apply any Rule 1 auto-fix
      silently — if a fix is needed, describe it in the Anti-Overclaim Scan and record the gate
      result as FINDING before the fix and PASS after the fix (and document what was fixed).

    After all six gates have actual results:

    Complete 144-VERIFICATION.md following the exact 140-VERIFICATION.md structure. All sections:

    YAML frontmatter:
      phase: 144-readme-evaluator-lane-docs-proof
      verified: [actual ISO 8601 timestamp from when gates were run]
      status: passed (if all hard gates green; "failed" or "partial" if any hard gate failed)
      score: X/6 hard gates PASS (count only PASS gates — FINDING rows do not count as PASS)
      overrides_applied: 0 (if no waivers applied)
      gaps: [] (or list pre-existing findings that are non-blocking)
      deferred: []
      human_verification: []

    Sections (copy structure from 140-VERIFICATION.md):
    - "# Phase 144: README Evaluator Lane & Docs/Proof — Verification Report"
    - Header lines: Phase Goal, Verified, Status, Re-verification
    - "## Result" — 2–3 sentence narrative of what passed and what (if anything) is a pre-existing FINDING
    - "## Goal Achievement" — table with observable truth rows from must_haves, Status, Evidence columns
    - "## Behavioral Spot-Checks" — gate table with all 6 gates and their sub-steps
    - "## Requirements Coverage" — one row per requirement: DOC-01, DOC-02, DOC-03, with Source Plan, Description, Status, Evidence
    - "## Anti-Overclaim Scan" — bullet list disclosing any findings, advisories, or deviations honestly
    - "## Gaps Summary" — list pre-existing findings (if any) with explanation; "None" if all gates clean

    Score calculation: count hard PASS gates only. Gate 1 FINDING (Xcode issue) from Phase 140
    precedent: count it as FINDING not PASS; the score reflects reality. For Phase 144, Gates 4, 5,
    and 6 are new and expected to be green. Gate 1 and Gate 2 dep-off test may show the same Xcode
    pre-existing finding as Phase 140 — handle identically (record as FINDING, non-blocking).

    Requirements Coverage rows:
    - DOC-01 | 144-01-PLAN.md | README has evaluator lane (Try it locally, credentials table, rough edges, dev tools) | SATISFIED | grep "Try it locally" test/example/README.md confirms...
    - DOC-02 | 144-02-PLAN.md | demo-showcase.md exists with 4 screenshots, wired into ExDoc extras | SATISFIED | Gate 5 + Gate 6 confirm...
    - DOC-03 | 144-03-PLAN.md | Six proof gates run on Phase 144 HEAD; results recorded verbatim | SATISFIED | All hard gates green (or FINDING documented verbatim)

    Anti-overclaim rules:
    - Never record assumed-green results in any gate row
    - Pre-existing install/upgrade test failures (Xcode license issue) → FINDING labeled as pre-existing, not suppressed
    - Gate 6 exit code 0 must be from a fresh run on Phase 144 HEAD, not a cached result
    - overrides_applied must be 0 unless a waiver was explicitly approved
  </action>

  <verify>
    <automated>grep -q "phase: 144-readme-evaluator-lane-docs-proof" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `ls .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md` exits 0
    - `grep -q "phase: 144-readme-evaluator-lane-docs-proof" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md` exits 0
    - `grep -q "status:" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md` exits 0
    - `grep -q "score:" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md` exits 0
    - VERIFICATION.md contains a "Behavioral Spot-Checks" section with rows for all 6 gates
    - VERIFICATION.md contains a "Requirements Coverage" table with DOC-01, DOC-02, DOC-03 rows
    - VERIFICATION.md contains an "Anti-Overclaim Scan" section
    - Gate 4 row records verbatim ls output listing all 4 PNGs (not placeholder text)
    - Gate 5 row records verbatim grep output showing 4 matches (not placeholder text)
    - Gate 6 row records verbatim exit code and output from mix docs --warnings-as-errors
    - `mix docs --warnings-as-errors` exit code in the Gate 6 row is 0 (not 1)
    - `grep "threadline" mix.lock` exits 0 (mix.lock not left stripped from Gate 2)
    - No row in VERIFICATION.md shows "TBD", "expected", "PENDING", or other placeholder text in the Result column
  </acceptance_criteria>

  <done>
    144-VERIFICATION.md is filed with all 6 gates recorded verbatim. Gates 4, 5, and 6 confirm
    screenshots are committed, referenced in the guide, and ExDoc builds clean. DOC-03 requirement
    is satisfied. Anti-Overclaim Scan honestly discloses any pre-existing findings. overrides_applied: 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test execution environment | mix test runs against live Postgres at localhost:5432 |
| dep-off lane | Temporarily mutates mix.lock (Threadline removed); must be restored after Gate 2e |
| test/example/ database | Gate 3 drops and recreates the example app dev database (not the library test DB) |
| doc/ tree | Generated by mix docs during Gate 6; grep assertions for Gate 5 read from live files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-03-01 | Tampering | mix.lock (Gate 2 dep-off lane) | mitigate | Gate 2e (mix deps.get) is a mandatory restore step; task action, acceptance criteria, and verify command all enforce it; mix.lock must NOT be left stripped |
| T-144-03-02 | Tampering | test/example/ dev database (Gate 3 ecto.drop) | accept | Gate 3 drops and recreates the example app's dev DB (not the library test DB); this is intentional clean-state proof; the example app DB is ephemeral and seeded from seeds.exs |
| T-144-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all gates run existing tools |

**No new attack surface.** This plan runs existing test suites, an existing Mix task (`mix docs`), file listing commands, and grep assertions. The only mutation is filing `144-VERIFICATION.md` (a new planning artifact) and temporarily modifying `mix.lock` during the dep-off lane (immediately restored). No new library code, inputs, or auth paths.
</threat_model>

<verification>
After both tasks complete:

1. VERIFICATION.md exists with canonical frontmatter:
   grep "phase: 144-readme-evaluator-lane-docs-proof" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md

2. mix.lock not stripped (threadline restored after Gate 2):
   grep "threadline" mix.lock

3. Gate 6 (mix docs) was run and exit 0 recorded:
   grep "mix docs --warnings-as-errors" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md

4. All 3 requirements covered in VERIFICATION.md:
   grep -E "DOC-01|DOC-02|DOC-03" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md

5. No placeholder text in Result columns:
   grep -v "TBD\|PENDING\|expected output" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md | wc -l
</verification>

<success_criteria>
- DOC-03 SC#3 satisfied: 144-VERIFICATION.md filed with YAML frontmatter (D-16), 6 gates run and recorded verbatim (D-17), no assumed-green results, overrides_applied: 0.
- Gate 1 (mix test): actual test count and exit code recorded.
- Gate 2 (dep-off): all 5 sub-steps recorded; mix.lock restored.
- Gate 3 (clean-state setup): ecto.drop first, all 4 sub-steps recorded from test/example/.
- Gate 4 (screenshots committed): ls output shows 4 PNGs.
- Gate 5 (screenshots in guide): grep shows 4 matches.
- Gate 6 (ExDoc clean): exit 0 recorded.
- DOC-01, DOC-02, DOC-03 all in Requirements Coverage table as SATISFIED.
</success_criteria>

<output>
Create `.planning/phases/144-readme-evaluator-lane-docs-proof/144-03-SUMMARY.md` when done.
</output>
