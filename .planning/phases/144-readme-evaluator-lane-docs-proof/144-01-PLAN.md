---
phase: 144-readme-evaluator-lane-docs-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "test/example/README.md begins with three-sentence framing naming Vaultr, linking [Sigra](https://hexdocs.pm/sigra), listing 6 feature categories, and stating the one-command spin-up (D-01, D-02)"
    - "README contains a Docker one-liner with container name vaultr-postgres (D-03)"
    - "README contains a credentials table with exact email/password/feature columns matching all 6 personas from personas.ex (D-04)"
    - "README contains rough-edge callouts for Dave (locked+unconfirmed, explicit trigger instructions) and Frank (scheduled_deletion_at, inspect path) (D-05)"
    - "README contains two Sigra hexdocs links: one inline in framing, one in a Learn more section at bottom (D-06)"
    - "README contains a Dev tools section with full localhost URLs for http://localhost:4000/dev/mailbox and http://localhost:4000/demo/credentials (D-07)"
    - "No Phoenix scaffold boilerplate remains (phoenixframework.org, hexdocs.pm/phoenix, elixirforum.com) (D-01)"
  artifacts:
    - path: "test/example/README.md"
      provides: "Evaluator-facing README with credentials table and conversion path"
      contains: "Try it locally"
  key_links:
    - from: "test/example/README.md credentials table"
      to: "test/example/lib/example/demo/personas.ex all/0"
      via: "exact email/password values copied from personas.ex"
      pattern: "demo.sigra.dev"
    - from: "README framing"
      to: "https://hexdocs.pm/sigra"
      via: "inline markdown link"
      pattern: "hexdocs.pm/sigra"
---

<objective>
Replace the 19-line Phoenix scaffold boilerplate in `test/example/README.md` with an evaluator-focused "Try it locally" lane covering prerequisites, Docker one-liner, one-command spin-up, credentials table, rough-edge persona callouts, dev tools links, and a Sigra conversion path.

Purpose: The current README signals "generic unfinished template" — it undermines Sigra's positioning as a drop-in auth library. An evaluator who lands on this README should immediately understand what Vaultr is, how to run it in one command, and how to experience every auth feature.

Output: `test/example/README.md` — complete replacement, evaluator-ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-CONTEXT.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-RESEARCH.md
@.planning/phases/144-readme-evaluator-lane-docs-proof/144-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace test/example/README.md with evaluator lane</name>
  <files>test/example/README.md</files>

  <read_first>
    Read `test/example/lib/example/demo/personas.ex` in full — specifically `all/0` (lines 37–118)
    and `feature_map/0` (lines 126–135). The credentials table MUST use the exact email and
    password values from `all/0`, not from CONTEXT.md or PATTERNS.md (those are copies and may
    diverge). Cross-check: each email in the README table must match `all/0` exactly, each
    password must match `all/0` exactly, and each "Feature demonstrated" cell must match
    `feature_map/0` exactly for that persona's email local-part key.

    Read `test/example/README.md` to confirm current content before replacing (currently 19 lines
    of Phoenix scaffold boilerplate).

    Read `guides/introduction/getting-started.md` lines 1–30 for the prose-first structural
    pattern used across guides: H1 → orientation sentences → prerequisite context → then steps.
  </read_first>

  <action>
    Write a complete replacement for `test/example/README.md`. The file must have exactly the
    following structure (all content per D-01 through D-07):

    Section 1 — H1 title: "# Vaultr — Sigra Demo App"

    Section 2 — Three-sentence framing block (D-02): answer what Vaultr is and why it exists.
    Sentence 1: "Vaultr is a showcase Phoenix app demonstrating [Sigra](https://hexdocs.pm/sigra)
    — a comprehensive authentication library for Phoenix 1.8+." Sentence 2: list 6 feature
    categories (registration, login/logout, email confirmation, password reset, TOTP MFA, passkeys,
    OAuth/social login, account lifecycle, audit logging — pick the 6 that are most legible in
    2-3 words each). Sentence 3: one sentence stating you can run it locally with one command.

    Section 3 — "## Try it locally" heading, then:
    - "### Prerequisites" subsection: Elixir 1.18+, Erlang/OTP 27+, PostgreSQL (or Docker).
      Include the Docker one-liner with container name `vaultr-postgres` (not `sigra-test-postgres`):
        docker run -d --name vaultr-postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16-alpine
      Note that the container password is `postgres` — matching the app's dev config default.

    - "### One-command setup" subsection:
        cd test/example
        mix setup && mix phx.server
      Then visit http://localhost:4000

    Section 4 — "## Demo Personas" heading with the credentials table. The table has three columns:
    "Email", "Password", "Feature demonstrated". Use the EXACT values from `personas.ex` `all/0`
    for email and password, and EXACT values from `feature_map/0` for "Feature demonstrated".
    Row order: admin, alice, bob, carol, dave, frank. Standard Markdown table formatting.

    Section 5 — "## Rough Edges" heading with two callouts (D-05):
    - Dave callout: Dave's account is locked AND unconfirmed. Include the explicit trigger
      instruction: "Try the wrong password to see the enumeration-resistant login response.
      Unlock via /admin/users as the admin persona."
    - Frank callout: Frank's `scheduled_deletion_at` is set — the account is still active and
      accessible. Include: "Inspect via /admin/users as the admin persona."

    Section 6 — "## Dev Tools" heading (D-07). Two bullet items with full localhost URLs:
    - http://localhost:4000/dev/mailbox — Swoosh local email inbox (confirmation emails,
      password reset links, magic links)
    - http://localhost:4000/demo/credentials — In-app credentials cheat-sheet (all personas
      listed while the server is running)

    Section 7 — "## Learn More About Sigra" heading (D-06). Bullet list:
    - [Getting Started](https://hexdocs.pm/sigra/getting-started.html) guide
    - [Full documentation](https://hexdocs.pm/sigra) on Hexdocs
    - [Demo Showcase guide](https://hexdocs.pm/sigra/demo-showcase.html) — walkthrough with
      screenshots

    Do NOT include any content from the original Phoenix scaffold:
    "Ready to run in production?", "Official website: https://www.phoenixframework.org/",
    "Guides: https://hexdocs.pm/phoenix/overview.html", "Docs: https://hexdocs.pm/phoenix",
    "Forum: https://elixirforum.com/c/phoenix-forum", "Source: https://github.com/phoenixframework/phoenix"

    Do NOT include fenced code blocks with Elixir module syntax in the README body — only shell
    commands belong in fenced code blocks.
  </action>

  <verify>
    <automated>grep -c "Try it locally\|demo.sigra.dev\|hexdocs.pm/sigra\|dev/mailbox\|vaultr-postgres" test/example/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "Try it locally" test/example/README.md` exits 0
    - `grep -q "vaultr-postgres" test/example/README.md` exits 0
    - `grep -c "demo.sigra.dev" test/example/README.md` outputs 6 or more (one per persona)
    - `grep -q "hexdocs.pm/sigra" test/example/README.md` exits 0
    - `grep -q "dev/mailbox" test/example/README.md` exits 0
    - `grep -q "demo/credentials" test/example/README.md` exits 0
    - `grep -q "DemoAdmin1!SecurePass" test/example/README.md` exits 0 (exact password from personas.ex)
    - `grep -q "FrankDemoPass1!Deleted" test/example/README.md` exits 0 (exact password from personas.ex)
    - `grep -q "phoenixframework.org" test/example/README.md` exits 1 (no scaffold boilerplate remains)
    - `grep -q "elixirforum.com" test/example/README.md` exits 1 (no scaffold boilerplate remains)
    - Dave callout mentions "locked" AND "unconfirmed" AND an explicit trigger instruction
    - Frank callout mentions "scheduled_deletion_at" or "scheduled deletion" AND an inspect path
  </acceptance_criteria>

  <done>
    `test/example/README.md` is a complete evaluator-focused document: three-sentence framing
    with inline Sigra link, Docker one-liner with container name `vaultr-postgres`, one-command
    spin-up, credentials table with exact values from personas.ex, rough-edge callouts for Dave
    and Frank with trigger instructions, dev tools section with full localhost URLs, and a "Learn
    More About Sigra" conversion section. No Phoenix scaffold boilerplate remains.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content | Static documentation file — no executable code, no input handling, no auth paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01-01 | Information Disclosure | README credentials table | accept | Passwords are public-by-design demo credentials labeled as such; isolated to `@demo.sigra.dev` domain; no production data; acceptable per SEED-06 + D-04 rationale |
| T-144-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure documentation authoring |

**No new attack surface.** This plan writes one Markdown file. The credentials table contains intentionally public demo passwords (design decision D-04, SEED-06) segregated to the `@demo.sigra.dev` email domain. No new auth paths, input handling, token management, or library code.
</threat_model>

<verification>
After task completes:

1. README exists and is not the Phoenix scaffold:
   grep -q "Try it locally" test/example/README.md

2. All 6 persona emails present:
   grep -c "demo.sigra.dev" test/example/README.md

3. Sigra hexdocs links present (inline + bottom section):
   grep -c "hexdocs.pm/sigra" test/example/README.md

4. No scaffold boilerplate remains:
   grep -q "phoenixframework.org" test/example/README.md && echo FAIL || echo PASS

5. Dev tools section present:
   grep -q "dev/mailbox" test/example/README.md && grep -q "demo/credentials" test/example/README.md
</verification>

<success_criteria>
- DOC-01 SC#1 satisfied: README contains "Try it locally" section, Docker one-liner, `mix setup && mix phx.server`, credentials table with all 6 personas (exact values from personas.ex), rough-edge callouts for Dave and Frank with explicit trigger instructions, dev tools section with /dev/mailbox and /demo/credentials, and "Learn More About Sigra" section.
- Zero Phoenix scaffold boilerplate: no phoenixframework.org, no elixirforum.com, no hexdocs.pm/phoenix links.
- Credential values match personas.ex exactly (not paraphrased from CONTEXT.md).
</success_criteria>

<output>
Create `.planning/phases/144-readme-evaluator-lane-docs-proof/144-01-SUMMARY.md` when done.
</output>
