---
phase: 48
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - AUD-06
  - AUD-07
must_haves:
  truths:
    - "**AUD-06** and **AUD-07** show `[x]` in **`REQUIREMENTS.md`** only when **`44-VERIFICATION.md`** frontmatter reads **`status: passed`**."
    - "Traceability table rows **AUD-06** and **AUD-07** show **Complete** with a date matching or after **`44-VERIFICATION.md`** `verified:`."
    - "Audit section prose does not claim full Nyquist **41–44** completion beyond **`44-VERIFICATION.md`** + **phase 50** deferral."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Flipped AUD-06/07 checkboxes + aligned traceability table"
nyquist_compliant: false
---

<objective>
Reconcile **`.planning/REQUIREMENTS.md`** (**AUD-06**, **AUD-07**, traceability table, prose) and **`.planning/ROADMAP.md`** (rows **44** / **48** only if narrative drift) after **`44-VERIFICATION.md`** proves merge gate green — same change-set as **D-48-02** / **D-48-04**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-48-10 | Tampering | REQ `[x]` without passing verification | high | Task 1 greps `status: passed` before any REQ edit |
| T-48-11 | Repudiation | Traceability dates / phases disagree with body | medium | Single edit session updates bullets + table together |
| T-48-12 | Elevation | ROADMAP implies Nyquist **41–44** done in phase **48** | medium | ROADMAP edits must keep criterion (3) aligned with **phase 50** ownership |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Pre-flight — verification gate</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
    - .planning/phases/48-phase-44-verification-aud0607/48-CONTEXT.md
  </read_first>
  <action>
    Abort plan **02** (do not edit **`REQUIREMENTS.md`**) unless **`44-VERIFICATION.md`** frontmatter contains **`status: passed`**. If **`status: draft`**, stop and return to **48-01** Task 3.
  </action>
  <acceptance_criteria>
    - `grep -E "^status: passed" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0 before any **`REQUIREMENTS.md`** edit in this plan.
  </acceptance_criteria>
  <verify>
    `grep "^status:" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 2: Flip AUD-06 / AUD-07 and refresh traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    In **## Audit trail atomicity**:

    - Change **AUD-06** bullet from `- [ ]` to `- [x]`; append `*(Closed in Phase 48 — see `44-VERIFICATION.md`.)*` with backtick path **`.planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md`** (or repo-relative equivalent used elsewhere in the file).
    - Change **AUD-07** bullet similarly.

    In **## Traceability** table:

    - Set **AUD-06** row: **Status** = `Complete (YYYY-MM-DD)` using **`44-VERIFICATION.md`** `verified:` date or execution date; **Phase** column **48**.
    - Set **AUD-07** row: same pattern.

    Ensure **Primary phase mapping** prose under the audit section still lists **48–49** for gap closure where applicable — adjust only if inconsistent after checkbox updates.
  </action>
  <acceptance_criteria>
    - `grep -E "^- \\[x\\] \\*\\*AUD-06" .planning/REQUIREMENTS.md` exits 0.
    - `grep -E "^- \\[x\\] \\*\\*AUD-07" .planning/REQUIREMENTS.md` exits 0.
    - `grep "AUD-06 | 48 | Complete" .planning/REQUIREMENTS.md` exits 0.
    - `grep "AUD-07 | 48 | Complete" .planning/REQUIREMENTS.md` exits 0.
    - `grep "44-VERIFICATION.md" .planning/REQUIREMENTS.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep "^| AUD-0[67]" .planning/REQUIREMENTS.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: ROADMAP micro-sync (optional)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
    - .planning/phases/48-phase-44-verification-aud0607/48-CONTEXT.md
  </read_first>
  <action>
    Inspect rows **44** and **48**. If **Success looks like** columns already match delivered artifacts (**44-VERIFICATION.md**, REQ flips, **phase 50** Nyquist wording), make **no** edit.

    If drift exists (e.g. implies full Nyquist satisfied in **48**): rewrite so criterion (3) for phase **48** explicitly references **`44-VERIFICATION.md`** + scoped merge gate, and full Nyquist **41–44** remains **phase 50** — aligned with **D-48-03**.
  </action>
  <acceptance_criteria>
    - `grep "| **48** |" .planning/ROADMAP.md` exits 0 (table row still present).
    - If **ROADMAP** edited: `git diff --stat .planning/ROADMAP.md` shows ≤ 20 lines changed.
  </acceptance_criteria>
  <verify>
    `grep "48" .planning/ROADMAP.md | head -n 5`
  </verify>
</task>

</tasks>

<verification>
1. `grep -E "^status: passed" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
2. AUD-06/07 lines in **`REQUIREMENTS.md`** match Task 2 acceptance greps.
3. No pending checkboxes remain: `grep "\\[ \\] \\*\\*AUD-0[67]" .planning/REQUIREMENTS.md` exits **1**.
</verification>

<success_criteria>
- Requirements truth matches **`44-VERIFICATION.md`** (no checkbox-before-evidence ordering violation).
- Traceability table matches audit bullets for **AUD-06** / **AUD-07**.
</success_criteria>
