---
phase: 48
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md
  - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
  - mix.exs
autonomous: true
requirements:
  - AUD-06
  - AUD-07
must_haves:
  truths:
    - "`44-VALIDATION.md` per-task rows for **44-03..44-05** reference literal `test/sigra/` paths with **File Exists** ✅ where files exist on `main`."
    - "`44-VERIFICATION.md` exists beside phase **44** artifacts, mirrors **`43-VERIFICATION.md`** shape with **distinct AUD-06 vs AUD-07** Must-have evidence, verbatim merge gate + **Automated checks run**."
    - "Nyquist batch **41–44** remains **phase 50** — `44-VERIFICATION.md` **Notes** and `44-VALIDATION.md` deferral text say so; `44-VALIDATION.md` frontmatter stays `nyquist_compliant: false` unless `STATE.md` records `nyquist_escalation_authorized`."
  artifacts:
    - path: ".planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md"
      provides: "Dated verification snapshot for phase 44 AUD-06/07 closure"
nyquist_compliant: false
---

<objective>
Refresh **`44-VALIDATION.md`** to match merged phase **44** tests and **D-48-03** Nyquist policy, publish **`44-VERIFICATION.md`** (AUD-06 + AUD-07 Must-haves, merge gate, receipts), run the merge gate against Postgres, and optionally add **`mix ci.audit_44`** (**D-48-01** discretion).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-48-01 | Tampering | Validation map shows ❌ **File Exists** for tests that exist | high | Task 1 greps each `*_test.exs` path on disk then updates table |
| T-48-02 | Repudiation | `44-VERIFICATION.md` omits verbatim commands or SHA | medium | Task 2–3 require literal command lines + `git rev-parse HEAD` in **Notes** |
| T-48-03 | Elevation | `nyquist_compliant: true` flipped on `44-VALIDATION.md` without escalation | high | Task 1 keeps `nyquist_compliant: false`; sign-off checklist must not demand `true` |
| T-48-04 | Information disclosure | Verification pastes secrets from logs | low | Task 2 forbids `client_secret` / long `Bearer` tokens in new markdown |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Align 44-VALIDATION.md with merged tests + Nyquist policy</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md</files>
  <read_first>
    - .planning/phases/48-phase-44-verification-aud0607/48-CONTEXT.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-03-PLAN.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-04-PLAN.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-05-PLAN.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md
    - test/sigra/mfa_audit_atomicity_test.exs
    - test/sigra/account_audit_atomicity_test.exs
    - test/sigra/api_token_audit_atomic_test.exs
    - test/sigra/audit_multi_step_test.exs
  </read_first>
  <action>
    Update **`44-VALIDATION.md`**:

    - **Per-Task Verification Map:** For rows tied to **44-03** (AUD-06), **44-04** / **44-05** (AUD-07), set **Automated Command** cells to the exact `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test …` lines for: `test/sigra/mfa_audit_atomicity_test.exs`, `test/sigra/account_audit_atomicity_test.exs`, `test/sigra/api_token_audit_atomic_test.exs`. For **44-02** foundation coverage, bind **44-02-01** row to `test/sigra/audit_multi_step_test.exs` (or keep `audit_test.exs` only if that row truly verifies `test/sigra/audit_test.exs` — prefer the path that exists and matches **44-02-SUMMARY.md**).
    - Set **File Exists** ✅ for each row whose test file exists on disk.
    - Set **Status** to ✅ green only where summaries + code justify it; otherwise ⚠️ with one-line note.
    - Add **## Nyquist deferral** before **Validation Sign-Off**: full Nyquist **41–44** is **phase 50**; phase **48** does not assert global Nyquist completion on **`44-VALIDATION.md`**.
    - Replace **Validation Sign-Off** bullet that requires `nyquist_compliant: true` with: checkbox text **“`nyquist_compliant: false` remains until phase 50 (or explicit `nyquist_escalation_authorized` in STATE.md)”** — aligned with **D-48-03**.
    - YAML frontmatter: keep `nyquist_compliant: false` unless `.planning/STATE.md` contains literal `nyquist_escalation_authorized`.
  </action>
  <acceptance_criteria>
    - `grep -F "test/sigra/mfa_audit_atomicity_test.exs" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md` exits 0.
    - `grep -F "test/sigra/account_audit_atomicity_test.exs" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md` exits 0.
    - `grep -F "test/sigra/api_token_audit_atomic_test.exs" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md` exits 0.
    - `grep -i "phase 50" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md` exits 0.
    - `awk 'NR>=1&&NR<=12&&/^nyquist_compliant:/{print; exit}' .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md | grep -q "nyquist_compliant: false"` exits 0, unless `.planning/STATE.md` contains literal `nyquist_escalation_authorized`.
    - `grep -A30 "## Validation Sign-Off" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md | grep -F "nyquist_compliant: true"` exits **1** (no sign-off checklist may require `nyquist_compliant: true` per **D-48-03**).
  </acceptance_criteria>
  <verify>
    `grep -c "mfa_audit_atomicity" .planning/phases/44-mfa-account-api-atomic-batches/44-VALIDATION.md` returns integer ≥ 1.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Create 44-VERIFICATION.md snapshot</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-03-SUMMARY.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-04-SUMMARY.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-05-SUMMARY.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-02-SUMMARY.md
  </read_first>
  <action>
    Create **`.planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md`** modeled on **`43-VERIFICATION.md`** with **D-48-02** structure:

    YAML frontmatter: `status:` start as `draft` until Task 3 passes, then `passed`; `phase: "44"`; `verified:` ISO date when merge gate passes.

    Sections:
    1. **## Must-haves (AUD-06 — MFA)** — table **Item | Evidence** citing **`44-AUD-04-INVENTORY.md`** row IDs (or subsection), **`test/sigra/mfa_audit_atomicity_test.exs`**, and **`44-03-SUMMARY.md`** (and **44-02** if Multi-step plumbing required).
    2. **## Must-haves (AUD-07 — Account + API)** — same columns; cite **`test/sigra/account_audit_atomicity_test.exs`**, **`test/sigra/api_token_audit_atomic_test.exs`**, **`44-04-SUMMARY.md`**, **`44-05-SUMMARY.md`**, plus inventory rows for Account/API.
    3. **## Merge gate** — verbatim lines:
       - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile`
       - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs test/sigra/account_audit_atomicity_test.exs test/sigra/api_token_audit_atomic_test.exs test/sigra/audit_multi_step_test.exs`
    4. **## Release attestation (optional)** — full root `mix test` per **`CLAUDE.md`**; state **Executed** or **Not run in this closure**.
    5. **## Notes** — `git rev-parse HEAD` at verification time; paragraph: Nyquist **41–44** → **phase 50** (**D-48-03**).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - `grep -E "^---$" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md | wc -l | grep -qE "^[[:space:]]*2"` exits 0.
    - `grep -F "mfa_audit_atomicity_test.exs" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - `grep -F "44-AUD-04-INVENTORY.md" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - `grep -i "AUD-06" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - `grep -i "AUD-07" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - `grep -i "phase 50" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - `grep -iE "client_secret|Bearer [A-Za-z0-9_-]{20,}" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits **1**.
  </acceptance_criteria>
  <verify>
    `head -n 12 .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: Run merge gate and finalize verification status</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
    - mix.exs
  </read_first>
  <action>
    With Postgres at `localhost:5432` (`postgres`/`postgres` per **`CLAUDE.md`**), run:

    1. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix compile`
    2. `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs test/sigra/account_audit_atomicity_test.exs test/sigra/api_token_audit_atomic_test.exs test/sigra/audit_multi_step_test.exs`

    If both exit 0: set frontmatter `status: passed` and `verified:` to today’s ISO date; in **## Automated checks run**, record **PASS (exit 0)** with the literal command lines.

    If either fails: keep `status: draft`, document **FAIL** with the first error line — **do not** run plan **48-02**.
  </action>
  <acceptance_criteria>
    - `grep -E "^status: passed" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0 OR the file contains `status: draft` AND substring `FAIL` in the body (honest failure path).
    - If `status: passed`: `grep -F "PASS" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - If `status: passed`: `grep -E "^verified: 20" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    When `status: passed`, re-run the Task 3 compound `mix test` line; must exit 0.
  </verify>
</task>

<task type="execute">
  <name>Task 4 (optional): Add mix ci.audit_44 alias</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md
  </read_first>
  <action>
    If maintainers want a stable entry point: add **`aliases/0`** to **`Mix.Project`**, wire **`aliases: aliases()`** in **`project/0`**, and define **`"ci.audit_44"`** as a single **`mix test`** invocation listing the four test paths **in the same order as Task 3**. Update **`44-VERIFICATION.md` ## Merge gate** to cite **`mix ci.audit_44`** in addition to the raw compound command (or replace if the team prefers one line).

    If skipping: add one sentence to **`44-VERIFICATION.md` ## Notes**: “No Mix alias in this closure — raw compound `mix test` is canonical.”
  </action>
  <acceptance_criteria>
    - Either `grep -q "ci.audit_44" mix.exs` exits 0 **or** `grep -q "No Mix alias" .planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md` exits 0.
    - If alias added: `grep -q "mfa_audit_atomicity_test.exs" mix.exs` exits 0.
  </acceptance_criteria>
  <verify>
    If alias added: `mix compile` exits 0 after the edit.
  </verify>
</task>

</tasks>

<verification>
1. All Task 1–3 acceptance greps pass (or honest `draft`/`FAIL` path documented).
2. When `status: passed`, Task 3 compound `mix test` exits 0.
3. `44-VERIFICATION.md` contains no secret patterns per Task 2 acceptance.
</verification>

<success_criteria>
- **`44-VALIDATION.md`** and **`44-VERIFICATION.md`** are mutually consistent with merged phase **44** code and **D-48-01..04** policy.
- Merge gate commands are verbatim and cover **AUD-06**, **AUD-07**, and **44-02** audit Multi foundation.
</success_criteria>
