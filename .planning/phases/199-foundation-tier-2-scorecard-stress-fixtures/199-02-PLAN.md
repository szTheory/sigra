---
phase: 199-foundation-tier-2-scorecard-stress-fixtures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/quality-ledger-monotonic.test.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [LEDGER-02]
must_haves:
  truths:
    - "A 2→1 Tier decrease in the quality ledger is positively proven to fail the monotonic guard (non-zero exit), not merely assumed numeric"
    - "The self-test runs in CI as a merge-blocking step alongside the existing monotonic-guard invocation"
    - "The monotonic guard itself is unchanged — it already reads column-4 as a numeric integer and fails on any decrease, so Tier 2 is protected for free (D-04)"
  artifacts:
    - "scripts/ci/quality-ledger-monotonic.test.sh — a self-contained bash test that builds a throwaway git repo with a Tier-2→Tier-1 ledger delta and asserts the guard exits non-zero"
  key_links:
    - "The self-test invokes the REAL scripts/ci/quality-ledger-monotonic.sh against a synthetic git state (git show ${BASE}:${LEDGER} + working-tree LEDGER) — same interface the guard uses in CI"
    - "A new ci.yml step wires the self-test merge-blocking next to the existing guard step (lines 109-110), preserving --base origin/main wiring (D-07)"
---

<objective>
Positively exercise the Tier-2 ratchet: prove the monotonic guard fails when a cell drops from Tier 2 to Tier 1. The guard needs NO logic change (D-04) — it already compares column-4 integers and fails on `head_tier < base_tier`. This plan adds the missing *positive test* (D-05) so the Tier-2 case is verified, and confirms CI wiring stays merge-blocking (D-07). This is the LEDGER-02 deliverable.

Purpose: Tier 2 being "numerically free" is a claim. A self-test that drives the guard binary through a real 2→1 delta turns the claim into evidence and guards against future guard-parse regressions.
Output: A new bash self-test (scripts/ci/quality-ledger-monotonic.test.sh) wired as a merge-blocking CI step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-CONTEXT.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-RESEARCH.md

# The guard under test (read in full — the self-test must match its interface):
@scripts/ci/quality-ledger-monotonic.sh
# CI wiring point (read lines 95-115 for the existing guard step + base ref):
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 2→1 monotonic-guard self-test as a self-contained bash test (D-05)</name>
  <read_first>
    - scripts/ci/quality-ledger-monotonic.sh in full — note: it reads BASE via `git -C "$ROOT" show "${BASE}:${LEDGER}"` (line 33) and the working-tree `${ROOT}/${LEDGER}` (line 43); LEDGER is the fixed relative path `guides/reference/admin-quality-ledger.md`; it exits 1 on a decrease and 0 on PASS; with no base tiers it exits 0 (INFO skip).
    - 199-RESEARCH.md "Code Examples → D-05 guard self-test" and "D-05 guard self-test placement recommendation" — the recommended harness is a throwaway $TMPDIR git repo with two commits where one cell goes 2→1, run the guard with --base <first-commit>, assert non-zero exit.
    - 199-RESEARCH.md "Validation Architecture" — wire it merge-blocking alongside the existing guard invocation.
  </read_first>
  <files>scripts/ci/quality-ledger-monotonic.test.sh</files>
  <action>
    Create an executable bash script (set -euo pipefail; chmod +x via the file creation) that builds a self-contained, hermetic fixture proving the guard fails on a Tier-2 decrease. Because the guard hard-codes LEDGER=guides/reference/admin-quality-ledger.md relative to its own ROOT (two dirs up from the script), the cleanest hermetic harness is: create a throwaway directory under "$(mktemp -d)" laid out as a mini-repo (`git init`), copy scripts/ci/quality-ledger-monotonic.sh into the SAME relative location (`scripts/ci/`) inside the temp repo so its ROOT resolves to the temp repo, write a minimal valid ledger at guides/reference/admin-quality-ledger.md containing at least one parseable row whose Tier column is `2` (a row matching `^| [a-z]` with column-4 == `2`), `git add`+`git commit` (capture the commit SHA), then mutate that same cell's column-4 from `2` to `1` in the working tree (do NOT commit), and run `bash scripts/ci/quality-ledger-monotonic.sh --base <first-commit-sha>` from inside the temp repo. Assert the guard exits NON-ZERO (the 2→1 decrease must be caught) and that its stderr contains the literal failure marker the guard prints (`tier decreased`). Add a complementary positive assertion: a no-change run (working tree == committed) exits 0. Clean up the temp dir on exit (trap). Set up `git -c user.email=... -c user.name=...` or per-repo config so commits work in CI with no global git identity. Mirror the guard's own parse expectations exactly: the ledger rows must start with `| ` + lowercase and have a bare `[012]` integer in column-4 (so the synthetic fixture exercises the real parse path). Keep the success path quiet and print a clear `PASS`/`FAIL` summary line at the end; exit non-zero if any assertion fails.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && bash scripts/ci/quality-ledger-monotonic.test.sh && echo SELFTEST_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci/quality-ledger-monotonic.test.sh` exits 0 and prints a PASS summary.
    - The test internally asserts that running the real guard against a synthetic 2→1 ledger delta exits non-zero AND its stderr contains "tier decreased".
    - The test also asserts a no-change run exits 0 (guard isn't trivially always-failing).
    - The harness is hermetic: it operates entirely inside a `mktemp -d` git repo, sets a local git identity, and removes the temp dir via a trap (running it leaves no untracked files in the real repo, and `git status` of the real repo is unchanged after the run).
    - The script is executable (`test -x scripts/ci/quality-ledger-monotonic.test.sh`).
  </acceptance_criteria>
  <done>A hermetic bash self-test proves the monotonic guard rejects a Tier-2→Tier-1 decrease and accepts a no-op, exiting 0 overall.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the self-test as a merge-blocking CI step and confirm guard wiring unchanged (D-05, D-07)</name>
  <read_first>
    - .github/workflows/ci.yml lines 95-111 — the existing "Quality ledger monotonic guard" step at 109-110 runs `bash scripts/ci/quality-ledger-monotonic.sh --base "${{ steps.base.outputs.ref }}"` inside the same job that runs the snapshot drift guards. The self-test step goes adjacent to this, in the same job (so it is merge-blocking on the same path).
    - 199-CONTEXT.md D-07 — confirm (do NOT rewire) that the guard stays merge-blocking with --base origin/main; the `steps.base.outputs.ref` already resolves to the origin/main base on PRs.
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add a new CI step immediately after the existing "Quality ledger monotonic guard" step (after line 110), named "Quality ledger monotonic guard self-test", running `bash scripts/ci/quality-ledger-monotonic.test.sh`. The self-test is hermetic and base-independent, so it takes no `--base` argument. Place it in the SAME job as the existing guard so it shares that job's merge-blocking status — do not create a new job and do not add it to any needs/gate aggregator (it rides the existing required job). Do NOT modify the existing guard step's `--base "${{ steps.base.outputs.ref }}"` wiring (D-07: confirm-only, no rewiring). Verify the existing guard line is byte-unchanged after your edit.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -q "quality-ledger-monotonic.test.sh" .github/workflows/ci.yml && grep -q 'quality-ledger-monotonic.sh --base "${{ steps.base.outputs.ref }}"' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains a step invoking `bash scripts/ci/quality-ledger-monotonic.test.sh`, positioned in the same job as the existing monotonic-guard step.
    - The existing guard step line `bash scripts/ci/quality-ledger-monotonic.sh --base "${{ steps.base.outputs.ref }}"` is still present byte-for-byte (D-07 confirm-only).
    - ci.yml still parses as valid YAML.
    - No new job and no change to any `needs:`/gate aggregator (the self-test inherits the existing job's merge-blocking status).
  </acceptance_criteria>
  <done>The self-test runs merge-blocking in CI next to the existing guard, and the guard's origin/main wiring is confirmed unchanged.</done>
</task>

</tasks>

<verification>
- `bash scripts/ci/quality-ledger-monotonic.test.sh` exits 0 (LEDGER-02 self-test, VALIDATION.md Wave-0 gap).
- `bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` still exits 0 against the real ledger (guard untouched, D-04).
- `git status` of the real repo is clean after running the self-test (hermetic).
- ci.yml parses as YAML and contains both the self-test step and the unchanged guard step.
</verification>

<success_criteria>
The Tier-2 ratchet is positively tested: a 2→1 decrease provably fails the real guard binary, the test is hermetic, and it runs merge-blocking in CI. The existing guard logic and its origin/main wiring are unchanged (D-04, D-07). ROADMAP success criterion 2 (monotonic guard enforces Tier-2 ratchet, merge-blocking) is satisfied.
</success_criteria>

<output>
Create `.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-02-SUMMARY.md` when done.
</output>
