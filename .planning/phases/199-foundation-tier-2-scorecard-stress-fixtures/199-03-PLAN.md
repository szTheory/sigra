---
phase: 199-foundation-tier-2-scorecard-stress-fixtures
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/test/example/demo/seeds_test.exs
  - test/example/lib/example/demo/seeds.ex
autonomous: true
requirements: [FIXT-01, FIXT-02]
must_haves:
  truths:
    - "The admin persona carries ≥25 self-tied (effective_user_id) audit events so /admin/audit and the user-detail audit feed cross the @default_limit 25 page size and paginate (FIXT-01)"
    - "A list-scale bulk cohort of ~35-40 ugly users (long display names, near-max emails, UUID-shaped identifiers) exists so /admin/users renders >1 page (FIXT-02)"
    - "The bulk cohort is seeded BEFORE the personas in run/0 so a persona (admin) stays the newest, first-listed user the content-equivalence test lands on (Finding 1 / Pitfall 1)"
    - "Re-running run/0 is a no-op: bulk users and topped-up events use idempotent count-threshold / on_conflict:nothing upserts under the existing MIX_ENV=test raise guard (FIXT-02, D-10)"
    - "demo_users == length(Personas.all()) still holds — the bulk cohort is excluded from both Personas.all/0 and the demo_users count (Pitfall 3)"
  artifacts:
    - "test/example/lib/example/demo/seeds.ex — new seed_bulk_users/0 step + topped-up @audit_actions (admin ≥25 self-tied) + run/0 insert-order change"
    - "test/example/test/example/demo/seeds_test.exs — admin audit-liveness threshold raised to >=25, bulk-cohort idempotency + count-exclusion assertions, Pitfall-3 scoping"
  key_links:
    - "Bulk-user email marker is chosen so the seeds_test demo_users count query (currently like(u.email, '%@demo.tasklane.test')) EXCLUDES it — either a distinct domain marker or a scoped count update (resolve before writing seeds)"
    - "Topped-up admin audit rows pass allow_reserved: true (auth.*/session.*/mfa.* are reserved prefixes, Pitfall 5) and set effective_user_id: admin.id (the tie the admin-detail/audit query filters on)"
    - "Bulk cohort lives OUTSIDE Personas.all/0 so /demo/credentials, print_credentials, and feature_map SSoT are untouched (D-09)"
---

<objective>
Stress the admin surfaces with real data: top up the `admin` persona to ≥25 self-tied audit events (so pagination renders — FIXT-01) and add a list-scale "ugly" bulk user cohort (FIXT-02), all idempotent and under the existing raise guard. Resolve the two mechanical landmines research flagged: insert order (admin must stay first-listed) and the `demo_users` count assertion (bulk users must not inflate it).

Purpose: Phases 200-204 elevate pages and assert pagination/content-equivalence/overflow behavior; they cannot without this data. This plan feeds the instruments.
Output: A topped-up audit batch + a `seed_bulk_users/0` step in seeds.ex, with seeds_test.exs assertions raised and scoped to lock the contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-CONTEXT.md
@.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-RESEARCH.md

# Files edited / depended on (read in full before editing):
@test/example/lib/example/demo/seeds.ex
@test/example/lib/example/demo/personas.ex
@test/example/test/example/demo/seeds_test.exs
# Raise-guard contract (must keep bulk inserts under it):
@test/example/priv/repo/seeds.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the contract in seeds_test.exs — raise admin threshold to ≥25, add bulk-cohort assertions, resolve the demo_users count (Pitfall 3)</name>
  <read_first>
    - test/example/test/example/demo/seeds_test.exs lines 38-92 (snapshot_counts/0 — demo_users globs `%@demo.tasklane.test` at line 40), 96-109 (SEED-01 idempotency, asserts `first.demo_users == length(Personas.all())` at 107), 285-310 (SEED-04 admin audit-liveness, `admin_tied >= 15` at 294).
    - 199-RESEARCH.md Pitfall 3 + Assumption A1 — bulk users on `@demo.tasklane.test` would break the `demo_users == length(Personas.all())` assertion; resolve by EITHER a distinguishable bulk marker excluded from the count OR scoping the count to persona emails. Decide here and encode the decision in the test.
    - 199-CONTEXT.md D-08 (admin ≥25 self-tied) and D-09 (bulk cohort outside Personas.all).
  </read_first>
  <files>test/example/test/example/demo/seeds_test.exs</files>
  <action>
    This task writes the failing contract that Tasks 2-3 satisfy (Nyquist: test-first). Make three changes:
    1. Resolve Pitfall 3 first and document the chosen approach in a code comment. Recommended approach (lowest blast radius): give bulk users a distinguishable email shape that the demo_users count query can exclude — e.g. local-part prefix `loadtest-NN` on the `@demo.tasklane.test` domain, and change the demo_users count in snapshot_counts/0 to exclude that prefix (e.g. add `and not like(u.email, "loadtest-%")` to the query at line 40) so persona-only users are counted. Keep `first.demo_users == length(Personas.all())` (line 107) intact — it must stay green AFTER bulk users exist. (Alternative allowed: a separate sub-domain marker; pick one and apply it consistently with Task 3.)
    2. Raise the admin audit-liveness assertion from `admin_tied >= 15` to `admin_tied >= 25` (line ~294), keeping the distinct-actions `>= 6` assertion. This encodes FIXT-01's ≥25 self-tied requirement.
    3. Add a new describe block / tests for the bulk cohort: assert the bulk-user count equals the chosen cohort size after run/0 (a `like(u.email, "loadtest-%")` count), assert bulk users are NOT in Personas.all() (none of their emails appear in the persona catalog), and extend the SEED-01 idempotency test (or add a sibling) to assert the bulk count is identical across two run/0 calls (no duplicates). Reference the cohort size as a module attribute or literal that Task 3 will match.
    Do not implement the seeds themselves here — only the assertions. It is expected that this test FAILS until Tasks 2-3 land.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && grep -q "admin_tied >= 25" test/example/demo/seeds_test.exs && grep -q "loadtest-" test/example/demo/seeds_test.exs && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - seeds_test.exs asserts `admin_tied >= 25` (FIXT-01 threshold) and retains `distinct_actions >= 6`.
    - snapshot_counts/0 demo_users query excludes the bulk-user marker (e.g. `not like(u.email, "loadtest-%")`) so the `demo_users == length(Personas.all())` assertion can stay green once bulk users exist.
    - A bulk-cohort test asserts: (a) bulk count == chosen cohort size after run/0, (b) bulk emails are absent from Personas.all(), (c) bulk count is stable across two run/0 calls.
    - The test file compiles (`mix compile` clean from test/example). The new assertions are expected to FAIL on `mix test` until Tasks 2-3 land — that is correct (RED).
  </acceptance_criteria>
  <done>seeds_test.exs encodes the ≥25 admin threshold, the bulk-cohort idempotency/exclusion contract, and the Pitfall-3-resolved demo_users count; it compiles and fails red pending the seed implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Top up the admin persona to ≥25 self-tied audit events (FIXT-01, D-08, D-11, Pitfall 5)</name>
  <read_first>
    - test/example/lib/example/demo/seeds.ex lines 479-498 (@audit_actions — currently 18 admin-tied rows, all effective_user_id: admin.id via insert_audit_batch lines 666-680), 634-712 (count-threshold guard + transaction batch). Note admin currently also receives 2 persona_audit_events (offsets 28-29) → ~20 self-tied total, below 25.
    - 199-RESEARCH.md Pitfall 5 — auth.*/session.*/mfa.*/admin.* etc. are reserved prefixes; every audit insert already passes `allow_reserved: true` (seeds.ex:678,702) — keep that on the added rows.
    - lib/sigra/audit/changeset.ex:28 — outcome vocab is `~w(success failure error)`; there is NO severity column (D-11). "Varied outcomes/severities" = outcome + action-prefix variety only.
  </read_first>
  <files>test/example/lib/example/demo/seeds.ex</files>
  <action>
    Add enough additional admin-tied rows to @audit_actions so the admin persona crosses a hard ≥25 self-tied count with margin (target ≥27-28 to leave headroom). Extend the @audit_actions list with ~8-10 new `{action, outcome, offset_days}` tuples using the existing tuple shape — these are inserted by insert_audit_batch with `effective_user_id: admin.id` and `allow_reserved: true`, so they are self-tied automatically. Vary the action prefixes and outcomes across the existing vocabulary to add realistic variety (mix of `success` / `failure` / `error` outcomes; include at least one `error` outcome to exercise that vocab value, and span action prefixes already used in the file like auth.login, session.create, mfa.*, admin.impersonation, plus add an `error`-outcome row e.g. an auth or session action). Use new, larger `offset_days` values that do not collide with the persona_audit_events offsets (18-33) — continue the deterministic past-window spread anchored on @seed_reference_ts. CRITICAL: update the count-threshold guard's expected total at seeds.ex:648 — it compares `demo_tied_count < length(@audit_actions) + length(persona_audit_events())`; because you grew @audit_actions, `length(@audit_actions)` updates automatically, so the threshold self-adjusts — verify the comparison still references `length(@audit_actions)` (not a hard-coded number) so the bigger batch re-seeds correctly on older dev DBs. Update the module @moduledoc / @audit_actions comment that says ">=15 rows" to reflect the new admin self-tied count if it is now stale.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix test test/example/demo/seeds_test.exs -o "audit liveness" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - @audit_actions has enough admin-tied rows that admin's `effective_user_id == admin.id` count is ≥25 (target ≥27) after run/0; the SEED-04 `admin_tied >= 25` assertion from Task 1 passes.
    - Every added row's action passes `allow_reserved: true` (inherited from insert_audit_batch) and at least one added row uses the `error` outcome value.
    - The count-threshold guard at seeds.ex:648 still uses `length(@audit_actions)` (no hard-coded count) so re-seed on an older DB tops up correctly.
    - `mix test test/example/demo/seeds_test.exs` audit-liveness tests pass; idempotency (SEED-01) still passes (re-run yields identical audit_events count).
  </acceptance_criteria>
  <done>The admin persona has ≥25 self-tied audit events, the seeds_test audit-liveness assertion is green, and idempotency holds.</done>
</task>

<task type="auto">
  <name>Task 3: Add the list-scale "ugly" bulk user cohort, seeded before personas (FIXT-02, D-09, D-10, Finding 1)</name>
  <read_first>
    - test/example/lib/example/demo/seeds.ex lines 49-62 (run/0 step order — seed_users() is currently FIRST), 80-122 (seed_users/upsert_user pattern), 634-651 (count-threshold idempotency pattern to mirror for the bulk count).
    - test/example/lib/example/demo/personas.ex lines 50-201 — the bulk cohort must NOT be added to all/0 or feature_map/0 (D-09).
    - 199-RESEARCH.md Finding 1 / Pitfall 1 — user list sorts inserted_at DESC (lib/sigra/admin/users/query.ex:67-70), so the bulk cohort MUST be inserted BEFORE the personas in run/0 so a persona (admin) remains the newest, first-listed user; "Code Examples → Bulk cohort seed step shape"; Open Question 2 (cohort size ~35-40 for a clear second page on the 25-row page).
    - The Pitfall-3 marker decision from Task 1 (must match exactly — same `loadtest-` prefix / domain).
  </read_first>
  <files>test/example/lib/example/demo/seeds.ex</files>
  <action>
    Add a private `seed_bulk_users/0` step and call it at the TOP of run/0, BEFORE seed_users() (Finding 1 — so personas insert after and stay newest/first-listed). Implement it mirroring the existing count-threshold + transaction idempotency pattern (seeds.ex:634-651): aggregate the existing count of bulk users (`like(u.email, "loadtest-%")` matching the Task-1 marker) and only insert when below the target cohort size. Pick a cohort size of ~35-40 (Claude's discretion within research's ~30-60 guidance — choose a value that produces >1 page on the 25-row /admin/users list including personas; e.g. 36). For each bulk user n in 1..size, register/upsert via the same idempotent path used for personas (Accounts.register_user or a direct on_conflict:nothing insert keyed on email) with deliberately UGLY values per the stress-test goal: a near-max-length email local part on the chosen marker (e.g. `loadtest-NN-<long-token>@demo.tasklane.test`), a long multi-word display name (overflow pressure), and a UUID-shaped identifier embedded in the display name or a fixture field to pressure identifier rendering. Confirm/mark each user (so they appear in the list like real users) following the persona confirm pattern. Keep ALL inserts under the existing run/0 path (which only executes via priv/repo/seeds.exs under the MIX_ENV=test raise guard, D-10) — do NOT add a new entry point. Do NOT touch personas.ex (bulk users stay out of the catalog, D-09). Ensure re-running run/0 does not duplicate (the count-threshold guard makes the second run a no-op).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example && mix test test/example/demo/seeds_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `seed_bulk_users/0` is called at the start of run/0, BEFORE seed_users() (so personas remain the newest inserted_at rows).
    - After run/0 the bulk cohort count (`like(u.email, "loadtest-%")`) equals the chosen size (~36), and the bulk-cohort tests from Task 1 pass.
    - Bulk users carry ugly stress data: at least one near-max-length email, long multi-word display names, and a UUID-shaped identifier string.
    - `demo_users == length(Personas.all())` (seeds_test:107) is GREEN — bulk users are excluded from the demo_users count (Pitfall 3 resolution from Task 1) and absent from Personas.all().
    - The FULL seeds_test.exs suite passes (`mix test test/example/demo/seeds_test.exs` → 0 failures), including SEED-01 idempotency across two run/0 calls with bulk users present.
  </acceptance_criteria>
  <done>A ~36-user ugly bulk cohort is seeded before the personas, idempotently, excluded from the persona catalog and demo_users count; the full seeds_test suite is green.</done>
</task>

</tasks>

<verification>
- `cd test/example && mix test test/example/demo/seeds_test.exs` → 0 failures (FIXT-01 ≥25 admin events; FIXT-02 bulk cohort idempotency + exclusion).
- `cd test/example && mix test test/example/demo/seeds_script_test.exs` → still green (MIX_ENV=test raise guard contract intact; bulk inserts ride the same guarded entry point, FIXT-02 D-10).
- Re-running `Seeds.run()` twice yields identical counts (SEED-01 idempotency, including the new bulk + topped-up event rows).
- `demo_users == length(Personas.all())` remains true (Pitfall 3 resolved).
</verification>

<success_criteria>
The admin persona has ≥25 self-tied audit events (pagination data) and a ~36-user ugly bulk cohort exists, both idempotent and under the raise guard, with the persona-catalog SSoT and demo_users count invariants intact. The bulk cohort is inserted before personas so admin stays first-listed (de-risking plan 04's content-equivalence test). ROADMAP success criteria 3 (data half) and 4 are satisfied.
</success_criteria>

<output>
Create `.planning/phases/199-foundation-tier-2-scorecard-stress-fixtures/199-03-SUMMARY.md` when done.
</output>
