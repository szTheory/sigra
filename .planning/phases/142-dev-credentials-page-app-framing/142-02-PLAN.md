---
phase: 142-dev-credentials-page-app-framing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example_web/components/layouts/root.html.heex
  - test/example/lib/example_web/components/layouts.ex
autonomous: true
requirements:
  - DEMO-02

must_haves:
  truths:
    - "Browser <title> reads 'Vaultr' (default) and ' · Vaultr' (suffix) for all pages (D-08)"
    - "Header brand span displays 'Vaultr' with data-testid='app-name' (D-06, D-08)"
    - "Phoenix version span is gone — no Application.spec(:phoenix, :vsn) in the brand area"
    - "Org-switcher and impersonation_banner rows are untouched (D-09)"
    - "Nav <ul> renders 'Sign In →' link only when @current_scope is nil; no Phoenix fixture links (D-10)"
  artifacts:
    - path: "test/example/lib/example_web/components/layouts/root.html.heex"
      provides: "Vaultr page title default and suffix"
      contains: "default=\"Vaultr\" suffix=\" · Vaultr\""
    - path: "test/example/lib/example_web/components/layouts.ex"
      provides: "Vaultr brand span + data-testid + contextual nav"
      contains: "data-testid=\"app-name\""
  key_links:
    - from: "test/example/lib/example_web/components/layouts/root.html.heex"
      to: "browser <title> element"
      via: "<.live_title> component"
      pattern: "default=\"Vaultr\""
    - from: "test/example/lib/example_web/components/layouts.ex"
      to: "header brand <span>"
      via: "data-testid='app-name'"
      pattern: "data-testid=\"app-name\""
---

<objective>
Apply "Vaultr" SaaS branding to the example app layout so the demo reads as a purposeful product
rather than a bare Phoenix fixture.

Purpose: DEMO-02 — an evaluator running the dev server sees "Vaultr" in the browser tab and the
header, not "Example · Phoenix Framework" or a Phoenix version number.

Output:
- `root.html.heex` line 7 — `default="Vaultr" suffix=" · Vaultr"`
- `layouts.ex` lines 48-52 — brand span → "Vaultr" + `data-testid="app-name"`
- `layouts.ex` lines 60-72 — nav `&lt;ul&gt;` → contextual "Sign In →" (when unauthenticated) per D-10
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md
@.planning/phases/142-dev-credentials-page-app-framing/142-UI-SPEC.md
@.planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md

<!-- Source files — read before editing -->
@test/example/lib/example_web/components/layouts/root.html.heex
@test/example/lib/example_web/components/layouts.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand root.html.heex — Vaultr page title (D-08)</name>
  <files>test/example/lib/example_web/components/layouts/root.html.heex</files>
  <read_first>
    - test/example/lib/example_web/components/layouts/root.html.heex — full file (9 lines); line 7 is the target
    - .planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md — D-08 exact change spec
    - .planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md — "root.html.heex (edit, line 7)" section for before/after
  </read_first>
  <action>
    Edit `test/example/lib/example_web/components/layouts/root.html.heex` line 7.

    Change ONLY the `default` and `suffix` attribute values on the `&lt;.live_title&gt;` tag:
    - `default="Example"` → `default="Vaultr"`
    - `suffix=" · Phoenix Framework"` → `suffix=" · Vaultr"`

    Line 8 (`{assigns[:page_title]}`) and ALL other lines are unchanged.
    Do NOT reformat the file, add whitespace, or change any surrounding HTML.

    The tag must be a single line or maintain the same line structure as before — do not split attributes across lines.
  </action>
  <verify>
    <automated>grep "Vaultr" test/example/lib/example_web/components/layouts/root.html.heex</automated>
  </verify>
  <done>
    `grep 'default="Vaultr"' test/example/lib/example_web/components/layouts/root.html.heex` returns a match.
    `grep 'suffix=" · Vaultr"' test/example/lib/example_web/components/layouts/root.html.heex` returns a match.
    `grep "Phoenix Framework" test/example/lib/example_web/components/layouts/root.html.heex` returns no match.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand layouts.ex — Vaultr brand span + contextual nav (D-08, D-09, D-10)</name>
  <files>test/example/lib/example_web/components/layouts.ex</files>
  <read_first>
    - test/example/lib/example_web/components/layouts.ex (lines 40-86) — read the full app/1 component before editing; confirm exact current content of lines 46-72 and lines 53-76
    - .planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md — "layouts.ex (edit, lines 46-72)" section with verbatim before/after for both the brand span and the nav ul
    - .planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md — D-08 (brand span change), D-09 (do NOT touch org-switcher/impersonation rows), D-10 (nav ul treatment + fallback)
    - .planning/phases/142-dev-credentials-page-app-framing/142-RESEARCH.md — Pitfall 6 (nav link @current_scope nil guard)
  </read_first>
  <action>
    Edit `test/example/lib/example_web/components/layouts.ex` in the `app/1` function body. Two targeted edits:

    EDIT 1 — Brand span (lines 48-51, per D-08):
    Replace `&lt;span class="text-sm font-semibold"&gt;v{Application.spec(:phoenix, :vsn)}&lt;/span&gt;`
    with `&lt;span class="text-sm font-semibold" data-testid="app-name"&gt;Vaultr&lt;/span&gt;`.
    Keep the surrounding `&lt;a href="/"&gt;` and `&lt;img&gt;` elements unchanged.

    EDIT 2 — Nav `&lt;ul&gt;` (lines 60-72, per D-10):
    Replace the three `&lt;li&gt;` items (Website/GitHub/Get Started links to phoenixframework.org) with a single contextual `&lt;li&gt;`:

      &lt;li :if={is_nil(@current_scope)}&gt;
        &lt;a href={~p"/users/log_in"} class="btn btn-primary"&gt;
          Sign In &lt;span aria-hidden="true"&gt;&amp;rarr;&lt;/span&gt;
        &lt;/a&gt;
      &lt;/li&gt;

    The `&lt;ul&gt;` element's class attribute (`flex flex-column px-1 space-x-4 items-center`) is preserved unchanged.
    The guard `is_nil(@current_scope)` is safe because `current_scope` has `default: nil` in Layouts.app — no nil-dereference risk.

    D-10 fallback: if the `:if={is_nil(@current_scope)}` conditional causes any compile warning or runtime issue due to the unauthenticated /demo/credentials context, the acceptable fallback is an empty `&lt;ul&gt;` with no `&lt;li&gt;` items (removes Phoenix links without adding auth-conditional logic). The primary form (with Sign In link) is preferred.

    MUST NOT touch:
    - Lines 53-59 (`&lt;.org_switcher :if={@current_scope && @current_scope.active_organization}&gt;` block) — D-09
    - Line 76 (`&lt;.impersonation_banner :if={@current_scope && @current_scope.impersonating_from}&gt;`) — D-09
    - Lines 78-84 (`&lt;main&gt;` block, `&lt;.flash_group&gt;`)
    - Any attr declarations above the function body
  </action>
  <verify>
    <automated>cd test/example && mix compile 2>&1 | grep -E "(error|warning)" | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    `grep 'data-testid="app-name"' test/example/lib/example_web/components/layouts.ex` returns a match.
    `grep "Vaultr" test/example/lib/example_web/components/layouts.ex` returns a match.
    `grep "Application.spec(:phoenix, :vsn)" test/example/lib/example_web/components/layouts.ex` returns no match.
    `grep "phoenixframework.org" test/example/lib/example_web/components/layouts.ex` returns no match.
    `grep "org_switcher" test/example/lib/example_web/components/layouts.ex` returns a match — untouched (D-09 compliance).
    `grep "impersonation_banner" test/example/lib/example_web/components/layouts.ex` returns a match — untouched (D-09 compliance).
    `mix compile` exits 0 with no errors referencing layouts.ex or root.html.heex.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated browser → Layouts.app nav | The Sign In nav link is conditionally rendered; if guard is wrong, authenticated users could see a redundant Sign In link (cosmetic only, not a security risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Information Disclosure | `/demo/credentials` route | accept (scoped to Plan 01) | This plan does not add or modify routes; threat is addressed by Plan 01's compile_env gate. Branding changes carry no new trust boundary. |
| T-142-02 | Spoofing | Nav "Sign In →" link | accept | Link points to `~p"/users/log_in"` (verified path from PATTERNS.md: router.ex:106). The `:if={is_nil(@current_scope)}` guard prevents authenticated users from seeing it. No credentials are collected by this nav link — it redirects to the real login page. |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. Not applicable. |
</threat_model>

<verification>
After both tasks complete:

1. `cd test/example && mix compile` exits 0 with no errors or warnings for the modified files
2. `grep 'default="Vaultr"' test/example/lib/example_web/components/layouts/root.html.heex` — returns match
3. `grep 'suffix=" · Vaultr"' test/example/lib/example_web/components/layouts/root.html.heex` — returns match
4. `grep 'data-testid="app-name"' test/example/lib/example_web/components/layouts.ex` — returns match
5. `grep "Application.spec(:phoenix, :vsn)" test/example/lib/example_web/components/layouts.ex` — returns no match
6. `grep "phoenixframework.org" test/example/lib/example_web/components/layouts.ex` — returns no match
7. `grep "org_switcher" test/example/lib/example_web/components/layouts.ex` — returns match (untouched)
8. `cd test/example && mix test 2>&1 | tail -5` — full suite still passes (branding changes are backward-compatible)
</verification>

<success_criteria>
- Browser `&lt;title&gt;` tag uses `default="Vaultr" suffix=" · Vaultr"` (D-08, SC#3)
- Header brand span shows "Vaultr" with `data-testid="app-name"` (D-06, D-08)
- No Phoenix version string (`Application.spec(:phoenix, :vsn)`) or Phoenix fixture links (`phoenixframework.org`) remain in the layout
- Org-switcher and impersonation_banner rows are byte-for-byte unchanged (D-09)
- `mix compile` and `mix test` (full suite) exit 0 — branding edits are non-breaking
- Zero changes to `lib/sigra/`
</success_criteria>

<output>
Create `.planning/phases/142-dev-credentials-page-app-framing/142-02-SUMMARY.md` when done.
</output>
