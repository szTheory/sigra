---
phase: 142-dev-credentials-page-app-framing
plan: 03
type: execute
wave: 2
depends_on:
  - 142-01
  - 142-02
files_modified:
  - test/example/lib/example/demo/seeds.ex
  - test/example/test/example_web/live/demo/credentials_live_test.exs
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02

must_haves:
  truths:
    - "Seeds.run/0 prints '=== Demo Credentials ===' followed by one line per persona after seeding completes (D-11)"
    - "Seeds stdout calls Personas.feature_map() — NOT a locally-defined copy of the feature map (D-02)"
    - "credentials_live_test.exs: GET /demo/credentials returns 404 in MIX_ENV=test (D-12, T-142-01)"
    - "credentials_live_test.exs: rendered HTML from CredentialsLive.render/1 contains data-testid='demo-credentials-table' (D-06)"
    - "credentials_live_test.exs: rendered HTML contains data-testid='demo-persona-row-admin' through 'demo-persona-row-frank' (D-03, D-06)"
    - "credentials_live_test.exs: rendered HTML contains data-testid='demo-dev-only-badge' (D-06)"
    - "credentials_live_test.exs: rendered HTML contains data-testid='app-name' and string 'Vaultr' (DEMO-02)"
    - "Full suite cd test/example && mix test exits 0"
  artifacts:
    - path: "test/example/lib/example/demo/seeds.ex"
      provides: "print_credentials/0 private function + call from run/0 (D-11)"
      contains: "=== Demo Credentials ==="
    - path: "test/example/test/example_web/live/demo/credentials_live_test.exs"
      provides: "404 env-guard test + rendered-content testid assertions"
      contains: "assert conn.status == 404"
  key_links:
    - from: "test/example/lib/example/demo/seeds.ex"
      to: "Example.Demo.Personas.feature_map/0"
      via: "direct call in print_credentials/0"
      pattern: "Personas\\.feature_map"
    - from: "test/example/test/example_web/live/demo/credentials_live_test.exs"
      to: "ExampleWeb.Demo.CredentialsLive"
      via: "direct render/1 call for HTML content assertions"
      pattern: "CredentialsLive\\.render"
---

<objective>
Wire the seeds stdout block (D-11) using the D-02 feature-map source established in Plan 01, and
create the test file covering the two automated verification behaviors: the 404 env-guard (SC#2,
T-142-01) and the rendered-HTML testid contract (SC#1, DEMO-01, DEMO-02).

Purpose: Closes the remaining SC#1–SC#4 verification gaps: seeds stdout covers SC#4; the test file
covers SC#1 (testid assertions) and SC#2 (404 env-guard). SC#3 (Vaultr in layout) is verified by
the `data-testid="app-name"` + "Vaultr" string assertions in the same test file.

Output:
- `test/example/lib/example/demo/seeds.ex` — `print_credentials/0` private function + call at end of `run/0`
- `test/example/test/example_web/live/demo/credentials_live_test.exs` — new test file (new directory `test/example/test/example_web/live/demo/`)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md
@.planning/phases/142-dev-credentials-page-app-framing/142-RESEARCH.md
@.planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md
@.planning/phases/142-dev-credentials-page-app-framing/142-VALIDATION.md

<!-- Source files — read before implementing -->
@test/example/lib/example/demo/seeds.ex
@test/example/lib/example_web/live/demo/credentials_live.ex
@test/example/test/example_web/live/admin_user_index_live_test.exs

<!-- Prior plan summaries for context on what was built -->
@.planning/phases/142-dev-credentials-page-app-framing/142-01-SUMMARY.md
@.planning/phases/142-dev-credentials-page-app-framing/142-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seeds stdout block — print_credentials/0 calling Personas.feature_map/0 (D-11)</name>
  <files>test/example/lib/example/demo/seeds.ex</files>
  <read_first>
    - test/example/lib/example/demo/seeds.ex — read the full run/0 function (lines 46-57) and the module aliases (lines 20-33); confirm Personas alias is present
    - .planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md — "seeds.ex (edit, run/0 return)" section for the before/after pattern
    - .planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md — D-11 (format: `[persona]  email  password  (feature)`, two spaces between fields, feature in parens), D-02 (call Personas.feature_map — do NOT copy the map)
    - .planning/phases/142-dev-credentials-page-app-framing/142-UI-SPEC.md — Copywriting Contract: "Seeds stdout heading" = `=== Demo Credentials ===`, "Seeds stdout format" = one line per persona
  </read_first>
  <behavior>
    - After run/0 completes seeding: IO.puts outputs "=== Demo Credentials ===" as heading
    - Each persona produces one line: "[local]  email  password  (feature)" — two spaces between fields
    - Local part derived from email split on "@" (same as D-03 pattern in LiveView)
    - Feature text comes from Personas.feature_map()[local] — not a locally-defined copy
    - Six lines total, one per persona, in the order returned by Personas.all()
  </behavior>
  <action>
    Edit `test/example/lib/example/demo/seeds.ex`.

    In `run/0` (lines 46-57), add a call to `print_credentials()` between `seed_audit_events(users)` and the final `:ok` return. The new run/0 tail is:

      seed_audit_events(users)
      print_credentials()
      :ok

    Add a private function `print_credentials/0` after the `run/0` definition:

      defp print_credentials do
        IO.puts("\n=== Demo Credentials ===")

        Personas.all()
        |> Enum.each(fn p ->
          local = p.email |> String.split("@") |> hd()
          feature = Personas.feature_map()[local]
          IO.puts("[#{local}]  #{p.email}  #{p.password}  (#{feature})")
        end)
      end

    Key constraints:
    - Call `Personas.feature_map()[local]` — the `Personas` alias is already present at seeds.ex:33; do NOT import or redefine the map locally (D-02 violation)
    - Output format per UI-SPEC: `[local]  email  password  (feature)` — two spaces between each field segment, feature text in parentheses
    - The `\n` prefix before `=== Demo Credentials ===` provides visual separation from the preceding seed log output
    - Do NOT modify any other part of seeds.ex — only the run/0 tail and the new private function
  </action>
  <verify>
    <automated>cd test/example && mix compile 2>&1 | grep -E "(error|warning)" | grep -v "^$" | head -10</automated>
  </verify>
  <done>
    `grep "print_credentials" test/example/lib/example/demo/seeds.ex` returns at least two matches (call site in run/0 + defp declaration).
    `grep "Personas.feature_map" test/example/lib/example/demo/seeds.ex` returns a match in print_credentials/0.
    `grep "Demo Credentials" test/example/lib/example/demo/seeds.ex` returns a match.
    `mix compile` exits 0 with no errors referencing seeds.ex.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create credentials_live_test.exs — 404 env-guard + rendered HTML testid contract</name>
  <files>test/example/test/example_web/live/demo/credentials_live_test.exs</files>
  <read_first>
    - test/example/test/example_web/live/admin_user_index_live_test.exs — read the module header and describe/test structure; confirm `use ExampleWeb.ConnCase, async: false` pattern and `~s(data-testid="...")` assertion idiom
    - test/example/test/example_web/admin_shell_test.exs (lines 175-185) — read the `build_conn() |> get(...)` + `assert conn.status == 404` pattern
    - test/example/lib/example_web/live/demo/credentials_live.ex — confirm the module exists (from Plan 01) and `feature_map/0` is public; confirm assigns shape for render/1 call
    - test/example/lib/example/demo/personas.ex — confirm Personas.all/0 return shape to build the credentials assign for the render test
    - .planning/phases/142-dev-credentials-page-app-framing/142-RESEARCH.md — Validation Architecture: "Option A" (direct render/1 call), "Pitfall 3" (do NOT use live/2 — NoRouteError), Pattern 4 (testid assertion idiom), Pattern 5 (404 env-guard)
    - .planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md — "credentials_live_test.exs" section for both test describe blocks with full code patterns
    - .planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md — D-12 (404 is the only assertable direction in test env; do NOT use live/2)
  </read_first>
  <behavior>
    - 404 test: build_conn() |> get("/demo/credentials") → conn.status == 404 (compile_env gate compiles route out)
    - Content test: CredentialsLive.render/1 with full assigns map → rendered HTML contains data-testid="demo-credentials-table"
    - Content test: rendered HTML contains data-testid="demo-persona-row-admin"
    - Content test: rendered HTML contains data-testid="demo-persona-row-alice"
    - Content test: rendered HTML contains data-testid="demo-persona-row-bob"
    - Content test: rendered HTML contains data-testid="demo-persona-row-carol"
    - Content test: rendered HTML contains data-testid="demo-persona-row-dave"
    - Content test: rendered HTML contains data-testid="demo-persona-row-frank"
    - Content test: rendered HTML contains data-testid="demo-dev-only-badge"
    - Content test: rendered HTML contains data-testid="app-name" (DEMO-02 — from Layouts.app)
    - Content test: rendered HTML contains "Vaultr" string (DEMO-02)
  </behavior>
  <action>
    Create directory `test/example/test/example_web/live/demo/` and create
    `test/example/test/example_web/live/demo/credentials_live_test.exs`.

    Module structure:
    - `defmodule ExampleWeb.Demo.CredentialsLiveTest do`
    - `use ExampleWeb.ConnCase, async: false` — matches universal convention in this test suite
    - `alias ExampleWeb.Demo.CredentialsLive`
    - `alias Example.Demo.Personas`

    Test block 1 — env-guard (D-12, T-142-01):

      describe "env-guard: /demo/credentials" do
        test "route returns 404 in test env (compile_env gate compiles route out)" do
          conn = build_conn() |> get("/demo/credentials")
          assert conn.status == 404
        end
      end

    `build_conn()` is available from `Phoenix.ConnTest` imported by `ExampleWeb.ConnCase`. Do NOT use `live(conn, "/demo/credentials")` — it raises `Phoenix.Router.NoRouteError` (route compiled out). Use `get/2` which returns a Conn with `status: 404`.

    Test block 2 — rendered HTML contract (DEMO-01, DEMO-02):

      describe "rendered HTML contract" do
        test "contains required testids and branding" do
          credentials =
            Personas.all()
            |> Enum.map(fn p ->
              local = p.email |> String.split("@") |> hd()
              Map.merge(p, %{local: local, feature: Personas.feature_map()[local]})
            end)

          html =
            CredentialsLive.render(%{
              flash: %{},
              page_title: "Demo Credentials",
              credentials: credentials
            })
            |> Phoenix.HTML.safe_to_string()

          # DEMO-01: credentials table testids
          assert html =~ ~s(data-testid="demo-credentials-table")
          assert html =~ ~s(data-testid="demo-persona-row-admin")
          assert html =~ ~s(data-testid="demo-persona-row-alice")
          assert html =~ ~s(data-testid="demo-persona-row-bob")
          assert html =~ ~s(data-testid="demo-persona-row-carol")
          assert html =~ ~s(data-testid="demo-persona-row-dave")
          assert html =~ ~s(data-testid="demo-persona-row-frank")
          assert html =~ ~s(data-testid="demo-dev-only-badge")

          # DEMO-02: branding
          assert html =~ ~s(data-testid="app-name")
          assert html =~ "Vaultr"
        end
      end

    CRITICAL: `CredentialsLive.render/1` returns a `%Phoenix.LiveView.Rendered{}` struct.
    `Phoenix.HTML.safe_to_string/1` converts it to a binary for string assertions.
    If `safe_to_string/1` raises a protocol error on the struct at runtime, the executor
    MUST try the alternative conversion: `IO.iodata_to_binary(rendered)` or calling
    `Phoenix.HTML.html_escape(rendered) |> Phoenix.HTML.safe_to_string()`. The executor
    should verify which conversion cleanly handles `%Phoenix.LiveView.Rendered{}` in the
    Phoenix version present (`test/example/mix.lock`) and use the one that works.
    If neither approach works without routing, add a comment `# Content coverage: Phase 143 Playwright`
    and keep only the 404 test in the automated suite — but attempt the direct render approach first.

    Use `~s(data-testid="...")` sigil (not string interpolation) for all testid assertions — this
    is the idiomatic pattern throughout the test suite (`admin_user_index_live_test.exs:65-66`).
  </action>
  <verify>
    <automated>cd test/example && mix test test/example_web/live/demo/credentials_live_test.exs 2>&1</automated>
  </verify>
  <done>
    `mix test test/example_web/live/demo/credentials_live_test.exs` exits 0 with all tests passing.
    The output shows at minimum: "2 tests, 0 failures" (env-guard test + rendered HTML contract test).
    `cd test/example && mix test` (full suite) also exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test env → compiled router | The 404 test verifies the compile_env gate held — route is absent in MIX_ENV=test |
| Seeds stdout → stdout channel | Demo credentials are printed to stdout intentionally; this is in the demo harness only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Information Disclosure | `/demo/credentials` route leak into non-dev build | mitigate | The `test "route returns 404 in test env"` test directly asserts that `compile_env(:example, :dev_routes)` compiled the route out. A regression in the compile_env gate would surface as a test failure (GET /demo/credentials returning non-404). |
| T-142-03 | Information Disclosure | Seeds stdout printing demo passwords | accept | Seeds.run/0 only runs in MIX_ENV=dev (the raise-guard in seeds.exs blocks MIX_ENV=test). Demo passwords are public-by-design. Printing them to stdout for evaluator convenience is the explicit purpose of D-11 / SC#4. Blast radius: demo harness only. |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. Not applicable. |
</threat_model>

<verification>
After both tasks complete:

1. `cd test/example && mix test test/example_web/live/demo/credentials_live_test.exs` — exits 0, all tests pass
2. `cd test/example && mix test` — full suite exits 0
3. `grep "print_credentials" test/example/lib/example/demo/seeds.ex` — returns two matches
4. `grep "Personas.feature_map" test/example/lib/example/demo/seeds.ex` — returns a match in print_credentials/0
5. `grep "Demo Credentials" test/example/lib/example/demo/seeds.ex` — returns a match
6. `grep "assert conn.status == 404" test/example/test/example_web/live/demo/credentials_live_test.exs` — returns a match (D-12 verification)
7. `grep 'data-testid="demo-credentials-table"' test/example/test/example_web/live/demo/credentials_live_test.exs` — returns a match (DEMO-01 testid coverage)
</verification>

<success_criteria>
- `Example.Demo.Seeds.run/0` prints `=== Demo Credentials ===` then one line per persona in format `[local]  email  password  (feature)` — reads feature text from `Personas.feature_map/0` (D-02, D-11, SC#4)
- `credentials_live_test.exs` contains a test asserting `conn.status == 404` for `GET /demo/credentials` in test env (D-12, SC#2, T-142-01)
- `credentials_live_test.exs` contains testid assertions covering all 6 `demo-persona-row-{local}` values, `demo-credentials-table`, `demo-dev-only-badge`, and `data-testid="app-name"` + "Vaultr" string (DEMO-01, DEMO-02, SC#1, SC#3)
- `mix test test/example_web/live/demo/credentials_live_test.exs` exits 0, all tests pass
- `mix test` (full example suite) exits 0
- Zero changes to `lib/sigra/`
</success_criteria>

<output>
Update `.planning/phases/142-dev-credentials-page-app-framing/142-VALIDATION.md`:
- Set `nyquist_compliant: true` in frontmatter
- Set `wave_0_complete: true`
- Fill in the Per-Task Verification Map with real task IDs (142-01-T1, 142-01-T2, 142-02-T1, 142-02-T2, 142-03-T1, 142-03-T2) and their plan/wave/status

Create `.planning/phases/142-dev-credentials-page-app-framing/142-03-SUMMARY.md` when done.
</output>
