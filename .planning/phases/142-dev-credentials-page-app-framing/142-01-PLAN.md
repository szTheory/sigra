---
phase: 142-dev-credentials-page-app-framing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example/demo/personas.ex
  - test/example/lib/example_web/live/demo/credentials_live.ex
  - test/example/lib/example_web/router.ex
autonomous: true
requirements:
  - DEMO-01

must_haves:
  truths:
    - "The feature-text map is defined exactly once in Example.Demo.Personas.feature_map/0 and reachable by both the LiveView and Seeds (D-02)"
    - "ExampleWeb.Demo.CredentialsLive exists and compiles without errors"
    - "CredentialsLive.render/1 wraps content in <Layouts.app flash={@flash}> — no other layout form (D-04)"
    - "Rendered HTML contains data-testid='demo-credentials-table' on the hand-rolled <table> element (D-05, D-06)"
    - "Each persona row has data-testid='demo-persona-row-{local}' where {local} = email split on '@' (D-03, D-06)"
    - "DEV ONLY badge has data-testid='demo-dev-only-badge' and class 'badge badge-warning badge-sm' (D-06, D-07)"
    - "Password cells use <code class='font-mono text-sm'> (D-07)"
    - "Router has live '/credentials', Demo.CredentialsLive inside the compile_env(:example, :dev_routes) if-block (D-12)"
  artifacts:
    - path: "test/example/lib/example/demo/personas.ex"
      provides: "feature_map/0 public function — single source for feature text (D-02)"
      contains: "def feature_map"
    - path: "test/example/lib/example_web/live/demo/credentials_live.ex"
      provides: "ExampleWeb.Demo.CredentialsLive read-only LiveView"
      exports: ["mount/3", "render/1", "feature_map/0 — NOT used; Personas.feature_map/0 is the source"]
    - path: "test/example/lib/example_web/router.ex"
      provides: "Dev-only /demo/credentials route registration"
      contains: "live \"/credentials\", Demo.CredentialsLive"
  key_links:
    - from: "test/example/lib/example_web/live/demo/credentials_live.ex"
      to: "Example.Demo.Personas.feature_map/0"
      via: "alias + direct call in mount/3"
      pattern: "Personas\\.feature_map"
    - from: "test/example/lib/example_web/router.ex"
      to: "ExampleWeb.Demo.CredentialsLive"
      via: "live route inside compile_env if-block"
      pattern: "compile_env\\(:example, :dev_routes\\)"
---

<objective>
Deliver the `ExampleWeb.Demo.CredentialsLive` read-only LiveView at `/demo/credentials`, gated
behind the existing compile-env dev-route guard, and establish `Example.Demo.Personas.feature_map/0`
as the D-02 single source of truth for persona feature copy consumed by both the credentials page
and (in Plan 03) the seeds stdout block.

Purpose: DEMO-01 — evaluators can open `/demo/credentials` in a dev server and see every persona's
credentials plus a plain-language description of the auth feature each demonstrates.

Output:
- `test/example/lib/example/demo/personas.ex` — `feature_map/0` public function added
- `test/example/lib/example_web/live/demo/credentials_live.ex` — new LiveView
- `test/example/lib/example_web/router.ex` — `/demo/credentials` live route added to the
  existing `if Application.compile_env(:example, :dev_routes)` block
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md
@.planning/phases/142-dev-credentials-page-app-framing/142-UI-SPEC.md
@.planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md
@.planning/phases/142-dev-credentials-page-app-framing/142-RESEARCH.md

<!-- Analog files — read before implementing -->
@test/example/lib/example/demo/personas.ex
@test/example/lib/example_web/live/organization_settings_live.ex
@test/example/lib/example_web/router.ex
@test/example/lib/example_web/components/layouts.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Personas.feature_map/0 — D-02 single source for feature copy</name>
  <files>test/example/lib/example/demo/personas.ex</files>
  <read_first>
    - test/example/lib/example/demo/personas.ex — read full file to find insertion point after all/0 or at end of module
    - .planning/phases/142-dev-credentials-page-app-framing/142-UI-SPEC.md — Copywriting Contract table for verbatim feature strings
    - .planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md — D-01 (feature copy joined by email local part), D-02 (single source), D-11 (seeds also calls this)
  </read_first>
  <action>
    Add a public `feature_map/0` function to `Example.Demo.Personas` that returns a map keyed by email local part (string before "@") to the verbatim feature text locked in the UI-SPEC copywriting contract. Place it after the existing `all/0` function as the last public function in the module.

    The six keys and their EXACT values from the UI-SPEC copywriting table (do not paraphrase):
    - "admin" => "Admin — TOTP MFA, passkey display row, multi-org owner, rich audit trail"
    - "alice" => "Standard confirmed user — happy path login, Acme Corp member"
    - "bob"   => "TOTP MFA enrolled — org owner (Beta Labs)"
    - "carol" => "OAuth identity — GitHub-linked login (carol@demo.sigra.dev)"
    - "dave"  => "Locked account — failed login attempts exhausted, unconfirmed"
    - "frank" => "Scheduled deletion — account marked for deletion"

    Add a `@doc` comment: "Returns the feature-text map keyed by email local part. Single source of truth (D-02) consumed by CredentialsLive and Seeds.run/0. Keys are the email local part (string before '@') for all six @demo.sigra.dev personas."

    This is a pure-data function — no DB calls, no dependencies beyond the module itself. Return type is `%{String.t() => String.t()}`.
  </action>
  <verify>
    <automated>cd test/example && mix compile 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    `Example.Demo.Personas.feature_map/0` is callable and returns a map with exactly 6 string keys: "admin", "alice", "bob", "carol", "dave", "frank". `mix compile` exits 0 with no errors or warnings about personas.ex.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExampleWeb.Demo.CredentialsLive + add /demo/credentials route</name>
  <files>
    test/example/lib/example_web/live/demo/credentials_live.ex
    test/example/lib/example_web/router.ex
  </files>
  <read_first>
    - test/example/lib/example_web/live/organization_settings_live.ex (lines 55-61) — EXACT reference for `&lt;Layouts.app flash={@flash}&gt;` explicit wrap pattern (D-04)
    - test/example/lib/example_web/router.ex (lines 168-177) — EXACT current compile_env if-block; add /demo scope inside the same if, NOT outside
    - test/example/lib/example_web/components/layouts.ex (lines 44-86) — confirms flash required:true; current_scope/user_organizations have safe defaults
    - test/example/lib/example_web/components/core_components.ex (lines 309-390) — confirms &lt;.header&gt; slot shape and &lt;.table&gt; has no testid passthrough (D-05)
    - .planning/phases/142-dev-credentials-page-app-framing/142-PATTERNS.md — full module skeleton + hand-rolled table pattern + router edit pattern
    - .planning/phases/142-dev-credentials-page-app-framing/142-UI-SPEC.md — Copywriting Contract (page heading, badge text, column headers, disclaimer)
    - .planning/phases/142-dev-credentials-page-app-framing/142-CONTEXT.md — D-03, D-04, D-05, D-06, D-07
  </read_first>
  <action>
    Create directory `test/example/lib/example_web/live/demo/` and create
    `test/example/lib/example_web/live/demo/credentials_live.ex` implementing
    `ExampleWeb.Demo.CredentialsLive`.

    Module structure requirements:
    - `use ExampleWeb, :live_view`
    - `alias Example.Demo.Personas` and `alias ExampleWeb.Layouts`
    - `mount/3`: call `Personas.all()`, map each persona to add `:local` (split email on "@", take head) and `:feature` (from `Personas.feature_map()[local]`), assign `credentials:` and `page_title: "Demo Credentials"`; no auth socket assigns needed
    - `render/1`: wrap ALL content in `&lt;Layouts.app flash={@flash}&gt;` — no other layout form; `current_scope` and `user_organizations` may be omitted (they have safe defaults in layouts.ex)

    Inside the layout wrapper, per UI-SPEC copywriting contract:
    - `&lt;.header&gt;` CoreComponent with text "Demo Credentials"; place DEV ONLY badge in the `:actions` slot as `&lt;span class="badge badge-warning badge-sm" data-testid="demo-dev-only-badge"&gt;DEV ONLY&lt;/span&gt;`
    - A `&lt;p&gt;` with disclaimer text: "This page is only available in development mode." (class: `text-sm text-base-content/60`)
    - Hand-rolled `&lt;table class="table table-zebra" data-testid="demo-credentials-table"&gt;` with thead columns: Persona, Email, Password, Auth Feature Demonstrated
    - tbody rows with `:for={c &lt;- @credentials}` and `data-testid={"demo-persona-row-#{c.local}"}` on each `&lt;tr&gt;`; password cell uses `&lt;code class="font-mono text-sm"&gt;{c.password}&lt;/code&gt;`
    - A `&lt;p&gt;` with credentials disclaimer: "Passwords are public-by-design demo credentials. Never use in production." (class: `text-xs text-base-content/60`)

    Do NOT use the `&lt;.table&gt;` CoreComponent — it has no data-testid passthrough on `&lt;table&gt;` or `&lt;tr&gt;` (D-05). Do NOT pass `current_scope` or `user_organizations` to Layouts.app (safe to omit). Do NOT define a feature_map locally — call `Personas.feature_map()` (D-02).

    Router edit — in `test/example/lib/example_web/router.ex`, add a second scope INSIDE the existing `if Application.compile_env(:example, :dev_routes) do ... end` block (lines 172-177), after the `/dev` scope:

      scope "/demo" do
        pipe_through :browser
        live "/credentials", Demo.CredentialsLive
      end

    The if-block becomes: `/dev` scope first, then `/demo` scope. Both inside the same `if`. Do NOT move or alter the existing `/dev/mailbox` forward. Do NOT add a new `if` block.
  </action>
  <verify>
    <automated>cd test/example && mix compile 2>&1 | grep -E "(error|warning)" | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    `mix compile` exits 0 with no errors referencing credentials_live.ex or router.ex.
    `grep -r "demo-credentials-table" test/example/lib/` returns at least one match.
    `grep -r "demo-dev-only-badge" test/example/lib/` returns at least one match.
    `grep "Demo.CredentialsLive" test/example/lib/example_web/router.ex` returns a match.
    `grep "Application.compile_env(:example, :dev_routes)" test/example/lib/example_web/router.ex` shows a single if-block containing both Demo and dev scopes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev env → non-dev build | `/demo/credentials` publicly lists demo passwords; must not be reachable in test or prod builds |
| Anonymous browser → LiveView | Route requires no authentication — intentional for evaluator access; only safe because data is public-by-design demo credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Information Disclosure | `ExampleWeb.Demo.CredentialsLive` via `/demo/credentials` route | mitigate | Route is gated by `if Application.compile_env(:example, :dev_routes)` — resolves at compile time to `nil`/false in test and prod, compiling the scope out of the router entirely. The 404 / route-absent test (Plan 03 Task 1) verifies the gate holds in `MIX_ENV=test`. Blast radius is the `test/example/` demo harness only, not `lib/sigra/`. |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. Zero install tasks. Not applicable. |
</threat_model>

<verification>
After both tasks complete:

1. `cd test/example && mix compile` exits 0 — no errors or warnings referencing the modified files
2. `grep "def feature_map" test/example/lib/example/demo/personas.ex` returns a match — D-02 single source present
3. `grep -r "demo-credentials-table" test/example/lib/` returns a match in credentials_live.ex
4. `grep -r "demo-persona-row" test/example/lib/` returns a match with the `:for` loop pattern
5. `grep -r "demo-dev-only-badge" test/example/lib/` returns a match in credentials_live.ex
6. `grep "Demo.CredentialsLive" test/example/lib/example_web/router.ex` returns a match inside the compile_env if-block
7. `grep "Personas.feature_map" test/example/lib/example_web/live/demo/credentials_live.ex` returns a match — LiveView calls domain function, not own copy
</verification>

<success_criteria>
- `Example.Demo.Personas.feature_map/0` exists, is public, returns a map with exactly the 6 verbatim UI-SPEC feature strings keyed by email local part (D-01, D-02)
- `ExampleWeb.Demo.CredentialsLive` compiles without errors; render wraps in `&lt;Layouts.app flash={@flash}&gt;` (D-04); uses hand-rolled table with `data-testid="demo-credentials-table"` (D-05, D-06); per-row testids derive from email split on "@" (D-03); DEV ONLY badge has `data-testid="demo-dev-only-badge"` and class `badge badge-warning badge-sm` (D-06, D-07); password cells use `&lt;code class="font-mono text-sm"&gt;` (D-07)
- `router.ex` has `live "/credentials", Demo.CredentialsLive` inside the existing `Application.compile_env(:example, :dev_routes)` if-block alongside the `/dev/mailbox` forward (D-12)
- Zero changes to `lib/sigra/`
</success_criteria>

<output>
Create `.planning/phases/142-dev-credentials-page-app-framing/142-01-SUMMARY.md` when done.
</output>
