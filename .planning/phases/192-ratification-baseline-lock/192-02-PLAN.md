---
phase: 192-ratification-baseline-lock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/install/golden_diff_test.exs
  - test/sigra/install/vault_promotion_test.exs
  - test/example/priv/playwright/tests/admin-design.spec.ts
  - test/sigra/planning/phase_192_known_failure_contract_test.exs
  - .planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md
  - .planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md
autonomous: true
requirements:
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - "golden_diff_test.exs carries @moduletag known_failure: with a reason string, 'reproduces on origin/main', and the tracking todo path"
    - "vault_promotion_test.exs carries @moduletag known_failure: with a reason string, 'reproduces on origin/main', and the tracking todo path"
    - "admin-design.spec.ts MG-5/6 test at ~line 314 is marked test.fail() with an explanatory reason string"
    - "mix test --exclude known_failure exits 0 (all non-quarantined tests green)"
    - "mix test --only known_failure reports 2 expected failures (the two ExUnit tests; the MG-5/6 test is Playwright)"
    - "A new self-healing contract test exists at test/sigra/planning/phase_192_known_failure_contract_test.exs asserting exactly these 3 known failures are still red"
    - "Two tracking todos exist for the installer failures"
  artifacts:
    - path: "test/sigra/planning/phase_192_known_failure_contract_test.exs"
      provides: "Self-healing known-failure contract — goes green (fails CI) if any quarantined test starts passing"
      exports: ["Sigra.Planning.Phase192KnownFailureContractTest"]
    - path: ".planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md"
      provides: "Tracking todo for golden_diff_test.exs known failure"
    - path: ".planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md"
      provides: "Tracking todo for vault_promotion_test.exs known failure"
  key_links:
    - from: "test/sigra/planning/phase_192_known_failure_contract_test.exs"
      to: "test/sigra/install/golden_diff_test.exs and test/sigra/install/vault_promotion_test.exs"
      via: "ExUnit.Case tag exclusion/inclusion: --exclude known_failure / --only known_failure"
      pattern: "known_failure"
    - from: "test/sigra/planning/phase_192_known_failure_contract_test.exs"
      to: "test/sigra/planning/phase_51_install_golden_ci_contract_test.exs"
      via: "Modeled on the same file-read + assertion pattern"
      pattern: "known_failure"
---

<objective>
Implement the executable quarantine for the 3 known pre-existing v1.39 failures per D-11/D-12:
tag the two installer ExUnit tests with @moduletag known_failure, mark the admin-design MG-5/6
Playwright test with test.fail(), create the self-healing contract test (modeled on
phase_51_install_golden_ci_contract_test.exs), and create the 2 missing tracking todos.

Purpose: D-11 requires an executable quarantine (not prose) so that the blocking suite
(`--exclude known_failure`) stays green while the known failures are reported-but-not-blocking.
D-12 requires a self-healing contract test that goes red if any quarantined test starts passing,
preventing "quarantine rot" where stale tags silently accumulate. The 3 failures are:
  1. test/sigra/install/golden_diff_test.exs — installer/template golden-tree diff
  2. test/sigra/install/vault_promotion_test.exs — installer/template lane
  3. admin-design.spec.ts MG-5/6 — data-dependent pagination (needs 25+ audit events)

Output: Tagged ExUnit tests, test.fail() in Playwright, new contract test module, 2 new todos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-ratification-baseline-lock/192-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag installer ExUnit tests with @moduletag known_failure (D-11)</name>
  <files>
    test/sigra/install/golden_diff_test.exs,
    test/sigra/install/vault_promotion_test.exs
  </files>

  <read_first>
    - test/sigra/install/golden_diff_test.exs (full file — understand existing @moduletag lines at ~38-39 and the test failure reason: generated-tree byte diff vs committed fixture)
    - test/sigra/install/vault_promotion_test.exs (full file — understand existing @moduletag lines at ~6-7 and the test failure reason: undefined attribute "type" for CoreComponents.button/1 under --warnings-as-errors)
    - .planning/phases/191-microcopy-ia-sweep/191-VERIFICATION.md (lines 108-111 — the documented known-failure carve-out and the exact failure descriptions)
    - .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md (bottom section notes both installer failures and their reasons — use these as the reason strings)
    - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs (model for pattern, but DO NOT modify this file)
  </read_first>

  <action>
    In `test/sigra/install/golden_diff_test.exs`: add a new @moduletag line immediately after
    the last existing @moduletag line (after `@moduletag timeout: 300_000`). The tag must be:

      @moduletag known_failure: "generated-tree byte diff vs committed fixture; reproduces on origin/main; tracked: .planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md"

    In `test/sigra/install/vault_promotion_test.exs`: add a new @moduletag line immediately after
    the last existing @moduletag line (after `@moduletag timeout: 600_000`). The tag must be:

      @moduletag known_failure: "undefined attribute for CoreComponents.button/1 under --warnings-as-errors; installer template diverged from generated host; reproduces on origin/main; tracked: .planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md"

    The @moduletag form uses a keyword list syntax: `known_failure: "..."`. Do NOT use
    `@moduletag :known_failure` (atom) — the string value is needed so the self-healing
    contract test can assert the reason text. Both @moduletag additions go on their own lines,
    preserving the existing tag ordering.

    Do NOT modify any test logic, assertions, or other module attributes in either file.
  </action>

  <verify>
    <automated>grep -c "known_failure" test/sigra/install/golden_diff_test.exs test/sigra/install/vault_promotion_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "known_failure" test/sigra/install/golden_diff_test.exs` returns 1
    - `grep -c "known_failure" test/sigra/install/vault_promotion_test.exs` returns 1
    - `grep "known_failure" test/sigra/install/golden_diff_test.exs` contains "reproduces on origin/main"
    - `grep "known_failure" test/sigra/install/vault_promotion_test.exs` contains "reproduces on origin/main"
    - `grep "known_failure" test/sigra/install/golden_diff_test.exs` contains ".planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md"
    - `grep "known_failure" test/sigra/install/vault_promotion_test.exs` contains ".planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md"
    - Existing @moduletag lines (:golden, timeout, :install) remain untouched
  </acceptance_criteria>

  <done>Both installer ExUnit test modules carry a @moduletag known_failure with a reason, "reproduces on origin/main", and the tracking todo path. Existing tags are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Mark MG-5/6 Playwright test with test.fail() and create self-healing contract test + tracking todos (D-11/D-12)</name>
  <files>
    test/example/priv/playwright/tests/admin-design.spec.ts,
    test/sigra/planning/phase_192_known_failure_contract_test.exs,
    .planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md,
    .planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md
  </files>

  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts (lines 314-360 — the MG-5/6 test; understand its current form so test.fail() wraps the test correctly)
    - .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md (the MG-5/6 failure reason and fix direction — use as the test.fail() reason string)
    - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs (the model for the contract test — read every line; the phase_192 contract test follows the same pattern)
    - test/sigra/install/golden_diff_test.exs (for the module name Sigra.Install.GoldenDiffTest and test description at line ~53)
    - test/sigra/install/vault_promotion_test.exs (for the module name Sigra.Install.VaultPromotionTest and test description at line ~9)
    - .planning/phases/191-microcopy-ia-sweep/191-VERIFICATION.md (lines 108-111 for the exact failure carve-out notes)
  </read_first>

  <action>
    **Step A — admin-design.spec.ts MG-5/6 test.fail():**

    In `test/example/priv/playwright/tests/admin-design.spec.ts`, locate the test at ~line 314:
      `test('MG-5 and MG-6 desktop and mobile representations are content-equivalent', async ({`

    Add `test.fail(...)` immediately before the test body's opening brace to mark it as an
    expected failure. The Playwright `test.fail()` API called inside a test body (no arguments)
    marks the current test as expected to fail. Add it as the very first statement inside
    the test function body, with an inline comment:

      // known_failure: data-dependent pagination — needs 25+ audit events per user;
      // reproduces on origin/main; tracked: .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md
      test.fail();

    Do NOT modify any assertions in the test body. Do NOT rename or restructure the test.
    Do NOT touch any other test in admin-design.spec.ts.

    **Step B — self-healing contract test:**

    Create `test/sigra/planning/phase_192_known_failure_contract_test.exs` modeled on
    `test/sigra/planning/phase_51_install_golden_ci_contract_test.exs`.

    The module should be `Sigra.Planning.Phase192KnownFailureContractTest`.

    The contract test must assert EXACTLY these three conditions — each as a separate test:
      1. `golden_diff_test.exs` has exactly 1 `@moduletag known_failure` line
      2. `vault_promotion_test.exs` has exactly 1 `@moduletag known_failure` line
      3. `admin-design.spec.ts` contains the string `test.fail()` inside the MG-5/6 test block
         (assert the file contains the string `test.fail()` at least once in the block after
         'MG-5 and MG-6 desktop and mobile representations are content-equivalent')

    Use the same `root/0` + `read!/1` helper pattern from phase_51. Keep the test file minimal:
    `use ExUnit.Case, async: true`, three short tests, no external dependencies.

    The file's @moduledoc must explain: "Self-healing contract: if any quarantined known failure
    starts passing, remove its tag and resolve the tracking todo. This test goes green (fails CI)
    if any quarantine tag is removed, forcing cleanup."

    Do NOT model the assertion content directly on phase_51's CI yaml checks — this test checks
    source file tag presence, not CI yaml structure.

    **Step C — tracking todos:**

    Create `.planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md`:
    ```
    ---
    created: 2026-06-18T00:00:00.000Z
    status: pending
    title: golden_diff_test.exs known failure — generated-tree byte diff vs committed fixture
    area: test
    files:
      - test/sigra/install/golden_diff_test.exs
    source: phase 192 quarantine (D-11/D-12)
    ---

    ## What

    `test/sigra/install/golden_diff_test.exs` fails with a generated-tree byte diff
    vs the committed fixture in `test/fixtures/install_golden/`. Reproduces identically
    on clean `origin/main` — not a phase-192 regression.

    ## Fix direction

    Update the install golden fixture: run `mix sigra.install --yes` in a fresh app,
    normalize the tree, and commit the updated fixture per the runbook in
    `.planning/phases/11-generator-feature-system/11-01-SUMMARY.md`. Confirm the template
    and the example are in sync first.

    ## Quarantine

    Tagged `@moduletag known_failure: "..."` in phase 192. Remove the tag when the fixture
    is updated and the test is green on both local and CI.
    ```

    Create `.planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md`:
    ```
    ---
    created: 2026-06-18T00:00:00.000Z
    status: pending
    title: vault_promotion_test.exs known failure — undefined attribute for CoreComponents.button/1 under --warnings-as-errors
    area: test
    files:
      - test/sigra/install/vault_promotion_test.exs
    source: phase 192 quarantine (D-11/D-12)
    ---

    ## What

    `test/sigra/install/vault_promotion_test.exs` fails because the installed app's
    CoreComponents.button/1 does not accept a "type" attribute under --warnings-as-errors.
    The installer template emits code using an attribute that the generated host's
    CoreComponents does not expose. Reproduces identically on clean `origin/main`.

    ## Fix direction

    Update the installer template for the passkeys vault path: either (a) remove the
    `type` attribute from the generated button call, or (b) align with the CoreComponents
    API in the target Phoenix/LiveView version. Confirm fix against phx_new 1.8.7
    (SEED-004 pinned version).

    ## Quarantine

    Tagged `@moduletag known_failure: "..."` in phase 192. Remove the tag when the
    template is fixed and the test is green on both local and CI.
    ```

    Write both todo files with the exact YAML frontmatter and body shown above (substituting
    only the creation timestamp to match today's date 2026-06-18). Do NOT include fenced code
    block markers in the actual written files — write the YAML frontmatter and Markdown body
    as plain text.
  </action>

  <verify>
    <automated>grep -c "test.fail" test/example/priv/playwright/tests/admin-design.spec.ts && grep -c "known_failure" test/sigra/planning/phase_192_known_failure_contract_test.exs && ls .planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md .planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "test.fail" test/example/priv/playwright/tests/admin-design.spec.ts` returns 1
    - `test/sigra/planning/phase_192_known_failure_contract_test.exs` exists and defines module `Sigra.Planning.Phase192KnownFailureContractTest`
    - The contract test file has 3 tests, each asserting one quarantined failure's tag is present
    - `mix test test/sigra/planning/phase_192_known_failure_contract_test.exs` exits 0 (contract passes because tags are now present)
    - Both tracking todo files exist under `.planning/todos/pending/`
    - `.planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md` has YAML frontmatter with `status: pending` and `source: phase 192 quarantine (D-11/D-12)`
    - `.planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md` has YAML frontmatter with `status: pending` and `source: phase 192 quarantine (D-11/D-12)`
    - `mix test --exclude known_failure` exits 0 (blocked suite green, installer tests excluded)
    - `mix test --only known_failure 2>&1 | grep -E "Finished|failures"` shows the 2 ExUnit known failures
  </acceptance_criteria>

  <done>MG-5/6 Playwright test marked test.fail(). Self-healing contract test module created and green. Two installer tracking todos created. mix test --exclude known_failure exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → file system | Contract test reads source files from disk using File.read! — paths are hardcoded repo-relative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-02-01 | Information disclosure | known_failure reason string | accept | Reason strings are developer-facing, committed to the repo; no PII or secrets |
| T-192-02-SC | Tampering | No new npm/hex installs | accept | No new packages introduced; ExUnit and Playwright already in dependency set |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "known_failure" test/sigra/install/golden_diff_test.exs` → `1`
2. `grep -c "known_failure" test/sigra/install/vault_promotion_test.exs` → `1`
3. `grep -c "test.fail" test/example/priv/playwright/tests/admin-design.spec.ts` → `1`
4. `ls test/sigra/planning/phase_192_known_failure_contract_test.exs` → file exists
5. `mix test test/sigra/planning/phase_192_known_failure_contract_test.exs` → exits 0
6. `mix test --exclude known_failure` → exits 0
7. `mix test --only known_failure 2>&1` → reports 2 failures (golden_diff + vault_promotion), does NOT exit non-zero (quarantine lane is non-blocking with `|| echo "KNOWN-FAILURE LANE…"`)
8. `ls .planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md` → exists
9. `ls .planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md` → exists
</verification>

<success_criteria>
The 3 known pre-existing failures are quarantined via executable tags, not prose.
The blocking suite (--exclude known_failure) is green.
The self-healing contract test is green (tags present = contract satisfied).
Two tracking todos exist for the installer failures.
No quarantined failure is remediated — they remain red in the quarantine lane.
</success_criteria>

## Artifacts This Phase Produces

New files:
- `test/sigra/planning/phase_192_known_failure_contract_test.exs` — module `Sigra.Planning.Phase192KnownFailureContractTest`
- `.planning/todos/pending/2026-06-18-install-golden-diff-known-failure.md`
- `.planning/todos/pending/2026-06-18-install-vault-promotion-known-failure.md`

New symbols in existing files:
- `@moduletag known_failure: "..."` in `test/sigra/install/golden_diff_test.exs`
- `@moduletag known_failure: "..."` in `test/sigra/install/vault_promotion_test.exs`
- `test.fail()` call + comment in `test/example/priv/playwright/tests/admin-design.spec.ts` (MG-5/6 test body)

<output>
Create `.planning/phases/192-ratification-baseline-lock/192-02-SUMMARY.md` when done.
</output>
