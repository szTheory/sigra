---
phase: 192-ratification-baseline-lock
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - GATE-01

must_haves:
  truths:
    - "GATE-01 in REQUIREMENTS.md no longer says 'deliberately recaptured via the recapture gate' or 'reset to empty'"
    - "GATE-01 wording preserves the idempotency intent: zero-drift re-render + canary/allowlist invariants"
    - "GATE-01 wording removes the internal contradiction (force-recapture is self-contradictory for an idempotent system)"
    - "GATE-02 and GATE-03 lines are unchanged"
    - "No other content in REQUIREMENTS.md is modified"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Non-contradictory GATE-01 requirement aligned with D-04 idempotency interpretation"
      contains: "idempotent"
  key_links:
    - from: ".planning/REQUIREMENTS.md GATE-01"
      to: ".planning/phases/192-ratification-baseline-lock/192-CONTEXT.md D-04"
      via: "Reword removes force-recapture contradiction; preserves idempotency + canary/allowlist invariant intent"
      pattern: "idempotent"
---

<objective>
Reword GATE-01 in REQUIREMENTS.md to remove the internal contradiction introduced by the original
phrasing ("deliberately recapture all baselines + reset allowlists to empty"), replacing it with
the correct idempotency intent per D-04.

Purpose: The original GATE-01 wording says "deliberately recaptured via the recapture gate" and
"reset both allowlists to empty." This is self-contradictory for an idempotent system: the recapture
gate requires intended slugs and `--require-all` (a change-tool), but an idempotent re-render produces
no delta, so "every declared slug must change" fails by construction. D-04 confirmed with the
maintainer: GATE-01 means "re-render and prove zero-drift idempotency + canary/allowlist invariants."
The allowlists are ALREADY at steady-state empty — that empty state is what we lock and prove, not a
step to perform.

Output: GATE-01 line in REQUIREMENTS.md updated to reflect the idempotency intent without the
force-recapture contradiction. GATE-02 and GATE-03 lines are untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-ratification-baseline-lock/192-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword GATE-01 in REQUIREMENTS.md to remove force-recapture contradiction (D-04)</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (full file — read the exact current GATE-01 text at line ~82, and read GATE-02 and GATE-03 at lines ~83-84 to ensure they are left untouched)
    - .planning/phases/192-ratification-baseline-lock/192-CONTEXT.md (D-04 section — for the idempotency intent, and the exact quote: "deliberately re-render and prove zero-drift idempotency + canary/allowlist invariants")
  </read_first>

  <action>
    In `.planning/REQUIREMENTS.md`, locate the GATE-01 line (currently at ~line 82):

      `- [ ] **GATE-01**: All baselines (admin checkpoints + gallery boards) are deliberately recaptured via the recapture gate; both allowlists are reset to empty; both canaries are green.`

    Replace it with a reworded version that:
    - Removes "deliberately recaptured via the recapture gate" (the force-recapture contradiction)
    - Removes "both allowlists are reset to empty" (the empty state is proven, not performed)
    - Preserves the idempotency intent: baselines are verified as zero-drift via compare mode
    - Preserves the canary/allowlist invariants: both canaries green, both allowlists at
      steady-state empty (comments only)
    - Remains a single bullet-point line consistent with the format of GATE-02 and GATE-03
    - Contains the word "idempotent" or "idempotency" so the invariant is greppable

    Suggested wording (exact wording is planner's discretion per D-04, as long as it removes
    the contradiction and preserves idempotency intent):

      `- [ ] **GATE-01**: All baselines (admin checkpoints + gallery boards) are proven idempotent via compare-mode zero-drift re-render; both allowlists are verified at steady-state empty (comments only); both canaries are green and byte-stable.`

    Do NOT change the checkbox state `[ ]`. Do NOT modify GATE-02 or GATE-03. Do NOT change any
    other line in REQUIREMENTS.md. Use Edit (scoped replacement) — never rewrite the whole file.
  </action>

  <verify>
    <automated>grep -c "idempotent" .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "idempotent" .planning/REQUIREMENTS.md` returns at least 1
    - `grep "GATE-01" .planning/REQUIREMENTS.md` does NOT contain "recaptured via the recapture gate"
    - `grep "GATE-01" .planning/REQUIREMENTS.md` does NOT contain "reset to empty"
    - `grep "GATE-01" .planning/REQUIREMENTS.md` contains "idempotent" or "idempotency"
    - `grep "GATE-01" .planning/REQUIREMENTS.md` contains "canaries"
    - `grep "GATE-02" .planning/REQUIREMENTS.md` is unchanged from its original text
    - `grep "GATE-03" .planning/REQUIREMENTS.md` is unchanged from its original text
  </acceptance_criteria>

  <done>GATE-01 in REQUIREMENTS.md no longer contains the force-recapture contradiction; it now expresses the idempotency/zero-drift intent, canary-green invariant, and allowlist steady-state empty assertion per D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → developer | REQUIREMENTS.md is read by humans and tooling; wording change has no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-03-SC | Tampering | No npm/hex installs | accept | No packages introduced; pure documentation edit |
</threat_model>

<verification>
After the task completes:

1. `grep "GATE-01" .planning/REQUIREMENTS.md` — must NOT contain "recaptured via the recapture gate"
2. `grep "GATE-01" .planning/REQUIREMENTS.md` — must NOT contain "reset to empty"
3. `grep -c "idempotent" .planning/REQUIREMENTS.md` — returns ≥ 1
4. `grep "GATE-02" .planning/REQUIREMENTS.md` — unchanged (contains "RUN_PARITY=1")
5. `grep "GATE-03" .planning/REQUIREMENTS.md` — unchanged (contains "monotonic guard")
</verification>

<success_criteria>
GATE-01 phrasing in REQUIREMENTS.md expresses the idempotency/zero-drift intent without the
force-recapture self-contradiction. The word "idempotent" (or "idempotency") is present and
greppable. GATE-02 and GATE-03 are byte-identical to their pre-plan state.
</success_criteria>

## Artifacts This Phase Produces

No new files. Modified file:
- `.planning/REQUIREMENTS.md` — GATE-01 line reworded (idempotency-first, no force-recapture contradiction)

New symbol introduced: the word "idempotent" now appears in GATE-01.

<output>
Create `.planning/phases/192-ratification-baseline-lock/192-03-SUMMARY.md` when done.
</output>
