---
phase: 192-ratification-baseline-lock
plan: 04
type: execute
wave: 2
depends_on:
  - "192-01"
  - "192-02"
  - "192-03"
files_modified:
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - "GATE-01/D-05 clause 1: all 6 admin screenshot projects pass in plain compare mode (no --update-snapshots) against committed baselines under CI tolerances"
    - "GATE-01/D-05 clause 2: git status --porcelain over both *-snapshots/ dirs shows no modified/untracked PNGs after the compare-mode run"
    - "GATE-01/D-05 clause 3: impersonation-banner (checkpoints canary) and board-notice (design canary) are unchanged at byte level and neither canary slug appears in either allowlist"
    - "GATE-01/D-05 clause 4: both snapshot-allowlist and snapshot-allowlist-design are comments-only; snapshot-canary-guard.sh (plain mode, --base origin/main, no --require-all) reports PASS (0 changed slug(s)) for each lane"
    - "GATE-01/D-05 clause 5: mix test test/sigra/admin/components_test.exs exits 0 (byte-golden component suite green)"
    - "GATE-02/D-10: gh run list for admin-acceptance CI job on the head commit returns 'success'"
    - "GATE-02: mix test --only golden (golden_diff_test.exs fast text layer) exits 0 OR the known_failure exclusion accounts for it"
    - "GATE-02: bash -n scripts/ci/admin-acceptance-smoke.sh exits 0 (syntax valid)"
    - "GATE-03/D-13: git fetch origin main && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0"
    - "GATE-03/D-01: no ledger cell is promoted to Tier 2; all cells remain Tier 1 after the ratification note is added"
    - "The ledger contains a terminal-ratification note documenting that all cells are locked at Tier 1 as of Phase 192"
    - "mix test --exclude known_failure exits 0 (blocking suite green with quarantine applied)"
  prohibitions:
    - statement: "No --update-snapshots flag was passed during the compare-mode baseline run"
      status: "enforced"
      verification: "git status --porcelain test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/ shows empty output"
    - statement: "snapshot-recapture-gate.sh is NOT invoked for the GATE-01 lock"
      status: "enforced"
      verification: "bash history / no invocation of scripts/ci/snapshot-recapture-gate.sh in this plan's task actions"
    - statement: "No ledger cell is promoted to Tier 2"
      status: "enforced"
      verification: "git diff HEAD guides/reference/admin-quality-ledger.md shows no line changing a cell from '| 1 |' to '| 2 |'"
    - statement: "The 3 quarantined failures remain red (not remediated)"
      status: "enforced"
      verification: "mix test --only known_failure reports 2 ExUnit failures; the Playwright MG-5/6 test is test.fail() (expected failure, not a fix)"
    - statement: "No net-new admin surface, component, token, or copy change appears in this plan"
      status: "enforced"
      verification: "git diff --name-only HEAD shows only guides/reference/admin-quality-ledger.md from this plan"
  artifacts:
    - path: "guides/reference/admin-quality-ledger.md"
      provides: "Terminal ratification note + all cells locked at Tier 1"
      contains: "Phase 192"
  key_links:
    - from: "scripts/ci/quality-ledger-monotonic.sh"
      to: "guides/reference/admin-quality-ledger.md"
      via: "git fetch origin main && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main"
      pattern: "origin/main"
    - from: "scripts/ci/snapshot-canary-guard.sh"
      to: "test/example/priv/playwright/snapshot-allowlist (and snapshot-allowlist-design)"
      via: "plain compare mode, no --require-all, --base origin/main"
      pattern: "PASS (0 changed"
---

<objective>
Run all gate proof commands and add the terminal-ratification note to the quality ledger.
This plan PROVES the invariants established by plans 01-03 and by the already-built harness
(phases 184-191). It does not build new harness code — it operates the existing machinery and
records the outcome.

Purpose: GATE-01 (D-05): five-clause compare-mode idempotency proof + canary/allowlist/byte-golden.
GATE-02 (D-10): CI-SHA parity citation for admin-acceptance + fast text-golden layer + smoke syntax.
GATE-03 (D-13): fetch-then-origin/main monotonic guard + ledger ratification note locked at Tier 1.

Output: Terminal-ratification note added to guides/reference/admin-quality-ledger.md documenting
all ~35 cells locked at Tier 1 as of Phase 192. All gate invariants verified via re-runnable commands.

CRITICAL SCOPE NOTE: This plan contains NO feature code, NO new tests, NO new scripts beyond what
is already built. The deliverable is the verification record plus the single ledger note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-ratification-baseline-lock/192-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-01 — compare-mode zero-drift proof (D-03/D-05 clauses 1-5)</name>
  <files>
    guides/reference/admin-quality-ledger.md
  </files>

  <read_first>
    - scripts/ci/snapshot-canary-guard.sh (full script — understand the --base, --canary, --require-all flags; plain mode = no --require-all; confirm the "PASS (0 changed slug(s))" output format)
    - test/example/priv/playwright/snapshot-allowlist (confirm current state is comments-only)
    - test/example/priv/playwright/snapshot-allowlist-design (confirm current state is comments-only)
    - test/example/priv/playwright/playwright.config.ts (confirm the 6 admin projects: admin-checkpoints-{chromium,mobile,dark} + admin-design-{chromium,mobile,dark})
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts (lines 138-147 — CI tolerance values used in compare mode)
    - guides/reference/admin-quality-ledger.md (full file — understand all ~35 cells; read the note structure at the top; D-01: all cells must remain Tier 1 after the ratification note is added)
    - .planning/phases/192-ratification-baseline-lock/192-CONTEXT.md (D-03 compare mode, D-05 five clauses, D-06 tolerance caveat, D-13 fetch discipline)
  </read_first>

  <action>
    **Step A — Byte-golden component suite (D-05 clause 5):**

    Run `mix test test/sigra/admin/components_test.exs` and verify it exits 0.
    If it fails, investigate and fix before proceeding (this is the one truly byte-exact
    idempotent layer and must be green).

    **Step B — Allowlist steady-state verification (D-05 clause 4):**

    Run:
      `grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l`
    and:
      `grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -v '^$' | wc -l`

    Both must return 0. If either returns > 0, the allowlist has leftover entries that must be
    removed (they are carry-over from a previous re-record cycle) — remove the non-comment lines
    and verify again. Do NOT add entries.

    Run `git fetch origin main` first, then run the canary guard in plain compare mode for each
    lane:

      For the checkpoints lane:
        `bash scripts/ci/snapshot-canary-guard.sh --base origin/main`
        (no --require-all, no --allow, no extra args — plain mode only)

      For the design lane:
        `SNAP_DIR=test/example/priv/playwright/tests/admin-design.spec.ts-snapshots \
         ALLOWLIST=$(pwd)/test/example/priv/playwright/snapshot-allowlist-design \
         bash scripts/ci/snapshot-canary-guard.sh --base origin/main --canary board-notice`

    Both must output `PASS (0 changed slug(s))`. If either fails, the baseline has uncommitted
    drift — investigate (do NOT run --update-snapshots; do NOT invoke snapshot-recapture-gate.sh).

    Verify canary slugs do NOT appear in either allowlist:
      `grep -c "impersonation-banner" test/example/priv/playwright/snapshot-allowlist`  → 0
      `grep -c "board-notice" test/example/priv/playwright/snapshot-allowlist-design`   → 0

    **Step C — Zero PNG drift committed (D-05 clause 2):**

    After running canary guard (compare mode — it reads but never writes PNGs), verify:
      `git status --porcelain test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/`
      `git status --porcelain test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/`

    Both must produce empty output (no modified/untracked PNGs). If any PNGs appear as modified,
    the environment is dirty — do NOT commit them; investigate the source (likely an unrelated
    local tool invocation).

    **Step D — Terminal ratification note in ledger (D-01/GATE-03):**

    In `guides/reference/admin-quality-ledger.md`, add a terminal-ratification note section
    immediately AFTER the Quality Ledger table (after the last `| flow-org-admin |` row) and
    BEFORE any `## Future` or trailing sections (or at end-of-file if there are none).

    The note section must be:
    ```
    ## Terminal Ratification — Phase 192

    All ~35 quality-ledger cells are locked at **Tier 1 (Ratified)** as of Phase 192
    (2026-06-18). This is the terminal gate of the v1.39 DS-COHERENCE milestone.

    **Forward-only guarantee:** The monotonic guard (`scripts/ci/quality-ledger-monotonic.sh
    --base origin/main`) protects every cell permanently — no future PR may decrease any tier.
    Tier 1 is the minimum floor from this point forward.

    **Tier 2 is NOT declared here.** Tier 2 ("Award-grade") is subjective (see Tier Vocabulary
    above) and requires objective proxies ratcheted separately in a future milestone. It is
    never a build-failing monotonic integer on an aesthetic verdict. See D-02 in the
    192-CONTEXT.md for the earned-separately path.

    **Proof method:** compare-mode zero-drift idempotency (not force-recapture) — re-rendering
    all 6 Playwright projects produces zero PNG delta; both allowlists verified at steady-state
    empty; both canaries byte-stable; byte-golden component suite green; monotonic guard green
    vs `origin/main`; generated-host parity proven via CI SHA for the admin-acceptance job.
    ```

    Use Edit (scoped) to insert the section — do NOT rewrite the whole ledger file. Preserve
    all existing rows and their Tier 1 values exactly. Do NOT change any cell's tier value.
  </action>

  <verify>
    <automated>mix test test/sigra/admin/components_test.exs && grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l && grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -v '^$' | wc -l && grep -c "Phase 192" guides/reference/admin-quality-ledger.md</automated>
  </verify>

  <acceptance_criteria>
    - `mix test test/sigra/admin/components_test.exs` exits 0
    - `grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l` returns 0
    - `grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -v '^$' | wc -l` returns 0
    - `bash scripts/ci/snapshot-canary-guard.sh --base origin/main` outputs `PASS (0 changed slug(s))`
    - The design lane canary guard (SNAP_DIR + ALLOWLIST + --canary board-notice) outputs `PASS (0 changed slug(s))`
    - `grep -c "impersonation-banner" test/example/priv/playwright/snapshot-allowlist` returns 0
    - `grep -c "board-notice" test/example/priv/playwright/snapshot-allowlist-design` returns 0
    - `git status --porcelain test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/` produces empty output
    - `git status --porcelain test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/` produces empty output
    - `grep -c "Phase 192" guides/reference/admin-quality-ledger.md` returns at least 1
    - `grep -c "Tier 2" guides/reference/admin-quality-ledger.md` — the new note mentions Tier 2 is NOT declared here (deferred); the cell rows all remain `| 1 |`
    - `git diff HEAD guides/reference/admin-quality-ledger.md | grep "^+" | grep "| 2 |"` returns empty (no cell promoted to Tier 2)
  </acceptance_criteria>

  <done>All five GATE-01 clauses verified: byte-goldens green, allowlists empty, canaries byte-stable and not allowlisted, zero PNG drift in working tree. Terminal ratification note added to ledger with Tier 1 lock and Tier 2 deferral documented. No PNG commits. snapshot-recapture-gate.sh NOT invoked.</done>
</task>

<task type="auto">
  <name>Task 2: GATE-02 (D-10) and GATE-03 (D-13) — CI parity proof + monotonic guard</name>
  <files></files>

  <read_first>
    - scripts/ci/quality-ledger-monotonic.sh (full script — understand the --base flag and the git show ${BASE}:${LEDGER} mechanism; confirm why --base HEAD is wrong for a merge decision)
    - .github/workflows/ci.yml (the admin-acceptance job name and the git fetch origin / --base origin/$base_ref discipline already present in CI)
    - scripts/ci/admin-acceptance-smoke.sh (first 30 lines — understand the script's purpose, PORT=4017, phx.new scaffolding; syntax check only per D-10)
    - .planning/phases/192-ratification-baseline-lock/192-CONTEXT.md (D-10 verbatim parity proof command, D-13 verbatim monotonic command)
  </read_first>

  <action>
    This task has NO file modifications. It runs verification commands and records the output
    as evidence in the plan summary. No new code is written.

    **Step A — GATE-03: Monotonic guard vs origin/main (D-13):**

    Run the exact verbatim command from D-13:
      `git fetch origin main && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main`

    The fetch is MANDATORY — without it, `git show origin/main:${LEDGER}` reads a stale/missing
    ref. The script must exit 0. If it exits non-zero, a tier cell has decreased vs origin/main —
    investigate which cell and fix before proceeding (this is a blocking gate failure).

    Record the command's output in the plan summary.

    **Step B — GATE-02 parity: CI-SHA citation (D-10):**

    Determine the current branch name:
      `git rev-parse --abbrev-ref HEAD`

    Determine the current HEAD commit SHA:
      `git rev-parse HEAD`

    Run the verbatim parity proof from D-10 (substitute <branch> with the actual branch name):
      `gh run list --workflow ci.yml --branch <branch> --commit $(git rev-parse HEAD) --json name,conclusion --jq '.[]|select(.name=="admin-acceptance").conclusion'`

    The output must be `"success"`. If the output is empty (CI run not yet complete for this
    SHA), wait for the CI run to finish or check `gh run list --workflow ci.yml --branch <branch>`
    for the most recent run's status. If the CI lane is red or skipped, do NOT proceed — report
    the failure in the summary for the developer to investigate.

    Record the SHA and conclusion in the plan summary.

    **Step C — GATE-02 fast local text layer (D-10):**

    Run the fast text-golden layer:
      `mix test --only golden`

    Note: if `golden_diff_test.exs` is tagged known_failure, `--only golden` may not isolate
    it from `--exclude known_failure`. Run:
      `mix test --exclude known_failure test/sigra/install/golden_diff_test.exs 2>&1 | tail -5`
    to confirm the test is excluded (not run, not failing the suite). Then run:
      `mix test --only golden --exclude known_failure`
    to verify the remaining golden tests (if any) are green.

    Run the admin-acceptance-smoke.sh syntax check:
      `bash -n scripts/ci/admin-acceptance-smoke.sh`
    Must exit 0 (syntax valid). This is NOT a full scaffold-boot (that runs only in CI per D-10).

    **Step D — Blocking suite final check:**

    Run the full blocking suite with quarantine applied (confirm it is green after all Wave 1 edits):
      `mix test --exclude known_failure`

    Must exit 0. Record any failures in the summary.

    This is the final gate verification before the phase is complete.
  </action>

  <verify>
    <automated>git fetch origin main && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && bash -n scripts/ci/admin-acceptance-smoke.sh && mix test --exclude known_failure</automated>
  </verify>

  <acceptance_criteria>
    - `git fetch origin main && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` exits 0
    - `gh run list --workflow ci.yml --branch <branch> --commit $(git rev-parse HEAD) --json name,conclusion --jq '.[]|select(.name=="admin-acceptance").conclusion'` returns `"success"` (proof-by-CI for GATE-02 RUN_PARITY)
    - `bash -n scripts/ci/admin-acceptance-smoke.sh` exits 0
    - `mix test --exclude known_failure` exits 0
    - `mix test --only known_failure || echo "KNOWN-FAILURE LANE: expected failures"` — reports 2 ExUnit failures (golden_diff + vault_promotion), does not block
    - The plan summary records: HEAD commit SHA, CI run ID or URL, and the conclusion string from the gh command
  </acceptance_criteria>

  <done>GATE-03 monotonic guard green vs origin/main (fetch-then-base discipline confirmed). GATE-02 parity proven via CI SHA (admin-acceptance job = success) + smoke syntax valid + blocking suite green. All gate invariants satisfied. Phase 192 is ratified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → GitHub API | gh run list reads CI conclusion from GitHub; requires gh auth configured |
| local shell → git remote | git fetch origin main reads origin ref; requires network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-04-01 | Spoofing | gh run list CI conclusion | accept | We are the repo owner; the gh tool reads from the authenticated GitHub API; no external party can inject a false "success" |
| T-192-04-02 | Tampering | git fetch from origin | accept | Fetching from origin before comparing ensures we compare against the canonical remote, not a stale local cache — this is the security-correct direction |
| T-192-04-SC | Tampering | No npm/hex installs | accept | No new packages in this plan |
</threat_model>

<verification>
Terminal gate verification sequence (all must pass):

**GATE-01:**
1. `mix test test/sigra/admin/components_test.exs` → exits 0
2. `git status --porcelain test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/` → empty output
3. `grep -v '^#' test/example/priv/playwright/snapshot-allowlist | grep -v '^$' | wc -l` → `0`
4. `grep -v '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -v '^$' | wc -l` → `0`
5. `bash scripts/ci/snapshot-canary-guard.sh --base origin/main` → `PASS (0 changed slug(s))`
6. `grep -c "impersonation-banner" test/example/priv/playwright/snapshot-allowlist` → `0`
7. `grep -c "board-notice" test/example/priv/playwright/snapshot-allowlist-design` → `0`

**GATE-02:**
8. `gh run list --workflow ci.yml --branch $(git rev-parse --abbrev-ref HEAD) --commit $(git rev-parse HEAD) --json name,conclusion --jq '.[]|select(.name=="admin-acceptance").conclusion'` → `"success"`
9. `bash -n scripts/ci/admin-acceptance-smoke.sh` → exits 0
10. `mix test --exclude known_failure` → exits 0

**GATE-03:**
11. `git fetch origin main && bash scripts/ci/quality-ledger-monotonic.sh --base origin/main` → exits 0
12. `grep -c "Phase 192" guides/reference/admin-quality-ledger.md` → ≥ 1
13. `git diff origin/main -- guides/reference/admin-quality-ledger.md | grep "^+" | grep "| 2 |"` → empty (no Tier-2 promotions)

**Prohibitions check:**
14. `git status --porcelain test/example/priv/playwright/tests/admin-checkpoints.spec.ts-snapshots/ test/example/priv/playwright/tests/admin-design.spec.ts-snapshots/` → empty
15. `mix test --only known_failure || true` → reports 2 failures (golden_diff + vault_promotion remain red)
</verification>

<success_criteria>
All three gates pass:
- GATE-01: five-clause idempotency invariant verified (compare-mode, zero PNG drift, canaries byte-stable
  and not allowlisted, allowlists at steady-state empty, byte-goldens green)
- GATE-02: admin-acceptance CI job returns "success" for the head commit; smoke syntax valid;
  blocking suite green
- GATE-03: monotonic guard green vs origin/main (fetch-first discipline); ledger terminal
  ratification note added, all cells locked at Tier 1, no Tier-2 promotions

Phase 192 (Ratification & Baseline Lock) is fully ratified.
</success_criteria>

## Artifacts This Phase Produces

Modified file:
- `guides/reference/admin-quality-ledger.md` — `## Terminal Ratification — Phase 192` section added after the last ledger row

No new files. No new modules.

New human-readable symbol: `## Terminal Ratification — Phase 192` section in the ledger.

<output>
Create `.planning/phases/192-ratification-baseline-lock/192-04-SUMMARY.md` when done.
</output>
