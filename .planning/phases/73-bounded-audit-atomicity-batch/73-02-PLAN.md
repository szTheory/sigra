---
phase: 73
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/sigra/mfa_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-11
must_haves:
  truths:
    - "Each **new** fault-injection test uses **`try/after`** with **`ALTER TABLE ... DROP CONSTRAINT IF EXISTS`** cleanup (match existing **`mfa_disable_atomicity_guard`** style)."
    - "**`AUD-04-023`–`025`**, **`031`–`032`** gain rollback receipts where absent; **028–029** path already has **`mfa_disable_atomicity_guard`** — do **not** remove; optional duplicate only if it adds a distinct **Multi** assertion (default: **skip** duplicate disable test)."
    - "**`AUD-04-030`** regen success path gains **≥ 1** **`CHECK`**-forced rollback proving replace + audit co-fate."
  artifacts:
    - path: "test/sigra/mfa_audit_atomicity_test.exs"
      provides: "AUD-11 audit-aware atomicity receipts"
nyquist_compliant: false
---

<objective>
Add deterministic **Postgres `CHECK`** fault-injection tests in **`test/sigra/mfa_audit_atomicity_test.exs`** for **`Sigra.MFA.verify/4`**, **`regenerate_backup_codes/4`**, and (if not redundant) **`verify_backup/4`** **Multi** compositions covering **AUD-04-023..025**, **030..032**, per **073-CONTEXT D-07–D-09** and **073-RESEARCH**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-73-03 | Tampering | Partial **`user_mfa_credentials`** update without durable audit on claimed **T1** paths | high | **`CHECK`** rejects audit insert — assert **no** row-level security state change (counters / steps / backup rows) |
| T-73-04 | Repudiation | Tests use mocks instead of real **`Ecto`** rollback | medium | Only **`repo.transaction`** + real constraints — **no** **`Mox`** for audit |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: verify/4 TOTP success — audit CHECK forces full rollback</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/mfa_audit_atomicity_test.exs (existing **`verify TOTP success`** test ~236–261; enrollment guard test ~200–233)
    - lib/sigra/mfa.ex (`verify/4` success branch ~286–320)
  </read_first>
  <action>
    Add **`test "verify TOTP success rolls back credential updates when mfa.verify.success audit is blocked"`** (or equivalent name) that:

    1. Inserts a **TOTP** credential with **`last_verified_step: 0`**, **`failed_attempts: 0`**, known **`encrypted_secret`**.

    2. In **`try/after`**, runs **`ALTER TABLE audit_events ADD CONSTRAINT mfa_verify_success_audit_guard CHECK (action <> 'mfa.verify.success')`**.

    3. Calls **`MFA.verify(cfg(repo), user, NimbleTOTP.verification_code(raw, time: System.system_time(:second)), mfa_credential_schema: MfaCredential)`** and expects **`RuntimeError`** whose message matches **`~r/Sigra\\.MFA\\.verify\\/4 unexpected transaction failure/`** (see **`lib/sigra/mfa.ex`** `verify/4` success branch **`case repo.transaction(multi)`** — unlike **`confirm_enrollment/5`**, there is **no** dedicated **`raise Ecto.ConstraintError`** wrapper for audit-only failures).

    4. Asserts **`count_where(repo, \"audit_events\", \"action = 'mfa.verify.success'\") == 0`** and **`last_verified_step`** (or equivalent column read via raw SQL) on the credential row is still **0** / unchanged from pre-verify.

    5. **`after`** clause drops **`mfa_verify_success_audit_guard`**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'mfa_verify_success_audit_guard' test/sigra/mfa_audit_atomicity_test.exs` exits **0** with **≥ 2** lines (**ADD** + **`DROP IF EXISTS`**).
    - `grep -nF 'verify TOTP success rolls back' test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs --only line:NNN` **not** required; full file command below after all tasks land.
  </verify>
</task>

<task type="execute">
  <name>Task 2: verify/4 wrong TOTP — mfa.verify.failure audit CHECK rolls back lockout increment</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/mfa_audit_atomicity_test.exs (**`verify_backup wrong code rolls back`** ~360–427)
    - lib/sigra/mfa.ex (`verify/4` failure **`failure_multi`** ~322–382)
  </read_first>
  <action>
    Add a test that uses **`ALTER TABLE audit_events ADD CONSTRAINT mfa_totp_verify_failure_guard CHECK (action <> 'mfa.verify.failure')`**, supplies a **deliberately wrong** six-digit TOTP string, expects **`RuntimeError`** with message matching **`~r/Sigra\\.MFA\\.verify\\/4 unexpected transaction failure/`** from **`MFA.verify/4`**, asserts **`failed_attempts`** on the credential (via **`SELECT failed_attempts FROM user_mfa_credentials WHERE id = $1`**) remains **0**, and **`count_where(..., \"action = 'mfa.verify.failure'\") == 0`**. Use default **`cfg(repo)`** lockout threshold (**5**). Clean up constraint in **`after`**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'mfa_totp_verify_failure_guard' test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
    - `grep -nF 'wrong TOTP' test/sigra/mfa_audit_atomicity_test.exs` exits **0** OR the test name contains **`wrong`** and **`totp`** (case-insensitive): `grep -iE 'wrong.*totp|totp.*wrong' test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    Same scoped **`mix test`** as plan footer after Task 4.
  </verify>
</task>

<task type="execute">
  <name>Task 3: verify/4 lockout audit — CHECK on mfa.lockout rolls back entire failure Multi (threshold 1)</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - lib/sigra/mfa/lockout.ex (`increment/4` threshold semantics)
    - lib/sigra/mfa.ex (`verify/4` **`Multi.merge`** lockout branch ~342–360)
  </read_first>
  <action>
    Add a test using **`Config.new!(Keyword.merge([...], mfa: [totp_drift_steps: 1, backup_code_count: 2, lockout_threshold: 1, lockout_duration: 900]))`** so **one** wrong TOTP triggers **`mfa.lockout`**. Add **`CHECK (action <> 'mfa.lockout')`** on **`audit_events`**. Expect **`RuntimeError`** with **`~r/Sigra\\.MFA\\.verify\\/4 unexpected transaction failure/`**; assert **`failed_attempts == 0`** and **no** **`mfa.verify.failure`** or **`mfa.lockout`** rows. Drop constraint in **`after`**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'lockout_threshold: 1' test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
    - `grep -nF "action <> 'mfa.lockout'" test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    Scoped **`mix test`** after Task 4.
  </verify>
</task>

<task type="execute">
  <name>Task 4: regenerate_backup_codes/4 — success and wrong-code rollback receipts</name>
  <files>test/sigra/mfa_audit_atomicity_test.exs</files>
  <read_first>
    - lib/sigra/mfa.ex (`regenerate_backup_codes/4` ~664–799)
    - lib/sigra/mfa/backup_codes.ex (`append_replace_steps/5` if needed for understanding counts)
  </read_first>
  <action>
    Add **two** tests:

    1. **Success rollback:** Seed credential + **≥ 1** backup row. Call **`MFA.regenerate_backup_codes(config, user, {:totp, good_code}, mfa_credential_schema: MfaCredential, backup_code_schema: BackupCode)`** with **`ALTER TABLE audit_events ADD CONSTRAINT mfa_regen_success_guard CHECK (action <> 'mfa.backup_codes_regenerate')`**. Expect **`RuntimeError`** with **`~r/Sigra\\.MFA\\.regenerate_backup_codes\\/4 unexpected transaction failure/`**. Assert backup code **count** unchanged vs pre-call baseline and **no** regenerate audit row.

    2. **Wrong-code rollback:** Same fixture shape as verify failure test; **`ALTER TABLE audit_events ADD CONSTRAINT mfa_regen_verify_failure_guard CHECK (action <> 'mfa.verify.failure')`**; pass **`{:totp, \"000000\"}`** (or any provably invalid code vs secret); expect **`RuntimeError`** with the same **`regenerate_backup_codes`** failure regex; assert **`failed_attempts == 0`** and no matching audit rows. **`try/after`** cleanup for each constraint **independently**.
  </action>
  <acceptance_criteria>
    - `grep -nF 'mfa_regen_success_guard' test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
    - `grep -nF 'mfa_regen_verify_failure_guard' test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
    - `grep -nF 'regenerate_backup_codes' test/sigra/mfa_audit_atomicity_test.exs` exits **0** with **≥ 2** occurrences (calls in new tests).
  </acceptance_criteria>
  <verify>
    `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs` exits **0**.
- `MIX_ENV=test mix compile --warnings-as-errors` exits **0**.
</verification>

<success_criteria>
- **AUD-11** merge gate: audit-aware regression tests for **023–025** and **030–032** **Multi** paths ship with **073-01** doc reconciliation in one PR (**073-CONTEXT D-10**).
</success_criteria>

## PLANNING COMPLETE
