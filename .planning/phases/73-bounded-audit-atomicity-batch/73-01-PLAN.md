---
phase: 73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-audit-logging/09-VERIFICATION.md
  - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md
autonomous: true
requirements:
  - AUD-11
must_haves:
  truths:
    - "**AUD-04-023..032** C-1 + inventory **mechanism** and **verdict** cells match **`lib/sigra/mfa.ex`** (**Multi** + **`log_multi_safe`** for durable rows in this band; **022** / **033** / **034** unchanged **T2** with **EX-44-** pointers)."
    - "**`44-AUD-04-INVENTORY.md`** top **`rg`** block reflects **current** line numbers and **`log_safe` / `log_multi_safe`** usage (no stale **phase 44** closure language for rows already **T1** in **`lib/`**)."
  artifacts:
    - path: ".planning/phases/09-audit-logging/09-VERIFICATION.md"
      provides: "C-1 matrix rows AUD-04-023..032"
    - path: ".planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md"
      provides: "Phase 44 inventory slice + grep log"
nyquist_compliant: false
---

<objective>
Reconcile **C-1 — Phase 44 inventory** in **`09-VERIFICATION.md`** and matching rows in **`44-AUD-04-INVENTORY.md`** with authoritative **`lib/sigra/mfa.ex`** for **AUD-04-023..032**; fix **033–034** verdict wording to **EX-44-03** / **EX-44-04** (no false “target Multi” closure implication). **073-CONTEXT D-10..D-12** same-PR policy is satisfied when merged with **073-02-PLAN** tests in one gate.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-73-01 | Repudiation | Stale **C-1** rows claim **T2** / pending closure while **`lib/`** is **T1** | high | Replace mechanism + verdict text per **073-RESEARCH** row table; evidence pointers stay **`test/sigra/mfa_audit_atomicity_test.exs`** + **`lib/sigra/mfa.ex`** |
| T-73-02 | Information disclosure | Misleading **EX-44-03/04** rows imply imminent **Multi** migration | low | Set **033–034** to explicit **EX-** appendix language; do not mark **T1** |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Update 09-VERIFICATION C-1 rows AUD-04-023..032 (and 033–034 wording)</name>
  <files>.planning/phases/09-audit-logging/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-audit-logging/09-VERIFICATION.md (subsection **C-1 — Phase 44 inventory**, rows **AUD-04-020** onward)
    - lib/sigra/mfa.ex (`verify/4` ~265–383, `verify_backup/4` ~406–533, `regenerate_backup_codes/4` ~664–799, `cleanup_mfa/6` ~995–1054)
    - .planning/phases/073-bounded-audit-atomicity-batch/073-CONTEXT.md
    - .planning/phases/073-bounded-audit-atomicity-batch/073-RESEARCH.md
  </read_first>
  <action>
    In **`.planning/phases/09-audit-logging/09-VERIFICATION.md`**, replace the **markdown table lines** for **AUD-04-023** through **AUD-04-032** so each row reads:

    - **AUD-04-023** — mechanism: **`Multi` + `log_multi_safe`** (`verify/4` TOTP success — `Multi.update_all` + `mfa.verify.success` in one `repo.transaction/1`); tier **3**; verdict **T1 (Multi-bound; phase 73)**; evidence **`test/sigra/mfa_audit_atomicity_test.exs`**; optional sub-clause **`lib/sigra/mfa.ex` ~291–310**.

    - **AUD-04-024** — mechanism **`Multi` + `log_multi_safe`** (`verify/4` wrong TOTP — `Lockout.increment` + `mfa.verify.failure`); tier **5**; verdict **T1 (Multi-bound; phase 73)**; evidence **`test/sigra/mfa_audit_atomicity_test.exs`**; **`lib/sigra/mfa.ex` ~325–341**.

    - **AUD-04-025** — mechanism **`Multi` + `log_multi_safe`** (`mfa.lockout` appended via `Multi.merge` when threshold met); tier **4**; verdict **T1 (Multi-bound; phase 73)**; evidence **`test/sigra/mfa_audit_atomicity_test.exs`**; **`lib/sigra/mfa.ex` ~342–360**.

    - **AUD-04-026** / **027** / **067** — keep **Multi** facts; change verdict clause **“(phase 44-03)”** / phase pointers where needed to include **“closed in `lib/`; phase 73 verification receipts”** only if the sentence would otherwise imply **pending** work — minimal edit.

    - **AUD-04-028** — mechanism **`Multi` + `log_multi_safe`** (`cleanup_mfa/6` — `mfa.disable` on same `Multi` as deletes + trust revoke); tier **6**; verdict **T1 (Multi-bound; phase 73)**; evidence **`test/sigra/mfa_audit_atomicity_test.exs`**; **`lib/sigra/mfa.ex` ~1007–1033**.

    - **AUD-04-029** — same mechanism/verdict pattern as **028** for **`disable!/4`** admin path; tier **6**; **T1 (Multi-bound; phase 73)**.

    - **AUD-04-030** — mechanism **Multi (`log_multi_safe`)**; verdict **T1 (Multi-bound; phase 73)**` (refresh phase pointer from generic “Multi-bound”).

    - **AUD-04-031** — mechanism **`Multi` + `log_multi_safe`** (`regenerate_backup_codes/4` wrong TOTP — `mfa.verify.failure`); tier **5**; verdict **T1 (Multi-bound; phase 73)**; **`lib/sigra/mfa.ex` ~741–757**.

    - **AUD-04-032** — mechanism **`Multi` + `log_multi_safe`** (regenerate path `mfa.lockout`); tier **4**; verdict **T1 (Multi-bound; phase 73)**; **`lib/sigra/mfa.ex` ~758–776**.

    For **AUD-04-033** and **AUD-04-034** on the **same table**: replace verdict **“T2 / target Multi (phase 44 closure)”** with **`T2 / EX-44-03`** and **`T2 / EX-44-04`** respectively; keep mechanism **`log_safe`**. Do **not** change **AUD-04-035+** account rows in this task.

    Preserve **ASCII pipe table** formatting; do not reorder non-target rows except where a line must move for readability (avoid unless necessary).
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-023' .planning/phases/09-audit-logging/09-VERIFICATION.md` shows **one** line containing **`Multi` + `log_multi_safe`** and **not** containing **`log_safe` (after`.
    - `grep -nE 'AUD-04-02[4-9]|AUD-04-03[0-2]' .planning/phases/09-audit-logging/09-VERIFICATION.md | grep 'log_safe (after'` exits **1** (no match) for rows **024–029** / **031–032**.
    - `grep -nF 'AUD-04-033' .planning/phases/09-audit-logging/09-VERIFICATION.md` matches a line containing **`EX-44-03`**.
    - `grep -nF 'AUD-04-034' .planning/phases/09-audit-logging/09-VERIFICATION.md` matches a line containing **`EX-44-04`**.
  </acceptance_criteria>
  <verify>
    `grep -c 'phase 73' .planning/phases/09-audit-logging/09-VERIFICATION.md` returns **≥ 6** (spot-check that phase **73** pointers landed on the edited rows).
  </verify>
</task>

<task type="execute">
  <name>Task 2: Update 44-AUD-04-INVENTORY rows 023–032 + refresh grep log</name>
  <files>.planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md (table **AUD-04-020** onward + **## Grep log** fenced block)
    - lib/sigra/mfa.ex (D-26 comment header ~35–43 for cross-check)
    - .planning/phases/073-bounded-audit-atomicity-batch/073-RESEARCH.md
  </read_first>
  <action>
    1. Update the **inventory table** rows **AUD-04-023**–**AUD-04-032** so **`mechanism today`**, **`notes`**, and **`Phase`** columns match **`lib/sigra/mfa.ex`** (**`Multi` + `log_multi_safe`**) and point to **phase 73** for this honesty closure (keep **EX-44-02** wording on **022** unchanged).

    2. Replace the fenced **`## Grep log`** code block body with **fresh** shell output from the project root:

       `rg -n "Sigra\\.Audit\\.(log_safe|log_multi_safe|__log_internal__)" lib/sigra/mfa.ex lib/sigra/account.ex lib/sigra/api_token.ex`

       (Run locally; paste **complete** text output inside a **```text** fence.)

    3. If **`## Changelog pointer`** or footnotes still imply **023–032** are pending **`44-03`** **Multi** work, add **one** clarifying sentence: **`lib/`** already **Multi**-first; **phase 73** closed planning drift + tests.
  </action>
  <acceptance_criteria>
    - `grep -nF 'AUD-04-023' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` shows **`Multi` + `log_multi_safe`** in the **mechanism** column (pipe-separated table).
    - `grep -nF 'lib/sigra/mfa.ex' .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md | head -1` exits **0** (grep log references **`mfa.ex`**).
    - The **grep log** fenced block contains the substring **`log_multi_safe`** at least **once**.
  </acceptance_criteria>
  <verify>
    `wc -l .planning/phases/44-mfa-account-api-atomic-batches/44-AUD-04-INVENTORY.md` reports line count **> 50** (sanity — file not truncated).
  </verify>
</task>

</tasks>

<verification>
- No **`mix test`** required for this plan alone; full gate with **073-02** via `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/mfa_audit_atomicity_test.exs`.
</verification>

<success_criteria>
- **09-VERIFICATION** + **44-AUD-04-INVENTORY** are honest for **AUD-04-023..032** vs **`lib/sigra/mfa.ex`**; **033–034** explicitly **EX-** classified.
</success_criteria>

## PLANNING COMPLETE
