---
phase: 221-unblock-the-gate-ship-honest-generated-host-debt
plan: 04
type: execute
wave: 3
depends_on: ["221-03"]
files_modified: []
autonomous: false
requirements: [PUB-02, PUB-03]
user_setup:
  - service: hex.pm
    why: "Publishing v1.2.0 and v1.3.0 requires an operator to dispatch hex-publish.yml; the workflow uses the configured HEX_API_KEY but the dispatch itself is operator-gated by design (D-16)"
    dashboard_config:
      - task: "Dispatch hex-publish.yml (dry-run then real) for v1.2.0, then real for v1.3.0"
        location: "gh workflow run hex-publish.yml (or GitHub Actions UI → hex-publish workflow)"
must_haves:
  truths:
    - "Sigra v1.2.0 is published to Hex.pm (dry-run verified clean first) — PUB-02"
    - "Sigra v1.3.0 is published to Hex.pm after v1.2.0 — PUB-03"
    - "Live Hex API shows 1.2.0 and 1.3.0 in the release set"
  artifacts:
    - "Hex registry: sigra 1.2.0 and 1.3.0 published releases (external registry state — no repo file)"
  key_links:
    - "hex-publish.yml workflow_dispatch (tag + release_version + dry_run) → Hex registry → curl https://hex.pm/api/packages/sigra"
---

<objective>
Operator-gated release sequence: publish the already-tagged, `type=`-clean releases v1.2.0 then v1.3.0 to Hex.pm via the ungated `hex-publish.yml` `workflow_dispatch` (dry-run first for v1.2.0, then real; real for v1.3.0). These publishes are prerequisites of PUB-01 — the pin (Plan 03) can only green the gate once v1.3.0 is live (D-14).

Purpose: Make Sigra current on Hex AND supply the clean floor the smoke pin resolves to. The two Hex writes CANNOT be automated (D-16): the operator dispatches; the agent runs/verifies dry-runs and confirms via the Hex API. Re-query live Hex immediately before each write — registry state can change (RESEARCH "Valid until 2026-07-17").
Output: v1.2.0 and v1.3.0 live on Hex.pm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-CONTEXT.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-RESEARCH.md

@.github/workflows/hex-publish.yml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: PUB-02 — publish v1.2.0 (dry-run then real)</name>
  <files>none (external Hex registry state)</files>
  <action>
    Operator-gated (D-16). Agent preflight: re-query live Hex `curl -s https://hex.pm/api/packages/sigra` and record the current release set + `latest_stable_version` (expected: 1.20.0 latest, no 1.2.0/1.3.0 yet, retirements {}); confirm the v1.2.0 tag exists and points at a green commit. Operator runs the DRY-RUN dispatch `gh workflow run hex-publish.yml -f tag=v1.2.0 -f release_version=1.2.0 -f dry_run=true`; agent inspects the run log (`gh run watch`) for a clean preflight (provenance = tag, WAE compile, `mix test`, docs WAE, package rejects a packaged `.planning/`, idempotency skip-if-exists). If the dry-run fails loudly, STOP and report — do NOT proceed to real publish. Then operator runs the REAL dispatch `gh workflow run hex-publish.yml -f tag=v1.2.0 -f release_version=1.2.0 -f dry_run=false`; agent confirms via the Hex API.
  </action>
  <what-built>
    Agent verifies the operator's dry-run then real publish of v1.2.0 via the ungated hex-publish.yml workflow_dispatch and the live Hex API.
  </what-built>
  <how-to-verify>
    1. Agent: `curl -s https://hex.pm/api/packages/sigra | jq '.releases[].version, .latest_stable_version'` — snapshot before.
    2. Operator: dry-run dispatch, then real dispatch (commands in <action>).
    3. Agent: `curl -s https://hex.pm/api/packages/sigra | jq -r '.releases[].version' | grep -x '1.2.0'` returns 1.2.0.
  </how-to-verify>
  <verify>
    <automated>curl -s https://hex.pm/api/packages/sigra | jq -r '.releases[].version' | grep -x '1.2.0'</automated>
  </verify>
  <resume-signal>Type "1.2.0 published" once the Hex API shows version 1.2.0, or describe the dry-run/publish failure.</resume-signal>
  <done>Hex API lists sigra 1.2.0; the v1.2.0 dry-run concluded clean before the real publish.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: PUB-03 — publish v1.3.0 (real)</name>
  <files>none (external Hex registry state)</files>
  <action>
    Operator-gated (D-16). Agent preflight: re-query live Hex to confirm 1.2.0 is now present and 1.3.0 is not yet; confirm the v1.3.0 tag exists and points at a green commit. The v1.3.0 publish is the pin target (Plan 03) — it MUST be live for the smoke pin to green on `main`. Operator runs `gh workflow run hex-publish.yml -f tag=v1.3.0 -f release_version=1.3.0 -f dry_run=false` (the workflow re-runs its own preflight WAE compile + tests + provenance at the v1.3.0 ref; operator may dry-run first if preferred); agent watches the run (`gh run watch`) and confirms via the Hex API.
  </action>
  <what-built>
    Agent verifies the operator's publish of v1.3.0 (the smoke pin target) via hex-publish.yml and the live Hex API.
  </what-built>
  <how-to-verify>
    1. Agent: `curl -s https://hex.pm/api/packages/sigra | jq -r '.releases[].version'` — confirm 1.2.0 present, 1.3.0 absent.
    2. Operator: real dispatch (command in <action>).
    3. Agent: `curl -s https://hex.pm/api/packages/sigra | jq -r '.releases[].version' | grep -x '1.3.0'` returns 1.3.0.
  </how-to-verify>
  <verify>
    <automated>curl -s https://hex.pm/api/packages/sigra | jq -r '.releases[].version' | grep -x '1.3.0'</automated>
  </verify>
  <resume-signal>Type "1.3.0 published" once the Hex API shows version 1.3.0, or describe the failure.</resume-signal>
  <done>Hex API lists sigra 1.3.0; the smoke pin target is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Hex registry (write) | Publishing mutates the public package registry; must publish only provenance-verified, tagged releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-221-07 | Tampering | publishing an untagged / wrong-provenance artifact | high | mitigate | `hex-publish.yml` verifies provenance (tag == input ref), WAE compile, `mix test`, docs WAE, rejects a packaged `.planning/`; dry-run v1.2.0 first de-risks |
| T-221-SC | Tampering | supply-chain: dispatch of an unintended version | high | mitigate | Operator-gated dispatch (D-16); agent re-queries live Hex before/after each write; explicit tag + release_version inputs; idempotency skip-if-exists |

Package legitimacy: N/A — no package installs; this plan publishes the Sigra package itself (provenance-verified by the workflow).
</threat_model>

<verification>
- Live Hex API (`curl -s https://hex.pm/api/packages/sigra`) shows both 1.2.0 and 1.3.0 after the operator dispatches.
- The v1.2.0 dry-run concluded clean before the real publish.
- Gate-green and retirement are Plan 05.
</verification>

<success_criteria>
v1.2.0 and v1.3.0 are published to Hex.pm; v1.3.0 (the pin target) is live so Plan 05's gate-green can proceed.
</success_criteria>

<output>
Create `.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-04-SUMMARY.md` when done.
</output>
