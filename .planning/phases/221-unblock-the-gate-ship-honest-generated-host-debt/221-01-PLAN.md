---
phase: 221-unblock-the-gate-ship-honest-generated-host-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/core/mfa_settings_live.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
autonomous: true
requirements: [SHIP-01, SHIP-02]
must_haves:
  truths:
    - "A freshly generated host's save_passkey_name passes scope: socket.assigns.current_scope into Auth.rename_passkey and handles the {:error, :impersonation_forbidden} clause (SHIP-01 / D-06)"
    - "The generated delete-passkey confirmation body no longer repeats the leading 'Delete this passkey?' (heading keeps it) (SHIP-02a / D-07)"
    - "The install golden fixture is re-blessed and MIX_ENV=test mix sigra.fixture.rebless_golden --check reports no drift (D-11)"
  artifacts:
    - "priv/templates/sigra.install/core/mfa_settings_live.ex (scope arg + impersonation clause + deduped delete copy)"
    - "test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex (re-blessed snapshot)"
  key_links:
    - "template mfa_settings_live.ex ↔ golden fixture parity enforced by mix sigra.fixture.rebless_golden + golden_diff_test.exs"
    - "Auth.rename_passkey/4 scope: keyword → library-level impersonation guard (forbid_sensitive_operation)"
---

<objective>
Restore the generated-host passkey-rename defense-in-depth and dedupe the delete-passkey copy in the installer template, then re-bless the install golden fixture so generated hosts inherit the fix. This is a mechanical mirror from the example twin (`test/example/.../mfa_settings_live.ex`), the established source of truth.

Purpose: Every adopter's generated `mfa_settings_live.ex` currently omits `scope:` on `save_passkey_name`, so the library impersonation guard never fires for passkey renames — an Elevation-of-Privilege defense-in-depth gap (SHIP-01, ASVS V4). SHIP-02a fixes a redundant copy line.
Output: Edited installer template + re-blessed golden fixture, both `type=`-clean and parity-verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-CONTEXT.md
@.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-RESEARCH.md

# Source of truth (example twin) + the two files this plan edits:
@test/example/lib/example_web/live/mfa_settings_live.ex
@priv/templates/sigra.install/core/mfa_settings_live.ex
</context>

<prerequisites>
Local re-bless prerequisites (CLAUDE.md § Local development prerequisites):
- Live Postgres: `scripts/db/up.sh` then `source tmp/db.env` (or direnv). Falls back to localhost:5432.
- phx_new 1.8.8 archive pinned: `mix archive.install --force hex phx_new 1.8.8` (a different archive version yields spurious byte-diffs — Pitfall 4).
</prerequisites>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SHIP-01 — add scope: + impersonation clause to template save_passkey_name</name>
  <files>priv/templates/sigra.install/core/mfa_settings_live.ex</files>
  <read_first>
    - test/example/lib/example_web/live/mfa_settings_live.ex:768-787 (twin: rename_passkey with scope: 4th keyword arg + the {:error, :impersonation_forbidden} clause + copy)
    - priv/templates/sigra.install/core/mfa_settings_live.ex:728-751 (target handle_event) and :814, :874 (sibling disable_mfa/regenerate_codes clauses already passing scope: — match their local put_flash style)
  </read_first>
  <behavior>
    - The template's Auth.rename_passkey call passes scope: socket.assigns.current_scope (per D-06)
    - A {:error, :impersonation_forbidden} -> clause precedes the {:error, _reason} catch-all and flashes "You can't change account security settings while impersonating."
    - Sibling handlers (disable_mfa, regenerate_codes) are unchanged — rename_passkey was the lone gap
  </behavior>
  <action>
    In `save_passkey_name/2` at the `Auth.rename_passkey(user, credential_id, nickname || "")` call (template line ~736), add a fourth keyword argument `scope: socket.assigns.current_scope`, mirroring the twin at example line 768. Insert an `{:error, :impersonation_forbidden} ->` case clause BEFORE the existing `{:error, _reason} ->` catch-all; the new clause returns `{:noreply, put_flash(socket, :error, "You can't change account security settings while impersonating.")}` using the template's own local put_flash idiom (do NOT copy the twin's vt-* markup — only mirror the Elixir semantics). Match the impersonation-flash wording used by the sibling clauses at template lines 814 and 874. Do not touch disable_mfa or regenerate_codes.
  </action>
  <verify>
    <automated>grep -q "scope: socket.assigns.current_scope" priv/templates/sigra.install/core/mfa_settings_live.ex && [ "$(grep -c impersonation_forbidden priv/templates/sigra.install/core/mfa_settings_live.ex)" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'impersonation_forbidden' priv/templates/sigra.install/core/mfa_settings_live.ex` returns 3 (the two pre-existing sibling clauses + the new rename_passkey clause)
    - `grep -A6 'Auth.rename_passkey' priv/templates/sigra.install/core/mfa_settings_live.ex` shows the `scope: socket.assigns.current_scope` keyword on the call
    - `mix compile --warnings-as-errors` (library) still passes; no disable_mfa/regenerate_codes lines changed (`git diff` scoped to the save_passkey_name handler + its case)
  </acceptance_criteria>
  <done>Template save_passkey_name passes scope: and handles impersonation_forbidden, mirroring the twin; siblings untouched.</done>
</task>

<task type="auto">
  <name>Task 2: SHIP-02a — dedupe delete-passkey confirmation body copy</name>
  <files>priv/templates/sigra.install/core/mfa_settings_live.ex</files>
  <read_first>
    - test/example/lib/example_web/live/mfa_settings_live.ex:383-385 (twin heading + deduped body)
    - priv/templates/sigra.install/core/mfa_settings_live.ex:361 (heading — KEEP) and :363 (body — DROP leading duplicate)
  </read_first>
  <action>
    In the delete-passkey confirmation block, leave the heading paragraph at template line 361 unchanged. In the body paragraph at template line 363, remove ONLY the redundant leading sentence so the body reads exactly: `You'll still need another sign-in method before removing your last recovery option.` — matching the twin body at example line 385. Keep the template's own class names (do not import the twin's vt-* classes); mirror the copy text only.
  </action>
  <verify>
    <automated>grep -c "Delete this passkey" priv/templates/sigra.install/core/mfa_settings_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Delete this passkey' priv/templates/sigra.install/core/mfa_settings_live.ex` returns 1 (the heading at line 361 only; the duplicate in the body is gone)
    - The body paragraph text equals the twin's line 385 exactly: "You'll still need another sign-in method before removing your last recovery option."
    - Heading paragraph (line 361) unchanged
  </acceptance_criteria>
  <done>Delete-confirmation body no longer repeats the heading sentence; copy matches the example twin.</done>
</task>

<task type="auto">
  <name>Task 3: Re-bless the install golden fixture and prove no drift</name>
  <files>test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex</files>
  <read_first>
    - lib/mix/tasks/sigra.fixture.rebless_golden.ex (the re-bless task: scaffolds a fresh tmp app, normalizes, replaces the tree, prints a delta report)
    - .planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-RESEARCH.md (§ "Re-bless + drift-check", § Pitfall 4)
    - test/sigra/install/golden_diff_test.exs (the golden gate)
  </read_first>
  <action>
    Ensure Postgres is up (`scripts/db/up.sh`; `source tmp/db.env`) and the phx_new 1.8.8 archive is installed. Run `MIX_ENV=test mix sigra.fixture.rebless_golden` to regenerate the golden tree from the edited template. Do NOT hand-edit any file under test/fixtures/install_golden/tree/ — the generator owns it (Don't Hand-Roll). The only expected golden delta is in the re-blessed mfa_settings_live.ex (scope: arg, impersonation clause, deduped delete body); the organization_settings_live.ex fixture is already type=-clean and must NOT change (confirms D-01: PUB-01 has no golden delta). Then run the drift-check and the golden gate test.
  </action>
  <verify>
    <automated>MIX_ENV=test mix sigra.fixture.rebless_golden --check</automated>
  </verify>
  <acceptance_criteria>
    - `MIX_ENV=test mix sigra.fixture.rebless_golden --check` exits 0 (no residual drift)
    - `MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs` passes
    - `grep -c 'impersonation_forbidden' test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex` returns 3 and `grep -c 'Delete this passkey' <same file>` returns 1
    - `git status` shows changes limited to the re-blessed mfa_settings_live.ex fixture (organization_settings_live.ex fixture untouched)
  </acceptance_criteria>
  <done>Golden fixture re-blessed from the edited template; drift-check clean; golden_diff_test green; organization_settings fixture unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| impersonator session → account security operation | An admin impersonating a user must not perform sensitive security changes on the victim's account |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-221-01 | Elevation of Privilege | generated `save_passkey_name` in `mfa_settings_live.ex` | high | mitigate | Pass `scope: socket.assigns.current_scope` so `Auth.rename_passkey/4` runs `forbid_sensitive_operation` and returns `{:error, :impersonation_forbidden}` during impersonation (SHIP-01 / D-06) |
| T-221-02 | Tampering | golden fixture drift vs template | low | mitigate | `mix sigra.fixture.rebless_golden --check` + `golden_diff_test.exs` enforce template↔golden parity; hand-editing the tree is prohibited |

Package legitimacy: N/A — no package installs in this plan.
</threat_model>

<verification>
- Library compiles `--warnings-as-errors`.
- `MIX_ENV=test mix test test/sigra/install/golden_diff_test.exs` passes.
- Grep proofs above hold on both the template and the re-blessed golden fixture.
</verification>

<success_criteria>
Generated hosts inherit the passkey-rename impersonation guard and the deduped delete copy; the golden fixture is re-blessed with no drift and only the expected mfa_settings delta.
</success_criteria>

<output>
Create `.planning/phases/221-unblock-the-gate-ship-honest-generated-host-debt/221-01-SUMMARY.md` when done.
</output>
