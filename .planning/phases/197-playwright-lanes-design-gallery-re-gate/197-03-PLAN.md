---
phase: 197-playwright-lanes-design-gallery-re-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/static/assets/fonts/space-grotesk-var.woff2
  - test/example/priv/static/assets/css/app.css
  - test/example/priv/playwright/tests/admin-design.spec.ts
autonomous: true
requirements: [PW-03]
must_haves:
  truths:
    - "A self-hosted Space Grotesk woff2 is committed under the example priv/static and served at /assets/fonts/space-grotesk-var.woff2"
    - "app.css declares an @font-face for Space Grotesk and overrides :root --font-sans so body text uses the brand font OS-independently (D-08)"
    - "admin-design.spec.ts waits for `document.fonts.ready` (and asserts the face loaded) before capture so element heights are font-stable (D-08)"
    - "MG-5/6 no longer uses test.fail(): the seeded admin user has >=25 audit events so /admin/audit pagination renders deterministically, and test.fail() is removed (D-11a)"
  artifacts:
    - path: "test/example/priv/static/assets/fonts/space-grotesk-var.woff2"
      provides: "self-hosted brand font (converted from in-repo TTF)"
    - path: "test/example/priv/static/assets/css/app.css"
      provides: "@font-face + --font-sans override"
      contains: "Space Grotesk"
    - path: "test/example/priv/playwright/tests/admin-design.spec.ts"
      provides: "fonts.ready readiness + MG-5/6 deterministic pagination"
      contains: "fonts"
  key_links:
    - from: "test/example/priv/static/assets/css/app.css"
      to: "test/example/priv/static/assets/fonts/space-grotesk-var.woff2"
      via: "@font-face src url('/assets/fonts/space-grotesk-var.woff2')"
      pattern: "space-grotesk-var\\.woff2"
    - from: "test/example/priv/playwright/tests/admin-design.spec.ts"
      to: "browser font loading"
      via: "await document.fonts.ready in waitForLiveViewReady"
      pattern: "fonts\\.ready"
  prohibitions:
    - "Do NOT edit default.css (regenerable daisyUI dump — header says it can be removed)"
    - "Do NOT edit sigra_admin.css or any priv/templates/sigra.install/** file (installer-parity scope creep — Deferred Idea)"
    - "Do NOT leave test.fail() standing on MG-5/6 (it spuriously passes if pagination ever appears)"
    - "Do NOT add a fixed-duration wait to substitute for fonts.ready"
---

<objective>
PW-03 root-cause font determinism. The ~20-53px admin-design reflow is a host-OS `system-ui`
metric delta (macOS capture box vs ubuntu CI) — NOT a font-load failure (D-07: no webfont is
served anywhere today, verified). Remediation (D-08, user-confirmed): bundle a self-hosted
Space Grotesk woff2, set it as the served `--font-sans` so render is OS-INDEPENDENT in both
local capture and CI, and have the design spec wait for `document.fonts.ready` before capture.
Also resolve MG-5/6 `test.fail()` (D-11a) by relying on the existing seeded admin user (≥25
audit events) so `/admin/audit` pagination renders deterministically, then removing `test.fail()`.

Purpose: This is the render-determinism FOUNDATION. Baselines cannot be recaptured (Plan 04)
nor the gallery re-gated (Plan 05) until render is deterministic. This plan makes ROADMAP
success criterion #3 ("a brand font loads") LITERALLY true.
Output: committed woff2 + app.css @font-face/--font-sans + spec fonts.ready + MG-5/6 fixed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-RESEARCH.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-PATTERNS.md
@test/example/lib/example/demo/seeds.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate + commit the self-hosted Space Grotesk woff2 (D-08)</name>
  <files>test/example/priv/static/assets/fonts/space-grotesk-var.woff2</files>
  <read_first>
    - scripts/brand/fonts/SpaceGrotesk[wght].ttf — in-repo variable wght brand TTF (136 KB, OFL), the conversion source.
    - 197-RESEARCH.md §Standard Stack "woff2 generation" — the `python3 fontTools` (4.62.1) conversion snippet;
      `pip install brotli` only if woff2 save raises ImportError; commit the asset so CI needs no fonttools.
    - 197-PATTERNS.md (woff2 render-asset assignment) — convert-not-author; generate once locally, commit.
  </read_first>
  <action>
    Create the directory `test/example/priv/static/assets/fonts/` and generate
    `space-grotesk-var.woff2` from `scripts/brand/fonts/SpaceGrotesk[wght].ttf` using the installed
    python `fontTools` (set `TTFont.flavor = "woff2"`, save, preserving the variable wght axis). If the
    save raises `ImportError: No module named 'brotli'`, `pip install brotli` locally first, then retry.
    Commit the woff2 binary (the CI recapture job in Plan 04 consumes the committed asset — no fonttools in CI).
    Verify the output is a valid woff2 (non-empty, magic bytes `wOF2`).
  </action>
  <verify>
    <automated>test -f test/example/priv/static/assets/fonts/space-grotesk-var.woff2 && head -c4 test/example/priv/static/assets/fonts/space-grotesk-var.woff2 | grep -q 'wOF2' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test/example/priv/static/assets/fonts/space-grotesk-var.woff2` exists, is non-empty, and starts with the `wOF2` magic.
    - It was converted from the in-repo TTF (no third-party download — supply-chain provenance, T-197-07).
  </acceptance_criteria>
  <done>The brand font is committed as a served static asset.</done>
</task>

<task type="auto">
  <name>Task 2: Add @font-face + --font-sans override to app.css (D-08)</name>
  <files>test/example/priv/static/assets/css/app.css</files>
  <read_first>
    - test/example/priv/static/assets/css/app.css — the served example CSS; loads LAST per
      root.html.heex:12 (after default.css → sigra_admin.css), so its `:root` override wins the cascade.
    - test/example/priv/static/assets/default.css:9 — the canonical `--font-sans` system stack to mirror
      (DO NOT EDIT default.css — it is a regenerable daisyUI dump).
    - test/example/lib/example_web/components/layouts/root.html.heex:10-12 — the CSS link order proving app.css is last.
    - 197-RESEARCH.md Code Examples §3 + Pitfall 3 — the exact @font-face form (`format('woff2-variations')`,
      `font-weight: 300 700`, `font-display: swap`) and why app.css is the durable home (not default.css, not sigra_admin.css).
  </read_first>
  <action>
    Append to app.css (per D-08, RESEARCH §3): (1) an `@font-face` for `font-family: 'Space Grotesk'`
    with `src: url('/assets/fonts/space-grotesk-var.woff2') format('woff2-variations')`,
    `font-weight: 300 700`, `font-display: swap`, `font-style: normal`; and (2) a `:root { --font-sans:
    'Space Grotesk', ui-sans-serif, system-ui, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji',
    'Segoe UI Symbol', 'Noto Color Emoji'; }` override so body text uses the brand font OS-independently.
    Do NOT touch default.css or sigra_admin.css or any installer template (scope creep — Deferred Idea).
  </action>
  <verify>
    <automated>grep -q "space-grotesk-var.woff2" test/example/priv/static/assets/css/app.css && grep -q "font-family: 'Space Grotesk'" test/example/priv/static/assets/css/app.css && grep -q -- "--font-sans" test/example/priv/static/assets/css/app.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains an `@font-face` referencing `/assets/fonts/space-grotesk-var.woff2`.
    - app.css contains a `:root` `--font-sans` override leading with `'Space Grotesk'`.
    - default.css, sigra_admin.css, and all priv/templates/sigra.install/** are unchanged.
  </acceptance_criteria>
  <done>The served CSS loads the brand font and routes --font-sans to it OS-independently.</done>
</task>

<task type="auto">
  <name>Task 3: Wait for document.fonts.ready in admin-design.spec.ts + assert the face loaded (D-08, Pitfall 7)</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts:15-19 — the `waitForLiveViewReady(page)` helper
      to extend in place (currently only waits for `[data-phx-session].phx-connected`).
    - 197-RESEARCH.md Pitfall 7 — `document.fonts.ready` resolves instantly if no face is requested; guard with
      `document.fonts.check('16px "Space Grotesk"')` so a 404 woff2 / non-applied --font-sans fails LOUDLY.
    - 197-PATTERNS.md (admin-design.spec.ts D-08 assignment) — the exact extension shape.
  </read_first>
  <action>
    Extend `waitForLiveViewReady` (admin-design.spec.ts ~15-19): after the existing
    `[data-phx-session].phx-connected` wait, add `await page.evaluate(async () => { await (document as any).fonts.ready; });`
    then a HARD guard: `const ok = await page.evaluate(() => (document as any).fonts.check('16px "Space Grotesk"'));`
    and `expect(ok, 'Space Grotesk must be loaded before snapshot').toBe(true);` (Pitfall 7 — fail loudly if the
    face never loaded). Per D-08. Keep the helper's signature and all existing call sites unchanged. Do NOT add a
    fixed sleep.
  </action>
  <verify>
    <automated>grep -q 'fonts.ready' test/example/priv/playwright/tests/admin-design.spec.ts && grep -q "fonts.check('16px \"Space Grotesk\"')" test/example/priv/playwright/tests/admin-design.spec.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `waitForLiveViewReady` awaits `document.fonts.ready` and asserts `document.fonts.check('16px "Space Grotesk"')` is true.
    - No fixed-duration wait introduced.
    - All existing `waitForLiveViewReady(page)` call sites remain unchanged.
  </acceptance_criteria>
  <done>The design spec captures only after the brand font is loaded; a missing face fails loudly.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve MG-5/6 test.fail() via the seeded ≥25-event user (D-11a)</name>
  <files>test/example/priv/playwright/tests/admin-design.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-design.spec.ts:318-386 — the MG-5/6 content-equivalence test;
      `test.fail()` at line 323, then it navigates to the REAL /admin/users, /admin/audit, and a user-detail audit
      page and asserts pagination links (`getByRole('link', { name: 'Previous page' })`) — pagination only renders
      at ≥25 rows.
    - test/example/lib/example/demo/seeds.ex:634-710 — `seed_audit_events/2` → `insert_audit_batch/3` ALREADY
      inserts 41 `@audit_actions` rows tied to `admin.id` via `effective_user_id: admin.id` (lines 660-681), with
      `allow_reserved: true` (3rd changeset arg) and the pinned `@seed_reference_ts`, guarded by a count-threshold
      idempotency check (638-650) inside `Repo.transaction`. The lane runs `mix run priv/repo/seeds.exs` at boot, so
      the seeded heavy user (41 events ≥ the @default_limit 25 threshold) is present in the DB the spec hits.
    - lib/sigra/admin/audit/query_params.ex:22 — `@default_limit 25` (the pagination threshold).
    - 197-RESEARCH.md Pitfall 5 + Assumption A3 — the static gallery boards (design_gallery_live.ex:611-833)
      render hardcoded literal markup, so the seeded heavy user does NOT perturb the 72 element-scoped baselines.
  </read_first>
  <action>
    The seed path ALREADY produces a user (admin) with 41 audit events ≥ the 25-row pagination threshold, present
    in the dev DB the lane boots (`mix run priv/repo/seeds.exs`). Therefore the preferred D-11a outcome is reachable
    WITHOUT new seed code: confirm (by reading seeds.ex) that `seed_audit_events`/`insert_audit_batch` ties ≥25 rows
    to a user surfaced by the spec's `/admin/audit` navigation, then REMOVE `test.fail();` (line 323) and the stale
    `known_failure:` comment above it (lines ~321-322). If — and only if — reading the spec shows it navigates to a
    PER-TEST freshly-registered user's audit page (not the global /admin/audit and not the seeded admin), and that
    fresh user cannot reach ≥25 events, fall back to D-11b: replace `test.fail();` with
    `test.skip('data-dependent pagination; tracked in .planning/todos/pending/2026-06-17-admin-design-mg5-6-content-equivalence-data-dependent.md');`
    Either way `test.fail()` MUST be gone. Do NOT add an unguarded insert to the seed (respect the count-threshold
    idempotency guard if any seed change proves necessary).
  </action>
  <verify>
    <automated>grep -q 'test.fail()' test/example/priv/playwright/tests/admin-design.spec.ts && { echo "FAIL test.fail() still present"; exit 1; } || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test.fail()' test/example/priv/playwright/tests/admin-design.spec.ts` returns 0.
    - MG-5/6 either runs for-real (D-11a, relying on the seeded ≥25-event user) or is `test.skip(...)` with the recorded todo reason (D-11b).
    - The 72 element-scoped baselines are NOT perturbed by any seed reliance (gallery boards are static literals — A3).
    - If any seed change was needed, the count-threshold idempotency guard is preserved (`run/0` stays a no-op on re-run).
  </acceptance_criteria>
  <done>MG-5/6 no longer relies on test.fail(); pagination is exercised deterministically or explicitly skipped with a reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed binary asset → repo | The woff2 is a CI/render input; its provenance must be in-repo, not a network fetch |
| served CSS → browser render | --font-sans drives every body-text metric; a wrong override silently shifts ALL baselines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-06 | Tampering / Integrity | document.fonts.ready capture timing | mitigate | `document.fonts.ready` alone resolves instantly if the face never loads (Pitfall 7); the `fonts.check('16px "Space Grotesk"')` assertion fails the spec loudly if the woff2 404s or --font-sans didn't apply — prevents a silently-pre-font capture from establishing a wrong baseline |
| T-197-07 | Tampering / Supply chain | committed woff2 binary | mitigate | Generated locally from the in-repo OFL Space Grotesk TTF (`scripts/brand/fonts/`); NO Google Fonts CDN / third-party download (which would also reintroduce render nondeterminism). CI consumes the committed asset only |
| T-197-08 | Spoofing | test.fail() masking | mitigate | Removing `test.fail()` (D-11) eliminates a spurious-pass path where MG-5/6 would "pass" by failing; real assertion now runs |
</threat_model>

<verification>
- woff2 exists with `wOF2` magic; app.css references it and overrides `--font-sans`.
- admin-design.spec.ts awaits `document.fonts.ready` and asserts the face loaded.
- `grep -c 'test.fail()'` on admin-design.spec.ts returns 0.
- default.css / sigra_admin.css / installer templates unchanged
  (`git diff --name-only` shows only the 3 files_modified).
- Local sanity (optional, needs booted app): `curl -sf http://localhost:4011/assets/fonts/space-grotesk-var.woff2` → 200.
- NOTE: baselines will visibly change once the font loads — that is EXPECTED and is recaptured in Plan 04
  (do NOT recapture here; this plan only makes render deterministic).
</verification>

<success_criteria>
PW-03 render-determinism foundation in place: a brand font now loads (criterion #3 literally true),
render is OS-independent for both local and CI capture, the design spec waits for the font, and
MG-5/6 no longer uses test.fail(). Baseline recapture and re-gating follow in Plans 04 and 05.
</success_criteria>

<output>
Create `.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-03-SUMMARY.md` when done
</output>
