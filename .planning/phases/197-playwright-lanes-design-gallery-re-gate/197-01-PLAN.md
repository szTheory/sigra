---
phase: 197-playwright-lanes-design-gallery-re-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/priv/playwright/tests/organizations.spec.ts
  - test/example/priv/playwright/tests/ga-uat-shift-left.spec.ts
autonomous: true
requirements: [PW-02]
must_haves:
  truths:
    - "No `waitForTimeout` (fixed sleep) remains anywhere in test/example/priv/playwright/tests"
    - "Both invitation-link mailbox poll loops resolve via expect.poll() with a Playwright-managed interval/timeout"
    - "organizations.spec.ts and ga-uat-shift-left.spec.ts still pass against a booted example app"
  artifacts:
    - path: "test/example/priv/playwright/tests/organizations.spec.ts"
      provides: "expect.poll mailbox readiness (no fixed sleep)"
      contains: "expect"
    - path: "test/example/priv/playwright/tests/ga-uat-shift-left.spec.ts"
      provides: "expect.poll mailbox readiness (no fixed sleep)"
      contains: "expect"
  key_links:
    - from: "test/example/priv/playwright/tests/organizations.spec.ts"
      to: "/dev/mailbox/json"
      via: "expect.poll re-queries the dev mailbox until the invitation link appears"
      pattern: "expect\\.poll"
  prohibitions:
    - "No `await page.waitForTimeout(` call survives in either spec file"
    - "No `Process.sleep` or any fixed-duration browser-lane wait is introduced"
---

<objective>
PW-02 readiness determinism for the two mailbox poll loops. Replace the two
`await page.waitForTimeout(1_000)` fixed sleeps — the ONLY fixed-duration waits in
browser-lane test code (per RESEARCH §D-05/D-06, verified) — with `expect.poll()`
re-querying `/dev/mailbox/json` until the invitation link appears.

Purpose: PW-02 requires deterministic readiness with no fixed sleeps. The bash
readiness loops in ci.yml are explicit poll-until-ready probes (D-05, kept as-is);
these two spec sleeps are the only remaining offenders.
Output: Both specs poll via the established in-suite `expect.poll()` idiom; the static
grep `grep -rn waitForTimeout test/example/priv/playwright/tests` returns nothing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-RESEARCH.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite organizations.spec.ts mailbox loop to expect.poll (D-06)</name>
  <files>test/example/priv/playwright/tests/organizations.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/organizations.spec.ts:115-156 — the
      invitation-link helper: a `for (let attempt = 0; attempt < 30; ...)` loop that
      `fetch('/dev/mailbox/json')`, finds the row whose `to` includes the recipient and
      whose body contains `/invitations/`, extracts the `…/accept` link, and ends each
      iteration with `await page.waitForTimeout(1_000)` at line 152.
    - test/example/priv/playwright/tests/passkeys-hooks.spec.ts:101-114 — the in-suite
      `await expect.poll(async () => …).toEqual(...)` precedent to mirror (idiom, intervals,
      message shape).
    - 197-PATTERNS.md (the `organizations.spec.ts:152 → expect.poll()` assignment) — the
      faithful-port rewrite (capture `link` via a `let link: string | null = null` closure;
      old budget was 30×~1s → `timeout: 30_000`; intervals are Claude's-discretion).
  </read_first>
  <action>
    Replace the `for`-loop + `await page.waitForTimeout(1_000)` mailbox-poll body in the
    invitation-link helper (organizations.spec.ts ~115-156) with a single
    `await expect.poll(async () => { … return link !== null; }, { message, intervals: [250, 500, 1000], timeout: 30_000 }).toBe(true)`
    that re-queries `/dev/mailbox/json`, finds the recipient's `/invitations/` row, matches the
    `…/accept` URL, and assigns the resolved absolute link to an outer `let link: string | null`
    closure variable. After the poll, `if (!link) throw new Error(...)` then `return link`. Per D-06.
    Preserve the helper's existing signature and `page` parameter type exactly — only the loop body changes.
    Do NOT introduce any new fixed-duration wait. Keep the same recipient-match and link-extraction
    logic (the `URL` rewrite of protocol/host from `page.url()`).
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && grep -c 'waitForTimeout' tests/organizations.spec.ts | grep -qx 0 && grep -q 'expect' tests/organizations.spec.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'waitForTimeout' tests/organizations.spec.ts` returns 0.
    - The helper uses `expect.poll(...)` with an explicit `timeout` and `intervals`.
    - The link is still returned (closure `let link` assigned inside the poll, thrown if null after).
    - No behavior change to recipient-matching or link extraction.
  </acceptance_criteria>
  <done>organizations.spec.ts has no fixed sleep; mailbox readiness is expect.poll-driven.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ga-uat-shift-left.spec.ts mailbox loop to expect.poll (D-06)</name>
  <files>test/example/priv/playwright/tests/ga-uat-shift-left.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/ga-uat-shift-left.spec.ts:74-109 — the
      byte-identical mailbox poll loop, ending in `await page.waitForTimeout(1_000)` at line 106.
    - test/example/priv/playwright/tests/organizations.spec.ts — the sibling rewrite landed in
      Task 1 (apply the same transformation; this file uses the same loop shape with its own
      `page` parameter type — keep that file's existing signature).
    - 197-RESEARCH.md Code Examples §2 — the full expect.poll target shape.
  </read_first>
  <action>
    Apply the identical transformation from Task 1 to the mailbox-poll body at
    ga-uat-shift-left.spec.ts ~74-109: replace the `for`-loop + `await page.waitForTimeout(1_000)`
    (line 106) with `await expect.poll(async () => { … return link !== null; }, { message, intervals: [250, 500, 1000], timeout: 30_000 }).toBe(true)`,
    capturing the resolved link via an outer `let link: string | null` closure and throwing if null
    afterward. Per D-06. Preserve this file's existing helper signature and `page` parameter type; only
    swap the loop body. No new fixed-duration wait.
  </action>
  <verify>
    <automated>cd test/example/priv/playwright && grep -rc 'waitForTimeout' tests/ | grep -v ':0$' | grep -qv . && echo PASS-NO-SLEEPS-REMAIN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'waitForTimeout' tests/ga-uat-shift-left.spec.ts` returns 0.
    - `grep -rln 'waitForTimeout' test/example/priv/playwright/tests/` returns NOTHING (both sites gone).
    - The helper uses `expect.poll(...)` with explicit `timeout` and `intervals`; the link is still returned.
  </acceptance_criteria>
  <done>Both mailbox poll loops are expect.poll-driven; zero fixed sleeps remain in the browser-lane tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → dev mailbox endpoint | The spec reads `/dev/mailbox/json` from the local booted dev app; trusted, dev-only transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-01 | Denial of Service | expect.poll timeout | mitigate | Bound the poll with an explicit `timeout: 30_000` (≈ the prior 30×1s budget) so a missing email fails fast with a clear message instead of hanging the lane |
| T-197-02 | Tampering | test-readiness integrity | accept | No untrusted input crosses; the mailbox is the dev app's own outbox. Polling cannot weaken a gate — a never-arriving link still fails the test |
</threat_model>

<verification>
- `grep -rln 'waitForTimeout' test/example/priv/playwright/tests/` returns nothing.
- Both specs pass locally against a booted example app on :4011
  (`SIGRA_EXAMPLE_URL=http://localhost:4011`, per STATE.md Phase-191 note) or in CI on :4000.
- No `Process.sleep` or fixed sleep introduced.
</verification>

<success_criteria>
PW-02 (sleep-removal portion) satisfied: the two `waitForTimeout(1_000)` mailbox waits are
replaced by `expect.poll()`; the browser-lane test tree is free of fixed-duration waits.
</success_criteria>

<output>
Create `.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-01-SUMMARY.md` when done
</output>
