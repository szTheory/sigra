---
phase: 197-playwright-lanes-design-gallery-re-gate
plan: 04
type: execute
wave: 2
depends_on: ["197-02", "197-03"]
files_modified:
  - .github/workflows/ci.yml
  - test/example/priv/playwright/snapshot-allowlist-design
autonomous: true
requirements: [PW-03]
must_haves:
  truths:
    - "A new sibling job `admin_design_recapture` exists in ci.yml, gated to non-PR events (if: github.event_name != 'pull_request') with job-level permissions: contents: write (D-09)"
    - "The job boots the example app (cloned prelude from example_playwright_smoke) and runs admin-design.spec.ts across all 3 projects with --update-snapshots"
    - "Recaptured PNGs pass through scripts/ci/snapshot-canary-guard.sh with explicit --allow for each recaptured slug; the board-notice canary re-baseline is handled deliberately (OQ1)"
    - "The recapture commit targets a reviewable PR/branch, NOT a silent push to main (OQ2)"
    - "Whether demo-showcase / admin-checkpoints baselines also shift under the global --font-sans change is checked in COMPARE mode; if either shifts it is deferred to a tracked todo with a concrete per-lane recapture path (NOT an unscoped mid-run recapture), so no sibling lane is left silently red (OQ3)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "admin_design_recapture job"
      contains: "admin_design_recapture"
  key_links:
    - from: "admin_design_recapture job"
      to: "scripts/ci/snapshot-canary-guard.sh"
      via: "gate recaptured PNGs with --allowlist + --allow per slug + --canary board-notice"
      pattern: "snapshot-canary-guard"
  prohibitions:
    - "The recapture job must NOT carry contents: write at the workflow level — only at the job level (least privilege)"
    - "The recapture job must NOT run on pull_request events"
    - "Do NOT edit scripts/ci/snapshot-canary-guard.sh (consume it, do not modify the guard)"
    - "Do NOT auto-push recaptured baselines to main without review (OQ2)"
    - "Do NOT silently leave demo-showcase/admin-checkpoints red if the font shifts them — but do NOT recapture them in this execution either; a shift is deferred to a tracked todo with a concrete per-lane recapture path (OQ3, bounded scope)"
---

<objective>
PW-03 in-CI baseline recapture. With render now deterministic (Plan 03), recapture ALL
admin-design baselines in the ubuntu CI environment so the committed PNGs match what CI renders.
This is a dedicated non-PR-gated job (D-09) — NOT inline in the PR gate — because it commits
binary PNGs and needs `contents: write`, which the PR gate must not carry.

Purpose: The 72 committed admin-design PNGs (24 boards × 3 projects) were captured on macOS;
the font change moves them. Recapturing in CI makes the steady-state baselines CI-native so the
re-gated gallery (Plan 05) runs green. Resolves the three RESEARCH Open Questions:
OQ1 board-notice canary re-baseline, OQ2 commit target, OQ3 cross-lane scope.
Output: a reviewable recapture job in ci.yml + (if any) one-time design allowlist entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-RESEARCH.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-PATTERNS.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-02-SUMMARY.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-03-SUMMARY.md
@scripts/ci/snapshot-canary-guard.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the admin_design_recapture sibling job (boot prelude + recapture step) (D-09)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:512-563 — `upgrade_smoke` job: copy its EXACT header shape (name/runs-on/
      `if: github.event_name != 'pull_request'`/`needs: release_ref_guard`) and the `services: postgres:` block.
    - .github/workflows/ci.yml:24-26 — the workflow default `permissions: { contents: read }`; the recapture job
      overrides with a JOB-LEVEL `permissions: { contents: write }` (never widen the global block).
    - .github/workflows/ci.yml:886-968 — the example_playwright_smoke boot prelude to clone verbatim
      (checkout@v6 SHA-pin, setup-beam@v1.24.0, setup-node@v6, cache example deps, `mix deps.get`,
      `mix compile --warnings-as-errors`, `ecto.create && ecto.migrate`, `mix run priv/repo/seeds.exs`,
      `npm ci`, `npx playwright install --with-deps chromium webkit`, `mix phx.server > /tmp/...log 2>&1 &`,
      the curl + route warm-up readiness loops at 953-968). Use the SAME pinned action SHAs (PATTERNS: SHA-pinned refs).
    - .github/workflows/ci.yml:1027-1049 — the design-gallery step to mirror for the recapture command (same
      `--project=admin-design-{chromium,mobile,dark}`, add `--update-snapshots`).
    - 197-PATTERNS.md (admin_design_recapture job assignment) + 197-RESEARCH.md Code Examples §4 — full job shape.
  </read_first>
  <action>
    Add a new sibling job `admin_design_recapture` to ci.yml (per D-09, modeled on upgrade_smoke):
    `name: Recapture admin-design baselines (in-CI)`, `runs-on: ubuntu-latest`,
    `if: github.event_name != 'pull_request'`, `needs: release_ref_guard`, and a JOB-LEVEL
    `permissions: { contents: write }`. Add the `services: postgres:` block (copy from upgrade_smoke/
    example_playwright_smoke). Clone the example_playwright_smoke boot prelude verbatim (same SHA-pinned
    actions, same `MIX_ENV: dev` + `PGUSER/PGPASSWORD/PGHOST` env, same backgrounded-server log redirect +
    readiness loops). Add a "Recapture admin-design baselines" step that runs
    `npx playwright test tests/admin-design.spec.ts --project=admin-design-chromium --project=admin-design-mobile --project=admin-design-dark --update-snapshots`
    from `test/example/priv/playwright` with `CI: "true"` and `SIGRA_EXAMPLE_URL: "http://localhost:4000"`.
    Reuse the WR-04 backgrounded-server log + `if: failure()` dump idiom so a recapture boot failure is legible.
    Do NOT touch the example_playwright_smoke job here (Plan 05 owns the re-gate); this is purely additive.
  </action>
  <verify>
    <automated>grep -q 'admin_design_recapture' .github/workflows/ci.yml && grep -q 'contents: write' .github/workflows/ci.yml && grep -q -- '--update-snapshots' .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `admin_design_recapture` job exists with `if: github.event_name != 'pull_request'` and job-level `permissions: { contents: write }`.
    - Workflow default `permissions: { contents: read }` (ci.yml:26) is UNCHANGED.
    - The recapture step runs admin-design.spec.ts across all 3 admin-design projects with `--update-snapshots`.
    - The workflow still parses as valid YAML.
  </acceptance_criteria>
  <done>A non-PR-gated job boots the app on ubuntu and recaptures all admin-design baselines.</done>
</task>

<task type="auto">
  <name>Task 2: Gate + commit recaptured baselines via canary-guard; handle the board-notice canary (OQ1) and a reviewable commit target (OQ2)</name>
  <files>.github/workflows/ci.yml, test/example/priv/playwright/snapshot-allowlist-design</files>
  <read_first>
    - scripts/ci/snapshot-canary-guard.sh:27-29 (flags `--allowlist` / `--allow <slug>` / `--canary <slug>`),
      :53-57 (slug derivation strips `-admin-design-{chromium|mobile|dark}.png` so ONE slug covers all 3 projects),
      :93-112 (canary: tolerates `added`, FORBIDS `modify`/`delete`; canary is NOT allowlistable).
    - test/example/priv/playwright/snapshot-allowlist-design — steady-state EMPTY (0 non-comment lines, verified);
      slugs use the HYPHENATED filename form (e.g. `board-stat-link`).
    - 197-RESEARCH.md Pitfall 6 + Open Question 1 — board-notice is the design-lane canary AND the font change
      modifies it; the guard rejects a canary `modify`. Open Question 2 — push-to-main vs open-a-PR.
    - 197-PATTERNS.md "Snapshot drift gate / commit-path" — consume the guard, do not edit it.
  </read_first>
  <action>
    Add a "Gate + commit recaptured baselines" step to the admin_design_recapture job that:
    (1) computes the set of changed admin-design slugs (from `git status --porcelain` over
    `tests/admin-design.spec.ts-snapshots/`, slug-deduped) and runs
    `bash scripts/ci/snapshot-canary-guard.sh --base HEAD --allowlist test/example/priv/playwright/snapshot-allowlist-design --canary board-notice --allow <each changed non-canary slug>`;
    (2) RESOLVES OQ1 (board-notice canary): because the global font change modifies `board-notice` and the guard
    forbids a canary `modify`, do the recapture as a DELIBERATE, REVIEWED canary re-baseline — handle `board-notice`
    as its own explicit, scoped re-establishment so the guard sees it as a one-time `added` event (delete the existing
    board-notice PNGs in a prior recapture sub-step so `--update-snapshots` re-creates them as `added`, which the canary
    guard tolerates per line 100), and DO NOT add board-notice to the allowlist (it is not allowlistable). Document this
    one-time canary re-baseline inline in the step.
    (3) RESOLVES OQ2 (commit target): commit the recaptured PNGs to a `ci/recapture-admin-design-<run_id>` branch and
    open a PR (via `gh pr create` or a push + PR step) rather than pushing to `main` — baseline changes get human review
    under branch protection. Use `git config user.name/email github-actions[bot]` and a `[skip ci]` commit message to
    avoid re-triggering CI on the recapture commit.
    (4) Leave `snapshot-allowlist-design` at its empty steady state in the committed tree; if a transient allowlist
    entry is needed for the recapture PR, add it ONLY on the recapture branch and note that the steady-state allowlist
    returns to empty after merge (this is the file's committed contract).
    Do NOT edit scripts/ci/snapshot-canary-guard.sh.
  </action>
  <verify>
    <automated>grep -q 'snapshot-canary-guard.sh' .github/workflows/ci.yml && grep -Eq 'skip ci|gh pr create|ci/recapture' .github/workflows/ci.yml && grep -vc '^#' test/example/priv/playwright/snapshot-allowlist-design | grep -qx 0 && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The job gates recaptured PNGs through `snapshot-canary-guard.sh` with `--canary board-notice` and `--allow` per changed non-canary slug.
    - OQ1: the `board-notice` canary is handled as a deliberate one-time re-baseline (re-established as `added`, NOT allowlisted, NOT a forbidden `modify`).
    - OQ2: recaptured PNGs land on a `ci/recapture-*` branch + PR (reviewable), NOT a silent push to main; the commit carries `[skip ci]`.
    - The committed `snapshot-allowlist-design` remains empty (steady-state contract).
    - `scripts/ci/snapshot-canary-guard.sh` is unchanged.
  </acceptance_criteria>
  <done>Recaptured CI-native baselines are committed via the canary guard into a reviewable PR; the canary re-baseline is deliberate.</done>
</task>

<task type="auto">
  <name>Task 3: Check cross-lane baseline drift from the global --font-sans (OQ3)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - 197-RESEARCH.md Open Question 3 — the `:root --font-sans` override is GLOBAL, so demo-showcase and
      admin-checkpoints (which also render `--font-sans` body text) may also shift; admin-checkpoints is
      fixed-viewport + tolerant (SEED-006 asymmetry), demo-showcase has its own baselines with `maxDiffPixels`.
    - .github/workflows/ci.yml:991 (admin-checkpoints step), :1068 (demo-showcase step) — the lanes whose baselines
      could move.
    - test/example/priv/playwright/playwright.config.ts — the `maxDiffPixels` / tolerance settings per project that
      determine whether the shift is absorbed.
  </read_first>
  <action>
    Resolve OQ3 with a BOUNDED compare-and-report scope — do NOT expand into unscoped sibling-lane recapture
    mid-execution. Concretely: in the admin_design_recapture job (or a clearly documented sibling recapture step),
    run the admin-checkpoints and demo-showcase specs in COMPARE mode ONLY (no `--update-snapshots`) against the
    booted-with-font app.
    (a) If BOTH pass within existing tolerance: record in the SUMMARY that the global font does not move them; scope
        stays admin-design only. OQ3 closed.
    (b) If EITHER fails the compare: do NOT recapture those PNGs in THIS execution (the correct per-lane allowlist/
        canary names are not pre-resolved here and recapturing the wrong canary would arm/disarm the wrong tripwire).
        Instead OPEN A TRACKED TODO under `.planning/todos/pending/` capturing exactly which lane shifted, the observed
        maxDiffPixels delta, and the per-lane recapture command + allowlist/canary that lane uses
        (admin-checkpoints → snapshot-recapture-gate.sh + its allowlist/canary; demo-showcase → its own baselines),
        and record the deferral in the SUMMARY. Plan 05's gallery re-gate touches ONLY the design lane's
        `continue-on-error`, so a deferred sibling-lane recapture does NOT block the re-gate (those lanes were already
        green-gating pre-font; if the font moves them, that surfaces as a normal red the tracked todo addresses).
    Rationale for the bound: pre-resolving each sibling lane's allowlist/canary and doing a correct cross-lane
    recapture is a separate, deliberate operation; folding it into this job's mid-run path risks an unscoped,
    wrong-canary commit. Compare-and-report keeps OQ3 honestly closed without that risk.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The job runs admin-checkpoints and demo-showcase in COMPARE mode (no `--update-snapshots`) against the font-loaded app.
    - If both pass: SUMMARY records the font does not move them (scope stays admin-design). If either shifts: a tracked
      todo is opened (naming the lane, the delta, and that lane's recapture command + allowlist/canary) and the deferral
      is recorded in the SUMMARY — NOT an in-this-execution sibling recapture.
    - No sibling lane is left SILENTLY red (it is either ruled out or tracked); no wrong-canary/unscoped recapture occurs.
    - Workflow remains valid YAML.
  </acceptance_criteria>
  <done>Cross-lane font drift is measured in compare mode and either ruled out (recorded) or deferred to a tracked todo with a concrete recapture path — no unscoped mid-run recapture; re-gating is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recapture job → repo write | The only job in the workflow with `contents: write`; can commit to the repo |
| recaptured PNG → committed baseline | A recaptured baseline becomes the gate's source of truth; a bad capture poisons the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-09 | Elevation of Privilege | recapture job permissions | mitigate | `contents: write` is scoped to the recapture job ONLY (job-level block); the workflow default stays `contents: read` (ci.yml:26). The job is gated to non-PR events (`if: github.event_name != 'pull_request'`) so an untrusted PR cannot trigger a write-capable run. Block-worthy if write leaks to the PR gate. |
| T-197-10 | Tampering / Integrity | unreviewed baseline commit | mitigate | Recapture commits to a `ci/recapture-*` branch + PR (OQ2), NOT main — baseline changes get human review under branch protection instead of an auto-bypass of the gate they feed |
| T-197-11 | Spoofing | canary re-baseline | mitigate | The `board-notice` canary is re-established as a deliberate one-time `added` event (the guard's legitimate birth path), NOT allowlisted and NOT a silent `modify` — the tripwire stays armed for future incremental PRs |
| T-197-SC | Tampering | npm/playwright install in recapture job | accept | No NEW external package is added this phase (RESEARCH: Package Legitimacy Audit omitted — installs nothing new). The recapture job reuses the already-vetted pinned actions + vendored Playwright from example_playwright_smoke. No `[ASSUMED]`/`[SUS]` package introduced. |
</threat_model>

<verification>
- `admin_design_recapture` job present, non-PR-gated, job-level `contents: write`, default perms unchanged.
- Recapture runs admin-design across 3 projects with `--update-snapshots`; gates via canary-guard.
- board-notice canary handled as deliberate re-baseline (OQ1); commit targets a reviewable PR/branch (OQ2).
- Cross-lane drift (demo-showcase / admin-checkpoints) checked in COMPARE mode; ruled out (recorded) or deferred to a tracked todo with a concrete recapture path — no unscoped mid-run sibling recapture (OQ3).
- Committed `snapshot-allowlist-design` stays empty.
- Workflow parses as valid YAML; ci.yml-slicing ExUnit contract tests (`phase_58_*`) still green.
</verification>

<success_criteria>
PW-03 recapture in place: admin-design (and any font-shifted sibling) baselines are recaptured in the
ubuntu CI environment via a least-privilege, non-PR-gated, reviewable job; the canary is deliberately
re-baselined; cross-lane drift is closed. The gallery can now be hard-gated (Plan 05).
</success_criteria>

<output>
Create `.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-04-SUMMARY.md` when done
</output>