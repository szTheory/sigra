---
phase: 197-playwright-lanes-design-gallery-re-gate
plan: 05
type: execute
wave: 3
depends_on: ["197-04"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md
autonomous: true
requirements: [PW-03]
must_haves:
  truths:
    - "`continue-on-error: true` is removed from the 'Run design gallery boards' step — the gallery hard-gates on the PR path again (D-10)"
    - "The stale comment block encoding the false 'brand webfont does not load in CI' premise is rewritten to the corrected D-07 root cause"
    - "SEED-006 is updated in place to record the root-cause correction (no webfont existed; it was an OS system-font metric delta) — operator-truth requirement"
    - "The design-gallery step keeps its id: design_gallery + if: ${{ !cancelled() }} and hard-gates via the aggregator"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "re-gated (hard-gating) design gallery step"
    - path: ".planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md"
      provides: "recorded root-cause correction"
      contains: "system"
  key_links:
    - from: ".github/workflows/ci.yml design_gallery step"
      to: "the aggregator step (Plan 02)"
      via: "design_gallery.outcome is read by the aggregator; with continue-on-error gone, a real failure now goes red"
      pattern: "design_gallery"
  prohibitions:
    - "Do NOT remove continue-on-error before the recaptured baselines from Plan 04 are merged (the gallery must be green in-CI first)"
    - "Do NOT relocate the design-gallery step to nightly — it stays inline in example_playwright_smoke (D-10)"
    - "Do NOT silently fix SEED-006's false premise — record the correction explicitly"
    - "Do NOT widen pixel tolerance to mask the delta (root-caused, not papered over)"
---

<objective>
PW-03 re-gate. With render deterministic (Plan 03) and CI-native baselines recaptured (Plan 04),
remove `continue-on-error: true` from the "Run design gallery boards" step so the gallery
hard-gates on the PR path again (D-10), rewrite the stale comment block that encodes SEED-006's
false "brand webfont does not load in CI" premise, and record the root-cause correction (D-07)
back into SEED-006 (operator-truth requirement).

Purpose: Success criterion #4 and the phase goal require the gallery to hard-gate. It stays
INLINE in example_playwright_smoke (not relocated to nightly — D-10). The gallery now goes red
on a real visual regression via the Plan 02 aggregator. This plan is LAST because the gallery
must be green in CI (with Plan 04's recaptured baselines merged) before the hard gate is restored.
Output: continue-on-error removed, comment corrected, SEED-006 updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-RESEARCH.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-PATTERNS.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-02-SUMMARY.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-04-SUMMARY.md
@.planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove continue-on-error + rewrite the stale false-premise comment (D-10 / D-07)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:1027 — "Run design gallery boards (chromium, mobile, dark)" step. It carries
      `id: design_gallery` + `if: ${{ !cancelled() }}` (added in Plan 02) and `continue-on-error: true` at ~1043.
    - .github/workflows/ci.yml:~1032-1042 — the comment block stating "the brand webfont does not load in this
      dev-mode boot…" (the SEED-006 false premise corrected by D-07; RESEARCH §State of the Art deprecated note).
    - 197-RESEARCH.md D-07 / §State of the Art — the corrected root cause (no webfont existed; OS `system-ui`
      metric delta; now fixed by the self-hosted font in Plan 03 + CI-native recapture in Plan 04).
    - 197-PATTERNS.md (D-10 re-gate assignment) — delete continue-on-error AND rewrite the stale comment.
  </read_first>
  <action>
    Per D-10: DELETE the `continue-on-error: true` line (~1043) from the "Run design gallery boards" step so the
    gallery hard-gates again (its failure now propagates to the Plan 02 aggregator and reddens the job). KEEP the step
    inline in example_playwright_smoke (do NOT move to nightly) and keep its `id: design_gallery` + `if: ${{ !cancelled() }}`.
    Rewrite the stale ~1032-1042 comment block: remove the false "brand webfont does not load in this dev-mode boot"
    explanation and replace it with a brief accurate note — the gallery now hard-gates because render is deterministic
    via a self-hosted Space Grotesk woff2 + `document.fonts.ready` (Plan 03) and baselines are recaptured CI-native
    (Plan 04); the prior fragility was an OS system-font metric delta, not a font-load failure (D-07). Do NOT widen any
    pixel tolerance. Verify line numbers by step name before editing (drift-tolerant).
  </action>
  <verify>
    <automated>! grep -n 'continue-on-error' .github/workflows/ci.yml | grep -qi 'design\|gallery'; grep -A40 'Run design gallery boards' .github/workflows/ci.yml | grep -q 'design_gallery' && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The "Run design gallery boards" step no longer has `continue-on-error: true`.
    - The step retains `id: design_gallery` and `if: ${{ !cancelled() }}` (so the aggregator hard-gates on its failure).
    - The stale "brand webfont does not load in CI" comment is replaced with the corrected D-07 explanation.
    - No pixel tolerance widened; step stays inline (not nightly).
    - Workflow parses as valid YAML.
  </acceptance_criteria>
  <done>The admin-design gallery hard-gates on the PR path again, root-caused not tolerance-masked.</done>
</task>

<task type="auto">
  <name>Task 2: Record the SEED-006 root-cause correction (D-07 operator-truth)</name>
  <files>.planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md</files>
  <read_first>
    - .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md — the seed whose stated root cause
      ("the brand webfont (Space Grotesk) does not load in the CI dev boot") is FACTUALLY WRONG per D-07.
    - 197-RESEARCH.md D-07 + §State of the Art "Deprecated/outdated" — the verified correction: no `@font-face`,
      no `*.woff*`, no Google Fonts link served anywhere; served CSS used system stacks only; the ~20-53px reflow
      was a host-OS `system-ui` metric difference (macOS capture box vs ubuntu CI runner). Element-scoped boards
      reflow taller; fixed-viewport admin-checkpoints stays green.
    - 197-CONTEXT.md <specifics> — "The SEED-006 root-cause correction must be written back into the seed/VERIFICATION,
      not silently fixed."
  </read_first>
  <action>
    Add a clearly marked "Root-cause correction (Phase 197, D-07)" section to SEED-006 (do not delete the original
    text — record that it was corrected) stating: the original "brand webfont does not load in CI" premise was
    factually wrong (no webfont was served anywhere — verified: no @font-face, no woff2, no Google Fonts link); the
    real cause was a host-OS `system-ui` font-metric delta between the macOS capture box and the ubuntu CI runner; the
    Phase-197 remediation (bundle a self-hosted Space Grotesk woff2 + set --font-sans + await document.fonts.ready +
    recapture baselines CI-native) makes render OS-independent and lets the gallery hard-gate again. Mark SEED-006 as
    addressed/folded by Phase 197.
  </action>
  <verify>
    <automated>grep -qi 'system' .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md && grep -qi 'correct' .planning/seeds/SEED-006-admin-design-gallery-ci-baseline-recapture.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - SEED-006 has an explicit root-cause-correction section (D-07): no webfont existed; it was an OS system-font metric delta.
    - The correction notes the Phase-197 remediation and marks the seed addressed/folded.
    - The original (wrong) premise is recorded-as-corrected, not silently deleted.
  </acceptance_criteria>
  <done>SEED-006 reflects operator truth; the false-premise framing is corrected in writing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-gallery step → merge gate | Removing continue-on-error makes this step's red block merges again |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-12 | Tampering / Integrity | premature re-gate | mitigate | This plan is gated `depends_on: 197-04` (wave 3) — the hard gate is restored ONLY after CI-native baselines are recaptured, so re-gating cannot reintroduce the spurious-red the continue-on-error was masking |
| T-197-13 | Repudiation | false root-cause record | mitigate | SEED-006 is corrected in writing (D-07 operator-truth) so the historical record reflects the real OS-metric cause, not the disproven webfont-load premise |
| T-197-14 | Denial of Service | over-wide tolerance | accept | The delta is root-caused via the self-hosted font, NOT masked by widening `maxDiffPixels` — signal quality is preserved (GATE-02 equal-or-greater quality) |
</threat_model>

<verification>
- `continue-on-error` is absent from the design-gallery step (`grep` confirms).
- The design-gallery step keeps `id: design_gallery` + `if: ${{ !cancelled() }}`.
- The stale webfont-load comment is replaced with the corrected D-07 note.
- SEED-006 records the root-cause correction and is marked folded.
- Workflow parses as valid YAML; ci.yml-slicing ExUnit contract tests (`phase_58_*`) still green.
- End-to-end (zero-human-UAT): on a non-PR run, admin_design_recapture (Plan 04) recaptures + opens the
  baseline PR; once merged, example_playwright_smoke's design-gallery step runs GREEN across
  admin-design-{chromium,mobile,dark} with the font loaded — proving deterministic capture and a real hard gate.
</verification>

<success_criteria>
PW-03 complete: the admin-design gallery hard-gates again (continue-on-error removed, inline, no tolerance
widening), the root cause is fixed not masked, and SEED-006 records the operator-truth correction. Combined
with Plans 03-04, ROADMAP success criteria #3 (a brand font loads) and #4 (gallery re-gated, MG-5/6 resolved)
are met.
</success_criteria>

<output>
Create `.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-05-SUMMARY.md` when done
</output>
