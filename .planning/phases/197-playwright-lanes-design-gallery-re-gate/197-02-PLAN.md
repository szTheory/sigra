---
phase: 197-playwright-lanes-design-gallery-re-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [PW-01]
must_haves:
  truths:
    - "Every `npx playwright test` step in example_playwright_smoke carries an `id:` and `if: ${{ !cancelled() }}` so an early-step failure no longer aborts the job (D-01)"
    - "A final aggregating step (`if: always()`) fails the job if ANY guarded test step's outcome was 'failure' (D-02) — guards never silently swallow a failure"
    - "The `Stage admin checkpoint PNGs` step stages off the checkpoints step's own outcome, not cumulative job success (Pitfall 4)"
    - "The `webkit` browser install is retained (D-04 lever b is infeasible — three mobile projects use iPhone 13 = webkit)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Guarded + aggregated example_playwright_smoke test steps"
      contains: "Aggregate Playwright step outcomes"
  key_links:
    - from: ".github/workflows/ci.yml aggregator step"
      to: "each guarded test step id"
      via: "reads steps.<id>.outcome and exits 1 if any == 'failure'"
      pattern: "steps\\.[a-z_]+\\.outcome"
  prohibitions:
    - "No `continue-on-error` is added to any step in this plan (the design-gallery `continue-on-error` is removed later in Plan 05, not here)"
    - "The aggregator must NOT let any guarded step's `failure` pass — it must exit non-zero"
    - "`webkit` must NOT be dropped from `npx playwright install --with-deps chromium webkit`"
    - "The lane is NOT matrix-sharded (D-03)"
---

<objective>
PW-01 failure-surfacing: make every Playwright seam in `example_playwright_smoke` run
independently and surface all failures in a single run, then hard-gate via an aggregator.
Today the test steps carry no `if:` guard, so the first failure aborts the job and masks
the rest — the documented cause of this milestone's multiple ~25m round-trips.

Purpose: PW-01 criterion 1a (early-step failure no longer masks later steps). Criterion 1b
(critical-path time reduction) is HONESTLY modest — webkit cannot be dropped (Pitfall 2) and
the 5 launches are serial-by-design (`workers:1`, disjoint `--project` sets), so launch
consolidation yields near-zero wall-clock. The real win is reliability, not time. Do NOT
encode a time-reduction target as a must_have.
Output: 5 guarded+id'd test steps, a corrected staging guard, and one aggregator step in ci.yml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-RESEARCH.md
@.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add id + !cancelled() guard to all 5 Playwright test steps (D-01)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:973 — "Run admin behavior browser truth (chromium)" step (no `if:` today).
    - .github/workflows/ci.yml:991 — admin checkpoints step (`--project=admin-checkpoints-{chromium,mobile,dark}`).
    - .github/workflows/ci.yml:1014 — "Stage admin checkpoint PNGs" step, currently `if: success()`.
    - .github/workflows/ci.yml:1027 — "Run design gallery boards (chromium, mobile, dark)" step (carries `continue-on-error: true` at 1043 — DO NOT touch that here; Plan 05 removes it).
    - .github/workflows/ci.yml:1050 — "Run non-admin example browser smoke" step.
    - .github/workflows/ci.yml:1068 — "Run demo-showcase spec (demo-showcase-chromium)" step.
    - .github/workflows/ci.yml:937 — `npx playwright install --with-deps chromium webkit` (LEAVE UNCHANGED).
    - 197-PATTERNS.md (ci.yml D-01/D-02 guards + aggregator assignment) — exact `id:` + `if: ${{ !cancelled() }}` form and the existing in-job guard idioms (`if: success()`/`if: failure()`/`if: always()`).
  </read_first>
  <action>
    On each of the 5 `npx playwright test` steps (the steps at ci.yml ~973, ~991, ~1027, ~1050, ~1068),
    add two keys directly under the step `- name:`: a stable `id:` and `if: ${{ !cancelled() }}` (per D-01).
    Use these step ids exactly so the aggregator (Task 2) can reference them: `admin_behavior` (973),
    `admin_checkpoints` (991), `design_gallery` (1027), `non_admin_smoke` (1050), `demo_showcase` (1068).
    Do NOT alter the `run:` bodies, `working-directory`, or `env`. Do NOT add or remove `continue-on-error`
    anywhere (the design-gallery `continue-on-error: true` at 1043 stays for now — Plan 05 owns its removal).
    Do NOT change the `webkit` install at 937. Do NOT matrix-shard (D-03).
    Verify the exact step names against the file before editing (line numbers may have drifted; match by step name).
  </action>
  <verify>
    <automated>grep -cE 'if: \$\{\{ !cancelled\(\) \}\}' .github/workflows/ci.yml | awk '$1>=5{print "PASS"; exit} {print "FAIL need >=5 got " $1; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test steps carry `if: ${{ !cancelled() }}` and a unique `id:` (admin_behavior, admin_checkpoints, design_gallery, non_admin_smoke, demo_showcase).
    - `grep -n 'npx playwright install --with-deps chromium webkit' .github/workflows/ci.yml` still matches (webkit retained).
    - No `run:` body, `working-directory`, or `env` changed; no new `continue-on-error`.
  </acceptance_criteria>
  <done>Each Playwright seam runs regardless of prior-seam failure and exposes its outcome via a step id.</done>
</task>

<task type="auto">
  <name>Task 2: Add the always() aggregator + fix the staging-step guard (D-02 / Pitfall 4)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:1014 — "Stage admin checkpoint PNGs (before later runs clear test-results)" step, `if: success()` today.
    - .github/workflows/ci.yml:1077 — "Dump example app log (on failure)" step (`if: failure()`) — the aggregator must land BEFORE this so the dump still fires on a real failure.
    - .github/workflows/ci.yml:38-52 (release_ref_guard) — the in-repo `set -euo pipefail` + `exit 1` bash idiom to model the aggregator on.
    - 197-RESEARCH.md Pitfall 4 — why `if: success()` on the staging step breaks under `!cancelled()` guards
      (cumulative job status goes failure after any guarded failure, so staging skips even when checkpoints passed).
    - 197-PATTERNS.md aggregator snippet — the `for o in "${{ steps.<id>.outcome }}" …` loop that exits 1 if any == 'failure'.
  </read_first>
  <action>
    Two edits to ci.yml:
    (1) Change the "Stage admin checkpoint PNGs" step guard from `if: success()` to
    `if: ${{ steps.admin_checkpoints.outcome == 'success' }}` so it stages whenever checkpoints
    SPECIFICALLY passed, independent of unrelated seam failures (Pitfall 4).
    (2) Add a new step "Aggregate Playwright step outcomes" with `if: always()`, placed AFTER the last
    test step (demo_showcase, ~1068) and BEFORE the existing `if: failure()` log dump (~1077). Its `run:`
    body iterates the 5 guarded step outcomes — `steps.admin_behavior.outcome`, `steps.admin_checkpoints.outcome`,
    `steps.design_gallery.outcome`, `steps.non_admin_smoke.outcome`, `steps.demo_showcase.outcome` — and
    `exit 1` with `::error::` if ANY equals `failure`, else echoes success (per D-02). Use `set -euo pipefail`.
    The aggregator MUST go red on any seam failure — it must not let a guarded failure pass.
  </action>
  <verify>
    <automated>grep -q 'Aggregate Playwright step outcomes' .github/workflows/ci.yml && grep -q "steps.admin_checkpoints.outcome == 'success'" .github/workflows/ci.yml && grep -Eq 'steps\.demo_showcase\.outcome' .github/workflows/ci.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - An "Aggregate Playwright step outcomes" step exists with `if: always()` and references all 5 step outcome ids.
    - The aggregator exits non-zero (`exit 1`) when any referenced outcome is `failure`.
    - The staging step guard is `if: ${{ steps.admin_checkpoints.outcome == 'success' }}` (no longer `if: success()`).
    - The aggregator is positioned before the `if: failure()` log-dump step.
  </acceptance_criteria>
  <done>The job still hard-gates: all seams run, then the job goes red if any failed; staging is correct under the guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → merge | The example_playwright_smoke job is a required-check input; its red/green decides mergeability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-03 | Tampering / Integrity | `!cancelled()` step guards | mitigate | The guards let later seams run after an early failure; WITHOUT the aggregator they would silently turn a failing job green. The `if: always()` aggregator re-fails the job (`exit 1`) on any seam `failure` — this is the load-bearing integrity control. Block-worthy if absent. |
| T-197-04 | Repudiation | masked seam failure | mitigate | Each seam has a distinct `id`; the aggregator emits `::error::` naming the failure so the red is attributable to the right seam |
| T-197-05 | Denial of Service | runaway lane | accept | Guards add no new unbounded wait; the lane retains its existing step timeouts and serial `workers:1` execution |
</threat_model>

<verification>
- `grep -cE 'if: \$\{\{ !cancelled\(\) \}\}' .github/workflows/ci.yml` >= 5.
- The aggregator step references all 5 step outcome ids and exits 1 on any failure.
- The staging guard reads `steps.admin_checkpoints.outcome == 'success'`.
- `webkit` install unchanged (`grep -n 'chromium webkit' .github/workflows/ci.yml`).
- ExUnit CI-contract tests that slice ci.yml still pass after the edit
  (`phase_51_*` is known-red on main per RESEARCH Wave 0 / Phase-196 D-15 — confirm it is not a NEW
  regression introduced here; `phase_58_*` must stay green). Run:
  `mix test test/sigra/install/phase_58_oauth_oa01_ci_contract_test.exs` if present.
- YAML validity: the workflow parses (e.g. `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"`).
</verification>

<success_criteria>
PW-01 criterion 1a met: an early-step failure no longer masks later steps — all 5 seams run and
all failures surface in one run, and the job still hard-gates via the aggregator. Criterion 1b
recorded honestly as modestly met (webkit non-droppable; serial-by-design) in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/197-playwright-lanes-design-gallery-re-gate/197-02-SUMMARY.md` when done
</output>
