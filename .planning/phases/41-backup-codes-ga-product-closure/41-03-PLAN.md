---
phase: 41
plan: 03
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - priv/templates/sigra.install/core/mfa_settings_live.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
autonomous: true
requirements:
  - GA-01
must_haves:
  truths:
    - "Generator **`mfa_settings_live.ex`** template matches example **`regenerate_codes`** + Accounts delegate behavior."
    - "Install golden **`mfa_settings_live.ex`** stays in sync with template for diff hygiene."
  artifacts:
    - path: "priv/templates/sigra.install/core/mfa_settings_live.ex"
      provides: "Greenfield install parity for rotation"
nyquist_compliant: true
---

<objective>
Parity: update **`priv/templates/sigra.install/core/mfa_settings_live.ex`** and **`test/fixtures/install_golden/.../mfa_settings_live.ex`** so **`mix sigra.install`** outputs and golden tree match example **`Accounts`** + **`regenerate_codes`** wiring from Plan 02.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-41-06 | Tampering | Fresh installs keep TODO-only regenerate | high | Same LiveView logic as example; grep golden for absence of TODO marker. |
</threat_model>

<context>
@.planning/phases/41-backup-codes-ga-product-closure/41-CONTEXT.md
@test/example/lib/example_web/live/mfa_settings_live.ex
@priv/templates/sigra.install/core/mfa_settings_live.ex
@test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Template parity</name>
  <files>priv/templates/sigra.install/core/mfa_settings_live.ex</files>
  <read_first>
    - priv/templates/sigra.install/core/mfa_settings_live.ex
    - test/example/lib/example_web/live/mfa_settings_live.ex
  </read_first>
  <action>
    1. Port the **Plan 02** **`handle_event("regenerate_codes", ...)`** implementation from **`test/example/.../mfa_settings_live.ex`** into **`priv/templates/sigra.install/core/mfa_settings_live.ex`**, preserving EEx placeholders (**`<%= web_module %>`**, **`<%= context_module %>`**) and **`Auth`** alias style used elsewhere in the template.
    2. Ensure **`Auth.mfa_regenerate_backup_codes`** invocation uses the **same** tagged verification tuple as example.
    3. Remove any **`TODO: Wire to Auth.mfa_regenerate_backup_codes`** substring from the template.
  </action>
  <acceptance_criteria>
    - `! grep -q "TODO: Wire to Auth.mfa_regenerate_backup_codes" priv/templates/sigra.install/core/mfa_settings_live.ex` exits 0.
    - `grep -q "mfa_regenerate_backup_codes" priv/templates/sigra.install/core/mfa_settings_live.ex` exits 0.
    - `grep -q "{:totp" priv/templates/sigra.install/core/mfa_settings_live.ex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc '! grep -q "TODO: Wire to Auth.mfa_regenerate_backup_codes" /Users/jon/projects/sigra/priv/templates/sigra.install/core/mfa_settings_live.ex'</automated>
  </verify>
  <done>Install template ships real rotation handler.</done>
</task>

<task type="execute">
  <name>Task 2: Install golden mirror</name>
  <files>test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex</files>
  <read_first>
    - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex
    - priv/templates/sigra.install/core/mfa_settings_live.ex
  </read_first>
  <action>
    1. Apply the **same** functional changes to **`test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex`** (module name stays **`SigraInstallGoldenTmpWeb.MFASettingsLive`**).
    2. If **`mix sigra.fixture.rebless_golden`** (or project-documented golden refresh task) is required after edits, run it and commit resulting **`STDOUT.txt`** / fixture deltas **only** if this repo’s contributor docs say so — otherwise manual diff alignment is enough.
  </action>
  <acceptance_criteria>
    - `! grep -q "TODO: Wire to Auth.mfa_regenerate_backup_codes" test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex` exits 0.
    - `grep -q "mfa_regenerate_backup_codes" test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc '! grep -q "TODO: Wire to Auth" /Users/jon/projects/sigra/test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/live/mfa_settings_live.ex'</automated>
  </verify>
  <done>Golden fixture LiveView matches template behavior.</done>
</task>

</tasks>
