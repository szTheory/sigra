---
phase: 41
plan: 04
type: execute
wave: 3
depends_on:
  - 2
  - 3
files_modified:
  - test/example/test/example_web/smoke/backup_code_rotation_test.exs
  - docs/uat-ci-coverage.md
autonomous: true
requirements:
  - GA-01
must_haves:
  truths:
    - "Postgres-backed **`Example.DataCase`** test proves old backup plaintext works before rotation and fails verification after **`Accounts.mfa_regenerate_backup_codes`**."
    - "`docs/uat-ci-coverage.md` SEED-7 / GA-01 row points at the merge-blocking automated test path."
  artifacts:
    - path: "test/example/test/example_web/smoke/backup_code_rotation_test.exs"
      provides: "GA-01 merge-blocking proof"
nyquist_compliant: true
---

<objective>
Add the **authoritative GA-01 regression** (**`Example.DataCase`** integration) and update **`docs/uat-ci-coverage.md`** so SEED-001 item 7 traces to **`example`** unit smoke / library tests per **41-CONTEXT D-41-04**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-41-07 | Repudiation | CI green while rotation invariant untested | high | Integration test uses real **`Accounts`** + DB, not only LiveView render. |
| T-41-08 | Tampering | Test crafts hashes bypassing library | medium | Establish codes via **`Sigra.MFA.BackupCodes.generate/1`** + **`insert_all`** through **`Repo`** with same schema module as runtime **or** via **`mfa_confirm_enrollment`** fixture path documented in test. |
</threat_model>

<context>
@.planning/phases/41-backup-codes-ga-product-closure/41-CONTEXT.md
@.planning/phases/41-backup-codes-ga-product-closure/41-RESEARCH.md
@test/example/test/support/data_case.ex
@test/example/lib/example/accounts.ex
@docs/uat-ci-coverage.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: backup_code_rotation integration test</name>
  <files>test/example/test/example_web/smoke/backup_code_rotation_test.exs</files>
  <read_first>
    - test/example/test/example_web/smoke/mfa_totp_test.exs
    - test/example/test/support/data_case.ex
    - test/example/lib/example/accounts.ex
    - lib/sigra/testing.ex (setup_totp / generate_totp_code)
    - lib/sigra/mfa/backup_codes.ex
  </read_first>
  <action>
    1. Add **`test/example/test/example_web/smoke/backup_code_rotation_test.exs`** with **`use Example.DataCase`**, **`@moduletag :example_app`**, and **`import Example.AccountsFixtures`** mirroring **`mfa_totp_test.exs`**.
    2. In the happy-path test: **`{:ok, user} = Accounts.register_user(valid_user_attributes())`**, then **`Sigra.Testing.setup_totp(user, config: Accounts.sigra_config(), mfa_credential_schema: Example.Accounts.UserMFACredential, backup_code_schema: Example.Accounts.UserBackupCode)`** — capture **`backup_codes`** list and **`secret`** from the returned map.
    3. Let **`target_code = Enum.at(backup_codes, 4)`**. Assert **`{:ok, :consumed, _} = Accounts.mfa_verify_backup(user, target_code, [])`** once **before** rotation (same plaintext must have succeeded against the **pre-rotation** row set).
    4. **`totp = Sigra.Testing.generate_totp_code(secret)`** then **`assert {:ok, %{backup_codes: _new}} = Accounts.mfa_regenerate_backup_codes(user, {:totp, totp}, [])`** (adjust arity/opts to final delegate signature from Plan 02).
    5. Assert **`Accounts.mfa_verify_backup(user, target_code, [])`** returns **`{:error, :invalid_backup_code, _}`** after rotation (same plaintext string, new DB rows — grep **`def verify_backup`** in **`lib/sigra/mfa.ex`** for exact error tuple).
    6. Failure test: call **`mfa_regenerate_backup_codes`** with **`{:totp, "000000"}`** (or another guaranteed-invalid code) and assert **`{:error, _}`**; assert unused backup count unchanged vs pre-call (query via **`Sigra.MFA.BackupCodes.remaining_count/3`** or **`Accounts.mfa_status`**).
  </action>
  <acceptance_criteria>
    - `test -f test/example/test/example_web/smoke/backup_code_rotation_test.exs` exits 0.
    - `grep -q "Example.DataCase" test/example/test/example_web/smoke/backup_code_rotation_test.exs` exits 0.
    - `grep -q "mfa_regenerate_backup_codes" test/example/test/example_web/smoke/backup_code_rotation_test.exs` exits 0.
    - `grep -q "setup_totp" test/example/test/example_web/smoke/backup_code_rotation_test.exs` exits 0.
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/smoke/backup_code_rotation_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f /Users/jon/projects/sigra/test/example/test/example_web/smoke/backup_code_rotation_test.exs'</automated>
  </verify>
  <done>GA-01 cryptographic proof lives in example test suite.</done>
</task>

<task type="execute">
  <name>Task 2: uat-ci-coverage.md SEED-7 pointer</name>
  <files>docs/uat-ci-coverage.md</files>
  <read_first>
    - docs/uat-ci-coverage.md
    - .planning/phases/41-backup-codes-ga-product-closure/41-CONTEXT.md
  </read_first>
  <action>
    1. Locate **SEED-7** / backup rotation / **GA-01** row (grep **`SEED-7`** or **`GA-01`**).
    2. Update prose so the **primary automated closure** references **`test/example/test/example_web/smoke/backup_code_rotation_test.exs`** and the CI job that executes **`test/example`** tests — grep **`.github/workflows/*.yml`** for **`test/example`** / **`mix test`** and cite **that** job `name:` (or job id) verbatim.
    3. Narrow Playwright text to UX smoke only if doc currently over-claims browser proof for crypto.
  </action>
  <acceptance_criteria>
    - `grep -q "backup_code_rotation" docs/uat-ci-coverage.md` exits 0 **OR** `grep -q "rotation_test" docs/uat-ci-coverage.md` exits 0.
    - `grep -qi "GA-01\\|SEED-7" docs/uat-ci-coverage.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -Ei "GA-01|SEED-7" /Users/jon/projects/sigra/docs/uat-ci-coverage.md | head -1 | wc -l | grep -q "[1-9]"'</automated>
  </verify>
  <done>Documentation traces GA-01 to merge-blocking test.</done>
</task>

</tasks>
