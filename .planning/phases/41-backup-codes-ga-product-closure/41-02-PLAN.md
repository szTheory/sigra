---
phase: 41
plan: 02
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - test/example/lib/example/accounts.ex
  - test/example/lib/example_web/live/mfa_settings_live.ex
  - test/example/lib/example_web/router.ex
autonomous: true
requirements:
  - GA-01
must_haves:
  truths:
    - "`Example.Accounts` exposes `mfa_regenerate_backup_codes/3` delegating into `Sigra.MFA.regenerate_backup_codes/4` with merged MFA opts."
    - "`MFASettingsLive` regenerate event calls the delegate and maps `{:ok, %{backup_codes: _}}` to the same UX path as enrollment (no false success flash)."
    - "`live \"/settings/mfa\", MFASettingsLive` is registered only under `pipe_through` including `:require_sudo` (matches install golden)."
  artifacts:
    - path: "test/example/lib/example_web/router.ex"
      provides: "Sudo-gated MFA settings route"
nyquist_compliant: true
---

<objective>
Wire **example app**: **`Accounts`** delegate, **`MFASettingsLive`** regenerate handler + flashes, and **router** sudo alignment for **`/users/settings/mfa`** — closing the TODO stub and D-41-01 layered step-up for the reference host.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-41-04 | Elevation | MFA settings reachable without sudo while passkey admin requires sudo | medium | Move **`MFASettingsLive`** route to **`require_sudo`** pipeline matching golden. |
| T-41-05 | Spoofing / UX | User told codes rotated when DB unchanged | high | Branch on **`{:ok, _}`** vs **`{:error, _}`**; **`put_flash(:info, ...)`** only on real success. |
</threat_model>

<context>
@.planning/phases/41-backup-codes-ga-product-closure/41-CONTEXT.md
@.planning/phases/41-backup-codes-ga-product-closure/41-PATTERNS.md
@test/example/lib/example/accounts.ex
@test/example/lib/example_web/live/mfa_settings_live.ex
@test/example/lib/example_web/router.ex
@test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Accounts delegate</name>
  <files>test/example/lib/example/accounts.ex</files>
  <read_first>
    - test/example/lib/example/accounts.ex
    - lib/sigra/mfa.ex
  </read_first>
  <action>
    1. Add **`def mfa_regenerate_backup_codes(user, verification, opts \\ [])`** (final arity may be **`/2`** if opts empty — must match LiveView call site).
    2. Body calls **`Sigra.MFA.regenerate_backup_codes(sigra_config(), user, verification, Keyword.merge([mfa_credential_schema: UserMFACredential, backup_code_schema: UserBackupCode], opts))`** — mirror **`mfa_disable/2`** option merge style **exactly** (grep **`mfa_disable`** for pattern).
    3. **`@doc`** states: requires **`{:totp, code}`**; backup codes **cannot** authorize rotation.
  </action>
  <acceptance_criteria>
    - `grep -q "def mfa_regenerate_backup_codes" test/example/lib/example/accounts.ex` exits 0.
    - `grep -q "regenerate_backup_codes" test/example/lib/example/accounts.ex` exits 0.
    - `grep -q "UserMFACredential" test/example/lib/example/accounts.ex` exits 0 in the new function region.
    - `mix compile --warnings-as-errors` exits 0 inside **`test/example`** (`cd test/example && mix compile --warnings-as-errors`).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd /Users/jon/projects/sigra/test/example && mix compile --warnings-as-errors'</automated>
  </verify>
  <done>Example Accounts exposes rotation delegate.</done>
</task>

<task type="execute">
  <name>Task 2: Router sudo alignment</name>
  <files>test/example/lib/example_web/router.ex</files>
  <read_first>
    - test/example/lib/example_web/router.ex
    - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex
  </read_first>
  <action>
    1. Remove **`live "/settings/mfa", MFASettingsLive"`** from the **`pipe_through [:browser, :require_authenticated]`**-only **`live_session :require_authenticated`** block.
    2. Add a **`scope "/users", ExampleWeb do`** (or equivalent) with **`pipe_through [:browser, :require_authenticated, :require_sudo]`** and a **`live_session`** that mounts **`MFASettingsLive`** at **`live "/settings/mfa", MFASettingsLive"`** — structurally mirror **`sigra_install_golden_tmp_web/router.ex`** lines **117–122** (sudo before MFA settings).
    3. Preserve existing **`on_mount`** hooks used by the removed route (copy **`ensure_authenticated`** / scope hooks from the old session so behavior matches except sudo).
  </action>
  <acceptance_criteria>
    - `grep -B15 'live "/settings/mfa"' test/example/lib/example_web/router.ex | grep -q ":require_sudo"` exits 0.
    - `cd test/example && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -B12 "live \"/settings/mfa\"" /Users/jon/projects/sigra/test/example/lib/example_web/router.ex | grep -q ":require_sudo"'</automated>
  </verify>
  <done>MFA settings LiveView requires sudo pipeline in example router.</done>
</task>

<task type="execute">
  <name>Task 3: MFASettingsLive regenerate_codes event</name>
  <files>test/example/lib/example_web/live/mfa_settings_live.ex</files>
  <read_first>
    - test/example/lib/example_web/live/mfa_settings_live.ex
    - test/example/lib/example/accounts.ex
  </read_first>
  <action>
    1. Replace the **`# TODO: Wire to Auth.mfa_regenerate_backup_codes`** block in **`handle_event("regenerate_codes", ...)`** with a real call: **`Auth.mfa_regenerate_backup_codes(user, {:totp, code})`** (adjust module alias **`Auth`** vs **`Example.Accounts`** to match file).
    2. **`{:ok, %{backup_codes: codes}}`**: assign **`backup_codes: codes`**, **`enrollment_step: :backup_codes`**, **`codes_acknowledged: false`**, **`show_regenerate: false`**, refresh **`backup_remaining`** via **`Auth.mfa_status(user)`**, **`put_flash(:info, ...)`** with a message that states replacement (must contain substring **`regenerated`** or **`new backup`** — pick one string and keep consistent).
    3. **`{:error, :invalid_code, remaining}`** (and similar): **`put_flash(:error, ...)`** with remaining-attempts hint if available; **do not** claim success.
    4. **`{:error, :lockout, _}`**: error flash referencing lockout (reuse wording pattern from disable handler in same file).
    5. Remove stale TODO comment lines containing **`Wire to Auth.mfa_regenerate_backup_codes`**.
  </action>
  <acceptance_criteria>
    - `! grep -q "TODO: Wire to Auth.mfa_regenerate_backup_codes" test/example/lib/example_web/live/mfa_settings_live.ex` exits 0.
    - `grep -q "mfa_regenerate_backup_codes" test/example/lib/example_web/live/mfa_settings_live.ex` exits 0.
    - `grep -q "{:totp" test/example/lib/example_web/live/mfa_settings_live.ex` exits 0.
    - `grep -q "put_flash(:error" test/example/lib/example_web/live/mfa_settings_live.ex` exits 0 (file already has error flashes elsewhere — ensure regenerate path includes `:error` flash branch; if combined in one line use `grep -E "put_flash\\(:error"`).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc '! grep -q "TODO: Wire to Auth.mfa_regenerate_backup_codes" /Users/jon/projects/sigra/test/example/lib/example_web/live/mfa_settings_live.ex'</automated>
  </verify>
  <done>Example LiveView performs real rotation with honest error handling.</done>
</task>

</tasks>
