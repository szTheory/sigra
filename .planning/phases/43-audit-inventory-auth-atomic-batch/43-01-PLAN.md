---
phase: 43
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
  - CHANGELOG.md
autonomous: true
requirements:
  - AUD-04
must_haves:
  truths:
    - "Checked-in inventory lists every `Sigra.Audit.log_safe/3` site under `lib/sigra/auth.ex` plus `session.*` emissions from Auth session helpers with stable `AUD-04-NNN` IDs."
    - "Each row has AUD-05 batch tag (B1/B2/B3), deferral to phase 44/45, or numbered exclusion with D-01/C-1 traceability per D-43-03."
  artifacts:
    - path: ".planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md"
      provides: "Authoritative AUD-04 inventory + exclusions appendix"
nyquist_compliant: true
---

<objective>
Deliver **AUD-04**: checked-in **`43-AUD-04-INVENTORY.md`** under the phase directory with prioritized `log_safe` / hybrid **Auth** integration boundaries, **AUD-05 batch tags**, forward pointers to phases **44–45**, and **CHANGELOG.md** link per **D-43-01**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-43-01 | Repudiation | Silent omission of a `log_safe` site | high | Task 1 runs `rg` and pastes path:line evidence into inventory appendix |
| T-43-02 | Information disclosure | Inventory rows leak PII samples | low | Rows reference **action strings** and module boundaries only — no live user data |
</threat_model>

<context>
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-CONTEXT.md
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Ground-truth grep + Auth row table</name>
  <files>.planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md</files>
  <read_first>
    - lib/sigra/auth.ex
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-CONTEXT.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-RESEARCH.md
  </read_first>
  <action>
    1. Run **`rg -n "Audit\\.log_safe|Sigra\\.Audit\\.log_safe" lib/sigra/auth.ex`** (record date in inventory header).
    2. Run **`rg -n "Sigra\\.Audit\\.log_safe" lib/sigra/auth.ex`** and ensure **every** match is represented by **exactly one** inventory row (merge comment-only duplicates with the implementation row).
    3. Run **`rg -n "Sigra\\.Audit\\.log_safe\\(" lib/sigra/auth.ex`** for `session.create`, `session.delete`, `session.revoke_all`, `session.sudo_enter` / `session.sudo_expire` — include each as its own **`AUD-04-NNN`** row (module **`Sigra.Auth`** / boundary **session helper**).
    4. For rows already **`__log_internal__`** (confirm link/code, `auth.password_reset_complete`), mark **Mechanism** = `Multi (__log_internal__)` and **AUD-05 tag** = `— (done)` — must match comments ~lines 248–262 in `auth.ex`.
    5. Assign **Priority tier 1–9** per **D-43-02** in `43-CONTEXT.md` and **AUD-05 batch** tags: **`B1`** = registration; **`B2`** = magic link + password reset request; **`B3`** = authenticate / lockout / invalid_credentials / login success + session cluster; **`defer-44`** / **`defer-45`** for non-Auth `lib/sigra/*` hits (if any appear in grep for this file, still list with defer tag).
    6. Create **`.planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md`** with sections: `# AUD-04 Inventory`, `## Priority table` (markdown table: **ID | Boundary | action | mechanism | tier | AUD-05 tag | notes**), `## Exclusions appendix` (empty or rows with REQ + owner per D-43-03), `## Grep log` ( fenced block with verbatim `rg` output ).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md` exits 0.
    - `grep -q "AUD-04-" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md` exits 0.
    - `grep -q "auth.password_reset_complete" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md` exits 0 (done / Multi row documented).
    - `grep -q "B1" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md` exits 0.
    - `grep -q "Grep log" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md && grep -q "AUD-04-" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md'</automated>
  </verify>
  <done>Inventory file exists with stable IDs and batch tags.</done>
</task>

<task type="execute">
  <name>Task 2: CHANGELOG pointer</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
  </read_first>
  <action>
    1. Under the **Unreleased** / **v1.4** (whichever exists) section, add one bullet that links **`43-AUD-04-INVENTORY.md`** with relative path **`.planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md`** and mentions **AUD-04** inventory for `log_safe` → Multi migration waves.
  </action>
  <acceptance_criteria>
    - `grep -q "43-AUD-04-INVENTORY.md" CHANGELOG.md` exits 0.
    - `grep -q "AUD-04" CHANGELOG.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "43-AUD-04-INVENTORY.md" CHANGELOG.md'</automated>
  </verify>
  <done>GA readers can discover inventory from CHANGELOG.</done>
</task>

</tasks>

<verification>
- `mix format --check-formatted` passes for touched Elixir files (none expected) OR skip if no `.ex` changes.
- `CHANGELOG.md` and inventory markdown readable on GitHub.
</verification>

<success_criteria>
- **AUD-04** satisfied: inventory artifact + link; batch tags align with `43-02`/`43-03`/`43-04` plans.
</success_criteria>
