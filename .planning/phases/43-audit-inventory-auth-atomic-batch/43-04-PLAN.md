---
phase: 43
plan: 04
type: execute
wave: 2
depends_on:
  - "03"
files_modified:
  - lib/sigra/auth.ex
  - test/sigra/auth/login_and_lockout_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-05
must_haves:
  truths:
    - "Inventory B3 rows document which audits moved into `Ecto.Multi` vs remain `log_safe` with rationale (session store / fail-open selector)."
    - "At least one new audit atomicity test covers a converted B3 boundary (prefer `auth.login.success` + Repo-backed user/lockout fields in one transaction OR lockout threshold path)."
  artifacts:
    - path: "test/sigra/auth/login_and_lockout_audit_atomicity_test.exs"
      provides: "Proof for chosen B3 conversion"
nyquist_compliant: true
---

<objective>
Execute **AUD-05 B3** from **`43-AUD-04-INVENTORY.md`**: tighten **Repo-backed** portions of **`authenticate_with_config/2`** and related **lockout / invalid_credentials** audits toward **`Ecto.Multi`** where **`43-RESEARCH.md`** says feasible; **`session.create`** remains documented hybrid unless SessionStore composes into Multi in this phase.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-43-03 | Broken auth | Login returns wrong tuple if Multi ordering wrong | high | Preserve exact `{:ok, user, meta}` / error atoms; extend existing auth tests |
| T-43-05 | Denial of service | Extra transaction contention | low | Keep Multi minimal — only user/audit steps |
</threat_model>

<context>
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-RESEARCH.md
@lib/sigra/auth.ex
@lib/sigra/lockout.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Inventory alignment + scope cut line</name>
  <files>.planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md</files>
  <read_first>
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
    - lib/sigra/auth.ex
  </read_first>
  <action>
    1. Amend **`43-AUD-04-INVENTORY.md`** B3 rows with a **“Scope cut (Plan 04)”** note: list which actions **this plan converts in code** vs **explicit hybrid deferral** (must cite **`43-RESEARCH.md`** SessionStore limitation for **`session.create`** if unchanged).
    2. Ensure **`auth.login.failure`** / bare attempt rows match **D-43-02 tier 9** — if still `log_safe`, exclusion row references **AUD-08** / intentional hybrid.
  </action>
  <acceptance_criteria>
    - `grep -q "Scope cut" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md` exits 0.
    - `grep -q "B3" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -q "Scope cut" .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md'</automated>
  </verify>
  <done>Executors have explicit in/out scope for B3.</done>
</task>

<task type="execute">
  <name>Task 2: Implement feasible B3 Multi conversions</name>
  <files>lib/sigra/auth.ex</files>
  <read_first>
    - lib/sigra/auth.ex
    - lib/sigra/lockout.ex
    - lib/sigra/audit.ex
  </read_first>
  <action>
    1. Per amended inventory, refactor **`authenticate_with_config/2`** success branch so **`auth.login.success`** audit + **Repo-backed** updates that already run as separate **`repo.update`** / **`Lockout.reset!`** participate in **one `repo.transaction`** **where** the code path is already sequential Ecto operations **without** SessionStore participation.
    2. For **`handle_failed_login_with_lockout/5`**, evaluate promoting **`security.invalid_credentials`** + user counter update into shared transaction **only if** `Lockout.increment!` is Repo-backed in-process and audit must not outlive rollback — **if not feasible without Lockout API change**, document **hybrid** in inventory and add compensating test per D-43-04.
    3. **Do not** change **`create_session`** / **`maybe_assign_active_organization`** contract in this task unless inventory marks them in-scope and tests exist — default **leave `session.create` log_safe`** with explicit inventory rationale.
  </action>
  <acceptance_criteria>
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth_test.exs` exits 0.
    - `MIX_ENV=test mix compile` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'MIX_ENV=test mix compile'</automated>
  </verify>
  <done>B3 code changes match amended inventory.</done>
</task>

<task type="execute">
  <name>Task 3: login_and_lockout_audit_atomicity_test</name>
  <files>test/sigra/auth/login_and_lockout_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/auth/register_audit_atomicity_test.exs
    - test/sigra/api_token_audit_atomic_test.exs
    - lib/sigra/auth.ex
  </read_first>
  <action>
    1. Add **`test/sigra/auth/login_and_lockout_audit_atomicity_test.exs`** proving **at least one** converted B3 property (document which in moduledoc): e.g. **`auth.login.success`** row exists iff user+lockout reset transaction commits.
    2. Use **`Sigra.Config`** builder patterns from existing tests if any (`grep -R "Sigra.Config" test/sigra | head`); otherwise construct minimal config map per **`authenticate/3`** tests in repo.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/auth/login_and_lockout_audit_atomicity_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/login_and_lockout_audit_atomicity_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/login_and_lockout_audit_atomicity_test.exs'</automated>
  </verify>
  <done>B3 has automated atomicity proof.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test` (full suite) green before merge to main.
</verification>

<success_criteria>
- **B3** rows reconciled: each either **converted + tested** or **documented hybrid** with owner + trigger per D-43-03.
</success_criteria>
