---
phase: 43
plan: 03
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - lib/sigra/auth.ex
  - test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-05
must_haves:
  truths:
    - "`auth.magic_link_request`, `auth.magic_link_verify.success`, and `auth.password_reset_request` audits commit in the same transaction as their token persistence when audit_schema enabled (per inventory B2 rows)."
  artifacts:
    - path: "test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs"
      provides: "DB proofs for B2 boundaries"
nyquist_compliant: true
---

<objective>
Execute **AUD-05 B2** rows from **`43-AUD-04-INVENTORY.md`**: refactor **`request_magic_link/3`**, **`verify_magic_link/3`**, and **`request_password_reset/4`** so token writes + audit use **`Ecto.Multi`** + **`log_multi_safe`/`__log_internal__`** + **`emit_telemetry_from_changes`**, mirroring **`reset_password/4`**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-43-02 | Repudiation | Token row without matching audit | high | Single `repo.transaction` per success path |
| T-43-04 | Elevation | Magic link verify confirms user but audit missing | medium | Assert `auth.magic_link_verify.success` on happy path test |
</threat_model>

<context>
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
@lib/sigra/auth.ex
@lib/sigra/audit.ex
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-PATTERNS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: request_magic_link Multi</name>
  <files>lib/sigra/auth.ex</files>
  <read_first>
    - lib/sigra/auth.ex
    - lib/sigra/audit.ex
  </read_first>
  <action>
    1. Replace **`repo.insert!(token_struct)`** + trailing **`Audit.log_safe("auth.magic_link_request", ...)`** (~533–545) with **`Ecto.Multi.new()`** inserting **`:token`** then **`Sigra.Audit.log_multi_safe(multi, "auth.magic_link_request", audit_opts)`** (or **`__log_internal__`**) with resolver reading **user id** from inserted token / user struct per existing metadata.
    2. Use **`repo.transaction`** or **`repo.transact`** consistent with file conventions; on **`{:ok, changes}`** call **`Sigra.Audit.emit_telemetry_from_changes/1`** when audit present.
    3. Preserve **`{:ok, :sent}`**, **`{:error, :rate_limited}`**, and timing-safe dummy hash paths **without** structural regression.
  </action>
  <acceptance_criteria>
    - `grep -n "request_magic_link" lib/sigra/auth.ex | head -1` shows file still defines function.
    - `grep -q "auth.magic_link_request" lib/sigra/auth.ex` exits 0.
    - `grep -q "Ecto.Multi" lib/sigra/auth.ex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'MIX_ENV=test mix compile'</automated>
  </verify>
  <done>Magic link request path uses Multi+audit when DB write occurs.</done>
</task>

<task type="execute">
  <name>Task 2: verify_magic_link Multi</name>
  <files>lib/sigra/auth.ex</files>
  <read_first>
    - lib/sigra/auth.ex
    - lib/sigra/audit.ex
  </read_first>
  <action>
    1. Wrap **`repo.delete!(token_record)`**, **`maybe_confirm_user`** updates, and **`Audit.log_safe("auth.magic_link_verify.success", ...)`** (~595–616) into a **single `Ecto.Multi`** (use **`Multi.delete`**, **`Multi.update`** / **`Multi.run`** as needed) with **`log_multi_safe`** for **`"auth.magic_link_verify.success"`** using **`actor_resolver`** from updated user.
    2. Preserve **`{:error, :invalid}`** / **`{:error, :expired}`** behavior without inserting audits.
    3. Emit telemetry from changes on success branch only.
  </action>
  <acceptance_criteria>
    - `grep -q "auth.magic_link_verify.success" lib/sigra/auth.ex` exits 0.
    - No standalone `Audit.log_safe("auth.magic_link_verify.success"` remains (use `rg 'log_safe\\(\"auth.magic_link_verify'` — expect no match OR only in comments).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'MIX_ENV=test mix compile'</automated>
  </verify>
  <done>Magic link verify audit is transactional.</done>
</task>

<task type="execute">
  <name>Task 3: request_password_reset Multi</name>
  <files>lib/sigra/auth.ex</files>
  <read_first>
    - lib/sigra/auth.ex
    - lib/sigra/audit.ex
  </read_first>
  <action>
    1. Replace **`repo.insert!(token_struct)`** + **`Audit.log_safe("auth.password_reset_request", ...)`** (~952–966) with **`Ecto.Multi`** + **`log_multi_safe`** for **`"auth.password_reset_request"`**.
    2. Keep enumeration-safe branches (`{:ok, :sent}`, dummy hash) **audit-free** as today.
    3. **`emit_telemetry_from_changes`** on success.
  </action>
  <acceptance_criteria>
    - `grep -q "auth.password_reset_request" lib/sigra/auth.ex` exits 0.
    - `MIX_ENV=test mix compile` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'MIX_ENV=test mix compile'</automated>
  </verify>
  <done>Password reset request audit matches token insert transaction.</done>
</task>

<task type="execute">
  <name>Task 4: B2 atomicity tests</name>
  <files>test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/api_token_audit_atomic_test.exs
    - test/sigra/auth/register_audit_atomicity_test.exs
    - lib/sigra/auth.ex
  </read_first>
  <action>
    1. Create **`test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs`** covering at least **one** happy-path assertion per converted action (**`auth.magic_link_request`**, **`auth.magic_link_verify.success`**, **`auth.password_reset_request`**) with audit_schema enabled — reuse minimal schemas / repos from sibling test if possible.
    2. Include **one rollback / no-audit-on-abort** case across the three (minimum bar per **43-VALIDATION.md**).
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/magic_link_and_reset_request_audit_atomicity_test.exs'</automated>
  </verify>
  <done>B2 paths have audit-aware proofs.</done>
</task>

</tasks>

<verification>
- Scoped test file passes on Postgres.
- No new credo violations in `lib/sigra/auth.ex` beyond baseline (run `mix credo lib/sigra/auth.ex` if credo is standard).
</verification>

<success_criteria>
- **B2** inventory rows marked done with test evidence.
</success_criteria>
