---
phase: 43
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/sigra/auth.ex
  - test/sigra/auth/register_audit_atomicity_test.exs
autonomous: true
requirements:
  - AUD-05
must_haves:
  truths:
    - "`auth.register.success` audit row is inserted in the same Repo transaction as `:user` insert when audit_schema enabled."
    - "Public `register/3` return shape unchanged for {:ok, user}, {:error, changeset}, {:error, :email_taken}."
  artifacts:
    - path: "test/sigra/auth/register_audit_atomicity_test.exs"
      provides: "Rollback proof for registration + audit"
nyquist_compliant: true
---

<objective>
Execute **AUD-05 B1** from **`43-AUD-04-INVENTORY.md`**: move **`auth.register.success`** (and failure rows where still accurate) onto the **`register_user_multi`** transaction using **`Sigra.Audit.log_multi_safe/3`** or **`__log_internal__/3`**, following **`reset_password/4`** telemetry pattern.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-43-02 | Repudiation | Audit row without user row | high | Multi transaction + rollback test |
| T-43-03 | Tampering | `{:ok, user}` if audit disabled but user rolled back | medium | Test both audit_schema nil (no-op) and enabled branches |
</threat_model>

<context>
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-CONTEXT.md
@.planning/phases/43-audit-inventory-auth-atomic-batch/43-PATTERNS.md
@lib/sigra/audit.ex
</context>

<tasks>

<task type="execute">
  <name>Task 1: Multi-compose registration audit</name>
  <files>lib/sigra/auth.ex</files>
  <read_first>
    - lib/sigra/auth.ex
    - lib/sigra/audit.ex
    - .planning/phases/43-audit-inventory-auth-atomic-batch/43-AUD-04-INVENTORY.md
  </read_first>
  <action>
    1. Extend **`register/3`** so that when **`audit_schema`** is present in **`audit_opts`**, the **`register_user_multi(attrs, opts)`** pipeline appends an **`:audit_register_success`** step (name is example — pick one stable atom) using **`Sigra.Audit.log_multi_safe/3`** OR **`Sigra.Audit.__log_internal__/3`** with action **`"auth.register.success"`**, **`actor_resolver`** resolving **`user.id`** from **`changes.user`**, and metadata **`%{method: "password"}`** matching pre-change inventory row.
    2. On **`{:ok, changes}`** from **`repo.transact()`**, call **`Sigra.Audit.emit_telemetry_from_changes/1`** when the changes map includes the audit key (mirror **`reset_password/4`** ~1048–1051).
    3. Remove the post-success standalone **`Audit.log_safe("auth.register.success", ...)`** call that ran after transact (~lines 161–169) when audit is enabled; keep **`log_safe`** failure paths **OR** move failure audits only if changeset context is available inside Multi **`Multi.run`** without breaking enumeration-safe `{:error, :email_taken}` — **do not** regress **`email_taken`** vs validation error behavior.
    4. Update moduledoc / comment near **`register/3`** to state **atomic audit when enabled**.
  </action>
  <acceptance_criteria>
    - `bash -lc 'grep -A140 "^  def register(" lib/sigra/auth.ex | grep -qE "log_multi_safe|__log_internal__"'` exits 0.
    - `grep -q "auth.register.success" lib/sigra/auth.ex` exits 0.
    - `bash -lc 'grep -A140 "^  def register(" lib/sigra/auth.ex | grep -q emit_telemetry_from_changes'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'MIX_ENV=test mix compile'</automated>
  </verify>
  <done>Registration success audit is Multi-anchored when audit enabled.</done>
</task>

<task type="execute">
  <name>Task 2: register_audit_atomicity_test</name>
  <files>test/sigra/auth/register_audit_atomicity_test.exs</files>
  <read_first>
    - test/sigra/api_token_audit_atomic_test.exs
    - test/support/postgres_test_repo.ex
    - lib/sigra/audit/assertions.ex
    - lib/sigra/auth.ex
  </read_first>
  <action>
    1. Add **`test/sigra/auth/register_audit_atomicity_test.exs`** with **`async: false`**, minimal **`users`** + **`audit_events`** DDL (uuid ids if matching project conventions), using **`Sigra.Test.PostgresRepo`** pattern from **`api_token_audit_atomic_test.exs`**.
    2. **Happy path:** `Sigra.Auth.register(repo, attrs, user_schema: ..., changeset_fn: ..., audit_schema: Sigra.Test.AuditEvent, repo: repo, ...)` — assert **≥1** audit row with **`action == "auth.register.success"`** and **`target_id == user.id`** using **`Sigra.Audit.Assertions.assert_audit_fields/3`** OR ordered **`Repo.all`**.
    3. **Rollback path:** Force **`Multi`** to fail **after** user insert (e.g. inject **`Multi.run`** that returns `{:error, :forced}` in a **test-only** code path **OR** use constraint violation second step) — prove **no** `auth.register.success` row when transaction aborts (adjust strategy to what minimal diff allows; if injection is too invasive, use **invalid changeset** path proving no success audit row).
    4. Tag file must **`use ExUnit.Case, async: false`**.
  </action>
  <acceptance_criteria>
    - `test -f test/sigra/auth/register_audit_atomicity_test.exs` exits 0.
    - `grep -q "register_audit_atomicity" test/sigra/auth/register_audit_atomicity_test.exs` exits 0 OR `grep -q "Sigra.Auth.register" test/sigra/auth/register_audit_atomicity_test.exs` exits 0.
    - `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/register_audit_atomicity_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/register_audit_atomicity_test.exs'</automated>
  </verify>
  <done>Atomicity test guards registration audit.</done>
</task>

</tasks>

<verification>
- `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/auth/register_audit_atomicity_test.exs` exits 0.
- `MIX_ENV=test mix compile --warnings-as-errors` if project default uses it; else `mix compile`.
</verification>

<success_criteria>
- Inventory **B1** rows for **`auth.register.*`** marked **done** with evidence PR in follow-up commit message.
</success_criteria>
