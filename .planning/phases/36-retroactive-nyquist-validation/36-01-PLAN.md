---
phase: 36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-retroactive-nyquist-validation/36-INVENTORY.md
  - .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - "`36-INVENTORY.md` lists every `.planning/phases/*` directory (except transient junk) with validation_file + classification."
    - "`scripts/regen-inventory.sh` exists, is executable, and emits a stable TSV classification stream for re-audit."
  artifacts:
    - path: ".planning/phases/36-retroactive-nyquist-validation/36-INVENTORY.md"
      provides: "VAL-01 inventory table + regen instructions"
nyquist_compliant: true
---

<objective>
Satisfy **VAL-01**: a durable inventory of phase directories versus `*-VALIDATION.md` presence and coarse Nyquist/readiness classification, plus a regen script for future drift checks.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-36-01 | Tampering | Inventory omits dirs to hide debt | low | Regen script enumerates all `phases/*` |
| T-36-02 | Repudiation | Stale inventory after new phase dirs | low | Document regen command in inventory header |
</threat_model>

<context>
@.planning/phases/36-retroactive-nyquist-validation/36-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Harden regen-inventory.sh + reconcile 36-INVENTORY.md</name>
  <files>.planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh,.planning/phases/36-retroactive-nyquist-validation/36-INVENTORY.md</files>
  <read_first>
    - .planning/phases/36-retroactive-nyquist-validation/36-INVENTORY.md
    - .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh
    - .planning/phases/31-automation-first-verification/31-VALIDATION.md (Nyquist table shape reference)
  </read_first>
  <action>
    1. Ensure `scripts/regen-inventory.sh` starts with `#!/usr/bin/env bash`, uses `set -euo pipefail`, resolves repo root via `git rev-parse --show-toplevel` (fallback `pwd`), and classifies each `.planning/phases/*/` directory using the same rules already encoded in the script (MISSING vs COMPLIANT_APPROVED vs COMPLIANT_DRAFT_STATUS vs LEGACY_FORMAT when `Validation Architecture` appears without standard `status: approved`).
    2. Run `bash .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh | wc -l` — record the line count in a one-line HTML comment at the bottom of `36-INVENTORY.md` as `<!-- regen_lines: N -->` after reconciling.
    3. Update `36-INVENTORY.md` **Summary counts** row integers so they match the regen TSV when interpreted: count MISSING rows, count rows where column 3 is `COMPLIANT_DRAFT_STATUS`, `COMPLIANT_APPROVED`, `LEGACY_FORMAT`, `REVIEW`, `DRAFT_OR_FALSE` respectively. If any phase_dir from `find .planning/phases -maxdepth 1 -mindepth 1 -type d` is missing from the markdown table, add it.
    4. Keep the **Regenerate** fenced block pointing at exactly: `bash .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh`
  </action>
  <acceptance_criteria>
    - `test -x .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh` exits 0.
    - `bash .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh | head -1` produces a line containing a tab character (TSV).
    - `grep -q "10.1.1-example-app-repair-ci-install-usage-smoke-harness" .planning/phases/36-retroactive-nyquist-validation/36-INVENTORY.md` exits 0.
    - `grep -q "999.2-dependabot-major-version-bumps" .planning/phases/36-retroactive-nyquist-validation/36-INVENTORY.md` exits 0.
    - `grep -q "regen-inventory.sh" .planning/phases/36-retroactive-nyquist-validation/36-INVENTORY.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test "$(bash .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh | wc -l | tr -d " ")" -ge 20'</automated>
  </verify>
  <done>VAL-01 inventory + regen path are internally consistent.</done>
</task>

</tasks>

<verification>
- `bash -lc 'test "$(bash .planning/phases/36-retroactive-nyquist-validation/scripts/regen-inventory.sh | wc -l | tr -d " ")" -ge 20'` exits 0
</verification>

<success_criteria>
- VAL-01 satisfied: inventory + regen script prove no silent omission of known phase roots (≥20 phase dirs today).
</success_criteria>
