---
phase: 143-playwright-demo-spec-screenshots
plan: "02"
type: execute
wave: 2
depends_on:
  - "143-01"
files_modified:
  - test/example/priv/playwright/playwright.config.ts
  - test/example/priv/playwright/tests/demo-showcase.spec.ts
  - test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/demo-credentials-demo-showcase-chromium.png
  - test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/admin-user-list-demo-showcase-chromium.png
  - test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/admin-user-detail-demo-showcase-chromium.png
  - test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/audit-explorer-demo-showcase-chromium.png
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PW-01
  - PW-02

must_haves:
  truths:
    - "A demo-showcase-chromium Playwright project partition exists, runs the demo-showcase.spec.ts, and is excluded from the chromium and mobile projects via testIgnore"
    - "The spec asserts each of the 6 seeded personas via data-testid and structural email-based locators (not display-name text)"
    - "Four committed PNG baselines exist under tests/demo-showcase.spec.ts-snapshots/ with the correct {slug}-demo-showcase-chromium.png naming"
    - "The spec completes the TOTP challenge using otplib before accessing admin routes (MFAChallengeLive defaults to passkey panel; spec clicks show_totp first)"
    - "CI adds a demo-showcase run step that invokes npx playwright test --project=demo-showcase-chromium"
  artifacts:
    - path: "test/example/priv/playwright/playwright.config.ts"
      provides: "DEMO_SHOWCASE_SPEC constant + demo-showcase-chromium project + testIgnore entries on chromium and mobile"
      contains: "DEMO_SHOWCASE_SPEC"
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts"
      provides: "Full demo-showcase spec: credentials page assertions, admin login with TOTP challenge, admin user list, admin user detail, audit explorer, 4 screenshot captures"
      exports: []
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/demo-credentials-demo-showcase-chromium.png"
      provides: "Committed baseline for /demo/credentials screenshot"
      min_lines: 1
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/admin-user-list-demo-showcase-chromium.png"
      provides: "Committed baseline for /admin/users showing 6 demo personas"
      min_lines: 1
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/admin-user-detail-demo-showcase-chromium.png"
      provides: "Committed baseline for /admin/users/{admin-id} showing MFA + passkey rows"
      min_lines: 1
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/audit-explorer-demo-showcase-chromium.png"
      provides: "Committed baseline for /admin/audit showing event variety"
      min_lines: 1
  key_links:
    - from: "playwright.config.ts"
      to: "tests/demo-showcase.spec.ts"
      via: "testMatch: DEMO_SHOWCASE_SPEC in demo-showcase-chromium project"
      pattern: "DEMO_SHOWCASE_SPEC"
    - from: "playwright.config.ts"
      to: "chromium and mobile projects"
      via: "testIgnore: [..., DEMO_SHOWCASE_SPEC]"
      pattern: "DEMO_SHOWCASE_SPEC"
    - from: "tests/demo-showcase.spec.ts"
      to: "tests/demo-showcase.spec.ts-snapshots/"
      via: "toHaveScreenshot with pathTemplate"
      pattern: "demo-showcase-chromium.png"
---

<objective>
Author the demo-showcase Playwright project partition and spec that exercises seeded personas' distinct auth states using structural assertions and captures four committed PNG baselines. This is the core deliverable of Phase 143.

Purpose: Satisfies PW-01 (structural persona assertions in isolated partition) and PW-02 (committed evaluator-facing screenshots). The spec is Wave 2 because it depends on the Wave 1 SigraAdminPolicy fix (plan 01) — without that fix, `admin@demo.sigra.dev` cannot access `/admin/*` routes.

Output:
- `playwright.config.ts` gains `DEMO_SHOWCASE_SPEC` constant + `demo-showcase-chromium` project + `testIgnore` entries on `chromium` and `mobile` (D-01, D-02, D-03, D-04)
- `tests/demo-showcase.spec.ts` — the full spec: `/demo/credentials` persona testid assertions, login as demo admin with TOTP challenge (click "Use authenticator code instead" then fill code), admin user list email-based assertions, admin user detail MFA+passkey assertions, audit explorer event-variety assertion, 4 `assertDemoScreenshot` captures
- 4 committed PNG baselines generated via `--update-snapshots`
- CI `example_playwright_smoke` gains a `Run demo-showcase spec` step after the non-admin browser smoke step
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-playwright-demo-spec-screenshots/143-CONTEXT.md
@.planning/phases/143-playwright-demo-spec-screenshots/143-RESEARCH.md
@.planning/phases/143-playwright-demo-spec-screenshots/143-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add demo-showcase-chromium Playwright project partition to playwright.config.ts (D-01..D-04)</name>
  <files>test/example/priv/playwright/playwright.config.ts</files>
  <read_first>
    - test/example/priv/playwright/playwright.config.ts — read the entire file to understand the exact structure of existing constants (ADMIN_CHECKPOINTS_SPEC, ADMIN_GENERATED_SPEC), the testIgnore arrays on chromium and mobile, and the projects array before editing
  </read_first>
  <action>
    Three changes to `playwright.config.ts`:

    1. Add constant after WEBAUTHN_CDP_SPECS (line ~30):
       `const DEMO_SHOWCASE_SPEC = /demo-showcase\.spec\.ts/;`

    2. Add `DEMO_SHOWCASE_SPEC` to the `testIgnore` array on the `chromium` project (D-03). The chromium project currently has `testIgnore: [ADMIN_CHECKPOINTS_SPEC, ADMIN_GENERATED_SPEC]` — extend to `testIgnore: [ADMIN_CHECKPOINTS_SPEC, ADMIN_GENERATED_SPEC, DEMO_SHOWCASE_SPEC]`.

    3. Add `DEMO_SHOWCASE_SPEC` to the `testIgnore` array on the `mobile` project (D-03). The mobile project's testIgnore currently contains `ADMIN_BEHAVIOR_SPECS, ADMIN_CHECKPOINTS_SPEC, ADMIN_GENERATED_SPEC, WEBAUTHN_CDP_SPECS` — add `DEMO_SHOWCASE_SPEC` as the last entry.

    4. Add the new project entry at the end of the `projects` array (D-02), after the `admin-generated` entry:
       Project name `demo-showcase-chromium`, `testMatch: DEMO_SHOWCASE_SPEC`, `use: { ...devices['Desktop Chrome'] }`. No `video:` override — demo screenshots use the global `screenshot: 'only-on-failure'` setting (not retained video, unlike the checkpoint lanes).

    Do NOT add a comment block above the new project — the config already has thorough comments on earlier projects; a brief inline comment matching the existing style is sufficient.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra/test/example/priv/playwright && grep -c "DEMO_SHOWCASE_SPEC" playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEMO_SHOWCASE_SPEC" playwright.config.ts` returns `5` (constant declaration + chromium testIgnore + mobile testIgnore + testMatch + project name comment/context)
    - Actually: constant declaration appears once, testIgnore on chromium once, testIgnore on mobile once, testMatch on demo-showcase-chromium once = 4 uses minimum; exact count depends on whether executor adds comments — verify at least 4
    - `grep "demo-showcase-chromium" playwright.config.ts` returns a match
    - `grep "DEMO_SHOWCASE_SPEC" playwright.config.ts | grep -c "testIgnore"` returns `2` (chromium + mobile)
    - TypeScript compiles without errors: `cd test/example/priv/playwright && npx tsc --noEmit 2>&1 | head -5` — should be empty or only config info lines
    - `npx playwright test --list --project=demo-showcase-chromium 2>&1 | grep -v "^$"` runs without "project not found" error (even if no spec file exists yet)
  </acceptance_criteria>
  <done>playwright.config.ts has DEMO_SHOWCASE_SPEC constant, demo-showcase-chromium project with testMatch, and DEMO_SHOWCASE_SPEC in testIgnore on both chromium and mobile projects.</done>
</task>

<task type="auto">
  <name>Task 2: Write demo-showcase.spec.ts, generate and commit PNG baselines, add CI demo-showcase run step (D-05..D-10 + CI)</name>
  <files>
    test/example/priv/playwright/tests/demo-showcase.spec.ts
    test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/demo-credentials-demo-showcase-chromium.png
    test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/admin-user-list-demo-showcase-chromium.png
    test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/admin-user-detail-demo-showcase-chromium.png
    test/example/priv/playwright/tests/demo-showcase.spec.ts-snapshots/audit-explorer-demo-showcase-chromium.png
    .github/workflows/ci.yml
  </files>
  <read_first>
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts — read lines 1–145 for the `assertCheckpointScreenshot` helper pattern, `waitForLiveViewReady`, import style, and overall test structure to replicate
    - test/example/priv/playwright/helpers/adminUsersIndex.ts — confirm exact export name and call signature for `adminUsersEmailLocator` before importing it in the spec
    - test/example/lib/example_web/live/mfa_challenge_live.ex — read lines 46–165 to identify the TOTP tab switch button (phx-click="show_totp") and confirm `#mfa_totp_code` input is inside `div#panel-totp` which is only visible when `@active_method == "totp"`
    - test/example/lib/example_web/live/demo/credentials_live.ex — confirm `data-testid="demo-persona-row-{local}"` renders for all 6 locals (admin, alice, bob, carol, dave, frank) and that the route requires no auth
    - .github/workflows/ci.yml — read lines 738–755 (Run non-admin example browser smoke step and surrounding steps) to identify the correct insertion point for the new Run demo-showcase spec CI step
  </read_first>
  <action>
    PART A — Write `tests/demo-showcase.spec.ts`:

    The spec must be a single `test.describe` block with a single `test` (one journey, same pattern as `admin-checkpoints.spec.ts`). All helpers are module-level functions above the describe block.

    **Imports:** `test`, `expect`, `Page`, `TestInfo` from `@playwright/test`; `authenticator` from `otplib`; `adminUsersEmailLocator` from `../helpers/adminUsersIndex`.

    **Constants (at module top):**
    - `DEMO_TOTP_B32 = 'CSIL7ZDJ7RGXDGXRGIV3Q6CZIBOESTCW'` — base32 of SHA-256("sigra-demo-admin-totp-v1") first 20 bytes; verified in RESEARCH.md; labeled with comment `// Demo-only deterministic secret — matches Personas.demo_totp_secret/0`
    - `DEMO_ADMIN_EMAIL = 'admin@demo.sigra.dev'`
    - `DEMO_ADMIN_PASSWORD = 'DemoAdmin1!SecurePass'`
    - `DEMO_EMAILS = ['admin@demo.sigra.dev', 'alice@demo.sigra.dev', 'bob@demo.sigra.dev', 'carol@demo.sigra.dev', 'dave@demo.sigra.dev', 'frank@demo.sigra.dev']`
    - `DEMO_LOCALS = ['admin', 'alice', 'bob', 'carol', 'dave', 'frank']`

    **Helper `waitForLiveViewReady(page)`:** `await page.waitForSelector('[data-phx-session].phx-connected', { state: 'attached' })` — copied from `admin-checkpoints.spec.ts`.

    **Helper `assertDemoScreenshot(page, testInfo, slug)`:** Mirror of `assertCheckpointScreenshot` from `admin-checkpoints.spec.ts` lines 129–144. No axe assertions (axe is for the admin-checkpoints lane, not required by PW-02). CI-aware tolerances:
    - `ci = process.env.CI === 'true'`
    - `await expect(page).toHaveScreenshot(`${slug}.png`, { fullPage: false, maxDiffPixels: ci ? 200_000 : 30_000, maxDiffPixelRatio: ci ? 0.22 : 0.06 })`

    **Helper `loginDemoAdmin(page)`:** Implements Pattern 3 from RESEARCH.md with the Pitfall 5 fix (TOTP tab switch). Sequence:
    1. `await page.goto('/users/log_in')`
    2. `await waitForLiveViewReady(page)`
    3. Fill `input[name="user[email]"]` with `DEMO_ADMIN_EMAIL`
    4. Fill `input[name="user[password]"]` with `DEMO_ADMIN_PASSWORD`
    5. Click `button:has-text("Log in")` (same form-submit pattern as golden-path.spec.ts)
    6. Wait for URL to match `/users/mfa` (TOTP enrolled user → mfa_pending session)
    7. `await waitForLiveViewReady(page)`
    8. Click `button[phx-click="show_totp"]` — this switches `active_method` from "passkey" to "totp", making `#panel-totp` visible (CRITICAL: admin has 1 passkey row so `active_method` defaults to "passkey"; TOTP panel is hidden until this click)
    9. `await page.waitForSelector('#mfa_totp_code', { state: 'visible' })`
    10. Generate code immediately before fill: `const code = authenticator.generate(DEMO_TOTP_B32)`
    11. `await page.fill('#mfa_totp_code', code)`
    12. `await expect(page).not.toHaveURL(/\/users\/mfa/)` — auto-verify on 6 digits redirects to "/"

    **The test journey:**

    Step 1 — `/demo/credentials` (no auth required):
    - `await page.goto('/demo/credentials')`
    - `await waitForLiveViewReady(page)`
    - For each local in `DEMO_LOCALS`: `await expect(page.locator(`[data-testid="demo-persona-row-${local}"]`)).toBeVisible()` (D-05)
    - `await assertDemoScreenshot(page, testInfo, 'demo-credentials')` (D-10, screenshot 1 of 4)

    Step 2 — Login as demo admin:
    - `await loginDemoAdmin(page)`
    - After successful TOTP, navigate to admin pages explicitly (TOTP auto-verify redirects to "/", NOT to the admin route)

    Step 3 — `/admin/users?q=demo.sigra.dev` (admin user list):
    - `await page.goto('/admin/users?q=demo.sigra.dev')`
    - `await waitForLiveViewReady(page)`
    - For each email in `DEMO_EMAILS`: `await expect(adminUsersEmailLocator(page, email)).toBeVisible()` (D-06)
    - `await assertDemoScreenshot(page, testInfo, 'admin-user-list')` (D-10, screenshot 2 of 4)

    Step 4 — `/admin/users/{admin-id}` (admin user detail):
    - Use the `openUserDetail` pattern from `admin-checkpoints.spec.ts`: navigate to `/admin/users?q=${encodeURIComponent(DEMO_ADMIN_EMAIL)}`, click "Open user" link, wait for URL to match `/admin/users/[^?]+`
    - `await waitForLiveViewReady(page)`
    - Assert `page.getByText('MFA: Enabled')` is visible
    - Assert `page.getByText('1 passkey')` is visible (admin persona has 1 passkey row from seeds)
    - `await assertDemoScreenshot(page, testInfo, 'admin-user-detail')` (D-10, screenshot 3 of 4)

    Step 5 — `/admin/audit` (audit explorer):
    - `await page.goto('/admin/audit')`
    - `await waitForLiveViewReady(page)`
    - Assert the audit log table/list is non-empty — use `await expect(page.locator('[data-testid="audit-event-row"]').first()).toBeVisible()` OR if no testid exists, assert the audit page container renders by checking `await expect(page).toHaveURL(/\/admin\/audit/)` and that the audit heading or table element is visible (read the admin audit LiveView template to find the correct structural selector)
    - `await assertDemoScreenshot(page, testInfo, 'audit-explorer')` (D-10, screenshot 4 of 4)

    **Note on audit table selector:** Before writing the audit assertion, read `lib/sigra/admin/live/audit_live.ex` to confirm the actual data-testid or structural selector for audit event rows. Use the real selector — do not assert on text strings that vary by content. If no testid exists, assert on the presence of the audit heading element or a table row count > 0 using `page.locator('table tbody tr').count()`.

    PART B — Generate committed PNG baselines:

    After writing the spec, generate baselines using `--update-snapshots`:
    ```
    cd test/example && mix run priv/repo/seeds.exs
    cd test/example/priv/playwright && npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium --update-snapshots
    ```
    This creates the 4 PNG files in `tests/demo-showcase.spec.ts-snapshots/` with names `{slug}-demo-showcase-chromium.png`. These files must be committed.

    PART C — Add CI demo-showcase run step to `.github/workflows/ci.yml`:

    Insert a new step named `Run demo-showcase spec (demo-showcase-chromium)` in the `example_playwright_smoke` job, positioned after the `Run non-admin example browser smoke` step (which ends with `tests/passkey-options.spec.ts`). Use the exact format from RESEARCH.md CI Changes section:
    - `working-directory: test/example/priv/playwright`
    - env: `CI: "true"`, `SIGRA_EXAMPLE_URL: "http://localhost:4000"`
    - run: `npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium`

    The `demo-showcase-chromium` project's `testIgnore` exemptions prevent it from running incidentally in the earlier `chromium` and `mobile` steps — it only runs in this explicit step.
  </action>
  <verify>
    <automated>
      cd /Users/jon/projects/sigra/test/example/priv/playwright && npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium --reporter=line
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/demo-showcase.spec.ts` exists with imports for `@playwright/test`, `otplib`, and `adminUsersEmailLocator`
    - `tests/demo-showcase.spec.ts` contains `DEMO_TOTP_B32 = 'CSIL7ZDJ7RGXDGXRGIV3Q6CZIBOESTCW'`
    - `tests/demo-showcase.spec.ts` contains `button[phx-click="show_totp"]` click before `#mfa_totp_code` fill (TOTP tab switch per Pitfall 5 fix)
    - `tests/demo-showcase.spec.ts` contains `demo-persona-row-${local}` testid assertions for the 6 locals
    - `tests/demo-showcase.spec.ts` contains `assertDemoScreenshot` calls with slugs: `demo-credentials`, `admin-user-list`, `admin-user-detail`, `audit-explorer`
    - All 4 PNG files exist in `tests/demo-showcase.spec.ts-snapshots/` with exact names: `demo-credentials-demo-showcase-chromium.png`, `admin-user-list-demo-showcase-chromium.png`, `admin-user-detail-demo-showcase-chromium.png`, `audit-explorer-demo-showcase-chromium.png`
    - Each PNG file is non-empty: `stat tests/demo-showcase.spec.ts-snapshots/demo-credentials-demo-showcase-chromium.png` shows size > 0
    - `npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium --reporter=line` exits 0 (spec passes with committed baselines)
    - `grep "Run demo-showcase spec" .github/workflows/ci.yml` returns a match
    - `npx playwright test --list --project=chromium 2>&1 | grep -v "^$" | grep "demo-showcase"` returns empty (spec excluded from chromium lane)
  </acceptance_criteria>
  <done>demo-showcase.spec.ts passes with 4 committed PNG baselines; chromium and mobile projects exclude the spec; CI has a named step to run the demo-showcase-chromium project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright spec → example app (HTTP) | Spec drives a real browser against the dev-mode Phoenix app; all auth flows go through the real Sigra auth stack |
| TOTP secret in spec file → public repo | DEMO_TOTP_B32 is committed to source; this is intentional (public-by-design demo secret matching personas.ex) |
| CI workflow → booted dev app | Demo-showcase CI step sends authenticated requests as admin@demo.sigra.dev to /admin/* routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-04 | Information Disclosure | DEMO_TOTP_B32 committed in spec | accept | Secret is public-by-design. `Personas.demo_totp_secret/0` is already committed and labeled demo-only. Committing the base32 encoding in a spec file is equivalent; no production risk. |
| T-143-05 | Elevation of Privilege | demo-showcase spec bypassing chromium/mobile lanes | mitigate | D-03 testIgnore entries on chromium and mobile ensure the spec is structurally excluded from behavior-truth lanes. The `DEMO_SHOWCASE_SPEC` constant appears in testIgnore on both projects — verified by grep assertion in acceptance criteria. |
| T-143-06 | Denial of Service | Flaky TOTP code due to 30s window boundary (Pitfall 3) | mitigate | TOTP code is generated immediately before `page.fill` in `loginDemoAdmin`. Global `actionTimeout: 15_000` means the fill completes well within the 30s window. |
| T-143-07 | Tampering | PNG baselines captured on wrong environment | accept | Baselines captured locally (macOS) with CI-aware tolerances (`maxDiffPixels: 200_000, maxDiffPixelRatio: 0.22` when `CI=true`). Pattern is identical to the existing admin-checkpoints baselines which have been CI-green. |
| T-143-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages; all dependencies (otplib, @playwright/test) are already in package.json. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "DEMO_SHOWCASE_SPEC" test/example/priv/playwright/playwright.config.ts` returns ≥4
2. `grep -c "testIgnore" test/example/priv/playwright/playwright.config.ts | xargs` — the two testIgnore occurrences both include DEMO_SHOWCASE_SPEC
3. `npx playwright test tests/demo-showcase.spec.ts --project=demo-showcase-chromium --reporter=line` exits 0 (run from `test/example/priv/playwright`)
4. All 4 PNG files exist and are non-empty in `tests/demo-showcase.spec.ts-snapshots/`
5. `npx playwright test --list --project=chromium 2>&1 | grep "demo-showcase"` returns empty (partition isolation)
6. `grep "Run demo-showcase spec" .github/workflows/ci.yml` returns a match
</verification>

<success_criteria>
- PW-01: demo-showcase-chromium project partition exists; spec asserts 6 personas via data-testid and email-based structural locators; chromium and mobile projects exclude the spec
- PW-02: 4 PNG baselines committed under tests/demo-showcase.spec.ts-snapshots/ with correct {slug}-demo-showcase-chromium.png naming; spec passes with baselines on first run (no --update-snapshots needed)
- CI: Run demo-showcase spec step present after non-admin browser smoke, with --project=demo-showcase-chromium
</success_criteria>

<output>
Create `.planning/phases/143-playwright-demo-spec-screenshots/143-02-SUMMARY.md` when done
</output>
