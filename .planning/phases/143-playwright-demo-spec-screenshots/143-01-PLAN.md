---
phase: 143-playwright-demo-spec-screenshots
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example/sigra_admin_policy.ex
  - .github/workflows/ci.yml
  - test/example/test/example/demo/seeds_test.exs
autonomous: true
requirements:
  - PW-01
  - PW-03

must_haves:
  truths:
    - "admin@demo.sigra.dev receives platform-admin access via SigraAdminPolicy (verified by policy unit logic)"
    - "CI example_playwright_smoke job runs seeds before booting the server so demo personas are present"
    - "seeds_test.exs describe blocks covering idempotency and persona auth-state carry a # PW-03: seeds-smoke check comment (D-13: no CI changes needed for PW-03 — seeds_test.exs already runs via mix test alias)"
  artifacts:
    - path: "test/example/lib/example/sigra_admin_policy.ex"
      provides: "SigraAdminPolicy.platform_admin?/1 grants admin to admin@demo.sigra.dev"
      contains: "@demo_admin_email"
    - path: ".github/workflows/ci.yml"
      provides: "Run demo seeds step in example_playwright_smoke job before Boot example app in background"
      contains: "Run demo seeds"
    - path: "test/example/test/example/demo/seeds_test.exs"
      provides: "PW-03 comment cross-reference on idempotency and persona-state describe blocks"
      contains: "PW-03"
  key_links:
    - from: "test/example/lib/example/sigra_admin_policy.ex"
      to: "Sigra.Admin.Policy behaviour"
      via: "@impl true def platform_admin?/1"
      pattern: "demo_admin_email"
    - from: ".github/workflows/ci.yml"
      to: "priv/repo/seeds.exs"
      via: "MIX_ENV: dev mix run priv/repo/seeds.exs"
      pattern: "Run demo seeds"
---

<objective>
Close the two infrastructure gaps identified by research before the Playwright spec can run:
(1) `SigraAdminPolicy.platform_admin?/1` does not grant admin to `admin@demo.sigra.dev` — only to `platform-admin+` prefix emails;
(2) CI's `example_playwright_smoke` job runs `mix ecto.create && mix ecto.migrate` but NOT `mix run priv/repo/seeds.exs`, so the demo personas are absent when the spec runs.
Also cross-reference the existing `seeds_test.exs` as the PW-03 seeds-smoke check (D-11/D-12).

Purpose: Without these fixes the demo-showcase spec cannot log in as the demo admin, cannot assert on persona rows in `/admin/users`, and CI will fail on every assertion after login. These are Wave 1 prerequisites for Wave 2 spec authoring.

Output:
- `sigra_admin_policy.ex` extended with `@demo_admin_email` module attribute and OR clause in `platform_admin?/1`
- `.github/workflows/ci.yml` `example_playwright_smoke` job gains a `Run demo seeds` step between `Setup example dev DB` and `Boot example app in background`
- `seeds_test.exs` idempotency and persona-state describe blocks annotated with `# PW-03: seeds-smoke check` comments
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-playwright-demo-spec-screenshots/143-CONTEXT.md
@.planning/phases/143-playwright-demo-spec-screenshots/143-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SigraAdminPolicy to grant platform-admin to admin@demo.sigra.dev (D-07 gap fix)</name>
  <files>test/example/lib/example/sigra_admin_policy.ex</files>
  <read_first>
    - test/example/lib/example/sigra_admin_policy.ex — read the current module to understand the exact module attribute names and guard structure before editing
  </read_first>
  <action>
    Add a new module attribute `@demo_admin_email "admin@demo.sigra.dev"` immediately after the existing `@org_admin_prefix "org-admin+"` attribute (per D-07 and RESEARCH.md Pattern 5).

    Modify the `platform_admin?/1` implementation clause so it returns true when the email either starts with `@platform_admin_prefix` OR equals `@demo_admin_email`. The new body is: `String.starts_with?(email, @platform_admin_prefix) or email == @demo_admin_email`. The guard `when is_binary(email)` stays unchanged. The fallback `def platform_admin?(_scope), do: false` stays unchanged.

    No other changes to the file. Do not add a comment like "Demo persona" inline — the module-level doc already explains the policy is for fixture-backed users; the new attribute name is self-documenting.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -c "@demo_admin_email" test/example/lib/example/sigra_admin_policy.ex</automated>
  </verify>
  <acceptance_criteria>
    - `test/example/lib/example/sigra_admin_policy.ex` contains the line `@demo_admin_email "admin@demo.sigra.dev"`
    - `test/example/lib/example/sigra_admin_policy.ex` contains `or email == @demo_admin_email` in the `platform_admin?/1` body
    - `cd test/example && mix compile --warnings-as-errors` exits 0 in `MIX_ENV=dev`
  </acceptance_criteria>
  <done>SigraAdminPolicy.platform_admin?/1 returns true for both platform-admin+ prefix emails and admin@demo.sigra.dev; compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Run demo seeds step to CI example_playwright_smoke job (CI gap fix)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — read lines 635–665 (the Setup example dev DB and Boot example app in background steps) to identify the exact insertion point and indentation before editing
  </read_first>
  <action>
    Insert a new step named `Run demo seeds` into the `example_playwright_smoke` job, positioned immediately after the `Setup example dev DB` step (line ~649, the `mix ecto.create && mix ecto.migrate` step) and before the `Install Playwright deps` step (line ~650).

    The new step must match the YAML indentation and `env:` block style of the surrounding steps. Use the exact env vars and run command from RESEARCH.md CI Changes section:

    Step name: `Run demo seeds`
    working-directory: `test/example`
    env block: `MIX_ENV: dev`, `PGUSER: postgres`, `PGPASSWORD: postgres`, `PGHOST: localhost`
    run: `mix run priv/repo/seeds.exs`

    The seeds are idempotent (Phase 141 verified) so running them here is safe on every CI run. Do not add a `# PW-03` comment to the CI YAML — the CI step is named clearly and belongs to PW-01/PW-02 gap closure, not the PW-03 seeds-smoke check.
  </action>
  <verify>
    <automated>grep -c "Run demo seeds" /Users/jon/projects/sigra/.github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a step with `name: Run demo seeds` inside the `example_playwright_smoke` job
    - The step uses `working-directory: test/example`, `MIX_ENV: dev`, and `run: mix run priv/repo/seeds.exs`
    - The step appears after `mix ecto.create && mix ecto.migrate` and before `Boot example app in background`
    - `grep -A5 "Run demo seeds" .github/workflows/ci.yml` shows `MIX_ENV: dev` in the env block
    - YAML parses without error: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo ok` (run from repo root)
  </acceptance_criteria>
  <done>CI example_playwright_smoke job runs `mix run priv/repo/seeds.exs` (MIX_ENV=dev) after migrate and before server boot so demo personas are present when Playwright runs.</done>
</task>

<task type="auto">
  <name>Task 3: Add PW-03 comment cross-reference to seeds_test.exs (D-12)</name>
  <files>test/example/test/example/demo/seeds_test.exs</files>
  <read_first>
    - test/example/test/example/demo/seeds_test.exs — read the full file to identify the exact describe block headers that cover idempotency (SEED-01), persona auth-states (the dave locked_at and frank scheduled_deletion_at tests), and any block covering SEED-02/SEED-03 before adding comments
  </read_first>
  <action>
    Add a single-line `# PW-03: seeds-smoke check` comment immediately above each of the following describe block lines (D-12):

    1. The `describe "idempotency (SEED-01)"` block (line ~89) — this is the block that asserts `Seeds.run()` twice yields identical counts, satisfying PW-03's idempotency requirement.
    2. The `describe "six personas + states (SEED-02, SEED-03)"` block (line ~106) — this is the block containing the dave `locked_at` and frank `scheduled_deletion_at` assertions, satisfying PW-03's persona auth-state verifiability requirement.

    The comment text is exactly `# PW-03: seeds-smoke check` — no period, no trailing space. Place it on a line by itself at the same indentation level as the `describe` keyword.

    No other changes. Do not add comments to individual test functions or modify any existing code.
  </action>
  <verify>
    <automated>grep -c "PW-03" /Users/jon/projects/sigra/test/example/test/example/demo/seeds_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PW-03" test/example/test/example/demo/seeds_test.exs` returns `2`
    - Both occurrences appear immediately before a `describe` block line (not inside a test body)
    - `cd test/example && mix test test/example/demo/seeds_test.exs` exits 0 (no compilation errors from comment placement)
  </acceptance_criteria>
  <done>seeds_test.exs describe blocks for idempotency and persona auth-state carry # PW-03: seeds-smoke check comments; tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SigraAdminPolicy host-app → Sigra.Admin.Policy | Policy grant is host-app-owned; extending it changes who can access `/admin/*` routes in the example app |
| CI workflow → demo database | Seeds step runs with postgres credentials in CI; seeds are idempotent and contain only public-by-design demo data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Elevation of Privilege | SigraAdminPolicy.platform_admin?/1 | mitigate | Demo admin grant is scoped to `admin@demo.sigra.dev` email literal — not a domain wildcard. Only the seeded demo persona gains platform-admin; no other `@demo.sigra.dev` email does. |
| T-143-02 | Information Disclosure | CI seeds step exposes demo credentials in CI log | accept | Demo credentials are public-by-design (committed in `personas.ex`, labeled as demo-only). Seeds run with `PGUSER=postgres` which matches the existing CI DB setup step — no new secret surface. |
| T-143-03 | Tampering | seeds_test.exs comment edit could corrupt test | accept | Adding a comment line above a describe block cannot affect test execution; compile-verify step confirms the file remains valid. |
| T-143-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages; Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
After all three tasks complete:

1. `cd test/example && mix compile --warnings-as-errors MIX_ENV=dev` exits 0 — SigraAdminPolicy compiles cleanly
2. `grep "or email == @demo_admin_email" test/example/lib/example/sigra_admin_policy.ex` returns a match
3. `grep "Run demo seeds" .github/workflows/ci.yml` returns a match
4. `grep -A3 "Run demo seeds" .github/workflows/ci.yml` shows `MIX_ENV: dev`
5. `grep -c "PW-03" test/example/test/example/demo/seeds_test.exs` returns `2`
6. `cd test/example && mix test test/example/demo/seeds_test.exs` exits 0
</verification>

<success_criteria>
- SigraAdminPolicy.platform_admin?/1 grants true for email == "admin@demo.sigra.dev" (prerequisite for Wave 2 admin page access)
- CI example_playwright_smoke job seeds the demo database before server boot (prerequisite for Wave 2 CI green)
- seeds_test.exs carries PW-03 comment cross-references on both PW-03-relevant describe blocks
- No existing tests broken; all three files compile/parse cleanly
</success_criteria>

<output>
Create `.planning/phases/143-playwright-demo-spec-screenshots/143-01-SUMMARY.md` when done
</output>
