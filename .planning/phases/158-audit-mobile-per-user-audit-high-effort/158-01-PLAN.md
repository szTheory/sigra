---
phase: 158-audit-mobile-per-user-audit-high-effort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/components.ex
  - test/sigra/admin/components_test.exs
autonomous: true
requirements: [AUDX-03]
must_haves:
  truths:
    - "A single `audit_row/1` function component renders the audit card form used by all three audit sites"
    - "One unified tone derivation maps outcome/action_badge to risk/info/nil (the prior 3 divergent helpers are retired by this component)"
    - "The card's date helper formats %DateTime and %NaiveDateTime, renders \"—\" for nil, and raises ArgumentError on any other type"
    - "Byte-goldens freeze the compact and full variants so consumers and Playwright baselines cannot silently drift"
  artifacts:
    - path: "lib/sigra/admin/components.ex"
      provides: "audit_row/1 (11th component) + private audit_tone/1 + private format_date/1"
      contains: "def audit_row"
    - path: "test/sigra/admin/components_test.exs"
      provides: "byte-goldens for compact + full audit_row variants + tone-mapping golden + format_date unit cases"
      contains: "audit_row"
  key_links:
    - from: "lib/sigra/admin/components.ex audit_row/1"
      to: "audit_tone/1"
      via: "data-tone attribute on <article> and <span class=sg-status-pill>"
      pattern: "data-tone=\\{audit_tone"
    - from: "lib/sigra/admin/components.ex audit_row/1"
      to: "format_date/1"
      via: "timestamp line rendering row.inserted_at"
      pattern: "format_date\\(@row.inserted_at\\)"
---

<objective>
Add the 11th shared function component `audit_row/1` to `Sigra.Admin.Components`, emitting the card (`sg-list-row <article>`) form, with a single internal `audit_tone/1` (the one tone source of truth, D-10) and a single internal `format_date/1` that fixes the D-09 NaiveDateTime gap. Freeze both the compact and full variants with `render_component` byte-goldens, and unit-test the date helper.

Purpose: This is the shared dependency for all three audit consumer sites (AuditIndex mobile cards, AuditUser mobile cards, UserShow "Recent Audit"). Building it FIRST with goldens means the three Wave-2 migrations wire against a frozen contract instead of inventing markup. It also retires the divergent `row_tone/1` (×2) and the old `audit_tone/1` so the three sites can no longer drift.

Output: `audit_row/1` component + `audit_tone/1` + `format_date/1` in `components.ex`; new goldens + date-helper unit cases in `components_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-CONTEXT.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-UI-SPEC.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-PATTERNS.md

<interfaces>
<!-- Presenter row map the audit_row :row attr consumes. Source: lib/sigra/admin/audit/presenter.ex present_event/2 -->
<!-- All three sites (AuditIndex, AuditUser via Explorer; UserShow via Detail.recent_audit_preview/3) call the SAME Presenter.present/2, so the row map is field-compatible by construction. -->
row = %{
  id: binary,                    # event UUID
  inserted_at: DateTime.t(),     # from Ecto; may be NaiveDateTime if host column is mistyped (D-09 concern)
  action: binary,                # "admin.impersonation.start"
  action_label: binary,          # "Impersonation started"
  action_badge: binary | nil,    # "Impersonation" when impersonation?, else nil
  actor_label: binary,
  effective_user_label: binary,
  actor_summary: binary,
  outcome: binary                # defaults "success" from explorer; never nil from explorer path
}

<!-- House style every existing component follows (components.ex passim): -->
<!-- required content attrs, then `attr :class, :any, default: nil`, then `attr :rest, :global`, -->
<!-- with `class={[..., @class]} {@rest}` on the root element. applied_chip/1 is the closest shape. -->
<!-- The byte-golden FREEZES whatever :class/:rest convention is chosen — be deliberate. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audit_row/1 + unified audit_tone/1 + reconciled format_date/1 to components.ex</name>
  <files>lib/sigra/admin/components.ex</files>
  <read_first>
    - lib/sigra/admin/components.ex (the file being modified: house-style attr/:class/:rest convention; moduledoc ":5" "Provides 10 flat, stateless" and the "## Components" bullet list ~:15-27; the applied_chip/1 analog ~:161-180 and empty_state/1 slot pattern)
    - .planning/phases/158-.../158-UI-SPEC.md (## Component Inventory → "New component: audit_row/1": the exact card markup shape, the attr table row/show_detail/show_codes, and the "Tone derivation" + "format_date/1 NaiveDateTime handling" interaction-contract sections)
    - .planning/phases/158-.../158-PATTERNS.md (## Pattern Assignments → components.ex section: Analog B compact card characterization source = user_show_live.ex ~:264-272 with inline Calendar.strftime "%Y-%m-%d %H:%M"; Analog C explorer detail lines; the "Target markup" block; the Shared Patterns "Date formatting (D-09)" and "Tone derivation (D-10)" blocks)
  </read_first>
  <behavior>
    - audit_row renders `<article class="sg-list-row sg-stack sg-stack--2" data-tone={audit_tone(@row)}>` with a `sg-cluster sg-cluster--2` pill cluster (action_label pill carries data-tone={audit_tone}; the action_badge pill, rendered only when present, carries data-tone="info"), the actor_summary muted line, then the timestamp line `{format_date(@row.inserted_at)}`.
    - Compact variant (show_detail=false, show_codes=false) reproduces the EXACT bytes user_show_live.ex ~:264-272 emits today, including the no-seconds "%Y-%m-%d %H:%M" timestamp — this is the characterization target.
    - Full variant (show_detail=true) additionally renders the "Actor: {actor_label}" line and (when action_badge present) the "Effective user: {effective_user_label}" line.
    - Full variant (show_codes=true) additionally renders the `<code class="sg-code">` id line and action line.
    - audit_tone/1: outcome not in ["success", nil, ""] → "risk"; action_badge present → "info"; else nil. (Verbatim row_tone/1 body from audit_index_live.ex ~:206-208 / audit_user_live.ex ~:246-248.)
    - format_date/1: %DateTime{} and %NaiveDateTime{} → Calendar.strftime("%Y-%m-%d %H:%M"); nil → "—"; any other value → raise ArgumentError. The catch-all MUST NOT silently absorb a populated-but-wrong-typed value (the D-09 bug).
  </behavior>
  <action>
    Add `audit_row/1` as the 11th component in `Sigra.Admin.Components` per the UI-SPEC card markup shape. Attrs: `attr :row, :map, required: true`; `attr :show_detail, :boolean, default: false`; `attr :show_codes, :boolean, default: false`. Follow the house `attr :class, :any, default: nil` + `attr :rest, :global` convention and merge `class={["sg-list-row sg-stack sg-stack--2", @class]} {@rest}` on the `<article>` root (consistent with the other 10 components; the golden will freeze this — author the goldens to match). Add a private `audit_tone/1` with the three-clause body above and a private `format_date/1` with the D-09 head set (%DateTime{}, %NaiveDateTime{} → strftime "%Y-%m-%d %H:%M"; nil → "—"; else raise ArgumentError). Decide format string = "%Y-%m-%d %H:%M" (no seconds) to match the compact-card characterization source and minimize that golden's diff (the explorer desktop `<tr>` keeps its own inline seconds format — out of scope here, handled in Plan 02/03). Update the moduledoc: change "Provides 10 flat, stateless" at :5 to 11 and add an `audit_row/1` bullet to the "## Components" list. Do NOT build a polymorphic `variant={:table|:card}` form (D-01 hard-fail). Card shape only. Source-of-truth markup lives in 158-UI-SPEC.md and 158-PATTERNS.md — do not re-invent it.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def audit_row" lib/sigra/admin/components.ex` returns a match (component defined).
    - `grep -n "raise ArgumentError" lib/sigra/admin/components.ex` returns a match inside the format_date catch-all (D-09 no-silent-swallow).
    - `grep -c "Provides 11\|audit_row" lib/sigra/admin/components.ex` confirms moduledoc count bump + bullet.
    - The `<article>` root carries `class="sg-list-row sg-stack sg-stack--2"` and `data-tone={audit_tone(@row)}`; the action_label pill is `<span class="sg-status-pill" data-tone={audit_tone(@row)}>`; the action_badge pill (rendered with `:if={@row.action_badge}`) is `<span class="sg-status-pill" data-tone="info">`.
    - No `variant` attr exists on audit_row (D-01 hard-fail boundary): `grep -c "variant" lib/sigra/admin/components.ex` does not increase relative to baseline for audit_row.
    - `mix test test/sigra/admin/components_test.exs` is green (goldens added in Task 2 pass against this implementation).
  </acceptance_criteria>
  <done>audit_row/1 exists with the card markup, internal audit_tone/1 and format_date/1; moduledoc updated to 11 components; no variant polymorphism; component goldens (Task 2) pass.</done>
</task>

<task type="auto">
  <name>Task 2: Byte-goldens for compact + full audit_row variants, tone-mapping golden, and format_date unit cases</name>
  <files>test/sigra/admin/components_test.exs</files>
  <read_first>
    - test/sigra/admin/components_test.exs (the file being modified: `@endpoint nil` at :3, the golden-inventory header comment ~:7-24, the literal-`==` `@applied_chip_golden` analog at :40 and its test ~:103-110 — note the "drifted — see admin-design-contract.md; do not re-record Playwright baselines" assertion message and the `render_component(&Components.fun/1, attrs)` call form)
    - .planning/phases/158-.../158-PATTERNS.md (## components_test.exs section: the @applied_chip_golden discipline, the instruction to author "at least two new goldens" + optional tone golden, and "compact golden must reproduce user_show_live.ex:264-272 bytes exactly")
    - lib/sigra/admin/components.ex (the audit_row/1 / audit_tone/1 / format_date/1 just authored in Task 1 — the goldens characterize its actual output)
  </read_first>
  <action>
    Add to `components_test.exs`: (1) `@audit_row_compact_golden` + a test rendering `render_component(&Components.audit_row/1, row: <fixed assigns>)` with show_detail/show_codes defaulting false — the literal string MUST equal the bytes the compact `<article>` emits (mirrors user_show_live.ex recent-audit output incl. "%Y-%m-%d %H:%M" timestamp). (2) `@audit_row_full_golden` + a test with `show_detail: true, show_codes: true` covering the Actor/Effective user lines and the id/action code lines. (3) A tone-mapping golden/test (D-10): assert a risk row (outcome "failure") yields `data-tone="risk"`, an impersonation row (action_badge present, outcome "success") yields `data-tone="info"`, and a plain success row yields no data-tone / nil. (4) format_date unit cases: %DateTime → "%Y-%m-%d %H:%M"; %NaiveDateTime → "%Y-%m-%d %H:%M"; nil → "—"; a wrong type (e.g. %Date{} or a string) → `assert_raise ArgumentError`. Since format_date is private, exercise it through audit_row rendering (pass a row whose inserted_at is each type) OR if cleaner, add a thin public test seam — prefer exercising through render_component to avoid widening the public surface. Each golden assertion carries the "drifted — see admin-design-contract.md; do not re-record Playwright baselines" message. Update the header golden-inventory comment to reflect the new audit_row goldens. Fixtures: use a fixed inserted_at (e.g. a literal `~U[2026-06-04 12:30:00Z]`) so bytes are deterministic.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/components_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "audit_row" test/sigra/admin/components_test.exs` >= 4 (compact golden + full golden + tone test + format_date coverage).
    - `grep -n "assert_raise ArgumentError" test/sigra/admin/components_test.exs` returns a match (D-09 wrong-type case).
    - `grep -n "data-tone=.risk\|data-tone=.info" test/sigra/admin/components_test.exs` confirms the tone-mapping golden asserts both risk and info.
    - The compact golden string contains the no-seconds timestamp pattern `%Y-%m-%d %H:%M` rendered form (a `2026-06-04 12:30` substring, NOT `12:30:00`) — proving the characterization fidelity to user_show_live.ex.
    - Every new golden assertion includes the message substring "do not re-record Playwright baselines".
    - `mix test test/sigra/admin/components_test.exs` exits 0 with all goldens green (no Postgres needed — `@endpoint nil`).
  </acceptance_criteria>
  <done>Compact + full + tone goldens and format_date unit cases exist and pass; the date helper provably raises on wrong type and renders "—" for nil; goldens carry the drift message and freeze the audit_row contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host audit data → audit_row/1 render | Audit row fields (action_label, actor_summary, action, id, effective_user_label) originate from host-controlled audit metadata and are rendered into HEEx. |
| host timestamp column → format_date/1 | `row.inserted_at` is a host-controlled column value that may be a wrong/mistyped struct (the D-09 concern). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-01 | Information Disclosure | format_date/1 error path | mitigate | On unexpected type, raise `ArgumentError` (the value/type label is the message) rather than rendering raw struct internals into the page; do NOT interpolate the offending value into user-facing HEEx. The catch-all must raise, never silently render. |
| T-158-02 | Tampering (XSS) | audit_row/1 HEEx | mitigate | All dynamic fields are rendered via HEEx `{...}` interpolation which auto-escapes; confirm NO `raw/1` and no `Phoenix.HTML.raw` is introduced anywhere in audit_row. `grep -c "raw(" lib/sigra/admin/components.ex` must not increase for this component. |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs zero packages (`mix.exs` unchanged, no new deps). No legitimacy gate required. |
</threat_model>

<verification>
- `mix test test/sigra/admin/components_test.exs` green (component goldens; no Postgres).
- No `raw/1` introduced in components.ex audit_row.
- Moduledoc reflects 11 components.
</verification>

<success_criteria>
- audit_row/1 is the single card-rendering component for audit data, with one internal audit_tone/1 and one internal format_date/1.
- Byte-goldens freeze compact + full variants; tone-mapping golden pins risk/info/nil; format_date unit cases prove %DateTime/%NaiveDateTime format, nil → "—", wrong type → ArgumentError.
- No variant polymorphism; no raw/1; card markup matches 158-UI-SPEC.md.
</success_criteria>

<output>
Create `.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-01-SUMMARY.md` when done.
</output>
