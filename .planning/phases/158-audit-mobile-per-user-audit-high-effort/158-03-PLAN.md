---
phase: 158-audit-mobile-per-user-audit-high-effort
plan: 03
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - lib/sigra/admin/live/audit_user_live.ex
autonomous: true
requirements: [AUDX-03]
must_haves:
  truths:
    - "AuditUserLive renders the dual-layout (sg-show-desktop table + sg-show-mobile audit_row cards) scoped to the subject user"
    - "AuditUserLive uses the shared <.page_back>, <.scope_ribbon>, <.notice>, <.empty_state>, and <.applied_chip> components instead of hand-rolled chrome"
    - "AuditUserLive has the same quick-filter chip idiom as the explorer, setting the real outcome/action_prefix params"
    - "The subject-user scoping (list_subject_events) is unchanged — per-user audit stays scoped to the subject user"
  artifacts:
    - path: "lib/sigra/admin/live/audit_user_live.ex"
      provides: "dual-layout + shared chrome components + chips + tone consolidation, subject-scoped"
      contains: "admin-audit-user-mobile-results"
  key_links:
    - from: "audit_user_live.ex mobile wrapper"
      to: "Components.audit_row/1"
      via: "<.audit_row :for={row <- @rows} row={row} show_detail show_codes />"
      pattern: "\\.audit_row"
    - from: "audit_user_live.ex chrome"
      to: "Sigra.Admin.Components page_back/scope_ribbon/notice/empty_state/applied_chip"
      via: "shared component tags replacing inline markup"
      pattern: "\\.page_back|\\.scope_ribbon|\\.empty_state"
---

<objective>
Reconcile `AuditUserLive` (per-user audit) with the explorer: add the dual-layout (desktop table + `<.audit_row>` mobile cards), replace the hand-rolled back-nav / scope ribbon / empty-state with the shared `<.page_back>`, `<.scope_ribbon>`, `<.notice>`, `<.empty_state>` components, add the same quick-filter chip idiom, and consolidate the local `row_tone/1` onto the unified tone source — all while preserving the subject-user scoping.

Purpose: AUDX-03 — per-user audit reconciled with the explorer via shared components, shared filter idiom, mobile layout, and the shared audit-row presentation. This is the "same job → same component" milestone law applied to the per-user audit chrome.

Output: modified `audit_user_live.ex` mirroring the explorer's coherent structure, subject-scoped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-CONTEXT.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-UI-SPEC.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-PATTERNS.md
@.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-RESEARCH.md

<interfaces>
<!-- audit_row/1 (frozen by Plan 01). Source: lib/sigra/admin/components.ex -->
<.audit_row row={row} show_detail show_codes />

<!-- Shared chrome components already in Sigra.Admin.Components (golden-tested), to wire in. -->
<.page_back return_to={@return_to} label="Back to user" />       <!-- ghost btn with &larr; ; source components.ex page_back/1 -->
<.scope_ribbon copy={scope_copy(@admin_scope)} />                 <!-- <span class="sg-muted sg-text-sm"> ; source scope_ribbon/1 -->
<.notice tone={:risk}>...</.notice>                              <!-- wraps slot in <p class="sg-text-sm"> ; source notice/1 -->
<.empty_state title="...">...body...</.empty_state>              <!-- source empty_state/1 -->
<.applied_chip label={chip.label} remove_href={...} />           <!-- already used by AuditIndex; AuditUser hand-rolls it today -->

<!-- The inline applied-chip markup in audit_user_live.ex ~:138-148 is byte-identical to the @applied_chip_golden, -->
<!-- so swapping to <.applied_chip> is behavior-preserving for desktop/dark baselines. -->

<!-- Subject scoping (DO NOT WEAKEN). Source: lib/sigra/admin/audit/explorer.ex list_subject_events + handle_params ~:26-56 -->
<!-- The per-user view must stay scoped to the subject user; this plan only changes presentation/chrome, not the query scope. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-layout + audit_row mobile cards + tone consolidation in AuditUserLive</name>
  <files>lib/sigra/admin/live/audit_user_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_user_live.ex (the file being modified: the table block and its `<tr>` rows; the `defp row_tone/1` ~:246-248; the `defp format_timestamp/1` ~:384-387; the sync `handle_params` ~:26-56 + `list_subject_events` scoping — confirm scoping is NOT touched; the import of `Sigra.Admin.Components`)
    - lib/sigra/admin/live/audit_index_live.ex (the sibling explorer — after Plan 02 this has the dual-layout + chips + unified tone to mirror; treat its post-Plan-02 shape as the structural template)
    - .planning/phases/158-.../158-UI-SPEC.md (## Component Inventory → "Dual-layout wrappers: AuditUserLive" table: `sg-table-panel sg-show-desktop` + testid `admin-audit-user-desktop-results`; `sg-stack sg-stack--3 sg-show-mobile` + testid `admin-audit-user-mobile-results`)
    - .planning/phases/158-.../158-PATTERNS.md (audit_user_live.ex section: the MIRROR instruction + the tone consolidation note)
  </read_first>
  <action>
    Mirror the explorer's dual-layout (matching the post-Plan-02 audit_index_live.ex structure): wrap the desktop table in `<div ... id="admin-audit-user-desktop-results" data-testid="admin-audit-user-desktop-results" class="sg-table-panel sg-show-desktop">` and add a sibling `<div id="admin-audit-user-mobile-results" data-testid="admin-audit-user-mobile-results" class="sg-stack sg-stack--3 sg-show-mobile">` iterating `<.audit_row :for={row <- @rows} row={row} show_detail show_codes />`. Use the AuditUser-specific testids (with `-user-`) per D-03. Do NOT solve mobile via horizontal-scroll (D-02 hard-fail). Retire the local `defp row_tone/1` (~:246-248) the same way Plan 02 did — the desktop `<tr>` `data-tone` resolves through the unified audit_tone body (identical three-clause body: outcome not in ["success",nil,""] → "risk"; action_badge present → "info"; else nil). Leave the desktop `<tr>` inline `format_timestamp/1` (seconds) as-is. CRITICAL: do NOT alter `handle_params`, `list_subject_events`, or any scoping — this plan changes presentation only; the per-user view must remain scoped to the subject user.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/live/audit_user_live_test.exs || mix test test/sigra/admin/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "admin-audit-user-desktop-results" lib/sigra/admin/live/audit_user_live.ex` >= 1 with class string containing `sg-table-panel sg-show-desktop`.
    - `grep -c "admin-audit-user-mobile-results" lib/sigra/admin/live/audit_user_live.ex` >= 1 with class string containing `sg-stack sg-stack--3 sg-show-mobile`.
    - `grep -n "\.audit_row" lib/sigra/admin/live/audit_user_live.ex` shows the mobile `<.audit_row ... show_detail show_codes />`.
    - `grep -c "defp row_tone" lib/sigra/admin/live/audit_user_live.ex` == 0 (local divergent helper retired).
    - Scoping unchanged: `grep -c "list_subject_events" lib/sigra/admin/live/audit_user_live.ex` is unchanged from baseline and the `handle_params` scope arguments are not modified (diff shows no change to the query/scope call).
    - No horizontal scroll: `grep -ci "overflow-x" lib/sigra/admin/live/audit_user_live.ex` does not increase.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>AuditUserLive has the subject-scoped dual-layout with the -user- testids and audit_row mobile cards; local row_tone/1 retired; subject scoping untouched; no horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shared chrome components + quick-filter chips into AuditUserLive</name>
  <files>lib/sigra/admin/live/audit_user_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/audit_user_live.ex (the hand-rolled chrome to replace: back-nav `<a class="sg-btn sg-btn--ghost sg-btn--sm" href={@return_to}>` ~:63-66; the inline scope span `<span class="sg-muted sg-text-sm">{scope_copy(@admin_scope)}</span>` ~:66; the inline `<span ... class="sg-applied-chip">` loop ~:138-148; the inline `<div ... class="sg-empty-state ...">` ~:197-217; the detailed filter `<form>` ~:81)
    - lib/sigra/admin/components.ex (the shared components to wire: page_back/1, scope_ribbon/1, notice/1, empty_state/1, applied_chip/1 — confirm attr signatures; notice/1 wraps its slot in `<p class="sg-text-sm">`)
    - lib/sigra/admin/live/audit_index_live.ex (post-Plan-02: the `<.applied_chip>` wiring ~:117-122 and the quick-filter chip row to mirror — AuditUser may reuse verbatim or a trimmed subset per Claude's discretion)
    - .planning/phases/158-.../158-UI-SPEC.md (## Component Inventory → "Shared components wired into AuditUserLive" table; the "Quick-filter chips" semantics; ## Copywriting Contract → per-user empty-state copy: Title "No audit events for this user" / Body "No scoped events are currently tied to this user."; error-state copy "Could not load audit events. Try reloading the page." via <.notice data-tone=risk>)
    - .planning/phases/158-.../158-RESEARCH.md (## Code Examples → the inline applied-chip block that is byte-identical to <.applied_chip>; ## Common Pitfalls → Pitfall 1 for the chip active-state Option (a))
  </read_first>
  <action>
    Replace the hand-rolled chrome with shared components: (1) back-nav `<a class="sg-btn sg-btn--ghost sg-btn--sm" href={@return_to}>...Back to user</a>` → `<.page_back return_to={@return_to} label="Back to user" />`. (2) inline scope span → `<.scope_ribbon copy={scope_copy(@admin_scope)} />`. (3) inline `<span class="sg-applied-chip">` loop ~:138-148 → `<.applied_chip :for={chip <- applied_chips(@current_params)} label={chip.label} remove_href={remove_chip_path(@admin_scope, @detail.user.id, @current_params, @return_to, chip.key)} />` (mirror AuditIndex's wiring; the inline markup is byte-identical to the golden so this is behavior-preserving for desktop/dark). (4) inline empty-state `<div class="sg-empty-state ...">` ~:197-217 → `<.empty_state title="No audit events for this user">No scoped events are currently tied to this user.</.empty_state>` per the per-user copy contract (keep any existing clear-filters CTA inside the slot if present). (5) Add the same two quick-filter chips as the explorer ("Failures" → outcome=failure; "Impersonation" → action_prefix=admin.impersonation) using the same Option (a) checked-input-shell-setting-real-string-value approach, on all viewports, above the detailed filter form — reuse AuditIndex's chip markup verbatim or a trimmed subset. (6) Route any load-error/contextual alert through `<.notice tone={:risk}>Could not load audit events. Try reloading the page.</.notice>` if such a branch exists; if there is no error branch today, do not invent one. Preserve `return_to`, `clear_path`, `export_path`, and `scope_copy/1`. Do NOT touch scoping.
  </action>
  <verify>
    <automated>mix test test/sigra/admin/live/audit_user_live_test.exs || mix test test/sigra/admin/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.page_back\|\.scope_ribbon\|\.empty_state\|\.applied_chip" lib/sigra/admin/live/audit_user_live.ex` >= 4 (all four shared components wired).
    - The hand-rolled chrome is gone: `grep -c "sg-btn--ghost sg-btn--sm\" href=" lib/sigra/admin/live/audit_user_live.ex` == 0 (back-nav now via component) and `grep -c "class=\"sg-applied-chip\"" lib/sigra/admin/live/audit_user_live.ex` == 0 (inline chip removed) and `grep -c "class=\"sg-empty-state" lib/sigra/admin/live/audit_user_live.ex` == 0 (inline empty-state removed).
    - Per-user empty-state copy present: `grep -c "No audit events for this user" lib/sigra/admin/live/audit_user_live.ex` >= 1.
    - Quick-filter chips present with real params and no dead boolean: `grep -c "sg-filter-chip" lib/sigra/admin/live/audit_user_live.ex` >= 2; `grep -c "admin.impersonation" lib/sigra/admin/live/audit_user_live.ex` >= 1; `grep -c "impersonation=true\|failure=true\|name=\"impersonation\"\|name=\"failure\"" lib/sigra/admin/live/audit_user_live.ex` == 0.
    - No `raw/1` introduced: `grep -c "raw(" lib/sigra/admin/live/audit_user_live.ex` does not increase.
    - A LiveView render test asserts the page_back ghost-button (`&larr;` + "Back to user") and the empty_state title render via the shared components; add to the audit_user test file if absent.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>AuditUserLive's back-nav, scope ribbon, applied-chip loop, and empty-state are the shared components; the per-user empty-state copy matches the contract; the same value-setting quick-filter chips render; scoping untouched; no inline duplicate chrome remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subject-user scope → list_subject_events | The per-user audit view must only show events scoped to the subject user. |
| host audit data → audit_row/1 cards + shared chrome | Audit metadata + return_to rendered into the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-06 | Information Disclosure (scope leak) | list_subject_events scoping | mitigate | This plan is presentation-only; `handle_params`/`list_subject_events` and the subject-user scope arguments are NOT modified. Acceptance criterion asserts the scoping call is byte-unchanged so the per-user view cannot widen beyond the subject user. |
| T-158-07 | Tampering (XSS) | audit_row/1 cards + shared chrome | mitigate | All dynamic fields render via HEEx auto-escaped interpolation through golden-tested components; no `raw/1` introduced (acceptance criterion). |
| T-158-08 | Tampering (open redirect) | page_back return_to | mitigate | `return_to` continues to flow through the existing `sanitize_return_to/3` (~:266-275) before reaching `<.page_back>`; this plan does not bypass that sanitizer. |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero packages installed. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/sigra/admin/` green.
- Subject scoping call unchanged; no `raw/1`; no inline duplicate chrome; no dead boolean param.
</verification>

<success_criteria>
- AuditUserLive mirrors the explorer: subject-scoped dual-layout, shared chrome components, value-setting quick-filter chips, unified tone.
- Per-user empty-state and error copy match the UI-SPEC copywriting contract.
- Subject-user scoping is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/158-audit-mobile-per-user-audit-high-effort/158-03-SUMMARY.md` when done.
</output>
