---
phase: 241-close-gap-ops-01-repair-controller-mfa-settings-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/core/settings_controller.ex
  - scripts/ci/passkeys-opt-out-smoke.sh
  - test/sigra/install/generated_rate_limit_contract_test.exs
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-01/D-02: a generated --no-live SettingsController.mfa/2 explicitly selects the emitted MFASettingsHTML module and renders the existing :mfa_settings function with its existing assigns."
    - "D-03/D-06: an authenticated request with fresh sudo state receives status 200 and stable MFA-page content from GET /users/settings/mfa; an authentication or sudo redirect cannot satisfy the proof."
    - "D-04/D-05: the disposable controller host proof runs inside the established credential-free smoke lifecycle and freshens the exact persisted session identified by the logged-in connection's :user_token without sleeps."
    - "D-07/D-08: controller MFA mutations, the canonical LiveView lane, passkeys, public APIs, dependencies, admin UI, and broader MFA behavior remain unchanged."
  artifacts:
    - path: "priv/templates/sigra.install/core/settings_controller.ex"
      provides: "Explicit controller-to-MFASettingsHTML render handoff"
      contains: "put_view(html: <%= web_module %>.MFASettingsHTML)"
    - path: "scripts/ci/passkeys-opt-out-smoke.sh"
      provides: "Disposable generated controller-host MFA route probe and execution hook"
      contains: "generated_mfa_settings_route_probe_test.exs"
    - path: "test/sigra/install/generated_rate_limit_contract_test.exs"
      provides: "Source contracts for render ownership, route-probe topology, and scope preservation"
  key_links:
    - from: "priv/templates/sigra.install/core/settings_controller.ex"
      to: "priv/templates/sigra.install/core/mfa_settings_html.ex"
      via: "Phoenix.Controller.put_view/2 before render(:mfa_settings, ...)"
      pattern: "put_view\\(html:.*MFASettingsHTML\\).*render\\(:mfa_settings"
    - from: "scripts/ci/passkeys-opt-out-smoke.sh"
      to: "generated sigra_b2c_controller test/generated_mfa_settings_route_probe_test.exs"
      via: "controller-label conditional injects and runs the ExUnit probe after test migration"
      pattern: "sigra_b2c_controller.*generated_mfa_settings_route_probe_test"
    - from: "generated MFA route probe"
      to: "persisted SigraB2cController.Accounts.UserSession"
      via: "Plug.Conn.get_session(conn, :user_token), Accounts.get_user_and_session_by_token/1, and Repo update of the matching hashed_token row"
      pattern: "get_session.*user_token.*get_user_and_session_by_token.*sudo_at"
---

<objective>
Make the real generated controller-mode MFA settings route render successfully after authentication and sudo authorization, closing the OPS-01 audit gap without expanding MFA behavior.

Purpose: Repair the runtime-only controller-to-HTML-module mismatch that compilation and root readiness cannot detect, and preserve the generated-host ownership and security boundaries locked by D-01 through D-08.
Output: The smallest controller render handoff, a deterministic disposable-host route probe, and focused regression contracts.

The phase has no mapped requirement IDs (`phase_req_ids` is null / ROADMAP says TBD), so `requirements: []` is an intentional visible specless-probe fallback. Traceability is through the OPS-01 closure and D-01–D-08; no requirement ID or probe-derived predicate is invented.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.48-MILESTONE-AUDIT.md
@.planning/phases/241-close-gap-ops-01-repair-controller-mfa-settings-rendering/241-CONTEXT.md
@.planning/phases/241-close-gap-ops-01-repair-controller-mfa-settings-rendering/241-RESEARCH.md
@.planning/phases/241-close-gap-ops-01-repair-controller-mfa-settings-rendering/241-PATTERNS.md
@.planning/phases/241-close-gap-ops-01-repair-controller-mfa-settings-rendering/241-VALIDATION.md
@.planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-01-SUMMARY.md
@.planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-VERIFICATION.md

<interfaces>
Existing contracts the executor must preserve and use:

- `SettingsController.mfa/2` currently computes `status = Auth.mfa_status(conn.assigns.current_scope.user)` and renders `:mfa_settings` with `mfa_enabled`, `backup_remaining`, `enrollment_step`, `svg`, `base32_secret`, `backup_codes`, and `show_disable`.
- `<WebModule>.MFASettingsHTML.mfa_settings/1` owns those seven assigns and contains stable disabled-state copy `Two-Factor Authentication` and `Set up two-factor authentication`.
- `<WebModule>.ConnCaseHelpers.log_in_user/2` generates the persisted session token and writes it to the test connection as `:user_token`.
- `<ContextModule>.get_user_and_session_by_token/1` returns `{user, session}` for the raw connection token; `<ContextModule>.UserSession` is the persisted Ecto schema and `<AppModule>.Repo` updates it.
- `Sigra.Plug.RequireSudo` consumes the session loaded by the authenticated pipeline and validates its persisted `sudo_at`.
- `run_leg(flags, label)` owns generation, dependency refresh, test/dev database setup, warning-free compile, assets, boot, bounded curl readiness, and cleanup; the controller invocation is `run_leg "--no-admin --no-organizations --no-passkeys --no-live" "sigra_b2c_controller"`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and repair the protected controller MFA render path end to end</name>
  <precondition>Phase 240.2's four-leg smoke exists, and `pg_isready -h "${PGHOST:-localhost}" -p "${PGPORT:-5432}"` succeeds for the disposable generated-host databases.</precondition>
  <reversibility rating="reversible">The explicit view selection and disposable probe are local internal changes that can be reverted without migration or public-contract breakage.</reversibility>
  <read_first>
    - priv/templates/sigra.install/core/settings_controller.ex
    - priv/templates/sigra.install/core/mfa_settings_html.ex
    - scripts/ci/passkeys-opt-out-smoke.sh
    - priv/templates/sigra.install/core/conn_case_helpers.ex
    - priv/templates/sigra.install/core/auth_fixtures.ex
    - priv/templates/sigra.install/core/auth.ex
    - priv/templates/sigra.install/core/user_session.ex
    - priv/templates/sigra.install/core/user_auth.ex
    - lib/sigra/plug/require_sudo.ex
    - lib/sigra/install/features/core.ex
    - test/example/test/example_web/live/passkey_settings_live_test.exs
    - .planning/phases/240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-/240.2-01-SUMMARY.md
  </read_first>
  <files>priv/templates/sigra.install/core/settings_controller.ex, scripts/ci/passkeys-opt-out-smoke.sh</files>
  <behavior>
    - RED: with the route probe wired but before the controller repair, the `sigra_b2c_controller` leg fails when the real protected GET reaches Phoenix's incorrect inferred HTML owner; a 302 redirect is not accepted as the expected failure.
    - GREEN: after the repair, the same logged-in connection's exact persisted session has a fresh `sudo_at`, and `GET /users/settings/mfa` returns 200 HTML containing `Two-Factor Authentication`.
    - REGRESSION: the probe runs only for `sigra_b2c_controller` inside the existing smoke lifecycle, with no fixed sleep, provider credentials, browser suite, or parallel host harness.
  </behavior>
  <action>
Add `install_generated_mfa_settings_route_probe/0` to `scripts/ci/passkeys-opt-out-smoke.sh`. It must emit disposable `test/generated_mfa_settings_route_probe_test.exs` containing `SigraB2cController.GeneratedMFASettingsRouteProbeTest`, use `SigraB2cControllerWeb.ConnCase` with `async: false`, import `SigraB2cController.AccountsFixtures`, and alias `SigraB2cController.Accounts`, `SigraB2cController.Accounts.UserSession`, and `SigraB2cController.Repo`. In the test, create a user, call the generated `log_in_user(conn, user)`, read `:user_token` from that returned connection, pattern-match `{^user, session} = Accounts.get_user_and_session_by_token(token)`, resolve the persisted `UserSession` by `session.hashed_token`, and update only that row's `sudo_at` to `DateTime.utc_now()`. Dispatch the real routed `GET /users/settings/mfa` with the same connection and require `html_response(200)` plus the stable existing `Two-Factor Authentication` text, so authentication/sudo redirects fail rather than count as success (D-03, D-05, D-06).

Wire this helper only in the existing `run_leg/2` branch where `label == "sigra_b2c_controller"`, after generated dependencies are fetched. Reuse the harness's test database drop/create/migrate sequence, then run exactly `MIX_ENV=test mix test test/generated_mfa_settings_route_probe_test.exs`; keep the four existing `run_leg` invocations and the bounded cleanup/readiness flow intact. Do not add a server/browser path or sleep, and do not inherit Google credentials (D-04, D-08).

Run the controller leg with the newly installed probe before changing the template and retain the failing ExUnit diagnostic showing the runtime owner mismatch, not a redirect. Then change only `SettingsController.mfa/2`: pipe the existing `conn` through `put_view(html: <%= web_module %>.MFASettingsHTML)` immediately before its existing `render(:mfa_settings, ...)`. Preserve the `MFASettingsHTML` module name, `mfa_settings/1`, status lookup, all seven assigns, and their values exactly (D-01, D-02). Leave `edit`, `reactivation`, `disable`, `regenerate`, `revoke_trust`, `enroll`, `confirm`, `complete`, and `unavailable/1` unchanged (D-07). Do not touch the canonical LiveView runtime script, passkey behavior, public APIs, dependencies, admin UI, or any presentation/template file (D-08).
  </action>
  <acceptance_criteria>
    - Source: `SettingsController.mfa/2` contains `put_view(html: <%= web_module %>.MFASettingsHTML)` before `render(:mfa_settings, ...)`, while the existing status lookup and seven assign names/values are unchanged (D-01, D-02).
    - RED evidence: the generated route probe is executed once before the source repair and fails at real render dispatch because Phoenix resolves the wrong HTML owner; status 302 is explicitly rejected and does not satisfy the recorded RED result.
    - Behavior: the post-repair generated probe logs in, derives `:user_token` from that same connection, resolves `{^user, session}`, updates the persisted row selected by `session.hashed_token`, and proves `html_response(200)` contains `Two-Factor Authentication` (D-03, D-05, D-06).
    - Harness: the probe is installed and run only when `label == "sigra_b2c_controller"`, after test migration, within the existing four-leg lifecycle; `rg -n '\bsleep\b|waitForTimeout|Process\.sleep' scripts/ci/passkeys-opt-out-smoke.sh` returns no matches (D-04).
    - Scope: `priv/templates/sigra.install/core/mfa_settings_html.ex`, mutation action bodies, `scripts/ci/generated-auth-runtime-proof.sh`, passkey sources, dependency manifests, public interfaces, and admin UI files have no phase diff (D-07, D-08).
    - Command: `GITHUB_WORKSPACE="$(pwd)" scripts/ci/passkeys-opt-out-smoke.sh` exits 0 and reports success after executing `sigra_b2c_controller`'s generated MFA route probe.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/ci/passkeys-opt-out-smoke.sh &amp;&amp; GITHUB_WORKSPACE="$(pwd)" scripts/ci/passkeys-opt-out-smoke.sh</automated>
  </verify>
  <done>The real generated --no-live protected MFA settings request returns status 200 through MFASettingsHTML, and the deterministic smoke would fail on the original runtime mismatch or on an auth/sudo redirect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock render ownership, exact-session authorization, and lane preservation into source contracts</name>
  <read_first>
    - test/sigra/install/generated_rate_limit_contract_test.exs
    - priv/templates/sigra.install/core/settings_controller.ex
    - priv/templates/sigra.install/core/mfa_settings_html.ex
    - scripts/ci/passkeys-opt-out-smoke.sh
    - scripts/ci/generated-auth-runtime-proof.sh
    - lib/sigra/install/features/core.ex
    - .planning/phases/241-close-gap-ops-01-repair-controller-mfa-settings-rendering/241-VALIDATION.md
  </read_first>
  <files>test/sigra/install/generated_rate_limit_contract_test.exs</files>
  <behavior>
    - The contract fails if explicit `MFASettingsHTML` ownership disappears, is ordered after render, or the emitted `mfa_settings/1` contract is renamed.
    - The contract fails if the disposable probe stops deriving and freshening the request connection's exact persisted session, stops demanding 200 stable MFA HTML, or moves outside the controller-only leg.
    - The contract fails if controller mutation handlers stop delegating to `unavailable/1`, the canonical runtime lane gains `--no-live`, or a sleep is introduced.
  </behavior>
  <action>
Extend `Sigra.Install.GeneratedRateLimitContractTest` rather than creating a parallel contract module. Add source constants only for the existing settings-controller and MFA-settings-HTML templates. Add one focused test that structurally proves the explicit `put_view(html: <%= web_module %>.MFASettingsHTML)` occurs between the `def mfa/2` status lookup and `render(:mfa_settings, ...)`, and that `MFASettingsHTML.mfa_settings/1` plus the seven existing assign markers remain present (D-01, D-02).

Add one focused harness contract that requires `install_generated_mfa_settings_route_probe`, the disposable filename, exact `:user_token` lookup, `Accounts.get_user_and_session_by_token`, `session.hashed_token`, `sudo_at`, `html_response(200)`, stable MFA text, and the focused mix-test command. Assert these markers are owned by the `sigra_b2c_controller` conditional and occur after its test migration; retain the existing global no-sleep guard and exact four-leg invocations (D-03–D-06). Contract-lock every controller mutation action as an `unavailable(conn)` delegation and retain the existing assertion that `scripts/ci/generated-auth-runtime-proof.sh` has no controller-mode flag (D-07, D-08). Use marker/order assertions or focused regexes that fail with actionable messages; do not assert formatting unrelated to the behavior.
  </action>
  <acceptance_criteria>
    - Source contract: deleting or moving the explicit `MFASettingsHTML` selection behind render fails `generated_rate_limit_contract_test.exs`; the emitted module/function/assign contract remains required (D-01, D-02).
    - Security contract: replacing `html_response(200)` with redirect acceptance, using a separately created sudo fixture/session, or omitting the logged-in connection token / matching hashed-session update fails the focused harness test (D-03, D-05, D-06).
    - Lifecycle contract: moving the generated MFA probe outside the `sigra_b2c_controller` branch, before test migration, into a new harness, or adding a sleep fails the focused test (D-04).
    - Scope contract: `disable`, `regenerate`, `revoke_trust`, `enroll`, `confirm`, and `complete` remain exact `unavailable(conn)` delegations; the independent LiveView runtime remains free of controller mode (D-07, D-08).
    - Command: `mix test test/sigra/install/generated_rate_limit_contract_test.exs` exits 0, `bash -n scripts/ci/passkeys-opt-out-smoke.sh` exits 0, and the final four-leg smoke exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/sigra/install/generated_rate_limit_contract_test.exs &amp;&amp; bash -n scripts/ci/passkeys-opt-out-smoke.sh &amp;&amp; GITHUB_WORKSPACE="$(pwd)" scripts/ci/passkeys-opt-out-smoke.sh</automated>
  </verify>
  <done>Focused contracts make the runtime render fix, exact-session sudo proof, redirect rejection, controller-only ownership, and all D-07/D-08 non-goals mechanically enforceable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated test connection → authenticated/sudo router pipelines | A test-session token crosses into real authentication and authorization plugs; the proof must exercise the same persisted session the request uses. |
| Generated controller → emitted HTML module | Phoenix controller view inference crosses the generated ownership boundary and currently targets a module that is not emitted. |
| Repository harness → disposable generated Phoenix host | Shell-injected proof code and local database state must remain bounded, credential-free, deterministic, and tied to the controller-mode leg. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-241-01 | Elevation of Privilege | Generated MFA route proof | high | mitigate | Demand `html_response(200)` and stable MFA-page content after real authentication/sudo pipelines; a 302 authentication or sudo redirect fails both RED and GREEN assertions (D-03, D-06). |
| T-241-02 | Tampering | Persisted sudo session setup | high | mitigate | Read `:user_token` from the logged-in request connection, pattern-match the same user, resolve the matching persisted row via `session.hashed_token`, and update only that row's `sudo_at`; forbid the separate-session sudo fixture (D-05). |
| T-241-03 | Denial of Service | `SettingsController.mfa/2` runtime render dispatch | high | mitigate | Select the emitted `MFASettingsHTML` explicitly before render and execute the protected generated route; compilation, root readiness, and source presence cannot substitute for this proof (D-01–D-03). |
| T-241-04 | Information Disclosure | Generated-host environment | medium | mitigate | Reuse the existing credential-free lifecycle, inherited Google credential unsets, disposable Cloak value, local database endpoint, and cleanup trap; add no provider call or secret-bearing fixture (D-04, D-08). |
| T-241-05 | Repudiation | OPS-01 closure evidence | medium | mitigate | Preserve both the pre-fix failing routed probe and the post-fix successful four-leg smoke, with focused source contracts that reject redirect-as-success and wrong-session freshening. |
| T-241-06 | Tampering | Deferred controller mutation and LiveView/passkey lanes | low | accept | The phase changes no mutation body, LiveView/passkey route, public API, dependency, or admin UI file; focused source contracts and the bounded `files_modified` list make the narrow acceptance appropriate at ASVS L1 (D-07, D-08). |
</threat_model>

## Artifacts this phase produces

| Category | Artifact | Disposition |
|----------|----------|-------------|
| Modified committed file | `priv/templates/sigra.install/core/settings_controller.ex` | Adds the explicit `MFASettingsHTML` selection inside `mfa/2`; no module, template, assign, or mutation redesign. |
| Modified committed file | `scripts/ci/passkeys-opt-out-smoke.sh` | Adds the controller-only disposable probe installer/execution hook inside the established lifecycle. |
| Modified committed file | `test/sigra/install/generated_rate_limit_contract_test.exs` | Adds focused render/session/lane regression contracts. |
| Created symbol | `install_generated_mfa_settings_route_probe/0` | Shell helper owned by the existing smoke script. |
| Created disposable probe file | `test/generated_mfa_settings_route_probe_test.exs` | Created only inside the generated `sigra_b2c_controller` host and removed with the temporary host. |
| Created disposable module/test | `SigraB2cController.GeneratedMFASettingsRouteProbeTest` / protected MFA render test | Logs in, freshens the exact persisted request session, and asserts routed 200 MFA HTML. |
| Created fixture | None | The probe consumes existing `SigraB2cController.AccountsFixtures.user_fixture/1` and creates no repository fixture. |
| Created public API, schema, dependency, browser suite, UI asset, or external coverage artifact | None | Explicitly outside D-07/D-08 and the phase capability gates. |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Generated controller MFA settings GET renders through its emitted HTML owner with authenticated fresh-sudo proof | 241-01 | COVERED | Tracer proves the real routed behavior before contracts expand. |
| REQ | — | No requirement IDs are mapped to Phase 241 | 241-01 | COVERED | Visible specless fallback; OPS-01 closure is named without inventing a phase requirement ID. |
| RESEARCH | — | Use action-local `put_view(html: MFASettingsHTML)` and preserve existing render assigns | 241-01 | COVERED | Task 1 implements; Task 2 locks ordering and ownership. |
| RESEARCH | — | Run a generated ConnCase probe in the controller leg after migration | 241-01 | COVERED | Task 1 injects and executes it in the existing lifecycle. |
| RESEARCH | — | Freshen the logged-in connection's exact persisted session and reject redirects | 241-01 | COVERED | Task 1 behavior and T-241-01/T-241-02 require it. |
| RESEARCH | — | Preserve no-sleep, credential-free, four-leg topology and LiveView isolation | 241-01 | COVERED | Both tasks and focused contracts enforce it. |
| CONTEXT | D-01 | Keep MFASettingsHTML; explicitly connect controller | 241-01 | COVERED | Task 1 render handoff; Task 2 source contract. |
| CONTEXT | D-02 | Preserve mfa_settings function and assigns | 241-01 | COVERED | Task 1 action/acceptance; Task 2 contract. |
| CONTEXT | D-03 | Prove authenticated fresh-sudo real GET renders | 241-01 | COVERED | Tracer's disposable ConnCase test. |
| CONTEXT | D-04 | Reuse deterministic generated-host harness without sleeps | 241-01 | COVERED | Controller-label hook in existing smoke. |
| CONTEXT | D-05 | Freshen exact request-token persisted session | 241-01 | COVERED | Token/session/hashed-row chain is explicit. |
| CONTEXT | D-06 | Assert action render, not sudo redirect | 241-01 | COVERED | `html_response(200)` plus stable content. |
| CONTEXT | D-07 | Stop after GET; leave mutations unchanged | 241-01 | COVERED | Mutation delegation scope contract. |
| CONTEXT | D-08 | No LiveView/passkey/API/dependency/admin UI expansion | 241-01 | COVERED | File fence, task prohibitions, and lane contract. |

Deferred controller MFA mutations, passkey/controller registration questions, malformed registration fallback behavior, general MFA capability work, auth/admin UI work, and unrelated reviewed todos are excluded exactly as directed by CONTEXT.md.

<verification>
1. Focused source contracts: `mix test test/sigra/install/generated_rate_limit_contract_test.exs`.
2. Harness syntax: `bash -n scripts/ci/passkeys-opt-out-smoke.sh`.
3. Required behavior: `GITHUB_WORKSPACE="$(pwd)" scripts/ci/passkeys-opt-out-smoke.sh` completes all four legs and the `sigra_b2c_controller` leg executes the generated MFA route probe successfully.
4. Security proof: the generated test requires status 200 plus stable MFA content after updating the exact request-token session; neither a redirect nor a separately freshened session passes.
5. Scope proof: only the three frontmatter `files_modified` paths have implementation changes; generated templates/lanes listed under D-07/D-08 remain otherwise unchanged.
</verification>

<success_criteria>
- A disposable generated `--no-live` host logs in a user, freshens that connection's exact persisted session, and receives 200 rendered MFA settings HTML from the real protected route.
- The probe fails on the pre-repair runtime module mismatch and cannot treat an authentication or sudo redirect as success.
- `SettingsController.mfa/2` explicitly selects `<WebModule>.MFASettingsHTML` while retaining `mfa_settings/1`, its seven assigns, and all mutation handling unchanged.
- The probe remains controller-only, readiness-driven, credential-free, and sleep-free inside the existing four-leg lifecycle.
- Focused contracts and the full generated-host smoke pass with no LiveView, passkey, public API, dependency, schema, browser, external service, or admin UI expansion.
</success_criteria>

<output>
Create `.planning/phases/241-close-gap-ops-01-repair-controller-mfa-settings-rendering/241-01-SUMMARY.md` when done.
</output>
