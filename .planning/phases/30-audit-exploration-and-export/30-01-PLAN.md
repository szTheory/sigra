---
phase: 30-audit-exploration-and-export
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/auth.ex
  - lib/sigra/admin/users/actions.ex
  - lib/sigra/admin/audit/query.ex
  - lib/sigra/admin/audit/query_params.ex
  - test/sigra/admin/users_actions_test.exs
  - test/sigra/admin/audit/query_test.exs
autonomous: true
requirements:
  - AUD-01
  - AUD-02
must_haves:
  truths:
    - "Admin-triggered support actions record the real admin in `actor_id`, the affected user in `target_id`, and the effective user context in canonical audit columns instead of collapsing attribution onto the target user."
    - "Audit explorer inputs normalize into one shared filter contract that supports actor, effective user, organization, action family, outcome, time range, cursor, and page size across every scope."
    - "The shared admin audit query layer can express per-user subject history without relying on `target_id` alone, so later explorer surfaces can include rows like `session.create` that only carry `effective_user_id`."
  artifacts:
    - path: "lib/sigra/admin/audit/query.ex"
      provides: "Admin-owned query wrapper for scope-safe explorer and per-user subject semantics"
    - path: "lib/sigra/admin/audit/query_params.ex"
      provides: "Whitelist-first URL filter normalization and cursor parsing for audit explorer and export"
    - path: "lib/sigra/admin/users/actions.ex"
      provides: "Admin support-action wrappers that preserve canonical dual-actor audit attribution"
    - path: "test/sigra/admin/audit/query_test.exs"
      provides: "Focused direct-path coverage for normalized audit filters and subject-user query behavior"
  key_links:
    - from: "lib/sigra/admin/users/actions.ex"
      to: "lib/sigra/auth.ex"
      via: "admin session revoke wrappers pass explicit actor/effective/target audit options"
      pattern: "actor_id|effective_user_id|target_id"
    - from: "lib/sigra/admin/audit/query.ex"
      to: "lib/sigra/audit/query.ex"
      via: "admin wrapper reuses canonical audit filter builder and keyset pagination runtime"
      pattern: "Sigra\\.Audit\\.Query"
    - from: "lib/sigra/admin/audit/query_params.ex"
      to: "lib/sigra/audit/cursor.ex"
      via: "shared cursor decode contract for every explorer and export entrypoint"
      pattern: "Sigra\\.Audit\\.Cursor"
nyquist_compliant: true
---

<objective>
Lock the Phase 30 runtime contract before any explorer UI lands: fix admin audit attribution, then create the shared normalized query/filter seam that every scope-specific audit surface will consume.

Purpose: The research shows AUD-01 is still open because some admin support actions are logged as though the target user acted, and Phase 28's audit preview boundary cannot support full per-user history without a subject-aware query layer.
Output: Canonical admin audit query modules, corrected support-action attribution, and focused direct-path tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-audit-exploration-and-export/30-RESEARCH.md
@.planning/phases/30-audit-exploration-and-export/30-PATTERNS.md
@.planning/phases/30-audit-exploration-and-export/30-VALIDATION.md
@.planning/phases/28-user-operations-surface/28-CONTEXT.md
@.planning/phases/29-secure-impersonation/29-CONTEXT.md
@/Users/jon/projects/sigra/lib/sigra/audit.ex
@/Users/jon/projects/sigra/lib/sigra/audit/query.ex
@/Users/jon/projects/sigra/lib/sigra/audit/cursor.ex
@/Users/jon/projects/sigra/lib/sigra/admin/users/actions.ex
@/Users/jon/projects/sigra/lib/sigra/auth.ex

<interfaces>
From `lib/sigra/audit.ex`:

```elixir
def log_safe(action, scope, opts) when is_binary(action) and is_list(opts)
```

From `lib/sigra/audit/query.ex`:

```elixir
allowed_filters()
build(audit_schema, filters \\ [])
paginate(query, cursor, limit)
```

From `lib/sigra/admin/users/actions.ex`:

```elixir
revoke_session(config, admin_scope, user_id, hashed_token)
revoke_all_sessions(config, admin_scope, user_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct-path tests for canonical admin audit attribution and normalized query params</name>
  <files>test/sigra/admin/users_actions_test.exs, test/sigra/admin/audit/query_test.exs</files>
  <behavior>
    - Test 1: per AUD-01, admin-driven revoke-one and revoke-all actions record the acting admin in `actor_id` rather than attributing the row to the target user.
    - Test 2: per research finding 2, a per-user subject query includes rows matched through `effective_user_id` as well as `target_id`.
    - Test 3: per AUD-02, query-param normalization accepts only the supported explorer/export filters and decodes invalid cursors as an explicit error instead of silently widening the query.
    - Test 4: per research finding 5, the normalized filter output is stable enough to be reused unchanged by both LiveView explorer routes and CSV export routes.
  </behavior>
  <action>Before implementation, extend `test/sigra/admin/users_actions_test.exs` with explicit audit-row assertions for revoke-one and revoke-all from an admin scope, including cases where the target user differs from the real acting admin. Add `test/sigra/admin/audit/query_test.exs` as the focused contract suite for the new Phase 30 admin query layer: normalize URL params, validate page-size and cursor handling, assert that unknown keys are rejected, and pin a subject-user query scenario that would miss `session.create` if it used only `target_id`. Keep this suite library-only and deterministic; no LiveView or router wiring belongs in this plan.</action>
  <verify>
    <automated>mix test test/sigra/admin/users_actions_test.exs test/sigra/admin/audit/query_test.exs --max-failures 1</automated>
  </verify>
  <done>The repo has focused failing-or-green tests that describe the corrected admin attribution contract and the exact normalized filter vocabulary the later explorer/export layers must share.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the shared admin audit query seam and close the admin support-action attribution gap</name>
  <files>lib/sigra/auth.ex, lib/sigra/admin/users/actions.ex, lib/sigra/admin/audit/query.ex, lib/sigra/admin/audit/query_params.ex</files>
  <action>Implement the Phase 30 runtime foundation additively. Create `Sigra.Admin.Audit.Query` as the admin-owned wrapper over `Sigra.Audit.Query` and `Sigra.Audit.Cursor`, with a subject-user helper that can express `(effective_user_id == user_id OR target_id == user_id)` without changing the lower-level canonical filter builder. Create `Sigra.Admin.Audit.QueryParams` using the whitelist-first normalization pattern from `Sigra.Admin.Users.Query`, but for the audit contract: actor, effective user, organization, action/action-prefix, outcome, from/to, cursor, page size, and any scope-derived subject-user fields needed later. In the same task, fix the AUD-01 gap identified in research finding 3 by updating the admin session-support action seam so revoke-one and revoke-all preserve the real admin actor, the target user, and effective user context when they reach Sigra's existing audit pipeline. Reuse the existing Sigra auth/session APIs and existing dual-actor assembly through `Sigra.Audit.scope_fields/1`; do not create a second audit attribution path or a one-off export-only filter format.</action>
  <verify>
    <automated>mix test test/sigra/admin/users_actions_test.exs test/sigra/admin/audit/query_test.exs --max-failures 1</automated>
  </verify>
  <done>Admin support actions now preserve canonical dual-actor attribution, and every later explorer/export route has one shared normalized query/filter module to build on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin action wrapper -> Sigra auth runtime | Support actions cross from admin-facing code into security-sensitive session deletion and audit logging |
| untrusted query string -> admin audit query contract | URL params control evidence queries and must not widen scope or inject unsupported filters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | R | `lib/sigra/admin/users/actions.ex` | mitigate | Pass explicit actor/effective/target audit options through the canonical Sigra session APIs and assert them directly in focused tests. |
| T-30-02 | T | `lib/sigra/admin/audit/query_params.ex` | mitigate | Use whitelist-first normalization with explicit cursor/date parsing so unknown or malformed filters fail closed instead of silently expanding scope. |
| T-30-03 | E | per-user subject query behavior | mitigate | Keep subject-user semantics in one admin-owned query wrapper and cover the `effective_user_id` plus `target_id` union with direct-path tests. |
</threat_model>

<verification>
Run the focused admin action and audit query suites. This plan is complete only when the repo proves the corrected dual-actor attribution and the reusable normalized filter contract without any LiveView code.
</verification>

<success_criteria>
- AUD-01's admin support-action attribution gap is closed at the library seam.
- The repo has one normalized audit filter contract for explorer and export routes.
- Per-user subject history can include rows matched through `effective_user_id` before any UI is built.
</success_criteria>

<output>
After completion, create `.planning/phases/30-audit-exploration-and-export/30-audit-exploration-and-export-01-SUMMARY.md`
</output>
