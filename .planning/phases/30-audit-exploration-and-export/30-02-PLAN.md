---
phase: 30-audit-exploration-and-export
plan: 2
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - lib/sigra/admin/audit/explorer.ex
  - lib/sigra/admin/audit/presenter.ex
  - lib/sigra/admin/live/audit_index_live.ex
  - test/example/lib/example_web/router.ex
  - priv/templates/sigra.install/admin/router_injection.ex
  - test/example/lib/example_web/components/admin_shell.ex
  - priv/templates/sigra.install/admin/components/admin_shell.ex
  - test/example/test/example_web/live/admin_audit_index_live_test.exs
  - test/example/test/example_web/admin_shell_test.exs
autonomous: true
requirements:
  - AUD-02
  - AUD-03
must_haves:
  truths:
    - "Admins can open a global or organization-scoped audit explorer from normal admin navigation and keep filters in the URL through LiveView `handle_params/3` rather than transient form state."
    - "Organization-scoped explorer routes enforce the same Phase 27 and Phase 28 structural scope guarantees as the rest of the admin surface."
    - "Audit rows visibly distinguish impersonation activity from normal user activity using canonical columns and action names instead of raw metadata inspection."
  artifacts:
    - path: "lib/sigra/admin/live/audit_index_live.ex"
      provides: "Router-mounted global and organization audit explorer LiveView"
    - path: "lib/sigra/admin/audit/explorer.ex"
      provides: "Shared list orchestration over normalized query params and scope-safe audit queries"
    - path: "lib/sigra/admin/audit/presenter.ex"
      provides: "Canonical row labels, impersonation badges, and stable display helpers for audit rows"
    - path: "test/example/lib/example_web/router.ex"
      provides: "Global and organization audit explorer routes mounted under existing admin scopes"
    - path: "test/example/lib/example_web/components/admin_shell.ex"
      provides: "Visible Audit navigation entry wired into the existing shell seam"
  key_links:
    - from: "lib/sigra/admin/live/audit_index_live.ex"
      to: "lib/sigra/admin/audit/explorer.ex"
      via: "URL params normalize into shared explorer list calls"
      pattern: "Sigra\\.Admin\\.Audit\\.Explorer"
    - from: "lib/sigra/admin/audit/explorer.ex"
      to: "lib/sigra/admin/audit/query.ex"
      via: "shared scope-safe query execution over canonical runtime"
      pattern: "Sigra\\.Admin\\.Audit\\.Query"
    - from: "test/example/lib/example_web/router.ex"
      to: "priv/templates/sigra.install/admin/router_injection.ex"
      via: "generated/example global and organization route parity"
      pattern: "/admin/audit|/admin/organizations/:org/audit"
    - from: "test/example/lib/example_web/components/admin_shell.ex"
      to: "priv/templates/sigra.install/admin/components/admin_shell.ex"
      via: "generated/example shell parity for Audit navigation"
      pattern: "Audit"
nyquist_compliant: true
---

<objective>
Build the main global and organization-scoped audit explorer surfaces on top of the shared Phase 30 runtime contract, then wire them into generated and example admin navigation.

Purpose: AUD-02 and AUD-03 require a real investigation UI, but it must preserve the Phase 27/28 scope split and make impersonation obvious without parsing raw metadata.
Output: Shared explorer service/presenter, a router-mounted audit index LiveView, route wiring, shell navigation, and focused example tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-audit-exploration-and-export/30-RESEARCH.md
@.planning/phases/30-audit-exploration-and-export/30-PATTERNS.md
@.planning/phases/30-audit-exploration-and-export/30-VALIDATION.md
@.planning/phases/28-user-operations-surface/28-CONTEXT.md
@.planning/phases/29-secure-impersonation/29-CONTEXT.md
@.planning/phases/30-audit-exploration-and-export/30-01-PLAN.md
@/Users/jon/projects/sigra/lib/sigra/admin/live/users_index_live.ex
@/Users/jon/projects/sigra/test/example/lib/example_web/router.ex
@/Users/jon/projects/sigra/test/example/lib/example_web/components/admin_shell.ex
@/Users/jon/projects/sigra/priv/templates/sigra.install/admin/router_injection.ex
@/Users/jon/projects/sigra/priv/templates/sigra.install/admin/components/admin_shell.ex

<interfaces>
From `lib/sigra/admin/live/users_index_live.ex`:

```elixir
def mount(_params, _session, socket)
def handle_params(params, _uri, socket)
```

From `test/example/lib/example_web/router.ex`:

```elixir
scope "/", alias: false do
  pipe_through [:browser, :require_authenticated, :admin_global]
end

scope "/admin/organizations/:org", alias: false do
  pipe_through [:browser, :require_authenticated, :admin_organization]
end
```

From `test/example/lib/example_web/components/admin_shell.ex`:

```elixir
def admin_shell(assigns)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add example tests for global and organization audit explorer routing, filters, and visible impersonation labels</name>
  <files>test/example/test/example_web/live/admin_audit_index_live_test.exs, test/example/test/example_web/admin_shell_test.exs</files>
  <behavior>
    - Test 1: per AUD-02, `/admin/audit` and `/admin/organizations/:org/audit` load through URL params and preserve filter state across pagination/sort/filter changes.
    - Test 2: per research finding 4, organization-scoped explorer routes fail closed outside the resolved organization scope rather than widening to cross-org data.
    - Test 3: per AUD-03, impersonation rows render a visible actor-versus-effective-user distinction without exposing raw metadata blobs as the primary cue.
    - Test 4: the shell exposes a real Audit navigation entry in both generated/example admin chrome instead of the current placeholder label.
  </behavior>
  <action>Create focused example tests before implementation. Add `admin_audit_index_live_test.exs` to cover the global route, organization route, URL-driven filter params, and visible impersonation labeling on representative rows. Extend `admin_shell_test.exs` so the current placeholder Audit item becomes a required interactive navigation entry and preserves the existing scope-aware shell behavior from Phases 27-29. Keep browser automation out of this plan; this suite should prove the server-rendered route, query-string, and impersonation-display contracts first.</action>
  <verify>
    <automated>cd test/example && mix test test/example_web/live/admin_audit_index_live_test.exs test/example_web/admin_shell_test.exs --max-failures 1</automated>
  </verify>
  <done>Focused example tests describe the global/org explorer routes, URL-param behavior, impersonation row labeling, and Audit navigation handoff before the LiveView and shell code are written.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the scope-safe global and organization audit explorer surfaces plus generated/example navigation wiring</name>
  <files>lib/sigra/admin/audit/explorer.ex, lib/sigra/admin/audit/presenter.ex, lib/sigra/admin/live/audit_index_live.ex, test/example/lib/example_web/router.ex, priv/templates/sigra.install/admin/router_injection.ex, test/example/lib/example_web/components/admin_shell.ex, priv/templates/sigra.install/admin/components/admin_shell.ex</files>
  <action>Implement the shared explorer UI using the route and query patterns already established by `Sigra.Admin.Live.UsersIndexLive`. Create `Sigra.Admin.Audit.Explorer` to list scoped audit rows over the normalized filter contract from Plan 30-01, and create `Sigra.Admin.Audit.Presenter` to derive operator-facing row copy, actor/effective-user labels, and impersonation badges from canonical `actor_id`, `effective_user_id`, `organization_id`, and explicit `admin.impersonation.*` actions. Build `Sigra.Admin.Live.AuditIndexLive` as the global plus org-scoped explorer LiveView mounted under the existing admin route split: `/admin/audit` and `/admin/organizations/:org/audit`. Preserve Phase 27/28 structural guarantees for org scope instead of fetching globally and trimming in the template. Wire the same routes and navigation into both `test/example/lib/example_web/router.ex` and `priv/templates/sigra.install/admin/router_injection.ex`, and convert the Audit shell entry in both example and generated `admin_shell.ex` from placeholder text into a real scope-aware link. Keep filters URL-addressable through `handle_params/3`; do not introduce client-only state or a second navigation model.</action>
  <verify>
    <automated>cd test/example && mix test test/example_web/live/admin_audit_index_live_test.exs test/example_web/admin_shell_test.exs --max-failures 1</automated>
  </verify>
  <done>Admins can open global and org audit explorers through normal admin navigation, keep the filter state in the URL, and visually distinguish impersonation activity without reading raw metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router scope -> audit explorer list service | Org-scoped admin routes must keep server-side scope enforcement intact |
| audit row data -> presenter/display layer | Security evidence is rendered for operators and must not depend on opaque metadata parsing |
| generated/example shell navigation -> mounted routes | Navigation must point at real scoped explorer routes without creating off-scope entry paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | E | org-scoped audit explorer routes | mitigate | Reuse the existing admin route split and scope-aware query execution so org paths cannot widen into global audit data. |
| T-30-05 | I | audit row presentation | mitigate | Derive impersonation indicators from canonical actor/effective-user columns and explicit action names rather than exposing metadata blobs. |
| T-30-06 | T | generated/example navigation wiring | mitigate | Keep route and shell parity between example and templates so Audit links always land inside the intended admin scopes. |
</threat_model>

<verification>
Run the focused example LiveView and shell tests after wiring the routes and navigation. Confirm that both global and organization explorers load, filters stay in the URL, and impersonation rows are visibly labeled.
</verification>

<success_criteria>
- Global and org audit explorers are mounted under the existing admin scope split.
- Audit navigation is real and scope-aware in both generated and example shells.
- Impersonation rows are readable from canonical columns rather than raw metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/30-audit-exploration-and-export/30-audit-exploration-and-export-02-SUMMARY.md`
</output>
