---
phase: 46
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/uat-evidence/v1.4/GA-03/steps.md
  - .planning/uat-evidence/v1.4/GA-03/waiver.md
  - .planning/v1.4-GA-UAT.md
autonomous: false
requirements:
  - GA-03
must_haves:
  truths:
    - "Matrix row **GA-03** is **Executed**, **Waived**, or **Blocked** with evidence under `.planning/uat-evidence/v1.4/GA-03/` and **Sigra.OAuthTest** baseline cited."
  artifacts:
    - path: ".planning/uat-evidence/v1.4/GA-03/steps.md"
      provides: "Live Google checklist or waiver pointer"
nyquist_compliant: true
---

<objective>
Close **GA-03**: exercise **live Google OAuth** flows per **REQUIREMENTS.md** **or** record a formal **Waived** / **Blocked** posture; prove **`Sigra.OAuthTest`** baseline green; update `.planning/v1.4-GA-UAT.md` row **GA-03**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-46-10 | Information disclosure | OAuth client secrets committed | high | Grep acceptance forbids `client_secret`, raw refresh tokens |
| T-46-11 | Spoofing | Matrix claims live Google without `steps.md` evidence | medium | Executed path requires dated **Record** section updates in `steps.md` |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Run OAuth machine contract tests</name>
  <files>test/sigra/oauth/oauth_test.exs</files>
  <read_first>
    - test/sigra/oauth/oauth_test.exs
    - docs/uat-ci-coverage.md
    - .planning/uat-evidence/v1.4/GA-03/README.md
  </read_first>
  <action>
    Run: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/sigra/oauth/oauth_test.exs`

    Confirm module under test matches library contract (**MockStrategy** / authorize URL / callback) per `GA-03/README.md`.
  </action>
  <acceptance_criteria>
    - The `mix test test/sigra/oauth/oauth_test.exs` command exits 0.
    - `grep -q "defmodule Sigra.OAuthTest" test/sigra/oauth/oauth_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    Re-run the same command; exit 0.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Human path — steps.md or waiver.md</name>
  <files>.planning/uat-evidence/v1.4/GA-03/steps.md, .planning/uat-evidence/v1.4/GA-03/waiver.md</files>
  <read_first>
    - .planning/uat-evidence/v1.4/GA-03/steps.md
    - .planning/uat-evidence/v1.4/GA-03/waiver.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    **Executed path:** Document register → login → provider linking / email-match confirmation in `steps.md` with **timestamps**, **outcomes**, and **build SHA**. Reference env vars by **name** only (`GOOGLE_CLIENT_ID`, etc.) — never paste secret values.

    **Waived / Blocked path:** Complete `waiver.md` rationale table; for **Blocked**, describe external dependency (e.g. no test Google project) in **Notes** and keep matrix honest.
  </action>
  <acceptance_criteria>
    - **Executed:** `grep -qE "(PASS|FAIL|BLOCKED|SUCCESS)" .planning/uat-evidence/v1.4/GA-03/steps.md` exits 0.
    - **Waived:** `grep -qE "^\\| \\*\\*reason\\*\\* \\|" .planning/uat-evidence/v1.4/GA-03/waiver.md` exits 0 AND `grep -v "^#" .planning/uat-evidence/v1.4/GA-03/waiver.md | grep -qv "^$"` shows body text beyond templates only (or simpler: `grep -c "|" .planning/uat-evidence/v1.4/GA-03/waiver.md` is ≥ 4).
    - `grep -i "client_secret" .planning/uat-evidence/v1.4/GA-03/steps.md .planning/uat-evidence/v1.4/GA-03/waiver.md`; exit code **1**.
  </acceptance_criteria>
  <verify>
    Manual read of `steps.md` / `waiver.md` for secret patterns.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Update matrix row GA-03</name>
  <files>.planning/v1.4-GA-UAT.md</files>
  <read_first>
    - .planning/v1.4-GA-UAT.md
    - .planning/uat-evidence/v1.4/GA-03/README.md
  </read_first>
  <action>
    Update the `| GA-03 |` table row: **Status** ∈ {Executed, Waived, Blocked}; fill **Date**, **Owner**, **Environment**; keep **Evidence_link** pointing to `.planning/uat-evidence/v1.4/GA-03/`; **CI_substitute** must still mention **`Sigra.OAuthTest`** and MockStrategy language consistent with current row.
  </action>
  <acceptance_criteria>
    - `grep -qE "^\\| GA-03 \\| (Executed|Waived|Blocked) \\|" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep "^| GA-03 |" .planning/v1.4-GA-UAT.md | grep -q "Sigra.OAuthTest"` exits 0.
  </acceptance_criteria>
  <verify>
    `grep "^| GA-03 |" .planning/v1.4-GA-UAT.md`
  </verify>
</task>

</tasks>

<verification>
1. `mix test test/sigra/oauth/oauth_test.exs` exits 0.
2. Matrix row GA-03 grep checks pass.
3. No `client_secret` in GA-03 evidence markdown.
</verification>

<success_criteria>
- GA-03 is not **Pending** in the canonical matrix.
- Machine + human story are consistent.
</success_criteria>
