---
phase: 46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/uat-evidence/v1.4/GA-02/steps.md
  - .planning/uat-evidence/v1.4/GA-02/waiver.md
  - .planning/v1.4-GA-UAT.md
autonomous: false
requirements:
  - GA-02
must_haves:
  truths:
    - "Matrix row **GA-02** shows **Executed**, **Waived**, or **Blocked** (not **Pending**) with **Date**, **Owner**, and **Evidence_link** consistent with `.planning/uat-evidence/v1.4/GA-02/`."
    - "HTML machine baseline tests for the same SHA were run (or explicitly waived with compensating CI citation in **Notes** / waiver)."
  artifacts:
    - path: ".planning/uat-evidence/v1.4/GA-02/steps.md"
      provides: "Human MUA checklist record or empty if waiver-only path documented in waiver.md"
nyquist_compliant: true
---

<objective>
Close **GA-02** in `.planning/v1.4-GA-UAT.md`: complete human email visual QA **or** a formal **Waived** record with compensating controls; prove HTML **machine baseline** green for the claimed SHA; update matrix row **GA-02** and keep evidence under **`.planning/uat-evidence/v1.4/GA-02/`**.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-46-01 | Tampering | Matrix shows **Executed** without matching `steps.md` / waiver | high | Acceptance greps tie Status to filled evidence or signed waiver table |
| T-46-02 | Information disclosure | Screenshots or notes leak magic-link secrets | high | `read_first` cites **D-38-P04**; acceptance greps forbid `client_secret`, `Bearer ` in edited evidence files |
| T-46-03 | Repudiation | Missing Owner/Date on transition | medium | Matrix columns populated for non-Pending |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Run GA-02 HTML machine baseline</name>
  <files>test/example/test/example/accounts/emails_security_html_test.exs, test/example/test/example/accounts/emails_lifecycle_html_test.exs</files>
  <read_first>
    - CLAUDE.md
    - mix.exs
    - .planning/v1.4-GA-UAT.md
    - .planning/uat-evidence/v1.4/GA-02/README.md
    - test/example/test/example/accounts/emails_security_html_test.exs
    - test/example/test/example/accounts/emails_lifecycle_html_test.exs
  </read_first>
  <action>
    Example tests are **not** loaded from the library root `mix test` (see root `mix.exs` `test_load_filters`). Run from the **example app** project directory:

    `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example/accounts/emails_security_html_test.exs test/example/accounts/emails_lifecycle_html_test.exs`

    Record the **git SHA** used at repo root (`git rev-parse HEAD`) in **Notes** / **Record** table.
  </action>
  <acceptance_criteria>
    - The `cd test/example && ... mix test test/example/accounts/emails_security_html_test.exs test/example/accounts/emails_lifecycle_html_test.exs` compound command exits 0 (capture log path or CI URL in `GA-02/steps.md` **Record** table or **Notes**).
    - `git rev-parse HEAD` output is copied into either `.planning/v1.4-GA-UAT.md` header `Git SHA:` line (if filling placeholders this phase) or `GA-02/steps.md` record table column **Run / build ref**.
  </acceptance_criteria>
  <verify>
    Re-run the same `mix test` command; exit 0.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Human path — fill steps.md OR formal waiver.md</name>
  <files>.planning/uat-evidence/v1.4/GA-02/steps.md, .planning/uat-evidence/v1.4/GA-02/waiver.md</files>
  <read_first>
    - .planning/uat-evidence/v1.4/GA-02/steps.md
    - .planning/uat-evidence/v1.4/GA-02/waiver.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    **Executed path:** Complete every checkbox in `steps.md` preconditions and client sections; fill the **Record** table with at least one row: **Date** (YYYY-MM-DD), **Owner** (handle or name), **Outcome** (`PASS` / `FAIL` / `PASS WITH ISSUES`), **Run / build ref** (SHA or CI URL), **Notes** (redacted).

    **Waived path:** Replace placeholder pipes in `waiver.md` table body with real text for **reason**, **compensating controls**, **residual risk**, **expiry_or_next_trigger**, **owner**, **date** (each cell non-empty); add one line at top of `steps.md` pointing readers to `waiver.md` for GA-02 status.

    Do **not** do both as contradictory: either steps show a real run **or** waiver documents why MUAs were not run.
  </action>
  <acceptance_criteria>
    - **Executed:** `grep -qE "[0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/uat-evidence/v1.4/GA-02/steps.md` exits 0 AND `grep -qi "PASS\\|FAIL" .planning/uat-evidence/v1.4/GA-02/steps.md` exits 0.
    - **Waived:** `grep -qE "[0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/uat-evidence/v1.4/GA-02/waiver.md` exits 0 AND `grep -qi "compensating\\|Litmus\\|Email on Acid\\|screenshot" .planning/uat-evidence/v1.4/GA-02/waiver.md` exits 0.
    - One path only: if **Waived** acceptance passes, matrix row must be **Waived** (not **Executed**); if **Executed** acceptance passes, matrix row must be **Executed**.
    - `grep -iE "client_secret|Bearer [A-Za-z0-9_-]{20,}" .planning/uat-evidence/v1.4/GA-02/steps.md .planning/uat-evidence/v1.4/GA-02/waiver.md` exits **1** (no matches).
  </acceptance_criteria>
  <verify>
    Maintainer confirms **Executed** vs **Waived** choice matches milestone intent.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Update matrix row GA-02</name>
  <files>.planning/v1.4-GA-UAT.md</files>
  <read_first>
    - .planning/v1.4-GA-UAT.md
    - .planning/uat-evidence/v1.4/GA-02/README.md
  </read_first>
  <action>
    In the pipe table, locate the row starting with `| GA-02 |`.

    Set **Status** to exactly **`Executed`** or **`Waived`** or **`Blocked`** (Pascal case as existing GA-01 row).

    Set **Date** to ISO date of decision.

    Set **Owner** to responsible party (e.g. `Sigra` or GitHub handle).

    Set **Environment** to short truth (e.g. `Gmail/Outlook/Apple Mail web` or `Waived — see waiver.md`).

    Keep **Evidence_link** as `.planning/uat-evidence/v1.4/GA-02/` or more specific path if you add a dated README under that folder.

    **Notes** must cite machine baseline (`EmailsSecurityHtmlTest`, `EmailsLifecycleHtmlTest`, `example_unit_smoke`) and human outcome one line.
  </action>
  <acceptance_criteria>
    - `grep -qE "^\\| GA-02 \\| (Executed|Waived|Blocked) \\|" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep "^| GA-02 |" .planning/v1.4-GA-UAT.md` shows non-empty **Date** and **Owner** cells (pipe segments between `|` not only `—` for those columns — literal em dash allowed only where still true; for closure, replace `—` with real values).
    - Row still contains substring `EmailsSecurityHtmlTest` in **CI_substitute** column.
  </acceptance_criteria>
  <verify>
    `grep "^| GA-02 |" .planning/v1.4-GA-UAT.md`
  </verify>
</task>

</tasks>

<verification>
1. `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example/accounts/emails_security_html_test.exs test/example/accounts/emails_lifecycle_html_test.exs` exits 0 (same as Task 1).
2. Matrix GA-02 row grep from Task 3 passes.
3. No secret patterns in `GA-02/*.md` per acceptance grep.
</verification>

<success_criteria>
- GA-02 is not **Pending** in `.planning/v1.4-GA-UAT.md`.
- Evidence folder reflects **Executed** or **Waived** truth.
</success_criteria>
