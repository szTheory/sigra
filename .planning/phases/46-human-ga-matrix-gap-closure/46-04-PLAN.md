---
phase: 46
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - .planning/v1.4-GA-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - GA-05
must_haves:
  truths:
    - "`.planning/v1.4-GA-UAT.md` row **GA-05** is **Executed**, **Waived**, or **Blocked**; matrix header **Hex package:** and **Git SHA:** are filled with real values **or** explicit bracketed waiver sentence in **Notes** for pre-tag state."
    - "**REQUIREMENTS.md** checkboxes for **GA-02..GA-05** match matrix (checked only when matrix shows **Executed** or **Waived**)."
    - "**ROADMAP.md** phase **46** narrative does not claim completion while any of GA-02..GA-05 remain **Pending** in the matrix."
  artifacts:
    - path: ".planning/v1.4-GA-UAT.md"
      provides: "Canonical GA closure"
nyquist_compliant: true
---

<objective>
Finish **GA-05** consolidation: align **Hex package** / **Git SHA** header, set matrix row **GA-05** to non-Pending, update **REQUIREMENTS.md** traceability checkboxes for GA-02..GA-05, and reconcile **ROADMAP.md** phase 46 line with matrix truth (**v1.4-MILESTONE-AUDIT** gap closure).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-46-30 | Tampering | REQUIREMENTS checked while matrix still Pending | high | Grep gate: checkboxes flip only after matrix rows closed |
| T-46-31 | Information disclosure | Real Hex publish token in matrix | medium | **Hex package:** line uses **public** package name + version only |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Verify dependencies GA-02..GA-04 are closed</name>
  <files>.planning/v1.4-GA-UAT.md</files>
  <read_first>
    - .planning/v1.4-GA-UAT.md
    - .planning/phases/46-human-ga-matrix-gap-closure/46-01-PLAN.md
    - .planning/phases/46-human-ga-matrix-gap-closure/46-02-PLAN.md
    - .planning/phases/46-human-ga-matrix-gap-closure/46-03-PLAN.md
  </read_first>
  <action>
    Confirm three grep conditions — do not start GA-05 narrative edits until all pass:

    1. `grep -qE "^\\| GA-02 \\| (Executed|Waived|Blocked) \\|" .planning/v1.4-GA-UAT.md`
    2. `grep -qE "^\\| GA-03 \\| (Executed|Waived|Blocked) \\|" .planning/v1.4-GA-UAT.md`
    3. `grep -qE "^\\| GA-04 \\| (Executed|Waived|Blocked) \\|" .planning/v1.4-GA-UAT.md`

    If any fail, return to plans **01–03** — do not fake GA-05 **Executed**.
  </action>
  <acceptance_criteria>
    - All three `grep` commands above exit 0.
  </acceptance_criteria>
  <verify>
    `grep "^| GA-0[2-4] |" .planning/v1.4-GA-UAT.md`
  </verify>
</task>

<task type="execute">
  <name>Task 2: Fill matrix header + GA-05 row</name>
  <files>.planning/v1.4-GA-UAT.md</files>
  <read_first>
    - .planning/v1.4-GA-UAT.md
    - mix.exs
  </read_first>
  <action>
    1. Replace **`[FILL AT TAG]`** placeholders:
       - **Hex package:** set to **`sigra` `0.2.0`** (from root `mix.exs` `@version` and `:app` — adjust if version bump before close) or, if Hex publish lags the git SHA, use exact literal: `sigra (Hex pre-release — see Git SHA)` replacing the placeholder.
       - **Git SHA:** set to output of `git rev-parse HEAD` at close time (full 40-char hex).

    2. Update **GA-05** row: **Status** = **`Executed`** when consolidation is true (all GA-02..04 non-Pending and this file updated); **Date** / **Owner** filled; **Evidence_link** may remain `_(this file)_` or change to `.planning/v1.4-GA-UAT.md` — pick one and keep consistent with other rows’ style.

    3. **Notes** for GA-05: one line stating `docs/uat-ci-coverage.md` remains authoritative for SEED ↔ CI map.
  </action>
  <acceptance_criteria>
    - `grep -v "\\[FILL AT TAG\\]" .planning/v1.4-GA-UAT.md | grep -q "Git SHA:"` — file must not contain literal `[FILL AT TAG]` after edit: `grep "\\[FILL AT TAG\\]" .planning/v1.4-GA-UAT.md` exits **1**.
    - `grep -qE "^\\| GA-05 \\| (Executed|Waived|Blocked) \\|" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep "^| GA-05 |" .planning/v1.4-GA-UAT.md | grep -q "uat-ci-coverage"` exits 0.
  </acceptance_criteria>
  <verify>
    `head -n 20 .planning/v1.4-GA-UAT.md`
  </verify>
</task>

<task type="execute">
  <name>Task 3: REQUIREMENTS.md GA-02..GA-05 checkboxes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/v1.4-GA-UAT.md
  </read_first>
  <action>
    For each of GA-02, GA-03, GA-04, GA-05 in **REQUIREMENTS.md** body list at top:

    - If matrix row shows **Executed** or **Waived**, change corresponding `- [ ]` to `- [x]`.
    - If matrix row shows **Blocked**, keep `[ ]` **and** add one short suffix in the same bullet line: `(Blocked — see v1.4-GA-UAT.md GA-0X row)` — do not check the box.

    Update traceability table rows for GA-02..GA-05 if present (Phase column may stay **46**; Status column should align with matrix vocabulary or `Done` per house style — pick **one** style and make table self-consistent).
  </action>
  <acceptance_criteria>
    - If matrix GA-02 is Executed or Waived: `grep -q '^- \[x\] \*\*GA-02\*\*' .planning/REQUIREMENTS.md` exits 0.
    - Same for GA-03, GA-04, GA-05: `grep -q '^- \[x\] \*\*GA-03\*\*'` … `GA-05` when those matrix rows are Executed or Waived.
    - If any GA-0X is **Blocked**, that bullet must stay `- [ ]` and include substring `Blocked` in the same line.
  </acceptance_criteria>
  <verify>
    `grep "GA-0" .planning/REQUIREMENTS.md | head -n 20`
  </verify>
</task>

<task type="execute">
  <name>Task 4: ROADMAP phase 46 bookkeeping</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/v1.4-MILESTONE-AUDIT.md
  </read_first>
  <action>
    When GA-02..GA-05 are all non-Pending and REQUIREMENTS updated:

    1. Add **✅** and date to phase **46** table row in **ROADMAP.md** (same style as phases 41–43), e.g. `| **46** ✅ (2026-04-21) |`.

    2. Ensure v1.4 milestone bullet does not claim “GA matrix complete” elsewhere while matrix still had Pending — after edit, grep audit sample lines if needed.

    If any GA remains **Blocked** by explicit decision, **do not** add ✅ to phase 46; instead extend **Goal** cell with `Blocked items: GA-0X` suffix.
  </action>
  <acceptance_criteria>
    - If all GA-02..05 matrix rows are Executed or Waived: `grep -qE "\\*\\*46\\*\\* ✅" .planning/ROADMAP.md` exits 0.
    - If any is Blocked: `grep "^| \\*\\*46\\*\\*" .planning/ROADMAP.md` line contains substring `Blocked`.
  </acceptance_criteria>
  <verify>
    `grep "^| \\*\\*46\\*" .planning/ROADMAP.md`
  </verify>
</task>

</tasks>

<verification>
1. No `[FILL AT TAG]` in `.planning/v1.4-GA-UAT.md`.
2. All GA-02..GA-05 rows: non-Pending status grep.
3. REQUIREMENTS GA lines reflect matrix.
4. ROADMAP phase 46 matches completion rule in Task 4.
</verification>

<success_criteria>
- Milestone audit gap “matrix Pending vs ROADMAP complete” is resolved.
- Single canonical truth in `.planning/v1.4-GA-UAT.md`.
</success_criteria>
