---
phase: 42
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.4-GA-UAT.md
autonomous: true
requirements:
  - GA-02
  - GA-03
  - GA-04
  - GA-05
must_haves:
  truths:
    - "`.planning/v1.4-GA-UAT.md` exists and is the single canonical v1.4 GA table with columns `Requirement`, `Status`, `Date`, `Owner`, `Environment`, `Evidence_link`, `Expiry`, `Notes`, `CI_substitute`, `Surface` (D-42-01)."
    - "Rows exist for **GA-01** (pointer to Phase 41 / CI proof only), **GA-02**, **GA-03**, **GA-04**, **GA-05**; statuses use **Executed / Waived / Blocked / Pending** vocabulary."
  artifacts:
    - path: ".planning/v1.4-GA-UAT.md"
      provides: "v1.4 human GA matrix"
nyquist_compliant: true
---

<objective>
Author **`.planning/v1.4-GA-UAT.md`**: canonical v1.4 GA matrix with extended columns, five requirement rows (GA-01 pointer + GA-02..GA-05), intro text aligned to **D-42-01** / **D-38-08**, and a **Changelog pointer** subsection mirroring v1.3 style.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-42-01 | Tampering | Matrix claims **Executed** without durable evidence path | high | Every row includes `Evidence_link` pointing under `.planning/uat-evidence/v1.4/` or explicit waiver doc path; GA-01 links to Phase 41 test paths only. |
| T-42-02 | Repudiation | Missing **Owner** / **Date** on status transitions | medium | Required columns populated or `Pending` with honest Notes. |
| T-42-03 | Information disclosure | Secrets pasted into matrix Notes | medium | Notes reference redacted transcripts per **D-38-P04**; no live tokens in matrix. |
</threat_model>

<context>
@.planning/phases/42-human-ga-matrix-evidence/42-CONTEXT.md
@.planning/phases/42-human-ga-matrix-evidence/42-RESEARCH.md
@.planning/phases/42-human-ga-matrix-evidence/42-PATTERNS.md
@.planning/v1.3-HUMAN-UAT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create v1.4-GA-UAT.md matrix skeleton</name>
  <files>.planning/v1.4-GA-UAT.md</files>
  <read_first>
    - .planning/v1.3-HUMAN-UAT.md
    - .planning/phases/42-human-ga-matrix-evidence/42-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    1. Create **new file** `.planning/v1.4-GA-UAT.md` at repo root of `.planning/` (sibling to `v1.3-HUMAN-UAT.md`).
    2. Title: **`# Sigra v1.4 — Human GA UAT (GA-02..GA-05 + GA-01 pointer)`** (exact hash spelling).
    3. Opening paragraph must state: **execute-by-default** (**D-38-01**); evidence under **`.planning/uat-evidence/v1.4/`**; machine baseline cites **`docs/uat-ci-coverage.md`** — copy tone from v1.3 intro but replace SEED-001 eight-pack with **GA-02..05** scope + **GA-01** pointer sentence: links to **`test/example/test/example_web/smoke/backup_code_rotation_test.exs`**, **`example_unit_smoke`**, optional Playwright — **does not** re-prove rotation in Phase 42.
    4. Table header row **exactly** (pipe-separated):

       `| Requirement | Status | Date | Owner | Environment | Evidence_link | Expiry | Notes | CI_substitute | Surface |`

    5. Insert **five** data rows after header + separator:
       - **GA-01** — `Pending` or `Executed` per current truth; `Evidence_link` points to `.planning/uat-evidence/v1.4/GA-01-pointer/README.md` (create in Plan 02) **or** direct test path strings if pointer folder deferred — pick **pointer README path** as canonical target.
       - **GA-02** — `Pending`; `CI_substitute` names **`EmailsSecurityHtmlTest`**, **`EmailsLifecycleHtmlTest`**, **`example_unit_smoke`** (exact class strings); `Surface` mixes **`lib`** / **`host`** as appropriate per CONTEXT.
       - **GA-03** — `Pending`; `CI_substitute` cites **`Sigra.OAuthTest`** / MockStrategy contract language consistent with `docs/uat-ci-coverage.md` SEED-4 row.
       - **GA-04** — `Pending`; `CI_substitute` cites **`getting_started_uat_contract`** job + **`scripts/ci/getting-started-contract.sh`**.
       - **GA-05** — `Pending`; Notes cell states this file **is** the consolidated artifact and links to **`docs/uat-ci-coverage.md`** for SEED map.
    6. Add subsection **`## Changelog pointer`** with body line: **`Release notes may add one line: Human GA outcomes in v1.4-GA-UAT.md.`** (exact sentence).
    7. Header metadata block (after title, before table): include placeholder lines **`Hex package:`** `[FILL AT TAG]` and **`Git SHA:`** `[FILL AT TAG]` per **D-38-P01** / **D-42-01**.
  </action>
  <acceptance_criteria>
    - `test -f .planning/v1.4-GA-UAT.md` exits 0.
    - `grep -q "Human GA UAT (GA-02..GA-05 + GA-01 pointer)" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep -q "| Requirement | Status | Date | Owner | Environment | Evidence_link | Expiry | Notes | CI_substitute | Surface |" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep -q "EmailsSecurityHtmlTest" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep -q "backup_code_rotation_test.exs" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep -q "Release notes may add one line: Human GA outcomes in v1.4-GA-UAT.md." .planning/v1.4-GA-UAT.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && test -f .planning/v1.4-GA-UAT.md && grep -q "CI_substitute" .planning/v1.4-GA-UAT.md'</automated>
  </verify>
  <done>Canonical v1.4 GA matrix file exists with required columns and five rows.</done>
</task>

</tasks>
