---
phase: 42
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docs/uat-ci-coverage.md
  - CHANGELOG.md
autonomous: true
requirements:
  - GA-05
must_haves:
  truths:
    - "`docs/uat-ci-coverage.md` contains a **v1.4 GA** subsection (or equivalent heading) that **cross-links** `.planning/v1.4-GA-UAT.md` and states the **GA-02..05** human residual vs machine substitute split (**D-42-04**)."
    - "`CHANGELOG.md` includes the **exact** one-line pointer: **`Human GA: see v1.4-GA-UAT.md`** (or release-notes sentence equivalent — see acceptance grep)."
  artifacts:
    - path: "docs/uat-ci-coverage.md"
      provides: "SEED / GA ↔ CI map update"
nyquist_compliant: true
---

<objective>
Update **`docs/uat-ci-coverage.md`** with an explicit **v1.4 / GA-02..05** cross-reference to the new matrix (without duplicating merge-blocking job lists as the second source of truth) and add a **CHANGELOG** pointer line for public discoverability.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-42-05 | Tampering | Coverage doc contradicts `.github/workflows/ci.yml` job names | medium | Edits use **grep-verified** job strings copied from existing doc rows or from `ci.yml` read_first; drift pass note in action. |
| T-42-02 | Repudiation | Changelog omits human GA pointer | low | Exact line added under **Unreleased** (or current section) per acceptance grep. |
</threat_model>

<context>
@.planning/v1.4-GA-UAT.md
@docs/uat-ci-coverage.md
@CHANGELOG.md
@.github/workflows/ci.yml
@.planning/phases/42-human-ga-matrix-evidence/42-CONTEXT.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: v1.4 cross-links in uat-ci-coverage.md</name>
  <files>docs/uat-ci-coverage.md</files>
  <read_first>
    - docs/uat-ci-coverage.md
    - .github/workflows/ci.yml
    - .planning/v1.4-GA-UAT.md
  </read_first>
  <action>
    1. Add a new section **`## v1.4 GA (GA-02..GA-05)`** after the existing SEED table (or integrate as a short subsection if maintainers prefer — **must** be discoverable from doc TOC via `##` heading containing **`v1.4`**).
    2. Body must include **relative** markdown link to **`.planning/v1.4-GA-UAT.md`** using path **`../.planning/v1.4-GA-UAT.md`** from `docs/` **OR** repo-root style `.planning/v1.4-GA-UAT.md` — pick **`.planning/v1.4-GA-UAT.md`** as displayed path in monospace backticks (consistent with other planning references in repo docs).
    3. Summarize in **≤8 bullet lines**: GA-02 human = MUA; machine = HTML tests; GA-03 human = live Google; machine = OAuth tests; GA-04 human = clean-machine; machine = getting-started contract; GA-05 = matrix ownership; **do not** remove existing SEED-1..8 table.
    4. If any **job name** is cited for v1.4, it must **substring-match** a `jobs:` entry in **`.github/workflows/ci.yml`** (read_first).
  </action>
  <acceptance_criteria>
    - `grep -q "v1.4" docs/uat-ci-coverage.md` exits 0.
    - `grep -q "v1.4-GA-UAT.md" docs/uat-ci-coverage.md` exits 0.
    - `grep -q "GA-02" docs/uat-ci-coverage.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && grep -q "v1.4-GA-UAT.md" docs/uat-ci-coverage.md'</automated>
  </verify>
  <done>Coverage doc points readers at v1.4 matrix.</done>
</task>

<task type="execute">
  <name>Task 2: CHANGELOG pointer</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/v1.4-GA-UAT.md
  </read_first>
  <action>
    1. Under the top **`## [Unreleased]`** section (create it if missing per Keep a Changelog style used in file), add bullet: **`- Human GA (v1.4): see .planning/v1.4-GA-UAT.md`** — exact substring **`Human GA (v1.4): see .planning/v1.4-GA-UAT.md`** must appear once.
    2. If **`[Unreleased]`** already exists, **append** the bullet; do not duplicate the exact line.
  </action>
  <acceptance_criteria>
    - `grep -q "Human GA (v1.4): see .planning/v1.4-GA-UAT.md" CHANGELOG.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'cd "$(git rev-parse --show-toplevel)" && grep -q "Human GA (v1.4): see .planning/v1.4-GA-UAT.md" CHANGELOG.md'</automated>
  </verify>
  <done>Changelog exposes matrix path.</done>
</task>

</tasks>
