---
phase: 223-get-current-on-hex-terminal-currency-proof
plan: 03
type: execute
wave: 3
depends_on: ["223-02"]
files_modified:
  - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-PROOF.md
  - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-03-SUMMARY.md
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "ci-gate is green on main HEAD — the required CI checks pass, verified live via gh"
    - "Hex latest_stable_version = 1.3.0, verified via verbatim curl|jq"
    - "adopter ~> 1.0 resolves to 1.3.0 (carried verbatim from the 223-02 scratch mix.lock)"
    - "library suite and example suite both run green with verbatim 'N tests, 0 failures' counts"
  artifacts:
    - "223-PROOF.md — standalone release-currency trust bundle in the 215/220 observable-truths format: a claims table where each of the 4 sub-claims is backed by a verbatim command + observed output"
    - "223-03-SUMMARY.md recording the suite commands, verbatim counts, and CI/Hex evidence"
  key_links:
    - "223-02 resolution evidence + live suite counts + live ci-gate + live Hex latest_stable -> aggregated into 223-PROOF.md"
  prohibitions:
    - "MUST NOT record 'it passed' without the exact command + verbatim 'N tests, 0 failures' counts (215 D-03 convention / prohibition table)"
    - "MUST NOT edit product code, tests, or fixtures to make a suite pass — capture the real state"
    - "MUST NOT treat a locally-filtered or pre-commit green as proof; the authoritative signal is the real unfiltered end state (216 SC-5 / D-13 overclaim guard)"
---

<objective>
PROOF-01: Emit the milestone's trust artifact — a standalone `223-PROOF.md`
release-currency proof bundle (D-06) recording the terminal end state with live,
verbatim evidence for all four sub-claims (D-07):
1. `ci-gate` green on `main` HEAD.
2. Hex `latest_stable_version` = `1.3.0`.
3. Adopter `~> 1.0` resolves to `1.3.0` (from the 223-02 raw scratch resolution).
4. Full library + example suites green (verbatim counts, D-08).

Runs LAST (depends_on 223-02, which depends on the 223-01 retire). Uses the
215/220 VERIFICATION observable-truths evidence format: a claims table where each
row cites the exact command and its observed output — never a bare "it passed"
(215 D-03).

Purpose: Give adopters and maintainers a single, self-contained trust record that
Sigra is current and green on Hex.
Output: `223-PROOF.md` + `223-03-SUMMARY.md`.

## Artifacts this phase produces (context)
- `223-PROOF.md` — THIS plan's primary deliverable (the trust bundle).
- `223-VERIFICATION.md` — the standard verifier report, produced DOWNSTREAM by
  `/gsd-verify-phase` (not by this plan). `223-PROOF.md` complements, does not
  replace, it (D-06).
- Verified Hex currency + folded todo (223-01); throwaway scratch resolution
  project (223-02, disposable).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-CONTEXT.md
@.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-02-SUMMARY.md
@.planning/milestones/v1.43-phases/215-terminal-ratification/215-VERIFICATION.md
@scripts/ci/release-post-publish-verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture library + example suite-green evidence (verbatim counts)</name>
  <files>.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-03-SUMMARY.md</files>
  <read_first>
    - CLAUDE.md ("Local development prerequisites" — scripts/db/up.sh + tmp/db.env; example-suite PG env)
    - .planning/milestones/v1.43-phases/215-terminal-ratification/215-VERIFICATION.md (rows 1-5: the verbatim-count shape and skip/exclude classification)
    - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-CONTEXT.md (D-08)
  </read_first>
  <action>
    Run both suites against live Postgres (sanctioned path: `scripts/db/up.sh` then
    `source tmp/db.env`, or localhost:5432 fallback per CLAUDE.md) and record verbatim
    results into 223-03-SUMMARY.md per the 215 D-03 convention:
    (a) Library suite: run `mix test`; record the exact command and the verbatim tail
        count string (e.g. "N doctests, N properties, N tests, 0 failures, N skipped").
    (b) Example suite: run the example app's suite with its Postgres env (per
        CLAUDE.md / 215-02 pattern); record the exact command and verbatim
        "N tests, 0 failures".
    Classify any skips/excludes as legitimate gates (as 215 did) — do NOT record a
    bare "it passed", and do NOT edit product code/tests/fixtures to force green
    (prohibition). If either suite is non-zero-failures, record the real failure —
    do not overclaim.
  </action>
  <verify>
    <automated>grep -Eq '[0-9]+ tests, 0 failures' .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    223-03-SUMMARY.md records the exact library-suite and example-suite commands and
    their verbatim "N tests, 0 failures" counts, with skips classified — no bare
    "it passed".
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Capture live ci-gate-on-main and Hex latest_stable evidence</name>
  <files>.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-CONTEXT.md (D-07 sub-claims 1 and 2; Claude's-Discretion note on existing-vs-fresh ci-gate run)
    - scripts/ci/release-post-publish-verify.sh (the curl + JSON-evidence pattern for the Hex API)
  </read_first>
  <action>
    Record two live-evidence sub-claims into 223-03-SUMMARY.md:
    (1) ci-gate green on `main` HEAD — capture via `gh` against the current `main`
        HEAD (e.g. `gh run list --branch main --workflow <ci-gate> -L 1` for the
        latest run, and/or `gh pr checks <PR> --required` if the terminal PR is the
        signal of record). Either an existing green run at current `main` HEAD or a
        fresh observation is acceptable (Claude's Discretion), as long as it reflects
        current `main` HEAD — record the run id/URL and the green status verbatim.
    (2) Hex `latest_stable_version` = `1.3.0` — run
        `curl -s https://hex.pm/api/packages/sigra | jq '.latest_stable_version, .retirements'`
        and record the exact command + verbatim output (confirming `1.3.0` and
        `1.20.0` retired). This re-queries live Hex per D-02 / 221 D-16.
  </action>
  <verify>
    <automated>curl -s https://hex.pm/api/packages/sigra | jq -e '.latest_stable_version == "1.3.0"' &amp;&amp; grep -qi 'ci-gate\|gh run\|gh pr checks' .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    223-03-SUMMARY.md records (1) the ci-gate-on-`main`-HEAD evidence (run id/URL +
    green status, verbatim) and (2) the verbatim `curl … | jq` showing Hex
    `latest_stable_version` = `1.3.0`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Emit the 223-PROOF.md trust bundle</name>
  <files>.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-PROOF.md</files>
  <read_first>
    - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-03-SUMMARY.md (suite + CI/Hex evidence from Tasks 1-2)
    - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-02-SUMMARY.md (PUB-05 adopter resolution evidence)
    - .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-01-SUMMARY.md (retire pre/post evidence)
    - .planning/milestones/v1.43-phases/215-terminal-ratification/215-VERIFICATION.md (observable-truths table format)
    - .planning/milestones/v1.44-phases/220-terminal-ratification/220-VERIFICATION.md (evidence-format precedent)
  </read_first>
  <action>
    Write a standalone `223-PROOF.md` in the phase dir (D-06) — the milestone's
    release-currency trust bundle — using the 215/220 observable-truths format: a
    claims table where every row is backed by a verbatim command + its observed
    output (never a bare "it passed", 215 D-03). Record all four PROOF-01 sub-claims
    (D-07), sourcing each from the SUMMARYs already produced:
    1. `ci-gate` green on `main` HEAD — run id/URL + green status (from 223-03).
    2. Hex `latest_stable_version` = `1.3.0`, `1.20.0` retired — verbatim `curl | jq`
       (from 223-01 post-retire + 223-03).
    3. Adopter `~> 1.0` -> `1.3.0` — verbatim scratch `mix deps.get` command +
       `mix.lock` `:sigra` entry, with the D-05 no-filter attestation (from 223-02).
    4. Library + example suites green — exact commands + verbatim
       "N tests, 0 failures" counts (from 223-03).
    Include a short header stating scope (release-ops verification; no code changes;
    v1.2.0/v1.3.0 already live from Phase 221) and note the reversible `--unretire`
    fallback (D-03). State that `223-PROOF.md` complements — does not replace — the
    standard `223-VERIFICATION.md` produced downstream by the verifier.
  </action>
  <verify>
    <automated>test -f .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-PROOF.md &amp;&amp; grep -q '1.3.0' .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-PROOF.md &amp;&amp; grep -Eq '[0-9]+ tests, 0 failures' .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-PROOF.md &amp;&amp; grep -qi 'ci-gate\|gh run\|gh pr checks' .planning/phases/223-get-current-on-hex-terminal-currency-proof/223-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    223-PROOF.md exists as an observable-truths claims table covering all four
    PROOF-01 sub-claims, each backed by a verbatim command + observed output, plus
    scope header and `--unretire` fallback note.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local suite run / gh / hex.pm | Read-only observation of test, CI, and registry state; this phase changes no source and adds no runtime surface. |

## STRIDE Threat Register

Non-code, release-ops verification — the deliverable is an evidence bundle. No
source changed, no attack surface added.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-223-04 | Repudiation | trust-bundle integrity | medium | mitigate | Every claim in 223-PROOF.md is backed by a verbatim command + observed output (215 D-03); no bare "it passed", so claims are independently reproducible. |
| T-223-05 | Spoofing | overclaimed green | medium | mitigate | Authoritative signal is real, unfiltered end state on live Hex / real adopter resolution (216 SC-5 / D-13); pre-commit or locally-filtered greens are not accepted as proof. |
| T-223-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs; no legitimacy audit required. |
</threat_model>

<verification>
- 223-03-SUMMARY.md holds verbatim library + example suite counts and ci-gate/Hex evidence.
- 223-PROOF.md aggregates all four PROOF-01 sub-claims, each with verbatim command + output.
- Live Hex confirms `latest_stable_version` = `1.3.0`; adopter resolution carried from 223-02.
- No bare "it passed"; no product code edited to force green.
</verification>

<success_criteria>
`223-PROOF.md` stands as the milestone's trust artifact — ci-gate green on `main`,
Hex current at `1.3.0`, adopter `~> 1.0` -> `1.3.0`, and both suites green, each
backed by verbatim evidence. PROOF-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-PROOF.md`
and `.planning/phases/223-get-current-on-hex-terminal-currency-proof/223-03-SUMMARY.md` when done.
</output>