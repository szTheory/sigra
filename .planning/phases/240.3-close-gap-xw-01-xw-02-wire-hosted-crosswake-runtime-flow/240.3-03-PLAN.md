---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "03"
type: execute
wave: 3
depends_on: ["240.3-02"]
files_modified:
  - test/example/lib/example_web/controllers/crosswake_controller.ex
  - test/example/test/example_web/controllers/crosswake_controller_test.exs
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-03/D-06/D-13, the real return route handles missing, revoked, expired, account-switched, and same-account/different-session state explicitly and denies before evaluator invocation."
    - "Per D-07/D-08/D-13, authority, route, grant, and destination input cannot change the stored binding, fixed evaluator route, or fixed local navigation."
    - "Per D-09/D-10/D-11, success is a 303 with clean final navigation while every denial uses one of two generic recovery messages and redacted telemetry only."
  artifacts:
    - path: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      provides: "Thin allow/deny HTTP mapping and redacted telemetry emission"
      exports: ["start/2", "return/2"]
    - path: "test/example/test/example_web/controllers/crosswake_controller_test.exs"
      provides: "Full real-router security and response matrix"
  key_links:
    - from: "test/example/test/example_web/controllers/crosswake_controller_test.exs"
      to: "test/example/lib/example_web/router.ex"
      via: "Phoenix.ConnTest dispatches actual POST/GET route scopes"
      pattern: "(post|get)\(conn, ~p\"/crosswake"
    - from: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      to: "Phoenix.Telemetry"
      via: "fixed event with correlation_ref, outcome, and reason only"
      pattern: ":telemetry\.execute"
---

<objective>
Expand the working runtime slice into the complete request/session, smuggling, recovery, and telemetry matrix.

Purpose: Close the exact milestone-audit gap at the HTTP boundary and prove the XW-02 denials occur through the real router—not only inside the already-finished adapter.
Output: Deterministic ConnCase coverage for all D-13 cases and stable response/telemetry mapping.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@test/example/AGENTS.md
@.planning/v1.48-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-CONTEXT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-RESEARCH.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-PATTERNS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-02-SUMMARY.md

<interfaces>
- Controller calls only `CrosswakeContinuations.issue/3` and `complete/5`; the context returns allow/deny categories already stripped of sensitive values.
- Stable internal reason classes: `invalid_or_expired_handle`, `already_used`, `invalid_return_evidence`, `oauth_state_or_pkce_failure`, `session_unavailable`, `binding_mismatch`, `route_denied`.
- Telemetry event: `[:example, :crosswake, :continuation]`; measurements `%{count: 1}`; metadata allowlist `%{correlation_ref, outcome, reason}`.
- User recovery destinations are server-owned: session-unavailable class -> `/users/log_in`; all other return failures -> `/`; success -> `/app`.
- Local GET transport input is limited to scalar `continuation`, `state`, and `pkce_verifier`, all covered by Phoenix parameter filtering. After claim and correlation match, the context creates the exact released OAuth `AuthReturn` map from server-owned fields; query maps are never merged into that envelope.
</interfaces>
</context>

## Artifacts this phase produces

- `test/example/lib/example_web/controllers/crosswake_controller.ex` — `ExampleWeb.CrosswakeController.start/2`, `return/2`, generic recovery mapping, and redacted telemetry helper.
- `test/example/test/example_web/controllers/crosswake_controller_test.exs` — real-router start/return, switch, replay, smuggling, response, evaluator non-invocation, and telemetry coverage.
- Downstream browser, recipe, guard, coverage, runner, and receipt artifacts are enumerated in Plan 01 and completed by Plans 04-05.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove every hostile return fails closed through the router</name>
  <files>test/example/lib/example_web/controllers/crosswake_controller.ex, test/example/test/example_web/controllers/crosswake_controller_test.exs</files>
  <read_first>
    - Both target files as committed by Plans 01-02.
    - `test/example/lib/example_web/router.ex` to confirm the start and return pipeline placement before adding assertions.
    - `test/example/lib/example_web/user_auth.ex` for current-scope/session fetch and automatic redirect behavior that the return route must avoid.
    - `test/example/test/support/conn_case_helpers.ex` for canonical `log_in_user/3` session creation.
    - `test/example/test/example/accounts/crosswake_session_adapter_test.exs` for exact missing/revoked/expired/account/session-switch and evaluator non-invocation fixtures.
    - `test/example/lib/example_web/controllers/enterprise_sso_controller.ex` and its test for fixed recovery flashes and router-dispatched callback tests.
    - `test/example/lib/example/accounts/crosswake_continuations.ex` for the only reason/result values the controller may map.
  </read_first>
  <behavior>
    - Unauthenticated start is rejected by the existing authentication pipeline; missing-session return reaches the controller and maps to sign-in recovery rather than being preempted.
    - Deleted/revoked/idle-expired/absolute-expired state, different account, and same account with a replacement session all deny and never invoke the evaluator.
    - A replayed handle denies after the first terminal result; invalid, expired, and already-used handles expose no forensic distinction in response copy.
    - Return parameters attempting to supply session/subject/org/authority/grant/route/destination or credential/provider material deny or are provably inert; none changes the fixed route or final location.
    - Named test `local AuthReturn ignores or rejects smuggled authority route and destination fields` sends every released forbidden authority/credential key plus `route_id`, `return_route_id`, `return_to`, `url`, and `destination`; the captured evaluator still receives only `crosswake-hosted-account`, or validation denies before it is called.
    - Success is 303 to `/app`; failures use only the two D-10 recovery messages; telemetry contains only the metadata allowlist and cannot contain any seeded raw sensitive value.
  </behavior>
  <action>Write the D-13 ConnCase matrix before altering response mapping. Exercise actual router paths with `ConnCaseHelpers.log_in_user/3`, `recycle/1`, real session rows, fixed clocks passed through the test injection seam, and a notifying/capturing evaluator. Keep the Plan 01 named local-protocol happy path, and add named cases `local AuthReturn rejects missing or mismatched state and PKCE before evaluation` and `local AuthReturn ignores or rejects smuggled authority route and destination fields`. Cover authenticated start, same-session allow, missing/deleted/revoked/idle-expired/absolute-expired session, different-account and same-account/different-session switch, invalid/expired/replayed handles, scalar/duplicate/list/malformed continuation/state/PKCE inputs, plus explicit smuggling keys matching released `AuthReturn` forbidden authority/credential fields and `route_id`, `return_route_id`, `return_to`, `url`, and `destination`. The tests must assert GET parameter handling reads only scalar continuation/state/verifier, never merges the query map into the server-built envelope, and captures the exact envelope fields fixed in Plan 01. Prove correlation/binding/session failures have zero evaluator messages; if extra keys are ignored after valid correlation, assert the evaluator route remains exactly `crosswake-hosted-account` and redirect remains `/app`. Keep the controller thin per D-02: map `CrosswakeContinuations` result categories to `303 /app`, sign-in recovery, or restart recovery; never inspect/derive binding facts or construct callback-selected routes. Emit `[:example, :crosswake, :continuation]` with one count and only opaque correlation reference plus stable outcome/reason; attach/detach a test handler and use `ExUnit.CaptureLog` around seeded requests. Assert exact telemetry metadata keys and assert seeded handles, state/verifier values, cookies, binding refs, database IDs, query values, and evidence bodies are absent from both telemetry and captured request/application logs. Use generic end-user language from D-10 and keep implementation vocabulary out of flash copy. Preserve D-01/D-17 boundaries: no public Sigra abstraction, generated router change, broad Plug, LiveView-only flow, or new UI.</action>
  <acceptance_criteria>
    - Every D-13 state has a named endpoint-level test; local state/PKCE correlation and exact AuthReturn construction have the three named protocol tests; every correlation/host/evidence/binding failure asserts evaluator non-invocation.
    - Same-session allow is exactly 303 to `/app`; no hostile parameter changes the Crosswake `RouteEntry` ID or navigation destination.
    - Missing session return reaches the controller under `[:browser]` and produces sign-in guidance; other terminal failures produce restart guidance without detailed reason leakage.
    - Telemetry metadata key equality is asserted, and dynamically seeded handle/token/ref/ID/query/evidence values are absent from metadata, captured request/application logs, response body, flash, and redirect location.
    - Tests use no sleeps, retry loops, direct controller calls, or mock-only persistence.
  </acceptance_criteria>
  <verify>
    <automated>set -a; source tmp/db.env; set +a; cd test/example &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet &amp;&amp; mix test test/example_web/controllers/crosswake_controller_test.exs</automated>
  </verify>
  <done>The real host request boundary permits only the same bound session, rejects replay/switch/smuggling before evaluation, maps safe generic responses, and emits no sensitive telemetry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser -> router/controller | Untrusted cookie, handle, and query data cross into HTTP orchestration. |
| controller -> continuation context | HTTP input becomes bounded application arguments and safe result classes. |
| context -> adapter/evaluator | Only claimed record plus fresh canonical session may invoke Crosswake. |
| controller -> response/telemetry | Internal outcomes become user-visible navigation and operational metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-01 | Elevation of Privilege | replay/racing return | high | mitigate | Plan 02 atomic claim plus endpoint replay test; no evaluator on subsequent return. Blocks on duplicate invocation. |
| T-240.3-02 | Spoofing | account/session switch | high | mitigate | Real cookie/session swaps are tested against stored departure binding and adapter fresh lookup. Blocks on allow. |
| T-240.3-03 | Tampering | authority/route/destination smuggling | high | mitigate | Hostile-key matrix, public AuthReturn validation, fixed context route/destination, and captured evaluator route assertion. Blocks on influence. |
| T-240.3-04 | Information Disclosure | flash, redirect, telemetry | high | mitigate | Generic response classes, exact metadata-key allowlist, and dynamic seeded-value negative assertions. Blocks on disclosure. |
| T-240.3-05 | Repudiation | false exact-SHA proof | high | mitigate | Plan 05 includes this complete ConnCase command and guard after commit, before receipt-last emission. |

## OWASP ASVS 4.0.3 Level 1 Mapping

| ASVS family | Plan control | Executable acceptance / evidence |
|---|---|---|
| V2 Authentication | Missing, revoked, expired, account-switched, and replacement-session cookies fail before evaluation. | Named real-router cases assert zero evaluator calls and safe recovery, directly blocking T-240.3-02. |
| V3 Session Management | Return remains reachable without auto-login redirect but cannot proceed without the exact departure session and unused continuation. | Missing-session, replay, same-account/different-session, and expiry endpoint tests block T-240.3-01/T-240.3-02. |
| V4 Access Control | Server-owned route/destination and adapter evaluation are the only grant path. | Captured route equality and exact `/app`/recovery redirects block T-240.3-03. |
| V5 Validation | Only scalar state/PKCE are read; request maps and forbidden/unknown AuthReturn keys cannot enter the envelope. | Three named local protocol cases plus the hostile-key matrix block T-240.3-03 before receipt generation. |
| V6 Cryptography | Dynamic handle/state/verifier/binding values remain opaque and are not copied into telemetry or responses. | Seeded-value negative assertions and exact telemetry-key equality block T-240.3-04. |
</threat_model>

<verification>
Migrate the example test schema, run the complete controller test file, and require all hostile cases to dispatch the real endpoint. Adapter-only tests are supporting authority evidence, not substitutes for this runtime proof.
</verification>

<success_criteria>
- The milestone audit's missing runtime caller is exercised for allow and all fail-closed outcomes.
- Return input is evidence/correlation only and cannot influence authority or navigation.
- Responses and telemetry preserve the locked disclosure boundary.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-03-SUMMARY.md` when done.
</output>
