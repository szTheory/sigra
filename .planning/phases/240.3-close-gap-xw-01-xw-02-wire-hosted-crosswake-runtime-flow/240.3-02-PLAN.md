---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "02"
type: execute
wave: 2
depends_on: ["240.3-01"]
files_modified:
  - test/example/lib/example/accounts/crosswake_continuation.ex
  - test/example/lib/example/accounts/crosswake_continuations.ex
  - test/example/test/example/accounts/crosswake_continuations_test.exs
autonomous: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-05/D-12, persisted continuations are digest-only, contain no canonical database IDs or secret/evidence material, and expire at the exact boundary."
    - "Per D-06/D-12, exactly one of racing or sequential claims can progress; every terminal result consumes the record."
    - "Per D-04/D-07, concurrent departures remain isolated and the stored departure binding is never recomputed from return-time state."
  artifacts:
    - path: "test/example/lib/example/accounts/crosswake_continuation.ex"
      provides: "Narrow trusted construction and redacted outcome changesets"
      exports: ["issue_changeset/1", "outcome_changeset/3"]
    - path: "test/example/lib/example/accounts/crosswake_continuations.ex"
      provides: "Atomic digest claim, completion, and bounded cleanup"
      exports: ["issue/3", "complete/5", "cleanup_expired/1"]
    - path: "test/example/test/example/accounts/crosswake_continuations_test.exs"
      provides: "Real PostgreSQL replay/race/privacy matrix"
  key_links:
    - from: "test/example/lib/example/accounts/crosswake_continuations.ex"
      to: "auth.crosswake_continuations"
      via: "single update_all predicate on digest, consumed_at, and expires_at"
      pattern: "update_all"
    - from: "test/example/test/example/accounts/crosswake_continuations_test.exs"
      to: "test/example/lib/example/accounts/crosswake_continuations.ex"
      via: "public operations against SQL Sandbox rows and fixed DateTimes"
      pattern: "CrosswakeContinuations\.(issue|complete|cleanup_expired)"
---

<objective>
Expand the tracer's continuation into a fully fail-closed one-time persistence boundary.

Purpose: Prove the hardest XW-02 constraint—atomic claim under replay and concurrency—before expanding HTTP hostile cases, while enforcing D-04 through D-07 and D-11/D-12 at the storage layer.
Output: Real-row digest, expiry, replay, race, isolation, cleanup, and sensitive-field tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@test/example/AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-CONTEXT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-RESEARCH.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-PATTERNS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-01-SUMMARY.md

<interfaces>
- Preserve `CrosswakeContinuations.issue/3` and `complete/5` from Plan 01; expand behavior without changing controller-facing return shapes.
- `CrosswakeContinuation` may persist only `handle_digest`, `state_digest`, `pkce_challenge_digest`, `return_ref`, `session_ref`, `subject_ref`, `session_version`, `route_id`, `return_route_id`, `issued_at`, `expires_at`, `consumed_at`, `outcome`, `reason`, `correlation_ref`, and timestamps. The handle, state, and verifier themselves never persist.
- All time-sensitive public operations accept a supplied `DateTime` for deterministic boundary tests.
</interfaces>
</context>

## Artifacts this phase produces

- `test/example/lib/example/accounts/crosswake_continuation.ex` — `Example.Accounts.CrosswakeContinuation`, `issue_changeset/1`, `outcome_changeset/3`.
- `test/example/lib/example/accounts/crosswake_continuations.ex` — `Example.Accounts.CrosswakeContinuations.issue/3`, `complete/5`, `cleanup_expired/1` and private atomic claim/digest helpers.
- `test/example/test/example/accounts/crosswake_continuations_test.exs` — digest/expiry/replay/race/concurrency/privacy coverage.
- Downstream phase artifacts are enumerated in Plan 01 and sealed by Plan 05.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden continuation issue, claim, expiry, and redaction</name>
  <files>test/example/lib/example/accounts/crosswake_continuation.ex, test/example/lib/example/accounts/crosswake_continuations.ex, test/example/test/example/accounts/crosswake_continuations_test.exs</files>
  <read_first>
    - All three target files as committed by Plan 01.
    - `test/example/AGENTS.md`, especially Ecto programmatic-field and no-sleep test rules.
    - `test/example/test/example/accounts/session_invalidation_test.exs` for real-row fixed-time state assertions.
    - `test/example/test/example/accounts/crosswake_session_adapter_test.exs` for raw-token fixture ownership, evaluator spy, exact idle/absolute boundaries, and account/session-switch semantics.
    - `test/example/lib/example/accounts.ex` functions `get_user_and_session_by_token/1` and `sigra_config/0`; do not duplicate them.
    - `.planning/phases/239-hosted-session-interop/239-04-SUMMARY.md` and `239-05-SUMMARY.md` for binding/evidence contracts already delivered.
  </read_first>
  <behavior>
    - A freshly issued row can be found only by SHA-256 digest; persisted inspection does not contain the raw handle, raw session token, token hash, user/session database ID, provider/evidence body, arbitrary URL, or Crosswake authority payload.
    - State and PKCE verifier correlation is independently digest-only; missing, list-valued, malformed, or mismatched query evidence consumes the claimed row and denies before evaluator invocation.
    - A claim succeeds before `expires_at` and denies at equality or after it without evaluator invocation.
    - Sequential replay and two synchronized concurrent claims yield exactly one winner; no read-then-write path exists.
    - Two simultaneous departures produce distinct handles/digests and cannot claim or overwrite each other's row.
    - Every terminal allow or denial records only stable redacted `outcome`/`reason` and consumption time; `cleanup_expired(as_of, limit: 500)` removes at most 500 oldest consumed/expired records without changing claim predicates.
  </behavior>
  <action>Write the failing real-PostgreSQL test matrix first, using `async: false`, fixed `DateTime` values, SQL Sandbox ownership appropriate for spawned claim tasks, monitors/barriers instead of sleeps, and only public continuation operations. Tighten `issue_changeset/1` so trusted server fields are assigned programmatically rather than cast from request input, validate required opaque/digest/version/route/expiry values, and retain only the D-05 allowlist including digest-only state/PKCE correlation and the opaque server-owned return reference. Implement the claim as one `Ecto.Query` plus `Repo.update_all` predicate over handle digest, `is_nil(consumed_at)`, and strict future expiry; use returned rows or an equivalent transaction-safe result supported by the repository PostgreSQL version. Treat zero affected rows as terminal generic denial and never fall back to `Repo.get` then update. After claim, validate scalar state and PKCE verifier by hashing and `Plug.Crypto.secure_compare/2`; failures record `oauth_state_or_pkce_failure`, stay consumed, and never call `AuthReturn` or the evaluator. Ensure every later terminal result records only a stable D-10 reason class; no failure may unconsume or retry the record. Implement `cleanup_expired(as_of, limit: 500)` with a selected-ID subquery ordered oldest-first and a hard maximum of 500 rows per call; include consumed or expired rows only, do not call it from the claim path, and add no scheduler/dependency. Preserve Plan 01 public signatures and fixed route/destination, and do not broaden into a second auth/session cache per D-04 through D-07 and D-12.</action>
  <acceptance_criteria>
    - Schema reflection and persisted-row assertions prove the exact allowed field set and absence of raw/session/provider/evidence/URL/database-ID fields.
    - Tests prove one winner for sequential replay and for synchronized concurrent claims, with no `Process.sleep/1` or timing-based polling.
    - Expiry equality denies, a timestamp immediately before expiry can claim, and cleanup does not weaken the claim predicate.
    - Two concurrent flows remain isolated by distinct digests and stored expected bindings.
    - Named tests `local AuthReturn rejects missing or mismatched state and PKCE before evaluation` and `cleanup removes at most 500 oldest terminal rows without weakening live claims` pass against PostgreSQL.
    - Host-gate denials do not call the injected evaluator and terminal rows remain consumed with only a stable reason class.
  </acceptance_criteria>
  <verify>
    <automated>set -a; source tmp/db.env; set +a; cd test/example &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet &amp;&amp; mix test test/example/accounts/crosswake_continuations_test.exs</automated>
  </verify>
  <done>The continuation is a one-time, expiring, digest-addressed correlation record with machine-proven race resistance, isolation, and sensitive-field exclusion for XW-01/XW-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser handle -> continuation context | Untrusted opaque handle is digested before lookup. |
| continuation context -> PostgreSQL | Claim atomicity and field minimization are authoritative. |
| claimed row -> adapter | Stored binding may be used only with fresh host validation. |
| proof runner -> receipt | Plan 05 turns test outcomes into an exact-SHA claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-01 | Elevation of Privilege | claim/replay race | high | mitigate | One conditional database update, strict expiry, exactly-one-winner concurrency tests. Blocks completion on a second winner. |
| T-240.3-02 | Spoofing | expected binding | high | mitigate | Departure binding remains persisted and is never recomputed; adapter revalidates return-time cookie. Plan 03 proves switch denials. |
| T-240.3-03 | Tampering | route/destination/evidence fields | high | mitigate | Schema has fixed route ID and no destination/evidence columns; context ignores callback route/destination claims. |
| T-240.3-04 | Information Disclosure | continuation storage/outcome | high | mitigate | D-05 field allowlist, digest-only inspection tests, redacted outcome codes, no database foreign identifiers. Blocks on leakage. |
| T-240.3-05 | Repudiation | exact-SHA proof | high | mitigate | Plan 05 receipt includes this focused command after explicit migration and emits only after it passes. |

## OWASP ASVS 4.0.3 Level 1 Mapping

| ASVS family | Plan control | Executable acceptance / evidence |
|---|---|---|
| V2 Authentication | Persisted expected binding remains server-owned and return correlation cannot authenticate a user. | Storage tests require zero evaluator calls on correlation/host denial; Plan 03 switch matrix closes T-240.3-02. |
| V3 Session Management | Strict future expiry and one conditional consumed-state update enforce one-time use. | Sequential and synchronized exactly-one-winner PostgreSQL assertions block T-240.3-01. |
| V4 Access Control | Only the fixed route IDs are stored; continuation state is insufficient without fresh adapter validation. | Schema reflection, isolated-flow tests, and evaluator non-invocation acceptance block T-240.3-02/T-240.3-03. |
| V5 Validation | Scalar state/PKCE values are independently hashed and matched after claim; failures remain consumed. | Named mismatch test asserts `oauth_state_or_pkce_failure` and zero evaluator messages, blocking T-240.3-03. |
| V6 Cryptography | Raw handle/state/verifier never persist; SHA-256 digests and constant-time comparisons address lookup/correlation. | Exact-field persisted-row assertions block T-240.3-04; focused storage suite is required by the receipt. |
</threat_model>

<verification>
Run the explicit test migration before the focused storage matrix. Inspect the generated SQL behavior through public Ecto operations; no in-memory substitute or mock-only proof satisfies this plan.
</verification>

<success_criteria>
- Replay and racing returns cannot both reach validation/evaluation.
- Persisted continuation state is intentionally insufficient to authorize without a fresh host session.
- All privacy assertions are behavioral/database assertions, not documentation-only claims.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-02-SUMMARY.md` when done.
</output>
