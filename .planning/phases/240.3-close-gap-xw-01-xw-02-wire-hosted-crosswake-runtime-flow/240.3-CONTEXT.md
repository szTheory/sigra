# Phase 240.3: Wire Hosted Crosswake Runtime Flow - Context

**Gathered:** 2026-08-11 (assumptions mode, advisor research expanded)
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the XW-01/XW-02 cross-phase integration gap identified by the v1.48 milestone audit: make the already-proven `Example.Accounts.CrosswakeSessionAdapter` reachable through a real host-owned request/session -> continuation -> adapter -> Crosswake evaluator -> evidence/navigation response flow, and exercise that flow deterministically.

This phase does not add Crosswake to Sigra core, change canonical generated-host output, create a networked Crosswake service, or claim real provider, deployment, or physical-device proof. An opt-in generated Crosswake integration remains a future capability to consider only after the reference-host contract proves stable.
</domain>

<decisions>
## Implementation Decisions

### Host ownership and runtime architecture

- **D-01:** Wire the runtime flow in `test/example` as a host-owned reference integration. Keep `crosswake_sigra`, the raw cookie/session seam, host schemas, routes, navigation, and Crosswake policy outside Sigra core and canonical generated B2C templates.
- **D-02:** Use thin Phoenix controller endpoints over explicit Accounts-context operations. Do not make the flow LiveView-only or hide the start/return workflow in a broad evaluator Plug. Shared pipelines may fetch and protect normal browser-session state, but the controller owns HTTP orchestration and response mapping.
- **D-03:** Use an authenticated, CSRF-protected `POST` start route under the normal browser/authentication pipeline. Put the return route under the normal browser/session-fetching pipeline without an automatic authentication redirect, so missing or changed session state is handled as an explicit fail-closed return.

### Server-owned continuation and authority

- **D-04:** At departure, freshly resolve the canonical backend SIGRA session and user, derive the opaque expected `session_ref`, `subject_ref`, and exact session version, then persist them in a short-lived, database-backed, one-time continuation record.
- **D-05:** Address each continuation with a random 256-bit opaque handle and store only its digest. The record contains the expected opaque binding, an immutable internal `route_id`, timestamps, expiry, consumption state, and optional redacted outcome metadata. It never contains a raw session token/cookie, token hash, user/database session ID, OAuth/provider credential, Crosswake authority payload, or arbitrary URL.
- **D-06:** On return, atomically claim an unused/unexpired continuation; validate approved return evidence and applicable transaction state/PKCE; freshly resolve and validate the current backend session/user; compare session ref, subject ref, and exact version; then construct facts and invoke the evaluator. Any terminal allow or deny consumes/invalidates the continuation. Binding/session/evidence failures deny before evaluator invocation.
- **D-07:** Return/callback data is evidence and correlation only. It cannot supply or replace the expected binding, SIGRA session, subject, organization, Crosswake route, grant decision, or navigation destination. Recomputing a new expected binding solely from the return-time session is forbidden because it would erase account/session-switch detection.

### Route, response, and failure semantics

- **D-08:** Use a fixed host-owned Crosswake route and fixed safe local destination for this phase. If multiple outcomes later become necessary, departure may select a closed server-side `route_id` allowlist stored in the continuation; callback-selected paths, URLs, policy routes, and open redirects remain forbidden.
- **D-09:** Complete successful browser returns with redirect-after-processing semantics, preferably `303 See Other`, leaving no handle, code, binding, or evidence in the final URL or rendered DOM. Do not introduce a Crosswake success interstitial.
- **D-10:** Collapse user-visible denials to safe recovery guidance: either the sign-in session is no longer available and the user should sign in again, or the return could not be completed and should be restarted. Keep forensic distinctions internal as stable redacted reason codes such as `invalid_or_expired_handle`, `already_used`, `invalid_return_evidence`, `oauth_state_or_pkce_failure`, `session_unavailable`, `binding_mismatch`, and `route_denied`.
- **D-11:** Structured logs/telemetry may include an opaque correlation reference and stable outcome/reason class, but never raw query parameters, cookies/tokens, continuation handles, binding values, database identifiers, provider payloads, or return evidence bodies.

### Deterministic closure evidence

- **D-12:** Preserve the Phase 239 database-backed adapter matrix as the authority layer, and add focused continuation-store tests for digest-only lookup, expiry, atomic single use, replay denial, concurrent-flow isolation, and sensitive-field exclusion.
- **D-13:** Add `ConnCase` request-level integration proof through the real router and Plug session for authenticated start, same-session allow, missing/revoked/expired denial, different-account and same-account/different-session denial, replay, smuggled authority/route/destination rejection, safe redirects, generic failure copy, evaluator non-invocation, and telemetry redaction.
- **D-14:** Add one focused deterministic Playwright happy-path journey in `test/example` to prove real cookie-jar departure/return navigation and absence of sensitive material from the final URL/DOM. Keep the negative-security matrix in Elixir tests; use role/label selectors, stable readiness signals, no sleeps, and no retries as evidence substitution.
- **D-15:** Extend the bounded hosted-session proof and machine-readable exact-SHA receipt to run the adapter, continuation, request-level, focused browser, and documentation/contract guards. CI may claim an in-process host reference runtime flow only—not real Google, email, deployment, Crosswake network service, or physical-device success.

### Developer experience and evolution

- **D-16:** Update the B2C alpha recipe with a copyable host-owned integration outline, explicit dependency placement, start/return flow, fixed-route and continuation rules, recovery behavior, and truthful proof/non-claim boundary. Keep implementation vocabulary out of end-user copy.
- **D-17:** Defer an opt-in Crosswake generator or Sigra public integration abstraction. Promote one only after this reference implementation demonstrates a stable contract across real host needs; do not commit generated migrations, router mutation, configuration, or companion compatibility prematurely.

### the agent's Discretion

- Choose precise schema/module/route names, continuation cleanup mechanics, transaction implementation, TTL configuration shape, telemetry event name, and safe internal route identifier while preserving all locked authority, one-time-use, privacy, and proof outcomes.
- Reuse existing example-host layouts and auth recovery affordances. No new branded surface is required; if existing UI renders the generic recovery copy, preserve its accessibility, responsive behavior, Light/Dark/System support, and established host visual system.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `.planning/v1.48-MILESTONE-AUDIT.md` — authoritative cross-phase runtime gap and required handoff.
- `.planning/REQUIREMENTS.md` — XW-01/XW-02 requirement language and milestone non-goals.
- `.planning/ROADMAP.md` — Phase 239/240 evidence boundary and inserted Phase 240.3 scope.
- `.planning/phases/239-hosted-session-interop/239-CONTEXT.md` — locked personal-account, binding, replay, and evidence decisions.
- `.planning/phases/239-hosted-session-interop/239-RESEARCH.md` — session authority, return-evidence, and ecosystem analysis.
- `.planning/phases/239-hosted-session-interop/239-PATTERNS.md` — explicit host/example ownership and code analogs.
- `.planning/phases/239-hosted-session-interop/239-04-PLAN.md` — expected binding and account-switch contract.
- `.planning/phases/239-hosted-session-interop/239-05-PLAN.md` — return-evidence boundary.
- `.planning/phases/240-alpha-operations-rehearsal/240-CONTEXT.md` — no-secrets and host launch-gate truth boundaries.
- `.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md` — deterministic browser/accessibility conventions and generated-host boundary.
- `prompts/Building the gold-standard Elixir:Phoenix authentication library.md` — hybrid ownership, ecosystem lessons, and anti-magic architecture principles.
- `prompts/Phoenix Auth Library — Jobs to Be Done, Personas & User Flows.md` — adopter personas, session-security JTBD, and visible/debuggable magic principle.
- `prompts/Auth Domain Language — A Field Guide.md` — session, authority, token, CSRF, fixation, and failure-language guidance.
- `prompts/phoenix-best-practices-deep-research.md` — context/controller/router/LiveView boundaries.
- `prompts/elixir-plug-ecto-phoenix-system-design-best-practices-deep-research.md` — cross-layer architecture and Ecto/Plug integration guidance.
- `test/example/lib/example/accounts/crosswake_session_adapter.ex` — existing fresh-resolution, binding, projection, and evaluator boundary.
- `test/example/test/example/accounts/crosswake_session_adapter_test.exs` — existing authority and denial matrix.
- `test/example/lib/example_web/router.ex` — runtime route and pipeline integration point.
- `test/example/lib/example_web/user_auth.ex` — canonical host Plug-session/current-scope seam.
- `test/example/lib/example/accounts.ex` — host context and session lookup patterns.
- `test/example/test/support/conn_case.ex` — request-level integration substrate.
- `test/example/priv/playwright/playwright.config.ts` — deterministic browser-test conventions.
- `scripts/ci/hosted-session-interop-proof.sh` — bounded proof/receipt integration point.
- `guides/recipes/b2c-alpha.md` — adopter-facing hosted-session recipe and truth boundary.
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `Example.Accounts.CrosswakeSessionAdapter` already owns fresh session/user resolution, opaque binding construction, fail-closed comparison, fact projection, and `evaluate_return/5`.
- Its database-backed test suite already covers personal `org_id: nil`, valid projection, currentness, expiry, revocation, binding/account switch, return evidence, forbidden fields, and evaluator non-invocation.
- `ExampleWeb.UserAuth` and the browser pipeline already expose the canonical Plug-session token/current-scope seams.
- Existing `ConnCase`, Ecto sandbox, serial Playwright configuration, and Phase 238 readiness patterns provide deterministic runtime proof substrates.
- `hosted-session-interop-proof.sh` and Phase 239 JSON receipts provide the bounded exact-SHA evidence pattern.

### Established Patterns

- Host-owned adapters compose Sigra storage/session authority with optional companion contracts; Sigra core does not own host schemas, cookies, navigation, or the Crosswake dependency.
- Phoenix contexts own persistence/application operations; controllers remain thin HTTP coordinators; pipelines perform shared request preparation and protection.
- Authentication decisions re-read canonical backend state instead of trusting cached assigns, transported facts, or callback data.
- Security-sensitive browser tests use deterministic local doubles, stable selectors/readiness, no sleeps, and explicit claim boundaries.

### Integration Points

- Add host continuation context/schema and migration within `test/example`.
- Add a thin example-web controller and explicit start/return routes around the adapter.
- Extend `ConnCase` and Playwright coverage without changing the canonical fresh-generated-host lane.
- Extend the Phase 239 proof script, source-contract tests, recipe, and machine-readable evidence schema as needed.
</code_context>

<specifics>
## Specific Ideas

- Treat successful return as invisible infrastructure: send the user directly to the pre-approved local destination rather than showing a Crosswake-branded success page.
- Use consumer/JTBD language in docs: “start,” “return,” “continue,” and “try again.” Keep “binding,” “authority lane,” evaluator internals, and denial taxonomy in developer/maintainer documentation and telemetry only.
- Model the host integrator’s job as a bounded copyable reference with explicit ownership and failure semantics, not hidden framework takeover.
- If a flow starts without an existing SIGRA session, it is a separate login transaction and must restart Crosswake only after login; it must not silently recompute and continue as though departure authority existed.
</specifics>

<deferred>
## Deferred Ideas

- Opt-in Crosswake installer/generator support or a Sigra public Crosswake behaviour/module — revisit only after the reference flow demonstrates a stable adopter contract.
- Broad route-guard Plug integration — revisit only if Crosswake must uniformly guard a route class rather than orchestrate a hosted continuation.
- LiveView-specific return UI — add only if a future product requirement introduces meaningful interaction that ordinary HTTP cannot provide.
- Network/service E2E, real provider, deployed HTTPS, and physical-device proof remain external host launch gates.

### Reviewed Todos (not folded)

- All keyword-matched pending todos were reviewed and left out because they concern unrelated auth-prefix, CSS, CI, release, schema-migration, admin, or prior-evidence work rather than the hosted Crosswake runtime handoff.
</deferred>
