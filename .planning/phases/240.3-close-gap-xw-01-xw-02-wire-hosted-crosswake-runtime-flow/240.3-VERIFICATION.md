---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
verified: 2026-08-11T19:14:43Z
status: gaps_found
score: 12/15 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 12/15
  gaps_closed:
    - "The callback response now sets Referrer-Policy: no-referrer before every allow or denial redirect."
    - "The proof runner now binds and rechecks one SHA while rejecting tracked and non-ignored untracked changes outside the evidence path."
  gaps_remaining:
    - "The raw PKCE verifier remains in the query-bearing callback URL."
  regressions: []
gaps:
  - truth: "Per D-05/D-07/D-08/D-09, the continuation stores only a digest and opaque expected binding, callback input cannot choose authority or navigation, and success ends with a safe 303 to /app."
    status: failed
    reason: "start/2 still serializes the raw PKCE verifier into the Location for GET /crosswake/return. The later no-referrer policy prevents propagation to /app, but cannot prevent browser history, proxy/access logs, extensions, or other observers of the callback URL from receiving the verifier."
    artifacts:
      - path: test/example/lib/example_web/controllers/crosswake_controller.ex
        issue: "Lines 11-19 place values.pkce_verifier in URI.encode_query(query) for the callback redirect."
      - path: test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
        issue: "The test intentionally reads pkce_verifier from the observed callback URL and has no assertion that the callback URL itself is secret-free."
    missing:
      - "Keep the PKCE verifier out of the callback URL, using a server-side continuation-bound transport or an HttpOnly host session, and add a router/browser assertion that the callback request URL, history, and observable request metadata never contain it."
  - truth: "Every flagged XW-01/XW-02 prohibition has an owner-approved verification disposition."
    status: partial
    reason: "All three prohibitions remain explicitly unresolved/unverified in the regenerated receipt; this verifier does not treat the receipt flags as a silent pass."
    artifacts:
      - path: .planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json
        issue: "unresolved_assumptions contains XW-01 and XW-02 and flagged_unverified_prohibitions contains authority-integrity, secret-boundary, and authority-smuggling."
    missing:
      - "Resolve the three prohibition dispositions with the requirement owner, or attach deterministic enforcement evidence and update their status."
behavior_unverified_items:
  - truth: "Real Chromium follows the callback 303 and sends no Referer on the subsequent /app document request."
    test: "With the configured PostgreSQL service available, run scripts/ci/hosted-session-interop-proof.sh --browser-only."
    expected: "The serial zero-retry browser test observes the real /app document request and its referer header is absent."
    why_human: "The verifier did not start services; the configured PostgreSQL endpoint at 127.0.0.1:53988 refused connections, so current-process browser behavior could not be exercised."
---

# Phase 240.3: Hosted Crosswake Runtime Flow Verification Report

**Phase Goal:** Make the already-proven personal-session Crosswake adapter reachable through a deterministic example-host request/session, one-time continuation, evaluator, and safe response flow without widening Sigra core or generated-host scope.
**Verified:** 2026-08-11T19:14:43Z
**Status:** gaps_found
**Re-verification:** Yes — after gap-closure Plans 06 and 07

## Goal Achievement

The host wiring, fresh-session evaluation, one-time database claim, fixed response destinations, and no-referrer *next-navigation* repair are substantive and connected. The phase goal is nevertheless not achieved: the start response directly exposes the raw PKCE verifier in the callback URL. This independently confirms the still-applicable substance of review finding CR-01.

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Authenticated request reaches the adapter through a DB continuation and fixed destination. | ✓ VERIFIED | Router dispatches POST `/crosswake/start` and GET `/crosswake/return`; controller calls `CrosswakeContinuations.issue/2` and `complete/4`; context calls the adapter and fixes `/app`. |
| 2 | Departure and return use the Plug token and a freshly resolved backend session. | ✓ VERIFIED | Controller reads `:user_token` on both legs; context calls `expected_binding/2` at issue and `evaluate_return/6` after claim. |
| 3 | Digest-only continuation, authority isolation, and safe 303 response. | ✗ FAILED | Digest/authority portions hold, but `values.pkce_verifier` is placed in the query-bearing callback URL. |
| 4 | Stored continuations contain only digests/opaque fields and enforce the exact expiry boundary. | ✓ VERIFIED | Schema has SHA-256 digest fields; claim requires `expires_at > as_of`; focused continuation matrix is substantive. |
| 5 | Only one racing or sequential claim progresses and terminal processing consumes it. | ✓ VERIFIED | A conditional `Repo.update_all` requires digest, unused state, and strict expiry before evaluation. |
| 6 | Concurrent departures remain isolated and their departure binding is not recomputed at return. | ✓ VERIFIED | Issue creates independent values; `ExpectedBinding` is constructed from the claimed row. |
| 7 | Missing, revoked, expired, switched, and replacement sessions fail closed before evaluation. | ✓ VERIFIED | Router matrix covers all cases and asserts evaluator non-invocation. |
| 8 | Callback input cannot select authority, route, grant, or destination. | ✓ VERIFIED | Controller permits exactly three scalar keys; context constructs route/envelope server-side; smuggling matrix denies before evaluator. |
| 9 | Success has safe navigation and denials have generic recovery/redacted telemetry. | ✗ FAILED | `return/2` now returns `Referrer-Policy: no-referrer`, but the callback URL itself remains sensitive. |
| 10 | Chromium cookie jar follows the local return to `/app` without a Referer header. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Test is wired with `waitForRequest` and asserts absent `referer`, but current execution was blocked by unavailable PostgreSQL. |
| 11 | Final browser URL and DOM contain no correlation/evidence/binding/token/DB identifier. | ✓ VERIFIED | Playwright spec keeps sentinels in memory and checks final URL/body exclusions. |
| 12 | Browser proof is serial, retry-free, readiness-driven, credential-free, and makes no external-provider claim. | ✓ VERIFIED | Dedicated Playwright project remains one worker/zero retry; runner has bounded readiness and local in-process boundary. |
| 13 | Ordered proof binds the receipt to one clean SHA and writes it last. | ✓ VERIFIED | Current source-contract test passed (7 tests); helper behavior covers tracked, staged, untracked, and sole receipt-path cases; runner binds before commands and rechecks before writing. |
| 14 | Recipe is copyable, host-owned, and states proof/non-proof boundaries. | ✓ VERIFIED | Recipe/coverage are present and guarded by the passing source contract. |
| 15 | Flow remains example-host composition without Sigra-core or generator expansion. | ✓ VERIFIED | Runtime sources remain under `test/example`; source contract rejects root dependency/public generator/external API additions. |

**Score:** 12/15 truths verified (1 present, behavior-unverified).

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `crosswake_continuation.ex` | Digest-only schema/changesets | ✓ VERIFIED | Substantive Ecto schema and narrow changesets. |
| `crosswake_continuations.ex` | Issue/atomic complete/cleanup plus adapter seam | ✓ VERIFIED | Repo-backed claim and direct adapter calls; controller and DB tests consume its public API. |
| `crosswake_controller.ex` | Thin safe HTTP orchestration | ⚠️ WIRED WITH GAP | All delegations and response policy are wired; start URL contains the PKCE verifier. |
| `router.ex` / `config.exs` | Authenticated start, browser callback, TTL | ✓ VERIFIED | Actual route declarations and 300-second policy exist. |
| Controller/continuation tests | Router and PostgreSQL security matrices | ✓ VERIFIED | Substantive test matrices; current live DB execution unavailable. |
| Browser spec/config | Deterministic cookie-jar flow | ⚠️ PRESENT, BEHAVIOR UNVERIFIED | Exclusive serial project and direct request-header assertion exist; not re-executed. |
| Exact-SHA helper/runner/contract/receipt | Bound receipt-last proof | ⚠️ VERIFIED WITH WARNING | Tracked/non-ignored-untracked behavior is tested and wired; ignored runtime inputs remain outside the guard. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Controller | Continuations context | `issue` / `complete` | ✓ WIRED | Direct calls in start/return path. |
| Continuations context | Session adapter | `expected_binding` / `evaluate_return` | ✓ WIRED | Direct calls at context lines 29 and 147. |
| Router | Controller | POST start and GET return | ✓ WIRED | Actual Phoenix declarations. |
| Context | `auth.crosswake_continuations` | atomic `update_all` claim | ✓ WIRED | Digest/unused/strict-expiry predicate at lines 114–126. |
| Callback response | `/app` and recovery | no-referrer policy | ✓ WIRED | `return/2` sets the header before any branch; controller tests assert it on allow and denial. |
| Runner | exact-SHA helper and evidence | prebind/recheck/receipt-last | ✓ WIRED | Helper is sourced; bind occurs before command 1 and recheck immediately precedes writer. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Controller/context | Plug token and transient correlation | Plug session → fresh adapter lookup → Ecto row → server-built envelope | Yes | ✓ FLOWING |
| Continuation context | claimed continuation | `Repo.update_all` against `auth.crosswake_continuations` | Yes | ✓ FLOWING |
| Browser test | observed callback and `/app` request | Real form POST / redirect chain | Yes, but not re-run | ⚠️ BEHAVIOR UNVERIFIED |
| Receipt | captured SHA and ordered outcomes | shell helper → bounded commands → writer | Yes for tracked/non-ignored inputs | ⚠️ IGNORED-INPUT WARNING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Exact-SHA helper behavior and runner source contract | `MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` | 7 tests, 0 failures | ✓ PASS |
| PostgreSQL continuation/router matrix | focused example-host suites | PostgreSQL at `127.0.0.1:53988` refused connection; verifier did not start a service | ? SKIP |
| Chromium no-Referer request behavior | `scripts/ci/hosted-session-interop-proof.sh --browser-only` | Not run: it starts a dev server and requires the unavailable PostgreSQL service | ? SKIP |

### Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| Hosted runtime proof | `scripts/ci/hosted-session-interop-proof.sh --browser-only` | Not executed by verifier: it would start a server and the configured PostgreSQL endpoint refused connections | ? SKIP |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| XW-01 | 01–07 | A validated personal session projects without organization invention or credential/token exposure. | ✗ BLOCKED | Personal `org_id: nil` projection is present, but raw PKCE material is exposed in the callback URL. |
| XW-02 | 01–07 | Missing, expired, revoked, or account-switched state fails closed; return data alone never grants access. | ✓ SATISFIED | Fresh lookup, atomic claim, strict expiry, binding preservation, and denial/no-evaluator matrices are wired. |

No phase-mapped requirement is orphaned: XW-01 and XW-02 occur in every phase-plan frontmatter.

### Anti-Patterns and Review Disposition

| Finding | Status | Impact |
| --- | --- | --- |
| CR-01: PKCE verifier in callback URL | 🛑 BLOCKER, confirmed | `no-referrer` closes only the downstream Referer leak; it does not protect the sensitive callback URL itself. |
| WR-01: ignored `Repo.update/1` result for terminal outcome | ⚠️ WARNING | A database failure after claim can leave outcome/reason absent. This does not let callback input grant access or undo consumption, but it violates the intended audit-record completeness and lacks a failure-path test. |
| WR-02: ignored runtime inputs are absent from exact-SHA guard | ⚠️ WARNING | `ls-files --others --exclude-standard` excludes ignored files (including repository `.env`); tracked and non-ignored-untracked binding is tested, but the receipt does not fingerprint/forbid relevant ignored runtime inputs. |

No `TBD`, `FIXME`, `XXX`, placeholder, empty implementation, or no-op handler was found in phase implementation. The `mktemp` use is normal temporary log handling.

### Gaps Summary

One blocker prevents Phase 240.3 from meeting its safe-response goal: the raw PKCE verifier remains in the callback redirect URL. Move that verifier to a continuation-bound server-side channel (or equivalent HttpOnly session transport), reduce callback input accordingly, and prove that neither the callback URL/history nor request metadata exposes it.

The proof runner improvement closes the prior tracked/untracked exact-SHA gap, but its ignored-input boundary and terminal outcome-write error handling remain advisory warnings. The explicitly unresolved XW prohibition rows remain owner-resolution items and are not accepted as passing evidence.

---

_Verified: 2026-08-11T19:14:43Z_
_Verifier: the agent (gsd-verifier)_
