---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "07"
type: execute
wave: 7
depends_on: ["240.3-06"]
files_modified:
  - scripts/ci/lib/exact-sha-worktree.sh
  - scripts/ci/hosted-session-interop-proof.sh
  - test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs
  - .planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json
autonomous: true
gap_closure: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-15, the bounded proof captures one HEAD before its first command, rejects every tracked or untracked repository change except the phase evidence path, rechecks the same invariant before sealing, and records that captured SHA only after all proof layers pass."
    - "Tracked and untracked fail-closed behavior is exercised against disposable Git repositories, including the sole exact-path evidence exception; source-text presence alone cannot satisfy the contract."
    - "The rewritten receipt is last, names the exact clean SHA exercised by the adapter/continuation/controller/browser/contract commands, preserves unresolved XW probe rows, and retains the existing in-process/no-external-API claim boundary."
  artifacts:
    - path: "scripts/ci/lib/exact-sha-worktree.sh"
      provides: "Reusable exact-path worktree cleanliness and immutable-HEAD binding functions"
      contains: "bind_clean_worktree_sha"
    - path: "scripts/ci/hosted-session-interop-proof.sh"
      provides: "Pre-proof and pre-receipt exact-SHA gates around the complete ordered runtime proof"
    - path: "test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs"
      provides: "Disposable-Git-repository behavior matrix plus runner ordering contract"
    - path: ".planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json"
      provides: "Receipt-last evidence regenerated from the fully bound proof"
  key_links:
    - from: "scripts/ci/hosted-session-interop-proof.sh"
      to: "scripts/ci/lib/exact-sha-worktree.sh"
      via: "source helper, capture clean tested SHA before commands, and revalidate it immediately before write_evidence"
      pattern: "bind_clean_worktree_sha"
    - from: "scripts/ci/hosted-session-interop-proof.sh"
      to: ".planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json"
      via: "write_evidence receives the captured pre-proof SHA only after the second guard passes"
      pattern: "TESTED_SIGRA_SHA"
    - from: "test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs"
      to: "scripts/ci/lib/exact-sha-worktree.sh"
      via: "temporary Git fixtures prove clean, tracked-dirty, untracked-dirty, and evidence-only cases"
      pattern: "exact SHA worktree"
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The descriptor-less fallback probe left XW-01 unclassified and unresolved; exact-SHA evidence must retain this row rather than promote it to a resolved claim."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The descriptor-less fallback probe left XW-02 unclassified and unresolved; the full denial evidence does not auto-dismiss that probe result."
  prohibitions:
    - requirement_id: XW-01
      category: secret-boundary
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT let an evidence receipt claim a committed SHA while any exercised source or test dependency differs in the worktree."
    - requirement_id: XW-01
      category: authority-integrity
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT manufacture organization authority for a personal SIGRA session."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT let callback data select or reconstruct the session, subject, policy route, grant decision, or navigation destination."
---

<objective>
Make the exact-SHA receipt fail closed over the complete worktree exercised by the proof and regenerate it receipt-last.

Purpose: Close D-15's evidence-integrity blocker so the recorded commit cannot differ from any tracked or untracked source/test input used by the adapter, continuation, controller, browser, and contract proof.
Output: A behavior-tested Git worktree-binding helper, pre/post proof gates using one captured SHA, hardened source contract, and a regenerated Phase 240.3 receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-CONTEXT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-VERIFICATION.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-REVIEW.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-05-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-06-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/COVERAGE.md

<interfaces>
- The full proof command is `scripts/ci/hosted-session-interop-proof.sh`; `--browser-only` remains a diagnostic sub-mode and does not write evidence.
- The only permitted worktree difference is the exact repository-relative path `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json`, which `write_evidence` rewrites after the second guard.
- `git diff HEAD -- . ':(exclude)&lt;exact evidence path&gt;'` covers staged and unstaged tracked changes; `git ls-files --others --exclude-standard -- . ':(exclude)&lt;exact evidence path&gt;'` covers untracked files. Both must fail closed on command errors and nonempty results.
- The existing receipt schema, ordered six command rows, immutable Phase 239 release coordinates, unresolved assumptions, three flagged prohibitions, and `api_detector.detected == false` remain unchanged except for newly captured time/SIGRA SHA.
</interfaces>
</context>

## Artifacts this phase produces

- `scripts/ci/lib/exact-sha-worktree.sh` — new `bind_clean_worktree_sha` and `assert_same_clean_worktree_sha` shell functions.
- `scripts/ci/hosted-session-interop-proof.sh` — new `TESTED_SIGRA_SHA` binding consumed by `write_evidence`.
- `test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` — new disposable-repository exact-SHA worktree behavior test and strengthened receipt-order contract.
- `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json` — regenerated receipt produced only by Task 2 after Task 1 is committed.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind the complete proof to one clean worktree SHA</name>
  <reversibility rating="reversible">The internal shell helper and runner contract can be replaced locally without changing receipt schema, database state, or a published API.</reversibility>
  <files>scripts/ci/lib/exact-sha-worktree.sh, scripts/ci/hosted-session-interop-proof.sh, test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs</files>
  <read_first>
    - Read all three target paths at their current state; the helper is expected not to exist, while the runner and contract test must be read in full before editing.
    - Read `scripts/ci/lib/free-port.sh` for sourced-shell style, strict-mode compatibility, and library naming conventions.
    - Read the current Phase 240.3 evidence JSON to preserve its schema, command order, unresolved assumptions, prohibitions, immutable release coordinates, redaction, and no-external-API declaration.
    - Read `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-06-SUMMARY.md` and the three modified runtime/browser files so the full proof still exercises the Referer correction before sealing.
  </read_first>
  <behavior>
    - A clean temporary Git repository returns its exact 40-hex HEAD as the tested SHA.
    - A staged or unstaged tracked change outside the evidence path fails; an untracked file outside the evidence path fails.
    - A modification or creation at exactly the configured evidence path is the sole allowed difference; prefix/suffix/lookalike paths still fail.
    - Changing HEAD or introducing any non-evidence difference between the pre-proof capture and pre-write recheck fails.
    - The runner captures the SHA before its first migration/test command, rechecks immediately after the final contract test, and passes the captured value into the receipt writer; `git rev-parse HEAD` inside `write_evidence` is removed.
  </behavior>
  <action>Start RED by adding an ExUnit behavior test that creates a disposable temporary Git repository, configures local author identity, commits a runtime file plus an evidence-path fixture, and invokes the sourced helper through `System.cmd/3`. Cover clean success with exact HEAD, unstaged and staged tracked changes, an untracked file, exact evidence-only modification/creation, a lookalike evidence filename, and a HEAD change between capture/recheck; assert nonzero results for every forbidden case without mutating the real repository. Implement `scripts/ci/lib/exact-sha-worktree.sh` with `bind_clean_worktree_sha(root, evidence_relative_path)` and `assert_same_clean_worktree_sha(root, evidence_relative_path, expected_sha)`. Use Git pathspec exclusion anchored to the one exact evidence path, compare the full tracked tree to HEAD, enumerate all untracked non-ignored paths, validate the 40-hex SHA, and propagate every Git error under strict mode; do not use a hand-maintained dependency list or ignore unrelated paths. In the runner, source the helper, compute `TESTED_SIGRA_SHA` before the first `run_bounded`, call the second function after the final source-contract command and immediately before `write_evidence`, and make `write_evidence` consume only `TESTED_SIGRA_SHA`. Update the existing source contract to prove both guard positions, the absence of the partial scoped-path function/list, the sole exact evidence exclusion, captured-SHA handoff, and receipt-last ordering. Preserve `--browser-only`, all six ordered commands, redacted diagnostics, the authenticated start and return pipeline (D-03), fresh binding and one-time claim/evaluation ordering (D-04/D-06), adapter/continuation/router matrices (D-12/D-13), copyable recipe contract (D-16), host-only/no-generator boundary (D-01/D-17), `COVERAGE.md`'s valid no-external-API result, and all existing receipt fields.</action>
  <acceptance_criteria>
    - The disposable-repository test fails first against the current partial guard, then passes for clean/evidence-only cases and fails for tracked, staged, untracked, lookalike-path, and changed-HEAD cases.
    - The full runner binds before any migration/test/browser command and revalidates after the last proof command but before `write_evidence`; both gates exclude only the exact evidence path.
    - No hand-maintained runtime dependency list remains, and neither guard suppresses Git errors, defaults missing results, ignores untracked paths, or reads the tested SHA after proof execution.
    - `--browser-only`, ordered runtime commands, receipt schema/content boundary, `COVERAGE.md`, host-only composition, and existing Phase 239 evidence remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs</automated>
  </verify>
  <done>The proof runner has a behavior-tested, repository-complete pre/post worktree gate and one immutable tested SHA ready for receipt-last sealing.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the receipt after the bound proof passes</name>
  <precondition>Task 1 changes are committed; `tmp/db.env` exists; and the repository has no tracked or untracked differences other than the Phase 240.3 evidence path.</precondition>
  <reversibility rating="reversible">The receipt is generated evidence and can be regenerated from the same committed proof inputs.</reversibility>
  <files>.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-HOSTED-RUNTIME-EVIDENCE.json</files>
  <read_first>
    - Read the committed Task 1 versions of `scripts/ci/lib/exact-sha-worktree.sh`, `scripts/ci/hosted-session-interop-proof.sh`, and `test/sigra/planning/phase_240_3_hosted_crosswake_runtime_test.exs` before invoking the proof.
    - Read the current evidence JSON and `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/COVERAGE.md`; preserve the receipt schema and the existing no-external-API boundary.
    - Run read-only `git status --porcelain=v1 --untracked-files=all` and `git rev-parse HEAD`; if anything other than the exact evidence path is dirty, stop with the durable path list and do not alter or remove user-owned changes.
  </read_first>
  <action>Run the full `scripts/ci/hosted-session-interop-proof.sh` once from repository root after Task 1 is committed. Do not delete the previous receipt before the run: it is the sole allowed path and must be rewritten only by `write_evidence` after schema migration, immutable release validation, adapter, continuation, controller, browser, and source-contract commands all return zero and the second SHA/worktree gate passes. If the guard reports any other tracked or untracked path, preserve it and stop rather than stashing, cleaning, reverting, or widening the exclusion. After success, validate that `sigra_git_sha` equals the captured committed Task 1 HEAD; the six command rows are ordered and passed; the receipt retains both unresolved XW assumptions, all three flagged prohibitions, immutable Phase 239 coordinates, redacted fields, and `api_detector.detected == false`. Do not modify `COVERAGE.md`, historical Phase 239 receipts, Sigra core, generated templates, schemas, dependencies, or product scope.</action>
  <acceptance_criteria>
    - One full bounded command runs every existing proof layer, including Plan 06's router/browser Referer assertions, and writes the evidence file only after the second clean-worktree/same-HEAD gate.
    - Receipt `sigra_git_sha` equals the 40-hex Task 1 commit exercised by the run, not a later lookup; any tracked/untracked non-evidence change or intervening commit prevents receipt creation.
    - Receipt schema, six ordered zero-status command rows, immutable Crosswake release coordinates, two unresolved assumptions, three flagged prohibitions, redaction allowlist, and internal-runtime `api_detector.detected == false` all validate.
    - The evidence file is the only post-proof worktree difference created by this task, and all existing unrelated user changes are preserved if the precondition blocks execution.
  </acceptance_criteria>
  <verify>
    <automated>scripts/ci/hosted-session-interop-proof.sh</automated>
  </verify>
  <done>The receipt is regenerated last from a fully passing runtime proof whose entire repository worktree and HEAD were bound before execution and revalidated before sealing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git worktree/index -> proof commands | Tracked and untracked files can alter the code/tests/configuration the proof executes. |
| ordered commands -> receipt writer | Command success is meaningful only if HEAD and source inputs remain unchanged throughout the run. |
| receipt -> maintainers/launch decisions | A machine-readable SHA claim may be trusted beyond the local process that produced it. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-07-01 | Tampering | tracked/untracked proof inputs | high | mitigate | Full-tree tracked comparison plus untracked enumeration excludes only the exact receipt path and is behavior-tested in disposable repositories. |
| T-240.3-07-02 | Repudiation | receipt `sigra_git_sha` | high | mitigate | Capture clean HEAD before commands, revalidate the same SHA immediately before writing, and pass the captured value to the writer. |
| T-240.3-07-03 | Spoofing | lookalike evidence path | high | mitigate | Exact Git pathspec exclusion and negative fixture reject prefix, suffix, and sibling lookalikes. |
| T-240.3-07-04 | Information Disclosure | receipt and failure diagnostics | high | mitigate | Preserve the existing outcome-only receipt schema, forbidden-field assertions, and redacted browser/server diagnostics. |
| T-240.3-07-05 | Denial of Service | unrelated dirty worktree state | medium | accept | Honest exact-SHA proof intentionally blocks and reports paths; it never cleans, stashes, or overwrites user-owned changes. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Deterministic safe example-host Crosswake runtime without core/generated-host widening | 06, 07 | COVERED | Plan 06 closes safe response flow; Plan 07 closes deterministic evidence integrity. |
| REQ | XW-01 | Personal-session projection without invented org or credential/token exposure | 06, 07 | COVERED | Referer disclosure is blocked and receipt stays redacted/exact-SHA. |
| REQ | XW-02 | Missing/stale/switched/replayed state fails closed; return data alone grants nothing | 06, 07 | COVERED | Existing denial suite remains mandatory in the newly bound full proof. |
| RESEARCH | D-09/D-11/D-14 response/browser boundary | Clean 303 navigation with deterministic browser evidence | 06 | COVERED | Actual next document request header is asserted. |
| RESEARCH | D-15 exact-SHA receipt-last proof | Ordered proof bound to immutable source before sealing | 07 | COVERED | Complete tracked/untracked guard plus same-HEAD recheck. |
| CONTEXT | D-01 | Example-host ownership; no Sigra core/generated-host widening | 06, 07 | COVERED | Files and full proof remain host-only. |
| CONTEXT | D-02 | Thin controller over Accounts context | 06 | COVERED | Header is set at controller response orchestration; authority remains in context. |
| CONTEXT | D-03 | Authenticated POST start and explicit callback handling | 07 | COVERED | Bound full proof retains the real router/controller command. |
| CONTEXT | D-04 | Fresh canonical binding persisted in one-time continuation | 07 | COVERED | Adapter and continuation commands remain mandatory inputs to the receipt. |
| CONTEXT | D-05 | Digest-only continuation and no secret disclosure | 06 | COVERED | Next-request Referer disclosure is suppressed and browser-proven. |
| CONTEXT | D-06 | Atomic claim, fresh validation, evaluator last | 07 | COVERED | Existing continuation/controller matrices remain ordered proof inputs. |
| CONTEXT | D-07 | Callback evidence cannot replace authority | 06, 07 | COVERED | Header correction does not alter evaluation; full denial suite is sealed. |
| CONTEXT | D-08 | Fixed route and safe local destination | 06 | COVERED | ConnCase/browser retain exact destination assertions. |
| CONTEXT | D-09 | 303 completion with no correlation disclosure | 06 | COVERED | Response policy and actual `/app` request header close the blocker. |
| CONTEXT | D-10 | Generic recovery classes | 06 | COVERED | Denied response keeps existing destination/copy and gains the same policy. |
| CONTEXT | D-11 | Redacted telemetry/log/evidence | 06, 07 | COVERED | No correlation artifact; existing receipt redaction remains guarded. |
| CONTEXT | D-12 | Adapter and continuation authority matrices | 07 | COVERED | Both remain ordered exact-SHA proof commands. |
| CONTEXT | D-13 | Real-router hostile-return matrix | 06, 07 | COVERED | Response assertion is added and the suite remains receipt-gating. |
| CONTEXT | D-14 | Focused deterministic Playwright journey | 06, 07 | COVERED | Next document header is asserted and browser command is receipt-gating. |
| CONTEXT | D-15 | Complete bounded exact-SHA receipt-last evidence | 07 | COVERED | Full-tree pre/post guard and captured SHA close the blocker. |
| CONTEXT | D-16 | Copyable host recipe and truthful non-claims | 07 | COVERED | Existing recipe/source contract remains an ordered proof input. |
| CONTEXT | D-17 | Defer public abstraction/generator | 06, 07 | COVERED | No core/generated/dependency surface is touched. |
| CONTEXT | Deferred ideas | Generator/public abstraction/network/provider/device work | — | EXCLUDED | Explicitly deferred and absent from both plans. |

External API coverage detector: existing `COVERAGE.md` remains valid (`detected == false`); no network API/SDK/webhook/auth integration is added. Assumption-delta detector result: `detected == false`, so no architecture decision is recorded. Schema-push scan: no supported Payload/Prisma/Drizzle/Supabase/TypeORM paths occur in gap scope; no schema task is added.

<verification>
First run the source/behavior contract against disposable Git fixtures. After Task 1 is committed and the worktree precondition is satisfied, run the single full proof command and validate the new JSON receipt. Source-text assertions alone cannot close the gap. ASVS L1 applies, and every high-severity threat above is blocking.
</verification>

<success_criteria>
- Every tracked/staged/unstaged/untracked non-evidence difference fails before proof and again before receipt creation.
- The receipt records the same clean SHA captured before the first command and is written only after the Plan 06 browser boundary plus every existing proof layer passes.
- XW-01/XW-02 unresolved assumptions and prohibitions remain explicit, and no external API, schema, core, generated-host, provider, deployment, or device scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-07-SUMMARY.md` when done.
</output>
