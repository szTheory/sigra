---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "08"
type: execute
wave: 8
depends_on: ["240.3-07"]
files_modified:
  - test/example/lib/example_web/endpoint.ex
  - test/example/lib/example_web/controllers/crosswake_controller.ex
  - test/example/test/example_web/controllers/crosswake_controller_test.exs
  - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
autonomous: true
gap_closure: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-03/D-05/D-06/D-07, POST /crosswake/start transports the raw PKCE verifier only inside the encrypted HttpOnly host session, binds it to the issued continuation, and GET /crosswake/return accepts only continuation and state in its URL."
    - "Per D-09/D-11/D-14, the verifier is absent from the callback URL, Chromium-observed navigation history, request URLs, request metadata, response headers, logs, final URL, and DOM while the real cookie jar still reaches the fixed /app destination."
    - "Per D-01/D-02/D-17, the correction stays in the example-host endpoint/controller and focused tests; it does not change Sigra core, generated hosts, the Crosswake adapter, persistence schema, or external API scope."
  artifacts:
    - path: "test/example/lib/example_web/endpoint.ex"
      provides: "Encrypted, HttpOnly example-host Plug session transport"
      contains: "encryption_salt"
    - path: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      provides: "Continuation-bound PKCE session handoff and two-key callback URL"
      contains: "crosswake_pkce"
    - path: "test/example/test/example_web/controllers/crosswake_controller_test.exs"
      provides: "Real-router verifier confidentiality, one-time transport, and denial proof"
    - path: "test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts"
      provides: "Real Chromium URL/history/header/cookie-attribute confidentiality proof"
  key_links:
    - from: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      to: "test/example/lib/example/accounts/crosswake_continuations.ex"
      via: "return/2 recovers the verifier from the continuation-bound session entry, deletes it, and passes it as private correlation input to complete/5"
      pattern: "CrosswakeContinuations.complete"
    - from: "test/example/lib/example_web/endpoint.ex"
      to: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      via: "Plug.Session signs and encrypts the HttpOnly cookie carrying the host-only verifier transport"
      pattern: "Plug.Session"
    - from: "test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts"
      to: "/crosswake/return"
      via: "Chromium records the actual navigation requests and cookie attributes while following the server redirects"
      pattern: "waitForRequest"
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-01 unclassified; this plan closes the verified secret-transport gap without inventing an owner disposition for that separate assumption."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-02 unclassified; the existing fail-closed denial matrix remains evidence and does not auto-dismiss that assumption."
  prohibitions:
    - requirement_id: XW-01
      category: secret-boundary
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT expose the PKCE verifier in the callback URL, browser history, logs, or observable request metadata."
    - requirement_id: XW-01
      category: authority-integrity
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT manufacture organization authority for a personal SIGRA session."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT let callback data select or reconstruct the session, subject, policy route, grant decision, or navigation destination."
---

<objective>
Remove the raw PKCE verifier from every GET callback surface while preserving the already-proven one-time continuation and evaluator flow.

Purpose: Close CR-01 and the remaining XW-01 blocker with a production-quality browser path: the verifier crosses the host boundary only in an encrypted HttpOnly session entry bound to the opaque continuation, never in a URL.
Output: Encrypted host-session transport, thin controller handoff, real-router negative coverage, and deterministic Chromium confidentiality evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@test/example/AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-CONTEXT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-VERIFICATION.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-REVIEW.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-06-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-07-SUMMARY.md

<interfaces>
- Preserve `CrosswakeContinuations.issue/3 -> {:ok, %{handle, state, pkce_verifier}}` and `complete/5`; only the controller transport changes.
- The callback URL contract becomes exactly `GET /crosswake/return?continuation=...&amp;state=...`; `pkce_verifier` is no longer an allowed request parameter.
- The host session entry is private to `CrosswakeController`, contains the issued continuation identity plus verifier, and is removed before terminal completion so replay cannot recover it.
- The endpoint cookie session must be signed, encrypted, SameSite=Lax, and explicitly HttpOnly; local HTTP tests remain supported without weakening production cookie policy elsewhere.
</interfaces>
</context>

## Artifacts this phase produces

- No new file paths or public symbols.
- `ExampleWeb.Endpoint.@session_options` gains encrypted and explicit HttpOnly cookie-session attributes.
- `ExampleWeb.CrosswakeController` gains one private continuation-bound PKCE session key plus private store/take/match helpers; `start/2` and `return/2` keep their public signatures.
- Existing ConnCase and Playwright suites gain verifier confidentiality and one-time transport assertions.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one PKCE verifier through encrypted host session transport</name>
  <reversibility rating="costly">Changing the example host's cookie-session encryption invalidates existing example-host cookies, but it requires no data migration and does not alter a published Sigra or generated-host contract.</reversibility>
  <files>test/example/lib/example_web/endpoint.ex, test/example/lib/example_web/controllers/crosswake_controller.ex, test/example/test/example_web/controllers/crosswake_controller_test.exs, test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts</files>
  <read_first>
    - Read all four target files completely before editing; preserve the existing session pipeline, fixed routes, evaluator assertions, response policy, and browser readiness behavior.
    - Read `test/example/lib/example/accounts/crosswake_continuations.ex` for the unchanged issue/complete correlation contract and atomic claim ordering.
    - Read `test/example/test/example/accounts/crosswake_continuations_test.exs` for the direct PKCE digest mismatch proof that remains below the HTTP transport.
    - Read `test/example/lib/example_web/user_auth.ex` for existing HttpOnly/SameSite cookie conventions and session renewal behavior.
    - Read `scripts/ci/hosted-session-interop-proof.sh` function `browser_only` so the browser verification stays bounded, readiness-driven, serial, retry-free, and redacted.
  </read_first>
  <behavior>
    - RED: the real-router tracer requires callback query keys to be exactly continuation/state and fails while start/2 still serializes pkce_verifier.
    - RED: a named confidentiality test obtains the server-side verifier from the test connection session and fails if that exact value occurs in Location, request URL/query string, Set-Cookie bytes, captured logs, response headers/body, or final redirect.
    - GREEN: start/2 stores verifier transport under a controller-private session key bound to the issued handle, then emits a 303 callback URL containing only continuation and state.
    - GREEN: return/2 accepts exactly two nonempty scalar URL values, rejects a missing/mismatched session transport before evaluator invocation, deletes a matching transport before complete/5, and supplies state plus the recovered verifier only to the context.
    - GREEN: Chromium observes no pkce_verifier query key in any navigation request, confirms the host session cookie is HttpOnly, keeps request URLs/history entries and observable headers free of verifier-shaped callback data, then lands at /app with the existing no-Referer and final DOM assertions intact.
  </behavior>
  <action>Write the failing ConnCase and Playwright expectations first. In `ExampleWeb.Endpoint`, add a stable example-host `encryption_salt` and explicit `http_only: true` to the existing `@session_options` while retaining the current store, key, signing salt, and SameSite=Lax behavior; this is required because signed-only cookie content is readable and is not an acceptable secret transport. In `CrosswakeController.start/2`, bind the issued verifier to its continuation in a private Plug-session entry before returning the 303, and build the callback query from only `continuation` and `state`. In `return/2`, make the two-key scalar allowlist exact, recover only a matching continuation-bound transport, remove it from the session before calling `complete/5`, and construct `%{"state" =&gt; state, "pkce_verifier" =&gt; verifier}` internally. Use constant-time comparison for the opaque continuation binding; do not trust or accept a verifier, route, authority, or destination from params. Missing, malformed, mismatched, or replayed transport must use the existing generic recovery and send no evaluator message. Preserve D-01/D-02/D-03/D-05 through D-14/D-17: controller remains thin, context still atomically claims and digest-validates, route/destination remain fixed, Referrer-Policy remains callback-wide, telemetry stays allowlisted, and no adapter/schema/core/generator/external-service changes occur. In ConnCase, capture the transient verifier only from the server-side test session, never print it, and assert it is absent from every observable HTTP/log surface; prove the session entry is removed on allow and cannot be replayed. In Playwright, record actual navigation requests from start through `/app`, require callback search keys exactly `continuation` and `state`, assert no recorded URL/header contains the `pkce_verifier` name, inspect cookie attributes for HttpOnly without logging values, retain the actual `/app` no-Referer assertion, and keep all waits event/readiness based with zero sleeps/retries.</action>
  <verify>
    <automated>bash -lc 'set -a; source tmp/db.env; set +a; cd test/example &amp;&amp; mix test test/example_web/controllers/crosswake_controller_test.exs --include example_app &amp;&amp; cd ../.. &amp;&amp; scripts/ci/hosted-session-interop-proof.sh --browser-only'</automated>
  </verify>
  <acceptance_criteria>
    - Real-router start returns 303 and `URI.decode_query/1` yields exactly `continuation` and `state`; no callback request accepts `pkce_verifier` from params.
    - The example session cookie is signed, encrypted, SameSite=Lax, and HttpOnly; the actual verifier is absent from Location, query/request URL, Set-Cookie bytes, response metadata/body, captured logs, final URL, and DOM.
    - A matching session transport reaches the unchanged `complete/5` correlation check once; missing/mismatched/replayed transport denies generically with zero evaluator calls and cannot change route or destination.
    - The Chromium test records actual navigation rather than synthesizing requests, sees only the two callback query keys, proves the cookie is HttpOnly and `/app` has no Referer, and uses no sleeps, retries, traces, screenshots, attachments, or secret logging.
    - `test/example/lib/example/accounts/crosswake_continuations.ex`, its migration/schema, Sigra core, root dependencies, installer/templates, generated-host fixtures, and COVERAGE.md remain unchanged.
  </acceptance_criteria>
  <done>The real authenticated cookie jar completes the same one-time Crosswake return, but the raw PKCE verifier exists only in encrypted HttpOnly host-session transport and never crosses any GET URL or observable metadata surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| controller -> encrypted host session | Raw verifier leaves transient issuance and must be confidential, bound, and one-time. |
| browser -> callback controller | Untrusted callback URL supplies only continuation and state; the cookie supplies opaque encrypted transport. |
| callback -> context/evaluator | Only matching server-recovered correlation may reach the atomic claim and evaluator sequence. |
| browser observer -> logs/history/metadata | URLs, headers, cookies, and test artifacts must not reveal the verifier. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-08-01 | Information Disclosure | callback URL/history/request metadata | high | mitigate | Remove verifier from params, encrypt/HttpOnly the host session, and block on dynamic ConnCase plus real Chromium negative assertions. |
| T-240.3-08-02 | Spoofing | continuation-to-verifier binding | high | mitigate | Bind transport to the issued opaque continuation, compare in constant time, and deny missing/mismatch before complete/evaluator. |
| T-240.3-08-03 | Elevation of Privilege | replayed transport | high | mitigate | Delete matching transport before terminal completion and retain the database atomic claim; router replay test requires zero second evaluator call. |
| T-240.3-08-04 | Tampering | callback authority/route/destination | high | mitigate | Exact two-key URL allowlist plus server-built correlation input and existing fixed route/destination assertions. |
| T-240.3-08-05 | Repudiation | browser test infers confidentiality from final URL only | high | mitigate | Record the actual navigation requests, cookie attributes, callback query keys, response metadata, and final `/app` request header. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe deterministic example-host runtime without core/generated-host widening | 08 | COVERED | Corrects only the proven callback secret leak. |
| REQ | XW-01 | Personal projection without credential/token exposure | 08 | COVERED | PKCE verifier leaves the GET surface and is encrypted/HttpOnly. |
| REQ | XW-02 | Return data alone grants nothing | 08 | COVERED | URL loses verifier input; matching host session and existing atomic claim remain required. |
| RESEARCH | — | Continuation-bound PKCE validation and telemetry redaction | 08 | COVERED | Preserves digest validation while correcting the research transport assumption. |
| CONTEXT | D-01–D-17 | Locked host ownership, authority, response, proof, and scope boundaries | 08 | COVERED | D-05/D-11 drive the correction; every other decision is preserved by regression checks. |
| CONTEXT | Deferred ideas | Generator/public abstraction/network/provider/device work | — | EXCLUDED | Explicitly deferred and absent. |

API coverage detector: existing `COVERAGE.md` remains authoritative and unchanged; no external endpoint, SDK, webhook, hosted API, or remote authentication surface is added. Assumption-delta detector: `detected: true` for lexical pluralization signals `session`, `cookie`, and `callback`; the plan records them here and introduces no selectable/configurable architecture. Schema-push detector: the changed files contain no supported JS/TS ORM pattern; the Ecto schema is intentionally untouched.

<verification>
Run the complete real-router suite and the bounded serial browser proof. Source inspection or a clean final URL alone is insufficient: the callback must have only two query keys, the cookie must be encrypted/HttpOnly, and dynamic server-known verifier bytes must be absent from observable surfaces. Every high-severity threat blocks completion.
</verification>

<success_criteria>
- The verifier is no longer serialized into any GET URL and cannot be supplied by callback input.
- One matching encrypted HttpOnly host-session transport completes exactly once through the unchanged context/evaluator path.
- Router and Chromium evidence prove the URL/history/header/log/final-response confidentiality boundary deterministically.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-08-SUMMARY.md` when done.
</output>
