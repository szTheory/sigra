---
phase: 240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow
plan: "08"
type: execute
wave: 8
depends_on: ["240.3-07"]
files_modified:
  - test/example/config/config.exs
  - test/example/config/test.exs
  - test/example/lib/example_web/endpoint.ex
  - test/example/lib/example/accounts/crosswake_continuations.ex
  - test/example/lib/example_web/controllers/crosswake_controller.ex
  - test/example/test/example_web/controllers/crosswake_controller_test.exs
  - test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts
  - scripts/ci/hosted-session-interop-proof.sh
autonomous: true
gap_closure: true
requirements: [XW-01, XW-02]
must_haves:
  truths:
    - "Per D-03/D-05/D-06/D-07, POST /crosswake/start transports the raw PKCE verifier only inside a signed, encrypted, HttpOnly, SameSite=Lax host session entry whose absolute expiry is the continuation's issued expiry; GET /crosswake/return accepts only continuation and state in its URL."
    - "The host session cookie is Secure by default in every non-test configuration; only the deterministic MIX_ENV=test loopback browser harness sets Secure=false, and expired, missing, mismatched, or terminal transport entries cannot survive or reach complete/5 or evaluation."
    - "Per D-09/D-11/D-14, the verifier is absent from the callback URL, Chromium-observed navigation history, request URLs, request metadata, response headers, logs, final URL, and DOM while the real cookie jar still reaches the fixed /app destination."
    - "Per D-01/D-02/D-17, the correction stays in the example-host endpoint/controller and focused tests; it does not change Sigra core, generated hosts, the Crosswake adapter, persistence schema, or external API scope."
  artifacts:
    - path: "test/example/lib/example_web/endpoint.ex"
      provides: "Signed, encrypted, HttpOnly, SameSite=Lax Plug session with fail-closed configurable Secure policy"
      contains: "encryption_salt"
    - path: "test/example/config/config.exs"
      provides: "Secure=true default for every non-test example-host configuration"
      contains: "crosswake_session_secure"
    - path: "test/example/config/test.exs"
      provides: "Narrow Secure=false override for deterministic loopback tests only"
      contains: "crosswake_session_secure"
    - path: "test/example/lib/example/accounts/crosswake_continuations.ex"
      provides: "The exact persisted continuation expiry returned with issued correlation material"
      contains: "expires_at"
    - path: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      provides: "Continuation-bound PKCE session handoff and two-key callback URL"
      contains: "crosswake_pkce"
    - path: "test/example/test/example_web/controllers/crosswake_controller_test.exs"
      provides: "Real-router verifier confidentiality, one-time transport, and denial proof"
    - path: "test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts"
      provides: "Real Chromium URL/history/header/cookie-attribute confidentiality proof"
  key_links:
    - from: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      to: "test/example/lib/example/accounts/crosswake_continuations.ex"
      via: "return/2 recovers the verifier from the continuation-bound session entry, deletes it, and passes it as private correlation input to complete/5"
      pattern: "CrosswakeContinuations.complete"
    - from: "test/example/lib/example_web/endpoint.ex"
      to: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      via: "Plug.Session signs and encrypts the HttpOnly/SameSite cookie carrying the host-only verifier transport and applies the configured Secure flag"
      pattern: "Plug.Session"
    - from: "test/example/lib/example/accounts/crosswake_continuations.ex"
      to: "test/example/lib/example_web/controllers/crosswake_controller.ex"
      via: "issue/3 returns the exact persisted expires_at; return/2 denies and deletes at now >= expires_at before complete/5"
      pattern: "expires_at"
    - from: "test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts"
      to: "/crosswake/return"
      via: "Chromium records the actual navigation requests and cookie attributes while following the server redirects"
      pattern: "waitForRequest"
  assumptions:
    - requirement_id: XW-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-01 unclassified; this plan closes the verified secret-transport gap without inventing an owner disposition for that separate assumption."
    - requirement_id: XW-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less edge probe left XW-02 unclassified; the existing fail-closed denial matrix remains evidence and does not auto-dismiss that assumption."
  prohibitions:
    - requirement_id: XW-01
      category: secret-boundary
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT expose the PKCE verifier in the callback URL, browser history, logs, or observable request metadata."
    - requirement_id: XW-01
      category: authority-integrity
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT manufacture organization authority for a personal SIGRA session."
    - requirement_id: XW-02
      category: authority-smuggling
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT let callback data select or reconstruct the session, subject, policy route, grant decision, or navigation destination."
---

<objective>
Remove the raw PKCE verifier from every GET callback surface while preserving the already-proven one-time continuation and evaluator flow.

Purpose: Close CR-01 and the remaining XW-01 blocker with a production-quality browser path: the verifier crosses the host boundary only in an encrypted HttpOnly session entry bound to the opaque continuation, never in a URL.
Output: Encrypted host-session transport, thin controller handoff, real-router negative coverage, and deterministic Chromium confidentiality evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@test/example/AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-CONTEXT.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-VERIFICATION.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-REVIEW.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-06-SUMMARY.md
@.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-07-SUMMARY.md

<interfaces>
- Extend the internal example-host `CrosswakeContinuations.issue/3` success value to `{:ok, %{handle, state, pkce_verifier, expires_at}}`, where `expires_at` is the exact value persisted on the continuation; preserve `complete/5` unchanged.
- The callback URL contract becomes exactly `GET /crosswake/return?continuation=...&amp;state=...`; `pkce_verifier` is no longer an allowed request parameter.
- The host session entry is private to `CrosswakeController`, is keyed and redundantly bound to the issued continuation, contains only the verifier and the continuation's exact absolute expiry, and supports concurrent departures without one flow overwriting another.
- On `now >= expires_at`, missing entry/binding, binding mismatch, malformed entry, or any terminal completion attempt, remove the addressed transport entry before denying or calling `complete/5`; no such path may invoke the evaluator with stale or mismatched transport.
- `config :example, :crosswake_session_secure` defaults to `true`; `test.exs` alone overrides it to `false` for the deterministic loopback HTTP harness. `Endpoint` consumes it with `Application.compile_env!/2`; signed/encrypted, `http_only: true`, and `same_site: "Lax"` remain unconditional.
- `hosted-session-interop-proof.sh --browser-only` runs the example server under `MIX_ENV=test`, making the insecure-cookie exception test-only rather than weakening any dev/prod host configuration.
</interfaces>
</context>

## Artifacts this phase produces

- No new file paths or public symbols.
- Example-host config and `ExampleWeb.Endpoint.@session_options` gain the fail-closed Secure default, test-only loopback override, encryption, integrity, explicit HttpOnly, and SameSite attributes.
- `CrosswakeContinuations.issue/3` returns its exact persisted expiry; `ExampleWeb.CrosswakeController` gains one private continuation-bound PKCE session map plus private store/take/prune/match helpers, while `start/2` and `return/2` keep their public signatures.
- The browser harness runs in the test environment, and existing ConnCase and Playwright suites gain cookie-policy, verifier-confidentiality, expiry-boundary, integrity-tamper, and one-time-deletion assertions.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce the host cookie policy through the real loopback browser path</name>
  <reversibility rating="costly">Changing the example host's cookie-session encryption invalidates existing example-host cookies, but it requires no data migration and does not alter a published Sigra or generated-host contract.</reversibility>
  <files>test/example/config/config.exs, test/example/config/test.exs, test/example/lib/example_web/endpoint.ex, test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts, scripts/ci/hosted-session-interop-proof.sh</files>
  <read_first>
    - Read all five target files completely before editing; preserve endpoint/session initialization, proof-runner bounds, fixed routes, response policy, and browser readiness behavior.
    - Read `test/example/lib/example_web/user_auth.ex` for existing HttpOnly/SameSite cookie conventions and session renewal behavior.
    - Read `scripts/ci/hosted-session-interop-proof.sh` function `browser_only` so the browser verification stays bounded, readiness-driven, serial, retry-free, and redacted.
  </read_first>
  <behavior>
    - RED: configuration/source assertions fail until `crosswake_session_secure` is true by default, overridden to false only by `test.exs`, and consumed by the endpoint without an environment-variable bypass.
    - RED: the real Chromium cookie assertion fails until the session cookie is HttpOnly and SameSite=Lax and the loopback harness is explicitly running the sole Secure=false test configuration.
    - GREEN: every dev/prod or other non-test compilation receives Secure=true, encryption and integrity remain unconditional, and only MIX_ENV=test loopback HTTP receives Secure=false.
    - GREEN: Chromium follows the existing hosted return, observes the test cookie as `httpOnly: true`, `sameSite: "Lax"`, and `secure: false` on the loopback HTTP origin, and lands at /app with the existing no-Referer and DOM assertions intact.
  </behavior>
  <action>Write the failing configuration and Playwright cookie-attribute expectations first. Define `config :example, :crosswake_session_secure, true` in `config.exs` and override it with `false` only in `test.exs`; do not add an environment-variable escape hatch. In `ExampleWeb.Endpoint`, load that value with `Application.compile_env!/2` and set `secure:`, a stable example-host `encryption_salt`, `http_only: true`, and `same_site: "Lax"` in the existing cookie `@session_options`, retaining the current store, key, and signing salt so confidentiality and integrity are both configured on every environment. Change only the `browser_only` harness commands needed to compile, migrate, seed, and serve under `MIX_ENV=test`, with `PHX_SERVER=true` and the existing loopback/free-port boundary; preserve bounded commands, redacted diagnostics, readiness checks, and zero retries in Playwright. In Playwright, inspect the actual `_example_key` cookie without logging its value: `httpOnly` must be true, `sameSite` must be `Lax`, and `secure` must be false only because the asserted origin is loopback HTTP and the server is the MIX_ENV=test harness. Retain the existing hosted-return navigation, actual `/app` no-Referer assertion, and event/readiness waits; Task 2 narrows the callback and adds the final secret-negative assertions after its controller change. Preserve D-01/D-03/D-09/D-11/D-14/D-15/D-17 and do not change application routes, destinations, public APIs, schemas, dependencies, generated hosts, or external-service scope.</action>
  <verify>
    <automated>bash -lc 'set -a; source tmp/db.env; set +a; cd test/example &amp;&amp; MIX_ENV=test mix compile --force &amp;&amp; cd ../.. &amp;&amp; scripts/ci/hosted-session-interop-proof.sh --browser-only'</automated>
  </verify>
  <acceptance_criteria>
    - `crosswake_session_secure` is true in shared config and false only in `test.exs`; endpoint compilation fails closed if the setting is absent and no runtime environment variable can disable Secure in dev or prod.
    - Endpoint cookie options unconditionally include the existing signing salt plus encryption salt, HttpOnly=true, SameSite=Lax, and the configured Secure value.
    - The browser-only proof compiles and serves with MIX_ENV=test on loopback HTTP; Chromium observes `_example_key` with HttpOnly=true, SameSite=Lax, Secure=false and no Referer on `/app`.
    - The Chromium proof uses actual navigation with no sleeps, retries, traces, screenshots, attachments, cookie-value output, or secret logging.
  </acceptance_criteria>
  <done>The real authenticated loopback test cookie jar proves the narrowly scoped Secure exception while every non-test host configuration retains signed, encrypted, HttpOnly, SameSite=Lax, Secure-only transmission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind verifier transport to the continuation's exact expiry and delete it once</name>
  <reversibility rating="reversible">The added expiry field is an internal example-host issue result and the session entry is transient; no persisted schema or published interface changes.</reversibility>
  <files>test/example/lib/example/accounts/crosswake_continuations.ex, test/example/lib/example_web/controllers/crosswake_controller.ex, test/example/test/example_web/controllers/crosswake_controller_test.exs, test/example/priv/playwright/tests/crosswake-hosted-runtime.spec.ts</files>
  <read_first>
    - Read all four target files completely before editing; preserve the persisted strict-expiry predicate, atomic claim ordering, fixed routes, evaluator assertions, response policy, telemetry allowlist, cookie checks from Task 1, and browser readiness behavior.
    - Read `test/example/test/example/accounts/crosswake_continuations_test.exs` for the existing exact expiry boundary and concurrent-flow proof.
    - Read `Plug.Crypto.MessageEncryptor` and `Plug.Session.COOKIE` behavior already available through the locked Plug dependency only as needed to author the tampered-cookie integrity assertion; add no package.
  </read_first>
  <behavior>
    - RED: the real-router start test fails until the private session entry contains the issued handle binding, verifier, and the exact `expires_at` persisted for that continuation, while the callback URL has exactly continuation/state.
    - RED: expiry, malformed entry, missing entry, binding mismatch, tampered cookie, callback state mismatch, allow, deny, and replay cases fail until the addressed verifier entry is removed exactly once and no pre-completion denial invokes complete/5 or the evaluator.
    - GREEN: `issue/3` returns the persisted `expires_at`; start stores independent handle-keyed entries so concurrent departures remain isolated; return prunes expired entries and atomically takes the addressed entry from Plug session before any completion call.
    - GREEN: the strict boundary is `now >= expires_at`: the entry is deleted and denied before complete/5/evaluation; a live constant-time-matched binding supplies the recovered verifier internally to complete/5, and its entry remains deleted for either terminal allow or deny.
    - GREEN: tampering with the signed/encrypted cookie yields generic recovery and zero evaluator calls; the actual verifier is absent from Location, callback URL/query, Set-Cookie bytes, response headers/body, captured logs, final URL, and DOM-facing redirect data.
    - GREEN: Chromium records every actual navigation request, observes callback search keys exactly continuation/state, finds no verifier name or server-known verifier bytes in URL/history/observable headers, and preserves the cookie-policy and `/app` assertions from Task 1.
  </behavior>
  <action>Write the failing ConnCase and final Playwright confidentiality cases first. In `CrosswakeContinuations.insert_continuation/3`, return the exact `expires_at` value already persisted alongside handle, state, and verifier; do not change the database schema, TTL computation, `complete/5`, or atomic claim predicate. In `CrosswakeController.start/2`, store a controller-private map keyed by issued handle; each value must redundantly contain that handle, the verifier, and the returned absolute expiry, preserving multiple concurrent departures. Build the 303 callback query from only `continuation` and `state`. In `return/2`, accept exactly two nonempty scalar URL values, prune expired entries, remove the addressed entry from the Plug session before validation or `complete/5`, compare the requested handle with the embedded binding in constant time, and require `DateTime.compare(now, expires_at) == :lt`. On expiry, malformed data, missing entry/binding, or binding mismatch, persist the cleaned session and use generic recovery before `complete/5` or evaluation. For a live match, construct `%{"state" =&gt; state, "pkce_verifier" =&gt; verifier}` internally and call unchanged `complete/5`; because the take occurs first, both allow and terminal deny leave no recoverable verifier and replay cannot reuse it. In ConnCase, obtain the transient verifier and exact expiry only from the server-side test session, never print either secret-bearing entry or cookie value, and assert: callback query keys are exactly continuation/state; stored expiry equals the value persisted for that handle; `now >= expires_at` deletes and denies before complete/evaluator; malformed/missing/mismatched/tampered transport deletes or is absent and denies; allow and state/PKCE terminal deny each delete once; replay has zero evaluator calls. Assert configuration/source contracts for Secure=true default, test-only false override, signing salt, encryption salt, HttpOnly, and SameSite=Lax; mutate one byte of the actual session cookie to prove integrity rejection. Scan Location, query/request URL, Set-Cookie bytes, response metadata/body, captured logs, and final redirect for the server-known verifier. In Playwright, retain Task 1's cookie checks, record actual navigation requests, require callback search keys exactly continuation/state, and assert that no observed request URL/header, history entry, final URL, or DOM contains the verifier name or verifier-shaped callback data; keep the real `/app` no-Referer assertion and zero sleeps/retries. Preserve D-01/D-02/D-03/D-05 through D-14/D-17: controller remains thin, route/destination fixed, Referrer-Policy callback-wide, telemetry allowlisted, and no adapter/schema/core/generator/external-service changes occur.</action>
  <verify>
    <automated>bash -lc 'set -a; source tmp/db.env; set +a; cd test/example &amp;&amp; MIX_ENV=test mix test test/example_web/controllers/crosswake_controller_test.exs --include example_app &amp;&amp; cd ../.. &amp;&amp; scripts/ci/hosted-session-interop-proof.sh --browser-only'</automated>
  </verify>
  <acceptance_criteria>
    - Real-router start returns 303, `URI.decode_query/1` yields exactly continuation/state, and the matching session entry carries the issued handle, verifier, and exact persisted continuation expiry.
    - Shared/test config and endpoint source assertions prove Secure=true outside test, the sole loopback-test override, signing plus encryption, HttpOnly=true, and SameSite=Lax; a one-byte cookie mutation is rejected generically with zero evaluator calls.
    - At `now >= expires_at`, and on malformed, missing, or mismatched binding, the addressed entry is absent after the response and denial occurs before `complete/5` or evaluation.
    - A live matched entry reaches unchanged `complete/5` once; allow and terminal correlation denial both remove it before completion, and replay cannot recover it or invoke the evaluator again.
    - The server-known verifier is absent from Location, query/request URL, Set-Cookie bytes, response metadata/body, captured logs, final URL, and DOM-facing redirect data.
    - Chromium records actual navigation, sees only continuation/state on the callback, preserves HttpOnly/SameSite/Secure-exception and `/app` no-Referer assertions, and emits no secret-bearing artifacts.
    - Continuation migration/schema, Sigra core, root dependencies, installer/templates, generated-host fixtures, and COVERAGE.md remain unchanged.
  </acceptance_criteria>
  <done>The verifier transport is confidential, integrity-protected, bound to the continuation's exact lifetime, and removed before every expiry, invalid binding, or terminal completion path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| controller -> signed/encrypted host session | Raw verifier leaves transient issuance and must be confidential, integrity-protected, expiry-bound, and one-time. |
| browser -> callback controller | Untrusted callback URL supplies only continuation and state; the cookie supplies opaque encrypted transport. |
| callback -> context/evaluator | Only matching server-recovered correlation may reach the atomic claim and evaluator sequence. |
| browser observer -> logs/history/metadata | URLs, headers, cookies, and test artifacts must not reveal the verifier. |
| host configuration -> cookie transmission | Every non-test host must require Secure HTTPS transmission; only the deterministic test loopback may disable it. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-240.3-08-01 | Information Disclosure | callback URL/history/request metadata | high | mitigate | Remove verifier from params, sign/encrypt/HttpOnly the host session, and block on dynamic ConnCase plus real Chromium negative assertions. |
| T-240.3-08-02 | Spoofing | continuation-to-verifier binding | high | mitigate | Bind transport to the issued opaque continuation and exact persisted expiry, compare in constant time, and delete/deny missing, malformed, expired, or mismatched entries before complete/evaluator. |
| T-240.3-08-03 | Elevation of Privilege | replayed or stale transport | high | mitigate | Atomically take transport before every terminal completion, enforce `now < expires_at`, retain the database atomic claim, and require zero replay evaluator calls. |
| T-240.3-08-04 | Tampering | callback authority/route/destination | high | mitigate | Exact two-key URL allowlist plus server-built correlation input and existing fixed route/destination assertions. |
| T-240.3-08-05 | Repudiation | browser test infers confidentiality from final URL only | high | mitigate | Record actual navigation requests, all cookie policy attributes, callback query keys, response metadata, and the final `/app` request header. |
| T-240.3-08-06 | Information Disclosure | non-test HTTP cookie transmission | high | mitigate | Default Secure=true, permit false only in test config, run loopback browser under MIX_ENV=test, and assert config/source plus actual cookie attributes. |
| T-240.3-08-07 | Tampering | client cookie payload | high | mitigate | Retain signing with encryption and prove a one-byte cookie mutation fails closed before evaluator invocation. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe deterministic example-host runtime without core/generated-host widening | 08 | COVERED | Corrects only the proven callback secret leak. |
| REQ | XW-01 | Personal projection without credential/token exposure | 08 | COVERED | PKCE verifier leaves the GET surface and uses signed/encrypted, HttpOnly, SameSite=Lax transport with Secure-by-default transmission and exact expiry. |
| REQ | XW-02 | Return data alone grants nothing | 08 | COVERED | URL loses verifier input; live matching one-time host transport and the existing atomic claim remain required. |
| RESEARCH | — | Continuation-bound PKCE validation and telemetry redaction | 08 | COVERED | Preserves digest validation while correcting the research transport assumption. |
| CONTEXT | D-01–D-17 | Locked host ownership, authority, response, proof, and scope boundaries | 08 | COVERED | D-05/D-11 drive the correction; every other decision is preserved by regression checks. |
| CONTEXT | Deferred ideas | Generator/public abstraction/network/provider/device work | — | EXCLUDED | Explicitly deferred and absent. |

API coverage detector: existing `COVERAGE.md` remains authoritative and unchanged; no external endpoint, SDK, webhook, hosted API, or remote authentication surface is added. Assumption-delta detector: `detected: true` for lexical pluralization signals `session`, `cookie`, and `callback`; the plan records them here and introduces no selectable/configurable architecture. Schema-push detector: the changed files contain no supported JS/TS ORM pattern; the Ecto schema is intentionally untouched.

<verification>
Run the complete real-router suite and bounded serial browser proof. Source inspection or a clean final URL alone is insufficient: the callback must have only two query keys; config/source and runtime evidence must prove Secure-by-default, the test-only exception, signing, encryption, HttpOnly, and SameSite=Lax; the exact issued expiry and one-time deletion must be exercised; and server-known verifier bytes must be absent from observable surfaces. Every high-severity threat blocks completion.
</verification>

<success_criteria>
- The verifier is no longer serialized into any GET URL and cannot be supplied by callback input.
- The host cookie is Secure in every non-test configuration, with Secure=false limited to the asserted MIX_ENV=test loopback harness; signing, encryption, HttpOnly, and SameSite=Lax are unconditional.
- One matching live host-session transport completes exactly once through the unchanged context/evaluator path; exact-boundary expiry, invalid binding, terminal allow/deny, and replay leave no verifier entry.
- ConnCase and Chromium evidence prove cookie policy, integrity rejection, exact transport lifetime, one-time deletion, and the URL/history/header/log/final-response confidentiality boundary deterministically.
</success_criteria>

<output>
Create `.planning/phases/240.3-close-gap-xw-01-xw-02-wire-hosted-crosswake-runtime-flow/240.3-08-SUMMARY.md` when done.
</output>
