# Phase 240.2: Close gap: OPS-01 — add controller-mode generated-host compile proof - Context

**Gathered:** 2026-08-10 (assumptions mode)
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the single OPS-01 integration gap identified by the v1.48 milestone audit: add deterministic fresh generated-host compile proof for the B2C controller-mode (`--no-live`) output. Preserve the canonical LiveView B2C leg and the existing provider-neutral, no-secrets proof boundary. This phase does not add browser coverage, provider behavior, mail/device evidence, product capability, or unrelated CI repair.
</domain>

<decisions>
## Implementation Decisions

### Requirement and closure scope
- **D-01:** Map Phase 240.2 explicitly to OPS-01 and close only the broken integration from the generated controller limiter to fresh `--no-live` compile proof. Do not broaden this urgent repair into other operations, provider, device, validation, or CI-gate work.

### Controller-mode lane shape
- **D-02:** Add a distinct fresh B2C controller-mode leg using `--no-admin --no-organizations --no-passkeys --no-live`; do not replace or alter the canonical LiveView B2C leg established by Phase 237.

### Proof boundary and harness reuse
- **D-03:** Reuse `scripts/ci/passkeys-opt-out-smoke.sh` and its established isolated fresh-host lifecycle, including dependency refresh after installation, assets, `mix compile --warnings-as-errors`, migration, and boot. A source-only assertion or partial compile fixture is insufficient.
- **D-04:** Keep rendered browser and provider-runtime coverage in the independent Phase 238/generated-auth runtime lane. Phase 240.2 proves generated controller output compiles and boots; it does not create another browser suite.

### No-secrets and provider ownership boundary
- **D-05:** Preserve credential-free execution: unset inherited Google credentials and use only the existing disposable Cloak fixture. Do not change OAuth dependency ownership, provider behavior, or claim real Google, transactional-email, or physical-device success.

### the agent's Discretion
- Choose the smallest repo-consistent harness refactor, leg name, and focused contract assertions that make both LiveView and controller-mode ownership explicit without duplicating the lifecycle implementation.
- Choose whether the controller leg needs additional bounded runtime probes beyond the required compile/boot lifecycle only when existing harness structure makes them effectively free; do not expand into browser or external-provider proof.
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `.planning/ROADMAP.md` — Phase 240.2 insertion and dependency boundary.
- `.planning/REQUIREMENTS.md` — OPS-01 acceptance requirement and operations scope.
- `.planning/v1.48-MILESTONE-AUDIT.md` — authoritative broken integration, failing evidence, and required closure.
- `.planning/phases/237-canonical-b2c-generator-contract/237-CONTEXT.md` — canonical LiveView B2C profile that must remain unchanged.
- `.planning/phases/238-generated-auth-runtime-proof/238-CONTEXT.md` — independent rendered/browser and no-secrets runtime proof boundary.
- `.planning/phases/240-alpha-operations-rehearsal/240-CONTEXT.md` — generated limiter, provider-neutral evidence, and truthful-claims decisions.
- `.planning/phases/240.1-repair-canonical-b2c-oauth-recipe-handoff/240.1-CONTEXT.md` — host-owned `cloak_ecto` prerequisite and generator-ownership fence.
- `scripts/ci/passkeys-opt-out-smoke.sh` — canonical fresh generated-host lifecycle to extend with the controller-mode leg.
- `scripts/ci/generated-auth-runtime-proof.sh` — separate LiveView runtime/browser lane that must not absorb this proof.
- `test/sigra/install/generated_rate_limit_contract_test.exs` — existing failing contract requiring LiveView and controller generated-host compile coverage.
- `test/sigra/install/generator_passkeys_opt_out_test.exs` — canonical B2C harness and profile invariants.
- `lib/mix/tasks/sigra.install.ex` — public `--live` / `--no-live` installer contract.
- `lib/sigra/install/features/core.ex` — controller-versus-LiveView generated file and router ownership.
- `test/sigra/install/features/core_test.exs` — controller-mode emission and routing expectations.
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `scripts/ci/passkeys-opt-out-smoke.sh` already owns isolated Phoenix/Postgres host creation, local Sigra dependency wiring, installer execution, dependency refresh, assets, warning-free compilation, migration, boot, and cleanup.
- `test/sigra/install/generated_rate_limit_contract_test.exs` already states the missing acceptance contract: the canonical smoke must include `--no-live` and warning-free controller-router compilation while the runtime harness retains LiveView proof.
- Existing controller templates and feature tests already define what `--no-live` emits; the gap is executable fresh-host integration evidence, not a new generator design.

### Established Patterns
- The canonical B2C LiveView profile is a locked generated-host contract and must not be silently converted to controller mode.
- Fresh-host evidence is command-level and deterministic: generated artifacts must compile with warnings as errors and boot in an isolated host rather than pass through source-string checks alone.
- Library CI uses disposable fixtures and explicitly excludes live provider credentials and adopter staging claims.

### Integration Points
- Extend the existing smoke harness with a separate controller-mode leg that invokes the same lifecycle with `--no-live`.
- Keep the focused generated-rate-limit contract aligned with the executable harness rather than weakening or deleting its controller compile expectation.
- Preserve the existing CI job boundary: the canonical generator smoke owns controller compile proof; the generated-auth runtime job remains LiveView/browser proof.
</code_context>

<specifics>
## Specific Ideas

- The controller leg should be visibly distinct from the canonical LiveView leg so future maintainers cannot mistake `--no-live` for a change to the canonical B2C profile.
- Warning-free compilation and boot are the minimum honest closure because controller-only router/template references can evade source-level checks.
</specifics>

<deferred>
## Deferred Ideas

- Nyquist validation refreshes for Phases 238, 239, 240, and 240.1 remain separate audit follow-up work.
- Real Google authorization, transactional-email delivery, adopter-host integration, and physical-iPhone proof remain host launch gates.

### Reviewed Todos (not folded)
- All 32 automatic TODO matches were reviewed; the user chose to fold none. Historical auth UI, admin, security, release, configuration, and CI issues remain outside this narrow OPS-01 closure.
</deferred>

---

*Phase: 240.2-close-gap-ops-01-add-controller-mode-generated-host-compile-proof*
*Context gathered: 2026-08-10*
