---
phase: "206"
plan: "04"
type: execute
wave: 2
depends_on:
  - "206-01"
  - "206-02"
files_modified:
  - guides/reference/admin-quality-ledger.md
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "D-01: All 8 ledger rows are L1 components only (notice, notice_link, stat, stat_link, summary_chip, task_card, applied_chip, audit_row) — no page-composition rows modified"
    - "D-08: All 8 L1 ledger rows show bare 2 in the tier column (column 4, awk -F'|' parse); no decorated integers like 2* or 2+ anywhere in the ledger"
    - "D-08: Each flipped row's Evidence column contains semicolon-delimited proof covering: 3-project axe+screenshot citation (admin-design.spec.ts); motion-tokens guard citation (scripts/ci/admin-css-conformance.sh); target-size (reviewed — specific minimum per component); token-conformance (admin-css-conformance.sh exits 0); N/A for inapplicable proxies"
    - "D-07: Evidence strings cite --sg-motion-* and --sg-ease / --sg-ease-* (not --sg-duration-*)"
    - "scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 after all 8 rows are flipped to 2"
  artifacts:
    - guides/reference/admin-quality-ledger.md
  key_links:
    - "The monotonic guard reads column 4 with awk -F'|' and /^[012]$/ — bare 2 only, no decorators"
    - "Plan 02 SUMMARY provides the per-component audit findings (interaction-state, target-size, light/dark) that become the evidence strings"
    - "Plan 01 produced scripts/ci/admin-css-conformance.sh whose name is the citable motion/hex guard"
---

<objective>
Flip all 8 L1 component rows in guides/reference/admin-quality-ledger.md from tier 1
to bare 2, with rich semicolon-delimited evidence strings mirroring the Tier-2 exemplar
format established by the index-live row (~:85). Verify the monotonic guard exits 0 vs
origin/main after the flip.

Purpose: This is the ledger-flip step — the final assertion that locks Tier-2 status
for all 8 components under the monotonic guard's forward-only protection (D-08). Every
evidence string cites the automated gates (Plan 01 guard + existing Playwright axe/screenshot
harness) and the documented-as-manual proxy outcomes from Plan 02's audit.

Output: guides/reference/admin-quality-ledger.md with 8 rows at bare tier 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/206-l1-component-elevation-wave-a/206-CONTEXT.md
@.planning/phases/206-l1-component-elevation-wave-a/206-01-SUMMARY.md
@.planning/phases/206-l1-component-elevation-wave-a/206-02-SUMMARY.md
@guides/reference/admin-quality-ledger.md
@guides/reference/admin-fractal-scorecard.md
@scripts/ci/quality-ledger-monotonic.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip all 8 L1 ledger rows to bare tier 2 with rich evidence</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — read the full file; pay particular attention to:
      (a) the 8 L1 rows at lines ~61–73 (stat, stat_link, task_card, summary_chip, applied_chip, notice, notice_link, audit_row — each currently at tier 1 with sparse evidence)
      (b) the Tier-2 exemplar evidence string on the index-live row at ~:85 (semicolon-delimited, covers axe+screenshot / motion-tokens / density / target-size / content-equivalence / overlay-axe / APG — with N/A for inapplicable proxies)
      (c) the "Parsing Rules" section and "Asserting Tier 2" section explaining the bare-2 requirement and forbidden decorators
  - .planning/phases/206-l1-component-elevation-wave-a/206-02-SUMMARY.md — read the "Per-Component Audit" section for per-component target-size findings and interaction-state confirmations
  - .planning/phases/206-l1-component-elevation-wave-a/206-01-SUMMARY.md — read to confirm the guard script name and its exit-0 evidence (the PASS output line)
  - guides/reference/admin-fractal-scorecard.md lines 54 and 160–175 — L1 add-on proxy vocabulary (interaction-state, motion-token, target-size, token-conformance); confirm which proxies are inapplicable for pure display-only L1 components
  </read_first>

  <action>
  Use Edit (scoped replacement) to update each of the 8 L1 rows. Never use Write on the full ledger file. Apply one Edit call per row (or batch rows together if the surrounding context makes them unambiguous).

  For each row, the new format is:
    | {component-name} | L1 | 2 | {evidence-string} |

  The tier column must be the bare integer 2 — no asterisks, no decorators, no trailing spaces inside the pipe-delimited cell beyond normal table padding. The awk -F'|' parser reads column 4 ($4) and requires /^[012]$/ after trimming whitespace.

  Evidence string template (semicolon-delimited; omit inapplicable proxies with N/A):

  "admin-design.spec.ts assertBoardScreenshot board-{name} — 3 projects (admin-design-chromium / -mobile / -dark) × toHaveScreenshot + assertNoAxeViolations (wcag2a/2aa/21a/21aa/22aa) — 0 violations; motion-tokens: guarded — scripts/ci/admin-css-conformance.sh exits 0; no transition: all; component transitions reference --sg-motion-* / --sg-ease tokens and inherit global @media (prefers-reduced-motion: reduce) strip at sigra_admin.css:~1467; target-size: reviewed — {component-specific-value}; token-conformance: admin-css-conformance.sh exits 0; no raw hex outside :root token defs; {inapplicable-proxies}"

  Per-component evidence specifics (fill in from Plan 02 SUMMARY audit findings):

  STAT (stat): display-only metric block. target-size: N/A — no interactive target on stat; interaction-state: N/A — display-only; content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  STAT_LINK (stat_link): interactive metric link (anchor wraps full card). target-size: reviewed — full card block ≥ 44×44 CSS px; hover/focus/active states present via sg-metric-link CSS (~:1384); interaction-state: confirmed present; content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  TASK_CARD (task_card): interactive card (sg-card-hover). target-size: reviewed — card block min-height from layout ≥ 44 CSS px tall, full width; hover/focus state present via sg-card-hover CSS (~:559); interaction-state: confirmed present; content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  SUMMARY_CHIP (summary_chip): display-only chip. target-size: N/A — display-only; interaction-state: N/A; content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  APPLIED_CHIP (applied_chip): interactive chip with remove control. target-size: reviewed — remove control inherits sg-filter-chip__remove min-height ≥ 24×24 CSS px; hover/focus on remove control present via sg-* CSS (~:966); interaction-state: confirmed present; content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  NOTICE (notice): display-only notice with 5 tone variants (info/warn/risk/ok/caution). target-size: reviewed — notice__action link where present ≥ 24×24 CSS px (inline link height from line-height); interaction-state: notice__action hover present (~:1064); content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  NOTICE_LINK (notice_link): inline link used within notice body. target-size: reviewed — inline anchor ≥ 24×24 CSS px from line-height context; hover/focus/active via inherited sg-* link styles; interaction-state: confirmed present; content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  AUDIT_ROW (audit_row): display-only audit feed row. target-size: N/A — display-only; interaction-state: N/A; content-equivalence: N/A; overlay-axe: N/A; APG: N/A.

  Use real line references from Plan 02 SUMMARY wherever Plan 02's audit documented specific CSS line numbers. If SUMMARY gives exact target-size px values (e.g., "min-height: 32px confirmed from line 987"), use those; otherwise use "≥ 24×24 CSS px" as the documented minimum.

  Apply Edits using the existing sparse evidence string as the old_string to replace (each row is unique, so old_string is unambiguous). Preserve table column spacing.
  </action>

  <verify>
    <automated>grep -E '^\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{tier=$4; gsub(/^ +| +$/, "", tier); if (tier ~ /^[012]$/) print tier}' | sort | uniq -c</automated>
  </verify>

  <acceptance_criteria>
  - All 8 L1 component rows (stat, stat_link, task_card, summary_chip, applied_chip, notice, notice_link, audit_row) show bare 2 in column 4 — confirmed by: grep -E '^\| (stat|stat_link|task_card|summary_chip|applied_chip|notice|notice_link|audit_row) ' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -E '^ *2 *$' | wc -l returns 8
  - No decorated tier values in the ledger: grep -E '^\| [a-z]' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -vE '^ *[012] *$' returns empty output
  - Each flipped row evidence string contains "admin-css-conformance.sh" (the guard citation) — grep -c 'admin-css-conformance' guides/reference/admin-quality-ledger.md returns >= 8
  - Each flipped row evidence string contains "assertBoardScreenshot" or "assertNoAxeViolations" — grep -c 'assertBoardScreenshot\|assertNoAxeViolations\|admin-design.spec.ts' guides/reference/admin-quality-ledger.md returns >= 8
  - No --sg-duration evidence strings: grep -c 'sg-duration' guides/reference/admin-quality-ledger.md returns 0
  - Other (non-L1) rows retain their original tiers — token-layer stays 1, L2 rows stay 1, L3 index-live/organization-live/etc. stay 2, L4 flow rows stay 1
  </acceptance_criteria>

  <done>All 8 L1 rows show bare 2 with rich evidence strings; no decorators; non-L1 rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run monotonic guard to confirm no regressions</name>
  <files>guides/reference/admin-quality-ledger.md</files>

  <read_first>
  - guides/reference/admin-quality-ledger.md — re-read after edits to confirm all rows are correct before running the guard
  - scripts/ci/quality-ledger-monotonic.sh — confirm the --base argument usage: --base origin/main compares working tree against the main branch tip
  </read_first>

  <action>
  Run the monotonic guard against origin/main to confirm:
  1. No tier values decreased vs the main branch (8 rows moved from 1 → 2, which is an allowed increase)
  2. All existing Tier-2 rows (index-live, organization-live, users-index-live, user-show-live, audit-index-live, audit-user-live, branding-live) still show 2 (no accidental edit)

  Command: bash scripts/ci/quality-ledger-monotonic.sh --base origin/main

  Expected output: "quality-ledger-monotonic: PASS (N cells checked vs origin/main)" where N covers all ledger rows.

  If the guard exits non-zero, diagnose: either a row edit introduced a bare non-[012] tier value (making the row invisible to the guard — which is a protection loss, not a regression detection), or a row was accidentally decreased. Fix and re-run.

  Also run the self-test to confirm the guard itself is still passing:
  bash scripts/ci/quality-ledger-monotonic.test.sh
  </action>

  <verify>
    <automated>bash scripts/ci/quality-ledger-monotonic.sh --base origin/main && bash scripts/ci/quality-ledger-monotonic.test.sh</automated>
  </verify>

  <acceptance_criteria>
  - bash scripts/ci/quality-ledger-monotonic.sh --base origin/main exits 0 and emits a PASS line
  - bash scripts/ci/quality-ledger-monotonic.test.sh exits 0 (all 4 tests still passing)
  - The PASS line from the monotonic guard reports a cell count >= 30 (all ledger rows visible to the awk parse — confirms no bare-2 rows became invisible due to decorator corruption)
  - git diff --stat shows only guides/reference/admin-quality-ledger.md modified in this plan (no accidental edits to other files)
  </acceptance_criteria>

  <done>Monotonic guard exits 0 vs origin/main; all 8 L1 rows locked at tier 2; no regressions in any other row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger edit → awk parse | Malformed tier column (e.g., "2*") silently removes row from guard protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-206-07 | Tampering | Ledger tier column format | high | mitigate | Acceptance criteria explicitly verify no decorated values exist (grep -vE '^[012]$' returns empty) and guard cell count >= 30; any invisible row is caught |
| T-206-08 | Repudiation | Evidence string accuracy | low | accept | Evidence strings are human-curated; their accuracy is bounded by Plan 02 SUMMARY audit findings, which are derived from code inspection |
</threat_model>

<verification>
After both tasks complete:
  bash scripts/ci/quality-ledger-monotonic.sh --base origin/main   (exits 0)
  bash scripts/ci/quality-ledger-monotonic.test.sh                 (exits 0)
  grep -E '^\| (stat|stat_link|task_card|summary_chip|applied_chip|notice|notice_link|audit_row) ' guides/reference/admin-quality-ledger.md | awk -F'|' '{print $4}' | grep -E '^ *2 *$' | wc -l
  (returns 8)
</verification>

<success_criteria>
- All 8 L1 ledger rows at bare tier 2 with rich evidence strings
- No decorated tier values anywhere in the ledger
- Monotonic guard exits 0 vs origin/main (all increases allowed, no decreases)
- Both guard and guard self-test green
</success_criteria>

<output>
Create .planning/phases/206-l1-component-elevation-wave-a/206-04-SUMMARY.md when done
</output>
