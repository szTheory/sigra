---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 02
type: execute
wave: 2
depends_on: [242-01]
files_modified: []
autonomous: true
requirements: [REL-03, REL-04]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The fixed-target lane reaches the default branch only after its hermetic tests and the exact contributor gate pass."
    - "The merged default-branch workflow is byte-identical to the p22-green candidate and remains dispatch-only."
    - "No Hex workflow is dispatched and no Hex state changes during this plan."
    - "The continuation branch starts from the observed merge on `origin/main`, so later public evidence names the workflow revision that actually ran."
  artifacts:
    - path: .github/workflows/hex-remediate-phantom.yml
      provides: "Merged, default-branch-visible workflow eligible for later dispatch"
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-01-SUMMARY.md
      provides: "Exact candidate commit and hermetic verification record"
  key_links:
    - from: 242-01
      to: origin/main
      via: "scoped pull request whose exact head receives `ci.yml` success before merge"
      pattern: "hex-remediate-phantom.yml"
---

<objective>
Land the already-tested remediation tracer on the default branch without running it, then establish a
clean continuation branch from the observed merge.

Purpose: GitHub only dispatches a newly added workflow reliably after the workflow exists on the default branch.
Output: Green merged infrastructure PR and a continuation branch rooted at that merge.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-01-SUMMARY.md
@.github/workflows/hex-remediate-phantom.yml
@scripts/ci/prohibitions/p22-hex-remediation.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the tested infrastructure commit and prove the pre-push gate</name>
  <files>(no tracked files modified; read-only verification and git commit identity only)</files>
  <read_first>
    Read AGENTS.md's automation-first, `mix ci`, watcher, and rate-limit rules. Read the 242-01
    summary for its exact commit list. Inspect `git status --short` and preserve every unrelated or
    pre-existing modification; never use a broad staging command.
  </read_first>
  <action>
    Resolve the Plan 01 candidate SHA and require its diff to contain only the seven Plan 01 paths.
    Re-run the Bash verifier self-test, p22, the full prohibition glob, the existing Phase 234 action
    pinning test, and `MIX_ENV=test mix ci`. Refuse to proceed if the workflow or verifier changed
    after those commands, if the tree contains staged unrelated paths, or if the candidate is not
    based on current `origin/main`. Record the candidate SHA and each command verdict in the plan
    summary; this task performs no push, dispatch, or external mutation.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; bash scripts/ci/hex-remediation-verify.test.sh; node --test --test-reporter=tap scripts/ci/prohibitions/p22-hex-remediation.test.mjs; MIX_ENV=test mix test test/sigra/planning/phase_234_action_pinning_contract_test.exs; git diff --check'</automated>
    <fails_when>Any focused guard is red, the workflow is absent, the candidate diff includes an unowned path, or the candidate identity changes after verification.</fails_when>
  </verify>
  <acceptance_criteria>
    - The exact candidate SHA and owned-path diff are frozen before the first push.
    - `MIX_ENV=test mix ci` has passed at that SHA as the mandatory pre-push gate.
    - No Hex or GitHub workflow has been dispatched.
  </acceptance_criteria>
  <done>The default-branch candidate is source-complete, hermetically green, contributor-gate green, and safe to present as one scoped PR.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the remediation-infrastructure PR and branch from the observed merge</name>
  <precondition>`gh auth status` succeeds for `szTheory/sigra`, GitHub REST core remaining is above 250, and Task 1's exact candidate SHA is unchanged.</precondition>
  <files>(no tracked files modified; GitHub PR/CI/merge state and continuation branch only)</files>
  <read_first>
    Re-read AGENTS.md's single-watcher and 403/429 hard-stop rules immediately before GitHub calls.
    Read the candidate SHA from Task 1 rather than resolving a moving branch name.
  </read_first>
  <action>
    Push the exact candidate object to a scoped infrastructure branch and open one PR against
    `main`, reusing a same-head open PR on resume. Confirm the PR head OID equals the candidate and
    its diff is still limited to Plan 01 paths. Inspect `gh api rate_limit` once before waiting. Find
    the single `ci.yml` pull-request run for that head; if it is active, use exactly one
    `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`. Fetch the
    structured summary once after completion and failed logs only if the conclusion is failure.
    Retry a transient failure once; repair deterministic failures before merge. Treat 403/429 as a
    stop until the reported time.

    Merge only after the exact-head run and required checks are green. Re-read the merged PR and
    `origin/main`; require the candidate to be an ancestor and the default-branch workflow blob to
    match the tested blob. Then create/switch to a continuation branch rooted exactly at the observed
    merge commit for Plans 03-07. Do not dispatch the remediation workflow in this plan.
  </action>
  <verify>
    <automated>bash -c 'set -eu; observed=$(mktemp); trap '"'"'rm -f "$observed"'"'"' EXIT; git fetch origin main; git show origin/main:.github/workflows/hex-remediate-phantom.yml >"$observed"; cmp -s "$observed" .github/workflows/hex-remediate-phantom.yml; gh workflow view hex-remediate-phantom.yml --repo szTheory/sigra >/dev/null'</automated>
    <fails_when>The PR head differs from the tested SHA, CI is skipped/red/unknown, a second watcher is started, rate limiting is ignored, the merged workflow differs, or any remediation workflow run is dispatched.</fails_when>
  </verify>
  <acceptance_criteria>
    - The scoped PR is merged with exact-head CI evidence.
    - The workflow is visible on the default branch and remains byte-identical to the p22-green source.
    - The continuation branch is based on that merge and carries no unrelated staged changes.
  </acceptance_criteria>
  <done>The tested remediation lane is on `main`, no Hex mutation has occurred, and later work has an exact default-branch provenance anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local candidate → GitHub PR | Tested workflow code becomes externally executable after merge. |
| GitHub CI → merge decision | Remote status determines whether the lane enters the default branch. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-06 | Tampering | PR head/diff | high | mitigate | Freeze exact SHA, assert owned diff, require exact-head CI, compare merged workflow blob. |
| T-242-07 | Spoofing | CI status | high | mitigate | Read the run by repository/workflow/head SHA and inspect structured jobs, not a badge or count. |
| T-242-08 | Denial of service | GitHub polling | medium | mitigate | One watcher, 60-second interval, rate-limit preflight, hard stop on 403/429. |
| T-242-09 | Repudiation | merge provenance | low | accept | Merged PR and summary retain candidate/merge SHAs; GitHub is the authoritative external record. |
</threat_model>

<artifacts_produced>
- Merged infrastructure PR with exact-head CI proof.
- Default-branch-visible remediation workflow.
- Clean continuation branch rooted at the merge.
</artifacts_produced>

<verification>
Confirm the candidate SHA, PR head SHA, successful CI run head SHA, and merged workflow blob all agree. Confirm the workflow run list shows no Phase 242 remediation dispatch from this plan.
</verification>

<success_criteria>
The default branch contains exactly the hermetically tested remediation lane, with no external Hex mutation and no provenance ambiguity.
</success_criteria>

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-02-SUMMARY.md` when done.
</output>
