---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 06
status: superseded
superseded_by: 242-14
type: execute
wave: 4
depends_on: [242-03, 242-04, 242-05]
files_modified:
  - .planning/decisions/005-hex-retirement-and-safe-install-constraints.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - test/sigra/planning/phase_242_hex_release_contract_test.exs
autonomous: true
requirements: [REL-03, REL-04, REL-05, REL-06]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ADR 005 records the safe tuple, advisory retirement, immutable-package/reversible-docs split, and the actual measured latest/root results without changing ADR 003 or ADR 004 (D-07)."
    - "Active requirements, roadmap success criteria, and state no longer present the superseded two-segment shorthand or claim retirement repairs resolver eligibility/latest behavior."
    - "Historical changelog entries, archived phase evidence, and prior summaries remain historical; current active records are visibly superseded rather than silently rewritten."
    - "A semantic contract scans tracked README/guides for Sigra dependency tuples and rejects any supported line other than `{:sigra, \"~> 1.5.0\"}`."
    - "The contract always binds `doc/llms.txt`'s version header to `mix.exs`, and on a Release Please branch additionally requires comment-only Unreleased content, nonempty 1.5.1 notes, adjacency, and version/index agreement."
  artifacts:
    - path: .planning/decisions/005-hex-retirement-and-safe-install-constraints.md
      provides: "Durable retirement/docs/resolver/install decision with measured consequences"
    - path: test/sigra/planning/phase_242_hex_release_contract_test.exs
      provides: "Semantic safe-pin, active-record, docs-index, and release-candidate ordering contract"
    - path: .planning/REQUIREMENTS.md
      provides: "Visible D-05 supersession of REL-05's unsafe shorthand"
  key_links:
    - from: 242-HEX-REMEDIATION-EVIDENCE.json
      to: .planning/decisions/005-hex-retirement-and-safe-install-constraints.md
      via: "ADR quotes values from named JSON fields, not expected behavior"
      pattern: "latest_stable_version|after_docs_revert|resolver_broad|resolver_safe"
    - from: test/sigra/planning/phase_242_hex_release_contract_test.exs
      to: README.md and guides/**/*.md
      via: "tracked-file semantic tuple scan"
      pattern: "~> 1.5.0"
    - from: test/sigra/planning/phase_242_hex_release_contract_test.exs
      to: CHANGELOG.md and doc/llms.txt
      via: "release-candidate branch/env contract"
      pattern: "GITHUB_HEAD_REF|SIGRA_RELEASE_CANDIDATE"
---

<objective>
Turn the measured remediation into ADR 005, correct every current project record, and install a
semantic contract that prevents the unsafe pin, stale docs index, or orphaned release notes from recurring.

Purpose: The project's durable record must state exactly what the public mutations did and did not fix.
Output: ADR 005, reconciled active planning records, and Phase 242 release contract test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md
@.planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
@test/sigra/planning/phase_146_release_validation_test.exs
@CHANGELOG.md
@doc/llms.txt
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record ADR 005 from measured package, docs, and resolver facts</name>
  <files>.planning/decisions/005-hex-retirement-and-safe-install-constraints.md</files>
  <read_first>
    Read ADR 003 and ADR 004 completely, then read every required slot in the committed Phase 242
    remediation evidence. Read D-05 through D-07. Use actual `latest_stable_version`, root classifier,
    and `has_docs` observations even if they differ from a roadmap prediction.
  </read_first>
  <action>
    Create ADR 005 with Accepted status/date/context and sections for problem, decision, measured
    result, boundaries, consequences, and revisit triggers. Preserve ADR 003's ownership of
    tag-derived publishing and ADR 004's number/subject. Record the exact supported tuple, explain
    pessimistic arity, state that retirement remains advisory and leaves the release resolvable, and
    distinguish immutable package content from independently reversible hosted docs. Cite the
    committed evidence path and state the observed pre/after-retire/after-docs `latest_stable_version`,
    release `has_docs`, root-doc classification, and both resolver outcomes. Do not generalize those
    observations into a Hex contract.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; adr=.planning/decisions/005-hex-retirement-and-safe-install-constraints.md; test -s "$adr"; rg -n -F '"'"'{:sigra, "~> 1.5.0"}'"'"' "$adr"; rg -n '"'"'advisory|resolv|immutable|docs|latest_stable_version|242-HEX-REMEDIATION-EVIDENCE'"'"' "$adr"; test -s .planning/decisions/003-hex-release-versioning-no-tag-derived-publish.md; test -s .planning/decisions/004-test-01-02-superseded-by-single-owner-mix-ci.md; git diff --check'</automated>
    <fails_when>ADR 005 omits any locked boundary, substitutes expected for measured values, claims retirement fixes latest/resolution, edits ADR 003/004, or presents a two-segment supported tuple.</fails_when>
  </verify>
  <acceptance_criteria>
    - ADR number, status, date, context, and ownership relationships are explicit.
    - Every public outcome is traceable to a named evidence slot.
    - The safe install decision and its reason are unambiguous.
  </acceptance_criteria>
  <done>ADR 005 is the durable, measured decision record for retirement, docs reversibility, and safe installation.</done>
</task>

<task type="auto">
  <name>Task 2: Visibly supersede unsafe and obsolete active project records</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    Read REL-03 through REL-06, Phase 242 success criteria, and STATE's current pending todo/blocker
    entries. Classify records as current/actionable versus archival/historical before editing; the
    latter remain untouched.
  </read_first>
  <action>
    Add a dated D-05 supersession immediately beside REL-05 that preserves the original requirement
    wording as historical input while stating that the supported executable line is the exact
    three-segment tuple and why. Amend Phase 242's active success criteria to use that exact tuple and
    to report latest/root behavior from the committed observations rather than encode a prediction.
    Update STATE's live pending/blocker/current-focus prose so it points to the completed retirement
    evidence and advisory semantics instead of the retired todo's interactive/fixes-resolution story.
    Leave archived milestone artifacts, old summaries, and historical changelog entries unchanged.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; for f in .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md; do rg -n -F '"'"'{:sigra, "~> 1.5.0"}'"'"' "$f"; done; rg -n '"'"'supersed|advisory|does not|measured|observed'"'"' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md; git diff --check'</automated>
    <fails_when>Any active record still instructs the superseded shorthand, promises retirement changes resolver/latest behavior, loses the original requirement history, or rewrites an archival artifact.</fails_when>
  </verify>
  <acceptance_criteria>
    - REL-05's correction is adjacent, dated, and explicit.
    - ROADMAP and STATE agree with the measured evidence and completed todo.
    - The edit boundary is limited to current/actionable sections.
  </acceptance_criteria>
  <done>The active project record and ADR now tell one coherent, measured story without erasing historical context.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce safe docs, active truth, index freshness, and release-candidate ordering</name>
  <files>test/sigra/planning/phase_242_hex_release_contract_test.exs</files>
  <read_first>
    Read Phase 146's `root/0` and `read!/1` patterns, current `mix.exs` version/docs configuration,
    `doc/llms.txt` header, CHANGELOG's Unreleased warning/first release heading, and the Phase 242
    source docs changed by Plans 04-05. Read the three folded todos so each acceptance condition has
    an executable owner.
  </read_first>
  <behavior>
    - Enumerate tracked README/guides source files and fail non-vacuously if no Sigra dependency tuple is found.
    - Every found Sigra tuple equals the exact supported three-segment tuple; unrelated dependency tuples are ignored.
    - Active requirement/roadmap/state and ADR 005 contain the correction/advisory guarantees.
    - `doc/llms.txt` first-line version equals `mix.exs` `@version` on every branch.
    - When `GITHUB_HEAD_REF` denotes Release Please or `SIGRA_RELEASE_CANDIDATE=1`, manifest, mix version, first release heading, and docs index all equal 1.5.1; Unreleased has no substantive non-comment content; the 1.5.1 section is adjacent after comments/whitespace and contains substantive notes.
    - Release Please still gates publish on exact-SHA CI and invokes the established post-publish verifier.
  </behavior>
  <action>
    Add an async ExUnit planning contract using tracked-file discovery rather than a hand-maintained
    count. Parse Sigra dependency tuples semantically so comments or unrelated `~>` requirements do
    not create false positives. Establish a nonzero floor and clear path/line diagnostics. Assert
    active records/ADR semantics by stable headings and required exact tuple, not brittle prose
    duplication.

    Implement the docs-index comparison generically from `mix.exs` `@version`. Implement the release
    candidate branch/env mode so CI on Release Please PR #224 enforces the 1.5.1-specific adjacency,
    substantive-empty Unreleased, nonempty release-section, manifest/version/index agreement, and
    existing release workflow provenance chain. Test parser edge behavior with in-memory examples
    for comment-only emptiness and heading adjacency. Do not edit `mix ci`; normal test discovery is
    the durable wiring.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/sigra/planning/phase_242_hex_release_contract_test.exs</automated>
    <fails_when>The test passes vacuously, misses any tracked Sigra tuple, treats comments as substantive notes, allows a stale docs-index header, fails to detect release-candidate mode, or does not enforce exact-SHA/post-publish workflow links.</fails_when>
  </verify>
  <acceptance_criteria>
    - The real tree passes in normal mode with a nonzero discovered tuple inventory.
    - In-memory bad cases prove tuple, index, empty-body, adjacency, and ordering failures are independently reachable.
    - No alias or CI topology change is needed.
  </acceptance_criteria>
  <done>The safe install and release-candidate invariants are executable contracts rather than reviewer memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public evidence → ADR/current planning record | External observations become durable project claims. |
| Tracked docs/release metadata → contract test | Parser correctness determines whether unsafe guidance or stale release artifacts pass. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-22 | Repudiation | ADR measured claims | high | mitigate | Cite exact evidence slots and retain all observed values instead of expectations. |
| T-242-23 | Tampering | active requirements/roadmap | medium | mitigate | Adjacent dated supersession plus semantic contract assertions. |
| T-242-24 | Tampering | tuple/index/changelog parser | high | mitigate | Non-vacuity floors and independently reachable in-memory edge cases. |
| T-242-25 | Information disclosure | ADR/planning artifacts | low | accept | Only public projected observations are cited; evidence schema already rejects sensitive fields. |
</threat_model>

<artifacts_produced>
- ADR 005.
- Reconciled REQUIREMENTS/ROADMAP/STATE active truth.
- Semantic Phase 242 docs/release contract test.
</artifacts_produced>

<verification>
Run the focused ExUnit contract, p22, p18 bookkeeping ratchet, and `mix docs --warnings-as-errors`; inspect that ADR values match the committed evidence and only active planning sections changed.
</verification>

<success_criteria>
The project's live record, adopter docs, and executable contracts all agree on the safe tuple and advisory retirement semantics, while release-candidate CI now owns the unresolved REL-06 adjacency/empty/ordering cases.
</success_criteria>

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-06-SUMMARY.md` when done.
</output>
