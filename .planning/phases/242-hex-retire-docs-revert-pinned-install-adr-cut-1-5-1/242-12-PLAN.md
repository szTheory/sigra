---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 12
type: execute
wave: 6
depends_on: [242-11]
files_modified:
  - .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-12-SUMMARY.md
  - .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
  - .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md
  - .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md
autonomous: false
requirements: [REL-03, REL-04, REL-05]

must_haves:
  truths:
    - "The default branch at dispatch time passes p22 and includes the dependency-safe workflow repair."
    - "Exactly one newly authorized workflow dispatch occurs, with one 60-second watcher and a REST-budget preflight."
    - "A success is promoted only after named-artifact allowlisting, validator/reprojection, and public-state agreement; a failure is classified without repair or redispatch."
  artifacts:
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-12-SUMMARY.md
      provides: "Distinct final dispatch outcome and immutable invocation count"
---

<objective>
Perform one new, explicitly approved remediation dispatch only after revalidating current `origin/main`.
This plan supersedes neither Plan 03 nor Plan 10: their halted records remain immutable. It exists
because the dependency-safe repair merged after Plan 10's consumed invocation.
</objective>

<tasks>
<task type="auto">
  <name>Revalidate current default branch and establish duplicate-prevention receipt</name>
  <action>Fetch `origin/main`, record its full SHA, export the workflow/p22/helpers/fixtures from that exact tree, and run p22 using only the export. Require the exact fixed `--message` retire command and `mix deps.get --check-locked` before both mutation steps. Create a new control JSON with baseline matching workflow-dispatch IDs, zero invocation/watcher/structured-summary counts, and no environment dump. If any matching unclassified run exists or any source check fails, write a blocking summary and stop without dispatch.</action>
  <verify><automated>Source SHA, p22-green export, zero-count control receipt, and no prior matching corrected-SHA run are recorded.</automated></verify>
</task>
<task type="checkpoint:blocking-human">
  <name>Authorize one irreversible Hex remediation workflow dispatch</name>
  <what-built>The fixed default-branch workflow and deterministic preflight are ready; no public mutation has occurred under this plan.</what-built>
  <how-to-verify>Review the recorded `origin/main` SHA, p22 result, and zero-count dispatch control receipt.</how-to-verify>
  <resume-signal>Explicit confirmation to dispatch the one workflow run against the recorded SHA.</resume-signal>
</task>
<task type="auto">
  <name>Invoke once and classify result</name>
  <action>Invoke `hex-remediate-phantom.yml` once against `main` with no inputs. Resolve one matching run, persist its ID atomically, inspect REST core budget once, and start exactly one `gh run watch RUN_ID --repo szTheory/sigra --compact --interval 60 --exit-status`. Fetch one structured summary after completion. On failure, fetch failed logs once, retain only a sanitized job/step/error classification, make at most one public observation to classify mutation state, write the summary, and halt—never repair or redispatch.</action>
  <verify><automated>The control receipt shows exactly one invocation, one watcher, one structured summary, and no success-log access.</automated></verify>
</task>
<task type="auto">
  <name>Promote evidence only after successful receipt validation</name>
  <action>On success only, download the single allowlisted public artifact, reject unsafe shape/content, validate and independently reproject it against run metadata, compare public Hex and HexDocs observations, then atomically install evidence and move the historical todo. On any mismatch, halt without promotion.</action>
  <verify><automated>Validator output, reprojection, receipt/run identity, public observations, immutable Plan 03 hash, and the todo move all agree.</automated></verify>
</task>
</tasks>

<boundary>
No source repair, rerun endpoint, second dispatch, secret inspection, success-log access, or downstream-plan
execution is allowed. The checkpoint is mandatory because a workflow run can mutate the Hex registry.
</boundary>
