---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 09
type: execute
wave: 7
depends_on: [242-08]
files_modified:
  - .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-RELEASE-EVIDENCE.json
autonomous: false
requirements: [REL-03, REL-04, REL-05, REL-06]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A blocking decision inside this plan revalidates Plan 08's authorization against the live PR head immediately before any merge; a mismatch or hold response stops publication."
    - "Release Please creates v1.5.1 and Hex 1.5.1 only after its exact release SHA receives the existing green gate (D-08)."
    - "The established tagged-source-link/post-publish verifier succeeds and its artifact agrees with the Hex API, GitHub release/tag, and HexDocs observations."
    - "The HexDocs root still serves current 1.5.x documentation and the committed package receipt preserves the advisory 1.20.0 retirement observations."
    - "A sanitized machine-readable release receipt is committed, then its evidence-only PR and final main SHA receive automation-first CI proof with no later tracked write."
  artifacts:
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-RELEASE-EVIDENCE.json
      provides: "Committed exact-SHA gate, GitHub release/tag, Hex release, HexDocs source/root, and post-publish receipt"
    - path: "GitHub Release v1.5.1"
      provides: "Release Please-owned public release/tag"
    - path: "Hex package sigra 1.5.1"
      provides: "Installable real release"
  key_links:
    - from: "Plan 08 authorization"
      to: "PR #224 merge"
      via: "exact head SHA equality immediately before merge"
      pattern: "authorize-1.5.1"
    - from: .github/workflows/release-please.yml
      to: "Hex sigra 1.5.1"
      via: "release-please → gate-ci-green → publish-hex dependency chain"
      pattern: "needs: [release-please, gate-ci-green]"
    - from: scripts/ci/release-post-publish-verify.sh
      to: 242-RELEASE-EVIDENCE.json
      via: "downloaded artifact plus independent public read"
      pattern: "v1.5.1"
---

<objective>
Cross the authorized release door exactly once, prove Release Please's exact-SHA gate and public
publication outcomes, then commit a sanitized receipt and close on a final green main SHA.

Purpose: Finish the adopter journey with a real installable release and durable evidence of provenance and visibility.
Output: Published 1.5.1, tagged/current HexDocs verification, committed release evidence, and final-head CI receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-08-SUMMARY.md
@.github/workflows/release-please.yml
@scripts/ci/wait-for-ci-gate.sh
@scripts/ci/release-post-publish-verify.sh
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Revalidate the recorded exact-SHA authorization at the release boundary</name>
  <read_first>
    Read `242-08-SUMMARY.md`, the `sigra.phase-242-release-readiness/1` PR comment, and live PR #224.
    The recorded Plan 08 choice must be `authorize-1.5.1`, and all three sources must name the same
    40-hex head SHA. This gate carries forward D-08's locked Release Please path; it does not reopen
    version, workflow, package, or scope selection.
  </read_first>
  <decision>Confirm that Plan 08's explicit authorization still applies to the exact live PR #224 head immediately before the permanent merge/publication operation.</decision>
  <context>Plan 08 contains the substantive human authorization and automation packet. This same-plan blocking gate prevents an executor from treating a prior-plan summary as sufficient when the live head has moved or authorization was held.</context>
  <options>
    <option id="authorization-valid">
      <name>Authorization remains valid</name>
      <pros>Allows only the exact already-authorized, still-green SHA to enter Release Please.</pros>
      <cons>The following task may create permanent tag and package records.</cons>
    </option>
    <option id="hold">
      <name>Hold release</name>
      <pros>Performs no merge, tag, or publication.</pros>
      <cons>REL-06 and Phase 242 remain incomplete pending a fresh readiness proof and authorization.</cons>
    </option>
  </options>
  <action>
    Present the Plan 08 decision, exact authorized SHA, current PR head, readiness comment URL, and
    exact-head CI URL. Accept `authorization-valid` only when Plan 08 recorded `authorize-1.5.1`,
    the live head and receipt still equal that SHA, and no release state contradicts the packet;
    otherwise accept only `hold`. Do not auto-select, infer consent, request a secret, or propose an
    alternate release path. Record this same-plan gate outcome and SHA before Task 2 begins.
  </action>
  <verify>
    <automated>bash -c 'set -eu; test -s .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-08-SUMMARY.md; rg -q '"'"'authorize-1\.5\.1'"'"' .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-08-SUMMARY.md; gh pr view 224 --repo szTheory/sigra --json state,headRefOid --jq '"'"'select(.state == "OPEN") | .headRefOid'"'"' | grep -Eq '"'"'^[0-9a-f]{40}$'"'"''</automated>
    <fails_when>Plan 08 did not authorize, the prior summary/receipt/live head disagree, the PR is not open and green at that head, or the human does not explicitly confirm the allowed exact-SHA transition.</fails_when>
  </verify>
  <acceptance_criteria>
    - The checkpoint is inside the same plan as the one-way release task and immediately precedes it.
    - The carried authorization names one live, already-proven SHA and no alternate release choice.
    - A hold or any state mismatch performs no external write.
  </acceptance_criteria>
  <resume-signal>Reply `authorization-valid` to proceed with the unchanged Plan 08-authorized SHA, or `hold` to stop before merge.</resume-signal>
  <done>The recognized same-plan blocking gate has explicitly accepted or held the exact authorized one-way operation.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the authorized head and observe the gated Release Please publication</name>
  <precondition>Task 1 recorded `authorization-valid`; `242-08-SUMMARY.md` records `authorize-1.5.1`; and both decisions, the live PR head, and the readiness receipt name the same exact 40-hex SHA.</precondition>
  <reversibility rating="one-way">Hex 1.5.1 and tag v1.5.1 become permanent public records; Plan 08's blocking-human authorization is the preceding gate.</reversibility>
  <files>(no tracked files modified; PR merge, Release Please run, GitHub release/tag, and Hex publication only)</files>
  <read_first>
    Read the Plan 08 summary/receipt, live PR #224, live Hex 1.5.1 endpoint, live v1.5.1 release/tag,
    and Release Please job graph. Read AGENTS.md immediately before GitHub calls. Never infer absence
    or success from a count-only result.
  </read_first>
  <action>
    Immediately before merge, require PR #224 open head to equal the authorized SHA, not behind main,
    and still green. Read Hex and GitHub release/tag state. If 1.5.1 already exists, do not issue a
    merge/publish retry: require its tag/source SHA to equal the authorized candidate and switch to
    proof-only recovery; any mismatch is a hard block. Otherwise merge PR #224 using the repository's
    established allowed merge method and re-read the PR to capture the merge/release SHA.

    Identify exactly one Release Please push run for that merge SHA. Inspect rate limit before a long
    wait; use one 60-second compact watcher and reuse its result. Fetch one structured summary after
    completion. Require `release-please`, `gate-ci-green`, and `publish-hex` success, and require the
    gate job to name the same release SHA/tag created by Release Please. If a failure occurs, read
    public GitHub/Hex state before any retry; never repeat a write whose state may already have landed.
    Retry one transient read/runner failure only. For protected-key OTP/authorization failure, emit a
    dynamic authentication checkpoint with diagnostics and no key request/template. Deterministic
    failure is repaired through a new release-candidate proof cycle, not waived.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; gh release view v1.5.1 --repo szTheory/sigra --json tagName,targetCommitish,url >/tmp/sigra-242-gh-release.json; curl -fsS https://hex.pm/api/packages/sigra/releases/1.5.1 | jq -e '"'"'.version == "1.5.1"'"'"' >/dev/null; jq -e '"'"'.tagName == "v1.5.1"'"'"' /tmp/sigra-242-gh-release.json >/dev/null'</automated>
    <fails_when>The live head differs from authorization, an existing release has mismatched provenance, the merge/publish write is retried blindly, any required Release Please job is non-success, exact release-SHA CI is unproven, or 1.5.1 is absent from either public release system.</fails_when>
  </verify>
  <acceptance_criteria>
    - The merged head is byte-identical to the authorized head.
    - Release Please, exact-SHA gate, and publish job all succeed in the same run chain.
    - GitHub Release/tag and Hex release 1.5.1 are publicly visible.
  </acceptance_criteria>
  <done>The authorized 1.5.1 candidate is the permanent published artifact, and its exact-SHA gated workflow run is identified.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the public 1.5.1 receipt and prove the final main SHA</name>
  <precondition>The release is public with matching provenance, all public endpoints are stable, and the working tree has no staged path outside the evidence JSON plus executor-owned summary artifacts.</precondition>
  <files>.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-RELEASE-EVIDENCE.json</files>
  <read_first>
    Read the Release Please structured summary, its uploaded post-publish evidence artifact, the
    Phase 242 remediation evidence, ADR 005, and the final public package/root/versioned HexDocs
    pages. Read AGENTS.md's watcher/rate-limit rules and `git status --short`. Expose only projected
    public facts, preserve unrelated user changes, and use an explicit staging allowlist.
  </read_first>
  <action>
    Download the named `release-post-publish-evidence-1.5.1` artifact from the successful Release
    Please run into a temporary directory and validate its passed status, package/version/tag, and
    v1.5.1 source-link needle. Independently read the GitHub release/tag target, Hex release endpoint,
    package endpoint, versioned HexDocs pages, and HexDocs root. Require the tag to resolve to the
    release SHA, the versioned source link to contain v1.5.1, the root to classify as current 1.5.x,
    and the package record to retain the invalid 1.20.0 retirement.

    Write `242-RELEASE-EVIDENCE.json` atomically with schema
    `sigra.phase-242-release/1`: authorized/candidate/merge/release SHAs; PR/release/workflow/run URLs;
    exact-SHA gate and job conclusions; Hex release projection; package latest/retirements projection;
    root and versioned HexDocs observations; post-publish verifier projection; changelog/index
    versions; capture time; and linkage to remediation evidence. Scan for credentials, headers,
    environment dumps, raw request material, and token-like strings before staging. Record observed
    latest fields without claiming the release/retire changed undocumented behavior.

    Run the Phase 242 contract in normal mode, p22, p18 ratchet, docs warning gate, release
    validation tests, and `MIX_ENV=test mix ci`. Commit only the validated release evidence (and an
    executor-owned summary if required by the execution protocol) on a scoped branch rooted at
    current `origin/main`; never stage unrelated planning-state edits. Push with an explicit refspec,
    open or reuse one same-head evidence-only PR, assert its diff, and watch one exact-head `ci.yml`
    run under AGENTS.md rules.

    Merge only after exact-head success. Identify the main `ci.yml` run whose head SHA equals the
    evidence PR merge SHA, use at most one watcher if active, and require success. Post a sanitized
    `sigra.phase-242-final-head/1` JSON comment on the evidence PR with final main SHA/run URL,
    release evidence path/hash, and live Hex/HexDocs re-observation. Make no tracked write after
    freezing that final SHA. Retry one transient external read once; a deterministic or rate-limited
    failure leaves a diagnostic comment and blocks completion rather than manufacturing a pass.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; f=.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-RELEASE-EVIDENCE.json; jq -e '"'"'.schema == "sigra.phase-242-release/1" and .release.version == "1.5.1" and .release.tag == "v1.5.1" and .gate.conclusion == "success" and .post_publish.status == "passed" and .hexdocs_root.classification == "current_1_5"'"'"' "$f" >/dev/null; bash scripts/ci/hex-remediation-verify.sh --validate-evidence .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json; MIX_ENV=test mix test test/sigra/planning/phase_242_hex_release_contract_test.exs test/sigra/planning/phase_146_release_validation_test.exs; node --test --test-reporter=tap scripts/ci/prohibitions/p22-hex-remediation.test.mjs; git diff --check'</automated>
    <fails_when>The artifact/public reads disagree, provenance or root/retirement checks fail, the receipt is empty or unsafe, a local gate is red, the evidence PR carries another path, exact-head PR/main CI is unproven, the final receipt points at another SHA, or a tracked write follows final-SHA capture.</fails_when>
  </verify>
  <acceptance_criteria>
    - One schema-v1 JSON file proves the full authorized candidate → green gate → GitHub/Hex/HexDocs chain.
    - The post-publish artifact and independent public observations agree.
    - The receipt remains secret-free and preserves advisory retirement semantics.
    - The committed evidence lands through a scoped green PR whose merge SHA has a successful exact-SHA main run.
    - Repository and public systems are re-read after merge with no later tracked write.
  </acceptance_criteria>
  <done>Phase 242 closes on a final green main SHA with committed public release evidence and no unresolved publication, docs, resolver, or todo claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human authorization → PR merge | Exact approved source crosses into an immutable release process. |
| Release Please → GitHub/Hex/HexDocs | Automation creates permanent public tag/package/docs state. |
| Public systems/artifact → committed evidence | Multiple external facts become the phase's durable success record. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-35 | Tampering | head changed after authorization | critical | mitigate | Exact SHA equality immediately before merge; any change returns to Plan 08. |
| T-242-36 | Repudiation | exact-SHA green claim | high | mitigate | Release Please outputs, gate job conclusions, run/SHA/tag links, committed schema-v1 receipt. |
| T-242-37 | Information disclosure | publish logs/evidence | high | mitigate | Structured summaries/artifacts only, public projections, secret-content scan, no success-log capture. |
| T-242-38 | Tampering | blind republish/retry | high | mitigate | Read GitHub/Hex state before every write/retry and accept only matching already-achieved state. |
| T-242-39 | Denial of service | propagation/CI polling | medium | mitigate | Existing bounded verifier, one watcher, 60-second interval, rate-limit hard stops. |
| T-242-40 | Spoofing | final-head proof | high | mitigate | Evidence-only PR diff, exact PR/main run SHA checks, no post-freeze tracked write. |
</threat_model>

<artifacts_produced>
- GitHub Release/tag v1.5.1 and Hex package 1.5.1.
- Successful exact-SHA Release Please gate/publish/post-publish chain.
- Committed `sigra.phase-242-release/1` evidence.
- Final-main exact-SHA CI receipt.
</artifacts_produced>

<verification>
Run focused Phase 242 and release-lane tests, p22 and p18, docs warnings, full `mix ci`, verify exact release/tag/source SHA, re-read Hex/HexDocs, and prove the final evidence-only merge SHA green without a later tracked write.
</verification>

<success_criteria>
An adopter can install 1.5.1 with the documented safe tuple, sees current root docs and the invalid-version warning when relevant, while the repository retains exact, secret-free proof of the remediation and gated release.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Current Hex docs, visible phantom warning, real installable release, honest project record | 01-09 | COVERED | Public journey and durable record are both verified. |
| REQ | REL-03 | Retire 1.20.0 as invalid through fixed workflow and prove API retirements | 01-03 | COVERED | Structural RED fixtures plus live receipt. |
| REQ | REL-04 | Docs-only revert and current root HexDocs | 01, 03, 09 | COVERED | Three-state root classifier; package revert structurally forbidden. |
| REQ | REL-05 | Safe install ADR and no false resolver/latest claim | 03-06 | COVERED | Exact three-segment tuple, fresh resolver evidence, ADR 005, active-record supersession. |
| REQ | REL-06 | Fold notes and publish 1.5.1 from green gate | 04, 06-09 | COVERED | Candidate contract, one-way checkpoint, Release Please exact-SHA/post-publish proof. |
| RESEARCH | R-01 | Fixed dispatch, least privilege, step-scoped secret, no arbitrary inputs | 01 | COVERED | p22 owns authority and secret-scope drift. |
| RESEARCH | R-02 | BEFORE / AFTER_RETIRE / AFTER_DOCS_REVERT causal projections | 01, 03 | COVERED | One committed schema with nonempty ordered slots. |
| RESEARCH | R-03 | Fresh independent resolver homes with warnings enabled | 01, 03 | COVERED | Broad and safe lock/warning facts captured separately. |
| RESEARCH | R-04 | No blind mutation retry; observed-state idempotency | 01, 03, 09 | COVERED | Read-before/write/read-after on remediation and release. |
| RESEARCH | R-05 | Root routing and package metadata are observed, not inferred | 01, 03, 06, 09 | COVERED | ADR cites measured slots; root has explicit classifier. |
| RESEARCH | R-06 | Existing Release Please publisher/gate/verifier remain authoritative | 06-09 | COVERED | No second publisher and no release topology rewrite. |
| RESEARCH | R-07 | Secret/OTP capability can only be proven in protected execution | 03, 09 | COVERED | Fail closed; dynamic auth checkpoint only if the run actually requires it. |
| CONTEXT | D-01 | Dedicated fixed-target remediation workflow | 01-03 | COVERED | Separate from manual publisher and landed before dispatch. |
| CONTEXT | D-02 | Three committed public boundaries plus isolated resolver proof | 01, 03 | COVERED | Schema-v1 receipt. |
| CONTEXT | D-03 | `invalid` reason, truthful message, safe credential handling | 01, 03 | COVERED | Fixed ASCII message and two-step secret scope. |
| CONTEXT | D-04 | Docs-only revert; live root; measure effects | 01, 03, 09 | COVERED | Wrong revert class is a fixture-proven failure. |
| CONTEXT | D-05 | Exact `{:sigra, \"~> 1.5.0\"}` supported line | 04-06 | COVERED | Core and companion docs plus semantic corpus contract. |
| CONTEXT | D-06 | Broad-vs-safe fresh resolver and observed latest field | 01, 03, 06 | COVERED | ADR/current records derive from receipt. |
| CONTEXT | D-07 | ADR 005 and actionable-record reconciliation | 03, 06 | COVERED | ADR 003/004 preserved; archival history excluded. |
| CONTEXT | D-08 | Release Please 1.5.1 after remediation/docs and exact-SHA green | 07-09 | COVERED | Ancestor proof, checkpoint, workflow receipt. |
| CONTEXT | D-09 | Fold Unreleased, refresh index, preserve p18 ratchet | 04, 06-09 | COVERED | Candidate mode + todo closures + p18 verification. |
| CONTEXT | TODO-RETIRE | Correct and close 2026-07-03 retirement todo | 03 | COVERED | Completed against live evidence. |
| CONTEXT | TODO-CHANGELOG | Prevent Release Please orphaned notes and close todo | 06-07 | COVERED | Executable candidate-mode control and observed fold. |
| CONTEXT | TODO-INDEX | Refresh and durably guard docs index; close todo | 06-07 | COVERED | Generic version comparison plus 1.5.1 regeneration. |

Excluded by locked scope: package-tarball revert, arbitrary package/version inputs, a second
publisher, UI/auth feature work, dependency upgrades, release-lane refactors, and reclaiming
`latest_stable_version` by publishing above the phantom.

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-09-SUMMARY.md` when done.
</output>
