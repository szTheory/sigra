---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 03
type: execute
wave: 3
depends_on: [242-02]
files_modified:
  - .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
  - .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md
  - .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md
autonomous: true
requirements: [REL-03, REL-04, REL-05]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One default-branch run retires `sigra 1.20.0` as `invalid`, reverts only its docs, and exposes no secret or interactive runbook path (D-01, D-03, D-04)."
    - "The committed receipt contains sanitized BEFORE, AFTER_RETIRE, and AFTER_DOCS_REVERT projections so retirement and docs effects are not conflated (D-02)."
    - "The live HexDocs root is observed as current 1.5.x; ambiguous, unavailable, or phantom-root outcomes block REL-04."
    - "Fresh independent resolver homes prove the broad case still selects 1.20.0 with a retirement warning and the safe `{:sigra, \"~> 1.5.0\"}` case selects 1.5.x without suppressing retirements (D-05, D-06)."
    - "`latest_stable_version` is recorded exactly as observed at every boundary, never promised as an effect of retirement or docs reversion."
  artifacts:
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
      provides: "Committed public causal, root-doc, resolver, workflow-run, and source-SHA receipt"
    - path: .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md
      provides: "Corrected closure record replacing the obsolete claim that retirement repairs resolution/latest"
  key_links:
    - from: .github/workflows/hex-remediate-phantom.yml
      to: 242-HEX-REMEDIATION-EVIDENCE.json
      via: "downloaded public-evidence artifact validated again before commit"
      pattern: "sigra.phase-242-hex-remediation/1"
    - from: 242-HEX-REMEDIATION-EVIDENCE.json
      to: .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md
      via: "closure cites measured resolver/latest/docs results rather than the todo's prediction"
      pattern: "workflow_run_url|after_retire|resolver_broad"
---

<objective>
Execute the fixed remediation once, validate its public artifact independently, and commit the actual
registry/docs/resolver observations while correcting the stale retirement todo.

Purpose: Change the public adopter experience and preserve causally separated evidence of what changed and what did not.
Output: Successful workflow run, secret-free schema-v1 evidence JSON, and corrected completed todo.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-02-SUMMARY.md
@.planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md
@.github/workflows/hex-remediate-phantom.yml
@scripts/ci/hex-remediation-verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch once, watch once, and retrieve the public remediation receipt</name>
  <precondition>`origin/main` contains the p22-green remediation workflow from Plan 02; `gh auth status` succeeds; GitHub REST core remaining is above 250.</precondition>
  <files>(no tracked files modified; workflow run and downloaded artifact remain external/temporary)</files>
  <read_first>
    Read AGENTS.md immediately before GitHub calls. Read Plan 02's merged workflow SHA and the
    workflow's concurrency/idempotency rules. Read the verifier's schema validator and never inspect,
    print, request, or template the value of `HEX_API_KEY`.
  </read_first>
  <action>
    Record a dispatch-start timestamp and query for an existing in-progress Phase 242 remediation run
    at the Plan 02 default-branch SHA. Reuse it if exactly one exists; otherwise dispatch the named
    workflow once on `main` with no `-f` inputs. Resolve exactly one new run whose workflow, event,
    head branch, head SHA, and creation time match. Before waiting, inspect `gh api rate_limit`; at or
    below 250 stop until reset. Use only one active watcher and the exact 60-second compact command
    required by AGENTS.md. Fetch one structured summary after completion, and failed logs only on
    failure.

    Do not redispatch after a failure. First read the public Hex package/root state and classify
    whether the intended state was already achieved. Retry a transient read/watch failure once; a
    deterministic workflow failure is diagnosed and repaired in source through a new green PR. If
    Hex reports an OTP/device-flow/authorization requirement that the protected key cannot satisfy,
    emit the executor's dynamic authentication checkpoint with the run URL and diagnostic; do not ask
    for, paste, rotate, or store a key and do not fall back to an interactive runbook.

    On success, download only the named public-evidence artifact to a new temporary directory. Run
    the committed schema validator locally and compare its run ID/SHA against the observed run. Keep
    the downloaded file out of the repository until Task 2 reprojects and validates it.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; test -s /tmp/sigra-242-remediation-run-id; run_id=$(cat /tmp/sigra-242-remediation-run-id); gh run view "$run_id" --repo szTheory/sigra --json event,headSha,status,conclusion,url --jq '"'"'select(.event == "workflow_dispatch" and .status == "completed" and .conclusion == "success") | .headSha'"'"' | grep -Eq '"'"'^[0-9a-f]{40}$'"'"''</automated>
    <fails_when>No unique matching run exists; the run is not successful at the merged workflow SHA; more than one watcher/dispatch is used; rate-limit rules are violated; the protected credential appears in output; or the artifact fails schema/run-identity validation.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one matching workflow run is selected and its URL, ID, and head SHA are durable inputs to Task 2.
    - The run succeeds or the plan stops with a classified diagnostic; no mutation is blindly retried.
    - The downloaded artifact validates without network assumptions and contains no sensitive material.
  </acceptance_criteria>
  <done>The fixed workflow has completed once and a validated temporary public receipt is ready for a controlled commit.</done>
</task>

<task type="auto">
  <name>Task 2: Re-observe public state, commit the causal receipt, and correct the retirement record</name>
  <files>.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json, .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md, .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md</files>
  <read_first>
    Read the downloaded receipt, but display only its schema/key inventory until the secret-content
    scanner passes. Read D-02/D-06/D-07 and the pending todo's incorrect Done-when prediction. Read
    the Phase 240 evidence pattern that permits an evidence commit after a clean-tree capture while
    preserving the captured source SHA explicitly.
  </read_first>
  <action>
    Validate the artifact for a second time, scan it for environment dumps, headers, authorization
    material, token/key shapes, raw request bodies, and unprojected API fields, then copy it atomically
    to `242-HEX-REMEDIATION-EVIDENCE.json`. Independently fetch the public package endpoint and
    HexDocs root once; project them through the same helper and require agreement with the final
    artifact slots. Require retirement reason `invalid`, the root classifier `current_1_5`, broad
    resolver version `1.20.0` with warning true, safe resolver version in 1.5.x with warning false,
    distinct fresh homes, and no retirement-ignore flag. Preserve all three actually observed
    `latest_stable_version` values even if they contradict the old todo or roadmap prediction.

    Move the named todo to `completed/` and append a dated closure section citing the workflow run,
    evidence path, public observations, and corrected semantics: retirement is advisory, the broad
    constraint remains unsafe, and only the three-segment supported constraint bounds resolution to
    the maintained line. Retain the todo's historical text as history and explicitly supersede its
    interactive-runbook and resolver/latest claims rather than rewriting them as if they had never
    existed. Stage only these three paths.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; evidence=.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json; bash scripts/ci/hex-remediation-verify.sh --validate-evidence "$evidence"; test ! -e .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md; test -s .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md; git diff --check'</automated>
    <fails_when>The receipt is incomplete, sensitive, or inconsistent with a fresh public read; the retired reason/root/resolver facts do not meet the locked outcomes; the completed todo repeats the false resolution claim as current truth; or an unowned path is staged.</fails_when>
  </verify>
  <acceptance_criteria>
    - The committed JSON is the sanitized workflow artifact and passes the same schema validator locally.
    - Three causal package snapshots, the root observation, both resolver cases, run URL/SHA, and timestamps are nonempty.
    - The retirement todo is completed with an explicit correction, not silently rewritten.
  </acceptance_criteria>
  <done>REL-03 and REL-04 have committed live evidence, and REL-05's resolver premise is measured rather than inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent/GitHub CLI → workflow dispatch | The plan initiates fixed external mutations. |
| Workflow artifact → repository | Public observations become committed evidence. |
| Historical todo → current project truth | An obsolete operational claim is reclassified without erasing history. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-10 | Tampering | duplicate/blind dispatch | high | mitigate | Detect/reuse one matching run, fixed concurrency, no redispatch on failure, public read-after-error. |
| T-242-11 | Information disclosure | workflow logs/artifact | high | mitigate | Never fetch success logs, download named artifact only, schema/content scan before commit. |
| T-242-12 | Repudiation | causal outcome | high | mitigate | Run ID/SHA plus three boundary snapshots and independent final public read. |
| T-242-13 | Spoofing | resolver proof | medium | mitigate | Separate fresh homes/projects, lock-derived selected version, warning boolean, no ignored retirements. |
| T-242-14 | Denial of service | GitHub rate limit | medium | mitigate | One watcher, 60-second interval, rate-limit preflight, 403/429 hard stop. |
</threat_model>

<artifacts_produced>
- One successful fixed-target remediation workflow run.
- Committed `sigra.phase-242-hex-remediation/1` public evidence.
- Corrected completed retirement todo.
</artifacts_produced>

<verification>
Validate the evidence schema, independently re-read Hex and HexDocs, inspect the completed todo correction, run p22, and confirm the staged diff is limited to the three declared paths.
</verification>

<success_criteria>
The public package is retired with reason `invalid`, the phantom docs no longer own the root, the actual resolver/latest behavior is captured without claims beyond observation, and no secret enters repository history.
</success_criteria>

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md` when done.
</output>
