---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/hex-remediate-phantom.yml
  - scripts/ci/hex-remediation-verify.sh
  - scripts/ci/hex-remediation-verify.test.sh
  - scripts/ci/prohibitions/p22-hex-remediation.test.mjs
  - test/fixtures/prohibitions/p22-hex-remediation-broadened.yml
  - test/fixtures/prohibitions/p22-hex-remediation-unsafe-secret.yml
  - test/fixtures/prohibitions/p22-hex-evidence-incomplete.json
autonomous: true
requirements: [REL-03, REL-04, REL-05]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A maintainer can dispatch one dedicated remediation workflow with no package, version, reason, message, or command inputs; the only mutation target is `sigra 1.20.0` (D-01, D-03)."
    - "The lane records three ordered public package observations, one classified root-HexDocs observation, and two isolated resolver receipts without exposing a credential (D-02, D-04, D-06)."
    - "Empty API bodies, invalid UTF-8/JSON projection, or missing evidence slots fail before success can be reported; the fixed ASCII retirement message remains within Hex's length bound."
    - "A fixed concurrency group with cancellation disabled, public-state preflights, and read-after-error classification prevent concurrent or blind repeat writes."
    - "The unsafe broad constraint remains resolvable with a retirement warning while `{:sigra, \"~> 1.5.0\"}` resolves a real 1.5.x release in a separate fresh home (D-05, D-06)."
  artifacts:
    - path: .github/workflows/hex-remediate-phantom.yml
      provides: "Dispatch-only, least-privilege, fixed-target Hex remediation lane"
    - path: scripts/ci/hex-remediation-verify.sh
      provides: "Read-only evidence projection, validation, root-doc classification, and isolated resolver proof"
    - path: scripts/ci/hex-remediation-verify.test.sh
      provides: "Hermetic fake-network/fake-Mix coverage for evidence and resolver failure modes"
    - path: scripts/ci/prohibitions/p22-hex-remediation.test.mjs
      provides: "Structural fixed-target, permission, credential-scope, action-pin, command, and evidence-schema guard"
  key_links:
    - from: .github/workflows/hex-remediate-phantom.yml
      to: scripts/ci/hex-remediation-verify.sh
      via: "checked-out helper captures each causal boundary and validates the combined public receipt"
      pattern: "BEFORE|AFTER_RETIRE|AFTER_DOCS_REVERT|HEXDOCS_ROOT|RESOLVER_BROAD|RESOLVER_SAFE"
    - from: scripts/ci/prohibitions/p22-hex-remediation.test.mjs
      to: .github/workflows/hex-remediate-phantom.yml
      via: "substitutable workflow subject and known-bad fixtures"
      pattern: "GSD_PROHIB_SUBJECT"
---

<objective>
Create the production remediation tracer: one fixed-target workflow path from dispatch through public
observations, retirement, docs-only revert, root-doc classification, and fresh resolver proof, with
hermetic checks that make every trust boundary and evidence slot fail closed per D-01 through D-06.

Purpose: Prove the complete architecture before any public Hex mutation occurs.
Output: Dedicated workflow, reusable verifier, hermetic self-test, p22 structural guard, and attributable known-bad fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-RESEARCH.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-PATTERNS.md
@.github/workflows/hex-publish.yml
@scripts/ci/release-post-publish-verify.sh
@scripts/ci/prohibitions/_lib.mjs
@test/sigra/planning/phase_234_action_pinning_contract_test.exs

<interfaces>
The verifier created here owns schema `sigra.phase-242-hex-remediation/1` and these required,
nonempty slots: `before`, `after_retire`, `after_docs_revert`, `hexdocs_root`,
`resolver_broad`, `resolver_safe`, plus workflow run URL/SHA and capture timestamps.
Package snapshots project only `name`, `latest_version`, `latest_stable_version`,
`retirements`, and selected release `{version, has_docs}` fields. Resolver receipts expose only
the requirement label, selected version, retirement-warning boolean, fresh-home boolean, and exit verdict.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end fixed-target remediation lane — dispatch to validated public receipt</name>
  <files>.github/workflows/hex-remediate-phantom.yml, scripts/ci/hex-remediation-verify.sh, scripts/ci/hex-remediation-verify.test.sh, test/fixtures/prohibitions/p22-hex-evidence-incomplete.json</files>
  <read_first>
    Read D-01 through D-06 in `242-CONTEXT.md`; the permissions, concurrency, setup-beam, and
    step-scoped secret patterns in `.github/workflows/hex-publish.yml`; and the bounded observation
    style in `scripts/ci/release-post-publish-verify.sh`. Read AGENTS.md before designing any watcher
    or external retry. Preserve the exactly-two release-workflow universe in the Phase 234 pin test;
    this new non-publisher is owned by p22 instead.
  </read_first>
  <behavior>
    - Dispatch accepts no user values and always targets package `sigra`, version `1.20.0`, reason `invalid`, and one fixed ASCII message no longer than 140 characters.
    - `contents: read` is the only repository permission; immutable action SHA pins carry same-line version comments; the workflow has a fixed concurrency group and `cancel-in-progress: false`.
    - The key is scoped only to the retirement and docs-revert steps. Each step checks nonempty presence without printing it and provides no interactive fallback.
    - Retirement first observes public state. If already retired with reason `invalid`, it records an idempotent no-op. After a failed write it re-reads state once and accepts only an already-achieved intended state; it never blindly retries.
    - The only docs write is `mix hex.publish docs --revert 1.20.0`; it follows the same observed-state/no-blind-retry discipline and never invokes a package-tarball revert.
    - Public reads reject empty bodies, invalid JSON/encoding, absent target releases, and missing projected fields.
    - Root docs are classified as `current_1_5`, `still_phantom_1_20`, or `unavailable_or_ambiguous`; only `current_1_5` is a successful REL-04 outcome.
    - Broad and safe cases use separate temporary projects and separate fresh `HEX_HOME`/`MIX_HOME`, explicitly unset retirement-ignore behavior, and derive selected versions from each new lockfile.
  </behavior>
  <action>
    Implement `.github/workflows/hex-remediate-phantom.yml` as a dedicated `workflow_dispatch`
    workflow with no `inputs` mapping, fixed constants, `contents: read`, fixed non-cancelling
    concurrency, pinned checkout/setup-beam/upload actions, and no repository write token. Checkout is
    permitted only so the workflow can execute the committed read-only verifier. Install the current
    Hex client, then order steps as BEFORE capture, observed-state retirement, AFTER_RETIRE capture,
    observed-state docs-only revert, AFTER_DOCS_REVERT capture, HexDocs-root classification, fresh
    broad resolver, fresh safe resolver, schema validation, and public-evidence artifact upload.

    Implement `scripts/ci/hex-remediation-verify.sh` with fixed package/version defaults and explicit
    modes for projected snapshot capture, root classification, resolver cases, and final receipt
    validation. Allow overrides only for test-owned public endpoint/fake-command locations and safe
    output paths; never allow package/version/reason/message mutation. Use `mktemp -d` cleanup,
    atomic output replacement, canonical jq projection/sorting, empty-response refusal, and explicit
    UTF-8/JSON validation. Resolver cases must not reuse the repository lock/cache and must record the
    warning fact without retaining full console logs.

    Start `scripts/ci/hex-remediation-verify.test.sh` red against the incomplete JSON fixture, then
    cover success with PATH-shadowed `curl`/`mix` fakes and independent failures for empty response,
    invalid encoding/JSON, absent target release, missing causal slot, broad case lacking a retirement
    warning, safe case selecting outside 1.5.x, shared/nonfresh homes, and any evidence field that
    resembles credential/request material. The self-test performs no network access, uses no sleeps,
    and never requires a real key.
  </action>
  <verify>
    <automated>bash scripts/ci/hex-remediation-verify.test.sh</automated>
    <fails_when>Any empty/malformed observation becomes success; the two resolver cases share state; the broad case lacks the warning or does not select 1.20.0; the safe case does not select 1.5.x; root classification accepts an ambiguous/phantom page; or a credential-shaped/request field survives validation.</fails_when>
  </verify>
  <acceptance_criteria>
    - The happy-path fake run produces every schema-v1 slot in causal order and validates atomically.
    - Every unresolved REL-03 edge is concrete: no dispatch inputs, canonical encoding validation, and one non-cancelling concurrency group.
    - REL-04's previously unclassified root result is now an explicit three-state classifier with one pass state.
    - The real workflow exposes the existing key only at the two authenticated writes and carries no interactive runbook path.
  </acceptance_criteria>
  <done>The complete fixed remediation path exists and its offline tracer proves all layers without touching Hex.</done>
</task>

<task type="auto">
  <name>Task 2: Make target, authority, command, and evidence drift permanently red</name>
  <files>scripts/ci/prohibitions/p22-hex-remediation.test.mjs, test/fixtures/prohibitions/p22-hex-remediation-broadened.yml, test/fixtures/prohibitions/p22-hex-remediation-unsafe-secret.yml</files>
  <read_first>
    Read `scripts/ci/prohibitions/_lib.mjs` for `subjectPath`, `readSubject`, `jobBlock`, and
    comment stripping. Read the completed Task 1 workflow and verifier contracts. Read the Phase 234
    pin test's D-18 invariant so p22 covers this remediation workflow without widening the two
    release-publisher universe.
  </read_first>
  <action>
    Create p22 as a dependency-free `node:test` guard with one substitutable workflow subject and
    non-vacuity floors for dispatch trigger, jobs, mutation steps, captures, action references, and
    evidence slots. Parse by semantic job/step regions after stripping YAML comments. Assert no
    dispatch input surface, exact package/version/reason, fixed bounded ASCII message, `contents:
    read`, fixed concurrency with cancellation disabled, immutable annotated action pins, key use
    only in the two mutation step bodies, no shell tracing, exact retire-before-docs order, the
    docs-only command shape, read-after-error handling, all required evidence slots, public-only
    artifact path, and the absence of repository-write behavior.

    The broadened fixture must retain parseable jobs/steps but independently violate fixed target,
    empty-input, permission, and concurrency contracts. The unsafe-secret fixture must retain target
    and ordering floors while independently violating secret scope, shell/log safety, and revert
    command class. Tests must identify the intended field/step, not merely fail parsing. Include the
    incomplete evidence fixture from Task 1 as the attributable RED for required-slot and empty-value
    validation. Run the real workflow subject green after every fixture assertion.
  </action>
  <verify>
    <automated>node --test --test-reporter=tap scripts/ci/prohibitions/p22-hex-remediation.test.mjs</automated>
    <fails_when>A known-bad fixture passes, fails only because parsing is empty, or lacks its specific diagnostic; the real workflow fails; an unpinned action/key leak/package-revert/broadened input can evade p22; or p22 alters the Phase 234 release-workflow universe.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both YAML fixtures and the incomplete JSON fixture prove distinct RED directions with stable diagnostics.
    - The real workflow is GREEN and the existing prohibitions glob will discover p22 without a `ci.yml` or `mix ci` edit.
    - No new dependency or package install is introduced.
  </acceptance_criteria>
  <done>The remediation contract cannot drift toward arbitrary inputs, wider authority, unsafe credentials, wrong revert class, or vacuous evidence without an attributable test failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub dispatch → Actions job | A repository actor starts an externally mutating workflow. |
| Actions step → Hex | The protected API key crosses only into two fixed official Mix commands. |
| Hex/HexDocs → committed evidence artifact | Untrusted public responses are projected into a durable receipt. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-01 | Elevation of privilege | workflow inputs/permissions | high | mitigate | No dispatch inputs, fixed literals, `contents: read`, p22 broadened fixture. |
| T-242-02 | Information disclosure | key-bearing mutation steps | high | mitigate | Step-only env, no tracing/raw requests, evidence allowlist, credential-content rejection. |
| T-242-03 | Tampering | concurrent/retried Hex writes | high | mitigate | Fixed non-cancelling concurrency plus observed-state preflight and read-after-error; no blind write retry. |
| T-242-04 | Repudiation | merged final observation | medium | mitigate | Three named causal snapshots, root classifier, run URL/SHA, timestamps, and schema validation. |
| T-242-05 | Denial of service | public reads/resolver | low | accept | Bounded external calls fail closed; a transient read failure blocks the lane without corrupting public state. |
</threat_model>

<artifacts_produced>
- Dedicated fixed-target remediation workflow.
- Hermetic evidence/resolver verifier and self-test.
- p22 fixed-target/security/evidence guard with known-bad fixtures.
</artifacts_produced>

<verification>
Run the Bash self-test, p22 Node test, the full prohibition glob, and the existing Phase 234 action-pinning contract. Confirm no package install, `ci.yml`, release publisher, or `mix ci` topology change appears in the diff.
</verification>

<success_criteria>
The new lane is safe to land without executing: every layer is wired end to end, every external write is fixed and observed-state guarded, the evidence schema is secret-free, and all eight REL-03/REL-04 edge concerns assigned to this slice have deterministic handling.
</success_criteria>

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-01-SUMMARY.md` when done.
</output>
