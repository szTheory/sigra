---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 10
type: execute
wave: 4
depends_on: [242-02]
files_modified:
  - .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md
  - .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-10-SUMMARY.md
  - .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
  - .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md
  - .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md
autonomous: true
requirements: [REL-03, REL-04, REL-05]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The halted 242-03 summary is restored byte-for-byte from commit `ee61cfb3` and remains the historical record of run `35554955828`; it is never rewritten as a success summary."
    - "A freshly fetched `origin/main` contains the corrected fixed-target `--message` retire command and a p22 contract that passes against that exact workflow before any dispatch is attempted (D-01, D-03)."
    - "Exactly one new `workflow_dispatch` invocation is made for the corrected workflow; no failure, ambiguous observation, interruption, or resume path causes a second invocation."
    - "The one watcher uses the required 60-second interval after a REST-budget preflight; 403/429 stops all polling until the reported reset/retry time."
    - "Only after a successful run, the named public artifact is downloaded, filename-allowlisted, validated, independently reprojected, and matched to the run SHA without reading success logs or exposing a credential (D-02)."
    - "The normal evidence JSON and retirement-todo closure record the observed invalid retirement, current 1.5.x root docs, broad warning, safe resolver result, and all observed latest fields without claiming retirement repaired resolution (D-04, D-05, D-06)."
  artifacts:
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md
      provides: "Byte-equivalent halted failure/repair evidence recovered from `ee61cfb3`"
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
      provides: "Validated and independently reprojected `sigra.phase-242-hex-remediation/1` public receipt"
    - path: .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md
      provides: "Historical todo retained with a dated, evidence-backed correction and closure"
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-10-SUMMARY.md
      provides: "Distinct re-dispatch outcome; on failure, durable classification without source repair or redispatch"
  key_links:
    - from: ee61cfb3:.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md
      to: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md
      via: "Git blob identity, SHA-256, and byte comparison"
      pattern: "f9659699640d7f145a46289b17da6313d6c30158eb47424e40609bd5febaab97"
    - from: origin/main:.github/workflows/hex-remediate-phantom.yml
      to: origin/main:scripts/ci/prohibitions/p22-hex-remediation.test.mjs
      via: "p22 executes against the freshly fetched default-branch workflow before dispatch"
      pattern: "mix hex.retire sigra 1.20.0 invalid --message"
    - from: "the one successful corrected workflow run"
      to: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json
      via: "named public artifact allowlist, local validator, deterministic reprojection, run-ID/SHA match, and independent public reads"
      pattern: "sigra.phase-242-hex-remediation/1"
---

<objective>
Perform the one explicitly authorized remediation re-dispatch against the corrected default-branch
workflow, while preserving the consumed Plan 03 failure as immutable history and failing closed at
every provenance, mutation, CI, and artifact boundary.

Purpose: Obtain the successful public remediation receipt that halted Plan 03 could not produce,
without erasing that failure, replaying Plan 03, or broadening Phase 242 authorization.
Output: Restored historical 242-03 summary, one corrected workflow run, validated public evidence,
corrected completed todo, and a distinct 242-10 execution summary.

Decision coverage: D-01: reuse only the dedicated fixed-target lane; D-02: preserve the three
causal projections and isolated resolver receipts; D-03: use the corrected noninteractive invalid
retirement with secret-free evidence; D-04: accept only the docs-only revert and measured root;
D-05: preserve the three-segment safe constraint; D-06: prove broad-versus-safe resolver behavior
and observed latest fields. D-07: ADR 005 remains owned by Plan 06; D-08: the Release Please cut
remains owned by Plans 08-09; D-09: the release-note fold/index work remains owned by Plan 07.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-RESEARCH.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-PATTERNS.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-PLAN.md
@.planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md

<interfaces>
Historical evidence source: `ee61cfb3:.../242-03-SUMMARY.md`; expected SHA-256
`f9659699640d7f145a46289b17da6313d6c30158eb47424e40609bd5febaab97`. It records halted run
`35554955828`, unsupported retire-option failure, correction commit
`907d875f5e0953777ffde0657e59082bf68d9e21`, reported merge
`0826a06d48b638c84af5db8984a1e8739f89cfaa`, zero completed Plan 03 tasks, and no completed requirement.

Corrected command contract: `mix hex.retire sigra 1.20.0 invalid --message
'Retired: use the supported ~> 1.5.0 requirement instead.'`. The executor must prove this contract
from freshly fetched `origin/main`, not infer it from either historical commit identifier.

The only accepted artifact is `hex-remediation-public-evidence`. Its allowlisted files are
`before.json`, `after_retire.json`, `after_docs_revert.json`, `hexdocs_root.json`,
`resolver_broad.json`, `resolver_safe.json`, and `receipt.json`. The final receipt schema is
`sigra.phase-242-hex-remediation/1` and its `workflow_sha` must equal the observed run head SHA.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end readiness tracer — preserve the halt and prove corrected live main</name>
  <precondition>Git objects `ee61cfb3:.../242-03-SUMMARY.md` and `907d875f` are locally readable, and `git fetch origin main` can refresh the default-branch ref.</precondition>
  <files>.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md, .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-10-SUMMARY.md</files>
  <read_first>
    Read the complete halted summary directly from `ee61cfb3`, then read the workflow and p22 blobs
    from `907d875f` only as historical correction evidence. Read AGENTS.md. Do not use the current
    checkout or the reported merge SHA as proof of live default-branch content.
  </read_first>
  <action>
    Restore `242-03-SUMMARY.md` from the exact `ee61cfb3` tree entry without editing, normalizing,
    appending, or regenerating it. Prove byte equivalence with the source blob and the expected
    SHA-256. Preserve its `status: halted`, `requirements-completed: []`, failed run, correction,
    and no-redispatch decision exactly as historical evidence; Plan 10 will own its own summary.

    Fetch `origin/main` immediately before the source gate and persist the resolved 40-hex SHA in
    `/tmp/sigra-242-10-main-sha`. Export the freshly fetched workflow, p22 test, its helper, and its
    fixtures into a new temporary directory. Require the workflow to contain the exact corrected
    fixed-target message command, then execute that exported p22 test against that exported
    workflow. Also require dispatch-only/no-input shape, fixed non-cancelling concurrency,
    `contents: read`, exactly two step-scoped key references, and the named public artifact. If any
    blob, semantic assertion, or historical byte check fails, write a blocking diagnostic to the
    distinct 242-10 summary and HALT without dispatch. Source repair is not authorized here.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; s=.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md; test "$(git hash-object "$s")" = "$(git rev-parse ee61cfb3:"$s")"; digest=$(shasum -a 256 "$s"); test "${digest%% *}" = f9659699640d7f145a46289b17da6313d6c30158eb47424e40609bd5febaab97; test -s /tmp/sigra-242-10-main-sha; sha=$(cat /tmp/sigra-242-10-main-sha); test "$sha" = "$(git rev-parse origin/main)"; tmp=$(mktemp -d); trap '\''rm -rf -- "$tmp"'\'' EXIT; git archive -o "$tmp/live-main.tar" "$sha" .github/workflows/hex-remediate-phantom.yml scripts/ci/prohibitions/p22-hex-remediation.test.mjs scripts/ci/prohibitions/_lib.mjs test/fixtures/prohibitions/p22-hex-remediation-broadened.yml test/fixtures/prohibitions/p22-hex-remediation-unsafe-secret.yml; tar -xf "$tmp/live-main.tar" -C "$tmp"; rg -F -- "mix hex.retire sigra 1.20.0 invalid --message '\''Retired: use the supported ~> 1.5.0 requirement instead.'\''" "$tmp/.github/workflows/hex-remediate-phantom.yml" >/dev/null; GSD_PROHIB_SUBJECT="$tmp/.github/workflows/hex-remediate-phantom.yml" node --test --test-reporter=tap "$tmp/scripts/ci/prohibitions/p22-hex-remediation.test.mjs"'</automated>
    <fails_when>The restored file differs by one byte; its expected source is unavailable; `origin/main` is stale/unfetched; the exact corrected command is absent; p22 is absent, stale, vacuous, or red against the live workflow; or any source change would be required.</fails_when>
  </verify>
  <acceptance_criteria>
    - `242-03-SUMMARY.md` is byte-identical to `ee61cfb3` and retains the halted status and consumed-dispatch facts.
    - The fetched default-branch SHA is recorded before any GitHub workflow mutation.
    - p22 passes using only files exported from that fetched SHA and proves the corrected command/authority boundary.
    - A failed readiness gate produces no workflow dispatch and no source edit.
  </acceptance_criteria>
  <done>The historical failure is durably restored and the actual default branch—not a reported merge—is proven safe for one new dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Invoke once, watch once, and preserve either success or classified failure</name>
  <precondition>Task 1 recorded a p22-green fetched `origin/main` SHA; `gh auth status` succeeds for `szTheory/sigra`; and the GitHub REST core budget is greater than 250.</precondition>
  <reversibility rating="costly">This changes public Hex retirement/docs state; reversal would require a separately authorized registry mutation even though the operations are technically reversible.</reversibility>
  <files>.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-10-SUMMARY.md</files>
  <read_first>
    Read AGENTS.md immediately before every GitHub wait. Read the fetched workflow source and the
    restored 242-03 summary. The old run `35554955828` is evidence, never a run to rerun or relabel.
    Never inspect, print, request, rotate, collect, or write `HEX_API_KEY`.
  </read_first>
  <action>
    Create `/tmp/sigra-242-10-dispatch-control.json` with the fetched source SHA, UTC start time,
    baseline workflow-run IDs, and zero invocation/watcher counts; it must contain no environment
    dump. If `/tmp/sigra-242-10-remediation-run-id` already exists on resume, validate and reuse that
    exact run without invoking. If the marker is absent but a workflow-dispatch run already exists at
    the fetched corrected SHA outside the recorded baseline, HALT for classification rather than
    risk a duplicate.

    Invoke exactly once with `gh workflow run hex-remediate-phantom.yml --repo szTheory/sigra
    --ref main` and no inputs. Resolve exactly one newly created run whose workflow name, event,
    branch, head SHA, and creation time match, then atomically persist its numeric ID in
    `/tmp/sigra-242-10-remediation-run-id`; increment the control receipt to one invocation. Do not
    call a rerun endpoint and do not invoke again after any error or ambiguous observation.

    Inspect `gh api rate_limit` once immediately before waiting. At 250 or fewer remaining requests,
    stop until the reported reset time. Treat 403/429 as a hard stop until GitHub's reported
    reset/retry time. Start at most one watcher using exactly `gh run watch RUN_ID --repo
    szTheory/sigra --compact --interval 60 --exit-status`; never poll the same run concurrently.
    After completion fetch one structured JSON summary into
    `/tmp/sigra-242-10-run-summary.json`. On success, do not download or inspect logs and continue to
    Task 3. On failure, fetch failed logs once, retain only a sanitized job/step/error classification
    in `242-10-SUMMARY.md`, re-observe public state only to classify whether a mutation landed, and
    HALT. Never redispatch; any repair or further mutation needs a separate plan and authorization.
    A transient read/watch failure may be retried once only when it does not issue a mutation.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -s /tmp/sigra-242-10-remediation-run-id; test -s /tmp/sigra-242-10-main-sha; test -s /tmp/sigra-242-10-run-summary.json; test -s /tmp/sigra-242-10-dispatch-control.json; id=$(cat /tmp/sigra-242-10-remediation-run-id); sha=$(cat /tmp/sigra-242-10-main-sha); jq -e --argjson id "$id" --arg sha "$sha" '\''.databaseId == $id and .event == "workflow_dispatch" and .headBranch == "main" and .headSha == $sha and .status == "completed" and .conclusion == "success"'\'' /tmp/sigra-242-10-run-summary.json >/dev/null; jq -e '\''.dispatch_invocations == 1 and .watchers_started == 1 and .structured_summary_fetches == 1 and .success_logs_downloaded == false'\'' /tmp/sigra-242-10-dispatch-control.json >/dev/null'</automated>
    <fails_when>The readiness SHA moves; invocation count is not exactly one; a pre-existing unclassified corrected-SHA run exists; the run identity is ambiguous; rate-limit policy is violated; more than one watcher or structured-summary fetch occurs; success logs are accessed; or the run is not successful.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one new invocation and one run ID are recorded for the Task 1 source SHA.
    - Exactly one 60-second watcher is used after an acceptable REST-budget reading.
    - Success is represented by one structured summary and no success-log access.
    - Failure is sanitized and durable in the distinct 242-10 summary, with no redispatch, source repair, evidence promotion, todo move, or execution of Plans 04-09.
  </acceptance_criteria>
  <done>One corrected run has succeeded and is eligible for receipt processing, or the addendum has halted with classified evidence and no repeated mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Validate and reproject the public artifact, then update only normal evidence paths</name>
  <precondition>Task 2's one run completed successfully at the Task 1 SHA, and the structured summary identifies exactly one artifact named `hex-remediation-public-evidence`.</precondition>
  <files>.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-10-SUMMARY.md, .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json, .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md, .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md</files>
  <read_first>
    Read the validator from the exact Task 1 default-branch SHA and Plan 03's intended receipt/todo
    destinations. Read D-02 and D-04 through D-07. Inspect artifact names, paths, sizes, and file
    types before content; never download or inspect success logs, and never print a rejected value.
  </read_first>
  <action>
    Download only `hex-remediation-public-evidence` with `gh run download` into a new `mktemp -d`
    directory. Reject symlinks, nested surprises, duplicate basenames, empty files, or any filename
    outside the seven-file allowlist in `<interfaces>`. Run the fetched-source validator first
    against the artifact's `receipt.json` into a temporary output, suppressing receipt contents.
    Then independently reproject the six component files through the same validator with
    `GITHUB_RUN_ID`, `GITHUB_SHA`, `GITHUB_SERVER_URL`, and `GITHUB_REPOSITORY` fixed to the observed
    run metadata. Require the validated receipt, deterministic reprojection, artifact receipt, and
    run identity to match byte-for-byte. The validator's credential-shaped-field rejection must pass
    without echoing any rejected content.

    Independently capture the public Hex package projection and classify the HexDocs root once into
    temporary files. Require the public package to list retirement version `1.20.0` with reason
    `invalid`, root classification `current_1_5`, broad resolver `1.20.0` with warning true, safe
    resolver in 1.5.x with warning false, and fresh-home facts true. Compare current public
    retirements/root facts to the receipt while preserving the receipt's BEFORE, AFTER_RETIRE,
    AFTER_DOCS_REVERT, and every observed `latest_stable_version` value verbatim; do not infer a
    resolver or latest-version repair.

    Atomically install only the independently reprojected JSON at
    `242-HEX-REMEDIATION-EVIDENCE.json`. Move the named todo from `pending/` to `completed/`, retain
    all prior text as historical context, and append a dated closure citing the Plan 10 run URL,
    evidence path, invalid retirement, docs-root result, broad warning, safe constraint, and the
    supersession of its interactive-runbook and resolver/latest predictions. Stage only the evidence
    JSON, the todo move, restored historical summary, and executor-owned 242-10 summary. Never edit
    the restored 242-03 summary, workflow, p22, permissions, release files, safe pin, or Plans 04-09.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; e=.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-HEX-REMEDIATION-EVIDENCE.json; s=.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-03-SUMMARY.md; test -s "$e"; tmp=$(mktemp -d); trap '\''rm -rf -- "$tmp"'\'' EXIT; scripts/ci/hex-remediation-verify.sh validate "$e" "$tmp/validated.json" >/dev/null; cmp -s "$e" "$tmp/validated.json"; test "$(git hash-object "$s")" = "$(git rev-parse ee61cfb3:"$s")"; jq -e '\''.schema == "sigra.phase-242-hex-remediation/1" and any(.after_retire.package.retirements[]?; .version == "1.20.0" and .reason == "invalid") and .hexdocs_root.classification == "current_1_5" and .resolver_broad.selected_version == "1.20.0" and .resolver_broad.retirement_warning == true and .resolver_safe.retirement_warning == false'\'' "$e" >/dev/null; safe=$(jq -er '\''.resolver_safe.selected_version'\'' "$e"); [[ "$safe" =~ ^1\.5\.[0-9]+$ ]]; test ! -e .planning/todos/pending/2026-07-03-hex-retire-stray-1-20-0.md; test -s .planning/todos/completed/2026-07-03-hex-retire-stray-1-20-0.md; git diff --check'</automated>
    <fails_when>The artifact name/file set is broader than allowed; any file is empty, linked, nested, unsafe, or fails schema validation; receipt/reprojection/run identity differs; independent public observations disagree; the required retirement/docs/resolver facts are absent; the historical summary changes; the todo loses history; or an unowned path is staged.</fails_when>
  </verify>
  <acceptance_criteria>
    - The artifact is accepted only after file-shape, schema, content-safety, run-identity, and deterministic-reprojection checks.
    - The committed JSON is the validated reprojection, not an unexamined artifact copy.
    - Independent Hex and HexDocs reads agree with the receipt's final public facts.
    - The completed todo explicitly corrects its old predictions while retaining them as history.
    - `242-03-SUMMARY.md` remains byte-equivalent to `ee61cfb3`; success is recorded only in `242-10-SUMMARY.md`.
  </acceptance_criteria>
  <done>REL-03 and REL-04 have a successful, secret-free live receipt, and REL-05 has the measured resolver premise needed by its existing ADR/docs plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local executor → GitHub Actions | One authorized CLI invocation starts externally mutating automation. |
| GitHub Actions secret env → Hex | Protected authority crosses only into the workflow's two fixed mutation steps. |
| Hex/HexDocs → workflow artifact → repository | Untrusted external observations become durable project evidence. |
| Historical git object → working tree | A halted execution record is restored without allowing present success to rewrite it. |
| GitHub REST budget → watcher | Rate-limited API state controls whether observation may continue safely. |

## STRIDE Threat Register (ASVS L1; critical/high findings block)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-10-01 | Information disclosure | secret scope and logs/artifact | high | mitigate | Prove p22 on fetched main; never access success logs; download only the named public artifact; reject credential-shaped fields without echoing values. |
| T-242-10-02 | Tampering | duplicate dispatch/resume | high | mitigate | Baseline run IDs, atomic run-ID marker, exactly one invocation counter, reuse only the marked run, halt on any unclassified corrected-SHA run, no rerun API. |
| T-242-10-03 | Tampering | external Hex mutation | high | mitigate | Exact fixed target/reason/message, p22-green live source, non-cancelling concurrency, observed-state workflow behavior, and no redispatch or in-plan repair after failure. |
| T-242-10-04 | Tampering | historical evidence | high | mitigate | Restore only from `ee61cfb3`; require Git blob identity and SHA-256 before and after; record success solely in 242-10 summary. |
| T-242-10-05 | Spoofing | artifact-to-repository trust | high | mitigate | Exact run ID/SHA, artifact filename allowlist, symlink/extra-file rejection, schema validation, deterministic reprojection, byte comparison, and independent public reads. |
| T-242-10-06 | Denial of service | GitHub rate limiting | medium | mitigate | One watcher at 60 seconds, core-budget preflight, one structured summary, and hard stop on 403/429 until the reported time. |
| T-242-10-07 | Repudiation | failed re-dispatch | medium | mitigate | Distinct halted 242-10 summary records run URL/SHA, structured conclusion, sanitized failed-step classification, and public-state classification without altering 242-03. |
</threat_model>

<artifacts_produced>
- Byte-for-byte restored historical `242-03-SUMMARY.md` from `ee61cfb3`.
- Exactly one corrected fixed-target workflow-dispatch run, or a distinct durable halted classification.
- Validated, deterministically reprojected `242-HEX-REMEDIATION-EVIDENCE.json` after success only.
- Corrected completed retirement todo after success only.
- Distinct `242-10-SUMMARY.md`; never a rewritten 242-03 success narrative.
</artifacts_produced>

<verification>
The three task gates jointly prove historical-byte identity, live-default-branch correction, one
invocation/one watcher, structured run success, artifact allowlisting and reprojection, independent
public-state agreement, and an owned-path-only repository update. Any failed mutation or
deterministic observation halts without redispatch, source repair, or evidence promotion.
</verification>

<success_criteria>
- The historical Plan 03 halt is present byte-for-byte and separately auditable.
- The new run is the sole Plan 10 invocation at the fetched corrected SHA and completed successfully under the AGENTS.md watcher/rate-limit contract.
- The committed receipt validates as `sigra.phase-242-hex-remediation/1`, matches its artifact and run identity, and agrees with independent public reads.
- Hex reports the invalid `1.20.0` retirement, root HexDocs classify as current 1.5.x, and resolver facts preserve the broad-versus-safe distinction.
- Only the normal remediation evidence/todo paths plus the two plan summaries change; no source, permission, release, package-tarball, pin, or Plans 04-09 work occurs.
</success_criteria>

## Source Coverage Audit — remediation addendum only

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | remediation segment | Warn about phantom 1.20.0, restore current root docs, and state what changed honestly | 242-10 | COVERED | This addendum supplies the missing successful receipt; installable 1.5.1 remains in existing Plans 07-09. |
| REQ | REL-03 | Fixed workflow retirement plus Hex API proof | 242-10 | COVERED | Live-main preflight, one dispatch, invalid-retirement receipt. |
| REQ | REL-04 | Docs-only revert plus current root HexDocs observation | 242-10 | COVERED | Corrected existing workflow only; no package-tarball path. |
| REQ | REL-05 | Safe-pin decision grounded in honest broad/safe resolver facts | 242-10 | COVERED | Advances the measured premise only; ADR/docs implementation remains in existing Plans 04-06. |
| REQ | REL-06 | Cut/publish 1.5.1 from green provenance | 07-09 | RETAINED | Outside this authorization; no Plan 10 task executes or alters it. |
| RESEARCH | remediation execution | Fixed dispatch, secret scope, causal receipt, fresh resolver proof, no blind retry | 242-10 | COVERED | Reuses the corrected Plan 01 infrastructure and Plan 03 schema/destinations. |
| RESEARCH | adopter docs and release | Safe docs, ADR, Release Please path, post-publish proof | 04-09 | RETAINED | Non-remediation coverage stays in existing plans and is not duplicated. |
| CONTEXT | D-01–D-04 | Dedicated lane, three boundaries, exact retire semantics, docs-only revert | 242-10 | COVERED | The addendum dispatches the corrected fixed-target lane once and validates its receipt. |
| CONTEXT | D-05–D-06 | Three-segment safe constraint and honest broad/safe resolver/latest evidence | 242-10 | COVERED | Receipt facts only; no safe-pin text is changed. |
| CONTEXT | D-07 | ADR 005 and active-record reconciliation | 06 | RETAINED | Existing plan retains ownership after successful evidence. |
| CONTEXT | D-08–D-09 | Release Please 1.5.1, Unreleased fold, docs index | 07-09 | RETAINED | Explicitly outside the remediation addendum. |
| CONTEXT | Deferred Ideas | Release-lane refactors, UI/auth work, dependency updates; package-tarball revert forbidden | NONE | EXCLUDED | Deferred/out-of-scope items remain absent. |

No source item is missing: Plan 10 covers only the explicitly authorized re-dispatch gap; existing
Plans 01-09 retain every non-remediation responsibility without modification or duplication.

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-10-SUMMARY.md` when done. On a failed run, create it with `status: halted`, preserve the classified run evidence, and stop without redispatch.
</output>
