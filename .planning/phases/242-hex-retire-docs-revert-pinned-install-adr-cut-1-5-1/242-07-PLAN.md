---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 07
type: execute
wave: 5
depends_on: [242-06]
files_modified:
  - CHANGELOG.md
  - doc/llms.txt
  - .planning/todos/pending/2026-07-28-release-please-orphans-unreleased-block.md
  - .planning/todos/completed/2026-07-28-release-please-orphans-unreleased-block.md
  - .planning/todos/pending/2026-09-16-docs-index-goes-stale-on-every-version-bump.md
  - .planning/todos/completed/2026-09-16-docs-index-goes-stale-on-every-version-bump.md
autonomous: true
requirements: [REL-05, REL-06]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Remediation evidence, safe docs, ADR 005, and their contracts land on `main` before the release candidate can be merged (D-08)."
    - "Release Please PR #224 is based on that corrective merge and owns the 1.5.1 changes to `mix.exs`, `.release-please-manifest.json`, and the generated release heading."
    - "Before PR #224 merges, the substantive Unreleased material is inside the nonempty 1.5.1 section; the Unreleased region is comment-only and adjacent to the new heading (D-09)."
    - "`mix docs` regenerates tracked `doc/llms.txt` at 1.5.1, and the always-on Phase 242 contract makes later version drift fail."
    - "The Release Please orphaning and docs-index staleness todos move to completed with executable-contract evidence, not reviewer-attention claims."
  artifacts:
    - path: CHANGELOG.md
      provides: "Release-candidate 1.5.1 notes with empty substantive Unreleased body"
    - path: doc/llms.txt
      provides: "Tracked documentation index regenerated at version 1.5.1"
    - path: .planning/todos/completed/2026-07-28-release-please-orphans-unreleased-block.md
      provides: "Closure against automatic release-candidate contract"
    - path: .planning/todos/completed/2026-09-16-docs-index-goes-stale-on-every-version-bump.md
      provides: "Closure against generic mix-version/docs-index comparison"
  key_links:
    - from: test/sigra/planning/phase_242_hex_release_contract_test.exs
      to: CHANGELOG.md
      via: "Release Please branch detection enforces comment-only Unreleased, adjacency, and nonempty current section"
      pattern: "GITHUB_HEAD_REF|SIGRA_RELEASE_CANDIDATE"
    - from: mix.exs
      to: doc/llms.txt
      via: "`mix docs` generation and generic version-header assertion"
      pattern: "# Sigra v1.5.1 - Table of Contents"
---

<objective>
Land all corrective work, let Release Please produce the 1.5.1 candidate, then complete that candidate
by folding hand-written notes and regenerating the tracked documentation index before any merge authorization.

Purpose: Make the exact release PR self-consistent and mechanically incapable of shipping orphaned notes or a stale index.
Output: Corrective main merge, updated release PR #224, folded CHANGELOG, refreshed `doc/llms.txt`, and two completed todos.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-06-SUMMARY.md
@.planning/todos/pending/2026-07-28-release-please-orphans-unreleased-block.md
@.planning/todos/pending/2026-09-16-docs-index-goes-stale-on-every-version-bump.md
@.github/workflows/release-please.yml
@release-please-config.json
@.release-please-manifest.json
@CHANGELOG.md
@doc/llms.txt
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land the corrective branch and prove release PR #224 includes it</name>
  <precondition>`gh auth status` succeeds; Plans 03-06 are committed on one clean scoped branch rooted at Plan 02's default-branch merge; GitHub REST core remaining is above 250.</precondition>
  <files>(no tracked files modified; corrective PR, CI, merge, and Release Please PR state only)</files>
  <read_first>
    Read AGENTS.md's push, watcher, and rate-limit rules. Read Plans 03-06 summaries and build an
    explicit path allowlist; preserve unrelated working-tree changes. Read PR #224 and Release
    Please workflow state before assuming the PR is open or current.
  </read_first>
  <action>
    Run the focused Phase 242 tests, p22, p18 ratchet, docs warnings gate, and `MIX_ENV=test mix ci`
    at the exact corrective branch SHA. Push that SHA with an explicit refspec, open/reuse one scoped
    corrective PR, assert its owned diff, and use the AGENTS.md single-watcher/rate-limit protocol for
    the exact-head `ci.yml` run. Merge only after required checks are green and re-read `origin/main`
    to prove the corrective SHA is an ancestor.

    Observe the Release Please workflow caused by that main merge. Query PR #224 directly and require
    it to be open, based on `main`, owned by the configured Release Please branch, and updated after
    the corrective merge. Fetch its head and require the corrective main merge to be an ancestor.
    Require its diff to show version `1.5.1` in `mix.exs` and `.release-please-manifest.json` plus a
    generated 1.5.1 CHANGELOG heading. If PR #224 is absent, closed, merged, points elsewhere, or is
    not updated to the corrective base, stop; do not substitute another PR or publish path.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; pr_json=$(gh pr view 224 --repo szTheory/sigra --json state,baseRefName,headRefName,headRefOid,mergeStateStatus,url); test "$(jq -r .state <<<"$pr_json")" = OPEN; test "$(jq -r .baseRefName <<<"$pr_json")" = main; head=$(jq -r .headRefOid <<<"$pr_json"); git fetch origin main "$(jq -r .headRefName <<<"$pr_json")"; git cat-file -e "${head}^{commit}"'</automated>
    <fails_when>The corrective gate is red, an unowned file enters its PR, the merge is not on main, PR #224 is absent/wrong/stale, or the release candidate does not carry the corrective merge and generated 1.5.1 metadata.</fails_when>
  </verify>
  <acceptance_criteria>
    - Corrective work is merged only from an exact green SHA.
    - PR #224's head contains that main merge and Release Please's 1.5.1 version changes.
    - No release PR merge or Hex publication occurs in this task.
  </acceptance_criteria>
  <done>The corrective source of truth is on main and the exact unmerged 1.5.1 Release Please head is ready for maintainer additions.</done>
</task>

<task type="auto">
  <name>Task 2: Fold Unreleased into 1.5.1 and regenerate the tracked docs index</name>
  <precondition>PR #224 remains open at the head OID proven by Task 1; its checked-out tree has `@version`, manifest version, and first generated release heading equal to `1.5.1`.</precondition>
  <files>CHANGELOG.md, doc/llms.txt</files>
  <read_first>
    Read the complete Unreleased and 1.5.1 regions at the checked-out PR head, D-09, and the
    docs-index todo. Read `git status --short` before generation and remember that all generated
    `doc/` content except `doc/llms.txt` is ignored and must not be staged.
  </read_first>
  <action>
    Check out the exact PR #224 head branch without discarding unrelated user changes. Move every
    substantive hand-written bullet/heading from Unreleased into the generated 1.5.1 section,
    preserving the maintainer warning comment under Unreleased and integrating the prose with
    Release Please's generated sections without duplication. Leave only comments and whitespace
    between `## Unreleased` and the 1.5.1 heading. Ensure the 1.5.1 section remains substantive and
    adopter-oriented.

    Run `mix docs` at the candidate's 1.5.1 version. Stage only the resulting tracked
    `doc/llms.txt` plus CHANGELOG. Require the first line to name 1.5.1 and inspect `git status` so no
    ignored generated page, package artifact, or unrelated path is included. Do not manually edit
    `mix.exs`, `.release-please-manifest.json`, tag metadata, or Release Please configuration.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; SIGRA_RELEASE_CANDIDATE=1 MIX_ENV=test mix test test/sigra/planning/phase_242_hex_release_contract_test.exs; head -n 1 doc/llms.txt | rg -F '"'"'# Sigra v1.5.1 - Table of Contents'"'"'; mix docs --warnings-as-errors; git diff --check'</automated>
    <fails_when>Unreleased retains substantive text, the 1.5.1 section is empty/nonadjacent, version/manifest/index disagree, generated output beyond `doc/llms.txt` is staged, or a Release Please-owned version file is manually edited.</fails_when>
  </verify>
  <acceptance_criteria>
    - Unreleased is substantively empty while retaining its maintainer warning.
    - The adjacent 1.5.1 section contains all curated Phase 242 notes and generated entries.
    - `doc/llms.txt` is regenerated at 1.5.1 and docs build warning-free.
  </acceptance_criteria>
  <done>The candidate's shipped changelog and tracked docs index are internally consistent before merge.</done>
</task>

<task type="auto">
  <name>Task 3: Close the orphan-note and index-drift todos, then push a green candidate head</name>
  <files>.planning/todos/pending/2026-07-28-release-please-orphans-unreleased-block.md, .planning/todos/completed/2026-07-28-release-please-orphans-unreleased-block.md, .planning/todos/pending/2026-09-16-docs-index-goes-stale-on-every-version-bump.md, .planning/todos/completed/2026-09-16-docs-index-goes-stale-on-every-version-bump.md</files>
  <read_first>
    Read both pending todos, the final Phase 242 contract test, and Task 2's exact diff. Preserve the
    original diagnosis/history in each todo while adding measured completion evidence.
  </read_first>
  <action>
    Move both todos to `completed/`. For the orphan-note todo, cite the release-candidate branch
    detection plus semantic empty/adjacent/nonempty assertions and the observed 1.5.1 candidate pass;
    distinguish this durable test from the former HTML-comment-only mitigation. For the docs-index
    todo, cite the generic `mix.exs`/`doc/llms.txt` version comparison, the regenerated 1.5.1 header,
    and normal `mix ci` discovery. Preserve all historical content and append dated closure sections.

    Commit only CHANGELOG, `doc/llms.txt`, and the four todo paths on the existing PR #224 branch.
    Run the release-candidate focused test and `MIX_ENV=test mix ci` before pushing. Push without
    force, re-read PR #224 head OID, and require it to equal the tested commit. Do not merge the PR;
    Plan 08 owns exact-head readiness and the human one-way gate.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; test ! -e .planning/todos/pending/2026-07-28-release-please-orphans-unreleased-block.md; test ! -e .planning/todos/pending/2026-09-16-docs-index-goes-stale-on-every-version-bump.md; test -s .planning/todos/completed/2026-07-28-release-please-orphans-unreleased-block.md; test -s .planning/todos/completed/2026-09-16-docs-index-goes-stale-on-every-version-bump.md; SIGRA_RELEASE_CANDIDATE=1 MIX_ENV=test mix test test/sigra/planning/phase_242_hex_release_contract_test.exs; git diff --check'</automated>
    <fails_when>A todo is closed without executable evidence, the candidate contract is red, `mix ci` was not green before push, the tested commit differs from PR #224 head, or the PR is merged.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both todos retain their diagnoses and gain dated, measured closure sections.
    - The exact pushed PR head passes candidate mode and the full contributor gate.
    - PR #224 remains open at the end of the plan.
  </acceptance_criteria>
  <done>The complete 1.5.1 candidate is green and reviewable, with both release-maintenance todos durably resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Corrective branch → main | Remediation/docs/ADR contracts become the release base. |
| Release Please branch → immutable release | Candidate content will become a published package if merged. |
| `mix docs` output → tracked index | Generated version metadata becomes committed documentation. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-26 | Tampering | stale release PR base | high | mitigate | Require corrective main merge ancestry at exact PR head. |
| T-242-27 | Repudiation | orphaned notes | high | mitigate | Semantic candidate-mode test for empty Unreleased, adjacency, and nonempty 1.5.1 section. |
| T-242-28 | Tampering | docs index | medium | mitigate | Generate with candidate version and compare header generically in normal CI. |
| T-242-29 | Elevation of privilege | release branch push | medium | mitigate | Non-force push to the existing Release Please branch, explicit file allowlist, no merge. |
| T-242-30 | Denial of service | CI wait | low | accept | One watcher and rate-limit protocol; failure leaves the release unmerged. |
</threat_model>

<artifacts_produced>
- Corrective source PR merged before release.
- Green updated Release Please PR #224.
- Folded 1.5.1 changelog and regenerated tracked docs index.
- Two completed release-maintenance todos.
</artifacts_produced>

<verification>
Confirm exact corrective ancestry, release candidate mode, version/manifest/index equality, comment-only Unreleased region, nonempty 1.5.1 notes, full `mix ci`, and open/unmerged PR #224 at the tested head.
</verification>

<success_criteria>
The only remaining action on PR #224 is the explicitly gated release merge: all source, documentation, evidence, changelog, index, and todo obligations are already green at its exact head.
</success_criteria>

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-07-SUMMARY.md` when done.
</output>
