---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 11
type: execute
wave: 5
depends_on: [242-10]
files_modified:
  - .github/workflows/hex-remediate-phantom.yml
  - scripts/ci/prohibitions/p22-hex-remediation.test.mjs
  - test/fixtures/prohibitions/p22-hex-remediation-missing-deps.yml
autonomous: true
requirements: [REL-03, REL-04]

must_haves:
  truths:
    - "The remediation workflow fetches locked project dependencies before either Hex mutation command can run."
    - "The p22 contract fails if dependency setup is removed, moved after a mutation, or given the Hex credential."
    - "No workflow dispatch, registry mutation, or evidence/todo promotion occurs in this plan."
  artifacts:
    - path: .github/workflows/hex-remediate-phantom.yml
      provides: "Dependency-ready remediation workflow with unchanged least-privilege mutation boundary"
    - path: scripts/ci/prohibitions/p22-hex-remediation.test.mjs
      provides: "Deterministic structural guard for dependency ordering and secret scope"
---

<objective>
Repair the deterministic CI precondition exposed by remediation run 35709493996 without invoking the
workflow or changing Hex state. The resulting workflow must obtain dependencies before any command
that can retire a release or revert published docs, and its prohibition test must enforce that order.
</objective>

<tasks>
<task type="auto">
  <name>Add dependency setup before the mutation boundary</name>
  <action>Add a named `mix deps.get --check-locked` step after the Hex-client setup and before the BEFORE observation and both mutation steps. Do not add `HEX_API_KEY` to that step, broaden permissions, alter inputs, alter concurrency, or change the two existing mutation commands.</action>
  <verify><automated>Export the workflow from the repair commit and run p22 against it; inspect that the dependency step precedes both mutation steps, has no `env`, and uses `--check-locked`.</automated></verify>
</task>
<task type="auto">
  <name>Extend the p22 contract with an ordering regression fixture</name>
  <action>Add a fixture lacking or misordering the dependency step. Extend p22 so that production workflow acceptance requires exactly one dependency-fetch step before mutations, and the fixture is rejected. Preserve existing fixed-target, dispatch-only, artifact, secret-scope, and command-syntax assertions.</action>
  <verify><automated>node --test --test-reporter=tap scripts/ci/prohibitions/p22-hex-remediation.test.mjs && bash scripts/ci/hex-remediation-verify.test.sh && git diff --check</automated></verify>
</task>
</tasks>

<boundary>
This is a source-only repair. It must not dispatch GitHub Actions, call a rerun endpoint, invoke Hex
mutation commands, download artifacts, modify remediation evidence, move the pending todo, or start
Plans 04–09. A subsequent public workflow invocation remains a separate explicitly authorized plan.
</boundary>
