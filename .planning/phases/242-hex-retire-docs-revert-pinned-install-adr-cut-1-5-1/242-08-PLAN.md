---
phase: 242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1
plan: 08
type: execute
wave: 6
depends_on: [242-07]
files_modified: []
autonomous: false
requirements: [REL-06]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PR #224's current head is not behind `main`, includes every Phase 242 prerequisite, and passes an exact-head `ci.yml` run with successful `ci-gate`."
    - "The release-readiness receipt is a sanitized public PR comment bound to the exact head SHA, CI run, corrective merge, changelog/index checks, and absent/matching Hex 1.5.1 state."
    - "Nothing is merged, tagged, or published before a blocking-human authorization at the one-way door."
    - "The checkpoint does not reopen the locked 1.5.1/version/path decision; it authorizes or holds the already-defined immutable operation."
  artifacts:
    - path: "GitHub PR #224 comment marker `sigra.phase-242-release-readiness/1`"
      provides: "No-HEAD-movement exact candidate proof packet"
    - path: .planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-08-SUMMARY.md
      provides: "Durable checkpoint outcome and authorized exact PR head"
  key_links:
    - from: "PR #224 head SHA"
      to: "ci.yml run"
      via: "run headSha equality and successful ci-gate job"
      pattern: "sigra.phase-242-release-readiness/1"
    - from: 242-08-SUMMARY.md
      to: 242-09
      via: "blocking-human authorization names the exact immutable candidate SHA"
      pattern: "authorize-1.5.1"
---

<objective>
Assemble a read-only exact-SHA release proof packet and stop at the required human authorization
immediately before merging the immutable 1.5.1 release candidate.

Purpose: Make the one-way door explicit without asking a human to repeat automatable verification.
Output: Exact-head readiness comment and recorded authorize/hold checkpoint outcome.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-CONTEXT.md
@.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-07-SUMMARY.md
@.github/workflows/release-please.yml
@scripts/ci/wait-for-ci-gate.sh
@scripts/ci/release-post-publish-verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove exact-head release readiness and post the immutable candidate receipt</name>
  <precondition>PR #224 remains open at Plan 07's tested head; `gh auth status` succeeds; GitHub REST core remaining is above 250.</precondition>
  <files>(no tracked files modified; read-only checks and one sanitized PR comment)</files>
  <read_first>
    Read AGENTS.md immediately before GitHub calls. Read PR #224 live, Plan 07's tested head, Release
    Please's gate/publish dependency chain, the post-publish verifier contract, and the public Phase
    242 remediation evidence. Do not trust a prior summary when live state can be re-read.
  </read_first>
  <action>
    Resolve PR #224 head/base live and require the head to equal Plan 07's tested SHA, the corrective
    main merge to be an ancestor, and `mergeStateStatus` not behind/dirty/blocked. Re-run candidate
    mode locally at that exact checkout and `MIX_ENV=test mix ci`. Inspect the public Hex API before
    release: normally 1.5.1 must be absent; if present, require a matching GitHub tag/source SHA and
    treat the operation as already crossed rather than attempting a duplicate.

    Inspect GitHub REST rate limit once. Locate the exact-head `ci.yml` run. If active, use one
    `gh run watch` with compact output and 60-second interval; fetch the structured summary once at
    completion and failed logs only for failure. Require successful run conclusion and successful
    `ci-gate`, plus successful Phase 242 contract/prohibition execution. Retry a transient runner
    failure once; deterministic or rate-limited failures block readiness.

    Post one sanitized fenced JSON comment to PR #224 with marker
    `sigra.phase-242-release-readiness/1`: candidate/base/corrective SHAs, CI run ID/URL/conclusion,
    contract verdicts, changelog/index versions, remediation evidence path/run URL, public pre-release
    Hex observation, and capture time. Exclude logs, headers, environment values, and credentials.
    Re-read the comment and live PR head; if the head changed, invalidate the receipt and repeat the
    proof for the new head before reaching Task 2.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; pr=$(gh pr view 224 --repo szTheory/sigra --json state,headRefOid,mergeStateStatus,comments); test "$(jq -r .state <<<"$pr")" = OPEN; sha=$(jq -r .headRefOid <<<"$pr"); jq -e --arg sha "$sha" '"'"'[.comments[].body | select(contains("sigra.phase-242-release-readiness/1") and contains($sha))] | length >= 1'"'"' <<<"$pr" >/dev/null'</automated>
    <fails_when>The PR head/base is stale or blocked, exact-head CI/ci-gate is not successful, candidate contracts are red, 1.5.1 exists with mismatched provenance, the receipt leaks sensitive data, or the head changes after receipt capture.</fails_when>
  </verify>
  <acceptance_criteria>
    - The exact current PR head has local and remote green proof.
    - Every D-08/D-09 prerequisite is machine-readable in one public readiness comment.
    - No merge, tag creation, or package publish has occurred from this plan.
  </acceptance_criteria>
  <done>The immutable candidate is fully proven and bound to one SHA, with no automatable question left for the human checkpoint.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize the already-defined immutable 1.5.1 publication</name>
  <read_first>
    Read Task 1's `sigra.phase-242-release-readiness/1` comment and confirm its SHA still equals live
    PR #224 head. This is execution authorization for D-08, not a request to choose another version,
    workflow, or release scope.
  </read_first>
  <decision>Authorize merging the exact proven PR #224 head, which triggers the established Release Please tag and Hex publication path.</decision>
  <context>The source choice is locked by D-08/D-09. Publication of Hex 1.5.1 and tag v1.5.1 is a one-way external event; a later correction requires a new release rather than rewriting this artifact.</context>
  <options>
    <option id="authorize-1.5.1">
      <name>Authorize 1.5.1</name>
      <pros>Publishes the exact green candidate proven in Task 1 through the existing gated path.</pros>
      <cons>The public version and tag become permanent records.</cons>
    </option>
    <option id="hold">
      <name>Hold release</name>
      <pros>Leaves PR #224 open and makes no immutable external change.</pros>
      <cons>REL-06 and Phase 242 remain incomplete until a newly proven head is authorized.</cons>
    </option>
  </options>
  <action>
    Present the exact PR URL, head SHA, CI run URL, remediation evidence link, changelog/index
    verdicts, and public pre-release state from the readiness receipt. Accept only
    `authorize-1.5.1` or `hold`; do not auto-select, reinterpret silence, or offer an alternate
    version/path. Record the choice and exact SHA in `242-08-SUMMARY.md`.
  </action>
  <verify>
    <automated>bash -c 'set -eo pipefail; gh pr view 224 --repo szTheory/sigra --json state,headRefOid --jq '"'"'select(.state == "OPEN") | .headRefOid'"'"' | grep -Eq '"'"'^[0-9a-f]{40}$'"'"''</automated>
    <fails_when>PR #224 is no longer open at the proven SHA, the readiness receipt is stale/missing, or no explicit allowed response is received.</fails_when>
  </verify>
  <acceptance_criteria>
    - A human sees the complete automated proof packet before the irreversible step.
    - Authorization, if granted, names the exact candidate SHA.
    - Holding performs no merge or publication.
  </acceptance_criteria>
  <resume-signal>Reply `authorize-1.5.1` to release the exact proven head, or `hold` to stop with PR #224 open.</resume-signal>
  <done>The one-way publication door has an explicit human decision bound to the exact candidate SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live PR/CI/Hex state → readiness receipt | Multiple external systems are reduced to one authorization packet. |
| Human authorization → immutable release task | A deliberate decision unlocks permanent public mutation. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-242-31 | Spoofing | stale/fabricated readiness | high | mitigate | Re-read PR/CI/Hex live, bind comment to exact SHA/run, invalidate on head movement. |
| T-242-32 | Tampering | premature merge | high | mitigate | No merge capability exercised; blocking-human checkpoint is the only transition. |
| T-242-33 | Repudiation | authorization | high | mitigate | Summary records exact allowed response and SHA; no auto-selection. |
| T-242-34 | Information disclosure | readiness comment | low | accept | Comment contains only public SHAs/URLs/verdicts and excludes logs/env/headers. |
</threat_model>

<artifacts_produced>
- Sanitized exact-head release-readiness PR comment.
- Blocking-human authorization or hold decision recorded against the exact SHA.
</artifacts_produced>

<verification>
Re-read PR #224 head and readiness comment immediately before the checkpoint; no state-changing command follows until explicit authorization.
</verification>

<success_criteria>
All automatable readiness work is complete, and the permanent publication cannot proceed without a human explicitly authorizing the exact proven 1.5.1 head.
</success_criteria>

<output>
Create `.planning/phases/242-hex-retire-docs-revert-pinned-install-adr-cut-1-5-1/242-08-SUMMARY.md` when the checkpoint decision is recorded.
</output>
