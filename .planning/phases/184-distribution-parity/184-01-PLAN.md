---
phase: 184-distribution-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/sigra.install/admin/sigra_admin.css
autonomous: true
requirements:
  - DIST-01

must_haves:
  truths:
    - "The file priv/templates/sigra.install/admin/sigra_admin.css exists and contains the @layer sg-base, sg-components, sg-overrides; declaration and all three @layer sg-* rule blocks"
    - "The extracted file contains zero var(--vt-*) references and zero .vt-* selectors"
    - "The dark-mode @media (prefers-color-scheme: dark) block is present with all --sg-* overrides"
    - "The @media (prefers-reduced-motion: reduce) block is present (it uses --sg-motion-fast and targets .sg-admin-loading-bar)"
    - "All trailing --sg-* component sizing tokens (--sg-pill-h, --sg-pill-gap, etc.) after the vt-* block are included"
    - "The header comment and layer-order rationale are carried over from app.css"
  artifacts:
    - path: "priv/templates/sigra.install/admin/sigra_admin.css"
      provides: "Canonical admin design system; template for all generated hosts"
      contains: "@layer sg-base, sg-components, sg-overrides;"
  key_links:
    - from: "priv/templates/sigra.install/admin/sigra_admin.css"
      to: "test/example/priv/static/assets/css/app.css"
      via: "selective sg-* extraction (Plan 02 copies this to example + fixture)"
      pattern: "--sg-[a-z]"
---

<objective>
Extract the canonical admin sg-* design system from the example app into a single installer template CSS file, leaving the example-only vt-*/Vaultr brand layer in app.css.

Purpose: Close the admin-CSS distribution gap for v1.39 DS-COHERENCE. This single file is the canonical source of truth that the installer ships to every generated host; Plans 02 and 03 depend on this file existing.

Output: priv/templates/sigra.install/admin/sigra_admin.css — a new CSS file extracted from test/example/priv/static/assets/css/app.css, containing all sg-* material and none of the vt-* material.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/184-distribution-parity/184-CONTEXT.md
@.planning/phases/184-distribution-parity/184-RESEARCH.md

<interfaces>
<!-- Extraction source: test/example/priv/static/assets/css/app.css (3,848 lines / 107,242 bytes) -->
<!-- Key structural landmarks (all verified against live file with line citations in RESEARCH.md): -->
<!--   Line 1–13:  Header comment "Vaultr/Sigra demo design layer..." — carry over -->
<!--   Line 15:    @layer sg-base, sg-components, sg-overrides; — CARRY OVER -->
<!--   Lines 20–154:  --sg-* custom properties in :root{} — CARRY OVER -->
<!--   Lines 156–177: --vt-* custom properties in :root{} — DO NOT EXTRACT (interleaved!) -->
<!--   Lines 179–185: trailing --sg-* sizing tokens (--sg-pill-h, --sg-pill-gap, etc.) — CARRY OVER -->
<!--   Line 187:   color-scheme: light; — CARRY OVER (inside :root) -->
<!--   Lines 190–224: @media (prefers-color-scheme: dark) { :root { --sg-* } } — CARRY OVER -->
<!--   Lines 225–242: --vt-* dark overrides inside same @media block — DO NOT EXTRACT -->
<!--   Lines 249–260: @layer sg-base{} (html + :where(a) rules) — CARRY OVER, zero --vt-* -->
<!--   Lines 262–349: @layer sg-components{} sg-* rules ONLY — CARRY OVER -->
<!--   Lines 351–3789: "VAULTR HOST APP" subsection inside @layer sg-components — DO NOT EXTRACT -->
<!--   Lines 3791–3824: @layer sg-overrides{} — CARRY OVER, zero --vt-* -->
<!--   Lines 3831–3848: @media (prefers-reduced-motion: reduce){} — CARRY OVER (uses --sg-motion-fast) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract sg-* CSS into canonical installer template</name>
  <files>priv/templates/sigra.install/admin/sigra_admin.css</files>
  <read_first>
    - test/example/priv/static/assets/css/app.css — the source file (full read required; extraction is selective, not a line-range cut per D-01)
    - priv/templates/sigra.install/core/sigra_auth.css — the auth precedent; observe the header comment convention and layer structure
    - .planning/phases/184-distribution-parity/184-RESEARCH.md § Verification Target 1 — exact line boundaries, landmine warnings, D-03 audit result
  </read_first>
  <behavior>
    - Extracted file contains @layer sg-base, sg-components, sg-overrides; declaration (line 15 of app.css)
    - Extracted file contains all --sg-* properties from :root{} block, including the trailing sizing tokens (--sg-pill-h, --sg-pill-gap, etc. at lines 179–185) that come AFTER the --vt-* block
    - Extracted file does NOT contain any --vt-* property declarations (lines 156–177 and 225–242 are skipped)
    - Extracted file contains the dark-mode @media (prefers-color-scheme: dark) { :root { --sg-* } } block (lines 190–224)
    - Extracted file contains @layer sg-base{} (lines 249–260, html + :where(a) rules)
    - Extracted file contains @layer sg-components{} with ONLY the sg-* selectors (lines 262–349), NOT the VAULTR subsection (lines 351–3789)
    - Extracted file contains @layer sg-overrides{} (lines 3791–3824)
    - Extracted file contains @media (prefers-reduced-motion: reduce){} block (lines 3831–3848)
    - grep -c 'var(--vt-' priv/templates/sigra.install/admin/sigra_admin.css returns 0
    - grep -c '\.vt-' priv/templates/sigra.install/admin/sigra_admin.css returns 0
    - grep -c 'VAULTR HOST APP' priv/templates/sigra.install/admin/sigra_admin.css returns 0
  </behavior>
  <action>
    Create priv/templates/sigra.install/admin/sigra_admin.css by performing a selective property/rule extraction from test/example/priv/static/assets/css/app.css — NOT a contiguous line-range cut (per D-01).

    The critical landmines to avoid (documented in RESEARCH.md § Common Pitfalls):
    1. INTERLEAVED vt-* in :root{}: the --vt-* token block (lines 156–177) is inside the single :root{} block, not in a separate block. Extract only --sg-* declarations from :root, skipping --vt-* lines. Do the same for the dark-mode :root block (skip lines 225–242).
    2. TRAILING --sg-* tokens: lines 179–185 (--sg-pill-h, --sg-pill-gap, etc.) come AFTER the --vt-* block and must be included.
    3. VAULTR subsection: the comment "VAULTR HOST APP" at app.css:351 begins the excluded section inside @layer sg-components{}; extract only lines 262–349 for the sg-* component rules, then close the @layer sg-components{} block before the VAULTR content.
    4. prefers-reduced-motion: the block at lines 3831–3848 is outside all @layer blocks; include it.

    Carry over from app.css: the header comment (lines 1–13), the layer declaration (line 15), the :root{} block with sg-* only, the dark-mode :root block with sg-* only, @layer sg-base{}, @layer sg-components{} (sg-* rules only), @layer sg-overrides{}, and the reduced-motion block.

    After writing the file, run the D-03 dependency audit commands in acceptance_criteria to confirm zero vt-* and zero daisyUI variable references.
  </action>
  <verify>
    <automated>
      grep -c 'var(--vt-' priv/templates/sigra.install/admin/sigra_admin.css || echo "PASS: 0 vt-* refs"
      grep -c '\.vt-' priv/templates/sigra.install/admin/sigra_admin.css || echo "PASS: 0 .vt-* selectors"
      grep -c 'VAULTR' priv/templates/sigra.install/admin/sigra_admin.css || echo "PASS: 0 VAULTR references"
      grep -c '@layer sg-base, sg-components, sg-overrides;' priv/templates/sigra.install/admin/sigra_admin.css
      grep -c '@layer sg-base' priv/templates/sigra.install/admin/sigra_admin.css
      grep -c '@layer sg-components' priv/templates/sigra.install/admin/sigra_admin.css
      grep -c '@layer sg-overrides' priv/templates/sigra.install/admin/sigra_admin.css
      grep -c 'prefers-reduced-motion' priv/templates/sigra.install/admin/sigra_admin.css
      grep -c 'prefers-color-scheme: dark' priv/templates/sigra.install/admin/sigra_admin.css
      grep -c '\-\-sg-pill-h' priv/templates/sigra.install/admin/sigra_admin.css
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'var(--vt-' priv/templates/sigra.install/admin/sigra_admin.css returns 0 (D-03 audit passes)
    - grep -c '\.vt-' priv/templates/sigra.install/admin/sigra_admin.css returns 0
    - grep -c 'VAULTR' priv/templates/sigra.install/admin/sigra_admin.css returns 0
    - grep -c '@layer sg-base, sg-components, sg-overrides;' priv/templates/sigra.install/admin/sigra_admin.css returns 1
    - grep -c '@layer sg-base' priv/templates/sigra.install/admin/sigra_admin.css returns at least 1
    - grep -c '@layer sg-components' priv/templates/sigra.install/admin/sigra_admin.css returns at least 1
    - grep -c '@layer sg-overrides' priv/templates/sigra.install/admin/sigra_admin.css returns at least 1
    - grep -c 'prefers-reduced-motion' priv/templates/sigra.install/admin/sigra_admin.css returns at least 1 (block included)
    - grep -c 'prefers-color-scheme: dark' priv/templates/sigra.install/admin/sigra_admin.css returns at least 1 (dark block included)
    - grep -c '\-\-sg-pill-h' priv/templates/sigra.install/admin/sigra_admin.css returns at least 1 (trailing sizing tokens included)
    - File exists at priv/templates/sigra.install/admin/sigra_admin.css
  </acceptance_criteria>
  <done>priv/templates/sigra.install/admin/sigra_admin.css exists; zero var(--vt-*) references; zero .vt-* selectors; all three @layer sg-* blocks present; dark-mode block present; reduced-motion block present; trailing sizing tokens present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.css → sigra_admin.css | CSS source transformation; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-SC | Tampering | CSS extraction (no npm/pip/cargo installs) | accept | No package manager installs in this plan; pure file creation from existing source |

CSS asset distribution — no ASVS category applies. This plan creates a static CSS file from an existing CSS source. There is no user input, no authentication, no session, no cryptographic operation, and no secret. The only relevant check is that the extracted file contains no embedded secrets/config — which is not a risk for a pure CSS design system derived from sg-* rules.
</threat_model>

<verification>
After Plan 01 completes, all of the following must be true before Plans 02+03 proceed:

1. priv/templates/sigra.install/admin/sigra_admin.css exists
2. grep -c 'var(--vt-' priv/templates/sigra.install/admin/sigra_admin.css returns 0
3. grep -c '@layer sg-base, sg-components, sg-overrides;' priv/templates/sigra.install/admin/sigra_admin.css returns 1
4. grep -c 'prefers-color-scheme: dark' priv/templates/sigra.install/admin/sigra_admin.css returns ≥ 1
5. grep -c 'prefers-reduced-motion' priv/templates/sigra.install/admin/sigra_admin.css returns ≥ 1
</verification>

<success_criteria>
- priv/templates/sigra.install/admin/sigra_admin.css created with selective sg-* extraction
- D-03 dependency audit clean: zero var(--vt-*), zero .vt-* selectors, zero VAULTR references
- All structural components present: layer declaration, :root sg-* (including trailing sizing tokens), dark-mode block, @layer sg-base/sg-components/sg-overrides, reduced-motion block
- Wave 2 plans (184-02, 184-03) can proceed with the canonical template in place
</success_criteria>

<output>
Create .planning/phases/184-distribution-parity/184-01-SUMMARY.md when done
</output>
