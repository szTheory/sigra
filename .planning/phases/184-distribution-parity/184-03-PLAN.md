---
phase: 184-distribution-parity
plan: 03
type: execute
wave: 3
depends_on:
  - "184-01"
  - "184-02"
files_modified:
  - test/example/priv/playwright/tests/admin-generated.spec.ts
autonomous: true
requirements:
  - DIST-06

must_haves:
  truths:
    - "test/example/priv/playwright/tests/admin-generated.spec.ts contains a computed-style assertion proving --sg-color-brand is defined on the document root with value #c2410c"
    - "The styled assertion lives within the existing desktop shell-render test block, after the page.goto('/admin') call"
    - "All pre-existing shell/scope/denial/CSV/impersonation assertions in admin-generated.spec.ts remain intact"
    - "The CI generated_admin_playwright_smoke job (ci.yml:952) via scripts/ci/admin-acceptance-smoke.sh --test all runs the styled assertion against a freshly generated host that has sigra_admin.css installed (proving DIST-02+03+06 together)"
    - "scripts/ci/snapshot-canary-guard.sh (empty allowlist) stays green — the extraction is a visual no-op and no new allowlist entry is needed"
  artifacts:
    - path: "test/example/priv/playwright/tests/admin-generated.spec.ts"
      provides: "DIST-06 styled assertion proving sigra_admin.css loaded and sg-* :root block parsed in a generated host"
  key_links:
    - from: "test/example/priv/playwright/tests/admin-generated.spec.ts"
      to: "priv/static/assets/sigra_admin.css"
      via: "getComputedStyle(document.documentElement).getPropertyValue('--sg-color-brand').trim() === '#c2410c'"
      pattern: "sg-color-brand"
---

<objective>
Add a merge-blocking styled assertion to the existing generated-host Playwright spec, proving that sigra_admin.css loaded and the sg-* :root token block was parsed in a freshly generated host — and verify the snapshot canary stays green.

Purpose: DIST-06 closes the final gap. Plans 01+02 ship and link sigra_admin.css into the installer pipeline; Plan 03 proves the end result: a generated host actually renders a styled admin UI. The styled assertion is the only change; the CI job, script, and Playwright project already exist.

Output: admin-generated.spec.ts with one new CSS-custom-property assertion. The existing --test all CI job picks it up automatically on the next run.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/184-distribution-parity/184-CONTEXT.md
@.planning/phases/184-distribution-parity/184-RESEARCH.md
@.planning/phases/184-distribution-parity/184-01-SUMMARY.md
@.planning/phases/184-distribution-parity/184-02-SUMMARY.md

<interfaces>
<!-- Current spec: test/example/priv/playwright/tests/admin-generated.spec.ts -->
<!-- Current assertions (all must be preserved per RESEARCH.md § Verification Target 4): -->
<!--   - Shell renders on desktop and mobile (scope labels, nav presence, screenshots) -->
<!--   - Admin denial responses show explicit copy (403/404 + body text) -->
<!--   - Global users index lists users for platform admin -->
<!--   - Bounded enterprise surface renders -->
<!--   - Audit CSV export returns stable columns -->
<!--   - Impersonation start flow works -->
<!-- No styled assertion exists today — the generated host currently links no admin CSS -->
<!-- (DIST-03 not yet wired when spec was written). -->

<!-- Recommended styled assertion (RESEARCH.md § Verification Target 4 + D-07): -->
<!--   const brandColor = await page.evaluate( -->
<!--     () => getComputedStyle(document.documentElement) -->
<!--            .getPropertyValue('--sg-color-brand').trim() -->
<!--   ); -->
<!--   expect(brandColor).toBe('#c2410c'); -->
<!--   // Proves the :root token block from sigra_admin.css was parsed; -->
<!--   // --sg-color-brand is defined in sigra_admin.css :root and has no fallback in default.css. -->

<!-- Placement: inside the existing "generated host admin shell renders on desktop and mobile" test, -->
<!-- after await page.goto("/admin") and the shell-visible assertion, before the screenshot. -->

<!-- Snapshot canary: scripts/ci/snapshot-canary-guard.sh (empty allowlist). -->
<!-- Phase 184 is a visual no-op — no new allowlist entry needed if canary stays green. -->
<!-- If a snapshot drifts, the PR must declare the slug in the allowlist. -->

<!-- CI job: .github/workflows/ci.yml:952 — generated_admin_playwright_smoke -->
<!-- Driver: scripts/ci/admin-acceptance-smoke.sh --test all -->
<!-- Note: RUN_PARITY=1 does NOT exist in admin-acceptance-smoke.sh (RESEARCH correction of CONTEXT). -->
<!-- The --test all flag is what exercises admin-generated.spec.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DIST-06 styled assertion to admin-generated.spec.ts</name>
  <files>test/example/priv/playwright/tests/admin-generated.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/admin-generated.spec.ts — full current content; identify the "generated host admin shell renders on desktop and mobile" test block; understand exact insertion point (after goto + shell visible, before screenshot)
    - scripts/ci/admin-acceptance-smoke.sh — confirm --test all target and that RUN_PARITY=1 does not exist (RESEARCH correction); do NOT add RUN_PARITY=1 to the script
    - .planning/phases/184-distribution-parity/184-RESEARCH.md § Verification Target 4 — the exact TypeScript assertion shape and rationale for using --sg-color-brand over .sg-admin-topbar background-color
  </read_first>
  <action>
    In the existing "generated host admin shell renders on desktop and mobile" test within admin-generated.spec.ts, add the DIST-06 styled assertion per D-07 after the page.goto("/admin") call and the shell-visible check, before the screenshot capture.

    The assertion reads the --sg-color-brand CSS custom property from the document root via getComputedStyle(document.documentElement).getPropertyValue('--sg-color-brand').trim() and asserts it equals '#c2410c'. This is the most brittle-resistant signal (per RESEARCH recommendation): it directly proves the :root token block from sigra_admin.css was parsed by the browser, with no dependency on element layout or positioning.

    Add an inline comment: "DIST-06: proves sigra_admin.css :root token block loaded (--sg-color-brand is defined only in sigra_admin.css, not in default.css or app.css)".

    Do NOT: add a new Playwright project, add a new CI job, add RUN_PARITY=1 to admin-acceptance-smoke.sh, or modify .github/workflows/ci.yml. The existing generated_admin_playwright_smoke job at ci.yml:952 picks up the new assertion automatically via --test all.

    Preserve ALL existing assertions in the file — the denial/scope/CSV/impersonation tests must remain intact.
  </action>
  <verify>
    <automated>
      grep -c 'sg-color-brand' test/example/priv/playwright/tests/admin-generated.spec.ts
      grep -c 'DIST-06' test/example/priv/playwright/tests/admin-generated.spec.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'sg-color-brand' test/example/priv/playwright/tests/admin-generated.spec.ts returns 1
    - grep -c 'DIST-06' test/example/priv/playwright/tests/admin-generated.spec.ts returns 1
    - grep -c '#c2410c' test/example/priv/playwright/tests/admin-generated.spec.ts returns at least 1
    - All pre-existing test descriptions still present (shell/scope/denial/CSV/impersonation assertions not removed)
    - No modifications to scripts/ci/admin-acceptance-smoke.sh or .github/workflows/ci.yml
    - mix test (full ExUnit suite) passes (no TS compilation issue possible but confirm no files accidentally touched)
  </acceptance_criteria>
  <done>admin-generated.spec.ts has the --sg-color-brand computed-style assertion inside the desktop shell test; all existing assertions intact; no CI/script changes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify snapshot canary stays green (D-11 visual no-op confirmation)</name>
  <files></files>
  <read_first>
    - scripts/ci/snapshot-canary-guard.sh — understand the allowlist mechanism and canary slug (impersonation-banner)
    - test/example/priv/playwright/snapshot-allowlist — confirm currently empty (comments only)
    - .planning/phases/184-distribution-parity/184-RESEARCH.md § Verification Target 5 — cascade-layer analysis confirming no visual delta is expected
  </read_first>
  <action>
    Run the snapshot canary guard locally against the current state to confirm it passes with an empty allowlist.

    Command: bash scripts/ci/snapshot-canary-guard.sh

    Expected outcome: PASS — no snapshot changes detected, canary (impersonation-banner slug) is unchanged.

    If the canary FAILS (unexpected): investigate whether the app.css reduction in Plan 02 changed any computed style that the Playwright screenshots depend on. The extraction is designed to be a visual no-op (same sg-* rules, relocated); any visual delta would indicate a missed sg-* rule in the extraction.

    If a snapshot slug changed (non-canary): add that slug to test/example/priv/playwright/snapshot-allowlist with the comment "Phase 184 D-11: extraction visual delta (expected none)" and document the finding in the phase summary. A blank allowlist line from a visual no-op is the expected steady state.

    Do NOT re-capture baselines — the extraction is a visual no-op; if it is not, that is a bug in Plan 01's extraction, not a baseline to update.
  </action>
  <verify>
    <automated>bash scripts/ci/snapshot-canary-guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/ci/snapshot-canary-guard.sh exits 0 (PASS) with empty allowlist
    - test/example/priv/playwright/snapshot-allowlist remains empty (comments only) — no new allowlist entry needed for a visual no-op
    - If any snapshot changed: executor must stop and diagnose before adding allowlist entries; the phase spec guarantees a visual no-op
  </acceptance_criteria>
  <done>Snapshot canary PASS with empty allowlist; D-11 visual no-op confirmed; no baseline recapture needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test → generated host | E2E test reads computed styles; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; single TypeScript file edit + bash script invocation |

CSS asset distribution — no ASVS category applies. This plan adds a read-only computed-style assertion to a Playwright test. The assertion reads a CSS custom property; no user input is processed, no auth is involved, and no data is mutated.
</threat_model>

<verification>
Phase 184 is complete when all of the following are true:

ExUnit gates:
1. mix test test/sigra/install/features/admin_test.exs passes (DIST-02, DIST-03, DIST-05 assertions green)
2. mix test test/sigra/install/golden_diff_test.exs --only golden passes (template≡fixture)
3. mix test (full suite) passes

CLI D-03 audit:
4. grep -c 'var(--vt-' priv/templates/sigra.install/admin/sigra_admin.css returns 0
5. grep -c '@layer sg-' test/example/priv/static/assets/css/app.css returns 0

Snapshot canary:
6. bash scripts/ci/snapshot-canary-guard.sh exits 0 with empty allowlist

DIST-06 Playwright (CI gate):
7. CI generated_admin_playwright_smoke job green (admin-generated.spec.ts styled assertion passes against freshly generated host)

Structural checks:
8. grep -c 'admin/sigra_admin\.css' lib/sigra/install/features/admin.ex returns 1
9. grep -c 'sigra_admin\.css' priv/templates/sigra.install/admin/layouts_admin_injection.ex returns 1
10. grep -c 'sg-color-brand' test/example/priv/playwright/tests/admin-generated.spec.ts returns 1
</verification>

<success_criteria>
- DIST-06: admin-generated.spec.ts styled assertion proves --sg-color-brand is #c2410c in a generated host
- D-11: snapshot canary passes with empty allowlist (visual no-op confirmed)
- All six DIST-01..06 requirements satisfied across Plans 01-03
- CI generated_admin_playwright_smoke job will pass on next run (cannot run locally without phx_new scaffold, but the assertion is correctly placed for CI to exercise)
</success_criteria>

<output>
Create .planning/phases/184-distribution-parity/184-03-SUMMARY.md when done
</output>
