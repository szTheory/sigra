---
phase: 184-distribution-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "184-01"
files_modified:
  - lib/sigra/install/features/admin.ex
  - priv/templates/sigra.install/admin/layouts_admin_injection.ex
  - test/example/priv/static/assets/sigra_admin.css
  - test/example/priv/static/assets/css/app.css
  - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css
  - test/sigra/install/features/admin_test.exs
autonomous: true
requirements:
  - DIST-02
  - DIST-03
  - DIST-04
  - DIST-05

must_haves:
  truths:
    - "lib/sigra/install/features/admin.ex files/1 includes the tuple {:eex, \"admin/sigra_admin.css\", \"priv/static/assets/sigra_admin.css\"} (per D-08)"
    - "priv/templates/sigra.install/admin/layouts_admin_injection.ex def admin/1 contains <link phx-track-static rel=\"stylesheet\" href={~p\"/assets/sigra_admin.css\"} /> (per D-09)"
    - "test/example/priv/static/assets/sigra_admin.css exists and is byte-identical to priv/templates/sigra.install/admin/sigra_admin.css"
    - "test/example/priv/static/assets/css/app.css no longer contains @layer sg-base, sg-components, sg-overrides; (the sg-* layer declaration moved to sigra_admin.css)"
    - "test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css exists and is byte-identical to the template"
    - "mix test test/sigra/install/features/admin_test.exs passes with new files/1 tuple assertion"
    - "mix test test/sigra/install/features/admin_test.exs passes with new layouts_admin_injection <link> assertion"
    - "mix test test/sigra/install/features/admin_test.exs passes with new DIST-05 example≡template byte-compare describe block"
    - "mix test test/sigra/install/golden_diff_test.exs --only golden passes (fixture includes new sigra_admin.css)"
  artifacts:
    - path: "test/example/priv/static/assets/sigra_admin.css"
      provides: "Byte-guarded example copy (DIST-04); proves example runs against shipped stylesheet"
    - path: "test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css"
      provides: "Golden fixture copy; makes golden_diff_test byte-check template≡fixture automatically"
  key_links:
    - from: "lib/sigra/install/features/admin.ex"
      to: "priv/templates/sigra.install/admin/sigra_admin.css"
      via: "files/1 tuple {:eex, \"admin/sigra_admin.css\", \"priv/static/assets/sigra_admin.css\"}"
      pattern: "admin/sigra_admin\\.css"
    - from: "priv/templates/sigra.install/admin/layouts_admin_injection.ex"
      to: "priv/static/assets/sigra_admin.css"
      via: "<link phx-track-static rel=\"stylesheet\" href={~p\"/assets/sigra_admin.css\"} />"
      pattern: "sigra_admin\\.css"
    - from: "test/sigra/install/features/admin_test.exs"
      to: "priv/templates/sigra.install/admin/sigra_admin.css"
      via: "File.read! byte-compare (DIST-05)"
      pattern: "DIST-05"
---

<objective>
Wire the canonical sigra_admin.css template into the installer pipeline (DIST-02), link it in the generated admin layout (DIST-03), check in the byte-guarded example copy (DIST-04), register the golden fixture copy, and add merge-blocking ExUnit parity tests (DIST-05).

Purpose: After Plan 01 creates the canonical template, this plan completes the full six-step auth-pattern analog: installer ships the file, admin layout links it, example consumes the same copy, and two byte-parity gates make divergence impossible.

Output:
- admin.ex files/1 tuple for sigra_admin.css
- layouts_admin_injection.ex with body-level link tag
- test/example/.../sigra_admin.css checked-in byte-identical copy
- test/example/.../app.css reduced to vt-*-only content
- test/fixtures/.../sigra_admin.css golden fixture copy
- admin_test.exs with files/1, layout injection, and DIST-05 byte-compare assertions
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/184-distribution-parity/184-CONTEXT.md
@.planning/phases/184-distribution-parity/184-RESEARCH.md
@.planning/phases/184-distribution-parity/184-01-SUMMARY.md

<interfaces>
<!-- Auth precedent files — read these to understand the exact pattern to replicate: -->
<!--   lib/sigra/install/features/core.ex:256 — auth files/1 tuple: -->
<!--     {:eex, "core/sigra_auth.css", Path.join(["priv", "static", "assets", "sigra_auth.css"])} -->
<!--   priv/templates/sigra.install/core/sigra_auth_components.ex:27 — auth body-level link: -->
<!--     <link phx-track-static rel="stylesheet" href={~p"/assets/sigra_auth.css"} /> -->
<!--   test/sigra/install/features/core_test.exs:155 — source assert pattern: -->
<!--     assert "core/sigra_auth.css" in sources -->
<!--   test/sigra/install/features/core_test.exs:293 — target assert pattern: -->
<!--     assert "priv/static/assets/sigra_auth.css" in targets -->

<!-- Current admin.ex files/1 shape (DIST-02 target, lib/sigra/install/features/admin.ex:26–43): -->
<!--   Tuples: admin/policy.ex, admin/components/admin_shell.ex, two SVGs, -->
<!--           admin/impersonation_controller.ex, admin/audit_export_controller.ex -->
<!--   No CSS tuple today — add {:eex, "admin/sigra_admin.css", Path.join(["priv","static","assets","sigra_admin.css"])} -->

<!-- Current layouts_admin_injection.ex def admin/1 (DIST-03 target): -->
<!--   ~H""" heredoc with <.admin_shell ...>, {@inner_content}, <.flash_group /> -->
<!--   The <link> tag goes at the TOP of the heredoc, before <.admin_shell> -->

<!-- DIST-05 byte-compare shape (add to admin_test.exs): -->
<!--   describe "DIST-05 example≡template byte-parity (sigra_admin.css)" do -->
<!--     test "example copy is byte-identical to the installer template" do -->
<!--       template = File.read!("priv/templates/sigra.install/admin/sigra_admin.css") -->
<!--       example  = File.read!("test/example/priv/static/assets/sigra_admin.css") -->
<!--       assert byte_size(template) == byte_size(example), "size mismatch — ..." -->
<!--       assert template == example, "content mismatch — example copy has diverged" -->
<!--     end -->
<!--   end -->

<!-- Pitfall (RESEARCH.md § Pitfall 6): -->
<!--   Admin files/1 tuple assertion must use string literal path, not Path.join/1 at assertion time: -->
<!--   {:eex, "admin/sigra_admin.css", "priv/static/assets/sigra_admin.css"} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire admin.ex files/1 (DIST-02) and layouts_admin_injection.ex link (DIST-03)</name>
  <files>lib/sigra/install/features/admin.ex, priv/templates/sigra.install/admin/layouts_admin_injection.ex</files>
  <read_first>
    - lib/sigra/install/features/admin.ex — read the full files/1 list to find the correct append location; the new tuple follows the pattern of the six existing tuples
    - lib/sigra/install/features/core.ex (around line 256) — the exact auth files/1 tuple form to replicate
    - priv/templates/sigra.install/admin/layouts_admin_injection.ex — full current content (22 lines, from RESEARCH.md); add the link inside the ~H""" heredoc before <.admin_shell>
    - priv/templates/sigra.install/core/sigra_auth_components.ex (around line 27) — the exact <link> form to replicate
  </read_first>
  <action>
    In lib/sigra/install/features/admin.ex: append {:eex, "admin/sigra_admin.css", Path.join(["priv", "static", "assets", "sigra_admin.css"])} to the files/1 return list (per D-08). This is the exact analog of core.ex:256 — `:eex` copies verbatim when no EEX markers are present.

    In priv/templates/sigra.install/admin/layouts_admin_injection.ex: add a body-level link tag inside the def admin/1 heredoc, before <.admin_shell>, per D-09. The exact markup is: `<link phx-track-static rel="stylesheet" href={~p"/assets/sigra_admin.css"} />` — identical to the form at sigra_auth_components.ex:27 except for the path. This renders body-level; the host <head> in root.html.heex is NOT modified.
  </action>
  <verify>
    <automated>mix test test/sigra/install/features/admin_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'admin/sigra_admin\.css' lib/sigra/install/features/admin.ex returns 1
    - grep -c 'sigra_admin\.css' priv/templates/sigra.install/admin/layouts_admin_injection.ex returns 1
    - mix test test/sigra/install/features/admin_test.exs passes (existing tests stay green; new assertions added in Task 2 will confirm wiring)
  </acceptance_criteria>
  <done>admin.ex files/1 includes the CSS tuple; layouts_admin_injection.ex def admin/1 includes the stylesheet link; existing admin_test.exs tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Example copy + app.css reduction + golden fixture + DIST-05 parity tests</name>
  <files>
    test/example/priv/static/assets/sigra_admin.css,
    test/example/priv/static/assets/css/app.css,
    test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css,
    test/sigra/install/features/admin_test.exs
  </files>
  <read_first>
    - priv/templates/sigra.install/admin/sigra_admin.css — the canonical template (created in Plan 01); this file is byte-copied to the example and fixture paths
    - test/example/priv/static/assets/css/app.css — current state; remove all @layer sg-base/sg-components/sg-overrides content and the sg-* :root block after extraction, leaving only vt-* material (and the layer declaration if needed for vt-* ordering, but no sg-* rules)
    - test/sigra/install/features/admin_test.exs — full current test file; add new assertions without breaking existing tests
    - test/sigra/install/features/core_test.exs (lines 155, 293) — the sources/targets pattern used in core; use the simpler direct tuple assertion in admin_test.exs (RESEARCH.md recommendation)
    - test/sigra/install/golden_diff_test.exs — understand the whole-tree mechanism; no edits needed to this file, just commit the fixture
    - .planning/phases/184-distribution-parity/184-RESEARCH.md § Verification Target 3 — exact shape of the DIST-05 byte-compare test and the files/1 tuple assertion form
  </read_first>
  <action>
    Four changes in one task to keep all byte-related operations atomic (preventing a commit state where template and example are different sizes):

    1. EXAMPLE COPY (DIST-04, per D-04): Copy priv/templates/sigra.install/admin/sigra_admin.css verbatim to test/example/priv/static/assets/sigra_admin.css. This must be a true byte copy — do not re-generate from app.css, do not paraphrase. Use File.read!/write! or equivalent to ensure byte identity.

    2. APP.CSS REDUCTION (DIST-04, per D-02): Edit test/example/priv/static/assets/css/app.css to remove all sg-* content: remove the @layer sg-base, sg-components, sg-overrides; declaration, the --sg-* :root token block (both light and dark), @layer sg-base{}, @layer sg-components{} (both the sg-* rules AND the VAULTR subsection — the entire block goes), @layer sg-overrides{}, and the prefers-reduced-motion block. What remains is the --vt-* :root tokens, the vt-* dark overrides, and the vt-* selector rules. Verify after: grep -c '@layer sg-' test/example/priv/static/assets/css/app.css should return 0.

    3. GOLDEN FIXTURE COPY (DIST-05 part 1, per D-06): Copy priv/templates/sigra.install/admin/sigra_admin.css verbatim to test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css. This enables golden_diff_test.exs to automatically byte-check template≡fixture (the existing mechanism). No edits to golden_diff_test.exs required.

    4. ADMIN_TEST.EXS ASSERTIONS (DIST-02, DIST-03, DIST-05 part 2): Add three new items to test/sigra/install/features/admin_test.exs:
       a. In the existing "files/1" describe block, add a test asserting {:eex, "admin/sigra_admin.css", "priv/static/assets/sigra_admin.css"} in Admin.files([]) (use string literals, not Path.join — RESEARCH.md Pitfall 6).
       b. In the existing "injections/1" describe block (or a new describe "layouts_admin_injection template"), add an assertion that layouts_admin.content contains ~p"/assets/sigra_admin.css" or the <link> markup with sigra_admin.css.
       c. Add a new describe "DIST-05 example≡template byte-parity (sigra_admin.css)" block with a test that File.read!s both paths and asserts byte_size equal and content equal, with a helpful error message showing the cp command to resync.
  </action>
  <verify>
    <automated>
      mix test test/sigra/install/features/admin_test.exs
      mix test test/sigra/install/golden_diff_test.exs --only golden
      grep -c '@layer sg-' test/example/priv/static/assets/css/app.css
    </automated>
  </verify>
  <acceptance_criteria>
    - test/example/priv/static/assets/sigra_admin.css exists and is byte-identical to priv/templates/sigra.install/admin/sigra_admin.css
    - test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css exists and is byte-identical to the template
    - grep -c '@layer sg-' test/example/priv/static/assets/css/app.css returns 0 (app.css reduced to vt-* only)
    - mix test test/sigra/install/features/admin_test.exs passes (all existing tests plus new files/1 + layout + DIST-05 assertions)
    - mix test test/sigra/install/golden_diff_test.exs --only golden passes (fixture matches newly generated file)
    - mix test (full suite) passes
  </acceptance_criteria>
  <done>Example copy checked in; app.css reduced to vt-* only; golden fixture registered; admin_test.exs has files/1, layout injection, and DIST-05 byte-compare assertions; all three test targets green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| priv/templates → generated host | Installer copies CSS verbatim; no user input crosses this boundary |
| test/example → Playwright | Example serves the checked-in CSS; no runtime generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; pure Elixir/CSS file modifications |

CSS asset distribution — no ASVS category applies. This plan wires an existing CSS file into the installer pipeline and adds ExUnit byte-compare tests. No user input, auth, session, or crypto surface is involved. The installer tuple copies the file verbatim; the <link> tag is static markup. Secrets audit: sigra_admin.css is a pure CSS design system — no application secrets or config can be embedded.
</threat_model>

<verification>
After Plan 02 completes, all of the following must be true before Plan 03 (Wave 3) proceeds:

1. mix test test/sigra/install/features/admin_test.exs passes
2. mix test test/sigra/install/golden_diff_test.exs --only golden passes
3. mix test (full suite) passes
4. grep -c 'admin/sigra_admin\.css' lib/sigra/install/features/admin.ex returns 1
5. grep -c 'sigra_admin\.css' priv/templates/sigra.install/admin/layouts_admin_injection.ex returns 1
6. wc -c test/example/priv/static/assets/sigra_admin.css == wc -c priv/templates/sigra.install/admin/sigra_admin.css (byte sizes identical)
7. grep -c '@layer sg-' test/example/priv/static/assets/css/app.css returns 0
</verification>

<success_criteria>
- DIST-02: admin.ex files/1 tuple present and tested
- DIST-03: layouts_admin_injection.ex <link> present and tested
- DIST-04: example copy exists, app.css reduced to vt-* only
- DIST-05 (template≡example): byte-compare test in admin_test.exs is merge-blocking
- DIST-05 (template≡fixture): golden fixture registered, golden_diff_test passes
- Full suite green; no regressions
</success_criteria>

<output>
Create .planning/phases/184-distribution-parity/184-02-SUMMARY.md when done
</output>
