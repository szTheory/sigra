---
phase: 248-crosswake-native-proof
plan: "02"
type: execute
wave: 2
depends_on: [248-01]
files_modified:
  - scripts/ci/lib/native-proof-receipt.mjs
  - scripts/ci/lib/native-proof-receipt.test.mjs
  - scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs
  - test/fixtures/prohibitions/p17-crosswake-native-boundary-bad.json
  - test/fixtures/prohibitions/p17-crosswake-native-boundary-clean.json
  - test/example/priv/native-fixtures/native-proof-status.json
autonomous: true
requirements: [XW-01, NAT-01, NAT-02]
must_haves:
  truths:
    - "D-12/D-13/D-15: each native claim is accepted only from a complete, target-correct, source-bound, cleanup-complete, secret-scan-clean terminal receipt."
    - "D-09: retained evidence exposes credential-store posture only, never credential or stable identity values."
  artifacts:
    - path: scripts/ci/lib/native-proof-receipt.mjs
      provides: Shared allowlist schema and fail-closed receipt validator
      exports: [TARGET_CLASSES, REQUIRED_SCENARIOS, validateNativeReceipt, writeNativeReceiptLast]
    - path: scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs
      provides: Non-vacuous authority/secret/claim guard
  key_links:
    - from: scripts/ci/lib/native-proof-receipt.mjs
      to: scripts/ci/lib/exact-sha-worktree.sh
      via: source SHA supplied by each platform orchestrator before atomic write
  assumptions:
    - statement: "NAT-01 edge classification is intentionally unclassified pending its terminal physical-device receipt; validator fails closed on any missing physical-device field."
      status: proof-pending
      flagged: true
    - statement: "NAT-02 edge classification is intentionally unclassified pending its terminal emulator receipt; validator fails closed on any missing emulator/toolchain field."
      status: proof-pending
      flagged: true
  prohibitions:
    - statement: "NAT-01 must not label simulator evidence or controlled transport failure as physical radio-disconnection proof."
      status: unverified
      flagged: true
    - statement: "NAT-02 must not label emulator evidence as physical Android or hardware-backed key attestation."
      status: unverified
      flagged: true
---

<objective>
Create the shared fail-closed evidence contract both platform lanes must satisfy before either can make a support claim.

Purpose: Make evidence truth, redaction, cleanup, and receipt ordering reusable and deterministic.
Output: Receipt validator/writer, fixtures, and non-vacuous native-boundary guard.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-RESEARCH.md
@.planning/phases/248-crosswake-native-proof/248-01-SUMMARY.md
@scripts/ci/hosted-session-interop-proof.sh
@scripts/ci/lib/exact-sha-worktree.sh
@scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and prove the native receipt-last schema</name>
  <files>scripts/ci/lib/native-proof-receipt.mjs, scripts/ci/lib/native-proof-receipt.test.mjs, test/example/priv/native-fixtures/native-proof-status.json</files>
  <read_first>scripts/ci/lib/native-proof-receipt.mjs; scripts/ci/lib/native-proof-receipt.test.mjs; test/example/priv/native-fixtures/native-proof-status.json; scripts/ci/hosted-session-interop-proof.sh; scripts/ci/lib/exact-sha-worktree.sh; .planning/phases/247-language-learning-digital-twin/247-EVIDENCE.json; .planning/phases/248-crosswake-native-proof/248-CONTEXT.md; .planning/phases/248-crosswake-native-proof/248-VALIDATION.md</read_first>
  <behavior>
    - Only `physical_iphone` and `android_emulator` target classes validate.
    - Missing source binding, exact target identity, required scenario, cleanup, secret scan, artifact hash, or terminal completion rejects.
    - iPhone transport evidence accepts only the truthful `controlled_transport_failure` label; Android requires verified all-transport disablement.
  </behavior>
  <action>Implement `TARGET_CLASSES`, `REQUIRED_SCENARIOS`, `validateNativeReceipt`, and `writeNativeReceiptLast` with one exact allowlist per D-09 and D-12 through D-15. The shared top-level fields are `schema_version`, `implementation_sha`, `target_class`, `target_identity`, `toolchain`, `browser`, `callback`, `storage`, `scenarios`, `transport`, `artifact_hashes`, `cleanup_status`, `secret_scan_status`, and `terminal_status`; require `schema_version: native-proof-receipt/1`. Require `target_class` in `physical_iphone|android_emulator`; `callback` fields `transport`, `link_verification`, `callback_binding` with values `custom_scheme`, `registered_scheme`, `matched`; `storage` fields `present`, `rotated`, `recovered_after_relaunch`, `deleted_after_logout`, `deleted_after_revocation`, `read_result`, `access_persisted`, where the first five and `access_persisted` are booleans, `access_persisted` is always false, and `read_result` is one of `read_ok|not_found|decrypt_failed|key_unavailable`; `scenarios` fields `hosted_return`, `image_verified`, `audio_verified`, `strict_lease_edge`, `offline_use`, `kill_relaunch`, `account_switch`, `server_revocation`, `replay_accepted`, `replay_rejected`, `replay_conflict`, all true at terminal validation; `cleanup_status: complete`, `secret_scan_status: clean`, and `terminal_status: complete`. Define target-specific keys exactly: iOS `target_identity.{platform,model_class,os_version,physical}`, `toolchain.{xcode_version,xcode_build}`, `browser.{component,version,mode}`, `transport.{claim}`, and `artifact_hashes.{app_bundle_sha256,xctest_bundle_sha256,diagnostics_sha256}`, where claim is only `controlled_transport_failure`; Android `target_identity.{platform,avd_device,api,abi,emulated}`, `toolchain.{jdk,cmdline_tools,platform_tools,emulator,sdk_platform,build_tools,system_image,system_image_revision,gradle,agp,kotlin,androidx_browser,test_core,test_runner,espresso,uiautomator}`, `browser.{component,version,apk_sha256,mode}`, `transport.{wifi_disabled,cellular_disabled,emulator_network_disabled,force_stop,cold_start}`, and `artifact_hashes.{app_apk_sha256,test_apk_sha256,diagnostics_sha256}`. Reject unknown keys recursively, non-SHA hashes, secret/stable-identity-shaped values, and incompatible target fields. Make the writer accept an already-bound SHA and atomically publish only validated JSON. Add clean/bad fixtures for every enum and target-specific branch.</action>
  <acceptance_criteria>Node tests reject wrong target, simulator substitution, false iPhone radio claim, incomplete scenario set, source mismatch, cleanup failure, secret finding, unknown keys, and pre-terminal writes; clean platform fixtures validate with no secret-shaped values.</acceptance_criteria>
  <verify><automated>node --test scripts/ci/lib/native-proof-receipt.test.mjs</automated></verify>
  <done>The common receipt can represent both claims precisely and cannot green an incomplete or overstated claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the Crosswake/native authority and retained-secret prohibition</name>
  <files>scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs, test/fixtures/prohibitions/p17-crosswake-native-boundary-bad.json, test/fixtures/prohibitions/p17-crosswake-native-boundary-clean.json</files>
  <read_first>scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs; test/fixtures/prohibitions/p17-crosswake-native-boundary-bad.json; test/fixtures/prohibitions/p17-crosswake-native-boundary-clean.json; scripts/ci/prohibitions/p14-crosswake-authority-secrets.test.mjs; test/fixtures/prohibitions/p14-crosswake-authority-secrets-bad.json; test/fixtures/prohibitions/p14-crosswake-authority-secrets-clean.json; test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/telemetry.ex; test/example/lib/example/accounts/crosswake_native_bridge.ex</read_first>
  <behavior>
    - Real repository sources derive non-empty facts and pass.
    - Injected known-bad facts fail; injected known-clean facts pass.
    - The guard detects companion authority, sensitive evidence/status fields, embedded browser/direct password paths, and target-class overclaim markers.
  </behavior>
  <action>Mirror the P14 real/bad/clean subject-injection pattern. Derive facts from the bridge, released telemetry allowlist, shared receipt module, fixture status schema, and—when created—the two native source roots. Keep fact parsing strict and non-vacuous. This closes the D-03/D-04/D-05/D-09 boundaries mechanically without placing credentials in the fixtures.</action>
  <acceptance_criteria>Real source run passes; `GSD_PROHIB_SUBJECT` bad fixture run fails; clean fixture run passes; absent or malformed injected facts fail rather than defaulting to zero.</acceptance_criteria>
  <verify><automated>node --test scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs &amp;&amp; ! GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p17-crosswake-native-boundary-bad.json node --test scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs &amp;&amp; GSD_PROHIB_SUBJECT=test/fixtures/prohibitions/p17-crosswake-native-boundary-clean.json node --test scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs</automated></verify>
  <done>The shared safety boundary has machine-proven red/green causation and no vacuous pass path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| platform test output → retained receipt | Untrusted logs/status become a durable support claim. |
| repository source → prohibition facts | Guard must derive real, non-empty facts without trusting comments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-04 | Tampering/Repudiation | receipt validator | high | mitigate | Exact schema, source hashes, target class, required scenario matrix, atomic terminal write, negative fixtures. |
| T-248-05 | Information disclosure | status/evidence | high | mitigate | Allowlisted posture-only fields, unknown-key rejection, post-artifact secret scan requirement. |
| T-248-06 | Spoofing | target identity | high | mitigate | Reject simulator/emulator substitutions and unverified identity fields by target-specific validation. |
| T-248-SC | Tampering | package installs | low | accept | No npm/pip/cargo install occurs; existing Node runtime only. |
</threat_model>

## Artifacts this phase produces

- `scripts/ci/lib/native-proof-receipt.mjs` with `TARGET_CLASSES`, `REQUIRED_SCENARIOS`, `validateNativeReceipt`, `writeNativeReceiptLast`.
- Shared receipt top-level allowlist: `schema_version: native-proof-receipt/1`, `implementation_sha`, `target_class`, `target_identity`, `toolchain`, `browser`, `callback`, `storage`, `scenarios`, `transport`, `artifact_hashes`, `cleanup_status`, `secret_scan_status`, `terminal_status`.
- Shared status fields/enums: callback `{transport: custom_scheme, link_verification: registered_scheme, callback_binding: matched}`; storage `{present, rotated, recovered_after_relaunch, deleted_after_logout, deleted_after_revocation, read_result: read_ok|not_found|decrypt_failed|key_unavailable, access_persisted: false}`; all eleven named scenario booleans true; cleanup `complete`; secret scan `clean`; terminal `complete`.
- iOS-only allowlist: target `{platform, model_class, os_version, physical}`, toolchain `{xcode_version, xcode_build}`, browser `{component, version, mode}`, transport `{claim: controlled_transport_failure}`, artifact hashes `{app_bundle_sha256, xctest_bundle_sha256, diagnostics_sha256}`.
- Android-only allowlist: target `{platform, avd_device, api, abi, emulated}`, toolchain `{jdk, cmdline_tools, platform_tools, emulator, sdk_platform, build_tools, system_image, system_image_revision, gradle, agp, kotlin, androidx_browser, test_core, test_runner, espresso, uiautomator}`, browser `{component, version, apk_sha256, mode}`, transport `{wifi_disabled, cellular_disabled, emulator_network_disabled, force_stop, cold_start}`, artifact hashes `{app_apk_sha256, test_apk_sha256, diagnostics_sha256}`.
- `scripts/ci/lib/native-proof-receipt.test.mjs`.
- `scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs`.
- `test/fixtures/prohibitions/p17-crosswake-native-boundary-{bad,clean}.json`.
- `test/example/priv/native-fixtures/native-proof-status.json`.

<verification>Run both Node commands and require the expected real-green/bad-red/clean-green sequence.</verification>
<success_criteria>Neither platform lane can retain a credential, misstate its execution class, omit a required scenario, or write evidence before cleanup and validation.</success_criteria>
<output>Create `.planning/phases/248-crosswake-native-proof/248-02-SUMMARY.md` when done.</output>
