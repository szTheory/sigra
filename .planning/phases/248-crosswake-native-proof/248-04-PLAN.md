---
phase: 248-crosswake-native-proof
plan: "04"
type: execute
wave: 4
depends_on: [248-03]
files_modified:
  - scripts/ci/crosswake-native-ios-proof.sh
  - scripts/ci/crosswake-native-ios-proof.test.sh
  - .planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json
autonomous: true
requirements: [NAT-01]
user_setup:
  - service: physical-ios-runner
    why: NAT-01 requires automated XCUITest on an attached physical iPhone; simulator evidence is rejected.
    env_vars:
      - name: SIGRA_IOS_DEVICE_UDID
        source: Attached-device runner or device-lab destination identifier
      - name: SIGRA_IOS_DEVELOPMENT_TEAM
        source: Apple signing configuration available to the runner
    dashboard_config:
      - task: Ensure the device is paired, trusted, unlocked for automation, and the proof app can be signed/installed.
        location: Attached macOS runner or device-lab configuration
must_haves:
  truths:
    - "D-10: NAT-01 receipt proves an exact physical iPhone destination and rejects every simulator destination."
    - "D-12/D-13: the complete iPhone scenario matrix, cleanup, source hashes, and secret scan pass before the receipt is written."
    - "D-14: offline evidence is truthfully labeled controlled_transport_failure and never physical radio-off."
  artifacts:
    - path: scripts/ci/crosswake-native-ios-proof.sh
      provides: Bounded physical-device orchestration and receipt-last sealing
    - path: .planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json
      provides: Redacted source-bound physical-iPhone receipt
  key_links:
    - from: scripts/ci/crosswake-native-ios-proof.sh
      to: scripts/ci/lib/native-proof-receipt.mjs
      via: validate then atomic terminal write
  assumptions:
    - statement: "NAT-01 unclassified edge remains flagged until a real attached-device receipt passes; no simulator result resolves it."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "NAT-01 must not substitute simulator, browser emulation, or controlled transport failure for the claimed physical/radio evidence class."
      status: unverified
      flagged: true
---

<objective>Execute the full NAT-01 matrix against a receipt-proven physical iPhone and publish the redacted receipt last.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-03-SUMMARY.md
@scripts/ci/hosted-session-interop-proof.sh
@scripts/ci/lib/exact-sha-worktree.sh
@scripts/ci/lib/native-proof-receipt.mjs</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Seal physical-iPhone proof from exact destination and complete scenarios</name>
  <precondition>`SIGRA_IOS_DEVICE_UDID` resolves through `xcrun xctrace list devices` to an attached non-simulator iPhone and signing is available.</precondition>
  <files>scripts/ci/crosswake-native-ios-proof.sh, scripts/ci/crosswake-native-ios-proof.test.sh, .planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json</files>
  <read_first>scripts/ci/crosswake-native-ios-proof.sh; scripts/ci/crosswake-native-ios-proof.test.sh; .planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json; scripts/ci/hosted-session-interop-proof.sh; scripts/ci/lib/exact-sha-worktree.sh; scripts/ci/lib/native-proof-receipt.mjs; test/example/native/ios/SigraNativeProof/UITests/NativeProofUITests.swift; .planning/phases/248-crosswake-native-proof/248-CONTEXT.md</read_first>
  <behavior>
    - Simulator/unresolved/wrong-platform destinations fail before install or test.
    - Bounded XCUITest completes every scenario with stable readiness, then app/artifacts are cleaned and scanned.
    - Receipt is the sole allowed worktree change and is written after unchanged-SHA revalidation.
  </behavior>
  <action>Implement a `set -euo pipefail` runner with bounded commands, trap cleanup, physical destination resolution, redacted device class/model/OS capture, exact Xcode/component versions, test host readiness, `xcodebuild test` for `NativeProofUITests`, controlled transport-failure fixture, app uninstall and test-data cleanup, artifact/source SHA-256 validation, and secret scan. Bind a clean SHA before execution, require every D-12 scenario flag and D-10/D-14 target/transport claim, reassert the same SHA, then invoke `writeNativeReceiptLast` for `248-IOS-EVIDENCE.json`. Add hermetic script tests that inject fake `xcrun/xcodebuild` outputs to prove simulator rejection and receipt ordering without a device.</action>
  <acceptance_criteria>Hermetic tests prove target fail-closed and receipt-last ordering; the real command exits zero only for a physical iPhone and produces a schema-valid receipt with all scenarios true, cleanup true, secret scan clean, and exact source hashes.</acceptance_criteria>
  <verify><automated>bash scripts/ci/crosswake-native-ios-proof.test.sh &amp;&amp; scripts/ci/crosswake-native-ios-proof.sh &amp;&amp; node scripts/ci/lib/native-proof-receipt.mjs --validate .planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json --target physical_iphone</automated></verify>
  <done>A real automated physical-iPhone receipt—not simulator evidence—proves NAT-01 completely.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| runner/device discovery → support claim | Tool output must prove physical destination without retaining stable identifiers. |
| test artifacts → repository receipt | Logs/screenshots can contain secrets and must be scanned/redacted before retention. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-11 | Spoofing | destination resolver | high | mitigate | Exact UDID lookup plus explicit simulator rejection and target-class validator. |
| T-248-12 | Information disclosure | test artifacts | high | mitigate | Bounded diagnostics, allowlisted receipt, secret scan, cleanup before write. |
| T-248-13 | Tampering/Repudiation | receipt | high | mitigate | Clean SHA binding, artifact hashes, terminal atomic write. |
</threat_model>

## Artifacts this phase produces
- `scripts/ci/crosswake-native-ios-proof.sh` with CLI env inputs `SIGRA_IOS_DEVICE_UDID` and `SIGRA_IOS_DEVELOPMENT_TEAM`.
- `scripts/ci/crosswake-native-ios-proof.test.sh`.
- `.planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json`.

<verification>Run the exact automated command; no manual UAT can substitute for its receipt.</verification>
<success_criteria>NAT-01 is backed by a complete, secret-free, exact-source physical-device receipt.</success_criteria>
<output>Create `.planning/phases/248-crosswake-native-proof/248-04-SUMMARY.md` when done.</output>
