---
phase: 248-crosswake-native-proof
plan: "06"
type: execute
wave: 5
depends_on: [248-05]
files_modified:
  - scripts/ci/crosswake-native-android-proof.sh
  - scripts/ci/crosswake-native-android-proof.test.sh
  - .planning/phases/248-crosswake-native-proof/248-ANDROID-EVIDENCE.json
autonomous: true
requirements: [NAT-02]
user_setup:
  - service: android-emulator-toolchain
    why: The proof runner needs Android command-line tools matching Plan 248-08; the image-bundled browser version and APK SHA are already fixed by that committed lock.
    env_vars:
      - name: ANDROID_SDK_ROOT
        source: Pinned Android SDK command-line tools installation
must_haves:
  truths:
    - "D-11: exact emulator image, browser, AndroidX Browser, instrumentation, and toolchain versions are captured and checked."
    - "D-11/D-14: host orchestration proves all available emulator transports disabled and performs real force-stop/relaunch."
    - "D-12/D-13/D-15: complete, cleaned, secret-scan-clean Android evidence is written last and source-bound."
  artifacts:
    - path: test/example/native/android/toolchain.lock.json
      provides: Exact SDK/image/emulator/browser APK/version/SHA contract
    - path: scripts/ci/crosswake-native-android-proof.sh
      provides: Pinned emulator lifecycle and receipt-last runner
  key_links:
    - from: scripts/ci/crosswake-native-android-proof.sh
      to: scripts/ci/lib/native-proof-receipt.mjs
      via: target/scenario/version validation before write
  assumptions:
    - statement: "NAT-02 unclassified edge remains proof-pending until the pinned emulator/browser receipt proves all scenarios and transport state."
      status: proof-pending
      flagged: true
  prohibitions:
    - statement: "NAT-02 must not substitute Activity recreation or network throttling for force-stop/relaunch and verified transport-off behavior."
      status: unverified
      flagged: true
---

<objective>Provision and run the pinned Android emulator lane, then seal its complete source-bound receipt.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-05-SUMMARY.md
@scripts/ci/lib/exact-sha-worktree.sh
@scripts/ci/lib/native-proof-receipt.mjs</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run pinned emulator, browser, transport, process-death, and receipt-last proof</name>
  <precondition>Plan 248-08 completed: `ANDROID_SDK_ROOT` validates against the committed exact toolchain/browser lock and the checksum-pinned Gradle wrapper is executable.</precondition>
  <files>scripts/ci/crosswake-native-android-proof.sh, scripts/ci/crosswake-native-android-proof.test.sh, .planning/phases/248-crosswake-native-proof/248-ANDROID-EVIDENCE.json</files>
  <read_first>test/example/native/android/toolchain.lock.json; scripts/ci/crosswake-native-android-proof.sh; scripts/ci/crosswake-native-android-proof.test.sh; test/example/native/android/gradle/libs.versions.toml; test/example/native/android/app/src/androidTest/java/dev/sigra/proof/NativeProofInstrumentedTest.kt; scripts/ci/lib/exact-sha-worktree.sh; scripts/ci/lib/native-proof-receipt.mjs; .planning/phases/248-crosswake-native-proof/248-CONTEXT.md</read_first>
  <behavior>
    - Toolchain/image/browser mismatch fails before test execution.
    - Emulator boot/readiness uses bounded property polling; browser boundary uses UI Automator; no fixed sleep.
    - Offline phase disables and verifies Wi-Fi, cellular/data, and emulator network reachability; force-stop launches a new process.
  </behavior>
  <action>Consume the immutable Plan 248-08 `toolchain.lock.json` and wrapper; validate every SDK/image/browser/build/test coordinate and hash before invoking `./gradlew` or creating the AVD. Implement a bounded `set -euo pipefail` script that provisions only those locked coordinates, confirms image-bundled `com.android.chrome` has the exact locked version/APK SHA and recorded Auth Tab capability or bounded fallback, starts host/app, runs synchronized instrumentation phases, disables and verifies every available transport before offline audio/state, invokes `adb shell am force-stop` and a cold start, runs account/revocation/replay phases, captures allowlisted diagnostics, cleans app/AVD artifacts, scans secrets, validates artifact/source hashes, and writes `248-ANDROID-EVIDENCE.json` last per D-11 through D-15. Emit the exact shared receipt allowlist, including the full D-09 storage object and Android target/toolchain/browser/transport fields; reject any unknown, secret-shaped, or stable-identity field/value. Hermetic fake-tool tests must prove version mismatch, residual transport, Activity-only restart, missing scenario, storage-field omission, identity/secret injection, and early receipt writes fail.</action>
  <acceptance_criteria>Hermetic tests pass; real runner captures exact versions and completes the entire NAT-02 matrix; validator accepts target `android_emulator`; receipt records all transports disabled, force-stop/cold-start true, cleanup true, secret scan clean, and source hashes exact.</acceptance_criteria>
  <verify><automated>bash scripts/ci/crosswake-native-android-proof.test.sh &amp;&amp; scripts/ci/crosswake-native-android-proof.sh &amp;&amp; node scripts/ci/lib/native-proof-receipt.mjs --validate .planning/phases/248-crosswake-native-proof/248-ANDROID-EVIDENCE.json --target android_emulator</automated></verify>
  <done>A pinned Android emulator receipt proves NAT-02 without implying physical-device evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SDK/browser/emulator → runner | Mutable tooling could silently change the tested capability. |
| emulator transport/process → scenario claim | Local state must be exercised after verified network shutdown and real process death. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-17 | Tampering | toolchain/browser | high | mitigate | Exact versions and browser APK SHA checked before launch. |
| T-248-18 | Spoofing/Repudiation | offline/relaunch claim | high | mitigate | Transport enumeration/verification and adb force-stop/cold-start receipt fields. |
| T-248-19 | Information disclosure | artifacts | high | mitigate | Bounded diagnostics, cleanup, allowlisted receipt and secret scan before write. |
</threat_model>

## Artifacts this phase produces
- `scripts/ci/crosswake-native-android-proof.sh` and `scripts/ci/crosswake-native-android-proof.test.sh`.
- `.planning/phases/248-crosswake-native-proof/248-ANDROID-EVIDENCE.json` with shared fields `schema_version: native-proof-receipt/1`, `implementation_sha`, `target_class: android_emulator`, `target_identity`, `toolchain`, `browser`, `callback`, `storage`, `scenarios`, `transport`, `artifact_hashes`, `cleanup_status`, `secret_scan_status`, `terminal_status`.
- Exact Android nested fields: target `{platform: android, avd_device: pixel_8, api: 36, abi: x86_64, emulated: true}`; toolchain `{jdk, cmdline_tools, platform_tools, emulator, sdk_platform, build_tools, system_image, system_image_revision, gradle, agp, kotlin, androidx_browser, test_core, test_runner, espresso, uiautomator}` matching the lock; browser `{component: com.android.chrome, version, apk_sha256, mode: auth_tab|custom_tab_fallback}`; callback `{transport: custom_scheme, link_verification: registered_scheme, callback_binding: matched}`; storage `{present, rotated, recovered_after_relaunch, deleted_after_logout, deleted_after_revocation, read_result: read_ok|not_found|decrypt_failed|key_unavailable, access_persisted: false}`; scenarios `{hosted_return, image_verified, audio_verified, strict_lease_edge, offline_use, kill_relaunch, account_switch, server_revocation, replay_accepted, replay_rejected, replay_conflict}` all true; transport `{wifi_disabled, cellular_disabled, emulator_network_disabled, force_stop, cold_start}` all true; artifact hashes `{app_apk_sha256, test_apk_sha256, diagnostics_sha256}`; cleanup `complete`; secret scan `clean`; terminal `complete`.

<verification>Run the automated command and reject missing external provisioning rather than downgrading the claim.</verification>
<success_criteria>NAT-02 is backed by complete, pinned, secret-free emulator evidence with real transport/process controls.</success_criteria>
<output>Create `.planning/phases/248-crosswake-native-proof/248-06-SUMMARY.md` when done.</output>
