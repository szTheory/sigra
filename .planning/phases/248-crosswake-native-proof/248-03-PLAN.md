---
phase: 248-crosswake-native-proof
plan: "03"
type: execute
wave: 3
depends_on: [248-02]
files_modified:
  - test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj/project.pbxproj
  - test/example/native/ios/SigraNativeProof/App/SigraNativeProofApp.swift
  - test/example/native/ios/SigraNativeProof/App/HostedAuthSession.swift
  - test/example/native/ios/SigraNativeProof/App/SecureRefreshStore.swift
  - test/example/native/ios/SigraNativeProof/App/NativeLessonStore.swift
  - test/example/native/ios/SigraNativeProof/App/NativeProofStatusView.swift
  - test/example/native/ios/SigraNativeProof/Tests/NativeContractTests.swift
  - test/example/native/ios/SigraNativeProof/UITests/NativeProofUITests.swift
autonomous: true
requirements: [NAT-01, XW-01]
must_haves:
  truths:
    - "D-05/D-06: iOS authenticates only with ASWebAuthenticationSession and exact callback/state/PKCE checks before one-time exchange."
    - "D-08/D-09: access is memory-only; only rotating refresh material persists in restrictive Keychain storage; tests expose posture only."
    - "D-12/D-14: verified media, strict lease, relaunch, account isolation, revocation, and replay are testable without claiming radio-off."
  artifacts:
    - path: test/example/native/ios/SigraNativeProof/App/HostedAuthSession.swift
      provides: Hosted system-browser ceremony and exact callback validation
    - path: test/example/native/ios/SigraNativeProof/App/SecureRefreshStore.swift
      provides: Keychain-only rotating refresh store
    - path: test/example/native/ios/SigraNativeProof/UITests/NativeProofUITests.swift
      provides: Stable-identifier XCUITest scenario driver
  key_links:
    - from: HostedAuthSession.swift
      to: Example.Accounts.CrosswakeNativeBridge
      via: validated posture-only native return request
    - from: NativeLessonStore.swift
      to: Example.LearningTwin
      via: manifest/lease/replay HTTP contract
  assumptions:
    - statement: "NAT-01 edge remains unresolved; the most restrictive Keychain accessibility class is accepted only if contract tests prove required foreground relaunch recovery."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "NAT-01 must not present ASWebAuthenticationSession as standalone Safari or controlled transport failure as physical radio disconnection."
      status: unverified
      flagged: true
---

<objective>
Build the host-owned iOS example shell and deterministic test surface for the complete native session/offline contract.

Purpose: Implement NAT-01 behavior without publishing an SDK or weakening first-party authority.
Output: Xcode app/test targets with system-browser auth, Keychain posture, verified local lesson state, and stable XCUITest hooks.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-RESEARCH.md
@.planning/phases/248-crosswake-native-proof/248-PATTERNS.md
@.planning/phases/248-crosswake-native-proof/248-02-SUMMARY.md
@test/example/priv/static/assets/js/learning_twin.js
@test/example/priv/playwright/tests/twin-offline.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete hosted iOS login and Keychain rotation behind posture-only status</name>
  <files>test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj/project.pbxproj, test/example/native/ios/SigraNativeProof/App/SigraNativeProofApp.swift, test/example/native/ios/SigraNativeProof/App/HostedAuthSession.swift, test/example/native/ios/SigraNativeProof/App/SecureRefreshStore.swift, test/example/native/ios/SigraNativeProof/Tests/NativeContractTests.swift</files>
  <read_first>test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj/project.pbxproj; test/example/native/ios/SigraNativeProof/App/SigraNativeProofApp.swift; test/example/native/ios/SigraNativeProof/App/HostedAuthSession.swift; test/example/native/ios/SigraNativeProof/App/SecureRefreshStore.swift; test/example/native/ios/SigraNativeProof/Tests/NativeContractTests.swift; test/example/lib/example/accounts/crosswake_native_bridge.ex; test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/auth_return.ex; test/sigra/app_session_test.exs; .planning/phases/246-hosted-and-direct-login-ceremonies/246-CONTEXT.md; .planning/phases/248-crosswake-native-proof/248-CONTEXT.md</read_first>
  <behavior>
    - ASWebAuthenticationSession starts hosted PKCE S256 and accepts only the registered custom-scheme callback with exact state.
    - Successful exchange keeps access in memory, writes only refresh to Keychain, rotates internally, recovers after relaunch, and deletes after logout/revocation.
    - Status exposes only fixed booleans/enums and `access_persisted=false`.
  </behavior>
  <action>Create a host-owned iOS app target using exact custom scheme `sigra-native-proof://auth/callback`. Implement `HostedAuthSession` around `ASWebAuthenticationSession`, application-level callback/state validation, Phase 246 one-time exchange/refresh/logout calls, and fact-only Crosswake projection per D-03 through D-06. Implement `SecureRefreshStore` with Keychain generic-password storage and the most restrictive device-only accessibility class that passes foreground kill/relaunch tests per D-08; compare rotations internally and expose only D-09 posture. Do not introduce a reusable framework/product or direct-password/embedded-browser path.</action>
  <acceptance_criteria>`xcodebuild` builds the app and runs `NativeContractTests`; source imports AuthenticationServices/Security, uses the exact callback, has no SDK/library product, and contract tests prove state/callback denial, access non-persistence, refresh rotation/recovery/deletion, and secret-free status.</acceptance_criteria>
  <verify><automated>xcodebuild -project test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj -scheme SigraNativeProof -destination 'generic/platform=iOS' build-for-testing &amp;&amp; xcodebuild -project test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj -scheme SigraNativeProof -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:SigraNativeProofTests test</automated></verify>
  <done>The iOS shell implements the hosted session and secure-storage contract with deterministic contract tests; simulator use here is build/contract evidence only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add verified native lesson, lease/account gates, and synchronized XCUITest scenarios</name>
  <files>test/example/native/ios/SigraNativeProof/App/NativeLessonStore.swift, test/example/native/ios/SigraNativeProof/App/NativeProofStatusView.swift, test/example/native/ios/SigraNativeProof/Tests/NativeContractTests.swift, test/example/native/ios/SigraNativeProof/UITests/NativeProofUITests.swift, test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj/project.pbxproj</files>
  <read_first>test/example/priv/static/assets/js/learning_twin.js; test/example/priv/playwright/tests/twin-offline.spec.ts; test/example/lib/example/learning_twin.ex; test/example/priv/native-fixtures/native-proof-status.json; scripts/ci/lib/native-proof-receipt.mjs; .planning/phases/247-language-learning-digital-twin/247-05-SUMMARY.md</read_first>
  <behavior>
    - Image/audio activate only after exact length and SHA-256 verification and durable file write.
    - Exact seven-day expiry, missing/changed partition, logout, account switch, and revocation clear activation before local use.
    - Relaunch restores bounded local state; accepted/rejected/conflict retries reconcile exactly once through the host.
  </behavior>
  <action>Implement `NativeLessonStore` with partition-leading filenames/metadata, marker-last verified media promotion, strict microsecond lease comparison, offline audio playback, credential-free journal entries, and host-authoritative replay mapping per D-01/D-02/D-12. Add `NativeProofStatusView` only in the proof build configuration using stable accessibility identifiers for hosted return, storage posture, media readiness, lease edge, offline fixture, relaunch, account switch, revocation, cleanup, and three replay statuses. Drive it with XCUITest expectations/predicates and launch-environment fixtures; use no elapsed-time sleeps. Label transport evidence `controlled_transport_failure` per D-14.</action>
  <acceptance_criteria>Unit tests cover corrupt/short media, strict expiry, partition switch, duplicate replay and revocation; XCUITest source covers every NAT-01 scenario through stable identifiers and contains no fixed sleep; retained/status values conform to the shared allowlist.</acceptance_criteria>
  <verify><automated>xcodebuild -project test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj -scheme SigraNativeProof -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:SigraNativeProofTests test &amp;&amp; node --test scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs</automated></verify>
  <done>The iOS shell carries the complete bounded native behavior and is ready for physical-device orchestration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| system auth session → app | Callback/state/code data is untrusted until exact validation and host exchange. |
| app memory → Keychain/local files | Only refresh may cross secure storage; lesson state is partition/lease bounded. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-07 | Spoofing/Replay | HostedAuthSession | high | mitigate | Exact callback/state, PKCE S256, one-time host exchange, negative tests. |
| T-248-08 | Information disclosure | SecureRefreshStore/status | high | mitigate | Memory-only access, device-only Keychain refresh, posture-only UI, prohibition scan. |
| T-248-09 | Elevation of privilege | NativeLessonStore | high | mitigate | Strict lease/current partition/revocation gates and host-only terminal replay. |
| T-248-10 | Denial of service | media/cache | medium | mitigate | Marker-last writes, bounded files, explicit retry categories. |
</threat_model>

## Artifacts this phase produces
- Xcode project `test/example/native/ios/SigraNativeProof/SigraNativeProof.xcodeproj/project.pbxproj` and app/test targets.
- Types `HostedAuthSession`, `SecureRefreshStore`, `NativeLessonStore`, `NativeProofStatusView`.
- Callback `sigra-native-proof://auth/callback`.
- Test file `Tests/NativeContractTests.swift` and UI suite `UITests/NativeProofUITests.swift`.
- Status fields `hosted_return`, `refresh_present`, `refresh_rotated`, `refresh_recovered`, `refresh_deleted`, `refresh_read_result`, `access_persisted`, `image_verified`, `audio_verified`, `lease_edge`, `transport_fixture`, `relaunch`, `account_isolation`, `revoked`, `replay_outcomes`, `cleanup`.

<verification>Run both task commands; physical execution is deliberately reserved for Plan 04.</verification>
<success_criteria>The host-owned iOS shell implements every NAT-01 scenario without moving authority or leaking credential material.</success_criteria>
<output>Create `.planning/phases/248-crosswake-native-proof/248-03-SUMMARY.md` when done.</output>
