---
phase: 248-crosswake-native-proof
plan: "07"
type: execute
wave: 6
depends_on: [248-04, 248-06]
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/phase-248-native-proof.sh
  - test/sigra/planning/phase_248_native_proof_test.exs
  - .planning/phases/248-crosswake-native-proof/248-EVIDENCE.json
autonomous: true
requirements: [XW-01, NAT-01, NAT-02]
must_haves:
  truths:
    - "Both platform receipts independently validate, reference the same immutable implementation SHA, and preserve their distinct evidence classes."
    - "The final gate proves released package pins, host authority, Crosswake/replay mapping, physical-iPhone proof, pinned-emulator proof, cleanup, and secret scans before sealing."
  artifacts:
    - path: scripts/ci/phase-248-native-proof.sh
      provides: Cross-platform convergence and exact-source gate
    - path: .planning/phases/248-crosswake-native-proof/248-EVIDENCE.json
      provides: Credential-free phase index over both immutable receipts
  key_links:
    - from: scripts/ci/phase-248-native-proof.sh
      to: .planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json
      via: exact target/schema/source validation
    - from: scripts/ci/phase-248-native-proof.sh
      to: .planning/phases/248-crosswake-native-proof/248-ANDROID-EVIDENCE.json
      via: exact target/schema/source validation
  assumptions:
    - statement: "All three spec-less edge rows remain proof-pending until final verification reviews the machine evidence; no plan silently reclassifies them."
      status: proof-pending
      flagged: true
  prohibitions:
    - statement: "The phase must not merge, relabel, or overstate physical-iPhone, controlled-transport, Android-emulator, browser, or Crosswake contract evidence classes."
      status: unverified
      flagged: true
---

<objective>Converge the host, iPhone, and Android proofs into one fail-closed CI contract and phase evidence index.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-04-SUMMARY.md
@.planning/phases/248-crosswake-native-proof/248-06-SUMMARY.md
@.planning/phases/248-crosswake-native-proof/COVERAGE.md
@.planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json
@.planning/phases/248-crosswake-native-proof/248-ANDROID-EVIDENCE.json</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Fail closed on stale, incomplete, secret-bearing, or evidence-class-mismatched native proof</name>
  <files>scripts/ci/phase-248-native-proof.sh, test/sigra/planning/phase_248_native_proof_test.exs, .planning/phases/248-crosswake-native-proof/248-EVIDENCE.json</files>
  <read_first>scripts/ci/phase-248-native-proof.sh; test/sigra/planning/phase_248_native_proof_test.exs; .planning/phases/248-crosswake-native-proof/248-IOS-EVIDENCE.json; .planning/phases/248-crosswake-native-proof/248-ANDROID-EVIDENCE.json; scripts/ci/lib/native-proof-receipt.mjs; scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs; .planning/phases/248-crosswake-native-proof/COVERAGE.md</read_first>
  <behavior>
    - Missing or stale platform receipt fails; one platform cannot substitute for the other.
    - Both receipts bind the same current SHA and their declared source/artifact hashes match repository bytes.
    - Phase index contains hashes/status/evidence classes only and is written after all gates.
  </behavior>
  <action>Create a bounded convergence runner that validates COVERAGE.md, exact Hex locks, focused Crosswake bridge/LearningTwin tests, native receipt schema tests, real/bad/clean prohibition enforcement, both platform receipt target classes, source/artifact hashes, cleanup and secret scans. Require `schema_version: native-proof-receipt/1` and the exact Plan 248-02 recursive allowlist: all named top-level fields, callback enums, all seven D-09 storage fields and four `read_result` categories, all eleven scenario booleans, iOS target/toolchain/browser/controlled-transport and three artifact-hash fields, Android target/all sixteen toolchain/browser/five transport and three artifact-hash fields, and terminal `complete|clean|complete` statuses; reject missing, extra, incompatible, secret-shaped, or stable-identity fields/values. Revalidate the current clean SHA, then atomically write a small `248-EVIDENCE.json` index containing schema version, implementation SHA, receipt paths/hashes, exact `physical_iphone` and `android_emulator` classes, iPhone `controlled_transport_failure`, and terminal completion. Add ExUnit source-contract tests for all gates and failure when either receipt is missing, stale, mislabeled, schema-divergent, or contains prohibited values.</action>
  <acceptance_criteria>The complete runner passes only with both exact receipts; mutation fixtures for absent/stale/wrong-target/secret-bearing receipts fail; index contains no callback, credential, account, device identifier, or raw diagnostic.</acceptance_criteria>
  <verify><automated>scripts/ci/phase-248-native-proof.sh &amp;&amp; mix test test/sigra/planning/phase_248_native_proof_test.exs --trace</automated></verify>
  <done>The complete phase claim is source-bound, machine-readable, and fail-closed across all three requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Register separate native lanes without weakening execution-class requirements</name>
  <files>.github/workflows/ci.yml, test/sigra/planning/phase_248_native_proof_test.exs</files>
  <read_first>.github/workflows/ci.yml; test/sigra/planning/phase_248_native_proof_test.exs; scripts/ci/crosswake-native-ios-proof.sh; scripts/ci/crosswake-native-android-proof.sh; scripts/ci/phase-248-native-proof.sh; AGENTS.md</read_first>
  <action>Add explicit retry-zero jobs/steps for the attached-device iOS lane, pinned Android emulator lane, and final convergence validator. Preserve separate artifacts and target labels; do not let ordinary Linux/browser success mark either native claim. Use deterministic readiness, bounded timeouts, one watcher per externally dispatched run, and no sleeps. Ensure the physical lane requires its configured runner/device labels and the Android lane provisions the committed lock.</action>
  <acceptance_criteria>Workflow source has distinct iOS/Android/convergence commands and artifacts; no simulator command feeds NAT-01; no generic success fallback exists; planning contract test pins the selectors, commands, receipt paths, and dependency ordering.</acceptance_criteria>
  <verify><automated>mix test test/sigra/planning/phase_248_native_proof_test.exs --trace &amp;&amp; ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml", aliases: true)'</automated></verify>
  <done>CI preserves honest evidence classes and cannot report Phase 248 green without both native receipts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| platform receipts → milestone support claim | Separate evidence classes converge into one public readiness decision. |
| CI configuration → retained artifacts | Conditional/skipped jobs must not be mistaken for successful proof. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-20 | Tampering/Repudiation | convergence runner | high | mitigate | Exact SHA/hash/schema/target checks and missing-receipt hard failure. |
| T-248-21 | Information disclosure | phase evidence | high | mitigate | Hash/status-only index plus platform secret-scan requirements. |
| T-248-22 | Elevation of privilege | CI claim logic | high | mitigate | Distinct required jobs/artifacts; no skip/simulator/emulator substitution. |
</threat_model>

## Artifacts this phase produces
- `scripts/ci/phase-248-native-proof.sh`.
- `test/sigra/planning/phase_248_native_proof_test.exs`.
- `.planning/phases/248-crosswake-native-proof/248-EVIDENCE.json` with fields `schema_version`, `implementation_sha`, `ios_receipt`, `ios_receipt_sha256`, `ios_evidence_class`, `ios_transport_claim`, `android_receipt`, `android_receipt_sha256`, `android_evidence_class`, `complete`.
- CI jobs/steps `crosswake_native_ios_physical`, `crosswake_native_android_emulator`, and `crosswake_native_convergence`.

<verification>Run the convergence runner, focused ExUnit contract, and YAML parser.</verification>
<success_criteria>XW-01, NAT-01, and NAT-02 are simultaneously proven without credential leakage or evidence-class overstatement.</success_criteria>
<output>Create `.planning/phases/248-crosswake-native-proof/248-07-SUMMARY.md` when done.</output>
