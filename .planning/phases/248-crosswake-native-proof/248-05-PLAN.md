---
phase: 248-crosswake-native-proof
plan: "05"
type: execute
wave: 3
depends_on: [248-02]
files_modified:
  - test/example/native/android/settings.gradle.kts
  - test/example/native/android/gradle/libs.versions.toml
  - test/example/native/android/app/build.gradle.kts
  - test/example/native/android/app/src/main/AndroidManifest.xml
  - test/example/native/android/app/src/main/java/dev/sigra/proof/MainActivity.kt
  - test/example/native/android/app/src/main/java/dev/sigra/proof/HostedAuthSession.kt
  - test/example/native/android/app/src/main/java/dev/sigra/proof/SecureRefreshStore.kt
  - test/example/native/android/app/src/main/java/dev/sigra/proof/NativeLessonStore.kt
  - test/example/native/android/app/src/androidTest/java/dev/sigra/proof/NativeProofInstrumentedTest.kt
autonomous: true
requirements: [NAT-02, XW-01]
must_haves:
  truths:
    - "D-05/D-07: Android uses pinned AuthTabIntent when capability is verified and a declared Custom Tabs fallback otherwise."
    - "D-08/D-09: access is memory-only; only AES-GCM ciphertext/nonce/key metadata persist under a non-exportable AndroidKeyStore key."
    - "D-11/D-12: synchronized instrumentation covers verified media, lease, relaunch, isolation, revocation, and exactly-once outcomes."
  artifacts:
    - path: test/example/native/android/gradle/libs.versions.toml
      provides: Exact Android/AndroidX/test dependency coordinates
    - path: test/example/native/android/app/src/main/java/dev/sigra/proof/HostedAuthSession.kt
      provides: Auth Tab-first hosted ceremony
    - path: test/example/native/android/app/src/androidTest/java/dev/sigra/proof/NativeProofInstrumentedTest.kt
      provides: Espresso/UI Automator scenario contract
  key_links:
    - from: HostedAuthSession.kt
      to: Example.Accounts.CrosswakeNativeBridge
      via: validated posture-only return request
    - from: NativeLessonStore.kt
      to: Example.LearningTwin
      via: host manifest/lease/replay APIs
  assumptions:
    - statement: "NAT-02 edge remains unresolved; the selected pinned browser must prove Auth Tab capability or record and exercise only the bounded fallback."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "NAT-02 must not silently treat Activity recreation, throttled networking, or emulator storage as physical-device/process/offline proof."
      status: unverified
      flagged: true
---

<objective>Build the pinned host-owned Android emulator shell and deterministic instrumentation contract for NAT-02.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-RESEARCH.md
@.planning/phases/248-crosswake-native-proof/248-PATTERNS.md
@.planning/phases/248-crosswake-native-proof/248-02-SUMMARY.md
@test/example/priv/static/assets/js/learning_twin.js
@test/example/lib/example/learning_twin.ex</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Pin and launch the host-owned Android proof application</name>
  <files>test/example/native/android/settings.gradle.kts, test/example/native/android/gradle/libs.versions.toml, test/example/native/android/app/build.gradle.kts, test/example/native/android/app/src/main/AndroidManifest.xml, test/example/native/android/app/src/main/java/dev/sigra/proof/MainActivity.kt</files>
  <read_first>test/example/native/android/settings.gradle.kts; test/example/native/android/gradle/libs.versions.toml; test/example/native/android/app/build.gradle.kts; test/example/native/android/app/src/main/AndroidManifest.xml; test/example/native/android/app/src/main/java/dev/sigra/proof/MainActivity.kt; .planning/phases/248-crosswake-native-proof/248-RESEARCH.md; .planning/phases/248-crosswake-native-proof/248-CONTEXT.md</read_first>
  <behavior>
    - Exact released/pinned dependencies resolve; AndroidX Browser is exactly 1.9.0 and test libraries/toolchain coordinates are locked in the catalog.
    - The debug application launches into a stable, non-secret not-authenticated readiness state.
    - No Android library/Maven publication surface is configured.
  </behavior>
  <action>Create an application-only Gradle project with AndroidX Browser 1.9.0 and exact AndroidX Test/Espresso/UI Automator coordinates in the version catalog. Register custom scheme `sigra-native-proof://auth/android`, add the proof-only application activity, and expose one stable readiness resource/content-description containing only `not_authenticated`. Configure no Android library plugin, Maven publication, or reusable SDK product per D-03.</action>
  <acceptance_criteria>Gradle dependency output resolves exact catalog versions; the debug APK builds and installs; a launch assertion finds the readiness state; project has no library/publication configuration.</acceptance_criteria>
  <verify><automated>cd test/example/native/android &amp;&amp; ./gradlew --no-daemon --console=plain dependencies assembleDebug testDebugUnitTest</automated></verify>
  <done>A real user can launch the pinned host-owned Android proof app, establishing the build/runtime slice for the behavior expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete hosted auth, secure rotation, verified lesson, and synchronized instrumentation</name>
  <files>test/example/native/android/app/src/main/java/dev/sigra/proof/MainActivity.kt, test/example/native/android/app/src/main/java/dev/sigra/proof/HostedAuthSession.kt, test/example/native/android/app/src/main/java/dev/sigra/proof/SecureRefreshStore.kt, test/example/native/android/app/src/main/java/dev/sigra/proof/NativeLessonStore.kt, test/example/native/android/app/src/androidTest/java/dev/sigra/proof/NativeProofInstrumentedTest.kt</files>
  <read_first>test/example/native/android/app/src/main/java/dev/sigra/proof/MainActivity.kt; test/example/native/android/app/src/main/java/dev/sigra/proof/HostedAuthSession.kt; test/example/native/android/app/src/main/java/dev/sigra/proof/SecureRefreshStore.kt; test/example/native/android/app/src/main/java/dev/sigra/proof/NativeLessonStore.kt; test/example/native/android/app/src/androidTest/java/dev/sigra/proof/NativeProofInstrumentedTest.kt; test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/auth_return.ex; test/example/priv/static/assets/js/learning_twin.js; test/example/lib/example/learning_twin.ex; test/example/priv/native-fixtures/native-proof-status.json; scripts/ci/lib/native-proof-receipt.mjs</read_first>
  <behavior>
    - Byte length/SHA-256/durable write precede image/audio ready metadata.
    - Strict lease and current partition gate local access; logout/account switch/revocation clear activation.
    - Espresso owns in-app synchronization; UI Automator owns browser/system assertions; no fixed sleep occurs.
  </behavior>
  <action>Implement `HostedAuthSession` using `AuthTabIntent` capability detection, exact callback/state validation, and bounded Custom Tabs fallback per D-05/D-07. Implement `SecureRefreshStore` using a non-exportable AndroidKeyStore AES/GCM/NoPadding key plus app-private `{nonce,ciphertext,keyAlias,version}` record per D-08, with memory-only access and D-09 posture transitions. Implement `NativeLessonStore` with marker-last app-private media/state, strict seven-day microsecond edge, partition-first filenames, credential-free Crosswake journal fields, and host-authoritative accepted/rejected/conflict reconciliation per D-01/D-02/D-12. Add stable status hooks; use Espresso synchronization in-app and UI Automator conditions at browser/system boundaries; expose host-orchestration selectors for force-stop/relaunch and transport shutdown per D-11.</action>
  <acceptance_criteria>Unit/instrumentation source covers corrupt media, exact expiry, offline audio, account switch, revocation and all replay statuses; no sleep calls; process-death and transport claims are emitted only after the external orchestration phases.</acceptance_criteria>
  <verify><automated>cd test/example/native/android &amp;&amp; ./gradlew --no-daemon --console=plain assembleDebug assembleDebugAndroidTest testDebugUnitTest &amp;&amp; node --test ../../../../scripts/ci/prohibitions/p17-crosswake-native-boundary.test.mjs</automated></verify>
  <done>The complete Android behavior is buildable and ready for pinned-emulator orchestration.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| system browser → app callback | Untrusted return data crosses exact callback/state/PKCE validation. |
| memory → Keystore/app-private storage | Only encrypted refresh material may persist. |
| local journal/cache → host | Cached facts cannot choose owner, lease, revocation, or terminal outcome. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-14 | Spoofing/Tampering | HostedAuthSession | high | mitigate | Exact callback/state, capability/version capture, host one-time exchange. |
| T-248-15 | Information disclosure | SecureRefreshStore | high | mitigate | Keystore AES-GCM, private storage, posture-only status and secret scan. |
| T-248-16 | Elevation of privilege | NativeLessonStore | high | mitigate | Current partition/strict lease/revocation gates and host terminal replay. |
| T-248-SC | Tampering | Maven packages | medium | mitigate | Official AndroidX coordinates are pinned exactly in committed version catalog; resolved dependency output is captured by proof. |
</threat_model>

## Artifacts this phase produces
- Android Gradle application files listed in `files_modified`, including `libs.versions.toml`.
- Types `MainActivity`, `HostedAuthSession`, `SecureRefreshStore`, `NativeLessonStore`, `NativeProofInstrumentedTest`.
- Callback `sigra-native-proof://auth/android`.
- Instrumentation phase flags `hosted_return`, `storage_rotation`, `verified_media`, `offline_local`, `relaunch_recovery`, `account_switch`, `revocation`, `replay_outcomes`, `cleanup`.

<verification>Run both Gradle/Node commands with no watch mode or sleeps.</verification>
<success_criteria>The Android shell implements every NAT-02 behavior with pinned dependencies and preserved host authority.</success_criteria>
<output>Create `.planning/phases/248-crosswake-native-proof/248-05-SUMMARY.md` when done.</output>
