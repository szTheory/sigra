---
phase: 248-crosswake-native-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/lib/example/accounts/crosswake_native_bridge.ex
  - test/example/lib/example/accounts/crosswake_session_adapter.ex
  - test/example/test/example/accounts/crosswake_native_bridge_test.exs
  - test/example/test/example/accounts/crosswake_session_adapter_test.exs
autonomous: true
requirements: [XW-01]
must_haves:
  truths:
    - "D-01/D-04: a native return is evaluated only after fresh Sigra session resolution and Crosswake receives only allowlisted posture facts."
    - "D-02: native replay uses released journal/request/outcome vocabulary while Example.LearningTwin remains the terminal authority."
  artifacts:
    - path: test/example/lib/example/accounts/crosswake_native_bridge.ex
      provides: Host-owned native evidence and replay projection bridge
      exports: [evaluate_return/6, replay_request/1, replay_outcome/2]
    - path: test/example/test/example/accounts/crosswake_native_bridge_test.exs
      provides: Authority, revocation, replay, and secret-boundary proof
  key_links:
    - from: test/example/lib/example/accounts/crosswake_native_bridge.ex
      to: test/example/lib/example/accounts/crosswake_session_adapter.ex
      via: evaluate_return/6 after exact local callback posture
    - from: test/example/lib/example/accounts/crosswake_native_bridge.ex
      to: test/example/lib/example/learning_twin.ex
      via: Crosswake Replay.Request mapped into host replay/3
  assumptions:
    - statement: "XW-01 edge classification is unresolved (spec-less probe: unclassified — review manually); implementation assumes released 0.2.0/0.1.3 contracts are the complete companion surface."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "XW-01 must not silently turn Crosswake or native evidence into credential custody, account selection, or authentication authority."
      status: unverified
      flagged: true
---

<objective>
Trace one real native-return and offline-replay path through the already released Crosswake contracts while preserving Sigra/Phoenix authority per D-01 through D-04.

Purpose: Prove the phase's riskiest ownership seam before either platform shell expands it.
Output: A production host bridge and focused contract/security tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-RESEARCH.md
@.planning/phases/247-language-learning-digital-twin/247-06-SUMMARY.md
@test/example/deps/crosswake/lib/crosswake/offline/journal.ex
@test/example/deps/crosswake/lib/crosswake/offline/replay.ex
@test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/auth_return.ex
@test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/telemetry.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one native return through fresh host authority and released NativeEvidence</name>
  <files>test/example/lib/example/accounts/crosswake_native_bridge.ex, test/example/lib/example/accounts/crosswake_session_adapter.ex, test/example/test/example/accounts/crosswake_native_bridge_test.exs, test/example/test/example/accounts/crosswake_session_adapter_test.exs</files>
  <read_first>test/example/lib/example/accounts/crosswake_native_bridge.ex; test/example/lib/example/accounts/crosswake_session_adapter.ex; test/example/test/example/accounts/crosswake_native_bridge_test.exs; test/example/test/example/accounts/crosswake_session_adapter_test.exs; test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/auth_return.ex; test/example/deps/crosswake_sigra/lib/crosswake/companions/sigra/telemetry.ex; .planning/phases/248-crosswake-native-proof/248-CONTEXT.md</read_first>
  <behavior>
    - A locally validated iOS or Android posture becomes `AuthReturn.NativeEvidence`, then the existing adapter freshly reloads the session before evaluation.
    - Revoked, stale, missing, or binding-mismatched sessions deny before evaluator invocation.
    - Unsupported platform/transport/link/callback/replay values and any sensitive or authority-shaped input fail closed.
  </behavior>
  <action>Create `Example.Accounts.CrosswakeNativeBridge.evaluate_return/6` as the only new native entry point. Accept only platform, return transport, link verification, callback binding, replay posture, and a newly derived opaque assertion reference; construct the released `AuthReturn.NativeEvidence` and delegate to `CrosswakeSessionAdapter.evaluate_return/6`. Extend the adapter only where the released native evidence type requires it. Preserve fresh host lookup and denial precedence per D-01, consume the locked Hex versions per D-03, and keep credentials, callback material, account identity, codes, PKCE values, and authority decisions outside the constructed contract per D-04.</action>
  <acceptance_criteria>`evaluate_return/6` exists and tests prove both platforms' allowlisted postures; evaluator capture contains only released typed evidence; revoked/binding-mismatch cases prove evaluator non-invocation; `test/example/mix.lock` remains pinned to crosswake 0.2.0 and crosswake_sigra 0.1.3.</acceptance_criteria>
  <verify><automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_native_bridge_test.exs test/example/accounts/crosswake_session_adapter_test.exs --trace</automated></verify>
  <done>One native-return happy path and its fail-closed authority boundary pass end to end against released packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map native outbox identity and terminal host outcomes onto Crosswake replay</name>
  <files>test/example/lib/example/accounts/crosswake_native_bridge.ex, test/example/test/example/accounts/crosswake_native_bridge_test.exs</files>
  <read_first>test/example/lib/example/accounts/crosswake_native_bridge.ex; test/example/lib/example/learning_twin.ex; test/example/test/example/learning_twin/learning_twin_test.exs; test/example/deps/crosswake/lib/crosswake/offline/journal.ex; test/example/deps/crosswake/lib/crosswake/offline/replay.ex; .planning/phases/247-language-learning-digital-twin/247-04-SUMMARY.md</read_first>
  <behavior>
    - `client_mutation_id`, idempotency key, base checkpoint, route and journal entry map without renaming or loss.
    - Host `accepted`, `rejected`, and `conflict` receipts map to matching released outcomes.
    - Duplicate requests reload the host's stored terminal receipt; local/Crosswake input cannot select account or terminal status.
  </behavior>
  <action>Add `replay_request/1` and `replay_outcome/2` to build released `Crosswake.Offline.Replay.Request` and `Outcome` values around the existing `LearningTwin.replay/3` call. Per D-02, Crosswake structures correlation only: derive current Scope/lease/partition and terminal result exclusively in the host, preserve the existing unique receipt as authority, and reject owner/outcome smuggling.</action>
  <acceptance_criteria>Focused tests cover all three terminal statuses, sequential duplicate stability, current-Scope account isolation, exact field preservation, and absence of a code path where a Crosswake/native status becomes the stored result.</acceptance_criteria>
  <verify><automated>cd test/example &amp;&amp; mix test test/example/accounts/crosswake_native_bridge_test.exs test/example/learning_twin/learning_twin_test.exs --trace</automated></verify>
  <done>Released replay vocabulary and host-owned exactly-once authority are connected and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| native callback facts → Phoenix host | Untrusted platform-derived posture enters typed validation. |
| host session/replay → Crosswake | Only freshly derived bounded facts may cross into the companion runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-01 | Spoofing/Tampering | `evaluate_return/6` | high | mitigate | Exact typed NativeEvidence plus fresh session/binding/revocation lookup before evaluator; focused denial tests. |
| T-248-02 | Elevation of privilege | replay bridge | high | mitigate | Current Scope owns partition and terminal result; Crosswake fields carry correlation only; duplicate/account-isolation tests. |
| T-248-03 | Information disclosure | evidence/telemetry | high | mitigate | Released allowlists and forbidden metadata filter; injected sensitive-field rejection tests. |
| T-248-SC | Tampering | package supply chain | low | accept | Existing audited Hex lock entries are reused exactly; no package install occurs in this plan. |
</threat_model>

## Artifacts this phase produces

- File `test/example/lib/example/accounts/crosswake_native_bridge.ex`.
- Module `Example.Accounts.CrosswakeNativeBridge`.
- Functions `evaluate_return/6`, `replay_request/1`, and `replay_outcome/2`.
- Test file `test/example/test/example/accounts/crosswake_native_bridge_test.exs`.

<verification>
Run the two focused ExUnit commands above and confirm the locked package versions remain exact.
</verification>

<success_criteria>
XW-01 has an executable, secret-free host/Crosswake path whose session, account, lease, revocation, and terminal replay authority remains first-party.
</success_criteria>

<output>Create `.planning/phases/248-crosswake-native-proof/248-01-SUMMARY.md` when done.</output>
