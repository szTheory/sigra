---
phase: 248-crosswake-native-proof
plan: "08"
type: execute
wave: 3
depends_on: [248-02]
files_modified:
  - scripts/ci/native-proof-provision.sh
  - scripts/ci/native-proof-provision.test.sh
  - test/example/native/native-proof-environment.lock.json
  - test/example/native/android/toolchain.lock.json
  - test/example/native/android/gradlew
  - test/example/native/android/gradlew.bat
  - test/example/native/android/gradle/wrapper/gradle-wrapper.jar
  - test/example/native/android/gradle/wrapper/gradle-wrapper.properties
autonomous: true
requirements: [NAT-01, NAT-02]
user_setup:
  - service: sigra-ios-physical-runner
    why: "D-10 requires a repository-managed self-hosted Apple-silicon macOS runner with labels self-hosted, macOS, ARM64, sigra-ios-physical and an attached physical iPhone."
    env_vars:
      - name: SIGRA_IOS_DEVICE_UDID
        source: "UDID of the paired, trusted, attached physical iPhone on the selected runner"
      - name: SIGRA_IOS_DEVELOPMENT_TEAM
        source: "Apple development team available to that runner's signing identity"
  - service: android-proof-provisioning
    why: "D-11 requires exact SDK/image/browser coordinates before the Android shell may build."
    env_vars:
      - name: ANDROID_SDK_ROOT
        source: "Writable Android SDK root on the x86_64 Linux proof runner"
must_haves:
  truths:
    - "D-10: the selected iOS execution environment is exactly a self-hosted Apple-silicon macOS runner labeled sigra-ios-physical with an attached non-simulator iPhone, or provisioning fails before either native shell builds."
    - "D-11: exact Android SDK, image, browser, Gradle, AndroidX, and test coordinates are committed and validated before Plan 248-05 invokes the wrapper."
    - "The committed Gradle wrapper is generated from the checksum-pinned Gradle 8.13 distribution and validates before any project build command."
  artifacts:
    - path: test/example/native/native-proof-environment.lock.json
      provides: Selected iOS runner contract and exact callback transports for both shells
    - path: test/example/native/android/toolchain.lock.json
      provides: Exact pre-build Android SDK/image/browser/build/test coordinates
    - path: test/example/native/android/gradle/wrapper/gradle-wrapper.properties
      provides: Gradle 8.13 distribution URL and SHA-256 pin
    - path: test/example/native/android/gradle/wrapper/gradle-wrapper.jar
      provides: Generated Gradle wrapper bootstrap binary
  key_links:
    - from: scripts/ci/native-proof-provision.sh
      to: test/example/native/native-proof-environment.lock.json
      via: fail-closed physical destination and callback-coordinate validation
    - from: scripts/ci/native-proof-provision.sh
      to: test/example/native/android/toolchain.lock.json
      via: sdkmanager/package/browser version and SHA capture before native build
    - from: test/example/native/android/gradlew
      to: test/example/native/android/gradle/wrapper/gradle-wrapper.properties
      via: checksum-verified Gradle 8.13 bootstrap
  assumptions:
    - statement: "The selected physical iPhone and Android SDK are not available in the current local environment; this prerequisite must fail closed rather than fabricate availability or downgrade either evidence class."
      status: resolved-by-precondition
      flagged: true
  prohibitions:
    - statement: "No simulator, mutable latest coordinate, unversioned browser, unchecked wrapper binary, or post-build coordinate capture may satisfy this prerequisite."
      status: unverified
      flagged: true
---

<objective>
Lock and validate the exact physical-iPhone runner contract, callback transports, Android toolchain/browser coordinates, and Gradle wrapper before either native shell builds.

Purpose: Resolve the phase's external-environment and missing-wrapper blockers at the earliest executable boundary.
Output: A fail-closed provisioning runner, two machine-readable environment locks, and a checksum-pinned Gradle wrapper.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/248-crosswake-native-proof/248-CONTEXT.md
@.planning/phases/248-crosswake-native-proof/248-RESEARCH.md
@.planning/phases/248-crosswake-native-proof/248-02-SUMMARY.md
@scripts/ci/lib/native-proof-receipt.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the selected physical-iPhone runner and callback coordinates</name>
  <precondition>`SIGRA_IOS_DEVICE_UDID` and `SIGRA_IOS_DEVELOPMENT_TEAM` are present on a self-hosted Apple-silicon macOS runner labeled `self-hosted`, `macOS`, `ARM64`, `sigra-ios-physical`; `xcrun xctrace list devices` resolves the UDID to an attached physical iPhone.</precondition>
  <files>scripts/ci/native-proof-provision.sh, scripts/ci/native-proof-provision.test.sh, test/example/native/native-proof-environment.lock.json</files>
  <read_first>scripts/ci/native-proof-provision.sh; scripts/ci/native-proof-provision.test.sh; test/example/native/native-proof-environment.lock.json; .planning/phases/248-crosswake-native-proof/248-CONTEXT.md; .planning/phases/248-crosswake-native-proof/248-RESEARCH.md</read_first>
  <behavior>
    - Missing runner labels, UDID, signing team, device lookup, physical-platform classification, or Xcode 26.6 fails before any Xcode/Gradle build.
    - The environment lock contains runner class/labels, Xcode build, redacted device class/OS, and exact iOS/Android custom-scheme callbacks; it contains no raw UDID, signing identity, secret, or stable device identifier.
    - Hermetic fake-tool cases prove simulator, unavailable device, wrong architecture, missing label, and secret-bearing lock rejection.
  </behavior>
  <action>Implement the D-10 prerequisite around the selected repository-managed self-hosted Apple-silicon runner contract. Validate the exact four runner labels, Xcode 26.6 build `17F113`, signing availability, and the supplied UDID through `xcrun xctrace list devices`; require `platform=iOS` and reject every simulator/unavailable destination. Atomically write `native-proof-environment.lock.json` only after validation with `ios_runner_class: self_hosted_attached_device`, the exact labels, Xcode version/build, redacted model class and OS, `ios_callback: sigra-native-proof://auth/callback`, `android_callback: sigra-native-proof://auth/android`, and `callback_transport: custom_scheme`. Hash a run-scoped device binding rather than retaining the UDID. Add hermetic fake-command tests; the absent local device is an expected hard failure, not a substitute claim.</action>
  <acceptance_criteria>The provisioning script rejects every non-physical or incompletely configured iOS environment and emits the exact selected runner/callback contract without raw device/signing data.</acceptance_criteria>
  <verify><automated>bash scripts/ci/native-proof-provision.test.sh &amp;&amp; scripts/ci/native-proof-provision.sh --validate-ios</automated></verify>
  <done>The selected NAT-01 runner and both callback transports are machine-locked before native build execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the Android toolchain/browser and generate the verified Gradle wrapper</name>
  <precondition>`ANDROID_SDK_ROOT` is writable on the x86_64 Linux proof runner and stable-channel Google Android packages are reachable.</precondition>
  <files>scripts/ci/native-proof-provision.sh, scripts/ci/native-proof-provision.test.sh, test/example/native/android/toolchain.lock.json, test/example/native/android/gradlew, test/example/native/android/gradlew.bat, test/example/native/android/gradle/wrapper/gradle-wrapper.jar, test/example/native/android/gradle/wrapper/gradle-wrapper.properties</files>
  <read_first>scripts/ci/native-proof-provision.sh; scripts/ci/native-proof-provision.test.sh; test/example/native/android/toolchain.lock.json; test/example/native/android/gradlew; test/example/native/android/gradlew.bat; test/example/native/android/gradle/wrapper/gradle-wrapper.jar; test/example/native/android/gradle/wrapper/gradle-wrapper.properties; .planning/phases/248-crosswake-native-proof/248-RESEARCH.md</read_first>
  <behavior>
    - Only the exact locked Android/Gradle/Maven coordinates validate; missing or mismatched versions and hashes fail before shell compilation.
    - Image-bundled `com.android.chrome` must exist and its exact installed version/APK SHA plus Auth Tab or Custom Tabs capability are captured into the lock before downstream build.
    - The wrapper distribution and committed wrapper JAR are checksum-verified before `./gradlew --version` executes.
  </behavior>
  <action>Install/validate JDK 17, Command-line Tools 23.0, Platform Tools 37.0.1, stable Emulator 37.1.11, `platforms;android-36`, Build Tools 35.0.0, and `system-images;android-36;google_apis_playstore;x86_64` revision 7 with AVD device `pixel_8`. Lock Gradle 8.13 and distribution SHA-256 `20f1b1176237254a6fc204d8434196fa11a4cfb387567519c61556e8710aed78`, AGP 8.13.2, Kotlin 2.2.10, AndroidX Browser 1.9.0, AndroidX Test core/runner 1.7.0, Espresso 3.7.0, and UI Automator 2.4.0. Boot the pinned image only for provisioning, require image-bundled package `com.android.chrome`, capture its non-empty exact version and APK SHA-256 plus verified `auth_tab` or `custom_tab_fallback` capability, then stop/clean the AVD. Generate the standard wrapper files from the checksum-verified Gradle distribution, set `distributionSha256Sum` in `gradle-wrapper.properties`, record the wrapper JAR SHA in `toolchain.lock.json`, and verify both checksums before running `./gradlew --version`. Tests must reject mutable aliases, absent browser identity/capability, any coordinate mismatch, or wrapper tampering per D-11.</action>
  <acceptance_criteria>`toolchain.lock.json` contains every exact coordinate plus browser/wrapper hashes; the four wrapper files are committed; hermetic mismatch/tamper cases fail; verified `./gradlew --version` reports Gradle 8.13.</acceptance_criteria>
  <verify><automated>bash scripts/ci/native-proof-provision.test.sh &amp;&amp; scripts/ci/native-proof-provision.sh --validate-android &amp;&amp; cd test/example/native/android &amp;&amp; ./gradlew --no-daemon --console=plain --version</automated></verify>
  <done>NAT-02 consumes a complete exact pre-build lock and an executable checksum-pinned Gradle wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| runner/device discovery → environment lock | Mutable tool output must prove the selected physical class without retaining stable identifiers. |
| remote Android distributions/image → committed wrapper/toolchain lock | Downloaded binaries and mutable package aliases must be reduced to exact verified coordinates and hashes before use. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-248-23 | Spoofing | iOS destination/runner labels | high | mitigate | Exact label/architecture/Xcode/device resolution with simulator rejection and hermetic negative tests. |
| T-248-24 | Tampering | Gradle wrapper/Android packages | high | mitigate | Exact versions, official Gradle distribution SHA, wrapper-JAR SHA, stable-channel package checks, and pre-build validation. |
| T-248-25 | Information disclosure | environment locks | high | mitigate | Strict allowlists, run-scoped device digest, and rejection of UDID/signing/secret/stable-identity fields. |
| T-248-SC | Tampering | Maven packages | medium | mitigate | Exact official Google/AndroidX coordinates are locked before resolution and downstream dependency output is compared to the lock. |
</threat_model>

## Artifacts this phase produces

- `test/example/native/native-proof-environment.lock.json` with exact fields `schema_version`, `ios_runner_class`, `ios_runner_labels`, `runner_arch`, `xcode_version`, `xcode_build`, `ios_device_class`, `ios_os_version`, `ios_device_binding_digest`, `ios_callback`, `android_callback`, `callback_transport`, and `complete`; enums are `self_hosted_attached_device`, `arm64`, `custom_scheme`, and `complete: true`.
- `test/example/native/android/toolchain.lock.json` with exact fields `schema_version`, `jdk`, `cmdline_tools`, `platform_tools`, `emulator`, `sdk_platform`, `build_tools`, `system_image`, `system_image_revision`, `abi`, `avd_device`, `browser_package`, `browser_version`, `browser_apk_sha256`, `browser_mode`, `gradle`, `gradle_distribution_sha256`, `gradle_wrapper_jar_sha256`, `agp`, `kotlin`, `androidx_browser`, `test_core`, `test_runner`, `espresso`, `uiautomator`, and `complete`; `browser_mode` is exactly `auth_tab` or `custom_tab_fallback`.
- Standard wrapper files `gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar`, and `gradle/wrapper/gradle-wrapper.properties`, with Gradle 8.13 URL and `distributionSha256Sum` pin.
- `scripts/ci/native-proof-provision.sh` and hermetic `scripts/ci/native-proof-provision.test.sh`.

<verification>Run both task commands and require downstream Plans 248-03 and 248-05 to consume these locks without changing them.</verification>
<success_criteria>Native implementation cannot begin until the exact selected physical runner, callbacks, Android coordinates/browser, and Gradle wrapper are validated and committed.</success_criteria>
<output>Create `.planning/phases/248-crosswake-native-proof/248-08-SUMMARY.md` when done.</output>
