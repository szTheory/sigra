---
phase: 52
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phases **44** and **45** in **`.planning/ROADMAP.md`** use the same **✅ (2026-04-21)** completion convention as phases **48** and **49** on the **v1.4** table rows."
    - "A dedicated reader note subsection exists whose title starts with **`## Reader note:`** and body mentions **`47`** (or **47–49**) and **`*-VERIFICATION.md`** so **AUD-06..AUD-08** closure is not misread as missing."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Single honest story for implementation rows 44–45 vs verification phases 47–49"
nyquist_compliant: false
---

<objective>
Reconcile **`.planning/ROADMAP.md`** so phases **44–45** no longer imply unfinished audit work now that **47–49** shipped formal verification — without rewriting **REQUIREMENTS.md** traceability (out of scope per **52-CONTEXT.md**).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-52-01 | Spoofing | ROADMAP claims verify pass without files | medium | Reader note explicitly points at **48/49** and **`*-VERIFICATION.md`** paths; plan **52-02** adds existence checks in ExUnit |
| T-52-02 | Repudiation | Checkmarks added with wrong date | low | Use **`2026-04-21`** to match adjacent completed rows **46–49** |
| T-52-03 | Tampering | Supersession note removed later undetected | low | Contract test in **52-02** pins key substrings |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Mark phases 44 and 45 complete in ROADMAP table</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/52-roadmap-nyquist-milestone-honesty/52-CONTEXT.md
    - .planning/phases/52-roadmap-nyquist-milestone-honesty/52-RESEARCH.md
  </read_first>
  <action>
    In the **v1.4** phases table (same table as phase **48**):

    1. On the row for phase **44**, change the first cell from **`| **44** |`** to **`| **44** ✅ (2026-04-21) |`** — mirror the formatting style of the **48** row (`✅` + space + paren date).
    2. On the row for phase **45**, change **`| **45** |`** to **`| **45** ✅ (2026-04-21) |`** the same way.

    Do **not** alter phases **41–43**, **46–49**, **50–52** cells except if required to fix a merge conflict.
  </action>
  <acceptance_criteria>
    - `grep -F "| **44** ✅ (2026-04-21) |" .planning/ROADMAP.md` exits 0.
    - `grep -F "| **45** ✅ (2026-04-21) |" .planning/ROADMAP.md` exits 0.
    - `grep -F "**Numbering:**" .planning/ROADMAP.md` exits 0 (table footer intact).
  </acceptance_criteria>
  <verify>
    `test -f .planning/ROADMAP.md` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Add reader note subsection (44–45 vs 47–49)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Immediately **after** the closing line of the **v1.4** markdown table (the row whose first cell is **`| **52** |`** — i.e. after the table’s final `|`), **before** the existing **`**Numbering:**`** paragraph, insert this exact markdown block (preserve blank line after table if one exists):

    ```markdown
    ## Reader note: phases 44–45 vs 47–49 (AUD closure)

    Phases **47–49** published the formal **`*-VERIFICATION.md`** merge gates and reconciled **REQUIREMENTS.md** for **AUD-04..AUD-08** (scoped work in phases **43–45**). Rows **44** and **45** above name the *implementation* batches (MFA/account/API and OAuth/C-1); the matching *verification + REQ closure* milestones are **48** and **49**. **Nyquist** sampling posture for historical batch **41–44** remains owned by phase **50** and **`MAINTAINING.md`** → **`## Nyquist policy (phases 41-44)`** — per-phase **`nyquist_compliant: false`** in **41–44 `*-VALIDATION.md`** is intentional unless the team explicitly elevates it.

    ```

    Ensure the heading is **exactly** `## Reader note: phases 44–45 vs 47–49 (AUD closure)` (ASCII hyphen in **44–45** and **47–49**).
  </action>
  <acceptance_criteria>
    - `grep -F "## Reader note: phases 44–45 vs 47–49 (AUD closure)" .planning/ROADMAP.md` exits 0.
    - `grep -F "48** and **49" .planning/ROADMAP.md` exits 0.
    - `grep -F "## Nyquist policy (phases 41-44)" .planning/ROADMAP.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep -F "Reader note" .planning/ROADMAP.md | head -1` prints a line starting with `## Reader note:`.
  </verify>
</task>

</tasks>

<verification>
- `grep -F "| **44** ✅ (2026-04-21) |" .planning/ROADMAP.md` and `grep -F "## Reader note: phases 44–45 vs 47–49 (AUD closure)" .planning/ROADMAP.md` both exit 0.
</verification>

<success_criteria>
- ROADMAP presents a **single** reconciled story: implementation rows **44–45** are complete **and** readers are routed to **47–49** for verification evidence.
- No edits to **`.planning/REQUIREMENTS.md`** in this plan.
</success_criteria>
