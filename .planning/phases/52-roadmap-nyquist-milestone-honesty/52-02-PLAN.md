---
phase: 52
plan: 02
type: execute
wave: 1
depends_on:
  - "52-01"
files_modified:
  - .planning/v1.4-MILESTONE-AUDIT.md
  - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
  - test/sigra/planning/phase_52_milestone_honesty_contract_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "**`v1.4-MILESTONE-AUDIT.md`** contains heading **`## Tech debt disposition (phase 52)`** with body referencing **missing *-VERIFICATION** as **superseded** by phases **47–49**."
    - "**`50-VERIFICATION.md`** Notes mention **phase 52** ROADMAP gate so **50** and **52** stay cross-linked."
    - "**`phase_52_milestone_honesty_contract_test.exs`** asserts ROADMAP reader-note heading + existence of **43/44/45 `*-VERIFICATION.md`**."
nyquist_compliant: false
---

<objective>
Disposition stale **`tech_debt`** narrative in **`v1.4-MILESTONE-AUDIT.md`**, cross-link **phase 50** verification notes to **phase 52**, and add an ExUnit **doc contract** so honesty requirements cannot regress silently.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-52-04 | Information disclosure | Audit file exposes secrets | low | Markdown only — no credentials |
| T-52-05 | Repudiation | Disposition text contradicts git history | low | Cite phase numbers **47–49** and file paths literally |
| T-52-06 | Elevation of privilege | N/A for docs | — | — |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add tech debt disposition section to milestone audit</name>
  <files>.planning/v1.4-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v1.4-MILESTONE-AUDIT.md
    - .planning/ROADMAP.md
    - .planning/phases/52-roadmap-nyquist-milestone-honesty/52-RESEARCH.md
  </read_first>
  <action>
    Open **`.planning/v1.4-MILESTONE-AUDIT.md`**. In the Markdown body (after the second `---` delimiter that ends YAML), insert a new section **immediately after** the first-line H1 **`# Milestone audit — v1.4 GA readiness & audit trail completeness`** and the blank line following it, **before** the **`## Verdict`** heading:

    **Heading (exact):** `## Tech debt disposition (phase 52)`

    **Body (paragraphs, include these verbatim sentences or equivalent merge-friendly wording):**

    1. `Phases 47–49 published *-VERIFICATION.md for phases 43–45; the tech_debt YAML entries that claim *-VERIFICATION.md is missing for 43–45 are superseded as-of phase 52 planning — treat those bullets as historical audit capture, not current blockers.`
    2. `Nyquist nyquist_compliant false on 41–44 *-VALIDATION.md remains intentional batch posture per MAINTAINING.md § Nyquist policy (phases 41-44) until explicitly elevated — not automatically a bug.`
    3. `Golden / long-budget class notes (e.g. golden_diff_test.exs timeout class) remain tracked via MAINTAINING.md installer golden contract + phase 50 CI attestation — disposition is policy documentation, not deletion of the YAML tech_debt block.`

    Do **not** delete the existing `tech_debt:` YAML block; append interpretive Markdown only.
  </action>
  <acceptance_criteria>
    - `grep -F "## Tech debt disposition (phase 52)" .planning/v1.4-MILESTONE-AUDIT.md` exits 0.
    - `grep -F "47–49" .planning/v1.4-MILESTONE-AUDIT.md` exits 0.
    - `grep -F "superseded" .planning/v1.4-MILESTONE-AUDIT.md` exits 0.
  </acceptance_criteria>
  <verify>
    `wc -l .planning/v1.4-MILESTONE-AUDIT.md` reports line count ≥ prior baseline (no accidental truncation).
  </verify>
</task>

<task type="execute">
  <name>Task 2: Cross-link phase 50 verification Notes to phase 52</name>
  <files>.planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
    - .planning/phases/52-roadmap-nyquist-milestone-honesty/52-01-PLAN.md
  </read_first>
  <action>
    In **`.planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md`**, locate the **`## Notes`** section (create it at file end before EOF if absent).

    Append a bullet line **exactly** (Markdown code spans use single backticks):

    `- **Phase 52:** ROADMAP reader honesty for phases **44–45** vs **47–49** — see `.planning/ROADMAP.md` reader note and `test/sigra/planning/phase_52_milestone_honesty_contract_test.exs`.`
  </action>
  <acceptance_criteria>
    - `grep -F "Phase 52:" .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0.
    - `grep -F "phase_52_milestone_honesty_contract_test.exs" .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0.
  </acceptance_criteria>
  <verify>
    `grep "## Notes" .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Add phase 52 milestone honesty ExUnit contract</name>
  <files>test/sigra/planning/phase_52_milestone_honesty_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_50_nyquist_docs_contract_test.exs
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Create **`test/sigra/planning/phase_52_milestone_honesty_contract_test.exs`** with:

    - `use ExUnit.Case, async: true`
    - `@moduledoc` one sentence: encodes **52-VALIDATION** / **52-RESEARCH** ROADMAP honesty checks.
    - Private **`root/0`** same pattern as **`Phase50NyquistDocsContractTest`** (`Path.expand("../../..", __DIR__)`).
    - Private **`read!/1`** same as phase **50** test.
    - **`test`** `"ROADMAP: reader note and completed 44/45 rows"` asserting **`read!(".planning/ROADMAP.md")`** contains:
      - substring **`## Reader note: phases 44–45 vs 47–49 (AUD closure)`**
      - substring **`| **44** ✅ (2026-04-21) |`**
      - substring **`| **45** ✅ (2026-04-21) |`**
    - **`test`** `"verification files exist for phases 43–45"` asserting **`File.exists?/1`** for:
      - **`.planning/phases/43-audit-inventory-auth-atomic-batch/43-VERIFICATION.md`**
      - **`.planning/phases/44-mfa-account-api-atomic-batches/44-VERIFICATION.md`**
      - **`.planning/phases/45-oauth-ops-c1-signoff/45-VERIFICATION.md`**
      (join `root()` with each relative path).
    - **`test`** `"milestone audit disposition heading present"` asserting **`read!(".planning/v1.4-MILESTONE-AUDIT.md")`** includes **`## Tech debt disposition (phase 52)`**.

    Match **ExUnit** style / naming conventions from **`phase_50_nyquist_docs_contract_test.exs`**.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test test/sigra/planning/phase_52_milestone_honesty_contract_test.exs` exits 0 **after** plans **52-01** and task **1–2** of **52-02** are applied (executor order).
    - `mix format --check-formatted test/sigra/planning/phase_52_milestone_honesty_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    `MIX_ENV=test mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_52_milestone_honesty_contract_test.exs` exits 0.
  </verify>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_52_milestone_honesty_contract_test.exs` exits 0.
</verification>

<success_criteria>
- Stale **missing *-VERIFICATION** framing is explicitly **superseded** in prose.
- **50** and **52** artifacts reference each other.
- Contract tests prevent ROADMAP regression without re-reading the entire audit.
</success_criteria>
