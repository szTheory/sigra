---
phase: 217-adversarial-panel-auto-fix-safety-rails
plan: 02
type: execute
wave: 2
depends_on: ["217-01"]
files_modified:
  - scripts/ci/fix-queue-build.mjs
  - scripts/ci/fix-queue-lint.sh
  - scripts/ci/fix-queue-build.test.mjs
  - scripts/ci/admin-eval-harness.sh
  - guides/reference/fix-queue.json
  - guides/reference/admin-render-sha.json
autonomous: true
requirements: [AUTOFIX-01]
files_note: "admin-render-sha.json write-path is relocated here (sole writer becomes fix-queue-build.mjs); the ledger values are unchanged unless a rebuild recomputes them."

must_haves:
  truths:
    - "fix-queue.json is a committed, sorted, derived open-set queue where open = built(findings.json) - settled(settled-findings.tsv)."
    - "Every derived field (auto_eligible, priority, systemic_group) is recomputed by fix-queue-lint.sh and fails on drift."
    - "Any anchor recurring across >=2 surfaces collapses into a single high-priority systemic parent floated to the top of the queue."
    - "fix-queue-build.mjs is the SOLE writer of open_findings in admin-render-sha.json; no other producer writes that field."
    - "auto_eligible is DERIVED as fix_class in {copy, token} and never trusted from a typed bit."
  artifacts:
    - scripts/ci/fix-queue-build.mjs
    - scripts/ci/fix-queue-lint.sh
    - guides/reference/fix-queue.json
  key_links:
    - "fix-queue-build.mjs reads findings.json bundles + settled-findings.tsv; writes fix-queue.json AND open_findings in admin-render-sha.json (one builder, two outputs)."
    - "admin-eval-harness.sh chains fix-queue-build.mjs in the same wave the old open_findings writer is removed (Pitfall 3 ordering)."
  prohibitions:
    - "open_findings must live ONLY in admin-render-sha.json — never duplicated into fix-queue.json."
    - "class-chain-anchored findings are policy fix_class=judgment (never auto), because auto-editing class= would change the finding_id."
    - "The fix queue must never be gitignored — it must diff against merge-base."
---

<objective>
Build the deterministic fix queue (AUTOFIX-01): a new `scripts/ci/fix-queue-build.mjs` that derives a committed, sorted `guides/reference/fix-queue.json` from the `findings.json` bundles, keyed by the byte-stable `finding_id`, with cross-surface systemic collapse; plus `fix-queue-lint.sh` that recomputes every derived field. Relocate the sole writer of `open_findings` in `admin-render-sha.json` into `fix-queue-build.mjs` (D-12), killing the hand-maintained-count drift, and chain it into `admin-eval-harness.sh` in the same wave the old writer is removed (Pitfall 3).

Purpose: The fix queue is the operator's highest-value-first worklist and the input to the auto-fix loop (Plan 06); making `fix-queue-build.mjs` the one authoritative writer of `open_findings` closes the two-producer drift hazard.
Output: `fix-queue-build.mjs`, `fix-queue-lint.sh`, a self-test, the committed `fix-queue.json`, and the rewired harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-CONTEXT.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-RESEARCH.md

@scripts/ci/admin-eval-harness.sh
@scripts/ci/settled-findings-lint.sh
@scripts/ci/quality-findings-monotonic.sh
@guides/reference/admin-render-sha.json
@guides/reference/settled-findings.tsv
@guides/reference/admin-eval-schema.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fix-queue-build.mjs — derive the open-set queue + become sole open_findings writer</name>
  <read_first>guides/reference/admin-render-sha.json, guides/reference/settled-findings.tsv, guides/reference/admin-eval-schema.md, scripts/panel/panel-schema.mjs</read_first>
  <files>scripts/ci/fix-queue-build.mjs, scripts/ci/fix-queue-build.test.mjs, guides/reference/fix-queue.json</files>
  <behavior>
    - Test 1: given synthetic findings.json bundles + a settled-findings.tsv, the built queue contains exactly `built - settled` finding_ids (settled rows excluded).
    - Test 2: an anchor appearing on >=2 surfaces produces ONE systemic parent entry with high priority floated to the top; single-surface anchors do not.
    - Test 3: `auto_eligible` is recomputed as `fix_class in {copy, token}`; a `component`/`judgment` finding is not auto_eligible even if a typed bit said otherwise.
    - Test 4: `open_findings` written into admin-render-sha.json equals the per-cell `built - settled` count; the value is written ONLY here.
  </behavior>
  <action>Per D-12: create `scripts/ci/fix-queue-build.mjs` (Node built-ins + `createRequire`-from-Playwright only where a dep is needed; reuse the `findingId` helper from `scripts/panel/panel-schema.mjs`). It (a) reads all `findings.json` bundles under the eval bundle dir, (b) subtracts settled rows from `settled-findings.tsv` (`open = built - settled`), (c) emits `guides/reference/fix-queue.json`: a sorted array of `{finding_id, surface, class, anchor, lens, severity, fix_class, auto_eligible, systemic_group, priority}` where `fix_class` classification is `copy|token|component|judgment` (class-chain-anchored findings are forced to `judgment` per D-12), `auto_eligible` is DERIVED (`fix_class in {copy,token}`), `systemic_group = sha256(class + "\0" + anchor)`, and `priority` is DERIVED (systemic parents highest), (d) collapses any anchor recurring across >=2 surfaces into a single high-priority systemic parent floated to the top, and (e) becomes the SOLE writer of `open_findings` per cell in `admin-render-sha.json` (write render_sha256 through unchanged; only recompute/write open_findings). Sort deterministically (systemic parents first, then by priority, then finding_id) so the committed file diffs cleanly. Author `fix-queue-build.test.mjs` per Behavior.</action>
  <verify>
    <automated>node scripts/ci/fix-queue-build.test.mjs</automated>
  </verify>
  <done>Self-test passes; `fix-queue.json` is derived + sorted; `open_findings` is written only by this builder; systemic collapse floats cross-surface anchors to the top.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fix-queue-lint.sh — recompute derived fields + assert open = built - settled</name>
  <read_first>scripts/ci/settled-findings-lint.sh (recompute idiom), scripts/ci/fix-queue-build.mjs</read_first>
  <files>scripts/ci/fix-queue-lint.sh, scripts/ci/fix-queue-lint.test.sh</files>
  <behavior>
    - Test 1: a fix-queue.json with a tampered `auto_eligible` (true on a `judgment` finding) makes the lint exit non-zero.
    - Test 2: a tampered `priority`/`systemic_group` makes the lint exit non-zero.
    - Test 3: an `open_findings` in admin-render-sha.json that does not equal `built - settled` makes the lint exit non-zero.
    - Test 4: a clean, freshly-built queue passes (exit 0).
  </behavior>
  <action>Create `scripts/ci/fix-queue-lint.sh` (bash guard, Node at the leaf via `node -e` per house idiom, no jq). It recomputes `auto_eligible`, `priority`, and `systemic_group` from the queue rows and fails on any drift; it asserts `open_findings == built - settled` per cell against `admin-render-sha.json`; it validates the queue is valid JSON, sorted, and has no duplicate finding_id. Clone the mktemp-hermetic self-test idiom from `settled-findings-lint.test.sh` for `fix-queue-lint.test.sh` per Behavior.</action>
  <verify>
    <automated>bash scripts/ci/fix-queue-lint.test.sh</automated>
  </verify>
  <done>Lint self-test passes; tampered derived fields and a wrong open_findings each trip a non-zero exit; a clean queue passes.</done>
</task>

<task type="auto">
  <name>Task 3: Chain fix-queue-build.mjs into admin-eval-harness.sh (Pitfall 3 ordering)</name>
  <read_first>scripts/ci/admin-eval-harness.sh</read_first>
  <files>scripts/ci/admin-eval-harness.sh, guides/reference/admin-render-sha.json</files>
  <action>Per Pitfall 3: wire `node "${ROOT}/scripts/ci/fix-queue-build.mjs"` into `admin-eval-harness.sh` at the point where `open_findings` must be written (BEFORE `quality-findings-monotonic.sh` reads it), and REMOVE the old `open_findings` writer in the SAME change so there is never a window with an unwritten count. Add `bash "${ROOT}/scripts/ci/fix-queue-lint.sh"` to the harness's derivative-guard chain (alongside `settled-findings-lint.sh`). Regenerate `admin-render-sha.json` open_findings via the new builder and commit the resulting ledger only after the builder produced it. Update the harness header comment noting `fix-queue-build.mjs` is now the sole `open_findings` writer. Do NOT attach the harness to a new CI lane — it stays in the existing non-required render job (JUDGE-CI-01).</action>
  <verify>
    <automated>bash scripts/ci/fix-queue-lint.sh && bash scripts/ci/quality-findings-monotonic.sh --base HEAD</automated>
  </verify>
  <done>Harness chains the builder before the count guard; the old writer is gone; `fix-queue-lint.sh` passes and `quality-findings-monotonic.sh` diffs a valid `open_findings`; no new CI lane wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| findings.json bundles -> committed ledgers | Untrusted-ish rendered evidence is aggregated into a committed, merge-gating count |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-217-02-DRIFT | Tampering | open_findings dual-writer window | high | mitigate | Sole-writer relocation + harness chaining happen in one change (Task 3); `fix-queue-lint.sh` asserts `open == built - settled` so a stale/hand-edited count fails fast |
| T-217-02-EOP | Elevation of Privilege | panel findings inflating the gating count | high | mitigate | Builder reads only `findings.json` (probe findings), never `panel-findings.json`; panel findings can never enter open_findings |
| T-217-02-PARSE | Tampering | JSON/TSV parsing of ledgers | low | mitigate | Ledgers are JSON.parse'd / tab-split, never eval'd; no shell interpolation of finding text |
</threat_model>

<verification>
- `node scripts/ci/fix-queue-build.test.mjs` passes.
- `bash scripts/ci/fix-queue-lint.test.sh` passes.
- `bash scripts/ci/fix-queue-lint.sh` passes on the committed `fix-queue.json`.
- `bash scripts/ci/quality-findings-monotonic.sh --base HEAD` reads a valid `open_findings` written by the builder.
- `grep -rn 'admin-panel.sh\|admin-autofix-loop.sh' .github/workflows/*.yml` returns nothing (no CI wiring introduced).
</verification>

<success_criteria>
A committed, derived, sorted `fix-queue.json` exists with systemic collapse; `fix-queue-build.mjs` is the sole `open_findings` writer chained into the harness before the count guard; `fix-queue-lint.sh` recomputes every derived field.
</success_criteria>

<output>
Create `.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-02-SUMMARY.md` when done
</output>
