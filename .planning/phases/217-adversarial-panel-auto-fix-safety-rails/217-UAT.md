---
status: resolved
phase: 217-adversarial-panel-auto-fix-safety-rails
source: [217-VERIFICATION.md]
started: 2026-07-04T20:45:00Z
updated: 2026-07-04T20:10:00Z
resolved_by: 217-08
---

## Current Test

[complete — both outstanding items closed by gap-closure plan 217-08]

## Tests

### 1. SC-2 live zero-calls reality-check
expected: 2nd admin-panel.sh run reports 0 API calls; admin-panel-verdicts.json shows no diff. Requires real ANTHROPIC_API_KEY + live example server + prior resolution of the surface mismatch (and the judge.mjs CLI-wiring stub). Mechanism already hermetically proven by judge.test.mjs 11/11 (callCount===0 path).
result: resolved
resolved_by: 217-08
resolution: "The blocking prerequisite (panel/render surface mismatch) is closed — 217-08 aligned admin-panel.sh PILOT_SURFACES with admin-render-sha.json on the same board-mg-5/9 surfaces (LOCKED Option 2), so a live run now enumerates cells that have real captured bundles on disk. The zero-calls mechanism is now proven KEY-FREE by the new deterministic judge-cli.test.mjs (4/4, no skip): a real on-disk board-mg-5 bundle yields callCount===0 on a cache hit and k=3 on a cache miss via an injected SDK double. judge.mjs refuses paid calls on an empty DOM BEFORE importing/constructing the Anthropic SDK (ordering verified). The remaining TRUE-live paid run (real key + booted server) is documented as an OPTIONAL, post-merge, operator-only confirmation in admin-eval-runbook.md — it is NOT a task gate and does not hold the phase (see 217-VERIFICATION.md optional_operator_confirmation)."

### 2. SC-4 live board-autofix-seed companion
expected: On a clean tree at final committed HEAD, the autofix loop against the seeded board-autofix-seed finding produces a `Revert "autofix(...)"` commit, restores admin-award-ledger.json to pre-loop state, writes the reverted finding to settled-findings.tsv (disposition=waived), and leaves a clean reflog (no force-push / reset --hard).
result: passed
resolved_by: 217-08
resolution: "Now AUTONOMOUS and API-free. 217-08 seeded one appliable in-band SPACE-token finding (12.5px inline padding on #board-autofix-seed; fix-queue entry measured_px:[12.5] + 10-entry scale_px → var(--sg-space-12), no token_family) and runs the apply→rail-trip→revert→waive chain inside a THROWAWAY git clone of the final committed HEAD — real repo history untouched. Asserted by an automated <verify> block (git log / reflog / ledger diff / settled-findings.tsv), not operator attestation. The phase verifier independently re-ran the throwaway-clone chain on committed HEAD and reproduced: seed applied → var(--sg-space-12), rail-1 tripped, git revert --no-edit landed the Revert commit, ledger restored, finding settled=waived, reflog clean, real working tree + settled-findings.tsv untouched."

## Summary

total: 2
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0
resolved: 1

## Gaps

[none — both outstanding items resolved by gap-closure plan 217-08; the sole residual is the OPTIONAL post-merge operator-only TRUE-live SC-2 paid run, which is explicitly non-gating]
