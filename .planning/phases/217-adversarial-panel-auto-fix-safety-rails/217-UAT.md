---
status: partial
phase: 217-adversarial-panel-auto-fix-safety-rails
source: [217-VERIFICATION.md]
started: 2026-07-04T20:45:00Z
updated: 2026-07-04T23:05:00Z
---

## Current Test

[testing paused — 2 items outstanding, both blocked on gap-closure]

## Tests

### 1. SC-2 live zero-calls reality-check
expected: 2nd admin-panel.sh run reports 0 API calls; admin-panel-verdicts.json shows no diff. Requires real ANTHROPIC_API_KEY + live example server + prior resolution of the surface mismatch (and the judge.mjs CLI-wiring stub). Mechanism already hermetically proven by judge.test.mjs 11/11 (callCount===0 path). Tracked: .planning/todos/pending/2026-07-04-panel-pilot-surface-render-mismatch.md
result: blocked
blocked_by: prior-phase
reason: "Blocked on the unresolved surface-mismatch gap-closure (panel targets users-index-live/user-show-live but the 217 render matrix renders board-mg-*; disjoint surface sets make a live run make 0 calls for the wrong reason). Also requires a real ANTHROPIC_API_KEY + booted example server. Mechanism hermetically proven (judge.test.mjs 11/11, callCount===0 skip path). IN-01 advanced the judge CLI wiring (reads dom.html/facts.json, refuses paid calls on empty DOM) but the surface scoping gap itself remains open. Deferred to gap-closure per operator decision 2026-07-04. Tracked: .planning/todos/pending/2026-07-04-panel-pilot-surface-render-mismatch.md"

### 2. SC-4 live board-autofix-seed companion
expected: On a clean tree at final committed HEAD, `bash scripts/ci/admin-autofix-loop.sh --max-fixes 5` against board-mg-* bundles produces a `Revert "autofix(...)"` commit, restores admin-award-ledger.json to pre-loop state, writes the reverted finding to settled-findings.tsv (disposition=waived), and leaves a clean reflog (no force-push / reset --hard). Deferred to avoid landing fix/revert commits on main outside a gap-closure plan. Mechanism already hermetically proven by admin-autofix-loop.test.sh 9/9 (both rails fire).
result: blocked
blocked_by: prior-phase
reason: "Runnable in principle (board-mg bundles + 12 auto_eligible token findings exist) but lands a real autofix commit + Revert commit on main; deferred to gap-closure to avoid touching main's git history outside a gap-closure plan. Mechanism hermetically proven (admin-autofix-loop.test.sh 9/9, both rails fire; IN-05 strengthened the fixture so a REFUSED/SKIP outcome now fails and a Revert commit is required). Tracked alongside SC-2 in the same gap-closure todo."

## Summary

total: 2
passed: 0
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

[none — both outstanding items are prerequisite-gated (gap-closure), not code defects]
