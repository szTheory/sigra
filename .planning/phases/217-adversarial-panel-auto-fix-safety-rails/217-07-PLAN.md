---
phase: 217-adversarial-panel-auto-fix-safety-rails
plan: 07
type: execute
wave: 4
depends_on: ["217-03", "217-05", "217-06"]
files_modified:
  - scripts/ci/admin-panel.sh
  - guides/reference/admin-eval-runbook.md
autonomous: false
requirements: [PANEL-01, PANEL-02, AUTOFIX-02]
user_setup:
  - service: anthropic
    why: "The two off-CI live verifications (SC-2 zero-calls reality, SC-4 board-autofix-seed companion) require a real ANTHROPIC_API_KEY. No merge gate ever needs it."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (env-only; NEVER committed or logged)"

must_haves:
  truths:
    - "admin-panel.sh resolves bundles by app_git_sha = git rev-parse HEAD and hard-degrades to exit 0 (skip-with-warning) when ANTHROPIC_API_KEY is unset."
    - "admin-panel.sh warns/skips (not hard-fail) on stale bundles vs HEAD, defaults to pilot surfaces, requires --all to fan out, and prints an estimated call count first."
    - "admin-panel.sh never writes any git-tracked ledger the deterministic guards read."
    - "The runbook documents the off-CI LLM-panel step, the API-key no-op degrade, and where the human sign-off sits."
    - "SC-2 zero-calls reality is proven by a live off-CI run; SC-4 board-autofix-seed companion is proven by a live off-CI run on a clean tree at final committed HEAD."
  artifacts:
    - scripts/ci/admin-panel.sh
    - guides/reference/admin-eval-runbook.md
  key_links:
    - "admin-panel.sh invokes scripts/panel/judge.mjs (Plan 05) after the API-key/freshness preconditions."
    - "The runbook extends admin-eval-runbook.md's 'Phase 217 will add the LLM-panel step (off-CI)' note."
  prohibitions:
    - "admin-panel.sh must NEVER be wired into any CI lane (JUDGE-CI-01)."
    - "admin-panel.sh must never write a git-tracked deterministic ledger."
    - "ANTHROPIC_API_KEY must never be echoed/logged by the no-op degrade path."
---

<objective>
Deliver the operator invocation (`scripts/ci/admin-panel.sh`) that fronts the judge, plus the runbook update, and perform the two off-CI live verifications that prove reality (SC-2 zero-calls, SC-4 board-autofix-seed companion) under 216-09 SC-5 discipline. The `admin-panel.sh` Hammer no-op (`exit 0` on missing `ANTHROPIC_API_KEY`) is the structural JUDGE-CI-01 guarantee.

Purpose: This wires the deterministic guards (Plans 03/05) to the advisory judge (Plan 05) and the loop (Plan 06) into a single operator entrypoint, and closes the two SC verifications that only a real API-key run can prove. All of it stays off the merge path.
Output: `admin-panel.sh`, an updated `admin-eval-runbook.md`, and two completed off-CI live verifications recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-CONTEXT.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-RESEARCH.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-VALIDATION.md

@scripts/ci/admin-eval-harness.sh
@scripts/panel/judge.mjs
@scripts/ci/admin-autofix-loop.sh
@guides/reference/admin-eval-runbook.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-03-SUMMARY.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-05-SUMMARY.md
@.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin-panel.sh — operator invocation with the Hammer no-op degrade</name>
  <read_first>scripts/ci/admin-eval-harness.sh (structural clone target), scripts/panel/judge.mjs</read_first>
  <files>scripts/ci/admin-panel.sh</files>
  <action>Per D-02: create the thin `scripts/ci/admin-panel.sh` cloned structurally from `admin-eval-harness.sh`. It (a) resolves bundles by `app_git_sha = git rev-parse HEAD`; (b) HARD-DEGRADES to `exit 0` (skip-with-warning) when `ANTHROPIC_API_KEY` is unset — the Hammer no-op idiom that structurally guarantees JUDGE-CI-01 (a missing key can only ever pass); the warning must NOT echo the key; (c) enforces a bundle-freshness precondition (warn/skip, not hard-fail, if bundles are stale vs HEAD — do not burn API tokens on a stale render); (d) defaults to the pilot surfaces and requires `--all` to fan out (Phase-218 scope), printing an estimated call count FIRST; (e) NEVER writes any git-tracked ledger the deterministic guards read — it invokes `scripts/panel/judge.mjs`, which writes only the parallel `panel-findings.json` (gitignored) + the committed `admin-panel-verdicts.json` cache. Do NOT wire this into any CI lane.</action>
  <verify>
    <automated>bash -n scripts/ci/admin-panel.sh && env -u ANTHROPIC_API_KEY bash scripts/ci/admin-panel.sh; test $? -eq 0 && bash scripts/ci/panel-ci-isolation.test.sh</automated>
  </verify>
  <done>`admin-panel.sh` syntax-checks; with no API key it exits 0 with a warning (no key echoed); the CI-isolation test confirms it is not wired into any lane.</done>
</task>

<task type="auto">
  <name>Task 2: Update admin-eval-runbook.md with the off-CI LLM-panel + auto-fix loop step</name>
  <read_first>guides/reference/admin-eval-runbook.md (the existing 'Phase 217 will add the LLM-panel step' note)</read_first>
  <files>guides/reference/admin-eval-runbook.md</files>
  <action>Extend `admin-eval-runbook.md` (ADD-only where possible): document the off-CI LLM-panel step (`admin-panel.sh` — how to run, the `ANTHROPIC_API_KEY` requirement + no-op degrade, `--all` fan-out + estimated call count, pilot-surface default), the auto-fix loop (`admin-autofix-loop.sh` — `--max-fixes` default, the three rails, `git revert` auto-revert, the poison-set), where the human sign-off sits (the operator reviews the fix-queue + verdicts + before/after; the panel is advisory and off-CI throughout), and how to read the dossier (`fix-queue.json`, `admin-panel-verdicts.json`, the gitignored `panel-findings.json`/`admin-panel-report.md`). State the JUDGE-CI-01 invariant explicitly: neither `admin-panel.sh` nor `admin-autofix-loop.sh` is ever in `fast_checks` or any merge-blocking gate; only their deterministic derivatives gate.</action>
  <verify>
    <automated>grep -q 'admin-panel.sh' guides/reference/admin-eval-runbook.md && grep -q 'admin-autofix-loop.sh' guides/reference/admin-eval-runbook.md && grep -qi 'JUDGE-CI-01\|off-CI\|advisory' guides/reference/admin-eval-runbook.md</automated>
  </verify>
  <done>The runbook documents both off-CI tools, the no-op degrade, the three rails, the human sign-off locus, and restates JUDGE-CI-01.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Off-CI live verification — SC-2 zero-calls reality + SC-4 board-autofix-seed companion</name>
  <read_first>.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-VALIDATION.md (§ Manual-Only Verifications), .planning/phases/216-harness-foundation-award-gradient/216-CONTEXT.md (216-09 SC-5 committed-HEAD discipline)</read_first>
  <what-built>
    `admin-panel.sh` + `judge.mjs` (content-hash skip, k=3 quorum) and `admin-autofix-loop.sh` + `board-autofix-seed`. These two verifications require a real `ANTHROPIC_API_KEY` and prove REALITY (the hermetic tests already prove the WIRING). Neither is a merge gate.
  </what-built>
  <how-to-verify>
    Preconditions: `export ANTHROPIC_API_KEY=...`; boot the example server (`scripts/db/up.sh`, then the example on port 4011 per the runbook); ensure a CLEAN tree at the final committed HEAD (216-09 SC-5 trap: a pre-commit render sha is invalid).
    SC-2 (zero-calls reality):
    1. Run `bash scripts/ci/admin-panel.sh` once (populates `admin-panel-verdicts.json` for the pilot surfaces).
    2. Run it AGAIN on the unchanged tree.
    3. Confirm the 2nd run reports 0 API calls (log/report) AND `git diff guides/reference/admin-panel-verdicts.json` is empty.
    SC-4 (board-autofix-seed companion):
    4. On a clean tree at final committed HEAD, run `bash scripts/ci/admin-autofix-loop.sh --max-fixes 5` against the seeded board through the real harness.
    5. Confirm a `Revert "autofix…"` commit appears when a rail trips, the award/count ledger is restored, the reverted finding is in `settled-findings.tsv`, and the reflog is clean (no force-push).
  </how-to-verify>
  <resume-signal>Type "approved" with the observed 2nd-run call count (should be 0) + confirmation of the Revert commit + restored ledger, or describe the discrepancy.</resume-signal>
  <acceptance_criteria>2nd panel run makes 0 API calls with an empty verdicts diff (SC-2 reality); the loop produces a `Revert "autofix…"` commit + restored ledger + settled finding + clean reflog against the seeded board at final committed HEAD (SC-4 reality).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env -> orchestrator | `ANTHROPIC_API_KEY` gates the run; a missing key must fail-safe (no-op pass), never fail-open into a gate |
| orchestrator -> committed ledgers | `admin-panel.sh` must never write a deterministic gating ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-217-07-JUDGE | Elevation of Privilege | panel/loop entering a merge gate | high | mitigate | Hammer no-op (`exit 0` on missing key) is the belt; the CI-isolation test (Plan 03) is the suspenders; `admin-panel.sh` never writes a deterministic ledger |
| T-217-07-KEY | Information Disclosure | key echoed by the no-op path | high | mitigate | The skip-with-warning message names the env var, never its value; verified by an env-unset run + a grep in the verify |
| T-217-07-STALE | Tampering | judging a stale render (216-09 SC-5 trap) | medium | mitigate | Bundle-freshness precondition (warn/skip); the live companion captures on a clean tree at final committed HEAD |
</threat_model>

<verification>
- `bash -n scripts/ci/admin-panel.sh` clean; env-unset run exits 0 with a warning that does not echo the key.
- `bash scripts/ci/panel-ci-isolation.test.sh` passes (admin-panel.sh not wired).
- Runbook documents both off-CI tools + JUDGE-CI-01.
- Off-CI live: 2nd panel run = 0 API calls + empty verdicts diff; loop produces a Revert commit + restored ledger against the seeded board (human-verified).
</verification>

<success_criteria>
The operator entrypoint fronts the judge with a fail-safe no-op degrade and never writes a deterministic ledger; the runbook documents the off-CI loop + sign-off locus; the two live verifications prove SC-2 zero-calls reality and SC-4 companion reality at final committed HEAD.
</success_criteria>

<output>
Create `.planning/phases/217-adversarial-panel-auto-fix-safety-rails/217-07-SUMMARY.md` when done
</output>
