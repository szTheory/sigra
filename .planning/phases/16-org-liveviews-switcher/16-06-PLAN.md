---
phase: 16
plan: 06
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03, 16-04, 16-05]
files_modified:
  - test/example/lib/example_web/components/layouts.ex
  - test/example/lib/example_web/router.ex
  - test/example/lib/example_web/user_auth.ex
  - test/example/lib/example_web/controllers/organization_switch_controller.ex
  - test/example/lib/example_web/components/org_switcher.ex
  - test/example/lib/example_web/live/organizations_live/index.ex
  - test/example/lib/example_web/live/organizations_live/new.ex
  - test/example/lib/example_web/live/organization_settings_live.ex
  - test/example/lib/example_web/live/organization_members_live.ex
  - test/example/priv/repo/migrations
  - .planning/phases/16-org-liveviews-switcher/16-VALIDATION.md
  - test/example/test/example_web/integration/phase_16_integration_test.exs
autonomous: false
requirements:
  - ORG-UX-01
  - ORG-UX-02
  - ORG-UX-03
  - ORG-UX-04
  - ORG-UX-05
  - ORG-UX-06
  - ORG-UX-07
  - ORG-UX-08
  - ORG-UX-09

must_haves:
  truths:
    - "Example app compiles clean with all Phase 16 templates instantiated into test/example via the generator"
    - "Example app layouts.ex contains <.org_switcher current_scope={@current_scope} user_organizations={@user_organizations} /> inside the header"
    - "mix test (full library suite) green"
    - "cd test/example && mix test (full example suite) green"
    - "mix credo --strict clean"
    - "mix dialyzer clean (if PLT cached)"
    - "An end-to-end integration test covers: register → land on /organizations Branch A → create org → land inside org → switch orgs (after creating a second) → rename → change slug (verify 7d alias redirect) → invite nav stub → change a member's role → remove a member (verify force-logout DB state) → soft-delete → land back on /organizations"
    - "16-VALIDATION.md Dimension 8 checklist fully populated and signed off (nyquist_compliant: true)"
    - "Every ORG-UX-01..09 requirement has at least one green automated test mapped in the per-task validation table"
    - "A human-verification checkpoint confirms the UI matches UI-SPEC copy/styles on a live dev server"
  artifacts:
    - path: "test/example/test/example_web/integration/phase_16_integration_test.exs"
      provides: "End-to-end phase-level smoke test hitting every ORG-UX requirement"
      contains: "defmodule Phase16IntegrationTest"
    - path: ".planning/phases/16-org-liveviews-switcher/16-VALIDATION.md"
      provides: "Updated validation contract with per-task map filled and Dimension 8 checklist signed"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "test/example layouts.ex"
      to: "generated org_switcher component"
      via: "manual paste per post-install instructions D-27"
      pattern: "<.org_switcher"
    - from: "phase_16_integration_test.exs"
      to: "every LV + controller from Plans 03/04/05/02"
      via: "Phoenix.ConnTest + Phoenix.LiveViewTest drive the full flow"
      pattern: "live(conn,"
---

<objective>
Finalize Phase 16: instantiate every generator template into the test/example app, paste the switcher into layouts.ex per D-27, run the Features.Organizations generator on a fresh example-app fixture to prove idempotency and compile-cleanliness, write the phase-level end-to-end integration test that exercises every ORG-UX requirement, populate 16-VALIDATION.md Dimension 8 checklist, and hold a human-verification checkpoint on a live dev server.

Purpose: Plans 01–05 each deliver isolated units that pass isolated tests. This plan stitches them together, runs them against the real example app, proves they compose correctly, and gives the human user a chance to visually verify the UI matches UI-SPEC before the phase ships.

Output: Compiling example app with full org UX reachable end-to-end, phase integration test green, validation contract signed, human checkpoint passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-org-liveviews-switcher/16-CONTEXT.md
@.planning/phases/16-org-liveviews-switcher/16-UI-SPEC.md
@.planning/phases/16-org-liveviews-switcher/16-VALIDATION.md
@.planning/phases/16-org-liveviews-switcher/16-01-PLAN.md
@.planning/phases/16-org-liveviews-switcher/16-02-PLAN.md
@.planning/phases/16-org-liveviews-switcher/16-03-PLAN.md
@.planning/phases/16-org-liveviews-switcher/16-04-PLAN.md
@.planning/phases/16-org-liveviews-switcher/16-05-PLAN.md
@lib/sigra/install/features/organizations.ex
@test/example/lib/example_web/router.ex
@test/example/lib/example_web/user_auth.ex
@test/example/lib/example_web/components/layouts.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mix sigra.install against test/example, paste switcher into layouts.ex, migrate, compile clean, full suite green</name>
  <files>
    test/example/lib/example_web/components/layouts.ex,
    test/example/lib/example_web/router.ex,
    test/example/lib/example_web/user_auth.ex,
    test/example/lib/example_web/controllers/organization_switch_controller.ex,
    test/example/lib/example_web/components/org_switcher.ex,
    test/example/lib/example_web/live/organizations_live/index.ex,
    test/example/lib/example_web/live/organizations_live/new.ex,
    test/example/lib/example_web/live/organization_settings_live.ex,
    test/example/lib/example_web/live/organization_members_live.ex,
    test/example/priv/repo/migrations
  </files>
  <read_first>
    - test/example/lib/example_web/components/layouts.ex (current header structure — find the right insertion point)
    - test/example/lib/example_web/router.ex (confirm injection targets are present)
    - lib/sigra/install/features/organizations.ex (the manifest Plan 02 populated)
    - .planning/phases/16-org-liveviews-switcher/16-CONTEXT.md D-24, D-27
  </read_first>
  <action>
    Step A — Instantiate templates: run `mix sigra.install --organizations --yes` against test/example (if the installer has an idempotent flag, use it; otherwise, manually copy each template from priv/templates/sigra.install/organizations/* into test/example/lib/example_web/* mapping `<%= @web_namespace %>` → `ExampleWeb` and `<%= @app_namespace %>` → `Example`). Run the generator from the project root.

    Step B — Paste org_switcher into layouts.ex: open test/example/lib/example_web/components/layouts.ex, find the `<header>` block, and insert:

    ```heex
    <.org_switcher
      :if={@current_scope && @current_scope.active_organization}
      current_scope={@current_scope}
      user_organizations={@user_organizations || []}
      return_to={@current_path || "/"} />
    ```

    Position: right side of header, LEFT of the existing user menu. Import `ExampleWeb.Components.OrgSwitcher` at the top of layouts.ex.

    Step C — Run migrations: `cd test/example && mix ecto.migrate` to apply the organization_slug_aliases migration (Plan 01 Task 1 Step C).

    Step D — Compile clean: `cd test/example && mix compile --force --warnings-as-errors` — must exit 0. Fix any namespace / import issues.

    Step E — Run full suites: `mix test` (library) and `cd test/example && mix test` (example). Both must exit 0. Resolve any integration failures (most likely: thin-wrapper signatures or stale fixtures).

    Step F — Credo: `mix credo --strict` — no new warnings in any file added or modified by Phase 16. Fix credo issues now; do not defer.
  </action>
  <verify>
    <automated>cd test/example && mix compile --force --warnings-as-errors && mix ecto.migrate && mix test && cd .. && mix test && mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `test/example/lib/example_web/controllers/organization_switch_controller.ex` exists on disk (proves template was instantiated)
    - `test/example/lib/example_web/components/org_switcher.ex` exists
    - `test/example/lib/example_web/live/organizations_live/index.ex` exists
    - `test/example/lib/example_web/live/organizations_live/new.ex` exists
    - `test/example/lib/example_web/live/organization_settings_live.ex` exists
    - `test/example/lib/example_web/live/organization_members_live.ex` exists
    - grep shows `<.org_switcher` in test/example/lib/example_web/components/layouts.ex
    - grep shows `scope "/organizations/:org"` in test/example/lib/example_web/router.ex
    - grep shows `post "/organizations/switch"` in test/example/lib/example_web/router.ex appearing BEFORE the scoped block (line-order check)
    - `ls test/example/priv/repo/migrations | grep organization_slug_aliases` returns a match
    - `cd test/example && mix compile --force --warnings-as-errors` exits 0
    - `mix test` (library) exits 0
    - `cd test/example && mix test` exits 0
    - `mix credo --strict` exits 0
  </acceptance_criteria>
  <done>Example app is fully wired with the generated templates, the switcher is in the layout, migrations are applied, and every automated suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase-level end-to-end integration test covering every ORG-UX requirement + populate 16-VALIDATION.md per-task map</name>
  <files>
    test/example/test/example_web/integration/phase_16_integration_test.exs,
    .planning/phases/16-org-liveviews-switcher/16-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/16-org-liveviews-switcher/16-VALIDATION.md (the empty Dimension 8 checklist + per-task map stub)
    - test/example/test/example_web/flows/signup_zero_org_flow_test.exs (Plan 03 — register-to-Branch-A flow, reuse as setup)
    - Plans 01–05 (SUMMARY sections for the verification commands)
  </read_first>
  <behavior>
    End-to-end integration test — all steps in one `test "phase 16 full user journey"` block (per AAA + self-contained convention):

    - Setup: register a new user via the real RegistrationLive flow (no fixture shortcut — prove ORG-UX-09 again at integration level)
    - Step 1 (ORG-UX-09): after register, confirm redirect lands on /organizations Branch A with zero memberships
    - Step 2 (ORG-UX-01): submit create-org form with name "Acme Corp" → redirects to /organizations/acme-corp/members; assert scope.active_organization.name == "Acme Corp" and membership role == :owner
    - Step 3 (ORG-UX-02): visit /organizations/acme-corp/members and assert the rendered layout contains `<.org_switcher>` output with the active org name and role badge
    - Step 4: create a SECOND org "Beta LLC" via /organizations/new → redirects to /organizations/beta-llc/members; active org is now Beta LLC
    - Step 5 (ORG-UX-03): POST /organizations/switch with organization_id = acme_id and return_to = "/organizations/acme-corp/members" → redirect target is "/organizations/acme-corp/members"; user_sessions.active_organization_id in DB == acme.id
    - Step 6 (ORG-UX-04 rename): visit /organizations/acme-corp/settings → submit rename with name "Acme Corporation" → flash "Name updated."; DB org.name updated
    - Step 7 (ORG-UX-04 slug change): click "Change slug" → fill slug="acme-co", password, confirm_slug="acme-corp" → submit → flash "Slug updated. The old slug redirects for 7 days."; DB has OrganizationSlugAlias row with old_slug="acme-corp"
    - Step 8 (slug alias redirect): GET /organizations/acme-corp/settings → 301 redirect to /organizations/acme-co/settings (through LoadOrganizationFromSlug plug from Plan 01)
    - Step 9 (ORG-UX-06 members list): fixture-insert a second member (role: :admin) and a third member (role: :member) into the Acme org → GET /organizations/acme-co/members → rendered HTML contains both emails + role badges + sorted inserted_at DESC
    - Step 10 (ORG-UX-07 role change): open role modal on :member → submit new_role=:admin → flash "Role updated."; DB membership.role == :admin
    - Step 11 (ORG-UX-08 remove + force-logout DB assertion): fixture-insert user_sessions row for the promoted admin member with active_organization_id = acme.id AND a separate session with a DIFFERENT org id → open remove modal → submit → assert:
      (a) membership row is deleted
      (b) user_sessions row with active_organization_id == acme.id is GONE (SELECT COUNT = 0)
      (c) user_sessions row in the OTHER org survives (SELECT COUNT = 1)
      (d) flash shows "Removed {email} from Acme Corporation."
    - Step 12 (ORG-UX-05 soft-delete): open delete form → submit with password + confirm_name="Acme Corporation" → redirect to /organizations; DB org.deleted_at set
    - Step 13 (cleanup validation): /organizations now renders Branch C (only Beta LLC) AND the deleted Acme org does NOT appear anywhere in the rendered HTML

    Then update 16-VALIDATION.md:
    - Fill every row of "Per-Task Verification Map" with the commands from Plans 01–05 and this plan
    - Check every "Wave 0 Requirements" box with the test file path that fulfills it (Plans 01–05 created them; this task only confirms)
    - Check every "Validation Sign-Off" box
    - Set `nyquist_compliant: true` in frontmatter
  </behavior>
  <action>
    Write the integration test in test/example/test/example_web/integration/phase_16_integration_test.exs. Tag `@moduletag :phase16` and `@moduletag :integration`. Use Phoenix.ConnTest + Phoenix.LiveViewTest extensively. The test follows redirects with `conn |> follow_redirect(...)` where needed. Use real fixtures from Phase 13's organization_fixtures.ex + Phase 13/14 user_fixtures — do NOT create inline DB records except for the cross-org user_sessions assertion in step 11.

    After the test is green, update 16-VALIDATION.md:
    - Replace the "_Planner fills in during PLAN.md creation_" row with one row per ORG-UX requirement plus one row per library-side test, per Plan 01–05:
      | ORG-UX-01 | 03 | 2 | ORG-UX-01 | — | Create org form happy path | LV integration | `cd test/example && mix test test/example_web/live/organizations_live/index_test.exs -t phase16 -x` | ✅ | ✅ green |
      (... one row per behavior ...)
    - Set `nyquist_compliant: true` in frontmatter
    - Set `wave_0_complete: true` in frontmatter
    - Fill "Manual-Only Verifications" section with: (row) Visual UI matches UI-SPEC copy/colors — requirement ORG-UX-01..09 — reason "Pixel-level visual match" — instructions "Visit dev server http://localhost:4000/organizations and verify each of the 6 screens per UI-SPEC §Screen Anatomy; check headings/copy/badge colors/spacing match exactly"

    Commit sequence:
    1. RED: write the integration test with expected assertions before Plans 01–05 are instantiated into the example app (should fail if any plan has a gap)
    2. Run → if any assertion fails, fix the underlying plan output before proceeding
    3. GREEN: commit `test(16-06): add phase 16 end-to-end integration test`
    4. Commit `docs(16-06): populate 16-VALIDATION.md per-task map + sign off Dimension 8`
  </action>
  <verify>
    <automated>cd test/example && mix test test/example_web/integration/phase_16_integration_test.exs --only phase16</automated>
  </verify>
  <acceptance_criteria>
    - test/example/test/example_web/integration/phase_16_integration_test.exs exists
    - grep shows `test "phase 16 full user journey"` (or equivalent name) in the file
    - The test executes all 13 steps (grep for step comments or assertion markers)
    - Step 11 force-logout DB assertion is explicit (grep `active_organization_id ==` + `aggregate`)
    - Step 8 alias redirect assertion is explicit (grep `follow_redirect` after old slug request)
    - `cd test/example && mix test test/example_web/integration/phase_16_integration_test.exs --only phase16` exits 0
    - 16-VALIDATION.md frontmatter has `nyquist_compliant: true`
    - 16-VALIDATION.md frontmatter has `wave_0_complete: true`
    - 16-VALIDATION.md per-task map has at least 9 rows (one per ORG-UX requirement)
    - 16-VALIDATION.md "Validation Sign-Off" section has all 6 checkboxes checked
    - 16-VALIDATION.md "Manual-Only Verifications" section is populated (not TBD)
  </acceptance_criteria>
  <done>One end-to-end integration test proves every ORG-UX requirement against the real example app. Validation contract signed. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — visual UI + error copy match UI-SPEC on live dev server</name>
  <what-built>
    Phase 16 ships a full organization UX in the example app:
    - /organizations landing (3 branches)
    - /organizations/new create form
    - /organizations/:slug/settings (rename + slug + soft-delete)
    - /organizations/:slug/members (list + role/remove modals)
    - Header org switcher dropdown in layouts.ex
    - POST /organizations/switch controller
  </what-built>
  <how-to-verify>
    1. Start the example app: `cd test/example && iex -S mix phx.server`
    2. Open http://localhost:4000 in a browser
    3. Register a new user. Confirm the automatic redirect lands on /organizations Branch A with heading "Create your first organization" and the exact body copy from UI-SPEC §Copywriting Contract (lines ~127–128).
    4. Create an organization named "Test Org". Confirm the slug preview updates live as you type the name (text below the name field showing lowercase-hyphenated form).
    5. After create, confirm you land inside the org (URL /organizations/test-org/members) and the header switcher chip shows "Test Org" + "Owner" badge in primary color.
    6. Click the switcher trigger. Confirm the dropdown opens with: "ACTIVE" section showing Test Org, "+ Create organization" link, "Organization settings" link (because you are owner), "Manage organizations" link. Verify Esc key closes the dropdown and Tab cycles through items.
    7. Click "Organization settings". Confirm three sections visible in this order: General / Slug / Danger zone. Danger zone has a red left border.
    8. In General, change the name to "Test Organization" and click "Save name". Confirm flash "Name updated." appears in the toast-top-end position.
    9. In Slug section, click "Change slug". Confirm inline form appears with:
       - new slug input
       - current password input (type=password)
       - confirm_slug input labeled "Type test-org to confirm"
       - yellow warning banner with exact copy about 7-day redirect
       - "Update slug" (red destructive button) + Cancel
    10. Try submitting with wrong password first → inline error "That password is incorrect." on password field. Then submit with wrong confirm text → inline error "Type test-org exactly to confirm.". Then submit with valid values and new slug "test-org-renamed" → flash "Slug updated. The old slug redirects for 7 days."; URL updates to /organizations/test-org-renamed/settings.
    11. Manually navigate to http://localhost:4000/organizations/test-org/settings (old slug) → confirm browser redirects to the new slug.
    12. Scroll to Danger zone, click "Delete organization". Confirm form appears with password + confirm_name (labeled "Type Test Organization to confirm"). Wrong password → inline error. Wrong name → inline error "Type Test Organization exactly to confirm.". Cancel and DO NOT delete yet.
    13. Navigate to /organizations/test-org-renamed/members. Confirm table renders with your email in the only row, role badge "Owner" in primary color, Joined showing relative time, Last active showing a real time, Status "Active". Invite member button is DISABLED with tooltip "Available in the next release".
    14. Open dev tools, create a second org "Second Co" via /organizations/new. Switch back to the first org via the header switcher dropdown. Confirm the dropdown now shows "SWITCH TO" with "Second Co" and posting the form rotates the active org.
    15. Return to UI-SPEC.md and spot-check: spacing (4/8/16/24/32 px scale), typography (4 sizes, 2 weights), color (primary used only for CTAs + active-org + owner badge, error only for destructive + danger zone border).

    If any step fails, respond with "failed: step N — <description>" and stop. If everything passes, respond with "approved".
  </how-to-verify>
  <files>manual verification against dev server at http://localhost:4000</files>
  <action>Human steps only — Claude must NOT perform these. Wait for the human to complete the 15-step checklist above against a live dev server and respond with "approved" or "failed: step N — description".</action>
  <verify>Human responds with "approved" after completing all 15 steps.</verify>
  <done>Human typed "approved" — visual UI and error copy confirmed to match UI-SPEC on the live dev server.</done>
  <resume-signal>Type "approved" or describe failures.</resume-signal>
  <historical-note>
    The 15-step manual checklist above is preserved for historical context only.
    Task 3 is now satisfied by an automated Playwright spec at
    `test/example/priv/playwright/tests/organizations.spec.ts` that walks the
    same flow (register → Branch A → create → switcher → settings → rename →
    slug change with error paths → 7-day alias redirect → danger-zone disclosure
    → members → multi-org switch → copywriting spot-checks). The spec runs in
    CI under the existing `example_playwright_smoke` job. The Plan 06 executor
    crashed with an API 500 after Tasks 1 and 2 committed; the user requested
    automating Task 3 instead of walking it manually, and the orchestrator
    delivered the spec, fixed an example-app gap (Phase 16 LVs were not wrapped
    in `<Layouts.app>` so the switcher never rendered in the navbar), and
    re-ran the library + example + browser suites green before completing the
    phase.
  </historical-note>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator output → example app | instantiation must not introduce namespace drift or break existing example-app tests |
| End-to-end integration test → real DB | must clean up fixtures to avoid cross-test pollution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering | Template instantiation drifting from generator manifest over time | mitigate | Task 1 Step A uses `mix sigra.install` (not manual copy) where possible; Task 1 verify command includes `mix compile --warnings-as-errors` which catches drift. |
| T-16-06-02 | Information Disclosure | Integration test logging real user passwords | accept | Tests use `valid_test_password()` helper; ExUnit logs are not persisted in CI secrets. |
| T-16-06-03 | Elevation of Privilege | Human-verification step missed | mitigate | Task 3 is a blocking checkpoint gate; pipeline cannot proceed without human response. |
</threat_model>

<verification>
1. `cd test/example && mix compile --force --warnings-as-errors` clean
2. `mix test && cd test/example && mix test` both green
3. `mix credo --strict` clean
4. `cd test/example && mix test test/example_web/integration/phase_16_integration_test.exs --only phase16` green
5. 16-VALIDATION.md frontmatter: nyquist_compliant: true AND wave_0_complete: true
6. Human checkpoint: "approved" received
</verification>

<success_criteria>
- Example app fully wired with all Phase 16 templates
- Switcher pasted into layouts.ex
- Migrations applied
- Full library + example suites green
- Credo clean
- Phase 16 integration test green
- 16-VALIDATION.md Dimension 8 checklist complete
- Human verification passed
</success_criteria>

<output>
Create `.planning/phases/16-org-liveviews-switcher/16-06-SUMMARY.md` documenting:
- Integration test steps and their requirement coverage (ORG-UX-01..09 → specific assertions)
- Any drift fixes made to templates during instantiation
- Final validation sign-off

Also create `.planning/phases/16-org-liveviews-switcher/16-SUMMARY.md` rolling up the entire phase for future phases (17/18/21) to read.
</output>
