---
phase: 200-user-detail-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sigra/admin/live/user_sessions_live.ex
  - test/example/lib/example_web/router.ex
  - priv/templates/sigra.install/admin/router_injection.ex
  - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex
  - test/sigra/admin/glossary_test.exs
autonomous: true
requirements:
  - DETAIL-02
  - DETAIL-03
user_setup: []

must_haves:
  truths:
    - "Navigating to /admin/users/:id/sessions renders a dedicated lib-owned Sessions page with the full session table and a Revoke all sessions button (global and org scope)."
    - "The per-session revoke and revoke-all destructive flow runs through the reused ConfirmDialog APG dialog on the new page (focus trap + restore, Escape, click-outside, no scrim-hidden modal)."
    - "The new /admin/users/:id/sessions route exists byte-coherently in the example router, the installer router template, and the golden install fixture router (both global and org live_session blocks)."
    - "The new LiveView is covered by the glossary synonym-drift guard."
  artifacts:
    - "lib/sigra/admin/live/user_sessions_live.ex"
    - "/admin/users/:id/sessions and /admin/organizations/:org/users/:id/sessions routes in all three router files"
  key_links:
    - "UserSessionsLive mount/handle_params reuse runtime_config!/0 + Detail.load!/3 (scope-safe) — same contract as audit_user_live.ex."
    - "Revoke handlers call Actions.revoke_session/4 and Actions.revoke_all_sessions/3 unchanged."
    - "ConfirmDialog hook selectors (#user-session-confirm-overlay, #user-session-confirm-title, .sg-confirm-dialog, [data-sg-confirm-cancel]) are byte-identical to the moved markup so admin-modal-interaction.spec.ts still passes."
  prohibitions:
    - statement: "No new or changed `sg-*` CSS is introduced in Phase 200 (reuse-only, D-01); if any `sg-*` CSS is added or changed, it is written byte-identically into all three copies — priv/templates/sigra.install/admin/sigra_admin.css, test/example/priv/static/assets/sigra_admin.css, test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css (D-11)."
      status: resolved
      verification: "The three CSS copies remain byte-identical — `md5 -q priv/templates/sigra.install/admin/sigra_admin.css test/example/priv/static/assets/sigra_admin.css test/fixtures/install_golden/tree/priv/static/assets/sigra_admin.css | sort -u | wc -l` returns 1; and `git diff --stat` shows zero changes to any `sigra_admin.css` copy at end of phase."
---

<objective>
Build the net-new lib-owned `Sigra.Admin.Live.UserSessionsLive` (the load-bearing prerequisite for moving the session revoke flow off User Detail per D-04), wire its `/admin/users/:id/sessions` route across all three router files in lockstep (D-12), and extend the glossary drift guard to cover it.

Purpose: DETAIL-02 turns Sessions into a true link-out and DETAIL-03 gives the destructive flow a dedicated, APG-confirmed surface. This plan must land before the User Detail recompose (Plan 02) can safely remove the session handlers and confirm overlay from `user_show_live.ex`.

Output: a new award-grade-from-the-start Sessions LiveView, the route propagated to example + installer template + golden fixture, and the glossary scope updated to 8 LiveViews.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-user-detail-elevation/200-CONTEXT.md
@.planning/phases/200-user-detail-elevation/200-PATTERNS.md
@.planning/phases/200-user-detail-elevation/200-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan introduces the following new symbols (downstream plans and verification depend on them):
- Module: `Sigra.Admin.Live.UserSessionsLive`
- Routes: `live "/admin/users/:id/sessions"` (global) and `live "/users/:id/sessions"` (org) → `Sigra.Admin.Live.UserSessionsLive, :show`
- No new `sg-*` CSS classes (reuse-only — `sg-page-header`, `sg-card`, `sg-table-panel`, `sg-table`, `sg-confirm-overlay`, `sg-confirm-dialog`, `sg-btn--danger`, `sg-stack--N`).
- Private presentation helpers moved/cloned into the new module: `session_type/1`, `activity_value/1`, `relative_activity/1`, `pluralize/2`, scope-aware path/breadcrumb helpers.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create UserSessionsLive with reused session table and APG confirm dialog</name>
  <files>lib/sigra/admin/live/user_sessions_live.ex</files>
  <read_first>
    - lib/sigra/admin/live/user_show_live.ex (source of the session table markup `:141-194`, the revoke handlers `:42-94`, the confirm overlay `:315-333`, `reload_detail/2` `:338-341`, and the session helpers `session_type/1` `:471`, `activity_value/1` `:473-475`, `relative_activity/1` `:516-527`, `pluralize/2` `:529-530`, `runtime_config!/0` `:532-545`)
    - lib/sigra/admin/live/audit_user_live.ex (clone analog for the per-user `/users/:id/X` skeleton: mount `:14-26`, handle_params `:28-61`, scope-aware path/breadcrumb helpers `:293-372`, `runtime_config!/0` `:277-291`)
    - lib/sigra/admin/users/actions.ex (the unchanged revoke mutations Actions.revoke_session/4 and Actions.revoke_all_sessions/3)
    - .planning/phases/200-user-detail-elevation/200-UI-SPEC.md (UserSessionsLive composition `:185-202`, Copywriting Contract `:206-247`, Interaction Contract APG table `:258-272`)
  </read_first>
  <action>
    Create lib/sigra/admin/live/user_sessions_live.ex defining `Sigra.Admin.Live.UserSessionsLive` (per D-04). Clone the module skeleton, `mount/3`, `handle_params/3`, `runtime_config!/0`, and the scope-aware path/breadcrumb helper pattern from audit_user_live.ex (use `Phoenix.LiveView`, import `Sigra.Admin.Components`, alias `Sigra.Admin.Scope` / `Sigra.Admin.Users.Actions` / `Sigra.Admin.Users.Detail`). In `handle_params/3` call `Detail.load!(config, admin_scope, user_id)`, sanitize `return_to`, and assign a breadcrumb that mirrors audit_user_live.ex's `audit_breadcrumbs/3` but ends in a `Sessions` crumb under the user detail crumb (thread `user_detail_path/3` as the parent). Set `page_title` to the user email/display-name plus sessions.

    Lift verbatim into this module (they MOVE here from user_show_live.ex per D-04): the `open_revoke_session` / `open_revoke_all_sessions` / `cancel_confirm` / `confirm_action` handlers, `reload_detail/2`, `revoke_session_copy/1`, `revoke_all_sessions_copy/1`, and the private helpers `session_type/1`, `activity_value/1`, `relative_activity/1`, `pluralize/2`. The revoke handlers call `Actions.revoke_session/4` and `Actions.revoke_all_sessions/3` unchanged.

    Render with `<.scope_ribbon>`, an `sg-page-header` (kicker, h1 "Sessions", session count via pluralize), then an `sg-card sg-stack sg-stack--3` containing a heading cluster with the "Revoke all sessions" `sg-btn sg-btn--danger sg-btn--sm` (shown only when sessions present), the full `sg-table-panel`/`sg-table` session table copied from user_show_live.ex:158-191 (Type / IP address / Last activity / per-row Revoke session button), and an `<.empty_state title="No active sessions">` for the zero state with body copy from the UI-SPEC Copywriting Contract.

    Lift the confirm overlay verbatim from user_show_live.ex:315-333 — preserve EXACTLY (D-06): id `user-session-confirm-overlay`, `phx-hook="ConfirmDialog"`, `class="sg-confirm-overlay"`, the inner `class="sg-confirm-dialog"` with `role="dialog"` `aria-modal="true"` `aria-labelledby="user-session-confirm-title"`, the `id="user-session-confirm-title"` node, and `data-sg-confirm-cancel` on the cancel button. Do NOT rename any of these selectors. Keep the overlay un-nested and not scrim-hidden (no `display:none`).

    All visible copy must match the UI-SPEC Copywriting Contract verbatim (Sessions / Revoke all sessions / Revoke session / No active sessions / confirm titles + bodies / Cancel / Revoke) and stay glossary-clean. Use only existing `--sg-*` tokens and existing `sg-*` primitives — introduce NO new CSS class. Do not place `transition: all` anywhere. Do not extract a shared `runtime_config!/0` helper this phase — copy it as-is, changing only the error string.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'defmodule Sigra.Admin.Live.UserSessionsLive' lib/sigra/admin/live/user_sessions_live.ex` returns 1.
    - `grep -F 'id="user-session-confirm-overlay"' lib/sigra/admin/live/user_sessions_live.ex` and `grep -F 'phx-hook="ConfirmDialog"' lib/sigra/admin/live/user_sessions_live.ex` each return at least 1 (selectors preserved for admin-modal-interaction.spec.ts).
    - `grep -F 'data-sg-confirm-cancel' lib/sigra/admin/live/user_sessions_live.ex` and `grep -F 'id="user-session-confirm-title"' lib/sigra/admin/live/user_sessions_live.ex` each return at least 1.
    - `grep -F 'Actions.revoke_session(' lib/sigra/admin/live/user_sessions_live.ex` and `grep -F 'Actions.revoke_all_sessions(' lib/sigra/admin/live/user_sessions_live.ex` each return at least 1.
    - `grep -c 'transition: all' lib/sigra/admin/live/user_sessions_live.ex` returns 0.
    - `mix compile --warnings-as-errors` succeeds with no warnings.
  </acceptance_criteria>
  <done>UserSessionsLive compiles clean, renders the full session table + APG confirm dialog with preserved hook selectors, and routes session mutations through the unchanged Actions module.</done>
</task>

<task type="auto">
  <name>Task 2: Add /admin/users/:id/sessions route to all three router files in lockstep</name>
  <files>test/example/lib/example_web/router.ex, priv/templates/sigra.install/admin/router_injection.ex, test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex</files>
  <read_first>
    - test/example/lib/example_web/router.ex (global admin live_session `:280-286`, org admin live_session `:312-319`)
    - priv/templates/sigra.install/admin/router_injection.ex (global block `:35-41`, org block `:67-72`)
    - test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex (global block `:252-258`, org block `:284-289`)
    - .planning/phases/200-user-detail-elevation/200-PATTERNS.md (Generated-host route lockstep `:323-328`, exact insertion guidance `:179-201`)
  </read_first>
  <action>
    Add the new Sessions route immediately after the existing `AuditUserLive` route line in BOTH the `:admin_global` and `:admin_organization` `live_session` blocks of all three router files (D-12 lockstep — example + installer template + golden fixture must change in the same commit or golden_diff_test and generated-host acceptance smoke break).

    In test/example/lib/example_web/router.ex: after line 285 (global) add `live "/admin/users/:id/sessions", Elixir.Sigra.Admin.Live.UserSessionsLive, :show`; after line 318 (org) add `live "/users/:id/sessions", Elixir.Sigra.Admin.Live.UserSessionsLive, :show`.

    In priv/templates/sigra.install/admin/router_injection.ex: after the global `AuditUserLive` line (`:40`) add the identical `live "/admin/users/:id/sessions", Elixir.Sigra.Admin.Live.UserSessionsLive, :show`; after the org `AuditUserLive` line (`:71`) add `live "/users/:id/sessions", Elixir.Sigra.Admin.Live.UserSessionsLive, :show`. These new `live` lines carry no EEx interpolation, so they paste as-is.

    In test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex: after the global `AuditUserLive` line (`:257`) add the identical global `live` line; after the org `AuditUserLive` line (`:288`) add the identical org `live` line.

    The new routes inherit the existing `:admin_global` / `:admin_organization` pipelines (RequireAdminAccess + AdminScope on_mount) unchanged — no new pipeline, no new on_mount. This gates the surface with the same admin auth as the per-user audit route (mitigates the IDOR/authorization threat).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && grep -rc 'UserSessionsLive' test/example/lib/example_web/router.ex priv/templates/sigra.install/admin/router_injection.ex test/fixtures/install_golden/tree/lib/sigra_install_golden_tmp_web/router.ex</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three router files contains exactly 2 `UserSessionsLive` route lines (one global `/admin/users/:id/sessions`, one org `/users/:id/sessions`) — `grep -c 'UserSessionsLive' <file>` returns 2 for each of the three files.
    - `grep -F '/admin/users/:id/sessions' test/example/lib/example_web/router.ex` returns 1 and the org `grep -F '"/users/:id/sessions"' test/example/lib/example_web/router.ex` returns 1.
    - `cd test/example && mix compile --warnings-as-errors` succeeds (route module resolves).
  </acceptance_criteria>
  <done>The Sessions route is present and byte-coherent in all three router files in both scope blocks; the example app compiles with the route resolving to UserSessionsLive.</done>
</task>

<task type="auto">
  <name>Task 3: Extend glossary drift guard to cover UserSessionsLive</name>
  <files>test/sigra/admin/glossary_test.exs</files>
  <read_first>
    - test/sigra/admin/glossary_test.exs (the `@in_scope_files` list `:21-30` and the header comment count `:8`,`:11`)
  </read_first>
  <action>
    Add `"lib/sigra/admin/live/user_sessions_live.ex"` to the `@in_scope_files` list in test/sigra/admin/glossary_test.exs so the new surface inherits the synonym-drift guard (PATTERNS.md). Update the header comment count references that say "8 in-scope admin source files" / "exactly the 7 admin LiveViews" to reflect the new total (now 8 LiveViews + components.ex). Keep the list ordering consistent with the existing entries.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/sigra && mix test test/sigra/admin/glossary_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'lib/sigra/admin/live/user_sessions_live.ex' test/sigra/admin/glossary_test.exs` returns 1.
    - `mix test test/sigra/admin/glossary_test.exs` passes with 0 failures (the new LiveView's copy is glossary-clean).
  </acceptance_criteria>
  <done>The glossary test scopes UserSessionsLive and passes, proving the new surface's microcopy is one-term-per-concept clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → /admin/users/:id/sessions LiveView | admin operator requests a per-user sessions surface keyed by an `:id` path param (untrusted) |
| operator browser → revoke / revoke-all phx-click events | destructive session mutation events cross from the client into the LiveView |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-01 | Elevation/IDOR | /admin/users/:id/sessions route | high | mitigate | Route mounts under the existing `:admin_global` / `:admin_organization` live_session blocks (RequireAdminAccess + AdminScope on_mount). `Detail.load!/3` is scope-safe and raises outside scope. Verify present in new surface — same gate as the per-user audit route; no new auth code. |
| T-200-02 | Tampering/CSRF | revoke / revoke-all phx-click | high | mitigate | Mutations are LiveView events over the authenticated, CSRF-protected websocket channel (Phoenix LiveView default); `Actions.revoke_session/4` / `revoke_all_sessions/3` re-load the user scope-safely before mutating. No raw POST form introduced. |
| T-200-03 | Repudiation | session revoke | low | accept | Actions module already emits audit events on revoke (unchanged); no new repudiation surface. |
| T-200-04 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — N/A; no new dependencies added. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (library).
- `cd test/example && mix compile --warnings-as-errors` clean (route resolves).
- `mix test test/sigra/admin/glossary_test.exs` — 0 failures.
- All three router files contain the two new Sessions route lines (global + org).
- No new `sg-*` CSS class introduced (CSS three-copy parity untouched this plan).
</verification>

<success_criteria>
- UserSessionsLive renders the full session table + APG confirm dialog (selectors preserved) and routes revoke/revoke-all through the unchanged Actions module.
- The `/admin/users/:id/sessions` route is byte-coherent in example + installer template + golden fixture (both scopes).
- The glossary guard covers the new LiveView and passes.
</success_criteria>

<output>
Create `.planning/phases/200-user-detail-elevation/200-01-SUMMARY.md` when done.
</output>
