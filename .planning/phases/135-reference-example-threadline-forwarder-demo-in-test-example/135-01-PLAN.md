---
phase: 135-reference-example-threadline-forwarder-demo-in-test-example
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/example/mix.exs
  - test/example/mix.lock
  - test/example/priv/repo/migrations/*_threadline_audit_schema.exs
  - test/example/priv/repo/migrations/*_threadline_semantics_schema.exs
  - test/example/lib/example/accounts.ex
  - test/example/config/config.exs
  - test/example/AGENTS.md
  - test/example/test/example_web/threadline_forwarder_test.exs
autonomous: true
requirements: [EX-01]

must_haves:
  truths:
    - "Running `mix test test/example_web/threadline_forwarder_test.exs --include example_app` inside test/example/ exits 0 (Success Criterion #1)."
    - "The new test drives the REAL auth path (ExampleWeb.UserAuth.log_in_user/2) and asserts a real Threadline.Semantics.AuditAction row joined on correlation_id == audit_event.id (Success Criterion #1)."
    - "The asserted Threadline row has name == \"session.create\", status == :ok, actor_ref.id == user.id, actor_ref.type == :user (Success Criterion #1 — expected actor + action shape)."
    - "An adopter running `grep -ri threadline test/example/` finds the dep, both config blocks, the test, and the AGENTS.md section (Success Criterion #2)."
    - "No new top-level examples/ directory and no new CI job are created; the existing example_unit_smoke lane executes the new test as-is (Success Criterion #3)."
    - "No file under lib/ is modified (library code frozen)."
  artifacts:
    - path: "test/example/mix.exs"
      provides: "Threadline as a :dev,:test-scoped dep"
      contains: "{:threadline, \"~> 0.5\", only: [:dev, :test]}"
    - path: "test/example/priv/repo/migrations/*_threadline_audit_schema.exs"
      provides: "Threadline capture migration (creates audit_transactions); committed verbatim"
      contains: "audit_transactions"
    - path: "test/example/priv/repo/migrations/*_threadline_semantics_schema.exs"
      provides: "Threadline semantics migration (creates audit_actions, ALTERs audit_transactions); timestamp sorts after capture"
      contains: "audit_actions"
    - path: "test/example/lib/example/accounts.ex"
      provides: "forwarders: block under the existing audit: keyword in sigra_config/0"
      contains: "Sigra.Audit.Forwarders.Threadline"
    - path: "test/example/config/config.exs"
      provides: "mirrored forwarders: block under audit: in :sigra_config (the surface attach_forwarders/0 reads)"
      contains: "Sigra.Audit.Forwarders.Threadline"
    - path: "test/example/AGENTS.md"
      provides: "Threadline audit forwarder demo section, additive, outside the usage-rules fence"
      contains: "Threadline audit forwarder demo"
    - path: "test/example/test/example_web/threadline_forwarder_test.exs"
      provides: "Integration test asserting the full Sigra->Threadline projection chain"
      contains: "Threadline.Semantics.AuditAction"
  key_links:
    - from: "test/example/test/example_web/threadline_forwarder_test.exs (setup)"
      to: "Sigra.Audit.Forwarders.Threadline.attach/1"
      via: "attach(repo: Example.Repo, id: :test, dispatch: :sync, actor_type: :user) + on_exit :telemetry.detach({Sigra.Audit.Forwarders.Threadline, :test})"
      pattern: "Sigra\\.Audit\\.Forwarders\\.Threadline\\.attach"
    - from: "test/example/test/example_web/threadline_forwarder_test.exs (assertion)"
      to: "Threadline.Semantics.AuditAction (audit_actions table)"
      via: "Repo.one(from a in AuditAction, where: a.correlation_id == ^audit_event.id)"
      pattern: "correlation_id == \\^audit_event\\.id"
    - from: "test/example/priv/repo/migrations semantics migration"
      to: "test/example/priv/repo/migrations capture migration"
      via: "semantics ALTERs audit_transactions that capture creates; semantics timestamp prefix must be > capture timestamp prefix"
      pattern: "ALTER TABLE audit_transactions"
---

<objective>
Extend the existing `test/example/` app with a runnable Threadline audit-forwarder demo that
projects a real Sigra `session.create` login audit event into a real Threadline `audit_actions`
row, proving the Phase 131 forwarder library code through the Phase 132 recipe config block, via
the existing `example_unit_smoke` CI lane (no new job).

Purpose: Give adopters a copy-pasteable, end-to-end reference (dep + migrations + config + test +
AGENTS.md doc) they can grep for in under a minute. This is the working proof of the published
Threadline recipe — not a divergent variant.

Output:
- `test/example/mix.exs` gains `{:threadline, "~> 0.5", only: [:dev, :test]}` (+ committed `mix.lock`).
- Two committed Threadline migrations (capture THEN semantics) under `test/example/priv/repo/migrations/`.
- A `forwarders:` block under the existing `audit:` keyword in BOTH `accounts.ex` `sigra_config/0`
  AND `config.exs` `:sigra_config`.
- A new `test/example/test/example_web/threadline_forwarder_test.exs` asserting the full projection chain.
- A "Threadline audit forwarder demo" section appended to `test/example/AGENTS.md`.

HARD CONSTRAINTS (do not violate): NO new code under `lib/`. NO new top-level `examples/` directory.
NO new CI jobs. Only `test/example/` files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-CONTEXT.md
@.planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-RESEARCH.md
@.planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-PATTERNS.md
@.planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-VALIDATION.md

<interfaces>
<!-- Frozen Phase 131 library contracts the test CALLS/ASSERTS. Do NOT edit any lib/ file. -->

Sigra.Audit.Forwarders.Threadline.attach/1 (lib/sigra/audit/forwarders/threadline.ex:82-91):
  - handler_id = {__MODULE__, Keyword.get(opts, :id, :default)}  ⇒ id: :test yields {Sigra.Audit.Forwarders.Threadline, :test}
  - attaches handle_event/4 to telemetry event [:sigra, :audit, :log]
  - opts read downstream by call_threadline/2 (lines 238-241): :threadline_module (default Threadline),
    :actor_type (default :user), :repo; :dispatch resolved by resolve_dispatch_mode/1 (:auto collapses to
    :sync when Oban not supervised; D-06 pins :sync explicitly).

Telemetry seam (lib/sigra/audit.ex:315-322):
  [:sigra, :audit, :log] metadata = %{action: "session.create", actor_id: user.id, outcome: "success", id: event.id}
  event.id (the Sigra audit UUID) → Threadline correlation_id (the join key).

Threadline.Semantics.AuditAction schema (deps/threadline/lib/threadline/semantics/audit_action.ex:35-53):
  field :name, :string                                          # stored as STRING "session.create"
  field :actor_ref, Threadline.Semantics.ActorRef              # JSONB; loads as %ActorRef{type: :user, id: "..."}
  field :status, Ecto.Enum, values: [ok: "ok", error: "error"] # loads as :ok
  field :correlation_id, :string                              # == audit_event.id (join key)
  @required_fields ~w(name actor_ref status)a
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Threadline dep + commit the two generated migrations (capture then semantics)</name>
  <files>test/example/mix.exs, test/example/mix.lock, test/example/priv/repo/migrations/*_threadline_audit_schema.exs, test/example/priv/repo/migrations/*_threadline_semantics_schema.exs</files>
  <read_first>
    - test/example/mix.exs (deps list lines 41-71 — add after the `{:mox, "~> 1.1", only: :test}` line 69 which is the `only:`-scope precedent; aliases lines 79-87 — the `test` alias at line 84 already runs `ecto.migrate --quiet`, so committed migrations auto-apply with NO lane edit)
    - test/example/priv/repo/migrations/ (existing migrations; newest is 20260526043000_create_organization_auth_policies.exs — the new 20260528… files sort after it; the ONLY relative risk is the two new files vs each other)
    - deps/threadline/lib/threadline/capture/migration.ex (lines 17-64 — creates audit_transactions + audit_changes + trigger fn; module ThreadlineAuditSchema, def up/def down, raw `execute` DDL — this is the verbatim body to commit)
    - deps/threadline/lib/threadline/semantics/migration.ex (lines 10-62 — creates audit_actions AND ALTERs audit_transactions at 48-52; module ThreadlineSemanticsMigration; depends on the capture-created table)
    - deps/threadline/lib/mix/tasks/threadline.install.ex (lines 27-95 — writes BOTH files in one run/0 using a SECOND-resolution timestamp/0 at 92-95; the collision risk source)
    - test/example/priv/repo/migrations/20260526043000_create_organization_auth_policies.exs (lines 1-4 — the local `Example.Repo.Migrations.* / def change` convention the Threadline files DIVERGE from; do NOT reformat the generated files to match)
    - .planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-RESEARCH.md (Pitfall 2 timestamp collision; Pitfall 1 Oban-compiled-but-not-supervised; Environment Availability — fallback: write the two bodies from `Threadline.Capture.Migration.migration_content/0` + `Threadline.Semantics.Migration.migration_content/0` with controlled ascending timestamps to pre-empt the collision)
    - .planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-PATTERNS.md (mix.exs dep section ~216-239; migrations section ~243-278 incl. FORMAT DIVERGENCE NOTE)
  </read_first>
  <action>
    Append the dep tuple `{:threadline, "~> 0.5", only: [:dev, :test]}` to the deps/0 list in test/example/mix.exs
    immediately after the `{:mox, "~> 1.1", only: :test}` entry (add the trailing comma to the mox line). Use
    `only: [:dev, :test]` exactly — NOT `:test`-only — per D-09, so the forwarder module compiles in the dev-boot smoke
    lanes and emits no `maybe_warn_missing_forwarder_deps` warning. Then, inside test/example/, run `mix deps.get` to
    pull threadline 0.5.0 (already pinned in the root mix.lock:52) and commit the regenerated test/example/mix.lock
    (the CI cache key hashes it). Generate the two migrations by running `mix threadline.install` inside test/example/
    (fallback per RESEARCH Environment Availability: write both files by hand from
    `Threadline.Capture.Migration.migration_content/0` and `Threadline.Semantics.Migration.migration_content/0` with
    explicit ascending timestamps if the task path is awkward). Commit the two files VERBATIM — the capture file
    (`*_threadline_audit_schema.exs`, module ThreadlineAuditSchema, creates audit_transactions) and the semantics file
    (`*_threadline_semantics_schema.exs`, module ThreadlineSemanticsMigration, creates audit_actions + ALTERs
    audit_transactions, per D-02). Do NOT reformat the generated bodies to the local `Example.Repo.Migrations.* /
    def change` convention (FORMAT DIVERGENCE is intentional; reformatting risks schema drift from what the AuditAction
    Ecto module expects). MANDATORY collision check (RESEARCH Pitfall 2): verify the two filenames have distinct,
    ascending YYYYMMDDHHMMSS prefixes with capture < semantics; if equal OR reversed, rename the semantics file's
    timestamp to capture's prefix + 1 second so capture sorts first (it creates the table semantics ALTERs). Do NOT add
    an Oban supervisor to "fix" dispatch (Oban is a compiled-but-unsupervised dep in this app — Pitfall 1 — which is
    why :auto collapses to :sync; the test pins :sync anyway). Do NOT add `:requires_threadline` anywhere. Verify the
    migrations apply by running `cd test/example && MIX_ENV=test mix ecto.create && MIX_ENV=test mix ecto.migrate`
    (live Postgres at localhost:5432 postgres/postgres per CLAUDE.md).
  </action>
  <verify>
    <automated>cd test/example && grep -q '{:threadline, "~> 0.5", only: \[:dev, :test\]}' mix.exs && grep -q '"threadline"' mix.lock && ls priv/repo/migrations/*_threadline_audit_schema.exs priv/repo/migrations/*_threadline_semantics_schema.exs && cap=$(basename priv/repo/migrations/*_threadline_audit_schema.exs | cut -d_ -f1) && sem=$(basename priv/repo/migrations/*_threadline_semantics_schema.exs | cut -d_ -f1) && [ "$cap" -lt "$sem" ] && echo "ORDER_OK cap=$cap sem=$sem"</automated>
  </verify>
  <acceptance_criteria>
    - `cd test/example && grep '{:threadline, "~> 0.5", only: \[:dev, :test\]}' mix.exs` matches exactly (dep present, scope `[:dev, :test]`, not `:test`-only).
    - `test/example/mix.lock` contains a `"threadline"` entry (committed updated lock).
    - Exactly two new files exist: one matching `*_threadline_audit_schema.exs` (capture) and one matching `*_threadline_semantics_schema.exs` (semantics) under `test/example/priv/repo/migrations/`.
    - The two migration filename timestamp prefixes are distinct and ascending: capture prefix < semantics prefix (the ORDER_OK assertion above; bump semantics +1s if they collided).
    - The capture file declares `defmodule ThreadlineAuditSchema` with `def up`/`def down` and creates `audit_transactions`; the semantics file declares `defmodule ThreadlineSemanticsMigration`, creates `audit_actions`, and contains `ALTER TABLE audit_transactions` — bodies committed verbatim (NOT reformatted to `Example.Repo.Migrations.* / def change`).
    - `cd test/example && MIX_ENV=test mix ecto.migrate` applies both with no "migration version … is duplicated" error and no "relation audit_transactions does not exist" / missing-column error (proves correct ordering).
    - No file under `lib/` was modified; no Oban supervisor was added to `test/example/lib/example/application.ex`.
  </acceptance_criteria>
  <done>Threadline is a `[:dev, :test]` dep with committed lock; both migrations are committed verbatim in correct ascending capture-then-semantics order and apply cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add the forwarders: config block (accounts.ex + config.exs) and the AGENTS.md demo section</name>
  <files>test/example/lib/example/accounts.ex, test/example/config/config.exs, test/example/AGENTS.md</files>
  <read_first>
    - test/example/lib/example/accounts.ex (sigra_config/0 lines 590-622; the existing `audit:` keyword at 607-609 — the `forwarders:` key slots INSIDE this `audit:` list per EX-01)
    - test/example/config/config.exs (config :example, :sigra_config lines 43-63; the existing `audit:` block at 50-52 — mirror the identical `forwarders:` entry here per D-12; this is the surface attach_forwarders/0 actually reads)
    - guides/recipes/companion-libs/threadline.md (the published Phase 132 `forwarders:` recipe block ~62-71 — the committed config must mirror it for grep fidelity; it shows `dispatch: :auto` + dead `endpoint:`/`api_key:` keys)
    - lib/sigra/audit/forwarders/threadline.ex (call_threadline/2 lines 238-308 — confirms the forwarder reads ONLY :repo/:id/:dispatch/:actor_type/:threadline_module; `endpoint:`/`api_key:` are DEAD config in 0.5.0 which is DB-based, not HTTP)
    - test/example/AGENTS.md (existing `## …`/`### …` section style; `<!-- usage-rules-start -->` at line 22 and `<!-- usage-rules-end -->` at line 190 — the new section goes OUTSIDE this generated fence, after line 190)
    - .planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-RESEARCH.md (Open Question 1 recipe-parity-vs-dead-config; State of the Art — Threadline is DB-based not HTTP)
    - .planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-PATTERNS.md (accounts.ex section ~147-189; config.exs section ~193-212; AGENTS.md section ~282-290)
    - CLAUDE.md (banned marketing voice: "seamlessly", "just works", "production-ready out of the box", "the recommended way")
  </read_first>
  <action>
    PLANNER DECISION (resolving RESEARCH Open Question 1 — finalize this exact shape, do not re-litigate): the committed
    `forwarders:` block uses the recipe-parity-with-live-keys shape — keep `dispatch: :auto` literal (matches the
    published recipe; the inline comment notes it collapses to :sync without Oban) and `id: :default`, ADD
    `repo: Example.Repo` (the one runtime key the forwarder actually reads, for copy-paste fidelity), and DROP the dead
    `endpoint:`/`api_key:` keys (Threadline 0.5.0 is DB-based; those keys are never read by call_threadline/2 and would
    mislead an adopter). Add one inline comment stating Threadline 0.5.0 writes to `repo:` (no HTTP endpoint). The
    deterministic `dispatch: :sync` lives ONLY in the test's attach/1 call (Task 3), NOT here — the committed block is a
    grep/reference artifact; the vanilla Example.Application never calls attach_forwarders/0. In
    test/example/lib/example/accounts.ex, add the `forwarders:` key as a second entry inside the existing `audit:` list
    in sigra_config/0 (alongside `audit_schema: Example.Accounts.AuditEvent`), with `module:
    Sigra.Audit.Forwarders.Threadline`, `id: :default`, `dispatch: :auto`, `repo: Example.Repo`. Touch NO other key in
    sigra_config/0. Mirror the BYTE-IDENTICAL `forwarders:` entry into the `audit:` block in test/example/config/config.exs
    (D-12) so the two blocks match exactly for copy-paste fidelity. Append a new h2 section `## Threadline audit forwarder
    demo` to test/example/AGENTS.md AFTER the `<!-- usage-rules-end -->` marker (line 190 — never edit the fenced
    usage-rules block). The section points at the four touchpoints an adopter greps for: the dep (mix.exs), the config
    block (accounts.ex + config.exs), the test (test/example/test/example_web/threadline_forwarder_test.exs), and the two
    committed migrations; note the test attaches the forwarder itself (the vanilla Application does not call
    attach_forwarders/0) and pins `dispatch: :sync` + `repo: Example.Repo`. Match the file's terse imperative tone; use
    NO marketing voice (no "seamlessly"/"just works"/"production-ready out of the box"/"the recommended way").
  </action>
  <verify>
    <automated>cd test/example && grep -q "Sigra.Audit.Forwarders.Threadline" lib/example/accounts.ex && grep -q "Sigra.Audit.Forwarders.Threadline" config/config.exs && ! grep -q "THREADLINE_ENDPOINT\|THREADLINE_API_KEY\|api_key:" lib/example/accounts.ex config/config.exs && grep -q "## Threadline audit forwarder demo" AGENTS.md && ! grep -Eqi "seamlessly|just works|production-ready out of the box|the recommended way" AGENTS.md && MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/example/lib/example/accounts.ex` `sigra_config/0` contains a `forwarders:` key INSIDE the existing `audit:` list with `module: Sigra.Audit.Forwarders.Threadline`, `id: :default`, `dispatch: :auto`, `repo: Example.Repo`; no other key in sigra_config/0 changed.
    - `test/example/config/config.exs` `:sigra_config` contains the byte-identical `forwarders:` entry inside its `audit:` block (D-12).
    - Neither config file contains `endpoint:`, `api_key:`, `THREADLINE_ENDPOINT`, or `THREADLINE_API_KEY` (dead HTTP config dropped).
    - `test/example/AGENTS.md` contains a `## Threadline audit forwarder demo` h2 section located AFTER the `<!-- usage-rules-end -->` marker; the fenced usage-rules block is byte-unchanged.
    - The AGENTS.md section names all four grep touchpoints (mix.exs, accounts.ex/config.exs, the test file, the two migrations) and contains no banned marketing phrases.
    - `cd test/example && MIX_ENV=test mix compile --warnings-as-errors` exits 0 (config compiles, no missing-forwarder-deps warning).
  </acceptance_criteria>
  <done>Both config surfaces carry the identical recipe-parity `forwarders:` block (no dead HTTP keys); AGENTS.md documents the demo wiring outside the usage-rules fence with no marketing voice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the integration test asserting the full Sigra→Threadline projection chain</name>
  <files>test/example/test/example_web/threadline_forwarder_test.exs</files>
  <read_first>
    - test/example/test/example_web/audit_integration_test.exs (full file 1-77 — the EXACT structural analog: `use ExampleWeb.ConnCase, async: true`, `@moduletag :example_app`, `setup` registers a user, login via `Plug.Test.init_test_session(%{}) |> ExampleWeb.UserAuth.log_in_user(user)`, then `Repo.one(from a in AuditEvent, where: action == "session.create" and actor_id == ^user.id, order_by: [desc: :inserted_at], limit: 1)`; new test renames `row` → `audit_event` and adds the downstream hop)
    - lib/sigra/audit/forwarders/threadline.ex (attach/1 lines 82-91 — handler id = {__MODULE__, opts[:id] || :default}, so `id: :test` ⇒ detach key `{Sigra.Audit.Forwarders.Threadline, :test}`; call_threadline/2 238-308 — the mapping the assertions verify: status :ok, name String.to_atom→stored string, correlation_id metadata[:id], actor_ref via ActorRef.new(:user, actor_id))
    - deps/threadline/lib/threadline/semantics/audit_action.ex (audit_actions schema 35-53 — name :string, actor_ref Threadline.Semantics.ActorRef, status Ecto.Enum [ok/error], correlation_id :string)
    - deps/threadline/lib/threadline/semantics/actor_ref.ex (ActorRef.new/2 35-52 — loads as %ActorRef{type: :user, id: ...}; access via struct fields, never map syntax)
    - deps/threadline/lib/threadline.ex (record_action/2 40-62 — requires :actor/:actor_ref + :repo, inserts to audit_actions, returns {:ok, %AuditAction{}})
    - test/example/test/support/data_case.ex (setup_sandbox lines 38-41 — start_owner!(Example.Repo, shared: not tags[:async]); with async:true the conn is owner-pid-bound, but the :sync telemetry handler runs in the test process so the inline insert lands on the owned connection — passing repo: Example.Repo is necessary AND sufficient)
    - test/example/test/test_helper.exs (ExUnit.start(exclude: [:example_app]); Ecto.Adapters.SQL.Sandbox.mode(Example.Repo, :manual) — confirms @moduletag :example_app is the gate)
    - .planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-RESEARCH.md (Recommended Test Structure 160-215; Pitfall 3 query the right table; Pitfall 4 sandbox ownership; Open Question 2 register BEFORE attach so registration events are not forwarded; Validation Architecture anti-under-sampling)
    - .planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-PATTERNS.md (the crux test section 22-143)
    - .planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-VALIDATION.md (anti-under-sampling spec + forbidden under-sampling list)
    - CLAUDE.md (test style: AAA, flat, self-contained; live Postgres at localhost:5432 postgres/postgres required)
  </read_first>
  <behavior>
    - Test: "login audit event materializes as a Threadline audit_actions row" — drive the REAL auth path
      ExampleWeb.UserAuth.log_in_user/2 (NOT a direct Sigra.Audit call), then assert the projected row.
    - Source row exists: a `session.create` Example.Accounts.AuditEvent row for `user.id` exists (the join key audit_event.id).
    - Projection row materialized: Repo.one(from a in Threadline.Semantics.AuditAction, where: a.correlation_id == ^audit_event.id) returns a row (the correlation_id join is MANDATORY — do not weaken to "some row exists").
    - Shape correctness on the projected row: action.name == "session.create" (string), action.status == :ok,
      action.actor_ref.id == user.id, action.actor_ref.type == :user (struct field access, not map access).
  </behavior>
  <action>
    Create test/example/test/example_web/threadline_forwarder_test.exs modeled structurally on
    audit_integration_test.exs. Use `use ExampleWeb.ConnCase, async: true`, `import Ecto.Query`,
    `alias Example.{Accounts, Repo}`, `alias Example.Accounts.AuditEvent`, `alias Threadline.Semantics.AuditAction`.
    Tag `@moduletag :example_app` ONLY (D-10) — do NOT add `:requires_threadline` (that tag belongs to the repo-root
    dep-off lane which never runs inside test/example/). In `setup`: FIRST register the user via
    `Accounts.register_user/1` with a unique email (`tl-#{System.unique_integer([:positive])}@example.test`) and a
    valid password, THEN attach the forwarder (register-before-attach per RESEARCH Open Question 2 so registration-time
    events are never forwarded). Attach with `Sigra.Audit.Forwarders.Threadline.attach(repo: Example.Repo, id: :test,
    dispatch: :sync, actor_type: :user)` — `repo: Example.Repo` (D-08, owns the SQL Sandbox conn), `dispatch: :sync`
    (D-06, deterministic inline; NEVER :auto/:async here). Register `on_exit(fn ->
    :telemetry.detach({Sigra.Audit.Forwarders.Threadline, :test}) end)` using the exact handler-id tuple
    attach/1 builds from `id: :test`. In the single test: drive login via `conn |> Plug.Test.init_test_session(%{}) |>
    ExampleWeb.UserAuth.log_in_user(user)`; fetch the source row `audit_event = Repo.one(from a in AuditEvent, where:
    a.action == "session.create" and a.actor_id == ^user.id, order_by: [desc: a.inserted_at], limit: 1)` and assert it
    is non-nil; then fetch the projection row `action = Repo.one(from a in AuditAction, where: a.correlation_id ==
    ^audit_event.id)` and assert: `action` non-nil, `action.name == "session.create"`, `action.status == :ok`,
    `action.actor_ref.id == user.id`, `action.actor_ref.type == :user` (struct field access). Do NOT use Mox, a test
    sink, or a `:threadline_module:` override (D-01 — assert the REAL row). Do NOT query `Threadline.timeline/2` or
    `actor_history/2` (Pitfall 3 — they read the capture tables, not audit_actions). Do NOT weaken the join to "some
    audit_actions row exists". Keep it flat AAA, self-contained.
  </action>
  <verify>
    <automated>cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/threadline_forwarder_test.exs --include example_app</automated>
  </verify>
  <acceptance_criteria>
    - `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test test/example_web/threadline_forwarder_test.exs --include example_app` exits 0 (live Postgres at localhost:5432 postgres/postgres required per CLAUDE.md).
    - The test file declares `@moduletag :example_app` and does NOT contain `:requires_threadline`, `Mox`, `mock`, or `threadline_module:`.
    - The test drives login through `ExampleWeb.UserAuth.log_in_user` (real auth path) — `grep -q "ExampleWeb.UserAuth.log_in_user" test/example/test/example_web/threadline_forwarder_test.exs`.
    - The projection query joins on the correlation_id key — `grep -q "correlation_id == \^audit_event.id" test/example/test/example_web/threadline_forwarder_test.exs`.
    - The test asserts all four shape conditions: name == "session.create", status == :ok, actor_ref.id == user.id, actor_ref.type == :user (struct field access, not `["..."]` map access).
    - Setup attaches with `dispatch: :sync` and `repo: Example.Repo` and registers an `on_exit` `:telemetry.detach({Sigra.Audit.Forwarders.Threadline, :test})`.
    - PHASE-LEVEL grep proof: `grep -ril threadline test/example/` returns at minimum mix.exs, lib/example/accounts.ex, config/config.exs, test/example_web/threadline_forwarder_test.exs, AGENTS.md, and the two migration files (Success Criterion #2).
    - PHASE-LEVEL lane proof: `cd test/example && PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix test --include example_app` exits 0 (the example_unit_smoke lane command, ci.yml:267) — no new CI job needed.
  </acceptance_criteria>
  <done>The new integration test exits 0 via the existing example_unit_smoke lane command, asserting the full real Sigra→Threadline projection chain joined on correlation_id with the correct actor + action shape, using no mock/stub.</done>
</task>

</tasks>

<threat_model>
This phase ships NO new production code — `lib/` is frozen. It touches only the `test/example/` demo app
and adds a `:dev, :test`-scoped dependency. The realistic threat surface is minimal; threats are
enumerated honestly and proportionately.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/test build ↔ release artifact | The Threadline dep is `only: [:dev, :test]`, so it is excluded from any prod/release build of the example app — it cannot reach a deployed artifact. |
| committed config/docs ↔ adopter copy-paste | The demo config + AGENTS.md are reference material an adopter may copy; leaked secrets or dead/misleading keys would propagate. |
| test process ↔ DB (SQL Sandbox) | The `:sync` inline insert runs in the test process on `Example.Repo`'s owned sandbox connection; rolled back per-test. No external service is contacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Information Disclosure | committed `forwarders:` config block (accounts.ex + config.exs) | mitigate | Drop the dead `endpoint:`/`api_key:` (`THREADLINE_ENDPOINT`/`THREADLINE_API_KEY`) keys entirely (Task 2). Threadline 0.5.0 is DB-based and never reads them; committing them risks adopters wiring real secrets into a demo config block. Verified absent by the Task 2 grep gate. No real credentials appear anywhere in this phase. |
| T-135-02 | Tampering / supply-chain | new `{:threadline, "~> 0.5"}` dep | accept | Threadline is already vendored at 0.5.0 in `deps/threadline/`, pinned in the root `mix.lock:52`, authored within the same szTheory OSS suite, and exercised by the existing library test suite (RESEARCH Package Legitimacy Audit → Approved). slopcheck (npm/PyPI hallucination detector) is N/A for a vendored Hex package. The `:dev, :test` scope keeps it out of any release artifact. |
| T-135-03 | Elevation / scope creep | release artifact contamination | mitigate | `only: [:dev, :test]` scope (Task 1) keeps Threadline out of prod builds; the Task 1 grep gate asserts the exact scope tuple. No `lib/` code is added, so no new runtime attack surface ships to adopters. |
| T-135-SC | Tampering | `mix deps.get` / `mix threadline.install` package install | accept | Threadline is pre-vendored and lock-pinned (no new external fetch resolves a new package); `mix deps.get` reuses the existing root lock entry. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package is introduced, so no blocking-human legitimacy checkpoint is required (RESEARCH audit disposition: Approved). |

N/A categories (no applicable threat for a test-only demo): Spoofing (no authn boundary added — the test uses the real
existing `log_in_user` path), Repudiation (the demo's entire point is to PRODUCE an audit row; no audit suppression is
introduced), Denial of Service (no network listener, rate limiter, or external endpoint added; the `:sync` insert is
sandbox-rolled-back per test).
</threat_model>

<verification>
Phase-level checks (all run inside `test/example/` unless noted; live Postgres at localhost:5432 postgres/postgres):

1. New test green: `mix test test/example_web/threadline_forwarder_test.exs --include example_app` exits 0.
2. Lane parity (no new CI job): `mix test --include example_app` exits 0 (the `example_unit_smoke` command, ci.yml:267).
3. Migrations apply in order: `MIX_ENV=test mix ecto.migrate` runs capture-then-semantics with no duplicate-version
   or missing-table/column error; capture timestamp prefix < semantics timestamp prefix.
4. Grep discoverability (Success Criterion #2): `grep -ril threadline test/example/` returns mix.exs, accounts.ex,
   config.exs, the new test, AGENTS.md, and the two migrations.
5. No dead secrets keys: `grep -r "THREADLINE_ENDPOINT\|THREADLINE_API_KEY\|api_key:" test/example/lib test/example/config`
   returns nothing.
6. Frozen library invariant: `git status --porcelain lib/` shows no changes (no `lib/` file modified).
7. No new top-level examples/ dir and no new CI job: `git status --porcelain examples/ .github/workflows/` shows
   nothing relevant added.
8. Compile clean: `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
</verification>

<success_criteria>
- Running the new test (or the `example_unit_smoke` lane command) exits 0 and asserts a Sigra `session.create` login
  audit event materializes as a real Threadline `audit_actions` row joined on `correlation_id == audit_event.id`, with
  `name == "session.create"`, `status == :ok`, `actor_ref.id == user.id`, `actor_ref.type == :user` (ROADMAP #1).
- `test/example/mix.exs` carries Threadline as a `:dev, :test` dep, both `accounts.ex` and `config.exs` carry the
  `forwarders:` block under the existing `audit:` keyword, and `AGENTS.md` documents the demo — `grep -ri threadline
  test/example/` surfaces a working end-to-end reference in under a minute (ROADMAP #2).
- No new top-level `examples/` directory, no new CI job; the existing 3 `test/example/` CI lanes remain green and the
  `example_unit_smoke` lane executes the new test as-is (ROADMAP #3).
- No file under `lib/` is modified (library code frozen).
</success_criteria>

<output>
Create `.planning/phases/135-reference-example-threadline-forwarder-demo-in-test-example/135-01-SUMMARY.md` when done.
</output>
