---
phase: "182"
plan: "02"
type: execute
wave: 2
depends_on:
  - "182-01"
files_modified:
  - brandbook/index.html
  - scripts/brand/axe-brandbook.mjs
autonomous: true
requirements:
  - BRAND2-09
  - BRAND2-10

must_haves:
  truths:
    - "index.html opens from disk with no build step, CDN, web font, or runtime dependency — only tokens.css"
    - "index.html displays all 8 D4 logo assets in an expanded #logo section with clearspace, misuse, and typemark anatomy documented inline"
    - "A new #suite section documents the szTheory suite architecture (7 libs, shared/per-library table, 3 onboarding rules)"
    - "scripts/brand/axe-brandbook.mjs exits 0 with zero WCAG 2A/2AA violations on brandbook/index.html"
    - "The unnamed scorecard section has id='scorecard' and the nav links to it"
    - "index.html nav includes links to #suite and #scorecard"
  artifacts:
    - path: "scripts/brand/axe-brandbook.mjs"
      provides: "Committed axe harness for brandbook/index.html"
      contains: "AxeBuilder"
    - path: "brandbook/index.html"
      provides: "Professional standalone v2 brand book document"
      contains: "id=\"suite\""
  key_links:
    - from: "scripts/brand/axe-brandbook.mjs"
      to: "test/example/priv/playwright/node_modules"
      via: "createRequire(playwrightBase + 'package.json')"
      pattern: "createRequire"
    - from: "brandbook/index.html"
      to: "brandbook/tokens.css"
      via: "<link rel=\"stylesheet\" href=\"tokens.css\">"
      pattern: "tokens\\.css"
---

<objective>
Expand brandbook/index.html to a professional v2 document: add #scorecard id to the unnamed section, expand the #logo section to show all 8 D4 assets with typemark anatomy + clearspace + misuse inline, add a new #suite section for the szTheory suite architecture. Then create scripts/brand/axe-brandbook.mjs — a committed axe harness serving brandbook/ over localhost — and run it to confirm zero WCAG 2A/2AA violations. Remediate any violations discovered before the plan completes.

Purpose: Closes BRAND2-09 in full (v2 document + axe pass). The axe harness is sequenced after all HTML edits so the gate is run against the final state.

Output: Two files — brandbook/index.html (v2 expansion) and scripts/brand/axe-brandbook.mjs (committed harness).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/182-brand-book-v2-tokens/182-CONTEXT.md
@.planning/phases/182-brand-book-v2-tokens/182-RESEARCH.md
@.planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md
@.planning/phases/182-brand-book-v2-tokens/182-01-SUMMARY.md

<interfaces>
<!-- index.html nav pattern (lines 330-339) — add two entries: -->
<!--   <a href="#scorecard">Scorecard</a>  (after #dna) -->
<!--   <a href="#suite">Suite</a>           (after #logo, before #examples) -->

<!-- index.html #logo section (lines 428-457) — extend, do not rewrite: -->
<!--   .logo-strip / .logo-box.light.lockup / .logo-box.dark.lockup / .logo-box.mark / .logo-box.favicon -->
<!--   New assets to surface (exist in brandbook/ as v2 SVGs): -->
<!--     logo-primary-subtitle.svg  → class="logo-box light lockup" -->
<!--     logo-monochrome.svg        → class="logo-box light mark" (not yet shown in strip) -->
<!--     social-card.svg            → class="logo-box light" -->
<!--     social-card-dark.svg       → class="logo-box dark" -->
<!--   Every <img> MUST have descriptive alt text — not empty (logo specimens communicate meaning) -->

<!-- index.html scorecard section (between #dna and #tokens, no id currently): -->
<!--   Add id="scorecard" to the opening <section> tag -->

<!-- New #suite section pattern (after expanded #logo, before #examples): -->
<!--   <section id="suite"> -->
<!--     <p class="eyebrow">Suite architecture</p> -->
<!--     <h2>Seven libraries, one coherent system.</h2> -->
<!--     <div class="table-wrap panel">  ... 7-lib table ...  </div> -->
<!--     <div class="grid" style="margin-top:var(--sigra-space-4)">  ... 3 rule panels ...  </div> -->
<!--   </section> -->

<!-- Dependency-free constraint: ONLY <link rel="stylesheet" href="tokens.css"> is allowed. -->
<!-- No new <link>, <script src>, @import, CDN, or web font may be added. -->
<!-- All new CSS goes in the existing embedded <style> block. -->

<!-- axe-brandbook.mjs createRequire pattern (from scripts/brand/critique-render.mjs lines 19-24): -->
<!--   const playwrightBase = new URL('../../test/example/priv/playwright/', import.meta.url).pathname; -->
<!--   const require = createRequire(playwrightBase + 'package.json'); -->
<!--   const { chromium } = require('playwright-core'); -->
<!--   const { default: AxeBuilder } = require('@axe-core/playwright'); -->

<!-- axe AxeBuilder call pattern (from admin-checkpoints.spec.ts lines 120-122): -->
<!--   .withTags(['wcag2a', 'wcag2aa'])  — same ruleset scope as admin checkpoint suite -->

<!-- python3 static server: python3 -m http.server PORT --directory brandbookDir -->
<!-- PORT: 7743 (hardcoded; low-collision). Startup wait: ~600ms. -->
<!-- Script exits 0 on zero violations, non-zero on any violation. No screenshots. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand brandbook/index.html — scorecard id, expanded #logo, new #suite section</name>
  <files>brandbook/index.html</files>
  <read_first>
    - brandbook/index.html — full file (understand all 8 existing sections, the current #logo section structure, the unnamed scorecard section, the nav, and the embedded style block)
    - .planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md — "index.html (static document — extend in-place)" section: nav pattern, logo-strip CSS classes, new assets list with alt text, suite section HTML pattern, scorecard id fix
    - .planning/phases/182-brand-book-v2-tokens/182-RESEARCH.md — Section 2 (alt text pitfall note), Section 5 (suite architecture: 7 libs, shared table, 3 rules, domain metaphors), Section 6 stale-specimens note (confirm existing #examples section references are still correct)
    - brandbook/pressure-test-audit-v2.md — Section 3 (Pressure-Test Scorecard: section content and scores for the scorecard display), Section 8 (suite architecture logos and domain metaphors)
    - .planning/phases/182-brand-book-v2-tokens/182-01-SUMMARY.md — confirm brand-book.md suite architecture content to keep index.html consistent
  </read_first>
  <action>
    Make four targeted edits to brandbook/index.html. Do not rewrite sections wholesale — extend and fix surgically.

    Edit 1 — Fix unnamed scorecard section:
    Find the `<section>` tag that opens the scorecard section (it has an eyebrow element containing "Scorecard" and is between #dna and #tokens). Add `id="scorecard"` to that opening tag. This section now has an accessible id.

    Edit 2 — Update nav to include #scorecard and #suite:
    In the `<nav class="nav" aria-label="Brand book sections">`, add `<a href="#scorecard">Scorecard</a>` after the `<a href="#dna">` link. Add `<a href="#suite">Suite</a>` after the `<a href="#logo">` link and before the `<a href="#examples">` link. Keep all existing nav links unchanged.

    Edit 3 — Expand the #logo section:
    Within the existing `<section id="logo">` element, keep the existing content (h2, lead paragraph, the logo-strip with the four currently shown lockups, and the rules/avoid article panels). Extend it by:
    a) Update the `<h2>` to reflect v2 ("D4 Linked Rail: the ratified Sigra mark system." or similar — make it accurate to the ratified name).
    b) After the existing logo-strip div, add a second row or extend the existing strip to include: logo-primary-subtitle.svg (class="logo-box light lockup", alt="Sigra primary logo with subtitle, light surface"), logo-monochrome.svg (class="logo-box light mark", alt="Sigra monochrome mark for single-ink contexts"), social-card.svg (class="logo-box light", alt="Sigra social card preview, light surface"), social-card-dark.svg (class="logo-box dark", alt="Sigra social card preview, dark surface"). All four new img elements MUST have non-empty descriptive alt text.
    c) After the rules/avoid section (or within it), add a brief `<article class="panel">` covering the typemark anatomy: that the rail-block tittle (ember-colored block above the `i`) and the extended `g` tail are the two integrated elements forming the bracketing rail system. Use `<h3>` for the heading ("Integrated typemark anatomy"), followed by a short `<ul>` or `<p>`.
    d) Document clearspace and minimum sizes inline — the existing avoid/use panels may already touch this; extend them or add a new panel if the content is not already present. Minimum size rule: primary lockup at 120px minimum width; mark alone at 24px minimum; favicon at 16px. Clearspace: equal to the mark height on all four sides.
    e) Add (or expand) a misuse panel showing at least 4 misuse examples as list items (e.g., do not add rectangular container behind the mark; do not rotate; do not use ember-300 as fill color; do not use monochrome at sizes below 24px). The existing "Avoid" panel may be extended rather than replaced.

    Edit 4 — Add new #suite section:
    Insert a new `<section id="suite">` after the closing tag of `<section id="logo">` and before `<section id="examples">`.
    Structure (follow the section pattern from PATTERNS.md):
    - `<p class="eyebrow">Suite architecture</p>`
    - `<h2>Seven libraries, one coherent system.</h2>`
    - A brief lead paragraph: the szTheory OSS suite uses a shared design vocabulary while each library owns its mark, accent hue, and domain metaphor.
    - A `<div class="table-wrap panel">` containing a `<table>` with columns: Library | Domain metaphor | Accent hue. Rows for all 7 libraries (Sigra/ember ~15°, Accrue/ledger-accumulation/TBD, Mailglass/transparent-delivery/TBD, Threadline/trace/TBD, Lockspire/lock-spire/TBD, Relyra/reliability/TBD, Rulestead/governance/TBD). For libraries where accent hue is TBD, use "unique hue, ≥15° from siblings" as the value — do not invent colors.
    - A `<div class="grid">` with three `<article class="panel">` elements for the three onboarding rules: (1) Inherit Space Grotesk v2.0 OFL wght=700 as the suite wordmark typeface; (2) Adopt a unique mark reflecting the library's domain metaphor; (3) Select an accent hue at least 15° from any existing sibling's accent on the color wheel.

    CSS constraint: All new CSS needed for the suite section (table styles, grid, panel) should reuse existing classes already defined in the embedded `<style>` block (.table-wrap, .panel, .grid, .eyebrow, etc.). Do not hardcode new hex values in the style block — use `var(--sigra-*)` tokens. Do not add any `<link>`, `<script src>`, `@import`, or CDN reference.
  </action>
  <verify>
    <automated>
      grep -c 'id="scorecard"' /Users/jon/projects/sigra/brandbook/index.html
      grep -c 'href="#scorecard"' /Users/jon/projects/sigra/brandbook/index.html
      grep -c 'id="suite"' /Users/jon/projects/sigra/brandbook/index.html
      grep -c 'href="#suite"' /Users/jon/projects/sigra/brandbook/index.html
      grep -c 'logo-primary-subtitle' /Users/jon/projects/sigra/brandbook/index.html
      grep -c 'social-card-dark' /Users/jon/projects/sigra/brandbook/index.html
      grep -cE 'http[s]?://|cdn\.|@import' /Users/jon/projects/sigra/brandbook/index.html
      grep -c '<link' /Users/jon/projects/sigra/brandbook/index.html
      xmllint --noout /Users/jon/projects/sigra/brandbook/index.html && echo "index.html: valid"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="scorecard"' brandbook/index.html` outputs `1`
    - `grep -c 'href="#scorecard"' brandbook/index.html` outputs `1` (nav link present)
    - `grep -c 'id="suite"' brandbook/index.html` outputs `1`
    - `grep -c 'href="#suite"' brandbook/index.html` outputs `1` (nav link present)
    - `grep -c 'logo-primary-subtitle' brandbook/index.html` outputs at least `1`
    - `grep -c 'social-card-dark' brandbook/index.html` outputs at least `1`
    - No external dependencies: `grep -cE 'http[s]?://|cdn\.|@import' brandbook/index.html` outputs `0`
    - Exactly one `<link>` tag: `grep -c '<link' brandbook/index.html` outputs `1`
    - That link is tokens.css: `grep -c 'tokens.css' brandbook/index.html` outputs `1`
    - `xmllint --noout brandbook/index.html` exits 0 (valid HTML/XML)
    - Every new `<img>` tag has a non-empty `alt` attribute (verify: `grep 'logo-primary-subtitle\|social-card-dark\|social-card\.svg\|logo-monochrome' brandbook/index.html | grep -c 'alt=""'` outputs `0`)
  </acceptance_criteria>
  <done>index.html v2: scorecard section has id; nav links to #scorecard and #suite; #logo section shows all 8 D4 assets with typemark anatomy + clearspace + misuse; #suite section documents 7-lib suite architecture; no external dependencies introduced</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scripts/brand/axe-brandbook.mjs and run the axe gate; remediate any violations</name>
  <files>scripts/brand/axe-brandbook.mjs</files>
  <read_first>
    - scripts/brand/critique-render.mjs — full file (the createRequire pattern lines 12-24; module structure; shebang + imports; top-level await usage)
    - scripts/brand/package.json — confirm "type": "module" and that no new deps are needed
    - .planning/phases/182-brand-book-v2-tokens/182-RESEARCH.md — Section 1 (full axe-brandbook.mjs skeleton — use it verbatim as the starting implementation, then adjust for any deviation needed)
    - .planning/phases/182-brand-book-v2-tokens/182-PATTERNS.md — "axe-brandbook.mjs (NEW utility script)" section: createRequire pattern, AxeBuilder call, divergence from critique-render.mjs
    - test/example/priv/playwright/tests/admin-checkpoints.spec.ts — lines 115-126 (AxeBuilder + assertNoAxeViolations pattern; confirm .withTags(['wcag2a','wcag2aa']))
  </read_first>
  <action>
    Create scripts/brand/axe-brandbook.mjs using the RESEARCH.md Section 1 skeleton as the implementation. Key implementation decisions (do not deviate):

    - Shebang: `#!/usr/bin/env node`
    - Imports: `createRequire` from 'module', `spawn` from 'child_process', `resolve` and `dirname` from 'path', `fileURLToPath` from 'url'
    - The `createRequire` pattern loads playwright-core from the existing test/example install (uses `new URL('../../test/example/priv/playwright/', import.meta.url).pathname` for the base path, exactly as in critique-render.mjs)
    - Load `@axe-core/playwright` via the same `require` call: `const { default: AxeBuilder } = require('@axe-core/playwright')`
    - PORT: 7743 (hardcoded in the script)
    - Static server: `spawn('python3', ['-m', 'http.server', String(PORT), '--directory', brandbookDir], { stdio: 'ignore' })`
    - Server startup wait: 600ms (single `await new Promise(r => setTimeout(r, 600))`)
    - AxeBuilder call: `new AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa']).analyze()`
    - On zero violations: log "axe: PASS — zero violations on brandbook/index.html" and exit 0
    - On violations: log each violation id, impact, description, and first 2 affected node targets; set exitCode = 1; exit 1
    - `server.kill()` in a `finally` block before `process.exit(exitCode)`
    - No `page.screenshot()` calls — do not commit screenshots

    After creating the script, run it:
    `node scripts/brand/axe-brandbook.mjs`

    If the script exits 0: the axe gate passes — done.

    If the script exits non-zero (violations found): read the violation output carefully. For each violation:
    - If it is a missing alt text on a new img element added in Task 1: edit brandbook/index.html to add the descriptive alt text, then re-run.
    - If it is a color contrast issue on new content added in Task 1: replace the hardcoded color with the appropriate `var(--sigra-*)` token (all tokens are pre-verified to pass AA), then re-run.
    - If it is a structural issue (missing label, role, etc.): fix the specific element in brandbook/index.html, then re-run.
    - Log the remediation performed. Re-run until the script exits 0.

    Do not add the `--no-sandbox` Chromium flag unless the script fails to launch the browser entirely (on macOS this is not needed for Chromium from playwright-core).
  </action>
  <verify>
    <automated>
      node /Users/jon/projects/sigra/scripts/brand/axe-brandbook.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/brand/axe-brandbook.mjs` exits 0 with output "axe: PASS — zero violations on brandbook/index.html"
    - The script file exists: `test -f scripts/brand/axe-brandbook.mjs && echo PASS`
    - The script uses the createRequire pattern (no new npm install): `grep -c 'createRequire' scripts/brand/axe-brandbook.mjs` outputs `1`
    - The script loads from the test/example playwright node_modules path: `grep -c 'test/example/priv/playwright' scripts/brand/axe-brandbook.mjs` outputs `1`
    - The script uses the correct tag scope: `grep -c "withTags\(\['wcag2a'" scripts/brand/axe-brandbook.mjs` outputs `1`
    - No screenshot calls committed: `grep -c 'screenshot' scripts/brand/axe-brandbook.mjs` outputs `0`
    - scripts/brand/package.json is UNCHANGED (no new dependencies added)
  </acceptance_criteria>
  <done>scripts/brand/axe-brandbook.mjs committed; runs against brandbook/index.html over localhost:7743; exits 0 with zero WCAG 2A/2AA violations; any violations discovered during the run were remediated in brandbook/index.html before the plan completed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → localhost:7743 | python3 static server serves brandbook/ over loopback only; no external network exposure |
| localhost:7743 → Chromium | Chromium (headless) navigates to the local page; no external URL loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-03 | Information Disclosure | axe-brandbook.mjs logging | accept | Script only logs violation metadata to stdout/stderr; no PII, no secrets |
| T-182-04 | Tampering | index.html dependency-free constraint | mitigate | Automated grep gate: `grep -cE 'http[s]?://|cdn\.|@import' brandbook/index.html` must output 0; only one `<link>` tag allowed |
| T-182-05 | Denial of Service | Port 7743 collision | accept | Low-probability on dev machine; script fails fast with a bind error; no data loss |
| T-182-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; createRequire reuses existing test/example node_modules without install |
</threat_model>

<verification>
After both tasks complete, run the full Phase 182 check suite:

```bash
# 1. axe gate — zero violations (primary gate, must be last)
node scripts/brand/axe-brandbook.mjs

# 2. No external dependencies in index.html (expected: 0)
grep -cE 'http[s]?://|cdn\.|@import|<script[[:space:]]+src' brandbook/index.html

# 3. Only one <link> tag and it's tokens.css (expected: 1, 1)
grep -c '<link' brandbook/index.html
grep -c 'tokens.css' brandbook/index.html

# 4. New sections present
grep -c 'id="scorecard"' brandbook/index.html    # expected: 1
grep -c 'id="suite"' brandbook/index.html        # expected: 1

# 5. All 8 D4 logo assets referenced in index.html
grep -c 'logo-primary-subtitle' brandbook/index.html    # expected: ≥1
grep -c 'social-card-dark' brandbook/index.html         # expected: ≥1

# 6. HTML parses as valid
xmllint --noout brandbook/index.html

# 7. axe script exists and has correct pattern
grep -c 'createRequire' scripts/brand/axe-brandbook.mjs    # expected: 1
grep -c 'screenshot' scripts/brand/axe-brandbook.mjs       # expected: 0

# 8. All prior Plan 01 checks still green
jq -r '.version' brandbook/tokens.json                # expected: 1.0.1
grep -c 'M17 14v14' brandbook/examples/landing-hero.svg   # expected: 0
grep -c 'M17 14v14' brandbook/examples/readme-header.svg  # expected: 0
```
</verification>

<success_criteria>
- brandbook/index.html is a standalone v2 document with #scorecard id, expanded #logo showing all 8 D4 assets, typemark anatomy, clearspace, misuse, and a new #suite section for suite architecture
- scripts/brand/axe-brandbook.mjs is committed and exits 0 (zero WCAG 2A/2AA violations) when run against the final brandbook/index.html
- No external dependency introduced into index.html (only tokens.css <link> present)
- All Plan 01 checks remain green (tokens, specimens, README)
</success_criteria>

<output>
Create `.planning/phases/182-brand-book-v2-tokens/182-02-SUMMARY.md` when done
</output>
