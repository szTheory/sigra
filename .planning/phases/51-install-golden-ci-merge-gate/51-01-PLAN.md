---
phase: "51"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - MAINTAINING.md
  - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "PRs touching **`lib/sigra/mfa/`**, **`lib/sigra/oauth/`**, **`lib/sigra/account/`**, or **`lib/sigra/passkeys/`** (root `.ex` or subtree) trigger **`install_golden_contract`** and **`installer_milestone_audit`** the same way installer-template paths already do."
    - "**`installer_milestone_audit`** and **`install_golden_contract`** use one identical **`grep -qE`** pattern (verified by ExUnit)."
    - "**`MAINTAINING.md`** documents the expanded PR path policy next to the existing installer golden section."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Extended path detector for both installer-related jobs"
    - path: "test/sigra/planning/phase_51_install_golden_ci_contract_test.exs"
      provides: "Regression lock on detector parity + extended prefixes"
nyquist_compliant: false
---

<objective>
Close **CI-PATH-COUPLING** by widening the PR diff detector for **`install_golden_contract`** and **`installer_milestone_audit`** to GA-adjacent **`lib/sigra`** surfaces, and lock the behavior with a fast structural ExUnit contract.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-51-01 | Tampering | Maintainer edits one job’s regex but not the other | high | ExUnit asserts both jobs contain the same canonical substring |
| T-51-02 | Elevation | Over-broad regex runs golden harness on every doc PR | medium | Limit extensions to **`mfa` / `oauth` / `account` / `passkeys`** paths only (per **`51-RESEARCH.md`**) |
| T-51-03 | Repudiation | Docs disagree with YAML | low | Update **`MAINTAINING.md`** in the same plan with the literal path classes |
| T-51-04 | Information disclosure | N/A for path filters | low | No new secrets |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Extend PR path detectors in ci.yml (both jobs)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/51-install-golden-ci-merge-gate/51-RESEARCH.md
    - .planning/phases/51-install-golden-ci-merge-gate/51-CONTEXT.md
  </read_first>
  <action>
    In **both** jobs **`installer_milestone_audit`** and **`install_golden_contract`**, replace the **`git diff … \| grep -qE`** pattern:

    **From (current):**
    `grep -qE '^priv/templates/sigra\.install/|^lib/sigra/install/'`

    **To (new — copy verbatim into both jobs, same string):**
    `grep -qE '^priv/templates/sigra\.install/|^lib/sigra/install/|^lib/sigra/mfa(\.ex|/)|^lib/sigra/oauth(\.ex|/)|^lib/sigra/account(\.ex|/)|^lib/sigra/passkeys(\.ex|/)'`

    Add a one-line YAML comment above each modified **`run:`** block: `# Phase 51: include GA-adjacent lib surfaces (MFA/OAuth/account/passkeys) — see MAINTAINING.md §Installer golden CI contract.`
  </action>
  <acceptance_criteria>
    - `grep -c "lib/sigra/mfa" .github/workflows/ci.yml` prints **`2`** (both detect steps updated).
    - `grep -c "lib/sigra/passkeys" .github/workflows/ci.yml` prints **`2`**.
    - `grep -c '\^lib/sigra/install/' .github/workflows/ci.yml` prints **`2`**.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
  </acceptance_criteria>
  <verify>
    `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs:41` exits 0 (existing **`50-02-01`** contract still satisfied for unchanged **`mix.exs`** / job names).
  </verify>
</task>

<task type="execute">
  <name>Task 2: Document expanded PR path policy in MAINTAINING.md</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Under **`## Installer golden CI contract (phase 50)`**, after the paragraph that cites **`.github/workflows/ci.yml`**, insert a short bullet list titled **`### PR paths that run install_golden_contract (phase 51)`** listing exactly these classes (plain text, no regex): **`priv/templates/sigra.install/`**, **`lib/sigra/install/`**, **`lib/sigra/mfa`** (file + directory), **`lib/sigra/oauth`**, **`lib/sigra/account`**, **`lib/sigra/passkeys`**. State that **`installer_milestone_audit`** uses the same diff rule.
  </action>
  <acceptance_criteria>
    - `grep -q "PR paths that run install_golden_contract" MAINTAINING.md` exits 0.
    - `grep -q "lib/sigra/passkeys" MAINTAINING.md` exits 0.
    - `grep -q "installer_milestone_audit" MAINTAINING.md` exits 0.
  </acceptance_criteria>
  <verify>
    `mix format --check-formatted` is not required for Markdown; skip unless **`MAINTAINING.md`** embeds Elixir.
  </verify>
</task>

<task type="execute">
  <name>Task 3: Add phase_51_install_golden_ci_contract_test.exs</name>
  <files>test/sigra/planning/phase_51_install_golden_ci_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_50_nyquist_docs_contract_test.exs
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Create **`test/sigra/planning/phase_51_install_golden_ci_contract_test.exs`** with `use ExUnit.Case, async: true` and:

    1. A module attribute **`@path_detector_regex`** holding the exact extended **`grep -qE '…'`** inner pattern string (without the wrapping single quotes): `^priv/templates/sigra\.install/|^lib/sigra/install/|^lib/sigra/mfa(\.ex|/)|^lib/sigra/oauth(\.ex|/)|^lib/sigra/account(\.ex|/)|^lib/sigra/passkeys(\.ex|/)`

    2. A test that reads **`.github/workflows/ci.yml`** as a string and asserts **`String.split(yml, @path_detector_regex)`** has length **>** 1 (pattern present), and that **`Regex.scan(~r/grep -qE '([^']+)'/, yml, capture: :all_but_first)`** or a simpler **`String.split(yml, @path_detector_regex)`** count shows the detector appears **exactly twice** (both jobs). Prefer a straightforward assertion: `assert Regex.scan(~r#{Regex.escape(@path_detector_regex)}#, yml) |> length() == 2`.

    3. Assert the file still contains **`install_golden_contract:`** and **`installer_milestone_audit:`** job keys.

    Name the test **`test "51-01: installer PR path detector extended and duplicated across both jobs"`**.
  </action>
  <acceptance_criteria>
    - `mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits 0.
    - File path exists: **`test/sigra/planning/phase_51_install_golden_ci_contract_test.exs`**.
  </acceptance_criteria>
  <verify>
    `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits 0.
  </verify>
</task>

</tasks>

<verification>
- `mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits 0.
- `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs` exits 0.
- Manual spot-check: open **`.github/workflows/ci.yml`** and confirm both **`Detect installer-related changes`** steps share the same **`grep -qE`** argument.
</verification>

<success_criteria>
- ROADMAP acceptance **(2)** satisfied: CI matrix does not skip **`install_golden_contract`** on PRs that only touch the listed MFA/OAuth/account/passkeys **`lib/sigra`** paths.
- **`51-VALIDATION.md`** rows **51-01-01** / **51-01-02** can move to ✅ after tasks complete.
</success_criteria>
