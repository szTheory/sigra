---
phase: "51"
plan: "02"
type: execute
wave: 2
depends_on:
  - "51-01"
files_modified:
  - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
  - test/sigra/planning/phase_50_nyquist_docs_contract_test.exs
  - MAINTAINING.md
  - .planning/v1.4-GA-UAT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "**`50-VERIFICATION.md`** records a grep-able **`PASS`** token for the merge gate row **before** frontmatter **`status: passed`**."
    - "**`phase_50_nyquist_docs_contract_test.exs`** no longer requires eternal **`status: draft`** once a PASS receipt exists."
    - "**`v1.4-GA-UAT.md`** GA-03 / GA-04 rows (or adjacent note) cite **`mix ci.install_golden`** / **`install_golden_contract`** as the installer subprocess substitute for template drift, without changing waiver dates or owners."
  artifacts:
    - path: ".planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md"
      provides: "Merge gate PASS receipt + timing"
nyquist_compliant: false
---

<objective>
Close **PHASE-50-INSTALL-GOLDEN** and **GA-WAIVERS-vs-INSTALL-RECEIPT**: record a real **`mix ci.install_golden`** **PASS** in **`50-VERIFICATION.md`**, align the phase-50 ExUnit contract with **`status: passed`**, and add explicit cross-links for GA-03/GA-04 readers to the installer golden harness.
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-51-05 | Repudiation | **`PASS`** recorded without a green merge gate | high | Executor runs **`mix ci.install_golden`** (or pastes CI run link + run id in **Notes** if local impossible — must not fabricate timing) |
| T-51-06 | Tampering | Frontmatter **`status: passed`** while table still says Not recorded | high | Single edit session: update table row then YAML frontmatter |
| T-51-07 | Information disclosure | Paste secrets into verification doc | low | Only **`git rev-parse HEAD`**, wall time, and public CI URLs |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Run merge gate and record PASS + timing in 50-VERIFICATION.md</name>
  <files>.planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
    - CLAUDE.md
    - mix.exs
  </read_first>
  <action>
    1. Ensure Postgres **`localhost:5432`** **`postgres`/`postgres`** is available (see **`CLAUDE.md`**).

    2. From repo root, time and run:

       `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.install_golden`

    3. On **exit 0**, edit **`50-VERIFICATION.md`**:
       - In **`## Automated checks run`**, replace the **`Not recorded yet`** cell with **`PASS`** and append **wall-clock seconds** (e.g. **`PASS (187s)`**) from `/usr/bin/time` or shell **`time`** output.
       - Update **`## Notes`** with new **`git rev-parse HEAD`** after the run.
       - Set YAML frontmatter **`status: passed`** (keep **`phase: \"50\"`**).

    If the command cannot complete locally, **do not** mark **`PASS`**: instead add an honest **Notes** line citing a successful GitHub Actions **`install_golden_contract`** run URL + run id, and set **`status: draft`** until a maintainer reruns locally — then stop this task as blocked (**`autonomous: false`**).
  </action>
  <acceptance_criteria>
    - `grep -E '\|.*PASS' .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0 when merge gate succeeded.
    - `grep -q 'mix ci.install_golden' .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0 (unchanged requirement).
    - Frontmatter line is either **`status: passed`** with a **PASS** row, or task stopped with CI URL note and **`status: draft`**.
  </acceptance_criteria>
  <verify>
    If **`status: passed`**: `PGUSER=postgres PGPASSWORD=postgres PGHOST=localhost MIX_ENV=test mix ci.install_golden` exits 0 when re-run on the same commit.
  </verify>
</task>

<task type="execute">
  <name>Task 2: Update phase_50 ExUnit contract for 50-VERIFICATION status + PASS</name>
  <files>test/sigra/planning/phase_50_nyquist_docs_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_50_nyquist_docs_contract_test.exs
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
  </read_first>
  <action>
    Replace test **`50-02 plan: 50-VERIFICATION exists with merge gate block and draft posture until run`** with two assertions:

    1. **`assert body =~ "mix ci.install_golden"`** and **`assert body =~ "git rev-parse HEAD"`** (retain).

    2. If **`body =~ ~r/^status: passed/m`**, then **`assert body =~ "PASS"`** (substring anywhere in **Automated checks** table). Else **`assert body =~ ~r/^status: draft/m`** (draft posture still allowed).

    Do **not** require **`status: draft`** unconditionally.
  </action>
  <acceptance_criteria>
    - `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs:82` (line range containing the renamed test — adjust to match) exits 0.
    - `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    `mix format --check-formatted test/sigra/planning/phase_50_nyquist_docs_contract_test.exs` exits 0.
  </verify>
</task>

<task type="execute">
  <name>Task 3: GA waiver cross-link (MAINTAINING + v1.4-GA-UAT)</name>
  <files>MAINTAINING.md, .planning/v1.4-GA-UAT.md</files>
  <read_first>
    - .planning/v1.4-GA-UAT.md
    - MAINTAINING.md
    - .planning/phases/51-install-golden-ci-merge-gate/51-CONTEXT.md
  </read_first>
  <action>
    1. **`MAINTAINING.md`**: Under the same **Installer golden** section, add one sentence: waived **GA-03** / **GA-04** machine substitutes (OAuth mock + getting-started contract) do **not** replace **`mix ci.install_golden`** for **`priv/templates/sigra.install/`** drift — cite **`v1.4-GA-UAT.md`** and **`.planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md`**.

    2. **`v1.4-GA-UAT.md`**: Append a **single footnote line** immediately under the GA-03 / GA-04 table (or a new **`### Phase 51 — installer receipt cross-link`** subsection above the table if cleaner) containing the exact substrings **`mix ci.install_golden`** and **`install_golden_contract`** explaining installer subprocess attestation is tracked in **`50-VERIFICATION.md`**. Do **not** alter existing **Waived** status cells.
  </action>
  <acceptance_criteria>
    - `grep -n "GA-03" MAINTAINING.md | head -1` exits 0 **or** `grep -n "GA-04" MAINTAINING.md | head -1` exits 0 **or** both — at least one GA row referenced near **`ci.install_golden`**.
    - `grep -q "50-VERIFICATION" MAINTAINING.md` exits 0.
    - `grep -q "install_golden_contract" .planning/v1.4-GA-UAT.md` exits 0.
    - `grep -q "mix ci.install_golden" .planning/v1.4-GA-UAT.md` exits 0.
  </acceptance_criteria>
  <verify>
    `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits 0.
  </verify>
</task>

</tasks>

<verification>
- `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits 0.
- `grep PASS .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits 0 **if** **`status: passed`**.
</verification>

<success_criteria>
- ROADMAP **(1)** and **(3)** satisfied.
- **`51-VALIDATION.md`** manual row for merge gate marked done in sign-off when PASS is real.
</success_criteria>
