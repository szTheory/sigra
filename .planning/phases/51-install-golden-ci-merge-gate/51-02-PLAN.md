---
phase: "51"
plan: "02"
type: execute
wave: 2
depends_on:
  - "51-01"
files_modified:
  - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "**`50-VERIFICATION.md`** keeps **CI-as-truth** wording (required **`install_golden_contract`** on **`main`**); no pasted markdown **PASS** row as proof of merge gate."
    - "**`MAINTAINING.md`** and **`v1.4-GA-UAT.md`** both cite **`mix ci.install_golden`** / **`install_golden_contract`** and point readers at **`50-VERIFICATION.md`** for installer attestation."
    - "**`phase_51_install_golden_ci_contract_test.exs`** includes a regression test locking the GA waiver ↔ installer cross-link substrings."
  artifacts:
    - path: "test/sigra/planning/phase_51_install_golden_ci_contract_test.exs"
      provides: "ExUnit lock on GA-UAT + MAINTAINING installer attestation strings"
nyquist_compliant: false
---

<objective>
Close **GA-WAIVERS-vs-INSTALL-RECEIPT** for the **post-phase-50** truth model: waived GA rows stay clearly distinct from **`priv/templates/sigra.install/`** drift coverage, with machine-verifiable doc strings and one structural ExUnit test (no edits to **`50-VERIFICATION.md`** frontmatter or CI-as-truth section unless a regression is found).
</objective>

<threat_model>
| ID | STRIDE | Finding | Severity | Mitigation in plan |
|----|--------|---------|----------|-------------------|
| T-51-05 | Repudiation | Docs imply GA waivers replace installer golden | high | Assertions require **`50-VERIFICATION.md`** substring + **`do not`** / **`not`** disambiguation language in **`MAINTAINING.md`** or **`v1.4-GA-UAT.md`** |
| T-51-06 | Tampering | Cross-links removed silently | medium | New ExUnit reads both Markdown files from repo root |
| T-51-07 | Information disclosure | N/A | low | Tests only assert public doc paths |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Structural tests green (50 + 51 planning contracts)</name>
  <files>none</files>
  <read_first>
    - test/sigra/planning/phase_50_nyquist_docs_contract_test.exs
    - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
  </read_first>
  <action>
    From repo root, run:

    `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_51_install_golden_ci_contract_test.exs`

    If anything fails, fix **code or planning docs** until this command exits **0** — do **not** add a pasted **PASS** row to **`50-VERIFICATION.md`** (that model was superseded by CI-as-truth; see **`51-VALIDATION.md`**).
  </action>
  <acceptance_criteria>
    - The **`mix test …`** command above exits **0**.
  </acceptance_criteria>
  <verify>
    Re-run the same **`mix test`** line once after any edit; exit **0**.
  </verify>
</task>

<task type="execute">
  <name>Task 2: ExUnit lock — GA waiver docs reference installer attestation</name>
  <files>test/sigra/planning/phase_51_install_golden_ci_contract_test.exs</files>
  <read_first>
    - test/sigra/planning/phase_51_install_golden_ci_contract_test.exs
    - MAINTAINING.md
    - .planning/v1.4-GA-UAT.md
    - .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md
  </read_first>
  <action>
    Append a new test named **`test "51-02: GA waiver docs tie waived rows to installer golden attestation"`** that:

    1. Reads **`MAINTAINING.md`** and asserts it contains **`v1.4-GA-UAT.md`**, **`50-VERIFICATION.md`**, **`install_golden_contract`**, and **`mix ci.install_golden`** (substring assertions).

    2. Reads **`.planning/v1.4-GA-UAT.md`** and asserts it contains **`50-VERIFICATION.md`**, **`install_golden_contract`**, and **`mix ci.install_golden`**.

    Use the same **`read!/1`** helper pattern as existing tests in this file.
  </action>
  <acceptance_criteria>
    - `mix test test/sigra/planning/phase_51_install_golden_ci_contract_test.exs:31` exits **0** (line anchors the **`51-02:`** doc-link regression test).
    - `mix format --check-formatted test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits **0**.
  </acceptance_criteria>
  <verify>
    `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits **0**.
  </verify>
</task>

</tasks>

<verification>
- `mix test test/sigra/planning/phase_50_nyquist_docs_contract_test.exs test/sigra/planning/phase_51_install_golden_ci_contract_test.exs` exits **0**.
- `grep -q "CI attestation" .planning/phases/50-nyquist-ci-gate-hygiene/50-VERIFICATION.md` exits **0**.
</verification>

<success_criteria>
- ROADMAP acceptance **(1)** and **(3)** remain satisfied with machine-checked doc strings.
- **`51-VALIDATION.md`** sign-off rows for **51-02-0x** stay accurate after the new test lands.
</success_criteria>
