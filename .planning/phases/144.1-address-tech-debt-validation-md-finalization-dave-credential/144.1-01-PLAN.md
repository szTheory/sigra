---
phase: 144.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md
  - docs/ga-evidence.md
  - test/example/README.md
  - guides/introduction/demo-showcase.md
  - test/example/priv/playwright/tests/demo-showcase.spec.ts
autonomous: true
requirements:
  - TD-01
  - TD-02
  - TD-03
  - TD-04
  - TD-05
must_haves:
  truths:
    - "144-VALIDATION.md frontmatter reads status: finalized, nyquist_compliant: true, wave_0_complete: true"
    - "All 10 Per-Task Verification Map rows in 144-VALIDATION.md show ✅ green (none show ⬜ pending)"
    - "Wave 0 checkboxes in 144-VALIDATION.md are all checked and sign-off block uses [x]"
    - "docs/ga-evidence.md contains no v0.2.0 tag URLs; all three tag snapshot links use v0.2.1"
    - "test/example/README.md Dave callout refers to correct password and wrong passwords both triggering enumeration-resistant response"
    - "guides/introduction/demo-showcase.md Dave section includes correct password DaveDemoPass1!Locked and explains enumeration resistance applies to both correct and wrong password attempts"
    - "demo-showcase.spec.ts Step 2 comment contains no TOTP challenge language"
  artifacts:
    - path: ".planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md"
      provides: "Finalized phase validation record for Phase 144"
      contains: "nyquist_compliant: true"
    - path: "docs/ga-evidence.md"
      provides: "GA evidence page with correct v0.2.1 tag links"
      contains: "v0.2.1"
    - path: "test/example/README.md"
      provides: "Updated Dave callout with accurate enumeration-resistance framing"
      contains: "correct password"
    - path: "guides/introduction/demo-showcase.md"
      provides: "Updated Dave section with correct password and full enumeration-resistance explanation"
      contains: "DaveDemoPass1!Locked"
    - path: "test/example/priv/playwright/tests/demo-showcase.spec.ts"
      provides: "Corrected Step 2 comment — no TOTP challenge language"
  key_links:
    - from: "144-VALIDATION.md frontmatter"
      to: "nyquist_compliant: true"
      via: "direct frontmatter field"
      pattern: "nyquist_compliant: true"
    - from: "docs/ga-evidence.md tag snapshot links"
      to: "v0.2.1 git tag on GitHub"
      via: "hardcoded URL"
      pattern: "blob/v0.2.1"
---

<objective>
Close five discrete tech-debt items deferred from Phase 144 (README Evaluator Lane & Docs/Proof). Phase 144 shipped with 6/6 proof gates green but left: its own VALIDATION.md in draft state, one outstanding code review warning (WR-01 — stale v0.2.0 tag URLs), a misleading Dave credential callout in two docs files, and an incorrect step label in the Playwright spec.

Purpose: Bring all Phase 144 planning artifacts and documentation into accurate final state. Zero functional or behavioral changes — pure prose, metadata, and comment corrections.

Output: Five file edits. One planning artifact finalized. One broken-link warning resolved. Two docs files with accurate Dave enumeration-resistance framing. One spec comment corrected.
</objective>

<execution_context>
@/Users/jon/projects/sigra/.planning/phases/144.1-address-tech-debt-validation-md-finalization-dave-credential/144.1-01-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/sigra/.planning/PROJECT.md
@/Users/jon/projects/sigra/.planning/ROADMAP.md
@/Users/jon/projects/sigra/.planning/STATE.md
@/Users/jon/projects/sigra/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md
@/Users/jon/projects/sigra/.planning/phases/144-readme-evaluator-lane-docs-proof/144-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize 144-VALIDATION.md — update frontmatter, all task statuses, Wave 0 checkboxes, and sign-off block (TD-01)</name>
  <files>.planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md</files>
  <read_first>
    - .planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md (the file being edited — read the full file before making any change)
    - .planning/phases/144-readme-evaluator-lane-docs-proof/144-VERIFICATION.md (evidence that all 10 tasks are green; confirms which tasks belong to Wave 1 vs Wave 2 and what Wave 0 deliverables exist)
  </read_first>
  <action>
Phase 144 executed successfully — VERIFICATION.md records 6/6 gates PASS, all 10 tasks completed, all automated verify commands confirmed passing. The VALIDATION.md was written pre-execution and never updated. Make the following concrete changes:

**Frontmatter** — replace the current block:
```
status: draft
nyquist_compliant: false
wave_0_complete: false
```
with:
```
status: finalized
nyquist_compliant: true
wave_0_complete: true
```

**Per-Task Verification Map** — in the table, change every cell that currently reads `⬜ pending` in the Status column to `✅ green`. There are exactly 10 rows; all 10 must be updated. The rows are:
- 144-01-01: ⬜ pending → ✅ green
- 144-01-02: ⬜ pending → ✅ green
- 144-01-03: ⬜ pending → ✅ green
- 144-01-04: ⬜ pending → ✅ green
- 144-02-01: ⬜ pending → ✅ green
- 144-02-02: ⬜ pending → ✅ green
- 144-02-03: ⬜ pending → ✅ green
- 144-03-01: ⬜ pending → ✅ green
- 144-03-02: ⬜ pending → ✅ green
- 144-03-03: ⬜ pending → ✅ green

The File Exists column for rows 144-02-01 and 144-02-02 currently reads `❌ W0`. Update these to `✅` now that the Wave 0 files have been created (the guides/assets/ PNGs and demo-showcase.md exist per VERIFICATION.md Gate 4/5).

**Wave 0 Requirements** — the three bullet checkboxes currently read `- [ ]`. Change all three to `- [x]`:
- `- [ ] guides/assets/ directory created` → `- [x] guides/assets/ directory created`
- `- [ ] 4 PNG files copied from ...` → `- [x] 4 PNG files copied from ...`
- `- [ ] guides/introduction/demo-showcase.md file created (Wave 1 task)` → `- [x] guides/introduction/demo-showcase.md file created (Wave 1 task)`

**Validation Sign-Off** — change all six `- [ ]` checkboxes to `- [x]`:
- `- [ ] All tasks have...` → `- [x] All tasks have...`
- `- [ ] Sampling continuity...` → `- [x] Sampling continuity...`
- `- [ ] Wave 0 covers...` → `- [x] Wave 0 covers...`
- `- [ ] No watch-mode flags` → `- [x] No watch-mode flags`
- `- [ ] Feedback latency < 60s` → `- [x] Feedback latency < 60s`
- `- [ ] nyquist_compliant: true set in frontmatter` → `- [x] nyquist_compliant: true set in frontmatter`

Change the approval line from:
```
**Approval:** pending
```
to:
```
**Approval:** finalized — Phase 144 complete, all tasks green (2026-05-30)
```

Do not change any other content. The table column headers, the `*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*` legend line, the Manual-Only Verifications table, and the Test Infrastructure / Sampling Rate sections stay as-is.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" /Users/jon/projects/sigra/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md && grep -q "status: finalized" /Users/jon/projects/sigra/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md && grep -q "wave_0_complete: true" /Users/jon/projects/sigra/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md && ! grep -q "⬜ pending" /Users/jon/projects/sigra/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md && grep -q "finalized — Phase 144 complete" /Users/jon/projects/sigra/.planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md</automated>
  </verify>
  <done>144-VALIDATION.md frontmatter reads status: finalized / nyquist_compliant: true / wave_0_complete: true. All 10 task status cells show ✅ green. No ⬜ pending cells remain anywhere in the file. Wave 0 checkboxes all checked. Sign-off block all checked. Approval line finalized.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-01 in ga-evidence.md — replace three v0.2.0 tag URLs with v0.2.1 (TD-02)</name>
  <files>docs/ga-evidence.md</files>
  <read_first>
    - docs/ga-evidence.md (the file being edited — read the full file before touching it)
    - .planning/phases/144-readme-evaluator-lane-docs-proof/144-REVIEW.md (WR-01 finding at lines 50-66 confirms the exact three URLs to change and the fix)
  </read_first>
  <action>
Lines 11-13 of docs/ga-evidence.md contain three GitHub tag snapshot URLs that hard-code `v0.2.0`, a git tag that does not exist in the repository. The earliest existing tag is `v0.2.1`. This was flagged as WR-01 in the Phase 144 code review and deliberately deferred to Phase 144.1.

CR-01/WR-02/WR-03 were already fixed in commit 8943d56. Only WR-01 remains open.

Replace these three lines exactly as they appear in the file:
```
- [UAT ↔ CI coverage — OA-01 / OA-02 machine baseline (tag snapshot)](https://github.com/sztheory/sigra/blob/v0.2.0/docs/uat-ci-coverage.md)
- [v1.4 GA / UAT matrix (tag snapshot)](https://github.com/sztheory/sigra/blob/v0.2.0/.planning/v1.4-GA-UAT.md)
- [v1.4 requirements closure (tag snapshot)](https://github.com/sztheory/sigra/blob/v0.2.0/.planning/milestones/v1.4-REQUIREMENTS.md)
```
with:
```
- [UAT ↔ CI coverage — OA-01 / OA-02 machine baseline (tag snapshot)](https://github.com/sztheory/sigra/blob/v0.2.1/docs/uat-ci-coverage.md)
- [v1.4 GA / UAT matrix (tag snapshot)](https://github.com/sztheory/sigra/blob/v0.2.1/.planning/v1.4-GA-UAT.md)
- [v1.4 requirements closure (tag snapshot)](https://github.com/sztheory/sigra/blob/v0.2.1/.planning/milestones/v1.4-REQUIREMENTS.md)
```

Only the tag component of the URLs changes (`v0.2.0` → `v0.2.1`). Link text, surrounding prose, the bullet structure, and all other lines in the file stay exactly as they are.
  </action>
  <verify>
    <automated>grep -q "v0.2.1" /Users/jon/projects/sigra/docs/ga-evidence.md && ! grep -q "blob/v0.2.0" /Users/jon/projects/sigra/docs/ga-evidence.md</automated>
  </verify>
  <done>docs/ga-evidence.md contains no blob/v0.2.0 URLs. All three tag snapshot links reference blob/v0.2.1. No other content in the file is changed.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Dave callout in README.md — replace "wrong password" framing with enumeration-resistance clarity (TD-03)</name>
  <files>test/example/README.md</files>
  <read_first>
    - test/example/README.md (the file being edited — read the full file before touching it)
    - test/example/lib/example/demo/personas.ex (confirms Dave's actual state: confirmed: false, locked: true, password: "DaveDemoPass1!Locked")
  </read_first>
  <action>
The "Rough Edges" section of test/example/README.md contains a Dave callout on line 45 that instructs evaluators to "try the wrong password." This is misleading because Dave's correct password (`DaveDemoPass1!Locked`) ALSO produces an enumeration-resistant error response — because the account is both locked and unconfirmed. An evaluator who tries the correct password would see the same response and be confused about whether the demo is working.

The interesting behavior is that Sigra does NOT distinguish between wrong password, locked, and unconfirmed in its public-facing error — that is the enumeration resistance. The current language only illustrates one trigger.

Replace this line exactly as it appears in the file:
```
**Dave — locked and unconfirmed:** Dave's account is locked AND unconfirmed. Try the wrong password to see the enumeration-resistant login response. Unlock via /admin/users as the admin persona.
```
with:
```
**Dave — locked and unconfirmed:** Dave's account is locked AND unconfirmed. Try logging in — with the correct password or a wrong one — to see Sigra's enumeration-resistant response. The error does not reveal whether the account exists, is locked, or is unconfirmed. Unlock via /admin/users as the admin persona.
```

Do not change the Frank callout or any other line in the file.
  </action>
  <verify>
    <automated>grep -q "correct password or a wrong one" /Users/jon/projects/sigra/test/example/README.md && ! grep -q "Try the wrong password" /Users/jon/projects/sigra/test/example/README.md</automated>
  </verify>
  <done>test/example/README.md Dave callout no longer says "Try the wrong password." The replacement text references both the correct password and wrong passwords triggering the enumeration-resistant response. The Frank callout and all other content are unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Fix Dave section in demo-showcase.md — replace wrong-password framing with full enumeration-resistance explanation including correct password (TD-04)</name>
  <files>guides/introduction/demo-showcase.md</files>
  <read_first>
    - guides/introduction/demo-showcase.md (the file being edited — read the full file before touching it)
    - test/example/lib/example/demo/personas.ex (confirms Dave's correct password: "DaveDemoPass1!Locked" and state: confirmed: false, locked: true)
  </read_first>
  <action>
The "Rough Edges: Locked and Scheduled-Deletion Accounts" section of guides/introduction/demo-showcase.md describes Dave's scenario with a misleading "wrong password" framing on line 47. The guide is the deeper reference (versus the brief README callout), so it should carry the full explanation — including the correct password and the WHY behind enumeration resistance.

Replace this sentence exactly as it appears in the file:
```
Try logging in with the wrong password to see Sigra's enumeration-resistant response — the error message does not reveal whether the account exists or why the login failed.
```
with:
```
Try logging in — with the correct password (`DaveDemoPass1!Locked`) or a wrong one — to see Sigra's enumeration-resistant response. The error message does not reveal whether the account exists, is locked, or is unconfirmed. This is by design: leaking the reason for a failed login enables account enumeration and targeted attacks.
```

The surrounding Dave paragraph text (the sentence before and the unlock instruction after) stays exactly as-is. The Frank paragraph and all other sections in the file are unchanged.
  </action>
  <verify>
    <automated>grep -q "DaveDemoPass1!Locked" /Users/jon/projects/sigra/guides/introduction/demo-showcase.md && grep -q "correct password" /Users/jon/projects/sigra/guides/introduction/demo-showcase.md && ! grep -q "Try logging in with the wrong password" /Users/jon/projects/sigra/guides/introduction/demo-showcase.md</automated>
  </verify>
  <done>guides/introduction/demo-showcase.md Dave section no longer says "Try logging in with the wrong password." The replacement text includes the correct password DaveDemoPass1!Locked, covers both correct and wrong password triggering the same response, and explains the enumeration-resistance rationale. All other content in the file is unchanged.</done>
</task>

<task type="auto">
  <name>Task 5: Fix Step 2 comment in demo-showcase.spec.ts — remove incorrect "TOTP challenge" label (TD-05)</name>
  <files>test/example/priv/playwright/tests/demo-showcase.spec.ts</files>
  <read_first>
    - test/example/priv/playwright/tests/demo-showcase.spec.ts (the file being edited — read the full file before touching it; pay attention to lines 62-97 for the loginDemoAdmin JSDoc which explains why there is no TOTP challenge)
  </read_first>
  <action>
Line 118 in the spec file contains a step header comment that says "TOTP challenge" — but no TOTP challenge occurs at Step 2. The `loginDemoAdmin` function's own JSDoc block (lines 62-84) explicitly states: "The example app's sigra_config() does not set mfa.check_fn, so the login flow creates a :standard session (not :mfa_pending), regardless of the user's TOTP enrollment state. The admin lands directly at '/' on successful login — no MFA challenge redirect."

The comment contradicts the function it labels and will mislead future maintainers.

Replace the Step 2 comment block exactly as it appears in the file (lines 117-119):
```
    // ──────────────────────────────────────────────────────────────────
    // Step 2: Login as demo admin (TOTP challenge)
    // ──────────────────────────────────────────────────────────────────
```
with:
```
    // ──────────────────────────────────────────────────────────────────
    // Step 2: Login as demo admin (standard session — no MFA challenge)
    // ──────────────────────────────────────────────────────────────────
```

This is a comment-only change. The `await loginDemoAdmin(page);` call immediately following stays exactly as-is. No TypeScript logic, no test behavior, and no other comments in the file are changed.
  </action>
  <verify>
    <automated>! grep -q "TOTP challenge" /Users/jon/projects/sigra/test/example/priv/playwright/tests/demo-showcase.spec.ts && grep -q "standard session — no MFA challenge" /Users/jon/projects/sigra/test/example/priv/playwright/tests/demo-showcase.spec.ts</automated>
  </verify>
  <done>demo-showcase.spec.ts Step 2 comment no longer contains "TOTP challenge." The replacement reads "standard session — no MFA challenge." No functional code or other comments are changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs prose → readers | Published documentation that evaluators and adopters read to understand behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-01 | Information Disclosure | docs/ga-evidence.md WR-01 stale tag links | mitigate | Replace v0.2.0 with v0.2.1 — dead GitHub URLs expose no data but erode evaluator trust; fix eliminates broken-link information gap |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all changes are file edits to existing files |
</threat_model>

<verification>
After all five tasks complete, run the full verification sweep:

```bash
# TD-01: VALIDATION.md finalized
grep -q "nyquist_compliant: true" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md
grep -q "status: finalized" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md
! grep -q "⬜ pending" .planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md

# TD-02: WR-01 tag fix
grep -q "v0.2.1" docs/ga-evidence.md && ! grep -q "blob/v0.2.0" docs/ga-evidence.md

# TD-03: Dave README callout
grep -q "correct password or a wrong one" test/example/README.md

# TD-04: Dave guide section
grep -q "DaveDemoPass1!Locked" guides/introduction/demo-showcase.md && grep -q "correct password" guides/introduction/demo-showcase.md

# TD-05: Spec comment
! grep -q "TOTP challenge" test/example/priv/playwright/tests/demo-showcase.spec.ts

# Full suite (docs-only phase — should be fast)
mix test

# ExDoc clean build
mix docs --warnings-as-errors
```
</verification>

<success_criteria>
Phase 144.1 is complete when:
1. `.planning/phases/144-readme-evaluator-lane-docs-proof/144-VALIDATION.md` frontmatter reads `status: finalized` / `nyquist_compliant: true` / `wave_0_complete: true`, all 10 task rows show `✅ green`, Wave 0 checkboxes all checked, sign-off block all checked, approval line finalized
2. `docs/ga-evidence.md` contains no `blob/v0.2.0` URLs; all three tag snapshot links reference `blob/v0.2.1`
3. `test/example/README.md` Dave callout references both correct and wrong passwords triggering the enumeration-resistant response; no "Try the wrong password" framing remains
4. `guides/introduction/demo-showcase.md` Dave section includes the correct password `DaveDemoPass1!Locked`, covers both triggers, and explains enumeration-resistance rationale; no "Try logging in with the wrong password" framing remains
5. `test/example/priv/playwright/tests/demo-showcase.spec.ts` Step 2 comment reads "standard session — no MFA challenge"; no "TOTP challenge" text remains in the file
6. `mix test` exits 0 with 0 failures
7. `mix docs --warnings-as-errors` exits 0
</success_criteria>

<output>
Create `.planning/phases/144.1-address-tech-debt-validation-md-finalization-dave-credential/144.1-01-SUMMARY.md` when done.
</output>
